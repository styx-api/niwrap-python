# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INFLATE_SUBJECT_METADATA = Metadata(
    id="inflate_subject",
    name="inflate_subject",
    package="freesurfer",
)

InflateSubject = typing.TypedDict(
    "InflateSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/inflate_subject"]],
        "args": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class InflateSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output of inflate_subject command"""

def inflate_subject_params(
    args: str | None = None,
) -> InflateSubject:
    """
    Build parameters.

    Args:
        args: Arguments for the inflate_subject command

    Returns:
        Parameter dictionary.
    """
    params: InflateSubject = {
        "@type": "freesurfer/inflate_subject",
    }
    if args is not None:
        params["args"] = args
    return params

def inflate_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid InflateSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("args", None) is not None:
        if not isinstance(params["args"], str):
            raise StyxValidationError(f'`args` has the wrong type: Received `{type(params["args"])}` expected `str`')

def inflate_subject_cargs(params: InflateSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("inflate_subject")
    v_0 = params.get("args")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def inflate_subject_outputs(params: InflateSubject, execution: Execution) -> InflateSubjectOutputs:
    """Build the InflateSubjectOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("args")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_output.txt')
    return InflateSubjectOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def inflate_subject_execute(params: InflateSubject, runner: Runner | None = None) -> InflateSubjectOutputs:
    """
    inflate_subject

    Inflate subject script for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    inflate_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INFLATE_SUBJECT_METADATA)
    execution.params(params)
    args = inflate_subject_cargs(params, execution)
    out = inflate_subject_outputs(params, execution)
    execution.run(args)
    return out

def inflate_subject(
    args: str | None = None,
    runner: Runner | None = None,
) -> InflateSubjectOutputs:
    """
    inflate_subject

    Inflate subject script for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        args: Arguments for the inflate_subject command
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = inflate_subject_params(
        args=args,
    )
    return inflate_subject_execute(params, runner)

__all__ = [
    "InflateSubject",
    "InflateSubjectOutputs",
    "INFLATE_SUBJECT_METADATA",
    "inflate_subject_cargs",
    "inflate_subject_outputs",
    "inflate_subject_params",
    "inflate_subject_execute",
    "inflate_subject_validate",
    "inflate_subject",
]