# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONGMC_METADATA = Metadata(
    id="longmc",
    name="longmc",
    package="freesurfer",
)

Longmc = typing.TypedDict(
    "Longmc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/longmc"]],
        "cross_tp_name": str,
        "base_name": str,
        "conform_to_hires": typing.NotRequired[bool],
        "no_conform_to_hires": typing.NotRequired[bool],
        "subjects_dir": str,
        "subject_name": typing.NotRequired[str],
        "no_force_update": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LongmcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def longmc_params(
    cross_tp_name: str,
    base_name: str,
    subjects_dir: str,
    conform_to_hires: bool = False,
    no_conform_to_hires: bool = False,
    subject_name: str | None = None,
    no_force_update: bool = False,
) -> Longmc:
    """
    Build parameters.

    Args:
        cross_tp_name: Cross time point name for the longitudinal analysis
        base_name: Base name for the longitudinal analysis
        subjects_dir: Set the SUBJECTS_DIR directory
        conform_to_hires: Option to conform input to high-resolution
        no_conform_to_hires: Option to not conform input to high-resolution
        subject_name: Subject name override, must be declared after -long
        no_force_update: Do not force update

    Returns:
        Parameter dictionary.
    """
    params: Longmc = {
        "@type": "freesurfer/longmc",
        "cross_tp_name": cross_tp_name,
        "base_name": base_name,
        "subjects_dir": subjects_dir,
        "conform_to_hires": conform_to_hires,
        "no_conform_to_hires": no_conform_to_hires,
        "no_force_update": no_force_update,
    }
    if subject_name is not None:
        params["subject_name"] = subject_name
    return params

def longmc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Longmc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cross_tp_name", None) is None:
        raise StyxValidationError("`cross_tp_name` must not be None")
    if not isinstance(params["cross_tp_name"], str):
        raise StyxValidationError(f'`cross_tp_name` has the wrong type: Received `{type(params["cross_tp_name"])}` expected `str`')
    if params.get("base_name", None) is None:
        raise StyxValidationError("`base_name` must not be None")
    if not isinstance(params["base_name"], str):
        raise StyxValidationError(f'`base_name` has the wrong type: Received `{type(params["base_name"])}` expected `str`')
    if params.get("conform_to_hires", None) is not None:
        if not isinstance(params["conform_to_hires"], bool):
            raise StyxValidationError(f'`conform_to_hires` has the wrong type: Received `{type(params["conform_to_hires"])}` expected `bool`')
    if params.get("no_conform_to_hires", None) is not None:
        if not isinstance(params["no_conform_to_hires"], bool):
            raise StyxValidationError(f'`no_conform_to_hires` has the wrong type: Received `{type(params["no_conform_to_hires"])}` expected `bool`')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("subject_name", None) is not None:
        if not isinstance(params["subject_name"], str):
            raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("no_force_update", None) is not None:
        if not isinstance(params["no_force_update"], bool):
            raise StyxValidationError(f'`no_force_update` has the wrong type: Received `{type(params["no_force_update"])}` expected `bool`')

def longmc_cargs(params: Longmc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("longmc")
    cargs.append("-long")
    cargs.append(params["cross_tp_name"])
    cargs.append(params["base_name"])
    if params.get("conform_to_hires"):
        cargs.append("-conf2hires")
    if params.get("no_conform_to_hires"):
        cargs.append("-no-conf2hires")
    cargs.append("-sd")
    cargs.append(params["subjects_dir"])
    v_0 = params.get("subject_name")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(v_0)
    if params.get("no_force_update"):
        cargs.append("-no-force-update")
    return cargs

def longmc_outputs(params: Longmc, execution: Execution) -> LongmcOutputs:
    """Build the LongmcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LongmcOutputs(
        root=root_v,
    )

def longmc_execute(params: Longmc, runner: Runner | None = None) -> LongmcOutputs:
    """
    longmc

    Perform motion correction for the longitudinal recon-all stream when creating the longitudinal timepoint of a subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    longmc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONGMC_METADATA)
    execution.params(params)
    args = longmc_cargs(params, execution)
    out = longmc_outputs(params, execution)
    execution.run(args)
    return out

def longmc(
    cross_tp_name: str,
    base_name: str,
    subjects_dir: str,
    conform_to_hires: bool = False,
    no_conform_to_hires: bool = False,
    subject_name: str | None = None,
    no_force_update: bool = False,
    runner: Runner | None = None,
) -> LongmcOutputs:
    """
    longmc

    Perform motion correction for the longitudinal recon-all stream when creating the longitudinal timepoint of a subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        cross_tp_name: Cross time point name for the longitudinal analysis
        base_name: Base name for the longitudinal analysis
        subjects_dir: Set the SUBJECTS_DIR directory
        conform_to_hires: Option to conform input to high-resolution
        no_conform_to_hires: Option to not conform input to high-resolution
        subject_name: Subject name override, must be declared after -long
        no_force_update: Do not force update
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = longmc_params(
        cross_tp_name=cross_tp_name,
        base_name=base_name,
        subjects_dir=subjects_dir,
        conform_to_hires=conform_to_hires,
        no_conform_to_hires=no_conform_to_hires,
        subject_name=subject_name,
        no_force_update=no_force_update,
    )
    return longmc_execute(params, runner)

__all__ = [
    "Longmc",
    "LongmcOutputs",
    "LONGMC_METADATA",
    "longmc_cargs",
    "longmc_outputs",
    "longmc_params",
    "longmc_execute",
    "longmc_validate",
    "longmc",
]