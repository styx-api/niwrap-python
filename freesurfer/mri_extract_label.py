# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EXTRACT_LABEL_METADATA = Metadata(
    id="mri_extract_label",
    name="mri_extract_label",
    package="freesurfer",
)

MriExtractLabel = typing.TypedDict(
    "MriExtractLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_extract_label"]],
        "input_volume": InputPathType,
        "labels": list[str],
        "output_name": str,
        "gaussian_smoothing": typing.NotRequired[float],
        "transform_file": typing.NotRequired[InputPathType],
        "exit_none_found": typing.NotRequired[bool],
        "dilate": typing.NotRequired[float],
        "erode": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriExtractLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Extracted labeled voxels output volume."""

def mri_extract_label_params(
    input_volume: InputPathType,
    labels: list[str],
    output_name: str,
    gaussian_smoothing: float | None = None,
    transform_file: InputPathType | None = None,
    exit_none_found: bool = False,
    dilate: float | None = None,
    erode: float | None = None,
) -> MriExtractLabel:
    """
    Build parameters.

    Args:
        input_volume: Input volume from which to extract labels.
        labels: Labels to extract. Can include one or more labels.
        output_name: Name of the output file.
        gaussian_smoothing: Apply a Gaussian smoothing kernel with sigma.
        transform_file: Apply the transform in xform file to the extracted volume.
        exit_none_found: Exit with error if none of the specified labels are found.
        dilate: Dilate the output volume n times.
        erode: Erode the output volume n times.

    Returns:
        Parameter dictionary.
    """
    params: MriExtractLabel = {
        "@type": "freesurfer/mri_extract_label",
        "input_volume": input_volume,
        "labels": labels,
        "output_name": output_name,
        "exit_none_found": exit_none_found,
    }
    if gaussian_smoothing is not None:
        params["gaussian_smoothing"] = gaussian_smoothing
    if transform_file is not None:
        params["transform_file"] = transform_file
    if dilate is not None:
        params["dilate"] = dilate
    if erode is not None:
        params["erode"] = erode
    return params

def mri_extract_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriExtractLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("gaussian_smoothing", None) is not None:
        if not isinstance(params["gaussian_smoothing"], (float, int)):
            raise StyxValidationError(f'`gaussian_smoothing` has the wrong type: Received `{type(params["gaussian_smoothing"])}` expected `float`')
    if params.get("transform_file", None) is not None:
        if not isinstance(params["transform_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("exit_none_found", None) is not None:
        if not isinstance(params["exit_none_found"], bool):
            raise StyxValidationError(f'`exit_none_found` has the wrong type: Received `{type(params["exit_none_found"])}` expected `bool`')
    if params.get("dilate", None) is not None:
        if not isinstance(params["dilate"], (float, int)):
            raise StyxValidationError(f'`dilate` has the wrong type: Received `{type(params["dilate"])}` expected `float`')
    if params.get("erode", None) is not None:
        if not isinstance(params["erode"], (float, int)):
            raise StyxValidationError(f'`erode` has the wrong type: Received `{type(params["erode"])}` expected `float`')

def mri_extract_label_cargs(params: MriExtractLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_extract_label")
    cargs.append(execution.input_file(params["input_volume"]))
    for item0 in params["labels"]:
        cargs.append(item0)
    cargs.append(params["output_name"])
    v_0 = params.get("gaussian_smoothing")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(str(v_0))
    v_1 = params.get("transform_file")
    if v_1 is not None:
        cargs.append("-t")
        cargs.append(execution.input_file(v_1))
    if params.get("exit_none_found"):
        cargs.append("-exit_none_found")
    v_2 = params.get("dilate")
    if v_2 is not None:
        cargs.append("-dilate")
        cargs.append(str(v_2))
    v_3 = params.get("erode")
    if v_3 is not None:
        cargs.append("-erode")
        cargs.append(str(v_3))
    return cargs

def mri_extract_label_outputs(params: MriExtractLabel, execution: Execution) -> MriExtractLabelOutputs:
    """Build the MriExtractLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["output_name"])
    return MriExtractLabelOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def mri_extract_label_execute(params: MriExtractLabel, runner: Runner | None = None) -> MriExtractLabelOutputs:
    """
    mri_extract_label

    Extracts a set of labeled voxels from an image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_extract_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EXTRACT_LABEL_METADATA)
    execution.params(params)
    args = mri_extract_label_cargs(params, execution)
    out = mri_extract_label_outputs(params, execution)
    execution.run(args)
    return out

def mri_extract_label(
    input_volume: InputPathType,
    labels: list[str],
    output_name: str,
    gaussian_smoothing: float | None = None,
    transform_file: InputPathType | None = None,
    exit_none_found: bool = False,
    dilate: float | None = None,
    erode: float | None = None,
    runner: Runner | None = None,
) -> MriExtractLabelOutputs:
    """
    mri_extract_label

    Extracts a set of labeled voxels from an image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume from which to extract labels.
        labels: Labels to extract. Can include one or more labels.
        output_name: Name of the output file.
        gaussian_smoothing: Apply a Gaussian smoothing kernel with sigma.
        transform_file: Apply the transform in xform file to the extracted volume.
        exit_none_found: Exit with error if none of the specified labels are found.
        dilate: Dilate the output volume n times.
        erode: Erode the output volume n times.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_extract_label_params(
        input_volume=input_volume,
        labels=labels,
        output_name=output_name,
        gaussian_smoothing=gaussian_smoothing,
        transform_file=transform_file,
        exit_none_found=exit_none_found,
        dilate=dilate,
        erode=erode,
    )
    return mri_extract_label_execute(params, runner)

__all__ = [
    "MriExtractLabel",
    "MriExtractLabelOutputs",
    "MRI_EXTRACT_LABEL_METADATA",
    "mri_extract_label_cargs",
    "mri_extract_label_outputs",
    "mri_extract_label_params",
    "mri_extract_label_execute",
    "mri_extract_label_validate",
    "mri_extract_label",
]