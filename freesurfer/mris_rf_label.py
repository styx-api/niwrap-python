# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_RF_LABEL_METADATA = Metadata(
    id="mris_rf_label",
    name="mris_rf_label",
    package="freesurfer",
)

MrisRfLabel = typing.TypedDict(
    "MrisRfLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_rf_label"]],
        "subject": str,
        "rf_classifier": str,
        "output_name": str,
        "hemi": typing.NotRequired[str],
        "surf": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisRfLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_rf_label_params(
    subject: str,
    rf_classifier: str,
    output_name: str,
    hemi: str | None = None,
    surf: str | None = None,
) -> MrisRfLabel:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        rf_classifier: Random Forest classifier
        output_name: Output name
        hemi: Process specified hemisphere instead of default (lh).
        surf: Change default surface name from 'white' to specified surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisRfLabel = {
        "@type": "freesurfer/mris_rf_label",
        "subject": subject,
        "rf_classifier": rf_classifier,
        "output_name": output_name,
    }
    if hemi is not None:
        params["hemi"] = hemi
    if surf is not None:
        params["surf"] = surf
    return params

def mris_rf_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRfLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("rf_classifier", None) is None:
        raise StyxValidationError("`rf_classifier` must not be None")
    if not isinstance(params["rf_classifier"], str):
        raise StyxValidationError(f'`rf_classifier` has the wrong type: Received `{type(params["rf_classifier"])}` expected `str`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')

def mris_rf_label_cargs(params: MrisRfLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_rf_label")
    cargs.append(params["subject"])
    cargs.append(params["rf_classifier"])
    cargs.append(params["output_name"])
    v_0 = params.get("hemi")
    if v_0 is not None:
        cargs.append("--hemi")
        cargs.append(v_0)
    v_1 = params.get("surf")
    if v_1 is not None:
        cargs.append("--surf")
        cargs.append(v_1)
    return cargs

def mris_rf_label_outputs(params: MrisRfLabel, execution: Execution) -> MrisRfLabelOutputs:
    """Build the MrisRfLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisRfLabelOutputs(
        root=root_v,
    )

def mris_rf_label_execute(params: MrisRfLabel, runner: Runner | None = None) -> MrisRfLabelOutputs:
    """
    mris_rf_label

    A tool for labeling regions of a surface using random forest classification.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_rf_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_RF_LABEL_METADATA)
    execution.params(params)
    args = mris_rf_label_cargs(params, execution)
    out = mris_rf_label_outputs(params, execution)
    execution.run(args)
    return out

def mris_rf_label(
    subject: str,
    rf_classifier: str,
    output_name: str,
    hemi: str | None = None,
    surf: str | None = None,
    runner: Runner | None = None,
) -> MrisRfLabelOutputs:
    """
    mris_rf_label

    A tool for labeling regions of a surface using random forest classification.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        rf_classifier: Random Forest classifier
        output_name: Output name
        hemi: Process specified hemisphere instead of default (lh).
        surf: Change default surface name from 'white' to specified surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_rf_label_params(
        subject=subject,
        rf_classifier=rf_classifier,
        output_name=output_name,
        hemi=hemi,
        surf=surf,
    )
    return mris_rf_label_execute(params, runner)

__all__ = [
    "MrisRfLabel",
    "MrisRfLabelOutputs",
    "MRIS_RF_LABEL_METADATA",
    "mris_rf_label_cargs",
    "mris_rf_label_outputs",
    "mris_rf_label_params",
    "mris_rf_label_execute",
    "mris_rf_label_validate",
    "mris_rf_label",
]