# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_SEG_OVERLAP_METADATA = Metadata(
    id="mri_compute_seg_overlap",
    name="mri_compute_seg_overlap",
    package="freesurfer",
)

MriComputeSegOverlap = typing.TypedDict(
    "MriComputeSegOverlap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_seg_overlap"]],
        "segvol1": InputPathType,
        "segvol2": InputPathType,
        "log_file": typing.NotRequired[str],
        "mean_log_file": typing.NotRequired[str],
        "std_log_file": typing.NotRequired[str],
        "overall_log_flag": typing.NotRequired[bool],
        "exclude_cortex_flag": typing.NotRequired[bool],
        "exclude_wm_flag": typing.NotRequired[bool],
        "all_labels_flag": typing.NotRequired[bool],
        "dice_params": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriComputeSegOverlapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_compute_seg_overlap_params(
    segvol1: InputPathType,
    segvol2: InputPathType,
    log_file: str | None = None,
    mean_log_file: str | None = None,
    std_log_file: str | None = None,
    overall_log_flag: bool = False,
    exclude_cortex_flag: bool = False,
    exclude_wm_flag: bool = False,
    all_labels_flag: bool = False,
    dice_params: str | None = None,
) -> MriComputeSegOverlap:
    """
    Build parameters.

    Args:
        segvol1: First segmentation volume.
        segvol2: Second segmentation volume.
        log_file: Log file for individual Dice coefficients for 12 structure pairs,\
            plus mean, std, and 'overall'.
        mean_log_file: Log file for mean Dice.
        std_log_file: Log file for std Dice.
        overall_log_flag: Log file for 'overall' Dice (mean excluding wm, gm, and\
            accumbens).
        exclude_cortex_flag: Exclude cerebral cortex labels from all calculation.\
            (0/1 flag, if nonzero)
        exclude_wm_flag: Exclude cerebral white matter labels from all calculation.\
            (0/1 flag, if nonzero)
        all_labels_flag: Check all labels, not just the predefined main structures.
        dice_params: Standalone way to compute Dice coefficients, using seg1, seg2,\
            ctab, ReportEmpty01, ExcludeId, datfile, and tablefile as additional\
            parameters.

    Returns:
        Parameter dictionary.
    """
    params: MriComputeSegOverlap = {
        "@type": "freesurfer/mri_compute_seg_overlap",
        "segvol1": segvol1,
        "segvol2": segvol2,
        "overall_log_flag": overall_log_flag,
        "exclude_cortex_flag": exclude_cortex_flag,
        "exclude_wm_flag": exclude_wm_flag,
        "all_labels_flag": all_labels_flag,
    }
    if log_file is not None:
        params["log_file"] = log_file
    if mean_log_file is not None:
        params["mean_log_file"] = mean_log_file
    if std_log_file is not None:
        params["std_log_file"] = std_log_file
    if dice_params is not None:
        params["dice_params"] = dice_params
    return params

def mri_compute_seg_overlap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeSegOverlap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("segvol1", None) is None:
        raise StyxValidationError("`segvol1` must not be None")
    if not isinstance(params["segvol1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segvol1` has the wrong type: Received `{type(params["segvol1"])}` expected `InputPathType`')
    if params.get("segvol2", None) is None:
        raise StyxValidationError("`segvol2` must not be None")
    if not isinstance(params["segvol2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segvol2` has the wrong type: Received `{type(params["segvol2"])}` expected `InputPathType`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("mean_log_file", None) is not None:
        if not isinstance(params["mean_log_file"], str):
            raise StyxValidationError(f'`mean_log_file` has the wrong type: Received `{type(params["mean_log_file"])}` expected `str`')
    if params.get("std_log_file", None) is not None:
        if not isinstance(params["std_log_file"], str):
            raise StyxValidationError(f'`std_log_file` has the wrong type: Received `{type(params["std_log_file"])}` expected `str`')
    if params.get("overall_log_flag", None) is not None:
        if not isinstance(params["overall_log_flag"], bool):
            raise StyxValidationError(f'`overall_log_flag` has the wrong type: Received `{type(params["overall_log_flag"])}` expected `bool`')
    if params.get("exclude_cortex_flag", None) is not None:
        if not isinstance(params["exclude_cortex_flag"], bool):
            raise StyxValidationError(f'`exclude_cortex_flag` has the wrong type: Received `{type(params["exclude_cortex_flag"])}` expected `bool`')
    if params.get("exclude_wm_flag", None) is not None:
        if not isinstance(params["exclude_wm_flag"], bool):
            raise StyxValidationError(f'`exclude_wm_flag` has the wrong type: Received `{type(params["exclude_wm_flag"])}` expected `bool`')
    if params.get("all_labels_flag", None) is not None:
        if not isinstance(params["all_labels_flag"], bool):
            raise StyxValidationError(f'`all_labels_flag` has the wrong type: Received `{type(params["all_labels_flag"])}` expected `bool`')
    if params.get("dice_params", None) is not None:
        if not isinstance(params["dice_params"], str):
            raise StyxValidationError(f'`dice_params` has the wrong type: Received `{type(params["dice_params"])}` expected `str`')

def mri_compute_seg_overlap_cargs(params: MriComputeSegOverlap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_seg_overlap")
    cargs.append(execution.input_file(params["segvol1"]))
    cargs.append(execution.input_file(params["segvol2"]))
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-log")
        cargs.append(v_0)
    v_1 = params.get("mean_log_file")
    if v_1 is not None:
        cargs.append("-mlog")
        cargs.append(v_1)
    v_2 = params.get("std_log_file")
    if v_2 is not None:
        cargs.append("-slog")
        cargs.append(v_2)
    if params.get("overall_log_flag"):
        cargs.append("-olog")
    if params.get("exclude_cortex_flag"):
        cargs.append("-cortex")
    if params.get("exclude_wm_flag"):
        cargs.append("-wm")
    if params.get("all_labels_flag"):
        cargs.append("-all_labels")
    v_3 = params.get("dice_params")
    if v_3 is not None:
        cargs.append("-dice")
        cargs.append(v_3)
    return cargs

def mri_compute_seg_overlap_outputs(params: MriComputeSegOverlap, execution: Execution) -> MriComputeSegOverlapOutputs:
    """Build the MriComputeSegOverlapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriComputeSegOverlapOutputs(
        root=root_v,
    )

def mri_compute_seg_overlap_execute(params: MriComputeSegOverlap, runner: Runner | None = None) -> MriComputeSegOverlapOutputs:
    """
    mri_compute_seg_overlap

    Compute coefficients of overlap between segmentation volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_seg_overlap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_SEG_OVERLAP_METADATA)
    execution.params(params)
    args = mri_compute_seg_overlap_cargs(params, execution)
    out = mri_compute_seg_overlap_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_seg_overlap(
    segvol1: InputPathType,
    segvol2: InputPathType,
    log_file: str | None = None,
    mean_log_file: str | None = None,
    std_log_file: str | None = None,
    overall_log_flag: bool = False,
    exclude_cortex_flag: bool = False,
    exclude_wm_flag: bool = False,
    all_labels_flag: bool = False,
    dice_params: str | None = None,
    runner: Runner | None = None,
) -> MriComputeSegOverlapOutputs:
    """
    mri_compute_seg_overlap

    Compute coefficients of overlap between segmentation volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        segvol1: First segmentation volume.
        segvol2: Second segmentation volume.
        log_file: Log file for individual Dice coefficients for 12 structure pairs,\
            plus mean, std, and 'overall'.
        mean_log_file: Log file for mean Dice.
        std_log_file: Log file for std Dice.
        overall_log_flag: Log file for 'overall' Dice (mean excluding wm, gm, and\
            accumbens).
        exclude_cortex_flag: Exclude cerebral cortex labels from all calculation.\
            (0/1 flag, if nonzero)
        exclude_wm_flag: Exclude cerebral white matter labels from all calculation.\
            (0/1 flag, if nonzero)
        all_labels_flag: Check all labels, not just the predefined main structures.
        dice_params: Standalone way to compute Dice coefficients, using seg1, seg2,\
            ctab, ReportEmpty01, ExcludeId, datfile, and tablefile as additional\
            parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_seg_overlap_params(
        segvol1=segvol1,
        segvol2=segvol2,
        log_file=log_file,
        mean_log_file=mean_log_file,
        std_log_file=std_log_file,
        overall_log_flag=overall_log_flag,
        exclude_cortex_flag=exclude_cortex_flag,
        exclude_wm_flag=exclude_wm_flag,
        all_labels_flag=all_labels_flag,
        dice_params=dice_params,
    )
    return mri_compute_seg_overlap_execute(params, runner)

__all__ = [
    "MriComputeSegOverlap",
    "MriComputeSegOverlapOutputs",
    "MRI_COMPUTE_SEG_OVERLAP_METADATA",
    "mri_compute_seg_overlap_cargs",
    "mri_compute_seg_overlap_outputs",
    "mri_compute_seg_overlap_params",
    "mri_compute_seg_overlap_execute",
    "mri_compute_seg_overlap_validate",
    "mri_compute_seg_overlap",
]