# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_INSTALL_MCR_METADATA = Metadata(
    id="fs_install_mcr",
    name="fs_install_mcr",
    package="freesurfer",
)

FsInstallMcr = typing.TypedDict(
    "FsInstallMcr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_install_mcr"]],
        "mcr_version": str,
    },
)

@dataclasses.dataclass
class FsInstallMcrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fs_install_mcr_params(
    mcr_version: str,
) -> FsInstallMcr:
    """
    Build parameters.

    Args:
        mcr_version: Specify the MATLAB Compiler Runtime (MCR) version to install

    Returns:
        Parameter dictionary.
    """
    params: FsInstallMcr = {
        "@type": "freesurfer/fs_install_mcr",
        "mcr_version": mcr_version,
    }
    return params

def fs_install_mcr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsInstallMcr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mcr_version", None) is None:
        raise StyxValidationError("`mcr_version` must not be None")
    if not isinstance(params["mcr_version"], str):
        raise StyxValidationError(f'`mcr_version` has the wrong type: Received `{type(params["mcr_version"])}` expected `str`')

def fs_install_mcr_cargs(params: FsInstallMcr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_install_mcr")
    cargs.append(params["mcr_version"])
    return cargs

def fs_install_mcr_outputs(params: FsInstallMcr, execution: Execution) -> FsInstallMcrOutputs:
    """Build the FsInstallMcrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsInstallMcrOutputs(
        root=root_v,
    )

def fs_install_mcr_execute(params: FsInstallMcr, runner: Runner | None = None) -> FsInstallMcrOutputs:
    """
    fs_install_mcr

    MCR installation tool for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_install_mcr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_INSTALL_MCR_METADATA)
    execution.params(params)
    args = fs_install_mcr_cargs(params, execution)
    out = fs_install_mcr_outputs(params, execution)
    execution.run(args)
    return out

def fs_install_mcr(
    mcr_version: str,
    runner: Runner | None = None,
) -> FsInstallMcrOutputs:
    """
    fs_install_mcr

    MCR installation tool for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mcr_version: Specify the MATLAB Compiler Runtime (MCR) version to install
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_install_mcr_params(
        mcr_version=mcr_version,
    )
    return fs_install_mcr_execute(params, runner)

__all__ = [
    "FsInstallMcr",
    "FsInstallMcrOutputs",
    "FS_INSTALL_MCR_METADATA",
    "fs_install_mcr_cargs",
    "fs_install_mcr_outputs",
    "fs_install_mcr_params",
    "fs_install_mcr_execute",
    "fs_install_mcr_validate",
    "fs_install_mcr",
]