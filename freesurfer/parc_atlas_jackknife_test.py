# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PARC_ATLAS_JACKKNIFE_TEST_METADATA = Metadata(
    id="parc_atlas_jackknife_test",
    name="parc_atlas_jackknife_test",
    package="freesurfer",
)

ParcAtlasJackknifeTest = typing.TypedDict(
    "ParcAtlasJackknifeTest",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/parc_atlas_jackknife_test"]],
        "register": typing.NotRequired[bool],
        "reg_dist": typing.NotRequired[str],
        "reg_append": typing.NotRequired[str],
        "reg_copy": typing.NotRequired[str],
        "train": typing.NotRequired[bool],
        "classify": typing.NotRequired[bool],
        "test": typing.NotRequired[bool],
        "all": typing.NotRequired[bool],
        "subjects_dir": typing.NotRequired[str],
        "freesurfer_home": typing.NotRequired[str],
        "binaries_path": typing.NotRequired[str],
        "dontrun": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ParcAtlasJackknifeTestOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    jackknife_output: OutputPathType
    """Directory named 'jackknife' containing output files"""

def parc_atlas_jackknife_test_params(
    register: bool = False,
    reg_dist: str | None = None,
    reg_append: str | None = None,
    reg_copy: str | None = None,
    train: bool = False,
    classify: bool = False,
    test: bool = False,
    all: bool = False,
    subjects_dir: str | None = None,
    freesurfer_home: str | None = None,
    binaries_path: str | None = None,
    dontrun: bool = False,
) -> ParcAtlasJackknifeTest:
    """
    Build parameters.

    Args:
        register: Run mris_register: creates .sphere.reg files
        reg_dist: Run mris_register with '-dist <arg>' flag
        reg_append: Append <string> to end of ?h.sphere.reg
        reg_copy: Copy ?h.sphere.reg<string> ?h.sphere.reg
        train: Run mris_ca_train: creates .gcs files
        classify: Run mris_ca_label: creates .annot files
        test: Run mris_compute_parc_overlap
        all: Run train, classify, and test
        subjects_dir: Override default subjects directory
        freesurfer_home: Source a new FREESURFER_HOME
        binaries_path: Specify override path to binaries
        dontrun: Don't execute the commands

    Returns:
        Parameter dictionary.
    """
    params: ParcAtlasJackknifeTest = {
        "@type": "freesurfer/parc_atlas_jackknife_test",
        "register": register,
        "train": train,
        "classify": classify,
        "test": test,
        "all": all,
        "dontrun": dontrun,
    }
    if reg_dist is not None:
        params["reg_dist"] = reg_dist
    if reg_append is not None:
        params["reg_append"] = reg_append
    if reg_copy is not None:
        params["reg_copy"] = reg_copy
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if freesurfer_home is not None:
        params["freesurfer_home"] = freesurfer_home
    if binaries_path is not None:
        params["binaries_path"] = binaries_path
    return params

def parc_atlas_jackknife_test_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ParcAtlasJackknifeTest."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("register", None) is not None:
        if not isinstance(params["register"], bool):
            raise StyxValidationError(f'`register` has the wrong type: Received `{type(params["register"])}` expected `bool`')
    if params.get("reg_dist", None) is not None:
        if not isinstance(params["reg_dist"], str):
            raise StyxValidationError(f'`reg_dist` has the wrong type: Received `{type(params["reg_dist"])}` expected `str`')
    if params.get("reg_append", None) is not None:
        if not isinstance(params["reg_append"], str):
            raise StyxValidationError(f'`reg_append` has the wrong type: Received `{type(params["reg_append"])}` expected `str`')
    if params.get("reg_copy", None) is not None:
        if not isinstance(params["reg_copy"], str):
            raise StyxValidationError(f'`reg_copy` has the wrong type: Received `{type(params["reg_copy"])}` expected `str`')
    if params.get("train", None) is not None:
        if not isinstance(params["train"], bool):
            raise StyxValidationError(f'`train` has the wrong type: Received `{type(params["train"])}` expected `bool`')
    if params.get("classify", None) is not None:
        if not isinstance(params["classify"], bool):
            raise StyxValidationError(f'`classify` has the wrong type: Received `{type(params["classify"])}` expected `bool`')
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')
    if params.get("all", None) is not None:
        if not isinstance(params["all"], bool):
            raise StyxValidationError(f'`all` has the wrong type: Received `{type(params["all"])}` expected `bool`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("freesurfer_home", None) is not None:
        if not isinstance(params["freesurfer_home"], str):
            raise StyxValidationError(f'`freesurfer_home` has the wrong type: Received `{type(params["freesurfer_home"])}` expected `str`')
    if params.get("binaries_path", None) is not None:
        if not isinstance(params["binaries_path"], str):
            raise StyxValidationError(f'`binaries_path` has the wrong type: Received `{type(params["binaries_path"])}` expected `str`')
    if params.get("dontrun", None) is not None:
        if not isinstance(params["dontrun"], bool):
            raise StyxValidationError(f'`dontrun` has the wrong type: Received `{type(params["dontrun"])}` expected `bool`')

def parc_atlas_jackknife_test_cargs(params: ParcAtlasJackknifeTest, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("parc_atlas_jackknife_test")
    if params.get("register"):
        cargs.append("-register")
    v_0 = params.get("reg_dist")
    if v_0 is not None:
        cargs.append("-reg_dist")
        cargs.append(v_0)
    v_1 = params.get("reg_append")
    if v_1 is not None:
        cargs.append("-reg_append")
        cargs.append(v_1)
    v_2 = params.get("reg_copy")
    if v_2 is not None:
        cargs.append("-reg_copy")
        cargs.append(v_2)
    if params.get("train"):
        cargs.append("-train")
    if params.get("classify"):
        cargs.append("-classify")
    if params.get("test"):
        cargs.append("-test")
    if params.get("all"):
        cargs.append("-all")
    v_3 = params.get("subjects_dir")
    if v_3 is not None:
        cargs.append("-sd")
        cargs.append(v_3)
    v_4 = params.get("freesurfer_home")
    if v_4 is not None:
        cargs.append("-fshome")
        cargs.append(v_4)
    v_5 = params.get("binaries_path")
    if v_5 is not None:
        cargs.append("-binhome")
        cargs.append(v_5)
    if params.get("dontrun"):
        cargs.append("-dontrun")
    return cargs

def parc_atlas_jackknife_test_outputs(params: ParcAtlasJackknifeTest, execution: Execution) -> ParcAtlasJackknifeTestOutputs:
    """Build the ParcAtlasJackknifeTestOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    jackknife_output_v: OutputPathType = execution.output_file("jackknife")
    return ParcAtlasJackknifeTestOutputs(
        root=root_v,
        jackknife_output=jackknife_output_v,
    )

def parc_atlas_jackknife_test_execute(params: ParcAtlasJackknifeTest, runner: Runner | None = None) -> ParcAtlasJackknifeTestOutputs:
    """
    parc_atlas_jackknife_test

    Tool for conducting a jackknife accuracy test using FreeSurfer atlases.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    parc_atlas_jackknife_test_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PARC_ATLAS_JACKKNIFE_TEST_METADATA)
    execution.params(params)
    args = parc_atlas_jackknife_test_cargs(params, execution)
    out = parc_atlas_jackknife_test_outputs(params, execution)
    execution.run(args)
    return out

def parc_atlas_jackknife_test(
    register: bool = False,
    reg_dist: str | None = None,
    reg_append: str | None = None,
    reg_copy: str | None = None,
    train: bool = False,
    classify: bool = False,
    test: bool = False,
    all: bool = False,
    subjects_dir: str | None = None,
    freesurfer_home: str | None = None,
    binaries_path: str | None = None,
    dontrun: bool = False,
    runner: Runner | None = None,
) -> ParcAtlasJackknifeTestOutputs:
    """
    parc_atlas_jackknife_test

    Tool for conducting a jackknife accuracy test using FreeSurfer atlases.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        register: Run mris_register: creates .sphere.reg files
        reg_dist: Run mris_register with '-dist <arg>' flag
        reg_append: Append <string> to end of ?h.sphere.reg
        reg_copy: Copy ?h.sphere.reg<string> ?h.sphere.reg
        train: Run mris_ca_train: creates .gcs files
        classify: Run mris_ca_label: creates .annot files
        test: Run mris_compute_parc_overlap
        all: Run train, classify, and test
        subjects_dir: Override default subjects directory
        freesurfer_home: Source a new FREESURFER_HOME
        binaries_path: Specify override path to binaries
        dontrun: Don't execute the commands
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = parc_atlas_jackknife_test_params(
        register=register,
        reg_dist=reg_dist,
        reg_append=reg_append,
        reg_copy=reg_copy,
        train=train,
        classify=classify,
        test=test,
        all=all,
        subjects_dir=subjects_dir,
        freesurfer_home=freesurfer_home,
        binaries_path=binaries_path,
        dontrun=dontrun,
    )
    return parc_atlas_jackknife_test_execute(params, runner)

__all__ = [
    "ParcAtlasJackknifeTest",
    "ParcAtlasJackknifeTestOutputs",
    "PARC_ATLAS_JACKKNIFE_TEST_METADATA",
    "parc_atlas_jackknife_test_cargs",
    "parc_atlas_jackknife_test_outputs",
    "parc_atlas_jackknife_test_params",
    "parc_atlas_jackknife_test_execute",
    "parc_atlas_jackknife_test_validate",
    "parc_atlas_jackknife_test",
]