# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RF_LABEL_METADATA = Metadata(
    id="mri_rf_label",
    name="mri_rf_label",
    package="freesurfer",
)

MriRfLabel = typing.TypedDict(
    "MriRfLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_rf_label"]],
        "input_volumes": list[InputPathType],
        "transform_file": InputPathType,
        "gcafile": InputPathType,
        "output_volume": str,
        "cross_sequence_flag": typing.NotRequired[bool],
        "nogibbs_flag": typing.NotRequired[bool],
        "wm_path": typing.NotRequired[InputPathType],
        "conform_flag": typing.NotRequired[bool],
        "normpd_flag": typing.NotRequired[bool],
        "gca_tl": typing.NotRequired[InputPathType],
        "debug_voxel": typing.NotRequired[list[float]],
        "debug_node": typing.NotRequired[list[float]],
        "debug_label": typing.NotRequired[float],
        "tr": typing.NotRequired[float],
        "te": typing.NotRequired[float],
        "alpha": typing.NotRequired[float],
        "example": typing.NotRequired[list[InputPathType]],
        "pthresh": typing.NotRequired[float],
        "niter": typing.NotRequired[float],
        "novar_flag": typing.NotRequired[bool],
        "regularize": typing.NotRequired[float],
        "nohippo_flag": typing.NotRequired[bool],
        "fwm": typing.NotRequired[InputPathType],
        "mri_vol": typing.NotRequired[InputPathType],
        "heq": typing.NotRequired[InputPathType],
        "renorm": typing.NotRequired[InputPathType],
        "flash_flag": typing.NotRequired[bool],
        "flash_params": typing.NotRequired[InputPathType],
        "renormalize": typing.NotRequired[list[float]],
        "set_input": typing.NotRequired[InputPathType],
        "histogram_flag": typing.NotRequired[bool],
        "cond_density_mean": typing.NotRequired[float],
        "snapshots": typing.NotRequired[list[str]],
        "mask": typing.NotRequired[InputPathType],
        "expand": typing.NotRequired[float],
        "max_iter": typing.NotRequired[float],
        "filter_mode": typing.NotRequired[list[float]],
        "longitudinal_vol": typing.NotRequired[InputPathType],
        "longitudinal_lta": typing.NotRequired[InputPathType],
        "relabel_unlikely_flag": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class MriRfLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outvol: OutputPathType
    """Output volume from mri_ca_label"""

def mri_rf_label_params(
    input_volumes: list[InputPathType],
    transform_file: InputPathType,
    gcafile: InputPathType,
    output_volume: str,
    cross_sequence_flag: bool = False,
    nogibbs_flag: bool = False,
    wm_path: InputPathType | None = None,
    conform_flag: bool = False,
    normpd_flag: bool = False,
    gca_tl: InputPathType | None = None,
    debug_voxel: list[float] | None = None,
    debug_node: list[float] | None = None,
    debug_label: float | None = None,
    tr: float | None = None,
    te: float | None = None,
    alpha: float | None = None,
    example: list[InputPathType] | None = None,
    pthresh: float | None = None,
    niter: float | None = None,
    novar_flag: bool = False,
    regularize: float | None = None,
    nohippo_flag: bool = False,
    fwm: InputPathType | None = None,
    mri_vol: InputPathType | None = None,
    heq: InputPathType | None = None,
    renorm: InputPathType | None = None,
    flash_flag: bool = False,
    flash_params: InputPathType | None = None,
    renormalize: list[float] | None = None,
    set_input: InputPathType | None = None,
    histogram_flag: bool = False,
    cond_density_mean: float | None = None,
    snapshots: list[str] | None = None,
    mask: InputPathType | None = None,
    expand: float | None = None,
    max_iter: float | None = None,
    filter_mode: list[float] | None = None,
    longitudinal_vol: InputPathType | None = None,
    longitudinal_lta: InputPathType | None = None,
    relabel_unlikely_flag: list[float] | None = None,
) -> MriRfLabel:
    """
    Build parameters.

    Args:
        input_volumes: Input volume(s)
        transform_file: Transform file
        gcafile: GCA file
        output_volume: Output volume
        cross_sequence_flag: Label a volume acquired with a sequence different than\
            atlas
        nogibbs_flag: Disable gibbs priors
        wm_path: Use WM segmentation from provided file
        conform_flag: Interpolate volume to be isotropic 1mm^3
        normpd_flag: Normalize PD image to GCA means
        gca_tl: Use file to label the thin temporal lobe
        debug_voxel: Debug voxel at specified coordinates
        debug_node: Debug node at specified coordinates
        debug_label: Debug label at specified index
        tr: Set TR in msec
        te: Set TE in msec
        alpha: Set alpha in radians
        example: Use T1 (mri_vol) and segmentation as example
        pthresh: Use p threshold for adaptive renormalization
        niter: Apply max likelihood for n iterations
        novar_flag: Do not use variance in classification
        regularize: Regularize variance to be sigma+nC(noise)
        nohippo_flag: Do not auto-edit hippocampus
        fwm: Use fixed white matter segmentation from wm
        mri_vol: Write most likely MR volume to specified file
        heq: Use histogram equalization from specified volume
        renorm: Renormalize using predicted intensity values
        flash_flag: Use FLASH forward model to predict intensity values
        flash_params: Use FLASH forward model and tissue params from file
        renormalize: Renorm class means iter times after initial label with window\
            of wsize
        set_input: Set input volume
        histogram_flag: Use GCA to histogram normalize input image
        cond_density_mean: Mean filter n times to conditional densities
        snapshots: Write snapshots of gibbs process every n times to filename
        mask: Use mri_vol to mask final labeling
        expand: Expand
        max_iter: Set max iterations
        filter_mode: Filter labeled volume with threshold t mode filter f times
        longitudinal_vol: Longitudinal processing: mri_vol is label from tp1, LTA\
            is registration from tp1 to current data
        longitudinal_lta: Longitudinal LTA registration
        relabel_unlikely_flag: Reclassify voxels using a Gaussian window to\
            recomute priors and likelihoods

    Returns:
        Parameter dictionary.
    """
    params: MriRfLabel = {
        "@type": "freesurfer/mri_rf_label",
        "input_volumes": input_volumes,
        "transform_file": transform_file,
        "gcafile": gcafile,
        "output_volume": output_volume,
        "cross_sequence_flag": cross_sequence_flag,
        "nogibbs_flag": nogibbs_flag,
        "conform_flag": conform_flag,
        "normpd_flag": normpd_flag,
        "novar_flag": novar_flag,
        "nohippo_flag": nohippo_flag,
        "flash_flag": flash_flag,
        "histogram_flag": histogram_flag,
    }
    if wm_path is not None:
        params["wm_path"] = wm_path
    if gca_tl is not None:
        params["gca_tl"] = gca_tl
    if debug_voxel is not None:
        params["debug_voxel"] = debug_voxel
    if debug_node is not None:
        params["debug_node"] = debug_node
    if debug_label is not None:
        params["debug_label"] = debug_label
    if tr is not None:
        params["tr"] = tr
    if te is not None:
        params["te"] = te
    if alpha is not None:
        params["alpha"] = alpha
    if example is not None:
        params["example"] = example
    if pthresh is not None:
        params["pthresh"] = pthresh
    if niter is not None:
        params["niter"] = niter
    if regularize is not None:
        params["regularize"] = regularize
    if fwm is not None:
        params["fwm"] = fwm
    if mri_vol is not None:
        params["mri_vol"] = mri_vol
    if heq is not None:
        params["heq"] = heq
    if renorm is not None:
        params["renorm"] = renorm
    if flash_params is not None:
        params["flash_params"] = flash_params
    if renormalize is not None:
        params["renormalize"] = renormalize
    if set_input is not None:
        params["set_input"] = set_input
    if cond_density_mean is not None:
        params["cond_density_mean"] = cond_density_mean
    if snapshots is not None:
        params["snapshots"] = snapshots
    if mask is not None:
        params["mask"] = mask
    if expand is not None:
        params["expand"] = expand
    if max_iter is not None:
        params["max_iter"] = max_iter
    if filter_mode is not None:
        params["filter_mode"] = filter_mode
    if longitudinal_vol is not None:
        params["longitudinal_vol"] = longitudinal_vol
    if longitudinal_lta is not None:
        params["longitudinal_lta"] = longitudinal_lta
    if relabel_unlikely_flag is not None:
        params["relabel_unlikely_flag"] = relabel_unlikely_flag
    return params

def mri_rf_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRfLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("gcafile", None) is None:
        raise StyxValidationError("`gcafile` must not be None")
    if not isinstance(params["gcafile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gcafile` has the wrong type: Received `{type(params["gcafile"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("cross_sequence_flag", None) is not None:
        if not isinstance(params["cross_sequence_flag"], bool):
            raise StyxValidationError(f'`cross_sequence_flag` has the wrong type: Received `{type(params["cross_sequence_flag"])}` expected `bool`')
    if params.get("nogibbs_flag", None) is not None:
        if not isinstance(params["nogibbs_flag"], bool):
            raise StyxValidationError(f'`nogibbs_flag` has the wrong type: Received `{type(params["nogibbs_flag"])}` expected `bool`')
    if params.get("wm_path", None) is not None:
        if not isinstance(params["wm_path"], (pathlib.Path, str)):
            raise StyxValidationError(f'`wm_path` has the wrong type: Received `{type(params["wm_path"])}` expected `InputPathType`')
    if params.get("conform_flag", None) is not None:
        if not isinstance(params["conform_flag"], bool):
            raise StyxValidationError(f'`conform_flag` has the wrong type: Received `{type(params["conform_flag"])}` expected `bool`')
    if params.get("normpd_flag", None) is not None:
        if not isinstance(params["normpd_flag"], bool):
            raise StyxValidationError(f'`normpd_flag` has the wrong type: Received `{type(params["normpd_flag"])}` expected `bool`')
    if params.get("gca_tl", None) is not None:
        if not isinstance(params["gca_tl"], (pathlib.Path, str)):
            raise StyxValidationError(f'`gca_tl` has the wrong type: Received `{type(params["gca_tl"])}` expected `InputPathType`')
    if params.get("debug_voxel", None) is not None:
        if not isinstance(params["debug_voxel"], list):
            raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(params["debug_voxel"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["debug_voxel"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("debug_node", None) is not None:
        if not isinstance(params["debug_node"], list):
            raise StyxValidationError(f'`debug_node` has the wrong type: Received `{type(params["debug_node"])}` expected `list[float]`')
        if not (3 <= len(params["debug_node"]) <= 3):
            raise StyxValidationError("Parameter `debug_node` must contain between 3 and 3 elements (inclusive)")
        for e_3 in params["debug_node"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`debug_node` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("debug_label", None) is not None:
        if not isinstance(params["debug_label"], (float, int)):
            raise StyxValidationError(f'`debug_label` has the wrong type: Received `{type(params["debug_label"])}` expected `float`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("te", None) is not None:
        if not isinstance(params["te"], (float, int)):
            raise StyxValidationError(f'`te` has the wrong type: Received `{type(params["te"])}` expected `float`')
    if params.get("alpha", None) is not None:
        if not isinstance(params["alpha"], (float, int)):
            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["alpha"])}` expected `float`')
    if params.get("example", None) is not None:
        if not isinstance(params["example"], list):
            raise StyxValidationError(f'`example` has the wrong type: Received `{type(params["example"])}` expected `list[InputPathType]`')
        for e_4 in params["example"]:
            if not isinstance(e_4, (pathlib.Path, str)):
                raise StyxValidationError(f'`example` has the wrong type: Received `{type(e_4)}` expected `list[InputPathType]`')
    if params.get("pthresh", None) is not None:
        if not isinstance(params["pthresh"], (float, int)):
            raise StyxValidationError(f'`pthresh` has the wrong type: Received `{type(params["pthresh"])}` expected `float`')
    if params.get("niter", None) is not None:
        if not isinstance(params["niter"], (float, int)):
            raise StyxValidationError(f'`niter` has the wrong type: Received `{type(params["niter"])}` expected `float`')
    if params.get("novar_flag", None) is not None:
        if not isinstance(params["novar_flag"], bool):
            raise StyxValidationError(f'`novar_flag` has the wrong type: Received `{type(params["novar_flag"])}` expected `bool`')
    if params.get("regularize", None) is not None:
        if not isinstance(params["regularize"], (float, int)):
            raise StyxValidationError(f'`regularize` has the wrong type: Received `{type(params["regularize"])}` expected `float`')
    if params.get("nohippo_flag", None) is not None:
        if not isinstance(params["nohippo_flag"], bool):
            raise StyxValidationError(f'`nohippo_flag` has the wrong type: Received `{type(params["nohippo_flag"])}` expected `bool`')
    if params.get("fwm", None) is not None:
        if not isinstance(params["fwm"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fwm` has the wrong type: Received `{type(params["fwm"])}` expected `InputPathType`')
    if params.get("mri_vol", None) is not None:
        if not isinstance(params["mri_vol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mri_vol` has the wrong type: Received `{type(params["mri_vol"])}` expected `InputPathType`')
    if params.get("heq", None) is not None:
        if not isinstance(params["heq"], (pathlib.Path, str)):
            raise StyxValidationError(f'`heq` has the wrong type: Received `{type(params["heq"])}` expected `InputPathType`')
    if params.get("renorm", None) is not None:
        if not isinstance(params["renorm"], (pathlib.Path, str)):
            raise StyxValidationError(f'`renorm` has the wrong type: Received `{type(params["renorm"])}` expected `InputPathType`')
    if params.get("flash_flag", None) is not None:
        if not isinstance(params["flash_flag"], bool):
            raise StyxValidationError(f'`flash_flag` has the wrong type: Received `{type(params["flash_flag"])}` expected `bool`')
    if params.get("flash_params", None) is not None:
        if not isinstance(params["flash_params"], (pathlib.Path, str)):
            raise StyxValidationError(f'`flash_params` has the wrong type: Received `{type(params["flash_params"])}` expected `InputPathType`')
    if params.get("renormalize", None) is not None:
        if not isinstance(params["renormalize"], list):
            raise StyxValidationError(f'`renormalize` has the wrong type: Received `{type(params["renormalize"])}` expected `list[float]`')
        for e_5 in params["renormalize"]:
            if not isinstance(e_5, (float, int)):
                raise StyxValidationError(f'`renormalize` has the wrong type: Received `{type(e_5)}` expected `list[float]`')
    if params.get("set_input", None) is not None:
        if not isinstance(params["set_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`set_input` has the wrong type: Received `{type(params["set_input"])}` expected `InputPathType`')
    if params.get("histogram_flag", None) is not None:
        if not isinstance(params["histogram_flag"], bool):
            raise StyxValidationError(f'`histogram_flag` has the wrong type: Received `{type(params["histogram_flag"])}` expected `bool`')
    if params.get("cond_density_mean", None) is not None:
        if not isinstance(params["cond_density_mean"], (float, int)):
            raise StyxValidationError(f'`cond_density_mean` has the wrong type: Received `{type(params["cond_density_mean"])}` expected `float`')
    if params.get("snapshots", None) is not None:
        if not isinstance(params["snapshots"], list):
            raise StyxValidationError(f'`snapshots` has the wrong type: Received `{type(params["snapshots"])}` expected `list[str]`')
        if not (2 <= len(params["snapshots"]) <= 2):
            raise StyxValidationError("Parameter `snapshots` must contain between 2 and 2 elements (inclusive)")
        for e_6 in params["snapshots"]:
            if not isinstance(e_6, str):
                raise StyxValidationError(f'`snapshots` has the wrong type: Received `{type(e_6)}` expected `list[str]`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("expand", None) is not None:
        if not isinstance(params["expand"], (float, int)):
            raise StyxValidationError(f'`expand` has the wrong type: Received `{type(params["expand"])}` expected `float`')
    if params.get("max_iter", None) is not None:
        if not isinstance(params["max_iter"], (float, int)):
            raise StyxValidationError(f'`max_iter` has the wrong type: Received `{type(params["max_iter"])}` expected `float`')
    if params.get("filter_mode", None) is not None:
        if not isinstance(params["filter_mode"], list):
            raise StyxValidationError(f'`filter_mode` has the wrong type: Received `{type(params["filter_mode"])}` expected `list[float]`')
        if not (2 <= len(params["filter_mode"]) <= 2):
            raise StyxValidationError("Parameter `filter_mode` must contain between 2 and 2 elements (inclusive)")
        for e_7 in params["filter_mode"]:
            if not isinstance(e_7, (float, int)):
                raise StyxValidationError(f'`filter_mode` has the wrong type: Received `{type(e_7)}` expected `list[float]`')
    if params.get("longitudinal_vol", None) is not None:
        if not isinstance(params["longitudinal_vol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`longitudinal_vol` has the wrong type: Received `{type(params["longitudinal_vol"])}` expected `InputPathType`')
    if params.get("longitudinal_lta", None) is not None:
        if not isinstance(params["longitudinal_lta"], (pathlib.Path, str)):
            raise StyxValidationError(f'`longitudinal_lta` has the wrong type: Received `{type(params["longitudinal_lta"])}` expected `InputPathType`')
    if params.get("relabel_unlikely_flag", None) is not None:
        if not isinstance(params["relabel_unlikely_flag"], list):
            raise StyxValidationError(f'`relabel_unlikely_flag` has the wrong type: Received `{type(params["relabel_unlikely_flag"])}` expected `list[float]`')
        if not (4 <= len(params["relabel_unlikely_flag"]) <= 4):
            raise StyxValidationError("Parameter `relabel_unlikely_flag` must contain between 4 and 4 elements (inclusive)")
        for e_8 in params["relabel_unlikely_flag"]:
            if not isinstance(e_8, (float, int)):
                raise StyxValidationError(f'`relabel_unlikely_flag` has the wrong type: Received `{type(e_8)}` expected `list[float]`')

def mri_rf_label_cargs(params: MriRfLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_rf_label")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(execution.input_file(params["gcafile"]))
    cargs.append(params["output_volume"])
    if params.get("cross_sequence_flag"):
        cargs.append("-cross-sequence")
    if params.get("nogibbs_flag"):
        cargs.append("-nogibbs")
    v_0 = params.get("wm_path")
    if v_0 is not None:
        cargs.append("-wm")
        cargs.append(execution.input_file(v_0))
    if params.get("conform_flag"):
        cargs.append("-conform")
    if params.get("normpd_flag"):
        cargs.append("-normpd")
    v_1 = params.get("gca_tl")
    if v_1 is not None:
        cargs.append("-tl")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("debug_voxel")
    if v_2 is not None:
        cargs.append("-debug_voxel")
        for item1 in v_2:
            cargs.append(str(item1))
    v_3 = params.get("debug_node")
    if v_3 is not None:
        cargs.append("-debug_node")
        for item2 in v_3:
            cargs.append(str(item2))
    v_4 = params.get("debug_label")
    if v_4 is not None:
        cargs.append("-debug_label")
        cargs.append(str(v_4))
    v_5 = params.get("tr")
    if v_5 is not None:
        cargs.append("-tr")
        cargs.append(str(v_5))
    v_6 = params.get("te")
    if v_6 is not None:
        cargs.append("-te")
        cargs.append(str(v_6))
    v_7 = params.get("alpha")
    if v_7 is not None:
        cargs.append("-alpha")
        cargs.append(str(v_7))
    v_8 = params.get("example")
    if v_8 is not None:
        cargs.append("-example")
        for item3 in v_8:
            cargs.append(execution.input_file(item3))
    v_9 = params.get("pthresh")
    if v_9 is not None:
        cargs.append("-pthresh")
        cargs.append(str(v_9))
    v_10 = params.get("niter")
    if v_10 is not None:
        cargs.append("-niter")
        cargs.append(str(v_10))
    if params.get("novar_flag"):
        cargs.append("-novar")
    v_11 = params.get("regularize")
    if v_11 is not None:
        cargs.append("-regularize")
        cargs.append(str(v_11))
    if params.get("nohippo_flag"):
        cargs.append("-nohippo")
    v_12 = params.get("fwm")
    if v_12 is not None:
        cargs.append("-fwm")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("mri_vol")
    if v_13 is not None:
        cargs.append("-mri")
        cargs.append(execution.input_file(v_13))
    v_14 = params.get("heq")
    if v_14 is not None:
        cargs.append("-heq")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("renorm")
    if v_15 is not None:
        cargs.append("-renorm")
        cargs.append(execution.input_file(v_15))
    if params.get("flash_flag"):
        cargs.append("-flash")
    v_16 = params.get("flash_params")
    if v_16 is not None:
        cargs.append("-flash_params")
        cargs.append(execution.input_file(v_16))
    v_17 = params.get("renormalize")
    if v_17 is not None:
        cargs.append("-renormalize")
        for item4 in v_17:
            cargs.append(str(item4))
    v_18 = params.get("set_input")
    if v_18 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_18))
    if params.get("histogram_flag"):
        cargs.append("-h")
    v_19 = params.get("cond_density_mean")
    if v_19 is not None:
        cargs.append("-a")
        cargs.append(str(v_19))
    v_20 = params.get("snapshots")
    if v_20 is not None:
        cargs.append("-w")
        for item5 in v_20:
            cargs.append(item5)
    v_21 = params.get("mask")
    if v_21 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_21))
    v_22 = params.get("expand")
    if v_22 is not None:
        cargs.append("-e")
        cargs.append(str(v_22))
    v_23 = params.get("max_iter")
    if v_23 is not None:
        cargs.append("-n")
        cargs.append(str(v_23))
    v_24 = params.get("filter_mode")
    if v_24 is not None:
        cargs.append("-f")
        for item6 in v_24:
            cargs.append(str(item6))
    v_25 = params.get("longitudinal_vol")
    if v_25 is not None:
        cargs.append("-L")
        cargs.append(execution.input_file(v_25))
    v_26 = params.get("longitudinal_lta")
    if v_26 is not None:
        cargs.append(execution.input_file(v_26))
    v_27 = params.get("relabel_unlikely_flag")
    if v_27 is not None:
        cargs.append("-RELABEL_UNLIKELY")
        for item7 in v_27:
            cargs.append(str(item7))
    return cargs

def mri_rf_label_outputs(params: MriRfLabel, execution: Execution) -> MriRfLabelOutputs:
    """Build the MriRfLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outvol_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriRfLabelOutputs(
        root=root_v,
        outvol=outvol_v,
    )

def mri_rf_label_execute(params: MriRfLabel, runner: Runner | None = None) -> MriRfLabelOutputs:
    """
    mri_rf_label

    MRI automatic tissue labeling using a Gaussian Classifier Atlas (GCA).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_rf_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RF_LABEL_METADATA)
    execution.params(params)
    args = mri_rf_label_cargs(params, execution)
    out = mri_rf_label_outputs(params, execution)
    execution.run(args)
    return out

def mri_rf_label(
    input_volumes: list[InputPathType],
    transform_file: InputPathType,
    gcafile: InputPathType,
    output_volume: str,
    cross_sequence_flag: bool = False,
    nogibbs_flag: bool = False,
    wm_path: InputPathType | None = None,
    conform_flag: bool = False,
    normpd_flag: bool = False,
    gca_tl: InputPathType | None = None,
    debug_voxel: list[float] | None = None,
    debug_node: list[float] | None = None,
    debug_label: float | None = None,
    tr: float | None = None,
    te: float | None = None,
    alpha: float | None = None,
    example: list[InputPathType] | None = None,
    pthresh: float | None = None,
    niter: float | None = None,
    novar_flag: bool = False,
    regularize: float | None = None,
    nohippo_flag: bool = False,
    fwm: InputPathType | None = None,
    mri_vol: InputPathType | None = None,
    heq: InputPathType | None = None,
    renorm: InputPathType | None = None,
    flash_flag: bool = False,
    flash_params: InputPathType | None = None,
    renormalize: list[float] | None = None,
    set_input: InputPathType | None = None,
    histogram_flag: bool = False,
    cond_density_mean: float | None = None,
    snapshots: list[str] | None = None,
    mask: InputPathType | None = None,
    expand: float | None = None,
    max_iter: float | None = None,
    filter_mode: list[float] | None = None,
    longitudinal_vol: InputPathType | None = None,
    longitudinal_lta: InputPathType | None = None,
    relabel_unlikely_flag: list[float] | None = None,
    runner: Runner | None = None,
) -> MriRfLabelOutputs:
    """
    mri_rf_label

    MRI automatic tissue labeling using a Gaussian Classifier Atlas (GCA).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Input volume(s)
        transform_file: Transform file
        gcafile: GCA file
        output_volume: Output volume
        cross_sequence_flag: Label a volume acquired with a sequence different than\
            atlas
        nogibbs_flag: Disable gibbs priors
        wm_path: Use WM segmentation from provided file
        conform_flag: Interpolate volume to be isotropic 1mm^3
        normpd_flag: Normalize PD image to GCA means
        gca_tl: Use file to label the thin temporal lobe
        debug_voxel: Debug voxel at specified coordinates
        debug_node: Debug node at specified coordinates
        debug_label: Debug label at specified index
        tr: Set TR in msec
        te: Set TE in msec
        alpha: Set alpha in radians
        example: Use T1 (mri_vol) and segmentation as example
        pthresh: Use p threshold for adaptive renormalization
        niter: Apply max likelihood for n iterations
        novar_flag: Do not use variance in classification
        regularize: Regularize variance to be sigma+nC(noise)
        nohippo_flag: Do not auto-edit hippocampus
        fwm: Use fixed white matter segmentation from wm
        mri_vol: Write most likely MR volume to specified file
        heq: Use histogram equalization from specified volume
        renorm: Renormalize using predicted intensity values
        flash_flag: Use FLASH forward model to predict intensity values
        flash_params: Use FLASH forward model and tissue params from file
        renormalize: Renorm class means iter times after initial label with window\
            of wsize
        set_input: Set input volume
        histogram_flag: Use GCA to histogram normalize input image
        cond_density_mean: Mean filter n times to conditional densities
        snapshots: Write snapshots of gibbs process every n times to filename
        mask: Use mri_vol to mask final labeling
        expand: Expand
        max_iter: Set max iterations
        filter_mode: Filter labeled volume with threshold t mode filter f times
        longitudinal_vol: Longitudinal processing: mri_vol is label from tp1, LTA\
            is registration from tp1 to current data
        longitudinal_lta: Longitudinal LTA registration
        relabel_unlikely_flag: Reclassify voxels using a Gaussian window to\
            recomute priors and likelihoods
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_rf_label_params(
        input_volumes=input_volumes,
        transform_file=transform_file,
        gcafile=gcafile,
        output_volume=output_volume,
        cross_sequence_flag=cross_sequence_flag,
        nogibbs_flag=nogibbs_flag,
        wm_path=wm_path,
        conform_flag=conform_flag,
        normpd_flag=normpd_flag,
        gca_tl=gca_tl,
        debug_voxel=debug_voxel,
        debug_node=debug_node,
        debug_label=debug_label,
        tr=tr,
        te=te,
        alpha=alpha,
        example=example,
        pthresh=pthresh,
        niter=niter,
        novar_flag=novar_flag,
        regularize=regularize,
        nohippo_flag=nohippo_flag,
        fwm=fwm,
        mri_vol=mri_vol,
        heq=heq,
        renorm=renorm,
        flash_flag=flash_flag,
        flash_params=flash_params,
        renormalize=renormalize,
        set_input=set_input,
        histogram_flag=histogram_flag,
        cond_density_mean=cond_density_mean,
        snapshots=snapshots,
        mask=mask,
        expand=expand,
        max_iter=max_iter,
        filter_mode=filter_mode,
        longitudinal_vol=longitudinal_vol,
        longitudinal_lta=longitudinal_lta,
        relabel_unlikely_flag=relabel_unlikely_flag,
    )
    return mri_rf_label_execute(params, runner)

__all__ = [
    "MriRfLabel",
    "MriRfLabelOutputs",
    "MRI_RF_LABEL_METADATA",
    "mri_rf_label_cargs",
    "mri_rf_label_outputs",
    "mri_rf_label_params",
    "mri_rf_label_execute",
    "mri_rf_label_validate",
    "mri_rf_label",
]