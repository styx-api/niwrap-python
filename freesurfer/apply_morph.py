# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APPLY_MORPH_METADATA = Metadata(
    id="applyMorph",
    name="applyMorph",
    package="freesurfer",
)

ApplyMorph = typing.TypedDict(
    "ApplyMorph",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/applyMorph"]],
        "inputs": list[InputPathType],
        "template": InputPathType,
        "transform": InputPathType,
        "zlib_buffer": typing.NotRequired[float],
        "dbg_coords": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class ApplyMorphOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def apply_morph_params(
    inputs: list[InputPathType],
    template: InputPathType,
    transform: InputPathType,
    zlib_buffer: float | None = None,
    dbg_coords: list[float] | None = None,
) -> ApplyMorph:
    """
    Build parameters.

    Args:
        inputs: Input files.
        template: Template volume.
        transform: Transform file.
        zlib_buffer: Zlib buffer pre-allocation multiplier.
        dbg_coords: Debugging coordinates.

    Returns:
        Parameter dictionary.
    """
    params: ApplyMorph = {
        "@type": "freesurfer/applyMorph",
        "inputs": inputs,
        "template": template,
        "transform": transform,
    }
    if zlib_buffer is not None:
        params["zlib_buffer"] = zlib_buffer
    if dbg_coords is not None:
        params["dbg_coords"] = dbg_coords
    return params

def apply_morph_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ApplyMorph."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputs", None) is None:
        raise StyxValidationError("`inputs` must not be None")
    if not isinstance(params["inputs"], list):
        raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(params["inputs"])}` expected `list[InputPathType]`')
    for e in params["inputs"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("template", None) is None:
        raise StyxValidationError("`template` must not be None")
    if not isinstance(params["template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("zlib_buffer", None) is not None:
        if not isinstance(params["zlib_buffer"], (float, int)):
            raise StyxValidationError(f'`zlib_buffer` has the wrong type: Received `{type(params["zlib_buffer"])}` expected `float`')
    if params.get("dbg_coords", None) is not None:
        if not isinstance(params["dbg_coords"], list):
            raise StyxValidationError(f'`dbg_coords` has the wrong type: Received `{type(params["dbg_coords"])}` expected `list[float]`')
        if not (3 <= len(params["dbg_coords"]) <= 3):
            raise StyxValidationError("Parameter `dbg_coords` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["dbg_coords"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`dbg_coords` has the wrong type: Received `{type(e_2)}` expected `list[float]`')

def apply_morph_cargs(params: ApplyMorph, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("applyMorph")
    for item0 in params["inputs"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--template")
    cargs.append(execution.input_file(params["template"]))
    cargs.append("--transform")
    cargs.append(execution.input_file(params["transform"]))
    v_0 = params.get("zlib_buffer")
    if v_0 is not None:
        cargs.append("--zlib_buffer")
        cargs.append(str(v_0))
    v_1 = params.get("dbg_coords")
    if v_1 is not None:
        cargs.append("--dbg_coords")
        for item1 in v_1:
            cargs.append(str(item1))
    return cargs

def apply_morph_outputs(params: ApplyMorph, execution: Execution) -> ApplyMorphOutputs:
    """Build the ApplyMorphOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ApplyMorphOutputs(
        root=root_v,
    )

def apply_morph_execute(params: ApplyMorph, runner: Runner | None = None) -> ApplyMorphOutputs:
    """
    applyMorph

    A tool for applying a morph to a volume using a template and a transform

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    apply_morph_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APPLY_MORPH_METADATA)
    execution.params(params)
    args = apply_morph_cargs(params, execution)
    out = apply_morph_outputs(params, execution)
    execution.run(args)
    return out

def apply_morph(
    inputs: list[InputPathType],
    template: InputPathType,
    transform: InputPathType,
    zlib_buffer: float | None = None,
    dbg_coords: list[float] | None = None,
    runner: Runner | None = None,
) -> ApplyMorphOutputs:
    """
    applyMorph

    A tool for applying a morph to a volume using a template and a transform

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inputs: Input files.
        template: Template volume.
        transform: Transform file.
        zlib_buffer: Zlib buffer pre-allocation multiplier.
        dbg_coords: Debugging coordinates.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = apply_morph_params(
        inputs=inputs,
        template=template,
        transform=transform,
        zlib_buffer=zlib_buffer,
        dbg_coords=dbg_coords,
    )
    return apply_morph_execute(params, runner)

__all__ = [
    "ApplyMorph",
    "ApplyMorphOutputs",
    "APPLY_MORPH_METADATA",
    "apply_morph_cargs",
    "apply_morph_outputs",
    "apply_morph_params",
    "apply_morph_execute",
    "apply_morph_validate",
    "apply_morph",
]