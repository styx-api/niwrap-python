# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SWI_PREPROCESS_METADATA = Metadata(
    id="swi_preprocess",
    name="swi_preprocess",
    package="freesurfer",
)

SwiPreprocessScanner = typing.Literal["ge"] | typing.Literal["siemens"] | typing.Literal["philips"]

SwiPreprocess = typing.TypedDict(
    "SwiPreprocess",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/swi_preprocess"]],
        "scanner": SwiPreprocessScanner,
        "ge_file": typing.NotRequired[InputPathType],
        "philips_file": typing.NotRequired[InputPathType],
        "siemens_magnitude": typing.NotRequired[InputPathType],
        "siemens_phase": typing.NotRequired[InputPathType],
        "out_magnitude": str,
        "out_phase": str,
    },
)

@dataclasses.dataclass
class SwiPreprocessOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_magnitude_file: OutputPathType
    """Output magnitude file in NIfTI format."""
    output_phase_file: OutputPathType
    """Output phase file in NIfTI format."""

def swi_preprocess_params(
    scanner: SwiPreprocessScanner,
    out_magnitude: str,
    out_phase: str,
    ge_file: InputPathType | None = None,
    philips_file: InputPathType | None = None,
    siemens_magnitude: InputPathType | None = None,
    siemens_phase: InputPathType | None = None,
) -> SwiPreprocess:
    """
    Build parameters.

    Args:
        scanner: Name of the scanner (one of ge, siemens or philips)
        out_magnitude: Name of the output magnitude file after preprocessing.\
            Ensure it has a .nii suffix
        out_phase: Name of the output phase file after preprocessing. Ensure it has\
            a .nii suffix
        ge_file: Name of the input GE file (only compatible with --scanner ge\
            option)
        philips_file: Name of the input Philips file (only compatible with\
            --scanner philips option)
        siemens_magnitude: Name of the input Siemens magnitude file (only\
            compatible with --scanner siemens option)
        siemens_phase: Name of the input Siemens phase file (only compatible with\
            --scanner siemens option)

    Returns:
        Parameter dictionary.
    """
    params: SwiPreprocess = {
        "@type": "freesurfer/swi_preprocess",
        "scanner": scanner,
        "out_magnitude": out_magnitude,
        "out_phase": out_phase,
    }
    if ge_file is not None:
        params["ge_file"] = ge_file
    if philips_file is not None:
        params["philips_file"] = philips_file
    if siemens_magnitude is not None:
        params["siemens_magnitude"] = siemens_magnitude
    if siemens_phase is not None:
        params["siemens_phase"] = siemens_phase
    return params

def swi_preprocess_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SwiPreprocess."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scanner", None) is None:
        raise StyxValidationError("`scanner` must not be None")
    if not isinstance(params["scanner"], str):
        raise StyxValidationError(f'`scanner` has the wrong type: Received `{type(params["scanner"])}` expected `SwiPreprocessScanner`')
    if params["scanner"] not in ["ge", "siemens", "philips"]:
        raise StyxValidationError("Parameter `scanner` must be one of [\"ge\", \"siemens\", \"philips\"]")
    if params.get("ge_file", None) is not None:
        if not isinstance(params["ge_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ge_file` has the wrong type: Received `{type(params["ge_file"])}` expected `InputPathType`')
    if params.get("philips_file", None) is not None:
        if not isinstance(params["philips_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`philips_file` has the wrong type: Received `{type(params["philips_file"])}` expected `InputPathType`')
    if params.get("siemens_magnitude", None) is not None:
        if not isinstance(params["siemens_magnitude"], (pathlib.Path, str)):
            raise StyxValidationError(f'`siemens_magnitude` has the wrong type: Received `{type(params["siemens_magnitude"])}` expected `InputPathType`')
    if params.get("siemens_phase", None) is not None:
        if not isinstance(params["siemens_phase"], (pathlib.Path, str)):
            raise StyxValidationError(f'`siemens_phase` has the wrong type: Received `{type(params["siemens_phase"])}` expected `InputPathType`')
    if params.get("out_magnitude", None) is None:
        raise StyxValidationError("`out_magnitude` must not be None")
    if not isinstance(params["out_magnitude"], str):
        raise StyxValidationError(f'`out_magnitude` has the wrong type: Received `{type(params["out_magnitude"])}` expected `str`')
    if params.get("out_phase", None) is None:
        raise StyxValidationError("`out_phase` must not be None")
    if not isinstance(params["out_phase"], str):
        raise StyxValidationError(f'`out_phase` has the wrong type: Received `{type(params["out_phase"])}` expected `str`')

def swi_preprocess_cargs(params: SwiPreprocess, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("swi_preprocess")
    cargs.append("--scanner")
    cargs.append(str(params["scanner"]))
    v_0 = params.get("ge_file")
    if v_0 is not None:
        cargs.append("--ge_file")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("philips_file")
    if v_1 is not None:
        cargs.append("--philips_file")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("siemens_magnitude")
    if v_2 is not None:
        cargs.append("--siemens_mag")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("siemens_phase")
    if v_3 is not None:
        cargs.append("--siemens_phase")
        cargs.append(execution.input_file(v_3))
    cargs.append("--out_magnitude")
    cargs.append(params["out_magnitude"])
    cargs.append("--out_phase")
    cargs.append(params["out_phase"])
    return cargs

def swi_preprocess_outputs(params: SwiPreprocess, execution: Execution) -> SwiPreprocessOutputs:
    """Build the SwiPreprocessOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_magnitude_file_v: OutputPathType = execution.output_file(params["out_magnitude"])
    output_phase_file_v: OutputPathType = execution.output_file(params["out_phase"])
    return SwiPreprocessOutputs(
        root=root_v,
        output_magnitude_file=output_magnitude_file_v,
        output_phase_file=output_phase_file_v,
    )

def swi_preprocess_execute(params: SwiPreprocess, runner: Runner | None = None) -> SwiPreprocessOutputs:
    """
    swi_preprocess

    Pre-process the Susceptibility-weighted images and write out nifti files for feeding into PRELUDE (Phase Unwrapping Library of FSL).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    swi_preprocess_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SWI_PREPROCESS_METADATA)
    execution.params(params)
    args = swi_preprocess_cargs(params, execution)
    out = swi_preprocess_outputs(params, execution)
    execution.run(args)
    return out

def swi_preprocess(
    scanner: SwiPreprocessScanner,
    out_magnitude: str,
    out_phase: str,
    ge_file: InputPathType | None = None,
    philips_file: InputPathType | None = None,
    siemens_magnitude: InputPathType | None = None,
    siemens_phase: InputPathType | None = None,
    runner: Runner | None = None,
) -> SwiPreprocessOutputs:
    """
    swi_preprocess

    Pre-process the Susceptibility-weighted images and write out nifti files for feeding into PRELUDE (Phase Unwrapping Library of FSL).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        scanner: Name of the scanner (one of ge, siemens or philips)
        out_magnitude: Name of the output magnitude file after preprocessing.\
            Ensure it has a .nii suffix
        out_phase: Name of the output phase file after preprocessing. Ensure it has\
            a .nii suffix
        ge_file: Name of the input GE file (only compatible with --scanner ge\
            option)
        philips_file: Name of the input Philips file (only compatible with\
            --scanner philips option)
        siemens_magnitude: Name of the input Siemens magnitude file (only\
            compatible with --scanner siemens option)
        siemens_phase: Name of the input Siemens phase file (only compatible with\
            --scanner siemens option)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = swi_preprocess_params(
        scanner=scanner,
        out_magnitude=out_magnitude,
        out_phase=out_phase,
        ge_file=ge_file,
        philips_file=philips_file,
        siemens_magnitude=siemens_magnitude,
        siemens_phase=siemens_phase,
    )
    return swi_preprocess_execute(params, runner)

__all__ = [
    "SwiPreprocess",
    "SwiPreprocessOutputs",
    "SWI_PREPROCESS_METADATA",
    "swi_preprocess_cargs",
    "swi_preprocess_outputs",
    "swi_preprocess_params",
    "swi_preprocess_execute",
    "swi_preprocess_validate",
    "swi_preprocess",
]