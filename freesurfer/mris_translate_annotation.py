# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_TRANSLATE_ANNOTATION_METADATA = Metadata(
    id="mris_translate_annotation",
    name="mris_translate_annotation",
    package="freesurfer",
)

MrisTranslateAnnotation = typing.TypedDict(
    "MrisTranslateAnnotation",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_translate_annotation"]],
        "subject": str,
        "hemi": str,
        "in_annot": InputPathType,
        "translation_file": InputPathType,
        "out_annot": str,
    },
)

@dataclasses.dataclass
class MrisTranslateAnnotationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_annotation: OutputPathType
    """The output file with the translated annotation."""

def mris_translate_annotation_params(
    subject: str,
    hemi: str,
    in_annot: InputPathType,
    translation_file: InputPathType,
    out_annot: str,
) -> MrisTranslateAnnotation:
    """
    Build parameters.

    Args:
        subject: The subject identifier.
        hemi: Hemisphere identifier (e.g., lh or rh).
        in_annot: Input annotation file.
        translation_file: Translation table file.
        out_annot: Output annotation file.

    Returns:
        Parameter dictionary.
    """
    params: MrisTranslateAnnotation = {
        "@type": "freesurfer/mris_translate_annotation",
        "subject": subject,
        "hemi": hemi,
        "in_annot": in_annot,
        "translation_file": translation_file,
        "out_annot": out_annot,
    }
    return params

def mris_translate_annotation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisTranslateAnnotation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("in_annot", None) is None:
        raise StyxValidationError("`in_annot` must not be None")
    if not isinstance(params["in_annot"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_annot` has the wrong type: Received `{type(params["in_annot"])}` expected `InputPathType`')
    if params.get("translation_file", None) is None:
        raise StyxValidationError("`translation_file` must not be None")
    if not isinstance(params["translation_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`translation_file` has the wrong type: Received `{type(params["translation_file"])}` expected `InputPathType`')
    if params.get("out_annot", None) is None:
        raise StyxValidationError("`out_annot` must not be None")
    if not isinstance(params["out_annot"], str):
        raise StyxValidationError(f'`out_annot` has the wrong type: Received `{type(params["out_annot"])}` expected `str`')

def mris_translate_annotation_cargs(params: MrisTranslateAnnotation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_translate_annotation")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["in_annot"]))
    cargs.append(execution.input_file(params["translation_file"]))
    cargs.append(params["out_annot"])
    return cargs

def mris_translate_annotation_outputs(params: MrisTranslateAnnotation, execution: Execution) -> MrisTranslateAnnotationOutputs:
    """Build the MrisTranslateAnnotationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_annotation_v: OutputPathType = execution.output_file(params["out_annot"])
    return MrisTranslateAnnotationOutputs(
        root=root_v,
        output_annotation=output_annotation_v,
    )

def mris_translate_annotation_execute(params: MrisTranslateAnnotation, runner: Runner | None = None) -> MrisTranslateAnnotationOutputs:
    """
    mris_translate_annotation

    This program applies a translation table to an annotation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_translate_annotation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_TRANSLATE_ANNOTATION_METADATA)
    execution.params(params)
    args = mris_translate_annotation_cargs(params, execution)
    out = mris_translate_annotation_outputs(params, execution)
    execution.run(args)
    return out

def mris_translate_annotation(
    subject: str,
    hemi: str,
    in_annot: InputPathType,
    translation_file: InputPathType,
    out_annot: str,
    runner: Runner | None = None,
) -> MrisTranslateAnnotationOutputs:
    """
    mris_translate_annotation

    This program applies a translation table to an annotation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject identifier.
        hemi: Hemisphere identifier (e.g., lh or rh).
        in_annot: Input annotation file.
        translation_file: Translation table file.
        out_annot: Output annotation file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_translate_annotation_params(
        subject=subject,
        hemi=hemi,
        in_annot=in_annot,
        translation_file=translation_file,
        out_annot=out_annot,
    )
    return mris_translate_annotation_execute(params, runner)

__all__ = [
    "MrisTranslateAnnotation",
    "MrisTranslateAnnotationOutputs",
    "MRIS_TRANSLATE_ANNOTATION_METADATA",
    "mris_translate_annotation_cargs",
    "mris_translate_annotation_outputs",
    "mris_translate_annotation_params",
    "mris_translate_annotation_execute",
    "mris_translate_annotation_validate",
    "mris_translate_annotation",
]