# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DEFACE_SUBJECT_METADATA = Metadata(
    id="deface_subject",
    name="deface_subject",
    package="freesurfer",
)

DefaceSubject = typing.TypedDict(
    "DefaceSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/deface_subject"]],
        "subjects_dir": str,
        "subject_id": str,
        "volume_input": InputPathType,
        "volume_output": str,
    },
)

@dataclasses.dataclass
class DefaceSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Defaced output volume."""

def deface_subject_params(
    subjects_dir: str,
    subject_id: str,
    volume_input: InputPathType,
    volume_output: str,
) -> DefaceSubject:
    """
    Build parameters.

    Args:
        subjects_dir:
        subject_id:
        volume_input:
        volume_output:

    Returns:
        Parameter dictionary.
    """
    params: DefaceSubject = {
        "@type": "freesurfer/deface_subject",
        "subjects_dir": subjects_dir,
        "subject_id": subject_id,
        "volume_input": volume_input,
        "volume_output": volume_output,
    }
    return params

def deface_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DefaceSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("volume_input", None) is None:
        raise StyxValidationError("`volume_input` must not be None")
    if not isinstance(params["volume_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_input` has the wrong type: Received `{type(params["volume_input"])}` expected `InputPathType`')
    if params.get("volume_output", None) is None:
        raise StyxValidationError("`volume_output` must not be None")
    if not isinstance(params["volume_output"], str):
        raise StyxValidationError(f'`volume_output` has the wrong type: Received `{type(params["volume_output"])}` expected `str`')

def deface_subject_cargs(params: DefaceSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("deface_subject")
    cargs.append("-sdir")
    cargs.append(params["subjects_dir"])
    cargs.append("-id")
    cargs.append(params["subject_id"])
    cargs.append("-i")
    cargs.append(execution.input_file(params["volume_input"]))
    cargs.append("-o")
    cargs.append(params["volume_output"])
    return cargs

def deface_subject_outputs(params: DefaceSubject, execution: Execution) -> DefaceSubjectOutputs:
    """Build the DefaceSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["volume_output"])
    return DefaceSubjectOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def deface_subject_execute(params: DefaceSubject, runner: Runner | None = None) -> DefaceSubjectOutputs:
    """
    deface_subject

    Tool for defacing MRI images to anonymize patient data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    deface_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DEFACE_SUBJECT_METADATA)
    execution.params(params)
    args = deface_subject_cargs(params, execution)
    out = deface_subject_outputs(params, execution)
    execution.run(args)
    return out

def deface_subject(
    subjects_dir: str,
    subject_id: str,
    volume_input: InputPathType,
    volume_output: str,
    runner: Runner | None = None,
) -> DefaceSubjectOutputs:
    """
    deface_subject

    Tool for defacing MRI images to anonymize patient data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects_dir:
        subject_id:
        volume_input:
        volume_output:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = deface_subject_params(
        subjects_dir=subjects_dir,
        subject_id=subject_id,
        volume_input=volume_input,
        volume_output=volume_output,
    )
    return deface_subject_execute(params, runner)

__all__ = [
    "DefaceSubject",
    "DefaceSubjectOutputs",
    "DEFACE_SUBJECT_METADATA",
    "deface_subject_cargs",
    "deface_subject_outputs",
    "deface_subject_params",
    "deface_subject_execute",
    "deface_subject_validate",
    "deface_subject",
]