# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CC_METADATA = Metadata(
    id="mri_cc",
    name="mri_cc",
    package="freesurfer",
)

MriCc = typing.TypedDict(
    "MriCc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cc"]],
        "subject_name": str,
        "output_file": typing.NotRequired[str],
        "aseg_file": typing.NotRequired[InputPathType],
        "norm_file": typing.NotRequired[InputPathType],
        "sdir": typing.NotRequired[str],
        "rotation_lta": typing.NotRequired[InputPathType],
        "force_flag": typing.NotRequired[bool],
        "include_fornix": typing.NotRequired[bool],
        "compartments": typing.NotRequired[float],
        "thickness": typing.NotRequired[float],
        "skip_voxels": typing.NotRequired[float],
        "max_rotation": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriCcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType | None
    """Output volume including CC segmentation results."""

def mri_cc_params(
    subject_name: str,
    output_file: str | None = None,
    aseg_file: InputPathType | None = None,
    norm_file: InputPathType | None = None,
    sdir: str | None = None,
    rotation_lta: InputPathType | None = None,
    force_flag: bool = False,
    include_fornix: bool = False,
    compartments: float | None = None,
    thickness: float | None = None,
    skip_voxels: float | None = None,
    max_rotation: float | None = None,
) -> MriCc:
    """
    Build parameters.

    Args:
        subject_name: Subject name used in processing
        output_file: Write aseg including CC to SDIR/mri/<fname>
        aseg_file: Read aseg from SDIR/mri/<fname>
        norm_file: Read norm from SDIR/mri/<fname>
        sdir: Set SUBJECTS_DIR to <dname>
        rotation_lta: Write rotation lta to global <fname>
        force_flag: Process regardless of existing CC in input
        include_fornix: Include fornix in segmentation
        compartments: Subdivide into <int> compartments
        thickness: Setting CC thickness to <int> mm
        skip_voxels: Skipping <int> voxels in rotational align
        max_rotation: Set max of rotations to be searched (default=7deg)

    Returns:
        Parameter dictionary.
    """
    params: MriCc = {
        "@type": "freesurfer/mri_cc",
        "subject_name": subject_name,
        "force_flag": force_flag,
        "include_fornix": include_fornix,
    }
    if output_file is not None:
        params["output_file"] = output_file
    if aseg_file is not None:
        params["aseg_file"] = aseg_file
    if norm_file is not None:
        params["norm_file"] = norm_file
    if sdir is not None:
        params["sdir"] = sdir
    if rotation_lta is not None:
        params["rotation_lta"] = rotation_lta
    if compartments is not None:
        params["compartments"] = compartments
    if thickness is not None:
        params["thickness"] = thickness
    if skip_voxels is not None:
        params["skip_voxels"] = skip_voxels
    if max_rotation is not None:
        params["max_rotation"] = max_rotation
    return params

def mri_cc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("aseg_file", None) is not None:
        if not isinstance(params["aseg_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`aseg_file` has the wrong type: Received `{type(params["aseg_file"])}` expected `InputPathType`')
    if params.get("norm_file", None) is not None:
        if not isinstance(params["norm_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`norm_file` has the wrong type: Received `{type(params["norm_file"])}` expected `InputPathType`')
    if params.get("sdir", None) is not None:
        if not isinstance(params["sdir"], str):
            raise StyxValidationError(f'`sdir` has the wrong type: Received `{type(params["sdir"])}` expected `str`')
    if params.get("rotation_lta", None) is not None:
        if not isinstance(params["rotation_lta"], (pathlib.Path, str)):
            raise StyxValidationError(f'`rotation_lta` has the wrong type: Received `{type(params["rotation_lta"])}` expected `InputPathType`')
    if params.get("force_flag", None) is not None:
        if not isinstance(params["force_flag"], bool):
            raise StyxValidationError(f'`force_flag` has the wrong type: Received `{type(params["force_flag"])}` expected `bool`')
    if params.get("include_fornix", None) is not None:
        if not isinstance(params["include_fornix"], bool):
            raise StyxValidationError(f'`include_fornix` has the wrong type: Received `{type(params["include_fornix"])}` expected `bool`')
    if params.get("compartments", None) is not None:
        if not isinstance(params["compartments"], (float, int)):
            raise StyxValidationError(f'`compartments` has the wrong type: Received `{type(params["compartments"])}` expected `float`')
    if params.get("thickness", None) is not None:
        if not isinstance(params["thickness"], (float, int)):
            raise StyxValidationError(f'`thickness` has the wrong type: Received `{type(params["thickness"])}` expected `float`')
    if params.get("skip_voxels", None) is not None:
        if not isinstance(params["skip_voxels"], (float, int)):
            raise StyxValidationError(f'`skip_voxels` has the wrong type: Received `{type(params["skip_voxels"])}` expected `float`')
    if params.get("max_rotation", None) is not None:
        if not isinstance(params["max_rotation"], (float, int)):
            raise StyxValidationError(f'`max_rotation` has the wrong type: Received `{type(params["max_rotation"])}` expected `float`')

def mri_cc_cargs(params: MriCc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cc")
    cargs.append(params["subject_name"])
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("aseg_file")
    if v_1 is not None:
        cargs.append("-aseg")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("norm_file")
    if v_2 is not None:
        cargs.append("-norm")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("sdir")
    if v_3 is not None:
        cargs.append("-sdir")
        cargs.append(v_3)
    v_4 = params.get("rotation_lta")
    if v_4 is not None:
        cargs.append("-lta")
        cargs.append(execution.input_file(v_4))
    if params.get("force_flag"):
        cargs.append("-force")
    if params.get("include_fornix"):
        cargs.append("-f")
    v_5 = params.get("compartments")
    if v_5 is not None:
        cargs.append("-d")
        cargs.append(str(v_5))
    v_6 = params.get("thickness")
    if v_6 is not None:
        cargs.append("-t")
        cargs.append(str(v_6))
    v_7 = params.get("skip_voxels")
    if v_7 is not None:
        cargs.append("-s")
        cargs.append(str(v_7))
    v_8 = params.get("max_rotation")
    if v_8 is not None:
        cargs.append("-m")
        cargs.append(str(v_8))
    return cargs

def mri_cc_outputs(params: MriCc, execution: Execution) -> MriCcOutputs:
    """Build the MriCcOutputs object for this tool."""
    output_volume_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_volume_v = execution.output_file(f'SDIR/mri/{(__v0 if __v0 is not None else "")}')
    return MriCcOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def mri_cc_execute(params: MriCc, runner: Runner | None = None) -> MriCcOutputs:
    """
    mri_cc

    Segments the corpus callosum into five separate labels in the subcortical segmentation volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CC_METADATA)
    execution.params(params)
    args = mri_cc_cargs(params, execution)
    out = mri_cc_outputs(params, execution)
    execution.run(args)
    return out

def mri_cc(
    subject_name: str,
    output_file: str | None = None,
    aseg_file: InputPathType | None = None,
    norm_file: InputPathType | None = None,
    sdir: str | None = None,
    rotation_lta: InputPathType | None = None,
    force_flag: bool = False,
    include_fornix: bool = False,
    compartments: float | None = None,
    thickness: float | None = None,
    skip_voxels: float | None = None,
    max_rotation: float | None = None,
    runner: Runner | None = None,
) -> MriCcOutputs:
    """
    mri_cc

    Segments the corpus callosum into five separate labels in the subcortical segmentation volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name used in processing
        output_file: Write aseg including CC to SDIR/mri/<fname>
        aseg_file: Read aseg from SDIR/mri/<fname>
        norm_file: Read norm from SDIR/mri/<fname>
        sdir: Set SUBJECTS_DIR to <dname>
        rotation_lta: Write rotation lta to global <fname>
        force_flag: Process regardless of existing CC in input
        include_fornix: Include fornix in segmentation
        compartments: Subdivide into <int> compartments
        thickness: Setting CC thickness to <int> mm
        skip_voxels: Skipping <int> voxels in rotational align
        max_rotation: Set max of rotations to be searched (default=7deg)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cc_params(
        subject_name=subject_name,
        output_file=output_file,
        aseg_file=aseg_file,
        norm_file=norm_file,
        sdir=sdir,
        rotation_lta=rotation_lta,
        force_flag=force_flag,
        include_fornix=include_fornix,
        compartments=compartments,
        thickness=thickness,
        skip_voxels=skip_voxels,
        max_rotation=max_rotation,
    )
    return mri_cc_execute(params, runner)

__all__ = [
    "MriCc",
    "MriCcOutputs",
    "MRI_CC_METADATA",
    "mri_cc_cargs",
    "mri_cc_outputs",
    "mri_cc_params",
    "mri_cc_execute",
    "mri_cc_validate",
    "mri_cc",
]