# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BREC_METADATA = Metadata(
    id="brec",
    name="brec",
    package="freesurfer",
)

Brec = typing.TypedDict(
    "Brec",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/brec"]],
        "my_file": str,
        "depth_limit": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BrecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def brec_params(
    my_file: str,
    depth_limit: bool = False,
) -> Brec:
    """
    Build parameters.

    Args:
        my_file: Input file with .rec extension
        depth_limit: Optional depth limit flag

    Returns:
        Parameter dictionary.
    """
    params: Brec = {
        "@type": "freesurfer/brec",
        "my_file": my_file,
        "depth_limit": depth_limit,
    }
    return params

def brec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Brec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("my_file", None) is None:
        raise StyxValidationError("`my_file` must not be None")
    if not isinstance(params["my_file"], str):
        raise StyxValidationError(f'`my_file` has the wrong type: Received `{type(params["my_file"])}` expected `str`')
    if params.get("depth_limit", None) is not None:
        if not isinstance(params["depth_limit"], bool):
            raise StyxValidationError(f'`depth_limit` has the wrong type: Received `{type(params["depth_limit"])}` expected `bool`')

def brec_cargs(params: Brec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("brec")
    cargs.append(params["my_file"])
    if params.get("depth_limit"):
        cargs.append("-depth_limit")
    return cargs

def brec_outputs(params: Brec, execution: Execution) -> BrecOutputs:
    """Build the BrecOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BrecOutputs(
        root=root_v,
    )

def brec_execute(params: Brec, runner: Runner | None = None) -> BrecOutputs:
    """
    brec

    A description for brec tool could not be retrieved

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    brec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BREC_METADATA)
    execution.params(params)
    args = brec_cargs(params, execution)
    out = brec_outputs(params, execution)
    execution.run(args)
    return out

def brec(
    my_file: str,
    depth_limit: bool = False,
    runner: Runner | None = None,
) -> BrecOutputs:
    """
    brec

    A description for brec tool could not be retrieved

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        my_file: Input file with .rec extension
        depth_limit: Optional depth limit flag
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = brec_params(
        my_file=my_file,
        depth_limit=depth_limit,
    )
    return brec_execute(params, runner)

__all__ = [
    "Brec",
    "BrecOutputs",
    "BREC_METADATA",
    "brec_cargs",
    "brec_outputs",
    "brec_params",
    "brec_execute",
    "brec_validate",
    "brec",
]