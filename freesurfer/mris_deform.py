# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_DEFORM_METADATA = Metadata(
    id="mris_deform",
    name="mris_deform",
    package="freesurfer",
)

MrisDeform = typing.TypedDict(
    "MrisDeform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_deform"]],
        "input_surface": InputPathType,
        "input_volume": InputPathType,
        "xform": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class MrisDeformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    deformed_surface: OutputPathType
    """Deformed surface file output by mris_deform"""

def mris_deform_params(
    input_surface: InputPathType,
    input_volume: InputPathType,
    xform: InputPathType,
    output_surface: str,
) -> MrisDeform:
    """
    Build parameters.

    Args:
        input_surface: Input surface file (e.g., lh.white)
        input_volume: Input volume file (e.g., brain.mgz)
        xform: Transformation file (e.g., talairach.xfm)
        output_surface: Output surface file (e.g., lh.white.deformed)

    Returns:
        Parameter dictionary.
    """
    params: MrisDeform = {
        "@type": "freesurfer/mris_deform",
        "input_surface": input_surface,
        "input_volume": input_volume,
        "xform": xform,
        "output_surface": output_surface,
    }
    return params

def mris_deform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisDeform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("xform", None) is None:
        raise StyxValidationError("`xform` must not be None")
    if not isinstance(params["xform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def mris_deform_cargs(params: MrisDeform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_deform")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["xform"]))
    cargs.append(params["output_surface"])
    return cargs

def mris_deform_outputs(params: MrisDeform, execution: Execution) -> MrisDeformOutputs:
    """Build the MrisDeformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    deformed_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisDeformOutputs(
        root=root_v,
        deformed_surface=deformed_surface_v,
    )

def mris_deform_execute(params: MrisDeform, runner: Runner | None = None) -> MrisDeformOutputs:
    """
    mris_deform

    A tool for deforming surface meshes using volumetric information from an auxiliary volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_deform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_DEFORM_METADATA)
    execution.params(params)
    args = mris_deform_cargs(params, execution)
    out = mris_deform_outputs(params, execution)
    execution.run(args)
    return out

def mris_deform(
    input_surface: InputPathType,
    input_volume: InputPathType,
    xform: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> MrisDeformOutputs:
    """
    mris_deform

    A tool for deforming surface meshes using volumetric information from an auxiliary volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file (e.g., lh.white)
        input_volume: Input volume file (e.g., brain.mgz)
        xform: Transformation file (e.g., talairach.xfm)
        output_surface: Output surface file (e.g., lh.white.deformed)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_deform_params(
        input_surface=input_surface,
        input_volume=input_volume,
        xform=xform,
        output_surface=output_surface,
    )
    return mris_deform_execute(params, runner)

__all__ = [
    "MrisDeform",
    "MrisDeformOutputs",
    "MRIS_DEFORM_METADATA",
    "mris_deform_cargs",
    "mris_deform_outputs",
    "mris_deform_params",
    "mris_deform_execute",
    "mris_deform_validate",
    "mris_deform",
]