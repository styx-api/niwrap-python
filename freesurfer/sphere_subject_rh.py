# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHERE_SUBJECT_RH_METADATA = Metadata(
    id="sphere_subject-rh",
    name="sphere_subject-rh",
    package="freesurfer",
)

SphereSubjectRh = typing.TypedDict(
    "SphereSubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sphere_subject-rh"]],
        "license_file": InputPathType,
    },
)

@dataclasses.dataclass
class SphereSubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def sphere_subject_rh_params(
    license_file: InputPathType,
) -> SphereSubjectRh:
    """
    Build parameters.

    Args:
        license_file:

    Returns:
        Parameter dictionary.
    """
    params: SphereSubjectRh = {
        "@type": "freesurfer/sphere_subject-rh",
        "license_file": license_file,
    }
    return params

def sphere_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SphereSubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("license_file", None) is None:
        raise StyxValidationError("`license_file` must not be None")
    if not isinstance(params["license_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`license_file` has the wrong type: Received `{type(params["license_file"])}` expected `InputPathType`')

def sphere_subject_rh_cargs(params: SphereSubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sphere_subject-rh")
    cargs.append("-rh")
    cargs.append(execution.input_file(params["license_file"]))
    return cargs

def sphere_subject_rh_outputs(params: SphereSubjectRh, execution: Execution) -> SphereSubjectRhOutputs:
    """Build the SphereSubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SphereSubjectRhOutputs(
        root=root_v,
    )

def sphere_subject_rh_execute(params: SphereSubjectRh, runner: Runner | None = None) -> SphereSubjectRhOutputs:
    """
    sphere_subject-rh

    Sphere Subject RH tool for FreeSurfer, requires valid license.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sphere_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHERE_SUBJECT_RH_METADATA)
    execution.params(params)
    args = sphere_subject_rh_cargs(params, execution)
    out = sphere_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def sphere_subject_rh(
    license_file: InputPathType,
    runner: Runner | None = None,
) -> SphereSubjectRhOutputs:
    """
    sphere_subject-rh

    Sphere Subject RH tool for FreeSurfer, requires valid license.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        license_file:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sphere_subject_rh_params(
        license_file=license_file,
    )
    return sphere_subject_rh_execute(params, runner)

__all__ = [
    "SphereSubjectRh",
    "SphereSubjectRhOutputs",
    "SPHERE_SUBJECT_RH_METADATA",
    "sphere_subject_rh_cargs",
    "sphere_subject_rh_outputs",
    "sphere_subject_rh_params",
    "sphere_subject_rh_execute",
    "sphere_subject_rh_validate",
    "sphere_subject_rh",
]