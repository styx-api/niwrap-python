# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LABEL_VALS_METADATA = Metadata(
    id="mri_label_vals",
    name="mri_label_vals",
    package="freesurfer",
)

MriLabelVals = typing.TypedDict(
    "MriLabelVals",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_label_vals"]],
        "volume": InputPathType,
        "label_file": InputPathType,
        "cras_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriLabelValsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_label_vals_params(
    volume: InputPathType,
    label_file: InputPathType,
    cras_flag: bool = False,
    help_flag: bool = False,
) -> MriLabelVals:
    """
    Build parameters.

    Args:
        volume: Input volume file
        label_file: Input label file
        cras_flag: Use this option if the label was created where c_(r,a,s) != 0
        help_flag: Print help

    Returns:
        Parameter dictionary.
    """
    params: MriLabelVals = {
        "@type": "freesurfer/mri_label_vals",
        "volume": volume,
        "label_file": label_file,
        "cras_flag": cras_flag,
        "help_flag": help_flag,
    }
    return params

def mri_label_vals_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLabelVals."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("cras_flag", None) is not None:
        if not isinstance(params["cras_flag"], bool):
            raise StyxValidationError(f'`cras_flag` has the wrong type: Received `{type(params["cras_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def mri_label_vals_cargs(params: MriLabelVals, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_label_vals")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(execution.input_file(params["label_file"]))
    if params.get("cras_flag"):
        cargs.append("-cras")
    if params.get("help_flag"):
        cargs.append("-u")
    return cargs

def mri_label_vals_outputs(params: MriLabelVals, execution: Execution) -> MriLabelValsOutputs:
    """Build the MriLabelValsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriLabelValsOutputs(
        root=root_v,
    )

def mri_label_vals_execute(params: MriLabelVals, runner: Runner | None = None) -> MriLabelValsOutputs:
    """
    mri_label_vals

    Extract values at label coordinates from a volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_label_vals_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LABEL_VALS_METADATA)
    execution.params(params)
    args = mri_label_vals_cargs(params, execution)
    out = mri_label_vals_outputs(params, execution)
    execution.run(args)
    return out

def mri_label_vals(
    volume: InputPathType,
    label_file: InputPathType,
    cras_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> MriLabelValsOutputs:
    """
    mri_label_vals

    Extract values at label coordinates from a volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume: Input volume file
        label_file: Input label file
        cras_flag: Use this option if the label was created where c_(r,a,s) != 0
        help_flag: Print help
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_label_vals_params(
        volume=volume,
        label_file=label_file,
        cras_flag=cras_flag,
        help_flag=help_flag,
    )
    return mri_label_vals_execute(params, runner)

__all__ = [
    "MriLabelVals",
    "MriLabelValsOutputs",
    "MRI_LABEL_VALS_METADATA",
    "mri_label_vals_cargs",
    "mri_label_vals_outputs",
    "mri_label_vals_params",
    "mri_label_vals_execute",
    "mri_label_vals_validate",
    "mri_label_vals",
]