# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RIGID_REGISTER_METADATA = Metadata(
    id="mri_rigid_register",
    name="mri_rigid_register",
    package="freesurfer",
)

MriRigidRegister = typing.TypedDict(
    "MriRigidRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_rigid_register"]],
        "source_volume": InputPathType,
        "target_volume": InputPathType,
        "transform_output": str,
    },
)

@dataclasses.dataclass
class MriRigidRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transform_file: OutputPathType
    """Output file for transform matrix"""

def mri_rigid_register_params(
    source_volume: InputPathType,
    target_volume: InputPathType,
    transform_output: str,
) -> MriRigidRegister:
    """
    Build parameters.

    Args:
        source_volume: Source volume file for registration
        target_volume: Target volume file for registration
        transform_output: Output file name for the transform

    Returns:
        Parameter dictionary.
    """
    params: MriRigidRegister = {
        "@type": "freesurfer/mri_rigid_register",
        "source_volume": source_volume,
        "target_volume": target_volume,
        "transform_output": transform_output,
    }
    return params

def mri_rigid_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRigidRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_volume", None) is None:
        raise StyxValidationError("`source_volume` must not be None")
    if not isinstance(params["source_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["source_volume"])}` expected `InputPathType`')
    if params.get("target_volume", None) is None:
        raise StyxValidationError("`target_volume` must not be None")
    if not isinstance(params["target_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["target_volume"])}` expected `InputPathType`')
    if params.get("transform_output", None) is None:
        raise StyxValidationError("`transform_output` must not be None")
    if not isinstance(params["transform_output"], str):
        raise StyxValidationError(f'`transform_output` has the wrong type: Received `{type(params["transform_output"])}` expected `str`')

def mri_rigid_register_cargs(params: MriRigidRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_rigid_register")
    cargs.append(execution.input_file(params["source_volume"]))
    cargs.append(execution.input_file(params["target_volume"]))
    cargs.append(params["transform_output"])
    return cargs

def mri_rigid_register_outputs(params: MriRigidRegister, execution: Execution) -> MriRigidRegisterOutputs:
    """Build the MriRigidRegisterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transform_file_v: OutputPathType = execution.output_file(params["transform_output"])
    return MriRigidRegisterOutputs(
        root=root_v,
        transform_file=transform_file_v,
    )

def mri_rigid_register_execute(params: MriRigidRegister, runner: Runner | None = None) -> MriRigidRegisterOutputs:
    """
    mri_rigid_register

    Rigid registration tool for MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_rigid_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RIGID_REGISTER_METADATA)
    execution.params(params)
    args = mri_rigid_register_cargs(params, execution)
    out = mri_rigid_register_outputs(params, execution)
    execution.run(args)
    return out

def mri_rigid_register(
    source_volume: InputPathType,
    target_volume: InputPathType,
    transform_output: str,
    runner: Runner | None = None,
) -> MriRigidRegisterOutputs:
    """
    mri_rigid_register

    Rigid registration tool for MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_volume: Source volume file for registration
        target_volume: Target volume file for registration
        transform_output: Output file name for the transform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_rigid_register_params(
        source_volume=source_volume,
        target_volume=target_volume,
        transform_output=transform_output,
    )
    return mri_rigid_register_execute(params, runner)

__all__ = [
    "MriRigidRegister",
    "MriRigidRegisterOutputs",
    "MRI_RIGID_REGISTER_METADATA",
    "mri_rigid_register_cargs",
    "mri_rigid_register_outputs",
    "mri_rigid_register_params",
    "mri_rigid_register_execute",
    "mri_rigid_register_validate",
    "mri_rigid_register",
]