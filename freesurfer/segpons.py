# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGPONS_METADATA = Metadata(
    id="segpons",
    name="segpons",
    package="freesurfer",
)

Segpons = typing.TypedDict(
    "Segpons",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segpons"]],
        "subject": str,
        "aseg": typing.NotRequired[bool],
        "apas": typing.NotRequired[bool],
        "seg": typing.NotRequired[InputPathType],
        "no_refine": typing.NotRequired[bool],
        "pons152_mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SegponsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    pons_output: OutputPathType
    """Output pons segmentation file."""

def segpons_params(
    subject: str,
    aseg: bool = False,
    apas: bool = False,
    seg: InputPathType | None = None,
    no_refine: bool = False,
    pons152_mask: InputPathType | None = None,
) -> Segpons:
    """
    Build parameters.

    Args:
        subject: Subject identifier.
        aseg: Use and refine aseg (default, output will be aseg+pons.mgz).
        apas: Use aparc+aseg.mgz (output will be apas+pons.mgz).
        seg: Specify your own segmentation file.
        no_refine: Do not refine when using aseg.
        pons152_mask: Mask of pons in MNI152 space.

    Returns:
        Parameter dictionary.
    """
    params: Segpons = {
        "@type": "freesurfer/segpons",
        "subject": subject,
        "aseg": aseg,
        "apas": apas,
        "no_refine": no_refine,
    }
    if seg is not None:
        params["seg"] = seg
    if pons152_mask is not None:
        params["pons152_mask"] = pons152_mask
    return params

def segpons_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Segpons."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("aseg", None) is not None:
        if not isinstance(params["aseg"], bool):
            raise StyxValidationError(f'`aseg` has the wrong type: Received `{type(params["aseg"])}` expected `bool`')
    if params.get("apas", None) is not None:
        if not isinstance(params["apas"], bool):
            raise StyxValidationError(f'`apas` has the wrong type: Received `{type(params["apas"])}` expected `bool`')
    if params.get("seg", None) is not None:
        if not isinstance(params["seg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg` has the wrong type: Received `{type(params["seg"])}` expected `InputPathType`')
    if params.get("no_refine", None) is not None:
        if not isinstance(params["no_refine"], bool):
            raise StyxValidationError(f'`no_refine` has the wrong type: Received `{type(params["no_refine"])}` expected `bool`')
    if params.get("pons152_mask", None) is not None:
        if not isinstance(params["pons152_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`pons152_mask` has the wrong type: Received `{type(params["pons152_mask"])}` expected `InputPathType`')

def segpons_cargs(params: Segpons, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segpons")
    cargs.append("-s")
    cargs.append(params["subject"])
    if params.get("aseg"):
        cargs.append("--aseg")
    if params.get("apas"):
        cargs.append("--apas")
    v_0 = params.get("seg")
    if v_0 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_0))
    if params.get("no_refine"):
        cargs.append("--no-refine")
    v_1 = params.get("pons152_mask")
    if v_1 is not None:
        cargs.append("--pons152")
        cargs.append(execution.input_file(v_1))
    return cargs

def segpons_outputs(params: Segpons, execution: Execution) -> SegponsOutputs:
    """Build the SegponsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    pons_output_v: OutputPathType = execution.output_file(f'{params["subject"]}+pons.mgz')
    return SegponsOutputs(
        root=root_v,
        pons_output=pons_output_v,
    )

def segpons_execute(params: Segpons, runner: Runner | None = None) -> SegponsOutputs:
    """
    segpons

    Approximate segmentation of pons using MNI152 space registration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segpons_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGPONS_METADATA)
    execution.params(params)
    args = segpons_cargs(params, execution)
    out = segpons_outputs(params, execution)
    execution.run(args)
    return out

def segpons(
    subject: str,
    aseg: bool = False,
    apas: bool = False,
    seg: InputPathType | None = None,
    no_refine: bool = False,
    pons152_mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> SegponsOutputs:
    """
    segpons

    Approximate segmentation of pons using MNI152 space registration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier.
        aseg: Use and refine aseg (default, output will be aseg+pons.mgz).
        apas: Use aparc+aseg.mgz (output will be apas+pons.mgz).
        seg: Specify your own segmentation file.
        no_refine: Do not refine when using aseg.
        pons152_mask: Mask of pons in MNI152 space.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segpons_params(
        subject=subject,
        aseg=aseg,
        apas=apas,
        seg=seg,
        no_refine=no_refine,
        pons152_mask=pons152_mask,
    )
    return segpons_execute(params, runner)

__all__ = [
    "Segpons",
    "SegponsOutputs",
    "SEGPONS_METADATA",
    "segpons_cargs",
    "segpons_outputs",
    "segpons_params",
    "segpons_execute",
    "segpons_validate",
    "segpons",
]