# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_RUN_FROM_MCR_METADATA = Metadata(
    id="fs_run_from_mcr",
    name="fs_run_from_mcr",
    package="freesurfer",
)

FsRunFromMcr = typing.TypedDict(
    "FsRunFromMcr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_run_from_mcr"]],
        "name": typing.NotRequired[str],
        "command": typing.NotRequired[str],
        "zeroth_flag": typing.NotRequired[bool],
        "empty_env_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsRunFromMcrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fs_run_from_mcr_params(
    name: str | None = None,
    command: str | None = None,
    zeroth_flag: bool = False,
    empty_env_flag: bool = False,
) -> FsRunFromMcr:
    """
    Build parameters.

    Args:
        name: Pass NAME as the zeroth argument to COMMAND
        command: The command to execute
        zeroth_flag: Place a dash in the zeroth argument to COMMAND
        empty_env_flag: Execute COMMAND with an empty environment

    Returns:
        Parameter dictionary.
    """
    params: FsRunFromMcr = {
        "@type": "freesurfer/fs_run_from_mcr",
        "zeroth_flag": zeroth_flag,
        "empty_env_flag": empty_env_flag,
    }
    if name is not None:
        params["name"] = name
    if command is not None:
        params["command"] = command
    return params

def fs_run_from_mcr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsRunFromMcr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("command", None) is not None:
        if not isinstance(params["command"], str):
            raise StyxValidationError(f'`command` has the wrong type: Received `{type(params["command"])}` expected `str`')
    if params.get("zeroth_flag", None) is not None:
        if not isinstance(params["zeroth_flag"], bool):
            raise StyxValidationError(f'`zeroth_flag` has the wrong type: Received `{type(params["zeroth_flag"])}` expected `bool`')
    if params.get("empty_env_flag", None) is not None:
        if not isinstance(params["empty_env_flag"], bool):
            raise StyxValidationError(f'`empty_env_flag` has the wrong type: Received `{type(params["empty_env_flag"])}` expected `bool`')

def fs_run_from_mcr_cargs(params: FsRunFromMcr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_run_from_mcr")
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(v_0)
    v_1 = params.get("command")
    if v_1 is not None:
        cargs.append(v_1)
    if params.get("zeroth_flag"):
        cargs.append("-l")
    if params.get("empty_env_flag"):
        cargs.append("-c")
    return cargs

def fs_run_from_mcr_outputs(params: FsRunFromMcr, execution: Execution) -> FsRunFromMcrOutputs:
    """Build the FsRunFromMcrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsRunFromMcrOutputs(
        root=root_v,
    )

def fs_run_from_mcr_execute(params: FsRunFromMcr, runner: Runner | None = None) -> FsRunFromMcrOutputs:
    """
    fs_run_from_mcr

    Replace the shell with the given command.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_run_from_mcr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_RUN_FROM_MCR_METADATA)
    execution.params(params)
    args = fs_run_from_mcr_cargs(params, execution)
    out = fs_run_from_mcr_outputs(params, execution)
    execution.run(args)
    return out

def fs_run_from_mcr(
    name: str | None = None,
    command: str | None = None,
    zeroth_flag: bool = False,
    empty_env_flag: bool = False,
    runner: Runner | None = None,
) -> FsRunFromMcrOutputs:
    """
    fs_run_from_mcr

    Replace the shell with the given command.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        name: Pass NAME as the zeroth argument to COMMAND
        command: The command to execute
        zeroth_flag: Place a dash in the zeroth argument to COMMAND
        empty_env_flag: Execute COMMAND with an empty environment
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_run_from_mcr_params(
        name=name,
        command=command,
        zeroth_flag=zeroth_flag,
        empty_env_flag=empty_env_flag,
    )
    return fs_run_from_mcr_execute(params, runner)

__all__ = [
    "FsRunFromMcr",
    "FsRunFromMcrOutputs",
    "FS_RUN_FROM_MCR_METADATA",
    "fs_run_from_mcr_cargs",
    "fs_run_from_mcr_outputs",
    "fs_run_from_mcr_params",
    "fs_run_from_mcr_execute",
    "fs_run_from_mcr_validate",
    "fs_run_from_mcr",
]