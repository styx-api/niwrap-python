# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WFILEMASK_METADATA = Metadata(
    id="wfilemask",
    name="wfilemask",
    package="freesurfer",
)

Wfilemask = typing.TypedDict(
    "Wfilemask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/wfilemask"]],
        "w_file": InputPathType,
        "label_file": InputPathType,
        "output_file": str,
        "permission_mask": typing.NotRequired[str],
        "help_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class WfilemaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_w_file: OutputPathType
    """Output file with specified label regions zeroed out"""

def wfilemask_params(
    w_file: InputPathType,
    label_file: InputPathType,
    output_file: str,
    permission_mask: str | None = None,
    help_flag: bool = False,
    version_flag: bool = False,
) -> Wfilemask:
    """
    Build parameters.

    Args:
        w_file: Input w file
        label_file: Label file to use as a mask
        output_file: Output w file
        permission_mask: Set Unix file permission mask
        help_flag: Display help message and exit
        version_flag: Print version and exit

    Returns:
        Parameter dictionary.
    """
    params: Wfilemask = {
        "@type": "freesurfer/wfilemask",
        "w_file": w_file,
        "label_file": label_file,
        "output_file": output_file,
        "help_flag": help_flag,
        "version_flag": version_flag,
    }
    if permission_mask is not None:
        params["permission_mask"] = permission_mask
    return params

def wfilemask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Wfilemask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("w_file", None) is None:
        raise StyxValidationError("`w_file` must not be None")
    if not isinstance(params["w_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`w_file` has the wrong type: Received `{type(params["w_file"])}` expected `InputPathType`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("permission_mask", None) is not None:
        if not isinstance(params["permission_mask"], str):
            raise StyxValidationError(f'`permission_mask` has the wrong type: Received `{type(params["permission_mask"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def wfilemask_cargs(params: Wfilemask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wfilemask")
    cargs.append("-w")
    cargs.append(execution.input_file(params["w_file"]))
    cargs.append("-l")
    cargs.append(execution.input_file(params["label_file"]))
    cargs.append("-o")
    cargs.append(params["output_file"])
    v_0 = params.get("permission_mask")
    if v_0 is not None:
        cargs.append("-umask")
        cargs.append(v_0)
    if params.get("help_flag"):
        cargs.append("-help")
    if params.get("version_flag"):
        cargs.append("-version")
    return cargs

def wfilemask_outputs(params: Wfilemask, execution: Execution) -> WfilemaskOutputs:
    """Build the WfilemaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_w_file_v: OutputPathType = execution.output_file(params["output_file"])
    return WfilemaskOutputs(
        root=root_v,
        output_w_file=output_w_file_v,
    )

def wfilemask_execute(params: Wfilemask, runner: Runner | None = None) -> WfilemaskOutputs:
    """
    wfilemask

    Zero-out regions of a surface value file (.w file) using a label.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    wfilemask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WFILEMASK_METADATA)
    execution.params(params)
    args = wfilemask_cargs(params, execution)
    out = wfilemask_outputs(params, execution)
    execution.run(args)
    return out

def wfilemask(
    w_file: InputPathType,
    label_file: InputPathType,
    output_file: str,
    permission_mask: str | None = None,
    help_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> WfilemaskOutputs:
    """
    wfilemask

    Zero-out regions of a surface value file (.w file) using a label.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        w_file: Input w file
        label_file: Label file to use as a mask
        output_file: Output w file
        permission_mask: Set Unix file permission mask
        help_flag: Display help message and exit
        version_flag: Print version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = wfilemask_params(
        w_file=w_file,
        label_file=label_file,
        output_file=output_file,
        permission_mask=permission_mask,
        help_flag=help_flag,
        version_flag=version_flag,
    )
    return wfilemask_execute(params, runner)

__all__ = [
    "Wfilemask",
    "WfilemaskOutputs",
    "WFILEMASK_METADATA",
    "wfilemask_cargs",
    "wfilemask_outputs",
    "wfilemask_params",
    "wfilemask_execute",
    "wfilemask_validate",
    "wfilemask",
]