# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_STOPMASK_METADATA = Metadata(
    id="mri_stopmask",
    name="mri_stopmask",
    package="freesurfer",
)

MriStopmask = typing.TypedDict(
    "MriStopmask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_stopmask"]],
        "output_mask": str,
        "filled": list[InputPathType],
        "aseg_presurf": InputPathType,
        "lateral_ventricles": typing.NotRequired[bool],
        "wmsa": typing.NotRequired[float],
        "wm_voxels": typing.NotRequired[InputPathType],
        "brain_final_surfs": typing.NotRequired[InputPathType],
        "no_filled": typing.NotRequired[bool],
        "no_lv": typing.NotRequired[bool],
        "no_wmsa": typing.NotRequired[bool],
        "no_wm": typing.NotRequired[bool],
        "no_bfs": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriStopmaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    generated_mask: OutputPathType
    """The generated stop mask output file"""

def mri_stopmask_params(
    output_mask: str,
    filled: list[InputPathType],
    aseg_presurf: InputPathType,
    lateral_ventricles: bool = False,
    wmsa: float | None = None,
    wm_voxels: InputPathType | None = None,
    brain_final_surfs: InputPathType | None = None,
    no_filled: bool = False,
    no_lv: bool = False,
    no_wmsa: bool = False,
    no_wm: bool = False,
    no_bfs: bool = False,
) -> MriStopmask:
    """
    Build parameters.

    Args:
        output_mask: Output stop mask in volume format
        filled: Include voxels edited fill voxels that are set in the filled.mgz
        aseg_presurf: Used with --lv and/or --wmsa; Note: must be aseg.presurf.mgz,\
            not aseg.mgz
        lateral_ventricles: Add lateral ventricles and choroid plexus to the mask\
            (needs --aseg)
        wmsa: Add WM hypointensities to the mask (needs --aseg); erode by given\
            distance away from any adjacent cortex
        wm_voxels: Include voxels that =255 from wm.mgz
        brain_final_surfs: Include voxels that =255 from brain.finalsurfs.mgz
        no_filled: Turns off --filled option
        no_lv: Turns off --lv option
        no_wmsa: Turns off --wmsa option
        no_wm: Turns off --wm option
        no_bfs: Turns off --bfs option

    Returns:
        Parameter dictionary.
    """
    params: MriStopmask = {
        "@type": "freesurfer/mri_stopmask",
        "output_mask": output_mask,
        "filled": filled,
        "aseg_presurf": aseg_presurf,
        "lateral_ventricles": lateral_ventricles,
        "no_filled": no_filled,
        "no_lv": no_lv,
        "no_wmsa": no_wmsa,
        "no_wm": no_wm,
        "no_bfs": no_bfs,
    }
    if wmsa is not None:
        params["wmsa"] = wmsa
    if wm_voxels is not None:
        params["wm_voxels"] = wm_voxels
    if brain_final_surfs is not None:
        params["brain_final_surfs"] = brain_final_surfs
    return params

def mri_stopmask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriStopmask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_mask", None) is None:
        raise StyxValidationError("`output_mask` must not be None")
    if not isinstance(params["output_mask"], str):
        raise StyxValidationError(f'`output_mask` has the wrong type: Received `{type(params["output_mask"])}` expected `str`')
    if params.get("filled", None) is None:
        raise StyxValidationError("`filled` must not be None")
    if not isinstance(params["filled"], list):
        raise StyxValidationError(f'`filled` has the wrong type: Received `{type(params["filled"])}` expected `list[InputPathType]`')
    if len(params["filled"]) > 2:
        raise StyxValidationError("Parameter `filled` must contain at most 2 elements")
    for e in params["filled"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`filled` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("aseg_presurf", None) is None:
        raise StyxValidationError("`aseg_presurf` must not be None")
    if not isinstance(params["aseg_presurf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`aseg_presurf` has the wrong type: Received `{type(params["aseg_presurf"])}` expected `InputPathType`')
    if params.get("lateral_ventricles", None) is not None:
        if not isinstance(params["lateral_ventricles"], bool):
            raise StyxValidationError(f'`lateral_ventricles` has the wrong type: Received `{type(params["lateral_ventricles"])}` expected `bool`')
    if params.get("wmsa", None) is not None:
        if not isinstance(params["wmsa"], (float, int)):
            raise StyxValidationError(f'`wmsa` has the wrong type: Received `{type(params["wmsa"])}` expected `float`')
    if params.get("wm_voxels", None) is not None:
        if not isinstance(params["wm_voxels"], (pathlib.Path, str)):
            raise StyxValidationError(f'`wm_voxels` has the wrong type: Received `{type(params["wm_voxels"])}` expected `InputPathType`')
    if params.get("brain_final_surfs", None) is not None:
        if not isinstance(params["brain_final_surfs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`brain_final_surfs` has the wrong type: Received `{type(params["brain_final_surfs"])}` expected `InputPathType`')
    if params.get("no_filled", None) is not None:
        if not isinstance(params["no_filled"], bool):
            raise StyxValidationError(f'`no_filled` has the wrong type: Received `{type(params["no_filled"])}` expected `bool`')
    if params.get("no_lv", None) is not None:
        if not isinstance(params["no_lv"], bool):
            raise StyxValidationError(f'`no_lv` has the wrong type: Received `{type(params["no_lv"])}` expected `bool`')
    if params.get("no_wmsa", None) is not None:
        if not isinstance(params["no_wmsa"], bool):
            raise StyxValidationError(f'`no_wmsa` has the wrong type: Received `{type(params["no_wmsa"])}` expected `bool`')
    if params.get("no_wm", None) is not None:
        if not isinstance(params["no_wm"], bool):
            raise StyxValidationError(f'`no_wm` has the wrong type: Received `{type(params["no_wm"])}` expected `bool`')
    if params.get("no_bfs", None) is not None:
        if not isinstance(params["no_bfs"], bool):
            raise StyxValidationError(f'`no_bfs` has the wrong type: Received `{type(params["no_bfs"])}` expected `bool`')

def mri_stopmask_cargs(params: MriStopmask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_stopmask")
    cargs.append("--o")
    cargs.append(params["output_mask"])
    cargs.append("--filled")
    for item0 in params["filled"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--aseg")
    cargs.append(execution.input_file(params["aseg_presurf"]))
    if params.get("lateral_ventricles"):
        cargs.append("--lv")
    v_0 = params.get("wmsa")
    if v_0 is not None:
        cargs.append("--wmsa")
        cargs.append(str(v_0))
    v_1 = params.get("wm_voxels")
    if v_1 is not None:
        cargs.append("--wm")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("brain_final_surfs")
    if v_2 is not None:
        cargs.append("--bfs")
        cargs.append(execution.input_file(v_2))
    if params.get("no_filled"):
        cargs.append("--no-filled")
    if params.get("no_lv"):
        cargs.append("--no-lv")
    if params.get("no_wmsa"):
        cargs.append("--no-wmsa")
    if params.get("no_wm"):
        cargs.append("--no-wm")
    if params.get("no_bfs"):
        cargs.append("--no-bfs")
    return cargs

def mri_stopmask_outputs(params: MriStopmask, execution: Execution) -> MriStopmaskOutputs:
    """Build the MriStopmaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    generated_mask_v: OutputPathType = execution.output_file(params["output_mask"])
    return MriStopmaskOutputs(
        root=root_v,
        generated_mask=generated_mask_v,
    )

def mri_stopmask_execute(params: MriStopmask, runner: Runner | None = None) -> MriStopmaskOutputs:
    """
    mri_stopmask

    This program creates a mask used to stop the search for the maximum gradient in mris_place_surface, preventing the surface from wandering into areas it should not.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_stopmask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_STOPMASK_METADATA)
    execution.params(params)
    args = mri_stopmask_cargs(params, execution)
    out = mri_stopmask_outputs(params, execution)
    execution.run(args)
    return out

def mri_stopmask(
    output_mask: str,
    filled: list[InputPathType],
    aseg_presurf: InputPathType,
    lateral_ventricles: bool = False,
    wmsa: float | None = None,
    wm_voxels: InputPathType | None = None,
    brain_final_surfs: InputPathType | None = None,
    no_filled: bool = False,
    no_lv: bool = False,
    no_wmsa: bool = False,
    no_wm: bool = False,
    no_bfs: bool = False,
    runner: Runner | None = None,
) -> MriStopmaskOutputs:
    """
    mri_stopmask

    This program creates a mask used to stop the search for the maximum gradient in mris_place_surface, preventing the surface from wandering into areas it should not.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_mask: Output stop mask in volume format
        filled: Include voxels edited fill voxels that are set in the filled.mgz
        aseg_presurf: Used with --lv and/or --wmsa; Note: must be aseg.presurf.mgz,\
            not aseg.mgz
        lateral_ventricles: Add lateral ventricles and choroid plexus to the mask\
            (needs --aseg)
        wmsa: Add WM hypointensities to the mask (needs --aseg); erode by given\
            distance away from any adjacent cortex
        wm_voxels: Include voxels that =255 from wm.mgz
        brain_final_surfs: Include voxels that =255 from brain.finalsurfs.mgz
        no_filled: Turns off --filled option
        no_lv: Turns off --lv option
        no_wmsa: Turns off --wmsa option
        no_wm: Turns off --wm option
        no_bfs: Turns off --bfs option
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_stopmask_params(
        output_mask=output_mask,
        filled=filled,
        aseg_presurf=aseg_presurf,
        lateral_ventricles=lateral_ventricles,
        wmsa=wmsa,
        wm_voxels=wm_voxels,
        brain_final_surfs=brain_final_surfs,
        no_filled=no_filled,
        no_lv=no_lv,
        no_wmsa=no_wmsa,
        no_wm=no_wm,
        no_bfs=no_bfs,
    )
    return mri_stopmask_execute(params, runner)

__all__ = [
    "MriStopmask",
    "MriStopmaskOutputs",
    "MRI_STOPMASK_METADATA",
    "mri_stopmask_cargs",
    "mri_stopmask_outputs",
    "mri_stopmask_params",
    "mri_stopmask_execute",
    "mri_stopmask_validate",
    "mri_stopmask",
]