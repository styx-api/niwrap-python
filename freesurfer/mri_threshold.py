# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_THRESHOLD_METADATA = Metadata(
    id="mri_threshold",
    name="mri_threshold",
    package="freesurfer",
)

MriThreshold = typing.TypedDict(
    "MriThreshold",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_threshold"]],
        "input_vol": InputPathType,
        "threshold": float,
        "output_vol": str,
        "binarize": typing.NotRequired[float],
        "upper_threshold": typing.NotRequired[bool],
        "frame_number": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriThresholdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vol_file: OutputPathType
    """Thresholded output volume"""

def mri_threshold_params(
    input_vol: InputPathType,
    threshold: float,
    output_vol: str,
    binarize: float | None = None,
    upper_threshold: bool = False,
    frame_number: float | None = None,
) -> MriThreshold:
    """
    Build parameters.

    Args:
        input_vol: Input volume file
        threshold: Threshold value for the volume
        output_vol: Output volume file
        binarize: Binarize the output volume with specified bval
        upper_threshold: Upper threshold the volume instead of lower thresholding
        frame_number: Apply thresholding to a specific frame indexed by fnum

    Returns:
        Parameter dictionary.
    """
    params: MriThreshold = {
        "@type": "freesurfer/mri_threshold",
        "input_vol": input_vol,
        "threshold": threshold,
        "output_vol": output_vol,
        "upper_threshold": upper_threshold,
    }
    if binarize is not None:
        params["binarize"] = binarize
    if frame_number is not None:
        params["frame_number"] = frame_number
    return params

def mri_threshold_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriThreshold."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_vol", None) is None:
        raise StyxValidationError("`input_vol` must not be None")
    if not isinstance(params["input_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vol` has the wrong type: Received `{type(params["input_vol"])}` expected `InputPathType`')
    if params.get("threshold", None) is None:
        raise StyxValidationError("`threshold` must not be None")
    if not isinstance(params["threshold"], (float, int)):
        raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("output_vol", None) is None:
        raise StyxValidationError("`output_vol` must not be None")
    if not isinstance(params["output_vol"], str):
        raise StyxValidationError(f'`output_vol` has the wrong type: Received `{type(params["output_vol"])}` expected `str`')
    if params.get("binarize", None) is not None:
        if not isinstance(params["binarize"], (float, int)):
            raise StyxValidationError(f'`binarize` has the wrong type: Received `{type(params["binarize"])}` expected `float`')
    if params.get("upper_threshold", None) is not None:
        if not isinstance(params["upper_threshold"], bool):
            raise StyxValidationError(f'`upper_threshold` has the wrong type: Received `{type(params["upper_threshold"])}` expected `bool`')
    if params.get("frame_number", None) is not None:
        if not isinstance(params["frame_number"], (float, int)):
            raise StyxValidationError(f'`frame_number` has the wrong type: Received `{type(params["frame_number"])}` expected `float`')

def mri_threshold_cargs(params: MriThreshold, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_threshold")
    cargs.append(execution.input_file(params["input_vol"]))
    cargs.append(str(params["threshold"]))
    cargs.append(params["output_vol"])
    v_0 = params.get("binarize")
    if v_0 is not None:
        cargs.append("-B")
        cargs.append(str(v_0))
    if params.get("upper_threshold"):
        cargs.append("-U")
    v_1 = params.get("frame_number")
    if v_1 is not None:
        cargs.append("-F")
        cargs.append(str(v_1))
    return cargs

def mri_threshold_outputs(params: MriThreshold, execution: Execution) -> MriThresholdOutputs:
    """Build the MriThresholdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_vol_file_v: OutputPathType = execution.output_file(params["output_vol"])
    return MriThresholdOutputs(
        root=root_v,
        output_vol_file=output_vol_file_v,
    )

def mri_threshold_execute(params: MriThreshold, runner: Runner | None = None) -> MriThresholdOutputs:
    """
    mri_threshold

    This program will lower threshold an input volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_threshold_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_THRESHOLD_METADATA)
    execution.params(params)
    args = mri_threshold_cargs(params, execution)
    out = mri_threshold_outputs(params, execution)
    execution.run(args)
    return out

def mri_threshold(
    input_vol: InputPathType,
    threshold: float,
    output_vol: str,
    binarize: float | None = None,
    upper_threshold: bool = False,
    frame_number: float | None = None,
    runner: Runner | None = None,
) -> MriThresholdOutputs:
    """
    mri_threshold

    This program will lower threshold an input volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_vol: Input volume file
        threshold: Threshold value for the volume
        output_vol: Output volume file
        binarize: Binarize the output volume with specified bval
        upper_threshold: Upper threshold the volume instead of lower thresholding
        frame_number: Apply thresholding to a specific frame indexed by fnum
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_threshold_params(
        input_vol=input_vol,
        threshold=threshold,
        output_vol=output_vol,
        binarize=binarize,
        upper_threshold=upper_threshold,
        frame_number=frame_number,
    )
    return mri_threshold_execute(params, runner)

__all__ = [
    "MriThreshold",
    "MriThresholdOutputs",
    "MRI_THRESHOLD_METADATA",
    "mri_threshold_cargs",
    "mri_threshold_outputs",
    "mri_threshold_params",
    "mri_threshold_execute",
    "mri_threshold_validate",
    "mri_threshold",
]