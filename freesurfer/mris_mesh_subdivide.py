# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MESH_SUBDIVIDE_METADATA = Metadata(
    id="mris_mesh_subdivide",
    name="mris_mesh_subdivide",
    package="freesurfer",
)

MrisMeshSubdivideSubdivisionMethod = typing.Literal["butterfly"] | typing.Literal["loop"] | typing.Literal["linear"]

MrisMeshSubdivide = typing.TypedDict(
    "MrisMeshSubdivide",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_mesh_subdivide"]],
        "input_surface": InputPathType,
        "output_surface": str,
        "subdivision_method": typing.NotRequired[MrisMeshSubdivideSubdivisionMethod],
        "iterations": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisMeshSubdivideOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    subdivided_surface: OutputPathType
    """The subdivided mesh surface output file"""

def mris_mesh_subdivide_params(
    input_surface: InputPathType,
    output_surface: str,
    subdivision_method: MrisMeshSubdivideSubdivisionMethod | None = None,
    iterations: float | None = None,
) -> MrisMeshSubdivide:
    """
    Build parameters.

    Args:
        input_surface: Name of input surface file
        output_surface: Name for output surface file (outputs to same directory as\
            input if path not provided)
        subdivision_method: Subdivision method: options are 'butterfly' (default),\
            'loop', or 'linear'
        iterations: Number of subdivision iterations

    Returns:
        Parameter dictionary.
    """
    params: MrisMeshSubdivide = {
        "@type": "freesurfer/mris_mesh_subdivide",
        "input_surface": input_surface,
        "output_surface": output_surface,
    }
    if subdivision_method is not None:
        params["subdivision_method"] = subdivision_method
    if iterations is not None:
        params["iterations"] = iterations
    return params

def mris_mesh_subdivide_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMeshSubdivide."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("subdivision_method", None) is not None:
        if not isinstance(params["subdivision_method"], str):
            raise StyxValidationError(f'`subdivision_method` has the wrong type: Received `{type(params["subdivision_method"])}` expected `MrisMeshSubdivideSubdivisionMethod`')
        if params["subdivision_method"] not in ["butterfly", "loop", "linear"]:
            raise StyxValidationError("Parameter `subdivision_method` must be one of [\"butterfly\", \"loop\", \"linear\"]")
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], (float, int)):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `float`')

def mris_mesh_subdivide_cargs(params: MrisMeshSubdivide, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_mesh_subdivide")
    cargs.append("--surf")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append("--out")
    cargs.append(params["output_surface"])
    v_0 = params.get("subdivision_method")
    if v_0 is not None:
        cargs.append("--method")
        cargs.append(str(v_0))
    v_1 = params.get("iterations")
    if v_1 is not None:
        cargs.append("--iter")
        cargs.append(str(v_1))
    return cargs

def mris_mesh_subdivide_outputs(params: MrisMeshSubdivide, execution: Execution) -> MrisMeshSubdivideOutputs:
    """Build the MrisMeshSubdivideOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    subdivided_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisMeshSubdivideOutputs(
        root=root_v,
        subdivided_surface=subdivided_surface_v,
    )

def mris_mesh_subdivide_execute(params: MrisMeshSubdivide, runner: Runner | None = None) -> MrisMeshSubdivideOutputs:
    """
    mris_mesh_subdivide

    This program will subdivide a triangular mesh surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_mesh_subdivide_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MESH_SUBDIVIDE_METADATA)
    execution.params(params)
    args = mris_mesh_subdivide_cargs(params, execution)
    out = mris_mesh_subdivide_outputs(params, execution)
    execution.run(args)
    return out

def mris_mesh_subdivide(
    input_surface: InputPathType,
    output_surface: str,
    subdivision_method: MrisMeshSubdivideSubdivisionMethod | None = None,
    iterations: float | None = None,
    runner: Runner | None = None,
) -> MrisMeshSubdivideOutputs:
    """
    mris_mesh_subdivide

    This program will subdivide a triangular mesh surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Name of input surface file
        output_surface: Name for output surface file (outputs to same directory as\
            input if path not provided)
        subdivision_method: Subdivision method: options are 'butterfly' (default),\
            'loop', or 'linear'
        iterations: Number of subdivision iterations
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_mesh_subdivide_params(
        input_surface=input_surface,
        output_surface=output_surface,
        subdivision_method=subdivision_method,
        iterations=iterations,
    )
    return mris_mesh_subdivide_execute(params, runner)

__all__ = [
    "MrisMeshSubdivide",
    "MrisMeshSubdivideOutputs",
    "MRIS_MESH_SUBDIVIDE_METADATA",
    "mris_mesh_subdivide_cargs",
    "mris_mesh_subdivide_outputs",
    "mris_mesh_subdivide_params",
    "mris_mesh_subdivide_execute",
    "mris_mesh_subdivide_validate",
    "mris_mesh_subdivide",
]