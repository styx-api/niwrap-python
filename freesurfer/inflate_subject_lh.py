# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INFLATE_SUBJECT_LH_METADATA = Metadata(
    id="inflate_subject-lh",
    name="inflate_subject-lh",
    package="freesurfer",
)

InflateSubjectLh = typing.TypedDict(
    "InflateSubjectLh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/inflate_subject-lh"]],
        "input_folder": str,
        "hostname_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class InflateSubjectLhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def inflate_subject_lh_params(
    input_folder: str,
    hostname_flag: bool = False,
) -> InflateSubjectLh:
    """
    Build parameters.

    Args:
        input_folder: Input folder containing subject data
        hostname_flag: Flag to check hostname

    Returns:
        Parameter dictionary.
    """
    params: InflateSubjectLh = {
        "@type": "freesurfer/inflate_subject-lh",
        "input_folder": input_folder,
        "hostname_flag": hostname_flag,
    }
    return params

def inflate_subject_lh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid InflateSubjectLh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_folder", None) is None:
        raise StyxValidationError("`input_folder` must not be None")
    if not isinstance(params["input_folder"], str):
        raise StyxValidationError(f'`input_folder` has the wrong type: Received `{type(params["input_folder"])}` expected `str`')
    if params.get("hostname_flag", None) is not None:
        if not isinstance(params["hostname_flag"], bool):
            raise StyxValidationError(f'`hostname_flag` has the wrong type: Received `{type(params["hostname_flag"])}` expected `bool`')

def inflate_subject_lh_cargs(params: InflateSubjectLh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("inflate_subject-lh")
    cargs.append("-lh")
    cargs.append(params["input_folder"])
    if params.get("hostname_flag"):
        cargs.append("hostname")
    return cargs

def inflate_subject_lh_outputs(params: InflateSubjectLh, execution: Execution) -> InflateSubjectLhOutputs:
    """Build the InflateSubjectLhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return InflateSubjectLhOutputs(
        root=root_v,
    )

def inflate_subject_lh_execute(params: InflateSubjectLh, runner: Runner | None = None) -> InflateSubjectLhOutputs:
    """
    inflate_subject-lh

    A tool to process and inflate left hemisphere subject data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    inflate_subject_lh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INFLATE_SUBJECT_LH_METADATA)
    execution.params(params)
    args = inflate_subject_lh_cargs(params, execution)
    out = inflate_subject_lh_outputs(params, execution)
    execution.run(args)
    return out

def inflate_subject_lh(
    input_folder: str,
    hostname_flag: bool = False,
    runner: Runner | None = None,
) -> InflateSubjectLhOutputs:
    """
    inflate_subject-lh

    A tool to process and inflate left hemisphere subject data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_folder: Input folder containing subject data
        hostname_flag: Flag to check hostname
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = inflate_subject_lh_params(
        input_folder=input_folder,
        hostname_flag=hostname_flag,
    )
    return inflate_subject_lh_execute(params, runner)

__all__ = [
    "InflateSubjectLh",
    "InflateSubjectLhOutputs",
    "INFLATE_SUBJECT_LH_METADATA",
    "inflate_subject_lh_cargs",
    "inflate_subject_lh_outputs",
    "inflate_subject_lh_params",
    "inflate_subject_lh_execute",
    "inflate_subject_lh_validate",
    "inflate_subject_lh",
]