# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPILE_EDITS_METADATA = Metadata(
    id="mri_compile_edits",
    name="mri_compile_edits",
    package="freesurfer",
)

MriCompileEdits = typing.TypedDict(
    "MriCompileEdits",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compile_edits"]],
        "subject_name": str,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriCompileEditsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    compiled_edit_volume: OutputPathType
    """Compiled volume showing all the edits"""

def mri_compile_edits_params(
    subject_name: str,
    output_volume: str,
) -> MriCompileEdits:
    """
    Build parameters.

    Args:
        subject_name: Subject name whose edits are to be compiled into a volume
        output_volume: Output volume file

    Returns:
        Parameter dictionary.
    """
    params: MriCompileEdits = {
        "@type": "freesurfer/mri_compile_edits",
        "subject_name": subject_name,
        "output_volume": output_volume,
    }
    return params

def mri_compile_edits_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCompileEdits."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_compile_edits_cargs(params: MriCompileEdits, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compile_edits")
    cargs.append(params["subject_name"])
    cargs.append(params["output_volume"])
    return cargs

def mri_compile_edits_outputs(params: MriCompileEdits, execution: Execution) -> MriCompileEditsOutputs:
    """Build the MriCompileEditsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    compiled_edit_volume_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriCompileEditsOutputs(
        root=root_v,
        compiled_edit_volume=compiled_edit_volume_v,
    )

def mri_compile_edits_execute(params: MriCompileEdits, runner: Runner | None = None) -> MriCompileEditsOutputs:
    """
    mri_compile_edits

    Program to create a single volume showing all the volumetric edits made to a subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compile_edits_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPILE_EDITS_METADATA)
    execution.params(params)
    args = mri_compile_edits_cargs(params, execution)
    out = mri_compile_edits_outputs(params, execution)
    execution.run(args)
    return out

def mri_compile_edits(
    subject_name: str,
    output_volume: str,
    runner: Runner | None = None,
) -> MriCompileEditsOutputs:
    """
    mri_compile_edits

    Program to create a single volume showing all the volumetric edits made to a subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name whose edits are to be compiled into a volume
        output_volume: Output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compile_edits_params(
        subject_name=subject_name,
        output_volume=output_volume,
    )
    return mri_compile_edits_execute(params, runner)

__all__ = [
    "MriCompileEdits",
    "MriCompileEditsOutputs",
    "MRI_COMPILE_EDITS_METADATA",
    "mri_compile_edits_cargs",
    "mri_compile_edits_outputs",
    "mri_compile_edits_params",
    "mri_compile_edits_execute",
    "mri_compile_edits_validate",
    "mri_compile_edits",
]