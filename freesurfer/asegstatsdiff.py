# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ASEGSTATSDIFF_METADATA = Metadata(
    id="asegstatsdiff",
    name="asegstatsdiff",
    package="freesurfer",
)

class AsegstatsdiffSubject2(typing.TypedDict):
    subject2: str
    """The second subject to be compared."""
    outdir: typing.NotRequired[str]
    """Optionally specify a directory to write the output asegstats.txt."""

Asegstatsdiff = typing.TypedDict(
    "Asegstatsdiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/asegstatsdiff"]],
        "subject1": str,
        "subject2": AsegstatsdiffSubject2,
    },
)

@dataclasses.dataclass
class AsegstatsdiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    asegstats_output: OutputPathType | None
    """Output table with percent differences added."""

def asegstatsdiff_params(
    subject1: str,
    subject2: AsegstatsdiffSubject2,
) -> Asegstatsdiff:
    """
    Build parameters.

    Args:
        subject1: The first subject to be compared.
        subject2:

    Returns:
        Parameter dictionary.
    """
    params: Asegstatsdiff = {
        "@type": "freesurfer/asegstatsdiff",
        "subject1": subject1,
        "subject2": subject2,
    }
    return params

def asegstatsdiff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Asegstatsdiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject1", None) is None:
        raise StyxValidationError("`subject1` must not be None")
    if not isinstance(params["subject1"], str):
        raise StyxValidationError(f'`subject1` has the wrong type: Received `{type(params["subject1"])}` expected `str`')
    if params.get("subject2", None) is None:
        raise StyxValidationError("`subject2` must not be None")
    if not isinstance(params["subject2"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["subject2"])}\'')
    if params["subject2"].get("subject2", None) is None:
        raise StyxValidationError("`subject2` must not be None")
    if not isinstance(params["subject2"]["subject2"], str):
        raise StyxValidationError(f'`subject2` has the wrong type: Received `{type(params["subject2"]["subject2"])}` expected `str`')
    if params["subject2"].get("outdir", None) is not None:
        if not isinstance(params["subject2"]["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["subject2"]["outdir"])}` expected `str`')

def asegstatsdiff_cargs(params: Asegstatsdiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("asegstatsdiff")
    cargs.append(params["subject1"])
    cargs.append("".join([params["subject2"]["subject2"], (v_0 if (v_0 := params["subject2"].get("outdir")) is not None else "")]))
    return cargs

def asegstatsdiff_outputs(params: Asegstatsdiff, execution: Execution) -> AsegstatsdiffOutputs:
    """Build the AsegstatsdiffOutputs object for this tool."""
    asegstats_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params["subject2"].get("outdir")
    if __v0 is not None:
        asegstats_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/asegstats.txt')
    return AsegstatsdiffOutputs(
        root=root_v,
        asegstats_output=asegstats_output_v,
    )

def asegstatsdiff_execute(params: Asegstatsdiff, runner: Runner | None = None) -> AsegstatsdiffOutputs:
    """
    asegstatsdiff

    Compute and report percentage differences in aseg morphometry data between two subjects.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    asegstatsdiff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ASEGSTATSDIFF_METADATA)
    execution.params(params)
    args = asegstatsdiff_cargs(params, execution)
    out = asegstatsdiff_outputs(params, execution)
    execution.run(args)
    return out

def asegstatsdiff(
    subject1: str,
    subject2: AsegstatsdiffSubject2,
    runner: Runner | None = None,
) -> AsegstatsdiffOutputs:
    """
    asegstatsdiff

    Compute and report percentage differences in aseg morphometry data between two subjects.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject1: The first subject to be compared.
        subject2:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = asegstatsdiff_params(
        subject1=subject1,
        subject2=subject2,
    )
    return asegstatsdiff_execute(params, runner)

__all__ = [
    "Asegstatsdiff",
    "AsegstatsdiffOutputs",
    "ASEGSTATSDIFF_METADATA",
    "asegstatsdiff_cargs",
    "asegstatsdiff_outputs",
    "asegstatsdiff_params",
    "asegstatsdiff_execute",
    "asegstatsdiff_validate",
    "asegstatsdiff",
]