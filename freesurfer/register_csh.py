# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REGISTER_CSH_METADATA = Metadata(
    id="register.csh",
    name="register.csh",
    package="freesurfer",
)

RegisterCsh = typing.TypedDict(
    "RegisterCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/register.csh"]],
        "base_image": InputPathType,
        "new_image": InputPathType,
        "options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RegisterCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_image: OutputPathType
    """The output registered image"""

def register_csh_params(
    base_image: InputPathType,
    new_image: InputPathType,
    options: str | None = None,
) -> RegisterCsh:
    """
    Build parameters.

    Args:
        base_image: The base image for registration
        new_image: The new image to be registered to the base image
        options: Additional options for the register.csh command

    Returns:
        Parameter dictionary.
    """
    params: RegisterCsh = {
        "@type": "freesurfer/register.csh",
        "base_image": base_image,
        "new_image": new_image,
    }
    if options is not None:
        params["options"] = options
    return params

def register_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegisterCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_image", None) is None:
        raise StyxValidationError("`base_image` must not be None")
    if not isinstance(params["base_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`base_image` has the wrong type: Received `{type(params["base_image"])}` expected `InputPathType`')
    if params.get("new_image", None) is None:
        raise StyxValidationError("`new_image` must not be None")
    if not isinstance(params["new_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_image` has the wrong type: Received `{type(params["new_image"])}` expected `InputPathType`')
    if params.get("options", None) is not None:
        if not isinstance(params["options"], str):
            raise StyxValidationError(f'`options` has the wrong type: Received `{type(params["options"])}` expected `str`')

def register_csh_cargs(params: RegisterCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("register.csh")
    cargs.append(execution.input_file(params["base_image"]))
    cargs.append(execution.input_file(params["new_image"]))
    v_0 = params.get("options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def register_csh_outputs(params: RegisterCsh, execution: Execution) -> RegisterCshOutputs:
    """Build the RegisterCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_image_v: OutputPathType = execution.output_file(f'{params["new_image"]}_registered')
    return RegisterCshOutputs(
        root=root_v,
        registered_image=registered_image_v,
    )

def register_csh_execute(params: RegisterCsh, runner: Runner | None = None) -> RegisterCshOutputs:
    """
    register.csh

    A script for registering MRI images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    register_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REGISTER_CSH_METADATA)
    execution.params(params)
    args = register_csh_cargs(params, execution)
    out = register_csh_outputs(params, execution)
    execution.run(args)
    return out

def register_csh(
    base_image: InputPathType,
    new_image: InputPathType,
    options: str | None = None,
    runner: Runner | None = None,
) -> RegisterCshOutputs:
    """
    register.csh

    A script for registering MRI images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_image: The base image for registration
        new_image: The new image to be registered to the base image
        options: Additional options for the register.csh command
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = register_csh_params(
        base_image=base_image,
        new_image=new_image,
        options=options,
    )
    return register_csh_execute(params, runner)

__all__ = [
    "RegisterCsh",
    "RegisterCshOutputs",
    "REGISTER_CSH_METADATA",
    "register_csh_cargs",
    "register_csh_outputs",
    "register_csh_params",
    "register_csh_execute",
    "register_csh_validate",
    "register_csh",
]