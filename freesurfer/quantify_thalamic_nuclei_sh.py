# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QUANTIFY_THALAMIC_NUCLEI_SH_METADATA = Metadata(
    id="quantifyThalamicNuclei.sh",
    name="quantifyThalamicNuclei.sh",
    package="freesurfer",
)

QuantifyThalamicNucleiSh = typing.TypedDict(
    "QuantifyThalamicNucleiSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/quantifyThalamicNuclei.sh"]],
        "output_file": str,
        "analysis_id": str,
        "subjects_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class QuantifyThalamicNucleiShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    results_file: OutputPathType
    """The output file containing quantified thalamic nuclei results."""

def quantify_thalamic_nuclei_sh_params(
    output_file: str,
    analysis_id: str,
    subjects_directory: str | None = None,
) -> QuantifyThalamicNucleiSh:
    """
    Build parameters.

    Args:
        output_file: Output file for the results.
        analysis_id: Analysis ID for specificity.
        subjects_directory: Directory containing subject data.

    Returns:
        Parameter dictionary.
    """
    params: QuantifyThalamicNucleiSh = {
        "@type": "freesurfer/quantifyThalamicNuclei.sh",
        "output_file": output_file,
        "analysis_id": analysis_id,
    }
    if subjects_directory is not None:
        params["subjects_directory"] = subjects_directory
    return params

def quantify_thalamic_nuclei_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid QuantifyThalamicNucleiSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("analysis_id", None) is None:
        raise StyxValidationError("`analysis_id` must not be None")
    if not isinstance(params["analysis_id"], str):
        raise StyxValidationError(f'`analysis_id` has the wrong type: Received `{type(params["analysis_id"])}` expected `str`')
    if params.get("subjects_directory", None) is not None:
        if not isinstance(params["subjects_directory"], str):
            raise StyxValidationError(f'`subjects_directory` has the wrong type: Received `{type(params["subjects_directory"])}` expected `str`')

def quantify_thalamic_nuclei_sh_cargs(params: QuantifyThalamicNucleiSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("quantifyThalamicNuclei.sh")
    cargs.append(params["output_file"])
    cargs.append(params["analysis_id"])
    v_0 = params.get("subjects_directory")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def quantify_thalamic_nuclei_sh_outputs(params: QuantifyThalamicNucleiSh, execution: Execution) -> QuantifyThalamicNucleiShOutputs:
    """Build the QuantifyThalamicNucleiShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    results_file_v: OutputPathType = execution.output_file(params["output_file"])
    return QuantifyThalamicNucleiShOutputs(
        root=root_v,
        results_file=results_file_v,
    )

def quantify_thalamic_nuclei_sh_execute(params: QuantifyThalamicNucleiSh, runner: Runner | None = None) -> QuantifyThalamicNucleiShOutputs:
    """
    quantifyThalamicNuclei.sh

    Command-line tool to quantify thalamic nuclei using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    quantify_thalamic_nuclei_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QUANTIFY_THALAMIC_NUCLEI_SH_METADATA)
    execution.params(params)
    args = quantify_thalamic_nuclei_sh_cargs(params, execution)
    out = quantify_thalamic_nuclei_sh_outputs(params, execution)
    execution.run(args)
    return out

def quantify_thalamic_nuclei_sh(
    output_file: str,
    analysis_id: str,
    subjects_directory: str | None = None,
    runner: Runner | None = None,
) -> QuantifyThalamicNucleiShOutputs:
    """
    quantifyThalamicNuclei.sh

    Command-line tool to quantify thalamic nuclei using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_file: Output file for the results.
        analysis_id: Analysis ID for specificity.
        subjects_directory: Directory containing subject data.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = quantify_thalamic_nuclei_sh_params(
        output_file=output_file,
        analysis_id=analysis_id,
        subjects_directory=subjects_directory,
    )
    return quantify_thalamic_nuclei_sh_execute(params, runner)

__all__ = [
    "QuantifyThalamicNucleiSh",
    "QuantifyThalamicNucleiShOutputs",
    "QUANTIFY_THALAMIC_NUCLEI_SH_METADATA",
    "quantify_thalamic_nuclei_sh_cargs",
    "quantify_thalamic_nuclei_sh_outputs",
    "quantify_thalamic_nuclei_sh_params",
    "quantify_thalamic_nuclei_sh_execute",
    "quantify_thalamic_nuclei_sh_validate",
    "quantify_thalamic_nuclei_sh",
]