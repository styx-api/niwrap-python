# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_BRAINVOL_STATS_METADATA = Metadata(
    id="mri_brainvol_stats",
    name="mri_brainvol_stats",
    package="freesurfer",
)

MriBrainvolStats = typing.TypedDict(
    "MriBrainvolStats",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_brainvol_stats"]],
        "subject_id": str,
        "xml_string": typing.NotRequired[str],
        "no_surface": typing.NotRequired[bool],
        "include_segmentation": typing.NotRequired[bool],
        "output_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriBrainvolStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    brain_vol_stats_output: OutputPathType | None
    """Brain volume statistics output file."""

def mri_brainvol_stats_params(
    subject_id: str,
    xml_string: str | None = None,
    no_surface: bool = False,
    include_segmentation: bool = False,
    output_file: str | None = None,
) -> MriBrainvolStats:
    """
    Build parameters.

    Args:
        subject_id: The subject ID for which brain volume statistics are computed
        xml_string: XML string containing input data. The XML is used to define\
            additional parameters for computation.
        no_surface: Flag to specify that surface-based measurements should not be\
            included in the output.
        include_segmentation: Flag to include segmentation information in the brain\
            volume calculations.
        output_file: Output file path to write the brain volume statistics.

    Returns:
        Parameter dictionary.
    """
    params: MriBrainvolStats = {
        "@type": "freesurfer/mri_brainvol_stats",
        "subject_id": subject_id,
        "no_surface": no_surface,
        "include_segmentation": include_segmentation,
    }
    if xml_string is not None:
        params["xml_string"] = xml_string
    if output_file is not None:
        params["output_file"] = output_file
    return params

def mri_brainvol_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriBrainvolStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("xml_string", None) is not None:
        if not isinstance(params["xml_string"], str):
            raise StyxValidationError(f'`xml_string` has the wrong type: Received `{type(params["xml_string"])}` expected `str`')
    if params.get("no_surface", None) is not None:
        if not isinstance(params["no_surface"], bool):
            raise StyxValidationError(f'`no_surface` has the wrong type: Received `{type(params["no_surface"])}` expected `bool`')
    if params.get("include_segmentation", None) is not None:
        if not isinstance(params["include_segmentation"], bool):
            raise StyxValidationError(f'`include_segmentation` has the wrong type: Received `{type(params["include_segmentation"])}` expected `bool`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_brainvol_stats_cargs(params: MriBrainvolStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_brainvol_stats")
    cargs.append("--subject")
    cargs.append(params["subject_id"])
    v_0 = params.get("xml_string")
    if v_0 is not None:
        cargs.append("--xml")
        cargs.append(v_0)
    if params.get("no_surface"):
        cargs.append("--no-surface")
    if params.get("include_segmentation"):
        cargs.append("--seg")
    v_1 = params.get("output_file")
    if v_1 is not None:
        cargs.append("--out")
        cargs.append(v_1)
    return cargs

def mri_brainvol_stats_outputs(params: MriBrainvolStats, execution: Execution) -> MriBrainvolStatsOutputs:
    """Build the MriBrainvolStatsOutputs object for this tool."""
    brain_vol_stats_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        brain_vol_stats_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriBrainvolStatsOutputs(
        root=root_v,
        brain_vol_stats_output=brain_vol_stats_output_v,
    )

def mri_brainvol_stats_execute(params: MriBrainvolStats, runner: Runner | None = None) -> MriBrainvolStatsOutputs:
    """
    mri_brainvol_stats

    Tool for computing brain volume statistics with FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_brainvol_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_BRAINVOL_STATS_METADATA)
    execution.params(params)
    args = mri_brainvol_stats_cargs(params, execution)
    out = mri_brainvol_stats_outputs(params, execution)
    execution.run(args)
    return out

def mri_brainvol_stats(
    subject_id: str,
    xml_string: str | None = None,
    no_surface: bool = False,
    include_segmentation: bool = False,
    output_file: str | None = None,
    runner: Runner | None = None,
) -> MriBrainvolStatsOutputs:
    """
    mri_brainvol_stats

    Tool for computing brain volume statistics with FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id: The subject ID for which brain volume statistics are computed
        xml_string: XML string containing input data. The XML is used to define\
            additional parameters for computation.
        no_surface: Flag to specify that surface-based measurements should not be\
            included in the output.
        include_segmentation: Flag to include segmentation information in the brain\
            volume calculations.
        output_file: Output file path to write the brain volume statistics.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_brainvol_stats_params(
        subject_id=subject_id,
        xml_string=xml_string,
        no_surface=no_surface,
        include_segmentation=include_segmentation,
        output_file=output_file,
    )
    return mri_brainvol_stats_execute(params, runner)

__all__ = [
    "MriBrainvolStats",
    "MriBrainvolStatsOutputs",
    "MRI_BRAINVOL_STATS_METADATA",
    "mri_brainvol_stats_cargs",
    "mri_brainvol_stats_outputs",
    "mri_brainvol_stats_params",
    "mri_brainvol_stats_execute",
    "mri_brainvol_stats_validate",
    "mri_brainvol_stats",
]