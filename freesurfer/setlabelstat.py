# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SETLABELSTAT_METADATA = Metadata(
    id="setlabelstat",
    name="setlabelstat",
    package="freesurfer",
)

Setlabelstat = typing.TypedDict(
    "Setlabelstat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/setlabelstat"]],
        "inlabelfile": InputPathType,
        "outlabelfile": InputPathType,
        "statval": float,
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SetlabelstatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_file: OutputPathType
    """The resulting label file with updated stat values."""

def setlabelstat_params(
    inlabelfile: InputPathType,
    outlabelfile: InputPathType,
    statval: float,
    help: bool = False,
) -> Setlabelstat:
    """
    Build parameters.

    Args:
        inlabelfile: Input label file.
        outlabelfile: Output label file.
        statval: Stat value to replace in the label file.
        help: Displays help information.

    Returns:
        Parameter dictionary.
    """
    params: Setlabelstat = {
        "@type": "freesurfer/setlabelstat",
        "inlabelfile": inlabelfile,
        "outlabelfile": outlabelfile,
        "statval": statval,
        "help": help,
    }
    return params

def setlabelstat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Setlabelstat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inlabelfile", None) is None:
        raise StyxValidationError("`inlabelfile` must not be None")
    if not isinstance(params["inlabelfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inlabelfile` has the wrong type: Received `{type(params["inlabelfile"])}` expected `InputPathType`')
    if params.get("outlabelfile", None) is None:
        raise StyxValidationError("`outlabelfile` must not be None")
    if not isinstance(params["outlabelfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outlabelfile` has the wrong type: Received `{type(params["outlabelfile"])}` expected `InputPathType`')
    if params.get("statval", None) is None:
        raise StyxValidationError("`statval` must not be None")
    if not isinstance(params["statval"], (float, int)):
        raise StyxValidationError(f'`statval` has the wrong type: Received `{type(params["statval"])}` expected `float`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def setlabelstat_cargs(params: Setlabelstat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("setlabelstat")
    cargs.append("-i")
    cargs.append(execution.input_file(params["inlabelfile"]))
    cargs.append("-o")
    cargs.append(execution.input_file(params["outlabelfile"]))
    cargs.append("-s")
    cargs.append(str(params["statval"]))
    if params.get("help"):
        cargs.append("-help")
    return cargs

def setlabelstat_outputs(params: Setlabelstat, execution: Execution) -> SetlabelstatOutputs:
    """Build the SetlabelstatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_file_v: OutputPathType = execution.output_file(params["outlabelfile"])
    return SetlabelstatOutputs(
        root=root_v,
        output_label_file=output_label_file_v,
    )

def setlabelstat_execute(params: Setlabelstat, runner: Runner | None = None) -> SetlabelstatOutputs:
    """
    setlabelstat

    Replaces the stat values in a label file with the single stat value supplied on the command-line.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    setlabelstat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SETLABELSTAT_METADATA)
    execution.params(params)
    args = setlabelstat_cargs(params, execution)
    out = setlabelstat_outputs(params, execution)
    execution.run(args)
    return out

def setlabelstat(
    inlabelfile: InputPathType,
    outlabelfile: InputPathType,
    statval: float,
    help: bool = False,
    runner: Runner | None = None,
) -> SetlabelstatOutputs:
    """
    setlabelstat

    Replaces the stat values in a label file with the single stat value supplied on the command-line.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inlabelfile: Input label file.
        outlabelfile: Output label file.
        statval: Stat value to replace in the label file.
        help: Displays help information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = setlabelstat_params(
        inlabelfile=inlabelfile,
        outlabelfile=outlabelfile,
        statval=statval,
        help=help,
    )
    return setlabelstat_execute(params, runner)

__all__ = [
    "Setlabelstat",
    "SetlabelstatOutputs",
    "SETLABELSTAT_METADATA",
    "setlabelstat_cargs",
    "setlabelstat_outputs",
    "setlabelstat_params",
    "setlabelstat_execute",
    "setlabelstat_validate",
    "setlabelstat",
]