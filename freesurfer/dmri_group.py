# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_GROUP_METADATA = Metadata(
    id="dmri_group",
    name="dmri_group",
    package="freesurfer",
)

DmriGroup = typing.TypedDict(
    "DmriGroup",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_group"]],
        "input_list": InputPathType,
        "reference_volume": InputPathType,
        "output_base": str,
        "no_interpolation": typing.NotRequired[bool],
        "sections_num": typing.NotRequired[float],
        "debug_mode": typing.NotRequired[bool],
        "check_options": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriGroupOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_group_params(
    input_list: InputPathType,
    reference_volume: InputPathType,
    output_base: str,
    no_interpolation: bool = False,
    sections_num: float | None = None,
    debug_mode: bool = False,
    check_options: bool = False,
) -> DmriGroup:
    """
    Build parameters.

    Args:
        input_list: Text file with list of individual inputs
        reference_volume: Reference volume for output path
        output_base: Base name of output stats files
        no_interpolation: Do not attempt to interpolate along-tract measures\
            (Assume that subjects are sampled at equivalent positions)
        sections_num: Divide the pathway into a number of sections and output\
            average measures for each section
        debug_mode: Turn on debugging
        check_options: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriGroup = {
        "@type": "freesurfer/dmri_group",
        "input_list": input_list,
        "reference_volume": reference_volume,
        "output_base": output_base,
        "no_interpolation": no_interpolation,
        "debug_mode": debug_mode,
        "check_options": check_options,
    }
    if sections_num is not None:
        params["sections_num"] = sections_num
    return params

def dmri_group_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriGroup."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_list", None) is None:
        raise StyxValidationError("`input_list` must not be None")
    if not isinstance(params["input_list"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_list` has the wrong type: Received `{type(params["input_list"])}` expected `InputPathType`')
    if params.get("reference_volume", None) is None:
        raise StyxValidationError("`reference_volume` must not be None")
    if not isinstance(params["reference_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_volume` has the wrong type: Received `{type(params["reference_volume"])}` expected `InputPathType`')
    if params.get("output_base", None) is None:
        raise StyxValidationError("`output_base` must not be None")
    if not isinstance(params["output_base"], str):
        raise StyxValidationError(f'`output_base` has the wrong type: Received `{type(params["output_base"])}` expected `str`')
    if params.get("no_interpolation", None) is not None:
        if not isinstance(params["no_interpolation"], bool):
            raise StyxValidationError(f'`no_interpolation` has the wrong type: Received `{type(params["no_interpolation"])}` expected `bool`')
    if params.get("sections_num", None) is not None:
        if not isinstance(params["sections_num"], (float, int)):
            raise StyxValidationError(f'`sections_num` has the wrong type: Received `{type(params["sections_num"])}` expected `float`')
    if params.get("debug_mode", None) is not None:
        if not isinstance(params["debug_mode"], bool):
            raise StyxValidationError(f'`debug_mode` has the wrong type: Received `{type(params["debug_mode"])}` expected `bool`')
    if params.get("check_options", None) is not None:
        if not isinstance(params["check_options"], bool):
            raise StyxValidationError(f'`check_options` has the wrong type: Received `{type(params["check_options"])}` expected `bool`')

def dmri_group_cargs(params: DmriGroup, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_group")
    cargs.append("--list")
    cargs.append(execution.input_file(params["input_list"]))
    cargs.append("--ref")
    cargs.append(execution.input_file(params["reference_volume"]))
    cargs.append("--out")
    cargs.append(params["output_base"])
    if params.get("no_interpolation"):
        cargs.append("--nointerp")
    v_0 = params.get("sections_num")
    if v_0 is not None:
        cargs.append("--sec")
        cargs.append(str(v_0))
    if params.get("debug_mode"):
        cargs.append("--debug")
    if params.get("check_options"):
        cargs.append("--checkopts")
    return cargs

def dmri_group_outputs(params: DmriGroup, execution: Execution) -> DmriGroupOutputs:
    """Build the DmriGroupOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriGroupOutputs(
        root=root_v,
    )

def dmri_group_execute(params: DmriGroup, runner: Runner | None = None) -> DmriGroupOutputs:
    """
    dmri_group

    A tool to process and analyze diffusion MRI group data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_group_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_GROUP_METADATA)
    execution.params(params)
    args = dmri_group_cargs(params, execution)
    out = dmri_group_outputs(params, execution)
    execution.run(args)
    return out

def dmri_group(
    input_list: InputPathType,
    reference_volume: InputPathType,
    output_base: str,
    no_interpolation: bool = False,
    sections_num: float | None = None,
    debug_mode: bool = False,
    check_options: bool = False,
    runner: Runner | None = None,
) -> DmriGroupOutputs:
    """
    dmri_group

    A tool to process and analyze diffusion MRI group data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_list: Text file with list of individual inputs
        reference_volume: Reference volume for output path
        output_base: Base name of output stats files
        no_interpolation: Do not attempt to interpolate along-tract measures\
            (Assume that subjects are sampled at equivalent positions)
        sections_num: Divide the pathway into a number of sections and output\
            average measures for each section
        debug_mode: Turn on debugging
        check_options: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_group_params(
        input_list=input_list,
        reference_volume=reference_volume,
        output_base=output_base,
        no_interpolation=no_interpolation,
        sections_num=sections_num,
        debug_mode=debug_mode,
        check_options=check_options,
    )
    return dmri_group_execute(params, runner)

__all__ = [
    "DmriGroup",
    "DmriGroupOutputs",
    "DMRI_GROUP_METADATA",
    "dmri_group_cargs",
    "dmri_group_outputs",
    "dmri_group_params",
    "dmri_group_execute",
    "dmri_group_validate",
    "dmri_group",
]