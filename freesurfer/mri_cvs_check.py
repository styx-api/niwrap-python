# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CVS_CHECK_METADATA = Metadata(
    id="mri_cvs_check",
    name="mri_cvs_check",
    package="freesurfer",
)

MriCvsCheckHemi = typing.Literal["lh"] | typing.Literal["rh"]

MriCvsCheck = typing.TypedDict(
    "MriCvsCheck",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cvs_check"]],
        "mov_subjid": str,
        "template_subjid": typing.NotRequired[str],
        "hemi": typing.NotRequired[MriCvsCheckHemi],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCvsCheckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_cvs_check_params(
    mov_subjid: str,
    template_subjid: str | None = None,
    hemi: MriCvsCheckHemi | None = None,
    help: bool = False,
    version: bool = False,
) -> MriCvsCheck:
    """
    Build parameters.

    Args:
        mov_subjid: Subject id for the subject to be moved/registered (Should be\
            present in SUBJECTS_DIR).
        template_subjid: Subject id for the template subject to be kept fixed. If\
            missing, CVS template is assumed as a target.
        hemi: The hemisphere that is going to be processed. It can be 'lh' or 'rh'.
        help: Print help and exit.
        version: Print version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriCvsCheck = {
        "@type": "freesurfer/mri_cvs_check",
        "mov_subjid": mov_subjid,
        "help": help,
        "version": version,
    }
    if template_subjid is not None:
        params["template_subjid"] = template_subjid
    if hemi is not None:
        params["hemi"] = hemi
    return params

def mri_cvs_check_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCvsCheck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mov_subjid", None) is None:
        raise StyxValidationError("`mov_subjid` must not be None")
    if not isinstance(params["mov_subjid"], str):
        raise StyxValidationError(f'`mov_subjid` has the wrong type: Received `{type(params["mov_subjid"])}` expected `str`')
    if params.get("template_subjid", None) is not None:
        if not isinstance(params["template_subjid"], str):
            raise StyxValidationError(f'`template_subjid` has the wrong type: Received `{type(params["template_subjid"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `MriCvsCheckHemi`')
        if params["hemi"] not in ["lh", "rh"]:
            raise StyxValidationError("Parameter `hemi` must be one of [\"lh\", \"rh\"]")
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_cvs_check_cargs(params: MriCvsCheck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cvs_check")
    cargs.append("--mov")
    cargs.append(params["mov_subjid"])
    v_0 = params.get("template_subjid")
    if v_0 is not None:
        cargs.append("--template")
        cargs.append(v_0)
    v_1 = params.get("hemi")
    if v_1 is not None:
        cargs.append("--hemi")
        cargs.append(str(v_1))
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_cvs_check_outputs(params: MriCvsCheck, execution: Execution) -> MriCvsCheckOutputs:
    """Build the MriCvsCheckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriCvsCheckOutputs(
        root=root_v,
    )

def mri_cvs_check_execute(params: MriCvsCheck, runner: Runner | None = None) -> MriCvsCheckOutputs:
    """
    mri_cvs_check

    Checks whether the files required for mri_cvs_register all exist.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cvs_check_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CVS_CHECK_METADATA)
    execution.params(params)
    args = mri_cvs_check_cargs(params, execution)
    out = mri_cvs_check_outputs(params, execution)
    execution.run(args)
    return out

def mri_cvs_check(
    mov_subjid: str,
    template_subjid: str | None = None,
    hemi: MriCvsCheckHemi | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriCvsCheckOutputs:
    """
    mri_cvs_check

    Checks whether the files required for mri_cvs_register all exist.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mov_subjid: Subject id for the subject to be moved/registered (Should be\
            present in SUBJECTS_DIR).
        template_subjid: Subject id for the template subject to be kept fixed. If\
            missing, CVS template is assumed as a target.
        hemi: The hemisphere that is going to be processed. It can be 'lh' or 'rh'.
        help: Print help and exit.
        version: Print version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cvs_check_params(
        mov_subjid=mov_subjid,
        template_subjid=template_subjid,
        hemi=hemi,
        help=help,
        version=version,
    )
    return mri_cvs_check_execute(params, runner)

__all__ = [
    "MriCvsCheck",
    "MriCvsCheckOutputs",
    "MRI_CVS_CHECK_METADATA",
    "mri_cvs_check_cargs",
    "mri_cvs_check_outputs",
    "mri_cvs_check_params",
    "mri_cvs_check_execute",
    "mri_cvs_check_validate",
    "mri_cvs_check",
]