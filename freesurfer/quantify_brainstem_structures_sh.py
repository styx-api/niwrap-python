# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QUANTIFY_BRAINSTEM_STRUCTURES_SH_METADATA = Metadata(
    id="quantifyBrainstemStructures.sh",
    name="quantifyBrainstemStructures.sh",
    package="freesurfer",
)

QuantifyBrainstemStructuresSh = typing.TypedDict(
    "QuantifyBrainstemStructuresSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/quantifyBrainstemStructures.sh"]],
        "output_file": str,
        "subjects_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class QuantifyBrainstemStructuresShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    results_file: OutputPathType
    """Output file containing the gathered results."""

def quantify_brainstem_structures_sh_params(
    output_file: str,
    subjects_directory: str | None = None,
) -> QuantifyBrainstemStructuresSh:
    """
    Build parameters.

    Args:
        output_file: The path to the output file where results should be written.
        subjects_directory: The directory containing subject data. Defaults to\
            '/usr/local/freesurfer/subjects' if not provided.

    Returns:
        Parameter dictionary.
    """
    params: QuantifyBrainstemStructuresSh = {
        "@type": "freesurfer/quantifyBrainstemStructures.sh",
        "output_file": output_file,
    }
    if subjects_directory is not None:
        params["subjects_directory"] = subjects_directory
    return params

def quantify_brainstem_structures_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid QuantifyBrainstemStructuresSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("subjects_directory", None) is not None:
        if not isinstance(params["subjects_directory"], str):
            raise StyxValidationError(f'`subjects_directory` has the wrong type: Received `{type(params["subjects_directory"])}` expected `str`')

def quantify_brainstem_structures_sh_cargs(params: QuantifyBrainstemStructuresSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("quantifyBrainstemStructures.sh")
    cargs.append(params["output_file"])
    v_0 = params.get("subjects_directory")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def quantify_brainstem_structures_sh_outputs(params: QuantifyBrainstemStructuresSh, execution: Execution) -> QuantifyBrainstemStructuresShOutputs:
    """Build the QuantifyBrainstemStructuresShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    results_file_v: OutputPathType = execution.output_file(params["output_file"])
    return QuantifyBrainstemStructuresShOutputs(
        root=root_v,
        results_file=results_file_v,
    )

def quantify_brainstem_structures_sh_execute(params: QuantifyBrainstemStructuresSh, runner: Runner | None = None) -> QuantifyBrainstemStructuresShOutputs:
    """
    quantifyBrainstemStructures.sh

    A script to gather results from FreeSurfer brainstem processing and write them to an output file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    quantify_brainstem_structures_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QUANTIFY_BRAINSTEM_STRUCTURES_SH_METADATA)
    execution.params(params)
    args = quantify_brainstem_structures_sh_cargs(params, execution)
    out = quantify_brainstem_structures_sh_outputs(params, execution)
    execution.run(args)
    return out

def quantify_brainstem_structures_sh(
    output_file: str,
    subjects_directory: str | None = None,
    runner: Runner | None = None,
) -> QuantifyBrainstemStructuresShOutputs:
    """
    quantifyBrainstemStructures.sh

    A script to gather results from FreeSurfer brainstem processing and write them to an output file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_file: The path to the output file where results should be written.
        subjects_directory: The directory containing subject data. Defaults to\
            '/usr/local/freesurfer/subjects' if not provided.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = quantify_brainstem_structures_sh_params(
        output_file=output_file,
        subjects_directory=subjects_directory,
    )
    return quantify_brainstem_structures_sh_execute(params, runner)

__all__ = [
    "QuantifyBrainstemStructuresSh",
    "QuantifyBrainstemStructuresShOutputs",
    "QUANTIFY_BRAINSTEM_STRUCTURES_SH_METADATA",
    "quantify_brainstem_structures_sh_cargs",
    "quantify_brainstem_structures_sh_outputs",
    "quantify_brainstem_structures_sh_params",
    "quantify_brainstem_structures_sh_execute",
    "quantify_brainstem_structures_sh_validate",
    "quantify_brainstem_structures_sh",
]