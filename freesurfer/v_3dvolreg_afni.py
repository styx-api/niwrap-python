# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DVOLREG_AFNI_METADATA = Metadata(
    id="3dvolreg.afni",
    name="3dvolreg.afni",
    package="freesurfer",
)

V3dvolregAfni = typing.TypedDict(
    "V3dvolregAfni",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/3dvolreg.afni"]],
        "input_file": InputPathType,
        "output_file": str,
        "options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dvolregAfniOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_output: OutputPathType
    """Output of the registered dataset"""

def v_3dvolreg_afni_params(
    input_file: InputPathType,
    output_file: str,
    options: str | None = None,
) -> V3dvolregAfni:
    """
    Build parameters.

    Args:
        input_file: Input dataset to be registered
        output_file: Output dataset with applied registration
        options: Options for 3dvolreg (consult AFNI documentation for details)

    Returns:
        Parameter dictionary.
    """
    params: V3dvolregAfni = {
        "@type": "freesurfer/3dvolreg.afni",
        "input_file": input_file,
        "output_file": output_file,
    }
    if options is not None:
        params["options"] = options
    return params

def v_3dvolreg_afni_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dvolregAfni."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("options", None) is not None:
        if not isinstance(params["options"], str):
            raise StyxValidationError(f'`options` has the wrong type: Received `{type(params["options"])}` expected `str`')

def v_3dvolreg_afni_cargs(params: V3dvolregAfni, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dvolreg.afni")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    v_0 = params.get("options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def v_3dvolreg_afni_outputs(params: V3dvolregAfni, execution: Execution) -> V3dvolregAfniOutputs:
    """Build the V3dvolregAfniOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_output_v: OutputPathType = execution.output_file(f'{params["output_file"]}.nii')
    return V3dvolregAfniOutputs(
        root=root_v,
        registered_output=registered_output_v,
    )

def v_3dvolreg_afni_execute(params: V3dvolregAfni, runner: Runner | None = None) -> V3dvolregAfniOutputs:
    """
    3dvolreg.afni

    AFNI program for volume registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dvolreg_afni_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DVOLREG_AFNI_METADATA)
    execution.params(params)
    args = v_3dvolreg_afni_cargs(params, execution)
    out = v_3dvolreg_afni_outputs(params, execution)
    execution.run(args)
    return out

def v_3dvolreg_afni(
    input_file: InputPathType,
    output_file: str,
    options: str | None = None,
    runner: Runner | None = None,
) -> V3dvolregAfniOutputs:
    """
    3dvolreg.afni

    AFNI program for volume registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input dataset to be registered
        output_file: Output dataset with applied registration
        options: Options for 3dvolreg (consult AFNI documentation for details)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dvolreg_afni_params(
        input_file=input_file,
        output_file=output_file,
        options=options,
    )
    return v_3dvolreg_afni_execute(params, runner)

__all__ = [
    "V3dvolregAfni",
    "V3dvolregAfniOutputs",
    "V_3DVOLREG_AFNI_METADATA",
    "v_3dvolreg_afni_cargs",
    "v_3dvolreg_afni_outputs",
    "v_3dvolreg_afni_params",
    "v_3dvolreg_afni_execute",
    "v_3dvolreg_afni_validate",
    "v_3dvolreg_afni",
]