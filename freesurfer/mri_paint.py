# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PAINT_METADATA = Metadata(
    id="mri_paint",
    name="mri_paint",
    package="freesurfer",
)

MriPaint = typing.TypedDict(
    "MriPaint",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_paint"]],
        "input_volume": InputPathType,
        "input_surface": InputPathType,
        "registration_file": InputPathType,
        "output_float_file": str,
        "image_offset": typing.NotRequired[float],
        "paint_surf_coords": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriPaintOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_float: OutputPathType
    """The output .float file resulting from the painting process"""

def mri_paint_params(
    input_volume: InputPathType,
    input_surface: InputPathType,
    registration_file: InputPathType,
    output_float_file: str,
    image_offset: float | None = None,
    paint_surf_coords: bool = False,
) -> MriPaint:
    """
    Build parameters.

    Args:
        input_volume: The input volume file
        input_surface: The input surface file
        registration_file: The registration file
        output_float_file: The output .float file
        image_offset: Set offset to use
        paint_surf_coords: Paint using surface coordinates

    Returns:
        Parameter dictionary.
    """
    params: MriPaint = {
        "@type": "freesurfer/mri_paint",
        "input_volume": input_volume,
        "input_surface": input_surface,
        "registration_file": registration_file,
        "output_float_file": output_float_file,
        "paint_surf_coords": paint_surf_coords,
    }
    if image_offset is not None:
        params["image_offset"] = image_offset
    return params

def mri_paint_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriPaint."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("registration_file", None) is None:
        raise StyxValidationError("`registration_file` must not be None")
    if not isinstance(params["registration_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("output_float_file", None) is None:
        raise StyxValidationError("`output_float_file` must not be None")
    if not isinstance(params["output_float_file"], str):
        raise StyxValidationError(f'`output_float_file` has the wrong type: Received `{type(params["output_float_file"])}` expected `str`')
    if params.get("image_offset", None) is not None:
        if not isinstance(params["image_offset"], (float, int)):
            raise StyxValidationError(f'`image_offset` has the wrong type: Received `{type(params["image_offset"])}` expected `float`')
    if params.get("paint_surf_coords", None) is not None:
        if not isinstance(params["paint_surf_coords"], bool):
            raise StyxValidationError(f'`paint_surf_coords` has the wrong type: Received `{type(params["paint_surf_coords"])}` expected `bool`')

def mri_paint_cargs(params: MriPaint, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_paint")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["registration_file"]))
    cargs.append(params["output_float_file"])
    v_0 = params.get("image_offset")
    if v_0 is not None:
        cargs.append("-imageoffset")
        cargs.append(str(v_0))
    if params.get("paint_surf_coords"):
        cargs.append("-S")
    return cargs

def mri_paint_outputs(params: MriPaint, execution: Execution) -> MriPaintOutputs:
    """Build the MriPaintOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_float_v: OutputPathType = execution.output_file(params["output_float_file"])
    return MriPaintOutputs(
        root=root_v,
        output_float=output_float_v,
    )

def mri_paint_execute(params: MriPaint, runner: Runner | None = None) -> MriPaintOutputs:
    """
    mri_paint

    This program will paint average Talairach stats onto a surface

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_paint_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PAINT_METADATA)
    execution.params(params)
    args = mri_paint_cargs(params, execution)
    out = mri_paint_outputs(params, execution)
    execution.run(args)
    return out

def mri_paint(
    input_volume: InputPathType,
    input_surface: InputPathType,
    registration_file: InputPathType,
    output_float_file: str,
    image_offset: float | None = None,
    paint_surf_coords: bool = False,
    runner: Runner | None = None,
) -> MriPaintOutputs:
    """
    mri_paint

    This program will paint average Talairach stats onto a surface

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: The input volume file
        input_surface: The input surface file
        registration_file: The registration file
        output_float_file: The output .float file
        image_offset: Set offset to use
        paint_surf_coords: Paint using surface coordinates
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_paint_params(
        input_volume=input_volume,
        input_surface=input_surface,
        registration_file=registration_file,
        output_float_file=output_float_file,
        image_offset=image_offset,
        paint_surf_coords=paint_surf_coords,
    )
    return mri_paint_execute(params, runner)

__all__ = [
    "MriPaint",
    "MriPaintOutputs",
    "MRI_PAINT_METADATA",
    "mri_paint_cargs",
    "mri_paint_outputs",
    "mri_paint_params",
    "mri_paint_execute",
    "mri_paint_validate",
    "mri_paint",
]