# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SEGMENT_METADATA = Metadata(
    id="mris_segment",
    name="mris_segment",
    package="freesurfer",
)

MrisSegment = typing.TypedDict(
    "MrisSegment",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_segment"]],
        "subjects": list[str],
        "output_subject": str,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MrisSegmentOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmented_output: OutputPathType
    """Segmented surfaces output file"""

def mris_segment_params(
    subjects: list[str],
    output_subject: str,
    output_file: str,
) -> MrisSegment:
    """
    Build parameters.

    Args:
        subjects: A list of input subjects
        output_subject: Output subject name
        output_file: Output file path

    Returns:
        Parameter dictionary.
    """
    params: MrisSegment = {
        "@type": "freesurfer/mris_segment",
        "subjects": subjects,
        "output_subject": output_subject,
        "output_file": output_file,
    }
    return params

def mris_segment_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSegment."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_subject", None) is None:
        raise StyxValidationError("`output_subject` must not be None")
    if not isinstance(params["output_subject"], str):
        raise StyxValidationError(f'`output_subject` has the wrong type: Received `{type(params["output_subject"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mris_segment_cargs(params: MrisSegment, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_segment")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_subject"])
    cargs.append(params["output_file"])
    return cargs

def mris_segment_outputs(params: MrisSegment, execution: Execution) -> MrisSegmentOutputs:
    """Build the MrisSegmentOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmented_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MrisSegmentOutputs(
        root=root_v,
        segmented_output=segmented_output_v,
    )

def mris_segment_execute(params: MrisSegment, runner: Runner | None = None) -> MrisSegmentOutputs:
    """
    mris_segment

    A command-line tool for segmenting surfaces in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_segment_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SEGMENT_METADATA)
    execution.params(params)
    args = mris_segment_cargs(params, execution)
    out = mris_segment_outputs(params, execution)
    execution.run(args)
    return out

def mris_segment(
    subjects: list[str],
    output_subject: str,
    output_file: str,
    runner: Runner | None = None,
) -> MrisSegmentOutputs:
    """
    mris_segment

    A command-line tool for segmenting surfaces in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: A list of input subjects
        output_subject: Output subject name
        output_file: Output file path
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_segment_params(
        subjects=subjects,
        output_subject=output_subject,
        output_file=output_file,
    )
    return mris_segment_execute(params, runner)

__all__ = [
    "MrisSegment",
    "MrisSegmentOutputs",
    "MRIS_SEGMENT_METADATA",
    "mris_segment_cargs",
    "mris_segment_outputs",
    "mris_segment_params",
    "mris_segment_execute",
    "mris_segment_validate",
    "mris_segment",
]