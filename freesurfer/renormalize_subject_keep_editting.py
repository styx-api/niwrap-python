# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RENORMALIZE_SUBJECT_KEEP_EDITTING_METADATA = Metadata(
    id="renormalize_subject_keep_editting",
    name="renormalize_subject_keep_editting",
    package="freesurfer",
)

RenormalizeSubjectKeepEditting = typing.TypedDict(
    "RenormalizeSubjectKeepEditting",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/renormalize_subject_keep_editting"]],
        "placeholder_input": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RenormalizeSubjectKeepEdittingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Placeholder output file since tool details are not available."""

def renormalize_subject_keep_editting_params(
    placeholder_input: str | None = None,
) -> RenormalizeSubjectKeepEditting:
    """
    Build parameters.

    Args:
        placeholder_input: Placeholder input since tool details are not available.

    Returns:
        Parameter dictionary.
    """
    params: RenormalizeSubjectKeepEditting = {
        "@type": "freesurfer/renormalize_subject_keep_editting",
    }
    if placeholder_input is not None:
        params["placeholder_input"] = placeholder_input
    return params

def renormalize_subject_keep_editting_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RenormalizeSubjectKeepEditting."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("placeholder_input", None) is not None:
        if not isinstance(params["placeholder_input"], str):
            raise StyxValidationError(f'`placeholder_input` has the wrong type: Received `{type(params["placeholder_input"])}` expected `str`')

def renormalize_subject_keep_editting_cargs(params: RenormalizeSubjectKeepEditting, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("renormalize_subject_keep_editting")
    v_0 = params.get("placeholder_input")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def renormalize_subject_keep_editting_outputs(params: RenormalizeSubjectKeepEditting, execution: Execution) -> RenormalizeSubjectKeepEdittingOutputs:
    """Build the RenormalizeSubjectKeepEdittingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file("output.txt")
    return RenormalizeSubjectKeepEdittingOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def renormalize_subject_keep_editting_execute(params: RenormalizeSubjectKeepEditting, runner: Runner | None = None) -> RenormalizeSubjectKeepEdittingOutputs:
    """
    renormalize_subject_keep_editting

    A placeholder for the renormalize_subject_keep_editting tool, details not available.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    renormalize_subject_keep_editting_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RENORMALIZE_SUBJECT_KEEP_EDITTING_METADATA)
    execution.params(params)
    args = renormalize_subject_keep_editting_cargs(params, execution)
    out = renormalize_subject_keep_editting_outputs(params, execution)
    execution.run(args)
    return out

def renormalize_subject_keep_editting(
    placeholder_input: str | None = None,
    runner: Runner | None = None,
) -> RenormalizeSubjectKeepEdittingOutputs:
    """
    renormalize_subject_keep_editting

    A placeholder for the renormalize_subject_keep_editting tool, details not available.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        placeholder_input: Placeholder input since tool details are not available.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = renormalize_subject_keep_editting_params(
        placeholder_input=placeholder_input,
    )
    return renormalize_subject_keep_editting_execute(params, runner)

__all__ = [
    "RenormalizeSubjectKeepEditting",
    "RenormalizeSubjectKeepEdittingOutputs",
    "RENORMALIZE_SUBJECT_KEEP_EDITTING_METADATA",
    "renormalize_subject_keep_editting_cargs",
    "renormalize_subject_keep_editting_outputs",
    "renormalize_subject_keep_editting_params",
    "renormalize_subject_keep_editting_execute",
    "renormalize_subject_keep_editting_validate",
    "renormalize_subject_keep_editting",
]