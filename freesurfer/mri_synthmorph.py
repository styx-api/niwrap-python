# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SYNTHMORPH_METADATA = Metadata(
    id="mri_synthmorph",
    name="mri_synthmorph",
    package="freesurfer",
)

MriSynthmorphTransformationModel = typing.Literal["deform"] | typing.Literal["affine"] | typing.Literal["rigid"]

MriSynthmorphExtent = typing.Literal[192] | typing.Literal[256]

MriSynthmorph = typing.TypedDict(
    "MriSynthmorph",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_synthmorph"]],
        "moving_image": InputPathType,
        "fixed_image": InputPathType,
        "moved_output": typing.NotRequired[str],
        "transform_output": typing.NotRequired[InputPathType],
        "header_only": typing.NotRequired[bool],
        "transformation_model": typing.NotRequired[MriSynthmorphTransformationModel],
        "init_transform": typing.NotRequired[InputPathType],
        "threads": typing.NotRequired[float],
        "gpu_flag": typing.NotRequired[bool],
        "smooth": typing.NotRequired[float],
        "extent": typing.NotRequired[MriSynthmorphExtent],
        "model_weights": typing.NotRequired[InputPathType],
        "inspect_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriSynthmorphOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    moved_output_file: OutputPathType | None
    """The registered output image file, resulting from the operation."""
    transform_output_file: OutputPathType
    """Transform file resulting from registration."""

def mri_synthmorph_params(
    moving_image: InputPathType,
    fixed_image: InputPathType,
    moved_output: str | None = None,
    transform_output: InputPathType | None = None,
    header_only: bool = False,
    transformation_model: MriSynthmorphTransformationModel | None = None,
    init_transform: InputPathType | None = None,
    threads: float | None = None,
    gpu_flag: bool = False,
    smooth: float | None = None,
    extent: MriSynthmorphExtent | None = None,
    model_weights: InputPathType | None = None,
    inspect_directory: str | None = None,
) -> MriSynthmorph:
    """
    Build parameters.

    Args:
        moving_image: The moving input image, which will be registered to the fixed\
            image.
        fixed_image: The fixed input image, which is used as the reference for\
            registration.
        moved_output: The resulting image after registration.
        transform_output: Output transform file for registration. Can be a text\
            file for linear or an image file for deformable registration.
        header_only: Adjust the voxel-to-world matrix instead of resampling. Linear\
            registration only.
        transformation_model: Specifies the registration transformation model.\
            Options include 'deform', 'affine', and 'rigid'.
        init_transform: Initial linear transform for registration.
        threads: Number of TensorFlow threads to utilize. Defaults to the number of\
            cores.
        gpu_flag: Utilize the GPU specified by CUDA_VISIBLE_DEVICES or GPU 0 if\
            unset or empty.
        smooth: Regularization parameter for deformable registration. Higher values\
            indicate smoother displacement fields.
        extent: Isotropic extent of the registration space in unit voxels.
        model_weights: Alternative model weights as an H5 file.
        inspect_directory: Save model inputs resampled into network space for\
            inspection.

    Returns:
        Parameter dictionary.
    """
    params: MriSynthmorph = {
        "@type": "freesurfer/mri_synthmorph",
        "moving_image": moving_image,
        "fixed_image": fixed_image,
        "header_only": header_only,
        "gpu_flag": gpu_flag,
    }
    if moved_output is not None:
        params["moved_output"] = moved_output
    if transform_output is not None:
        params["transform_output"] = transform_output
    if transformation_model is not None:
        params["transformation_model"] = transformation_model
    if init_transform is not None:
        params["init_transform"] = init_transform
    if threads is not None:
        params["threads"] = threads
    if smooth is not None:
        params["smooth"] = smooth
    if extent is not None:
        params["extent"] = extent
    if model_weights is not None:
        params["model_weights"] = model_weights
    if inspect_directory is not None:
        params["inspect_directory"] = inspect_directory
    return params

def mri_synthmorph_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSynthmorph."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("moving_image", None) is None:
        raise StyxValidationError("`moving_image` must not be None")
    if not isinstance(params["moving_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_image` has the wrong type: Received `{type(params["moving_image"])}` expected `InputPathType`')
    if params.get("fixed_image", None) is None:
        raise StyxValidationError("`fixed_image` must not be None")
    if not isinstance(params["fixed_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_image` has the wrong type: Received `{type(params["fixed_image"])}` expected `InputPathType`')
    if params.get("moved_output", None) is not None:
        if not isinstance(params["moved_output"], str):
            raise StyxValidationError(f'`moved_output` has the wrong type: Received `{type(params["moved_output"])}` expected `str`')
    if params.get("transform_output", None) is not None:
        if not isinstance(params["transform_output"], (pathlib.Path, str)):
            raise StyxValidationError(f'`transform_output` has the wrong type: Received `{type(params["transform_output"])}` expected `InputPathType`')
    if params.get("header_only", None) is not None:
        if not isinstance(params["header_only"], bool):
            raise StyxValidationError(f'`header_only` has the wrong type: Received `{type(params["header_only"])}` expected `bool`')
    if params.get("transformation_model", None) is not None:
        if not isinstance(params["transformation_model"], str):
            raise StyxValidationError(f'`transformation_model` has the wrong type: Received `{type(params["transformation_model"])}` expected `MriSynthmorphTransformationModel`')
        if params["transformation_model"] not in ["deform", "affine", "rigid"]:
            raise StyxValidationError("Parameter `transformation_model` must be one of [\"deform\", \"affine\", \"rigid\"]")
    if params.get("init_transform", None) is not None:
        if not isinstance(params["init_transform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_transform` has the wrong type: Received `{type(params["init_transform"])}` expected `InputPathType`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("gpu_flag", None) is not None:
        if not isinstance(params["gpu_flag"], bool):
            raise StyxValidationError(f'`gpu_flag` has the wrong type: Received `{type(params["gpu_flag"])}` expected `bool`')
    if params.get("smooth", None) is not None:
        if not isinstance(params["smooth"], (float, int)):
            raise StyxValidationError(f'`smooth` has the wrong type: Received `{type(params["smooth"])}` expected `float`')
    if params.get("extent", None) is not None:
        if not isinstance(params["extent"], (float, int)):
            raise StyxValidationError(f'`extent` has the wrong type: Received `{type(params["extent"])}` expected `MriSynthmorphExtent`')
        if params["extent"] not in [192, 256]:
            raise StyxValidationError("Parameter `extent` must be one of [192, 256]")
    if params.get("model_weights", None) is not None:
        if not isinstance(params["model_weights"], (pathlib.Path, str)):
            raise StyxValidationError(f'`model_weights` has the wrong type: Received `{type(params["model_weights"])}` expected `InputPathType`')
    if params.get("inspect_directory", None) is not None:
        if not isinstance(params["inspect_directory"], str):
            raise StyxValidationError(f'`inspect_directory` has the wrong type: Received `{type(params["inspect_directory"])}` expected `str`')

def mri_synthmorph_cargs(params: MriSynthmorph, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_synthmorph")
    cargs.append(execution.input_file(params["moving_image"]))
    cargs.append(execution.input_file(params["fixed_image"]))
    v_0 = params.get("moved_output")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("transform_output")
    if v_1 is not None:
        cargs.append("-t")
        cargs.append(execution.input_file(v_1))
    if params.get("header_only"):
        cargs.append("-H")
    v_2 = params.get("transformation_model")
    if v_2 is not None:
        cargs.append("-m")
        cargs.append(str(v_2))
    v_3 = params.get("init_transform")
    if v_3 is not None:
        cargs.append("-i")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("threads")
    if v_4 is not None:
        cargs.append("-j")
        cargs.append(str(v_4))
    if params.get("gpu_flag"):
        cargs.append("-g")
    v_5 = params.get("smooth")
    if v_5 is not None:
        cargs.append("-s")
        cargs.append(str(v_5))
    v_6 = params.get("extent")
    if v_6 is not None:
        cargs.append("-e")
        cargs.append(str(v_6))
    v_7 = params.get("model_weights")
    if v_7 is not None:
        cargs.append("-w")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("inspect_directory")
    if v_8 is not None:
        cargs.append("--inspect")
        cargs.append(v_8)
    return cargs

def mri_synthmorph_outputs(params: MriSynthmorph, execution: Execution) -> MriSynthmorphOutputs:
    """Build the MriSynthmorphOutputs object for this tool."""
    moved_output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("moved_output")
    if __v0 is not None:
        moved_output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    transform_output_file_v: OutputPathType = execution.output_file("[TRANS_OUTPUT]")
    return MriSynthmorphOutputs(
        root=root_v,
        moved_output_file=moved_output_file_v,
        transform_output_file=transform_output_file_v,
    )

def mri_synthmorph_execute(params: MriSynthmorph, runner: Runner | None = None) -> MriSynthmorphOutputs:
    """
    mri_synthmorph

    SynthMorph is a deep-learning tool for brain-specific MRI image registration without preprocessing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_synthmorph_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SYNTHMORPH_METADATA)
    execution.params(params)
    args = mri_synthmorph_cargs(params, execution)
    out = mri_synthmorph_outputs(params, execution)
    execution.run(args)
    return out

def mri_synthmorph(
    moving_image: InputPathType,
    fixed_image: InputPathType,
    moved_output: str | None = None,
    transform_output: InputPathType | None = None,
    header_only: bool = False,
    transformation_model: MriSynthmorphTransformationModel | None = None,
    init_transform: InputPathType | None = None,
    threads: float | None = None,
    gpu_flag: bool = False,
    smooth: float | None = None,
    extent: MriSynthmorphExtent | None = None,
    model_weights: InputPathType | None = None,
    inspect_directory: str | None = None,
    runner: Runner | None = None,
) -> MriSynthmorphOutputs:
    """
    mri_synthmorph

    SynthMorph is a deep-learning tool for brain-specific MRI image registration without preprocessing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        moving_image: The moving input image, which will be registered to the fixed\
            image.
        fixed_image: The fixed input image, which is used as the reference for\
            registration.
        moved_output: The resulting image after registration.
        transform_output: Output transform file for registration. Can be a text\
            file for linear or an image file for deformable registration.
        header_only: Adjust the voxel-to-world matrix instead of resampling. Linear\
            registration only.
        transformation_model: Specifies the registration transformation model.\
            Options include 'deform', 'affine', and 'rigid'.
        init_transform: Initial linear transform for registration.
        threads: Number of TensorFlow threads to utilize. Defaults to the number of\
            cores.
        gpu_flag: Utilize the GPU specified by CUDA_VISIBLE_DEVICES or GPU 0 if\
            unset or empty.
        smooth: Regularization parameter for deformable registration. Higher values\
            indicate smoother displacement fields.
        extent: Isotropic extent of the registration space in unit voxels.
        model_weights: Alternative model weights as an H5 file.
        inspect_directory: Save model inputs resampled into network space for\
            inspection.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_synthmorph_params(
        moving_image=moving_image,
        fixed_image=fixed_image,
        moved_output=moved_output,
        transform_output=transform_output,
        header_only=header_only,
        transformation_model=transformation_model,
        init_transform=init_transform,
        threads=threads,
        gpu_flag=gpu_flag,
        smooth=smooth,
        extent=extent,
        model_weights=model_weights,
        inspect_directory=inspect_directory,
    )
    return mri_synthmorph_execute(params, runner)

__all__ = [
    "MriSynthmorph",
    "MriSynthmorphOutputs",
    "MRI_SYNTHMORPH_METADATA",
    "mri_synthmorph_cargs",
    "mri_synthmorph_outputs",
    "mri_synthmorph_params",
    "mri_synthmorph_execute",
    "mri_synthmorph_validate",
    "mri_synthmorph",
]