# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REGISTER_ELDERLY_SUBJECT_METADATA = Metadata(
    id="register_elderly_subject",
    name="register_elderly_subject",
    package="freesurfer",
)

RegisterElderlySubject = typing.TypedDict(
    "RegisterElderlySubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/register_elderly_subject"]],
        "sampling_percentage": typing.NotRequired[float],
        "output_fsamples": str,
        "output_norm": str,
        "input_volume": InputPathType,
        "gca_file": InputPathType,
        "transform_file": InputPathType,
    },
)

@dataclasses.dataclass
class RegisterElderlySubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_fsamples_output: OutputPathType
    """Transformed control points output"""
    normalized_volume_output: OutputPathType
    """Intensity normalized volume"""

def register_elderly_subject_params(
    output_fsamples: str,
    output_norm: str,
    input_volume: InputPathType,
    gca_file: InputPathType,
    transform_file: InputPathType,
    sampling_percentage: float | None = None,
) -> RegisterElderlySubject:
    """
    Build parameters.

    Args:
        output_fsamples: Output path for transformed control points
        output_norm: Output path for intensity normalized volume
        input_volume: Input MRI volume to register
        gca_file: GCA file for registration
        transform_file: Output transform file
        sampling_percentage: Percentage of white matter points to use as control\
            points

    Returns:
        Parameter dictionary.
    """
    params: RegisterElderlySubject = {
        "@type": "freesurfer/register_elderly_subject",
        "output_fsamples": output_fsamples,
        "output_norm": output_norm,
        "input_volume": input_volume,
        "gca_file": gca_file,
        "transform_file": transform_file,
    }
    if sampling_percentage is not None:
        params["sampling_percentage"] = sampling_percentage
    return params

def register_elderly_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegisterElderlySubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sampling_percentage", None) is not None:
        if not isinstance(params["sampling_percentage"], (float, int)):
            raise StyxValidationError(f'`sampling_percentage` has the wrong type: Received `{type(params["sampling_percentage"])}` expected `float`')
        if not (0 <= params["sampling_percentage"] <= 1):
            raise StyxValidationError("Parameter `sampling_percentage` must be between 0 and 1 (inclusive)")
    if params.get("output_fsamples", None) is None:
        raise StyxValidationError("`output_fsamples` must not be None")
    if not isinstance(params["output_fsamples"], str):
        raise StyxValidationError(f'`output_fsamples` has the wrong type: Received `{type(params["output_fsamples"])}` expected `str`')
    if params.get("output_norm", None) is None:
        raise StyxValidationError("`output_norm` must not be None")
    if not isinstance(params["output_norm"], str):
        raise StyxValidationError(f'`output_norm` has the wrong type: Received `{type(params["output_norm"])}` expected `str`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("gca_file", None) is None:
        raise StyxValidationError("`gca_file` must not be None")
    if not isinstance(params["gca_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')

def register_elderly_subject_cargs(params: RegisterElderlySubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("register_elderly_subject")
    v_0 = params.get("sampling_percentage")
    if v_0 is not None:
        cargs.append("-p")
        cargs.append(str(v_0))
    cargs.append("-fsamples")
    cargs.append(params["output_fsamples"])
    cargs.append("-norm")
    cargs.append(params["output_norm"])
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["gca_file"]))
    cargs.append(execution.input_file(params["transform_file"]))
    return cargs

def register_elderly_subject_outputs(params: RegisterElderlySubject, execution: Execution) -> RegisterElderlySubjectOutputs:
    """Build the RegisterElderlySubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_fsamples_output_v: OutputPathType = execution.output_file(params["output_fsamples"])
    normalized_volume_output_v: OutputPathType = execution.output_file(params["output_norm"])
    return RegisterElderlySubjectOutputs(
        root=root_v,
        transformed_fsamples_output=transformed_fsamples_output_v,
        normalized_volume_output=normalized_volume_output_v,
    )

def register_elderly_subject_execute(params: RegisterElderlySubject, runner: Runner | None = None) -> RegisterElderlySubjectOutputs:
    """
    register_elderly_subject

    Tool for registering MRI images of elderly subjects using Freesurfer's mri_em_register

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    register_elderly_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REGISTER_ELDERLY_SUBJECT_METADATA)
    execution.params(params)
    args = register_elderly_subject_cargs(params, execution)
    out = register_elderly_subject_outputs(params, execution)
    execution.run(args)
    return out

def register_elderly_subject(
    output_fsamples: str,
    output_norm: str,
    input_volume: InputPathType,
    gca_file: InputPathType,
    transform_file: InputPathType,
    sampling_percentage: float | None = None,
    runner: Runner | None = None,
) -> RegisterElderlySubjectOutputs:
    """
    register_elderly_subject

    Tool for registering MRI images of elderly subjects using Freesurfer's mri_em_register

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_fsamples: Output path for transformed control points
        output_norm: Output path for intensity normalized volume
        input_volume: Input MRI volume to register
        gca_file: GCA file for registration
        transform_file: Output transform file
        sampling_percentage: Percentage of white matter points to use as control\
            points
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = register_elderly_subject_params(
        output_fsamples=output_fsamples,
        output_norm=output_norm,
        input_volume=input_volume,
        gca_file=gca_file,
        transform_file=transform_file,
        sampling_percentage=sampling_percentage,
    )
    return register_elderly_subject_execute(params, runner)

__all__ = [
    "RegisterElderlySubject",
    "RegisterElderlySubjectOutputs",
    "REGISTER_ELDERLY_SUBJECT_METADATA",
    "register_elderly_subject_cargs",
    "register_elderly_subject_outputs",
    "register_elderly_subject_params",
    "register_elderly_subject_execute",
    "register_elderly_subject_validate",
    "register_elderly_subject",
]