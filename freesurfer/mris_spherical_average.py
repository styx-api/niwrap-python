# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SPHERICAL_AVERAGE_METADATA = Metadata(
    id="mris_spherical_average",
    name="mris_spherical_average",
    package="freesurfer",
)

MrisSphericalAverageWhich = typing.Literal["coords"] | typing.Literal["label"] | typing.Literal["vals"] | typing.Literal["curv"] | typing.Literal["area"]

MrisSphericalAverageHemi = typing.Literal["lh"] | typing.Literal["rh"]

MrisSphericalAverage = typing.TypedDict(
    "MrisSphericalAverage",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_spherical_average"]],
        "which": MrisSphericalAverageWhich,
        "fname": str,
        "hemi": MrisSphericalAverageHemi,
        "spherical_surf": str,
        "subjects": list[str],
        "output": str,
        "segment": typing.NotRequired[bool],
        "normalize": typing.NotRequired[bool],
        "orig": typing.NotRequired[str],
        "output_subject_name": typing.NotRequired[str],
        "output_subject_dir": typing.NotRequired[str],
        "subjects_dir": typing.NotRequired[str],
        "average_area": typing.NotRequired[bool],
        "summary_statistics": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisSphericalAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_spherical_average_params(
    which: MrisSphericalAverageWhich,
    fname: str,
    hemi: MrisSphericalAverageHemi,
    spherical_surf: str,
    subjects: list[str],
    output: str,
    segment: bool = False,
    normalize: bool = False,
    orig: str | None = None,
    output_subject_name: str | None = None,
    output_subject_dir: str | None = None,
    subjects_dir: str | None = None,
    average_area: bool = False,
    summary_statistics: str | None = None,
) -> MrisSphericalAverage:
    """
    Build parameters.

    Args:
        which: Specifies the type, one of: coords, label, vals, curv, or area.
        fname: Input file name.
        hemi: Hemisphere: one of lh or rh.
        spherical_surf: The spherical surface file.
        subjects: List of subjects.
        output: Output file or directory.
        segment: Only use largest connected component of label.
        normalize: Normalize output so it can be interpreted as a probability.
        orig: Use <name> as original surface position; default=orig.
        output_subject_name: Use <output subject name> as the space to write the\
            results in instead of the last subject given.
        output_subject_dir: Use <output subject dir> as the subjects dir for the\
            output subject.
        subjects_dir: Set the subjects directory.
        average_area: Compute threshold for label that will give the average label\
            approximately the average surface area.
        summary_statistics: Generate summary statistics and write them into\
            sigavg<cond #>-<hemi>.w and sigvar<cond #>-<hemi>.w.

    Returns:
        Parameter dictionary.
    """
    params: MrisSphericalAverage = {
        "@type": "freesurfer/mris_spherical_average",
        "which": which,
        "fname": fname,
        "hemi": hemi,
        "spherical_surf": spherical_surf,
        "subjects": subjects,
        "output": output,
        "segment": segment,
        "normalize": normalize,
        "average_area": average_area,
    }
    if orig is not None:
        params["orig"] = orig
    if output_subject_name is not None:
        params["output_subject_name"] = output_subject_name
    if output_subject_dir is not None:
        params["output_subject_dir"] = output_subject_dir
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if summary_statistics is not None:
        params["summary_statistics"] = summary_statistics
    return params

def mris_spherical_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSphericalAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("which", None) is None:
        raise StyxValidationError("`which` must not be None")
    if not isinstance(params["which"], str):
        raise StyxValidationError(f'`which` has the wrong type: Received `{type(params["which"])}` expected `MrisSphericalAverageWhich`')
    if params["which"] not in ["coords", "label", "vals", "curv", "area"]:
        raise StyxValidationError("Parameter `which` must be one of [\"coords\", \"label\", \"vals\", \"curv\", \"area\"]")
    if params.get("fname", None) is None:
        raise StyxValidationError("`fname` must not be None")
    if not isinstance(params["fname"], str):
        raise StyxValidationError(f'`fname` has the wrong type: Received `{type(params["fname"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `MrisSphericalAverageHemi`')
    if params["hemi"] not in ["lh", "rh"]:
        raise StyxValidationError("Parameter `hemi` must be one of [\"lh\", \"rh\"]")
    if params.get("spherical_surf", None) is None:
        raise StyxValidationError("`spherical_surf` must not be None")
    if not isinstance(params["spherical_surf"], str):
        raise StyxValidationError(f'`spherical_surf` has the wrong type: Received `{type(params["spherical_surf"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("segment", None) is not None:
        if not isinstance(params["segment"], bool):
            raise StyxValidationError(f'`segment` has the wrong type: Received `{type(params["segment"])}` expected `bool`')
    if params.get("normalize", None) is not None:
        if not isinstance(params["normalize"], bool):
            raise StyxValidationError(f'`normalize` has the wrong type: Received `{type(params["normalize"])}` expected `bool`')
    if params.get("orig", None) is not None:
        if not isinstance(params["orig"], str):
            raise StyxValidationError(f'`orig` has the wrong type: Received `{type(params["orig"])}` expected `str`')
    if params.get("output_subject_name", None) is not None:
        if not isinstance(params["output_subject_name"], str):
            raise StyxValidationError(f'`output_subject_name` has the wrong type: Received `{type(params["output_subject_name"])}` expected `str`')
    if params.get("output_subject_dir", None) is not None:
        if not isinstance(params["output_subject_dir"], str):
            raise StyxValidationError(f'`output_subject_dir` has the wrong type: Received `{type(params["output_subject_dir"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("average_area", None) is not None:
        if not isinstance(params["average_area"], bool):
            raise StyxValidationError(f'`average_area` has the wrong type: Received `{type(params["average_area"])}` expected `bool`')
    if params.get("summary_statistics", None) is not None:
        if not isinstance(params["summary_statistics"], str):
            raise StyxValidationError(f'`summary_statistics` has the wrong type: Received `{type(params["summary_statistics"])}` expected `str`')

def mris_spherical_average_cargs(params: MrisSphericalAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_spherical_average")
    cargs.append(str(params["which"]))
    cargs.append(params["fname"])
    cargs.append(str(params["hemi"]))
    cargs.append(params["spherical_surf"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output"])
    if params.get("segment"):
        cargs.append("-segment")
    if params.get("normalize"):
        cargs.append("-n")
    v_0 = params.get("orig")
    if v_0 is not None:
        cargs.append("-orig")
        cargs.append(v_0)
    v_1 = params.get("output_subject_name")
    if v_1 is not None:
        cargs.append("-o")
        cargs.append(v_1)
    v_2 = params.get("output_subject_dir")
    if v_2 is not None:
        cargs.append("-osdir")
        cargs.append(v_2)
    v_3 = params.get("subjects_dir")
    if v_3 is not None:
        cargs.append("-sdir")
        cargs.append(v_3)
    if params.get("average_area"):
        cargs.append("-average_area")
    v_4 = params.get("summary_statistics")
    if v_4 is not None:
        cargs.append("-s")
        cargs.append(v_4)
    return cargs

def mris_spherical_average_outputs(params: MrisSphericalAverage, execution: Execution) -> MrisSphericalAverageOutputs:
    """Build the MrisSphericalAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisSphericalAverageOutputs(
        root=root_v,
    )

def mris_spherical_average_execute(params: MrisSphericalAverage, runner: Runner | None = None) -> MrisSphericalAverageOutputs:
    """
    mris_spherical_average

    This tool adds a template into an average surface in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_spherical_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SPHERICAL_AVERAGE_METADATA)
    execution.params(params)
    args = mris_spherical_average_cargs(params, execution)
    out = mris_spherical_average_outputs(params, execution)
    execution.run(args)
    return out

def mris_spherical_average(
    which: MrisSphericalAverageWhich,
    fname: str,
    hemi: MrisSphericalAverageHemi,
    spherical_surf: str,
    subjects: list[str],
    output: str,
    segment: bool = False,
    normalize: bool = False,
    orig: str | None = None,
    output_subject_name: str | None = None,
    output_subject_dir: str | None = None,
    subjects_dir: str | None = None,
    average_area: bool = False,
    summary_statistics: str | None = None,
    runner: Runner | None = None,
) -> MrisSphericalAverageOutputs:
    """
    mris_spherical_average

    This tool adds a template into an average surface in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        which: Specifies the type, one of: coords, label, vals, curv, or area.
        fname: Input file name.
        hemi: Hemisphere: one of lh or rh.
        spherical_surf: The spherical surface file.
        subjects: List of subjects.
        output: Output file or directory.
        segment: Only use largest connected component of label.
        normalize: Normalize output so it can be interpreted as a probability.
        orig: Use <name> as original surface position; default=orig.
        output_subject_name: Use <output subject name> as the space to write the\
            results in instead of the last subject given.
        output_subject_dir: Use <output subject dir> as the subjects dir for the\
            output subject.
        subjects_dir: Set the subjects directory.
        average_area: Compute threshold for label that will give the average label\
            approximately the average surface area.
        summary_statistics: Generate summary statistics and write them into\
            sigavg<cond #>-<hemi>.w and sigvar<cond #>-<hemi>.w.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_spherical_average_params(
        which=which,
        fname=fname,
        hemi=hemi,
        spherical_surf=spherical_surf,
        subjects=subjects,
        output=output,
        segment=segment,
        normalize=normalize,
        orig=orig,
        output_subject_name=output_subject_name,
        output_subject_dir=output_subject_dir,
        subjects_dir=subjects_dir,
        average_area=average_area,
        summary_statistics=summary_statistics,
    )
    return mris_spherical_average_execute(params, runner)

__all__ = [
    "MrisSphericalAverage",
    "MrisSphericalAverageOutputs",
    "MRIS_SPHERICAL_AVERAGE_METADATA",
    "mris_spherical_average_cargs",
    "mris_spherical_average_outputs",
    "mris_spherical_average_params",
    "mris_spherical_average_execute",
    "mris_spherical_average_validate",
    "mris_spherical_average",
]