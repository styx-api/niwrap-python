# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TALAIRACH_MGH_METADATA = Metadata(
    id="talairach_mgh",
    name="talairach_mgh",
    package="freesurfer",
)

TalairachMgh = typing.TypedDict(
    "TalairachMgh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/talairach_mgh"]],
        "input_volume": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class TalairachMghOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_output: OutputPathType
    """Transformed output volume aligned with Talairach reference brain"""

def talairach_mgh_params(
    input_volume: InputPathType,
    output_volume: str,
) -> TalairachMgh:
    """
    Build parameters.

    Args:
        input_volume: Input volume file for the talairach transformation
        output_volume: Output volume file for the talairach transformation

    Returns:
        Parameter dictionary.
    """
    params: TalairachMgh = {
        "@type": "freesurfer/talairach_mgh",
        "input_volume": input_volume,
        "output_volume": output_volume,
    }
    return params

def talairach_mgh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TalairachMgh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def talairach_mgh_cargs(params: TalairachMgh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("talairach_mgh")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_volume"])
    return cargs

def talairach_mgh_outputs(params: TalairachMgh, execution: Execution) -> TalairachMghOutputs:
    """Build the TalairachMghOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_output_v: OutputPathType = execution.output_file(params["output_volume"])
    return TalairachMghOutputs(
        root=root_v,
        transformed_output=transformed_output_v,
    )

def talairach_mgh_execute(params: TalairachMgh, runner: Runner | None = None) -> TalairachMghOutputs:
    """
    talairach_mgh

    A tool for aligning brain volume with Talairach reference brain

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    talairach_mgh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TALAIRACH_MGH_METADATA)
    execution.params(params)
    args = talairach_mgh_cargs(params, execution)
    out = talairach_mgh_outputs(params, execution)
    execution.run(args)
    return out

def talairach_mgh(
    input_volume: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> TalairachMghOutputs:
    """
    talairach_mgh

    A tool for aligning brain volume with Talairach reference brain

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file for the talairach transformation
        output_volume: Output volume file for the talairach transformation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = talairach_mgh_params(
        input_volume=input_volume,
        output_volume=output_volume,
    )
    return talairach_mgh_execute(params, runner)

__all__ = [
    "TalairachMgh",
    "TalairachMghOutputs",
    "TALAIRACH_MGH_METADATA",
    "talairach_mgh_cargs",
    "talairach_mgh_outputs",
    "talairach_mgh_params",
    "talairach_mgh_execute",
    "talairach_mgh_validate",
    "talairach_mgh",
]