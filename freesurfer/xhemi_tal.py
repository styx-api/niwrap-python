# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XHEMI_TAL_METADATA = Metadata(
    id="xhemi-tal",
    name="xhemi-tal",
    package="freesurfer",
)

XhemiTal = typing.TypedDict(
    "XhemiTal",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xhemi-tal"]],
        "subject": str,
    },
)

@dataclasses.dataclass
class XhemiTalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def xhemi_tal_params(
    subject: str,
) -> XhemiTal:
    """
    Build parameters.

    Args:
        subject:

    Returns:
        Parameter dictionary.
    """
    params: XhemiTal = {
        "@type": "freesurfer/xhemi-tal",
        "subject": subject,
    }
    return params

def xhemi_tal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid XhemiTal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')

def xhemi_tal_cargs(params: XhemiTal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xhemi-tal")
    cargs.append("--s")
    cargs.append(params["subject"])
    return cargs

def xhemi_tal_outputs(params: XhemiTal, execution: Execution) -> XhemiTalOutputs:
    """Build the XhemiTalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return XhemiTalOutputs(
        root=root_v,
    )

def xhemi_tal_execute(params: XhemiTal, runner: Runner | None = None) -> XhemiTalOutputs:
    """
    xhemi-tal

    Computes the talairach.xfm for xhemi based on the original (unflipped) talairach.xfm

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xhemi_tal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XHEMI_TAL_METADATA)
    execution.params(params)
    args = xhemi_tal_cargs(params, execution)
    out = xhemi_tal_outputs(params, execution)
    execution.run(args)
    return out

def xhemi_tal(
    subject: str,
    runner: Runner | None = None,
) -> XhemiTalOutputs:
    """
    xhemi-tal

    Computes the talairach.xfm for xhemi based on the original (unflipped) talairach.xfm

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xhemi_tal_params(
        subject=subject,
    )
    return xhemi_tal_execute(params, runner)

__all__ = [
    "XhemiTal",
    "XhemiTalOutputs",
    "XHEMI_TAL_METADATA",
    "xhemi_tal_cargs",
    "xhemi_tal_outputs",
    "xhemi_tal_params",
    "xhemi_tal_execute",
    "xhemi_tal_validate",
    "xhemi_tal",
]