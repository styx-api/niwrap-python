# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FWHM_METADATA = Metadata(
    id="mri_fwhm",
    name="mri_fwhm",
    package="freesurfer",
)

MriFwhm = typing.TypedDict(
    "MriFwhm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_fwhm"]],
        "inputvol": InputPathType,
        "outputvol": str,
        "save_detrended": typing.NotRequired[bool],
        "save_unmasked": typing.NotRequired[bool],
        "smooth_only": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
        "mask_thresh": typing.NotRequired[float],
        "auto_mask": typing.NotRequired[float],
        "nerode": typing.NotRequired[float],
        "mask_inv": typing.NotRequired[bool],
        "out_mask": typing.NotRequired[str],
        "detrend_matrix": typing.NotRequired[InputPathType],
        "detrend_order": typing.NotRequired[float],
        "square_input": typing.NotRequired[bool],
        "smooth_by_fwhm": typing.NotRequired[float],
        "smooth_by_gstd": typing.NotRequired[float],
        "median_filter": typing.NotRequired[float],
        "smooth_to_fwhm": typing.NotRequired[float],
        "to_fwhm_tol": typing.NotRequired[float],
        "to_fwhm_nmax": typing.NotRequired[float],
        "to_fwhm_file": typing.NotRequired[str],
        "summary_file": typing.NotRequired[str],
        "dat_file": typing.NotRequired[str],
        "fwhm_dat_file": typing.NotRequired[str],
        "fwhm_vol_mean_file": typing.NotRequired[str],
        "fwhm_vol": typing.NotRequired[str],
        "synth": typing.NotRequired[bool],
        "synth_frames": typing.NotRequired[float],
        "nframes_min": typing.NotRequired[float],
        "ispm": typing.NotRequired[bool],
        "nspm_zero_padding": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriFwhmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Output volume after smoothing."""
    final_mask_output_file: OutputPathType | None
    """Final mask volume."""
    summary_log_file: OutputPathType | None
    """Summary log file."""
    final_fwhm_estimate_file: OutputPathType | None
    """Final FWHM estimate file."""
    fwhm_of_each_dimension_file: OutputPathType | None
    """File containing the FWHM of each dimension."""
    mean_fwhm_volume_file: OutputPathType | None
    """Mean FWHM from volume file."""
    fwhm_volume_file: OutputPathType | None
    """FWHM volume file."""

def mri_fwhm_params(
    inputvol: InputPathType,
    outputvol: str,
    save_detrended: bool = False,
    save_unmasked: bool = False,
    smooth_only: bool = False,
    mask: InputPathType | None = None,
    mask_thresh: float | None = None,
    auto_mask: float | None = None,
    nerode: float | None = None,
    mask_inv: bool = False,
    out_mask: str | None = None,
    detrend_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    square_input: bool = False,
    smooth_by_fwhm: float | None = None,
    smooth_by_gstd: float | None = None,
    median_filter: float | None = None,
    smooth_to_fwhm: float | None = None,
    to_fwhm_tol: float | None = None,
    to_fwhm_nmax: float | None = None,
    to_fwhm_file: str | None = None,
    summary_file: str | None = None,
    dat_file: str | None = None,
    fwhm_dat_file: str | None = None,
    fwhm_vol_mean_file: str | None = None,
    fwhm_vol: str | None = None,
    synth: bool = False,
    synth_frames: float | None = None,
    nframes_min: float | None = None,
    ispm: bool = False,
    nspm_zero_padding: float | None = None,
    threads: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
) -> MriFwhm:
    """
    Build parameters.

    Args:
        inputvol: Input volume file. Format must be something readable by\
            mri_convert (e.g., mgh, mgz, img, nii, nii.gz).
        outputvol: Output volume file: save input after smoothing.
        save_detrended: Save input after smoothing and detrending.
        save_unmasked: Do not mask output volume.
        smooth_only: Smooth and save, do not compute fwhm.
        mask: Binary mask file.
        mask_thresh: Threshold for mask (default is 0.5).
        auto_mask: Auto compute mask based on global mean threshold.
        nerode: Erode mask n times prior to FWHM computation.
        mask_inv: Invert mask.
        out_mask: Save final mask to outmaskvol.
        detrend_matrix: Detrending matrix file in MATLAB4 format.
        detrend_order: Polynomial detrending order (default 0).
        square_input: Compute square of input before smoothing.
        smooth_by_fwhm: Smooth BY fwhm before measuring.
        smooth_by_gstd: Smooth using gstd (equivalent to --fwhm).
        median_filter: Perform median filtering instead of Gaussian.
        smooth_to_fwhm: Smooth TO this FWHM.
        to_fwhm_tol: Tolerance for smoothing to FWHM (default 0.5mm).
        to_fwhm_nmax: Maximum iterations for smoothing to FWHM (default 20).
        to_fwhm_file: Save smoothing to FWHM parameters to file.
        summary_file: Summary/log file.
        dat_file: Prints only the final FWHM estimate into this file.
        fwhm_dat_file: Compute and save the FWHM of each dimension.
        fwhm_vol_mean_file: Compute and save the FWHM of each dimension based on\
            fwhmvol.
        fwhm_vol: Save FWHM volume.
        synth: Synthesize input with white Gaussian noise.
        synth_frames: Number of frames for synthesized input (default is 10).
        nframes_min: Require at least this many frames.
        ispm: Input is SPM-analyze.
        nspm_zero_padding: Zero-padding for SPM-analyze.
        threads: Set OPEN MP threads.
        debug: Turn on debugging.
        checkopts: Check options and exit without running.
        version: Print version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriFwhm = {
        "@type": "freesurfer/mri_fwhm",
        "inputvol": inputvol,
        "outputvol": outputvol,
        "save_detrended": save_detrended,
        "save_unmasked": save_unmasked,
        "smooth_only": smooth_only,
        "mask_inv": mask_inv,
        "square_input": square_input,
        "synth": synth,
        "ispm": ispm,
        "debug": debug,
        "checkopts": checkopts,
        "version": version,
    }
    if mask is not None:
        params["mask"] = mask
    if mask_thresh is not None:
        params["mask_thresh"] = mask_thresh
    if auto_mask is not None:
        params["auto_mask"] = auto_mask
    if nerode is not None:
        params["nerode"] = nerode
    if out_mask is not None:
        params["out_mask"] = out_mask
    if detrend_matrix is not None:
        params["detrend_matrix"] = detrend_matrix
    if detrend_order is not None:
        params["detrend_order"] = detrend_order
    if smooth_by_fwhm is not None:
        params["smooth_by_fwhm"] = smooth_by_fwhm
    if smooth_by_gstd is not None:
        params["smooth_by_gstd"] = smooth_by_gstd
    if median_filter is not None:
        params["median_filter"] = median_filter
    if smooth_to_fwhm is not None:
        params["smooth_to_fwhm"] = smooth_to_fwhm
    if to_fwhm_tol is not None:
        params["to_fwhm_tol"] = to_fwhm_tol
    if to_fwhm_nmax is not None:
        params["to_fwhm_nmax"] = to_fwhm_nmax
    if to_fwhm_file is not None:
        params["to_fwhm_file"] = to_fwhm_file
    if summary_file is not None:
        params["summary_file"] = summary_file
    if dat_file is not None:
        params["dat_file"] = dat_file
    if fwhm_dat_file is not None:
        params["fwhm_dat_file"] = fwhm_dat_file
    if fwhm_vol_mean_file is not None:
        params["fwhm_vol_mean_file"] = fwhm_vol_mean_file
    if fwhm_vol is not None:
        params["fwhm_vol"] = fwhm_vol
    if synth_frames is not None:
        params["synth_frames"] = synth_frames
    if nframes_min is not None:
        params["nframes_min"] = nframes_min
    if nspm_zero_padding is not None:
        params["nspm_zero_padding"] = nspm_zero_padding
    if threads is not None:
        params["threads"] = threads
    return params

def mri_fwhm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFwhm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputvol", None) is None:
        raise StyxValidationError("`inputvol` must not be None")
    if not isinstance(params["inputvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputvol` has the wrong type: Received `{type(params["inputvol"])}` expected `InputPathType`')
    if params.get("outputvol", None) is None:
        raise StyxValidationError("`outputvol` must not be None")
    if not isinstance(params["outputvol"], str):
        raise StyxValidationError(f'`outputvol` has the wrong type: Received `{type(params["outputvol"])}` expected `str`')
    if params.get("save_detrended", None) is not None:
        if not isinstance(params["save_detrended"], bool):
            raise StyxValidationError(f'`save_detrended` has the wrong type: Received `{type(params["save_detrended"])}` expected `bool`')
    if params.get("save_unmasked", None) is not None:
        if not isinstance(params["save_unmasked"], bool):
            raise StyxValidationError(f'`save_unmasked` has the wrong type: Received `{type(params["save_unmasked"])}` expected `bool`')
    if params.get("smooth_only", None) is not None:
        if not isinstance(params["smooth_only"], bool):
            raise StyxValidationError(f'`smooth_only` has the wrong type: Received `{type(params["smooth_only"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("mask_thresh", None) is not None:
        if not isinstance(params["mask_thresh"], (float, int)):
            raise StyxValidationError(f'`mask_thresh` has the wrong type: Received `{type(params["mask_thresh"])}` expected `float`')
    if params.get("auto_mask", None) is not None:
        if not isinstance(params["auto_mask"], (float, int)):
            raise StyxValidationError(f'`auto_mask` has the wrong type: Received `{type(params["auto_mask"])}` expected `float`')
    if params.get("nerode", None) is not None:
        if not isinstance(params["nerode"], (float, int)):
            raise StyxValidationError(f'`nerode` has the wrong type: Received `{type(params["nerode"])}` expected `float`')
    if params.get("mask_inv", None) is not None:
        if not isinstance(params["mask_inv"], bool):
            raise StyxValidationError(f'`mask_inv` has the wrong type: Received `{type(params["mask_inv"])}` expected `bool`')
    if params.get("out_mask", None) is not None:
        if not isinstance(params["out_mask"], str):
            raise StyxValidationError(f'`out_mask` has the wrong type: Received `{type(params["out_mask"])}` expected `str`')
    if params.get("detrend_matrix", None) is not None:
        if not isinstance(params["detrend_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`detrend_matrix` has the wrong type: Received `{type(params["detrend_matrix"])}` expected `InputPathType`')
    if params.get("detrend_order", None) is not None:
        if not isinstance(params["detrend_order"], (float, int)):
            raise StyxValidationError(f'`detrend_order` has the wrong type: Received `{type(params["detrend_order"])}` expected `float`')
    if params.get("square_input", None) is not None:
        if not isinstance(params["square_input"], bool):
            raise StyxValidationError(f'`square_input` has the wrong type: Received `{type(params["square_input"])}` expected `bool`')
    if params.get("smooth_by_fwhm", None) is not None:
        if not isinstance(params["smooth_by_fwhm"], (float, int)):
            raise StyxValidationError(f'`smooth_by_fwhm` has the wrong type: Received `{type(params["smooth_by_fwhm"])}` expected `float`')
    if params.get("smooth_by_gstd", None) is not None:
        if not isinstance(params["smooth_by_gstd"], (float, int)):
            raise StyxValidationError(f'`smooth_by_gstd` has the wrong type: Received `{type(params["smooth_by_gstd"])}` expected `float`')
    if params.get("median_filter", None) is not None:
        if not isinstance(params["median_filter"], (float, int)):
            raise StyxValidationError(f'`median_filter` has the wrong type: Received `{type(params["median_filter"])}` expected `float`')
    if params.get("smooth_to_fwhm", None) is not None:
        if not isinstance(params["smooth_to_fwhm"], (float, int)):
            raise StyxValidationError(f'`smooth_to_fwhm` has the wrong type: Received `{type(params["smooth_to_fwhm"])}` expected `float`')
    if params.get("to_fwhm_tol", None) is not None:
        if not isinstance(params["to_fwhm_tol"], (float, int)):
            raise StyxValidationError(f'`to_fwhm_tol` has the wrong type: Received `{type(params["to_fwhm_tol"])}` expected `float`')
    if params.get("to_fwhm_nmax", None) is not None:
        if not isinstance(params["to_fwhm_nmax"], (float, int)):
            raise StyxValidationError(f'`to_fwhm_nmax` has the wrong type: Received `{type(params["to_fwhm_nmax"])}` expected `float`')
    if params.get("to_fwhm_file", None) is not None:
        if not isinstance(params["to_fwhm_file"], str):
            raise StyxValidationError(f'`to_fwhm_file` has the wrong type: Received `{type(params["to_fwhm_file"])}` expected `str`')
    if params.get("summary_file", None) is not None:
        if not isinstance(params["summary_file"], str):
            raise StyxValidationError(f'`summary_file` has the wrong type: Received `{type(params["summary_file"])}` expected `str`')
    if params.get("dat_file", None) is not None:
        if not isinstance(params["dat_file"], str):
            raise StyxValidationError(f'`dat_file` has the wrong type: Received `{type(params["dat_file"])}` expected `str`')
    if params.get("fwhm_dat_file", None) is not None:
        if not isinstance(params["fwhm_dat_file"], str):
            raise StyxValidationError(f'`fwhm_dat_file` has the wrong type: Received `{type(params["fwhm_dat_file"])}` expected `str`')
    if params.get("fwhm_vol_mean_file", None) is not None:
        if not isinstance(params["fwhm_vol_mean_file"], str):
            raise StyxValidationError(f'`fwhm_vol_mean_file` has the wrong type: Received `{type(params["fwhm_vol_mean_file"])}` expected `str`')
    if params.get("fwhm_vol", None) is not None:
        if not isinstance(params["fwhm_vol"], str):
            raise StyxValidationError(f'`fwhm_vol` has the wrong type: Received `{type(params["fwhm_vol"])}` expected `str`')
    if params.get("synth", None) is not None:
        if not isinstance(params["synth"], bool):
            raise StyxValidationError(f'`synth` has the wrong type: Received `{type(params["synth"])}` expected `bool`')
    if params.get("synth_frames", None) is not None:
        if not isinstance(params["synth_frames"], (float, int)):
            raise StyxValidationError(f'`synth_frames` has the wrong type: Received `{type(params["synth_frames"])}` expected `float`')
    if params.get("nframes_min", None) is not None:
        if not isinstance(params["nframes_min"], (float, int)):
            raise StyxValidationError(f'`nframes_min` has the wrong type: Received `{type(params["nframes_min"])}` expected `float`')
    if params.get("ispm", None) is not None:
        if not isinstance(params["ispm"], bool):
            raise StyxValidationError(f'`ispm` has the wrong type: Received `{type(params["ispm"])}` expected `bool`')
    if params.get("nspm_zero_padding", None) is not None:
        if not isinstance(params["nspm_zero_padding"], (float, int)):
            raise StyxValidationError(f'`nspm_zero_padding` has the wrong type: Received `{type(params["nspm_zero_padding"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_fwhm_cargs(params: MriFwhm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_fwhm")
    cargs.append("--i")
    cargs.append(execution.input_file(params["inputvol"]))
    cargs.append("--o")
    cargs.append(params["outputvol"])
    if params.get("save_detrended"):
        cargs.append("--save-detrended")
    if params.get("save_unmasked"):
        cargs.append("--save-unmasked")
    if params.get("smooth_only"):
        cargs.append("--smooth-only")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask_thresh")
    if v_1 is not None:
        cargs.append("--mask-thresh")
        cargs.append(str(v_1))
    v_2 = params.get("auto_mask")
    if v_2 is not None:
        cargs.append("--auto-mask")
        cargs.append(str(v_2))
    v_3 = params.get("nerode")
    if v_3 is not None:
        cargs.append("--nerode")
        cargs.append(str(v_3))
    if params.get("mask_inv"):
        cargs.append("--mask-inv")
    v_4 = params.get("out_mask")
    if v_4 is not None:
        cargs.append("--out-mask")
        cargs.append(v_4)
    v_5 = params.get("detrend_matrix")
    if v_5 is not None:
        cargs.append("--X")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("detrend_order")
    if v_6 is not None:
        cargs.append("--detrend")
        cargs.append(str(v_6))
    if params.get("square_input"):
        cargs.append("--sqr")
    v_7 = params.get("smooth_by_fwhm")
    if v_7 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_7))
    v_8 = params.get("smooth_by_gstd")
    if v_8 is not None:
        cargs.append("--gstd")
        cargs.append(str(v_8))
    v_9 = params.get("median_filter")
    if v_9 is not None:
        cargs.append("--median")
        cargs.append(str(v_9))
    v_10 = params.get("smooth_to_fwhm")
    if v_10 is not None:
        cargs.append("--to-fwhm")
        cargs.append(str(v_10))
    v_11 = params.get("to_fwhm_tol")
    if v_11 is not None:
        cargs.append("--to-fwhm-tol")
        cargs.append(str(v_11))
    v_12 = params.get("to_fwhm_nmax")
    if v_12 is not None:
        cargs.append("--to-fwhm-nmax")
        cargs.append(str(v_12))
    v_13 = params.get("to_fwhm_file")
    if v_13 is not None:
        cargs.append("--to-fwhm-file")
        cargs.append(v_13)
    v_14 = params.get("summary_file")
    if v_14 is not None:
        cargs.append("--sum")
        cargs.append(v_14)
    v_15 = params.get("dat_file")
    if v_15 is not None:
        cargs.append("--dat")
        cargs.append(v_15)
    v_16 = params.get("fwhm_dat_file")
    if v_16 is not None:
        cargs.append("--fwhmdat")
        cargs.append(v_16)
    v_17 = params.get("fwhm_vol_mean_file")
    if v_17 is not None:
        cargs.append("--fwhmvolmn")
        cargs.append(v_17)
    v_18 = params.get("fwhm_vol")
    if v_18 is not None:
        cargs.append("--fwhmvol")
        cargs.append(v_18)
    if params.get("synth"):
        cargs.append("--synth")
    v_19 = params.get("synth_frames")
    if v_19 is not None:
        cargs.append("--synth-frames")
        cargs.append(str(v_19))
    v_20 = params.get("nframes_min")
    if v_20 is not None:
        cargs.append("--nframesmin")
        cargs.append(str(v_20))
    if params.get("ispm"):
        cargs.append("--ispm")
    v_21 = params.get("nspm_zero_padding")
    if v_21 is not None:
        cargs.append("--in_nspmzeropad")
        cargs.append(str(v_21))
    v_22 = params.get("threads")
    if v_22 is not None:
        cargs.append("--nthreads")
        cargs.append(str(v_22))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_fwhm_outputs(params: MriFwhm, execution: Execution) -> MriFwhmOutputs:
    """Build the MriFwhmOutputs object for this tool."""
    final_mask_output_file_v: OutputPathType | None = None
    summary_log_file_v: OutputPathType | None = None
    final_fwhm_estimate_file_v: OutputPathType | None = None
    fwhm_of_each_dimension_file_v: OutputPathType | None = None
    mean_fwhm_volume_file_v: OutputPathType | None = None
    fwhm_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["outputvol"])
    __v0 = params.get("out_mask")
    if __v0 is not None:
        final_mask_output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("summary_file")
    if __v1 is not None:
        summary_log_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("dat_file")
    if __v2 is not None:
        final_fwhm_estimate_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("fwhm_dat_file")
    if __v3 is not None:
        fwhm_of_each_dimension_file_v = execution.output_file((__v3 if __v3 is not None else ""))
    __v4 = params.get("fwhm_vol_mean_file")
    if __v4 is not None:
        mean_fwhm_volume_file_v = execution.output_file((__v4 if __v4 is not None else ""))
    __v5 = params.get("fwhm_vol")
    if __v5 is not None:
        fwhm_volume_file_v = execution.output_file((__v5 if __v5 is not None else ""))
    return MriFwhmOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
        final_mask_output_file=final_mask_output_file_v,
        summary_log_file=summary_log_file_v,
        final_fwhm_estimate_file=final_fwhm_estimate_file_v,
        fwhm_of_each_dimension_file=fwhm_of_each_dimension_file_v,
        mean_fwhm_volume_file=mean_fwhm_volume_file_v,
        fwhm_volume_file=fwhm_volume_file_v,
    )

def mri_fwhm_execute(params: MriFwhm, runner: Runner | None = None) -> MriFwhmOutputs:
    """
    mri_fwhm

    FreeSurfer program to estimate the global Gaussian smoothness of a multi-frame, volume-based data set.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_fwhm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FWHM_METADATA)
    execution.params(params)
    args = mri_fwhm_cargs(params, execution)
    out = mri_fwhm_outputs(params, execution)
    execution.run(args)
    return out

def mri_fwhm(
    inputvol: InputPathType,
    outputvol: str,
    save_detrended: bool = False,
    save_unmasked: bool = False,
    smooth_only: bool = False,
    mask: InputPathType | None = None,
    mask_thresh: float | None = None,
    auto_mask: float | None = None,
    nerode: float | None = None,
    mask_inv: bool = False,
    out_mask: str | None = None,
    detrend_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    square_input: bool = False,
    smooth_by_fwhm: float | None = None,
    smooth_by_gstd: float | None = None,
    median_filter: float | None = None,
    smooth_to_fwhm: float | None = None,
    to_fwhm_tol: float | None = None,
    to_fwhm_nmax: float | None = None,
    to_fwhm_file: str | None = None,
    summary_file: str | None = None,
    dat_file: str | None = None,
    fwhm_dat_file: str | None = None,
    fwhm_vol_mean_file: str | None = None,
    fwhm_vol: str | None = None,
    synth: bool = False,
    synth_frames: float | None = None,
    nframes_min: float | None = None,
    ispm: bool = False,
    nspm_zero_padding: float | None = None,
    threads: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriFwhmOutputs:
    """
    mri_fwhm

    FreeSurfer program to estimate the global Gaussian smoothness of a multi-frame, volume-based data set.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inputvol: Input volume file. Format must be something readable by\
            mri_convert (e.g., mgh, mgz, img, nii, nii.gz).
        outputvol: Output volume file: save input after smoothing.
        save_detrended: Save input after smoothing and detrending.
        save_unmasked: Do not mask output volume.
        smooth_only: Smooth and save, do not compute fwhm.
        mask: Binary mask file.
        mask_thresh: Threshold for mask (default is 0.5).
        auto_mask: Auto compute mask based on global mean threshold.
        nerode: Erode mask n times prior to FWHM computation.
        mask_inv: Invert mask.
        out_mask: Save final mask to outmaskvol.
        detrend_matrix: Detrending matrix file in MATLAB4 format.
        detrend_order: Polynomial detrending order (default 0).
        square_input: Compute square of input before smoothing.
        smooth_by_fwhm: Smooth BY fwhm before measuring.
        smooth_by_gstd: Smooth using gstd (equivalent to --fwhm).
        median_filter: Perform median filtering instead of Gaussian.
        smooth_to_fwhm: Smooth TO this FWHM.
        to_fwhm_tol: Tolerance for smoothing to FWHM (default 0.5mm).
        to_fwhm_nmax: Maximum iterations for smoothing to FWHM (default 20).
        to_fwhm_file: Save smoothing to FWHM parameters to file.
        summary_file: Summary/log file.
        dat_file: Prints only the final FWHM estimate into this file.
        fwhm_dat_file: Compute and save the FWHM of each dimension.
        fwhm_vol_mean_file: Compute and save the FWHM of each dimension based on\
            fwhmvol.
        fwhm_vol: Save FWHM volume.
        synth: Synthesize input with white Gaussian noise.
        synth_frames: Number of frames for synthesized input (default is 10).
        nframes_min: Require at least this many frames.
        ispm: Input is SPM-analyze.
        nspm_zero_padding: Zero-padding for SPM-analyze.
        threads: Set OPEN MP threads.
        debug: Turn on debugging.
        checkopts: Check options and exit without running.
        version: Print version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_fwhm_params(
        inputvol=inputvol,
        outputvol=outputvol,
        save_detrended=save_detrended,
        save_unmasked=save_unmasked,
        smooth_only=smooth_only,
        mask=mask,
        mask_thresh=mask_thresh,
        auto_mask=auto_mask,
        nerode=nerode,
        mask_inv=mask_inv,
        out_mask=out_mask,
        detrend_matrix=detrend_matrix,
        detrend_order=detrend_order,
        square_input=square_input,
        smooth_by_fwhm=smooth_by_fwhm,
        smooth_by_gstd=smooth_by_gstd,
        median_filter=median_filter,
        smooth_to_fwhm=smooth_to_fwhm,
        to_fwhm_tol=to_fwhm_tol,
        to_fwhm_nmax=to_fwhm_nmax,
        to_fwhm_file=to_fwhm_file,
        summary_file=summary_file,
        dat_file=dat_file,
        fwhm_dat_file=fwhm_dat_file,
        fwhm_vol_mean_file=fwhm_vol_mean_file,
        fwhm_vol=fwhm_vol,
        synth=synth,
        synth_frames=synth_frames,
        nframes_min=nframes_min,
        ispm=ispm,
        nspm_zero_padding=nspm_zero_padding,
        threads=threads,
        debug=debug,
        checkopts=checkopts,
        version=version,
    )
    return mri_fwhm_execute(params, runner)

__all__ = [
    "MriFwhm",
    "MriFwhmOutputs",
    "MRI_FWHM_METADATA",
    "mri_fwhm_cargs",
    "mri_fwhm_outputs",
    "mri_fwhm_params",
    "mri_fwhm_execute",
    "mri_fwhm_validate",
    "mri_fwhm",
]