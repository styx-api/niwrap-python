# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XSANATREG_METADATA = Metadata(
    id="xsanatreg",
    name="xsanatreg",
    package="freesurfer",
)

Xsanatreg = typing.TypedDict(
    "Xsanatreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xsanatreg"]],
        "src_cordir": str,
        "targ_cordir": str,
        "transform_file": str,
        "temp_directory": typing.NotRequired[str],
        "source_minc": typing.NotRequired[str],
        "target_minc": typing.NotRequired[str],
        "no_cleanup": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "umask": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class XsanatregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def xsanatreg_params(
    src_cordir: str,
    targ_cordir: str,
    transform_file: str,
    temp_directory: str | None = None,
    source_minc: str | None = None,
    target_minc: str | None = None,
    no_cleanup: bool = False,
    version: bool = False,
    umask: str | None = None,
) -> Xsanatreg:
    """
    Build parameters.

    Args:
        src_cordir: Directory of source COR volume
        targ_cordir: Directory of target COR volume
        transform_file: File in which to store the transformation
        temp_directory: Directory for temporary storage, defaults to /tmp
        source_minc: File name for source minc, set automatically if not specified
        target_minc: File name for target minc, set automatically if not specified
        no_cleanup: Do not delete temporary minc files
        version: Print version and exit
        umask: Set file mode creation mask

    Returns:
        Parameter dictionary.
    """
    params: Xsanatreg = {
        "@type": "freesurfer/xsanatreg",
        "src_cordir": src_cordir,
        "targ_cordir": targ_cordir,
        "transform_file": transform_file,
        "no_cleanup": no_cleanup,
        "version": version,
    }
    if temp_directory is not None:
        params["temp_directory"] = temp_directory
    if source_minc is not None:
        params["source_minc"] = source_minc
    if target_minc is not None:
        params["target_minc"] = target_minc
    if umask is not None:
        params["umask"] = umask
    return params

def xsanatreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Xsanatreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("src_cordir", None) is None:
        raise StyxValidationError("`src_cordir` must not be None")
    if not isinstance(params["src_cordir"], str):
        raise StyxValidationError(f'`src_cordir` has the wrong type: Received `{type(params["src_cordir"])}` expected `str`')
    if params.get("targ_cordir", None) is None:
        raise StyxValidationError("`targ_cordir` must not be None")
    if not isinstance(params["targ_cordir"], str):
        raise StyxValidationError(f'`targ_cordir` has the wrong type: Received `{type(params["targ_cordir"])}` expected `str`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], str):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `str`')
    if params.get("temp_directory", None) is not None:
        if not isinstance(params["temp_directory"], str):
            raise StyxValidationError(f'`temp_directory` has the wrong type: Received `{type(params["temp_directory"])}` expected `str`')
    if params.get("source_minc", None) is not None:
        if not isinstance(params["source_minc"], str):
            raise StyxValidationError(f'`source_minc` has the wrong type: Received `{type(params["source_minc"])}` expected `str`')
    if params.get("target_minc", None) is not None:
        if not isinstance(params["target_minc"], str):
            raise StyxValidationError(f'`target_minc` has the wrong type: Received `{type(params["target_minc"])}` expected `str`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')

def xsanatreg_cargs(params: Xsanatreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xsanatreg")
    cargs.append("-src")
    cargs.append(params["src_cordir"])
    cargs.append("-targ")
    cargs.append(params["targ_cordir"])
    cargs.append("-xfm")
    cargs.append(params["transform_file"])
    v_0 = params.get("temp_directory")
    if v_0 is not None:
        cargs.append("-tmpdir")
        cargs.append(v_0)
    v_1 = params.get("source_minc")
    if v_1 is not None:
        cargs.append("-srcminc")
        cargs.append(v_1)
    v_2 = params.get("target_minc")
    if v_2 is not None:
        cargs.append("-targminc")
        cargs.append(v_2)
    if params.get("no_cleanup"):
        cargs.append("-nocleanup")
    if params.get("version"):
        cargs.append("-version")
    v_3 = params.get("umask")
    if v_3 is not None:
        cargs.append("-umask")
        cargs.append(v_3)
    return cargs

def xsanatreg_outputs(params: Xsanatreg, execution: Execution) -> XsanatregOutputs:
    """Build the XsanatregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return XsanatregOutputs(
        root=root_v,
    )

def xsanatreg_execute(params: Xsanatreg, runner: Runner | None = None) -> XsanatregOutputs:
    """
    xsanatreg

    A tool for registering source and target COR volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xsanatreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XSANATREG_METADATA)
    execution.params(params)
    args = xsanatreg_cargs(params, execution)
    out = xsanatreg_outputs(params, execution)
    execution.run(args)
    return out

def xsanatreg(
    src_cordir: str,
    targ_cordir: str,
    transform_file: str,
    temp_directory: str | None = None,
    source_minc: str | None = None,
    target_minc: str | None = None,
    no_cleanup: bool = False,
    version: bool = False,
    umask: str | None = None,
    runner: Runner | None = None,
) -> XsanatregOutputs:
    """
    xsanatreg

    A tool for registering source and target COR volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        src_cordir: Directory of source COR volume
        targ_cordir: Directory of target COR volume
        transform_file: File in which to store the transformation
        temp_directory: Directory for temporary storage, defaults to /tmp
        source_minc: File name for source minc, set automatically if not specified
        target_minc: File name for target minc, set automatically if not specified
        no_cleanup: Do not delete temporary minc files
        version: Print version and exit
        umask: Set file mode creation mask
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xsanatreg_params(
        src_cordir=src_cordir,
        targ_cordir=targ_cordir,
        transform_file=transform_file,
        temp_directory=temp_directory,
        source_minc=source_minc,
        target_minc=target_minc,
        no_cleanup=no_cleanup,
        version=version,
        umask=umask,
    )
    return xsanatreg_execute(params, runner)

__all__ = [
    "Xsanatreg",
    "XsanatregOutputs",
    "XSANATREG_METADATA",
    "xsanatreg_cargs",
    "xsanatreg_outputs",
    "xsanatreg_params",
    "xsanatreg_execute",
    "xsanatreg_validate",
    "xsanatreg",
]