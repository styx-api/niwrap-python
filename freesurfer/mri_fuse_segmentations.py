# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FUSE_SEGMENTATIONS_METADATA = Metadata(
    id="mri_fuse_segmentations",
    name="mri_fuse_segmentations",
    package="freesurfer",
)

MriFuseSegmentations = typing.TypedDict(
    "MriFuseSegmentations",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_fuse_segmentations"]],
        "asegs": list[InputPathType],
        "nocc_asegs": list[InputPathType],
        "norm_volumes": list[InputPathType],
        "transforms": typing.NotRequired[list[InputPathType]],
        "sigma": typing.NotRequired[float],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MriFuseSegmentationsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Resulting fused segmentation as aseg.fused.mgz file"""

def mri_fuse_segmentations_params(
    asegs: list[InputPathType],
    nocc_asegs: list[InputPathType],
    norm_volumes: list[InputPathType],
    input_file: InputPathType,
    output_file: str,
    transforms: list[InputPathType] | None = None,
    sigma: float | None = None,
) -> MriFuseSegmentations:
    """
    Build parameters.

    Args:
        asegs: Path to aseg.mgz files, one per TP
        nocc_asegs: Path to aseg.auto_noCCseg.mgz files without CC labels, one per\
            TP
        norm_volumes: Path to norm.mgz files, one per TP
        input_file: Input norm.mgz file
        output_file: Resulting fused segmentation as aseg.fused.mgz file
        transforms: Transform files from each TP to the input norm.mgz, can be LTA,\
            M3Z or identity.nofile
        sigma: Cross-time sigma (default 3.0)

    Returns:
        Parameter dictionary.
    """
    params: MriFuseSegmentations = {
        "@type": "freesurfer/mri_fuse_segmentations",
        "asegs": asegs,
        "nocc_asegs": nocc_asegs,
        "norm_volumes": norm_volumes,
        "input_file": input_file,
        "output_file": output_file,
    }
    if transforms is not None:
        params["transforms"] = transforms
    if sigma is not None:
        params["sigma"] = sigma
    return params

def mri_fuse_segmentations_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFuseSegmentations."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("asegs", None) is None:
        raise StyxValidationError("`asegs` must not be None")
    if not isinstance(params["asegs"], list):
        raise StyxValidationError(f'`asegs` has the wrong type: Received `{type(params["asegs"])}` expected `list[InputPathType]`')
    for e in params["asegs"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`asegs` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("nocc_asegs", None) is None:
        raise StyxValidationError("`nocc_asegs` must not be None")
    if not isinstance(params["nocc_asegs"], list):
        raise StyxValidationError(f'`nocc_asegs` has the wrong type: Received `{type(params["nocc_asegs"])}` expected `list[InputPathType]`')
    for e_2 in params["nocc_asegs"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`nocc_asegs` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("norm_volumes", None) is None:
        raise StyxValidationError("`norm_volumes` must not be None")
    if not isinstance(params["norm_volumes"], list):
        raise StyxValidationError(f'`norm_volumes` has the wrong type: Received `{type(params["norm_volumes"])}` expected `list[InputPathType]`')
    for e_3 in params["norm_volumes"]:
        if not isinstance(e_3, (pathlib.Path, str)):
            raise StyxValidationError(f'`norm_volumes` has the wrong type: Received `{type(e_3)}` expected `list[InputPathType]`')
    if params.get("transforms", None) is not None:
        if not isinstance(params["transforms"], list):
            raise StyxValidationError(f'`transforms` has the wrong type: Received `{type(params["transforms"])}` expected `list[InputPathType]`')
        for e_4 in params["transforms"]:
            if not isinstance(e_4, (pathlib.Path, str)):
                raise StyxValidationError(f'`transforms` has the wrong type: Received `{type(e_4)}` expected `list[InputPathType]`')
    if params.get("sigma", None) is not None:
        if not isinstance(params["sigma"], (float, int)):
            raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_fuse_segmentations_cargs(params: MriFuseSegmentations, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_fuse_segmentations")
    cargs.append("-a")
    for item0 in params["asegs"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-c")
    for item1 in params["nocc_asegs"]:
        cargs.append(execution.input_file(item1))
    cargs.append("-n")
    for item2 in params["norm_volumes"]:
        cargs.append(execution.input_file(item2))
    v_0 = params.get("transforms")
    if v_0 is not None:
        cargs.append("-t")
        for item3 in v_0:
            cargs.append(execution.input_file(item3))
    v_1 = params.get("sigma")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(str(v_1))
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def mri_fuse_segmentations_outputs(params: MriFuseSegmentations, execution: Execution) -> MriFuseSegmentationsOutputs:
    """Build the MriFuseSegmentationsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MriFuseSegmentationsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_fuse_segmentations_execute(params: MriFuseSegmentations, runner: Runner | None = None) -> MriFuseSegmentationsOutputs:
    """
    mri_fuse_segmentations

    Fuse a set of segmentations (asegs) into an initial estimate of a longitudinal one.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_fuse_segmentations_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FUSE_SEGMENTATIONS_METADATA)
    execution.params(params)
    args = mri_fuse_segmentations_cargs(params, execution)
    out = mri_fuse_segmentations_outputs(params, execution)
    execution.run(args)
    return out

def mri_fuse_segmentations(
    asegs: list[InputPathType],
    nocc_asegs: list[InputPathType],
    norm_volumes: list[InputPathType],
    input_file: InputPathType,
    output_file: str,
    transforms: list[InputPathType] | None = None,
    sigma: float | None = None,
    runner: Runner | None = None,
) -> MriFuseSegmentationsOutputs:
    """
    mri_fuse_segmentations

    Fuse a set of segmentations (asegs) into an initial estimate of a longitudinal one.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        asegs: Path to aseg.mgz files, one per TP
        nocc_asegs: Path to aseg.auto_noCCseg.mgz files without CC labels, one per\
            TP
        norm_volumes: Path to norm.mgz files, one per TP
        input_file: Input norm.mgz file
        output_file: Resulting fused segmentation as aseg.fused.mgz file
        transforms: Transform files from each TP to the input norm.mgz, can be LTA,\
            M3Z or identity.nofile
        sigma: Cross-time sigma (default 3.0)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_fuse_segmentations_params(
        asegs=asegs,
        nocc_asegs=nocc_asegs,
        norm_volumes=norm_volumes,
        input_file=input_file,
        output_file=output_file,
        transforms=transforms,
        sigma=sigma,
    )
    return mri_fuse_segmentations_execute(params, runner)

__all__ = [
    "MriFuseSegmentations",
    "MriFuseSegmentationsOutputs",
    "MRI_FUSE_SEGMENTATIONS_METADATA",
    "mri_fuse_segmentations_cargs",
    "mri_fuse_segmentations_outputs",
    "mri_fuse_segmentations_params",
    "mri_fuse_segmentations_execute",
    "mri_fuse_segmentations_validate",
    "mri_fuse_segmentations",
]