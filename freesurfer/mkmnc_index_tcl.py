# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKMNC_INDEX_TCL_METADATA = Metadata(
    id="mkmnc_index.tcl",
    name="mkmnc_index.tcl",
    package="freesurfer",
)

MkmncIndexTcl = typing.TypedDict(
    "MkmncIndexTcl",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mkmnc_index.tcl"]],
        "infile": InputPathType,
        "outfile": str,
    },
)

@dataclasses.dataclass
class MkmncIndexTclOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    indexfile: OutputPathType
    """Generated index file"""

def mkmnc_index_tcl_params(
    infile: InputPathType,
    outfile: str,
) -> MkmncIndexTcl:
    """
    Build parameters.

    Args:
        infile: Input MINC file
        outfile: Output index file

    Returns:
        Parameter dictionary.
    """
    params: MkmncIndexTcl = {
        "@type": "freesurfer/mkmnc_index.tcl",
        "infile": infile,
        "outfile": outfile,
    }
    return params

def mkmnc_index_tcl_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MkmncIndexTcl."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def mkmnc_index_tcl_cargs(params: MkmncIndexTcl, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mkmnc_index.tcl")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(params["outfile"])
    return cargs

def mkmnc_index_tcl_outputs(params: MkmncIndexTcl, execution: Execution) -> MkmncIndexTclOutputs:
    """Build the MkmncIndexTclOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    indexfile_v: OutputPathType = execution.output_file(params["outfile"])
    return MkmncIndexTclOutputs(
        root=root_v,
        indexfile=indexfile_v,
    )

def mkmnc_index_tcl_execute(params: MkmncIndexTcl, runner: Runner | None = None) -> MkmncIndexTclOutputs:
    """
    mkmnc_index.tcl

    A tool for creating MINC indices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mkmnc_index_tcl_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKMNC_INDEX_TCL_METADATA)
    execution.params(params)
    args = mkmnc_index_tcl_cargs(params, execution)
    out = mkmnc_index_tcl_outputs(params, execution)
    execution.run(args)
    return out

def mkmnc_index_tcl(
    infile: InputPathType,
    outfile: str,
    runner: Runner | None = None,
) -> MkmncIndexTclOutputs:
    """
    mkmnc_index.tcl

    A tool for creating MINC indices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input MINC file
        outfile: Output index file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mkmnc_index_tcl_params(
        infile=infile,
        outfile=outfile,
    )
    return mkmnc_index_tcl_execute(params, runner)

__all__ = [
    "MkmncIndexTcl",
    "MkmncIndexTclOutputs",
    "MKMNC_INDEX_TCL_METADATA",
    "mkmnc_index_tcl_cargs",
    "mkmnc_index_tcl_outputs",
    "mkmnc_index_tcl_params",
    "mkmnc_index_tcl_execute",
    "mkmnc_index_tcl_validate",
    "mkmnc_index_tcl",
]