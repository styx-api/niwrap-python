# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKSURFATLAS_METADATA = Metadata(
    id="mksurfatlas",
    name="mksurfatlas",
    package="freesurfer",
)

Mksurfatlas = typing.TypedDict(
    "Mksurfatlas",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mksurfatlas"]],
        "atlas": str,
        "hemi": str,
        "subjects": list[str],
        "surfval": str,
        "surfvaldir": typing.NotRequired[str],
        "regsurf": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MksurfatlasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_atlas: OutputPathType
    """Resulting atlas file in TIFF format."""

def mksurfatlas_params(
    atlas: str,
    hemi: str,
    subjects: list[str],
    surfval: str,
    surfvaldir: str | None = None,
    regsurf: str | None = None,
    debug: bool = False,
    version: bool = False,
    help: bool = False,
) -> Mksurfatlas:
    """
    Build parameters.

    Args:
        atlas: Save results to this file (tif file).
        hemi: Hemisphere to process.
        subjects: Subject(s) to process. Multiple subjects can be specified by\
            repeating the flag.
        surfval: Surface values file. Looks for subject/surfvaldir/hemi.surfval.
        surfvaldir: Directory for surface values; default is 'label'.
        regsurf: Registration surface; default is 'sphere'.
        debug: Turn on debugging.
        version: Print version and exit.
        help: Print help and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mksurfatlas = {
        "@type": "freesurfer/mksurfatlas",
        "atlas": atlas,
        "hemi": hemi,
        "subjects": subjects,
        "surfval": surfval,
        "debug": debug,
        "version": version,
        "help": help,
    }
    if surfvaldir is not None:
        params["surfvaldir"] = surfvaldir
    if regsurf is not None:
        params["regsurf"] = regsurf
    return params

def mksurfatlas_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mksurfatlas."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("atlas", None) is None:
        raise StyxValidationError("`atlas` must not be None")
    if not isinstance(params["atlas"], str):
        raise StyxValidationError(f'`atlas` has the wrong type: Received `{type(params["atlas"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("surfval", None) is None:
        raise StyxValidationError("`surfval` must not be None")
    if not isinstance(params["surfval"], str):
        raise StyxValidationError(f'`surfval` has the wrong type: Received `{type(params["surfval"])}` expected `str`')
    if params.get("surfvaldir", None) is not None:
        if not isinstance(params["surfvaldir"], str):
            raise StyxValidationError(f'`surfvaldir` has the wrong type: Received `{type(params["surfvaldir"])}` expected `str`')
    if params.get("regsurf", None) is not None:
        if not isinstance(params["regsurf"], str):
            raise StyxValidationError(f'`regsurf` has the wrong type: Received `{type(params["regsurf"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mksurfatlas_cargs(params: Mksurfatlas, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mksurfatlas")
    cargs.append("--a")
    cargs.append(params["atlas"])
    cargs.append("--h")
    cargs.append(params["hemi"])
    cargs.append("--s")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append("--v")
    cargs.append(params["surfval"])
    v_0 = params.get("surfvaldir")
    if v_0 is not None:
        cargs.append("--d")
        cargs.append(v_0)
    v_1 = params.get("regsurf")
    if v_1 is not None:
        cargs.append("--r")
        cargs.append(v_1)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mksurfatlas_outputs(params: Mksurfatlas, execution: Execution) -> MksurfatlasOutputs:
    """Build the MksurfatlasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_atlas_v: OutputPathType = execution.output_file(params["atlas"])
    return MksurfatlasOutputs(
        root=root_v,
        output_atlas=output_atlas_v,
    )

def mksurfatlas_execute(params: Mksurfatlas, runner: Runner | None = None) -> MksurfatlasOutputs:
    """
    mksurfatlas

    Creates an atlas using mris_make_template. The atlas can then be used to create the surface registration for each subject based on this atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mksurfatlas_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKSURFATLAS_METADATA)
    execution.params(params)
    args = mksurfatlas_cargs(params, execution)
    out = mksurfatlas_outputs(params, execution)
    execution.run(args)
    return out

def mksurfatlas(
    atlas: str,
    hemi: str,
    subjects: list[str],
    surfval: str,
    surfvaldir: str | None = None,
    regsurf: str | None = None,
    debug: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MksurfatlasOutputs:
    """
    mksurfatlas

    Creates an atlas using mris_make_template. The atlas can then be used to create the surface registration for each subject based on this atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        atlas: Save results to this file (tif file).
        hemi: Hemisphere to process.
        subjects: Subject(s) to process. Multiple subjects can be specified by\
            repeating the flag.
        surfval: Surface values file. Looks for subject/surfvaldir/hemi.surfval.
        surfvaldir: Directory for surface values; default is 'label'.
        regsurf: Registration surface; default is 'sphere'.
        debug: Turn on debugging.
        version: Print version and exit.
        help: Print help and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mksurfatlas_params(
        atlas=atlas,
        hemi=hemi,
        subjects=subjects,
        surfval=surfval,
        surfvaldir=surfvaldir,
        regsurf=regsurf,
        debug=debug,
        version=version,
        help=help,
    )
    return mksurfatlas_execute(params, runner)

__all__ = [
    "Mksurfatlas",
    "MksurfatlasOutputs",
    "MKSURFATLAS_METADATA",
    "mksurfatlas_cargs",
    "mksurfatlas_outputs",
    "mksurfatlas_params",
    "mksurfatlas_execute",
    "mksurfatlas_validate",
    "mksurfatlas",
]