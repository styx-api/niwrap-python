# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MC_METADATA = Metadata(
    id="mri_mc",
    name="mri_mc",
    package="freesurfer",
)

class MriMcOutputSurface(typing.TypedDict):
    output_surface: str
    """The file where the extracted surface mesh will be saved."""
    connectivity: typing.NotRequired[float]
    """The connectivity used for Marching Cubes. Options are: 1=6+, 2=18, 3=6, 4=26."""

MriMc = typing.TypedDict(
    "MriMc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mc"]],
        "input_volume": InputPathType,
        "label_value": float,
        "output_surface": MriMcOutputSurface,
    },
)

@dataclasses.dataclass
class MriMcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    extracted_surface: OutputPathType
    """The extracted surface output file."""

def mri_mc_params(
    input_volume: InputPathType,
    label_value: float,
    output_surface: MriMcOutputSurface,
) -> MriMc:
    """
    Build parameters.

    Args:
        input_volume: The input volume from which to extract the surface.
        label_value: The label value of the structure to extract.
        output_surface:

    Returns:
        Parameter dictionary.
    """
    params: MriMc = {
        "@type": "freesurfer/mri_mc",
        "input_volume": input_volume,
        "label_value": label_value,
        "output_surface": output_surface,
    }
    return params

def mri_mc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("label_value", None) is None:
        raise StyxValidationError("`label_value` must not be None")
    if not isinstance(params["label_value"], (float, int)):
        raise StyxValidationError(f'`label_value` has the wrong type: Received `{type(params["label_value"])}` expected `float`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["output_surface"])}\'')
    if params["output_surface"].get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"]["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"]["output_surface"])}` expected `str`')
    if params["output_surface"].get("connectivity", None) is not None:
        if not isinstance(params["output_surface"]["connectivity"], (float, int)):
            raise StyxValidationError(f'`connectivity` has the wrong type: Received `{type(params["output_surface"]["connectivity"])}` expected `float`')

def mri_mc_cargs(params: MriMc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mc")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["label_value"]))
    cargs.append("".join([params["output_surface"]["output_surface"], (str(v_0) if (v_0 := params["output_surface"].get("connectivity")) is not None else "")]))
    return cargs

def mri_mc_outputs(params: MriMc, execution: Execution) -> MriMcOutputs:
    """Build the MriMcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    extracted_surface_v: OutputPathType = execution.output_file(params["output_surface"]["output_surface"])
    return MriMcOutputs(
        root=root_v,
        extracted_surface=extracted_surface_v,
    )

def mri_mc_execute(params: MriMc, runner: Runner | None = None) -> MriMcOutputs:
    """
    mri_mc

    Extract a surface from a label volume using Marching Cubes algorithm.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MC_METADATA)
    execution.params(params)
    args = mri_mc_cargs(params, execution)
    out = mri_mc_outputs(params, execution)
    execution.run(args)
    return out

def mri_mc(
    input_volume: InputPathType,
    label_value: float,
    output_surface: MriMcOutputSurface,
    runner: Runner | None = None,
) -> MriMcOutputs:
    """
    mri_mc

    Extract a surface from a label volume using Marching Cubes algorithm.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: The input volume from which to extract the surface.
        label_value: The label value of the structure to extract.
        output_surface:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mc_params(
        input_volume=input_volume,
        label_value=label_value,
        output_surface=output_surface,
    )
    return mri_mc_execute(params, runner)

__all__ = [
    "MriMc",
    "MriMcOutputs",
    "MRI_MC_METADATA",
    "mri_mc_cargs",
    "mri_mc_outputs",
    "mri_mc_params",
    "mri_mc_execute",
    "mri_mc_validate",
    "mri_mc",
]