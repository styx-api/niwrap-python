# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MOTION_CORRECT_METADATA = Metadata(
    id="mri_motion_correct",
    name="mri_motion_correct",
    package="freesurfer",
)

MriMotionCorrect = typing.TypedDict(
    "MriMotionCorrect",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_motion_correct"]],
        "outfile": str,
        "infiles": list[InputPathType],
    },
)

@dataclasses.dataclass
class MriMotionCorrectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_outfile: OutputPathType
    """Corrected MRI image output file."""

def mri_motion_correct_params(
    outfile: str,
    infiles: list[InputPathType],
) -> MriMotionCorrect:
    """
    Build parameters.

    Args:
        outfile: Output file where the corrected MRI image(s) will be stored.
        infiles: Input MRI image files to be corrected.

    Returns:
        Parameter dictionary.
    """
    params: MriMotionCorrect = {
        "@type": "freesurfer/mri_motion_correct",
        "outfile": outfile,
        "infiles": infiles,
    }
    return params

def mri_motion_correct_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMotionCorrect."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("infiles", None) is None:
        raise StyxValidationError("`infiles` must not be None")
    if not isinstance(params["infiles"], list):
        raise StyxValidationError(f'`infiles` has the wrong type: Received `{type(params["infiles"])}` expected `list[InputPathType]`')
    for e in params["infiles"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`infiles` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def mri_motion_correct_cargs(params: MriMotionCorrect, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_motion_correct")
    cargs.append(params["outfile"])
    for item0 in params["infiles"]:
        cargs.append(execution.input_file(item0))
    return cargs

def mri_motion_correct_outputs(params: MriMotionCorrect, execution: Execution) -> MriMotionCorrectOutputs:
    """Build the MriMotionCorrectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_outfile_v: OutputPathType = execution.output_file(params["outfile"])
    return MriMotionCorrectOutputs(
        root=root_v,
        corrected_outfile=corrected_outfile_v,
    )

def mri_motion_correct_execute(params: MriMotionCorrect, runner: Runner | None = None) -> MriMotionCorrectOutputs:
    """
    mri_motion_correct

    Tool for correcting motion in MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_motion_correct_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MOTION_CORRECT_METADATA)
    execution.params(params)
    args = mri_motion_correct_cargs(params, execution)
    out = mri_motion_correct_outputs(params, execution)
    execution.run(args)
    return out

def mri_motion_correct(
    outfile: str,
    infiles: list[InputPathType],
    runner: Runner | None = None,
) -> MriMotionCorrectOutputs:
    """
    mri_motion_correct

    Tool for correcting motion in MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        outfile: Output file where the corrected MRI image(s) will be stored.
        infiles: Input MRI image files to be corrected.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_motion_correct_params(
        outfile=outfile,
        infiles=infiles,
    )
    return mri_motion_correct_execute(params, runner)

__all__ = [
    "MriMotionCorrect",
    "MriMotionCorrectOutputs",
    "MRI_MOTION_CORRECT_METADATA",
    "mri_motion_correct_cargs",
    "mri_motion_correct_outputs",
    "mri_motion_correct_params",
    "mri_motion_correct_execute",
    "mri_motion_correct_validate",
    "mri_motion_correct",
]