# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REINFLATE_SUBJECT_RH_METADATA = Metadata(
    id="reinflate_subject-rh",
    name="reinflate_subject-rh",
    package="freesurfer",
)

ReinflateSubjectRh = typing.TypedDict(
    "ReinflateSubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reinflate_subject-rh"]],
        "subject_dir": str,
        "additional_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class ReinflateSubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def reinflate_subject_rh_params(
    subject_dir: str,
    additional_options: str | None = None,
) -> ReinflateSubjectRh:
    """
    Build parameters.

    Args:
        subject_dir: Directory of the subject within FreeSurfer environment
        additional_options: Additional options for the reinflate_subject-rh command

    Returns:
        Parameter dictionary.
    """
    params: ReinflateSubjectRh = {
        "@type": "freesurfer/reinflate_subject-rh",
        "subject_dir": subject_dir,
    }
    if additional_options is not None:
        params["additional_options"] = additional_options
    return params

def reinflate_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ReinflateSubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("additional_options", None) is not None:
        if not isinstance(params["additional_options"], str):
            raise StyxValidationError(f'`additional_options` has the wrong type: Received `{type(params["additional_options"])}` expected `str`')

def reinflate_subject_rh_cargs(params: ReinflateSubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reinflate_subject-rh")
    cargs.append("-rh")
    cargs.append(params["subject_dir"])
    v_0 = params.get("additional_options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def reinflate_subject_rh_outputs(params: ReinflateSubjectRh, execution: Execution) -> ReinflateSubjectRhOutputs:
    """Build the ReinflateSubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ReinflateSubjectRhOutputs(
        root=root_v,
    )

def reinflate_subject_rh_execute(params: ReinflateSubjectRh, runner: Runner | None = None) -> ReinflateSubjectRhOutputs:
    """
    reinflate_subject-rh

    A tool for reinflating the cortical surfaces for a given subject in FreeSurfer, specifically for the right hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reinflate_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REINFLATE_SUBJECT_RH_METADATA)
    execution.params(params)
    args = reinflate_subject_rh_cargs(params, execution)
    out = reinflate_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def reinflate_subject_rh(
    subject_dir: str,
    additional_options: str | None = None,
    runner: Runner | None = None,
) -> ReinflateSubjectRhOutputs:
    """
    reinflate_subject-rh

    A tool for reinflating the cortical surfaces for a given subject in FreeSurfer, specifically for the right hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Directory of the subject within FreeSurfer environment
        additional_options: Additional options for the reinflate_subject-rh command
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reinflate_subject_rh_params(
        subject_dir=subject_dir,
        additional_options=additional_options,
    )
    return reinflate_subject_rh_execute(params, runner)

__all__ = [
    "ReinflateSubjectRh",
    "ReinflateSubjectRhOutputs",
    "REINFLATE_SUBJECT_RH_METADATA",
    "reinflate_subject_rh_cargs",
    "reinflate_subject_rh_outputs",
    "reinflate_subject_rh_params",
    "reinflate_subject_rh_execute",
    "reinflate_subject_rh_validate",
    "reinflate_subject_rh",
]