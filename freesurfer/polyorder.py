# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POLYORDER_METADATA = Metadata(
    id="polyorder",
    name="polyorder",
    package="freesurfer",
)

Polyorder = typing.TypedDict(
    "Polyorder",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/polyorder"]],
        "ntp": float,
        "tr": float,
        "cutoff": float,
    },
)

@dataclasses.dataclass
class PolyorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def polyorder_params(
    ntp: float,
    tr: float,
    cutoff: float,
) -> Polyorder:
    """
    Build parameters.

    Args:
        ntp:
        tr:
        cutoff:

    Returns:
        Parameter dictionary.
    """
    params: Polyorder = {
        "@type": "freesurfer/polyorder",
        "ntp": ntp,
        "tr": tr,
        "cutoff": cutoff,
    }
    return params

def polyorder_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Polyorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ntp", None) is None:
        raise StyxValidationError("`ntp` must not be None")
    if not isinstance(params["ntp"], (float, int)):
        raise StyxValidationError(f'`ntp` has the wrong type: Received `{type(params["ntp"])}` expected `float`')
    if params.get("tr", None) is None:
        raise StyxValidationError("`tr` must not be None")
    if not isinstance(params["tr"], (float, int)):
        raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("cutoff", None) is None:
        raise StyxValidationError("`cutoff` must not be None")
    if not isinstance(params["cutoff"], (float, int)):
        raise StyxValidationError(f'`cutoff` has the wrong type: Received `{type(params["cutoff"])}` expected `float`')

def polyorder_cargs(params: Polyorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("polyorder")
    cargs.append("--ntp")
    cargs.append(str(params["ntp"]))
    cargs.append("--TR")
    cargs.append(str(params["tr"]))
    cargs.append("--cutoff")
    cargs.append(str(params["cutoff"]))
    return cargs

def polyorder_outputs(params: Polyorder, execution: Execution) -> PolyorderOutputs:
    """Build the PolyorderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PolyorderOutputs(
        root=root_v,
    )

def polyorder_execute(params: Polyorder, runner: Runner | None = None) -> PolyorderOutputs:
    """
    polyorder

    Computes the order of polynomial regressors needed to achieve a highpass filter with the given cutoff frequency.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    polyorder_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POLYORDER_METADATA)
    execution.params(params)
    args = polyorder_cargs(params, execution)
    out = polyorder_outputs(params, execution)
    execution.run(args)
    return out

def polyorder(
    ntp: float,
    tr: float,
    cutoff: float,
    runner: Runner | None = None,
) -> PolyorderOutputs:
    """
    polyorder

    Computes the order of polynomial regressors needed to achieve a highpass filter with the given cutoff frequency.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        ntp:
        tr:
        cutoff:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = polyorder_params(
        ntp=ntp,
        tr=tr,
        cutoff=cutoff,
    )
    return polyorder_execute(params, runner)

__all__ = [
    "Polyorder",
    "PolyorderOutputs",
    "POLYORDER_METADATA",
    "polyorder_cargs",
    "polyorder_outputs",
    "polyorder_params",
    "polyorder_execute",
    "polyorder_validate",
    "polyorder",
]