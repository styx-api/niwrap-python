# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REGISTER_SUBJECT_FLASH_METADATA = Metadata(
    id="register_subject_flash",
    name="register_subject_flash",
    package="freesurfer",
)

RegisterSubjectFlash = typing.TypedDict(
    "RegisterSubjectFlash",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/register_subject_flash"]],
        "input_volumes": list[InputPathType],
    },
)

@dataclasses.dataclass
class RegisterSubjectFlashOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    log_file: OutputPathType
    """Log file output with registration results."""

def register_subject_flash_params(
    input_volumes: list[InputPathType],
) -> RegisterSubjectFlash:
    """
    Build parameters.

    Args:
        input_volumes: Input volumes to register.

    Returns:
        Parameter dictionary.
    """
    params: RegisterSubjectFlash = {
        "@type": "freesurfer/register_subject_flash",
        "input_volumes": input_volumes,
    }
    return params

def register_subject_flash_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegisterSubjectFlash."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def register_subject_flash_cargs(params: RegisterSubjectFlash, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("register_subject_flash")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    return cargs

def register_subject_flash_outputs(params: RegisterSubjectFlash, execution: Execution) -> RegisterSubjectFlashOutputs:
    """Build the RegisterSubjectFlashOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    log_file_v: OutputPathType = execution.output_file("talairach.log")
    return RegisterSubjectFlashOutputs(
        root=root_v,
        log_file=log_file_v,
    )

def register_subject_flash_execute(params: RegisterSubjectFlash, runner: Runner | None = None) -> RegisterSubjectFlashOutputs:
    """
    register_subject_flash

    Register subject using the FLASH forward model to predict intensity values.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    register_subject_flash_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REGISTER_SUBJECT_FLASH_METADATA)
    execution.params(params)
    args = register_subject_flash_cargs(params, execution)
    out = register_subject_flash_outputs(params, execution)
    execution.run(args)
    return out

def register_subject_flash(
    input_volumes: list[InputPathType],
    runner: Runner | None = None,
) -> RegisterSubjectFlashOutputs:
    """
    register_subject_flash

    Register subject using the FLASH forward model to predict intensity values.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Input volumes to register.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = register_subject_flash_params(
        input_volumes=input_volumes,
    )
    return register_subject_flash_execute(params, runner)

__all__ = [
    "RegisterSubjectFlash",
    "RegisterSubjectFlashOutputs",
    "REGISTER_SUBJECT_FLASH_METADATA",
    "register_subject_flash_cargs",
    "register_subject_flash_outputs",
    "register_subject_flash_params",
    "register_subject_flash_execute",
    "register_subject_flash_validate",
    "register_subject_flash",
]