# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MERGESEG_METADATA = Metadata(
    id="mergeseg",
    name="mergeseg",
    package="freesurfer",
)

Mergeseg = typing.TypedDict(
    "Mergeseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mergeseg"]],
        "src_seg": InputPathType,
        "merge_seg": InputPathType,
        "out_seg": str,
        "segid": typing.NotRequired[float],
        "segid_only": typing.NotRequired[float],
        "segid_erode": typing.NotRequired[float],
        "ctab": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MergesegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_seg: OutputPathType
    """Output merged segmentation result."""

def mergeseg_params(
    src_seg: InputPathType,
    merge_seg: InputPathType,
    out_seg: str,
    segid: float | None = None,
    segid_only: float | None = None,
    segid_erode: float | None = None,
    ctab: InputPathType | None = None,
) -> Mergeseg:
    """
    Build parameters.

    Args:
        src_seg: Source segmentation image file
        merge_seg: Merge segmentation volume file
        out_seg: Output merged segmentation
        segid: Segmentation index (optional). If specified, all the voxels in the\
            merge seg will be set to segindex.
        segid_only: Only take segindex from merge and use it for merging.
        segid_erode: Erode seg-only segindex before merge. Specify the number of\
            erosion iterations.
        ctab: Color table to embed in the output segmentation.

    Returns:
        Parameter dictionary.
    """
    params: Mergeseg = {
        "@type": "freesurfer/mergeseg",
        "src_seg": src_seg,
        "merge_seg": merge_seg,
        "out_seg": out_seg,
    }
    if segid is not None:
        params["segid"] = segid
    if segid_only is not None:
        params["segid_only"] = segid_only
    if segid_erode is not None:
        params["segid_erode"] = segid_erode
    if ctab is not None:
        params["ctab"] = ctab
    return params

def mergeseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mergeseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("src_seg", None) is None:
        raise StyxValidationError("`src_seg` must not be None")
    if not isinstance(params["src_seg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`src_seg` has the wrong type: Received `{type(params["src_seg"])}` expected `InputPathType`')
    if params.get("merge_seg", None) is None:
        raise StyxValidationError("`merge_seg` must not be None")
    if not isinstance(params["merge_seg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`merge_seg` has the wrong type: Received `{type(params["merge_seg"])}` expected `InputPathType`')
    if params.get("out_seg", None) is None:
        raise StyxValidationError("`out_seg` must not be None")
    if not isinstance(params["out_seg"], str):
        raise StyxValidationError(f'`out_seg` has the wrong type: Received `{type(params["out_seg"])}` expected `str`')
    if params.get("segid", None) is not None:
        if not isinstance(params["segid"], (float, int)):
            raise StyxValidationError(f'`segid` has the wrong type: Received `{type(params["segid"])}` expected `float`')
    if params.get("segid_only", None) is not None:
        if not isinstance(params["segid_only"], (float, int)):
            raise StyxValidationError(f'`segid_only` has the wrong type: Received `{type(params["segid_only"])}` expected `float`')
    if params.get("segid_erode", None) is not None:
        if not isinstance(params["segid_erode"], (float, int)):
            raise StyxValidationError(f'`segid_erode` has the wrong type: Received `{type(params["segid_erode"])}` expected `float`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `InputPathType`')

def mergeseg_cargs(params: Mergeseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mergeseg")
    cargs.append(execution.input_file(params["src_seg"]))
    cargs.append(execution.input_file(params["merge_seg"]))
    cargs.append("--o")
    cargs.append(params["out_seg"])
    v_0 = params.get("segid")
    if v_0 is not None:
        cargs.append("--segid")
        cargs.append(str(v_0))
    v_1 = params.get("segid_only")
    if v_1 is not None:
        cargs.append("--segid-only")
        cargs.append(str(v_1))
    v_2 = params.get("segid_erode")
    if v_2 is not None:
        cargs.append("--segid-erode")
        cargs.append(str(v_2))
    v_3 = params.get("ctab")
    if v_3 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_3))
    return cargs

def mergeseg_outputs(params: Mergeseg, execution: Execution) -> MergesegOutputs:
    """Build the MergesegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_seg_v: OutputPathType = execution.output_file(params["out_seg"])
    return MergesegOutputs(
        root=root_v,
        output_seg=output_seg_v,
    )

def mergeseg_execute(params: Mergeseg, runner: Runner | None = None) -> MergesegOutputs:
    """
    mergeseg

    Merges one segmentation into another, replacing the source voxels with those from the merge segmentation where non-zero.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mergeseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MERGESEG_METADATA)
    execution.params(params)
    args = mergeseg_cargs(params, execution)
    out = mergeseg_outputs(params, execution)
    execution.run(args)
    return out

def mergeseg(
    src_seg: InputPathType,
    merge_seg: InputPathType,
    out_seg: str,
    segid: float | None = None,
    segid_only: float | None = None,
    segid_erode: float | None = None,
    ctab: InputPathType | None = None,
    runner: Runner | None = None,
) -> MergesegOutputs:
    """
    mergeseg

    Merges one segmentation into another, replacing the source voxels with those from the merge segmentation where non-zero.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        src_seg: Source segmentation image file
        merge_seg: Merge segmentation volume file
        out_seg: Output merged segmentation
        segid: Segmentation index (optional). If specified, all the voxels in the\
            merge seg will be set to segindex.
        segid_only: Only take segindex from merge and use it for merging.
        segid_erode: Erode seg-only segindex before merge. Specify the number of\
            erosion iterations.
        ctab: Color table to embed in the output segmentation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mergeseg_params(
        src_seg=src_seg,
        merge_seg=merge_seg,
        out_seg=out_seg,
        segid=segid,
        segid_only=segid_only,
        segid_erode=segid_erode,
        ctab=ctab,
    )
    return mergeseg_execute(params, runner)

__all__ = [
    "Mergeseg",
    "MergesegOutputs",
    "MERGESEG_METADATA",
    "mergeseg_cargs",
    "mergeseg_outputs",
    "mergeseg_params",
    "mergeseg_execute",
    "mergeseg_validate",
    "mergeseg",
]