# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_BS_SH_METADATA = Metadata(
    id="segmentBS.sh",
    name="segmentBS.sh",
    package="freesurfer",
)

SegmentBsSh = typing.TypedDict(
    "SegmentBsSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segmentBS.sh"]],
        "matlab_runtime": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SegmentBsShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def segment_bs_sh_params(
    matlab_runtime: str | None = None,
) -> SegmentBsSh:
    """
    Build parameters.

    Args:
        matlab_runtime: Path to the Matlab 2019b runtime environment; necessary for\
            running the segmentation tool.

    Returns:
        Parameter dictionary.
    """
    params: SegmentBsSh = {
        "@type": "freesurfer/segmentBS.sh",
    }
    if matlab_runtime is not None:
        params["matlab_runtime"] = matlab_runtime
    return params

def segment_bs_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentBsSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("matlab_runtime", None) is not None:
        if not isinstance(params["matlab_runtime"], str):
            raise StyxValidationError(f'`matlab_runtime` has the wrong type: Received `{type(params["matlab_runtime"])}` expected `str`')

def segment_bs_sh_cargs(params: SegmentBsSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segmentBS.sh")
    v_0 = params.get("matlab_runtime")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def segment_bs_sh_outputs(params: SegmentBsSh, execution: Execution) -> SegmentBsShOutputs:
    """Build the SegmentBsShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SegmentBsShOutputs(
        root=root_v,
    )

def segment_bs_sh_execute(params: SegmentBsSh, runner: Runner | None = None) -> SegmentBsShOutputs:
    """
    segmentBS.sh

    Segmentation tool for hippocampal/amygdala and brainstem modules

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_bs_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_BS_SH_METADATA)
    execution.params(params)
    args = segment_bs_sh_cargs(params, execution)
    out = segment_bs_sh_outputs(params, execution)
    execution.run(args)
    return out

def segment_bs_sh(
    matlab_runtime: str | None = None,
    runner: Runner | None = None,
) -> SegmentBsShOutputs:
    """
    segmentBS.sh

    Segmentation tool for hippocampal/amygdala and brainstem modules

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        matlab_runtime: Path to the Matlab 2019b runtime environment; necessary for\
            running the segmentation tool.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_bs_sh_params(
        matlab_runtime=matlab_runtime,
    )
    return segment_bs_sh_execute(params, runner)

__all__ = [
    "SegmentBsSh",
    "SegmentBsShOutputs",
    "SEGMENT_BS_SH_METADATA",
    "segment_bs_sh_cargs",
    "segment_bs_sh_outputs",
    "segment_bs_sh_params",
    "segment_bs_sh_execute",
    "segment_bs_sh_validate",
    "segment_bs_sh",
]