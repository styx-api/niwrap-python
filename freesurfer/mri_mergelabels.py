# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MERGELABELS_METADATA = Metadata(
    id="mri_mergelabels",
    name="mri_mergelabels",
    package="freesurfer",
)

MriMergelabels = typing.TypedDict(
    "MriMergelabels",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mergelabels"]],
        "input_labels": list[InputPathType],
        "output_label": str,
        "input_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriMergelabelsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    merged_label_file: OutputPathType
    """Merged label output file"""

def mri_mergelabels_params(
    input_labels: list[InputPathType],
    output_label: str,
    input_directory: str | None = None,
) -> MriMergelabels:
    """
    Build parameters.

    Args:
        input_labels: Input label files to be merged.
        output_label: Output file for the merged label.
        input_directory: Directory containing label files to be merged.

    Returns:
        Parameter dictionary.
    """
    params: MriMergelabels = {
        "@type": "freesurfer/mri_mergelabels",
        "input_labels": input_labels,
        "output_label": output_label,
    }
    if input_directory is not None:
        params["input_directory"] = input_directory
    return params

def mri_mergelabels_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMergelabels."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_labels", None) is None:
        raise StyxValidationError("`input_labels` must not be None")
    if not isinstance(params["input_labels"], list):
        raise StyxValidationError(f'`input_labels` has the wrong type: Received `{type(params["input_labels"])}` expected `list[InputPathType]`')
    for e in params["input_labels"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_labels` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_label", None) is None:
        raise StyxValidationError("`output_label` must not be None")
    if not isinstance(params["output_label"], str):
        raise StyxValidationError(f'`output_label` has the wrong type: Received `{type(params["output_label"])}` expected `str`')
    if params.get("input_directory", None) is not None:
        if not isinstance(params["input_directory"], str):
            raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')

def mri_mergelabels_cargs(params: MriMergelabels, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mergelabels")
    cargs.append("-i")
    for item0 in params["input_labels"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-o")
    cargs.append(params["output_label"])
    v_0 = params.get("input_directory")
    if v_0 is not None:
        cargs.append("-d")
        cargs.append(v_0)
    return cargs

def mri_mergelabels_outputs(params: MriMergelabels, execution: Execution) -> MriMergelabelsOutputs:
    """Build the MriMergelabelsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    merged_label_file_v: OutputPathType = execution.output_file(params["output_label"])
    return MriMergelabelsOutputs(
        root=root_v,
        merged_label_file=merged_label_file_v,
    )

def mri_mergelabels_execute(params: MriMergelabels, runner: Runner | None = None) -> MriMergelabelsOutputs:
    """
    mri_mergelabels

    A tool to merge two or more label files by concatenating them together.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mergelabels_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MERGELABELS_METADATA)
    execution.params(params)
    args = mri_mergelabels_cargs(params, execution)
    out = mri_mergelabels_outputs(params, execution)
    execution.run(args)
    return out

def mri_mergelabels(
    input_labels: list[InputPathType],
    output_label: str,
    input_directory: str | None = None,
    runner: Runner | None = None,
) -> MriMergelabelsOutputs:
    """
    mri_mergelabels

    A tool to merge two or more label files by concatenating them together.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_labels: Input label files to be merged.
        output_label: Output file for the merged label.
        input_directory: Directory containing label files to be merged.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mergelabels_params(
        input_labels=input_labels,
        output_label=output_label,
        input_directory=input_directory,
    )
    return mri_mergelabels_execute(params, runner)

__all__ = [
    "MriMergelabels",
    "MriMergelabelsOutputs",
    "MRI_MERGELABELS_METADATA",
    "mri_mergelabels_cargs",
    "mri_mergelabels_outputs",
    "mri_mergelabels_params",
    "mri_mergelabels_execute",
    "mri_mergelabels_validate",
    "mri_mergelabels",
]