# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MORPH_STATS_METADATA = Metadata(
    id="mris_morph_stats",
    name="mris_morph_stats",
    package="freesurfer",
)

MrisMorphStatsHemisphere = typing.Literal["lh"] | typing.Literal["rh"]

MrisMorphStats = typing.TypedDict(
    "MrisMorphStats",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_morph_stats"]],
        "subject_name": str,
        "hemisphere": MrisMorphStatsHemisphere,
        "morphed_surface": InputPathType,
        "output_name": str,
    },
)

@dataclasses.dataclass
class MrisMorphStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stats_output: OutputPathType
    """Generated statistics of the surface-based deformation field."""

def mris_morph_stats_params(
    subject_name: str,
    hemisphere: MrisMorphStatsHemisphere,
    morphed_surface: InputPathType,
    output_name: str,
) -> MrisMorphStats:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject.
        hemisphere: Hemisphere, either 'lh' for left hemisphere or 'rh' for right\
            hemisphere.
        morphed_surface: Morphed surface file.
        output_name: Name of the output to be generated.

    Returns:
        Parameter dictionary.
    """
    params: MrisMorphStats = {
        "@type": "freesurfer/mris_morph_stats",
        "subject_name": subject_name,
        "hemisphere": hemisphere,
        "morphed_surface": morphed_surface,
        "output_name": output_name,
    }
    return params

def mris_morph_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMorphStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `MrisMorphStatsHemisphere`')
    if params["hemisphere"] not in ["lh", "rh"]:
        raise StyxValidationError("Parameter `hemisphere` must be one of [\"lh\", \"rh\"]")
    if params.get("morphed_surface", None) is None:
        raise StyxValidationError("`morphed_surface` must not be None")
    if not isinstance(params["morphed_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`morphed_surface` has the wrong type: Received `{type(params["morphed_surface"])}` expected `InputPathType`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')

def mris_morph_stats_cargs(params: MrisMorphStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_morph_stats")
    cargs.append(params["subject_name"])
    cargs.append(str(params["hemisphere"]))
    cargs.append(execution.input_file(params["morphed_surface"]))
    cargs.append(params["output_name"])
    return cargs

def mris_morph_stats_outputs(params: MrisMorphStats, execution: Execution) -> MrisMorphStatsOutputs:
    """Build the MrisMorphStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stats_output_v: OutputPathType = execution.output_file(params["output_name"])
    return MrisMorphStatsOutputs(
        root=root_v,
        stats_output=stats_output_v,
    )

def mris_morph_stats_execute(params: MrisMorphStats, runner: Runner | None = None) -> MrisMorphStatsOutputs:
    """
    mris_morph_stats

    This program generates statistics which characterize a surface-based deformation field.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_morph_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MORPH_STATS_METADATA)
    execution.params(params)
    args = mris_morph_stats_cargs(params, execution)
    out = mris_morph_stats_outputs(params, execution)
    execution.run(args)
    return out

def mris_morph_stats(
    subject_name: str,
    hemisphere: MrisMorphStatsHemisphere,
    morphed_surface: InputPathType,
    output_name: str,
    runner: Runner | None = None,
) -> MrisMorphStatsOutputs:
    """
    mris_morph_stats

    This program generates statistics which characterize a surface-based deformation field.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject.
        hemisphere: Hemisphere, either 'lh' for left hemisphere or 'rh' for right\
            hemisphere.
        morphed_surface: Morphed surface file.
        output_name: Name of the output to be generated.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_morph_stats_params(
        subject_name=subject_name,
        hemisphere=hemisphere,
        morphed_surface=morphed_surface,
        output_name=output_name,
    )
    return mris_morph_stats_execute(params, runner)

__all__ = [
    "MrisMorphStats",
    "MrisMorphStatsOutputs",
    "MRIS_MORPH_STATS_METADATA",
    "mris_morph_stats_cargs",
    "mris_morph_stats_outputs",
    "mris_morph_stats_params",
    "mris_morph_stats_execute",
    "mris_morph_stats_validate",
    "mris_morph_stats",
]