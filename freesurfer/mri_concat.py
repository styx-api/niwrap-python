# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CONCAT_METADATA = Metadata(
    id="mri_concat",
    name="mri_concat",
    package="freesurfer",
)

MriConcat = typing.TypedDict(
    "MriConcat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_concat"]],
        "input_files": list[InputPathType],
        "output_file": str,
        "file_list": typing.NotRequired[str],
        "paired_sum": typing.NotRequired[bool],
        "paired_avg": typing.NotRequired[bool],
        "paired_diff": typing.NotRequired[bool],
        "paired_diff_norm": typing.NotRequired[bool],
        "paired_diff_norm1": typing.NotRequired[bool],
        "paired_diff_norm2": typing.NotRequired[bool],
        "norm_mean": typing.NotRequired[bool],
        "norm1": typing.NotRequired[bool],
        "matrix": typing.NotRequired[InputPathType],
        "frame_weight": typing.NotRequired[InputPathType],
        "norm_weight": typing.NotRequired[bool],
        "group_mean": typing.NotRequired[float],
        "combine": typing.NotRequired[bool],
        "keep_datatype": typing.NotRequired[bool],
        "abs": typing.NotRequired[bool],
        "pos": typing.NotRequired[bool],
        "neg": typing.NotRequired[bool],
        "mean": typing.NotRequired[bool],
        "median": typing.NotRequired[bool],
        "mean_div_n": typing.NotRequired[bool],
        "sum": typing.NotRequired[bool],
        "var": typing.NotRequired[bool],
        "std": typing.NotRequired[bool],
        "max": typing.NotRequired[bool],
        "max_index": typing.NotRequired[bool],
        "max_index_prune": typing.NotRequired[bool],
        "max_index_add": typing.NotRequired[float],
        "min": typing.NotRequired[bool],
        "replicate_times": typing.NotRequired[float],
        "fnorm": typing.NotRequired[bool],
        "conjunction": typing.NotRequired[bool],
        "vote": typing.NotRequired[bool],
        "sort": typing.NotRequired[bool],
        "temporal_ar1": typing.NotRequired[float],
        "prune": typing.NotRequired[bool],
        "pca": typing.NotRequired[bool],
        "pca_mask": typing.NotRequired[InputPathType],
        "scm": typing.NotRequired[bool],
        "zconcat": typing.NotRequired[str],
        "max_bonfcor": typing.NotRequired[bool],
        "multiply": typing.NotRequired[float],
        "add": typing.NotRequired[float],
        "mask_file": typing.NotRequired[InputPathType],
        "rms": typing.NotRequired[bool],
        "no_check": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriConcatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_concat_params(
    input_files: list[InputPathType],
    output_file: str,
    file_list: str | None = None,
    paired_sum: bool = False,
    paired_avg: bool = False,
    paired_diff: bool = False,
    paired_diff_norm: bool = False,
    paired_diff_norm1: bool = False,
    paired_diff_norm2: bool = False,
    norm_mean: bool = False,
    norm1: bool = False,
    matrix: InputPathType | None = None,
    frame_weight: InputPathType | None = None,
    norm_weight: bool = False,
    group_mean: float | None = None,
    combine: bool = False,
    keep_datatype: bool = False,
    abs: bool = False,
    pos: bool = False,
    neg: bool = False,
    mean: bool = False,
    median: bool = False,
    mean_div_n: bool = False,
    sum: bool = False,
    var: bool = False,
    std: bool = False,
    max: bool = False,
    max_index: bool = False,
    max_index_prune: bool = False,
    max_index_add: float | None = None,
    min: bool = False,
    replicate_times: float | None = None,
    fnorm: bool = False,
    conjunction: bool = False,
    vote: bool = False,
    sort: bool = False,
    temporal_ar1: float | None = None,
    prune: bool = False,
    pca: bool = False,
    pca_mask: InputPathType | None = None,
    scm: bool = False,
    zconcat: str | None = None,
    max_bonfcor: bool = False,
    multiply: float | None = None,
    add: float | None = None,
    mask_file: InputPathType | None = None,
    rms: bool = False,
    no_check: bool = False,
) -> MriConcat:
    """
    Build parameters.

    Args:
        input_files: Input image files (e.g. file1.mgh file2.mgh ...).
        output_file: Output file name (e.g. output.mgh).
        file_list: List file containing a text list of files to process (up to\
            400000 files).
        paired_sum: Compute paired sum (1+2, 3+4, etc).
        paired_avg: Compute paired average (1+2, 3+4, etc).
        paired_diff: Compute paired difference (1-2, 3-4, etc).
        paired_diff_norm: Compute paired difference normalized by TP1,2 average.
        paired_diff_norm1: Compute paired difference normalized by TP1.
        paired_diff_norm2: Compute paired difference normalized by TP2.
        norm_mean: Normalize frames by mean of all time points.
        norm1: Normalize frames by first time point (TP1).
        matrix: Multiply by matrix from ASCII file.
        frame_weight: Weight each frame by values in ASCII file (one value per\
            frame).
        norm_weight: Normalize frames to sum to 1 after weighting.
        group_mean: Create matrix to average Ng groups, Nper=Ntot/Ng.
        combine: Average frames from non-zero voxels.
        keep_datatype: Write output in the same datatype as input (default is Float\
            format).
        abs: Take absolute value of input.
        pos: Set input negatives to 0.
        neg: Set input positives to 0.
        mean: Compute mean of concatenated volumes.
        median: Compute median of concatenated volumes.
        mean_div_n: Compute mean divided by number of frames.
        sum: Compute sum of concatenated volumes.
        var: Compute variance of concatenated volumes.
        std: Compute standard deviation of concatenated volumes.
        max: Compute maximum of concatenated volumes.
        max_index: Compute index of maximum of concatenated volumes.
        max_index_prune: Set max index to 0 where all frames are 0.
        max_index_add: Add value to non-zero max indices.
        min: Compute minimum of concatenated volumes.
        replicate_times: Replicate N times over frames.
        fnorm: Normalize time series at each voxel.
        conjunction: Compute voxel-wise conjunction of concatenated volumes.
        vote: Most frequent value at each voxel and fraction of occurrences.
        sort: Sort each voxel by ascending frame value.
        temporal_ar1: Compute temporal AR1 with degree of freedom adjustment.
        prune: Set voxel value to 0 unless all frames are non-zero.
        pca: Compute and output principal component analysis (PCA).
        pca_mask: Mask used to select voxels for PCA (mask > 0.5).
        scm: Compute spatial covariance matrix.
        zconcat: Concatenate in slice direction skipping nskip slices.
        max_bonfcor: Compute maximum and Bonferroni correct.
        multiply: Multiply volumes by value.
        add: Add value to volumes.
        mask_file: Mask file used with vote or sort.
        rms: Compute root mean square of concatenated volumes.
        no_check: Do not check inputs.

    Returns:
        Parameter dictionary.
    """
    params: MriConcat = {
        "@type": "freesurfer/mri_concat",
        "input_files": input_files,
        "output_file": output_file,
        "paired_sum": paired_sum,
        "paired_avg": paired_avg,
        "paired_diff": paired_diff,
        "paired_diff_norm": paired_diff_norm,
        "paired_diff_norm1": paired_diff_norm1,
        "paired_diff_norm2": paired_diff_norm2,
        "norm_mean": norm_mean,
        "norm1": norm1,
        "norm_weight": norm_weight,
        "combine": combine,
        "keep_datatype": keep_datatype,
        "abs": abs,
        "pos": pos,
        "neg": neg,
        "mean": mean,
        "median": median,
        "mean_div_n": mean_div_n,
        "sum": sum,
        "var": var,
        "std": std,
        "max": max,
        "max_index": max_index,
        "max_index_prune": max_index_prune,
        "min": min,
        "fnorm": fnorm,
        "conjunction": conjunction,
        "vote": vote,
        "sort": sort,
        "prune": prune,
        "pca": pca,
        "scm": scm,
        "max_bonfcor": max_bonfcor,
        "rms": rms,
        "no_check": no_check,
    }
    if file_list is not None:
        params["file_list"] = file_list
    if matrix is not None:
        params["matrix"] = matrix
    if frame_weight is not None:
        params["frame_weight"] = frame_weight
    if group_mean is not None:
        params["group_mean"] = group_mean
    if max_index_add is not None:
        params["max_index_add"] = max_index_add
    if replicate_times is not None:
        params["replicate_times"] = replicate_times
    if temporal_ar1 is not None:
        params["temporal_ar1"] = temporal_ar1
    if pca_mask is not None:
        params["pca_mask"] = pca_mask
    if zconcat is not None:
        params["zconcat"] = zconcat
    if multiply is not None:
        params["multiply"] = multiply
    if add is not None:
        params["add"] = add
    if mask_file is not None:
        params["mask_file"] = mask_file
    return params

def mri_concat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriConcat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("file_list", None) is not None:
        if not isinstance(params["file_list"], str):
            raise StyxValidationError(f'`file_list` has the wrong type: Received `{type(params["file_list"])}` expected `str`')
    if params.get("paired_sum", None) is not None:
        if not isinstance(params["paired_sum"], bool):
            raise StyxValidationError(f'`paired_sum` has the wrong type: Received `{type(params["paired_sum"])}` expected `bool`')
    if params.get("paired_avg", None) is not None:
        if not isinstance(params["paired_avg"], bool):
            raise StyxValidationError(f'`paired_avg` has the wrong type: Received `{type(params["paired_avg"])}` expected `bool`')
    if params.get("paired_diff", None) is not None:
        if not isinstance(params["paired_diff"], bool):
            raise StyxValidationError(f'`paired_diff` has the wrong type: Received `{type(params["paired_diff"])}` expected `bool`')
    if params.get("paired_diff_norm", None) is not None:
        if not isinstance(params["paired_diff_norm"], bool):
            raise StyxValidationError(f'`paired_diff_norm` has the wrong type: Received `{type(params["paired_diff_norm"])}` expected `bool`')
    if params.get("paired_diff_norm1", None) is not None:
        if not isinstance(params["paired_diff_norm1"], bool):
            raise StyxValidationError(f'`paired_diff_norm1` has the wrong type: Received `{type(params["paired_diff_norm1"])}` expected `bool`')
    if params.get("paired_diff_norm2", None) is not None:
        if not isinstance(params["paired_diff_norm2"], bool):
            raise StyxValidationError(f'`paired_diff_norm2` has the wrong type: Received `{type(params["paired_diff_norm2"])}` expected `bool`')
    if params.get("norm_mean", None) is not None:
        if not isinstance(params["norm_mean"], bool):
            raise StyxValidationError(f'`norm_mean` has the wrong type: Received `{type(params["norm_mean"])}` expected `bool`')
    if params.get("norm1", None) is not None:
        if not isinstance(params["norm1"], bool):
            raise StyxValidationError(f'`norm1` has the wrong type: Received `{type(params["norm1"])}` expected `bool`')
    if params.get("matrix", None) is not None:
        if not isinstance(params["matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`matrix` has the wrong type: Received `{type(params["matrix"])}` expected `InputPathType`')
    if params.get("frame_weight", None) is not None:
        if not isinstance(params["frame_weight"], (pathlib.Path, str)):
            raise StyxValidationError(f'`frame_weight` has the wrong type: Received `{type(params["frame_weight"])}` expected `InputPathType`')
    if params.get("norm_weight", None) is not None:
        if not isinstance(params["norm_weight"], bool):
            raise StyxValidationError(f'`norm_weight` has the wrong type: Received `{type(params["norm_weight"])}` expected `bool`')
    if params.get("group_mean", None) is not None:
        if not isinstance(params["group_mean"], (float, int)):
            raise StyxValidationError(f'`group_mean` has the wrong type: Received `{type(params["group_mean"])}` expected `float`')
    if params.get("combine", None) is not None:
        if not isinstance(params["combine"], bool):
            raise StyxValidationError(f'`combine` has the wrong type: Received `{type(params["combine"])}` expected `bool`')
    if params.get("keep_datatype", None) is not None:
        if not isinstance(params["keep_datatype"], bool):
            raise StyxValidationError(f'`keep_datatype` has the wrong type: Received `{type(params["keep_datatype"])}` expected `bool`')
    if params.get("abs", None) is not None:
        if not isinstance(params["abs"], bool):
            raise StyxValidationError(f'`abs` has the wrong type: Received `{type(params["abs"])}` expected `bool`')
    if params.get("pos", None) is not None:
        if not isinstance(params["pos"], bool):
            raise StyxValidationError(f'`pos` has the wrong type: Received `{type(params["pos"])}` expected `bool`')
    if params.get("neg", None) is not None:
        if not isinstance(params["neg"], bool):
            raise StyxValidationError(f'`neg` has the wrong type: Received `{type(params["neg"])}` expected `bool`')
    if params.get("mean", None) is not None:
        if not isinstance(params["mean"], bool):
            raise StyxValidationError(f'`mean` has the wrong type: Received `{type(params["mean"])}` expected `bool`')
    if params.get("median", None) is not None:
        if not isinstance(params["median"], bool):
            raise StyxValidationError(f'`median` has the wrong type: Received `{type(params["median"])}` expected `bool`')
    if params.get("mean_div_n", None) is not None:
        if not isinstance(params["mean_div_n"], bool):
            raise StyxValidationError(f'`mean_div_n` has the wrong type: Received `{type(params["mean_div_n"])}` expected `bool`')
    if params.get("sum", None) is not None:
        if not isinstance(params["sum"], bool):
            raise StyxValidationError(f'`sum` has the wrong type: Received `{type(params["sum"])}` expected `bool`')
    if params.get("var", None) is not None:
        if not isinstance(params["var"], bool):
            raise StyxValidationError(f'`var` has the wrong type: Received `{type(params["var"])}` expected `bool`')
    if params.get("std", None) is not None:
        if not isinstance(params["std"], bool):
            raise StyxValidationError(f'`std` has the wrong type: Received `{type(params["std"])}` expected `bool`')
    if params.get("max", None) is not None:
        if not isinstance(params["max"], bool):
            raise StyxValidationError(f'`max` has the wrong type: Received `{type(params["max"])}` expected `bool`')
    if params.get("max_index", None) is not None:
        if not isinstance(params["max_index"], bool):
            raise StyxValidationError(f'`max_index` has the wrong type: Received `{type(params["max_index"])}` expected `bool`')
    if params.get("max_index_prune", None) is not None:
        if not isinstance(params["max_index_prune"], bool):
            raise StyxValidationError(f'`max_index_prune` has the wrong type: Received `{type(params["max_index_prune"])}` expected `bool`')
    if params.get("max_index_add", None) is not None:
        if not isinstance(params["max_index_add"], (float, int)):
            raise StyxValidationError(f'`max_index_add` has the wrong type: Received `{type(params["max_index_add"])}` expected `float`')
    if params.get("min", None) is not None:
        if not isinstance(params["min"], bool):
            raise StyxValidationError(f'`min` has the wrong type: Received `{type(params["min"])}` expected `bool`')
    if params.get("replicate_times", None) is not None:
        if not isinstance(params["replicate_times"], (float, int)):
            raise StyxValidationError(f'`replicate_times` has the wrong type: Received `{type(params["replicate_times"])}` expected `float`')
    if params.get("fnorm", None) is not None:
        if not isinstance(params["fnorm"], bool):
            raise StyxValidationError(f'`fnorm` has the wrong type: Received `{type(params["fnorm"])}` expected `bool`')
    if params.get("conjunction", None) is not None:
        if not isinstance(params["conjunction"], bool):
            raise StyxValidationError(f'`conjunction` has the wrong type: Received `{type(params["conjunction"])}` expected `bool`')
    if params.get("vote", None) is not None:
        if not isinstance(params["vote"], bool):
            raise StyxValidationError(f'`vote` has the wrong type: Received `{type(params["vote"])}` expected `bool`')
    if params.get("sort", None) is not None:
        if not isinstance(params["sort"], bool):
            raise StyxValidationError(f'`sort` has the wrong type: Received `{type(params["sort"])}` expected `bool`')
    if params.get("temporal_ar1", None) is not None:
        if not isinstance(params["temporal_ar1"], (float, int)):
            raise StyxValidationError(f'`temporal_ar1` has the wrong type: Received `{type(params["temporal_ar1"])}` expected `float`')
    if params.get("prune", None) is not None:
        if not isinstance(params["prune"], bool):
            raise StyxValidationError(f'`prune` has the wrong type: Received `{type(params["prune"])}` expected `bool`')
    if params.get("pca", None) is not None:
        if not isinstance(params["pca"], bool):
            raise StyxValidationError(f'`pca` has the wrong type: Received `{type(params["pca"])}` expected `bool`')
    if params.get("pca_mask", None) is not None:
        if not isinstance(params["pca_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`pca_mask` has the wrong type: Received `{type(params["pca_mask"])}` expected `InputPathType`')
    if params.get("scm", None) is not None:
        if not isinstance(params["scm"], bool):
            raise StyxValidationError(f'`scm` has the wrong type: Received `{type(params["scm"])}` expected `bool`')
    if params.get("zconcat", None) is not None:
        if not isinstance(params["zconcat"], str):
            raise StyxValidationError(f'`zconcat` has the wrong type: Received `{type(params["zconcat"])}` expected `str`')
    if params.get("max_bonfcor", None) is not None:
        if not isinstance(params["max_bonfcor"], bool):
            raise StyxValidationError(f'`max_bonfcor` has the wrong type: Received `{type(params["max_bonfcor"])}` expected `bool`')
    if params.get("multiply", None) is not None:
        if not isinstance(params["multiply"], (float, int)):
            raise StyxValidationError(f'`multiply` has the wrong type: Received `{type(params["multiply"])}` expected `float`')
    if params.get("add", None) is not None:
        if not isinstance(params["add"], (float, int)):
            raise StyxValidationError(f'`add` has the wrong type: Received `{type(params["add"])}` expected `float`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("rms", None) is not None:
        if not isinstance(params["rms"], bool):
            raise StyxValidationError(f'`rms` has the wrong type: Received `{type(params["rms"])}` expected `bool`')
    if params.get("no_check", None) is not None:
        if not isinstance(params["no_check"], bool):
            raise StyxValidationError(f'`no_check` has the wrong type: Received `{type(params["no_check"])}` expected `bool`')

def mri_concat_cargs(params: MriConcat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_concat")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--o")
    cargs.append(params["output_file"])
    v_0 = params.get("file_list")
    if v_0 is not None:
        cargs.append("--f")
        cargs.append(v_0)
    if params.get("paired_sum"):
        cargs.append("--paired-sum")
    if params.get("paired_avg"):
        cargs.append("--paired-avg")
    if params.get("paired_diff"):
        cargs.append("--paired-diff")
    if params.get("paired_diff_norm"):
        cargs.append("--paired-diff-norm")
    if params.get("paired_diff_norm1"):
        cargs.append("--paired-diff-norm1")
    if params.get("paired_diff_norm2"):
        cargs.append("--paired-diff-norm2")
    if params.get("norm_mean"):
        cargs.append("--norm-mean")
    if params.get("norm1"):
        cargs.append("--norm1")
    v_1 = params.get("matrix")
    if v_1 is not None:
        cargs.append("--mtx")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("frame_weight")
    if v_2 is not None:
        cargs.append("--w")
        cargs.append(execution.input_file(v_2))
    if params.get("norm_weight"):
        cargs.append("--wn")
    v_3 = params.get("group_mean")
    if v_3 is not None:
        cargs.append("--gmean")
        cargs.append(str(v_3))
    if params.get("combine"):
        cargs.append("--combine")
    if params.get("keep_datatype"):
        cargs.append("--keep-datatype")
    if params.get("abs"):
        cargs.append("--abs")
    if params.get("pos"):
        cargs.append("--pos")
    if params.get("neg"):
        cargs.append("--neg")
    if params.get("mean"):
        cargs.append("--mean")
    if params.get("median"):
        cargs.append("--median")
    if params.get("mean_div_n"):
        cargs.append("--mean-div-n")
    if params.get("sum"):
        cargs.append("--sum")
    if params.get("var"):
        cargs.append("--var")
    if params.get("std"):
        cargs.append("--std")
    if params.get("max"):
        cargs.append("--max")
    if params.get("max_index"):
        cargs.append("--max-index")
    if params.get("max_index_prune"):
        cargs.append("--max-index-prune")
    v_4 = params.get("max_index_add")
    if v_4 is not None:
        cargs.append("--max-index-add")
        cargs.append(str(v_4))
    if params.get("min"):
        cargs.append("--min")
    v_5 = params.get("replicate_times")
    if v_5 is not None:
        cargs.append("--rep")
        cargs.append(str(v_5))
    if params.get("fnorm"):
        cargs.append("--fnorm")
    if params.get("conjunction"):
        cargs.append("--conjunct")
    if params.get("vote"):
        cargs.append("--vote")
    if params.get("sort"):
        cargs.append("--sort")
    v_6 = params.get("temporal_ar1")
    if v_6 is not None:
        cargs.append("--tar1")
        cargs.append(str(v_6))
    if params.get("prune"):
        cargs.append("--prune")
    if params.get("pca"):
        cargs.append("--pca")
    v_7 = params.get("pca_mask")
    if v_7 is not None:
        cargs.append("--pca-mask")
        cargs.append(execution.input_file(v_7))
    if params.get("scm"):
        cargs.append("--scm")
    v_8 = params.get("zconcat")
    if v_8 is not None:
        cargs.append("--zconcat")
        cargs.append(v_8)
    if params.get("max_bonfcor"):
        cargs.append("--max-bonfcor")
    v_9 = params.get("multiply")
    if v_9 is not None:
        cargs.append("--mul")
        cargs.append(str(v_9))
    v_10 = params.get("add")
    if v_10 is not None:
        cargs.append("--add")
        cargs.append(str(v_10))
    v_11 = params.get("mask_file")
    if v_11 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_11))
    if params.get("rms"):
        cargs.append("--rms")
    if params.get("no_check"):
        cargs.append("--no-check")
    return cargs

def mri_concat_outputs(params: MriConcat, execution: Execution) -> MriConcatOutputs:
    """Build the MriConcatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriConcatOutputs(
        root=root_v,
    )

def mri_concat_execute(params: MriConcat, runner: Runner | None = None) -> MriConcatOutputs:
    """
    mri_concat

    Concatenates input data sets.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_concat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CONCAT_METADATA)
    execution.params(params)
    args = mri_concat_cargs(params, execution)
    out = mri_concat_outputs(params, execution)
    execution.run(args)
    return out

def mri_concat(
    input_files: list[InputPathType],
    output_file: str,
    file_list: str | None = None,
    paired_sum: bool = False,
    paired_avg: bool = False,
    paired_diff: bool = False,
    paired_diff_norm: bool = False,
    paired_diff_norm1: bool = False,
    paired_diff_norm2: bool = False,
    norm_mean: bool = False,
    norm1: bool = False,
    matrix: InputPathType | None = None,
    frame_weight: InputPathType | None = None,
    norm_weight: bool = False,
    group_mean: float | None = None,
    combine: bool = False,
    keep_datatype: bool = False,
    abs: bool = False,
    pos: bool = False,
    neg: bool = False,
    mean: bool = False,
    median: bool = False,
    mean_div_n: bool = False,
    sum: bool = False,
    var: bool = False,
    std: bool = False,
    max: bool = False,
    max_index: bool = False,
    max_index_prune: bool = False,
    max_index_add: float | None = None,
    min: bool = False,
    replicate_times: float | None = None,
    fnorm: bool = False,
    conjunction: bool = False,
    vote: bool = False,
    sort: bool = False,
    temporal_ar1: float | None = None,
    prune: bool = False,
    pca: bool = False,
    pca_mask: InputPathType | None = None,
    scm: bool = False,
    zconcat: str | None = None,
    max_bonfcor: bool = False,
    multiply: float | None = None,
    add: float | None = None,
    mask_file: InputPathType | None = None,
    rms: bool = False,
    no_check: bool = False,
    runner: Runner | None = None,
) -> MriConcatOutputs:
    """
    mri_concat

    Concatenates input data sets.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input image files (e.g. file1.mgh file2.mgh ...).
        output_file: Output file name (e.g. output.mgh).
        file_list: List file containing a text list of files to process (up to\
            400000 files).
        paired_sum: Compute paired sum (1+2, 3+4, etc).
        paired_avg: Compute paired average (1+2, 3+4, etc).
        paired_diff: Compute paired difference (1-2, 3-4, etc).
        paired_diff_norm: Compute paired difference normalized by TP1,2 average.
        paired_diff_norm1: Compute paired difference normalized by TP1.
        paired_diff_norm2: Compute paired difference normalized by TP2.
        norm_mean: Normalize frames by mean of all time points.
        norm1: Normalize frames by first time point (TP1).
        matrix: Multiply by matrix from ASCII file.
        frame_weight: Weight each frame by values in ASCII file (one value per\
            frame).
        norm_weight: Normalize frames to sum to 1 after weighting.
        group_mean: Create matrix to average Ng groups, Nper=Ntot/Ng.
        combine: Average frames from non-zero voxels.
        keep_datatype: Write output in the same datatype as input (default is Float\
            format).
        abs: Take absolute value of input.
        pos: Set input negatives to 0.
        neg: Set input positives to 0.
        mean: Compute mean of concatenated volumes.
        median: Compute median of concatenated volumes.
        mean_div_n: Compute mean divided by number of frames.
        sum: Compute sum of concatenated volumes.
        var: Compute variance of concatenated volumes.
        std: Compute standard deviation of concatenated volumes.
        max: Compute maximum of concatenated volumes.
        max_index: Compute index of maximum of concatenated volumes.
        max_index_prune: Set max index to 0 where all frames are 0.
        max_index_add: Add value to non-zero max indices.
        min: Compute minimum of concatenated volumes.
        replicate_times: Replicate N times over frames.
        fnorm: Normalize time series at each voxel.
        conjunction: Compute voxel-wise conjunction of concatenated volumes.
        vote: Most frequent value at each voxel and fraction of occurrences.
        sort: Sort each voxel by ascending frame value.
        temporal_ar1: Compute temporal AR1 with degree of freedom adjustment.
        prune: Set voxel value to 0 unless all frames are non-zero.
        pca: Compute and output principal component analysis (PCA).
        pca_mask: Mask used to select voxels for PCA (mask > 0.5).
        scm: Compute spatial covariance matrix.
        zconcat: Concatenate in slice direction skipping nskip slices.
        max_bonfcor: Compute maximum and Bonferroni correct.
        multiply: Multiply volumes by value.
        add: Add value to volumes.
        mask_file: Mask file used with vote or sort.
        rms: Compute root mean square of concatenated volumes.
        no_check: Do not check inputs.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_concat_params(
        input_files=input_files,
        output_file=output_file,
        file_list=file_list,
        paired_sum=paired_sum,
        paired_avg=paired_avg,
        paired_diff=paired_diff,
        paired_diff_norm=paired_diff_norm,
        paired_diff_norm1=paired_diff_norm1,
        paired_diff_norm2=paired_diff_norm2,
        norm_mean=norm_mean,
        norm1=norm1,
        matrix=matrix,
        frame_weight=frame_weight,
        norm_weight=norm_weight,
        group_mean=group_mean,
        combine=combine,
        keep_datatype=keep_datatype,
        abs=abs,
        pos=pos,
        neg=neg,
        mean=mean,
        median=median,
        mean_div_n=mean_div_n,
        sum=sum,
        var=var,
        std=std,
        max=max,
        max_index=max_index,
        max_index_prune=max_index_prune,
        max_index_add=max_index_add,
        min=min,
        replicate_times=replicate_times,
        fnorm=fnorm,
        conjunction=conjunction,
        vote=vote,
        sort=sort,
        temporal_ar1=temporal_ar1,
        prune=prune,
        pca=pca,
        pca_mask=pca_mask,
        scm=scm,
        zconcat=zconcat,
        max_bonfcor=max_bonfcor,
        multiply=multiply,
        add=add,
        mask_file=mask_file,
        rms=rms,
        no_check=no_check,
    )
    return mri_concat_execute(params, runner)

__all__ = [
    "MriConcat",
    "MriConcatOutputs",
    "MRI_CONCAT_METADATA",
    "mri_concat_cargs",
    "mri_concat_outputs",
    "mri_concat_params",
    "mri_concat_execute",
    "mri_concat_validate",
    "mri_concat",
]