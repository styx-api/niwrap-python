# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IS_LTA_METADATA = Metadata(
    id="IsLTA",
    name="IsLTA",
    package="freesurfer",
)

IsLta = typing.TypedDict(
    "IsLta",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/IsLTA"]],
        "candidate_file": InputPathType,
        "outfile": str,
    },
)

@dataclasses.dataclass
class IsLtaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """File containing 1 if the candidate file is an LTA, otherwise 0"""

def is_lta_params(
    candidate_file: InputPathType,
    outfile: str,
) -> IsLta:
    """
    Build parameters.

    Args:
        candidate_file:
        outfile:

    Returns:
        Parameter dictionary.
    """
    params: IsLta = {
        "@type": "freesurfer/IsLTA",
        "candidate_file": candidate_file,
        "outfile": outfile,
    }
    return params

def is_lta_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IsLta."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("candidate_file", None) is None:
        raise StyxValidationError("`candidate_file` must not be None")
    if not isinstance(params["candidate_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`candidate_file` has the wrong type: Received `{type(params["candidate_file"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def is_lta_cargs(params: IsLta, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("IsLTA")
    cargs.append("--r")
    cargs.append(execution.input_file(params["candidate_file"]))
    cargs.append("--o")
    cargs.append(params["outfile"])
    return cargs

def is_lta_outputs(params: IsLta, execution: Execution) -> IsLtaOutputs:
    """Build the IsLtaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["outfile"])
    return IsLtaOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def is_lta_execute(params: IsLta, runner: Runner | None = None) -> IsLtaOutputs:
    """
    IsLTA

    Determines if a given file is an LTA (Linear Transform Array) file. Outputs 1 if true, otherwise outputs 0.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    is_lta_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IS_LTA_METADATA)
    execution.params(params)
    args = is_lta_cargs(params, execution)
    out = is_lta_outputs(params, execution)
    execution.run(args)
    return out

def is_lta(
    candidate_file: InputPathType,
    outfile: str,
    runner: Runner | None = None,
) -> IsLtaOutputs:
    """
    IsLTA

    Determines if a given file is an LTA (Linear Transform Array) file. Outputs 1 if true, otherwise outputs 0.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        candidate_file:
        outfile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = is_lta_params(
        candidate_file=candidate_file,
        outfile=outfile,
    )
    return is_lta_execute(params, runner)

__all__ = [
    "IsLta",
    "IsLtaOutputs",
    "IS_LTA_METADATA",
    "is_lta_cargs",
    "is_lta_outputs",
    "is_lta_params",
    "is_lta_execute",
    "is_lta_validate",
    "is_lta",
]