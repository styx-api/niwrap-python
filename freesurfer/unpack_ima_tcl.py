# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACK_IMA_TCL_METADATA = Metadata(
    id="unpack_ima.tcl",
    name="unpack_ima.tcl",
    package="freesurfer",
)

UnpackImaTcl = typing.TypedDict(
    "UnpackImaTcl",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpack_ima.tcl"]],
        "target_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class UnpackImaTclOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def unpack_ima_tcl_params(
    target_dir: str = "~",
) -> UnpackImaTcl:
    """
    Build parameters.

    Args:
        target_dir: Target directory to unpack the files.

    Returns:
        Parameter dictionary.
    """
    params: UnpackImaTcl = {
        "@type": "freesurfer/unpack_ima.tcl",
        "target_dir": target_dir,
    }
    return params

def unpack_ima_tcl_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UnpackImaTcl."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("target_dir", None) is not None:
        if not isinstance(params["target_dir"], str):
            raise StyxValidationError(f'`target_dir` has the wrong type: Received `{type(params["target_dir"])}` expected `str`')

def unpack_ima_tcl_cargs(params: UnpackImaTcl, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpack_ima.tcl")
    cargs.append(params.get("target_dir", "~"))
    return cargs

def unpack_ima_tcl_outputs(params: UnpackImaTcl, execution: Execution) -> UnpackImaTclOutputs:
    """Build the UnpackImaTclOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return UnpackImaTclOutputs(
        root=root_v,
    )

def unpack_ima_tcl_execute(params: UnpackImaTcl, runner: Runner | None = None) -> UnpackImaTclOutputs:
    """
    unpack_ima.tcl

    A tool to unpack MRI DICOM .ima files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpack_ima_tcl_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACK_IMA_TCL_METADATA)
    execution.params(params)
    args = unpack_ima_tcl_cargs(params, execution)
    out = unpack_ima_tcl_outputs(params, execution)
    execution.run(args)
    return out

def unpack_ima_tcl(
    target_dir: str = "~",
    runner: Runner | None = None,
) -> UnpackImaTclOutputs:
    """
    unpack_ima.tcl

    A tool to unpack MRI DICOM .ima files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        target_dir: Target directory to unpack the files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpack_ima_tcl_params(
        target_dir=target_dir,
    )
    return unpack_ima_tcl_execute(params, runner)

__all__ = [
    "UnpackImaTcl",
    "UnpackImaTclOutputs",
    "UNPACK_IMA_TCL_METADATA",
    "unpack_ima_tcl_cargs",
    "unpack_ima_tcl_outputs",
    "unpack_ima_tcl_params",
    "unpack_ima_tcl_execute",
    "unpack_ima_tcl_validate",
    "unpack_ima_tcl",
]