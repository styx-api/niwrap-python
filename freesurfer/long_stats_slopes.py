# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_STATS_SLOPES_METADATA = Metadata(
    id="long_stats_slopes",
    name="long_stats_slopes",
    package="freesurfer",
)

LongStatsSlopes = typing.TypedDict(
    "LongStatsSlopes",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_stats_slopes"]],
        "qdec_table": InputPathType,
        "stats_file": InputPathType,
        "measure": str,
        "subjects_dir": str,
        "do_avg": typing.NotRequired[bool],
        "do_rate": typing.NotRequired[bool],
        "do_pc1fit": typing.NotRequired[bool],
        "do_pc1": typing.NotRequired[bool],
        "do_spc": typing.NotRequired[bool],
        "do_stack": typing.NotRequired[bool],
        "resid": typing.NotRequired[float],
        "time_var": typing.NotRequired[str],
        "generic_time": typing.NotRequired[bool],
        "cross_sectional": typing.NotRequired[bool],
        "out_avg": typing.NotRequired[str],
        "out_rate": typing.NotRequired[str],
        "out_pc1fit": typing.NotRequired[str],
        "out_pc1": typing.NotRequired[str],
        "out_spc": typing.NotRequired[str],
        "out_resid": typing.NotRequired[str],
        "out_stack": typing.NotRequired[str],
        "stack_avg": typing.NotRequired[str],
        "stack_rate": typing.NotRequired[str],
        "stack_pc1fit": typing.NotRequired[str],
        "stack_pc1": typing.NotRequired[str],
        "stack_spc": typing.NotRequired[str],
        "stack_resid": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LongStatsSlopesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def long_stats_slopes_params(
    qdec_table: InputPathType,
    stats_file: InputPathType,
    measure: str,
    subjects_dir: str,
    do_avg: bool = False,
    do_rate: bool = False,
    do_pc1fit: bool = False,
    do_pc1: bool = False,
    do_spc: bool = False,
    do_stack: bool = False,
    resid: float | None = None,
    time_var: str | None = None,
    generic_time: bool = False,
    cross_sectional: bool = False,
    out_avg: str | None = None,
    out_rate: str | None = None,
    out_pc1fit: str | None = None,
    out_pc1: str | None = None,
    out_spc: str | None = None,
    out_resid: str | None = None,
    out_stack: str | None = None,
    stack_avg: str | None = None,
    stack_rate: str | None = None,
    stack_pc1fit: str | None = None,
    stack_pc1: str | None = None,
    stack_spc: str | None = None,
    stack_resid: str | None = None,
) -> LongStatsSlopes:
    """
    Build parameters.

    Args:
        qdec_table: QDEC table file specifying the subjects and time points.
        stats_file: The stats file, e.g. aseg.stats or lh.aparc.stats.
        measure: The stats measure (e.g. volume, thickness, mean, std).
        subjects_dir: Full path to FreeSurfer subjects directory.
        do_avg: Compute and output the temporal average.
        do_rate: Compute and output the rate.
        do_pc1fit: Compute and output the percent change with respect to tp1 from\
            linear fit.
        do_pc1: Compute and output the percent change with respect to tp1.
        do_spc: Compute and output the symmetric percent change with respect to\
            temporal average.
        do_stack: Compute and output tables showing the time series (row per time\
            point).
        resid: Residual timepoint to export (pass 1 for tp1, etc., pass 0 for\
            average).
        time_var: Variable name for time column variable (e.g., age) in QDEC table.
        generic_time: Time points are ordered in QDEC file; assume time=1,2,3...
        cross_sectional: Use cross-sectional results (for testing only).
        out_avg: Filename to store temporal average in <template>/stats/<OUT_AVG>.
        out_rate: Filename to store rate in <template>/stats/<OUT_RATE>.
        out_pc1fit: Filename to store percent change to tp1fit in\
            <template>/stats/<OUT_PC1FIT>.
        out_pc1: Filename to store percent change to tp1 in\
            <template>/stats/<OUT_PC1>.
        out_spc: Filename to store symmetric percent change in\
            <template>/stats/<OUT_SPC>.
        out_resid: Filename to store residual in <template>/stats/<OUT_RESID>.
        out_stack: Filename to store stacked measure file in\
            <template>/stats/<OUT_STACK>.
        stack_avg: Full filename to stack temporal average tables.
        stack_rate: Full filename to stack rate tables.
        stack_pc1fit: Full filename to stack percent change to tp1fit tables.
        stack_pc1: Full filename to stack percent change to tp1 tables.
        stack_spc: Full filename to stack symmetric percent change tables.
        stack_resid: Full filename to stack residual tables.

    Returns:
        Parameter dictionary.
    """
    params: LongStatsSlopes = {
        "@type": "freesurfer/long_stats_slopes",
        "qdec_table": qdec_table,
        "stats_file": stats_file,
        "measure": measure,
        "subjects_dir": subjects_dir,
        "do_avg": do_avg,
        "do_rate": do_rate,
        "do_pc1fit": do_pc1fit,
        "do_pc1": do_pc1,
        "do_spc": do_spc,
        "do_stack": do_stack,
        "generic_time": generic_time,
        "cross_sectional": cross_sectional,
    }
    if resid is not None:
        params["resid"] = resid
    if time_var is not None:
        params["time_var"] = time_var
    if out_avg is not None:
        params["out_avg"] = out_avg
    if out_rate is not None:
        params["out_rate"] = out_rate
    if out_pc1fit is not None:
        params["out_pc1fit"] = out_pc1fit
    if out_pc1 is not None:
        params["out_pc1"] = out_pc1
    if out_spc is not None:
        params["out_spc"] = out_spc
    if out_resid is not None:
        params["out_resid"] = out_resid
    if out_stack is not None:
        params["out_stack"] = out_stack
    if stack_avg is not None:
        params["stack_avg"] = stack_avg
    if stack_rate is not None:
        params["stack_rate"] = stack_rate
    if stack_pc1fit is not None:
        params["stack_pc1fit"] = stack_pc1fit
    if stack_pc1 is not None:
        params["stack_pc1"] = stack_pc1
    if stack_spc is not None:
        params["stack_spc"] = stack_spc
    if stack_resid is not None:
        params["stack_resid"] = stack_resid
    return params

def long_stats_slopes_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongStatsSlopes."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec_table", None) is None:
        raise StyxValidationError("`qdec_table` must not be None")
    if not isinstance(params["qdec_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec_table` has the wrong type: Received `{type(params["qdec_table"])}` expected `InputPathType`')
    if params.get("stats_file", None) is None:
        raise StyxValidationError("`stats_file` must not be None")
    if not isinstance(params["stats_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`stats_file` has the wrong type: Received `{type(params["stats_file"])}` expected `InputPathType`')
    if params.get("measure", None) is None:
        raise StyxValidationError("`measure` must not be None")
    if not isinstance(params["measure"], str):
        raise StyxValidationError(f'`measure` has the wrong type: Received `{type(params["measure"])}` expected `str`')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("do_avg", None) is not None:
        if not isinstance(params["do_avg"], bool):
            raise StyxValidationError(f'`do_avg` has the wrong type: Received `{type(params["do_avg"])}` expected `bool`')
    if params.get("do_rate", None) is not None:
        if not isinstance(params["do_rate"], bool):
            raise StyxValidationError(f'`do_rate` has the wrong type: Received `{type(params["do_rate"])}` expected `bool`')
    if params.get("do_pc1fit", None) is not None:
        if not isinstance(params["do_pc1fit"], bool):
            raise StyxValidationError(f'`do_pc1fit` has the wrong type: Received `{type(params["do_pc1fit"])}` expected `bool`')
    if params.get("do_pc1", None) is not None:
        if not isinstance(params["do_pc1"], bool):
            raise StyxValidationError(f'`do_pc1` has the wrong type: Received `{type(params["do_pc1"])}` expected `bool`')
    if params.get("do_spc", None) is not None:
        if not isinstance(params["do_spc"], bool):
            raise StyxValidationError(f'`do_spc` has the wrong type: Received `{type(params["do_spc"])}` expected `bool`')
    if params.get("do_stack", None) is not None:
        if not isinstance(params["do_stack"], bool):
            raise StyxValidationError(f'`do_stack` has the wrong type: Received `{type(params["do_stack"])}` expected `bool`')
    if params.get("resid", None) is not None:
        if not isinstance(params["resid"], (float, int)):
            raise StyxValidationError(f'`resid` has the wrong type: Received `{type(params["resid"])}` expected `float`')
    if params.get("time_var", None) is not None:
        if not isinstance(params["time_var"], str):
            raise StyxValidationError(f'`time_var` has the wrong type: Received `{type(params["time_var"])}` expected `str`')
    if params.get("generic_time", None) is not None:
        if not isinstance(params["generic_time"], bool):
            raise StyxValidationError(f'`generic_time` has the wrong type: Received `{type(params["generic_time"])}` expected `bool`')
    if params.get("cross_sectional", None) is not None:
        if not isinstance(params["cross_sectional"], bool):
            raise StyxValidationError(f'`cross_sectional` has the wrong type: Received `{type(params["cross_sectional"])}` expected `bool`')
    if params.get("out_avg", None) is not None:
        if not isinstance(params["out_avg"], str):
            raise StyxValidationError(f'`out_avg` has the wrong type: Received `{type(params["out_avg"])}` expected `str`')
    if params.get("out_rate", None) is not None:
        if not isinstance(params["out_rate"], str):
            raise StyxValidationError(f'`out_rate` has the wrong type: Received `{type(params["out_rate"])}` expected `str`')
    if params.get("out_pc1fit", None) is not None:
        if not isinstance(params["out_pc1fit"], str):
            raise StyxValidationError(f'`out_pc1fit` has the wrong type: Received `{type(params["out_pc1fit"])}` expected `str`')
    if params.get("out_pc1", None) is not None:
        if not isinstance(params["out_pc1"], str):
            raise StyxValidationError(f'`out_pc1` has the wrong type: Received `{type(params["out_pc1"])}` expected `str`')
    if params.get("out_spc", None) is not None:
        if not isinstance(params["out_spc"], str):
            raise StyxValidationError(f'`out_spc` has the wrong type: Received `{type(params["out_spc"])}` expected `str`')
    if params.get("out_resid", None) is not None:
        if not isinstance(params["out_resid"], str):
            raise StyxValidationError(f'`out_resid` has the wrong type: Received `{type(params["out_resid"])}` expected `str`')
    if params.get("out_stack", None) is not None:
        if not isinstance(params["out_stack"], str):
            raise StyxValidationError(f'`out_stack` has the wrong type: Received `{type(params["out_stack"])}` expected `str`')
    if params.get("stack_avg", None) is not None:
        if not isinstance(params["stack_avg"], str):
            raise StyxValidationError(f'`stack_avg` has the wrong type: Received `{type(params["stack_avg"])}` expected `str`')
    if params.get("stack_rate", None) is not None:
        if not isinstance(params["stack_rate"], str):
            raise StyxValidationError(f'`stack_rate` has the wrong type: Received `{type(params["stack_rate"])}` expected `str`')
    if params.get("stack_pc1fit", None) is not None:
        if not isinstance(params["stack_pc1fit"], str):
            raise StyxValidationError(f'`stack_pc1fit` has the wrong type: Received `{type(params["stack_pc1fit"])}` expected `str`')
    if params.get("stack_pc1", None) is not None:
        if not isinstance(params["stack_pc1"], str):
            raise StyxValidationError(f'`stack_pc1` has the wrong type: Received `{type(params["stack_pc1"])}` expected `str`')
    if params.get("stack_spc", None) is not None:
        if not isinstance(params["stack_spc"], str):
            raise StyxValidationError(f'`stack_spc` has the wrong type: Received `{type(params["stack_spc"])}` expected `str`')
    if params.get("stack_resid", None) is not None:
        if not isinstance(params["stack_resid"], str):
            raise StyxValidationError(f'`stack_resid` has the wrong type: Received `{type(params["stack_resid"])}` expected `str`')

def long_stats_slopes_cargs(params: LongStatsSlopes, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_stats_slopes")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec_table"]))
    cargs.append("--stats")
    cargs.append(execution.input_file(params["stats_file"]))
    cargs.append("--meas")
    cargs.append(params["measure"])
    cargs.append("--sd")
    cargs.append(params["subjects_dir"])
    if params.get("do_avg"):
        cargs.append("--do-avg")
    if params.get("do_rate"):
        cargs.append("--do-rate")
    if params.get("do_pc1fit"):
        cargs.append("--do-pc1fit")
    if params.get("do_pc1"):
        cargs.append("--do-pc1")
    if params.get("do_spc"):
        cargs.append("--do-spc")
    if params.get("do_stack"):
        cargs.append("--do-stack")
    v_0 = params.get("resid")
    if v_0 is not None:
        cargs.append("--resid")
        cargs.append(str(v_0))
    v_1 = params.get("time_var")
    if v_1 is not None:
        cargs.append("--time")
        cargs.append(v_1)
    if params.get("generic_time"):
        cargs.append("--generic-time")
    if params.get("cross_sectional"):
        cargs.append("--cross")
    v_2 = params.get("out_avg")
    if v_2 is not None:
        cargs.append("--out-avg")
        cargs.append(v_2)
    v_3 = params.get("out_rate")
    if v_3 is not None:
        cargs.append("--out-rate")
        cargs.append(v_3)
    v_4 = params.get("out_pc1fit")
    if v_4 is not None:
        cargs.append("--out-pc1fit")
        cargs.append(v_4)
    v_5 = params.get("out_pc1")
    if v_5 is not None:
        cargs.append("--out-pc1")
        cargs.append(v_5)
    v_6 = params.get("out_spc")
    if v_6 is not None:
        cargs.append("--out-spc")
        cargs.append(v_6)
    v_7 = params.get("out_resid")
    if v_7 is not None:
        cargs.append("--out-resid")
        cargs.append(v_7)
    v_8 = params.get("out_stack")
    if v_8 is not None:
        cargs.append("--out-stack")
        cargs.append(v_8)
    v_9 = params.get("stack_avg")
    if v_9 is not None:
        cargs.append("--stack-avg")
        cargs.append(v_9)
    v_10 = params.get("stack_rate")
    if v_10 is not None:
        cargs.append("--stack-rate")
        cargs.append(v_10)
    v_11 = params.get("stack_pc1fit")
    if v_11 is not None:
        cargs.append("--stack-pc1fit")
        cargs.append(v_11)
    v_12 = params.get("stack_pc1")
    if v_12 is not None:
        cargs.append("--stack-pc1")
        cargs.append(v_12)
    v_13 = params.get("stack_spc")
    if v_13 is not None:
        cargs.append("--stack-spc")
        cargs.append(v_13)
    v_14 = params.get("stack_resid")
    if v_14 is not None:
        cargs.append("--stack-resid")
        cargs.append(v_14)
    return cargs

def long_stats_slopes_outputs(params: LongStatsSlopes, execution: Execution) -> LongStatsSlopesOutputs:
    """Build the LongStatsSlopesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LongStatsSlopesOutputs(
        root=root_v,
    )

def long_stats_slopes_execute(params: LongStatsSlopes, runner: Runner | None = None) -> LongStatsSlopesOutputs:
    """
    long_stats_slopes

    Computes slopes of statistics in a longitudinal study for each subject from processed results, outputting to designated directories for further analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_stats_slopes_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_STATS_SLOPES_METADATA)
    execution.params(params)
    args = long_stats_slopes_cargs(params, execution)
    out = long_stats_slopes_outputs(params, execution)
    execution.run(args)
    return out

def long_stats_slopes(
    qdec_table: InputPathType,
    stats_file: InputPathType,
    measure: str,
    subjects_dir: str,
    do_avg: bool = False,
    do_rate: bool = False,
    do_pc1fit: bool = False,
    do_pc1: bool = False,
    do_spc: bool = False,
    do_stack: bool = False,
    resid: float | None = None,
    time_var: str | None = None,
    generic_time: bool = False,
    cross_sectional: bool = False,
    out_avg: str | None = None,
    out_rate: str | None = None,
    out_pc1fit: str | None = None,
    out_pc1: str | None = None,
    out_spc: str | None = None,
    out_resid: str | None = None,
    out_stack: str | None = None,
    stack_avg: str | None = None,
    stack_rate: str | None = None,
    stack_pc1fit: str | None = None,
    stack_pc1: str | None = None,
    stack_spc: str | None = None,
    stack_resid: str | None = None,
    runner: Runner | None = None,
) -> LongStatsSlopesOutputs:
    """
    long_stats_slopes

    Computes slopes of statistics in a longitudinal study for each subject from processed results, outputting to designated directories for further analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec_table: QDEC table file specifying the subjects and time points.
        stats_file: The stats file, e.g. aseg.stats or lh.aparc.stats.
        measure: The stats measure (e.g. volume, thickness, mean, std).
        subjects_dir: Full path to FreeSurfer subjects directory.
        do_avg: Compute and output the temporal average.
        do_rate: Compute and output the rate.
        do_pc1fit: Compute and output the percent change with respect to tp1 from\
            linear fit.
        do_pc1: Compute and output the percent change with respect to tp1.
        do_spc: Compute and output the symmetric percent change with respect to\
            temporal average.
        do_stack: Compute and output tables showing the time series (row per time\
            point).
        resid: Residual timepoint to export (pass 1 for tp1, etc., pass 0 for\
            average).
        time_var: Variable name for time column variable (e.g., age) in QDEC table.
        generic_time: Time points are ordered in QDEC file; assume time=1,2,3...
        cross_sectional: Use cross-sectional results (for testing only).
        out_avg: Filename to store temporal average in <template>/stats/<OUT_AVG>.
        out_rate: Filename to store rate in <template>/stats/<OUT_RATE>.
        out_pc1fit: Filename to store percent change to tp1fit in\
            <template>/stats/<OUT_PC1FIT>.
        out_pc1: Filename to store percent change to tp1 in\
            <template>/stats/<OUT_PC1>.
        out_spc: Filename to store symmetric percent change in\
            <template>/stats/<OUT_SPC>.
        out_resid: Filename to store residual in <template>/stats/<OUT_RESID>.
        out_stack: Filename to store stacked measure file in\
            <template>/stats/<OUT_STACK>.
        stack_avg: Full filename to stack temporal average tables.
        stack_rate: Full filename to stack rate tables.
        stack_pc1fit: Full filename to stack percent change to tp1fit tables.
        stack_pc1: Full filename to stack percent change to tp1 tables.
        stack_spc: Full filename to stack symmetric percent change tables.
        stack_resid: Full filename to stack residual tables.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_stats_slopes_params(
        qdec_table=qdec_table,
        stats_file=stats_file,
        measure=measure,
        subjects_dir=subjects_dir,
        do_avg=do_avg,
        do_rate=do_rate,
        do_pc1fit=do_pc1fit,
        do_pc1=do_pc1,
        do_spc=do_spc,
        do_stack=do_stack,
        resid=resid,
        time_var=time_var,
        generic_time=generic_time,
        cross_sectional=cross_sectional,
        out_avg=out_avg,
        out_rate=out_rate,
        out_pc1fit=out_pc1fit,
        out_pc1=out_pc1,
        out_spc=out_spc,
        out_resid=out_resid,
        out_stack=out_stack,
        stack_avg=stack_avg,
        stack_rate=stack_rate,
        stack_pc1fit=stack_pc1fit,
        stack_pc1=stack_pc1,
        stack_spc=stack_spc,
        stack_resid=stack_resid,
    )
    return long_stats_slopes_execute(params, runner)

__all__ = [
    "LongStatsSlopes",
    "LongStatsSlopesOutputs",
    "LONG_STATS_SLOPES_METADATA",
    "long_stats_slopes_cargs",
    "long_stats_slopes_outputs",
    "long_stats_slopes_params",
    "long_stats_slopes_execute",
    "long_stats_slopes_validate",
    "long_stats_slopes",
]