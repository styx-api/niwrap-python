# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCMDRLE_FS_METADATA = Metadata(
    id="dcmdrle.fs",
    name="dcmdrle.fs",
    package="freesurfer",
)

DcmdrleFs = typing.TypedDict(
    "DcmdrleFs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dcmdrle.fs"]],
        "input_file": InputPathType,
        "output_file": str,
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "arguments": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "log_level": typing.NotRequired[str],
        "log_config": typing.NotRequired[InputPathType],
        "read_file": typing.NotRequired[bool],
        "read_file_only": typing.NotRequired[bool],
        "read_dataset": typing.NotRequired[bool],
        "uid_default": typing.NotRequired[bool],
        "uid_always": typing.NotRequired[bool],
        "byte_order_default": typing.NotRequired[bool],
        "byte_order_reverse": typing.NotRequired[bool],
        "write_file": typing.NotRequired[bool],
        "write_dataset": typing.NotRequired[bool],
        "write_xfer_little": typing.NotRequired[bool],
        "write_xfer_big": typing.NotRequired[bool],
        "write_xfer_implicit": typing.NotRequired[bool],
        "enable_new_vr": typing.NotRequired[bool],
        "disable_new_vr": typing.NotRequired[bool],
        "group_length_recalc": typing.NotRequired[bool],
        "group_length_create": typing.NotRequired[bool],
        "group_length_remove": typing.NotRequired[bool],
        "length_explicit": typing.NotRequired[bool],
        "length_undefined": typing.NotRequired[bool],
        "padding_retain": typing.NotRequired[bool],
        "padding_off": typing.NotRequired[bool],
        "padding_create": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class DcmdrleFsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output DICOM file."""

def dcmdrle_fs_params(
    input_file: InputPathType,
    output_file: str,
    help: bool = False,
    version: bool = False,
    arguments: bool = False,
    quiet: bool = False,
    verbose: bool = False,
    debug: bool = False,
    log_level: str | None = None,
    log_config: InputPathType | None = None,
    read_file: bool = False,
    read_file_only: bool = False,
    read_dataset: bool = False,
    uid_default: bool = False,
    uid_always: bool = False,
    byte_order_default: bool = False,
    byte_order_reverse: bool = False,
    write_file: bool = False,
    write_dataset: bool = False,
    write_xfer_little: bool = False,
    write_xfer_big: bool = False,
    write_xfer_implicit: bool = False,
    enable_new_vr: bool = False,
    disable_new_vr: bool = False,
    group_length_recalc: bool = False,
    group_length_create: bool = False,
    group_length_remove: bool = False,
    length_explicit: bool = False,
    length_undefined: bool = False,
    padding_retain: bool = False,
    padding_off: bool = False,
    padding_create: str | None = None,
) -> DcmdrleFs:
    """
    Build parameters.

    Args:
        input_file: DICOM input filename to be converted.
        output_file: DICOM output filename.
        help: Print help text and exit.
        version: Print version information and exit.
        arguments: Print expanded command line arguments.
        quiet: Quiet mode, print no warnings and errors.
        verbose: Verbose mode, print processing details.
        debug: Debug mode, print debug information.
        log_level: Set logger level.
        log_config: Use config file for the logger.
        read_file: Read file format or data set (default).
        read_file_only: Read file format only.
        read_dataset: Read data set without file meta information.
        uid_default: Keep same SOP Instance UID (default).
        uid_always: Always assign new UID.
        byte_order_default: Most significant byte first (default).
        byte_order_reverse: Least significant byte first.
        write_file: Write file format (default).
        write_dataset: Write data set without file meta information.
        write_xfer_little: Write with explicit VR little endian (default).
        write_xfer_big: Write with explicit VR big endian TS.
        write_xfer_implicit: Write with implicit VR little endian TS.
        enable_new_vr: Enable support for new VRs (UN/UT) (default).
        disable_new_vr: Disable support for new VRs, convert to OB.
        group_length_recalc: Recalculate group lengths if present (default).
        group_length_create: Always write with group length elements.
        group_length_remove: Always write without group length elements.
        length_explicit: Write with explicit lengths (default).
        length_undefined: Write with undefined lengths.
        padding_retain: Do not change padding.
        padding_off: No padding (implicit if --write-dataset).
        padding_create: Align file on multiple of file-pad bytes and items on\
            multiple of item-pad bytes.

    Returns:
        Parameter dictionary.
    """
    params: DcmdrleFs = {
        "@type": "freesurfer/dcmdrle.fs",
        "input_file": input_file,
        "output_file": output_file,
        "help": help,
        "version": version,
        "arguments": arguments,
        "quiet": quiet,
        "verbose": verbose,
        "debug": debug,
        "read_file": read_file,
        "read_file_only": read_file_only,
        "read_dataset": read_dataset,
        "uid_default": uid_default,
        "uid_always": uid_always,
        "byte_order_default": byte_order_default,
        "byte_order_reverse": byte_order_reverse,
        "write_file": write_file,
        "write_dataset": write_dataset,
        "write_xfer_little": write_xfer_little,
        "write_xfer_big": write_xfer_big,
        "write_xfer_implicit": write_xfer_implicit,
        "enable_new_vr": enable_new_vr,
        "disable_new_vr": disable_new_vr,
        "group_length_recalc": group_length_recalc,
        "group_length_create": group_length_create,
        "group_length_remove": group_length_remove,
        "length_explicit": length_explicit,
        "length_undefined": length_undefined,
        "padding_retain": padding_retain,
        "padding_off": padding_off,
    }
    if log_level is not None:
        params["log_level"] = log_level
    if log_config is not None:
        params["log_config"] = log_config
    if padding_create is not None:
        params["padding_create"] = padding_create
    return params

def dcmdrle_fs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DcmdrleFs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("arguments", None) is not None:
        if not isinstance(params["arguments"], bool):
            raise StyxValidationError(f'`arguments` has the wrong type: Received `{type(params["arguments"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("log_level", None) is not None:
        if not isinstance(params["log_level"], str):
            raise StyxValidationError(f'`log_level` has the wrong type: Received `{type(params["log_level"])}` expected `str`')
    if params.get("log_config", None) is not None:
        if not isinstance(params["log_config"], (pathlib.Path, str)):
            raise StyxValidationError(f'`log_config` has the wrong type: Received `{type(params["log_config"])}` expected `InputPathType`')
    if params.get("read_file", None) is not None:
        if not isinstance(params["read_file"], bool):
            raise StyxValidationError(f'`read_file` has the wrong type: Received `{type(params["read_file"])}` expected `bool`')
    if params.get("read_file_only", None) is not None:
        if not isinstance(params["read_file_only"], bool):
            raise StyxValidationError(f'`read_file_only` has the wrong type: Received `{type(params["read_file_only"])}` expected `bool`')
    if params.get("read_dataset", None) is not None:
        if not isinstance(params["read_dataset"], bool):
            raise StyxValidationError(f'`read_dataset` has the wrong type: Received `{type(params["read_dataset"])}` expected `bool`')
    if params.get("uid_default", None) is not None:
        if not isinstance(params["uid_default"], bool):
            raise StyxValidationError(f'`uid_default` has the wrong type: Received `{type(params["uid_default"])}` expected `bool`')
    if params.get("uid_always", None) is not None:
        if not isinstance(params["uid_always"], bool):
            raise StyxValidationError(f'`uid_always` has the wrong type: Received `{type(params["uid_always"])}` expected `bool`')
    if params.get("byte_order_default", None) is not None:
        if not isinstance(params["byte_order_default"], bool):
            raise StyxValidationError(f'`byte_order_default` has the wrong type: Received `{type(params["byte_order_default"])}` expected `bool`')
    if params.get("byte_order_reverse", None) is not None:
        if not isinstance(params["byte_order_reverse"], bool):
            raise StyxValidationError(f'`byte_order_reverse` has the wrong type: Received `{type(params["byte_order_reverse"])}` expected `bool`')
    if params.get("write_file", None) is not None:
        if not isinstance(params["write_file"], bool):
            raise StyxValidationError(f'`write_file` has the wrong type: Received `{type(params["write_file"])}` expected `bool`')
    if params.get("write_dataset", None) is not None:
        if not isinstance(params["write_dataset"], bool):
            raise StyxValidationError(f'`write_dataset` has the wrong type: Received `{type(params["write_dataset"])}` expected `bool`')
    if params.get("write_xfer_little", None) is not None:
        if not isinstance(params["write_xfer_little"], bool):
            raise StyxValidationError(f'`write_xfer_little` has the wrong type: Received `{type(params["write_xfer_little"])}` expected `bool`')
    if params.get("write_xfer_big", None) is not None:
        if not isinstance(params["write_xfer_big"], bool):
            raise StyxValidationError(f'`write_xfer_big` has the wrong type: Received `{type(params["write_xfer_big"])}` expected `bool`')
    if params.get("write_xfer_implicit", None) is not None:
        if not isinstance(params["write_xfer_implicit"], bool):
            raise StyxValidationError(f'`write_xfer_implicit` has the wrong type: Received `{type(params["write_xfer_implicit"])}` expected `bool`')
    if params.get("enable_new_vr", None) is not None:
        if not isinstance(params["enable_new_vr"], bool):
            raise StyxValidationError(f'`enable_new_vr` has the wrong type: Received `{type(params["enable_new_vr"])}` expected `bool`')
    if params.get("disable_new_vr", None) is not None:
        if not isinstance(params["disable_new_vr"], bool):
            raise StyxValidationError(f'`disable_new_vr` has the wrong type: Received `{type(params["disable_new_vr"])}` expected `bool`')
    if params.get("group_length_recalc", None) is not None:
        if not isinstance(params["group_length_recalc"], bool):
            raise StyxValidationError(f'`group_length_recalc` has the wrong type: Received `{type(params["group_length_recalc"])}` expected `bool`')
    if params.get("group_length_create", None) is not None:
        if not isinstance(params["group_length_create"], bool):
            raise StyxValidationError(f'`group_length_create` has the wrong type: Received `{type(params["group_length_create"])}` expected `bool`')
    if params.get("group_length_remove", None) is not None:
        if not isinstance(params["group_length_remove"], bool):
            raise StyxValidationError(f'`group_length_remove` has the wrong type: Received `{type(params["group_length_remove"])}` expected `bool`')
    if params.get("length_explicit", None) is not None:
        if not isinstance(params["length_explicit"], bool):
            raise StyxValidationError(f'`length_explicit` has the wrong type: Received `{type(params["length_explicit"])}` expected `bool`')
    if params.get("length_undefined", None) is not None:
        if not isinstance(params["length_undefined"], bool):
            raise StyxValidationError(f'`length_undefined` has the wrong type: Received `{type(params["length_undefined"])}` expected `bool`')
    if params.get("padding_retain", None) is not None:
        if not isinstance(params["padding_retain"], bool):
            raise StyxValidationError(f'`padding_retain` has the wrong type: Received `{type(params["padding_retain"])}` expected `bool`')
    if params.get("padding_off", None) is not None:
        if not isinstance(params["padding_off"], bool):
            raise StyxValidationError(f'`padding_off` has the wrong type: Received `{type(params["padding_off"])}` expected `bool`')
    if params.get("padding_create", None) is not None:
        if not isinstance(params["padding_create"], str):
            raise StyxValidationError(f'`padding_create` has the wrong type: Received `{type(params["padding_create"])}` expected `str`')

def dcmdrle_fs_cargs(params: DcmdrleFs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcmdrle.fs")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    if params.get("help"):
        cargs.append("-h")
    if params.get("version"):
        cargs.append("--version")
    if params.get("arguments"):
        cargs.append("--arguments")
    if params.get("quiet"):
        cargs.append("-q")
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("debug"):
        cargs.append("-d")
    v_0 = params.get("log_level")
    if v_0 is not None:
        cargs.append("-ll")
        cargs.append(v_0)
    v_1 = params.get("log_config")
    if v_1 is not None:
        cargs.append("-lc")
        cargs.append(execution.input_file(v_1))
    if params.get("read_file"):
        cargs.append("+f")
    if params.get("read_file_only"):
        cargs.append("+fo")
    if params.get("read_dataset"):
        cargs.append("-f")
    if params.get("uid_default"):
        cargs.append("+ud")
    if params.get("uid_always"):
        cargs.append("+ua")
    if params.get("byte_order_default"):
        cargs.append("+bd")
    if params.get("byte_order_reverse"):
        cargs.append("+br")
    if params.get("write_file"):
        cargs.append("+F")
    if params.get("write_dataset"):
        cargs.append("-F")
    if params.get("write_xfer_little"):
        cargs.append("+te")
    if params.get("write_xfer_big"):
        cargs.append("+tb")
    if params.get("write_xfer_implicit"):
        cargs.append("+ti")
    if params.get("enable_new_vr"):
        cargs.append("+u")
    if params.get("disable_new_vr"):
        cargs.append("-u")
    if params.get("group_length_recalc"):
        cargs.append("+g=")
    if params.get("group_length_create"):
        cargs.append("+g")
    if params.get("group_length_remove"):
        cargs.append("-g")
    if params.get("length_explicit"):
        cargs.append("+e")
    if params.get("length_undefined"):
        cargs.append("-e")
    if params.get("padding_retain"):
        cargs.append("-p=")
    if params.get("padding_off"):
        cargs.append("-p")
    v_2 = params.get("padding_create")
    if v_2 is not None:
        cargs.append("+p")
        cargs.append(v_2)
    return cargs

def dcmdrle_fs_outputs(params: DcmdrleFs, execution: Execution) -> DcmdrleFsOutputs:
    """Build the DcmdrleFsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return DcmdrleFsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def dcmdrle_fs_execute(params: DcmdrleFs, runner: Runner | None = None) -> DcmdrleFsOutputs:
    """
    dcmdrle.fs

    Decodes RLE-compressed DICOM files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcmdrle_fs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCMDRLE_FS_METADATA)
    execution.params(params)
    args = dcmdrle_fs_cargs(params, execution)
    out = dcmdrle_fs_outputs(params, execution)
    execution.run(args)
    return out

def dcmdrle_fs(
    input_file: InputPathType,
    output_file: str,
    help: bool = False,
    version: bool = False,
    arguments: bool = False,
    quiet: bool = False,
    verbose: bool = False,
    debug: bool = False,
    log_level: str | None = None,
    log_config: InputPathType | None = None,
    read_file: bool = False,
    read_file_only: bool = False,
    read_dataset: bool = False,
    uid_default: bool = False,
    uid_always: bool = False,
    byte_order_default: bool = False,
    byte_order_reverse: bool = False,
    write_file: bool = False,
    write_dataset: bool = False,
    write_xfer_little: bool = False,
    write_xfer_big: bool = False,
    write_xfer_implicit: bool = False,
    enable_new_vr: bool = False,
    disable_new_vr: bool = False,
    group_length_recalc: bool = False,
    group_length_create: bool = False,
    group_length_remove: bool = False,
    length_explicit: bool = False,
    length_undefined: bool = False,
    padding_retain: bool = False,
    padding_off: bool = False,
    padding_create: str | None = None,
    runner: Runner | None = None,
) -> DcmdrleFsOutputs:
    """
    dcmdrle.fs

    Decodes RLE-compressed DICOM files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: DICOM input filename to be converted.
        output_file: DICOM output filename.
        help: Print help text and exit.
        version: Print version information and exit.
        arguments: Print expanded command line arguments.
        quiet: Quiet mode, print no warnings and errors.
        verbose: Verbose mode, print processing details.
        debug: Debug mode, print debug information.
        log_level: Set logger level.
        log_config: Use config file for the logger.
        read_file: Read file format or data set (default).
        read_file_only: Read file format only.
        read_dataset: Read data set without file meta information.
        uid_default: Keep same SOP Instance UID (default).
        uid_always: Always assign new UID.
        byte_order_default: Most significant byte first (default).
        byte_order_reverse: Least significant byte first.
        write_file: Write file format (default).
        write_dataset: Write data set without file meta information.
        write_xfer_little: Write with explicit VR little endian (default).
        write_xfer_big: Write with explicit VR big endian TS.
        write_xfer_implicit: Write with implicit VR little endian TS.
        enable_new_vr: Enable support for new VRs (UN/UT) (default).
        disable_new_vr: Disable support for new VRs, convert to OB.
        group_length_recalc: Recalculate group lengths if present (default).
        group_length_create: Always write with group length elements.
        group_length_remove: Always write without group length elements.
        length_explicit: Write with explicit lengths (default).
        length_undefined: Write with undefined lengths.
        padding_retain: Do not change padding.
        padding_off: No padding (implicit if --write-dataset).
        padding_create: Align file on multiple of file-pad bytes and items on\
            multiple of item-pad bytes.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcmdrle_fs_params(
        input_file=input_file,
        output_file=output_file,
        help=help,
        version=version,
        arguments=arguments,
        quiet=quiet,
        verbose=verbose,
        debug=debug,
        log_level=log_level,
        log_config=log_config,
        read_file=read_file,
        read_file_only=read_file_only,
        read_dataset=read_dataset,
        uid_default=uid_default,
        uid_always=uid_always,
        byte_order_default=byte_order_default,
        byte_order_reverse=byte_order_reverse,
        write_file=write_file,
        write_dataset=write_dataset,
        write_xfer_little=write_xfer_little,
        write_xfer_big=write_xfer_big,
        write_xfer_implicit=write_xfer_implicit,
        enable_new_vr=enable_new_vr,
        disable_new_vr=disable_new_vr,
        group_length_recalc=group_length_recalc,
        group_length_create=group_length_create,
        group_length_remove=group_length_remove,
        length_explicit=length_explicit,
        length_undefined=length_undefined,
        padding_retain=padding_retain,
        padding_off=padding_off,
        padding_create=padding_create,
    )
    return dcmdrle_fs_execute(params, runner)

__all__ = [
    "DcmdrleFs",
    "DcmdrleFsOutputs",
    "DCMDRLE_FS_METADATA",
    "dcmdrle_fs_cargs",
    "dcmdrle_fs_outputs",
    "dcmdrle_fs_params",
    "dcmdrle_fs_execute",
    "dcmdrle_fs_validate",
    "dcmdrle_fs",
]