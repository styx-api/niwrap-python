# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_SUBREGIONS_METADATA = Metadata(
    id="segment_subregions",
    name="segment_subregions",
    package="freesurfer",
)

SegmentSubregions = typing.TypedDict(
    "SegmentSubregions",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segment_subregions"]],
        "structure": str,
        "cross": typing.NotRequired[str],
        "long_base": typing.NotRequired[str],
        "sd": typing.NotRequired[str],
        "suffix": typing.NotRequired[str],
        "temp_dir": typing.NotRequired[str],
        "out_dir": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SegmentSubregionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def segment_subregions_params(
    structure: str,
    cross: str | None = None,
    long_base: str | None = None,
    sd: str | None = None,
    suffix: str | None = None,
    temp_dir: str | None = None,
    out_dir: str | None = None,
    debug: bool = False,
    threads: float | None = None,
) -> SegmentSubregions:
    """
    Build parameters.

    Args:
        structure: Structure to segment. Options are: thalamus, brainstem,\
            hippo-amygdala.
        cross: Subject to segment in cross-sectional analysis.
        long_base: Base subject for longitudinal analysis. Timepoints are extracted\
            from the base-tps file.
        sd: Specify subjects directory (will override SUBJECTS_DIR env variable).
        suffix: Optional output file suffix.
        temp_dir: Use alternative temporary directory. This will get deleted unless\
            --debug is enabled.
        out_dir: Use alternative output directory (only for cross-sectional).\
            Default is the subject's `mri` directory.
        debug: Write intermediate debugging outputs.
        threads: Number of threads to use. Defaults to 1.

    Returns:
        Parameter dictionary.
    """
    params: SegmentSubregions = {
        "@type": "freesurfer/segment_subregions",
        "structure": structure,
        "debug": debug,
    }
    if cross is not None:
        params["cross"] = cross
    if long_base is not None:
        params["long_base"] = long_base
    if sd is not None:
        params["sd"] = sd
    if suffix is not None:
        params["suffix"] = suffix
    if temp_dir is not None:
        params["temp_dir"] = temp_dir
    if out_dir is not None:
        params["out_dir"] = out_dir
    if threads is not None:
        params["threads"] = threads
    return params

def segment_subregions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentSubregions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("structure", None) is None:
        raise StyxValidationError("`structure` must not be None")
    if not isinstance(params["structure"], str):
        raise StyxValidationError(f'`structure` has the wrong type: Received `{type(params["structure"])}` expected `str`')
    if params.get("cross", None) is not None:
        if not isinstance(params["cross"], str):
            raise StyxValidationError(f'`cross` has the wrong type: Received `{type(params["cross"])}` expected `str`')
    if params.get("long_base", None) is not None:
        if not isinstance(params["long_base"], str):
            raise StyxValidationError(f'`long_base` has the wrong type: Received `{type(params["long_base"])}` expected `str`')
    if params.get("sd", None) is not None:
        if not isinstance(params["sd"], str):
            raise StyxValidationError(f'`sd` has the wrong type: Received `{type(params["sd"])}` expected `str`')
    if params.get("suffix", None) is not None:
        if not isinstance(params["suffix"], str):
            raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')
    if params.get("temp_dir", None) is not None:
        if not isinstance(params["temp_dir"], str):
            raise StyxValidationError(f'`temp_dir` has the wrong type: Received `{type(params["temp_dir"])}` expected `str`')
    if params.get("out_dir", None) is not None:
        if not isinstance(params["out_dir"], str):
            raise StyxValidationError(f'`out_dir` has the wrong type: Received `{type(params["out_dir"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def segment_subregions_cargs(params: SegmentSubregions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segment_subregions")
    cargs.append(params["structure"])
    v_0 = params.get("cross")
    if v_0 is not None:
        cargs.append("--cross")
        cargs.append(v_0)
    v_1 = params.get("long_base")
    if v_1 is not None:
        cargs.append("--long-base")
        cargs.append(v_1)
    v_2 = params.get("sd")
    if v_2 is not None:
        cargs.append("--sd")
        cargs.append(v_2)
    v_3 = params.get("suffix")
    if v_3 is not None:
        cargs.append("--suffix")
        cargs.append(v_3)
    v_4 = params.get("temp_dir")
    if v_4 is not None:
        cargs.append("--temp-dir")
        cargs.append(v_4)
    v_5 = params.get("out_dir")
    if v_5 is not None:
        cargs.append("--out-dir")
        cargs.append(v_5)
    if params.get("debug"):
        cargs.append("--debug")
    v_6 = params.get("threads")
    if v_6 is not None:
        cargs.append("--threads")
        cargs.append(str(v_6))
    return cargs

def segment_subregions_outputs(params: SegmentSubregions, execution: Execution) -> SegmentSubregionsOutputs:
    """Build the SegmentSubregionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SegmentSubregionsOutputs(
        root=root_v,
    )

def segment_subregions_execute(params: SegmentSubregions, runner: Runner | None = None) -> SegmentSubregionsOutputs:
    """
    segment_subregions

    Cross-sectional and longitudinal segmentation for brain structures like thalamus, brainstem, and hippo-amygdala.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_subregions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_SUBREGIONS_METADATA)
    execution.params(params)
    args = segment_subregions_cargs(params, execution)
    out = segment_subregions_outputs(params, execution)
    execution.run(args)
    return out

def segment_subregions(
    structure: str,
    cross: str | None = None,
    long_base: str | None = None,
    sd: str | None = None,
    suffix: str | None = None,
    temp_dir: str | None = None,
    out_dir: str | None = None,
    debug: bool = False,
    threads: float | None = None,
    runner: Runner | None = None,
) -> SegmentSubregionsOutputs:
    """
    segment_subregions

    Cross-sectional and longitudinal segmentation for brain structures like thalamus, brainstem, and hippo-amygdala.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        structure: Structure to segment. Options are: thalamus, brainstem,\
            hippo-amygdala.
        cross: Subject to segment in cross-sectional analysis.
        long_base: Base subject for longitudinal analysis. Timepoints are extracted\
            from the base-tps file.
        sd: Specify subjects directory (will override SUBJECTS_DIR env variable).
        suffix: Optional output file suffix.
        temp_dir: Use alternative temporary directory. This will get deleted unless\
            --debug is enabled.
        out_dir: Use alternative output directory (only for cross-sectional).\
            Default is the subject's `mri` directory.
        debug: Write intermediate debugging outputs.
        threads: Number of threads to use. Defaults to 1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_subregions_params(
        structure=structure,
        cross=cross,
        long_base=long_base,
        sd=sd,
        suffix=suffix,
        temp_dir=temp_dir,
        out_dir=out_dir,
        debug=debug,
        threads=threads,
    )
    return segment_subregions_execute(params, runner)

__all__ = [
    "SegmentSubregions",
    "SegmentSubregionsOutputs",
    "SEGMENT_SUBREGIONS_METADATA",
    "segment_subregions_cargs",
    "segment_subregions_outputs",
    "segment_subregions_params",
    "segment_subregions_execute",
    "segment_subregions_validate",
    "segment_subregions",
]