# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_RF_TRAIN_METADATA = Metadata(
    id="mris_rf_train",
    name="mris_rf_train",
    package="freesurfer",
)

MrisRfTrain = typing.TypedDict(
    "MrisRfTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_rf_train"]],
        "subjects": list[str],
        "output_name": str,
        "hemi": typing.NotRequired[str],
        "surf": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisRfTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_rf_train_params(
    subjects: list[str],
    output_name: str,
    hemi: str | None = None,
    surf: str | None = None,
) -> MrisRfTrain:
    """
    Build parameters.

    Args:
        subjects: List of subjects to process
        output_name: Output name for the trained model
        hemi: Process specified hemisphere instead of the default 'lh'.
        surf: Change the default surface name from 'white' to the specified\
            surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisRfTrain = {
        "@type": "freesurfer/mris_rf_train",
        "subjects": subjects,
        "output_name": output_name,
    }
    if hemi is not None:
        params["hemi"] = hemi
    if surf is not None:
        params["surf"] = surf
    return params

def mris_rf_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRfTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')

def mris_rf_train_cargs(params: MrisRfTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_rf_train")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_name"])
    v_0 = params.get("hemi")
    if v_0 is not None:
        cargs.append("--hemi")
        cargs.append(v_0)
    v_1 = params.get("surf")
    if v_1 is not None:
        cargs.append("--surf")
        cargs.append(v_1)
    return cargs

def mris_rf_train_outputs(params: MrisRfTrain, execution: Execution) -> MrisRfTrainOutputs:
    """Build the MrisRfTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisRfTrainOutputs(
        root=root_v,
    )

def mris_rf_train_execute(params: MrisRfTrain, runner: Runner | None = None) -> MrisRfTrainOutputs:
    """
    mris_rf_train

    Tool for training a random forest classifier using MRIS surface data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_rf_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_RF_TRAIN_METADATA)
    execution.params(params)
    args = mris_rf_train_cargs(params, execution)
    out = mris_rf_train_outputs(params, execution)
    execution.run(args)
    return out

def mris_rf_train(
    subjects: list[str],
    output_name: str,
    hemi: str | None = None,
    surf: str | None = None,
    runner: Runner | None = None,
) -> MrisRfTrainOutputs:
    """
    mris_rf_train

    Tool for training a random forest classifier using MRIS surface data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subjects to process
        output_name: Output name for the trained model
        hemi: Process specified hemisphere instead of the default 'lh'.
        surf: Change the default surface name from 'white' to the specified\
            surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_rf_train_params(
        subjects=subjects,
        output_name=output_name,
        hemi=hemi,
        surf=surf,
    )
    return mris_rf_train_execute(params, runner)

__all__ = [
    "MrisRfTrain",
    "MrisRfTrainOutputs",
    "MRIS_RF_TRAIN_METADATA",
    "mris_rf_train_cargs",
    "mris_rf_train_outputs",
    "mris_rf_train_params",
    "mris_rf_train_execute",
    "mris_rf_train_validate",
    "mris_rf_train",
]