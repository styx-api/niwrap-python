# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_REMESH_METADATA = Metadata(
    id="mris_remesh",
    name="mris_remesh",
    package="freesurfer",
)

MrisRemesh = typing.TypedDict(
    "MrisRemesh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_remesh"]],
        "input": InputPathType,
        "output": str,
        "edge_length": typing.NotRequired[float],
        "num_vertices": typing.NotRequired[float],
        "face_area": typing.NotRequired[float],
        "remesh": typing.NotRequired[bool],
        "iterations": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisRemeshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    remeshed_output: OutputPathType
    """Remeshed output surface file"""

def mris_remesh_params(
    input_: InputPathType,
    output: str,
    edge_length: float | None = None,
    num_vertices: float | None = None,
    face_area: float | None = None,
    remesh: bool = False,
    iterations: float | None = None,
) -> MrisRemesh:
    """
    Build parameters.

    Args:
        input_: Input surface file
        output: Output surface file
        edge_length: Target average edge length in mm for remeshed surface
        num_vertices: Target number of vertices for remeshed surface
        face_area: Desired average face area in mm²
        remesh: Improve the quality while only reducing vertices by a small amount
        iterations: Number of remeshing iterations (default is 5)

    Returns:
        Parameter dictionary.
    """
    params: MrisRemesh = {
        "@type": "freesurfer/mris_remesh",
        "input": input_,
        "output": output,
        "remesh": remesh,
    }
    if edge_length is not None:
        params["edge_length"] = edge_length
    if num_vertices is not None:
        params["num_vertices"] = num_vertices
    if face_area is not None:
        params["face_area"] = face_area
    if iterations is not None:
        params["iterations"] = iterations
    return params

def mris_remesh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRemesh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("edge_length", None) is not None:
        if not isinstance(params["edge_length"], (float, int)):
            raise StyxValidationError(f'`edge_length` has the wrong type: Received `{type(params["edge_length"])}` expected `float`')
    if params.get("num_vertices", None) is not None:
        if not isinstance(params["num_vertices"], (float, int)):
            raise StyxValidationError(f'`num_vertices` has the wrong type: Received `{type(params["num_vertices"])}` expected `float`')
    if params.get("face_area", None) is not None:
        if not isinstance(params["face_area"], (float, int)):
            raise StyxValidationError(f'`face_area` has the wrong type: Received `{type(params["face_area"])}` expected `float`')
    if params.get("remesh", None) is not None:
        if not isinstance(params["remesh"], bool):
            raise StyxValidationError(f'`remesh` has the wrong type: Received `{type(params["remesh"])}` expected `bool`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], (float, int)):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `float`')

def mris_remesh_cargs(params: MrisRemesh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_remesh")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input"]))
    cargs.append("-o")
    cargs.append(params["output"])
    v_0 = params.get("edge_length")
    if v_0 is not None:
        cargs.append("--edge-len")
        cargs.append(str(v_0))
    v_1 = params.get("num_vertices")
    if v_1 is not None:
        cargs.append("--nvert")
        cargs.append(str(v_1))
    v_2 = params.get("face_area")
    if v_2 is not None:
        cargs.append("--desired-face-area")
        cargs.append(str(v_2))
    if params.get("remesh"):
        cargs.append("--remesh")
    v_3 = params.get("iterations")
    if v_3 is not None:
        cargs.append("--iters")
        cargs.append(str(v_3))
    return cargs

def mris_remesh_outputs(params: MrisRemesh, execution: Execution) -> MrisRemeshOutputs:
    """Build the MrisRemeshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    remeshed_output_v: OutputPathType = execution.output_file(params["output"])
    return MrisRemeshOutputs(
        root=root_v,
        remeshed_output=remeshed_output_v,
    )

def mris_remesh_execute(params: MrisRemesh, runner: Runner | None = None) -> MrisRemeshOutputs:
    """
    mris_remesh

    Remeshes a surface to a desired edge length, number of vertices, or average face area.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_remesh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_REMESH_METADATA)
    execution.params(params)
    args = mris_remesh_cargs(params, execution)
    out = mris_remesh_outputs(params, execution)
    execution.run(args)
    return out

def mris_remesh(
    input_: InputPathType,
    output: str,
    edge_length: float | None = None,
    num_vertices: float | None = None,
    face_area: float | None = None,
    remesh: bool = False,
    iterations: float | None = None,
    runner: Runner | None = None,
) -> MrisRemeshOutputs:
    """
    mris_remesh

    Remeshes a surface to a desired edge length, number of vertices, or average face area.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_: Input surface file
        output: Output surface file
        edge_length: Target average edge length in mm for remeshed surface
        num_vertices: Target number of vertices for remeshed surface
        face_area: Desired average face area in mm²
        remesh: Improve the quality while only reducing vertices by a small amount
        iterations: Number of remeshing iterations (default is 5)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_remesh_params(
        input_=input_,
        output=output,
        edge_length=edge_length,
        num_vertices=num_vertices,
        face_area=face_area,
        remesh=remesh,
        iterations=iterations,
    )
    return mris_remesh_execute(params, runner)

__all__ = [
    "MrisRemesh",
    "MrisRemeshOutputs",
    "MRIS_REMESH_METADATA",
    "mris_remesh_cargs",
    "mris_remesh_outputs",
    "mris_remesh_params",
    "mris_remesh_execute",
    "mris_remesh_validate",
    "mris_remesh",
]