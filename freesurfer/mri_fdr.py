# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FDR_METADATA = Metadata(
    id="mri_fdr",
    name="mri_fdr",
    package="freesurfer",
)

MriFdr = typing.TypedDict(
    "MriFdr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_fdr"]],
        "input_files": list[str],
        "fdr_value": float,
        "default_frame": typing.NotRequired[int],
        "positive_only": typing.NotRequired[bool],
        "negative_only": typing.NotRequired[bool],
        "all_voxels": typing.NotRequired[bool],
        "raw_p_values": typing.NotRequired[bool],
        "threshold_file": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "check_options": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriFdrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_fdr_params(
    input_files: list[str],
    fdr_value: float,
    default_frame: int | None = None,
    positive_only: bool = False,
    negative_only: bool = False,
    all_voxels: bool = False,
    raw_p_values: bool = False,
    threshold_file: str | None = None,
    debug: bool = False,
    check_options: bool = False,
) -> MriFdr:
    """
    Build parameters.

    Args:
        input_files: Input source volume or surface overlay. Specify mask, output,\
            and frame as needed.
        fdr_value: FDR value between 0 and 1, typically .05
        default_frame: Use input frame when not specifying frame in --i
        positive_only: Only consider positive voxels
        negative_only: Only consider negative voxels
        all_voxels: Consider all voxels regardless of sign (default)
        raw_p_values: Input is raw p-values, not -log10(p)
        threshold_file: Write threshold to text file
        debug: Turn on debugging
        check_options: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: MriFdr = {
        "@type": "freesurfer/mri_fdr",
        "input_files": input_files,
        "fdr_value": fdr_value,
        "positive_only": positive_only,
        "negative_only": negative_only,
        "all_voxels": all_voxels,
        "raw_p_values": raw_p_values,
        "debug": debug,
        "check_options": check_options,
    }
    if default_frame is not None:
        params["default_frame"] = default_frame
    if threshold_file is not None:
        params["threshold_file"] = threshold_file
    return params

def mri_fdr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFdr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[str]`')
    if len(params["input_files"]) < 1:
        raise StyxValidationError("Parameter `input_files` must contain at least 1 element")
    for e in params["input_files"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("fdr_value", None) is None:
        raise StyxValidationError("`fdr_value` must not be None")
    if not isinstance(params["fdr_value"], (float, int)):
        raise StyxValidationError(f'`fdr_value` has the wrong type: Received `{type(params["fdr_value"])}` expected `float`')
    if not (0 <= params["fdr_value"] <= 1):
        raise StyxValidationError("Parameter `fdr_value` must be between 0 and 1 (inclusive)")
    if params.get("default_frame", None) is not None:
        if not isinstance(params["default_frame"], int):
            raise StyxValidationError(f'`default_frame` has the wrong type: Received `{type(params["default_frame"])}` expected `int`')
    if params.get("positive_only", None) is not None:
        if not isinstance(params["positive_only"], bool):
            raise StyxValidationError(f'`positive_only` has the wrong type: Received `{type(params["positive_only"])}` expected `bool`')
    if params.get("negative_only", None) is not None:
        if not isinstance(params["negative_only"], bool):
            raise StyxValidationError(f'`negative_only` has the wrong type: Received `{type(params["negative_only"])}` expected `bool`')
    if params.get("all_voxels", None) is not None:
        if not isinstance(params["all_voxels"], bool):
            raise StyxValidationError(f'`all_voxels` has the wrong type: Received `{type(params["all_voxels"])}` expected `bool`')
    if params.get("raw_p_values", None) is not None:
        if not isinstance(params["raw_p_values"], bool):
            raise StyxValidationError(f'`raw_p_values` has the wrong type: Received `{type(params["raw_p_values"])}` expected `bool`')
    if params.get("threshold_file", None) is not None:
        if not isinstance(params["threshold_file"], str):
            raise StyxValidationError(f'`threshold_file` has the wrong type: Received `{type(params["threshold_file"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_options", None) is not None:
        if not isinstance(params["check_options"], bool):
            raise StyxValidationError(f'`check_options` has the wrong type: Received `{type(params["check_options"])}` expected `bool`')

def mri_fdr_cargs(params: MriFdr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_fdr")
    cargs.append("--i")
    for item0 in params["input_files"]:
        cargs.append(item0)
    cargs.append("--fdr")
    cargs.append(str(params["fdr_value"]))
    v_0 = params.get("default_frame")
    if v_0 is not None:
        cargs.append("--f")
        cargs.append(str(v_0))
    if params.get("positive_only"):
        cargs.append("--pos")
    if params.get("negative_only"):
        cargs.append("--neg")
    if params.get("all_voxels"):
        cargs.append("--abs")
    if params.get("raw_p_values"):
        cargs.append("--no-log10p")
    v_1 = params.get("threshold_file")
    if v_1 is not None:
        cargs.append("--thfile")
        cargs.append(v_1)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_options"):
        cargs.append("--checkopts")
    return cargs

def mri_fdr_outputs(params: MriFdr, execution: Execution) -> MriFdrOutputs:
    """Build the MriFdrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriFdrOutputs(
        root=root_v,
    )

def mri_fdr_execute(params: MriFdr, runner: Runner | None = None) -> MriFdrOutputs:
    """
    mri_fdr

    A program that performs False Discovery Rate correction.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_fdr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FDR_METADATA)
    execution.params(params)
    args = mri_fdr_cargs(params, execution)
    out = mri_fdr_outputs(params, execution)
    execution.run(args)
    return out

def mri_fdr(
    input_files: list[str],
    fdr_value: float,
    default_frame: int | None = None,
    positive_only: bool = False,
    negative_only: bool = False,
    all_voxels: bool = False,
    raw_p_values: bool = False,
    threshold_file: str | None = None,
    debug: bool = False,
    check_options: bool = False,
    runner: Runner | None = None,
) -> MriFdrOutputs:
    """
    mri_fdr

    A program that performs False Discovery Rate correction.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input source volume or surface overlay. Specify mask, output,\
            and frame as needed.
        fdr_value: FDR value between 0 and 1, typically .05
        default_frame: Use input frame when not specifying frame in --i
        positive_only: Only consider positive voxels
        negative_only: Only consider negative voxels
        all_voxels: Consider all voxels regardless of sign (default)
        raw_p_values: Input is raw p-values, not -log10(p)
        threshold_file: Write threshold to text file
        debug: Turn on debugging
        check_options: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_fdr_params(
        input_files=input_files,
        fdr_value=fdr_value,
        default_frame=default_frame,
        positive_only=positive_only,
        negative_only=negative_only,
        all_voxels=all_voxels,
        raw_p_values=raw_p_values,
        threshold_file=threshold_file,
        debug=debug,
        check_options=check_options,
    )
    return mri_fdr_execute(params, runner)

__all__ = [
    "MriFdr",
    "MriFdrOutputs",
    "MRI_FDR_METADATA",
    "mri_fdr_cargs",
    "mri_fdr_outputs",
    "mri_fdr_params",
    "mri_fdr_execute",
    "mri_fdr_validate",
    "mri_fdr",
]