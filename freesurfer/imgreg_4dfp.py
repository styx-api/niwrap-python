# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMGREG_4DFP_METADATA = Metadata(
    id="imgreg_4dfp",
    name="imgreg_4dfp",
    package="freesurfer",
)

Imgreg4dfp = typing.TypedDict(
    "Imgreg4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/imgreg_4dfp"]],
        "target_image": InputPathType,
        "target_mask": typing.NotRequired[str],
        "source_image": InputPathType,
        "source_mask": typing.NotRequired[str],
        "t4file": str,
        "mode": str,
    },
)

@dataclasses.dataclass
class Imgreg4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def imgreg_4dfp_params(
    target_image: InputPathType,
    source_image: InputPathType,
    t4file: str,
    mode: str,
    target_mask: str = "none",
    source_mask: str = "none",
) -> Imgreg4dfp:
    """
    Build parameters.

    Args:
        target_image: Target image
        source_image: Source image
        t4file: Transformation file
        mode: Mode of operation
        target_mask: Target mask
        source_mask: Source mask

    Returns:
        Parameter dictionary.
    """
    params: Imgreg4dfp = {
        "@type": "freesurfer/imgreg_4dfp",
        "target_image": target_image,
        "source_image": source_image,
        "t4file": t4file,
        "mode": mode,
        "target_mask": target_mask,
        "source_mask": source_mask,
    }
    return params

def imgreg_4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imgreg4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("target_image", None) is None:
        raise StyxValidationError("`target_image` must not be None")
    if not isinstance(params["target_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(params["target_image"])}` expected `InputPathType`')
    if params.get("target_mask", None) is not None:
        if not isinstance(params["target_mask"], str):
            raise StyxValidationError(f'`target_mask` has the wrong type: Received `{type(params["target_mask"])}` expected `str`')
    if params.get("source_image", None) is None:
        raise StyxValidationError("`source_image` must not be None")
    if not isinstance(params["source_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_image` has the wrong type: Received `{type(params["source_image"])}` expected `InputPathType`')
    if params.get("source_mask", None) is not None:
        if not isinstance(params["source_mask"], str):
            raise StyxValidationError(f'`source_mask` has the wrong type: Received `{type(params["source_mask"])}` expected `str`')
    if params.get("t4file", None) is None:
        raise StyxValidationError("`t4file` must not be None")
    if not isinstance(params["t4file"], str):
        raise StyxValidationError(f'`t4file` has the wrong type: Received `{type(params["t4file"])}` expected `str`')
    if params.get("mode", None) is None:
        raise StyxValidationError("`mode` must not be None")
    if not isinstance(params["mode"], str):
        raise StyxValidationError(f'`mode` has the wrong type: Received `{type(params["mode"])}` expected `str`')

def imgreg_4dfp_cargs(params: Imgreg4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imgreg_4dfp")
    cargs.append(execution.input_file(params["target_image"]))
    cargs.append(params.get("target_mask", "none"))
    cargs.append(execution.input_file(params["source_image"]))
    cargs.append(params.get("source_mask", "none"))
    cargs.append(params["t4file"])
    cargs.append(params["mode"])
    return cargs

def imgreg_4dfp_outputs(params: Imgreg4dfp, execution: Execution) -> Imgreg4dfpOutputs:
    """Build the Imgreg4dfpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Imgreg4dfpOutputs(
        root=root_v,
    )

def imgreg_4dfp_execute(params: Imgreg4dfp, runner: Runner | None = None) -> Imgreg4dfpOutputs:
    """
    imgreg_4dfp

    Image registration utility using 4dfp

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imgreg_4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMGREG_4DFP_METADATA)
    execution.params(params)
    args = imgreg_4dfp_cargs(params, execution)
    out = imgreg_4dfp_outputs(params, execution)
    execution.run(args)
    return out

def imgreg_4dfp(
    target_image: InputPathType,
    source_image: InputPathType,
    t4file: str,
    mode: str,
    target_mask: str = "none",
    source_mask: str = "none",
    runner: Runner | None = None,
) -> Imgreg4dfpOutputs:
    """
    imgreg_4dfp

    Image registration utility using 4dfp

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        target_image: Target image
        source_image: Source image
        t4file: Transformation file
        mode: Mode of operation
        target_mask: Target mask
        source_mask: Source mask
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imgreg_4dfp_params(
        target_image=target_image,
        source_image=source_image,
        t4file=t4file,
        mode=mode,
        target_mask=target_mask,
        source_mask=source_mask,
    )
    return imgreg_4dfp_execute(params, runner)

__all__ = [
    "Imgreg4dfp",
    "Imgreg4dfpOutputs",
    "IMGREG_4DFP_METADATA",
    "imgreg_4dfp_cargs",
    "imgreg_4dfp_outputs",
    "imgreg_4dfp_params",
    "imgreg_4dfp_execute",
    "imgreg_4dfp_validate",
    "imgreg_4dfp",
]