# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANATOMI_CUTS_UTILS_METADATA = Metadata(
    id="anatomiCutsUtils",
    name="anatomiCutsUtils",
    package="freesurfer",
)

AnatomiCutsUtils = typing.TypedDict(
    "AnatomiCutsUtils",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/anatomiCutsUtils"]],
        "modules": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class AnatomiCutsUtilsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def anatomi_cuts_utils_params(
    modules: list[str] | None = None,
) -> AnatomiCutsUtils:
    """
    Build parameters.

    Args:
        modules: Specify the modules to import for processing. Ensure necessary\
            modules like 'graph_tools' are installed.

    Returns:
        Parameter dictionary.
    """
    params: AnatomiCutsUtils = {
        "@type": "freesurfer/anatomiCutsUtils",
    }
    if modules is not None:
        params["modules"] = modules
    return params

def anatomi_cuts_utils_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AnatomiCutsUtils."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("modules", None) is not None:
        if not isinstance(params["modules"], list):
            raise StyxValidationError(f'`modules` has the wrong type: Received `{type(params["modules"])}` expected `list[str]`')
        for e in params["modules"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`modules` has the wrong type: Received `{type(e)}` expected `list[str]`')

def anatomi_cuts_utils_cargs(params: AnatomiCutsUtils, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("anatomiCutsUtils")
    v_0 = params.get("modules")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    return cargs

def anatomi_cuts_utils_outputs(params: AnatomiCutsUtils, execution: Execution) -> AnatomiCutsUtilsOutputs:
    """Build the AnatomiCutsUtilsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AnatomiCutsUtilsOutputs(
        root=root_v,
    )

def anatomi_cuts_utils_execute(params: AnatomiCutsUtils, runner: Runner | None = None) -> AnatomiCutsUtilsOutputs:
    """
    anatomiCutsUtils

    A tool for anatomical segmentation using graph-based methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    anatomi_cuts_utils_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANATOMI_CUTS_UTILS_METADATA)
    execution.params(params)
    args = anatomi_cuts_utils_cargs(params, execution)
    out = anatomi_cuts_utils_outputs(params, execution)
    execution.run(args)
    return out

def anatomi_cuts_utils(
    modules: list[str] | None = None,
    runner: Runner | None = None,
) -> AnatomiCutsUtilsOutputs:
    """
    anatomiCutsUtils

    A tool for anatomical segmentation using graph-based methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        modules: Specify the modules to import for processing. Ensure necessary\
            modules like 'graph_tools' are installed.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = anatomi_cuts_utils_params(
        modules=modules,
    )
    return anatomi_cuts_utils_execute(params, runner)

__all__ = [
    "AnatomiCutsUtils",
    "AnatomiCutsUtilsOutputs",
    "ANATOMI_CUTS_UTILS_METADATA",
    "anatomi_cuts_utils_cargs",
    "anatomi_cuts_utils_outputs",
    "anatomi_cuts_utils_params",
    "anatomi_cuts_utils_execute",
    "anatomi_cuts_utils_validate",
    "anatomi_cuts_utils",
]