# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_ONLY_SUBJECT_RH_METADATA = Metadata(
    id="morph_only_subject-rh",
    name="morph_only_subject-rh",
    package="freesurfer",
)

MorphOnlySubjectRh = typing.TypedDict(
    "MorphOnlySubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/morph_only_subject-rh"]],
        "subject_dir": InputPathType,
    },
)

@dataclasses.dataclass
class MorphOnlySubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_files: OutputPathType
    """The output morphing files for the right hemisphere generated by the process."""

def morph_only_subject_rh_params(
    subject_dir: InputPathType,
) -> MorphOnlySubjectRh:
    """
    Build parameters.

    Args:
        subject_dir:

    Returns:
        Parameter dictionary.
    """
    params: MorphOnlySubjectRh = {
        "@type": "freesurfer/morph_only_subject-rh",
        "subject_dir": subject_dir,
    }
    return params

def morph_only_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphOnlySubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `InputPathType`')

def morph_only_subject_rh_cargs(params: MorphOnlySubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_only_subject-rh")
    cargs.append("-rh")
    cargs.append(execution.input_file(params["subject_dir"]))
    return cargs

def morph_only_subject_rh_outputs(params: MorphOnlySubjectRh, execution: Execution) -> MorphOnlySubjectRhOutputs:
    """Build the MorphOnlySubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_files_v: OutputPathType = execution.output_file(f'/usr/local/freesurfer/subjects/{params["subject_dir"]}/rh.morph')
    return MorphOnlySubjectRhOutputs(
        root=root_v,
        output_files=output_files_v,
    )

def morph_only_subject_rh_execute(params: MorphOnlySubjectRh, runner: Runner | None = None) -> MorphOnlySubjectRhOutputs:
    """
    morph_only_subject-rh

    This tool processes morph-specific operations for the right hemisphere of the brain using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_only_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_ONLY_SUBJECT_RH_METADATA)
    execution.params(params)
    args = morph_only_subject_rh_cargs(params, execution)
    out = morph_only_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def morph_only_subject_rh(
    subject_dir: InputPathType,
    runner: Runner | None = None,
) -> MorphOnlySubjectRhOutputs:
    """
    morph_only_subject-rh

    This tool processes morph-specific operations for the right hemisphere of the brain using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_only_subject_rh_params(
        subject_dir=subject_dir,
    )
    return morph_only_subject_rh_execute(params, runner)

__all__ = [
    "MorphOnlySubjectRh",
    "MorphOnlySubjectRhOutputs",
    "MORPH_ONLY_SUBJECT_RH_METADATA",
    "morph_only_subject_rh_cargs",
    "morph_only_subject_rh_outputs",
    "morph_only_subject_rh_params",
    "morph_only_subject_rh_execute",
    "morph_only_subject_rh_validate",
    "morph_only_subject_rh",
]