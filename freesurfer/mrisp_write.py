# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRISP_WRITE_METADATA = Metadata(
    id="mrisp_write",
    name="mrisp_write",
    package="freesurfer",
)

MrispWrite = typing.TypedDict(
    "MrispWrite",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mrisp_write"]],
        "input_surface": InputPathType,
        "overlay_filename": InputPathType,
        "output_name": str,
        "subjects_dir": typing.NotRequired[str],
        "coords": typing.NotRequired[str],
        "average_curvature": typing.NotRequired[float],
        "correlation_matrix": typing.NotRequired[InputPathType],
        "scale_factor": typing.NotRequired[float],
        "normalize_curvature": typing.NotRequired[bool],
        "verbose_vertex": typing.NotRequired[float],
        "write_diagnostics": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrispWriteOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """A file containing a surface-worth of per-vertex values saved in spherical coordinates."""

def mrisp_write_params(
    input_surface: InputPathType,
    overlay_filename: InputPathType,
    output_name: str,
    subjects_dir: str | None = None,
    coords: str | None = None,
    average_curvature: float | None = None,
    correlation_matrix: InputPathType | None = None,
    scale_factor: float | None = None,
    normalize_curvature: bool = False,
    verbose_vertex: float | None = None,
    write_diagnostics: bool = False,
) -> MrispWrite:
    """
    Build parameters.

    Args:
        input_surface: Full path to input surface.
        overlay_filename: Full path to the overlay to map.
        output_name: Output file name. Can be full path to a .tif or .mgz file.
        subjects_dir: Set SUBJECTS_DIR. Default: uses environment variable.
        coords: Treat overlay as a surface and write it into a 3 frame\
            parameterization.
        average_curvature: Average curvature patterns navgs times.
        correlation_matrix: Use the overlay to compute the correlation matrix\
            within the specified label.
        scale_factor: Scale factor to adjust resolution of the spherical map.
        normalize_curvature: Normalize curvature by variance.
        verbose_vertex: Invoke diagnostics for specified vertex number.
        write_diagnostics: Write some diagnostics.

    Returns:
        Parameter dictionary.
    """
    params: MrispWrite = {
        "@type": "freesurfer/mrisp_write",
        "input_surface": input_surface,
        "overlay_filename": overlay_filename,
        "output_name": output_name,
        "normalize_curvature": normalize_curvature,
        "write_diagnostics": write_diagnostics,
    }
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if coords is not None:
        params["coords"] = coords
    if average_curvature is not None:
        params["average_curvature"] = average_curvature
    if correlation_matrix is not None:
        params["correlation_matrix"] = correlation_matrix
    if scale_factor is not None:
        params["scale_factor"] = scale_factor
    if verbose_vertex is not None:
        params["verbose_vertex"] = verbose_vertex
    return params

def mrisp_write_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrispWrite."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("overlay_filename", None) is None:
        raise StyxValidationError("`overlay_filename` must not be None")
    if not isinstance(params["overlay_filename"], (pathlib.Path, str)):
        raise StyxValidationError(f'`overlay_filename` has the wrong type: Received `{type(params["overlay_filename"])}` expected `InputPathType`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("coords", None) is not None:
        if not isinstance(params["coords"], str):
            raise StyxValidationError(f'`coords` has the wrong type: Received `{type(params["coords"])}` expected `str`')
    if params.get("average_curvature", None) is not None:
        if not isinstance(params["average_curvature"], (float, int)):
            raise StyxValidationError(f'`average_curvature` has the wrong type: Received `{type(params["average_curvature"])}` expected `float`')
    if params.get("correlation_matrix", None) is not None:
        if not isinstance(params["correlation_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`correlation_matrix` has the wrong type: Received `{type(params["correlation_matrix"])}` expected `InputPathType`')
    if params.get("scale_factor", None) is not None:
        if not isinstance(params["scale_factor"], (float, int)):
            raise StyxValidationError(f'`scale_factor` has the wrong type: Received `{type(params["scale_factor"])}` expected `float`')
    if params.get("normalize_curvature", None) is not None:
        if not isinstance(params["normalize_curvature"], bool):
            raise StyxValidationError(f'`normalize_curvature` has the wrong type: Received `{type(params["normalize_curvature"])}` expected `bool`')
    if params.get("verbose_vertex", None) is not None:
        if not isinstance(params["verbose_vertex"], (float, int)):
            raise StyxValidationError(f'`verbose_vertex` has the wrong type: Received `{type(params["verbose_vertex"])}` expected `float`')
    if params.get("write_diagnostics", None) is not None:
        if not isinstance(params["write_diagnostics"], bool):
            raise StyxValidationError(f'`write_diagnostics` has the wrong type: Received `{type(params["write_diagnostics"])}` expected `bool`')

def mrisp_write_cargs(params: MrispWrite, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrisp_write")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["overlay_filename"]))
    cargs.append(params["output_name"])
    v_0 = params.get("subjects_dir")
    if v_0 is not None:
        cargs.append("-SDIR")
        cargs.append(v_0)
    v_1 = params.get("coords")
    if v_1 is not None:
        cargs.append("-coords")
        cargs.append(v_1)
    v_2 = params.get("average_curvature")
    if v_2 is not None:
        cargs.append("-A")
        cargs.append(str(v_2))
    v_3 = params.get("correlation_matrix")
    if v_3 is not None:
        cargs.append("-CORR")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("scale_factor")
    if v_4 is not None:
        cargs.append("-SCALE")
        cargs.append(str(v_4))
    if params.get("normalize_curvature"):
        cargs.append("-N")
    v_5 = params.get("verbose_vertex")
    if v_5 is not None:
        cargs.append("-V")
        cargs.append(str(v_5))
    if params.get("write_diagnostics"):
        cargs.append("-W")
    return cargs

def mrisp_write_outputs(params: MrispWrite, execution: Execution) -> MrispWriteOutputs:
    """Build the MrispWriteOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_name"])
    return MrispWriteOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mrisp_write_execute(params: MrispWrite, runner: Runner | None = None) -> MrispWriteOutputs:
    """
    mrisp_write

    This tool converts a surface overlay on a sphere into spherical coordinates.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrisp_write_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRISP_WRITE_METADATA)
    execution.params(params)
    args = mrisp_write_cargs(params, execution)
    out = mrisp_write_outputs(params, execution)
    execution.run(args)
    return out

def mrisp_write(
    input_surface: InputPathType,
    overlay_filename: InputPathType,
    output_name: str,
    subjects_dir: str | None = None,
    coords: str | None = None,
    average_curvature: float | None = None,
    correlation_matrix: InputPathType | None = None,
    scale_factor: float | None = None,
    normalize_curvature: bool = False,
    verbose_vertex: float | None = None,
    write_diagnostics: bool = False,
    runner: Runner | None = None,
) -> MrispWriteOutputs:
    """
    mrisp_write

    This tool converts a surface overlay on a sphere into spherical coordinates.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Full path to input surface.
        overlay_filename: Full path to the overlay to map.
        output_name: Output file name. Can be full path to a .tif or .mgz file.
        subjects_dir: Set SUBJECTS_DIR. Default: uses environment variable.
        coords: Treat overlay as a surface and write it into a 3 frame\
            parameterization.
        average_curvature: Average curvature patterns navgs times.
        correlation_matrix: Use the overlay to compute the correlation matrix\
            within the specified label.
        scale_factor: Scale factor to adjust resolution of the spherical map.
        normalize_curvature: Normalize curvature by variance.
        verbose_vertex: Invoke diagnostics for specified vertex number.
        write_diagnostics: Write some diagnostics.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrisp_write_params(
        input_surface=input_surface,
        overlay_filename=overlay_filename,
        output_name=output_name,
        subjects_dir=subjects_dir,
        coords=coords,
        average_curvature=average_curvature,
        correlation_matrix=correlation_matrix,
        scale_factor=scale_factor,
        normalize_curvature=normalize_curvature,
        verbose_vertex=verbose_vertex,
        write_diagnostics=write_diagnostics,
    )
    return mrisp_write_execute(params, runner)

__all__ = [
    "MrispWrite",
    "MrispWriteOutputs",
    "MRISP_WRITE_METADATA",
    "mrisp_write_cargs",
    "mrisp_write_outputs",
    "mrisp_write_params",
    "mrisp_write_execute",
    "mrisp_write_validate",
    "mrisp_write",
]