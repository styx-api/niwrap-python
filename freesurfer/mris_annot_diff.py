# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ANNOT_DIFF_METADATA = Metadata(
    id="mris_annot_diff",
    name="mris_annot_diff",
    package="freesurfer",
)

MrisAnnotDiff = typing.TypedDict(
    "MrisAnnotDiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_annot_diff"]],
        "annot1": InputPathType,
        "annot2": InputPathType,
        "diff_ctab": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisAnnotDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_annot_diff_params(
    annot1: InputPathType,
    annot2: InputPathType,
    diff_ctab: bool = False,
    verbose: bool = False,
) -> MrisAnnotDiff:
    """
    Build parameters.

    Args:
        annot1: Input .annot file 1
        annot2: Input .annot file 2
        diff_ctab: Diff colortable included in .annot
        verbose: Print details of annotation/colortable differences

    Returns:
        Parameter dictionary.
    """
    params: MrisAnnotDiff = {
        "@type": "freesurfer/mris_annot_diff",
        "annot1": annot1,
        "annot2": annot2,
        "diff_ctab": diff_ctab,
        "verbose": verbose,
    }
    return params

def mris_annot_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisAnnotDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("annot1", None) is None:
        raise StyxValidationError("`annot1` must not be None")
    if not isinstance(params["annot1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`annot1` has the wrong type: Received `{type(params["annot1"])}` expected `InputPathType`')
    if params.get("annot2", None) is None:
        raise StyxValidationError("`annot2` must not be None")
    if not isinstance(params["annot2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`annot2` has the wrong type: Received `{type(params["annot2"])}` expected `InputPathType`')
    if params.get("diff_ctab", None) is not None:
        if not isinstance(params["diff_ctab"], bool):
            raise StyxValidationError(f'`diff_ctab` has the wrong type: Received `{type(params["diff_ctab"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def mris_annot_diff_cargs(params: MrisAnnotDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_annot_diff")
    cargs.append(execution.input_file(params["annot1"]))
    cargs.append(execution.input_file(params["annot2"]))
    if params.get("diff_ctab"):
        cargs.append("--diff-ctab")
    if params.get("verbose"):
        cargs.append("--verbose")
    return cargs

def mris_annot_diff_outputs(params: MrisAnnotDiff, execution: Execution) -> MrisAnnotDiffOutputs:
    """Build the MrisAnnotDiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisAnnotDiffOutputs(
        root=root_v,
    )

def mris_annot_diff_execute(params: MrisAnnotDiff, runner: Runner | None = None) -> MrisAnnotDiffOutputs:
    """
    mris_annot_diff

    Compare two surface annotation files. The program works with .annot only.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_annot_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ANNOT_DIFF_METADATA)
    execution.params(params)
    args = mris_annot_diff_cargs(params, execution)
    out = mris_annot_diff_outputs(params, execution)
    execution.run(args)
    return out

def mris_annot_diff(
    annot1: InputPathType,
    annot2: InputPathType,
    diff_ctab: bool = False,
    verbose: bool = False,
    runner: Runner | None = None,
) -> MrisAnnotDiffOutputs:
    """
    mris_annot_diff

    Compare two surface annotation files. The program works with .annot only.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        annot1: Input .annot file 1
        annot2: Input .annot file 2
        diff_ctab: Diff colortable included in .annot
        verbose: Print details of annotation/colortable differences
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_annot_diff_params(
        annot1=annot1,
        annot2=annot2,
        diff_ctab=diff_ctab,
        verbose=verbose,
    )
    return mris_annot_diff_execute(params, runner)

__all__ = [
    "MrisAnnotDiff",
    "MrisAnnotDiffOutputs",
    "MRIS_ANNOT_DIFF_METADATA",
    "mris_annot_diff_cargs",
    "mris_annot_diff_outputs",
    "mris_annot_diff_params",
    "mris_annot_diff_execute",
    "mris_annot_diff_validate",
    "mris_annot_diff",
]