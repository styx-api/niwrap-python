# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EXVIVO_HEMI_PROC_METADATA = Metadata(
    id="exvivo-hemi-proc",
    name="exvivo-hemi-proc",
    package="freesurfer",
)

ExvivoHemiProc = typing.TypedDict(
    "ExvivoHemiProc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/exvivo-hemi-proc"]],
        "flashdir": str,
        "outdir": str,
        "subject": str,
        "left_hemi": typing.NotRequired[bool],
        "right_hemi": typing.NotRequired[bool],
        "suptent": typing.NotRequired[bool],
        "no_rotate": typing.NotRequired[bool],
        "t1thresh": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "check_only": typing.NotRequired[bool],
        "prep_only": typing.NotRequired[bool],
        "mask_only": typing.NotRequired[bool],
        "samseg_only": typing.NotRequired[bool],
        "stop_mmppsp_after": typing.NotRequired[str],
        "force": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ExvivoHemiProcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def exvivo_hemi_proc_params(
    flashdir: str,
    outdir: str,
    subject: str,
    left_hemi: bool = False,
    right_hemi: bool = False,
    suptent: bool = False,
    no_rotate: bool = False,
    t1thresh: float | None = None,
    threads: float | None = None,
    check_only: bool = False,
    prep_only: bool = False,
    mask_only: bool = False,
    samseg_only: bool = False,
    stop_mmppsp_after: str | None = None,
    force: bool = False,
) -> ExvivoHemiProc:
    """
    Build parameters.

    Args:
        flashdir: Path to the FLASH data directory.
        outdir: Output directory where results will be saved.
        subject: Full path to the subject.
        left_hemi: Process left hemisphere.
        right_hemi: Process right hemisphere.
        suptent: Indicate no tentorium (cblum and bstem) in the sample.
        no_rotate: Indicate rotation is not needed.
        t1thresh: T1 threshold, default is 415.
        threads: Number of threads to use.
        check_only: Only perform check, without further processing.
        prep_only: Only run up to manual rotation.
        mask_only: Only run up to creation of masks.
        samseg_only: Only run up to samseg.
        stop_mmppsp_after: Stop processing after a specific step\
            {tess,fix,preaparc,sphere,spherereg,white,pial}.
        force: Force the execution, overriding checks.

    Returns:
        Parameter dictionary.
    """
    params: ExvivoHemiProc = {
        "@type": "freesurfer/exvivo-hemi-proc",
        "flashdir": flashdir,
        "outdir": outdir,
        "subject": subject,
        "left_hemi": left_hemi,
        "right_hemi": right_hemi,
        "suptent": suptent,
        "no_rotate": no_rotate,
        "check_only": check_only,
        "prep_only": prep_only,
        "mask_only": mask_only,
        "samseg_only": samseg_only,
        "force": force,
    }
    if t1thresh is not None:
        params["t1thresh"] = t1thresh
    if threads is not None:
        params["threads"] = threads
    if stop_mmppsp_after is not None:
        params["stop_mmppsp_after"] = stop_mmppsp_after
    return params

def exvivo_hemi_proc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ExvivoHemiProc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("flashdir", None) is None:
        raise StyxValidationError("`flashdir` must not be None")
    if not isinstance(params["flashdir"], str):
        raise StyxValidationError(f'`flashdir` has the wrong type: Received `{type(params["flashdir"])}` expected `str`')
    if params.get("outdir", None) is None:
        raise StyxValidationError("`outdir` must not be None")
    if not isinstance(params["outdir"], str):
        raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("left_hemi", None) is not None:
        if not isinstance(params["left_hemi"], bool):
            raise StyxValidationError(f'`left_hemi` has the wrong type: Received `{type(params["left_hemi"])}` expected `bool`')
    if params.get("right_hemi", None) is not None:
        if not isinstance(params["right_hemi"], bool):
            raise StyxValidationError(f'`right_hemi` has the wrong type: Received `{type(params["right_hemi"])}` expected `bool`')
    if params.get("suptent", None) is not None:
        if not isinstance(params["suptent"], bool):
            raise StyxValidationError(f'`suptent` has the wrong type: Received `{type(params["suptent"])}` expected `bool`')
    if params.get("no_rotate", None) is not None:
        if not isinstance(params["no_rotate"], bool):
            raise StyxValidationError(f'`no_rotate` has the wrong type: Received `{type(params["no_rotate"])}` expected `bool`')
    if params.get("t1thresh", None) is not None:
        if not isinstance(params["t1thresh"], (float, int)):
            raise StyxValidationError(f'`t1thresh` has the wrong type: Received `{type(params["t1thresh"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("check_only", None) is not None:
        if not isinstance(params["check_only"], bool):
            raise StyxValidationError(f'`check_only` has the wrong type: Received `{type(params["check_only"])}` expected `bool`')
    if params.get("prep_only", None) is not None:
        if not isinstance(params["prep_only"], bool):
            raise StyxValidationError(f'`prep_only` has the wrong type: Received `{type(params["prep_only"])}` expected `bool`')
    if params.get("mask_only", None) is not None:
        if not isinstance(params["mask_only"], bool):
            raise StyxValidationError(f'`mask_only` has the wrong type: Received `{type(params["mask_only"])}` expected `bool`')
    if params.get("samseg_only", None) is not None:
        if not isinstance(params["samseg_only"], bool):
            raise StyxValidationError(f'`samseg_only` has the wrong type: Received `{type(params["samseg_only"])}` expected `bool`')
    if params.get("stop_mmppsp_after", None) is not None:
        if not isinstance(params["stop_mmppsp_after"], str):
            raise StyxValidationError(f'`stop_mmppsp_after` has the wrong type: Received `{type(params["stop_mmppsp_after"])}` expected `str`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')

def exvivo_hemi_proc_cargs(params: ExvivoHemiProc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("exvivo-hemi-proc")
    cargs.append("--i")
    cargs.append(params["flashdir"])
    cargs.append("--o")
    cargs.append(params["outdir"])
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("left_hemi"):
        cargs.append("--lh")
    if params.get("right_hemi"):
        cargs.append("--rh")
    if params.get("suptent"):
        cargs.append("--suptent")
    if params.get("no_rotate"):
        cargs.append("--no-rotate")
    v_0 = params.get("t1thresh")
    if v_0 is not None:
        cargs.append("--t1thresh")
        cargs.append(str(v_0))
    v_1 = params.get("threads")
    if v_1 is not None:
        cargs.append("--threads")
        cargs.append(str(v_1))
    if params.get("check_only"):
        cargs.append("--check-only")
    if params.get("prep_only"):
        cargs.append("--prep-only")
    if params.get("mask_only"):
        cargs.append("--mask-only")
    if params.get("samseg_only"):
        cargs.append("--samseg-only")
    v_2 = params.get("stop_mmppsp_after")
    if v_2 is not None:
        cargs.append("--stop-mmppsp-after")
        cargs.append(v_2)
    if params.get("force"):
        cargs.append("--force")
    return cargs

def exvivo_hemi_proc_outputs(params: ExvivoHemiProc, execution: Execution) -> ExvivoHemiProcOutputs:
    """Build the ExvivoHemiProcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ExvivoHemiProcOutputs(
        root=root_v,
    )

def exvivo_hemi_proc_execute(params: ExvivoHemiProc, runner: Runner | None = None) -> ExvivoHemiProcOutputs:
    """
    exvivo-hemi-proc

    Processes whole hemisphere data for Jeans entorhinal subfield labeling project.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    exvivo_hemi_proc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EXVIVO_HEMI_PROC_METADATA)
    execution.params(params)
    args = exvivo_hemi_proc_cargs(params, execution)
    out = exvivo_hemi_proc_outputs(params, execution)
    execution.run(args)
    return out

def exvivo_hemi_proc(
    flashdir: str,
    outdir: str,
    subject: str,
    left_hemi: bool = False,
    right_hemi: bool = False,
    suptent: bool = False,
    no_rotate: bool = False,
    t1thresh: float | None = None,
    threads: float | None = None,
    check_only: bool = False,
    prep_only: bool = False,
    mask_only: bool = False,
    samseg_only: bool = False,
    stop_mmppsp_after: str | None = None,
    force: bool = False,
    runner: Runner | None = None,
) -> ExvivoHemiProcOutputs:
    """
    exvivo-hemi-proc

    Processes whole hemisphere data for Jeans entorhinal subfield labeling project.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        flashdir: Path to the FLASH data directory.
        outdir: Output directory where results will be saved.
        subject: Full path to the subject.
        left_hemi: Process left hemisphere.
        right_hemi: Process right hemisphere.
        suptent: Indicate no tentorium (cblum and bstem) in the sample.
        no_rotate: Indicate rotation is not needed.
        t1thresh: T1 threshold, default is 415.
        threads: Number of threads to use.
        check_only: Only perform check, without further processing.
        prep_only: Only run up to manual rotation.
        mask_only: Only run up to creation of masks.
        samseg_only: Only run up to samseg.
        stop_mmppsp_after: Stop processing after a specific step\
            {tess,fix,preaparc,sphere,spherereg,white,pial}.
        force: Force the execution, overriding checks.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = exvivo_hemi_proc_params(
        flashdir=flashdir,
        outdir=outdir,
        subject=subject,
        left_hemi=left_hemi,
        right_hemi=right_hemi,
        suptent=suptent,
        no_rotate=no_rotate,
        t1thresh=t1thresh,
        threads=threads,
        check_only=check_only,
        prep_only=prep_only,
        mask_only=mask_only,
        samseg_only=samseg_only,
        stop_mmppsp_after=stop_mmppsp_after,
        force=force,
    )
    return exvivo_hemi_proc_execute(params, runner)

__all__ = [
    "ExvivoHemiProc",
    "ExvivoHemiProcOutputs",
    "EXVIVO_HEMI_PROC_METADATA",
    "exvivo_hemi_proc_cargs",
    "exvivo_hemi_proc_outputs",
    "exvivo_hemi_proc_params",
    "exvivo_hemi_proc_execute",
    "exvivo_hemi_proc_validate",
    "exvivo_hemi_proc",
]