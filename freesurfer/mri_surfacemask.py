# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SURFACEMASK_METADATA = Metadata(
    id="mri_surfacemask",
    name="mri_surfacemask",
    package="freesurfer",
)

MriSurfacemask = typing.TypedDict(
    "MriSurfacemask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_surfacemask"]],
        "input_volume": InputPathType,
        "input_surface": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriSurfacemaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """The resulting surface-masked volume"""

def mri_surfacemask_params(
    input_volume: InputPathType,
    input_surface: InputPathType,
    output_volume: str,
) -> MriSurfacemask:
    """
    Build parameters.

    Args:
        input_volume: Input volume which will be masked
        input_surface: Surface file used for masking the volume
        output_volume: Output volume file where pixels outside the surface are set\
            to zero

    Returns:
        Parameter dictionary.
    """
    params: MriSurfacemask = {
        "@type": "freesurfer/mri_surfacemask",
        "input_volume": input_volume,
        "input_surface": input_surface,
        "output_volume": output_volume,
    }
    return params

def mri_surfacemask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSurfacemask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_surfacemask_cargs(params: MriSurfacemask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_surfacemask")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_surfacemask_outputs(params: MriSurfacemask, execution: Execution) -> MriSurfacemaskOutputs:
    """Build the MriSurfacemaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriSurfacemaskOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_surfacemask_execute(params: MriSurfacemask, runner: Runner | None = None) -> MriSurfacemaskOutputs:
    """
    mri_surfacemask

    Tool to produce a new volume where all pixels outside the surface are set to zero.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_surfacemask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SURFACEMASK_METADATA)
    execution.params(params)
    args = mri_surfacemask_cargs(params, execution)
    out = mri_surfacemask_outputs(params, execution)
    execution.run(args)
    return out

def mri_surfacemask(
    input_volume: InputPathType,
    input_surface: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriSurfacemaskOutputs:
    """
    mri_surfacemask

    Tool to produce a new volume where all pixels outside the surface are set to zero.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume which will be masked
        input_surface: Surface file used for masking the volume
        output_volume: Output volume file where pixels outside the surface are set\
            to zero
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_surfacemask_params(
        input_volume=input_volume,
        input_surface=input_surface,
        output_volume=output_volume,
    )
    return mri_surfacemask_execute(params, runner)

__all__ = [
    "MriSurfacemask",
    "MriSurfacemaskOutputs",
    "MRI_SURFACEMASK_METADATA",
    "mri_surfacemask_cargs",
    "mri_surfacemask_outputs",
    "mri_surfacemask_params",
    "mri_surfacemask_execute",
    "mri_surfacemask_validate",
    "mri_surfacemask",
]