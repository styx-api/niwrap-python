# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RUN_SAMSEG_LONG_METADATA = Metadata(
    id="run_samseg_long",
    name="run_samseg_long",
    package="freesurfer",
)

RunSamsegLong = typing.TypedDict(
    "RunSamsegLong",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/run_samseg_long"]],
        "timepoint": list[InputPathType],
        "output": str,
        "lesion": typing.NotRequired[bool],
        "threshold": typing.NotRequired[float],
        "samples": typing.NotRequired[float],
        "burnin": typing.NotRequired[float],
        "lesion_mask_structure": typing.NotRequired[str],
        "lesion_mask_pattern": typing.NotRequired[list[float]],
        "mode": typing.NotRequired[list[str]],
        "atlas": typing.NotRequired[str],
        "deformation_hyperprior": typing.NotRequired[float],
        "gmm_hyperprior": typing.NotRequired[float],
        "save_warp": typing.NotRequired[bool],
        "save_mesh": typing.NotRequired[bool],
        "save_posteriors": typing.NotRequired[list[str]],
        "pallidum_separate": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "tp_to_base_transform": typing.NotRequired[list[InputPathType]],
        "force_different_resolutions": typing.NotRequired[bool],
        "history": typing.NotRequired[bool],
        "showfigs": typing.NotRequired[bool],
        "movie": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RunSamsegLongOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def run_samseg_long_params(
    timepoint: list[InputPathType],
    output: str,
    lesion: bool = False,
    threshold: float | None = None,
    samples: float | None = None,
    burnin: float | None = None,
    lesion_mask_structure: str | None = None,
    lesion_mask_pattern: list[float] | None = None,
    mode: list[str] | None = None,
    atlas: str | None = None,
    deformation_hyperprior: float | None = None,
    gmm_hyperprior: float | None = None,
    save_warp: bool = False,
    save_mesh: bool = False,
    save_posteriors: list[str] | None = None,
    pallidum_separate: bool = False,
    threads: float | None = None,
    tp_to_base_transform: list[InputPathType] | None = None,
    force_different_resolutions: bool = False,
    history: bool = False,
    showfigs: bool = False,
    movie: bool = False,
) -> RunSamsegLong:
    """
    Build parameters.

    Args:
        timepoint: Configure a timepoint with multiple inputs.
        output: Output directory.
        lesion: Enable lesion segmentation (requires tensorflow).
        threshold: Lesion threshold for final segmentation. Requires lesion\
            segmentation.
        samples: Number of samples for lesion segmentation. Requires lesion\
            segmentation.
        burnin: Number of burn-in samples for lesion segmentation. Requires lesion\
            segmentation.
        lesion_mask_structure: Intensity mask brain structure. Requires lesion\
            segmentation.
        lesion_mask_pattern: Lesion mask list: -1 below lesion mask structure mean,\
            +1 above, 0 no mask. Requires lesion segmentation.
        mode: Output basenames for the input image mode.
        atlas: Point to an alternative atlas directory.
        deformation_hyperprior: Strength of the latent deformation hyperprior.
        gmm_hyperprior: Strength of the latent GMM hyperprior.
        save_warp: Save the image->template warp fields.
        save_mesh: Save the final mesh of each timepoint in template space.
        save_posteriors: Save posterior volumes to the 'posteriors' subdirectory.
        pallidum_separate: Move pallidum outside of global white matter class. Use\
            with T2/flair.
        threads: Number of threads to use. Defaults to OMP_NUM_THREADS or 1.
        tp_to_base_transform: Transformation file for each time point to base.
        force_different_resolutions: Force run even if time points have different\
            resolutions.
        history: Save history.
        showfigs: Show figures during run.
        movie: Show history as arrow key controlled time sequence.

    Returns:
        Parameter dictionary.
    """
    params: RunSamsegLong = {
        "@type": "freesurfer/run_samseg_long",
        "timepoint": timepoint,
        "output": output,
        "lesion": lesion,
        "save_warp": save_warp,
        "save_mesh": save_mesh,
        "pallidum_separate": pallidum_separate,
        "force_different_resolutions": force_different_resolutions,
        "history": history,
        "showfigs": showfigs,
        "movie": movie,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if samples is not None:
        params["samples"] = samples
    if burnin is not None:
        params["burnin"] = burnin
    if lesion_mask_structure is not None:
        params["lesion_mask_structure"] = lesion_mask_structure
    if lesion_mask_pattern is not None:
        params["lesion_mask_pattern"] = lesion_mask_pattern
    if mode is not None:
        params["mode"] = mode
    if atlas is not None:
        params["atlas"] = atlas
    if deformation_hyperprior is not None:
        params["deformation_hyperprior"] = deformation_hyperprior
    if gmm_hyperprior is not None:
        params["gmm_hyperprior"] = gmm_hyperprior
    if save_posteriors is not None:
        params["save_posteriors"] = save_posteriors
    if threads is not None:
        params["threads"] = threads
    if tp_to_base_transform is not None:
        params["tp_to_base_transform"] = tp_to_base_transform
    return params

def run_samseg_long_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RunSamsegLong."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("timepoint", None) is None:
        raise StyxValidationError("`timepoint` must not be None")
    if not isinstance(params["timepoint"], list):
        raise StyxValidationError(f'`timepoint` has the wrong type: Received `{type(params["timepoint"])}` expected `list[InputPathType]`')
    for e in params["timepoint"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`timepoint` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("lesion", None) is not None:
        if not isinstance(params["lesion"], bool):
            raise StyxValidationError(f'`lesion` has the wrong type: Received `{type(params["lesion"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("samples", None) is not None:
        if not isinstance(params["samples"], (float, int)):
            raise StyxValidationError(f'`samples` has the wrong type: Received `{type(params["samples"])}` expected `float`')
    if params.get("burnin", None) is not None:
        if not isinstance(params["burnin"], (float, int)):
            raise StyxValidationError(f'`burnin` has the wrong type: Received `{type(params["burnin"])}` expected `float`')
    if params.get("lesion_mask_structure", None) is not None:
        if not isinstance(params["lesion_mask_structure"], str):
            raise StyxValidationError(f'`lesion_mask_structure` has the wrong type: Received `{type(params["lesion_mask_structure"])}` expected `str`')
    if params.get("lesion_mask_pattern", None) is not None:
        if not isinstance(params["lesion_mask_pattern"], list):
            raise StyxValidationError(f'`lesion_mask_pattern` has the wrong type: Received `{type(params["lesion_mask_pattern"])}` expected `list[float]`')
        for e_2 in params["lesion_mask_pattern"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`lesion_mask_pattern` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("mode", None) is not None:
        if not isinstance(params["mode"], list):
            raise StyxValidationError(f'`mode` has the wrong type: Received `{type(params["mode"])}` expected `list[str]`')
        for e_3 in params["mode"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`mode` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("atlas", None) is not None:
        if not isinstance(params["atlas"], str):
            raise StyxValidationError(f'`atlas` has the wrong type: Received `{type(params["atlas"])}` expected `str`')
    if params.get("deformation_hyperprior", None) is not None:
        if not isinstance(params["deformation_hyperprior"], (float, int)):
            raise StyxValidationError(f'`deformation_hyperprior` has the wrong type: Received `{type(params["deformation_hyperprior"])}` expected `float`')
    if params.get("gmm_hyperprior", None) is not None:
        if not isinstance(params["gmm_hyperprior"], (float, int)):
            raise StyxValidationError(f'`gmm_hyperprior` has the wrong type: Received `{type(params["gmm_hyperprior"])}` expected `float`')
    if params.get("save_warp", None) is not None:
        if not isinstance(params["save_warp"], bool):
            raise StyxValidationError(f'`save_warp` has the wrong type: Received `{type(params["save_warp"])}` expected `bool`')
    if params.get("save_mesh", None) is not None:
        if not isinstance(params["save_mesh"], bool):
            raise StyxValidationError(f'`save_mesh` has the wrong type: Received `{type(params["save_mesh"])}` expected `bool`')
    if params.get("save_posteriors", None) is not None:
        if not isinstance(params["save_posteriors"], list):
            raise StyxValidationError(f'`save_posteriors` has the wrong type: Received `{type(params["save_posteriors"])}` expected `list[str]`')
        for e_4 in params["save_posteriors"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`save_posteriors` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("pallidum_separate", None) is not None:
        if not isinstance(params["pallidum_separate"], bool):
            raise StyxValidationError(f'`pallidum_separate` has the wrong type: Received `{type(params["pallidum_separate"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("tp_to_base_transform", None) is not None:
        if not isinstance(params["tp_to_base_transform"], list):
            raise StyxValidationError(f'`tp_to_base_transform` has the wrong type: Received `{type(params["tp_to_base_transform"])}` expected `list[InputPathType]`')
        for e_5 in params["tp_to_base_transform"]:
            if not isinstance(e_5, (pathlib.Path, str)):
                raise StyxValidationError(f'`tp_to_base_transform` has the wrong type: Received `{type(e_5)}` expected `list[InputPathType]`')
    if params.get("force_different_resolutions", None) is not None:
        if not isinstance(params["force_different_resolutions"], bool):
            raise StyxValidationError(f'`force_different_resolutions` has the wrong type: Received `{type(params["force_different_resolutions"])}` expected `bool`')
    if params.get("history", None) is not None:
        if not isinstance(params["history"], bool):
            raise StyxValidationError(f'`history` has the wrong type: Received `{type(params["history"])}` expected `bool`')
    if params.get("showfigs", None) is not None:
        if not isinstance(params["showfigs"], bool):
            raise StyxValidationError(f'`showfigs` has the wrong type: Received `{type(params["showfigs"])}` expected `bool`')
    if params.get("movie", None) is not None:
        if not isinstance(params["movie"], bool):
            raise StyxValidationError(f'`movie` has the wrong type: Received `{type(params["movie"])}` expected `bool`')

def run_samseg_long_cargs(params: RunSamsegLong, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("run_samseg_long")
    cargs.append("-t")
    for item0 in params["timepoint"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-o")
    cargs.append(params["output"])
    if params.get("lesion"):
        cargs.append("--lesion")
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("--threshold")
        cargs.append(str(v_0))
    v_1 = params.get("samples")
    if v_1 is not None:
        cargs.append("--samples")
        cargs.append(str(v_1))
    v_2 = params.get("burnin")
    if v_2 is not None:
        cargs.append("--burnin")
        cargs.append(str(v_2))
    v_3 = params.get("lesion_mask_structure")
    if v_3 is not None:
        cargs.append("--lesion-mask-structure")
        cargs.append(v_3)
    v_4 = params.get("lesion_mask_pattern")
    if v_4 is not None:
        cargs.append("--lesion-mask-pattern")
        for item1 in v_4:
            cargs.append(str(item1))
    v_5 = params.get("mode")
    if v_5 is not None:
        cargs.append("-m")
        for item2 in v_5:
            cargs.append(item2)
    v_6 = params.get("atlas")
    if v_6 is not None:
        cargs.append("-a")
        cargs.append(v_6)
    v_7 = params.get("deformation_hyperprior")
    if v_7 is not None:
        cargs.append("--deformation-hyperprior")
        cargs.append(str(v_7))
    v_8 = params.get("gmm_hyperprior")
    if v_8 is not None:
        cargs.append("--gmm-hyperprior")
        cargs.append(str(v_8))
    if params.get("save_warp"):
        cargs.append("--save-warp")
    if params.get("save_mesh"):
        cargs.append("--save-mesh")
    v_9 = params.get("save_posteriors")
    if v_9 is not None:
        cargs.append("--save-posteriors")
        for item3 in v_9:
            cargs.append(item3)
    if params.get("pallidum_separate"):
        cargs.append("--pallidum-separate")
    v_10 = params.get("threads")
    if v_10 is not None:
        cargs.append("--threads")
        cargs.append(str(v_10))
    v_11 = params.get("tp_to_base_transform")
    if v_11 is not None:
        cargs.append("--tp-to-base-transform")
        for item4 in v_11:
            cargs.append(execution.input_file(item4))
    if params.get("force_different_resolutions"):
        cargs.append("--force-different-resolutions")
    if params.get("history"):
        cargs.append("--history")
    if params.get("showfigs"):
        cargs.append("--showfigs")
    if params.get("movie"):
        cargs.append("--movie")
    return cargs

def run_samseg_long_outputs(params: RunSamsegLong, execution: Execution) -> RunSamsegLongOutputs:
    """Build the RunSamsegLongOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RunSamsegLongOutputs(
        root=root_v,
    )

def run_samseg_long_execute(params: RunSamsegLong, runner: Runner | None = None) -> RunSamsegLongOutputs:
    """
    run_samseg_long

    Longitudinal image segmentation using SAMSEG.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    run_samseg_long_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RUN_SAMSEG_LONG_METADATA)
    execution.params(params)
    args = run_samseg_long_cargs(params, execution)
    out = run_samseg_long_outputs(params, execution)
    execution.run(args)
    return out

def run_samseg_long(
    timepoint: list[InputPathType],
    output: str,
    lesion: bool = False,
    threshold: float | None = None,
    samples: float | None = None,
    burnin: float | None = None,
    lesion_mask_structure: str | None = None,
    lesion_mask_pattern: list[float] | None = None,
    mode: list[str] | None = None,
    atlas: str | None = None,
    deformation_hyperprior: float | None = None,
    gmm_hyperprior: float | None = None,
    save_warp: bool = False,
    save_mesh: bool = False,
    save_posteriors: list[str] | None = None,
    pallidum_separate: bool = False,
    threads: float | None = None,
    tp_to_base_transform: list[InputPathType] | None = None,
    force_different_resolutions: bool = False,
    history: bool = False,
    showfigs: bool = False,
    movie: bool = False,
    runner: Runner | None = None,
) -> RunSamsegLongOutputs:
    """
    run_samseg_long

    Longitudinal image segmentation using SAMSEG.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        timepoint: Configure a timepoint with multiple inputs.
        output: Output directory.
        lesion: Enable lesion segmentation (requires tensorflow).
        threshold: Lesion threshold for final segmentation. Requires lesion\
            segmentation.
        samples: Number of samples for lesion segmentation. Requires lesion\
            segmentation.
        burnin: Number of burn-in samples for lesion segmentation. Requires lesion\
            segmentation.
        lesion_mask_structure: Intensity mask brain structure. Requires lesion\
            segmentation.
        lesion_mask_pattern: Lesion mask list: -1 below lesion mask structure mean,\
            +1 above, 0 no mask. Requires lesion segmentation.
        mode: Output basenames for the input image mode.
        atlas: Point to an alternative atlas directory.
        deformation_hyperprior: Strength of the latent deformation hyperprior.
        gmm_hyperprior: Strength of the latent GMM hyperprior.
        save_warp: Save the image->template warp fields.
        save_mesh: Save the final mesh of each timepoint in template space.
        save_posteriors: Save posterior volumes to the 'posteriors' subdirectory.
        pallidum_separate: Move pallidum outside of global white matter class. Use\
            with T2/flair.
        threads: Number of threads to use. Defaults to OMP_NUM_THREADS or 1.
        tp_to_base_transform: Transformation file for each time point to base.
        force_different_resolutions: Force run even if time points have different\
            resolutions.
        history: Save history.
        showfigs: Show figures during run.
        movie: Show history as arrow key controlled time sequence.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = run_samseg_long_params(
        timepoint=timepoint,
        output=output,
        lesion=lesion,
        threshold=threshold,
        samples=samples,
        burnin=burnin,
        lesion_mask_structure=lesion_mask_structure,
        lesion_mask_pattern=lesion_mask_pattern,
        mode=mode,
        atlas=atlas,
        deformation_hyperprior=deformation_hyperprior,
        gmm_hyperprior=gmm_hyperprior,
        save_warp=save_warp,
        save_mesh=save_mesh,
        save_posteriors=save_posteriors,
        pallidum_separate=pallidum_separate,
        threads=threads,
        tp_to_base_transform=tp_to_base_transform,
        force_different_resolutions=force_different_resolutions,
        history=history,
        showfigs=showfigs,
        movie=movie,
    )
    return run_samseg_long_execute(params, runner)

__all__ = [
    "RunSamsegLong",
    "RunSamsegLongOutputs",
    "RUN_SAMSEG_LONG_METADATA",
    "run_samseg_long_cargs",
    "run_samseg_long_outputs",
    "run_samseg_long_params",
    "run_samseg_long_execute",
    "run_samseg_long_validate",
    "run_samseg_long",
]