# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CURVATURE2IMAGE_METADATA = Metadata(
    id="mris_curvature2image",
    name="mris_curvature2image",
    package="freesurfer",
)

MrisCurvature2image = typing.TypedDict(
    "MrisCurvature2image",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_curvature2image"]],
        "surface": InputPathType,
        "mask": InputPathType,
        "output_overlay": str,
        "output_distance": str,
        "overlay": InputPathType,
        "label": InputPathType,
        "invert_flag": typing.NotRequired[bool],
        "radius": float,
    },
)

@dataclasses.dataclass
class MrisCurvature2imageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_overlay_img: OutputPathType
    """Generated overlay image file."""
    output_distance_img: OutputPathType
    """Generated distance image file."""

def mris_curvature2image_params(
    surface: InputPathType,
    mask: InputPathType,
    output_overlay: str,
    output_distance: str,
    overlay: InputPathType,
    label: InputPathType,
    radius: float,
    invert_flag: bool = False,
) -> MrisCurvature2image:
    """
    Build parameters.

    Args:
        surface: Input surface file.
        mask: Input mask file.
        output_overlay: Output overlay image file.
        output_distance: Output distance image file.
        overlay: Overlay file.
        label: Label file.
        radius: Radius value for processing.
        invert_flag: Flag to invert the curvature values.

    Returns:
        Parameter dictionary.
    """
    params: MrisCurvature2image = {
        "@type": "freesurfer/mris_curvature2image",
        "surface": surface,
        "mask": mask,
        "output_overlay": output_overlay,
        "output_distance": output_distance,
        "overlay": overlay,
        "label": label,
        "radius": radius,
        "invert_flag": invert_flag,
    }
    return params

def mris_curvature2image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCurvature2image."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("output_overlay", None) is None:
        raise StyxValidationError("`output_overlay` must not be None")
    if not isinstance(params["output_overlay"], str):
        raise StyxValidationError(f'`output_overlay` has the wrong type: Received `{type(params["output_overlay"])}` expected `str`')
    if params.get("output_distance", None) is None:
        raise StyxValidationError("`output_distance` must not be None")
    if not isinstance(params["output_distance"], str):
        raise StyxValidationError(f'`output_distance` has the wrong type: Received `{type(params["output_distance"])}` expected `str`')
    if params.get("overlay", None) is None:
        raise StyxValidationError("`overlay` must not be None")
    if not isinstance(params["overlay"], (pathlib.Path, str)):
        raise StyxValidationError(f'`overlay` has the wrong type: Received `{type(params["overlay"])}` expected `InputPathType`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("invert_flag", None) is not None:
        if not isinstance(params["invert_flag"], bool):
            raise StyxValidationError(f'`invert_flag` has the wrong type: Received `{type(params["invert_flag"])}` expected `bool`')
    if params.get("radius", None) is None:
        raise StyxValidationError("`radius` must not be None")
    if not isinstance(params["radius"], (float, int)):
        raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')

def mris_curvature2image_cargs(params: MrisCurvature2image, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_curvature2image")
    cargs.append("-s")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append("-m")
    cargs.append(execution.input_file(params["mask"]))
    cargs.append("-o")
    cargs.append(params["output_overlay"])
    cargs.append("-d")
    cargs.append(params["output_distance"])
    cargs.append("-c")
    cargs.append(execution.input_file(params["overlay"]))
    cargs.append("-l")
    cargs.append(execution.input_file(params["label"]))
    if params.get("invert_flag"):
        cargs.append("-inv")
    cargs.append("-r")
    cargs.append(str(params["radius"]))
    return cargs

def mris_curvature2image_outputs(params: MrisCurvature2image, execution: Execution) -> MrisCurvature2imageOutputs:
    """Build the MrisCurvature2imageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_overlay_img_v: OutputPathType = execution.output_file(params["output_overlay"])
    output_distance_img_v: OutputPathType = execution.output_file(params["output_distance"])
    return MrisCurvature2imageOutputs(
        root=root_v,
        output_overlay_img=output_overlay_img_v,
        output_distance_img=output_distance_img_v,
    )

def mris_curvature2image_execute(params: MrisCurvature2image, runner: Runner | None = None) -> MrisCurvature2imageOutputs:
    """
    mris_curvature2image

    Tool to convert surface curvature data to an image using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_curvature2image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CURVATURE2IMAGE_METADATA)
    execution.params(params)
    args = mris_curvature2image_cargs(params, execution)
    out = mris_curvature2image_outputs(params, execution)
    execution.run(args)
    return out

def mris_curvature2image(
    surface: InputPathType,
    mask: InputPathType,
    output_overlay: str,
    output_distance: str,
    overlay: InputPathType,
    label: InputPathType,
    radius: float,
    invert_flag: bool = False,
    runner: Runner | None = None,
) -> MrisCurvature2imageOutputs:
    """
    mris_curvature2image

    Tool to convert surface curvature data to an image using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Input surface file.
        mask: Input mask file.
        output_overlay: Output overlay image file.
        output_distance: Output distance image file.
        overlay: Overlay file.
        label: Label file.
        radius: Radius value for processing.
        invert_flag: Flag to invert the curvature values.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_curvature2image_params(
        surface=surface,
        mask=mask,
        output_overlay=output_overlay,
        output_distance=output_distance,
        overlay=overlay,
        label=label,
        radius=radius,
        invert_flag=invert_flag,
    )
    return mris_curvature2image_execute(params, runner)

__all__ = [
    "MrisCurvature2image",
    "MrisCurvature2imageOutputs",
    "MRIS_CURVATURE2IMAGE_METADATA",
    "mris_curvature2image_cargs",
    "mris_curvature2image_outputs",
    "mris_curvature2image_params",
    "mris_curvature2image_execute",
    "mris_curvature2image_validate",
    "mris_curvature2image",
]