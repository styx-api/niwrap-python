# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CNR_METADATA = Metadata(
    id="mri_cnr",
    name="mri_cnr",
    package="freesurfer",
)

MriCnr = typing.TypedDict(
    "MriCnr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cnr"]],
        "surf_dir": str,
        "volume_files": list[InputPathType],
        "slope": typing.NotRequired[list[str]],
        "logfile": typing.NotRequired[str],
        "labels": typing.NotRequired[list[str]],
        "print_total_cnr": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCnrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_cnr_params(
    surf_dir: str,
    volume_files: list[InputPathType],
    slope: list[str] | None = None,
    logfile: str | None = None,
    labels: list[str] | None = None,
    print_total_cnr: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
) -> MriCnr:
    """
    Build parameters.

    Args:
        surf_dir: Directory containing surface data.
        volume_files: Volumes to process.
        slope: Compute slope and write to files labeled with slope_fname. Requires\
            four additional values: dist_in, dist_out, step_in, and step_out.
        logfile: Log CNR to specified logfile. Will contain 8 values in a specific\
            order: gray_white_cnr, gray_csf_cnr, white_mean, gray_mean, csf_mean,\
            sqrt(white_var), sqrt(gray_var), sqrt(csf_var).
        labels: Read hemisphere labels from specified left and right hemisphere\
            files.
        print_total_cnr: Print only the total CNR to stdout.
        version_flag: Print software version information and quit.
        help_flag: Print usage information and quit.

    Returns:
        Parameter dictionary.
    """
    params: MriCnr = {
        "@type": "freesurfer/mri_cnr",
        "surf_dir": surf_dir,
        "volume_files": volume_files,
        "print_total_cnr": print_total_cnr,
        "version_flag": version_flag,
        "help_flag": help_flag,
    }
    if slope is not None:
        params["slope"] = slope
    if logfile is not None:
        params["logfile"] = logfile
    if labels is not None:
        params["labels"] = labels
    return params

def mri_cnr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCnr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surf_dir", None) is None:
        raise StyxValidationError("`surf_dir` must not be None")
    if not isinstance(params["surf_dir"], str):
        raise StyxValidationError(f'`surf_dir` has the wrong type: Received `{type(params["surf_dir"])}` expected `str`')
    if params.get("volume_files", None) is None:
        raise StyxValidationError("`volume_files` must not be None")
    if not isinstance(params["volume_files"], list):
        raise StyxValidationError(f'`volume_files` has the wrong type: Received `{type(params["volume_files"])}` expected `list[InputPathType]`')
    for e in params["volume_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`volume_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("slope", None) is not None:
        if not isinstance(params["slope"], list):
            raise StyxValidationError(f'`slope` has the wrong type: Received `{type(params["slope"])}` expected `list[str]`')
        if not (5 <= len(params["slope"]) <= 5):
            raise StyxValidationError("Parameter `slope` must contain between 5 and 5 elements (inclusive)")
        for e_2 in params["slope"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`slope` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("logfile", None) is not None:
        if not isinstance(params["logfile"], str):
            raise StyxValidationError(f'`logfile` has the wrong type: Received `{type(params["logfile"])}` expected `str`')
    if params.get("labels", None) is not None:
        if not isinstance(params["labels"], list):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
        if not (2 <= len(params["labels"]) <= 2):
            raise StyxValidationError("Parameter `labels` must contain between 2 and 2 elements (inclusive)")
        for e_3 in params["labels"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("print_total_cnr", None) is not None:
        if not isinstance(params["print_total_cnr"], bool):
            raise StyxValidationError(f'`print_total_cnr` has the wrong type: Received `{type(params["print_total_cnr"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def mri_cnr_cargs(params: MriCnr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cnr")
    cargs.append(params["surf_dir"])
    for item0 in params["volume_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("slope")
    if v_0 is not None:
        cargs.append("-s")
        for item1 in v_0:
            cargs.append(item1)
    v_1 = params.get("logfile")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(v_1)
    v_2 = params.get("labels")
    if v_2 is not None:
        cargs.append("label")
        for item2 in v_2:
            cargs.append(item2)
    if params.get("print_total_cnr"):
        cargs.append("-t")
    if params.get("version_flag"):
        cargs.append("-version")
    if params.get("help_flag"):
        cargs.append("-help")
    return cargs

def mri_cnr_outputs(params: MriCnr, execution: Execution) -> MriCnrOutputs:
    """Build the MriCnrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriCnrOutputs(
        root=root_v,
    )

def mri_cnr_execute(params: MriCnr, runner: Runner | None = None) -> MriCnrOutputs:
    """
    mri_cnr

    Compute the gray/white/csf contrast-to-noise ratio for volumes using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cnr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CNR_METADATA)
    execution.params(params)
    args = mri_cnr_cargs(params, execution)
    out = mri_cnr_outputs(params, execution)
    execution.run(args)
    return out

def mri_cnr(
    surf_dir: str,
    volume_files: list[InputPathType],
    slope: list[str] | None = None,
    logfile: str | None = None,
    labels: list[str] | None = None,
    print_total_cnr: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> MriCnrOutputs:
    """
    mri_cnr

    Compute the gray/white/csf contrast-to-noise ratio for volumes using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surf_dir: Directory containing surface data.
        volume_files: Volumes to process.
        slope: Compute slope and write to files labeled with slope_fname. Requires\
            four additional values: dist_in, dist_out, step_in, and step_out.
        logfile: Log CNR to specified logfile. Will contain 8 values in a specific\
            order: gray_white_cnr, gray_csf_cnr, white_mean, gray_mean, csf_mean,\
            sqrt(white_var), sqrt(gray_var), sqrt(csf_var).
        labels: Read hemisphere labels from specified left and right hemisphere\
            files.
        print_total_cnr: Print only the total CNR to stdout.
        version_flag: Print software version information and quit.
        help_flag: Print usage information and quit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cnr_params(
        surf_dir=surf_dir,
        volume_files=volume_files,
        slope=slope,
        logfile=logfile,
        labels=labels,
        print_total_cnr=print_total_cnr,
        version_flag=version_flag,
        help_flag=help_flag,
    )
    return mri_cnr_execute(params, runner)

__all__ = [
    "MriCnr",
    "MriCnrOutputs",
    "MRI_CNR_METADATA",
    "mri_cnr_cargs",
    "mri_cnr_outputs",
    "mri_cnr_params",
    "mri_cnr_execute",
    "mri_cnr_validate",
    "mri_cnr",
]