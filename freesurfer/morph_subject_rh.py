# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_SUBJECT_RH_METADATA = Metadata(
    id="morph_subject-rh",
    name="morph_subject-rh",
    package="freesurfer",
)

MorphSubjectRh = typing.TypedDict(
    "MorphSubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/morph_subject-rh"]],
        "subject_id": str,
    },
)

@dataclasses.dataclass
class MorphSubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def morph_subject_rh_params(
    subject_id: str,
) -> MorphSubjectRh:
    """
    Build parameters.

    Args:
        subject_id:

    Returns:
        Parameter dictionary.
    """
    params: MorphSubjectRh = {
        "@type": "freesurfer/morph_subject-rh",
        "subject_id": subject_id,
    }
    return params

def morph_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphSubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')

def morph_subject_rh_cargs(params: MorphSubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_subject-rh")
    cargs.append("-rh")
    cargs.append(params["subject_id"])
    return cargs

def morph_subject_rh_outputs(params: MorphSubjectRh, execution: Execution) -> MorphSubjectRhOutputs:
    """Build the MorphSubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MorphSubjectRhOutputs(
        root=root_v,
    )

def morph_subject_rh_execute(params: MorphSubjectRh, runner: Runner | None = None) -> MorphSubjectRhOutputs:
    """
    morph_subject-rh

    A tool for morphing the right hemisphere of subject data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_SUBJECT_RH_METADATA)
    execution.params(params)
    args = morph_subject_rh_cargs(params, execution)
    out = morph_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def morph_subject_rh(
    subject_id: str,
    runner: Runner | None = None,
) -> MorphSubjectRhOutputs:
    """
    morph_subject-rh

    A tool for morphing the right hemisphere of subject data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_subject_rh_params(
        subject_id=subject_id,
    )
    return morph_subject_rh_execute(params, runner)

__all__ = [
    "MorphSubjectRh",
    "MorphSubjectRhOutputs",
    "MORPH_SUBJECT_RH_METADATA",
    "morph_subject_rh_cargs",
    "morph_subject_rh_outputs",
    "morph_subject_rh_params",
    "morph_subject_rh_execute",
    "morph_subject_rh_validate",
    "morph_subject_rh",
]