# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ENTROPY_METADATA = Metadata(
    id="mris_entropy",
    name="mris_entropy",
    package="freesurfer",
)

MrisEntropy = typing.TypedDict(
    "MrisEntropy",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_entropy"]],
        "subject": str,
        "hemi": str,
        "wfile": InputPathType,
        "curvfile": InputPathType,
        "average_iterations": typing.NotRequired[float],
        "normalize": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisEntropyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file containing the computed entropy"""

def mris_entropy_params(
    subject: str,
    hemi: str,
    wfile: InputPathType,
    curvfile: InputPathType,
    average_iterations: float | None = None,
    normalize: bool = False,
) -> MrisEntropy:
    """
    Build parameters.

    Args:
        subject: Subject ID
        hemi: Hemisphere (e.g., lh or rh)
        wfile: Weight file for surface
        curvfile: Curvature file for input
        average_iterations: Specify number of curvature averaging iterations\
            (default=0)
        normalize: Normalize curvature before writing

    Returns:
        Parameter dictionary.
    """
    params: MrisEntropy = {
        "@type": "freesurfer/mris_entropy",
        "subject": subject,
        "hemi": hemi,
        "wfile": wfile,
        "curvfile": curvfile,
        "normalize": normalize,
    }
    if average_iterations is not None:
        params["average_iterations"] = average_iterations
    return params

def mris_entropy_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisEntropy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("wfile", None) is None:
        raise StyxValidationError("`wfile` must not be None")
    if not isinstance(params["wfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`wfile` has the wrong type: Received `{type(params["wfile"])}` expected `InputPathType`')
    if params.get("curvfile", None) is None:
        raise StyxValidationError("`curvfile` must not be None")
    if not isinstance(params["curvfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`curvfile` has the wrong type: Received `{type(params["curvfile"])}` expected `InputPathType`')
    if params.get("average_iterations", None) is not None:
        if not isinstance(params["average_iterations"], (float, int)):
            raise StyxValidationError(f'`average_iterations` has the wrong type: Received `{type(params["average_iterations"])}` expected `float`')
    if params.get("normalize", None) is not None:
        if not isinstance(params["normalize"], bool):
            raise StyxValidationError(f'`normalize` has the wrong type: Received `{type(params["normalize"])}` expected `bool`')

def mris_entropy_cargs(params: MrisEntropy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_entropy")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["wfile"]))
    cargs.append(execution.input_file(params["curvfile"]))
    v_0 = params.get("average_iterations")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    if params.get("normalize"):
        cargs.append("-n")
    return cargs

def mris_entropy_outputs(params: MrisEntropy, execution: Execution) -> MrisEntropyOutputs:
    """Build the MrisEntropyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["subject"]}_{params["hemi"]}_output.txt')
    return MrisEntropyOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mris_entropy_execute(params: MrisEntropy, runner: Runner | None = None) -> MrisEntropyOutputs:
    """
    mris_entropy

    Computes the entropy of a surface activation pattern for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_entropy_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ENTROPY_METADATA)
    execution.params(params)
    args = mris_entropy_cargs(params, execution)
    out = mris_entropy_outputs(params, execution)
    execution.run(args)
    return out

def mris_entropy(
    subject: str,
    hemi: str,
    wfile: InputPathType,
    curvfile: InputPathType,
    average_iterations: float | None = None,
    normalize: bool = False,
    runner: Runner | None = None,
) -> MrisEntropyOutputs:
    """
    mris_entropy

    Computes the entropy of a surface activation pattern for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject ID
        hemi: Hemisphere (e.g., lh or rh)
        wfile: Weight file for surface
        curvfile: Curvature file for input
        average_iterations: Specify number of curvature averaging iterations\
            (default=0)
        normalize: Normalize curvature before writing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_entropy_params(
        subject=subject,
        hemi=hemi,
        wfile=wfile,
        curvfile=curvfile,
        average_iterations=average_iterations,
        normalize=normalize,
    )
    return mris_entropy_execute(params, runner)

__all__ = [
    "MrisEntropy",
    "MrisEntropyOutputs",
    "MRIS_ENTROPY_METADATA",
    "mris_entropy_cargs",
    "mris_entropy_outputs",
    "mris_entropy_params",
    "mris_entropy_execute",
    "mris_entropy_validate",
    "mris_entropy",
]