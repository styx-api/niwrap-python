# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_ADD_XFORM_TO_HEADER_METADATA = Metadata(
    id="mri_add_xform_to_header",
    name="mri_add_xform_to_header",
    package="freesurfer",
)

MriAddXformToHeader = typing.TypedDict(
    "MriAddXformToHeader",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_add_xform_to_header"]],
        "xfm_file": InputPathType,
        "input_volume": InputPathType,
        "output_volume": str,
        "verbose": typing.NotRequired[bool],
        "copy_name": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriAddXformToHeaderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Volume output with added transformation."""

def mri_add_xform_to_header_params(
    xfm_file: InputPathType,
    input_volume: InputPathType,
    output_volume: str,
    verbose: bool = False,
    copy_name: bool = False,
) -> MriAddXformToHeader:
    """
    Build parameters.

    Args:
        xfm_file: Transformation file to be added to the volume header.
        input_volume: Input volume to which the transformation is added.
        output_volume: Output volume with the transformation included.
        verbose: Enable verbose output for more detailed logging.
        copy_name: Copy the name of the xfm file without loading it.

    Returns:
        Parameter dictionary.
    """
    params: MriAddXformToHeader = {
        "@type": "freesurfer/mri_add_xform_to_header",
        "xfm_file": xfm_file,
        "input_volume": input_volume,
        "output_volume": output_volume,
        "verbose": verbose,
        "copy_name": copy_name,
    }
    return params

def mri_add_xform_to_header_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAddXformToHeader."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("xfm_file", None) is None:
        raise StyxValidationError("`xfm_file` must not be None")
    if not isinstance(params["xfm_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xfm_file` has the wrong type: Received `{type(params["xfm_file"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("copy_name", None) is not None:
        if not isinstance(params["copy_name"], bool):
            raise StyxValidationError(f'`copy_name` has the wrong type: Received `{type(params["copy_name"])}` expected `bool`')

def mri_add_xform_to_header_cargs(params: MriAddXformToHeader, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_add_xform_to_header")
    cargs.append(execution.input_file(params["xfm_file"]))
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_volume"])
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("copy_name"):
        cargs.append("-c")
    return cargs

def mri_add_xform_to_header_outputs(params: MriAddXformToHeader, execution: Execution) -> MriAddXformToHeaderOutputs:
    """Build the MriAddXformToHeaderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriAddXformToHeaderOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_add_xform_to_header_execute(params: MriAddXformToHeader, runner: Runner | None = None) -> MriAddXformToHeaderOutputs:
    """
    mri_add_xform_to_header

    Program to add specified transformation to the volume header.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_add_xform_to_header_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_ADD_XFORM_TO_HEADER_METADATA)
    execution.params(params)
    args = mri_add_xform_to_header_cargs(params, execution)
    out = mri_add_xform_to_header_outputs(params, execution)
    execution.run(args)
    return out

def mri_add_xform_to_header(
    xfm_file: InputPathType,
    input_volume: InputPathType,
    output_volume: str,
    verbose: bool = False,
    copy_name: bool = False,
    runner: Runner | None = None,
) -> MriAddXformToHeaderOutputs:
    """
    mri_add_xform_to_header

    Program to add specified transformation to the volume header.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        xfm_file: Transformation file to be added to the volume header.
        input_volume: Input volume to which the transformation is added.
        output_volume: Output volume with the transformation included.
        verbose: Enable verbose output for more detailed logging.
        copy_name: Copy the name of the xfm file without loading it.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_add_xform_to_header_params(
        xfm_file=xfm_file,
        input_volume=input_volume,
        output_volume=output_volume,
        verbose=verbose,
        copy_name=copy_name,
    )
    return mri_add_xform_to_header_execute(params, runner)

__all__ = [
    "MriAddXformToHeader",
    "MriAddXformToHeaderOutputs",
    "MRI_ADD_XFORM_TO_HEADER_METADATA",
    "mri_add_xform_to_header_cargs",
    "mri_add_xform_to_header_outputs",
    "mri_add_xform_to_header_params",
    "mri_add_xform_to_header_execute",
    "mri_add_xform_to_header_validate",
    "mri_add_xform_to_header",
]