# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCBF_PREP_METADATA = Metadata(
    id="rcbf-prep",
    name="rcbf-prep",
    package="freesurfer",
)

RcbfPrep = typing.TypedDict(
    "RcbfPrep",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rcbf-prep"]],
        "outdir": str,
        "rcbfvol": InputPathType,
        "subject": typing.NotRequired[str],
        "roitab": typing.NotRequired[InputPathType],
        "register": typing.NotRequired[InputPathType],
        "template": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class RcbfPrepOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_hemisphere_rcbf: OutputPathType
    """Registered and resampled rCBF volume for left hemisphere."""
    rh_hemisphere_rcbf_right: OutputPathType
    """Registered and resampled rCBF volume for right hemisphere."""
    mni305_rcbf: OutputPathType
    """rCBF volume resampled to MNI305 space."""
    roi_stats: OutputPathType
    """Stats file summarizing the ROIs."""

def rcbf_prep_params(
    outdir: str,
    rcbfvol: InputPathType,
    subject: str | None = None,
    roitab: InputPathType | None = None,
    register: InputPathType | None = None,
    template: InputPathType | None = None,
) -> RcbfPrep:
    """
    Build parameters.

    Args:
        outdir: Output directory where results will be stored.
        rcbfvol: Input rCBF volume to be registered to the FreeSurfer anatomical.
        subject: Subject identifier for FreeSurfer anatomical registration.
        roitab: ROI table specifying which ROIs will be summarized, e.g.,\
            FreeSurferColorLUT.txt.
        register: Registration data file instead of a subject.
        template: Template file used instead of rCBF itself.

    Returns:
        Parameter dictionary.
    """
    params: RcbfPrep = {
        "@type": "freesurfer/rcbf-prep",
        "outdir": outdir,
        "rcbfvol": rcbfvol,
    }
    if subject is not None:
        params["subject"] = subject
    if roitab is not None:
        params["roitab"] = roitab
    if register is not None:
        params["register"] = register
    if template is not None:
        params["template"] = template
    return params

def rcbf_prep_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcbfPrep."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("outdir", None) is None:
        raise StyxValidationError("`outdir` must not be None")
    if not isinstance(params["outdir"], str):
        raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("rcbfvol", None) is None:
        raise StyxValidationError("`rcbfvol` must not be None")
    if not isinstance(params["rcbfvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`rcbfvol` has the wrong type: Received `{type(params["rcbfvol"])}` expected `InputPathType`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("roitab", None) is not None:
        if not isinstance(params["roitab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roitab` has the wrong type: Received `{type(params["roitab"])}` expected `InputPathType`')
    if params.get("register", None) is not None:
        if not isinstance(params["register"], (pathlib.Path, str)):
            raise StyxValidationError(f'`register` has the wrong type: Received `{type(params["register"])}` expected `InputPathType`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')

def rcbf_prep_cargs(params: RcbfPrep, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rcbf-prep")
    cargs.append("--o")
    cargs.append(params["outdir"])
    cargs.append("--rcbf")
    cargs.append(execution.input_file(params["rcbfvol"]))
    v_0 = params.get("subject")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    v_1 = params.get("roitab")
    if v_1 is not None:
        cargs.append("--roitab")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("register")
    if v_2 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("template")
    if v_3 is not None:
        cargs.append("--t")
        cargs.append(execution.input_file(v_3))
    return cargs

def rcbf_prep_outputs(params: RcbfPrep, execution: Execution) -> RcbfPrepOutputs:
    """Build the RcbfPrepOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    lh_hemisphere_rcbf_v: OutputPathType = execution.output_file(f'{params["outdir"]}/lh.rcbf.mgh')
    rh_hemisphere_rcbf_right_v: OutputPathType = execution.output_file(f'{params["outdir"]}/rh.rcbf.mgh')
    mni305_rcbf_v: OutputPathType = execution.output_file(f'{params["outdir"]}/rcbf.mni305.nii')
    roi_stats_v: OutputPathType = execution.output_file(f'{params["outdir"]}/roi.dat')
    return RcbfPrepOutputs(
        root=root_v,
        lh_hemisphere_rcbf=lh_hemisphere_rcbf_v,
        rh_hemisphere_rcbf_right=rh_hemisphere_rcbf_right_v,
        mni305_rcbf=mni305_rcbf_v,
        roi_stats=roi_stats_v,
    )

def rcbf_prep_execute(params: RcbfPrep, runner: Runner | None = None) -> RcbfPrepOutputs:
    """
    rcbf-prep

    Performs integration of rCBF as produced by Siemens scanners with FreeSurfer analysis in preparation for group analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rcbf_prep_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCBF_PREP_METADATA)
    execution.params(params)
    args = rcbf_prep_cargs(params, execution)
    out = rcbf_prep_outputs(params, execution)
    execution.run(args)
    return out

def rcbf_prep(
    outdir: str,
    rcbfvol: InputPathType,
    subject: str | None = None,
    roitab: InputPathType | None = None,
    register: InputPathType | None = None,
    template: InputPathType | None = None,
    runner: Runner | None = None,
) -> RcbfPrepOutputs:
    """
    rcbf-prep

    Performs integration of rCBF as produced by Siemens scanners with FreeSurfer analysis in preparation for group analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        outdir: Output directory where results will be stored.
        rcbfvol: Input rCBF volume to be registered to the FreeSurfer anatomical.
        subject: Subject identifier for FreeSurfer anatomical registration.
        roitab: ROI table specifying which ROIs will be summarized, e.g.,\
            FreeSurferColorLUT.txt.
        register: Registration data file instead of a subject.
        template: Template file used instead of rCBF itself.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rcbf_prep_params(
        outdir=outdir,
        rcbfvol=rcbfvol,
        subject=subject,
        roitab=roitab,
        register=register,
        template=template,
    )
    return rcbf_prep_execute(params, runner)

__all__ = [
    "RcbfPrep",
    "RcbfPrepOutputs",
    "RCBF_PREP_METADATA",
    "rcbf_prep_cargs",
    "rcbf_prep_outputs",
    "rcbf_prep_params",
    "rcbf_prep_execute",
    "rcbf_prep_validate",
    "rcbf_prep",
]