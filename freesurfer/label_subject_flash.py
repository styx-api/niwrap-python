# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_SUBJECT_FLASH_METADATA = Metadata(
    id="label_subject_flash",
    name="label_subject_flash",
    package="freesurfer",
)

LabelSubjectFlash = typing.TypedDict(
    "LabelSubjectFlash",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label_subject_flash"]],
        "tissue_params": InputPathType,
        "norm_volume": InputPathType,
        "transform_file": InputPathType,
        "classifier_array": InputPathType,
        "aseg_output": str,
    },
)

@dataclasses.dataclass
class LabelSubjectFlashOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    aseg_outfile: OutputPathType
    """Automatic segmentation (aseg) output file"""

def label_subject_flash_params(
    tissue_params: InputPathType,
    norm_volume: InputPathType,
    transform_file: InputPathType,
    classifier_array: InputPathType,
    aseg_output: str,
) -> LabelSubjectFlash:
    """
    Build parameters.

    Args:
        tissue_params: Path to the tissue parameter file for FLASH sequences.
        norm_volume: Path to the normalized T1 volume.
        transform_file: Talairach linear transform file.
        classifier_array: Path to the classifier array in GCA format.
        aseg_output: Output path for the automatic segmentation (aseg) file.

    Returns:
        Parameter dictionary.
    """
    params: LabelSubjectFlash = {
        "@type": "freesurfer/label_subject_flash",
        "tissue_params": tissue_params,
        "norm_volume": norm_volume,
        "transform_file": transform_file,
        "classifier_array": classifier_array,
        "aseg_output": aseg_output,
    }
    return params

def label_subject_flash_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelSubjectFlash."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tissue_params", None) is None:
        raise StyxValidationError("`tissue_params` must not be None")
    if not isinstance(params["tissue_params"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tissue_params` has the wrong type: Received `{type(params["tissue_params"])}` expected `InputPathType`')
    if params.get("norm_volume", None) is None:
        raise StyxValidationError("`norm_volume` must not be None")
    if not isinstance(params["norm_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`norm_volume` has the wrong type: Received `{type(params["norm_volume"])}` expected `InputPathType`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("classifier_array", None) is None:
        raise StyxValidationError("`classifier_array` must not be None")
    if not isinstance(params["classifier_array"], (pathlib.Path, str)):
        raise StyxValidationError(f'`classifier_array` has the wrong type: Received `{type(params["classifier_array"])}` expected `InputPathType`')
    if params.get("aseg_output", None) is None:
        raise StyxValidationError("`aseg_output` must not be None")
    if not isinstance(params["aseg_output"], str):
        raise StyxValidationError(f'`aseg_output` has the wrong type: Received `{type(params["aseg_output"])}` expected `str`')

def label_subject_flash_cargs(params: LabelSubjectFlash, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label_subject_flash")
    cargs.append("-flash")
    cargs.append(execution.input_file(params["tissue_params"]))
    cargs.append(execution.input_file(params["norm_volume"]))
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(execution.input_file(params["classifier_array"]))
    cargs.append(params["aseg_output"])
    return cargs

def label_subject_flash_outputs(params: LabelSubjectFlash, execution: Execution) -> LabelSubjectFlashOutputs:
    """Build the LabelSubjectFlashOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    aseg_outfile_v: OutputPathType = execution.output_file(params["aseg_output"])
    return LabelSubjectFlashOutputs(
        root=root_v,
        aseg_outfile=aseg_outfile_v,
    )

def label_subject_flash_execute(params: LabelSubjectFlash, runner: Runner | None = None) -> LabelSubjectFlashOutputs:
    """
    label_subject_flash

    A tool for labeling brain structures in an MRI dataset using FLASH sequences and the FreeSurfer software

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_subject_flash_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_SUBJECT_FLASH_METADATA)
    execution.params(params)
    args = label_subject_flash_cargs(params, execution)
    out = label_subject_flash_outputs(params, execution)
    execution.run(args)
    return out

def label_subject_flash(
    tissue_params: InputPathType,
    norm_volume: InputPathType,
    transform_file: InputPathType,
    classifier_array: InputPathType,
    aseg_output: str,
    runner: Runner | None = None,
) -> LabelSubjectFlashOutputs:
    """
    label_subject_flash

    A tool for labeling brain structures in an MRI dataset using FLASH sequences and the FreeSurfer software

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        tissue_params: Path to the tissue parameter file for FLASH sequences.
        norm_volume: Path to the normalized T1 volume.
        transform_file: Talairach linear transform file.
        classifier_array: Path to the classifier array in GCA format.
        aseg_output: Output path for the automatic segmentation (aseg) file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_subject_flash_params(
        tissue_params=tissue_params,
        norm_volume=norm_volume,
        transform_file=transform_file,
        classifier_array=classifier_array,
        aseg_output=aseg_output,
    )
    return label_subject_flash_execute(params, runner)

__all__ = [
    "LabelSubjectFlash",
    "LabelSubjectFlashOutputs",
    "LABEL_SUBJECT_FLASH_METADATA",
    "label_subject_flash_cargs",
    "label_subject_flash_outputs",
    "label_subject_flash_params",
    "label_subject_flash_execute",
    "label_subject_flash_validate",
    "label_subject_flash",
]