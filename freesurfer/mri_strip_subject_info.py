# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_STRIP_SUBJECT_INFO_METADATA = Metadata(
    id="mri_strip_subject_info",
    name="mri_strip_subject_info",
    package="freesurfer",
)

MriStripSubjectInfo = typing.TypedDict(
    "MriStripSubjectInfo",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_strip_subject_info"]],
        "input_files": list[InputPathType],
        "output_directory": str,
    },
)

@dataclasses.dataclass
class MriStripSubjectInfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_strip_subject_info_params(
    input_files: list[InputPathType],
    output_directory: str,
) -> MriStripSubjectInfo:
    """
    Build parameters.

    Args:
        input_files: Input file(s) containing MRI data
        output_directory: Directory where stripped files will be saved

    Returns:
        Parameter dictionary.
    """
    params: MriStripSubjectInfo = {
        "@type": "freesurfer/mri_strip_subject_info",
        "input_files": input_files,
        "output_directory": output_directory,
    }
    return params

def mri_strip_subject_info_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriStripSubjectInfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def mri_strip_subject_info_cargs(params: MriStripSubjectInfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_strip_subject_info")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_directory"])
    return cargs

def mri_strip_subject_info_outputs(params: MriStripSubjectInfo, execution: Execution) -> MriStripSubjectInfoOutputs:
    """Build the MriStripSubjectInfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriStripSubjectInfoOutputs(
        root=root_v,
    )

def mri_strip_subject_info_execute(params: MriStripSubjectInfo, runner: Runner | None = None) -> MriStripSubjectInfoOutputs:
    """
    mri_strip_subject_info

    A tool to strip subject information from MRI data files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_strip_subject_info_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_STRIP_SUBJECT_INFO_METADATA)
    execution.params(params)
    args = mri_strip_subject_info_cargs(params, execution)
    out = mri_strip_subject_info_outputs(params, execution)
    execution.run(args)
    return out

def mri_strip_subject_info(
    input_files: list[InputPathType],
    output_directory: str,
    runner: Runner | None = None,
) -> MriStripSubjectInfoOutputs:
    """
    mri_strip_subject_info

    A tool to strip subject information from MRI data files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input file(s) containing MRI data
        output_directory: Directory where stripped files will be saved
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_strip_subject_info_params(
        input_files=input_files,
        output_directory=output_directory,
    )
    return mri_strip_subject_info_execute(params, runner)

__all__ = [
    "MriStripSubjectInfo",
    "MriStripSubjectInfoOutputs",
    "MRI_STRIP_SUBJECT_INFO_METADATA",
    "mri_strip_subject_info_cargs",
    "mri_strip_subject_info_outputs",
    "mri_strip_subject_info_params",
    "mri_strip_subject_info_execute",
    "mri_strip_subject_info_validate",
    "mri_strip_subject_info",
]