# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GCATRAINSKULL_METADATA = Metadata(
    id="gcatrainskull",
    name="gcatrainskull",
    package="freesurfer",
)

Gcatrainskull = typing.TypedDict(
    "Gcatrainskull",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/gcatrainskull"]],
        "gcatrain_dir": str,
    },
)

@dataclasses.dataclass
class GcatrainskullOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def gcatrainskull_params(
    gcatrain_dir: str,
) -> Gcatrainskull:
    """
    Build parameters.

    Args:
        gcatrain_dir:

    Returns:
        Parameter dictionary.
    """
    params: Gcatrainskull = {
        "@type": "freesurfer/gcatrainskull",
        "gcatrain_dir": gcatrain_dir,
    }
    return params

def gcatrainskull_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gcatrainskull."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gcatrain_dir", None) is None:
        raise StyxValidationError("`gcatrain_dir` must not be None")
    if not isinstance(params["gcatrain_dir"], str):
        raise StyxValidationError(f'`gcatrain_dir` has the wrong type: Received `{type(params["gcatrain_dir"])}` expected `str`')

def gcatrainskull_cargs(params: Gcatrainskull, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gcatrainskull")
    cargs.append("--g")
    cargs.append(params["gcatrain_dir"])
    return cargs

def gcatrainskull_outputs(params: Gcatrainskull, execution: Execution) -> GcatrainskullOutputs:
    """Build the GcatrainskullOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GcatrainskullOutputs(
        root=root_v,
    )

def gcatrainskull_execute(params: Gcatrainskull, runner: Runner | None = None) -> GcatrainskullOutputs:
    """
    gcatrainskull

    GCA train skull stripping tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gcatrainskull_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GCATRAINSKULL_METADATA)
    execution.params(params)
    args = gcatrainskull_cargs(params, execution)
    out = gcatrainskull_outputs(params, execution)
    execution.run(args)
    return out

def gcatrainskull(
    gcatrain_dir: str,
    runner: Runner | None = None,
) -> GcatrainskullOutputs:
    """
    gcatrainskull

    GCA train skull stripping tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        gcatrain_dir:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gcatrainskull_params(
        gcatrain_dir=gcatrain_dir,
    )
    return gcatrainskull_execute(params, runner)

__all__ = [
    "Gcatrainskull",
    "GcatrainskullOutputs",
    "GCATRAINSKULL_METADATA",
    "gcatrainskull_cargs",
    "gcatrainskull_outputs",
    "gcatrainskull_params",
    "gcatrainskull_execute",
    "gcatrainskull_validate",
    "gcatrainskull",
]