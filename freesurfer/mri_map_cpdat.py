# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MAP_CPDAT_METADATA = Metadata(
    id="mri_map_cpdat",
    name="mri_map_cpdat",
    package="freesurfer",
)

MriMapCpdat = typing.TypedDict(
    "MriMapCpdat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_map_cpdat"]],
        "input_file": InputPathType,
        "output_file": str,
        "lta_file": typing.NotRequired[InputPathType],
        "to_mni305": typing.NotRequired[str],
        "from_mni305": typing.NotRequired[str],
        "subject_list_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MriMapCpdatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_ctrl_file: OutputPathType
    """Output control point text file"""

def mri_map_cpdat_params(
    input_file: InputPathType,
    output_file: str,
    lta_file: InputPathType | None = None,
    to_mni305: str | None = None,
    from_mni305: str | None = None,
    subject_list_file: InputPathType | None = None,
) -> MriMapCpdat:
    """
    Build parameters.

    Args:
        input_file: Input control point text file
        output_file: Output control point text file
        lta_file: LTA transform file to be applied
        to_mni305: Get LTA from talairach.xfm for a specific subject
        from_mni305: Get LTA from talairach.xfm from a specific subject
        subject_list_file: Maps all control points from all subjects listed in the\
            text/ascii subjectlistfile to MNI305 (talairach) space

    Returns:
        Parameter dictionary.
    """
    params: MriMapCpdat = {
        "@type": "freesurfer/mri_map_cpdat",
        "input_file": input_file,
        "output_file": output_file,
    }
    if lta_file is not None:
        params["lta_file"] = lta_file
    if to_mni305 is not None:
        params["to_mni305"] = to_mni305
    if from_mni305 is not None:
        params["from_mni305"] = from_mni305
    if subject_list_file is not None:
        params["subject_list_file"] = subject_list_file
    return params

def mri_map_cpdat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMapCpdat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("lta_file", None) is not None:
        if not isinstance(params["lta_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lta_file` has the wrong type: Received `{type(params["lta_file"])}` expected `InputPathType`')
    if params.get("to_mni305", None) is not None:
        if not isinstance(params["to_mni305"], str):
            raise StyxValidationError(f'`to_mni305` has the wrong type: Received `{type(params["to_mni305"])}` expected `str`')
    if params.get("from_mni305", None) is not None:
        if not isinstance(params["from_mni305"], str):
            raise StyxValidationError(f'`from_mni305` has the wrong type: Received `{type(params["from_mni305"])}` expected `str`')
    if params.get("subject_list_file", None) is not None:
        if not isinstance(params["subject_list_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`subject_list_file` has the wrong type: Received `{type(params["subject_list_file"])}` expected `InputPathType`')

def mri_map_cpdat_cargs(params: MriMapCpdat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_map_cpdat")
    cargs.append("-in")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-out")
    cargs.append(params["output_file"])
    v_0 = params.get("lta_file")
    if v_0 is not None:
        cargs.append("-lta")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("to_mni305")
    if v_1 is not None:
        cargs.append("-tomni305")
        cargs.append(v_1)
    v_2 = params.get("from_mni305")
    if v_2 is not None:
        cargs.append("-frommni305")
        cargs.append(v_2)
    v_3 = params.get("subject_list_file")
    if v_3 is not None:
        cargs.append("-slf")
        cargs.append(execution.input_file(v_3))
    return cargs

def mri_map_cpdat_outputs(params: MriMapCpdat, execution: Execution) -> MriMapCpdatOutputs:
    """Build the MriMapCpdatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_ctrl_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MriMapCpdatOutputs(
        root=root_v,
        output_ctrl_file=output_ctrl_file_v,
    )

def mri_map_cpdat_execute(params: MriMapCpdat, runner: Runner | None = None) -> MriMapCpdatOutputs:
    """
    mri_map_cpdat

    Maps a control.dat file to a different space using an LTA

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_map_cpdat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MAP_CPDAT_METADATA)
    execution.params(params)
    args = mri_map_cpdat_cargs(params, execution)
    out = mri_map_cpdat_outputs(params, execution)
    execution.run(args)
    return out

def mri_map_cpdat(
    input_file: InputPathType,
    output_file: str,
    lta_file: InputPathType | None = None,
    to_mni305: str | None = None,
    from_mni305: str | None = None,
    subject_list_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> MriMapCpdatOutputs:
    """
    mri_map_cpdat

    Maps a control.dat file to a different space using an LTA

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input control point text file
        output_file: Output control point text file
        lta_file: LTA transform file to be applied
        to_mni305: Get LTA from talairach.xfm for a specific subject
        from_mni305: Get LTA from talairach.xfm from a specific subject
        subject_list_file: Maps all control points from all subjects listed in the\
            text/ascii subjectlistfile to MNI305 (talairach) space
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_map_cpdat_params(
        input_file=input_file,
        output_file=output_file,
        lta_file=lta_file,
        to_mni305=to_mni305,
        from_mni305=from_mni305,
        subject_list_file=subject_list_file,
    )
    return mri_map_cpdat_execute(params, runner)

__all__ = [
    "MriMapCpdat",
    "MriMapCpdatOutputs",
    "MRI_MAP_CPDAT_METADATA",
    "mri_map_cpdat_cargs",
    "mri_map_cpdat_outputs",
    "mri_map_cpdat_params",
    "mri_map_cpdat_execute",
    "mri_map_cpdat_validate",
    "mri_map_cpdat",
]