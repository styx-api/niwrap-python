# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HIAM_MAKE_SURFACES_METADATA = Metadata(
    id="hiam_make_surfaces",
    name="hiam_make_surfaces",
    package="freesurfer",
)

HiamMakeSurfacesStructure = typing.Literal["RA"] | typing.Literal["LA"] | typing.Literal["RH"] | typing.Literal["LH"]

HiamMakeSurfaces = typing.TypedDict(
    "HiamMakeSurfaces",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/hiam_make_surfaces"]],
        "subject_name": str,
        "structure": HiamMakeSurfacesStructure,
    },
)

@dataclasses.dataclass
class HiamMakeSurfacesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def hiam_make_surfaces_params(
    subject_name: str,
    structure: HiamMakeSurfacesStructure,
) -> HiamMakeSurfaces:
    """
    Build parameters.

    Args:
        subject_name: Subject name for which surfaces are to be created
        structure: Structure for which surfaces will be created. Valid values are\
            RA, LA, RH, and LH.

    Returns:
        Parameter dictionary.
    """
    params: HiamMakeSurfaces = {
        "@type": "freesurfer/hiam_make_surfaces",
        "subject_name": subject_name,
        "structure": structure,
    }
    return params

def hiam_make_surfaces_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HiamMakeSurfaces."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("structure", None) is None:
        raise StyxValidationError("`structure` must not be None")
    if not isinstance(params["structure"], str):
        raise StyxValidationError(f'`structure` has the wrong type: Received `{type(params["structure"])}` expected `HiamMakeSurfacesStructure`')
    if params["structure"] not in ["RA", "LA", "RH", "LH"]:
        raise StyxValidationError("Parameter `structure` must be one of [\"RA\", \"LA\", \"RH\", \"LH\"]")

def hiam_make_surfaces_cargs(params: HiamMakeSurfaces, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("hiam_make_surfaces")
    cargs.append(params["subject_name"])
    cargs.append(str(params["structure"]))
    return cargs

def hiam_make_surfaces_outputs(params: HiamMakeSurfaces, execution: Execution) -> HiamMakeSurfacesOutputs:
    """Build the HiamMakeSurfacesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return HiamMakeSurfacesOutputs(
        root=root_v,
    )

def hiam_make_surfaces_execute(params: HiamMakeSurfaces, runner: Runner | None = None) -> HiamMakeSurfacesOutputs:
    """
    hiam_make_surfaces

    Surface creation tool for specified brain structures

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    hiam_make_surfaces_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HIAM_MAKE_SURFACES_METADATA)
    execution.params(params)
    args = hiam_make_surfaces_cargs(params, execution)
    out = hiam_make_surfaces_outputs(params, execution)
    execution.run(args)
    return out

def hiam_make_surfaces(
    subject_name: str,
    structure: HiamMakeSurfacesStructure,
    runner: Runner | None = None,
) -> HiamMakeSurfacesOutputs:
    """
    hiam_make_surfaces

    Surface creation tool for specified brain structures

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name for which surfaces are to be created
        structure: Structure for which surfaces will be created. Valid values are\
            RA, LA, RH, and LH.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = hiam_make_surfaces_params(
        subject_name=subject_name,
        structure=structure,
    )
    return hiam_make_surfaces_execute(params, runner)

__all__ = [
    "HiamMakeSurfaces",
    "HiamMakeSurfacesOutputs",
    "HIAM_MAKE_SURFACES_METADATA",
    "hiam_make_surfaces_cargs",
    "hiam_make_surfaces_outputs",
    "hiam_make_surfaces_params",
    "hiam_make_surfaces_execute",
    "hiam_make_surfaces_validate",
    "hiam_make_surfaces",
]