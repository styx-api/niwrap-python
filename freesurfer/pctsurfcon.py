# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PCTSURFCON_METADATA = Metadata(
    id="pctsurfcon",
    name="pctsurfcon",
    package="freesurfer",
)

Pctsurfcon = typing.TypedDict(
    "Pctsurfcon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/pctsurfcon"]],
        "subject": str,
        "fsvol": typing.NotRequired[str],
        "outbase": typing.NotRequired[str],
        "lh_only": typing.NotRequired[bool],
        "rh_only": typing.NotRequired[bool],
        "gm_proj_frac": typing.NotRequired[float],
        "gm_proj_abs": typing.NotRequired[float],
        "wm_proj_abs": typing.NotRequired[float],
        "neg": typing.NotRequired[bool],
        "no_mask": typing.NotRequired[bool],
        "pial": typing.NotRequired[bool],
        "tmp": typing.NotRequired[str],
        "nocleanup": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PctsurfconOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def pctsurfcon_params(
    subject: str,
    fsvol: str | None = None,
    outbase: str | None = None,
    lh_only: bool = False,
    rh_only: bool = False,
    gm_proj_frac: float | None = None,
    gm_proj_abs: float | None = None,
    wm_proj_abs: float | None = None,
    neg: bool = False,
    no_mask: bool = False,
    pial: bool = False,
    tmp: str | None = None,
    nocleanup: bool = False,
) -> Pctsurfcon:
    """
    Build parameters.

    Args:
        subject: FreeSurfer subject name
        fsvol: Use fsvol instead of rawavg
        outbase: Use outbase instead of w-g.pct (?h.w-g.pct.mgh)
        lh_only: Compute left hemisphere only
        rh_only: Compute right hemisphere only
        gm_proj_frac: GM projection fraction (default 0.3)
        gm_proj_abs: GM projection distance (default is to use frac)
        wm_proj_abs: WM projection distance (default is 1 mm)
        neg: Compute G-W instead of W-G
        no_mask: Do not mask out non-cortical regions
        pial: Use pial surface as base to compute gray/CSF contrast
        tmp: Temporary directory (implies --nocleanup)
        nocleanup: Do not delete temporary files

    Returns:
        Parameter dictionary.
    """
    params: Pctsurfcon = {
        "@type": "freesurfer/pctsurfcon",
        "subject": subject,
        "lh_only": lh_only,
        "rh_only": rh_only,
        "neg": neg,
        "no_mask": no_mask,
        "pial": pial,
        "nocleanup": nocleanup,
    }
    if fsvol is not None:
        params["fsvol"] = fsvol
    if outbase is not None:
        params["outbase"] = outbase
    if gm_proj_frac is not None:
        params["gm_proj_frac"] = gm_proj_frac
    if gm_proj_abs is not None:
        params["gm_proj_abs"] = gm_proj_abs
    if wm_proj_abs is not None:
        params["wm_proj_abs"] = wm_proj_abs
    if tmp is not None:
        params["tmp"] = tmp
    return params

def pctsurfcon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Pctsurfcon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("fsvol", None) is not None:
        if not isinstance(params["fsvol"], str):
            raise StyxValidationError(f'`fsvol` has the wrong type: Received `{type(params["fsvol"])}` expected `str`')
    if params.get("outbase", None) is not None:
        if not isinstance(params["outbase"], str):
            raise StyxValidationError(f'`outbase` has the wrong type: Received `{type(params["outbase"])}` expected `str`')
    if params.get("lh_only", None) is not None:
        if not isinstance(params["lh_only"], bool):
            raise StyxValidationError(f'`lh_only` has the wrong type: Received `{type(params["lh_only"])}` expected `bool`')
    if params.get("rh_only", None) is not None:
        if not isinstance(params["rh_only"], bool):
            raise StyxValidationError(f'`rh_only` has the wrong type: Received `{type(params["rh_only"])}` expected `bool`')
    if params.get("gm_proj_frac", None) is not None:
        if not isinstance(params["gm_proj_frac"], (float, int)):
            raise StyxValidationError(f'`gm_proj_frac` has the wrong type: Received `{type(params["gm_proj_frac"])}` expected `float`')
    if params.get("gm_proj_abs", None) is not None:
        if not isinstance(params["gm_proj_abs"], (float, int)):
            raise StyxValidationError(f'`gm_proj_abs` has the wrong type: Received `{type(params["gm_proj_abs"])}` expected `float`')
    if params.get("wm_proj_abs", None) is not None:
        if not isinstance(params["wm_proj_abs"], (float, int)):
            raise StyxValidationError(f'`wm_proj_abs` has the wrong type: Received `{type(params["wm_proj_abs"])}` expected `float`')
    if params.get("neg", None) is not None:
        if not isinstance(params["neg"], bool):
            raise StyxValidationError(f'`neg` has the wrong type: Received `{type(params["neg"])}` expected `bool`')
    if params.get("no_mask", None) is not None:
        if not isinstance(params["no_mask"], bool):
            raise StyxValidationError(f'`no_mask` has the wrong type: Received `{type(params["no_mask"])}` expected `bool`')
    if params.get("pial", None) is not None:
        if not isinstance(params["pial"], bool):
            raise StyxValidationError(f'`pial` has the wrong type: Received `{type(params["pial"])}` expected `bool`')
    if params.get("tmp", None) is not None:
        if not isinstance(params["tmp"], str):
            raise StyxValidationError(f'`tmp` has the wrong type: Received `{type(params["tmp"])}` expected `str`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')

def pctsurfcon_cargs(params: Pctsurfcon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("pctsurfcon")
    cargs.append("-s")
    cargs.append(params["subject"])
    v_0 = params.get("fsvol")
    if v_0 is not None:
        cargs.append("--fsvol")
        cargs.append(v_0)
    v_1 = params.get("outbase")
    if v_1 is not None:
        cargs.append("--b")
        cargs.append(v_1)
    if params.get("lh_only"):
        cargs.append("--lh-only")
    if params.get("rh_only"):
        cargs.append("--rh-only")
    v_2 = params.get("gm_proj_frac")
    if v_2 is not None:
        cargs.append("--gm-proj-frac")
        cargs.append(str(v_2))
    v_3 = params.get("gm_proj_abs")
    if v_3 is not None:
        cargs.append("--gm-proj-abs")
        cargs.append(str(v_3))
    v_4 = params.get("wm_proj_abs")
    if v_4 is not None:
        cargs.append("--wm-proj-abs")
        cargs.append(str(v_4))
    if params.get("neg"):
        cargs.append("--neg")
    if params.get("no_mask"):
        cargs.append("--no-mask")
    if params.get("pial"):
        cargs.append("--pial")
    v_5 = params.get("tmp")
    if v_5 is not None:
        cargs.append("--tmp")
        cargs.append(v_5)
    if params.get("nocleanup"):
        cargs.append("--nocleanup")
    return cargs

def pctsurfcon_outputs(params: Pctsurfcon, execution: Execution) -> PctsurfconOutputs:
    """Build the PctsurfconOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PctsurfconOutputs(
        root=root_v,
    )

def pctsurfcon_execute(params: Pctsurfcon, runner: Runner | None = None) -> PctsurfconOutputs:
    """
    pctsurfcon

    Compute surface-wise gray/white matter contrast.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    pctsurfcon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PCTSURFCON_METADATA)
    execution.params(params)
    args = pctsurfcon_cargs(params, execution)
    out = pctsurfcon_outputs(params, execution)
    execution.run(args)
    return out

def pctsurfcon(
    subject: str,
    fsvol: str | None = None,
    outbase: str | None = None,
    lh_only: bool = False,
    rh_only: bool = False,
    gm_proj_frac: float | None = None,
    gm_proj_abs: float | None = None,
    wm_proj_abs: float | None = None,
    neg: bool = False,
    no_mask: bool = False,
    pial: bool = False,
    tmp: str | None = None,
    nocleanup: bool = False,
    runner: Runner | None = None,
) -> PctsurfconOutputs:
    """
    pctsurfcon

    Compute surface-wise gray/white matter contrast.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: FreeSurfer subject name
        fsvol: Use fsvol instead of rawavg
        outbase: Use outbase instead of w-g.pct (?h.w-g.pct.mgh)
        lh_only: Compute left hemisphere only
        rh_only: Compute right hemisphere only
        gm_proj_frac: GM projection fraction (default 0.3)
        gm_proj_abs: GM projection distance (default is to use frac)
        wm_proj_abs: WM projection distance (default is 1 mm)
        neg: Compute G-W instead of W-G
        no_mask: Do not mask out non-cortical regions
        pial: Use pial surface as base to compute gray/CSF contrast
        tmp: Temporary directory (implies --nocleanup)
        nocleanup: Do not delete temporary files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = pctsurfcon_params(
        subject=subject,
        fsvol=fsvol,
        outbase=outbase,
        lh_only=lh_only,
        rh_only=rh_only,
        gm_proj_frac=gm_proj_frac,
        gm_proj_abs=gm_proj_abs,
        wm_proj_abs=wm_proj_abs,
        neg=neg,
        no_mask=no_mask,
        pial=pial,
        tmp=tmp,
        nocleanup=nocleanup,
    )
    return pctsurfcon_execute(params, runner)

__all__ = [
    "Pctsurfcon",
    "PctsurfconOutputs",
    "PCTSURFCON_METADATA",
    "pctsurfcon_cargs",
    "pctsurfcon_outputs",
    "pctsurfcon_params",
    "pctsurfcon_execute",
    "pctsurfcon_validate",
    "pctsurfcon",
]