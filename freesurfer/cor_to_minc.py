# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COR_TO_MINC_METADATA = Metadata(
    id="cor_to_minc",
    name="cor_to_minc",
    package="freesurfer",
)

CorToMinc = typing.TypedDict(
    "CorToMinc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/cor_to_minc"]],
        "cor_directory": str,
        "minc_file": str,
    },
)

@dataclasses.dataclass
class CorToMincOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_minc: OutputPathType
    """Converted MINC file"""

def cor_to_minc_params(
    cor_directory: str,
    minc_file: str,
) -> CorToMinc:
    """
    Build parameters.

    Args:
        cor_directory: Directory containing COR files
        minc_file: Output MINC file name

    Returns:
        Parameter dictionary.
    """
    params: CorToMinc = {
        "@type": "freesurfer/cor_to_minc",
        "cor_directory": cor_directory,
        "minc_file": minc_file,
    }
    return params

def cor_to_minc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CorToMinc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cor_directory", None) is None:
        raise StyxValidationError("`cor_directory` must not be None")
    if not isinstance(params["cor_directory"], str):
        raise StyxValidationError(f'`cor_directory` has the wrong type: Received `{type(params["cor_directory"])}` expected `str`')
    if params.get("minc_file", None) is None:
        raise StyxValidationError("`minc_file` must not be None")
    if not isinstance(params["minc_file"], str):
        raise StyxValidationError(f'`minc_file` has the wrong type: Received `{type(params["minc_file"])}` expected `str`')

def cor_to_minc_cargs(params: CorToMinc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cor_to_minc")
    cargs.append(params["cor_directory"])
    cargs.append(params["minc_file"])
    return cargs

def cor_to_minc_outputs(params: CorToMinc, execution: Execution) -> CorToMincOutputs:
    """Build the CorToMincOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_minc_v: OutputPathType = execution.output_file(params["minc_file"])
    return CorToMincOutputs(
        root=root_v,
        output_minc=output_minc_v,
    )

def cor_to_minc_execute(params: CorToMinc, runner: Runner | None = None) -> CorToMincOutputs:
    """
    cor_to_minc

    Converts a directory of COR files to a MINC file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cor_to_minc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COR_TO_MINC_METADATA)
    execution.params(params)
    args = cor_to_minc_cargs(params, execution)
    out = cor_to_minc_outputs(params, execution)
    execution.run(args)
    return out

def cor_to_minc(
    cor_directory: str,
    minc_file: str,
    runner: Runner | None = None,
) -> CorToMincOutputs:
    """
    cor_to_minc

    Converts a directory of COR files to a MINC file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        cor_directory: Directory containing COR files
        minc_file: Output MINC file name
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cor_to_minc_params(
        cor_directory=cor_directory,
        minc_file=minc_file,
    )
    return cor_to_minc_execute(params, runner)

__all__ = [
    "CorToMinc",
    "CorToMincOutputs",
    "COR_TO_MINC_METADATA",
    "cor_to_minc_cargs",
    "cor_to_minc_outputs",
    "cor_to_minc_params",
    "cor_to_minc_execute",
    "cor_to_minc_validate",
    "cor_to_minc",
]