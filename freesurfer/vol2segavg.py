# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOL2SEGAVG_METADATA = Metadata(
    id="vol2segavg",
    name="vol2segavg",
    package="freesurfer",
)

Vol2segavg = typing.TypedDict(
    "Vol2segavg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vol2segavg"]],
        "output_file": str,
        "input_volume": InputPathType,
        "registration": str,
        "segmentation_file": InputPathType,
        "aparc_aseg_flag": typing.NotRequired[bool],
        "subject_id": typing.NotRequired[str],
        "segmentation_id": typing.NotRequired[list[float]],
        "multiply_value": typing.NotRequired[float],
        "no_bb_flag": typing.NotRequired[bool],
        "erode_value": typing.NotRequired[float],
        "dilate_value": typing.NotRequired[float],
        "wm_flag": typing.NotRequired[bool],
        "vcsf_flag": typing.NotRequired[bool],
        "xcsf_flag": typing.NotRequired[bool],
        "remove_mean_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Vol2segavgOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file containing the computed average"""

def vol2segavg_params(
    output_file: str,
    input_volume: InputPathType,
    registration: str,
    segmentation_file: InputPathType,
    aparc_aseg_flag: bool = False,
    subject_id: str | None = None,
    segmentation_id: list[float] | None = None,
    multiply_value: float | None = None,
    no_bb_flag: bool = False,
    erode_value: float | None = None,
    dilate_value: float | None = None,
    wm_flag: bool = False,
    vcsf_flag: bool = False,
    xcsf_flag: bool = False,
    remove_mean_flag: bool = False,
) -> Vol2segavg:
    """
    Build parameters.

    Args:
        output_file: Output file, can be .txt or a binary (e.g., .nii, .mgh)
        input_volume: Input volume file (e.g., vol.nii)
        registration: Registration file, can be reg.dat or use --regheader
        segmentation_file: Segmentation file (e.g., seg.mgz)
        aparc_aseg_flag: Use aparc+aseg flag
        subject_id: Subject ID, may be needed if --reg not supplied
        segmentation_id: Segmentation ID(s). Multiple IDs can be supplied.
        multiply_value: Multiply input by MulVal
        no_bb_flag: Do not use bounding box
        erode_value: Erode segmentation
        dilate_value: Dilate segmentation
        wm_flag: Sets segid to 2, 41, 7, 46, 251, 252, 253, 254, 255, 77, 78, 79\
            and use aparc+aseg
        vcsf_flag: Sets segid to 4, 5, 43, 44, 31, 63 and use aparc+aseg
        xcsf_flag: Sets segid to 257 and use apas+head
        remove_mean_flag: Remove mean from time course

    Returns:
        Parameter dictionary.
    """
    params: Vol2segavg = {
        "@type": "freesurfer/vol2segavg",
        "output_file": output_file,
        "input_volume": input_volume,
        "registration": registration,
        "segmentation_file": segmentation_file,
        "aparc_aseg_flag": aparc_aseg_flag,
        "no_bb_flag": no_bb_flag,
        "wm_flag": wm_flag,
        "vcsf_flag": vcsf_flag,
        "xcsf_flag": xcsf_flag,
        "remove_mean_flag": remove_mean_flag,
    }
    if subject_id is not None:
        params["subject_id"] = subject_id
    if segmentation_id is not None:
        params["segmentation_id"] = segmentation_id
    if multiply_value is not None:
        params["multiply_value"] = multiply_value
    if erode_value is not None:
        params["erode_value"] = erode_value
    if dilate_value is not None:
        params["dilate_value"] = dilate_value
    return params

def vol2segavg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Vol2segavg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("registration", None) is None:
        raise StyxValidationError("`registration` must not be None")
    if not isinstance(params["registration"], str):
        raise StyxValidationError(f'`registration` has the wrong type: Received `{type(params["registration"])}` expected `str`')
    if params.get("segmentation_file", None) is None:
        raise StyxValidationError("`segmentation_file` must not be None")
    if not isinstance(params["segmentation_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segmentation_file` has the wrong type: Received `{type(params["segmentation_file"])}` expected `InputPathType`')
    if params.get("aparc_aseg_flag", None) is not None:
        if not isinstance(params["aparc_aseg_flag"], bool):
            raise StyxValidationError(f'`aparc_aseg_flag` has the wrong type: Received `{type(params["aparc_aseg_flag"])}` expected `bool`')
    if params.get("subject_id", None) is not None:
        if not isinstance(params["subject_id"], str):
            raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("segmentation_id", None) is not None:
        if not isinstance(params["segmentation_id"], list):
            raise StyxValidationError(f'`segmentation_id` has the wrong type: Received `{type(params["segmentation_id"])}` expected `list[float]`')
        for e in params["segmentation_id"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`segmentation_id` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("multiply_value", None) is not None:
        if not isinstance(params["multiply_value"], (float, int)):
            raise StyxValidationError(f'`multiply_value` has the wrong type: Received `{type(params["multiply_value"])}` expected `float`')
    if params.get("no_bb_flag", None) is not None:
        if not isinstance(params["no_bb_flag"], bool):
            raise StyxValidationError(f'`no_bb_flag` has the wrong type: Received `{type(params["no_bb_flag"])}` expected `bool`')
    if params.get("erode_value", None) is not None:
        if not isinstance(params["erode_value"], (float, int)):
            raise StyxValidationError(f'`erode_value` has the wrong type: Received `{type(params["erode_value"])}` expected `float`')
    if params.get("dilate_value", None) is not None:
        if not isinstance(params["dilate_value"], (float, int)):
            raise StyxValidationError(f'`dilate_value` has the wrong type: Received `{type(params["dilate_value"])}` expected `float`')
    if params.get("wm_flag", None) is not None:
        if not isinstance(params["wm_flag"], bool):
            raise StyxValidationError(f'`wm_flag` has the wrong type: Received `{type(params["wm_flag"])}` expected `bool`')
    if params.get("vcsf_flag", None) is not None:
        if not isinstance(params["vcsf_flag"], bool):
            raise StyxValidationError(f'`vcsf_flag` has the wrong type: Received `{type(params["vcsf_flag"])}` expected `bool`')
    if params.get("xcsf_flag", None) is not None:
        if not isinstance(params["xcsf_flag"], bool):
            raise StyxValidationError(f'`xcsf_flag` has the wrong type: Received `{type(params["xcsf_flag"])}` expected `bool`')
    if params.get("remove_mean_flag", None) is not None:
        if not isinstance(params["remove_mean_flag"], bool):
            raise StyxValidationError(f'`remove_mean_flag` has the wrong type: Received `{type(params["remove_mean_flag"])}` expected `bool`')

def vol2segavg_cargs(params: Vol2segavg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vol2segavg")
    cargs.append("--o")
    cargs.append(params["output_file"])
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--reg")
    cargs.append(params["registration"])
    cargs.append("--seg")
    cargs.append(execution.input_file(params["segmentation_file"]))
    if params.get("aparc_aseg_flag"):
        cargs.append("--aparc+aseg")
    v_0 = params.get("subject_id")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    v_1 = params.get("segmentation_id")
    if v_1 is not None:
        cargs.append("--segid")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("multiply_value")
    if v_2 is not None:
        cargs.append("--mul")
        cargs.append(str(v_2))
    if params.get("no_bb_flag"):
        cargs.append("--no-bb")
    v_3 = params.get("erode_value")
    if v_3 is not None:
        cargs.append("--erode")
        cargs.append(str(v_3))
    v_4 = params.get("dilate_value")
    if v_4 is not None:
        cargs.append("--dilate")
        cargs.append(str(v_4))
    if params.get("wm_flag"):
        cargs.append("--wm")
    if params.get("vcsf_flag"):
        cargs.append("--vcsf")
    if params.get("xcsf_flag"):
        cargs.append("--xcsf")
    if params.get("remove_mean_flag"):
        cargs.append("--remove-mean")
    return cargs

def vol2segavg_outputs(params: Vol2segavg, execution: Execution) -> Vol2segavgOutputs:
    """Build the Vol2segavgOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return Vol2segavgOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def vol2segavg_execute(params: Vol2segavg, runner: Runner | None = None) -> Vol2segavgOutputs:
    """
    vol2segavg

    Computes the average of a volume inside a given segment of a segmentation resampling the input volume to the segmentation space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vol2segavg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOL2SEGAVG_METADATA)
    execution.params(params)
    args = vol2segavg_cargs(params, execution)
    out = vol2segavg_outputs(params, execution)
    execution.run(args)
    return out

def vol2segavg(
    output_file: str,
    input_volume: InputPathType,
    registration: str,
    segmentation_file: InputPathType,
    aparc_aseg_flag: bool = False,
    subject_id: str | None = None,
    segmentation_id: list[float] | None = None,
    multiply_value: float | None = None,
    no_bb_flag: bool = False,
    erode_value: float | None = None,
    dilate_value: float | None = None,
    wm_flag: bool = False,
    vcsf_flag: bool = False,
    xcsf_flag: bool = False,
    remove_mean_flag: bool = False,
    runner: Runner | None = None,
) -> Vol2segavgOutputs:
    """
    vol2segavg

    Computes the average of a volume inside a given segment of a segmentation resampling the input volume to the segmentation space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_file: Output file, can be .txt or a binary (e.g., .nii, .mgh)
        input_volume: Input volume file (e.g., vol.nii)
        registration: Registration file, can be reg.dat or use --regheader
        segmentation_file: Segmentation file (e.g., seg.mgz)
        aparc_aseg_flag: Use aparc+aseg flag
        subject_id: Subject ID, may be needed if --reg not supplied
        segmentation_id: Segmentation ID(s). Multiple IDs can be supplied.
        multiply_value: Multiply input by MulVal
        no_bb_flag: Do not use bounding box
        erode_value: Erode segmentation
        dilate_value: Dilate segmentation
        wm_flag: Sets segid to 2, 41, 7, 46, 251, 252, 253, 254, 255, 77, 78, 79\
            and use aparc+aseg
        vcsf_flag: Sets segid to 4, 5, 43, 44, 31, 63 and use aparc+aseg
        xcsf_flag: Sets segid to 257 and use apas+head
        remove_mean_flag: Remove mean from time course
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vol2segavg_params(
        output_file=output_file,
        input_volume=input_volume,
        registration=registration,
        segmentation_file=segmentation_file,
        aparc_aseg_flag=aparc_aseg_flag,
        subject_id=subject_id,
        segmentation_id=segmentation_id,
        multiply_value=multiply_value,
        no_bb_flag=no_bb_flag,
        erode_value=erode_value,
        dilate_value=dilate_value,
        wm_flag=wm_flag,
        vcsf_flag=vcsf_flag,
        xcsf_flag=xcsf_flag,
        remove_mean_flag=remove_mean_flag,
    )
    return vol2segavg_execute(params, runner)

__all__ = [
    "Vol2segavg",
    "Vol2segavgOutputs",
    "VOL2SEGAVG_METADATA",
    "vol2segavg_cargs",
    "vol2segavg_outputs",
    "vol2segavg_params",
    "vol2segavg_execute",
    "vol2segavg_validate",
    "vol2segavg",
]