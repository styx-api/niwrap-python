# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GCA_AMBIGUOUS_METADATA = Metadata(
    id="mri_gca_ambiguous",
    name="mri_gca_ambiguous",
    package="freesurfer",
)

MriGcaAmbiguous = typing.TypedDict(
    "MriGcaAmbiguous",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gca_ambiguous"]],
        "gca_file": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriGcaAmbiguousOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType
    """Resulting MR image file with computed ambiguity measure"""

def mri_gca_ambiguous_params(
    gca_file: InputPathType,
    output_volume: str,
) -> MriGcaAmbiguous:
    """
    Build parameters.

    Args:
        gca_file: The input GCA file
        output_volume: The output MR image file

    Returns:
        Parameter dictionary.
    """
    params: MriGcaAmbiguous = {
        "@type": "freesurfer/mri_gca_ambiguous",
        "gca_file": gca_file,
        "output_volume": output_volume,
    }
    return params

def mri_gca_ambiguous_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGcaAmbiguous."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gca_file", None) is None:
        raise StyxValidationError("`gca_file` must not be None")
    if not isinstance(params["gca_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_gca_ambiguous_cargs(params: MriGcaAmbiguous, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gca_ambiguous")
    cargs.append(execution.input_file(params["gca_file"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_gca_ambiguous_outputs(params: MriGcaAmbiguous, execution: Execution) -> MriGcaAmbiguousOutputs:
    """Build the MriGcaAmbiguousOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriGcaAmbiguousOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def mri_gca_ambiguous_execute(params: MriGcaAmbiguous, runner: Runner | None = None) -> MriGcaAmbiguousOutputs:
    """
    mri_gca_ambiguous

    This program computes an ambiguity measure across a GCA and outputs an MR image of it

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gca_ambiguous_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GCA_AMBIGUOUS_METADATA)
    execution.params(params)
    args = mri_gca_ambiguous_cargs(params, execution)
    out = mri_gca_ambiguous_outputs(params, execution)
    execution.run(args)
    return out

def mri_gca_ambiguous(
    gca_file: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriGcaAmbiguousOutputs:
    """
    mri_gca_ambiguous

    This program computes an ambiguity measure across a GCA and outputs an MR image of it

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        gca_file: The input GCA file
        output_volume: The output MR image file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gca_ambiguous_params(
        gca_file=gca_file,
        output_volume=output_volume,
    )
    return mri_gca_ambiguous_execute(params, runner)

__all__ = [
    "MriGcaAmbiguous",
    "MriGcaAmbiguousOutputs",
    "MRI_GCA_AMBIGUOUS_METADATA",
    "mri_gca_ambiguous_cargs",
    "mri_gca_ambiguous_outputs",
    "mri_gca_ambiguous_params",
    "mri_gca_ambiguous_execute",
    "mri_gca_ambiguous_validate",
    "mri_gca_ambiguous",
]