# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_COMPUTE_VOLUME_FRACTIONS_METADATA = Metadata(
    id="mris_compute_volume_fractions",
    name="mris_compute_volume_fractions",
    package="freesurfer",
)

MrisComputeVolumeFractions = typing.TypedDict(
    "MrisComputeVolumeFractions",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_compute_volume_fractions"]],
        "volume_file": InputPathType,
        "surface_file": InputPathType,
        "accuracy": float,
        "output_file": str,
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisComputeVolumeFractionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """The output volume file containing the computed fractions."""

def mris_compute_volume_fractions_params(
    volume_file: InputPathType,
    surface_file: InputPathType,
    accuracy: float,
    output_file: str,
    debug: bool = False,
    checkopts: bool = False,
) -> MrisComputeVolumeFractions:
    """
    Build parameters.

    Args:
        volume_file: Input volume file.
        surface_file: Input surface file.
        accuracy: Required accuracy.
        output_file: Output volume file for the fractions.
        debug: Turn on debugging.
        checkopts: Don't run anything, just check options and exit.

    Returns:
        Parameter dictionary.
    """
    params: MrisComputeVolumeFractions = {
        "@type": "freesurfer/mris_compute_volume_fractions",
        "volume_file": volume_file,
        "surface_file": surface_file,
        "accuracy": accuracy,
        "output_file": output_file,
        "debug": debug,
        "checkopts": checkopts,
    }
    return params

def mris_compute_volume_fractions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisComputeVolumeFractions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_file", None) is None:
        raise StyxValidationError("`volume_file` must not be None")
    if not isinstance(params["volume_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_file` has the wrong type: Received `{type(params["volume_file"])}` expected `InputPathType`')
    if params.get("surface_file", None) is None:
        raise StyxValidationError("`surface_file` must not be None")
    if not isinstance(params["surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_file` has the wrong type: Received `{type(params["surface_file"])}` expected `InputPathType`')
    if params.get("accuracy", None) is None:
        raise StyxValidationError("`accuracy` must not be None")
    if not isinstance(params["accuracy"], (float, int)):
        raise StyxValidationError(f'`accuracy` has the wrong type: Received `{type(params["accuracy"])}` expected `float`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')

def mris_compute_volume_fractions_cargs(params: MrisComputeVolumeFractions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_compute_volume_fractions")
    cargs.append("--vol")
    cargs.append(execution.input_file(params["volume_file"]))
    cargs.append("--surf")
    cargs.append(execution.input_file(params["surface_file"]))
    cargs.append("--acc")
    cargs.append(str(params["accuracy"]))
    cargs.append("--out")
    cargs.append(params["output_file"])
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    return cargs

def mris_compute_volume_fractions_outputs(params: MrisComputeVolumeFractions, execution: Execution) -> MrisComputeVolumeFractionsOutputs:
    """Build the MrisComputeVolumeFractionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MrisComputeVolumeFractionsOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mris_compute_volume_fractions_execute(params: MrisComputeVolumeFractions, runner: Runner | None = None) -> MrisComputeVolumeFractionsOutputs:
    """
    mris_compute_volume_fractions

    Computes volume fractions based on a given surface and volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_compute_volume_fractions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_COMPUTE_VOLUME_FRACTIONS_METADATA)
    execution.params(params)
    args = mris_compute_volume_fractions_cargs(params, execution)
    out = mris_compute_volume_fractions_outputs(params, execution)
    execution.run(args)
    return out

def mris_compute_volume_fractions(
    volume_file: InputPathType,
    surface_file: InputPathType,
    accuracy: float,
    output_file: str,
    debug: bool = False,
    checkopts: bool = False,
    runner: Runner | None = None,
) -> MrisComputeVolumeFractionsOutputs:
    """
    mris_compute_volume_fractions

    Computes volume fractions based on a given surface and volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume_file: Input volume file.
        surface_file: Input surface file.
        accuracy: Required accuracy.
        output_file: Output volume file for the fractions.
        debug: Turn on debugging.
        checkopts: Don't run anything, just check options and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_compute_volume_fractions_params(
        volume_file=volume_file,
        surface_file=surface_file,
        accuracy=accuracy,
        output_file=output_file,
        debug=debug,
        checkopts=checkopts,
    )
    return mris_compute_volume_fractions_execute(params, runner)

__all__ = [
    "MrisComputeVolumeFractions",
    "MrisComputeVolumeFractionsOutputs",
    "MRIS_COMPUTE_VOLUME_FRACTIONS_METADATA",
    "mris_compute_volume_fractions_cargs",
    "mris_compute_volume_fractions_outputs",
    "mris_compute_volume_fractions_params",
    "mris_compute_volume_fractions_execute",
    "mris_compute_volume_fractions_validate",
    "mris_compute_volume_fractions",
]