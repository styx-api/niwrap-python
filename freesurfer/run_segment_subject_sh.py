# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RUN_SEGMENT_SUBJECT_SH_METADATA = Metadata(
    id="run_SegmentSubject.sh",
    name="run_SegmentSubject.sh",
    package="freesurfer",
)

RunSegmentSubjectSh = typing.TypedDict(
    "RunSegmentSubjectSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/run_SegmentSubject.sh"]],
        "deployedMCRroot": str,
        "arguments": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RunSegmentSubjectShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """The generated output file from the segmentation process."""

def run_segment_subject_sh_params(
    deployedMCRroot: str,
    arguments: str | None = None,
) -> RunSegmentSubjectSh:
    """
    Build parameters.

    Args:
        deployedMCRroot: The root directory of the deployed MATLAB Compiler\
            Runtime.
        arguments: Additional arguments to be passed to the SegmentSubject script.

    Returns:
        Parameter dictionary.
    """
    params: RunSegmentSubjectSh = {
        "@type": "freesurfer/run_SegmentSubject.sh",
        "deployedMCRroot": deployedMCRroot,
    }
    if arguments is not None:
        params["arguments"] = arguments
    return params

def run_segment_subject_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RunSegmentSubjectSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("deployedMCRroot", None) is None:
        raise StyxValidationError("`deployedMCRroot` must not be None")
    if not isinstance(params["deployedMCRroot"], str):
        raise StyxValidationError(f'`deployedMCRroot` has the wrong type: Received `{type(params["deployedMCRroot"])}` expected `str`')
    if params.get("arguments", None) is not None:
        if not isinstance(params["arguments"], str):
            raise StyxValidationError(f'`arguments` has the wrong type: Received `{type(params["arguments"])}` expected `str`')

def run_segment_subject_sh_cargs(params: RunSegmentSubjectSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("run_SegmentSubject.sh")
    cargs.append(params["deployedMCRroot"])
    v_0 = params.get("arguments")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def run_segment_subject_sh_outputs(params: RunSegmentSubjectSh, execution: Execution) -> RunSegmentSubjectShOutputs:
    """Build the RunSegmentSubjectShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file("outputFileName")
    return RunSegmentSubjectShOutputs(
        root=root_v,
        output=output_v,
    )

def run_segment_subject_sh_execute(params: RunSegmentSubjectSh, runner: Runner | None = None) -> RunSegmentSubjectShOutputs:
    """
    run_SegmentSubject.sh

    A command-line tool for subject segmentation in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    run_segment_subject_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RUN_SEGMENT_SUBJECT_SH_METADATA)
    execution.params(params)
    args = run_segment_subject_sh_cargs(params, execution)
    out = run_segment_subject_sh_outputs(params, execution)
    execution.run(args)
    return out

def run_segment_subject_sh(
    deployedMCRroot: str,
    arguments: str | None = None,
    runner: Runner | None = None,
) -> RunSegmentSubjectShOutputs:
    """
    run_SegmentSubject.sh

    A command-line tool for subject segmentation in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        deployedMCRroot: The root directory of the deployed MATLAB Compiler\
            Runtime.
        arguments: Additional arguments to be passed to the SegmentSubject script.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = run_segment_subject_sh_params(
        deployedMCRroot=deployedMCRroot,
        arguments=arguments,
    )
    return run_segment_subject_sh_execute(params, runner)

__all__ = [
    "RunSegmentSubjectSh",
    "RunSegmentSubjectShOutputs",
    "RUN_SEGMENT_SUBJECT_SH_METADATA",
    "run_segment_subject_sh_cargs",
    "run_segment_subject_sh_outputs",
    "run_segment_subject_sh_params",
    "run_segment_subject_sh_execute",
    "run_segment_subject_sh_validate",
    "run_segment_subject_sh",
]