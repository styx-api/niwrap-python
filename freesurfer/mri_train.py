# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_TRAIN_METADATA = Metadata(
    id="mri_train",
    name="mri_train",
    package="freesurfer",
)

MriTrain = typing.TypedDict(
    "MriTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_train"]],
        "training_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MriTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file_generated: OutputPathType
    """Output file generated by mri_train."""

def mri_train_params(
    training_file: InputPathType,
    output_file: str,
) -> MriTrain:
    """
    Build parameters.

    Args:
        training_file: Input training file name
        output_file: Output file

    Returns:
        Parameter dictionary.
    """
    params: MriTrain = {
        "@type": "freesurfer/mri_train",
        "training_file": training_file,
        "output_file": output_file,
    }
    return params

def mri_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("training_file", None) is None:
        raise StyxValidationError("`training_file` must not be None")
    if not isinstance(params["training_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`training_file` has the wrong type: Received `{type(params["training_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_train_cargs(params: MriTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_train")
    cargs.append(execution.input_file(params["training_file"]))
    cargs.append(params["output_file"])
    return cargs

def mri_train_outputs(params: MriTrain, execution: Execution) -> MriTrainOutputs:
    """Build the MriTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_generated_v: OutputPathType = execution.output_file(params["output_file"])
    return MriTrainOutputs(
        root=root_v,
        output_file_generated=output_file_generated_v,
    )

def mri_train_execute(params: MriTrain, runner: Runner | None = None) -> MriTrainOutputs:
    """
    mri_train

    Tool for training with MRI data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_TRAIN_METADATA)
    execution.params(params)
    args = mri_train_cargs(params, execution)
    out = mri_train_outputs(params, execution)
    execution.run(args)
    return out

def mri_train(
    training_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> MriTrainOutputs:
    """
    mri_train

    Tool for training with MRI data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        training_file: Input training file name
        output_file: Output file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_train_params(
        training_file=training_file,
        output_file=output_file,
    )
    return mri_train_execute(params, runner)

__all__ = [
    "MriTrain",
    "MriTrainOutputs",
    "MRI_TRAIN_METADATA",
    "mri_train_cargs",
    "mri_train_outputs",
    "mri_train_params",
    "mri_train_execute",
    "mri_train_validate",
    "mri_train",
]