# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REREGISTER_SUBJECT_MIXED_METADATA = Metadata(
    id="reregister_subject_mixed",
    name="reregister_subject_mixed",
    package="freesurfer",
)

ReregisterSubjectMixed = typing.TypedDict(
    "ReregisterSubjectMixed",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reregister_subject_mixed"]],
        "input_volume": InputPathType,
        "output_directory": str,
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class ReregisterSubjectMixedOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    control_points: OutputPathType
    """Transformed control points"""
    intensity_normalized: OutputPathType
    """Intensity normalized output"""
    log_file: OutputPathType
    """Log file for talairach processing"""

def reregister_subject_mixed_params(
    input_volume: InputPathType,
    output_directory: str,
    threads: float | None = None,
) -> ReregisterSubjectMixed:
    """
    Build parameters.

    Args:
        input_volume: Input volume file path
        output_directory: Output directory for transformed control points and\
            intensity normalized files
        threads: Number of threads available to mri_em_register for OpenMP

    Returns:
        Parameter dictionary.
    """
    params: ReregisterSubjectMixed = {
        "@type": "freesurfer/reregister_subject_mixed",
        "input_volume": input_volume,
        "output_directory": output_directory,
    }
    if threads is not None:
        params["threads"] = threads
    return params

def reregister_subject_mixed_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ReregisterSubjectMixed."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def reregister_subject_mixed_cargs(params: ReregisterSubjectMixed, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reregister_subject_mixed")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_directory"])
    v_0 = params.get("threads")
    if v_0 is not None:
        cargs.append(str(v_0))
    return cargs

def reregister_subject_mixed_outputs(params: ReregisterSubjectMixed, execution: Execution) -> ReregisterSubjectMixedOutputs:
    """Build the ReregisterSubjectMixedOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    control_points_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/mri/fsamples')
    intensity_normalized_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/mri/norm')
    log_file_v: OutputPathType = execution.output_file("talairach.log")
    return ReregisterSubjectMixedOutputs(
        root=root_v,
        control_points=control_points_v,
        intensity_normalized=intensity_normalized_v,
        log_file=log_file_v,
    )

def reregister_subject_mixed_execute(params: ReregisterSubjectMixed, runner: Runner | None = None) -> ReregisterSubjectMixedOutputs:
    """
    reregister_subject_mixed

    Tool for re-registering a subject's MRI volumes using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reregister_subject_mixed_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REREGISTER_SUBJECT_MIXED_METADATA)
    execution.params(params)
    args = reregister_subject_mixed_cargs(params, execution)
    out = reregister_subject_mixed_outputs(params, execution)
    execution.run(args)
    return out

def reregister_subject_mixed(
    input_volume: InputPathType,
    output_directory: str,
    threads: float | None = None,
    runner: Runner | None = None,
) -> ReregisterSubjectMixedOutputs:
    """
    reregister_subject_mixed

    Tool for re-registering a subject's MRI volumes using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file path
        output_directory: Output directory for transformed control points and\
            intensity normalized files
        threads: Number of threads available to mri_em_register for OpenMP
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reregister_subject_mixed_params(
        input_volume=input_volume,
        output_directory=output_directory,
        threads=threads,
    )
    return reregister_subject_mixed_execute(params, runner)

__all__ = [
    "ReregisterSubjectMixed",
    "ReregisterSubjectMixedOutputs",
    "REREGISTER_SUBJECT_MIXED_METADATA",
    "reregister_subject_mixed_cargs",
    "reregister_subject_mixed_outputs",
    "reregister_subject_mixed_params",
    "reregister_subject_mixed_execute",
    "reregister_subject_mixed_validate",
    "reregister_subject_mixed",
]