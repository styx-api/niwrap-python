# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MORPHOLOGY_METADATA = Metadata(
    id="mri_morphology",
    name="mri_morphology",
    package="freesurfer",
)

MriMorphologyOperation = typing.Literal["open"] | typing.Literal["close"] | typing.Literal["dilate"] | typing.Literal["erode"] | typing.Literal["mode"] | typing.Literal["fill_holes"] | typing.Literal["erode_bottom"] | typing.Literal["dilate_thresh"] | typing.Literal["erode_thresh"]

MriMorphology = typing.TypedDict(
    "MriMorphology",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_morphology"]],
        "input_volume": InputPathType,
        "operation": MriMorphologyOperation,
        "number_iter": int,
        "output_volume": str,
        "label_option": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriMorphologyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The result of the morphological operation applied to the input volume."""

def mri_morphology_params(
    input_volume: InputPathType,
    operation: MriMorphologyOperation,
    number_iter: int,
    output_volume: str,
    label_option: float | None = None,
) -> MriMorphology:
    """
    Build parameters.

    Args:
        input_volume: Input volume file on which operations are to be applied.
        operation: Morphological operation to be performed. Options include: open,\
            close, dilate, erode, mode, fill_holes, erode_bottom, dilate_thresh,\
            erode_thresh.
        number_iter: Number of iterations to apply the operation.
        output_volume: Output volume file to store the results of the operation.
        label_option: Only apply operations to the specified label instead of all\
            nonzero voxels.

    Returns:
        Parameter dictionary.
    """
    params: MriMorphology = {
        "@type": "freesurfer/mri_morphology",
        "input_volume": input_volume,
        "operation": operation,
        "number_iter": number_iter,
        "output_volume": output_volume,
    }
    if label_option is not None:
        params["label_option"] = label_option
    return params

def mri_morphology_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMorphology."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `MriMorphologyOperation`')
    if params["operation"] not in ["open", "close", "dilate", "erode", "mode", "fill_holes", "erode_bottom", "dilate_thresh", "erode_thresh"]:
        raise StyxValidationError("Parameter `operation` must be one of [\"open\", \"close\", \"dilate\", \"erode\", \"mode\", \"fill_holes\", \"erode_bottom\", \"dilate_thresh\", \"erode_thresh\"]")
    if params.get("number_iter", None) is None:
        raise StyxValidationError("`number_iter` must not be None")
    if not isinstance(params["number_iter"], int):
        raise StyxValidationError(f'`number_iter` has the wrong type: Received `{type(params["number_iter"])}` expected `int`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("label_option", None) is not None:
        if not isinstance(params["label_option"], (float, int)):
            raise StyxValidationError(f'`label_option` has the wrong type: Received `{type(params["label_option"])}` expected `float`')

def mri_morphology_cargs(params: MriMorphology, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_morphology")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["operation"]))
    cargs.append(str(params["number_iter"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("label_option")
    if v_0 is not None:
        cargs.append("-l")
        cargs.append(str(v_0))
    return cargs

def mri_morphology_outputs(params: MriMorphology, execution: Execution) -> MriMorphologyOutputs:
    """Build the MriMorphologyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriMorphologyOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_morphology_execute(params: MriMorphology, runner: Runner | None = None) -> MriMorphologyOutputs:
    """
    mri_morphology

    MRI Morphology Tool - performs various morphological operations on a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_morphology_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MORPHOLOGY_METADATA)
    execution.params(params)
    args = mri_morphology_cargs(params, execution)
    out = mri_morphology_outputs(params, execution)
    execution.run(args)
    return out

def mri_morphology(
    input_volume: InputPathType,
    operation: MriMorphologyOperation,
    number_iter: int,
    output_volume: str,
    label_option: float | None = None,
    runner: Runner | None = None,
) -> MriMorphologyOutputs:
    """
    mri_morphology

    MRI Morphology Tool - performs various morphological operations on a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file on which operations are to be applied.
        operation: Morphological operation to be performed. Options include: open,\
            close, dilate, erode, mode, fill_holes, erode_bottom, dilate_thresh,\
            erode_thresh.
        number_iter: Number of iterations to apply the operation.
        output_volume: Output volume file to store the results of the operation.
        label_option: Only apply operations to the specified label instead of all\
            nonzero voxels.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_morphology_params(
        input_volume=input_volume,
        operation=operation,
        number_iter=number_iter,
        output_volume=output_volume,
        label_option=label_option,
    )
    return mri_morphology_execute(params, runner)

__all__ = [
    "MriMorphology",
    "MriMorphologyOutputs",
    "MRI_MORPHOLOGY_METADATA",
    "mri_morphology_cargs",
    "mri_morphology_outputs",
    "mri_morphology_params",
    "mri_morphology_execute",
    "mri_morphology_validate",
    "mri_morphology",
]