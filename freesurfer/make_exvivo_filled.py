# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_EXVIVO_FILLED_METADATA = Metadata(
    id="make_exvivo_filled",
    name="make_exvivo_filled",
    package="freesurfer",
)

MakeExvivoFilled = typing.TypedDict(
    "MakeExvivoFilled",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_exvivo_filled"]],
        "subject_name": str,
        "input_samseg": InputPathType,
        "input_intensity_vol": InputPathType,
        "hemi_both": str,
    },
)

@dataclasses.dataclass
class MakeExvivoFilledOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def make_exvivo_filled_params(
    subject_name: str,
    input_samseg: InputPathType,
    input_intensity_vol: InputPathType,
    hemi_both: str,
) -> MakeExvivoFilled:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject.
        input_samseg: Input SAMSEG (Segmentation Analysis of MRI brain images).
        input_intensity_vol: Input intensity volume.
        hemi_both: Specify hemisphere or both.

    Returns:
        Parameter dictionary.
    """
    params: MakeExvivoFilled = {
        "@type": "freesurfer/make_exvivo_filled",
        "subject_name": subject_name,
        "input_samseg": input_samseg,
        "input_intensity_vol": input_intensity_vol,
        "hemi_both": hemi_both,
    }
    return params

def make_exvivo_filled_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeExvivoFilled."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("input_samseg", None) is None:
        raise StyxValidationError("`input_samseg` must not be None")
    if not isinstance(params["input_samseg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_samseg` has the wrong type: Received `{type(params["input_samseg"])}` expected `InputPathType`')
    if params.get("input_intensity_vol", None) is None:
        raise StyxValidationError("`input_intensity_vol` must not be None")
    if not isinstance(params["input_intensity_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_intensity_vol` has the wrong type: Received `{type(params["input_intensity_vol"])}` expected `InputPathType`')
    if params.get("hemi_both", None) is None:
        raise StyxValidationError("`hemi_both` must not be None")
    if not isinstance(params["hemi_both"], str):
        raise StyxValidationError(f'`hemi_both` has the wrong type: Received `{type(params["hemi_both"])}` expected `str`')

def make_exvivo_filled_cargs(params: MakeExvivoFilled, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_exvivo_filled")
    cargs.append(params["subject_name"])
    cargs.append(execution.input_file(params["input_samseg"]))
    cargs.append(execution.input_file(params["input_intensity_vol"]))
    cargs.append(params["hemi_both"])
    return cargs

def make_exvivo_filled_outputs(params: MakeExvivoFilled, execution: Execution) -> MakeExvivoFilledOutputs:
    """Build the MakeExvivoFilledOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MakeExvivoFilledOutputs(
        root=root_v,
    )

def make_exvivo_filled_execute(params: MakeExvivoFilled, runner: Runner | None = None) -> MakeExvivoFilledOutputs:
    """
    make_exvivo_filled

    A command-line tool for generating filled ex vivo brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_exvivo_filled_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_EXVIVO_FILLED_METADATA)
    execution.params(params)
    args = make_exvivo_filled_cargs(params, execution)
    out = make_exvivo_filled_outputs(params, execution)
    execution.run(args)
    return out

def make_exvivo_filled(
    subject_name: str,
    input_samseg: InputPathType,
    input_intensity_vol: InputPathType,
    hemi_both: str,
    runner: Runner | None = None,
) -> MakeExvivoFilledOutputs:
    """
    make_exvivo_filled

    A command-line tool for generating filled ex vivo brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject.
        input_samseg: Input SAMSEG (Segmentation Analysis of MRI brain images).
        input_intensity_vol: Input intensity volume.
        hemi_both: Specify hemisphere or both.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_exvivo_filled_params(
        subject_name=subject_name,
        input_samseg=input_samseg,
        input_intensity_vol=input_intensity_vol,
        hemi_both=hemi_both,
    )
    return make_exvivo_filled_execute(params, runner)

__all__ = [
    "MakeExvivoFilled",
    "MakeExvivoFilledOutputs",
    "MAKE_EXVIVO_FILLED_METADATA",
    "make_exvivo_filled_cargs",
    "make_exvivo_filled_outputs",
    "make_exvivo_filled_params",
    "make_exvivo_filled_execute",
    "make_exvivo_filled_validate",
    "make_exvivo_filled",
]