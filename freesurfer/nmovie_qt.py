# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NMOVIE_QT_METADATA = Metadata(
    id="nmovie_qt",
    name="nmovie_qt",
    package="freesurfer",
)

NmovieQt = typing.TypedDict(
    "NmovieQt",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/nmovie_qt"]],
        "images": list[InputPathType],
    },
)

@dataclasses.dataclass
class NmovieQtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def nmovie_qt_params(
    images: list[InputPathType],
) -> NmovieQt:
    """
    Build parameters.

    Args:
        images: Input image files to be displayed. Multiple files can be provided.

    Returns:
        Parameter dictionary.
    """
    params: NmovieQt = {
        "@type": "freesurfer/nmovie_qt",
        "images": images,
    }
    return params

def nmovie_qt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NmovieQt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("images", None) is None:
        raise StyxValidationError("`images` must not be None")
    if not isinstance(params["images"], list):
        raise StyxValidationError(f'`images` has the wrong type: Received `{type(params["images"])}` expected `list[InputPathType]`')
    for e in params["images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def nmovie_qt_cargs(params: NmovieQt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("nmovie_qt")
    for item0 in params["images"]:
        cargs.append(execution.input_file(item0))
    return cargs

def nmovie_qt_outputs(params: NmovieQt, execution: Execution) -> NmovieQtOutputs:
    """Build the NmovieQtOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return NmovieQtOutputs(
        root=root_v,
    )

def nmovie_qt_execute(params: NmovieQt, runner: Runner | None = None) -> NmovieQtOutputs:
    """
    nmovie_qt

    An image viewer using Qt for displaying images in sequence.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    nmovie_qt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NMOVIE_QT_METADATA)
    execution.params(params)
    args = nmovie_qt_cargs(params, execution)
    out = nmovie_qt_outputs(params, execution)
    execution.run(args)
    return out

def nmovie_qt(
    images: list[InputPathType],
    runner: Runner | None = None,
) -> NmovieQtOutputs:
    """
    nmovie_qt

    An image viewer using Qt for displaying images in sequence.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        images: Input image files to be displayed. Multiple files can be provided.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = nmovie_qt_params(
        images=images,
    )
    return nmovie_qt_execute(params, runner)

__all__ = [
    "NmovieQt",
    "NmovieQtOutputs",
    "NMOVIE_QT_METADATA",
    "nmovie_qt_cargs",
    "nmovie_qt_outputs",
    "nmovie_qt_params",
    "nmovie_qt_execute",
    "nmovie_qt_validate",
    "nmovie_qt",
]