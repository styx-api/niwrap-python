# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HISTO_SYNTHESIZE_METADATA = Metadata(
    id="histo_synthesize",
    name="histo_synthesize",
    package="freesurfer",
)

HistoSynthesize = typing.TypedDict(
    "HistoSynthesize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/histo_synthesize"]],
        "mri_volume": InputPathType,
        "histo_volume": InputPathType,
        "synthetic_histo": str,
    },
)

@dataclasses.dataclass
class HistoSynthesizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_synthetic_histo: OutputPathType
    """Output file of the synthetic histo volume."""

def histo_synthesize_params(
    mri_volume: InputPathType,
    histo_volume: InputPathType,
    synthetic_histo: str,
) -> HistoSynthesize:
    """
    Build parameters.

    Args:
        mri_volume: Input MRI volume file.
        histo_volume: Input HISTO volume file.
        synthetic_histo: Output synthetic histo volume file.

    Returns:
        Parameter dictionary.
    """
    params: HistoSynthesize = {
        "@type": "freesurfer/histo_synthesize",
        "mri_volume": mri_volume,
        "histo_volume": histo_volume,
        "synthetic_histo": synthetic_histo,
    }
    return params

def histo_synthesize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HistoSynthesize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mri_volume", None) is None:
        raise StyxValidationError("`mri_volume` must not be None")
    if not isinstance(params["mri_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mri_volume` has the wrong type: Received `{type(params["mri_volume"])}` expected `InputPathType`')
    if params.get("histo_volume", None) is None:
        raise StyxValidationError("`histo_volume` must not be None")
    if not isinstance(params["histo_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`histo_volume` has the wrong type: Received `{type(params["histo_volume"])}` expected `InputPathType`')
    if params.get("synthetic_histo", None) is None:
        raise StyxValidationError("`synthetic_histo` must not be None")
    if not isinstance(params["synthetic_histo"], str):
        raise StyxValidationError(f'`synthetic_histo` has the wrong type: Received `{type(params["synthetic_histo"])}` expected `str`')

def histo_synthesize_cargs(params: HistoSynthesize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("histo_synthesize")
    cargs.append(execution.input_file(params["mri_volume"]))
    cargs.append(execution.input_file(params["histo_volume"]))
    cargs.append(params["synthetic_histo"])
    return cargs

def histo_synthesize_outputs(params: HistoSynthesize, execution: Execution) -> HistoSynthesizeOutputs:
    """Build the HistoSynthesizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_synthetic_histo_v: OutputPathType = execution.output_file(params["synthetic_histo"])
    return HistoSynthesizeOutputs(
        root=root_v,
        output_synthetic_histo=output_synthetic_histo_v,
    )

def histo_synthesize_execute(params: HistoSynthesize, runner: Runner | None = None) -> HistoSynthesizeOutputs:
    """
    histo_synthesize

    Tool for synthesizing histology-like volumes from MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    histo_synthesize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HISTO_SYNTHESIZE_METADATA)
    execution.params(params)
    args = histo_synthesize_cargs(params, execution)
    out = histo_synthesize_outputs(params, execution)
    execution.run(args)
    return out

def histo_synthesize(
    mri_volume: InputPathType,
    histo_volume: InputPathType,
    synthetic_histo: str,
    runner: Runner | None = None,
) -> HistoSynthesizeOutputs:
    """
    histo_synthesize

    Tool for synthesizing histology-like volumes from MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mri_volume: Input MRI volume file.
        histo_volume: Input HISTO volume file.
        synthetic_histo: Output synthetic histo volume file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = histo_synthesize_params(
        mri_volume=mri_volume,
        histo_volume=histo_volume,
        synthetic_histo=synthetic_histo,
    )
    return histo_synthesize_execute(params, runner)

__all__ = [
    "HistoSynthesize",
    "HistoSynthesizeOutputs",
    "HISTO_SYNTHESIZE_METADATA",
    "histo_synthesize_cargs",
    "histo_synthesize_outputs",
    "histo_synthesize_params",
    "histo_synthesize_execute",
    "histo_synthesize_validate",
    "histo_synthesize",
]