# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CONGEAL_METADATA = Metadata(
    id="mris_congeal",
    name="mris_congeal",
    package="freesurfer",
)

MrisCongeal = typing.TypedDict(
    "MrisCongeal",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_congeal"]],
        "input_surface_name": str,
        "hemi": str,
        "subjects": list[str],
        "output_surface_name": str,
        "subjects_dir": typing.NotRequired[str],
        "disable_rigid_alignment": typing.NotRequired[bool],
        "disable_sulc_alignment": typing.NotRequired[bool],
        "smoothwm_curv": typing.NotRequired[bool],
        "jacobian_output": typing.NotRequired[str],
        "distance_term": typing.NotRequired[float],
        "manual_label": typing.NotRequired[list[str]],
        "addframe": typing.NotRequired[list[str]],
        "overlay": typing.NotRequired[list[str]],
        "overlay_dir": typing.NotRequired[str],
        "target_subject": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisCongealOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType
    """Output surface"""

def mris_congeal_params(
    input_surface_name: str,
    hemi: str,
    subjects: list[str],
    output_surface_name: str,
    subjects_dir: str | None = None,
    disable_rigid_alignment: bool = False,
    disable_sulc_alignment: bool = False,
    smoothwm_curv: bool = False,
    jacobian_output: str | None = None,
    distance_term: float | None = None,
    manual_label: list[str] | None = None,
    addframe: list[str] | None = None,
    overlay: list[str] | None = None,
    overlay_dir: str | None = None,
    target_subject: bool = False,
) -> MrisCongeal:
    """
    Build parameters.

    Args:
        input_surface_name: Input surface name
        hemi: Hemisphere (hemi)
        subjects: List of subjects
        output_surface_name: Output surface name
        subjects_dir: Subjects directory
        disable_rigid_alignment: Disable initial rigid alignment
        disable_sulc_alignment: Disable initial sulc alignment
        smoothwm_curv: Use smoothwm curvature for final alignment
        jacobian_output: Write-out jacobian overlay data to file
        distance_term: Specify distance term
        manual_label: Specify a manual label to align with atlas label
        addframe: Add frame with specified parameters
        overlay: Specify overlay surface values and number of averages
        overlay_dir: Specify overlay directory
        target_subject: Target specifies a subject's surface, not a template file

    Returns:
        Parameter dictionary.
    """
    params: MrisCongeal = {
        "@type": "freesurfer/mris_congeal",
        "input_surface_name": input_surface_name,
        "hemi": hemi,
        "subjects": subjects,
        "output_surface_name": output_surface_name,
        "disable_rigid_alignment": disable_rigid_alignment,
        "disable_sulc_alignment": disable_sulc_alignment,
        "smoothwm_curv": smoothwm_curv,
        "target_subject": target_subject,
    }
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if jacobian_output is not None:
        params["jacobian_output"] = jacobian_output
    if distance_term is not None:
        params["distance_term"] = distance_term
    if manual_label is not None:
        params["manual_label"] = manual_label
    if addframe is not None:
        params["addframe"] = addframe
    if overlay is not None:
        params["overlay"] = overlay
    if overlay_dir is not None:
        params["overlay_dir"] = overlay_dir
    return params

def mris_congeal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCongeal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface_name", None) is None:
        raise StyxValidationError("`input_surface_name` must not be None")
    if not isinstance(params["input_surface_name"], str):
        raise StyxValidationError(f'`input_surface_name` has the wrong type: Received `{type(params["input_surface_name"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_surface_name", None) is None:
        raise StyxValidationError("`output_surface_name` must not be None")
    if not isinstance(params["output_surface_name"], str):
        raise StyxValidationError(f'`output_surface_name` has the wrong type: Received `{type(params["output_surface_name"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("disable_rigid_alignment", None) is not None:
        if not isinstance(params["disable_rigid_alignment"], bool):
            raise StyxValidationError(f'`disable_rigid_alignment` has the wrong type: Received `{type(params["disable_rigid_alignment"])}` expected `bool`')
    if params.get("disable_sulc_alignment", None) is not None:
        if not isinstance(params["disable_sulc_alignment"], bool):
            raise StyxValidationError(f'`disable_sulc_alignment` has the wrong type: Received `{type(params["disable_sulc_alignment"])}` expected `bool`')
    if params.get("smoothwm_curv", None) is not None:
        if not isinstance(params["smoothwm_curv"], bool):
            raise StyxValidationError(f'`smoothwm_curv` has the wrong type: Received `{type(params["smoothwm_curv"])}` expected `bool`')
    if params.get("jacobian_output", None) is not None:
        if not isinstance(params["jacobian_output"], str):
            raise StyxValidationError(f'`jacobian_output` has the wrong type: Received `{type(params["jacobian_output"])}` expected `str`')
    if params.get("distance_term", None) is not None:
        if not isinstance(params["distance_term"], (float, int)):
            raise StyxValidationError(f'`distance_term` has the wrong type: Received `{type(params["distance_term"])}` expected `float`')
    if params.get("manual_label", None) is not None:
        if not isinstance(params["manual_label"], list):
            raise StyxValidationError(f'`manual_label` has the wrong type: Received `{type(params["manual_label"])}` expected `list[str]`')
        if len(params["manual_label"]) > 3:
            raise StyxValidationError("Parameter `manual_label` must contain at most 3 elements")
        for e_2 in params["manual_label"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`manual_label` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("addframe", None) is not None:
        if not isinstance(params["addframe"], list):
            raise StyxValidationError(f'`addframe` has the wrong type: Received `{type(params["addframe"])}` expected `list[str]`')
        if len(params["addframe"]) > 4:
            raise StyxValidationError("Parameter `addframe` must contain at most 4 elements")
        for e_3 in params["addframe"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`addframe` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("overlay", None) is not None:
        if not isinstance(params["overlay"], list):
            raise StyxValidationError(f'`overlay` has the wrong type: Received `{type(params["overlay"])}` expected `list[str]`')
        if len(params["overlay"]) > 2:
            raise StyxValidationError("Parameter `overlay` must contain at most 2 elements")
        for e_4 in params["overlay"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`overlay` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("overlay_dir", None) is not None:
        if not isinstance(params["overlay_dir"], str):
            raise StyxValidationError(f'`overlay_dir` has the wrong type: Received `{type(params["overlay_dir"])}` expected `str`')
    if params.get("target_subject", None) is not None:
        if not isinstance(params["target_subject"], bool):
            raise StyxValidationError(f'`target_subject` has the wrong type: Received `{type(params["target_subject"])}` expected `bool`')

def mris_congeal_cargs(params: MrisCongeal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_congeal")
    cargs.append(params["input_surface_name"])
    cargs.append(params["hemi"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_surface_name"])
    v_0 = params.get("subjects_dir")
    if v_0 is not None:
        cargs.append("-SDIR")
        cargs.append(v_0)
    if params.get("disable_rigid_alignment"):
        cargs.append("-norot")
    if params.get("disable_sulc_alignment"):
        cargs.append("-nosulc")
    if params.get("smoothwm_curv"):
        cargs.append("-curv")
    v_1 = params.get("jacobian_output")
    if v_1 is not None:
        cargs.append("-jacobian")
        cargs.append(v_1)
    v_2 = params.get("distance_term")
    if v_2 is not None:
        cargs.append("-dist")
        cargs.append(str(v_2))
    v_3 = params.get("manual_label")
    if v_3 is not None:
        cargs.append("-l")
        for item1 in v_3:
            cargs.append(item1)
    v_4 = params.get("addframe")
    if v_4 is not None:
        cargs.append("-addframe")
        for item2 in v_4:
            cargs.append(item2)
    v_5 = params.get("overlay")
    if v_5 is not None:
        cargs.append("-overlay")
        for item3 in v_5:
            cargs.append(item3)
    v_6 = params.get("overlay_dir")
    if v_6 is not None:
        cargs.append("-overlay-dir")
        cargs.append(v_6)
    if params.get("target_subject"):
        cargs.append("-1")
    return cargs

def mris_congeal_outputs(params: MrisCongeal, execution: Execution) -> MrisCongealOutputs:
    """Build the MrisCongealOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_v: OutputPathType = execution.output_file(params["output_surface_name"])
    return MrisCongealOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def mris_congeal_execute(params: MrisCongeal, runner: Runner | None = None) -> MrisCongealOutputs:
    """
    mris_congeal

    Program that registers a set of input surfaces together and generates an atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_congeal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CONGEAL_METADATA)
    execution.params(params)
    args = mris_congeal_cargs(params, execution)
    out = mris_congeal_outputs(params, execution)
    execution.run(args)
    return out

def mris_congeal(
    input_surface_name: str,
    hemi: str,
    subjects: list[str],
    output_surface_name: str,
    subjects_dir: str | None = None,
    disable_rigid_alignment: bool = False,
    disable_sulc_alignment: bool = False,
    smoothwm_curv: bool = False,
    jacobian_output: str | None = None,
    distance_term: float | None = None,
    manual_label: list[str] | None = None,
    addframe: list[str] | None = None,
    overlay: list[str] | None = None,
    overlay_dir: str | None = None,
    target_subject: bool = False,
    runner: Runner | None = None,
) -> MrisCongealOutputs:
    """
    mris_congeal

    Program that registers a set of input surfaces together and generates an atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface_name: Input surface name
        hemi: Hemisphere (hemi)
        subjects: List of subjects
        output_surface_name: Output surface name
        subjects_dir: Subjects directory
        disable_rigid_alignment: Disable initial rigid alignment
        disable_sulc_alignment: Disable initial sulc alignment
        smoothwm_curv: Use smoothwm curvature for final alignment
        jacobian_output: Write-out jacobian overlay data to file
        distance_term: Specify distance term
        manual_label: Specify a manual label to align with atlas label
        addframe: Add frame with specified parameters
        overlay: Specify overlay surface values and number of averages
        overlay_dir: Specify overlay directory
        target_subject: Target specifies a subject's surface, not a template file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_congeal_params(
        input_surface_name=input_surface_name,
        hemi=hemi,
        subjects=subjects,
        output_surface_name=output_surface_name,
        subjects_dir=subjects_dir,
        disable_rigid_alignment=disable_rigid_alignment,
        disable_sulc_alignment=disable_sulc_alignment,
        smoothwm_curv=smoothwm_curv,
        jacobian_output=jacobian_output,
        distance_term=distance_term,
        manual_label=manual_label,
        addframe=addframe,
        overlay=overlay,
        overlay_dir=overlay_dir,
        target_subject=target_subject,
    )
    return mris_congeal_execute(params, runner)

__all__ = [
    "MrisCongeal",
    "MrisCongealOutputs",
    "MRIS_CONGEAL_METADATA",
    "mris_congeal_cargs",
    "mris_congeal_outputs",
    "mris_congeal_params",
    "mris_congeal_execute",
    "mris_congeal_validate",
    "mris_congeal",
]