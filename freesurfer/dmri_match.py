# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_MATCH_METADATA = Metadata(
    id="dmri_match",
    name="dmri_match",
    package="freesurfer",
)

DmriMatch = typing.TypedDict(
    "DmriMatch",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_match"]],
        "parcellation1": InputPathType,
        "parcellation2": InputPathType,
        "num_clusters": float,
        "clustering_path1": InputPathType,
        "clustering_path2": InputPathType,
        "labels": typing.NotRequired[bool],
        "euclidean": typing.NotRequired[bool],
        "bounding_box": typing.NotRequired[bool],
        "symmetry": typing.NotRequired[bool],
        "inter_hemi_ratio_removal": typing.NotRequired[str],
        "output": str,
    },
)

@dataclasses.dataclass
class DmriMatchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file from dmri_match"""

def dmri_match_params(
    parcellation1: InputPathType,
    parcellation2: InputPathType,
    num_clusters: float,
    clustering_path1: InputPathType,
    clustering_path2: InputPathType,
    output: str,
    labels: bool = False,
    euclidean: bool = False,
    bounding_box: bool = False,
    symmetry: bool = False,
    inter_hemi_ratio_removal: str | None = None,
) -> DmriMatch:
    """
    Build parameters.

    Args:
        parcellation1: First parcellation input file
        parcellation2: Second parcellation input file
        num_clusters: Number of clusters
        clustering_path1: First clustering path
        clustering_path2: Second clustering path
        output: Output file
        labels: Use labels (no additional input expected)
        euclidean: Use Euclidean distance for matching
        bounding_box: Use bounding box constraint (no additional input expected)
        symmetry: Use symmetry constraint (no additional input expected)
        inter_hemi_ratio_removal: Inter-hemispheric ratio cluster removal\
            constraint

    Returns:
        Parameter dictionary.
    """
    params: DmriMatch = {
        "@type": "freesurfer/dmri_match",
        "parcellation1": parcellation1,
        "parcellation2": parcellation2,
        "num_clusters": num_clusters,
        "clustering_path1": clustering_path1,
        "clustering_path2": clustering_path2,
        "output": output,
        "labels": labels,
        "euclidean": euclidean,
        "bounding_box": bounding_box,
        "symmetry": symmetry,
    }
    if inter_hemi_ratio_removal is not None:
        params["inter_hemi_ratio_removal"] = inter_hemi_ratio_removal
    return params

def dmri_match_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriMatch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("parcellation1", None) is None:
        raise StyxValidationError("`parcellation1` must not be None")
    if not isinstance(params["parcellation1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`parcellation1` has the wrong type: Received `{type(params["parcellation1"])}` expected `InputPathType`')
    if params.get("parcellation2", None) is None:
        raise StyxValidationError("`parcellation2` must not be None")
    if not isinstance(params["parcellation2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`parcellation2` has the wrong type: Received `{type(params["parcellation2"])}` expected `InputPathType`')
    if params.get("num_clusters", None) is None:
        raise StyxValidationError("`num_clusters` must not be None")
    if not isinstance(params["num_clusters"], (float, int)):
        raise StyxValidationError(f'`num_clusters` has the wrong type: Received `{type(params["num_clusters"])}` expected `float`')
    if params.get("clustering_path1", None) is None:
        raise StyxValidationError("`clustering_path1` must not be None")
    if not isinstance(params["clustering_path1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`clustering_path1` has the wrong type: Received `{type(params["clustering_path1"])}` expected `InputPathType`')
    if params.get("clustering_path2", None) is None:
        raise StyxValidationError("`clustering_path2` must not be None")
    if not isinstance(params["clustering_path2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`clustering_path2` has the wrong type: Received `{type(params["clustering_path2"])}` expected `InputPathType`')
    if params.get("labels", None) is not None:
        if not isinstance(params["labels"], bool):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `bool`')
    if params.get("euclidean", None) is not None:
        if not isinstance(params["euclidean"], bool):
            raise StyxValidationError(f'`euclidean` has the wrong type: Received `{type(params["euclidean"])}` expected `bool`')
    if params.get("bounding_box", None) is not None:
        if not isinstance(params["bounding_box"], bool):
            raise StyxValidationError(f'`bounding_box` has the wrong type: Received `{type(params["bounding_box"])}` expected `bool`')
    if params.get("symmetry", None) is not None:
        if not isinstance(params["symmetry"], bool):
            raise StyxValidationError(f'`symmetry` has the wrong type: Received `{type(params["symmetry"])}` expected `bool`')
    if params.get("inter_hemi_ratio_removal", None) is not None:
        if not isinstance(params["inter_hemi_ratio_removal"], str):
            raise StyxValidationError(f'`inter_hemi_ratio_removal` has the wrong type: Received `{type(params["inter_hemi_ratio_removal"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def dmri_match_cargs(params: DmriMatch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_match")
    cargs.append("-s1")
    cargs.append(execution.input_file(params["parcellation1"]))
    cargs.append("-s2")
    cargs.append(execution.input_file(params["parcellation2"]))
    cargs.append("-c")
    cargs.append(str(params["num_clusters"]))
    cargs.append("-h1")
    cargs.append(execution.input_file(params["clustering_path1"]))
    cargs.append("-h2")
    cargs.append(execution.input_file(params["clustering_path2"]))
    if params.get("labels"):
        cargs.append("-labels")
    if params.get("euclidean"):
        cargs.append("-euclid")
    if params.get("bounding_box"):
        cargs.append("-bb")
    if params.get("symmetry"):
        cargs.append("-sym")
    v_0 = params.get("inter_hemi_ratio_removal")
    if v_0 is not None:
        cargs.append(v_0)
    cargs.append("-o")
    cargs.append(params["output"])
    return cargs

def dmri_match_outputs(params: DmriMatch, execution: Execution) -> DmriMatchOutputs:
    """Build the DmriMatchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output"])
    return DmriMatchOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def dmri_match_execute(params: DmriMatch, runner: Runner | None = None) -> DmriMatchOutputs:
    """
    dmri_match

    Tool for matching diffusion MRI parcellations

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_match_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_MATCH_METADATA)
    execution.params(params)
    args = dmri_match_cargs(params, execution)
    out = dmri_match_outputs(params, execution)
    execution.run(args)
    return out

def dmri_match(
    parcellation1: InputPathType,
    parcellation2: InputPathType,
    num_clusters: float,
    clustering_path1: InputPathType,
    clustering_path2: InputPathType,
    output: str,
    labels: bool = False,
    euclidean: bool = False,
    bounding_box: bool = False,
    symmetry: bool = False,
    inter_hemi_ratio_removal: str | None = None,
    runner: Runner | None = None,
) -> DmriMatchOutputs:
    """
    dmri_match

    Tool for matching diffusion MRI parcellations

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        parcellation1: First parcellation input file
        parcellation2: Second parcellation input file
        num_clusters: Number of clusters
        clustering_path1: First clustering path
        clustering_path2: Second clustering path
        output: Output file
        labels: Use labels (no additional input expected)
        euclidean: Use Euclidean distance for matching
        bounding_box: Use bounding box constraint (no additional input expected)
        symmetry: Use symmetry constraint (no additional input expected)
        inter_hemi_ratio_removal: Inter-hemispheric ratio cluster removal\
            constraint
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_match_params(
        parcellation1=parcellation1,
        parcellation2=parcellation2,
        num_clusters=num_clusters,
        clustering_path1=clustering_path1,
        clustering_path2=clustering_path2,
        output=output,
        labels=labels,
        euclidean=euclidean,
        bounding_box=bounding_box,
        symmetry=symmetry,
        inter_hemi_ratio_removal=inter_hemi_ratio_removal,
    )
    return dmri_match_execute(params, runner)

__all__ = [
    "DmriMatch",
    "DmriMatchOutputs",
    "DMRI_MATCH_METADATA",
    "dmri_match_cargs",
    "dmri_match_outputs",
    "dmri_match_params",
    "dmri_match_execute",
    "dmri_match_validate",
    "dmri_match",
]