# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RECON_ALL_EXVIVO_METADATA = Metadata(
    id="recon-all-exvivo",
    name="recon-all-exvivo",
    package="freesurfer",
)

ReconAllExvivo = typing.TypedDict(
    "ReconAllExvivo",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/recon-all-exvivo"]],
        "subject_id": str,
        "hemisphere": typing.NotRequired[str],
        "nocerebellum": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ReconAllExvivoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def recon_all_exvivo_params(
    subject_id: str,
    hemisphere: str | None = None,
    nocerebellum: bool = False,
) -> ReconAllExvivo:
    """
    Build parameters.

    Args:
        subject_id: Subject identifier
        hemisphere: Specify hemisphere: -lh for left, -rh for right hemisphere
        nocerebellum: Do not process cerebellum

    Returns:
        Parameter dictionary.
    """
    params: ReconAllExvivo = {
        "@type": "freesurfer/recon-all-exvivo",
        "subject_id": subject_id,
        "nocerebellum": nocerebellum,
    }
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    return params

def recon_all_exvivo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ReconAllExvivo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("nocerebellum", None) is not None:
        if not isinstance(params["nocerebellum"], bool):
            raise StyxValidationError(f'`nocerebellum` has the wrong type: Received `{type(params["nocerebellum"])}` expected `bool`')

def recon_all_exvivo_cargs(params: ReconAllExvivo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("recon-all-exvivo")
    cargs.append("-s")
    cargs.append(params["subject_id"])
    v_0 = params.get("hemisphere")
    if v_0 is not None:
        cargs.append("-[lr]h")
        cargs.append(v_0)
    if params.get("nocerebellum"):
        cargs.append("-nocerebellum")
    return cargs

def recon_all_exvivo_outputs(params: ReconAllExvivo, execution: Execution) -> ReconAllExvivoOutputs:
    """Build the ReconAllExvivoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ReconAllExvivoOutputs(
        root=root_v,
    )

def recon_all_exvivo_execute(params: ReconAllExvivo, runner: Runner | None = None) -> ReconAllExvivoOutputs:
    """
    recon-all-exvivo

    A script to perform an ex vivo reconstruction with FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    recon_all_exvivo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RECON_ALL_EXVIVO_METADATA)
    execution.params(params)
    args = recon_all_exvivo_cargs(params, execution)
    out = recon_all_exvivo_outputs(params, execution)
    execution.run(args)
    return out

def recon_all_exvivo(
    subject_id: str,
    hemisphere: str | None = None,
    nocerebellum: bool = False,
    runner: Runner | None = None,
) -> ReconAllExvivoOutputs:
    """
    recon-all-exvivo

    A script to perform an ex vivo reconstruction with FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id: Subject identifier
        hemisphere: Specify hemisphere: -lh for left, -rh for right hemisphere
        nocerebellum: Do not process cerebellum
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = recon_all_exvivo_params(
        subject_id=subject_id,
        hemisphere=hemisphere,
        nocerebellum=nocerebellum,
    )
    return recon_all_exvivo_execute(params, runner)

__all__ = [
    "ReconAllExvivo",
    "ReconAllExvivoOutputs",
    "RECON_ALL_EXVIVO_METADATA",
    "recon_all_exvivo_cargs",
    "recon_all_exvivo_outputs",
    "recon_all_exvivo_params",
    "recon_all_exvivo_execute",
    "recon_all_exvivo_validate",
    "recon_all_exvivo",
]