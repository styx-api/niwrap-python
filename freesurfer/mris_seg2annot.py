# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SEG2ANNOT_METADATA = Metadata(
    id="mris_seg2annot",
    name="mris_seg2annot",
    package="freesurfer",
)

MrisSeg2annot = typing.TypedDict(
    "MrisSeg2annot",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_seg2annot"]],
        "surfseg": InputPathType,
        "colortable": typing.NotRequired[InputPathType],
        "auto_ctab": typing.NotRequired[str],
        "subject": str,
        "hemi": str,
        "output_annotation": str,
        "surf": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "debug_vertex": typing.NotRequired[float],
        "checkopts": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisSeg2annotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    annotation_file: OutputPathType
    """Output custom annotation file."""

def mris_seg2annot_params(
    surfseg: InputPathType,
    subject: str,
    hemi: str,
    output_annotation: str,
    colortable: InputPathType | None = None,
    auto_ctab: str | None = None,
    surf: str | None = None,
    debug: bool = False,
    debug_vertex: float | None = None,
    checkopts: bool = False,
    version: bool = False,
) -> MrisSeg2annot:
    """
    Build parameters.

    Args:
        surfseg: Volume-encoded surface segmentation. Values are indices into the\
            color table.
        subject: Subject name.
        hemi: Surface hemifield.
        output_annotation: Output annotation file. E.g., lh.aparc.annot
        colortable: Color table used to map segmentation index to name and color.
        auto_ctab: Create a random color table and optionally save it.
        surf: Surface name, default is white.
        debug: Turn on debugging.
        debug_vertex: Turn on debugging for vertex.
        checkopts: Don't run anything, just check options and exit.
        version: Print out version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MrisSeg2annot = {
        "@type": "freesurfer/mris_seg2annot",
        "surfseg": surfseg,
        "subject": subject,
        "hemi": hemi,
        "output_annotation": output_annotation,
        "debug": debug,
        "checkopts": checkopts,
        "version": version,
    }
    if colortable is not None:
        params["colortable"] = colortable
    if auto_ctab is not None:
        params["auto_ctab"] = auto_ctab
    if surf is not None:
        params["surf"] = surf
    if debug_vertex is not None:
        params["debug_vertex"] = debug_vertex
    return params

def mris_seg2annot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSeg2annot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surfseg", None) is None:
        raise StyxValidationError("`surfseg` must not be None")
    if not isinstance(params["surfseg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surfseg` has the wrong type: Received `{type(params["surfseg"])}` expected `InputPathType`')
    if params.get("colortable", None) is not None:
        if not isinstance(params["colortable"], (pathlib.Path, str)):
            raise StyxValidationError(f'`colortable` has the wrong type: Received `{type(params["colortable"])}` expected `InputPathType`')
    if params.get("auto_ctab", None) is not None:
        if not isinstance(params["auto_ctab"], str):
            raise StyxValidationError(f'`auto_ctab` has the wrong type: Received `{type(params["auto_ctab"])}` expected `str`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("output_annotation", None) is None:
        raise StyxValidationError("`output_annotation` must not be None")
    if not isinstance(params["output_annotation"], str):
        raise StyxValidationError(f'`output_annotation` has the wrong type: Received `{type(params["output_annotation"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("debug_vertex", None) is not None:
        if not isinstance(params["debug_vertex"], (float, int)):
            raise StyxValidationError(f'`debug_vertex` has the wrong type: Received `{type(params["debug_vertex"])}` expected `float`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mris_seg2annot_cargs(params: MrisSeg2annot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_seg2annot")
    cargs.append("--seg")
    cargs.append(execution.input_file(params["surfseg"]))
    v_0 = params.get("colortable")
    if v_0 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("auto_ctab")
    if v_1 is not None:
        cargs.append("--ctab-auto")
        cargs.append(v_1)
    cargs.append("--s")
    cargs.append(params["subject"])
    cargs.append("--h")
    cargs.append(params["hemi"])
    cargs.append("--o")
    cargs.append(params["output_annotation"])
    v_2 = params.get("surf")
    if v_2 is not None:
        cargs.append("--surf")
        cargs.append(v_2)
    if params.get("debug"):
        cargs.append("--debug")
    v_3 = params.get("debug_vertex")
    if v_3 is not None:
        cargs.append("--debug-vertex")
        cargs.append(str(v_3))
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mris_seg2annot_outputs(params: MrisSeg2annot, execution: Execution) -> MrisSeg2annotOutputs:
    """Build the MrisSeg2annotOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    annotation_file_v: OutputPathType = execution.output_file(params["output_annotation"])
    return MrisSeg2annotOutputs(
        root=root_v,
        annotation_file=annotation_file_v,
    )

def mris_seg2annot_execute(params: MrisSeg2annot, runner: Runner | None = None) -> MrisSeg2annotOutputs:
    """
    mris_seg2annot

    Converts a surface-based segmentation into a custom annotation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_seg2annot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SEG2ANNOT_METADATA)
    execution.params(params)
    args = mris_seg2annot_cargs(params, execution)
    out = mris_seg2annot_outputs(params, execution)
    execution.run(args)
    return out

def mris_seg2annot(
    surfseg: InputPathType,
    subject: str,
    hemi: str,
    output_annotation: str,
    colortable: InputPathType | None = None,
    auto_ctab: str | None = None,
    surf: str | None = None,
    debug: bool = False,
    debug_vertex: float | None = None,
    checkopts: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrisSeg2annotOutputs:
    """
    mris_seg2annot

    Converts a surface-based segmentation into a custom annotation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surfseg: Volume-encoded surface segmentation. Values are indices into the\
            color table.
        subject: Subject name.
        hemi: Surface hemifield.
        output_annotation: Output annotation file. E.g., lh.aparc.annot
        colortable: Color table used to map segmentation index to name and color.
        auto_ctab: Create a random color table and optionally save it.
        surf: Surface name, default is white.
        debug: Turn on debugging.
        debug_vertex: Turn on debugging for vertex.
        checkopts: Don't run anything, just check options and exit.
        version: Print out version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_seg2annot_params(
        surfseg=surfseg,
        subject=subject,
        hemi=hemi,
        output_annotation=output_annotation,
        colortable=colortable,
        auto_ctab=auto_ctab,
        surf=surf,
        debug=debug,
        debug_vertex=debug_vertex,
        checkopts=checkopts,
        version=version,
    )
    return mris_seg2annot_execute(params, runner)

__all__ = [
    "MrisSeg2annot",
    "MrisSeg2annotOutputs",
    "MRIS_SEG2ANNOT_METADATA",
    "mris_seg2annot_cargs",
    "mris_seg2annot_outputs",
    "mris_seg2annot_params",
    "mris_seg2annot_execute",
    "mris_seg2annot_validate",
    "mris_seg2annot",
]