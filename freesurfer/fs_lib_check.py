# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_LIB_CHECK_METADATA = Metadata(
    id="fs_lib_check",
    name="fs_lib_check",
    package="freesurfer",
)

FsLibCheck = typing.TypedDict(
    "FsLibCheck",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_lib_check"]],
        "use_ldconfig": typing.NotRequired[bool],
        "use_rpm": typing.NotRequired[bool],
        "show_help": typing.NotRequired[bool],
        "show_version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsLibCheckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fs_lib_check_params(
    use_ldconfig: bool = False,
    use_rpm: bool = False,
    show_help: bool = False,
    show_version: bool = False,
) -> FsLibCheck:
    """
    Build parameters.

    Args:
        use_ldconfig: Force use of 'ldconfig' to check for instances of required\
            system libraries. This is useful on rpm systems if system libraries\
            were installed from source code, and are thus not known to the system\
            rpm database.
        use_rpm: Force use of 'rpm -qa' to check for installed library packages. If\
            -r and -l are both specified concurrently, then behavior is\
            unspecified.
        show_help: Show this synopsis and exit.
        show_version: Show a version number and exit.

    Returns:
        Parameter dictionary.
    """
    params: FsLibCheck = {
        "@type": "freesurfer/fs_lib_check",
        "use_ldconfig": use_ldconfig,
        "use_rpm": use_rpm,
        "show_help": show_help,
        "show_version": show_version,
    }
    return params

def fs_lib_check_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsLibCheck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("use_ldconfig", None) is not None:
        if not isinstance(params["use_ldconfig"], bool):
            raise StyxValidationError(f'`use_ldconfig` has the wrong type: Received `{type(params["use_ldconfig"])}` expected `bool`')
    if params.get("use_rpm", None) is not None:
        if not isinstance(params["use_rpm"], bool):
            raise StyxValidationError(f'`use_rpm` has the wrong type: Received `{type(params["use_rpm"])}` expected `bool`')
    if params.get("show_help", None) is not None:
        if not isinstance(params["show_help"], bool):
            raise StyxValidationError(f'`show_help` has the wrong type: Received `{type(params["show_help"])}` expected `bool`')
    if params.get("show_version", None) is not None:
        if not isinstance(params["show_version"], bool):
            raise StyxValidationError(f'`show_version` has the wrong type: Received `{type(params["show_version"])}` expected `bool`')

def fs_lib_check_cargs(params: FsLibCheck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_lib_check")
    if params.get("use_ldconfig"):
        cargs.append("-l")
    if params.get("use_rpm"):
        cargs.append("-r")
    if params.get("show_help"):
        cargs.append("-h")
    if params.get("show_version"):
        cargs.append("-v")
    return cargs

def fs_lib_check_outputs(params: FsLibCheck, execution: Execution) -> FsLibCheckOutputs:
    """Build the FsLibCheckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsLibCheckOutputs(
        root=root_v,
    )

def fs_lib_check_execute(params: FsLibCheck, runner: Runner | None = None) -> FsLibCheckOutputs:
    """
    fs_lib_check

    Checks if the operating system has the necessary system libraries required to run FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_lib_check_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_LIB_CHECK_METADATA)
    execution.params(params)
    args = fs_lib_check_cargs(params, execution)
    out = fs_lib_check_outputs(params, execution)
    execution.run(args)
    return out

def fs_lib_check(
    use_ldconfig: bool = False,
    use_rpm: bool = False,
    show_help: bool = False,
    show_version: bool = False,
    runner: Runner | None = None,
) -> FsLibCheckOutputs:
    """
    fs_lib_check

    Checks if the operating system has the necessary system libraries required to run FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        use_ldconfig: Force use of 'ldconfig' to check for instances of required\
            system libraries. This is useful on rpm systems if system libraries\
            were installed from source code, and are thus not known to the system\
            rpm database.
        use_rpm: Force use of 'rpm -qa' to check for installed library packages. If\
            -r and -l are both specified concurrently, then behavior is\
            unspecified.
        show_help: Show this synopsis and exit.
        show_version: Show a version number and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_lib_check_params(
        use_ldconfig=use_ldconfig,
        use_rpm=use_rpm,
        show_help=show_help,
        show_version=show_version,
    )
    return fs_lib_check_execute(params, runner)

__all__ = [
    "FsLibCheck",
    "FsLibCheckOutputs",
    "FS_LIB_CHECK_METADATA",
    "fs_lib_check_cargs",
    "fs_lib_check_outputs",
    "fs_lib_check_params",
    "fs_lib_check_execute",
    "fs_lib_check_validate",
    "fs_lib_check",
]