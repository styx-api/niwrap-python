# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_BSET_METADATA = Metadata(
    id="dmri_bset",
    name="dmri_bset",
    package="freesurfer",
)

DmriBset = typing.TypedDict(
    "DmriBset",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_bset"]],
        "input_dwi": InputPathType,
        "output_dwi": str,
        "b_values": typing.NotRequired[list[float]],
        "btol": typing.NotRequired[float],
        "bsort": typing.NotRequired[bool],
        "bmax": typing.NotRequired[float],
        "input_b_table": typing.NotRequired[InputPathType],
        "input_g_table": typing.NotRequired[InputPathType],
        "output_b_table": typing.NotRequired[str],
        "output_g_table": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class DmriBsetOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dwi_file: OutputPathType
    """Output DWI series"""
    output_b_table_file: OutputPathType | None
    """Output b-value table"""
    output_g_table_file: OutputPathType | None
    """Output gradient table"""

def dmri_bset_params(
    input_dwi: InputPathType,
    output_dwi: str,
    b_values: list[float] | None = None,
    btol: float | None = None,
    bsort: bool = False,
    bmax: float | None = None,
    input_b_table: InputPathType | None = None,
    input_g_table: InputPathType | None = None,
    output_b_table: str | None = None,
    output_g_table: str | None = None,
) -> DmriBset:
    """
    Build parameters.

    Args:
        input_dwi: Input DWI series
        output_dwi: Output DWI series
        b_values: Extract one or more b-values
        btol: Tolerance around each single b-value (default: 0.05)
        bsort: Reorder output data by b-shell (default: maintain original order)
        bmax: Extract all b-values less than or equal to a maximum
        input_b_table: Input b-value table (default: input DWI base, .bvals\
            extension)
        input_g_table: Input gradient table (default: input DWI base, .bvecs\
            extension)
        output_b_table: Output b-value table (default: output DWI base, .bvals\
            extension)
        output_g_table: Output gradient table (default: output DWI base, .bvecs\
            extension)

    Returns:
        Parameter dictionary.
    """
    params: DmriBset = {
        "@type": "freesurfer/dmri_bset",
        "input_dwi": input_dwi,
        "output_dwi": output_dwi,
        "bsort": bsort,
    }
    if b_values is not None:
        params["b_values"] = b_values
    if btol is not None:
        params["btol"] = btol
    if bmax is not None:
        params["bmax"] = bmax
    if input_b_table is not None:
        params["input_b_table"] = input_b_table
    if input_g_table is not None:
        params["input_g_table"] = input_g_table
    if output_b_table is not None:
        params["output_b_table"] = output_b_table
    if output_g_table is not None:
        params["output_g_table"] = output_g_table
    return params

def dmri_bset_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriBset."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dwi", None) is None:
        raise StyxValidationError("`input_dwi` must not be None")
    if not isinstance(params["input_dwi"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dwi` has the wrong type: Received `{type(params["input_dwi"])}` expected `InputPathType`')
    if params.get("output_dwi", None) is None:
        raise StyxValidationError("`output_dwi` must not be None")
    if not isinstance(params["output_dwi"], str):
        raise StyxValidationError(f'`output_dwi` has the wrong type: Received `{type(params["output_dwi"])}` expected `str`')
    if params.get("b_values", None) is not None:
        if not isinstance(params["b_values"], list):
            raise StyxValidationError(f'`b_values` has the wrong type: Received `{type(params["b_values"])}` expected `list[float]`')
        for e in params["b_values"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`b_values` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("btol", None) is not None:
        if not isinstance(params["btol"], (float, int)):
            raise StyxValidationError(f'`btol` has the wrong type: Received `{type(params["btol"])}` expected `float`')
    if params.get("bsort", None) is not None:
        if not isinstance(params["bsort"], bool):
            raise StyxValidationError(f'`bsort` has the wrong type: Received `{type(params["bsort"])}` expected `bool`')
    if params.get("bmax", None) is not None:
        if not isinstance(params["bmax"], (float, int)):
            raise StyxValidationError(f'`bmax` has the wrong type: Received `{type(params["bmax"])}` expected `float`')
    if params.get("input_b_table", None) is not None:
        if not isinstance(params["input_b_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_b_table` has the wrong type: Received `{type(params["input_b_table"])}` expected `InputPathType`')
    if params.get("input_g_table", None) is not None:
        if not isinstance(params["input_g_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_g_table` has the wrong type: Received `{type(params["input_g_table"])}` expected `InputPathType`')
    if params.get("output_b_table", None) is not None:
        if not isinstance(params["output_b_table"], str):
            raise StyxValidationError(f'`output_b_table` has the wrong type: Received `{type(params["output_b_table"])}` expected `str`')
    if params.get("output_g_table", None) is not None:
        if not isinstance(params["output_g_table"], str):
            raise StyxValidationError(f'`output_g_table` has the wrong type: Received `{type(params["output_g_table"])}` expected `str`')

def dmri_bset_cargs(params: DmriBset, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_bset")
    cargs.append(execution.input_file(params["input_dwi"]))
    cargs.append(params["output_dwi"])
    v_0 = params.get("b_values")
    if v_0 is not None:
        cargs.append("--b")
        for item0 in v_0:
            cargs.append(str(item0))
    v_1 = params.get("btol")
    if v_1 is not None:
        cargs.append("--btol")
        cargs.append(str(v_1))
    if params.get("bsort"):
        cargs.append("--bsort")
    v_2 = params.get("bmax")
    if v_2 is not None:
        cargs.append("--bmax")
        cargs.append(str(v_2))
    v_3 = params.get("input_b_table")
    if v_3 is not None:
        cargs.append("--inb")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("input_g_table")
    if v_4 is not None:
        cargs.append("--ing")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("output_b_table")
    if v_5 is not None:
        cargs.append("--outb")
        cargs.append(v_5)
    v_6 = params.get("output_g_table")
    if v_6 is not None:
        cargs.append("--outg")
        cargs.append(v_6)
    return cargs

def dmri_bset_outputs(params: DmriBset, execution: Execution) -> DmriBsetOutputs:
    """Build the DmriBsetOutputs object for this tool."""
    output_b_table_file_v: OutputPathType | None = None
    output_g_table_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_dwi_file_v: OutputPathType = execution.output_file(params["output_dwi"])
    __v0 = params.get("output_b_table")
    if __v0 is not None:
        output_b_table_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_g_table")
    if __v1 is not None:
        output_g_table_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    return DmriBsetOutputs(
        root=root_v,
        output_dwi_file=output_dwi_file_v,
        output_b_table_file=output_b_table_file_v,
        output_g_table_file=output_g_table_file_v,
    )

def dmri_bset_execute(params: DmriBset, runner: Runner | None = None) -> DmriBsetOutputs:
    """
    dmri_bset

    This tool extracts a subset of volumes, b-values, and gradient directions from a diffusion MRI data set.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_bset_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_BSET_METADATA)
    execution.params(params)
    args = dmri_bset_cargs(params, execution)
    out = dmri_bset_outputs(params, execution)
    execution.run(args)
    return out

def dmri_bset(
    input_dwi: InputPathType,
    output_dwi: str,
    b_values: list[float] | None = None,
    btol: float | None = None,
    bsort: bool = False,
    bmax: float | None = None,
    input_b_table: InputPathType | None = None,
    input_g_table: InputPathType | None = None,
    output_b_table: str | None = None,
    output_g_table: str | None = None,
    runner: Runner | None = None,
) -> DmriBsetOutputs:
    """
    dmri_bset

    This tool extracts a subset of volumes, b-values, and gradient directions from a diffusion MRI data set.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_dwi: Input DWI series
        output_dwi: Output DWI series
        b_values: Extract one or more b-values
        btol: Tolerance around each single b-value (default: 0.05)
        bsort: Reorder output data by b-shell (default: maintain original order)
        bmax: Extract all b-values less than or equal to a maximum
        input_b_table: Input b-value table (default: input DWI base, .bvals\
            extension)
        input_g_table: Input gradient table (default: input DWI base, .bvecs\
            extension)
        output_b_table: Output b-value table (default: output DWI base, .bvals\
            extension)
        output_g_table: Output gradient table (default: output DWI base, .bvecs\
            extension)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_bset_params(
        input_dwi=input_dwi,
        output_dwi=output_dwi,
        b_values=b_values,
        btol=btol,
        bsort=bsort,
        bmax=bmax,
        input_b_table=input_b_table,
        input_g_table=input_g_table,
        output_b_table=output_b_table,
        output_g_table=output_g_table,
    )
    return dmri_bset_execute(params, runner)

__all__ = [
    "DmriBset",
    "DmriBsetOutputs",
    "DMRI_BSET_METADATA",
    "dmri_bset_cargs",
    "dmri_bset_outputs",
    "dmri_bset_params",
    "dmri_bset_execute",
    "dmri_bset_validate",
    "dmri_bset",
]