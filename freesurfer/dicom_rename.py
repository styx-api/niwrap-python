# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DICOM_RENAME_METADATA = Metadata(
    id="dicom-rename",
    name="dicom-rename",
    package="freesurfer",
)

DicomRename = typing.TypedDict(
    "DicomRename",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dicom-rename"]],
        "input_files": list[InputPathType],
        "output_base": str,
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DicomRenameOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    renamed_dicom: OutputPathType
    """Renamed output DICOM file with series and image numbers."""

def dicom_rename_params(
    input_files: list[InputPathType],
    output_base: str,
    version: bool = False,
    help: bool = False,
) -> DicomRename:
    """
    Build parameters.

    Args:
        input_files: Input DICOM files to be renamed.
        output_base: Base name for output files that includes series and image\
            numbers.
        version: Print version and exit.
        help: Print help and exit.

    Returns:
        Parameter dictionary.
    """
    params: DicomRename = {
        "@type": "freesurfer/dicom-rename",
        "input_files": input_files,
        "output_base": output_base,
        "version": version,
        "help": help,
    }
    return params

def dicom_rename_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DicomRename."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_base", None) is None:
        raise StyxValidationError("`output_base` must not be None")
    if not isinstance(params["output_base"], str):
        raise StyxValidationError(f'`output_base` has the wrong type: Received `{type(params["output_base"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def dicom_rename_cargs(params: DicomRename, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dicom-rename")
    cargs.append("-rename")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--o")
    cargs.append(params["output_base"])
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def dicom_rename_outputs(params: DicomRename, execution: Execution) -> DicomRenameOutputs:
    """Build the DicomRenameOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    renamed_dicom_v: OutputPathType = execution.output_file(f'{params["output_base"]}-SSS-IIIII.dcm')
    return DicomRenameOutputs(
        root=root_v,
        renamed_dicom=renamed_dicom_v,
    )

def dicom_rename_execute(params: DicomRename, runner: Runner | None = None) -> DicomRenameOutputs:
    """
    dicom-rename

    Copies dicom file(s) to new path with more meaningful names

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dicom_rename_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DICOM_RENAME_METADATA)
    execution.params(params)
    args = dicom_rename_cargs(params, execution)
    out = dicom_rename_outputs(params, execution)
    execution.run(args)
    return out

def dicom_rename(
    input_files: list[InputPathType],
    output_base: str,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> DicomRenameOutputs:
    """
    dicom-rename

    Copies dicom file(s) to new path with more meaningful names

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input DICOM files to be renamed.
        output_base: Base name for output files that includes series and image\
            numbers.
        version: Print version and exit.
        help: Print help and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dicom_rename_params(
        input_files=input_files,
        output_base=output_base,
        version=version,
        help=help,
    )
    return dicom_rename_execute(params, runner)

__all__ = [
    "DicomRename",
    "DicomRenameOutputs",
    "DICOM_RENAME_METADATA",
    "dicom_rename_cargs",
    "dicom_rename_outputs",
    "dicom_rename_params",
    "dicom_rename_execute",
    "dicom_rename_validate",
    "dicom_rename",
]