# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APARCSTATSDIFF_METADATA = Metadata(
    id="aparcstatsdiff",
    name="aparcstatsdiff",
    package="freesurfer",
)

Aparcstatsdiff = typing.TypedDict(
    "Aparcstatsdiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/aparcstatsdiff"]],
        "subj1": str,
        "subj2": str,
        "hemi": str,
        "parc": str,
        "meas": str,
        "outdir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class AparcstatsdiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output table with percentage differences"""

def aparcstatsdiff_params(
    subj1: str,
    subj2: str,
    hemi: str,
    parc: str,
    meas: str,
    outdir: str | None = None,
) -> Aparcstatsdiff:
    """
    Build parameters.

    Args:
        subj1: Subject 1 identifier
        subj2: Subject 2 identifier
        hemi: Hemisphere (rh or lh)
        parc: Parcellation scheme (aparc or aparc.a2009s)
        meas: Measure type (area, volume, or thickness)
        outdir: Directory to write the output table file

    Returns:
        Parameter dictionary.
    """
    params: Aparcstatsdiff = {
        "@type": "freesurfer/aparcstatsdiff",
        "subj1": subj1,
        "subj2": subj2,
        "hemi": hemi,
        "parc": parc,
        "meas": meas,
    }
    if outdir is not None:
        params["outdir"] = outdir
    return params

def aparcstatsdiff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aparcstatsdiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subj1", None) is None:
        raise StyxValidationError("`subj1` must not be None")
    if not isinstance(params["subj1"], str):
        raise StyxValidationError(f'`subj1` has the wrong type: Received `{type(params["subj1"])}` expected `str`')
    if params.get("subj2", None) is None:
        raise StyxValidationError("`subj2` must not be None")
    if not isinstance(params["subj2"], str):
        raise StyxValidationError(f'`subj2` has the wrong type: Received `{type(params["subj2"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("parc", None) is None:
        raise StyxValidationError("`parc` must not be None")
    if not isinstance(params["parc"], str):
        raise StyxValidationError(f'`parc` has the wrong type: Received `{type(params["parc"])}` expected `str`')
    if params.get("meas", None) is None:
        raise StyxValidationError("`meas` must not be None")
    if not isinstance(params["meas"], str):
        raise StyxValidationError(f'`meas` has the wrong type: Received `{type(params["meas"])}` expected `str`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')

def aparcstatsdiff_cargs(params: Aparcstatsdiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aparcstatsdiff")
    cargs.append(params["subj1"])
    cargs.append(params["subj2"])
    cargs.append(params["hemi"])
    cargs.append(params["parc"])
    cargs.append(params["meas"])
    v_0 = params.get("outdir")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def aparcstatsdiff_outputs(params: Aparcstatsdiff, execution: Execution) -> AparcstatsdiffOutputs:
    """Build the AparcstatsdiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'aparcstats-{params["hemi"]}.{params["parc"]}.{params["meas"]}.txt')
    return AparcstatsdiffOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def aparcstatsdiff_execute(params: Aparcstatsdiff, runner: Runner | None = None) -> AparcstatsdiffOutputs:
    """
    aparcstatsdiff

    Utility to calculate percentage differences in aparc morphometry data between two subjects

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aparcstatsdiff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APARCSTATSDIFF_METADATA)
    execution.params(params)
    args = aparcstatsdiff_cargs(params, execution)
    out = aparcstatsdiff_outputs(params, execution)
    execution.run(args)
    return out

def aparcstatsdiff(
    subj1: str,
    subj2: str,
    hemi: str,
    parc: str,
    meas: str,
    outdir: str | None = None,
    runner: Runner | None = None,
) -> AparcstatsdiffOutputs:
    """
    aparcstatsdiff

    Utility to calculate percentage differences in aparc morphometry data between two subjects

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subj1: Subject 1 identifier
        subj2: Subject 2 identifier
        hemi: Hemisphere (rh or lh)
        parc: Parcellation scheme (aparc or aparc.a2009s)
        meas: Measure type (area, volume, or thickness)
        outdir: Directory to write the output table file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aparcstatsdiff_params(
        subj1=subj1,
        subj2=subj2,
        hemi=hemi,
        parc=parc,
        meas=meas,
        outdir=outdir,
    )
    return aparcstatsdiff_execute(params, runner)

__all__ = [
    "Aparcstatsdiff",
    "AparcstatsdiffOutputs",
    "APARCSTATSDIFF_METADATA",
    "aparcstatsdiff_cargs",
    "aparcstatsdiff_outputs",
    "aparcstatsdiff_params",
    "aparcstatsdiff_execute",
    "aparcstatsdiff_validate",
    "aparcstatsdiff",
]