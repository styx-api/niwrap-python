# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BEDPOSTX_MGH_METADATA = Metadata(
    id="bedpostx_mgh",
    name="bedpostx_mgh",
    package="freesurfer",
)

BedpostxMgh = typing.TypedDict(
    "BedpostxMgh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bedpostx_mgh"]],
        "subject_directory": str,
        "fibres": typing.NotRequired[float],
        "ard_weight": typing.NotRequired[float],
        "burnin": typing.NotRequired[float],
        "jumps": typing.NotRequired[float],
        "sample_every": typing.NotRequired[float],
        "deconv_model": typing.NotRequired[float],
        "gradient_nonlin": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BedpostxMghOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def bedpostx_mgh_params(
    subject_directory: str,
    fibres: float | None = None,
    ard_weight: float | None = None,
    burnin: float | None = None,
    jumps: float | None = None,
    sample_every: float | None = None,
    deconv_model: float | None = None,
    gradient_nonlin: bool = False,
) -> BedpostxMgh:
    """
    Build parameters.

    Args:
        subject_directory: Subject directory containing necessary files such as\
            bvals, bvecs, data, and nodif_brain_mask.
        fibres: Number of fibres per voxel, default is 3
        ard_weight: ARD weight, more weight means fewer secondary fibres per voxel,\
            default is 1
        burnin: Burnin period, default is 1000
        jumps: Number of jumps, default is 1250
        sample_every: Sample every n steps, default is 25
        deconv_model: Deconvolution model selection. 1: with sticks, 2: with sticks\
            with a range of diffusivities (default), 3: with zeppelins
        gradient_nonlin: Consider gradient nonlinearities, default is off

    Returns:
        Parameter dictionary.
    """
    params: BedpostxMgh = {
        "@type": "freesurfer/bedpostx_mgh",
        "subject_directory": subject_directory,
        "gradient_nonlin": gradient_nonlin,
    }
    if fibres is not None:
        params["fibres"] = fibres
    if ard_weight is not None:
        params["ard_weight"] = ard_weight
    if burnin is not None:
        params["burnin"] = burnin
    if jumps is not None:
        params["jumps"] = jumps
    if sample_every is not None:
        params["sample_every"] = sample_every
    if deconv_model is not None:
        params["deconv_model"] = deconv_model
    return params

def bedpostx_mgh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BedpostxMgh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_directory", None) is None:
        raise StyxValidationError("`subject_directory` must not be None")
    if not isinstance(params["subject_directory"], str):
        raise StyxValidationError(f'`subject_directory` has the wrong type: Received `{type(params["subject_directory"])}` expected `str`')
    if params.get("fibres", None) is not None:
        if not isinstance(params["fibres"], (float, int)):
            raise StyxValidationError(f'`fibres` has the wrong type: Received `{type(params["fibres"])}` expected `float`')
    if params.get("ard_weight", None) is not None:
        if not isinstance(params["ard_weight"], (float, int)):
            raise StyxValidationError(f'`ard_weight` has the wrong type: Received `{type(params["ard_weight"])}` expected `float`')
    if params.get("burnin", None) is not None:
        if not isinstance(params["burnin"], (float, int)):
            raise StyxValidationError(f'`burnin` has the wrong type: Received `{type(params["burnin"])}` expected `float`')
    if params.get("jumps", None) is not None:
        if not isinstance(params["jumps"], (float, int)):
            raise StyxValidationError(f'`jumps` has the wrong type: Received `{type(params["jumps"])}` expected `float`')
    if params.get("sample_every", None) is not None:
        if not isinstance(params["sample_every"], (float, int)):
            raise StyxValidationError(f'`sample_every` has the wrong type: Received `{type(params["sample_every"])}` expected `float`')
    if params.get("deconv_model", None) is not None:
        if not isinstance(params["deconv_model"], (float, int)):
            raise StyxValidationError(f'`deconv_model` has the wrong type: Received `{type(params["deconv_model"])}` expected `float`')
    if params.get("gradient_nonlin", None) is not None:
        if not isinstance(params["gradient_nonlin"], bool):
            raise StyxValidationError(f'`gradient_nonlin` has the wrong type: Received `{type(params["gradient_nonlin"])}` expected `bool`')

def bedpostx_mgh_cargs(params: BedpostxMgh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bedpostx_mgh")
    cargs.append(params["subject_directory"])
    v_0 = params.get("fibres")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    v_1 = params.get("ard_weight")
    if v_1 is not None:
        cargs.append("-w")
        cargs.append(str(v_1))
    v_2 = params.get("burnin")
    if v_2 is not None:
        cargs.append("-b")
        cargs.append(str(v_2))
    v_3 = params.get("jumps")
    if v_3 is not None:
        cargs.append("-j")
        cargs.append(str(v_3))
    v_4 = params.get("sample_every")
    if v_4 is not None:
        cargs.append("-s")
        cargs.append(str(v_4))
    v_5 = params.get("deconv_model")
    if v_5 is not None:
        cargs.append("-model")
        cargs.append(str(v_5))
    if params.get("gradient_nonlin"):
        cargs.append("-g")
    return cargs

def bedpostx_mgh_outputs(params: BedpostxMgh, execution: Execution) -> BedpostxMghOutputs:
    """Build the BedpostxMghOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BedpostxMghOutputs(
        root=root_v,
    )

def bedpostx_mgh_execute(params: BedpostxMgh, runner: Runner | None = None) -> BedpostxMghOutputs:
    """
    bedpostx_mgh

    A modified version of FSL's bedpostx compatible with PBS queueing system for parallel computation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bedpostx_mgh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BEDPOSTX_MGH_METADATA)
    execution.params(params)
    args = bedpostx_mgh_cargs(params, execution)
    out = bedpostx_mgh_outputs(params, execution)
    execution.run(args)
    return out

def bedpostx_mgh(
    subject_directory: str,
    fibres: float | None = None,
    ard_weight: float | None = None,
    burnin: float | None = None,
    jumps: float | None = None,
    sample_every: float | None = None,
    deconv_model: float | None = None,
    gradient_nonlin: bool = False,
    runner: Runner | None = None,
) -> BedpostxMghOutputs:
    """
    bedpostx_mgh

    A modified version of FSL's bedpostx compatible with PBS queueing system for parallel computation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_directory: Subject directory containing necessary files such as\
            bvals, bvecs, data, and nodif_brain_mask.
        fibres: Number of fibres per voxel, default is 3
        ard_weight: ARD weight, more weight means fewer secondary fibres per voxel,\
            default is 1
        burnin: Burnin period, default is 1000
        jumps: Number of jumps, default is 1250
        sample_every: Sample every n steps, default is 25
        deconv_model: Deconvolution model selection. 1: with sticks, 2: with sticks\
            with a range of diffusivities (default), 3: with zeppelins
        gradient_nonlin: Consider gradient nonlinearities, default is off
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bedpostx_mgh_params(
        subject_directory=subject_directory,
        fibres=fibres,
        ard_weight=ard_weight,
        burnin=burnin,
        jumps=jumps,
        sample_every=sample_every,
        deconv_model=deconv_model,
        gradient_nonlin=gradient_nonlin,
    )
    return bedpostx_mgh_execute(params, runner)

__all__ = [
    "BedpostxMgh",
    "BedpostxMghOutputs",
    "BEDPOSTX_MGH_METADATA",
    "bedpostx_mgh_cargs",
    "bedpostx_mgh_outputs",
    "bedpostx_mgh_params",
    "bedpostx_mgh_execute",
    "bedpostx_mgh_validate",
    "bedpostx_mgh",
]