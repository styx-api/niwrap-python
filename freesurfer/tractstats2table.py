# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRACTSTATS2TABLE_METADATA = Metadata(
    id="tractstats2table",
    name="tractstats2table",
    package="freesurfer",
)

Tractstats2tableByvoxelMeasure = typing.Literal["AD"] | typing.Literal["RD"] | typing.Literal["MD"] | typing.Literal["FA"]

Tractstats2tableDelimiter = typing.Literal["tab"] | typing.Literal["comma"] | typing.Literal["space"] | typing.Literal["semicolon"]

Tractstats2table = typing.TypedDict(
    "Tractstats2table",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tractstats2table"]],
        "inputs": typing.NotRequired[list[str]],
        "load_pathstats_from_file": typing.NotRequired[InputPathType],
        "overall": typing.NotRequired[bool],
        "byvoxel": typing.NotRequired[bool],
        "byvoxel_measure": typing.NotRequired[Tractstats2tableByvoxelMeasure],
        "tablefile": InputPathType,
        "delimiter": typing.NotRequired[Tractstats2tableDelimiter],
        "transpose": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Tractstats2tableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_tablefile: OutputPathType
    """The output table file."""

def tractstats2table_params(
    tablefile: InputPathType,
    inputs: list[str] | None = None,
    load_pathstats_from_file: InputPathType | None = None,
    overall: bool = False,
    byvoxel: bool = False,
    byvoxel_measure: Tractstats2tableByvoxelMeasure | None = None,
    delimiter: Tractstats2tableDelimiter | None = None,
    transpose: bool = False,
    debug: bool = False,
) -> Tractstats2table:
    """
    Build parameters.

    Args:
        tablefile: The output table file.
        inputs: Specify input stat files.
        load_pathstats_from_file: Name of the file which has the list of subjects\
            (one subject per line).
        overall: Operate on the overall path statistics.
        byvoxel: Operate on the byvoxel path statistics.
        byvoxel_measure: Specify byvoxel measure. One of [AD, RD, MD, FA]. Required\
            with --byvoxel option.
        delimiter: Delimiter between measures in the table. Default is tab (alt\
            comma, space, semicolon).
        transpose: Transpose the table (default is subject in rows and\
            measures/count in cols).
        debug: Increase verbosity.

    Returns:
        Parameter dictionary.
    """
    params: Tractstats2table = {
        "@type": "freesurfer/tractstats2table",
        "tablefile": tablefile,
        "overall": overall,
        "byvoxel": byvoxel,
        "transpose": transpose,
        "debug": debug,
    }
    if inputs is not None:
        params["inputs"] = inputs
    if load_pathstats_from_file is not None:
        params["load_pathstats_from_file"] = load_pathstats_from_file
    if byvoxel_measure is not None:
        params["byvoxel_measure"] = byvoxel_measure
    if delimiter is not None:
        params["delimiter"] = delimiter
    return params

def tractstats2table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tractstats2table."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputs", None) is not None:
        if not isinstance(params["inputs"], list):
            raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(params["inputs"])}` expected `list[str]`')
        for e in params["inputs"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("load_pathstats_from_file", None) is not None:
        if not isinstance(params["load_pathstats_from_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`load_pathstats_from_file` has the wrong type: Received `{type(params["load_pathstats_from_file"])}` expected `InputPathType`')
    if params.get("overall", None) is not None:
        if not isinstance(params["overall"], bool):
            raise StyxValidationError(f'`overall` has the wrong type: Received `{type(params["overall"])}` expected `bool`')
    if params.get("byvoxel", None) is not None:
        if not isinstance(params["byvoxel"], bool):
            raise StyxValidationError(f'`byvoxel` has the wrong type: Received `{type(params["byvoxel"])}` expected `bool`')
    if params.get("byvoxel_measure", None) is not None:
        if not isinstance(params["byvoxel_measure"], str):
            raise StyxValidationError(f'`byvoxel_measure` has the wrong type: Received `{type(params["byvoxel_measure"])}` expected `Tractstats2tableByvoxelMeasure`')
        if params["byvoxel_measure"] not in ["AD", "RD", "MD", "FA"]:
            raise StyxValidationError("Parameter `byvoxel_measure` must be one of [\"AD\", \"RD\", \"MD\", \"FA\"]")
    if params.get("tablefile", None) is None:
        raise StyxValidationError("`tablefile` must not be None")
    if not isinstance(params["tablefile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tablefile` has the wrong type: Received `{type(params["tablefile"])}` expected `InputPathType`')
    if params.get("delimiter", None) is not None:
        if not isinstance(params["delimiter"], str):
            raise StyxValidationError(f'`delimiter` has the wrong type: Received `{type(params["delimiter"])}` expected `Tractstats2tableDelimiter`')
        if params["delimiter"] not in ["tab", "comma", "space", "semicolon"]:
            raise StyxValidationError("Parameter `delimiter` must be one of [\"tab\", \"comma\", \"space\", \"semicolon\"]")
    if params.get("transpose", None) is not None:
        if not isinstance(params["transpose"], bool):
            raise StyxValidationError(f'`transpose` has the wrong type: Received `{type(params["transpose"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def tractstats2table_cargs(params: Tractstats2table, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tractstats2table")
    v_0 = params.get("inputs")
    if v_0 is not None:
        cargs.append("--inputs")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("load_pathstats_from_file")
    if v_1 is not None:
        cargs.append("--load-pathstats-from-file")
        cargs.append(execution.input_file(v_1))
    if params.get("overall"):
        cargs.append("-o")
    if params.get("byvoxel"):
        cargs.append("-b")
    v_2 = params.get("byvoxel_measure")
    if v_2 is not None:
        cargs.append("--byvoxel-measure")
        cargs.append(str(v_2))
    cargs.append("-t")
    cargs.append(execution.input_file(params["tablefile"]))
    v_3 = params.get("delimiter")
    if v_3 is not None:
        cargs.append("-d")
        cargs.append(str(v_3))
    if params.get("transpose"):
        cargs.append("--transpose")
    if params.get("debug"):
        cargs.append("-v")
    return cargs

def tractstats2table_outputs(params: Tractstats2table, execution: Execution) -> Tractstats2tableOutputs:
    """Build the Tractstats2tableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_tablefile_v: OutputPathType = execution.output_file(params["tablefile"])
    return Tractstats2tableOutputs(
        root=root_v,
        output_tablefile=output_tablefile_v,
    )

def tractstats2table_execute(params: Tractstats2table, runner: Runner | None = None) -> Tractstats2tableOutputs:
    """
    tractstats2table

    Converts a track overall stats file created by tracula into a table used for group statistics.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tractstats2table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRACTSTATS2TABLE_METADATA)
    execution.params(params)
    args = tractstats2table_cargs(params, execution)
    out = tractstats2table_outputs(params, execution)
    execution.run(args)
    return out

def tractstats2table(
    tablefile: InputPathType,
    inputs: list[str] | None = None,
    load_pathstats_from_file: InputPathType | None = None,
    overall: bool = False,
    byvoxel: bool = False,
    byvoxel_measure: Tractstats2tableByvoxelMeasure | None = None,
    delimiter: Tractstats2tableDelimiter | None = None,
    transpose: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> Tractstats2tableOutputs:
    """
    tractstats2table

    Converts a track overall stats file created by tracula into a table used for group statistics.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        tablefile: The output table file.
        inputs: Specify input stat files.
        load_pathstats_from_file: Name of the file which has the list of subjects\
            (one subject per line).
        overall: Operate on the overall path statistics.
        byvoxel: Operate on the byvoxel path statistics.
        byvoxel_measure: Specify byvoxel measure. One of [AD, RD, MD, FA]. Required\
            with --byvoxel option.
        delimiter: Delimiter between measures in the table. Default is tab (alt\
            comma, space, semicolon).
        transpose: Transpose the table (default is subject in rows and\
            measures/count in cols).
        debug: Increase verbosity.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tractstats2table_params(
        tablefile=tablefile,
        inputs=inputs,
        load_pathstats_from_file=load_pathstats_from_file,
        overall=overall,
        byvoxel=byvoxel,
        byvoxel_measure=byvoxel_measure,
        delimiter=delimiter,
        transpose=transpose,
        debug=debug,
    )
    return tractstats2table_execute(params, runner)

__all__ = [
    "Tractstats2table",
    "Tractstats2tableOutputs",
    "TRACTSTATS2TABLE_METADATA",
    "tractstats2table_cargs",
    "tractstats2table_outputs",
    "tractstats2table_params",
    "tractstats2table_execute",
    "tractstats2table_validate",
    "tractstats2table",
]