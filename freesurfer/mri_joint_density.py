# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_JOINT_DENSITY_METADATA = Metadata(
    id="mri_joint_density",
    name="mri_joint_density",
    package="freesurfer",
)

MriJointDensity = typing.TypedDict(
    "MriJointDensity",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_joint_density"]],
        "vol1": InputPathType,
        "vol2": InputPathType,
        "output_density_file": str,
    },
)

@dataclasses.dataclass
class MriJointDensityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file containing the computed joint density"""

def mri_joint_density_params(
    vol1: InputPathType,
    vol2: InputPathType,
    output_density_file: str,
) -> MriJointDensity:
    """
    Build parameters.

    Args:
        vol1: First input volume
        vol2: Second input volume
        output_density_file: Output joint density file

    Returns:
        Parameter dictionary.
    """
    params: MriJointDensity = {
        "@type": "freesurfer/mri_joint_density",
        "vol1": vol1,
        "vol2": vol2,
        "output_density_file": output_density_file,
    }
    return params

def mri_joint_density_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriJointDensity."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vol1", None) is None:
        raise StyxValidationError("`vol1` must not be None")
    if not isinstance(params["vol1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol1` has the wrong type: Received `{type(params["vol1"])}` expected `InputPathType`')
    if params.get("vol2", None) is None:
        raise StyxValidationError("`vol2` must not be None")
    if not isinstance(params["vol2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol2` has the wrong type: Received `{type(params["vol2"])}` expected `InputPathType`')
    if params.get("output_density_file", None) is None:
        raise StyxValidationError("`output_density_file` must not be None")
    if not isinstance(params["output_density_file"], str):
        raise StyxValidationError(f'`output_density_file` has the wrong type: Received `{type(params["output_density_file"])}` expected `str`')

def mri_joint_density_cargs(params: MriJointDensity, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_joint_density")
    cargs.append(execution.input_file(params["vol1"]))
    cargs.append(execution.input_file(params["vol2"]))
    cargs.append(params["output_density_file"])
    return cargs

def mri_joint_density_outputs(params: MriJointDensity, execution: Execution) -> MriJointDensityOutputs:
    """Build the MriJointDensityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_density_file"])
    return MriJointDensityOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_joint_density_execute(params: MriJointDensity, runner: Runner | None = None) -> MriJointDensityOutputs:
    """
    mri_joint_density

    Tool for computing joint density from two volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_joint_density_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_JOINT_DENSITY_METADATA)
    execution.params(params)
    args = mri_joint_density_cargs(params, execution)
    out = mri_joint_density_outputs(params, execution)
    execution.run(args)
    return out

def mri_joint_density(
    vol1: InputPathType,
    vol2: InputPathType,
    output_density_file: str,
    runner: Runner | None = None,
) -> MriJointDensityOutputs:
    """
    mri_joint_density

    Tool for computing joint density from two volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        vol1: First input volume
        vol2: Second input volume
        output_density_file: Output joint density file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_joint_density_params(
        vol1=vol1,
        vol2=vol2,
        output_density_file=output_density_file,
    )
    return mri_joint_density_execute(params, runner)

__all__ = [
    "MriJointDensity",
    "MriJointDensityOutputs",
    "MRI_JOINT_DENSITY_METADATA",
    "mri_joint_density_cargs",
    "mri_joint_density_outputs",
    "mri_joint_density_params",
    "mri_joint_density_execute",
    "mri_joint_density_validate",
    "mri_joint_density",
]