# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LPCREGISTER_METADATA = Metadata(
    id="lpcregister",
    name="lpcregister",
    package="freesurfer",
)

Lpcregister = typing.TypedDict(
    "Lpcregister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/lpcregister"]],
        "subject_id": str,
        "mov_volume": str,
        "reg_file": str,
        "dof_9": typing.NotRequired[bool],
        "dof_12": typing.NotRequired[bool],
        "frame_number": typing.NotRequired[float],
        "mid_frame": typing.NotRequired[bool],
        "fsvol": typing.NotRequired[str],
        "output_volume": typing.NotRequired[str],
        "tmp_dir": typing.NotRequired[str],
        "no_cleanup": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LpcregisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_reg_file: OutputPathType
    """Output registration file in FreeSurfer format."""
    resampled_volume: OutputPathType | None
    """The resampled movable volume if --o is specified."""

def lpcregister_params(
    subject_id: str,
    mov_volume: str,
    reg_file: str,
    dof_9: bool = False,
    dof_12: bool = False,
    frame_number: float | None = None,
    mid_frame: bool = False,
    fsvol: str | None = None,
    output_volume: str | None = None,
    tmp_dir: str | None = None,
    no_cleanup: bool = False,
    version: bool = False,
    help: bool = False,
) -> Lpcregister:
    """
    Build parameters.

    Args:
        subject_id: Subject ID found in SUBJECTS_DIR. The reference volume is the\
            mri/brain volume (modifiable with --fsvol). Converted to analyze using\
            mri_convert.
        mov_volume: Volume identifier of the movable volume. Must be specified\
            suitably for mri_convert. Uses first frame unless --frame is specified.
        reg_file: Output registration file mapping RAS in the reference to RAS in\
            the movable. The file/matrix format is understood by freesurfer.
        dof_9: Use 9 degrees of freedom; default is 6.
        dof_12: Use 12 degrees of freedom; default is 6.
        frame_number: Specify the frame number other than the first. For SPM\
            analyze, specify the file corresponding to the desired frame as each\
            file only has one frame.
        mid_frame: Register to the middle frame of the mov volume as the template.\
            Cannot be used with --frame.
        fsvol: Use FreeSurfer volume id; default is brainmask.
        output_volume: Output volume file for resampled mov.
        tmp_dir: Specify temporary directory (implies --nocleanup).
        no_cleanup: Do not delete temporary files.
        version: Print the version and exit.
        help: Print help information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Lpcregister = {
        "@type": "freesurfer/lpcregister",
        "subject_id": subject_id,
        "mov_volume": mov_volume,
        "reg_file": reg_file,
        "dof_9": dof_9,
        "dof_12": dof_12,
        "mid_frame": mid_frame,
        "no_cleanup": no_cleanup,
        "version": version,
        "help": help,
    }
    if frame_number is not None:
        params["frame_number"] = frame_number
    if fsvol is not None:
        params["fsvol"] = fsvol
    if output_volume is not None:
        params["output_volume"] = output_volume
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def lpcregister_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Lpcregister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("mov_volume", None) is None:
        raise StyxValidationError("`mov_volume` must not be None")
    if not isinstance(params["mov_volume"], str):
        raise StyxValidationError(f'`mov_volume` has the wrong type: Received `{type(params["mov_volume"])}` expected `str`')
    if params.get("reg_file", None) is None:
        raise StyxValidationError("`reg_file` must not be None")
    if not isinstance(params["reg_file"], str):
        raise StyxValidationError(f'`reg_file` has the wrong type: Received `{type(params["reg_file"])}` expected `str`')
    if params.get("dof_9", None) is not None:
        if not isinstance(params["dof_9"], bool):
            raise StyxValidationError(f'`dof_9` has the wrong type: Received `{type(params["dof_9"])}` expected `bool`')
    if params.get("dof_12", None) is not None:
        if not isinstance(params["dof_12"], bool):
            raise StyxValidationError(f'`dof_12` has the wrong type: Received `{type(params["dof_12"])}` expected `bool`')
    if params.get("frame_number", None) is not None:
        if not isinstance(params["frame_number"], (float, int)):
            raise StyxValidationError(f'`frame_number` has the wrong type: Received `{type(params["frame_number"])}` expected `float`')
    if params.get("mid_frame", None) is not None:
        if not isinstance(params["mid_frame"], bool):
            raise StyxValidationError(f'`mid_frame` has the wrong type: Received `{type(params["mid_frame"])}` expected `bool`')
    if params.get("fsvol", None) is not None:
        if not isinstance(params["fsvol"], str):
            raise StyxValidationError(f'`fsvol` has the wrong type: Received `{type(params["fsvol"])}` expected `str`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def lpcregister_cargs(params: Lpcregister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("lpcregister")
    cargs.append("--s")
    cargs.append(params["subject_id"])
    cargs.append("--mov")
    cargs.append(params["mov_volume"])
    cargs.append("--reg")
    cargs.append(params["reg_file"])
    if params.get("dof_9"):
        cargs.append("--9")
    if params.get("dof_12"):
        cargs.append("--12")
    v_0 = params.get("frame_number")
    if v_0 is not None:
        cargs.append("--frame")
        cargs.append(str(v_0))
    if params.get("mid_frame"):
        cargs.append("--mid-frame")
    v_1 = params.get("fsvol")
    if v_1 is not None:
        cargs.append("--fsvol")
        cargs.append(v_1)
    v_2 = params.get("output_volume")
    if v_2 is not None:
        cargs.append("--o")
        cargs.append(v_2)
    v_3 = params.get("tmp_dir")
    if v_3 is not None:
        cargs.append("--tmp")
        cargs.append(v_3)
    if params.get("no_cleanup"):
        cargs.append("--nocleanup")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def lpcregister_outputs(params: Lpcregister, execution: Execution) -> LpcregisterOutputs:
    """Build the LpcregisterOutputs object for this tool."""
    resampled_volume_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_reg_file_v: OutputPathType = execution.output_file("register.dat")
    __v0 = params.get("output_volume")
    if __v0 is not None:
        resampled_volume_v = execution.output_file((__v0 if __v0 is not None else ""))
    return LpcregisterOutputs(
        root=root_v,
        output_reg_file=output_reg_file_v,
        resampled_volume=resampled_volume_v,
    )

def lpcregister_execute(params: Lpcregister, runner: Runner | None = None) -> LpcregisterOutputs:
    """
    lpcregister

    Registers a volume to its FreeSurfer anatomical using Local Pearson Correlation (LPC) (the AFNI lpc_align.py program). Creates a FreeSurfer register.dat file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    lpcregister_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LPCREGISTER_METADATA)
    execution.params(params)
    args = lpcregister_cargs(params, execution)
    out = lpcregister_outputs(params, execution)
    execution.run(args)
    return out

def lpcregister(
    subject_id: str,
    mov_volume: str,
    reg_file: str,
    dof_9: bool = False,
    dof_12: bool = False,
    frame_number: float | None = None,
    mid_frame: bool = False,
    fsvol: str | None = None,
    output_volume: str | None = None,
    tmp_dir: str | None = None,
    no_cleanup: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> LpcregisterOutputs:
    """
    lpcregister

    Registers a volume to its FreeSurfer anatomical using Local Pearson Correlation (LPC) (the AFNI lpc_align.py program). Creates a FreeSurfer register.dat file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id: Subject ID found in SUBJECTS_DIR. The reference volume is the\
            mri/brain volume (modifiable with --fsvol). Converted to analyze using\
            mri_convert.
        mov_volume: Volume identifier of the movable volume. Must be specified\
            suitably for mri_convert. Uses first frame unless --frame is specified.
        reg_file: Output registration file mapping RAS in the reference to RAS in\
            the movable. The file/matrix format is understood by freesurfer.
        dof_9: Use 9 degrees of freedom; default is 6.
        dof_12: Use 12 degrees of freedom; default is 6.
        frame_number: Specify the frame number other than the first. For SPM\
            analyze, specify the file corresponding to the desired frame as each\
            file only has one frame.
        mid_frame: Register to the middle frame of the mov volume as the template.\
            Cannot be used with --frame.
        fsvol: Use FreeSurfer volume id; default is brainmask.
        output_volume: Output volume file for resampled mov.
        tmp_dir: Specify temporary directory (implies --nocleanup).
        no_cleanup: Do not delete temporary files.
        version: Print the version and exit.
        help: Print help information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = lpcregister_params(
        subject_id=subject_id,
        mov_volume=mov_volume,
        reg_file=reg_file,
        dof_9=dof_9,
        dof_12=dof_12,
        frame_number=frame_number,
        mid_frame=mid_frame,
        fsvol=fsvol,
        output_volume=output_volume,
        tmp_dir=tmp_dir,
        no_cleanup=no_cleanup,
        version=version,
        help=help,
    )
    return lpcregister_execute(params, runner)

__all__ = [
    "Lpcregister",
    "LpcregisterOutputs",
    "LPCREGISTER_METADATA",
    "lpcregister_cargs",
    "lpcregister_outputs",
    "lpcregister_params",
    "lpcregister_execute",
    "lpcregister_validate",
    "lpcregister",
]