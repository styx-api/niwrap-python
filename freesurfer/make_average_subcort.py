# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_AVERAGE_SUBCORT_METADATA = Metadata(
    id="make_average_subcort",
    name="make_average_subcort",
    package="freesurfer",
)

MakeAverageSubcort = typing.TypedDict(
    "MakeAverageSubcort",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_average_subcort"]],
        "subjects": list[str],
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MakeAverageSubcortOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_mask_file: OutputPathType
    """Final average subcortical mask in fsaverage/mni305 space"""

def make_average_subcort_params(
    subjects: list[str],
    output_volume: str,
) -> MakeAverageSubcort:
    """
    Build parameters.

    Args:
        subjects: List of subject identifiers
        output_volume: Output volume file

    Returns:
        Parameter dictionary.
    """
    params: MakeAverageSubcort = {
        "@type": "freesurfer/make_average_subcort",
        "subjects": subjects,
        "output_volume": output_volume,
    }
    return params

def make_average_subcort_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeAverageSubcort."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def make_average_subcort_cargs(params: MakeAverageSubcort, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_average_subcort")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append("--o")
    cargs.append(params["output_volume"])
    return cargs

def make_average_subcort_outputs(params: MakeAverageSubcort, execution: Execution) -> MakeAverageSubcortOutputs:
    """Build the MakeAverageSubcortOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_mask_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MakeAverageSubcortOutputs(
        root=root_v,
        output_mask_file=output_mask_file_v,
    )

def make_average_subcort_execute(params: MakeAverageSubcort, runner: Runner | None = None) -> MakeAverageSubcortOutputs:
    """
    make_average_subcort

    This creates an average subcortical mask for the given input subjects, intended for use in subcortical analysis in FSFAST to exclude cortical voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_average_subcort_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_AVERAGE_SUBCORT_METADATA)
    execution.params(params)
    args = make_average_subcort_cargs(params, execution)
    out = make_average_subcort_outputs(params, execution)
    execution.run(args)
    return out

def make_average_subcort(
    subjects: list[str],
    output_volume: str,
    runner: Runner | None = None,
) -> MakeAverageSubcortOutputs:
    """
    make_average_subcort

    This creates an average subcortical mask for the given input subjects, intended for use in subcortical analysis in FSFAST to exclude cortical voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subject identifiers
        output_volume: Output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_average_subcort_params(
        subjects=subjects,
        output_volume=output_volume,
    )
    return make_average_subcort_execute(params, runner)

__all__ = [
    "MakeAverageSubcort",
    "MakeAverageSubcortOutputs",
    "MAKE_AVERAGE_SUBCORT_METADATA",
    "make_average_subcort_cargs",
    "make_average_subcort_outputs",
    "make_average_subcort_params",
    "make_average_subcort_execute",
    "make_average_subcort_validate",
    "make_average_subcort",
]