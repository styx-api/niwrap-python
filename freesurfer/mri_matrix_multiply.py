# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MATRIX_MULTIPLY_METADATA = Metadata(
    id="mri_matrix_multiply",
    name="mri_matrix_multiply",
    package="freesurfer",
)

MriMatrixMultiply = typing.TypedDict(
    "MriMatrixMultiply",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_matrix_multiply"]],
        "input_matrices": list[InputPathType],
        "inverted_input_matrices": typing.NotRequired[list[InputPathType]],
        "output_matrix": str,
        "verbose": typing.NotRequired[bool],
        "fsl": typing.NotRequired[bool],
        "binarize": typing.NotRequired[bool],
        "subject_name": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriMatrixMultiplyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_matrix_file: OutputPathType
    """Output matrix file resulting from the matrix multiplication."""

def mri_matrix_multiply_params(
    input_matrices: list[InputPathType],
    output_matrix: str,
    inverted_input_matrices: list[InputPathType] | None = None,
    verbose: bool = False,
    fsl: bool = False,
    binarize: bool = False,
    subject_name: str | None = None,
) -> MriMatrixMultiply:
    """
    Build parameters.

    Args:
        input_matrices: Input matrix files for multiplication.
        output_matrix: Output matrix file.
        inverted_input_matrices: Input matrix files to be inverted before\
            multiplication.
        verbose: Verbose output.
        fsl: Assume input/output are FSL-style matrix files.
        binarize: 'Binarize' the output matrix.
        subject_name: Subject name for output reg.dat files.

    Returns:
        Parameter dictionary.
    """
    params: MriMatrixMultiply = {
        "@type": "freesurfer/mri_matrix_multiply",
        "input_matrices": input_matrices,
        "output_matrix": output_matrix,
        "verbose": verbose,
        "fsl": fsl,
        "binarize": binarize,
    }
    if inverted_input_matrices is not None:
        params["inverted_input_matrices"] = inverted_input_matrices
    if subject_name is not None:
        params["subject_name"] = subject_name
    return params

def mri_matrix_multiply_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMatrixMultiply."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_matrices", None) is None:
        raise StyxValidationError("`input_matrices` must not be None")
    if not isinstance(params["input_matrices"], list):
        raise StyxValidationError(f'`input_matrices` has the wrong type: Received `{type(params["input_matrices"])}` expected `list[InputPathType]`')
    for e in params["input_matrices"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_matrices` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("inverted_input_matrices", None) is not None:
        if not isinstance(params["inverted_input_matrices"], list):
            raise StyxValidationError(f'`inverted_input_matrices` has the wrong type: Received `{type(params["inverted_input_matrices"])}` expected `list[InputPathType]`')
        for e_2 in params["inverted_input_matrices"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`inverted_input_matrices` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("output_matrix", None) is None:
        raise StyxValidationError("`output_matrix` must not be None")
    if not isinstance(params["output_matrix"], str):
        raise StyxValidationError(f'`output_matrix` has the wrong type: Received `{type(params["output_matrix"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("fsl", None) is not None:
        if not isinstance(params["fsl"], bool):
            raise StyxValidationError(f'`fsl` has the wrong type: Received `{type(params["fsl"])}` expected `bool`')
    if params.get("binarize", None) is not None:
        if not isinstance(params["binarize"], bool):
            raise StyxValidationError(f'`binarize` has the wrong type: Received `{type(params["binarize"])}` expected `bool`')
    if params.get("subject_name", None) is not None:
        if not isinstance(params["subject_name"], str):
            raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')

def mri_matrix_multiply_cargs(params: MriMatrixMultiply, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_matrix_multiply")
    cargs.append("-im")
    for item0 in params["input_matrices"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("inverted_input_matrices")
    if v_0 is not None:
        cargs.append("-iim")
        for item1 in v_0:
            cargs.append(execution.input_file(item1))
    cargs.append("-om")
    cargs.append(params["output_matrix"])
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("fsl"):
        cargs.append("-fsl")
    if params.get("binarize"):
        cargs.append("-bin")
    v_1 = params.get("subject_name")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(v_1)
    return cargs

def mri_matrix_multiply_outputs(params: MriMatrixMultiply, execution: Execution) -> MriMatrixMultiplyOutputs:
    """Build the MriMatrixMultiplyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_matrix_file_v: OutputPathType = execution.output_file(params["output_matrix"])
    return MriMatrixMultiplyOutputs(
        root=root_v,
        output_matrix_file=output_matrix_file_v,
    )

def mri_matrix_multiply_execute(params: MriMatrixMultiply, runner: Runner | None = None) -> MriMatrixMultiplyOutputs:
    """
    mri_matrix_multiply

    Command-line tool for multiplying and manipulating MRI transformation matrices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_matrix_multiply_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MATRIX_MULTIPLY_METADATA)
    execution.params(params)
    args = mri_matrix_multiply_cargs(params, execution)
    out = mri_matrix_multiply_outputs(params, execution)
    execution.run(args)
    return out

def mri_matrix_multiply(
    input_matrices: list[InputPathType],
    output_matrix: str,
    inverted_input_matrices: list[InputPathType] | None = None,
    verbose: bool = False,
    fsl: bool = False,
    binarize: bool = False,
    subject_name: str | None = None,
    runner: Runner | None = None,
) -> MriMatrixMultiplyOutputs:
    """
    mri_matrix_multiply

    Command-line tool for multiplying and manipulating MRI transformation matrices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_matrices: Input matrix files for multiplication.
        output_matrix: Output matrix file.
        inverted_input_matrices: Input matrix files to be inverted before\
            multiplication.
        verbose: Verbose output.
        fsl: Assume input/output are FSL-style matrix files.
        binarize: 'Binarize' the output matrix.
        subject_name: Subject name for output reg.dat files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_matrix_multiply_params(
        input_matrices=input_matrices,
        output_matrix=output_matrix,
        inverted_input_matrices=inverted_input_matrices,
        verbose=verbose,
        fsl=fsl,
        binarize=binarize,
        subject_name=subject_name,
    )
    return mri_matrix_multiply_execute(params, runner)

__all__ = [
    "MriMatrixMultiply",
    "MriMatrixMultiplyOutputs",
    "MRI_MATRIX_MULTIPLY_METADATA",
    "mri_matrix_multiply_cargs",
    "mri_matrix_multiply_outputs",
    "mri_matrix_multiply_params",
    "mri_matrix_multiply_execute",
    "mri_matrix_multiply_validate",
    "mri_matrix_multiply",
]