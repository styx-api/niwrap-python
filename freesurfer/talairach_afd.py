# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TALAIRACH_AFD_METADATA = Metadata(
    id="talairach_afd",
    name="talairach_afd",
    package="freesurfer",
)

TalairachAfd = typing.TypedDict(
    "TalairachAfd",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/talairach_afd"]],
        "subject_name": typing.NotRequired[str],
        "xfm_file": typing.NotRequired[InputPathType],
        "p_value_threshold": typing.NotRequired[float],
        "afd_directory": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TalairachAfdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def talairach_afd_params(
    subject_name: str | None = None,
    xfm_file: InputPathType | None = None,
    p_value_threshold: float | None = None,
    afd_directory: str | None = None,
    verbose: bool = False,
) -> TalairachAfd:
    """
    Build parameters.

    Args:
        subject_name: Specify subject's name
        xfm_file: Specify the talairach.xfm file to check
        p_value_threshold: Threshold the p-values at #; Talairach transforms for\
            subjects with p-values <= T are considered as very unlikely
        afd_directory: Specify directory containing .afd data files
        verbose: Enable verbose output

    Returns:
        Parameter dictionary.
    """
    params: TalairachAfd = {
        "@type": "freesurfer/talairach_afd",
        "verbose": verbose,
    }
    if subject_name is not None:
        params["subject_name"] = subject_name
    if xfm_file is not None:
        params["xfm_file"] = xfm_file
    if p_value_threshold is not None:
        params["p_value_threshold"] = p_value_threshold
    if afd_directory is not None:
        params["afd_directory"] = afd_directory
    return params

def talairach_afd_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TalairachAfd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is not None:
        if not isinstance(params["subject_name"], str):
            raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("xfm_file", None) is not None:
        if not isinstance(params["xfm_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`xfm_file` has the wrong type: Received `{type(params["xfm_file"])}` expected `InputPathType`')
    if params.get("p_value_threshold", None) is not None:
        if not isinstance(params["p_value_threshold"], (float, int)):
            raise StyxValidationError(f'`p_value_threshold` has the wrong type: Received `{type(params["p_value_threshold"])}` expected `float`')
    if params.get("afd_directory", None) is not None:
        if not isinstance(params["afd_directory"], str):
            raise StyxValidationError(f'`afd_directory` has the wrong type: Received `{type(params["afd_directory"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def talairach_afd_cargs(params: TalairachAfd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("talairach_afd")
    v_0 = params.get("subject_name")
    if v_0 is not None:
        cargs.append("-subj")
        cargs.append(v_0)
    v_1 = params.get("xfm_file")
    if v_1 is not None:
        cargs.append("-xfm")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("p_value_threshold")
    if v_2 is not None:
        cargs.append("-T")
        cargs.append(str(v_2))
    v_3 = params.get("afd_directory")
    if v_3 is not None:
        cargs.append("-afd")
        cargs.append(v_3)
    if params.get("verbose"):
        cargs.append("-V")
    return cargs

def talairach_afd_outputs(params: TalairachAfd, execution: Execution) -> TalairachAfdOutputs:
    """Build the TalairachAfdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TalairachAfdOutputs(
        root=root_v,
    )

def talairach_afd_execute(params: TalairachAfd, runner: Runner | None = None) -> TalairachAfdOutputs:
    """
    talairach_afd

    Detects Talairach alignment failures

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    talairach_afd_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TALAIRACH_AFD_METADATA)
    execution.params(params)
    args = talairach_afd_cargs(params, execution)
    out = talairach_afd_outputs(params, execution)
    execution.run(args)
    return out

def talairach_afd(
    subject_name: str | None = None,
    xfm_file: InputPathType | None = None,
    p_value_threshold: float | None = None,
    afd_directory: str | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> TalairachAfdOutputs:
    """
    talairach_afd

    Detects Talairach alignment failures

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Specify subject's name
        xfm_file: Specify the talairach.xfm file to check
        p_value_threshold: Threshold the p-values at #; Talairach transforms for\
            subjects with p-values <= T are considered as very unlikely
        afd_directory: Specify directory containing .afd data files
        verbose: Enable verbose output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = talairach_afd_params(
        subject_name=subject_name,
        xfm_file=xfm_file,
        p_value_threshold=p_value_threshold,
        afd_directory=afd_directory,
        verbose=verbose,
    )
    return talairach_afd_execute(params, runner)

__all__ = [
    "TalairachAfd",
    "TalairachAfdOutputs",
    "TALAIRACH_AFD_METADATA",
    "talairach_afd_cargs",
    "talairach_afd_outputs",
    "talairach_afd_params",
    "talairach_afd_execute",
    "talairach_afd_validate",
    "talairach_afd",
]