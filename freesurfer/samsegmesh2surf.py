# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SAMSEGMESH2SURF_METADATA = Metadata(
    id="samsegmesh2surf",
    name="samsegmesh2surf",
    package="freesurfer",
)

Samsegmesh2surf = typing.TypedDict(
    "Samsegmesh2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/samsegmesh2surf"]],
        "atlas_mesh": InputPathType,
        "template": typing.NotRequired[InputPathType],
        "lta_transform": typing.NotRequired[InputPathType],
        "output_surface": typing.NotRequired[str],
        "output_priors": typing.NotRequired[str],
        "invert_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Samsegmesh2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface_file: OutputPathType | None
    """FreeSurfer surface file generated from SAMSEG atlas mesh"""
    output_priors_file: OutputPathType | None
    """Priors MRI volume at each vertex"""

def samsegmesh2surf_params(
    atlas_mesh: InputPathType,
    template: InputPathType | None = None,
    lta_transform: InputPathType | None = None,
    output_surface: str | None = None,
    output_priors: str | None = None,
    invert_flag: bool = False,
) -> Samsegmesh2surf:
    """
    Build parameters.

    Args:
        atlas_mesh: Input SAMSEG atlas mesh collection file
        template: Input atlas template volume
        lta_transform: Input LTA transform to be applied to surface
        output_surface: Output FreeSurfer surface file
        output_priors: Output priors as MRI volume
        invert_flag: Invert LTA transform

    Returns:
        Parameter dictionary.
    """
    params: Samsegmesh2surf = {
        "@type": "freesurfer/samsegmesh2surf",
        "atlas_mesh": atlas_mesh,
        "invert_flag": invert_flag,
    }
    if template is not None:
        params["template"] = template
    if lta_transform is not None:
        params["lta_transform"] = lta_transform
    if output_surface is not None:
        params["output_surface"] = output_surface
    if output_priors is not None:
        params["output_priors"] = output_priors
    return params

def samsegmesh2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Samsegmesh2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("atlas_mesh", None) is None:
        raise StyxValidationError("`atlas_mesh` must not be None")
    if not isinstance(params["atlas_mesh"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_mesh` has the wrong type: Received `{type(params["atlas_mesh"])}` expected `InputPathType`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("lta_transform", None) is not None:
        if not isinstance(params["lta_transform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lta_transform` has the wrong type: Received `{type(params["lta_transform"])}` expected `InputPathType`')
    if params.get("output_surface", None) is not None:
        if not isinstance(params["output_surface"], str):
            raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("output_priors", None) is not None:
        if not isinstance(params["output_priors"], str):
            raise StyxValidationError(f'`output_priors` has the wrong type: Received `{type(params["output_priors"])}` expected `str`')
    if params.get("invert_flag", None) is not None:
        if not isinstance(params["invert_flag"], bool):
            raise StyxValidationError(f'`invert_flag` has the wrong type: Received `{type(params["invert_flag"])}` expected `bool`')

def samsegmesh2surf_cargs(params: Samsegmesh2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("samsegmesh2surf")
    cargs.append("--atlasmesh")
    cargs.append(execution.input_file(params["atlas_mesh"]))
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("--template")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("lta_transform")
    if v_1 is not None:
        cargs.append("--lta")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("output_surface")
    if v_2 is not None:
        cargs.append("--osurf")
        cargs.append(v_2)
    v_3 = params.get("output_priors")
    if v_3 is not None:
        cargs.append("--opriors")
        cargs.append(v_3)
    if params.get("invert_flag"):
        cargs.append("--invert")
    return cargs

def samsegmesh2surf_outputs(params: Samsegmesh2surf, execution: Execution) -> Samsegmesh2surfOutputs:
    """Build the Samsegmesh2surfOutputs object for this tool."""
    output_surface_file_v: OutputPathType | None = None
    output_priors_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_surface")
    if __v0 is not None:
        output_surface_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_priors")
    if __v1 is not None:
        output_priors_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    return Samsegmesh2surfOutputs(
        root=root_v,
        output_surface_file=output_surface_file_v,
        output_priors_file=output_priors_file_v,
    )

def samsegmesh2surf_execute(params: Samsegmesh2surf, runner: Runner | None = None) -> Samsegmesh2surfOutputs:
    """
    samsegmesh2surf

    Generate Freesurfer surface from a SAMSEG atlas mesh file and generate priors at each vertex as overlay MRI volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    samsegmesh2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SAMSEGMESH2SURF_METADATA)
    execution.params(params)
    args = samsegmesh2surf_cargs(params, execution)
    out = samsegmesh2surf_outputs(params, execution)
    execution.run(args)
    return out

def samsegmesh2surf(
    atlas_mesh: InputPathType,
    template: InputPathType | None = None,
    lta_transform: InputPathType | None = None,
    output_surface: str | None = None,
    output_priors: str | None = None,
    invert_flag: bool = False,
    runner: Runner | None = None,
) -> Samsegmesh2surfOutputs:
    """
    samsegmesh2surf

    Generate Freesurfer surface from a SAMSEG atlas mesh file and generate priors at each vertex as overlay MRI volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        atlas_mesh: Input SAMSEG atlas mesh collection file
        template: Input atlas template volume
        lta_transform: Input LTA transform to be applied to surface
        output_surface: Output FreeSurfer surface file
        output_priors: Output priors as MRI volume
        invert_flag: Invert LTA transform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = samsegmesh2surf_params(
        atlas_mesh=atlas_mesh,
        template=template,
        lta_transform=lta_transform,
        output_surface=output_surface,
        output_priors=output_priors,
        invert_flag=invert_flag,
    )
    return samsegmesh2surf_execute(params, runner)

__all__ = [
    "Samsegmesh2surf",
    "Samsegmesh2surfOutputs",
    "SAMSEGMESH2SURF_METADATA",
    "samsegmesh2surf_cargs",
    "samsegmesh2surf_outputs",
    "samsegmesh2surf_params",
    "samsegmesh2surf_execute",
    "samsegmesh2surf_validate",
    "samsegmesh2surf",
]