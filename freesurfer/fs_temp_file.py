# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_TEMP_FILE_METADATA = Metadata(
    id="fs_temp_file",
    name="fs_temp_file",
    package="freesurfer",
)

FsTempFile = typing.TypedDict(
    "FsTempFile",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_temp_file"]],
        "base_dir": typing.NotRequired[str],
        "base_dir_alt": typing.NotRequired[str],
        "suffix": typing.NotRequired[str],
        "suffix_alt": typing.NotRequired[str],
        "scratch": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "help_alt": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsTempFileOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fs_temp_file_params(
    base_dir: str | None = None,
    base_dir_alt: str | None = None,
    suffix: str | None = None,
    suffix_alt: str | None = None,
    scratch: bool = False,
    help: bool = False,
    help_alt: bool = False,
) -> FsTempFile:
    """
    Build parameters.

    Args:
        base_dir: Manually specify base temporary directory.
        base_dir_alt: Manually specify base temporary directory.
        suffix: Optional file suffix.
        suffix_alt: Optional file suffix.
        scratch: Use /scratch directory if available, but FS_TMPDIR takes priority.
        help: Print help text and exit.
        help_alt: Print help text and exit.

    Returns:
        Parameter dictionary.
    """
    params: FsTempFile = {
        "@type": "freesurfer/fs_temp_file",
        "scratch": scratch,
        "help": help,
        "help_alt": help_alt,
    }
    if base_dir is not None:
        params["base_dir"] = base_dir
    if base_dir_alt is not None:
        params["base_dir_alt"] = base_dir_alt
    if suffix is not None:
        params["suffix"] = suffix
    if suffix_alt is not None:
        params["suffix_alt"] = suffix_alt
    return params

def fs_temp_file_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsTempFile."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_dir", None) is not None:
        if not isinstance(params["base_dir"], str):
            raise StyxValidationError(f'`base_dir` has the wrong type: Received `{type(params["base_dir"])}` expected `str`')
    if params.get("base_dir_alt", None) is not None:
        if not isinstance(params["base_dir_alt"], str):
            raise StyxValidationError(f'`base_dir_alt` has the wrong type: Received `{type(params["base_dir_alt"])}` expected `str`')
    if params.get("suffix", None) is not None:
        if not isinstance(params["suffix"], str):
            raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')
    if params.get("suffix_alt", None) is not None:
        if not isinstance(params["suffix_alt"], str):
            raise StyxValidationError(f'`suffix_alt` has the wrong type: Received `{type(params["suffix_alt"])}` expected `str`')
    if params.get("scratch", None) is not None:
        if not isinstance(params["scratch"], bool):
            raise StyxValidationError(f'`scratch` has the wrong type: Received `{type(params["scratch"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("help_alt", None) is not None:
        if not isinstance(params["help_alt"], bool):
            raise StyxValidationError(f'`help_alt` has the wrong type: Received `{type(params["help_alt"])}` expected `bool`')

def fs_temp_file_cargs(params: FsTempFile, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_temp_file")
    v_0 = params.get("base_dir")
    if v_0 is not None:
        cargs.append("-b")
        cargs.append(v_0)
    v_1 = params.get("base_dir_alt")
    if v_1 is not None:
        cargs.append("--base")
        cargs.append(v_1)
    v_2 = params.get("suffix")
    if v_2 is not None:
        cargs.append("-s")
        cargs.append(v_2)
    v_3 = params.get("suffix_alt")
    if v_3 is not None:
        cargs.append("--suffix")
        cargs.append(v_3)
    if params.get("scratch"):
        cargs.append("--scratch")
    if params.get("help"):
        cargs.append("-h")
    if params.get("help_alt"):
        cargs.append("--help")
    return cargs

def fs_temp_file_outputs(params: FsTempFile, execution: Execution) -> FsTempFileOutputs:
    """Build the FsTempFileOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsTempFileOutputs(
        root=root_v,
    )

def fs_temp_file_execute(params: FsTempFile, runner: Runner | None = None) -> FsTempFileOutputs:
    """
    fs_temp_file

    Generates and creates an empty temporary file, printing the resulting path to stdout.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_temp_file_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_TEMP_FILE_METADATA)
    execution.params(params)
    args = fs_temp_file_cargs(params, execution)
    out = fs_temp_file_outputs(params, execution)
    execution.run(args)
    return out

def fs_temp_file(
    base_dir: str | None = None,
    base_dir_alt: str | None = None,
    suffix: str | None = None,
    suffix_alt: str | None = None,
    scratch: bool = False,
    help: bool = False,
    help_alt: bool = False,
    runner: Runner | None = None,
) -> FsTempFileOutputs:
    """
    fs_temp_file

    Generates and creates an empty temporary file, printing the resulting path to stdout.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_dir: Manually specify base temporary directory.
        base_dir_alt: Manually specify base temporary directory.
        suffix: Optional file suffix.
        suffix_alt: Optional file suffix.
        scratch: Use /scratch directory if available, but FS_TMPDIR takes priority.
        help: Print help text and exit.
        help_alt: Print help text and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_temp_file_params(
        base_dir=base_dir,
        base_dir_alt=base_dir_alt,
        suffix=suffix,
        suffix_alt=suffix_alt,
        scratch=scratch,
        help=help,
        help_alt=help_alt,
    )
    return fs_temp_file_execute(params, runner)

__all__ = [
    "FsTempFile",
    "FsTempFileOutputs",
    "FS_TEMP_FILE_METADATA",
    "fs_temp_file_cargs",
    "fs_temp_file_outputs",
    "fs_temp_file_params",
    "fs_temp_file_execute",
    "fs_temp_file_validate",
    "fs_temp_file",
]