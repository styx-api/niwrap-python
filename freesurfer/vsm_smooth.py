# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VSM_SMOOTH_METADATA = Metadata(
    id="vsm-smooth",
    name="vsm-smooth",
    package="freesurfer",
)

VsmSmooth = typing.TypedDict(
    "VsmSmooth",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vsm-smooth"]],
        "input_file": InputPathType,
        "output_file": str,
        "fwhm_value": float,
        "temp_dir": str,
    },
)

@dataclasses.dataclass
class VsmSmoothOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vsm: OutputPathType
    """The resulting smoothed voxel shift map."""

def vsm_smooth_params(
    input_file: InputPathType,
    output_file: str,
    fwhm_value: float,
    temp_dir: str,
) -> VsmSmooth:
    """
    Build parameters.

    Args:
        input_file:
        output_file:
        fwhm_value:
        temp_dir:

    Returns:
        Parameter dictionary.
    """
    params: VsmSmooth = {
        "@type": "freesurfer/vsm-smooth",
        "input_file": input_file,
        "output_file": output_file,
        "fwhm_value": fwhm_value,
        "temp_dir": temp_dir,
    }
    return params

def vsm_smooth_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VsmSmooth."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("fwhm_value", None) is None:
        raise StyxValidationError("`fwhm_value` must not be None")
    if not isinstance(params["fwhm_value"], (float, int)):
        raise StyxValidationError(f'`fwhm_value` has the wrong type: Received `{type(params["fwhm_value"])}` expected `float`')
    if params.get("temp_dir", None) is None:
        raise StyxValidationError("`temp_dir` must not be None")
    if not isinstance(params["temp_dir"], str):
        raise StyxValidationError(f'`temp_dir` has the wrong type: Received `{type(params["temp_dir"])}` expected `str`')

def vsm_smooth_cargs(params: VsmSmooth, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vsm-smooth")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("--o")
    cargs.append(params["output_file"])
    cargs.append("--fwhm")
    cargs.append(str(params["fwhm_value"]))
    cargs.append("--tmpdir")
    cargs.append(params["temp_dir"])
    return cargs

def vsm_smooth_outputs(params: VsmSmooth, execution: Execution) -> VsmSmoothOutputs:
    """Build the VsmSmoothOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_vsm_v: OutputPathType = execution.output_file(params["output_file"])
    return VsmSmoothOutputs(
        root=root_v,
        output_vsm=output_vsm_v,
    )

def vsm_smooth_execute(params: VsmSmooth, runner: Runner | None = None) -> VsmSmoothOutputs:
    """
    vsm-smooth

    Implements a masked smoothing in which the input (vsm) is unchanged in voxels that have a non-zero value. In voxels with a zero value, the value is replaced with vsm smoothed by the given amount. This will likely only change the voxels that are near the edge of the non-zero voxels. This is a simple way to extrapolate the non-zero voxels beyond their range. This works well for a B0 distortion correction voxel shift map (vsm), but it can be applied to other maps.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vsm_smooth_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VSM_SMOOTH_METADATA)
    execution.params(params)
    args = vsm_smooth_cargs(params, execution)
    out = vsm_smooth_outputs(params, execution)
    execution.run(args)
    return out

def vsm_smooth(
    input_file: InputPathType,
    output_file: str,
    fwhm_value: float,
    temp_dir: str,
    runner: Runner | None = None,
) -> VsmSmoothOutputs:
    """
    vsm-smooth

    Implements a masked smoothing in which the input (vsm) is unchanged in voxels that have a non-zero value. In voxels with a zero value, the value is replaced with vsm smoothed by the given amount. This will likely only change the voxels that are near the edge of the non-zero voxels. This is a simple way to extrapolate the non-zero voxels beyond their range. This works well for a B0 distortion correction voxel shift map (vsm), but it can be applied to other maps.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file:
        output_file:
        fwhm_value:
        temp_dir:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vsm_smooth_params(
        input_file=input_file,
        output_file=output_file,
        fwhm_value=fwhm_value,
        temp_dir=temp_dir,
    )
    return vsm_smooth_execute(params, runner)

__all__ = [
    "VsmSmooth",
    "VsmSmoothOutputs",
    "VSM_SMOOTH_METADATA",
    "vsm_smooth_cargs",
    "vsm_smooth_outputs",
    "vsm_smooth_params",
    "vsm_smooth_execute",
    "vsm_smooth_validate",
    "vsm_smooth",
]