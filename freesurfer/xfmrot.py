# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XFMROT_METADATA = Metadata(
    id="xfmrot",
    name="xfmrot",
    package="freesurfer",
)

Xfmrot = typing.TypedDict(
    "Xfmrot",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xfmrot"]],
        "transform_file": InputPathType,
        "input_vector_file": InputPathType,
        "output_vector_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class XfmrotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_vector: OutputPathType | None
    """The transformed output vector file with the same format as input."""

def xfmrot_params(
    transform_file: InputPathType,
    input_vector_file: InputPathType,
    output_vector_file: str | None = None,
) -> Xfmrot:
    """
    Build parameters.

    Args:
        transform_file: Transform file, can be an eddy_correct/eddy log file or a\
            .mat file.
        input_vector_file: Input vector file which can be formatted in 3 rows or 3\
            columns.
        output_vector_file: Output vector file will have the same format as input.

    Returns:
        Parameter dictionary.
    """
    params: Xfmrot = {
        "@type": "freesurfer/xfmrot",
        "transform_file": transform_file,
        "input_vector_file": input_vector_file,
    }
    if output_vector_file is not None:
        params["output_vector_file"] = output_vector_file
    return params

def xfmrot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Xfmrot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("input_vector_file", None) is None:
        raise StyxValidationError("`input_vector_file` must not be None")
    if not isinstance(params["input_vector_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vector_file` has the wrong type: Received `{type(params["input_vector_file"])}` expected `InputPathType`')
    if params.get("output_vector_file", None) is not None:
        if not isinstance(params["output_vector_file"], str):
            raise StyxValidationError(f'`output_vector_file` has the wrong type: Received `{type(params["output_vector_file"])}` expected `str`')

def xfmrot_cargs(params: Xfmrot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xfmrot")
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(execution.input_file(params["input_vector_file"]))
    v_0 = params.get("output_vector_file")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def xfmrot_outputs(params: Xfmrot, execution: Execution) -> XfmrotOutputs:
    """Build the XfmrotOutputs object for this tool."""
    transformed_vector_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_vector_file")
    if __v0 is not None:
        transformed_vector_v = execution.output_file((__v0 if __v0 is not None else ""))
    return XfmrotOutputs(
        root=root_v,
        transformed_vector=transformed_vector_v,
    )

def xfmrot_execute(params: Xfmrot, runner: Runner | None = None) -> XfmrotOutputs:
    """
    xfmrot

    Tool to apply a transformation defined in a transform file to an input vector file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xfmrot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XFMROT_METADATA)
    execution.params(params)
    args = xfmrot_cargs(params, execution)
    out = xfmrot_outputs(params, execution)
    execution.run(args)
    return out

def xfmrot(
    transform_file: InputPathType,
    input_vector_file: InputPathType,
    output_vector_file: str | None = None,
    runner: Runner | None = None,
) -> XfmrotOutputs:
    """
    xfmrot

    Tool to apply a transformation defined in a transform file to an input vector file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        transform_file: Transform file, can be an eddy_correct/eddy log file or a\
            .mat file.
        input_vector_file: Input vector file which can be formatted in 3 rows or 3\
            columns.
        output_vector_file: Output vector file will have the same format as input.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xfmrot_params(
        transform_file=transform_file,
        input_vector_file=input_vector_file,
        output_vector_file=output_vector_file,
    )
    return xfmrot_execute(params, runner)

__all__ = [
    "Xfmrot",
    "XfmrotOutputs",
    "XFMROT_METADATA",
    "xfmrot_cargs",
    "xfmrot_outputs",
    "xfmrot_params",
    "xfmrot_execute",
    "xfmrot_validate",
    "xfmrot",
]