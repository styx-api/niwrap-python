# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CA_LABEL_METADATA = Metadata(
    id="mris_ca_label",
    name="mris_ca_label",
    package="freesurfer",
)

MrisCaLabel = typing.TypedDict(
    "MrisCaLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_ca_label"]],
        "subject": str,
        "hemi": str,
        "canonsurf": InputPathType,
        "classifier": InputPathType,
        "outputfile": str,
        "seed": typing.NotRequired[float],
        "sdir": typing.NotRequired[str],
        "orig": typing.NotRequired[str],
        "long_flag": typing.NotRequired[bool],
        "r": typing.NotRequired[InputPathType],
        "novar_flag": typing.NotRequired[bool],
        "nbrs": typing.NotRequired[float],
        "f": typing.NotRequired[float],
        "t": typing.NotRequired[InputPathType],
        "p": typing.NotRequired[str],
        "v": typing.NotRequired[float],
        "w": typing.NotRequired[str],
        "help_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisCaLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    labeled_surface: OutputPathType
    """Labeled cortical surface model"""

def mris_ca_label_params(
    subject: str,
    hemi: str,
    canonsurf: InputPathType,
    classifier: InputPathType,
    outputfile: str,
    seed: float | None = None,
    sdir: str | None = None,
    orig: str | None = None,
    long_flag: bool = False,
    r: InputPathType | None = None,
    novar_flag: bool = False,
    nbrs: float | None = None,
    f: float | None = None,
    t: InputPathType | None = None,
    p: str | None = None,
    v: float | None = None,
    w: str | None = None,
    help_flag: bool = False,
    version_flag: bool = False,
) -> MrisCaLabel:
    """
    Build parameters.

    Args:
        subject: Subject ID for labeling
        hemi: Hemisphere (rh or lh)
        canonsurf: Name of the spherical surface file that describes the\
            registration of a subject's vertices to the average surface
        classifier: Specify classifier array input file
        outputfile: Output annotated surface file listing the region label for each\
            vertex
        seed: Specify the seed for the random number generator
        sdir: Specify a subjects directory (default=$SUBJECTS_DIR)
        orig: Specify filename of original surface (default=smoothwm)
        long_flag: Refines the initial labeling read-in from -r (default: disabled)
        r: File containing precomputed parcellation
        novar_flag: Sets all covariance matrices to the identity (default:\
            disabled)
        nbrs: Neighborhood size (default=2)
        f: Applies mode filter <number> times before writing output (default=10)
        t: Specify parcellation table input file (default: none)
        p: Output file containing label probability for each vertex.
        v: Diagnostic level (default=0)
        w: Writes-out snapshots of gibbs process every <number> iterations to\
            <filename> (default=disabled)
        help_flag: Print help information
        version_flag: Print version information

    Returns:
        Parameter dictionary.
    """
    params: MrisCaLabel = {
        "@type": "freesurfer/mris_ca_label",
        "subject": subject,
        "hemi": hemi,
        "canonsurf": canonsurf,
        "classifier": classifier,
        "outputfile": outputfile,
        "long_flag": long_flag,
        "novar_flag": novar_flag,
        "help_flag": help_flag,
        "version_flag": version_flag,
    }
    if seed is not None:
        params["seed"] = seed
    if sdir is not None:
        params["sdir"] = sdir
    if orig is not None:
        params["orig"] = orig
    if r is not None:
        params["r"] = r
    if nbrs is not None:
        params["nbrs"] = nbrs
    if f is not None:
        params["f"] = f
    if t is not None:
        params["t"] = t
    if p is not None:
        params["p"] = p
    if v is not None:
        params["v"] = v
    if w is not None:
        params["w"] = w
    return params

def mris_ca_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCaLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("canonsurf", None) is None:
        raise StyxValidationError("`canonsurf` must not be None")
    if not isinstance(params["canonsurf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`canonsurf` has the wrong type: Received `{type(params["canonsurf"])}` expected `InputPathType`')
    if params.get("classifier", None) is None:
        raise StyxValidationError("`classifier` must not be None")
    if not isinstance(params["classifier"], (pathlib.Path, str)):
        raise StyxValidationError(f'`classifier` has the wrong type: Received `{type(params["classifier"])}` expected `InputPathType`')
    if params.get("outputfile", None) is None:
        raise StyxValidationError("`outputfile` must not be None")
    if not isinstance(params["outputfile"], str):
        raise StyxValidationError(f'`outputfile` has the wrong type: Received `{type(params["outputfile"])}` expected `str`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (float, int)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `float`')
    if params.get("sdir", None) is not None:
        if not isinstance(params["sdir"], str):
            raise StyxValidationError(f'`sdir` has the wrong type: Received `{type(params["sdir"])}` expected `str`')
    if params.get("orig", None) is not None:
        if not isinstance(params["orig"], str):
            raise StyxValidationError(f'`orig` has the wrong type: Received `{type(params["orig"])}` expected `str`')
    if params.get("long_flag", None) is not None:
        if not isinstance(params["long_flag"], bool):
            raise StyxValidationError(f'`long_flag` has the wrong type: Received `{type(params["long_flag"])}` expected `bool`')
    if params.get("r", None) is not None:
        if not isinstance(params["r"], (pathlib.Path, str)):
            raise StyxValidationError(f'`r` has the wrong type: Received `{type(params["r"])}` expected `InputPathType`')
    if params.get("novar_flag", None) is not None:
        if not isinstance(params["novar_flag"], bool):
            raise StyxValidationError(f'`novar_flag` has the wrong type: Received `{type(params["novar_flag"])}` expected `bool`')
    if params.get("nbrs", None) is not None:
        if not isinstance(params["nbrs"], (float, int)):
            raise StyxValidationError(f'`nbrs` has the wrong type: Received `{type(params["nbrs"])}` expected `float`')
    if params.get("f", None) is not None:
        if not isinstance(params["f"], (float, int)):
            raise StyxValidationError(f'`f` has the wrong type: Received `{type(params["f"])}` expected `float`')
    if params.get("t", None) is not None:
        if not isinstance(params["t"], (pathlib.Path, str)):
            raise StyxValidationError(f'`t` has the wrong type: Received `{type(params["t"])}` expected `InputPathType`')
    if params.get("p", None) is not None:
        if not isinstance(params["p"], str):
            raise StyxValidationError(f'`p` has the wrong type: Received `{type(params["p"])}` expected `str`')
    if params.get("v", None) is not None:
        if not isinstance(params["v"], (float, int)):
            raise StyxValidationError(f'`v` has the wrong type: Received `{type(params["v"])}` expected `float`')
    if params.get("w", None) is not None:
        if not isinstance(params["w"], str):
            raise StyxValidationError(f'`w` has the wrong type: Received `{type(params["w"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def mris_ca_label_cargs(params: MrisCaLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_ca_label")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["canonsurf"]))
    cargs.append(execution.input_file(params["classifier"]))
    cargs.append(params["outputfile"])
    v_0 = params.get("seed")
    if v_0 is not None:
        cargs.append("-seed")
        cargs.append(str(v_0))
    v_1 = params.get("sdir")
    if v_1 is not None:
        cargs.append("-sdir")
        cargs.append(v_1)
    v_2 = params.get("orig")
    if v_2 is not None:
        cargs.append("-orig")
        cargs.append(v_2)
    if params.get("long_flag"):
        cargs.append("-long")
    v_3 = params.get("r")
    if v_3 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_3))
    if params.get("novar_flag"):
        cargs.append("-novar")
    v_4 = params.get("nbrs")
    if v_4 is not None:
        cargs.append("-nbrs")
        cargs.append(str(v_4))
    v_5 = params.get("f")
    if v_5 is not None:
        cargs.append("-f")
        cargs.append(str(v_5))
    v_6 = params.get("t")
    if v_6 is not None:
        cargs.append("-t")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("p")
    if v_7 is not None:
        cargs.append("-p")
        cargs.append(v_7)
    v_8 = params.get("v")
    if v_8 is not None:
        cargs.append("-v")
        cargs.append(str(v_8))
    v_9 = params.get("w")
    if v_9 is not None:
        cargs.append("-w")
        cargs.append(v_9)
    if params.get("help_flag"):
        cargs.append("--help")
    if params.get("version_flag"):
        cargs.append("--version")
    return cargs

def mris_ca_label_outputs(params: MrisCaLabel, execution: Execution) -> MrisCaLabelOutputs:
    """Build the MrisCaLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    labeled_surface_v: OutputPathType = execution.output_file(params["outputfile"])
    return MrisCaLabelOutputs(
        root=root_v,
        labeled_surface=labeled_surface_v,
    )

def mris_ca_label_execute(params: MrisCaLabel, runner: Runner | None = None) -> MrisCaLabelOutputs:
    """
    mris_ca_label

    Produces an annotation file in which each cortical surface vertex is assigned a neuroanatomical label using a previously-prepared atlas file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_ca_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CA_LABEL_METADATA)
    execution.params(params)
    args = mris_ca_label_cargs(params, execution)
    out = mris_ca_label_outputs(params, execution)
    execution.run(args)
    return out

def mris_ca_label(
    subject: str,
    hemi: str,
    canonsurf: InputPathType,
    classifier: InputPathType,
    outputfile: str,
    seed: float | None = None,
    sdir: str | None = None,
    orig: str | None = None,
    long_flag: bool = False,
    r: InputPathType | None = None,
    novar_flag: bool = False,
    nbrs: float | None = None,
    f: float | None = None,
    t: InputPathType | None = None,
    p: str | None = None,
    v: float | None = None,
    w: str | None = None,
    help_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> MrisCaLabelOutputs:
    """
    mris_ca_label

    Produces an annotation file in which each cortical surface vertex is assigned a neuroanatomical label using a previously-prepared atlas file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject ID for labeling
        hemi: Hemisphere (rh or lh)
        canonsurf: Name of the spherical surface file that describes the\
            registration of a subject's vertices to the average surface
        classifier: Specify classifier array input file
        outputfile: Output annotated surface file listing the region label for each\
            vertex
        seed: Specify the seed for the random number generator
        sdir: Specify a subjects directory (default=$SUBJECTS_DIR)
        orig: Specify filename of original surface (default=smoothwm)
        long_flag: Refines the initial labeling read-in from -r (default: disabled)
        r: File containing precomputed parcellation
        novar_flag: Sets all covariance matrices to the identity (default:\
            disabled)
        nbrs: Neighborhood size (default=2)
        f: Applies mode filter <number> times before writing output (default=10)
        t: Specify parcellation table input file (default: none)
        p: Output file containing label probability for each vertex.
        v: Diagnostic level (default=0)
        w: Writes-out snapshots of gibbs process every <number> iterations to\
            <filename> (default=disabled)
        help_flag: Print help information
        version_flag: Print version information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_ca_label_params(
        subject=subject,
        hemi=hemi,
        canonsurf=canonsurf,
        classifier=classifier,
        outputfile=outputfile,
        seed=seed,
        sdir=sdir,
        orig=orig,
        long_flag=long_flag,
        r=r,
        novar_flag=novar_flag,
        nbrs=nbrs,
        f=f,
        t=t,
        p=p,
        v=v,
        w=w,
        help_flag=help_flag,
        version_flag=version_flag,
    )
    return mris_ca_label_execute(params, runner)

__all__ = [
    "MrisCaLabel",
    "MrisCaLabelOutputs",
    "MRIS_CA_LABEL_METADATA",
    "mris_ca_label_cargs",
    "mris_ca_label_outputs",
    "mris_ca_label_params",
    "mris_ca_label_execute",
    "mris_ca_label_validate",
    "mris_ca_label",
]