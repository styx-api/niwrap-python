# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABELS_INTERSECT_METADATA = Metadata(
    id="labels_intersect",
    name="labels_intersect",
    package="freesurfer",
)

LabelsIntersect = typing.TypedDict(
    "LabelsIntersect",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/labels_intersect"]],
        "label1": InputPathType,
        "label2": InputPathType,
        "outputname": str,
    },
)

@dataclasses.dataclass
class LabelsIntersectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label: OutputPathType
    """The resulting intersected label file"""

def labels_intersect_params(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
) -> LabelsIntersect:
    """
    Build parameters.

    Args:
        label1: First input label file
        label2: Second input label file
        outputname: Output label file name

    Returns:
        Parameter dictionary.
    """
    params: LabelsIntersect = {
        "@type": "freesurfer/labels_intersect",
        "label1": label1,
        "label2": label2,
        "outputname": outputname,
    }
    return params

def labels_intersect_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelsIntersect."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label1", None) is None:
        raise StyxValidationError("`label1` must not be None")
    if not isinstance(params["label1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label1` has the wrong type: Received `{type(params["label1"])}` expected `InputPathType`')
    if params.get("label2", None) is None:
        raise StyxValidationError("`label2` must not be None")
    if not isinstance(params["label2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label2` has the wrong type: Received `{type(params["label2"])}` expected `InputPathType`')
    if params.get("outputname", None) is None:
        raise StyxValidationError("`outputname` must not be None")
    if not isinstance(params["outputname"], str):
        raise StyxValidationError(f'`outputname` has the wrong type: Received `{type(params["outputname"])}` expected `str`')

def labels_intersect_cargs(params: LabelsIntersect, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("labels_intersect")
    cargs.append(execution.input_file(params["label1"]))
    cargs.append(execution.input_file(params["label2"]))
    cargs.append(params["outputname"])
    return cargs

def labels_intersect_outputs(params: LabelsIntersect, execution: Execution) -> LabelsIntersectOutputs:
    """Build the LabelsIntersectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_v: OutputPathType = execution.output_file(params["outputname"])
    return LabelsIntersectOutputs(
        root=root_v,
        output_label=output_label_v,
    )

def labels_intersect_execute(params: LabelsIntersect, runner: Runner | None = None) -> LabelsIntersectOutputs:
    """
    labels_intersect

    Tool to find the intersection of two label files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    labels_intersect_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABELS_INTERSECT_METADATA)
    execution.params(params)
    args = labels_intersect_cargs(params, execution)
    out = labels_intersect_outputs(params, execution)
    execution.run(args)
    return out

def labels_intersect(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
    runner: Runner | None = None,
) -> LabelsIntersectOutputs:
    """
    labels_intersect

    Tool to find the intersection of two label files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label1: First input label file
        label2: Second input label file
        outputname: Output label file name
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = labels_intersect_params(
        label1=label1,
        label2=label2,
        outputname=outputname,
    )
    return labels_intersect_execute(params, runner)

__all__ = [
    "LabelsIntersect",
    "LabelsIntersectOutputs",
    "LABELS_INTERSECT_METADATA",
    "labels_intersect_cargs",
    "labels_intersect_outputs",
    "labels_intersect_params",
    "labels_intersect_execute",
    "labels_intersect_validate",
    "labels_intersect",
]