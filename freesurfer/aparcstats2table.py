# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APARCSTATS2TABLE_METADATA = Metadata(
    id="aparcstats2table",
    name="aparcstats2table",
    package="freesurfer",
)

Aparcstats2table = typing.TypedDict(
    "Aparcstats2table",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/aparcstats2table"]],
        "subjects": typing.NotRequired[list[str]],
        "subjectsfile": typing.NotRequired[InputPathType],
        "qdec": typing.NotRequired[InputPathType],
        "qdec_long": typing.NotRequired[InputPathType],
        "hemi": str,
        "tablefile": InputPathType,
        "parcellation": typing.NotRequired[str],
        "measure": typing.NotRequired[str],
        "delimiter": typing.NotRequired[str],
        "skip_missing": typing.NotRequired[bool],
        "parcid_only": typing.NotRequired[bool],
        "common_parcs": typing.NotRequired[bool],
        "parcs_file": typing.NotRequired[InputPathType],
        "report_rois": typing.NotRequired[bool],
        "transpose": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "etiv": typing.NotRequired[bool],
        "scale": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class Aparcstats2tableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_table: OutputPathType
    """Output table file with processed data."""

def aparcstats2table_params(
    hemi: str,
    tablefile: InputPathType,
    subjects: list[str] | None = None,
    subjectsfile: InputPathType | None = None,
    qdec: InputPathType | None = None,
    qdec_long: InputPathType | None = None,
    parcellation: str | None = None,
    measure: str | None = None,
    delimiter: str | None = None,
    skip_missing: bool = False,
    parcid_only: bool = False,
    common_parcs: bool = False,
    parcs_file: InputPathType | None = None,
    report_rois: bool = False,
    transpose: bool = False,
    debug: bool = False,
    etiv: bool = False,
    scale: float | None = None,
) -> Aparcstats2table:
    """
    Build parameters.

    Args:
        hemi: Hemisphere being analyzed (lh or rh required).
        tablefile: Output table file.
        subjects: List of subject IDs.
        subjectsfile: File containing list of subjects (one per line).
        qdec: QDEC table file with subject IDs.
        qdec_long: Longitudinal QDEC table with subject templates.
        parcellation: Parcellation atlas (default: aparc).
        measure: Measure type (e.g., area, volume, thickness).
        delimiter: Delimiter between measures in the table (default: tab).
        skip_missing: Skip subjects if input is not found.
        parcid_only: Write only ROI names without pre/appended hemi/measure.
        common_parcs: Output only common parcellations across all subjects.
        parcs_file: File specifying which parcellations to output.
        report_rois: Print ROI information for each subject.
        transpose: Transpose the table (subjects in columns).
        debug: Increase verbosity.
        etiv: Report volume as percent estimated total intracranial volume.
        scale: Scale factor for output values.

    Returns:
        Parameter dictionary.
    """
    params: Aparcstats2table = {
        "@type": "freesurfer/aparcstats2table",
        "hemi": hemi,
        "tablefile": tablefile,
        "skip_missing": skip_missing,
        "parcid_only": parcid_only,
        "common_parcs": common_parcs,
        "report_rois": report_rois,
        "transpose": transpose,
        "debug": debug,
        "etiv": etiv,
    }
    if subjects is not None:
        params["subjects"] = subjects
    if subjectsfile is not None:
        params["subjectsfile"] = subjectsfile
    if qdec is not None:
        params["qdec"] = qdec
    if qdec_long is not None:
        params["qdec_long"] = qdec_long
    if parcellation is not None:
        params["parcellation"] = parcellation
    if measure is not None:
        params["measure"] = measure
    if delimiter is not None:
        params["delimiter"] = delimiter
    if parcs_file is not None:
        params["parcs_file"] = parcs_file
    if scale is not None:
        params["scale"] = scale
    return params

def aparcstats2table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aparcstats2table."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is not None:
        if not isinstance(params["subjects"], list):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
        for e in params["subjects"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subjectsfile", None) is not None:
        if not isinstance(params["subjectsfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`subjectsfile` has the wrong type: Received `{type(params["subjectsfile"])}` expected `InputPathType`')
    if params.get("qdec", None) is not None:
        if not isinstance(params["qdec"], (pathlib.Path, str)):
            raise StyxValidationError(f'`qdec` has the wrong type: Received `{type(params["qdec"])}` expected `InputPathType`')
    if params.get("qdec_long", None) is not None:
        if not isinstance(params["qdec_long"], (pathlib.Path, str)):
            raise StyxValidationError(f'`qdec_long` has the wrong type: Received `{type(params["qdec_long"])}` expected `InputPathType`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("tablefile", None) is None:
        raise StyxValidationError("`tablefile` must not be None")
    if not isinstance(params["tablefile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tablefile` has the wrong type: Received `{type(params["tablefile"])}` expected `InputPathType`')
    if params.get("parcellation", None) is not None:
        if not isinstance(params["parcellation"], str):
            raise StyxValidationError(f'`parcellation` has the wrong type: Received `{type(params["parcellation"])}` expected `str`')
    if params.get("measure", None) is not None:
        if not isinstance(params["measure"], str):
            raise StyxValidationError(f'`measure` has the wrong type: Received `{type(params["measure"])}` expected `str`')
    if params.get("delimiter", None) is not None:
        if not isinstance(params["delimiter"], str):
            raise StyxValidationError(f'`delimiter` has the wrong type: Received `{type(params["delimiter"])}` expected `str`')
    if params.get("skip_missing", None) is not None:
        if not isinstance(params["skip_missing"], bool):
            raise StyxValidationError(f'`skip_missing` has the wrong type: Received `{type(params["skip_missing"])}` expected `bool`')
    if params.get("parcid_only", None) is not None:
        if not isinstance(params["parcid_only"], bool):
            raise StyxValidationError(f'`parcid_only` has the wrong type: Received `{type(params["parcid_only"])}` expected `bool`')
    if params.get("common_parcs", None) is not None:
        if not isinstance(params["common_parcs"], bool):
            raise StyxValidationError(f'`common_parcs` has the wrong type: Received `{type(params["common_parcs"])}` expected `bool`')
    if params.get("parcs_file", None) is not None:
        if not isinstance(params["parcs_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`parcs_file` has the wrong type: Received `{type(params["parcs_file"])}` expected `InputPathType`')
    if params.get("report_rois", None) is not None:
        if not isinstance(params["report_rois"], bool):
            raise StyxValidationError(f'`report_rois` has the wrong type: Received `{type(params["report_rois"])}` expected `bool`')
    if params.get("transpose", None) is not None:
        if not isinstance(params["transpose"], bool):
            raise StyxValidationError(f'`transpose` has the wrong type: Received `{type(params["transpose"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("etiv", None) is not None:
        if not isinstance(params["etiv"], bool):
            raise StyxValidationError(f'`etiv` has the wrong type: Received `{type(params["etiv"])}` expected `bool`')
    if params.get("scale", None) is not None:
        if not isinstance(params["scale"], (float, int)):
            raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `float`')

def aparcstats2table_cargs(params: Aparcstats2table, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aparcstats2table")
    v_0 = params.get("subjects")
    if v_0 is not None:
        cargs.append("--subjects")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("subjectsfile")
    if v_1 is not None:
        cargs.append("--subjectsfile")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("qdec")
    if v_2 is not None:
        cargs.append("--qdec")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("qdec_long")
    if v_3 is not None:
        cargs.append("--qdec-long")
        cargs.append(execution.input_file(v_3))
    cargs.append("--hemi")
    cargs.append(params["hemi"])
    cargs.append("-t")
    cargs.append(execution.input_file(params["tablefile"]))
    v_4 = params.get("parcellation")
    if v_4 is not None:
        cargs.append("--parc")
        cargs.append(v_4)
    v_5 = params.get("measure")
    if v_5 is not None:
        cargs.append("-m")
        cargs.append(v_5)
    v_6 = params.get("delimiter")
    if v_6 is not None:
        cargs.append("-d")
        cargs.append(v_6)
    if params.get("skip_missing"):
        cargs.append("--skip")
    if params.get("parcid_only"):
        cargs.append("--parcid-only")
    if params.get("common_parcs"):
        cargs.append("--common-parcs")
    v_7 = params.get("parcs_file")
    if v_7 is not None:
        cargs.append("--parcs-from-file")
        cargs.append(execution.input_file(v_7))
    if params.get("report_rois"):
        cargs.append("--report-rois")
    if params.get("transpose"):
        cargs.append("--transpose")
    if params.get("debug"):
        cargs.append("-v")
    if params.get("etiv"):
        cargs.append("--etiv")
    v_8 = params.get("scale")
    if v_8 is not None:
        cargs.append("--scale")
        cargs.append(str(v_8))
    return cargs

def aparcstats2table_outputs(params: Aparcstats2table, execution: Execution) -> Aparcstats2tableOutputs:
    """Build the Aparcstats2tableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_table_v: OutputPathType = execution.output_file(params["tablefile"])
    return Aparcstats2tableOutputs(
        root=root_v,
        output_table=output_table_v,
    )

def aparcstats2table_execute(params: Aparcstats2table, runner: Runner | None = None) -> Aparcstats2tableOutputs:
    """
    aparcstats2table

    Converts a cortical stats file into a table format with subjects as rows and parcellations as columns.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aparcstats2table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APARCSTATS2TABLE_METADATA)
    execution.params(params)
    args = aparcstats2table_cargs(params, execution)
    out = aparcstats2table_outputs(params, execution)
    execution.run(args)
    return out

def aparcstats2table(
    hemi: str,
    tablefile: InputPathType,
    subjects: list[str] | None = None,
    subjectsfile: InputPathType | None = None,
    qdec: InputPathType | None = None,
    qdec_long: InputPathType | None = None,
    parcellation: str | None = None,
    measure: str | None = None,
    delimiter: str | None = None,
    skip_missing: bool = False,
    parcid_only: bool = False,
    common_parcs: bool = False,
    parcs_file: InputPathType | None = None,
    report_rois: bool = False,
    transpose: bool = False,
    debug: bool = False,
    etiv: bool = False,
    scale: float | None = None,
    runner: Runner | None = None,
) -> Aparcstats2tableOutputs:
    """
    aparcstats2table

    Converts a cortical stats file into a table format with subjects as rows and parcellations as columns.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hemi: Hemisphere being analyzed (lh or rh required).
        tablefile: Output table file.
        subjects: List of subject IDs.
        subjectsfile: File containing list of subjects (one per line).
        qdec: QDEC table file with subject IDs.
        qdec_long: Longitudinal QDEC table with subject templates.
        parcellation: Parcellation atlas (default: aparc).
        measure: Measure type (e.g., area, volume, thickness).
        delimiter: Delimiter between measures in the table (default: tab).
        skip_missing: Skip subjects if input is not found.
        parcid_only: Write only ROI names without pre/appended hemi/measure.
        common_parcs: Output only common parcellations across all subjects.
        parcs_file: File specifying which parcellations to output.
        report_rois: Print ROI information for each subject.
        transpose: Transpose the table (subjects in columns).
        debug: Increase verbosity.
        etiv: Report volume as percent estimated total intracranial volume.
        scale: Scale factor for output values.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aparcstats2table_params(
        hemi=hemi,
        tablefile=tablefile,
        subjects=subjects,
        subjectsfile=subjectsfile,
        qdec=qdec,
        qdec_long=qdec_long,
        parcellation=parcellation,
        measure=measure,
        delimiter=delimiter,
        skip_missing=skip_missing,
        parcid_only=parcid_only,
        common_parcs=common_parcs,
        parcs_file=parcs_file,
        report_rois=report_rois,
        transpose=transpose,
        debug=debug,
        etiv=etiv,
        scale=scale,
    )
    return aparcstats2table_execute(params, runner)

__all__ = [
    "Aparcstats2table",
    "Aparcstats2tableOutputs",
    "APARCSTATS2TABLE_METADATA",
    "aparcstats2table_cargs",
    "aparcstats2table_outputs",
    "aparcstats2table_params",
    "aparcstats2table_execute",
    "aparcstats2table_validate",
    "aparcstats2table",
]