# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SYNTHSR_METADATA = Metadata(
    id="mri_synthsr",
    name="mri_synthsr",
    package="freesurfer",
)

MriSynthsr = typing.TypedDict(
    "MriSynthsr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_synthsr"]],
        "input": str,
        "output": str,
        "ct": typing.NotRequired[bool],
        "disable_sharpening": typing.NotRequired[bool],
        "disable_flipping": typing.NotRequired[bool],
        "lowfield": typing.NotRequired[bool],
        "v1": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "cpu": typing.NotRequired[bool],
        "model": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriSynthsrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_synthsr_params(
    input_: str,
    output: str,
    ct: bool = False,
    disable_sharpening: bool = False,
    disable_flipping: bool = False,
    lowfield: bool = False,
    v1: bool = False,
    threads: float | None = None,
    cpu: bool = False,
    model: str | None = None,
) -> MriSynthsr:
    """
    Build parameters.

    Args:
        input_: Image(s) to super-resolve. Can be a path to an image or to a\
            folder.
        output: Output(s), i.e., synthetic 1mm MP-RAGE(s). Must be a folder if\
            input is a folder.
        ct: Use this flag for CT scans in Hounsfield scale, it clips intensities to\
            [0,80].
        disable_sharpening: Use this flag to disable unsharp masking.
        disable_flipping: Use this flag to disable flipping augmentation at test\
            time.
        lowfield: Use model for low-field scans (e.g., acquired with Hyperfine's\
            Swoop scanner).
        v1: Use version 1 model from July 2021.
        threads: Number of cores to be used. Default is 1.
        cpu: Enforce running with CPU rather than GPU.
        model: Use a different model file.

    Returns:
        Parameter dictionary.
    """
    params: MriSynthsr = {
        "@type": "freesurfer/mri_synthsr",
        "input": input_,
        "output": output,
        "ct": ct,
        "disable_sharpening": disable_sharpening,
        "disable_flipping": disable_flipping,
        "lowfield": lowfield,
        "v1": v1,
        "cpu": cpu,
    }
    if threads is not None:
        params["threads"] = threads
    if model is not None:
        params["model"] = model
    return params

def mri_synthsr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSynthsr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], str):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("ct", None) is not None:
        if not isinstance(params["ct"], bool):
            raise StyxValidationError(f'`ct` has the wrong type: Received `{type(params["ct"])}` expected `bool`')
    if params.get("disable_sharpening", None) is not None:
        if not isinstance(params["disable_sharpening"], bool):
            raise StyxValidationError(f'`disable_sharpening` has the wrong type: Received `{type(params["disable_sharpening"])}` expected `bool`')
    if params.get("disable_flipping", None) is not None:
        if not isinstance(params["disable_flipping"], bool):
            raise StyxValidationError(f'`disable_flipping` has the wrong type: Received `{type(params["disable_flipping"])}` expected `bool`')
    if params.get("lowfield", None) is not None:
        if not isinstance(params["lowfield"], bool):
            raise StyxValidationError(f'`lowfield` has the wrong type: Received `{type(params["lowfield"])}` expected `bool`')
    if params.get("v1", None) is not None:
        if not isinstance(params["v1"], bool):
            raise StyxValidationError(f'`v1` has the wrong type: Received `{type(params["v1"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("cpu", None) is not None:
        if not isinstance(params["cpu"], bool):
            raise StyxValidationError(f'`cpu` has the wrong type: Received `{type(params["cpu"])}` expected `bool`')
    if params.get("model", None) is not None:
        if not isinstance(params["model"], str):
            raise StyxValidationError(f'`model` has the wrong type: Received `{type(params["model"])}` expected `str`')

def mri_synthsr_cargs(params: MriSynthsr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_synthsr")
    cargs.append(params["input"])
    cargs.append(params["output"])
    if params.get("ct"):
        cargs.append("--ct")
    if params.get("disable_sharpening"):
        cargs.append("--disable_sharpening")
    if params.get("disable_flipping"):
        cargs.append("--disable_flipping")
    if params.get("lowfield"):
        cargs.append("--lowfield")
    if params.get("v1"):
        cargs.append("--v1")
    v_0 = params.get("threads")
    if v_0 is not None:
        cargs.append("--threads")
        cargs.append(str(v_0))
    if params.get("cpu"):
        cargs.append("--cpu")
    v_1 = params.get("model")
    if v_1 is not None:
        cargs.append("--model")
        cargs.append(v_1)
    return cargs

def mri_synthsr_outputs(params: MriSynthsr, execution: Execution) -> MriSynthsrOutputs:
    """Build the MriSynthsrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriSynthsrOutputs(
        root=root_v,
    )

def mri_synthsr_execute(params: MriSynthsr, runner: Runner | None = None) -> MriSynthsrOutputs:
    """
    mri_synthsr

    Implementation of SynthSR that generates a synthetic 1mm MP-RAGE from a scan of any contrast and resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_synthsr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SYNTHSR_METADATA)
    execution.params(params)
    args = mri_synthsr_cargs(params, execution)
    out = mri_synthsr_outputs(params, execution)
    execution.run(args)
    return out

def mri_synthsr(
    input_: str,
    output: str,
    ct: bool = False,
    disable_sharpening: bool = False,
    disable_flipping: bool = False,
    lowfield: bool = False,
    v1: bool = False,
    threads: float | None = None,
    cpu: bool = False,
    model: str | None = None,
    runner: Runner | None = None,
) -> MriSynthsrOutputs:
    """
    mri_synthsr

    Implementation of SynthSR that generates a synthetic 1mm MP-RAGE from a scan of any contrast and resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_: Image(s) to super-resolve. Can be a path to an image or to a\
            folder.
        output: Output(s), i.e., synthetic 1mm MP-RAGE(s). Must be a folder if\
            input is a folder.
        ct: Use this flag for CT scans in Hounsfield scale, it clips intensities to\
            [0,80].
        disable_sharpening: Use this flag to disable unsharp masking.
        disable_flipping: Use this flag to disable flipping augmentation at test\
            time.
        lowfield: Use model for low-field scans (e.g., acquired with Hyperfine's\
            Swoop scanner).
        v1: Use version 1 model from July 2021.
        threads: Number of cores to be used. Default is 1.
        cpu: Enforce running with CPU rather than GPU.
        model: Use a different model file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_synthsr_params(
        input_=input_,
        output=output,
        ct=ct,
        disable_sharpening=disable_sharpening,
        disable_flipping=disable_flipping,
        lowfield=lowfield,
        v1=v1,
        threads=threads,
        cpu=cpu,
        model=model,
    )
    return mri_synthsr_execute(params, runner)

__all__ = [
    "MriSynthsr",
    "MriSynthsrOutputs",
    "MRI_SYNTHSR_METADATA",
    "mri_synthsr_cargs",
    "mri_synthsr_outputs",
    "mri_synthsr_params",
    "mri_synthsr_execute",
    "mri_synthsr_validate",
    "mri_synthsr",
]