# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SEGMENTATION_STATS_METADATA = Metadata(
    id="mris_segmentation_stats",
    name="mris_segmentation_stats",
    package="freesurfer",
)

MrisSegmentationStats = typing.TypedDict(
    "MrisSegmentationStats",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_segmentation_stats"]],
        "overlay_name": str,
        "segmentation_label_name": str,
        "subjects": list[str],
        "roc_file": str,
    },
)

@dataclasses.dataclass
class MrisSegmentationStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    roc_output_file: OutputPathType
    """ROC curve file"""

def mris_segmentation_stats_params(
    overlay_name: str,
    segmentation_label_name: str,
    subjects: list[str],
    roc_file: str,
) -> MrisSegmentationStats:
    """
    Build parameters.

    Args:
        overlay_name: Overlay name for segmentation
        segmentation_label_name: Segmentation label name
        subjects: List of subjects to process
        roc_file: File for ROC curve output

    Returns:
        Parameter dictionary.
    """
    params: MrisSegmentationStats = {
        "@type": "freesurfer/mris_segmentation_stats",
        "overlay_name": overlay_name,
        "segmentation_label_name": segmentation_label_name,
        "subjects": subjects,
        "roc_file": roc_file,
    }
    return params

def mris_segmentation_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSegmentationStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("overlay_name", None) is None:
        raise StyxValidationError("`overlay_name` must not be None")
    if not isinstance(params["overlay_name"], str):
        raise StyxValidationError(f'`overlay_name` has the wrong type: Received `{type(params["overlay_name"])}` expected `str`')
    if params.get("segmentation_label_name", None) is None:
        raise StyxValidationError("`segmentation_label_name` must not be None")
    if not isinstance(params["segmentation_label_name"], str):
        raise StyxValidationError(f'`segmentation_label_name` has the wrong type: Received `{type(params["segmentation_label_name"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("roc_file", None) is None:
        raise StyxValidationError("`roc_file` must not be None")
    if not isinstance(params["roc_file"], str):
        raise StyxValidationError(f'`roc_file` has the wrong type: Received `{type(params["roc_file"])}` expected `str`')

def mris_segmentation_stats_cargs(params: MrisSegmentationStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_segmentation_stats")
    cargs.append(params["overlay_name"])
    cargs.append(params["segmentation_label_name"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["roc_file"])
    return cargs

def mris_segmentation_stats_outputs(params: MrisSegmentationStats, execution: Execution) -> MrisSegmentationStatsOutputs:
    """Build the MrisSegmentationStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    roc_output_file_v: OutputPathType = execution.output_file(params["roc_file"])
    return MrisSegmentationStatsOutputs(
        root=root_v,
        roc_output_file=roc_output_file_v,
    )

def mris_segmentation_stats_execute(params: MrisSegmentationStats, runner: Runner | None = None) -> MrisSegmentationStatsOutputs:
    """
    mris_segmentation_stats

    Tool for calculating segmentation statistics

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_segmentation_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SEGMENTATION_STATS_METADATA)
    execution.params(params)
    args = mris_segmentation_stats_cargs(params, execution)
    out = mris_segmentation_stats_outputs(params, execution)
    execution.run(args)
    return out

def mris_segmentation_stats(
    overlay_name: str,
    segmentation_label_name: str,
    subjects: list[str],
    roc_file: str,
    runner: Runner | None = None,
) -> MrisSegmentationStatsOutputs:
    """
    mris_segmentation_stats

    Tool for calculating segmentation statistics

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        overlay_name: Overlay name for segmentation
        segmentation_label_name: Segmentation label name
        subjects: List of subjects to process
        roc_file: File for ROC curve output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_segmentation_stats_params(
        overlay_name=overlay_name,
        segmentation_label_name=segmentation_label_name,
        subjects=subjects,
        roc_file=roc_file,
    )
    return mris_segmentation_stats_execute(params, runner)

__all__ = [
    "MrisSegmentationStats",
    "MrisSegmentationStatsOutputs",
    "MRIS_SEGMENTATION_STATS_METADATA",
    "mris_segmentation_stats_cargs",
    "mris_segmentation_stats_outputs",
    "mris_segmentation_stats_params",
    "mris_segmentation_stats_execute",
    "mris_segmentation_stats_validate",
    "mris_segmentation_stats",
]