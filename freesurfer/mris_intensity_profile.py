# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_INTENSITY_PROFILE_METADATA = Metadata(
    id="mris_intensity_profile",
    name="mris_intensity_profile",
    package="freesurfer",
)

MrisIntensityProfile = typing.TypedDict(
    "MrisIntensityProfile",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_intensity_profile"]],
        "subject_name": str,
        "hemi": str,
        "volume": InputPathType,
        "output_file": str,
        "write_surf": typing.NotRequired[str],
        "sdir": typing.NotRequired[str],
        "white": typing.NotRequired[str],
        "pial": typing.NotRequired[str],
        "normalize_flag": typing.NotRequired[bool],
        "mean": typing.NotRequired[str],
        "xform": typing.NotRequired[InputPathType],
        "src": typing.NotRequired[InputPathType],
        "dst": typing.NotRequired[InputPathType],
        "invert_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisIntensityProfileOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_curvature_file: OutputPathType
    """Curvature file with the intensity profile measurement."""
    output_mean_profile_integral: OutputPathType | None
    """File with the mean profile-integral."""

def mris_intensity_profile_params(
    subject_name: str,
    hemi: str,
    volume: InputPathType,
    output_file: str,
    write_surf: str | None = None,
    sdir: str | None = None,
    white: str | None = None,
    pial: str | None = None,
    normalize_flag: bool = False,
    mean: str | None = None,
    xform: InputPathType | None = None,
    src: InputPathType | None = None,
    dst: InputPathType | None = None,
    invert_flag: bool = False,
) -> MrisIntensityProfile:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject.
        hemi: Hemisphere (e.g. lh or rh).
        volume: Volume file to be processed.
        output_file: Output file where the measurement is saved.
        write_surf: Write the variational pial surface target locations.
        sdir: Specifies the SUBJECTS_DIR.
        white: Specifies the WHITE surface filename.
        pial: Specifies the PIAL surface filename.
        normalize_flag: Normalized sampling with respect to thickness.
        mean: Outputs the mean profile-integral to the specified file (output is in\
            curv format).
        xform: Specify the registration transformation that maps the T1 volume to\
            the input volume to be sampled.
        src: Source volume used when computing the registration transformation.
        dst: Destination volume used when computing the registration\
            transformation.
        invert_flag: Apply the registration transformation inversely.

    Returns:
        Parameter dictionary.
    """
    params: MrisIntensityProfile = {
        "@type": "freesurfer/mris_intensity_profile",
        "subject_name": subject_name,
        "hemi": hemi,
        "volume": volume,
        "output_file": output_file,
        "normalize_flag": normalize_flag,
        "invert_flag": invert_flag,
    }
    if write_surf is not None:
        params["write_surf"] = write_surf
    if sdir is not None:
        params["sdir"] = sdir
    if white is not None:
        params["white"] = white
    if pial is not None:
        params["pial"] = pial
    if mean is not None:
        params["mean"] = mean
    if xform is not None:
        params["xform"] = xform
    if src is not None:
        params["src"] = src
    if dst is not None:
        params["dst"] = dst
    return params

def mris_intensity_profile_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisIntensityProfile."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("write_surf", None) is not None:
        if not isinstance(params["write_surf"], str):
            raise StyxValidationError(f'`write_surf` has the wrong type: Received `{type(params["write_surf"])}` expected `str`')
    if params.get("sdir", None) is not None:
        if not isinstance(params["sdir"], str):
            raise StyxValidationError(f'`sdir` has the wrong type: Received `{type(params["sdir"])}` expected `str`')
    if params.get("white", None) is not None:
        if not isinstance(params["white"], str):
            raise StyxValidationError(f'`white` has the wrong type: Received `{type(params["white"])}` expected `str`')
    if params.get("pial", None) is not None:
        if not isinstance(params["pial"], str):
            raise StyxValidationError(f'`pial` has the wrong type: Received `{type(params["pial"])}` expected `str`')
    if params.get("normalize_flag", None) is not None:
        if not isinstance(params["normalize_flag"], bool):
            raise StyxValidationError(f'`normalize_flag` has the wrong type: Received `{type(params["normalize_flag"])}` expected `bool`')
    if params.get("mean", None) is not None:
        if not isinstance(params["mean"], str):
            raise StyxValidationError(f'`mean` has the wrong type: Received `{type(params["mean"])}` expected `str`')
    if params.get("xform", None) is not None:
        if not isinstance(params["xform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `InputPathType`')
    if params.get("src", None) is not None:
        if not isinstance(params["src"], (pathlib.Path, str)):
            raise StyxValidationError(f'`src` has the wrong type: Received `{type(params["src"])}` expected `InputPathType`')
    if params.get("dst", None) is not None:
        if not isinstance(params["dst"], (pathlib.Path, str)):
            raise StyxValidationError(f'`dst` has the wrong type: Received `{type(params["dst"])}` expected `InputPathType`')
    if params.get("invert_flag", None) is not None:
        if not isinstance(params["invert_flag"], bool):
            raise StyxValidationError(f'`invert_flag` has the wrong type: Received `{type(params["invert_flag"])}` expected `bool`')

def mris_intensity_profile_cargs(params: MrisIntensityProfile, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_intensity_profile")
    cargs.append(params["subject_name"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(params["output_file"])
    v_0 = params.get("write_surf")
    if v_0 is not None:
        cargs.append("-write_surf")
        cargs.append(v_0)
    v_1 = params.get("sdir")
    if v_1 is not None:
        cargs.append("-sdir")
        cargs.append(v_1)
    v_2 = params.get("white")
    if v_2 is not None:
        cargs.append("-white")
        cargs.append(v_2)
    v_3 = params.get("pial")
    if v_3 is not None:
        cargs.append("-pial")
        cargs.append(v_3)
    if params.get("normalize_flag"):
        cargs.append("-normalize")
    v_4 = params.get("mean")
    if v_4 is not None:
        cargs.append("-mean")
        cargs.append(v_4)
    v_5 = params.get("xform")
    if v_5 is not None:
        cargs.append("-xform")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("src")
    if v_6 is not None:
        cargs.append("-src")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("dst")
    if v_7 is not None:
        cargs.append("-dst")
        cargs.append(execution.input_file(v_7))
    if params.get("invert_flag"):
        cargs.append("-invert")
    return cargs

def mris_intensity_profile_outputs(params: MrisIntensityProfile, execution: Execution) -> MrisIntensityProfileOutputs:
    """Build the MrisIntensityProfileOutputs object for this tool."""
    output_mean_profile_integral_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_curvature_file_v: OutputPathType = execution.output_file(params["output_file"])
    __v0 = params.get("mean")
    if __v0 is not None:
        output_mean_profile_integral_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrisIntensityProfileOutputs(
        root=root_v,
        output_curvature_file=output_curvature_file_v,
        output_mean_profile_integral=output_mean_profile_integral_v,
    )

def mris_intensity_profile_execute(params: MrisIntensityProfile, runner: Runner | None = None) -> MrisIntensityProfileOutputs:
    """
    mris_intensity_profile

    This program computes the intensity profile of the cortical ribbon and writes the resulting measurement into a 'curvature' file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_intensity_profile_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_INTENSITY_PROFILE_METADATA)
    execution.params(params)
    args = mris_intensity_profile_cargs(params, execution)
    out = mris_intensity_profile_outputs(params, execution)
    execution.run(args)
    return out

def mris_intensity_profile(
    subject_name: str,
    hemi: str,
    volume: InputPathType,
    output_file: str,
    write_surf: str | None = None,
    sdir: str | None = None,
    white: str | None = None,
    pial: str | None = None,
    normalize_flag: bool = False,
    mean: str | None = None,
    xform: InputPathType | None = None,
    src: InputPathType | None = None,
    dst: InputPathType | None = None,
    invert_flag: bool = False,
    runner: Runner | None = None,
) -> MrisIntensityProfileOutputs:
    """
    mris_intensity_profile

    This program computes the intensity profile of the cortical ribbon and writes the resulting measurement into a 'curvature' file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject.
        hemi: Hemisphere (e.g. lh or rh).
        volume: Volume file to be processed.
        output_file: Output file where the measurement is saved.
        write_surf: Write the variational pial surface target locations.
        sdir: Specifies the SUBJECTS_DIR.
        white: Specifies the WHITE surface filename.
        pial: Specifies the PIAL surface filename.
        normalize_flag: Normalized sampling with respect to thickness.
        mean: Outputs the mean profile-integral to the specified file (output is in\
            curv format).
        xform: Specify the registration transformation that maps the T1 volume to\
            the input volume to be sampled.
        src: Source volume used when computing the registration transformation.
        dst: Destination volume used when computing the registration\
            transformation.
        invert_flag: Apply the registration transformation inversely.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_intensity_profile_params(
        subject_name=subject_name,
        hemi=hemi,
        volume=volume,
        output_file=output_file,
        write_surf=write_surf,
        sdir=sdir,
        white=white,
        pial=pial,
        normalize_flag=normalize_flag,
        mean=mean,
        xform=xform,
        src=src,
        dst=dst,
        invert_flag=invert_flag,
    )
    return mris_intensity_profile_execute(params, runner)

__all__ = [
    "MrisIntensityProfile",
    "MrisIntensityProfileOutputs",
    "MRIS_INTENSITY_PROFILE_METADATA",
    "mris_intensity_profile_cargs",
    "mris_intensity_profile_outputs",
    "mris_intensity_profile_params",
    "mris_intensity_profile_execute",
    "mris_intensity_profile_validate",
    "mris_intensity_profile",
]