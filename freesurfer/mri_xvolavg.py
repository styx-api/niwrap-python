# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_XVOLAVG_METADATA = Metadata(
    id="mri_xvolavg",
    name="mri_xvolavg",
    package="freesurfer",
)

MriXvolavg = typing.TypedDict(
    "MriXvolavg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_xvolavg"]],
        "input_volumes": list[InputPathType],
        "vol_type": str,
        "output_volume": str,
        "output_type": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriXvolavgOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    averaged_output: OutputPathType
    """The averaged output volume."""

def mri_xvolavg_params(
    input_volumes: list[InputPathType],
    vol_type: str,
    output_volume: str,
    output_type: str | None = None,
) -> MriXvolavg:
    """
    Build parameters.

    Args:
        input_volumes: Path(s) to input volume(s). This option can be repeated for\
            each input volume.
        vol_type: Format type of all input volumes.
        output_volume: Path to output volume.
        output_type: Format type of the output volume (default is that of input\
            volumes).

    Returns:
        Parameter dictionary.
    """
    params: MriXvolavg = {
        "@type": "freesurfer/mri_xvolavg",
        "input_volumes": input_volumes,
        "vol_type": vol_type,
        "output_volume": output_volume,
    }
    if output_type is not None:
        params["output_type"] = output_type
    return params

def mri_xvolavg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriXvolavg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("vol_type", None) is None:
        raise StyxValidationError("`vol_type` must not be None")
    if not isinstance(params["vol_type"], str):
        raise StyxValidationError(f'`vol_type` has the wrong type: Received `{type(params["vol_type"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("output_type", None) is not None:
        if not isinstance(params["output_type"], str):
            raise StyxValidationError(f'`output_type` has the wrong type: Received `{type(params["output_type"])}` expected `str`')

def mri_xvolavg_cargs(params: MriXvolavg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_xvolavg")
    cargs.append("--vol")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--vol_type")
    cargs.append(params["vol_type"])
    cargs.append("--out")
    cargs.append(params["output_volume"])
    v_0 = params.get("output_type")
    if v_0 is not None:
        cargs.append("--out_type")
        cargs.append(v_0)
    return cargs

def mri_xvolavg_outputs(params: MriXvolavg, execution: Execution) -> MriXvolavgOutputs:
    """Build the MriXvolavgOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    averaged_output_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriXvolavgOutputs(
        root=root_v,
        averaged_output=averaged_output_v,
    )

def mri_xvolavg_execute(params: MriXvolavg, runner: Runner | None = None) -> MriXvolavgOutputs:
    """
    mri_xvolavg

    Tool to average multiple volumes together (including 4D volumes).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_xvolavg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_XVOLAVG_METADATA)
    execution.params(params)
    args = mri_xvolavg_cargs(params, execution)
    out = mri_xvolavg_outputs(params, execution)
    execution.run(args)
    return out

def mri_xvolavg(
    input_volumes: list[InputPathType],
    vol_type: str,
    output_volume: str,
    output_type: str | None = None,
    runner: Runner | None = None,
) -> MriXvolavgOutputs:
    """
    mri_xvolavg

    Tool to average multiple volumes together (including 4D volumes).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Path(s) to input volume(s). This option can be repeated for\
            each input volume.
        vol_type: Format type of all input volumes.
        output_volume: Path to output volume.
        output_type: Format type of the output volume (default is that of input\
            volumes).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_xvolavg_params(
        input_volumes=input_volumes,
        vol_type=vol_type,
        output_volume=output_volume,
        output_type=output_type,
    )
    return mri_xvolavg_execute(params, runner)

__all__ = [
    "MriXvolavg",
    "MriXvolavgOutputs",
    "MRI_XVOLAVG_METADATA",
    "mri_xvolavg_cargs",
    "mri_xvolavg_outputs",
    "mri_xvolavg_params",
    "mri_xvolavg_execute",
    "mri_xvolavg_validate",
    "mri_xvolavg",
]