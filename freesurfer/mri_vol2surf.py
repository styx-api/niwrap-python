# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_VOL2SURF_METADATA = Metadata(
    id="mri_vol2surf",
    name="mri_vol2surf",
    package="freesurfer",
)

MriVol2surfHemisphere = typing.Literal["lh"] | typing.Literal["rh"]

MriVol2surf = typing.TypedDict(
    "MriVol2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_vol2surf"]],
        "input_volume": InputPathType,
        "registration_file": InputPathType,
        "output_path": str,
        "reference_volume": typing.NotRequired[str],
        "regheader_subject": typing.NotRequired[str],
        "mni152reg_flag": typing.NotRequired[bool],
        "target_subject": typing.NotRequired[str],
        "hemisphere": typing.NotRequired[MriVol2surfHemisphere],
        "surface": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriVol2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resampled_volume_output: OutputPathType
    """Output file containing the resampled volume onto the surface"""

def mri_vol2surf_params(
    input_volume: InputPathType,
    registration_file: InputPathType,
    output_path: str,
    reference_volume: str | None = None,
    regheader_subject: str | None = None,
    mni152reg_flag: bool = False,
    target_subject: str | None = None,
    hemisphere: MriVol2surfHemisphere | None = None,
    surface: str | None = None,
) -> MriVol2surf:
    """
    Build parameters.

    Args:
        input_volume: Path to input volume file
        registration_file: Registration file as computed by tkregister,\
            spmregister, bbregister, etc.
        output_path: Output path for the resampled volume
        reference_volume: Reference volume name, default is orig.mgz
        regheader_subject: Compute registration from header information, aligning\
            the current volume with the subject/mri/orig.mgz
        mni152reg_flag: Use MNI152 registration:\
            $FREESURFER_HOME/average/mni152.register.dat
        target_subject: Target subject for resampling, can be a subject name or\
            'ico' for icosahedron
        hemisphere: Hemisphere to process: lh = left hemisphere or rh = right\
            hemisphere
        surface: Target surface on which to resample, default is 'white'

    Returns:
        Parameter dictionary.
    """
    params: MriVol2surf = {
        "@type": "freesurfer/mri_vol2surf",
        "input_volume": input_volume,
        "registration_file": registration_file,
        "output_path": output_path,
        "mni152reg_flag": mni152reg_flag,
    }
    if reference_volume is not None:
        params["reference_volume"] = reference_volume
    if regheader_subject is not None:
        params["regheader_subject"] = regheader_subject
    if target_subject is not None:
        params["target_subject"] = target_subject
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    if surface is not None:
        params["surface"] = surface
    return params

def mri_vol2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriVol2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("registration_file", None) is None:
        raise StyxValidationError("`registration_file` must not be None")
    if not isinstance(params["registration_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("output_path", None) is None:
        raise StyxValidationError("`output_path` must not be None")
    if not isinstance(params["output_path"], str):
        raise StyxValidationError(f'`output_path` has the wrong type: Received `{type(params["output_path"])}` expected `str`')
    if params.get("reference_volume", None) is not None:
        if not isinstance(params["reference_volume"], str):
            raise StyxValidationError(f'`reference_volume` has the wrong type: Received `{type(params["reference_volume"])}` expected `str`')
    if params.get("regheader_subject", None) is not None:
        if not isinstance(params["regheader_subject"], str):
            raise StyxValidationError(f'`regheader_subject` has the wrong type: Received `{type(params["regheader_subject"])}` expected `str`')
    if params.get("mni152reg_flag", None) is not None:
        if not isinstance(params["mni152reg_flag"], bool):
            raise StyxValidationError(f'`mni152reg_flag` has the wrong type: Received `{type(params["mni152reg_flag"])}` expected `bool`')
    if params.get("target_subject", None) is not None:
        if not isinstance(params["target_subject"], str):
            raise StyxValidationError(f'`target_subject` has the wrong type: Received `{type(params["target_subject"])}` expected `str`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `MriVol2surfHemisphere`')
        if params["hemisphere"] not in ["lh", "rh"]:
            raise StyxValidationError("Parameter `hemisphere` must be one of [\"lh\", \"rh\"]")
    if params.get("surface", None) is not None:
        if not isinstance(params["surface"], str):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')

def mri_vol2surf_cargs(params: MriVol2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_vol2surf")
    cargs.append("--mov")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--reg")
    cargs.append(execution.input_file(params["registration_file"]))
    cargs.append("--o")
    cargs.append(params["output_path"])
    v_0 = params.get("reference_volume")
    if v_0 is not None:
        cargs.append("--ref")
        cargs.append(v_0)
    v_1 = params.get("regheader_subject")
    if v_1 is not None:
        cargs.append("--regheader")
        cargs.append(v_1)
    if params.get("mni152reg_flag"):
        cargs.append("--mni152reg")
    v_2 = params.get("target_subject")
    if v_2 is not None:
        cargs.append("--trgsubject")
        cargs.append(v_2)
    v_3 = params.get("hemisphere")
    if v_3 is not None:
        cargs.append("--hemi")
        cargs.append(str(v_3))
    v_4 = params.get("surface")
    if v_4 is not None:
        cargs.append("--surf")
        cargs.append(v_4)
    return cargs

def mri_vol2surf_outputs(params: MriVol2surf, execution: Execution) -> MriVol2surfOutputs:
    """Build the MriVol2surfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    resampled_volume_output_v: OutputPathType = execution.output_file(params["output_path"])
    return MriVol2surfOutputs(
        root=root_v,
        resampled_volume_output=resampled_volume_output_v,
    )

def mri_vol2surf_execute(params: MriVol2surf, runner: Runner | None = None) -> MriVol2surfOutputs:
    """
    mri_vol2surf

    This program resamples a volume onto a surface of a subject or the sphere. The output can be viewed on the surface (using tksurfer) or can be used for surface-based intersubject averaging.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_vol2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_VOL2SURF_METADATA)
    execution.params(params)
    args = mri_vol2surf_cargs(params, execution)
    out = mri_vol2surf_outputs(params, execution)
    execution.run(args)
    return out

def mri_vol2surf(
    input_volume: InputPathType,
    registration_file: InputPathType,
    output_path: str,
    reference_volume: str | None = None,
    regheader_subject: str | None = None,
    mni152reg_flag: bool = False,
    target_subject: str | None = None,
    hemisphere: MriVol2surfHemisphere | None = None,
    surface: str | None = None,
    runner: Runner | None = None,
) -> MriVol2surfOutputs:
    """
    mri_vol2surf

    This program resamples a volume onto a surface of a subject or the sphere. The output can be viewed on the surface (using tksurfer) or can be used for surface-based intersubject averaging.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Path to input volume file
        registration_file: Registration file as computed by tkregister,\
            spmregister, bbregister, etc.
        output_path: Output path for the resampled volume
        reference_volume: Reference volume name, default is orig.mgz
        regheader_subject: Compute registration from header information, aligning\
            the current volume with the subject/mri/orig.mgz
        mni152reg_flag: Use MNI152 registration:\
            $FREESURFER_HOME/average/mni152.register.dat
        target_subject: Target subject for resampling, can be a subject name or\
            'ico' for icosahedron
        hemisphere: Hemisphere to process: lh = left hemisphere or rh = right\
            hemisphere
        surface: Target surface on which to resample, default is 'white'
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_vol2surf_params(
        input_volume=input_volume,
        registration_file=registration_file,
        output_path=output_path,
        reference_volume=reference_volume,
        regheader_subject=regheader_subject,
        mni152reg_flag=mni152reg_flag,
        target_subject=target_subject,
        hemisphere=hemisphere,
        surface=surface,
    )
    return mri_vol2surf_execute(params, runner)

__all__ = [
    "MriVol2surf",
    "MriVol2surfOutputs",
    "MRI_VOL2SURF_METADATA",
    "mri_vol2surf_cargs",
    "mri_vol2surf_outputs",
    "mri_vol2surf_params",
    "mri_vol2surf_execute",
    "mri_vol2surf_validate",
    "mri_vol2surf",
]