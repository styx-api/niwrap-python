# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_TIME_METADATA = Metadata(
    id="fs_time",
    name="fs_time",
    package="freesurfer",
)

FsTime = typing.TypedDict(
    "FsTime",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_time"]],
        "output_file": typing.NotRequired[str],
        "key": typing.NotRequired[str],
        "load_avg": typing.NotRequired[bool],
        "command": str,
        "args": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class FsTimeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resource_output: OutputPathType | None
    """File containing resource usage information."""

def fs_time_params(
    command: str,
    output_file: str | None = None,
    key: str | None = None,
    load_avg: bool = False,
    args: list[str] | None = None,
) -> FsTime:
    """
    Build parameters.

    Args:
        command: The command to be timed with fs_time.
        output_file: Save resource info into output file.
        key: Specify a key for the resource information.
        load_avg: Report on load averages as from uptime.
        args: Arguments for the command.

    Returns:
        Parameter dictionary.
    """
    params: FsTime = {
        "@type": "freesurfer/fs_time",
        "command": command,
        "load_avg": load_avg,
    }
    if output_file is not None:
        params["output_file"] = output_file
    if key is not None:
        params["key"] = key
    if args is not None:
        params["args"] = args
    return params

def fs_time_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsTime."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("key", None) is not None:
        if not isinstance(params["key"], str):
            raise StyxValidationError(f'`key` has the wrong type: Received `{type(params["key"])}` expected `str`')
    if params.get("load_avg", None) is not None:
        if not isinstance(params["load_avg"], bool):
            raise StyxValidationError(f'`load_avg` has the wrong type: Received `{type(params["load_avg"])}` expected `bool`')
    if params.get("command", None) is None:
        raise StyxValidationError("`command` must not be None")
    if not isinstance(params["command"], str):
        raise StyxValidationError(f'`command` has the wrong type: Received `{type(params["command"])}` expected `str`')
    if params.get("args", None) is not None:
        if not isinstance(params["args"], list):
            raise StyxValidationError(f'`args` has the wrong type: Received `{type(params["args"])}` expected `list[str]`')
        for e in params["args"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`args` has the wrong type: Received `{type(e)}` expected `list[str]`')

def fs_time_cargs(params: FsTime, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_time")
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("key")
    if v_1 is not None:
        cargs.append("-k")
        cargs.append(v_1)
    if params.get("load_avg"):
        cargs.append("-l")
    cargs.append(params["command"])
    v_2 = params.get("args")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append(item0)
    return cargs

def fs_time_outputs(params: FsTime, execution: Execution) -> FsTimeOutputs:
    """Build the FsTimeOutputs object for this tool."""
    resource_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        resource_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return FsTimeOutputs(
        root=root_v,
        resource_output=resource_output_v,
    )

def fs_time_execute(params: FsTime, runner: Runner | None = None) -> FsTimeOutputs:
    """
    fs_time

    A frontend for the unix /usr/bin/time program to track resource usage by a process.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_time_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_TIME_METADATA)
    execution.params(params)
    args = fs_time_cargs(params, execution)
    out = fs_time_outputs(params, execution)
    execution.run(args)
    return out

def fs_time(
    command: str,
    output_file: str | None = None,
    key: str | None = None,
    load_avg: bool = False,
    args: list[str] | None = None,
    runner: Runner | None = None,
) -> FsTimeOutputs:
    """
    fs_time

    A frontend for the unix /usr/bin/time program to track resource usage by a process.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        command: The command to be timed with fs_time.
        output_file: Save resource info into output file.
        key: Specify a key for the resource information.
        load_avg: Report on load averages as from uptime.
        args: Arguments for the command.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_time_params(
        command=command,
        output_file=output_file,
        key=key,
        load_avg=load_avg,
        args=args,
    )
    return fs_time_execute(params, runner)

__all__ = [
    "FsTime",
    "FsTimeOutputs",
    "FS_TIME_METADATA",
    "fs_time_cargs",
    "fs_time_outputs",
    "fs_time_params",
    "fs_time_execute",
    "fs_time_validate",
    "fs_time",
]