# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SAMPLE_LABEL_METADATA = Metadata(
    id="mris_sample_label",
    name="mris_sample_label",
    package="freesurfer",
)

MrisSampleLabel = typing.TypedDict(
    "MrisSampleLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_sample_label"]],
        "input_label_file": InputPathType,
        "input_surface_file": InputPathType,
        "output_label_file": str,
    },
)

@dataclasses.dataclass
class MrisSampleLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_file: OutputPathType
    """The resulting output label file"""

def mris_sample_label_params(
    input_label_file: InputPathType,
    input_surface_file: InputPathType,
    output_label_file: str,
) -> MrisSampleLabel:
    """
    Build parameters.

    Args:
        input_label_file: Input label file
        input_surface_file: Input surface file
        output_label_file: Output label file

    Returns:
        Parameter dictionary.
    """
    params: MrisSampleLabel = {
        "@type": "freesurfer/mris_sample_label",
        "input_label_file": input_label_file,
        "input_surface_file": input_surface_file,
        "output_label_file": output_label_file,
    }
    return params

def mris_sample_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSampleLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_label_file", None) is None:
        raise StyxValidationError("`input_label_file` must not be None")
    if not isinstance(params["input_label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_label_file` has the wrong type: Received `{type(params["input_label_file"])}` expected `InputPathType`')
    if params.get("input_surface_file", None) is None:
        raise StyxValidationError("`input_surface_file` must not be None")
    if not isinstance(params["input_surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface_file` has the wrong type: Received `{type(params["input_surface_file"])}` expected `InputPathType`')
    if params.get("output_label_file", None) is None:
        raise StyxValidationError("`output_label_file` must not be None")
    if not isinstance(params["output_label_file"], str):
        raise StyxValidationError(f'`output_label_file` has the wrong type: Received `{type(params["output_label_file"])}` expected `str`')

def mris_sample_label_cargs(params: MrisSampleLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_sample_label")
    cargs.append(execution.input_file(params["input_label_file"]))
    cargs.append(execution.input_file(params["input_surface_file"]))
    cargs.append(params["output_label_file"])
    return cargs

def mris_sample_label_outputs(params: MrisSampleLabel, execution: Execution) -> MrisSampleLabelOutputs:
    """Build the MrisSampleLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_file_v: OutputPathType = execution.output_file(params["output_label_file"])
    return MrisSampleLabelOutputs(
        root=root_v,
        output_label_file=output_label_file_v,
    )

def mris_sample_label_execute(params: MrisSampleLabel, runner: Runner | None = None) -> MrisSampleLabelOutputs:
    """
    mris_sample_label

    This tool samples a label onto a surface model.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_sample_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SAMPLE_LABEL_METADATA)
    execution.params(params)
    args = mris_sample_label_cargs(params, execution)
    out = mris_sample_label_outputs(params, execution)
    execution.run(args)
    return out

def mris_sample_label(
    input_label_file: InputPathType,
    input_surface_file: InputPathType,
    output_label_file: str,
    runner: Runner | None = None,
) -> MrisSampleLabelOutputs:
    """
    mris_sample_label

    This tool samples a label onto a surface model.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_label_file: Input label file
        input_surface_file: Input surface file
        output_label_file: Output label file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_sample_label_params(
        input_label_file=input_label_file,
        input_surface_file=input_surface_file,
        output_label_file=output_label_file,
    )
    return mris_sample_label_execute(params, runner)

__all__ = [
    "MrisSampleLabel",
    "MrisSampleLabelOutputs",
    "MRIS_SAMPLE_LABEL_METADATA",
    "mris_sample_label_cargs",
    "mris_sample_label_outputs",
    "mris_sample_label_params",
    "mris_sample_label_execute",
    "mris_sample_label_validate",
    "mris_sample_label",
]