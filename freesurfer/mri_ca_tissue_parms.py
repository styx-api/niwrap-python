# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CA_TISSUE_PARMS_METADATA = Metadata(
    id="mri_ca_tissue_parms",
    name="mri_ca_tissue_parms",
    package="freesurfer",
)

MriCaTissueParms = typing.TypedDict(
    "MriCaTissueParms",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_ca_tissue_parms"]],
        "subjects": list[str],
        "output_file": str,
        "spacing_flag": typing.NotRequired[bool],
        "gradient_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCaTissueParmsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """File containing the computed tissue parameters."""

def mri_ca_tissue_parms_params(
    subjects: list[str],
    output_file: str,
    spacing_flag: bool = False,
    gradient_flag: bool = False,
) -> MriCaTissueParms:
    """
    Build parameters.

    Args:
        subjects: List of subjects for processing.
        output_file: Output file for storing results.
        spacing_flag: Specify spacing of classifiers in canonical space.
        gradient_flag: Use intensity gradient as input to classifier.

    Returns:
        Parameter dictionary.
    """
    params: MriCaTissueParms = {
        "@type": "freesurfer/mri_ca_tissue_parms",
        "subjects": subjects,
        "output_file": output_file,
        "spacing_flag": spacing_flag,
        "gradient_flag": gradient_flag,
    }
    return params

def mri_ca_tissue_parms_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCaTissueParms."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("spacing_flag", None) is not None:
        if not isinstance(params["spacing_flag"], bool):
            raise StyxValidationError(f'`spacing_flag` has the wrong type: Received `{type(params["spacing_flag"])}` expected `bool`')
    if params.get("gradient_flag", None) is not None:
        if not isinstance(params["gradient_flag"], bool):
            raise StyxValidationError(f'`gradient_flag` has the wrong type: Received `{type(params["gradient_flag"])}` expected `bool`')

def mri_ca_tissue_parms_cargs(params: MriCaTissueParms, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_ca_tissue_parms")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_file"])
    if params.get("spacing_flag"):
        cargs.append("-spacing")
    if params.get("gradient_flag"):
        cargs.append("-gradient")
    return cargs

def mri_ca_tissue_parms_outputs(params: MriCaTissueParms, execution: Execution) -> MriCaTissueParmsOutputs:
    """Build the MriCaTissueParmsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MriCaTissueParmsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_ca_tissue_parms_execute(params: MriCaTissueParms, runner: Runner | None = None) -> MriCaTissueParmsOutputs:
    """
    mri_ca_tissue_parms

    Tool for computing tissue parameters in canonical space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_ca_tissue_parms_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CA_TISSUE_PARMS_METADATA)
    execution.params(params)
    args = mri_ca_tissue_parms_cargs(params, execution)
    out = mri_ca_tissue_parms_outputs(params, execution)
    execution.run(args)
    return out

def mri_ca_tissue_parms(
    subjects: list[str],
    output_file: str,
    spacing_flag: bool = False,
    gradient_flag: bool = False,
    runner: Runner | None = None,
) -> MriCaTissueParmsOutputs:
    """
    mri_ca_tissue_parms

    Tool for computing tissue parameters in canonical space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subjects for processing.
        output_file: Output file for storing results.
        spacing_flag: Specify spacing of classifiers in canonical space.
        gradient_flag: Use intensity gradient as input to classifier.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_ca_tissue_parms_params(
        subjects=subjects,
        output_file=output_file,
        spacing_flag=spacing_flag,
        gradient_flag=gradient_flag,
    )
    return mri_ca_tissue_parms_execute(params, runner)

__all__ = [
    "MriCaTissueParms",
    "MriCaTissueParmsOutputs",
    "MRI_CA_TISSUE_PARMS_METADATA",
    "mri_ca_tissue_parms_cargs",
    "mri_ca_tissue_parms_outputs",
    "mri_ca_tissue_parms_params",
    "mri_ca_tissue_parms_execute",
    "mri_ca_tissue_parms_validate",
    "mri_ca_tissue_parms",
]