# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MARK_TEMPORAL_LOBE_METADATA = Metadata(
    id="mri_mark_temporal_lobe",
    name="mri_mark_temporal_lobe",
    package="freesurfer",
)

class MriMarkTemporalLobeUseGradient(typing.TypedDict):
    use_gradient: typing.NotRequired[bool]
    """Flag to use intensity gradient as input to classifier"""
    subjects: list[InputPathType]
    """Subject MRI images"""
    output_file: str
    """Output file for marked temporal lobes"""

MriMarkTemporalLobe = typing.TypedDict(
    "MriMarkTemporalLobe",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mark_temporal_lobe"]],
        "spacing": typing.NotRequired[str],
        "use_gradient": MriMarkTemporalLobeUseGradient,
    },
)

@dataclasses.dataclass
class MriMarkTemporalLobeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Marked temporal lobe output file"""

def mri_mark_temporal_lobe_params(
    use_gradient: MriMarkTemporalLobeUseGradient,
    spacing: str | None = None,
) -> MriMarkTemporalLobe:
    """
    Build parameters.

    Args:
        use_gradient:
        spacing: The spacing of classifiers in canonical space

    Returns:
        Parameter dictionary.
    """
    params: MriMarkTemporalLobe = {
        "@type": "freesurfer/mri_mark_temporal_lobe",
        "use_gradient": use_gradient,
    }
    if spacing is not None:
        params["spacing"] = spacing
    return params

def mri_mark_temporal_lobe_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMarkTemporalLobe."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spacing", None) is not None:
        if not isinstance(params["spacing"], str):
            raise StyxValidationError(f'`spacing` has the wrong type: Received `{type(params["spacing"])}` expected `str`')
    if params.get("use_gradient", None) is None:
        raise StyxValidationError("`use_gradient` must not be None")
    if not isinstance(params["use_gradient"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["use_gradient"])}\'')
    if params["use_gradient"].get("use_gradient", None) is not None:
        if not isinstance(params["use_gradient"]["use_gradient"], bool):
            raise StyxValidationError(f'`use_gradient` has the wrong type: Received `{type(params["use_gradient"]["use_gradient"])}` expected `bool`')
    if params["use_gradient"].get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["use_gradient"]["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["use_gradient"]["subjects"])}` expected `list[InputPathType]`')
    for e in params["use_gradient"]["subjects"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params["use_gradient"].get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["use_gradient"]["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["use_gradient"]["output_file"])}` expected `str`')

def mri_mark_temporal_lobe_cargs(params: MriMarkTemporalLobe, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mark_temporal_lobe")
    v_0 = params.get("spacing")
    if v_0 is not None:
        cargs.append("-spacing")
        cargs.append(v_0)
    cargs.append("".join([("-gradient" if params["use_gradient"].get("use_gradient") else ""), "".join([execution.input_file(item0) for item0 in params["use_gradient"]["subjects"]]), params["use_gradient"]["output_file"]]))
    return cargs

def mri_mark_temporal_lobe_outputs(params: MriMarkTemporalLobe, execution: Execution) -> MriMarkTemporalLobeOutputs:
    """Build the MriMarkTemporalLobeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["use_gradient"]["output_file"])
    return MriMarkTemporalLobeOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_mark_temporal_lobe_execute(params: MriMarkTemporalLobe, runner: Runner | None = None) -> MriMarkTemporalLobeOutputs:
    """
    mri_mark_temporal_lobe

    A tool for marking the temporal lobe in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mark_temporal_lobe_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MARK_TEMPORAL_LOBE_METADATA)
    execution.params(params)
    args = mri_mark_temporal_lobe_cargs(params, execution)
    out = mri_mark_temporal_lobe_outputs(params, execution)
    execution.run(args)
    return out

def mri_mark_temporal_lobe(
    use_gradient: MriMarkTemporalLobeUseGradient,
    spacing: str | None = None,
    runner: Runner | None = None,
) -> MriMarkTemporalLobeOutputs:
    """
    mri_mark_temporal_lobe

    A tool for marking the temporal lobe in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        use_gradient:
        spacing: The spacing of classifiers in canonical space
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mark_temporal_lobe_params(
        use_gradient=use_gradient,
        spacing=spacing,
    )
    return mri_mark_temporal_lobe_execute(params, runner)

__all__ = [
    "MriMarkTemporalLobe",
    "MriMarkTemporalLobeOutputs",
    "MRI_MARK_TEMPORAL_LOBE_METADATA",
    "mri_mark_temporal_lobe_cargs",
    "mri_mark_temporal_lobe_outputs",
    "mri_mark_temporal_lobe_params",
    "mri_mark_temporal_lobe_execute",
    "mri_mark_temporal_lobe_validate",
    "mri_mark_temporal_lobe",
]