# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_ELDERLY_SUBJECT_METADATA = Metadata(
    id="label_elderly_subject",
    name="label_elderly_subject",
    package="freesurfer",
)

LabelElderlySubject = typing.TypedDict(
    "LabelElderlySubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label_elderly_subject"]],
        "norm_volume": InputPathType,
        "transform_lta": InputPathType,
        "classifier_array": typing.NotRequired[InputPathType],
        "aseg_volume": InputPathType,
    },
)

@dataclasses.dataclass
class LabelElderlySubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    labeled_volume: OutputPathType
    """Labeled output volume"""

def label_elderly_subject_params(
    norm_volume: InputPathType,
    transform_lta: InputPathType,
    aseg_volume: InputPathType,
    classifier_array: InputPathType | None = None,
) -> LabelElderlySubject:
    """
    Build parameters.

    Args:
        norm_volume: Normalized input volume (e.g. norm.mgz)
        transform_lta: Transformation file (e.g. talairach.lta)
        aseg_volume: Asegmentation volume file (e.g. aseg.mgz)
        classifier_array: Classifier array file (e.g. mixed.gca)

    Returns:
        Parameter dictionary.
    """
    params: LabelElderlySubject = {
        "@type": "freesurfer/label_elderly_subject",
        "norm_volume": norm_volume,
        "transform_lta": transform_lta,
        "aseg_volume": aseg_volume,
    }
    if classifier_array is not None:
        params["classifier_array"] = classifier_array
    return params

def label_elderly_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelElderlySubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("norm_volume", None) is None:
        raise StyxValidationError("`norm_volume` must not be None")
    if not isinstance(params["norm_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`norm_volume` has the wrong type: Received `{type(params["norm_volume"])}` expected `InputPathType`')
    if params.get("transform_lta", None) is None:
        raise StyxValidationError("`transform_lta` must not be None")
    if not isinstance(params["transform_lta"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_lta` has the wrong type: Received `{type(params["transform_lta"])}` expected `InputPathType`')
    if params.get("classifier_array", None) is not None:
        if not isinstance(params["classifier_array"], (pathlib.Path, str)):
            raise StyxValidationError(f'`classifier_array` has the wrong type: Received `{type(params["classifier_array"])}` expected `InputPathType`')
    if params.get("aseg_volume", None) is None:
        raise StyxValidationError("`aseg_volume` must not be None")
    if not isinstance(params["aseg_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`aseg_volume` has the wrong type: Received `{type(params["aseg_volume"])}` expected `InputPathType`')

def label_elderly_subject_cargs(params: LabelElderlySubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label_elderly_subject")
    cargs.append(execution.input_file(params["norm_volume"]))
    cargs.append(execution.input_file(params["transform_lta"]))
    v_0 = params.get("classifier_array")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    cargs.append(execution.input_file(params["aseg_volume"]))
    return cargs

def label_elderly_subject_outputs(params: LabelElderlySubject, execution: Execution) -> LabelElderlySubjectOutputs:
    """Build the LabelElderlySubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    labeled_volume_v: OutputPathType = execution.output_file(f'{params["aseg_volume"]}_labeled.mgz')
    return LabelElderlySubjectOutputs(
        root=root_v,
        labeled_volume=labeled_volume_v,
    )

def label_elderly_subject_execute(params: LabelElderlySubject, runner: Runner | None = None) -> LabelElderlySubjectOutputs:
    """
    label_elderly_subject

    Tool for labeling brain structures in MRI images of elderly subjects using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_elderly_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_ELDERLY_SUBJECT_METADATA)
    execution.params(params)
    args = label_elderly_subject_cargs(params, execution)
    out = label_elderly_subject_outputs(params, execution)
    execution.run(args)
    return out

def label_elderly_subject(
    norm_volume: InputPathType,
    transform_lta: InputPathType,
    aseg_volume: InputPathType,
    classifier_array: InputPathType | None = None,
    runner: Runner | None = None,
) -> LabelElderlySubjectOutputs:
    """
    label_elderly_subject

    Tool for labeling brain structures in MRI images of elderly subjects using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        norm_volume: Normalized input volume (e.g. norm.mgz)
        transform_lta: Transformation file (e.g. talairach.lta)
        aseg_volume: Asegmentation volume file (e.g. aseg.mgz)
        classifier_array: Classifier array file (e.g. mixed.gca)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_elderly_subject_params(
        norm_volume=norm_volume,
        transform_lta=transform_lta,
        aseg_volume=aseg_volume,
        classifier_array=classifier_array,
    )
    return label_elderly_subject_execute(params, runner)

__all__ = [
    "LabelElderlySubject",
    "LabelElderlySubjectOutputs",
    "LABEL_ELDERLY_SUBJECT_METADATA",
    "label_elderly_subject_cargs",
    "label_elderly_subject_outputs",
    "label_elderly_subject_params",
    "label_elderly_subject_execute",
    "label_elderly_subject_validate",
    "label_elderly_subject",
]