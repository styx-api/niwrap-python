# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HIAM_MAKE_TEMPLATE_METADATA = Metadata(
    id="hiam_make_template",
    name="hiam_make_template",
    package="freesurfer",
)

HiamMakeTemplate = typing.TypedDict(
    "HiamMakeTemplate",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/hiam_make_template"]],
        "hemi": str,
        "surface_name": str,
        "subjects": list[str],
        "output_name": str,
    },
)

@dataclasses.dataclass
class HiamMakeTemplateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def hiam_make_template_params(
    hemi: str,
    surface_name: str,
    subjects: list[str],
    output_name: str,
) -> HiamMakeTemplate:
    """
    Build parameters.

    Args:
        hemi: Hemisphere to be processed (e.g. lh or rh)
        surface_name: Name of the surface
        subjects: List of subject identifiers
        output_name: Name of the output template

    Returns:
        Parameter dictionary.
    """
    params: HiamMakeTemplate = {
        "@type": "freesurfer/hiam_make_template",
        "hemi": hemi,
        "surface_name": surface_name,
        "subjects": subjects,
        "output_name": output_name,
    }
    return params

def hiam_make_template_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HiamMakeTemplate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface_name", None) is None:
        raise StyxValidationError("`surface_name` must not be None")
    if not isinstance(params["surface_name"], str):
        raise StyxValidationError(f'`surface_name` has the wrong type: Received `{type(params["surface_name"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')

def hiam_make_template_cargs(params: HiamMakeTemplate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("hiam_make_template")
    cargs.append(params["hemi"])
    cargs.append(params["surface_name"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_name"])
    return cargs

def hiam_make_template_outputs(params: HiamMakeTemplate, execution: Execution) -> HiamMakeTemplateOutputs:
    """Build the HiamMakeTemplateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return HiamMakeTemplateOutputs(
        root=root_v,
    )

def hiam_make_template_execute(params: HiamMakeTemplate, runner: Runner | None = None) -> HiamMakeTemplateOutputs:
    """
    hiam_make_template

    This program adds a template into an average surface using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    hiam_make_template_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HIAM_MAKE_TEMPLATE_METADATA)
    execution.params(params)
    args = hiam_make_template_cargs(params, execution)
    out = hiam_make_template_outputs(params, execution)
    execution.run(args)
    return out

def hiam_make_template(
    hemi: str,
    surface_name: str,
    subjects: list[str],
    output_name: str,
    runner: Runner | None = None,
) -> HiamMakeTemplateOutputs:
    """
    hiam_make_template

    This program adds a template into an average surface using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hemi: Hemisphere to be processed (e.g. lh or rh)
        surface_name: Name of the surface
        subjects: List of subject identifiers
        output_name: Name of the output template
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = hiam_make_template_params(
        hemi=hemi,
        surface_name=surface_name,
        subjects=subjects,
        output_name=output_name,
    )
    return hiam_make_template_execute(params, runner)

__all__ = [
    "HiamMakeTemplate",
    "HiamMakeTemplateOutputs",
    "HIAM_MAKE_TEMPLATE_METADATA",
    "hiam_make_template_cargs",
    "hiam_make_template_outputs",
    "hiam_make_template_params",
    "hiam_make_template_execute",
    "hiam_make_template_validate",
    "hiam_make_template",
]