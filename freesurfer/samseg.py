# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SAMSEG_METADATA = Metadata(
    id="samseg",
    name="samseg",
    package="freesurfer",
)

Samseg = typing.TypedDict(
    "Samseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/samseg"]],
        "input_files": list[InputPathType],
        "t1w_files": typing.NotRequired[list[InputPathType]],
        "t2w_files": typing.NotRequired[list[InputPathType]],
        "flair_files": typing.NotRequired[list[InputPathType]],
        "other_modality_files": typing.NotRequired[list[InputPathType]],
        "output_directory": str,
        "options_file": typing.NotRequired[InputPathType],
        "dissection_photo_mode": typing.NotRequired[str],
        "save_history": typing.NotRequired[bool],
        "subject": typing.NotRequired[str],
        "save_posteriors": typing.NotRequired[bool],
        "save_probabilities": typing.NotRequired[bool],
        "no_save_warp": typing.NotRequired[bool],
        "mrf": typing.NotRequired[bool],
        "no_mrf": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "atlas_directory": typing.NotRequired[str],
        "gmm_file": typing.NotRequired[InputPathType],
        "no_block_coordinate_descent": typing.NotRequired[bool],
        "logdomain_costandgradient_calculator": typing.NotRequired[bool],
        "no_logdomain_costandgradient_calculator": typing.NotRequired[bool],
        "recon": typing.NotRequired[bool],
        "fill": typing.NotRequired[bool],
        "normalization2": typing.NotRequired[bool],
        "use_t2w": typing.NotRequired[bool],
        "use_flair": typing.NotRequired[bool],
        "hires": typing.NotRequired[bool],
        "subjects_directory": typing.NotRequired[str],
        "pallidum_separate": typing.NotRequired[bool],
        "stiffness": typing.NotRequired[float],
        "lesion": typing.NotRequired[bool],
        "lesion_mask_pattern": typing.NotRequired[list[float]],
        "bias_field_smoothing_kernel": typing.NotRequired[float],
        "registration_file": typing.NotRequired[InputPathType],
        "regmat_file": typing.NotRequired[InputPathType],
        "init_lta": typing.NotRequired[InputPathType],
        "reg_only": typing.NotRequired[bool],
        "ssdd_directory": typing.NotRequired[str],
        "save_mesh": typing.NotRequired[bool],
        "max_iters": typing.NotRequired[float],
        "dice_file": typing.NotRequired[InputPathType],
        "ignore_unknown": typing.NotRequired[bool],
        "smooth_wm_cortex": typing.NotRequired[float],
        "profile_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SamsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmentation_output: OutputPathType
    """Segmentation output file."""
    posteriors_output: OutputPathType
    """Posterior probabilities output file."""
    probabilities_output: OutputPathType
    """Probabilities output file, including post, prior, and likelihood."""
    mesh_output: OutputPathType
    """Mesh output file (if save-mesh is enabled)."""

def samseg_params(
    input_files: list[InputPathType],
    output_directory: str,
    t1w_files: list[InputPathType] | None = None,
    t2w_files: list[InputPathType] | None = None,
    flair_files: list[InputPathType] | None = None,
    other_modality_files: list[InputPathType] | None = None,
    options_file: InputPathType | None = None,
    dissection_photo_mode: str | None = None,
    save_history: bool = False,
    subject: str | None = None,
    save_posteriors: bool = False,
    save_probabilities: bool = False,
    no_save_warp: bool = False,
    mrf: bool = False,
    no_mrf: bool = False,
    threads: float | None = None,
    atlas_directory: str | None = None,
    gmm_file: InputPathType | None = None,
    no_block_coordinate_descent: bool = False,
    logdomain_costandgradient_calculator: bool = False,
    no_logdomain_costandgradient_calculator: bool = False,
    recon: bool = False,
    fill: bool = False,
    normalization2: bool = False,
    use_t2w: bool = False,
    use_flair: bool = False,
    hires: bool = False,
    subjects_directory: str | None = None,
    pallidum_separate: bool = False,
    stiffness: float | None = None,
    lesion: bool = False,
    lesion_mask_pattern: list[float] | None = None,
    bias_field_smoothing_kernel: float | None = None,
    registration_file: InputPathType | None = None,
    regmat_file: InputPathType | None = None,
    init_lta: InputPathType | None = None,
    reg_only: bool = False,
    ssdd_directory: str | None = None,
    save_mesh: bool = False,
    max_iters: float | None = None,
    dice_file: InputPathType | None = None,
    ignore_unknown: bool = False,
    smooth_wm_cortex: float | None = None,
    profile_file: InputPathType | None = None,
) -> Samseg:
    """
    Build parameters.

    Args:
        input_files: Input image files, must be in registration with each other.
        output_directory: Output directory where results will be saved.
        t1w_files: T1-weighted input file, causes input to be conformed unless\
            --hires is used.
        t2w_files: T2-weighted input file.
        flair_files: FLAIR-weighted input file.
        other_modality_files: Specific mode input files, each associated with a\
            custom modality name.
        options_file: JSON file for specifying advanced options.
        dissection_photo_mode: Mode for processing 3D reconstructed dissection\
            photos, specifying left/right/both.
        save_history: Turns on saving of processing history.
        subject: Subject identifier, sets output directory to subject/mri/samseg.\
            The first input must be the conformed T1 weighted input.
        save_posteriors: Save posterior probabilities.
        save_probabilities: Save posterior, prior, and likelihood as 3-frame volume\
            for each tissue type.
        no_save_warp: Do not save m3z-style warp.
        mrf: Perform Markov Random Field processing.
        no_mrf: Do not perform Markov Random Field processing.
        threads: Set the number of CPUs to use.
        atlas_directory: Path to the SAMSEG atlas directory.
        gmm_file: Gaussian Mixture Model file.
        no_block_coordinate_descent: Do not use block coordinate descent.
        logdomain_costandgradient_calculator: Use log-domain cost and gradient\
            calculator.
        no_logdomain_costandgradient_calculator: Do not use log-domain cost and\
            gradient calculator.
        recon: Run recon-all.
        fill: Use samseg to create filled.mgz instead of recon-all.
        normalization2: Use samseg to create brain.mgz instead of recon-all (with\
            --recon).
        use_t2w: Use the T2-weighted input when running recon-all.
        use_flair: Use the FLAIR-weighted input when running recon-all.
        hires: Use -hires when running recon-all.
        subjects_directory: Path to the SUBJECTS_DIR.
        pallidum_separate: Move pallidum outside of global white matter class, used\
            for analyzing T2 or FLAIR.
        stiffness: Set mesh stiffness.
        lesion: Turn on lesion segmentation.
        lesion_mask_pattern: Pattern needed when using T2 for lesion mask, e.g., 0\
            1.
        bias_field_smoothing_kernel: Width of bias field smoothness.
        registration_file: Registration file (LTA or MAT) as output by samseg.
        regmat_file: Same as --reg.
        init_lta: Initial registration LTA file.
        reg_only: Only perform registration.
        ssdd_directory: Path to SAMSEG_Data_DIR where the atlas is located.
        save_mesh: Save the mesh, useful for longitudinal analysis.
        max_iters: Maximum number of iterations.
        dice_file: DICE coefficient file for segmentation.
        ignore_unknown: Ignore unknown flags.
        smooth_wm_cortex: Smooth white matter and cortex priors using specified\
            sigma value.
        profile_file: Run using the cProfile Python profiler, with the specified\
            profile file.

    Returns:
        Parameter dictionary.
    """
    params: Samseg = {
        "@type": "freesurfer/samseg",
        "input_files": input_files,
        "output_directory": output_directory,
        "save_history": save_history,
        "save_posteriors": save_posteriors,
        "save_probabilities": save_probabilities,
        "no_save_warp": no_save_warp,
        "mrf": mrf,
        "no_mrf": no_mrf,
        "no_block_coordinate_descent": no_block_coordinate_descent,
        "logdomain_costandgradient_calculator": logdomain_costandgradient_calculator,
        "no_logdomain_costandgradient_calculator": no_logdomain_costandgradient_calculator,
        "recon": recon,
        "fill": fill,
        "normalization2": normalization2,
        "use_t2w": use_t2w,
        "use_flair": use_flair,
        "hires": hires,
        "pallidum_separate": pallidum_separate,
        "lesion": lesion,
        "reg_only": reg_only,
        "save_mesh": save_mesh,
        "ignore_unknown": ignore_unknown,
    }
    if t1w_files is not None:
        params["t1w_files"] = t1w_files
    if t2w_files is not None:
        params["t2w_files"] = t2w_files
    if flair_files is not None:
        params["flair_files"] = flair_files
    if other_modality_files is not None:
        params["other_modality_files"] = other_modality_files
    if options_file is not None:
        params["options_file"] = options_file
    if dissection_photo_mode is not None:
        params["dissection_photo_mode"] = dissection_photo_mode
    if subject is not None:
        params["subject"] = subject
    if threads is not None:
        params["threads"] = threads
    if atlas_directory is not None:
        params["atlas_directory"] = atlas_directory
    if gmm_file is not None:
        params["gmm_file"] = gmm_file
    if subjects_directory is not None:
        params["subjects_directory"] = subjects_directory
    if stiffness is not None:
        params["stiffness"] = stiffness
    if lesion_mask_pattern is not None:
        params["lesion_mask_pattern"] = lesion_mask_pattern
    if bias_field_smoothing_kernel is not None:
        params["bias_field_smoothing_kernel"] = bias_field_smoothing_kernel
    if registration_file is not None:
        params["registration_file"] = registration_file
    if regmat_file is not None:
        params["regmat_file"] = regmat_file
    if init_lta is not None:
        params["init_lta"] = init_lta
    if ssdd_directory is not None:
        params["ssdd_directory"] = ssdd_directory
    if max_iters is not None:
        params["max_iters"] = max_iters
    if dice_file is not None:
        params["dice_file"] = dice_file
    if smooth_wm_cortex is not None:
        params["smooth_wm_cortex"] = smooth_wm_cortex
    if profile_file is not None:
        params["profile_file"] = profile_file
    return params

def samseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Samseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("t1w_files", None) is not None:
        if not isinstance(params["t1w_files"], list):
            raise StyxValidationError(f'`t1w_files` has the wrong type: Received `{type(params["t1w_files"])}` expected `list[InputPathType]`')
        for e_2 in params["t1w_files"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`t1w_files` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("t2w_files", None) is not None:
        if not isinstance(params["t2w_files"], list):
            raise StyxValidationError(f'`t2w_files` has the wrong type: Received `{type(params["t2w_files"])}` expected `list[InputPathType]`')
        for e_3 in params["t2w_files"]:
            if not isinstance(e_3, (pathlib.Path, str)):
                raise StyxValidationError(f'`t2w_files` has the wrong type: Received `{type(e_3)}` expected `list[InputPathType]`')
    if params.get("flair_files", None) is not None:
        if not isinstance(params["flair_files"], list):
            raise StyxValidationError(f'`flair_files` has the wrong type: Received `{type(params["flair_files"])}` expected `list[InputPathType]`')
        for e_4 in params["flair_files"]:
            if not isinstance(e_4, (pathlib.Path, str)):
                raise StyxValidationError(f'`flair_files` has the wrong type: Received `{type(e_4)}` expected `list[InputPathType]`')
    if params.get("other_modality_files", None) is not None:
        if not isinstance(params["other_modality_files"], list):
            raise StyxValidationError(f'`other_modality_files` has the wrong type: Received `{type(params["other_modality_files"])}` expected `list[InputPathType]`')
        for e_5 in params["other_modality_files"]:
            if not isinstance(e_5, (pathlib.Path, str)):
                raise StyxValidationError(f'`other_modality_files` has the wrong type: Received `{type(e_5)}` expected `list[InputPathType]`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("options_file", None) is not None:
        if not isinstance(params["options_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`options_file` has the wrong type: Received `{type(params["options_file"])}` expected `InputPathType`')
    if params.get("dissection_photo_mode", None) is not None:
        if not isinstance(params["dissection_photo_mode"], str):
            raise StyxValidationError(f'`dissection_photo_mode` has the wrong type: Received `{type(params["dissection_photo_mode"])}` expected `str`')
    if params.get("save_history", None) is not None:
        if not isinstance(params["save_history"], bool):
            raise StyxValidationError(f'`save_history` has the wrong type: Received `{type(params["save_history"])}` expected `bool`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("save_posteriors", None) is not None:
        if not isinstance(params["save_posteriors"], bool):
            raise StyxValidationError(f'`save_posteriors` has the wrong type: Received `{type(params["save_posteriors"])}` expected `bool`')
    if params.get("save_probabilities", None) is not None:
        if not isinstance(params["save_probabilities"], bool):
            raise StyxValidationError(f'`save_probabilities` has the wrong type: Received `{type(params["save_probabilities"])}` expected `bool`')
    if params.get("no_save_warp", None) is not None:
        if not isinstance(params["no_save_warp"], bool):
            raise StyxValidationError(f'`no_save_warp` has the wrong type: Received `{type(params["no_save_warp"])}` expected `bool`')
    if params.get("mrf", None) is not None:
        if not isinstance(params["mrf"], bool):
            raise StyxValidationError(f'`mrf` has the wrong type: Received `{type(params["mrf"])}` expected `bool`')
    if params.get("no_mrf", None) is not None:
        if not isinstance(params["no_mrf"], bool):
            raise StyxValidationError(f'`no_mrf` has the wrong type: Received `{type(params["no_mrf"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("atlas_directory", None) is not None:
        if not isinstance(params["atlas_directory"], str):
            raise StyxValidationError(f'`atlas_directory` has the wrong type: Received `{type(params["atlas_directory"])}` expected `str`')
    if params.get("gmm_file", None) is not None:
        if not isinstance(params["gmm_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`gmm_file` has the wrong type: Received `{type(params["gmm_file"])}` expected `InputPathType`')
    if params.get("no_block_coordinate_descent", None) is not None:
        if not isinstance(params["no_block_coordinate_descent"], bool):
            raise StyxValidationError(f'`no_block_coordinate_descent` has the wrong type: Received `{type(params["no_block_coordinate_descent"])}` expected `bool`')
    if params.get("logdomain_costandgradient_calculator", None) is not None:
        if not isinstance(params["logdomain_costandgradient_calculator"], bool):
            raise StyxValidationError(f'`logdomain_costandgradient_calculator` has the wrong type: Received `{type(params["logdomain_costandgradient_calculator"])}` expected `bool`')
    if params.get("no_logdomain_costandgradient_calculator", None) is not None:
        if not isinstance(params["no_logdomain_costandgradient_calculator"], bool):
            raise StyxValidationError(f'`no_logdomain_costandgradient_calculator` has the wrong type: Received `{type(params["no_logdomain_costandgradient_calculator"])}` expected `bool`')
    if params.get("recon", None) is not None:
        if not isinstance(params["recon"], bool):
            raise StyxValidationError(f'`recon` has the wrong type: Received `{type(params["recon"])}` expected `bool`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], bool):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `bool`')
    if params.get("normalization2", None) is not None:
        if not isinstance(params["normalization2"], bool):
            raise StyxValidationError(f'`normalization2` has the wrong type: Received `{type(params["normalization2"])}` expected `bool`')
    if params.get("use_t2w", None) is not None:
        if not isinstance(params["use_t2w"], bool):
            raise StyxValidationError(f'`use_t2w` has the wrong type: Received `{type(params["use_t2w"])}` expected `bool`')
    if params.get("use_flair", None) is not None:
        if not isinstance(params["use_flair"], bool):
            raise StyxValidationError(f'`use_flair` has the wrong type: Received `{type(params["use_flair"])}` expected `bool`')
    if params.get("hires", None) is not None:
        if not isinstance(params["hires"], bool):
            raise StyxValidationError(f'`hires` has the wrong type: Received `{type(params["hires"])}` expected `bool`')
    if params.get("subjects_directory", None) is not None:
        if not isinstance(params["subjects_directory"], str):
            raise StyxValidationError(f'`subjects_directory` has the wrong type: Received `{type(params["subjects_directory"])}` expected `str`')
    if params.get("pallidum_separate", None) is not None:
        if not isinstance(params["pallidum_separate"], bool):
            raise StyxValidationError(f'`pallidum_separate` has the wrong type: Received `{type(params["pallidum_separate"])}` expected `bool`')
    if params.get("stiffness", None) is not None:
        if not isinstance(params["stiffness"], (float, int)):
            raise StyxValidationError(f'`stiffness` has the wrong type: Received `{type(params["stiffness"])}` expected `float`')
    if params.get("lesion", None) is not None:
        if not isinstance(params["lesion"], bool):
            raise StyxValidationError(f'`lesion` has the wrong type: Received `{type(params["lesion"])}` expected `bool`')
    if params.get("lesion_mask_pattern", None) is not None:
        if not isinstance(params["lesion_mask_pattern"], list):
            raise StyxValidationError(f'`lesion_mask_pattern` has the wrong type: Received `{type(params["lesion_mask_pattern"])}` expected `list[float]`')
        for e_6 in params["lesion_mask_pattern"]:
            if not isinstance(e_6, (float, int)):
                raise StyxValidationError(f'`lesion_mask_pattern` has the wrong type: Received `{type(e_6)}` expected `list[float]`')
    if params.get("bias_field_smoothing_kernel", None) is not None:
        if not isinstance(params["bias_field_smoothing_kernel"], (float, int)):
            raise StyxValidationError(f'`bias_field_smoothing_kernel` has the wrong type: Received `{type(params["bias_field_smoothing_kernel"])}` expected `float`')
    if params.get("registration_file", None) is not None:
        if not isinstance(params["registration_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("regmat_file", None) is not None:
        if not isinstance(params["regmat_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`regmat_file` has the wrong type: Received `{type(params["regmat_file"])}` expected `InputPathType`')
    if params.get("init_lta", None) is not None:
        if not isinstance(params["init_lta"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_lta` has the wrong type: Received `{type(params["init_lta"])}` expected `InputPathType`')
    if params.get("reg_only", None) is not None:
        if not isinstance(params["reg_only"], bool):
            raise StyxValidationError(f'`reg_only` has the wrong type: Received `{type(params["reg_only"])}` expected `bool`')
    if params.get("ssdd_directory", None) is not None:
        if not isinstance(params["ssdd_directory"], str):
            raise StyxValidationError(f'`ssdd_directory` has the wrong type: Received `{type(params["ssdd_directory"])}` expected `str`')
    if params.get("save_mesh", None) is not None:
        if not isinstance(params["save_mesh"], bool):
            raise StyxValidationError(f'`save_mesh` has the wrong type: Received `{type(params["save_mesh"])}` expected `bool`')
    if params.get("max_iters", None) is not None:
        if not isinstance(params["max_iters"], (float, int)):
            raise StyxValidationError(f'`max_iters` has the wrong type: Received `{type(params["max_iters"])}` expected `float`')
    if params.get("dice_file", None) is not None:
        if not isinstance(params["dice_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`dice_file` has the wrong type: Received `{type(params["dice_file"])}` expected `InputPathType`')
    if params.get("ignore_unknown", None) is not None:
        if not isinstance(params["ignore_unknown"], bool):
            raise StyxValidationError(f'`ignore_unknown` has the wrong type: Received `{type(params["ignore_unknown"])}` expected `bool`')
    if params.get("smooth_wm_cortex", None) is not None:
        if not isinstance(params["smooth_wm_cortex"], (float, int)):
            raise StyxValidationError(f'`smooth_wm_cortex` has the wrong type: Received `{type(params["smooth_wm_cortex"])}` expected `float`')
    if params.get("profile_file", None) is not None:
        if not isinstance(params["profile_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`profile_file` has the wrong type: Received `{type(params["profile_file"])}` expected `InputPathType`')

def samseg_cargs(params: Samseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("samseg")
    cargs.append("--i")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("t1w_files")
    if v_0 is not None:
        cargs.append("--t1w")
        for item1 in v_0:
            cargs.append(execution.input_file(item1))
    v_1 = params.get("t2w_files")
    if v_1 is not None:
        cargs.append("--t2w")
        for item2 in v_1:
            cargs.append(execution.input_file(item2))
    v_2 = params.get("flair_files")
    if v_2 is not None:
        cargs.append("--flair")
        for item3 in v_2:
            cargs.append(execution.input_file(item3))
    v_3 = params.get("other_modality_files")
    if v_3 is not None:
        cargs.append("--mode")
        for item4 in v_3:
            cargs.append(execution.input_file(item4))
    cargs.append("--o")
    cargs.append(params["output_directory"])
    v_4 = params.get("options_file")
    if v_4 is not None:
        cargs.append("--options")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("dissection_photo_mode")
    if v_5 is not None:
        cargs.append("--dissection-photo")
        cargs.append(v_5)
    if params.get("save_history"):
        cargs.append("--history")
    v_6 = params.get("subject")
    if v_6 is not None:
        cargs.append("--s")
        cargs.append(v_6)
    if params.get("save_posteriors"):
        cargs.append("--save-posteriors")
    if params.get("save_probabilities"):
        cargs.append("--save-probabilities")
    if params.get("no_save_warp"):
        cargs.append("--no-save-warp")
    if params.get("mrf"):
        cargs.append("--mrf")
    if params.get("no_mrf"):
        cargs.append("--no-mrf")
    v_7 = params.get("threads")
    if v_7 is not None:
        cargs.append("--threads")
        cargs.append(str(v_7))
    v_8 = params.get("atlas_directory")
    if v_8 is not None:
        cargs.append("--atlas")
        cargs.append(v_8)
    v_9 = params.get("gmm_file")
    if v_9 is not None:
        cargs.append("--gmm")
        cargs.append(execution.input_file(v_9))
    if params.get("no_block_coordinate_descent"):
        cargs.append("--no-block-coordinate-descent")
    if params.get("logdomain_costandgradient_calculator"):
        cargs.append("--logdomain-costandgradient-calculator")
    if params.get("no_logdomain_costandgradient_calculator"):
        cargs.append("--no-logdomain-costandgradient-calculator")
    if params.get("recon"):
        cargs.append("--recon")
    if params.get("fill"):
        cargs.append("--fill")
    if params.get("normalization2"):
        cargs.append("--normalization2")
    if params.get("use_t2w"):
        cargs.append("--use-t2w")
    if params.get("use_flair"):
        cargs.append("--use-flair")
    if params.get("hires"):
        cargs.append("--hires")
    v_10 = params.get("subjects_directory")
    if v_10 is not None:
        cargs.append("--sd")
        cargs.append(v_10)
    if params.get("pallidum_separate"):
        cargs.append("--pallidum-separate")
    v_11 = params.get("stiffness")
    if v_11 is not None:
        cargs.append("--stiffness")
        cargs.append(str(v_11))
    if params.get("lesion"):
        cargs.append("--lesion")
    v_12 = params.get("lesion_mask_pattern")
    if v_12 is not None:
        cargs.append("--lesion-mask-pattern")
        for item5 in v_12:
            cargs.append(str(item5))
    v_13 = params.get("bias_field_smoothing_kernel")
    if v_13 is not None:
        cargs.append("--bias-field-smoothing-kernel")
        cargs.append(str(v_13))
    v_14 = params.get("registration_file")
    if v_14 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("regmat_file")
    if v_15 is not None:
        cargs.append("--regmat")
        cargs.append(execution.input_file(v_15))
    v_16 = params.get("init_lta")
    if v_16 is not None:
        cargs.append("--initlta")
        cargs.append(execution.input_file(v_16))
    if params.get("reg_only"):
        cargs.append("--reg-only")
    v_17 = params.get("ssdd_directory")
    if v_17 is not None:
        cargs.append("--ssdd")
        cargs.append(v_17)
    if params.get("save_mesh"):
        cargs.append("--save-mesh")
    v_18 = params.get("max_iters")
    if v_18 is not None:
        cargs.append("--max-iters")
        cargs.append(str(v_18))
    v_19 = params.get("dice_file")
    if v_19 is not None:
        cargs.append("--dice")
        cargs.append(execution.input_file(v_19))
    if params.get("ignore_unknown"):
        cargs.append("--ignore-unknown")
    v_20 = params.get("smooth_wm_cortex")
    if v_20 is not None:
        cargs.append("--smooth-wm-cortex")
        cargs.append(str(v_20))
    v_21 = params.get("profile_file")
    if v_21 is not None:
        cargs.append("--profile")
        cargs.append(execution.input_file(v_21))
    return cargs

def samseg_outputs(params: Samseg, execution: Execution) -> SamsegOutputs:
    """Build the SamsegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmentation_output_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/seg.mgz')
    posteriors_output_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/posteriors.mgz')
    probabilities_output_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/probabilities.mgz')
    mesh_output_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/mesh.stl')
    return SamsegOutputs(
        root=root_v,
        segmentation_output=segmentation_output_v,
        posteriors_output=posteriors_output_v,
        probabilities_output=probabilities_output_v,
        mesh_output=mesh_output_v,
    )

def samseg_execute(params: Samseg, runner: Runner | None = None) -> SamsegOutputs:
    """
    samseg

    SAMSEG (Statistical and Algorithmic Methods for Segmentation) is a tool for segmenting neuroimaging data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    samseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SAMSEG_METADATA)
    execution.params(params)
    args = samseg_cargs(params, execution)
    out = samseg_outputs(params, execution)
    execution.run(args)
    return out

def samseg(
    input_files: list[InputPathType],
    output_directory: str,
    t1w_files: list[InputPathType] | None = None,
    t2w_files: list[InputPathType] | None = None,
    flair_files: list[InputPathType] | None = None,
    other_modality_files: list[InputPathType] | None = None,
    options_file: InputPathType | None = None,
    dissection_photo_mode: str | None = None,
    save_history: bool = False,
    subject: str | None = None,
    save_posteriors: bool = False,
    save_probabilities: bool = False,
    no_save_warp: bool = False,
    mrf: bool = False,
    no_mrf: bool = False,
    threads: float | None = None,
    atlas_directory: str | None = None,
    gmm_file: InputPathType | None = None,
    no_block_coordinate_descent: bool = False,
    logdomain_costandgradient_calculator: bool = False,
    no_logdomain_costandgradient_calculator: bool = False,
    recon: bool = False,
    fill: bool = False,
    normalization2: bool = False,
    use_t2w: bool = False,
    use_flair: bool = False,
    hires: bool = False,
    subjects_directory: str | None = None,
    pallidum_separate: bool = False,
    stiffness: float | None = None,
    lesion: bool = False,
    lesion_mask_pattern: list[float] | None = None,
    bias_field_smoothing_kernel: float | None = None,
    registration_file: InputPathType | None = None,
    regmat_file: InputPathType | None = None,
    init_lta: InputPathType | None = None,
    reg_only: bool = False,
    ssdd_directory: str | None = None,
    save_mesh: bool = False,
    max_iters: float | None = None,
    dice_file: InputPathType | None = None,
    ignore_unknown: bool = False,
    smooth_wm_cortex: float | None = None,
    profile_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> SamsegOutputs:
    """
    samseg

    SAMSEG (Statistical and Algorithmic Methods for Segmentation) is a tool for segmenting neuroimaging data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input image files, must be in registration with each other.
        output_directory: Output directory where results will be saved.
        t1w_files: T1-weighted input file, causes input to be conformed unless\
            --hires is used.
        t2w_files: T2-weighted input file.
        flair_files: FLAIR-weighted input file.
        other_modality_files: Specific mode input files, each associated with a\
            custom modality name.
        options_file: JSON file for specifying advanced options.
        dissection_photo_mode: Mode for processing 3D reconstructed dissection\
            photos, specifying left/right/both.
        save_history: Turns on saving of processing history.
        subject: Subject identifier, sets output directory to subject/mri/samseg.\
            The first input must be the conformed T1 weighted input.
        save_posteriors: Save posterior probabilities.
        save_probabilities: Save posterior, prior, and likelihood as 3-frame volume\
            for each tissue type.
        no_save_warp: Do not save m3z-style warp.
        mrf: Perform Markov Random Field processing.
        no_mrf: Do not perform Markov Random Field processing.
        threads: Set the number of CPUs to use.
        atlas_directory: Path to the SAMSEG atlas directory.
        gmm_file: Gaussian Mixture Model file.
        no_block_coordinate_descent: Do not use block coordinate descent.
        logdomain_costandgradient_calculator: Use log-domain cost and gradient\
            calculator.
        no_logdomain_costandgradient_calculator: Do not use log-domain cost and\
            gradient calculator.
        recon: Run recon-all.
        fill: Use samseg to create filled.mgz instead of recon-all.
        normalization2: Use samseg to create brain.mgz instead of recon-all (with\
            --recon).
        use_t2w: Use the T2-weighted input when running recon-all.
        use_flair: Use the FLAIR-weighted input when running recon-all.
        hires: Use -hires when running recon-all.
        subjects_directory: Path to the SUBJECTS_DIR.
        pallidum_separate: Move pallidum outside of global white matter class, used\
            for analyzing T2 or FLAIR.
        stiffness: Set mesh stiffness.
        lesion: Turn on lesion segmentation.
        lesion_mask_pattern: Pattern needed when using T2 for lesion mask, e.g., 0\
            1.
        bias_field_smoothing_kernel: Width of bias field smoothness.
        registration_file: Registration file (LTA or MAT) as output by samseg.
        regmat_file: Same as --reg.
        init_lta: Initial registration LTA file.
        reg_only: Only perform registration.
        ssdd_directory: Path to SAMSEG_Data_DIR where the atlas is located.
        save_mesh: Save the mesh, useful for longitudinal analysis.
        max_iters: Maximum number of iterations.
        dice_file: DICE coefficient file for segmentation.
        ignore_unknown: Ignore unknown flags.
        smooth_wm_cortex: Smooth white matter and cortex priors using specified\
            sigma value.
        profile_file: Run using the cProfile Python profiler, with the specified\
            profile file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = samseg_params(
        input_files=input_files,
        output_directory=output_directory,
        t1w_files=t1w_files,
        t2w_files=t2w_files,
        flair_files=flair_files,
        other_modality_files=other_modality_files,
        options_file=options_file,
        dissection_photo_mode=dissection_photo_mode,
        save_history=save_history,
        subject=subject,
        save_posteriors=save_posteriors,
        save_probabilities=save_probabilities,
        no_save_warp=no_save_warp,
        mrf=mrf,
        no_mrf=no_mrf,
        threads=threads,
        atlas_directory=atlas_directory,
        gmm_file=gmm_file,
        no_block_coordinate_descent=no_block_coordinate_descent,
        logdomain_costandgradient_calculator=logdomain_costandgradient_calculator,
        no_logdomain_costandgradient_calculator=no_logdomain_costandgradient_calculator,
        recon=recon,
        fill=fill,
        normalization2=normalization2,
        use_t2w=use_t2w,
        use_flair=use_flair,
        hires=hires,
        subjects_directory=subjects_directory,
        pallidum_separate=pallidum_separate,
        stiffness=stiffness,
        lesion=lesion,
        lesion_mask_pattern=lesion_mask_pattern,
        bias_field_smoothing_kernel=bias_field_smoothing_kernel,
        registration_file=registration_file,
        regmat_file=regmat_file,
        init_lta=init_lta,
        reg_only=reg_only,
        ssdd_directory=ssdd_directory,
        save_mesh=save_mesh,
        max_iters=max_iters,
        dice_file=dice_file,
        ignore_unknown=ignore_unknown,
        smooth_wm_cortex=smooth_wm_cortex,
        profile_file=profile_file,
    )
    return samseg_execute(params, runner)

__all__ = [
    "Samseg",
    "SamsegOutputs",
    "SAMSEG_METADATA",
    "samseg_cargs",
    "samseg_outputs",
    "samseg_params",
    "samseg_execute",
    "samseg_validate",
    "samseg",
]