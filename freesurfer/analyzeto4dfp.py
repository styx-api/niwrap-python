# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANALYZETO4DFP_METADATA = Metadata(
    id="analyzeto4dfp",
    name="analyzeto4dfp",
    package="freesurfer",
)

Analyzeto4dfp = typing.TypedDict(
    "Analyzeto4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/analyzeto4dfp"]],
        "analyze_image": InputPathType,
        "rois_scale": typing.NotRequired[bool],
        "flip_x": typing.NotRequired[bool],
        "flip_y": typing.NotRequired[bool],
        "flip_z": typing.NotRequired[bool],
        "endian": typing.NotRequired[str],
        "orientation": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class Analyzeto4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def analyzeto4dfp_params(
    analyze_image: InputPathType,
    rois_scale: bool = False,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endian: str | None = None,
    orientation: int | None = None,
) -> Analyzeto4dfp:
    """
    Build parameters.

    Args:
        analyze_image: Input ANALYZE image file
        rois_scale: Apply ROIScaleFactor
        flip_x: Flip first axis
        flip_y: Flip second axis
        flip_z: Flip third axis
        endian: Output big or little endian (default CPU endian)
        orientation: Supply orientation code (in range [0-5])

    Returns:
        Parameter dictionary.
    """
    params: Analyzeto4dfp = {
        "@type": "freesurfer/analyzeto4dfp",
        "analyze_image": analyze_image,
        "rois_scale": rois_scale,
        "flip_x": flip_x,
        "flip_y": flip_y,
        "flip_z": flip_z,
    }
    if endian is not None:
        params["endian"] = endian
    if orientation is not None:
        params["orientation"] = orientation
    return params

def analyzeto4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Analyzeto4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("analyze_image", None) is None:
        raise StyxValidationError("`analyze_image` must not be None")
    if not isinstance(params["analyze_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`analyze_image` has the wrong type: Received `{type(params["analyze_image"])}` expected `InputPathType`')
    if params.get("rois_scale", None) is not None:
        if not isinstance(params["rois_scale"], bool):
            raise StyxValidationError(f'`rois_scale` has the wrong type: Received `{type(params["rois_scale"])}` expected `bool`')
    if params.get("flip_x", None) is not None:
        if not isinstance(params["flip_x"], bool):
            raise StyxValidationError(f'`flip_x` has the wrong type: Received `{type(params["flip_x"])}` expected `bool`')
    if params.get("flip_y", None) is not None:
        if not isinstance(params["flip_y"], bool):
            raise StyxValidationError(f'`flip_y` has the wrong type: Received `{type(params["flip_y"])}` expected `bool`')
    if params.get("flip_z", None) is not None:
        if not isinstance(params["flip_z"], bool):
            raise StyxValidationError(f'`flip_z` has the wrong type: Received `{type(params["flip_z"])}` expected `bool`')
    if params.get("endian", None) is not None:
        if not isinstance(params["endian"], str):
            raise StyxValidationError(f'`endian` has the wrong type: Received `{type(params["endian"])}` expected `str`')
    if params.get("orientation", None) is not None:
        if not isinstance(params["orientation"], int):
            raise StyxValidationError(f'`orientation` has the wrong type: Received `{type(params["orientation"])}` expected `int`')
        if not (0 <= params["orientation"] <= 5):
            raise StyxValidationError("Parameter `orientation` must be between 0 and 5 (inclusive)")

def analyzeto4dfp_cargs(params: Analyzeto4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("analyzeto4dfp")
    cargs.append(execution.input_file(params["analyze_image"]))
    if params.get("rois_scale"):
        cargs.append("-s")
    if params.get("flip_x"):
        cargs.append("-x")
    if params.get("flip_y"):
        cargs.append("-y")
    if params.get("flip_z"):
        cargs.append("-z")
    v_0 = params.get("endian")
    if v_0 is not None:
        cargs.append("-@")
        cargs.append(v_0)
    v_1 = params.get("orientation")
    if v_1 is not None:
        cargs.append("-O")
        cargs.append(str(v_1))
    return cargs

def analyzeto4dfp_outputs(params: Analyzeto4dfp, execution: Execution) -> Analyzeto4dfpOutputs:
    """Build the Analyzeto4dfpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Analyzeto4dfpOutputs(
        root=root_v,
    )

def analyzeto4dfp_execute(params: Analyzeto4dfp, runner: Runner | None = None) -> Analyzeto4dfpOutputs:
    """
    analyzeto4dfp

    Convert ANALYZE image format to 4dfp format with various options

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    analyzeto4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANALYZETO4DFP_METADATA)
    execution.params(params)
    args = analyzeto4dfp_cargs(params, execution)
    out = analyzeto4dfp_outputs(params, execution)
    execution.run(args)
    return out

def analyzeto4dfp(
    analyze_image: InputPathType,
    rois_scale: bool = False,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endian: str | None = None,
    orientation: int | None = None,
    runner: Runner | None = None,
) -> Analyzeto4dfpOutputs:
    """
    analyzeto4dfp

    Convert ANALYZE image format to 4dfp format with various options

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        analyze_image: Input ANALYZE image file
        rois_scale: Apply ROIScaleFactor
        flip_x: Flip first axis
        flip_y: Flip second axis
        flip_z: Flip third axis
        endian: Output big or little endian (default CPU endian)
        orientation: Supply orientation code (in range [0-5])
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = analyzeto4dfp_params(
        analyze_image=analyze_image,
        rois_scale=rois_scale,
        flip_x=flip_x,
        flip_y=flip_y,
        flip_z=flip_z,
        endian=endian,
        orientation=orientation,
    )
    return analyzeto4dfp_execute(params, runner)

__all__ = [
    "Analyzeto4dfp",
    "Analyzeto4dfpOutputs",
    "ANALYZETO4DFP_METADATA",
    "analyzeto4dfp_cargs",
    "analyzeto4dfp_outputs",
    "analyzeto4dfp_params",
    "analyzeto4dfp_execute",
    "analyzeto4dfp_validate",
    "analyzeto4dfp",
]