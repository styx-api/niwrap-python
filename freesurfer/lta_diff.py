# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LTA_DIFF_METADATA = Metadata(
    id="lta_diff",
    name="lta_diff",
    package="freesurfer",
)

LtaDiff = typing.TypedDict(
    "LtaDiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/lta_diff"]],
        "transform1": InputPathType,
        "transform2": typing.NotRequired[InputPathType],
        "dist_type": typing.NotRequired[int],
        "invert1": typing.NotRequired[bool],
        "invert2": typing.NotRequired[bool],
        "vox": typing.NotRequired[bool],
        "normdiv": typing.NotRequired[float],
        "radius": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class LtaDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def lta_diff_params(
    transform1: InputPathType,
    transform2: InputPathType | None = None,
    dist_type: int | None = None,
    invert1: bool = False,
    invert2: bool = False,
    vox: bool = False,
    normdiv: float | None = None,
    radius: float | None = None,
) -> LtaDiff:
    """
    Build parameters.

    Args:
        transform1: First transform file
        transform2: Second transform file
        dist_type: Distance type: 1 (Rigid Trans. Dist.), 2 (Affine Transform\
            Distance), 3 (8-corners mean distance), 4 (Max Displacement), 5\
            (Determinant scaling), 6 (Interpolation Smoothing), 7 (Decomposition).\
            Default is 2.
        invert1: Invert first transform before computing difference matrix D
        invert2: Invert second transform before computing difference matrix D
        vox: Compute distance in vox coordinates, after adjusting for voxel sizes.\
            Default is RAS coordinates.
        normdiv: Divide final distance by this value for step adjustment.
        radius: Radius in mm, used for RMS distance. Default is 100 to include the\
            head.

    Returns:
        Parameter dictionary.
    """
    params: LtaDiff = {
        "@type": "freesurfer/lta_diff",
        "transform1": transform1,
        "invert1": invert1,
        "invert2": invert2,
        "vox": vox,
    }
    if transform2 is not None:
        params["transform2"] = transform2
    if dist_type is not None:
        params["dist_type"] = dist_type
    if normdiv is not None:
        params["normdiv"] = normdiv
    if radius is not None:
        params["radius"] = radius
    return params

def lta_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LtaDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("transform1", None) is None:
        raise StyxValidationError("`transform1` must not be None")
    if not isinstance(params["transform1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform1` has the wrong type: Received `{type(params["transform1"])}` expected `InputPathType`')
    if params.get("transform2", None) is not None:
        if not isinstance(params["transform2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`transform2` has the wrong type: Received `{type(params["transform2"])}` expected `InputPathType`')
    if params.get("dist_type", None) is not None:
        if not isinstance(params["dist_type"], int):
            raise StyxValidationError(f'`dist_type` has the wrong type: Received `{type(params["dist_type"])}` expected `int`')
    if params.get("invert1", None) is not None:
        if not isinstance(params["invert1"], bool):
            raise StyxValidationError(f'`invert1` has the wrong type: Received `{type(params["invert1"])}` expected `bool`')
    if params.get("invert2", None) is not None:
        if not isinstance(params["invert2"], bool):
            raise StyxValidationError(f'`invert2` has the wrong type: Received `{type(params["invert2"])}` expected `bool`')
    if params.get("vox", None) is not None:
        if not isinstance(params["vox"], bool):
            raise StyxValidationError(f'`vox` has the wrong type: Received `{type(params["vox"])}` expected `bool`')
    if params.get("normdiv", None) is not None:
        if not isinstance(params["normdiv"], (float, int)):
            raise StyxValidationError(f'`normdiv` has the wrong type: Received `{type(params["normdiv"])}` expected `float`')
    if params.get("radius", None) is not None:
        if not isinstance(params["radius"], (float, int)):
            raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')

def lta_diff_cargs(params: LtaDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("lta_diff")
    cargs.append(execution.input_file(params["transform1"]))
    v_0 = params.get("transform2")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("dist_type")
    if v_1 is not None:
        cargs.append("--dist")
        cargs.append(str(v_1))
    if params.get("invert1"):
        cargs.append("--invert1")
    if params.get("invert2"):
        cargs.append("--invert2")
    if params.get("vox"):
        cargs.append("--vox")
    v_2 = params.get("normdiv")
    if v_2 is not None:
        cargs.append("--normdiv")
        cargs.append(str(v_2))
    v_3 = params.get("radius")
    if v_3 is not None:
        cargs.append("--radius")
        cargs.append(str(v_3))
    return cargs

def lta_diff_outputs(params: LtaDiff, execution: Execution) -> LtaDiffOutputs:
    """Build the LtaDiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LtaDiffOutputs(
        root=root_v,
    )

def lta_diff_execute(params: LtaDiff, runner: Runner | None = None) -> LtaDiffOutputs:
    """
    lta_diff

    A tool to compute different distance norms for a single transform or for the difference between two transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    lta_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LTA_DIFF_METADATA)
    execution.params(params)
    args = lta_diff_cargs(params, execution)
    out = lta_diff_outputs(params, execution)
    execution.run(args)
    return out

def lta_diff(
    transform1: InputPathType,
    transform2: InputPathType | None = None,
    dist_type: int | None = None,
    invert1: bool = False,
    invert2: bool = False,
    vox: bool = False,
    normdiv: float | None = None,
    radius: float | None = None,
    runner: Runner | None = None,
) -> LtaDiffOutputs:
    """
    lta_diff

    A tool to compute different distance norms for a single transform or for the difference between two transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        transform1: First transform file
        transform2: Second transform file
        dist_type: Distance type: 1 (Rigid Trans. Dist.), 2 (Affine Transform\
            Distance), 3 (8-corners mean distance), 4 (Max Displacement), 5\
            (Determinant scaling), 6 (Interpolation Smoothing), 7 (Decomposition).\
            Default is 2.
        invert1: Invert first transform before computing difference matrix D
        invert2: Invert second transform before computing difference matrix D
        vox: Compute distance in vox coordinates, after adjusting for voxel sizes.\
            Default is RAS coordinates.
        normdiv: Divide final distance by this value for step adjustment.
        radius: Radius in mm, used for RMS distance. Default is 100 to include the\
            head.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = lta_diff_params(
        transform1=transform1,
        transform2=transform2,
        dist_type=dist_type,
        invert1=invert1,
        invert2=invert2,
        vox=vox,
        normdiv=normdiv,
        radius=radius,
    )
    return lta_diff_execute(params, runner)

__all__ = [
    "LtaDiff",
    "LtaDiffOutputs",
    "LTA_DIFF_METADATA",
    "lta_diff_cargs",
    "lta_diff_outputs",
    "lta_diff_params",
    "lta_diff_execute",
    "lta_diff_validate",
    "lta_diff",
]