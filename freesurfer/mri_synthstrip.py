# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SYNTHSTRIP_METADATA = Metadata(
    id="mri_synthstrip",
    name="mri_synthstrip",
    package="freesurfer",
)

class MriSynthstripOutputImage(typing.TypedDict):
    output_image: typing.NotRequired[str]
    """Save stripped image to path."""

class MriSynthstripMask(typing.TypedDict):
    mask: typing.NotRequired[InputPathType]
    """Save binary brain mask to path."""

class MriSynthstripGpu(typing.TypedDict):
    gpu: typing.NotRequired[bool]
    """Use the GPU."""

class MriSynthstripBorder(typing.TypedDict):
    border: typing.NotRequired[float]
    """Mask border threshold in mm. Default is 1."""

class MriSynthstripExcludeCsf(typing.TypedDict):
    exclude_csf: typing.NotRequired[bool]
    """Exclude CSF from brain border."""

class MriSynthstripModelWeights(typing.TypedDict):
    model_weights: typing.NotRequired[InputPathType]
    """Alternative model weights."""

MriSynthstrip = typing.TypedDict(
    "MriSynthstrip",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_synthstrip"]],
        "image": InputPathType,
        "output_image": MriSynthstripOutputImage,
        "mask": MriSynthstripMask,
        "gpu": MriSynthstripGpu,
        "border": MriSynthstripBorder,
        "exclude_csf": MriSynthstripExcludeCsf,
        "model_weights": MriSynthstripModelWeights,
    },
)

@dataclasses.dataclass
class MriSynthstripOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType | None
    """Stripped brain image output."""
    output_mask_file: OutputPathType | None
    """Binary brain mask output."""

def mri_synthstrip_params(
    image: InputPathType,
    output_image: MriSynthstripOutputImage,
    mask: MriSynthstripMask,
    gpu: MriSynthstripGpu,
    border: MriSynthstripBorder,
    exclude_csf: MriSynthstripExcludeCsf,
    model_weights: MriSynthstripModelWeights,
) -> MriSynthstrip:
    """
    Build parameters.

    Args:
        image: Input image to skullstrip.
        output_image:
        mask:
        gpu:
        border:
        exclude_csf:
        model_weights:

    Returns:
        Parameter dictionary.
    """
    params: MriSynthstrip = {
        "@type": "freesurfer/mri_synthstrip",
        "image": image,
        "output_image": output_image,
        "mask": mask,
        "gpu": gpu,
        "border": border,
        "exclude_csf": exclude_csf,
        "model_weights": model_weights,
    }
    return params

def mri_synthstrip_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSynthstrip."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["output_image"])}\'')
    if params["output_image"].get("output_image", None) is not None:
        if not isinstance(params["output_image"]["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"]["output_image"])}` expected `str`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["mask"])}\'')
    if params["mask"].get("mask", None) is not None:
        if not isinstance(params["mask"]["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"]["mask"])}` expected `InputPathType`')
    if params.get("gpu", None) is None:
        raise StyxValidationError("`gpu` must not be None")
    if not isinstance(params["gpu"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["gpu"])}\'')
    if params["gpu"].get("gpu", None) is not None:
        if not isinstance(params["gpu"]["gpu"], bool):
            raise StyxValidationError(f'`gpu` has the wrong type: Received `{type(params["gpu"]["gpu"])}` expected `bool`')
    if params.get("border", None) is None:
        raise StyxValidationError("`border` must not be None")
    if not isinstance(params["border"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["border"])}\'')
    if params["border"].get("border", None) is not None:
        if not isinstance(params["border"]["border"], (float, int)):
            raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"]["border"])}` expected `float`')
    if params.get("exclude_csf", None) is None:
        raise StyxValidationError("`exclude_csf` must not be None")
    if not isinstance(params["exclude_csf"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_csf"])}\'')
    if params["exclude_csf"].get("exclude_csf", None) is not None:
        if not isinstance(params["exclude_csf"]["exclude_csf"], bool):
            raise StyxValidationError(f'`exclude_csf` has the wrong type: Received `{type(params["exclude_csf"]["exclude_csf"])}` expected `bool`')
    if params.get("model_weights", None) is None:
        raise StyxValidationError("`model_weights` must not be None")
    if not isinstance(params["model_weights"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["model_weights"])}\'')
    if params["model_weights"].get("model_weights", None) is not None:
        if not isinstance(params["model_weights"]["model_weights"], (pathlib.Path, str)):
            raise StyxValidationError(f'`model_weights` has the wrong type: Received `{type(params["model_weights"]["model_weights"])}` expected `InputPathType`')

def mri_synthstrip_cargs(params: MriSynthstrip, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_synthstrip")
    cargs.append("-i")
    cargs.append(execution.input_file(params["image"]))
    cargs.append("".join(["[", ("".join(["-o", v_0]) if (v_0 := params["output_image"].get("output_image")) is not None else ""), "]"]))
    cargs.append("".join(["[", ("".join(["-m", execution.input_file(v_1)]) if (v_1 := params["mask"].get("mask")) is not None else ""), "]"]))
    cargs.append("".join(["[", ("-g" if params["gpu"].get("gpu") else ""), "]"]))
    cargs.append("".join(["[", ("".join(["-b", str(v_2)]) if (v_2 := params["border"].get("border")) is not None else ""), "]"]))
    cargs.append("".join(["[", ("--no-csf" if params["exclude_csf"].get("exclude_csf") else ""), "]"]))
    cargs.append("".join(["[", ("".join(["--model", execution.input_file(v_3)]) if (v_3 := params["model_weights"].get("model_weights")) is not None else ""), "]"]))
    return cargs

def mri_synthstrip_outputs(params: MriSynthstrip, execution: Execution) -> MriSynthstripOutputs:
    """Build the MriSynthstripOutputs object for this tool."""
    output_image_file_v: OutputPathType | None = None
    output_mask_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params["output_image"].get("output_image")
    if __v0 is not None:
        output_image_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params["mask"].get("mask")
    if __v1 is not None:
        output_mask_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    return MriSynthstripOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
        output_mask_file=output_mask_file_v,
    )

def mri_synthstrip_execute(params: MriSynthstrip, runner: Runner | None = None) -> MriSynthstripOutputs:
    """
    mri_synthstrip

    Robust, universal skull-stripping for brain images of any type.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_synthstrip_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SYNTHSTRIP_METADATA)
    execution.params(params)
    args = mri_synthstrip_cargs(params, execution)
    out = mri_synthstrip_outputs(params, execution)
    execution.run(args)
    return out

def mri_synthstrip(
    image: InputPathType,
    output_image: MriSynthstripOutputImage,
    mask: MriSynthstripMask,
    gpu: MriSynthstripGpu,
    border: MriSynthstripBorder,
    exclude_csf: MriSynthstripExcludeCsf,
    model_weights: MriSynthstripModelWeights,
    runner: Runner | None = None,
) -> MriSynthstripOutputs:
    """
    mri_synthstrip

    Robust, universal skull-stripping for brain images of any type.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        image: Input image to skullstrip.
        output_image:
        mask:
        gpu:
        border:
        exclude_csf:
        model_weights:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_synthstrip_params(
        image=image,
        output_image=output_image,
        mask=mask,
        gpu=gpu,
        border=border,
        exclude_csf=exclude_csf,
        model_weights=model_weights,
    )
    return mri_synthstrip_execute(params, runner)

__all__ = [
    "MriSynthstrip",
    "MriSynthstripOutputs",
    "MRI_SYNTHSTRIP_METADATA",
    "mri_synthstrip_cargs",
    "mri_synthstrip_outputs",
    "mri_synthstrip_params",
    "mri_synthstrip_execute",
    "mri_synthstrip_validate",
    "mri_synthstrip",
]