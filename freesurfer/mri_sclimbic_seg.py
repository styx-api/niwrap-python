# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SCLIMBIC_SEG_METADATA = Metadata(
    id="mri_sclimbic_seg",
    name="mri_sclimbic_seg",
    package="freesurfer",
)

MriSclimbicSeg = typing.TypedDict(
    "MriSclimbicSeg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_sclimbic_seg"]],
        "input_file": str,
        "output_file": str,
        "subjects": typing.NotRequired[list[str]],
        "subjects_dir": typing.NotRequired[str],
        "conform": typing.NotRequired[bool],
        "etiv": typing.NotRequired[bool],
        "exclude_labels": typing.NotRequired[list[str]],
        "keep_ac": typing.NotRequired[bool],
        "vox_count_volumes": typing.NotRequired[bool],
        "model_file": typing.NotRequired[InputPathType],
        "ctab_file": typing.NotRequired[InputPathType],
        "population_stats": typing.NotRequired[InputPathType],
        "debug": typing.NotRequired[bool],
        "vmp": typing.NotRequired[bool],
        "threads": typing.NotRequired[int],
        "tal_xfm": typing.NotRequired[str],
        "write_posteriors": typing.NotRequired[bool],
        "write_volumes": typing.NotRequired[bool],
        "write_qa_stats": typing.NotRequired[bool],
        "preprocess_7T": typing.NotRequired[bool],
        "percentile": typing.NotRequired[float],
        "cuda_device": typing.NotRequired[str],
        "output_base": typing.NotRequired[str],
        "no_cite": typing.NotRequired[bool],
        "nchannels": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class MriSclimbicSegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmentation_output: OutputPathType
    """Segmentation output file."""

def mri_sclimbic_seg_params(
    input_file: str,
    output_file: str,
    subjects: list[str] | None = None,
    subjects_dir: str | None = None,
    conform: bool = False,
    etiv: bool = False,
    exclude_labels: list[str] | None = None,
    keep_ac: bool = False,
    vox_count_volumes: bool = False,
    model_file: InputPathType | None = None,
    ctab_file: InputPathType | None = None,
    population_stats: InputPathType | None = None,
    debug: bool = False,
    vmp: bool = False,
    threads: int | None = None,
    tal_xfm: str | None = None,
    write_posteriors: bool = False,
    write_volumes: bool = False,
    write_qa_stats: bool = False,
    preprocess_7T: bool = False,
    percentile: float | None = None,
    cuda_device: str | None = None,
    output_base: str | None = None,
    no_cite: bool = False,
    nchannels: int | None = None,
) -> MriSclimbicSeg:
    """
    Build parameters.

    Args:
        input_file: T1-w image(s) to segment. Can be a path to a single image or a\
            directory of images.
        output_file: Segmentation output (required if --i is provided). Must be the\
            same type as the input path (a single file or directory).
        subjects: Process a series of freesurfer recon-all subjects (enables\
            subject-mode).
        subjects_dir: Set the subjects directory (overrides the SUBJECTS_DIR env\
            variable).
        conform: Resample input to 1mm-iso; results will be put back in native\
            resolution.
        etiv: Include eTIV in volume stats (enabled by default in subject-mode and\
            --tal).
        exclude_labels: List of label IDs to exclude in any output stats files.
        keep_ac: Explicitly keep anterior commissure in the volume/qa files.
        vox_count_volumes: Use discrete voxel count for label volumes.
        model_file: Alternative model weights to load.
        ctab_file: Alternative color lookup table to embed in segmentation. Must be\
            minimal, including 0, and sorted.
        population_stats: Alternative population volume stats for QA output.
        debug: Enable debug logging.
        vmp: Enable printing of vmpeak at the end.
        threads: Number of threads to use. Default is 1.
        tal_xfm: Alternative talairach xfm transform for estimating TIV. Can be\
            file or suffix (for multiple inputs).
        write_posteriors: Save the label posteriors.
        write_volumes: Save label volume stats (enabled by default in\
            subject-mode).
        write_qa_stats: Save QA stats (z and confidence).
        preprocess_7T: Preprocess 7T images (just sets percentile to 99.9).
        percentile: Use intensity percentile threshold for normalization.
        cuda_device: Cuda device for GPU support.
        output_base: String to use in output file name; default is sclimbic.
        no_cite: Do not cite sclimbic paper at the end.
        nchannels: Number of channels

    Returns:
        Parameter dictionary.
    """
    params: MriSclimbicSeg = {
        "@type": "freesurfer/mri_sclimbic_seg",
        "input_file": input_file,
        "output_file": output_file,
        "conform": conform,
        "etiv": etiv,
        "keep_ac": keep_ac,
        "vox_count_volumes": vox_count_volumes,
        "debug": debug,
        "vmp": vmp,
        "write_posteriors": write_posteriors,
        "write_volumes": write_volumes,
        "write_qa_stats": write_qa_stats,
        "preprocess_7T": preprocess_7T,
        "no_cite": no_cite,
    }
    if subjects is not None:
        params["subjects"] = subjects
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if exclude_labels is not None:
        params["exclude_labels"] = exclude_labels
    if model_file is not None:
        params["model_file"] = model_file
    if ctab_file is not None:
        params["ctab_file"] = ctab_file
    if population_stats is not None:
        params["population_stats"] = population_stats
    if threads is not None:
        params["threads"] = threads
    if tal_xfm is not None:
        params["tal_xfm"] = tal_xfm
    if percentile is not None:
        params["percentile"] = percentile
    if cuda_device is not None:
        params["cuda_device"] = cuda_device
    if output_base is not None:
        params["output_base"] = output_base
    if nchannels is not None:
        params["nchannels"] = nchannels
    return params

def mri_sclimbic_seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSclimbicSeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], str):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("subjects", None) is not None:
        if not isinstance(params["subjects"], list):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
        for e in params["subjects"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("conform", None) is not None:
        if not isinstance(params["conform"], bool):
            raise StyxValidationError(f'`conform` has the wrong type: Received `{type(params["conform"])}` expected `bool`')
    if params.get("etiv", None) is not None:
        if not isinstance(params["etiv"], bool):
            raise StyxValidationError(f'`etiv` has the wrong type: Received `{type(params["etiv"])}` expected `bool`')
    if params.get("exclude_labels", None) is not None:
        if not isinstance(params["exclude_labels"], list):
            raise StyxValidationError(f'`exclude_labels` has the wrong type: Received `{type(params["exclude_labels"])}` expected `list[str]`')
        for e_2 in params["exclude_labels"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`exclude_labels` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("keep_ac", None) is not None:
        if not isinstance(params["keep_ac"], bool):
            raise StyxValidationError(f'`keep_ac` has the wrong type: Received `{type(params["keep_ac"])}` expected `bool`')
    if params.get("vox_count_volumes", None) is not None:
        if not isinstance(params["vox_count_volumes"], bool):
            raise StyxValidationError(f'`vox_count_volumes` has the wrong type: Received `{type(params["vox_count_volumes"])}` expected `bool`')
    if params.get("model_file", None) is not None:
        if not isinstance(params["model_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`model_file` has the wrong type: Received `{type(params["model_file"])}` expected `InputPathType`')
    if params.get("ctab_file", None) is not None:
        if not isinstance(params["ctab_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab_file` has the wrong type: Received `{type(params["ctab_file"])}` expected `InputPathType`')
    if params.get("population_stats", None) is not None:
        if not isinstance(params["population_stats"], (pathlib.Path, str)):
            raise StyxValidationError(f'`population_stats` has the wrong type: Received `{type(params["population_stats"])}` expected `InputPathType`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("vmp", None) is not None:
        if not isinstance(params["vmp"], bool):
            raise StyxValidationError(f'`vmp` has the wrong type: Received `{type(params["vmp"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], int):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `int`')
    if params.get("tal_xfm", None) is not None:
        if not isinstance(params["tal_xfm"], str):
            raise StyxValidationError(f'`tal_xfm` has the wrong type: Received `{type(params["tal_xfm"])}` expected `str`')
    if params.get("write_posteriors", None) is not None:
        if not isinstance(params["write_posteriors"], bool):
            raise StyxValidationError(f'`write_posteriors` has the wrong type: Received `{type(params["write_posteriors"])}` expected `bool`')
    if params.get("write_volumes", None) is not None:
        if not isinstance(params["write_volumes"], bool):
            raise StyxValidationError(f'`write_volumes` has the wrong type: Received `{type(params["write_volumes"])}` expected `bool`')
    if params.get("write_qa_stats", None) is not None:
        if not isinstance(params["write_qa_stats"], bool):
            raise StyxValidationError(f'`write_qa_stats` has the wrong type: Received `{type(params["write_qa_stats"])}` expected `bool`')
    if params.get("preprocess_7T", None) is not None:
        if not isinstance(params["preprocess_7T"], bool):
            raise StyxValidationError(f'`preprocess_7T` has the wrong type: Received `{type(params["preprocess_7T"])}` expected `bool`')
    if params.get("percentile", None) is not None:
        if not isinstance(params["percentile"], (float, int)):
            raise StyxValidationError(f'`percentile` has the wrong type: Received `{type(params["percentile"])}` expected `float`')
    if params.get("cuda_device", None) is not None:
        if not isinstance(params["cuda_device"], str):
            raise StyxValidationError(f'`cuda_device` has the wrong type: Received `{type(params["cuda_device"])}` expected `str`')
    if params.get("output_base", None) is not None:
        if not isinstance(params["output_base"], str):
            raise StyxValidationError(f'`output_base` has the wrong type: Received `{type(params["output_base"])}` expected `str`')
    if params.get("no_cite", None) is not None:
        if not isinstance(params["no_cite"], bool):
            raise StyxValidationError(f'`no_cite` has the wrong type: Received `{type(params["no_cite"])}` expected `bool`')
    if params.get("nchannels", None) is not None:
        if not isinstance(params["nchannels"], int):
            raise StyxValidationError(f'`nchannels` has the wrong type: Received `{type(params["nchannels"])}` expected `int`')

def mri_sclimbic_seg_cargs(params: MriSclimbicSeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_sclimbic_seg")
    cargs.append("-i")
    cargs.append(params["input_file"])
    cargs.append("-o")
    cargs.append(params["output_file"])
    v_0 = params.get("subjects")
    if v_0 is not None:
        cargs.append("-s")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("subjects_dir")
    if v_1 is not None:
        cargs.append("--sd")
        cargs.append(v_1)
    if params.get("conform"):
        cargs.append("--conform")
    if params.get("etiv"):
        cargs.append("--etiv")
    v_2 = params.get("exclude_labels")
    if v_2 is not None:
        cargs.append("--exclude")
        for item1 in v_2:
            cargs.append(item1)
    if params.get("keep_ac"):
        cargs.append("--keep_ac")
    if params.get("vox_count_volumes"):
        cargs.append("--vox-count-volumes")
    v_3 = params.get("model_file")
    if v_3 is not None:
        cargs.append("--model")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("ctab_file")
    if v_4 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("population_stats")
    if v_5 is not None:
        cargs.append("--population-stats")
        cargs.append(execution.input_file(v_5))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("vmp"):
        cargs.append("--vmp")
    v_6 = params.get("threads")
    if v_6 is not None:
        cargs.append("--threads")
        cargs.append(str(v_6))
    v_7 = params.get("tal_xfm")
    if v_7 is not None:
        cargs.append("--tal")
        cargs.append(v_7)
    if params.get("write_posteriors"):
        cargs.append("--write_posteriors")
    if params.get("write_volumes"):
        cargs.append("--write_volumes")
    if params.get("write_qa_stats"):
        cargs.append("--write_qa_stats")
    if params.get("preprocess_7T"):
        cargs.append("--7T")
    v_8 = params.get("percentile")
    if v_8 is not None:
        cargs.append("--percentile")
        cargs.append(str(v_8))
    v_9 = params.get("cuda_device")
    if v_9 is not None:
        cargs.append("--cuda-device")
        cargs.append(v_9)
    v_10 = params.get("output_base")
    if v_10 is not None:
        cargs.append("--output-base")
        cargs.append(v_10)
    if params.get("no_cite"):
        cargs.append("--no-cite-sclimbic")
    v_11 = params.get("nchannels")
    if v_11 is not None:
        cargs.append("--nchannels")
        cargs.append(str(v_11))
    return cargs

def mri_sclimbic_seg_outputs(params: MriSclimbicSeg, execution: Execution) -> MriSclimbicSegOutputs:
    """Build the MriSclimbicSegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmentation_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MriSclimbicSegOutputs(
        root=root_v,
        segmentation_output=segmentation_output_v,
    )

def mri_sclimbic_seg_execute(params: MriSclimbicSeg, runner: Runner | None = None) -> MriSclimbicSegOutputs:
    """
    mri_sclimbic_seg

    Segment subcortical limbic structures using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_sclimbic_seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SCLIMBIC_SEG_METADATA)
    execution.params(params)
    args = mri_sclimbic_seg_cargs(params, execution)
    out = mri_sclimbic_seg_outputs(params, execution)
    execution.run(args)
    return out

def mri_sclimbic_seg(
    input_file: str,
    output_file: str,
    subjects: list[str] | None = None,
    subjects_dir: str | None = None,
    conform: bool = False,
    etiv: bool = False,
    exclude_labels: list[str] | None = None,
    keep_ac: bool = False,
    vox_count_volumes: bool = False,
    model_file: InputPathType | None = None,
    ctab_file: InputPathType | None = None,
    population_stats: InputPathType | None = None,
    debug: bool = False,
    vmp: bool = False,
    threads: int | None = None,
    tal_xfm: str | None = None,
    write_posteriors: bool = False,
    write_volumes: bool = False,
    write_qa_stats: bool = False,
    preprocess_7T: bool = False,
    percentile: float | None = None,
    cuda_device: str | None = None,
    output_base: str | None = None,
    no_cite: bool = False,
    nchannels: int | None = None,
    runner: Runner | None = None,
) -> MriSclimbicSegOutputs:
    """
    mri_sclimbic_seg

    Segment subcortical limbic structures using Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: T1-w image(s) to segment. Can be a path to a single image or a\
            directory of images.
        output_file: Segmentation output (required if --i is provided). Must be the\
            same type as the input path (a single file or directory).
        subjects: Process a series of freesurfer recon-all subjects (enables\
            subject-mode).
        subjects_dir: Set the subjects directory (overrides the SUBJECTS_DIR env\
            variable).
        conform: Resample input to 1mm-iso; results will be put back in native\
            resolution.
        etiv: Include eTIV in volume stats (enabled by default in subject-mode and\
            --tal).
        exclude_labels: List of label IDs to exclude in any output stats files.
        keep_ac: Explicitly keep anterior commissure in the volume/qa files.
        vox_count_volumes: Use discrete voxel count for label volumes.
        model_file: Alternative model weights to load.
        ctab_file: Alternative color lookup table to embed in segmentation. Must be\
            minimal, including 0, and sorted.
        population_stats: Alternative population volume stats for QA output.
        debug: Enable debug logging.
        vmp: Enable printing of vmpeak at the end.
        threads: Number of threads to use. Default is 1.
        tal_xfm: Alternative talairach xfm transform for estimating TIV. Can be\
            file or suffix (for multiple inputs).
        write_posteriors: Save the label posteriors.
        write_volumes: Save label volume stats (enabled by default in\
            subject-mode).
        write_qa_stats: Save QA stats (z and confidence).
        preprocess_7T: Preprocess 7T images (just sets percentile to 99.9).
        percentile: Use intensity percentile threshold for normalization.
        cuda_device: Cuda device for GPU support.
        output_base: String to use in output file name; default is sclimbic.
        no_cite: Do not cite sclimbic paper at the end.
        nchannels: Number of channels
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_sclimbic_seg_params(
        input_file=input_file,
        output_file=output_file,
        subjects=subjects,
        subjects_dir=subjects_dir,
        conform=conform,
        etiv=etiv,
        exclude_labels=exclude_labels,
        keep_ac=keep_ac,
        vox_count_volumes=vox_count_volumes,
        model_file=model_file,
        ctab_file=ctab_file,
        population_stats=population_stats,
        debug=debug,
        vmp=vmp,
        threads=threads,
        tal_xfm=tal_xfm,
        write_posteriors=write_posteriors,
        write_volumes=write_volumes,
        write_qa_stats=write_qa_stats,
        preprocess_7T=preprocess_7T,
        percentile=percentile,
        cuda_device=cuda_device,
        output_base=output_base,
        no_cite=no_cite,
        nchannels=nchannels,
    )
    return mri_sclimbic_seg_execute(params, runner)

__all__ = [
    "MriSclimbicSeg",
    "MriSclimbicSegOutputs",
    "MRI_SCLIMBIC_SEG_METADATA",
    "mri_sclimbic_seg_cargs",
    "mri_sclimbic_seg_outputs",
    "mri_sclimbic_seg_params",
    "mri_sclimbic_seg_execute",
    "mri_sclimbic_seg_validate",
    "mri_sclimbic_seg",
]