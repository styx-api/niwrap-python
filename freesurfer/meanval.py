# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MEANVAL_METADATA = Metadata(
    id="meanval",
    name="meanval",
    package="freesurfer",
)

Meanval = typing.TypedDict(
    "Meanval",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/meanval"]],
        "input_file": InputPathType,
        "mask_file": InputPathType,
        "output_file": str,
        "avgwf_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MeanvalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mean_output_file: OutputPathType
    """File containing the mean value calculated"""

def meanval_params(
    input_file: InputPathType,
    mask_file: InputPathType,
    output_file: str,
    avgwf_flag: bool = False,
) -> Meanval:
    """
    Build parameters.

    Args:
        input_file: Input volume file
        mask_file: Mask file
        output_file: Output file where mean value will be stored
        avgwf_flag: Flag to calculate the average waveform

    Returns:
        Parameter dictionary.
    """
    params: Meanval = {
        "@type": "freesurfer/meanval",
        "input_file": input_file,
        "mask_file": mask_file,
        "output_file": output_file,
        "avgwf_flag": avgwf_flag,
    }
    return params

def meanval_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Meanval."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("mask_file", None) is None:
        raise StyxValidationError("`mask_file` must not be None")
    if not isinstance(params["mask_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("avgwf_flag", None) is not None:
        if not isinstance(params["avgwf_flag"], bool):
            raise StyxValidationError(f'`avgwf_flag` has the wrong type: Received `{type(params["avgwf_flag"])}` expected `bool`')

def meanval_cargs(params: Meanval, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("meanval")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("--m")
    cargs.append(execution.input_file(params["mask_file"]))
    cargs.append("--o")
    cargs.append(params["output_file"])
    if params.get("avgwf_flag"):
        cargs.append("--avgwf")
    return cargs

def meanval_outputs(params: Meanval, execution: Execution) -> MeanvalOutputs:
    """Build the MeanvalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    mean_output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MeanvalOutputs(
        root=root_v,
        mean_output_file=mean_output_file_v,
    )

def meanval_execute(params: Meanval, runner: Runner | None = None) -> MeanvalOutputs:
    """
    meanval

    Tool to calculate the mean value of an image within a mask

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    meanval_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MEANVAL_METADATA)
    execution.params(params)
    args = meanval_cargs(params, execution)
    out = meanval_outputs(params, execution)
    execution.run(args)
    return out

def meanval(
    input_file: InputPathType,
    mask_file: InputPathType,
    output_file: str,
    avgwf_flag: bool = False,
    runner: Runner | None = None,
) -> MeanvalOutputs:
    """
    meanval

    Tool to calculate the mean value of an image within a mask

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input volume file
        mask_file: Mask file
        output_file: Output file where mean value will be stored
        avgwf_flag: Flag to calculate the average waveform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = meanval_params(
        input_file=input_file,
        mask_file=mask_file,
        output_file=output_file,
        avgwf_flag=avgwf_flag,
    )
    return meanval_execute(params, runner)

__all__ = [
    "Meanval",
    "MeanvalOutputs",
    "MEANVAL_METADATA",
    "meanval_cargs",
    "meanval_outputs",
    "meanval_params",
    "meanval_execute",
    "meanval_validate",
    "meanval",
]