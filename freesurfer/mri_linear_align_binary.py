# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LINEAR_ALIGN_BINARY_METADATA = Metadata(
    id="mri_linear_align_binary",
    name="mri_linear_align_binary",
    package="freesurfer",
)

MriLinearAlignBinary = typing.TypedDict(
    "MriLinearAlignBinary",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_linear_align_binary"]],
        "source": InputPathType,
        "target": InputPathType,
        "output_xform": str,
        "target_label": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriLinearAlignBinaryOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xform_file: OutputPathType
    """The output transformation file"""

def mri_linear_align_binary_params(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
    target_label: str | None = None,
) -> MriLinearAlignBinary:
    """
    Build parameters.

    Args:
        source: Source image file
        target: Target image file
        output_xform: Output transformation file
        target_label: Target label, assume source is high-resolution hippocampal\
            labeling

    Returns:
        Parameter dictionary.
    """
    params: MriLinearAlignBinary = {
        "@type": "freesurfer/mri_linear_align_binary",
        "source": source,
        "target": target,
        "output_xform": output_xform,
    }
    if target_label is not None:
        params["target_label"] = target_label
    return params

def mri_linear_align_binary_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLinearAlignBinary."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("output_xform", None) is None:
        raise StyxValidationError("`output_xform` must not be None")
    if not isinstance(params["output_xform"], str):
        raise StyxValidationError(f'`output_xform` has the wrong type: Received `{type(params["output_xform"])}` expected `str`')
    if params.get("target_label", None) is not None:
        if not isinstance(params["target_label"], str):
            raise StyxValidationError(f'`target_label` has the wrong type: Received `{type(params["target_label"])}` expected `str`')

def mri_linear_align_binary_cargs(params: MriLinearAlignBinary, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_linear_align_binary")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["target"]))
    cargs.append(params["output_xform"])
    v_0 = params.get("target_label")
    if v_0 is not None:
        cargs.append("-h")
        cargs.append(v_0)
    return cargs

def mri_linear_align_binary_outputs(params: MriLinearAlignBinary, execution: Execution) -> MriLinearAlignBinaryOutputs:
    """Build the MriLinearAlignBinaryOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xform_file_v: OutputPathType = execution.output_file(params["output_xform"])
    return MriLinearAlignBinaryOutputs(
        root=root_v,
        output_xform_file=output_xform_file_v,
    )

def mri_linear_align_binary_execute(params: MriLinearAlignBinary, runner: Runner | None = None) -> MriLinearAlignBinaryOutputs:
    """
    mri_linear_align_binary

    A tool for linear alignment of MRI images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_linear_align_binary_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LINEAR_ALIGN_BINARY_METADATA)
    execution.params(params)
    args = mri_linear_align_binary_cargs(params, execution)
    out = mri_linear_align_binary_outputs(params, execution)
    execution.run(args)
    return out

def mri_linear_align_binary(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
    target_label: str | None = None,
    runner: Runner | None = None,
) -> MriLinearAlignBinaryOutputs:
    """
    mri_linear_align_binary

    A tool for linear alignment of MRI images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source: Source image file
        target: Target image file
        output_xform: Output transformation file
        target_label: Target label, assume source is high-resolution hippocampal\
            labeling
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_linear_align_binary_params(
        source=source,
        target=target,
        output_xform=output_xform,
        target_label=target_label,
    )
    return mri_linear_align_binary_execute(params, runner)

__all__ = [
    "MriLinearAlignBinary",
    "MriLinearAlignBinaryOutputs",
    "MRI_LINEAR_ALIGN_BINARY_METADATA",
    "mri_linear_align_binary_cargs",
    "mri_linear_align_binary_outputs",
    "mri_linear_align_binary_params",
    "mri_linear_align_binary_execute",
    "mri_linear_align_binary_validate",
    "mri_linear_align_binary",
]