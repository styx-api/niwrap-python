# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_CREATE_BASE_SIGMA_METADATA = Metadata(
    id="long_create_base_sigma",
    name="long_create_base_sigma",
    package="freesurfer",
)

LongCreateBaseSigma = typing.TypedDict(
    "LongCreateBaseSigma",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_create_base_sigma"]],
        "base_id": str,
        "sigma": int,
    },
)

@dataclasses.dataclass
class LongCreateBaseSigmaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def long_create_base_sigma_params(
    base_id: str,
    sigma: int,
) -> LongCreateBaseSigma:
    """
    Build parameters.

    Args:
        base_id: Subject ID of the base
        sigma: Integer value of sigma smoothing (usually 2..6)

    Returns:
        Parameter dictionary.
    """
    params: LongCreateBaseSigma = {
        "@type": "freesurfer/long_create_base_sigma",
        "base_id": base_id,
        "sigma": sigma,
    }
    return params

def long_create_base_sigma_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongCreateBaseSigma."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_id", None) is None:
        raise StyxValidationError("`base_id` must not be None")
    if not isinstance(params["base_id"], str):
        raise StyxValidationError(f'`base_id` has the wrong type: Received `{type(params["base_id"])}` expected `str`')
    if params.get("sigma", None) is None:
        raise StyxValidationError("`sigma` must not be None")
    if not isinstance(params["sigma"], int):
        raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `int`')

def long_create_base_sigma_cargs(params: LongCreateBaseSigma, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_create_base_sigma")
    cargs.append(params["base_id"])
    cargs.append(str(params["sigma"]))
    return cargs

def long_create_base_sigma_outputs(params: LongCreateBaseSigma, execution: Execution) -> LongCreateBaseSigmaOutputs:
    """Build the LongCreateBaseSigmaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LongCreateBaseSigmaOutputs(
        root=root_v,
    )

def long_create_base_sigma_execute(params: LongCreateBaseSigma, runner: Runner | None = None) -> LongCreateBaseSigmaOutputs:
    """
    long_create_base_sigma

    Performs a joint normalization and atlas renormalization at a specific sigma smoothing level. This is usually done as part of the base stream but can be performed to add files for a different sigma level to an existing base.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_create_base_sigma_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_CREATE_BASE_SIGMA_METADATA)
    execution.params(params)
    args = long_create_base_sigma_cargs(params, execution)
    out = long_create_base_sigma_outputs(params, execution)
    execution.run(args)
    return out

def long_create_base_sigma(
    base_id: str,
    sigma: int,
    runner: Runner | None = None,
) -> LongCreateBaseSigmaOutputs:
    """
    long_create_base_sigma

    Performs a joint normalization and atlas renormalization at a specific sigma smoothing level. This is usually done as part of the base stream but can be performed to add files for a different sigma level to an existing base.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_id: Subject ID of the base
        sigma: Integer value of sigma smoothing (usually 2..6)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_create_base_sigma_params(
        base_id=base_id,
        sigma=sigma,
    )
    return long_create_base_sigma_execute(params, runner)

__all__ = [
    "LongCreateBaseSigma",
    "LongCreateBaseSigmaOutputs",
    "LONG_CREATE_BASE_SIGMA_METADATA",
    "long_create_base_sigma_cargs",
    "long_create_base_sigma_outputs",
    "long_create_base_sigma_params",
    "long_create_base_sigma_execute",
    "long_create_base_sigma_validate",
    "long_create_base_sigma",
]