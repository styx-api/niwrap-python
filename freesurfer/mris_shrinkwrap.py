# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SHRINKWRAP_METADATA = Metadata(
    id="mris_shrinkwrap",
    name="mris_shrinkwrap",
    package="freesurfer",
)

MrisShrinkwrap = typing.TypedDict(
    "MrisShrinkwrap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_shrinkwrap"]],
        "volume": InputPathType,
        "output_name": str,
        "threshold": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisShrinkwrapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    inner_skull: OutputPathType
    """Output surface file representing the inner skull."""
    outer_skull: OutputPathType
    """Output surface file representing the outer skull."""
    outer_skin: OutputPathType
    """Output surface file representing the outer skin."""

def mris_shrinkwrap_params(
    volume: InputPathType,
    output_name: str,
    threshold: float | None = None,
) -> MrisShrinkwrap:
    """
    Build parameters.

    Args:
        volume: Input image volume for shrink wrap.
        output_name: Base name for output surface files.
        threshold: Apply threshold to image before deforming on distance transform.

    Returns:
        Parameter dictionary.
    """
    params: MrisShrinkwrap = {
        "@type": "freesurfer/mris_shrinkwrap",
        "volume": volume,
        "output_name": output_name,
    }
    if threshold is not None:
        params["threshold"] = threshold
    return params

def mris_shrinkwrap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisShrinkwrap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')

def mris_shrinkwrap_cargs(params: MrisShrinkwrap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_shrinkwrap")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(params["output_name"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(str(v_0))
    return cargs

def mris_shrinkwrap_outputs(params: MrisShrinkwrap, execution: Execution) -> MrisShrinkwrapOutputs:
    """Build the MrisShrinkwrapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    inner_skull_v: OutputPathType = execution.output_file(f'{params["output_name"]}_inner_skull.tri')
    outer_skull_v: OutputPathType = execution.output_file(f'{params["output_name"]}_outer_skull.tri')
    outer_skin_v: OutputPathType = execution.output_file(f'{params["output_name"]}_outer_skin.tri')
    return MrisShrinkwrapOutputs(
        root=root_v,
        inner_skull=inner_skull_v,
        outer_skull=outer_skull_v,
        outer_skin=outer_skin_v,
    )

def mris_shrinkwrap_execute(params: MrisShrinkwrap, runner: Runner | None = None) -> MrisShrinkwrapOutputs:
    """
    mris_shrinkwrap

    Generate shrink-wrapped tessellations of the input volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_shrinkwrap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SHRINKWRAP_METADATA)
    execution.params(params)
    args = mris_shrinkwrap_cargs(params, execution)
    out = mris_shrinkwrap_outputs(params, execution)
    execution.run(args)
    return out

def mris_shrinkwrap(
    volume: InputPathType,
    output_name: str,
    threshold: float | None = None,
    runner: Runner | None = None,
) -> MrisShrinkwrapOutputs:
    """
    mris_shrinkwrap

    Generate shrink-wrapped tessellations of the input volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume: Input image volume for shrink wrap.
        output_name: Base name for output surface files.
        threshold: Apply threshold to image before deforming on distance transform.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_shrinkwrap_params(
        volume=volume,
        output_name=output_name,
        threshold=threshold,
    )
    return mris_shrinkwrap_execute(params, runner)

__all__ = [
    "MrisShrinkwrap",
    "MrisShrinkwrapOutputs",
    "MRIS_SHRINKWRAP_METADATA",
    "mris_shrinkwrap_cargs",
    "mris_shrinkwrap_outputs",
    "mris_shrinkwrap_params",
    "mris_shrinkwrap_execute",
    "mris_shrinkwrap_validate",
    "mris_shrinkwrap",
]