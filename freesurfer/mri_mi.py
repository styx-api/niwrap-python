# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MI_METADATA = Metadata(
    id="mri_mi",
    name="mri_mi",
    package="freesurfer",
)

MriMi = typing.TypedDict(
    "MriMi",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mi"]],
        "input_file1": InputPathType,
        "input_file2": InputPathType,
        "bins": typing.NotRequired[str],
        "silent": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriMiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_mi_params(
    input_file1: InputPathType,
    input_file2: InputPathType,
    bins: str | None = None,
    silent: bool = False,
) -> MriMi:
    """
    Build parameters.

    Args:
        input_file1: First input file name.
        input_file2: Second input file name.
        bins: Specifies the number of bins for the two input volumes. Default is\
            64x64.
        silent: Write out only the final mutual information result.

    Returns:
        Parameter dictionary.
    """
    params: MriMi = {
        "@type": "freesurfer/mri_mi",
        "input_file1": input_file1,
        "input_file2": input_file2,
        "silent": silent,
    }
    if bins is not None:
        params["bins"] = bins
    return params

def mri_mi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file1", None) is None:
        raise StyxValidationError("`input_file1` must not be None")
    if not isinstance(params["input_file1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file1` has the wrong type: Received `{type(params["input_file1"])}` expected `InputPathType`')
    if params.get("input_file2", None) is None:
        raise StyxValidationError("`input_file2` must not be None")
    if not isinstance(params["input_file2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file2` has the wrong type: Received `{type(params["input_file2"])}` expected `InputPathType`')
    if params.get("bins", None) is not None:
        if not isinstance(params["bins"], str):
            raise StyxValidationError(f'`bins` has the wrong type: Received `{type(params["bins"])}` expected `str`')
    if params.get("silent", None) is not None:
        if not isinstance(params["silent"], bool):
            raise StyxValidationError(f'`silent` has the wrong type: Received `{type(params["silent"])}` expected `bool`')

def mri_mi_cargs(params: MriMi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mi")
    cargs.append(execution.input_file(params["input_file1"]))
    cargs.append(execution.input_file(params["input_file2"]))
    v_0 = params.get("bins")
    if v_0 is not None:
        cargs.append("--bins")
        cargs.append(v_0)
    if params.get("silent"):
        cargs.append("--silent")
    return cargs

def mri_mi_outputs(params: MriMi, execution: Execution) -> MriMiOutputs:
    """Build the MriMiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriMiOutputs(
        root=root_v,
    )

def mri_mi_execute(params: MriMi, runner: Runner | None = None) -> MriMiOutputs:
    """
    mri_mi

    Computes mutual information (mi) between two input volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MI_METADATA)
    execution.params(params)
    args = mri_mi_cargs(params, execution)
    out = mri_mi_outputs(params, execution)
    execution.run(args)
    return out

def mri_mi(
    input_file1: InputPathType,
    input_file2: InputPathType,
    bins: str | None = None,
    silent: bool = False,
    runner: Runner | None = None,
) -> MriMiOutputs:
    """
    mri_mi

    Computes mutual information (mi) between two input volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file1: First input file name.
        input_file2: Second input file name.
        bins: Specifies the number of bins for the two input volumes. Default is\
            64x64.
        silent: Write out only the final mutual information result.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mi_params(
        input_file1=input_file1,
        input_file2=input_file2,
        bins=bins,
        silent=silent,
    )
    return mri_mi_execute(params, runner)

__all__ = [
    "MriMi",
    "MriMiOutputs",
    "MRI_MI_METADATA",
    "mri_mi_cargs",
    "mri_mi_outputs",
    "mri_mi_params",
    "mri_mi_execute",
    "mri_mi_validate",
    "mri_mi",
]