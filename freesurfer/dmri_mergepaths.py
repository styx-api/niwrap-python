# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_MERGEPATHS_METADATA = Metadata(
    id="dmri_mergepaths",
    name="dmri_mergepaths",
    package="freesurfer",
)

DmriMergepaths = typing.TypedDict(
    "DmriMergepaths",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_mergepaths"]],
        "input_volumes": list[InputPathType],
        "input_directory": typing.NotRequired[str],
        "output_volume": str,
        "color_table": InputPathType,
        "threshold": float,
        "debug": typing.NotRequired[bool],
        "check_opts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriMergepathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_mergepaths_params(
    input_volumes: list[InputPathType],
    output_volume: str,
    color_table: InputPathType,
    threshold: float,
    input_directory: str | None = None,
    debug: bool = False,
    check_opts: bool = False,
) -> DmriMergepaths:
    """
    Build parameters.

    Args:
        input_volumes: Input volume(s)
        output_volume: Output volume
        color_table: Color table file
        threshold: Lower threshold for display (0<=num<=1, as fraction of max)
        input_directory: Input directory (optional). If specified, names of input\
            files are relative to this
        debug: Turn on debugging
        check_opts: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriMergepaths = {
        "@type": "freesurfer/dmri_mergepaths",
        "input_volumes": input_volumes,
        "output_volume": output_volume,
        "color_table": color_table,
        "threshold": threshold,
        "debug": debug,
        "check_opts": check_opts,
    }
    if input_directory is not None:
        params["input_directory"] = input_directory
    return params

def dmri_mergepaths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriMergepaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("input_directory", None) is not None:
        if not isinstance(params["input_directory"], str):
            raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("color_table", None) is None:
        raise StyxValidationError("`color_table` must not be None")
    if not isinstance(params["color_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`color_table` has the wrong type: Received `{type(params["color_table"])}` expected `InputPathType`')
    if params.get("threshold", None) is None:
        raise StyxValidationError("`threshold` must not be None")
    if not isinstance(params["threshold"], (float, int)):
        raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if not (0 <= params["threshold"] <= 1):
        raise StyxValidationError("Parameter `threshold` must be between 0 and 1 (inclusive)")
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_opts", None) is not None:
        if not isinstance(params["check_opts"], bool):
            raise StyxValidationError(f'`check_opts` has the wrong type: Received `{type(params["check_opts"])}` expected `bool`')

def dmri_mergepaths_cargs(params: DmriMergepaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_mergepaths")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("input_directory")
    if v_0 is not None:
        cargs.append("--indir")
        cargs.append(v_0)
    cargs.append("--out")
    cargs.append(params["output_volume"])
    cargs.append("--ctab")
    cargs.append(execution.input_file(params["color_table"]))
    cargs.append("--thresh")
    cargs.append(str(params["threshold"]))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_opts"):
        cargs.append("--checkopts")
    return cargs

def dmri_mergepaths_outputs(params: DmriMergepaths, execution: Execution) -> DmriMergepathsOutputs:
    """Build the DmriMergepathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriMergepathsOutputs(
        root=root_v,
    )

def dmri_mergepaths_execute(params: DmriMergepaths, runner: Runner | None = None) -> DmriMergepathsOutputs:
    """
    dmri_mergepaths

    A tool for merging diffusion MRI path data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_mergepaths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_MERGEPATHS_METADATA)
    execution.params(params)
    args = dmri_mergepaths_cargs(params, execution)
    out = dmri_mergepaths_outputs(params, execution)
    execution.run(args)
    return out

def dmri_mergepaths(
    input_volumes: list[InputPathType],
    output_volume: str,
    color_table: InputPathType,
    threshold: float,
    input_directory: str | None = None,
    debug: bool = False,
    check_opts: bool = False,
    runner: Runner | None = None,
) -> DmriMergepathsOutputs:
    """
    dmri_mergepaths

    A tool for merging diffusion MRI path data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Input volume(s)
        output_volume: Output volume
        color_table: Color table file
        threshold: Lower threshold for display (0<=num<=1, as fraction of max)
        input_directory: Input directory (optional). If specified, names of input\
            files are relative to this
        debug: Turn on debugging
        check_opts: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_mergepaths_params(
        input_volumes=input_volumes,
        output_volume=output_volume,
        color_table=color_table,
        threshold=threshold,
        input_directory=input_directory,
        debug=debug,
        check_opts=check_opts,
    )
    return dmri_mergepaths_execute(params, runner)

__all__ = [
    "DmriMergepaths",
    "DmriMergepathsOutputs",
    "DMRI_MERGEPATHS_METADATA",
    "dmri_mergepaths_cargs",
    "dmri_mergepaths_outputs",
    "dmri_mergepaths_params",
    "dmri_mergepaths_execute",
    "dmri_mergepaths_validate",
    "dmri_mergepaths",
]