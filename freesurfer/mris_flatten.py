# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_FLATTEN_METADATA = Metadata(
    id="mris_flatten",
    name="mris_flatten",
    package="freesurfer",
)

MrisFlatten = typing.TypedDict(
    "MrisFlatten",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_flatten"]],
        "input_patch": InputPathType,
        "output_patch": str,
        "iterations": typing.NotRequired[float],
        "distances": typing.NotRequired[list[float]],
        "dilations": typing.NotRequired[float],
        "random_seed": typing.NotRequired[float],
        "copy_coords": typing.NotRequired[str],
        "norand": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisFlattenOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_patch_file: OutputPathType
    """Output flattened surface patch"""

def mris_flatten_params(
    input_patch: InputPathType,
    output_patch: str,
    iterations: float | None = None,
    distances: list[float] | None = None,
    dilations: float | None = None,
    random_seed: float | None = None,
    copy_coords: str | None = None,
    norand: bool = False,
) -> MrisFlatten:
    """
    Build parameters.

    Args:
        input_patch: Input surface patch
        output_patch: Output flattened surface patch
        iterations: Write out the surface every # of iterations.
        distances: Specify size of neighborhood and number of vertices at each\
            distance to be used in the optimization.
        dilations: Specify the number of times to dilate the ripped edges to ensure\
            a clean cut
        random_seed: Set the random seed to a specific value so that flattening is\
            repeatable
        copy_coords: Copy xyz coords from surface before flattening.
        norand: Set the random seed to 0 so that flattening is repeatable

    Returns:
        Parameter dictionary.
    """
    params: MrisFlatten = {
        "@type": "freesurfer/mris_flatten",
        "input_patch": input_patch,
        "output_patch": output_patch,
        "norand": norand,
    }
    if iterations is not None:
        params["iterations"] = iterations
    if distances is not None:
        params["distances"] = distances
    if dilations is not None:
        params["dilations"] = dilations
    if random_seed is not None:
        params["random_seed"] = random_seed
    if copy_coords is not None:
        params["copy_coords"] = copy_coords
    return params

def mris_flatten_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisFlatten."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_patch", None) is None:
        raise StyxValidationError("`input_patch` must not be None")
    if not isinstance(params["input_patch"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_patch` has the wrong type: Received `{type(params["input_patch"])}` expected `InputPathType`')
    if params.get("output_patch", None) is None:
        raise StyxValidationError("`output_patch` must not be None")
    if not isinstance(params["output_patch"], str):
        raise StyxValidationError(f'`output_patch` has the wrong type: Received `{type(params["output_patch"])}` expected `str`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], (float, int)):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `float`')
    if params.get("distances", None) is not None:
        if not isinstance(params["distances"], list):
            raise StyxValidationError(f'`distances` has the wrong type: Received `{type(params["distances"])}` expected `list[float]`')
        if not (2 <= len(params["distances"]) <= 2):
            raise StyxValidationError("Parameter `distances` must contain between 2 and 2 elements (inclusive)")
        for e in params["distances"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`distances` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("dilations", None) is not None:
        if not isinstance(params["dilations"], (float, int)):
            raise StyxValidationError(f'`dilations` has the wrong type: Received `{type(params["dilations"])}` expected `float`')
    if params.get("random_seed", None) is not None:
        if not isinstance(params["random_seed"], (float, int)):
            raise StyxValidationError(f'`random_seed` has the wrong type: Received `{type(params["random_seed"])}` expected `float`')
    if params.get("copy_coords", None) is not None:
        if not isinstance(params["copy_coords"], str):
            raise StyxValidationError(f'`copy_coords` has the wrong type: Received `{type(params["copy_coords"])}` expected `str`')
    if params.get("norand", None) is not None:
        if not isinstance(params["norand"], bool):
            raise StyxValidationError(f'`norand` has the wrong type: Received `{type(params["norand"])}` expected `bool`')

def mris_flatten_cargs(params: MrisFlatten, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_flatten")
    cargs.append(execution.input_file(params["input_patch"]))
    cargs.append(params["output_patch"])
    v_0 = params.get("iterations")
    if v_0 is not None:
        cargs.append("-w")
        cargs.append(str(v_0))
    v_1 = params.get("distances")
    if v_1 is not None:
        cargs.append("-distances")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("dilations")
    if v_2 is not None:
        cargs.append("-dilate")
        cargs.append(str(v_2))
    v_3 = params.get("random_seed")
    if v_3 is not None:
        cargs.append("-seed")
        cargs.append(str(v_3))
    v_4 = params.get("copy_coords")
    if v_4 is not None:
        cargs.append("-copy-coords")
        cargs.append(v_4)
    if params.get("norand"):
        cargs.append("-norand")
    return cargs

def mris_flatten_outputs(params: MrisFlatten, execution: Execution) -> MrisFlattenOutputs:
    """Build the MrisFlattenOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_patch_file_v: OutputPathType = execution.output_file(params["output_patch"])
    return MrisFlattenOutputs(
        root=root_v,
        output_patch_file=output_patch_file_v,
    )

def mris_flatten_execute(params: MrisFlatten, runner: Runner | None = None) -> MrisFlattenOutputs:
    """
    mris_flatten

    This program will flatten a surface patch

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_flatten_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_FLATTEN_METADATA)
    execution.params(params)
    args = mris_flatten_cargs(params, execution)
    out = mris_flatten_outputs(params, execution)
    execution.run(args)
    return out

def mris_flatten(
    input_patch: InputPathType,
    output_patch: str,
    iterations: float | None = None,
    distances: list[float] | None = None,
    dilations: float | None = None,
    random_seed: float | None = None,
    copy_coords: str | None = None,
    norand: bool = False,
    runner: Runner | None = None,
) -> MrisFlattenOutputs:
    """
    mris_flatten

    This program will flatten a surface patch

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_patch: Input surface patch
        output_patch: Output flattened surface patch
        iterations: Write out the surface every # of iterations.
        distances: Specify size of neighborhood and number of vertices at each\
            distance to be used in the optimization.
        dilations: Specify the number of times to dilate the ripped edges to ensure\
            a clean cut
        random_seed: Set the random seed to a specific value so that flattening is\
            repeatable
        copy_coords: Copy xyz coords from surface before flattening.
        norand: Set the random seed to 0 so that flattening is repeatable
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_flatten_params(
        input_patch=input_patch,
        output_patch=output_patch,
        iterations=iterations,
        distances=distances,
        dilations=dilations,
        random_seed=random_seed,
        copy_coords=copy_coords,
        norand=norand,
    )
    return mris_flatten_execute(params, runner)

__all__ = [
    "MrisFlatten",
    "MrisFlattenOutputs",
    "MRIS_FLATTEN_METADATA",
    "mris_flatten_cargs",
    "mris_flatten_outputs",
    "mris_flatten_params",
    "mris_flatten_execute",
    "mris_flatten_validate",
    "mris_flatten",
]