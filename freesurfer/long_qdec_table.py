# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_QDEC_TABLE_METADATA = Metadata(
    id="long_qdec_table",
    name="long_qdec_table",
    package="freesurfer",
)

LongQdecTable = typing.TypedDict(
    "LongQdecTable",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_qdec_table"]],
        "qdec_table": InputPathType,
        "split": typing.NotRequired[str],
        "cross_flag": typing.NotRequired[bool],
        "sort": typing.NotRequired[str],
        "out": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LongQdecTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_table: OutputPathType | None
    """Output table file generated by the specified operations"""

def long_qdec_table_params(
    qdec_table: InputPathType,
    split: str | None = None,
    cross_flag: bool = False,
    sort: str | None = None,
    out: str | None = None,
) -> LongQdecTable:
    """
    Build parameters.

    Args:
        qdec_table: QDEC table file specifying the subjects and time points
        split: Split table based on column (e.g., use 'fsid-base' to separate\
            subjects)
        cross_flag: Collapse table to cross sectional (one line per subject)
        sort: Sort table based on column within subject (e.g., 'age')
        out: Output file name for operations producing a single table

    Returns:
        Parameter dictionary.
    """
    params: LongQdecTable = {
        "@type": "freesurfer/long_qdec_table",
        "qdec_table": qdec_table,
        "cross_flag": cross_flag,
    }
    if split is not None:
        params["split"] = split
    if sort is not None:
        params["sort"] = sort
    if out is not None:
        params["out"] = out
    return params

def long_qdec_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongQdecTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec_table", None) is None:
        raise StyxValidationError("`qdec_table` must not be None")
    if not isinstance(params["qdec_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec_table` has the wrong type: Received `{type(params["qdec_table"])}` expected `InputPathType`')
    if params.get("split", None) is not None:
        if not isinstance(params["split"], str):
            raise StyxValidationError(f'`split` has the wrong type: Received `{type(params["split"])}` expected `str`')
    if params.get("cross_flag", None) is not None:
        if not isinstance(params["cross_flag"], bool):
            raise StyxValidationError(f'`cross_flag` has the wrong type: Received `{type(params["cross_flag"])}` expected `bool`')
    if params.get("sort", None) is not None:
        if not isinstance(params["sort"], str):
            raise StyxValidationError(f'`sort` has the wrong type: Received `{type(params["sort"])}` expected `str`')
    if params.get("out", None) is not None:
        if not isinstance(params["out"], str):
            raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def long_qdec_table_cargs(params: LongQdecTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_qdec_table")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec_table"]))
    v_0 = params.get("split")
    if v_0 is not None:
        cargs.append("--split")
        cargs.append(v_0)
    if params.get("cross_flag"):
        cargs.append("--cross")
    v_1 = params.get("sort")
    if v_1 is not None:
        cargs.append("--sort")
        cargs.append(v_1)
    v_2 = params.get("out")
    if v_2 is not None:
        cargs.append("--out")
        cargs.append(v_2)
    return cargs

def long_qdec_table_outputs(params: LongQdecTable, execution: Execution) -> LongQdecTableOutputs:
    """Build the LongQdecTableOutputs object for this tool."""
    output_table_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out")
    if __v0 is not None:
        output_table_v = execution.output_file((__v0 if __v0 is not None else ""))
    return LongQdecTableOutputs(
        root=root_v,
        output_table=output_table_v,
    )

def long_qdec_table_execute(params: LongQdecTable, runner: Runner | None = None) -> LongQdecTableOutputs:
    """
    long_qdec_table

    Tool to operate on longitudinal QDEC tables.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_qdec_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_QDEC_TABLE_METADATA)
    execution.params(params)
    args = long_qdec_table_cargs(params, execution)
    out = long_qdec_table_outputs(params, execution)
    execution.run(args)
    return out

def long_qdec_table(
    qdec_table: InputPathType,
    split: str | None = None,
    cross_flag: bool = False,
    sort: str | None = None,
    out: str | None = None,
    runner: Runner | None = None,
) -> LongQdecTableOutputs:
    """
    long_qdec_table

    Tool to operate on longitudinal QDEC tables.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec_table: QDEC table file specifying the subjects and time points
        split: Split table based on column (e.g., use 'fsid-base' to separate\
            subjects)
        cross_flag: Collapse table to cross sectional (one line per subject)
        sort: Sort table based on column within subject (e.g., 'age')
        out: Output file name for operations producing a single table
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_qdec_table_params(
        qdec_table=qdec_table,
        split=split,
        cross_flag=cross_flag,
        sort=sort,
        out=out,
    )
    return long_qdec_table_execute(params, runner)

__all__ = [
    "LongQdecTable",
    "LongQdecTableOutputs",
    "LONG_QDEC_TABLE_METADATA",
    "long_qdec_table_cargs",
    "long_qdec_table_outputs",
    "long_qdec_table_params",
    "long_qdec_table_execute",
    "long_qdec_table_validate",
    "long_qdec_table",
]