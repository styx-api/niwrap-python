# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_DENOISE_IMAGE_FS_METADATA = Metadata(
    id="AntsDenoiseImageFs",
    name="AntsDenoiseImageFs",
    package="freesurfer",
)

AntsDenoiseImageFs = typing.TypedDict(
    "AntsDenoiseImageFs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/AntsDenoiseImageFs"]],
        "input_image": InputPathType,
        "output_image": typing.NotRequired[str],
        "rician_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsDenoiseImageFsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    denoised_output: OutputPathType
    """Resulting denoised volume file"""

def ants_denoise_image_fs_params(
    input_image: InputPathType,
    output_image: str = "output.nii",
    rician_flag: bool = False,
) -> AntsDenoiseImageFs:
    """
    Build parameters.

    Args:
        input_image: Input volume file
        output_image: Denoised volume file
        rician_flag: Enable Rician noise model (otherwise Gaussian is used)

    Returns:
        Parameter dictionary.
    """
    params: AntsDenoiseImageFs = {
        "@type": "freesurfer/AntsDenoiseImageFs",
        "input_image": input_image,
        "output_image": output_image,
        "rician_flag": rician_flag,
    }
    return params

def ants_denoise_image_fs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsDenoiseImageFs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("rician_flag", None) is not None:
        if not isinstance(params["rician_flag"], bool):
            raise StyxValidationError(f'`rician_flag` has the wrong type: Received `{type(params["rician_flag"])}` expected `bool`')

def ants_denoise_image_fs_cargs(params: AntsDenoiseImageFs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("AntsDenoiseImageFs")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("-o")
    cargs.append(params.get("output_image", "output.nii"))
    if params.get("rician_flag"):
        cargs.append("--rician")
    return cargs

def ants_denoise_image_fs_outputs(params: AntsDenoiseImageFs, execution: Execution) -> AntsDenoiseImageFsOutputs:
    """Build the AntsDenoiseImageFsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    denoised_output_v: OutputPathType = execution.output_file(params.get("output_image", "output.nii"))
    return AntsDenoiseImageFsOutputs(
        root=root_v,
        denoised_output=denoised_output_v,
    )

def ants_denoise_image_fs_execute(params: AntsDenoiseImageFs, runner: Runner | None = None) -> AntsDenoiseImageFsOutputs:
    """
    AntsDenoiseImageFs

    Denoises an image with a spatially adaptive filter. This program wraps the AntsDenoiseImage utility available in the ANTs package.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_denoise_image_fs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_DENOISE_IMAGE_FS_METADATA)
    execution.params(params)
    args = ants_denoise_image_fs_cargs(params, execution)
    out = ants_denoise_image_fs_outputs(params, execution)
    execution.run(args)
    return out

def ants_denoise_image_fs(
    input_image: InputPathType,
    output_image: str = "output.nii",
    rician_flag: bool = False,
    runner: Runner | None = None,
) -> AntsDenoiseImageFsOutputs:
    """
    AntsDenoiseImageFs

    Denoises an image with a spatially adaptive filter. This program wraps the AntsDenoiseImage utility available in the ANTs package.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input volume file
        output_image: Denoised volume file
        rician_flag: Enable Rician noise model (otherwise Gaussian is used)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_denoise_image_fs_params(
        input_image=input_image,
        output_image=output_image,
        rician_flag=rician_flag,
    )
    return ants_denoise_image_fs_execute(params, runner)

__all__ = [
    "AntsDenoiseImageFs",
    "AntsDenoiseImageFsOutputs",
    "ANTS_DENOISE_IMAGE_FS_METADATA",
    "ants_denoise_image_fs_cargs",
    "ants_denoise_image_fs_outputs",
    "ants_denoise_image_fs_params",
    "ants_denoise_image_fs_execute",
    "ants_denoise_image_fs_validate",
    "ants_denoise_image_fs",
]