# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_SUBJECT_LH_METADATA = Metadata(
    id="morph_subject-lh",
    name="morph_subject-lh",
    package="freesurfer",
)

MorphSubjectLh = typing.TypedDict(
    "MorphSubjectLh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/morph_subject-lh"]],
        "subject_id": str,
    },
)

@dataclasses.dataclass
class MorphSubjectLhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def morph_subject_lh_params(
    subject_id: str,
) -> MorphSubjectLh:
    """
    Build parameters.

    Args:
        subject_id:

    Returns:
        Parameter dictionary.
    """
    params: MorphSubjectLh = {
        "@type": "freesurfer/morph_subject-lh",
        "subject_id": subject_id,
    }
    return params

def morph_subject_lh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphSubjectLh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')

def morph_subject_lh_cargs(params: MorphSubjectLh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_subject-lh")
    cargs.append("-lh")
    cargs.append(params["subject_id"])
    return cargs

def morph_subject_lh_outputs(params: MorphSubjectLh, execution: Execution) -> MorphSubjectLhOutputs:
    """Build the MorphSubjectLhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MorphSubjectLhOutputs(
        root=root_v,
    )

def morph_subject_lh_execute(params: MorphSubjectLh, runner: Runner | None = None) -> MorphSubjectLhOutputs:
    """
    morph_subject-lh

    A tool for morphing subject's left hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_subject_lh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_SUBJECT_LH_METADATA)
    execution.params(params)
    args = morph_subject_lh_cargs(params, execution)
    out = morph_subject_lh_outputs(params, execution)
    execution.run(args)
    return out

def morph_subject_lh(
    subject_id: str,
    runner: Runner | None = None,
) -> MorphSubjectLhOutputs:
    """
    morph_subject-lh

    A tool for morphing subject's left hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_subject_lh_params(
        subject_id=subject_id,
    )
    return morph_subject_lh_execute(params, runner)

__all__ = [
    "MorphSubjectLh",
    "MorphSubjectLhOutputs",
    "MORPH_SUBJECT_LH_METADATA",
    "morph_subject_lh_cargs",
    "morph_subject_lh_outputs",
    "morph_subject_lh_params",
    "morph_subject_lh_execute",
    "morph_subject_lh_validate",
    "morph_subject_lh",
]