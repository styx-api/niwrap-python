# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IREPIFITVOL_METADATA = Metadata(
    id="irepifitvol",
    name="irepifitvol",
    package="freesurfer",
)

Irepifitvol = typing.TypedDict(
    "Irepifitvol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/irepifitvol"]],
        "input_file": InputPathType,
        "output_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class IrepifitvolOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fitted_output: OutputPathType
    """The resulting volume file after fitting"""

def irepifitvol_params(
    input_file: InputPathType,
    output_file: str = "fitted_output",
) -> Irepifitvol:
    """
    Build parameters.

    Args:
        input_file: Input volume file for fitting
        output_file: Output volume file after fitting

    Returns:
        Parameter dictionary.
    """
    params: Irepifitvol = {
        "@type": "freesurfer/irepifitvol",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def irepifitvol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Irepifitvol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def irepifitvol_cargs(params: Irepifitvol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("irepifitvol")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params.get("output_file", "fitted_output"))
    return cargs

def irepifitvol_outputs(params: Irepifitvol, execution: Execution) -> IrepifitvolOutputs:
    """Build the IrepifitvolOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fitted_output_v: OutputPathType = execution.output_file(f'{params.get("output_file", "fitted_output")}.ext')
    return IrepifitvolOutputs(
        root=root_v,
        fitted_output=fitted_output_v,
    )

def irepifitvol_execute(params: Irepifitvol, runner: Runner | None = None) -> IrepifitvolOutputs:
    """
    irepifitvol

    A tool for 3D volume fitting

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    irepifitvol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IREPIFITVOL_METADATA)
    execution.params(params)
    args = irepifitvol_cargs(params, execution)
    out = irepifitvol_outputs(params, execution)
    execution.run(args)
    return out

def irepifitvol(
    input_file: InputPathType,
    output_file: str = "fitted_output",
    runner: Runner | None = None,
) -> IrepifitvolOutputs:
    """
    irepifitvol

    A tool for 3D volume fitting

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input volume file for fitting
        output_file: Output volume file after fitting
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = irepifitvol_params(
        input_file=input_file,
        output_file=output_file,
    )
    return irepifitvol_execute(params, runner)

__all__ = [
    "Irepifitvol",
    "IrepifitvolOutputs",
    "IREPIFITVOL_METADATA",
    "irepifitvol_cargs",
    "irepifitvol_outputs",
    "irepifitvol_params",
    "irepifitvol_execute",
    "irepifitvol_validate",
    "irepifitvol",
]