# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_TWOCLASS_METADATA = Metadata(
    id="mri_twoclass",
    name="mri_twoclass",
    package="freesurfer",
)

MriTwoclass = typing.TypedDict(
    "MriTwoclass",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_twoclass"]],
        "segmentation_volume": InputPathType,
        "output_subject": str,
        "output_volume": str,
        "c1_subjects": list[str],
        "c2_subjects": list[str],
        "f_threshold": typing.NotRequired[float],
        "bonferroni_correction": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriTwoclassOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_volume: OutputPathType
    """Output volume storing the results"""

def mri_twoclass_params(
    segmentation_volume: InputPathType,
    output_subject: str,
    output_volume: str,
    c1_subjects: list[str],
    c2_subjects: list[str],
    f_threshold: float | None = None,
    bonferroni_correction: bool = False,
) -> MriTwoclass:
    """
    Build parameters.

    Args:
        segmentation_volume: Input segmentation volume
        output_subject: Output subject name
        output_volume: Output volume
        c1_subjects: List of subjects from class 1
        c2_subjects: List of subjects from class 2
        f_threshold: Specify F threshold
        bonferroni_correction: Perform Bonferroni correction

    Returns:
        Parameter dictionary.
    """
    params: MriTwoclass = {
        "@type": "freesurfer/mri_twoclass",
        "segmentation_volume": segmentation_volume,
        "output_subject": output_subject,
        "output_volume": output_volume,
        "c1_subjects": c1_subjects,
        "c2_subjects": c2_subjects,
        "bonferroni_correction": bonferroni_correction,
    }
    if f_threshold is not None:
        params["f_threshold"] = f_threshold
    return params

def mri_twoclass_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriTwoclass."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("segmentation_volume", None) is None:
        raise StyxValidationError("`segmentation_volume` must not be None")
    if not isinstance(params["segmentation_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segmentation_volume` has the wrong type: Received `{type(params["segmentation_volume"])}` expected `InputPathType`')
    if params.get("output_subject", None) is None:
        raise StyxValidationError("`output_subject` must not be None")
    if not isinstance(params["output_subject"], str):
        raise StyxValidationError(f'`output_subject` has the wrong type: Received `{type(params["output_subject"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("c1_subjects", None) is None:
        raise StyxValidationError("`c1_subjects` must not be None")
    if not isinstance(params["c1_subjects"], list):
        raise StyxValidationError(f'`c1_subjects` has the wrong type: Received `{type(params["c1_subjects"])}` expected `list[str]`')
    for e in params["c1_subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`c1_subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("c2_subjects", None) is None:
        raise StyxValidationError("`c2_subjects` must not be None")
    if not isinstance(params["c2_subjects"], list):
        raise StyxValidationError(f'`c2_subjects` has the wrong type: Received `{type(params["c2_subjects"])}` expected `list[str]`')
    for e_2 in params["c2_subjects"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`c2_subjects` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("f_threshold", None) is not None:
        if not isinstance(params["f_threshold"], (float, int)):
            raise StyxValidationError(f'`f_threshold` has the wrong type: Received `{type(params["f_threshold"])}` expected `float`')
    if params.get("bonferroni_correction", None) is not None:
        if not isinstance(params["bonferroni_correction"], bool):
            raise StyxValidationError(f'`bonferroni_correction` has the wrong type: Received `{type(params["bonferroni_correction"])}` expected `bool`')

def mri_twoclass_cargs(params: MriTwoclass, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_twoclass")
    cargs.append(execution.input_file(params["segmentation_volume"]))
    cargs.append(params["output_subject"])
    cargs.append(params["output_volume"])
    for item0 in params["c1_subjects"]:
        cargs.append(item0)
    for item1 in params["c2_subjects"]:
        cargs.append(item1)
    v_0 = params.get("f_threshold")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(str(v_0))
    if params.get("bonferroni_correction"):
        cargs.append("-b")
    return cargs

def mri_twoclass_outputs(params: MriTwoclass, execution: Execution) -> MriTwoclassOutputs:
    """Build the MriTwoclassOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_volume_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriTwoclassOutputs(
        root=root_v,
        result_volume=result_volume_v,
    )

def mri_twoclass_execute(params: MriTwoclass, runner: Runner | None = None) -> MriTwoclassOutputs:
    """
    mri_twoclass

    Compute cross-subject statistics of two sets of labels

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_twoclass_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_TWOCLASS_METADATA)
    execution.params(params)
    args = mri_twoclass_cargs(params, execution)
    out = mri_twoclass_outputs(params, execution)
    execution.run(args)
    return out

def mri_twoclass(
    segmentation_volume: InputPathType,
    output_subject: str,
    output_volume: str,
    c1_subjects: list[str],
    c2_subjects: list[str],
    f_threshold: float | None = None,
    bonferroni_correction: bool = False,
    runner: Runner | None = None,
) -> MriTwoclassOutputs:
    """
    mri_twoclass

    Compute cross-subject statistics of two sets of labels

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        segmentation_volume: Input segmentation volume
        output_subject: Output subject name
        output_volume: Output volume
        c1_subjects: List of subjects from class 1
        c2_subjects: List of subjects from class 2
        f_threshold: Specify F threshold
        bonferroni_correction: Perform Bonferroni correction
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_twoclass_params(
        segmentation_volume=segmentation_volume,
        output_subject=output_subject,
        output_volume=output_volume,
        c1_subjects=c1_subjects,
        c2_subjects=c2_subjects,
        f_threshold=f_threshold,
        bonferroni_correction=bonferroni_correction,
    )
    return mri_twoclass_execute(params, runner)

__all__ = [
    "MriTwoclass",
    "MriTwoclassOutputs",
    "MRI_TWOCLASS_METADATA",
    "mri_twoclass_cargs",
    "mri_twoclass_outputs",
    "mri_twoclass_params",
    "mri_twoclass_execute",
    "mri_twoclass_validate",
    "mri_twoclass",
]