# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_NEIGHBORING_REGIONS_METADATA = Metadata(
    id="dmri_neighboringRegions",
    name="dmri_neighboringRegions",
    package="freesurfer",
)

DmriNeighboringRegions = typing.TypedDict(
    "DmriNeighboringRegions",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_neighboringRegions"]],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class DmriNeighboringRegionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_file: OutputPathType
    """Resulting output file from dmri_neighboringRegions"""

def dmri_neighboring_regions_params(
    input_file: InputPathType,
    output_file: str,
) -> DmriNeighboringRegions:
    """
    Build parameters.

    Args:
        input_file: Input image file (e.g. img.nii.gz)
        output_file: Output result file (e.g. result.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: DmriNeighboringRegions = {
        "@type": "freesurfer/dmri_neighboringRegions",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def dmri_neighboring_regions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriNeighboringRegions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def dmri_neighboring_regions_cargs(params: DmriNeighboringRegions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_neighboringRegions")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def dmri_neighboring_regions_outputs(params: DmriNeighboringRegions, execution: Execution) -> DmriNeighboringRegionsOutputs:
    """Build the DmriNeighboringRegionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_file_v: OutputPathType = execution.output_file(params["output_file"])
    return DmriNeighboringRegionsOutputs(
        root=root_v,
        result_file=result_file_v,
    )

def dmri_neighboring_regions_execute(params: DmriNeighboringRegions, runner: Runner | None = None) -> DmriNeighboringRegionsOutputs:
    """
    dmri_neighboringRegions

    A tool for diffusion MRI analysis involving neighboring regions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_neighboring_regions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_NEIGHBORING_REGIONS_METADATA)
    execution.params(params)
    args = dmri_neighboring_regions_cargs(params, execution)
    out = dmri_neighboring_regions_outputs(params, execution)
    execution.run(args)
    return out

def dmri_neighboring_regions(
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> DmriNeighboringRegionsOutputs:
    """
    dmri_neighboringRegions

    A tool for diffusion MRI analysis involving neighboring regions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input image file (e.g. img.nii.gz)
        output_file: Output result file (e.g. result.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_neighboring_regions_params(
        input_file=input_file,
        output_file=output_file,
    )
    return dmri_neighboring_regions_execute(params, runner)

__all__ = [
    "DmriNeighboringRegions",
    "DmriNeighboringRegionsOutputs",
    "DMRI_NEIGHBORING_REGIONS_METADATA",
    "dmri_neighboring_regions_cargs",
    "dmri_neighboring_regions_outputs",
    "dmri_neighboring_regions_params",
    "dmri_neighboring_regions_execute",
    "dmri_neighboring_regions_validate",
    "dmri_neighboring_regions",
]