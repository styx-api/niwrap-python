# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

T4IMG_4DFP_METADATA = Metadata(
    id="t4img_4dfp",
    name="t4img_4dfp",
    package="freesurfer",
)

T4img4dfp = typing.TypedDict(
    "T4img4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/t4img_4dfp"]],
        "t4file": InputPathType,
        "imgfile": InputPathType,
        "outfile": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class T4img4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_image: OutputPathType | None
    """The transformed output image file in 4dfp format."""

def t4img_4dfp_params(
    t4file: InputPathType,
    imgfile: InputPathType,
    outfile: str | None = None,
) -> T4img4dfp:
    """
    Build parameters.

    Args:
        t4file: Transformation matrix file (t4 file format).
        imgfile: Input image file (4dfp format).
        outfile: Output image file (optional, defaults to <imgfile>t if not\
            provided).

    Returns:
        Parameter dictionary.
    """
    params: T4img4dfp = {
        "@type": "freesurfer/t4img_4dfp",
        "t4file": t4file,
        "imgfile": imgfile,
    }
    if outfile is not None:
        params["outfile"] = outfile
    return params

def t4img_4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid T4img4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("t4file", None) is None:
        raise StyxValidationError("`t4file` must not be None")
    if not isinstance(params["t4file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`t4file` has the wrong type: Received `{type(params["t4file"])}` expected `InputPathType`')
    if params.get("imgfile", None) is None:
        raise StyxValidationError("`imgfile` must not be None")
    if not isinstance(params["imgfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`imgfile` has the wrong type: Received `{type(params["imgfile"])}` expected `InputPathType`')
    if params.get("outfile", None) is not None:
        if not isinstance(params["outfile"], str):
            raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def t4img_4dfp_cargs(params: T4img4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("t4img_4dfp")
    cargs.append(execution.input_file(params["t4file"]))
    cargs.append(execution.input_file(params["imgfile"]))
    v_0 = params.get("outfile")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def t4img_4dfp_outputs(params: T4img4dfp, execution: Execution) -> T4img4dfpOutputs:
    """Build the T4img4dfpOutputs object for this tool."""
    transformed_image_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outfile")
    if __v0 is not None:
        transformed_image_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.4dfp.img')
    return T4img4dfpOutputs(
        root=root_v,
        transformed_image=transformed_image_v,
    )

def t4img_4dfp_execute(params: T4img4dfp, runner: Runner | None = None) -> T4img4dfpOutputs:
    """
    t4img_4dfp

    Transforms a 4dfp image using a specified t4 file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    t4img_4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(T4IMG_4DFP_METADATA)
    execution.params(params)
    args = t4img_4dfp_cargs(params, execution)
    out = t4img_4dfp_outputs(params, execution)
    execution.run(args)
    return out

def t4img_4dfp(
    t4file: InputPathType,
    imgfile: InputPathType,
    outfile: str | None = None,
    runner: Runner | None = None,
) -> T4img4dfpOutputs:
    """
    t4img_4dfp

    Transforms a 4dfp image using a specified t4 file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        t4file: Transformation matrix file (t4 file format).
        imgfile: Input image file (4dfp format).
        outfile: Output image file (optional, defaults to <imgfile>t if not\
            provided).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = t4img_4dfp_params(
        t4file=t4file,
        imgfile=imgfile,
        outfile=outfile,
    )
    return t4img_4dfp_execute(params, runner)

__all__ = [
    "T4img4dfp",
    "T4img4dfpOutputs",
    "T4IMG_4DFP_METADATA",
    "t4img_4dfp_cargs",
    "t4img_4dfp_outputs",
    "t4img_4dfp_params",
    "t4img_4dfp_execute",
    "t4img_4dfp_validate",
    "t4img_4dfp",
]