# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_TUTORIAL_DATA_METADATA = Metadata(
    id="fs_tutorial_data",
    name="fs_tutorial_data",
    package="freesurfer",
)

FsTutorialData = typing.TypedDict(
    "FsTutorialData",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_tutorial_data"]],
        "rsync_options": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class FsTutorialDataOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tutorial_data_dir: OutputPathType
    """Directory where tutorial data will be downloaded and installed."""

def fs_tutorial_data_params(
    rsync_options: list[str] | None = None,
) -> FsTutorialData:
    """
    Build parameters.

    Args:
        rsync_options: Additional rsync options for downloading tutorial data.

    Returns:
        Parameter dictionary.
    """
    params: FsTutorialData = {
        "@type": "freesurfer/fs_tutorial_data",
    }
    if rsync_options is not None:
        params["rsync_options"] = rsync_options
    return params

def fs_tutorial_data_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsTutorialData."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("rsync_options", None) is not None:
        if not isinstance(params["rsync_options"], list):
            raise StyxValidationError(f'`rsync_options` has the wrong type: Received `{type(params["rsync_options"])}` expected `list[str]`')
        for e in params["rsync_options"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`rsync_options` has the wrong type: Received `{type(e)}` expected `list[str]`')

def fs_tutorial_data_cargs(params: FsTutorialData, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_tutorial_data")
    v_0 = params.get("rsync_options")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    return cargs

def fs_tutorial_data_outputs(params: FsTutorialData, execution: Execution) -> FsTutorialDataOutputs:
    """Build the FsTutorialDataOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tutorial_data_dir_v: OutputPathType = execution.output_file("/usr/local/freesurfer/subjects/tutorial_data")
    return FsTutorialDataOutputs(
        root=root_v,
        tutorial_data_dir=tutorial_data_dir_v,
    )

def fs_tutorial_data_execute(params: FsTutorialData, runner: Runner | None = None) -> FsTutorialDataOutputs:
    """
    fs_tutorial_data

    Tool to download and install FreeSurfer tutorial data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_tutorial_data_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_TUTORIAL_DATA_METADATA)
    execution.params(params)
    args = fs_tutorial_data_cargs(params, execution)
    out = fs_tutorial_data_outputs(params, execution)
    execution.run(args)
    return out

def fs_tutorial_data(
    rsync_options: list[str] | None = None,
    runner: Runner | None = None,
) -> FsTutorialDataOutputs:
    """
    fs_tutorial_data

    Tool to download and install FreeSurfer tutorial data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        rsync_options: Additional rsync options for downloading tutorial data.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_tutorial_data_params(
        rsync_options=rsync_options,
    )
    return fs_tutorial_data_execute(params, runner)

__all__ = [
    "FsTutorialData",
    "FsTutorialDataOutputs",
    "FS_TUTORIAL_DATA_METADATA",
    "fs_tutorial_data_cargs",
    "fs_tutorial_data_outputs",
    "fs_tutorial_data_params",
    "fs_tutorial_data_execute",
    "fs_tutorial_data_validate",
    "fs_tutorial_data",
]