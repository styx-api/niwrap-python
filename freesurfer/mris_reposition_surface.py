# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_REPOSITION_SURFACE_METADATA = Metadata(
    id="mris_reposition_surface",
    name="mris_reposition_surface",
    package="freesurfer",
)

MrisRepositionSurface = typing.TypedDict(
    "MrisRepositionSurface",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_reposition_surface"]],
        "surf": InputPathType,
        "volume": InputPathType,
        "points": InputPathType,
        "output": str,
        "size": typing.NotRequired[float],
        "sigma": typing.NotRequired[float],
        "iterations": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisRepositionSurfaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType
    """Output surface file"""

def mris_reposition_surface_params(
    surf: InputPathType,
    volume: InputPathType,
    points: InputPathType,
    output: str,
    size: float | None = None,
    sigma: float | None = None,
    iterations: float | None = None,
) -> MrisRepositionSurface:
    """
    Build parameters.

    Args:
        surf: Input surface
        volume: Input volume
        points: Input points
        output: Output surface filename
        size: Size parameter for repositioning. Default is 1.
        sigma: Sigma. Default is 2.0.
        iterations: Number of iterations. Default is 1.

    Returns:
        Parameter dictionary.
    """
    params: MrisRepositionSurface = {
        "@type": "freesurfer/mris_reposition_surface",
        "surf": surf,
        "volume": volume,
        "points": points,
        "output": output,
    }
    if size is not None:
        params["size"] = size
    if sigma is not None:
        params["sigma"] = sigma
    if iterations is not None:
        params["iterations"] = iterations
    return params

def mris_reposition_surface_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRepositionSurface."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surf", None) is None:
        raise StyxValidationError("`surf` must not be None")
    if not isinstance(params["surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `InputPathType`')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("points", None) is None:
        raise StyxValidationError("`points` must not be None")
    if not isinstance(params["points"], (pathlib.Path, str)):
        raise StyxValidationError(f'`points` has the wrong type: Received `{type(params["points"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("size", None) is not None:
        if not isinstance(params["size"], (float, int)):
            raise StyxValidationError(f'`size` has the wrong type: Received `{type(params["size"])}` expected `float`')
    if params.get("sigma", None) is not None:
        if not isinstance(params["sigma"], (float, int)):
            raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], (float, int)):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `float`')

def mris_reposition_surface_cargs(params: MrisRepositionSurface, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_reposition_surface")
    cargs.append("-s")
    cargs.append(execution.input_file(params["surf"]))
    cargs.append("-v")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append("-p")
    cargs.append(execution.input_file(params["points"]))
    cargs.append("-o")
    cargs.append(params["output"])
    v_0 = params.get("size")
    if v_0 is not None:
        cargs.append("-z")
        cargs.append(str(v_0))
    v_1 = params.get("sigma")
    if v_1 is not None:
        cargs.append("-g")
        cargs.append(str(v_1))
    v_2 = params.get("iterations")
    if v_2 is not None:
        cargs.append("-i")
        cargs.append(str(v_2))
    return cargs

def mris_reposition_surface_outputs(params: MrisRepositionSurface, execution: Execution) -> MrisRepositionSurfaceOutputs:
    """Build the MrisRepositionSurfaceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_v: OutputPathType = execution.output_file(params["output"])
    return MrisRepositionSurfaceOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def mris_reposition_surface_execute(params: MrisRepositionSurface, runner: Runner | None = None) -> MrisRepositionSurfaceOutputs:
    """
    mris_reposition_surface

    Reposition a surface based on the given control points (in JSON format)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_reposition_surface_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_REPOSITION_SURFACE_METADATA)
    execution.params(params)
    args = mris_reposition_surface_cargs(params, execution)
    out = mris_reposition_surface_outputs(params, execution)
    execution.run(args)
    return out

def mris_reposition_surface(
    surf: InputPathType,
    volume: InputPathType,
    points: InputPathType,
    output: str,
    size: float | None = None,
    sigma: float | None = None,
    iterations: float | None = None,
    runner: Runner | None = None,
) -> MrisRepositionSurfaceOutputs:
    """
    mris_reposition_surface

    Reposition a surface based on the given control points (in JSON format)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surf: Input surface
        volume: Input volume
        points: Input points
        output: Output surface filename
        size: Size parameter for repositioning. Default is 1.
        sigma: Sigma. Default is 2.0.
        iterations: Number of iterations. Default is 1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_reposition_surface_params(
        surf=surf,
        volume=volume,
        points=points,
        output=output,
        size=size,
        sigma=sigma,
        iterations=iterations,
    )
    return mris_reposition_surface_execute(params, runner)

__all__ = [
    "MrisRepositionSurface",
    "MrisRepositionSurfaceOutputs",
    "MRIS_REPOSITION_SURFACE_METADATA",
    "mris_reposition_surface_cargs",
    "mris_reposition_surface_outputs",
    "mris_reposition_surface_params",
    "mris_reposition_surface_execute",
    "mris_reposition_surface_validate",
    "mris_reposition_surface",
]