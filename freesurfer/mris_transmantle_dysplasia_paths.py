# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_TRANSMANTLE_DYSPLASIA_PATHS_METADATA = Metadata(
    id="mris_transmantle_dysplasia_paths",
    name="mris_transmantle_dysplasia_paths",
    package="freesurfer",
)

MrisTransmantleDysplasiaPaths = typing.TypedDict(
    "MrisTransmantleDysplasiaPaths",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_transmantle_dysplasia_paths"]],
        "surface": InputPathType,
        "aseg_volume": InputPathType,
        "intensity_volume": InputPathType,
        "xform": InputPathType,
        "output_volume": str,
        "filter": typing.NotRequired[list[float]],
        "noise_sensitivity": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisTransmantleDysplasiaPathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume file"""

def mris_transmantle_dysplasia_paths_params(
    surface: InputPathType,
    aseg_volume: InputPathType,
    intensity_volume: InputPathType,
    xform: InputPathType,
    output_volume: str,
    filter: list[float] | None = None,
    noise_sensitivity: bool = False,
) -> MrisTransmantleDysplasiaPaths:
    """
    Build parameters.

    Args:
        surface: Surface file
        aseg_volume: ASEG volume file
        intensity_volume: Intensity volume file
        xform: Transformation file
        output_volume: Output volume file
        filter: Apply specified filter with low and high values (not implemented\
            yet)
        noise_sensitivity: Noise-sensitivity normalize inverse (default=1)

    Returns:
        Parameter dictionary.
    """
    params: MrisTransmantleDysplasiaPaths = {
        "@type": "freesurfer/mris_transmantle_dysplasia_paths",
        "surface": surface,
        "aseg_volume": aseg_volume,
        "intensity_volume": intensity_volume,
        "xform": xform,
        "output_volume": output_volume,
        "noise_sensitivity": noise_sensitivity,
    }
    if filter is not None:
        params["filter"] = filter
    return params

def mris_transmantle_dysplasia_paths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisTransmantleDysplasiaPaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("aseg_volume", None) is None:
        raise StyxValidationError("`aseg_volume` must not be None")
    if not isinstance(params["aseg_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`aseg_volume` has the wrong type: Received `{type(params["aseg_volume"])}` expected `InputPathType`')
    if params.get("intensity_volume", None) is None:
        raise StyxValidationError("`intensity_volume` must not be None")
    if not isinstance(params["intensity_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`intensity_volume` has the wrong type: Received `{type(params["intensity_volume"])}` expected `InputPathType`')
    if params.get("xform", None) is None:
        raise StyxValidationError("`xform` must not be None")
    if not isinstance(params["xform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("filter", None) is not None:
        if not isinstance(params["filter"], list):
            raise StyxValidationError(f'`filter` has the wrong type: Received `{type(params["filter"])}` expected `list[float]`')
        if not (2 <= len(params["filter"]) <= 2):
            raise StyxValidationError("Parameter `filter` must contain between 2 and 2 elements (inclusive)")
        for e in params["filter"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`filter` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("noise_sensitivity", None) is not None:
        if not isinstance(params["noise_sensitivity"], bool):
            raise StyxValidationError(f'`noise_sensitivity` has the wrong type: Received `{type(params["noise_sensitivity"])}` expected `bool`')

def mris_transmantle_dysplasia_paths_cargs(params: MrisTransmantleDysplasiaPaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_transmantle_dysplasia_paths")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["aseg_volume"]))
    cargs.append(execution.input_file(params["intensity_volume"]))
    cargs.append(execution.input_file(params["xform"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("filter")
    if v_0 is not None:
        cargs.append("-f")
        for item0 in v_0:
            cargs.append(str(item0))
    if params.get("noise_sensitivity"):
        cargs.append("-n")
    return cargs

def mris_transmantle_dysplasia_paths_outputs(params: MrisTransmantleDysplasiaPaths, execution: Execution) -> MrisTransmantleDysplasiaPathsOutputs:
    """Build the MrisTransmantleDysplasiaPathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MrisTransmantleDysplasiaPathsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mris_transmantle_dysplasia_paths_execute(params: MrisTransmantleDysplasiaPaths, runner: Runner | None = None) -> MrisTransmantleDysplasiaPathsOutputs:
    """
    mris_transmantle_dysplasia_paths

    Tool for transmantle dysplasia path computation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_transmantle_dysplasia_paths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_TRANSMANTLE_DYSPLASIA_PATHS_METADATA)
    execution.params(params)
    args = mris_transmantle_dysplasia_paths_cargs(params, execution)
    out = mris_transmantle_dysplasia_paths_outputs(params, execution)
    execution.run(args)
    return out

def mris_transmantle_dysplasia_paths(
    surface: InputPathType,
    aseg_volume: InputPathType,
    intensity_volume: InputPathType,
    xform: InputPathType,
    output_volume: str,
    filter: list[float] | None = None,
    noise_sensitivity: bool = False,
    runner: Runner | None = None,
) -> MrisTransmantleDysplasiaPathsOutputs:
    """
    mris_transmantle_dysplasia_paths

    Tool for transmantle dysplasia path computation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Surface file
        aseg_volume: ASEG volume file
        intensity_volume: Intensity volume file
        xform: Transformation file
        output_volume: Output volume file
        filter: Apply specified filter with low and high values (not implemented\
            yet)
        noise_sensitivity: Noise-sensitivity normalize inverse (default=1)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_transmantle_dysplasia_paths_params(
        surface=surface,
        aseg_volume=aseg_volume,
        intensity_volume=intensity_volume,
        xform=xform,
        output_volume=output_volume,
        filter=filter,
        noise_sensitivity=noise_sensitivity,
    )
    return mris_transmantle_dysplasia_paths_execute(params, runner)

__all__ = [
    "MrisTransmantleDysplasiaPaths",
    "MrisTransmantleDysplasiaPathsOutputs",
    "MRIS_TRANSMANTLE_DYSPLASIA_PATHS_METADATA",
    "mris_transmantle_dysplasia_paths_cargs",
    "mris_transmantle_dysplasia_paths_outputs",
    "mris_transmantle_dysplasia_paths_params",
    "mris_transmantle_dysplasia_paths_execute",
    "mris_transmantle_dysplasia_paths_validate",
    "mris_transmantle_dysplasia_paths",
]