# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_STRIP_NONWHITE_METADATA = Metadata(
    id="mri_strip_nonwhite",
    name="mri_strip_nonwhite",
    package="freesurfer",
)

MriStripNonwhite = typing.TypedDict(
    "MriStripNonwhite",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_strip_nonwhite"]],
        "input_volume": InputPathType,
        "transform": InputPathType,
        "template_volume": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriStripNonwhiteOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Processed output volume file"""

def mri_strip_nonwhite_params(
    input_volume: InputPathType,
    transform: InputPathType,
    template_volume: InputPathType,
    output_volume: str,
) -> MriStripNonwhite:
    """
    Build parameters.

    Args:
        input_volume: Input MRI volume file
        transform: Transformation file to be applied to the input volume
        template_volume: Template volume file used in conjunction with the\
            transform
        output_volume: Name of the output volume file

    Returns:
        Parameter dictionary.
    """
    params: MriStripNonwhite = {
        "@type": "freesurfer/mri_strip_nonwhite",
        "input_volume": input_volume,
        "transform": transform,
        "template_volume": template_volume,
        "output_volume": output_volume,
    }
    return params

def mri_strip_nonwhite_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriStripNonwhite."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("template_volume", None) is None:
        raise StyxValidationError("`template_volume` must not be None")
    if not isinstance(params["template_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_volume` has the wrong type: Received `{type(params["template_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_strip_nonwhite_cargs(params: MriStripNonwhite, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_strip_nonwhite")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(execution.input_file(params["template_volume"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_strip_nonwhite_outputs(params: MriStripNonwhite, execution: Execution) -> MriStripNonwhiteOutputs:
    """Build the MriStripNonwhiteOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriStripNonwhiteOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_strip_nonwhite_execute(params: MriStripNonwhite, runner: Runner | None = None) -> MriStripNonwhiteOutputs:
    """
    mri_strip_nonwhite

    Tool for processing MRI images, transforming volumetric data using provided transformation and template files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_strip_nonwhite_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_STRIP_NONWHITE_METADATA)
    execution.params(params)
    args = mri_strip_nonwhite_cargs(params, execution)
    out = mri_strip_nonwhite_outputs(params, execution)
    execution.run(args)
    return out

def mri_strip_nonwhite(
    input_volume: InputPathType,
    transform: InputPathType,
    template_volume: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriStripNonwhiteOutputs:
    """
    mri_strip_nonwhite

    Tool for processing MRI images, transforming volumetric data using provided transformation and template files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input MRI volume file
        transform: Transformation file to be applied to the input volume
        template_volume: Template volume file used in conjunction with the\
            transform
        output_volume: Name of the output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_strip_nonwhite_params(
        input_volume=input_volume,
        transform=transform,
        template_volume=template_volume,
        output_volume=output_volume,
    )
    return mri_strip_nonwhite_execute(params, runner)

__all__ = [
    "MriStripNonwhite",
    "MriStripNonwhiteOutputs",
    "MRI_STRIP_NONWHITE_METADATA",
    "mri_strip_nonwhite_cargs",
    "mri_strip_nonwhite_outputs",
    "mri_strip_nonwhite_params",
    "mri_strip_nonwhite_execute",
    "mri_strip_nonwhite_validate",
    "mri_strip_nonwhite",
]