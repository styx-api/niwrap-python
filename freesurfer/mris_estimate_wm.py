# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ESTIMATE_WM_METADATA = Metadata(
    id="mris_estimate_wm",
    name="mris_estimate_wm",
    package="freesurfer",
)

MrisEstimateWm = typing.TypedDict(
    "MrisEstimateWm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_estimate_wm"]],
        "subjs": list[str],
        "hemi": str,
        "sdir": typing.NotRequired[str],
        "model": typing.NotRequired[str],
        "suffix": typing.NotRequired[str],
        "gpu": typing.NotRequired[bool],
        "rsi": typing.NotRequired[bool],
        "single_iter": typing.NotRequired[bool],
        "vol": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisEstimateWmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_estimate_wm_params(
    subjs: list[str],
    hemi: str,
    sdir: str | None = None,
    model: str | None = None,
    suffix: str | None = None,
    gpu: bool = False,
    rsi: bool = False,
    single_iter: bool = False,
    vol: str | None = None,
) -> MrisEstimateWm:
    """
    Build parameters.

    Args:
        subjs: List of subjects to process.
        hemi: Hemisphere to reconstruct (lh or rh).
        sdir: Override SUBJECTS_DIR.
        model: Override default model.
        suffix: Suffix of output surface (default is 'topofit').
        gpu: Use the GPU.
        rsi: Remove self-intersecting faces during the deformation.
        single_iter: Prevent deformation steps from running more than once.
        vol: Subject volume to use as input.

    Returns:
        Parameter dictionary.
    """
    params: MrisEstimateWm = {
        "@type": "freesurfer/mris_estimate_wm",
        "subjs": subjs,
        "hemi": hemi,
        "gpu": gpu,
        "rsi": rsi,
        "single_iter": single_iter,
    }
    if sdir is not None:
        params["sdir"] = sdir
    if model is not None:
        params["model"] = model
    if suffix is not None:
        params["suffix"] = suffix
    if vol is not None:
        params["vol"] = vol
    return params

def mris_estimate_wm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisEstimateWm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjs", None) is None:
        raise StyxValidationError("`subjs` must not be None")
    if not isinstance(params["subjs"], list):
        raise StyxValidationError(f'`subjs` has the wrong type: Received `{type(params["subjs"])}` expected `list[str]`')
    for e in params["subjs"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjs` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("sdir", None) is not None:
        if not isinstance(params["sdir"], str):
            raise StyxValidationError(f'`sdir` has the wrong type: Received `{type(params["sdir"])}` expected `str`')
    if params.get("model", None) is not None:
        if not isinstance(params["model"], str):
            raise StyxValidationError(f'`model` has the wrong type: Received `{type(params["model"])}` expected `str`')
    if params.get("suffix", None) is not None:
        if not isinstance(params["suffix"], str):
            raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')
    if params.get("gpu", None) is not None:
        if not isinstance(params["gpu"], bool):
            raise StyxValidationError(f'`gpu` has the wrong type: Received `{type(params["gpu"])}` expected `bool`')
    if params.get("rsi", None) is not None:
        if not isinstance(params["rsi"], bool):
            raise StyxValidationError(f'`rsi` has the wrong type: Received `{type(params["rsi"])}` expected `bool`')
    if params.get("single_iter", None) is not None:
        if not isinstance(params["single_iter"], bool):
            raise StyxValidationError(f'`single_iter` has the wrong type: Received `{type(params["single_iter"])}` expected `bool`')
    if params.get("vol", None) is not None:
        if not isinstance(params["vol"], str):
            raise StyxValidationError(f'`vol` has the wrong type: Received `{type(params["vol"])}` expected `str`')

def mris_estimate_wm_cargs(params: MrisEstimateWm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_estimate_wm")
    cargs.append("-s")
    for item0 in params["subjs"]:
        cargs.append(item0)
    cargs.append("--hemi")
    cargs.append(params["hemi"])
    v_0 = params.get("sdir")
    if v_0 is not None:
        cargs.append("-d")
        cargs.append(v_0)
    v_1 = params.get("model")
    if v_1 is not None:
        cargs.append("-m")
        cargs.append(v_1)
    v_2 = params.get("suffix")
    if v_2 is not None:
        cargs.append("-x")
        cargs.append(v_2)
    if params.get("gpu"):
        cargs.append("-g")
    if params.get("rsi"):
        cargs.append("--rsi")
    if params.get("single_iter"):
        cargs.append("--single-iter")
    v_3 = params.get("vol")
    if v_3 is not None:
        cargs.append("--vol")
        cargs.append(v_3)
    return cargs

def mris_estimate_wm_outputs(params: MrisEstimateWm, execution: Execution) -> MrisEstimateWmOutputs:
    """Build the MrisEstimateWmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisEstimateWmOutputs(
        root=root_v,
    )

def mris_estimate_wm_execute(params: MrisEstimateWm, runner: Runner | None = None) -> MrisEstimateWmOutputs:
    """
    mris_estimate_wm

    Tool to estimate white matter surfaces using MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_estimate_wm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ESTIMATE_WM_METADATA)
    execution.params(params)
    args = mris_estimate_wm_cargs(params, execution)
    out = mris_estimate_wm_outputs(params, execution)
    execution.run(args)
    return out

def mris_estimate_wm(
    subjs: list[str],
    hemi: str,
    sdir: str | None = None,
    model: str | None = None,
    suffix: str | None = None,
    gpu: bool = False,
    rsi: bool = False,
    single_iter: bool = False,
    vol: str | None = None,
    runner: Runner | None = None,
) -> MrisEstimateWmOutputs:
    """
    mris_estimate_wm

    Tool to estimate white matter surfaces using MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjs: List of subjects to process.
        hemi: Hemisphere to reconstruct (lh or rh).
        sdir: Override SUBJECTS_DIR.
        model: Override default model.
        suffix: Suffix of output surface (default is 'topofit').
        gpu: Use the GPU.
        rsi: Remove self-intersecting faces during the deformation.
        single_iter: Prevent deformation steps from running more than once.
        vol: Subject volume to use as input.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_estimate_wm_params(
        subjs=subjs,
        hemi=hemi,
        sdir=sdir,
        model=model,
        suffix=suffix,
        gpu=gpu,
        rsi=rsi,
        single_iter=single_iter,
        vol=vol,
    )
    return mris_estimate_wm_execute(params, runner)

__all__ = [
    "MrisEstimateWm",
    "MrisEstimateWmOutputs",
    "MRIS_ESTIMATE_WM_METADATA",
    "mris_estimate_wm_cargs",
    "mris_estimate_wm_outputs",
    "mris_estimate_wm_params",
    "mris_estimate_wm_execute",
    "mris_estimate_wm_validate",
    "mris_estimate_wm",
]