# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_SPLINE_METADATA = Metadata(
    id="dmri_spline",
    name="dmri_spline",
    package="freesurfer",
)

DmriSpline = typing.TypedDict(
    "DmriSpline",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_spline"]],
        "control_points_file": InputPathType,
        "mask_volume": InputPathType,
        "output_volume": typing.NotRequired[str],
        "show_points": typing.NotRequired[bool],
        "output_points": typing.NotRequired[str],
        "output_vectors_base": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "check_options": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriSplineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_volume: OutputPathType | None
    """Output volume of the interpolated spline"""
    out_points_file: OutputPathType | None
    """Output text file with interpolated spline points"""
    out_tangent_vectors: OutputPathType | None
    """Output text file containing tangent vectors"""
    out_normal_vectors: OutputPathType | None
    """Output text file containing normal vectors"""
    out_curvature: OutputPathType | None
    """Output text file containing curvatures"""

def dmri_spline_params(
    control_points_file: InputPathType,
    mask_volume: InputPathType,
    output_volume: str | None = None,
    show_points: bool = False,
    output_points: str | None = None,
    output_vectors_base: str | None = None,
    debug: bool = False,
    check_options: bool = False,
) -> DmriSpline:
    """
    Build parameters.

    Args:
        control_points_file: Input text file containing control points
        mask_volume: Input mask volume (spline is not allowed to stray off mask)
        output_volume: Output volume of the interpolated spline
        show_points: Highlight control points in output volume (default: no)
        output_points: Output text file containing all interpolated spline points
        output_vectors_base: Base name of output text files containing tangent\
            vectors, normal vectors, and curvatures at every point along the spline
        debug: Turn on debugging
        check_options: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriSpline = {
        "@type": "freesurfer/dmri_spline",
        "control_points_file": control_points_file,
        "mask_volume": mask_volume,
        "show_points": show_points,
        "debug": debug,
        "check_options": check_options,
    }
    if output_volume is not None:
        params["output_volume"] = output_volume
    if output_points is not None:
        params["output_points"] = output_points
    if output_vectors_base is not None:
        params["output_vectors_base"] = output_vectors_base
    return params

def dmri_spline_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriSpline."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("control_points_file", None) is None:
        raise StyxValidationError("`control_points_file` must not be None")
    if not isinstance(params["control_points_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`control_points_file` has the wrong type: Received `{type(params["control_points_file"])}` expected `InputPathType`')
    if params.get("mask_volume", None) is None:
        raise StyxValidationError("`mask_volume` must not be None")
    if not isinstance(params["mask_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("show_points", None) is not None:
        if not isinstance(params["show_points"], bool):
            raise StyxValidationError(f'`show_points` has the wrong type: Received `{type(params["show_points"])}` expected `bool`')
    if params.get("output_points", None) is not None:
        if not isinstance(params["output_points"], str):
            raise StyxValidationError(f'`output_points` has the wrong type: Received `{type(params["output_points"])}` expected `str`')
    if params.get("output_vectors_base", None) is not None:
        if not isinstance(params["output_vectors_base"], str):
            raise StyxValidationError(f'`output_vectors_base` has the wrong type: Received `{type(params["output_vectors_base"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_options", None) is not None:
        if not isinstance(params["check_options"], bool):
            raise StyxValidationError(f'`check_options` has the wrong type: Received `{type(params["check_options"])}` expected `bool`')

def dmri_spline_cargs(params: DmriSpline, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_spline")
    cargs.append("--cpts")
    cargs.append(execution.input_file(params["control_points_file"]))
    cargs.append("--mask")
    cargs.append(execution.input_file(params["mask_volume"]))
    v_0 = params.get("output_volume")
    if v_0 is not None:
        cargs.append("--out")
        cargs.append(v_0)
    if params.get("show_points"):
        cargs.append("--show")
    v_1 = params.get("output_points")
    if v_1 is not None:
        cargs.append("--outpts")
        cargs.append(v_1)
    v_2 = params.get("output_vectors_base")
    if v_2 is not None:
        cargs.append("--outvec")
        cargs.append(v_2)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_options"):
        cargs.append("--checkopts")
    return cargs

def dmri_spline_outputs(params: DmriSpline, execution: Execution) -> DmriSplineOutputs:
    """Build the DmriSplineOutputs object for this tool."""
    out_volume_v: OutputPathType | None = None
    out_points_file_v: OutputPathType | None = None
    out_tangent_vectors_v: OutputPathType | None = None
    out_normal_vectors_v: OutputPathType | None = None
    out_curvature_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_volume")
    if __v0 is not None:
        out_volume_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_points")
    if __v1 is not None:
        out_points_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("output_vectors_base")
    if __v2 is not None:
        out_tangent_vectors_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}_tangent.txt')
    __v3 = params.get("output_vectors_base")
    if __v3 is not None:
        out_normal_vectors_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}_normal.txt')
    __v4 = params.get("output_vectors_base")
    if __v4 is not None:
        out_curvature_v = execution.output_file(f'{(__v4 if __v4 is not None else "")}_curvature.txt')
    return DmriSplineOutputs(
        root=root_v,
        out_volume=out_volume_v,
        out_points_file=out_points_file_v,
        out_tangent_vectors=out_tangent_vectors_v,
        out_normal_vectors=out_normal_vectors_v,
        out_curvature=out_curvature_v,
    )

def dmri_spline_execute(params: DmriSpline, runner: Runner | None = None) -> DmriSplineOutputs:
    """
    dmri_spline

    Tool for interpolating and analyzing splines within a defined mask

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_spline_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_SPLINE_METADATA)
    execution.params(params)
    args = dmri_spline_cargs(params, execution)
    out = dmri_spline_outputs(params, execution)
    execution.run(args)
    return out

def dmri_spline(
    control_points_file: InputPathType,
    mask_volume: InputPathType,
    output_volume: str | None = None,
    show_points: bool = False,
    output_points: str | None = None,
    output_vectors_base: str | None = None,
    debug: bool = False,
    check_options: bool = False,
    runner: Runner | None = None,
) -> DmriSplineOutputs:
    """
    dmri_spline

    Tool for interpolating and analyzing splines within a defined mask

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        control_points_file: Input text file containing control points
        mask_volume: Input mask volume (spline is not allowed to stray off mask)
        output_volume: Output volume of the interpolated spline
        show_points: Highlight control points in output volume (default: no)
        output_points: Output text file containing all interpolated spline points
        output_vectors_base: Base name of output text files containing tangent\
            vectors, normal vectors, and curvatures at every point along the spline
        debug: Turn on debugging
        check_options: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_spline_params(
        control_points_file=control_points_file,
        mask_volume=mask_volume,
        output_volume=output_volume,
        show_points=show_points,
        output_points=output_points,
        output_vectors_base=output_vectors_base,
        debug=debug,
        check_options=check_options,
    )
    return dmri_spline_execute(params, runner)

__all__ = [
    "DmriSpline",
    "DmriSplineOutputs",
    "DMRI_SPLINE_METADATA",
    "dmri_spline_cargs",
    "dmri_spline_outputs",
    "dmri_spline_params",
    "dmri_spline_execute",
    "dmri_spline_validate",
    "dmri_spline",
]