# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_HIRES_REGISTER_METADATA = Metadata(
    id="mri_hires_register",
    name="mri_hires_register",
    package="freesurfer",
)

MriHiresRegister = typing.TypedDict(
    "MriHiresRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_hires_register"]],
        "hires_labeling": InputPathType,
        "input_intensity": InputPathType,
        "input_aseg": InputPathType,
        "output_xform": str,
    },
)

@dataclasses.dataclass
class MriHiresRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output transform file resulting from high-resolution registration"""

def mri_hires_register_params(
    hires_labeling: InputPathType,
    input_intensity: InputPathType,
    input_aseg: InputPathType,
    output_xform: str,
) -> MriHiresRegister:
    """
    Build parameters.

    Args:
        hires_labeling: The high resolution labeling input file
        input_intensity: The input intensity file
        input_aseg: The input aseg file
        output_xform: The output transform file

    Returns:
        Parameter dictionary.
    """
    params: MriHiresRegister = {
        "@type": "freesurfer/mri_hires_register",
        "hires_labeling": hires_labeling,
        "input_intensity": input_intensity,
        "input_aseg": input_aseg,
        "output_xform": output_xform,
    }
    return params

def mri_hires_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriHiresRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hires_labeling", None) is None:
        raise StyxValidationError("`hires_labeling` must not be None")
    if not isinstance(params["hires_labeling"], (pathlib.Path, str)):
        raise StyxValidationError(f'`hires_labeling` has the wrong type: Received `{type(params["hires_labeling"])}` expected `InputPathType`')
    if params.get("input_intensity", None) is None:
        raise StyxValidationError("`input_intensity` must not be None")
    if not isinstance(params["input_intensity"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_intensity` has the wrong type: Received `{type(params["input_intensity"])}` expected `InputPathType`')
    if params.get("input_aseg", None) is None:
        raise StyxValidationError("`input_aseg` must not be None")
    if not isinstance(params["input_aseg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_aseg` has the wrong type: Received `{type(params["input_aseg"])}` expected `InputPathType`')
    if params.get("output_xform", None) is None:
        raise StyxValidationError("`output_xform` must not be None")
    if not isinstance(params["output_xform"], str):
        raise StyxValidationError(f'`output_xform` has the wrong type: Received `{type(params["output_xform"])}` expected `str`')

def mri_hires_register_cargs(params: MriHiresRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_hires_register")
    cargs.append(execution.input_file(params["hires_labeling"]))
    cargs.append(execution.input_file(params["input_intensity"]))
    cargs.append(execution.input_file(params["input_aseg"]))
    cargs.append(params["output_xform"])
    return cargs

def mri_hires_register_outputs(params: MriHiresRegister, execution: Execution) -> MriHiresRegisterOutputs:
    """Build the MriHiresRegisterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_xform"])
    return MriHiresRegisterOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_hires_register_execute(params: MriHiresRegister, runner: Runner | None = None) -> MriHiresRegisterOutputs:
    """
    mri_hires_register

    A tool for high-resolution registration for Freesurfer images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_hires_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_HIRES_REGISTER_METADATA)
    execution.params(params)
    args = mri_hires_register_cargs(params, execution)
    out = mri_hires_register_outputs(params, execution)
    execution.run(args)
    return out

def mri_hires_register(
    hires_labeling: InputPathType,
    input_intensity: InputPathType,
    input_aseg: InputPathType,
    output_xform: str,
    runner: Runner | None = None,
) -> MriHiresRegisterOutputs:
    """
    mri_hires_register

    A tool for high-resolution registration for Freesurfer images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hires_labeling: The high resolution labeling input file
        input_intensity: The input intensity file
        input_aseg: The input aseg file
        output_xform: The output transform file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_hires_register_params(
        hires_labeling=hires_labeling,
        input_intensity=input_intensity,
        input_aseg=input_aseg,
        output_xform=output_xform,
    )
    return mri_hires_register_execute(params, runner)

__all__ = [
    "MriHiresRegister",
    "MriHiresRegisterOutputs",
    "MRI_HIRES_REGISTER_METADATA",
    "mri_hires_register_cargs",
    "mri_hires_register_outputs",
    "mri_hires_register_params",
    "mri_hires_register_execute",
    "mri_hires_register_validate",
    "mri_hires_register",
]