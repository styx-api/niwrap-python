# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EXVIVO_STRIP_METADATA = Metadata(
    id="mri_exvivo_strip",
    name="mri_exvivo_strip",
    package="freesurfer",
)

MriExvivoStrip = typing.TypedDict(
    "MriExvivoStrip",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_exvivo_strip"]],
        "invol": InputPathType,
        "outvol": str,
        "hemi": str,
        "pred": typing.NotRequired[InputPathType],
        "norm": typing.NotRequired[InputPathType],
        "fv": typing.NotRequired[bool],
        "uthresh": typing.NotRequired[float],
        "border": typing.NotRequired[float],
        "multichannel": typing.NotRequired[bool],
        "model": typing.NotRequired[InputPathType],
        "wts": typing.NotRequired[InputPathType],
        "gpu": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriExvivoStripOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Processed output MRI volume"""
    prediction_output: OutputPathType | None
    """Prediction volume if specified"""
    normalized_output: OutputPathType | None
    """Normalized volume if specified"""

def mri_exvivo_strip_params(
    invol: InputPathType,
    outvol: str,
    hemi: str,
    pred: InputPathType | None = None,
    norm: InputPathType | None = None,
    fv: bool = False,
    uthresh: float | None = None,
    border: float | None = None,
    multichannel: bool = False,
    model: InputPathType | None = None,
    wts: InputPathType | None = None,
    gpu: float | None = None,
) -> MriExvivoStrip:
    """
    Build parameters.

    Args:
        invol: Input MRI volume
        outvol: Output MRI volume
        hemi: Hemi to process
        pred: Write prediction volume
        norm: Write normalized volume
        fv: Bring up freeview to show results
        uthresh: Specify threshold to erase above
        border: Number of border voxels to set threshold at
        multichannel: Specify that data has multiple channels
        model: Use alternative model file
        wts: Weight filename
        gpu: GPU number - if not supplied, CPU is used

    Returns:
        Parameter dictionary.
    """
    params: MriExvivoStrip = {
        "@type": "freesurfer/mri_exvivo_strip",
        "invol": invol,
        "outvol": outvol,
        "hemi": hemi,
        "fv": fv,
        "multichannel": multichannel,
    }
    if pred is not None:
        params["pred"] = pred
    if norm is not None:
        params["norm"] = norm
    if uthresh is not None:
        params["uthresh"] = uthresh
    if border is not None:
        params["border"] = border
    if model is not None:
        params["model"] = model
    if wts is not None:
        params["wts"] = wts
    if gpu is not None:
        params["gpu"] = gpu
    return params

def mri_exvivo_strip_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriExvivoStrip."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("invol", None) is None:
        raise StyxValidationError("`invol` must not be None")
    if not isinstance(params["invol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`invol` has the wrong type: Received `{type(params["invol"])}` expected `InputPathType`')
    if params.get("outvol", None) is None:
        raise StyxValidationError("`outvol` must not be None")
    if not isinstance(params["outvol"], str):
        raise StyxValidationError(f'`outvol` has the wrong type: Received `{type(params["outvol"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("pred", None) is not None:
        if not isinstance(params["pred"], (pathlib.Path, str)):
            raise StyxValidationError(f'`pred` has the wrong type: Received `{type(params["pred"])}` expected `InputPathType`')
    if params.get("norm", None) is not None:
        if not isinstance(params["norm"], (pathlib.Path, str)):
            raise StyxValidationError(f'`norm` has the wrong type: Received `{type(params["norm"])}` expected `InputPathType`')
    if params.get("fv", None) is not None:
        if not isinstance(params["fv"], bool):
            raise StyxValidationError(f'`fv` has the wrong type: Received `{type(params["fv"])}` expected `bool`')
    if params.get("uthresh", None) is not None:
        if not isinstance(params["uthresh"], (float, int)):
            raise StyxValidationError(f'`uthresh` has the wrong type: Received `{type(params["uthresh"])}` expected `float`')
    if params.get("border", None) is not None:
        if not isinstance(params["border"], (float, int)):
            raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `float`')
    if params.get("multichannel", None) is not None:
        if not isinstance(params["multichannel"], bool):
            raise StyxValidationError(f'`multichannel` has the wrong type: Received `{type(params["multichannel"])}` expected `bool`')
    if params.get("model", None) is not None:
        if not isinstance(params["model"], (pathlib.Path, str)):
            raise StyxValidationError(f'`model` has the wrong type: Received `{type(params["model"])}` expected `InputPathType`')
    if params.get("wts", None) is not None:
        if not isinstance(params["wts"], (pathlib.Path, str)):
            raise StyxValidationError(f'`wts` has the wrong type: Received `{type(params["wts"])}` expected `InputPathType`')
    if params.get("gpu", None) is not None:
        if not isinstance(params["gpu"], (float, int)):
            raise StyxValidationError(f'`gpu` has the wrong type: Received `{type(params["gpu"])}` expected `float`')

def mri_exvivo_strip_cargs(params: MriExvivoStrip, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_exvivo_strip")
    cargs.append("-i")
    cargs.append(execution.input_file(params["invol"]))
    cargs.append("-o")
    cargs.append(params["outvol"])
    cargs.append("--hemi")
    cargs.append(params["hemi"])
    v_0 = params.get("pred")
    if v_0 is not None:
        cargs.append("--pred")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("norm")
    if v_1 is not None:
        cargs.append("--norm")
        cargs.append(execution.input_file(v_1))
    if params.get("fv"):
        cargs.append("--fv")
    v_2 = params.get("uthresh")
    if v_2 is not None:
        cargs.append("--uthresh")
        cargs.append(str(v_2))
    v_3 = params.get("border")
    if v_3 is not None:
        cargs.append("--border")
        cargs.append(str(v_3))
    if params.get("multichannel"):
        cargs.append("--multichannel")
    v_4 = params.get("model")
    if v_4 is not None:
        cargs.append("--model")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("wts")
    if v_5 is not None:
        cargs.append("--wts")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("gpu")
    if v_6 is not None:
        cargs.append("--gpu")
        cargs.append(str(v_6))
    return cargs

def mri_exvivo_strip_outputs(params: MriExvivoStrip, execution: Execution) -> MriExvivoStripOutputs:
    """Build the MriExvivoStripOutputs object for this tool."""
    prediction_output_v: OutputPathType | None = None
    normalized_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["outvol"])
    __v0 = params.get("pred")
    if __v0 is not None:
        prediction_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("norm")
    if __v1 is not None:
        normalized_output_v = execution.output_file((__v1 if __v1 is not None else ""))
    return MriExvivoStripOutputs(
        root=root_v,
        output_file=output_file_v,
        prediction_output=prediction_output_v,
        normalized_output=normalized_output_v,
    )

def mri_exvivo_strip_execute(params: MriExvivoStrip, runner: Runner | None = None) -> MriExvivoStripOutputs:
    """
    mri_exvivo_strip

    Tool for processing MRI volumes for ex vivo data

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_exvivo_strip_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EXVIVO_STRIP_METADATA)
    execution.params(params)
    args = mri_exvivo_strip_cargs(params, execution)
    out = mri_exvivo_strip_outputs(params, execution)
    execution.run(args)
    return out

def mri_exvivo_strip(
    invol: InputPathType,
    outvol: str,
    hemi: str,
    pred: InputPathType | None = None,
    norm: InputPathType | None = None,
    fv: bool = False,
    uthresh: float | None = None,
    border: float | None = None,
    multichannel: bool = False,
    model: InputPathType | None = None,
    wts: InputPathType | None = None,
    gpu: float | None = None,
    runner: Runner | None = None,
) -> MriExvivoStripOutputs:
    """
    mri_exvivo_strip

    Tool for processing MRI volumes for ex vivo data

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        invol: Input MRI volume
        outvol: Output MRI volume
        hemi: Hemi to process
        pred: Write prediction volume
        norm: Write normalized volume
        fv: Bring up freeview to show results
        uthresh: Specify threshold to erase above
        border: Number of border voxels to set threshold at
        multichannel: Specify that data has multiple channels
        model: Use alternative model file
        wts: Weight filename
        gpu: GPU number - if not supplied, CPU is used
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_exvivo_strip_params(
        invol=invol,
        outvol=outvol,
        hemi=hemi,
        pred=pred,
        norm=norm,
        fv=fv,
        uthresh=uthresh,
        border=border,
        multichannel=multichannel,
        model=model,
        wts=wts,
        gpu=gpu,
    )
    return mri_exvivo_strip_execute(params, runner)

__all__ = [
    "MriExvivoStrip",
    "MriExvivoStripOutputs",
    "MRI_EXVIVO_STRIP_METADATA",
    "mri_exvivo_strip_cargs",
    "mri_exvivo_strip_outputs",
    "mri_exvivo_strip_params",
    "mri_exvivo_strip_execute",
    "mri_exvivo_strip_validate",
    "mri_exvivo_strip",
]