# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACK_IMA1_TCL_METADATA = Metadata(
    id="unpack_ima1.tcl",
    name="unpack_ima1.tcl",
    package="freesurfer",
)

UnpackIma1Tcl = typing.TypedDict(
    "UnpackIma1Tcl",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpack_ima1.tcl"]],
        "input_directory": str,
        "output_directory": str,
    },
)

@dataclasses.dataclass
class UnpackIma1TclOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType
    """Output directory containing unpacked files"""

def unpack_ima1_tcl_params(
    input_directory: str,
    output_directory: str,
) -> UnpackIma1Tcl:
    """
    Build parameters.

    Args:
        input_directory: The directory containing the input files to be unpacked.
        output_directory: The directory where the unpacked files will be stored.

    Returns:
        Parameter dictionary.
    """
    params: UnpackIma1Tcl = {
        "@type": "freesurfer/unpack_ima1.tcl",
        "input_directory": input_directory,
        "output_directory": output_directory,
    }
    return params

def unpack_ima1_tcl_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UnpackIma1Tcl."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_directory", None) is None:
        raise StyxValidationError("`input_directory` must not be None")
    if not isinstance(params["input_directory"], str):
        raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def unpack_ima1_tcl_cargs(params: UnpackIma1Tcl, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpack_ima1.tcl")
    cargs.append(params["input_directory"])
    cargs.append(params["output_directory"])
    return cargs

def unpack_ima1_tcl_outputs(params: UnpackIma1Tcl, execution: Execution) -> UnpackIma1TclOutputs:
    """Build the UnpackIma1TclOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_directory_v: OutputPathType = execution.output_file(params["output_directory"])
    return UnpackIma1TclOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def unpack_ima1_tcl_execute(params: UnpackIma1Tcl, runner: Runner | None = None) -> UnpackIma1TclOutputs:
    """
    unpack_ima1.tcl

    A tool for unpacking images using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpack_ima1_tcl_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACK_IMA1_TCL_METADATA)
    execution.params(params)
    args = unpack_ima1_tcl_cargs(params, execution)
    out = unpack_ima1_tcl_outputs(params, execution)
    execution.run(args)
    return out

def unpack_ima1_tcl(
    input_directory: str,
    output_directory: str,
    runner: Runner | None = None,
) -> UnpackIma1TclOutputs:
    """
    unpack_ima1.tcl

    A tool for unpacking images using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_directory: The directory containing the input files to be unpacked.
        output_directory: The directory where the unpacked files will be stored.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpack_ima1_tcl_params(
        input_directory=input_directory,
        output_directory=output_directory,
    )
    return unpack_ima1_tcl_execute(params, runner)

__all__ = [
    "UnpackIma1Tcl",
    "UnpackIma1TclOutputs",
    "UNPACK_IMA1_TCL_METADATA",
    "unpack_ima1_tcl_cargs",
    "unpack_ima1_tcl_outputs",
    "unpack_ima1_tcl_params",
    "unpack_ima1_tcl_execute",
    "unpack_ima1_tcl_validate",
    "unpack_ima1_tcl",
]