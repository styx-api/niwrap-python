# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SAMSEG_LONG_METADATA = Metadata(
    id="samseg-long",
    name="samseg-long",
    package="freesurfer",
)

SamsegLong = typing.TypedDict(
    "SamsegLong",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/samseg-long"]],
        "output_dir": str,
        "input_files": list[InputPathType],
        "align_mc": typing.NotRequired[bool],
        "align_no_mc": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "save_posteriors": typing.NotRequired[bool],
        "force_update": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SamsegLongOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tp001_output: OutputPathType
    """Samseg output folder for the first time point."""
    tp002_output: OutputPathType
    """Samseg output folder for the second time point."""
    base_output: OutputPathType
    """Base folder created by the samseg-long process."""

def samseg_long_params(
    output_dir: str,
    input_files: list[InputPathType],
    align_mc: bool = False,
    align_no_mc: bool = False,
    threads: float | None = None,
    save_posteriors: bool = False,
    force_update: bool = False,
) -> SamsegLong:
    """
    Build parameters.

    Args:
        output_dir: Output directory.
        input_files: Input image files. All inputs must be a single modality.
        align_mc: Align all inputs using robust register.
        align_no_mc: Do not align inputs using robust register.
        threads: Number of threads to use.
        save_posteriors: Save posterior probabilities.
        force_update: Force update of outputs.

    Returns:
        Parameter dictionary.
    """
    params: SamsegLong = {
        "@type": "freesurfer/samseg-long",
        "output_dir": output_dir,
        "input_files": input_files,
        "align_mc": align_mc,
        "align_no_mc": align_no_mc,
        "save_posteriors": save_posteriors,
        "force_update": force_update,
    }
    if threads is not None:
        params["threads"] = threads
    return params

def samseg_long_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SamsegLong."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("align_mc", None) is not None:
        if not isinstance(params["align_mc"], bool):
            raise StyxValidationError(f'`align_mc` has the wrong type: Received `{type(params["align_mc"])}` expected `bool`')
    if params.get("align_no_mc", None) is not None:
        if not isinstance(params["align_no_mc"], bool):
            raise StyxValidationError(f'`align_no_mc` has the wrong type: Received `{type(params["align_no_mc"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("save_posteriors", None) is not None:
        if not isinstance(params["save_posteriors"], bool):
            raise StyxValidationError(f'`save_posteriors` has the wrong type: Received `{type(params["save_posteriors"])}` expected `bool`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')

def samseg_long_cargs(params: SamsegLong, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("samseg-long")
    cargs.append("--o")
    cargs.append(params["output_dir"])
    cargs.append("--i")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    if params.get("align_mc"):
        cargs.append("--mc")
    if params.get("align_no_mc"):
        cargs.append("--no-mc")
    v_0 = params.get("threads")
    if v_0 is not None:
        cargs.append("--threads")
        cargs.append(str(v_0))
    if params.get("save_posteriors"):
        cargs.append("--save-posteriors")
    if params.get("force_update"):
        cargs.append("--force-update")
    return cargs

def samseg_long_outputs(params: SamsegLong, execution: Execution) -> SamsegLongOutputs:
    """Build the SamsegLongOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tp001_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/tp001')
    tp002_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/tp002')
    base_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/base')
    return SamsegLongOutputs(
        root=root_v,
        tp001_output=tp001_output_v,
        tp002_output=tp002_output_v,
        base_output=base_output_v,
    )

def samseg_long_execute(params: SamsegLong, runner: Runner | None = None) -> SamsegLongOutputs:
    """
    samseg-long

    Longitudinal analysis tool using SAMSEG in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    samseg_long_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SAMSEG_LONG_METADATA)
    execution.params(params)
    args = samseg_long_cargs(params, execution)
    out = samseg_long_outputs(params, execution)
    execution.run(args)
    return out

def samseg_long(
    output_dir: str,
    input_files: list[InputPathType],
    align_mc: bool = False,
    align_no_mc: bool = False,
    threads: float | None = None,
    save_posteriors: bool = False,
    force_update: bool = False,
    runner: Runner | None = None,
) -> SamsegLongOutputs:
    """
    samseg-long

    Longitudinal analysis tool using SAMSEG in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_dir: Output directory.
        input_files: Input image files. All inputs must be a single modality.
        align_mc: Align all inputs using robust register.
        align_no_mc: Do not align inputs using robust register.
        threads: Number of threads to use.
        save_posteriors: Save posterior probabilities.
        force_update: Force update of outputs.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = samseg_long_params(
        output_dir=output_dir,
        input_files=input_files,
        align_mc=align_mc,
        align_no_mc=align_no_mc,
        threads=threads,
        save_posteriors=save_posteriors,
        force_update=force_update,
    )
    return samseg_long_execute(params, runner)

__all__ = [
    "SamsegLong",
    "SamsegLongOutputs",
    "SAMSEG_LONG_METADATA",
    "samseg_long_cargs",
    "samseg_long_outputs",
    "samseg_long_params",
    "samseg_long_execute",
    "samseg_long_validate",
    "samseg_long",
]