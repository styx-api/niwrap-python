# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

STAT_NORMALIZE_METADATA = Metadata(
    id="stat_normalize",
    name="stat_normalize",
    package="freesurfer",
)

StatNormalize = typing.TypedDict(
    "StatNormalize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/stat_normalize"]],
        "input_sv_prefix": str,
        "output_sv_prefix": str,
        "resolution": typing.NotRequired[float],
        "field_of_view": typing.NotRequired[float],
        "sph_avg": typing.NotRequired[str],
        "xfm_file": typing.NotRequired[str],
        "fix_xfm_flag": typing.NotRequired[bool],
        "float2int_option": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class StatNormalizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def stat_normalize_params(
    input_sv_prefix: str,
    output_sv_prefix: str,
    resolution: float | None = None,
    field_of_view: float | None = None,
    sph_avg: str | None = None,
    xfm_file: str | None = None,
    fix_xfm_flag: bool = False,
    float2int_option: str | None = None,
) -> StatNormalize:
    """
    Build parameters.

    Args:
        input_sv_prefix: Input subject volume prefix.
        output_sv_prefix: Output subject volume prefix.
        resolution: Set output resolution in mm (default is 8mm).
        field_of_view: Set output field of view (default is 256).
        sph_avg: Average in spherical coordinates by specifying hemisphere and\
            surface.
        xfm_file: Use specified transform file (subjid/mri/transforms/xfmfile).
        fix_xfm_flag: Fix transform for non-zero center of original volume.
        float2int_option: Specify float to int conversion to tkregister or round.

    Returns:
        Parameter dictionary.
    """
    params: StatNormalize = {
        "@type": "freesurfer/stat_normalize",
        "input_sv_prefix": input_sv_prefix,
        "output_sv_prefix": output_sv_prefix,
        "fix_xfm_flag": fix_xfm_flag,
    }
    if resolution is not None:
        params["resolution"] = resolution
    if field_of_view is not None:
        params["field_of_view"] = field_of_view
    if sph_avg is not None:
        params["sph_avg"] = sph_avg
    if xfm_file is not None:
        params["xfm_file"] = xfm_file
    if float2int_option is not None:
        params["float2int_option"] = float2int_option
    return params

def stat_normalize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid StatNormalize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_sv_prefix", None) is None:
        raise StyxValidationError("`input_sv_prefix` must not be None")
    if not isinstance(params["input_sv_prefix"], str):
        raise StyxValidationError(f'`input_sv_prefix` has the wrong type: Received `{type(params["input_sv_prefix"])}` expected `str`')
    if params.get("output_sv_prefix", None) is None:
        raise StyxValidationError("`output_sv_prefix` must not be None")
    if not isinstance(params["output_sv_prefix"], str):
        raise StyxValidationError(f'`output_sv_prefix` has the wrong type: Received `{type(params["output_sv_prefix"])}` expected `str`')
    if params.get("resolution", None) is not None:
        if not isinstance(params["resolution"], (float, int)):
            raise StyxValidationError(f'`resolution` has the wrong type: Received `{type(params["resolution"])}` expected `float`')
    if params.get("field_of_view", None) is not None:
        if not isinstance(params["field_of_view"], (float, int)):
            raise StyxValidationError(f'`field_of_view` has the wrong type: Received `{type(params["field_of_view"])}` expected `float`')
    if params.get("sph_avg", None) is not None:
        if not isinstance(params["sph_avg"], str):
            raise StyxValidationError(f'`sph_avg` has the wrong type: Received `{type(params["sph_avg"])}` expected `str`')
    if params.get("xfm_file", None) is not None:
        if not isinstance(params["xfm_file"], str):
            raise StyxValidationError(f'`xfm_file` has the wrong type: Received `{type(params["xfm_file"])}` expected `str`')
    if params.get("fix_xfm_flag", None) is not None:
        if not isinstance(params["fix_xfm_flag"], bool):
            raise StyxValidationError(f'`fix_xfm_flag` has the wrong type: Received `{type(params["fix_xfm_flag"])}` expected `bool`')
    if params.get("float2int_option", None) is not None:
        if not isinstance(params["float2int_option"], str):
            raise StyxValidationError(f'`float2int_option` has the wrong type: Received `{type(params["float2int_option"])}` expected `str`')

def stat_normalize_cargs(params: StatNormalize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("stat_normalize")
    cargs.append(params["input_sv_prefix"])
    cargs.append(params["output_sv_prefix"])
    v_0 = params.get("resolution")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(str(v_0))
    v_1 = params.get("field_of_view")
    if v_1 is not None:
        cargs.append("-f")
        cargs.append(str(v_1))
    v_2 = params.get("sph_avg")
    if v_2 is not None:
        cargs.append("-S")
        cargs.append(v_2)
    v_3 = params.get("xfm_file")
    if v_3 is not None:
        cargs.append("-x")
        cargs.append(v_3)
    if params.get("fix_xfm_flag"):
        cargs.append("-i")
    v_4 = params.get("float2int_option")
    if v_4 is not None:
        cargs.append("-c")
        cargs.append(v_4)
    return cargs

def stat_normalize_outputs(params: StatNormalize, execution: Execution) -> StatNormalizeOutputs:
    """Build the StatNormalizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return StatNormalizeOutputs(
        root=root_v,
    )

def stat_normalize_execute(params: StatNormalize, runner: Runner | None = None) -> StatNormalizeOutputs:
    """
    stat_normalize

    This program will convert and average a sequence of volume-based statistics in Talairach space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    stat_normalize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(STAT_NORMALIZE_METADATA)
    execution.params(params)
    args = stat_normalize_cargs(params, execution)
    out = stat_normalize_outputs(params, execution)
    execution.run(args)
    return out

def stat_normalize(
    input_sv_prefix: str,
    output_sv_prefix: str,
    resolution: float | None = None,
    field_of_view: float | None = None,
    sph_avg: str | None = None,
    xfm_file: str | None = None,
    fix_xfm_flag: bool = False,
    float2int_option: str | None = None,
    runner: Runner | None = None,
) -> StatNormalizeOutputs:
    """
    stat_normalize

    This program will convert and average a sequence of volume-based statistics in Talairach space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_sv_prefix: Input subject volume prefix.
        output_sv_prefix: Output subject volume prefix.
        resolution: Set output resolution in mm (default is 8mm).
        field_of_view: Set output field of view (default is 256).
        sph_avg: Average in spherical coordinates by specifying hemisphere and\
            surface.
        xfm_file: Use specified transform file (subjid/mri/transforms/xfmfile).
        fix_xfm_flag: Fix transform for non-zero center of original volume.
        float2int_option: Specify float to int conversion to tkregister or round.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = stat_normalize_params(
        input_sv_prefix=input_sv_prefix,
        output_sv_prefix=output_sv_prefix,
        resolution=resolution,
        field_of_view=field_of_view,
        sph_avg=sph_avg,
        xfm_file=xfm_file,
        fix_xfm_flag=fix_xfm_flag,
        float2int_option=float2int_option,
    )
    return stat_normalize_execute(params, runner)

__all__ = [
    "StatNormalize",
    "StatNormalizeOutputs",
    "STAT_NORMALIZE_METADATA",
    "stat_normalize_cargs",
    "stat_normalize_outputs",
    "stat_normalize_params",
    "stat_normalize_execute",
    "stat_normalize_validate",
    "stat_normalize",
]