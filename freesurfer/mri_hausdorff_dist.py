# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_HAUSDORFF_DIST_METADATA = Metadata(
    id="mri_hausdorff_dist",
    name="mri_hausdorff_dist",
    package="freesurfer",
)

MriHausdorffDist = typing.TypedDict(
    "MriHausdorffDist",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_hausdorff_dist"]],
        "vol1": InputPathType,
        "vol2": InputPathType,
        "output_text_file": str,
        "threshold": typing.NotRequired[float],
        "input_file_flag": typing.NotRequired[bool],
        "blur_sigma": typing.NotRequired[float],
        "max_flag": typing.NotRequired[bool],
        "label_index": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriHausdorffDistOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_text: OutputPathType
    """Output text file containing the results of Hausdorff distance calculation"""

def mri_hausdorff_dist_params(
    vol1: InputPathType,
    vol2: InputPathType,
    output_text_file: str,
    threshold: float | None = None,
    input_file_flag: bool = False,
    blur_sigma: float | None = None,
    max_flag: bool = False,
    label_index: float | None = None,
) -> MriHausdorffDist:
    """
    Build parameters.

    Args:
        vol1: First input volume
        vol2: Second input volume
        output_text_file: Output text file
        threshold: Binarize input volumes with given threshold
        input_file_flag: Read volumes from an input file (first argument is the\
            input filename)
        blur_sigma: Blur the input image with Gaussian of specified sigma
        max_flag: Compute the maximum of the minimum distances instead of the mean
        label_index: Use specified label index as the target label

    Returns:
        Parameter dictionary.
    """
    params: MriHausdorffDist = {
        "@type": "freesurfer/mri_hausdorff_dist",
        "vol1": vol1,
        "vol2": vol2,
        "output_text_file": output_text_file,
        "input_file_flag": input_file_flag,
        "max_flag": max_flag,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if blur_sigma is not None:
        params["blur_sigma"] = blur_sigma
    if label_index is not None:
        params["label_index"] = label_index
    return params

def mri_hausdorff_dist_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriHausdorffDist."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vol1", None) is None:
        raise StyxValidationError("`vol1` must not be None")
    if not isinstance(params["vol1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol1` has the wrong type: Received `{type(params["vol1"])}` expected `InputPathType`')
    if params.get("vol2", None) is None:
        raise StyxValidationError("`vol2` must not be None")
    if not isinstance(params["vol2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol2` has the wrong type: Received `{type(params["vol2"])}` expected `InputPathType`')
    if params.get("output_text_file", None) is None:
        raise StyxValidationError("`output_text_file` must not be None")
    if not isinstance(params["output_text_file"], str):
        raise StyxValidationError(f'`output_text_file` has the wrong type: Received `{type(params["output_text_file"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("input_file_flag", None) is not None:
        if not isinstance(params["input_file_flag"], bool):
            raise StyxValidationError(f'`input_file_flag` has the wrong type: Received `{type(params["input_file_flag"])}` expected `bool`')
    if params.get("blur_sigma", None) is not None:
        if not isinstance(params["blur_sigma"], (float, int)):
            raise StyxValidationError(f'`blur_sigma` has the wrong type: Received `{type(params["blur_sigma"])}` expected `float`')
    if params.get("max_flag", None) is not None:
        if not isinstance(params["max_flag"], bool):
            raise StyxValidationError(f'`max_flag` has the wrong type: Received `{type(params["max_flag"])}` expected `bool`')
    if params.get("label_index", None) is not None:
        if not isinstance(params["label_index"], (float, int)):
            raise StyxValidationError(f'`label_index` has the wrong type: Received `{type(params["label_index"])}` expected `float`')

def mri_hausdorff_dist_cargs(params: MriHausdorffDist, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_hausdorff_dist")
    cargs.append(execution.input_file(params["vol1"]))
    cargs.append(execution.input_file(params["vol2"]))
    cargs.append(params["output_text_file"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-b")
        cargs.append(str(v_0))
    if params.get("input_file_flag"):
        cargs.append("-F")
    v_1 = params.get("blur_sigma")
    if v_1 is not None:
        cargs.append("-g")
        cargs.append(str(v_1))
    if params.get("max_flag"):
        cargs.append("-max")
    v_2 = params.get("label_index")
    if v_2 is not None:
        cargs.append("-l")
        cargs.append(str(v_2))
    return cargs

def mri_hausdorff_dist_outputs(params: MriHausdorffDist, execution: Execution) -> MriHausdorffDistOutputs:
    """Build the MriHausdorffDistOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_text_v: OutputPathType = execution.output_file(params["output_text_file"])
    return MriHausdorffDistOutputs(
        root=root_v,
        output_text=output_text_v,
    )

def mri_hausdorff_dist_execute(params: MriHausdorffDist, runner: Runner | None = None) -> MriHausdorffDistOutputs:
    """
    mri_hausdorff_dist

    Tool for computing the mean or max of the minimum distances between point sets in 3D volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_hausdorff_dist_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_HAUSDORFF_DIST_METADATA)
    execution.params(params)
    args = mri_hausdorff_dist_cargs(params, execution)
    out = mri_hausdorff_dist_outputs(params, execution)
    execution.run(args)
    return out

def mri_hausdorff_dist(
    vol1: InputPathType,
    vol2: InputPathType,
    output_text_file: str,
    threshold: float | None = None,
    input_file_flag: bool = False,
    blur_sigma: float | None = None,
    max_flag: bool = False,
    label_index: float | None = None,
    runner: Runner | None = None,
) -> MriHausdorffDistOutputs:
    """
    mri_hausdorff_dist

    Tool for computing the mean or max of the minimum distances between point sets in 3D volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        vol1: First input volume
        vol2: Second input volume
        output_text_file: Output text file
        threshold: Binarize input volumes with given threshold
        input_file_flag: Read volumes from an input file (first argument is the\
            input filename)
        blur_sigma: Blur the input image with Gaussian of specified sigma
        max_flag: Compute the maximum of the minimum distances instead of the mean
        label_index: Use specified label index as the target label
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_hausdorff_dist_params(
        vol1=vol1,
        vol2=vol2,
        output_text_file=output_text_file,
        threshold=threshold,
        input_file_flag=input_file_flag,
        blur_sigma=blur_sigma,
        max_flag=max_flag,
        label_index=label_index,
    )
    return mri_hausdorff_dist_execute(params, runner)

__all__ = [
    "MriHausdorffDist",
    "MriHausdorffDistOutputs",
    "MRI_HAUSDORFF_DIST_METADATA",
    "mri_hausdorff_dist_cargs",
    "mri_hausdorff_dist_outputs",
    "mri_hausdorff_dist_params",
    "mri_hausdorff_dist_execute",
    "mri_hausdorff_dist_validate",
    "mri_hausdorff_dist",
]