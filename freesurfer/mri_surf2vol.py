# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SURF2VOL_METADATA = Metadata(
    id="mri_surf2vol",
    name="mri_surf2vol",
    package="freesurfer",
)

MriSurf2vol = typing.TypedDict(
    "MriSurf2vol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_surf2vol"]],
        "surface_overlay": typing.NotRequired[list[str]],
        "ltafile": typing.NotRequired[InputPathType],
        "outfile": str,
        "subject": typing.NotRequired[str],
        "ribbonfile": typing.NotRequired[InputPathType],
        "merge_volume": typing.NotRequired[InputPathType],
        "surface_values": typing.NotRequired[str],
        "mkmask": typing.NotRequired[bool],
        "hemi": typing.NotRequired[str],
        "surfname": typing.NotRequired[str],
        "projfrac": typing.NotRequired[float],
        "fill_ribbon": typing.NotRequired[bool],
        "fill_projfrac": typing.NotRequired[str],
        "reg_volume": typing.NotRequired[InputPathType],
        "identity": typing.NotRequired[str],
        "template_volume": typing.NotRequired[InputPathType],
        "fstal_res": typing.NotRequired[str],
        "vtxvol": typing.NotRequired[InputPathType],
        "flat2mri": typing.NotRequired[str],
        "sphpvf": typing.NotRequired[str],
        "mask_to_cortex": typing.NotRequired[bool],
        "mask_to_label": typing.NotRequired[InputPathType],
        "surface_mask": typing.NotRequired[InputPathType],
        "add_const": typing.NotRequired[float],
        "copy_ctab": typing.NotRequired[bool],
        "subjects_dir": typing.NotRequired[str],
        "gdiagno": typing.NotRequired[int],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSurf2volOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Output volume with resampled surface"""
    vertex_volume: OutputPathType | None
    """Vertex map volume indicating mapped voxels"""

def mri_surf2vol_params(
    outfile: str,
    surface_overlay: list[str] | None = None,
    ltafile: InputPathType | None = None,
    subject: str | None = None,
    ribbonfile: InputPathType | None = None,
    merge_volume: InputPathType | None = None,
    surface_values: str | None = None,
    mkmask: bool = False,
    hemi: str | None = None,
    surfname: str | None = None,
    projfrac: float | None = None,
    fill_ribbon: bool = False,
    fill_projfrac: str | None = None,
    reg_volume: InputPathType | None = None,
    identity: str | None = None,
    template_volume: InputPathType | None = None,
    fstal_res: str | None = None,
    vtxvol: InputPathType | None = None,
    flat2mri: str | None = None,
    sphpvf: str | None = None,
    mask_to_cortex: bool = False,
    mask_to_label: InputPathType | None = None,
    surface_mask: InputPathType | None = None,
    add_const: float | None = None,
    copy_ctab: bool = False,
    subjects_dir: str | None = None,
    gdiagno: int | None = None,
    version: bool = False,
    help: bool = False,
) -> MriSurf2vol:
    """
    Build parameters.

    Args:
        outfile: Path to output volume
        surface_overlay: Specify path to a surface and matching overlay
        ltafile: Specify registration file
        subject: Specify subject name
        ribbonfile: Specify path to ribbon file
        merge_volume: Merge with this volume, replacing surface values
        surface_values: Source of surface values, optionally with format
        mkmask: Create a binary mask instead of loading surfval
        hemi: Hemisphere for the surface values (lh or rh)
        surfname: Surface name in surf directory (default is white)
        projfrac: Fraction for thickness projection
        fill_ribbon: Fill the entire ribbon
        fill_projfrac: Fill ribbon by iterating projfrac
        reg_volume: Volume registration file
        identity: Use identity matrix for volume registration (requires subject\
            name)
        template_volume: Template volume for output configuration
        fstal_res: Use fs Talairach registration with specified resolution
        vtxvol: Vertex map volume path
        flat2mri: Options for flat surface to MRI mapping
        sphpvf: Spherical point to voxel function options
        mask_to_cortex: Mask to cortex label
        mask_to_label: Mask to specified label file
        surface_mask: Mask to specified surface mask
        add_const: Add constant value to each non-zero output voxel
        copy_ctab: Copy color table header
        subjects_dir: FreeSurfer subjects directory
        gdiagno: Set diagnostic level
        version: Print version and exit
        help: Display help information

    Returns:
        Parameter dictionary.
    """
    params: MriSurf2vol = {
        "@type": "freesurfer/mri_surf2vol",
        "outfile": outfile,
        "mkmask": mkmask,
        "fill_ribbon": fill_ribbon,
        "mask_to_cortex": mask_to_cortex,
        "copy_ctab": copy_ctab,
        "version": version,
        "help": help,
    }
    if surface_overlay is not None:
        params["surface_overlay"] = surface_overlay
    if ltafile is not None:
        params["ltafile"] = ltafile
    if subject is not None:
        params["subject"] = subject
    if ribbonfile is not None:
        params["ribbonfile"] = ribbonfile
    if merge_volume is not None:
        params["merge_volume"] = merge_volume
    if surface_values is not None:
        params["surface_values"] = surface_values
    if hemi is not None:
        params["hemi"] = hemi
    if surfname is not None:
        params["surfname"] = surfname
    if projfrac is not None:
        params["projfrac"] = projfrac
    if fill_projfrac is not None:
        params["fill_projfrac"] = fill_projfrac
    if reg_volume is not None:
        params["reg_volume"] = reg_volume
    if identity is not None:
        params["identity"] = identity
    if template_volume is not None:
        params["template_volume"] = template_volume
    if fstal_res is not None:
        params["fstal_res"] = fstal_res
    if vtxvol is not None:
        params["vtxvol"] = vtxvol
    if flat2mri is not None:
        params["flat2mri"] = flat2mri
    if sphpvf is not None:
        params["sphpvf"] = sphpvf
    if mask_to_label is not None:
        params["mask_to_label"] = mask_to_label
    if surface_mask is not None:
        params["surface_mask"] = surface_mask
    if add_const is not None:
        params["add_const"] = add_const
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if gdiagno is not None:
        params["gdiagno"] = gdiagno
    return params

def mri_surf2vol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSurf2vol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_overlay", None) is not None:
        if not isinstance(params["surface_overlay"], list):
            raise StyxValidationError(f'`surface_overlay` has the wrong type: Received `{type(params["surface_overlay"])}` expected `list[str]`')
        for e in params["surface_overlay"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`surface_overlay` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("ltafile", None) is not None:
        if not isinstance(params["ltafile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ltafile` has the wrong type: Received `{type(params["ltafile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("ribbonfile", None) is not None:
        if not isinstance(params["ribbonfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ribbonfile` has the wrong type: Received `{type(params["ribbonfile"])}` expected `InputPathType`')
    if params.get("merge_volume", None) is not None:
        if not isinstance(params["merge_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`merge_volume` has the wrong type: Received `{type(params["merge_volume"])}` expected `InputPathType`')
    if params.get("surface_values", None) is not None:
        if not isinstance(params["surface_values"], str):
            raise StyxValidationError(f'`surface_values` has the wrong type: Received `{type(params["surface_values"])}` expected `str`')
    if params.get("mkmask", None) is not None:
        if not isinstance(params["mkmask"], bool):
            raise StyxValidationError(f'`mkmask` has the wrong type: Received `{type(params["mkmask"])}` expected `bool`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surfname", None) is not None:
        if not isinstance(params["surfname"], str):
            raise StyxValidationError(f'`surfname` has the wrong type: Received `{type(params["surfname"])}` expected `str`')
    if params.get("projfrac", None) is not None:
        if not isinstance(params["projfrac"], (float, int)):
            raise StyxValidationError(f'`projfrac` has the wrong type: Received `{type(params["projfrac"])}` expected `float`')
    if params.get("fill_ribbon", None) is not None:
        if not isinstance(params["fill_ribbon"], bool):
            raise StyxValidationError(f'`fill_ribbon` has the wrong type: Received `{type(params["fill_ribbon"])}` expected `bool`')
    if params.get("fill_projfrac", None) is not None:
        if not isinstance(params["fill_projfrac"], str):
            raise StyxValidationError(f'`fill_projfrac` has the wrong type: Received `{type(params["fill_projfrac"])}` expected `str`')
    if params.get("reg_volume", None) is not None:
        if not isinstance(params["reg_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reg_volume` has the wrong type: Received `{type(params["reg_volume"])}` expected `InputPathType`')
    if params.get("identity", None) is not None:
        if not isinstance(params["identity"], str):
            raise StyxValidationError(f'`identity` has the wrong type: Received `{type(params["identity"])}` expected `str`')
    if params.get("template_volume", None) is not None:
        if not isinstance(params["template_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template_volume` has the wrong type: Received `{type(params["template_volume"])}` expected `InputPathType`')
    if params.get("fstal_res", None) is not None:
        if not isinstance(params["fstal_res"], str):
            raise StyxValidationError(f'`fstal_res` has the wrong type: Received `{type(params["fstal_res"])}` expected `str`')
    if params.get("vtxvol", None) is not None:
        if not isinstance(params["vtxvol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vtxvol` has the wrong type: Received `{type(params["vtxvol"])}` expected `InputPathType`')
    if params.get("flat2mri", None) is not None:
        if not isinstance(params["flat2mri"], str):
            raise StyxValidationError(f'`flat2mri` has the wrong type: Received `{type(params["flat2mri"])}` expected `str`')
    if params.get("sphpvf", None) is not None:
        if not isinstance(params["sphpvf"], str):
            raise StyxValidationError(f'`sphpvf` has the wrong type: Received `{type(params["sphpvf"])}` expected `str`')
    if params.get("mask_to_cortex", None) is not None:
        if not isinstance(params["mask_to_cortex"], bool):
            raise StyxValidationError(f'`mask_to_cortex` has the wrong type: Received `{type(params["mask_to_cortex"])}` expected `bool`')
    if params.get("mask_to_label", None) is not None:
        if not isinstance(params["mask_to_label"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_to_label` has the wrong type: Received `{type(params["mask_to_label"])}` expected `InputPathType`')
    if params.get("surface_mask", None) is not None:
        if not isinstance(params["surface_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surface_mask` has the wrong type: Received `{type(params["surface_mask"])}` expected `InputPathType`')
    if params.get("add_const", None) is not None:
        if not isinstance(params["add_const"], (float, int)):
            raise StyxValidationError(f'`add_const` has the wrong type: Received `{type(params["add_const"])}` expected `float`')
    if params.get("copy_ctab", None) is not None:
        if not isinstance(params["copy_ctab"], bool):
            raise StyxValidationError(f'`copy_ctab` has the wrong type: Received `{type(params["copy_ctab"])}` expected `bool`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("gdiagno", None) is not None:
        if not isinstance(params["gdiagno"], int):
            raise StyxValidationError(f'`gdiagno` has the wrong type: Received `{type(params["gdiagno"])}` expected `int`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_surf2vol_cargs(params: MriSurf2vol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_surf2vol")
    v_0 = params.get("surface_overlay")
    if v_0 is not None:
        cargs.append("--so")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("ltafile")
    if v_1 is not None:
        cargs.append("--lta")
        cargs.append(execution.input_file(v_1))
    cargs.append("--o")
    cargs.append(params["outfile"])
    v_2 = params.get("subject")
    if v_2 is not None:
        cargs.append("--subject")
        cargs.append(v_2)
    v_3 = params.get("ribbonfile")
    if v_3 is not None:
        cargs.append("--ribbon")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("merge_volume")
    if v_4 is not None:
        cargs.append("--merge")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("surface_values")
    if v_5 is not None:
        cargs.append("--surfval")
        cargs.append(v_5)
    if params.get("mkmask"):
        cargs.append("--mkmask")
    v_6 = params.get("hemi")
    if v_6 is not None:
        cargs.append("--hemi")
        cargs.append(v_6)
    v_7 = params.get("surfname")
    if v_7 is not None:
        cargs.append("--surf")
        cargs.append(v_7)
    v_8 = params.get("projfrac")
    if v_8 is not None:
        cargs.append("--projfrac")
        cargs.append(str(v_8))
    if params.get("fill_ribbon"):
        cargs.append("--fillribbon")
    v_9 = params.get("fill_projfrac")
    if v_9 is not None:
        cargs.append("--fill-projfrac")
        cargs.append(v_9)
    v_10 = params.get("reg_volume")
    if v_10 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("identity")
    if v_11 is not None:
        cargs.append("--identity")
        cargs.append(v_11)
    v_12 = params.get("template_volume")
    if v_12 is not None:
        cargs.append("--template")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("fstal_res")
    if v_13 is not None:
        cargs.append("--fstal")
        cargs.append(v_13)
    v_14 = params.get("vtxvol")
    if v_14 is not None:
        cargs.append("--vtxvol")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("flat2mri")
    if v_15 is not None:
        cargs.append("--flat2mri")
        cargs.append(v_15)
    v_16 = params.get("sphpvf")
    if v_16 is not None:
        cargs.append("--sphpvf")
        cargs.append(v_16)
    if params.get("mask_to_cortex"):
        cargs.append("--mask-to-cortex")
    v_17 = params.get("mask_to_label")
    if v_17 is not None:
        cargs.append("--mask-to-label")
        cargs.append(execution.input_file(v_17))
    v_18 = params.get("surface_mask")
    if v_18 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_18))
    v_19 = params.get("add_const")
    if v_19 is not None:
        cargs.append("--add")
        cargs.append(str(v_19))
    if params.get("copy_ctab"):
        cargs.append("--copy-ctab")
    v_20 = params.get("subjects_dir")
    if v_20 is not None:
        cargs.append("--sd")
        cargs.append(v_20)
    v_21 = params.get("gdiagno")
    if v_21 is not None:
        cargs.append("--gdiagno")
        cargs.append(str(v_21))
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_surf2vol_outputs(params: MriSurf2vol, execution: Execution) -> MriSurf2volOutputs:
    """Build the MriSurf2volOutputs object for this tool."""
    vertex_volume_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["outfile"])
    __v0 = params.get("vtxvol")
    if __v0 is not None:
        vertex_volume_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriSurf2volOutputs(
        root=root_v,
        output_volume=output_volume_v,
        vertex_volume=vertex_volume_v,
    )

def mri_surf2vol_execute(params: MriSurf2vol, runner: Runner | None = None) -> MriSurf2volOutputs:
    """
    mri_surf2vol

    Resamples a surface into a volume using one of two methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_surf2vol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SURF2VOL_METADATA)
    execution.params(params)
    args = mri_surf2vol_cargs(params, execution)
    out = mri_surf2vol_outputs(params, execution)
    execution.run(args)
    return out

def mri_surf2vol(
    outfile: str,
    surface_overlay: list[str] | None = None,
    ltafile: InputPathType | None = None,
    subject: str | None = None,
    ribbonfile: InputPathType | None = None,
    merge_volume: InputPathType | None = None,
    surface_values: str | None = None,
    mkmask: bool = False,
    hemi: str | None = None,
    surfname: str | None = None,
    projfrac: float | None = None,
    fill_ribbon: bool = False,
    fill_projfrac: str | None = None,
    reg_volume: InputPathType | None = None,
    identity: str | None = None,
    template_volume: InputPathType | None = None,
    fstal_res: str | None = None,
    vtxvol: InputPathType | None = None,
    flat2mri: str | None = None,
    sphpvf: str | None = None,
    mask_to_cortex: bool = False,
    mask_to_label: InputPathType | None = None,
    surface_mask: InputPathType | None = None,
    add_const: float | None = None,
    copy_ctab: bool = False,
    subjects_dir: str | None = None,
    gdiagno: int | None = None,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MriSurf2volOutputs:
    """
    mri_surf2vol

    Resamples a surface into a volume using one of two methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        outfile: Path to output volume
        surface_overlay: Specify path to a surface and matching overlay
        ltafile: Specify registration file
        subject: Specify subject name
        ribbonfile: Specify path to ribbon file
        merge_volume: Merge with this volume, replacing surface values
        surface_values: Source of surface values, optionally with format
        mkmask: Create a binary mask instead of loading surfval
        hemi: Hemisphere for the surface values (lh or rh)
        surfname: Surface name in surf directory (default is white)
        projfrac: Fraction for thickness projection
        fill_ribbon: Fill the entire ribbon
        fill_projfrac: Fill ribbon by iterating projfrac
        reg_volume: Volume registration file
        identity: Use identity matrix for volume registration (requires subject\
            name)
        template_volume: Template volume for output configuration
        fstal_res: Use fs Talairach registration with specified resolution
        vtxvol: Vertex map volume path
        flat2mri: Options for flat surface to MRI mapping
        sphpvf: Spherical point to voxel function options
        mask_to_cortex: Mask to cortex label
        mask_to_label: Mask to specified label file
        surface_mask: Mask to specified surface mask
        add_const: Add constant value to each non-zero output voxel
        copy_ctab: Copy color table header
        subjects_dir: FreeSurfer subjects directory
        gdiagno: Set diagnostic level
        version: Print version and exit
        help: Display help information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_surf2vol_params(
        outfile=outfile,
        surface_overlay=surface_overlay,
        ltafile=ltafile,
        subject=subject,
        ribbonfile=ribbonfile,
        merge_volume=merge_volume,
        surface_values=surface_values,
        mkmask=mkmask,
        hemi=hemi,
        surfname=surfname,
        projfrac=projfrac,
        fill_ribbon=fill_ribbon,
        fill_projfrac=fill_projfrac,
        reg_volume=reg_volume,
        identity=identity,
        template_volume=template_volume,
        fstal_res=fstal_res,
        vtxvol=vtxvol,
        flat2mri=flat2mri,
        sphpvf=sphpvf,
        mask_to_cortex=mask_to_cortex,
        mask_to_label=mask_to_label,
        surface_mask=surface_mask,
        add_const=add_const,
        copy_ctab=copy_ctab,
        subjects_dir=subjects_dir,
        gdiagno=gdiagno,
        version=version,
        help=help,
    )
    return mri_surf2vol_execute(params, runner)

__all__ = [
    "MriSurf2vol",
    "MriSurf2volOutputs",
    "MRI_SURF2VOL_METADATA",
    "mri_surf2vol_cargs",
    "mri_surf2vol_outputs",
    "mri_surf2vol_params",
    "mri_surf2vol_execute",
    "mri_surf2vol_validate",
    "mri_surf2vol",
]