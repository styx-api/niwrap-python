# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SRATIO_METADATA = Metadata(
    id="sratio",
    name="sratio",
    package="freesurfer",
)

Sratio = typing.TypedDict(
    "Sratio",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sratio"]],
        "value_a": float,
        "value_b": float,
        "abs_flag": typing.NotRequired[bool],
        "mask_threshold": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SratioOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sratio_result: OutputPathType
    """Result of the sratio operation"""

def sratio_params(
    value_a: float,
    value_b: float,
    abs_flag: bool = False,
    mask_threshold: float | None = None,
) -> Sratio:
    """
    Build parameters.

    Args:
        value_a: First value for the sratio operation
        value_b: Second value for the sratio operation
        abs_flag: Compute absolute value of both A and B before sratio
        mask_threshold: Threshold based on max(abs(A),abs(B)) > thresh

    Returns:
        Parameter dictionary.
    """
    params: Sratio = {
        "@type": "freesurfer/sratio",
        "value_a": value_a,
        "value_b": value_b,
        "abs_flag": abs_flag,
    }
    if mask_threshold is not None:
        params["mask_threshold"] = mask_threshold
    return params

def sratio_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sratio."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("value_a", None) is None:
        raise StyxValidationError("`value_a` must not be None")
    if not isinstance(params["value_a"], (float, int)):
        raise StyxValidationError(f'`value_a` has the wrong type: Received `{type(params["value_a"])}` expected `float`')
    if params.get("value_b", None) is None:
        raise StyxValidationError("`value_b` must not be None")
    if not isinstance(params["value_b"], (float, int)):
        raise StyxValidationError(f'`value_b` has the wrong type: Received `{type(params["value_b"])}` expected `float`')
    if params.get("abs_flag", None) is not None:
        if not isinstance(params["abs_flag"], bool):
            raise StyxValidationError(f'`abs_flag` has the wrong type: Received `{type(params["abs_flag"])}` expected `bool`')
    if params.get("mask_threshold", None) is not None:
        if not isinstance(params["mask_threshold"], (float, int)):
            raise StyxValidationError(f'`mask_threshold` has the wrong type: Received `{type(params["mask_threshold"])}` expected `float`')

def sratio_cargs(params: Sratio, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sratio")
    cargs.append(str(params["value_a"]))
    cargs.append(str(params["value_b"]))
    if params.get("abs_flag"):
        cargs.append("--abs")
    v_0 = params.get("mask_threshold")
    if v_0 is not None:
        cargs.append("--mask-thresh")
        cargs.append(str(v_0))
    return cargs

def sratio_outputs(params: Sratio, execution: Execution) -> SratioOutputs:
    """Build the SratioOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    sratio_result_v: OutputPathType = execution.output_file("[SRATIO_RESULT]")
    return SratioOutputs(
        root=root_v,
        sratio_result=sratio_result_v,
    )

def sratio_execute(params: Sratio, runner: Runner | None = None) -> SratioOutputs:
    """
    sratio

    Tool to compute ratio A/B if A>B, -B/A if B>A, with options for absolute computation and threshold masking.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sratio_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SRATIO_METADATA)
    execution.params(params)
    args = sratio_cargs(params, execution)
    out = sratio_outputs(params, execution)
    execution.run(args)
    return out

def sratio(
    value_a: float,
    value_b: float,
    abs_flag: bool = False,
    mask_threshold: float | None = None,
    runner: Runner | None = None,
) -> SratioOutputs:
    """
    sratio

    Tool to compute ratio A/B if A>B, -B/A if B>A, with options for absolute computation and threshold masking.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        value_a: First value for the sratio operation
        value_b: Second value for the sratio operation
        abs_flag: Compute absolute value of both A and B before sratio
        mask_threshold: Threshold based on max(abs(A),abs(B)) > thresh
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sratio_params(
        value_a=value_a,
        value_b=value_b,
        abs_flag=abs_flag,
        mask_threshold=mask_threshold,
    )
    return sratio_execute(params, runner)

__all__ = [
    "Sratio",
    "SratioOutputs",
    "SRATIO_METADATA",
    "sratio_cargs",
    "sratio_outputs",
    "sratio_params",
    "sratio_execute",
    "sratio_validate",
    "sratio",
]