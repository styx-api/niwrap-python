# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CONCATENATE_GCAM_METADATA = Metadata(
    id="mri_concatenate_gcam",
    name="mri_concatenate_gcam",
    package="freesurfer",
)

MriConcatenateGcam = typing.TypedDict(
    "MriConcatenateGcam",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_concatenate_gcam"]],
        "inputs": list[InputPathType],
        "output": str,
        "source_image": typing.NotRequired[InputPathType],
        "target_image": typing.NotRequired[InputPathType],
        "reduce": typing.NotRequired[bool],
        "invert": typing.NotRequired[bool],
        "downsample": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriConcatenateGcamOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Composite LTA or M3Z transform, depending on input."""

def mri_concatenate_gcam_params(
    inputs: list[InputPathType],
    output: str,
    source_image: InputPathType | None = None,
    target_image: InputPathType | None = None,
    reduce: bool = False,
    invert: bool = False,
    downsample: bool = False,
) -> MriConcatenateGcam:
    """
    Build parameters.

    Args:
        inputs: Combination of input LTAs and M3Zs.
        output: Concatenated output transform, saved as an LTA or M3Z depending on\
            the input transforms.
        source_image: Change source image geometry of output M3Z, useful for GCAM\
            inversion if the path of the original source volume changed.
        target_image: Change destination image geometry of output M3Z.
        reduce: Reduce output LTA to single LT.
        invert: Invert the output transform.
        downsample: Downsample output M3Z to spacing of 2; by default, the output\
            spacing is that of the rightmost input M3Z.

    Returns:
        Parameter dictionary.
    """
    params: MriConcatenateGcam = {
        "@type": "freesurfer/mri_concatenate_gcam",
        "inputs": inputs,
        "output": output,
        "reduce": reduce,
        "invert": invert,
        "downsample": downsample,
    }
    if source_image is not None:
        params["source_image"] = source_image
    if target_image is not None:
        params["target_image"] = target_image
    return params

def mri_concatenate_gcam_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriConcatenateGcam."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputs", None) is None:
        raise StyxValidationError("`inputs` must not be None")
    if not isinstance(params["inputs"], list):
        raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(params["inputs"])}` expected `list[InputPathType]`')
    for e in params["inputs"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`inputs` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("source_image", None) is not None:
        if not isinstance(params["source_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`source_image` has the wrong type: Received `{type(params["source_image"])}` expected `InputPathType`')
    if params.get("target_image", None) is not None:
        if not isinstance(params["target_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(params["target_image"])}` expected `InputPathType`')
    if params.get("reduce", None) is not None:
        if not isinstance(params["reduce"], bool):
            raise StyxValidationError(f'`reduce` has the wrong type: Received `{type(params["reduce"])}` expected `bool`')
    if params.get("invert", None) is not None:
        if not isinstance(params["invert"], bool):
            raise StyxValidationError(f'`invert` has the wrong type: Received `{type(params["invert"])}` expected `bool`')
    if params.get("downsample", None) is not None:
        if not isinstance(params["downsample"], bool):
            raise StyxValidationError(f'`downsample` has the wrong type: Received `{type(params["downsample"])}` expected `bool`')

def mri_concatenate_gcam_cargs(params: MriConcatenateGcam, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_concatenate_gcam")
    for item0 in params["inputs"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output"])
    v_0 = params.get("source_image")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("target_image")
    if v_1 is not None:
        cargs.append("-t")
        cargs.append(execution.input_file(v_1))
    if params.get("reduce"):
        cargs.append("-r")
    if params.get("invert"):
        cargs.append("-i")
    if params.get("downsample"):
        cargs.append("-d")
    return cargs

def mri_concatenate_gcam_outputs(params: MriConcatenateGcam, execution: Execution) -> MriConcatenateGcamOutputs:
    """Build the MriConcatenateGcamOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output"])
    return MriConcatenateGcamOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_concatenate_gcam_execute(params: MriConcatenateGcam, runner: Runner | None = None) -> MriConcatenateGcamOutputs:
    """
    mri_concatenate_gcam

    Concatenate a combination of input LTAs (linear transform array) and GCAMs (Gaussian classifier atlas, M3Z).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_concatenate_gcam_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CONCATENATE_GCAM_METADATA)
    execution.params(params)
    args = mri_concatenate_gcam_cargs(params, execution)
    out = mri_concatenate_gcam_outputs(params, execution)
    execution.run(args)
    return out

def mri_concatenate_gcam(
    inputs: list[InputPathType],
    output: str,
    source_image: InputPathType | None = None,
    target_image: InputPathType | None = None,
    reduce: bool = False,
    invert: bool = False,
    downsample: bool = False,
    runner: Runner | None = None,
) -> MriConcatenateGcamOutputs:
    """
    mri_concatenate_gcam

    Concatenate a combination of input LTAs (linear transform array) and GCAMs (Gaussian classifier atlas, M3Z).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inputs: Combination of input LTAs and M3Zs.
        output: Concatenated output transform, saved as an LTA or M3Z depending on\
            the input transforms.
        source_image: Change source image geometry of output M3Z, useful for GCAM\
            inversion if the path of the original source volume changed.
        target_image: Change destination image geometry of output M3Z.
        reduce: Reduce output LTA to single LT.
        invert: Invert the output transform.
        downsample: Downsample output M3Z to spacing of 2; by default, the output\
            spacing is that of the rightmost input M3Z.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_concatenate_gcam_params(
        inputs=inputs,
        output=output,
        source_image=source_image,
        target_image=target_image,
        reduce=reduce,
        invert=invert,
        downsample=downsample,
    )
    return mri_concatenate_gcam_execute(params, runner)

__all__ = [
    "MriConcatenateGcam",
    "MriConcatenateGcamOutputs",
    "MRI_CONCATENATE_GCAM_METADATA",
    "mri_concatenate_gcam_cargs",
    "mri_concatenate_gcam_outputs",
    "mri_concatenate_gcam_params",
    "mri_concatenate_gcam_execute",
    "mri_concatenate_gcam_validate",
    "mri_concatenate_gcam",
]