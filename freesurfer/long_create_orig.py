# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_CREATE_ORIG_METADATA = Metadata(
    id="long_create_orig",
    name="long_create_orig",
    package="freesurfer",
)

LongCreateOrig = typing.TypedDict(
    "LongCreateOrig",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_create_orig"]],
        "base_id": str,
        "tp_id": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LongCreateOrigOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType | None
    """Output directory within <SUBJECTS_DIR> for processed time point data."""

def long_create_orig_params(
    base_id: str,
    tp_id: str | None = None,
) -> LongCreateOrig:
    """
    Build parameters.

    Args:
        base_id: Base ID to be utilized in the process.
        tp_id: Time point ID. If omitted, operates on all time points in base.

    Returns:
        Parameter dictionary.
    """
    params: LongCreateOrig = {
        "@type": "freesurfer/long_create_orig",
        "base_id": base_id,
    }
    if tp_id is not None:
        params["tp_id"] = tp_id
    return params

def long_create_orig_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongCreateOrig."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_id", None) is None:
        raise StyxValidationError("`base_id` must not be None")
    if not isinstance(params["base_id"], str):
        raise StyxValidationError(f'`base_id` has the wrong type: Received `{type(params["base_id"])}` expected `str`')
    if params.get("tp_id", None) is not None:
        if not isinstance(params["tp_id"], str):
            raise StyxValidationError(f'`tp_id` has the wrong type: Received `{type(params["tp_id"])}` expected `str`')

def long_create_orig_cargs(params: LongCreateOrig, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_create_orig")
    cargs.append(params["base_id"])
    v_0 = params.get("tp_id")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def long_create_orig_outputs(params: LongCreateOrig, execution: Execution) -> LongCreateOrigOutputs:
    """Build the LongCreateOrigOutputs object for this tool."""
    output_directory_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("tp_id")
    if __v0 is not None:
        output_directory_v = execution.output_file(f'{params["base_id"]}/longtp/{(__v0 if __v0 is not None else "")}')
    return LongCreateOrigOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def long_create_orig_execute(params: LongCreateOrig, runner: Runner | None = None) -> LongCreateOrigOutputs:
    """
    long_create_orig

    Maps, conforms and averages (motioncorrect) raw inputs from cross-sectional directory to base space in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_create_orig_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_CREATE_ORIG_METADATA)
    execution.params(params)
    args = long_create_orig_cargs(params, execution)
    out = long_create_orig_outputs(params, execution)
    execution.run(args)
    return out

def long_create_orig(
    base_id: str,
    tp_id: str | None = None,
    runner: Runner | None = None,
) -> LongCreateOrigOutputs:
    """
    long_create_orig

    Maps, conforms and averages (motioncorrect) raw inputs from cross-sectional directory to base space in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_id: Base ID to be utilized in the process.
        tp_id: Time point ID. If omitted, operates on all time points in base.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_create_orig_params(
        base_id=base_id,
        tp_id=tp_id,
    )
    return long_create_orig_execute(params, runner)

__all__ = [
    "LongCreateOrig",
    "LongCreateOrigOutputs",
    "LONG_CREATE_ORIG_METADATA",
    "long_create_orig_cargs",
    "long_create_orig_outputs",
    "long_create_orig_params",
    "long_create_orig_execute",
    "long_create_orig_validate",
    "long_create_orig",
]