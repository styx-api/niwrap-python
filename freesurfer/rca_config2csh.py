# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCA_CONFIG2CSH_METADATA = Metadata(
    id="rca-config2csh",
    name="rca-config2csh",
    package="freesurfer",
)

RcaConfig2csh = typing.TypedDict(
    "RcaConfig2csh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rca-config2csh"]],
        "configfile": InputPathType,
    },
)

@dataclasses.dataclass
class RcaConfig2cshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rca_config2csh_params(
    configfile: InputPathType,
) -> RcaConfig2csh:
    """
    Build parameters.

    Args:
        configfile:

    Returns:
        Parameter dictionary.
    """
    params: RcaConfig2csh = {
        "@type": "freesurfer/rca-config2csh",
        "configfile": configfile,
    }
    return params

def rca_config2csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcaConfig2csh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("configfile", None) is None:
        raise StyxValidationError("`configfile` must not be None")
    if not isinstance(params["configfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`configfile` has the wrong type: Received `{type(params["configfile"])}` expected `InputPathType`')

def rca_config2csh_cargs(params: RcaConfig2csh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rca-config2csh")
    cargs.append("-config2csh")
    cargs.append(execution.input_file(params["configfile"]))
    return cargs

def rca_config2csh_outputs(params: RcaConfig2csh, execution: Execution) -> RcaConfig2cshOutputs:
    """Build the RcaConfig2cshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RcaConfig2cshOutputs(
        root=root_v,
    )

def rca_config2csh_execute(params: RcaConfig2csh, runner: Runner | None = None) -> RcaConfig2cshOutputs:
    """
    rca-config2csh

    rca-config2csh is a utility to convert configuration files into C-shell syntax.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rca_config2csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCA_CONFIG2CSH_METADATA)
    execution.params(params)
    args = rca_config2csh_cargs(params, execution)
    out = rca_config2csh_outputs(params, execution)
    execution.run(args)
    return out

def rca_config2csh(
    configfile: InputPathType,
    runner: Runner | None = None,
) -> RcaConfig2cshOutputs:
    """
    rca-config2csh

    rca-config2csh is a utility to convert configuration files into C-shell syntax.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        configfile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rca_config2csh_params(
        configfile=configfile,
    )
    return rca_config2csh_execute(params, runner)

__all__ = [
    "RcaConfig2csh",
    "RcaConfig2cshOutputs",
    "RCA_CONFIG2CSH_METADATA",
    "rca_config2csh_cargs",
    "rca_config2csh_outputs",
    "rca_config2csh_params",
    "rca_config2csh_execute",
    "rca_config2csh_validate",
    "rca_config2csh",
]