# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LISTSUBJ_METADATA = Metadata(
    id="listsubj",
    name="listsubj",
    package="freesurfer",
)

Listsubj = typing.TypedDict(
    "Listsubj",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/listsubj"]],
        "subject_dir": str,
        "cross": typing.NotRequired[bool],
        "base": typing.NotRequired[bool],
        "long": typing.NotRequired[bool],
        "done": typing.NotRequired[bool],
        "error": typing.NotRequired[bool],
        "running": typing.NotRequired[bool],
        "full_path": typing.NotRequired[bool],
        "count": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ListsubjOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def listsubj_params(
    subject_dir: str,
    cross: bool = False,
    base: bool = False,
    long: bool = False,
    done: bool = False,
    error: bool = False,
    running: bool = False,
    full_path: bool = False,
    count: bool = False,
    help: bool = False,
) -> Listsubj:
    """
    Build parameters.

    Args:
        subject_dir: Directory where subjects are stored
        cross: Only cross-sectional stream
        base: Only base stream
        long: Only longitudinal stream
        done: Only subject IDs with scripts/recon-all.done
        error: Only subject IDs with scripts/recon-all.error
        running: Only subject IDs with scripts/IsRunning.?h
        full_path: Prepend full absolute path
        count: Print number of subjects found
        help: Show this help text

    Returns:
        Parameter dictionary.
    """
    params: Listsubj = {
        "@type": "freesurfer/listsubj",
        "subject_dir": subject_dir,
        "cross": cross,
        "base": base,
        "long": long,
        "done": done,
        "error": error,
        "running": running,
        "full_path": full_path,
        "count": count,
        "help": help,
    }
    return params

def listsubj_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Listsubj."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("cross", None) is not None:
        if not isinstance(params["cross"], bool):
            raise StyxValidationError(f'`cross` has the wrong type: Received `{type(params["cross"])}` expected `bool`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], bool):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `bool`')
    if params.get("long", None) is not None:
        if not isinstance(params["long"], bool):
            raise StyxValidationError(f'`long` has the wrong type: Received `{type(params["long"])}` expected `bool`')
    if params.get("done", None) is not None:
        if not isinstance(params["done"], bool):
            raise StyxValidationError(f'`done` has the wrong type: Received `{type(params["done"])}` expected `bool`')
    if params.get("error", None) is not None:
        if not isinstance(params["error"], bool):
            raise StyxValidationError(f'`error` has the wrong type: Received `{type(params["error"])}` expected `bool`')
    if params.get("running", None) is not None:
        if not isinstance(params["running"], bool):
            raise StyxValidationError(f'`running` has the wrong type: Received `{type(params["running"])}` expected `bool`')
    if params.get("full_path", None) is not None:
        if not isinstance(params["full_path"], bool):
            raise StyxValidationError(f'`full_path` has the wrong type: Received `{type(params["full_path"])}` expected `bool`')
    if params.get("count", None) is not None:
        if not isinstance(params["count"], bool):
            raise StyxValidationError(f'`count` has the wrong type: Received `{type(params["count"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def listsubj_cargs(params: Listsubj, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("listsubj")
    cargs.append(params["subject_dir"])
    if params.get("cross"):
        cargs.append("-c")
    if params.get("base"):
        cargs.append("-b")
    if params.get("long"):
        cargs.append("-l")
    if params.get("done"):
        cargs.append("-d")
    if params.get("error"):
        cargs.append("-e")
    if params.get("running"):
        cargs.append("-r")
    if params.get("full_path"):
        cargs.append("-f")
    if params.get("count"):
        cargs.append("-n")
    if params.get("help"):
        cargs.append("-h")
    return cargs

def listsubj_outputs(params: Listsubj, execution: Execution) -> ListsubjOutputs:
    """Build the ListsubjOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ListsubjOutputs(
        root=root_v,
    )

def listsubj_execute(params: Listsubj, runner: Runner | None = None) -> ListsubjOutputs:
    """
    listsubj

    List subject IDs in SUBJECT_DIR

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    listsubj_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LISTSUBJ_METADATA)
    execution.params(params)
    args = listsubj_cargs(params, execution)
    out = listsubj_outputs(params, execution)
    execution.run(args)
    return out

def listsubj(
    subject_dir: str,
    cross: bool = False,
    base: bool = False,
    long: bool = False,
    done: bool = False,
    error: bool = False,
    running: bool = False,
    full_path: bool = False,
    count: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> ListsubjOutputs:
    """
    listsubj

    List subject IDs in SUBJECT_DIR

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Directory where subjects are stored
        cross: Only cross-sectional stream
        base: Only base stream
        long: Only longitudinal stream
        done: Only subject IDs with scripts/recon-all.done
        error: Only subject IDs with scripts/recon-all.error
        running: Only subject IDs with scripts/IsRunning.?h
        full_path: Prepend full absolute path
        count: Print number of subjects found
        help: Show this help text
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = listsubj_params(
        subject_dir=subject_dir,
        cross=cross,
        base=base,
        long=long,
        done=done,
        error=error,
        running=running,
        full_path=full_path,
        count=count,
        help=help,
    )
    return listsubj_execute(params, runner)

__all__ = [
    "Listsubj",
    "ListsubjOutputs",
    "LISTSUBJ_METADATA",
    "listsubj_cargs",
    "listsubj_outputs",
    "listsubj_params",
    "listsubj_execute",
    "listsubj_validate",
    "listsubj",
]