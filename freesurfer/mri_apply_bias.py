# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_APPLY_BIAS_METADATA = Metadata(
    id="mri_apply_bias",
    name="mri_apply_bias",
    package="freesurfer",
)

MriApplyBias = typing.TypedDict(
    "MriApplyBias",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_apply_bias"]],
        "input_volume": InputPathType,
        "bias_volume": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriApplyBiasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Output volume after applying bias"""

def mri_apply_bias_params(
    input_volume: InputPathType,
    bias_volume: InputPathType,
    output_volume: str,
) -> MriApplyBias:
    """
    Build parameters.

    Args:
        input_volume: The input volume file
        bias_volume: The bias volume file
        output_volume: The output volume file

    Returns:
        Parameter dictionary.
    """
    params: MriApplyBias = {
        "@type": "freesurfer/mri_apply_bias",
        "input_volume": input_volume,
        "bias_volume": bias_volume,
        "output_volume": output_volume,
    }
    return params

def mri_apply_bias_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriApplyBias."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("bias_volume", None) is None:
        raise StyxValidationError("`bias_volume` must not be None")
    if not isinstance(params["bias_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bias_volume` has the wrong type: Received `{type(params["bias_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_apply_bias_cargs(params: MriApplyBias, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_apply_bias")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["bias_volume"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_apply_bias_outputs(params: MriApplyBias, execution: Execution) -> MriApplyBiasOutputs:
    """Build the MriApplyBiasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriApplyBiasOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_apply_bias_execute(params: MriApplyBias, runner: Runner | None = None) -> MriApplyBiasOutputs:
    """
    mri_apply_bias

    A tool for applying a bias volume to an input volume to produce an output volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_apply_bias_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_APPLY_BIAS_METADATA)
    execution.params(params)
    args = mri_apply_bias_cargs(params, execution)
    out = mri_apply_bias_outputs(params, execution)
    execution.run(args)
    return out

def mri_apply_bias(
    input_volume: InputPathType,
    bias_volume: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriApplyBiasOutputs:
    """
    mri_apply_bias

    A tool for applying a bias volume to an input volume to produce an output volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: The input volume file
        bias_volume: The bias volume file
        output_volume: The output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_apply_bias_params(
        input_volume=input_volume,
        bias_volume=bias_volume,
        output_volume=output_volume,
    )
    return mri_apply_bias_execute(params, runner)

__all__ = [
    "MriApplyBias",
    "MriApplyBiasOutputs",
    "MRI_APPLY_BIAS_METADATA",
    "mri_apply_bias_cargs",
    "mri_apply_bias_outputs",
    "mri_apply_bias_params",
    "mri_apply_bias_execute",
    "mri_apply_bias_validate",
    "mri_apply_bias",
]