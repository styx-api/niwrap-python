# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_JACOBIAN_METADATA = Metadata(
    id="mris_jacobian",
    name="mris_jacobian",
    package="freesurfer",
)

MrisJacobian = typing.TypedDict(
    "MrisJacobian",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_jacobian"]],
        "original_surface": InputPathType,
        "mapped_surface": InputPathType,
        "jacobian_file": str,
        "log": typing.NotRequired[bool],
        "noscale": typing.NotRequired[bool],
        "invert": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisJacobianOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_jacobian_file: OutputPathType
    """Output file containing the Jacobian of the surface mapping."""

def mris_jacobian_params(
    original_surface: InputPathType,
    mapped_surface: InputPathType,
    jacobian_file: str,
    log: bool = False,
    noscale: bool = False,
    invert: bool = False,
) -> MrisJacobian:
    """
    Build parameters.

    Args:
        original_surface: The original surface file.
        mapped_surface: The mapped surface file.
        jacobian_file: The output file name for the Jacobian.
        log: Compute and write out log of Jacobian.
        noscale: Don't scale Jacobian by total surface areas.
        invert: Compute -1/Jacobian for Jacobian < 1.

    Returns:
        Parameter dictionary.
    """
    params: MrisJacobian = {
        "@type": "freesurfer/mris_jacobian",
        "original_surface": original_surface,
        "mapped_surface": mapped_surface,
        "jacobian_file": jacobian_file,
        "log": log,
        "noscale": noscale,
        "invert": invert,
    }
    return params

def mris_jacobian_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisJacobian."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("original_surface", None) is None:
        raise StyxValidationError("`original_surface` must not be None")
    if not isinstance(params["original_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`original_surface` has the wrong type: Received `{type(params["original_surface"])}` expected `InputPathType`')
    if params.get("mapped_surface", None) is None:
        raise StyxValidationError("`mapped_surface` must not be None")
    if not isinstance(params["mapped_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mapped_surface` has the wrong type: Received `{type(params["mapped_surface"])}` expected `InputPathType`')
    if params.get("jacobian_file", None) is None:
        raise StyxValidationError("`jacobian_file` must not be None")
    if not isinstance(params["jacobian_file"], str):
        raise StyxValidationError(f'`jacobian_file` has the wrong type: Received `{type(params["jacobian_file"])}` expected `str`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], bool):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `bool`')
    if params.get("noscale", None) is not None:
        if not isinstance(params["noscale"], bool):
            raise StyxValidationError(f'`noscale` has the wrong type: Received `{type(params["noscale"])}` expected `bool`')
    if params.get("invert", None) is not None:
        if not isinstance(params["invert"], bool):
            raise StyxValidationError(f'`invert` has the wrong type: Received `{type(params["invert"])}` expected `bool`')

def mris_jacobian_cargs(params: MrisJacobian, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_jacobian")
    cargs.append(execution.input_file(params["original_surface"]))
    cargs.append(execution.input_file(params["mapped_surface"]))
    cargs.append(params["jacobian_file"])
    if params.get("log"):
        cargs.append("-log")
    if params.get("noscale"):
        cargs.append("-noscale")
    if params.get("invert"):
        cargs.append("-invert")
    return cargs

def mris_jacobian_outputs(params: MrisJacobian, execution: Execution) -> MrisJacobianOutputs:
    """Build the MrisJacobianOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_jacobian_file_v: OutputPathType = execution.output_file(params["jacobian_file"])
    return MrisJacobianOutputs(
        root=root_v,
        output_jacobian_file=output_jacobian_file_v,
    )

def mris_jacobian_execute(params: MrisJacobian, runner: Runner | None = None) -> MrisJacobianOutputs:
    """
    mris_jacobian

    This program computes the Jacobian of a surface mapping.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_jacobian_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_JACOBIAN_METADATA)
    execution.params(params)
    args = mris_jacobian_cargs(params, execution)
    out = mris_jacobian_outputs(params, execution)
    execution.run(args)
    return out

def mris_jacobian(
    original_surface: InputPathType,
    mapped_surface: InputPathType,
    jacobian_file: str,
    log: bool = False,
    noscale: bool = False,
    invert: bool = False,
    runner: Runner | None = None,
) -> MrisJacobianOutputs:
    """
    mris_jacobian

    This program computes the Jacobian of a surface mapping.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        original_surface: The original surface file.
        mapped_surface: The mapped surface file.
        jacobian_file: The output file name for the Jacobian.
        log: Compute and write out log of Jacobian.
        noscale: Don't scale Jacobian by total surface areas.
        invert: Compute -1/Jacobian for Jacobian < 1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_jacobian_params(
        original_surface=original_surface,
        mapped_surface=mapped_surface,
        jacobian_file=jacobian_file,
        log=log,
        noscale=noscale,
        invert=invert,
    )
    return mris_jacobian_execute(params, runner)

__all__ = [
    "MrisJacobian",
    "MrisJacobianOutputs",
    "MRIS_JACOBIAN_METADATA",
    "mris_jacobian_cargs",
    "mris_jacobian_outputs",
    "mris_jacobian_params",
    "mris_jacobian_execute",
    "mris_jacobian_validate",
    "mris_jacobian",
]