# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSVGLRUN_METADATA = Metadata(
    id="fsvglrun",
    name="fsvglrun",
    package="freesurfer",
)

Fsvglrun = typing.TypedDict(
    "Fsvglrun",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsvglrun"]],
        "zeroth_arg_name": typing.NotRequired[str],
        "empty_env": typing.NotRequired[bool],
        "dashed_arg": typing.NotRequired[bool],
        "command": str,
        "command_args": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class FsvglrunOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fsvglrun_params(
    command: str,
    zeroth_arg_name: str | None = None,
    empty_env: bool = False,
    dashed_arg: bool = False,
    command_args: list[str] | None = None,
) -> Fsvglrun:
    """
    Build parameters.

    Args:
        command: The command to be executed, replacing the shell with the specified\
            program
        zeroth_arg_name: Pass NAME as the zeroth argument to COMMAND
        empty_env: Execute COMMAND with an empty environment
        dashed_arg: Place a dash in the zeroth argument to COMMAND
        command_args: Arguments for the command executed by fsvglrun

    Returns:
        Parameter dictionary.
    """
    params: Fsvglrun = {
        "@type": "freesurfer/fsvglrun",
        "command": command,
        "empty_env": empty_env,
        "dashed_arg": dashed_arg,
    }
    if zeroth_arg_name is not None:
        params["zeroth_arg_name"] = zeroth_arg_name
    if command_args is not None:
        params["command_args"] = command_args
    return params

def fsvglrun_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fsvglrun."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("zeroth_arg_name", None) is not None:
        if not isinstance(params["zeroth_arg_name"], str):
            raise StyxValidationError(f'`zeroth_arg_name` has the wrong type: Received `{type(params["zeroth_arg_name"])}` expected `str`')
    if params.get("empty_env", None) is not None:
        if not isinstance(params["empty_env"], bool):
            raise StyxValidationError(f'`empty_env` has the wrong type: Received `{type(params["empty_env"])}` expected `bool`')
    if params.get("dashed_arg", None) is not None:
        if not isinstance(params["dashed_arg"], bool):
            raise StyxValidationError(f'`dashed_arg` has the wrong type: Received `{type(params["dashed_arg"])}` expected `bool`')
    if params.get("command", None) is None:
        raise StyxValidationError("`command` must not be None")
    if not isinstance(params["command"], str):
        raise StyxValidationError(f'`command` has the wrong type: Received `{type(params["command"])}` expected `str`')
    if params.get("command_args", None) is not None:
        if not isinstance(params["command_args"], list):
            raise StyxValidationError(f'`command_args` has the wrong type: Received `{type(params["command_args"])}` expected `list[str]`')
        for e in params["command_args"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`command_args` has the wrong type: Received `{type(e)}` expected `list[str]`')

def fsvglrun_cargs(params: Fsvglrun, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsvglrun")
    v_0 = params.get("zeroth_arg_name")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(v_0)
    if params.get("empty_env"):
        cargs.append("-c")
    if params.get("dashed_arg"):
        cargs.append("-l")
    cargs.append(params["command"])
    v_1 = params.get("command_args")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append(item0)
    return cargs

def fsvglrun_outputs(params: Fsvglrun, execution: Execution) -> FsvglrunOutputs:
    """Build the FsvglrunOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsvglrunOutputs(
        root=root_v,
    )

def fsvglrun_execute(params: Fsvglrun, runner: Runner | None = None) -> FsvglrunOutputs:
    """
    fsvglrun

    A tool to execute a command, replacing the shell with the specified program, typically used within FreeSurfer environment.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsvglrun_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSVGLRUN_METADATA)
    execution.params(params)
    args = fsvglrun_cargs(params, execution)
    out = fsvglrun_outputs(params, execution)
    execution.run(args)
    return out

def fsvglrun(
    command: str,
    zeroth_arg_name: str | None = None,
    empty_env: bool = False,
    dashed_arg: bool = False,
    command_args: list[str] | None = None,
    runner: Runner | None = None,
) -> FsvglrunOutputs:
    """
    fsvglrun

    A tool to execute a command, replacing the shell with the specified program, typically used within FreeSurfer environment.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        command: The command to be executed, replacing the shell with the specified\
            program
        zeroth_arg_name: Pass NAME as the zeroth argument to COMMAND
        empty_env: Execute COMMAND with an empty environment
        dashed_arg: Place a dash in the zeroth argument to COMMAND
        command_args: Arguments for the command executed by fsvglrun
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsvglrun_params(
        command=command,
        zeroth_arg_name=zeroth_arg_name,
        empty_env=empty_env,
        dashed_arg=dashed_arg,
        command_args=command_args,
    )
    return fsvglrun_execute(params, runner)

__all__ = [
    "Fsvglrun",
    "FsvglrunOutputs",
    "FSVGLRUN_METADATA",
    "fsvglrun_cargs",
    "fsvglrun_outputs",
    "fsvglrun_params",
    "fsvglrun_execute",
    "fsvglrun_validate",
    "fsvglrun",
]