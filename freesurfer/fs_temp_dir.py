# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_TEMP_DIR_METADATA = Metadata(
    id="fs_temp_dir",
    name="fs_temp_dir",
    package="freesurfer",
)

FsTempDir = typing.TypedDict(
    "FsTempDir",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_temp_dir"]],
        "base_directory": typing.NotRequired[str],
        "scratch": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsTempDirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType
    """The created temporary directory path"""

def fs_temp_dir_params(
    base_directory: str | None = None,
    scratch: bool = False,
) -> FsTempDir:
    """
    Build parameters.

    Args:
        base_directory: Manually specify base temporary directory.
        scratch: Use /scratch directory if available, but FS_TMPDIR takes priority.

    Returns:
        Parameter dictionary.
    """
    params: FsTempDir = {
        "@type": "freesurfer/fs_temp_dir",
        "scratch": scratch,
    }
    if base_directory is not None:
        params["base_directory"] = base_directory
    return params

def fs_temp_dir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsTempDir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_directory", None) is not None:
        if not isinstance(params["base_directory"], str):
            raise StyxValidationError(f'`base_directory` has the wrong type: Received `{type(params["base_directory"])}` expected `str`')
    if params.get("scratch", None) is not None:
        if not isinstance(params["scratch"], bool):
            raise StyxValidationError(f'`scratch` has the wrong type: Received `{type(params["scratch"])}` expected `bool`')

def fs_temp_dir_cargs(params: FsTempDir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_temp_dir")
    v_0 = params.get("base_directory")
    if v_0 is not None:
        cargs.append("-b")
        cargs.append(v_0)
    if params.get("scratch"):
        cargs.append("--scratch")
    return cargs

def fs_temp_dir_outputs(params: FsTempDir, execution: Execution) -> FsTempDirOutputs:
    """Build the FsTempDirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_directory_v: OutputPathType = execution.output_file("/tmp/tmp.SF5J66")
    return FsTempDirOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def fs_temp_dir_execute(params: FsTempDir, runner: Runner | None = None) -> FsTempDirOutputs:
    """
    fs_temp_dir

    Generates and creates an empty temporary directory

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_temp_dir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_TEMP_DIR_METADATA)
    execution.params(params)
    args = fs_temp_dir_cargs(params, execution)
    out = fs_temp_dir_outputs(params, execution)
    execution.run(args)
    return out

def fs_temp_dir(
    base_directory: str | None = None,
    scratch: bool = False,
    runner: Runner | None = None,
) -> FsTempDirOutputs:
    """
    fs_temp_dir

    Generates and creates an empty temporary directory

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_directory: Manually specify base temporary directory.
        scratch: Use /scratch directory if available, but FS_TMPDIR takes priority.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_temp_dir_params(
        base_directory=base_directory,
        scratch=scratch,
    )
    return fs_temp_dir_execute(params, runner)

__all__ = [
    "FsTempDir",
    "FsTempDirOutputs",
    "FS_TEMP_DIR_METADATA",
    "fs_temp_dir_cargs",
    "fs_temp_dir_outputs",
    "fs_temp_dir_params",
    "fs_temp_dir_execute",
    "fs_temp_dir_validate",
    "fs_temp_dir",
]