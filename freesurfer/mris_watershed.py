# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_WATERSHED_METADATA = Metadata(
    id="mris_watershed",
    name="mris_watershed",
    package="freesurfer",
)

MrisWatershed = typing.TypedDict(
    "MrisWatershed",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_watershed"]],
        "input_surface": InputPathType,
        "input_gradient_field": InputPathType,
        "output_annotation": str,
        "max_clusters": typing.NotRequired[float],
        "mask_label": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisWatershedOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_annotation_file: OutputPathType
    """The resulting annotation file from the watershed transform"""

def mris_watershed_params(
    input_surface: InputPathType,
    input_gradient_field: InputPathType,
    output_annotation: str,
    max_clusters: float | None = None,
    mask_label: str | None = None,
) -> MrisWatershed:
    """
    Build parameters.

    Args:
        input_surface: Input surface file
        input_gradient_field: Input gradient field file
        output_annotation: Output annotation file
        max_clusters: Set the number of maximum clusters
        mask_label: Read in and mask the input volume that is not in the specified\
            label

    Returns:
        Parameter dictionary.
    """
    params: MrisWatershed = {
        "@type": "freesurfer/mris_watershed",
        "input_surface": input_surface,
        "input_gradient_field": input_gradient_field,
        "output_annotation": output_annotation,
    }
    if max_clusters is not None:
        params["max_clusters"] = max_clusters
    if mask_label is not None:
        params["mask_label"] = mask_label
    return params

def mris_watershed_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisWatershed."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("input_gradient_field", None) is None:
        raise StyxValidationError("`input_gradient_field` must not be None")
    if not isinstance(params["input_gradient_field"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_gradient_field` has the wrong type: Received `{type(params["input_gradient_field"])}` expected `InputPathType`')
    if params.get("output_annotation", None) is None:
        raise StyxValidationError("`output_annotation` must not be None")
    if not isinstance(params["output_annotation"], str):
        raise StyxValidationError(f'`output_annotation` has the wrong type: Received `{type(params["output_annotation"])}` expected `str`')
    if params.get("max_clusters", None) is not None:
        if not isinstance(params["max_clusters"], (float, int)):
            raise StyxValidationError(f'`max_clusters` has the wrong type: Received `{type(params["max_clusters"])}` expected `float`')
    if params.get("mask_label", None) is not None:
        if not isinstance(params["mask_label"], str):
            raise StyxValidationError(f'`mask_label` has the wrong type: Received `{type(params["mask_label"])}` expected `str`')

def mris_watershed_cargs(params: MrisWatershed, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_watershed")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["input_gradient_field"]))
    cargs.append(params["output_annotation"])
    v_0 = params.get("max_clusters")
    if v_0 is not None:
        cargs.append("-M")
        cargs.append(str(v_0))
    v_1 = params.get("mask_label")
    if v_1 is not None:
        cargs.append("-mask_label")
        cargs.append(v_1)
    return cargs

def mris_watershed_outputs(params: MrisWatershed, execution: Execution) -> MrisWatershedOutputs:
    """Build the MrisWatershedOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_annotation_file_v: OutputPathType = execution.output_file(params["output_annotation"])
    return MrisWatershedOutputs(
        root=root_v,
        output_annotation_file=output_annotation_file_v,
    )

def mris_watershed_execute(params: MrisWatershed, runner: Runner | None = None) -> MrisWatershedOutputs:
    """
    mris_watershed

    This program computes the watershed transform on the surface of an intensity gradient and writes the resulting measurement into a .annot file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_watershed_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_WATERSHED_METADATA)
    execution.params(params)
    args = mris_watershed_cargs(params, execution)
    out = mris_watershed_outputs(params, execution)
    execution.run(args)
    return out

def mris_watershed(
    input_surface: InputPathType,
    input_gradient_field: InputPathType,
    output_annotation: str,
    max_clusters: float | None = None,
    mask_label: str | None = None,
    runner: Runner | None = None,
) -> MrisWatershedOutputs:
    """
    mris_watershed

    This program computes the watershed transform on the surface of an intensity gradient and writes the resulting measurement into a .annot file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file
        input_gradient_field: Input gradient field file
        output_annotation: Output annotation file
        max_clusters: Set the number of maximum clusters
        mask_label: Read in and mask the input volume that is not in the specified\
            label
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_watershed_params(
        input_surface=input_surface,
        input_gradient_field=input_gradient_field,
        output_annotation=output_annotation,
        max_clusters=max_clusters,
        mask_label=mask_label,
    )
    return mris_watershed_execute(params, runner)

__all__ = [
    "MrisWatershed",
    "MrisWatershedOutputs",
    "MRIS_WATERSHED_METADATA",
    "mris_watershed_cargs",
    "mris_watershed_outputs",
    "mris_watershed_params",
    "mris_watershed_execute",
    "mris_watershed_validate",
    "mris_watershed",
]