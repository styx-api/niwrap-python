# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LINEAR_ALIGN_METADATA = Metadata(
    id="mri_linear_align",
    name="mri_linear_align",
    package="freesurfer",
)

MriLinearAlign = typing.TypedDict(
    "MriLinearAlign",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_linear_align"]],
        "source": InputPathType,
        "target": InputPathType,
        "output_xform": str,
    },
)

@dataclasses.dataclass
class MriLinearAlignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_linear_align_params(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
) -> MriLinearAlign:
    """
    Build parameters.

    Args:
        source: Source image file for alignment
        target: Target image file for alignment
        output_xform: Output transformation file

    Returns:
        Parameter dictionary.
    """
    params: MriLinearAlign = {
        "@type": "freesurfer/mri_linear_align",
        "source": source,
        "target": target,
        "output_xform": output_xform,
    }
    return params

def mri_linear_align_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLinearAlign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("output_xform", None) is None:
        raise StyxValidationError("`output_xform` must not be None")
    if not isinstance(params["output_xform"], str):
        raise StyxValidationError(f'`output_xform` has the wrong type: Received `{type(params["output_xform"])}` expected `str`')

def mri_linear_align_cargs(params: MriLinearAlign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_linear_align")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["target"]))
    cargs.append(params["output_xform"])
    return cargs

def mri_linear_align_outputs(params: MriLinearAlign, execution: Execution) -> MriLinearAlignOutputs:
    """Build the MriLinearAlignOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriLinearAlignOutputs(
        root=root_v,
    )

def mri_linear_align_execute(params: MriLinearAlign, runner: Runner | None = None) -> MriLinearAlignOutputs:
    """
    mri_linear_align

    MRI Linear Alignment Tool for Freesurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_linear_align_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LINEAR_ALIGN_METADATA)
    execution.params(params)
    args = mri_linear_align_cargs(params, execution)
    out = mri_linear_align_outputs(params, execution)
    execution.run(args)
    return out

def mri_linear_align(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
    runner: Runner | None = None,
) -> MriLinearAlignOutputs:
    """
    mri_linear_align

    MRI Linear Alignment Tool for Freesurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source: Source image file for alignment
        target: Target image file for alignment
        output_xform: Output transformation file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_linear_align_params(
        source=source,
        target=target,
        output_xform=output_xform,
    )
    return mri_linear_align_execute(params, runner)

__all__ = [
    "MriLinearAlign",
    "MriLinearAlignOutputs",
    "MRI_LINEAR_ALIGN_METADATA",
    "mri_linear_align_cargs",
    "mri_linear_align_outputs",
    "mri_linear_align_params",
    "mri_linear_align_execute",
    "mri_linear_align_validate",
    "mri_linear_align",
]