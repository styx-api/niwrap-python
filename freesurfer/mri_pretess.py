# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PRETESS_METADATA = Metadata(
    id="mri_pretess",
    name="mri_pretess",
    package="freesurfer",
)

MriPretess = typing.TypedDict(
    "MriPretess",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_pretess"]],
        "filledvol": InputPathType,
        "labelstring": str,
        "normvol": InputPathType,
        "newfilledvol": str,
        "debug_voxel": typing.NotRequired[list[float]],
        "nocorners": typing.NotRequired[bool],
        "write": typing.NotRequired[bool],
        "keep": typing.NotRequired[bool],
        "test": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriPretessOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_newfilledvol: OutputPathType
    """Output new filled volume"""

def mri_pretess_params(
    filledvol: InputPathType,
    labelstring: str,
    normvol: InputPathType,
    newfilledvol: str,
    debug_voxel: list[float] | None = None,
    nocorners: bool = False,
    write: bool = False,
    keep: bool = False,
    test: bool = False,
) -> MriPretess:
    """
    Build parameters.

    Args:
        filledvol: Input filled volume, usually wm.mgz
        labelstring: Label string, usually wm
        normvol: Normalization volume, usually norm.mgz
        newfilledvol: New filled volume output, usually wm.mgz
        debug_voxel: Specify the voxel to debug with coordinates C R S
        nocorners: No removal of corner configurations in addition to edge ones
        write: Turn on diagnostic writing
        keep: Keep WM edits
        test: Adds a voxel to test removal by mri_pretess, retained with -keep.\
            Output not saved.

    Returns:
        Parameter dictionary.
    """
    params: MriPretess = {
        "@type": "freesurfer/mri_pretess",
        "filledvol": filledvol,
        "labelstring": labelstring,
        "normvol": normvol,
        "newfilledvol": newfilledvol,
        "nocorners": nocorners,
        "write": write,
        "keep": keep,
        "test": test,
    }
    if debug_voxel is not None:
        params["debug_voxel"] = debug_voxel
    return params

def mri_pretess_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriPretess."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filledvol", None) is None:
        raise StyxValidationError("`filledvol` must not be None")
    if not isinstance(params["filledvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`filledvol` has the wrong type: Received `{type(params["filledvol"])}` expected `InputPathType`')
    if params.get("labelstring", None) is None:
        raise StyxValidationError("`labelstring` must not be None")
    if not isinstance(params["labelstring"], str):
        raise StyxValidationError(f'`labelstring` has the wrong type: Received `{type(params["labelstring"])}` expected `str`')
    if params.get("normvol", None) is None:
        raise StyxValidationError("`normvol` must not be None")
    if not isinstance(params["normvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`normvol` has the wrong type: Received `{type(params["normvol"])}` expected `InputPathType`')
    if params.get("newfilledvol", None) is None:
        raise StyxValidationError("`newfilledvol` must not be None")
    if not isinstance(params["newfilledvol"], str):
        raise StyxValidationError(f'`newfilledvol` has the wrong type: Received `{type(params["newfilledvol"])}` expected `str`')
    if params.get("debug_voxel", None) is not None:
        if not isinstance(params["debug_voxel"], list):
            raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(params["debug_voxel"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel` must contain between 3 and 3 elements (inclusive)")
        for e in params["debug_voxel"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("nocorners", None) is not None:
        if not isinstance(params["nocorners"], bool):
            raise StyxValidationError(f'`nocorners` has the wrong type: Received `{type(params["nocorners"])}` expected `bool`')
    if params.get("write", None) is not None:
        if not isinstance(params["write"], bool):
            raise StyxValidationError(f'`write` has the wrong type: Received `{type(params["write"])}` expected `bool`')
    if params.get("keep", None) is not None:
        if not isinstance(params["keep"], bool):
            raise StyxValidationError(f'`keep` has the wrong type: Received `{type(params["keep"])}` expected `bool`')
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')

def mri_pretess_cargs(params: MriPretess, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_pretess")
    cargs.append(execution.input_file(params["filledvol"]))
    cargs.append(params["labelstring"])
    cargs.append(execution.input_file(params["normvol"]))
    cargs.append(params["newfilledvol"])
    v_0 = params.get("debug_voxel")
    if v_0 is not None:
        cargs.append("-debug_voxel")
        for item0 in v_0:
            cargs.append(str(item0))
    if params.get("nocorners"):
        cargs.append("-nocorners")
    if params.get("write"):
        cargs.append("-w")
    if params.get("keep"):
        cargs.append("-keep")
    if params.get("test"):
        cargs.append("-test")
    return cargs

def mri_pretess_outputs(params: MriPretess, execution: Execution) -> MriPretessOutputs:
    """Build the MriPretessOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_newfilledvol_v: OutputPathType = execution.output_file(params["newfilledvol"])
    return MriPretessOutputs(
        root=root_v,
        out_newfilledvol=out_newfilledvol_v,
    )

def mri_pretess_execute(params: MriPretess, runner: Runner | None = None) -> MriPretessOutputs:
    """
    mri_pretess

    Tool to modify WM segmentation so that all neighbors of WM voxels have a common face.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_pretess_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PRETESS_METADATA)
    execution.params(params)
    args = mri_pretess_cargs(params, execution)
    out = mri_pretess_outputs(params, execution)
    execution.run(args)
    return out

def mri_pretess(
    filledvol: InputPathType,
    labelstring: str,
    normvol: InputPathType,
    newfilledvol: str,
    debug_voxel: list[float] | None = None,
    nocorners: bool = False,
    write: bool = False,
    keep: bool = False,
    test: bool = False,
    runner: Runner | None = None,
) -> MriPretessOutputs:
    """
    mri_pretess

    Tool to modify WM segmentation so that all neighbors of WM voxels have a common face.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        filledvol: Input filled volume, usually wm.mgz
        labelstring: Label string, usually wm
        normvol: Normalization volume, usually norm.mgz
        newfilledvol: New filled volume output, usually wm.mgz
        debug_voxel: Specify the voxel to debug with coordinates C R S
        nocorners: No removal of corner configurations in addition to edge ones
        write: Turn on diagnostic writing
        keep: Keep WM edits
        test: Adds a voxel to test removal by mri_pretess, retained with -keep.\
            Output not saved.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_pretess_params(
        filledvol=filledvol,
        labelstring=labelstring,
        normvol=normvol,
        newfilledvol=newfilledvol,
        debug_voxel=debug_voxel,
        nocorners=nocorners,
        write=write,
        keep=keep,
        test=test,
    )
    return mri_pretess_execute(params, runner)

__all__ = [
    "MriPretess",
    "MriPretessOutputs",
    "MRI_PRETESS_METADATA",
    "mri_pretess_cargs",
    "mri_pretess_outputs",
    "mri_pretess_params",
    "mri_pretess_execute",
    "mri_pretess_validate",
    "mri_pretess",
]