# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCMUNPACK_METADATA = Metadata(
    id="dcmunpack",
    name="dcmunpack",
    package="freesurfer",
)

Dcmunpack = typing.TypedDict(
    "Dcmunpack",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dcmunpack"]],
        "src": str,
        "targ": typing.NotRequired[str],
        "run": typing.NotRequired[str],
        "auto_runseq": typing.NotRequired[str],
        "keep_scouts": typing.NotRequired[bool],
        "scanonly": typing.NotRequired[str],
        "one_per_dir": typing.NotRequired[bool],
        "ext": typing.NotRequired[str],
        "pre": typing.NotRequired[str],
        "pat": typing.NotRequired[str],
        "no_infodump": typing.NotRequired[bool],
        "generic": typing.NotRequired[bool],
        "copy_only": typing.NotRequired[bool],
        "no_convert": typing.NotRequired[bool],
        "force_update": typing.NotRequired[bool],
        "max": typing.NotRequired[bool],
        "base": typing.NotRequired[bool],
        "key_string": typing.NotRequired[str],
        "index_out": typing.NotRequired[str],
        "index_in": typing.NotRequired[str],
        "it_dicom": typing.NotRequired[bool],
        "no_exit_on_error": typing.NotRequired[bool],
        "run_skip": typing.NotRequired[str],
        "no_rescale_dicom": typing.NotRequired[bool],
        "rescale_dicom": typing.NotRequired[bool],
        "no_dwi": typing.NotRequired[bool],
        "iid": typing.NotRequired[list[float]],
        "ijd": typing.NotRequired[list[float]],
        "ikd": typing.NotRequired[list[float]],
        "extra_info": typing.NotRequired[bool],
        "first_dicom": typing.NotRequired[bool],
        "no_dcm2niix": typing.NotRequired[bool],
        "phase": typing.NotRequired[bool],
        "fips": typing.NotRequired[str],
        "fips_run": typing.NotRequired[str],
        "xml_only": typing.NotRequired[bool],
        "log": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DcmunpackOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dcmunpack_params(
    src: str,
    targ: str | None = None,
    run: str | None = None,
    auto_runseq: str | None = None,
    keep_scouts: bool = False,
    scanonly: str | None = None,
    one_per_dir: bool = False,
    ext: str | None = None,
    pre: str | None = None,
    pat: str | None = None,
    no_infodump: bool = False,
    generic: bool = False,
    copy_only: bool = False,
    no_convert: bool = False,
    force_update: bool = False,
    max: bool = False,
    base: bool = False,
    key_string: str | None = None,
    index_out: str | None = None,
    index_in: str | None = None,
    it_dicom: bool = False,
    no_exit_on_error: bool = False,
    run_skip: str | None = None,
    no_rescale_dicom: bool = False,
    rescale_dicom: bool = False,
    no_dwi: bool = False,
    iid: list[float] | None = None,
    ijd: list[float] | None = None,
    ikd: list[float] | None = None,
    extra_info: bool = False,
    first_dicom: bool = False,
    no_dcm2niix: bool = False,
    phase: bool = False,
    fips: str | None = None,
    fips_run: str | None = None,
    xml_only: bool = False,
    log: str | None = None,
    debug: bool = False,
) -> Dcmunpack:
    """
    Build parameters.

    Args:
        src: Dicom source directory. You can specify more than one.
        targ: Output directory. Do not need to include when just getting\
            information about what is in the directory.
        run: Specify unpacking rules for a given run (series). Eg, "-run 3 bold nii\
            f.nii".
        auto_runseq: Save all scans in the targetdir as runo.seqname.format.
        keep_scouts: Unpack series with 'scout' or 'setter' in the name.
        scanonly: Only scan the directory and put result in file.
        one_per_dir: Assume that there is only one dicom series in each subdir.
        ext: Input extension (eg, dcm).
        pre: Input prefix (i.e., input file name init string).
        pat: Input pattern (i.e., string that occurs in the middle of file name).
        no_infodump: Do not create the fname-infodump.dat file.
        generic: Do not use FSFAST hierarchy.
        copy_only: Only copy dicom files to output directory (implies -no-convert).
        no_convert: Do not convert to output format.
        force_update: Convert even if output is newer than the input dicom.
        max: Print out max in given dicom file.
        base: Report filename without path.
        key_string: Put keystring before each run line (good for searching).
        index_out: Save index of files to index.out.dat (for re-use).
        index_in: Read index of files (can make things much faster on 2nd run).
        it_dicom: Add -it dicom to mri_convert cmd line.
        no_exit_on_error: Continue to unpack data even if there is an error in\
            conversion.
        run_skip: Skip a given run (good when using -auto-runseq).
        no_rescale_dicom: Turn off DICOM rescaling based on tags (0028,1052)\
            (0028,1053).
        rescale_dicom: Turn DICOM rescaling on.
        no_dwi: Turn off trying to read DWI parameters.
        iid: Set -iid to mri_convert.
        ijd: Set -ijd to mri_convert.
        ikd: Set -ikd to mri_convert.
        extra_info: Add session info to each line of the info file (pat, date, man,\
            scan, field, serno).
        first_dicom: Copy first dicom file into output folder.
        no_dcm2niix: Turn off dcm2niix conversion.
        phase: Add the string _phase to volumes that are phase images based on\
            ImageType.
        fips: Fips parameters: project, site, birnid, visit.
        fips_run: Fips-run parameters: run paradigm.
        xml_only: For fips, only create xml file, do not convert to output.
        log: Log output to a file.
        debug: Enable debug mode.

    Returns:
        Parameter dictionary.
    """
    params: Dcmunpack = {
        "@type": "freesurfer/dcmunpack",
        "src": src,
        "keep_scouts": keep_scouts,
        "one_per_dir": one_per_dir,
        "no_infodump": no_infodump,
        "generic": generic,
        "copy_only": copy_only,
        "no_convert": no_convert,
        "force_update": force_update,
        "max": max,
        "base": base,
        "it_dicom": it_dicom,
        "no_exit_on_error": no_exit_on_error,
        "no_rescale_dicom": no_rescale_dicom,
        "rescale_dicom": rescale_dicom,
        "no_dwi": no_dwi,
        "extra_info": extra_info,
        "first_dicom": first_dicom,
        "no_dcm2niix": no_dcm2niix,
        "phase": phase,
        "xml_only": xml_only,
        "debug": debug,
    }
    if targ is not None:
        params["targ"] = targ
    if run is not None:
        params["run"] = run
    if auto_runseq is not None:
        params["auto_runseq"] = auto_runseq
    if scanonly is not None:
        params["scanonly"] = scanonly
    if ext is not None:
        params["ext"] = ext
    if pre is not None:
        params["pre"] = pre
    if pat is not None:
        params["pat"] = pat
    if key_string is not None:
        params["key_string"] = key_string
    if index_out is not None:
        params["index_out"] = index_out
    if index_in is not None:
        params["index_in"] = index_in
    if run_skip is not None:
        params["run_skip"] = run_skip
    if iid is not None:
        params["iid"] = iid
    if ijd is not None:
        params["ijd"] = ijd
    if ikd is not None:
        params["ikd"] = ikd
    if fips is not None:
        params["fips"] = fips
    if fips_run is not None:
        params["fips_run"] = fips_run
    if log is not None:
        params["log"] = log
    return params

def dcmunpack_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dcmunpack."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("src", None) is None:
        raise StyxValidationError("`src` must not be None")
    if not isinstance(params["src"], str):
        raise StyxValidationError(f'`src` has the wrong type: Received `{type(params["src"])}` expected `str`')
    if params.get("targ", None) is not None:
        if not isinstance(params["targ"], str):
            raise StyxValidationError(f'`targ` has the wrong type: Received `{type(params["targ"])}` expected `str`')
    if params.get("run", None) is not None:
        if not isinstance(params["run"], str):
            raise StyxValidationError(f'`run` has the wrong type: Received `{type(params["run"])}` expected `str`')
    if params.get("auto_runseq", None) is not None:
        if not isinstance(params["auto_runseq"], str):
            raise StyxValidationError(f'`auto_runseq` has the wrong type: Received `{type(params["auto_runseq"])}` expected `str`')
    if params.get("keep_scouts", None) is not None:
        if not isinstance(params["keep_scouts"], bool):
            raise StyxValidationError(f'`keep_scouts` has the wrong type: Received `{type(params["keep_scouts"])}` expected `bool`')
    if params.get("scanonly", None) is not None:
        if not isinstance(params["scanonly"], str):
            raise StyxValidationError(f'`scanonly` has the wrong type: Received `{type(params["scanonly"])}` expected `str`')
    if params.get("one_per_dir", None) is not None:
        if not isinstance(params["one_per_dir"], bool):
            raise StyxValidationError(f'`one_per_dir` has the wrong type: Received `{type(params["one_per_dir"])}` expected `bool`')
    if params.get("ext", None) is not None:
        if not isinstance(params["ext"], str):
            raise StyxValidationError(f'`ext` has the wrong type: Received `{type(params["ext"])}` expected `str`')
    if params.get("pre", None) is not None:
        if not isinstance(params["pre"], str):
            raise StyxValidationError(f'`pre` has the wrong type: Received `{type(params["pre"])}` expected `str`')
    if params.get("pat", None) is not None:
        if not isinstance(params["pat"], str):
            raise StyxValidationError(f'`pat` has the wrong type: Received `{type(params["pat"])}` expected `str`')
    if params.get("no_infodump", None) is not None:
        if not isinstance(params["no_infodump"], bool):
            raise StyxValidationError(f'`no_infodump` has the wrong type: Received `{type(params["no_infodump"])}` expected `bool`')
    if params.get("generic", None) is not None:
        if not isinstance(params["generic"], bool):
            raise StyxValidationError(f'`generic` has the wrong type: Received `{type(params["generic"])}` expected `bool`')
    if params.get("copy_only", None) is not None:
        if not isinstance(params["copy_only"], bool):
            raise StyxValidationError(f'`copy_only` has the wrong type: Received `{type(params["copy_only"])}` expected `bool`')
    if params.get("no_convert", None) is not None:
        if not isinstance(params["no_convert"], bool):
            raise StyxValidationError(f'`no_convert` has the wrong type: Received `{type(params["no_convert"])}` expected `bool`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')
    if params.get("max", None) is not None:
        if not isinstance(params["max"], bool):
            raise StyxValidationError(f'`max` has the wrong type: Received `{type(params["max"])}` expected `bool`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], bool):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `bool`')
    if params.get("key_string", None) is not None:
        if not isinstance(params["key_string"], str):
            raise StyxValidationError(f'`key_string` has the wrong type: Received `{type(params["key_string"])}` expected `str`')
    if params.get("index_out", None) is not None:
        if not isinstance(params["index_out"], str):
            raise StyxValidationError(f'`index_out` has the wrong type: Received `{type(params["index_out"])}` expected `str`')
    if params.get("index_in", None) is not None:
        if not isinstance(params["index_in"], str):
            raise StyxValidationError(f'`index_in` has the wrong type: Received `{type(params["index_in"])}` expected `str`')
    if params.get("it_dicom", None) is not None:
        if not isinstance(params["it_dicom"], bool):
            raise StyxValidationError(f'`it_dicom` has the wrong type: Received `{type(params["it_dicom"])}` expected `bool`')
    if params.get("no_exit_on_error", None) is not None:
        if not isinstance(params["no_exit_on_error"], bool):
            raise StyxValidationError(f'`no_exit_on_error` has the wrong type: Received `{type(params["no_exit_on_error"])}` expected `bool`')
    if params.get("run_skip", None) is not None:
        if not isinstance(params["run_skip"], str):
            raise StyxValidationError(f'`run_skip` has the wrong type: Received `{type(params["run_skip"])}` expected `str`')
    if params.get("no_rescale_dicom", None) is not None:
        if not isinstance(params["no_rescale_dicom"], bool):
            raise StyxValidationError(f'`no_rescale_dicom` has the wrong type: Received `{type(params["no_rescale_dicom"])}` expected `bool`')
    if params.get("rescale_dicom", None) is not None:
        if not isinstance(params["rescale_dicom"], bool):
            raise StyxValidationError(f'`rescale_dicom` has the wrong type: Received `{type(params["rescale_dicom"])}` expected `bool`')
    if params.get("no_dwi", None) is not None:
        if not isinstance(params["no_dwi"], bool):
            raise StyxValidationError(f'`no_dwi` has the wrong type: Received `{type(params["no_dwi"])}` expected `bool`')
    if params.get("iid", None) is not None:
        if not isinstance(params["iid"], list):
            raise StyxValidationError(f'`iid` has the wrong type: Received `{type(params["iid"])}` expected `list[float]`')
        for e in params["iid"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`iid` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("ijd", None) is not None:
        if not isinstance(params["ijd"], list):
            raise StyxValidationError(f'`ijd` has the wrong type: Received `{type(params["ijd"])}` expected `list[float]`')
        for e_2 in params["ijd"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`ijd` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("ikd", None) is not None:
        if not isinstance(params["ikd"], list):
            raise StyxValidationError(f'`ikd` has the wrong type: Received `{type(params["ikd"])}` expected `list[float]`')
        for e_3 in params["ikd"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`ikd` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("extra_info", None) is not None:
        if not isinstance(params["extra_info"], bool):
            raise StyxValidationError(f'`extra_info` has the wrong type: Received `{type(params["extra_info"])}` expected `bool`')
    if params.get("first_dicom", None) is not None:
        if not isinstance(params["first_dicom"], bool):
            raise StyxValidationError(f'`first_dicom` has the wrong type: Received `{type(params["first_dicom"])}` expected `bool`')
    if params.get("no_dcm2niix", None) is not None:
        if not isinstance(params["no_dcm2niix"], bool):
            raise StyxValidationError(f'`no_dcm2niix` has the wrong type: Received `{type(params["no_dcm2niix"])}` expected `bool`')
    if params.get("phase", None) is not None:
        if not isinstance(params["phase"], bool):
            raise StyxValidationError(f'`phase` has the wrong type: Received `{type(params["phase"])}` expected `bool`')
    if params.get("fips", None) is not None:
        if not isinstance(params["fips"], str):
            raise StyxValidationError(f'`fips` has the wrong type: Received `{type(params["fips"])}` expected `str`')
    if params.get("fips_run", None) is not None:
        if not isinstance(params["fips_run"], str):
            raise StyxValidationError(f'`fips_run` has the wrong type: Received `{type(params["fips_run"])}` expected `str`')
    if params.get("xml_only", None) is not None:
        if not isinstance(params["xml_only"], bool):
            raise StyxValidationError(f'`xml_only` has the wrong type: Received `{type(params["xml_only"])}` expected `bool`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], str):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def dcmunpack_cargs(params: Dcmunpack, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcmunpack")
    cargs.append("-src")
    cargs.append(params["src"])
    v_0 = params.get("targ")
    if v_0 is not None:
        cargs.append("-targ")
        cargs.append(v_0)
    v_1 = params.get("run")
    if v_1 is not None:
        cargs.append("-run")
        cargs.append(v_1)
    v_2 = params.get("auto_runseq")
    if v_2 is not None:
        cargs.append("-auto-runseq")
        cargs.append(v_2)
    if params.get("keep_scouts"):
        cargs.append("-keep-scouts")
    v_3 = params.get("scanonly")
    if v_3 is not None:
        cargs.append("-scanonly")
        cargs.append(v_3)
    if params.get("one_per_dir"):
        cargs.append("-one-per-dir")
    v_4 = params.get("ext")
    if v_4 is not None:
        cargs.append("-ext")
        cargs.append(v_4)
    v_5 = params.get("pre")
    if v_5 is not None:
        cargs.append("-pre")
        cargs.append(v_5)
    v_6 = params.get("pat")
    if v_6 is not None:
        cargs.append("-pat")
        cargs.append(v_6)
    if params.get("no_infodump"):
        cargs.append("-no-infodump")
    if params.get("generic"):
        cargs.append("-generic")
    if params.get("copy_only"):
        cargs.append("-copy-only")
    if params.get("no_convert"):
        cargs.append("-no-convert")
    if params.get("force_update"):
        cargs.append("-force-update")
    if params.get("max"):
        cargs.append("-max")
    if params.get("base"):
        cargs.append("-base")
    v_7 = params.get("key_string")
    if v_7 is not None:
        cargs.append("-key")
        cargs.append(v_7)
    v_8 = params.get("index_out")
    if v_8 is not None:
        cargs.append("-index-out")
        cargs.append(v_8)
    v_9 = params.get("index_in")
    if v_9 is not None:
        cargs.append("-index-in")
        cargs.append(v_9)
    if params.get("it_dicom"):
        cargs.append("-itdicom")
    if params.get("no_exit_on_error"):
        cargs.append("-no-exit-on-error")
    v_10 = params.get("run_skip")
    if v_10 is not None:
        cargs.append("-run-skip")
        cargs.append(v_10)
    if params.get("no_rescale_dicom"):
        cargs.append("-no-rescale-dicom")
    if params.get("rescale_dicom"):
        cargs.append("-rescale-dicom")
    if params.get("no_dwi"):
        cargs.append("-no-dwi")
    v_11 = params.get("iid")
    if v_11 is not None:
        cargs.append("-iid")
        for item0 in v_11:
            cargs.append(str(item0))
    v_12 = params.get("ijd")
    if v_12 is not None:
        cargs.append("-ijd")
        for item1 in v_12:
            cargs.append(str(item1))
    v_13 = params.get("ikd")
    if v_13 is not None:
        cargs.append("-ikd")
        for item2 in v_13:
            cargs.append(str(item2))
    if params.get("extra_info"):
        cargs.append("-extra-info")
    if params.get("first_dicom"):
        cargs.append("-first-dicom")
    if params.get("no_dcm2niix"):
        cargs.append("-no-dcm2niix")
    if params.get("phase"):
        cargs.append("-phase")
    v_14 = params.get("fips")
    if v_14 is not None:
        cargs.append("-fips")
        cargs.append(v_14)
    v_15 = params.get("fips_run")
    if v_15 is not None:
        cargs.append("-fips-run")
        cargs.append(v_15)
    if params.get("xml_only"):
        cargs.append("-xml-only")
    v_16 = params.get("log")
    if v_16 is not None:
        cargs.append("-log")
        cargs.append(v_16)
    if params.get("debug"):
        cargs.append("-debug")
    return cargs

def dcmunpack_outputs(params: Dcmunpack, execution: Execution) -> DcmunpackOutputs:
    """Build the DcmunpackOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DcmunpackOutputs(
        root=root_v,
    )

def dcmunpack_execute(params: Dcmunpack, runner: Runner | None = None) -> DcmunpackOutputs:
    """
    dcmunpack

    Sorts and converts a directory of DICOM files (Siemens, GE, Philips) into an output hierarchy with nifti (nii), mgh, mgz, or analyze output formats.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcmunpack_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCMUNPACK_METADATA)
    execution.params(params)
    args = dcmunpack_cargs(params, execution)
    out = dcmunpack_outputs(params, execution)
    execution.run(args)
    return out

def dcmunpack(
    src: str,
    targ: str | None = None,
    run: str | None = None,
    auto_runseq: str | None = None,
    keep_scouts: bool = False,
    scanonly: str | None = None,
    one_per_dir: bool = False,
    ext: str | None = None,
    pre: str | None = None,
    pat: str | None = None,
    no_infodump: bool = False,
    generic: bool = False,
    copy_only: bool = False,
    no_convert: bool = False,
    force_update: bool = False,
    max: bool = False,
    base: bool = False,
    key_string: str | None = None,
    index_out: str | None = None,
    index_in: str | None = None,
    it_dicom: bool = False,
    no_exit_on_error: bool = False,
    run_skip: str | None = None,
    no_rescale_dicom: bool = False,
    rescale_dicom: bool = False,
    no_dwi: bool = False,
    iid: list[float] | None = None,
    ijd: list[float] | None = None,
    ikd: list[float] | None = None,
    extra_info: bool = False,
    first_dicom: bool = False,
    no_dcm2niix: bool = False,
    phase: bool = False,
    fips: str | None = None,
    fips_run: str | None = None,
    xml_only: bool = False,
    log: str | None = None,
    debug: bool = False,
    runner: Runner | None = None,
) -> DcmunpackOutputs:
    """
    dcmunpack

    Sorts and converts a directory of DICOM files (Siemens, GE, Philips) into an output hierarchy with nifti (nii), mgh, mgz, or analyze output formats.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        src: Dicom source directory. You can specify more than one.
        targ: Output directory. Do not need to include when just getting\
            information about what is in the directory.
        run: Specify unpacking rules for a given run (series). Eg, "-run 3 bold nii\
            f.nii".
        auto_runseq: Save all scans in the targetdir as runo.seqname.format.
        keep_scouts: Unpack series with 'scout' or 'setter' in the name.
        scanonly: Only scan the directory and put result in file.
        one_per_dir: Assume that there is only one dicom series in each subdir.
        ext: Input extension (eg, dcm).
        pre: Input prefix (i.e., input file name init string).
        pat: Input pattern (i.e., string that occurs in the middle of file name).
        no_infodump: Do not create the fname-infodump.dat file.
        generic: Do not use FSFAST hierarchy.
        copy_only: Only copy dicom files to output directory (implies -no-convert).
        no_convert: Do not convert to output format.
        force_update: Convert even if output is newer than the input dicom.
        max: Print out max in given dicom file.
        base: Report filename without path.
        key_string: Put keystring before each run line (good for searching).
        index_out: Save index of files to index.out.dat (for re-use).
        index_in: Read index of files (can make things much faster on 2nd run).
        it_dicom: Add -it dicom to mri_convert cmd line.
        no_exit_on_error: Continue to unpack data even if there is an error in\
            conversion.
        run_skip: Skip a given run (good when using -auto-runseq).
        no_rescale_dicom: Turn off DICOM rescaling based on tags (0028,1052)\
            (0028,1053).
        rescale_dicom: Turn DICOM rescaling on.
        no_dwi: Turn off trying to read DWI parameters.
        iid: Set -iid to mri_convert.
        ijd: Set -ijd to mri_convert.
        ikd: Set -ikd to mri_convert.
        extra_info: Add session info to each line of the info file (pat, date, man,\
            scan, field, serno).
        first_dicom: Copy first dicom file into output folder.
        no_dcm2niix: Turn off dcm2niix conversion.
        phase: Add the string _phase to volumes that are phase images based on\
            ImageType.
        fips: Fips parameters: project, site, birnid, visit.
        fips_run: Fips-run parameters: run paradigm.
        xml_only: For fips, only create xml file, do not convert to output.
        log: Log output to a file.
        debug: Enable debug mode.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcmunpack_params(
        src=src,
        targ=targ,
        run=run,
        auto_runseq=auto_runseq,
        keep_scouts=keep_scouts,
        scanonly=scanonly,
        one_per_dir=one_per_dir,
        ext=ext,
        pre=pre,
        pat=pat,
        no_infodump=no_infodump,
        generic=generic,
        copy_only=copy_only,
        no_convert=no_convert,
        force_update=force_update,
        max=max,
        base=base,
        key_string=key_string,
        index_out=index_out,
        index_in=index_in,
        it_dicom=it_dicom,
        no_exit_on_error=no_exit_on_error,
        run_skip=run_skip,
        no_rescale_dicom=no_rescale_dicom,
        rescale_dicom=rescale_dicom,
        no_dwi=no_dwi,
        iid=iid,
        ijd=ijd,
        ikd=ikd,
        extra_info=extra_info,
        first_dicom=first_dicom,
        no_dcm2niix=no_dcm2niix,
        phase=phase,
        fips=fips,
        fips_run=fips_run,
        xml_only=xml_only,
        log=log,
        debug=debug,
    )
    return dcmunpack_execute(params, runner)

__all__ = [
    "Dcmunpack",
    "DcmunpackOutputs",
    "DCMUNPACK_METADATA",
    "dcmunpack_cargs",
    "dcmunpack_outputs",
    "dcmunpack_params",
    "dcmunpack_execute",
    "dcmunpack_validate",
    "dcmunpack",
]