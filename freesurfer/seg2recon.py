# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEG2RECON_METADATA = Metadata(
    id="seg2recon",
    name="seg2recon",
    package="freesurfer",
)

Seg2recon = typing.TypedDict(
    "Seg2recon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/seg2recon"]],
        "subject": str,
        "segvol": InputPathType,
        "inputvol": InputPathType,
        "ctab": typing.NotRequired[InputPathType],
        "ndilate": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "force_update": typing.NotRequired[bool],
        "no_cc": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
        "headmask": typing.NotRequired[InputPathType],
        "thresh": typing.NotRequired[float],
        "expert": typing.NotRequired[InputPathType],
        "rca": typing.NotRequired[bool],
        "no_bias_field_cor": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Seg2reconOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    aseg_auto_mgz: OutputPathType
    """Output aseg.auto.mgz file including corpus callosum segmentation"""
    nu_mgz: OutputPathType
    """Bias field corrected output nu.mgz file"""
    brainmask_mgz: OutputPathType
    """Output brainmask.mgz file"""

def seg2recon_params(
    subject: str,
    segvol: InputPathType,
    inputvol: InputPathType,
    ctab: InputPathType | None = None,
    ndilate: float | None = None,
    threads: float | None = None,
    force_update: bool = False,
    no_cc: bool = False,
    mask: InputPathType | None = None,
    headmask: InputPathType | None = None,
    thresh: float | None = None,
    expert: InputPathType | None = None,
    rca: bool = False,
    no_bias_field_cor: bool = False,
) -> Seg2recon:
    """
    Build parameters.

    Args:
        subject: Output subject directory name.
        segvol: Aseg-type volume, e.g., from synthseg, fastsurfer, psacnn, samseg,\
            or aseg.
        inputvol: Input volume as would be passed to recon-all.
        ctab: Color table for the segmentation. Uses embedded table if available,\
            or FreeSurferColorLUT.txt if not specified.
        ndilate: Dilate binarization of segmentation when creating brainmask.\
            Default is 2.
        threads: Number of threads to use for processing.
        force_update: Force regeneration of files whether needed or not.
        no_cc: Do not segment corpus callosum.
        mask: Use this mask as brainmask instead of computing from segmentation.
        headmask: Use this headmask instead of running mri_seghead.
        thresh: Threshold for bias field estimation.
        expert: Path to expert options file.
        rca: Run recon-all on the output.
        no_bias_field_cor: Do not compute or apply bias field correction.

    Returns:
        Parameter dictionary.
    """
    params: Seg2recon = {
        "@type": "freesurfer/seg2recon",
        "subject": subject,
        "segvol": segvol,
        "inputvol": inputvol,
        "force_update": force_update,
        "no_cc": no_cc,
        "rca": rca,
        "no_bias_field_cor": no_bias_field_cor,
    }
    if ctab is not None:
        params["ctab"] = ctab
    if ndilate is not None:
        params["ndilate"] = ndilate
    if threads is not None:
        params["threads"] = threads
    if mask is not None:
        params["mask"] = mask
    if headmask is not None:
        params["headmask"] = headmask
    if thresh is not None:
        params["thresh"] = thresh
    if expert is not None:
        params["expert"] = expert
    return params

def seg2recon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Seg2recon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("segvol", None) is None:
        raise StyxValidationError("`segvol` must not be None")
    if not isinstance(params["segvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segvol` has the wrong type: Received `{type(params["segvol"])}` expected `InputPathType`')
    if params.get("inputvol", None) is None:
        raise StyxValidationError("`inputvol` must not be None")
    if not isinstance(params["inputvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputvol` has the wrong type: Received `{type(params["inputvol"])}` expected `InputPathType`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `InputPathType`')
    if params.get("ndilate", None) is not None:
        if not isinstance(params["ndilate"], (float, int)):
            raise StyxValidationError(f'`ndilate` has the wrong type: Received `{type(params["ndilate"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')
    if params.get("no_cc", None) is not None:
        if not isinstance(params["no_cc"], bool):
            raise StyxValidationError(f'`no_cc` has the wrong type: Received `{type(params["no_cc"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("headmask", None) is not None:
        if not isinstance(params["headmask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`headmask` has the wrong type: Received `{type(params["headmask"])}` expected `InputPathType`')
    if params.get("thresh", None) is not None:
        if not isinstance(params["thresh"], (float, int)):
            raise StyxValidationError(f'`thresh` has the wrong type: Received `{type(params["thresh"])}` expected `float`')
    if params.get("expert", None) is not None:
        if not isinstance(params["expert"], (pathlib.Path, str)):
            raise StyxValidationError(f'`expert` has the wrong type: Received `{type(params["expert"])}` expected `InputPathType`')
    if params.get("rca", None) is not None:
        if not isinstance(params["rca"], bool):
            raise StyxValidationError(f'`rca` has the wrong type: Received `{type(params["rca"])}` expected `bool`')
    if params.get("no_bias_field_cor", None) is not None:
        if not isinstance(params["no_bias_field_cor"], bool):
            raise StyxValidationError(f'`no_bias_field_cor` has the wrong type: Received `{type(params["no_bias_field_cor"])}` expected `bool`')

def seg2recon_cargs(params: Seg2recon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("seg2recon")
    cargs.append("-s")
    cargs.append(params["subject"])
    cargs.append("-seg")
    cargs.append(execution.input_file(params["segvol"]))
    cargs.append("-i")
    cargs.append(execution.input_file(params["inputvol"]))
    v_0 = params.get("ctab")
    if v_0 is not None:
        cargs.append("-ctab")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("ndilate")
    if v_1 is not None:
        cargs.append("--ndilate")
        cargs.append(str(v_1))
    v_2 = params.get("threads")
    if v_2 is not None:
        cargs.append("--threads")
        cargs.append(str(v_2))
    if params.get("force_update"):
        cargs.append("--force-update")
    if params.get("no_cc"):
        cargs.append("--no-cc")
    v_3 = params.get("mask")
    if v_3 is not None:
        cargs.append("--m")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("headmask")
    if v_4 is not None:
        cargs.append("--h")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("thresh")
    if v_5 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_5))
    v_6 = params.get("expert")
    if v_6 is not None:
        cargs.append("--expert")
        cargs.append(execution.input_file(v_6))
    if params.get("rca"):
        cargs.append("--rca")
    if params.get("no_bias_field_cor"):
        cargs.append("--no-bias-field-cor")
    return cargs

def seg2recon_outputs(params: Seg2recon, execution: Execution) -> Seg2reconOutputs:
    """Build the Seg2reconOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    aseg_auto_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/aseg.auto.mgz')
    nu_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/nu.mgz')
    brainmask_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/brainmask.mgz')
    return Seg2reconOutputs(
        root=root_v,
        aseg_auto_mgz=aseg_auto_mgz_v,
        nu_mgz=nu_mgz_v,
        brainmask_mgz=brainmask_mgz_v,
    )

def seg2recon_execute(params: Seg2recon, runner: Runner | None = None) -> Seg2reconOutputs:
    """
    seg2recon

    Creates and populates a subjects directory from an input image and segmentation suitable for running recon-all.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    seg2recon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEG2RECON_METADATA)
    execution.params(params)
    args = seg2recon_cargs(params, execution)
    out = seg2recon_outputs(params, execution)
    execution.run(args)
    return out

def seg2recon(
    subject: str,
    segvol: InputPathType,
    inputvol: InputPathType,
    ctab: InputPathType | None = None,
    ndilate: float | None = None,
    threads: float | None = None,
    force_update: bool = False,
    no_cc: bool = False,
    mask: InputPathType | None = None,
    headmask: InputPathType | None = None,
    thresh: float | None = None,
    expert: InputPathType | None = None,
    rca: bool = False,
    no_bias_field_cor: bool = False,
    runner: Runner | None = None,
) -> Seg2reconOutputs:
    """
    seg2recon

    Creates and populates a subjects directory from an input image and segmentation suitable for running recon-all.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Output subject directory name.
        segvol: Aseg-type volume, e.g., from synthseg, fastsurfer, psacnn, samseg,\
            or aseg.
        inputvol: Input volume as would be passed to recon-all.
        ctab: Color table for the segmentation. Uses embedded table if available,\
            or FreeSurferColorLUT.txt if not specified.
        ndilate: Dilate binarization of segmentation when creating brainmask.\
            Default is 2.
        threads: Number of threads to use for processing.
        force_update: Force regeneration of files whether needed or not.
        no_cc: Do not segment corpus callosum.
        mask: Use this mask as brainmask instead of computing from segmentation.
        headmask: Use this headmask instead of running mri_seghead.
        thresh: Threshold for bias field estimation.
        expert: Path to expert options file.
        rca: Run recon-all on the output.
        no_bias_field_cor: Do not compute or apply bias field correction.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = seg2recon_params(
        subject=subject,
        segvol=segvol,
        inputvol=inputvol,
        ctab=ctab,
        ndilate=ndilate,
        threads=threads,
        force_update=force_update,
        no_cc=no_cc,
        mask=mask,
        headmask=headmask,
        thresh=thresh,
        expert=expert,
        rca=rca,
        no_bias_field_cor=no_bias_field_cor,
    )
    return seg2recon_execute(params, runner)

__all__ = [
    "Seg2recon",
    "Seg2reconOutputs",
    "SEG2RECON_METADATA",
    "seg2recon_cargs",
    "seg2recon_outputs",
    "seg2recon_params",
    "seg2recon_execute",
    "seg2recon_validate",
    "seg2recon",
]