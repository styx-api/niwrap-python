# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SURFACE_TO_VOL_DISTANCES_METADATA = Metadata(
    id="mris_surface_to_vol_distances",
    name="mris_surface_to_vol_distances",
    package="freesurfer",
)

MrisSurfaceToVolDistances = typing.TypedDict(
    "MrisSurfaceToVolDistances",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_surface_to_vol_distances"]],
        "average_subject": str,
        "hemisphere": str,
        "subjects": list[str],
        "output_prefix": str,
    },
)

@dataclasses.dataclass
class MrisSurfaceToVolDistancesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_distances: OutputPathType
    """Output file containing surface-to-volume distances."""

def mris_surface_to_vol_distances_params(
    average_subject: str,
    hemisphere: str,
    subjects: list[str],
    output_prefix: str,
) -> MrisSurfaceToVolDistances:
    """
    Build parameters.

    Args:
        average_subject: The average subject.
        hemisphere: Hemisphere identifier (e.g., lh or rh).
        subjects: List of subjects for distance calculation.
        output_prefix: Prefix for output files.

    Returns:
        Parameter dictionary.
    """
    params: MrisSurfaceToVolDistances = {
        "@type": "freesurfer/mris_surface_to_vol_distances",
        "average_subject": average_subject,
        "hemisphere": hemisphere,
        "subjects": subjects,
        "output_prefix": output_prefix,
    }
    return params

def mris_surface_to_vol_distances_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSurfaceToVolDistances."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("average_subject", None) is None:
        raise StyxValidationError("`average_subject` must not be None")
    if not isinstance(params["average_subject"], str):
        raise StyxValidationError(f'`average_subject` has the wrong type: Received `{type(params["average_subject"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def mris_surface_to_vol_distances_cargs(params: MrisSurfaceToVolDistances, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_surface_to_vol_distances")
    cargs.append(params["average_subject"])
    cargs.append(params["hemisphere"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_prefix"])
    return cargs

def mris_surface_to_vol_distances_outputs(params: MrisSurfaceToVolDistances, execution: Execution) -> MrisSurfaceToVolDistancesOutputs:
    """Build the MrisSurfaceToVolDistancesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_distances_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_distances.txt')
    return MrisSurfaceToVolDistancesOutputs(
        root=root_v,
        output_distances=output_distances_v,
    )

def mris_surface_to_vol_distances_execute(params: MrisSurfaceToVolDistances, runner: Runner | None = None) -> MrisSurfaceToVolDistancesOutputs:
    """
    mris_surface_to_vol_distances

    Tool from FreeSurfer to calculate surface-to-volume distances

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_surface_to_vol_distances_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SURFACE_TO_VOL_DISTANCES_METADATA)
    execution.params(params)
    args = mris_surface_to_vol_distances_cargs(params, execution)
    out = mris_surface_to_vol_distances_outputs(params, execution)
    execution.run(args)
    return out

def mris_surface_to_vol_distances(
    average_subject: str,
    hemisphere: str,
    subjects: list[str],
    output_prefix: str,
    runner: Runner | None = None,
) -> MrisSurfaceToVolDistancesOutputs:
    """
    mris_surface_to_vol_distances

    Tool from FreeSurfer to calculate surface-to-volume distances

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        average_subject: The average subject.
        hemisphere: Hemisphere identifier (e.g., lh or rh).
        subjects: List of subjects for distance calculation.
        output_prefix: Prefix for output files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_surface_to_vol_distances_params(
        average_subject=average_subject,
        hemisphere=hemisphere,
        subjects=subjects,
        output_prefix=output_prefix,
    )
    return mris_surface_to_vol_distances_execute(params, runner)

__all__ = [
    "MrisSurfaceToVolDistances",
    "MrisSurfaceToVolDistancesOutputs",
    "MRIS_SURFACE_TO_VOL_DISTANCES_METADATA",
    "mris_surface_to_vol_distances_cargs",
    "mris_surface_to_vol_distances_outputs",
    "mris_surface_to_vol_distances_params",
    "mris_surface_to_vol_distances_execute",
    "mris_surface_to_vol_distances_validate",
    "mris_surface_to_vol_distances",
]