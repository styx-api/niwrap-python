# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_STATS_AC_METADATA = Metadata(
    id="dmri_stats_ac",
    name="dmri_stats_ac",
    package="freesurfer",
)

DmriStatsAc = typing.TypedDict(
    "DmriStatsAc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_stats_ac"]],
        "anatomicuts_folder": str,
        "num_clusters": int,
        "correspondence_file": str,
        "measures": list[str],
        "output_file": str,
    },
)

@dataclasses.dataclass
class DmriStatsAcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output of the dMRI statistical analysis"""

def dmri_stats_ac_params(
    anatomicuts_folder: str,
    num_clusters: int,
    correspondence_file: str,
    measures: list[str],
    output_file: str,
) -> DmriStatsAc:
    """
    Build parameters.

    Args:
        anatomicuts_folder:
        num_clusters:
        correspondence_file:
        measures:
        output_file:

    Returns:
        Parameter dictionary.
    """
    params: DmriStatsAc = {
        "@type": "freesurfer/dmri_stats_ac",
        "anatomicuts_folder": anatomicuts_folder,
        "num_clusters": num_clusters,
        "correspondence_file": correspondence_file,
        "measures": measures,
        "output_file": output_file,
    }
    return params

def dmri_stats_ac_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriStatsAc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("anatomicuts_folder", None) is None:
        raise StyxValidationError("`anatomicuts_folder` must not be None")
    if not isinstance(params["anatomicuts_folder"], str):
        raise StyxValidationError(f'`anatomicuts_folder` has the wrong type: Received `{type(params["anatomicuts_folder"])}` expected `str`')
    if params.get("num_clusters", None) is None:
        raise StyxValidationError("`num_clusters` must not be None")
    if not isinstance(params["num_clusters"], int):
        raise StyxValidationError(f'`num_clusters` has the wrong type: Received `{type(params["num_clusters"])}` expected `int`')
    if params.get("correspondence_file", None) is None:
        raise StyxValidationError("`correspondence_file` must not be None")
    if not isinstance(params["correspondence_file"], str):
        raise StyxValidationError(f'`correspondence_file` has the wrong type: Received `{type(params["correspondence_file"])}` expected `str`')
    if params.get("measures", None) is None:
        raise StyxValidationError("`measures` must not be None")
    if not isinstance(params["measures"], list):
        raise StyxValidationError(f'`measures` has the wrong type: Received `{type(params["measures"])}` expected `list[str]`')
    for e in params["measures"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`measures` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def dmri_stats_ac_cargs(params: DmriStatsAc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_stats_ac")
    cargs.append("-i")
    cargs.append(params["anatomicuts_folder"])
    cargs.append("-n")
    cargs.append(str(params["num_clusters"]))
    cargs.append("-c")
    cargs.append(params["correspondence_file"])
    cargs.append("-m")
    for item0 in params["measures"]:
        cargs.append(item0)
    cargs.append("-o")
    cargs.append(params["output_file"])
    return cargs

def dmri_stats_ac_outputs(params: DmriStatsAc, execution: Execution) -> DmriStatsAcOutputs:
    """Build the DmriStatsAcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return DmriStatsAcOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def dmri_stats_ac_execute(params: DmriStatsAc, runner: Runner | None = None) -> DmriStatsAcOutputs:
    """
    dmri_stats_ac

    The tool 'dmri_stats_ac' performs statistical analysis on dMRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_stats_ac_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_STATS_AC_METADATA)
    execution.params(params)
    args = dmri_stats_ac_cargs(params, execution)
    out = dmri_stats_ac_outputs(params, execution)
    execution.run(args)
    return out

def dmri_stats_ac(
    anatomicuts_folder: str,
    num_clusters: int,
    correspondence_file: str,
    measures: list[str],
    output_file: str,
    runner: Runner | None = None,
) -> DmriStatsAcOutputs:
    """
    dmri_stats_ac

    The tool 'dmri_stats_ac' performs statistical analysis on dMRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        anatomicuts_folder:
        num_clusters:
        correspondence_file:
        measures:
        output_file:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_stats_ac_params(
        anatomicuts_folder=anatomicuts_folder,
        num_clusters=num_clusters,
        correspondence_file=correspondence_file,
        measures=measures,
        output_file=output_file,
    )
    return dmri_stats_ac_execute(params, runner)

__all__ = [
    "DmriStatsAc",
    "DmriStatsAcOutputs",
    "DMRI_STATS_AC_METADATA",
    "dmri_stats_ac_cargs",
    "dmri_stats_ac_outputs",
    "dmri_stats_ac_params",
    "dmri_stats_ac_execute",
    "dmri_stats_ac_validate",
    "dmri_stats_ac",
]