# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_TRAIN_METADATA = Metadata(
    id="dmri_train",
    name="dmri_train",
    package="freesurfer",
)

DmriTrain = typing.TypedDict(
    "DmriTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_train"]],
        "slist": InputPathType,
        "trk_files": list[InputPathType],
        "rois": typing.NotRequired[list[InputPathType]],
        "seg": InputPathType,
        "cmask": InputPathType,
        "lmask": list[float],
        "bmask_training": list[InputPathType],
        "outtrk": list[InputPathType],
        "bmask_test": list[InputPathType],
        "fa": typing.NotRequired[list[InputPathType]],
        "reg": typing.NotRequired[InputPathType],
        "regnl": typing.NotRequired[InputPathType],
        "refnl": typing.NotRequired[InputPathType],
        "basereg": typing.NotRequired[list[InputPathType]],
        "baseref": typing.NotRequired[list[InputPathType]],
        "ncpts": list[float],
        "max_streamlines": float,
        "xstr": typing.NotRequired[bool],
        "aprior": typing.NotRequired[bool],
        "sprior": typing.NotRequired[bool],
        "trunc": typing.NotRequired[bool],
        "out_files": list[str],
        "outdir": typing.NotRequired[str],
        "cptdir": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_train_params(
    slist: InputPathType,
    trk_files: list[InputPathType],
    seg: InputPathType,
    cmask: InputPathType,
    lmask: list[float],
    bmask_training: list[InputPathType],
    outtrk: list[InputPathType],
    bmask_test: list[InputPathType],
    ncpts: list[float],
    max_streamlines: float,
    out_files: list[str],
    rois: list[InputPathType] | None = None,
    fa: list[InputPathType] | None = None,
    reg: InputPathType | None = None,
    regnl: InputPathType | None = None,
    refnl: InputPathType | None = None,
    basereg: list[InputPathType] | None = None,
    baseref: list[InputPathType] | None = None,
    xstr: bool = False,
    aprior: bool = False,
    sprior: bool = False,
    trunc: bool = False,
    outdir: str | None = None,
    cptdir: str | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
    version: bool = False,
) -> DmriTrain:
    """
    Build parameters.

    Args:
        slist: Text file with list of training subject directories
        trk_files: Name(s) of input .trk file(s), one per path
        seg: Name of input aparc+aseg volume
        cmask: Name of input cortex mask volume
        lmask: Add a label ID from aparc+aseg to cortex mask, one per path
        bmask_training: Input brain mask volume(s)
        outtrk: Name(s) of output, pre-sorted .trk file(s), one per path
        bmask_test: Input brain mask volume(s) for test subject
        ncpts: Number of control points for initial spline
        max_streamlines: Maximum number of training streamlines to keep per path
        out_files: Base name(s) of output(s) for test subject, one per path
        rois: Optional, names of input tract labeling ROIs, two per path
        fa: Input FA volume(s) for test subject
        reg: Affine registration from atlas to base space
        regnl: Nonlinear registration from atlas to base space
        refnl: Nonlinear registration source reference volume
        basereg: Affine registration(s) from base to FA volume(s)
        baseref: Base space reference volume
        xstr: Exclude previously chosen center streamline(s)
        aprior: Compute priors on underlying anatomy
        sprior: Compute priors on shape
        trunc: Use all training streamlines, truncated or not
        outdir: Output directory
        cptdir: Output directory for control points in test subject's space
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit
        help: Print out information on how to use this program
        version: Print out version and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriTrain = {
        "@type": "freesurfer/dmri_train",
        "slist": slist,
        "trk_files": trk_files,
        "seg": seg,
        "cmask": cmask,
        "lmask": lmask,
        "bmask_training": bmask_training,
        "outtrk": outtrk,
        "bmask_test": bmask_test,
        "ncpts": ncpts,
        "max_streamlines": max_streamlines,
        "out_files": out_files,
        "xstr": xstr,
        "aprior": aprior,
        "sprior": sprior,
        "trunc": trunc,
        "debug": debug,
        "checkopts": checkopts,
        "help": help,
        "version": version,
    }
    if rois is not None:
        params["rois"] = rois
    if fa is not None:
        params["fa"] = fa
    if reg is not None:
        params["reg"] = reg
    if regnl is not None:
        params["regnl"] = regnl
    if refnl is not None:
        params["refnl"] = refnl
    if basereg is not None:
        params["basereg"] = basereg
    if baseref is not None:
        params["baseref"] = baseref
    if outdir is not None:
        params["outdir"] = outdir
    if cptdir is not None:
        params["cptdir"] = cptdir
    return params

def dmri_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("slist", None) is None:
        raise StyxValidationError("`slist` must not be None")
    if not isinstance(params["slist"], (pathlib.Path, str)):
        raise StyxValidationError(f'`slist` has the wrong type: Received `{type(params["slist"])}` expected `InputPathType`')
    if params.get("trk_files", None) is None:
        raise StyxValidationError("`trk_files` must not be None")
    if not isinstance(params["trk_files"], list):
        raise StyxValidationError(f'`trk_files` has the wrong type: Received `{type(params["trk_files"])}` expected `list[InputPathType]`')
    for e in params["trk_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`trk_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("rois", None) is not None:
        if not isinstance(params["rois"], list):
            raise StyxValidationError(f'`rois` has the wrong type: Received `{type(params["rois"])}` expected `list[InputPathType]`')
        for e_2 in params["rois"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`rois` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("seg", None) is None:
        raise StyxValidationError("`seg` must not be None")
    if not isinstance(params["seg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`seg` has the wrong type: Received `{type(params["seg"])}` expected `InputPathType`')
    if params.get("cmask", None) is None:
        raise StyxValidationError("`cmask` must not be None")
    if not isinstance(params["cmask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cmask` has the wrong type: Received `{type(params["cmask"])}` expected `InputPathType`')
    if params.get("lmask", None) is None:
        raise StyxValidationError("`lmask` must not be None")
    if not isinstance(params["lmask"], list):
        raise StyxValidationError(f'`lmask` has the wrong type: Received `{type(params["lmask"])}` expected `list[float]`')
    for e_3 in params["lmask"]:
        if not isinstance(e_3, (float, int)):
            raise StyxValidationError(f'`lmask` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("bmask_training", None) is None:
        raise StyxValidationError("`bmask_training` must not be None")
    if not isinstance(params["bmask_training"], list):
        raise StyxValidationError(f'`bmask_training` has the wrong type: Received `{type(params["bmask_training"])}` expected `list[InputPathType]`')
    for e_4 in params["bmask_training"]:
        if not isinstance(e_4, (pathlib.Path, str)):
            raise StyxValidationError(f'`bmask_training` has the wrong type: Received `{type(e_4)}` expected `list[InputPathType]`')
    if params.get("outtrk", None) is None:
        raise StyxValidationError("`outtrk` must not be None")
    if not isinstance(params["outtrk"], list):
        raise StyxValidationError(f'`outtrk` has the wrong type: Received `{type(params["outtrk"])}` expected `list[InputPathType]`')
    for e_5 in params["outtrk"]:
        if not isinstance(e_5, (pathlib.Path, str)):
            raise StyxValidationError(f'`outtrk` has the wrong type: Received `{type(e_5)}` expected `list[InputPathType]`')
    if params.get("bmask_test", None) is None:
        raise StyxValidationError("`bmask_test` must not be None")
    if not isinstance(params["bmask_test"], list):
        raise StyxValidationError(f'`bmask_test` has the wrong type: Received `{type(params["bmask_test"])}` expected `list[InputPathType]`')
    for e_6 in params["bmask_test"]:
        if not isinstance(e_6, (pathlib.Path, str)):
            raise StyxValidationError(f'`bmask_test` has the wrong type: Received `{type(e_6)}` expected `list[InputPathType]`')
    if params.get("fa", None) is not None:
        if not isinstance(params["fa"], list):
            raise StyxValidationError(f'`fa` has the wrong type: Received `{type(params["fa"])}` expected `list[InputPathType]`')
        for e_7 in params["fa"]:
            if not isinstance(e_7, (pathlib.Path, str)):
                raise StyxValidationError(f'`fa` has the wrong type: Received `{type(e_7)}` expected `list[InputPathType]`')
    if params.get("reg", None) is not None:
        if not isinstance(params["reg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reg` has the wrong type: Received `{type(params["reg"])}` expected `InputPathType`')
    if params.get("regnl", None) is not None:
        if not isinstance(params["regnl"], (pathlib.Path, str)):
            raise StyxValidationError(f'`regnl` has the wrong type: Received `{type(params["regnl"])}` expected `InputPathType`')
    if params.get("refnl", None) is not None:
        if not isinstance(params["refnl"], (pathlib.Path, str)):
            raise StyxValidationError(f'`refnl` has the wrong type: Received `{type(params["refnl"])}` expected `InputPathType`')
    if params.get("basereg", None) is not None:
        if not isinstance(params["basereg"], list):
            raise StyxValidationError(f'`basereg` has the wrong type: Received `{type(params["basereg"])}` expected `list[InputPathType]`')
        for e_8 in params["basereg"]:
            if not isinstance(e_8, (pathlib.Path, str)):
                raise StyxValidationError(f'`basereg` has the wrong type: Received `{type(e_8)}` expected `list[InputPathType]`')
    if params.get("baseref", None) is not None:
        if not isinstance(params["baseref"], list):
            raise StyxValidationError(f'`baseref` has the wrong type: Received `{type(params["baseref"])}` expected `list[InputPathType]`')
        for e_9 in params["baseref"]:
            if not isinstance(e_9, (pathlib.Path, str)):
                raise StyxValidationError(f'`baseref` has the wrong type: Received `{type(e_9)}` expected `list[InputPathType]`')
    if params.get("ncpts", None) is None:
        raise StyxValidationError("`ncpts` must not be None")
    if not isinstance(params["ncpts"], list):
        raise StyxValidationError(f'`ncpts` has the wrong type: Received `{type(params["ncpts"])}` expected `list[float]`')
    for e_10 in params["ncpts"]:
        if not isinstance(e_10, (float, int)):
            raise StyxValidationError(f'`ncpts` has the wrong type: Received `{type(e_10)}` expected `list[float]`')
    if params.get("max_streamlines", None) is None:
        raise StyxValidationError("`max_streamlines` must not be None")
    if not isinstance(params["max_streamlines"], (float, int)):
        raise StyxValidationError(f'`max_streamlines` has the wrong type: Received `{type(params["max_streamlines"])}` expected `float`')
    if params.get("xstr", None) is not None:
        if not isinstance(params["xstr"], bool):
            raise StyxValidationError(f'`xstr` has the wrong type: Received `{type(params["xstr"])}` expected `bool`')
    if params.get("aprior", None) is not None:
        if not isinstance(params["aprior"], bool):
            raise StyxValidationError(f'`aprior` has the wrong type: Received `{type(params["aprior"])}` expected `bool`')
    if params.get("sprior", None) is not None:
        if not isinstance(params["sprior"], bool):
            raise StyxValidationError(f'`sprior` has the wrong type: Received `{type(params["sprior"])}` expected `bool`')
    if params.get("trunc", None) is not None:
        if not isinstance(params["trunc"], bool):
            raise StyxValidationError(f'`trunc` has the wrong type: Received `{type(params["trunc"])}` expected `bool`')
    if params.get("out_files", None) is None:
        raise StyxValidationError("`out_files` must not be None")
    if not isinstance(params["out_files"], list):
        raise StyxValidationError(f'`out_files` has the wrong type: Received `{type(params["out_files"])}` expected `list[str]`')
    for e_11 in params["out_files"]:
        if not isinstance(e_11, str):
            raise StyxValidationError(f'`out_files` has the wrong type: Received `{type(e_11)}` expected `list[str]`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("cptdir", None) is not None:
        if not isinstance(params["cptdir"], str):
            raise StyxValidationError(f'`cptdir` has the wrong type: Received `{type(params["cptdir"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def dmri_train_cargs(params: DmriTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_train")
    cargs.append("--slist")
    cargs.append(execution.input_file(params["slist"]))
    cargs.append("--trk")
    for item0 in params["trk_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("rois")
    if v_0 is not None:
        cargs.append("--rois")
        for item1 in v_0:
            cargs.append(execution.input_file(item1))
    cargs.append("--seg")
    cargs.append(execution.input_file(params["seg"]))
    cargs.append("--cmask")
    cargs.append(execution.input_file(params["cmask"]))
    cargs.append("--lmask")
    for item2 in params["lmask"]:
        cargs.append(str(item2))
    cargs.append("--bmask")
    for item3 in params["bmask_training"]:
        cargs.append(execution.input_file(item3))
    cargs.append("--outtrk")
    for item4 in params["outtrk"]:
        cargs.append(execution.input_file(item4))
    cargs.append("--bmask")
    for item5 in params["bmask_test"]:
        cargs.append(execution.input_file(item5))
    v_1 = params.get("fa")
    if v_1 is not None:
        cargs.append("--fa")
        for item6 in v_1:
            cargs.append(execution.input_file(item6))
    v_2 = params.get("reg")
    if v_2 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("regnl")
    if v_3 is not None:
        cargs.append("--regnl")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("refnl")
    if v_4 is not None:
        cargs.append("--refnl")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("basereg")
    if v_5 is not None:
        cargs.append("--basereg")
        for item7 in v_5:
            cargs.append(execution.input_file(item7))
    v_6 = params.get("baseref")
    if v_6 is not None:
        cargs.append("--baseref")
        for item8 in v_6:
            cargs.append(execution.input_file(item8))
    cargs.append("--ncpts")
    for item9 in params["ncpts"]:
        cargs.append(str(item9))
    cargs.append("--max")
    cargs.append(str(params["max_streamlines"]))
    if params.get("xstr"):
        cargs.append("--xstr")
    if params.get("aprior"):
        cargs.append("--aprior")
    if params.get("sprior"):
        cargs.append("--sprior")
    if params.get("trunc"):
        cargs.append("--trunc")
    cargs.append("--out")
    for item10 in params["out_files"]:
        cargs.append(item10)
    v_7 = params.get("outdir")
    if v_7 is not None:
        cargs.append("--outdir")
        cargs.append(v_7)
    v_8 = params.get("cptdir")
    if v_8 is not None:
        cargs.append("--cptdir")
        cargs.append(v_8)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def dmri_train_outputs(params: DmriTrain, execution: Execution) -> DmriTrainOutputs:
    """Build the DmriTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriTrainOutputs(
        root=root_v,
    )

def dmri_train_execute(params: DmriTrain, runner: Runner | None = None) -> DmriTrainOutputs:
    """
    dmri_train

    DMRI training tool for processing diffusion MRI data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_TRAIN_METADATA)
    execution.params(params)
    args = dmri_train_cargs(params, execution)
    out = dmri_train_outputs(params, execution)
    execution.run(args)
    return out

def dmri_train(
    slist: InputPathType,
    trk_files: list[InputPathType],
    seg: InputPathType,
    cmask: InputPathType,
    lmask: list[float],
    bmask_training: list[InputPathType],
    outtrk: list[InputPathType],
    bmask_test: list[InputPathType],
    ncpts: list[float],
    max_streamlines: float,
    out_files: list[str],
    rois: list[InputPathType] | None = None,
    fa: list[InputPathType] | None = None,
    reg: InputPathType | None = None,
    regnl: InputPathType | None = None,
    refnl: InputPathType | None = None,
    basereg: list[InputPathType] | None = None,
    baseref: list[InputPathType] | None = None,
    xstr: bool = False,
    aprior: bool = False,
    sprior: bool = False,
    trunc: bool = False,
    outdir: str | None = None,
    cptdir: str | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DmriTrainOutputs:
    """
    dmri_train

    DMRI training tool for processing diffusion MRI data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        slist: Text file with list of training subject directories
        trk_files: Name(s) of input .trk file(s), one per path
        seg: Name of input aparc+aseg volume
        cmask: Name of input cortex mask volume
        lmask: Add a label ID from aparc+aseg to cortex mask, one per path
        bmask_training: Input brain mask volume(s)
        outtrk: Name(s) of output, pre-sorted .trk file(s), one per path
        bmask_test: Input brain mask volume(s) for test subject
        ncpts: Number of control points for initial spline
        max_streamlines: Maximum number of training streamlines to keep per path
        out_files: Base name(s) of output(s) for test subject, one per path
        rois: Optional, names of input tract labeling ROIs, two per path
        fa: Input FA volume(s) for test subject
        reg: Affine registration from atlas to base space
        regnl: Nonlinear registration from atlas to base space
        refnl: Nonlinear registration source reference volume
        basereg: Affine registration(s) from base to FA volume(s)
        baseref: Base space reference volume
        xstr: Exclude previously chosen center streamline(s)
        aprior: Compute priors on underlying anatomy
        sprior: Compute priors on shape
        trunc: Use all training streamlines, truncated or not
        outdir: Output directory
        cptdir: Output directory for control points in test subject's space
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit
        help: Print out information on how to use this program
        version: Print out version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_train_params(
        slist=slist,
        trk_files=trk_files,
        seg=seg,
        cmask=cmask,
        lmask=lmask,
        bmask_training=bmask_training,
        outtrk=outtrk,
        bmask_test=bmask_test,
        ncpts=ncpts,
        max_streamlines=max_streamlines,
        out_files=out_files,
        rois=rois,
        fa=fa,
        reg=reg,
        regnl=regnl,
        refnl=refnl,
        basereg=basereg,
        baseref=baseref,
        xstr=xstr,
        aprior=aprior,
        sprior=sprior,
        trunc=trunc,
        outdir=outdir,
        cptdir=cptdir,
        debug=debug,
        checkopts=checkopts,
        help=help,
        version=version,
    )
    return dmri_train_execute(params, runner)

__all__ = [
    "DmriTrain",
    "DmriTrainOutputs",
    "DMRI_TRAIN_METADATA",
    "dmri_train_cargs",
    "dmri_train_outputs",
    "dmri_train_params",
    "dmri_train_execute",
    "dmri_train_validate",
    "dmri_train",
]