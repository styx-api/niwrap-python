# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCMSPLIT_METADATA = Metadata(
    id="dcmsplit",
    name="dcmsplit",
    package="freesurfer",
)

Dcmsplit = typing.TypedDict(
    "Dcmsplit",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dcmsplit"]],
        "dcm_dir": str,
        "out_dir": str,
        "copy": typing.NotRequired[bool],
        "link": typing.NotRequired[bool],
        "split_name": typing.NotRequired[bool],
        "split_uid": typing.NotRequired[bool],
        "series_no": typing.NotRequired[bool],
        "series_plus": typing.NotRequired[bool],
        "dicom_tag": typing.NotRequired[str],
        "study_description": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DcmsplitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dcmsplit_params(
    dcm_dir: str,
    out_dir: str,
    copy: bool = False,
    link: bool = False,
    split_name: bool = False,
    split_uid: bool = False,
    series_no: bool = False,
    series_plus: bool = False,
    dicom_tag: str | None = None,
    study_description: bool = False,
) -> Dcmsplit:
    """
    Build parameters.

    Args:
        dcm_dir: Directory containing the DICOM files
        out_dir: Output directory for split DICOM files
        copy: Copy files instead of creating symbolic links
        link: Link files instead of copying (default behavior)
        split_name: Split files by patient name instead of UID
        split_uid: Split files by Study UID instead of name (default behavior)
        series_no: Split files by series number
        series_plus: Split files by series number and either name or UID
        dicom_tag: Split files by given DICOM tag (group element)
        study_description: Split files by Study Description

    Returns:
        Parameter dictionary.
    """
    params: Dcmsplit = {
        "@type": "freesurfer/dcmsplit",
        "dcm_dir": dcm_dir,
        "out_dir": out_dir,
        "copy": copy,
        "link": link,
        "split_name": split_name,
        "split_uid": split_uid,
        "series_no": series_no,
        "series_plus": series_plus,
        "study_description": study_description,
    }
    if dicom_tag is not None:
        params["dicom_tag"] = dicom_tag
    return params

def dcmsplit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dcmsplit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dcm_dir", None) is None:
        raise StyxValidationError("`dcm_dir` must not be None")
    if not isinstance(params["dcm_dir"], str):
        raise StyxValidationError(f'`dcm_dir` has the wrong type: Received `{type(params["dcm_dir"])}` expected `str`')
    if params.get("out_dir", None) is None:
        raise StyxValidationError("`out_dir` must not be None")
    if not isinstance(params["out_dir"], str):
        raise StyxValidationError(f'`out_dir` has the wrong type: Received `{type(params["out_dir"])}` expected `str`')
    if params.get("copy", None) is not None:
        if not isinstance(params["copy"], bool):
            raise StyxValidationError(f'`copy` has the wrong type: Received `{type(params["copy"])}` expected `bool`')
    if params.get("link", None) is not None:
        if not isinstance(params["link"], bool):
            raise StyxValidationError(f'`link` has the wrong type: Received `{type(params["link"])}` expected `bool`')
    if params.get("split_name", None) is not None:
        if not isinstance(params["split_name"], bool):
            raise StyxValidationError(f'`split_name` has the wrong type: Received `{type(params["split_name"])}` expected `bool`')
    if params.get("split_uid", None) is not None:
        if not isinstance(params["split_uid"], bool):
            raise StyxValidationError(f'`split_uid` has the wrong type: Received `{type(params["split_uid"])}` expected `bool`')
    if params.get("series_no", None) is not None:
        if not isinstance(params["series_no"], bool):
            raise StyxValidationError(f'`series_no` has the wrong type: Received `{type(params["series_no"])}` expected `bool`')
    if params.get("series_plus", None) is not None:
        if not isinstance(params["series_plus"], bool):
            raise StyxValidationError(f'`series_plus` has the wrong type: Received `{type(params["series_plus"])}` expected `bool`')
    if params.get("dicom_tag", None) is not None:
        if not isinstance(params["dicom_tag"], str):
            raise StyxValidationError(f'`dicom_tag` has the wrong type: Received `{type(params["dicom_tag"])}` expected `str`')
    if params.get("study_description", None) is not None:
        if not isinstance(params["study_description"], bool):
            raise StyxValidationError(f'`study_description` has the wrong type: Received `{type(params["study_description"])}` expected `bool`')

def dcmsplit_cargs(params: Dcmsplit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcmsplit")
    cargs.append("--dcm")
    cargs.append(params["dcm_dir"])
    cargs.append("--o")
    cargs.append(params["out_dir"])
    if params.get("copy"):
        cargs.append("--cp")
    if params.get("link"):
        cargs.append("--link")
    if params.get("split_name"):
        cargs.append("--name")
    if params.get("split_uid"):
        cargs.append("--uid")
    if params.get("series_no"):
        cargs.append("--seriesno")
    if params.get("series_plus"):
        cargs.append("--series+")
    v_0 = params.get("dicom_tag")
    if v_0 is not None:
        cargs.append("--t")
        cargs.append(v_0)
    if params.get("study_description"):
        cargs.append("--studyDes")
    return cargs

def dcmsplit_outputs(params: Dcmsplit, execution: Execution) -> DcmsplitOutputs:
    """Build the DcmsplitOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DcmsplitOutputs(
        root=root_v,
    )

def dcmsplit_execute(params: Dcmsplit, runner: Runner | None = None) -> DcmsplitOutputs:
    """
    dcmsplit

    Splits DICOM files into separate folders based on a unique identifier (UID).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcmsplit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCMSPLIT_METADATA)
    execution.params(params)
    args = dcmsplit_cargs(params, execution)
    out = dcmsplit_outputs(params, execution)
    execution.run(args)
    return out

def dcmsplit(
    dcm_dir: str,
    out_dir: str,
    copy: bool = False,
    link: bool = False,
    split_name: bool = False,
    split_uid: bool = False,
    series_no: bool = False,
    series_plus: bool = False,
    dicom_tag: str | None = None,
    study_description: bool = False,
    runner: Runner | None = None,
) -> DcmsplitOutputs:
    """
    dcmsplit

    Splits DICOM files into separate folders based on a unique identifier (UID).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dcm_dir: Directory containing the DICOM files
        out_dir: Output directory for split DICOM files
        copy: Copy files instead of creating symbolic links
        link: Link files instead of copying (default behavior)
        split_name: Split files by patient name instead of UID
        split_uid: Split files by Study UID instead of name (default behavior)
        series_no: Split files by series number
        series_plus: Split files by series number and either name or UID
        dicom_tag: Split files by given DICOM tag (group element)
        study_description: Split files by Study Description
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcmsplit_params(
        dcm_dir=dcm_dir,
        out_dir=out_dir,
        copy=copy,
        link=link,
        split_name=split_name,
        split_uid=split_uid,
        series_no=series_no,
        series_plus=series_plus,
        dicom_tag=dicom_tag,
        study_description=study_description,
    )
    return dcmsplit_execute(params, runner)

__all__ = [
    "Dcmsplit",
    "DcmsplitOutputs",
    "DCMSPLIT_METADATA",
    "dcmsplit_cargs",
    "dcmsplit_outputs",
    "dcmsplit_params",
    "dcmsplit_execute",
    "dcmsplit_validate",
    "dcmsplit",
]