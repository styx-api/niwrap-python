# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_GRADIENT_METADATA = Metadata(
    id="mris_gradient",
    name="mris_gradient",
    package="freesurfer",
)

MrisGradient = typing.TypedDict(
    "MrisGradient",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_gradient"]],
        "input_surface": InputPathType,
        "input_vector_field": InputPathType,
        "output_gradient_file": str,
    },
)

@dataclasses.dataclass
class MrisGradientOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_gradient: OutputPathType
    """The resulting gradient measurement written into a .mgz file."""

def mris_gradient_params(
    input_surface: InputPathType,
    input_vector_field: InputPathType,
    output_gradient_file: str,
) -> MrisGradient:
    """
    Build parameters.

    Args:
        input_surface: Path to the input surface file.
        input_vector_field: Path to the input vector field file.
        output_gradient_file: Path to the output gradient file ending with .mgz.

    Returns:
        Parameter dictionary.
    """
    params: MrisGradient = {
        "@type": "freesurfer/mris_gradient",
        "input_surface": input_surface,
        "input_vector_field": input_vector_field,
        "output_gradient_file": output_gradient_file,
    }
    return params

def mris_gradient_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisGradient."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("input_vector_field", None) is None:
        raise StyxValidationError("`input_vector_field` must not be None")
    if not isinstance(params["input_vector_field"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vector_field` has the wrong type: Received `{type(params["input_vector_field"])}` expected `InputPathType`')
    if params.get("output_gradient_file", None) is None:
        raise StyxValidationError("`output_gradient_file` must not be None")
    if not isinstance(params["output_gradient_file"], str):
        raise StyxValidationError(f'`output_gradient_file` has the wrong type: Received `{type(params["output_gradient_file"])}` expected `str`')

def mris_gradient_cargs(params: MrisGradient, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_gradient")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["input_vector_field"]))
    cargs.append(params["output_gradient_file"])
    return cargs

def mris_gradient_outputs(params: MrisGradient, execution: Execution) -> MrisGradientOutputs:
    """Build the MrisGradientOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_gradient_v: OutputPathType = execution.output_file(params["output_gradient_file"])
    return MrisGradientOutputs(
        root=root_v,
        output_gradient=output_gradient_v,
    )

def mris_gradient_execute(params: MrisGradient, runner: Runner | None = None) -> MrisGradientOutputs:
    """
    mris_gradient

    This program computes the gradient of an intensity profile of the cortical ribbon and writes the resulting measurement into a .mgz file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_gradient_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_GRADIENT_METADATA)
    execution.params(params)
    args = mris_gradient_cargs(params, execution)
    out = mris_gradient_outputs(params, execution)
    execution.run(args)
    return out

def mris_gradient(
    input_surface: InputPathType,
    input_vector_field: InputPathType,
    output_gradient_file: str,
    runner: Runner | None = None,
) -> MrisGradientOutputs:
    """
    mris_gradient

    This program computes the gradient of an intensity profile of the cortical ribbon and writes the resulting measurement into a .mgz file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Path to the input surface file.
        input_vector_field: Path to the input vector field file.
        output_gradient_file: Path to the output gradient file ending with .mgz.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_gradient_params(
        input_surface=input_surface,
        input_vector_field=input_vector_field,
        output_gradient_file=output_gradient_file,
    )
    return mris_gradient_execute(params, runner)

__all__ = [
    "MrisGradient",
    "MrisGradientOutputs",
    "MRIS_GRADIENT_METADATA",
    "mris_gradient_cargs",
    "mris_gradient_outputs",
    "mris_gradient_params",
    "mris_gradient_execute",
    "mris_gradient_validate",
    "mris_gradient",
]