# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_4DFPTOANALYZE_METADATA = Metadata(
    id="4dfptoanalyze",
    name="4dfptoanalyze",
    package="freesurfer",
)

V4dfptoanalyze = typing.TypedDict(
    "V4dfptoanalyze",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/4dfptoanalyze"]],
        "input_file": InputPathType,
        "scale_factor": typing.NotRequired[float],
        "output_8bit": typing.NotRequired[bool],
        "spm99": typing.NotRequired[bool],
        "endianness": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V4dfptoanalyzeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    analyze_hdr: OutputPathType
    """Header file of Analyze format output"""
    analyze_img: OutputPathType
    """Image file of Analyze format output"""

def v_4dfptoanalyze_params(
    input_file: InputPathType,
    scale_factor: float | None = None,
    output_8bit: bool = False,
    spm99: bool = False,
    endianness: str | None = None,
) -> V4dfptoanalyze:
    """
    Build parameters.

    Args:
        input_file: Input 4dfp filename
        scale_factor: Scale output values by specified factor
        output_8bit: Output 8 bit unsigned char
        spm99: Include origin and scale in hdr
        endianness: Output big or little endian (default CPU endian)

    Returns:
        Parameter dictionary.
    """
    params: V4dfptoanalyze = {
        "@type": "freesurfer/4dfptoanalyze",
        "input_file": input_file,
        "output_8bit": output_8bit,
        "spm99": spm99,
    }
    if scale_factor is not None:
        params["scale_factor"] = scale_factor
    if endianness is not None:
        params["endianness"] = endianness
    return params

def v_4dfptoanalyze_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V4dfptoanalyze."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("scale_factor", None) is not None:
        if not isinstance(params["scale_factor"], (float, int)):
            raise StyxValidationError(f'`scale_factor` has the wrong type: Received `{type(params["scale_factor"])}` expected `float`')
    if params.get("output_8bit", None) is not None:
        if not isinstance(params["output_8bit"], bool):
            raise StyxValidationError(f'`output_8bit` has the wrong type: Received `{type(params["output_8bit"])}` expected `bool`')
    if params.get("spm99", None) is not None:
        if not isinstance(params["spm99"], bool):
            raise StyxValidationError(f'`spm99` has the wrong type: Received `{type(params["spm99"])}` expected `bool`')
    if params.get("endianness", None) is not None:
        if not isinstance(params["endianness"], str):
            raise StyxValidationError(f'`endianness` has the wrong type: Received `{type(params["endianness"])}` expected `str`')

def v_4dfptoanalyze_cargs(params: V4dfptoanalyze, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("4dfptoanalyze")
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("scale_factor")
    if v_0 is not None:
        cargs.append("-c")
        cargs.append(str(v_0))
    if params.get("output_8bit"):
        cargs.append("-8")
    if params.get("spm99"):
        cargs.append("-SPM99")
    v_1 = params.get("endianness")
    if v_1 is not None:
        cargs.append("-@")
        cargs.append(v_1)
    return cargs

def v_4dfptoanalyze_outputs(params: V4dfptoanalyze, execution: Execution) -> V4dfptoanalyzeOutputs:
    """Build the V4dfptoanalyzeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    analyze_hdr_v: OutputPathType = execution.output_file(f'{params["input_file"]}_analyze.hdr')
    analyze_img_v: OutputPathType = execution.output_file(f'{params["input_file"]}_analyze.img')
    return V4dfptoanalyzeOutputs(
        root=root_v,
        analyze_hdr=analyze_hdr_v,
        analyze_img=analyze_img_v,
    )

def v_4dfptoanalyze_execute(params: V4dfptoanalyze, runner: Runner | None = None) -> V4dfptoanalyzeOutputs:
    """
    4dfptoanalyze

    Converts 4dfp formatted files to Analyze format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_4dfptoanalyze_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_4DFPTOANALYZE_METADATA)
    execution.params(params)
    args = v_4dfptoanalyze_cargs(params, execution)
    out = v_4dfptoanalyze_outputs(params, execution)
    execution.run(args)
    return out

def v_4dfptoanalyze(
    input_file: InputPathType,
    scale_factor: float | None = None,
    output_8bit: bool = False,
    spm99: bool = False,
    endianness: str | None = None,
    runner: Runner | None = None,
) -> V4dfptoanalyzeOutputs:
    """
    4dfptoanalyze

    Converts 4dfp formatted files to Analyze format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input 4dfp filename
        scale_factor: Scale output values by specified factor
        output_8bit: Output 8 bit unsigned char
        spm99: Include origin and scale in hdr
        endianness: Output big or little endian (default CPU endian)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_4dfptoanalyze_params(
        input_file=input_file,
        scale_factor=scale_factor,
        output_8bit=output_8bit,
        spm99=spm99,
        endianness=endianness,
    )
    return v_4dfptoanalyze_execute(params, runner)

__all__ = [
    "V4dfptoanalyze",
    "V4dfptoanalyzeOutputs",
    "V_4DFPTOANALYZE_METADATA",
    "v_4dfptoanalyze_cargs",
    "v_4dfptoanalyze_outputs",
    "v_4dfptoanalyze_params",
    "v_4dfptoanalyze_execute",
    "v_4dfptoanalyze_validate",
    "v_4dfptoanalyze",
]