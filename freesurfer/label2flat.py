# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL2FLAT_METADATA = Metadata(
    id="label2flat",
    name="label2flat",
    package="freesurfer",
)

Label2flat = typing.TypedDict(
    "Label2flat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label2flat"]],
        "subject_name": str,
        "label_file": InputPathType,
        "patch_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class Label2flatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_file: OutputPathType
    """The processed output file generated by label2flat."""

def label2flat_params(
    subject_name: str,
    label_file: InputPathType,
    patch_file: InputPathType,
    output_file: str,
) -> Label2flat:
    """
    Build parameters.

    Args:
        subject_name: The name of the subject.
        label_file: The label file path.
        patch_file: The patch file path.
        output_file: The output file path.

    Returns:
        Parameter dictionary.
    """
    params: Label2flat = {
        "@type": "freesurfer/label2flat",
        "subject_name": subject_name,
        "label_file": label_file,
        "patch_file": patch_file,
        "output_file": output_file,
    }
    return params

def label2flat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Label2flat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("patch_file", None) is None:
        raise StyxValidationError("`patch_file` must not be None")
    if not isinstance(params["patch_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`patch_file` has the wrong type: Received `{type(params["patch_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def label2flat_cargs(params: Label2flat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label2flat")
    cargs.append(params["subject_name"])
    cargs.append(execution.input_file(params["label_file"]))
    cargs.append(execution.input_file(params["patch_file"]))
    cargs.append(params["output_file"])
    return cargs

def label2flat_outputs(params: Label2flat, execution: Execution) -> Label2flatOutputs:
    """Build the Label2flatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_file_v: OutputPathType = execution.output_file(params["output_file"])
    return Label2flatOutputs(
        root=root_v,
        result_file=result_file_v,
    )

def label2flat_execute(params: Label2flat, runner: Runner | None = None) -> Label2flatOutputs:
    """
    label2flat

    A tool used in FreeSurfer to process labeling and patch files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label2flat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL2FLAT_METADATA)
    execution.params(params)
    args = label2flat_cargs(params, execution)
    out = label2flat_outputs(params, execution)
    execution.run(args)
    return out

def label2flat(
    subject_name: str,
    label_file: InputPathType,
    patch_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> Label2flatOutputs:
    """
    label2flat

    A tool used in FreeSurfer to process labeling and patch files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: The name of the subject.
        label_file: The label file path.
        patch_file: The patch file path.
        output_file: The output file path.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label2flat_params(
        subject_name=subject_name,
        label_file=label_file,
        patch_file=patch_file,
        output_file=output_file,
    )
    return label2flat_execute(params, runner)

__all__ = [
    "Label2flat",
    "Label2flatOutputs",
    "LABEL2FLAT_METADATA",
    "label2flat_cargs",
    "label2flat_outputs",
    "label2flat_params",
    "label2flat_execute",
    "label2flat_validate",
    "label2flat",
]