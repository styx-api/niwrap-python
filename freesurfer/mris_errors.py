# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ERRORS_METADATA = Metadata(
    id="mris_errors",
    name="mris_errors",
    package="freesurfer",
)

MrisErrors = typing.TypedDict(
    "MrisErrors",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_errors"]],
        "input_image_file": InputPathType,
    },
)

@dataclasses.dataclass
class MrisErrorsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_errors_params(
    input_image_file: InputPathType,
) -> MrisErrors:
    """
    Build parameters.

    Args:
        input_image_file: Input image file.

    Returns:
        Parameter dictionary.
    """
    params: MrisErrors = {
        "@type": "freesurfer/mris_errors",
        "input_image_file": input_image_file,
    }
    return params

def mris_errors_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisErrors."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image_file", None) is None:
        raise StyxValidationError("`input_image_file` must not be None")
    if not isinstance(params["input_image_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image_file` has the wrong type: Received `{type(params["input_image_file"])}` expected `InputPathType`')

def mris_errors_cargs(params: MrisErrors, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_errors")
    cargs.append(execution.input_file(params["input_image_file"]))
    return cargs

def mris_errors_outputs(params: MrisErrors, execution: Execution) -> MrisErrorsOutputs:
    """Build the MrisErrorsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisErrorsOutputs(
        root=root_v,
    )

def mris_errors_execute(params: MrisErrors, runner: Runner | None = None) -> MrisErrorsOutputs:
    """
    mris_errors

    This program will unfold an MRI on the surface of an ellipsoid.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_errors_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ERRORS_METADATA)
    execution.params(params)
    args = mris_errors_cargs(params, execution)
    out = mris_errors_outputs(params, execution)
    execution.run(args)
    return out

def mris_errors(
    input_image_file: InputPathType,
    runner: Runner | None = None,
) -> MrisErrorsOutputs:
    """
    mris_errors

    This program will unfold an MRI on the surface of an ellipsoid.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image_file: Input image file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_errors_params(
        input_image_file=input_image_file,
    )
    return mris_errors_execute(params, runner)

__all__ = [
    "MrisErrors",
    "MrisErrorsOutputs",
    "MRIS_ERRORS_METADATA",
    "mris_errors_cargs",
    "mris_errors_outputs",
    "mris_errors_params",
    "mris_errors_execute",
    "mris_errors_validate",
    "mris_errors",
]