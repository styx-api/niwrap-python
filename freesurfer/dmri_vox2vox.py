# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_VOX2VOX_METADATA = Metadata(
    id="dmri_vox2vox",
    name="dmri_vox2vox",
    package="freesurfer",
)

DmriVox2vox = typing.TypedDict(
    "DmriVox2vox",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_vox2vox"]],
        "input_files": list[InputPathType],
        "input_directory": typing.NotRequired[str],
        "output_files": list[str],
        "output_directory": typing.NotRequired[str],
        "input_reference": InputPathType,
        "output_reference": InputPathType,
        "affine_registration": InputPathType,
        "nonlinear_registration": InputPathType,
        "inverse_nonlinear": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "check_options": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriVox2voxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_vox2vox_params(
    input_files: list[InputPathType],
    output_files: list[str],
    input_reference: InputPathType,
    output_reference: InputPathType,
    affine_registration: InputPathType,
    nonlinear_registration: InputPathType,
    input_directory: str | None = None,
    output_directory: str | None = None,
    inverse_nonlinear: bool = False,
    debug: bool = False,
    check_options: bool = False,
    help: bool = False,
    version: bool = False,
) -> DmriVox2vox:
    """
    Build parameters.

    Args:
        input_files: Input text file(s)
        output_files: Output text file(s), as many as inputs
        input_reference: Input reference volume
        output_reference: Output reference volume
        affine_registration: Affine registration file (.mat) to be applied first
        nonlinear_registration: Nonlinear registration file (.m3z) to be applied\
            second
        input_directory: Input directory, if specified, names of input text files\
            are relative to this
        output_directory: Output directory, if specified, names of output text\
            files are relative to this
        inverse_nonlinear: Apply inverse of nonlinear warp when --regnl is used
        debug: Turn on debugging
        check_options: Check options and exit without running
        help: Print out information on how to use this program
        version: Print out version and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriVox2vox = {
        "@type": "freesurfer/dmri_vox2vox",
        "input_files": input_files,
        "output_files": output_files,
        "input_reference": input_reference,
        "output_reference": output_reference,
        "affine_registration": affine_registration,
        "nonlinear_registration": nonlinear_registration,
        "inverse_nonlinear": inverse_nonlinear,
        "debug": debug,
        "check_options": check_options,
        "help": help,
        "version": version,
    }
    if input_directory is not None:
        params["input_directory"] = input_directory
    if output_directory is not None:
        params["output_directory"] = output_directory
    return params

def dmri_vox2vox_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriVox2vox."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("input_directory", None) is not None:
        if not isinstance(params["input_directory"], str):
            raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("output_files", None) is None:
        raise StyxValidationError("`output_files` must not be None")
    if not isinstance(params["output_files"], list):
        raise StyxValidationError(f'`output_files` has the wrong type: Received `{type(params["output_files"])}` expected `list[str]`')
    for e_2 in params["output_files"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`output_files` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("input_reference", None) is None:
        raise StyxValidationError("`input_reference` must not be None")
    if not isinstance(params["input_reference"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_reference` has the wrong type: Received `{type(params["input_reference"])}` expected `InputPathType`')
    if params.get("output_reference", None) is None:
        raise StyxValidationError("`output_reference` must not be None")
    if not isinstance(params["output_reference"], (pathlib.Path, str)):
        raise StyxValidationError(f'`output_reference` has the wrong type: Received `{type(params["output_reference"])}` expected `InputPathType`')
    if params.get("affine_registration", None) is None:
        raise StyxValidationError("`affine_registration` must not be None")
    if not isinstance(params["affine_registration"], (pathlib.Path, str)):
        raise StyxValidationError(f'`affine_registration` has the wrong type: Received `{type(params["affine_registration"])}` expected `InputPathType`')
    if params.get("nonlinear_registration", None) is None:
        raise StyxValidationError("`nonlinear_registration` must not be None")
    if not isinstance(params["nonlinear_registration"], (pathlib.Path, str)):
        raise StyxValidationError(f'`nonlinear_registration` has the wrong type: Received `{type(params["nonlinear_registration"])}` expected `InputPathType`')
    if params.get("inverse_nonlinear", None) is not None:
        if not isinstance(params["inverse_nonlinear"], bool):
            raise StyxValidationError(f'`inverse_nonlinear` has the wrong type: Received `{type(params["inverse_nonlinear"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_options", None) is not None:
        if not isinstance(params["check_options"], bool):
            raise StyxValidationError(f'`check_options` has the wrong type: Received `{type(params["check_options"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def dmri_vox2vox_cargs(params: DmriVox2vox, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_vox2vox")
    cargs.append("--in")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("input_directory")
    if v_0 is not None:
        cargs.append("--indir")
        cargs.append(v_0)
    cargs.append("--out")
    for item1 in params["output_files"]:
        cargs.append(item1)
    v_1 = params.get("output_directory")
    if v_1 is not None:
        cargs.append("--outdir")
        cargs.append(v_1)
    cargs.append("--inref")
    cargs.append(execution.input_file(params["input_reference"]))
    cargs.append("--outref")
    cargs.append(execution.input_file(params["output_reference"]))
    cargs.append("--reg")
    cargs.append(execution.input_file(params["affine_registration"]))
    cargs.append("--regnl")
    cargs.append(execution.input_file(params["nonlinear_registration"]))
    if params.get("inverse_nonlinear"):
        cargs.append("--invnl")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_options"):
        cargs.append("--checkopts")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def dmri_vox2vox_outputs(params: DmriVox2vox, execution: Execution) -> DmriVox2voxOutputs:
    """Build the DmriVox2voxOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriVox2voxOutputs(
        root=root_v,
    )

def dmri_vox2vox_execute(params: DmriVox2vox, runner: Runner | None = None) -> DmriVox2voxOutputs:
    """
    dmri_vox2vox

    Tool for voxel-to-voxel transformations in diffusion MRI data processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_vox2vox_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_VOX2VOX_METADATA)
    execution.params(params)
    args = dmri_vox2vox_cargs(params, execution)
    out = dmri_vox2vox_outputs(params, execution)
    execution.run(args)
    return out

def dmri_vox2vox(
    input_files: list[InputPathType],
    output_files: list[str],
    input_reference: InputPathType,
    output_reference: InputPathType,
    affine_registration: InputPathType,
    nonlinear_registration: InputPathType,
    input_directory: str | None = None,
    output_directory: str | None = None,
    inverse_nonlinear: bool = False,
    debug: bool = False,
    check_options: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DmriVox2voxOutputs:
    """
    dmri_vox2vox

    Tool for voxel-to-voxel transformations in diffusion MRI data processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input text file(s)
        output_files: Output text file(s), as many as inputs
        input_reference: Input reference volume
        output_reference: Output reference volume
        affine_registration: Affine registration file (.mat) to be applied first
        nonlinear_registration: Nonlinear registration file (.m3z) to be applied\
            second
        input_directory: Input directory, if specified, names of input text files\
            are relative to this
        output_directory: Output directory, if specified, names of output text\
            files are relative to this
        inverse_nonlinear: Apply inverse of nonlinear warp when --regnl is used
        debug: Turn on debugging
        check_options: Check options and exit without running
        help: Print out information on how to use this program
        version: Print out version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_vox2vox_params(
        input_files=input_files,
        output_files=output_files,
        input_reference=input_reference,
        output_reference=output_reference,
        affine_registration=affine_registration,
        nonlinear_registration=nonlinear_registration,
        input_directory=input_directory,
        output_directory=output_directory,
        inverse_nonlinear=inverse_nonlinear,
        debug=debug,
        check_options=check_options,
        help=help,
        version=version,
    )
    return dmri_vox2vox_execute(params, runner)

__all__ = [
    "DmriVox2vox",
    "DmriVox2voxOutputs",
    "DMRI_VOX2VOX_METADATA",
    "dmri_vox2vox_cargs",
    "dmri_vox2vox_outputs",
    "dmri_vox2vox_params",
    "dmri_vox2vox_execute",
    "dmri_vox2vox_validate",
    "dmri_vox2vox",
]