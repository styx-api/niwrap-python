# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSL_LABEL2VOXEL_METADATA = Metadata(
    id="fsl_label2voxel",
    name="fsl_label2voxel",
    package="freesurfer",
)

FslLabel2voxel = typing.TypedDict(
    "FslLabel2voxel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsl_label2voxel"]],
        "label_value": float,
        "labeled_volume": InputPathType,
        "src_volume": InputPathType,
        "output_filename": str,
    },
)

@dataclasses.dataclass
class FslLabel2voxelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_voxel_data: OutputPathType
    """Output voxel data based on the specified label value"""

def fsl_label2voxel_params(
    label_value: float,
    labeled_volume: InputPathType,
    src_volume: InputPathType,
    output_filename: str,
) -> FslLabel2voxel:
    """
    Build parameters.

    Args:
        label_value: Label value to convert
        labeled_volume: Labeled volume file
        src_volume: Source volume file
        output_filename: Output filename for voxel data

    Returns:
        Parameter dictionary.
    """
    params: FslLabel2voxel = {
        "@type": "freesurfer/fsl_label2voxel",
        "label_value": label_value,
        "labeled_volume": labeled_volume,
        "src_volume": src_volume,
        "output_filename": output_filename,
    }
    return params

def fsl_label2voxel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FslLabel2voxel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_value", None) is None:
        raise StyxValidationError("`label_value` must not be None")
    if not isinstance(params["label_value"], (float, int)):
        raise StyxValidationError(f'`label_value` has the wrong type: Received `{type(params["label_value"])}` expected `float`')
    if params.get("labeled_volume", None) is None:
        raise StyxValidationError("`labeled_volume` must not be None")
    if not isinstance(params["labeled_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`labeled_volume` has the wrong type: Received `{type(params["labeled_volume"])}` expected `InputPathType`')
    if params.get("src_volume", None) is None:
        raise StyxValidationError("`src_volume` must not be None")
    if not isinstance(params["src_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`src_volume` has the wrong type: Received `{type(params["src_volume"])}` expected `InputPathType`')
    if params.get("output_filename", None) is None:
        raise StyxValidationError("`output_filename` must not be None")
    if not isinstance(params["output_filename"], str):
        raise StyxValidationError(f'`output_filename` has the wrong type: Received `{type(params["output_filename"])}` expected `str`')

def fsl_label2voxel_cargs(params: FslLabel2voxel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsl_label2voxel")
    cargs.append(str(params["label_value"]))
    cargs.append(execution.input_file(params["labeled_volume"]))
    cargs.append(execution.input_file(params["src_volume"]))
    cargs.append(params["output_filename"])
    return cargs

def fsl_label2voxel_outputs(params: FslLabel2voxel, execution: Execution) -> FslLabel2voxelOutputs:
    """Build the FslLabel2voxelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_voxel_data_v: OutputPathType = execution.output_file(params["output_filename"])
    return FslLabel2voxelOutputs(
        root=root_v,
        output_voxel_data=output_voxel_data_v,
    )

def fsl_label2voxel_execute(params: FslLabel2voxel, runner: Runner | None = None) -> FslLabel2voxelOutputs:
    """
    fsl_label2voxel

    Converts labeled volumes to voxels based on specified labels

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsl_label2voxel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSL_LABEL2VOXEL_METADATA)
    execution.params(params)
    args = fsl_label2voxel_cargs(params, execution)
    out = fsl_label2voxel_outputs(params, execution)
    execution.run(args)
    return out

def fsl_label2voxel(
    label_value: float,
    labeled_volume: InputPathType,
    src_volume: InputPathType,
    output_filename: str,
    runner: Runner | None = None,
) -> FslLabel2voxelOutputs:
    """
    fsl_label2voxel

    Converts labeled volumes to voxels based on specified labels

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label_value: Label value to convert
        labeled_volume: Labeled volume file
        src_volume: Source volume file
        output_filename: Output filename for voxel data
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsl_label2voxel_params(
        label_value=label_value,
        labeled_volume=labeled_volume,
        src_volume=src_volume,
        output_filename=output_filename,
    )
    return fsl_label2voxel_execute(params, runner)

__all__ = [
    "FslLabel2voxel",
    "FslLabel2voxelOutputs",
    "FSL_LABEL2VOXEL_METADATA",
    "fsl_label2voxel_cargs",
    "fsl_label2voxel_outputs",
    "fsl_label2voxel_params",
    "fsl_label2voxel_execute",
    "fsl_label2voxel_validate",
    "fsl_label2voxel",
]