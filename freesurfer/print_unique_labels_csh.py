# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PRINT_UNIQUE_LABELS_CSH_METADATA = Metadata(
    id="print_unique_labels.csh",
    name="print_unique_labels.csh",
    package="freesurfer",
)

PrintUniqueLabelsCsh = typing.TypedDict(
    "PrintUniqueLabelsCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/print_unique_labels.csh"]],
        "label_volume": InputPathType,
        "output_file": typing.NotRequired[str],
        "list_only": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PrintUniqueLabelsCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    results_file: OutputPathType | None
    """Output file with the list of unique labels"""

def print_unique_labels_csh_params(
    label_volume: InputPathType,
    output_file: str | None = None,
    list_only: bool = False,
    version: bool = False,
    help: bool = False,
) -> PrintUniqueLabelsCsh:
    """
    Build parameters.

    Args:
        label_volume: Label volume to be analyzed
        output_file: Text file where the results are written
        list_only: Only list the labels
        version: Print version and exit
        help: Print help and exit

    Returns:
        Parameter dictionary.
    """
    params: PrintUniqueLabelsCsh = {
        "@type": "freesurfer/print_unique_labels.csh",
        "label_volume": label_volume,
        "list_only": list_only,
        "version": version,
        "help": help,
    }
    if output_file is not None:
        params["output_file"] = output_file
    return params

def print_unique_labels_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PrintUniqueLabelsCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_volume", None) is None:
        raise StyxValidationError("`label_volume` must not be None")
    if not isinstance(params["label_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_volume` has the wrong type: Received `{type(params["label_volume"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("list_only", None) is not None:
        if not isinstance(params["list_only"], bool):
            raise StyxValidationError(f'`list_only` has the wrong type: Received `{type(params["list_only"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def print_unique_labels_csh_cargs(params: PrintUniqueLabelsCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("print_unique_labels.csh")
    cargs.append("--vol")
    cargs.append(execution.input_file(params["label_volume"]))
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("--out")
        cargs.append(v_0)
    if params.get("list_only"):
        cargs.append("--list")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def print_unique_labels_csh_outputs(params: PrintUniqueLabelsCsh, execution: Execution) -> PrintUniqueLabelsCshOutputs:
    """Build the PrintUniqueLabelsCshOutputs object for this tool."""
    results_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        results_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return PrintUniqueLabelsCshOutputs(
        root=root_v,
        results_file=results_file_v,
    )

def print_unique_labels_csh_execute(params: PrintUniqueLabelsCsh, runner: Runner | None = None) -> PrintUniqueLabelsCshOutputs:
    """
    print_unique_labels.csh

    Prints the list of unique labels (with structure name) in the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    print_unique_labels_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PRINT_UNIQUE_LABELS_CSH_METADATA)
    execution.params(params)
    args = print_unique_labels_csh_cargs(params, execution)
    out = print_unique_labels_csh_outputs(params, execution)
    execution.run(args)
    return out

def print_unique_labels_csh(
    label_volume: InputPathType,
    output_file: str | None = None,
    list_only: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> PrintUniqueLabelsCshOutputs:
    """
    print_unique_labels.csh

    Prints the list of unique labels (with structure name) in the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label_volume: Label volume to be analyzed
        output_file: Text file where the results are written
        list_only: Only list the labels
        version: Print version and exit
        help: Print help and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = print_unique_labels_csh_params(
        label_volume=label_volume,
        output_file=output_file,
        list_only=list_only,
        version=version,
        help=help,
    )
    return print_unique_labels_csh_execute(params, runner)

__all__ = [
    "PrintUniqueLabelsCsh",
    "PrintUniqueLabelsCshOutputs",
    "PRINT_UNIQUE_LABELS_CSH_METADATA",
    "print_unique_labels_csh_cargs",
    "print_unique_labels_csh_outputs",
    "print_unique_labels_csh_params",
    "print_unique_labels_csh_execute",
    "print_unique_labels_csh_validate",
    "print_unique_labels_csh",
]