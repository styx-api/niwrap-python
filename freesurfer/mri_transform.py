# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_TRANSFORM_METADATA = Metadata(
    id="mri_transform",
    name="mri_transform",
    package="freesurfer",
)

MriTransform = typing.TypedDict(
    "MriTransform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_transform"]],
        "input_volume": InputPathType,
        "lta_file": InputPathType,
        "output_file": str,
        "out_like": typing.NotRequired[InputPathType],
        "invert": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriTransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_output: OutputPathType
    """Transformed output volume"""

def mri_transform_params(
    input_volume: InputPathType,
    lta_file: InputPathType,
    output_file: str,
    out_like: InputPathType | None = None,
    invert: bool = False,
) -> MriTransform:
    """
    Build parameters.

    Args:
        input_volume: Input MRI volume
        lta_file: Linear Transform Array (LTA) file
        output_file: Output file for the transformed MRI volume
        out_like: Set output volume parameters like the reference volume
        invert: Invert transform coordinates

    Returns:
        Parameter dictionary.
    """
    params: MriTransform = {
        "@type": "freesurfer/mri_transform",
        "input_volume": input_volume,
        "lta_file": lta_file,
        "output_file": output_file,
        "invert": invert,
    }
    if out_like is not None:
        params["out_like"] = out_like
    return params

def mri_transform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriTransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("lta_file", None) is None:
        raise StyxValidationError("`lta_file` must not be None")
    if not isinstance(params["lta_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`lta_file` has the wrong type: Received `{type(params["lta_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("out_like", None) is not None:
        if not isinstance(params["out_like"], (pathlib.Path, str)):
            raise StyxValidationError(f'`out_like` has the wrong type: Received `{type(params["out_like"])}` expected `InputPathType`')
    if params.get("invert", None) is not None:
        if not isinstance(params["invert"], bool):
            raise StyxValidationError(f'`invert` has the wrong type: Received `{type(params["invert"])}` expected `bool`')

def mri_transform_cargs(params: MriTransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_transform")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["lta_file"]))
    cargs.append(params["output_file"])
    v_0 = params.get("out_like")
    if v_0 is not None:
        cargs.append("-out_like")
        cargs.append(execution.input_file(v_0))
    if params.get("invert"):
        cargs.append("-I")
    return cargs

def mri_transform_outputs(params: MriTransform, execution: Execution) -> MriTransformOutputs:
    """Build the MriTransformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MriTransformOutputs(
        root=root_v,
        transformed_output=transformed_output_v,
    )

def mri_transform_execute(params: MriTransform, runner: Runner | None = None) -> MriTransformOutputs:
    """
    mri_transform

    Applies a linear transform to an MRI volume and writes out the result.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_transform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_TRANSFORM_METADATA)
    execution.params(params)
    args = mri_transform_cargs(params, execution)
    out = mri_transform_outputs(params, execution)
    execution.run(args)
    return out

def mri_transform(
    input_volume: InputPathType,
    lta_file: InputPathType,
    output_file: str,
    out_like: InputPathType | None = None,
    invert: bool = False,
    runner: Runner | None = None,
) -> MriTransformOutputs:
    """
    mri_transform

    Applies a linear transform to an MRI volume and writes out the result.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input MRI volume
        lta_file: Linear Transform Array (LTA) file
        output_file: Output file for the transformed MRI volume
        out_like: Set output volume parameters like the reference volume
        invert: Invert transform coordinates
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_transform_params(
        input_volume=input_volume,
        lta_file=lta_file,
        output_file=output_file,
        out_like=out_like,
        invert=invert,
    )
    return mri_transform_execute(params, runner)

__all__ = [
    "MriTransform",
    "MriTransformOutputs",
    "MRI_TRANSFORM_METADATA",
    "mri_transform_cargs",
    "mri_transform_outputs",
    "mri_transform_params",
    "mri_transform_execute",
    "mri_transform_validate",
    "mri_transform",
]