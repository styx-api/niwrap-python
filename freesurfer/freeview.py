# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FREEVIEW_METADATA = Metadata(
    id="freeview",
    name="freeview",
    package="freesurfer",
)

Freeview = typing.TypedDict(
    "Freeview",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/freeview"]],
        "args": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FreeviewOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def freeview_params(
    args: str | None = None,
) -> Freeview:
    """
    Build parameters.

    Args:
        args: Arguments for freeview

    Returns:
        Parameter dictionary.
    """
    params: Freeview = {
        "@type": "freesurfer/freeview",
    }
    if args is not None:
        params["args"] = args
    return params

def freeview_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Freeview."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("args", None) is not None:
        if not isinstance(params["args"], str):
            raise StyxValidationError(f'`args` has the wrong type: Received `{type(params["args"])}` expected `str`')

def freeview_cargs(params: Freeview, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("freeview")
    v_0 = params.get("args")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def freeview_outputs(params: Freeview, execution: Execution) -> FreeviewOutputs:
    """Build the FreeviewOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FreeviewOutputs(
        root=root_v,
    )

def freeview_execute(params: Freeview, runner: Runner | None = None) -> FreeviewOutputs:
    """
    freeview

    Freeview is a 3D/2D brain visualization tool.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    freeview_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FREEVIEW_METADATA)
    execution.params(params)
    args = freeview_cargs(params, execution)
    out = freeview_outputs(params, execution)
    execution.run(args)
    return out

def freeview(
    args: str | None = None,
    runner: Runner | None = None,
) -> FreeviewOutputs:
    """
    freeview

    Freeview is a 3D/2D brain visualization tool.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        args: Arguments for freeview
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = freeview_params(
        args=args,
    )
    return freeview_execute(params, runner)

__all__ = [
    "Freeview",
    "FreeviewOutputs",
    "FREEVIEW_METADATA",
    "freeview_cargs",
    "freeview_outputs",
    "freeview_params",
    "freeview_execute",
    "freeview_validate",
    "freeview",
]