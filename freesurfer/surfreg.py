# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFREG_METADATA = Metadata(
    id="surfreg",
    name="surfreg",
    package="freesurfer",
)

Surfreg = typing.TypedDict(
    "Surfreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/surfreg"]],
        "subject": str,
        "target": str,
        "cross_hemi": typing.NotRequired[bool],
        "reg_lh": typing.NotRequired[bool],
        "reg_rh": typing.NotRequired[bool],
        "reg_both": typing.NotRequired[bool],
        "no_annot": typing.NotRequired[bool],
        "annot": typing.NotRequired[str],
        "aparc": typing.NotRequired[bool],
        "noneg": typing.NotRequired[bool],
        "init_reg": typing.NotRequired[str],
        "lta": typing.NotRequired[str],
        "init_from_tal": typing.NotRequired[bool],
        "outsurf": typing.NotRequired[str],
        "no_set_vol_geom": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SurfregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType
    """Output surface registration file"""

def surfreg_params(
    subject: str,
    target: str,
    cross_hemi: bool = False,
    reg_lh: bool = False,
    reg_rh: bool = False,
    reg_both: bool = False,
    no_annot: bool = False,
    annot: str | None = None,
    aparc: bool = False,
    noneg: bool = False,
    init_reg: str | None = None,
    lta: str | None = None,
    init_from_tal: bool = False,
    outsurf: str | None = None,
    no_set_vol_geom: bool = False,
    threads: float | None = None,
) -> Surfreg:
    """
    Build parameters.

    Args:
        subject: Subject to register
        target: Target average subject to use as a registration target
        cross_hemi: Perform cross-hemi registration
        reg_lh: Register left hemisphere only
        reg_rh: Register right hemisphere only
        reg_both: Register both left and right hemispheres
        no_annot: Do not use annot to rip
        annot: Use specified annotation name
        aparc: Set annotation name to aparc.annot
        noneg: Option flag with unspecified behavior in the provided help text
        init_reg: Initial registration name, default is sphere
        lta: Apply rotational components of affine registration
        init_from_tal: Use talaiach.xfm.lta for initial spherical registration
        outsurf: Output surface name, default depends on the target
        no_set_vol_geom: Do not set volume geometry and center the sphere
        threads: Number of threads to run in parallel

    Returns:
        Parameter dictionary.
    """
    params: Surfreg = {
        "@type": "freesurfer/surfreg",
        "subject": subject,
        "target": target,
        "cross_hemi": cross_hemi,
        "reg_lh": reg_lh,
        "reg_rh": reg_rh,
        "reg_both": reg_both,
        "no_annot": no_annot,
        "aparc": aparc,
        "noneg": noneg,
        "init_from_tal": init_from_tal,
        "no_set_vol_geom": no_set_vol_geom,
    }
    if annot is not None:
        params["annot"] = annot
    if init_reg is not None:
        params["init_reg"] = init_reg
    if lta is not None:
        params["lta"] = lta
    if outsurf is not None:
        params["outsurf"] = outsurf
    if threads is not None:
        params["threads"] = threads
    return params

def surfreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Surfreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], str):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `str`')
    if params.get("cross_hemi", None) is not None:
        if not isinstance(params["cross_hemi"], bool):
            raise StyxValidationError(f'`cross_hemi` has the wrong type: Received `{type(params["cross_hemi"])}` expected `bool`')
    if params.get("reg_lh", None) is not None:
        if not isinstance(params["reg_lh"], bool):
            raise StyxValidationError(f'`reg_lh` has the wrong type: Received `{type(params["reg_lh"])}` expected `bool`')
    if params.get("reg_rh", None) is not None:
        if not isinstance(params["reg_rh"], bool):
            raise StyxValidationError(f'`reg_rh` has the wrong type: Received `{type(params["reg_rh"])}` expected `bool`')
    if params.get("reg_both", None) is not None:
        if not isinstance(params["reg_both"], bool):
            raise StyxValidationError(f'`reg_both` has the wrong type: Received `{type(params["reg_both"])}` expected `bool`')
    if params.get("no_annot", None) is not None:
        if not isinstance(params["no_annot"], bool):
            raise StyxValidationError(f'`no_annot` has the wrong type: Received `{type(params["no_annot"])}` expected `bool`')
    if params.get("annot", None) is not None:
        if not isinstance(params["annot"], str):
            raise StyxValidationError(f'`annot` has the wrong type: Received `{type(params["annot"])}` expected `str`')
    if params.get("aparc", None) is not None:
        if not isinstance(params["aparc"], bool):
            raise StyxValidationError(f'`aparc` has the wrong type: Received `{type(params["aparc"])}` expected `bool`')
    if params.get("noneg", None) is not None:
        if not isinstance(params["noneg"], bool):
            raise StyxValidationError(f'`noneg` has the wrong type: Received `{type(params["noneg"])}` expected `bool`')
    if params.get("init_reg", None) is not None:
        if not isinstance(params["init_reg"], str):
            raise StyxValidationError(f'`init_reg` has the wrong type: Received `{type(params["init_reg"])}` expected `str`')
    if params.get("lta", None) is not None:
        if not isinstance(params["lta"], str):
            raise StyxValidationError(f'`lta` has the wrong type: Received `{type(params["lta"])}` expected `str`')
    if params.get("init_from_tal", None) is not None:
        if not isinstance(params["init_from_tal"], bool):
            raise StyxValidationError(f'`init_from_tal` has the wrong type: Received `{type(params["init_from_tal"])}` expected `bool`')
    if params.get("outsurf", None) is not None:
        if not isinstance(params["outsurf"], str):
            raise StyxValidationError(f'`outsurf` has the wrong type: Received `{type(params["outsurf"])}` expected `str`')
    if params.get("no_set_vol_geom", None) is not None:
        if not isinstance(params["no_set_vol_geom"], bool):
            raise StyxValidationError(f'`no_set_vol_geom` has the wrong type: Received `{type(params["no_set_vol_geom"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def surfreg_cargs(params: Surfreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("surfreg")
    cargs.append("--s")
    cargs.append(params["subject"])
    cargs.append("--t")
    cargs.append(params["target"])
    if params.get("cross_hemi"):
        cargs.append("--xhemi")
    if params.get("reg_lh"):
        cargs.append("--lh")
    if params.get("reg_rh"):
        cargs.append("--rh")
    if params.get("reg_both"):
        cargs.append("--lhrh")
    if params.get("no_annot"):
        cargs.append("--no-annot")
    v_0 = params.get("annot")
    if v_0 is not None:
        cargs.append("--annot")
        cargs.append(v_0)
    if params.get("aparc"):
        cargs.append("--aparc")
    if params.get("noneg"):
        cargs.append("--noneg")
    v_1 = params.get("init_reg")
    if v_1 is not None:
        cargs.append("--init-reg")
        cargs.append(v_1)
    v_2 = params.get("lta")
    if v_2 is not None:
        cargs.append("--lta")
        cargs.append(v_2)
    if params.get("init_from_tal"):
        cargs.append("--init-from-tal")
    v_3 = params.get("outsurf")
    if v_3 is not None:
        cargs.append("--o")
        cargs.append(v_3)
    if params.get("no_set_vol_geom"):
        cargs.append("--no-set-vol-geom")
    v_4 = params.get("threads")
    if v_4 is not None:
        cargs.append("--threads")
        cargs.append(str(v_4))
    return cargs

def surfreg_outputs(params: Surfreg, execution: Execution) -> SurfregOutputs:
    """Build the SurfregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_v: OutputPathType = execution.output_file("subject/surf/hemi.target.sphere.reg")
    return SurfregOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def surfreg_execute(params: Surfreg, runner: Runner | None = None) -> SurfregOutputs:
    """
    surfreg

    Performs surface registration (mris_register) between a subject and a target average subject based on the hemi.reg.template.tif atlas in the average subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surfreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFREG_METADATA)
    execution.params(params)
    args = surfreg_cargs(params, execution)
    out = surfreg_outputs(params, execution)
    execution.run(args)
    return out

def surfreg(
    subject: str,
    target: str,
    cross_hemi: bool = False,
    reg_lh: bool = False,
    reg_rh: bool = False,
    reg_both: bool = False,
    no_annot: bool = False,
    annot: str | None = None,
    aparc: bool = False,
    noneg: bool = False,
    init_reg: str | None = None,
    lta: str | None = None,
    init_from_tal: bool = False,
    outsurf: str | None = None,
    no_set_vol_geom: bool = False,
    threads: float | None = None,
    runner: Runner | None = None,
) -> SurfregOutputs:
    """
    surfreg

    Performs surface registration (mris_register) between a subject and a target average subject based on the hemi.reg.template.tif atlas in the average subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject to register
        target: Target average subject to use as a registration target
        cross_hemi: Perform cross-hemi registration
        reg_lh: Register left hemisphere only
        reg_rh: Register right hemisphere only
        reg_both: Register both left and right hemispheres
        no_annot: Do not use annot to rip
        annot: Use specified annotation name
        aparc: Set annotation name to aparc.annot
        noneg: Option flag with unspecified behavior in the provided help text
        init_reg: Initial registration name, default is sphere
        lta: Apply rotational components of affine registration
        init_from_tal: Use talaiach.xfm.lta for initial spherical registration
        outsurf: Output surface name, default depends on the target
        no_set_vol_geom: Do not set volume geometry and center the sphere
        threads: Number of threads to run in parallel
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surfreg_params(
        subject=subject,
        target=target,
        cross_hemi=cross_hemi,
        reg_lh=reg_lh,
        reg_rh=reg_rh,
        reg_both=reg_both,
        no_annot=no_annot,
        annot=annot,
        aparc=aparc,
        noneg=noneg,
        init_reg=init_reg,
        lta=lta,
        init_from_tal=init_from_tal,
        outsurf=outsurf,
        no_set_vol_geom=no_set_vol_geom,
        threads=threads,
    )
    return surfreg_execute(params, runner)

__all__ = [
    "Surfreg",
    "SurfregOutputs",
    "SURFREG_METADATA",
    "surfreg_cargs",
    "surfreg_outputs",
    "surfreg_params",
    "surfreg_execute",
    "surfreg_validate",
    "surfreg",
]