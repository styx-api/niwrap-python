# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PROBE_IMA_METADATA = Metadata(
    id="mri_probe_ima",
    name="mri_probe_ima",
    package="freesurfer",
)

MriProbeIma = typing.TypedDict(
    "MriProbeIma",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_probe_ima"]],
        "ima_file": InputPathType,
        "key_string": typing.NotRequired[str],
        "offset_type_len": typing.NotRequired[str],
        "attribute_name": typing.NotRequired[str],
        "fileinfo": typing.NotRequired[bool],
        "dictionary": typing.NotRequired[bool],
        "ob_stem": typing.NotRequired[str],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriProbeImaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_probe_ima_params(
    ima_file: InputPathType,
    key_string: str | None = None,
    offset_type_len: str | None = None,
    attribute_name: str | None = None,
    fileinfo: bool = False,
    dictionary: bool = False,
    ob_stem: str | None = None,
    help: bool = False,
    version: bool = False,
) -> MriProbeIma:
    """
    Build parameters.

    Args:
        ima_file: Path to the IMA file to be probed.
        key_string: String from dictionary to query the IMA file.
        offset_type_len: Offset, type, and string length for querying. Type can be\
            short, int, long, float, double, or string.
        attribute_name: Name of the file information attribute to query.
        fileinfo: Dump the interpreted file information.
        dictionary: Dump the dictionary without the need of an IMA file.
        ob_stem: Dump binary pixel data into bshort with stem prefix.
        help: Show help message and exit.
        version: Print version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriProbeIma = {
        "@type": "freesurfer/mri_probe_ima",
        "ima_file": ima_file,
        "fileinfo": fileinfo,
        "dictionary": dictionary,
        "help": help,
        "version": version,
    }
    if key_string is not None:
        params["key_string"] = key_string
    if offset_type_len is not None:
        params["offset_type_len"] = offset_type_len
    if attribute_name is not None:
        params["attribute_name"] = attribute_name
    if ob_stem is not None:
        params["ob_stem"] = ob_stem
    return params

def mri_probe_ima_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriProbeIma."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ima_file", None) is None:
        raise StyxValidationError("`ima_file` must not be None")
    if not isinstance(params["ima_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ima_file` has the wrong type: Received `{type(params["ima_file"])}` expected `InputPathType`')
    if params.get("key_string", None) is not None:
        if not isinstance(params["key_string"], str):
            raise StyxValidationError(f'`key_string` has the wrong type: Received `{type(params["key_string"])}` expected `str`')
    if params.get("offset_type_len", None) is not None:
        if not isinstance(params["offset_type_len"], str):
            raise StyxValidationError(f'`offset_type_len` has the wrong type: Received `{type(params["offset_type_len"])}` expected `str`')
    if params.get("attribute_name", None) is not None:
        if not isinstance(params["attribute_name"], str):
            raise StyxValidationError(f'`attribute_name` has the wrong type: Received `{type(params["attribute_name"])}` expected `str`')
    if params.get("fileinfo", None) is not None:
        if not isinstance(params["fileinfo"], bool):
            raise StyxValidationError(f'`fileinfo` has the wrong type: Received `{type(params["fileinfo"])}` expected `bool`')
    if params.get("dictionary", None) is not None:
        if not isinstance(params["dictionary"], bool):
            raise StyxValidationError(f'`dictionary` has the wrong type: Received `{type(params["dictionary"])}` expected `bool`')
    if params.get("ob_stem", None) is not None:
        if not isinstance(params["ob_stem"], str):
            raise StyxValidationError(f'`ob_stem` has the wrong type: Received `{type(params["ob_stem"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_probe_ima_cargs(params: MriProbeIma, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_probe_ima")
    cargs.append("--i")
    cargs.append(execution.input_file(params["ima_file"]))
    v_0 = params.get("key_string")
    if v_0 is not None:
        cargs.append("--key")
        cargs.append(v_0)
    v_1 = params.get("offset_type_len")
    if v_1 is not None:
        cargs.append("--o")
        cargs.append(v_1)
    v_2 = params.get("attribute_name")
    if v_2 is not None:
        cargs.append("--attr")
        cargs.append(v_2)
    if params.get("fileinfo"):
        cargs.append("--fileinfo")
    if params.get("dictionary"):
        cargs.append("--dictionary")
    v_3 = params.get("ob_stem")
    if v_3 is not None:
        cargs.append("--ob")
        cargs.append(v_3)
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_probe_ima_outputs(params: MriProbeIma, execution: Execution) -> MriProbeImaOutputs:
    """Build the MriProbeImaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriProbeImaOutputs(
        root=root_v,
    )

def mri_probe_ima_execute(params: MriProbeIma, runner: Runner | None = None) -> MriProbeImaOutputs:
    """
    mri_probe_ima

    Query Siemens IMA files to extract header information

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_probe_ima_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PROBE_IMA_METADATA)
    execution.params(params)
    args = mri_probe_ima_cargs(params, execution)
    out = mri_probe_ima_outputs(params, execution)
    execution.run(args)
    return out

def mri_probe_ima(
    ima_file: InputPathType,
    key_string: str | None = None,
    offset_type_len: str | None = None,
    attribute_name: str | None = None,
    fileinfo: bool = False,
    dictionary: bool = False,
    ob_stem: str | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriProbeImaOutputs:
    """
    mri_probe_ima

    Query Siemens IMA files to extract header information

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        ima_file: Path to the IMA file to be probed.
        key_string: String from dictionary to query the IMA file.
        offset_type_len: Offset, type, and string length for querying. Type can be\
            short, int, long, float, double, or string.
        attribute_name: Name of the file information attribute to query.
        fileinfo: Dump the interpreted file information.
        dictionary: Dump the dictionary without the need of an IMA file.
        ob_stem: Dump binary pixel data into bshort with stem prefix.
        help: Show help message and exit.
        version: Print version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_probe_ima_params(
        ima_file=ima_file,
        key_string=key_string,
        offset_type_len=offset_type_len,
        attribute_name=attribute_name,
        fileinfo=fileinfo,
        dictionary=dictionary,
        ob_stem=ob_stem,
        help=help,
        version=version,
    )
    return mri_probe_ima_execute(params, runner)

__all__ = [
    "MriProbeIma",
    "MriProbeImaOutputs",
    "MRI_PROBE_IMA_METADATA",
    "mri_probe_ima_cargs",
    "mri_probe_ima_outputs",
    "mri_probe_ima_params",
    "mri_probe_ima_execute",
    "mri_probe_ima_validate",
    "mri_probe_ima",
]