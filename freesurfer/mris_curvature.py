# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CURVATURE_METADATA = Metadata(
    id="mris_curvature",
    name="mris_curvature",
    package="freesurfer",
)

MrisCurvature = typing.TypedDict(
    "MrisCurvature",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_curvature"]],
        "save_curvature_files": typing.NotRequired[bool],
        "max_principal_curvature": typing.NotRequired[bool],
        "mgh_output_format": typing.NotRequired[bool],
        "min_principal_curvature": typing.NotRequired[bool],
        "iterative_averages": typing.NotRequired[float],
        "neighborhood_size": typing.NotRequired[float],
        "random_seed": typing.NotRequired[float],
        "curvatures": typing.NotRequired[str],
        "H_curvature": typing.NotRequired[str],
        "K_curvature": typing.NotRequired[str],
        "k1_curvature": typing.NotRequired[str],
        "k2_curvature": typing.NotRequired[str],
        "k1k2_curvature": typing.NotRequired[str],
        "input_surface": InputPathType,
    },
)

@dataclasses.dataclass
class MrisCurvatureOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_curvature_params(
    input_surface: InputPathType,
    save_curvature_files: bool = False,
    max_principal_curvature: bool = False,
    mgh_output_format: bool = False,
    min_principal_curvature: bool = False,
    iterative_averages: float | None = None,
    neighborhood_size: float | None = None,
    random_seed: float | None = None,
    curvatures: str | None = None,
    H_curvature: str | None = None,
    K_curvature: str | None = None,
    k1_curvature: str | None = None,
    k2_curvature: str | None = None,
    k1k2_curvature: str | None = None,
) -> MrisCurvature:
    """
    Build parameters.

    Args:
        input_surface: Input surface file
        save_curvature_files: Save curvature files (will only generate screen\
            output without this option)
        max_principal_curvature: Save 1st (max) principal curvature in\
            ?h.<surface>.max file
        mgh_output_format: Save outputs in .mgz format
        min_principal_curvature: Save 2nd (min) principal curvature in\
            ?h.<surface>.min file
        iterative_averages: Perform <avgs> iterative averages of curvature measure\
            before saving
        neighborhood_size: Set neighborhood size to nbrs
        random_seed: Set random number generator to seed N
        curvatures: Stand-alone option to save H (mean), K (gaussian), k1, and k2\
            curvatures to stem.{curvname}.mgz
        H_curvature: Stand-alone option to save H mean curvature to stem.H.mgz
        K_curvature: Stand-alone option to save K gaussian curvature to stem.K.mgz
        k1_curvature: Stand-alone option to save k1 (primary principle) curvature\
            to stem.k1.mgz
        k2_curvature: Stand-alone option to save k2 (secondary principle) curvature\
            to stem.k2.mgz
        k1k2_curvature: Stand-alone option to save k1 and k2 curvature to\
            stem.{k1,k2}.mgz

    Returns:
        Parameter dictionary.
    """
    params: MrisCurvature = {
        "@type": "freesurfer/mris_curvature",
        "input_surface": input_surface,
        "save_curvature_files": save_curvature_files,
        "max_principal_curvature": max_principal_curvature,
        "mgh_output_format": mgh_output_format,
        "min_principal_curvature": min_principal_curvature,
    }
    if iterative_averages is not None:
        params["iterative_averages"] = iterative_averages
    if neighborhood_size is not None:
        params["neighborhood_size"] = neighborhood_size
    if random_seed is not None:
        params["random_seed"] = random_seed
    if curvatures is not None:
        params["curvatures"] = curvatures
    if H_curvature is not None:
        params["H_curvature"] = H_curvature
    if K_curvature is not None:
        params["K_curvature"] = K_curvature
    if k1_curvature is not None:
        params["k1_curvature"] = k1_curvature
    if k2_curvature is not None:
        params["k2_curvature"] = k2_curvature
    if k1k2_curvature is not None:
        params["k1k2_curvature"] = k1k2_curvature
    return params

def mris_curvature_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCurvature."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("save_curvature_files", None) is not None:
        if not isinstance(params["save_curvature_files"], bool):
            raise StyxValidationError(f'`save_curvature_files` has the wrong type: Received `{type(params["save_curvature_files"])}` expected `bool`')
    if params.get("max_principal_curvature", None) is not None:
        if not isinstance(params["max_principal_curvature"], bool):
            raise StyxValidationError(f'`max_principal_curvature` has the wrong type: Received `{type(params["max_principal_curvature"])}` expected `bool`')
    if params.get("mgh_output_format", None) is not None:
        if not isinstance(params["mgh_output_format"], bool):
            raise StyxValidationError(f'`mgh_output_format` has the wrong type: Received `{type(params["mgh_output_format"])}` expected `bool`')
    if params.get("min_principal_curvature", None) is not None:
        if not isinstance(params["min_principal_curvature"], bool):
            raise StyxValidationError(f'`min_principal_curvature` has the wrong type: Received `{type(params["min_principal_curvature"])}` expected `bool`')
    if params.get("iterative_averages", None) is not None:
        if not isinstance(params["iterative_averages"], (float, int)):
            raise StyxValidationError(f'`iterative_averages` has the wrong type: Received `{type(params["iterative_averages"])}` expected `float`')
    if params.get("neighborhood_size", None) is not None:
        if not isinstance(params["neighborhood_size"], (float, int)):
            raise StyxValidationError(f'`neighborhood_size` has the wrong type: Received `{type(params["neighborhood_size"])}` expected `float`')
    if params.get("random_seed", None) is not None:
        if not isinstance(params["random_seed"], (float, int)):
            raise StyxValidationError(f'`random_seed` has the wrong type: Received `{type(params["random_seed"])}` expected `float`')
    if params.get("curvatures", None) is not None:
        if not isinstance(params["curvatures"], str):
            raise StyxValidationError(f'`curvatures` has the wrong type: Received `{type(params["curvatures"])}` expected `str`')
    if params.get("H_curvature", None) is not None:
        if not isinstance(params["H_curvature"], str):
            raise StyxValidationError(f'`H_curvature` has the wrong type: Received `{type(params["H_curvature"])}` expected `str`')
    if params.get("K_curvature", None) is not None:
        if not isinstance(params["K_curvature"], str):
            raise StyxValidationError(f'`K_curvature` has the wrong type: Received `{type(params["K_curvature"])}` expected `str`')
    if params.get("k1_curvature", None) is not None:
        if not isinstance(params["k1_curvature"], str):
            raise StyxValidationError(f'`k1_curvature` has the wrong type: Received `{type(params["k1_curvature"])}` expected `str`')
    if params.get("k2_curvature", None) is not None:
        if not isinstance(params["k2_curvature"], str):
            raise StyxValidationError(f'`k2_curvature` has the wrong type: Received `{type(params["k2_curvature"])}` expected `str`')
    if params.get("k1k2_curvature", None) is not None:
        if not isinstance(params["k1k2_curvature"], str):
            raise StyxValidationError(f'`k1k2_curvature` has the wrong type: Received `{type(params["k1k2_curvature"])}` expected `str`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')

def mris_curvature_cargs(params: MrisCurvature, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_curvature")
    if params.get("save_curvature_files"):
        cargs.append("-w")
    if params.get("max_principal_curvature"):
        cargs.append("-max")
    if params.get("mgh_output_format"):
        cargs.append("-mgh")
    if params.get("min_principal_curvature"):
        cargs.append("-min")
    v_0 = params.get("iterative_averages")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    v_1 = params.get("neighborhood_size")
    if v_1 is not None:
        cargs.append("-nbrs")
        cargs.append(str(v_1))
    v_2 = params.get("random_seed")
    if v_2 is not None:
        cargs.append("-seed")
        cargs.append(str(v_2))
    v_3 = params.get("curvatures")
    if v_3 is not None:
        cargs.append("-curvs")
        cargs.append(v_3)
    v_4 = params.get("H_curvature")
    if v_4 is not None:
        cargs.append("-H")
        cargs.append(v_4)
    v_5 = params.get("K_curvature")
    if v_5 is not None:
        cargs.append("-K")
        cargs.append(v_5)
    v_6 = params.get("k1_curvature")
    if v_6 is not None:
        cargs.append("-k1")
        cargs.append(v_6)
    v_7 = params.get("k2_curvature")
    if v_7 is not None:
        cargs.append("-k2")
        cargs.append(v_7)
    v_8 = params.get("k1k2_curvature")
    if v_8 is not None:
        cargs.append("-k1k2")
        cargs.append(v_8)
    cargs.append(execution.input_file(params["input_surface"]))
    return cargs

def mris_curvature_outputs(params: MrisCurvature, execution: Execution) -> MrisCurvatureOutputs:
    """Build the MrisCurvatureOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisCurvatureOutputs(
        root=root_v,
    )

def mris_curvature_execute(params: MrisCurvature, runner: Runner | None = None) -> MrisCurvatureOutputs:
    """
    mris_curvature

    Compute the second fundamental form of a cortical surface to generate mean and Gaussian curvature.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_curvature_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CURVATURE_METADATA)
    execution.params(params)
    args = mris_curvature_cargs(params, execution)
    out = mris_curvature_outputs(params, execution)
    execution.run(args)
    return out

def mris_curvature(
    input_surface: InputPathType,
    save_curvature_files: bool = False,
    max_principal_curvature: bool = False,
    mgh_output_format: bool = False,
    min_principal_curvature: bool = False,
    iterative_averages: float | None = None,
    neighborhood_size: float | None = None,
    random_seed: float | None = None,
    curvatures: str | None = None,
    H_curvature: str | None = None,
    K_curvature: str | None = None,
    k1_curvature: str | None = None,
    k2_curvature: str | None = None,
    k1k2_curvature: str | None = None,
    runner: Runner | None = None,
) -> MrisCurvatureOutputs:
    """
    mris_curvature

    Compute the second fundamental form of a cortical surface to generate mean and Gaussian curvature.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file
        save_curvature_files: Save curvature files (will only generate screen\
            output without this option)
        max_principal_curvature: Save 1st (max) principal curvature in\
            ?h.<surface>.max file
        mgh_output_format: Save outputs in .mgz format
        min_principal_curvature: Save 2nd (min) principal curvature in\
            ?h.<surface>.min file
        iterative_averages: Perform <avgs> iterative averages of curvature measure\
            before saving
        neighborhood_size: Set neighborhood size to nbrs
        random_seed: Set random number generator to seed N
        curvatures: Stand-alone option to save H (mean), K (gaussian), k1, and k2\
            curvatures to stem.{curvname}.mgz
        H_curvature: Stand-alone option to save H mean curvature to stem.H.mgz
        K_curvature: Stand-alone option to save K gaussian curvature to stem.K.mgz
        k1_curvature: Stand-alone option to save k1 (primary principle) curvature\
            to stem.k1.mgz
        k2_curvature: Stand-alone option to save k2 (secondary principle) curvature\
            to stem.k2.mgz
        k1k2_curvature: Stand-alone option to save k1 and k2 curvature to\
            stem.{k1,k2}.mgz
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_curvature_params(
        input_surface=input_surface,
        save_curvature_files=save_curvature_files,
        max_principal_curvature=max_principal_curvature,
        mgh_output_format=mgh_output_format,
        min_principal_curvature=min_principal_curvature,
        iterative_averages=iterative_averages,
        neighborhood_size=neighborhood_size,
        random_seed=random_seed,
        curvatures=curvatures,
        H_curvature=H_curvature,
        K_curvature=K_curvature,
        k1_curvature=k1_curvature,
        k2_curvature=k2_curvature,
        k1k2_curvature=k1k2_curvature,
    )
    return mris_curvature_execute(params, runner)

__all__ = [
    "MrisCurvature",
    "MrisCurvatureOutputs",
    "MRIS_CURVATURE_METADATA",
    "mris_curvature_cargs",
    "mris_curvature_outputs",
    "mris_curvature_params",
    "mris_curvature_execute",
    "mris_curvature_validate",
    "mris_curvature",
]