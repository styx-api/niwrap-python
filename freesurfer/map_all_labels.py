# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAP_ALL_LABELS_METADATA = Metadata(
    id="map_all_labels",
    name="map_all_labels",
    package="freesurfer",
)

MapAllLabels = typing.TypedDict(
    "MapAllLabels",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/map_all_labels"]],
        "which": str,
        "fname": str,
        "hemi": str,
        "spherical_surf": str,
        "subjects": list[str],
        "output": str,
    },
)

@dataclasses.dataclass
class MapAllLabelsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The output of the mapping process"""

def map_all_labels_params(
    which: str,
    fname: str,
    hemi: str,
    spherical_surf: str,
    subjects: list[str],
    output: str,
) -> MapAllLabels:
    """
    Build parameters.

    Args:
        which: Select what to map (e.g. coords, label, vals, curv, area)
        fname: Filename related to the mapping
        hemi: Hemisphere to process (e.g. lh, rh)
        spherical_surf: Spherical surface to use
        subjects: List of subject identifiers
        output: Output path

    Returns:
        Parameter dictionary.
    """
    params: MapAllLabels = {
        "@type": "freesurfer/map_all_labels",
        "which": which,
        "fname": fname,
        "hemi": hemi,
        "spherical_surf": spherical_surf,
        "subjects": subjects,
        "output": output,
    }
    return params

def map_all_labels_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MapAllLabels."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("which", None) is None:
        raise StyxValidationError("`which` must not be None")
    if not isinstance(params["which"], str):
        raise StyxValidationError(f'`which` has the wrong type: Received `{type(params["which"])}` expected `str`')
    if params.get("fname", None) is None:
        raise StyxValidationError("`fname` must not be None")
    if not isinstance(params["fname"], str):
        raise StyxValidationError(f'`fname` has the wrong type: Received `{type(params["fname"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("spherical_surf", None) is None:
        raise StyxValidationError("`spherical_surf` must not be None")
    if not isinstance(params["spherical_surf"], str):
        raise StyxValidationError(f'`spherical_surf` has the wrong type: Received `{type(params["spherical_surf"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def map_all_labels_cargs(params: MapAllLabels, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("map_all_labels")
    cargs.append(params["which"])
    cargs.append(params["fname"])
    cargs.append(params["hemi"])
    cargs.append(params["spherical_surf"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output"])
    return cargs

def map_all_labels_outputs(params: MapAllLabels, execution: Execution) -> MapAllLabelsOutputs:
    """Build the MapAllLabelsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output"])
    return MapAllLabelsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def map_all_labels_execute(params: MapAllLabels, runner: Runner | None = None) -> MapAllLabelsOutputs:
    """
    map_all_labels

    Tool for mapping labels onto subject surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    map_all_labels_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAP_ALL_LABELS_METADATA)
    execution.params(params)
    args = map_all_labels_cargs(params, execution)
    out = map_all_labels_outputs(params, execution)
    execution.run(args)
    return out

def map_all_labels(
    which: str,
    fname: str,
    hemi: str,
    spherical_surf: str,
    subjects: list[str],
    output: str,
    runner: Runner | None = None,
) -> MapAllLabelsOutputs:
    """
    map_all_labels

    Tool for mapping labels onto subject surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        which: Select what to map (e.g. coords, label, vals, curv, area)
        fname: Filename related to the mapping
        hemi: Hemisphere to process (e.g. lh, rh)
        spherical_surf: Spherical surface to use
        subjects: List of subject identifiers
        output: Output path
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = map_all_labels_params(
        which=which,
        fname=fname,
        hemi=hemi,
        spherical_surf=spherical_surf,
        subjects=subjects,
        output=output,
    )
    return map_all_labels_execute(params, runner)

__all__ = [
    "MapAllLabels",
    "MapAllLabelsOutputs",
    "MAP_ALL_LABELS_METADATA",
    "map_all_labels_cargs",
    "map_all_labels_outputs",
    "map_all_labels_params",
    "map_all_labels_execute",
    "map_all_labels_validate",
    "map_all_labels",
]