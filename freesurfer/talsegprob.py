# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TALSEGPROB_METADATA = Metadata(
    id="talsegprob",
    name="talsegprob",
    package="freesurfer",
)

Talsegprob = typing.TypedDict(
    "Talsegprob",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/talsegprob"]],
        "subjects_list": typing.NotRequired[list[str]],
        "fsgd_file": typing.NotRequired[InputPathType],
        "segmentation_number": typing.NotRequired[float],
        "second_segmentation_number": typing.NotRequired[float],
        "hippo_flag": typing.NotRequired[bool],
        "left_hippo_flag": typing.NotRequired[bool],
        "right_hippo_flag": typing.NotRequired[bool],
        "segmentation_file": typing.NotRequired[InputPathType],
        "probability_output": typing.NotRequired[str],
        "vote_output": typing.NotRequired[str],
        "concat_output": typing.NotRequired[str],
        "xform_file": typing.NotRequired[InputPathType],
        "subjects_dir": typing.NotRequired[str],
        "tmpdir": typing.NotRequired[str],
        "nocleanup_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
        "echo_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TalsegprobOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    probability_output_file: OutputPathType | None
    """Probability output file."""
    vote_output_file: OutputPathType | None
    """Vote output file."""
    concat_output_file: OutputPathType | None
    """Concatenated output file."""

def talsegprob_params(
    subjects_list: list[str] | None = None,
    fsgd_file: InputPathType | None = None,
    segmentation_number: float | None = None,
    second_segmentation_number: float | None = None,
    hippo_flag: bool = False,
    left_hippo_flag: bool = False,
    right_hippo_flag: bool = False,
    segmentation_file: InputPathType | None = None,
    probability_output: str | None = None,
    vote_output: str | None = None,
    concat_output: str | None = None,
    xform_file: InputPathType | None = None,
    subjects_dir: str | None = None,
    tmpdir: str | None = None,
    nocleanup_flag: bool = False,
    version_flag: bool = False,
    echo_flag: bool = False,
) -> Talsegprob:
    """
    Build parameters.

    Args:
        subjects_list: List of subjects to include in the analysis.
        fsgd_file: FSGD file to get subject list.
        segmentation_number: Segmentation number.
        second_segmentation_number: Second segmentation number.
        hippo_flag: Use segmentation numbers 17 and 53.
        left_hippo_flag: Use segmentation number 17.
        right_hippo_flag: Use segmentation number 53.
        segmentation_file: Use subject/mri/segfile.mgz instead of aseg.
        probability_output: Probability output file name.
        vote_output: Vote output file name.
        concat_output: Concatenated output file name.
        xform_file: Transformation file to use (default is talairach.xfm).
        subjects_dir: SUBJECTS_DIR to use instead of the one in the environment.
        tmpdir: Temporary directory (implies --nocleanup).
        nocleanup_flag: Do not delete temporary directory.
        version_flag: Display script version information.
        echo_flag: Enable command echo, for debug.

    Returns:
        Parameter dictionary.
    """
    params: Talsegprob = {
        "@type": "freesurfer/talsegprob",
        "hippo_flag": hippo_flag,
        "left_hippo_flag": left_hippo_flag,
        "right_hippo_flag": right_hippo_flag,
        "nocleanup_flag": nocleanup_flag,
        "version_flag": version_flag,
        "echo_flag": echo_flag,
    }
    if subjects_list is not None:
        params["subjects_list"] = subjects_list
    if fsgd_file is not None:
        params["fsgd_file"] = fsgd_file
    if segmentation_number is not None:
        params["segmentation_number"] = segmentation_number
    if second_segmentation_number is not None:
        params["second_segmentation_number"] = second_segmentation_number
    if segmentation_file is not None:
        params["segmentation_file"] = segmentation_file
    if probability_output is not None:
        params["probability_output"] = probability_output
    if vote_output is not None:
        params["vote_output"] = vote_output
    if concat_output is not None:
        params["concat_output"] = concat_output
    if xform_file is not None:
        params["xform_file"] = xform_file
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if tmpdir is not None:
        params["tmpdir"] = tmpdir
    return params

def talsegprob_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Talsegprob."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects_list", None) is not None:
        if not isinstance(params["subjects_list"], list):
            raise StyxValidationError(f'`subjects_list` has the wrong type: Received `{type(params["subjects_list"])}` expected `list[str]`')
        for e in params["subjects_list"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`subjects_list` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("fsgd_file", None) is not None:
        if not isinstance(params["fsgd_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fsgd_file` has the wrong type: Received `{type(params["fsgd_file"])}` expected `InputPathType`')
    if params.get("segmentation_number", None) is not None:
        if not isinstance(params["segmentation_number"], (float, int)):
            raise StyxValidationError(f'`segmentation_number` has the wrong type: Received `{type(params["segmentation_number"])}` expected `float`')
    if params.get("second_segmentation_number", None) is not None:
        if not isinstance(params["second_segmentation_number"], (float, int)):
            raise StyxValidationError(f'`second_segmentation_number` has the wrong type: Received `{type(params["second_segmentation_number"])}` expected `float`')
    if params.get("hippo_flag", None) is not None:
        if not isinstance(params["hippo_flag"], bool):
            raise StyxValidationError(f'`hippo_flag` has the wrong type: Received `{type(params["hippo_flag"])}` expected `bool`')
    if params.get("left_hippo_flag", None) is not None:
        if not isinstance(params["left_hippo_flag"], bool):
            raise StyxValidationError(f'`left_hippo_flag` has the wrong type: Received `{type(params["left_hippo_flag"])}` expected `bool`')
    if params.get("right_hippo_flag", None) is not None:
        if not isinstance(params["right_hippo_flag"], bool):
            raise StyxValidationError(f'`right_hippo_flag` has the wrong type: Received `{type(params["right_hippo_flag"])}` expected `bool`')
    if params.get("segmentation_file", None) is not None:
        if not isinstance(params["segmentation_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`segmentation_file` has the wrong type: Received `{type(params["segmentation_file"])}` expected `InputPathType`')
    if params.get("probability_output", None) is not None:
        if not isinstance(params["probability_output"], str):
            raise StyxValidationError(f'`probability_output` has the wrong type: Received `{type(params["probability_output"])}` expected `str`')
    if params.get("vote_output", None) is not None:
        if not isinstance(params["vote_output"], str):
            raise StyxValidationError(f'`vote_output` has the wrong type: Received `{type(params["vote_output"])}` expected `str`')
    if params.get("concat_output", None) is not None:
        if not isinstance(params["concat_output"], str):
            raise StyxValidationError(f'`concat_output` has the wrong type: Received `{type(params["concat_output"])}` expected `str`')
    if params.get("xform_file", None) is not None:
        if not isinstance(params["xform_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`xform_file` has the wrong type: Received `{type(params["xform_file"])}` expected `InputPathType`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("tmpdir", None) is not None:
        if not isinstance(params["tmpdir"], str):
            raise StyxValidationError(f'`tmpdir` has the wrong type: Received `{type(params["tmpdir"])}` expected `str`')
    if params.get("nocleanup_flag", None) is not None:
        if not isinstance(params["nocleanup_flag"], bool):
            raise StyxValidationError(f'`nocleanup_flag` has the wrong type: Received `{type(params["nocleanup_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')
    if params.get("echo_flag", None) is not None:
        if not isinstance(params["echo_flag"], bool):
            raise StyxValidationError(f'`echo_flag` has the wrong type: Received `{type(params["echo_flag"])}` expected `bool`')

def talsegprob_cargs(params: Talsegprob, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("talsegprob")
    v_0 = params.get("subjects_list")
    if v_0 is not None:
        cargs.append("--subjects")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("fsgd_file")
    if v_1 is not None:
        cargs.append("--fsgd")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("segmentation_number")
    if v_2 is not None:
        cargs.append("--seg")
        cargs.append(str(v_2))
    v_3 = params.get("second_segmentation_number")
    if v_3 is not None:
        cargs.append("<--seg")
        cargs.append(str(v_3))
    if params.get("hippo_flag"):
        cargs.append("--hippo")
    if params.get("left_hippo_flag"):
        cargs.append("--left-hippo")
    if params.get("right_hippo_flag"):
        cargs.append("--right-hippo")
    v_4 = params.get("segmentation_file")
    if v_4 is not None:
        cargs.append("--segmentation")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("probability_output")
    if v_5 is not None:
        cargs.append("--p")
        cargs.append(v_5)
    v_6 = params.get("vote_output")
    if v_6 is not None:
        cargs.append("--vote")
        cargs.append(v_6)
    v_7 = params.get("concat_output")
    if v_7 is not None:
        cargs.append("--c")
        cargs.append(v_7)
    v_8 = params.get("xform_file")
    if v_8 is not None:
        cargs.append("--xform")
        cargs.append(execution.input_file(v_8))
    v_9 = params.get("subjects_dir")
    if v_9 is not None:
        cargs.append("--sdir")
        cargs.append(v_9)
    v_10 = params.get("tmpdir")
    if v_10 is not None:
        cargs.append("--tmpdir")
        cargs.append(v_10)
    if params.get("nocleanup_flag"):
        cargs.append("--nocleanup")
    if params.get("version_flag"):
        cargs.append("--version")
    if params.get("echo_flag"):
        cargs.append("--echo")
    return cargs

def talsegprob_outputs(params: Talsegprob, execution: Execution) -> TalsegprobOutputs:
    """Build the TalsegprobOutputs object for this tool."""
    probability_output_file_v: OutputPathType | None = None
    vote_output_file_v: OutputPathType | None = None
    concat_output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("probability_output")
    if __v0 is not None:
        probability_output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("vote_output")
    if __v1 is not None:
        vote_output_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("concat_output")
    if __v2 is not None:
        concat_output_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    return TalsegprobOutputs(
        root=root_v,
        probability_output_file=probability_output_file_v,
        vote_output_file=vote_output_file_v,
        concat_output_file=concat_output_file_v,
    )

def talsegprob_execute(params: Talsegprob, runner: Runner | None = None) -> TalsegprobOutputs:
    """
    talsegprob

    Tool to create a binary probability volume from aseg.mgz based on segmentation numbers, resliced to talirach/MNI305/fsaverage space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    talsegprob_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TALSEGPROB_METADATA)
    execution.params(params)
    args = talsegprob_cargs(params, execution)
    out = talsegprob_outputs(params, execution)
    execution.run(args)
    return out

def talsegprob(
    subjects_list: list[str] | None = None,
    fsgd_file: InputPathType | None = None,
    segmentation_number: float | None = None,
    second_segmentation_number: float | None = None,
    hippo_flag: bool = False,
    left_hippo_flag: bool = False,
    right_hippo_flag: bool = False,
    segmentation_file: InputPathType | None = None,
    probability_output: str | None = None,
    vote_output: str | None = None,
    concat_output: str | None = None,
    xform_file: InputPathType | None = None,
    subjects_dir: str | None = None,
    tmpdir: str | None = None,
    nocleanup_flag: bool = False,
    version_flag: bool = False,
    echo_flag: bool = False,
    runner: Runner | None = None,
) -> TalsegprobOutputs:
    """
    talsegprob

    Tool to create a binary probability volume from aseg.mgz based on segmentation numbers, resliced to talirach/MNI305/fsaverage space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects_list: List of subjects to include in the analysis.
        fsgd_file: FSGD file to get subject list.
        segmentation_number: Segmentation number.
        second_segmentation_number: Second segmentation number.
        hippo_flag: Use segmentation numbers 17 and 53.
        left_hippo_flag: Use segmentation number 17.
        right_hippo_flag: Use segmentation number 53.
        segmentation_file: Use subject/mri/segfile.mgz instead of aseg.
        probability_output: Probability output file name.
        vote_output: Vote output file name.
        concat_output: Concatenated output file name.
        xform_file: Transformation file to use (default is talairach.xfm).
        subjects_dir: SUBJECTS_DIR to use instead of the one in the environment.
        tmpdir: Temporary directory (implies --nocleanup).
        nocleanup_flag: Do not delete temporary directory.
        version_flag: Display script version information.
        echo_flag: Enable command echo, for debug.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = talsegprob_params(
        subjects_list=subjects_list,
        fsgd_file=fsgd_file,
        segmentation_number=segmentation_number,
        second_segmentation_number=second_segmentation_number,
        hippo_flag=hippo_flag,
        left_hippo_flag=left_hippo_flag,
        right_hippo_flag=right_hippo_flag,
        segmentation_file=segmentation_file,
        probability_output=probability_output,
        vote_output=vote_output,
        concat_output=concat_output,
        xform_file=xform_file,
        subjects_dir=subjects_dir,
        tmpdir=tmpdir,
        nocleanup_flag=nocleanup_flag,
        version_flag=version_flag,
        echo_flag=echo_flag,
    )
    return talsegprob_execute(params, runner)

__all__ = [
    "Talsegprob",
    "TalsegprobOutputs",
    "TALSEGPROB_METADATA",
    "talsegprob_cargs",
    "talsegprob_outputs",
    "talsegprob_params",
    "talsegprob_execute",
    "talsegprob_validate",
    "talsegprob",
]