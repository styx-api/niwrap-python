# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HISTO_COMPUTE_JOINT_DENSITY_METADATA = Metadata(
    id="histo_compute_joint_density",
    name="histo_compute_joint_density",
    package="freesurfer",
)

HistoComputeJointDensity = typing.TypedDict(
    "HistoComputeJointDensity",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/histo_compute_joint_density"]],
        "volume1": InputPathType,
        "volume2": InputPathType,
        "joint_density_file": str,
    },
)

@dataclasses.dataclass
class HistoComputeJointDensityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_joint_density: OutputPathType
    """Computed joint density output file"""

def histo_compute_joint_density_params(
    volume1: InputPathType,
    volume2: InputPathType,
    joint_density_file: str,
) -> HistoComputeJointDensity:
    """
    Build parameters.

    Args:
        volume1: Input volume 1
        volume2: Input volume 2
        joint_density_file: Output joint density file

    Returns:
        Parameter dictionary.
    """
    params: HistoComputeJointDensity = {
        "@type": "freesurfer/histo_compute_joint_density",
        "volume1": volume1,
        "volume2": volume2,
        "joint_density_file": joint_density_file,
    }
    return params

def histo_compute_joint_density_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HistoComputeJointDensity."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume1", None) is None:
        raise StyxValidationError("`volume1` must not be None")
    if not isinstance(params["volume1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume1` has the wrong type: Received `{type(params["volume1"])}` expected `InputPathType`')
    if params.get("volume2", None) is None:
        raise StyxValidationError("`volume2` must not be None")
    if not isinstance(params["volume2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume2` has the wrong type: Received `{type(params["volume2"])}` expected `InputPathType`')
    if params.get("joint_density_file", None) is None:
        raise StyxValidationError("`joint_density_file` must not be None")
    if not isinstance(params["joint_density_file"], str):
        raise StyxValidationError(f'`joint_density_file` has the wrong type: Received `{type(params["joint_density_file"])}` expected `str`')

def histo_compute_joint_density_cargs(params: HistoComputeJointDensity, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("histo_compute_joint_density")
    cargs.append(execution.input_file(params["volume1"]))
    cargs.append(execution.input_file(params["volume2"]))
    cargs.append(params["joint_density_file"])
    return cargs

def histo_compute_joint_density_outputs(params: HistoComputeJointDensity, execution: Execution) -> HistoComputeJointDensityOutputs:
    """Build the HistoComputeJointDensityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_joint_density_v: OutputPathType = execution.output_file(params["joint_density_file"])
    return HistoComputeJointDensityOutputs(
        root=root_v,
        output_joint_density=output_joint_density_v,
    )

def histo_compute_joint_density_execute(params: HistoComputeJointDensity, runner: Runner | None = None) -> HistoComputeJointDensityOutputs:
    """
    histo_compute_joint_density

    A tool to compute the joint density of two volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    histo_compute_joint_density_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HISTO_COMPUTE_JOINT_DENSITY_METADATA)
    execution.params(params)
    args = histo_compute_joint_density_cargs(params, execution)
    out = histo_compute_joint_density_outputs(params, execution)
    execution.run(args)
    return out

def histo_compute_joint_density(
    volume1: InputPathType,
    volume2: InputPathType,
    joint_density_file: str,
    runner: Runner | None = None,
) -> HistoComputeJointDensityOutputs:
    """
    histo_compute_joint_density

    A tool to compute the joint density of two volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume1: Input volume 1
        volume2: Input volume 2
        joint_density_file: Output joint density file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = histo_compute_joint_density_params(
        volume1=volume1,
        volume2=volume2,
        joint_density_file=joint_density_file,
    )
    return histo_compute_joint_density_execute(params, runner)

__all__ = [
    "HistoComputeJointDensity",
    "HistoComputeJointDensityOutputs",
    "HISTO_COMPUTE_JOINT_DENSITY_METADATA",
    "histo_compute_joint_density_cargs",
    "histo_compute_joint_density_outputs",
    "histo_compute_joint_density_params",
    "histo_compute_joint_density_execute",
    "histo_compute_joint_density_validate",
    "histo_compute_joint_density",
]