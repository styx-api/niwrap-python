# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_SUBJECT_SC_METADATA = Metadata(
    id="segment_subject_sc",
    name="segment_subject_sc",
    package="freesurfer",
)

SegmentSubjectSc = typing.TypedDict(
    "SegmentSubjectSc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segment_subject_sc"]],
        "invol": InputPathType,
        "outxfm": InputPathType,
        "log": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SegmentSubjectScOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xfm_file: OutputPathType
    """Output transform file (xfm)"""

def segment_subject_sc_params(
    invol: InputPathType,
    outxfm: InputPathType,
    log: str | None = None,
    debug: bool = False,
) -> SegmentSubjectSc:
    """
    Build parameters.

    Args:
        invol: Input volume
        outxfm: Output xfm file
        log: Log file. Default is outdir/talarach.log
        debug: Turn on debugging

    Returns:
        Parameter dictionary.
    """
    params: SegmentSubjectSc = {
        "@type": "freesurfer/segment_subject_sc",
        "invol": invol,
        "outxfm": outxfm,
        "debug": debug,
    }
    if log is not None:
        params["log"] = log
    return params

def segment_subject_sc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentSubjectSc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("invol", None) is None:
        raise StyxValidationError("`invol` must not be None")
    if not isinstance(params["invol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`invol` has the wrong type: Received `{type(params["invol"])}` expected `InputPathType`')
    if params.get("outxfm", None) is None:
        raise StyxValidationError("`outxfm` must not be None")
    if not isinstance(params["outxfm"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outxfm` has the wrong type: Received `{type(params["outxfm"])}` expected `InputPathType`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], str):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def segment_subject_sc_cargs(params: SegmentSubjectSc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segment_subject_sc")
    cargs.append("-i")
    cargs.append(execution.input_file(params["invol"]))
    cargs.append("-xfm")
    cargs.append(execution.input_file(params["outxfm"]))
    v_0 = params.get("log")
    if v_0 is not None:
        cargs.append("--log")
        cargs.append(v_0)
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def segment_subject_sc_outputs(params: SegmentSubjectSc, execution: Execution) -> SegmentSubjectScOutputs:
    """Build the SegmentSubjectScOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xfm_file_v: OutputPathType = execution.output_file(params["outxfm"])
    return SegmentSubjectScOutputs(
        root=root_v,
        output_xfm_file=output_xfm_file_v,
    )

def segment_subject_sc_execute(params: SegmentSubjectSc, runner: Runner | None = None) -> SegmentSubjectScOutputs:
    """
    segment_subject_sc

    Front-end for MINC's mritotal. Computes the Talairach transform for mapping the input volume to the MNI305 space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_subject_sc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_SUBJECT_SC_METADATA)
    execution.params(params)
    args = segment_subject_sc_cargs(params, execution)
    out = segment_subject_sc_outputs(params, execution)
    execution.run(args)
    return out

def segment_subject_sc(
    invol: InputPathType,
    outxfm: InputPathType,
    log: str | None = None,
    debug: bool = False,
    runner: Runner | None = None,
) -> SegmentSubjectScOutputs:
    """
    segment_subject_sc

    Front-end for MINC's mritotal. Computes the Talairach transform for mapping the input volume to the MNI305 space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        invol: Input volume
        outxfm: Output xfm file
        log: Log file. Default is outdir/talarach.log
        debug: Turn on debugging
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_subject_sc_params(
        invol=invol,
        outxfm=outxfm,
        log=log,
        debug=debug,
    )
    return segment_subject_sc_execute(params, runner)

__all__ = [
    "SegmentSubjectSc",
    "SegmentSubjectScOutputs",
    "SEGMENT_SUBJECT_SC_METADATA",
    "segment_subject_sc_cargs",
    "segment_subject_sc_outputs",
    "segment_subject_sc_params",
    "segment_subject_sc_execute",
    "segment_subject_sc_validate",
    "segment_subject_sc",
]