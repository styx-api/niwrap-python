# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PARSE_SDCMDIR_METADATA = Metadata(
    id="mri_parse_sdcmdir",
    name="mri_parse_sdcmdir",
    package="freesurfer",
)

MriParseSdcmdir = typing.TypedDict(
    "MriParseSdcmdir",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_parse_sdcmdir"]],
        "sdicomdir": str,
        "outfile": typing.NotRequired[str],
        "sortbyrun": typing.NotRequired[bool],
        "summarize": typing.NotRequired[bool],
        "dwi": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriParseSdcmdirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_parse_sdcmdir_params(
    sdicomdir: str,
    outfile: str | None = None,
    sortbyrun: bool = False,
    summarize: bool = False,
    dwi: bool = False,
) -> MriParseSdcmdir:
    """
    Build parameters.

    Args:
        sdicomdir: Path to Siemens DICOM directory
        outfile: Name of a file to which the results will be printed. If\
            unspecified, the results will be printed to stdout.
        sortbyrun: Assign run numbers
        summarize: Only print out info for run leaders
        dwi: Try to read DWI params. Generally no need to.

    Returns:
        Parameter dictionary.
    """
    params: MriParseSdcmdir = {
        "@type": "freesurfer/mri_parse_sdcmdir",
        "sdicomdir": sdicomdir,
        "sortbyrun": sortbyrun,
        "summarize": summarize,
        "dwi": dwi,
    }
    if outfile is not None:
        params["outfile"] = outfile
    return params

def mri_parse_sdcmdir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriParseSdcmdir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sdicomdir", None) is None:
        raise StyxValidationError("`sdicomdir` must not be None")
    if not isinstance(params["sdicomdir"], str):
        raise StyxValidationError(f'`sdicomdir` has the wrong type: Received `{type(params["sdicomdir"])}` expected `str`')
    if params.get("outfile", None) is not None:
        if not isinstance(params["outfile"], str):
            raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("sortbyrun", None) is not None:
        if not isinstance(params["sortbyrun"], bool):
            raise StyxValidationError(f'`sortbyrun` has the wrong type: Received `{type(params["sortbyrun"])}` expected `bool`')
    if params.get("summarize", None) is not None:
        if not isinstance(params["summarize"], bool):
            raise StyxValidationError(f'`summarize` has the wrong type: Received `{type(params["summarize"])}` expected `bool`')
    if params.get("dwi", None) is not None:
        if not isinstance(params["dwi"], bool):
            raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(params["dwi"])}` expected `bool`')

def mri_parse_sdcmdir_cargs(params: MriParseSdcmdir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_parse_sdcmdir")
    cargs.append("--d")
    cargs.append(params["sdicomdir"])
    v_0 = params.get("outfile")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    if params.get("sortbyrun"):
        cargs.append("--sortbyrun")
    if params.get("summarize"):
        cargs.append("--summarize")
    if params.get("dwi"):
        cargs.append("--dwi")
    return cargs

def mri_parse_sdcmdir_outputs(params: MriParseSdcmdir, execution: Execution) -> MriParseSdcmdirOutputs:
    """Build the MriParseSdcmdirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriParseSdcmdirOutputs(
        root=root_v,
    )

def mri_parse_sdcmdir_execute(params: MriParseSdcmdir, runner: Runner | None = None) -> MriParseSdcmdirOutputs:
    """
    mri_parse_sdcmdir

    This program parses the Siemens DICOM files in a given directory and prints out information about each file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_parse_sdcmdir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PARSE_SDCMDIR_METADATA)
    execution.params(params)
    args = mri_parse_sdcmdir_cargs(params, execution)
    out = mri_parse_sdcmdir_outputs(params, execution)
    execution.run(args)
    return out

def mri_parse_sdcmdir(
    sdicomdir: str,
    outfile: str | None = None,
    sortbyrun: bool = False,
    summarize: bool = False,
    dwi: bool = False,
    runner: Runner | None = None,
) -> MriParseSdcmdirOutputs:
    """
    mri_parse_sdcmdir

    This program parses the Siemens DICOM files in a given directory and prints out information about each file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        sdicomdir: Path to Siemens DICOM directory
        outfile: Name of a file to which the results will be printed. If\
            unspecified, the results will be printed to stdout.
        sortbyrun: Assign run numbers
        summarize: Only print out info for run leaders
        dwi: Try to read DWI params. Generally no need to.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_parse_sdcmdir_params(
        sdicomdir=sdicomdir,
        outfile=outfile,
        sortbyrun=sortbyrun,
        summarize=summarize,
        dwi=dwi,
    )
    return mri_parse_sdcmdir_execute(params, runner)

__all__ = [
    "MriParseSdcmdir",
    "MriParseSdcmdirOutputs",
    "MRI_PARSE_SDCMDIR_METADATA",
    "mri_parse_sdcmdir_cargs",
    "mri_parse_sdcmdir_outputs",
    "mri_parse_sdcmdir_params",
    "mri_parse_sdcmdir_execute",
    "mri_parse_sdcmdir_validate",
    "mri_parse_sdcmdir",
]