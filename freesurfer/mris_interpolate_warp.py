# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_INTERPOLATE_WARP_METADATA = Metadata(
    id="mris_interpolate_warp",
    name="mris_interpolate_warp",
    package="freesurfer",
)

MrisInterpolateWarp = typing.TypedDict(
    "MrisInterpolateWarp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_interpolate_warp"]],
        "start_surface": InputPathType,
        "end_surface": InputPathType,
        "warp_field": InputPathType,
    },
)

@dataclasses.dataclass
class MrisInterpolateWarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_interpolate_warp_params(
    start_surface: InputPathType,
    end_surface: InputPathType,
    warp_field: InputPathType,
) -> MrisInterpolateWarp:
    """
    Build parameters.

    Args:
        start_surface: The starting surface file.
        end_surface: The ending surface file.
        warp_field: The warp field file with .m3z extension.

    Returns:
        Parameter dictionary.
    """
    params: MrisInterpolateWarp = {
        "@type": "freesurfer/mris_interpolate_warp",
        "start_surface": start_surface,
        "end_surface": end_surface,
        "warp_field": warp_field,
    }
    return params

def mris_interpolate_warp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisInterpolateWarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("start_surface", None) is None:
        raise StyxValidationError("`start_surface` must not be None")
    if not isinstance(params["start_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`start_surface` has the wrong type: Received `{type(params["start_surface"])}` expected `InputPathType`')
    if params.get("end_surface", None) is None:
        raise StyxValidationError("`end_surface` must not be None")
    if not isinstance(params["end_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`end_surface` has the wrong type: Received `{type(params["end_surface"])}` expected `InputPathType`')
    if params.get("warp_field", None) is None:
        raise StyxValidationError("`warp_field` must not be None")
    if not isinstance(params["warp_field"], (pathlib.Path, str)):
        raise StyxValidationError(f'`warp_field` has the wrong type: Received `{type(params["warp_field"])}` expected `InputPathType`')

def mris_interpolate_warp_cargs(params: MrisInterpolateWarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_interpolate_warp")
    cargs.append(execution.input_file(params["start_surface"]))
    cargs.append(execution.input_file(params["end_surface"]))
    cargs.append(execution.input_file(params["warp_field"]))
    return cargs

def mris_interpolate_warp_outputs(params: MrisInterpolateWarp, execution: Execution) -> MrisInterpolateWarpOutputs:
    """Build the MrisInterpolateWarpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisInterpolateWarpOutputs(
        root=root_v,
    )

def mris_interpolate_warp_execute(params: MrisInterpolateWarp, runner: Runner | None = None) -> MrisInterpolateWarpOutputs:
    """
    mris_interpolate_warp

    Interpolate warp on cortical surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_interpolate_warp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_INTERPOLATE_WARP_METADATA)
    execution.params(params)
    args = mris_interpolate_warp_cargs(params, execution)
    out = mris_interpolate_warp_outputs(params, execution)
    execution.run(args)
    return out

def mris_interpolate_warp(
    start_surface: InputPathType,
    end_surface: InputPathType,
    warp_field: InputPathType,
    runner: Runner | None = None,
) -> MrisInterpolateWarpOutputs:
    """
    mris_interpolate_warp

    Interpolate warp on cortical surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        start_surface: The starting surface file.
        end_surface: The ending surface file.
        warp_field: The warp field file with .m3z extension.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_interpolate_warp_params(
        start_surface=start_surface,
        end_surface=end_surface,
        warp_field=warp_field,
    )
    return mris_interpolate_warp_execute(params, runner)

__all__ = [
    "MrisInterpolateWarp",
    "MrisInterpolateWarpOutputs",
    "MRIS_INTERPOLATE_WARP_METADATA",
    "mris_interpolate_warp_cargs",
    "mris_interpolate_warp_outputs",
    "mris_interpolate_warp_params",
    "mris_interpolate_warp_execute",
    "mris_interpolate_warp_validate",
    "mris_interpolate_warp",
]