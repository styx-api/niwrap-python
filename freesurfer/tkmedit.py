# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TKMEDIT_METADATA = Metadata(
    id="tkmedit",
    name="tkmedit",
    package="freesurfer",
)

Tkmedit = typing.TypedDict(
    "Tkmedit",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tkmedit"]],
        "input_volume": InputPathType,
        "options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class TkmeditOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tkmedit_params(
    input_volume: InputPathType,
    options: str | None = None,
) -> Tkmedit:
    """
    Build parameters.

    Args:
        input_volume: Input volume file (e.g., nu.mgz or brain.mgz)
        options: Options for running tkmedit.

    Returns:
        Parameter dictionary.
    """
    params: Tkmedit = {
        "@type": "freesurfer/tkmedit",
        "input_volume": input_volume,
    }
    if options is not None:
        params["options"] = options
    return params

def tkmedit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tkmedit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("options", None) is not None:
        if not isinstance(params["options"], str):
            raise StyxValidationError(f'`options` has the wrong type: Received `{type(params["options"])}` expected `str`')

def tkmedit_cargs(params: Tkmedit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tkmedit")
    cargs.append(execution.input_file(params["input_volume"]))
    v_0 = params.get("options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def tkmedit_outputs(params: Tkmedit, execution: Execution) -> TkmeditOutputs:
    """Build the TkmeditOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TkmeditOutputs(
        root=root_v,
    )

def tkmedit_execute(params: Tkmedit, runner: Runner | None = None) -> TkmeditOutputs:
    """
    tkmedit

    tkmedit is a multi-functional imaging tool for viewing and editing surface models and volumes in FreeSurfer. It provides a GUI representation of volumetric data and facilitates modifications in the data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tkmedit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TKMEDIT_METADATA)
    execution.params(params)
    args = tkmedit_cargs(params, execution)
    out = tkmedit_outputs(params, execution)
    execution.run(args)
    return out

def tkmedit(
    input_volume: InputPathType,
    options: str | None = None,
    runner: Runner | None = None,
) -> TkmeditOutputs:
    """
    tkmedit

    tkmedit is a multi-functional imaging tool for viewing and editing surface models and volumes in FreeSurfer. It provides a GUI representation of volumetric data and facilitates modifications in the data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file (e.g., nu.mgz or brain.mgz)
        options: Options for running tkmedit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tkmedit_params(
        input_volume=input_volume,
        options=options,
    )
    return tkmedit_execute(params, runner)

__all__ = [
    "Tkmedit",
    "TkmeditOutputs",
    "TKMEDIT_METADATA",
    "tkmedit_cargs",
    "tkmedit_outputs",
    "tkmedit_params",
    "tkmedit_execute",
    "tkmedit_validate",
    "tkmedit",
]