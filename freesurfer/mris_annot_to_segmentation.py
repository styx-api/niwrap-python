# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ANNOT_TO_SEGMENTATION_METADATA = Metadata(
    id="mris_annot_to_segmentation",
    name="mris_annot_to_segmentation",
    package="freesurfer",
)

MrisAnnotToSegmentation = typing.TypedDict(
    "MrisAnnotToSegmentation",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_annot_to_segmentation"]],
        "subject_name": str,
        "hemi": str,
        "surface": str,
        "annot_file": InputPathType,
        "color_table": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MrisAnnotToSegmentationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Generated segmentation volume."""

def mris_annot_to_segmentation_params(
    subject_name: str,
    hemi: str,
    surface: str,
    annot_file: InputPathType,
    color_table: InputPathType,
    output_volume: str,
) -> MrisAnnotToSegmentation:
    """
    Build parameters.

    Args:
        subject_name: Subject name.
        hemi: Hemisphere (e.g., lh or rh).
        surface: Surface file.
        annot_file: Annotation file.
        color_table: Color table file.
        output_volume: Output volume file.

    Returns:
        Parameter dictionary.
    """
    params: MrisAnnotToSegmentation = {
        "@type": "freesurfer/mris_annot_to_segmentation",
        "subject_name": subject_name,
        "hemi": hemi,
        "surface": surface,
        "annot_file": annot_file,
        "color_table": color_table,
        "output_volume": output_volume,
    }
    return params

def mris_annot_to_segmentation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisAnnotToSegmentation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("annot_file", None) is None:
        raise StyxValidationError("`annot_file` must not be None")
    if not isinstance(params["annot_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`annot_file` has the wrong type: Received `{type(params["annot_file"])}` expected `InputPathType`')
    if params.get("color_table", None) is None:
        raise StyxValidationError("`color_table` must not be None")
    if not isinstance(params["color_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`color_table` has the wrong type: Received `{type(params["color_table"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mris_annot_to_segmentation_cargs(params: MrisAnnotToSegmentation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_annot_to_segmentation")
    cargs.append(params["subject_name"])
    cargs.append(params["hemi"])
    cargs.append(params["surface"])
    cargs.append(execution.input_file(params["annot_file"]))
    cargs.append(execution.input_file(params["color_table"]))
    cargs.append(params["output_volume"])
    return cargs

def mris_annot_to_segmentation_outputs(params: MrisAnnotToSegmentation, execution: Execution) -> MrisAnnotToSegmentationOutputs:
    """Build the MrisAnnotToSegmentationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MrisAnnotToSegmentationOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mris_annot_to_segmentation_execute(params: MrisAnnotToSegmentation, runner: Runner | None = None) -> MrisAnnotToSegmentationOutputs:
    """
    mris_annot_to_segmentation

    Converts annotation files to segmentation volumes in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_annot_to_segmentation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ANNOT_TO_SEGMENTATION_METADATA)
    execution.params(params)
    args = mris_annot_to_segmentation_cargs(params, execution)
    out = mris_annot_to_segmentation_outputs(params, execution)
    execution.run(args)
    return out

def mris_annot_to_segmentation(
    subject_name: str,
    hemi: str,
    surface: str,
    annot_file: InputPathType,
    color_table: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MrisAnnotToSegmentationOutputs:
    """
    mris_annot_to_segmentation

    Converts annotation files to segmentation volumes in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name.
        hemi: Hemisphere (e.g., lh or rh).
        surface: Surface file.
        annot_file: Annotation file.
        color_table: Color table file.
        output_volume: Output volume file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_annot_to_segmentation_params(
        subject_name=subject_name,
        hemi=hemi,
        surface=surface,
        annot_file=annot_file,
        color_table=color_table,
        output_volume=output_volume,
    )
    return mris_annot_to_segmentation_execute(params, runner)

__all__ = [
    "MrisAnnotToSegmentation",
    "MrisAnnotToSegmentationOutputs",
    "MRIS_ANNOT_TO_SEGMENTATION_METADATA",
    "mris_annot_to_segmentation_cargs",
    "mris_annot_to_segmentation_outputs",
    "mris_annot_to_segmentation_params",
    "mris_annot_to_segmentation_execute",
    "mris_annot_to_segmentation_validate",
    "mris_annot_to_segmentation",
]