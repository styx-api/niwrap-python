# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_LABEL_CALC_METADATA = Metadata(
    id="mris_label_calc",
    name="mris_label_calc",
    package="freesurfer",
)

MrisLabelCalcCommand = typing.Literal["union"] | typing.Literal["intersect"] | typing.Literal["invert"] | typing.Literal["erode"] | typing.Literal["dilate"]

MrisLabelCalc = typing.TypedDict(
    "MrisLabelCalc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_label_calc"]],
        "command": MrisLabelCalcCommand,
        "input1": InputPathType,
        "input2": InputPathType,
        "output": str,
        "iterations": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class MrisLabelCalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label: OutputPathType
    """The resulting label file after operation"""

def mris_label_calc_params(
    command: MrisLabelCalcCommand,
    input1: InputPathType,
    input2: InputPathType,
    output: str,
    iterations: int | None = None,
) -> MrisLabelCalc:
    """
    Build parameters.

    Args:
        command: Command to perform on input labels
        input1: First input label file
        input2: Second input label file (used for 'invert', 'erode', 'dilate'\
            operations)
        output: Output label file
        iterations: Number of times to erode or dilate label

    Returns:
        Parameter dictionary.
    """
    params: MrisLabelCalc = {
        "@type": "freesurfer/mris_label_calc",
        "command": command,
        "input1": input1,
        "input2": input2,
        "output": output,
    }
    if iterations is not None:
        params["iterations"] = iterations
    return params

def mris_label_calc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisLabelCalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("command", None) is None:
        raise StyxValidationError("`command` must not be None")
    if not isinstance(params["command"], str):
        raise StyxValidationError(f'`command` has the wrong type: Received `{type(params["command"])}` expected `MrisLabelCalcCommand`')
    if params["command"] not in ["union", "intersect", "invert", "erode", "dilate"]:
        raise StyxValidationError("Parameter `command` must be one of [\"union\", \"intersect\", \"invert\", \"erode\", \"dilate\"]")
    if params.get("input1", None) is None:
        raise StyxValidationError("`input1` must not be None")
    if not isinstance(params["input1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input1` has the wrong type: Received `{type(params["input1"])}` expected `InputPathType`')
    if params.get("input2", None) is None:
        raise StyxValidationError("`input2` must not be None")
    if not isinstance(params["input2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input2` has the wrong type: Received `{type(params["input2"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], int):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `int`')

def mris_label_calc_cargs(params: MrisLabelCalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_label_calc")
    cargs.append(str(params["command"]))
    cargs.append(execution.input_file(params["input1"]))
    cargs.append(execution.input_file(params["input2"]))
    cargs.append(params["output"])
    v_0 = params.get("iterations")
    if v_0 is not None:
        cargs.append("<n>")
        cargs.append(str(v_0))
    return cargs

def mris_label_calc_outputs(params: MrisLabelCalc, execution: Execution) -> MrisLabelCalcOutputs:
    """Build the MrisLabelCalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_v: OutputPathType = execution.output_file(f'{params["output"]}.label')
    return MrisLabelCalcOutputs(
        root=root_v,
        output_label=output_label_v,
    )

def mris_label_calc_execute(params: MrisLabelCalc, runner: Runner | None = None) -> MrisLabelCalcOutputs:
    """
    mris_label_calc

    Tool for surface label calculations

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_label_calc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_LABEL_CALC_METADATA)
    execution.params(params)
    args = mris_label_calc_cargs(params, execution)
    out = mris_label_calc_outputs(params, execution)
    execution.run(args)
    return out

def mris_label_calc(
    command: MrisLabelCalcCommand,
    input1: InputPathType,
    input2: InputPathType,
    output: str,
    iterations: int | None = None,
    runner: Runner | None = None,
) -> MrisLabelCalcOutputs:
    """
    mris_label_calc

    Tool for surface label calculations

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        command: Command to perform on input labels
        input1: First input label file
        input2: Second input label file (used for 'invert', 'erode', 'dilate'\
            operations)
        output: Output label file
        iterations: Number of times to erode or dilate label
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_label_calc_params(
        command=command,
        input1=input1,
        input2=input2,
        output=output,
        iterations=iterations,
    )
    return mris_label_calc_execute(params, runner)

__all__ = [
    "MrisLabelCalc",
    "MrisLabelCalcOutputs",
    "MRIS_LABEL_CALC_METADATA",
    "mris_label_calc_cargs",
    "mris_label_calc_outputs",
    "mris_label_calc_params",
    "mris_label_calc_execute",
    "mris_label_calc_validate",
    "mris_label_calc",
]