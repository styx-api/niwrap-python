# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XCORR_METADATA = Metadata(
    id="xcorr",
    name="xcorr",
    package="freesurfer",
)

Xcorr = typing.TypedDict(
    "Xcorr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xcorr"]],
        "input1": InputPathType,
        "input2": InputPathType,
        "output": str,
        "log_file": typing.NotRequired[str],
        "tmp_dir": typing.NotRequired[str],
        "no_cleanup": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class XcorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_xcorrfile: OutputPathType
    """Output xcorr file"""
    log_output: OutputPathType | None
    """Log of xcorr execution"""

def xcorr_params(
    input1: InputPathType,
    input2: InputPathType,
    output: str,
    log_file: str | None = None,
    tmp_dir: str | None = None,
    no_cleanup: bool = False,
) -> Xcorr:
    """
    Build parameters.

    Args:
        input1: First input volume file
        input2: Second input volume file
        output: Output xcorr file
        log_file: Log file
        tmp_dir: Temporary directory
        no_cleanup: Prevent cleanup of temporary files

    Returns:
        Parameter dictionary.
    """
    params: Xcorr = {
        "@type": "freesurfer/xcorr",
        "input1": input1,
        "input2": input2,
        "output": output,
        "no_cleanup": no_cleanup,
    }
    if log_file is not None:
        params["log_file"] = log_file
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def xcorr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Xcorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1", None) is None:
        raise StyxValidationError("`input1` must not be None")
    if not isinstance(params["input1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input1` has the wrong type: Received `{type(params["input1"])}` expected `InputPathType`')
    if params.get("input2", None) is None:
        raise StyxValidationError("`input2` must not be None")
    if not isinstance(params["input2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input2` has the wrong type: Received `{type(params["input2"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')

def xcorr_cargs(params: Xcorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xcorr")
    cargs.append("--i1")
    cargs.append(execution.input_file(params["input1"]))
    cargs.append("--i2")
    cargs.append(execution.input_file(params["input2"]))
    cargs.append("--o")
    cargs.append(params["output"])
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("--log")
        cargs.append(v_0)
    v_1 = params.get("tmp_dir")
    if v_1 is not None:
        cargs.append("--tmp")
        cargs.append(v_1)
    if params.get("no_cleanup"):
        cargs.append("--no-cleanup")
    return cargs

def xcorr_outputs(params: Xcorr, execution: Execution) -> XcorrOutputs:
    """Build the XcorrOutputs object for this tool."""
    log_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_xcorrfile_v: OutputPathType = execution.output_file(params["output"])
    __v0 = params.get("log_file")
    if __v0 is not None:
        log_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return XcorrOutputs(
        root=root_v,
        out_xcorrfile=out_xcorrfile_v,
        log_output=log_output_v,
    )

def xcorr_execute(params: Xcorr, runner: Runner | None = None) -> XcorrOutputs:
    """
    xcorr

    Computes the voxel-for-voxel correlation coefficient between two volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xcorr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XCORR_METADATA)
    execution.params(params)
    args = xcorr_cargs(params, execution)
    out = xcorr_outputs(params, execution)
    execution.run(args)
    return out

def xcorr(
    input1: InputPathType,
    input2: InputPathType,
    output: str,
    log_file: str | None = None,
    tmp_dir: str | None = None,
    no_cleanup: bool = False,
    runner: Runner | None = None,
) -> XcorrOutputs:
    """
    xcorr

    Computes the voxel-for-voxel correlation coefficient between two volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input1: First input volume file
        input2: Second input volume file
        output: Output xcorr file
        log_file: Log file
        tmp_dir: Temporary directory
        no_cleanup: Prevent cleanup of temporary files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xcorr_params(
        input1=input1,
        input2=input2,
        output=output,
        log_file=log_file,
        tmp_dir=tmp_dir,
        no_cleanup=no_cleanup,
    )
    return xcorr_execute(params, runner)

__all__ = [
    "Xcorr",
    "XcorrOutputs",
    "XCORR_METADATA",
    "xcorr_cargs",
    "xcorr_outputs",
    "xcorr_params",
    "xcorr_execute",
    "xcorr_validate",
    "xcorr",
]