# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIX_SUBJECT_CORRECTED_METADATA = Metadata(
    id="fix_subject_corrected",
    name="fix_subject_corrected",
    package="freesurfer",
)

FixSubjectCorrected = typing.TypedDict(
    "FixSubjectCorrected",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fix_subject_corrected"]],
        "subject_directory": str,
        "output_directory": str,
    },
)

@dataclasses.dataclass
class FixSubjectCorrectedOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fix_subject_corrected_params(
    subject_directory: str,
    output_directory: str,
) -> FixSubjectCorrected:
    """
    Build parameters.

    Args:
        subject_directory: Path to the subject's directory
        output_directory: Path to the output directory

    Returns:
        Parameter dictionary.
    """
    params: FixSubjectCorrected = {
        "@type": "freesurfer/fix_subject_corrected",
        "subject_directory": subject_directory,
        "output_directory": output_directory,
    }
    return params

def fix_subject_corrected_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixSubjectCorrected."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_directory", None) is None:
        raise StyxValidationError("`subject_directory` must not be None")
    if not isinstance(params["subject_directory"], str):
        raise StyxValidationError(f'`subject_directory` has the wrong type: Received `{type(params["subject_directory"])}` expected `str`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def fix_subject_corrected_cargs(params: FixSubjectCorrected, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fix_subject_corrected")
    cargs.append(params["subject_directory"])
    cargs.append(params["output_directory"])
    return cargs

def fix_subject_corrected_outputs(params: FixSubjectCorrected, execution: Execution) -> FixSubjectCorrectedOutputs:
    """Build the FixSubjectCorrectedOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixSubjectCorrectedOutputs(
        root=root_v,
    )

def fix_subject_corrected_execute(params: FixSubjectCorrected, runner: Runner | None = None) -> FixSubjectCorrectedOutputs:
    """
    fix_subject_corrected

    Corrects subject data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fix_subject_corrected_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIX_SUBJECT_CORRECTED_METADATA)
    execution.params(params)
    args = fix_subject_corrected_cargs(params, execution)
    out = fix_subject_corrected_outputs(params, execution)
    execution.run(args)
    return out

def fix_subject_corrected(
    subject_directory: str,
    output_directory: str,
    runner: Runner | None = None,
) -> FixSubjectCorrectedOutputs:
    """
    fix_subject_corrected

    Corrects subject data in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_directory: Path to the subject's directory
        output_directory: Path to the output directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fix_subject_corrected_params(
        subject_directory=subject_directory,
        output_directory=output_directory,
    )
    return fix_subject_corrected_execute(params, runner)

__all__ = [
    "FixSubjectCorrected",
    "FixSubjectCorrectedOutputs",
    "FIX_SUBJECT_CORRECTED_METADATA",
    "fix_subject_corrected_cargs",
    "fix_subject_corrected_outputs",
    "fix_subject_corrected_params",
    "fix_subject_corrected_execute",
    "fix_subject_corrected_validate",
    "fix_subject_corrected",
]