# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIDUCIALS_CALIBRATION_METADATA = Metadata(
    id="fiducials_calibration",
    name="fiducials_calibration",
    package="freesurfer",
)

FiducialsCalibration = typing.TypedDict(
    "FiducialsCalibration",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fiducials_calibration"]],
        "qt_plugin_installation": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FiducialsCalibrationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fiducials_calibration_params(
    qt_plugin_installation: str | None = None,
) -> FiducialsCalibration:
    """
    Build parameters.

    Args:
        qt_plugin_installation: This application requires a functional Qt\
            installation. If it fails to start, reinstalling the application might\
            fix the problem.

    Returns:
        Parameter dictionary.
    """
    params: FiducialsCalibration = {
        "@type": "freesurfer/fiducials_calibration",
    }
    if qt_plugin_installation is not None:
        params["qt_plugin_installation"] = qt_plugin_installation
    return params

def fiducials_calibration_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FiducialsCalibration."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qt_plugin_installation", None) is not None:
        if not isinstance(params["qt_plugin_installation"], str):
            raise StyxValidationError(f'`qt_plugin_installation` has the wrong type: Received `{type(params["qt_plugin_installation"])}` expected `str`')

def fiducials_calibration_cargs(params: FiducialsCalibration, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fiducials_calibration")
    v_0 = params.get("qt_plugin_installation")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fiducials_calibration_outputs(params: FiducialsCalibration, execution: Execution) -> FiducialsCalibrationOutputs:
    """Build the FiducialsCalibrationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FiducialsCalibrationOutputs(
        root=root_v,
    )

def fiducials_calibration_execute(params: FiducialsCalibration, runner: Runner | None = None) -> FiducialsCalibrationOutputs:
    """
    fiducials_calibration

    A tool used for calibrating fiducials.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fiducials_calibration_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIDUCIALS_CALIBRATION_METADATA)
    execution.params(params)
    args = fiducials_calibration_cargs(params, execution)
    out = fiducials_calibration_outputs(params, execution)
    execution.run(args)
    return out

def fiducials_calibration(
    qt_plugin_installation: str | None = None,
    runner: Runner | None = None,
) -> FiducialsCalibrationOutputs:
    """
    fiducials_calibration

    A tool used for calibrating fiducials.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qt_plugin_installation: This application requires a functional Qt\
            installation. If it fails to start, reinstalling the application might\
            fix the problem.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fiducials_calibration_params(
        qt_plugin_installation=qt_plugin_installation,
    )
    return fiducials_calibration_execute(params, runner)

__all__ = [
    "FiducialsCalibration",
    "FiducialsCalibrationOutputs",
    "FIDUCIALS_CALIBRATION_METADATA",
    "fiducials_calibration_cargs",
    "fiducials_calibration_outputs",
    "fiducials_calibration_params",
    "fiducials_calibration_execute",
    "fiducials_calibration_validate",
    "fiducials_calibration",
]