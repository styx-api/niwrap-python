# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GCAPREPONE_METADATA = Metadata(
    id="gcaprepone",
    name="gcaprepone",
    package="freesurfer",
)

Gcaprepone = typing.TypedDict(
    "Gcaprepone",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/gcaprepone"]],
        "gcadir": str,
        "subject": str,
        "init_subject": typing.NotRequired[bool],
        "source_subjects_dir": str,
        "done_file": str,
        "no_emreg": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class GcapreponeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def gcaprepone_params(
    gcadir: str,
    subject: str,
    source_subjects_dir: str,
    done_file: str,
    init_subject: bool = False,
    no_emreg: bool = False,
) -> Gcaprepone:
    """
    Build parameters.

    Args:
        gcadir: Directory to be the new SUBJECTS_DIR
        subject: Subject for the process
        source_subjects_dir: SUBJECTS_DIR for source data
        done_file: File to indicate completion
        init_subject: Flag indicating the initial subject
        no_emreg: Flag to skip EM registration

    Returns:
        Parameter dictionary.
    """
    params: Gcaprepone = {
        "@type": "freesurfer/gcaprepone",
        "gcadir": gcadir,
        "subject": subject,
        "source_subjects_dir": source_subjects_dir,
        "done_file": done_file,
        "init_subject": init_subject,
        "no_emreg": no_emreg,
    }
    return params

def gcaprepone_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gcaprepone."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gcadir", None) is None:
        raise StyxValidationError("`gcadir` must not be None")
    if not isinstance(params["gcadir"], str):
        raise StyxValidationError(f'`gcadir` has the wrong type: Received `{type(params["gcadir"])}` expected `str`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("init_subject", None) is not None:
        if not isinstance(params["init_subject"], bool):
            raise StyxValidationError(f'`init_subject` has the wrong type: Received `{type(params["init_subject"])}` expected `bool`')
    if params.get("source_subjects_dir", None) is None:
        raise StyxValidationError("`source_subjects_dir` must not be None")
    if not isinstance(params["source_subjects_dir"], str):
        raise StyxValidationError(f'`source_subjects_dir` has the wrong type: Received `{type(params["source_subjects_dir"])}` expected `str`')
    if params.get("done_file", None) is None:
        raise StyxValidationError("`done_file` must not be None")
    if not isinstance(params["done_file"], str):
        raise StyxValidationError(f'`done_file` has the wrong type: Received `{type(params["done_file"])}` expected `str`')
    if params.get("no_emreg", None) is not None:
        if not isinstance(params["no_emreg"], bool):
            raise StyxValidationError(f'`no_emreg` has the wrong type: Received `{type(params["no_emreg"])}` expected `bool`')

def gcaprepone_cargs(params: Gcaprepone, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gcaprepone")
    cargs.append("--o")
    cargs.append(params["gcadir"])
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("init_subject"):
        cargs.append("--init-subject")
    cargs.append("--sd")
    cargs.append(params["source_subjects_dir"])
    cargs.append("--done")
    cargs.append(params["done_file"])
    if params.get("no_emreg"):
        cargs.append("--no-emreg")
    return cargs

def gcaprepone_outputs(params: Gcaprepone, execution: Execution) -> GcapreponeOutputs:
    """Build the GcapreponeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GcapreponeOutputs(
        root=root_v,
    )

def gcaprepone_execute(params: Gcaprepone, runner: Runner | None = None) -> GcapreponeOutputs:
    """
    gcaprepone

    Tool for preparing FreeSurfer subjects for use with group-wise template generation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gcaprepone_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GCAPREPONE_METADATA)
    execution.params(params)
    args = gcaprepone_cargs(params, execution)
    out = gcaprepone_outputs(params, execution)
    execution.run(args)
    return out

def gcaprepone(
    gcadir: str,
    subject: str,
    source_subjects_dir: str,
    done_file: str,
    init_subject: bool = False,
    no_emreg: bool = False,
    runner: Runner | None = None,
) -> GcapreponeOutputs:
    """
    gcaprepone

    Tool for preparing FreeSurfer subjects for use with group-wise template generation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        gcadir: Directory to be the new SUBJECTS_DIR
        subject: Subject for the process
        source_subjects_dir: SUBJECTS_DIR for source data
        done_file: File to indicate completion
        init_subject: Flag indicating the initial subject
        no_emreg: Flag to skip EM registration
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gcaprepone_params(
        gcadir=gcadir,
        subject=subject,
        source_subjects_dir=source_subjects_dir,
        done_file=done_file,
        init_subject=init_subject,
        no_emreg=no_emreg,
    )
    return gcaprepone_execute(params, runner)

__all__ = [
    "Gcaprepone",
    "GcapreponeOutputs",
    "GCAPREPONE_METADATA",
    "gcaprepone_cargs",
    "gcaprepone_outputs",
    "gcaprepone_params",
    "gcaprepone_execute",
    "gcaprepone_validate",
    "gcaprepone",
]