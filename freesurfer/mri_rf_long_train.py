# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RF_LONG_TRAIN_METADATA = Metadata(
    id="mri_rf_long_train",
    name="mri_rf_long_train",
    package="freesurfer",
)

MriRfLongTrain = typing.TypedDict(
    "MriRfLongTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_rf_long_train"]],
        "seg_dir": str,
        "xform": str,
        "mask": typing.NotRequired[str],
        "node_spacing": typing.NotRequired[float],
        "prior_spacing": typing.NotRequired[float],
        "input_data": typing.NotRequired[list[str]],
        "check": typing.NotRequired[bool],
        "subjects": list[str],
        "output_rfa": str,
    },
)

@dataclasses.dataclass
class MriRfLongTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_rfa_file: OutputPathType
    """The output RFA file generated by the tool"""

def mri_rf_long_train_params(
    seg_dir: str,
    xform: str,
    subjects: list[str],
    output_rfa: str,
    mask: str | None = None,
    node_spacing: float | None = None,
    prior_spacing: float | None = None,
    input_data: list[str] | None = None,
    check: bool = False,
) -> MriRfLongTrain:
    """
    Build parameters.

    Args:
        seg_dir: Path to the segmentation volume directory, relative to\
            $subject/mri.
        xform: Atlas transform path relative to $subject/mri/transforms.
        subjects: List of subjects for training.
        output_rfa: Output RFA filename.
        mask: Volume name to use as a mask, path relative to $subject/mri.
        node_spacing: Spacing of classifiers in canonical space.
        prior_spacing: Spacing of class priors in canonical space.
        input_data: Specify training data, path relative to $subject/mri. Can\
            specify multiple inputs. Defaults to 'orig' if not specified.
        check: Conduct sanity-check of labels for obvious edit errors.

    Returns:
        Parameter dictionary.
    """
    params: MriRfLongTrain = {
        "@type": "freesurfer/mri_rf_long_train",
        "seg_dir": seg_dir,
        "xform": xform,
        "subjects": subjects,
        "output_rfa": output_rfa,
        "check": check,
    }
    if mask is not None:
        params["mask"] = mask
    if node_spacing is not None:
        params["node_spacing"] = node_spacing
    if prior_spacing is not None:
        params["prior_spacing"] = prior_spacing
    if input_data is not None:
        params["input_data"] = input_data
    return params

def mri_rf_long_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRfLongTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("seg_dir", None) is None:
        raise StyxValidationError("`seg_dir` must not be None")
    if not isinstance(params["seg_dir"], str):
        raise StyxValidationError(f'`seg_dir` has the wrong type: Received `{type(params["seg_dir"])}` expected `str`')
    if params.get("xform", None) is None:
        raise StyxValidationError("`xform` must not be None")
    if not isinstance(params["xform"], str):
        raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], str):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `str`')
    if params.get("node_spacing", None) is not None:
        if not isinstance(params["node_spacing"], (float, int)):
            raise StyxValidationError(f'`node_spacing` has the wrong type: Received `{type(params["node_spacing"])}` expected `float`')
    if params.get("prior_spacing", None) is not None:
        if not isinstance(params["prior_spacing"], (float, int)):
            raise StyxValidationError(f'`prior_spacing` has the wrong type: Received `{type(params["prior_spacing"])}` expected `float`')
    if params.get("input_data", None) is not None:
        if not isinstance(params["input_data"], list):
            raise StyxValidationError(f'`input_data` has the wrong type: Received `{type(params["input_data"])}` expected `list[str]`')
        for e in params["input_data"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`input_data` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("check", None) is not None:
        if not isinstance(params["check"], bool):
            raise StyxValidationError(f'`check` has the wrong type: Received `{type(params["check"])}` expected `bool`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e_2 in params["subjects"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("output_rfa", None) is None:
        raise StyxValidationError("`output_rfa` must not be None")
    if not isinstance(params["output_rfa"], str):
        raise StyxValidationError(f'`output_rfa` has the wrong type: Received `{type(params["output_rfa"])}` expected `str`')

def mri_rf_long_train_cargs(params: MriRfLongTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_rf_long_train")
    cargs.append("-seg")
    cargs.append(params["seg_dir"])
    cargs.append("-xform")
    cargs.append(params["xform"])
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(v_0)
    v_1 = params.get("node_spacing")
    if v_1 is not None:
        cargs.append("-node_spacing")
        cargs.append(str(v_1))
    v_2 = params.get("prior_spacing")
    if v_2 is not None:
        cargs.append("-prior_spacing")
        cargs.append(str(v_2))
    v_3 = params.get("input_data")
    if v_3 is not None:
        cargs.append("-input")
        for item0 in v_3:
            cargs.append(item0)
    if params.get("check"):
        cargs.append("-check")
    for item1 in params["subjects"]:
        cargs.append(item1)
    cargs.append(params["output_rfa"])
    return cargs

def mri_rf_long_train_outputs(params: MriRfLongTrain, execution: Execution) -> MriRfLongTrainOutputs:
    """Build the MriRfLongTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_rfa_file_v: OutputPathType = execution.output_file(f'{params["output_rfa"]}.rfa')
    return MriRfLongTrainOutputs(
        root=root_v,
        output_rfa_file=output_rfa_file_v,
    )

def mri_rf_long_train_execute(params: MriRfLongTrain, runner: Runner | None = None) -> MriRfLongTrainOutputs:
    """
    mri_rf_long_train

    Trains GCA data with multiple subjects for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_rf_long_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RF_LONG_TRAIN_METADATA)
    execution.params(params)
    args = mri_rf_long_train_cargs(params, execution)
    out = mri_rf_long_train_outputs(params, execution)
    execution.run(args)
    return out

def mri_rf_long_train(
    seg_dir: str,
    xform: str,
    subjects: list[str],
    output_rfa: str,
    mask: str | None = None,
    node_spacing: float | None = None,
    prior_spacing: float | None = None,
    input_data: list[str] | None = None,
    check: bool = False,
    runner: Runner | None = None,
) -> MriRfLongTrainOutputs:
    """
    mri_rf_long_train

    Trains GCA data with multiple subjects for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        seg_dir: Path to the segmentation volume directory, relative to\
            $subject/mri.
        xform: Atlas transform path relative to $subject/mri/transforms.
        subjects: List of subjects for training.
        output_rfa: Output RFA filename.
        mask: Volume name to use as a mask, path relative to $subject/mri.
        node_spacing: Spacing of classifiers in canonical space.
        prior_spacing: Spacing of class priors in canonical space.
        input_data: Specify training data, path relative to $subject/mri. Can\
            specify multiple inputs. Defaults to 'orig' if not specified.
        check: Conduct sanity-check of labels for obvious edit errors.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_rf_long_train_params(
        seg_dir=seg_dir,
        xform=xform,
        subjects=subjects,
        output_rfa=output_rfa,
        mask=mask,
        node_spacing=node_spacing,
        prior_spacing=prior_spacing,
        input_data=input_data,
        check=check,
    )
    return mri_rf_long_train_execute(params, runner)

__all__ = [
    "MriRfLongTrain",
    "MriRfLongTrainOutputs",
    "MRI_RF_LONG_TRAIN_METADATA",
    "mri_rf_long_train_cargs",
    "mri_rf_long_train_outputs",
    "mri_rf_long_train_params",
    "mri_rf_long_train_execute",
    "mri_rf_long_train_validate",
    "mri_rf_long_train",
]