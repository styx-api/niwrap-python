# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCA_CONFIG_METADATA = Metadata(
    id="rca-config",
    name="rca-config",
    package="freesurfer",
)

RcaConfig = typing.TypedDict(
    "RcaConfig",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rca-config"]],
        "source_config": InputPathType,
        "updated_config": InputPathType,
        "unknown_args_file": InputPathType,
        "args": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class RcaConfigOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rca_config_params(
    source_config: InputPathType,
    updated_config: InputPathType,
    unknown_args_file: InputPathType,
    args: list[str] | None = None,
) -> RcaConfig:
    """
    Build parameters.

    Args:
        source_config: Path to the source configuration file
        updated_config: Path to the updated configuration file
        unknown_args_file: Path to the file where unknown arguments are recorded
        args: Additional arguments to be processed

    Returns:
        Parameter dictionary.
    """
    params: RcaConfig = {
        "@type": "freesurfer/rca-config",
        "source_config": source_config,
        "updated_config": updated_config,
        "unknown_args_file": unknown_args_file,
    }
    if args is not None:
        params["args"] = args
    return params

def rca_config_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcaConfig."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_config", None) is None:
        raise StyxValidationError("`source_config` must not be None")
    if not isinstance(params["source_config"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_config` has the wrong type: Received `{type(params["source_config"])}` expected `InputPathType`')
    if params.get("updated_config", None) is None:
        raise StyxValidationError("`updated_config` must not be None")
    if not isinstance(params["updated_config"], (pathlib.Path, str)):
        raise StyxValidationError(f'`updated_config` has the wrong type: Received `{type(params["updated_config"])}` expected `InputPathType`')
    if params.get("unknown_args_file", None) is None:
        raise StyxValidationError("`unknown_args_file` must not be None")
    if not isinstance(params["unknown_args_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`unknown_args_file` has the wrong type: Received `{type(params["unknown_args_file"])}` expected `InputPathType`')
    if params.get("args", None) is not None:
        if not isinstance(params["args"], list):
            raise StyxValidationError(f'`args` has the wrong type: Received `{type(params["args"])}` expected `list[str]`')
        for e in params["args"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`args` has the wrong type: Received `{type(e)}` expected `list[str]`')

def rca_config_cargs(params: RcaConfig, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rca-config")
    cargs.append("-config")
    cargs.append(execution.input_file(params["source_config"]))
    cargs.append(execution.input_file(params["updated_config"]))
    cargs.append(execution.input_file(params["unknown_args_file"]))
    v_0 = params.get("args")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    return cargs

def rca_config_outputs(params: RcaConfig, execution: Execution) -> RcaConfigOutputs:
    """Build the RcaConfigOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RcaConfigOutputs(
        root=root_v,
    )

def rca_config_execute(params: RcaConfig, runner: Runner | None = None) -> RcaConfigOutputs:
    """
    rca-config

    A command-line tool that processes configuration files and arguments.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rca_config_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCA_CONFIG_METADATA)
    execution.params(params)
    args = rca_config_cargs(params, execution)
    out = rca_config_outputs(params, execution)
    execution.run(args)
    return out

def rca_config(
    source_config: InputPathType,
    updated_config: InputPathType,
    unknown_args_file: InputPathType,
    args: list[str] | None = None,
    runner: Runner | None = None,
) -> RcaConfigOutputs:
    """
    rca-config

    A command-line tool that processes configuration files and arguments.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_config: Path to the source configuration file
        updated_config: Path to the updated configuration file
        unknown_args_file: Path to the file where unknown arguments are recorded
        args: Additional arguments to be processed
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rca_config_params(
        source_config=source_config,
        updated_config=updated_config,
        unknown_args_file=unknown_args_file,
        args=args,
    )
    return rca_config_execute(params, runner)

__all__ = [
    "RcaConfig",
    "RcaConfigOutputs",
    "RCA_CONFIG_METADATA",
    "rca_config_cargs",
    "rca_config_outputs",
    "rca_config_params",
    "rca_config_execute",
    "rca_config_validate",
    "rca_config",
]