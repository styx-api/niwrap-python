# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INFLATE_SUBJECT_NEW_METADATA = Metadata(
    id="inflate_subject_new",
    name="inflate_subject_new",
    package="freesurfer",
)

InflateSubjectNew = typing.TypedDict(
    "InflateSubjectNew",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/inflate_subject_new"]],
        "subject_dir": str,
    },
)

@dataclasses.dataclass
class InflateSubjectNewOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def inflate_subject_new_params(
    subject_dir: str,
) -> InflateSubjectNew:
    """
    Build parameters.

    Args:
        subject_dir: Directory containing the subject data to be inflated.

    Returns:
        Parameter dictionary.
    """
    params: InflateSubjectNew = {
        "@type": "freesurfer/inflate_subject_new",
        "subject_dir": subject_dir,
    }
    return params

def inflate_subject_new_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid InflateSubjectNew."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')

def inflate_subject_new_cargs(params: InflateSubjectNew, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("inflate_subject_new")
    cargs.append(params["subject_dir"])
    return cargs

def inflate_subject_new_outputs(params: InflateSubjectNew, execution: Execution) -> InflateSubjectNewOutputs:
    """Build the InflateSubjectNewOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return InflateSubjectNewOutputs(
        root=root_v,
    )

def inflate_subject_new_execute(params: InflateSubjectNew, runner: Runner | None = None) -> InflateSubjectNewOutputs:
    """
    inflate_subject_new

    Tool to inflate subject surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    inflate_subject_new_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INFLATE_SUBJECT_NEW_METADATA)
    execution.params(params)
    args = inflate_subject_new_cargs(params, execution)
    out = inflate_subject_new_outputs(params, execution)
    execution.run(args)
    return out

def inflate_subject_new(
    subject_dir: str,
    runner: Runner | None = None,
) -> InflateSubjectNewOutputs:
    """
    inflate_subject_new

    Tool to inflate subject surfaces

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Directory containing the subject data to be inflated.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = inflate_subject_new_params(
        subject_dir=subject_dir,
    )
    return inflate_subject_new_execute(params, runner)

__all__ = [
    "InflateSubjectNew",
    "InflateSubjectNewOutputs",
    "INFLATE_SUBJECT_NEW_METADATA",
    "inflate_subject_new_cargs",
    "inflate_subject_new_outputs",
    "inflate_subject_new_params",
    "inflate_subject_new_execute",
    "inflate_subject_new_validate",
    "inflate_subject_new",
]