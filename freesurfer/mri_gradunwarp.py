# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GRADUNWARP_METADATA = Metadata(
    id="mri_gradunwarp",
    name="mri_gradunwarp",
    package="freesurfer",
)

MriGradunwarp = typing.TypedDict(
    "MriGradunwarp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gradunwarp"]],
        "gradient_coeff": typing.NotRequired[InputPathType],
        "load_transtbl": typing.NotRequired[InputPathType],
        "input_file": InputPathType,
        "output_file": typing.NotRequired[str],
        "out_transtbl": typing.NotRequired[str],
        "save_transtbl_only": typing.NotRequired[bool],
        "interpolation_type": typing.NotRequired[str],
        "nthreads": typing.NotRequired[float],
        "checkopts": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriGradunwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    unwarped_output: OutputPathType | None
    """Unwarped output volume or surface"""
    output_transform_table: OutputPathType | None
    """Saved unwarp transform table in m3z format"""

def mri_gradunwarp_params(
    input_file: InputPathType,
    gradient_coeff: InputPathType | None = None,
    load_transtbl: InputPathType | None = None,
    output_file: str | None = None,
    out_transtbl: str | None = None,
    save_transtbl_only: bool = False,
    interpolation_type: str | None = None,
    nthreads: float | None = None,
    checkopts: bool = False,
    version: bool = False,
    help: bool = False,
) -> MriGradunwarp:
    """
    Build parameters.

    Args:
        input_file: Input warped volume or surface
        gradient_coeff: Gradient coefficient input file (not to be used with\
            --load_transtbl)
        load_transtbl: Load unwarp transform table in m3z format (not to be used\
            with --gradcoeff)
        output_file: Output unwarped volume or surface
        out_transtbl: Save unwarp transform table in m3z format
        save_transtbl_only: Just save unwarp transform table in m3z format,\
            requires --gradcoeff
        interpolation_type: Interpolation method: nearest | trilinear | cubic\
            (default is trilinear)
        nthreads: Number of threads to run
        checkopts: Don't run anything, just check options and exit
        version: Print out version and exit
        help: Print out information on how to use this program

    Returns:
        Parameter dictionary.
    """
    params: MriGradunwarp = {
        "@type": "freesurfer/mri_gradunwarp",
        "input_file": input_file,
        "save_transtbl_only": save_transtbl_only,
        "checkopts": checkopts,
        "version": version,
        "help": help,
    }
    if gradient_coeff is not None:
        params["gradient_coeff"] = gradient_coeff
    if load_transtbl is not None:
        params["load_transtbl"] = load_transtbl
    if output_file is not None:
        params["output_file"] = output_file
    if out_transtbl is not None:
        params["out_transtbl"] = out_transtbl
    if interpolation_type is not None:
        params["interpolation_type"] = interpolation_type
    if nthreads is not None:
        params["nthreads"] = nthreads
    return params

def mri_gradunwarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGradunwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gradient_coeff", None) is not None:
        if not isinstance(params["gradient_coeff"], (pathlib.Path, str)):
            raise StyxValidationError(f'`gradient_coeff` has the wrong type: Received `{type(params["gradient_coeff"])}` expected `InputPathType`')
    if params.get("load_transtbl", None) is not None:
        if not isinstance(params["load_transtbl"], (pathlib.Path, str)):
            raise StyxValidationError(f'`load_transtbl` has the wrong type: Received `{type(params["load_transtbl"])}` expected `InputPathType`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("out_transtbl", None) is not None:
        if not isinstance(params["out_transtbl"], str):
            raise StyxValidationError(f'`out_transtbl` has the wrong type: Received `{type(params["out_transtbl"])}` expected `str`')
    if params.get("save_transtbl_only", None) is not None:
        if not isinstance(params["save_transtbl_only"], bool):
            raise StyxValidationError(f'`save_transtbl_only` has the wrong type: Received `{type(params["save_transtbl_only"])}` expected `bool`')
    if params.get("interpolation_type", None) is not None:
        if not isinstance(params["interpolation_type"], str):
            raise StyxValidationError(f'`interpolation_type` has the wrong type: Received `{type(params["interpolation_type"])}` expected `str`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], (float, int)):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `float`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_gradunwarp_cargs(params: MriGradunwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gradunwarp")
    v_0 = params.get("gradient_coeff")
    if v_0 is not None:
        cargs.append("--gradcoeff")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("load_transtbl")
    if v_1 is not None:
        cargs.append("--load_transtbl")
        cargs.append(execution.input_file(v_1))
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_file"]))
    v_2 = params.get("output_file")
    if v_2 is not None:
        cargs.append("--o")
        cargs.append(v_2)
    v_3 = params.get("out_transtbl")
    if v_3 is not None:
        cargs.append("--out_transtbl")
        cargs.append(v_3)
    if params.get("save_transtbl_only"):
        cargs.append("--save_transtbl_only")
    v_4 = params.get("interpolation_type")
    if v_4 is not None:
        cargs.append("--interp")
        cargs.append(v_4)
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("--nthreads")
        cargs.append(str(v_5))
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_gradunwarp_outputs(params: MriGradunwarp, execution: Execution) -> MriGradunwarpOutputs:
    """Build the MriGradunwarpOutputs object for this tool."""
    unwarped_output_v: OutputPathType | None = None
    output_transform_table_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        unwarped_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("out_transtbl")
    if __v1 is not None:
        output_transform_table_v = execution.output_file((__v1 if __v1 is not None else ""))
    return MriGradunwarpOutputs(
        root=root_v,
        unwarped_output=unwarped_output_v,
        output_transform_table=output_transform_table_v,
    )

def mri_gradunwarp_execute(params: MriGradunwarp, runner: Runner | None = None) -> MriGradunwarpOutputs:
    """
    mri_gradunwarp

    Tool to correct gradient non-linearity distortions in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gradunwarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GRADUNWARP_METADATA)
    execution.params(params)
    args = mri_gradunwarp_cargs(params, execution)
    out = mri_gradunwarp_outputs(params, execution)
    execution.run(args)
    return out

def mri_gradunwarp(
    input_file: InputPathType,
    gradient_coeff: InputPathType | None = None,
    load_transtbl: InputPathType | None = None,
    output_file: str | None = None,
    out_transtbl: str | None = None,
    save_transtbl_only: bool = False,
    interpolation_type: str | None = None,
    nthreads: float | None = None,
    checkopts: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MriGradunwarpOutputs:
    """
    mri_gradunwarp

    Tool to correct gradient non-linearity distortions in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input warped volume or surface
        gradient_coeff: Gradient coefficient input file (not to be used with\
            --load_transtbl)
        load_transtbl: Load unwarp transform table in m3z format (not to be used\
            with --gradcoeff)
        output_file: Output unwarped volume or surface
        out_transtbl: Save unwarp transform table in m3z format
        save_transtbl_only: Just save unwarp transform table in m3z format,\
            requires --gradcoeff
        interpolation_type: Interpolation method: nearest | trilinear | cubic\
            (default is trilinear)
        nthreads: Number of threads to run
        checkopts: Don't run anything, just check options and exit
        version: Print out version and exit
        help: Print out information on how to use this program
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gradunwarp_params(
        input_file=input_file,
        gradient_coeff=gradient_coeff,
        load_transtbl=load_transtbl,
        output_file=output_file,
        out_transtbl=out_transtbl,
        save_transtbl_only=save_transtbl_only,
        interpolation_type=interpolation_type,
        nthreads=nthreads,
        checkopts=checkopts,
        version=version,
        help=help,
    )
    return mri_gradunwarp_execute(params, runner)

__all__ = [
    "MriGradunwarp",
    "MriGradunwarpOutputs",
    "MRI_GRADUNWARP_METADATA",
    "mri_gradunwarp_cargs",
    "mri_gradunwarp_outputs",
    "mri_gradunwarp_params",
    "mri_gradunwarp_execute",
    "mri_gradunwarp_validate",
    "mri_gradunwarp",
]