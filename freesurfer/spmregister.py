# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPMREGISTER_METADATA = Metadata(
    id="spmregister",
    name="spmregister",
    package="freesurfer",
)

Spmregister = typing.TypedDict(
    "Spmregister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/spmregister"]],
        "subjid": str,
        "mov": str,
        "reg": str,
        "frame": typing.NotRequired[float],
        "mid_frame": typing.NotRequired[bool],
        "template_out": typing.NotRequired[str],
        "fsvol": typing.NotRequired[str],
        "force_ras": typing.NotRequired[bool],
        "outvol": typing.NotRequired[str],
        "tmpdir": typing.NotRequired[str],
        "nocleanup": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SpmregisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registration_file: OutputPathType
    """Output registration file."""
    resampled_mov: OutputPathType | None
    """Resampled mov volume."""

def spmregister_params(
    subjid: str,
    mov: str,
    reg: str,
    frame: float | None = None,
    mid_frame: bool = False,
    template_out: str | None = None,
    fsvol: str | None = None,
    force_ras: bool = False,
    outvol: str | None = None,
    tmpdir: str | None = None,
    nocleanup: bool = False,
    version: bool = False,
    help: bool = False,
) -> Spmregister:
    """
    Build parameters.

    Args:
        subjid: Id of the subject as found in SUBJECTS_DIR. This is converted to\
            analyze using mri_convert.
        mov: Volume identifier of the movable volume. Must be specified in a way\
            suitable for mri_convert.
        reg: Output registration file. Maps RAS in the reference to RAS in the\
            movable.
        frame: Use something other than the first frame. Specify the frame number\
            you want.
        mid_frame: Use the middle frame of the mov volume as the template.
        template_out: Save the mov template when template is something other than\
            the first frame.
        fsvol: Use FreeSurfer volid (default brainmask).
        force_ras: Force input geometry to be RAS.
        outvol: Resample mov and save as outvol.
        tmpdir: Temporary directory (implies --nocleanup).
        nocleanup: Do not delete temporary files.
        version: Print version and exit.
        help: Print help and exit.

    Returns:
        Parameter dictionary.
    """
    params: Spmregister = {
        "@type": "freesurfer/spmregister",
        "subjid": subjid,
        "mov": mov,
        "reg": reg,
        "mid_frame": mid_frame,
        "force_ras": force_ras,
        "nocleanup": nocleanup,
        "version": version,
        "help": help,
    }
    if frame is not None:
        params["frame"] = frame
    if template_out is not None:
        params["template_out"] = template_out
    if fsvol is not None:
        params["fsvol"] = fsvol
    if outvol is not None:
        params["outvol"] = outvol
    if tmpdir is not None:
        params["tmpdir"] = tmpdir
    return params

def spmregister_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Spmregister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjid", None) is None:
        raise StyxValidationError("`subjid` must not be None")
    if not isinstance(params["subjid"], str):
        raise StyxValidationError(f'`subjid` has the wrong type: Received `{type(params["subjid"])}` expected `str`')
    if params.get("mov", None) is None:
        raise StyxValidationError("`mov` must not be None")
    if not isinstance(params["mov"], str):
        raise StyxValidationError(f'`mov` has the wrong type: Received `{type(params["mov"])}` expected `str`')
    if params.get("reg", None) is None:
        raise StyxValidationError("`reg` must not be None")
    if not isinstance(params["reg"], str):
        raise StyxValidationError(f'`reg` has the wrong type: Received `{type(params["reg"])}` expected `str`')
    if params.get("frame", None) is not None:
        if not isinstance(params["frame"], (float, int)):
            raise StyxValidationError(f'`frame` has the wrong type: Received `{type(params["frame"])}` expected `float`')
    if params.get("mid_frame", None) is not None:
        if not isinstance(params["mid_frame"], bool):
            raise StyxValidationError(f'`mid_frame` has the wrong type: Received `{type(params["mid_frame"])}` expected `bool`')
    if params.get("template_out", None) is not None:
        if not isinstance(params["template_out"], str):
            raise StyxValidationError(f'`template_out` has the wrong type: Received `{type(params["template_out"])}` expected `str`')
    if params.get("fsvol", None) is not None:
        if not isinstance(params["fsvol"], str):
            raise StyxValidationError(f'`fsvol` has the wrong type: Received `{type(params["fsvol"])}` expected `str`')
    if params.get("force_ras", None) is not None:
        if not isinstance(params["force_ras"], bool):
            raise StyxValidationError(f'`force_ras` has the wrong type: Received `{type(params["force_ras"])}` expected `bool`')
    if params.get("outvol", None) is not None:
        if not isinstance(params["outvol"], str):
            raise StyxValidationError(f'`outvol` has the wrong type: Received `{type(params["outvol"])}` expected `str`')
    if params.get("tmpdir", None) is not None:
        if not isinstance(params["tmpdir"], str):
            raise StyxValidationError(f'`tmpdir` has the wrong type: Received `{type(params["tmpdir"])}` expected `str`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def spmregister_cargs(params: Spmregister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("spmregister")
    cargs.append("--s")
    cargs.append(params["subjid"])
    cargs.append("--mov")
    cargs.append(params["mov"])
    cargs.append("--reg")
    cargs.append(params["reg"])
    v_0 = params.get("frame")
    if v_0 is not None:
        cargs.append("--frame")
        cargs.append(str(v_0))
    if params.get("mid_frame"):
        cargs.append("--mid-frame")
    v_1 = params.get("template_out")
    if v_1 is not None:
        cargs.append("--template-out")
        cargs.append(v_1)
    v_2 = params.get("fsvol")
    if v_2 is not None:
        cargs.append("--fsvol")
        cargs.append(v_2)
    if params.get("force_ras"):
        cargs.append("--force-ras")
    v_3 = params.get("outvol")
    if v_3 is not None:
        cargs.append("--o")
        cargs.append(v_3)
    v_4 = params.get("tmpdir")
    if v_4 is not None:
        cargs.append("--tmp")
        cargs.append(v_4)
    if params.get("nocleanup"):
        cargs.append("--nocleanup")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def spmregister_outputs(params: Spmregister, execution: Execution) -> SpmregisterOutputs:
    """Build the SpmregisterOutputs object for this tool."""
    resampled_mov_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    registration_file_v: OutputPathType = execution.output_file("register.dat")
    __v0 = params.get("outvol")
    if __v0 is not None:
        resampled_mov_v = execution.output_file((__v0 if __v0 is not None else ""))
    return SpmregisterOutputs(
        root=root_v,
        registration_file=registration_file_v,
        resampled_mov=resampled_mov_v,
    )

def spmregister_execute(params: Spmregister, runner: Runner | None = None) -> SpmregisterOutputs:
    """
    spmregister

    Registers a volume to its FreeSurfer anatomical using SPM's spm_coreg

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spmregister_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPMREGISTER_METADATA)
    execution.params(params)
    args = spmregister_cargs(params, execution)
    out = spmregister_outputs(params, execution)
    execution.run(args)
    return out

def spmregister(
    subjid: str,
    mov: str,
    reg: str,
    frame: float | None = None,
    mid_frame: bool = False,
    template_out: str | None = None,
    fsvol: str | None = None,
    force_ras: bool = False,
    outvol: str | None = None,
    tmpdir: str | None = None,
    nocleanup: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> SpmregisterOutputs:
    """
    spmregister

    Registers a volume to its FreeSurfer anatomical using SPM's spm_coreg

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjid: Id of the subject as found in SUBJECTS_DIR. This is converted to\
            analyze using mri_convert.
        mov: Volume identifier of the movable volume. Must be specified in a way\
            suitable for mri_convert.
        reg: Output registration file. Maps RAS in the reference to RAS in the\
            movable.
        frame: Use something other than the first frame. Specify the frame number\
            you want.
        mid_frame: Use the middle frame of the mov volume as the template.
        template_out: Save the mov template when template is something other than\
            the first frame.
        fsvol: Use FreeSurfer volid (default brainmask).
        force_ras: Force input geometry to be RAS.
        outvol: Resample mov and save as outvol.
        tmpdir: Temporary directory (implies --nocleanup).
        nocleanup: Do not delete temporary files.
        version: Print version and exit.
        help: Print help and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spmregister_params(
        subjid=subjid,
        mov=mov,
        reg=reg,
        frame=frame,
        mid_frame=mid_frame,
        template_out=template_out,
        fsvol=fsvol,
        force_ras=force_ras,
        outvol=outvol,
        tmpdir=tmpdir,
        nocleanup=nocleanup,
        version=version,
        help=help,
    )
    return spmregister_execute(params, runner)

__all__ = [
    "Spmregister",
    "SpmregisterOutputs",
    "SPMREGISTER_METADATA",
    "spmregister_cargs",
    "spmregister_outputs",
    "spmregister_params",
    "spmregister_execute",
    "spmregister_validate",
    "spmregister",
]