# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_SUBJECT_METADATA = Metadata(
    id="morph_subject",
    name="morph_subject",
    package="freesurfer",
)

MorphSubject = typing.TypedDict(
    "MorphSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/morph_subject"]],
        "subjid": str,
    },
)

@dataclasses.dataclass
class MorphSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def morph_subject_params(
    subjid: str,
) -> MorphSubject:
    """
    Build parameters.

    Args:
        subjid: Subject ID for processing

    Returns:
        Parameter dictionary.
    """
    params: MorphSubject = {
        "@type": "freesurfer/morph_subject",
        "subjid": subjid,
    }
    return params

def morph_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjid", None) is None:
        raise StyxValidationError("`subjid` must not be None")
    if not isinstance(params["subjid"], str):
        raise StyxValidationError(f'`subjid` has the wrong type: Received `{type(params["subjid"])}` expected `str`')

def morph_subject_cargs(params: MorphSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_subject")
    cargs.append(params["subjid"])
    return cargs

def morph_subject_outputs(params: MorphSubject, execution: Execution) -> MorphSubjectOutputs:
    """Build the MorphSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MorphSubjectOutputs(
        root=root_v,
    )

def morph_subject_execute(params: MorphSubject, runner: Runner | None = None) -> MorphSubjectOutputs:
    """
    morph_subject

    A morphological processing tool for Freesurfer subjects. The specific operations and options are not documented in the available help text.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_SUBJECT_METADATA)
    execution.params(params)
    args = morph_subject_cargs(params, execution)
    out = morph_subject_outputs(params, execution)
    execution.run(args)
    return out

def morph_subject(
    subjid: str,
    runner: Runner | None = None,
) -> MorphSubjectOutputs:
    """
    morph_subject

    A morphological processing tool for Freesurfer subjects. The specific operations and options are not documented in the available help text.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjid: Subject ID for processing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_subject_params(
        subjid=subjid,
    )
    return morph_subject_execute(params, runner)

__all__ = [
    "MorphSubject",
    "MorphSubjectOutputs",
    "MORPH_SUBJECT_METADATA",
    "morph_subject_cargs",
    "morph_subject_outputs",
    "morph_subject_params",
    "morph_subject_execute",
    "morph_subject_validate",
    "morph_subject",
]