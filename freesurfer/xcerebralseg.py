# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XCEREBRALSEG_METADATA = Metadata(
    id="xcerebralseg",
    name="xcerebralseg",
    package="freesurfer",
)

Xcerebralseg = typing.TypedDict(
    "Xcerebralseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xcerebralseg"]],
        "subject": str,
        "output_volume": typing.NotRequired[str],
        "atlas": typing.NotRequired[str],
        "mergevol": typing.NotRequired[str],
        "source_volume": typing.NotRequired[str],
        "no_stats": typing.NotRequired[bool],
        "seg1_name": typing.NotRequired[str],
        "no_pons": typing.NotRequired[bool],
        "no_vermis": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class XcerebralsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Output volume file for the whole head segmentation"""

def xcerebralseg_params(
    subject: str,
    output_volume: str | None = None,
    atlas: str | None = None,
    mergevol: str | None = None,
    source_volume: str | None = None,
    no_stats: bool = False,
    seg1_name: str | None = None,
    no_pons: bool = False,
    no_vermis: bool = False,
    threads: float | None = None,
) -> Xcerebralseg:
    """
    Build parameters.

    Args:
        subject: Subject identifier (required)
        output_volume: Output volume file (default: apas+head.mgz)
        atlas: Atlas file path (default:\
            /usr/local/freesurfer/average/aseg+spmhead+vermis+pons.ixi.gca)
        mergevol: Merge with mergevol (default: aparc+aseg.mgz)
        source_volume: Source intensity volume (default: nu.mgz)
        no_stats: Do not run mri_segstats
        seg1_name: Full-head segmentation name (usually computed with mri_ca_label)
        no_pons: Exclude pons segmentation
        no_vermis: Exclude vermis segmentation
        threads: Set number of OPENMP threads

    Returns:
        Parameter dictionary.
    """
    params: Xcerebralseg = {
        "@type": "freesurfer/xcerebralseg",
        "subject": subject,
        "no_stats": no_stats,
        "no_pons": no_pons,
        "no_vermis": no_vermis,
    }
    if output_volume is not None:
        params["output_volume"] = output_volume
    if atlas is not None:
        params["atlas"] = atlas
    if mergevol is not None:
        params["mergevol"] = mergevol
    if source_volume is not None:
        params["source_volume"] = source_volume
    if seg1_name is not None:
        params["seg1_name"] = seg1_name
    if threads is not None:
        params["threads"] = threads
    return params

def xcerebralseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Xcerebralseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("atlas", None) is not None:
        if not isinstance(params["atlas"], str):
            raise StyxValidationError(f'`atlas` has the wrong type: Received `{type(params["atlas"])}` expected `str`')
    if params.get("mergevol", None) is not None:
        if not isinstance(params["mergevol"], str):
            raise StyxValidationError(f'`mergevol` has the wrong type: Received `{type(params["mergevol"])}` expected `str`')
    if params.get("source_volume", None) is not None:
        if not isinstance(params["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["source_volume"])}` expected `str`')
    if params.get("no_stats", None) is not None:
        if not isinstance(params["no_stats"], bool):
            raise StyxValidationError(f'`no_stats` has the wrong type: Received `{type(params["no_stats"])}` expected `bool`')
    if params.get("seg1_name", None) is not None:
        if not isinstance(params["seg1_name"], str):
            raise StyxValidationError(f'`seg1_name` has the wrong type: Received `{type(params["seg1_name"])}` expected `str`')
    if params.get("no_pons", None) is not None:
        if not isinstance(params["no_pons"], bool):
            raise StyxValidationError(f'`no_pons` has the wrong type: Received `{type(params["no_pons"])}` expected `bool`')
    if params.get("no_vermis", None) is not None:
        if not isinstance(params["no_vermis"], bool):
            raise StyxValidationError(f'`no_vermis` has the wrong type: Received `{type(params["no_vermis"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def xcerebralseg_cargs(params: Xcerebralseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xcerebralseg")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("output_volume")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    v_1 = params.get("atlas")
    if v_1 is not None:
        cargs.append("--atlas")
        cargs.append(v_1)
    v_2 = params.get("mergevol")
    if v_2 is not None:
        cargs.append("--m")
        cargs.append(v_2)
    v_3 = params.get("source_volume")
    if v_3 is not None:
        cargs.append("--srcvol")
        cargs.append(v_3)
    if params.get("no_stats"):
        cargs.append("--no-stats")
    v_4 = params.get("seg1_name")
    if v_4 is not None:
        cargs.append("--seg1")
        cargs.append(v_4)
    if params.get("no_pons"):
        cargs.append("--no-pons")
    if params.get("no_vermis"):
        cargs.append("--no-vermis")
    v_5 = params.get("threads")
    if v_5 is not None:
        cargs.append("--threads")
        cargs.append(str(v_5))
    return cargs

def xcerebralseg_outputs(params: Xcerebralseg, execution: Execution) -> XcerebralsegOutputs:
    """Build the XcerebralsegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file("apas+head.mgz")
    return XcerebralsegOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def xcerebralseg_execute(params: Xcerebralseg, runner: Runner | None = None) -> XcerebralsegOutputs:
    """
    xcerebralseg

    Tool for labeling extracerebral structures including sulcal CSF, skull/bone, head soft tissue, and air inside the head, merged with aparc+aseg.mgz segmentation for a whole head segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xcerebralseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XCEREBRALSEG_METADATA)
    execution.params(params)
    args = xcerebralseg_cargs(params, execution)
    out = xcerebralseg_outputs(params, execution)
    execution.run(args)
    return out

def xcerebralseg(
    subject: str,
    output_volume: str | None = None,
    atlas: str | None = None,
    mergevol: str | None = None,
    source_volume: str | None = None,
    no_stats: bool = False,
    seg1_name: str | None = None,
    no_pons: bool = False,
    no_vermis: bool = False,
    threads: float | None = None,
    runner: Runner | None = None,
) -> XcerebralsegOutputs:
    """
    xcerebralseg

    Tool for labeling extracerebral structures including sulcal CSF, skull/bone, head soft tissue, and air inside the head, merged with aparc+aseg.mgz segmentation for a whole head segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier (required)
        output_volume: Output volume file (default: apas+head.mgz)
        atlas: Atlas file path (default:\
            /usr/local/freesurfer/average/aseg+spmhead+vermis+pons.ixi.gca)
        mergevol: Merge with mergevol (default: aparc+aseg.mgz)
        source_volume: Source intensity volume (default: nu.mgz)
        no_stats: Do not run mri_segstats
        seg1_name: Full-head segmentation name (usually computed with mri_ca_label)
        no_pons: Exclude pons segmentation
        no_vermis: Exclude vermis segmentation
        threads: Set number of OPENMP threads
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xcerebralseg_params(
        subject=subject,
        output_volume=output_volume,
        atlas=atlas,
        mergevol=mergevol,
        source_volume=source_volume,
        no_stats=no_stats,
        seg1_name=seg1_name,
        no_pons=no_pons,
        no_vermis=no_vermis,
        threads=threads,
    )
    return xcerebralseg_execute(params, runner)

__all__ = [
    "Xcerebralseg",
    "XcerebralsegOutputs",
    "XCEREBRALSEG_METADATA",
    "xcerebralseg_cargs",
    "xcerebralseg_outputs",
    "xcerebralseg_params",
    "xcerebralseg_execute",
    "xcerebralseg_validate",
    "xcerebralseg",
]