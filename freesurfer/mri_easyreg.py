# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EASYREG_METADATA = Metadata(
    id="mri_easyreg",
    name="mri_easyreg",
    package="freesurfer",
)

MriEasyreg = typing.TypedDict(
    "MriEasyreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_easyreg"]],
        "reference_image": InputPathType,
        "reference_segmentation": typing.NotRequired[InputPathType],
        "floating_image": InputPathType,
        "floating_segmentation": typing.NotRequired[InputPathType],
        "registered_reference": typing.NotRequired[InputPathType],
        "registered_floating": typing.NotRequired[InputPathType],
        "forward_field": typing.NotRequired[InputPathType],
        "inverse_field": typing.NotRequired[InputPathType],
        "affine_only": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriEasyregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_reference_output: OutputPathType
    """Output registered reference image"""
    registered_floating_output: OutputPathType
    """Output registered floating image"""
    forward_field_output: OutputPathType
    """Output forward field file"""
    inverse_field_output: OutputPathType
    """Output inverse field file"""

def mri_easyreg_params(
    reference_image: InputPathType,
    floating_image: InputPathType,
    reference_segmentation: InputPathType | None = None,
    floating_segmentation: InputPathType | None = None,
    registered_reference: InputPathType | None = None,
    registered_floating: InputPathType | None = None,
    forward_field: InputPathType | None = None,
    inverse_field: InputPathType | None = None,
    affine_only: bool = False,
    threads: float | None = None,
) -> MriEasyreg:
    """
    Build parameters.

    Args:
        reference_image: Reference image
        floating_image: Floating image
        reference_segmentation: Reference SynthSeg segmentation (will be created if\
            it does not exist)
        floating_segmentation: Floating SynthSeg segmentation (will be created if\
            it does not exist)
        registered_reference: (optional) Registered reference
        registered_floating: (optional) Registered floating images (in space of\
            reference)
        forward_field: (optional) Forward field
        inverse_field: (optional) Inverse field
        affine_only: (optional) Skips nonlinear part
        threads: (optional) Number of cores to be used. Default is 1. You can use\
            -1 to use all available cores

    Returns:
        Parameter dictionary.
    """
    params: MriEasyreg = {
        "@type": "freesurfer/mri_easyreg",
        "reference_image": reference_image,
        "floating_image": floating_image,
        "affine_only": affine_only,
    }
    if reference_segmentation is not None:
        params["reference_segmentation"] = reference_segmentation
    if floating_segmentation is not None:
        params["floating_segmentation"] = floating_segmentation
    if registered_reference is not None:
        params["registered_reference"] = registered_reference
    if registered_floating is not None:
        params["registered_floating"] = registered_floating
    if forward_field is not None:
        params["forward_field"] = forward_field
    if inverse_field is not None:
        params["inverse_field"] = inverse_field
    if threads is not None:
        params["threads"] = threads
    return params

def mri_easyreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriEasyreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("reference_segmentation", None) is not None:
        if not isinstance(params["reference_segmentation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reference_segmentation` has the wrong type: Received `{type(params["reference_segmentation"])}` expected `InputPathType`')
    if params.get("floating_image", None) is None:
        raise StyxValidationError("`floating_image` must not be None")
    if not isinstance(params["floating_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`floating_image` has the wrong type: Received `{type(params["floating_image"])}` expected `InputPathType`')
    if params.get("floating_segmentation", None) is not None:
        if not isinstance(params["floating_segmentation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`floating_segmentation` has the wrong type: Received `{type(params["floating_segmentation"])}` expected `InputPathType`')
    if params.get("registered_reference", None) is not None:
        if not isinstance(params["registered_reference"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registered_reference` has the wrong type: Received `{type(params["registered_reference"])}` expected `InputPathType`')
    if params.get("registered_floating", None) is not None:
        if not isinstance(params["registered_floating"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registered_floating` has the wrong type: Received `{type(params["registered_floating"])}` expected `InputPathType`')
    if params.get("forward_field", None) is not None:
        if not isinstance(params["forward_field"], (pathlib.Path, str)):
            raise StyxValidationError(f'`forward_field` has the wrong type: Received `{type(params["forward_field"])}` expected `InputPathType`')
    if params.get("inverse_field", None) is not None:
        if not isinstance(params["inverse_field"], (pathlib.Path, str)):
            raise StyxValidationError(f'`inverse_field` has the wrong type: Received `{type(params["inverse_field"])}` expected `InputPathType`')
    if params.get("affine_only", None) is not None:
        if not isinstance(params["affine_only"], bool):
            raise StyxValidationError(f'`affine_only` has the wrong type: Received `{type(params["affine_only"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def mri_easyreg_cargs(params: MriEasyreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_easyreg")
    cargs.append("--ref")
    cargs.append(execution.input_file(params["reference_image"]))
    v_0 = params.get("reference_segmentation")
    if v_0 is not None:
        cargs.append("--ref_seg")
        cargs.append(execution.input_file(v_0))
    cargs.append("--flo")
    cargs.append(execution.input_file(params["floating_image"]))
    v_1 = params.get("floating_segmentation")
    if v_1 is not None:
        cargs.append("--flo_seg")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("registered_reference")
    if v_2 is not None:
        cargs.append("--ref_reg")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("registered_floating")
    if v_3 is not None:
        cargs.append("--flo_reg")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("forward_field")
    if v_4 is not None:
        cargs.append("--fwd_field")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("inverse_field")
    if v_5 is not None:
        cargs.append("--bak_field")
        cargs.append(execution.input_file(v_5))
    if params.get("affine_only"):
        cargs.append("--affine_only")
    v_6 = params.get("threads")
    if v_6 is not None:
        cargs.append("--threads")
        cargs.append(str(v_6))
    return cargs

def mri_easyreg_outputs(params: MriEasyreg, execution: Execution) -> MriEasyregOutputs:
    """Build the MriEasyregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_reference_output_v: OutputPathType = execution.output_file("registered_reference.nii.gz")
    registered_floating_output_v: OutputPathType = execution.output_file("registered_floating.nii.gz")
    forward_field_output_v: OutputPathType = execution.output_file("forward_field.nii.gz")
    inverse_field_output_v: OutputPathType = execution.output_file("inverse_field.nii.gz")
    return MriEasyregOutputs(
        root=root_v,
        registered_reference_output=registered_reference_output_v,
        registered_floating_output=registered_floating_output_v,
        forward_field_output=forward_field_output_v,
        inverse_field_output=inverse_field_output_v,
    )

def mri_easyreg_execute(params: MriEasyreg, runner: Runner | None = None) -> MriEasyregOutputs:
    """
    mri_easyreg

    EasyReg: deep learning registration simple and easy

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_easyreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EASYREG_METADATA)
    execution.params(params)
    args = mri_easyreg_cargs(params, execution)
    out = mri_easyreg_outputs(params, execution)
    execution.run(args)
    return out

def mri_easyreg(
    reference_image: InputPathType,
    floating_image: InputPathType,
    reference_segmentation: InputPathType | None = None,
    floating_segmentation: InputPathType | None = None,
    registered_reference: InputPathType | None = None,
    registered_floating: InputPathType | None = None,
    forward_field: InputPathType | None = None,
    inverse_field: InputPathType | None = None,
    affine_only: bool = False,
    threads: float | None = None,
    runner: Runner | None = None,
) -> MriEasyregOutputs:
    """
    mri_easyreg

    EasyReg: deep learning registration simple and easy

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        reference_image: Reference image
        floating_image: Floating image
        reference_segmentation: Reference SynthSeg segmentation (will be created if\
            it does not exist)
        floating_segmentation: Floating SynthSeg segmentation (will be created if\
            it does not exist)
        registered_reference: (optional) Registered reference
        registered_floating: (optional) Registered floating images (in space of\
            reference)
        forward_field: (optional) Forward field
        inverse_field: (optional) Inverse field
        affine_only: (optional) Skips nonlinear part
        threads: (optional) Number of cores to be used. Default is 1. You can use\
            -1 to use all available cores
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_easyreg_params(
        reference_image=reference_image,
        floating_image=floating_image,
        reference_segmentation=reference_segmentation,
        floating_segmentation=floating_segmentation,
        registered_reference=registered_reference,
        registered_floating=registered_floating,
        forward_field=forward_field,
        inverse_field=inverse_field,
        affine_only=affine_only,
        threads=threads,
    )
    return mri_easyreg_execute(params, runner)

__all__ = [
    "MriEasyreg",
    "MriEasyregOutputs",
    "MRI_EASYREG_METADATA",
    "mri_easyreg_cargs",
    "mri_easyreg_outputs",
    "mri_easyreg_params",
    "mri_easyreg_execute",
    "mri_easyreg_validate",
    "mri_easyreg",
]