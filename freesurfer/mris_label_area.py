# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_LABEL_AREA_METADATA = Metadata(
    id="mris_label_area",
    name="mris_label_area",
    package="freesurfer",
)

MrisLabelArea = typing.TypedDict(
    "MrisLabelArea",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_label_area"]],
        "pct_flag": typing.NotRequired[bool],
        "log_file": typing.NotRequired[str],
        "brain_vol": typing.NotRequired[str],
        "subject_name": str,
        "hemi": str,
        "surf_name": str,
        "annot_name": str,
        "labels": list[str],
    },
)

@dataclasses.dataclass
class MrisLabelAreaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_label_area_params(
    subject_name: str,
    hemi: str,
    surf_name: str,
    annot_name: str,
    labels: list[str],
    pct_flag: bool = False,
    log_file: str | None = None,
    brain_vol: str | None = None,
) -> MrisLabelArea:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject
        hemi: Hemisphere, typically 'lh' or 'rh'
        surf_name: Surface name
        annot_name: Annotation name
        labels: Labels to calculate area for
        pct_flag: Compute brain area as a percentage of all brain labels
        log_file: Log results to file (use %d to include label number)
        brain_vol: Load brain volume and use it to normalize areas

    Returns:
        Parameter dictionary.
    """
    params: MrisLabelArea = {
        "@type": "freesurfer/mris_label_area",
        "subject_name": subject_name,
        "hemi": hemi,
        "surf_name": surf_name,
        "annot_name": annot_name,
        "labels": labels,
        "pct_flag": pct_flag,
    }
    if log_file is not None:
        params["log_file"] = log_file
    if brain_vol is not None:
        params["brain_vol"] = brain_vol
    return params

def mris_label_area_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisLabelArea."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("pct_flag", None) is not None:
        if not isinstance(params["pct_flag"], bool):
            raise StyxValidationError(f'`pct_flag` has the wrong type: Received `{type(params["pct_flag"])}` expected `bool`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("brain_vol", None) is not None:
        if not isinstance(params["brain_vol"], str):
            raise StyxValidationError(f'`brain_vol` has the wrong type: Received `{type(params["brain_vol"])}` expected `str`')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf_name", None) is None:
        raise StyxValidationError("`surf_name` must not be None")
    if not isinstance(params["surf_name"], str):
        raise StyxValidationError(f'`surf_name` has the wrong type: Received `{type(params["surf_name"])}` expected `str`')
    if params.get("annot_name", None) is None:
        raise StyxValidationError("`annot_name` must not be None")
    if not isinstance(params["annot_name"], str):
        raise StyxValidationError(f'`annot_name` has the wrong type: Received `{type(params["annot_name"])}` expected `str`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')

def mris_label_area_cargs(params: MrisLabelArea, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_label_area")
    if params.get("pct_flag"):
        cargs.append("-p")
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-l")
        cargs.append(v_0)
    v_1 = params.get("brain_vol")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(v_1)
    cargs.append(params["subject_name"])
    cargs.append(params["hemi"])
    cargs.append(params["surf_name"])
    cargs.append(params["annot_name"])
    for item0 in params["labels"]:
        cargs.append(item0)
    return cargs

def mris_label_area_outputs(params: MrisLabelArea, execution: Execution) -> MrisLabelAreaOutputs:
    """Build the MrisLabelAreaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisLabelAreaOutputs(
        root=root_v,
    )

def mris_label_area_execute(params: MrisLabelArea, runner: Runner | None = None) -> MrisLabelAreaOutputs:
    """
    mris_label_area

    Compute the area of specific labels on a surface of a brain hemisphere in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_label_area_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_LABEL_AREA_METADATA)
    execution.params(params)
    args = mris_label_area_cargs(params, execution)
    out = mris_label_area_outputs(params, execution)
    execution.run(args)
    return out

def mris_label_area(
    subject_name: str,
    hemi: str,
    surf_name: str,
    annot_name: str,
    labels: list[str],
    pct_flag: bool = False,
    log_file: str | None = None,
    brain_vol: str | None = None,
    runner: Runner | None = None,
) -> MrisLabelAreaOutputs:
    """
    mris_label_area

    Compute the area of specific labels on a surface of a brain hemisphere in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject
        hemi: Hemisphere, typically 'lh' or 'rh'
        surf_name: Surface name
        annot_name: Annotation name
        labels: Labels to calculate area for
        pct_flag: Compute brain area as a percentage of all brain labels
        log_file: Log results to file (use %d to include label number)
        brain_vol: Load brain volume and use it to normalize areas
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_label_area_params(
        subject_name=subject_name,
        hemi=hemi,
        surf_name=surf_name,
        annot_name=annot_name,
        labels=labels,
        pct_flag=pct_flag,
        log_file=log_file,
        brain_vol=brain_vol,
    )
    return mris_label_area_execute(params, runner)

__all__ = [
    "MrisLabelArea",
    "MrisLabelAreaOutputs",
    "MRIS_LABEL_AREA_METADATA",
    "mris_label_area_cargs",
    "mris_label_area_outputs",
    "mris_label_area_params",
    "mris_label_area_execute",
    "mris_label_area_validate",
    "mris_label_area",
]