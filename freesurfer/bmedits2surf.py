# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BMEDITS2SURF_METADATA = Metadata(
    id="bmedits2surf",
    name="bmedits2surf",
    package="freesurfer",
)

Bmedits2surf = typing.TypedDict(
    "Bmedits2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bmedits2surf"]],
        "subject": str,
        "self": typing.NotRequired[bool],
        "overwrite": typing.NotRequired[bool],
        "tmp_dir": typing.NotRequired[str],
        "cleanup": typing.NotRequired[bool],
        "no_cleanup": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "left_hemisphere": typing.NotRequired[bool],
        "right_hemisphere": typing.NotRequired[bool],
        "no_surfs": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Bmedits2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_bmerase: OutputPathType
    """Binary mask of erased surface locations for the left hemisphere in fsaverage space."""
    rh_bmerase: OutputPathType
    """Binary mask of erased surface locations for the right hemisphere in fsaverage space."""
    lh_bmclone: OutputPathType
    """Binary mask of cloned surface locations for the left hemisphere in fsaverage space."""
    rh_bmclone: OutputPathType
    """Binary mask of cloned surface locations for the right hemisphere in fsaverage space."""
    bmclone_stats: OutputPathType
    """Statistics about the number of voxels cloned."""
    bmerase_stats: OutputPathType
    """Statistics about the number of voxels erased."""

def bmedits2surf_params(
    subject: str,
    self: bool = False,
    overwrite: bool = False,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
    debug: bool = False,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    no_surfs: bool = False,
) -> Bmedits2surf:
    """
    Build parameters.

    Args:
        subject: The subject for which the binary map will be computed.
        self: Output will be on self instead of fsaverage.
        overwrite: Force overwriting of existing results.
        tmp_dir: Temporary directory.
        cleanup: Clean up temporary files.
        no_cleanup: Do not clean up temporary files.
        debug: Enable debug mode.
        left_hemisphere: Perform operation only on the left hemisphere.
        right_hemisphere: Perform operation only on the right hemisphere.
        no_surfs: Do not compute surfaces, only statistics.

    Returns:
        Parameter dictionary.
    """
    params: Bmedits2surf = {
        "@type": "freesurfer/bmedits2surf",
        "subject": subject,
        "self": self,
        "overwrite": overwrite,
        "cleanup": cleanup,
        "no_cleanup": no_cleanup,
        "debug": debug,
        "left_hemisphere": left_hemisphere,
        "right_hemisphere": right_hemisphere,
        "no_surfs": no_surfs,
    }
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def bmedits2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bmedits2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("self", None) is not None:
        if not isinstance(params["self"], bool):
            raise StyxValidationError(f'`self` has the wrong type: Received `{type(params["self"])}` expected `bool`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("cleanup", None) is not None:
        if not isinstance(params["cleanup"], bool):
            raise StyxValidationError(f'`cleanup` has the wrong type: Received `{type(params["cleanup"])}` expected `bool`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("left_hemisphere", None) is not None:
        if not isinstance(params["left_hemisphere"], bool):
            raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(params["left_hemisphere"])}` expected `bool`')
    if params.get("right_hemisphere", None) is not None:
        if not isinstance(params["right_hemisphere"], bool):
            raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(params["right_hemisphere"])}` expected `bool`')
    if params.get("no_surfs", None) is not None:
        if not isinstance(params["no_surfs"], bool):
            raise StyxValidationError(f'`no_surfs` has the wrong type: Received `{type(params["no_surfs"])}` expected `bool`')

def bmedits2surf_cargs(params: Bmedits2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bmedits2surf")
    cargs.append("-s")
    cargs.append(params["subject"])
    if params.get("self"):
        cargs.append("--self")
    if params.get("overwrite"):
        cargs.append("--overwrite")
    v_0 = params.get("tmp_dir")
    if v_0 is not None:
        cargs.append("--tmp")
        cargs.append(v_0)
    if params.get("cleanup"):
        cargs.append("--cleanup")
    if params.get("no_cleanup"):
        cargs.append("--no-cleanup")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("left_hemisphere"):
        cargs.append("--lh")
    if params.get("right_hemisphere"):
        cargs.append("--rh")
    if params.get("no_surfs"):
        cargs.append("--no-surfs")
    return cargs

def bmedits2surf_outputs(params: Bmedits2surf, execution: Execution) -> Bmedits2surfOutputs:
    """Build the Bmedits2surfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    lh_bmerase_v: OutputPathType = execution.output_file(f'{params["subject"]}/surf/lh.bmerase.fsa.mgh')
    rh_bmerase_v: OutputPathType = execution.output_file(f'{params["subject"]}/surf/rh.bmerase.fsa.mgh')
    lh_bmclone_v: OutputPathType = execution.output_file(f'{params["subject"]}/surf/lh.bmclone.fsa.mgh')
    rh_bmclone_v: OutputPathType = execution.output_file(f'{params["subject"]}/surf/rh.bmclone.fsa.mgh')
    bmclone_stats_v: OutputPathType = execution.output_file(f'{params["subject"]}/stats/bmclone.dat')
    bmerase_stats_v: OutputPathType = execution.output_file(f'{params["subject"]}/stats/bmerase.dat')
    return Bmedits2surfOutputs(
        root=root_v,
        lh_bmerase=lh_bmerase_v,
        rh_bmerase=rh_bmerase_v,
        lh_bmclone=lh_bmclone_v,
        rh_bmclone=rh_bmclone_v,
        bmclone_stats=bmclone_stats_v,
        bmerase_stats=bmerase_stats_v,
    )

def bmedits2surf_execute(params: Bmedits2surf, runner: Runner | None = None) -> Bmedits2surfOutputs:
    """
    bmedits2surf

    Computes a binary map of surface locations where the brainmask.mgz has been edited.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bmedits2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BMEDITS2SURF_METADATA)
    execution.params(params)
    args = bmedits2surf_cargs(params, execution)
    out = bmedits2surf_outputs(params, execution)
    execution.run(args)
    return out

def bmedits2surf(
    subject: str,
    self: bool = False,
    overwrite: bool = False,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
    debug: bool = False,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    no_surfs: bool = False,
    runner: Runner | None = None,
) -> Bmedits2surfOutputs:
    """
    bmedits2surf

    Computes a binary map of surface locations where the brainmask.mgz has been edited.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject for which the binary map will be computed.
        self: Output will be on self instead of fsaverage.
        overwrite: Force overwriting of existing results.
        tmp_dir: Temporary directory.
        cleanup: Clean up temporary files.
        no_cleanup: Do not clean up temporary files.
        debug: Enable debug mode.
        left_hemisphere: Perform operation only on the left hemisphere.
        right_hemisphere: Perform operation only on the right hemisphere.
        no_surfs: Do not compute surfaces, only statistics.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bmedits2surf_params(
        subject=subject,
        self=self,
        overwrite=overwrite,
        tmp_dir=tmp_dir,
        cleanup=cleanup,
        no_cleanup=no_cleanup,
        debug=debug,
        left_hemisphere=left_hemisphere,
        right_hemisphere=right_hemisphere,
        no_surfs=no_surfs,
    )
    return bmedits2surf_execute(params, runner)

__all__ = [
    "Bmedits2surf",
    "Bmedits2surfOutputs",
    "BMEDITS2SURF_METADATA",
    "bmedits2surf_cargs",
    "bmedits2surf_outputs",
    "bmedits2surf_params",
    "bmedits2surf_execute",
    "bmedits2surf_validate",
    "bmedits2surf",
]