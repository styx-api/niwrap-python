# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GETFULLPATH_METADATA = Metadata(
    id="getfullpath",
    name="getfullpath",
    package="freesurfer",
)

Getfullpath = typing.TypedDict(
    "Getfullpath",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/getfullpath"]],
        "filename": str,
    },
)

@dataclasses.dataclass
class GetfullpathOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def getfullpath_params(
    filename: str,
) -> Getfullpath:
    """
    Build parameters.

    Args:
        filename: Filename for which to get the full path.

    Returns:
        Parameter dictionary.
    """
    params: Getfullpath = {
        "@type": "freesurfer/getfullpath",
        "filename": filename,
    }
    return params

def getfullpath_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Getfullpath."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename", None) is None:
        raise StyxValidationError("`filename` must not be None")
    if not isinstance(params["filename"], str):
        raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')

def getfullpath_cargs(params: Getfullpath, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("getfullpath")
    cargs.append(params["filename"])
    return cargs

def getfullpath_outputs(params: Getfullpath, execution: Execution) -> GetfullpathOutputs:
    """Build the GetfullpathOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GetfullpathOutputs(
        root=root_v,
    )

def getfullpath_execute(params: Getfullpath, runner: Runner | None = None) -> GetfullpathOutputs:
    """
    getfullpath

    A utility to retrieve the full path of a specified file or directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    getfullpath_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GETFULLPATH_METADATA)
    execution.params(params)
    args = getfullpath_cargs(params, execution)
    out = getfullpath_outputs(params, execution)
    execution.run(args)
    return out

def getfullpath(
    filename: str,
    runner: Runner | None = None,
) -> GetfullpathOutputs:
    """
    getfullpath

    A utility to retrieve the full path of a specified file or directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        filename: Filename for which to get the full path.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = getfullpath_params(
        filename=filename,
    )
    return getfullpath_execute(params, runner)

__all__ = [
    "Getfullpath",
    "GetfullpathOutputs",
    "GETFULLPATH_METADATA",
    "getfullpath_cargs",
    "getfullpath_outputs",
    "getfullpath_params",
    "getfullpath_execute",
    "getfullpath_validate",
    "getfullpath",
]