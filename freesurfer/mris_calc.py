# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CALC_METADATA = Metadata(
    id="mris_calc",
    name="mris_calc",
    package="freesurfer",
)

MrisCalc = typing.TypedDict(
    "MrisCalc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_calc"]],
        "input_file1": InputPathType,
        "action": str,
        "input_file2_or_float": typing.NotRequired[InputPathType],
        "output_file": typing.NotRequired[str],
        "label_file": typing.NotRequired[InputPathType],
        "verbosity": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisCalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_curv_file: OutputPathType | None
    """The resulting FreeSurfer curvature overlay or volume file."""

def mris_calc_params(
    input_file1: InputPathType,
    action: str,
    input_file2_or_float: InputPathType | None = None,
    output_file: str | None = None,
    label_file: InputPathType | None = None,
    verbosity: str | None = None,
) -> MrisCalc:
    """
    Build parameters.

    Args:
        input_file1: The name of a FreeSurfer curvature overlay (e.g., rh.curv) or\
            volume file (e.g., orig.mgz).
        action: Mathematical action to perform on the input file(s), written as a\
            text string.
        input_file2_or_float: The second input for the calculation. Can be a file\
            (e.g., rh.thickness) or a float number if the file does not exist.
        output_file: Specify the output file name for the result of the\
            calculation.
        label_file: Constrain the calculation to vertices defined in the FreeSurfer\
            label file.
        verbosity: Set the verbosity of the program.

    Returns:
        Parameter dictionary.
    """
    params: MrisCalc = {
        "@type": "freesurfer/mris_calc",
        "input_file1": input_file1,
        "action": action,
    }
    if input_file2_or_float is not None:
        params["input_file2_or_float"] = input_file2_or_float
    if output_file is not None:
        params["output_file"] = output_file
    if label_file is not None:
        params["label_file"] = label_file
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def mris_calc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file1", None) is None:
        raise StyxValidationError("`input_file1` must not be None")
    if not isinstance(params["input_file1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file1` has the wrong type: Received `{type(params["input_file1"])}` expected `InputPathType`')
    if params.get("action", None) is None:
        raise StyxValidationError("`action` must not be None")
    if not isinstance(params["action"], str):
        raise StyxValidationError(f'`action` has the wrong type: Received `{type(params["action"])}` expected `str`')
    if params.get("input_file2_or_float", None) is not None:
        if not isinstance(params["input_file2_or_float"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_file2_or_float` has the wrong type: Received `{type(params["input_file2_or_float"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], str):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `str`')

def mris_calc_cargs(params: MrisCalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_calc")
    cargs.append(execution.input_file(params["input_file1"]))
    cargs.append(params["action"])
    v_0 = params.get("input_file2_or_float")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("output_file")
    if v_1 is not None:
        cargs.append("--output")
        cargs.append(v_1)
    v_2 = params.get("label_file")
    if v_2 is not None:
        cargs.append("--label")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("verbosity")
    if v_3 is not None:
        cargs.append("--verbosity")
        cargs.append(v_3)
    return cargs

def mris_calc_outputs(params: MrisCalc, execution: Execution) -> MrisCalcOutputs:
    """Build the MrisCalcOutputs object for this tool."""
    output_curv_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_curv_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrisCalcOutputs(
        root=root_v,
        output_curv_file=output_curv_file_v,
    )

def mris_calc_execute(params: MrisCalc, runner: Runner | None = None) -> MrisCalcOutputs:
    """
    mris_calc

    Simple calculator that operates on FreeSurfer curvatures and volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_calc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CALC_METADATA)
    execution.params(params)
    args = mris_calc_cargs(params, execution)
    out = mris_calc_outputs(params, execution)
    execution.run(args)
    return out

def mris_calc(
    input_file1: InputPathType,
    action: str,
    input_file2_or_float: InputPathType | None = None,
    output_file: str | None = None,
    label_file: InputPathType | None = None,
    verbosity: str | None = None,
    runner: Runner | None = None,
) -> MrisCalcOutputs:
    """
    mris_calc

    Simple calculator that operates on FreeSurfer curvatures and volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file1: The name of a FreeSurfer curvature overlay (e.g., rh.curv) or\
            volume file (e.g., orig.mgz).
        action: Mathematical action to perform on the input file(s), written as a\
            text string.
        input_file2_or_float: The second input for the calculation. Can be a file\
            (e.g., rh.thickness) or a float number if the file does not exist.
        output_file: Specify the output file name for the result of the\
            calculation.
        label_file: Constrain the calculation to vertices defined in the FreeSurfer\
            label file.
        verbosity: Set the verbosity of the program.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_calc_params(
        input_file1=input_file1,
        action=action,
        input_file2_or_float=input_file2_or_float,
        output_file=output_file,
        label_file=label_file,
        verbosity=verbosity,
    )
    return mris_calc_execute(params, runner)

__all__ = [
    "MrisCalc",
    "MrisCalcOutputs",
    "MRIS_CALC_METADATA",
    "mris_calc_cargs",
    "mris_calc_outputs",
    "mris_calc_params",
    "mris_calc_execute",
    "mris_calc_validate",
    "mris_calc",
]