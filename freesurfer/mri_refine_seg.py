# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_REFINE_SEG_METADATA = Metadata(
    id="mri_refine_seg",
    name="mri_refine_seg",
    package="freesurfer",
)

MriRefineSeg = typing.TypedDict(
    "MriRefineSeg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_refine_seg"]],
        "input_segmentation": InputPathType,
        "output_segmentation": str,
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriRefineSegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    refined_output: OutputPathType
    """Refined segmentation output file."""

def mri_refine_seg_params(
    input_segmentation: InputPathType,
    output_segmentation: str,
    debug: bool = False,
) -> MriRefineSeg:
    """
    Build parameters.

    Args:
        input_segmentation: Input segmentation file.
        output_segmentation: Refined segmentation output name.
        debug: Save the original segmentation, a mask of the refined voxels, and a\
            pointset of the refined clusters to the output directory.

    Returns:
        Parameter dictionary.
    """
    params: MriRefineSeg = {
        "@type": "freesurfer/mri_refine_seg",
        "input_segmentation": input_segmentation,
        "output_segmentation": output_segmentation,
        "debug": debug,
    }
    return params

def mri_refine_seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRefineSeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_segmentation", None) is None:
        raise StyxValidationError("`input_segmentation` must not be None")
    if not isinstance(params["input_segmentation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_segmentation` has the wrong type: Received `{type(params["input_segmentation"])}` expected `InputPathType`')
    if params.get("output_segmentation", None) is None:
        raise StyxValidationError("`output_segmentation` must not be None")
    if not isinstance(params["output_segmentation"], str):
        raise StyxValidationError(f'`output_segmentation` has the wrong type: Received `{type(params["output_segmentation"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def mri_refine_seg_cargs(params: MriRefineSeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_refine_seg")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_segmentation"]))
    cargs.append("-o")
    cargs.append(params["output_segmentation"])
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def mri_refine_seg_outputs(params: MriRefineSeg, execution: Execution) -> MriRefineSegOutputs:
    """Build the MriRefineSegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    refined_output_v: OutputPathType = execution.output_file(params["output_segmentation"])
    return MriRefineSegOutputs(
        root=root_v,
        refined_output=refined_output_v,
    )

def mri_refine_seg_execute(params: MriRefineSeg, runner: Runner | None = None) -> MriRefineSegOutputs:
    """
    mri_refine_seg

    Refines a messy segmentation by recoding stray voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_refine_seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_REFINE_SEG_METADATA)
    execution.params(params)
    args = mri_refine_seg_cargs(params, execution)
    out = mri_refine_seg_outputs(params, execution)
    execution.run(args)
    return out

def mri_refine_seg(
    input_segmentation: InputPathType,
    output_segmentation: str,
    debug: bool = False,
    runner: Runner | None = None,
) -> MriRefineSegOutputs:
    """
    mri_refine_seg

    Refines a messy segmentation by recoding stray voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_segmentation: Input segmentation file.
        output_segmentation: Refined segmentation output name.
        debug: Save the original segmentation, a mask of the refined voxels, and a\
            pointset of the refined clusters to the output directory.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_refine_seg_params(
        input_segmentation=input_segmentation,
        output_segmentation=output_segmentation,
        debug=debug,
    )
    return mri_refine_seg_execute(params, runner)

__all__ = [
    "MriRefineSeg",
    "MriRefineSegOutputs",
    "MRI_REFINE_SEG_METADATA",
    "mri_refine_seg_cargs",
    "mri_refine_seg_outputs",
    "mri_refine_seg_params",
    "mri_refine_seg_execute",
    "mri_refine_seg_validate",
    "mri_refine_seg",
]