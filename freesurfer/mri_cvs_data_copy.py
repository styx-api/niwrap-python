# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CVS_DATA_COPY_METADATA = Metadata(
    id="mri_cvs_data_copy",
    name="mri_cvs_data_copy",
    package="freesurfer",
)

MriCvsDataCopy = typing.TypedDict(
    "MriCvsDataCopy",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cvs_data_copy"]],
        "subjid": str,
        "olddir": str,
        "newdir": str,
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCvsDataCopyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_cvs_data_copy_params(
    subjid: str,
    olddir: str,
    newdir: str,
    version: bool = False,
    help: bool = False,
) -> MriCvsDataCopy:
    """
    Build parameters.

    Args:
        subjid: Subject ID of the subject to be moved/registered.
        olddir: Directory where data (FS reconned output files for the subject) is\
            currently located. Use full path.
        newdir: Directory where data (FS reconned output files for the subject)\
            should be moved to. Use full path.
        version: Print version and exit.
        help: Print help and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriCvsDataCopy = {
        "@type": "freesurfer/mri_cvs_data_copy",
        "subjid": subjid,
        "olddir": olddir,
        "newdir": newdir,
        "version": version,
        "help": help,
    }
    return params

def mri_cvs_data_copy_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCvsDataCopy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjid", None) is None:
        raise StyxValidationError("`subjid` must not be None")
    if not isinstance(params["subjid"], str):
        raise StyxValidationError(f'`subjid` has the wrong type: Received `{type(params["subjid"])}` expected `str`')
    if params.get("olddir", None) is None:
        raise StyxValidationError("`olddir` must not be None")
    if not isinstance(params["olddir"], str):
        raise StyxValidationError(f'`olddir` has the wrong type: Received `{type(params["olddir"])}` expected `str`')
    if params.get("newdir", None) is None:
        raise StyxValidationError("`newdir` must not be None")
    if not isinstance(params["newdir"], str):
        raise StyxValidationError(f'`newdir` has the wrong type: Received `{type(params["newdir"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_cvs_data_copy_cargs(params: MriCvsDataCopy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cvs_data_copy")
    cargs.append("--subjid")
    cargs.append(params["subjid"])
    cargs.append("--olddir")
    cargs.append(params["olddir"])
    cargs.append("--newdir")
    cargs.append(params["newdir"])
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_cvs_data_copy_outputs(params: MriCvsDataCopy, execution: Execution) -> MriCvsDataCopyOutputs:
    """Build the MriCvsDataCopyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriCvsDataCopyOutputs(
        root=root_v,
    )

def mri_cvs_data_copy_execute(params: MriCvsDataCopy, runner: Runner | None = None) -> MriCvsDataCopyOutputs:
    """
    mri_cvs_data_copy

    Packs and copies files that are required for mri_cvs_register.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cvs_data_copy_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CVS_DATA_COPY_METADATA)
    execution.params(params)
    args = mri_cvs_data_copy_cargs(params, execution)
    out = mri_cvs_data_copy_outputs(params, execution)
    execution.run(args)
    return out

def mri_cvs_data_copy(
    subjid: str,
    olddir: str,
    newdir: str,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MriCvsDataCopyOutputs:
    """
    mri_cvs_data_copy

    Packs and copies files that are required for mri_cvs_register.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjid: Subject ID of the subject to be moved/registered.
        olddir: Directory where data (FS reconned output files for the subject) is\
            currently located. Use full path.
        newdir: Directory where data (FS reconned output files for the subject)\
            should be moved to. Use full path.
        version: Print version and exit.
        help: Print help and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cvs_data_copy_params(
        subjid=subjid,
        olddir=olddir,
        newdir=newdir,
        version=version,
        help=help,
    )
    return mri_cvs_data_copy_execute(params, runner)

__all__ = [
    "MriCvsDataCopy",
    "MriCvsDataCopyOutputs",
    "MRI_CVS_DATA_COPY_METADATA",
    "mri_cvs_data_copy_cargs",
    "mri_cvs_data_copy_outputs",
    "mri_cvs_data_copy_params",
    "mri_cvs_data_copy_execute",
    "mri_cvs_data_copy_validate",
    "mri_cvs_data_copy",
]