# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ISNIFTI_METADATA = Metadata(
    id="isnifti",
    name="isnifti",
    package="freesurfer",
)

Isnifti = typing.TypedDict(
    "Isnifti",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/isnifti"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class IsniftiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def isnifti_params(
    infile: InputPathType,
) -> Isnifti:
    """
    Build parameters.

    Args:
        infile: Input file to be checked if it is a NIfTI image

    Returns:
        Parameter dictionary.
    """
    params: Isnifti = {
        "@type": "freesurfer/isnifti",
        "infile": infile,
    }
    return params

def isnifti_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Isnifti."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def isnifti_cargs(params: Isnifti, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("isnifti")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def isnifti_outputs(params: Isnifti, execution: Execution) -> IsniftiOutputs:
    """Build the IsniftiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return IsniftiOutputs(
        root=root_v,
    )

def isnifti_execute(params: Isnifti, runner: Runner | None = None) -> IsniftiOutputs:
    """
    isnifti

    A simple tool to check if a file is a NIfTI image

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    isnifti_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ISNIFTI_METADATA)
    execution.params(params)
    args = isnifti_cargs(params, execution)
    out = isnifti_outputs(params, execution)
    execution.run(args)
    return out

def isnifti(
    infile: InputPathType,
    runner: Runner | None = None,
) -> IsniftiOutputs:
    """
    isnifti

    A simple tool to check if a file is a NIfTI image

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input file to be checked if it is a NIfTI image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = isnifti_params(
        infile=infile,
    )
    return isnifti_execute(params, runner)

__all__ = [
    "Isnifti",
    "IsniftiOutputs",
    "ISNIFTI_METADATA",
    "isnifti_cargs",
    "isnifti_outputs",
    "isnifti_params",
    "isnifti_execute",
    "isnifti_validate",
    "isnifti",
]