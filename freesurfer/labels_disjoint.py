# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABELS_DISJOINT_METADATA = Metadata(
    id="labels_disjoint",
    name="labels_disjoint",
    package="freesurfer",
)

LabelsDisjoint = typing.TypedDict(
    "LabelsDisjoint",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/labels_disjoint"]],
        "label1": InputPathType,
        "label2": InputPathType,
        "outputname": str,
    },
)

@dataclasses.dataclass
class LabelsDisjointOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Resulting output label file after subtraction."""

def labels_disjoint_params(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
) -> LabelsDisjoint:
    """
    Build parameters.

    Args:
        label1: First label file (e.g., rh.Occ.label).
        label2: Second label file to subtract from the first (e.g., rh.V1.label).
        outputname: Output label file name where the result will be saved (e.g.,\
            rh.Occ_V1.label).

    Returns:
        Parameter dictionary.
    """
    params: LabelsDisjoint = {
        "@type": "freesurfer/labels_disjoint",
        "label1": label1,
        "label2": label2,
        "outputname": outputname,
    }
    return params

def labels_disjoint_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelsDisjoint."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label1", None) is None:
        raise StyxValidationError("`label1` must not be None")
    if not isinstance(params["label1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label1` has the wrong type: Received `{type(params["label1"])}` expected `InputPathType`')
    if params.get("label2", None) is None:
        raise StyxValidationError("`label2` must not be None")
    if not isinstance(params["label2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label2` has the wrong type: Received `{type(params["label2"])}` expected `InputPathType`')
    if params.get("outputname", None) is None:
        raise StyxValidationError("`outputname` must not be None")
    if not isinstance(params["outputname"], str):
        raise StyxValidationError(f'`outputname` has the wrong type: Received `{type(params["outputname"])}` expected `str`')

def labels_disjoint_cargs(params: LabelsDisjoint, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("labels_disjoint")
    cargs.append(execution.input_file(params["label1"]))
    cargs.append(execution.input_file(params["label2"]))
    cargs.append(params["outputname"])
    return cargs

def labels_disjoint_outputs(params: LabelsDisjoint, execution: Execution) -> LabelsDisjointOutputs:
    """Build the LabelsDisjointOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["outputname"])
    return LabelsDisjointOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def labels_disjoint_execute(params: LabelsDisjoint, runner: Runner | None = None) -> LabelsDisjointOutputs:
    """
    labels_disjoint

    Subtracts one label file from another, effectively creating a label that represents label1 minus label2

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    labels_disjoint_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABELS_DISJOINT_METADATA)
    execution.params(params)
    args = labels_disjoint_cargs(params, execution)
    out = labels_disjoint_outputs(params, execution)
    execution.run(args)
    return out

def labels_disjoint(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
    runner: Runner | None = None,
) -> LabelsDisjointOutputs:
    """
    labels_disjoint

    Subtracts one label file from another, effectively creating a label that represents label1 minus label2

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label1: First label file (e.g., rh.Occ.label).
        label2: Second label file to subtract from the first (e.g., rh.V1.label).
        outputname: Output label file name where the result will be saved (e.g.,\
            rh.Occ_V1.label).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = labels_disjoint_params(
        label1=label1,
        label2=label2,
        outputname=outputname,
    )
    return labels_disjoint_execute(params, runner)

__all__ = [
    "LabelsDisjoint",
    "LabelsDisjointOutputs",
    "LABELS_DISJOINT_METADATA",
    "labels_disjoint_cargs",
    "labels_disjoint_outputs",
    "labels_disjoint_params",
    "labels_disjoint_execute",
    "labels_disjoint_validate",
    "labels_disjoint",
]