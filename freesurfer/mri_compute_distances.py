# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_DISTANCES_METADATA = Metadata(
    id="mri_compute_distances",
    name="mri_compute_distances",
    package="freesurfer",
)

MriComputeDistances = typing.TypedDict(
    "MriComputeDistances",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_distances"]],
        "source": InputPathType,
        "target": InputPathType,
        "output_xform": str,
    },
)

@dataclasses.dataclass
class MriComputeDistancesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xform_file: OutputPathType
    """Output transformation file"""

def mri_compute_distances_params(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
) -> MriComputeDistances:
    """
    Build parameters.

    Args:
        source: Input source image file
        target: Input target image file
        output_xform: Output transformation file containing the computed distances

    Returns:
        Parameter dictionary.
    """
    params: MriComputeDistances = {
        "@type": "freesurfer/mri_compute_distances",
        "source": source,
        "target": target,
        "output_xform": output_xform,
    }
    return params

def mri_compute_distances_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeDistances."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("output_xform", None) is None:
        raise StyxValidationError("`output_xform` must not be None")
    if not isinstance(params["output_xform"], str):
        raise StyxValidationError(f'`output_xform` has the wrong type: Received `{type(params["output_xform"])}` expected `str`')

def mri_compute_distances_cargs(params: MriComputeDistances, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_distances")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["target"]))
    cargs.append(params["output_xform"])
    return cargs

def mri_compute_distances_outputs(params: MriComputeDistances, execution: Execution) -> MriComputeDistancesOutputs:
    """Build the MriComputeDistancesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xform_file_v: OutputPathType = execution.output_file(params["output_xform"])
    return MriComputeDistancesOutputs(
        root=root_v,
        output_xform_file=output_xform_file_v,
    )

def mri_compute_distances_execute(params: MriComputeDistances, runner: Runner | None = None) -> MriComputeDistancesOutputs:
    """
    mri_compute_distances

    A tool to compute distances between source and target images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_distances_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_DISTANCES_METADATA)
    execution.params(params)
    args = mri_compute_distances_cargs(params, execution)
    out = mri_compute_distances_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_distances(
    source: InputPathType,
    target: InputPathType,
    output_xform: str,
    runner: Runner | None = None,
) -> MriComputeDistancesOutputs:
    """
    mri_compute_distances

    A tool to compute distances between source and target images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source: Input source image file
        target: Input target image file
        output_xform: Output transformation file containing the computed distances
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_distances_params(
        source=source,
        target=target,
        output_xform=output_xform,
    )
    return mri_compute_distances_execute(params, runner)

__all__ = [
    "MriComputeDistances",
    "MriComputeDistancesOutputs",
    "MRI_COMPUTE_DISTANCES_METADATA",
    "mri_compute_distances_cargs",
    "mri_compute_distances_outputs",
    "mri_compute_distances_params",
    "mri_compute_distances_execute",
    "mri_compute_distances_validate",
    "mri_compute_distances",
]