# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REMOVE_TALAIRACH_METADATA = Metadata(
    id="remove_talairach",
    name="remove_talairach",
    package="freesurfer",
)

RemoveTalairach = typing.TypedDict(
    "RemoveTalairach",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/remove_talairach"]],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class RemoveTalairachOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Output volume with Talairach transformation removed"""

def remove_talairach_params(
    input_file: InputPathType,
    output_file: str,
) -> RemoveTalairach:
    """
    Build parameters.

    Args:
        input_file: Input volume with Talairach transformation (e.g., volume.nii)
        output_file: Output volume after removing Talairach transformation (e.g.,\
            volume_notal.nii)

    Returns:
        Parameter dictionary.
    """
    params: RemoveTalairach = {
        "@type": "freesurfer/remove_talairach",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def remove_talairach_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RemoveTalairach."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def remove_talairach_cargs(params: RemoveTalairach, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("remove_talairach")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def remove_talairach_outputs(params: RemoveTalairach, execution: Execution) -> RemoveTalairachOutputs:
    """Build the RemoveTalairachOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["output_file"])
    return RemoveTalairachOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def remove_talairach_execute(params: RemoveTalairach, runner: Runner | None = None) -> RemoveTalairachOutputs:
    """
    remove_talairach

    A tool for removing the Talairach transformation from a volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    remove_talairach_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REMOVE_TALAIRACH_METADATA)
    execution.params(params)
    args = remove_talairach_cargs(params, execution)
    out = remove_talairach_outputs(params, execution)
    execution.run(args)
    return out

def remove_talairach(
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> RemoveTalairachOutputs:
    """
    remove_talairach

    A tool for removing the Talairach transformation from a volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input volume with Talairach transformation (e.g., volume.nii)
        output_file: Output volume after removing Talairach transformation (e.g.,\
            volume_notal.nii)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = remove_talairach_params(
        input_file=input_file,
        output_file=output_file,
    )
    return remove_talairach_execute(params, runner)

__all__ = [
    "RemoveTalairach",
    "RemoveTalairachOutputs",
    "REMOVE_TALAIRACH_METADATA",
    "remove_talairach_cargs",
    "remove_talairach_outputs",
    "remove_talairach_params",
    "remove_talairach_execute",
    "remove_talairach_validate",
    "remove_talairach",
]