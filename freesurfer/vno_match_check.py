# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VNO_MATCH_CHECK_METADATA = Metadata(
    id="vno_match_check",
    name="vno_match_check",
    package="freesurfer",
)

VnoMatchCheck = typing.TypedDict(
    "VnoMatchCheck",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vno_match_check"]],
        "subjid": str,
        "debug": typing.NotRequired[bool],
        "right_hemi": typing.NotRequired[bool],
        "left_hemi": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VnoMatchCheckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def vno_match_check_params(
    subjid: str,
    debug: bool = False,
    right_hemi: bool = False,
    left_hemi: bool = False,
) -> VnoMatchCheck:
    """
    Build parameters.

    Args:
        subjid: Subject ID for which the vertex number check is performed
        debug: Prints progress text
        right_hemi: Checks only the right hemisphere
        left_hemi: Checks only the left hemisphere

    Returns:
        Parameter dictionary.
    """
    params: VnoMatchCheck = {
        "@type": "freesurfer/vno_match_check",
        "subjid": subjid,
        "debug": debug,
        "right_hemi": right_hemi,
        "left_hemi": left_hemi,
    }
    return params

def vno_match_check_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VnoMatchCheck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjid", None) is None:
        raise StyxValidationError("`subjid` must not be None")
    if not isinstance(params["subjid"], str):
        raise StyxValidationError(f'`subjid` has the wrong type: Received `{type(params["subjid"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("right_hemi", None) is not None:
        if not isinstance(params["right_hemi"], bool):
            raise StyxValidationError(f'`right_hemi` has the wrong type: Received `{type(params["right_hemi"])}` expected `bool`')
    if params.get("left_hemi", None) is not None:
        if not isinstance(params["left_hemi"], bool):
            raise StyxValidationError(f'`left_hemi` has the wrong type: Received `{type(params["left_hemi"])}` expected `bool`')

def vno_match_check_cargs(params: VnoMatchCheck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vno_match_check")
    cargs.append(params["subjid"])
    if params.get("debug"):
        cargs.append("debug")
    if params.get("right_hemi"):
        cargs.append("rh")
    if params.get("left_hemi"):
        cargs.append("lh")
    return cargs

def vno_match_check_outputs(params: VnoMatchCheck, execution: Execution) -> VnoMatchCheckOutputs:
    """Build the VnoMatchCheckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VnoMatchCheckOutputs(
        root=root_v,
    )

def vno_match_check_execute(params: VnoMatchCheck, runner: Runner | None = None) -> VnoMatchCheckOutputs:
    """
    vno_match_check

    Checks that all surfaces and surface data files for a subject have the same number of vertices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vno_match_check_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VNO_MATCH_CHECK_METADATA)
    execution.params(params)
    args = vno_match_check_cargs(params, execution)
    out = vno_match_check_outputs(params, execution)
    execution.run(args)
    return out

def vno_match_check(
    subjid: str,
    debug: bool = False,
    right_hemi: bool = False,
    left_hemi: bool = False,
    runner: Runner | None = None,
) -> VnoMatchCheckOutputs:
    """
    vno_match_check

    Checks that all surfaces and surface data files for a subject have the same number of vertices.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjid: Subject ID for which the vertex number check is performed
        debug: Prints progress text
        right_hemi: Checks only the right hemisphere
        left_hemi: Checks only the left hemisphere
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vno_match_check_params(
        subjid=subjid,
        debug=debug,
        right_hemi=right_hemi,
        left_hemi=left_hemi,
    )
    return vno_match_check_execute(params, runner)

__all__ = [
    "VnoMatchCheck",
    "VnoMatchCheckOutputs",
    "VNO_MATCH_CHECK_METADATA",
    "vno_match_check_cargs",
    "vno_match_check_outputs",
    "vno_match_check_params",
    "vno_match_check_execute",
    "vno_match_check_validate",
    "vno_match_check",
]