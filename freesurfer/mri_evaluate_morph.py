# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EVALUATE_MORPH_METADATA = Metadata(
    id="mri_evaluate_morph",
    name="mri_evaluate_morph",
    package="freesurfer",
)

MriEvaluateMorph = typing.TypedDict(
    "MriEvaluateMorph",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_evaluate_morph"]],
        "xform_name": InputPathType,
        "segmentation_files": list[InputPathType],
        "output_file": str,
    },
)

@dataclasses.dataclass
class MriEvaluateMorphOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_overlap_file: OutputPathType
    """The output file containing overlap results."""

def mri_evaluate_morph_params(
    xform_name: InputPathType,
    segmentation_files: list[InputPathType],
    output_file: str,
) -> MriEvaluateMorph:
    """
    Build parameters.

    Args:
        xform_name: Transformation file name.
        segmentation_files: Input segmentation files.
        output_file: Output file.

    Returns:
        Parameter dictionary.
    """
    params: MriEvaluateMorph = {
        "@type": "freesurfer/mri_evaluate_morph",
        "xform_name": xform_name,
        "segmentation_files": segmentation_files,
        "output_file": output_file,
    }
    return params

def mri_evaluate_morph_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriEvaluateMorph."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("xform_name", None) is None:
        raise StyxValidationError("`xform_name` must not be None")
    if not isinstance(params["xform_name"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xform_name` has the wrong type: Received `{type(params["xform_name"])}` expected `InputPathType`')
    if params.get("segmentation_files", None) is None:
        raise StyxValidationError("`segmentation_files` must not be None")
    if not isinstance(params["segmentation_files"], list):
        raise StyxValidationError(f'`segmentation_files` has the wrong type: Received `{type(params["segmentation_files"])}` expected `list[InputPathType]`')
    for e in params["segmentation_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`segmentation_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_evaluate_morph_cargs(params: MriEvaluateMorph, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_evaluate_morph")
    cargs.append(execution.input_file(params["xform_name"]))
    for item0 in params["segmentation_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_file"])
    return cargs

def mri_evaluate_morph_outputs(params: MriEvaluateMorph, execution: Execution) -> MriEvaluateMorphOutputs:
    """Build the MriEvaluateMorphOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_overlap_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MriEvaluateMorphOutputs(
        root=root_v,
        output_overlap_file=output_overlap_file_v,
    )

def mri_evaluate_morph_execute(params: MriEvaluateMorph, runner: Runner | None = None) -> MriEvaluateMorphOutputs:
    """
    mri_evaluate_morph

    This program computes the overlap of a set of segmentations for a given morph using an xform file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_evaluate_morph_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EVALUATE_MORPH_METADATA)
    execution.params(params)
    args = mri_evaluate_morph_cargs(params, execution)
    out = mri_evaluate_morph_outputs(params, execution)
    execution.run(args)
    return out

def mri_evaluate_morph(
    xform_name: InputPathType,
    segmentation_files: list[InputPathType],
    output_file: str,
    runner: Runner | None = None,
) -> MriEvaluateMorphOutputs:
    """
    mri_evaluate_morph

    This program computes the overlap of a set of segmentations for a given morph using an xform file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        xform_name: Transformation file name.
        segmentation_files: Input segmentation files.
        output_file: Output file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_evaluate_morph_params(
        xform_name=xform_name,
        segmentation_files=segmentation_files,
        output_file=output_file,
    )
    return mri_evaluate_morph_execute(params, runner)

__all__ = [
    "MriEvaluateMorph",
    "MriEvaluateMorphOutputs",
    "MRI_EVALUATE_MORPH_METADATA",
    "mri_evaluate_morph_cargs",
    "mri_evaluate_morph_outputs",
    "mri_evaluate_morph_params",
    "mri_evaluate_morph_execute",
    "mri_evaluate_morph_validate",
    "mri_evaluate_morph",
]