# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FNAME2EXT_METADATA = Metadata(
    id="fname2ext",
    name="fname2ext",
    package="freesurfer",
)

Fname2ext = typing.TypedDict(
    "Fname2ext",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fname2ext"]],
        "filename": str,
    },
)

@dataclasses.dataclass
class Fname2extOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    file_extension: OutputPathType
    """Extracted file extension"""

def fname2ext_params(
    filename: str,
) -> Fname2ext:
    """
    Build parameters.

    Args:
        filename: The name of the file for which to extract the extension. The file\
            does not need to exist.

    Returns:
        Parameter dictionary.
    """
    params: Fname2ext = {
        "@type": "freesurfer/fname2ext",
        "filename": filename,
    }
    return params

def fname2ext_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fname2ext."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename", None) is None:
        raise StyxValidationError("`filename` must not be None")
    if not isinstance(params["filename"], str):
        raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')

def fname2ext_cargs(params: Fname2ext, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fname2ext")
    cargs.append(params["filename"])
    return cargs

def fname2ext_outputs(params: Fname2ext, execution: Execution) -> Fname2extOutputs:
    """Build the Fname2extOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    file_extension_v: OutputPathType = execution.output_file("extension.txt")
    return Fname2extOutputs(
        root=root_v,
        file_extension=file_extension_v,
    )

def fname2ext_execute(params: Fname2ext, runner: Runner | None = None) -> Fname2extOutputs:
    """
    fname2ext

    Converts the name of a file to its extension.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fname2ext_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FNAME2EXT_METADATA)
    execution.params(params)
    args = fname2ext_cargs(params, execution)
    out = fname2ext_outputs(params, execution)
    execution.run(args)
    return out

def fname2ext(
    filename: str,
    runner: Runner | None = None,
) -> Fname2extOutputs:
    """
    fname2ext

    Converts the name of a file to its extension.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        filename: The name of the file for which to extract the extension. The file\
            does not need to exist.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fname2ext_params(
        filename=filename,
    )
    return fname2ext_execute(params, runner)

__all__ = [
    "Fname2ext",
    "Fname2extOutputs",
    "FNAME2EXT_METADATA",
    "fname2ext_cargs",
    "fname2ext_outputs",
    "fname2ext_params",
    "fname2ext_execute",
    "fname2ext_validate",
    "fname2ext",
]