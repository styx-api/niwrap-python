# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CSVPRINT_METADATA = Metadata(
    id="csvprint",
    name="csvprint",
    package="freesurfer",
)

Csvprint = typing.TypedDict(
    "Csvprint",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/csvprint"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class CsvprintOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def csvprint_params(
    infile: InputPathType,
) -> Csvprint:
    """
    Build parameters.

    Args:
        infile: Input CSV file to be printed

    Returns:
        Parameter dictionary.
    """
    params: Csvprint = {
        "@type": "freesurfer/csvprint",
        "infile": infile,
    }
    return params

def csvprint_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Csvprint."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def csvprint_cargs(params: Csvprint, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("csvprint")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def csvprint_outputs(params: Csvprint, execution: Execution) -> CsvprintOutputs:
    """Build the CsvprintOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CsvprintOutputs(
        root=root_v,
    )

def csvprint_execute(params: Csvprint, runner: Runner | None = None) -> CsvprintOutputs:
    """
    csvprint

    Command-line tool for printing CSV files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    csvprint_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CSVPRINT_METADATA)
    execution.params(params)
    args = csvprint_cargs(params, execution)
    out = csvprint_outputs(params, execution)
    execution.run(args)
    return out

def csvprint(
    infile: InputPathType,
    runner: Runner | None = None,
) -> CsvprintOutputs:
    """
    csvprint

    Command-line tool for printing CSV files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input CSV file to be printed
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = csvprint_params(
        infile=infile,
    )
    return csvprint_execute(params, runner)

__all__ = [
    "Csvprint",
    "CsvprintOutputs",
    "CSVPRINT_METADATA",
    "csvprint_cargs",
    "csvprint_outputs",
    "csvprint_params",
    "csvprint_execute",
    "csvprint_validate",
    "csvprint",
]