# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COMPUTE_LABEL_VOLUMES_CSH_METADATA = Metadata(
    id="compute_label_volumes.csh",
    name="compute_label_volumes.csh",
    package="freesurfer",
)

class ComputeLabelVolumesCshLabelL(typing.TypedDict):
    upper_L: typing.NotRequired[str]
    """The particular label to be analyzed (case-sensitive option)"""
    lower_L: typing.NotRequired[str]
    """The particular label to be analyzed (case-insensitive option)"""

ComputeLabelVolumesCsh = typing.TypedDict(
    "ComputeLabelVolumesCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/compute_label_volumes.csh"]],
        "label_vol": InputPathType,
        "output_file": str,
        "label_L": typing.NotRequired[ComputeLabelVolumesCshLabelL],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ComputeLabelVolumesCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_file: OutputPathType
    """Text file with the computed number of voxels and volumes"""

def compute_label_volumes_csh_params(
    label_vol: InputPathType,
    output_file: str,
    label_L: ComputeLabelVolumesCshLabelL | None = None,
    version: bool = False,
    help: bool = False,
) -> ComputeLabelVolumesCsh:
    """
    Build parameters.

    Args:
        label_vol: Label volume to be analyzed
        output_file: Text file where the results are written
        label_L: The particular label to be analyzed
        version: Print version and exit
        help: Print help and exit

    Returns:
        Parameter dictionary.
    """
    params: ComputeLabelVolumesCsh = {
        "@type": "freesurfer/compute_label_volumes.csh",
        "label_vol": label_vol,
        "output_file": output_file,
        "version": version,
        "help": help,
    }
    if label_L is not None:
        params["label_L"] = label_L
    return params

def compute_label_volumes_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ComputeLabelVolumesCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_vol", None) is None:
        raise StyxValidationError("`label_vol` must not be None")
    if not isinstance(params["label_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_vol` has the wrong type: Received `{type(params["label_vol"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("label_L", None) is not None:
        if not isinstance(params["label_L"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["label_L"])}\'')
        if params["label_L"].get("upper_L", None) is not None:
            if not isinstance(params["label_L"]["upper_L"], str):
                raise StyxValidationError(f'`upper_L` has the wrong type: Received `{type(params["label_L"]["upper_L"])}` expected `str`')
        if params["label_L"].get("lower_L", None) is not None:
            if not isinstance(params["label_L"]["lower_L"], str):
                raise StyxValidationError(f'`lower_L` has the wrong type: Received `{type(params["label_L"]["lower_L"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def compute_label_volumes_csh_cargs(params: ComputeLabelVolumesCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("compute_label_volumes.csh")
    cargs.append("--vol")
    cargs.append(execution.input_file(params["label_vol"]))
    cargs.append("--out")
    cargs.append(params["output_file"])
    v_0 = params.get("label_L")
    if v_0 is not None:
        v_1 = v_0.get("upper_L")
        if v_1 is not None:
            cargs.append("--L")
            cargs.append(v_1)
        v_2 = v_0.get("lower_L")
        if v_2 is not None:
            cargs.append("--l")
            cargs.append(v_2)
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def compute_label_volumes_csh_outputs(params: ComputeLabelVolumesCsh, execution: Execution) -> ComputeLabelVolumesCshOutputs:
    """Build the ComputeLabelVolumesCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_file_v: OutputPathType = execution.output_file(params["output_file"])
    return ComputeLabelVolumesCshOutputs(
        root=root_v,
        result_file=result_file_v,
    )

def compute_label_volumes_csh_execute(params: ComputeLabelVolumesCsh, runner: Runner | None = None) -> ComputeLabelVolumesCshOutputs:
    """
    compute_label_volumes.csh

    Computes the number of voxels and the volumes of either all or a particular label in the input label volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    compute_label_volumes_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COMPUTE_LABEL_VOLUMES_CSH_METADATA)
    execution.params(params)
    args = compute_label_volumes_csh_cargs(params, execution)
    out = compute_label_volumes_csh_outputs(params, execution)
    execution.run(args)
    return out

def compute_label_volumes_csh(
    label_vol: InputPathType,
    output_file: str,
    label_L: ComputeLabelVolumesCshLabelL | None = None,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> ComputeLabelVolumesCshOutputs:
    """
    compute_label_volumes.csh

    Computes the number of voxels and the volumes of either all or a particular label in the input label volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label_vol: Label volume to be analyzed
        output_file: Text file where the results are written
        label_L: The particular label to be analyzed
        version: Print version and exit
        help: Print help and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = compute_label_volumes_csh_params(
        label_vol=label_vol,
        output_file=output_file,
        label_L=label_L,
        version=version,
        help=help,
    )
    return compute_label_volumes_csh_execute(params, runner)

__all__ = [
    "ComputeLabelVolumesCsh",
    "ComputeLabelVolumesCshOutputs",
    "COMPUTE_LABEL_VOLUMES_CSH_METADATA",
    "compute_label_volumes_csh_cargs",
    "compute_label_volumes_csh_outputs",
    "compute_label_volumes_csh_params",
    "compute_label_volumes_csh_execute",
    "compute_label_volumes_csh_validate",
    "compute_label_volumes_csh",
]