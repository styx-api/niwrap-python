# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LABEL_VOLUME_METADATA = Metadata(
    id="mri_label_volume",
    name="mri_label_volume",
    package="freesurfer",
)

MriLabelVolume = typing.TypedDict(
    "MriLabelVolume",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_label_volume"]],
        "volume": InputPathType,
        "labels": list[str],
        "partial_volume_effects": typing.NotRequired[InputPathType],
        "intracranial_volume": typing.NotRequired[InputPathType],
        "spreadsheet_subject": typing.NotRequired[str],
        "non_zero_voxels": typing.NotRequired[bool],
        "replace_label_in": typing.NotRequired[str],
        "replace_label_out": typing.NotRequired[str],
        "brain_volume": typing.NotRequired[InputPathType],
        "percentage": typing.NotRequired[bool],
        "log_results": typing.NotRequired[InputPathType],
        "atlas_transform_file": typing.NotRequired[InputPathType],
        "atlas_scalefactor": typing.NotRequired[float],
        "etiv_transform_file": typing.NotRequired[InputPathType],
        "etiv_scalefactor": typing.NotRequired[float],
        "etiv_subject": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriLabelVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_results: OutputPathType
    """Computed volume results."""
    output_log_results: OutputPathType
    """Log file if -l option is used."""

def mri_label_volume_params(
    volume: InputPathType,
    labels: list[str],
    partial_volume_effects: InputPathType | None = None,
    intracranial_volume: InputPathType | None = None,
    spreadsheet_subject: str | None = None,
    non_zero_voxels: bool = False,
    replace_label_in: str | None = None,
    replace_label_out: str | None = None,
    brain_volume: InputPathType | None = None,
    percentage: bool = False,
    log_results: InputPathType | None = None,
    atlas_transform_file: InputPathType | None = None,
    atlas_scalefactor: float | None = None,
    etiv_transform_file: InputPathType | None = None,
    etiv_scalefactor: float | None = None,
    etiv_subject: str | None = None,
) -> MriLabelVolume:
    """
    Build parameters.

    Args:
        volume: Volume file (e.g., volume.mgz) to analyze
        labels: Labels to compute volume for
        partial_volume_effects: Compute partial volume effects using intensity\
            volume
        intracranial_volume: Normalize by the intracranial volume in provided file
        spreadsheet_subject: Output in spreadsheet mode, including subject name
        non_zero_voxels: Compute volume of all non-zero voxels (e.g. for computing\
            brain volume)
        replace_label_in: Replace label <in> with label <out>.
        replace_label_out: Label to replace with
        brain_volume: Compute the brain volume from provided brain volume file and\
            normalize by it
        percentage: Compute volume as a percentage of all non-zero labels
        log_results: Log results to provided file
        atlas_transform_file: Specify LTA or XFM atlas transform file and scale\
            factor to use for ICV correction
        atlas_scalefactor: Scale factor for ICV correction
        etiv_transform_file: Same as -atlas_icv
        etiv_scalefactor: eTIV scale factor
        etiv_subject: Same as -eTIV, and generate MATLAB data appending subject to\
            structure

    Returns:
        Parameter dictionary.
    """
    params: MriLabelVolume = {
        "@type": "freesurfer/mri_label_volume",
        "volume": volume,
        "labels": labels,
        "non_zero_voxels": non_zero_voxels,
        "percentage": percentage,
    }
    if partial_volume_effects is not None:
        params["partial_volume_effects"] = partial_volume_effects
    if intracranial_volume is not None:
        params["intracranial_volume"] = intracranial_volume
    if spreadsheet_subject is not None:
        params["spreadsheet_subject"] = spreadsheet_subject
    if replace_label_in is not None:
        params["replace_label_in"] = replace_label_in
    if replace_label_out is not None:
        params["replace_label_out"] = replace_label_out
    if brain_volume is not None:
        params["brain_volume"] = brain_volume
    if log_results is not None:
        params["log_results"] = log_results
    if atlas_transform_file is not None:
        params["atlas_transform_file"] = atlas_transform_file
    if atlas_scalefactor is not None:
        params["atlas_scalefactor"] = atlas_scalefactor
    if etiv_transform_file is not None:
        params["etiv_transform_file"] = etiv_transform_file
    if etiv_scalefactor is not None:
        params["etiv_scalefactor"] = etiv_scalefactor
    if etiv_subject is not None:
        params["etiv_subject"] = etiv_subject
    return params

def mri_label_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLabelVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("partial_volume_effects", None) is not None:
        if not isinstance(params["partial_volume_effects"], (pathlib.Path, str)):
            raise StyxValidationError(f'`partial_volume_effects` has the wrong type: Received `{type(params["partial_volume_effects"])}` expected `InputPathType`')
    if params.get("intracranial_volume", None) is not None:
        if not isinstance(params["intracranial_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`intracranial_volume` has the wrong type: Received `{type(params["intracranial_volume"])}` expected `InputPathType`')
    if params.get("spreadsheet_subject", None) is not None:
        if not isinstance(params["spreadsheet_subject"], str):
            raise StyxValidationError(f'`spreadsheet_subject` has the wrong type: Received `{type(params["spreadsheet_subject"])}` expected `str`')
    if params.get("non_zero_voxels", None) is not None:
        if not isinstance(params["non_zero_voxels"], bool):
            raise StyxValidationError(f'`non_zero_voxels` has the wrong type: Received `{type(params["non_zero_voxels"])}` expected `bool`')
    if params.get("replace_label_in", None) is not None:
        if not isinstance(params["replace_label_in"], str):
            raise StyxValidationError(f'`replace_label_in` has the wrong type: Received `{type(params["replace_label_in"])}` expected `str`')
    if params.get("replace_label_out", None) is not None:
        if not isinstance(params["replace_label_out"], str):
            raise StyxValidationError(f'`replace_label_out` has the wrong type: Received `{type(params["replace_label_out"])}` expected `str`')
    if params.get("brain_volume", None) is not None:
        if not isinstance(params["brain_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`brain_volume` has the wrong type: Received `{type(params["brain_volume"])}` expected `InputPathType`')
    if params.get("percentage", None) is not None:
        if not isinstance(params["percentage"], bool):
            raise StyxValidationError(f'`percentage` has the wrong type: Received `{type(params["percentage"])}` expected `bool`')
    if params.get("log_results", None) is not None:
        if not isinstance(params["log_results"], (pathlib.Path, str)):
            raise StyxValidationError(f'`log_results` has the wrong type: Received `{type(params["log_results"])}` expected `InputPathType`')
    if params.get("atlas_transform_file", None) is not None:
        if not isinstance(params["atlas_transform_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`atlas_transform_file` has the wrong type: Received `{type(params["atlas_transform_file"])}` expected `InputPathType`')
    if params.get("atlas_scalefactor", None) is not None:
        if not isinstance(params["atlas_scalefactor"], (float, int)):
            raise StyxValidationError(f'`atlas_scalefactor` has the wrong type: Received `{type(params["atlas_scalefactor"])}` expected `float`')
    if params.get("etiv_transform_file", None) is not None:
        if not isinstance(params["etiv_transform_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`etiv_transform_file` has the wrong type: Received `{type(params["etiv_transform_file"])}` expected `InputPathType`')
    if params.get("etiv_scalefactor", None) is not None:
        if not isinstance(params["etiv_scalefactor"], (float, int)):
            raise StyxValidationError(f'`etiv_scalefactor` has the wrong type: Received `{type(params["etiv_scalefactor"])}` expected `float`')
    if params.get("etiv_subject", None) is not None:
        if not isinstance(params["etiv_subject"], str):
            raise StyxValidationError(f'`etiv_subject` has the wrong type: Received `{type(params["etiv_subject"])}` expected `str`')

def mri_label_volume_cargs(params: MriLabelVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_label_volume")
    cargs.append(execution.input_file(params["volume"]))
    for item0 in params["labels"]:
        cargs.append(item0)
    v_0 = params.get("partial_volume_effects")
    if v_0 is not None:
        cargs.append("-pv")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("intracranial_volume")
    if v_1 is not None:
        cargs.append("-icv")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("spreadsheet_subject")
    if v_2 is not None:
        cargs.append("-s")
        cargs.append(v_2)
    if params.get("non_zero_voxels"):
        cargs.append("-a")
    v_3 = params.get("replace_label_in")
    if v_3 is not None:
        cargs.append("-t")
        cargs.append(v_3)
    v_4 = params.get("replace_label_out")
    if v_4 is not None:
        cargs.append(v_4)
    v_5 = params.get("brain_volume")
    if v_5 is not None:
        cargs.append("-b")
        cargs.append(execution.input_file(v_5))
    if params.get("percentage"):
        cargs.append("-p")
    v_6 = params.get("log_results")
    if v_6 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("atlas_transform_file")
    if v_7 is not None:
        cargs.append("-atlas_icv")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("atlas_scalefactor")
    if v_8 is not None:
        cargs.append(str(v_8))
    v_9 = params.get("etiv_transform_file")
    if v_9 is not None:
        cargs.append("-eTIV")
        cargs.append(execution.input_file(v_9))
    v_10 = params.get("etiv_scalefactor")
    if v_10 is not None:
        cargs.append(str(v_10))
    v_11 = params.get("etiv_subject")
    if v_11 is not None:
        cargs.append("-eTIV_matdat")
        cargs.append(v_11)
    return cargs

def mri_label_volume_outputs(params: MriLabelVolume, execution: Execution) -> MriLabelVolumeOutputs:
    """Build the MriLabelVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_results_v: OutputPathType = execution.output_file("volume_label_results.txt")
    output_log_results_v: OutputPathType = execution.output_file("volume_log.txt")
    return MriLabelVolumeOutputs(
        root=root_v,
        output_volume_results=output_volume_results_v,
        output_log_results=output_log_results_v,
    )

def mri_label_volume_execute(params: MriLabelVolume, runner: Runner | None = None) -> MriLabelVolumeOutputs:
    """
    mri_label_volume

    A tool to compute volumes of labeled voxels within MRI images, often used in conjunction with FreeSurfer processed data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_label_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LABEL_VOLUME_METADATA)
    execution.params(params)
    args = mri_label_volume_cargs(params, execution)
    out = mri_label_volume_outputs(params, execution)
    execution.run(args)
    return out

def mri_label_volume(
    volume: InputPathType,
    labels: list[str],
    partial_volume_effects: InputPathType | None = None,
    intracranial_volume: InputPathType | None = None,
    spreadsheet_subject: str | None = None,
    non_zero_voxels: bool = False,
    replace_label_in: str | None = None,
    replace_label_out: str | None = None,
    brain_volume: InputPathType | None = None,
    percentage: bool = False,
    log_results: InputPathType | None = None,
    atlas_transform_file: InputPathType | None = None,
    atlas_scalefactor: float | None = None,
    etiv_transform_file: InputPathType | None = None,
    etiv_scalefactor: float | None = None,
    etiv_subject: str | None = None,
    runner: Runner | None = None,
) -> MriLabelVolumeOutputs:
    """
    mri_label_volume

    A tool to compute volumes of labeled voxels within MRI images, often used in conjunction with FreeSurfer processed data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume: Volume file (e.g., volume.mgz) to analyze
        labels: Labels to compute volume for
        partial_volume_effects: Compute partial volume effects using intensity\
            volume
        intracranial_volume: Normalize by the intracranial volume in provided file
        spreadsheet_subject: Output in spreadsheet mode, including subject name
        non_zero_voxels: Compute volume of all non-zero voxels (e.g. for computing\
            brain volume)
        replace_label_in: Replace label <in> with label <out>.
        replace_label_out: Label to replace with
        brain_volume: Compute the brain volume from provided brain volume file and\
            normalize by it
        percentage: Compute volume as a percentage of all non-zero labels
        log_results: Log results to provided file
        atlas_transform_file: Specify LTA or XFM atlas transform file and scale\
            factor to use for ICV correction
        atlas_scalefactor: Scale factor for ICV correction
        etiv_transform_file: Same as -atlas_icv
        etiv_scalefactor: eTIV scale factor
        etiv_subject: Same as -eTIV, and generate MATLAB data appending subject to\
            structure
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_label_volume_params(
        volume=volume,
        labels=labels,
        partial_volume_effects=partial_volume_effects,
        intracranial_volume=intracranial_volume,
        spreadsheet_subject=spreadsheet_subject,
        non_zero_voxels=non_zero_voxels,
        replace_label_in=replace_label_in,
        replace_label_out=replace_label_out,
        brain_volume=brain_volume,
        percentage=percentage,
        log_results=log_results,
        atlas_transform_file=atlas_transform_file,
        atlas_scalefactor=atlas_scalefactor,
        etiv_transform_file=etiv_transform_file,
        etiv_scalefactor=etiv_scalefactor,
        etiv_subject=etiv_subject,
    )
    return mri_label_volume_execute(params, runner)

__all__ = [
    "MriLabelVolume",
    "MriLabelVolumeOutputs",
    "MRI_LABEL_VOLUME_METADATA",
    "mri_label_volume_cargs",
    "mri_label_volume_outputs",
    "mri_label_volume_params",
    "mri_label_volume_execute",
    "mri_label_volume_validate",
    "mri_label_volume",
]