# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOL2SYMSURF_METADATA = Metadata(
    id="vol2symsurf",
    name="vol2symsurf",
    package="freesurfer",
)

Vol2symsurf = typing.TypedDict(
    "Vol2symsurf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vol2symsurf"]],
        "registration_file": InputPathType,
        "input_volume": InputPathType,
        "fwhm": float,
        "output_stem": typing.NotRequired[str],
        "regheader": typing.NotRequired[str],
        "projection_fraction": typing.NotRequired[float],
        "no_diff": typing.NotRequired[bool],
        "laterality_index": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Vol2symsurfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_lh: OutputPathType | None
    """Output for left hemisphere"""
    output_rh: OutputPathType | None
    """Output for right hemisphere"""
    output_lh_rh_difference: OutputPathType | None
    """Output for left-right hemisphere difference"""
    output_li_difference: OutputPathType | None
    """Output for laterality index"""

def vol2symsurf_params(
    registration_file: InputPathType,
    input_volume: InputPathType,
    fwhm: float,
    output_stem: str | None = None,
    regheader: str | None = None,
    projection_fraction: float | None = None,
    no_diff: bool = False,
    laterality_index: bool = False,
) -> Vol2symsurf:
    """
    Build parameters.

    Args:
        registration_file: Registration file
        input_volume: Input volume in NIfTI format
        fwhm: Full width at half maximum for surface smoothing
        output_stem: Output stem
        regheader: Subject for regheader
        projection_fraction: Projection fraction
        no_diff: Do not compute left-right hemisphere difference
        laterality_index: Compute laterality index instead of simple difference

    Returns:
        Parameter dictionary.
    """
    params: Vol2symsurf = {
        "@type": "freesurfer/vol2symsurf",
        "registration_file": registration_file,
        "input_volume": input_volume,
        "fwhm": fwhm,
        "no_diff": no_diff,
        "laterality_index": laterality_index,
    }
    if output_stem is not None:
        params["output_stem"] = output_stem
    if regheader is not None:
        params["regheader"] = regheader
    if projection_fraction is not None:
        params["projection_fraction"] = projection_fraction
    return params

def vol2symsurf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Vol2symsurf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("registration_file", None) is None:
        raise StyxValidationError("`registration_file` must not be None")
    if not isinstance(params["registration_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("fwhm", None) is None:
        raise StyxValidationError("`fwhm` must not be None")
    if not isinstance(params["fwhm"], (float, int)):
        raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("output_stem", None) is not None:
        if not isinstance(params["output_stem"], str):
            raise StyxValidationError(f'`output_stem` has the wrong type: Received `{type(params["output_stem"])}` expected `str`')
    if params.get("regheader", None) is not None:
        if not isinstance(params["regheader"], str):
            raise StyxValidationError(f'`regheader` has the wrong type: Received `{type(params["regheader"])}` expected `str`')
    if params.get("projection_fraction", None) is not None:
        if not isinstance(params["projection_fraction"], (float, int)):
            raise StyxValidationError(f'`projection_fraction` has the wrong type: Received `{type(params["projection_fraction"])}` expected `float`')
    if params.get("no_diff", None) is not None:
        if not isinstance(params["no_diff"], bool):
            raise StyxValidationError(f'`no_diff` has the wrong type: Received `{type(params["no_diff"])}` expected `bool`')
    if params.get("laterality_index", None) is not None:
        if not isinstance(params["laterality_index"], bool):
            raise StyxValidationError(f'`laterality_index` has the wrong type: Received `{type(params["laterality_index"])}` expected `bool`')

def vol2symsurf_cargs(params: Vol2symsurf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vol2symsurf")
    cargs.append("--reg")
    cargs.append(execution.input_file(params["registration_file"]))
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--fwhm")
    cargs.append(str(params["fwhm"]))
    v_0 = params.get("output_stem")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    v_1 = params.get("regheader")
    if v_1 is not None:
        cargs.append("--regheader")
        cargs.append(v_1)
    v_2 = params.get("projection_fraction")
    if v_2 is not None:
        cargs.append("--projfrac")
        cargs.append(str(v_2))
    if params.get("no_diff"):
        cargs.append("--no-diff")
    if params.get("laterality_index"):
        cargs.append("--li")
    return cargs

def vol2symsurf_outputs(params: Vol2symsurf, execution: Execution) -> Vol2symsurfOutputs:
    """Build the Vol2symsurfOutputs object for this tool."""
    output_lh_v: OutputPathType | None = None
    output_rh_v: OutputPathType | None = None
    output_lh_rh_difference_v: OutputPathType | None = None
    output_li_difference_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_stem")
    if __v0 is not None:
        output_lh_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.lh.nii')
    __v1 = params.get("output_stem")
    if __v1 is not None:
        output_rh_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.rh.nii')
    __v2 = params.get("output_stem")
    if __v2 is not None:
        output_lh_rh_difference_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.lh-rh.nii')
    __v3 = params.get("output_stem")
    if __v3 is not None:
        output_li_difference_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}.li.lh-rh.nii')
    return Vol2symsurfOutputs(
        root=root_v,
        output_lh=output_lh_v,
        output_rh=output_rh_v,
        output_lh_rh_difference=output_lh_rh_difference_v,
        output_li_difference=output_li_difference_v,
    )

def vol2symsurf_execute(params: Vol2symsurf, runner: Runner | None = None) -> Vol2symsurfOutputs:
    """
    vol2symsurf

    A tool that samples a volume onto the surface of the left-right symmetric subject (fsaverage_sym).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vol2symsurf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOL2SYMSURF_METADATA)
    execution.params(params)
    args = vol2symsurf_cargs(params, execution)
    out = vol2symsurf_outputs(params, execution)
    execution.run(args)
    return out

def vol2symsurf(
    registration_file: InputPathType,
    input_volume: InputPathType,
    fwhm: float,
    output_stem: str | None = None,
    regheader: str | None = None,
    projection_fraction: float | None = None,
    no_diff: bool = False,
    laterality_index: bool = False,
    runner: Runner | None = None,
) -> Vol2symsurfOutputs:
    """
    vol2symsurf

    A tool that samples a volume onto the surface of the left-right symmetric subject (fsaverage_sym).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        registration_file: Registration file
        input_volume: Input volume in NIfTI format
        fwhm: Full width at half maximum for surface smoothing
        output_stem: Output stem
        regheader: Subject for regheader
        projection_fraction: Projection fraction
        no_diff: Do not compute left-right hemisphere difference
        laterality_index: Compute laterality index instead of simple difference
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vol2symsurf_params(
        registration_file=registration_file,
        input_volume=input_volume,
        fwhm=fwhm,
        output_stem=output_stem,
        regheader=regheader,
        projection_fraction=projection_fraction,
        no_diff=no_diff,
        laterality_index=laterality_index,
    )
    return vol2symsurf_execute(params, runner)

__all__ = [
    "Vol2symsurf",
    "Vol2symsurfOutputs",
    "VOL2SYMSURF_METADATA",
    "vol2symsurf_cargs",
    "vol2symsurf_outputs",
    "vol2symsurf_params",
    "vol2symsurf_execute",
    "vol2symsurf_validate",
    "vol2symsurf",
]