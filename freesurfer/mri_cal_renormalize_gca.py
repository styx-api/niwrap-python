# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CAL_RENORMALIZE_GCA_METADATA = Metadata(
    id="mri_cal_renormalize_gca",
    name="mri_cal_renormalize_gca",
    package="freesurfer",
)

MriCalRenormalizeGca = typing.TypedDict(
    "MriCalRenormalizeGca",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cal_renormalize_gca"]],
        "timepoint_file": InputPathType,
        "in_vol": InputPathType,
        "input_atlas": InputPathType,
        "transform_file": InputPathType,
        "output_atlas": str,
    },
)

@dataclasses.dataclass
class MriCalRenormalizeGcaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_atlas_file: OutputPathType
    """Renormalized output atlas"""

def mri_cal_renormalize_gca_params(
    timepoint_file: InputPathType,
    in_vol: InputPathType,
    input_atlas: InputPathType,
    transform_file: InputPathType,
    output_atlas: str,
) -> MriCalRenormalizeGca:
    """
    Build parameters.

    Args:
        timepoint_file: Longitudinal time point file
        in_vol: Input volume
        input_atlas: Input atlas
        transform_file: Transform file
        output_atlas: Output atlas

    Returns:
        Parameter dictionary.
    """
    params: MriCalRenormalizeGca = {
        "@type": "freesurfer/mri_cal_renormalize_gca",
        "timepoint_file": timepoint_file,
        "in_vol": in_vol,
        "input_atlas": input_atlas,
        "transform_file": transform_file,
        "output_atlas": output_atlas,
    }
    return params

def mri_cal_renormalize_gca_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCalRenormalizeGca."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("timepoint_file", None) is None:
        raise StyxValidationError("`timepoint_file` must not be None")
    if not isinstance(params["timepoint_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`timepoint_file` has the wrong type: Received `{type(params["timepoint_file"])}` expected `InputPathType`')
    if params.get("in_vol", None) is None:
        raise StyxValidationError("`in_vol` must not be None")
    if not isinstance(params["in_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_vol` has the wrong type: Received `{type(params["in_vol"])}` expected `InputPathType`')
    if params.get("input_atlas", None) is None:
        raise StyxValidationError("`input_atlas` must not be None")
    if not isinstance(params["input_atlas"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_atlas` has the wrong type: Received `{type(params["input_atlas"])}` expected `InputPathType`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("output_atlas", None) is None:
        raise StyxValidationError("`output_atlas` must not be None")
    if not isinstance(params["output_atlas"], str):
        raise StyxValidationError(f'`output_atlas` has the wrong type: Received `{type(params["output_atlas"])}` expected `str`')

def mri_cal_renormalize_gca_cargs(params: MriCalRenormalizeGca, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cal_renormalize_gca")
    cargs.append(execution.input_file(params["timepoint_file"]))
    cargs.append(execution.input_file(params["in_vol"]))
    cargs.append(execution.input_file(params["input_atlas"]))
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(params["output_atlas"])
    return cargs

def mri_cal_renormalize_gca_outputs(params: MriCalRenormalizeGca, execution: Execution) -> MriCalRenormalizeGcaOutputs:
    """Build the MriCalRenormalizeGcaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_atlas_file_v: OutputPathType = execution.output_file(params["output_atlas"])
    return MriCalRenormalizeGcaOutputs(
        root=root_v,
        output_atlas_file=output_atlas_file_v,
    )

def mri_cal_renormalize_gca_execute(params: MriCalRenormalizeGca, runner: Runner | None = None) -> MriCalRenormalizeGcaOutputs:
    """
    mri_cal_renormalize_gca

    Tool for atlas renormalization in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cal_renormalize_gca_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CAL_RENORMALIZE_GCA_METADATA)
    execution.params(params)
    args = mri_cal_renormalize_gca_cargs(params, execution)
    out = mri_cal_renormalize_gca_outputs(params, execution)
    execution.run(args)
    return out

def mri_cal_renormalize_gca(
    timepoint_file: InputPathType,
    in_vol: InputPathType,
    input_atlas: InputPathType,
    transform_file: InputPathType,
    output_atlas: str,
    runner: Runner | None = None,
) -> MriCalRenormalizeGcaOutputs:
    """
    mri_cal_renormalize_gca

    Tool for atlas renormalization in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        timepoint_file: Longitudinal time point file
        in_vol: Input volume
        input_atlas: Input atlas
        transform_file: Transform file
        output_atlas: Output atlas
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cal_renormalize_gca_params(
        timepoint_file=timepoint_file,
        in_vol=in_vol,
        input_atlas=input_atlas,
        transform_file=transform_file,
        output_atlas=output_atlas,
    )
    return mri_cal_renormalize_gca_execute(params, runner)

__all__ = [
    "MriCalRenormalizeGca",
    "MriCalRenormalizeGcaOutputs",
    "MRI_CAL_RENORMALIZE_GCA_METADATA",
    "mri_cal_renormalize_gca_cargs",
    "mri_cal_renormalize_gca_outputs",
    "mri_cal_renormalize_gca_params",
    "mri_cal_renormalize_gca_execute",
    "mri_cal_renormalize_gca_validate",
    "mri_cal_renormalize_gca",
]