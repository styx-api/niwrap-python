# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_VOL2LABEL_METADATA = Metadata(
    id="mri_vol2label",
    name="mri_vol2label",
    package="freesurfer",
)

MriVol2label = typing.TypedDict(
    "MriVol2label",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_vol2label"]],
        "input": InputPathType,
        "label_id": typing.NotRequired[float],
        "threshold": typing.NotRequired[float],
        "label_file": str,
        "vol_file": typing.NotRequired[str],
        "surf_subject_hemi": typing.NotRequired[str],
        "surf_path": typing.NotRequired[str],
        "opt_params": typing.NotRequired[str],
        "remove_holes": typing.NotRequired[bool],
        "dilations": typing.NotRequired[float],
        "erosions": typing.NotRequired[float],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriVol2labelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_file: OutputPathType
    """Generated label file."""
    output_volume_file: OutputPathType | None
    """Generated label volume file."""

def mri_vol2label_params(
    input_: InputPathType,
    label_file: str,
    label_id: float | None = None,
    threshold: float | None = None,
    vol_file: str | None = None,
    surf_subject_hemi: str | None = None,
    surf_path: str | None = None,
    opt_params: str | None = None,
    remove_holes: bool = False,
    dilations: float | None = None,
    erosions: float | None = None,
    help: bool = False,
) -> MriVol2label:
    """
    Build parameters.

    Args:
        input_: Input volume or surface overlay.
        label_file: Name of output label file.
        label_id: Value to match in the input.
        threshold: Threshold the input to make label (i.e., input > threshold)\
            instead of using Label ID.
        vol_file: Write label volume in this file.
        surf_subject_hemi: Interpret input as surface overlay with the given\
            subject and hemisphere (optionally with surface).
        surf_path: Specify surface path instead of subject/hemi.
        opt_params: Treats input as a probability map. Format: target delta valmap.
        remove_holes: Remove holes in label and islands (with --surf only).
        dilations: Dilate label (with --surf only).
        erosions: Erode label (with --surf only).
        help: Print out help information.

    Returns:
        Parameter dictionary.
    """
    params: MriVol2label = {
        "@type": "freesurfer/mri_vol2label",
        "input": input_,
        "label_file": label_file,
        "remove_holes": remove_holes,
        "help": help,
    }
    if label_id is not None:
        params["label_id"] = label_id
    if threshold is not None:
        params["threshold"] = threshold
    if vol_file is not None:
        params["vol_file"] = vol_file
    if surf_subject_hemi is not None:
        params["surf_subject_hemi"] = surf_subject_hemi
    if surf_path is not None:
        params["surf_path"] = surf_path
    if opt_params is not None:
        params["opt_params"] = opt_params
    if dilations is not None:
        params["dilations"] = dilations
    if erosions is not None:
        params["erosions"] = erosions
    return params

def mri_vol2label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriVol2label."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("label_id", None) is not None:
        if not isinstance(params["label_id"], (float, int)):
            raise StyxValidationError(f'`label_id` has the wrong type: Received `{type(params["label_id"])}` expected `float`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], str):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `str`')
    if params.get("vol_file", None) is not None:
        if not isinstance(params["vol_file"], str):
            raise StyxValidationError(f'`vol_file` has the wrong type: Received `{type(params["vol_file"])}` expected `str`')
    if params.get("surf_subject_hemi", None) is not None:
        if not isinstance(params["surf_subject_hemi"], str):
            raise StyxValidationError(f'`surf_subject_hemi` has the wrong type: Received `{type(params["surf_subject_hemi"])}` expected `str`')
    if params.get("surf_path", None) is not None:
        if not isinstance(params["surf_path"], str):
            raise StyxValidationError(f'`surf_path` has the wrong type: Received `{type(params["surf_path"])}` expected `str`')
    if params.get("opt_params", None) is not None:
        if not isinstance(params["opt_params"], str):
            raise StyxValidationError(f'`opt_params` has the wrong type: Received `{type(params["opt_params"])}` expected `str`')
    if params.get("remove_holes", None) is not None:
        if not isinstance(params["remove_holes"], bool):
            raise StyxValidationError(f'`remove_holes` has the wrong type: Received `{type(params["remove_holes"])}` expected `bool`')
    if params.get("dilations", None) is not None:
        if not isinstance(params["dilations"], (float, int)):
            raise StyxValidationError(f'`dilations` has the wrong type: Received `{type(params["dilations"])}` expected `float`')
    if params.get("erosions", None) is not None:
        if not isinstance(params["erosions"], (float, int)):
            raise StyxValidationError(f'`erosions` has the wrong type: Received `{type(params["erosions"])}` expected `float`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_vol2label_cargs(params: MriVol2label, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_vol2label")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input"]))
    v_0 = params.get("label_id")
    if v_0 is not None:
        cargs.append("--id")
        cargs.append(str(v_0))
    v_1 = params.get("threshold")
    if v_1 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_1))
    cargs.append("--l")
    cargs.append(params["label_file"])
    v_2 = params.get("vol_file")
    if v_2 is not None:
        cargs.append("--v")
        cargs.append(v_2)
    v_3 = params.get("surf_subject_hemi")
    if v_3 is not None:
        cargs.append("--surf")
        cargs.append(v_3)
    v_4 = params.get("surf_path")
    if v_4 is not None:
        cargs.append("--surf-path")
        cargs.append(v_4)
    v_5 = params.get("opt_params")
    if v_5 is not None:
        cargs.append("--opt")
        cargs.append(v_5)
    if params.get("remove_holes"):
        cargs.append("--remove-holes-islands")
    v_6 = params.get("dilations")
    if v_6 is not None:
        cargs.append("--dilate")
        cargs.append(str(v_6))
    v_7 = params.get("erosions")
    if v_7 is not None:
        cargs.append("--erode")
        cargs.append(str(v_7))
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_vol2label_outputs(params: MriVol2label, execution: Execution) -> MriVol2labelOutputs:
    """Build the MriVol2labelOutputs object for this tool."""
    output_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_label_file_v: OutputPathType = execution.output_file(params["label_file"])
    __v0 = params.get("vol_file")
    if __v0 is not None:
        output_volume_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriVol2labelOutputs(
        root=root_v,
        output_label_file=output_label_file_v,
        output_volume_file=output_volume_file_v,
    )

def mri_vol2label_execute(params: MriVol2label, runner: Runner | None = None) -> MriVol2labelOutputs:
    """
    mri_vol2label

    Converts values in a volume or surface overlay to a label using specified parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_vol2label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_VOL2LABEL_METADATA)
    execution.params(params)
    args = mri_vol2label_cargs(params, execution)
    out = mri_vol2label_outputs(params, execution)
    execution.run(args)
    return out

def mri_vol2label(
    input_: InputPathType,
    label_file: str,
    label_id: float | None = None,
    threshold: float | None = None,
    vol_file: str | None = None,
    surf_subject_hemi: str | None = None,
    surf_path: str | None = None,
    opt_params: str | None = None,
    remove_holes: bool = False,
    dilations: float | None = None,
    erosions: float | None = None,
    help: bool = False,
    runner: Runner | None = None,
) -> MriVol2labelOutputs:
    """
    mri_vol2label

    Converts values in a volume or surface overlay to a label using specified parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_: Input volume or surface overlay.
        label_file: Name of output label file.
        label_id: Value to match in the input.
        threshold: Threshold the input to make label (i.e., input > threshold)\
            instead of using Label ID.
        vol_file: Write label volume in this file.
        surf_subject_hemi: Interpret input as surface overlay with the given\
            subject and hemisphere (optionally with surface).
        surf_path: Specify surface path instead of subject/hemi.
        opt_params: Treats input as a probability map. Format: target delta valmap.
        remove_holes: Remove holes in label and islands (with --surf only).
        dilations: Dilate label (with --surf only).
        erosions: Erode label (with --surf only).
        help: Print out help information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_vol2label_params(
        input_=input_,
        label_file=label_file,
        label_id=label_id,
        threshold=threshold,
        vol_file=vol_file,
        surf_subject_hemi=surf_subject_hemi,
        surf_path=surf_path,
        opt_params=opt_params,
        remove_holes=remove_holes,
        dilations=dilations,
        erosions=erosions,
        help=help,
    )
    return mri_vol2label_execute(params, runner)

__all__ = [
    "MriVol2label",
    "MriVol2labelOutputs",
    "MRI_VOL2LABEL_METADATA",
    "mri_vol2label_cargs",
    "mri_vol2label_outputs",
    "mri_vol2label_params",
    "mri_vol2label_execute",
    "mri_vol2label_validate",
    "mri_vol2label",
]