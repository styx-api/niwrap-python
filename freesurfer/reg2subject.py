# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG2SUBJECT_METADATA = Metadata(
    id="reg2subject",
    name="reg2subject",
    package="freesurfer",
)

Reg2subject = typing.TypedDict(
    "Reg2subject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reg2subject"]],
        "regfile": InputPathType,
    },
)

@dataclasses.dataclass
class Reg2subjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def reg2subject_params(
    regfile: InputPathType,
) -> Reg2subject:
    """
    Build parameters.

    Args:
        regfile:

    Returns:
        Parameter dictionary.
    """
    params: Reg2subject = {
        "@type": "freesurfer/reg2subject",
        "regfile": regfile,
    }
    return params

def reg2subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Reg2subject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("regfile", None) is None:
        raise StyxValidationError("`regfile` must not be None")
    if not isinstance(params["regfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`regfile` has the wrong type: Received `{type(params["regfile"])}` expected `InputPathType`')

def reg2subject_cargs(params: Reg2subject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg2subject")
    cargs.append("--r")
    cargs.append(execution.input_file(params["regfile"]))
    return cargs

def reg2subject_outputs(params: Reg2subject, execution: Execution) -> Reg2subjectOutputs:
    """Build the Reg2subjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Reg2subjectOutputs(
        root=root_v,
    )

def reg2subject_execute(params: Reg2subject, runner: Runner | None = None) -> Reg2subjectOutputs:
    """
    reg2subject

    Returns the name of the subject in the registration file, whether it is an LTA or register.dat file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg2subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG2SUBJECT_METADATA)
    execution.params(params)
    args = reg2subject_cargs(params, execution)
    out = reg2subject_outputs(params, execution)
    execution.run(args)
    return out

def reg2subject(
    regfile: InputPathType,
    runner: Runner | None = None,
) -> Reg2subjectOutputs:
    """
    reg2subject

    Returns the name of the subject in the registration file, whether it is an LTA or register.dat file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        regfile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg2subject_params(
        regfile=regfile,
    )
    return reg2subject_execute(params, runner)

__all__ = [
    "Reg2subject",
    "Reg2subjectOutputs",
    "REG2SUBJECT_METADATA",
    "reg2subject_cargs",
    "reg2subject_outputs",
    "reg2subject_params",
    "reg2subject_execute",
    "reg2subject_validate",
    "reg2subject",
]