# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CA_TRAIN_METADATA = Metadata(
    id="mri_ca_train",
    name="mri_ca_train",
    package="freesurfer",
)

MriCaTrain = typing.TypedDict(
    "MriCaTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_ca_train"]],
        "subjects": list[str],
        "output_gca": str,
        "segmentation": str,
        "transform": typing.NotRequired[str],
        "mask_volume": typing.NotRequired[str],
        "node_spacing": typing.NotRequired[str],
        "prior_spacing": typing.NotRequired[str],
        "input_training": typing.NotRequired[list[str]],
        "symmetrize": typing.NotRequired[bool],
        "makesym": typing.NotRequired[list[str]],
        "check_symmetry": typing.NotRequired[list[str]],
        "sanity_check": typing.NotRequired[bool],
        "threads": typing.NotRequired[int],
        "done_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriCaTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_ca_train_params(
    subjects: list[str],
    output_gca: str,
    segmentation: str,
    transform: str | None = None,
    mask_volume: str | None = None,
    node_spacing: str | None = None,
    prior_spacing: str | None = None,
    input_training: list[str] | None = None,
    symmetrize: bool = False,
    makesym: list[str] | None = None,
    check_symmetry: list[str] | None = None,
    sanity_check: bool = False,
    threads: int | None = None,
    done_file: str | None = None,
) -> MriCaTrain:
    """
    Build parameters.

    Args:
        subjects: List of subject directories under the SUBJECTS_DIR. Each is a\
            path relative to the subject's MRI directory.
        output_gca: Output GCA file name.
        segmentation: Segmentation volume directory relative to each subject's MRI\
            path.
        transform: Atlas transform path relative to each subject's MRI transforms\
            directory.
        mask_volume: Volume name used as a mask, path relative to each subject's\
            MRI directory.
        node_spacing: Spacing of classifiers in canonical space
        prior_spacing: Spacing of class priors in canonical space
        input_training: Specifying training data, path relative to each subject's\
            MRI directory. Can specify multiple inputs.
        symmetrize: Symmetrize the atlas after creation
        makesym: Symmetrize an already existing atlas. Specify input GCA and\
            symmetrized GCA.
        check_symmetry: Check the symmetry of an already existing atlas. Specify\
            input GCA and symmetrized GCA.
        sanity_check: Conduct sanity-check of labels for obvious edit errors
        threads: Specify number of threads to use (also known as -nthreads)
        done_file: Create DoneFile when done (contents: 0=ok, 1=error)

    Returns:
        Parameter dictionary.
    """
    params: MriCaTrain = {
        "@type": "freesurfer/mri_ca_train",
        "subjects": subjects,
        "output_gca": output_gca,
        "segmentation": segmentation,
        "symmetrize": symmetrize,
        "sanity_check": sanity_check,
    }
    if transform is not None:
        params["transform"] = transform
    if mask_volume is not None:
        params["mask_volume"] = mask_volume
    if node_spacing is not None:
        params["node_spacing"] = node_spacing
    if prior_spacing is not None:
        params["prior_spacing"] = prior_spacing
    if input_training is not None:
        params["input_training"] = input_training
    if makesym is not None:
        params["makesym"] = makesym
    if check_symmetry is not None:
        params["check_symmetry"] = check_symmetry
    if threads is not None:
        params["threads"] = threads
    if done_file is not None:
        params["done_file"] = done_file
    return params

def mri_ca_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCaTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    if len(params["subjects"]) < 1:
        raise StyxValidationError("Parameter `subjects` must contain at least 1 element")
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_gca", None) is None:
        raise StyxValidationError("`output_gca` must not be None")
    if not isinstance(params["output_gca"], str):
        raise StyxValidationError(f'`output_gca` has the wrong type: Received `{type(params["output_gca"])}` expected `str`')
    if params.get("segmentation", None) is None:
        raise StyxValidationError("`segmentation` must not be None")
    if not isinstance(params["segmentation"], str):
        raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `str`')
    if params.get("transform", None) is not None:
        if not isinstance(params["transform"], str):
            raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `str`')
    if params.get("mask_volume", None) is not None:
        if not isinstance(params["mask_volume"], str):
            raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `str`')
    if params.get("node_spacing", None) is not None:
        if not isinstance(params["node_spacing"], str):
            raise StyxValidationError(f'`node_spacing` has the wrong type: Received `{type(params["node_spacing"])}` expected `str`')
    if params.get("prior_spacing", None) is not None:
        if not isinstance(params["prior_spacing"], str):
            raise StyxValidationError(f'`prior_spacing` has the wrong type: Received `{type(params["prior_spacing"])}` expected `str`')
    if params.get("input_training", None) is not None:
        if not isinstance(params["input_training"], list):
            raise StyxValidationError(f'`input_training` has the wrong type: Received `{type(params["input_training"])}` expected `list[str]`')
        for e_2 in params["input_training"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`input_training` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("symmetrize", None) is not None:
        if not isinstance(params["symmetrize"], bool):
            raise StyxValidationError(f'`symmetrize` has the wrong type: Received `{type(params["symmetrize"])}` expected `bool`')
    if params.get("makesym", None) is not None:
        if not isinstance(params["makesym"], list):
            raise StyxValidationError(f'`makesym` has the wrong type: Received `{type(params["makesym"])}` expected `list[str]`')
        if len(params["makesym"]) > 2:
            raise StyxValidationError("Parameter `makesym` must contain at most 2 elements")
        for e_3 in params["makesym"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`makesym` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("check_symmetry", None) is not None:
        if not isinstance(params["check_symmetry"], list):
            raise StyxValidationError(f'`check_symmetry` has the wrong type: Received `{type(params["check_symmetry"])}` expected `list[str]`')
        if len(params["check_symmetry"]) > 2:
            raise StyxValidationError("Parameter `check_symmetry` must contain at most 2 elements")
        for e_4 in params["check_symmetry"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`check_symmetry` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("sanity_check", None) is not None:
        if not isinstance(params["sanity_check"], bool):
            raise StyxValidationError(f'`sanity_check` has the wrong type: Received `{type(params["sanity_check"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], int):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `int`')
    if params.get("done_file", None) is not None:
        if not isinstance(params["done_file"], str):
            raise StyxValidationError(f'`done_file` has the wrong type: Received `{type(params["done_file"])}` expected `str`')

def mri_ca_train_cargs(params: MriCaTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_ca_train")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_gca"])
    cargs.append("-seg")
    cargs.append(params["segmentation"])
    v_0 = params.get("transform")
    if v_0 is not None:
        cargs.append("-xform")
        cargs.append(v_0)
    v_1 = params.get("mask_volume")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(v_1)
    v_2 = params.get("node_spacing")
    if v_2 is not None:
        cargs.append("-node_spacing")
        cargs.append(v_2)
    v_3 = params.get("prior_spacing")
    if v_3 is not None:
        cargs.append("-prior_spacing")
        cargs.append(v_3)
    v_4 = params.get("input_training")
    if v_4 is not None:
        cargs.append("-input")
        for item1 in v_4:
            cargs.append(item1)
    if params.get("symmetrize"):
        cargs.append("-sym")
    v_5 = params.get("makesym")
    if v_5 is not None:
        cargs.append("-makesym")
        for item2 in v_5:
            cargs.append(item2)
    v_6 = params.get("check_symmetry")
    if v_6 is not None:
        cargs.append("-checksym")
        for item3 in v_6:
            cargs.append(item3)
    if params.get("sanity_check"):
        cargs.append("-check")
    v_7 = params.get("threads")
    if v_7 is not None:
        cargs.append("-threads")
        cargs.append(str(v_7))
    v_8 = params.get("done_file")
    if v_8 is not None:
        cargs.append("-done")
        cargs.append(v_8)
    return cargs

def mri_ca_train_outputs(params: MriCaTrain, execution: Execution) -> MriCaTrainOutputs:
    """Build the MriCaTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriCaTrainOutputs(
        root=root_v,
    )

def mri_ca_train_execute(params: MriCaTrain, runner: Runner | None = None) -> MriCaTrainOutputs:
    """
    mri_ca_train

    Trains GCA data with multiple subjects using provided segmentation volumes and other configuration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_ca_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CA_TRAIN_METADATA)
    execution.params(params)
    args = mri_ca_train_cargs(params, execution)
    out = mri_ca_train_outputs(params, execution)
    execution.run(args)
    return out

def mri_ca_train(
    subjects: list[str],
    output_gca: str,
    segmentation: str,
    transform: str | None = None,
    mask_volume: str | None = None,
    node_spacing: str | None = None,
    prior_spacing: str | None = None,
    input_training: list[str] | None = None,
    symmetrize: bool = False,
    makesym: list[str] | None = None,
    check_symmetry: list[str] | None = None,
    sanity_check: bool = False,
    threads: int | None = None,
    done_file: str | None = None,
    runner: Runner | None = None,
) -> MriCaTrainOutputs:
    """
    mri_ca_train

    Trains GCA data with multiple subjects using provided segmentation volumes and other configuration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subject directories under the SUBJECTS_DIR. Each is a\
            path relative to the subject's MRI directory.
        output_gca: Output GCA file name.
        segmentation: Segmentation volume directory relative to each subject's MRI\
            path.
        transform: Atlas transform path relative to each subject's MRI transforms\
            directory.
        mask_volume: Volume name used as a mask, path relative to each subject's\
            MRI directory.
        node_spacing: Spacing of classifiers in canonical space
        prior_spacing: Spacing of class priors in canonical space
        input_training: Specifying training data, path relative to each subject's\
            MRI directory. Can specify multiple inputs.
        symmetrize: Symmetrize the atlas after creation
        makesym: Symmetrize an already existing atlas. Specify input GCA and\
            symmetrized GCA.
        check_symmetry: Check the symmetry of an already existing atlas. Specify\
            input GCA and symmetrized GCA.
        sanity_check: Conduct sanity-check of labels for obvious edit errors
        threads: Specify number of threads to use (also known as -nthreads)
        done_file: Create DoneFile when done (contents: 0=ok, 1=error)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_ca_train_params(
        subjects=subjects,
        output_gca=output_gca,
        segmentation=segmentation,
        transform=transform,
        mask_volume=mask_volume,
        node_spacing=node_spacing,
        prior_spacing=prior_spacing,
        input_training=input_training,
        symmetrize=symmetrize,
        makesym=makesym,
        check_symmetry=check_symmetry,
        sanity_check=sanity_check,
        threads=threads,
        done_file=done_file,
    )
    return mri_ca_train_execute(params, runner)

__all__ = [
    "MriCaTrain",
    "MriCaTrainOutputs",
    "MRI_CA_TRAIN_METADATA",
    "mri_ca_train_cargs",
    "mri_ca_train_outputs",
    "mri_ca_train_params",
    "mri_ca_train_execute",
    "mri_ca_train_validate",
    "mri_ca_train",
]