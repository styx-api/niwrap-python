# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCA_FIX_ENTO_METADATA = Metadata(
    id="rca-fix-ento",
    name="rca-fix-ento",
    package="freesurfer",
)

RcaFixEnto = typing.TypedDict(
    "RcaFixEnto",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rca-fix-ento"]],
        "subject": str,
        "threads": typing.NotRequired[float],
        "submit": typing.NotRequired[bool],
        "account": typing.NotRequired[str],
        "brain_mask": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RcaFixEntoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    entowm_output: OutputPathType
    """Output segmentation of the WM around entorhinal cortex."""
    finalsurfs_output: OutputPathType
    """Edited brain.finalsurfs with manual edits."""
    backup_finalsufs_output: OutputPathType
    """Backup of the original brain.finalsurfs.manedit.mgz before edits."""

def rca_fix_ento_params(
    subject: str,
    threads: float | None = None,
    submit: bool = False,
    account: str | None = None,
    brain_mask: bool = False,
) -> RcaFixEnto:
    """
    Build parameters.

    Args:
        subject: The subject identifier for processing.
        threads: Number of threads to use for processing.
        submit: Submit the task to sbatch with 1 thread and 14GB of memory.
        account: Specify the account to use when submitting to sbatch; default is\
            'fhs'.
        brain_mask: Apply the ento fix to the brain.finalsurfs; this is turned off\
            due to a conflict with 255.

    Returns:
        Parameter dictionary.
    """
    params: RcaFixEnto = {
        "@type": "freesurfer/rca-fix-ento",
        "subject": subject,
        "submit": submit,
        "brain_mask": brain_mask,
    }
    if threads is not None:
        params["threads"] = threads
    if account is not None:
        params["account"] = account
    return params

def rca_fix_ento_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcaFixEnto."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("submit", None) is not None:
        if not isinstance(params["submit"], bool):
            raise StyxValidationError(f'`submit` has the wrong type: Received `{type(params["submit"])}` expected `bool`')
    if params.get("account", None) is not None:
        if not isinstance(params["account"], str):
            raise StyxValidationError(f'`account` has the wrong type: Received `{type(params["account"])}` expected `str`')
    if params.get("brain_mask", None) is not None:
        if not isinstance(params["brain_mask"], bool):
            raise StyxValidationError(f'`brain_mask` has the wrong type: Received `{type(params["brain_mask"])}` expected `bool`')

def rca_fix_ento_cargs(params: RcaFixEnto, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rca-fix-ento")
    cargs.append("-s")
    cargs.append(params["subject"])
    v_0 = params.get("threads")
    if v_0 is not None:
        cargs.append("--threads")
        cargs.append(str(v_0))
    if params.get("submit"):
        cargs.append("--submit")
    v_1 = params.get("account")
    if v_1 is not None:
        cargs.append("--account")
        cargs.append(v_1)
    if params.get("brain_mask"):
        cargs.append("--brain-mask")
    return cargs

def rca_fix_ento_outputs(params: RcaFixEnto, execution: Execution) -> RcaFixEntoOutputs:
    """Build the RcaFixEntoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    entowm_output_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/entowm.mgz')
    finalsurfs_output_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/brain.finalsurfs.manedit.mgz')
    backup_finalsufs_output_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/backup.brain.finalsurfs.manedit.mgz')
    return RcaFixEntoOutputs(
        root=root_v,
        entowm_output=entowm_output_v,
        finalsurfs_output=finalsurfs_output_v,
        backup_finalsufs_output=backup_finalsufs_output_v,
    )

def rca_fix_ento_execute(params: RcaFixEnto, runner: Runner | None = None) -> RcaFixEntoOutputs:
    """
    rca-fix-ento

    A tool to fix the entorhinal white matter in FreeSurfer using a deep learning network.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rca_fix_ento_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCA_FIX_ENTO_METADATA)
    execution.params(params)
    args = rca_fix_ento_cargs(params, execution)
    out = rca_fix_ento_outputs(params, execution)
    execution.run(args)
    return out

def rca_fix_ento(
    subject: str,
    threads: float | None = None,
    submit: bool = False,
    account: str | None = None,
    brain_mask: bool = False,
    runner: Runner | None = None,
) -> RcaFixEntoOutputs:
    """
    rca-fix-ento

    A tool to fix the entorhinal white matter in FreeSurfer using a deep learning network.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject identifier for processing.
        threads: Number of threads to use for processing.
        submit: Submit the task to sbatch with 1 thread and 14GB of memory.
        account: Specify the account to use when submitting to sbatch; default is\
            'fhs'.
        brain_mask: Apply the ento fix to the brain.finalsurfs; this is turned off\
            due to a conflict with 255.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rca_fix_ento_params(
        subject=subject,
        threads=threads,
        submit=submit,
        account=account,
        brain_mask=brain_mask,
    )
    return rca_fix_ento_execute(params, runner)

__all__ = [
    "RcaFixEnto",
    "RcaFixEntoOutputs",
    "RCA_FIX_ENTO_METADATA",
    "rca_fix_ento_cargs",
    "rca_fix_ento_outputs",
    "rca_fix_ento_params",
    "rca_fix_ento_execute",
    "rca_fix_ento_validate",
    "rca_fix_ento",
]