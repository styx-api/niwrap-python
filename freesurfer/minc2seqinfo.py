# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MINC2SEQINFO_METADATA = Metadata(
    id="minc2seqinfo",
    name="minc2seqinfo",
    package="freesurfer",
)

Minc2seqinfo = typing.TypedDict(
    "Minc2seqinfo",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/minc2seqinfo"]],
        "mincfile": InputPathType,
        "seqinfofile": str,
    },
)

@dataclasses.dataclass
class Minc2seqinfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_seqinfofile: OutputPathType
    """File containing the extracted sequence information."""

def minc2seqinfo_params(
    mincfile: InputPathType,
    seqinfofile: str,
) -> Minc2seqinfo:
    """
    Build parameters.

    Args:
        mincfile: Input MINC file from which to extract sequence information.
        seqinfofile: Output file where the sequence information will be stored.

    Returns:
        Parameter dictionary.
    """
    params: Minc2seqinfo = {
        "@type": "freesurfer/minc2seqinfo",
        "mincfile": mincfile,
        "seqinfofile": seqinfofile,
    }
    return params

def minc2seqinfo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Minc2seqinfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mincfile", None) is None:
        raise StyxValidationError("`mincfile` must not be None")
    if not isinstance(params["mincfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mincfile` has the wrong type: Received `{type(params["mincfile"])}` expected `InputPathType`')
    if params.get("seqinfofile", None) is None:
        raise StyxValidationError("`seqinfofile` must not be None")
    if not isinstance(params["seqinfofile"], str):
        raise StyxValidationError(f'`seqinfofile` has the wrong type: Received `{type(params["seqinfofile"])}` expected `str`')

def minc2seqinfo_cargs(params: Minc2seqinfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("minc2seqinfo")
    cargs.append(execution.input_file(params["mincfile"]))
    cargs.append(params["seqinfofile"])
    return cargs

def minc2seqinfo_outputs(params: Minc2seqinfo, execution: Execution) -> Minc2seqinfoOutputs:
    """Build the Minc2seqinfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_seqinfofile_v: OutputPathType = execution.output_file(params["seqinfofile"])
    return Minc2seqinfoOutputs(
        root=root_v,
        out_seqinfofile=out_seqinfofile_v,
    )

def minc2seqinfo_execute(params: Minc2seqinfo, runner: Runner | None = None) -> Minc2seqinfoOutputs:
    """
    minc2seqinfo

    Tool for extracting sequence information from MINC files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    minc2seqinfo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MINC2SEQINFO_METADATA)
    execution.params(params)
    args = minc2seqinfo_cargs(params, execution)
    out = minc2seqinfo_outputs(params, execution)
    execution.run(args)
    return out

def minc2seqinfo(
    mincfile: InputPathType,
    seqinfofile: str,
    runner: Runner | None = None,
) -> Minc2seqinfoOutputs:
    """
    minc2seqinfo

    Tool for extracting sequence information from MINC files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mincfile: Input MINC file from which to extract sequence information.
        seqinfofile: Output file where the sequence information will be stored.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = minc2seqinfo_params(
        mincfile=mincfile,
        seqinfofile=seqinfofile,
    )
    return minc2seqinfo_execute(params, runner)

__all__ = [
    "Minc2seqinfo",
    "Minc2seqinfoOutputs",
    "MINC2SEQINFO_METADATA",
    "minc2seqinfo_cargs",
    "minc2seqinfo_outputs",
    "minc2seqinfo_params",
    "minc2seqinfo_execute",
    "minc2seqinfo_validate",
    "minc2seqinfo",
]