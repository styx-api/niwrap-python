# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DEFECT_SEG_METADATA = Metadata(
    id="defect-seg",
    name="defect-seg",
    package="freesurfer",
)

DefectSeg = typing.TypedDict(
    "DefectSeg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/defect-seg"]],
        "subject": str,
        "lh_only": typing.NotRequired[bool],
        "rh_only": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DefectSegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_defects: OutputPathType
    """Surface defects volume."""
    lh_defects_summary: OutputPathType
    """Defects summary with ID and number of vertices for left hemisphere."""
    lh_defect_labels_fix: OutputPathType
    """Fixed defect labels on the left hemisphere surface."""
    lh_defect_labels_fix_bin: OutputPathType
    """Binary labels for fixed defects on the left hemisphere."""
    lh_defects_nofix_annot: OutputPathType
    """Annotation file for defects without fixes on the left hemisphere."""
    lh_defects_fix_annot: OutputPathType
    """Annotation file for defects with fixes on the left hemisphere."""
    lh_defects_stats: OutputPathType
    """Statistics summary of defects indicating area and average thickness on the left hemisphere."""
    rh_defects_summary: OutputPathType
    """Defects summary with ID and number of vertices for right hemisphere."""
    rh_defect_labels_fix: OutputPathType
    """Fixed defect labels on the Right hemisphere surface."""
    rh_defect_labels_fix_bin: OutputPathType
    """Binary labels for fixed defects on the right hemisphere."""
    rh_defects_nofix_annot: OutputPathType
    """Annotation file for defects without fixes on the right hemisphere."""
    rh_defects_fix_annot: OutputPathType
    """Annotation file for defects with fixes on the right hemisphere."""
    rh_defects_stats: OutputPathType
    """Statistics summary of defects indicating area and average thickness on the right hemisphere."""

def defect_seg_params(
    subject: str,
    lh_only: bool = False,
    rh_only: bool = False,
) -> DefectSeg:
    """
    Build parameters.

    Args:
        subject: Subject identifier.
        lh_only: Only process the left hemisphere.
        rh_only: Only process the right hemisphere.

    Returns:
        Parameter dictionary.
    """
    params: DefectSeg = {
        "@type": "freesurfer/defect-seg",
        "subject": subject,
        "lh_only": lh_only,
        "rh_only": rh_only,
    }
    return params

def defect_seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DefectSeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("lh_only", None) is not None:
        if not isinstance(params["lh_only"], bool):
            raise StyxValidationError(f'`lh_only` has the wrong type: Received `{type(params["lh_only"])}` expected `bool`')
    if params.get("rh_only", None) is not None:
        if not isinstance(params["rh_only"], bool):
            raise StyxValidationError(f'`rh_only` has the wrong type: Received `{type(params["rh_only"])}` expected `bool`')

def defect_seg_cargs(params: DefectSeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("defect-seg")
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("lh_only"):
        cargs.append("--lh-only")
    if params.get("rh_only"):
        cargs.append("--rh-only")
    return cargs

def defect_seg_outputs(params: DefectSeg, execution: Execution) -> DefectSegOutputs:
    """Build the DefectSegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_defects_v: OutputPathType = execution.output_file("mri/surface.defects.mgz")
    lh_defects_summary_v: OutputPathType = execution.output_file("scripts/lh.defects.summary")
    lh_defect_labels_fix_v: OutputPathType = execution.output_file("surf/lh.defect_labels.fix.mgz")
    lh_defect_labels_fix_bin_v: OutputPathType = execution.output_file("surf/lh.defect_labels.fix.bin.mgz")
    lh_defects_nofix_annot_v: OutputPathType = execution.output_file("label/lh.defects.nofix.annot")
    lh_defects_fix_annot_v: OutputPathType = execution.output_file("label/lh.defects.fix.annot")
    lh_defects_stats_v: OutputPathType = execution.output_file("stats/lh.defects.stats")
    rh_defects_summary_v: OutputPathType = execution.output_file("scripts/rh.defects.summary")
    rh_defect_labels_fix_v: OutputPathType = execution.output_file("surf/rh.defect_labels.fix.mgz")
    rh_defect_labels_fix_bin_v: OutputPathType = execution.output_file("surf/rh.defect_labels.fix.bin.mgz")
    rh_defects_nofix_annot_v: OutputPathType = execution.output_file("label/rh.defects.nofix.annot")
    rh_defects_fix_annot_v: OutputPathType = execution.output_file("label/rh.defects.fix.annot")
    rh_defects_stats_v: OutputPathType = execution.output_file("stats/rh.defects.stats")
    return DefectSegOutputs(
        root=root_v,
        surface_defects=surface_defects_v,
        lh_defects_summary=lh_defects_summary_v,
        lh_defect_labels_fix=lh_defect_labels_fix_v,
        lh_defect_labels_fix_bin=lh_defect_labels_fix_bin_v,
        lh_defects_nofix_annot=lh_defects_nofix_annot_v,
        lh_defects_fix_annot=lh_defects_fix_annot_v,
        lh_defects_stats=lh_defects_stats_v,
        rh_defects_summary=rh_defects_summary_v,
        rh_defect_labels_fix=rh_defect_labels_fix_v,
        rh_defect_labels_fix_bin=rh_defect_labels_fix_bin_v,
        rh_defects_nofix_annot=rh_defects_nofix_annot_v,
        rh_defects_fix_annot=rh_defects_fix_annot_v,
        rh_defects_stats=rh_defects_stats_v,
    )

def defect_seg_execute(params: DefectSeg, runner: Runner | None = None) -> DefectSegOutputs:
    """
    defect-seg

    This program creates some files that allows the user to visualize and evaluate the surface defects that were automatically detected and fixed by the topology correction program.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    defect_seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DEFECT_SEG_METADATA)
    execution.params(params)
    args = defect_seg_cargs(params, execution)
    out = defect_seg_outputs(params, execution)
    execution.run(args)
    return out

def defect_seg(
    subject: str,
    lh_only: bool = False,
    rh_only: bool = False,
    runner: Runner | None = None,
) -> DefectSegOutputs:
    """
    defect-seg

    This program creates some files that allows the user to visualize and evaluate the surface defects that were automatically detected and fixed by the topology correction program.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier.
        lh_only: Only process the left hemisphere.
        rh_only: Only process the right hemisphere.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = defect_seg_params(
        subject=subject,
        lh_only=lh_only,
        rh_only=rh_only,
    )
    return defect_seg_execute(params, runner)

__all__ = [
    "DefectSeg",
    "DefectSegOutputs",
    "DEFECT_SEG_METADATA",
    "defect_seg_cargs",
    "defect_seg_outputs",
    "defect_seg_params",
    "defect_seg_execute",
    "defect_seg_validate",
    "defect_seg",
]