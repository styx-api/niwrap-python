# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RECON_ALL_CLINICAL_SH_METADATA = Metadata(
    id="recon-all-clinical.sh",
    name="recon-all-clinical.sh",
    package="freesurfer",
)

ReconAllClinicalSh = typing.TypedDict(
    "ReconAllClinicalSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/recon-all-clinical.sh"]],
        "input_scan": InputPathType,
        "subject_id": str,
        "threads": int,
        "subject_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class ReconAllClinicalShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def recon_all_clinical_sh_params(
    input_scan: InputPathType,
    subject_id: str,
    threads: int,
    subject_dir: str | None = None,
) -> ReconAllClinicalSh:
    """
    Build parameters.

    Args:
        input_scan: Input scan file to be processed.
        subject_id: Identifier for the subject being processed.
        threads: Number of threads to use for processing.
        subject_dir: Optional subjects directory. Only necessary if the environment\
            variable SUBJECTS_DIR is not set or needs to be overridden.

    Returns:
        Parameter dictionary.
    """
    params: ReconAllClinicalSh = {
        "@type": "freesurfer/recon-all-clinical.sh",
        "input_scan": input_scan,
        "subject_id": subject_id,
        "threads": threads,
    }
    if subject_dir is not None:
        params["subject_dir"] = subject_dir
    return params

def recon_all_clinical_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ReconAllClinicalSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_scan", None) is None:
        raise StyxValidationError("`input_scan` must not be None")
    if not isinstance(params["input_scan"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_scan` has the wrong type: Received `{type(params["input_scan"])}` expected `InputPathType`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("threads", None) is None:
        raise StyxValidationError("`threads` must not be None")
    if not isinstance(params["threads"], int):
        raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `int`')
    if params.get("subject_dir", None) is not None:
        if not isinstance(params["subject_dir"], str):
            raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')

def recon_all_clinical_sh_cargs(params: ReconAllClinicalSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("recon-all-clinical.sh")
    cargs.append(execution.input_file(params["input_scan"]))
    cargs.append(params["subject_id"])
    cargs.append(str(params["threads"]))
    v_0 = params.get("subject_dir")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def recon_all_clinical_sh_outputs(params: ReconAllClinicalSh, execution: Execution) -> ReconAllClinicalShOutputs:
    """Build the ReconAllClinicalShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ReconAllClinicalShOutputs(
        root=root_v,
    )

def recon_all_clinical_sh_execute(params: ReconAllClinicalSh, runner: Runner | None = None) -> ReconAllClinicalShOutputs:
    """
    recon-all-clinical.sh

    Recon-all-like stream for processing clinical brain MRI scans of arbitrary orientation, resolution, and contrast using SynthSeg and SynthSR.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    recon_all_clinical_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RECON_ALL_CLINICAL_SH_METADATA)
    execution.params(params)
    args = recon_all_clinical_sh_cargs(params, execution)
    out = recon_all_clinical_sh_outputs(params, execution)
    execution.run(args)
    return out

def recon_all_clinical_sh(
    input_scan: InputPathType,
    subject_id: str,
    threads: int,
    subject_dir: str | None = None,
    runner: Runner | None = None,
) -> ReconAllClinicalShOutputs:
    """
    recon-all-clinical.sh

    Recon-all-like stream for processing clinical brain MRI scans of arbitrary orientation, resolution, and contrast using SynthSeg and SynthSR.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_scan: Input scan file to be processed.
        subject_id: Identifier for the subject being processed.
        threads: Number of threads to use for processing.
        subject_dir: Optional subjects directory. Only necessary if the environment\
            variable SUBJECTS_DIR is not set or needs to be overridden.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = recon_all_clinical_sh_params(
        input_scan=input_scan,
        subject_id=subject_id,
        threads=threads,
        subject_dir=subject_dir,
    )
    return recon_all_clinical_sh_execute(params, runner)

__all__ = [
    "ReconAllClinicalSh",
    "ReconAllClinicalShOutputs",
    "RECON_ALL_CLINICAL_SH_METADATA",
    "recon_all_clinical_sh_cargs",
    "recon_all_clinical_sh_outputs",
    "recon_all_clinical_sh_params",
    "recon_all_clinical_sh_execute",
    "recon_all_clinical_sh_validate",
    "recon_all_clinical_sh",
]