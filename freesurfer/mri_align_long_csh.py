# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_ALIGN_LONG_CSH_METADATA = Metadata(
    id="mri_align_long.csh",
    name="mri_align_long.csh",
    package="freesurfer",
)

MriAlignLongCsh = typing.TypedDict(
    "MriAlignLongCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_align_long.csh"]],
        "base_id": str,
    },
)

@dataclasses.dataclass
class MriAlignLongCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    aligned_norm: OutputPathType
    """Aligned norm volume in base space"""
    aligned_aseg: OutputPathType
    """Aligned aseg volume in base space"""

def mri_align_long_csh_params(
    base_id: str,
) -> MriAlignLongCsh:
    """
    Build parameters.

    Args:
        base_id: Identifier for the base subject

    Returns:
        Parameter dictionary.
    """
    params: MriAlignLongCsh = {
        "@type": "freesurfer/mri_align_long.csh",
        "base_id": base_id,
    }
    return params

def mri_align_long_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAlignLongCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_id", None) is None:
        raise StyxValidationError("`base_id` must not be None")
    if not isinstance(params["base_id"], str):
        raise StyxValidationError(f'`base_id` has the wrong type: Received `{type(params["base_id"])}` expected `str`')

def mri_align_long_csh_cargs(params: MriAlignLongCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_align_long.csh")
    cargs.append(params["base_id"])
    return cargs

def mri_align_long_csh_outputs(params: MriAlignLongCsh, execution: Execution) -> MriAlignLongCshOutputs:
    """Build the MriAlignLongCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    aligned_norm_v: OutputPathType = execution.output_file("[base_id].long.base/mri/norm-base.mgz")
    aligned_aseg_v: OutputPathType = execution.output_file("[base_id].long.base/mri/aseg.base.mgz")
    return MriAlignLongCshOutputs(
        root=root_v,
        aligned_norm=aligned_norm_v,
        aligned_aseg=aligned_aseg_v,
    )

def mri_align_long_csh_execute(params: MriAlignLongCsh, runner: Runner | None = None) -> MriAlignLongCshOutputs:
    """
    mri_align_long.csh

    Aligns all longitudinal norm and aseg files to the base space in FreeSurfer processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_align_long_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_ALIGN_LONG_CSH_METADATA)
    execution.params(params)
    args = mri_align_long_csh_cargs(params, execution)
    out = mri_align_long_csh_outputs(params, execution)
    execution.run(args)
    return out

def mri_align_long_csh(
    base_id: str,
    runner: Runner | None = None,
) -> MriAlignLongCshOutputs:
    """
    mri_align_long.csh

    Aligns all longitudinal norm and aseg files to the base space in FreeSurfer processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_id: Identifier for the base subject
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_align_long_csh_params(
        base_id=base_id,
    )
    return mri_align_long_csh_execute(params, runner)

__all__ = [
    "MriAlignLongCsh",
    "MriAlignLongCshOutputs",
    "MRI_ALIGN_LONG_CSH_METADATA",
    "mri_align_long_csh_cargs",
    "mri_align_long_csh_outputs",
    "mri_align_long_csh_params",
    "mri_align_long_csh_execute",
    "mri_align_long_csh_validate",
    "mri_align_long_csh",
]