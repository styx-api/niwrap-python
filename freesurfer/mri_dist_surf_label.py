# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_DIST_SURF_LABEL_METADATA = Metadata(
    id="mri_dist_surf_label",
    name="mri_dist_surf_label",
    package="freesurfer",
)

MriDistSurfLabel = typing.TypedDict(
    "MriDistSurfLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_dist_surf_label"]],
        "surface": InputPathType,
        "label_file": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class MriDistSurfLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """Output file containing computed distances."""

def mri_dist_surf_label_params(
    surface: InputPathType,
    label_file: InputPathType,
    output: str,
) -> MriDistSurfLabel:
    """
    Build parameters.

    Args:
        surface: Input surface file.
        label_file: Input label file.
        output: Output file for distances.

    Returns:
        Parameter dictionary.
    """
    params: MriDistSurfLabel = {
        "@type": "freesurfer/mri_dist_surf_label",
        "surface": surface,
        "label_file": label_file,
        "output": output,
    }
    return params

def mri_dist_surf_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriDistSurfLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mri_dist_surf_label_cargs(params: MriDistSurfLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_dist_surf_label")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["label_file"]))
    cargs.append(params["output"])
    return cargs

def mri_dist_surf_label_outputs(params: MriDistSurfLabel, execution: Execution) -> MriDistSurfLabelOutputs:
    """Build the MriDistSurfLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MriDistSurfLabelOutputs(
        root=root_v,
        output=output_v,
    )

def mri_dist_surf_label_execute(params: MriDistSurfLabel, runner: Runner | None = None) -> MriDistSurfLabelOutputs:
    """
    mri_dist_surf_label

    Computes distances from input surface to label points or waypoints.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_dist_surf_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_DIST_SURF_LABEL_METADATA)
    execution.params(params)
    args = mri_dist_surf_label_cargs(params, execution)
    out = mri_dist_surf_label_outputs(params, execution)
    execution.run(args)
    return out

def mri_dist_surf_label(
    surface: InputPathType,
    label_file: InputPathType,
    output: str,
    runner: Runner | None = None,
) -> MriDistSurfLabelOutputs:
    """
    mri_dist_surf_label

    Computes distances from input surface to label points or waypoints.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Input surface file.
        label_file: Input label file.
        output: Output file for distances.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_dist_surf_label_params(
        surface=surface,
        label_file=label_file,
        output=output,
    )
    return mri_dist_surf_label_execute(params, runner)

__all__ = [
    "MriDistSurfLabel",
    "MriDistSurfLabelOutputs",
    "MRI_DIST_SURF_LABEL_METADATA",
    "mri_dist_surf_label_cargs",
    "mri_dist_surf_label_outputs",
    "mri_dist_surf_label_params",
    "mri_dist_surf_label_execute",
    "mri_dist_surf_label_validate",
    "mri_dist_surf_label",
]