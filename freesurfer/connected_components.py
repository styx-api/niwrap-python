# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONNECTED_COMPONENTS_METADATA = Metadata(
    id="connected_components",
    name="connected_components",
    package="freesurfer",
)

ConnectedComponents = typing.TypedDict(
    "ConnectedComponents",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/connected_components"]],
        "input_image": InputPathType,
        "output_image": typing.NotRequired[str],
        "threshold": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class ConnectedComponentsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_labelled_image_file: OutputPathType
    """Labeled connected components image output file."""

def connected_components_params(
    input_image: InputPathType,
    output_image: str = "output_labelled_image",
    threshold: float | None = None,
) -> ConnectedComponents:
    """
    Build parameters.

    Args:
        input_image: Input image file.
        output_image: Output labeled connected components image.
        threshold: Threshold for binarizing the input image.

    Returns:
        Parameter dictionary.
    """
    params: ConnectedComponents = {
        "@type": "freesurfer/connected_components",
        "input_image": input_image,
        "output_image": output_image,
    }
    if threshold is not None:
        params["threshold"] = threshold
    return params

def connected_components_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ConnectedComponents."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')

def connected_components_cargs(params: ConnectedComponents, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("connected_components")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("-o")
    cargs.append(params.get("output_image", "output_labelled_image"))
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(str(v_0))
    return cargs

def connected_components_outputs(params: ConnectedComponents, execution: Execution) -> ConnectedComponentsOutputs:
    """Build the ConnectedComponentsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_labelled_image_file_v: OutputPathType = execution.output_file(f'{params.get("output_image", "output_labelled_image")}.nii.gz')
    return ConnectedComponentsOutputs(
        root=root_v,
        output_labelled_image_file=output_labelled_image_file_v,
    )

def connected_components_execute(params: ConnectedComponents, runner: Runner | None = None) -> ConnectedComponentsOutputs:
    """
    connected_components

    A tool for identifying connected components in an image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    connected_components_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONNECTED_COMPONENTS_METADATA)
    execution.params(params)
    args = connected_components_cargs(params, execution)
    out = connected_components_outputs(params, execution)
    execution.run(args)
    return out

def connected_components(
    input_image: InputPathType,
    output_image: str = "output_labelled_image",
    threshold: float | None = None,
    runner: Runner | None = None,
) -> ConnectedComponentsOutputs:
    """
    connected_components

    A tool for identifying connected components in an image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input image file.
        output_image: Output labeled connected components image.
        threshold: Threshold for binarizing the input image.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = connected_components_params(
        input_image=input_image,
        output_image=output_image,
        threshold=threshold,
    )
    return connected_components_execute(params, runner)

__all__ = [
    "ConnectedComponents",
    "ConnectedComponentsOutputs",
    "CONNECTED_COMPONENTS_METADATA",
    "connected_components_cargs",
    "connected_components_outputs",
    "connected_components_params",
    "connected_components_execute",
    "connected_components_validate",
    "connected_components",
]