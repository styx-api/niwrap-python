# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ICO_SUPERSAMPLE_METADATA = Metadata(
    id="ico_supersample",
    name="ico_supersample",
    package="freesurfer",
)

IcoSupersample = typing.TypedDict(
    "IcoSupersample",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/ico_supersample"]],
        "refine": typing.NotRequired[bool],
        "radius": typing.NotRequired[float],
        "projection_point": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class IcoSupersampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The output file containing the refined icosahedron mesh."""

def ico_supersample_params(
    refine: bool = False,
    radius: float | None = None,
    projection_point: list[float] | None = None,
) -> IcoSupersample:
    """
    Build parameters.

    Args:
        refine: Refine the icosahedron mesh
        radius: Radius of the sphere onto which the icosahedron is projected
        projection_point: Projection point for the icosahedron refinement. Enter\
            three floating point values separated by spaces.

    Returns:
        Parameter dictionary.
    """
    params: IcoSupersample = {
        "@type": "freesurfer/ico_supersample",
        "refine": refine,
    }
    if radius is not None:
        params["radius"] = radius
    if projection_point is not None:
        params["projection_point"] = projection_point
    return params

def ico_supersample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IcoSupersample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("refine", None) is not None:
        if not isinstance(params["refine"], bool):
            raise StyxValidationError(f'`refine` has the wrong type: Received `{type(params["refine"])}` expected `bool`')
    if params.get("radius", None) is not None:
        if not isinstance(params["radius"], (float, int)):
            raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')
    if params.get("projection_point", None) is not None:
        if not isinstance(params["projection_point"], list):
            raise StyxValidationError(f'`projection_point` has the wrong type: Received `{type(params["projection_point"])}` expected `list[float]`')
        for e in params["projection_point"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`projection_point` has the wrong type: Received `{type(e)}` expected `list[float]`')

def ico_supersample_cargs(params: IcoSupersample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ico_supersample")
    if params.get("refine"):
        cargs.append("-y")
    v_0 = params.get("radius")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("projection_point")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append(str(item0))
    return cargs

def ico_supersample_outputs(params: IcoSupersample, execution: Execution) -> IcoSupersampleOutputs:
    """Build the IcoSupersampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("refined_icosahedron.obj")
    return IcoSupersampleOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def ico_supersample_execute(params: IcoSupersample, runner: Runner | None = None) -> IcoSupersampleOutputs:
    """
    ico_supersample

    A tool for refining icosahedron meshes with user-specified parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ico_supersample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ICO_SUPERSAMPLE_METADATA)
    execution.params(params)
    args = ico_supersample_cargs(params, execution)
    out = ico_supersample_outputs(params, execution)
    execution.run(args)
    return out

def ico_supersample(
    refine: bool = False,
    radius: float | None = None,
    projection_point: list[float] | None = None,
    runner: Runner | None = None,
) -> IcoSupersampleOutputs:
    """
    ico_supersample

    A tool for refining icosahedron meshes with user-specified parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        refine: Refine the icosahedron mesh
        radius: Radius of the sphere onto which the icosahedron is projected
        projection_point: Projection point for the icosahedron refinement. Enter\
            three floating point values separated by spaces.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ico_supersample_params(
        refine=refine,
        radius=radius,
        projection_point=projection_point,
    )
    return ico_supersample_execute(params, runner)

__all__ = [
    "IcoSupersample",
    "IcoSupersampleOutputs",
    "ICO_SUPERSAMPLE_METADATA",
    "ico_supersample_cargs",
    "ico_supersample_outputs",
    "ico_supersample_params",
    "ico_supersample_execute",
    "ico_supersample_validate",
    "ico_supersample",
]