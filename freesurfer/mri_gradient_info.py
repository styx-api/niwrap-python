# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GRADIENT_INFO_METADATA = Metadata(
    id="mri_gradient_info",
    name="mri_gradient_info",
    package="freesurfer",
)

MriGradientInfo = typing.TypedDict(
    "MriGradientInfo",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gradient_info"]],
        "input_image": InputPathType,
    },
)

@dataclasses.dataclass
class MriGradientInfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    gradient_info_output: OutputPathType
    """Text file containing the extracted gradient information."""

def mri_gradient_info_params(
    input_image: InputPathType,
) -> MriGradientInfo:
    """
    Build parameters.

    Args:
        input_image: Input MRI image file, typically in .mgz format

    Returns:
        Parameter dictionary.
    """
    params: MriGradientInfo = {
        "@type": "freesurfer/mri_gradient_info",
        "input_image": input_image,
    }
    return params

def mri_gradient_info_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGradientInfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')

def mri_gradient_info_cargs(params: MriGradientInfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gradient_info")
    cargs.append(execution.input_file(params["input_image"]))
    return cargs

def mri_gradient_info_outputs(params: MriGradientInfo, execution: Execution) -> MriGradientInfoOutputs:
    """Build the MriGradientInfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    gradient_info_output_v: OutputPathType = execution.output_file("gradient_info_output.txt")
    return MriGradientInfoOutputs(
        root=root_v,
        gradient_info_output=gradient_info_output_v,
    )

def mri_gradient_info_execute(params: MriGradientInfo, runner: Runner | None = None) -> MriGradientInfoOutputs:
    """
    mri_gradient_info

    A utility to obtain gradient information from MRI images using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gradient_info_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GRADIENT_INFO_METADATA)
    execution.params(params)
    args = mri_gradient_info_cargs(params, execution)
    out = mri_gradient_info_outputs(params, execution)
    execution.run(args)
    return out

def mri_gradient_info(
    input_image: InputPathType,
    runner: Runner | None = None,
) -> MriGradientInfoOutputs:
    """
    mri_gradient_info

    A utility to obtain gradient information from MRI images using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input MRI image file, typically in .mgz format
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gradient_info_params(
        input_image=input_image,
    )
    return mri_gradient_info_execute(params, runner)

__all__ = [
    "MriGradientInfo",
    "MriGradientInfoOutputs",
    "MRI_GRADIENT_INFO_METADATA",
    "mri_gradient_info_cargs",
    "mri_gradient_info_outputs",
    "mri_gradient_info_params",
    "mri_gradient_info_execute",
    "mri_gradient_info_validate",
    "mri_gradient_info",
]