# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INFLATE_SUBJECT_SC_METADATA = Metadata(
    id="inflate_subject_sc",
    name="inflate_subject_sc",
    package="freesurfer",
)

InflateSubjectSc = typing.TypedDict(
    "InflateSubjectSc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/inflate_subject_sc"]],
        "subject_dir": str,
        "verbose": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class InflateSubjectScOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    inflated_output: OutputPathType
    """Inflated subject surface output."""

def inflate_subject_sc_params(
    subject_dir: str,
    verbose: bool = False,
    debug: bool = False,
) -> InflateSubjectSc:
    """
    Build parameters.

    Args:
        subject_dir: Path to the subject directory.
        verbose: Enable verbose output.
        debug: Enable debug mode.

    Returns:
        Parameter dictionary.
    """
    params: InflateSubjectSc = {
        "@type": "freesurfer/inflate_subject_sc",
        "subject_dir": subject_dir,
        "verbose": verbose,
        "debug": debug,
    }
    return params

def inflate_subject_sc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid InflateSubjectSc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def inflate_subject_sc_cargs(params: InflateSubjectSc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("inflate_subject_sc")
    cargs.append(params["subject_dir"])
    if params.get("verbose"):
        cargs.append("--verbose")
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def inflate_subject_sc_outputs(params: InflateSubjectSc, execution: Execution) -> InflateSubjectScOutputs:
    """Build the InflateSubjectScOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    inflated_output_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}/inflated_output')
    return InflateSubjectScOutputs(
        root=root_v,
        inflated_output=inflated_output_v,
    )

def inflate_subject_sc_execute(params: InflateSubjectSc, runner: Runner | None = None) -> InflateSubjectScOutputs:
    """
    inflate_subject_sc

    Inferred description: Tool for inflating subject surfaces, specific details unavailable.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    inflate_subject_sc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INFLATE_SUBJECT_SC_METADATA)
    execution.params(params)
    args = inflate_subject_sc_cargs(params, execution)
    out = inflate_subject_sc_outputs(params, execution)
    execution.run(args)
    return out

def inflate_subject_sc(
    subject_dir: str,
    verbose: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> InflateSubjectScOutputs:
    """
    inflate_subject_sc

    Inferred description: Tool for inflating subject surfaces, specific details unavailable.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Path to the subject directory.
        verbose: Enable verbose output.
        debug: Enable debug mode.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = inflate_subject_sc_params(
        subject_dir=subject_dir,
        verbose=verbose,
        debug=debug,
    )
    return inflate_subject_sc_execute(params, runner)

__all__ = [
    "InflateSubjectSc",
    "InflateSubjectScOutputs",
    "INFLATE_SUBJECT_SC_METADATA",
    "inflate_subject_sc_cargs",
    "inflate_subject_sc_outputs",
    "inflate_subject_sc_params",
    "inflate_subject_sc_execute",
    "inflate_subject_sc_validate",
    "inflate_subject_sc",
]