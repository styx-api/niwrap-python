# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FUSE_INTENSITY_IMAGES_METADATA = Metadata(
    id="mri_fuse_intensity_images",
    name="mri_fuse_intensity_images",
    package="freesurfer",
)

MriFuseIntensityImages = typing.TypedDict(
    "MriFuseIntensityImages",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_fuse_intensity_images"]],
        "longitudinal_time_point_file": InputPathType,
        "input_volume": InputPathType,
        "transform_file": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriFuseIntensityImagesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fused_intensity_image: OutputPathType
    """The resulting fused intensity image"""

def mri_fuse_intensity_images_params(
    longitudinal_time_point_file: InputPathType,
    input_volume: InputPathType,
    transform_file: InputPathType,
    output_volume: str,
) -> MriFuseIntensityImages:
    """
    Build parameters.

    Args:
        longitudinal_time_point_file: File containing the longitudinal time points
        input_volume: Input volume to be fused
        transform_file: File containing the transforms
        output_volume: Output fused volume

    Returns:
        Parameter dictionary.
    """
    params: MriFuseIntensityImages = {
        "@type": "freesurfer/mri_fuse_intensity_images",
        "longitudinal_time_point_file": longitudinal_time_point_file,
        "input_volume": input_volume,
        "transform_file": transform_file,
        "output_volume": output_volume,
    }
    return params

def mri_fuse_intensity_images_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFuseIntensityImages."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("longitudinal_time_point_file", None) is None:
        raise StyxValidationError("`longitudinal_time_point_file` must not be None")
    if not isinstance(params["longitudinal_time_point_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`longitudinal_time_point_file` has the wrong type: Received `{type(params["longitudinal_time_point_file"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_fuse_intensity_images_cargs(params: MriFuseIntensityImages, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_fuse_intensity_images")
    cargs.append(execution.input_file(params["longitudinal_time_point_file"]))
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_fuse_intensity_images_outputs(params: MriFuseIntensityImages, execution: Execution) -> MriFuseIntensityImagesOutputs:
    """Build the MriFuseIntensityImagesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fused_intensity_image_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriFuseIntensityImagesOutputs(
        root=root_v,
        fused_intensity_image=fused_intensity_image_v,
    )

def mri_fuse_intensity_images_execute(params: MriFuseIntensityImages, runner: Runner | None = None) -> MriFuseIntensityImagesOutputs:
    """
    mri_fuse_intensity_images

    Fuses intensity images based on given transforms

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_fuse_intensity_images_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FUSE_INTENSITY_IMAGES_METADATA)
    execution.params(params)
    args = mri_fuse_intensity_images_cargs(params, execution)
    out = mri_fuse_intensity_images_outputs(params, execution)
    execution.run(args)
    return out

def mri_fuse_intensity_images(
    longitudinal_time_point_file: InputPathType,
    input_volume: InputPathType,
    transform_file: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriFuseIntensityImagesOutputs:
    """
    mri_fuse_intensity_images

    Fuses intensity images based on given transforms

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        longitudinal_time_point_file: File containing the longitudinal time points
        input_volume: Input volume to be fused
        transform_file: File containing the transforms
        output_volume: Output fused volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_fuse_intensity_images_params(
        longitudinal_time_point_file=longitudinal_time_point_file,
        input_volume=input_volume,
        transform_file=transform_file,
        output_volume=output_volume,
    )
    return mri_fuse_intensity_images_execute(params, runner)

__all__ = [
    "MriFuseIntensityImages",
    "MriFuseIntensityImagesOutputs",
    "MRI_FUSE_INTENSITY_IMAGES_METADATA",
    "mri_fuse_intensity_images_cargs",
    "mri_fuse_intensity_images_outputs",
    "mri_fuse_intensity_images_params",
    "mri_fuse_intensity_images_execute",
    "mri_fuse_intensity_images_validate",
    "mri_fuse_intensity_images",
]