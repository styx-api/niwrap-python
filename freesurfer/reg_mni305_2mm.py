# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_MNI305_2MM_METADATA = Metadata(
    id="reg-mni305.2mm",
    name="reg-mni305.2mm",
    package="freesurfer",
)

RegMni3052mm = typing.TypedDict(
    "RegMni3052mm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reg-mni305.2mm"]],
        "subject_id": str,
        "regfile": InputPathType,
    },
)

@dataclasses.dataclass
class RegMni3052mmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_regfile: OutputPathType
    """tkregister2-style registration matrix output file."""

def reg_mni305_2mm_params(
    subject_id: str,
    regfile: InputPathType,
) -> RegMni3052mm:
    """
    Build parameters.

    Args:
        subject_id:
        regfile:

    Returns:
        Parameter dictionary.
    """
    params: RegMni3052mm = {
        "@type": "freesurfer/reg-mni305.2mm",
        "subject_id": subject_id,
        "regfile": regfile,
    }
    return params

def reg_mni305_2mm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegMni3052mm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("regfile", None) is None:
        raise StyxValidationError("`regfile` must not be None")
    if not isinstance(params["regfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`regfile` has the wrong type: Received `{type(params["regfile"])}` expected `InputPathType`')

def reg_mni305_2mm_cargs(params: RegMni3052mm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg-mni305.2mm")
    cargs.append("--s")
    cargs.append(params["subject_id"])
    cargs.append("--reg")
    cargs.append(execution.input_file(params["regfile"]))
    return cargs

def reg_mni305_2mm_outputs(params: RegMni3052mm, execution: Execution) -> RegMni3052mmOutputs:
    """Build the RegMni3052mmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_regfile_v: OutputPathType = execution.output_file(params["regfile"])
    return RegMni3052mmOutputs(
        root=root_v,
        output_regfile=output_regfile_v,
    )

def reg_mni305_2mm_execute(params: RegMni3052mm, runner: Runner | None = None) -> RegMni3052mmOutputs:
    """
    reg-mni305.2mm

    Computes the registration between the FreeSurfer MNI305 2mm space and a subject's FreeSurfer anatomical space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_mni305_2mm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_MNI305_2MM_METADATA)
    execution.params(params)
    args = reg_mni305_2mm_cargs(params, execution)
    out = reg_mni305_2mm_outputs(params, execution)
    execution.run(args)
    return out

def reg_mni305_2mm(
    subject_id: str,
    regfile: InputPathType,
    runner: Runner | None = None,
) -> RegMni3052mmOutputs:
    """
    reg-mni305.2mm

    Computes the registration between the FreeSurfer MNI305 2mm space and a subject's FreeSurfer anatomical space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id:
        regfile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_mni305_2mm_params(
        subject_id=subject_id,
        regfile=regfile,
    )
    return reg_mni305_2mm_execute(params, runner)

__all__ = [
    "RegMni3052mm",
    "RegMni3052mmOutputs",
    "REG_MNI305_2MM_METADATA",
    "reg_mni305_2mm_cargs",
    "reg_mni305_2mm_outputs",
    "reg_mni305_2mm_params",
    "reg_mni305_2mm_execute",
    "reg_mni305_2mm_validate",
    "reg_mni305_2mm",
]