# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_JACOBIAN_METADATA = Metadata(
    id="mri_jacobian",
    name="mri_jacobian",
    package="freesurfer",
)

MriJacobian = typing.TypedDict(
    "MriJacobian",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_jacobian"]],
        "morph_file": InputPathType,
        "template_vol": InputPathType,
        "output_vol": str,
        "atlas_coord": typing.NotRequired[bool],
        "write_area_vols": typing.NotRequired[bool],
        "log_jacobian": typing.NotRequired[bool],
        "smooth_sigma": typing.NotRequired[float],
        "zero_mean_log": typing.NotRequired[bool],
        "tm3d": typing.NotRequired[bool],
        "help1": typing.NotRequired[bool],
        "help2": typing.NotRequired[bool],
        "dt": typing.NotRequired[bool],
        "debug_voxel": typing.NotRequired[list[float]],
        "remove": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriJacobianOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_vol: OutputPathType
    """Resulting volume file after Jacobian computation"""

def mri_jacobian_params(
    morph_file: InputPathType,
    template_vol: InputPathType,
    output_vol: str,
    atlas_coord: bool = False,
    write_area_vols: bool = False,
    log_jacobian: bool = False,
    smooth_sigma: float | None = None,
    zero_mean_log: bool = False,
    tm3d: bool = False,
    help1: bool = False,
    help2: bool = False,
    dt: bool = False,
    debug_voxel: list[float] | None = None,
    remove: bool = False,
) -> MriJacobian:
    """
    Build parameters.

    Args:
        morph_file: 3D morph input file
        template_vol: Template volume file
        output_vol: Output volume file
        atlas_coord: Output is written in atlas coordinate system
        write_area_vols: Writing area volumes
        log_jacobian: Taking log of Jacobian values before saving
        smooth_sigma: Smoothing Jacobian volume with sigma
        zero_mean_log: Make log Jacobian zero mean
        tm3d: The input morph (m3z) originated from tm3d (mri_cvs_register)
        help1: Writing out help
        help2: Writing out help
        dt: DT option (description not provided in help text)
        debug_voxel: Debug voxel with specified Gx, Gy, Gz coordinates
        remove: Remove option (description not provided in help text)

    Returns:
        Parameter dictionary.
    """
    params: MriJacobian = {
        "@type": "freesurfer/mri_jacobian",
        "morph_file": morph_file,
        "template_vol": template_vol,
        "output_vol": output_vol,
        "atlas_coord": atlas_coord,
        "write_area_vols": write_area_vols,
        "log_jacobian": log_jacobian,
        "zero_mean_log": zero_mean_log,
        "tm3d": tm3d,
        "help1": help1,
        "help2": help2,
        "dt": dt,
        "remove": remove,
    }
    if smooth_sigma is not None:
        params["smooth_sigma"] = smooth_sigma
    if debug_voxel is not None:
        params["debug_voxel"] = debug_voxel
    return params

def mri_jacobian_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriJacobian."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("morph_file", None) is None:
        raise StyxValidationError("`morph_file` must not be None")
    if not isinstance(params["morph_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`morph_file` has the wrong type: Received `{type(params["morph_file"])}` expected `InputPathType`')
    if params.get("template_vol", None) is None:
        raise StyxValidationError("`template_vol` must not be None")
    if not isinstance(params["template_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_vol` has the wrong type: Received `{type(params["template_vol"])}` expected `InputPathType`')
    if params.get("output_vol", None) is None:
        raise StyxValidationError("`output_vol` must not be None")
    if not isinstance(params["output_vol"], str):
        raise StyxValidationError(f'`output_vol` has the wrong type: Received `{type(params["output_vol"])}` expected `str`')
    if params.get("atlas_coord", None) is not None:
        if not isinstance(params["atlas_coord"], bool):
            raise StyxValidationError(f'`atlas_coord` has the wrong type: Received `{type(params["atlas_coord"])}` expected `bool`')
    if params.get("write_area_vols", None) is not None:
        if not isinstance(params["write_area_vols"], bool):
            raise StyxValidationError(f'`write_area_vols` has the wrong type: Received `{type(params["write_area_vols"])}` expected `bool`')
    if params.get("log_jacobian", None) is not None:
        if not isinstance(params["log_jacobian"], bool):
            raise StyxValidationError(f'`log_jacobian` has the wrong type: Received `{type(params["log_jacobian"])}` expected `bool`')
    if params.get("smooth_sigma", None) is not None:
        if not isinstance(params["smooth_sigma"], (float, int)):
            raise StyxValidationError(f'`smooth_sigma` has the wrong type: Received `{type(params["smooth_sigma"])}` expected `float`')
    if params.get("zero_mean_log", None) is not None:
        if not isinstance(params["zero_mean_log"], bool):
            raise StyxValidationError(f'`zero_mean_log` has the wrong type: Received `{type(params["zero_mean_log"])}` expected `bool`')
    if params.get("tm3d", None) is not None:
        if not isinstance(params["tm3d"], bool):
            raise StyxValidationError(f'`tm3d` has the wrong type: Received `{type(params["tm3d"])}` expected `bool`')
    if params.get("help1", None) is not None:
        if not isinstance(params["help1"], bool):
            raise StyxValidationError(f'`help1` has the wrong type: Received `{type(params["help1"])}` expected `bool`')
    if params.get("help2", None) is not None:
        if not isinstance(params["help2"], bool):
            raise StyxValidationError(f'`help2` has the wrong type: Received `{type(params["help2"])}` expected `bool`')
    if params.get("dt", None) is not None:
        if not isinstance(params["dt"], bool):
            raise StyxValidationError(f'`dt` has the wrong type: Received `{type(params["dt"])}` expected `bool`')
    if params.get("debug_voxel", None) is not None:
        if not isinstance(params["debug_voxel"], list):
            raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(params["debug_voxel"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel` must contain between 3 and 3 elements (inclusive)")
        for e in params["debug_voxel"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("remove", None) is not None:
        if not isinstance(params["remove"], bool):
            raise StyxValidationError(f'`remove` has the wrong type: Received `{type(params["remove"])}` expected `bool`')

def mri_jacobian_cargs(params: MriJacobian, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_jacobian")
    cargs.append(execution.input_file(params["morph_file"]))
    cargs.append(execution.input_file(params["template_vol"]))
    cargs.append(params["output_vol"])
    if params.get("atlas_coord"):
        cargs.append("-a")
    if params.get("write_area_vols"):
        cargs.append("-w")
    if params.get("log_jacobian"):
        cargs.append("-l")
    v_0 = params.get("smooth_sigma")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(str(v_0))
    if params.get("zero_mean_log"):
        cargs.append("-z")
    if params.get("tm3d"):
        cargs.append("-tm3d")
    if params.get("help1"):
        cargs.append("-?")
    if params.get("help2"):
        cargs.append("-u")
    if params.get("dt"):
        cargs.append("-dt")
    v_1 = params.get("debug_voxel")
    if v_1 is not None:
        cargs.append("-debug_voxel")
        for item0 in v_1:
            cargs.append(str(item0))
    if params.get("remove"):
        cargs.append("-remove")
    return cargs

def mri_jacobian_outputs(params: MriJacobian, execution: Execution) -> MriJacobianOutputs:
    """Build the MriJacobianOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_vol_v: OutputPathType = execution.output_file(params["output_vol"])
    return MriJacobianOutputs(
        root=root_v,
        result_vol=result_vol_v,
    )

def mri_jacobian_execute(params: MriJacobian, runner: Runner | None = None) -> MriJacobianOutputs:
    """
    mri_jacobian

    Compute the Jacobian of a morph with FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_jacobian_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_JACOBIAN_METADATA)
    execution.params(params)
    args = mri_jacobian_cargs(params, execution)
    out = mri_jacobian_outputs(params, execution)
    execution.run(args)
    return out

def mri_jacobian(
    morph_file: InputPathType,
    template_vol: InputPathType,
    output_vol: str,
    atlas_coord: bool = False,
    write_area_vols: bool = False,
    log_jacobian: bool = False,
    smooth_sigma: float | None = None,
    zero_mean_log: bool = False,
    tm3d: bool = False,
    help1: bool = False,
    help2: bool = False,
    dt: bool = False,
    debug_voxel: list[float] | None = None,
    remove: bool = False,
    runner: Runner | None = None,
) -> MriJacobianOutputs:
    """
    mri_jacobian

    Compute the Jacobian of a morph with FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        morph_file: 3D morph input file
        template_vol: Template volume file
        output_vol: Output volume file
        atlas_coord: Output is written in atlas coordinate system
        write_area_vols: Writing area volumes
        log_jacobian: Taking log of Jacobian values before saving
        smooth_sigma: Smoothing Jacobian volume with sigma
        zero_mean_log: Make log Jacobian zero mean
        tm3d: The input morph (m3z) originated from tm3d (mri_cvs_register)
        help1: Writing out help
        help2: Writing out help
        dt: DT option (description not provided in help text)
        debug_voxel: Debug voxel with specified Gx, Gy, Gz coordinates
        remove: Remove option (description not provided in help text)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_jacobian_params(
        morph_file=morph_file,
        template_vol=template_vol,
        output_vol=output_vol,
        atlas_coord=atlas_coord,
        write_area_vols=write_area_vols,
        log_jacobian=log_jacobian,
        smooth_sigma=smooth_sigma,
        zero_mean_log=zero_mean_log,
        tm3d=tm3d,
        help1=help1,
        help2=help2,
        dt=dt,
        debug_voxel=debug_voxel,
        remove=remove,
    )
    return mri_jacobian_execute(params, runner)

__all__ = [
    "MriJacobian",
    "MriJacobianOutputs",
    "MRI_JACOBIAN_METADATA",
    "mri_jacobian_cargs",
    "mri_jacobian_outputs",
    "mri_jacobian_params",
    "mri_jacobian_execute",
    "mri_jacobian_validate",
    "mri_jacobian",
]