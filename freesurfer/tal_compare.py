# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TAL_COMPARE_METADATA = Metadata(
    id="tal_compare",
    name="tal_compare",
    package="freesurfer",
)

TalCompare = typing.TypedDict(
    "TalCompare",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tal_compare"]],
        "ref_file": InputPathType,
        "moving_file": InputPathType,
        "output_file": str,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TalCompareOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    comparison_results: OutputPathType
    """Comparison results output file."""

def tal_compare_params(
    ref_file: InputPathType,
    moving_file: InputPathType,
    output_file: str,
    verbose: bool = False,
) -> TalCompare:
    """
    Build parameters.

    Args:
        ref_file: Reference TAL database file.
        moving_file: Moving TAL database file to compare against the reference.
        output_file: Output file to store comparison results.
        verbose: Enable verbose output.

    Returns:
        Parameter dictionary.
    """
    params: TalCompare = {
        "@type": "freesurfer/tal_compare",
        "ref_file": ref_file,
        "moving_file": moving_file,
        "output_file": output_file,
        "verbose": verbose,
    }
    return params

def tal_compare_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TalCompare."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ref_file", None) is None:
        raise StyxValidationError("`ref_file` must not be None")
    if not isinstance(params["ref_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ref_file` has the wrong type: Received `{type(params["ref_file"])}` expected `InputPathType`')
    if params.get("moving_file", None) is None:
        raise StyxValidationError("`moving_file` must not be None")
    if not isinstance(params["moving_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_file` has the wrong type: Received `{type(params["moving_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def tal_compare_cargs(params: TalCompare, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tal_compare")
    cargs.append(execution.input_file(params["ref_file"]))
    cargs.append(execution.input_file(params["moving_file"]))
    cargs.append(params["output_file"])
    if params.get("verbose"):
        cargs.append("-v")
    return cargs

def tal_compare_outputs(params: TalCompare, execution: Execution) -> TalCompareOutputs:
    """Build the TalCompareOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    comparison_results_v: OutputPathType = execution.output_file(params["output_file"])
    return TalCompareOutputs(
        root=root_v,
        comparison_results=comparison_results_v,
    )

def tal_compare_execute(params: TalCompare, runner: Runner | None = None) -> TalCompareOutputs:
    """
    tal_compare

    Tool for comparing TAL databases

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tal_compare_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TAL_COMPARE_METADATA)
    execution.params(params)
    args = tal_compare_cargs(params, execution)
    out = tal_compare_outputs(params, execution)
    execution.run(args)
    return out

def tal_compare(
    ref_file: InputPathType,
    moving_file: InputPathType,
    output_file: str,
    verbose: bool = False,
    runner: Runner | None = None,
) -> TalCompareOutputs:
    """
    tal_compare

    Tool for comparing TAL databases

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        ref_file: Reference TAL database file.
        moving_file: Moving TAL database file to compare against the reference.
        output_file: Output file to store comparison results.
        verbose: Enable verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tal_compare_params(
        ref_file=ref_file,
        moving_file=moving_file,
        output_file=output_file,
        verbose=verbose,
    )
    return tal_compare_execute(params, runner)

__all__ = [
    "TalCompare",
    "TalCompareOutputs",
    "TAL_COMPARE_METADATA",
    "tal_compare_cargs",
    "tal_compare_outputs",
    "tal_compare_params",
    "tal_compare_execute",
    "tal_compare_validate",
    "tal_compare",
]