# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ORIENT_LAS_METADATA = Metadata(
    id="orientLAS",
    name="orientLAS",
    package="freesurfer",
)

OrientLas = typing.TypedDict(
    "OrientLas",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/orientLAS"]],
        "input_image": InputPathType,
        "output_image": str,
        "check": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class OrientLasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_las_image: OutputPathType
    """Output image with LAS orientation"""

def orient_las_params(
    input_image: InputPathType,
    output_image: str,
    check: bool = False,
) -> OrientLas:
    """
    Build parameters.

    Args:
        input_image: Input image in NIfTI format
        output_image: Output image in NIfTI format with LAS orientation
        check: Check the match of input and output images using tkregister, and for\
            diffusion data, run dtifit and show tensors with fslview

    Returns:
        Parameter dictionary.
    """
    params: OrientLas = {
        "@type": "freesurfer/orientLAS",
        "input_image": input_image,
        "output_image": output_image,
        "check": check,
    }
    return params

def orient_las_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid OrientLas."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("check", None) is not None:
        if not isinstance(params["check"], bool):
            raise StyxValidationError(f'`check` has the wrong type: Received `{type(params["check"])}` expected `bool`')

def orient_las_cargs(params: OrientLas, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("orientLAS")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    if params.get("check"):
        cargs.append("--check")
    return cargs

def orient_las_outputs(params: OrientLas, execution: Execution) -> OrientLasOutputs:
    """Build the OrientLasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_las_image_v: OutputPathType = execution.output_file(params["output_image"])
    return OrientLasOutputs(
        root=root_v,
        output_las_image=output_las_image_v,
    )

def orient_las_execute(params: OrientLas, runner: Runner | None = None) -> OrientLasOutputs:
    """
    orientLAS

    Convert image to LAS orientation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    orient_las_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ORIENT_LAS_METADATA)
    execution.params(params)
    args = orient_las_cargs(params, execution)
    out = orient_las_outputs(params, execution)
    execution.run(args)
    return out

def orient_las(
    input_image: InputPathType,
    output_image: str,
    check: bool = False,
    runner: Runner | None = None,
) -> OrientLasOutputs:
    """
    orientLAS

    Convert image to LAS orientation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input image in NIfTI format
        output_image: Output image in NIfTI format with LAS orientation
        check: Check the match of input and output images using tkregister, and for\
            diffusion data, run dtifit and show tensors with fslview
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = orient_las_params(
        input_image=input_image,
        output_image=output_image,
        check=check,
    )
    return orient_las_execute(params, runner)

__all__ = [
    "OrientLas",
    "OrientLasOutputs",
    "ORIENT_LAS_METADATA",
    "orient_las_cargs",
    "orient_las_outputs",
    "orient_las_params",
    "orient_las_execute",
    "orient_las_validate",
    "orient_las",
]