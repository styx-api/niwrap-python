# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DIFFUSION_UTILS_METADATA = Metadata(
    id="diffusionUtils",
    name="diffusionUtils",
    package="freesurfer",
)

DiffusionUtils = typing.TypedDict(
    "DiffusionUtils",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/diffusionUtils"]],
        "dummy_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DiffusionUtilsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def diffusion_utils_params(
    dummy_flag: bool = False,
) -> DiffusionUtils:
    """
    Build parameters.

    Args:
        dummy_flag: Dummy input as no valid help information is provided due to\
            missing module.

    Returns:
        Parameter dictionary.
    """
    params: DiffusionUtils = {
        "@type": "freesurfer/diffusionUtils",
        "dummy_flag": dummy_flag,
    }
    return params

def diffusion_utils_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DiffusionUtils."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dummy_flag", None) is not None:
        if not isinstance(params["dummy_flag"], bool):
            raise StyxValidationError(f'`dummy_flag` has the wrong type: Received `{type(params["dummy_flag"])}` expected `bool`')

def diffusion_utils_cargs(params: DiffusionUtils, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("diffusionUtils")
    if params.get("dummy_flag"):
        cargs.append("--dummy")
    return cargs

def diffusion_utils_outputs(params: DiffusionUtils, execution: Execution) -> DiffusionUtilsOutputs:
    """Build the DiffusionUtilsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DiffusionUtilsOutputs(
        root=root_v,
    )

def diffusion_utils_execute(params: DiffusionUtils, runner: Runner | None = None) -> DiffusionUtilsOutputs:
    """
    diffusionUtils

    A utility related to diffusion data, potentially using the DIPY library.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    diffusion_utils_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DIFFUSION_UTILS_METADATA)
    execution.params(params)
    args = diffusion_utils_cargs(params, execution)
    out = diffusion_utils_outputs(params, execution)
    execution.run(args)
    return out

def diffusion_utils(
    dummy_flag: bool = False,
    runner: Runner | None = None,
) -> DiffusionUtilsOutputs:
    """
    diffusionUtils

    A utility related to diffusion data, potentially using the DIPY library.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dummy_flag: Dummy input as no valid help information is provided due to\
            missing module.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = diffusion_utils_params(
        dummy_flag=dummy_flag,
    )
    return diffusion_utils_execute(params, runner)

__all__ = [
    "DiffusionUtils",
    "DiffusionUtilsOutputs",
    "DIFFUSION_UTILS_METADATA",
    "diffusion_utils_cargs",
    "diffusion_utils_outputs",
    "diffusion_utils_params",
    "diffusion_utils_execute",
    "diffusion_utils_validate",
    "diffusion_utils",
]