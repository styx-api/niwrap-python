# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_NUDGE_METADATA = Metadata(
    id="mris_nudge",
    name="mris_nudge",
    package="freesurfer",
)

MrisNudge = typing.TypedDict(
    "MrisNudge",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_nudge"]],
        "input_surface": InputPathType,
        "input_volume": InputPathType,
        "vertex": int,
        "target_val": float,
        "nbhd": int,
    },
)

@dataclasses.dataclass
class MrisNudgeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType
    """Output surface file after nudging"""

def mris_nudge_params(
    input_surface: InputPathType,
    input_volume: InputPathType,
    vertex: int,
    target_val: float,
    nbhd: int,
) -> MrisNudge:
    """
    Build parameters.

    Args:
        input_surface: Input surface file for nudging
        input_volume: Input volume file
        vertex: Vertex to nudge
        target_val: Target value for nudging
        nbhd: Neighborhood size for nudge operation

    Returns:
        Parameter dictionary.
    """
    params: MrisNudge = {
        "@type": "freesurfer/mris_nudge",
        "input_surface": input_surface,
        "input_volume": input_volume,
        "vertex": vertex,
        "target_val": target_val,
        "nbhd": nbhd,
    }
    return params

def mris_nudge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisNudge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("vertex", None) is None:
        raise StyxValidationError("`vertex` must not be None")
    if not isinstance(params["vertex"], int):
        raise StyxValidationError(f'`vertex` has the wrong type: Received `{type(params["vertex"])}` expected `int`')
    if params.get("target_val", None) is None:
        raise StyxValidationError("`target_val` must not be None")
    if not isinstance(params["target_val"], (float, int)):
        raise StyxValidationError(f'`target_val` has the wrong type: Received `{type(params["target_val"])}` expected `float`')
    if params.get("nbhd", None) is None:
        raise StyxValidationError("`nbhd` must not be None")
    if not isinstance(params["nbhd"], int):
        raise StyxValidationError(f'`nbhd` has the wrong type: Received `{type(params["nbhd"])}` expected `int`')

def mris_nudge_cargs(params: MrisNudge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_nudge")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["vertex"]))
    cargs.append(str(params["target_val"]))
    cargs.append(str(params["nbhd"]))
    return cargs

def mris_nudge_outputs(params: MrisNudge, execution: Execution) -> MrisNudgeOutputs:
    """Build the MrisNudgeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_v: OutputPathType = execution.output_file("[OUTPUT_SURF]")
    return MrisNudgeOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def mris_nudge_execute(params: MrisNudge, runner: Runner | None = None) -> MrisNudgeOutputs:
    """
    mris_nudge

    A tool to nudge vertex positions on a surface using a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_nudge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_NUDGE_METADATA)
    execution.params(params)
    args = mris_nudge_cargs(params, execution)
    out = mris_nudge_outputs(params, execution)
    execution.run(args)
    return out

def mris_nudge(
    input_surface: InputPathType,
    input_volume: InputPathType,
    vertex: int,
    target_val: float,
    nbhd: int,
    runner: Runner | None = None,
) -> MrisNudgeOutputs:
    """
    mris_nudge

    A tool to nudge vertex positions on a surface using a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file for nudging
        input_volume: Input volume file
        vertex: Vertex to nudge
        target_val: Target value for nudging
        nbhd: Neighborhood size for nudge operation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_nudge_params(
        input_surface=input_surface,
        input_volume=input_volume,
        vertex=vertex,
        target_val=target_val,
        nbhd=nbhd,
    )
    return mris_nudge_execute(params, runner)

__all__ = [
    "MrisNudge",
    "MrisNudgeOutputs",
    "MRIS_NUDGE_METADATA",
    "mris_nudge_cargs",
    "mris_nudge_outputs",
    "mris_nudge_params",
    "mris_nudge_execute",
    "mris_nudge_validate",
    "mris_nudge",
]