# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TALAIRACH_METADATA = Metadata(
    id="talairach",
    name="talairach",
    package="freesurfer",
)

Talairach = typing.TypedDict(
    "Talairach",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/talairach"]],
        "input_volume": InputPathType,
        "output_transform": str,
        "log_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TalairachOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    xfm_output: OutputPathType
    """Output transformation file."""

def talairach_params(
    input_volume: InputPathType,
    output_transform: str,
    log_flag: bool = False,
    debug_flag: bool = False,
) -> Talairach:
    """
    Build parameters.

    Args:
        input_volume: Input volume.
        output_transform: Output transform file (xfm).
        log_flag: Specify log file. Default is outdir/talarach.log.
        debug_flag: Turn on debugging.

    Returns:
        Parameter dictionary.
    """
    params: Talairach = {
        "@type": "freesurfer/talairach",
        "input_volume": input_volume,
        "output_transform": output_transform,
        "log_flag": log_flag,
        "debug_flag": debug_flag,
    }
    return params

def talairach_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Talairach."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_transform", None) is None:
        raise StyxValidationError("`output_transform` must not be None")
    if not isinstance(params["output_transform"], str):
        raise StyxValidationError(f'`output_transform` has the wrong type: Received `{type(params["output_transform"])}` expected `str`')
    if params.get("log_flag", None) is not None:
        if not isinstance(params["log_flag"], bool):
            raise StyxValidationError(f'`log_flag` has the wrong type: Received `{type(params["log_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')

def talairach_cargs(params: Talairach, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("talairach")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("-xfm")
    cargs.append(params["output_transform"])
    if params.get("log_flag"):
        cargs.append("--log")
    if params.get("debug_flag"):
        cargs.append("--debug")
    return cargs

def talairach_outputs(params: Talairach, execution: Execution) -> TalairachOutputs:
    """Build the TalairachOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    xfm_output_v: OutputPathType = execution.output_file(params["output_transform"])
    return TalairachOutputs(
        root=root_v,
        xfm_output=xfm_output_v,
    )

def talairach_execute(params: Talairach, runner: Runner | None = None) -> TalairachOutputs:
    """
    talairach

    Front-end for MINC's mritotal to compute the Talairach transform mapping the input volume to the MNI305.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    talairach_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TALAIRACH_METADATA)
    execution.params(params)
    args = talairach_cargs(params, execution)
    out = talairach_outputs(params, execution)
    execution.run(args)
    return out

def talairach(
    input_volume: InputPathType,
    output_transform: str,
    log_flag: bool = False,
    debug_flag: bool = False,
    runner: Runner | None = None,
) -> TalairachOutputs:
    """
    talairach

    Front-end for MINC's mritotal to compute the Talairach transform mapping the input volume to the MNI305.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume.
        output_transform: Output transform file (xfm).
        log_flag: Specify log file. Default is outdir/talarach.log.
        debug_flag: Turn on debugging.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = talairach_params(
        input_volume=input_volume,
        output_transform=output_transform,
        log_flag=log_flag,
        debug_flag=debug_flag,
    )
    return talairach_execute(params, runner)

__all__ = [
    "Talairach",
    "TalairachOutputs",
    "TALAIRACH_METADATA",
    "talairach_cargs",
    "talairach_outputs",
    "talairach_params",
    "talairach_execute",
    "talairach_validate",
    "talairach",
]