# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_AVERAGE_METADATA = Metadata(
    id="mri_average",
    name="mri_average",
    package="freesurfer",
)

MriAverage = typing.TypedDict(
    "MriAverage",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_average"]],
        "input_volumes": list[InputPathType],
        "output_volume": str,
        "rigid_alignment": typing.NotRequired[bool],
        "read_from_file": typing.NotRequired[bool],
        "dt": typing.NotRequired[float],
        "tol": typing.NotRequired[float],
        "conform": typing.NotRequired[bool],
        "noconform": typing.NotRequired[bool],
        "reduce": typing.NotRequired[float],
        "sinc_interpolation": typing.NotRequired[float],
        "trilinear": typing.NotRequired[bool],
        "window": typing.NotRequired[bool],
        "snapshots": typing.NotRequired[float],
        "translation": typing.NotRequired[list[float]],
        "rotation": typing.NotRequired[list[float]],
        "momentum": typing.NotRequired[float],
        "rms": typing.NotRequired[bool],
        "rms_alt": typing.NotRequired[bool],
        "percent": typing.NotRequired[bool],
        "binarize": typing.NotRequired[float],
        "absolute": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The averaged output volume."""

def mri_average_params(
    input_volumes: list[InputPathType],
    output_volume: str,
    rigid_alignment: bool = False,
    read_from_file: bool = False,
    dt: float | None = None,
    tol: float | None = None,
    conform: bool = False,
    noconform: bool = False,
    reduce: float | None = None,
    sinc_interpolation: float | None = None,
    trilinear: bool = False,
    window: bool = False,
    snapshots: float | None = None,
    translation: list[float] | None = None,
    rotation: list[float] | None = None,
    momentum: float | None = None,
    rms: bool = False,
    rms_alt: bool = False,
    percent: bool = False,
    binarize: float | None = None,
    absolute: bool = False,
) -> MriAverage:
    """
    Build parameters.

    Args:
        input_volumes: Input volumes to average.
        output_volume: Output volume file.
        rigid_alignment: Rigid alignment of input volumes before averaging.
        read_from_file: Read volumes from an input file (first argument is the\
            input filename).
        dt: Set dt to n (default=1e-6).
        tol: Set tolerance to n (default=1e-5).
        conform: Interpolate volume to be isotropic 1mm^3 (on by default).
        noconform: Inhibit isotropic volume interpolation.
        reduce: Reduce input images n (default=2) times.
        sinc_interpolation: Using sinc interpolation with window width of 2*n\
            (default=3).
        trilinear: Use trilinear interpolation.
        window: Apply hanning window to volumes.
        snapshots: Write snapshots every n iterations.
        translation: Translation of second volume.
        rotation: Rotation of second volume around each axis in degrees.
        momentum: Use momentum n (default=0).
        rms: Compute sqrt of average of sum of squares (RMS, same as -rms).
        rms_alt: Compute sqrt of average of sum of squares (RMS, same as -sqr).
        percent: Compute percentage.
        binarize: Binarize the input volumes using threshold th.
        absolute: Take absolute value of volume.

    Returns:
        Parameter dictionary.
    """
    params: MriAverage = {
        "@type": "freesurfer/mri_average",
        "input_volumes": input_volumes,
        "output_volume": output_volume,
        "rigid_alignment": rigid_alignment,
        "read_from_file": read_from_file,
        "conform": conform,
        "noconform": noconform,
        "trilinear": trilinear,
        "window": window,
        "rms": rms,
        "rms_alt": rms_alt,
        "percent": percent,
        "absolute": absolute,
    }
    if dt is not None:
        params["dt"] = dt
    if tol is not None:
        params["tol"] = tol
    if reduce is not None:
        params["reduce"] = reduce
    if sinc_interpolation is not None:
        params["sinc_interpolation"] = sinc_interpolation
    if snapshots is not None:
        params["snapshots"] = snapshots
    if translation is not None:
        params["translation"] = translation
    if rotation is not None:
        params["rotation"] = rotation
    if momentum is not None:
        params["momentum"] = momentum
    if binarize is not None:
        params["binarize"] = binarize
    return params

def mri_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("rigid_alignment", None) is not None:
        if not isinstance(params["rigid_alignment"], bool):
            raise StyxValidationError(f'`rigid_alignment` has the wrong type: Received `{type(params["rigid_alignment"])}` expected `bool`')
    if params.get("read_from_file", None) is not None:
        if not isinstance(params["read_from_file"], bool):
            raise StyxValidationError(f'`read_from_file` has the wrong type: Received `{type(params["read_from_file"])}` expected `bool`')
    if params.get("dt", None) is not None:
        if not isinstance(params["dt"], (float, int)):
            raise StyxValidationError(f'`dt` has the wrong type: Received `{type(params["dt"])}` expected `float`')
    if params.get("tol", None) is not None:
        if not isinstance(params["tol"], (float, int)):
            raise StyxValidationError(f'`tol` has the wrong type: Received `{type(params["tol"])}` expected `float`')
    if params.get("conform", None) is not None:
        if not isinstance(params["conform"], bool):
            raise StyxValidationError(f'`conform` has the wrong type: Received `{type(params["conform"])}` expected `bool`')
    if params.get("noconform", None) is not None:
        if not isinstance(params["noconform"], bool):
            raise StyxValidationError(f'`noconform` has the wrong type: Received `{type(params["noconform"])}` expected `bool`')
    if params.get("reduce", None) is not None:
        if not isinstance(params["reduce"], (float, int)):
            raise StyxValidationError(f'`reduce` has the wrong type: Received `{type(params["reduce"])}` expected `float`')
    if params.get("sinc_interpolation", None) is not None:
        if not isinstance(params["sinc_interpolation"], (float, int)):
            raise StyxValidationError(f'`sinc_interpolation` has the wrong type: Received `{type(params["sinc_interpolation"])}` expected `float`')
    if params.get("trilinear", None) is not None:
        if not isinstance(params["trilinear"], bool):
            raise StyxValidationError(f'`trilinear` has the wrong type: Received `{type(params["trilinear"])}` expected `bool`')
    if params.get("window", None) is not None:
        if not isinstance(params["window"], bool):
            raise StyxValidationError(f'`window` has the wrong type: Received `{type(params["window"])}` expected `bool`')
    if params.get("snapshots", None) is not None:
        if not isinstance(params["snapshots"], (float, int)):
            raise StyxValidationError(f'`snapshots` has the wrong type: Received `{type(params["snapshots"])}` expected `float`')
    if params.get("translation", None) is not None:
        if not isinstance(params["translation"], list):
            raise StyxValidationError(f'`translation` has the wrong type: Received `{type(params["translation"])}` expected `list[float]`')
        if not (3 <= len(params["translation"]) <= 3):
            raise StyxValidationError("Parameter `translation` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["translation"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`translation` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("rotation", None) is not None:
        if not isinstance(params["rotation"], list):
            raise StyxValidationError(f'`rotation` has the wrong type: Received `{type(params["rotation"])}` expected `list[float]`')
        if not (3 <= len(params["rotation"]) <= 3):
            raise StyxValidationError("Parameter `rotation` must contain between 3 and 3 elements (inclusive)")
        for e_3 in params["rotation"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`rotation` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("momentum", None) is not None:
        if not isinstance(params["momentum"], (float, int)):
            raise StyxValidationError(f'`momentum` has the wrong type: Received `{type(params["momentum"])}` expected `float`')
    if params.get("rms", None) is not None:
        if not isinstance(params["rms"], bool):
            raise StyxValidationError(f'`rms` has the wrong type: Received `{type(params["rms"])}` expected `bool`')
    if params.get("rms_alt", None) is not None:
        if not isinstance(params["rms_alt"], bool):
            raise StyxValidationError(f'`rms_alt` has the wrong type: Received `{type(params["rms_alt"])}` expected `bool`')
    if params.get("percent", None) is not None:
        if not isinstance(params["percent"], bool):
            raise StyxValidationError(f'`percent` has the wrong type: Received `{type(params["percent"])}` expected `bool`')
    if params.get("binarize", None) is not None:
        if not isinstance(params["binarize"], (float, int)):
            raise StyxValidationError(f'`binarize` has the wrong type: Received `{type(params["binarize"])}` expected `float`')
    if params.get("absolute", None) is not None:
        if not isinstance(params["absolute"], bool):
            raise StyxValidationError(f'`absolute` has the wrong type: Received `{type(params["absolute"])}` expected `bool`')

def mri_average_cargs(params: MriAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_average")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_volume"])
    if params.get("rigid_alignment"):
        cargs.append("-a")
    if params.get("read_from_file"):
        cargs.append("-F")
    v_0 = params.get("dt")
    if v_0 is not None:
        cargs.append("-dt")
        cargs.append(str(v_0))
    v_1 = params.get("tol")
    if v_1 is not None:
        cargs.append("-tol")
        cargs.append(str(v_1))
    if params.get("conform"):
        cargs.append("-conform")
    if params.get("noconform"):
        cargs.append("-noconform")
    v_2 = params.get("reduce")
    if v_2 is not None:
        cargs.append("-reduce")
        cargs.append(str(v_2))
    v_3 = params.get("sinc_interpolation")
    if v_3 is not None:
        cargs.append("-sinc")
        cargs.append(str(v_3))
    if params.get("trilinear"):
        cargs.append("-trilinear")
    if params.get("window"):
        cargs.append("-window")
    v_4 = params.get("snapshots")
    if v_4 is not None:
        cargs.append("-w")
        cargs.append(str(v_4))
    v_5 = params.get("translation")
    if v_5 is not None:
        cargs.append("-t")
        for item1 in v_5:
            cargs.append(str(item1))
    v_6 = params.get("rotation")
    if v_6 is not None:
        cargs.append("-r")
        for item2 in v_6:
            cargs.append(str(item2))
    v_7 = params.get("momentum")
    if v_7 is not None:
        cargs.append("-m")
        cargs.append(str(v_7))
    if params.get("rms"):
        cargs.append("-sqr")
    if params.get("rms_alt"):
        cargs.append("-rms")
    if params.get("percent"):
        cargs.append("-p")
    v_8 = params.get("binarize")
    if v_8 is not None:
        cargs.append("-b")
        cargs.append(str(v_8))
    if params.get("absolute"):
        cargs.append("-abs")
    return cargs

def mri_average_outputs(params: MriAverage, execution: Execution) -> MriAverageOutputs:
    """Build the MriAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriAverageOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_average_execute(params: MriAverage, runner: Runner | None = None) -> MriAverageOutputs:
    """
    mri_average

    Averages multiple volumes with various options for alignment, interpolation, and transformations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_AVERAGE_METADATA)
    execution.params(params)
    args = mri_average_cargs(params, execution)
    out = mri_average_outputs(params, execution)
    execution.run(args)
    return out

def mri_average(
    input_volumes: list[InputPathType],
    output_volume: str,
    rigid_alignment: bool = False,
    read_from_file: bool = False,
    dt: float | None = None,
    tol: float | None = None,
    conform: bool = False,
    noconform: bool = False,
    reduce: float | None = None,
    sinc_interpolation: float | None = None,
    trilinear: bool = False,
    window: bool = False,
    snapshots: float | None = None,
    translation: list[float] | None = None,
    rotation: list[float] | None = None,
    momentum: float | None = None,
    rms: bool = False,
    rms_alt: bool = False,
    percent: bool = False,
    binarize: float | None = None,
    absolute: bool = False,
    runner: Runner | None = None,
) -> MriAverageOutputs:
    """
    mri_average

    Averages multiple volumes with various options for alignment, interpolation, and transformations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Input volumes to average.
        output_volume: Output volume file.
        rigid_alignment: Rigid alignment of input volumes before averaging.
        read_from_file: Read volumes from an input file (first argument is the\
            input filename).
        dt: Set dt to n (default=1e-6).
        tol: Set tolerance to n (default=1e-5).
        conform: Interpolate volume to be isotropic 1mm^3 (on by default).
        noconform: Inhibit isotropic volume interpolation.
        reduce: Reduce input images n (default=2) times.
        sinc_interpolation: Using sinc interpolation with window width of 2*n\
            (default=3).
        trilinear: Use trilinear interpolation.
        window: Apply hanning window to volumes.
        snapshots: Write snapshots every n iterations.
        translation: Translation of second volume.
        rotation: Rotation of second volume around each axis in degrees.
        momentum: Use momentum n (default=0).
        rms: Compute sqrt of average of sum of squares (RMS, same as -rms).
        rms_alt: Compute sqrt of average of sum of squares (RMS, same as -sqr).
        percent: Compute percentage.
        binarize: Binarize the input volumes using threshold th.
        absolute: Take absolute value of volume.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_average_params(
        input_volumes=input_volumes,
        output_volume=output_volume,
        rigid_alignment=rigid_alignment,
        read_from_file=read_from_file,
        dt=dt,
        tol=tol,
        conform=conform,
        noconform=noconform,
        reduce=reduce,
        sinc_interpolation=sinc_interpolation,
        trilinear=trilinear,
        window=window,
        snapshots=snapshots,
        translation=translation,
        rotation=rotation,
        momentum=momentum,
        rms=rms,
        rms_alt=rms_alt,
        percent=percent,
        binarize=binarize,
        absolute=absolute,
    )
    return mri_average_execute(params, runner)

__all__ = [
    "MriAverage",
    "MriAverageOutputs",
    "MRI_AVERAGE_METADATA",
    "mri_average_cargs",
    "mri_average_outputs",
    "mri_average_params",
    "mri_average_execute",
    "mri_average_validate",
    "mri_average",
]