# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_ENTOWM_SEG_METADATA = Metadata(
    id="mri_entowm_seg",
    name="mri_entowm_seg",
    package="freesurfer",
)

MriEntowmSeg = typing.TypedDict(
    "MriEntowmSeg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_entowm_seg"]],
        "input_image": typing.NotRequired[InputPathType],
        "output_segmentation": typing.NotRequired[str],
        "recon_subjects": typing.NotRequired[list[str]],
        "subjects_directory": typing.NotRequired[str],
        "conform": typing.NotRequired[bool],
        "etiv": typing.NotRequired[bool],
        "tal": typing.NotRequired[str],
        "write_posteriors": typing.NotRequired[bool],
        "write_volumes": typing.NotRequired[bool],
        "write_qa_stats": typing.NotRequired[bool],
        "exclude_labels": typing.NotRequired[list[str]],
        "keep_ac": typing.NotRequired[bool],
        "vox_count_volumes": typing.NotRequired[bool],
        "model_weights": typing.NotRequired[str],
        "color_table": typing.NotRequired[str],
        "population_stats": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "vmp": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "seven_tesla": typing.NotRequired[bool],
        "percentile": typing.NotRequired[float],
        "cuda_device": typing.NotRequired[str],
        "output_base": typing.NotRequired[str],
        "no_cite_sclimbic": typing.NotRequired[bool],
        "nchannels": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriEntowmSegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Segmentation output file"""
    label_posteriors: OutputPathType | None
    """Label posterior probabilities"""
    volume_stats: OutputPathType | None
    """Volume statistics"""
    qa_stats: OutputPathType | None
    """Quality assurance statistics"""

def mri_entowm_seg_params(
    input_image: InputPathType | None = None,
    output_segmentation: str | None = None,
    recon_subjects: list[str] | None = None,
    subjects_directory: str | None = None,
    conform: bool = False,
    etiv: bool = False,
    tal: str | None = None,
    write_posteriors: bool = False,
    write_volumes: bool = False,
    write_qa_stats: bool = False,
    exclude_labels: list[str] | None = None,
    keep_ac: bool = False,
    vox_count_volumes: bool = False,
    model_weights: str | None = None,
    color_table: str | None = None,
    population_stats: str | None = None,
    debug: bool = False,
    vmp: bool = False,
    threads: float | None = None,
    seven_tesla: bool = False,
    percentile: float | None = None,
    cuda_device: str | None = None,
    output_base: str | None = None,
    no_cite_sclimbic: bool = False,
    nchannels: float | None = None,
) -> MriEntowmSeg:
    """
    Build parameters.

    Args:
        input_image: T1-weighted image(s) to segment. Can be a path to a single\
            image or a directory of images.
        output_segmentation: Segmentation output file or directory (required if --i\
            is provided).
        recon_subjects: Process a series of FreeSurfer recon-all subjects, enables\
            subject-mode.
        subjects_directory: Set the subjects directory, overrides the SUBJECTS_DIR\
            env variable.
        conform: Resample input to 1mm-iso; results will be put back in native\
            resolution.
        etiv: Include eTIV in volume stats (enabled by default in subject-mode and\
            with --tal).
        tal: Alternative talairach xfm transform for estimating TIV, can be file or\
            suffix (for multiple inputs).
        write_posteriors: Save the label posteriors.
        write_volumes: Save label volume stats (enabled by default in\
            subject-mode).
        write_qa_stats: Save QA stats (z and confidence).
        exclude_labels: List of label IDs to exclude in any output stats files.
        keep_ac: Explicitly keep anterior commissure in the volume/QA files.
        vox_count_volumes: Use discrete voxel count for label volumes.
        model_weights: Alternative model weights to load.
        color_table: Alternative color lookup table to embed in segmentation. Must\
            be minimal, including 0, and sorted.
        population_stats: Alternative population volume stats for QA output.
        debug: Enable debug logging.
        vmp: Enable printing of vmpeak at the end.
        threads: Number of threads to use. Default is 1.
        seven_tesla: Preprocess 7T images (just sets percentile to 99.9).
        percentile: Use intensity percentile threshold for normalization.
        cuda_device: CUDA device for GPU support.
        output_base: String to use in output file name; default is sclimbic.
        no_cite_sclimbic: Do not cite sclimbic paper at the end.
        nchannels: Number of channels.

    Returns:
        Parameter dictionary.
    """
    params: MriEntowmSeg = {
        "@type": "freesurfer/mri_entowm_seg",
        "conform": conform,
        "etiv": etiv,
        "write_posteriors": write_posteriors,
        "write_volumes": write_volumes,
        "write_qa_stats": write_qa_stats,
        "keep_ac": keep_ac,
        "vox_count_volumes": vox_count_volumes,
        "debug": debug,
        "vmp": vmp,
        "seven_tesla": seven_tesla,
        "no_cite_sclimbic": no_cite_sclimbic,
    }
    if input_image is not None:
        params["input_image"] = input_image
    if output_segmentation is not None:
        params["output_segmentation"] = output_segmentation
    if recon_subjects is not None:
        params["recon_subjects"] = recon_subjects
    if subjects_directory is not None:
        params["subjects_directory"] = subjects_directory
    if tal is not None:
        params["tal"] = tal
    if exclude_labels is not None:
        params["exclude_labels"] = exclude_labels
    if model_weights is not None:
        params["model_weights"] = model_weights
    if color_table is not None:
        params["color_table"] = color_table
    if population_stats is not None:
        params["population_stats"] = population_stats
    if threads is not None:
        params["threads"] = threads
    if percentile is not None:
        params["percentile"] = percentile
    if cuda_device is not None:
        params["cuda_device"] = cuda_device
    if output_base is not None:
        params["output_base"] = output_base
    if nchannels is not None:
        params["nchannels"] = nchannels
    return params

def mri_entowm_seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriEntowmSeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is not None:
        if not isinstance(params["input_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_segmentation", None) is not None:
        if not isinstance(params["output_segmentation"], str):
            raise StyxValidationError(f'`output_segmentation` has the wrong type: Received `{type(params["output_segmentation"])}` expected `str`')
    if params.get("recon_subjects", None) is not None:
        if not isinstance(params["recon_subjects"], list):
            raise StyxValidationError(f'`recon_subjects` has the wrong type: Received `{type(params["recon_subjects"])}` expected `list[str]`')
        for e in params["recon_subjects"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`recon_subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subjects_directory", None) is not None:
        if not isinstance(params["subjects_directory"], str):
            raise StyxValidationError(f'`subjects_directory` has the wrong type: Received `{type(params["subjects_directory"])}` expected `str`')
    if params.get("conform", None) is not None:
        if not isinstance(params["conform"], bool):
            raise StyxValidationError(f'`conform` has the wrong type: Received `{type(params["conform"])}` expected `bool`')
    if params.get("etiv", None) is not None:
        if not isinstance(params["etiv"], bool):
            raise StyxValidationError(f'`etiv` has the wrong type: Received `{type(params["etiv"])}` expected `bool`')
    if params.get("tal", None) is not None:
        if not isinstance(params["tal"], str):
            raise StyxValidationError(f'`tal` has the wrong type: Received `{type(params["tal"])}` expected `str`')
    if params.get("write_posteriors", None) is not None:
        if not isinstance(params["write_posteriors"], bool):
            raise StyxValidationError(f'`write_posteriors` has the wrong type: Received `{type(params["write_posteriors"])}` expected `bool`')
    if params.get("write_volumes", None) is not None:
        if not isinstance(params["write_volumes"], bool):
            raise StyxValidationError(f'`write_volumes` has the wrong type: Received `{type(params["write_volumes"])}` expected `bool`')
    if params.get("write_qa_stats", None) is not None:
        if not isinstance(params["write_qa_stats"], bool):
            raise StyxValidationError(f'`write_qa_stats` has the wrong type: Received `{type(params["write_qa_stats"])}` expected `bool`')
    if params.get("exclude_labels", None) is not None:
        if not isinstance(params["exclude_labels"], list):
            raise StyxValidationError(f'`exclude_labels` has the wrong type: Received `{type(params["exclude_labels"])}` expected `list[str]`')
        for e_2 in params["exclude_labels"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`exclude_labels` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("keep_ac", None) is not None:
        if not isinstance(params["keep_ac"], bool):
            raise StyxValidationError(f'`keep_ac` has the wrong type: Received `{type(params["keep_ac"])}` expected `bool`')
    if params.get("vox_count_volumes", None) is not None:
        if not isinstance(params["vox_count_volumes"], bool):
            raise StyxValidationError(f'`vox_count_volumes` has the wrong type: Received `{type(params["vox_count_volumes"])}` expected `bool`')
    if params.get("model_weights", None) is not None:
        if not isinstance(params["model_weights"], str):
            raise StyxValidationError(f'`model_weights` has the wrong type: Received `{type(params["model_weights"])}` expected `str`')
    if params.get("color_table", None) is not None:
        if not isinstance(params["color_table"], str):
            raise StyxValidationError(f'`color_table` has the wrong type: Received `{type(params["color_table"])}` expected `str`')
    if params.get("population_stats", None) is not None:
        if not isinstance(params["population_stats"], str):
            raise StyxValidationError(f'`population_stats` has the wrong type: Received `{type(params["population_stats"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("vmp", None) is not None:
        if not isinstance(params["vmp"], bool):
            raise StyxValidationError(f'`vmp` has the wrong type: Received `{type(params["vmp"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("seven_tesla", None) is not None:
        if not isinstance(params["seven_tesla"], bool):
            raise StyxValidationError(f'`seven_tesla` has the wrong type: Received `{type(params["seven_tesla"])}` expected `bool`')
    if params.get("percentile", None) is not None:
        if not isinstance(params["percentile"], (float, int)):
            raise StyxValidationError(f'`percentile` has the wrong type: Received `{type(params["percentile"])}` expected `float`')
    if params.get("cuda_device", None) is not None:
        if not isinstance(params["cuda_device"], str):
            raise StyxValidationError(f'`cuda_device` has the wrong type: Received `{type(params["cuda_device"])}` expected `str`')
    if params.get("output_base", None) is not None:
        if not isinstance(params["output_base"], str):
            raise StyxValidationError(f'`output_base` has the wrong type: Received `{type(params["output_base"])}` expected `str`')
    if params.get("no_cite_sclimbic", None) is not None:
        if not isinstance(params["no_cite_sclimbic"], bool):
            raise StyxValidationError(f'`no_cite_sclimbic` has the wrong type: Received `{type(params["no_cite_sclimbic"])}` expected `bool`')
    if params.get("nchannels", None) is not None:
        if not isinstance(params["nchannels"], (float, int)):
            raise StyxValidationError(f'`nchannels` has the wrong type: Received `{type(params["nchannels"])}` expected `float`')

def mri_entowm_seg_cargs(params: MriEntowmSeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_entowm_seg")
    v_0 = params.get("input_image")
    if v_0 is not None:
        cargs.append("-i")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("output_segmentation")
    if v_1 is not None:
        cargs.append("-o")
        cargs.append(v_1)
    v_2 = params.get("recon_subjects")
    if v_2 is not None:
        cargs.append("-s")
        for item0 in v_2:
            cargs.append(item0)
    v_3 = params.get("subjects_directory")
    if v_3 is not None:
        cargs.append("--sd")
        cargs.append(v_3)
    if params.get("conform"):
        cargs.append("--conform")
    if params.get("etiv"):
        cargs.append("--etiv")
    v_4 = params.get("tal")
    if v_4 is not None:
        cargs.append("--tal")
        cargs.append(v_4)
    if params.get("write_posteriors"):
        cargs.append("--write_posteriors")
    if params.get("write_volumes"):
        cargs.append("--write_volumes")
    if params.get("write_qa_stats"):
        cargs.append("--write_qa_stats")
    v_5 = params.get("exclude_labels")
    if v_5 is not None:
        cargs.append("--exclude")
        for item1 in v_5:
            cargs.append(item1)
    if params.get("keep_ac"):
        cargs.append("--keep_ac")
    if params.get("vox_count_volumes"):
        cargs.append("--vox-count-volumes")
    v_6 = params.get("model_weights")
    if v_6 is not None:
        cargs.append("--model")
        cargs.append(v_6)
    v_7 = params.get("color_table")
    if v_7 is not None:
        cargs.append("--ctab")
        cargs.append(v_7)
    v_8 = params.get("population_stats")
    if v_8 is not None:
        cargs.append("--population-stats")
        cargs.append(v_8)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("vmp"):
        cargs.append("--vmp")
    v_9 = params.get("threads")
    if v_9 is not None:
        cargs.append("--threads")
        cargs.append(str(v_9))
    if params.get("seven_tesla"):
        cargs.append("--7T")
    v_10 = params.get("percentile")
    if v_10 is not None:
        cargs.append("--percentile")
        cargs.append(str(v_10))
    v_11 = params.get("cuda_device")
    if v_11 is not None:
        cargs.append("--cuda-device")
        cargs.append(v_11)
    v_12 = params.get("output_base")
    if v_12 is not None:
        cargs.append("--output-base")
        cargs.append(v_12)
    if params.get("no_cite_sclimbic"):
        cargs.append("--no-cite-sclimbic")
    v_13 = params.get("nchannels")
    if v_13 is not None:
        cargs.append("--nchannels")
        cargs.append(str(v_13))
    return cargs

def mri_entowm_seg_outputs(params: MriEntowmSeg, execution: Execution) -> MriEntowmSegOutputs:
    """Build the MriEntowmSegOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    label_posteriors_v: OutputPathType | None = None
    volume_stats_v: OutputPathType | None = None
    qa_stats_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_segmentation")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_base")
    if __v1 is not None:
        label_posteriors_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}_posterior.mgz')
    __v2 = params.get("output_base")
    if __v2 is not None:
        volume_stats_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}_volumes.txt')
    __v3 = params.get("output_base")
    if __v3 is not None:
        qa_stats_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}_qa_stats.txt')
    return MriEntowmSegOutputs(
        root=root_v,
        output_file=output_file_v,
        label_posteriors=label_posteriors_v,
        volume_stats=volume_stats_v,
        qa_stats=qa_stats_v,
    )

def mri_entowm_seg_execute(params: MriEntowmSeg, runner: Runner | None = None) -> MriEntowmSegOutputs:
    """
    mri_entowm_seg

    Segment white matter near gyrus ambiens entorhinal cortex using a deep learning model.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_entowm_seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_ENTOWM_SEG_METADATA)
    execution.params(params)
    args = mri_entowm_seg_cargs(params, execution)
    out = mri_entowm_seg_outputs(params, execution)
    execution.run(args)
    return out

def mri_entowm_seg(
    input_image: InputPathType | None = None,
    output_segmentation: str | None = None,
    recon_subjects: list[str] | None = None,
    subjects_directory: str | None = None,
    conform: bool = False,
    etiv: bool = False,
    tal: str | None = None,
    write_posteriors: bool = False,
    write_volumes: bool = False,
    write_qa_stats: bool = False,
    exclude_labels: list[str] | None = None,
    keep_ac: bool = False,
    vox_count_volumes: bool = False,
    model_weights: str | None = None,
    color_table: str | None = None,
    population_stats: str | None = None,
    debug: bool = False,
    vmp: bool = False,
    threads: float | None = None,
    seven_tesla: bool = False,
    percentile: float | None = None,
    cuda_device: str | None = None,
    output_base: str | None = None,
    no_cite_sclimbic: bool = False,
    nchannels: float | None = None,
    runner: Runner | None = None,
) -> MriEntowmSegOutputs:
    """
    mri_entowm_seg

    Segment white matter near gyrus ambiens entorhinal cortex using a deep learning model.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: T1-weighted image(s) to segment. Can be a path to a single\
            image or a directory of images.
        output_segmentation: Segmentation output file or directory (required if --i\
            is provided).
        recon_subjects: Process a series of FreeSurfer recon-all subjects, enables\
            subject-mode.
        subjects_directory: Set the subjects directory, overrides the SUBJECTS_DIR\
            env variable.
        conform: Resample input to 1mm-iso; results will be put back in native\
            resolution.
        etiv: Include eTIV in volume stats (enabled by default in subject-mode and\
            with --tal).
        tal: Alternative talairach xfm transform for estimating TIV, can be file or\
            suffix (for multiple inputs).
        write_posteriors: Save the label posteriors.
        write_volumes: Save label volume stats (enabled by default in\
            subject-mode).
        write_qa_stats: Save QA stats (z and confidence).
        exclude_labels: List of label IDs to exclude in any output stats files.
        keep_ac: Explicitly keep anterior commissure in the volume/QA files.
        vox_count_volumes: Use discrete voxel count for label volumes.
        model_weights: Alternative model weights to load.
        color_table: Alternative color lookup table to embed in segmentation. Must\
            be minimal, including 0, and sorted.
        population_stats: Alternative population volume stats for QA output.
        debug: Enable debug logging.
        vmp: Enable printing of vmpeak at the end.
        threads: Number of threads to use. Default is 1.
        seven_tesla: Preprocess 7T images (just sets percentile to 99.9).
        percentile: Use intensity percentile threshold for normalization.
        cuda_device: CUDA device for GPU support.
        output_base: String to use in output file name; default is sclimbic.
        no_cite_sclimbic: Do not cite sclimbic paper at the end.
        nchannels: Number of channels.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_entowm_seg_params(
        input_image=input_image,
        output_segmentation=output_segmentation,
        recon_subjects=recon_subjects,
        subjects_directory=subjects_directory,
        conform=conform,
        etiv=etiv,
        tal=tal,
        write_posteriors=write_posteriors,
        write_volumes=write_volumes,
        write_qa_stats=write_qa_stats,
        exclude_labels=exclude_labels,
        keep_ac=keep_ac,
        vox_count_volumes=vox_count_volumes,
        model_weights=model_weights,
        color_table=color_table,
        population_stats=population_stats,
        debug=debug,
        vmp=vmp,
        threads=threads,
        seven_tesla=seven_tesla,
        percentile=percentile,
        cuda_device=cuda_device,
        output_base=output_base,
        no_cite_sclimbic=no_cite_sclimbic,
        nchannels=nchannels,
    )
    return mri_entowm_seg_execute(params, runner)

__all__ = [
    "MriEntowmSeg",
    "MriEntowmSegOutputs",
    "MRI_ENTOWM_SEG_METADATA",
    "mri_entowm_seg_cargs",
    "mri_entowm_seg_outputs",
    "mri_entowm_seg_params",
    "mri_entowm_seg_execute",
    "mri_entowm_seg_validate",
    "mri_entowm_seg",
]