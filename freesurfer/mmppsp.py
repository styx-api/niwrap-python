# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MMPPSP_METADATA = Metadata(
    id="mmppsp",
    name="mmppsp",
    package="freesurfer",
)

Mmppsp = typing.TypedDict(
    "Mmppsp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mmppsp"]],
        "samseg_dir": str,
        "outdir": str,
        "lh_flag": typing.NotRequired[bool],
        "rh_flag": typing.NotRequired[bool],
        "likelihood_flag": typing.NotRequired[bool],
        "posterior_flag": typing.NotRequired[bool],
        "force_update_flag": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "no_initsphreg_flag": typing.NotRequired[bool],
        "stop_after": typing.NotRequired[str],
        "wexpanddist": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MmppspOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType
    """Output surface files"""

def mmppsp_params(
    samseg_dir: str,
    outdir: str,
    lh_flag: bool = False,
    rh_flag: bool = False,
    likelihood_flag: bool = False,
    posterior_flag: bool = False,
    force_update_flag: bool = False,
    threads: float | None = None,
    no_initsphreg_flag: bool = False,
    stop_after: str | None = None,
    wexpanddist: float | None = None,
) -> Mmppsp:
    """
    Build parameters.

    Args:
        samseg_dir: Directory containing Samseg output
        outdir: Output directory for the results
        lh_flag: Process left hemisphere
        rh_flag: Process right hemisphere
        likelihood_flag: Use likelihood for surface placement
        posterior_flag: Use posteriors instead of likelihood for surface placement
        force_update_flag: Force update the surface placement
        threads: Number of threads to use
        no_initsphreg_flag: Do not use talairach.lta to initialize rotation
        stop_after: Stop the processing after a specified step
        wexpanddist: Distance to expand white surface to initialize pial (in mm)

    Returns:
        Parameter dictionary.
    """
    params: Mmppsp = {
        "@type": "freesurfer/mmppsp",
        "samseg_dir": samseg_dir,
        "outdir": outdir,
        "lh_flag": lh_flag,
        "rh_flag": rh_flag,
        "likelihood_flag": likelihood_flag,
        "posterior_flag": posterior_flag,
        "force_update_flag": force_update_flag,
        "no_initsphreg_flag": no_initsphreg_flag,
    }
    if threads is not None:
        params["threads"] = threads
    if stop_after is not None:
        params["stop_after"] = stop_after
    if wexpanddist is not None:
        params["wexpanddist"] = wexpanddist
    return params

def mmppsp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mmppsp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("samseg_dir", None) is None:
        raise StyxValidationError("`samseg_dir` must not be None")
    if not isinstance(params["samseg_dir"], str):
        raise StyxValidationError(f'`samseg_dir` has the wrong type: Received `{type(params["samseg_dir"])}` expected `str`')
    if params.get("outdir", None) is None:
        raise StyxValidationError("`outdir` must not be None")
    if not isinstance(params["outdir"], str):
        raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("lh_flag", None) is not None:
        if not isinstance(params["lh_flag"], bool):
            raise StyxValidationError(f'`lh_flag` has the wrong type: Received `{type(params["lh_flag"])}` expected `bool`')
    if params.get("rh_flag", None) is not None:
        if not isinstance(params["rh_flag"], bool):
            raise StyxValidationError(f'`rh_flag` has the wrong type: Received `{type(params["rh_flag"])}` expected `bool`')
    if params.get("likelihood_flag", None) is not None:
        if not isinstance(params["likelihood_flag"], bool):
            raise StyxValidationError(f'`likelihood_flag` has the wrong type: Received `{type(params["likelihood_flag"])}` expected `bool`')
    if params.get("posterior_flag", None) is not None:
        if not isinstance(params["posterior_flag"], bool):
            raise StyxValidationError(f'`posterior_flag` has the wrong type: Received `{type(params["posterior_flag"])}` expected `bool`')
    if params.get("force_update_flag", None) is not None:
        if not isinstance(params["force_update_flag"], bool):
            raise StyxValidationError(f'`force_update_flag` has the wrong type: Received `{type(params["force_update_flag"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("no_initsphreg_flag", None) is not None:
        if not isinstance(params["no_initsphreg_flag"], bool):
            raise StyxValidationError(f'`no_initsphreg_flag` has the wrong type: Received `{type(params["no_initsphreg_flag"])}` expected `bool`')
    if params.get("stop_after", None) is not None:
        if not isinstance(params["stop_after"], str):
            raise StyxValidationError(f'`stop_after` has the wrong type: Received `{type(params["stop_after"])}` expected `str`')
    if params.get("wexpanddist", None) is not None:
        if not isinstance(params["wexpanddist"], (float, int)):
            raise StyxValidationError(f'`wexpanddist` has the wrong type: Received `{type(params["wexpanddist"])}` expected `float`')

def mmppsp_cargs(params: Mmppsp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mmppsp")
    cargs.append("--samseg")
    cargs.append(params["samseg_dir"])
    cargs.append("--o")
    cargs.append(params["outdir"])
    if params.get("lh_flag"):
        cargs.append("--lh")
    if params.get("rh_flag"):
        cargs.append("--rh")
    if params.get("likelihood_flag"):
        cargs.append("--likelihood")
    if params.get("posterior_flag"):
        cargs.append("--posterior")
    if params.get("force_update_flag"):
        cargs.append("--force-update")
    v_0 = params.get("threads")
    if v_0 is not None:
        cargs.append("--threads")
        cargs.append(str(v_0))
    if params.get("no_initsphreg_flag"):
        cargs.append("--no-initsphreg")
    v_1 = params.get("stop_after")
    if v_1 is not None:
        cargs.append("--stop-after")
        cargs.append(v_1)
    v_2 = params.get("wexpanddist")
    if v_2 is not None:
        cargs.append("--wexpanddist")
        cargs.append(str(v_2))
    return cargs

def mmppsp_outputs(params: Mmppsp, execution: Execution) -> MmppspOutputs:
    """Build the MmppspOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_v: OutputPathType = execution.output_file(f'{params["outdir"]}/surf')
    return MmppspOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def mmppsp_execute(params: Mmppsp, runner: Runner | None = None) -> MmppspOutputs:
    """
    mmppsp

    MultiModal Posterior Probability Surface Placement

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mmppsp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MMPPSP_METADATA)
    execution.params(params)
    args = mmppsp_cargs(params, execution)
    out = mmppsp_outputs(params, execution)
    execution.run(args)
    return out

def mmppsp(
    samseg_dir: str,
    outdir: str,
    lh_flag: bool = False,
    rh_flag: bool = False,
    likelihood_flag: bool = False,
    posterior_flag: bool = False,
    force_update_flag: bool = False,
    threads: float | None = None,
    no_initsphreg_flag: bool = False,
    stop_after: str | None = None,
    wexpanddist: float | None = None,
    runner: Runner | None = None,
) -> MmppspOutputs:
    """
    mmppsp

    MultiModal Posterior Probability Surface Placement

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        samseg_dir: Directory containing Samseg output
        outdir: Output directory for the results
        lh_flag: Process left hemisphere
        rh_flag: Process right hemisphere
        likelihood_flag: Use likelihood for surface placement
        posterior_flag: Use posteriors instead of likelihood for surface placement
        force_update_flag: Force update the surface placement
        threads: Number of threads to use
        no_initsphreg_flag: Do not use talairach.lta to initialize rotation
        stop_after: Stop the processing after a specified step
        wexpanddist: Distance to expand white surface to initialize pial (in mm)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mmppsp_params(
        samseg_dir=samseg_dir,
        outdir=outdir,
        lh_flag=lh_flag,
        rh_flag=rh_flag,
        likelihood_flag=likelihood_flag,
        posterior_flag=posterior_flag,
        force_update_flag=force_update_flag,
        threads=threads,
        no_initsphreg_flag=no_initsphreg_flag,
        stop_after=stop_after,
        wexpanddist=wexpanddist,
    )
    return mmppsp_execute(params, runner)

__all__ = [
    "Mmppsp",
    "MmppspOutputs",
    "MMPPSP_METADATA",
    "mmppsp_cargs",
    "mmppsp_outputs",
    "mmppsp_params",
    "mmppsp_execute",
    "mmppsp_validate",
    "mmppsp",
]