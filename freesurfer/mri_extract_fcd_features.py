# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EXTRACT_FCD_FEATURES_METADATA = Metadata(
    id="mri_extract_fcd_features",
    name="mri_extract_fcd_features",
    package="freesurfer",
)

MriExtractFcdFeatures = typing.TypedDict(
    "MriExtractFcdFeatures",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_extract_fcd_features"]],
        "subject": str,
        "hemi": str,
        "output_file": InputPathType,
        "subjects_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriExtractFcdFeaturesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_extract_fcd_features_params(
    subject: str,
    hemi: str,
    output_file: InputPathType,
    subjects_dir: str | None = None,
) -> MriExtractFcdFeatures:
    """
    Build parameters.

    Args:
        subject: The subject identifier.
        hemi: The hemisphere (e.g., lh or rh).
        output_file: Output file path.
        subjects_dir: Specify SUBJECTS_DIR on the command line instead of in the\
            environment.

    Returns:
        Parameter dictionary.
    """
    params: MriExtractFcdFeatures = {
        "@type": "freesurfer/mri_extract_fcd_features",
        "subject": subject,
        "hemi": hemi,
        "output_file": output_file,
    }
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    return params

def mri_extract_fcd_features_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriExtractFcdFeatures."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `InputPathType`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')

def mri_extract_fcd_features_cargs(params: MriExtractFcdFeatures, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_extract_fcd_features")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["output_file"]))
    v_0 = params.get("subjects_dir")
    if v_0 is not None:
        cargs.append("sdir")
        cargs.append(v_0)
    return cargs

def mri_extract_fcd_features_outputs(params: MriExtractFcdFeatures, execution: Execution) -> MriExtractFcdFeaturesOutputs:
    """Build the MriExtractFcdFeaturesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriExtractFcdFeaturesOutputs(
        root=root_v,
    )

def mri_extract_fcd_features_execute(params: MriExtractFcdFeatures, runner: Runner | None = None) -> MriExtractFcdFeaturesOutputs:
    """
    mri_extract_fcd_features

    A tool for extracting focal cortical dysplasia features.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_extract_fcd_features_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EXTRACT_FCD_FEATURES_METADATA)
    execution.params(params)
    args = mri_extract_fcd_features_cargs(params, execution)
    out = mri_extract_fcd_features_outputs(params, execution)
    execution.run(args)
    return out

def mri_extract_fcd_features(
    subject: str,
    hemi: str,
    output_file: InputPathType,
    subjects_dir: str | None = None,
    runner: Runner | None = None,
) -> MriExtractFcdFeaturesOutputs:
    """
    mri_extract_fcd_features

    A tool for extracting focal cortical dysplasia features.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject identifier.
        hemi: The hemisphere (e.g., lh or rh).
        output_file: Output file path.
        subjects_dir: Specify SUBJECTS_DIR on the command line instead of in the\
            environment.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_extract_fcd_features_params(
        subject=subject,
        hemi=hemi,
        output_file=output_file,
        subjects_dir=subjects_dir,
    )
    return mri_extract_fcd_features_execute(params, runner)

__all__ = [
    "MriExtractFcdFeatures",
    "MriExtractFcdFeaturesOutputs",
    "MRI_EXTRACT_FCD_FEATURES_METADATA",
    "mri_extract_fcd_features_cargs",
    "mri_extract_fcd_features_outputs",
    "mri_extract_fcd_features_params",
    "mri_extract_fcd_features_execute",
    "mri_extract_fcd_features_validate",
    "mri_extract_fcd_features",
]