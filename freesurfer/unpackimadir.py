# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACKIMADIR_METADATA = Metadata(
    id="unpackimadir",
    name="unpackimadir",
    package="freesurfer",
)

Unpackimadir = typing.TypedDict(
    "Unpackimadir",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpackimadir"]],
        "source_directory": str,
        "target_directory": str,
    },
)

@dataclasses.dataclass
class UnpackimadirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def unpackimadir_params(
    source_directory: str,
    target_directory: str,
) -> Unpackimadir:
    """
    Build parameters.

    Args:
        source_directory:
        target_directory:

    Returns:
        Parameter dictionary.
    """
    params: Unpackimadir = {
        "@type": "freesurfer/unpackimadir",
        "source_directory": source_directory,
        "target_directory": target_directory,
    }
    return params

def unpackimadir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Unpackimadir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_directory", None) is None:
        raise StyxValidationError("`source_directory` must not be None")
    if not isinstance(params["source_directory"], str):
        raise StyxValidationError(f'`source_directory` has the wrong type: Received `{type(params["source_directory"])}` expected `str`')
    if params.get("target_directory", None) is None:
        raise StyxValidationError("`target_directory` must not be None")
    if not isinstance(params["target_directory"], str):
        raise StyxValidationError(f'`target_directory` has the wrong type: Received `{type(params["target_directory"])}` expected `str`')

def unpackimadir_cargs(params: Unpackimadir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpackimadir")
    cargs.append("-src")
    cargs.append(params["source_directory"])
    cargs.append("-targ")
    cargs.append(params["target_directory"])
    return cargs

def unpackimadir_outputs(params: Unpackimadir, execution: Execution) -> UnpackimadirOutputs:
    """Build the UnpackimadirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return UnpackimadirOutputs(
        root=root_v,
    )

def unpackimadir_execute(params: Unpackimadir, runner: Runner | None = None) -> UnpackimadirOutputs:
    """
    unpackimadir

    Unpack image directories.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpackimadir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACKIMADIR_METADATA)
    execution.params(params)
    args = unpackimadir_cargs(params, execution)
    out = unpackimadir_outputs(params, execution)
    execution.run(args)
    return out

def unpackimadir(
    source_directory: str,
    target_directory: str,
    runner: Runner | None = None,
) -> UnpackimadirOutputs:
    """
    unpackimadir

    Unpack image directories.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_directory:
        target_directory:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpackimadir_params(
        source_directory=source_directory,
        target_directory=target_directory,
    )
    return unpackimadir_execute(params, runner)

__all__ = [
    "Unpackimadir",
    "UnpackimadirOutputs",
    "UNPACKIMADIR_METADATA",
    "unpackimadir_cargs",
    "unpackimadir_outputs",
    "unpackimadir_params",
    "unpackimadir_execute",
    "unpackimadir_validate",
    "unpackimadir",
]