# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRAIN_GCS_ATLAS_METADATA = Metadata(
    id="train-gcs-atlas",
    name="train-gcs-atlas",
    package="freesurfer",
)

TrainGcsAtlas = typing.TypedDict(
    "TrainGcsAtlas",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/train-gcs-atlas"]],
        "manual_parcellation": typing.NotRequired[str],
        "subjlist_file": typing.NotRequired[InputPathType],
        "left_hemi": typing.NotRequired[bool],
        "right_hemi": typing.NotRequired[bool],
        "hemi_spec": typing.NotRequired[str],
        "output_gcs": str,
        "surf_reg": typing.NotRequired[InputPathType],
        "color_table": typing.NotRequired[InputPathType],
        "exclude_subject": typing.NotRequired[str],
        "jackknife_flag": typing.NotRequired[bool],
        "aseg_filename": typing.NotRequired[str],
        "threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class TrainGcsAtlasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_gcs_file: OutputPathType
    """Output GCS file produced by training"""

def train_gcs_atlas_params(
    output_gcs: str,
    manual_parcellation: str | None = None,
    subjlist_file: InputPathType | None = None,
    left_hemi: bool = False,
    right_hemi: bool = False,
    hemi_spec: str | None = None,
    surf_reg: InputPathType | None = None,
    color_table: InputPathType | None = None,
    exclude_subject: str | None = None,
    jackknife_flag: bool = False,
    aseg_filename: str | None = None,
    threads: float | None = None,
) -> TrainGcsAtlas:
    """
    Build parameters.

    Args:
        output_gcs: Output GCS file
        manual_parcellation: Manual parcellation; default is aparc_edited
        subjlist_file: File containing the list of subjects
        left_hemi: Left hemisphere processing
        right_hemi: Right hemisphere processing
        hemi_spec: Specify hemisphere for processing
        surf_reg: Surface registration file; default is sphere.reg
        color_table: Color table file
        exclude_subject: Exclude a subject from the atlas
        jackknife_flag: Submit a job for each subject excluding it
        aseg_filename: Aseg filename; default is aseg.auto.mgz
        threads: Number of threads to use

    Returns:
        Parameter dictionary.
    """
    params: TrainGcsAtlas = {
        "@type": "freesurfer/train-gcs-atlas",
        "output_gcs": output_gcs,
        "left_hemi": left_hemi,
        "right_hemi": right_hemi,
        "jackknife_flag": jackknife_flag,
    }
    if manual_parcellation is not None:
        params["manual_parcellation"] = manual_parcellation
    if subjlist_file is not None:
        params["subjlist_file"] = subjlist_file
    if hemi_spec is not None:
        params["hemi_spec"] = hemi_spec
    if surf_reg is not None:
        params["surf_reg"] = surf_reg
    if color_table is not None:
        params["color_table"] = color_table
    if exclude_subject is not None:
        params["exclude_subject"] = exclude_subject
    if aseg_filename is not None:
        params["aseg_filename"] = aseg_filename
    if threads is not None:
        params["threads"] = threads
    return params

def train_gcs_atlas_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TrainGcsAtlas."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("manual_parcellation", None) is not None:
        if not isinstance(params["manual_parcellation"], str):
            raise StyxValidationError(f'`manual_parcellation` has the wrong type: Received `{type(params["manual_parcellation"])}` expected `str`')
    if params.get("subjlist_file", None) is not None:
        if not isinstance(params["subjlist_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`subjlist_file` has the wrong type: Received `{type(params["subjlist_file"])}` expected `InputPathType`')
    if params.get("left_hemi", None) is not None:
        if not isinstance(params["left_hemi"], bool):
            raise StyxValidationError(f'`left_hemi` has the wrong type: Received `{type(params["left_hemi"])}` expected `bool`')
    if params.get("right_hemi", None) is not None:
        if not isinstance(params["right_hemi"], bool):
            raise StyxValidationError(f'`right_hemi` has the wrong type: Received `{type(params["right_hemi"])}` expected `bool`')
    if params.get("hemi_spec", None) is not None:
        if not isinstance(params["hemi_spec"], str):
            raise StyxValidationError(f'`hemi_spec` has the wrong type: Received `{type(params["hemi_spec"])}` expected `str`')
    if params.get("output_gcs", None) is None:
        raise StyxValidationError("`output_gcs` must not be None")
    if not isinstance(params["output_gcs"], str):
        raise StyxValidationError(f'`output_gcs` has the wrong type: Received `{type(params["output_gcs"])}` expected `str`')
    if params.get("surf_reg", None) is not None:
        if not isinstance(params["surf_reg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surf_reg` has the wrong type: Received `{type(params["surf_reg"])}` expected `InputPathType`')
    if params.get("color_table", None) is not None:
        if not isinstance(params["color_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`color_table` has the wrong type: Received `{type(params["color_table"])}` expected `InputPathType`')
    if params.get("exclude_subject", None) is not None:
        if not isinstance(params["exclude_subject"], str):
            raise StyxValidationError(f'`exclude_subject` has the wrong type: Received `{type(params["exclude_subject"])}` expected `str`')
    if params.get("jackknife_flag", None) is not None:
        if not isinstance(params["jackknife_flag"], bool):
            raise StyxValidationError(f'`jackknife_flag` has the wrong type: Received `{type(params["jackknife_flag"])}` expected `bool`')
    if params.get("aseg_filename", None) is not None:
        if not isinstance(params["aseg_filename"], str):
            raise StyxValidationError(f'`aseg_filename` has the wrong type: Received `{type(params["aseg_filename"])}` expected `str`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')

def train_gcs_atlas_cargs(params: TrainGcsAtlas, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("train-gcs-atlas")
    v_0 = params.get("manual_parcellation")
    if v_0 is not None:
        cargs.append("--man")
        cargs.append(v_0)
    v_1 = params.get("subjlist_file")
    if v_1 is not None:
        cargs.append("--f")
        cargs.append(execution.input_file(v_1))
    if params.get("left_hemi"):
        cargs.append("--lh")
    if params.get("right_hemi"):
        cargs.append("--rh")
    v_2 = params.get("hemi_spec")
    if v_2 is not None:
        cargs.append("--hemi")
        cargs.append(v_2)
    cargs.append("--o")
    cargs.append(params["output_gcs"])
    v_3 = params.get("surf_reg")
    if v_3 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("color_table")
    if v_4 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("exclude_subject")
    if v_5 is not None:
        cargs.append("--x")
        cargs.append(v_5)
    if params.get("jackknife_flag"):
        cargs.append("--jackknife")
    v_6 = params.get("aseg_filename")
    if v_6 is not None:
        cargs.append("--aseg")
        cargs.append(v_6)
    v_7 = params.get("threads")
    if v_7 is not None:
        cargs.append("--threads")
        cargs.append(str(v_7))
    return cargs

def train_gcs_atlas_outputs(params: TrainGcsAtlas, execution: Execution) -> TrainGcsAtlasOutputs:
    """Build the TrainGcsAtlasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_gcs_file_v: OutputPathType = execution.output_file(params["output_gcs"])
    return TrainGcsAtlasOutputs(
        root=root_v,
        output_gcs_file=output_gcs_file_v,
    )

def train_gcs_atlas_execute(params: TrainGcsAtlas, runner: Runner | None = None) -> TrainGcsAtlasOutputs:
    """
    train-gcs-atlas

    Script to train a surface-based gaussian classifier for cortical surface parcellation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    train_gcs_atlas_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRAIN_GCS_ATLAS_METADATA)
    execution.params(params)
    args = train_gcs_atlas_cargs(params, execution)
    out = train_gcs_atlas_outputs(params, execution)
    execution.run(args)
    return out

def train_gcs_atlas(
    output_gcs: str,
    manual_parcellation: str | None = None,
    subjlist_file: InputPathType | None = None,
    left_hemi: bool = False,
    right_hemi: bool = False,
    hemi_spec: str | None = None,
    surf_reg: InputPathType | None = None,
    color_table: InputPathType | None = None,
    exclude_subject: str | None = None,
    jackknife_flag: bool = False,
    aseg_filename: str | None = None,
    threads: float | None = None,
    runner: Runner | None = None,
) -> TrainGcsAtlasOutputs:
    """
    train-gcs-atlas

    Script to train a surface-based gaussian classifier for cortical surface parcellation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_gcs: Output GCS file
        manual_parcellation: Manual parcellation; default is aparc_edited
        subjlist_file: File containing the list of subjects
        left_hemi: Left hemisphere processing
        right_hemi: Right hemisphere processing
        hemi_spec: Specify hemisphere for processing
        surf_reg: Surface registration file; default is sphere.reg
        color_table: Color table file
        exclude_subject: Exclude a subject from the atlas
        jackknife_flag: Submit a job for each subject excluding it
        aseg_filename: Aseg filename; default is aseg.auto.mgz
        threads: Number of threads to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = train_gcs_atlas_params(
        output_gcs=output_gcs,
        manual_parcellation=manual_parcellation,
        subjlist_file=subjlist_file,
        left_hemi=left_hemi,
        right_hemi=right_hemi,
        hemi_spec=hemi_spec,
        surf_reg=surf_reg,
        color_table=color_table,
        exclude_subject=exclude_subject,
        jackknife_flag=jackknife_flag,
        aseg_filename=aseg_filename,
        threads=threads,
    )
    return train_gcs_atlas_execute(params, runner)

__all__ = [
    "TrainGcsAtlas",
    "TrainGcsAtlasOutputs",
    "TRAIN_GCS_ATLAS_METADATA",
    "train_gcs_atlas_cargs",
    "train_gcs_atlas_outputs",
    "train_gcs_atlas_params",
    "train_gcs_atlas_execute",
    "train_gcs_atlas_validate",
    "train_gcs_atlas",
]