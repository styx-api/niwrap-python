# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONF2HIRES_METADATA = Metadata(
    id="conf2hires",
    name="conf2hires",
    package="freesurfer",
)

Conf2hires = typing.TypedDict(
    "Conf2hires",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/conf2hires"]],
        "subject": str,
        "t2": typing.NotRequired[bool],
        "no_t2": typing.NotRequired[bool],
        "mm_norm_sigma": typing.NotRequired[float],
        "flair": typing.NotRequired[bool],
        "no_flair": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "copy_bias_from_conf": typing.NotRequired[bool],
        "norm_opts_rca": typing.NotRequired[bool],
        "cubic": typing.NotRequired[bool],
        "trilin": typing.NotRequired[bool],
        "dev": typing.NotRequired[bool],
        "no_dev": typing.NotRequired[bool],
        "bbr_con": typing.NotRequired[str],
        "bbr_t1": typing.NotRequired[bool],
        "bbr_t2": typing.NotRequired[bool],
        "first_peak_d1": typing.NotRequired[bool],
        "first_peak_d2": typing.NotRequired[bool],
        "stopmask": typing.NotRequired[str],
        "expert": typing.NotRequired[str],
        "force_update": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Conf2hiresOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def conf2hires_params(
    subject: str,
    t2: bool = False,
    no_t2: bool = False,
    mm_norm_sigma: float | None = None,
    flair: bool = False,
    no_flair: bool = False,
    threads: float | None = None,
    copy_bias_from_conf: bool = False,
    norm_opts_rca: bool = False,
    cubic: bool = False,
    trilin: bool = False,
    dev: bool = False,
    no_dev: bool = False,
    bbr_con: str | None = None,
    bbr_t1: bool = False,
    bbr_t2: bool = False,
    first_peak_d1: bool = False,
    first_peak_d2: bool = False,
    stopmask: str | None = None,
    expert: str | None = None,
    force_update: bool = False,
) -> Conf2hires:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        t2: Enable T2 processing
        no_t2: Disable T2 processing (default)
        mm_norm_sigma: Smoothing level for T2 mri_normalize (default is 8)
        flair: Enable FLAIR processing
        no_flair: Disable FLAIR processing (default)
        threads: Number of threads to use
        copy_bias_from_conf: Copy bias field from conformed instead of computing\
            directly
        norm_opts_rca: Compute bias directly using recon-all opts to mri_normalize
        cubic: Use cubic normalization (applies with --copy-bias-from-conf)
        trilin: Use trilinear normalization (default, applies with\
            --copy-bias-from-conf)
        dev: Use mris_make_surfaces.dev
        no_dev: Do not use mris_make_surfaces.dev (default)
        bbr_con: Set BBR contrast type (default t2)
        bbr_t1: Set BBR contrast type to t1
        bbr_t2: Set BBR contrast type to t2
        first_peak_d1: Refine surface targets in MRIScomputeBorderValues() using\
            first peak method D1
        first_peak_d2: Refine surface targets in MRIScomputeBorderValues() using\
            first peak method D2
        stopmask: Specify stop mask
        expert: Use expert options
        force_update: Force update of final surfaces

    Returns:
        Parameter dictionary.
    """
    params: Conf2hires = {
        "@type": "freesurfer/conf2hires",
        "subject": subject,
        "t2": t2,
        "no_t2": no_t2,
        "flair": flair,
        "no_flair": no_flair,
        "copy_bias_from_conf": copy_bias_from_conf,
        "norm_opts_rca": norm_opts_rca,
        "cubic": cubic,
        "trilin": trilin,
        "dev": dev,
        "no_dev": no_dev,
        "bbr_t1": bbr_t1,
        "bbr_t2": bbr_t2,
        "first_peak_d1": first_peak_d1,
        "first_peak_d2": first_peak_d2,
        "force_update": force_update,
    }
    if mm_norm_sigma is not None:
        params["mm_norm_sigma"] = mm_norm_sigma
    if threads is not None:
        params["threads"] = threads
    if bbr_con is not None:
        params["bbr_con"] = bbr_con
    if stopmask is not None:
        params["stopmask"] = stopmask
    if expert is not None:
        params["expert"] = expert
    return params

def conf2hires_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Conf2hires."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("t2", None) is not None:
        if not isinstance(params["t2"], bool):
            raise StyxValidationError(f'`t2` has the wrong type: Received `{type(params["t2"])}` expected `bool`')
    if params.get("no_t2", None) is not None:
        if not isinstance(params["no_t2"], bool):
            raise StyxValidationError(f'`no_t2` has the wrong type: Received `{type(params["no_t2"])}` expected `bool`')
    if params.get("mm_norm_sigma", None) is not None:
        if not isinstance(params["mm_norm_sigma"], (float, int)):
            raise StyxValidationError(f'`mm_norm_sigma` has the wrong type: Received `{type(params["mm_norm_sigma"])}` expected `float`')
    if params.get("flair", None) is not None:
        if not isinstance(params["flair"], bool):
            raise StyxValidationError(f'`flair` has the wrong type: Received `{type(params["flair"])}` expected `bool`')
    if params.get("no_flair", None) is not None:
        if not isinstance(params["no_flair"], bool):
            raise StyxValidationError(f'`no_flair` has the wrong type: Received `{type(params["no_flair"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("copy_bias_from_conf", None) is not None:
        if not isinstance(params["copy_bias_from_conf"], bool):
            raise StyxValidationError(f'`copy_bias_from_conf` has the wrong type: Received `{type(params["copy_bias_from_conf"])}` expected `bool`')
    if params.get("norm_opts_rca", None) is not None:
        if not isinstance(params["norm_opts_rca"], bool):
            raise StyxValidationError(f'`norm_opts_rca` has the wrong type: Received `{type(params["norm_opts_rca"])}` expected `bool`')
    if params.get("cubic", None) is not None:
        if not isinstance(params["cubic"], bool):
            raise StyxValidationError(f'`cubic` has the wrong type: Received `{type(params["cubic"])}` expected `bool`')
    if params.get("trilin", None) is not None:
        if not isinstance(params["trilin"], bool):
            raise StyxValidationError(f'`trilin` has the wrong type: Received `{type(params["trilin"])}` expected `bool`')
    if params.get("dev", None) is not None:
        if not isinstance(params["dev"], bool):
            raise StyxValidationError(f'`dev` has the wrong type: Received `{type(params["dev"])}` expected `bool`')
    if params.get("no_dev", None) is not None:
        if not isinstance(params["no_dev"], bool):
            raise StyxValidationError(f'`no_dev` has the wrong type: Received `{type(params["no_dev"])}` expected `bool`')
    if params.get("bbr_con", None) is not None:
        if not isinstance(params["bbr_con"], str):
            raise StyxValidationError(f'`bbr_con` has the wrong type: Received `{type(params["bbr_con"])}` expected `str`')
    if params.get("bbr_t1", None) is not None:
        if not isinstance(params["bbr_t1"], bool):
            raise StyxValidationError(f'`bbr_t1` has the wrong type: Received `{type(params["bbr_t1"])}` expected `bool`')
    if params.get("bbr_t2", None) is not None:
        if not isinstance(params["bbr_t2"], bool):
            raise StyxValidationError(f'`bbr_t2` has the wrong type: Received `{type(params["bbr_t2"])}` expected `bool`')
    if params.get("first_peak_d1", None) is not None:
        if not isinstance(params["first_peak_d1"], bool):
            raise StyxValidationError(f'`first_peak_d1` has the wrong type: Received `{type(params["first_peak_d1"])}` expected `bool`')
    if params.get("first_peak_d2", None) is not None:
        if not isinstance(params["first_peak_d2"], bool):
            raise StyxValidationError(f'`first_peak_d2` has the wrong type: Received `{type(params["first_peak_d2"])}` expected `bool`')
    if params.get("stopmask", None) is not None:
        if not isinstance(params["stopmask"], str):
            raise StyxValidationError(f'`stopmask` has the wrong type: Received `{type(params["stopmask"])}` expected `str`')
    if params.get("expert", None) is not None:
        if not isinstance(params["expert"], str):
            raise StyxValidationError(f'`expert` has the wrong type: Received `{type(params["expert"])}` expected `str`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')

def conf2hires_cargs(params: Conf2hires, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("conf2hires")
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("t2"):
        cargs.append("--t2")
    if params.get("no_t2"):
        cargs.append("--no-t2")
    v_0 = params.get("mm_norm_sigma")
    if v_0 is not None:
        cargs.append("--mm-norm-sigma")
        cargs.append(str(v_0))
    if params.get("flair"):
        cargs.append("--flair")
    if params.get("no_flair"):
        cargs.append("--no-flair")
    v_1 = params.get("threads")
    if v_1 is not None:
        cargs.append("--threads")
        cargs.append(str(v_1))
    if params.get("copy_bias_from_conf"):
        cargs.append("--copy-bias-from-conf")
    if params.get("norm_opts_rca"):
        cargs.append("--norm-opts-rca")
    if params.get("cubic"):
        cargs.append("--cubic")
    if params.get("trilin"):
        cargs.append("--trilin")
    if params.get("dev"):
        cargs.append("--dev")
    if params.get("no_dev"):
        cargs.append("--no-dev")
    v_2 = params.get("bbr_con")
    if v_2 is not None:
        cargs.append("--bbr-con")
        cargs.append(v_2)
    if params.get("bbr_t1"):
        cargs.append("--bbr-t1")
    if params.get("bbr_t2"):
        cargs.append("--bbr-t2")
    if params.get("first_peak_d1"):
        cargs.append("--first-peak-d1")
    if params.get("first_peak_d2"):
        cargs.append("--first-peak-d2")
    v_3 = params.get("stopmask")
    if v_3 is not None:
        cargs.append("--stopmask")
        cargs.append(v_3)
    v_4 = params.get("expert")
    if v_4 is not None:
        cargs.append("--expert")
        cargs.append(v_4)
    if params.get("force_update"):
        cargs.append("--force-update")
    return cargs

def conf2hires_outputs(params: Conf2hires, execution: Execution) -> Conf2hiresOutputs:
    """Build the Conf2hiresOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Conf2hiresOutputs(
        root=root_v,
    )

def conf2hires_execute(params: Conf2hires, runner: Runner | None = None) -> Conf2hiresOutputs:
    """
    conf2hires

    Places the surfaces on high resolution T1 (and maybe T2) volumes based on an initial placement on a conformed volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    conf2hires_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONF2HIRES_METADATA)
    execution.params(params)
    args = conf2hires_cargs(params, execution)
    out = conf2hires_outputs(params, execution)
    execution.run(args)
    return out

def conf2hires(
    subject: str,
    t2: bool = False,
    no_t2: bool = False,
    mm_norm_sigma: float | None = None,
    flair: bool = False,
    no_flair: bool = False,
    threads: float | None = None,
    copy_bias_from_conf: bool = False,
    norm_opts_rca: bool = False,
    cubic: bool = False,
    trilin: bool = False,
    dev: bool = False,
    no_dev: bool = False,
    bbr_con: str | None = None,
    bbr_t1: bool = False,
    bbr_t2: bool = False,
    first_peak_d1: bool = False,
    first_peak_d2: bool = False,
    stopmask: str | None = None,
    expert: str | None = None,
    force_update: bool = False,
    runner: Runner | None = None,
) -> Conf2hiresOutputs:
    """
    conf2hires

    Places the surfaces on high resolution T1 (and maybe T2) volumes based on an initial placement on a conformed volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        t2: Enable T2 processing
        no_t2: Disable T2 processing (default)
        mm_norm_sigma: Smoothing level for T2 mri_normalize (default is 8)
        flair: Enable FLAIR processing
        no_flair: Disable FLAIR processing (default)
        threads: Number of threads to use
        copy_bias_from_conf: Copy bias field from conformed instead of computing\
            directly
        norm_opts_rca: Compute bias directly using recon-all opts to mri_normalize
        cubic: Use cubic normalization (applies with --copy-bias-from-conf)
        trilin: Use trilinear normalization (default, applies with\
            --copy-bias-from-conf)
        dev: Use mris_make_surfaces.dev
        no_dev: Do not use mris_make_surfaces.dev (default)
        bbr_con: Set BBR contrast type (default t2)
        bbr_t1: Set BBR contrast type to t1
        bbr_t2: Set BBR contrast type to t2
        first_peak_d1: Refine surface targets in MRIScomputeBorderValues() using\
            first peak method D1
        first_peak_d2: Refine surface targets in MRIScomputeBorderValues() using\
            first peak method D2
        stopmask: Specify stop mask
        expert: Use expert options
        force_update: Force update of final surfaces
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = conf2hires_params(
        subject=subject,
        t2=t2,
        no_t2=no_t2,
        mm_norm_sigma=mm_norm_sigma,
        flair=flair,
        no_flair=no_flair,
        threads=threads,
        copy_bias_from_conf=copy_bias_from_conf,
        norm_opts_rca=norm_opts_rca,
        cubic=cubic,
        trilin=trilin,
        dev=dev,
        no_dev=no_dev,
        bbr_con=bbr_con,
        bbr_t1=bbr_t1,
        bbr_t2=bbr_t2,
        first_peak_d1=first_peak_d1,
        first_peak_d2=first_peak_d2,
        stopmask=stopmask,
        expert=expert,
        force_update=force_update,
    )
    return conf2hires_execute(params, runner)

__all__ = [
    "Conf2hires",
    "Conf2hiresOutputs",
    "CONF2HIRES_METADATA",
    "conf2hires_cargs",
    "conf2hires_outputs",
    "conf2hires_params",
    "conf2hires_execute",
    "conf2hires_validate",
    "conf2hires",
]