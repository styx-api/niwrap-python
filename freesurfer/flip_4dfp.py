# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FLIP_4DFP_METADATA = Metadata(
    id="flip_4dfp",
    name="flip_4dfp",
    package="freesurfer",
)

Flip4dfpEndianness = typing.Literal["b"] | typing.Literal["l"]

Flip4dfp = typing.TypedDict(
    "Flip4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/flip_4dfp"]],
        "input_image": InputPathType,
        "output_image": typing.NotRequired[str],
        "flip_x": typing.NotRequired[bool],
        "flip_y": typing.NotRequired[bool],
        "flip_z": typing.NotRequired[bool],
        "endianness": typing.NotRequired[Flip4dfpEndianness],
    },
)

@dataclasses.dataclass
class Flip4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    flipped_image: OutputPathType | None
    """Flipped output 4dfp image"""

def flip_4dfp_params(
    input_image: InputPathType,
    output_image: str | None = None,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endianness: Flip4dfpEndianness | None = None,
) -> Flip4dfp:
    """
    Build parameters.

    Args:
        input_image: Input 4dfp image file
        output_image: Output 4dfp image file. Default is input image root with\
            '_flip' suffix.
        flip_x: Flip along x-axis
        flip_y: Flip along y-axis
        flip_z: Flip along z-axis
        endianness: Specify output endianness: 'b' for big endian, 'l' for little\
            endian. Default is input endianness.

    Returns:
        Parameter dictionary.
    """
    params: Flip4dfp = {
        "@type": "freesurfer/flip_4dfp",
        "input_image": input_image,
        "flip_x": flip_x,
        "flip_y": flip_y,
        "flip_z": flip_z,
    }
    if output_image is not None:
        params["output_image"] = output_image
    if endianness is not None:
        params["endianness"] = endianness
    return params

def flip_4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Flip4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("flip_x", None) is not None:
        if not isinstance(params["flip_x"], bool):
            raise StyxValidationError(f'`flip_x` has the wrong type: Received `{type(params["flip_x"])}` expected `bool`')
    if params.get("flip_y", None) is not None:
        if not isinstance(params["flip_y"], bool):
            raise StyxValidationError(f'`flip_y` has the wrong type: Received `{type(params["flip_y"])}` expected `bool`')
    if params.get("flip_z", None) is not None:
        if not isinstance(params["flip_z"], bool):
            raise StyxValidationError(f'`flip_z` has the wrong type: Received `{type(params["flip_z"])}` expected `bool`')
    if params.get("endianness", None) is not None:
        if not isinstance(params["endianness"], str):
            raise StyxValidationError(f'`endianness` has the wrong type: Received `{type(params["endianness"])}` expected `Flip4dfpEndianness`')
        if params["endianness"] not in ["b", "l"]:
            raise StyxValidationError("Parameter `endianness` must be one of [\"b\", \"l\"]")

def flip_4dfp_cargs(params: Flip4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("flip_4dfp")
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("output_image")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("flip_x"):
        cargs.append("-x")
    if params.get("flip_y"):
        cargs.append("-y")
    if params.get("flip_z"):
        cargs.append("-z")
    v_1 = params.get("endianness")
    if v_1 is not None:
        cargs.append("-@")
        cargs.append(str(v_1))
    return cargs

def flip_4dfp_outputs(params: Flip4dfp, execution: Execution) -> Flip4dfpOutputs:
    """Build the Flip4dfpOutputs object for this tool."""
    flipped_image_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_image")
    if __v0 is not None:
        flipped_image_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.4dfp.img')
    return Flip4dfpOutputs(
        root=root_v,
        flipped_image=flipped_image_v,
    )

def flip_4dfp_execute(params: Flip4dfp, runner: Runner | None = None) -> Flip4dfpOutputs:
    """
    flip_4dfp

    A tool to flip 4dfp images along specified axes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    flip_4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FLIP_4DFP_METADATA)
    execution.params(params)
    args = flip_4dfp_cargs(params, execution)
    out = flip_4dfp_outputs(params, execution)
    execution.run(args)
    return out

def flip_4dfp(
    input_image: InputPathType,
    output_image: str | None = None,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endianness: Flip4dfpEndianness | None = None,
    runner: Runner | None = None,
) -> Flip4dfpOutputs:
    """
    flip_4dfp

    A tool to flip 4dfp images along specified axes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input 4dfp image file
        output_image: Output 4dfp image file. Default is input image root with\
            '_flip' suffix.
        flip_x: Flip along x-axis
        flip_y: Flip along y-axis
        flip_z: Flip along z-axis
        endianness: Specify output endianness: 'b' for big endian, 'l' for little\
            endian. Default is input endianness.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = flip_4dfp_params(
        input_image=input_image,
        output_image=output_image,
        flip_x=flip_x,
        flip_y=flip_y,
        flip_z=flip_z,
        endianness=endianness,
    )
    return flip_4dfp_execute(params, runner)

__all__ = [
    "Flip4dfp",
    "Flip4dfpOutputs",
    "FLIP_4DFP_METADATA",
    "flip_4dfp_cargs",
    "flip_4dfp_outputs",
    "flip_4dfp_params",
    "flip_4dfp_execute",
    "flip_4dfp_validate",
    "flip_4dfp",
]