# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

STEM2FNAME_METADATA = Metadata(
    id="stem2fname",
    name="stem2fname",
    package="freesurfer",
)

Stem2fname = typing.TypedDict(
    "Stem2fname",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/stem2fname"]],
        "stem": str,
    },
)

@dataclasses.dataclass
class Stem2fnameOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Full filename with the detected format. Format could be one of mgh, mgz, nii, nii.gz, bhdr, img, or w."""

def stem2fname_params(
    stem: str,
) -> Stem2fname:
    """
    Build parameters.

    Args:
        stem: The stem of the file (without extension).

    Returns:
        Parameter dictionary.
    """
    params: Stem2fname = {
        "@type": "freesurfer/stem2fname",
        "stem": stem,
    }
    return params

def stem2fname_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Stem2fname."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stem", None) is None:
        raise StyxValidationError("`stem` must not be None")
    if not isinstance(params["stem"], str):
        raise StyxValidationError(f'`stem` has the wrong type: Received `{type(params["stem"])}` expected `str`')

def stem2fname_cargs(params: Stem2fname, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("stem2fname")
    cargs.append(params["stem"])
    return cargs

def stem2fname_outputs(params: Stem2fname, execution: Execution) -> Stem2fnameOutputs:
    """Build the Stem2fnameOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["stem"]}.[FORMAT]')
    return Stem2fnameOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def stem2fname_execute(params: Stem2fname, runner: Runner | None = None) -> Stem2fnameOutputs:
    """
    stem2fname

    Determines the full filename with extension for a given file stem by checking various formats.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    stem2fname_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(STEM2FNAME_METADATA)
    execution.params(params)
    args = stem2fname_cargs(params, execution)
    out = stem2fname_outputs(params, execution)
    execution.run(args)
    return out

def stem2fname(
    stem: str,
    runner: Runner | None = None,
) -> Stem2fnameOutputs:
    """
    stem2fname

    Determines the full filename with extension for a given file stem by checking various formats.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        stem: The stem of the file (without extension).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = stem2fname_params(
        stem=stem,
    )
    return stem2fname_execute(params, runner)

__all__ = [
    "Stem2fname",
    "Stem2fnameOutputs",
    "STEM2FNAME_METADATA",
    "stem2fname_cargs",
    "stem2fname_outputs",
    "stem2fname_params",
    "stem2fname_execute",
    "stem2fname_validate",
    "stem2fname",
]