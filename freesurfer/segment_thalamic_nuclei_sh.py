# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_THALAMIC_NUCLEI_SH_METADATA = Metadata(
    id="segmentThalamicNuclei.sh",
    name="segmentThalamicNuclei.sh",
    package="freesurfer",
)

SegmentThalamicNucleiSh = typing.TypedDict(
    "SegmentThalamicNucleiSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segmentThalamicNuclei.sh"]],
        "subject_id": str,
        "output_dir": str,
    },
)

@dataclasses.dataclass
class SegmentThalamicNucleiShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    thalamic_nuclei_output: OutputPathType
    """Segmented thalamic nuclei output file"""

def segment_thalamic_nuclei_sh_params(
    subject_id: str,
    output_dir: str,
) -> SegmentThalamicNucleiSh:
    """
    Build parameters.

    Args:
        subject_id: Subject ID for segmentation
        output_dir: Output directory for the segmented thalamic nuclei

    Returns:
        Parameter dictionary.
    """
    params: SegmentThalamicNucleiSh = {
        "@type": "freesurfer/segmentThalamicNuclei.sh",
        "subject_id": subject_id,
        "output_dir": output_dir,
    }
    return params

def segment_thalamic_nuclei_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentThalamicNucleiSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')

def segment_thalamic_nuclei_sh_cargs(params: SegmentThalamicNucleiSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segmentThalamicNuclei.sh")
    cargs.append(params["subject_id"])
    cargs.append(params["output_dir"])
    return cargs

def segment_thalamic_nuclei_sh_outputs(params: SegmentThalamicNucleiSh, execution: Execution) -> SegmentThalamicNucleiShOutputs:
    """Build the SegmentThalamicNucleiShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    thalamic_nuclei_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/{params["subject_id"]}_thalamic_nuclei.mgz')
    return SegmentThalamicNucleiShOutputs(
        root=root_v,
        thalamic_nuclei_output=thalamic_nuclei_output_v,
    )

def segment_thalamic_nuclei_sh_execute(params: SegmentThalamicNucleiSh, runner: Runner | None = None) -> SegmentThalamicNucleiShOutputs:
    """
    segmentThalamicNuclei.sh

    Segment the thalamic nuclei using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_thalamic_nuclei_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_THALAMIC_NUCLEI_SH_METADATA)
    execution.params(params)
    args = segment_thalamic_nuclei_sh_cargs(params, execution)
    out = segment_thalamic_nuclei_sh_outputs(params, execution)
    execution.run(args)
    return out

def segment_thalamic_nuclei_sh(
    subject_id: str,
    output_dir: str,
    runner: Runner | None = None,
) -> SegmentThalamicNucleiShOutputs:
    """
    segmentThalamicNuclei.sh

    Segment the thalamic nuclei using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id: Subject ID for segmentation
        output_dir: Output directory for the segmented thalamic nuclei
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_thalamic_nuclei_sh_params(
        subject_id=subject_id,
        output_dir=output_dir,
    )
    return segment_thalamic_nuclei_sh_execute(params, runner)

__all__ = [
    "SegmentThalamicNucleiSh",
    "SegmentThalamicNucleiShOutputs",
    "SEGMENT_THALAMIC_NUCLEI_SH_METADATA",
    "segment_thalamic_nuclei_sh_cargs",
    "segment_thalamic_nuclei_sh_outputs",
    "segment_thalamic_nuclei_sh_params",
    "segment_thalamic_nuclei_sh_execute",
    "segment_thalamic_nuclei_sh_validate",
    "segment_thalamic_nuclei_sh",
]