# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MOTION_CORRECT2_METADATA = Metadata(
    id="mri_motion_correct2",
    name="mri_motion_correct2",
    package="freesurfer",
)

MriMotionCorrect2 = typing.TypedDict(
    "MriMotionCorrect2",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_motion_correct2"]],
        "output_spec": str,
        "input_files": list[InputPathType],
        "target": typing.NotRequired[InputPathType],
        "wild": typing.NotRequired[bool],
        "tmp_dir": typing.NotRequired[str],
        "nocleanup": typing.NotRequired[bool],
        "umask": typing.NotRequired[str],
        "cm": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriMotionCorrect2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The aligned and averaged output file or directory"""

def mri_motion_correct2_params(
    output_spec: str,
    input_files: list[InputPathType],
    target: InputPathType | None = None,
    wild: bool = False,
    tmp_dir: str | None = None,
    nocleanup: bool = False,
    umask: str | None = None,
    cm: bool = False,
    version: bool = False,
    debug: bool = False,
) -> MriMotionCorrect2:
    """
    Build parameters.

    Args:
        output_spec: Output file or directory (for COR format)
        input_files: Input volume files, specified multiple times for multiple\
            files
        target: Use the specified target instead of the first input file
        wild: Assume unmatched arguments are input files
        tmp_dir: Directory for temporary files
        nocleanup: Do not delete temporary files
        umask: Set UNIX file permission mask
        cm: Make COR volumes conform to minimum voxel size
        version: Print version information and exit
        debug: Print detailed debug information to screen

    Returns:
        Parameter dictionary.
    """
    params: MriMotionCorrect2 = {
        "@type": "freesurfer/mri_motion_correct2",
        "output_spec": output_spec,
        "input_files": input_files,
        "wild": wild,
        "nocleanup": nocleanup,
        "cm": cm,
        "version": version,
        "debug": debug,
    }
    if target is not None:
        params["target"] = target
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    if umask is not None:
        params["umask"] = umask
    return params

def mri_motion_correct2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMotionCorrect2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_spec", None) is None:
        raise StyxValidationError("`output_spec` must not be None")
    if not isinstance(params["output_spec"], str):
        raise StyxValidationError(f'`output_spec` has the wrong type: Received `{type(params["output_spec"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("target", None) is not None:
        if not isinstance(params["target"], (pathlib.Path, str)):
            raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("wild", None) is not None:
        if not isinstance(params["wild"], bool):
            raise StyxValidationError(f'`wild` has the wrong type: Received `{type(params["wild"])}` expected `bool`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')
    if params.get("cm", None) is not None:
        if not isinstance(params["cm"], bool):
            raise StyxValidationError(f'`cm` has the wrong type: Received `{type(params["cm"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def mri_motion_correct2_cargs(params: MriMotionCorrect2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_motion_correct2")
    cargs.append("-o")
    cargs.append(params["output_spec"])
    cargs.append("-i")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("target")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(execution.input_file(v_0))
    if params.get("wild"):
        cargs.append("-wild")
    v_1 = params.get("tmp_dir")
    if v_1 is not None:
        cargs.append("-tmpdir")
        cargs.append(v_1)
    if params.get("nocleanup"):
        cargs.append("-nocleanup")
    v_2 = params.get("umask")
    if v_2 is not None:
        cargs.append("-umask")
        cargs.append(v_2)
    if params.get("cm"):
        cargs.append("-cm")
    if params.get("version"):
        cargs.append("-version")
    if params.get("debug"):
        cargs.append("-debug")
    return cargs

def mri_motion_correct2_outputs(params: MriMotionCorrect2, execution: Execution) -> MriMotionCorrect2Outputs:
    """Build the MriMotionCorrect2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_spec"])
    return MriMotionCorrect2Outputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_motion_correct2_execute(params: MriMotionCorrect2, runner: Runner | None = None) -> MriMotionCorrect2Outputs:
    """
    mri_motion_correct2

    Aligns and averages two or more volumes using minctracc for alignment and mincresample and mincaverage for resampling.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_motion_correct2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MOTION_CORRECT2_METADATA)
    execution.params(params)
    args = mri_motion_correct2_cargs(params, execution)
    out = mri_motion_correct2_outputs(params, execution)
    execution.run(args)
    return out

def mri_motion_correct2(
    output_spec: str,
    input_files: list[InputPathType],
    target: InputPathType | None = None,
    wild: bool = False,
    tmp_dir: str | None = None,
    nocleanup: bool = False,
    umask: str | None = None,
    cm: bool = False,
    version: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> MriMotionCorrect2Outputs:
    """
    mri_motion_correct2

    Aligns and averages two or more volumes using minctracc for alignment and mincresample and mincaverage for resampling.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_spec: Output file or directory (for COR format)
        input_files: Input volume files, specified multiple times for multiple\
            files
        target: Use the specified target instead of the first input file
        wild: Assume unmatched arguments are input files
        tmp_dir: Directory for temporary files
        nocleanup: Do not delete temporary files
        umask: Set UNIX file permission mask
        cm: Make COR volumes conform to minimum voxel size
        version: Print version information and exit
        debug: Print detailed debug information to screen
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_motion_correct2_params(
        output_spec=output_spec,
        input_files=input_files,
        target=target,
        wild=wild,
        tmp_dir=tmp_dir,
        nocleanup=nocleanup,
        umask=umask,
        cm=cm,
        version=version,
        debug=debug,
    )
    return mri_motion_correct2_execute(params, runner)

__all__ = [
    "MriMotionCorrect2",
    "MriMotionCorrect2Outputs",
    "MRI_MOTION_CORRECT2_METADATA",
    "mri_motion_correct2_cargs",
    "mri_motion_correct2_outputs",
    "mri_motion_correct2_params",
    "mri_motion_correct2_execute",
    "mri_motion_correct2_validate",
    "mri_motion_correct2",
]