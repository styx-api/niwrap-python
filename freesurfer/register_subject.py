# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REGISTER_SUBJECT_METADATA = Metadata(
    id="register_subject",
    name="register_subject",
    package="freesurfer",
)

RegisterSubject = typing.TypedDict(
    "RegisterSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/register_subject"]],
        "input_volume": typing.NotRequired[InputPathType],
        "mask_volume": typing.NotRequired[InputPathType],
        "control_points": typing.NotRequired[str],
        "output_directory": typing.NotRequired[str],
        "log_file": typing.NotRequired[InputPathType],
        "gca_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class RegisterSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    normalized_output: OutputPathType | None
    """Intensity normalized output volume."""
    transformed_fsamples: OutputPathType | None
    """Transformed control points."""

def register_subject_params(
    input_volume: InputPathType | None = None,
    mask_volume: InputPathType | None = None,
    control_points: str | None = None,
    output_directory: str | None = None,
    log_file: InputPathType | None = None,
    gca_file: InputPathType | None = None,
) -> RegisterSubject:
    """
    Build parameters.

    Args:
        input_volume: Input volume for registration (e.g., brain.mgz)
        mask_volume: MR volume used to mask input volume.
        control_points: Control points used for registration.
        output_directory: Directory to write output files (e.g., transformed\
            fsamples).
        log_file: Log file for recording registration results.
        gca_file: GCA file required for registration.

    Returns:
        Parameter dictionary.
    """
    params: RegisterSubject = {
        "@type": "freesurfer/register_subject",
    }
    if input_volume is not None:
        params["input_volume"] = input_volume
    if mask_volume is not None:
        params["mask_volume"] = mask_volume
    if control_points is not None:
        params["control_points"] = control_points
    if output_directory is not None:
        params["output_directory"] = output_directory
    if log_file is not None:
        params["log_file"] = log_file
    if gca_file is not None:
        params["gca_file"] = gca_file
    return params

def register_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegisterSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is not None:
        if not isinstance(params["input_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("mask_volume", None) is not None:
        if not isinstance(params["mask_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `InputPathType`')
    if params.get("control_points", None) is not None:
        if not isinstance(params["control_points"], str):
            raise StyxValidationError(f'`control_points` has the wrong type: Received `{type(params["control_points"])}` expected `str`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `InputPathType`')
    if params.get("gca_file", None) is not None:
        if not isinstance(params["gca_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')

def register_subject_cargs(params: RegisterSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("register_subject")
    v_0 = params.get("input_volume")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask_volume")
    if v_1 is not None:
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("control_points")
    if v_2 is not None:
        cargs.append(v_2)
    v_3 = params.get("output_directory")
    if v_3 is not None:
        cargs.append(v_3)
    v_4 = params.get("log_file")
    if v_4 is not None:
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("gca_file")
    if v_5 is not None:
        cargs.append(execution.input_file(v_5))
    return cargs

def register_subject_outputs(params: RegisterSubject, execution: Execution) -> RegisterSubjectOutputs:
    """Build the RegisterSubjectOutputs object for this tool."""
    normalized_output_v: OutputPathType | None = None
    transformed_fsamples_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_directory")
    if __v0 is not None:
        normalized_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/norm.mgz')
    __v1 = params.get("output_directory")
    if __v1 is not None:
        transformed_fsamples_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/fsamples')
    return RegisterSubjectOutputs(
        root=root_v,
        normalized_output=normalized_output_v,
        transformed_fsamples=transformed_fsamples_v,
    )

def register_subject_execute(params: RegisterSubject, runner: Runner | None = None) -> RegisterSubjectOutputs:
    """
    register_subject

    Tool for registering brain MR volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    register_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REGISTER_SUBJECT_METADATA)
    execution.params(params)
    args = register_subject_cargs(params, execution)
    out = register_subject_outputs(params, execution)
    execution.run(args)
    return out

def register_subject(
    input_volume: InputPathType | None = None,
    mask_volume: InputPathType | None = None,
    control_points: str | None = None,
    output_directory: str | None = None,
    log_file: InputPathType | None = None,
    gca_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> RegisterSubjectOutputs:
    """
    register_subject

    Tool for registering brain MR volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume for registration (e.g., brain.mgz)
        mask_volume: MR volume used to mask input volume.
        control_points: Control points used for registration.
        output_directory: Directory to write output files (e.g., transformed\
            fsamples).
        log_file: Log file for recording registration results.
        gca_file: GCA file required for registration.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = register_subject_params(
        input_volume=input_volume,
        mask_volume=mask_volume,
        control_points=control_points,
        output_directory=output_directory,
        log_file=log_file,
        gca_file=gca_file,
    )
    return register_subject_execute(params, runner)

__all__ = [
    "RegisterSubject",
    "RegisterSubjectOutputs",
    "REGISTER_SUBJECT_METADATA",
    "register_subject_cargs",
    "register_subject_outputs",
    "register_subject_params",
    "register_subject_execute",
    "register_subject_validate",
    "register_subject",
]