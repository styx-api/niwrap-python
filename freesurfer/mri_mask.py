# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MASK_METADATA = Metadata(
    id="mri_mask",
    name="mri_mask",
    package="freesurfer",
)

MriMask = typing.TypedDict(
    "MriMask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mask"]],
        "input_volume": InputPathType,
        "mask_volume": InputPathType,
        "output_volume": str,
        "xform": typing.NotRequired[str],
        "lta_src": typing.NotRequired[str],
        "lta_dst": typing.NotRequired[str],
        "threshold": typing.NotRequired[float],
        "npad": typing.NotRequired[float],
        "npad_vector": typing.NotRequired[list[float]],
        "npad_multi_vector": typing.NotRequired[list[float]],
        "abs": typing.NotRequired[bool],
        "invert": typing.NotRequired[bool],
        "no_invert": typing.NotRequired[bool],
        "rh_labels": typing.NotRequired[bool],
        "lh_labels": typing.NotRequired[bool],
        "dilate": typing.NotRequired[float],
        "no_cerebellum": typing.NotRequired[bool],
        "oval_value": typing.NotRequired[float],
        "transfer_value": typing.NotRequired[float],
        "keep_mask_deletion_edits": typing.NotRequired[bool],
        "samseg": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriMaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume file"""

def mri_mask_params(
    input_volume: InputPathType,
    mask_volume: InputPathType,
    output_volume: str,
    xform: str | None = None,
    lta_src: str | None = None,
    lta_dst: str | None = None,
    threshold: float | None = None,
    npad: float | None = None,
    npad_vector: list[float] | None = None,
    npad_multi_vector: list[float] | None = None,
    abs: bool = False,
    invert: bool = False,
    no_invert: bool = False,
    rh_labels: bool = False,
    lh_labels: bool = False,
    dilate: float | None = None,
    no_cerebellum: bool = False,
    oval_value: float | None = None,
    transfer_value: float | None = None,
    keep_mask_deletion_edits: bool = False,
    samseg: bool = False,
) -> MriMask:
    """
    Build parameters.

    Args:
        input_volume: Input volume file
        mask_volume: Mask volume file
        output_volume: Output volume file
        xform: Apply M3Z/LTA to transform mask to the space of input volume\
            (identity.nofile possible, will invert if needed)
        lta_src: Source volume for -xform (if not available from the xform file)
        lta_dst: Destination volume for -xform (if not available from the xform\
            file)
        threshold: Threshold mask volume at a given threshold (values <= threshold\
            considered zero)
        npad: Create a bounding box around the mask expanded by npad voxels in each\
            direction
        npad_vector: Create a bounding box around the mask expanded by npad1 npad2\
            npad3 voxels in each direction
        npad_multi_vector: Create a bounding box around the mask, expanded by\
            npad1a npad1b npad2a npad2b npad3a npad3b in each direction
        abs: Take absolute value before applying threshold
        invert: Invert mask
        no_invert: Turn off inversion of mask
        rh_labels: Set mask in right hemisphere labels to 1 (assumes input mask is\
            an aseg)
        lh_labels: Set mask in left hemisphere labels to 1 (assumes input mask is\
            an aseg)
        dilate: Dilate mask N times before applying
        no_cerebellum: Remove cerebellum from aseg mask (assumes input mask is an\
            aseg)
        oval_value: Use specified oval value as output instead of 0
        transfer_value: Transfer only the specified voxel value from mask to output
        keep_mask_deletion_edits: Transfer voxel-deletion edits (voxels=1) from\
            mask to output volume
        samseg: Assume mask is a SAMSEG segmentation and mask all non-brain labels

    Returns:
        Parameter dictionary.
    """
    params: MriMask = {
        "@type": "freesurfer/mri_mask",
        "input_volume": input_volume,
        "mask_volume": mask_volume,
        "output_volume": output_volume,
        "abs": abs,
        "invert": invert,
        "no_invert": no_invert,
        "rh_labels": rh_labels,
        "lh_labels": lh_labels,
        "no_cerebellum": no_cerebellum,
        "keep_mask_deletion_edits": keep_mask_deletion_edits,
        "samseg": samseg,
    }
    if xform is not None:
        params["xform"] = xform
    if lta_src is not None:
        params["lta_src"] = lta_src
    if lta_dst is not None:
        params["lta_dst"] = lta_dst
    if threshold is not None:
        params["threshold"] = threshold
    if npad is not None:
        params["npad"] = npad
    if npad_vector is not None:
        params["npad_vector"] = npad_vector
    if npad_multi_vector is not None:
        params["npad_multi_vector"] = npad_multi_vector
    if dilate is not None:
        params["dilate"] = dilate
    if oval_value is not None:
        params["oval_value"] = oval_value
    if transfer_value is not None:
        params["transfer_value"] = transfer_value
    return params

def mri_mask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("mask_volume", None) is None:
        raise StyxValidationError("`mask_volume` must not be None")
    if not isinstance(params["mask_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("xform", None) is not None:
        if not isinstance(params["xform"], str):
            raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `str`')
    if params.get("lta_src", None) is not None:
        if not isinstance(params["lta_src"], str):
            raise StyxValidationError(f'`lta_src` has the wrong type: Received `{type(params["lta_src"])}` expected `str`')
    if params.get("lta_dst", None) is not None:
        if not isinstance(params["lta_dst"], str):
            raise StyxValidationError(f'`lta_dst` has the wrong type: Received `{type(params["lta_dst"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("npad", None) is not None:
        if not isinstance(params["npad"], (float, int)):
            raise StyxValidationError(f'`npad` has the wrong type: Received `{type(params["npad"])}` expected `float`')
    if params.get("npad_vector", None) is not None:
        if not isinstance(params["npad_vector"], list):
            raise StyxValidationError(f'`npad_vector` has the wrong type: Received `{type(params["npad_vector"])}` expected `list[float]`')
        for e in params["npad_vector"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`npad_vector` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("npad_multi_vector", None) is not None:
        if not isinstance(params["npad_multi_vector"], list):
            raise StyxValidationError(f'`npad_multi_vector` has the wrong type: Received `{type(params["npad_multi_vector"])}` expected `list[float]`')
        for e_2 in params["npad_multi_vector"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`npad_multi_vector` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("abs", None) is not None:
        if not isinstance(params["abs"], bool):
            raise StyxValidationError(f'`abs` has the wrong type: Received `{type(params["abs"])}` expected `bool`')
    if params.get("invert", None) is not None:
        if not isinstance(params["invert"], bool):
            raise StyxValidationError(f'`invert` has the wrong type: Received `{type(params["invert"])}` expected `bool`')
    if params.get("no_invert", None) is not None:
        if not isinstance(params["no_invert"], bool):
            raise StyxValidationError(f'`no_invert` has the wrong type: Received `{type(params["no_invert"])}` expected `bool`')
    if params.get("rh_labels", None) is not None:
        if not isinstance(params["rh_labels"], bool):
            raise StyxValidationError(f'`rh_labels` has the wrong type: Received `{type(params["rh_labels"])}` expected `bool`')
    if params.get("lh_labels", None) is not None:
        if not isinstance(params["lh_labels"], bool):
            raise StyxValidationError(f'`lh_labels` has the wrong type: Received `{type(params["lh_labels"])}` expected `bool`')
    if params.get("dilate", None) is not None:
        if not isinstance(params["dilate"], (float, int)):
            raise StyxValidationError(f'`dilate` has the wrong type: Received `{type(params["dilate"])}` expected `float`')
    if params.get("no_cerebellum", None) is not None:
        if not isinstance(params["no_cerebellum"], bool):
            raise StyxValidationError(f'`no_cerebellum` has the wrong type: Received `{type(params["no_cerebellum"])}` expected `bool`')
    if params.get("oval_value", None) is not None:
        if not isinstance(params["oval_value"], (float, int)):
            raise StyxValidationError(f'`oval_value` has the wrong type: Received `{type(params["oval_value"])}` expected `float`')
    if params.get("transfer_value", None) is not None:
        if not isinstance(params["transfer_value"], (float, int)):
            raise StyxValidationError(f'`transfer_value` has the wrong type: Received `{type(params["transfer_value"])}` expected `float`')
    if params.get("keep_mask_deletion_edits", None) is not None:
        if not isinstance(params["keep_mask_deletion_edits"], bool):
            raise StyxValidationError(f'`keep_mask_deletion_edits` has the wrong type: Received `{type(params["keep_mask_deletion_edits"])}` expected `bool`')
    if params.get("samseg", None) is not None:
        if not isinstance(params["samseg"], bool):
            raise StyxValidationError(f'`samseg` has the wrong type: Received `{type(params["samseg"])}` expected `bool`')

def mri_mask_cargs(params: MriMask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mask")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["mask_volume"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("xform")
    if v_0 is not None:
        cargs.append("-xform")
        cargs.append(v_0)
    v_1 = params.get("lta_src")
    if v_1 is not None:
        cargs.append("-lta_src")
        cargs.append(v_1)
    v_2 = params.get("lta_dst")
    if v_2 is not None:
        cargs.append("-lta_dst")
        cargs.append(v_2)
    v_3 = params.get("threshold")
    if v_3 is not None:
        cargs.append("-T")
        cargs.append(str(v_3))
    v_4 = params.get("npad")
    if v_4 is not None:
        cargs.append("-bb")
        cargs.append(str(v_4))
    v_5 = params.get("npad_vector")
    if v_5 is not None:
        cargs.append("-bbm")
        for item0 in v_5:
            cargs.append(str(item0))
    v_6 = params.get("npad_multi_vector")
    if v_6 is not None:
        cargs.append("-bbmm")
        for item1 in v_6:
            cargs.append(str(item1))
    if params.get("abs"):
        cargs.append("-abs")
    if params.get("invert"):
        cargs.append("-invert")
    if params.get("no_invert"):
        cargs.append("-no-invert")
    if params.get("rh_labels"):
        cargs.append("-rh")
    if params.get("lh_labels"):
        cargs.append("-lh")
    v_7 = params.get("dilate")
    if v_7 is not None:
        cargs.append("-dilate")
        cargs.append(str(v_7))
    if params.get("no_cerebellum"):
        cargs.append("-no_cerebellum")
    v_8 = params.get("oval_value")
    if v_8 is not None:
        cargs.append("-oval")
        cargs.append(str(v_8))
    v_9 = params.get("transfer_value")
    if v_9 is not None:
        cargs.append("-transfer")
        cargs.append(str(v_9))
    if params.get("keep_mask_deletion_edits"):
        cargs.append("-keep_mask_deletion_edits")
    if params.get("samseg"):
        cargs.append("-samseg")
    return cargs

def mri_mask_outputs(params: MriMask, execution: Execution) -> MriMaskOutputs:
    """Build the MriMaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriMaskOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_mask_execute(params: MriMask, runner: Runner | None = None) -> MriMaskOutputs:
    """
    mri_mask

    Applies a mask volume (typically skull stripped)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MASK_METADATA)
    execution.params(params)
    args = mri_mask_cargs(params, execution)
    out = mri_mask_outputs(params, execution)
    execution.run(args)
    return out

def mri_mask(
    input_volume: InputPathType,
    mask_volume: InputPathType,
    output_volume: str,
    xform: str | None = None,
    lta_src: str | None = None,
    lta_dst: str | None = None,
    threshold: float | None = None,
    npad: float | None = None,
    npad_vector: list[float] | None = None,
    npad_multi_vector: list[float] | None = None,
    abs: bool = False,
    invert: bool = False,
    no_invert: bool = False,
    rh_labels: bool = False,
    lh_labels: bool = False,
    dilate: float | None = None,
    no_cerebellum: bool = False,
    oval_value: float | None = None,
    transfer_value: float | None = None,
    keep_mask_deletion_edits: bool = False,
    samseg: bool = False,
    runner: Runner | None = None,
) -> MriMaskOutputs:
    """
    mri_mask

    Applies a mask volume (typically skull stripped)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file
        mask_volume: Mask volume file
        output_volume: Output volume file
        xform: Apply M3Z/LTA to transform mask to the space of input volume\
            (identity.nofile possible, will invert if needed)
        lta_src: Source volume for -xform (if not available from the xform file)
        lta_dst: Destination volume for -xform (if not available from the xform\
            file)
        threshold: Threshold mask volume at a given threshold (values <= threshold\
            considered zero)
        npad: Create a bounding box around the mask expanded by npad voxels in each\
            direction
        npad_vector: Create a bounding box around the mask expanded by npad1 npad2\
            npad3 voxels in each direction
        npad_multi_vector: Create a bounding box around the mask, expanded by\
            npad1a npad1b npad2a npad2b npad3a npad3b in each direction
        abs: Take absolute value before applying threshold
        invert: Invert mask
        no_invert: Turn off inversion of mask
        rh_labels: Set mask in right hemisphere labels to 1 (assumes input mask is\
            an aseg)
        lh_labels: Set mask in left hemisphere labels to 1 (assumes input mask is\
            an aseg)
        dilate: Dilate mask N times before applying
        no_cerebellum: Remove cerebellum from aseg mask (assumes input mask is an\
            aseg)
        oval_value: Use specified oval value as output instead of 0
        transfer_value: Transfer only the specified voxel value from mask to output
        keep_mask_deletion_edits: Transfer voxel-deletion edits (voxels=1) from\
            mask to output volume
        samseg: Assume mask is a SAMSEG segmentation and mask all non-brain labels
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mask_params(
        input_volume=input_volume,
        mask_volume=mask_volume,
        output_volume=output_volume,
        xform=xform,
        lta_src=lta_src,
        lta_dst=lta_dst,
        threshold=threshold,
        npad=npad,
        npad_vector=npad_vector,
        npad_multi_vector=npad_multi_vector,
        abs=abs,
        invert=invert,
        no_invert=no_invert,
        rh_labels=rh_labels,
        lh_labels=lh_labels,
        dilate=dilate,
        no_cerebellum=no_cerebellum,
        oval_value=oval_value,
        transfer_value=transfer_value,
        keep_mask_deletion_edits=keep_mask_deletion_edits,
        samseg=samseg,
    )
    return mri_mask_execute(params, runner)

__all__ = [
    "MriMask",
    "MriMaskOutputs",
    "MRI_MASK_METADATA",
    "mri_mask_cargs",
    "mri_mask_outputs",
    "mri_mask_params",
    "mri_mask_execute",
    "mri_mask_validate",
    "mri_mask",
]