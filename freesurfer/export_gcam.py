# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EXPORT_GCAM_METADATA = Metadata(
    id="exportGcam",
    name="exportGcam",
    package="freesurfer",
)

ExportGcamInterpMethod = typing.Literal["linear"] | typing.Literal["nearest"]

ExportGcam = typing.TypedDict(
    "ExportGcam",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/exportGcam"]],
        "fixed": InputPathType,
        "moving": InputPathType,
        "morph": InputPathType,
        "out_gcam": str,
        "zlib_buffer": typing.NotRequired[float],
        "bbox_threshold": typing.NotRequired[float],
        "interp_method": typing.NotRequired[ExportGcamInterpMethod],
        "test": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ExportGcamOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_gcam_file: OutputPathType
    """Output GCAM file."""

def export_gcam_params(
    fixed: InputPathType,
    moving: InputPathType,
    morph: InputPathType,
    out_gcam: str,
    zlib_buffer: float | None = None,
    bbox_threshold: float | None = None,
    interp_method: ExportGcamInterpMethod | None = None,
    test: bool = False,
) -> ExportGcam:
    """
    Build parameters.

    Args:
        fixed: Fixed volume.
        moving: Moving volume.
        morph: Morph.
        out_gcam: Output GCAM (Geodesic Coordinate-based Anatomic Mapping).
        zlib_buffer: Zlib buffer pre-allocation multiplier.
        bbox_threshold: Threshold for bounding box. If absent, no bbox will be\
            used.
        interp_method: Interpolation method. Choices: linear, nearest. Default is\
            linear.
        test: Write out test files to verify the equivalence of tm3d and gcam\
            morphs.

    Returns:
        Parameter dictionary.
    """
    params: ExportGcam = {
        "@type": "freesurfer/exportGcam",
        "fixed": fixed,
        "moving": moving,
        "morph": morph,
        "out_gcam": out_gcam,
        "test": test,
    }
    if zlib_buffer is not None:
        params["zlib_buffer"] = zlib_buffer
    if bbox_threshold is not None:
        params["bbox_threshold"] = bbox_threshold
    if interp_method is not None:
        params["interp_method"] = interp_method
    return params

def export_gcam_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ExportGcam."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed", None) is None:
        raise StyxValidationError("`fixed` must not be None")
    if not isinstance(params["fixed"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed` has the wrong type: Received `{type(params["fixed"])}` expected `InputPathType`')
    if params.get("moving", None) is None:
        raise StyxValidationError("`moving` must not be None")
    if not isinstance(params["moving"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving` has the wrong type: Received `{type(params["moving"])}` expected `InputPathType`')
    if params.get("morph", None) is None:
        raise StyxValidationError("`morph` must not be None")
    if not isinstance(params["morph"], (pathlib.Path, str)):
        raise StyxValidationError(f'`morph` has the wrong type: Received `{type(params["morph"])}` expected `InputPathType`')
    if params.get("out_gcam", None) is None:
        raise StyxValidationError("`out_gcam` must not be None")
    if not isinstance(params["out_gcam"], str):
        raise StyxValidationError(f'`out_gcam` has the wrong type: Received `{type(params["out_gcam"])}` expected `str`')
    if params.get("zlib_buffer", None) is not None:
        if not isinstance(params["zlib_buffer"], (float, int)):
            raise StyxValidationError(f'`zlib_buffer` has the wrong type: Received `{type(params["zlib_buffer"])}` expected `float`')
    if params.get("bbox_threshold", None) is not None:
        if not isinstance(params["bbox_threshold"], (float, int)):
            raise StyxValidationError(f'`bbox_threshold` has the wrong type: Received `{type(params["bbox_threshold"])}` expected `float`')
    if params.get("interp_method", None) is not None:
        if not isinstance(params["interp_method"], str):
            raise StyxValidationError(f'`interp_method` has the wrong type: Received `{type(params["interp_method"])}` expected `ExportGcamInterpMethod`')
        if params["interp_method"] not in ["linear", "nearest"]:
            raise StyxValidationError("Parameter `interp_method` must be one of [\"linear\", \"nearest\"]")
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')

def export_gcam_cargs(params: ExportGcam, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("exportGcam")
    cargs.append("--fixed")
    cargs.append(execution.input_file(params["fixed"]))
    cargs.append("--moving")
    cargs.append(execution.input_file(params["moving"]))
    cargs.append("--morph")
    cargs.append(execution.input_file(params["morph"]))
    cargs.append("--out_gcam")
    cargs.append(params["out_gcam"])
    v_0 = params.get("zlib_buffer")
    if v_0 is not None:
        cargs.append("--zlib_buffer")
        cargs.append(str(v_0))
    v_1 = params.get("bbox_threshold")
    if v_1 is not None:
        cargs.append("--bbox_threshold")
        cargs.append(str(v_1))
    v_2 = params.get("interp_method")
    if v_2 is not None:
        cargs.append("--interp")
        cargs.append(str(v_2))
    if params.get("test"):
        cargs.append("--test")
    return cargs

def export_gcam_outputs(params: ExportGcam, execution: Execution) -> ExportGcamOutputs:
    """Build the ExportGcamOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_gcam_file_v: OutputPathType = execution.output_file(params["out_gcam"])
    return ExportGcamOutputs(
        root=root_v,
        output_gcam_file=output_gcam_file_v,
    )

def export_gcam_execute(params: ExportGcam, runner: Runner | None = None) -> ExportGcamOutputs:
    """
    exportGcam

    A tool for exporting GCAM (Geodesic Coordinate-based Anatomic Mapping) morphs.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    export_gcam_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EXPORT_GCAM_METADATA)
    execution.params(params)
    args = export_gcam_cargs(params, execution)
    out = export_gcam_outputs(params, execution)
    execution.run(args)
    return out

def export_gcam(
    fixed: InputPathType,
    moving: InputPathType,
    morph: InputPathType,
    out_gcam: str,
    zlib_buffer: float | None = None,
    bbox_threshold: float | None = None,
    interp_method: ExportGcamInterpMethod | None = None,
    test: bool = False,
    runner: Runner | None = None,
) -> ExportGcamOutputs:
    """
    exportGcam

    A tool for exporting GCAM (Geodesic Coordinate-based Anatomic Mapping) morphs.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        fixed: Fixed volume.
        moving: Moving volume.
        morph: Morph.
        out_gcam: Output GCAM (Geodesic Coordinate-based Anatomic Mapping).
        zlib_buffer: Zlib buffer pre-allocation multiplier.
        bbox_threshold: Threshold for bounding box. If absent, no bbox will be\
            used.
        interp_method: Interpolation method. Choices: linear, nearest. Default is\
            linear.
        test: Write out test files to verify the equivalence of tm3d and gcam\
            morphs.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = export_gcam_params(
        fixed=fixed,
        moving=moving,
        morph=morph,
        out_gcam=out_gcam,
        zlib_buffer=zlib_buffer,
        bbox_threshold=bbox_threshold,
        interp_method=interp_method,
        test=test,
    )
    return export_gcam_execute(params, runner)

__all__ = [
    "ExportGcam",
    "ExportGcamOutputs",
    "EXPORT_GCAM_METADATA",
    "export_gcam_cargs",
    "export_gcam_outputs",
    "export_gcam_params",
    "export_gcam_execute",
    "export_gcam_validate",
    "export_gcam",
]