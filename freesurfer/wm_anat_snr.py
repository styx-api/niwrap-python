# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WM_ANAT_SNR_METADATA = Metadata(
    id="wm-anat-snr",
    name="wm-anat-snr",
    package="freesurfer",
)

WmAnatSnr = typing.TypedDict(
    "WmAnatSnr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/wm-anat-snr"]],
        "subject": str,
        "output_file": str,
        "force": typing.NotRequired[bool],
        "nerode": typing.NotRequired[int],
        "tmp_dir": typing.NotRequired[str],
        "cleanup": typing.NotRequired[bool],
        "no_cleanup": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class WmAnatSnrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_datafile: OutputPathType
    """Output data file containing SNR and associated metrics"""

def wm_anat_snr_params(
    subject: str,
    output_file: str,
    force: bool = False,
    nerode: int | None = None,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
) -> WmAnatSnr:
    """
    Build parameters.

    Args:
        subject: FreeSurfer subject name
        output_file: Output file
        force: Force analysis even if output is up-to-date
        nerode: Number of erosions for the WM mask
        tmp_dir: Temporary directory
        cleanup: Delete temporary directory after execution
        no_cleanup: Do not delete temporary directory after execution

    Returns:
        Parameter dictionary.
    """
    params: WmAnatSnr = {
        "@type": "freesurfer/wm-anat-snr",
        "subject": subject,
        "output_file": output_file,
        "force": force,
        "cleanup": cleanup,
        "no_cleanup": no_cleanup,
    }
    if nerode is not None:
        params["nerode"] = nerode
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def wm_anat_snr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid WmAnatSnr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nerode", None) is not None:
        if not isinstance(params["nerode"], int):
            raise StyxValidationError(f'`nerode` has the wrong type: Received `{type(params["nerode"])}` expected `int`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("cleanup", None) is not None:
        if not isinstance(params["cleanup"], bool):
            raise StyxValidationError(f'`cleanup` has the wrong type: Received `{type(params["cleanup"])}` expected `bool`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')

def wm_anat_snr_cargs(params: WmAnatSnr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wm-anat-snr")
    cargs.append("--s")
    cargs.append(params["subject"])
    cargs.append("--o")
    cargs.append(params["output_file"])
    if params.get("force"):
        cargs.append("--force")
    v_0 = params.get("nerode")
    if v_0 is not None:
        cargs.append("--nerode")
        cargs.append(str(v_0))
    v_1 = params.get("tmp_dir")
    if v_1 is not None:
        cargs.append("--tmp")
        cargs.append(v_1)
    if params.get("cleanup"):
        cargs.append("--cleanup")
    if params.get("no_cleanup"):
        cargs.append("--no-cleanup")
    return cargs

def wm_anat_snr_outputs(params: WmAnatSnr, execution: Execution) -> WmAnatSnrOutputs:
    """Build the WmAnatSnrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_datafile_v: OutputPathType = execution.output_file(params["output_file"])
    return WmAnatSnrOutputs(
        root=root_v,
        output_datafile=output_datafile_v,
    )

def wm_anat_snr_execute(params: WmAnatSnr, runner: Runner | None = None) -> WmAnatSnrOutputs:
    """
    wm-anat-snr

    Measures the anatomical SNR in white matter (WM) for quality assurance (QA). This is an experimental metric.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    wm_anat_snr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WM_ANAT_SNR_METADATA)
    execution.params(params)
    args = wm_anat_snr_cargs(params, execution)
    out = wm_anat_snr_outputs(params, execution)
    execution.run(args)
    return out

def wm_anat_snr(
    subject: str,
    output_file: str,
    force: bool = False,
    nerode: int | None = None,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
    runner: Runner | None = None,
) -> WmAnatSnrOutputs:
    """
    wm-anat-snr

    Measures the anatomical SNR in white matter (WM) for quality assurance (QA). This is an experimental metric.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: FreeSurfer subject name
        output_file: Output file
        force: Force analysis even if output is up-to-date
        nerode: Number of erosions for the WM mask
        tmp_dir: Temporary directory
        cleanup: Delete temporary directory after execution
        no_cleanup: Do not delete temporary directory after execution
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = wm_anat_snr_params(
        subject=subject,
        output_file=output_file,
        force=force,
        nerode=nerode,
        tmp_dir=tmp_dir,
        cleanup=cleanup,
        no_cleanup=no_cleanup,
    )
    return wm_anat_snr_execute(params, runner)

__all__ = [
    "WmAnatSnr",
    "WmAnatSnrOutputs",
    "WM_ANAT_SNR_METADATA",
    "wm_anat_snr_cargs",
    "wm_anat_snr_outputs",
    "wm_anat_snr_params",
    "wm_anat_snr_execute",
    "wm_anat_snr_validate",
    "wm_anat_snr",
]