# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCA_LONG_TP_INIT_METADATA = Metadata(
    id="rca-long-tp-init",
    name="rca-long-tp-init",
    package="freesurfer",
)

RcaLongTpInitHemisphere = typing.Literal["lh"] | typing.Literal["rh"]

RcaLongTpInit = typing.TypedDict(
    "RcaLongTpInit",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rca-long-tp-init"]],
        "timepoint": str,
        "base": str,
        "use_long_base_ctrl_vol": typing.NotRequired[bool],
        "hemisphere": typing.NotRequired[RcaLongTpInitHemisphere],
        "expert_opts": typing.NotRequired[InputPathType],
        "subject": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RcaLongTpInitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rca_long_tp_init_params(
    timepoint: str,
    base: str,
    use_long_base_ctrl_vol: bool = False,
    hemisphere: RcaLongTpInitHemisphere | None = None,
    expert_opts: InputPathType | None = None,
    subject: str | None = None,
) -> RcaLongTpInit:
    """
    Build parameters.

    Args:
        timepoint: Timepoint identifier
        base: Base identifier
        use_long_base_ctrl_vol: Use long base control volume
        hemisphere: Specify the hemisphere (left or right)
        expert_opts: Expert options file
        subject: Subject identifier for testing; put after -long

    Returns:
        Parameter dictionary.
    """
    params: RcaLongTpInit = {
        "@type": "freesurfer/rca-long-tp-init",
        "timepoint": timepoint,
        "base": base,
        "use_long_base_ctrl_vol": use_long_base_ctrl_vol,
    }
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    if expert_opts is not None:
        params["expert_opts"] = expert_opts
    if subject is not None:
        params["subject"] = subject
    return params

def rca_long_tp_init_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcaLongTpInit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("timepoint", None) is None:
        raise StyxValidationError("`timepoint` must not be None")
    if not isinstance(params["timepoint"], str):
        raise StyxValidationError(f'`timepoint` has the wrong type: Received `{type(params["timepoint"])}` expected `str`')
    if params.get("base", None) is None:
        raise StyxValidationError("`base` must not be None")
    if not isinstance(params["base"], str):
        raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `str`')
    if params.get("use_long_base_ctrl_vol", None) is not None:
        if not isinstance(params["use_long_base_ctrl_vol"], bool):
            raise StyxValidationError(f'`use_long_base_ctrl_vol` has the wrong type: Received `{type(params["use_long_base_ctrl_vol"])}` expected `bool`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `RcaLongTpInitHemisphere`')
        if params["hemisphere"] not in ["lh", "rh"]:
            raise StyxValidationError("Parameter `hemisphere` must be one of [\"lh\", \"rh\"]")
    if params.get("expert_opts", None) is not None:
        if not isinstance(params["expert_opts"], (pathlib.Path, str)):
            raise StyxValidationError(f'`expert_opts` has the wrong type: Received `{type(params["expert_opts"])}` expected `InputPathType`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')

def rca_long_tp_init_cargs(params: RcaLongTpInit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rca-long-tp-init")
    cargs.append("-long")
    cargs.append(params["timepoint"])
    cargs.append(params["base"])
    if params.get("use_long_base_ctrl_vol"):
        cargs.append("-uselongbasectrlvol")
    v_0 = params.get("hemisphere")
    if v_0 is not None:
        cargs.append("-hemi")
        cargs.append(str(v_0))
    v_1 = params.get("expert_opts")
    if v_1 is not None:
        cargs.append("-expert")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("subject")
    if v_2 is not None:
        cargs.append("-s")
        cargs.append(v_2)
    return cargs

def rca_long_tp_init_outputs(params: RcaLongTpInit, execution: Execution) -> RcaLongTpInitOutputs:
    """Build the RcaLongTpInitOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RcaLongTpInitOutputs(
        root=root_v,
    )

def rca_long_tp_init_execute(params: RcaLongTpInit, runner: Runner | None = None) -> RcaLongTpInitOutputs:
    """
    rca-long-tp-init

    Initialize long timepoint subject for recon-all processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rca_long_tp_init_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCA_LONG_TP_INIT_METADATA)
    execution.params(params)
    args = rca_long_tp_init_cargs(params, execution)
    out = rca_long_tp_init_outputs(params, execution)
    execution.run(args)
    return out

def rca_long_tp_init(
    timepoint: str,
    base: str,
    use_long_base_ctrl_vol: bool = False,
    hemisphere: RcaLongTpInitHemisphere | None = None,
    expert_opts: InputPathType | None = None,
    subject: str | None = None,
    runner: Runner | None = None,
) -> RcaLongTpInitOutputs:
    """
    rca-long-tp-init

    Initialize long timepoint subject for recon-all processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        timepoint: Timepoint identifier
        base: Base identifier
        use_long_base_ctrl_vol: Use long base control volume
        hemisphere: Specify the hemisphere (left or right)
        expert_opts: Expert options file
        subject: Subject identifier for testing; put after -long
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rca_long_tp_init_params(
        timepoint=timepoint,
        base=base,
        use_long_base_ctrl_vol=use_long_base_ctrl_vol,
        hemisphere=hemisphere,
        expert_opts=expert_opts,
        subject=subject,
    )
    return rca_long_tp_init_execute(params, runner)

__all__ = [
    "RcaLongTpInit",
    "RcaLongTpInitOutputs",
    "RCA_LONG_TP_INIT_METADATA",
    "rca_long_tp_init_cargs",
    "rca_long_tp_init_outputs",
    "rca_long_tp_init_params",
    "rca_long_tp_init_execute",
    "rca_long_tp_init_validate",
    "rca_long_tp_init",
]