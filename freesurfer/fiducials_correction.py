# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIDUCIALS_CORRECTION_METADATA = Metadata(
    id="fiducials_correction",
    name="fiducials_correction",
    package="freesurfer",
)

FiducialsCorrection = typing.TypedDict(
    "FiducialsCorrection",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fiducials_correction"]],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class FiducialsCorrectionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file with corrected fiducial markers."""

def fiducials_correction_params(
    input_file: InputPathType,
    output_file: str,
) -> FiducialsCorrection:
    """
    Build parameters.

    Args:
        input_file: Input file containing imaging data with fiducial markers to\
            correct.
        output_file: Output file with corrected fiducial markers.

    Returns:
        Parameter dictionary.
    """
    params: FiducialsCorrection = {
        "@type": "freesurfer/fiducials_correction",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def fiducials_correction_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FiducialsCorrection."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def fiducials_correction_cargs(params: FiducialsCorrection, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fiducials_correction")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def fiducials_correction_outputs(params: FiducialsCorrection, execution: Execution) -> FiducialsCorrectionOutputs:
    """Build the FiducialsCorrectionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return FiducialsCorrectionOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def fiducials_correction_execute(params: FiducialsCorrection, runner: Runner | None = None) -> FiducialsCorrectionOutputs:
    """
    fiducials_correction

    A tool for correcting fiducial markers in imaging data. Note: This command has dependency issues with the Qt platform plugin 'xcb'. Ensure dependencies are properly configured.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fiducials_correction_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIDUCIALS_CORRECTION_METADATA)
    execution.params(params)
    args = fiducials_correction_cargs(params, execution)
    out = fiducials_correction_outputs(params, execution)
    execution.run(args)
    return out

def fiducials_correction(
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> FiducialsCorrectionOutputs:
    """
    fiducials_correction

    A tool for correcting fiducial markers in imaging data. Note: This command has dependency issues with the Qt platform plugin 'xcb'. Ensure dependencies are properly configured.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input file containing imaging data with fiducial markers to\
            correct.
        output_file: Output file with corrected fiducial markers.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fiducials_correction_params(
        input_file=input_file,
        output_file=output_file,
    )
    return fiducials_correction_execute(params, runner)

__all__ = [
    "FiducialsCorrection",
    "FiducialsCorrectionOutputs",
    "FIDUCIALS_CORRECTION_METADATA",
    "fiducials_correction_cargs",
    "fiducials_correction_outputs",
    "fiducials_correction_params",
    "fiducials_correction_execute",
    "fiducials_correction_validate",
    "fiducials_correction",
]