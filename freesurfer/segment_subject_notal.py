# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_SUBJECT_NOTAL_METADATA = Metadata(
    id="segment_subject_notal",
    name="segment_subject_notal",
    package="freesurfer",
)

SegmentSubjectNotal = typing.TypedDict(
    "SegmentSubjectNotal",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segment_subject_notal"]],
        "subject_path": str,
    },
)

@dataclasses.dataclass
class SegmentSubjectNotalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def segment_subject_notal_params(
    subject_path: str,
) -> SegmentSubjectNotal:
    """
    Build parameters.

    Args:
        subject_path: Path to the subject's directory

    Returns:
        Parameter dictionary.
    """
    params: SegmentSubjectNotal = {
        "@type": "freesurfer/segment_subject_notal",
        "subject_path": subject_path,
    }
    return params

def segment_subject_notal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentSubjectNotal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_path", None) is None:
        raise StyxValidationError("`subject_path` must not be None")
    if not isinstance(params["subject_path"], str):
        raise StyxValidationError(f'`subject_path` has the wrong type: Received `{type(params["subject_path"])}` expected `str`')

def segment_subject_notal_cargs(params: SegmentSubjectNotal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segment_subject_notal")
    cargs.append(params["subject_path"])
    return cargs

def segment_subject_notal_outputs(params: SegmentSubjectNotal, execution: Execution) -> SegmentSubjectNotalOutputs:
    """Build the SegmentSubjectNotalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SegmentSubjectNotalOutputs(
        root=root_v,
    )

def segment_subject_notal_execute(params: SegmentSubjectNotal, runner: Runner | None = None) -> SegmentSubjectNotalOutputs:
    """
    segment_subject_notal

    A script to segment subjects (notal)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_subject_notal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_SUBJECT_NOTAL_METADATA)
    execution.params(params)
    args = segment_subject_notal_cargs(params, execution)
    out = segment_subject_notal_outputs(params, execution)
    execution.run(args)
    return out

def segment_subject_notal(
    subject_path: str,
    runner: Runner | None = None,
) -> SegmentSubjectNotalOutputs:
    """
    segment_subject_notal

    A script to segment subjects (notal)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_path: Path to the subject's directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_subject_notal_params(
        subject_path=subject_path,
    )
    return segment_subject_notal_execute(params, runner)

__all__ = [
    "SegmentSubjectNotal",
    "SegmentSubjectNotalOutputs",
    "SEGMENT_SUBJECT_NOTAL_METADATA",
    "segment_subject_notal_cargs",
    "segment_subject_notal_outputs",
    "segment_subject_notal_params",
    "segment_subject_notal_execute",
    "segment_subject_notal_validate",
    "segment_subject_notal",
]