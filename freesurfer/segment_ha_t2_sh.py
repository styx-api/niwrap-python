# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_HA_T2_SH_METADATA = Metadata(
    id="segmentHA_T2.sh",
    name="segmentHA_T2.sh",
    package="freesurfer",
)

SegmentHaT2Sh = typing.TypedDict(
    "SegmentHaT2Sh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segmentHA_T2.sh"]],
        "input_image": InputPathType,
        "output_directory": str,
    },
)

@dataclasses.dataclass
class SegmentHaT2ShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    left_hemisphere_labels: OutputPathType
    """Left hemisphere hippocampal and amygdala labels"""
    right_hemisphere_labels: OutputPathType
    """Right hemisphere hippocampal and amygdala labels"""

def segment_ha_t2_sh_params(
    input_image: InputPathType,
    output_directory: str,
) -> SegmentHaT2Sh:
    """
    Build parameters.

    Args:
        input_image: Input T2-weighted MRI image file
        output_directory: Output directory for segmented structures

    Returns:
        Parameter dictionary.
    """
    params: SegmentHaT2Sh = {
        "@type": "freesurfer/segmentHA_T2.sh",
        "input_image": input_image,
        "output_directory": output_directory,
    }
    return params

def segment_ha_t2_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentHaT2Sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def segment_ha_t2_sh_cargs(params: SegmentHaT2Sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segmentHA_T2.sh")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_directory"])
    return cargs

def segment_ha_t2_sh_outputs(params: SegmentHaT2Sh, execution: Execution) -> SegmentHaT2ShOutputs:
    """Build the SegmentHaT2ShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    left_hemisphere_labels_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/lh.hippoAmygLabels-T2.v21.mgz')
    right_hemisphere_labels_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/rh.hippoAmygLabels-T2.v21.mgz')
    return SegmentHaT2ShOutputs(
        root=root_v,
        left_hemisphere_labels=left_hemisphere_labels_v,
        right_hemisphere_labels=right_hemisphere_labels_v,
    )

def segment_ha_t2_sh_execute(params: SegmentHaT2Sh, runner: Runner | None = None) -> SegmentHaT2ShOutputs:
    """
    segmentHA_T2.sh

    Segments hippocampal and amygdala structures from T2-weighted MRI images using the FreeSurfer suite.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_ha_t2_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_HA_T2_SH_METADATA)
    execution.params(params)
    args = segment_ha_t2_sh_cargs(params, execution)
    out = segment_ha_t2_sh_outputs(params, execution)
    execution.run(args)
    return out

def segment_ha_t2_sh(
    input_image: InputPathType,
    output_directory: str,
    runner: Runner | None = None,
) -> SegmentHaT2ShOutputs:
    """
    segmentHA_T2.sh

    Segments hippocampal and amygdala structures from T2-weighted MRI images using the FreeSurfer suite.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input T2-weighted MRI image file
        output_directory: Output directory for segmented structures
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_ha_t2_sh_params(
        input_image=input_image,
        output_directory=output_directory,
    )
    return segment_ha_t2_sh_execute(params, runner)

__all__ = [
    "SegmentHaT2Sh",
    "SegmentHaT2ShOutputs",
    "SEGMENT_HA_T2_SH_METADATA",
    "segment_ha_t2_sh_cargs",
    "segment_ha_t2_sh_outputs",
    "segment_ha_t2_sh_params",
    "segment_ha_t2_sh_execute",
    "segment_ha_t2_sh_validate",
    "segment_ha_t2_sh",
]