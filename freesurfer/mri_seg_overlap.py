# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEG_OVERLAP_METADATA = Metadata(
    id="mri_seg_overlap",
    name="mri_seg_overlap",
    package="freesurfer",
)

MriSegOverlap = typing.TypedDict(
    "MriSegOverlap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_seg_overlap"]],
        "vol1": InputPathType,
        "vol2": InputPathType,
        "out_file": typing.NotRequired[str],
        "measures": typing.NotRequired[list[str]],
        "labels": typing.NotRequired[list[str]],
        "label_names": typing.NotRequired[list[str]],
        "label_file": typing.NotRequired[InputPathType],
        "no_names_flag": typing.NotRequired[bool],
        "seg_flag": typing.NotRequired[bool],
        "quiet_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSegOverlapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    overlap_report: OutputPathType | None
    """Detailed overlap report saved to a JSON file."""

def mri_seg_overlap_params(
    vol1: InputPathType,
    vol2: InputPathType,
    out_file: str | None = None,
    measures: list[str] | None = None,
    labels: list[str] | None = None,
    label_names: list[str] | None = None,
    label_file: InputPathType | None = None,
    no_names_flag: bool = False,
    seg_flag: bool = False,
    quiet_flag: bool = False,
) -> MriSegOverlap:
    """
    Build parameters.

    Args:
        vol1: First segmentation volume input
        vol2: Second segmentation volume input
        out_file: Save detailed overlap report to a JSON file.
        measures: List of measures to compute. Options include: dice, jaccard,\
            voldiff.
        labels: Space-separated list of label values to include.
        label_names: Custom label names corresponding to the values specified with\
            the --labels flag.
        label_file: Text file specifying the label values to include. Must be in\
            the format of a freesurfer lookup-table.
        no_names_flag: Do not report label names.
        seg_flag: Compute overlap between the major segmentation structures.
        quiet_flag: Quiet mode - do not print results to stdout.

    Returns:
        Parameter dictionary.
    """
    params: MriSegOverlap = {
        "@type": "freesurfer/mri_seg_overlap",
        "vol1": vol1,
        "vol2": vol2,
        "no_names_flag": no_names_flag,
        "seg_flag": seg_flag,
        "quiet_flag": quiet_flag,
    }
    if out_file is not None:
        params["out_file"] = out_file
    if measures is not None:
        params["measures"] = measures
    if labels is not None:
        params["labels"] = labels
    if label_names is not None:
        params["label_names"] = label_names
    if label_file is not None:
        params["label_file"] = label_file
    return params

def mri_seg_overlap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegOverlap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vol1", None) is None:
        raise StyxValidationError("`vol1` must not be None")
    if not isinstance(params["vol1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol1` has the wrong type: Received `{type(params["vol1"])}` expected `InputPathType`')
    if params.get("vol2", None) is None:
        raise StyxValidationError("`vol2` must not be None")
    if not isinstance(params["vol2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vol2` has the wrong type: Received `{type(params["vol2"])}` expected `InputPathType`')
    if params.get("out_file", None) is not None:
        if not isinstance(params["out_file"], str):
            raise StyxValidationError(f'`out_file` has the wrong type: Received `{type(params["out_file"])}` expected `str`')
    if params.get("measures", None) is not None:
        if not isinstance(params["measures"], list):
            raise StyxValidationError(f'`measures` has the wrong type: Received `{type(params["measures"])}` expected `list[str]`')
        for e in params["measures"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`measures` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("labels", None) is not None:
        if not isinstance(params["labels"], list):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
        for e_2 in params["labels"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("label_names", None) is not None:
        if not isinstance(params["label_names"], list):
            raise StyxValidationError(f'`label_names` has the wrong type: Received `{type(params["label_names"])}` expected `list[str]`')
        for e_3 in params["label_names"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`label_names` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("no_names_flag", None) is not None:
        if not isinstance(params["no_names_flag"], bool):
            raise StyxValidationError(f'`no_names_flag` has the wrong type: Received `{type(params["no_names_flag"])}` expected `bool`')
    if params.get("seg_flag", None) is not None:
        if not isinstance(params["seg_flag"], bool):
            raise StyxValidationError(f'`seg_flag` has the wrong type: Received `{type(params["seg_flag"])}` expected `bool`')
    if params.get("quiet_flag", None) is not None:
        if not isinstance(params["quiet_flag"], bool):
            raise StyxValidationError(f'`quiet_flag` has the wrong type: Received `{type(params["quiet_flag"])}` expected `bool`')

def mri_seg_overlap_cargs(params: MriSegOverlap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_seg_overlap")
    cargs.append(execution.input_file(params["vol1"]))
    cargs.append(execution.input_file(params["vol2"]))
    v_0 = params.get("out_file")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("measures")
    if v_1 is not None:
        cargs.append("-m")
        for item0 in v_1:
            cargs.append(item0)
    v_2 = params.get("labels")
    if v_2 is not None:
        cargs.append("-l")
        for item1 in v_2:
            cargs.append(item1)
    v_3 = params.get("label_names")
    if v_3 is not None:
        cargs.append("-n")
        for item2 in v_3:
            cargs.append(item2)
    v_4 = params.get("label_file")
    if v_4 is not None:
        cargs.append("-f")
        cargs.append(execution.input_file(v_4))
    if params.get("no_names_flag"):
        cargs.append("-x")
    if params.get("seg_flag"):
        cargs.append("-s")
    if params.get("quiet_flag"):
        cargs.append("-q")
    return cargs

def mri_seg_overlap_outputs(params: MriSegOverlap, execution: Execution) -> MriSegOverlapOutputs:
    """Build the MriSegOverlapOutputs object for this tool."""
    overlap_report_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_file")
    if __v0 is not None:
        overlap_report_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriSegOverlapOutputs(
        root=root_v,
        overlap_report=overlap_report_v,
    )

def mri_seg_overlap_execute(params: MriSegOverlap, runner: Runner | None = None) -> MriSegOverlapOutputs:
    """
    mri_seg_overlap

    Compute the structural overlap between two segmentation volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_seg_overlap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEG_OVERLAP_METADATA)
    execution.params(params)
    args = mri_seg_overlap_cargs(params, execution)
    out = mri_seg_overlap_outputs(params, execution)
    execution.run(args)
    return out

def mri_seg_overlap(
    vol1: InputPathType,
    vol2: InputPathType,
    out_file: str | None = None,
    measures: list[str] | None = None,
    labels: list[str] | None = None,
    label_names: list[str] | None = None,
    label_file: InputPathType | None = None,
    no_names_flag: bool = False,
    seg_flag: bool = False,
    quiet_flag: bool = False,
    runner: Runner | None = None,
) -> MriSegOverlapOutputs:
    """
    mri_seg_overlap

    Compute the structural overlap between two segmentation volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        vol1: First segmentation volume input
        vol2: Second segmentation volume input
        out_file: Save detailed overlap report to a JSON file.
        measures: List of measures to compute. Options include: dice, jaccard,\
            voldiff.
        labels: Space-separated list of label values to include.
        label_names: Custom label names corresponding to the values specified with\
            the --labels flag.
        label_file: Text file specifying the label values to include. Must be in\
            the format of a freesurfer lookup-table.
        no_names_flag: Do not report label names.
        seg_flag: Compute overlap between the major segmentation structures.
        quiet_flag: Quiet mode - do not print results to stdout.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_seg_overlap_params(
        vol1=vol1,
        vol2=vol2,
        out_file=out_file,
        measures=measures,
        labels=labels,
        label_names=label_names,
        label_file=label_file,
        no_names_flag=no_names_flag,
        seg_flag=seg_flag,
        quiet_flag=quiet_flag,
    )
    return mri_seg_overlap_execute(params, runner)

__all__ = [
    "MriSegOverlap",
    "MriSegOverlapOutputs",
    "MRI_SEG_OVERLAP_METADATA",
    "mri_seg_overlap_cargs",
    "mri_seg_overlap_outputs",
    "mri_seg_overlap_params",
    "mri_seg_overlap_execute",
    "mri_seg_overlap_validate",
    "mri_seg_overlap",
]