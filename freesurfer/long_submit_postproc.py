# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_SUBMIT_POSTPROC_METADATA = Metadata(
    id="long_submit_postproc",
    name="long_submit_postproc",
    package="freesurfer",
)

LongSubmitPostproc = typing.TypedDict(
    "LongSubmitPostproc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_submit_postproc"]],
        "qdec": InputPathType,
        "prog": str,
        "flags": typing.NotRequired[str],
        "dir": typing.NotRequired[str],
        "simulate": typing.NotRequired[bool],
        "pause": typing.NotRequired[float],
        "max": typing.NotRequired[float],
        "queue": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LongSubmitPostprocOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def long_submit_postproc_params(
    qdec: InputPathType,
    prog: str,
    flags: str | None = None,
    dir: str | None = None,
    simulate: bool = False,
    pause: float | None = None,
    max: float | None = None,
    queue: str | None = None,
) -> LongSubmitPostproc:
    """
    Build parameters.

    Args:
        qdec: QDEC table file specifying the subjects and time points
        prog: Longitudinal script to call
        flags: Parameters (without --qdec) to pass to prog (using quotes ...)
        dir: Directory to store sub-tables and command files
        simulate: Do not submit anything, just print commands
        pause: Pause in seconds between submissions
        max: Maximum number of jobs for this user
        queue: Special queue to submit

    Returns:
        Parameter dictionary.
    """
    params: LongSubmitPostproc = {
        "@type": "freesurfer/long_submit_postproc",
        "qdec": qdec,
        "prog": prog,
        "simulate": simulate,
    }
    if flags is not None:
        params["flags"] = flags
    if dir is not None:
        params["dir"] = dir
    if pause is not None:
        params["pause"] = pause
    if max is not None:
        params["max"] = max
    if queue is not None:
        params["queue"] = queue
    return params

def long_submit_postproc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongSubmitPostproc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec", None) is None:
        raise StyxValidationError("`qdec` must not be None")
    if not isinstance(params["qdec"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec` has the wrong type: Received `{type(params["qdec"])}` expected `InputPathType`')
    if params.get("prog", None) is None:
        raise StyxValidationError("`prog` must not be None")
    if not isinstance(params["prog"], str):
        raise StyxValidationError(f'`prog` has the wrong type: Received `{type(params["prog"])}` expected `str`')
    if params.get("flags", None) is not None:
        if not isinstance(params["flags"], str):
            raise StyxValidationError(f'`flags` has the wrong type: Received `{type(params["flags"])}` expected `str`')
    if params.get("dir", None) is not None:
        if not isinstance(params["dir"], str):
            raise StyxValidationError(f'`dir` has the wrong type: Received `{type(params["dir"])}` expected `str`')
    if params.get("simulate", None) is not None:
        if not isinstance(params["simulate"], bool):
            raise StyxValidationError(f'`simulate` has the wrong type: Received `{type(params["simulate"])}` expected `bool`')
    if params.get("pause", None) is not None:
        if not isinstance(params["pause"], (float, int)):
            raise StyxValidationError(f'`pause` has the wrong type: Received `{type(params["pause"])}` expected `float`')
    if params.get("max", None) is not None:
        if not isinstance(params["max"], (float, int)):
            raise StyxValidationError(f'`max` has the wrong type: Received `{type(params["max"])}` expected `float`')
    if params.get("queue", None) is not None:
        if not isinstance(params["queue"], str):
            raise StyxValidationError(f'`queue` has the wrong type: Received `{type(params["queue"])}` expected `str`')

def long_submit_postproc_cargs(params: LongSubmitPostproc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_submit_postproc")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec"]))
    cargs.append("--prog")
    cargs.append(params["prog"])
    v_0 = params.get("flags")
    if v_0 is not None:
        cargs.append("--flags")
        cargs.append(v_0)
    v_1 = params.get("dir")
    if v_1 is not None:
        cargs.append("--dir")
        cargs.append(v_1)
    if params.get("simulate"):
        cargs.append("--simulate")
    v_2 = params.get("pause")
    if v_2 is not None:
        cargs.append("--pause")
        cargs.append(str(v_2))
    v_3 = params.get("max")
    if v_3 is not None:
        cargs.append("--max")
        cargs.append(str(v_3))
    v_4 = params.get("queue")
    if v_4 is not None:
        cargs.append("--queue")
        cargs.append(v_4)
    return cargs

def long_submit_postproc_outputs(params: LongSubmitPostproc, execution: Execution) -> LongSubmitPostprocOutputs:
    """Build the LongSubmitPostprocOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LongSubmitPostprocOutputs(
        root=root_v,
    )

def long_submit_postproc_execute(params: LongSubmitPostproc, runner: Runner | None = None) -> LongSubmitPostprocOutputs:
    """
    long_submit_postproc

    Submits jobs to the cluster (either seychelles or launchpad at NMR) for longitudinal post-processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_submit_postproc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_SUBMIT_POSTPROC_METADATA)
    execution.params(params)
    args = long_submit_postproc_cargs(params, execution)
    out = long_submit_postproc_outputs(params, execution)
    execution.run(args)
    return out

def long_submit_postproc(
    qdec: InputPathType,
    prog: str,
    flags: str | None = None,
    dir: str | None = None,
    simulate: bool = False,
    pause: float | None = None,
    max: float | None = None,
    queue: str | None = None,
    runner: Runner | None = None,
) -> LongSubmitPostprocOutputs:
    """
    long_submit_postproc

    Submits jobs to the cluster (either seychelles or launchpad at NMR) for longitudinal post-processing.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec: QDEC table file specifying the subjects and time points
        prog: Longitudinal script to call
        flags: Parameters (without --qdec) to pass to prog (using quotes ...)
        dir: Directory to store sub-tables and command files
        simulate: Do not submit anything, just print commands
        pause: Pause in seconds between submissions
        max: Maximum number of jobs for this user
        queue: Special queue to submit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_submit_postproc_params(
        qdec=qdec,
        prog=prog,
        flags=flags,
        dir=dir,
        simulate=simulate,
        pause=pause,
        max=max,
        queue=queue,
    )
    return long_submit_postproc_execute(params, runner)

__all__ = [
    "LongSubmitPostproc",
    "LongSubmitPostprocOutputs",
    "LONG_SUBMIT_POSTPROC_METADATA",
    "long_submit_postproc_cargs",
    "long_submit_postproc_outputs",
    "long_submit_postproc_params",
    "long_submit_postproc_execute",
    "long_submit_postproc_validate",
    "long_submit_postproc",
]