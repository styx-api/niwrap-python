# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_OR_METADATA = Metadata(
    id="mri_or",
    name="mri_or",
    package="freesurfer",
)

MriOr = typing.TypedDict(
    "MriOr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_or"]],
        "original_labels": typing.NotRequired[bool],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class MriOrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_or_params(
    input_files: list[InputPathType],
    original_labels: bool = False,
) -> MriOr:
    """
    Build parameters.

    Args:
        input_files: Input image files on which to perform the logical OR operation
        original_labels: Keeps the original label values in the input files when\
            creating the output

    Returns:
        Parameter dictionary.
    """
    params: MriOr = {
        "@type": "freesurfer/mri_or",
        "input_files": input_files,
        "original_labels": original_labels,
    }
    return params

def mri_or_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriOr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("original_labels", None) is not None:
        if not isinstance(params["original_labels"], bool):
            raise StyxValidationError(f'`original_labels` has the wrong type: Received `{type(params["original_labels"])}` expected `bool`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def mri_or_cargs(params: MriOr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_or")
    if params.get("original_labels"):
        cargs.append("-o")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def mri_or_outputs(params: MriOr, execution: Execution) -> MriOrOutputs:
    """Build the MriOrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriOrOutputs(
        root=root_v,
    )

def mri_or_execute(params: MriOr, runner: Runner | None = None) -> MriOrOutputs:
    """
    mri_or

    Performs a logical voxel-wise OR on a series of volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_or_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_OR_METADATA)
    execution.params(params)
    args = mri_or_cargs(params, execution)
    out = mri_or_outputs(params, execution)
    execution.run(args)
    return out

def mri_or(
    input_files: list[InputPathType],
    original_labels: bool = False,
    runner: Runner | None = None,
) -> MriOrOutputs:
    """
    mri_or

    Performs a logical voxel-wise OR on a series of volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input image files on which to perform the logical OR operation
        original_labels: Keeps the original label values in the input files when\
            creating the output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_or_params(
        input_files=input_files,
        original_labels=original_labels,
    )
    return mri_or_execute(params, runner)

__all__ = [
    "MriOr",
    "MriOrOutputs",
    "MRI_OR_METADATA",
    "mri_or_cargs",
    "mri_or_outputs",
    "mri_or_params",
    "mri_or_execute",
    "mri_or_validate",
    "mri_or",
]