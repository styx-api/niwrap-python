# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_CA_DEFORM_METADATA = Metadata(
    id="mris_ca_deform",
    name="mris_ca_deform",
    package="freesurfer",
)

MrisCaDeform = typing.TypedDict(
    "MrisCaDeform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_ca_deform"]],
        "input_surface": InputPathType,
        "label_vol": InputPathType,
        "transform": InputPathType,
        "intensity_vol": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class MrisCaDeformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    deformed_surface: OutputPathType
    """The result of the deformation process, output surface file."""

def mris_ca_deform_params(
    input_surface: InputPathType,
    label_vol: InputPathType,
    transform: InputPathType,
    intensity_vol: InputPathType,
    output_surface: str,
) -> MrisCaDeform:
    """
    Build parameters.

    Args:
        input_surface: The input surface file to be deformed.
        label_vol: The input volumetric label map.
        transform: The transform file, typically a matrix that aligns the volumes.
        intensity_vol: The intensity volume that is used in the deformation\
            process.
        output_surface: The file name for the output, deformed surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisCaDeform = {
        "@type": "freesurfer/mris_ca_deform",
        "input_surface": input_surface,
        "label_vol": label_vol,
        "transform": transform,
        "intensity_vol": intensity_vol,
        "output_surface": output_surface,
    }
    return params

def mris_ca_deform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCaDeform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("label_vol", None) is None:
        raise StyxValidationError("`label_vol` must not be None")
    if not isinstance(params["label_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_vol` has the wrong type: Received `{type(params["label_vol"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("intensity_vol", None) is None:
        raise StyxValidationError("`intensity_vol` must not be None")
    if not isinstance(params["intensity_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`intensity_vol` has the wrong type: Received `{type(params["intensity_vol"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def mris_ca_deform_cargs(params: MrisCaDeform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_ca_deform")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["label_vol"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(execution.input_file(params["intensity_vol"]))
    cargs.append(params["output_surface"])
    return cargs

def mris_ca_deform_outputs(params: MrisCaDeform, execution: Execution) -> MrisCaDeformOutputs:
    """Build the MrisCaDeformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    deformed_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisCaDeformOutputs(
        root=root_v,
        deformed_surface=deformed_surface_v,
    )

def mris_ca_deform_execute(params: MrisCaDeform, runner: Runner | None = None) -> MrisCaDeformOutputs:
    """
    mris_ca_deform

    Deforms a surface to match it to a volumetric map of cortical labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_ca_deform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_CA_DEFORM_METADATA)
    execution.params(params)
    args = mris_ca_deform_cargs(params, execution)
    out = mris_ca_deform_outputs(params, execution)
    execution.run(args)
    return out

def mris_ca_deform(
    input_surface: InputPathType,
    label_vol: InputPathType,
    transform: InputPathType,
    intensity_vol: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> MrisCaDeformOutputs:
    """
    mris_ca_deform

    Deforms a surface to match it to a volumetric map of cortical labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: The input surface file to be deformed.
        label_vol: The input volumetric label map.
        transform: The transform file, typically a matrix that aligns the volumes.
        intensity_vol: The intensity volume that is used in the deformation\
            process.
        output_surface: The file name for the output, deformed surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_ca_deform_params(
        input_surface=input_surface,
        label_vol=label_vol,
        transform=transform,
        intensity_vol=intensity_vol,
        output_surface=output_surface,
    )
    return mris_ca_deform_execute(params, runner)

__all__ = [
    "MrisCaDeform",
    "MrisCaDeformOutputs",
    "MRIS_CA_DEFORM_METADATA",
    "mris_ca_deform_cargs",
    "mris_ca_deform_outputs",
    "mris_ca_deform_params",
    "mris_ca_deform_execute",
    "mris_ca_deform_validate",
    "mris_ca_deform",
]