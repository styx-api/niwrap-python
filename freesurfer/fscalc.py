# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSCALC_METADATA = Metadata(
    id="fscalc",
    name="fscalc",
    package="freesurfer",
)

Fscalc = typing.TypedDict(
    "Fscalc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fscalc"]],
        "input1": str,
        "operation": str,
        "input2": typing.NotRequired[str],
        "output_file": str,
        "output_data_type": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "tmpdir": typing.NotRequired[str],
        "nocleanup": typing.NotRequired[bool],
        "log_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FscalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_vol: OutputPathType
    """Resulting output volume from the operations specified."""

def fscalc_params(
    input1: str,
    operation: str,
    output_file: str,
    input2: str | None = None,
    output_data_type: str | None = None,
    debug: bool = False,
    tmpdir: str | None = None,
    nocleanup: bool = False,
    log_file: str | None = None,
) -> Fscalc:
    """
    Build parameters.

    Args:
        input1: First input image or constant.
        operation: Operation to perform between input volumes.
        output_file: Output volume file.
        input2: Second input image or constant. Optional for some unary operations.
        output_data_type: Specify output data type (uchar, short, int, float).
        debug: Enable debug mode.
        tmpdir: Temporary directory for processing.
        nocleanup: Prevent cleanup of temporary files.
        log_file: Specify a log file for operation log.

    Returns:
        Parameter dictionary.
    """
    params: Fscalc = {
        "@type": "freesurfer/fscalc",
        "input1": input1,
        "operation": operation,
        "output_file": output_file,
        "debug": debug,
        "nocleanup": nocleanup,
    }
    if input2 is not None:
        params["input2"] = input2
    if output_data_type is not None:
        params["output_data_type"] = output_data_type
    if tmpdir is not None:
        params["tmpdir"] = tmpdir
    if log_file is not None:
        params["log_file"] = log_file
    return params

def fscalc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fscalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1", None) is None:
        raise StyxValidationError("`input1` must not be None")
    if not isinstance(params["input1"], str):
        raise StyxValidationError(f'`input1` has the wrong type: Received `{type(params["input1"])}` expected `str`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("input2", None) is not None:
        if not isinstance(params["input2"], str):
            raise StyxValidationError(f'`input2` has the wrong type: Received `{type(params["input2"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("output_data_type", None) is not None:
        if not isinstance(params["output_data_type"], str):
            raise StyxValidationError(f'`output_data_type` has the wrong type: Received `{type(params["output_data_type"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("tmpdir", None) is not None:
        if not isinstance(params["tmpdir"], str):
            raise StyxValidationError(f'`tmpdir` has the wrong type: Received `{type(params["tmpdir"])}` expected `str`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')

def fscalc_cargs(params: Fscalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fscalc")
    cargs.append(params["input1"])
    cargs.append(params["operation"])
    v_0 = params.get("input2")
    if v_0 is not None:
        cargs.append(v_0)
    cargs.append("--o")
    cargs.append(params["output_file"])
    v_1 = params.get("output_data_type")
    if v_1 is not None:
        cargs.append("--odt")
        cargs.append(v_1)
    if params.get("debug"):
        cargs.append("--debug")
    v_2 = params.get("tmpdir")
    if v_2 is not None:
        cargs.append("--tmpdir")
        cargs.append(v_2)
    if params.get("nocleanup"):
        cargs.append("--nocleanup")
    v_3 = params.get("log_file")
    if v_3 is not None:
        cargs.append("--log")
        cargs.append(v_3)
    return cargs

def fscalc_outputs(params: Fscalc, execution: Execution) -> FscalcOutputs:
    """Build the FscalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_vol_v: OutputPathType = execution.output_file(params["output_file"])
    return FscalcOutputs(
        root=root_v,
        result_vol=result_vol_v,
    )

def fscalc_execute(params: Fscalc, runner: Runner | None = None) -> FscalcOutputs:
    """
    fscalc

    A frontend tool to perform mathematical operations on volumes/surfaces of data using mris_calc.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fscalc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSCALC_METADATA)
    execution.params(params)
    args = fscalc_cargs(params, execution)
    out = fscalc_outputs(params, execution)
    execution.run(args)
    return out

def fscalc(
    input1: str,
    operation: str,
    output_file: str,
    input2: str | None = None,
    output_data_type: str | None = None,
    debug: bool = False,
    tmpdir: str | None = None,
    nocleanup: bool = False,
    log_file: str | None = None,
    runner: Runner | None = None,
) -> FscalcOutputs:
    """
    fscalc

    A frontend tool to perform mathematical operations on volumes/surfaces of data using mris_calc.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input1: First input image or constant.
        operation: Operation to perform between input volumes.
        output_file: Output volume file.
        input2: Second input image or constant. Optional for some unary operations.
        output_data_type: Specify output data type (uchar, short, int, float).
        debug: Enable debug mode.
        tmpdir: Temporary directory for processing.
        nocleanup: Prevent cleanup of temporary files.
        log_file: Specify a log file for operation log.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fscalc_params(
        input1=input1,
        operation=operation,
        output_file=output_file,
        input2=input2,
        output_data_type=output_data_type,
        debug=debug,
        tmpdir=tmpdir,
        nocleanup=nocleanup,
        log_file=log_file,
    )
    return fscalc_execute(params, runner)

__all__ = [
    "Fscalc",
    "FscalcOutputs",
    "FSCALC_METADATA",
    "fscalc_cargs",
    "fscalc_outputs",
    "fscalc_params",
    "fscalc_execute",
    "fscalc_validate",
    "fscalc",
]