# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REGISTER_CHILD_METADATA = Metadata(
    id="register_child",
    name="register_child",
    package="freesurfer",
)

RegisterChild = typing.TypedDict(
    "RegisterChild",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/register_child"]],
        "input_volume": InputPathType,
        "output_directory": str,
    },
)

@dataclasses.dataclass
class RegisterChildOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_control_points: OutputPathType
    """File where transformed control points are written."""
    intensity_normalized_volume: OutputPathType
    """File where intensity normalized volume is written."""

def register_child_params(
    input_volume: InputPathType,
    output_directory: str,
) -> RegisterChild:
    """
    Build parameters.

    Args:
        input_volume: Input MR volume to be used for registration.
        output_directory: Directory where output files will be written.

    Returns:
        Parameter dictionary.
    """
    params: RegisterChild = {
        "@type": "freesurfer/register_child",
        "input_volume": input_volume,
        "output_directory": output_directory,
    }
    return params

def register_child_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegisterChild."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def register_child_cargs(params: RegisterChild, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("register_child")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_directory"])
    return cargs

def register_child_outputs(params: RegisterChild, execution: Execution) -> RegisterChildOutputs:
    """Build the RegisterChildOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_control_points_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/fsamples')
    intensity_normalized_volume_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/norm')
    return RegisterChildOutputs(
        root=root_v,
        transformed_control_points=transformed_control_points_v,
        intensity_normalized_volume=intensity_normalized_volume_v,
    )

def register_child_execute(params: RegisterChild, runner: Runner | None = None) -> RegisterChildOutputs:
    """
    register_child

    A tool used for registering MR volumes with a child's atlas in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    register_child_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REGISTER_CHILD_METADATA)
    execution.params(params)
    args = register_child_cargs(params, execution)
    out = register_child_outputs(params, execution)
    execution.run(args)
    return out

def register_child(
    input_volume: InputPathType,
    output_directory: str,
    runner: Runner | None = None,
) -> RegisterChildOutputs:
    """
    register_child

    A tool used for registering MR volumes with a child's atlas in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input MR volume to be used for registration.
        output_directory: Directory where output files will be written.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = register_child_params(
        input_volume=input_volume,
        output_directory=output_directory,
    )
    return register_child_execute(params, runner)

__all__ = [
    "RegisterChild",
    "RegisterChildOutputs",
    "REGISTER_CHILD_METADATA",
    "register_child_cargs",
    "register_child_outputs",
    "register_child_params",
    "register_child_execute",
    "register_child_validate",
    "register_child",
]