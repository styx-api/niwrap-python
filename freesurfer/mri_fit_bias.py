# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FIT_BIAS_METADATA = Metadata(
    id="mri_fit_bias",
    name="mri_fit_bias",
    package="freesurfer",
)

MriFitBias = typing.TypedDict(
    "MriFitBias",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_fit_bias"]],
        "inputvol": InputPathType,
        "lpf_cutoff": typing.NotRequired[float],
        "segvol": InputPathType,
        "maskvol": InputPathType,
        "outvol": str,
        "biasfield": str,
        "dctvol": typing.NotRequired[str],
        "threshold": typing.NotRequired[float],
        "nerode": typing.NotRequired[float],
        "nthreads": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriFitBiasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_output: OutputPathType
    """Bias corrected output volume"""
    generated_bias_field: OutputPathType
    """Generated bias field"""

def mri_fit_bias_params(
    inputvol: InputPathType,
    segvol: InputPathType,
    maskvol: InputPathType,
    outvol: str,
    biasfield_2: str,
    lpf_cutoff: float | None = None,
    dctvol: str | None = None,
    threshold: float | None = None,
    nerode: float | None = None,
    nthreads: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
) -> MriFitBias:
    """
    Build parameters.

    Args:
        inputvol: Input volume for intensity normalization
        segvol: Segmentation volume to define WM and Cortex (e.g.,\
            aseg.presurf.mgz)
        maskvol: Mask volume; zero everything outside of the mask (e.g.,\
            brainmask.mgz)
        outvol: Bias corrected output volume
        biasfield_2: Output bias field
        lpf_cutoff: Low-pass filter cutoff in mm (default is 23.000000)
        dctvol: DCT fields file for debugging
        threshold: Mask out anything <= threshold value
        nerode: 3D erode segmentation by n steps (default is 1)
        nthreads: Number of threads to use
        debug: Turn on debugging mode
        checkopts: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: MriFitBias = {
        "@type": "freesurfer/mri_fit_bias",
        "inputvol": inputvol,
        "segvol": segvol,
        "maskvol": maskvol,
        "outvol": outvol,
        "biasfield": biasfield_2,
        "debug": debug,
        "checkopts": checkopts,
    }
    if lpf_cutoff is not None:
        params["lpf_cutoff"] = lpf_cutoff
    if dctvol is not None:
        params["dctvol"] = dctvol
    if threshold is not None:
        params["threshold"] = threshold
    if nerode is not None:
        params["nerode"] = nerode
    if nthreads is not None:
        params["nthreads"] = nthreads
    return params

def mri_fit_bias_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFitBias."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputvol", None) is None:
        raise StyxValidationError("`inputvol` must not be None")
    if not isinstance(params["inputvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputvol` has the wrong type: Received `{type(params["inputvol"])}` expected `InputPathType`')
    if params.get("lpf_cutoff", None) is not None:
        if not isinstance(params["lpf_cutoff"], (float, int)):
            raise StyxValidationError(f'`lpf_cutoff` has the wrong type: Received `{type(params["lpf_cutoff"])}` expected `float`')
    if params.get("segvol", None) is None:
        raise StyxValidationError("`segvol` must not be None")
    if not isinstance(params["segvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segvol` has the wrong type: Received `{type(params["segvol"])}` expected `InputPathType`')
    if params.get("maskvol", None) is None:
        raise StyxValidationError("`maskvol` must not be None")
    if not isinstance(params["maskvol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`maskvol` has the wrong type: Received `{type(params["maskvol"])}` expected `InputPathType`')
    if params.get("outvol", None) is None:
        raise StyxValidationError("`outvol` must not be None")
    if not isinstance(params["outvol"], str):
        raise StyxValidationError(f'`outvol` has the wrong type: Received `{type(params["outvol"])}` expected `str`')
    if params.get("biasfield", None) is None:
        raise StyxValidationError("`biasfield` must not be None")
    if not isinstance(params["biasfield"], str):
        raise StyxValidationError(f'`biasfield` has the wrong type: Received `{type(params["biasfield"])}` expected `str`')
    if params.get("dctvol", None) is not None:
        if not isinstance(params["dctvol"], str):
            raise StyxValidationError(f'`dctvol` has the wrong type: Received `{type(params["dctvol"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("nerode", None) is not None:
        if not isinstance(params["nerode"], (float, int)):
            raise StyxValidationError(f'`nerode` has the wrong type: Received `{type(params["nerode"])}` expected `float`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], (float, int)):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')

def mri_fit_bias_cargs(params: MriFitBias, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_fit_bias")
    cargs.append("--i")
    cargs.append(execution.input_file(params["inputvol"]))
    v_0 = params.get("lpf_cutoff")
    if v_0 is not None:
        cargs.append("--cutoff")
        cargs.append(str(v_0))
    cargs.append("--seg")
    cargs.append(execution.input_file(params["segvol"]))
    cargs.append("--mask")
    cargs.append(execution.input_file(params["maskvol"]))
    cargs.append("--o")
    cargs.append(params["outvol"])
    cargs.append("--bias")
    cargs.append(params["biasfield"])
    v_1 = params.get("dctvol")
    if v_1 is not None:
        cargs.append("--dct")
        cargs.append(v_1)
    v_2 = params.get("threshold")
    if v_2 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_2))
    v_3 = params.get("nerode")
    if v_3 is not None:
        cargs.append("--erode")
        cargs.append(str(v_3))
    v_4 = params.get("nthreads")
    if v_4 is not None:
        cargs.append("--threads")
        cargs.append(str(v_4))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    return cargs

def mri_fit_bias_outputs(params: MriFitBias, execution: Execution) -> MriFitBiasOutputs:
    """Build the MriFitBiasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_output_v: OutputPathType = execution.output_file(params["outvol"])
    generated_bias_field_v: OutputPathType = execution.output_file(params["biasfield"])
    return MriFitBiasOutputs(
        root=root_v,
        corrected_output=corrected_output_v,
        generated_bias_field=generated_bias_field_v,
    )

def mri_fit_bias_execute(params: MriFitBias, runner: Runner | None = None) -> MriFitBiasOutputs:
    """
    mri_fit_bias

    A tool for intensity normalization and bias correction in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_fit_bias_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FIT_BIAS_METADATA)
    execution.params(params)
    args = mri_fit_bias_cargs(params, execution)
    out = mri_fit_bias_outputs(params, execution)
    execution.run(args)
    return out

def mri_fit_bias(
    inputvol: InputPathType,
    segvol: InputPathType,
    maskvol: InputPathType,
    outvol: str,
    biasfield_2: str,
    lpf_cutoff: float | None = None,
    dctvol: str | None = None,
    threshold: float | None = None,
    nerode: float | None = None,
    nthreads: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    runner: Runner | None = None,
) -> MriFitBiasOutputs:
    """
    mri_fit_bias

    A tool for intensity normalization and bias correction in MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inputvol: Input volume for intensity normalization
        segvol: Segmentation volume to define WM and Cortex (e.g.,\
            aseg.presurf.mgz)
        maskvol: Mask volume; zero everything outside of the mask (e.g.,\
            brainmask.mgz)
        outvol: Bias corrected output volume
        biasfield_2: Output bias field
        lpf_cutoff: Low-pass filter cutoff in mm (default is 23.000000)
        dctvol: DCT fields file for debugging
        threshold: Mask out anything <= threshold value
        nerode: 3D erode segmentation by n steps (default is 1)
        nthreads: Number of threads to use
        debug: Turn on debugging mode
        checkopts: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_fit_bias_params(
        inputvol=inputvol,
        segvol=segvol,
        maskvol=maskvol,
        outvol=outvol,
        biasfield_2=biasfield_2,
        lpf_cutoff=lpf_cutoff,
        dctvol=dctvol,
        threshold=threshold,
        nerode=nerode,
        nthreads=nthreads,
        debug=debug,
        checkopts=checkopts,
    )
    return mri_fit_bias_execute(params, runner)

__all__ = [
    "MriFitBias",
    "MriFitBiasOutputs",
    "MRI_FIT_BIAS_METADATA",
    "mri_fit_bias_cargs",
    "mri_fit_bias_outputs",
    "mri_fit_bias_params",
    "mri_fit_bias_execute",
    "mri_fit_bias_validate",
    "mri_fit_bias",
]