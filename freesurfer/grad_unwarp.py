# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GRAD_UNWARP_METADATA = Metadata(
    id="grad_unwarp",
    name="grad_unwarp",
    package="freesurfer",
)

GradUnwarp = typing.TypedDict(
    "GradUnwarp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/grad_unwarp"]],
        "infile": InputPathType,
        "seriesno": typing.NotRequired[str],
        "unwarp_type": typing.NotRequired[str],
        "nojac": typing.NotRequired[bool],
        "corfov": typing.NotRequired[bool],
        "cor": typing.NotRequired[bool],
        "interp": typing.NotRequired[str],
        "outfile": str,
        "matlab_binary": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class GradUnwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mgh_output: OutputPathType
    """Output file in MGH format"""
    cor_output: OutputPathType
    """Output directory in COR format"""

def grad_unwarp_params(
    infile: InputPathType,
    outfile: str,
    seriesno: str | None = None,
    unwarp_type: str | None = None,
    nojac: bool = False,
    corfov: bool = False,
    cor: bool = False,
    interp: str | None = None,
    matlab_binary: str | None = None,
) -> GradUnwarp:
    """
    Build parameters.

    Args:
        infile: Input file or directory (dcmfile, dcmdir, or mghfile)
        outfile: Output file in MGH format
        seriesno: DICOM series number, required if input is a directory
        unwarp_type: Gradient unwarping displacement type or map file (required for\
            MGH file)
        nojac: Do not perform jacobian correction when unwarping
        corfov: Resample to Coronal FOV
        cor: Output in COR format instead of MGH
        interp: Interpolation method (cubic, linear, nearest, spline)
        matlab_binary: Path to the Matlab binary, version 6.5 or higher required

    Returns:
        Parameter dictionary.
    """
    params: GradUnwarp = {
        "@type": "freesurfer/grad_unwarp",
        "infile": infile,
        "outfile": outfile,
        "nojac": nojac,
        "corfov": corfov,
        "cor": cor,
    }
    if seriesno is not None:
        params["seriesno"] = seriesno
    if unwarp_type is not None:
        params["unwarp_type"] = unwarp_type
    if interp is not None:
        params["interp"] = interp
    if matlab_binary is not None:
        params["matlab_binary"] = matlab_binary
    return params

def grad_unwarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GradUnwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("seriesno", None) is not None:
        if not isinstance(params["seriesno"], str):
            raise StyxValidationError(f'`seriesno` has the wrong type: Received `{type(params["seriesno"])}` expected `str`')
    if params.get("unwarp_type", None) is not None:
        if not isinstance(params["unwarp_type"], str):
            raise StyxValidationError(f'`unwarp_type` has the wrong type: Received `{type(params["unwarp_type"])}` expected `str`')
    if params.get("nojac", None) is not None:
        if not isinstance(params["nojac"], bool):
            raise StyxValidationError(f'`nojac` has the wrong type: Received `{type(params["nojac"])}` expected `bool`')
    if params.get("corfov", None) is not None:
        if not isinstance(params["corfov"], bool):
            raise StyxValidationError(f'`corfov` has the wrong type: Received `{type(params["corfov"])}` expected `bool`')
    if params.get("cor", None) is not None:
        if not isinstance(params["cor"], bool):
            raise StyxValidationError(f'`cor` has the wrong type: Received `{type(params["cor"])}` expected `bool`')
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `str`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("matlab_binary", None) is not None:
        if not isinstance(params["matlab_binary"], str):
            raise StyxValidationError(f'`matlab_binary` has the wrong type: Received `{type(params["matlab_binary"])}` expected `str`')

def grad_unwarp_cargs(params: GradUnwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("grad_unwarp")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("seriesno")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(v_0)
    v_1 = params.get("unwarp_type")
    if v_1 is not None:
        cargs.append("-unwarp")
        cargs.append(v_1)
    if params.get("nojac"):
        cargs.append("-nojac")
    if params.get("corfov"):
        cargs.append("-corfov")
    if params.get("cor"):
        cargs.append("-cor")
    v_2 = params.get("interp")
    if v_2 is not None:
        cargs.append("-interp")
        cargs.append(v_2)
    cargs.append("-o")
    cargs.append(params["outfile"])
    v_3 = params.get("matlab_binary")
    if v_3 is not None:
        cargs.append("-matlab")
        cargs.append(v_3)
    return cargs

def grad_unwarp_outputs(params: GradUnwarp, execution: Execution) -> GradUnwarpOutputs:
    """Build the GradUnwarpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    mgh_output_v: OutputPathType = execution.output_file(params["outfile"])
    cor_output_v: OutputPathType = execution.output_file(f'{params["outfile"]}/')
    return GradUnwarpOutputs(
        root=root_v,
        mgh_output=mgh_output_v,
        cor_output=cor_output_v,
    )

def grad_unwarp_execute(params: GradUnwarp, runner: Runner | None = None) -> GradUnwarpOutputs:
    """
    grad_unwarp

    Convert, dewarp, and resample DICOM files to MGH files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    grad_unwarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GRAD_UNWARP_METADATA)
    execution.params(params)
    args = grad_unwarp_cargs(params, execution)
    out = grad_unwarp_outputs(params, execution)
    execution.run(args)
    return out

def grad_unwarp(
    infile: InputPathType,
    outfile: str,
    seriesno: str | None = None,
    unwarp_type: str | None = None,
    nojac: bool = False,
    corfov: bool = False,
    cor: bool = False,
    interp: str | None = None,
    matlab_binary: str | None = None,
    runner: Runner | None = None,
) -> GradUnwarpOutputs:
    """
    grad_unwarp

    Convert, dewarp, and resample DICOM files to MGH files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input file or directory (dcmfile, dcmdir, or mghfile)
        outfile: Output file in MGH format
        seriesno: DICOM series number, required if input is a directory
        unwarp_type: Gradient unwarping displacement type or map file (required for\
            MGH file)
        nojac: Do not perform jacobian correction when unwarping
        corfov: Resample to Coronal FOV
        cor: Output in COR format instead of MGH
        interp: Interpolation method (cubic, linear, nearest, spline)
        matlab_binary: Path to the Matlab binary, version 6.5 or higher required
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = grad_unwarp_params(
        infile=infile,
        outfile=outfile,
        seriesno=seriesno,
        unwarp_type=unwarp_type,
        nojac=nojac,
        corfov=corfov,
        cor=cor,
        interp=interp,
        matlab_binary=matlab_binary,
    )
    return grad_unwarp_execute(params, runner)

__all__ = [
    "GradUnwarp",
    "GradUnwarpOutputs",
    "GRAD_UNWARP_METADATA",
    "grad_unwarp_cargs",
    "grad_unwarp_outputs",
    "grad_unwarp_params",
    "grad_unwarp_execute",
    "grad_unwarp_validate",
    "grad_unwarp",
]