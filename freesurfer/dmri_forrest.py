# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_FORREST_METADATA = Metadata(
    id="dmri_forrest",
    name="dmri_forrest",
    package="freesurfer",
)

DmriForrest = typing.TypedDict(
    "DmriForrest",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_forrest"]],
        "test_dir": str,
        "train_file": InputPathType,
        "mask_file": InputPathType,
        "tract_files": list[InputPathType],
        "seg_file": typing.NotRequired[InputPathType],
        "diff_file": typing.NotRequired[InputPathType],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriForrestOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_forrest_params(
    test_dir: str,
    train_file: InputPathType,
    mask_file: InputPathType,
    tract_files: list[InputPathType],
    seg_file: InputPathType | None = None,
    diff_file: InputPathType | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
) -> DmriForrest:
    """
    Build parameters.

    Args:
        test_dir: Directory containing the test subject data
        train_file: File listing training subject directories, one per line
        mask_file: Input brain mask volume name, relative to subject directory
        tract_files: Input tract label volume(s), relative to subject directory
        seg_file: Input aparc+aseg volume name, relative to subject directory
        diff_file: Input diffusion orientation volume name, relative to subject\
            directory
        debug: Turn on debugging mode
        checkopts: Only check options and exit
        help: Display help information

    Returns:
        Parameter dictionary.
    """
    params: DmriForrest = {
        "@type": "freesurfer/dmri_forrest",
        "test_dir": test_dir,
        "train_file": train_file,
        "mask_file": mask_file,
        "tract_files": tract_files,
        "debug": debug,
        "checkopts": checkopts,
        "help": help,
    }
    if seg_file is not None:
        params["seg_file"] = seg_file
    if diff_file is not None:
        params["diff_file"] = diff_file
    return params

def dmri_forrest_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriForrest."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("test_dir", None) is None:
        raise StyxValidationError("`test_dir` must not be None")
    if not isinstance(params["test_dir"], str):
        raise StyxValidationError(f'`test_dir` has the wrong type: Received `{type(params["test_dir"])}` expected `str`')
    if params.get("train_file", None) is None:
        raise StyxValidationError("`train_file` must not be None")
    if not isinstance(params["train_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`train_file` has the wrong type: Received `{type(params["train_file"])}` expected `InputPathType`')
    if params.get("mask_file", None) is None:
        raise StyxValidationError("`mask_file` must not be None")
    if not isinstance(params["mask_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("tract_files", None) is None:
        raise StyxValidationError("`tract_files` must not be None")
    if not isinstance(params["tract_files"], list):
        raise StyxValidationError(f'`tract_files` has the wrong type: Received `{type(params["tract_files"])}` expected `list[InputPathType]`')
    for e in params["tract_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`tract_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("seg_file", None) is not None:
        if not isinstance(params["seg_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg_file` has the wrong type: Received `{type(params["seg_file"])}` expected `InputPathType`')
    if params.get("diff_file", None) is not None:
        if not isinstance(params["diff_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`diff_file` has the wrong type: Received `{type(params["diff_file"])}` expected `InputPathType`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def dmri_forrest_cargs(params: DmriForrest, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_forrest")
    cargs.append("--test")
    cargs.append(params["test_dir"])
    cargs.append("--train")
    cargs.append(execution.input_file(params["train_file"]))
    cargs.append("--mask")
    cargs.append(execution.input_file(params["mask_file"]))
    cargs.append("--tract")
    for item0 in params["tract_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("seg_file")
    if v_0 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("diff_file")
    if v_1 is not None:
        cargs.append("--diff")
        cargs.append(execution.input_file(v_1))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def dmri_forrest_outputs(params: DmriForrest, execution: Execution) -> DmriForrestOutputs:
    """Build the DmriForrestOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriForrestOutputs(
        root=root_v,
    )

def dmri_forrest_execute(params: DmriForrest, runner: Runner | None = None) -> DmriForrestOutputs:
    """
    dmri_forrest

    dmri_forrest is a tool for processing diffusion MRI data using a random forest-based method.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_forrest_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_FORREST_METADATA)
    execution.params(params)
    args = dmri_forrest_cargs(params, execution)
    out = dmri_forrest_outputs(params, execution)
    execution.run(args)
    return out

def dmri_forrest(
    test_dir: str,
    train_file: InputPathType,
    mask_file: InputPathType,
    tract_files: list[InputPathType],
    seg_file: InputPathType | None = None,
    diff_file: InputPathType | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> DmriForrestOutputs:
    """
    dmri_forrest

    dmri_forrest is a tool for processing diffusion MRI data using a random forest-based method.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        test_dir: Directory containing the test subject data
        train_file: File listing training subject directories, one per line
        mask_file: Input brain mask volume name, relative to subject directory
        tract_files: Input tract label volume(s), relative to subject directory
        seg_file: Input aparc+aseg volume name, relative to subject directory
        diff_file: Input diffusion orientation volume name, relative to subject\
            directory
        debug: Turn on debugging mode
        checkopts: Only check options and exit
        help: Display help information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_forrest_params(
        test_dir=test_dir,
        train_file=train_file,
        mask_file=mask_file,
        tract_files=tract_files,
        seg_file=seg_file,
        diff_file=diff_file,
        debug=debug,
        checkopts=checkopts,
        help=help,
    )
    return dmri_forrest_execute(params, runner)

__all__ = [
    "DmriForrest",
    "DmriForrestOutputs",
    "DMRI_FORREST_METADATA",
    "dmri_forrest_cargs",
    "dmri_forrest_outputs",
    "dmri_forrest_params",
    "dmri_forrest_execute",
    "dmri_forrest_validate",
    "dmri_forrest",
]