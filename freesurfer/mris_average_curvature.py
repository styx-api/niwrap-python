# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_AVERAGE_CURVATURE_METADATA = Metadata(
    id="mris_average_curvature",
    name="mris_average_curvature",
    package="freesurfer",
)

MrisAverageCurvature = typing.TypedDict(
    "MrisAverageCurvature",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_average_curvature"]],
        "input_curvature_file": InputPathType,
        "hemi": str,
        "surface": str,
        "subjects": list[str],
        "output_curvature_file": str,
        "summary_stats_flag": typing.NotRequired[bool],
        "output_surface_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisAverageCurvatureOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_average_curvature_params(
    input_curvature_file: InputPathType,
    hemi: str,
    surface: str,
    subjects: list[str],
    output_curvature_file: str,
    summary_stats_flag: bool = False,
    output_surface_flag: bool = False,
) -> MrisAverageCurvature:
    """
    Build parameters.

    Args:
        input_curvature_file: The input curvature file.
        hemi: Specifies the hemisphere (e.g., lh or rh).
        surface: Specifies the surface.
        subjects: The list of subjects. The output curvature file will be painted\
            onto the last subject specified.
        output_curvature_file: The output curvature file.
        summary_stats_flag: Generate summary statistics and write them into\
            specified directory.
        output_surface_flag: Override the last subject as the output surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisAverageCurvature = {
        "@type": "freesurfer/mris_average_curvature",
        "input_curvature_file": input_curvature_file,
        "hemi": hemi,
        "surface": surface,
        "subjects": subjects,
        "output_curvature_file": output_curvature_file,
        "summary_stats_flag": summary_stats_flag,
        "output_surface_flag": output_surface_flag,
    }
    return params

def mris_average_curvature_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisAverageCurvature."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_curvature_file", None) is None:
        raise StyxValidationError("`input_curvature_file` must not be None")
    if not isinstance(params["input_curvature_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_curvature_file` has the wrong type: Received `{type(params["input_curvature_file"])}` expected `InputPathType`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_curvature_file", None) is None:
        raise StyxValidationError("`output_curvature_file` must not be None")
    if not isinstance(params["output_curvature_file"], str):
        raise StyxValidationError(f'`output_curvature_file` has the wrong type: Received `{type(params["output_curvature_file"])}` expected `str`')
    if params.get("summary_stats_flag", None) is not None:
        if not isinstance(params["summary_stats_flag"], bool):
            raise StyxValidationError(f'`summary_stats_flag` has the wrong type: Received `{type(params["summary_stats_flag"])}` expected `bool`')
    if params.get("output_surface_flag", None) is not None:
        if not isinstance(params["output_surface_flag"], bool):
            raise StyxValidationError(f'`output_surface_flag` has the wrong type: Received `{type(params["output_surface_flag"])}` expected `bool`')

def mris_average_curvature_cargs(params: MrisAverageCurvature, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_average_curvature")
    cargs.append(execution.input_file(params["input_curvature_file"]))
    cargs.append(params["hemi"])
    cargs.append(params["surface"])
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_curvature_file"])
    if params.get("summary_stats_flag"):
        cargs.append("-s")
    if params.get("output_surface_flag"):
        cargs.append("-o")
    return cargs

def mris_average_curvature_outputs(params: MrisAverageCurvature, execution: Execution) -> MrisAverageCurvatureOutputs:
    """Build the MrisAverageCurvatureOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisAverageCurvatureOutputs(
        root=root_v,
    )

def mris_average_curvature_execute(params: MrisAverageCurvature, runner: Runner | None = None) -> MrisAverageCurvatureOutputs:
    """
    mris_average_curvature

    This tool averages curvature data across multiple subjects and generates an output curvature file painted onto the last subject specified. It can also generate summary statistics if requested.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_average_curvature_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_AVERAGE_CURVATURE_METADATA)
    execution.params(params)
    args = mris_average_curvature_cargs(params, execution)
    out = mris_average_curvature_outputs(params, execution)
    execution.run(args)
    return out

def mris_average_curvature(
    input_curvature_file: InputPathType,
    hemi: str,
    surface: str,
    subjects: list[str],
    output_curvature_file: str,
    summary_stats_flag: bool = False,
    output_surface_flag: bool = False,
    runner: Runner | None = None,
) -> MrisAverageCurvatureOutputs:
    """
    mris_average_curvature

    This tool averages curvature data across multiple subjects and generates an output curvature file painted onto the last subject specified. It can also generate summary statistics if requested.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_curvature_file: The input curvature file.
        hemi: Specifies the hemisphere (e.g., lh or rh).
        surface: Specifies the surface.
        subjects: The list of subjects. The output curvature file will be painted\
            onto the last subject specified.
        output_curvature_file: The output curvature file.
        summary_stats_flag: Generate summary statistics and write them into\
            specified directory.
        output_surface_flag: Override the last subject as the output surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_average_curvature_params(
        input_curvature_file=input_curvature_file,
        hemi=hemi,
        surface=surface,
        subjects=subjects,
        output_curvature_file=output_curvature_file,
        summary_stats_flag=summary_stats_flag,
        output_surface_flag=output_surface_flag,
    )
    return mris_average_curvature_execute(params, runner)

__all__ = [
    "MrisAverageCurvature",
    "MrisAverageCurvatureOutputs",
    "MRIS_AVERAGE_CURVATURE_METADATA",
    "mris_average_curvature_cargs",
    "mris_average_curvature_outputs",
    "mris_average_curvature_params",
    "mris_average_curvature_execute",
    "mris_average_curvature_validate",
    "mris_average_curvature",
]