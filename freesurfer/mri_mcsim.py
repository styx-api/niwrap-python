# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MCSIM_METADATA = Metadata(
    id="mri_mcsim",
    name="mri_mcsim",
    package="freesurfer",
)

MriMcsim = typing.TypedDict(
    "MriMcsim",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_mcsim"]],
        "top_output_dir": str,
        "base_name": str,
        "surface": list[str],
        "num_repetitions": float,
        "fwhm_values": typing.NotRequired[list[float]],
        "fwhm_max": typing.NotRequired[float],
        "avg_vertex_area": typing.NotRequired[bool],
        "random_seed": typing.NotRequired[float],
        "label_file": typing.NotRequired[InputPathType],
        "mask_file": typing.NotRequired[InputPathType],
        "no_label": typing.NotRequired[bool],
        "no_save_mask": typing.NotRequired[bool],
        "surface_name": typing.NotRequired[str],
        "log_file": typing.NotRequired[str],
        "done_file": typing.NotRequired[str],
        "stop_file": typing.NotRequired[str],
        "save_file": typing.NotRequired[str],
        "save_iter": typing.NotRequired[bool],
        "subjects_dir": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "check_opts": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriMcsimOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    csd_output: OutputPathType
    """Output CSD files based on the base name"""
    done_output: OutputPathType | None
    """Done file created upon completion"""
    iteration_save: OutputPathType | None
    """Iteration save file"""
    log_output: OutputPathType | None
    """Log file generated during execution"""

def mri_mcsim_params(
    top_output_dir: str,
    base_name: str,
    surface: list[str],
    num_repetitions: float,
    fwhm_values: list[float] | None = None,
    fwhm_max: float | None = None,
    avg_vertex_area: bool = False,
    random_seed: float | None = None,
    label_file: InputPathType | None = None,
    mask_file: InputPathType | None = None,
    no_label: bool = False,
    no_save_mask: bool = False,
    surface_name: str | None = None,
    log_file: str | None = None,
    done_file: str | None = None,
    stop_file: str | None = None,
    save_file: str | None = None,
    save_iter: bool = False,
    subjects_dir: str | None = None,
    debug: bool = False,
    check_opts: bool = False,
    help: bool = False,
    version: bool = False,
) -> MriMcsim:
    """
    Build parameters.

    Args:
        top_output_dir: Top output directory
        base_name: Base name for CSD files
        surface: Subject name and hemisphere for the surface (e.g., subjectname lh)
        num_repetitions: Number of repetitions for the simulation
        fwhm_values: Full Width at Half Maximum values for smoothing
        fwhm_max: Maximum FWHM for simulation (default 30)
        avg_vertex_area: Report cluster area based on average vertex area
        random_seed: Random seed value (default is based on Time of Day)
        label_file: Label file for masking (default is ?h.cortex.label)
        mask_file: Mask file instead of label
        no_label: Do not use a label to mask
        no_save_mask: Do not save mask to output
        surface_name: Surface name (default is white)
        log_file: Log file for the output
        done_file: Done file to create when finished
        stop_file: Stop file (default is ourdir/mri_mcsim.stop)
        save_file: Save file (default is ourdir/mri_mcsim.save)
        save_iter: Save output after each iteration
        subjects_dir: Subjects directory
        debug: Turn on debugging
        check_opts: Check options do not run
        help: Display help message
        version: Display version and exit

    Returns:
        Parameter dictionary.
    """
    params: MriMcsim = {
        "@type": "freesurfer/mri_mcsim",
        "top_output_dir": top_output_dir,
        "base_name": base_name,
        "surface": surface,
        "num_repetitions": num_repetitions,
        "avg_vertex_area": avg_vertex_area,
        "no_label": no_label,
        "no_save_mask": no_save_mask,
        "save_iter": save_iter,
        "debug": debug,
        "check_opts": check_opts,
        "help": help,
        "version": version,
    }
    if fwhm_values is not None:
        params["fwhm_values"] = fwhm_values
    if fwhm_max is not None:
        params["fwhm_max"] = fwhm_max
    if random_seed is not None:
        params["random_seed"] = random_seed
    if label_file is not None:
        params["label_file"] = label_file
    if mask_file is not None:
        params["mask_file"] = mask_file
    if surface_name is not None:
        params["surface_name"] = surface_name
    if log_file is not None:
        params["log_file"] = log_file
    if done_file is not None:
        params["done_file"] = done_file
    if stop_file is not None:
        params["stop_file"] = stop_file
    if save_file is not None:
        params["save_file"] = save_file
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    return params

def mri_mcsim_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMcsim."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("top_output_dir", None) is None:
        raise StyxValidationError("`top_output_dir` must not be None")
    if not isinstance(params["top_output_dir"], str):
        raise StyxValidationError(f'`top_output_dir` has the wrong type: Received `{type(params["top_output_dir"])}` expected `str`')
    if params.get("base_name", None) is None:
        raise StyxValidationError("`base_name` must not be None")
    if not isinstance(params["base_name"], str):
        raise StyxValidationError(f'`base_name` has the wrong type: Received `{type(params["base_name"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], list):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `list[str]`')
    if not (2 <= len(params["surface"]) <= 2):
        raise StyxValidationError("Parameter `surface` must contain between 2 and 2 elements (inclusive)")
    for e in params["surface"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("num_repetitions", None) is None:
        raise StyxValidationError("`num_repetitions` must not be None")
    if not isinstance(params["num_repetitions"], (float, int)):
        raise StyxValidationError(f'`num_repetitions` has the wrong type: Received `{type(params["num_repetitions"])}` expected `float`')
    if params.get("fwhm_values", None) is not None:
        if not isinstance(params["fwhm_values"], list):
            raise StyxValidationError(f'`fwhm_values` has the wrong type: Received `{type(params["fwhm_values"])}` expected `list[float]`')
        for e_2 in params["fwhm_values"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`fwhm_values` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("fwhm_max", None) is not None:
        if not isinstance(params["fwhm_max"], (float, int)):
            raise StyxValidationError(f'`fwhm_max` has the wrong type: Received `{type(params["fwhm_max"])}` expected `float`')
    if params.get("avg_vertex_area", None) is not None:
        if not isinstance(params["avg_vertex_area"], bool):
            raise StyxValidationError(f'`avg_vertex_area` has the wrong type: Received `{type(params["avg_vertex_area"])}` expected `bool`')
    if params.get("random_seed", None) is not None:
        if not isinstance(params["random_seed"], (float, int)):
            raise StyxValidationError(f'`random_seed` has the wrong type: Received `{type(params["random_seed"])}` expected `float`')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("no_label", None) is not None:
        if not isinstance(params["no_label"], bool):
            raise StyxValidationError(f'`no_label` has the wrong type: Received `{type(params["no_label"])}` expected `bool`')
    if params.get("no_save_mask", None) is not None:
        if not isinstance(params["no_save_mask"], bool):
            raise StyxValidationError(f'`no_save_mask` has the wrong type: Received `{type(params["no_save_mask"])}` expected `bool`')
    if params.get("surface_name", None) is not None:
        if not isinstance(params["surface_name"], str):
            raise StyxValidationError(f'`surface_name` has the wrong type: Received `{type(params["surface_name"])}` expected `str`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("done_file", None) is not None:
        if not isinstance(params["done_file"], str):
            raise StyxValidationError(f'`done_file` has the wrong type: Received `{type(params["done_file"])}` expected `str`')
    if params.get("stop_file", None) is not None:
        if not isinstance(params["stop_file"], str):
            raise StyxValidationError(f'`stop_file` has the wrong type: Received `{type(params["stop_file"])}` expected `str`')
    if params.get("save_file", None) is not None:
        if not isinstance(params["save_file"], str):
            raise StyxValidationError(f'`save_file` has the wrong type: Received `{type(params["save_file"])}` expected `str`')
    if params.get("save_iter", None) is not None:
        if not isinstance(params["save_iter"], bool):
            raise StyxValidationError(f'`save_iter` has the wrong type: Received `{type(params["save_iter"])}` expected `bool`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_opts", None) is not None:
        if not isinstance(params["check_opts"], bool):
            raise StyxValidationError(f'`check_opts` has the wrong type: Received `{type(params["check_opts"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_mcsim_cargs(params: MriMcsim, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_mcsim")
    cargs.append("--o")
    cargs.append(params["top_output_dir"])
    cargs.append("--base")
    cargs.append(params["base_name"])
    cargs.append("--surface")
    for item0 in params["surface"]:
        cargs.append(item0)
    cargs.append("--nreps")
    cargs.append(str(params["num_repetitions"]))
    v_0 = params.get("fwhm_values")
    if v_0 is not None:
        cargs.append("--fwhm")
        for item1 in v_0:
            cargs.append(str(item1))
    v_1 = params.get("fwhm_max")
    if v_1 is not None:
        cargs.append("--fwhm-max")
        cargs.append(str(v_1))
    if params.get("avg_vertex_area"):
        cargs.append("--avgvtxarea")
    v_2 = params.get("random_seed")
    if v_2 is not None:
        cargs.append("--seed")
        cargs.append(str(v_2))
    v_3 = params.get("label_file")
    if v_3 is not None:
        cargs.append("--label")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("mask_file")
    if v_4 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_4))
    if params.get("no_label"):
        cargs.append("--no-label")
    if params.get("no_save_mask"):
        cargs.append("--no-save-mask")
    v_5 = params.get("surface_name")
    if v_5 is not None:
        cargs.append("--surfname")
        cargs.append(v_5)
    v_6 = params.get("log_file")
    if v_6 is not None:
        cargs.append("--log")
        cargs.append(v_6)
    v_7 = params.get("done_file")
    if v_7 is not None:
        cargs.append("--done")
        cargs.append(v_7)
    v_8 = params.get("stop_file")
    if v_8 is not None:
        cargs.append("--stop")
        cargs.append(v_8)
    v_9 = params.get("save_file")
    if v_9 is not None:
        cargs.append("--save")
        cargs.append(v_9)
    if params.get("save_iter"):
        cargs.append("--save-iter")
    v_10 = params.get("subjects_dir")
    if v_10 is not None:
        cargs.append("--sd")
        cargs.append(v_10)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_opts"):
        cargs.append("--checkopts")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_mcsim_outputs(params: MriMcsim, execution: Execution) -> MriMcsimOutputs:
    """Build the MriMcsimOutputs object for this tool."""
    done_output_v: OutputPathType | None = None
    iteration_save_v: OutputPathType | None = None
    log_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    csd_output_v: OutputPathType = execution.output_file(f'{params["top_output_dir"]}/{params["base_name"]}.csd')
    __v0 = params.get("done_file")
    if __v0 is not None:
        done_output_v = execution.output_file(f'{params["top_output_dir"]}/done/{(__v0 if __v0 is not None else "")}')
    __v1 = params.get("save_file")
    if __v1 is not None:
        iteration_save_v = execution.output_file(f'{params["top_output_dir"]}/{(__v1 if __v1 is not None else "")}')
    __v2 = params.get("log_file")
    if __v2 is not None:
        log_output_v = execution.output_file(f'{params["top_output_dir"]}/log/{(__v2 if __v2 is not None else "")}')
    return MriMcsimOutputs(
        root=root_v,
        csd_output=csd_output_v,
        done_output=done_output_v,
        iteration_save=iteration_save_v,
        log_output=log_output_v,
    )

def mri_mcsim_execute(params: MriMcsim, runner: Runner | None = None) -> MriMcsimOutputs:
    """
    mri_mcsim

    Monte Carlo simulation tool for surface-based multiple comparisons

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_mcsim_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MCSIM_METADATA)
    execution.params(params)
    args = mri_mcsim_cargs(params, execution)
    out = mri_mcsim_outputs(params, execution)
    execution.run(args)
    return out

def mri_mcsim(
    top_output_dir: str,
    base_name: str,
    surface: list[str],
    num_repetitions: float,
    fwhm_values: list[float] | None = None,
    fwhm_max: float | None = None,
    avg_vertex_area: bool = False,
    random_seed: float | None = None,
    label_file: InputPathType | None = None,
    mask_file: InputPathType | None = None,
    no_label: bool = False,
    no_save_mask: bool = False,
    surface_name: str | None = None,
    log_file: str | None = None,
    done_file: str | None = None,
    stop_file: str | None = None,
    save_file: str | None = None,
    save_iter: bool = False,
    subjects_dir: str | None = None,
    debug: bool = False,
    check_opts: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriMcsimOutputs:
    """
    mri_mcsim

    Monte Carlo simulation tool for surface-based multiple comparisons

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        top_output_dir: Top output directory
        base_name: Base name for CSD files
        surface: Subject name and hemisphere for the surface (e.g., subjectname lh)
        num_repetitions: Number of repetitions for the simulation
        fwhm_values: Full Width at Half Maximum values for smoothing
        fwhm_max: Maximum FWHM for simulation (default 30)
        avg_vertex_area: Report cluster area based on average vertex area
        random_seed: Random seed value (default is based on Time of Day)
        label_file: Label file for masking (default is ?h.cortex.label)
        mask_file: Mask file instead of label
        no_label: Do not use a label to mask
        no_save_mask: Do not save mask to output
        surface_name: Surface name (default is white)
        log_file: Log file for the output
        done_file: Done file to create when finished
        stop_file: Stop file (default is ourdir/mri_mcsim.stop)
        save_file: Save file (default is ourdir/mri_mcsim.save)
        save_iter: Save output after each iteration
        subjects_dir: Subjects directory
        debug: Turn on debugging
        check_opts: Check options do not run
        help: Display help message
        version: Display version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_mcsim_params(
        top_output_dir=top_output_dir,
        base_name=base_name,
        surface=surface,
        num_repetitions=num_repetitions,
        fwhm_values=fwhm_values,
        fwhm_max=fwhm_max,
        avg_vertex_area=avg_vertex_area,
        random_seed=random_seed,
        label_file=label_file,
        mask_file=mask_file,
        no_label=no_label,
        no_save_mask=no_save_mask,
        surface_name=surface_name,
        log_file=log_file,
        done_file=done_file,
        stop_file=stop_file,
        save_file=save_file,
        save_iter=save_iter,
        subjects_dir=subjects_dir,
        debug=debug,
        check_opts=check_opts,
        help=help,
        version=version,
    )
    return mri_mcsim_execute(params, runner)

__all__ = [
    "MriMcsim",
    "MriMcsimOutputs",
    "MRI_MCSIM_METADATA",
    "mri_mcsim_cargs",
    "mri_mcsim_outputs",
    "mri_mcsim_params",
    "mri_mcsim_execute",
    "mri_mcsim_validate",
    "mri_mcsim",
]