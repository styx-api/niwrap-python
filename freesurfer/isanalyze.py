# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ISANALYZE_METADATA = Metadata(
    id="isanalyze",
    name="isanalyze",
    package="freesurfer",
)

Isanalyze = typing.TypedDict(
    "Isanalyze",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/isanalyze"]],
        "input_file": InputPathType,
    },
)

@dataclasses.dataclass
class IsanalyzeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def isanalyze_params(
    input_file: InputPathType,
) -> Isanalyze:
    """
    Build parameters.

    Args:
        input_file: Input file for IS analysis.

    Returns:
        Parameter dictionary.
    """
    params: Isanalyze = {
        "@type": "freesurfer/isanalyze",
        "input_file": input_file,
    }
    return params

def isanalyze_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Isanalyze."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def isanalyze_cargs(params: Isanalyze, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("isanalyze")
    cargs.append(execution.input_file(params["input_file"]))
    return cargs

def isanalyze_outputs(params: Isanalyze, execution: Execution) -> IsanalyzeOutputs:
    """Build the IsanalyzeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return IsanalyzeOutputs(
        root=root_v,
    )

def isanalyze_execute(params: Isanalyze, runner: Runner | None = None) -> IsanalyzeOutputs:
    """
    isanalyze

    A tool to analyze and process IS files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    isanalyze_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ISANALYZE_METADATA)
    execution.params(params)
    args = isanalyze_cargs(params, execution)
    out = isanalyze_outputs(params, execution)
    execution.run(args)
    return out

def isanalyze(
    input_file: InputPathType,
    runner: Runner | None = None,
) -> IsanalyzeOutputs:
    """
    isanalyze

    A tool to analyze and process IS files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input file for IS analysis.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = isanalyze_params(
        input_file=input_file,
    )
    return isanalyze_execute(params, runner)

__all__ = [
    "Isanalyze",
    "IsanalyzeOutputs",
    "ISANALYZE_METADATA",
    "isanalyze_cargs",
    "isanalyze_outputs",
    "isanalyze_params",
    "isanalyze_execute",
    "isanalyze_validate",
    "isanalyze",
]