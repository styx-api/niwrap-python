# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_CHANGE_MAP_METADATA = Metadata(
    id="mri_compute_change_map",
    name="mri_compute_change_map",
    package="freesurfer",
)

MriComputeChangeMap = typing.TypedDict(
    "MriComputeChangeMap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_change_map"]],
        "mean_filter": typing.NotRequired[bool],
        "gaussian_sigma": typing.NotRequired[float],
        "volume1": InputPathType,
        "volume2": InputPathType,
        "transform": InputPathType,
        "outvolume": str,
    },
)

@dataclasses.dataclass
class MriComputeChangeMapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_change_map: OutputPathType
    """Output change map registered with Volume 1"""

def mri_compute_change_map_params(
    volume1: InputPathType,
    volume2: InputPathType,
    transform: InputPathType,
    outvolume: str,
    mean_filter: bool = False,
    gaussian_sigma: float | None = None,
) -> MriComputeChangeMap:
    """
    Build parameters.

    Args:
        volume1: First volume (e.g. volume1.mgz)
        volume2: Second volume, transformed into the space of Volume 1 (e.g.\
            volume2.mgz)
        transform: Transform that takes Volume 2 coordinates into Volume 1 space
        outvolume: Output change map volume (e.g. change_map.mgz)
        mean_filter: Apply mean filter to the output before writing
        gaussian_sigma: Smooth with Gaussian filter of specified sigma before\
            writing

    Returns:
        Parameter dictionary.
    """
    params: MriComputeChangeMap = {
        "@type": "freesurfer/mri_compute_change_map",
        "volume1": volume1,
        "volume2": volume2,
        "transform": transform,
        "outvolume": outvolume,
        "mean_filter": mean_filter,
    }
    if gaussian_sigma is not None:
        params["gaussian_sigma"] = gaussian_sigma
    return params

def mri_compute_change_map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeChangeMap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mean_filter", None) is not None:
        if not isinstance(params["mean_filter"], bool):
            raise StyxValidationError(f'`mean_filter` has the wrong type: Received `{type(params["mean_filter"])}` expected `bool`')
    if params.get("gaussian_sigma", None) is not None:
        if not isinstance(params["gaussian_sigma"], (float, int)):
            raise StyxValidationError(f'`gaussian_sigma` has the wrong type: Received `{type(params["gaussian_sigma"])}` expected `float`')
    if params.get("volume1", None) is None:
        raise StyxValidationError("`volume1` must not be None")
    if not isinstance(params["volume1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume1` has the wrong type: Received `{type(params["volume1"])}` expected `InputPathType`')
    if params.get("volume2", None) is None:
        raise StyxValidationError("`volume2` must not be None")
    if not isinstance(params["volume2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume2` has the wrong type: Received `{type(params["volume2"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("outvolume", None) is None:
        raise StyxValidationError("`outvolume` must not be None")
    if not isinstance(params["outvolume"], str):
        raise StyxValidationError(f'`outvolume` has the wrong type: Received `{type(params["outvolume"])}` expected `str`')

def mri_compute_change_map_cargs(params: MriComputeChangeMap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_change_map")
    if params.get("mean_filter"):
        cargs.append("-m")
    v_0 = params.get("gaussian_sigma")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(str(v_0))
    cargs.append(execution.input_file(params["volume1"]))
    cargs.append(execution.input_file(params["volume2"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(params["outvolume"])
    return cargs

def mri_compute_change_map_outputs(params: MriComputeChangeMap, execution: Execution) -> MriComputeChangeMapOutputs:
    """Build the MriComputeChangeMapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_change_map_v: OutputPathType = execution.output_file(params["outvolume"])
    return MriComputeChangeMapOutputs(
        root=root_v,
        out_change_map=out_change_map_v,
    )

def mri_compute_change_map_execute(params: MriComputeChangeMap, runner: Runner | None = None) -> MriComputeChangeMapOutputs:
    """
    mri_compute_change_map

    Compute the change map between two MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_change_map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_CHANGE_MAP_METADATA)
    execution.params(params)
    args = mri_compute_change_map_cargs(params, execution)
    out = mri_compute_change_map_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_change_map(
    volume1: InputPathType,
    volume2: InputPathType,
    transform: InputPathType,
    outvolume: str,
    mean_filter: bool = False,
    gaussian_sigma: float | None = None,
    runner: Runner | None = None,
) -> MriComputeChangeMapOutputs:
    """
    mri_compute_change_map

    Compute the change map between two MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume1: First volume (e.g. volume1.mgz)
        volume2: Second volume, transformed into the space of Volume 1 (e.g.\
            volume2.mgz)
        transform: Transform that takes Volume 2 coordinates into Volume 1 space
        outvolume: Output change map volume (e.g. change_map.mgz)
        mean_filter: Apply mean filter to the output before writing
        gaussian_sigma: Smooth with Gaussian filter of specified sigma before\
            writing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_change_map_params(
        volume1=volume1,
        volume2=volume2,
        transform=transform,
        outvolume=outvolume,
        mean_filter=mean_filter,
        gaussian_sigma=gaussian_sigma,
    )
    return mri_compute_change_map_execute(params, runner)

__all__ = [
    "MriComputeChangeMap",
    "MriComputeChangeMapOutputs",
    "MRI_COMPUTE_CHANGE_MAP_METADATA",
    "mri_compute_change_map_cargs",
    "mri_compute_change_map_outputs",
    "mri_compute_change_map_params",
    "mri_compute_change_map_execute",
    "mri_compute_change_map_validate",
    "mri_compute_change_map",
]