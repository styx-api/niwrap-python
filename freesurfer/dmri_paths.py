# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_PATHS_METADATA = Metadata(
    id="dmri_paths",
    name="dmri_paths",
    package="freesurfer",
)

DmriPaths = typing.TypedDict(
    "DmriPaths",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_paths"]],
        "indir": typing.NotRequired[str],
        "outdir": typing.NotRequired[str],
        "dwi": typing.NotRequired[InputPathType],
        "grad": typing.NotRequired[InputPathType],
        "bval": typing.NotRequired[InputPathType],
        "mask": typing.NotRequired[InputPathType],
        "bpdir": typing.NotRequired[str],
        "ntr": typing.NotRequired[float],
        "fmin": typing.NotRequired[float],
        "basereg": typing.NotRequired[InputPathType],
        "basemask": typing.NotRequired[InputPathType],
        "roi1": typing.NotRequired[InputPathType],
        "roi2": typing.NotRequired[InputPathType],
        "roimesh1": typing.NotRequired[InputPathType],
        "roimesh2": typing.NotRequired[InputPathType],
        "roiref1": typing.NotRequired[InputPathType],
        "roiref2": typing.NotRequired[InputPathType],
        "prior": typing.NotRequired[InputPathType],
        "nprior": typing.NotRequired[InputPathType],
        "nset": typing.NotRequired[float],
        "lprior": typing.NotRequired[InputPathType],
        "lset": typing.NotRequired[float],
        "seg": typing.NotRequired[InputPathType],
        "tprior": typing.NotRequired[InputPathType],
        "cprior": typing.NotRequired[InputPathType],
        "reg": typing.NotRequired[InputPathType],
        "regnl": typing.NotRequired[InputPathType],
        "init": typing.NotRequired[InputPathType],
        "nb": typing.NotRequired[float],
        "ns": typing.NotRequired[float],
        "nk": typing.NotRequired[float],
        "nu": typing.NotRequired[float],
        "sdp": typing.NotRequired[InputPathType],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriPathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dmri_paths_params(
    indir: str | None = None,
    outdir: str | None = None,
    dwi: InputPathType | None = None,
    grad: InputPathType | None = None,
    bval: InputPathType | None = None,
    mask: InputPathType | None = None,
    bpdir: str | None = None,
    ntr: float | None = None,
    fmin: float | None = None,
    basereg: InputPathType | None = None,
    basemask: InputPathType | None = None,
    roi1: InputPathType | None = None,
    roi2: InputPathType | None = None,
    roimesh1: InputPathType | None = None,
    roimesh2: InputPathType | None = None,
    roiref1: InputPathType | None = None,
    roiref2: InputPathType | None = None,
    prior: InputPathType | None = None,
    nprior: InputPathType | None = None,
    nset: float | None = None,
    lprior: InputPathType | None = None,
    lset: float | None = None,
    seg: InputPathType | None = None,
    tprior: InputPathType | None = None,
    cprior: InputPathType | None = None,
    reg: InputPathType | None = None,
    regnl: InputPathType | None = None,
    init: InputPathType | None = None,
    nb: float | None = None,
    ns: float | None = None,
    nk: float | None = None,
    nu: float | None = None,
    sdp: InputPathType | None = None,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
) -> DmriPaths:
    """
    Build parameters.

    Args:
        indir: Input subject directory (optional), specify multiple for\
            longitudinal data.
        outdir: Output directory (one per path).
        dwi: DWI volume series.
        grad: Text file of diffusion gradients.
        bval: Text file of diffusion b-values.
        mask: Mask volume.
        bpdir: BEDPOST directory.
        ntr: Max number of tracts per voxel (default 1).
        fmin: Tract volume fraction threshold (default 0).
        basereg: Base-to-DWI registration, needed for longitudinal data only (.mat,\
            as many as input directories).
        basemask: Base template mask volume.
        roi1: End ROI 1 (volume or label, one per path).
        roi2: End ROI 2 (volume or label, one per path).
        roimesh1: Mesh for end ROI 1 (for label ROIs).
        roimesh2: Mesh for end ROI 2 (for label ROIs).
        roiref1: Reference volume for end ROI 1 (for label ROIs).
        roiref2: Reference volume for end ROI 2 (for label ROIs).
        prior: Spatial path priors (negative log-likelihoods off and on the path,\
            one pair per path).
        nprior: Near-neighbor label priors (negative log-likelihood and list of\
            labels, one pair per path).
        nset: Subset of near-neighbor label priors (default all).
        lprior: Local-neighbor label priors (negative log-likelihood and list of\
            labels, one pair per path).
        lset: Subset of local-neighbor label priors (default all).
        seg: Segmentation map of test subject, specify multiple for longitudinal\
            data.
        tprior: Path tangent vector priors (negative log-likelihood, one per path).
        cprior: Path curvature priors (negative log-likelihood, one per path).
        reg: DWI-to-atlas affine registration (.mat).
        regnl: DWI-to-atlas nonlinear registration (.m3z).
        init: Text file of initial control points (one per path).
        nb: Number of burn-in samples (default 5000).
        ns: Number of post-burn-in samples (default 5000).
        nk: Keep every nk-th sample (default 10).
        nu: Update proposal every nu-th sample (default 40).
        sdp: Text file with initial proposal standard deviations for control point\
            perturbations.
        debug: Turn on debugging.
        checkopts: Don't run anything, just check options and exit.
        version: Print out version and exit.

    Returns:
        Parameter dictionary.
    """
    params: DmriPaths = {
        "@type": "freesurfer/dmri_paths",
        "debug": debug,
        "checkopts": checkopts,
        "version": version,
    }
    if indir is not None:
        params["indir"] = indir
    if outdir is not None:
        params["outdir"] = outdir
    if dwi is not None:
        params["dwi"] = dwi
    if grad is not None:
        params["grad"] = grad
    if bval is not None:
        params["bval"] = bval
    if mask is not None:
        params["mask"] = mask
    if bpdir is not None:
        params["bpdir"] = bpdir
    if ntr is not None:
        params["ntr"] = ntr
    if fmin is not None:
        params["fmin"] = fmin
    if basereg is not None:
        params["basereg"] = basereg
    if basemask is not None:
        params["basemask"] = basemask
    if roi1 is not None:
        params["roi1"] = roi1
    if roi2 is not None:
        params["roi2"] = roi2
    if roimesh1 is not None:
        params["roimesh1"] = roimesh1
    if roimesh2 is not None:
        params["roimesh2"] = roimesh2
    if roiref1 is not None:
        params["roiref1"] = roiref1
    if roiref2 is not None:
        params["roiref2"] = roiref2
    if prior is not None:
        params["prior"] = prior
    if nprior is not None:
        params["nprior"] = nprior
    if nset is not None:
        params["nset"] = nset
    if lprior is not None:
        params["lprior"] = lprior
    if lset is not None:
        params["lset"] = lset
    if seg is not None:
        params["seg"] = seg
    if tprior is not None:
        params["tprior"] = tprior
    if cprior is not None:
        params["cprior"] = cprior
    if reg is not None:
        params["reg"] = reg
    if regnl is not None:
        params["regnl"] = regnl
    if init is not None:
        params["init"] = init
    if nb is not None:
        params["nb"] = nb
    if ns is not None:
        params["ns"] = ns
    if nk is not None:
        params["nk"] = nk
    if nu is not None:
        params["nu"] = nu
    if sdp is not None:
        params["sdp"] = sdp
    return params

def dmri_paths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriPaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("indir", None) is not None:
        if not isinstance(params["indir"], str):
            raise StyxValidationError(f'`indir` has the wrong type: Received `{type(params["indir"])}` expected `str`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("dwi", None) is not None:
        if not isinstance(params["dwi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(params["dwi"])}` expected `InputPathType`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("bval", None) is not None:
        if not isinstance(params["bval"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bval` has the wrong type: Received `{type(params["bval"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("bpdir", None) is not None:
        if not isinstance(params["bpdir"], str):
            raise StyxValidationError(f'`bpdir` has the wrong type: Received `{type(params["bpdir"])}` expected `str`')
    if params.get("ntr", None) is not None:
        if not isinstance(params["ntr"], (float, int)):
            raise StyxValidationError(f'`ntr` has the wrong type: Received `{type(params["ntr"])}` expected `float`')
    if params.get("fmin", None) is not None:
        if not isinstance(params["fmin"], (float, int)):
            raise StyxValidationError(f'`fmin` has the wrong type: Received `{type(params["fmin"])}` expected `float`')
    if params.get("basereg", None) is not None:
        if not isinstance(params["basereg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`basereg` has the wrong type: Received `{type(params["basereg"])}` expected `InputPathType`')
    if params.get("basemask", None) is not None:
        if not isinstance(params["basemask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`basemask` has the wrong type: Received `{type(params["basemask"])}` expected `InputPathType`')
    if params.get("roi1", None) is not None:
        if not isinstance(params["roi1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi1` has the wrong type: Received `{type(params["roi1"])}` expected `InputPathType`')
    if params.get("roi2", None) is not None:
        if not isinstance(params["roi2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi2` has the wrong type: Received `{type(params["roi2"])}` expected `InputPathType`')
    if params.get("roimesh1", None) is not None:
        if not isinstance(params["roimesh1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roimesh1` has the wrong type: Received `{type(params["roimesh1"])}` expected `InputPathType`')
    if params.get("roimesh2", None) is not None:
        if not isinstance(params["roimesh2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roimesh2` has the wrong type: Received `{type(params["roimesh2"])}` expected `InputPathType`')
    if params.get("roiref1", None) is not None:
        if not isinstance(params["roiref1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roiref1` has the wrong type: Received `{type(params["roiref1"])}` expected `InputPathType`')
    if params.get("roiref2", None) is not None:
        if not isinstance(params["roiref2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roiref2` has the wrong type: Received `{type(params["roiref2"])}` expected `InputPathType`')
    if params.get("prior", None) is not None:
        if not isinstance(params["prior"], (pathlib.Path, str)):
            raise StyxValidationError(f'`prior` has the wrong type: Received `{type(params["prior"])}` expected `InputPathType`')
    if params.get("nprior", None) is not None:
        if not isinstance(params["nprior"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nprior` has the wrong type: Received `{type(params["nprior"])}` expected `InputPathType`')
    if params.get("nset", None) is not None:
        if not isinstance(params["nset"], (float, int)):
            raise StyxValidationError(f'`nset` has the wrong type: Received `{type(params["nset"])}` expected `float`')
    if params.get("lprior", None) is not None:
        if not isinstance(params["lprior"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lprior` has the wrong type: Received `{type(params["lprior"])}` expected `InputPathType`')
    if params.get("lset", None) is not None:
        if not isinstance(params["lset"], (float, int)):
            raise StyxValidationError(f'`lset` has the wrong type: Received `{type(params["lset"])}` expected `float`')
    if params.get("seg", None) is not None:
        if not isinstance(params["seg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg` has the wrong type: Received `{type(params["seg"])}` expected `InputPathType`')
    if params.get("tprior", None) is not None:
        if not isinstance(params["tprior"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tprior` has the wrong type: Received `{type(params["tprior"])}` expected `InputPathType`')
    if params.get("cprior", None) is not None:
        if not isinstance(params["cprior"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cprior` has the wrong type: Received `{type(params["cprior"])}` expected `InputPathType`')
    if params.get("reg", None) is not None:
        if not isinstance(params["reg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reg` has the wrong type: Received `{type(params["reg"])}` expected `InputPathType`')
    if params.get("regnl", None) is not None:
        if not isinstance(params["regnl"], (pathlib.Path, str)):
            raise StyxValidationError(f'`regnl` has the wrong type: Received `{type(params["regnl"])}` expected `InputPathType`')
    if params.get("init", None) is not None:
        if not isinstance(params["init"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init` has the wrong type: Received `{type(params["init"])}` expected `InputPathType`')
    if params.get("nb", None) is not None:
        if not isinstance(params["nb"], (float, int)):
            raise StyxValidationError(f'`nb` has the wrong type: Received `{type(params["nb"])}` expected `float`')
    if params.get("ns", None) is not None:
        if not isinstance(params["ns"], (float, int)):
            raise StyxValidationError(f'`ns` has the wrong type: Received `{type(params["ns"])}` expected `float`')
    if params.get("nk", None) is not None:
        if not isinstance(params["nk"], (float, int)):
            raise StyxValidationError(f'`nk` has the wrong type: Received `{type(params["nk"])}` expected `float`')
    if params.get("nu", None) is not None:
        if not isinstance(params["nu"], (float, int)):
            raise StyxValidationError(f'`nu` has the wrong type: Received `{type(params["nu"])}` expected `float`')
    if params.get("sdp", None) is not None:
        if not isinstance(params["sdp"], (pathlib.Path, str)):
            raise StyxValidationError(f'`sdp` has the wrong type: Received `{type(params["sdp"])}` expected `InputPathType`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def dmri_paths_cargs(params: DmriPaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_paths")
    v_0 = params.get("indir")
    if v_0 is not None:
        cargs.append("--indir")
        cargs.append(v_0)
    v_1 = params.get("outdir")
    if v_1 is not None:
        cargs.append("--outdir")
        cargs.append(v_1)
    v_2 = params.get("dwi")
    if v_2 is not None:
        cargs.append("--dwi")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("grad")
    if v_3 is not None:
        cargs.append("--grad")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("bval")
    if v_4 is not None:
        cargs.append("--bval")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("mask")
    if v_5 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("bpdir")
    if v_6 is not None:
        cargs.append("--bpdir")
        cargs.append(v_6)
    v_7 = params.get("ntr")
    if v_7 is not None:
        cargs.append("--ntr")
        cargs.append(str(v_7))
    v_8 = params.get("fmin")
    if v_8 is not None:
        cargs.append("--fmin")
        cargs.append(str(v_8))
    v_9 = params.get("basereg")
    if v_9 is not None:
        cargs.append("--basereg")
        cargs.append(execution.input_file(v_9))
    v_10 = params.get("basemask")
    if v_10 is not None:
        cargs.append("--basemask")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("roi1")
    if v_11 is not None:
        cargs.append("--roi1")
        cargs.append(execution.input_file(v_11))
    v_12 = params.get("roi2")
    if v_12 is not None:
        cargs.append("--roi2")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("roimesh1")
    if v_13 is not None:
        cargs.append("--roimesh1")
        cargs.append(execution.input_file(v_13))
    v_14 = params.get("roimesh2")
    if v_14 is not None:
        cargs.append("--roimesh2")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("roiref1")
    if v_15 is not None:
        cargs.append("--roiref1")
        cargs.append(execution.input_file(v_15))
    v_16 = params.get("roiref2")
    if v_16 is not None:
        cargs.append("--roiref2")
        cargs.append(execution.input_file(v_16))
    v_17 = params.get("prior")
    if v_17 is not None:
        cargs.append("--prior")
        cargs.append(execution.input_file(v_17))
    v_18 = params.get("nprior")
    if v_18 is not None:
        cargs.append("--nprior")
        cargs.append(execution.input_file(v_18))
    v_19 = params.get("nset")
    if v_19 is not None:
        cargs.append("--nset")
        cargs.append(str(v_19))
    v_20 = params.get("lprior")
    if v_20 is not None:
        cargs.append("--lprior")
        cargs.append(execution.input_file(v_20))
    v_21 = params.get("lset")
    if v_21 is not None:
        cargs.append("--lset")
        cargs.append(str(v_21))
    v_22 = params.get("seg")
    if v_22 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_22))
    v_23 = params.get("tprior")
    if v_23 is not None:
        cargs.append("--tprior")
        cargs.append(execution.input_file(v_23))
    v_24 = params.get("cprior")
    if v_24 is not None:
        cargs.append("--cprior")
        cargs.append(execution.input_file(v_24))
    v_25 = params.get("reg")
    if v_25 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_25))
    v_26 = params.get("regnl")
    if v_26 is not None:
        cargs.append("--regnl")
        cargs.append(execution.input_file(v_26))
    v_27 = params.get("init")
    if v_27 is not None:
        cargs.append("--init")
        cargs.append(execution.input_file(v_27))
    v_28 = params.get("nb")
    if v_28 is not None:
        cargs.append("--nb")
        cargs.append(str(v_28))
    v_29 = params.get("ns")
    if v_29 is not None:
        cargs.append("--ns")
        cargs.append(str(v_29))
    v_30 = params.get("nk")
    if v_30 is not None:
        cargs.append("--nk")
        cargs.append(str(v_30))
    v_31 = params.get("nu")
    if v_31 is not None:
        cargs.append("--nu")
        cargs.append(str(v_31))
    v_32 = params.get("sdp")
    if v_32 is not None:
        cargs.append("--sdp")
        cargs.append(execution.input_file(v_32))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def dmri_paths_outputs(params: DmriPaths, execution: Execution) -> DmriPathsOutputs:
    """Build the DmriPathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DmriPathsOutputs(
        root=root_v,
    )

def dmri_paths_execute(params: DmriPaths, runner: Runner | None = None) -> DmriPathsOutputs:
    """
    dmri_paths

    Tool for diffusion MRI path analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_paths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_PATHS_METADATA)
    execution.params(params)
    args = dmri_paths_cargs(params, execution)
    out = dmri_paths_outputs(params, execution)
    execution.run(args)
    return out

def dmri_paths(
    indir: str | None = None,
    outdir: str | None = None,
    dwi: InputPathType | None = None,
    grad: InputPathType | None = None,
    bval: InputPathType | None = None,
    mask: InputPathType | None = None,
    bpdir: str | None = None,
    ntr: float | None = None,
    fmin: float | None = None,
    basereg: InputPathType | None = None,
    basemask: InputPathType | None = None,
    roi1: InputPathType | None = None,
    roi2: InputPathType | None = None,
    roimesh1: InputPathType | None = None,
    roimesh2: InputPathType | None = None,
    roiref1: InputPathType | None = None,
    roiref2: InputPathType | None = None,
    prior: InputPathType | None = None,
    nprior: InputPathType | None = None,
    nset: float | None = None,
    lprior: InputPathType | None = None,
    lset: float | None = None,
    seg: InputPathType | None = None,
    tprior: InputPathType | None = None,
    cprior: InputPathType | None = None,
    reg: InputPathType | None = None,
    regnl: InputPathType | None = None,
    init: InputPathType | None = None,
    nb: float | None = None,
    ns: float | None = None,
    nk: float | None = None,
    nu: float | None = None,
    sdp: InputPathType | None = None,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DmriPathsOutputs:
    """
    dmri_paths

    Tool for diffusion MRI path analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        indir: Input subject directory (optional), specify multiple for\
            longitudinal data.
        outdir: Output directory (one per path).
        dwi: DWI volume series.
        grad: Text file of diffusion gradients.
        bval: Text file of diffusion b-values.
        mask: Mask volume.
        bpdir: BEDPOST directory.
        ntr: Max number of tracts per voxel (default 1).
        fmin: Tract volume fraction threshold (default 0).
        basereg: Base-to-DWI registration, needed for longitudinal data only (.mat,\
            as many as input directories).
        basemask: Base template mask volume.
        roi1: End ROI 1 (volume or label, one per path).
        roi2: End ROI 2 (volume or label, one per path).
        roimesh1: Mesh for end ROI 1 (for label ROIs).
        roimesh2: Mesh for end ROI 2 (for label ROIs).
        roiref1: Reference volume for end ROI 1 (for label ROIs).
        roiref2: Reference volume for end ROI 2 (for label ROIs).
        prior: Spatial path priors (negative log-likelihoods off and on the path,\
            one pair per path).
        nprior: Near-neighbor label priors (negative log-likelihood and list of\
            labels, one pair per path).
        nset: Subset of near-neighbor label priors (default all).
        lprior: Local-neighbor label priors (negative log-likelihood and list of\
            labels, one pair per path).
        lset: Subset of local-neighbor label priors (default all).
        seg: Segmentation map of test subject, specify multiple for longitudinal\
            data.
        tprior: Path tangent vector priors (negative log-likelihood, one per path).
        cprior: Path curvature priors (negative log-likelihood, one per path).
        reg: DWI-to-atlas affine registration (.mat).
        regnl: DWI-to-atlas nonlinear registration (.m3z).
        init: Text file of initial control points (one per path).
        nb: Number of burn-in samples (default 5000).
        ns: Number of post-burn-in samples (default 5000).
        nk: Keep every nk-th sample (default 10).
        nu: Update proposal every nu-th sample (default 40).
        sdp: Text file with initial proposal standard deviations for control point\
            perturbations.
        debug: Turn on debugging.
        checkopts: Don't run anything, just check options and exit.
        version: Print out version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_paths_params(
        indir=indir,
        outdir=outdir,
        dwi=dwi,
        grad=grad,
        bval=bval,
        mask=mask,
        bpdir=bpdir,
        ntr=ntr,
        fmin=fmin,
        basereg=basereg,
        basemask=basemask,
        roi1=roi1,
        roi2=roi2,
        roimesh1=roimesh1,
        roimesh2=roimesh2,
        roiref1=roiref1,
        roiref2=roiref2,
        prior=prior,
        nprior=nprior,
        nset=nset,
        lprior=lprior,
        lset=lset,
        seg=seg,
        tprior=tprior,
        cprior=cprior,
        reg=reg,
        regnl=regnl,
        init=init,
        nb=nb,
        ns=ns,
        nk=nk,
        nu=nu,
        sdp=sdp,
        debug=debug,
        checkopts=checkopts,
        version=version,
    )
    return dmri_paths_execute(params, runner)

__all__ = [
    "DmriPaths",
    "DmriPathsOutputs",
    "DMRI_PATHS_METADATA",
    "dmri_paths_cargs",
    "dmri_paths_outputs",
    "dmri_paths_params",
    "dmri_paths_execute",
    "dmri_paths_validate",
    "dmri_paths",
]