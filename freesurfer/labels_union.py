# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABELS_UNION_METADATA = Metadata(
    id="labels_union",
    name="labels_union",
    package="freesurfer",
)

LabelsUnion = typing.TypedDict(
    "LabelsUnion",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/labels_union"]],
        "label1": InputPathType,
        "label2": InputPathType,
        "outputname": str,
    },
)

@dataclasses.dataclass
class LabelsUnionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    union_label: OutputPathType
    """Union of the two label files"""

def labels_union_params(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
) -> LabelsUnion:
    """
    Build parameters.

    Args:
        label1: First label file (e.g., rh.BA3a.label)
        label2: Second label file (e.g., rh.BA3b.label)
        outputname: Output name for the union label file (e.g.,\
            rh.BA3ab.union.label)

    Returns:
        Parameter dictionary.
    """
    params: LabelsUnion = {
        "@type": "freesurfer/labels_union",
        "label1": label1,
        "label2": label2,
        "outputname": outputname,
    }
    return params

def labels_union_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelsUnion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label1", None) is None:
        raise StyxValidationError("`label1` must not be None")
    if not isinstance(params["label1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label1` has the wrong type: Received `{type(params["label1"])}` expected `InputPathType`')
    if params.get("label2", None) is None:
        raise StyxValidationError("`label2` must not be None")
    if not isinstance(params["label2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label2` has the wrong type: Received `{type(params["label2"])}` expected `InputPathType`')
    if params.get("outputname", None) is None:
        raise StyxValidationError("`outputname` must not be None")
    if not isinstance(params["outputname"], str):
        raise StyxValidationError(f'`outputname` has the wrong type: Received `{type(params["outputname"])}` expected `str`')

def labels_union_cargs(params: LabelsUnion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("labels_union")
    cargs.append(execution.input_file(params["label1"]))
    cargs.append(execution.input_file(params["label2"]))
    cargs.append(params["outputname"])
    return cargs

def labels_union_outputs(params: LabelsUnion, execution: Execution) -> LabelsUnionOutputs:
    """Build the LabelsUnionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    union_label_v: OutputPathType = execution.output_file(params["outputname"])
    return LabelsUnionOutputs(
        root=root_v,
        union_label=union_label_v,
    )

def labels_union_execute(params: LabelsUnion, runner: Runner | None = None) -> LabelsUnionOutputs:
    """
    labels_union

    Utility to create the union of two label files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    labels_union_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABELS_UNION_METADATA)
    execution.params(params)
    args = labels_union_cargs(params, execution)
    out = labels_union_outputs(params, execution)
    execution.run(args)
    return out

def labels_union(
    label1: InputPathType,
    label2: InputPathType,
    outputname: str,
    runner: Runner | None = None,
) -> LabelsUnionOutputs:
    """
    labels_union

    Utility to create the union of two label files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label1: First label file (e.g., rh.BA3a.label)
        label2: Second label file (e.g., rh.BA3b.label)
        outputname: Output name for the union label file (e.g.,\
            rh.BA3ab.union.label)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = labels_union_params(
        label1=label1,
        label2=label2,
        outputname=outputname,
    )
    return labels_union_execute(params, runner)

__all__ = [
    "LabelsUnion",
    "LabelsUnionOutputs",
    "LABELS_UNION_METADATA",
    "labels_union_cargs",
    "labels_union_outputs",
    "labels_union_params",
    "labels_union_execute",
    "labels_union_validate",
    "labels_union",
]