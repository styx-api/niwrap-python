# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_COPY_HEADER_METADATA = Metadata(
    id="mris_copy_header",
    name="mris_copy_header",
    package="freesurfer",
)

MrisCopyHeader = typing.TypedDict(
    "MrisCopyHeader",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_copy_header"]],
        "input_surface": InputPathType,
        "template_surface": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class MrisCopyHeaderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_surface: OutputPathType
    """Output surface file with copied header."""

def mris_copy_header_params(
    input_surface: InputPathType,
    template_surface: InputPathType,
    output_surface: str,
) -> MrisCopyHeader:
    """
    Build parameters.

    Args:
        input_surface: Input surface file whose header will be replaced.
        template_surface: Template surface file from which the header will be\
            copied.
        output_surface: Output surface file with the updated header.

    Returns:
        Parameter dictionary.
    """
    params: MrisCopyHeader = {
        "@type": "freesurfer/mris_copy_header",
        "input_surface": input_surface,
        "template_surface": template_surface,
        "output_surface": output_surface,
    }
    return params

def mris_copy_header_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisCopyHeader."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("template_surface", None) is None:
        raise StyxValidationError("`template_surface` must not be None")
    if not isinstance(params["template_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_surface` has the wrong type: Received `{type(params["template_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def mris_copy_header_cargs(params: MrisCopyHeader, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_copy_header")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["template_surface"]))
    cargs.append(params["output_surface"])
    return cargs

def mris_copy_header_outputs(params: MrisCopyHeader, execution: Execution) -> MrisCopyHeaderOutputs:
    """Build the MrisCopyHeaderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisCopyHeaderOutputs(
        root=root_v,
        out_surface=out_surface_v,
    )

def mris_copy_header_execute(params: MrisCopyHeader, runner: Runner | None = None) -> MrisCopyHeaderOutputs:
    """
    mris_copy_header

    Tool to copy the header from a template surface to an input surface and save as the output surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_copy_header_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_COPY_HEADER_METADATA)
    execution.params(params)
    args = mris_copy_header_cargs(params, execution)
    out = mris_copy_header_outputs(params, execution)
    execution.run(args)
    return out

def mris_copy_header(
    input_surface: InputPathType,
    template_surface: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> MrisCopyHeaderOutputs:
    """
    mris_copy_header

    Tool to copy the header from a template surface to an input surface and save as the output surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file whose header will be replaced.
        template_surface: Template surface file from which the header will be\
            copied.
        output_surface: Output surface file with the updated header.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_copy_header_params(
        input_surface=input_surface,
        template_surface=template_surface,
        output_surface=output_surface,
    )
    return mris_copy_header_execute(params, runner)

__all__ = [
    "MrisCopyHeader",
    "MrisCopyHeaderOutputs",
    "MRIS_COPY_HEADER_METADATA",
    "mris_copy_header_cargs",
    "mris_copy_header_outputs",
    "mris_copy_header_params",
    "mris_copy_header_execute",
    "mris_copy_header_validate",
    "mris_copy_header",
]