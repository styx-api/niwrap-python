# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_BRAIN_VOLUME_METADATA = Metadata(
    id="mri_brain_volume",
    name="mri_brain_volume",
    package="freesurfer",
)

MriBrainVolume = typing.TypedDict(
    "MriBrainVolume",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_brain_volume"]],
        "input_file": InputPathType,
        "output_file": typing.NotRequired[str],
        "force_param": typing.NotRequired[float],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriBrainVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType | None
    """Output file containing brain volume information"""

def mri_brain_volume_params(
    input_file: InputPathType,
    output_file: str | None = None,
    force_param: float | None = None,
    version: bool = False,
) -> MriBrainVolume:
    """
    Build parameters.

    Args:
        input_file: Input MRI file
        output_file: Output file for brain volume
        force_param: Change pushout force (default 1.0)
        version: Show the current version

    Returns:
        Parameter dictionary.
    """
    params: MriBrainVolume = {
        "@type": "freesurfer/mri_brain_volume",
        "input_file": input_file,
        "version": version,
    }
    if output_file is not None:
        params["output_file"] = output_file
    if force_param is not None:
        params["force_param"] = force_param
    return params

def mri_brain_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriBrainVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("force_param", None) is not None:
        if not isinstance(params["force_param"], (float, int)):
            raise StyxValidationError(f'`force_param` has the wrong type: Received `{type(params["force_param"])}` expected `float`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_brain_volume_cargs(params: MriBrainVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_brain_volume")
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("force_param")
    if v_1 is not None:
        cargs.append("-forceParam")
        cargs.append(str(v_1))
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_brain_volume_outputs(params: MriBrainVolume, execution: Execution) -> MriBrainVolumeOutputs:
    """Build the MriBrainVolumeOutputs object for this tool."""
    output_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_volume_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriBrainVolumeOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_brain_volume_execute(params: MriBrainVolume, runner: Runner | None = None) -> MriBrainVolumeOutputs:
    """
    mri_brain_volume

    Tool to calculate brain volume from MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_brain_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_BRAIN_VOLUME_METADATA)
    execution.params(params)
    args = mri_brain_volume_cargs(params, execution)
    out = mri_brain_volume_outputs(params, execution)
    execution.run(args)
    return out

def mri_brain_volume(
    input_file: InputPathType,
    output_file: str | None = None,
    force_param: float | None = None,
    version: bool = False,
    runner: Runner | None = None,
) -> MriBrainVolumeOutputs:
    """
    mri_brain_volume

    Tool to calculate brain volume from MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input MRI file
        output_file: Output file for brain volume
        force_param: Change pushout force (default 1.0)
        version: Show the current version
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_brain_volume_params(
        input_file=input_file,
        output_file=output_file,
        force_param=force_param,
        version=version,
    )
    return mri_brain_volume_execute(params, runner)

__all__ = [
    "MriBrainVolume",
    "MriBrainVolumeOutputs",
    "MRI_BRAIN_VOLUME_METADATA",
    "mri_brain_volume_cargs",
    "mri_brain_volume_outputs",
    "mri_brain_volume_params",
    "mri_brain_volume_execute",
    "mri_brain_volume_validate",
    "mri_brain_volume",
]