# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POINTSET2LABEL_METADATA = Metadata(
    id="pointset2label",
    name="pointset2label",
    package="freesurfer",
)

Pointset2label = typing.TypedDict(
    "Pointset2label",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/pointset2label"]],
        "waypoint_file": InputPathType,
        "input_volume": InputPathType,
        "label_value": float,
        "output_volume": str,
        "clear_option": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Pointset2labelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_volume: OutputPathType
    """Output volume label file with applied labels."""

def pointset2label_params(
    waypoint_file: InputPathType,
    input_volume: InputPathType,
    label_value: float,
    output_volume: str,
    clear_option: bool = False,
) -> Pointset2label:
    """
    Build parameters.

    Args:
        waypoint_file: Path to the waypoint file.
        input_volume: Path to the input volume file.
        label_value: Label value to apply at the waypoints.
        output_volume: Path to the output volume label file.
        clear_option: Clear the input volume before processing.

    Returns:
        Parameter dictionary.
    """
    params: Pointset2label = {
        "@type": "freesurfer/pointset2label",
        "waypoint_file": waypoint_file,
        "input_volume": input_volume,
        "label_value": label_value,
        "output_volume": output_volume,
        "clear_option": clear_option,
    }
    return params

def pointset2label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Pointset2label."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("waypoint_file", None) is None:
        raise StyxValidationError("`waypoint_file` must not be None")
    if not isinstance(params["waypoint_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`waypoint_file` has the wrong type: Received `{type(params["waypoint_file"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("label_value", None) is None:
        raise StyxValidationError("`label_value` must not be None")
    if not isinstance(params["label_value"], (float, int)):
        raise StyxValidationError(f'`label_value` has the wrong type: Received `{type(params["label_value"])}` expected `float`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("clear_option", None) is not None:
        if not isinstance(params["clear_option"], bool):
            raise StyxValidationError(f'`clear_option` has the wrong type: Received `{type(params["clear_option"])}` expected `bool`')

def pointset2label_cargs(params: Pointset2label, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("pointset2label")
    cargs.append(execution.input_file(params["waypoint_file"]))
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["label_value"]))
    cargs.append(params["output_volume"])
    if params.get("clear_option"):
        cargs.append("-clear")
    return cargs

def pointset2label_outputs(params: Pointset2label, execution: Execution) -> Pointset2labelOutputs:
    """Build the Pointset2labelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_volume_v: OutputPathType = execution.output_file(params["output_volume"])
    return Pointset2labelOutputs(
        root=root_v,
        output_label_volume=output_label_volume_v,
    )

def pointset2label_execute(params: Pointset2label, runner: Runner | None = None) -> Pointset2labelOutputs:
    """
    pointset2label

    Tool for applying waypoint labels to a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    pointset2label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POINTSET2LABEL_METADATA)
    execution.params(params)
    args = pointset2label_cargs(params, execution)
    out = pointset2label_outputs(params, execution)
    execution.run(args)
    return out

def pointset2label(
    waypoint_file: InputPathType,
    input_volume: InputPathType,
    label_value: float,
    output_volume: str,
    clear_option: bool = False,
    runner: Runner | None = None,
) -> Pointset2labelOutputs:
    """
    pointset2label

    Tool for applying waypoint labels to a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        waypoint_file: Path to the waypoint file.
        input_volume: Path to the input volume file.
        label_value: Label value to apply at the waypoints.
        output_volume: Path to the output volume label file.
        clear_option: Clear the input volume before processing.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = pointset2label_params(
        waypoint_file=waypoint_file,
        input_volume=input_volume,
        label_value=label_value,
        output_volume=output_volume,
        clear_option=clear_option,
    )
    return pointset2label_execute(params, runner)

__all__ = [
    "Pointset2label",
    "Pointset2labelOutputs",
    "POINTSET2LABEL_METADATA",
    "pointset2label_cargs",
    "pointset2label_outputs",
    "pointset2label_params",
    "pointset2label_execute",
    "pointset2label_validate",
    "pointset2label",
]