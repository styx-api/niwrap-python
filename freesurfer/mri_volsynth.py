# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_VOLSYNTH_METADATA = Metadata(
    id="mri_volsynth",
    name="mri_volsynth",
    package="freesurfer",
)

MriVolsynth = typing.TypedDict(
    "MriVolsynth",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_volsynth"]],
        "output_volid": str,
        "template": typing.NotRequired[str],
        "nframes": typing.NotRequired[float],
        "offset_flag": typing.NotRequired[bool],
        "offset_mid_flag": typing.NotRequired[bool],
        "curv": typing.NotRequired[str],
        "dim": typing.NotRequired[list[float]],
        "res": typing.NotRequired[list[float]],
        "vox_size": typing.NotRequired[list[float]],
        "tr": typing.NotRequired[float],
        "cdircos": typing.NotRequired[list[float]],
        "rdircos": typing.NotRequired[list[float]],
        "sdircos": typing.NotRequired[list[float]],
        "c_ras": typing.NotRequired[list[float]],
        "p0": typing.NotRequired[list[float]],
        "precision": typing.NotRequired[str],
        "seed": typing.NotRequired[float],
        "seedfile": typing.NotRequired[InputPathType],
        "pdf": typing.NotRequired[str],
        "bb": typing.NotRequired[list[float]],
        "gmean": typing.NotRequired[float],
        "gstd": typing.NotRequired[float],
        "delta_crsf": typing.NotRequired[list[float]],
        "delta_val": typing.NotRequired[float],
        "delta_val_off": typing.NotRequired[float],
        "grid": typing.NotRequired[list[float]],
        "dof": typing.NotRequired[float],
        "dof_num": typing.NotRequired[float],
        "dof_den": typing.NotRequired[float],
        "rescale_flag": typing.NotRequired[bool],
        "val_a": typing.NotRequired[float],
        "val_b": typing.NotRequired[float],
        "vox_radius": typing.NotRequired[float],
        "mm_radius": typing.NotRequired[float],
        "sphere_center": typing.NotRequired[list[float]],
        "hsc": typing.NotRequired[list[float]],
        "abs_flag": typing.NotRequired[bool],
        "cp": typing.NotRequired[InputPathType],
        "spike": typing.NotRequired[float],
        "fwhm": typing.NotRequired[float],
        "sum2": typing.NotRequired[InputPathType],
        "dim_surf_flag": typing.NotRequired[bool],
        "ctab": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MriVolsynthOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_volsynth_params(
    output_volid: str,
    template: str | None = None,
    nframes: float | None = None,
    offset_flag: bool = False,
    offset_mid_flag: bool = False,
    curv: str | None = None,
    dim: list[float] | None = None,
    res: list[float] | None = None,
    vox_size: list[float] | None = None,
    tr: float | None = None,
    cdircos: list[float] | None = None,
    rdircos: list[float] | None = None,
    sdircos: list[float] | None = None,
    c_ras: list[float] | None = None,
    p0: list[float] | None = None,
    precision: str | None = None,
    seed: float | None = None,
    seedfile: InputPathType | None = None,
    pdf: str | None = None,
    bb: list[float] | None = None,
    gmean: float | None = None,
    gstd: float | None = None,
    delta_crsf: list[float] | None = None,
    delta_val: float | None = None,
    delta_val_off: float | None = None,
    grid: list[float] | None = None,
    dof: float | None = None,
    dof_num: float | None = None,
    dof_den: float | None = None,
    rescale_flag: bool = False,
    val_a: float | None = None,
    val_b: float | None = None,
    vox_radius: float | None = None,
    mm_radius: float | None = None,
    sphere_center: list[float] | None = None,
    hsc: list[float] | None = None,
    abs_flag: bool = False,
    cp: InputPathType | None = None,
    spike: float | None = None,
    fwhm: float | None = None,
    sum2: InputPathType | None = None,
    dim_surf_flag: bool = False,
    ctab: InputPathType | None = None,
) -> MriVolsynth:
    """
    Build parameters.

    Args:
        output_volid: Output volume path id and format
        template: Template volume id
        nframes: Override template number of frames
        offset_flag: Use template as intensity offset
        offset_mid_flag: Use middle frame of template as intensity offset
        curv: Save output as curvature, uses lh.thickness as template. Requires\
            subject and hemisphere.
        dim: Specify dimensionality nc nr ns nf
        res: Voxel resolution dc dr ds df (df is TR, in msec)
        vox_size: Change template voxel resolution and dimension dc dr ds
        tr: Time between frames in msec
        cdircos: Column cosine direction x, y, z
        rdircos: Row cosine direction x, y, z
        sdircos: Slice cosine direction x, y, z
        c_ras: RAS coordinates of 'center' voxel c_r c_a c_s
        p0: First voxel coordinates p0r p0a p0s
        precision: Precision of the output (e.g., float)
        seed: Seed for the random number generator
        seedfile: Write seed value to this file
        pdf: Probability distribution function (e.g., gaussian, uniform, const)
        bb: Bounding box c r s dc dr ds (In=ValA, Out=ValB)
        gmean: Mean for the gaussian distribution
        gstd: Standard deviation for the gaussian distribution
        delta_crsf: Delta's col, row, slice, and frame coordinates
        delta_val: Delta value
        delta_val_off: Delta background value
        grid: Grid dimensions dcol, drow, dslice
        dof: Degrees of freedom for t and chi2 distributions
        dof_num: Numerator degrees of freedom for F distribution
        dof_den: Denominator degrees of freedom for F distribution
        rescale_flag: Rescale z, t, F, or chi2 after smoothing
        val_a: Set ValA
        val_b: Set ValB
        vox_radius: Radius in voxels for sphere
        mm_radius: Radius in mm for sphere
        sphere_center: Sphere center coordinates column, row, slice
        hsc: Multiply each frame by a random number between min and max
        abs_flag: Compute absolute value
        cp: Set control point voxels to 1
        spike: Set all values at a given time point to 1e9
        fwhm: Smooth by Full Width at Half Maximum (FWHM) in mm
        sum2: Save sum of volume squared into specified file
        dim_surf_flag: Set dimension to nvertices x 1 x 1
        ctab: Embed color table

    Returns:
        Parameter dictionary.
    """
    params: MriVolsynth = {
        "@type": "freesurfer/mri_volsynth",
        "output_volid": output_volid,
        "offset_flag": offset_flag,
        "offset_mid_flag": offset_mid_flag,
        "rescale_flag": rescale_flag,
        "abs_flag": abs_flag,
        "dim_surf_flag": dim_surf_flag,
    }
    if template is not None:
        params["template"] = template
    if nframes is not None:
        params["nframes"] = nframes
    if curv is not None:
        params["curv"] = curv
    if dim is not None:
        params["dim"] = dim
    if res is not None:
        params["res"] = res
    if vox_size is not None:
        params["vox_size"] = vox_size
    if tr is not None:
        params["tr"] = tr
    if cdircos is not None:
        params["cdircos"] = cdircos
    if rdircos is not None:
        params["rdircos"] = rdircos
    if sdircos is not None:
        params["sdircos"] = sdircos
    if c_ras is not None:
        params["c_ras"] = c_ras
    if p0 is not None:
        params["p0"] = p0
    if precision is not None:
        params["precision"] = precision
    if seed is not None:
        params["seed"] = seed
    if seedfile is not None:
        params["seedfile"] = seedfile
    if pdf is not None:
        params["pdf"] = pdf
    if bb is not None:
        params["bb"] = bb
    if gmean is not None:
        params["gmean"] = gmean
    if gstd is not None:
        params["gstd"] = gstd
    if delta_crsf is not None:
        params["delta_crsf"] = delta_crsf
    if delta_val is not None:
        params["delta_val"] = delta_val
    if delta_val_off is not None:
        params["delta_val_off"] = delta_val_off
    if grid is not None:
        params["grid"] = grid
    if dof is not None:
        params["dof"] = dof
    if dof_num is not None:
        params["dof_num"] = dof_num
    if dof_den is not None:
        params["dof_den"] = dof_den
    if val_a is not None:
        params["val_a"] = val_a
    if val_b is not None:
        params["val_b"] = val_b
    if vox_radius is not None:
        params["vox_radius"] = vox_radius
    if mm_radius is not None:
        params["mm_radius"] = mm_radius
    if sphere_center is not None:
        params["sphere_center"] = sphere_center
    if hsc is not None:
        params["hsc"] = hsc
    if cp is not None:
        params["cp"] = cp
    if spike is not None:
        params["spike"] = spike
    if fwhm is not None:
        params["fwhm"] = fwhm
    if sum2 is not None:
        params["sum2"] = sum2
    if ctab is not None:
        params["ctab"] = ctab
    return params

def mri_volsynth_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriVolsynth."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_volid", None) is None:
        raise StyxValidationError("`output_volid` must not be None")
    if not isinstance(params["output_volid"], str):
        raise StyxValidationError(f'`output_volid` has the wrong type: Received `{type(params["output_volid"])}` expected `str`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], str):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `str`')
    if params.get("nframes", None) is not None:
        if not isinstance(params["nframes"], (float, int)):
            raise StyxValidationError(f'`nframes` has the wrong type: Received `{type(params["nframes"])}` expected `float`')
    if params.get("offset_flag", None) is not None:
        if not isinstance(params["offset_flag"], bool):
            raise StyxValidationError(f'`offset_flag` has the wrong type: Received `{type(params["offset_flag"])}` expected `bool`')
    if params.get("offset_mid_flag", None) is not None:
        if not isinstance(params["offset_mid_flag"], bool):
            raise StyxValidationError(f'`offset_mid_flag` has the wrong type: Received `{type(params["offset_mid_flag"])}` expected `bool`')
    if params.get("curv", None) is not None:
        if not isinstance(params["curv"], str):
            raise StyxValidationError(f'`curv` has the wrong type: Received `{type(params["curv"])}` expected `str`')
    if params.get("dim", None) is not None:
        if not isinstance(params["dim"], list):
            raise StyxValidationError(f'`dim` has the wrong type: Received `{type(params["dim"])}` expected `list[float]`')
        if not (4 <= len(params["dim"]) <= 4):
            raise StyxValidationError("Parameter `dim` must contain between 4 and 4 elements (inclusive)")
        for e in params["dim"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`dim` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("res", None) is not None:
        if not isinstance(params["res"], list):
            raise StyxValidationError(f'`res` has the wrong type: Received `{type(params["res"])}` expected `list[float]`')
        if not (4 <= len(params["res"]) <= 4):
            raise StyxValidationError("Parameter `res` must contain between 4 and 4 elements (inclusive)")
        for e_2 in params["res"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`res` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("vox_size", None) is not None:
        if not isinstance(params["vox_size"], list):
            raise StyxValidationError(f'`vox_size` has the wrong type: Received `{type(params["vox_size"])}` expected `list[float]`')
        if not (3 <= len(params["vox_size"]) <= 3):
            raise StyxValidationError("Parameter `vox_size` must contain between 3 and 3 elements (inclusive)")
        for e_3 in params["vox_size"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`vox_size` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("cdircos", None) is not None:
        if not isinstance(params["cdircos"], list):
            raise StyxValidationError(f'`cdircos` has the wrong type: Received `{type(params["cdircos"])}` expected `list[float]`')
        if not (3 <= len(params["cdircos"]) <= 3):
            raise StyxValidationError("Parameter `cdircos` must contain between 3 and 3 elements (inclusive)")
        for e_4 in params["cdircos"]:
            if not isinstance(e_4, (float, int)):
                raise StyxValidationError(f'`cdircos` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
    if params.get("rdircos", None) is not None:
        if not isinstance(params["rdircos"], list):
            raise StyxValidationError(f'`rdircos` has the wrong type: Received `{type(params["rdircos"])}` expected `list[float]`')
        if not (3 <= len(params["rdircos"]) <= 3):
            raise StyxValidationError("Parameter `rdircos` must contain between 3 and 3 elements (inclusive)")
        for e_5 in params["rdircos"]:
            if not isinstance(e_5, (float, int)):
                raise StyxValidationError(f'`rdircos` has the wrong type: Received `{type(e_5)}` expected `list[float]`')
    if params.get("sdircos", None) is not None:
        if not isinstance(params["sdircos"], list):
            raise StyxValidationError(f'`sdircos` has the wrong type: Received `{type(params["sdircos"])}` expected `list[float]`')
        if not (3 <= len(params["sdircos"]) <= 3):
            raise StyxValidationError("Parameter `sdircos` must contain between 3 and 3 elements (inclusive)")
        for e_6 in params["sdircos"]:
            if not isinstance(e_6, (float, int)):
                raise StyxValidationError(f'`sdircos` has the wrong type: Received `{type(e_6)}` expected `list[float]`')
    if params.get("c_ras", None) is not None:
        if not isinstance(params["c_ras"], list):
            raise StyxValidationError(f'`c_ras` has the wrong type: Received `{type(params["c_ras"])}` expected `list[float]`')
        if not (3 <= len(params["c_ras"]) <= 3):
            raise StyxValidationError("Parameter `c_ras` must contain between 3 and 3 elements (inclusive)")
        for e_7 in params["c_ras"]:
            if not isinstance(e_7, (float, int)):
                raise StyxValidationError(f'`c_ras` has the wrong type: Received `{type(e_7)}` expected `list[float]`')
    if params.get("p0", None) is not None:
        if not isinstance(params["p0"], list):
            raise StyxValidationError(f'`p0` has the wrong type: Received `{type(params["p0"])}` expected `list[float]`')
        if not (3 <= len(params["p0"]) <= 3):
            raise StyxValidationError("Parameter `p0` must contain between 3 and 3 elements (inclusive)")
        for e_8 in params["p0"]:
            if not isinstance(e_8, (float, int)):
                raise StyxValidationError(f'`p0` has the wrong type: Received `{type(e_8)}` expected `list[float]`')
    if params.get("precision", None) is not None:
        if not isinstance(params["precision"], str):
            raise StyxValidationError(f'`precision` has the wrong type: Received `{type(params["precision"])}` expected `str`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (float, int)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `float`')
    if params.get("seedfile", None) is not None:
        if not isinstance(params["seedfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seedfile` has the wrong type: Received `{type(params["seedfile"])}` expected `InputPathType`')
    if params.get("pdf", None) is not None:
        if not isinstance(params["pdf"], str):
            raise StyxValidationError(f'`pdf` has the wrong type: Received `{type(params["pdf"])}` expected `str`')
    if params.get("bb", None) is not None:
        if not isinstance(params["bb"], list):
            raise StyxValidationError(f'`bb` has the wrong type: Received `{type(params["bb"])}` expected `list[float]`')
        if not (6 <= len(params["bb"]) <= 6):
            raise StyxValidationError("Parameter `bb` must contain between 6 and 6 elements (inclusive)")
        for e_9 in params["bb"]:
            if not isinstance(e_9, (float, int)):
                raise StyxValidationError(f'`bb` has the wrong type: Received `{type(e_9)}` expected `list[float]`')
    if params.get("gmean", None) is not None:
        if not isinstance(params["gmean"], (float, int)):
            raise StyxValidationError(f'`gmean` has the wrong type: Received `{type(params["gmean"])}` expected `float`')
    if params.get("gstd", None) is not None:
        if not isinstance(params["gstd"], (float, int)):
            raise StyxValidationError(f'`gstd` has the wrong type: Received `{type(params["gstd"])}` expected `float`')
    if params.get("delta_crsf", None) is not None:
        if not isinstance(params["delta_crsf"], list):
            raise StyxValidationError(f'`delta_crsf` has the wrong type: Received `{type(params["delta_crsf"])}` expected `list[float]`')
        if not (4 <= len(params["delta_crsf"]) <= 4):
            raise StyxValidationError("Parameter `delta_crsf` must contain between 4 and 4 elements (inclusive)")
        for e_10 in params["delta_crsf"]:
            if not isinstance(e_10, (float, int)):
                raise StyxValidationError(f'`delta_crsf` has the wrong type: Received `{type(e_10)}` expected `list[float]`')
    if params.get("delta_val", None) is not None:
        if not isinstance(params["delta_val"], (float, int)):
            raise StyxValidationError(f'`delta_val` has the wrong type: Received `{type(params["delta_val"])}` expected `float`')
    if params.get("delta_val_off", None) is not None:
        if not isinstance(params["delta_val_off"], (float, int)):
            raise StyxValidationError(f'`delta_val_off` has the wrong type: Received `{type(params["delta_val_off"])}` expected `float`')
    if params.get("grid", None) is not None:
        if not isinstance(params["grid"], list):
            raise StyxValidationError(f'`grid` has the wrong type: Received `{type(params["grid"])}` expected `list[float]`')
        if not (3 <= len(params["grid"]) <= 3):
            raise StyxValidationError("Parameter `grid` must contain between 3 and 3 elements (inclusive)")
        for e_11 in params["grid"]:
            if not isinstance(e_11, (float, int)):
                raise StyxValidationError(f'`grid` has the wrong type: Received `{type(e_11)}` expected `list[float]`')
    if params.get("dof", None) is not None:
        if not isinstance(params["dof"], (float, int)):
            raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `float`')
    if params.get("dof_num", None) is not None:
        if not isinstance(params["dof_num"], (float, int)):
            raise StyxValidationError(f'`dof_num` has the wrong type: Received `{type(params["dof_num"])}` expected `float`')
    if params.get("dof_den", None) is not None:
        if not isinstance(params["dof_den"], (float, int)):
            raise StyxValidationError(f'`dof_den` has the wrong type: Received `{type(params["dof_den"])}` expected `float`')
    if params.get("rescale_flag", None) is not None:
        if not isinstance(params["rescale_flag"], bool):
            raise StyxValidationError(f'`rescale_flag` has the wrong type: Received `{type(params["rescale_flag"])}` expected `bool`')
    if params.get("val_a", None) is not None:
        if not isinstance(params["val_a"], (float, int)):
            raise StyxValidationError(f'`val_a` has the wrong type: Received `{type(params["val_a"])}` expected `float`')
    if params.get("val_b", None) is not None:
        if not isinstance(params["val_b"], (float, int)):
            raise StyxValidationError(f'`val_b` has the wrong type: Received `{type(params["val_b"])}` expected `float`')
    if params.get("vox_radius", None) is not None:
        if not isinstance(params["vox_radius"], (float, int)):
            raise StyxValidationError(f'`vox_radius` has the wrong type: Received `{type(params["vox_radius"])}` expected `float`')
    if params.get("mm_radius", None) is not None:
        if not isinstance(params["mm_radius"], (float, int)):
            raise StyxValidationError(f'`mm_radius` has the wrong type: Received `{type(params["mm_radius"])}` expected `float`')
    if params.get("sphere_center", None) is not None:
        if not isinstance(params["sphere_center"], list):
            raise StyxValidationError(f'`sphere_center` has the wrong type: Received `{type(params["sphere_center"])}` expected `list[float]`')
        if not (3 <= len(params["sphere_center"]) <= 3):
            raise StyxValidationError("Parameter `sphere_center` must contain between 3 and 3 elements (inclusive)")
        for e_12 in params["sphere_center"]:
            if not isinstance(e_12, (float, int)):
                raise StyxValidationError(f'`sphere_center` has the wrong type: Received `{type(e_12)}` expected `list[float]`')
    if params.get("hsc", None) is not None:
        if not isinstance(params["hsc"], list):
            raise StyxValidationError(f'`hsc` has the wrong type: Received `{type(params["hsc"])}` expected `list[float]`')
        if not (2 <= len(params["hsc"]) <= 2):
            raise StyxValidationError("Parameter `hsc` must contain between 2 and 2 elements (inclusive)")
        for e_13 in params["hsc"]:
            if not isinstance(e_13, (float, int)):
                raise StyxValidationError(f'`hsc` has the wrong type: Received `{type(e_13)}` expected `list[float]`')
    if params.get("abs_flag", None) is not None:
        if not isinstance(params["abs_flag"], bool):
            raise StyxValidationError(f'`abs_flag` has the wrong type: Received `{type(params["abs_flag"])}` expected `bool`')
    if params.get("cp", None) is not None:
        if not isinstance(params["cp"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cp` has the wrong type: Received `{type(params["cp"])}` expected `InputPathType`')
    if params.get("spike", None) is not None:
        if not isinstance(params["spike"], (float, int)):
            raise StyxValidationError(f'`spike` has the wrong type: Received `{type(params["spike"])}` expected `float`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("sum2", None) is not None:
        if not isinstance(params["sum2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`sum2` has the wrong type: Received `{type(params["sum2"])}` expected `InputPathType`')
    if params.get("dim_surf_flag", None) is not None:
        if not isinstance(params["dim_surf_flag"], bool):
            raise StyxValidationError(f'`dim_surf_flag` has the wrong type: Received `{type(params["dim_surf_flag"])}` expected `bool`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `InputPathType`')

def mri_volsynth_cargs(params: MriVolsynth, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_volsynth")
    cargs.append("--o")
    cargs.append(params["output_volid"])
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("--template")
        cargs.append(v_0)
    v_1 = params.get("nframes")
    if v_1 is not None:
        cargs.append("--nframes")
        cargs.append(str(v_1))
    if params.get("offset_flag"):
        cargs.append("--offset")
    if params.get("offset_mid_flag"):
        cargs.append("--offset-mid")
    v_2 = params.get("curv")
    if v_2 is not None:
        cargs.append("--curv")
        cargs.append(v_2)
    v_3 = params.get("dim")
    if v_3 is not None:
        cargs.append("--dim")
        for item0 in v_3:
            cargs.append(str(item0))
    v_4 = params.get("res")
    if v_4 is not None:
        cargs.append("--res")
        for item1 in v_4:
            cargs.append(str(item1))
    v_5 = params.get("vox_size")
    if v_5 is not None:
        cargs.append("--vox-size")
        for item2 in v_5:
            cargs.append(str(item2))
    v_6 = params.get("tr")
    if v_6 is not None:
        cargs.append("--tr")
        cargs.append(str(v_6))
    v_7 = params.get("cdircos")
    if v_7 is not None:
        cargs.append("--cdircos")
        for item3 in v_7:
            cargs.append(str(item3))
    v_8 = params.get("rdircos")
    if v_8 is not None:
        cargs.append("--rdircos")
        for item4 in v_8:
            cargs.append(str(item4))
    v_9 = params.get("sdircos")
    if v_9 is not None:
        cargs.append("--sdircos")
        for item5 in v_9:
            cargs.append(str(item5))
    v_10 = params.get("c_ras")
    if v_10 is not None:
        cargs.append("--c_ras")
        for item6 in v_10:
            cargs.append(str(item6))
    v_11 = params.get("p0")
    if v_11 is not None:
        cargs.append("--p0")
        for item7 in v_11:
            cargs.append(str(item7))
    v_12 = params.get("precision")
    if v_12 is not None:
        cargs.append("--precision")
        cargs.append(v_12)
    v_13 = params.get("seed")
    if v_13 is not None:
        cargs.append("--seed")
        cargs.append(str(v_13))
    v_14 = params.get("seedfile")
    if v_14 is not None:
        cargs.append("--seedfile")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("pdf")
    if v_15 is not None:
        cargs.append("--pdf")
        cargs.append(v_15)
    v_16 = params.get("bb")
    if v_16 is not None:
        cargs.append("--bb")
        for item8 in v_16:
            cargs.append(str(item8))
    v_17 = params.get("gmean")
    if v_17 is not None:
        cargs.append("--gmean")
        cargs.append(str(v_17))
    v_18 = params.get("gstd")
    if v_18 is not None:
        cargs.append("--gstd")
        cargs.append(str(v_18))
    v_19 = params.get("delta_crsf")
    if v_19 is not None:
        cargs.append("--delta-crsf")
        for item9 in v_19:
            cargs.append(str(item9))
    v_20 = params.get("delta_val")
    if v_20 is not None:
        cargs.append("--delta-val")
        cargs.append(str(v_20))
    v_21 = params.get("delta_val_off")
    if v_21 is not None:
        cargs.append("--delta-val-off")
        cargs.append(str(v_21))
    v_22 = params.get("grid")
    if v_22 is not None:
        cargs.append("--grid")
        for item10 in v_22:
            cargs.append(str(item10))
    v_23 = params.get("dof")
    if v_23 is not None:
        cargs.append("--dof")
        cargs.append(str(v_23))
    v_24 = params.get("dof_num")
    if v_24 is not None:
        cargs.append("--dof-num")
        cargs.append(str(v_24))
    v_25 = params.get("dof_den")
    if v_25 is not None:
        cargs.append("--dof-den")
        cargs.append(str(v_25))
    if params.get("rescale_flag"):
        cargs.append("--rescale")
    v_26 = params.get("val_a")
    if v_26 is not None:
        cargs.append("--val-a")
        cargs.append(str(v_26))
    v_27 = params.get("val_b")
    if v_27 is not None:
        cargs.append("--val-b")
        cargs.append(str(v_27))
    v_28 = params.get("vox_radius")
    if v_28 is not None:
        cargs.append("--vox-radius")
        cargs.append(str(v_28))
    v_29 = params.get("mm_radius")
    if v_29 is not None:
        cargs.append("--mm-radius")
        cargs.append(str(v_29))
    v_30 = params.get("sphere_center")
    if v_30 is not None:
        cargs.append("--sphere-center")
        for item11 in v_30:
            cargs.append(str(item11))
    v_31 = params.get("hsc")
    if v_31 is not None:
        cargs.append("--hsc")
        for item12 in v_31:
            cargs.append(str(item12))
    if params.get("abs_flag"):
        cargs.append("--abs")
    v_32 = params.get("cp")
    if v_32 is not None:
        cargs.append("--cp")
        cargs.append(execution.input_file(v_32))
    v_33 = params.get("spike")
    if v_33 is not None:
        cargs.append("--spike")
        cargs.append(str(v_33))
    v_34 = params.get("fwhm")
    if v_34 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_34))
    v_35 = params.get("sum2")
    if v_35 is not None:
        cargs.append("--sum2")
        cargs.append(execution.input_file(v_35))
    if params.get("dim_surf_flag"):
        cargs.append("--dim-surf")
    v_36 = params.get("ctab")
    if v_36 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_36))
    return cargs

def mri_volsynth_outputs(params: MriVolsynth, execution: Execution) -> MriVolsynthOutputs:
    """Build the MriVolsynthOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriVolsynthOutputs(
        root=root_v,
    )

def mri_volsynth_execute(params: MriVolsynth, runner: Runner | None = None) -> MriVolsynthOutputs:
    """
    mri_volsynth

    Synthesizes a volume with specified geometry and probability distribution function.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_volsynth_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_VOLSYNTH_METADATA)
    execution.params(params)
    args = mri_volsynth_cargs(params, execution)
    out = mri_volsynth_outputs(params, execution)
    execution.run(args)
    return out

def mri_volsynth(
    output_volid: str,
    template: str | None = None,
    nframes: float | None = None,
    offset_flag: bool = False,
    offset_mid_flag: bool = False,
    curv: str | None = None,
    dim: list[float] | None = None,
    res: list[float] | None = None,
    vox_size: list[float] | None = None,
    tr: float | None = None,
    cdircos: list[float] | None = None,
    rdircos: list[float] | None = None,
    sdircos: list[float] | None = None,
    c_ras: list[float] | None = None,
    p0: list[float] | None = None,
    precision: str | None = None,
    seed: float | None = None,
    seedfile: InputPathType | None = None,
    pdf: str | None = None,
    bb: list[float] | None = None,
    gmean: float | None = None,
    gstd: float | None = None,
    delta_crsf: list[float] | None = None,
    delta_val: float | None = None,
    delta_val_off: float | None = None,
    grid: list[float] | None = None,
    dof: float | None = None,
    dof_num: float | None = None,
    dof_den: float | None = None,
    rescale_flag: bool = False,
    val_a: float | None = None,
    val_b: float | None = None,
    vox_radius: float | None = None,
    mm_radius: float | None = None,
    sphere_center: list[float] | None = None,
    hsc: list[float] | None = None,
    abs_flag: bool = False,
    cp: InputPathType | None = None,
    spike: float | None = None,
    fwhm: float | None = None,
    sum2: InputPathType | None = None,
    dim_surf_flag: bool = False,
    ctab: InputPathType | None = None,
    runner: Runner | None = None,
) -> MriVolsynthOutputs:
    """
    mri_volsynth

    Synthesizes a volume with specified geometry and probability distribution function.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_volid: Output volume path id and format
        template: Template volume id
        nframes: Override template number of frames
        offset_flag: Use template as intensity offset
        offset_mid_flag: Use middle frame of template as intensity offset
        curv: Save output as curvature, uses lh.thickness as template. Requires\
            subject and hemisphere.
        dim: Specify dimensionality nc nr ns nf
        res: Voxel resolution dc dr ds df (df is TR, in msec)
        vox_size: Change template voxel resolution and dimension dc dr ds
        tr: Time between frames in msec
        cdircos: Column cosine direction x, y, z
        rdircos: Row cosine direction x, y, z
        sdircos: Slice cosine direction x, y, z
        c_ras: RAS coordinates of 'center' voxel c_r c_a c_s
        p0: First voxel coordinates p0r p0a p0s
        precision: Precision of the output (e.g., float)
        seed: Seed for the random number generator
        seedfile: Write seed value to this file
        pdf: Probability distribution function (e.g., gaussian, uniform, const)
        bb: Bounding box c r s dc dr ds (In=ValA, Out=ValB)
        gmean: Mean for the gaussian distribution
        gstd: Standard deviation for the gaussian distribution
        delta_crsf: Delta's col, row, slice, and frame coordinates
        delta_val: Delta value
        delta_val_off: Delta background value
        grid: Grid dimensions dcol, drow, dslice
        dof: Degrees of freedom for t and chi2 distributions
        dof_num: Numerator degrees of freedom for F distribution
        dof_den: Denominator degrees of freedom for F distribution
        rescale_flag: Rescale z, t, F, or chi2 after smoothing
        val_a: Set ValA
        val_b: Set ValB
        vox_radius: Radius in voxels for sphere
        mm_radius: Radius in mm for sphere
        sphere_center: Sphere center coordinates column, row, slice
        hsc: Multiply each frame by a random number between min and max
        abs_flag: Compute absolute value
        cp: Set control point voxels to 1
        spike: Set all values at a given time point to 1e9
        fwhm: Smooth by Full Width at Half Maximum (FWHM) in mm
        sum2: Save sum of volume squared into specified file
        dim_surf_flag: Set dimension to nvertices x 1 x 1
        ctab: Embed color table
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_volsynth_params(
        output_volid=output_volid,
        template=template,
        nframes=nframes,
        offset_flag=offset_flag,
        offset_mid_flag=offset_mid_flag,
        curv=curv,
        dim=dim,
        res=res,
        vox_size=vox_size,
        tr=tr,
        cdircos=cdircos,
        rdircos=rdircos,
        sdircos=sdircos,
        c_ras=c_ras,
        p0=p0,
        precision=precision,
        seed=seed,
        seedfile=seedfile,
        pdf=pdf,
        bb=bb,
        gmean=gmean,
        gstd=gstd,
        delta_crsf=delta_crsf,
        delta_val=delta_val,
        delta_val_off=delta_val_off,
        grid=grid,
        dof=dof,
        dof_num=dof_num,
        dof_den=dof_den,
        rescale_flag=rescale_flag,
        val_a=val_a,
        val_b=val_b,
        vox_radius=vox_radius,
        mm_radius=mm_radius,
        sphere_center=sphere_center,
        hsc=hsc,
        abs_flag=abs_flag,
        cp=cp,
        spike=spike,
        fwhm=fwhm,
        sum2=sum2,
        dim_surf_flag=dim_surf_flag,
        ctab=ctab,
    )
    return mri_volsynth_execute(params, runner)

__all__ = [
    "MriVolsynth",
    "MriVolsynthOutputs",
    "MRI_VOLSYNTH_METADATA",
    "mri_volsynth_cargs",
    "mri_volsynth_outputs",
    "mri_volsynth_params",
    "mri_volsynth_execute",
    "mri_volsynth_validate",
    "mri_volsynth",
]