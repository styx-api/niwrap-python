# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WMSASEG_METADATA = Metadata(
    id="wmsaseg",
    name="wmsaseg",
    package="freesurfer",
)

Wmsaseg = typing.TypedDict(
    "Wmsaseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/wmsaseg"]],
        "subject": str,
        "source_orig": typing.NotRequired[str],
        "source_long": typing.NotRequired[bool],
        "output_subdir": typing.NotRequired[str],
        "gca_file": typing.NotRequired[InputPathType],
        "no_reg": typing.NotRequired[bool],
        "no_canorm": typing.NotRequired[bool],
        "init_spm": typing.NotRequired[bool],
        "reg_only": typing.NotRequired[bool],
        "halo1": typing.NotRequired[bool],
        "halo2": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class WmsasegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    t1_canorm: OutputPathType | None
    """CA normalized T1 output"""
    wmsa_lta: OutputPathType | None
    """Linear transform to average space"""

def wmsaseg_params(
    subject: str,
    source_orig: str | None = None,
    source_long: bool = False,
    output_subdir: str | None = None,
    gca_file: InputPathType | None = None,
    no_reg: bool = False,
    no_canorm: bool = False,
    init_spm: bool = False,
    reg_only: bool = False,
    halo1: bool = False,
    halo2: bool = False,
) -> Wmsaseg:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        source_orig: Use T2 and PD images from original subject
        source_long: Use T2 and PD images from longitudinal subject
        output_subdir: Output subdirectory name (default is wmsa)
        gca_file: GCA file path
        no_reg: Do not register mode to anatomical image
        no_canorm: Do not run mri_ca_normalize
        init_spm: Initialize SPM (default is FSL)
        reg_only: Only perform registration
        halo1: Halo 1 option
        halo2: Halo 2 option

    Returns:
        Parameter dictionary.
    """
    params: Wmsaseg = {
        "@type": "freesurfer/wmsaseg",
        "subject": subject,
        "source_long": source_long,
        "no_reg": no_reg,
        "no_canorm": no_canorm,
        "init_spm": init_spm,
        "reg_only": reg_only,
        "halo1": halo1,
        "halo2": halo2,
    }
    if source_orig is not None:
        params["source_orig"] = source_orig
    if output_subdir is not None:
        params["output_subdir"] = output_subdir
    if gca_file is not None:
        params["gca_file"] = gca_file
    return params

def wmsaseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Wmsaseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("source_orig", None) is not None:
        if not isinstance(params["source_orig"], str):
            raise StyxValidationError(f'`source_orig` has the wrong type: Received `{type(params["source_orig"])}` expected `str`')
    if params.get("source_long", None) is not None:
        if not isinstance(params["source_long"], bool):
            raise StyxValidationError(f'`source_long` has the wrong type: Received `{type(params["source_long"])}` expected `bool`')
    if params.get("output_subdir", None) is not None:
        if not isinstance(params["output_subdir"], str):
            raise StyxValidationError(f'`output_subdir` has the wrong type: Received `{type(params["output_subdir"])}` expected `str`')
    if params.get("gca_file", None) is not None:
        if not isinstance(params["gca_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')
    if params.get("no_reg", None) is not None:
        if not isinstance(params["no_reg"], bool):
            raise StyxValidationError(f'`no_reg` has the wrong type: Received `{type(params["no_reg"])}` expected `bool`')
    if params.get("no_canorm", None) is not None:
        if not isinstance(params["no_canorm"], bool):
            raise StyxValidationError(f'`no_canorm` has the wrong type: Received `{type(params["no_canorm"])}` expected `bool`')
    if params.get("init_spm", None) is not None:
        if not isinstance(params["init_spm"], bool):
            raise StyxValidationError(f'`init_spm` has the wrong type: Received `{type(params["init_spm"])}` expected `bool`')
    if params.get("reg_only", None) is not None:
        if not isinstance(params["reg_only"], bool):
            raise StyxValidationError(f'`reg_only` has the wrong type: Received `{type(params["reg_only"])}` expected `bool`')
    if params.get("halo1", None) is not None:
        if not isinstance(params["halo1"], bool):
            raise StyxValidationError(f'`halo1` has the wrong type: Received `{type(params["halo1"])}` expected `bool`')
    if params.get("halo2", None) is not None:
        if not isinstance(params["halo2"], bool):
            raise StyxValidationError(f'`halo2` has the wrong type: Received `{type(params["halo2"])}` expected `bool`')

def wmsaseg_cargs(params: Wmsaseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wmsaseg")
    cargs.append("-s")
    cargs.append(params["subject"])
    v_0 = params.get("source_orig")
    if v_0 is not None:
        cargs.append("--s+orig")
        cargs.append(v_0)
    if params.get("source_long"):
        cargs.append("--s+long")
    v_1 = params.get("output_subdir")
    if v_1 is not None:
        cargs.append("--sub")
        cargs.append(v_1)
    v_2 = params.get("gca_file")
    if v_2 is not None:
        cargs.append("--gca")
        cargs.append(execution.input_file(v_2))
    if params.get("no_reg"):
        cargs.append("--no-reg")
    if params.get("no_canorm"):
        cargs.append("--no-canorm")
    if params.get("init_spm"):
        cargs.append("--init-spm")
    if params.get("reg_only"):
        cargs.append("--reg-only")
    if params.get("halo1"):
        cargs.append("--halo1")
    if params.get("halo2"):
        cargs.append("--halo2")
    return cargs

def wmsaseg_outputs(params: Wmsaseg, execution: Execution) -> WmsasegOutputs:
    """Build the WmsasegOutputs object for this tool."""
    t1_canorm_v: OutputPathType | None = None
    wmsa_lta_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_subdir")
    if __v0 is not None:
        t1_canorm_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/T1.canorm.mgz')
    __v1 = params.get("output_subdir")
    if __v1 is not None:
        wmsa_lta_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/wmsa.lta')
    return WmsasegOutputs(
        root=root_v,
        t1_canorm=t1_canorm_v,
        wmsa_lta=wmsa_lta_v,
    )

def wmsaseg_execute(params: Wmsaseg, runner: Runner | None = None) -> WmsasegOutputs:
    """
    wmsaseg

    White Matter Hyperintensity Segmentation Tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    wmsaseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WMSASEG_METADATA)
    execution.params(params)
    args = wmsaseg_cargs(params, execution)
    out = wmsaseg_outputs(params, execution)
    execution.run(args)
    return out

def wmsaseg(
    subject: str,
    source_orig: str | None = None,
    source_long: bool = False,
    output_subdir: str | None = None,
    gca_file: InputPathType | None = None,
    no_reg: bool = False,
    no_canorm: bool = False,
    init_spm: bool = False,
    reg_only: bool = False,
    halo1: bool = False,
    halo2: bool = False,
    runner: Runner | None = None,
) -> WmsasegOutputs:
    """
    wmsaseg

    White Matter Hyperintensity Segmentation Tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        source_orig: Use T2 and PD images from original subject
        source_long: Use T2 and PD images from longitudinal subject
        output_subdir: Output subdirectory name (default is wmsa)
        gca_file: GCA file path
        no_reg: Do not register mode to anatomical image
        no_canorm: Do not run mri_ca_normalize
        init_spm: Initialize SPM (default is FSL)
        reg_only: Only perform registration
        halo1: Halo 1 option
        halo2: Halo 2 option
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = wmsaseg_params(
        subject=subject,
        source_orig=source_orig,
        source_long=source_long,
        output_subdir=output_subdir,
        gca_file=gca_file,
        no_reg=no_reg,
        no_canorm=no_canorm,
        init_spm=init_spm,
        reg_only=reg_only,
        halo1=halo1,
        halo2=halo2,
    )
    return wmsaseg_execute(params, runner)

__all__ = [
    "Wmsaseg",
    "WmsasegOutputs",
    "WMSASEG_METADATA",
    "wmsaseg_cargs",
    "wmsaseg_outputs",
    "wmsaseg_params",
    "wmsaseg_execute",
    "wmsaseg_validate",
    "wmsaseg",
]