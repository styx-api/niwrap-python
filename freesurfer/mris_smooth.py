# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SMOOTH_METADATA = Metadata(
    id="mris_smooth",
    name="mris_smooth",
    package="freesurfer",
)

MrisSmooth = typing.TypedDict(
    "MrisSmooth",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_smooth"]],
        "input_surface": InputPathType,
        "output_surface": str,
        "average_iters": typing.NotRequired[float],
        "smoothing_iters": typing.NotRequired[float],
        "no_write": typing.NotRequired[bool],
        "curvature_name": typing.NotRequired[str],
        "area_name": typing.NotRequired[str],
        "gaussian_params": typing.NotRequired[list[float]],
        "normalize_area": typing.NotRequired[bool],
        "momentum": typing.NotRequired[float],
        "snapshot_interval": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisSmoothOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface_file: OutputPathType
    """Output smoothed surface file."""
    curvature_file: OutputPathType
    """Output curvature file (if written)."""
    area_file: OutputPathType
    """Output area file (if written)."""

def mris_smooth_params(
    input_surface: InputPathType,
    output_surface: str,
    average_iters: float | None = None,
    smoothing_iters: float | None = None,
    no_write: bool = False,
    curvature_name: str | None = None,
    area_name: str | None = None,
    gaussian_params: list[float] | None = None,
    normalize_area: bool = False,
    momentum: float | None = None,
    snapshot_interval: float | None = None,
) -> MrisSmooth:
    """
    Build parameters.

    Args:
        input_surface: Input surface file for smoothing.
        output_surface: Output surface file after smoothing.
        average_iters: Specify number of curvature averaging iterations (default is\
            10).
        smoothing_iters: Specify number of smoothing iterations (default is 10).
        no_write: Disable writing of curvature and area estimates.
        curvature_name: Write curvature to a specified file name (default 'curv').
        area_name: Write area to a specified file name (default 'area').
        gaussian_params: Use Gaussian curvature smoothing with specified norm and\
            steps.
        normalize_area: Normalize area after smoothing.
        momentum: Set momentum value.
        snapshot_interval: Write snapshot every specified number of iterations.

    Returns:
        Parameter dictionary.
    """
    params: MrisSmooth = {
        "@type": "freesurfer/mris_smooth",
        "input_surface": input_surface,
        "output_surface": output_surface,
        "no_write": no_write,
        "normalize_area": normalize_area,
    }
    if average_iters is not None:
        params["average_iters"] = average_iters
    if smoothing_iters is not None:
        params["smoothing_iters"] = smoothing_iters
    if curvature_name is not None:
        params["curvature_name"] = curvature_name
    if area_name is not None:
        params["area_name"] = area_name
    if gaussian_params is not None:
        params["gaussian_params"] = gaussian_params
    if momentum is not None:
        params["momentum"] = momentum
    if snapshot_interval is not None:
        params["snapshot_interval"] = snapshot_interval
    return params

def mris_smooth_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSmooth."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("average_iters", None) is not None:
        if not isinstance(params["average_iters"], (float, int)):
            raise StyxValidationError(f'`average_iters` has the wrong type: Received `{type(params["average_iters"])}` expected `float`')
    if params.get("smoothing_iters", None) is not None:
        if not isinstance(params["smoothing_iters"], (float, int)):
            raise StyxValidationError(f'`smoothing_iters` has the wrong type: Received `{type(params["smoothing_iters"])}` expected `float`')
    if params.get("no_write", None) is not None:
        if not isinstance(params["no_write"], bool):
            raise StyxValidationError(f'`no_write` has the wrong type: Received `{type(params["no_write"])}` expected `bool`')
    if params.get("curvature_name", None) is not None:
        if not isinstance(params["curvature_name"], str):
            raise StyxValidationError(f'`curvature_name` has the wrong type: Received `{type(params["curvature_name"])}` expected `str`')
    if params.get("area_name", None) is not None:
        if not isinstance(params["area_name"], str):
            raise StyxValidationError(f'`area_name` has the wrong type: Received `{type(params["area_name"])}` expected `str`')
    if params.get("gaussian_params", None) is not None:
        if not isinstance(params["gaussian_params"], list):
            raise StyxValidationError(f'`gaussian_params` has the wrong type: Received `{type(params["gaussian_params"])}` expected `list[float]`')
        if not (2 <= len(params["gaussian_params"]) <= 2):
            raise StyxValidationError("Parameter `gaussian_params` must contain between 2 and 2 elements (inclusive)")
        for e in params["gaussian_params"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`gaussian_params` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("normalize_area", None) is not None:
        if not isinstance(params["normalize_area"], bool):
            raise StyxValidationError(f'`normalize_area` has the wrong type: Received `{type(params["normalize_area"])}` expected `bool`')
    if params.get("momentum", None) is not None:
        if not isinstance(params["momentum"], (float, int)):
            raise StyxValidationError(f'`momentum` has the wrong type: Received `{type(params["momentum"])}` expected `float`')
    if params.get("snapshot_interval", None) is not None:
        if not isinstance(params["snapshot_interval"], (float, int)):
            raise StyxValidationError(f'`snapshot_interval` has the wrong type: Received `{type(params["snapshot_interval"])}` expected `float`')

def mris_smooth_cargs(params: MrisSmooth, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_smooth")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_surface"])
    v_0 = params.get("average_iters")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    v_1 = params.get("smoothing_iters")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    if params.get("no_write"):
        cargs.append("-nw")
    v_2 = params.get("curvature_name")
    if v_2 is not None:
        cargs.append("-c")
        cargs.append(v_2)
    v_3 = params.get("area_name")
    if v_3 is not None:
        cargs.append("-b")
        cargs.append(v_3)
    v_4 = params.get("gaussian_params")
    if v_4 is not None:
        cargs.append("-g")
        for item0 in v_4:
            cargs.append(str(item0))
    if params.get("normalize_area"):
        cargs.append("-area")
    v_5 = params.get("momentum")
    if v_5 is not None:
        cargs.append("-m")
        cargs.append(str(v_5))
    v_6 = params.get("snapshot_interval")
    if v_6 is not None:
        cargs.append("-w")
        cargs.append(str(v_6))
    return cargs

def mris_smooth_outputs(params: MrisSmooth, execution: Execution) -> MrisSmoothOutputs:
    """Build the MrisSmoothOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_file_v: OutputPathType = execution.output_file(params["output_surface"])
    curvature_file_v: OutputPathType = execution.output_file("${OUTPUT_SURFACE}_curvature")
    area_file_v: OutputPathType = execution.output_file("${OUTPUT_SURFACE}_area")
    return MrisSmoothOutputs(
        root=root_v,
        output_surface_file=output_surface_file_v,
        curvature_file=curvature_file_v,
        area_file=area_file_v,
    )

def mris_smooth_execute(params: MrisSmooth, runner: Runner | None = None) -> MrisSmoothOutputs:
    """
    mris_smooth

    This program smooths the tessellation of a cortical surface and writes out the mean curvature and area files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_smooth_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SMOOTH_METADATA)
    execution.params(params)
    args = mris_smooth_cargs(params, execution)
    out = mris_smooth_outputs(params, execution)
    execution.run(args)
    return out

def mris_smooth(
    input_surface: InputPathType,
    output_surface: str,
    average_iters: float | None = None,
    smoothing_iters: float | None = None,
    no_write: bool = False,
    curvature_name: str | None = None,
    area_name: str | None = None,
    gaussian_params: list[float] | None = None,
    normalize_area: bool = False,
    momentum: float | None = None,
    snapshot_interval: float | None = None,
    runner: Runner | None = None,
) -> MrisSmoothOutputs:
    """
    mris_smooth

    This program smooths the tessellation of a cortical surface and writes out the mean curvature and area files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file for smoothing.
        output_surface: Output surface file after smoothing.
        average_iters: Specify number of curvature averaging iterations (default is\
            10).
        smoothing_iters: Specify number of smoothing iterations (default is 10).
        no_write: Disable writing of curvature and area estimates.
        curvature_name: Write curvature to a specified file name (default 'curv').
        area_name: Write area to a specified file name (default 'area').
        gaussian_params: Use Gaussian curvature smoothing with specified norm and\
            steps.
        normalize_area: Normalize area after smoothing.
        momentum: Set momentum value.
        snapshot_interval: Write snapshot every specified number of iterations.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_smooth_params(
        input_surface=input_surface,
        output_surface=output_surface,
        average_iters=average_iters,
        smoothing_iters=smoothing_iters,
        no_write=no_write,
        curvature_name=curvature_name,
        area_name=area_name,
        gaussian_params=gaussian_params,
        normalize_area=normalize_area,
        momentum=momentum,
        snapshot_interval=snapshot_interval,
    )
    return mris_smooth_execute(params, runner)

__all__ = [
    "MrisSmooth",
    "MrisSmoothOutputs",
    "MRIS_SMOOTH_METADATA",
    "mris_smooth_cargs",
    "mris_smooth_outputs",
    "mris_smooth_params",
    "mris_smooth_execute",
    "mris_smooth_validate",
    "mris_smooth",
]