# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EASYWARP_METADATA = Metadata(
    id="mri_easywarp",
    name="mri_easywarp",
    package="freesurfer",
)

MriEasywarp = typing.TypedDict(
    "MriEasywarp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_easywarp"]],
        "input_image": InputPathType,
        "output_image": str,
        "deformation_field": typing.NotRequired[InputPathType],
        "nearest_neighbor": typing.NotRequired[bool],
        "num_threads": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriEasywarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_deformed_image: OutputPathType
    """Output deformed image"""

def mri_easywarp_params(
    input_image: InputPathType,
    output_image: str,
    deformation_field: InputPathType | None = None,
    nearest_neighbor: bool = False,
    num_threads: float | None = None,
) -> MriEasywarp:
    """
    Build parameters.

    Args:
        input_image: Input image
        output_image: Output (deformed) image
        deformation_field: Deformation field
        nearest_neighbor: Use nearest neighbor (rather than linear) interpolation
        num_threads: Number of cores to be used. Default is 1. You can use -1 to\
            use all available cores

    Returns:
        Parameter dictionary.
    """
    params: MriEasywarp = {
        "@type": "freesurfer/mri_easywarp",
        "input_image": input_image,
        "output_image": output_image,
        "nearest_neighbor": nearest_neighbor,
    }
    if deformation_field is not None:
        params["deformation_field"] = deformation_field
    if num_threads is not None:
        params["num_threads"] = num_threads
    return params

def mri_easywarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriEasywarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("deformation_field", None) is not None:
        if not isinstance(params["deformation_field"], (pathlib.Path, str)):
            raise StyxValidationError(f'`deformation_field` has the wrong type: Received `{type(params["deformation_field"])}` expected `InputPathType`')
    if params.get("nearest_neighbor", None) is not None:
        if not isinstance(params["nearest_neighbor"], bool):
            raise StyxValidationError(f'`nearest_neighbor` has the wrong type: Received `{type(params["nearest_neighbor"])}` expected `bool`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], (float, int)):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `float`')

def mri_easywarp_cargs(params: MriEasywarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_easywarp")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("--o")
    cargs.append(params["output_image"])
    v_0 = params.get("deformation_field")
    if v_0 is not None:
        cargs.append("--field")
        cargs.append(execution.input_file(v_0))
    if params.get("nearest_neighbor"):
        cargs.append("--nearest")
    v_1 = params.get("num_threads")
    if v_1 is not None:
        cargs.append("--threads")
        cargs.append(str(v_1))
    return cargs

def mri_easywarp_outputs(params: MriEasywarp, execution: Execution) -> MriEasywarpOutputs:
    """Build the MriEasywarpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_deformed_image_v: OutputPathType = execution.output_file(f'{params["output_image"]}.nii.gz')
    return MriEasywarpOutputs(
        root=root_v,
        output_deformed_image=output_deformed_image_v,
    )

def mri_easywarp_execute(params: MriEasywarp, runner: Runner | None = None) -> MriEasywarpOutputs:
    """
    mri_easywarp

    EasyReg: deep learning registration simple and easy

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_easywarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EASYWARP_METADATA)
    execution.params(params)
    args = mri_easywarp_cargs(params, execution)
    out = mri_easywarp_outputs(params, execution)
    execution.run(args)
    return out

def mri_easywarp(
    input_image: InputPathType,
    output_image: str,
    deformation_field: InputPathType | None = None,
    nearest_neighbor: bool = False,
    num_threads: float | None = None,
    runner: Runner | None = None,
) -> MriEasywarpOutputs:
    """
    mri_easywarp

    EasyReg: deep learning registration simple and easy

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input image
        output_image: Output (deformed) image
        deformation_field: Deformation field
        nearest_neighbor: Use nearest neighbor (rather than linear) interpolation
        num_threads: Number of cores to be used. Default is 1. You can use -1 to\
            use all available cores
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_easywarp_params(
        input_image=input_image,
        output_image=output_image,
        deformation_field=deformation_field,
        nearest_neighbor=nearest_neighbor,
        num_threads=num_threads,
    )
    return mri_easywarp_execute(params, runner)

__all__ = [
    "MriEasywarp",
    "MriEasywarpOutputs",
    "MRI_EASYWARP_METADATA",
    "mri_easywarp_cargs",
    "mri_easywarp_outputs",
    "mri_easywarp_params",
    "mri_easywarp_execute",
    "mri_easywarp_validate",
    "mri_easywarp",
]