# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DEFECT2SEG_METADATA = Metadata(
    id="defect2seg",
    name="defect2seg",
    package="freesurfer",
)

Defect2seg = typing.TypedDict(
    "Defect2seg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/defect2seg"]],
        "output_seg": str,
        "template": InputPathType,
        "left_hemisphere": typing.NotRequired[list[str]],
        "right_hemisphere": typing.NotRequired[list[str]],
        "subject": typing.NotRequired[str],
        "lh_only": typing.NotRequired[bool],
        "rh_only": typing.NotRequired[bool],
        "cortex": typing.NotRequired[bool],
        "no_cortex": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Defect2segOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_segmentation: OutputPathType
    """Resulting defect segmentation volume"""

def defect2seg_params(
    output_seg: str,
    template: InputPathType,
    left_hemisphere: list[str] | None = None,
    right_hemisphere: list[str] | None = None,
    subject: str | None = None,
    lh_only: bool = False,
    rh_only: bool = False,
    cortex: bool = False,
    no_cortex: bool = False,
) -> Defect2seg:
    """
    Build parameters.

    Args:
        output_seg: Output segmentation volume
        template: Template for segmentation
        left_hemisphere: Left hemisphere inputs: surface, defect labels, pointset,\
            and offset
        right_hemisphere: Right hemisphere inputs: surface, defect labels,\
            pointset, and offset
        subject: Subject identifier, sets default values for other parameters
        lh_only: Consider only left hemisphere defects
        rh_only: Consider only right hemisphere defects
        cortex: Constrain defects to within cortex
        no_cortex: Allow defects outside of cortex

    Returns:
        Parameter dictionary.
    """
    params: Defect2seg = {
        "@type": "freesurfer/defect2seg",
        "output_seg": output_seg,
        "template": template,
        "lh_only": lh_only,
        "rh_only": rh_only,
        "cortex": cortex,
        "no_cortex": no_cortex,
    }
    if left_hemisphere is not None:
        params["left_hemisphere"] = left_hemisphere
    if right_hemisphere is not None:
        params["right_hemisphere"] = right_hemisphere
    if subject is not None:
        params["subject"] = subject
    return params

def defect2seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Defect2seg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_seg", None) is None:
        raise StyxValidationError("`output_seg` must not be None")
    if not isinstance(params["output_seg"], str):
        raise StyxValidationError(f'`output_seg` has the wrong type: Received `{type(params["output_seg"])}` expected `str`')
    if params.get("template", None) is None:
        raise StyxValidationError("`template` must not be None")
    if not isinstance(params["template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("left_hemisphere", None) is not None:
        if not isinstance(params["left_hemisphere"], list):
            raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(params["left_hemisphere"])}` expected `list[str]`')
        if len(params["left_hemisphere"]) > 4:
            raise StyxValidationError("Parameter `left_hemisphere` must contain at most 4 elements")
        for e in params["left_hemisphere"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("right_hemisphere", None) is not None:
        if not isinstance(params["right_hemisphere"], list):
            raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(params["right_hemisphere"])}` expected `list[str]`')
        if len(params["right_hemisphere"]) > 4:
            raise StyxValidationError("Parameter `right_hemisphere` must contain at most 4 elements")
        for e_2 in params["right_hemisphere"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("lh_only", None) is not None:
        if not isinstance(params["lh_only"], bool):
            raise StyxValidationError(f'`lh_only` has the wrong type: Received `{type(params["lh_only"])}` expected `bool`')
    if params.get("rh_only", None) is not None:
        if not isinstance(params["rh_only"], bool):
            raise StyxValidationError(f'`rh_only` has the wrong type: Received `{type(params["rh_only"])}` expected `bool`')
    if params.get("cortex", None) is not None:
        if not isinstance(params["cortex"], bool):
            raise StyxValidationError(f'`cortex` has the wrong type: Received `{type(params["cortex"])}` expected `bool`')
    if params.get("no_cortex", None) is not None:
        if not isinstance(params["no_cortex"], bool):
            raise StyxValidationError(f'`no_cortex` has the wrong type: Received `{type(params["no_cortex"])}` expected `bool`')

def defect2seg_cargs(params: Defect2seg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("defect2seg")
    cargs.append("--o")
    cargs.append(params["output_seg"])
    cargs.append("--t")
    cargs.append(execution.input_file(params["template"]))
    v_0 = params.get("left_hemisphere")
    if v_0 is not None:
        cargs.append("--lh")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("right_hemisphere")
    if v_1 is not None:
        cargs.append("--rh")
        for item1 in v_1:
            cargs.append(item1)
    v_2 = params.get("subject")
    if v_2 is not None:
        cargs.append("--s")
        cargs.append(v_2)
    if params.get("lh_only"):
        cargs.append("--lh-only")
    if params.get("rh_only"):
        cargs.append("--rh-only")
    if params.get("cortex"):
        cargs.append("--cortex")
    if params.get("no_cortex"):
        cargs.append("--no-cortex")
    return cargs

def defect2seg_outputs(params: Defect2seg, execution: Execution) -> Defect2segOutputs:
    """Build the Defect2segOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_segmentation_v: OutputPathType = execution.output_file(params["output_seg"])
    return Defect2segOutputs(
        root=root_v,
        output_segmentation=output_segmentation_v,
    )

def defect2seg_execute(params: Defect2seg, runner: Runner | None = None) -> Defect2segOutputs:
    """
    defect2seg

    Converts surface defect labels into a segmentation volume and pointsets

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    defect2seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DEFECT2SEG_METADATA)
    execution.params(params)
    args = defect2seg_cargs(params, execution)
    out = defect2seg_outputs(params, execution)
    execution.run(args)
    return out

def defect2seg(
    output_seg: str,
    template: InputPathType,
    left_hemisphere: list[str] | None = None,
    right_hemisphere: list[str] | None = None,
    subject: str | None = None,
    lh_only: bool = False,
    rh_only: bool = False,
    cortex: bool = False,
    no_cortex: bool = False,
    runner: Runner | None = None,
) -> Defect2segOutputs:
    """
    defect2seg

    Converts surface defect labels into a segmentation volume and pointsets

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_seg: Output segmentation volume
        template: Template for segmentation
        left_hemisphere: Left hemisphere inputs: surface, defect labels, pointset,\
            and offset
        right_hemisphere: Right hemisphere inputs: surface, defect labels,\
            pointset, and offset
        subject: Subject identifier, sets default values for other parameters
        lh_only: Consider only left hemisphere defects
        rh_only: Consider only right hemisphere defects
        cortex: Constrain defects to within cortex
        no_cortex: Allow defects outside of cortex
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = defect2seg_params(
        output_seg=output_seg,
        template=template,
        left_hemisphere=left_hemisphere,
        right_hemisphere=right_hemisphere,
        subject=subject,
        lh_only=lh_only,
        rh_only=rh_only,
        cortex=cortex,
        no_cortex=no_cortex,
    )
    return defect2seg_execute(params, runner)

__all__ = [
    "Defect2seg",
    "Defect2segOutputs",
    "DEFECT2SEG_METADATA",
    "defect2seg_cargs",
    "defect2seg_outputs",
    "defect2seg_params",
    "defect2seg_execute",
    "defect2seg_validate",
    "defect2seg",
]