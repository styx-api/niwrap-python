# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_RESAMPLE_METADATA = Metadata(
    id="mris_resample",
    name="mris_resample",
    package="freesurfer",
)

MrisResample = typing.TypedDict(
    "MrisResample",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_resample"]],
        "atlas_reg": InputPathType,
        "subject_reg": InputPathType,
        "subject_surf": InputPathType,
        "output": str,
        "annot_in": typing.NotRequired[InputPathType],
        "annot_out": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resampled_surface_output: OutputPathType
    """Resampled surface output"""
    resampled_annotation_output: OutputPathType | None
    """Output annotation for the resampled atlas"""

def mris_resample_params(
    atlas_reg: InputPathType,
    subject_reg: InputPathType,
    subject_surf: InputPathType,
    output: str,
    annot_in: InputPathType | None = None,
    annot_out: str | None = None,
) -> MrisResample:
    """
    Build parameters.

    Args:
        atlas_reg: Atlas spherical registration file.
        subject_reg: Subject spherical registration file.
        subject_surf: Subject actual surface.
        output: Output resampled surface.
        annot_in: Input annotation (for the subject). If present, output annotation\
            must be present as well.
        annot_out: Output annotation (for the resampled atlas). If present, input\
            annotation must be present as well.

    Returns:
        Parameter dictionary.
    """
    params: MrisResample = {
        "@type": "freesurfer/mris_resample",
        "atlas_reg": atlas_reg,
        "subject_reg": subject_reg,
        "subject_surf": subject_surf,
        "output": output,
    }
    if annot_in is not None:
        params["annot_in"] = annot_in
    if annot_out is not None:
        params["annot_out"] = annot_out
    return params

def mris_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("atlas_reg", None) is None:
        raise StyxValidationError("`atlas_reg` must not be None")
    if not isinstance(params["atlas_reg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_reg` has the wrong type: Received `{type(params["atlas_reg"])}` expected `InputPathType`')
    if params.get("subject_reg", None) is None:
        raise StyxValidationError("`subject_reg` must not be None")
    if not isinstance(params["subject_reg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subject_reg` has the wrong type: Received `{type(params["subject_reg"])}` expected `InputPathType`')
    if params.get("subject_surf", None) is None:
        raise StyxValidationError("`subject_surf` must not be None")
    if not isinstance(params["subject_surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subject_surf` has the wrong type: Received `{type(params["subject_surf"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("annot_in", None) is not None:
        if not isinstance(params["annot_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`annot_in` has the wrong type: Received `{type(params["annot_in"])}` expected `InputPathType`')
    if params.get("annot_out", None) is not None:
        if not isinstance(params["annot_out"], str):
            raise StyxValidationError(f'`annot_out` has the wrong type: Received `{type(params["annot_out"])}` expected `str`')

def mris_resample_cargs(params: MrisResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_resample")
    cargs.append("-atlas_reg")
    cargs.append(execution.input_file(params["atlas_reg"]))
    cargs.append("-subject_reg")
    cargs.append(execution.input_file(params["subject_reg"]))
    cargs.append("-subject_surf")
    cargs.append(execution.input_file(params["subject_surf"]))
    cargs.append("-out")
    cargs.append(params["output"])
    v_0 = params.get("annot_in")
    if v_0 is not None:
        cargs.append("--annot_in")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("annot_out")
    if v_1 is not None:
        cargs.append("--annot_out")
        cargs.append(v_1)
    return cargs

def mris_resample_outputs(params: MrisResample, execution: Execution) -> MrisResampleOutputs:
    """Build the MrisResampleOutputs object for this tool."""
    resampled_annotation_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    resampled_surface_output_v: OutputPathType = execution.output_file(params["output"])
    __v0 = params.get("annot_out")
    if __v0 is not None:
        resampled_annotation_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrisResampleOutputs(
        root=root_v,
        resampled_surface_output=resampled_surface_output_v,
        resampled_annotation_output=resampled_annotation_output_v,
    )

def mris_resample_execute(params: MrisResample, runner: Runner | None = None) -> MrisResampleOutputs:
    """
    mris_resample

    Resample cortical surfaces in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_RESAMPLE_METADATA)
    execution.params(params)
    args = mris_resample_cargs(params, execution)
    out = mris_resample_outputs(params, execution)
    execution.run(args)
    return out

def mris_resample(
    atlas_reg: InputPathType,
    subject_reg: InputPathType,
    subject_surf: InputPathType,
    output: str,
    annot_in: InputPathType | None = None,
    annot_out: str | None = None,
    runner: Runner | None = None,
) -> MrisResampleOutputs:
    """
    mris_resample

    Resample cortical surfaces in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        atlas_reg: Atlas spherical registration file.
        subject_reg: Subject spherical registration file.
        subject_surf: Subject actual surface.
        output: Output resampled surface.
        annot_in: Input annotation (for the subject). If present, output annotation\
            must be present as well.
        annot_out: Output annotation (for the resampled atlas). If present, input\
            annotation must be present as well.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_resample_params(
        atlas_reg=atlas_reg,
        subject_reg=subject_reg,
        subject_surf=subject_surf,
        output=output,
        annot_in=annot_in,
        annot_out=annot_out,
    )
    return mris_resample_execute(params, runner)

__all__ = [
    "MrisResample",
    "MrisResampleOutputs",
    "MRIS_RESAMPLE_METADATA",
    "mris_resample_cargs",
    "mris_resample_outputs",
    "mris_resample_params",
    "mris_resample_execute",
    "mris_resample_validate",
    "mris_resample",
]