# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

JKGCATRAIN_METADATA = Metadata(
    id="jkgcatrain",
    name="jkgcatrain",
    package="freesurfer",
)

Jkgcatrain = typing.TypedDict(
    "Jkgcatrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/jkgcatrain"]],
        "gca_directory": str,
        "iteration_number": typing.NotRequired[float],
        "num_threads": typing.NotRequired[float],
        "no_submit": typing.NotRequired[bool],
        "mail_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class JkgcatrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def jkgcatrain_params(
    gca_directory: str,
    iteration_number: float | None = None,
    num_threads: float | None = None,
    no_submit: bool = False,
    mail_flag: bool = False,
) -> Jkgcatrain:
    """
    Build parameters.

    Args:
        gca_directory: Output directory from gcatrain.
        iteration_number: Iteration number (usually 2).
        num_threads: Number of threads to use.
        no_submit: Run serially, do not use pbsubmit.
        mail_flag: Mail to user when jobs are pbsubmitted or finished.

    Returns:
        Parameter dictionary.
    """
    params: Jkgcatrain = {
        "@type": "freesurfer/jkgcatrain",
        "gca_directory": gca_directory,
        "no_submit": no_submit,
        "mail_flag": mail_flag,
    }
    if iteration_number is not None:
        params["iteration_number"] = iteration_number
    if num_threads is not None:
        params["num_threads"] = num_threads
    return params

def jkgcatrain_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Jkgcatrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gca_directory", None) is None:
        raise StyxValidationError("`gca_directory` must not be None")
    if not isinstance(params["gca_directory"], str):
        raise StyxValidationError(f'`gca_directory` has the wrong type: Received `{type(params["gca_directory"])}` expected `str`')
    if params.get("iteration_number", None) is not None:
        if not isinstance(params["iteration_number"], (float, int)):
            raise StyxValidationError(f'`iteration_number` has the wrong type: Received `{type(params["iteration_number"])}` expected `float`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], (float, int)):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `float`')
    if params.get("no_submit", None) is not None:
        if not isinstance(params["no_submit"], bool):
            raise StyxValidationError(f'`no_submit` has the wrong type: Received `{type(params["no_submit"])}` expected `bool`')
    if params.get("mail_flag", None) is not None:
        if not isinstance(params["mail_flag"], bool):
            raise StyxValidationError(f'`mail_flag` has the wrong type: Received `{type(params["mail_flag"])}` expected `bool`')

def jkgcatrain_cargs(params: Jkgcatrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("jkgcatrain")
    cargs.append("--g")
    cargs.append(params["gca_directory"])
    v_0 = params.get("iteration_number")
    if v_0 is not None:
        cargs.append("--iter")
        cargs.append(str(v_0))
    v_1 = params.get("num_threads")
    if v_1 is not None:
        cargs.append("--nthreads")
        cargs.append(str(v_1))
    if params.get("no_submit"):
        cargs.append("--no-submit")
    if params.get("mail_flag"):
        cargs.append("--pb-m")
    return cargs

def jkgcatrain_outputs(params: Jkgcatrain, execution: Execution) -> JkgcatrainOutputs:
    """Build the JkgcatrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return JkgcatrainOutputs(
        root=root_v,
    )

def jkgcatrain_execute(params: Jkgcatrain, runner: Runner | None = None) -> JkgcatrainOutputs:
    """
    jkgcatrain

    Jackknife training of GCA using existing output from gcatrain.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    jkgcatrain_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(JKGCATRAIN_METADATA)
    execution.params(params)
    args = jkgcatrain_cargs(params, execution)
    out = jkgcatrain_outputs(params, execution)
    execution.run(args)
    return out

def jkgcatrain(
    gca_directory: str,
    iteration_number: float | None = None,
    num_threads: float | None = None,
    no_submit: bool = False,
    mail_flag: bool = False,
    runner: Runner | None = None,
) -> JkgcatrainOutputs:
    """
    jkgcatrain

    Jackknife training of GCA using existing output from gcatrain.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        gca_directory: Output directory from gcatrain.
        iteration_number: Iteration number (usually 2).
        num_threads: Number of threads to use.
        no_submit: Run serially, do not use pbsubmit.
        mail_flag: Mail to user when jobs are pbsubmitted or finished.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = jkgcatrain_params(
        gca_directory=gca_directory,
        iteration_number=iteration_number,
        num_threads=num_threads,
        no_submit=no_submit,
        mail_flag=mail_flag,
    )
    return jkgcatrain_execute(params, runner)

__all__ = [
    "Jkgcatrain",
    "JkgcatrainOutputs",
    "JKGCATRAIN_METADATA",
    "jkgcatrain_cargs",
    "jkgcatrain_outputs",
    "jkgcatrain_params",
    "jkgcatrain_execute",
    "jkgcatrain_validate",
    "jkgcatrain",
]