# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACKSDCMDIR_METADATA = Metadata(
    id="unpacksdcmdir",
    name="unpacksdcmdir",
    package="freesurfer",
)

Unpacksdcmdir = typing.TypedDict(
    "Unpacksdcmdir",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpacksdcmdir"]],
        "input_directory": str,
        "output_directory": str,
    },
)

@dataclasses.dataclass
class UnpacksdcmdirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    unpacked_data: OutputPathType
    """The directory that will contain the unpacked data."""

def unpacksdcmdir_params(
    input_directory: str,
    output_directory: str,
) -> Unpacksdcmdir:
    """
    Build parameters.

    Args:
        input_directory: The directory containing SD card data to be unpacked.
        output_directory: The output directory where unpacked data will be stored.

    Returns:
        Parameter dictionary.
    """
    params: Unpacksdcmdir = {
        "@type": "freesurfer/unpacksdcmdir",
        "input_directory": input_directory,
        "output_directory": output_directory,
    }
    return params

def unpacksdcmdir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Unpacksdcmdir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_directory", None) is None:
        raise StyxValidationError("`input_directory` must not be None")
    if not isinstance(params["input_directory"], str):
        raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def unpacksdcmdir_cargs(params: Unpacksdcmdir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpacksdcmdir")
    cargs.append(params["input_directory"])
    cargs.append(params["output_directory"])
    return cargs

def unpacksdcmdir_outputs(params: Unpacksdcmdir, execution: Execution) -> UnpacksdcmdirOutputs:
    """Build the UnpacksdcmdirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    unpacked_data_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/unpacked_data')
    return UnpacksdcmdirOutputs(
        root=root_v,
        unpacked_data=unpacked_data_v,
    )

def unpacksdcmdir_execute(params: Unpacksdcmdir, runner: Runner | None = None) -> UnpacksdcmdirOutputs:
    """
    unpacksdcmdir

    A tool for unpacking SD card directories, typically used in neuroimaging workflows. It seems to have a dependency on Tcl, as indicated by the error messages.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpacksdcmdir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACKSDCMDIR_METADATA)
    execution.params(params)
    args = unpacksdcmdir_cargs(params, execution)
    out = unpacksdcmdir_outputs(params, execution)
    execution.run(args)
    return out

def unpacksdcmdir(
    input_directory: str,
    output_directory: str,
    runner: Runner | None = None,
) -> UnpacksdcmdirOutputs:
    """
    unpacksdcmdir

    A tool for unpacking SD card directories, typically used in neuroimaging workflows. It seems to have a dependency on Tcl, as indicated by the error messages.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_directory: The directory containing SD card data to be unpacked.
        output_directory: The output directory where unpacked data will be stored.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpacksdcmdir_params(
        input_directory=input_directory,
        output_directory=output_directory,
    )
    return unpacksdcmdir_execute(params, runner)

__all__ = [
    "Unpacksdcmdir",
    "UnpacksdcmdirOutputs",
    "UNPACKSDCMDIR_METADATA",
    "unpacksdcmdir_cargs",
    "unpacksdcmdir_outputs",
    "unpacksdcmdir_params",
    "unpacksdcmdir_execute",
    "unpacksdcmdir_validate",
    "unpacksdcmdir",
]