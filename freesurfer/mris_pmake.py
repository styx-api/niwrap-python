# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_PMAKE_METADATA = Metadata(
    id="mris_pmake",
    name="mris_pmake",
    package="freesurfer",
)

MrisPmake = typing.TypedDict(
    "MrisPmake",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_pmake"]],
        "options_file": typing.NotRequired[str],
        "working_dir": typing.NotRequired[str],
        "listen_mode": typing.NotRequired[bool],
        "listen_on_port": typing.NotRequired[float],
        "subject": str,
        "hemisphere": str,
        "surface0": typing.NotRequired[str],
        "surface1": typing.NotRequired[str],
        "curve0": typing.NotRequired[str],
        "curve1": typing.NotRequired[str],
        "use_abs_curvs": typing.NotRequired[bool],
        "mpm_prog": typing.NotRequired[str],
        "mpm_args": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisPmakeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_pmake_params(
    subject: str,
    hemisphere: str,
    options_file: str | None = None,
    working_dir: str | None = None,
    listen_mode: bool = False,
    listen_on_port: float | None = None,
    surface0: str | None = None,
    surface1: str | None = None,
    curve0: str | None = None,
    curve1: str | None = None,
    use_abs_curvs: bool = False,
    mpm_prog: str | None = None,
    mpm_args: str | None = None,
) -> MrisPmake:
    """
    Build parameters.

    Args:
        subject: Set the subject to <subj>.
        hemisphere: The hemisphere to process.
        options_file: The main configuration file specifying the startup run-time\
            behaviour.
        working_dir: The working directory.
        listen_mode: Start in LISTEN mode without calculating a path.
        listen_on_port: Create the server port on specified port and do nothing\
            else.
        surface0: The main mesh surface to read.
        surface1: The aux mesh surface to read.
        curve0: The main curvature function maps.
        curve1: The aux curvature function maps.
        use_abs_curvs: Use absolute values on each curvature map.
        mpm_prog: The mpmProg to run.
        mpm_args: Arguments for the specified mpmProg.

    Returns:
        Parameter dictionary.
    """
    params: MrisPmake = {
        "@type": "freesurfer/mris_pmake",
        "subject": subject,
        "hemisphere": hemisphere,
        "listen_mode": listen_mode,
        "use_abs_curvs": use_abs_curvs,
    }
    if options_file is not None:
        params["options_file"] = options_file
    if working_dir is not None:
        params["working_dir"] = working_dir
    if listen_on_port is not None:
        params["listen_on_port"] = listen_on_port
    if surface0 is not None:
        params["surface0"] = surface0
    if surface1 is not None:
        params["surface1"] = surface1
    if curve0 is not None:
        params["curve0"] = curve0
    if curve1 is not None:
        params["curve1"] = curve1
    if mpm_prog is not None:
        params["mpm_prog"] = mpm_prog
    if mpm_args is not None:
        params["mpm_args"] = mpm_args
    return params

def mris_pmake_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisPmake."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("options_file", None) is not None:
        if not isinstance(params["options_file"], str):
            raise StyxValidationError(f'`options_file` has the wrong type: Received `{type(params["options_file"])}` expected `str`')
    if params.get("working_dir", None) is not None:
        if not isinstance(params["working_dir"], str):
            raise StyxValidationError(f'`working_dir` has the wrong type: Received `{type(params["working_dir"])}` expected `str`')
    if params.get("listen_mode", None) is not None:
        if not isinstance(params["listen_mode"], bool):
            raise StyxValidationError(f'`listen_mode` has the wrong type: Received `{type(params["listen_mode"])}` expected `bool`')
    if params.get("listen_on_port", None) is not None:
        if not isinstance(params["listen_on_port"], (float, int)):
            raise StyxValidationError(f'`listen_on_port` has the wrong type: Received `{type(params["listen_on_port"])}` expected `float`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("surface0", None) is not None:
        if not isinstance(params["surface0"], str):
            raise StyxValidationError(f'`surface0` has the wrong type: Received `{type(params["surface0"])}` expected `str`')
    if params.get("surface1", None) is not None:
        if not isinstance(params["surface1"], str):
            raise StyxValidationError(f'`surface1` has the wrong type: Received `{type(params["surface1"])}` expected `str`')
    if params.get("curve0", None) is not None:
        if not isinstance(params["curve0"], str):
            raise StyxValidationError(f'`curve0` has the wrong type: Received `{type(params["curve0"])}` expected `str`')
    if params.get("curve1", None) is not None:
        if not isinstance(params["curve1"], str):
            raise StyxValidationError(f'`curve1` has the wrong type: Received `{type(params["curve1"])}` expected `str`')
    if params.get("use_abs_curvs", None) is not None:
        if not isinstance(params["use_abs_curvs"], bool):
            raise StyxValidationError(f'`use_abs_curvs` has the wrong type: Received `{type(params["use_abs_curvs"])}` expected `bool`')
    if params.get("mpm_prog", None) is not None:
        if not isinstance(params["mpm_prog"], str):
            raise StyxValidationError(f'`mpm_prog` has the wrong type: Received `{type(params["mpm_prog"])}` expected `str`')
    if params.get("mpm_args", None) is not None:
        if not isinstance(params["mpm_args"], str):
            raise StyxValidationError(f'`mpm_args` has the wrong type: Received `{type(params["mpm_args"])}` expected `str`')

def mris_pmake_cargs(params: MrisPmake, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_pmake")
    v_0 = params.get("options_file")
    if v_0 is not None:
        cargs.append("--optionsFile")
        cargs.append(v_0)
    v_1 = params.get("working_dir")
    if v_1 is not None:
        cargs.append("--dir")
        cargs.append(v_1)
    if params.get("listen_mode"):
        cargs.append("--listen")
    v_2 = params.get("listen_on_port")
    if v_2 is not None:
        cargs.append("--listenOnPort")
        cargs.append(str(v_2))
    cargs.append("--subject")
    cargs.append(params["subject"])
    cargs.append("--hemi")
    cargs.append(params["hemisphere"])
    v_3 = params.get("surface0")
    if v_3 is not None:
        cargs.append("--surface0")
        cargs.append(v_3)
    v_4 = params.get("surface1")
    if v_4 is not None:
        cargs.append("--surface1")
        cargs.append(v_4)
    v_5 = params.get("curve0")
    if v_5 is not None:
        cargs.append("--curve0")
        cargs.append(v_5)
    v_6 = params.get("curve1")
    if v_6 is not None:
        cargs.append("--curve1")
        cargs.append(v_6)
    if params.get("use_abs_curvs"):
        cargs.append("--useAbsCurvs")
    v_7 = params.get("mpm_prog")
    if v_7 is not None:
        cargs.append("--mpmProg")
        cargs.append(v_7)
    v_8 = params.get("mpm_args")
    if v_8 is not None:
        cargs.append("--mpmArgs")
        cargs.append(v_8)
    return cargs

def mris_pmake_outputs(params: MrisPmake, execution: Execution) -> MrisPmakeOutputs:
    """Build the MrisPmakeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisPmakeOutputs(
        root=root_v,
    )

def mris_pmake_execute(params: MrisPmake, runner: Runner | None = None) -> MrisPmakeOutputs:
    """
    mris_pmake

    Calculates paths and related costs on FreeSurfer surfaces based on an edge cost and Dijkstra's algorithm.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_pmake_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_PMAKE_METADATA)
    execution.params(params)
    args = mris_pmake_cargs(params, execution)
    out = mris_pmake_outputs(params, execution)
    execution.run(args)
    return out

def mris_pmake(
    subject: str,
    hemisphere: str,
    options_file: str | None = None,
    working_dir: str | None = None,
    listen_mode: bool = False,
    listen_on_port: float | None = None,
    surface0: str | None = None,
    surface1: str | None = None,
    curve0: str | None = None,
    curve1: str | None = None,
    use_abs_curvs: bool = False,
    mpm_prog: str | None = None,
    mpm_args: str | None = None,
    runner: Runner | None = None,
) -> MrisPmakeOutputs:
    """
    mris_pmake

    Calculates paths and related costs on FreeSurfer surfaces based on an edge cost and Dijkstra's algorithm.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Set the subject to <subj>.
        hemisphere: The hemisphere to process.
        options_file: The main configuration file specifying the startup run-time\
            behaviour.
        working_dir: The working directory.
        listen_mode: Start in LISTEN mode without calculating a path.
        listen_on_port: Create the server port on specified port and do nothing\
            else.
        surface0: The main mesh surface to read.
        surface1: The aux mesh surface to read.
        curve0: The main curvature function maps.
        curve1: The aux curvature function maps.
        use_abs_curvs: Use absolute values on each curvature map.
        mpm_prog: The mpmProg to run.
        mpm_args: Arguments for the specified mpmProg.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_pmake_params(
        subject=subject,
        hemisphere=hemisphere,
        options_file=options_file,
        working_dir=working_dir,
        listen_mode=listen_mode,
        listen_on_port=listen_on_port,
        surface0=surface0,
        surface1=surface1,
        curve0=curve0,
        curve1=curve1,
        use_abs_curvs=use_abs_curvs,
        mpm_prog=mpm_prog,
        mpm_args=mpm_args,
    )
    return mris_pmake_execute(params, runner)

__all__ = [
    "MrisPmake",
    "MrisPmakeOutputs",
    "MRIS_PMAKE_METADATA",
    "mris_pmake_cargs",
    "mris_pmake_outputs",
    "mris_pmake_params",
    "mris_pmake_execute",
    "mris_pmake_validate",
    "mris_pmake",
]