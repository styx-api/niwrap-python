# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LABEL2VOL_METADATA = Metadata(
    id="mri_label2vol",
    name="mri_label2vol",
    package="freesurfer",
)

MriLabel2vol = typing.TypedDict(
    "MriLabel2vol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_label2vol"]],
        "labels": typing.NotRequired[list[str]],
        "annotation": typing.NotRequired[InputPathType],
        "segmentation": typing.NotRequired[InputPathType],
        "template": InputPathType,
        "registration": typing.NotRequired[InputPathType],
        "identity_flag": typing.NotRequired[bool],
        "fill_threshold": typing.NotRequired[float],
        "label_vox_vol": typing.NotRequired[float],
        "projection": typing.NotRequired[str],
        "subject": typing.NotRequired[str],
        "hemisphere": typing.NotRequired[str],
        "output_volume": str,
        "hits_volume": typing.NotRequired[str],
        "label_stat_volume": typing.NotRequired[str],
        "stat_threshold": typing.NotRequired[float],
        "offset": typing.NotRequired[float],
        "defects": typing.NotRequired[str],
        "native_vox2ras_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriLabel2volOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vol: OutputPathType
    """The output volume where each voxel has the label it's assigned to."""
    hits_vol: OutputPathType | None
    """The hit volume file showing number of hits per voxel."""
    label_stat_vol: OutputPathType | None
    """The label stat volume file."""

def mri_label2vol_params(
    template: InputPathType,
    output_volume: str,
    labels: list[str] | None = None,
    annotation: InputPathType | None = None,
    segmentation: InputPathType | None = None,
    registration: InputPathType | None = None,
    identity_flag: bool = False,
    fill_threshold: float | None = None,
    label_vox_vol: float | None = None,
    projection: str | None = None,
    subject: str | None = None,
    hemisphere: str | None = None,
    hits_volume: str | None = None,
    label_stat_volume: str | None = None,
    stat_threshold: float | None = None,
    offset: float | None = None,
    defects: str | None = None,
    native_vox2ras_flag: bool = False,
) -> MriLabel2vol:
    """
    Build parameters.

    Args:
        template: Template volume; the output will have the same size and geometry.
        output_volume: Output volume in which each voxel will have the number of\
            the label to which it is assigned.
        labels: Enter the name of the label file. For multiple labels, use multiple\
            --label flags.
        annotation: Surface annotation file. Use this to input annotations\
            directly.
        segmentation: Path to input segmentation.
        registration: tkregister-style registration matrix mapping LabelXYZ to\
            VolXYZ.
        identity_flag: Use the identity matrix as the registration.
        fill_threshold: Threshold for voxel fill; a value between 0 and 1.
        label_vox_vol: Volume of each label point; default is 1mm³.
        projection: Project the label along the surface normal. Type can be abs or\
            frac.
        subject: FREESURFER subject identifier; needed with --proj.
        hemisphere: Hemisphere to use for --proj or --annot. Legal values are lh\
            and rh.
        hits_volume: Hit volume, a multi-frame volume with one frame per label\
            showing the number of hits per voxel.
        label_stat_volume: Map the label stats field into the volume.
        stat_threshold: Only use label point where stat > thresh.
        offset: Add offset to the segmentation numbers.
        defects: Creates a segmentation volume of the surface defects.
        native_vox2ras_flag: Use native voxel-to-RAS transform instead of\
            tkregister-style.

    Returns:
        Parameter dictionary.
    """
    params: MriLabel2vol = {
        "@type": "freesurfer/mri_label2vol",
        "template": template,
        "output_volume": output_volume,
        "identity_flag": identity_flag,
        "native_vox2ras_flag": native_vox2ras_flag,
    }
    if labels is not None:
        params["labels"] = labels
    if annotation is not None:
        params["annotation"] = annotation
    if segmentation is not None:
        params["segmentation"] = segmentation
    if registration is not None:
        params["registration"] = registration
    if fill_threshold is not None:
        params["fill_threshold"] = fill_threshold
    if label_vox_vol is not None:
        params["label_vox_vol"] = label_vox_vol
    if projection is not None:
        params["projection"] = projection
    if subject is not None:
        params["subject"] = subject
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    if hits_volume is not None:
        params["hits_volume"] = hits_volume
    if label_stat_volume is not None:
        params["label_stat_volume"] = label_stat_volume
    if stat_threshold is not None:
        params["stat_threshold"] = stat_threshold
    if offset is not None:
        params["offset"] = offset
    if defects is not None:
        params["defects"] = defects
    return params

def mri_label2vol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLabel2vol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("labels", None) is not None:
        if not isinstance(params["labels"], list):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
        for e in params["labels"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("annotation", None) is not None:
        if not isinstance(params["annotation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`annotation` has the wrong type: Received `{type(params["annotation"])}` expected `InputPathType`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `InputPathType`')
    if params.get("template", None) is None:
        raise StyxValidationError("`template` must not be None")
    if not isinstance(params["template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("registration", None) is not None:
        if not isinstance(params["registration"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registration` has the wrong type: Received `{type(params["registration"])}` expected `InputPathType`')
    if params.get("identity_flag", None) is not None:
        if not isinstance(params["identity_flag"], bool):
            raise StyxValidationError(f'`identity_flag` has the wrong type: Received `{type(params["identity_flag"])}` expected `bool`')
    if params.get("fill_threshold", None) is not None:
        if not isinstance(params["fill_threshold"], (float, int)):
            raise StyxValidationError(f'`fill_threshold` has the wrong type: Received `{type(params["fill_threshold"])}` expected `float`')
        if not (0 <= params["fill_threshold"] <= 1):
            raise StyxValidationError("Parameter `fill_threshold` must be between 0 and 1 (inclusive)")
    if params.get("label_vox_vol", None) is not None:
        if not isinstance(params["label_vox_vol"], (float, int)):
            raise StyxValidationError(f'`label_vox_vol` has the wrong type: Received `{type(params["label_vox_vol"])}` expected `float`')
    if params.get("projection", None) is not None:
        if not isinstance(params["projection"], str):
            raise StyxValidationError(f'`projection` has the wrong type: Received `{type(params["projection"])}` expected `str`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("hits_volume", None) is not None:
        if not isinstance(params["hits_volume"], str):
            raise StyxValidationError(f'`hits_volume` has the wrong type: Received `{type(params["hits_volume"])}` expected `str`')
    if params.get("label_stat_volume", None) is not None:
        if not isinstance(params["label_stat_volume"], str):
            raise StyxValidationError(f'`label_stat_volume` has the wrong type: Received `{type(params["label_stat_volume"])}` expected `str`')
    if params.get("stat_threshold", None) is not None:
        if not isinstance(params["stat_threshold"], (float, int)):
            raise StyxValidationError(f'`stat_threshold` has the wrong type: Received `{type(params["stat_threshold"])}` expected `float`')
    if params.get("offset", None) is not None:
        if not isinstance(params["offset"], (float, int)):
            raise StyxValidationError(f'`offset` has the wrong type: Received `{type(params["offset"])}` expected `float`')
    if params.get("defects", None) is not None:
        if not isinstance(params["defects"], str):
            raise StyxValidationError(f'`defects` has the wrong type: Received `{type(params["defects"])}` expected `str`')
    if params.get("native_vox2ras_flag", None) is not None:
        if not isinstance(params["native_vox2ras_flag"], bool):
            raise StyxValidationError(f'`native_vox2ras_flag` has the wrong type: Received `{type(params["native_vox2ras_flag"])}` expected `bool`')

def mri_label2vol_cargs(params: MriLabel2vol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_label2vol")
    v_0 = params.get("labels")
    if v_0 is not None:
        cargs.append("--label")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("annotation")
    if v_1 is not None:
        cargs.append("--annot")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("segmentation")
    if v_2 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_2))
    cargs.append("--temp")
    cargs.append(execution.input_file(params["template"]))
    v_3 = params.get("registration")
    if v_3 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_3))
    if params.get("identity_flag"):
        cargs.append("--identity")
    v_4 = params.get("fill_threshold")
    if v_4 is not None:
        cargs.append("--fillthresh")
        cargs.append(str(v_4))
    v_5 = params.get("label_vox_vol")
    if v_5 is not None:
        cargs.append("--labvoxvol")
        cargs.append(str(v_5))
    v_6 = params.get("projection")
    if v_6 is not None:
        cargs.append("--proj")
        cargs.append(v_6)
    v_7 = params.get("subject")
    if v_7 is not None:
        cargs.append("--subject")
        cargs.append(v_7)
    v_8 = params.get("hemisphere")
    if v_8 is not None:
        cargs.append("--hemi")
        cargs.append(v_8)
    cargs.append("--o")
    cargs.append(params["output_volume"])
    v_9 = params.get("hits_volume")
    if v_9 is not None:
        cargs.append("--hits")
        cargs.append(v_9)
    v_10 = params.get("label_stat_volume")
    if v_10 is not None:
        cargs.append("--label-stat")
        cargs.append(v_10)
    v_11 = params.get("stat_threshold")
    if v_11 is not None:
        cargs.append("--stat-thresh")
        cargs.append(str(v_11))
    v_12 = params.get("offset")
    if v_12 is not None:
        cargs.append("--offset")
        cargs.append(str(v_12))
    v_13 = params.get("defects")
    if v_13 is not None:
        cargs.append("--defects")
        cargs.append(v_13)
    if params.get("native_vox2ras_flag"):
        cargs.append("--native-vox2ras")
    return cargs

def mri_label2vol_outputs(params: MriLabel2vol, execution: Execution) -> MriLabel2volOutputs:
    """Build the MriLabel2volOutputs object for this tool."""
    hits_vol_v: OutputPathType | None = None
    label_stat_vol_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_vol_v: OutputPathType = execution.output_file(params["output_volume"])
    __v0 = params.get("hits_volume")
    if __v0 is not None:
        hits_vol_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("label_stat_volume")
    if __v1 is not None:
        label_stat_vol_v = execution.output_file((__v1 if __v1 is not None else ""))
    return MriLabel2volOutputs(
        root=root_v,
        output_vol=output_vol_v,
        hits_vol=hits_vol_v,
        label_stat_vol=label_stat_vol_v,
    )

def mri_label2vol_execute(params: MriLabel2vol, runner: Runner | None = None) -> MriLabel2volOutputs:
    """
    mri_label2vol

    Converts a label or a set of labels into a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_label2vol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LABEL2VOL_METADATA)
    execution.params(params)
    args = mri_label2vol_cargs(params, execution)
    out = mri_label2vol_outputs(params, execution)
    execution.run(args)
    return out

def mri_label2vol(
    template: InputPathType,
    output_volume: str,
    labels: list[str] | None = None,
    annotation: InputPathType | None = None,
    segmentation: InputPathType | None = None,
    registration: InputPathType | None = None,
    identity_flag: bool = False,
    fill_threshold: float | None = None,
    label_vox_vol: float | None = None,
    projection: str | None = None,
    subject: str | None = None,
    hemisphere: str | None = None,
    hits_volume: str | None = None,
    label_stat_volume: str | None = None,
    stat_threshold: float | None = None,
    offset: float | None = None,
    defects: str | None = None,
    native_vox2ras_flag: bool = False,
    runner: Runner | None = None,
) -> MriLabel2volOutputs:
    """
    mri_label2vol

    Converts a label or a set of labels into a volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        template: Template volume; the output will have the same size and geometry.
        output_volume: Output volume in which each voxel will have the number of\
            the label to which it is assigned.
        labels: Enter the name of the label file. For multiple labels, use multiple\
            --label flags.
        annotation: Surface annotation file. Use this to input annotations\
            directly.
        segmentation: Path to input segmentation.
        registration: tkregister-style registration matrix mapping LabelXYZ to\
            VolXYZ.
        identity_flag: Use the identity matrix as the registration.
        fill_threshold: Threshold for voxel fill; a value between 0 and 1.
        label_vox_vol: Volume of each label point; default is 1mm³.
        projection: Project the label along the surface normal. Type can be abs or\
            frac.
        subject: FREESURFER subject identifier; needed with --proj.
        hemisphere: Hemisphere to use for --proj or --annot. Legal values are lh\
            and rh.
        hits_volume: Hit volume, a multi-frame volume with one frame per label\
            showing the number of hits per voxel.
        label_stat_volume: Map the label stats field into the volume.
        stat_threshold: Only use label point where stat > thresh.
        offset: Add offset to the segmentation numbers.
        defects: Creates a segmentation volume of the surface defects.
        native_vox2ras_flag: Use native voxel-to-RAS transform instead of\
            tkregister-style.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_label2vol_params(
        template=template,
        output_volume=output_volume,
        labels=labels,
        annotation=annotation,
        segmentation=segmentation,
        registration=registration,
        identity_flag=identity_flag,
        fill_threshold=fill_threshold,
        label_vox_vol=label_vox_vol,
        projection=projection,
        subject=subject,
        hemisphere=hemisphere,
        hits_volume=hits_volume,
        label_stat_volume=label_stat_volume,
        stat_threshold=stat_threshold,
        offset=offset,
        defects=defects,
        native_vox2ras_flag=native_vox2ras_flag,
    )
    return mri_label2vol_execute(params, runner)

__all__ = [
    "MriLabel2vol",
    "MriLabel2volOutputs",
    "MRI_LABEL2VOL_METADATA",
    "mri_label2vol_cargs",
    "mri_label2vol_outputs",
    "mri_label2vol_params",
    "mri_label2vol_execute",
    "mri_label2vol_validate",
    "mri_label2vol",
]