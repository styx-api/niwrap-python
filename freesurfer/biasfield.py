# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BIASFIELD_METADATA = Metadata(
    id="biasfield",
    name="biasfield",
    package="freesurfer",
)

Biasfield = typing.TypedDict(
    "Biasfield",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/biasfield"]],
        "subject": str,
        "tmpdir": typing.NotRequired[str],
        "no_cleanup": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BiasfieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    biasfield_output: OutputPathType
    """Generated bias field in the subject MRI directory"""
    rawavg_cor_norm: OutputPathType
    """The rawavg.mgz in 256^3, 1mm^3 space with the bias field removed"""

def biasfield_params(
    subject: str,
    tmpdir: str | None = None,
    no_cleanup: bool = False,
    help: bool = False,
    debug: bool = False,
    version: bool = False,
) -> Biasfield:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        tmpdir: Temporary directory
        no_cleanup: Prevent cleanup of temporary files
        help: Display help information
        debug: Enable debugging mode
        version: Display script version info

    Returns:
        Parameter dictionary.
    """
    params: Biasfield = {
        "@type": "freesurfer/biasfield",
        "subject": subject,
        "no_cleanup": no_cleanup,
        "help": help,
        "debug": debug,
        "version": version,
    }
    if tmpdir is not None:
        params["tmpdir"] = tmpdir
    return params

def biasfield_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Biasfield."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("tmpdir", None) is not None:
        if not isinstance(params["tmpdir"], str):
            raise StyxValidationError(f'`tmpdir` has the wrong type: Received `{type(params["tmpdir"])}` expected `str`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def biasfield_cargs(params: Biasfield, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("biasfield")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("tmpdir")
    if v_0 is not None:
        cargs.append("--tmp")
        cargs.append(v_0)
    if params.get("no_cleanup"):
        cargs.append("--nocleanup")
    if params.get("help"):
        cargs.append("--help")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def biasfield_outputs(params: Biasfield, execution: Execution) -> BiasfieldOutputs:
    """Build the BiasfieldOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    biasfield_output_v: OutputPathType = execution.output_file("biasfield.mgz")
    rawavg_cor_norm_v: OutputPathType = execution.output_file("rawavg.cor.norm.mgz")
    return BiasfieldOutputs(
        root=root_v,
        biasfield_output=biasfield_output_v,
        rawavg_cor_norm=rawavg_cor_norm_v,
    )

def biasfield_execute(params: Biasfield, runner: Runner | None = None) -> BiasfieldOutputs:
    """
    biasfield

    Computes the bias field by dividing the (unconformed) orig.mgz by the norm.mgz.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    biasfield_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BIASFIELD_METADATA)
    execution.params(params)
    args = biasfield_cargs(params, execution)
    out = biasfield_outputs(params, execution)
    execution.run(args)
    return out

def biasfield(
    subject: str,
    tmpdir: str | None = None,
    no_cleanup: bool = False,
    help: bool = False,
    debug: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> BiasfieldOutputs:
    """
    biasfield

    Computes the bias field by dividing the (unconformed) orig.mgz by the norm.mgz.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        tmpdir: Temporary directory
        no_cleanup: Prevent cleanup of temporary files
        help: Display help information
        debug: Enable debugging mode
        version: Display script version info
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = biasfield_params(
        subject=subject,
        tmpdir=tmpdir,
        no_cleanup=no_cleanup,
        help=help,
        debug=debug,
        version=version,
    )
    return biasfield_execute(params, runner)

__all__ = [
    "Biasfield",
    "BiasfieldOutputs",
    "BIASFIELD_METADATA",
    "biasfield_cargs",
    "biasfield_outputs",
    "biasfield_params",
    "biasfield_execute",
    "biasfield_validate",
    "biasfield",
]