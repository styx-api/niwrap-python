# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IS_SURFACE_METADATA = Metadata(
    id="is-surface",
    name="is-surface",
    package="freesurfer",
)

IsSurface = typing.TypedDict(
    "IsSurface",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/is-surface"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class IsSurfaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def is_surface_params(
    infile: InputPathType,
) -> IsSurface:
    """
    Build parameters.

    Args:
        infile:

    Returns:
        Parameter dictionary.
    """
    params: IsSurface = {
        "@type": "freesurfer/is-surface",
        "infile": infile,
    }
    return params

def is_surface_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IsSurface."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def is_surface_cargs(params: IsSurface, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("is-surface")
    cargs.append("-surface")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def is_surface_outputs(params: IsSurface, execution: Execution) -> IsSurfaceOutputs:
    """Build the IsSurfaceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return IsSurfaceOutputs(
        root=root_v,
    )

def is_surface_execute(params: IsSurface, runner: Runner | None = None) -> IsSurfaceOutputs:
    """
    is-surface

    Determines whether a file is a volume-encoded surface file by examining its dimensions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    is_surface_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IS_SURFACE_METADATA)
    execution.params(params)
    args = is_surface_cargs(params, execution)
    out = is_surface_outputs(params, execution)
    execution.run(args)
    return out

def is_surface(
    infile: InputPathType,
    runner: Runner | None = None,
) -> IsSurfaceOutputs:
    """
    is-surface

    Determines whether a file is a volume-encoded surface file by examining its dimensions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = is_surface_params(
        infile=infile,
    )
    return is_surface_execute(params, runner)

__all__ = [
    "IsSurface",
    "IsSurfaceOutputs",
    "IS_SURFACE_METADATA",
    "is_surface_cargs",
    "is_surface_outputs",
    "is_surface_params",
    "is_surface_execute",
    "is_surface_validate",
    "is_surface",
]