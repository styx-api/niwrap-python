# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CA_LABEL_METADATA = Metadata(
    id="mri_ca_label",
    name="mri_ca_label",
    package="freesurfer",
)

MriCaLabel = typing.TypedDict(
    "MriCaLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_ca_label"]],
        "input_volumes": list[InputPathType],
        "transform_file": InputPathType,
        "gca_file": InputPathType,
        "output_volume": str,
        "cross_sequence": typing.NotRequired[bool],
        "no_gibbs": typing.NotRequired[bool],
        "wm_segmentation": typing.NotRequired[str],
        "conform": typing.NotRequired[bool],
        "topo_dist_thresh": typing.NotRequired[float],
        "topo_volume_thresh1": typing.NotRequired[float],
        "topo_volume_thresh2": typing.NotRequired[float],
        "norm_pd": typing.NotRequired[bool],
        "thin_temporal_lobe": typing.NotRequired[str],
        "debug_voxel": typing.NotRequired[list[float]],
        "debug_node": typing.NotRequired[list[float]],
        "debug_label": typing.NotRequired[float],
        "tr": typing.NotRequired[float],
        "te": typing.NotRequired[float],
        "alpha": typing.NotRequired[float],
        "example": typing.NotRequired[list[InputPathType]],
        "pthresh": typing.NotRequired[float],
        "niter": typing.NotRequired[float],
        "write_probs": typing.NotRequired[str],
        "novar": typing.NotRequired[bool],
        "regularize": typing.NotRequired[float],
        "nohippo": typing.NotRequired[bool],
        "fixed_white_matter": typing.NotRequired[str],
        "mri": typing.NotRequired[InputPathType],
        "histogram_equalization": typing.NotRequired[InputPathType],
        "renorm": typing.NotRequired[InputPathType],
        "flash": typing.NotRequired[bool],
        "flash_params": typing.NotRequired[InputPathType],
        "renormalize": typing.NotRequired[str],
        "set_input_volume": typing.NotRequired[InputPathType],
        "histogram_normalize": typing.NotRequired[bool],
        "mean_filter": typing.NotRequired[float],
        "write_snapshots": typing.NotRequired[str],
        "mask_final_labeling": typing.NotRequired[InputPathType],
        "expand": typing.NotRequired[float],
        "max_iterations": typing.NotRequired[float],
        "filter_labeled_volume": typing.NotRequired[str],
        "longitudinal_processing": typing.NotRequired[str],
        "relabel_unlikely": typing.NotRequired[str],
        "disables_wmsa": typing.NotRequired[bool],
        "fix_ventricle": typing.NotRequired[str],
        "insert_wm_bet_putctx": typing.NotRequired[str],
        "sa_insert_wm_bet_putctx": typing.NotRequired[str],
        "insert_from_seg": typing.NotRequired[str],
        "sa_insert_from_seg": typing.NotRequired[str],
        "cblum_from_seg": typing.NotRequired[str],
        "sa_cblum_from_seg": typing.NotRequired[str],
        "threads": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class MriCaLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vol: OutputPathType
    """Output labeled volume."""

def mri_ca_label_params(
    input_volumes: list[InputPathType],
    transform_file: InputPathType,
    gca_file: InputPathType,
    output_volume: str,
    cross_sequence: bool = False,
    no_gibbs: bool = False,
    wm_segmentation: str | None = None,
    conform: bool = False,
    topo_dist_thresh: float | None = None,
    topo_volume_thresh1: float | None = None,
    topo_volume_thresh2: float | None = None,
    norm_pd: bool = False,
    thin_temporal_lobe: str | None = None,
    debug_voxel: list[float] | None = None,
    debug_node: list[float] | None = None,
    debug_label: float | None = None,
    tr: float | None = None,
    te: float | None = None,
    alpha: float | None = None,
    example: list[InputPathType] | None = None,
    pthresh: float | None = None,
    niter: float | None = None,
    write_probs: str | None = None,
    novar: bool = False,
    regularize: float | None = None,
    nohippo: bool = False,
    fixed_white_matter: str | None = None,
    mri: InputPathType | None = None,
    histogram_equalization: InputPathType | None = None,
    renorm: InputPathType | None = None,
    flash: bool = False,
    flash_params: InputPathType | None = None,
    renormalize: str | None = None,
    set_input_volume: InputPathType | None = None,
    histogram_normalize: bool = False,
    mean_filter: float | None = None,
    write_snapshots: str | None = None,
    mask_final_labeling: InputPathType | None = None,
    expand: float | None = None,
    max_iterations: float | None = None,
    filter_labeled_volume: str | None = None,
    longitudinal_processing: str | None = None,
    relabel_unlikely: str | None = None,
    disables_wmsa: bool = False,
    fix_ventricle: str | None = None,
    insert_wm_bet_putctx: str | None = None,
    sa_insert_wm_bet_putctx: str | None = None,
    insert_from_seg: str | None = None,
    sa_insert_from_seg: str | None = None,
    cblum_from_seg: str | None = None,
    sa_cblum_from_seg: str | None = None,
    threads: int | None = None,
) -> MriCaLabel:
    """
    Build parameters.

    Args:
        input_volumes: Input MRI volumes.
        transform_file: Transform file for the registration.
        gca_file: GCA file for the atlas.
        output_volume: Output labeled volume.
        cross_sequence: Label a volume acquired with sequence different than atlas
        no_gibbs: Disable gibbs priors
        wm_segmentation: Use wm segmentation
        conform: Interpolate volume to be isotropic 1mm^3
        topo_dist_thresh: Ventricle segments distance threshold
        topo_volume_thresh1: First ventricle segments volume threshold
        topo_volume_thresh2: Second ventricle segments volume threshold
        norm_pd: Normalize PD image to GCA means
        thin_temporal_lobe: Use file to label thin temporal lobe
        debug_voxel: Debug voxel coordinates
        debug_node: Debug node coordinates
        debug_label: Debug label
        tr: Set TR in msec
        te: Set TE in msec
        alpha: Set alpha in radians
        example: Use T1 and segmentation as example
        pthresh: P threshold for adaptive renormalization
        niter: Number of iterations for max likelihood
        write_probs: Write label probabilities to filename
        novar: Do not use variance in classification
        regularize: Regularize variance to be sigma+nC(noise)
        nohippo: Do not auto-edit hippocampus
        fixed_white_matter: Use fixed white matter segmentation
        mri: Write most likely MR volume to file
        histogram_equalization: Use histogram equalization from volume
        renorm: Renormalize using predicted intensity values
        flash: Use FLASH forward model to predict intensity values
        flash_params: Use FLASH forward model and tissue params to predict
        renormalize: Renormalize class means iter times after initial label with\
            window of wsize
        set_input_volume: Set input volume
        histogram_normalize: Use GCA to histogram normalize input image
        mean_filter: Mean filter n time to conditional densities
        write_snapshots: Write snapshots of gibbs process every n times to filename
        mask_final_labeling: Use mri_vol to mask final labeling
        expand: Expand
        max_iterations: Set max iterations
        filter_labeled_volume: Filter labeled volume with threshold t
        longitudinal_processing: Longitudinal processing with registrations
        relabel_unlikely: Reclassify unlikely voxels
        disables_wmsa: Disables WMSA labels
        fix_ventricle: Fix underlabeled ventricle
        insert_wm_bet_putctx: Insert WM between putamen and cortex
        sa_insert_wm_bet_putctx: Stand-alone operation to insert WM between putamen\
            and cortex
        insert_from_seg: Insert given indices from segmentation volume
        sa_insert_from_seg: Stand-alone insert given indices from segmentation\
            volume
        cblum_from_seg: Insert indices into segmentation volume with default label\
            set
        sa_cblum_from_seg: Stand-alone operation to insert indices into\
            segmentation with default label set
        threads: Set the number of open mp threads

    Returns:
        Parameter dictionary.
    """
    params: MriCaLabel = {
        "@type": "freesurfer/mri_ca_label",
        "input_volumes": input_volumes,
        "transform_file": transform_file,
        "gca_file": gca_file,
        "output_volume": output_volume,
        "cross_sequence": cross_sequence,
        "no_gibbs": no_gibbs,
        "conform": conform,
        "norm_pd": norm_pd,
        "novar": novar,
        "nohippo": nohippo,
        "flash": flash,
        "histogram_normalize": histogram_normalize,
        "disables_wmsa": disables_wmsa,
    }
    if wm_segmentation is not None:
        params["wm_segmentation"] = wm_segmentation
    if topo_dist_thresh is not None:
        params["topo_dist_thresh"] = topo_dist_thresh
    if topo_volume_thresh1 is not None:
        params["topo_volume_thresh1"] = topo_volume_thresh1
    if topo_volume_thresh2 is not None:
        params["topo_volume_thresh2"] = topo_volume_thresh2
    if thin_temporal_lobe is not None:
        params["thin_temporal_lobe"] = thin_temporal_lobe
    if debug_voxel is not None:
        params["debug_voxel"] = debug_voxel
    if debug_node is not None:
        params["debug_node"] = debug_node
    if debug_label is not None:
        params["debug_label"] = debug_label
    if tr is not None:
        params["tr"] = tr
    if te is not None:
        params["te"] = te
    if alpha is not None:
        params["alpha"] = alpha
    if example is not None:
        params["example"] = example
    if pthresh is not None:
        params["pthresh"] = pthresh
    if niter is not None:
        params["niter"] = niter
    if write_probs is not None:
        params["write_probs"] = write_probs
    if regularize is not None:
        params["regularize"] = regularize
    if fixed_white_matter is not None:
        params["fixed_white_matter"] = fixed_white_matter
    if mri is not None:
        params["mri"] = mri
    if histogram_equalization is not None:
        params["histogram_equalization"] = histogram_equalization
    if renorm is not None:
        params["renorm"] = renorm
    if flash_params is not None:
        params["flash_params"] = flash_params
    if renormalize is not None:
        params["renormalize"] = renormalize
    if set_input_volume is not None:
        params["set_input_volume"] = set_input_volume
    if mean_filter is not None:
        params["mean_filter"] = mean_filter
    if write_snapshots is not None:
        params["write_snapshots"] = write_snapshots
    if mask_final_labeling is not None:
        params["mask_final_labeling"] = mask_final_labeling
    if expand is not None:
        params["expand"] = expand
    if max_iterations is not None:
        params["max_iterations"] = max_iterations
    if filter_labeled_volume is not None:
        params["filter_labeled_volume"] = filter_labeled_volume
    if longitudinal_processing is not None:
        params["longitudinal_processing"] = longitudinal_processing
    if relabel_unlikely is not None:
        params["relabel_unlikely"] = relabel_unlikely
    if fix_ventricle is not None:
        params["fix_ventricle"] = fix_ventricle
    if insert_wm_bet_putctx is not None:
        params["insert_wm_bet_putctx"] = insert_wm_bet_putctx
    if sa_insert_wm_bet_putctx is not None:
        params["sa_insert_wm_bet_putctx"] = sa_insert_wm_bet_putctx
    if insert_from_seg is not None:
        params["insert_from_seg"] = insert_from_seg
    if sa_insert_from_seg is not None:
        params["sa_insert_from_seg"] = sa_insert_from_seg
    if cblum_from_seg is not None:
        params["cblum_from_seg"] = cblum_from_seg
    if sa_cblum_from_seg is not None:
        params["sa_cblum_from_seg"] = sa_cblum_from_seg
    if threads is not None:
        params["threads"] = threads
    return params

def mri_ca_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCaLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volumes", None) is None:
        raise StyxValidationError("`input_volumes` must not be None")
    if not isinstance(params["input_volumes"], list):
        raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(params["input_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')
    if params.get("gca_file", None) is None:
        raise StyxValidationError("`gca_file` must not be None")
    if not isinstance(params["gca_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("cross_sequence", None) is not None:
        if not isinstance(params["cross_sequence"], bool):
            raise StyxValidationError(f'`cross_sequence` has the wrong type: Received `{type(params["cross_sequence"])}` expected `bool`')
    if params.get("no_gibbs", None) is not None:
        if not isinstance(params["no_gibbs"], bool):
            raise StyxValidationError(f'`no_gibbs` has the wrong type: Received `{type(params["no_gibbs"])}` expected `bool`')
    if params.get("wm_segmentation", None) is not None:
        if not isinstance(params["wm_segmentation"], str):
            raise StyxValidationError(f'`wm_segmentation` has the wrong type: Received `{type(params["wm_segmentation"])}` expected `str`')
    if params.get("conform", None) is not None:
        if not isinstance(params["conform"], bool):
            raise StyxValidationError(f'`conform` has the wrong type: Received `{type(params["conform"])}` expected `bool`')
    if params.get("topo_dist_thresh", None) is not None:
        if not isinstance(params["topo_dist_thresh"], (float, int)):
            raise StyxValidationError(f'`topo_dist_thresh` has the wrong type: Received `{type(params["topo_dist_thresh"])}` expected `float`')
    if params.get("topo_volume_thresh1", None) is not None:
        if not isinstance(params["topo_volume_thresh1"], (float, int)):
            raise StyxValidationError(f'`topo_volume_thresh1` has the wrong type: Received `{type(params["topo_volume_thresh1"])}` expected `float`')
    if params.get("topo_volume_thresh2", None) is not None:
        if not isinstance(params["topo_volume_thresh2"], (float, int)):
            raise StyxValidationError(f'`topo_volume_thresh2` has the wrong type: Received `{type(params["topo_volume_thresh2"])}` expected `float`')
    if params.get("norm_pd", None) is not None:
        if not isinstance(params["norm_pd"], bool):
            raise StyxValidationError(f'`norm_pd` has the wrong type: Received `{type(params["norm_pd"])}` expected `bool`')
    if params.get("thin_temporal_lobe", None) is not None:
        if not isinstance(params["thin_temporal_lobe"], str):
            raise StyxValidationError(f'`thin_temporal_lobe` has the wrong type: Received `{type(params["thin_temporal_lobe"])}` expected `str`')
    if params.get("debug_voxel", None) is not None:
        if not isinstance(params["debug_voxel"], list):
            raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(params["debug_voxel"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["debug_voxel"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("debug_node", None) is not None:
        if not isinstance(params["debug_node"], list):
            raise StyxValidationError(f'`debug_node` has the wrong type: Received `{type(params["debug_node"])}` expected `list[float]`')
        if not (3 <= len(params["debug_node"]) <= 3):
            raise StyxValidationError("Parameter `debug_node` must contain between 3 and 3 elements (inclusive)")
        for e_3 in params["debug_node"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`debug_node` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("debug_label", None) is not None:
        if not isinstance(params["debug_label"], (float, int)):
            raise StyxValidationError(f'`debug_label` has the wrong type: Received `{type(params["debug_label"])}` expected `float`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("te", None) is not None:
        if not isinstance(params["te"], (float, int)):
            raise StyxValidationError(f'`te` has the wrong type: Received `{type(params["te"])}` expected `float`')
    if params.get("alpha", None) is not None:
        if not isinstance(params["alpha"], (float, int)):
            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["alpha"])}` expected `float`')
    if params.get("example", None) is not None:
        if not isinstance(params["example"], list):
            raise StyxValidationError(f'`example` has the wrong type: Received `{type(params["example"])}` expected `list[InputPathType]`')
        if not (2 <= len(params["example"]) <= 2):
            raise StyxValidationError("Parameter `example` must contain between 2 and 2 elements (inclusive)")
        for e_4 in params["example"]:
            if not isinstance(e_4, (pathlib.Path, str)):
                raise StyxValidationError(f'`example` has the wrong type: Received `{type(e_4)}` expected `list[InputPathType]`')
    if params.get("pthresh", None) is not None:
        if not isinstance(params["pthresh"], (float, int)):
            raise StyxValidationError(f'`pthresh` has the wrong type: Received `{type(params["pthresh"])}` expected `float`')
    if params.get("niter", None) is not None:
        if not isinstance(params["niter"], (float, int)):
            raise StyxValidationError(f'`niter` has the wrong type: Received `{type(params["niter"])}` expected `float`')
    if params.get("write_probs", None) is not None:
        if not isinstance(params["write_probs"], str):
            raise StyxValidationError(f'`write_probs` has the wrong type: Received `{type(params["write_probs"])}` expected `str`')
    if params.get("novar", None) is not None:
        if not isinstance(params["novar"], bool):
            raise StyxValidationError(f'`novar` has the wrong type: Received `{type(params["novar"])}` expected `bool`')
    if params.get("regularize", None) is not None:
        if not isinstance(params["regularize"], (float, int)):
            raise StyxValidationError(f'`regularize` has the wrong type: Received `{type(params["regularize"])}` expected `float`')
    if params.get("nohippo", None) is not None:
        if not isinstance(params["nohippo"], bool):
            raise StyxValidationError(f'`nohippo` has the wrong type: Received `{type(params["nohippo"])}` expected `bool`')
    if params.get("fixed_white_matter", None) is not None:
        if not isinstance(params["fixed_white_matter"], str):
            raise StyxValidationError(f'`fixed_white_matter` has the wrong type: Received `{type(params["fixed_white_matter"])}` expected `str`')
    if params.get("mri", None) is not None:
        if not isinstance(params["mri"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mri` has the wrong type: Received `{type(params["mri"])}` expected `InputPathType`')
    if params.get("histogram_equalization", None) is not None:
        if not isinstance(params["histogram_equalization"], (pathlib.Path, str)):
            raise StyxValidationError(f'`histogram_equalization` has the wrong type: Received `{type(params["histogram_equalization"])}` expected `InputPathType`')
    if params.get("renorm", None) is not None:
        if not isinstance(params["renorm"], (pathlib.Path, str)):
            raise StyxValidationError(f'`renorm` has the wrong type: Received `{type(params["renorm"])}` expected `InputPathType`')
    if params.get("flash", None) is not None:
        if not isinstance(params["flash"], bool):
            raise StyxValidationError(f'`flash` has the wrong type: Received `{type(params["flash"])}` expected `bool`')
    if params.get("flash_params", None) is not None:
        if not isinstance(params["flash_params"], (pathlib.Path, str)):
            raise StyxValidationError(f'`flash_params` has the wrong type: Received `{type(params["flash_params"])}` expected `InputPathType`')
    if params.get("renormalize", None) is not None:
        if not isinstance(params["renormalize"], str):
            raise StyxValidationError(f'`renormalize` has the wrong type: Received `{type(params["renormalize"])}` expected `str`')
    if params.get("set_input_volume", None) is not None:
        if not isinstance(params["set_input_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`set_input_volume` has the wrong type: Received `{type(params["set_input_volume"])}` expected `InputPathType`')
    if params.get("histogram_normalize", None) is not None:
        if not isinstance(params["histogram_normalize"], bool):
            raise StyxValidationError(f'`histogram_normalize` has the wrong type: Received `{type(params["histogram_normalize"])}` expected `bool`')
    if params.get("mean_filter", None) is not None:
        if not isinstance(params["mean_filter"], (float, int)):
            raise StyxValidationError(f'`mean_filter` has the wrong type: Received `{type(params["mean_filter"])}` expected `float`')
    if params.get("write_snapshots", None) is not None:
        if not isinstance(params["write_snapshots"], str):
            raise StyxValidationError(f'`write_snapshots` has the wrong type: Received `{type(params["write_snapshots"])}` expected `str`')
    if params.get("mask_final_labeling", None) is not None:
        if not isinstance(params["mask_final_labeling"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_final_labeling` has the wrong type: Received `{type(params["mask_final_labeling"])}` expected `InputPathType`')
    if params.get("expand", None) is not None:
        if not isinstance(params["expand"], (float, int)):
            raise StyxValidationError(f'`expand` has the wrong type: Received `{type(params["expand"])}` expected `float`')
    if params.get("max_iterations", None) is not None:
        if not isinstance(params["max_iterations"], (float, int)):
            raise StyxValidationError(f'`max_iterations` has the wrong type: Received `{type(params["max_iterations"])}` expected `float`')
    if params.get("filter_labeled_volume", None) is not None:
        if not isinstance(params["filter_labeled_volume"], str):
            raise StyxValidationError(f'`filter_labeled_volume` has the wrong type: Received `{type(params["filter_labeled_volume"])}` expected `str`')
    if params.get("longitudinal_processing", None) is not None:
        if not isinstance(params["longitudinal_processing"], str):
            raise StyxValidationError(f'`longitudinal_processing` has the wrong type: Received `{type(params["longitudinal_processing"])}` expected `str`')
    if params.get("relabel_unlikely", None) is not None:
        if not isinstance(params["relabel_unlikely"], str):
            raise StyxValidationError(f'`relabel_unlikely` has the wrong type: Received `{type(params["relabel_unlikely"])}` expected `str`')
    if params.get("disables_wmsa", None) is not None:
        if not isinstance(params["disables_wmsa"], bool):
            raise StyxValidationError(f'`disables_wmsa` has the wrong type: Received `{type(params["disables_wmsa"])}` expected `bool`')
    if params.get("fix_ventricle", None) is not None:
        if not isinstance(params["fix_ventricle"], str):
            raise StyxValidationError(f'`fix_ventricle` has the wrong type: Received `{type(params["fix_ventricle"])}` expected `str`')
    if params.get("insert_wm_bet_putctx", None) is not None:
        if not isinstance(params["insert_wm_bet_putctx"], str):
            raise StyxValidationError(f'`insert_wm_bet_putctx` has the wrong type: Received `{type(params["insert_wm_bet_putctx"])}` expected `str`')
    if params.get("sa_insert_wm_bet_putctx", None) is not None:
        if not isinstance(params["sa_insert_wm_bet_putctx"], str):
            raise StyxValidationError(f'`sa_insert_wm_bet_putctx` has the wrong type: Received `{type(params["sa_insert_wm_bet_putctx"])}` expected `str`')
    if params.get("insert_from_seg", None) is not None:
        if not isinstance(params["insert_from_seg"], str):
            raise StyxValidationError(f'`insert_from_seg` has the wrong type: Received `{type(params["insert_from_seg"])}` expected `str`')
    if params.get("sa_insert_from_seg", None) is not None:
        if not isinstance(params["sa_insert_from_seg"], str):
            raise StyxValidationError(f'`sa_insert_from_seg` has the wrong type: Received `{type(params["sa_insert_from_seg"])}` expected `str`')
    if params.get("cblum_from_seg", None) is not None:
        if not isinstance(params["cblum_from_seg"], str):
            raise StyxValidationError(f'`cblum_from_seg` has the wrong type: Received `{type(params["cblum_from_seg"])}` expected `str`')
    if params.get("sa_cblum_from_seg", None) is not None:
        if not isinstance(params["sa_cblum_from_seg"], str):
            raise StyxValidationError(f'`sa_cblum_from_seg` has the wrong type: Received `{type(params["sa_cblum_from_seg"])}` expected `str`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], int):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `int`')

def mri_ca_label_cargs(params: MriCaLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_ca_label")
    for item0 in params["input_volumes"]:
        cargs.append(execution.input_file(item0))
    cargs.append(execution.input_file(params["transform_file"]))
    cargs.append(execution.input_file(params["gca_file"]))
    cargs.append(params["output_volume"])
    if params.get("cross_sequence"):
        cargs.append("-cross-sequence")
    if params.get("no_gibbs"):
        cargs.append("-nogibbs")
    v_0 = params.get("wm_segmentation")
    if v_0 is not None:
        cargs.append("-wm")
        cargs.append(v_0)
    if params.get("conform"):
        cargs.append("-conform")
    v_1 = params.get("topo_dist_thresh")
    if v_1 is not None:
        cargs.append("-topo_dist_thresh")
        cargs.append(str(v_1))
    v_2 = params.get("topo_volume_thresh1")
    if v_2 is not None:
        cargs.append("-topo_volume_thresh1")
        cargs.append(str(v_2))
    v_3 = params.get("topo_volume_thresh2")
    if v_3 is not None:
        cargs.append("-topo_volume_thresh2")
        cargs.append(str(v_3))
    if params.get("norm_pd"):
        cargs.append("-normpd")
    v_4 = params.get("thin_temporal_lobe")
    if v_4 is not None:
        cargs.append("-tl")
        cargs.append(v_4)
    v_5 = params.get("debug_voxel")
    if v_5 is not None:
        cargs.append("-debug_voxel")
        for item1 in v_5:
            cargs.append(str(item1))
    v_6 = params.get("debug_node")
    if v_6 is not None:
        cargs.append("-debug_node")
        for item2 in v_6:
            cargs.append(str(item2))
    v_7 = params.get("debug_label")
    if v_7 is not None:
        cargs.append("-debug_label")
        cargs.append(str(v_7))
    v_8 = params.get("tr")
    if v_8 is not None:
        cargs.append("-tr")
        cargs.append(str(v_8))
    v_9 = params.get("te")
    if v_9 is not None:
        cargs.append("-te")
        cargs.append(str(v_9))
    v_10 = params.get("alpha")
    if v_10 is not None:
        cargs.append("-alpha")
        cargs.append(str(v_10))
    v_11 = params.get("example")
    if v_11 is not None:
        cargs.append("-example")
        for item3 in v_11:
            cargs.append(execution.input_file(item3))
    v_12 = params.get("pthresh")
    if v_12 is not None:
        cargs.append("-pthresh")
        cargs.append(str(v_12))
    v_13 = params.get("niter")
    if v_13 is not None:
        cargs.append("-niter")
        cargs.append(str(v_13))
    v_14 = params.get("write_probs")
    if v_14 is not None:
        cargs.append("-write_probs")
        cargs.append(v_14)
    if params.get("novar"):
        cargs.append("-novar")
    v_15 = params.get("regularize")
    if v_15 is not None:
        cargs.append("-regularize")
        cargs.append(str(v_15))
    if params.get("nohippo"):
        cargs.append("-nohippo")
    v_16 = params.get("fixed_white_matter")
    if v_16 is not None:
        cargs.append("-fwm")
        cargs.append(v_16)
    v_17 = params.get("mri")
    if v_17 is not None:
        cargs.append("-mri")
        cargs.append(execution.input_file(v_17))
    v_18 = params.get("histogram_equalization")
    if v_18 is not None:
        cargs.append("-heq")
        cargs.append(execution.input_file(v_18))
    v_19 = params.get("renorm")
    if v_19 is not None:
        cargs.append("-renorm")
        cargs.append(execution.input_file(v_19))
    if params.get("flash"):
        cargs.append("-flash")
    v_20 = params.get("flash_params")
    if v_20 is not None:
        cargs.append("-flash_params")
        cargs.append(execution.input_file(v_20))
    v_21 = params.get("renormalize")
    if v_21 is not None:
        cargs.append("-renormalize")
        cargs.append(v_21)
    v_22 = params.get("set_input_volume")
    if v_22 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_22))
    if params.get("histogram_normalize"):
        cargs.append("-h")
    v_23 = params.get("mean_filter")
    if v_23 is not None:
        cargs.append("-a")
        cargs.append(str(v_23))
    v_24 = params.get("write_snapshots")
    if v_24 is not None:
        cargs.append("-w")
        cargs.append(v_24)
    v_25 = params.get("mask_final_labeling")
    if v_25 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_25))
    v_26 = params.get("expand")
    if v_26 is not None:
        cargs.append("-e")
        cargs.append(str(v_26))
    v_27 = params.get("max_iterations")
    if v_27 is not None:
        cargs.append("-n")
        cargs.append(str(v_27))
    v_28 = params.get("filter_labeled_volume")
    if v_28 is not None:
        cargs.append("-f")
        cargs.append(v_28)
    v_29 = params.get("longitudinal_processing")
    if v_29 is not None:
        cargs.append("-L")
        cargs.append(v_29)
    v_30 = params.get("relabel_unlikely")
    if v_30 is not None:
        cargs.append("-RELABEL_UNLIKELY")
        cargs.append(v_30)
    if params.get("disables_wmsa"):
        cargs.append("-nowmsa")
    v_31 = params.get("fix_ventricle")
    if v_31 is not None:
        cargs.append("-vent-fix")
        cargs.append(v_31)
    v_32 = params.get("insert_wm_bet_putctx")
    if v_32 is not None:
        cargs.append("-insert-wm-bet-putctx")
        cargs.append(v_32)
    v_33 = params.get("sa_insert_wm_bet_putctx")
    if v_33 is not None:
        cargs.append("-sa-insert-wm-bet-putctx")
        cargs.append(v_33)
    v_34 = params.get("insert_from_seg")
    if v_34 is not None:
        cargs.append("-insert-from-seg")
        cargs.append(v_34)
    v_35 = params.get("sa_insert_from_seg")
    if v_35 is not None:
        cargs.append("-sa-insert-from-seg")
        cargs.append(v_35)
    v_36 = params.get("cblum_from_seg")
    if v_36 is not None:
        cargs.append("-cblum-from-seg")
        cargs.append(v_36)
    v_37 = params.get("sa_cblum_from_seg")
    if v_37 is not None:
        cargs.append("-sa-cblum-from-seg")
        cargs.append(v_37)
    v_38 = params.get("threads")
    if v_38 is not None:
        cargs.append("-threads")
        cargs.append(str(v_38))
    return cargs

def mri_ca_label_outputs(params: MriCaLabel, execution: Execution) -> MriCaLabelOutputs:
    """Build the MriCaLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_vol_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriCaLabelOutputs(
        root=root_v,
        output_vol=output_vol_v,
    )

def mri_ca_label_execute(params: MriCaLabel, runner: Runner | None = None) -> MriCaLabelOutputs:
    """
    mri_ca_label

    MRI cortical annotation labeler using atlas prior (GCA).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_ca_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CA_LABEL_METADATA)
    execution.params(params)
    args = mri_ca_label_cargs(params, execution)
    out = mri_ca_label_outputs(params, execution)
    execution.run(args)
    return out

def mri_ca_label(
    input_volumes: list[InputPathType],
    transform_file: InputPathType,
    gca_file: InputPathType,
    output_volume: str,
    cross_sequence: bool = False,
    no_gibbs: bool = False,
    wm_segmentation: str | None = None,
    conform: bool = False,
    topo_dist_thresh: float | None = None,
    topo_volume_thresh1: float | None = None,
    topo_volume_thresh2: float | None = None,
    norm_pd: bool = False,
    thin_temporal_lobe: str | None = None,
    debug_voxel: list[float] | None = None,
    debug_node: list[float] | None = None,
    debug_label: float | None = None,
    tr: float | None = None,
    te: float | None = None,
    alpha: float | None = None,
    example: list[InputPathType] | None = None,
    pthresh: float | None = None,
    niter: float | None = None,
    write_probs: str | None = None,
    novar: bool = False,
    regularize: float | None = None,
    nohippo: bool = False,
    fixed_white_matter: str | None = None,
    mri: InputPathType | None = None,
    histogram_equalization: InputPathType | None = None,
    renorm: InputPathType | None = None,
    flash: bool = False,
    flash_params: InputPathType | None = None,
    renormalize: str | None = None,
    set_input_volume: InputPathType | None = None,
    histogram_normalize: bool = False,
    mean_filter: float | None = None,
    write_snapshots: str | None = None,
    mask_final_labeling: InputPathType | None = None,
    expand: float | None = None,
    max_iterations: float | None = None,
    filter_labeled_volume: str | None = None,
    longitudinal_processing: str | None = None,
    relabel_unlikely: str | None = None,
    disables_wmsa: bool = False,
    fix_ventricle: str | None = None,
    insert_wm_bet_putctx: str | None = None,
    sa_insert_wm_bet_putctx: str | None = None,
    insert_from_seg: str | None = None,
    sa_insert_from_seg: str | None = None,
    cblum_from_seg: str | None = None,
    sa_cblum_from_seg: str | None = None,
    threads: int | None = None,
    runner: Runner | None = None,
) -> MriCaLabelOutputs:
    """
    mri_ca_label

    MRI cortical annotation labeler using atlas prior (GCA).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volumes: Input MRI volumes.
        transform_file: Transform file for the registration.
        gca_file: GCA file for the atlas.
        output_volume: Output labeled volume.
        cross_sequence: Label a volume acquired with sequence different than atlas
        no_gibbs: Disable gibbs priors
        wm_segmentation: Use wm segmentation
        conform: Interpolate volume to be isotropic 1mm^3
        topo_dist_thresh: Ventricle segments distance threshold
        topo_volume_thresh1: First ventricle segments volume threshold
        topo_volume_thresh2: Second ventricle segments volume threshold
        norm_pd: Normalize PD image to GCA means
        thin_temporal_lobe: Use file to label thin temporal lobe
        debug_voxel: Debug voxel coordinates
        debug_node: Debug node coordinates
        debug_label: Debug label
        tr: Set TR in msec
        te: Set TE in msec
        alpha: Set alpha in radians
        example: Use T1 and segmentation as example
        pthresh: P threshold for adaptive renormalization
        niter: Number of iterations for max likelihood
        write_probs: Write label probabilities to filename
        novar: Do not use variance in classification
        regularize: Regularize variance to be sigma+nC(noise)
        nohippo: Do not auto-edit hippocampus
        fixed_white_matter: Use fixed white matter segmentation
        mri: Write most likely MR volume to file
        histogram_equalization: Use histogram equalization from volume
        renorm: Renormalize using predicted intensity values
        flash: Use FLASH forward model to predict intensity values
        flash_params: Use FLASH forward model and tissue params to predict
        renormalize: Renormalize class means iter times after initial label with\
            window of wsize
        set_input_volume: Set input volume
        histogram_normalize: Use GCA to histogram normalize input image
        mean_filter: Mean filter n time to conditional densities
        write_snapshots: Write snapshots of gibbs process every n times to filename
        mask_final_labeling: Use mri_vol to mask final labeling
        expand: Expand
        max_iterations: Set max iterations
        filter_labeled_volume: Filter labeled volume with threshold t
        longitudinal_processing: Longitudinal processing with registrations
        relabel_unlikely: Reclassify unlikely voxels
        disables_wmsa: Disables WMSA labels
        fix_ventricle: Fix underlabeled ventricle
        insert_wm_bet_putctx: Insert WM between putamen and cortex
        sa_insert_wm_bet_putctx: Stand-alone operation to insert WM between putamen\
            and cortex
        insert_from_seg: Insert given indices from segmentation volume
        sa_insert_from_seg: Stand-alone insert given indices from segmentation\
            volume
        cblum_from_seg: Insert indices into segmentation volume with default label\
            set
        sa_cblum_from_seg: Stand-alone operation to insert indices into\
            segmentation with default label set
        threads: Set the number of open mp threads
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_ca_label_params(
        input_volumes=input_volumes,
        transform_file=transform_file,
        gca_file=gca_file,
        output_volume=output_volume,
        cross_sequence=cross_sequence,
        no_gibbs=no_gibbs,
        wm_segmentation=wm_segmentation,
        conform=conform,
        topo_dist_thresh=topo_dist_thresh,
        topo_volume_thresh1=topo_volume_thresh1,
        topo_volume_thresh2=topo_volume_thresh2,
        norm_pd=norm_pd,
        thin_temporal_lobe=thin_temporal_lobe,
        debug_voxel=debug_voxel,
        debug_node=debug_node,
        debug_label=debug_label,
        tr=tr,
        te=te,
        alpha=alpha,
        example=example,
        pthresh=pthresh,
        niter=niter,
        write_probs=write_probs,
        novar=novar,
        regularize=regularize,
        nohippo=nohippo,
        fixed_white_matter=fixed_white_matter,
        mri=mri,
        histogram_equalization=histogram_equalization,
        renorm=renorm,
        flash=flash,
        flash_params=flash_params,
        renormalize=renormalize,
        set_input_volume=set_input_volume,
        histogram_normalize=histogram_normalize,
        mean_filter=mean_filter,
        write_snapshots=write_snapshots,
        mask_final_labeling=mask_final_labeling,
        expand=expand,
        max_iterations=max_iterations,
        filter_labeled_volume=filter_labeled_volume,
        longitudinal_processing=longitudinal_processing,
        relabel_unlikely=relabel_unlikely,
        disables_wmsa=disables_wmsa,
        fix_ventricle=fix_ventricle,
        insert_wm_bet_putctx=insert_wm_bet_putctx,
        sa_insert_wm_bet_putctx=sa_insert_wm_bet_putctx,
        insert_from_seg=insert_from_seg,
        sa_insert_from_seg=sa_insert_from_seg,
        cblum_from_seg=cblum_from_seg,
        sa_cblum_from_seg=sa_cblum_from_seg,
        threads=threads,
    )
    return mri_ca_label_execute(params, runner)

__all__ = [
    "MriCaLabel",
    "MriCaLabelOutputs",
    "MRI_CA_LABEL_METADATA",
    "mri_ca_label_cargs",
    "mri_ca_label_outputs",
    "mri_ca_label_params",
    "mri_ca_label_execute",
    "mri_ca_label_validate",
    "mri_ca_label",
]