# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XHEMIREG_METADATA = Metadata(
    id="xhemireg",
    name="xhemireg",
    package="freesurfer",
)

Xhemireg = typing.TypedDict(
    "Xhemireg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/xhemireg"]],
        "subject": str,
        "output_dir": typing.NotRequired[str],
        "map_lh": typing.NotRequired[bool],
        "map_rh": typing.NotRequired[bool],
        "perform_reg": typing.NotRequired[bool],
        "tal_compute": typing.NotRequired[bool],
        "no_tal_compute": typing.NotRequired[bool],
        "tal_estimate": typing.NotRequired[bool],
        "no_tal_estimate": typing.NotRequired[bool],
        "gcaprep": typing.NotRequired[str],
        "threads": typing.NotRequired[float],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class XhemiregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def xhemireg_params(
    subject: str,
    output_dir: str | None = None,
    map_lh: bool = False,
    map_rh: bool = False,
    perform_reg: bool = False,
    tal_compute: bool = False,
    no_tal_compute: bool = False,
    tal_estimate: bool = False,
    no_tal_estimate: bool = False,
    gcaprep: str | None = None,
    threads: float | None = None,
    version: bool = False,
    help: bool = False,
) -> Xhemireg:
    """
    Build parameters.

    Args:
        subject: Subject ID for the hemisphere registration process
        output_dir: Output directory for the hemisphere registration
        map_lh: Map from left hemisphere to right hemisphere
        map_rh: Map from right hemisphere to left hemisphere
        perform_reg: Perform registration to create sphere.reg
        tal_compute: Recompute Talairach registration
        no_tal_compute: Do not perform Talairach registration
        tal_estimate: Compute estimate of Talairach registration from unflipped\
            registration
        no_tal_estimate: Do not perform estimation of Talairach registration
        gcaprep: Prepare GCA for training symmetrical GCA atlases
        threads: Number of threads used, applicable with --gcaprep option
        version: Print version and exit
        help: Print help and exit

    Returns:
        Parameter dictionary.
    """
    params: Xhemireg = {
        "@type": "freesurfer/xhemireg",
        "subject": subject,
        "map_lh": map_lh,
        "map_rh": map_rh,
        "perform_reg": perform_reg,
        "tal_compute": tal_compute,
        "no_tal_compute": no_tal_compute,
        "tal_estimate": tal_estimate,
        "no_tal_estimate": no_tal_estimate,
        "version": version,
        "help": help,
    }
    if output_dir is not None:
        params["output_dir"] = output_dir
    if gcaprep is not None:
        params["gcaprep"] = gcaprep
    if threads is not None:
        params["threads"] = threads
    return params

def xhemireg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Xhemireg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("map_lh", None) is not None:
        if not isinstance(params["map_lh"], bool):
            raise StyxValidationError(f'`map_lh` has the wrong type: Received `{type(params["map_lh"])}` expected `bool`')
    if params.get("map_rh", None) is not None:
        if not isinstance(params["map_rh"], bool):
            raise StyxValidationError(f'`map_rh` has the wrong type: Received `{type(params["map_rh"])}` expected `bool`')
    if params.get("perform_reg", None) is not None:
        if not isinstance(params["perform_reg"], bool):
            raise StyxValidationError(f'`perform_reg` has the wrong type: Received `{type(params["perform_reg"])}` expected `bool`')
    if params.get("tal_compute", None) is not None:
        if not isinstance(params["tal_compute"], bool):
            raise StyxValidationError(f'`tal_compute` has the wrong type: Received `{type(params["tal_compute"])}` expected `bool`')
    if params.get("no_tal_compute", None) is not None:
        if not isinstance(params["no_tal_compute"], bool):
            raise StyxValidationError(f'`no_tal_compute` has the wrong type: Received `{type(params["no_tal_compute"])}` expected `bool`')
    if params.get("tal_estimate", None) is not None:
        if not isinstance(params["tal_estimate"], bool):
            raise StyxValidationError(f'`tal_estimate` has the wrong type: Received `{type(params["tal_estimate"])}` expected `bool`')
    if params.get("no_tal_estimate", None) is not None:
        if not isinstance(params["no_tal_estimate"], bool):
            raise StyxValidationError(f'`no_tal_estimate` has the wrong type: Received `{type(params["no_tal_estimate"])}` expected `bool`')
    if params.get("gcaprep", None) is not None:
        if not isinstance(params["gcaprep"], str):
            raise StyxValidationError(f'`gcaprep` has the wrong type: Received `{type(params["gcaprep"])}` expected `str`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def xhemireg_cargs(params: Xhemireg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("xhemireg")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("output_dir")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    if params.get("map_lh"):
        cargs.append("--lh")
    if params.get("map_rh"):
        cargs.append("--rh")
    if params.get("perform_reg"):
        cargs.append("--reg")
    if params.get("tal_compute"):
        cargs.append("--tal-compute")
    if params.get("no_tal_compute"):
        cargs.append("--no-tal-compute")
    if params.get("tal_estimate"):
        cargs.append("--tal-estimate")
    if params.get("no_tal_estimate"):
        cargs.append("--no-tal-estimate")
    v_1 = params.get("gcaprep")
    if v_1 is not None:
        cargs.append("--gcaprep")
        cargs.append(v_1)
    v_2 = params.get("threads")
    if v_2 is not None:
        cargs.append("--threads")
        cargs.append(str(v_2))
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def xhemireg_outputs(params: Xhemireg, execution: Execution) -> XhemiregOutputs:
    """Build the XhemiregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return XhemiregOutputs(
        root=root_v,
    )

def xhemireg_execute(params: Xhemireg, runner: Runner | None = None) -> XhemiregOutputs:
    """
    xhemireg

    Tool for hemisphere registration in neuroimaging.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xhemireg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XHEMIREG_METADATA)
    execution.params(params)
    args = xhemireg_cargs(params, execution)
    out = xhemireg_outputs(params, execution)
    execution.run(args)
    return out

def xhemireg(
    subject: str,
    output_dir: str | None = None,
    map_lh: bool = False,
    map_rh: bool = False,
    perform_reg: bool = False,
    tal_compute: bool = False,
    no_tal_compute: bool = False,
    tal_estimate: bool = False,
    no_tal_estimate: bool = False,
    gcaprep: str | None = None,
    threads: float | None = None,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> XhemiregOutputs:
    """
    xhemireg

    Tool for hemisphere registration in neuroimaging.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject ID for the hemisphere registration process
        output_dir: Output directory for the hemisphere registration
        map_lh: Map from left hemisphere to right hemisphere
        map_rh: Map from right hemisphere to left hemisphere
        perform_reg: Perform registration to create sphere.reg
        tal_compute: Recompute Talairach registration
        no_tal_compute: Do not perform Talairach registration
        tal_estimate: Compute estimate of Talairach registration from unflipped\
            registration
        no_tal_estimate: Do not perform estimation of Talairach registration
        gcaprep: Prepare GCA for training symmetrical GCA atlases
        threads: Number of threads used, applicable with --gcaprep option
        version: Print version and exit
        help: Print help and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xhemireg_params(
        subject=subject,
        output_dir=output_dir,
        map_lh=map_lh,
        map_rh=map_rh,
        perform_reg=perform_reg,
        tal_compute=tal_compute,
        no_tal_compute=no_tal_compute,
        tal_estimate=tal_estimate,
        no_tal_estimate=no_tal_estimate,
        gcaprep=gcaprep,
        threads=threads,
        version=version,
        help=help,
    )
    return xhemireg_execute(params, runner)

__all__ = [
    "Xhemireg",
    "XhemiregOutputs",
    "XHEMIREG_METADATA",
    "xhemireg_cargs",
    "xhemireg_outputs",
    "xhemireg_params",
    "xhemireg_execute",
    "xhemireg_validate",
    "xhemireg",
]