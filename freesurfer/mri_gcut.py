# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GCUT_METADATA = Metadata(
    id="mri_gcut",
    name="mri_gcut",
    package="freesurfer",
)

MriGcut = typing.TypedDict(
    "MriGcut",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gcut"]],
        "wmmask_110": typing.NotRequired[bool],
        "mult_file": typing.NotRequired[InputPathType],
        "threshold_value": typing.NotRequired[float],
        "infile": InputPathType,
        "outfile": str,
    },
)

@dataclasses.dataclass
class MriGcutOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_mask_file: OutputPathType
    """The output file containing the skull-stripped brain volume."""

def mri_gcut_params(
    infile: InputPathType,
    outfile: str,
    wmmask_110: bool = False,
    mult_file: InputPathType | None = None,
    threshold_value: float | None = None,
) -> MriGcut:
    """
    Build parameters.

    Args:
        infile: Input brain volume file, e.g. T1.mgz
        outfile: Output file name, e.g. brainmask.auto.mgz
        wmmask_110: Use voxels with intensity 110 as white matter mask (when\
            applied on T1.mgz, FreeSurfer only)
        mult_file: Intersect the skull-stripped 'in_filename' and an existing\
            skull-stripped volume specified by 'filename', storing the result in\
            'out_filename'.
        threshold_value: Set threshold to value (%) of WM intensity, where the\
            value should be >0 and <1; defaults to 0.40.

    Returns:
        Parameter dictionary.
    """
    params: MriGcut = {
        "@type": "freesurfer/mri_gcut",
        "infile": infile,
        "outfile": outfile,
        "wmmask_110": wmmask_110,
    }
    if mult_file is not None:
        params["mult_file"] = mult_file
    if threshold_value is not None:
        params["threshold_value"] = threshold_value
    return params

def mri_gcut_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGcut."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("wmmask_110", None) is not None:
        if not isinstance(params["wmmask_110"], bool):
            raise StyxValidationError(f'`wmmask_110` has the wrong type: Received `{type(params["wmmask_110"])}` expected `bool`')
    if params.get("mult_file", None) is not None:
        if not isinstance(params["mult_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mult_file` has the wrong type: Received `{type(params["mult_file"])}` expected `InputPathType`')
    if params.get("threshold_value", None) is not None:
        if not isinstance(params["threshold_value"], (float, int)):
            raise StyxValidationError(f'`threshold_value` has the wrong type: Received `{type(params["threshold_value"])}` expected `float`')
        if not (0 <= params["threshold_value"] <= 1):
            raise StyxValidationError("Parameter `threshold_value` must be between 0 and 1 (inclusive)")
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def mri_gcut_cargs(params: MriGcut, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gcut")
    if params.get("wmmask_110"):
        cargs.append("-110")
    v_0 = params.get("mult_file")
    if v_0 is not None:
        cargs.append("-mult")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("threshold_value")
    if v_1 is not None:
        cargs.append("-T")
        cargs.append(str(v_1))
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(params["outfile"])
    return cargs

def mri_gcut_outputs(params: MriGcut, execution: Execution) -> MriGcutOutputs:
    """Build the MriGcutOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_mask_file_v: OutputPathType = execution.output_file(params["outfile"])
    return MriGcutOutputs(
        root=root_v,
        output_mask_file=output_mask_file_v,
    )

def mri_gcut_execute(params: MriGcut, runner: Runner | None = None) -> MriGcutOutputs:
    """
    mri_gcut

    Skull stripping algorithm based on graph cuts.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gcut_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GCUT_METADATA)
    execution.params(params)
    args = mri_gcut_cargs(params, execution)
    out = mri_gcut_outputs(params, execution)
    execution.run(args)
    return out

def mri_gcut(
    infile: InputPathType,
    outfile: str,
    wmmask_110: bool = False,
    mult_file: InputPathType | None = None,
    threshold_value: float | None = None,
    runner: Runner | None = None,
) -> MriGcutOutputs:
    """
    mri_gcut

    Skull stripping algorithm based on graph cuts.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input brain volume file, e.g. T1.mgz
        outfile: Output file name, e.g. brainmask.auto.mgz
        wmmask_110: Use voxels with intensity 110 as white matter mask (when\
            applied on T1.mgz, FreeSurfer only)
        mult_file: Intersect the skull-stripped 'in_filename' and an existing\
            skull-stripped volume specified by 'filename', storing the result in\
            'out_filename'.
        threshold_value: Set threshold to value (%) of WM intensity, where the\
            value should be >0 and <1; defaults to 0.40.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gcut_params(
        infile=infile,
        outfile=outfile,
        wmmask_110=wmmask_110,
        mult_file=mult_file,
        threshold_value=threshold_value,
    )
    return mri_gcut_execute(params, runner)

__all__ = [
    "MriGcut",
    "MriGcutOutputs",
    "MRI_GCUT_METADATA",
    "mri_gcut_cargs",
    "mri_gcut_outputs",
    "mri_gcut_params",
    "mri_gcut_execute",
    "mri_gcut_validate",
    "mri_gcut",
]