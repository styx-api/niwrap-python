# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_LABEL_THICKNESS_METADATA = Metadata(
    id="get_label_thickness",
    name="get_label_thickness",
    package="freesurfer",
)

GetLabelThickness = typing.TypedDict(
    "GetLabelThickness",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/get_label_thickness"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class GetLabelThicknessOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def get_label_thickness_params(
    infile: InputPathType,
) -> GetLabelThickness:
    """
    Build parameters.

    Args:
        infile: Input file

    Returns:
        Parameter dictionary.
    """
    params: GetLabelThickness = {
        "@type": "freesurfer/get_label_thickness",
        "infile": infile,
    }
    return params

def get_label_thickness_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetLabelThickness."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def get_label_thickness_cargs(params: GetLabelThickness, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("get_label_thickness")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def get_label_thickness_outputs(params: GetLabelThickness, execution: Execution) -> GetLabelThicknessOutputs:
    """Build the GetLabelThicknessOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GetLabelThicknessOutputs(
        root=root_v,
    )

def get_label_thickness_execute(params: GetLabelThickness, runner: Runner | None = None) -> GetLabelThicknessOutputs:
    """
    get_label_thickness

    Tool to calculate label thickness.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_label_thickness_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_LABEL_THICKNESS_METADATA)
    execution.params(params)
    args = get_label_thickness_cargs(params, execution)
    out = get_label_thickness_outputs(params, execution)
    execution.run(args)
    return out

def get_label_thickness(
    infile: InputPathType,
    runner: Runner | None = None,
) -> GetLabelThicknessOutputs:
    """
    get_label_thickness

    Tool to calculate label thickness.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        infile: Input file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_label_thickness_params(
        infile=infile,
    )
    return get_label_thickness_execute(params, runner)

__all__ = [
    "GetLabelThickness",
    "GetLabelThicknessOutputs",
    "GET_LABEL_THICKNESS_METADATA",
    "get_label_thickness_cargs",
    "get_label_thickness_outputs",
    "get_label_thickness_params",
    "get_label_thickness_execute",
    "get_label_thickness_validate",
    "get_label_thickness",
]