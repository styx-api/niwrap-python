# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BBLABEL_METADATA = Metadata(
    id="bblabel",
    name="bblabel",
    package="freesurfer",
)

Bblabel = typing.TypedDict(
    "Bblabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bblabel"]],
        "labelfile": InputPathType,
        "xmin": typing.NotRequired[float],
        "xmax": typing.NotRequired[float],
        "ymin": typing.NotRequired[float],
        "ymax": typing.NotRequired[float],
        "zmin": typing.NotRequired[float],
        "zmax": typing.NotRequired[float],
        "outlabelfile": str,
        "debug": typing.NotRequired[bool],
        "umask": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class BblabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file with the label points within the specified bounding box."""

def bblabel_params(
    labelfile: InputPathType,
    outlabelfile: str,
    xmin: float | None = None,
    xmax: float | None = None,
    ymin: float | None = None,
    ymax: float | None = None,
    zmin: float | None = None,
    zmax: float | None = None,
    debug: bool = False,
    umask: str | None = None,
) -> Bblabel:
    """
    Build parameters.

    Args:
        labelfile: Input label file.
        outlabelfile: Output label file.
        xmin: Minimum x-coordinate for bounding box.
        xmax: Maximum x-coordinate for bounding box.
        ymin: Minimum y-coordinate for bounding box.
        ymax: Maximum y-coordinate for bounding box.
        zmin: Minimum z-coordinate for bounding box.
        zmax: Maximum z-coordinate for bounding box.
        debug: Enable debug mode.
        umask: Set Unix file permission mask.

    Returns:
        Parameter dictionary.
    """
    params: Bblabel = {
        "@type": "freesurfer/bblabel",
        "labelfile": labelfile,
        "outlabelfile": outlabelfile,
        "debug": debug,
    }
    if xmin is not None:
        params["xmin"] = xmin
    if xmax is not None:
        params["xmax"] = xmax
    if ymin is not None:
        params["ymin"] = ymin
    if ymax is not None:
        params["ymax"] = ymax
    if zmin is not None:
        params["zmin"] = zmin
    if zmax is not None:
        params["zmax"] = zmax
    if umask is not None:
        params["umask"] = umask
    return params

def bblabel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bblabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("labelfile", None) is None:
        raise StyxValidationError("`labelfile` must not be None")
    if not isinstance(params["labelfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`labelfile` has the wrong type: Received `{type(params["labelfile"])}` expected `InputPathType`')
    if params.get("xmin", None) is not None:
        if not isinstance(params["xmin"], (float, int)):
            raise StyxValidationError(f'`xmin` has the wrong type: Received `{type(params["xmin"])}` expected `float`')
    if params.get("xmax", None) is not None:
        if not isinstance(params["xmax"], (float, int)):
            raise StyxValidationError(f'`xmax` has the wrong type: Received `{type(params["xmax"])}` expected `float`')
    if params.get("ymin", None) is not None:
        if not isinstance(params["ymin"], (float, int)):
            raise StyxValidationError(f'`ymin` has the wrong type: Received `{type(params["ymin"])}` expected `float`')
    if params.get("ymax", None) is not None:
        if not isinstance(params["ymax"], (float, int)):
            raise StyxValidationError(f'`ymax` has the wrong type: Received `{type(params["ymax"])}` expected `float`')
    if params.get("zmin", None) is not None:
        if not isinstance(params["zmin"], (float, int)):
            raise StyxValidationError(f'`zmin` has the wrong type: Received `{type(params["zmin"])}` expected `float`')
    if params.get("zmax", None) is not None:
        if not isinstance(params["zmax"], (float, int)):
            raise StyxValidationError(f'`zmax` has the wrong type: Received `{type(params["zmax"])}` expected `float`')
    if params.get("outlabelfile", None) is None:
        raise StyxValidationError("`outlabelfile` must not be None")
    if not isinstance(params["outlabelfile"], str):
        raise StyxValidationError(f'`outlabelfile` has the wrong type: Received `{type(params["outlabelfile"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')

def bblabel_cargs(params: Bblabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bblabel")
    cargs.append("--l")
    cargs.append(execution.input_file(params["labelfile"]))
    v_0 = params.get("xmin")
    if v_0 is not None:
        cargs.append("--xmin")
        cargs.append(str(v_0))
    v_1 = params.get("xmax")
    if v_1 is not None:
        cargs.append("--xmax")
        cargs.append(str(v_1))
    v_2 = params.get("ymin")
    if v_2 is not None:
        cargs.append("--ymin")
        cargs.append(str(v_2))
    v_3 = params.get("ymax")
    if v_3 is not None:
        cargs.append("--ymax")
        cargs.append(str(v_3))
    v_4 = params.get("zmin")
    if v_4 is not None:
        cargs.append("--zmin")
        cargs.append(str(v_4))
    v_5 = params.get("zmax")
    if v_5 is not None:
        cargs.append("--zmax")
        cargs.append(str(v_5))
    cargs.append("--o")
    cargs.append(params["outlabelfile"])
    if params.get("debug"):
        cargs.append("--debug")
    v_6 = params.get("umask")
    if v_6 is not None:
        cargs.append("--umask")
        cargs.append(v_6)
    return cargs

def bblabel_outputs(params: Bblabel, execution: Execution) -> BblabelOutputs:
    """Build the BblabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["outlabelfile"])
    return BblabelOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def bblabel_execute(params: Bblabel, runner: Runner | None = None) -> BblabelOutputs:
    """
    bblabel

    Applies a bounding box to a label, copying only the label points within the specified box to the output.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bblabel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BBLABEL_METADATA)
    execution.params(params)
    args = bblabel_cargs(params, execution)
    out = bblabel_outputs(params, execution)
    execution.run(args)
    return out

def bblabel(
    labelfile: InputPathType,
    outlabelfile: str,
    xmin: float | None = None,
    xmax: float | None = None,
    ymin: float | None = None,
    ymax: float | None = None,
    zmin: float | None = None,
    zmax: float | None = None,
    debug: bool = False,
    umask: str | None = None,
    runner: Runner | None = None,
) -> BblabelOutputs:
    """
    bblabel

    Applies a bounding box to a label, copying only the label points within the specified box to the output.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        labelfile: Input label file.
        outlabelfile: Output label file.
        xmin: Minimum x-coordinate for bounding box.
        xmax: Maximum x-coordinate for bounding box.
        ymin: Minimum y-coordinate for bounding box.
        ymax: Maximum y-coordinate for bounding box.
        zmin: Minimum z-coordinate for bounding box.
        zmax: Maximum z-coordinate for bounding box.
        debug: Enable debug mode.
        umask: Set Unix file permission mask.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bblabel_params(
        labelfile=labelfile,
        outlabelfile=outlabelfile,
        xmin=xmin,
        xmax=xmax,
        ymin=ymin,
        ymax=ymax,
        zmin=zmin,
        zmax=zmax,
        debug=debug,
        umask=umask,
    )
    return bblabel_execute(params, runner)

__all__ = [
    "Bblabel",
    "BblabelOutputs",
    "BBLABEL_METADATA",
    "bblabel_cargs",
    "bblabel_outputs",
    "bblabel_params",
    "bblabel_execute",
    "bblabel_validate",
    "bblabel",
]