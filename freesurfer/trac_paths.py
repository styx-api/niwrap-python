# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRAC_PATHS_METADATA = Metadata(
    id="trac-paths",
    name="trac-paths",
    package="freesurfer",
)

TracPaths = typing.TypedDict(
    "TracPaths",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/trac-paths"]],
        "dmrirc_file": InputPathType,
        "log_file": typing.NotRequired[str],
        "no_log": typing.NotRequired[bool],
        "cmd_file": typing.NotRequired[str],
        "no_cmd": typing.NotRequired[bool],
        "no_isrunning": typing.NotRequired[bool],
        "umask": typing.NotRequired[str],
        "group_id": typing.NotRequired[str],
        "allow_core_dump": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "dontrun": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TracPathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def trac_paths_params(
    dmrirc_file: InputPathType,
    log_file: str | None = None,
    no_log: bool = False,
    cmd_file: str | None = None,
    no_cmd: bool = False,
    no_isrunning: bool = False,
    umask: str | None = None,
    group_id: str | None = None,
    allow_core_dump: bool = False,
    debug: bool = False,
    dontrun: bool = False,
    version: bool = False,
    help: bool = False,
) -> TracPaths:
    """
    Build parameters.

    Args:
        dmrirc_file: dmrirc file (see dmrirc.example)
        log_file: Log file, default is trac-all.log in the same directory as dmrirc
        no_log: Do not save a log file
        cmd_file: Cmd file, default is trac-all.cmd in the same directory as dmrirc
        no_cmd: Do not save a cmd file
        no_isrunning: Do not check whether this subject is currently being\
            processed
        umask: Set Unix file permission mask (default 002)
        group_id: Check that current group is alpha groupid
        allow_core_dump: Set coredump limit to unlimited
        debug: Generate much more output
        dontrun: Do everything but execute each command
        version: Print version of this script and exit
        help: Print full contents of help

    Returns:
        Parameter dictionary.
    """
    params: TracPaths = {
        "@type": "freesurfer/trac-paths",
        "dmrirc_file": dmrirc_file,
        "no_log": no_log,
        "no_cmd": no_cmd,
        "no_isrunning": no_isrunning,
        "allow_core_dump": allow_core_dump,
        "debug": debug,
        "dontrun": dontrun,
        "version": version,
        "help": help,
    }
    if log_file is not None:
        params["log_file"] = log_file
    if cmd_file is not None:
        params["cmd_file"] = cmd_file
    if umask is not None:
        params["umask"] = umask
    if group_id is not None:
        params["group_id"] = group_id
    return params

def trac_paths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TracPaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dmrirc_file", None) is None:
        raise StyxValidationError("`dmrirc_file` must not be None")
    if not isinstance(params["dmrirc_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dmrirc_file` has the wrong type: Received `{type(params["dmrirc_file"])}` expected `InputPathType`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("no_log", None) is not None:
        if not isinstance(params["no_log"], bool):
            raise StyxValidationError(f'`no_log` has the wrong type: Received `{type(params["no_log"])}` expected `bool`')
    if params.get("cmd_file", None) is not None:
        if not isinstance(params["cmd_file"], str):
            raise StyxValidationError(f'`cmd_file` has the wrong type: Received `{type(params["cmd_file"])}` expected `str`')
    if params.get("no_cmd", None) is not None:
        if not isinstance(params["no_cmd"], bool):
            raise StyxValidationError(f'`no_cmd` has the wrong type: Received `{type(params["no_cmd"])}` expected `bool`')
    if params.get("no_isrunning", None) is not None:
        if not isinstance(params["no_isrunning"], bool):
            raise StyxValidationError(f'`no_isrunning` has the wrong type: Received `{type(params["no_isrunning"])}` expected `bool`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')
    if params.get("group_id", None) is not None:
        if not isinstance(params["group_id"], str):
            raise StyxValidationError(f'`group_id` has the wrong type: Received `{type(params["group_id"])}` expected `str`')
    if params.get("allow_core_dump", None) is not None:
        if not isinstance(params["allow_core_dump"], bool):
            raise StyxValidationError(f'`allow_core_dump` has the wrong type: Received `{type(params["allow_core_dump"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("dontrun", None) is not None:
        if not isinstance(params["dontrun"], bool):
            raise StyxValidationError(f'`dontrun` has the wrong type: Received `{type(params["dontrun"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def trac_paths_cargs(params: TracPaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("trac-paths")
    cargs.append("-c")
    cargs.append(execution.input_file(params["dmrirc_file"]))
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-log")
        cargs.append(v_0)
    if params.get("no_log"):
        cargs.append("-nolog")
    v_1 = params.get("cmd_file")
    if v_1 is not None:
        cargs.append("-cmd")
        cargs.append(v_1)
    if params.get("no_cmd"):
        cargs.append("-nocmd")
    if params.get("no_isrunning"):
        cargs.append("-no-isrunning")
    v_2 = params.get("umask")
    if v_2 is not None:
        cargs.append("-umask")
        cargs.append(v_2)
    v_3 = params.get("group_id")
    if v_3 is not None:
        cargs.append("-grp")
        cargs.append(v_3)
    if params.get("allow_core_dump"):
        cargs.append("-allowcoredump")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("dontrun"):
        cargs.append("-dontrun")
    if params.get("version"):
        cargs.append("-version")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def trac_paths_outputs(params: TracPaths, execution: Execution) -> TracPathsOutputs:
    """Build the TracPathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TracPathsOutputs(
        root=root_v,
    )

def trac_paths_execute(params: TracPaths, runner: Runner | None = None) -> TracPathsOutputs:
    """
    trac-paths

    Tractography for a single subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    trac_paths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRAC_PATHS_METADATA)
    execution.params(params)
    args = trac_paths_cargs(params, execution)
    out = trac_paths_outputs(params, execution)
    execution.run(args)
    return out

def trac_paths(
    dmrirc_file: InputPathType,
    log_file: str | None = None,
    no_log: bool = False,
    cmd_file: str | None = None,
    no_cmd: bool = False,
    no_isrunning: bool = False,
    umask: str | None = None,
    group_id: str | None = None,
    allow_core_dump: bool = False,
    debug: bool = False,
    dontrun: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> TracPathsOutputs:
    """
    trac-paths

    Tractography for a single subject.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dmrirc_file: dmrirc file (see dmrirc.example)
        log_file: Log file, default is trac-all.log in the same directory as dmrirc
        no_log: Do not save a log file
        cmd_file: Cmd file, default is trac-all.cmd in the same directory as dmrirc
        no_cmd: Do not save a cmd file
        no_isrunning: Do not check whether this subject is currently being\
            processed
        umask: Set Unix file permission mask (default 002)
        group_id: Check that current group is alpha groupid
        allow_core_dump: Set coredump limit to unlimited
        debug: Generate much more output
        dontrun: Do everything but execute each command
        version: Print version of this script and exit
        help: Print full contents of help
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = trac_paths_params(
        dmrirc_file=dmrirc_file,
        log_file=log_file,
        no_log=no_log,
        cmd_file=cmd_file,
        no_cmd=no_cmd,
        no_isrunning=no_isrunning,
        umask=umask,
        group_id=group_id,
        allow_core_dump=allow_core_dump,
        debug=debug,
        dontrun=dontrun,
        version=version,
        help=help,
    )
    return trac_paths_execute(params, runner)

__all__ = [
    "TracPaths",
    "TracPathsOutputs",
    "TRAC_PATHS_METADATA",
    "trac_paths_cargs",
    "trac_paths_outputs",
    "trac_paths_params",
    "trac_paths_execute",
    "trac_paths_validate",
    "trac_paths",
]