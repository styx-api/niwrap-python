# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_ANATOMI_CUTS_METADATA = Metadata(
    id="dmri_AnatomiCuts",
    name="dmri_AnatomiCuts",
    package="freesurfer",
)

DmriAnatomiCutsDirectionFlag = typing.Literal["s"] | typing.Literal["d"] | typing.Literal["a"] | typing.Literal["o"]

DmriAnatomiCuts = typing.TypedDict(
    "DmriAnatomiCuts",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_AnatomiCuts"]],
        "segmentation_file": InputPathType,
        "fiber_file": InputPathType,
        "clusters": float,
        "points": float,
        "fibers_eigen": float,
        "output_folder": str,
        "direction_flag": DmriAnatomiCutsDirectionFlag,
    },
)

@dataclasses.dataclass
class DmriAnatomiCutsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vtk: OutputPathType
    """Output VTK file of the segmentation"""

def dmri_anatomi_cuts_params(
    segmentation_file: InputPathType,
    fiber_file: InputPathType,
    clusters: float,
    points: float,
    fibers_eigen: float,
    output_folder: str,
    direction_flag: DmriAnatomiCutsDirectionFlag,
) -> DmriAnatomiCuts:
    """
    Build parameters.

    Args:
        segmentation_file:
        fiber_file:
        clusters:
        points:
        fibers_eigen:
        output_folder:
        direction_flag:

    Returns:
        Parameter dictionary.
    """
    params: DmriAnatomiCuts = {
        "@type": "freesurfer/dmri_AnatomiCuts",
        "segmentation_file": segmentation_file,
        "fiber_file": fiber_file,
        "clusters": clusters,
        "points": points,
        "fibers_eigen": fibers_eigen,
        "output_folder": output_folder,
        "direction_flag": direction_flag,
    }
    return params

def dmri_anatomi_cuts_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriAnatomiCuts."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("segmentation_file", None) is None:
        raise StyxValidationError("`segmentation_file` must not be None")
    if not isinstance(params["segmentation_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segmentation_file` has the wrong type: Received `{type(params["segmentation_file"])}` expected `InputPathType`')
    if params.get("fiber_file", None) is None:
        raise StyxValidationError("`fiber_file` must not be None")
    if not isinstance(params["fiber_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fiber_file` has the wrong type: Received `{type(params["fiber_file"])}` expected `InputPathType`')
    if params.get("clusters", None) is None:
        raise StyxValidationError("`clusters` must not be None")
    if not isinstance(params["clusters"], (float, int)):
        raise StyxValidationError(f'`clusters` has the wrong type: Received `{type(params["clusters"])}` expected `float`')
    if params.get("points", None) is None:
        raise StyxValidationError("`points` must not be None")
    if not isinstance(params["points"], (float, int)):
        raise StyxValidationError(f'`points` has the wrong type: Received `{type(params["points"])}` expected `float`')
    if params.get("fibers_eigen", None) is None:
        raise StyxValidationError("`fibers_eigen` must not be None")
    if not isinstance(params["fibers_eigen"], (float, int)):
        raise StyxValidationError(f'`fibers_eigen` has the wrong type: Received `{type(params["fibers_eigen"])}` expected `float`')
    if params.get("output_folder", None) is None:
        raise StyxValidationError("`output_folder` must not be None")
    if not isinstance(params["output_folder"], str):
        raise StyxValidationError(f'`output_folder` has the wrong type: Received `{type(params["output_folder"])}` expected `str`')
    if params.get("direction_flag", None) is None:
        raise StyxValidationError("`direction_flag` must not be None")
    if not isinstance(params["direction_flag"], str):
        raise StyxValidationError(f'`direction_flag` has the wrong type: Received `{type(params["direction_flag"])}` expected `DmriAnatomiCutsDirectionFlag`')
    if params["direction_flag"] not in ["s", "d", "a", "o"]:
        raise StyxValidationError("Parameter `direction_flag` must be one of [\"s\", \"d\", \"a\", \"o\"]")

def dmri_anatomi_cuts_cargs(params: DmriAnatomiCuts, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_AnatomiCuts")
    cargs.append("-s")
    cargs.append(execution.input_file(params["segmentation_file"]))
    cargs.append("-f")
    cargs.append(execution.input_file(params["fiber_file"]))
    cargs.append("-c")
    cargs.append(str(params["clusters"]))
    cargs.append("-n")
    cargs.append(str(params["points"]))
    cargs.append("-e")
    cargs.append(str(params["fibers_eigen"]))
    cargs.append("-o")
    cargs.append(params["output_folder"])
    cargs.append("-d")
    cargs.append(str(params["direction_flag"]))
    return cargs

def dmri_anatomi_cuts_outputs(params: DmriAnatomiCuts, execution: Execution) -> DmriAnatomiCutsOutputs:
    """Build the DmriAnatomiCutsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_vtk_v: OutputPathType = execution.output_file(f'{params["output_folder"]}/output.vtk')
    return DmriAnatomiCutsOutputs(
        root=root_v,
        output_vtk=output_vtk_v,
    )

def dmri_anatomi_cuts_execute(params: DmriAnatomiCuts, runner: Runner | None = None) -> DmriAnatomiCutsOutputs:
    """
    dmri_AnatomiCuts

    AnatomiCuts tool for DTI fibers segmentation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_anatomi_cuts_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_ANATOMI_CUTS_METADATA)
    execution.params(params)
    args = dmri_anatomi_cuts_cargs(params, execution)
    out = dmri_anatomi_cuts_outputs(params, execution)
    execution.run(args)
    return out

def dmri_anatomi_cuts(
    segmentation_file: InputPathType,
    fiber_file: InputPathType,
    clusters: float,
    points: float,
    fibers_eigen: float,
    output_folder: str,
    direction_flag: DmriAnatomiCutsDirectionFlag,
    runner: Runner | None = None,
) -> DmriAnatomiCutsOutputs:
    """
    dmri_AnatomiCuts

    AnatomiCuts tool for DTI fibers segmentation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        segmentation_file:
        fiber_file:
        clusters:
        points:
        fibers_eigen:
        output_folder:
        direction_flag:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_anatomi_cuts_params(
        segmentation_file=segmentation_file,
        fiber_file=fiber_file,
        clusters=clusters,
        points=points,
        fibers_eigen=fibers_eigen,
        output_folder=output_folder,
        direction_flag=direction_flag,
    )
    return dmri_anatomi_cuts_execute(params, runner)

__all__ = [
    "DmriAnatomiCuts",
    "DmriAnatomiCutsOutputs",
    "DMRI_ANATOMI_CUTS_METADATA",
    "dmri_anatomi_cuts_cargs",
    "dmri_anatomi_cuts_outputs",
    "dmri_anatomi_cuts_params",
    "dmri_anatomi_cuts_execute",
    "dmri_anatomi_cuts_validate",
    "dmri_anatomi_cuts",
]