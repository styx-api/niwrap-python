# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GTMSEG_METADATA = Metadata(
    id="gtmseg",
    name="gtmseg",
    package="freesurfer",
)

Gtmseg = typing.TypedDict(
    "Gtmseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/gtmseg"]],
        "subject": str,
        "outvol": typing.NotRequired[str],
        "usf": typing.NotRequired[float],
        "subsegwm": typing.NotRequired[bool],
        "keep_hypo": typing.NotRequired[bool],
        "keep_cc": typing.NotRequired[bool],
        "dmax": typing.NotRequired[float],
        "ctx_annot": typing.NotRequired[str],
        "wm_annot": typing.NotRequired[str],
        "output_usf": typing.NotRequired[float],
        "head": typing.NotRequired[str],
        "subseg_cbwm": typing.NotRequired[bool],
        "no_pons": typing.NotRequired[bool],
        "no_vermis": typing.NotRequired[bool],
        "ctab": typing.NotRequired[InputPathType],
        "no_seg_stats": typing.NotRequired[bool],
        "xcerseg": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class GtmsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType | None
    """Output segmentation volume"""
    output_ctab: OutputPathType
    """Generated color table for the output segmentation"""

def gtmseg_params(
    subject: str,
    outvol: str | None = None,
    usf: float | None = None,
    subsegwm: bool = False,
    keep_hypo: bool = False,
    keep_cc: bool = False,
    dmax: float | None = None,
    ctx_annot: str | None = None,
    wm_annot: str | None = None,
    output_usf: float | None = None,
    head: str | None = None,
    subseg_cbwm: bool = False,
    no_pons: bool = False,
    no_vermis: bool = False,
    ctab: InputPathType | None = None,
    no_seg_stats: bool = False,
    xcerseg: bool = False,
    debug: bool = False,
) -> Gtmseg:
    """
    Build parameters.

    Args:
        subject: Subject to analyze
        outvol: Output volume relative to subject/mri
        usf: Upsampling factor for segmentation resolution
        subsegwm: Subsegment white matter into lobes
        keep_hypo: Do not relabel hypointensities as white matter when\
            subsegmenting WM
        keep_cc: Do not relabel corpus callosum as white matter
        dmax: Distance threshold for subsegmenting WM
        ctx_annot: Annotation for cortical segmentation
        wm_annot: Annotation for WM segmentation with --subsegwm
        output_usf: Set output upsampling factor different from input USF for\
            debugging
        head: Use alternative head segmentation file
        subseg_cbwm: Subsegment cerebellum WM into core and gyri
        no_pons: Do not add pons segmentation when running xcerebralseg
        no_vermis: Do not add vermis segmentation when running xcerebralseg
        ctab: Color table for custom segmentation
        no_seg_stats: Do not compute segmentation statistics
        xcerseg: Run xcerebralseg to create apas+head.mgz
        debug: Enable debugging mode

    Returns:
        Parameter dictionary.
    """
    params: Gtmseg = {
        "@type": "freesurfer/gtmseg",
        "subject": subject,
        "subsegwm": subsegwm,
        "keep_hypo": keep_hypo,
        "keep_cc": keep_cc,
        "subseg_cbwm": subseg_cbwm,
        "no_pons": no_pons,
        "no_vermis": no_vermis,
        "no_seg_stats": no_seg_stats,
        "xcerseg": xcerseg,
        "debug": debug,
    }
    if outvol is not None:
        params["outvol"] = outvol
    if usf is not None:
        params["usf"] = usf
    if dmax is not None:
        params["dmax"] = dmax
    if ctx_annot is not None:
        params["ctx_annot"] = ctx_annot
    if wm_annot is not None:
        params["wm_annot"] = wm_annot
    if output_usf is not None:
        params["output_usf"] = output_usf
    if head is not None:
        params["head"] = head
    if ctab is not None:
        params["ctab"] = ctab
    return params

def gtmseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gtmseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("outvol", None) is not None:
        if not isinstance(params["outvol"], str):
            raise StyxValidationError(f'`outvol` has the wrong type: Received `{type(params["outvol"])}` expected `str`')
    if params.get("usf", None) is not None:
        if not isinstance(params["usf"], (float, int)):
            raise StyxValidationError(f'`usf` has the wrong type: Received `{type(params["usf"])}` expected `float`')
    if params.get("subsegwm", None) is not None:
        if not isinstance(params["subsegwm"], bool):
            raise StyxValidationError(f'`subsegwm` has the wrong type: Received `{type(params["subsegwm"])}` expected `bool`')
    if params.get("keep_hypo", None) is not None:
        if not isinstance(params["keep_hypo"], bool):
            raise StyxValidationError(f'`keep_hypo` has the wrong type: Received `{type(params["keep_hypo"])}` expected `bool`')
    if params.get("keep_cc", None) is not None:
        if not isinstance(params["keep_cc"], bool):
            raise StyxValidationError(f'`keep_cc` has the wrong type: Received `{type(params["keep_cc"])}` expected `bool`')
    if params.get("dmax", None) is not None:
        if not isinstance(params["dmax"], (float, int)):
            raise StyxValidationError(f'`dmax` has the wrong type: Received `{type(params["dmax"])}` expected `float`')
    if params.get("ctx_annot", None) is not None:
        if not isinstance(params["ctx_annot"], str):
            raise StyxValidationError(f'`ctx_annot` has the wrong type: Received `{type(params["ctx_annot"])}` expected `str`')
    if params.get("wm_annot", None) is not None:
        if not isinstance(params["wm_annot"], str):
            raise StyxValidationError(f'`wm_annot` has the wrong type: Received `{type(params["wm_annot"])}` expected `str`')
    if params.get("output_usf", None) is not None:
        if not isinstance(params["output_usf"], (float, int)):
            raise StyxValidationError(f'`output_usf` has the wrong type: Received `{type(params["output_usf"])}` expected `float`')
    if params.get("head", None) is not None:
        if not isinstance(params["head"], str):
            raise StyxValidationError(f'`head` has the wrong type: Received `{type(params["head"])}` expected `str`')
    if params.get("subseg_cbwm", None) is not None:
        if not isinstance(params["subseg_cbwm"], bool):
            raise StyxValidationError(f'`subseg_cbwm` has the wrong type: Received `{type(params["subseg_cbwm"])}` expected `bool`')
    if params.get("no_pons", None) is not None:
        if not isinstance(params["no_pons"], bool):
            raise StyxValidationError(f'`no_pons` has the wrong type: Received `{type(params["no_pons"])}` expected `bool`')
    if params.get("no_vermis", None) is not None:
        if not isinstance(params["no_vermis"], bool):
            raise StyxValidationError(f'`no_vermis` has the wrong type: Received `{type(params["no_vermis"])}` expected `bool`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `InputPathType`')
    if params.get("no_seg_stats", None) is not None:
        if not isinstance(params["no_seg_stats"], bool):
            raise StyxValidationError(f'`no_seg_stats` has the wrong type: Received `{type(params["no_seg_stats"])}` expected `bool`')
    if params.get("xcerseg", None) is not None:
        if not isinstance(params["xcerseg"], bool):
            raise StyxValidationError(f'`xcerseg` has the wrong type: Received `{type(params["xcerseg"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def gtmseg_cargs(params: Gtmseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gtmseg")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("outvol")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    v_1 = params.get("usf")
    if v_1 is not None:
        cargs.append("--usf")
        cargs.append(str(v_1))
    if params.get("subsegwm"):
        cargs.append("--subsegwm")
    if params.get("keep_hypo"):
        cargs.append("--keep-hypo")
    if params.get("keep_cc"):
        cargs.append("--keep-cc")
    v_2 = params.get("dmax")
    if v_2 is not None:
        cargs.append("--dmax")
        cargs.append(str(v_2))
    v_3 = params.get("ctx_annot")
    if v_3 is not None:
        cargs.append("--ctx-annot")
        cargs.append(v_3)
    v_4 = params.get("wm_annot")
    if v_4 is not None:
        cargs.append("--wm-annot")
        cargs.append(v_4)
    v_5 = params.get("output_usf")
    if v_5 is not None:
        cargs.append("--output-usf")
        cargs.append(str(v_5))
    v_6 = params.get("head")
    if v_6 is not None:
        cargs.append("--head")
        cargs.append(v_6)
    if params.get("subseg_cbwm"):
        cargs.append("--subseg-cblum-wm")
    if params.get("no_pons"):
        cargs.append("--no-pons")
    if params.get("no_vermis"):
        cargs.append("--no-vermis")
    v_7 = params.get("ctab")
    if v_7 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_7))
    if params.get("no_seg_stats"):
        cargs.append("--no-seg-stats")
    if params.get("xcerseg"):
        cargs.append("--xcerseg")
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def gtmseg_outputs(params: Gtmseg, execution: Execution) -> GtmsegOutputs:
    """Build the GtmsegOutputs object for this tool."""
    output_volume_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outvol")
    if __v0 is not None:
        output_volume_v = execution.output_file(f'$SUBJECTS_DIR/{params["subject"]}/mri/{(__v0 if __v0 is not None else "")}')
    output_ctab_v: OutputPathType = execution.output_file(f'$SUBJECTS_DIR/{params["subject"]}/mri/gtmseg+myseg.ctab')
    return GtmsegOutputs(
        root=root_v,
        output_volume=output_volume_v,
        output_ctab=output_ctab_v,
    )

def gtmseg_execute(params: Gtmseg, runner: Runner | None = None) -> GtmsegOutputs:
    """
    gtmseg

    Creates an anatomical segmentation for the geometric transfer matrix (GTM) used in PET partial volume correction.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gtmseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GTMSEG_METADATA)
    execution.params(params)
    args = gtmseg_cargs(params, execution)
    out = gtmseg_outputs(params, execution)
    execution.run(args)
    return out

def gtmseg(
    subject: str,
    outvol: str | None = None,
    usf: float | None = None,
    subsegwm: bool = False,
    keep_hypo: bool = False,
    keep_cc: bool = False,
    dmax: float | None = None,
    ctx_annot: str | None = None,
    wm_annot: str | None = None,
    output_usf: float | None = None,
    head: str | None = None,
    subseg_cbwm: bool = False,
    no_pons: bool = False,
    no_vermis: bool = False,
    ctab: InputPathType | None = None,
    no_seg_stats: bool = False,
    xcerseg: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> GtmsegOutputs:
    """
    gtmseg

    Creates an anatomical segmentation for the geometric transfer matrix (GTM) used in PET partial volume correction.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject to analyze
        outvol: Output volume relative to subject/mri
        usf: Upsampling factor for segmentation resolution
        subsegwm: Subsegment white matter into lobes
        keep_hypo: Do not relabel hypointensities as white matter when\
            subsegmenting WM
        keep_cc: Do not relabel corpus callosum as white matter
        dmax: Distance threshold for subsegmenting WM
        ctx_annot: Annotation for cortical segmentation
        wm_annot: Annotation for WM segmentation with --subsegwm
        output_usf: Set output upsampling factor different from input USF for\
            debugging
        head: Use alternative head segmentation file
        subseg_cbwm: Subsegment cerebellum WM into core and gyri
        no_pons: Do not add pons segmentation when running xcerebralseg
        no_vermis: Do not add vermis segmentation when running xcerebralseg
        ctab: Color table for custom segmentation
        no_seg_stats: Do not compute segmentation statistics
        xcerseg: Run xcerebralseg to create apas+head.mgz
        debug: Enable debugging mode
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gtmseg_params(
        subject=subject,
        outvol=outvol,
        usf=usf,
        subsegwm=subsegwm,
        keep_hypo=keep_hypo,
        keep_cc=keep_cc,
        dmax=dmax,
        ctx_annot=ctx_annot,
        wm_annot=wm_annot,
        output_usf=output_usf,
        head=head,
        subseg_cbwm=subseg_cbwm,
        no_pons=no_pons,
        no_vermis=no_vermis,
        ctab=ctab,
        no_seg_stats=no_seg_stats,
        xcerseg=xcerseg,
        debug=debug,
    )
    return gtmseg_execute(params, runner)

__all__ = [
    "Gtmseg",
    "GtmsegOutputs",
    "GTMSEG_METADATA",
    "gtmseg_cargs",
    "gtmseg_outputs",
    "gtmseg_params",
    "gtmseg_execute",
    "gtmseg_validate",
    "gtmseg",
]