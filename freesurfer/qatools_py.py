# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QATOOLS_PY_METADATA = Metadata(
    id="qatools.py",
    name="qatools.py",
    package="freesurfer",
)

QatoolsPy = typing.TypedDict(
    "QatoolsPy",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/qatools.py"]],
        "subjects_dir": str,
        "output_dir": str,
        "subjects": typing.NotRequired[list[str]],
        "screenshots": typing.NotRequired[bool],
        "fornix": typing.NotRequired[bool],
        "outlier": typing.NotRequired[bool],
        "outlier_table": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class QatoolsPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    summary_csv: OutputPathType
    """Summary CSV file containing quality metrics."""
    screenshots_output: OutputPathType
    """Directory containing screenshots of individual brains if screenshots option is selected."""

def qatools_py_params(
    subjects_dir: str,
    output_dir: str,
    subjects: list[str] | None = None,
    screenshots: bool = False,
    fornix: bool = False,
    outlier: bool = False,
    outlier_table: InputPathType | None = None,
) -> QatoolsPy:
    """
    Build parameters.

    Args:
        subjects_dir: Subjects directory containing a set of Freesurfer 6.0\
            processed individual datasets.
        output_dir: Output directory for the generated results.
        subjects: List of subject IDs. If omitted, all suitable subdirectories\
            within the subjects directory will be used.
        screenshots: Create screenshots of individual brains.
        fornix: Check fornix segmentation.
        outlier: Run outlier detection.
        outlier_table: Specify normative values for outlier detection.

    Returns:
        Parameter dictionary.
    """
    params: QatoolsPy = {
        "@type": "freesurfer/qatools.py",
        "subjects_dir": subjects_dir,
        "output_dir": output_dir,
        "screenshots": screenshots,
        "fornix": fornix,
        "outlier": outlier,
    }
    if subjects is not None:
        params["subjects"] = subjects
    if outlier_table is not None:
        params["outlier_table"] = outlier_table
    return params

def qatools_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid QatoolsPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("subjects", None) is not None:
        if not isinstance(params["subjects"], list):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
        for e in params["subjects"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("screenshots", None) is not None:
        if not isinstance(params["screenshots"], bool):
            raise StyxValidationError(f'`screenshots` has the wrong type: Received `{type(params["screenshots"])}` expected `bool`')
    if params.get("fornix", None) is not None:
        if not isinstance(params["fornix"], bool):
            raise StyxValidationError(f'`fornix` has the wrong type: Received `{type(params["fornix"])}` expected `bool`')
    if params.get("outlier", None) is not None:
        if not isinstance(params["outlier"], bool):
            raise StyxValidationError(f'`outlier` has the wrong type: Received `{type(params["outlier"])}` expected `bool`')
    if params.get("outlier_table", None) is not None:
        if not isinstance(params["outlier_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`outlier_table` has the wrong type: Received `{type(params["outlier_table"])}` expected `InputPathType`')

def qatools_py_cargs(params: QatoolsPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("qatools.py")
    cargs.append("--subjects_dir")
    cargs.append(params["subjects_dir"])
    cargs.append("--output_dir")
    cargs.append(params["output_dir"])
    v_0 = params.get("subjects")
    if v_0 is not None:
        cargs.append("--subjects")
        for item0 in v_0:
            cargs.append(item0)
    if params.get("screenshots"):
        cargs.append("--screenshots")
    if params.get("fornix"):
        cargs.append("--fornix")
    if params.get("outlier"):
        cargs.append("--outlier")
    v_1 = params.get("outlier_table")
    if v_1 is not None:
        cargs.append("--outlier-table")
        cargs.append(execution.input_file(v_1))
    return cargs

def qatools_py_outputs(params: QatoolsPy, execution: Execution) -> QatoolsPyOutputs:
    """Build the QatoolsPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    summary_csv_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/summary.csv')
    screenshots_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/screenshots/')
    return QatoolsPyOutputs(
        root=root_v,
        summary_csv=summary_csv_v,
        screenshots_output=screenshots_output_v,
    )

def qatools_py_execute(params: QatoolsPy, runner: Runner | None = None) -> QatoolsPyOutputs:
    """
    qatools.py

    A tool to compute quality metrics from Freesurfer 6.0 analysis results.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    qatools_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QATOOLS_PY_METADATA)
    execution.params(params)
    args = qatools_py_cargs(params, execution)
    out = qatools_py_outputs(params, execution)
    execution.run(args)
    return out

def qatools_py(
    subjects_dir: str,
    output_dir: str,
    subjects: list[str] | None = None,
    screenshots: bool = False,
    fornix: bool = False,
    outlier: bool = False,
    outlier_table: InputPathType | None = None,
    runner: Runner | None = None,
) -> QatoolsPyOutputs:
    """
    qatools.py

    A tool to compute quality metrics from Freesurfer 6.0 analysis results.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects_dir: Subjects directory containing a set of Freesurfer 6.0\
            processed individual datasets.
        output_dir: Output directory for the generated results.
        subjects: List of subject IDs. If omitted, all suitable subdirectories\
            within the subjects directory will be used.
        screenshots: Create screenshots of individual brains.
        fornix: Check fornix segmentation.
        outlier: Run outlier detection.
        outlier_table: Specify normative values for outlier detection.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = qatools_py_params(
        subjects_dir=subjects_dir,
        output_dir=output_dir,
        subjects=subjects,
        screenshots=screenshots,
        fornix=fornix,
        outlier=outlier,
        outlier_table=outlier_table,
    )
    return qatools_py_execute(params, runner)

__all__ = [
    "QatoolsPy",
    "QatoolsPyOutputs",
    "QATOOLS_PY_METADATA",
    "qatools_py_cargs",
    "qatools_py_outputs",
    "qatools_py_params",
    "qatools_py_execute",
    "qatools_py_validate",
    "qatools_py",
]