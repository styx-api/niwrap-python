# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COR2LABEL_METADATA = Metadata(
    id="mri_cor2label",
    name="mri_cor2label",
    package="freesurfer",
)

MriCor2label = typing.TypedDict(
    "MriCor2label",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cor2label"]],
        "input_file": InputPathType,
        "label_id": float,
        "label_file": str,
        "threshold": typing.NotRequired[float],
        "volume_file": typing.NotRequired[str],
        "surface_overlay": typing.NotRequired[list[str]],
        "surface_path": typing.NotRequired[str],
        "optimize": typing.NotRequired[list[str]],
        "remove_holes_islands": typing.NotRequired[bool],
        "dilate": typing.NotRequired[float],
        "erode": typing.NotRequired[float],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCor2labelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_file: OutputPathType
    """Generated label file."""
    output_volume_file: OutputPathType | None
    """Written volume label if specified."""

def mri_cor2label_params(
    input_file: InputPathType,
    label_id: float,
    label_file: str,
    threshold: float | None = None,
    volume_file: str | None = None,
    surface_overlay: list[str] | None = None,
    surface_path: str | None = None,
    optimize: list[str] | None = None,
    remove_holes_islands: bool = False,
    dilate: float | None = None,
    erode: float | None = None,
    help: bool = False,
) -> MriCor2label:
    """
    Build parameters.

    Args:
        input_file: Input volume or surface overlay file.
        label_id: Value to match in the input data.
        label_file: Name of the output label file.
        threshold: Threshold the input to make label, e.g., input values must be\
            greater than the threshold.
        volume_file: Write the label volume to a file.
        surface_overlay: Interpret input as a surface overlay, specifying subject,\
            hemisphere, and surface.
        surface_path: Specify surface path rather than subject/hemisphere.
        optimize: Treat input as a probability map and optimize thresholding.
        remove_holes_islands: Remove holes in label and islands (only valid with\
            --surf).
        dilate: Dilate label with specified number of dilations (only valid with\
            --surf).
        erode: Erode label with specified number of erosions (only valid with\
            --surf).
        help: Display help information.

    Returns:
        Parameter dictionary.
    """
    params: MriCor2label = {
        "@type": "freesurfer/mri_cor2label",
        "input_file": input_file,
        "label_id": label_id,
        "label_file": label_file,
        "remove_holes_islands": remove_holes_islands,
        "help": help,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if volume_file is not None:
        params["volume_file"] = volume_file
    if surface_overlay is not None:
        params["surface_overlay"] = surface_overlay
    if surface_path is not None:
        params["surface_path"] = surface_path
    if optimize is not None:
        params["optimize"] = optimize
    if dilate is not None:
        params["dilate"] = dilate
    if erode is not None:
        params["erode"] = erode
    return params

def mri_cor2label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCor2label."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("label_id", None) is None:
        raise StyxValidationError("`label_id` must not be None")
    if not isinstance(params["label_id"], (float, int)):
        raise StyxValidationError(f'`label_id` has the wrong type: Received `{type(params["label_id"])}` expected `float`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], str):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("volume_file", None) is not None:
        if not isinstance(params["volume_file"], str):
            raise StyxValidationError(f'`volume_file` has the wrong type: Received `{type(params["volume_file"])}` expected `str`')
    if params.get("surface_overlay", None) is not None:
        if not isinstance(params["surface_overlay"], list):
            raise StyxValidationError(f'`surface_overlay` has the wrong type: Received `{type(params["surface_overlay"])}` expected `list[str]`')
        if not (3 <= len(params["surface_overlay"]) <= 3):
            raise StyxValidationError("Parameter `surface_overlay` must contain between 3 and 3 elements (inclusive)")
        for e in params["surface_overlay"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`surface_overlay` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("surface_path", None) is not None:
        if not isinstance(params["surface_path"], str):
            raise StyxValidationError(f'`surface_path` has the wrong type: Received `{type(params["surface_path"])}` expected `str`')
    if params.get("optimize", None) is not None:
        if not isinstance(params["optimize"], list):
            raise StyxValidationError(f'`optimize` has the wrong type: Received `{type(params["optimize"])}` expected `list[str]`')
        if not (3 <= len(params["optimize"]) <= 3):
            raise StyxValidationError("Parameter `optimize` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["optimize"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`optimize` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("remove_holes_islands", None) is not None:
        if not isinstance(params["remove_holes_islands"], bool):
            raise StyxValidationError(f'`remove_holes_islands` has the wrong type: Received `{type(params["remove_holes_islands"])}` expected `bool`')
    if params.get("dilate", None) is not None:
        if not isinstance(params["dilate"], (float, int)):
            raise StyxValidationError(f'`dilate` has the wrong type: Received `{type(params["dilate"])}` expected `float`')
    if params.get("erode", None) is not None:
        if not isinstance(params["erode"], (float, int)):
            raise StyxValidationError(f'`erode` has the wrong type: Received `{type(params["erode"])}` expected `float`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_cor2label_cargs(params: MriCor2label, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cor2label")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-id")
    cargs.append(str(params["label_id"]))
    cargs.append("-l")
    cargs.append(params["label_file"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_0))
    v_1 = params.get("volume_file")
    if v_1 is not None:
        cargs.append("--v")
        cargs.append(v_1)
    v_2 = params.get("surface_overlay")
    if v_2 is not None:
        cargs.append("--surf")
        for item0 in v_2:
            cargs.append(item0)
    v_3 = params.get("surface_path")
    if v_3 is not None:
        cargs.append("--surf-path")
        cargs.append(v_3)
    v_4 = params.get("optimize")
    if v_4 is not None:
        cargs.append("--opt")
        for item1 in v_4:
            cargs.append(item1)
    if params.get("remove_holes_islands"):
        cargs.append("--remove-holes-islands")
    v_5 = params.get("dilate")
    if v_5 is not None:
        cargs.append("--dilate")
        cargs.append(str(v_5))
    v_6 = params.get("erode")
    if v_6 is not None:
        cargs.append("--erode")
        cargs.append(str(v_6))
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_cor2label_outputs(params: MriCor2label, execution: Execution) -> MriCor2labelOutputs:
    """Build the MriCor2labelOutputs object for this tool."""
    output_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_label_file_v: OutputPathType = execution.output_file(params["label_file"])
    __v0 = params.get("volume_file")
    if __v0 is not None:
        output_volume_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriCor2labelOutputs(
        root=root_v,
        output_label_file=output_label_file_v,
        output_volume_file=output_volume_file_v,
    )

def mri_cor2label_execute(params: MriCor2label, runner: Runner | None = None) -> MriCor2labelOutputs:
    """
    mri_cor2label

    Converts values in a volume or surface overlay to a label. Designed to convert parcellation volumes stored in mri format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cor2label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COR2LABEL_METADATA)
    execution.params(params)
    args = mri_cor2label_cargs(params, execution)
    out = mri_cor2label_outputs(params, execution)
    execution.run(args)
    return out

def mri_cor2label(
    input_file: InputPathType,
    label_id: float,
    label_file: str,
    threshold: float | None = None,
    volume_file: str | None = None,
    surface_overlay: list[str] | None = None,
    surface_path: str | None = None,
    optimize: list[str] | None = None,
    remove_holes_islands: bool = False,
    dilate: float | None = None,
    erode: float | None = None,
    help: bool = False,
    runner: Runner | None = None,
) -> MriCor2labelOutputs:
    """
    mri_cor2label

    Converts values in a volume or surface overlay to a label. Designed to convert parcellation volumes stored in mri format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input volume or surface overlay file.
        label_id: Value to match in the input data.
        label_file: Name of the output label file.
        threshold: Threshold the input to make label, e.g., input values must be\
            greater than the threshold.
        volume_file: Write the label volume to a file.
        surface_overlay: Interpret input as a surface overlay, specifying subject,\
            hemisphere, and surface.
        surface_path: Specify surface path rather than subject/hemisphere.
        optimize: Treat input as a probability map and optimize thresholding.
        remove_holes_islands: Remove holes in label and islands (only valid with\
            --surf).
        dilate: Dilate label with specified number of dilations (only valid with\
            --surf).
        erode: Erode label with specified number of erosions (only valid with\
            --surf).
        help: Display help information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cor2label_params(
        input_file=input_file,
        label_id=label_id,
        label_file=label_file,
        threshold=threshold,
        volume_file=volume_file,
        surface_overlay=surface_overlay,
        surface_path=surface_path,
        optimize=optimize,
        remove_holes_islands=remove_holes_islands,
        dilate=dilate,
        erode=erode,
        help=help,
    )
    return mri_cor2label_execute(params, runner)

__all__ = [
    "MriCor2label",
    "MriCor2labelOutputs",
    "MRI_COR2LABEL_METADATA",
    "mri_cor2label_cargs",
    "mri_cor2label_outputs",
    "mri_cor2label_params",
    "mri_cor2label_execute",
    "mri_cor2label_validate",
    "mri_cor2label",
]