# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_VOLDIFF_METADATA = Metadata(
    id="mri_voldiff",
    name="mri_voldiff",
    package="freesurfer",
)

MriVoldiff = typing.TypedDict(
    "MriVoldiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_voldiff"]],
        "volume1": InputPathType,
        "volume2": InputPathType,
        "vox2ras_thresh": typing.NotRequired[float],
        "pix_thresh": typing.NotRequired[float],
        "allow_precision": typing.NotRequired[bool],
        "allow_resolution": typing.NotRequired[bool],
        "allow_vox2ras": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriVoldiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_voldiff_params(
    volume1: InputPathType,
    volume2: InputPathType,
    vox2ras_thresh: float | None = None,
    pix_thresh: float | None = None,
    allow_precision: bool = False,
    allow_resolution: bool = False,
    allow_vox2ras: bool = False,
    debug: bool = False,
    checkopts: bool = False,
) -> MriVoldiff:
    """
    Build parameters.

    Args:
        volume1: First input volume
        volume2: Second input volume
        vox2ras_thresh: Vox2RAS threshold value
        pix_thresh: Pixel threshold value
        allow_precision: Allow differences in precision
        allow_resolution: Allow differences in resolution
        allow_vox2ras: Allow differences in Vox2RAS
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: MriVoldiff = {
        "@type": "freesurfer/mri_voldiff",
        "volume1": volume1,
        "volume2": volume2,
        "allow_precision": allow_precision,
        "allow_resolution": allow_resolution,
        "allow_vox2ras": allow_vox2ras,
        "debug": debug,
        "checkopts": checkopts,
    }
    if vox2ras_thresh is not None:
        params["vox2ras_thresh"] = vox2ras_thresh
    if pix_thresh is not None:
        params["pix_thresh"] = pix_thresh
    return params

def mri_voldiff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriVoldiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume1", None) is None:
        raise StyxValidationError("`volume1` must not be None")
    if not isinstance(params["volume1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume1` has the wrong type: Received `{type(params["volume1"])}` expected `InputPathType`')
    if params.get("volume2", None) is None:
        raise StyxValidationError("`volume2` must not be None")
    if not isinstance(params["volume2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume2` has the wrong type: Received `{type(params["volume2"])}` expected `InputPathType`')
    if params.get("vox2ras_thresh", None) is not None:
        if not isinstance(params["vox2ras_thresh"], (float, int)):
            raise StyxValidationError(f'`vox2ras_thresh` has the wrong type: Received `{type(params["vox2ras_thresh"])}` expected `float`')
    if params.get("pix_thresh", None) is not None:
        if not isinstance(params["pix_thresh"], (float, int)):
            raise StyxValidationError(f'`pix_thresh` has the wrong type: Received `{type(params["pix_thresh"])}` expected `float`')
    if params.get("allow_precision", None) is not None:
        if not isinstance(params["allow_precision"], bool):
            raise StyxValidationError(f'`allow_precision` has the wrong type: Received `{type(params["allow_precision"])}` expected `bool`')
    if params.get("allow_resolution", None) is not None:
        if not isinstance(params["allow_resolution"], bool):
            raise StyxValidationError(f'`allow_resolution` has the wrong type: Received `{type(params["allow_resolution"])}` expected `bool`')
    if params.get("allow_vox2ras", None) is not None:
        if not isinstance(params["allow_vox2ras"], bool):
            raise StyxValidationError(f'`allow_vox2ras` has the wrong type: Received `{type(params["allow_vox2ras"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')

def mri_voldiff_cargs(params: MriVoldiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_voldiff")
    cargs.append("-v1")
    cargs.append(execution.input_file(params["volume1"]))
    cargs.append("-v2")
    cargs.append(execution.input_file(params["volume2"]))
    v_0 = params.get("vox2ras_thresh")
    if v_0 is not None:
        cargs.append("--vox2ras")
        cargs.append(str(v_0))
    v_1 = params.get("pix_thresh")
    if v_1 is not None:
        cargs.append("--pix")
        cargs.append(str(v_1))
    if params.get("allow_precision"):
        cargs.append("--allow-prec")
    if params.get("allow_resolution"):
        cargs.append("--allow-res")
    if params.get("allow_vox2ras"):
        cargs.append("--allow-vox2ras")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    return cargs

def mri_voldiff_outputs(params: MriVoldiff, execution: Execution) -> MriVoldiffOutputs:
    """Build the MriVoldiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriVoldiffOutputs(
        root=root_v,
    )

def mri_voldiff_execute(params: MriVoldiff, runner: Runner | None = None) -> MriVoldiffOutputs:
    """
    mri_voldiff

    Determines whether two volumes are different in terms of pixel data, dimension, precision, resolution, or geometry.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_voldiff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_VOLDIFF_METADATA)
    execution.params(params)
    args = mri_voldiff_cargs(params, execution)
    out = mri_voldiff_outputs(params, execution)
    execution.run(args)
    return out

def mri_voldiff(
    volume1: InputPathType,
    volume2: InputPathType,
    vox2ras_thresh: float | None = None,
    pix_thresh: float | None = None,
    allow_precision: bool = False,
    allow_resolution: bool = False,
    allow_vox2ras: bool = False,
    debug: bool = False,
    checkopts: bool = False,
    runner: Runner | None = None,
) -> MriVoldiffOutputs:
    """
    mri_voldiff

    Determines whether two volumes are different in terms of pixel data, dimension, precision, resolution, or geometry.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        volume1: First input volume
        volume2: Second input volume
        vox2ras_thresh: Vox2RAS threshold value
        pix_thresh: Pixel threshold value
        allow_precision: Allow differences in precision
        allow_resolution: Allow differences in resolution
        allow_vox2ras: Allow differences in Vox2RAS
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_voldiff_params(
        volume1=volume1,
        volume2=volume2,
        vox2ras_thresh=vox2ras_thresh,
        pix_thresh=pix_thresh,
        allow_precision=allow_precision,
        allow_resolution=allow_resolution,
        allow_vox2ras=allow_vox2ras,
        debug=debug,
        checkopts=checkopts,
    )
    return mri_voldiff_execute(params, runner)

__all__ = [
    "MriVoldiff",
    "MriVoldiffOutputs",
    "MRI_VOLDIFF_METADATA",
    "mri_voldiff_cargs",
    "mri_voldiff_outputs",
    "mri_voldiff_params",
    "mri_voldiff_execute",
    "mri_voldiff_validate",
    "mri_voldiff",
]