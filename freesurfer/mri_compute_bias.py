# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_BIAS_METADATA = Metadata(
    id="mri_compute_bias",
    name="mri_compute_bias",
    package="freesurfer",
)

MriComputeBias = typing.TypedDict(
    "MriComputeBias",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_bias"]],
        "subjects": list[str],
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriComputeBiasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The output volume containing the bias correction result."""

def mri_compute_bias_params(
    subjects: list[str],
    output_volume: str,
) -> MriComputeBias:
    """
    Build parameters.

    Args:
        subjects: List of subjects for which bias correction is calculated.
        output_volume: Output volume where the result will be stored.

    Returns:
        Parameter dictionary.
    """
    params: MriComputeBias = {
        "@type": "freesurfer/mri_compute_bias",
        "subjects": subjects,
        "output_volume": output_volume,
    }
    return params

def mri_compute_bias_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeBias."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_compute_bias_cargs(params: MriComputeBias, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_bias")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append(params["output_volume"])
    return cargs

def mri_compute_bias_outputs(params: MriComputeBias, execution: Execution) -> MriComputeBiasOutputs:
    """Build the MriComputeBiasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriComputeBiasOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_compute_bias_execute(params: MriComputeBias, runner: Runner | None = None) -> MriComputeBiasOutputs:
    """
    mri_compute_bias

    Compute bias correction volumes for the given subjects and outputs the result to a specified volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_bias_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_BIAS_METADATA)
    execution.params(params)
    args = mri_compute_bias_cargs(params, execution)
    out = mri_compute_bias_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_bias(
    subjects: list[str],
    output_volume: str,
    runner: Runner | None = None,
) -> MriComputeBiasOutputs:
    """
    mri_compute_bias

    Compute bias correction volumes for the given subjects and outputs the result to a specified volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subjects for which bias correction is calculated.
        output_volume: Output volume where the result will be stored.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_bias_params(
        subjects=subjects,
        output_volume=output_volume,
    )
    return mri_compute_bias_execute(params, runner)

__all__ = [
    "MriComputeBias",
    "MriComputeBiasOutputs",
    "MRI_COMPUTE_BIAS_METADATA",
    "mri_compute_bias_cargs",
    "mri_compute_bias_outputs",
    "mri_compute_bias_params",
    "mri_compute_bias_execute",
    "mri_compute_bias_validate",
    "mri_compute_bias",
]