# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_TALAIRACH_METADATA = Metadata(
    id="mris_talairach",
    name="mris_talairach",
    package="freesurfer",
)

MrisTalairach = typing.TypedDict(
    "MrisTalairach",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_talairach"]],
        "input_image": InputPathType,
    },
)

@dataclasses.dataclass
class MrisTalairachOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_talairach_params(
    input_image: InputPathType,
) -> MrisTalairach:
    """
    Build parameters.

    Args:
        input_image: Input image file to be transformed into Talairach space

    Returns:
        Parameter dictionary.
    """
    params: MrisTalairach = {
        "@type": "freesurfer/mris_talairach",
        "input_image": input_image,
    }
    return params

def mris_talairach_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisTalairach."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')

def mris_talairach_cargs(params: MrisTalairach, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_talairach")
    cargs.append(execution.input_file(params["input_image"]))
    return cargs

def mris_talairach_outputs(params: MrisTalairach, execution: Execution) -> MrisTalairachOutputs:
    """Build the MrisTalairachOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisTalairachOutputs(
        root=root_v,
    )

def mris_talairach_execute(params: MrisTalairach, runner: Runner | None = None) -> MrisTalairachOutputs:
    """
    mris_talairach

    Transforms an MRI surface into Talairach space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_talairach_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_TALAIRACH_METADATA)
    execution.params(params)
    args = mris_talairach_cargs(params, execution)
    out = mris_talairach_outputs(params, execution)
    execution.run(args)
    return out

def mris_talairach(
    input_image: InputPathType,
    runner: Runner | None = None,
) -> MrisTalairachOutputs:
    """
    mris_talairach

    Transforms an MRI surface into Talairach space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Input image file to be transformed into Talairach space
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_talairach_params(
        input_image=input_image,
    )
    return mris_talairach_execute(params, runner)

__all__ = [
    "MrisTalairach",
    "MrisTalairachOutputs",
    "MRIS_TALAIRACH_METADATA",
    "mris_talairach_cargs",
    "mris_talairach_outputs",
    "mris_talairach_params",
    "mris_talairach_execute",
    "mris_talairach_validate",
    "mris_talairach",
]