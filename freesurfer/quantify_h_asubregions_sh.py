# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QUANTIFY_H_ASUBREGIONS_SH_METADATA = Metadata(
    id="quantifyHAsubregions.sh",
    name="quantifyHAsubregions.sh",
    package="freesurfer",
)

QuantifyHAsubregionsSh = typing.TypedDict(
    "QuantifyHAsubregionsSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/quantifyHAsubregions.sh"]],
        "prefix": str,
        "suffix": str,
        "output_file": str,
        "subjects_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class QuantifyHAsubregionsShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """File containing the quantification results of hippocampal subregions."""

def quantify_h_asubregions_sh_params(
    prefix: str,
    suffix: str,
    output_file: str,
    subjects_directory: str | None = None,
) -> QuantifyHAsubregionsSh:
    """
    Build parameters.

    Args:
        prefix: Prefix for the files to be processed.
        suffix: Suffix for the files to be processed.
        output_file: Output file name to store the results.
        subjects_directory: Directory containing the subject data. If not provided,\
            the current directory is used.

    Returns:
        Parameter dictionary.
    """
    params: QuantifyHAsubregionsSh = {
        "@type": "freesurfer/quantifyHAsubregions.sh",
        "prefix": prefix,
        "suffix": suffix,
        "output_file": output_file,
    }
    if subjects_directory is not None:
        params["subjects_directory"] = subjects_directory
    return params

def quantify_h_asubregions_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid QuantifyHAsubregionsSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("suffix", None) is None:
        raise StyxValidationError("`suffix` must not be None")
    if not isinstance(params["suffix"], str):
        raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("subjects_directory", None) is not None:
        if not isinstance(params["subjects_directory"], str):
            raise StyxValidationError(f'`subjects_directory` has the wrong type: Received `{type(params["subjects_directory"])}` expected `str`')

def quantify_h_asubregions_sh_cargs(params: QuantifyHAsubregionsSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("quantifyHAsubregions.sh")
    cargs.append(params["prefix"])
    cargs.append(params["suffix"])
    cargs.append(params["output_file"])
    v_0 = params.get("subjects_directory")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def quantify_h_asubregions_sh_outputs(params: QuantifyHAsubregionsSh, execution: Execution) -> QuantifyHAsubregionsShOutputs:
    """Build the QuantifyHAsubregionsShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return QuantifyHAsubregionsShOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def quantify_h_asubregions_sh_execute(params: QuantifyHAsubregionsSh, runner: Runner | None = None) -> QuantifyHAsubregionsShOutputs:
    """
    quantifyHAsubregions.sh

    Tool to quantify hippocampal subregions using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    quantify_h_asubregions_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QUANTIFY_H_ASUBREGIONS_SH_METADATA)
    execution.params(params)
    args = quantify_h_asubregions_sh_cargs(params, execution)
    out = quantify_h_asubregions_sh_outputs(params, execution)
    execution.run(args)
    return out

def quantify_h_asubregions_sh(
    prefix: str,
    suffix: str,
    output_file: str,
    subjects_directory: str | None = None,
    runner: Runner | None = None,
) -> QuantifyHAsubregionsShOutputs:
    """
    quantifyHAsubregions.sh

    Tool to quantify hippocampal subregions using FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        prefix: Prefix for the files to be processed.
        suffix: Suffix for the files to be processed.
        output_file: Output file name to store the results.
        subjects_directory: Directory containing the subject data. If not provided,\
            the current directory is used.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = quantify_h_asubregions_sh_params(
        prefix=prefix,
        suffix=suffix,
        output_file=output_file,
        subjects_directory=subjects_directory,
    )
    return quantify_h_asubregions_sh_execute(params, runner)

__all__ = [
    "QuantifyHAsubregionsSh",
    "QuantifyHAsubregionsShOutputs",
    "QUANTIFY_H_ASUBREGIONS_SH_METADATA",
    "quantify_h_asubregions_sh_cargs",
    "quantify_h_asubregions_sh_outputs",
    "quantify_h_asubregions_sh_params",
    "quantify_h_asubregions_sh_execute",
    "quantify_h_asubregions_sh_validate",
    "quantify_h_asubregions_sh",
]