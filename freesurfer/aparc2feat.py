# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APARC2FEAT_METADATA = Metadata(
    id="aparc2feat",
    name="aparc2feat",
    package="freesurfer",
)

Aparc2feat = typing.TypedDict(
    "Aparc2feat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/aparc2feat"]],
        "feat_directories": str,
        "featdirfile": typing.NotRequired[InputPathType],
        "hemi": typing.NotRequired[str],
        "annot": typing.NotRequired[str],
        "annot_a2005s_flag": typing.NotRequired[bool],
        "annot_a2009s_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Aparc2featOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_aparc_output: OutputPathType
    """Output Left Hemisphere aparc in nifti format."""
    rh_aparc_output: OutputPathType
    """Output Right Hemisphere aparc in nifti format."""

def aparc2feat_params(
    feat_directories: str,
    featdirfile: InputPathType | None = None,
    hemi: str | None = None,
    annot: str | None = None,
    annot_a2005s_flag: bool = False,
    annot_a2009s_flag: bool = False,
    debug_flag: bool = False,
    help_flag: bool = False,
    version_flag: bool = False,
) -> Aparc2feat:
    """
    Build parameters.

    Args:
        feat_directories: FEAT output directory. Multiple --feat arguments can be\
            supplied.
        featdirfile: File with a list of FEAT directories. Multiple --featdirfile\
            flags are allowed.
        hemi: Resample hemisphere only (default is both rh and lh).
        annot: Specify something other than aparc.
        annot_a2005s_flag: Specify annotation = aparc.a2005s.
        annot_a2009s_flag: Specify annotation = aparc.a2009s.
        debug_flag: Turn on debugging.
        help_flag: Print help and exit.
        version_flag: Print version and exit.

    Returns:
        Parameter dictionary.
    """
    params: Aparc2feat = {
        "@type": "freesurfer/aparc2feat",
        "feat_directories": feat_directories,
        "annot_a2005s_flag": annot_a2005s_flag,
        "annot_a2009s_flag": annot_a2009s_flag,
        "debug_flag": debug_flag,
        "help_flag": help_flag,
        "version_flag": version_flag,
    }
    if featdirfile is not None:
        params["featdirfile"] = featdirfile
    if hemi is not None:
        params["hemi"] = hemi
    if annot is not None:
        params["annot"] = annot
    return params

def aparc2feat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aparc2feat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_directories", None) is None:
        raise StyxValidationError("`feat_directories` must not be None")
    if not isinstance(params["feat_directories"], str):
        raise StyxValidationError(f'`feat_directories` has the wrong type: Received `{type(params["feat_directories"])}` expected `str`')
    if params.get("featdirfile", None) is not None:
        if not isinstance(params["featdirfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`featdirfile` has the wrong type: Received `{type(params["featdirfile"])}` expected `InputPathType`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("annot", None) is not None:
        if not isinstance(params["annot"], str):
            raise StyxValidationError(f'`annot` has the wrong type: Received `{type(params["annot"])}` expected `str`')
    if params.get("annot_a2005s_flag", None) is not None:
        if not isinstance(params["annot_a2005s_flag"], bool):
            raise StyxValidationError(f'`annot_a2005s_flag` has the wrong type: Received `{type(params["annot_a2005s_flag"])}` expected `bool`')
    if params.get("annot_a2009s_flag", None) is not None:
        if not isinstance(params["annot_a2009s_flag"], bool):
            raise StyxValidationError(f'`annot_a2009s_flag` has the wrong type: Received `{type(params["annot_a2009s_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def aparc2feat_cargs(params: Aparc2feat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aparc2feat")
    cargs.append("--feat")
    cargs.append(params["feat_directories"])
    v_0 = params.get("featdirfile")
    if v_0 is not None:
        cargs.append("--featdirfile")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("hemi")
    if v_1 is not None:
        cargs.append("--hemi")
        cargs.append(v_1)
    v_2 = params.get("annot")
    if v_2 is not None:
        cargs.append("--annot")
        cargs.append(v_2)
    if params.get("annot_a2005s_flag"):
        cargs.append("--a2005s")
    if params.get("annot_a2009s_flag"):
        cargs.append("--a2009s")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("help_flag"):
        cargs.append("--help")
    if params.get("version_flag"):
        cargs.append("--version")
    return cargs

def aparc2feat_outputs(params: Aparc2feat, execution: Execution) -> Aparc2featOutputs:
    """Build the Aparc2featOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    lh_aparc_output_v: OutputPathType = execution.output_file(f'{params["feat_directories"]}/reg/freesurfer/lh.aparc.nii.gz')
    rh_aparc_output_v: OutputPathType = execution.output_file(f'{params["feat_directories"]}/reg/freesurfer/rh.aparc.nii.gz')
    return Aparc2featOutputs(
        root=root_v,
        lh_aparc_output=lh_aparc_output_v,
        rh_aparc_output=rh_aparc_output_v,
    )

def aparc2feat_execute(params: Aparc2feat, runner: Runner | None = None) -> Aparc2featOutputs:
    """
    aparc2feat

    Resamples the FreeSurfer automatic cortical segmentation to the FEAT functional space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aparc2feat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APARC2FEAT_METADATA)
    execution.params(params)
    args = aparc2feat_cargs(params, execution)
    out = aparc2feat_outputs(params, execution)
    execution.run(args)
    return out

def aparc2feat(
    feat_directories: str,
    featdirfile: InputPathType | None = None,
    hemi: str | None = None,
    annot: str | None = None,
    annot_a2005s_flag: bool = False,
    annot_a2009s_flag: bool = False,
    debug_flag: bool = False,
    help_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> Aparc2featOutputs:
    """
    aparc2feat

    Resamples the FreeSurfer automatic cortical segmentation to the FEAT functional space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        feat_directories: FEAT output directory. Multiple --feat arguments can be\
            supplied.
        featdirfile: File with a list of FEAT directories. Multiple --featdirfile\
            flags are allowed.
        hemi: Resample hemisphere only (default is both rh and lh).
        annot: Specify something other than aparc.
        annot_a2005s_flag: Specify annotation = aparc.a2005s.
        annot_a2009s_flag: Specify annotation = aparc.a2009s.
        debug_flag: Turn on debugging.
        help_flag: Print help and exit.
        version_flag: Print version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aparc2feat_params(
        feat_directories=feat_directories,
        featdirfile=featdirfile,
        hemi=hemi,
        annot=annot,
        annot_a2005s_flag=annot_a2005s_flag,
        annot_a2009s_flag=annot_a2009s_flag,
        debug_flag=debug_flag,
        help_flag=help_flag,
        version_flag=version_flag,
    )
    return aparc2feat_execute(params, runner)

__all__ = [
    "Aparc2feat",
    "Aparc2featOutputs",
    "APARC2FEAT_METADATA",
    "aparc2feat_cargs",
    "aparc2feat_outputs",
    "aparc2feat_params",
    "aparc2feat_execute",
    "aparc2feat_validate",
    "aparc2feat",
]