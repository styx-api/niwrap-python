# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKXSUBJREG_METADATA = Metadata(
    id="mkxsubjreg",
    name="mkxsubjreg",
    package="freesurfer",
)

Mkxsubjreg = typing.TypedDict(
    "Mkxsubjreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mkxsubjreg"]],
        "srcreg": InputPathType,
        "targreg": InputPathType,
        "targsubj": typing.NotRequired[str],
        "xfm": typing.NotRequired[str],
        "sd": typing.NotRequired[str],
        "fvol": typing.NotRequired[InputPathType],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MkxsubjregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mkxsubjreg_params(
    srcreg: InputPathType,
    targreg: InputPathType,
    targsubj: str | None = None,
    xfm: str | None = None,
    sd: str | None = None,
    fvol: InputPathType | None = None,
    help: bool = False,
    version: bool = False,
) -> Mkxsubjreg:
    """
    Build parameters.

    Args:
        srcreg: Path to the source registration file (srcreg.dat)
        targreg: Path to the target registration file (targreg.dat)
        targsubj: Target subject ID; default is talairach
        xfm: XFM file name relative to transforms
        sd: Directory containing subject data; default is env SUBJECTS_DIR
        fvol: Path to example functional volume
        help: Display help information
        version: Display version information

    Returns:
        Parameter dictionary.
    """
    params: Mkxsubjreg = {
        "@type": "freesurfer/mkxsubjreg",
        "srcreg": srcreg,
        "targreg": targreg,
        "help": help,
        "version": version,
    }
    if targsubj is not None:
        params["targsubj"] = targsubj
    if xfm is not None:
        params["xfm"] = xfm
    if sd is not None:
        params["sd"] = sd
    if fvol is not None:
        params["fvol"] = fvol
    return params

def mkxsubjreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mkxsubjreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("srcreg", None) is None:
        raise StyxValidationError("`srcreg` must not be None")
    if not isinstance(params["srcreg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`srcreg` has the wrong type: Received `{type(params["srcreg"])}` expected `InputPathType`')
    if params.get("targreg", None) is None:
        raise StyxValidationError("`targreg` must not be None")
    if not isinstance(params["targreg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`targreg` has the wrong type: Received `{type(params["targreg"])}` expected `InputPathType`')
    if params.get("targsubj", None) is not None:
        if not isinstance(params["targsubj"], str):
            raise StyxValidationError(f'`targsubj` has the wrong type: Received `{type(params["targsubj"])}` expected `str`')
    if params.get("xfm", None) is not None:
        if not isinstance(params["xfm"], str):
            raise StyxValidationError(f'`xfm` has the wrong type: Received `{type(params["xfm"])}` expected `str`')
    if params.get("sd", None) is not None:
        if not isinstance(params["sd"], str):
            raise StyxValidationError(f'`sd` has the wrong type: Received `{type(params["sd"])}` expected `str`')
    if params.get("fvol", None) is not None:
        if not isinstance(params["fvol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fvol` has the wrong type: Received `{type(params["fvol"])}` expected `InputPathType`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mkxsubjreg_cargs(params: Mkxsubjreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mkxsubjreg")
    cargs.append("--srcreg")
    cargs.append(execution.input_file(params["srcreg"]))
    cargs.append("--targreg")
    cargs.append(execution.input_file(params["targreg"]))
    v_0 = params.get("targsubj")
    if v_0 is not None:
        cargs.append("--targsubj")
        cargs.append(v_0)
    v_1 = params.get("xfm")
    if v_1 is not None:
        cargs.append("--xfm")
        cargs.append(v_1)
    v_2 = params.get("sd")
    if v_2 is not None:
        cargs.append("--sd")
        cargs.append(v_2)
    v_3 = params.get("fvol")
    if v_3 is not None:
        cargs.append("--fvol")
        cargs.append(execution.input_file(v_3))
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mkxsubjreg_outputs(params: Mkxsubjreg, execution: Execution) -> MkxsubjregOutputs:
    """Build the MkxsubjregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MkxsubjregOutputs(
        root=root_v,
    )

def mkxsubjreg_execute(params: Mkxsubjreg, runner: Runner | None = None) -> MkxsubjregOutputs:
    """
    mkxsubjreg

    Creates a new registration matrix that maps from the functional volume of the source subject to the orig of the target subject through the talairach transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mkxsubjreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKXSUBJREG_METADATA)
    execution.params(params)
    args = mkxsubjreg_cargs(params, execution)
    out = mkxsubjreg_outputs(params, execution)
    execution.run(args)
    return out

def mkxsubjreg(
    srcreg: InputPathType,
    targreg: InputPathType,
    targsubj: str | None = None,
    xfm: str | None = None,
    sd: str | None = None,
    fvol: InputPathType | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MkxsubjregOutputs:
    """
    mkxsubjreg

    Creates a new registration matrix that maps from the functional volume of the source subject to the orig of the target subject through the talairach transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        srcreg: Path to the source registration file (srcreg.dat)
        targreg: Path to the target registration file (targreg.dat)
        targsubj: Target subject ID; default is talairach
        xfm: XFM file name relative to transforms
        sd: Directory containing subject data; default is env SUBJECTS_DIR
        fvol: Path to example functional volume
        help: Display help information
        version: Display version information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mkxsubjreg_params(
        srcreg=srcreg,
        targreg=targreg,
        targsubj=targsubj,
        xfm=xfm,
        sd=sd,
        fvol=fvol,
        help=help,
        version=version,
    )
    return mkxsubjreg_execute(params, runner)

__all__ = [
    "Mkxsubjreg",
    "MkxsubjregOutputs",
    "MKXSUBJREG_METADATA",
    "mkxsubjreg_cargs",
    "mkxsubjreg_outputs",
    "mkxsubjreg_params",
    "mkxsubjreg_execute",
    "mkxsubjreg_validate",
    "mkxsubjreg",
]