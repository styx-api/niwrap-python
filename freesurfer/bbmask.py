# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BBMASK_METADATA = Metadata(
    id="bbmask",
    name="bbmask",
    package="freesurfer",
)

Bbmask = typing.TypedDict(
    "Bbmask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bbmask"]],
        "mask": list[InputPathType],
        "src_volumes": typing.NotRequired[list[InputPathType]],
        "npad": typing.NotRequired[float],
        "registration": typing.NotRequired[list[InputPathType]],
        "regheader": typing.NotRequired[InputPathType],
        "sub2src": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class BbmaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mask_output_file: OutputPathType
    """Output mask volume"""
    src_output_file: OutputPathType
    """Output reduced volume(s)"""
    registration_output_file: OutputPathType
    """Output registration file"""
    regheader_output_file: OutputPathType
    """Output registration file from regheader"""
    sub2src_output_file: OutputPathType
    """Output file for sub2src registration"""

def bbmask_params(
    mask: list[InputPathType],
    src_volumes: list[InputPathType] | None = None,
    npad: float | None = None,
    registration: list[InputPathType] | None = None,
    regheader: InputPathType | None = None,
    sub2src: InputPathType | None = None,
) -> Bbmask:
    """
    Build parameters.

    Args:
        mask: Input and output for the mask volume.
        src_volumes: Input and output volumes to be reduced to the bounding box.
        npad: Number of voxels to expand the bounding box.
        registration: Input and output registration files.
        regheader: Output registration file from header.
        sub2src: Output file for sub-source registration.

    Returns:
        Parameter dictionary.
    """
    params: Bbmask = {
        "@type": "freesurfer/bbmask",
        "mask": mask,
    }
    if src_volumes is not None:
        params["src_volumes"] = src_volumes
    if npad is not None:
        params["npad"] = npad
    if registration is not None:
        params["registration"] = registration
    if regheader is not None:
        params["regheader"] = regheader
    if sub2src is not None:
        params["sub2src"] = sub2src
    return params

def bbmask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bbmask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], list):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `list[InputPathType]`')
    if not (2 <= len(params["mask"]) <= 2):
        raise StyxValidationError("Parameter `mask` must contain between 2 and 2 elements (inclusive)")
    for e in params["mask"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("src_volumes", None) is not None:
        if not isinstance(params["src_volumes"], list):
            raise StyxValidationError(f'`src_volumes` has the wrong type: Received `{type(params["src_volumes"])}` expected `list[InputPathType]`')
        if len(params["src_volumes"]) < 2:
            raise StyxValidationError("Parameter `src_volumes` must contain at least 2 elements")
        for e_2 in params["src_volumes"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`src_volumes` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("npad", None) is not None:
        if not isinstance(params["npad"], (float, int)):
            raise StyxValidationError(f'`npad` has the wrong type: Received `{type(params["npad"])}` expected `float`')
    if params.get("registration", None) is not None:
        if not isinstance(params["registration"], list):
            raise StyxValidationError(f'`registration` has the wrong type: Received `{type(params["registration"])}` expected `list[InputPathType]`')
        if not (2 <= len(params["registration"]) <= 2):
            raise StyxValidationError("Parameter `registration` must contain between 2 and 2 elements (inclusive)")
        for e_3 in params["registration"]:
            if not isinstance(e_3, (pathlib.Path, str)):
                raise StyxValidationError(f'`registration` has the wrong type: Received `{type(e_3)}` expected `list[InputPathType]`')
    if params.get("regheader", None) is not None:
        if not isinstance(params["regheader"], (pathlib.Path, str)):
            raise StyxValidationError(f'`regheader` has the wrong type: Received `{type(params["regheader"])}` expected `InputPathType`')
    if params.get("sub2src", None) is not None:
        if not isinstance(params["sub2src"], (pathlib.Path, str)):
            raise StyxValidationError(f'`sub2src` has the wrong type: Received `{type(params["sub2src"])}` expected `InputPathType`')

def bbmask_cargs(params: Bbmask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bbmask")
    cargs.append("--mask")
    for item0 in params["mask"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("src_volumes")
    if v_0 is not None:
        cargs.append("--src")
        for item1 in v_0:
            cargs.append(execution.input_file(item1))
    v_1 = params.get("npad")
    if v_1 is not None:
        cargs.append("--npad")
        cargs.append(str(v_1))
    v_2 = params.get("registration")
    if v_2 is not None:
        cargs.append("--reg")
        for item2 in v_2:
            cargs.append(execution.input_file(item2))
    v_3 = params.get("regheader")
    if v_3 is not None:
        cargs.append("--regheader")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("sub2src")
    if v_4 is not None:
        cargs.append("--sub2src")
        cargs.append(execution.input_file(v_4))
    return cargs

def bbmask_outputs(params: Bbmask, execution: Execution) -> BbmaskOutputs:
    """Build the BbmaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    mask_output_file_v: OutputPathType = execution.output_file("[MASK_OUTPUT]")
    src_output_file_v: OutputPathType = execution.output_file("[SRC_OUTPUT]")
    registration_output_file_v: OutputPathType = execution.output_file("[REG_OUTPUT]")
    regheader_output_file_v: OutputPathType = execution.output_file("[REG_HEADER_OUTPUT]")
    sub2src_output_file_v: OutputPathType = execution.output_file("[SUB2SRC_OUTPUT]")
    return BbmaskOutputs(
        root=root_v,
        mask_output_file=mask_output_file_v,
        src_output_file=src_output_file_v,
        registration_output_file=registration_output_file_v,
        regheader_output_file=regheader_output_file_v,
        sub2src_output_file=sub2src_output_file_v,
    )

def bbmask_execute(params: Bbmask, runner: Runner | None = None) -> BbmaskOutputs:
    """
    bbmask

    Tool to create a volume with a smaller field of view by creating a bounding box that encompasses a mask.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bbmask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BBMASK_METADATA)
    execution.params(params)
    args = bbmask_cargs(params, execution)
    out = bbmask_outputs(params, execution)
    execution.run(args)
    return out

def bbmask(
    mask: list[InputPathType],
    src_volumes: list[InputPathType] | None = None,
    npad: float | None = None,
    registration: list[InputPathType] | None = None,
    regheader: InputPathType | None = None,
    sub2src: InputPathType | None = None,
    runner: Runner | None = None,
) -> BbmaskOutputs:
    """
    bbmask

    Tool to create a volume with a smaller field of view by creating a bounding box that encompasses a mask.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mask: Input and output for the mask volume.
        src_volumes: Input and output volumes to be reduced to the bounding box.
        npad: Number of voxels to expand the bounding box.
        registration: Input and output registration files.
        regheader: Output registration file from header.
        sub2src: Output file for sub-source registration.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bbmask_params(
        mask=mask,
        src_volumes=src_volumes,
        npad=npad,
        registration=registration,
        regheader=regheader,
        sub2src=sub2src,
    )
    return bbmask_execute(params, runner)

__all__ = [
    "Bbmask",
    "BbmaskOutputs",
    "BBMASK_METADATA",
    "bbmask_cargs",
    "bbmask_outputs",
    "bbmask_params",
    "bbmask_execute",
    "bbmask_validate",
    "bbmask",
]