# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GCAINIT_METADATA = Metadata(
    id="gcainit",
    name="gcainit",
    package="freesurfer",
)

Gcainit = typing.TypedDict(
    "Gcainit",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/gcainit"]],
        "gcadir": str,
    },
)

@dataclasses.dataclass
class GcainitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def gcainit_params(
    gcadir: str,
) -> Gcainit:
    """
    Build parameters.

    Args:
        gcadir:

    Returns:
        Parameter dictionary.
    """
    params: Gcainit = {
        "@type": "freesurfer/gcainit",
        "gcadir": gcadir,
    }
    return params

def gcainit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gcainit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gcadir", None) is None:
        raise StyxValidationError("`gcadir` must not be None")
    if not isinstance(params["gcadir"], str):
        raise StyxValidationError(f'`gcadir` has the wrong type: Received `{type(params["gcadir"])}` expected `str`')

def gcainit_cargs(params: Gcainit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gcainit")
    cargs.append("--g")
    cargs.append(params["gcadir"])
    return cargs

def gcainit_outputs(params: Gcainit, execution: Execution) -> GcainitOutputs:
    """Build the GcainitOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GcainitOutputs(
        root=root_v,
    )

def gcainit_execute(params: Gcainit, runner: Runner | None = None) -> GcainitOutputs:
    """
    gcainit

    Initializes the GCA for brain processing tasks.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gcainit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GCAINIT_METADATA)
    execution.params(params)
    args = gcainit_cargs(params, execution)
    out = gcainit_outputs(params, execution)
    execution.run(args)
    return out

def gcainit(
    gcadir: str,
    runner: Runner | None = None,
) -> GcainitOutputs:
    """
    gcainit

    Initializes the GCA for brain processing tasks.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        gcadir:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gcainit_params(
        gcadir=gcadir,
    )
    return gcainit_execute(params, runner)

__all__ = [
    "Gcainit",
    "GcainitOutputs",
    "GCAINIT_METADATA",
    "gcainit_cargs",
    "gcainit_outputs",
    "gcainit_params",
    "gcainit_execute",
    "gcainit_validate",
    "gcainit",
]