# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIX_SUBJECT_METADATA = Metadata(
    id="fix_subject",
    name="fix_subject",
    package="freesurfer",
)

FixSubject = typing.TypedDict(
    "FixSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fix_subject"]],
        "arguments": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FixSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fix_subject_params(
    arguments: str | None = None,
) -> FixSubject:
    """
    Build parameters.

    Args:
        arguments: Arguments for fix_subject command. Currently, help output shows\
            directory errors. Ensure the correct paths are used.

    Returns:
        Parameter dictionary.
    """
    params: FixSubject = {
        "@type": "freesurfer/fix_subject",
    }
    if arguments is not None:
        params["arguments"] = arguments
    return params

def fix_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("arguments", None) is not None:
        if not isinstance(params["arguments"], str):
            raise StyxValidationError(f'`arguments` has the wrong type: Received `{type(params["arguments"])}` expected `str`')

def fix_subject_cargs(params: FixSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fix_subject")
    v_0 = params.get("arguments")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fix_subject_outputs(params: FixSubject, execution: Execution) -> FixSubjectOutputs:
    """Build the FixSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixSubjectOutputs(
        root=root_v,
    )

def fix_subject_execute(params: FixSubject, runner: Runner | None = None) -> FixSubjectOutputs:
    """
    fix_subject

    Tool to fix subjects in FreeSurfer, encountered errors due to incorrect path handling.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fix_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIX_SUBJECT_METADATA)
    execution.params(params)
    args = fix_subject_cargs(params, execution)
    out = fix_subject_outputs(params, execution)
    execution.run(args)
    return out

def fix_subject(
    arguments: str | None = None,
    runner: Runner | None = None,
) -> FixSubjectOutputs:
    """
    fix_subject

    Tool to fix subjects in FreeSurfer, encountered errors due to incorrect path handling.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        arguments: Arguments for fix_subject command. Currently, help output shows\
            directory errors. Ensure the correct paths are used.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fix_subject_params(
        arguments=arguments,
    )
    return fix_subject_execute(params, runner)

__all__ = [
    "FixSubject",
    "FixSubjectOutputs",
    "FIX_SUBJECT_METADATA",
    "fix_subject_cargs",
    "fix_subject_outputs",
    "fix_subject_params",
    "fix_subject_execute",
    "fix_subject_validate",
    "fix_subject",
]