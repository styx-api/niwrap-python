# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_IMAGE2VTK_METADATA = Metadata(
    id="mris_image2vtk",
    name="mris_image2vtk",
    package="freesurfer",
)

MrisImage2vtk = typing.TypedDict(
    "MrisImage2vtk",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_image2vtk"]],
        "input_filename": InputPathType,
        "output_filename": str,
        "lower_threshold": float,
        "upper_threshold": float,
        "vtk_smoothing_iters": float,
        "image_smoothing_size": float,
        "reduction_percent": float,
    },
)

@dataclasses.dataclass
class MrisImage2vtkOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_vtk_file: OutputPathType
    """Output VTK file generated by the conversion process"""

def mris_image2vtk_params(
    input_filename: InputPathType,
    output_filename: str,
    lower_threshold: float,
    upper_threshold: float,
    vtk_smoothing_iters: float,
    image_smoothing_size: float,
    reduction_percent: float,
) -> MrisImage2vtk:
    """
    Build parameters.

    Args:
        input_filename: Input image file name
        output_filename: Output VTK file name
        lower_threshold: Lower threshold for image conversion
        upper_threshold: Upper threshold for image conversion
        vtk_smoothing_iters: Number of VTK smoothing iterations
        image_smoothing_size: Image smoothing size
        reduction_percent: Reduction percentage for the conversion

    Returns:
        Parameter dictionary.
    """
    params: MrisImage2vtk = {
        "@type": "freesurfer/mris_image2vtk",
        "input_filename": input_filename,
        "output_filename": output_filename,
        "lower_threshold": lower_threshold,
        "upper_threshold": upper_threshold,
        "vtk_smoothing_iters": vtk_smoothing_iters,
        "image_smoothing_size": image_smoothing_size,
        "reduction_percent": reduction_percent,
    }
    return params

def mris_image2vtk_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisImage2vtk."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_filename", None) is None:
        raise StyxValidationError("`input_filename` must not be None")
    if not isinstance(params["input_filename"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_filename` has the wrong type: Received `{type(params["input_filename"])}` expected `InputPathType`')
    if params.get("output_filename", None) is None:
        raise StyxValidationError("`output_filename` must not be None")
    if not isinstance(params["output_filename"], str):
        raise StyxValidationError(f'`output_filename` has the wrong type: Received `{type(params["output_filename"])}` expected `str`')
    if params.get("lower_threshold", None) is None:
        raise StyxValidationError("`lower_threshold` must not be None")
    if not isinstance(params["lower_threshold"], (float, int)):
        raise StyxValidationError(f'`lower_threshold` has the wrong type: Received `{type(params["lower_threshold"])}` expected `float`')
    if params.get("upper_threshold", None) is None:
        raise StyxValidationError("`upper_threshold` must not be None")
    if not isinstance(params["upper_threshold"], (float, int)):
        raise StyxValidationError(f'`upper_threshold` has the wrong type: Received `{type(params["upper_threshold"])}` expected `float`')
    if params.get("vtk_smoothing_iters", None) is None:
        raise StyxValidationError("`vtk_smoothing_iters` must not be None")
    if not isinstance(params["vtk_smoothing_iters"], (float, int)):
        raise StyxValidationError(f'`vtk_smoothing_iters` has the wrong type: Received `{type(params["vtk_smoothing_iters"])}` expected `float`')
    if params.get("image_smoothing_size", None) is None:
        raise StyxValidationError("`image_smoothing_size` must not be None")
    if not isinstance(params["image_smoothing_size"], (float, int)):
        raise StyxValidationError(f'`image_smoothing_size` has the wrong type: Received `{type(params["image_smoothing_size"])}` expected `float`')
    if params.get("reduction_percent", None) is None:
        raise StyxValidationError("`reduction_percent` must not be None")
    if not isinstance(params["reduction_percent"], (float, int)):
        raise StyxValidationError(f'`reduction_percent` has the wrong type: Received `{type(params["reduction_percent"])}` expected `float`')

def mris_image2vtk_cargs(params: MrisImage2vtk, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_image2vtk")
    cargs.append(execution.input_file(params["input_filename"]))
    cargs.append(params["output_filename"])
    cargs.append(str(params["lower_threshold"]))
    cargs.append(str(params["upper_threshold"]))
    cargs.append(str(params["vtk_smoothing_iters"]))
    cargs.append(str(params["image_smoothing_size"]))
    cargs.append(str(params["reduction_percent"]))
    return cargs

def mris_image2vtk_outputs(params: MrisImage2vtk, execution: Execution) -> MrisImage2vtkOutputs:
    """Build the MrisImage2vtkOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_vtk_file_v: OutputPathType = execution.output_file(params["output_filename"])
    return MrisImage2vtkOutputs(
        root=root_v,
        output_vtk_file=output_vtk_file_v,
    )

def mris_image2vtk_execute(params: MrisImage2vtk, runner: Runner | None = None) -> MrisImage2vtkOutputs:
    """
    mris_image2vtk

    Convert image to VTK format with specified thresholds and smoothing parameters

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_image2vtk_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_IMAGE2VTK_METADATA)
    execution.params(params)
    args = mris_image2vtk_cargs(params, execution)
    out = mris_image2vtk_outputs(params, execution)
    execution.run(args)
    return out

def mris_image2vtk(
    input_filename: InputPathType,
    output_filename: str,
    lower_threshold: float,
    upper_threshold: float,
    vtk_smoothing_iters: float,
    image_smoothing_size: float,
    reduction_percent: float,
    runner: Runner | None = None,
) -> MrisImage2vtkOutputs:
    """
    mris_image2vtk

    Convert image to VTK format with specified thresholds and smoothing parameters

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_filename: Input image file name
        output_filename: Output VTK file name
        lower_threshold: Lower threshold for image conversion
        upper_threshold: Upper threshold for image conversion
        vtk_smoothing_iters: Number of VTK smoothing iterations
        image_smoothing_size: Image smoothing size
        reduction_percent: Reduction percentage for the conversion
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_image2vtk_params(
        input_filename=input_filename,
        output_filename=output_filename,
        lower_threshold=lower_threshold,
        upper_threshold=upper_threshold,
        vtk_smoothing_iters=vtk_smoothing_iters,
        image_smoothing_size=image_smoothing_size,
        reduction_percent=reduction_percent,
    )
    return mris_image2vtk_execute(params, runner)

__all__ = [
    "MrisImage2vtk",
    "MrisImage2vtkOutputs",
    "MRIS_IMAGE2VTK_METADATA",
    "mris_image2vtk_cargs",
    "mris_image2vtk_outputs",
    "mris_image2vtk_params",
    "mris_image2vtk_execute",
    "mris_image2vtk_validate",
    "mris_image2vtk",
]