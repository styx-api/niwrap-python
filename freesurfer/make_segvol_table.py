# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_SEGVOL_TABLE_METADATA = Metadata(
    id="make-segvol-table",
    name="make-segvol-table",
    package="freesurfer",
)

MakeSegvolTable = typing.TypedDict(
    "MakeSegvolTable",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make-segvol-table"]],
        "subjects": list[str],
        "subject_file": InputPathType,
        "outfile": str,
        "idmap": typing.NotRequired[InputPathType],
        "structure_ids": typing.NotRequired[list[str]],
        "segdir": typing.NotRequired[str],
        "subjects_dir": typing.NotRequired[str],
        "umask": typing.NotRequired[str],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MakeSegvolTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_table: OutputPathType
    """Output text file containing the table of subcortical structure volumes."""

def make_segvol_table_params(
    subjects: list[str],
    subject_file: InputPathType,
    outfile: str,
    idmap: InputPathType | None = None,
    structure_ids: list[str] | None = None,
    segdir: str | None = None,
    subjects_dir: str | None = None,
    umask: str | None = None,
    version: bool = False,
    help: bool = False,
) -> MakeSegvolTable:
    """
    Build parameters.

    Args:
        subjects: List of subject IDs. Each subject should be specified with a\
            separate -s flag.
        subject_file: Path to a file containing a list of subjects.
        outfile: Output file where the table will be saved.
        idmap: File with structure name and id number. Default is\
            FREESURFER_HOME/tkmeditColorsCMA.
        structure_ids: Names of structures to include in the table. Defaults to all\
            structures.
        segdir: Segmentation subdirectory name. Default is 'aseg'.
        subjects_dir: Path to the subjects directory. Default is SUBJECTS_DIR\
            environment variable.
        umask: Set UNIX file permission mask.
        version: Print version and exit.
        help: Display help information.

    Returns:
        Parameter dictionary.
    """
    params: MakeSegvolTable = {
        "@type": "freesurfer/make-segvol-table",
        "subjects": subjects,
        "subject_file": subject_file,
        "outfile": outfile,
        "version": version,
        "help": help,
    }
    if idmap is not None:
        params["idmap"] = idmap
    if structure_ids is not None:
        params["structure_ids"] = structure_ids
    if segdir is not None:
        params["segdir"] = segdir
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if umask is not None:
        params["umask"] = umask
    return params

def make_segvol_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeSegvolTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subject_file", None) is None:
        raise StyxValidationError("`subject_file` must not be None")
    if not isinstance(params["subject_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subject_file` has the wrong type: Received `{type(params["subject_file"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("idmap", None) is not None:
        if not isinstance(params["idmap"], (pathlib.Path, str)):
            raise StyxValidationError(f'`idmap` has the wrong type: Received `{type(params["idmap"])}` expected `InputPathType`')
    if params.get("structure_ids", None) is not None:
        if not isinstance(params["structure_ids"], list):
            raise StyxValidationError(f'`structure_ids` has the wrong type: Received `{type(params["structure_ids"])}` expected `list[str]`')
        for e_2 in params["structure_ids"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`structure_ids` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("segdir", None) is not None:
        if not isinstance(params["segdir"], str):
            raise StyxValidationError(f'`segdir` has the wrong type: Received `{type(params["segdir"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def make_segvol_table_cargs(params: MakeSegvolTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make-segvol-table")
    cargs.append("-s")
    for item0 in params["subjects"]:
        cargs.append(item0)
    cargs.append("-sf")
    cargs.append(execution.input_file(params["subject_file"]))
    cargs.append("-o")
    cargs.append(params["outfile"])
    v_0 = params.get("idmap")
    if v_0 is not None:
        cargs.append("-idmap")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("structure_ids")
    if v_1 is not None:
        cargs.append("-id")
        for item1 in v_1:
            cargs.append(item1)
    v_2 = params.get("segdir")
    if v_2 is not None:
        cargs.append("-segdir")
        cargs.append(v_2)
    v_3 = params.get("subjects_dir")
    if v_3 is not None:
        cargs.append("-sd")
        cargs.append(v_3)
    v_4 = params.get("umask")
    if v_4 is not None:
        cargs.append("-umask")
        cargs.append(v_4)
    if params.get("version"):
        cargs.append("-version")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def make_segvol_table_outputs(params: MakeSegvolTable, execution: Execution) -> MakeSegvolTableOutputs:
    """Build the MakeSegvolTableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_table_v: OutputPathType = execution.output_file(params["outfile"])
    return MakeSegvolTableOutputs(
        root=root_v,
        output_table=output_table_v,
    )

def make_segvol_table_execute(params: MakeSegvolTable, runner: Runner | None = None) -> MakeSegvolTableOutputs:
    """
    make-segvol-table

    Creates a table of volumes of subcortical structures for a given list of subjects using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_segvol_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_SEGVOL_TABLE_METADATA)
    execution.params(params)
    args = make_segvol_table_cargs(params, execution)
    out = make_segvol_table_outputs(params, execution)
    execution.run(args)
    return out

def make_segvol_table(
    subjects: list[str],
    subject_file: InputPathType,
    outfile: str,
    idmap: InputPathType | None = None,
    structure_ids: list[str] | None = None,
    segdir: str | None = None,
    subjects_dir: str | None = None,
    umask: str | None = None,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MakeSegvolTableOutputs:
    """
    make-segvol-table

    Creates a table of volumes of subcortical structures for a given list of subjects using FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subject IDs. Each subject should be specified with a\
            separate -s flag.
        subject_file: Path to a file containing a list of subjects.
        outfile: Output file where the table will be saved.
        idmap: File with structure name and id number. Default is\
            FREESURFER_HOME/tkmeditColorsCMA.
        structure_ids: Names of structures to include in the table. Defaults to all\
            structures.
        segdir: Segmentation subdirectory name. Default is 'aseg'.
        subjects_dir: Path to the subjects directory. Default is SUBJECTS_DIR\
            environment variable.
        umask: Set UNIX file permission mask.
        version: Print version and exit.
        help: Display help information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_segvol_table_params(
        subjects=subjects,
        subject_file=subject_file,
        outfile=outfile,
        idmap=idmap,
        structure_ids=structure_ids,
        segdir=segdir,
        subjects_dir=subjects_dir,
        umask=umask,
        version=version,
        help=help,
    )
    return make_segvol_table_execute(params, runner)

__all__ = [
    "MakeSegvolTable",
    "MakeSegvolTableOutputs",
    "MAKE_SEGVOL_TABLE_METADATA",
    "make_segvol_table_cargs",
    "make_segvol_table_outputs",
    "make_segvol_table_params",
    "make_segvol_table_execute",
    "make_segvol_table_validate",
    "make_segvol_table",
]