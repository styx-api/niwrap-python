# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_CORTEX_LABEL_METADATA = Metadata(
    id="make_cortex_label",
    name="make_cortex_label",
    package="freesurfer",
)

MakeCortexLabel = typing.TypedDict(
    "MakeCortexLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_cortex_label"]],
        "subject": str,
        "hemi": typing.NotRequired[str],
        "use_a2009s": typing.NotRequired[bool],
        "output_name": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MakeCortexLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_output_label_file: OutputPathType | None
    """The left hemisphere output cortex label file."""
    rh_output_label_file: OutputPathType | None
    """The right hemisphere output cortex label file."""

def make_cortex_label_params(
    subject: str,
    hemi: str | None = None,
    use_a2009s: bool = False,
    output_name: str | None = None,
) -> MakeCortexLabel:
    """
    Build parameters.

    Args:
        subject: The subject for which the cortex label is to be created.
        hemi: The hemisphere(s) on which to operate. Default is both hemispheres.
        use_a2009s: Use aparc.a2009 instead of aparc.
        output_name: Output name. The output will be ?h.outname.label. Default is\
            'cortex'.

    Returns:
        Parameter dictionary.
    """
    params: MakeCortexLabel = {
        "@type": "freesurfer/make_cortex_label",
        "subject": subject,
        "use_a2009s": use_a2009s,
    }
    if hemi is not None:
        params["hemi"] = hemi
    if output_name is not None:
        params["output_name"] = output_name
    return params

def make_cortex_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeCortexLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("use_a2009s", None) is not None:
        if not isinstance(params["use_a2009s"], bool):
            raise StyxValidationError(f'`use_a2009s` has the wrong type: Received `{type(params["use_a2009s"])}` expected `bool`')
    if params.get("output_name", None) is not None:
        if not isinstance(params["output_name"], str):
            raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')

def make_cortex_label_cargs(params: MakeCortexLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_cortex_label")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("hemi")
    if v_0 is not None:
        cargs.append("--h")
        cargs.append(v_0)
    if params.get("use_a2009s"):
        cargs.append("--a2009s")
    v_1 = params.get("output_name")
    if v_1 is not None:
        cargs.append("--o")
        cargs.append(v_1)
    return cargs

def make_cortex_label_outputs(params: MakeCortexLabel, execution: Execution) -> MakeCortexLabelOutputs:
    """Build the MakeCortexLabelOutputs object for this tool."""
    lh_output_label_file_v: OutputPathType | None = None
    rh_output_label_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_name")
    if __v0 is not None:
        lh_output_label_file_v = execution.output_file(f'lh.{(__v0 if __v0 is not None else "")}.label')
    __v1 = params.get("output_name")
    if __v1 is not None:
        rh_output_label_file_v = execution.output_file(f'rh.{(__v1 if __v1 is not None else "")}.label')
    return MakeCortexLabelOutputs(
        root=root_v,
        lh_output_label_file=lh_output_label_file_v,
        rh_output_label_file=rh_output_label_file_v,
    )

def make_cortex_label_execute(params: MakeCortexLabel, runner: Runner | None = None) -> MakeCortexLabelOutputs:
    """
    make_cortex_label

    A tool to create cortical labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_cortex_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_CORTEX_LABEL_METADATA)
    execution.params(params)
    args = make_cortex_label_cargs(params, execution)
    out = make_cortex_label_outputs(params, execution)
    execution.run(args)
    return out

def make_cortex_label(
    subject: str,
    hemi: str | None = None,
    use_a2009s: bool = False,
    output_name: str | None = None,
    runner: Runner | None = None,
) -> MakeCortexLabelOutputs:
    """
    make_cortex_label

    A tool to create cortical labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject for which the cortex label is to be created.
        hemi: The hemisphere(s) on which to operate. Default is both hemispheres.
        use_a2009s: Use aparc.a2009 instead of aparc.
        output_name: Output name. The output will be ?h.outname.label. Default is\
            'cortex'.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_cortex_label_params(
        subject=subject,
        hemi=hemi,
        use_a2009s=use_a2009s,
        output_name=output_name,
    )
    return make_cortex_label_execute(params, runner)

__all__ = [
    "MakeCortexLabel",
    "MakeCortexLabelOutputs",
    "MAKE_CORTEX_LABEL_METADATA",
    "make_cortex_label_cargs",
    "make_cortex_label_outputs",
    "make_cortex_label_params",
    "make_cortex_label_execute",
    "make_cortex_label_validate",
    "make_cortex_label",
]