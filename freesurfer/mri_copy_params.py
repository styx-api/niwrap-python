# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COPY_PARAMS_METADATA = Metadata(
    id="mri_copy_params",
    name="mri_copy_params",
    package="freesurfer",
)

MriCopyParams = typing.TypedDict(
    "MriCopyParams",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_copy_params"]],
        "in_vol": InputPathType,
        "template_vol": InputPathType,
        "out_vol": str,
        "size_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCopyParamsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Output volume file with parameters copied."""

def mri_copy_params_params(
    in_vol: InputPathType,
    template_vol: InputPathType,
    out_vol: str,
    size_flag: bool = False,
) -> MriCopyParams:
    """
    Build parameters.

    Args:
        in_vol: Input volume file whose parameters will be replaced.
        template_vol: Template volume file whose parameters will be copied.
        out_vol: Output volume file with replaced parameters.
        size_flag: Force copying of voxel sizes when resolutions vary.

    Returns:
        Parameter dictionary.
    """
    params: MriCopyParams = {
        "@type": "freesurfer/mri_copy_params",
        "in_vol": in_vol,
        "template_vol": template_vol,
        "out_vol": out_vol,
        "size_flag": size_flag,
    }
    return params

def mri_copy_params_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCopyParams."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_vol", None) is None:
        raise StyxValidationError("`in_vol` must not be None")
    if not isinstance(params["in_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_vol` has the wrong type: Received `{type(params["in_vol"])}` expected `InputPathType`')
    if params.get("template_vol", None) is None:
        raise StyxValidationError("`template_vol` must not be None")
    if not isinstance(params["template_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_vol` has the wrong type: Received `{type(params["template_vol"])}` expected `InputPathType`')
    if params.get("out_vol", None) is None:
        raise StyxValidationError("`out_vol` must not be None")
    if not isinstance(params["out_vol"], str):
        raise StyxValidationError(f'`out_vol` has the wrong type: Received `{type(params["out_vol"])}` expected `str`')
    if params.get("size_flag", None) is not None:
        if not isinstance(params["size_flag"], bool):
            raise StyxValidationError(f'`size_flag` has the wrong type: Received `{type(params["size_flag"])}` expected `bool`')

def mri_copy_params_cargs(params: MriCopyParams, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_copy_params")
    cargs.append(execution.input_file(params["in_vol"]))
    cargs.append(execution.input_file(params["template_vol"]))
    cargs.append(params["out_vol"])
    if params.get("size_flag"):
        cargs.append("--size")
    return cargs

def mri_copy_params_outputs(params: MriCopyParams, execution: Execution) -> MriCopyParamsOutputs:
    """Build the MriCopyParamsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["out_vol"])
    return MriCopyParamsOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def mri_copy_params_execute(params: MriCopyParams, runner: Runner | None = None) -> MriCopyParamsOutputs:
    """
    mri_copy_params

    A tool to replace the volume parameters of an input volume with those of a template volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_copy_params_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COPY_PARAMS_METADATA)
    execution.params(params)
    args = mri_copy_params_cargs(params, execution)
    out = mri_copy_params_outputs(params, execution)
    execution.run(args)
    return out

def mri_copy_params(
    in_vol: InputPathType,
    template_vol: InputPathType,
    out_vol: str,
    size_flag: bool = False,
    runner: Runner | None = None,
) -> MriCopyParamsOutputs:
    """
    mri_copy_params

    A tool to replace the volume parameters of an input volume with those of a template volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        in_vol: Input volume file whose parameters will be replaced.
        template_vol: Template volume file whose parameters will be copied.
        out_vol: Output volume file with replaced parameters.
        size_flag: Force copying of voxel sizes when resolutions vary.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_copy_params_params(
        in_vol=in_vol,
        template_vol=template_vol,
        out_vol=out_vol,
        size_flag=size_flag,
    )
    return mri_copy_params_execute(params, runner)

__all__ = [
    "MriCopyParams",
    "MriCopyParamsOutputs",
    "MRI_COPY_PARAMS_METADATA",
    "mri_copy_params_cargs",
    "mri_copy_params_outputs",
    "mri_copy_params_params",
    "mri_copy_params_execute",
    "mri_copy_params_validate",
    "mri_copy_params",
]