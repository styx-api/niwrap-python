# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXUP_MNI_PATHS_METADATA = Metadata(
    id="fixup_mni_paths",
    name="fixup_mni_paths",
    package="freesurfer",
)

FixupMniPaths = typing.TypedDict(
    "FixupMniPaths",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fixup_mni_paths"]],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FixupMniPathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    autocrop_backup: OutputPathType
    """Original file backup for autocrop"""
    field2imp_backup: OutputPathType
    """Original file backup for field2imp"""
    imp2field_backup: OutputPathType
    """Original file backup for imp2field"""
    make_template_backup: OutputPathType
    """Original file backup for make_template"""
    mritoself_backup: OutputPathType
    """Original file backup for mritoself"""
    mritotal_backup: OutputPathType
    """Original file backup for mritotal"""
    nu_correct_backup: OutputPathType
    """Original file backup for nu_correct"""
    nu_estimate_backup: OutputPathType
    """Original file backup for nu_estimate"""
    nu_estimate_np_and_em_backup: OutputPathType
    """Original file backup for nu_estimate_np_and_em"""
    nu_evaluate_backup: OutputPathType
    """Original file backup for nu_evaluate"""
    resample_labels_backup: OutputPathType
    """Original file backup for resample_labels"""
    sharpen_volume_backup: OutputPathType
    """Original file backup for sharpen_volume"""
    xfmtool_backup: OutputPathType
    """Original file backup for xfmtool"""

def fixup_mni_paths_params(
    verbose: bool = False,
) -> FixupMniPaths:
    """
    Build parameters.

    Args:
        verbose: Provide verbose output during the patch process

    Returns:
        Parameter dictionary.
    """
    params: FixupMniPaths = {
        "@type": "freesurfer/fixup_mni_paths",
        "verbose": verbose,
    }
    return params

def fixup_mni_paths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixupMniPaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def fixup_mni_paths_cargs(params: FixupMniPaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixup_mni_paths")
    if params.get("verbose"):
        cargs.append("-v")
    return cargs

def fixup_mni_paths_outputs(params: FixupMniPaths, execution: Execution) -> FixupMniPathsOutputs:
    """Build the FixupMniPathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    autocrop_backup_v: OutputPathType = execution.output_file("autocrop.old")
    field2imp_backup_v: OutputPathType = execution.output_file("field2imp.old")
    imp2field_backup_v: OutputPathType = execution.output_file("imp2field.old")
    make_template_backup_v: OutputPathType = execution.output_file("make_template.old")
    mritoself_backup_v: OutputPathType = execution.output_file("mritoself.old")
    mritotal_backup_v: OutputPathType = execution.output_file("mritotal.old")
    nu_correct_backup_v: OutputPathType = execution.output_file("nu_correct.old")
    nu_estimate_backup_v: OutputPathType = execution.output_file("nu_estimate.old")
    nu_estimate_np_and_em_backup_v: OutputPathType = execution.output_file("nu_estimate_np_and_em.old")
    nu_evaluate_backup_v: OutputPathType = execution.output_file("nu_evaluate.old")
    resample_labels_backup_v: OutputPathType = execution.output_file("resample_labels.old")
    sharpen_volume_backup_v: OutputPathType = execution.output_file("sharpen_volume.old")
    xfmtool_backup_v: OutputPathType = execution.output_file("xfmtool.old")
    return FixupMniPathsOutputs(
        root=root_v,
        autocrop_backup=autocrop_backup_v,
        field2imp_backup=field2imp_backup_v,
        imp2field_backup=imp2field_backup_v,
        make_template_backup=make_template_backup_v,
        mritoself_backup=mritoself_backup_v,
        mritotal_backup=mritotal_backup_v,
        nu_correct_backup=nu_correct_backup_v,
        nu_estimate_backup=nu_estimate_backup_v,
        nu_estimate_np_and_em_backup=nu_estimate_np_and_em_backup_v,
        nu_evaluate_backup=nu_evaluate_backup_v,
        resample_labels_backup=resample_labels_backup_v,
        sharpen_volume_backup=sharpen_volume_backup_v,
        xfmtool_backup=xfmtool_backup_v,
    )

def fixup_mni_paths_execute(params: FixupMniPaths, runner: Runner | None = None) -> FixupMniPathsOutputs:
    """
    fixup_mni_paths

    A utility for patching MNI tools to ensure correct paths are used.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixup_mni_paths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXUP_MNI_PATHS_METADATA)
    execution.params(params)
    args = fixup_mni_paths_cargs(params, execution)
    out = fixup_mni_paths_outputs(params, execution)
    execution.run(args)
    return out

def fixup_mni_paths(
    verbose: bool = False,
    runner: Runner | None = None,
) -> FixupMniPathsOutputs:
    """
    fixup_mni_paths

    A utility for patching MNI tools to ensure correct paths are used.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        verbose: Provide verbose output during the patch process
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixup_mni_paths_params(
        verbose=verbose,
    )
    return fixup_mni_paths_execute(params, runner)

__all__ = [
    "FixupMniPaths",
    "FixupMniPathsOutputs",
    "FIXUP_MNI_PATHS_METADATA",
    "fixup_mni_paths_cargs",
    "fixup_mni_paths_outputs",
    "fixup_mni_paths_params",
    "fixup_mni_paths_execute",
    "fixup_mni_paths_validate",
    "fixup_mni_paths",
]