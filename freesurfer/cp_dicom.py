# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CP_DICOM_METADATA = Metadata(
    id="cp-dicom",
    name="cp-dicom",
    package="freesurfer",
)

CpDicom = typing.TypedDict(
    "CpDicom",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/cp-dicom"]],
        "dicom_dir": str,
        "output_dir": str,
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CpDicomOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cp_dicom_params(
    dicom_dir: str,
    output_dir: str,
    debug: bool = False,
) -> CpDicom:
    """
    Build parameters.

    Args:
        dicom_dir: Directory containing DICOM files
        output_dir: Output directory where sorted DICOM files will be stored
        debug: Print additional debug information

    Returns:
        Parameter dictionary.
    """
    params: CpDicom = {
        "@type": "freesurfer/cp-dicom",
        "dicom_dir": dicom_dir,
        "output_dir": output_dir,
        "debug": debug,
    }
    return params

def cp_dicom_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CpDicom."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dicom_dir", None) is None:
        raise StyxValidationError("`dicom_dir` must not be None")
    if not isinstance(params["dicom_dir"], str):
        raise StyxValidationError(f'`dicom_dir` has the wrong type: Received `{type(params["dicom_dir"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def cp_dicom_cargs(params: CpDicom, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cp-dicom")
    cargs.append("-d")
    cargs.append(params["dicom_dir"])
    cargs.append("-o")
    cargs.append(params["output_dir"])
    if params.get("debug"):
        cargs.append("-debug")
    return cargs

def cp_dicom_outputs(params: CpDicom, execution: Execution) -> CpDicomOutputs:
    """Build the CpDicomOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CpDicomOutputs(
        root=root_v,
    )

def cp_dicom_execute(params: CpDicom, runner: Runner | None = None) -> CpDicomOutputs:
    """
    cp-dicom

    Copies DICOM files into separate directories for each series based on DICOM headers.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cp_dicom_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CP_DICOM_METADATA)
    execution.params(params)
    args = cp_dicom_cargs(params, execution)
    out = cp_dicom_outputs(params, execution)
    execution.run(args)
    return out

def cp_dicom(
    dicom_dir: str,
    output_dir: str,
    debug: bool = False,
    runner: Runner | None = None,
) -> CpDicomOutputs:
    """
    cp-dicom

    Copies DICOM files into separate directories for each series based on DICOM headers.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dicom_dir: Directory containing DICOM files
        output_dir: Output directory where sorted DICOM files will be stored
        debug: Print additional debug information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cp_dicom_params(
        dicom_dir=dicom_dir,
        output_dir=output_dir,
        debug=debug,
    )
    return cp_dicom_execute(params, runner)

__all__ = [
    "CpDicom",
    "CpDicomOutputs",
    "CP_DICOM_METADATA",
    "cp_dicom_cargs",
    "cp_dicom_outputs",
    "cp_dicom_params",
    "cp_dicom_execute",
    "cp_dicom_validate",
    "cp_dicom",
]