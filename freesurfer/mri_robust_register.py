# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_ROBUST_REGISTER_METADATA = Metadata(
    id="mri_robust_register",
    name="mri_robust_register",
    package="freesurfer",
)

MriRobustRegister = typing.TypedDict(
    "MriRobustRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_robust_register"]],
        "movable_volume": InputPathType,
        "target_volume": InputPathType,
        "output_registration": str,
        "outlier_sensitivity": typing.NotRequired[float],
        "satit": typing.NotRequired[bool],
        "mapped_movable": typing.NotRequired[str],
        "mapped_movable_hdr": typing.NotRequired[str],
        "weights": typing.NotRequired[str],
        "oneminus_w": typing.NotRequired[bool],
        "iscale": typing.NotRequired[bool],
        "iscale_only": typing.NotRequired[bool],
        "iscale_out": typing.NotRequired[str],
        "iscale_in": typing.NotRequired[str],
        "trans_only": typing.NotRequired[bool],
        "affine": typing.NotRequired[bool],
        "ixform": typing.NotRequired[str],
        "init_orient": typing.NotRequired[bool],
        "no_init": typing.NotRequired[bool],
        "vox2vox": typing.NotRequired[bool],
        "cost": typing.NotRequired[str],
        "ent_radius": typing.NotRequired[float],
        "ent_correction": typing.NotRequired[bool],
        "ent_ball": typing.NotRequired[bool],
        "ent_mov": typing.NotRequired[str],
        "powell_tolerance": typing.NotRequired[float],
        "sobel": typing.NotRequired[bool],
        "no_sym": typing.NotRequired[bool],
        "maximum_iterations": typing.NotRequired[float],
        "ent_dst": typing.NotRequired[str],
        "high_iter": typing.NotRequired[float],
        "eps_iteration": typing.NotRequired[float],
        "no_multiscale": typing.NotRequired[bool],
        "max_size": typing.NotRequired[float],
        "min_size": typing.NotRequired[float],
        "w_limit": typing.NotRequired[float],
        "sub_sample": typing.NotRequired[float],
        "float_type": typing.NotRequired[bool],
        "white_bg_mov": typing.NotRequired[bool],
        "white_bg_dst": typing.NotRequired[bool],
        "uchar": typing.NotRequired[bool],
        "mask_mov": typing.NotRequired[InputPathType],
        "mask_dst": typing.NotRequired[InputPathType],
        "half_mov": typing.NotRequired[str],
        "half_dst": typing.NotRequired[str],
        "half_weights": typing.NotRequired[str],
        "half_mov_lta": typing.NotRequired[str],
        "half_dst_lta": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "verbose": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriRobustRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reg_output: OutputPathType
    """Registration output file containing the transformation from movable to destination."""
    mapped_output: OutputPathType | None
    """Movable volume mapped and resampled at destination."""
    mapped_hdr_output: OutputPathType | None
    """Movable volume aligned to destination (header only)."""
    weights_output_file: OutputPathType | None
    """Weights indicating outlier probabilities in destination space."""
    iscale_out_file: OutputPathType | None
    """Output text file containing the intensity scale value."""
    half_mov_file: OutputPathType | None
    """Half-way movable volume resampled."""
    half_dst_file: OutputPathType | None
    """Half-way destination volume resampled."""
    half_weights_file: OutputPathType | None
    """Half-way weights resampled in halfway space."""
    half_mov_lta_file: OutputPathType | None
    """Transform output from movable to half-way space."""
    half_dst_lta_file: OutputPathType | None
    """Transform output from destination to half-way space."""
    ent_mov_file: OutputPathType | None
    """Movable entropy image for ROBENT cost function."""
    ent_dst_file: OutputPathType | None
    """Target entropy image for ROBENT cost function."""

def mri_robust_register_params(
    movable_volume: InputPathType,
    target_volume: InputPathType,
    output_registration: str,
    outlier_sensitivity: float | None = None,
    satit: bool = False,
    mapped_movable: str | None = None,
    mapped_movable_hdr: str | None = None,
    weights: str | None = None,
    oneminus_w: bool = False,
    iscale: bool = False,
    iscale_only: bool = False,
    iscale_out: str | None = None,
    iscale_in: str | None = None,
    trans_only: bool = False,
    affine: bool = False,
    ixform: str | None = None,
    init_orient: bool = False,
    no_init: bool = False,
    vox2vox: bool = False,
    cost: str | None = None,
    ent_radius: float | None = None,
    ent_correction: bool = False,
    ent_ball: bool = False,
    ent_mov: str | None = None,
    powell_tolerance: float | None = None,
    sobel: bool = False,
    no_sym: bool = False,
    maximum_iterations: float | None = None,
    ent_dst: str | None = None,
    high_iter: float | None = None,
    eps_iteration: float | None = None,
    no_multiscale: bool = False,
    max_size: float | None = None,
    min_size: float | None = None,
    w_limit: float | None = None,
    sub_sample: float | None = None,
    float_type: bool = False,
    white_bg_mov: bool = False,
    white_bg_dst: bool = False,
    uchar: bool = False,
    mask_mov: InputPathType | None = None,
    mask_dst: InputPathType | None = None,
    half_mov: str | None = None,
    half_dst: str | None = None,
    half_weights: str | None = None,
    half_mov_lta: str | None = None,
    half_dst_lta: str | None = None,
    debug: bool = False,
    verbose: float | None = None,
) -> MriRobustRegister:
    """
    Build parameters.

    Args:
        movable_volume: Input movable volume to be aligned to target.
        target_volume: Input target volume.
        output_registration: Output registration (transform from mov to dst).
        outlier_sensitivity: Set outlier sensitivity manually for robust cost\
            functions. Higher values mean less sensitivity.
        satit: Auto-detect good sensitivity for robust cost functions.
        mapped_movable: Output image: movable mapped and resampled at destination.
        mapped_movable_hdr: Output image: movable aligned to destination (no\
            resampling, only adjusting header vox2ras).
        weights: Output weights (outlier probabilities) in destination space\
            (0=regular,1=outlier).
        oneminus_w: Weights (outlier) map will be inverted (0=outlier), as in\
            earlier versions.
        iscale: Estimate intensity scale factor.
        iscale_only: Only perform intensity scaling (no transformation).
        iscale_out: Output text file for iscale value.
        iscale_in: Initial input text file for iscale value.
        trans_only: Find 3 parameter translation only.
        affine: Find 12 parameter affine transform.
        ixform: Use initial transform LTA on source.
        init_orient: Use moments for orientation initialization.
        no_init: Skip automatic transform initialization.
        vox2vox: Output VOX2VOX LTA file.
        cost: Set cost function for registration.
        ent_radius: With ROBENT: specify box radius for entropy computation.
        ent_correction: With ROBENT: use better entropy computation that works on\
            smaller boxes.
        ent_ball: With ROBENT: use ball around voxel instead of box.
        ent_mov: With ROBENT: write movable entropy image.
        powell_tolerance: With MI, NMI etc: set Powell tolerance.
        sobel: Register Sobel magnitude images.
        no_sym: Do not map to half way space.
        maximum_iterations: Maximum number of iterations on each resolution.
        ent_dst: With ROBENT: write target entropy image.
        high_iter: Maximum number of iterations on highest resolution.
        eps_iteration: Stop iterations when transform update falls below specified\
            RMS distance.
        no_multiscale: Process highest resolution only (no multiscale).
        max_size: Specify largest voxel dimension for gaussian pyramid.
        min_size: Specify smallest voxel dimension for gaussian pyramid.
        w_limit: (Expert) sets maximal outlier limit for --satit.
        sub_sample: Subsample if dimension is greater than the specified value on\
            all axes.
        float_type: Convert images to float internally.
        white_bg_mov: Assume white background in MOV for padding.
        white_bg_dst: Assume white background in DST for padding.
        uchar: Convert inputs to UCHAR with rescale and histogram cropping.
        mask_mov: Mask movable image with mask file.
        mask_dst: Mask destination image with mask file.
        half_mov: Outputs half-way movable (resampled in halfway space).
        half_dst: Outputs half-way destination (resampled in halfway space).
        half_weights: Outputs half-way weights (resampled in halfway space).
        half_mov_lta: Outputs transform from movable to half-way space.
        half_dst_lta: Outputs transform from destination to half-way space.
        debug: Show debug output.
        verbose: Verbosity level: 0 (quiet), 1 (normal), 2 (detail).

    Returns:
        Parameter dictionary.
    """
    params: MriRobustRegister = {
        "@type": "freesurfer/mri_robust_register",
        "movable_volume": movable_volume,
        "target_volume": target_volume,
        "output_registration": output_registration,
        "satit": satit,
        "oneminus_w": oneminus_w,
        "iscale": iscale,
        "iscale_only": iscale_only,
        "trans_only": trans_only,
        "affine": affine,
        "init_orient": init_orient,
        "no_init": no_init,
        "vox2vox": vox2vox,
        "ent_correction": ent_correction,
        "ent_ball": ent_ball,
        "sobel": sobel,
        "no_sym": no_sym,
        "no_multiscale": no_multiscale,
        "float_type": float_type,
        "white_bg_mov": white_bg_mov,
        "white_bg_dst": white_bg_dst,
        "uchar": uchar,
        "debug": debug,
    }
    if outlier_sensitivity is not None:
        params["outlier_sensitivity"] = outlier_sensitivity
    if mapped_movable is not None:
        params["mapped_movable"] = mapped_movable
    if mapped_movable_hdr is not None:
        params["mapped_movable_hdr"] = mapped_movable_hdr
    if weights is not None:
        params["weights"] = weights
    if iscale_out is not None:
        params["iscale_out"] = iscale_out
    if iscale_in is not None:
        params["iscale_in"] = iscale_in
    if ixform is not None:
        params["ixform"] = ixform
    if cost is not None:
        params["cost"] = cost
    if ent_radius is not None:
        params["ent_radius"] = ent_radius
    if ent_mov is not None:
        params["ent_mov"] = ent_mov
    if powell_tolerance is not None:
        params["powell_tolerance"] = powell_tolerance
    if maximum_iterations is not None:
        params["maximum_iterations"] = maximum_iterations
    if ent_dst is not None:
        params["ent_dst"] = ent_dst
    if high_iter is not None:
        params["high_iter"] = high_iter
    if eps_iteration is not None:
        params["eps_iteration"] = eps_iteration
    if max_size is not None:
        params["max_size"] = max_size
    if min_size is not None:
        params["min_size"] = min_size
    if w_limit is not None:
        params["w_limit"] = w_limit
    if sub_sample is not None:
        params["sub_sample"] = sub_sample
    if mask_mov is not None:
        params["mask_mov"] = mask_mov
    if mask_dst is not None:
        params["mask_dst"] = mask_dst
    if half_mov is not None:
        params["half_mov"] = half_mov
    if half_dst is not None:
        params["half_dst"] = half_dst
    if half_weights is not None:
        params["half_weights"] = half_weights
    if half_mov_lta is not None:
        params["half_mov_lta"] = half_mov_lta
    if half_dst_lta is not None:
        params["half_dst_lta"] = half_dst_lta
    if verbose is not None:
        params["verbose"] = verbose
    return params

def mri_robust_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRobustRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("movable_volume", None) is None:
        raise StyxValidationError("`movable_volume` must not be None")
    if not isinstance(params["movable_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`movable_volume` has the wrong type: Received `{type(params["movable_volume"])}` expected `InputPathType`')
    if params.get("target_volume", None) is None:
        raise StyxValidationError("`target_volume` must not be None")
    if not isinstance(params["target_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["target_volume"])}` expected `InputPathType`')
    if params.get("output_registration", None) is None:
        raise StyxValidationError("`output_registration` must not be None")
    if not isinstance(params["output_registration"], str):
        raise StyxValidationError(f'`output_registration` has the wrong type: Received `{type(params["output_registration"])}` expected `str`')
    if params.get("outlier_sensitivity", None) is not None:
        if not isinstance(params["outlier_sensitivity"], (float, int)):
            raise StyxValidationError(f'`outlier_sensitivity` has the wrong type: Received `{type(params["outlier_sensitivity"])}` expected `float`')
    if params.get("satit", None) is not None:
        if not isinstance(params["satit"], bool):
            raise StyxValidationError(f'`satit` has the wrong type: Received `{type(params["satit"])}` expected `bool`')
    if params.get("mapped_movable", None) is not None:
        if not isinstance(params["mapped_movable"], str):
            raise StyxValidationError(f'`mapped_movable` has the wrong type: Received `{type(params["mapped_movable"])}` expected `str`')
    if params.get("mapped_movable_hdr", None) is not None:
        if not isinstance(params["mapped_movable_hdr"], str):
            raise StyxValidationError(f'`mapped_movable_hdr` has the wrong type: Received `{type(params["mapped_movable_hdr"])}` expected `str`')
    if params.get("weights", None) is not None:
        if not isinstance(params["weights"], str):
            raise StyxValidationError(f'`weights` has the wrong type: Received `{type(params["weights"])}` expected `str`')
    if params.get("oneminus_w", None) is not None:
        if not isinstance(params["oneminus_w"], bool):
            raise StyxValidationError(f'`oneminus_w` has the wrong type: Received `{type(params["oneminus_w"])}` expected `bool`')
    if params.get("iscale", None) is not None:
        if not isinstance(params["iscale"], bool):
            raise StyxValidationError(f'`iscale` has the wrong type: Received `{type(params["iscale"])}` expected `bool`')
    if params.get("iscale_only", None) is not None:
        if not isinstance(params["iscale_only"], bool):
            raise StyxValidationError(f'`iscale_only` has the wrong type: Received `{type(params["iscale_only"])}` expected `bool`')
    if params.get("iscale_out", None) is not None:
        if not isinstance(params["iscale_out"], str):
            raise StyxValidationError(f'`iscale_out` has the wrong type: Received `{type(params["iscale_out"])}` expected `str`')
    if params.get("iscale_in", None) is not None:
        if not isinstance(params["iscale_in"], str):
            raise StyxValidationError(f'`iscale_in` has the wrong type: Received `{type(params["iscale_in"])}` expected `str`')
    if params.get("trans_only", None) is not None:
        if not isinstance(params["trans_only"], bool):
            raise StyxValidationError(f'`trans_only` has the wrong type: Received `{type(params["trans_only"])}` expected `bool`')
    if params.get("affine", None) is not None:
        if not isinstance(params["affine"], bool):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `bool`')
    if params.get("ixform", None) is not None:
        if not isinstance(params["ixform"], str):
            raise StyxValidationError(f'`ixform` has the wrong type: Received `{type(params["ixform"])}` expected `str`')
    if params.get("init_orient", None) is not None:
        if not isinstance(params["init_orient"], bool):
            raise StyxValidationError(f'`init_orient` has the wrong type: Received `{type(params["init_orient"])}` expected `bool`')
    if params.get("no_init", None) is not None:
        if not isinstance(params["no_init"], bool):
            raise StyxValidationError(f'`no_init` has the wrong type: Received `{type(params["no_init"])}` expected `bool`')
    if params.get("vox2vox", None) is not None:
        if not isinstance(params["vox2vox"], bool):
            raise StyxValidationError(f'`vox2vox` has the wrong type: Received `{type(params["vox2vox"])}` expected `bool`')
    if params.get("cost", None) is not None:
        if not isinstance(params["cost"], str):
            raise StyxValidationError(f'`cost` has the wrong type: Received `{type(params["cost"])}` expected `str`')
    if params.get("ent_radius", None) is not None:
        if not isinstance(params["ent_radius"], (float, int)):
            raise StyxValidationError(f'`ent_radius` has the wrong type: Received `{type(params["ent_radius"])}` expected `float`')
    if params.get("ent_correction", None) is not None:
        if not isinstance(params["ent_correction"], bool):
            raise StyxValidationError(f'`ent_correction` has the wrong type: Received `{type(params["ent_correction"])}` expected `bool`')
    if params.get("ent_ball", None) is not None:
        if not isinstance(params["ent_ball"], bool):
            raise StyxValidationError(f'`ent_ball` has the wrong type: Received `{type(params["ent_ball"])}` expected `bool`')
    if params.get("ent_mov", None) is not None:
        if not isinstance(params["ent_mov"], str):
            raise StyxValidationError(f'`ent_mov` has the wrong type: Received `{type(params["ent_mov"])}` expected `str`')
    if params.get("powell_tolerance", None) is not None:
        if not isinstance(params["powell_tolerance"], (float, int)):
            raise StyxValidationError(f'`powell_tolerance` has the wrong type: Received `{type(params["powell_tolerance"])}` expected `float`')
    if params.get("sobel", None) is not None:
        if not isinstance(params["sobel"], bool):
            raise StyxValidationError(f'`sobel` has the wrong type: Received `{type(params["sobel"])}` expected `bool`')
    if params.get("no_sym", None) is not None:
        if not isinstance(params["no_sym"], bool):
            raise StyxValidationError(f'`no_sym` has the wrong type: Received `{type(params["no_sym"])}` expected `bool`')
    if params.get("maximum_iterations", None) is not None:
        if not isinstance(params["maximum_iterations"], (float, int)):
            raise StyxValidationError(f'`maximum_iterations` has the wrong type: Received `{type(params["maximum_iterations"])}` expected `float`')
    if params.get("ent_dst", None) is not None:
        if not isinstance(params["ent_dst"], str):
            raise StyxValidationError(f'`ent_dst` has the wrong type: Received `{type(params["ent_dst"])}` expected `str`')
    if params.get("high_iter", None) is not None:
        if not isinstance(params["high_iter"], (float, int)):
            raise StyxValidationError(f'`high_iter` has the wrong type: Received `{type(params["high_iter"])}` expected `float`')
    if params.get("eps_iteration", None) is not None:
        if not isinstance(params["eps_iteration"], (float, int)):
            raise StyxValidationError(f'`eps_iteration` has the wrong type: Received `{type(params["eps_iteration"])}` expected `float`')
    if params.get("no_multiscale", None) is not None:
        if not isinstance(params["no_multiscale"], bool):
            raise StyxValidationError(f'`no_multiscale` has the wrong type: Received `{type(params["no_multiscale"])}` expected `bool`')
    if params.get("max_size", None) is not None:
        if not isinstance(params["max_size"], (float, int)):
            raise StyxValidationError(f'`max_size` has the wrong type: Received `{type(params["max_size"])}` expected `float`')
    if params.get("min_size", None) is not None:
        if not isinstance(params["min_size"], (float, int)):
            raise StyxValidationError(f'`min_size` has the wrong type: Received `{type(params["min_size"])}` expected `float`')
    if params.get("w_limit", None) is not None:
        if not isinstance(params["w_limit"], (float, int)):
            raise StyxValidationError(f'`w_limit` has the wrong type: Received `{type(params["w_limit"])}` expected `float`')
    if params.get("sub_sample", None) is not None:
        if not isinstance(params["sub_sample"], (float, int)):
            raise StyxValidationError(f'`sub_sample` has the wrong type: Received `{type(params["sub_sample"])}` expected `float`')
    if params.get("float_type", None) is not None:
        if not isinstance(params["float_type"], bool):
            raise StyxValidationError(f'`float_type` has the wrong type: Received `{type(params["float_type"])}` expected `bool`')
    if params.get("white_bg_mov", None) is not None:
        if not isinstance(params["white_bg_mov"], bool):
            raise StyxValidationError(f'`white_bg_mov` has the wrong type: Received `{type(params["white_bg_mov"])}` expected `bool`')
    if params.get("white_bg_dst", None) is not None:
        if not isinstance(params["white_bg_dst"], bool):
            raise StyxValidationError(f'`white_bg_dst` has the wrong type: Received `{type(params["white_bg_dst"])}` expected `bool`')
    if params.get("uchar", None) is not None:
        if not isinstance(params["uchar"], bool):
            raise StyxValidationError(f'`uchar` has the wrong type: Received `{type(params["uchar"])}` expected `bool`')
    if params.get("mask_mov", None) is not None:
        if not isinstance(params["mask_mov"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_mov` has the wrong type: Received `{type(params["mask_mov"])}` expected `InputPathType`')
    if params.get("mask_dst", None) is not None:
        if not isinstance(params["mask_dst"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_dst` has the wrong type: Received `{type(params["mask_dst"])}` expected `InputPathType`')
    if params.get("half_mov", None) is not None:
        if not isinstance(params["half_mov"], str):
            raise StyxValidationError(f'`half_mov` has the wrong type: Received `{type(params["half_mov"])}` expected `str`')
    if params.get("half_dst", None) is not None:
        if not isinstance(params["half_dst"], str):
            raise StyxValidationError(f'`half_dst` has the wrong type: Received `{type(params["half_dst"])}` expected `str`')
    if params.get("half_weights", None) is not None:
        if not isinstance(params["half_weights"], str):
            raise StyxValidationError(f'`half_weights` has the wrong type: Received `{type(params["half_weights"])}` expected `str`')
    if params.get("half_mov_lta", None) is not None:
        if not isinstance(params["half_mov_lta"], str):
            raise StyxValidationError(f'`half_mov_lta` has the wrong type: Received `{type(params["half_mov_lta"])}` expected `str`')
    if params.get("half_dst_lta", None) is not None:
        if not isinstance(params["half_dst_lta"], str):
            raise StyxValidationError(f'`half_dst_lta` has the wrong type: Received `{type(params["half_dst_lta"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], (float, int)):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `float`')

def mri_robust_register_cargs(params: MriRobustRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_robust_register")
    cargs.append("-mov")
    cargs.append(execution.input_file(params["movable_volume"]))
    cargs.append("-dst")
    cargs.append(execution.input_file(params["target_volume"]))
    cargs.append("-lta")
    cargs.append(params["output_registration"])
    v_0 = params.get("outlier_sensitivity")
    if v_0 is not None:
        cargs.append("--sat")
        cargs.append(str(v_0))
    if params.get("satit"):
        cargs.append("--satit")
    v_1 = params.get("mapped_movable")
    if v_1 is not None:
        cargs.append("--mapmov")
        cargs.append(v_1)
    v_2 = params.get("mapped_movable_hdr")
    if v_2 is not None:
        cargs.append("--mapmovhdr")
        cargs.append(v_2)
    v_3 = params.get("weights")
    if v_3 is not None:
        cargs.append("--weights")
        cargs.append(v_3)
    if params.get("oneminus_w"):
        cargs.append("--oneminusw")
    if params.get("iscale"):
        cargs.append("--iscale")
    if params.get("iscale_only"):
        cargs.append("--iscaleonly")
    v_4 = params.get("iscale_out")
    if v_4 is not None:
        cargs.append("--iscaleout")
        cargs.append(v_4)
    v_5 = params.get("iscale_in")
    if v_5 is not None:
        cargs.append("--iscalein")
        cargs.append(v_5)
    if params.get("trans_only"):
        cargs.append("--transonly")
    if params.get("affine"):
        cargs.append("--affine")
    v_6 = params.get("ixform")
    if v_6 is not None:
        cargs.append("--ixform")
        cargs.append(v_6)
    if params.get("init_orient"):
        cargs.append("--initorient")
    if params.get("no_init"):
        cargs.append("--noinit")
    if params.get("vox2vox"):
        cargs.append("--vox2vox")
    v_7 = params.get("cost")
    if v_7 is not None:
        cargs.append("--cost")
        cargs.append(v_7)
    v_8 = params.get("ent_radius")
    if v_8 is not None:
        cargs.append("--entradius")
        cargs.append(str(v_8))
    if params.get("ent_correction"):
        cargs.append("--entcorrection")
    if params.get("ent_ball"):
        cargs.append("--entball")
    v_9 = params.get("ent_mov")
    if v_9 is not None:
        cargs.append("--entmov")
        cargs.append(v_9)
    v_10 = params.get("powell_tolerance")
    if v_10 is not None:
        cargs.append("--powelltol")
        cargs.append(str(v_10))
    if params.get("sobel"):
        cargs.append("--sobel")
    if params.get("no_sym"):
        cargs.append("--nosym")
    v_11 = params.get("maximum_iterations")
    if v_11 is not None:
        cargs.append("--maxit")
        cargs.append(str(v_11))
    v_12 = params.get("ent_dst")
    if v_12 is not None:
        cargs.append("--entdst")
        cargs.append(v_12)
    v_13 = params.get("high_iter")
    if v_13 is not None:
        cargs.append("--highit")
        cargs.append(str(v_13))
    v_14 = params.get("eps_iteration")
    if v_14 is not None:
        cargs.append("--epsit")
        cargs.append(str(v_14))
    if params.get("no_multiscale"):
        cargs.append("--nomulti")
    v_15 = params.get("max_size")
    if v_15 is not None:
        cargs.append("--maxsize")
        cargs.append(str(v_15))
    v_16 = params.get("min_size")
    if v_16 is not None:
        cargs.append("--minsize")
        cargs.append(str(v_16))
    v_17 = params.get("w_limit")
    if v_17 is not None:
        cargs.append("--wlimit")
        cargs.append(str(v_17))
    v_18 = params.get("sub_sample")
    if v_18 is not None:
        cargs.append("--subsample")
        cargs.append(str(v_18))
    if params.get("float_type"):
        cargs.append("--floattype")
    if params.get("white_bg_mov"):
        cargs.append("--whitebgmov")
    if params.get("white_bg_dst"):
        cargs.append("--whitebgdst")
    if params.get("uchar"):
        cargs.append("--uchar")
    v_19 = params.get("mask_mov")
    if v_19 is not None:
        cargs.append("--maskmov")
        cargs.append(execution.input_file(v_19))
    v_20 = params.get("mask_dst")
    if v_20 is not None:
        cargs.append("--maskdst")
        cargs.append(execution.input_file(v_20))
    v_21 = params.get("half_mov")
    if v_21 is not None:
        cargs.append("--halfmov")
        cargs.append(v_21)
    v_22 = params.get("half_dst")
    if v_22 is not None:
        cargs.append("--halfdst")
        cargs.append(v_22)
    v_23 = params.get("half_weights")
    if v_23 is not None:
        cargs.append("--halfweights")
        cargs.append(v_23)
    v_24 = params.get("half_mov_lta")
    if v_24 is not None:
        cargs.append("--halfmovlta")
        cargs.append(v_24)
    v_25 = params.get("half_dst_lta")
    if v_25 is not None:
        cargs.append("--halfdstlta")
        cargs.append(v_25)
    if params.get("debug"):
        cargs.append("--debug")
    v_26 = params.get("verbose")
    if v_26 is not None:
        cargs.append("--verbose")
        cargs.append(str(v_26))
    return cargs

def mri_robust_register_outputs(params: MriRobustRegister, execution: Execution) -> MriRobustRegisterOutputs:
    """Build the MriRobustRegisterOutputs object for this tool."""
    mapped_output_v: OutputPathType | None = None
    mapped_hdr_output_v: OutputPathType | None = None
    weights_output_file_v: OutputPathType | None = None
    iscale_out_file_v: OutputPathType | None = None
    half_mov_file_v: OutputPathType | None = None
    half_dst_file_v: OutputPathType | None = None
    half_weights_file_v: OutputPathType | None = None
    half_mov_lta_file_v: OutputPathType | None = None
    half_dst_lta_file_v: OutputPathType | None = None
    ent_mov_file_v: OutputPathType | None = None
    ent_dst_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    reg_output_v: OutputPathType = execution.output_file(params["output_registration"])
    __v0 = params.get("mapped_movable")
    if __v0 is not None:
        mapped_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("mapped_movable_hdr")
    if __v1 is not None:
        mapped_hdr_output_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("weights")
    if __v2 is not None:
        weights_output_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("iscale_out")
    if __v3 is not None:
        iscale_out_file_v = execution.output_file((__v3 if __v3 is not None else ""))
    __v4 = params.get("half_mov")
    if __v4 is not None:
        half_mov_file_v = execution.output_file((__v4 if __v4 is not None else ""))
    __v5 = params.get("half_dst")
    if __v5 is not None:
        half_dst_file_v = execution.output_file((__v5 if __v5 is not None else ""))
    __v6 = params.get("half_weights")
    if __v6 is not None:
        half_weights_file_v = execution.output_file((__v6 if __v6 is not None else ""))
    __v7 = params.get("half_mov_lta")
    if __v7 is not None:
        half_mov_lta_file_v = execution.output_file((__v7 if __v7 is not None else ""))
    __v8 = params.get("half_dst_lta")
    if __v8 is not None:
        half_dst_lta_file_v = execution.output_file((__v8 if __v8 is not None else ""))
    __v9 = params.get("ent_mov")
    if __v9 is not None:
        ent_mov_file_v = execution.output_file((__v9 if __v9 is not None else ""))
    __v10 = params.get("ent_dst")
    if __v10 is not None:
        ent_dst_file_v = execution.output_file((__v10 if __v10 is not None else ""))
    return MriRobustRegisterOutputs(
        root=root_v,
        reg_output=reg_output_v,
        mapped_output=mapped_output_v,
        mapped_hdr_output=mapped_hdr_output_v,
        weights_output_file=weights_output_file_v,
        iscale_out_file=iscale_out_file_v,
        half_mov_file=half_mov_file_v,
        half_dst_file=half_dst_file_v,
        half_weights_file=half_weights_file_v,
        half_mov_lta_file=half_mov_lta_file_v,
        half_dst_lta_file=half_dst_lta_file_v,
        ent_mov_file=ent_mov_file_v,
        ent_dst_file=ent_dst_file_v,
    )

def mri_robust_register_execute(params: MriRobustRegister, runner: Runner | None = None) -> MriRobustRegisterOutputs:
    """
    mri_robust_register

    Inverse consistent registration of two volumes using robust and standard cost functions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_robust_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_ROBUST_REGISTER_METADATA)
    execution.params(params)
    args = mri_robust_register_cargs(params, execution)
    out = mri_robust_register_outputs(params, execution)
    execution.run(args)
    return out

def mri_robust_register(
    movable_volume: InputPathType,
    target_volume: InputPathType,
    output_registration: str,
    outlier_sensitivity: float | None = None,
    satit: bool = False,
    mapped_movable: str | None = None,
    mapped_movable_hdr: str | None = None,
    weights: str | None = None,
    oneminus_w: bool = False,
    iscale: bool = False,
    iscale_only: bool = False,
    iscale_out: str | None = None,
    iscale_in: str | None = None,
    trans_only: bool = False,
    affine: bool = False,
    ixform: str | None = None,
    init_orient: bool = False,
    no_init: bool = False,
    vox2vox: bool = False,
    cost: str | None = None,
    ent_radius: float | None = None,
    ent_correction: bool = False,
    ent_ball: bool = False,
    ent_mov: str | None = None,
    powell_tolerance: float | None = None,
    sobel: bool = False,
    no_sym: bool = False,
    maximum_iterations: float | None = None,
    ent_dst: str | None = None,
    high_iter: float | None = None,
    eps_iteration: float | None = None,
    no_multiscale: bool = False,
    max_size: float | None = None,
    min_size: float | None = None,
    w_limit: float | None = None,
    sub_sample: float | None = None,
    float_type: bool = False,
    white_bg_mov: bool = False,
    white_bg_dst: bool = False,
    uchar: bool = False,
    mask_mov: InputPathType | None = None,
    mask_dst: InputPathType | None = None,
    half_mov: str | None = None,
    half_dst: str | None = None,
    half_weights: str | None = None,
    half_mov_lta: str | None = None,
    half_dst_lta: str | None = None,
    debug: bool = False,
    verbose: float | None = None,
    runner: Runner | None = None,
) -> MriRobustRegisterOutputs:
    """
    mri_robust_register

    Inverse consistent registration of two volumes using robust and standard cost functions.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        movable_volume: Input movable volume to be aligned to target.
        target_volume: Input target volume.
        output_registration: Output registration (transform from mov to dst).
        outlier_sensitivity: Set outlier sensitivity manually for robust cost\
            functions. Higher values mean less sensitivity.
        satit: Auto-detect good sensitivity for robust cost functions.
        mapped_movable: Output image: movable mapped and resampled at destination.
        mapped_movable_hdr: Output image: movable aligned to destination (no\
            resampling, only adjusting header vox2ras).
        weights: Output weights (outlier probabilities) in destination space\
            (0=regular,1=outlier).
        oneminus_w: Weights (outlier) map will be inverted (0=outlier), as in\
            earlier versions.
        iscale: Estimate intensity scale factor.
        iscale_only: Only perform intensity scaling (no transformation).
        iscale_out: Output text file for iscale value.
        iscale_in: Initial input text file for iscale value.
        trans_only: Find 3 parameter translation only.
        affine: Find 12 parameter affine transform.
        ixform: Use initial transform LTA on source.
        init_orient: Use moments for orientation initialization.
        no_init: Skip automatic transform initialization.
        vox2vox: Output VOX2VOX LTA file.
        cost: Set cost function for registration.
        ent_radius: With ROBENT: specify box radius for entropy computation.
        ent_correction: With ROBENT: use better entropy computation that works on\
            smaller boxes.
        ent_ball: With ROBENT: use ball around voxel instead of box.
        ent_mov: With ROBENT: write movable entropy image.
        powell_tolerance: With MI, NMI etc: set Powell tolerance.
        sobel: Register Sobel magnitude images.
        no_sym: Do not map to half way space.
        maximum_iterations: Maximum number of iterations on each resolution.
        ent_dst: With ROBENT: write target entropy image.
        high_iter: Maximum number of iterations on highest resolution.
        eps_iteration: Stop iterations when transform update falls below specified\
            RMS distance.
        no_multiscale: Process highest resolution only (no multiscale).
        max_size: Specify largest voxel dimension for gaussian pyramid.
        min_size: Specify smallest voxel dimension for gaussian pyramid.
        w_limit: (Expert) sets maximal outlier limit for --satit.
        sub_sample: Subsample if dimension is greater than the specified value on\
            all axes.
        float_type: Convert images to float internally.
        white_bg_mov: Assume white background in MOV for padding.
        white_bg_dst: Assume white background in DST for padding.
        uchar: Convert inputs to UCHAR with rescale and histogram cropping.
        mask_mov: Mask movable image with mask file.
        mask_dst: Mask destination image with mask file.
        half_mov: Outputs half-way movable (resampled in halfway space).
        half_dst: Outputs half-way destination (resampled in halfway space).
        half_weights: Outputs half-way weights (resampled in halfway space).
        half_mov_lta: Outputs transform from movable to half-way space.
        half_dst_lta: Outputs transform from destination to half-way space.
        debug: Show debug output.
        verbose: Verbosity level: 0 (quiet), 1 (normal), 2 (detail).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_robust_register_params(
        movable_volume=movable_volume,
        target_volume=target_volume,
        output_registration=output_registration,
        outlier_sensitivity=outlier_sensitivity,
        satit=satit,
        mapped_movable=mapped_movable,
        mapped_movable_hdr=mapped_movable_hdr,
        weights=weights,
        oneminus_w=oneminus_w,
        iscale=iscale,
        iscale_only=iscale_only,
        iscale_out=iscale_out,
        iscale_in=iscale_in,
        trans_only=trans_only,
        affine=affine,
        ixform=ixform,
        init_orient=init_orient,
        no_init=no_init,
        vox2vox=vox2vox,
        cost=cost,
        ent_radius=ent_radius,
        ent_correction=ent_correction,
        ent_ball=ent_ball,
        ent_mov=ent_mov,
        powell_tolerance=powell_tolerance,
        sobel=sobel,
        no_sym=no_sym,
        maximum_iterations=maximum_iterations,
        ent_dst=ent_dst,
        high_iter=high_iter,
        eps_iteration=eps_iteration,
        no_multiscale=no_multiscale,
        max_size=max_size,
        min_size=min_size,
        w_limit=w_limit,
        sub_sample=sub_sample,
        float_type=float_type,
        white_bg_mov=white_bg_mov,
        white_bg_dst=white_bg_dst,
        uchar=uchar,
        mask_mov=mask_mov,
        mask_dst=mask_dst,
        half_mov=half_mov,
        half_dst=half_dst,
        half_weights=half_weights,
        half_mov_lta=half_mov_lta,
        half_dst_lta=half_dst_lta,
        debug=debug,
        verbose=verbose,
    )
    return mri_robust_register_execute(params, runner)

__all__ = [
    "MriRobustRegister",
    "MriRobustRegisterOutputs",
    "MRI_ROBUST_REGISTER_METADATA",
    "mri_robust_register_cargs",
    "mri_robust_register_outputs",
    "mri_robust_register_params",
    "mri_robust_register_execute",
    "mri_robust_register_validate",
    "mri_robust_register",
]