# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_WARP_METADATA = Metadata(
    id="mris_warp",
    name="mris_warp",
    package="freesurfer",
)

MrisWarp = typing.TypedDict(
    "MrisWarp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_warp"]],
        "deformvol": typing.NotRequired[str],
        "m3z": typing.NotRequired[str],
        "regfile": typing.NotRequired[str],
        "surf": typing.NotRequired[str],
        "out": typing.NotRequired[str],
        "abs": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisWarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType | None
    """Output surface file"""

def mris_warp_params(
    deformvol: str | None = None,
    m3z: str | None = None,
    regfile: str | None = None,
    surf: str | None = None,
    out: str | None = None,
    abs: bool = False,
    help: bool = False,
    version: bool = False,
) -> MrisWarp:
    """
    Build parameters.

    Args:
        deformvol: Volume containing deformation
        m3z: M3Z file containing deformation
        regfile: register.dat file between surface and volume
        surf: Surface file to warp
        out: Name for output surface (if does not contain '/', outputs to same\
            directory as input surface)
        abs: Absolute coordinate displacement convention (default)
        help: Print out information on how to use this program
        version: Print out version and exit

    Returns:
        Parameter dictionary.
    """
    params: MrisWarp = {
        "@type": "freesurfer/mris_warp",
        "abs": abs,
        "help": help,
        "version": version,
    }
    if deformvol is not None:
        params["deformvol"] = deformvol
    if m3z is not None:
        params["m3z"] = m3z
    if regfile is not None:
        params["regfile"] = regfile
    if surf is not None:
        params["surf"] = surf
    if out is not None:
        params["out"] = out
    return params

def mris_warp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisWarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("deformvol", None) is not None:
        if not isinstance(params["deformvol"], str):
            raise StyxValidationError(f'`deformvol` has the wrong type: Received `{type(params["deformvol"])}` expected `str`')
    if params.get("m3z", None) is not None:
        if not isinstance(params["m3z"], str):
            raise StyxValidationError(f'`m3z` has the wrong type: Received `{type(params["m3z"])}` expected `str`')
    if params.get("regfile", None) is not None:
        if not isinstance(params["regfile"], str):
            raise StyxValidationError(f'`regfile` has the wrong type: Received `{type(params["regfile"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')
    if params.get("out", None) is not None:
        if not isinstance(params["out"], str):
            raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')
    if params.get("abs", None) is not None:
        if not isinstance(params["abs"], bool):
            raise StyxValidationError(f'`abs` has the wrong type: Received `{type(params["abs"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mris_warp_cargs(params: MrisWarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_warp")
    v_0 = params.get("deformvol")
    if v_0 is not None:
        cargs.append("--deformvol")
        cargs.append(v_0)
    v_1 = params.get("m3z")
    if v_1 is not None:
        cargs.append("--m3z")
        cargs.append(v_1)
    v_2 = params.get("regfile")
    if v_2 is not None:
        cargs.append("--reg")
        cargs.append(v_2)
    v_3 = params.get("surf")
    if v_3 is not None:
        cargs.append("--surf")
        cargs.append(v_3)
    v_4 = params.get("out")
    if v_4 is not None:
        cargs.append("--out")
        cargs.append(v_4)
    if params.get("abs"):
        cargs.append("--abs")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mris_warp_outputs(params: MrisWarp, execution: Execution) -> MrisWarpOutputs:
    """Build the MrisWarpOutputs object for this tool."""
    output_surface_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out")
    if __v0 is not None:
        output_surface_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrisWarpOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def mris_warp_execute(params: MrisWarp, runner: Runner | None = None) -> MrisWarpOutputs:
    """
    mris_warp

    This program will warp a surface using a specified deformation field.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_warp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_WARP_METADATA)
    execution.params(params)
    args = mris_warp_cargs(params, execution)
    out = mris_warp_outputs(params, execution)
    execution.run(args)
    return out

def mris_warp(
    deformvol: str | None = None,
    m3z: str | None = None,
    regfile: str | None = None,
    surf: str | None = None,
    out: str | None = None,
    abs: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrisWarpOutputs:
    """
    mris_warp

    This program will warp a surface using a specified deformation field.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        deformvol: Volume containing deformation
        m3z: M3Z file containing deformation
        regfile: register.dat file between surface and volume
        surf: Surface file to warp
        out: Name for output surface (if does not contain '/', outputs to same\
            directory as input surface)
        abs: Absolute coordinate displacement convention (default)
        help: Print out information on how to use this program
        version: Print out version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_warp_params(
        deformvol=deformvol,
        m3z=m3z,
        regfile=regfile,
        surf=surf,
        out=out,
        abs=abs,
        help=help,
        version=version,
    )
    return mris_warp_execute(params, runner)

__all__ = [
    "MrisWarp",
    "MrisWarpOutputs",
    "MRIS_WARP_METADATA",
    "mris_warp_cargs",
    "mris_warp_outputs",
    "mris_warp_params",
    "mris_warp_execute",
    "mris_warp_validate",
    "mris_warp",
]