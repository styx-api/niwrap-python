# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TAL_QC_AZS_METADATA = Metadata(
    id="tal_QC_AZS",
    name="tal_QC_AZS",
    package="freesurfer",
)

TalQcAzs = typing.TypedDict(
    "TalQcAzs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tal_QC_AZS"]],
        "logfile": InputPathType,
    },
)

@dataclasses.dataclass
class TalQcAzsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tal_qc_azs_params(
    logfile: InputPathType,
) -> TalQcAzs:
    """
    Build parameters.

    Args:
        logfile: Input logfile for processing.

    Returns:
        Parameter dictionary.
    """
    params: TalQcAzs = {
        "@type": "freesurfer/tal_QC_AZS",
        "logfile": logfile,
    }
    return params

def tal_qc_azs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TalQcAzs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("logfile", None) is None:
        raise StyxValidationError("`logfile` must not be None")
    if not isinstance(params["logfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`logfile` has the wrong type: Received `{type(params["logfile"])}` expected `InputPathType`')

def tal_qc_azs_cargs(params: TalQcAzs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tal_QC_AZS")
    cargs.append(execution.input_file(params["logfile"]))
    return cargs

def tal_qc_azs_outputs(params: TalQcAzs, execution: Execution) -> TalQcAzsOutputs:
    """Build the TalQcAzsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TalQcAzsOutputs(
        root=root_v,
    )

def tal_qc_azs_execute(params: TalQcAzs, runner: Runner | None = None) -> TalQcAzsOutputs:
    """
    tal_QC_AZS

    A tool that processes a logfile.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tal_qc_azs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TAL_QC_AZS_METADATA)
    execution.params(params)
    args = tal_qc_azs_cargs(params, execution)
    out = tal_qc_azs_outputs(params, execution)
    execution.run(args)
    return out

def tal_qc_azs(
    logfile: InputPathType,
    runner: Runner | None = None,
) -> TalQcAzsOutputs:
    """
    tal_QC_AZS

    A tool that processes a logfile.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        logfile: Input logfile for processing.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tal_qc_azs_params(
        logfile=logfile,
    )
    return tal_qc_azs_execute(params, runner)

__all__ = [
    "TalQcAzs",
    "TalQcAzsOutputs",
    "TAL_QC_AZS_METADATA",
    "tal_qc_azs_cargs",
    "tal_qc_azs_outputs",
    "tal_qc_azs_params",
    "tal_qc_azs_execute",
    "tal_qc_azs_validate",
    "tal_qc_azs",
]