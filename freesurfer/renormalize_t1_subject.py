# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RENORMALIZE_T1_SUBJECT_METADATA = Metadata(
    id="renormalize_T1_subject",
    name="renormalize_T1_subject",
    package="freesurfer",
)

RenormalizeT1Subject = typing.TypedDict(
    "RenormalizeT1Subject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/renormalize_T1_subject"]],
        "subject_dir": str,
    },
)

@dataclasses.dataclass
class RenormalizeT1SubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def renormalize_t1_subject_params(
    subject_dir: str,
) -> RenormalizeT1Subject:
    """
    Build parameters.

    Args:
        subject_dir: Directory of the subject containing T1 images to renormalize

    Returns:
        Parameter dictionary.
    """
    params: RenormalizeT1Subject = {
        "@type": "freesurfer/renormalize_T1_subject",
        "subject_dir": subject_dir,
    }
    return params

def renormalize_t1_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RenormalizeT1Subject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')

def renormalize_t1_subject_cargs(params: RenormalizeT1Subject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("renormalize_T1_subject")
    cargs.append(params["subject_dir"])
    return cargs

def renormalize_t1_subject_outputs(params: RenormalizeT1Subject, execution: Execution) -> RenormalizeT1SubjectOutputs:
    """Build the RenormalizeT1SubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RenormalizeT1SubjectOutputs(
        root=root_v,
    )

def renormalize_t1_subject_execute(params: RenormalizeT1Subject, runner: Runner | None = None) -> RenormalizeT1SubjectOutputs:
    """
    renormalize_T1_subject

    Renormalize T1 subject images using FreeSurfer scripts

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    renormalize_t1_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RENORMALIZE_T1_SUBJECT_METADATA)
    execution.params(params)
    args = renormalize_t1_subject_cargs(params, execution)
    out = renormalize_t1_subject_outputs(params, execution)
    execution.run(args)
    return out

def renormalize_t1_subject(
    subject_dir: str,
    runner: Runner | None = None,
) -> RenormalizeT1SubjectOutputs:
    """
    renormalize_T1_subject

    Renormalize T1 subject images using FreeSurfer scripts

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Directory of the subject containing T1 images to renormalize
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = renormalize_t1_subject_params(
        subject_dir=subject_dir,
    )
    return renormalize_t1_subject_execute(params, runner)

__all__ = [
    "RenormalizeT1Subject",
    "RenormalizeT1SubjectOutputs",
    "RENORMALIZE_T1_SUBJECT_METADATA",
    "renormalize_t1_subject_cargs",
    "renormalize_t1_subject_outputs",
    "renormalize_t1_subject_params",
    "renormalize_t1_subject_execute",
    "renormalize_t1_subject_validate",
    "renormalize_t1_subject",
]