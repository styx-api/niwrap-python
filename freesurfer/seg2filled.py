# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEG2FILLED_METADATA = Metadata(
    id="seg2filled",
    name="seg2filled",
    package="freesurfer",
)

Seg2filled = typing.TypedDict(
    "Seg2filled",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/seg2filled"]],
        "seg_file": InputPathType,
        "norm_file": InputPathType,
        "output_file": str,
        "ndil": typing.NotRequired[int],
        "cavity_flag": typing.NotRequired[bool],
        "surf_name": typing.NotRequired[str],
        "surf_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Seg2filledOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_filled_mgz: OutputPathType
    """The output filled MGZ file."""

def seg2filled_params(
    seg_file: InputPathType,
    norm_file: InputPathType,
    output_file: str,
    ndil: int | None = None,
    cavity_flag: bool = False,
    surf_name: str | None = None,
    surf_dir: str | None = None,
) -> Seg2filled:
    """
    Build parameters.

    Args:
        seg_file: Input aseg-style segmentation file (e.g., seg.mgz)
        norm_file: Normalization file (e.g., norm.mgz)
        output_file: Filled output file (e.g., filled.mgz)
        ndil: Number of iterations for dilation used to speed cavity detection.
        cavity_flag: Simulate a cavity to test the filling operation.
        surf_name: Name for the surface output (creates ?h.surfname).
        surf_dir: Directory to put the surface (default is same as filled).

    Returns:
        Parameter dictionary.
    """
    params: Seg2filled = {
        "@type": "freesurfer/seg2filled",
        "seg_file": seg_file,
        "norm_file": norm_file,
        "output_file": output_file,
        "cavity_flag": cavity_flag,
    }
    if ndil is not None:
        params["ndil"] = ndil
    if surf_name is not None:
        params["surf_name"] = surf_name
    if surf_dir is not None:
        params["surf_dir"] = surf_dir
    return params

def seg2filled_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Seg2filled."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("seg_file", None) is None:
        raise StyxValidationError("`seg_file` must not be None")
    if not isinstance(params["seg_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`seg_file` has the wrong type: Received `{type(params["seg_file"])}` expected `InputPathType`')
    if params.get("norm_file", None) is None:
        raise StyxValidationError("`norm_file` must not be None")
    if not isinstance(params["norm_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`norm_file` has the wrong type: Received `{type(params["norm_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("ndil", None) is not None:
        if not isinstance(params["ndil"], int):
            raise StyxValidationError(f'`ndil` has the wrong type: Received `{type(params["ndil"])}` expected `int`')
    if params.get("cavity_flag", None) is not None:
        if not isinstance(params["cavity_flag"], bool):
            raise StyxValidationError(f'`cavity_flag` has the wrong type: Received `{type(params["cavity_flag"])}` expected `bool`')
    if params.get("surf_name", None) is not None:
        if not isinstance(params["surf_name"], str):
            raise StyxValidationError(f'`surf_name` has the wrong type: Received `{type(params["surf_name"])}` expected `str`')
    if params.get("surf_dir", None) is not None:
        if not isinstance(params["surf_dir"], str):
            raise StyxValidationError(f'`surf_dir` has the wrong type: Received `{type(params["surf_dir"])}` expected `str`')

def seg2filled_cargs(params: Seg2filled, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("seg2filled")
    cargs.append("-seg")
    cargs.append(execution.input_file(params["seg_file"]))
    cargs.append("-norm")
    cargs.append(execution.input_file(params["norm_file"]))
    cargs.append("-o")
    cargs.append(params["output_file"])
    v_0 = params.get("ndil")
    if v_0 is not None:
        cargs.append("--ndil")
        cargs.append(str(v_0))
    if params.get("cavity_flag"):
        cargs.append("--cavity")
    v_1 = params.get("surf_name")
    if v_1 is not None:
        cargs.append("--surf")
        cargs.append(v_1)
    v_2 = params.get("surf_dir")
    if v_2 is not None:
        cargs.append("--surfdir")
        cargs.append(v_2)
    return cargs

def seg2filled_outputs(params: Seg2filled, execution: Execution) -> Seg2filledOutputs:
    """Build the Seg2filledOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_filled_mgz_v: OutputPathType = execution.output_file(params["output_file"])
    return Seg2filledOutputs(
        root=root_v,
        out_filled_mgz=out_filled_mgz_v,
    )

def seg2filled_execute(params: Seg2filled, runner: Runner | None = None) -> Seg2filledOutputs:
    """
    seg2filled

    Creates a filled.mgz from an aseg-style segmentation using SAMSEG segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    seg2filled_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEG2FILLED_METADATA)
    execution.params(params)
    args = seg2filled_cargs(params, execution)
    out = seg2filled_outputs(params, execution)
    execution.run(args)
    return out

def seg2filled(
    seg_file: InputPathType,
    norm_file: InputPathType,
    output_file: str,
    ndil: int | None = None,
    cavity_flag: bool = False,
    surf_name: str | None = None,
    surf_dir: str | None = None,
    runner: Runner | None = None,
) -> Seg2filledOutputs:
    """
    seg2filled

    Creates a filled.mgz from an aseg-style segmentation using SAMSEG segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        seg_file: Input aseg-style segmentation file (e.g., seg.mgz)
        norm_file: Normalization file (e.g., norm.mgz)
        output_file: Filled output file (e.g., filled.mgz)
        ndil: Number of iterations for dilation used to speed cavity detection.
        cavity_flag: Simulate a cavity to test the filling operation.
        surf_name: Name for the surface output (creates ?h.surfname).
        surf_dir: Directory to put the surface (default is same as filled).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = seg2filled_params(
        seg_file=seg_file,
        norm_file=norm_file,
        output_file=output_file,
        ndil=ndil,
        cavity_flag=cavity_flag,
        surf_name=surf_name,
        surf_dir=surf_dir,
    )
    return seg2filled_execute(params, runner)

__all__ = [
    "Seg2filled",
    "Seg2filledOutputs",
    "SEG2FILLED_METADATA",
    "seg2filled_cargs",
    "seg2filled_outputs",
    "seg2filled_params",
    "seg2filled_execute",
    "seg2filled_validate",
    "seg2filled",
]