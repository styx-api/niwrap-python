# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_DISTANCE_TRANSFORM_METADATA = Metadata(
    id="mri_distance_transform",
    name="mri_distance_transform",
    package="freesurfer",
)

MriDistanceTransform = typing.TypedDict(
    "MriDistanceTransform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_distance_transform"]],
        "input_volume": InputPathType,
        "label": int,
        "max_distance": int,
        "mode": typing.NotRequired[int],
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriDistanceTransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume file after distance transform"""

def mri_distance_transform_params(
    input_volume: InputPathType,
    label: int,
    max_distance: int,
    output_volume: str,
    mode: int | None = None,
) -> MriDistanceTransform:
    """
    Build parameters.

    Args:
        input_volume: Input volume file
        label: Label value for distance transform calculation
        max_distance: Maximum distance for the transform
        output_volume: Output volume file
        mode: Mode of the distance transform: 1 = outside, 2 = inside, 3 = both, 4\
            = both unsigned

    Returns:
        Parameter dictionary.
    """
    params: MriDistanceTransform = {
        "@type": "freesurfer/mri_distance_transform",
        "input_volume": input_volume,
        "label": label,
        "max_distance": max_distance,
        "output_volume": output_volume,
    }
    if mode is not None:
        params["mode"] = mode
    return params

def mri_distance_transform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriDistanceTransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], int):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `int`')
    if params.get("max_distance", None) is None:
        raise StyxValidationError("`max_distance` must not be None")
    if not isinstance(params["max_distance"], int):
        raise StyxValidationError(f'`max_distance` has the wrong type: Received `{type(params["max_distance"])}` expected `int`')
    if params.get("mode", None) is not None:
        if not isinstance(params["mode"], int):
            raise StyxValidationError(f'`mode` has the wrong type: Received `{type(params["mode"])}` expected `int`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_distance_transform_cargs(params: MriDistanceTransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_distance_transform")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["label"]))
    cargs.append(str(params["max_distance"]))
    v_0 = params.get("mode")
    if v_0 is not None:
        cargs.append(str(v_0))
    cargs.append(params["output_volume"])
    return cargs

def mri_distance_transform_outputs(params: MriDistanceTransform, execution: Execution) -> MriDistanceTransformOutputs:
    """Build the MriDistanceTransformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriDistanceTransformOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_distance_transform_execute(params: MriDistanceTransform, runner: Runner | None = None) -> MriDistanceTransformOutputs:
    """
    mri_distance_transform

    Tool to compute distance transforms on MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_distance_transform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_DISTANCE_TRANSFORM_METADATA)
    execution.params(params)
    args = mri_distance_transform_cargs(params, execution)
    out = mri_distance_transform_outputs(params, execution)
    execution.run(args)
    return out

def mri_distance_transform(
    input_volume: InputPathType,
    label: int,
    max_distance: int,
    output_volume: str,
    mode: int | None = None,
    runner: Runner | None = None,
) -> MriDistanceTransformOutputs:
    """
    mri_distance_transform

    Tool to compute distance transforms on MRI volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file
        label: Label value for distance transform calculation
        max_distance: Maximum distance for the transform
        output_volume: Output volume file
        mode: Mode of the distance transform: 1 = outside, 2 = inside, 3 = both, 4\
            = both unsigned
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_distance_transform_params(
        input_volume=input_volume,
        label=label,
        max_distance=max_distance,
        output_volume=output_volume,
        mode=mode,
    )
    return mri_distance_transform_execute(params, runner)

__all__ = [
    "MriDistanceTransform",
    "MriDistanceTransformOutputs",
    "MRI_DISTANCE_TRANSFORM_METADATA",
    "mri_distance_transform_cargs",
    "mri_distance_transform_outputs",
    "mri_distance_transform_params",
    "mri_distance_transform_execute",
    "mri_distance_transform_validate",
    "mri_distance_transform",
]