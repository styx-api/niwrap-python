# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SIMULATE_ATROPHY_METADATA = Metadata(
    id="mris_simulate_atrophy",
    name="mris_simulate_atrophy",
    package="freesurfer",
)

MrisSimulateAtrophy = typing.TypedDict(
    "MrisSimulateAtrophy",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_simulate_atrophy"]],
        "subject": str,
        "hemi": str,
        "label": str,
        "atrophy_fraction": float,
        "output_volume": str,
        "atrophy_percent": typing.NotRequired[float],
        "noise_level": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisSimulateAtrophyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Atrophy simulation output volume."""

def mris_simulate_atrophy_params(
    subject: str,
    hemi: str,
    label: str,
    atrophy_fraction: float,
    output_volume: str,
    atrophy_percent: float | None = None,
    noise_level: float | None = None,
) -> MrisSimulateAtrophy:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        hemi: Hemisphere (e.g. lh or rh)
        label: Label of the brain region
        atrophy_fraction: Target atrophy fraction
        output_volume: Output volume file
        atrophy_percent: Percentage atrophy to simulate of structure
        noise_level: Gaussian noise level to add

    Returns:
        Parameter dictionary.
    """
    params: MrisSimulateAtrophy = {
        "@type": "freesurfer/mris_simulate_atrophy",
        "subject": subject,
        "hemi": hemi,
        "label": label,
        "atrophy_fraction": atrophy_fraction,
        "output_volume": output_volume,
    }
    if atrophy_percent is not None:
        params["atrophy_percent"] = atrophy_percent
    if noise_level is not None:
        params["noise_level"] = noise_level
    return params

def mris_simulate_atrophy_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSimulateAtrophy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], str):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `str`')
    if params.get("atrophy_fraction", None) is None:
        raise StyxValidationError("`atrophy_fraction` must not be None")
    if not isinstance(params["atrophy_fraction"], (float, int)):
        raise StyxValidationError(f'`atrophy_fraction` has the wrong type: Received `{type(params["atrophy_fraction"])}` expected `float`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("atrophy_percent", None) is not None:
        if not isinstance(params["atrophy_percent"], (float, int)):
            raise StyxValidationError(f'`atrophy_percent` has the wrong type: Received `{type(params["atrophy_percent"])}` expected `float`')
    if params.get("noise_level", None) is not None:
        if not isinstance(params["noise_level"], (float, int)):
            raise StyxValidationError(f'`noise_level` has the wrong type: Received `{type(params["noise_level"])}` expected `float`')

def mris_simulate_atrophy_cargs(params: MrisSimulateAtrophy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_simulate_atrophy")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(params["label"])
    cargs.append(str(params["atrophy_fraction"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("atrophy_percent")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    v_1 = params.get("noise_level")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    return cargs

def mris_simulate_atrophy_outputs(params: MrisSimulateAtrophy, execution: Execution) -> MrisSimulateAtrophyOutputs:
    """Build the MrisSimulateAtrophyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MrisSimulateAtrophyOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mris_simulate_atrophy_execute(params: MrisSimulateAtrophy, runner: Runner | None = None) -> MrisSimulateAtrophyOutputs:
    """
    mris_simulate_atrophy

    Simulate atrophy on brain structures.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_simulate_atrophy_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SIMULATE_ATROPHY_METADATA)
    execution.params(params)
    args = mris_simulate_atrophy_cargs(params, execution)
    out = mris_simulate_atrophy_outputs(params, execution)
    execution.run(args)
    return out

def mris_simulate_atrophy(
    subject: str,
    hemi: str,
    label: str,
    atrophy_fraction: float,
    output_volume: str,
    atrophy_percent: float | None = None,
    noise_level: float | None = None,
    runner: Runner | None = None,
) -> MrisSimulateAtrophyOutputs:
    """
    mris_simulate_atrophy

    Simulate atrophy on brain structures.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        hemi: Hemisphere (e.g. lh or rh)
        label: Label of the brain region
        atrophy_fraction: Target atrophy fraction
        output_volume: Output volume file
        atrophy_percent: Percentage atrophy to simulate of structure
        noise_level: Gaussian noise level to add
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_simulate_atrophy_params(
        subject=subject,
        hemi=hemi,
        label=label,
        atrophy_fraction=atrophy_fraction,
        output_volume=output_volume,
        atrophy_percent=atrophy_percent,
        noise_level=noise_level,
    )
    return mris_simulate_atrophy_execute(params, runner)

__all__ = [
    "MrisSimulateAtrophy",
    "MrisSimulateAtrophyOutputs",
    "MRIS_SIMULATE_ATROPHY_METADATA",
    "mris_simulate_atrophy_cargs",
    "mris_simulate_atrophy_outputs",
    "mris_simulate_atrophy_params",
    "mris_simulate_atrophy_execute",
    "mris_simulate_atrophy_validate",
    "mris_simulate_atrophy",
]