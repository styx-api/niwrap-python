# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_FWHM_METADATA = Metadata(
    id="mris_fwhm",
    name="mris_fwhm",
    package="freesurfer",
)

MrisFwhm = typing.TypedDict(
    "MrisFwhm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_fwhm"]],
        "input_file": InputPathType,
        "subject": str,
        "hemi": str,
        "surf": typing.NotRequired[str],
        "label_file": typing.NotRequired[InputPathType],
        "cortex_flag": typing.NotRequired[bool],
        "mask_file": typing.NotRequired[InputPathType],
        "x_matrix": typing.NotRequired[InputPathType],
        "detrend_order": typing.NotRequired[float],
        "smooth_only_flag": typing.NotRequired[bool],
        "no_detrend_flag": typing.NotRequired[bool],
        "sqr_flag": typing.NotRequired[bool],
        "sum_file": typing.NotRequired[str],
        "dat_file": typing.NotRequired[str],
        "ar1dat_file": typing.NotRequired[str],
        "ar1vol": typing.NotRequired[str],
        "fwhmmap": typing.NotRequired[str],
        "prune_flag": typing.NotRequired[bool],
        "no_prune_flag": typing.NotRequired[bool],
        "out_mask": typing.NotRequired[str],
        "varnorm_flag": typing.NotRequired[bool],
        "fwhm": typing.NotRequired[float],
        "niters_only": typing.NotRequired[str],
        "output_file": str,
        "sd": typing.NotRequired[str],
        "synth_flag": typing.NotRequired[bool],
        "synth_frames": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "debug_flag": typing.NotRequired[bool],
        "checkopts_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisFwhmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_fwhm_params(
    input_file: InputPathType,
    subject: str,
    hemi: str,
    output_file: str,
    surf: str | None = None,
    label_file: InputPathType | None = None,
    cortex_flag: bool = False,
    mask_file: InputPathType | None = None,
    x_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    smooth_only_flag: bool = False,
    no_detrend_flag: bool = False,
    sqr_flag: bool = False,
    sum_file: str | None = None,
    dat_file: str | None = None,
    ar1dat_file: str | None = None,
    ar1vol: str | None = None,
    fwhmmap: str | None = None,
    prune_flag: bool = False,
    no_prune_flag: bool = False,
    out_mask: str | None = None,
    varnorm_flag: bool = False,
    fwhm: float | None = None,
    niters_only: str | None = None,
    sd: str | None = None,
    synth_flag: bool = False,
    synth_frames: float | None = None,
    threads: float | None = None,
    debug_flag: bool = False,
    checkopts_flag: bool = False,
    version_flag: bool = False,
) -> MrisFwhm:
    """
    Build parameters.

    Args:
        input_file: Input data file.
        subject: Subject whose surface the input is defined on.
        hemi: Hemifield that the input is defined on. Legal values are lh and rh.
        output_file: Output file to save the processed data.
        surf: Surface name to compute AR1 on. Default is white.
        label_file: Label file to be used as a mask.
        cortex_flag: Use hemi.cortex.label as a mask.
        mask_file: Mask file. Compute AR1 only over voxels in the given mask.
        x_matrix: Detrend data with the matrix in x.mat.
        detrend_order: Order of polynomial detrending.
        smooth_only_flag: Only smooth the data, implies --no-detrend.
        no_detrend_flag: Turn off polynomial detrending.
        sqr_flag: Compute square of input before smoothing.
        sum_file: Prints ascii summary to sumfile.
        dat_file: File for FWHM data.
        ar1dat_file: File containing ar1mean and ar1std.
        ar1vol: Save spatial AR1 as an overlay.
        fwhmmap: Save vertex-wise spatial FWHM as an overlay.
        prune_flag: Remove any voxel that is zero in any subject (after any\
            inversion).
        no_prune_flag: Do not prune (default).
        out_mask: File to save the final mask.
        varnorm_flag: Normalize the variance across space within any mask.
        fwhm: Smooth input by the specified FWHM in mm.
        niters_only: File that reports the number of iterations needed to achieve\
            the specified FWHM.
        sd: Subjects directory.
        synth_flag: Synthesize input with white gaussian noise.
        synth_frames: Number of frames for synthesized input.
        threads: Number of threads to use.
        debug_flag: Turn on debugging.
        checkopts_flag: Don't run anything, just check options and exit.
        version_flag: Print out version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MrisFwhm = {
        "@type": "freesurfer/mris_fwhm",
        "input_file": input_file,
        "subject": subject,
        "hemi": hemi,
        "output_file": output_file,
        "cortex_flag": cortex_flag,
        "smooth_only_flag": smooth_only_flag,
        "no_detrend_flag": no_detrend_flag,
        "sqr_flag": sqr_flag,
        "prune_flag": prune_flag,
        "no_prune_flag": no_prune_flag,
        "varnorm_flag": varnorm_flag,
        "synth_flag": synth_flag,
        "debug_flag": debug_flag,
        "checkopts_flag": checkopts_flag,
        "version_flag": version_flag,
    }
    if surf is not None:
        params["surf"] = surf
    if label_file is not None:
        params["label_file"] = label_file
    if mask_file is not None:
        params["mask_file"] = mask_file
    if x_matrix is not None:
        params["x_matrix"] = x_matrix
    if detrend_order is not None:
        params["detrend_order"] = detrend_order
    if sum_file is not None:
        params["sum_file"] = sum_file
    if dat_file is not None:
        params["dat_file"] = dat_file
    if ar1dat_file is not None:
        params["ar1dat_file"] = ar1dat_file
    if ar1vol is not None:
        params["ar1vol"] = ar1vol
    if fwhmmap is not None:
        params["fwhmmap"] = fwhmmap
    if out_mask is not None:
        params["out_mask"] = out_mask
    if fwhm is not None:
        params["fwhm"] = fwhm
    if niters_only is not None:
        params["niters_only"] = niters_only
    if sd is not None:
        params["sd"] = sd
    if synth_frames is not None:
        params["synth_frames"] = synth_frames
    if threads is not None:
        params["threads"] = threads
    return params

def mris_fwhm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisFwhm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("cortex_flag", None) is not None:
        if not isinstance(params["cortex_flag"], bool):
            raise StyxValidationError(f'`cortex_flag` has the wrong type: Received `{type(params["cortex_flag"])}` expected `bool`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("x_matrix", None) is not None:
        if not isinstance(params["x_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`x_matrix` has the wrong type: Received `{type(params["x_matrix"])}` expected `InputPathType`')
    if params.get("detrend_order", None) is not None:
        if not isinstance(params["detrend_order"], (float, int)):
            raise StyxValidationError(f'`detrend_order` has the wrong type: Received `{type(params["detrend_order"])}` expected `float`')
    if params.get("smooth_only_flag", None) is not None:
        if not isinstance(params["smooth_only_flag"], bool):
            raise StyxValidationError(f'`smooth_only_flag` has the wrong type: Received `{type(params["smooth_only_flag"])}` expected `bool`')
    if params.get("no_detrend_flag", None) is not None:
        if not isinstance(params["no_detrend_flag"], bool):
            raise StyxValidationError(f'`no_detrend_flag` has the wrong type: Received `{type(params["no_detrend_flag"])}` expected `bool`')
    if params.get("sqr_flag", None) is not None:
        if not isinstance(params["sqr_flag"], bool):
            raise StyxValidationError(f'`sqr_flag` has the wrong type: Received `{type(params["sqr_flag"])}` expected `bool`')
    if params.get("sum_file", None) is not None:
        if not isinstance(params["sum_file"], str):
            raise StyxValidationError(f'`sum_file` has the wrong type: Received `{type(params["sum_file"])}` expected `str`')
    if params.get("dat_file", None) is not None:
        if not isinstance(params["dat_file"], str):
            raise StyxValidationError(f'`dat_file` has the wrong type: Received `{type(params["dat_file"])}` expected `str`')
    if params.get("ar1dat_file", None) is not None:
        if not isinstance(params["ar1dat_file"], str):
            raise StyxValidationError(f'`ar1dat_file` has the wrong type: Received `{type(params["ar1dat_file"])}` expected `str`')
    if params.get("ar1vol", None) is not None:
        if not isinstance(params["ar1vol"], str):
            raise StyxValidationError(f'`ar1vol` has the wrong type: Received `{type(params["ar1vol"])}` expected `str`')
    if params.get("fwhmmap", None) is not None:
        if not isinstance(params["fwhmmap"], str):
            raise StyxValidationError(f'`fwhmmap` has the wrong type: Received `{type(params["fwhmmap"])}` expected `str`')
    if params.get("prune_flag", None) is not None:
        if not isinstance(params["prune_flag"], bool):
            raise StyxValidationError(f'`prune_flag` has the wrong type: Received `{type(params["prune_flag"])}` expected `bool`')
    if params.get("no_prune_flag", None) is not None:
        if not isinstance(params["no_prune_flag"], bool):
            raise StyxValidationError(f'`no_prune_flag` has the wrong type: Received `{type(params["no_prune_flag"])}` expected `bool`')
    if params.get("out_mask", None) is not None:
        if not isinstance(params["out_mask"], str):
            raise StyxValidationError(f'`out_mask` has the wrong type: Received `{type(params["out_mask"])}` expected `str`')
    if params.get("varnorm_flag", None) is not None:
        if not isinstance(params["varnorm_flag"], bool):
            raise StyxValidationError(f'`varnorm_flag` has the wrong type: Received `{type(params["varnorm_flag"])}` expected `bool`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("niters_only", None) is not None:
        if not isinstance(params["niters_only"], str):
            raise StyxValidationError(f'`niters_only` has the wrong type: Received `{type(params["niters_only"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("sd", None) is not None:
        if not isinstance(params["sd"], str):
            raise StyxValidationError(f'`sd` has the wrong type: Received `{type(params["sd"])}` expected `str`')
    if params.get("synth_flag", None) is not None:
        if not isinstance(params["synth_flag"], bool):
            raise StyxValidationError(f'`synth_flag` has the wrong type: Received `{type(params["synth_flag"])}` expected `bool`')
    if params.get("synth_frames", None) is not None:
        if not isinstance(params["synth_frames"], (float, int)):
            raise StyxValidationError(f'`synth_frames` has the wrong type: Received `{type(params["synth_frames"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("checkopts_flag", None) is not None:
        if not isinstance(params["checkopts_flag"], bool):
            raise StyxValidationError(f'`checkopts_flag` has the wrong type: Received `{type(params["checkopts_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def mris_fwhm_cargs(params: MrisFwhm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_fwhm")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("--subject")
    cargs.append(params["subject"])
    cargs.append("--hemi")
    cargs.append(params["hemi"])
    v_0 = params.get("surf")
    if v_0 is not None:
        cargs.append("--surf")
        cargs.append(v_0)
    v_1 = params.get("label_file")
    if v_1 is not None:
        cargs.append("--label")
        cargs.append(execution.input_file(v_1))
    if params.get("cortex_flag"):
        cargs.append("--cortex")
    v_2 = params.get("mask_file")
    if v_2 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("x_matrix")
    if v_3 is not None:
        cargs.append("--X")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("detrend_order")
    if v_4 is not None:
        cargs.append("--detrend")
        cargs.append(str(v_4))
    if params.get("smooth_only_flag"):
        cargs.append("--smooth-only")
    if params.get("no_detrend_flag"):
        cargs.append("--no-detrend")
    if params.get("sqr_flag"):
        cargs.append("--sqr")
    v_5 = params.get("sum_file")
    if v_5 is not None:
        cargs.append("--sum")
        cargs.append(v_5)
    v_6 = params.get("dat_file")
    if v_6 is not None:
        cargs.append("--dat")
        cargs.append(v_6)
    v_7 = params.get("ar1dat_file")
    if v_7 is not None:
        cargs.append("--ar1dat")
        cargs.append(v_7)
    v_8 = params.get("ar1vol")
    if v_8 is not None:
        cargs.append("--ar1")
        cargs.append(v_8)
    v_9 = params.get("fwhmmap")
    if v_9 is not None:
        cargs.append("--fwhm-map")
        cargs.append(v_9)
    if params.get("prune_flag"):
        cargs.append("--prune")
    if params.get("no_prune_flag"):
        cargs.append("--no-prune")
    v_10 = params.get("out_mask")
    if v_10 is not None:
        cargs.append("--out-mask")
        cargs.append(v_10)
    if params.get("varnorm_flag"):
        cargs.append("--varnorm")
    v_11 = params.get("fwhm")
    if v_11 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_11))
    v_12 = params.get("niters_only")
    if v_12 is not None:
        cargs.append("--niters-only")
        cargs.append(v_12)
    cargs.append("--o")
    cargs.append(params["output_file"])
    v_13 = params.get("sd")
    if v_13 is not None:
        cargs.append("--sd")
        cargs.append(v_13)
    if params.get("synth_flag"):
        cargs.append("--synth")
    v_14 = params.get("synth_frames")
    if v_14 is not None:
        cargs.append("--synth-frames")
        cargs.append(str(v_14))
    v_15 = params.get("threads")
    if v_15 is not None:
        cargs.append("--threads")
        cargs.append(str(v_15))
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("checkopts_flag"):
        cargs.append("--checkopts")
    if params.get("version_flag"):
        cargs.append("--version")
    return cargs

def mris_fwhm_outputs(params: MrisFwhm, execution: Execution) -> MrisFwhmOutputs:
    """Build the MrisFwhmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisFwhmOutputs(
        root=root_v,
    )

def mris_fwhm_execute(params: MrisFwhm, runner: Runner | None = None) -> MrisFwhmOutputs:
    """
    mris_fwhm

    Smooths surface data and/or estimates FWHM

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_fwhm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_FWHM_METADATA)
    execution.params(params)
    args = mris_fwhm_cargs(params, execution)
    out = mris_fwhm_outputs(params, execution)
    execution.run(args)
    return out

def mris_fwhm(
    input_file: InputPathType,
    subject: str,
    hemi: str,
    output_file: str,
    surf: str | None = None,
    label_file: InputPathType | None = None,
    cortex_flag: bool = False,
    mask_file: InputPathType | None = None,
    x_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    smooth_only_flag: bool = False,
    no_detrend_flag: bool = False,
    sqr_flag: bool = False,
    sum_file: str | None = None,
    dat_file: str | None = None,
    ar1dat_file: str | None = None,
    ar1vol: str | None = None,
    fwhmmap: str | None = None,
    prune_flag: bool = False,
    no_prune_flag: bool = False,
    out_mask: str | None = None,
    varnorm_flag: bool = False,
    fwhm: float | None = None,
    niters_only: str | None = None,
    sd: str | None = None,
    synth_flag: bool = False,
    synth_frames: float | None = None,
    threads: float | None = None,
    debug_flag: bool = False,
    checkopts_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> MrisFwhmOutputs:
    """
    mris_fwhm

    Smooths surface data and/or estimates FWHM

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input data file.
        subject: Subject whose surface the input is defined on.
        hemi: Hemifield that the input is defined on. Legal values are lh and rh.
        output_file: Output file to save the processed data.
        surf: Surface name to compute AR1 on. Default is white.
        label_file: Label file to be used as a mask.
        cortex_flag: Use hemi.cortex.label as a mask.
        mask_file: Mask file. Compute AR1 only over voxels in the given mask.
        x_matrix: Detrend data with the matrix in x.mat.
        detrend_order: Order of polynomial detrending.
        smooth_only_flag: Only smooth the data, implies --no-detrend.
        no_detrend_flag: Turn off polynomial detrending.
        sqr_flag: Compute square of input before smoothing.
        sum_file: Prints ascii summary to sumfile.
        dat_file: File for FWHM data.
        ar1dat_file: File containing ar1mean and ar1std.
        ar1vol: Save spatial AR1 as an overlay.
        fwhmmap: Save vertex-wise spatial FWHM as an overlay.
        prune_flag: Remove any voxel that is zero in any subject (after any\
            inversion).
        no_prune_flag: Do not prune (default).
        out_mask: File to save the final mask.
        varnorm_flag: Normalize the variance across space within any mask.
        fwhm: Smooth input by the specified FWHM in mm.
        niters_only: File that reports the number of iterations needed to achieve\
            the specified FWHM.
        sd: Subjects directory.
        synth_flag: Synthesize input with white gaussian noise.
        synth_frames: Number of frames for synthesized input.
        threads: Number of threads to use.
        debug_flag: Turn on debugging.
        checkopts_flag: Don't run anything, just check options and exit.
        version_flag: Print out version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_fwhm_params(
        input_file=input_file,
        subject=subject,
        hemi=hemi,
        output_file=output_file,
        surf=surf,
        label_file=label_file,
        cortex_flag=cortex_flag,
        mask_file=mask_file,
        x_matrix=x_matrix,
        detrend_order=detrend_order,
        smooth_only_flag=smooth_only_flag,
        no_detrend_flag=no_detrend_flag,
        sqr_flag=sqr_flag,
        sum_file=sum_file,
        dat_file=dat_file,
        ar1dat_file=ar1dat_file,
        ar1vol=ar1vol,
        fwhmmap=fwhmmap,
        prune_flag=prune_flag,
        no_prune_flag=no_prune_flag,
        out_mask=out_mask,
        varnorm_flag=varnorm_flag,
        fwhm=fwhm,
        niters_only=niters_only,
        sd=sd,
        synth_flag=synth_flag,
        synth_frames=synth_frames,
        threads=threads,
        debug_flag=debug_flag,
        checkopts_flag=checkopts_flag,
        version_flag=version_flag,
    )
    return mris_fwhm_execute(params, runner)

__all__ = [
    "MrisFwhm",
    "MrisFwhmOutputs",
    "MRIS_FWHM_METADATA",
    "mris_fwhm_cargs",
    "mris_fwhm_outputs",
    "mris_fwhm_params",
    "mris_fwhm_execute",
    "mris_fwhm_validate",
    "mris_fwhm",
]