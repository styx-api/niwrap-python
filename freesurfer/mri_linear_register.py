# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LINEAR_REGISTER_METADATA = Metadata(
    id="mri_linear_register",
    name="mri_linear_register",
    package="freesurfer",
)

MriLinearRegister = typing.TypedDict(
    "MriLinearRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_linear_register"]],
        "input_brain": InputPathType,
        "template": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MriLinearRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_registered_image: OutputPathType
    """The registered brain image"""

def mri_linear_register_params(
    input_brain: InputPathType,
    template: InputPathType,
    output_file: str,
) -> MriLinearRegister:
    """
    Build parameters.

    Args:
        input_brain: Input brain image to be registered
        template: Template to which the brain image will be registered
        output_file: Output file name for the registered brain image

    Returns:
        Parameter dictionary.
    """
    params: MriLinearRegister = {
        "@type": "freesurfer/mri_linear_register",
        "input_brain": input_brain,
        "template": template,
        "output_file": output_file,
    }
    return params

def mri_linear_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLinearRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_brain", None) is None:
        raise StyxValidationError("`input_brain` must not be None")
    if not isinstance(params["input_brain"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_brain` has the wrong type: Received `{type(params["input_brain"])}` expected `InputPathType`')
    if params.get("template", None) is None:
        raise StyxValidationError("`template` must not be None")
    if not isinstance(params["template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_linear_register_cargs(params: MriLinearRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_linear_register")
    cargs.append(execution.input_file(params["input_brain"]))
    cargs.append(execution.input_file(params["template"]))
    cargs.append(params["output_file"])
    return cargs

def mri_linear_register_outputs(params: MriLinearRegister, execution: Execution) -> MriLinearRegisterOutputs:
    """Build the MriLinearRegisterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_registered_image_v: OutputPathType = execution.output_file(params["output_file"])
    return MriLinearRegisterOutputs(
        root=root_v,
        output_registered_image=output_registered_image_v,
    )

def mri_linear_register_execute(params: MriLinearRegister, runner: Runner | None = None) -> MriLinearRegisterOutputs:
    """
    mri_linear_register

    A tool for linear registration of MRI brain images to a template

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_linear_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LINEAR_REGISTER_METADATA)
    execution.params(params)
    args = mri_linear_register_cargs(params, execution)
    out = mri_linear_register_outputs(params, execution)
    execution.run(args)
    return out

def mri_linear_register(
    input_brain: InputPathType,
    template: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> MriLinearRegisterOutputs:
    """
    mri_linear_register

    A tool for linear registration of MRI brain images to a template

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_brain: Input brain image to be registered
        template: Template to which the brain image will be registered
        output_file: Output file name for the registered brain image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_linear_register_params(
        input_brain=input_brain,
        template=template,
        output_file=output_file,
    )
    return mri_linear_register_execute(params, runner)

__all__ = [
    "MriLinearRegister",
    "MriLinearRegisterOutputs",
    "MRI_LINEAR_REGISTER_METADATA",
    "mri_linear_register_cargs",
    "mri_linear_register_outputs",
    "mri_linear_register_params",
    "mri_linear_register_execute",
    "mri_linear_register_validate",
    "mri_linear_register",
]