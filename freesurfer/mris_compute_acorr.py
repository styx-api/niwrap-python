# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_COMPUTE_ACORR_METADATA = Metadata(
    id="mris_compute_acorr",
    name="mris_compute_acorr",
    package="freesurfer",
)

MrisComputeAcorr = typing.TypedDict(
    "MrisComputeAcorr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_compute_acorr"]],
        "output_subject": str,
        "hemi": str,
        "surf": InputPathType,
        "curv": InputPathType,
        "c1_subjects": list[str],
        "c2_subjects": list[str],
    },
)

@dataclasses.dataclass
class MrisComputeAcorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_compute_acorr_params(
    output_subject: str,
    hemi: str,
    surf: InputPathType,
    curv: InputPathType,
    c1_subjects: list[str],
    c2_subjects: list[str],
) -> MrisComputeAcorr:
    """
    Build parameters.

    Args:
        output_subject: The output subject file.
        hemi: Specify the hemisphere for processing.
        surf: The surface file which must be a spherical surface suitable for\
            computing geodesics.
        curv: The input curvature file.
        c1_subjects: List of subjects from one class.
        c2_subjects: List of subjects from another class.

    Returns:
        Parameter dictionary.
    """
    params: MrisComputeAcorr = {
        "@type": "freesurfer/mris_compute_acorr",
        "output_subject": output_subject,
        "hemi": hemi,
        "surf": surf,
        "curv": curv,
        "c1_subjects": c1_subjects,
        "c2_subjects": c2_subjects,
    }
    return params

def mris_compute_acorr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisComputeAcorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_subject", None) is None:
        raise StyxValidationError("`output_subject` must not be None")
    if not isinstance(params["output_subject"], str):
        raise StyxValidationError(f'`output_subject` has the wrong type: Received `{type(params["output_subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf", None) is None:
        raise StyxValidationError("`surf` must not be None")
    if not isinstance(params["surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `InputPathType`')
    if params.get("curv", None) is None:
        raise StyxValidationError("`curv` must not be None")
    if not isinstance(params["curv"], (pathlib.Path, str)):
        raise StyxValidationError(f'`curv` has the wrong type: Received `{type(params["curv"])}` expected `InputPathType`')
    if params.get("c1_subjects", None) is None:
        raise StyxValidationError("`c1_subjects` must not be None")
    if not isinstance(params["c1_subjects"], list):
        raise StyxValidationError(f'`c1_subjects` has the wrong type: Received `{type(params["c1_subjects"])}` expected `list[str]`')
    for e in params["c1_subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`c1_subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("c2_subjects", None) is None:
        raise StyxValidationError("`c2_subjects` must not be None")
    if not isinstance(params["c2_subjects"], list):
        raise StyxValidationError(f'`c2_subjects` has the wrong type: Received `{type(params["c2_subjects"])}` expected `list[str]`')
    for e_2 in params["c2_subjects"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`c2_subjects` has the wrong type: Received `{type(e_2)}` expected `list[str]`')

def mris_compute_acorr_cargs(params: MrisComputeAcorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_compute_acorr")
    cargs.append("-o")
    cargs.append(params["output_subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["surf"]))
    cargs.append(execution.input_file(params["curv"]))
    for item0 in params["c1_subjects"]:
        cargs.append(item0)
    for item1 in params["c2_subjects"]:
        cargs.append(item1)
    return cargs

def mris_compute_acorr_outputs(params: MrisComputeAcorr, execution: Execution) -> MrisComputeAcorrOutputs:
    """Build the MrisComputeAcorrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisComputeAcorrOutputs(
        root=root_v,
    )

def mris_compute_acorr_execute(params: MrisComputeAcorr, runner: Runner | None = None) -> MrisComputeAcorrOutputs:
    """
    mris_compute_acorr

    Compute the autocorrelation function of a curvature file on a spherical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_compute_acorr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_COMPUTE_ACORR_METADATA)
    execution.params(params)
    args = mris_compute_acorr_cargs(params, execution)
    out = mris_compute_acorr_outputs(params, execution)
    execution.run(args)
    return out

def mris_compute_acorr(
    output_subject: str,
    hemi: str,
    surf: InputPathType,
    curv: InputPathType,
    c1_subjects: list[str],
    c2_subjects: list[str],
    runner: Runner | None = None,
) -> MrisComputeAcorrOutputs:
    """
    mris_compute_acorr

    Compute the autocorrelation function of a curvature file on a spherical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_subject: The output subject file.
        hemi: Specify the hemisphere for processing.
        surf: The surface file which must be a spherical surface suitable for\
            computing geodesics.
        curv: The input curvature file.
        c1_subjects: List of subjects from one class.
        c2_subjects: List of subjects from another class.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_compute_acorr_params(
        output_subject=output_subject,
        hemi=hemi,
        surf=surf,
        curv=curv,
        c1_subjects=c1_subjects,
        c2_subjects=c2_subjects,
    )
    return mris_compute_acorr_execute(params, runner)

__all__ = [
    "MrisComputeAcorr",
    "MrisComputeAcorrOutputs",
    "MRIS_COMPUTE_ACORR_METADATA",
    "mris_compute_acorr_cargs",
    "mris_compute_acorr_outputs",
    "mris_compute_acorr_params",
    "mris_compute_acorr_execute",
    "mris_compute_acorr_validate",
    "mris_compute_acorr",
]