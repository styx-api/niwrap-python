# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_DISTANCE_MAP_METADATA = Metadata(
    id="mris_distance_map",
    name="mris_distance_map",
    package="freesurfer",
)

MrisDistanceMap = typing.TypedDict(
    "MrisDistanceMap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_distance_map"]],
        "input_surface_file": InputPathType,
        "output_scalar_field": str,
    },
)

@dataclasses.dataclass
class MrisDistanceMapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output scalar field file"""

def mris_distance_map_params(
    input_surface_file: InputPathType,
    output_scalar_field: str,
) -> MrisDistanceMap:
    """
    Build parameters.

    Args:
        input_surface_file: Input surface file
        output_scalar_field: Output scalar field (.mgz)

    Returns:
        Parameter dictionary.
    """
    params: MrisDistanceMap = {
        "@type": "freesurfer/mris_distance_map",
        "input_surface_file": input_surface_file,
        "output_scalar_field": output_scalar_field,
    }
    return params

def mris_distance_map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisDistanceMap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface_file", None) is None:
        raise StyxValidationError("`input_surface_file` must not be None")
    if not isinstance(params["input_surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface_file` has the wrong type: Received `{type(params["input_surface_file"])}` expected `InputPathType`')
    if params.get("output_scalar_field", None) is None:
        raise StyxValidationError("`output_scalar_field` must not be None")
    if not isinstance(params["output_scalar_field"], str):
        raise StyxValidationError(f'`output_scalar_field` has the wrong type: Received `{type(params["output_scalar_field"])}` expected `str`')

def mris_distance_map_cargs(params: MrisDistanceMap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_distance_map")
    cargs.append(execution.input_file(params["input_surface_file"]))
    cargs.append(params["output_scalar_field"])
    return cargs

def mris_distance_map_outputs(params: MrisDistanceMap, execution: Execution) -> MrisDistanceMapOutputs:
    """Build the MrisDistanceMapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["output_scalar_field"]}.mgz')
    return MrisDistanceMapOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mris_distance_map_execute(params: MrisDistanceMap, runner: Runner | None = None) -> MrisDistanceMapOutputs:
    """
    mris_distance_map

    Tool to compute a distance map of each point on the surface to a reference point.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_distance_map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_DISTANCE_MAP_METADATA)
    execution.params(params)
    args = mris_distance_map_cargs(params, execution)
    out = mris_distance_map_outputs(params, execution)
    execution.run(args)
    return out

def mris_distance_map(
    input_surface_file: InputPathType,
    output_scalar_field: str,
    runner: Runner | None = None,
) -> MrisDistanceMapOutputs:
    """
    mris_distance_map

    Tool to compute a distance map of each point on the surface to a reference point.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface_file: Input surface file
        output_scalar_field: Output scalar field (.mgz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_distance_map_params(
        input_surface_file=input_surface_file,
        output_scalar_field=output_scalar_field,
    )
    return mris_distance_map_execute(params, runner)

__all__ = [
    "MrisDistanceMap",
    "MrisDistanceMapOutputs",
    "MRIS_DISTANCE_MAP_METADATA",
    "mris_distance_map_cargs",
    "mris_distance_map_outputs",
    "mris_distance_map_params",
    "mris_distance_map_execute",
    "mris_distance_map_validate",
    "mris_distance_map",
]