# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BETA2SXA_METADATA = Metadata(
    id="beta2sxa",
    name="beta2sxa",
    package="freesurfer",
)

Beta2sxa = typing.TypedDict(
    "Beta2sxa",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/beta2sxa"]],
        "beta_files": list[InputPathType],
        "number_of_conditions": float,
        "number_of_per_subjects": float,
        "sxa_output": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Beta2sxaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sxa_output_file: OutputPathType
    """Output file for tkmedit or tksurfer visualization."""

def beta2sxa_params(
    beta_files: list[InputPathType],
    number_of_conditions: float,
    number_of_per_subjects: float,
    sxa_output: str | None = None,
) -> Beta2sxa:
    """
    Build parameters.

    Args:
        beta_files: Input beta files, e.g., data.nii
        number_of_conditions: Number of groups or conditions
        number_of_per_subjects: Number of subjects per group
        sxa_output: Output sxa file. Default is h.beta

    Returns:
        Parameter dictionary.
    """
    params: Beta2sxa = {
        "@type": "freesurfer/beta2sxa",
        "beta_files": beta_files,
        "number_of_conditions": number_of_conditions,
        "number_of_per_subjects": number_of_per_subjects,
    }
    if sxa_output is not None:
        params["sxa_output"] = sxa_output
    return params

def beta2sxa_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Beta2sxa."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("beta_files", None) is None:
        raise StyxValidationError("`beta_files` must not be None")
    if not isinstance(params["beta_files"], list):
        raise StyxValidationError(f'`beta_files` has the wrong type: Received `{type(params["beta_files"])}` expected `list[InputPathType]`')
    for e in params["beta_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`beta_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("number_of_conditions", None) is None:
        raise StyxValidationError("`number_of_conditions` must not be None")
    if not isinstance(params["number_of_conditions"], (float, int)):
        raise StyxValidationError(f'`number_of_conditions` has the wrong type: Received `{type(params["number_of_conditions"])}` expected `float`')
    if params.get("number_of_per_subjects", None) is None:
        raise StyxValidationError("`number_of_per_subjects` must not be None")
    if not isinstance(params["number_of_per_subjects"], (float, int)):
        raise StyxValidationError(f'`number_of_per_subjects` has the wrong type: Received `{type(params["number_of_per_subjects"])}` expected `float`')
    if params.get("sxa_output", None) is not None:
        if not isinstance(params["sxa_output"], str):
            raise StyxValidationError(f'`sxa_output` has the wrong type: Received `{type(params["sxa_output"])}` expected `str`')

def beta2sxa_cargs(params: Beta2sxa, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("beta2sxa")
    cargs.append("--b")
    for item0 in params["beta_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--nc")
    cargs.append(str(params["number_of_conditions"]))
    cargs.append("--nper")
    cargs.append(str(params["number_of_per_subjects"]))
    v_0 = params.get("sxa_output")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    return cargs

def beta2sxa_outputs(params: Beta2sxa, execution: Execution) -> Beta2sxaOutputs:
    """Build the Beta2sxaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    sxa_output_file_v: OutputPathType = execution.output_file("h.[MASK].nii")
    return Beta2sxaOutputs(
        root=root_v,
        sxa_output_file=sxa_output_file_v,
    )

def beta2sxa_execute(params: Beta2sxa, runner: Runner | None = None) -> Beta2sxaOutputs:
    """
    beta2sxa

    A script to create files for plotting in tkmedit or tksurfer based on tabular data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    beta2sxa_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BETA2SXA_METADATA)
    execution.params(params)
    args = beta2sxa_cargs(params, execution)
    out = beta2sxa_outputs(params, execution)
    execution.run(args)
    return out

def beta2sxa(
    beta_files: list[InputPathType],
    number_of_conditions: float,
    number_of_per_subjects: float,
    sxa_output: str | None = None,
    runner: Runner | None = None,
) -> Beta2sxaOutputs:
    """
    beta2sxa

    A script to create files for plotting in tkmedit or tksurfer based on tabular data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        beta_files: Input beta files, e.g., data.nii
        number_of_conditions: Number of groups or conditions
        number_of_per_subjects: Number of subjects per group
        sxa_output: Output sxa file. Default is h.beta
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = beta2sxa_params(
        beta_files=beta_files,
        number_of_conditions=number_of_conditions,
        number_of_per_subjects=number_of_per_subjects,
        sxa_output=sxa_output,
    )
    return beta2sxa_execute(params, runner)

__all__ = [
    "Beta2sxa",
    "Beta2sxaOutputs",
    "BETA2SXA_METADATA",
    "beta2sxa_cargs",
    "beta2sxa_outputs",
    "beta2sxa_params",
    "beta2sxa_execute",
    "beta2sxa_validate",
    "beta2sxa",
]