# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_SYMMETRIC_METADATA = Metadata(
    id="make_symmetric",
    name="make_symmetric",
    package="freesurfer",
)

MakeSymmetric = typing.TypedDict(
    "MakeSymmetric",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_symmetric"]],
        "hemi": str,
        "input_file": InputPathType,
        "output_file": str,
        "transform_map": str,
    },
)

@dataclasses.dataclass
class MakeSymmetricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    processed_output: OutputPathType
    """Output image file after processing."""
    map_output: OutputPathType
    """Transformation map file mapping the input to the upright space."""

def make_symmetric_params(
    hemi: str,
    input_file: InputPathType,
    output_file: str,
    transform_map: str,
) -> MakeSymmetric:
    """
    Build parameters.

    Args:
        hemi: The hemisphere to mirror; accepted values are 'lh' for left\
            hemisphere or 'rh' for right hemisphere.
        input_file: Input image in .mgz format.
        output_file: Output image in .mgz format resulting from the symmetric\
            processing.
        transform_map: Transformation map in .lta format mapping the input to the\
            upright space.

    Returns:
        Parameter dictionary.
    """
    params: MakeSymmetric = {
        "@type": "freesurfer/make_symmetric",
        "hemi": hemi,
        "input_file": input_file,
        "output_file": output_file,
        "transform_map": transform_map,
    }
    return params

def make_symmetric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeSymmetric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("transform_map", None) is None:
        raise StyxValidationError("`transform_map` must not be None")
    if not isinstance(params["transform_map"], str):
        raise StyxValidationError(f'`transform_map` has the wrong type: Received `{type(params["transform_map"])}` expected `str`')

def make_symmetric_cargs(params: MakeSymmetric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_symmetric")
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    cargs.append(params["transform_map"])
    return cargs

def make_symmetric_outputs(params: MakeSymmetric, execution: Execution) -> MakeSymmetricOutputs:
    """Build the MakeSymmetricOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    processed_output_v: OutputPathType = execution.output_file(params["output_file"])
    map_output_v: OutputPathType = execution.output_file(params["transform_map"])
    return MakeSymmetricOutputs(
        root=root_v,
        processed_output=processed_output_v,
        map_output=map_output_v,
    )

def make_symmetric_execute(params: MakeSymmetric, runner: Runner | None = None) -> MakeSymmetricOutputs:
    """
    make_symmetric

    Registers an input image to its left/right reversed version using mri_robust_register in a half-way space and mirrors the selected hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_symmetric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_SYMMETRIC_METADATA)
    execution.params(params)
    args = make_symmetric_cargs(params, execution)
    out = make_symmetric_outputs(params, execution)
    execution.run(args)
    return out

def make_symmetric(
    hemi: str,
    input_file: InputPathType,
    output_file: str,
    transform_map: str,
    runner: Runner | None = None,
) -> MakeSymmetricOutputs:
    """
    make_symmetric

    Registers an input image to its left/right reversed version using mri_robust_register in a half-way space and mirrors the selected hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hemi: The hemisphere to mirror; accepted values are 'lh' for left\
            hemisphere or 'rh' for right hemisphere.
        input_file: Input image in .mgz format.
        output_file: Output image in .mgz format resulting from the symmetric\
            processing.
        transform_map: Transformation map in .lta format mapping the input to the\
            upright space.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_symmetric_params(
        hemi=hemi,
        input_file=input_file,
        output_file=output_file,
        transform_map=transform_map,
    )
    return make_symmetric_execute(params, runner)

__all__ = [
    "MakeSymmetric",
    "MakeSymmetricOutputs",
    "MAKE_SYMMETRIC_METADATA",
    "make_symmetric_cargs",
    "make_symmetric_outputs",
    "make_symmetric_params",
    "make_symmetric_execute",
    "make_symmetric_validate",
    "make_symmetric",
]