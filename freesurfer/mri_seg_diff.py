# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEG_DIFF_METADATA = Metadata(
    id="mri_seg_diff",
    name="mri_seg_diff",
    package="freesurfer",
)

MriSegDiff = typing.TypedDict(
    "MriSegDiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_seg_diff"]],
        "seg1": typing.NotRequired[InputPathType],
        "seg2": typing.NotRequired[InputPathType],
        "seg": typing.NotRequired[InputPathType],
        "diff": str,
        "diff_in": typing.NotRequired[InputPathType],
        "merged": typing.NotRequired[str],
        "diff_force": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSegDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    diff_output: OutputPathType
    """Output diff segmentation file."""
    merged_output: OutputPathType | None
    """Output merged segmentation file."""

def mri_seg_diff_params(
    diff: str,
    seg1: InputPathType | None = None,
    seg2: InputPathType | None = None,
    seg: InputPathType | None = None,
    diff_in: InputPathType | None = None,
    merged: str | None = None,
    diff_force: bool = False,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
) -> MriSegDiff:
    """
    Build parameters.

    Args:
        diff: Output diff segmentation volume.
        seg1: First segmentation file (e.g., unedited).
        seg2: Second segmentation file (e.g., edited).
        seg: Source segmentation file (e.g., unedited).
        diff_in: Input diff segmentation volume.
        merged: Merged output, combining unedited with diff.
        diff_force: Force creation of a diff even if no diff is detected.
        debug: Turn on debugging.
        checkopts: Check options and exit without running.
        version: Print out version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriSegDiff = {
        "@type": "freesurfer/mri_seg_diff",
        "diff": diff,
        "diff_force": diff_force,
        "debug": debug,
        "checkopts": checkopts,
        "version": version,
    }
    if seg1 is not None:
        params["seg1"] = seg1
    if seg2 is not None:
        params["seg2"] = seg2
    if seg is not None:
        params["seg"] = seg
    if diff_in is not None:
        params["diff_in"] = diff_in
    if merged is not None:
        params["merged"] = merged
    return params

def mri_seg_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("seg1", None) is not None:
        if not isinstance(params["seg1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg1` has the wrong type: Received `{type(params["seg1"])}` expected `InputPathType`')
    if params.get("seg2", None) is not None:
        if not isinstance(params["seg2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg2` has the wrong type: Received `{type(params["seg2"])}` expected `InputPathType`')
    if params.get("seg", None) is not None:
        if not isinstance(params["seg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg` has the wrong type: Received `{type(params["seg"])}` expected `InputPathType`')
    if params.get("diff", None) is None:
        raise StyxValidationError("`diff` must not be None")
    if not isinstance(params["diff"], str):
        raise StyxValidationError(f'`diff` has the wrong type: Received `{type(params["diff"])}` expected `str`')
    if params.get("diff_in", None) is not None:
        if not isinstance(params["diff_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`diff_in` has the wrong type: Received `{type(params["diff_in"])}` expected `InputPathType`')
    if params.get("merged", None) is not None:
        if not isinstance(params["merged"], str):
            raise StyxValidationError(f'`merged` has the wrong type: Received `{type(params["merged"])}` expected `str`')
    if params.get("diff_force", None) is not None:
        if not isinstance(params["diff_force"], bool):
            raise StyxValidationError(f'`diff_force` has the wrong type: Received `{type(params["diff_force"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_seg_diff_cargs(params: MriSegDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_seg_diff")
    v_0 = params.get("seg1")
    if v_0 is not None:
        cargs.append("--seg1")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("seg2")
    if v_1 is not None:
        cargs.append("--seg2")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("seg")
    if v_2 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_2))
    cargs.append("--diff")
    cargs.append(params["diff"])
    v_3 = params.get("diff_in")
    if v_3 is not None:
        cargs.append("--diff-in")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("merged")
    if v_4 is not None:
        cargs.append("--merged")
        cargs.append(v_4)
    if params.get("diff_force"):
        cargs.append("--diff-force")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_seg_diff_outputs(params: MriSegDiff, execution: Execution) -> MriSegDiffOutputs:
    """Build the MriSegDiffOutputs object for this tool."""
    merged_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    diff_output_v: OutputPathType = execution.output_file(params["diff"])
    __v0 = params.get("merged")
    if __v0 is not None:
        merged_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MriSegDiffOutputs(
        root=root_v,
        diff_output=diff_output_v,
        merged_output=merged_output_v,
    )

def mri_seg_diff_execute(params: MriSegDiff, runner: Runner | None = None) -> MriSegDiffOutputs:
    """
    mri_seg_diff

    This program computes and merges differences in segmentation volumes, primarily for managing manual edits in FreeSurfer's aseg.mgz.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_seg_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEG_DIFF_METADATA)
    execution.params(params)
    args = mri_seg_diff_cargs(params, execution)
    out = mri_seg_diff_outputs(params, execution)
    execution.run(args)
    return out

def mri_seg_diff(
    diff: str,
    seg1: InputPathType | None = None,
    seg2: InputPathType | None = None,
    seg: InputPathType | None = None,
    diff_in: InputPathType | None = None,
    merged: str | None = None,
    diff_force: bool = False,
    debug: bool = False,
    checkopts: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriSegDiffOutputs:
    """
    mri_seg_diff

    This program computes and merges differences in segmentation volumes, primarily for managing manual edits in FreeSurfer's aseg.mgz.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        diff: Output diff segmentation volume.
        seg1: First segmentation file (e.g., unedited).
        seg2: Second segmentation file (e.g., edited).
        seg: Source segmentation file (e.g., unedited).
        diff_in: Input diff segmentation volume.
        merged: Merged output, combining unedited with diff.
        diff_force: Force creation of a diff even if no diff is detected.
        debug: Turn on debugging.
        checkopts: Check options and exit without running.
        version: Print out version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_seg_diff_params(
        diff=diff,
        seg1=seg1,
        seg2=seg2,
        seg=seg,
        diff_in=diff_in,
        merged=merged,
        diff_force=diff_force,
        debug=debug,
        checkopts=checkopts,
        version=version,
    )
    return mri_seg_diff_execute(params, runner)

__all__ = [
    "MriSegDiff",
    "MriSegDiffOutputs",
    "MRI_SEG_DIFF_METADATA",
    "mri_seg_diff_cargs",
    "mri_seg_diff_outputs",
    "mri_seg_diff_params",
    "mri_seg_diff_execute",
    "mri_seg_diff_validate",
    "mri_seg_diff",
]