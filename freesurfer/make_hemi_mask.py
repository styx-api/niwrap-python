# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_HEMI_MASK_METADATA = Metadata(
    id="make_hemi_mask",
    name="make_hemi_mask",
    package="freesurfer",
)

MakeHemiMask = typing.TypedDict(
    "MakeHemiMask",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_hemi_mask"]],
        "hemi": str,
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MakeHemiMaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Hemisphere masked MRI volume output"""

def make_hemi_mask_params(
    hemi: str,
    input_file: InputPathType,
    output_file: str,
) -> MakeHemiMask:
    """
    Build parameters.

    Args:
        hemi: Hemisphere to keep ('lh' for left hemisphere, 'rh' for right\
            hemisphere)
        input_file: Input MRI volume file (e.g. input.mgz)
        output_file: Output masked MRI volume file (e.g. output.mgz)

    Returns:
        Parameter dictionary.
    """
    params: MakeHemiMask = {
        "@type": "freesurfer/make_hemi_mask",
        "hemi": hemi,
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def make_hemi_mask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeHemiMask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def make_hemi_mask_cargs(params: MakeHemiMask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_hemi_mask")
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def make_hemi_mask_outputs(params: MakeHemiMask, execution: Execution) -> MakeHemiMaskOutputs:
    """Build the MakeHemiMaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["output_file"])
    return MakeHemiMaskOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def make_hemi_mask_execute(params: MakeHemiMask, runner: Runner | None = None) -> MakeHemiMaskOutputs:
    """
    make_hemi_mask

    Generates a hemisphere mask by registering input to the left/right reversed version using mri_robust_register, then keeps only the selected hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_hemi_mask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_HEMI_MASK_METADATA)
    execution.params(params)
    args = make_hemi_mask_cargs(params, execution)
    out = make_hemi_mask_outputs(params, execution)
    execution.run(args)
    return out

def make_hemi_mask(
    hemi: str,
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> MakeHemiMaskOutputs:
    """
    make_hemi_mask

    Generates a hemisphere mask by registering input to the left/right reversed version using mri_robust_register, then keeps only the selected hemisphere.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hemi: Hemisphere to keep ('lh' for left hemisphere, 'rh' for right\
            hemisphere)
        input_file: Input MRI volume file (e.g. input.mgz)
        output_file: Output masked MRI volume file (e.g. output.mgz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_hemi_mask_params(
        hemi=hemi,
        input_file=input_file,
        output_file=output_file,
    )
    return make_hemi_mask_execute(params, runner)

__all__ = [
    "MakeHemiMask",
    "MakeHemiMaskOutputs",
    "MAKE_HEMI_MASK_METADATA",
    "make_hemi_mask_cargs",
    "make_hemi_mask_outputs",
    "make_hemi_mask_params",
    "make_hemi_mask_execute",
    "make_hemi_mask_validate",
    "make_hemi_mask",
]