# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_VOLUME_METADATA = Metadata(
    id="mris_volume",
    name="mris_volume",
    package="freesurfer",
)

MrisVolume = typing.TypedDict(
    "MrisVolume",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_volume"]],
        "surface_file": InputPathType,
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_volume_params(
    surface_file: InputPathType,
    verbose_flag: bool = False,
) -> MrisVolume:
    """
    Build parameters.

    Args:
        surface_file: The closed surface file whose volume is to be computed.
        verbose_flag: Output more messages for verbose output.

    Returns:
        Parameter dictionary.
    """
    params: MrisVolume = {
        "@type": "freesurfer/mris_volume",
        "surface_file": surface_file,
        "verbose_flag": verbose_flag,
    }
    return params

def mris_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_file", None) is None:
        raise StyxValidationError("`surface_file` must not be None")
    if not isinstance(params["surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_file` has the wrong type: Received `{type(params["surface_file"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def mris_volume_cargs(params: MrisVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_volume")
    cargs.append(execution.input_file(params["surface_file"]))
    if params.get("verbose_flag"):
        cargs.append("-v")
    return cargs

def mris_volume_outputs(params: MrisVolume, execution: Execution) -> MrisVolumeOutputs:
    """Build the MrisVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisVolumeOutputs(
        root=root_v,
    )

def mris_volume_execute(params: MrisVolume, runner: Runner | None = None) -> MrisVolumeOutputs:
    """
    mris_volume

    A tool for computing the volume of a closed surface using a divergence formula.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_VOLUME_METADATA)
    execution.params(params)
    args = mris_volume_cargs(params, execution)
    out = mris_volume_outputs(params, execution)
    execution.run(args)
    return out

def mris_volume(
    surface_file: InputPathType,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> MrisVolumeOutputs:
    """
    mris_volume

    A tool for computing the volume of a closed surface using a divergence formula.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface_file: The closed surface file whose volume is to be computed.
        verbose_flag: Output more messages for verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_volume_params(
        surface_file=surface_file,
        verbose_flag=verbose_flag,
    )
    return mris_volume_execute(params, runner)

__all__ = [
    "MrisVolume",
    "MrisVolumeOutputs",
    "MRIS_VOLUME_METADATA",
    "mris_volume_cargs",
    "mris_volume_outputs",
    "mris_volume_params",
    "mris_volume_execute",
    "mris_volume_validate",
    "mris_volume",
]