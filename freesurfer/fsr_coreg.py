# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSR_COREG_METADATA = Metadata(
    id="fsr-coreg",
    name="fsr-coreg",
    package="freesurfer",
)

FsrCoreg = typing.TypedDict(
    "FsrCoreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsr-coreg"]],
        "import_dir": str,
        "reference_mode": str,
        "num_threads": typing.NotRequired[float],
        "force_update": typing.NotRequired[bool],
        "output_dir": typing.NotRequired[str],
        "expert_options": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class FsrCoregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    aligned_volume: OutputPathType | None
    """Volume produced for each mode in alignment with the reference mode"""
    registration_transform: OutputPathType | None
    """Registration transform file for mode to reference mode"""

def fsr_coreg_params(
    import_dir: str,
    reference_mode: str,
    num_threads: float | None = None,
    force_update: bool = False,
    output_dir: str | None = None,
    expert_options: InputPathType | None = None,
) -> FsrCoreg:
    """
    Build parameters.

    Args:
        import_dir: Data directory created by fsr-import
        reference_mode: Mode to use as a reference (all modes register to this\
            mode)
        num_threads: Number of threads to use
        force_update: Force update of files regardless of time stamp
        output_dir: Set the output directory, default is importdir
        expert_options: Expert options file

    Returns:
        Parameter dictionary.
    """
    params: FsrCoreg = {
        "@type": "freesurfer/fsr-coreg",
        "import_dir": import_dir,
        "reference_mode": reference_mode,
        "force_update": force_update,
    }
    if num_threads is not None:
        params["num_threads"] = num_threads
    if output_dir is not None:
        params["output_dir"] = output_dir
    if expert_options is not None:
        params["expert_options"] = expert_options
    return params

def fsr_coreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsrCoreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("import_dir", None) is None:
        raise StyxValidationError("`import_dir` must not be None")
    if not isinstance(params["import_dir"], str):
        raise StyxValidationError(f'`import_dir` has the wrong type: Received `{type(params["import_dir"])}` expected `str`')
    if params.get("reference_mode", None) is None:
        raise StyxValidationError("`reference_mode` must not be None")
    if not isinstance(params["reference_mode"], str):
        raise StyxValidationError(f'`reference_mode` has the wrong type: Received `{type(params["reference_mode"])}` expected `str`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], (float, int)):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `float`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("expert_options", None) is not None:
        if not isinstance(params["expert_options"], (pathlib.Path, str)):
            raise StyxValidationError(f'`expert_options` has the wrong type: Received `{type(params["expert_options"])}` expected `InputPathType`')

def fsr_coreg_cargs(params: FsrCoreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsr-coreg")
    cargs.append("--i")
    cargs.append(params["import_dir"])
    cargs.append("--ref")
    cargs.append(params["reference_mode"])
    v_0 = params.get("num_threads")
    if v_0 is not None:
        cargs.append("--threads")
        cargs.append(str(v_0))
    if params.get("force_update"):
        cargs.append("--force-update")
    v_1 = params.get("output_dir")
    if v_1 is not None:
        cargs.append("--o")
        cargs.append(v_1)
    v_2 = params.get("expert_options")
    if v_2 is not None:
        cargs.append("--expert")
        cargs.append(execution.input_file(v_2))
    return cargs

def fsr_coreg_outputs(params: FsrCoreg, execution: Execution) -> FsrCoregOutputs:
    """Build the FsrCoregOutputs object for this tool."""
    aligned_volume_v: OutputPathType | None = None
    registration_transform_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        aligned_volume_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/mode.mgz')
    __v1 = params.get("output_dir")
    if __v1 is not None:
        registration_transform_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/mode.reg-to-ref.lta')
    return FsrCoregOutputs(
        root=root_v,
        aligned_volume=aligned_volume_v,
        registration_transform=registration_transform_v,
    )

def fsr_coreg_execute(params: FsrCoreg, runner: Runner | None = None) -> FsrCoregOutputs:
    """
    fsr-coreg

    Co-registers input data in preparation for FreeSurfer analysis

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsr_coreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSR_COREG_METADATA)
    execution.params(params)
    args = fsr_coreg_cargs(params, execution)
    out = fsr_coreg_outputs(params, execution)
    execution.run(args)
    return out

def fsr_coreg(
    import_dir: str,
    reference_mode: str,
    num_threads: float | None = None,
    force_update: bool = False,
    output_dir: str | None = None,
    expert_options: InputPathType | None = None,
    runner: Runner | None = None,
) -> FsrCoregOutputs:
    """
    fsr-coreg

    Co-registers input data in preparation for FreeSurfer analysis

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        import_dir: Data directory created by fsr-import
        reference_mode: Mode to use as a reference (all modes register to this\
            mode)
        num_threads: Number of threads to use
        force_update: Force update of files regardless of time stamp
        output_dir: Set the output directory, default is importdir
        expert_options: Expert options file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsr_coreg_params(
        import_dir=import_dir,
        reference_mode=reference_mode,
        num_threads=num_threads,
        force_update=force_update,
        output_dir=output_dir,
        expert_options=expert_options,
    )
    return fsr_coreg_execute(params, runner)

__all__ = [
    "FsrCoreg",
    "FsrCoregOutputs",
    "FSR_COREG_METADATA",
    "fsr_coreg_cargs",
    "fsr_coreg_outputs",
    "fsr_coreg_params",
    "fsr_coreg_execute",
    "fsr_coreg_validate",
    "fsr_coreg",
]