# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MODIFY_METADATA = Metadata(
    id="mri_modify",
    name="mri_modify",
    package="freesurfer",
)

MriModify = typing.TypedDict(
    "MriModify",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_modify"]],
        "x_ras": list[float],
        "y_ras": list[float],
        "z_ras": list[float],
        "cras": list[float],
        "x_size": float,
        "y_size": float,
        "z_size": float,
        "tr": float,
        "te": float,
        "ti": float,
        "fa": float,
        "xform": str,
        "input_volume": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriModifyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_modify_params(
    x_ras: list[float],
    y_ras: list[float],
    z_ras: list[float],
    cras: list[float],
    x_size: float,
    y_size: float,
    z_size: float,
    tr: float,
    te: float,
    ti: float,
    fa: float,
    xform: str,
    input_volume: InputPathType,
    output_volume: str,
) -> MriModify:
    """
    Build parameters.

    Args:
        x_ras: X direction cosines (xr, xa, xs)
        y_ras: Y direction cosines (yr, ya, ys)
        z_ras: Z direction cosines (zr, za, zs)
        cras: Center RAS coordinates (cr, ca, cs)
        x_size: Size of voxel in X dimension
        y_size: Size of voxel in Y dimension
        z_size: Size of voxel in Z dimension
        tr: Repetition time (TR)
        te: Echo time (TE)
        ti: Inversion time (TI)
        fa: Flip angle (degrees)
        xform: New transformation file name
        input_volume: Input volume
        output_volume: Output volume

    Returns:
        Parameter dictionary.
    """
    params: MriModify = {
        "@type": "freesurfer/mri_modify",
        "x_ras": x_ras,
        "y_ras": y_ras,
        "z_ras": z_ras,
        "cras": cras,
        "x_size": x_size,
        "y_size": y_size,
        "z_size": z_size,
        "tr": tr,
        "te": te,
        "ti": ti,
        "fa": fa,
        "xform": xform,
        "input_volume": input_volume,
        "output_volume": output_volume,
    }
    return params

def mri_modify_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriModify."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("x_ras", None) is None:
        raise StyxValidationError("`x_ras` must not be None")
    if not isinstance(params["x_ras"], list):
        raise StyxValidationError(f'`x_ras` has the wrong type: Received `{type(params["x_ras"])}` expected `list[float]`')
    for e in params["x_ras"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`x_ras` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("y_ras", None) is None:
        raise StyxValidationError("`y_ras` must not be None")
    if not isinstance(params["y_ras"], list):
        raise StyxValidationError(f'`y_ras` has the wrong type: Received `{type(params["y_ras"])}` expected `list[float]`')
    for e_2 in params["y_ras"]:
        if not isinstance(e_2, (float, int)):
            raise StyxValidationError(f'`y_ras` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("z_ras", None) is None:
        raise StyxValidationError("`z_ras` must not be None")
    if not isinstance(params["z_ras"], list):
        raise StyxValidationError(f'`z_ras` has the wrong type: Received `{type(params["z_ras"])}` expected `list[float]`')
    for e_3 in params["z_ras"]:
        if not isinstance(e_3, (float, int)):
            raise StyxValidationError(f'`z_ras` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("cras", None) is None:
        raise StyxValidationError("`cras` must not be None")
    if not isinstance(params["cras"], list):
        raise StyxValidationError(f'`cras` has the wrong type: Received `{type(params["cras"])}` expected `list[float]`')
    for e_4 in params["cras"]:
        if not isinstance(e_4, (float, int)):
            raise StyxValidationError(f'`cras` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
    if params.get("x_size", None) is None:
        raise StyxValidationError("`x_size` must not be None")
    if not isinstance(params["x_size"], (float, int)):
        raise StyxValidationError(f'`x_size` has the wrong type: Received `{type(params["x_size"])}` expected `float`')
    if params.get("y_size", None) is None:
        raise StyxValidationError("`y_size` must not be None")
    if not isinstance(params["y_size"], (float, int)):
        raise StyxValidationError(f'`y_size` has the wrong type: Received `{type(params["y_size"])}` expected `float`')
    if params.get("z_size", None) is None:
        raise StyxValidationError("`z_size` must not be None")
    if not isinstance(params["z_size"], (float, int)):
        raise StyxValidationError(f'`z_size` has the wrong type: Received `{type(params["z_size"])}` expected `float`')
    if params.get("tr", None) is None:
        raise StyxValidationError("`tr` must not be None")
    if not isinstance(params["tr"], (float, int)):
        raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("te", None) is None:
        raise StyxValidationError("`te` must not be None")
    if not isinstance(params["te"], (float, int)):
        raise StyxValidationError(f'`te` has the wrong type: Received `{type(params["te"])}` expected `float`')
    if params.get("ti", None) is None:
        raise StyxValidationError("`ti` must not be None")
    if not isinstance(params["ti"], (float, int)):
        raise StyxValidationError(f'`ti` has the wrong type: Received `{type(params["ti"])}` expected `float`')
    if params.get("fa", None) is None:
        raise StyxValidationError("`fa` must not be None")
    if not isinstance(params["fa"], (float, int)):
        raise StyxValidationError(f'`fa` has the wrong type: Received `{type(params["fa"])}` expected `float`')
    if params.get("xform", None) is None:
        raise StyxValidationError("`xform` must not be None")
    if not isinstance(params["xform"], str):
        raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `str`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_modify_cargs(params: MriModify, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_modify")
    cargs.append("-xras")
    for item0 in params["x_ras"]:
        cargs.append(str(item0))
    cargs.append("-yras")
    for item1 in params["y_ras"]:
        cargs.append(str(item1))
    cargs.append("-zras")
    for item2 in params["z_ras"]:
        cargs.append(str(item2))
    cargs.append("-cras")
    for item3 in params["cras"]:
        cargs.append(str(item3))
    cargs.append("-xsize")
    cargs.append(str(params["x_size"]))
    cargs.append("-ysize")
    cargs.append(str(params["y_size"]))
    cargs.append("-zsize")
    cargs.append(str(params["z_size"]))
    cargs.append("-tr")
    cargs.append(str(params["tr"]))
    cargs.append("-te")
    cargs.append(str(params["te"]))
    cargs.append("-ti")
    cargs.append(str(params["ti"]))
    cargs.append("-fa")
    cargs.append(str(params["fa"]))
    cargs.append("-xform")
    cargs.append(params["xform"])
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_modify_outputs(params: MriModify, execution: Execution) -> MriModifyOutputs:
    """Build the MriModifyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriModifyOutputs(
        root=root_v,
    )

def mri_modify_execute(params: MriModify, runner: Runner | None = None) -> MriModifyOutputs:
    """
    mri_modify

    Tool for modifying MRI image headers

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_modify_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MODIFY_METADATA)
    execution.params(params)
    args = mri_modify_cargs(params, execution)
    out = mri_modify_outputs(params, execution)
    execution.run(args)
    return out

def mri_modify(
    x_ras: list[float],
    y_ras: list[float],
    z_ras: list[float],
    cras: list[float],
    x_size: float,
    y_size: float,
    z_size: float,
    tr: float,
    te: float,
    ti: float,
    fa: float,
    xform: str,
    input_volume: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriModifyOutputs:
    """
    mri_modify

    Tool for modifying MRI image headers

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        x_ras: X direction cosines (xr, xa, xs)
        y_ras: Y direction cosines (yr, ya, ys)
        z_ras: Z direction cosines (zr, za, zs)
        cras: Center RAS coordinates (cr, ca, cs)
        x_size: Size of voxel in X dimension
        y_size: Size of voxel in Y dimension
        z_size: Size of voxel in Z dimension
        tr: Repetition time (TR)
        te: Echo time (TE)
        ti: Inversion time (TI)
        fa: Flip angle (degrees)
        xform: New transformation file name
        input_volume: Input volume
        output_volume: Output volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_modify_params(
        x_ras=x_ras,
        y_ras=y_ras,
        z_ras=z_ras,
        cras=cras,
        x_size=x_size,
        y_size=y_size,
        z_size=z_size,
        tr=tr,
        te=te,
        ti=ti,
        fa=fa,
        xform=xform,
        input_volume=input_volume,
        output_volume=output_volume,
    )
    return mri_modify_execute(params, runner)

__all__ = [
    "MriModify",
    "MriModifyOutputs",
    "MRI_MODIFY_METADATA",
    "mri_modify_cargs",
    "mri_modify_outputs",
    "mri_modify_params",
    "mri_modify_execute",
    "mri_modify_validate",
    "mri_modify",
]