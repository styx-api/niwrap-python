# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEGCENTROIDS_METADATA = Metadata(
    id="mri_segcentroids",
    name="mri_segcentroids",
    package="freesurfer",
)

MriSegcentroids = typing.TypedDict(
    "MriSegcentroids",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_segcentroids"]],
        "input_segmentation": InputPathType,
        "output_file": str,
        "pointset_flag": typing.NotRequired[bool],
        "registration_file": typing.NotRequired[InputPathType],
        "weights_file": typing.NotRequired[InputPathType],
        "lut_file": typing.NotRequired[InputPathType],
        "default_lut_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSegcentroidsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_centroids: OutputPathType
    """Output text file containing the centroids."""

def mri_segcentroids_params(
    input_segmentation: InputPathType,
    output_file: str,
    pointset_flag: bool = False,
    registration_file: InputPathType | None = None,
    weights_file: InputPathType | None = None,
    lut_file: InputPathType | None = None,
    default_lut_flag: bool = False,
) -> MriSegcentroids:
    """
    Build parameters.

    Args:
        input_segmentation: Input segmentation volume file.
        output_file: Output text file for centroids.
        pointset_flag: Save centroids as a Freeview pointset (json).
        registration_file: Apply a linear registration (lta).
        weights_file: Compute weighted centroids with provided voxel weights.
        lut_file: Specify label lookup table.
        default_lut_flag: Use default FreeSurferColorLUT.txt for lookup table.

    Returns:
        Parameter dictionary.
    """
    params: MriSegcentroids = {
        "@type": "freesurfer/mri_segcentroids",
        "input_segmentation": input_segmentation,
        "output_file": output_file,
        "pointset_flag": pointset_flag,
        "default_lut_flag": default_lut_flag,
    }
    if registration_file is not None:
        params["registration_file"] = registration_file
    if weights_file is not None:
        params["weights_file"] = weights_file
    if lut_file is not None:
        params["lut_file"] = lut_file
    return params

def mri_segcentroids_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegcentroids."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_segmentation", None) is None:
        raise StyxValidationError("`input_segmentation` must not be None")
    if not isinstance(params["input_segmentation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_segmentation` has the wrong type: Received `{type(params["input_segmentation"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("pointset_flag", None) is not None:
        if not isinstance(params["pointset_flag"], bool):
            raise StyxValidationError(f'`pointset_flag` has the wrong type: Received `{type(params["pointset_flag"])}` expected `bool`')
    if params.get("registration_file", None) is not None:
        if not isinstance(params["registration_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("weights_file", None) is not None:
        if not isinstance(params["weights_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weights_file` has the wrong type: Received `{type(params["weights_file"])}` expected `InputPathType`')
    if params.get("lut_file", None) is not None:
        if not isinstance(params["lut_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lut_file` has the wrong type: Received `{type(params["lut_file"])}` expected `InputPathType`')
    if params.get("default_lut_flag", None) is not None:
        if not isinstance(params["default_lut_flag"], bool):
            raise StyxValidationError(f'`default_lut_flag` has the wrong type: Received `{type(params["default_lut_flag"])}` expected `bool`')

def mri_segcentroids_cargs(params: MriSegcentroids, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_segcentroids")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_segmentation"]))
    cargs.append("--o")
    cargs.append(params["output_file"])
    if params.get("pointset_flag"):
        cargs.append("--p")
    v_0 = params.get("registration_file")
    if v_0 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("weights_file")
    if v_1 is not None:
        cargs.append("--weights")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("lut_file")
    if v_2 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_2))
    if params.get("default_lut_flag"):
        cargs.append("--ctab-default")
    return cargs

def mri_segcentroids_outputs(params: MriSegcentroids, execution: Execution) -> MriSegcentroidsOutputs:
    """Build the MriSegcentroidsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_centroids_v: OutputPathType = execution.output_file(params["output_file"])
    return MriSegcentroidsOutputs(
        root=root_v,
        output_centroids=output_centroids_v,
    )

def mri_segcentroids_execute(params: MriSegcentroids, runner: Runner | None = None) -> MriSegcentroidsOutputs:
    """
    mri_segcentroids

    Computes the center of mass for individual structures in a segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_segcentroids_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEGCENTROIDS_METADATA)
    execution.params(params)
    args = mri_segcentroids_cargs(params, execution)
    out = mri_segcentroids_outputs(params, execution)
    execution.run(args)
    return out

def mri_segcentroids(
    input_segmentation: InputPathType,
    output_file: str,
    pointset_flag: bool = False,
    registration_file: InputPathType | None = None,
    weights_file: InputPathType | None = None,
    lut_file: InputPathType | None = None,
    default_lut_flag: bool = False,
    runner: Runner | None = None,
) -> MriSegcentroidsOutputs:
    """
    mri_segcentroids

    Computes the center of mass for individual structures in a segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_segmentation: Input segmentation volume file.
        output_file: Output text file for centroids.
        pointset_flag: Save centroids as a Freeview pointset (json).
        registration_file: Apply a linear registration (lta).
        weights_file: Compute weighted centroids with provided voxel weights.
        lut_file: Specify label lookup table.
        default_lut_flag: Use default FreeSurferColorLUT.txt for lookup table.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_segcentroids_params(
        input_segmentation=input_segmentation,
        output_file=output_file,
        pointset_flag=pointset_flag,
        registration_file=registration_file,
        weights_file=weights_file,
        lut_file=lut_file,
        default_lut_flag=default_lut_flag,
    )
    return mri_segcentroids_execute(params, runner)

__all__ = [
    "MriSegcentroids",
    "MriSegcentroidsOutputs",
    "MRI_SEGCENTROIDS_METADATA",
    "mri_segcentroids_cargs",
    "mri_segcentroids_outputs",
    "mri_segcentroids_params",
    "mri_segcentroids_execute",
    "mri_segcentroids_validate",
    "mri_segcentroids",
]