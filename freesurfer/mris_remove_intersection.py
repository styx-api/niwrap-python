# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_REMOVE_INTERSECTION_METADATA = Metadata(
    id="mris_remove_intersection",
    name="mris_remove_intersection",
    package="freesurfer",
)

MrisRemoveIntersection = typing.TypedDict(
    "MrisRemoveIntersection",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_remove_intersection"]],
        "surface_in_file": InputPathType,
        "corrected_surface_out_file": str,
        "fill_holes": typing.NotRequired[bool],
        "map_option": typing.NotRequired[InputPathType],
        "projdistmm": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisRemoveIntersectionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_corrected_surface: OutputPathType
    """Corrected surface output file."""
    out_map_file: OutputPathType | None
    """Binary map of intersections."""

def mris_remove_intersection_params(
    surface_in_file: InputPathType,
    corrected_surface_out_file: str,
    fill_holes: bool = False,
    map_option: InputPathType | None = None,
    projdistmm: float | None = None,
) -> MrisRemoveIntersection:
    """
    Build parameters.

    Args:
        surface_in_file: Input surface file.
        corrected_surface_out_file: Corrected output surface file.
        fill_holes: Fill any holes in the intersection mark map and include them in\
            the fix.
        map_option: Create a binary map of intersections.
        projdistmm: Projection distance in mm when using -map option.

    Returns:
        Parameter dictionary.
    """
    params: MrisRemoveIntersection = {
        "@type": "freesurfer/mris_remove_intersection",
        "surface_in_file": surface_in_file,
        "corrected_surface_out_file": corrected_surface_out_file,
        "fill_holes": fill_holes,
    }
    if map_option is not None:
        params["map_option"] = map_option
    if projdistmm is not None:
        params["projdistmm"] = projdistmm
    return params

def mris_remove_intersection_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRemoveIntersection."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_in_file", None) is None:
        raise StyxValidationError("`surface_in_file` must not be None")
    if not isinstance(params["surface_in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in_file` has the wrong type: Received `{type(params["surface_in_file"])}` expected `InputPathType`')
    if params.get("corrected_surface_out_file", None) is None:
        raise StyxValidationError("`corrected_surface_out_file` must not be None")
    if not isinstance(params["corrected_surface_out_file"], str):
        raise StyxValidationError(f'`corrected_surface_out_file` has the wrong type: Received `{type(params["corrected_surface_out_file"])}` expected `str`')
    if params.get("fill_holes", None) is not None:
        if not isinstance(params["fill_holes"], bool):
            raise StyxValidationError(f'`fill_holes` has the wrong type: Received `{type(params["fill_holes"])}` expected `bool`')
    if params.get("map_option", None) is not None:
        if not isinstance(params["map_option"], (pathlib.Path, str)):
            raise StyxValidationError(f'`map_option` has the wrong type: Received `{type(params["map_option"])}` expected `InputPathType`')
    if params.get("projdistmm", None) is not None:
        if not isinstance(params["projdistmm"], (float, int)):
            raise StyxValidationError(f'`projdistmm` has the wrong type: Received `{type(params["projdistmm"])}` expected `float`')

def mris_remove_intersection_cargs(params: MrisRemoveIntersection, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_remove_intersection")
    cargs.append(execution.input_file(params["surface_in_file"]))
    cargs.append(params["corrected_surface_out_file"])
    if params.get("fill_holes"):
        cargs.append("-fill-holes")
    v_0 = params.get("map_option")
    if v_0 is not None:
        cargs.append("-map")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("projdistmm")
    if v_1 is not None:
        cargs.append(str(v_1))
    return cargs

def mris_remove_intersection_outputs(params: MrisRemoveIntersection, execution: Execution) -> MrisRemoveIntersectionOutputs:
    """Build the MrisRemoveIntersectionOutputs object for this tool."""
    out_map_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_corrected_surface_v: OutputPathType = execution.output_file(params["corrected_surface_out_file"])
    __v0 = params.get("map_option")
    if __v0 is not None:
        out_map_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrisRemoveIntersectionOutputs(
        root=root_v,
        out_corrected_surface=out_corrected_surface_v,
        out_map_file=out_map_file_v,
    )

def mris_remove_intersection_execute(params: MrisRemoveIntersection, runner: Runner | None = None) -> MrisRemoveIntersectionOutputs:
    """
    mris_remove_intersection

    Tool to remove intersections in surface files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_remove_intersection_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_REMOVE_INTERSECTION_METADATA)
    execution.params(params)
    args = mris_remove_intersection_cargs(params, execution)
    out = mris_remove_intersection_outputs(params, execution)
    execution.run(args)
    return out

def mris_remove_intersection(
    surface_in_file: InputPathType,
    corrected_surface_out_file: str,
    fill_holes: bool = False,
    map_option: InputPathType | None = None,
    projdistmm: float | None = None,
    runner: Runner | None = None,
) -> MrisRemoveIntersectionOutputs:
    """
    mris_remove_intersection

    Tool to remove intersections in surface files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface_in_file: Input surface file.
        corrected_surface_out_file: Corrected output surface file.
        fill_holes: Fill any holes in the intersection mark map and include them in\
            the fix.
        map_option: Create a binary map of intersections.
        projdistmm: Projection distance in mm when using -map option.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_remove_intersection_params(
        surface_in_file=surface_in_file,
        corrected_surface_out_file=corrected_surface_out_file,
        fill_holes=fill_holes,
        map_option=map_option,
        projdistmm=projdistmm,
    )
    return mris_remove_intersection_execute(params, runner)

__all__ = [
    "MrisRemoveIntersection",
    "MrisRemoveIntersectionOutputs",
    "MRIS_REMOVE_INTERSECTION_METADATA",
    "mris_remove_intersection_cargs",
    "mris_remove_intersection_outputs",
    "mris_remove_intersection_params",
    "mris_remove_intersection_execute",
    "mris_remove_intersection_validate",
    "mris_remove_intersection",
]