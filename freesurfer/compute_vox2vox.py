# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COMPUTE_VOX2VOX_METADATA = Metadata(
    id="compute_vox2vox",
    name="compute_vox2vox",
    package="freesurfer",
)

ComputeVox2vox = typing.TypedDict(
    "ComputeVox2vox",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/compute_vox2vox"]],
        "source": InputPathType,
        "t4file": InputPathType,
        "target": InputPathType,
    },
)

@dataclasses.dataclass
class ComputeVox2voxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def compute_vox2vox_params(
    source: InputPathType,
    t4file: InputPathType,
    target: InputPathType,
) -> ComputeVox2vox:
    """
    Build parameters.

    Args:
        source: Input 4dfp source file.
        t4file: T4 transformation matrix file.
        target: Input 4dfp target file.

    Returns:
        Parameter dictionary.
    """
    params: ComputeVox2vox = {
        "@type": "freesurfer/compute_vox2vox",
        "source": source,
        "t4file": t4file,
        "target": target,
    }
    return params

def compute_vox2vox_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ComputeVox2vox."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("t4file", None) is None:
        raise StyxValidationError("`t4file` must not be None")
    if not isinstance(params["t4file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`t4file` has the wrong type: Received `{type(params["t4file"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')

def compute_vox2vox_cargs(params: ComputeVox2vox, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("compute_vox2vox")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["t4file"]))
    cargs.append(execution.input_file(params["target"]))
    return cargs

def compute_vox2vox_outputs(params: ComputeVox2vox, execution: Execution) -> ComputeVox2voxOutputs:
    """Build the ComputeVox2voxOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ComputeVox2voxOutputs(
        root=root_v,
    )

def compute_vox2vox_execute(params: ComputeVox2vox, runner: Runner | None = None) -> ComputeVox2voxOutputs:
    """
    compute_vox2vox

    Tool for computing voxel-to-voxel transformations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    compute_vox2vox_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COMPUTE_VOX2VOX_METADATA)
    execution.params(params)
    args = compute_vox2vox_cargs(params, execution)
    out = compute_vox2vox_outputs(params, execution)
    execution.run(args)
    return out

def compute_vox2vox(
    source: InputPathType,
    t4file: InputPathType,
    target: InputPathType,
    runner: Runner | None = None,
) -> ComputeVox2voxOutputs:
    """
    compute_vox2vox

    Tool for computing voxel-to-voxel transformations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source: Input 4dfp source file.
        t4file: T4 transformation matrix file.
        target: Input 4dfp target file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = compute_vox2vox_params(
        source=source,
        t4file=t4file,
        target=target,
    )
    return compute_vox2vox_execute(params, runner)

__all__ = [
    "ComputeVox2vox",
    "ComputeVox2voxOutputs",
    "COMPUTE_VOX2VOX_METADATA",
    "compute_vox2vox_cargs",
    "compute_vox2vox_outputs",
    "compute_vox2vox_params",
    "compute_vox2vox_execute",
    "compute_vox2vox_validate",
    "compute_vox2vox",
]