# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEGHEAD_METADATA = Metadata(
    id="mri_seghead",
    name="mri_seghead",
    package="freesurfer",
)

MriSeghead = typing.TypedDict(
    "MriSeghead",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_seghead"]],
        "input_volume": str,
        "output_volume": str,
        "fill_value": typing.NotRequired[float],
        "fhi_value": typing.NotRequired[float],
        "thresh1_value": typing.NotRequired[float],
        "thresh2_value": typing.NotRequired[float],
        "threshold": typing.NotRequired[float],
        "nhitsmin_value": typing.NotRequired[float],
        "hvoldat_file": typing.NotRequired[InputPathType],
        "signal_behind_head": typing.NotRequired[bool],
        "rescale": typing.NotRequired[bool],
        "fill_holes_islands": typing.NotRequired[bool],
        "seed_point": typing.NotRequired[list[float]],
        "or_mask_file": typing.NotRequired[InputPathType],
        "gdiag_option": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriSegheadOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_seghead_params(
    input_volume: str,
    output_volume: str,
    fill_value: float | None = None,
    fhi_value: float | None = None,
    thresh1_value: float | None = None,
    thresh2_value: float | None = None,
    threshold: float | None = None,
    nhitsmin_value: float | None = None,
    hvoldat_file: InputPathType | None = None,
    signal_behind_head: bool = False,
    rescale: bool = False,
    fill_holes_islands: bool = False,
    seed_point: list[float] | None = None,
    or_mask_file: InputPathType | None = None,
    gdiag_option: str | None = None,
) -> MriSeghead:
    """
    Build parameters.

    Args:
        input_volume: Input volume identifier, e.g., T1.
        output_volume: Output volume identifier.
        fill_value: Fill value for binarizing the head (default 255).
        fhi_value: FHI value used in type change operation (default 0.999).
        thresh1_value: Threshold value for detecting the skin (e.g., 20).
        thresh2_value: Threshold value for the final binarization (e.g., 20).
        threshold: Single threshold value applied to both thresh1 and thresh2.
        nhitsmin_value: Minimum number of consecutive hits needed to identify skin\
            (default 2).
        hvoldat_file: File to write the volume of the head in mm3 to an ASCII file.
        signal_behind_head: Consider signals behind the head in the binarization\
            process.
        rescale: Rescale the input when converting to uchar.
        fill_holes_islands: Fill holes and remove islands in the binary volume.
        seed_point: Seed point specified by column, row, slice for filling\
            operation.
        or_mask_file: Mask file to include voxels in the binarization process.
        gdiag_option: Optional diagnostic option for internal use.

    Returns:
        Parameter dictionary.
    """
    params: MriSeghead = {
        "@type": "freesurfer/mri_seghead",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "signal_behind_head": signal_behind_head,
        "rescale": rescale,
        "fill_holes_islands": fill_holes_islands,
    }
    if fill_value is not None:
        params["fill_value"] = fill_value
    if fhi_value is not None:
        params["fhi_value"] = fhi_value
    if thresh1_value is not None:
        params["thresh1_value"] = thresh1_value
    if thresh2_value is not None:
        params["thresh2_value"] = thresh2_value
    if threshold is not None:
        params["threshold"] = threshold
    if nhitsmin_value is not None:
        params["nhitsmin_value"] = nhitsmin_value
    if hvoldat_file is not None:
        params["hvoldat_file"] = hvoldat_file
    if seed_point is not None:
        params["seed_point"] = seed_point
    if or_mask_file is not None:
        params["or_mask_file"] = or_mask_file
    if gdiag_option is not None:
        params["gdiag_option"] = gdiag_option
    return params

def mri_seghead_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSeghead."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], str):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("fill_value", None) is not None:
        if not isinstance(params["fill_value"], (float, int)):
            raise StyxValidationError(f'`fill_value` has the wrong type: Received `{type(params["fill_value"])}` expected `float`')
    if params.get("fhi_value", None) is not None:
        if not isinstance(params["fhi_value"], (float, int)):
            raise StyxValidationError(f'`fhi_value` has the wrong type: Received `{type(params["fhi_value"])}` expected `float`')
    if params.get("thresh1_value", None) is not None:
        if not isinstance(params["thresh1_value"], (float, int)):
            raise StyxValidationError(f'`thresh1_value` has the wrong type: Received `{type(params["thresh1_value"])}` expected `float`')
    if params.get("thresh2_value", None) is not None:
        if not isinstance(params["thresh2_value"], (float, int)):
            raise StyxValidationError(f'`thresh2_value` has the wrong type: Received `{type(params["thresh2_value"])}` expected `float`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("nhitsmin_value", None) is not None:
        if not isinstance(params["nhitsmin_value"], (float, int)):
            raise StyxValidationError(f'`nhitsmin_value` has the wrong type: Received `{type(params["nhitsmin_value"])}` expected `float`')
    if params.get("hvoldat_file", None) is not None:
        if not isinstance(params["hvoldat_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`hvoldat_file` has the wrong type: Received `{type(params["hvoldat_file"])}` expected `InputPathType`')
    if params.get("signal_behind_head", None) is not None:
        if not isinstance(params["signal_behind_head"], bool):
            raise StyxValidationError(f'`signal_behind_head` has the wrong type: Received `{type(params["signal_behind_head"])}` expected `bool`')
    if params.get("rescale", None) is not None:
        if not isinstance(params["rescale"], bool):
            raise StyxValidationError(f'`rescale` has the wrong type: Received `{type(params["rescale"])}` expected `bool`')
    if params.get("fill_holes_islands", None) is not None:
        if not isinstance(params["fill_holes_islands"], bool):
            raise StyxValidationError(f'`fill_holes_islands` has the wrong type: Received `{type(params["fill_holes_islands"])}` expected `bool`')
    if params.get("seed_point", None) is not None:
        if not isinstance(params["seed_point"], list):
            raise StyxValidationError(f'`seed_point` has the wrong type: Received `{type(params["seed_point"])}` expected `list[float]`')
        if not (3 <= len(params["seed_point"]) <= 3):
            raise StyxValidationError("Parameter `seed_point` must contain between 3 and 3 elements (inclusive)")
        for e in params["seed_point"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`seed_point` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("or_mask_file", None) is not None:
        if not isinstance(params["or_mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`or_mask_file` has the wrong type: Received `{type(params["or_mask_file"])}` expected `InputPathType`')
    if params.get("gdiag_option", None) is not None:
        if not isinstance(params["gdiag_option"], str):
            raise StyxValidationError(f'`gdiag_option` has the wrong type: Received `{type(params["gdiag_option"])}` expected `str`')

def mri_seghead_cargs(params: MriSeghead, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_seghead")
    cargs.append("--invol")
    cargs.append(params["input_volume"])
    cargs.append("--outvol")
    cargs.append(params["output_volume"])
    v_0 = params.get("fill_value")
    if v_0 is not None:
        cargs.append("--fill")
        cargs.append(str(v_0))
    v_1 = params.get("fhi_value")
    if v_1 is not None:
        cargs.append("--fhi")
        cargs.append(str(v_1))
    v_2 = params.get("thresh1_value")
    if v_2 is not None:
        cargs.append("--thresh1")
        cargs.append(str(v_2))
    v_3 = params.get("thresh2_value")
    if v_3 is not None:
        cargs.append("--thresh2")
        cargs.append(str(v_3))
    v_4 = params.get("threshold")
    if v_4 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_4))
    v_5 = params.get("nhitsmin_value")
    if v_5 is not None:
        cargs.append("--nhitsmin")
        cargs.append(str(v_5))
    v_6 = params.get("hvoldat_file")
    if v_6 is not None:
        cargs.append("--hvoldat")
        cargs.append(execution.input_file(v_6))
    if params.get("signal_behind_head"):
        cargs.append("--get-signal-behind-head")
    if params.get("rescale"):
        cargs.append("--rescale")
    if params.get("fill_holes_islands"):
        cargs.append("--fill-holes-islands")
    v_7 = params.get("seed_point")
    if v_7 is not None:
        cargs.append("--seed")
        for item0 in v_7:
            cargs.append(str(item0))
    v_8 = params.get("or_mask_file")
    if v_8 is not None:
        cargs.append("--or-mask")
        cargs.append(execution.input_file(v_8))
    v_9 = params.get("gdiag_option")
    if v_9 is not None:
        cargs.append("--gdiag")
        cargs.append(v_9)
    return cargs

def mri_seghead_outputs(params: MriSeghead, execution: Execution) -> MriSegheadOutputs:
    """Build the MriSegheadOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriSegheadOutputs(
        root=root_v,
    )

def mri_seghead_execute(params: MriSeghead, runner: Runner | None = None) -> MriSegheadOutputs:
    """
    mri_seghead

    Binarizes an input volume to identify the head's voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_seghead_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEGHEAD_METADATA)
    execution.params(params)
    args = mri_seghead_cargs(params, execution)
    out = mri_seghead_outputs(params, execution)
    execution.run(args)
    return out

def mri_seghead(
    input_volume: str,
    output_volume: str,
    fill_value: float | None = None,
    fhi_value: float | None = None,
    thresh1_value: float | None = None,
    thresh2_value: float | None = None,
    threshold: float | None = None,
    nhitsmin_value: float | None = None,
    hvoldat_file: InputPathType | None = None,
    signal_behind_head: bool = False,
    rescale: bool = False,
    fill_holes_islands: bool = False,
    seed_point: list[float] | None = None,
    or_mask_file: InputPathType | None = None,
    gdiag_option: str | None = None,
    runner: Runner | None = None,
) -> MriSegheadOutputs:
    """
    mri_seghead

    Binarizes an input volume to identify the head's voxels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume identifier, e.g., T1.
        output_volume: Output volume identifier.
        fill_value: Fill value for binarizing the head (default 255).
        fhi_value: FHI value used in type change operation (default 0.999).
        thresh1_value: Threshold value for detecting the skin (e.g., 20).
        thresh2_value: Threshold value for the final binarization (e.g., 20).
        threshold: Single threshold value applied to both thresh1 and thresh2.
        nhitsmin_value: Minimum number of consecutive hits needed to identify skin\
            (default 2).
        hvoldat_file: File to write the volume of the head in mm3 to an ASCII file.
        signal_behind_head: Consider signals behind the head in the binarization\
            process.
        rescale: Rescale the input when converting to uchar.
        fill_holes_islands: Fill holes and remove islands in the binary volume.
        seed_point: Seed point specified by column, row, slice for filling\
            operation.
        or_mask_file: Mask file to include voxels in the binarization process.
        gdiag_option: Optional diagnostic option for internal use.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_seghead_params(
        input_volume=input_volume,
        output_volume=output_volume,
        fill_value=fill_value,
        fhi_value=fhi_value,
        thresh1_value=thresh1_value,
        thresh2_value=thresh2_value,
        threshold=threshold,
        nhitsmin_value=nhitsmin_value,
        hvoldat_file=hvoldat_file,
        signal_behind_head=signal_behind_head,
        rescale=rescale,
        fill_holes_islands=fill_holes_islands,
        seed_point=seed_point,
        or_mask_file=or_mask_file,
        gdiag_option=gdiag_option,
    )
    return mri_seghead_execute(params, runner)

__all__ = [
    "MriSeghead",
    "MriSegheadOutputs",
    "MRI_SEGHEAD_METADATA",
    "mri_seghead_cargs",
    "mri_seghead_outputs",
    "mri_seghead_params",
    "mri_seghead_execute",
    "mri_seghead_validate",
    "mri_seghead",
]