# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSREALPATH_METADATA = Metadata(
    id="fsrealpath",
    name="fsrealpath",
    package="freesurfer",
)

Fsrealpath = typing.TypedDict(
    "Fsrealpath",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsrealpath"]],
        "path": str,
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsrealpathOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fsrealpath_params(
    path: str,
    help: bool = False,
) -> Fsrealpath:
    """
    Build parameters.

    Args:
        path: The path to resolve
        help: Show this help message and exit

    Returns:
        Parameter dictionary.
    """
    params: Fsrealpath = {
        "@type": "freesurfer/fsrealpath",
        "path": path,
        "help": help,
    }
    return params

def fsrealpath_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fsrealpath."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("path", None) is None:
        raise StyxValidationError("`path` must not be None")
    if not isinstance(params["path"], str):
        raise StyxValidationError(f'`path` has the wrong type: Received `{type(params["path"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def fsrealpath_cargs(params: Fsrealpath, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsrealpath")
    cargs.append(params["path"])
    if params.get("help"):
        cargs.append("-h")
    return cargs

def fsrealpath_outputs(params: Fsrealpath, execution: Execution) -> FsrealpathOutputs:
    """Build the FsrealpathOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsrealpathOutputs(
        root=root_v,
    )

def fsrealpath_execute(params: Fsrealpath, runner: Runner | None = None) -> FsrealpathOutputs:
    """
    fsrealpath

    Resolve symbolic links in a path.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsrealpath_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSREALPATH_METADATA)
    execution.params(params)
    args = fsrealpath_cargs(params, execution)
    out = fsrealpath_outputs(params, execution)
    execution.run(args)
    return out

def fsrealpath(
    path: str,
    help: bool = False,
    runner: Runner | None = None,
) -> FsrealpathOutputs:
    """
    fsrealpath

    Resolve symbolic links in a path.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        path: The path to resolve
        help: Show this help message and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsrealpath_params(
        path=path,
        help=help,
    )
    return fsrealpath_execute(params, runner)

__all__ = [
    "Fsrealpath",
    "FsrealpathOutputs",
    "FSREALPATH_METADATA",
    "fsrealpath_cargs",
    "fsrealpath_outputs",
    "fsrealpath_params",
    "fsrealpath_execute",
    "fsrealpath_validate",
    "fsrealpath",
]