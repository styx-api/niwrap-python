# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_SUBMIT_JOBS_METADATA = Metadata(
    id="long_submit_jobs",
    name="long_submit_jobs",
    package="freesurfer",
)

LongSubmitJobs = typing.TypedDict(
    "LongSubmitJobs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_submit_jobs"]],
        "qdec": InputPathType,
        "cdir": str,
        "bdir": typing.NotRequired[str],
        "ldir": typing.NotRequired[str],
        "scriptsdir": typing.NotRequired[str],
        "cross": typing.NotRequired[bool],
        "base": typing.NotRequired[bool],
        "long": typing.NotRequired[bool],
        "cflags": typing.NotRequired[str],
        "bflags": typing.NotRequired[str],
        "lflags": typing.NotRequired[str],
        "affine": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "simulate": typing.NotRequired[bool],
        "simfiles": typing.NotRequired[bool],
        "check": typing.NotRequired[bool],
        "pause": typing.NotRequired[float],
        "max": typing.NotRequired[float],
        "queue": typing.NotRequired[str],
        "cmem": typing.NotRequired[float],
        "bmem": typing.NotRequired[float],
        "lmem": typing.NotRequired[float],
        "cnodes": typing.NotRequired[float],
        "bnodes": typing.NotRequired[float],
        "lnodes": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class LongSubmitJobsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def long_submit_jobs_params(
    qdec: InputPathType,
    cdir: str,
    bdir: str | None = None,
    ldir: str | None = None,
    scriptsdir: str | None = None,
    cross: bool = False,
    base: bool = False,
    long: bool = False,
    cflags: str | None = None,
    bflags: str | None = None,
    lflags: str | None = None,
    affine: bool = False,
    force: bool = False,
    simulate: bool = False,
    simfiles: bool = False,
    check: bool = False,
    pause: float | None = None,
    max: float | None = None,
    queue: str | None = None,
    cmem: float | None = None,
    bmem: float | None = None,
    lmem: float | None = None,
    cnodes: float | None = None,
    bnodes: float | None = None,
    lnodes: float | None = None,
) -> LongSubmitJobs:
    """
    Build parameters.

    Args:
        qdec: QDEC table file specifying the subjects and time points.
        cdir: Directory for cross-sectional subjects (inherited by base and long).
        bdir: Directory for base runs (default: inherit from cross).
        ldir: Directory for longitudinal runs (default: inherit from base).
        scriptsdir: Location to save submitted scripts (default:\
            <cdir,bdir,ldir>/scripts_submitted).
        cross: Process cross-sectional streams.
        base: Process base streams.
        long: Process longitudinal streams.
        cflags: Manual flags for cross processing (e.g., '-all -cw256').
        bflags: Manual flags for base processing (default: '-all').
        lflags: Manual flags for long processing (default: '-all').
        affine: Use affine registration for base.
        force: Force reprocessing of jobs even if marked as done.
        simulate: Simulate submission only, without executing.
        simfiles: Simulate command file creation only, without executing.
        check: Check if all longitudinal processing is complete.
        pause: Pause duration (in seconds) between submissions (default: 13).
        max: Maximum number of jobs per user (default: 100).
        queue: Queue to submit jobs.
        cmem: RAM (in GB) requested for cross (default: 7).
        bmem: RAM (in GB) requested for base (default: 7).
        lmem: RAM (in GB) requested for long (default: 7).
        cnodes: Number of nodes for cross runs (default: 1).
        bnodes: Number of nodes for base runs (default: 1).
        lnodes: Number of nodes for long runs (default: 1).

    Returns:
        Parameter dictionary.
    """
    params: LongSubmitJobs = {
        "@type": "freesurfer/long_submit_jobs",
        "qdec": qdec,
        "cdir": cdir,
        "cross": cross,
        "base": base,
        "long": long,
        "affine": affine,
        "force": force,
        "simulate": simulate,
        "simfiles": simfiles,
        "check": check,
    }
    if bdir is not None:
        params["bdir"] = bdir
    if ldir is not None:
        params["ldir"] = ldir
    if scriptsdir is not None:
        params["scriptsdir"] = scriptsdir
    if cflags is not None:
        params["cflags"] = cflags
    if bflags is not None:
        params["bflags"] = bflags
    if lflags is not None:
        params["lflags"] = lflags
    if pause is not None:
        params["pause"] = pause
    if max is not None:
        params["max"] = max
    if queue is not None:
        params["queue"] = queue
    if cmem is not None:
        params["cmem"] = cmem
    if bmem is not None:
        params["bmem"] = bmem
    if lmem is not None:
        params["lmem"] = lmem
    if cnodes is not None:
        params["cnodes"] = cnodes
    if bnodes is not None:
        params["bnodes"] = bnodes
    if lnodes is not None:
        params["lnodes"] = lnodes
    return params

def long_submit_jobs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongSubmitJobs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec", None) is None:
        raise StyxValidationError("`qdec` must not be None")
    if not isinstance(params["qdec"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec` has the wrong type: Received `{type(params["qdec"])}` expected `InputPathType`')
    if params.get("cdir", None) is None:
        raise StyxValidationError("`cdir` must not be None")
    if not isinstance(params["cdir"], str):
        raise StyxValidationError(f'`cdir` has the wrong type: Received `{type(params["cdir"])}` expected `str`')
    if params.get("bdir", None) is not None:
        if not isinstance(params["bdir"], str):
            raise StyxValidationError(f'`bdir` has the wrong type: Received `{type(params["bdir"])}` expected `str`')
    if params.get("ldir", None) is not None:
        if not isinstance(params["ldir"], str):
            raise StyxValidationError(f'`ldir` has the wrong type: Received `{type(params["ldir"])}` expected `str`')
    if params.get("scriptsdir", None) is not None:
        if not isinstance(params["scriptsdir"], str):
            raise StyxValidationError(f'`scriptsdir` has the wrong type: Received `{type(params["scriptsdir"])}` expected `str`')
    if params.get("cross", None) is not None:
        if not isinstance(params["cross"], bool):
            raise StyxValidationError(f'`cross` has the wrong type: Received `{type(params["cross"])}` expected `bool`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], bool):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `bool`')
    if params.get("long", None) is not None:
        if not isinstance(params["long"], bool):
            raise StyxValidationError(f'`long` has the wrong type: Received `{type(params["long"])}` expected `bool`')
    if params.get("cflags", None) is not None:
        if not isinstance(params["cflags"], str):
            raise StyxValidationError(f'`cflags` has the wrong type: Received `{type(params["cflags"])}` expected `str`')
    if params.get("bflags", None) is not None:
        if not isinstance(params["bflags"], str):
            raise StyxValidationError(f'`bflags` has the wrong type: Received `{type(params["bflags"])}` expected `str`')
    if params.get("lflags", None) is not None:
        if not isinstance(params["lflags"], str):
            raise StyxValidationError(f'`lflags` has the wrong type: Received `{type(params["lflags"])}` expected `str`')
    if params.get("affine", None) is not None:
        if not isinstance(params["affine"], bool):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("simulate", None) is not None:
        if not isinstance(params["simulate"], bool):
            raise StyxValidationError(f'`simulate` has the wrong type: Received `{type(params["simulate"])}` expected `bool`')
    if params.get("simfiles", None) is not None:
        if not isinstance(params["simfiles"], bool):
            raise StyxValidationError(f'`simfiles` has the wrong type: Received `{type(params["simfiles"])}` expected `bool`')
    if params.get("check", None) is not None:
        if not isinstance(params["check"], bool):
            raise StyxValidationError(f'`check` has the wrong type: Received `{type(params["check"])}` expected `bool`')
    if params.get("pause", None) is not None:
        if not isinstance(params["pause"], (float, int)):
            raise StyxValidationError(f'`pause` has the wrong type: Received `{type(params["pause"])}` expected `float`')
    if params.get("max", None) is not None:
        if not isinstance(params["max"], (float, int)):
            raise StyxValidationError(f'`max` has the wrong type: Received `{type(params["max"])}` expected `float`')
    if params.get("queue", None) is not None:
        if not isinstance(params["queue"], str):
            raise StyxValidationError(f'`queue` has the wrong type: Received `{type(params["queue"])}` expected `str`')
    if params.get("cmem", None) is not None:
        if not isinstance(params["cmem"], (float, int)):
            raise StyxValidationError(f'`cmem` has the wrong type: Received `{type(params["cmem"])}` expected `float`')
    if params.get("bmem", None) is not None:
        if not isinstance(params["bmem"], (float, int)):
            raise StyxValidationError(f'`bmem` has the wrong type: Received `{type(params["bmem"])}` expected `float`')
    if params.get("lmem", None) is not None:
        if not isinstance(params["lmem"], (float, int)):
            raise StyxValidationError(f'`lmem` has the wrong type: Received `{type(params["lmem"])}` expected `float`')
    if params.get("cnodes", None) is not None:
        if not isinstance(params["cnodes"], (float, int)):
            raise StyxValidationError(f'`cnodes` has the wrong type: Received `{type(params["cnodes"])}` expected `float`')
    if params.get("bnodes", None) is not None:
        if not isinstance(params["bnodes"], (float, int)):
            raise StyxValidationError(f'`bnodes` has the wrong type: Received `{type(params["bnodes"])}` expected `float`')
    if params.get("lnodes", None) is not None:
        if not isinstance(params["lnodes"], (float, int)):
            raise StyxValidationError(f'`lnodes` has the wrong type: Received `{type(params["lnodes"])}` expected `float`')

def long_submit_jobs_cargs(params: LongSubmitJobs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_submit_jobs")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec"]))
    cargs.append("--cdir")
    cargs.append(params["cdir"])
    v_0 = params.get("bdir")
    if v_0 is not None:
        cargs.append("--bdir")
        cargs.append(v_0)
    v_1 = params.get("ldir")
    if v_1 is not None:
        cargs.append("--ldir")
        cargs.append(v_1)
    v_2 = params.get("scriptsdir")
    if v_2 is not None:
        cargs.append("--scriptsdir")
        cargs.append(v_2)
    if params.get("cross"):
        cargs.append("--cross")
    if params.get("base"):
        cargs.append("--base")
    if params.get("long"):
        cargs.append("--long")
    v_3 = params.get("cflags")
    if v_3 is not None:
        cargs.append("--cflags")
        cargs.append(v_3)
    v_4 = params.get("bflags")
    if v_4 is not None:
        cargs.append("--bflags")
        cargs.append(v_4)
    v_5 = params.get("lflags")
    if v_5 is not None:
        cargs.append("--lflags")
        cargs.append(v_5)
    if params.get("affine"):
        cargs.append("--affine")
    if params.get("force"):
        cargs.append("--force")
    if params.get("simulate"):
        cargs.append("--simulate")
    if params.get("simfiles"):
        cargs.append("--simfiles")
    if params.get("check"):
        cargs.append("--check")
    v_6 = params.get("pause")
    if v_6 is not None:
        cargs.append("--pause")
        cargs.append(str(v_6))
    v_7 = params.get("max")
    if v_7 is not None:
        cargs.append("--max")
        cargs.append(str(v_7))
    v_8 = params.get("queue")
    if v_8 is not None:
        cargs.append("--queue")
        cargs.append(v_8)
    v_9 = params.get("cmem")
    if v_9 is not None:
        cargs.append("--cmem")
        cargs.append(str(v_9))
    v_10 = params.get("bmem")
    if v_10 is not None:
        cargs.append("--bmem")
        cargs.append(str(v_10))
    v_11 = params.get("lmem")
    if v_11 is not None:
        cargs.append("--lmem")
        cargs.append(str(v_11))
    v_12 = params.get("cnodes")
    if v_12 is not None:
        cargs.append("--cnodes")
        cargs.append(str(v_12))
    v_13 = params.get("bnodes")
    if v_13 is not None:
        cargs.append("--bnodes")
        cargs.append(str(v_13))
    v_14 = params.get("lnodes")
    if v_14 is not None:
        cargs.append("--lnodes")
        cargs.append(str(v_14))
    return cargs

def long_submit_jobs_outputs(params: LongSubmitJobs, execution: Execution) -> LongSubmitJobsOutputs:
    """Build the LongSubmitJobsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LongSubmitJobsOutputs(
        root=root_v,
    )

def long_submit_jobs_execute(params: LongSubmitJobs, runner: Runner | None = None) -> LongSubmitJobsOutputs:
    """
    long_submit_jobs

    Submits longitudinal processing jobs to the NMR cluster (seychelles or launchpad).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_submit_jobs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_SUBMIT_JOBS_METADATA)
    execution.params(params)
    args = long_submit_jobs_cargs(params, execution)
    out = long_submit_jobs_outputs(params, execution)
    execution.run(args)
    return out

def long_submit_jobs(
    qdec: InputPathType,
    cdir: str,
    bdir: str | None = None,
    ldir: str | None = None,
    scriptsdir: str | None = None,
    cross: bool = False,
    base: bool = False,
    long: bool = False,
    cflags: str | None = None,
    bflags: str | None = None,
    lflags: str | None = None,
    affine: bool = False,
    force: bool = False,
    simulate: bool = False,
    simfiles: bool = False,
    check: bool = False,
    pause: float | None = None,
    max: float | None = None,
    queue: str | None = None,
    cmem: float | None = None,
    bmem: float | None = None,
    lmem: float | None = None,
    cnodes: float | None = None,
    bnodes: float | None = None,
    lnodes: float | None = None,
    runner: Runner | None = None,
) -> LongSubmitJobsOutputs:
    """
    long_submit_jobs

    Submits longitudinal processing jobs to the NMR cluster (seychelles or launchpad).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec: QDEC table file specifying the subjects and time points.
        cdir: Directory for cross-sectional subjects (inherited by base and long).
        bdir: Directory for base runs (default: inherit from cross).
        ldir: Directory for longitudinal runs (default: inherit from base).
        scriptsdir: Location to save submitted scripts (default:\
            <cdir,bdir,ldir>/scripts_submitted).
        cross: Process cross-sectional streams.
        base: Process base streams.
        long: Process longitudinal streams.
        cflags: Manual flags for cross processing (e.g., '-all -cw256').
        bflags: Manual flags for base processing (default: '-all').
        lflags: Manual flags for long processing (default: '-all').
        affine: Use affine registration for base.
        force: Force reprocessing of jobs even if marked as done.
        simulate: Simulate submission only, without executing.
        simfiles: Simulate command file creation only, without executing.
        check: Check if all longitudinal processing is complete.
        pause: Pause duration (in seconds) between submissions (default: 13).
        max: Maximum number of jobs per user (default: 100).
        queue: Queue to submit jobs.
        cmem: RAM (in GB) requested for cross (default: 7).
        bmem: RAM (in GB) requested for base (default: 7).
        lmem: RAM (in GB) requested for long (default: 7).
        cnodes: Number of nodes for cross runs (default: 1).
        bnodes: Number of nodes for base runs (default: 1).
        lnodes: Number of nodes for long runs (default: 1).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_submit_jobs_params(
        qdec=qdec,
        cdir=cdir,
        bdir=bdir,
        ldir=ldir,
        scriptsdir=scriptsdir,
        cross=cross,
        base=base,
        long=long,
        cflags=cflags,
        bflags=bflags,
        lflags=lflags,
        affine=affine,
        force=force,
        simulate=simulate,
        simfiles=simfiles,
        check=check,
        pause=pause,
        max=max,
        queue=queue,
        cmem=cmem,
        bmem=bmem,
        lmem=lmem,
        cnodes=cnodes,
        bnodes=bnodes,
        lnodes=lnodes,
    )
    return long_submit_jobs_execute(params, runner)

__all__ = [
    "LongSubmitJobs",
    "LongSubmitJobsOutputs",
    "LONG_SUBMIT_JOBS_METADATA",
    "long_submit_jobs_cargs",
    "long_submit_jobs_outputs",
    "long_submit_jobs_params",
    "long_submit_jobs_execute",
    "long_submit_jobs_validate",
    "long_submit_jobs",
]