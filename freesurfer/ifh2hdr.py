# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IFH2HDR_METADATA = Metadata(
    id="ifh2hdr",
    name="ifh2hdr",
    package="freesurfer",
)

Ifh2hdr = typing.TypedDict(
    "Ifh2hdr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/ifh2hdr"]],
        "input_file": InputPathType,
        "range": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Ifh2hdrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def ifh2hdr_params(
    input_file: InputPathType,
    range_: str | None = None,
) -> Ifh2hdr:
    """
    Build parameters.

    Args:
        input_file: Input 4dfp file
        range_: Set range for the 4dfp file. Format: <float>[to<float>].

    Returns:
        Parameter dictionary.
    """
    params: Ifh2hdr = {
        "@type": "freesurfer/ifh2hdr",
        "input_file": input_file,
    }
    if range_ is not None:
        params["range"] = range_
    return params

def ifh2hdr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ifh2hdr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("range", None) is not None:
        if not isinstance(params["range"], str):
            raise StyxValidationError(f'`range` has the wrong type: Received `{type(params["range"])}` expected `str`')

def ifh2hdr_cargs(params: Ifh2hdr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ifh2hdr")
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("range")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(v_0)
    return cargs

def ifh2hdr_outputs(params: Ifh2hdr, execution: Execution) -> Ifh2hdrOutputs:
    """Build the Ifh2hdrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Ifh2hdrOutputs(
        root=root_v,
    )

def ifh2hdr_execute(params: Ifh2hdr, runner: Runner | None = None) -> Ifh2hdrOutputs:
    """
    ifh2hdr

    Tool for converting IFH (Interfile Header) to HDR (Header) format in 4dfp (Four-Dimensional Functional Image) file format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ifh2hdr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IFH2HDR_METADATA)
    execution.params(params)
    args = ifh2hdr_cargs(params, execution)
    out = ifh2hdr_outputs(params, execution)
    execution.run(args)
    return out

def ifh2hdr(
    input_file: InputPathType,
    range_: str | None = None,
    runner: Runner | None = None,
) -> Ifh2hdrOutputs:
    """
    ifh2hdr

    Tool for converting IFH (Interfile Header) to HDR (Header) format in 4dfp (Four-Dimensional Functional Image) file format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input 4dfp file
        range_: Set range for the 4dfp file. Format: <float>[to<float>].
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ifh2hdr_params(
        input_file=input_file,
        range_=range_,
    )
    return ifh2hdr_execute(params, runner)

__all__ = [
    "Ifh2hdr",
    "Ifh2hdrOutputs",
    "IFH2HDR_METADATA",
    "ifh2hdr_cargs",
    "ifh2hdr_outputs",
    "ifh2hdr_params",
    "ifh2hdr_execute",
    "ifh2hdr_validate",
    "ifh2hdr",
]