# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CHECK_MCR_SH_METADATA = Metadata(
    id="checkMCR.sh",
    name="checkMCR.sh",
    package="freesurfer",
)

CheckMcrSh = typing.TypedDict(
    "CheckMcrSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/checkMCR.sh"]],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CheckMcrShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def check_mcr_sh_params(
    help: bool = False,
) -> CheckMcrSh:
    """
    Build parameters.

    Args:
        help: Display help information about checkMCR.sh

    Returns:
        Parameter dictionary.
    """
    params: CheckMcrSh = {
        "@type": "freesurfer/checkMCR.sh",
        "help": help,
    }
    return params

def check_mcr_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CheckMcrSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def check_mcr_sh_cargs(params: CheckMcrSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("checkMCR.sh")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def check_mcr_sh_outputs(params: CheckMcrSh, execution: Execution) -> CheckMcrShOutputs:
    """Build the CheckMcrShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CheckMcrShOutputs(
        root=root_v,
    )

def check_mcr_sh_execute(params: CheckMcrSh, runner: Runner | None = None) -> CheckMcrShOutputs:
    """
    checkMCR.sh

    Script to check for the presence of Matlab Compiler Runtime (MCR) for Matlab 2019b, used for the hippocampal/amygdala and brainstem modules.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    check_mcr_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CHECK_MCR_SH_METADATA)
    execution.params(params)
    args = check_mcr_sh_cargs(params, execution)
    out = check_mcr_sh_outputs(params, execution)
    execution.run(args)
    return out

def check_mcr_sh(
    help: bool = False,
    runner: Runner | None = None,
) -> CheckMcrShOutputs:
    """
    checkMCR.sh

    Script to check for the presence of Matlab Compiler Runtime (MCR) for Matlab 2019b, used for the hippocampal/amygdala and brainstem modules.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        help: Display help information about checkMCR.sh
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = check_mcr_sh_params(
        help=help,
    )
    return check_mcr_sh_execute(params, runner)

__all__ = [
    "CheckMcrSh",
    "CheckMcrShOutputs",
    "CHECK_MCR_SH_METADATA",
    "check_mcr_sh_cargs",
    "check_mcr_sh_outputs",
    "check_mcr_sh_params",
    "check_mcr_sh_execute",
    "check_mcr_sh_validate",
    "check_mcr_sh",
]