# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_DCT_ALIGN_BINARY_METADATA = Metadata(
    id="mri_dct_align_binary",
    name="mri_dct_align_binary",
    package="freesurfer",
)

MriDctAlignBinary = typing.TypedDict(
    "MriDctAlignBinary",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_dct_align_binary"]],
        "source_image": InputPathType,
        "destination_image": InputPathType,
        "output_transformation": str,
    },
)

@dataclasses.dataclass
class MriDctAlignBinaryOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_transformation_file: OutputPathType
    """The resulting transformation from the alignment"""

def mri_dct_align_binary_params(
    source_image: InputPathType,
    destination_image: InputPathType,
    output_transformation: str,
) -> MriDctAlignBinary:
    """
    Build parameters.

    Args:
        source_image: Source image for alignment
        destination_image: Destination image for alignment
        output_transformation: Output transformation file

    Returns:
        Parameter dictionary.
    """
    params: MriDctAlignBinary = {
        "@type": "freesurfer/mri_dct_align_binary",
        "source_image": source_image,
        "destination_image": destination_image,
        "output_transformation": output_transformation,
    }
    return params

def mri_dct_align_binary_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriDctAlignBinary."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_image", None) is None:
        raise StyxValidationError("`source_image` must not be None")
    if not isinstance(params["source_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_image` has the wrong type: Received `{type(params["source_image"])}` expected `InputPathType`')
    if params.get("destination_image", None) is None:
        raise StyxValidationError("`destination_image` must not be None")
    if not isinstance(params["destination_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`destination_image` has the wrong type: Received `{type(params["destination_image"])}` expected `InputPathType`')
    if params.get("output_transformation", None) is None:
        raise StyxValidationError("`output_transformation` must not be None")
    if not isinstance(params["output_transformation"], str):
        raise StyxValidationError(f'`output_transformation` has the wrong type: Received `{type(params["output_transformation"])}` expected `str`')

def mri_dct_align_binary_cargs(params: MriDctAlignBinary, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_dct_align_binary")
    cargs.append(execution.input_file(params["source_image"]))
    cargs.append(execution.input_file(params["destination_image"]))
    cargs.append(params["output_transformation"])
    return cargs

def mri_dct_align_binary_outputs(params: MriDctAlignBinary, execution: Execution) -> MriDctAlignBinaryOutputs:
    """Build the MriDctAlignBinaryOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_transformation_file_v: OutputPathType = execution.output_file(params["output_transformation"])
    return MriDctAlignBinaryOutputs(
        root=root_v,
        output_transformation_file=output_transformation_file_v,
    )

def mri_dct_align_binary_execute(params: MriDctAlignBinary, runner: Runner | None = None) -> MriDctAlignBinaryOutputs:
    """
    mri_dct_align_binary

    A binary tool for aligning MRI images using DCT.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_dct_align_binary_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_DCT_ALIGN_BINARY_METADATA)
    execution.params(params)
    args = mri_dct_align_binary_cargs(params, execution)
    out = mri_dct_align_binary_outputs(params, execution)
    execution.run(args)
    return out

def mri_dct_align_binary(
    source_image: InputPathType,
    destination_image: InputPathType,
    output_transformation: str,
    runner: Runner | None = None,
) -> MriDctAlignBinaryOutputs:
    """
    mri_dct_align_binary

    A binary tool for aligning MRI images using DCT.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_image: Source image for alignment
        destination_image: Destination image for alignment
        output_transformation: Output transformation file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_dct_align_binary_params(
        source_image=source_image,
        destination_image=destination_image,
        output_transformation=output_transformation,
    )
    return mri_dct_align_binary_execute(params, runner)

__all__ = [
    "MriDctAlignBinary",
    "MriDctAlignBinaryOutputs",
    "MRI_DCT_ALIGN_BINARY_METADATA",
    "mri_dct_align_binary_cargs",
    "mri_dct_align_binary_outputs",
    "mri_dct_align_binary_params",
    "mri_dct_align_binary_execute",
    "mri_dct_align_binary_validate",
    "mri_dct_align_binary",
]