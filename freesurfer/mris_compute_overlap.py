# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_COMPUTE_OVERLAP_METADATA = Metadata(
    id="mris_compute_overlap",
    name="mris_compute_overlap",
    package="freesurfer",
)

MrisComputeOverlap = typing.TypedDict(
    "MrisComputeOverlap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_compute_overlap"]],
        "subject": str,
        "hemi": str,
        "surface": str,
        "annotation": str,
        "labels": list[str],
        "percentage": typing.NotRequired[bool],
        "log_file": typing.NotRequired[str],
        "brain_volume": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MrisComputeOverlapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_compute_overlap_params(
    subject: str,
    hemi: str,
    surface: str,
    annotation: str,
    labels: list[str],
    percentage: bool = False,
    log_file: str | None = None,
    brain_volume: InputPathType | None = None,
) -> MrisComputeOverlap:
    """
    Build parameters.

    Args:
        subject: Subject name
        hemi: Hemisphere (e.g. lh or rh)
        surface: Surface name
        annotation: Annotation name
        labels: Labels to compute overlap for
        percentage: Compute brain area as a percentage of all brain labels
        log_file: Log results to file, where %d will include label number
        brain_volume: Load brain volume and use it to normalize areas

    Returns:
        Parameter dictionary.
    """
    params: MrisComputeOverlap = {
        "@type": "freesurfer/mris_compute_overlap",
        "subject": subject,
        "hemi": hemi,
        "surface": surface,
        "annotation": annotation,
        "labels": labels,
        "percentage": percentage,
    }
    if log_file is not None:
        params["log_file"] = log_file
    if brain_volume is not None:
        params["brain_volume"] = brain_volume
    return params

def mris_compute_overlap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisComputeOverlap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("annotation", None) is None:
        raise StyxValidationError("`annotation` must not be None")
    if not isinstance(params["annotation"], str):
        raise StyxValidationError(f'`annotation` has the wrong type: Received `{type(params["annotation"])}` expected `str`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("percentage", None) is not None:
        if not isinstance(params["percentage"], bool):
            raise StyxValidationError(f'`percentage` has the wrong type: Received `{type(params["percentage"])}` expected `bool`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("brain_volume", None) is not None:
        if not isinstance(params["brain_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`brain_volume` has the wrong type: Received `{type(params["brain_volume"])}` expected `InputPathType`')

def mris_compute_overlap_cargs(params: MrisComputeOverlap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_compute_overlap")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(params["surface"])
    cargs.append(params["annotation"])
    for item0 in params["labels"]:
        cargs.append(item0)
    if params.get("percentage"):
        cargs.append("-p")
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-l")
        cargs.append(v_0)
    v_1 = params.get("brain_volume")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(execution.input_file(v_1))
    return cargs

def mris_compute_overlap_outputs(params: MrisComputeOverlap, execution: Execution) -> MrisComputeOverlapOutputs:
    """Build the MrisComputeOverlapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisComputeOverlapOutputs(
        root=root_v,
    )

def mris_compute_overlap_execute(params: MrisComputeOverlap, runner: Runner | None = None) -> MrisComputeOverlapOutputs:
    """
    mris_compute_overlap

    Tool to compute the overlap between two or more labels on a cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_compute_overlap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_COMPUTE_OVERLAP_METADATA)
    execution.params(params)
    args = mris_compute_overlap_cargs(params, execution)
    out = mris_compute_overlap_outputs(params, execution)
    execution.run(args)
    return out

def mris_compute_overlap(
    subject: str,
    hemi: str,
    surface: str,
    annotation: str,
    labels: list[str],
    percentage: bool = False,
    log_file: str | None = None,
    brain_volume: InputPathType | None = None,
    runner: Runner | None = None,
) -> MrisComputeOverlapOutputs:
    """
    mris_compute_overlap

    Tool to compute the overlap between two or more labels on a cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject name
        hemi: Hemisphere (e.g. lh or rh)
        surface: Surface name
        annotation: Annotation name
        labels: Labels to compute overlap for
        percentage: Compute brain area as a percentage of all brain labels
        log_file: Log results to file, where %d will include label number
        brain_volume: Load brain volume and use it to normalize areas
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_compute_overlap_params(
        subject=subject,
        hemi=hemi,
        surface=surface,
        annotation=annotation,
        labels=labels,
        percentage=percentage,
        log_file=log_file,
        brain_volume=brain_volume,
    )
    return mris_compute_overlap_execute(params, runner)

__all__ = [
    "MrisComputeOverlap",
    "MrisComputeOverlapOutputs",
    "MRIS_COMPUTE_OVERLAP_METADATA",
    "mris_compute_overlap_cargs",
    "mris_compute_overlap_outputs",
    "mris_compute_overlap_params",
    "mris_compute_overlap_execute",
    "mris_compute_overlap_validate",
    "mris_compute_overlap",
]