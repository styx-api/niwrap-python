# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SBTIV_METADATA = Metadata(
    id="sbtiv",
    name="sbtiv",
    package="freesurfer",
)

Sbtiv = typing.TypedDict(
    "Sbtiv",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sbtiv"]],
        "input_file": InputPathType,
        "output_file": typing.NotRequired[str],
        "labels_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SbtivOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Intracranial stats output file."""

def sbtiv_params(
    input_file: InputPathType,
    output_file: str | None = None,
    labels_file: InputPathType | None = None,
) -> Sbtiv:
    """
    Build parameters.

    Args:
        input_file: Volume stats input file.
        output_file: Intracranial stats output file.
        labels_file: File containing a list of intracranial structure labelnames to\
            include in the calculation

    Returns:
        Parameter dictionary.
    """
    params: Sbtiv = {
        "@type": "freesurfer/sbtiv",
        "input_file": input_file,
    }
    if output_file is not None:
        params["output_file"] = output_file
    if labels_file is not None:
        params["labels_file"] = labels_file
    return params

def sbtiv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sbtiv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("labels_file", None) is not None:
        if not isinstance(params["labels_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`labels_file` has the wrong type: Received `{type(params["labels_file"])}` expected `InputPathType`')

def sbtiv_cargs(params: Sbtiv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sbtiv")
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("labels_file")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_1))
    return cargs

def sbtiv_outputs(params: Sbtiv, execution: Execution) -> SbtivOutputs:
    """Build the SbtivOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        out_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return SbtivOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def sbtiv_execute(params: Sbtiv, runner: Runner | None = None) -> SbtivOutputs:
    """
    sbtiv

    Tool to calculate the total intracranial volume of a subject by summing individual volumes computed by samseg.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sbtiv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SBTIV_METADATA)
    execution.params(params)
    args = sbtiv_cargs(params, execution)
    out = sbtiv_outputs(params, execution)
    execution.run(args)
    return out

def sbtiv(
    input_file: InputPathType,
    output_file: str | None = None,
    labels_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> SbtivOutputs:
    """
    sbtiv

    Tool to calculate the total intracranial volume of a subject by summing individual volumes computed by samseg.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Volume stats input file.
        output_file: Intracranial stats output file.
        labels_file: File containing a list of intracranial structure labelnames to\
            include in the calculation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sbtiv_params(
        input_file=input_file,
        output_file=output_file,
        labels_file=labels_file,
    )
    return sbtiv_execute(params, runner)

__all__ = [
    "Sbtiv",
    "SbtivOutputs",
    "SBTIV_METADATA",
    "sbtiv_cargs",
    "sbtiv_outputs",
    "sbtiv_params",
    "sbtiv_execute",
    "sbtiv_validate",
    "sbtiv",
]