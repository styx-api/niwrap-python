# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAP_TO_BASE_METADATA = Metadata(
    id="map_to_base",
    name="map_to_base",
    package="freesurfer",
)

MapToBase = typing.TypedDict(
    "MapToBase",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/map_to_base"]],
        "baseid": str,
        "tpid": str,
        "input_image": str,
        "resample_type": str,
        "cross": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MapToBaseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_long_mri: OutputPathType
    """Output in base/mri directory when mapped from a longitudinal directory."""
    output_long_surf: OutputPathType
    """Output in base/surf directory when mapped from a longitudinal directory."""
    output_cross_mri: OutputPathType
    """Output in base/mri directory when mapped from a cross-sectional directory."""
    output_cross_surf: OutputPathType
    """Output in base/surf directory when mapped from a cross-sectional directory."""

def map_to_base_params(
    baseid: str,
    tpid: str,
    input_image: str,
    resample_type: str,
    cross: str | None = None,
) -> MapToBase:
    """
    Build parameters.

    Args:
        baseid: Identifier of the base.
        tpid: Identifier of the time point, without the '.long.baseid' suffix.
        input_image: Input image, e.g., norm.mgz, aseg.mgz, lh.white.
        resample_type: Resample type. 'interpolate' for norm, T1, orig; 'nearest'\
            for aseg; 'surface' for surfaces.
        cross: If '1', input is from cross sectionals; default is to use\
            longitudinal directories.

    Returns:
        Parameter dictionary.
    """
    params: MapToBase = {
        "@type": "freesurfer/map_to_base",
        "baseid": baseid,
        "tpid": tpid,
        "input_image": input_image,
        "resample_type": resample_type,
    }
    if cross is not None:
        params["cross"] = cross
    return params

def map_to_base_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MapToBase."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("baseid", None) is None:
        raise StyxValidationError("`baseid` must not be None")
    if not isinstance(params["baseid"], str):
        raise StyxValidationError(f'`baseid` has the wrong type: Received `{type(params["baseid"])}` expected `str`')
    if params.get("tpid", None) is None:
        raise StyxValidationError("`tpid` must not be None")
    if not isinstance(params["tpid"], str):
        raise StyxValidationError(f'`tpid` has the wrong type: Received `{type(params["tpid"])}` expected `str`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], str):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `str`')
    if params.get("resample_type", None) is None:
        raise StyxValidationError("`resample_type` must not be None")
    if not isinstance(params["resample_type"], str):
        raise StyxValidationError(f'`resample_type` has the wrong type: Received `{type(params["resample_type"])}` expected `str`')
    if params.get("cross", None) is not None:
        if not isinstance(params["cross"], str):
            raise StyxValidationError(f'`cross` has the wrong type: Received `{type(params["cross"])}` expected `str`')

def map_to_base_cargs(params: MapToBase, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("map_to_base")
    cargs.append(params["baseid"])
    cargs.append(params["tpid"])
    cargs.append(params["input_image"])
    cargs.append(params["resample_type"])
    v_0 = params.get("cross")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def map_to_base_outputs(params: MapToBase, execution: Execution) -> MapToBaseOutputs:
    """Build the MapToBaseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_long_mri_v: OutputPathType = execution.output_file(f'{params["baseid"]}/mri/{params["tpid"]}-long.{params["input_image"]}')
    output_long_surf_v: OutputPathType = execution.output_file(f'{params["baseid"]}/surf/{params["tpid"]}-long.{params["input_image"]}')
    output_cross_mri_v: OutputPathType = execution.output_file(f'{params["baseid"]}/mri/{params["tpid"]}-cross.{params["input_image"]}')
    output_cross_surf_v: OutputPathType = execution.output_file(f'{params["baseid"]}/surf/{params["tpid"]}-cross.{params["input_image"]}')
    return MapToBaseOutputs(
        root=root_v,
        output_long_mri=output_long_mri_v,
        output_long_surf=output_long_surf_v,
        output_cross_mri=output_cross_mri_v,
        output_cross_surf=output_cross_surf_v,
    )

def map_to_base_execute(params: MapToBase, runner: Runner | None = None) -> MapToBaseOutputs:
    """
    map_to_base

    Maps an image or surface from a time point directory (either cross-sectional or longitudinal) to the base space and outputs it in the appropriate base directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    map_to_base_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAP_TO_BASE_METADATA)
    execution.params(params)
    args = map_to_base_cargs(params, execution)
    out = map_to_base_outputs(params, execution)
    execution.run(args)
    return out

def map_to_base(
    baseid: str,
    tpid: str,
    input_image: str,
    resample_type: str,
    cross: str | None = None,
    runner: Runner | None = None,
) -> MapToBaseOutputs:
    """
    map_to_base

    Maps an image or surface from a time point directory (either cross-sectional or longitudinal) to the base space and outputs it in the appropriate base directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        baseid: Identifier of the base.
        tpid: Identifier of the time point, without the '.long.baseid' suffix.
        input_image: Input image, e.g., norm.mgz, aseg.mgz, lh.white.
        resample_type: Resample type. 'interpolate' for norm, T1, orig; 'nearest'\
            for aseg; 'surface' for surfaces.
        cross: If '1', input is from cross sectionals; default is to use\
            longitudinal directories.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = map_to_base_params(
        baseid=baseid,
        tpid=tpid,
        input_image=input_image,
        resample_type=resample_type,
        cross=cross,
    )
    return map_to_base_execute(params, runner)

__all__ = [
    "MapToBase",
    "MapToBaseOutputs",
    "MAP_TO_BASE_METADATA",
    "map_to_base_cargs",
    "map_to_base_outputs",
    "map_to_base_params",
    "map_to_base_execute",
    "map_to_base_validate",
    "map_to_base",
]