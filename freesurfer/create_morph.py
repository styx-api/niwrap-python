# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CREATE_MORPH_METADATA = Metadata(
    id="createMorph",
    name="createMorph",
    package="freesurfer",
)

CreateMorph = typing.TypedDict(
    "CreateMorph",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/createMorph"]],
        "input_transforms": list[str],
        "output_transform": str,
        "template": typing.NotRequired[InputPathType],
        "subject": typing.NotRequired[InputPathType],
        "debug_coordinates": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class CreateMorphOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_transform_file: OutputPathType
    """The resulting output transform file in tm3d format."""

def create_morph_params(
    input_transforms: list[str],
    output_transform: str,
    template: InputPathType | None = None,
    subject: InputPathType | None = None,
    debug_coordinates: list[float] | None = None,
) -> CreateMorph:
    """
    Build parameters.

    Args:
        input_transforms: Input transforms, must specify type (affine, volume,\
            morph, mesh, gcam) with filename.
        output_transform: Output transform file in tm3d format.
        template: Template volume for geometry. Required if a gcam is present.
        subject: Subject volume for geometry.
        debug_coordinates: Coordinates for debugging purposes. Requires three\
            integer values.

    Returns:
        Parameter dictionary.
    """
    params: CreateMorph = {
        "@type": "freesurfer/createMorph",
        "input_transforms": input_transforms,
        "output_transform": output_transform,
    }
    if template is not None:
        params["template"] = template
    if subject is not None:
        params["subject"] = subject
    if debug_coordinates is not None:
        params["debug_coordinates"] = debug_coordinates
    return params

def create_morph_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CreateMorph."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_transforms", None) is None:
        raise StyxValidationError("`input_transforms` must not be None")
    if not isinstance(params["input_transforms"], list):
        raise StyxValidationError(f'`input_transforms` has the wrong type: Received `{type(params["input_transforms"])}` expected `list[str]`')
    for e in params["input_transforms"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_transforms` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_transform", None) is None:
        raise StyxValidationError("`output_transform` must not be None")
    if not isinstance(params["output_transform"], str):
        raise StyxValidationError(f'`output_transform` has the wrong type: Received `{type(params["output_transform"])}` expected `str`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], (pathlib.Path, str)):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `InputPathType`')
    if params.get("debug_coordinates", None) is not None:
        if not isinstance(params["debug_coordinates"], list):
            raise StyxValidationError(f'`debug_coordinates` has the wrong type: Received `{type(params["debug_coordinates"])}` expected `list[float]`')
        if not (3 <= len(params["debug_coordinates"]) <= 3):
            raise StyxValidationError("Parameter `debug_coordinates` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["debug_coordinates"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`debug_coordinates` has the wrong type: Received `{type(e_2)}` expected `list[float]`')

def create_morph_cargs(params: CreateMorph, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("createMorph")
    cargs.append("--in")
    for item0 in params["input_transforms"]:
        cargs.append(item0)
    cargs.append("--out")
    cargs.append(params["output_transform"])
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("--template")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("subject")
    if v_1 is not None:
        cargs.append("--subject")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("debug_coordinates")
    if v_2 is not None:
        cargs.append("--dbg")
        for item1 in v_2:
            cargs.append(str(item1))
    return cargs

def create_morph_outputs(params: CreateMorph, execution: Execution) -> CreateMorphOutputs:
    """Build the CreateMorphOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_transform_file_v: OutputPathType = execution.output_file(params["output_transform"])
    return CreateMorphOutputs(
        root=root_v,
        output_transform_file=output_transform_file_v,
    )

def create_morph_execute(params: CreateMorph, runner: Runner | None = None) -> CreateMorphOutputs:
    """
    createMorph

    Tool to create morphological transformations using specified input transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    create_morph_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CREATE_MORPH_METADATA)
    execution.params(params)
    args = create_morph_cargs(params, execution)
    out = create_morph_outputs(params, execution)
    execution.run(args)
    return out

def create_morph(
    input_transforms: list[str],
    output_transform: str,
    template: InputPathType | None = None,
    subject: InputPathType | None = None,
    debug_coordinates: list[float] | None = None,
    runner: Runner | None = None,
) -> CreateMorphOutputs:
    """
    createMorph

    Tool to create morphological transformations using specified input transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_transforms: Input transforms, must specify type (affine, volume,\
            morph, mesh, gcam) with filename.
        output_transform: Output transform file in tm3d format.
        template: Template volume for geometry. Required if a gcam is present.
        subject: Subject volume for geometry.
        debug_coordinates: Coordinates for debugging purposes. Requires three\
            integer values.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = create_morph_params(
        input_transforms=input_transforms,
        output_transform=output_transform,
        template=template,
        subject=subject,
        debug_coordinates=debug_coordinates,
    )
    return create_morph_execute(params, runner)

__all__ = [
    "CreateMorph",
    "CreateMorphOutputs",
    "CREATE_MORPH_METADATA",
    "create_morph_cargs",
    "create_morph_outputs",
    "create_morph_params",
    "create_morph_execute",
    "create_morph_validate",
    "create_morph",
]