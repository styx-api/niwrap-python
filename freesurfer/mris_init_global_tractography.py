# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_INIT_GLOBAL_TRACTOGRAPHY_METADATA = Metadata(
    id="mris_init_global_tractography",
    name="mris_init_global_tractography",
    package="freesurfer",
)

MrisInitGlobalTractography = typing.TypedDict(
    "MrisInitGlobalTractography",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_init_global_tractography"]],
        "subject": str,
        "parcellation": str,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MrisInitGlobalTractographyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_init_global_tractography_params(
    subject: str,
    parcellation: str,
    output_volume: str,
) -> MrisInitGlobalTractography:
    """
    Build parameters.

    Args:
        subject: The subject on which to perform tractography.
        parcellation: The parcellation to use for tractography.
        output_volume: Output volume of the initialized global tractography.

    Returns:
        Parameter dictionary.
    """
    params: MrisInitGlobalTractography = {
        "@type": "freesurfer/mris_init_global_tractography",
        "subject": subject,
        "parcellation": parcellation,
        "output_volume": output_volume,
    }
    return params

def mris_init_global_tractography_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisInitGlobalTractography."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("parcellation", None) is None:
        raise StyxValidationError("`parcellation` must not be None")
    if not isinstance(params["parcellation"], str):
        raise StyxValidationError(f'`parcellation` has the wrong type: Received `{type(params["parcellation"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mris_init_global_tractography_cargs(params: MrisInitGlobalTractography, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_init_global_tractography")
    cargs.append(params["subject"])
    cargs.append(params["parcellation"])
    cargs.append(params["output_volume"])
    return cargs

def mris_init_global_tractography_outputs(params: MrisInitGlobalTractography, execution: Execution) -> MrisInitGlobalTractographyOutputs:
    """Build the MrisInitGlobalTractographyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisInitGlobalTractographyOutputs(
        root=root_v,
    )

def mris_init_global_tractography_execute(params: MrisInitGlobalTractography, runner: Runner | None = None) -> MrisInitGlobalTractographyOutputs:
    """
    mris_init_global_tractography

    Initializes global tractography for a given subject and parcellation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_init_global_tractography_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_INIT_GLOBAL_TRACTOGRAPHY_METADATA)
    execution.params(params)
    args = mris_init_global_tractography_cargs(params, execution)
    out = mris_init_global_tractography_outputs(params, execution)
    execution.run(args)
    return out

def mris_init_global_tractography(
    subject: str,
    parcellation: str,
    output_volume: str,
    runner: Runner | None = None,
) -> MrisInitGlobalTractographyOutputs:
    """
    mris_init_global_tractography

    Initializes global tractography for a given subject and parcellation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject on which to perform tractography.
        parcellation: The parcellation to use for tractography.
        output_volume: Output volume of the initialized global tractography.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_init_global_tractography_params(
        subject=subject,
        parcellation=parcellation,
        output_volume=output_volume,
    )
    return mris_init_global_tractography_execute(params, runner)

__all__ = [
    "MrisInitGlobalTractography",
    "MrisInitGlobalTractographyOutputs",
    "MRIS_INIT_GLOBAL_TRACTOGRAPHY_METADATA",
    "mris_init_global_tractography_cargs",
    "mris_init_global_tractography_outputs",
    "mris_init_global_tractography_params",
    "mris_init_global_tractography_execute",
    "mris_init_global_tractography_validate",
    "mris_init_global_tractography",
]