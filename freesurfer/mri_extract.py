# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EXTRACT_METADATA = Metadata(
    id="mri_extract",
    name="mri_extract",
    package="freesurfer",
)

MriExtract = typing.TypedDict(
    "MriExtract",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_extract"]],
        "like_template": typing.NotRequired[InputPathType],
        "src_volume": InputPathType,
        "dst_volume": InputPathType,
        "coordinates": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class MriExtractOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_extracted_volume: OutputPathType
    """Output file with extracted MRI data."""

def mri_extract_params(
    src_volume: InputPathType,
    dst_volume: InputPathType,
    like_template: InputPathType | None = None,
    coordinates: list[float] | None = None,
) -> MriExtract:
    """
    Build parameters.

    Args:
        src_volume: Source MRI volume file from which data will be extracted.
        dst_volume: The destination file where the extracted data will be saved.
        like_template: Extract region like the given template volume.
        coordinates: Coordinates and size of the extraction box: x0 y0 z0 dx dy dz.

    Returns:
        Parameter dictionary.
    """
    params: MriExtract = {
        "@type": "freesurfer/mri_extract",
        "src_volume": src_volume,
        "dst_volume": dst_volume,
    }
    if like_template is not None:
        params["like_template"] = like_template
    if coordinates is not None:
        params["coordinates"] = coordinates
    return params

def mri_extract_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriExtract."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("like_template", None) is not None:
        if not isinstance(params["like_template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`like_template` has the wrong type: Received `{type(params["like_template"])}` expected `InputPathType`')
    if params.get("src_volume", None) is None:
        raise StyxValidationError("`src_volume` must not be None")
    if not isinstance(params["src_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`src_volume` has the wrong type: Received `{type(params["src_volume"])}` expected `InputPathType`')
    if params.get("dst_volume", None) is None:
        raise StyxValidationError("`dst_volume` must not be None")
    if not isinstance(params["dst_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dst_volume` has the wrong type: Received `{type(params["dst_volume"])}` expected `InputPathType`')
    if params.get("coordinates", None) is not None:
        if not isinstance(params["coordinates"], list):
            raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(params["coordinates"])}` expected `list[float]`')
        if not (6 <= len(params["coordinates"]) <= 6):
            raise StyxValidationError("Parameter `coordinates` must contain between 6 and 6 elements (inclusive)")
        for e in params["coordinates"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(e)}` expected `list[float]`')

def mri_extract_cargs(params: MriExtract, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_extract")
    v_0 = params.get("like_template")
    if v_0 is not None:
        cargs.append("-like")
        cargs.append(execution.input_file(v_0))
    cargs.append(execution.input_file(params["src_volume"]))
    cargs.append(execution.input_file(params["dst_volume"]))
    v_1 = params.get("coordinates")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append(str(item0))
    return cargs

def mri_extract_outputs(params: MriExtract, execution: Execution) -> MriExtractOutputs:
    """Build the MriExtractOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_extracted_volume_v: OutputPathType = execution.output_file(params["dst_volume"])
    return MriExtractOutputs(
        root=root_v,
        output_extracted_volume=output_extracted_volume_v,
    )

def mri_extract_execute(params: MriExtract, runner: Runner | None = None) -> MriExtractOutputs:
    """
    mri_extract

    MRI data extraction tool for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_extract_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EXTRACT_METADATA)
    execution.params(params)
    args = mri_extract_cargs(params, execution)
    out = mri_extract_outputs(params, execution)
    execution.run(args)
    return out

def mri_extract(
    src_volume: InputPathType,
    dst_volume: InputPathType,
    like_template: InputPathType | None = None,
    coordinates: list[float] | None = None,
    runner: Runner | None = None,
) -> MriExtractOutputs:
    """
    mri_extract

    MRI data extraction tool for FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        src_volume: Source MRI volume file from which data will be extracted.
        dst_volume: The destination file where the extracted data will be saved.
        like_template: Extract region like the given template volume.
        coordinates: Coordinates and size of the extraction box: x0 y0 z0 dx dy dz.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_extract_params(
        src_volume=src_volume,
        dst_volume=dst_volume,
        like_template=like_template,
        coordinates=coordinates,
    )
    return mri_extract_execute(params, runner)

__all__ = [
    "MriExtract",
    "MriExtractOutputs",
    "MRI_EXTRACT_METADATA",
    "mri_extract_cargs",
    "mri_extract_outputs",
    "mri_extract_params",
    "mri_extract_execute",
    "mri_extract_validate",
    "mri_extract",
]