# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL2PATCH_METADATA = Metadata(
    id="label2patch",
    name="label2patch",
    package="freesurfer",
)

Label2patch = typing.TypedDict(
    "Label2patch",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label2patch"]],
        "subject_name": str,
        "hemisphere": str,
        "label_file": InputPathType,
        "output_patch": str,
        "dilate": typing.NotRequired[float],
        "erode": typing.NotRequired[float],
        "close": typing.NotRequired[float],
        "subjects_dir": typing.NotRequired[str],
        "surface_name": typing.NotRequired[str],
        "write_surface": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Label2patchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def label2patch_params(
    subject_name: str,
    hemisphere: str,
    label_file: InputPathType,
    output_patch: str,
    dilate: float | None = None,
    erode: float | None = None,
    close: float | None = None,
    subjects_dir: str | None = None,
    surface_name: str | None = None,
    write_surface: bool = False,
) -> Label2patch:
    """
    Build parameters.

    Args:
        subject_name: Subject name
        hemisphere: Hemisphere (e.g. lh or rh)
        label_file: Label file name
        output_patch: Output patch file
        dilate: Dilate the label n times before creating the patch
        erode: Erode the label n times before creating the patch
        close: Close the label n times before creating the patch
        subjects_dir: Use path as the SUBJECTS_DIR instead of environment
        surface_name: Use name as the surface (default 'inflated')
        write_surface: Write output to a surface file (not a patch). Use .stl in\
            filename to only write the mesh covered by the label, saving it in FS\
            format will save full surface

    Returns:
        Parameter dictionary.
    """
    params: Label2patch = {
        "@type": "freesurfer/label2patch",
        "subject_name": subject_name,
        "hemisphere": hemisphere,
        "label_file": label_file,
        "output_patch": output_patch,
        "write_surface": write_surface,
    }
    if dilate is not None:
        params["dilate"] = dilate
    if erode is not None:
        params["erode"] = erode
    if close is not None:
        params["close"] = close
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if surface_name is not None:
        params["surface_name"] = surface_name
    return params

def label2patch_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Label2patch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("label_file", None) is None:
        raise StyxValidationError("`label_file` must not be None")
    if not isinstance(params["label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("output_patch", None) is None:
        raise StyxValidationError("`output_patch` must not be None")
    if not isinstance(params["output_patch"], str):
        raise StyxValidationError(f'`output_patch` has the wrong type: Received `{type(params["output_patch"])}` expected `str`')
    if params.get("dilate", None) is not None:
        if not isinstance(params["dilate"], (float, int)):
            raise StyxValidationError(f'`dilate` has the wrong type: Received `{type(params["dilate"])}` expected `float`')
    if params.get("erode", None) is not None:
        if not isinstance(params["erode"], (float, int)):
            raise StyxValidationError(f'`erode` has the wrong type: Received `{type(params["erode"])}` expected `float`')
    if params.get("close", None) is not None:
        if not isinstance(params["close"], (float, int)):
            raise StyxValidationError(f'`close` has the wrong type: Received `{type(params["close"])}` expected `float`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("surface_name", None) is not None:
        if not isinstance(params["surface_name"], str):
            raise StyxValidationError(f'`surface_name` has the wrong type: Received `{type(params["surface_name"])}` expected `str`')
    if params.get("write_surface", None) is not None:
        if not isinstance(params["write_surface"], bool):
            raise StyxValidationError(f'`write_surface` has the wrong type: Received `{type(params["write_surface"])}` expected `bool`')

def label2patch_cargs(params: Label2patch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label2patch")
    cargs.append(params["subject_name"])
    cargs.append(params["hemisphere"])
    cargs.append(execution.input_file(params["label_file"]))
    cargs.append(params["output_patch"])
    v_0 = params.get("dilate")
    if v_0 is not None:
        cargs.append("-dilate")
        cargs.append(str(v_0))
    v_1 = params.get("erode")
    if v_1 is not None:
        cargs.append("-erode")
        cargs.append(str(v_1))
    v_2 = params.get("close")
    if v_2 is not None:
        cargs.append("-close")
        cargs.append(str(v_2))
    v_3 = params.get("subjects_dir")
    if v_3 is not None:
        cargs.append("-sdir")
        cargs.append(v_3)
    v_4 = params.get("surface_name")
    if v_4 is not None:
        cargs.append("-surf")
        cargs.append(v_4)
    if params.get("write_surface"):
        cargs.append("-writesurf")
    return cargs

def label2patch_outputs(params: Label2patch, execution: Execution) -> Label2patchOutputs:
    """Build the Label2patchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Label2patchOutputs(
        root=root_v,
    )

def label2patch_execute(params: Label2patch, runner: Runner | None = None) -> Label2patchOutputs:
    """
    label2patch

    Utility to create patches from label files in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label2patch_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL2PATCH_METADATA)
    execution.params(params)
    args = label2patch_cargs(params, execution)
    out = label2patch_outputs(params, execution)
    execution.run(args)
    return out

def label2patch(
    subject_name: str,
    hemisphere: str,
    label_file: InputPathType,
    output_patch: str,
    dilate: float | None = None,
    erode: float | None = None,
    close: float | None = None,
    subjects_dir: str | None = None,
    surface_name: str | None = None,
    write_surface: bool = False,
    runner: Runner | None = None,
) -> Label2patchOutputs:
    """
    label2patch

    Utility to create patches from label files in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name
        hemisphere: Hemisphere (e.g. lh or rh)
        label_file: Label file name
        output_patch: Output patch file
        dilate: Dilate the label n times before creating the patch
        erode: Erode the label n times before creating the patch
        close: Close the label n times before creating the patch
        subjects_dir: Use path as the SUBJECTS_DIR instead of environment
        surface_name: Use name as the surface (default 'inflated')
        write_surface: Write output to a surface file (not a patch). Use .stl in\
            filename to only write the mesh covered by the label, saving it in FS\
            format will save full surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label2patch_params(
        subject_name=subject_name,
        hemisphere=hemisphere,
        label_file=label_file,
        output_patch=output_patch,
        dilate=dilate,
        erode=erode,
        close=close,
        subjects_dir=subjects_dir,
        surface_name=surface_name,
        write_surface=write_surface,
    )
    return label2patch_execute(params, runner)

__all__ = [
    "Label2patch",
    "Label2patchOutputs",
    "LABEL2PATCH_METADATA",
    "label2patch_cargs",
    "label2patch_outputs",
    "label2patch_params",
    "label2patch_execute",
    "label2patch_validate",
    "label2patch",
]