# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_DISTANCE_TRANSFORM_METADATA = Metadata(
    id="mris_distance_transform",
    name="mris_distance_transform",
    package="freesurfer",
)

MrisDistanceTransformMode = typing.Literal["signed"] | typing.Literal["unsigned"] | typing.Literal["outside"]

MrisDistanceTransform = typing.TypedDict(
    "MrisDistanceTransform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_distance_transform"]],
        "surface": InputPathType,
        "label": InputPathType,
        "mode": MrisDistanceTransformMode,
        "output_file": str,
        "anterior": typing.NotRequired[float],
        "posterior": typing.NotRequired[float],
        "divide": typing.NotRequired[float],
        "olabel": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisDistanceTransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_file: OutputPathType
    """Resulting file after applying the distance transform."""

def mris_distance_transform_params(
    surface: InputPathType,
    label: InputPathType,
    mode: MrisDistanceTransformMode,
    output_file: str,
    anterior: float | None = None,
    posterior: float | None = None,
    divide: float | None = None,
    olabel: bool = False,
) -> MrisDistanceTransform:
    """
    Build parameters.

    Args:
        surface: Surface file for processing.
        label: Label file to apply distance transform.
        mode: Mode of the distance transform, can be 'signed', 'unsigned', or\
            'outside'.
        output_file: Output file for the distance transform results.
        anterior: Only use anteriormost <dist> portion of the label.
        posterior: Only use posteriormost <dist> portion of the label.
        divide: Divide label into <n> units along primary eigendirection.
        olabel: Output label subdivisions.

    Returns:
        Parameter dictionary.
    """
    params: MrisDistanceTransform = {
        "@type": "freesurfer/mris_distance_transform",
        "surface": surface,
        "label": label,
        "mode": mode,
        "output_file": output_file,
        "olabel": olabel,
    }
    if anterior is not None:
        params["anterior"] = anterior
    if posterior is not None:
        params["posterior"] = posterior
    if divide is not None:
        params["divide"] = divide
    return params

def mris_distance_transform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisDistanceTransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("mode", None) is None:
        raise StyxValidationError("`mode` must not be None")
    if not isinstance(params["mode"], str):
        raise StyxValidationError(f'`mode` has the wrong type: Received `{type(params["mode"])}` expected `MrisDistanceTransformMode`')
    if params["mode"] not in ["signed", "unsigned", "outside"]:
        raise StyxValidationError("Parameter `mode` must be one of [\"signed\", \"unsigned\", \"outside\"]")
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("anterior", None) is not None:
        if not isinstance(params["anterior"], (float, int)):
            raise StyxValidationError(f'`anterior` has the wrong type: Received `{type(params["anterior"])}` expected `float`')
    if params.get("posterior", None) is not None:
        if not isinstance(params["posterior"], (float, int)):
            raise StyxValidationError(f'`posterior` has the wrong type: Received `{type(params["posterior"])}` expected `float`')
    if params.get("divide", None) is not None:
        if not isinstance(params["divide"], (float, int)):
            raise StyxValidationError(f'`divide` has the wrong type: Received `{type(params["divide"])}` expected `float`')
    if params.get("olabel", None) is not None:
        if not isinstance(params["olabel"], bool):
            raise StyxValidationError(f'`olabel` has the wrong type: Received `{type(params["olabel"])}` expected `bool`')

def mris_distance_transform_cargs(params: MrisDistanceTransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_distance_transform")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["label"]))
    cargs.append(str(params["mode"]))
    cargs.append(params["output_file"])
    v_0 = params.get("anterior")
    if v_0 is not None:
        cargs.append("-anterior")
        cargs.append(str(v_0))
    v_1 = params.get("posterior")
    if v_1 is not None:
        cargs.append("-posterior")
        cargs.append(str(v_1))
    v_2 = params.get("divide")
    if v_2 is not None:
        cargs.append("-divide")
        cargs.append(str(v_2))
    if params.get("olabel"):
        cargs.append("-olabel")
    return cargs

def mris_distance_transform_outputs(params: MrisDistanceTransform, execution: Execution) -> MrisDistanceTransformOutputs:
    """Build the MrisDistanceTransformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_file_v: OutputPathType = execution.output_file(params["output_file"])
    return MrisDistanceTransformOutputs(
        root=root_v,
        result_file=result_file_v,
    )

def mris_distance_transform_execute(params: MrisDistanceTransform, runner: Runner | None = None) -> MrisDistanceTransformOutputs:
    """
    mris_distance_transform

    Computes the distance transform of a label on the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_distance_transform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_DISTANCE_TRANSFORM_METADATA)
    execution.params(params)
    args = mris_distance_transform_cargs(params, execution)
    out = mris_distance_transform_outputs(params, execution)
    execution.run(args)
    return out

def mris_distance_transform(
    surface: InputPathType,
    label: InputPathType,
    mode: MrisDistanceTransformMode,
    output_file: str,
    anterior: float | None = None,
    posterior: float | None = None,
    divide: float | None = None,
    olabel: bool = False,
    runner: Runner | None = None,
) -> MrisDistanceTransformOutputs:
    """
    mris_distance_transform

    Computes the distance transform of a label on the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Surface file for processing.
        label: Label file to apply distance transform.
        mode: Mode of the distance transform, can be 'signed', 'unsigned', or\
            'outside'.
        output_file: Output file for the distance transform results.
        anterior: Only use anteriormost <dist> portion of the label.
        posterior: Only use posteriormost <dist> portion of the label.
        divide: Divide label into <n> units along primary eigendirection.
        olabel: Output label subdivisions.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_distance_transform_params(
        surface=surface,
        label=label,
        mode=mode,
        output_file=output_file,
        anterior=anterior,
        posterior=posterior,
        divide=divide,
        olabel=olabel,
    )
    return mris_distance_transform_execute(params, runner)

__all__ = [
    "MrisDistanceTransform",
    "MrisDistanceTransformOutputs",
    "MRIS_DISTANCE_TRANSFORM_METADATA",
    "mris_distance_transform_cargs",
    "mris_distance_transform_outputs",
    "mris_distance_transform_params",
    "mris_distance_transform_execute",
    "mris_distance_transform_validate",
    "mris_distance_transform",
]