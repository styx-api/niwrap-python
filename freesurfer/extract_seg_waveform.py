# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EXTRACT_SEG_WAVEFORM_METADATA = Metadata(
    id="extract_seg_waveform",
    name="extract_seg_waveform",
    package="freesurfer",
)

ExtractSegWaveform = typing.TypedDict(
    "ExtractSegWaveform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/extract_seg_waveform"]],
        "seg_file": InputPathType,
        "seg_indices": list[float],
        "input_volume": InputPathType,
        "reg_file": InputPathType,
        "vsm_file": typing.NotRequired[InputPathType],
        "regheader_flag": typing.NotRequired[bool],
        "demean_flag": typing.NotRequired[bool],
        "output_file": str,
    },
)

@dataclasses.dataclass
class ExtractSegWaveformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def extract_seg_waveform_params(
    seg_file: InputPathType,
    seg_indices: list[float],
    input_volume: InputPathType,
    reg_file: InputPathType,
    output_file: str,
    vsm_file: InputPathType | None = None,
    regheader_flag: bool = False,
    demean_flag: bool = False,
) -> ExtractSegWaveform:
    """
    Build parameters.

    Args:
        seg_file: Segmentation file
        seg_indices: Segmentation indices, one or more indices can be specified
        input_volume: Input volume
        reg_file: Registration file (.lta)
        output_file: Output waveform file
        vsm_file: Voxel shift map for B0 distortion correction
        regheader_flag: Uses the header information in the registration file
        demean_flag: Remove mean, first, and second order trends

    Returns:
        Parameter dictionary.
    """
    params: ExtractSegWaveform = {
        "@type": "freesurfer/extract_seg_waveform",
        "seg_file": seg_file,
        "seg_indices": seg_indices,
        "input_volume": input_volume,
        "reg_file": reg_file,
        "output_file": output_file,
        "regheader_flag": regheader_flag,
        "demean_flag": demean_flag,
    }
    if vsm_file is not None:
        params["vsm_file"] = vsm_file
    return params

def extract_seg_waveform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ExtractSegWaveform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("seg_file", None) is None:
        raise StyxValidationError("`seg_file` must not be None")
    if not isinstance(params["seg_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`seg_file` has the wrong type: Received `{type(params["seg_file"])}` expected `InputPathType`')
    if params.get("seg_indices", None) is None:
        raise StyxValidationError("`seg_indices` must not be None")
    if not isinstance(params["seg_indices"], list):
        raise StyxValidationError(f'`seg_indices` has the wrong type: Received `{type(params["seg_indices"])}` expected `list[float]`')
    for e in params["seg_indices"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`seg_indices` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("reg_file", None) is None:
        raise StyxValidationError("`reg_file` must not be None")
    if not isinstance(params["reg_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reg_file` has the wrong type: Received `{type(params["reg_file"])}` expected `InputPathType`')
    if params.get("vsm_file", None) is not None:
        if not isinstance(params["vsm_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vsm_file` has the wrong type: Received `{type(params["vsm_file"])}` expected `InputPathType`')
    if params.get("regheader_flag", None) is not None:
        if not isinstance(params["regheader_flag"], bool):
            raise StyxValidationError(f'`regheader_flag` has the wrong type: Received `{type(params["regheader_flag"])}` expected `bool`')
    if params.get("demean_flag", None) is not None:
        if not isinstance(params["demean_flag"], bool):
            raise StyxValidationError(f'`demean_flag` has the wrong type: Received `{type(params["demean_flag"])}` expected `bool`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def extract_seg_waveform_cargs(params: ExtractSegWaveform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("extract_seg_waveform")
    cargs.append("--seg")
    cargs.append(execution.input_file(params["seg_file"]))
    cargs.append("--id")
    for item0 in params["seg_indices"]:
        cargs.append(str(item0))
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--reg")
    cargs.append(execution.input_file(params["reg_file"]))
    v_0 = params.get("vsm_file")
    if v_0 is not None:
        cargs.append("--vsm")
        cargs.append(execution.input_file(v_0))
    if params.get("regheader_flag"):
        cargs.append("--regheader")
    if params.get("demean_flag"):
        cargs.append("--demean")
    cargs.append("--o")
    cargs.append(params["output_file"])
    return cargs

def extract_seg_waveform_outputs(params: ExtractSegWaveform, execution: Execution) -> ExtractSegWaveformOutputs:
    """Build the ExtractSegWaveformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ExtractSegWaveformOutputs(
        root=root_v,
    )

def extract_seg_waveform_execute(params: ExtractSegWaveform, runner: Runner | None = None) -> ExtractSegWaveformOutputs:
    """
    extract_seg_waveform

    This program extracts an average waveform from an input volume where the average is computed over the voxels in the given segmentation indices. The input volume is mapped to the space of the segmentation given the registration, and if a voxel shift map (VSM) is supplied, it is applied simultaneously as part of the transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    extract_seg_waveform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EXTRACT_SEG_WAVEFORM_METADATA)
    execution.params(params)
    args = extract_seg_waveform_cargs(params, execution)
    out = extract_seg_waveform_outputs(params, execution)
    execution.run(args)
    return out

def extract_seg_waveform(
    seg_file: InputPathType,
    seg_indices: list[float],
    input_volume: InputPathType,
    reg_file: InputPathType,
    output_file: str,
    vsm_file: InputPathType | None = None,
    regheader_flag: bool = False,
    demean_flag: bool = False,
    runner: Runner | None = None,
) -> ExtractSegWaveformOutputs:
    """
    extract_seg_waveform

    This program extracts an average waveform from an input volume where the average is computed over the voxels in the given segmentation indices. The input volume is mapped to the space of the segmentation given the registration, and if a voxel shift map (VSM) is supplied, it is applied simultaneously as part of the transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        seg_file: Segmentation file
        seg_indices: Segmentation indices, one or more indices can be specified
        input_volume: Input volume
        reg_file: Registration file (.lta)
        output_file: Output waveform file
        vsm_file: Voxel shift map for B0 distortion correction
        regheader_flag: Uses the header information in the registration file
        demean_flag: Remove mean, first, and second order trends
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = extract_seg_waveform_params(
        seg_file=seg_file,
        seg_indices=seg_indices,
        input_volume=input_volume,
        reg_file=reg_file,
        output_file=output_file,
        vsm_file=vsm_file,
        regheader_flag=regheader_flag,
        demean_flag=demean_flag,
    )
    return extract_seg_waveform_execute(params, runner)

__all__ = [
    "ExtractSegWaveform",
    "ExtractSegWaveformOutputs",
    "EXTRACT_SEG_WAVEFORM_METADATA",
    "extract_seg_waveform_cargs",
    "extract_seg_waveform_outputs",
    "extract_seg_waveform_params",
    "extract_seg_waveform_execute",
    "extract_seg_waveform_validate",
    "extract_seg_waveform",
]