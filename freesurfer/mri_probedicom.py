# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PROBEDICOM_METADATA = Metadata(
    id="mri_probedicom",
    name="mri_probedicom",
    package="freesurfer",
)

MriProbedicom = typing.TypedDict(
    "MriProbedicom",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_probedicom"]],
        "dicom_file": InputPathType,
        "option1": typing.NotRequired[str],
        "option2": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriProbedicomOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Text file containing extracted information from DICOM."""

def mri_probedicom_params(
    dicom_file: InputPathType,
    option1: str | None = None,
    option2: str | None = None,
) -> MriProbedicom:
    """
    Build parameters.

    Args:
        dicom_file: DICOM file to be probed.
        option1: Description for option1
        option2: Description for option2

    Returns:
        Parameter dictionary.
    """
    params: MriProbedicom = {
        "@type": "freesurfer/mri_probedicom",
        "dicom_file": dicom_file,
    }
    if option1 is not None:
        params["option1"] = option1
    if option2 is not None:
        params["option2"] = option2
    return params

def mri_probedicom_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriProbedicom."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dicom_file", None) is None:
        raise StyxValidationError("`dicom_file` must not be None")
    if not isinstance(params["dicom_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dicom_file` has the wrong type: Received `{type(params["dicom_file"])}` expected `InputPathType`')
    if params.get("option1", None) is not None:
        if not isinstance(params["option1"], str):
            raise StyxValidationError(f'`option1` has the wrong type: Received `{type(params["option1"])}` expected `str`')
    if params.get("option2", None) is not None:
        if not isinstance(params["option2"], str):
            raise StyxValidationError(f'`option2` has the wrong type: Received `{type(params["option2"])}` expected `str`')

def mri_probedicom_cargs(params: MriProbedicom, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_probedicom")
    cargs.append(execution.input_file(params["dicom_file"]))
    v_0 = params.get("option1")
    if v_0 is not None:
        cargs.append("-option1")
        cargs.append(v_0)
    v_1 = params.get("option2")
    if v_1 is not None:
        cargs.append("-option2")
        cargs.append(v_1)
    return cargs

def mri_probedicom_outputs(params: MriProbedicom, execution: Execution) -> MriProbedicomOutputs:
    """Build the MriProbedicomOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("[OUTPUT].txt")
    return MriProbedicomOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_probedicom_execute(params: MriProbedicom, runner: Runner | None = None) -> MriProbedicomOutputs:
    """
    mri_probedicom

    Utility to probe DICOM files for information.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_probedicom_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PROBEDICOM_METADATA)
    execution.params(params)
    args = mri_probedicom_cargs(params, execution)
    out = mri_probedicom_outputs(params, execution)
    execution.run(args)
    return out

def mri_probedicom(
    dicom_file: InputPathType,
    option1: str | None = None,
    option2: str | None = None,
    runner: Runner | None = None,
) -> MriProbedicomOutputs:
    """
    mri_probedicom

    Utility to probe DICOM files for information.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dicom_file: DICOM file to be probed.
        option1: Description for option1
        option2: Description for option2
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_probedicom_params(
        dicom_file=dicom_file,
        option1=option1,
        option2=option2,
    )
    return mri_probedicom_execute(params, runner)

__all__ = [
    "MriProbedicom",
    "MriProbedicomOutputs",
    "MRI_PROBEDICOM_METADATA",
    "mri_probedicom_cargs",
    "mri_probedicom_outputs",
    "mri_probedicom_params",
    "mri_probedicom_execute",
    "mri_probedicom_validate",
    "mri_probedicom",
]