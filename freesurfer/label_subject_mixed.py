# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_SUBJECT_MIXED_METADATA = Metadata(
    id="label_subject_mixed",
    name="label_subject_mixed",
    package="freesurfer",
)

LabelSubjectMixed = typing.TypedDict(
    "LabelSubjectMixed",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label_subject_mixed"]],
        "brain_mask": InputPathType,
        "norm_volume": InputPathType,
        "transform": InputPathType,
        "gca_file": InputPathType,
        "aseg_output": str,
    },
)

@dataclasses.dataclass
class LabelSubjectMixedOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_aseg: OutputPathType
    """Labeled segmentation output"""

def label_subject_mixed_params(
    brain_mask: InputPathType,
    norm_volume: InputPathType,
    transform: InputPathType,
    gca_file: InputPathType,
    aseg_output: str,
) -> LabelSubjectMixed:
    """
    Build parameters.

    Args:
        brain_mask: Brain mask to be used for final labeling
        norm_volume: Normalized volume
        transform: Transform file in LTA format
        gca_file: Gaussian classifier array (GCA) file
        aseg_output: Output aseg file

    Returns:
        Parameter dictionary.
    """
    params: LabelSubjectMixed = {
        "@type": "freesurfer/label_subject_mixed",
        "brain_mask": brain_mask,
        "norm_volume": norm_volume,
        "transform": transform,
        "gca_file": gca_file,
        "aseg_output": aseg_output,
    }
    return params

def label_subject_mixed_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelSubjectMixed."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("brain_mask", None) is None:
        raise StyxValidationError("`brain_mask` must not be None")
    if not isinstance(params["brain_mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain_mask` has the wrong type: Received `{type(params["brain_mask"])}` expected `InputPathType`')
    if params.get("norm_volume", None) is None:
        raise StyxValidationError("`norm_volume` must not be None")
    if not isinstance(params["norm_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`norm_volume` has the wrong type: Received `{type(params["norm_volume"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("gca_file", None) is None:
        raise StyxValidationError("`gca_file` must not be None")
    if not isinstance(params["gca_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gca_file` has the wrong type: Received `{type(params["gca_file"])}` expected `InputPathType`')
    if params.get("aseg_output", None) is None:
        raise StyxValidationError("`aseg_output` must not be None")
    if not isinstance(params["aseg_output"], str):
        raise StyxValidationError(f'`aseg_output` has the wrong type: Received `{type(params["aseg_output"])}` expected `str`')

def label_subject_mixed_cargs(params: LabelSubjectMixed, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label_subject_mixed")
    cargs.append("-mask")
    cargs.append(execution.input_file(params["brain_mask"]))
    cargs.append(execution.input_file(params["norm_volume"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(execution.input_file(params["gca_file"]))
    cargs.append(params["aseg_output"])
    return cargs

def label_subject_mixed_outputs(params: LabelSubjectMixed, execution: Execution) -> LabelSubjectMixedOutputs:
    """Build the LabelSubjectMixedOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_aseg_v: OutputPathType = execution.output_file(params["aseg_output"])
    return LabelSubjectMixedOutputs(
        root=root_v,
        output_aseg=output_aseg_v,
    )

def label_subject_mixed_execute(params: LabelSubjectMixed, runner: Runner | None = None) -> LabelSubjectMixedOutputs:
    """
    label_subject_mixed

    Automatic labeling of brain regions using a Gaussian classifier array.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_subject_mixed_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_SUBJECT_MIXED_METADATA)
    execution.params(params)
    args = label_subject_mixed_cargs(params, execution)
    out = label_subject_mixed_outputs(params, execution)
    execution.run(args)
    return out

def label_subject_mixed(
    brain_mask: InputPathType,
    norm_volume: InputPathType,
    transform: InputPathType,
    gca_file: InputPathType,
    aseg_output: str,
    runner: Runner | None = None,
) -> LabelSubjectMixedOutputs:
    """
    label_subject_mixed

    Automatic labeling of brain regions using a Gaussian classifier array.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        brain_mask: Brain mask to be used for final labeling
        norm_volume: Normalized volume
        transform: Transform file in LTA format
        gca_file: Gaussian classifier array (GCA) file
        aseg_output: Output aseg file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_subject_mixed_params(
        brain_mask=brain_mask,
        norm_volume=norm_volume,
        transform=transform,
        gca_file=gca_file,
        aseg_output=aseg_output,
    )
    return label_subject_mixed_execute(params, runner)

__all__ = [
    "LabelSubjectMixed",
    "LabelSubjectMixedOutputs",
    "LABEL_SUBJECT_MIXED_METADATA",
    "label_subject_mixed_cargs",
    "label_subject_mixed_outputs",
    "label_subject_mixed_params",
    "label_subject_mixed_execute",
    "label_subject_mixed_validate",
    "label_subject_mixed",
]