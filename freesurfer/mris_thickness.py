# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_THICKNESS_METADATA = Metadata(
    id="mris_thickness",
    name="mris_thickness",
    package="freesurfer",
)

MrisThickness = typing.TypedDict(
    "MrisThickness",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_thickness"]],
        "subject_name": str,
        "hemi": str,
        "thickness_file": str,
        "max_threshold": typing.NotRequired[float],
        "fill_holes": typing.NotRequired[list[str]],
        "thickness_from_seg": typing.NotRequired[list[str]],
        "vector": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisThicknessOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_thickness_file: OutputPathType
    """Output curvature file containing thickness measurements."""

def mris_thickness_params(
    subject_name: str,
    hemi: str,
    thickness_file: str,
    max_threshold: float | None = None,
    fill_holes: list[str] | None = None,
    thickness_from_seg: list[str] | None = None,
    vector: bool = False,
) -> MrisThickness:
    """
    Build parameters.

    Args:
        subject_name: The subject name for processing.
        hemi: The hemisphere to process (e.g., lh or rh).
        thickness_file: Output file for thickness measurements.
        max_threshold: Use a maximum threshold for thickness (default is 5mm).
        fill_holes: Fill in thickness in holes in the cortex label using fsaverage\
            cortex label.
        thickness_from_seg: Compute thickness from segmentation. Requires the\
            following parameters: surf label, seg.mgz, dmaxmm, ddeltamm, and\
            output.mgz.
        vector: Compute the thickness using a variationally derived vector field.

    Returns:
        Parameter dictionary.
    """
    params: MrisThickness = {
        "@type": "freesurfer/mris_thickness",
        "subject_name": subject_name,
        "hemi": hemi,
        "thickness_file": thickness_file,
        "vector": vector,
    }
    if max_threshold is not None:
        params["max_threshold"] = max_threshold
    if fill_holes is not None:
        params["fill_holes"] = fill_holes
    if thickness_from_seg is not None:
        params["thickness_from_seg"] = thickness_from_seg
    return params

def mris_thickness_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisThickness."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("thickness_file", None) is None:
        raise StyxValidationError("`thickness_file` must not be None")
    if not isinstance(params["thickness_file"], str):
        raise StyxValidationError(f'`thickness_file` has the wrong type: Received `{type(params["thickness_file"])}` expected `str`')
    if params.get("max_threshold", None) is not None:
        if not isinstance(params["max_threshold"], (float, int)):
            raise StyxValidationError(f'`max_threshold` has the wrong type: Received `{type(params["max_threshold"])}` expected `float`')
    if params.get("fill_holes", None) is not None:
        if not isinstance(params["fill_holes"], list):
            raise StyxValidationError(f'`fill_holes` has the wrong type: Received `{type(params["fill_holes"])}` expected `list[str]`')
        if not (2 <= len(params["fill_holes"]) <= 2):
            raise StyxValidationError("Parameter `fill_holes` must contain between 2 and 2 elements (inclusive)")
        for e in params["fill_holes"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`fill_holes` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("thickness_from_seg", None) is not None:
        if not isinstance(params["thickness_from_seg"], list):
            raise StyxValidationError(f'`thickness_from_seg` has the wrong type: Received `{type(params["thickness_from_seg"])}` expected `list[str]`')
        if not (5 <= len(params["thickness_from_seg"]) <= 5):
            raise StyxValidationError("Parameter `thickness_from_seg` must contain between 5 and 5 elements (inclusive)")
        for e_2 in params["thickness_from_seg"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`thickness_from_seg` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("vector", None) is not None:
        if not isinstance(params["vector"], bool):
            raise StyxValidationError(f'`vector` has the wrong type: Received `{type(params["vector"])}` expected `bool`')

def mris_thickness_cargs(params: MrisThickness, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_thickness")
    cargs.append(params["subject_name"])
    cargs.append(params["hemi"])
    cargs.append(params["thickness_file"])
    v_0 = params.get("max_threshold")
    if v_0 is not None:
        cargs.append("-max")
        cargs.append(str(v_0))
    v_1 = params.get("fill_holes")
    if v_1 is not None:
        cargs.append("-fill_holes")
        for item0 in v_1:
            cargs.append(item0)
    v_2 = params.get("thickness_from_seg")
    if v_2 is not None:
        cargs.append("-thickness-from-seg")
        for item1 in v_2:
            cargs.append(item1)
    if params.get("vector"):
        cargs.append("-vector")
    return cargs

def mris_thickness_outputs(params: MrisThickness, execution: Execution) -> MrisThicknessOutputs:
    """Build the MrisThicknessOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_thickness_file_v: OutputPathType = execution.output_file(params["thickness_file"])
    return MrisThicknessOutputs(
        root=root_v,
        output_thickness_file=output_thickness_file_v,
    )

def mris_thickness_execute(params: MrisThickness, runner: Runner | None = None) -> MrisThicknessOutputs:
    """
    mris_thickness

    Measures the thickness of the cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_thickness_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_THICKNESS_METADATA)
    execution.params(params)
    args = mris_thickness_cargs(params, execution)
    out = mris_thickness_outputs(params, execution)
    execution.run(args)
    return out

def mris_thickness(
    subject_name: str,
    hemi: str,
    thickness_file: str,
    max_threshold: float | None = None,
    fill_holes: list[str] | None = None,
    thickness_from_seg: list[str] | None = None,
    vector: bool = False,
    runner: Runner | None = None,
) -> MrisThicknessOutputs:
    """
    mris_thickness

    Measures the thickness of the cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: The subject name for processing.
        hemi: The hemisphere to process (e.g., lh or rh).
        thickness_file: Output file for thickness measurements.
        max_threshold: Use a maximum threshold for thickness (default is 5mm).
        fill_holes: Fill in thickness in holes in the cortex label using fsaverage\
            cortex label.
        thickness_from_seg: Compute thickness from segmentation. Requires the\
            following parameters: surf label, seg.mgz, dmaxmm, ddeltamm, and\
            output.mgz.
        vector: Compute the thickness using a variationally derived vector field.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_thickness_params(
        subject_name=subject_name,
        hemi=hemi,
        thickness_file=thickness_file,
        max_threshold=max_threshold,
        fill_holes=fill_holes,
        thickness_from_seg=thickness_from_seg,
        vector=vector,
    )
    return mris_thickness_execute(params, runner)

__all__ = [
    "MrisThickness",
    "MrisThicknessOutputs",
    "MRIS_THICKNESS_METADATA",
    "mris_thickness_cargs",
    "mris_thickness_outputs",
    "mris_thickness_params",
    "mris_thickness_execute",
    "mris_thickness_validate",
    "mris_thickness",
]