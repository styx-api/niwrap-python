# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MAKE_FACE_PARCELLATION_METADATA = Metadata(
    id="mris_make_face_parcellation",
    name="mris_make_face_parcellation",
    package="freesurfer",
)

MrisMakeFaceParcellation = typing.TypedDict(
    "MrisMakeFaceParcellation",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_make_face_parcellation"]],
        "input_surface": InputPathType,
        "ico_file": InputPathType,
        "output_annot": str,
        "colortable": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MrisMakeFaceParcellationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    annot_file: OutputPathType
    """Generated annotation file based on the icosahedral face mapping."""

def mris_make_face_parcellation_params(
    input_surface: InputPathType,
    ico_file: InputPathType,
    output_annot: str,
    colortable: InputPathType | None = None,
) -> MrisMakeFaceParcellation:
    """
    Build parameters.

    Args:
        input_surface: Input surface file (e.g. lh.sphere or lh.sphere.reg)
        ico_file: Icosahedron file (e.g. ic3.tri)
        output_annot: Output annotation file (e.g. lh.ic3.annot)
        colortable: Color table file (e.g. colortable.txt)

    Returns:
        Parameter dictionary.
    """
    params: MrisMakeFaceParcellation = {
        "@type": "freesurfer/mris_make_face_parcellation",
        "input_surface": input_surface,
        "ico_file": ico_file,
        "output_annot": output_annot,
    }
    if colortable is not None:
        params["colortable"] = colortable
    return params

def mris_make_face_parcellation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMakeFaceParcellation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("ico_file", None) is None:
        raise StyxValidationError("`ico_file` must not be None")
    if not isinstance(params["ico_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ico_file` has the wrong type: Received `{type(params["ico_file"])}` expected `InputPathType`')
    if params.get("output_annot", None) is None:
        raise StyxValidationError("`output_annot` must not be None")
    if not isinstance(params["output_annot"], str):
        raise StyxValidationError(f'`output_annot` has the wrong type: Received `{type(params["output_annot"])}` expected `str`')
    if params.get("colortable", None) is not None:
        if not isinstance(params["colortable"], (pathlib.Path, str)):
            raise StyxValidationError(f'`colortable` has the wrong type: Received `{type(params["colortable"])}` expected `InputPathType`')

def mris_make_face_parcellation_cargs(params: MrisMakeFaceParcellation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_make_face_parcellation")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["ico_file"]))
    cargs.append(params["output_annot"])
    v_0 = params.get("colortable")
    if v_0 is not None:
        cargs.append("-ctab")
        cargs.append(execution.input_file(v_0))
    return cargs

def mris_make_face_parcellation_outputs(params: MrisMakeFaceParcellation, execution: Execution) -> MrisMakeFaceParcellationOutputs:
    """Build the MrisMakeFaceParcellationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    annot_file_v: OutputPathType = execution.output_file(params["output_annot"])
    return MrisMakeFaceParcellationOutputs(
        root=root_v,
        annot_file=annot_file_v,
    )

def mris_make_face_parcellation_execute(params: MrisMakeFaceParcellation, runner: Runner | None = None) -> MrisMakeFaceParcellationOutputs:
    """
    mris_make_face_parcellation

    Generates a parcellation based on which icosahedral face each vertex maps to.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_make_face_parcellation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MAKE_FACE_PARCELLATION_METADATA)
    execution.params(params)
    args = mris_make_face_parcellation_cargs(params, execution)
    out = mris_make_face_parcellation_outputs(params, execution)
    execution.run(args)
    return out

def mris_make_face_parcellation(
    input_surface: InputPathType,
    ico_file: InputPathType,
    output_annot: str,
    colortable: InputPathType | None = None,
    runner: Runner | None = None,
) -> MrisMakeFaceParcellationOutputs:
    """
    mris_make_face_parcellation

    Generates a parcellation based on which icosahedral face each vertex maps to.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file (e.g. lh.sphere or lh.sphere.reg)
        ico_file: Icosahedron file (e.g. ic3.tri)
        output_annot: Output annotation file (e.g. lh.ic3.annot)
        colortable: Color table file (e.g. colortable.txt)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_make_face_parcellation_params(
        input_surface=input_surface,
        ico_file=ico_file,
        output_annot=output_annot,
        colortable=colortable,
    )
    return mris_make_face_parcellation_execute(params, runner)

__all__ = [
    "MrisMakeFaceParcellation",
    "MrisMakeFaceParcellationOutputs",
    "MRIS_MAKE_FACE_PARCELLATION_METADATA",
    "mris_make_face_parcellation_cargs",
    "mris_make_face_parcellation_outputs",
    "mris_make_face_parcellation_params",
    "mris_make_face_parcellation_execute",
    "mris_make_face_parcellation_validate",
    "mris_make_face_parcellation",
]