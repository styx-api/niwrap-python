# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_POLV_METADATA = Metadata(
    id="mri_polv",
    name="mri_polv",
    package="freesurfer",
)

MriPolv = typing.TypedDict(
    "MriPolv",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_polv"]],
        "window_size": typing.NotRequired[float],
        "input_image": InputPathType,
        "output_image": InputPathType,
    },
)

@dataclasses.dataclass
class MriPolvOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_polv_params(
    input_image: InputPathType,
    output_image: InputPathType,
    window_size: float | None = None,
) -> MriPolv:
    """
    Build parameters.

    Args:
        input_image: The input image file for processing.
        output_image: The output image file specifying the plane of least variance.
        window_size: Specify the window size to be used in the calculation of the\
            central plane of least variance (default=5).

    Returns:
        Parameter dictionary.
    """
    params: MriPolv = {
        "@type": "freesurfer/mri_polv",
        "input_image": input_image,
        "output_image": output_image,
    }
    if window_size is not None:
        params["window_size"] = window_size
    return params

def mri_polv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriPolv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("window_size", None) is not None:
        if not isinstance(params["window_size"], (float, int)):
            raise StyxValidationError(f'`window_size` has the wrong type: Received `{type(params["window_size"])}` expected `float`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `InputPathType`')

def mri_polv_cargs(params: MriPolv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_polv")
    v_0 = params.get("window_size")
    if v_0 is not None:
        cargs.append("-w")
        cargs.append(str(v_0))
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(execution.input_file(params["output_image"]))
    return cargs

def mri_polv_outputs(params: MriPolv, execution: Execution) -> MriPolvOutputs:
    """Build the MriPolvOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriPolvOutputs(
        root=root_v,
    )

def mri_polv_execute(params: MriPolv, runner: Runner | None = None) -> MriPolvOutputs:
    """
    mri_polv

    Calculate an image specifying the plane of least variance at each point in the input image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_polv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_POLV_METADATA)
    execution.params(params)
    args = mri_polv_cargs(params, execution)
    out = mri_polv_outputs(params, execution)
    execution.run(args)
    return out

def mri_polv(
    input_image: InputPathType,
    output_image: InputPathType,
    window_size: float | None = None,
    runner: Runner | None = None,
) -> MriPolvOutputs:
    """
    mri_polv

    Calculate an image specifying the plane of least variance at each point in the input image.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: The input image file for processing.
        output_image: The output image file specifying the plane of least variance.
        window_size: Specify the window size to be used in the calculation of the\
            central plane of least variance (default=5).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_polv_params(
        input_image=input_image,
        output_image=output_image,
        window_size=window_size,
    )
    return mri_polv_execute(params, runner)

__all__ = [
    "MriPolv",
    "MriPolvOutputs",
    "MRI_POLV_METADATA",
    "mri_polv_cargs",
    "mri_polv_outputs",
    "mri_polv_params",
    "mri_polv_execute",
    "mri_polv_validate",
    "mri_polv",
]