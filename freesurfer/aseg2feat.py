# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ASEG2FEAT_METADATA = Metadata(
    id="aseg2feat",
    name="aseg2feat",
    package="freesurfer",
)

Aseg2feat = typing.TypedDict(
    "Aseg2feat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/aseg2feat"]],
        "feat": str,
        "featdirfile": typing.NotRequired[InputPathType],
        "seg": typing.NotRequired[str],
        "aparc_aseg": typing.NotRequired[bool],
        "svstats": typing.NotRequired[bool],
        "standard": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Aseg2featOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmentation_output: OutputPathType
    """Result segmentation in FEAT functional space."""
    stats_output: OutputPathType
    """Directory where results are saved when using --svstats."""

def aseg2feat_params(
    feat: str,
    featdirfile: InputPathType | None = None,
    seg: str | None = None,
    aparc_aseg: bool = False,
    svstats: bool = False,
    standard: bool = False,
    debug: bool = False,
    help: bool = False,
    version: bool = False,
) -> Aseg2feat:
    """
    Build parameters.

    Args:
        feat: FEAT output directory. Multiple --feat arguments can be supplied.
        featdirfile: File with a list of FEAT directories. Can be used in\
            conjunction with --feat.
        seg: Change segmentation volume, default is aseg.
        aparc_aseg: Use aparc+aseg.mgz. Same as --seg aparc+aseg.mgz.
        svstats: Save result in featdir/stats instead of featdir/reg/freesurfer
        standard: Map results to standard space instead of native functional space.\
            Implies --svstats.
        debug: Turn on debugging.
        help: Print help and exit.
        version: Print version and exit.

    Returns:
        Parameter dictionary.
    """
    params: Aseg2feat = {
        "@type": "freesurfer/aseg2feat",
        "feat": feat,
        "aparc_aseg": aparc_aseg,
        "svstats": svstats,
        "standard": standard,
        "debug": debug,
        "help": help,
        "version": version,
    }
    if featdirfile is not None:
        params["featdirfile"] = featdirfile
    if seg is not None:
        params["seg"] = seg
    return params

def aseg2feat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aseg2feat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat", None) is None:
        raise StyxValidationError("`feat` must not be None")
    if not isinstance(params["feat"], str):
        raise StyxValidationError(f'`feat` has the wrong type: Received `{type(params["feat"])}` expected `str`')
    if params.get("featdirfile", None) is not None:
        if not isinstance(params["featdirfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`featdirfile` has the wrong type: Received `{type(params["featdirfile"])}` expected `InputPathType`')
    if params.get("seg", None) is not None:
        if not isinstance(params["seg"], str):
            raise StyxValidationError(f'`seg` has the wrong type: Received `{type(params["seg"])}` expected `str`')
    if params.get("aparc_aseg", None) is not None:
        if not isinstance(params["aparc_aseg"], bool):
            raise StyxValidationError(f'`aparc_aseg` has the wrong type: Received `{type(params["aparc_aseg"])}` expected `bool`')
    if params.get("svstats", None) is not None:
        if not isinstance(params["svstats"], bool):
            raise StyxValidationError(f'`svstats` has the wrong type: Received `{type(params["svstats"])}` expected `bool`')
    if params.get("standard", None) is not None:
        if not isinstance(params["standard"], bool):
            raise StyxValidationError(f'`standard` has the wrong type: Received `{type(params["standard"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def aseg2feat_cargs(params: Aseg2feat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aseg2feat")
    cargs.append("--feat")
    cargs.append(params["feat"])
    v_0 = params.get("featdirfile")
    if v_0 is not None:
        cargs.append("--featdirfile")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("seg")
    if v_1 is not None:
        cargs.append("--seg")
        cargs.append(v_1)
    if params.get("aparc_aseg"):
        cargs.append("--aparc+aseg")
    if params.get("svstats"):
        cargs.append("--svstats")
    if params.get("standard"):
        cargs.append("--standard")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def aseg2feat_outputs(params: Aseg2feat, execution: Execution) -> Aseg2featOutputs:
    """Build the Aseg2featOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmentation_output_v: OutputPathType = execution.output_file(f'{params["feat"]}/reg/freesurfer/aseg.nii.gz')
    stats_output_v: OutputPathType = execution.output_file(f'{params["feat"]}/stats')
    return Aseg2featOutputs(
        root=root_v,
        segmentation_output=segmentation_output_v,
        stats_output=stats_output_v,
    )

def aseg2feat_execute(params: Aseg2feat, runner: Runner | None = None) -> Aseg2featOutputs:
    """
    aseg2feat

    Resamples the FreeSurfer automatic subcortical segmentation (aseg) to the FEAT functional space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aseg2feat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ASEG2FEAT_METADATA)
    execution.params(params)
    args = aseg2feat_cargs(params, execution)
    out = aseg2feat_outputs(params, execution)
    execution.run(args)
    return out

def aseg2feat(
    feat: str,
    featdirfile: InputPathType | None = None,
    seg: str | None = None,
    aparc_aseg: bool = False,
    svstats: bool = False,
    standard: bool = False,
    debug: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Aseg2featOutputs:
    """
    aseg2feat

    Resamples the FreeSurfer automatic subcortical segmentation (aseg) to the FEAT functional space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        feat: FEAT output directory. Multiple --feat arguments can be supplied.
        featdirfile: File with a list of FEAT directories. Can be used in\
            conjunction with --feat.
        seg: Change segmentation volume, default is aseg.
        aparc_aseg: Use aparc+aseg.mgz. Same as --seg aparc+aseg.mgz.
        svstats: Save result in featdir/stats instead of featdir/reg/freesurfer
        standard: Map results to standard space instead of native functional space.\
            Implies --svstats.
        debug: Turn on debugging.
        help: Print help and exit.
        version: Print version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aseg2feat_params(
        feat=feat,
        featdirfile=featdirfile,
        seg=seg,
        aparc_aseg=aparc_aseg,
        svstats=svstats,
        standard=standard,
        debug=debug,
        help=help,
        version=version,
    )
    return aseg2feat_execute(params, runner)

__all__ = [
    "Aseg2feat",
    "Aseg2featOutputs",
    "ASEG2FEAT_METADATA",
    "aseg2feat_cargs",
    "aseg2feat_outputs",
    "aseg2feat_params",
    "aseg2feat_execute",
    "aseg2feat_validate",
    "aseg2feat",
]