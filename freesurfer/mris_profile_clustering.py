# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_PROFILE_CLUSTERING_METADATA = Metadata(
    id="mris_profileClustering",
    name="mris_profileClustering",
    package="freesurfer",
)

MrisProfileClustering = typing.TypedDict(
    "MrisProfileClustering",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_profileClustering"]],
        "input_file": InputPathType,
        "output_file": str,
        "other_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisProfileClusteringOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    clustering_output: OutputPathType
    """File with the clustering results."""

def mris_profile_clustering_params(
    input_file: InputPathType,
    output_file: str,
    other_options: str | None = None,
) -> MrisProfileClustering:
    """
    Build parameters.

    Args:
        input_file: Input file required for clustering.
        output_file: Output file to store clustering results.
        other_options: Other various options for tuning the clustering process.

    Returns:
        Parameter dictionary.
    """
    params: MrisProfileClustering = {
        "@type": "freesurfer/mris_profileClustering",
        "input_file": input_file,
        "output_file": output_file,
    }
    if other_options is not None:
        params["other_options"] = other_options
    return params

def mris_profile_clustering_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisProfileClustering."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("other_options", None) is not None:
        if not isinstance(params["other_options"], str):
            raise StyxValidationError(f'`other_options` has the wrong type: Received `{type(params["other_options"])}` expected `str`')

def mris_profile_clustering_cargs(params: MrisProfileClustering, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_profileClustering")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    v_0 = params.get("other_options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def mris_profile_clustering_outputs(params: MrisProfileClustering, execution: Execution) -> MrisProfileClusteringOutputs:
    """Build the MrisProfileClusteringOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    clustering_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MrisProfileClusteringOutputs(
        root=root_v,
        clustering_output=clustering_output_v,
    )

def mris_profile_clustering_execute(params: MrisProfileClustering, runner: Runner | None = None) -> MrisProfileClusteringOutputs:
    """
    mris_profileClustering

    A tool from Freesurfer for clustering based on profile data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_profile_clustering_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_PROFILE_CLUSTERING_METADATA)
    execution.params(params)
    args = mris_profile_clustering_cargs(params, execution)
    out = mris_profile_clustering_outputs(params, execution)
    execution.run(args)
    return out

def mris_profile_clustering(
    input_file: InputPathType,
    output_file: str,
    other_options: str | None = None,
    runner: Runner | None = None,
) -> MrisProfileClusteringOutputs:
    """
    mris_profileClustering

    A tool from Freesurfer for clustering based on profile data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input file required for clustering.
        output_file: Output file to store clustering results.
        other_options: Other various options for tuning the clustering process.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_profile_clustering_params(
        input_file=input_file,
        output_file=output_file,
        other_options=other_options,
    )
    return mris_profile_clustering_execute(params, runner)

__all__ = [
    "MrisProfileClustering",
    "MrisProfileClusteringOutputs",
    "MRIS_PROFILE_CLUSTERING_METADATA",
    "mris_profile_clustering_cargs",
    "mris_profile_clustering_outputs",
    "mris_profile_clustering_params",
    "mris_profile_clustering_execute",
    "mris_profile_clustering_validate",
    "mris_profile_clustering",
]