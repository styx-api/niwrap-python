# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SWI_PROCESS_METADATA = Metadata(
    id="swi_process",
    name="swi_process",
    package="freesurfer",
)

SwiProcess = typing.TypedDict(
    "SwiProcess",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/swi_process"]],
        "magnitude_image": InputPathType,
        "phase_image": InputPathType,
        "swi_output": str,
        "stddev": typing.NotRequired[float],
        "phase_mask_cutoff": typing.NotRequired[float],
        "phase_mask_right_cutoff": typing.NotRequired[float],
        "sigmoid_a": typing.NotRequired[float],
        "sigmoid_b": typing.NotRequired[float],
        "phase_multiplications": typing.NotRequired[float],
        "mip_level": typing.NotRequired[float],
        "phase_mask_method": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SwiProcessOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    swi_output_file: OutputPathType
    """SWI processed output image"""

def swi_process_params(
    magnitude_image: InputPathType,
    phase_image: InputPathType,
    swi_output: str,
    stddev: float | None = None,
    phase_mask_cutoff: float | None = None,
    phase_mask_right_cutoff: float | None = None,
    sigmoid_a: float | None = None,
    sigmoid_b: float | None = None,
    phase_multiplications: float | None = None,
    mip_level: float | None = None,
    phase_mask_method: str | None = None,
) -> SwiProcess:
    """
    Build parameters.

    Args:
        magnitude_image: The magnitude image (Output from the PRELUDE program)
        phase_image: The phase image (Output from the PRELUDE program)
        swi_output: Name of the SWI processed output image
        stddev: Specify the standard deviation of the Gaussian Smoothing Filter.\
            Default is 2.0
        phase_mask_cutoff: Specify the negative phase mask cutoff frequency (in\
            radians). Default is the minimum value of the phase image.
        phase_mask_right_cutoff: Specify the positive phase mask cutoff frequency\
            (in radians). Default is the maximum value of the phase image.
        sigmoid_a: Specify 'a' for the sigmoid formula\
            f(phase)=1/(1+exp(-a*(phase-b))). Default is 1.0. Meaningless with\
            phase_method != sigmoid
        sigmoid_b: Specify 'b' for the sigmoid formula\
            f(phase)=1/(1+exp(-a*(phase-b))). Default is 0.0. Meaningless with\
            phase_method != sigmoid
        phase_multiplications: Specify the number of phase multiplications. Default\
            is 4
        mip_level: Specify the number of levels of mIP across the y direction.\
            Default is 4
        phase_mask_method: Specify the phase mask method. One of negative,\
            positive, symmetric, asymmetric, sigmoid. Default is negative

    Returns:
        Parameter dictionary.
    """
    params: SwiProcess = {
        "@type": "freesurfer/swi_process",
        "magnitude_image": magnitude_image,
        "phase_image": phase_image,
        "swi_output": swi_output,
    }
    if stddev is not None:
        params["stddev"] = stddev
    if phase_mask_cutoff is not None:
        params["phase_mask_cutoff"] = phase_mask_cutoff
    if phase_mask_right_cutoff is not None:
        params["phase_mask_right_cutoff"] = phase_mask_right_cutoff
    if sigmoid_a is not None:
        params["sigmoid_a"] = sigmoid_a
    if sigmoid_b is not None:
        params["sigmoid_b"] = sigmoid_b
    if phase_multiplications is not None:
        params["phase_multiplications"] = phase_multiplications
    if mip_level is not None:
        params["mip_level"] = mip_level
    if phase_mask_method is not None:
        params["phase_mask_method"] = phase_mask_method
    return params

def swi_process_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SwiProcess."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("magnitude_image", None) is None:
        raise StyxValidationError("`magnitude_image` must not be None")
    if not isinstance(params["magnitude_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`magnitude_image` has the wrong type: Received `{type(params["magnitude_image"])}` expected `InputPathType`')
    if params.get("phase_image", None) is None:
        raise StyxValidationError("`phase_image` must not be None")
    if not isinstance(params["phase_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`phase_image` has the wrong type: Received `{type(params["phase_image"])}` expected `InputPathType`')
    if params.get("swi_output", None) is None:
        raise StyxValidationError("`swi_output` must not be None")
    if not isinstance(params["swi_output"], str):
        raise StyxValidationError(f'`swi_output` has the wrong type: Received `{type(params["swi_output"])}` expected `str`')
    if params.get("stddev", None) is not None:
        if not isinstance(params["stddev"], (float, int)):
            raise StyxValidationError(f'`stddev` has the wrong type: Received `{type(params["stddev"])}` expected `float`')
    if params.get("phase_mask_cutoff", None) is not None:
        if not isinstance(params["phase_mask_cutoff"], (float, int)):
            raise StyxValidationError(f'`phase_mask_cutoff` has the wrong type: Received `{type(params["phase_mask_cutoff"])}` expected `float`')
    if params.get("phase_mask_right_cutoff", None) is not None:
        if not isinstance(params["phase_mask_right_cutoff"], (float, int)):
            raise StyxValidationError(f'`phase_mask_right_cutoff` has the wrong type: Received `{type(params["phase_mask_right_cutoff"])}` expected `float`')
    if params.get("sigmoid_a", None) is not None:
        if not isinstance(params["sigmoid_a"], (float, int)):
            raise StyxValidationError(f'`sigmoid_a` has the wrong type: Received `{type(params["sigmoid_a"])}` expected `float`')
    if params.get("sigmoid_b", None) is not None:
        if not isinstance(params["sigmoid_b"], (float, int)):
            raise StyxValidationError(f'`sigmoid_b` has the wrong type: Received `{type(params["sigmoid_b"])}` expected `float`')
    if params.get("phase_multiplications", None) is not None:
        if not isinstance(params["phase_multiplications"], (float, int)):
            raise StyxValidationError(f'`phase_multiplications` has the wrong type: Received `{type(params["phase_multiplications"])}` expected `float`')
    if params.get("mip_level", None) is not None:
        if not isinstance(params["mip_level"], (float, int)):
            raise StyxValidationError(f'`mip_level` has the wrong type: Received `{type(params["mip_level"])}` expected `float`')
    if params.get("phase_mask_method", None) is not None:
        if not isinstance(params["phase_mask_method"], str):
            raise StyxValidationError(f'`phase_mask_method` has the wrong type: Received `{type(params["phase_mask_method"])}` expected `str`')

def swi_process_cargs(params: SwiProcess, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("swi_process")
    cargs.append(execution.input_file(params["magnitude_image"]))
    cargs.append(execution.input_file(params["phase_image"]))
    cargs.append(params["swi_output"])
    v_0 = params.get("stddev")
    if v_0 is not None:
        cargs.append("--stddev")
        cargs.append(str(v_0))
    v_1 = params.get("phase_mask_cutoff")
    if v_1 is not None:
        cargs.append("--phase_mask_cutoff")
        cargs.append(str(v_1))
    v_2 = params.get("phase_mask_right_cutoff")
    if v_2 is not None:
        cargs.append("--phase_mask_right_cutoff")
        cargs.append(str(v_2))
    v_3 = params.get("sigmoid_a")
    if v_3 is not None:
        cargs.append("--sigmoid_a")
        cargs.append(str(v_3))
    v_4 = params.get("sigmoid_b")
    if v_4 is not None:
        cargs.append("--sigmoid_b")
        cargs.append(str(v_4))
    v_5 = params.get("phase_multiplications")
    if v_5 is not None:
        cargs.append("--phase_multiplications")
        cargs.append(str(v_5))
    v_6 = params.get("mip_level")
    if v_6 is not None:
        cargs.append("--mip_level")
        cargs.append(str(v_6))
    v_7 = params.get("phase_mask_method")
    if v_7 is not None:
        cargs.append("--phase_mask_method")
        cargs.append(v_7)
    return cargs

def swi_process_outputs(params: SwiProcess, execution: Execution) -> SwiProcessOutputs:
    """Build the SwiProcessOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    swi_output_file_v: OutputPathType = execution.output_file(params["swi_output"])
    return SwiProcessOutputs(
        root=root_v,
        swi_output_file=swi_output_file_v,
    )

def swi_process_execute(params: SwiProcess, runner: Runner | None = None) -> SwiProcessOutputs:
    """
    swi_process

    Process the Susceptibility-weighted images. Ensure the inputs are post-phase unwrapping using PRELUDE.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    swi_process_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SWI_PROCESS_METADATA)
    execution.params(params)
    args = swi_process_cargs(params, execution)
    out = swi_process_outputs(params, execution)
    execution.run(args)
    return out

def swi_process(
    magnitude_image: InputPathType,
    phase_image: InputPathType,
    swi_output: str,
    stddev: float | None = None,
    phase_mask_cutoff: float | None = None,
    phase_mask_right_cutoff: float | None = None,
    sigmoid_a: float | None = None,
    sigmoid_b: float | None = None,
    phase_multiplications: float | None = None,
    mip_level: float | None = None,
    phase_mask_method: str | None = None,
    runner: Runner | None = None,
) -> SwiProcessOutputs:
    """
    swi_process

    Process the Susceptibility-weighted images. Ensure the inputs are post-phase unwrapping using PRELUDE.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        magnitude_image: The magnitude image (Output from the PRELUDE program)
        phase_image: The phase image (Output from the PRELUDE program)
        swi_output: Name of the SWI processed output image
        stddev: Specify the standard deviation of the Gaussian Smoothing Filter.\
            Default is 2.0
        phase_mask_cutoff: Specify the negative phase mask cutoff frequency (in\
            radians). Default is the minimum value of the phase image.
        phase_mask_right_cutoff: Specify the positive phase mask cutoff frequency\
            (in radians). Default is the maximum value of the phase image.
        sigmoid_a: Specify 'a' for the sigmoid formula\
            f(phase)=1/(1+exp(-a*(phase-b))). Default is 1.0. Meaningless with\
            phase_method != sigmoid
        sigmoid_b: Specify 'b' for the sigmoid formula\
            f(phase)=1/(1+exp(-a*(phase-b))). Default is 0.0. Meaningless with\
            phase_method != sigmoid
        phase_multiplications: Specify the number of phase multiplications. Default\
            is 4
        mip_level: Specify the number of levels of mIP across the y direction.\
            Default is 4
        phase_mask_method: Specify the phase mask method. One of negative,\
            positive, symmetric, asymmetric, sigmoid. Default is negative
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = swi_process_params(
        magnitude_image=magnitude_image,
        phase_image=phase_image,
        swi_output=swi_output,
        stddev=stddev,
        phase_mask_cutoff=phase_mask_cutoff,
        phase_mask_right_cutoff=phase_mask_right_cutoff,
        sigmoid_a=sigmoid_a,
        sigmoid_b=sigmoid_b,
        phase_multiplications=phase_multiplications,
        mip_level=mip_level,
        phase_mask_method=phase_mask_method,
    )
    return swi_process_execute(params, runner)

__all__ = [
    "SwiProcess",
    "SwiProcessOutputs",
    "SWI_PROCESS_METADATA",
    "swi_process_cargs",
    "swi_process_outputs",
    "swi_process_params",
    "swi_process_execute",
    "swi_process_validate",
    "swi_process",
]