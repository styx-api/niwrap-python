# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_FIND_FLAT_REGIONS_METADATA = Metadata(
    id="mris_find_flat_regions",
    name="mris_find_flat_regions",
    package="freesurfer",
)

MrisFindFlatRegions = typing.TypedDict(
    "MrisFindFlatRegions",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_find_flat_regions"]],
        "surface": InputPathType,
        "wfile": str,
        "threshold": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisFindFlatRegionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_label_file: OutputPathType
    """Output label file with regions where the surface is almost perpendicular to one of the cardinal axes"""

def mris_find_flat_regions_params(
    surface: InputPathType,
    wfile: str,
    threshold: float | None = None,
) -> MrisFindFlatRegions:
    """
    Build parameters.

    Args:
        surface: Surface input file
        wfile: Output label file
        threshold: Threshold to use (default=0.990)

    Returns:
        Parameter dictionary.
    """
    params: MrisFindFlatRegions = {
        "@type": "freesurfer/mris_find_flat_regions",
        "surface": surface,
        "wfile": wfile,
    }
    if threshold is not None:
        params["threshold"] = threshold
    return params

def mris_find_flat_regions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisFindFlatRegions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("wfile", None) is None:
        raise StyxValidationError("`wfile` must not be None")
    if not isinstance(params["wfile"], str):
        raise StyxValidationError(f'`wfile` has the wrong type: Received `{type(params["wfile"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')

def mris_find_flat_regions_cargs(params: MrisFindFlatRegions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_find_flat_regions")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["wfile"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(str(v_0))
    return cargs

def mris_find_flat_regions_outputs(params: MrisFindFlatRegions, execution: Execution) -> MrisFindFlatRegionsOutputs:
    """Build the MrisFindFlatRegionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_label_file_v: OutputPathType = execution.output_file(params["wfile"])
    return MrisFindFlatRegionsOutputs(
        root=root_v,
        output_label_file=output_label_file_v,
    )

def mris_find_flat_regions_execute(params: MrisFindFlatRegions, runner: Runner | None = None) -> MrisFindFlatRegionsOutputs:
    """
    mris_find_flat_regions

    Compute regions in which the surface is almost perpendicular to one of the cardinal axes and write the results to a label file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_find_flat_regions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_FIND_FLAT_REGIONS_METADATA)
    execution.params(params)
    args = mris_find_flat_regions_cargs(params, execution)
    out = mris_find_flat_regions_outputs(params, execution)
    execution.run(args)
    return out

def mris_find_flat_regions(
    surface: InputPathType,
    wfile: str,
    threshold: float | None = None,
    runner: Runner | None = None,
) -> MrisFindFlatRegionsOutputs:
    """
    mris_find_flat_regions

    Compute regions in which the surface is almost perpendicular to one of the cardinal axes and write the results to a label file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Surface input file
        wfile: Output label file
        threshold: Threshold to use (default=0.990)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_find_flat_regions_params(
        surface=surface,
        wfile=wfile,
        threshold=threshold,
    )
    return mris_find_flat_regions_execute(params, runner)

__all__ = [
    "MrisFindFlatRegions",
    "MrisFindFlatRegionsOutputs",
    "MRIS_FIND_FLAT_REGIONS_METADATA",
    "mris_find_flat_regions_cargs",
    "mris_find_flat_regions_outputs",
    "mris_find_flat_regions_params",
    "mris_find_flat_regions_execute",
    "mris_find_flat_regions_validate",
    "mris_find_flat_regions",
]