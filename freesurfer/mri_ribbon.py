# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RIBBON_METADATA = Metadata(
    id="mri_ribbon",
    name="mri_ribbon",
    package="freesurfer",
)

MriRibbon = typing.TypedDict(
    "MriRibbon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_ribbon"]],
        "label_file": typing.NotRequired[InputPathType],
        "inner_surface": InputPathType,
        "outer_surface": InputPathType,
        "input_volume": str,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriRibbonOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    ribbon_output: OutputPathType
    """Output ribbon volume"""

def mri_ribbon_params(
    inner_surface: InputPathType,
    outer_surface: InputPathType,
    input_volume: str,
    output_volume: str,
    label_file: InputPathType | None = None,
) -> MriRibbon:
    """
    Build parameters.

    Args:
        inner_surface: File name of the inner surface
        outer_surface: File name of the outer surface
        input_volume: Prefix for the input volume file
        output_volume: Prefix for the output volume file
        label_file: Label file specifying regions of interest (optional)

    Returns:
        Parameter dictionary.
    """
    params: MriRibbon = {
        "@type": "freesurfer/mri_ribbon",
        "inner_surface": inner_surface,
        "outer_surface": outer_surface,
        "input_volume": input_volume,
        "output_volume": output_volume,
    }
    if label_file is not None:
        params["label_file"] = label_file
    return params

def mri_ribbon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRibbon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params["label_file"])}` expected `InputPathType`')
    if params.get("inner_surface", None) is None:
        raise StyxValidationError("`inner_surface` must not be None")
    if not isinstance(params["inner_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inner_surface` has the wrong type: Received `{type(params["inner_surface"])}` expected `InputPathType`')
    if params.get("outer_surface", None) is None:
        raise StyxValidationError("`outer_surface` must not be None")
    if not isinstance(params["outer_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outer_surface` has the wrong type: Received `{type(params["outer_surface"])}` expected `InputPathType`')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], str):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_ribbon_cargs(params: MriRibbon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_ribbon")
    v_0 = params.get("label_file")
    if v_0 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_0))
    cargs.append(execution.input_file(params["inner_surface"]))
    cargs.append(execution.input_file(params["outer_surface"]))
    cargs.append(params["input_volume"])
    cargs.append(params["output_volume"])
    return cargs

def mri_ribbon_outputs(params: MriRibbon, execution: Execution) -> MriRibbonOutputs:
    """Build the MriRibbonOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    ribbon_output_v: OutputPathType = execution.output_file(f'{params["output_volume"]}_ribbon.nii.gz')
    return MriRibbonOutputs(
        root=root_v,
        ribbon_output=ribbon_output_v,
    )

def mri_ribbon_execute(params: MriRibbon, runner: Runner | None = None) -> MriRibbonOutputs:
    """
    mri_ribbon

    MRI Ribbon tool to create ribbon volumes from surface files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_ribbon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RIBBON_METADATA)
    execution.params(params)
    args = mri_ribbon_cargs(params, execution)
    out = mri_ribbon_outputs(params, execution)
    execution.run(args)
    return out

def mri_ribbon(
    inner_surface: InputPathType,
    outer_surface: InputPathType,
    input_volume: str,
    output_volume: str,
    label_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> MriRibbonOutputs:
    """
    mri_ribbon

    MRI Ribbon tool to create ribbon volumes from surface files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        inner_surface: File name of the inner surface
        outer_surface: File name of the outer surface
        input_volume: Prefix for the input volume file
        output_volume: Prefix for the output volume file
        label_file: Label file specifying regions of interest (optional)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_ribbon_params(
        inner_surface=inner_surface,
        outer_surface=outer_surface,
        input_volume=input_volume,
        output_volume=output_volume,
        label_file=label_file,
    )
    return mri_ribbon_execute(params, runner)

__all__ = [
    "MriRibbon",
    "MriRibbonOutputs",
    "MRI_RIBBON_METADATA",
    "mri_ribbon_cargs",
    "mri_ribbon_outputs",
    "mri_ribbon_params",
    "mri_ribbon_execute",
    "mri_ribbon_validate",
    "mri_ribbon",
]