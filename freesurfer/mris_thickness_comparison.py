# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_THICKNESS_COMPARISON_METADATA = Metadata(
    id="mris_thickness_comparison",
    name="mris_thickness_comparison",
    package="freesurfer",
)

MrisThicknessComparison = typing.TypedDict(
    "MrisThicknessComparison",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_thickness_comparison"]],
        "subject": str,
        "hemi": str,
        "thickness_file": InputPathType,
        "w_file": InputPathType,
        "labels": list[str],
    },
)

@dataclasses.dataclass
class MrisThicknessComparisonOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_thickness_comparison_params(
    subject: str,
    hemi: str,
    thickness_file: InputPathType,
    w_file: InputPathType,
    labels: list[str],
) -> MrisThicknessComparison:
    """
    Build parameters.

    Args:
        subject: Subject identifier.
        hemi: Hemisphere (e.g., lh or rh).
        thickness_file: File containing thickness measurements.
        w_file: W file for cortical thickness comparison.
        labels: List of labels to compare, separated by spaces.

    Returns:
        Parameter dictionary.
    """
    params: MrisThicknessComparison = {
        "@type": "freesurfer/mris_thickness_comparison",
        "subject": subject,
        "hemi": hemi,
        "thickness_file": thickness_file,
        "w_file": w_file,
        "labels": labels,
    }
    return params

def mris_thickness_comparison_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisThicknessComparison."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("thickness_file", None) is None:
        raise StyxValidationError("`thickness_file` must not be None")
    if not isinstance(params["thickness_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`thickness_file` has the wrong type: Received `{type(params["thickness_file"])}` expected `InputPathType`')
    if params.get("w_file", None) is None:
        raise StyxValidationError("`w_file` must not be None")
    if not isinstance(params["w_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`w_file` has the wrong type: Received `{type(params["w_file"])}` expected `InputPathType`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')

def mris_thickness_comparison_cargs(params: MrisThicknessComparison, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_thickness_comparison")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["thickness_file"]))
    cargs.append(execution.input_file(params["w_file"]))
    for item0 in params["labels"]:
        cargs.append(item0)
    return cargs

def mris_thickness_comparison_outputs(params: MrisThicknessComparison, execution: Execution) -> MrisThicknessComparisonOutputs:
    """Build the MrisThicknessComparisonOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisThicknessComparisonOutputs(
        root=root_v,
    )

def mris_thickness_comparison_execute(params: MrisThicknessComparison, runner: Runner | None = None) -> MrisThicknessComparisonOutputs:
    """
    mris_thickness_comparison

    Tool to compare cortical thickness measurements between two or more specified labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_thickness_comparison_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_THICKNESS_COMPARISON_METADATA)
    execution.params(params)
    args = mris_thickness_comparison_cargs(params, execution)
    out = mris_thickness_comparison_outputs(params, execution)
    execution.run(args)
    return out

def mris_thickness_comparison(
    subject: str,
    hemi: str,
    thickness_file: InputPathType,
    w_file: InputPathType,
    labels: list[str],
    runner: Runner | None = None,
) -> MrisThicknessComparisonOutputs:
    """
    mris_thickness_comparison

    Tool to compare cortical thickness measurements between two or more specified labels.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier.
        hemi: Hemisphere (e.g., lh or rh).
        thickness_file: File containing thickness measurements.
        w_file: W file for cortical thickness comparison.
        labels: List of labels to compare, separated by spaces.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_thickness_comparison_params(
        subject=subject,
        hemi=hemi,
        thickness_file=thickness_file,
        w_file=w_file,
        labels=labels,
    )
    return mris_thickness_comparison_execute(params, runner)

__all__ = [
    "MrisThicknessComparison",
    "MrisThicknessComparisonOutputs",
    "MRIS_THICKNESS_COMPARISON_METADATA",
    "mris_thickness_comparison_cargs",
    "mris_thickness_comparison_outputs",
    "mris_thickness_comparison_params",
    "mris_thickness_comparison_execute",
    "mris_thickness_comparison_validate",
    "mris_thickness_comparison",
]