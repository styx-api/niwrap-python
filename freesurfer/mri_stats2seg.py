# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_STATS2SEG_METADATA = Metadata(
    id="mri_stats2seg",
    name="mri_stats2seg",
    package="freesurfer",
)

MriStats2seg = typing.TypedDict(
    "MriStats2seg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_stats2seg"]],
        "stat_file": InputPathType,
        "segmentation_volume": InputPathType,
        "output_file": str,
        "debug": typing.NotRequired[bool],
        "check_opts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriStats2segOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmented_output: OutputPathType
    """Output segmented file."""

def mri_stats2seg_params(
    stat_file: InputPathType,
    segmentation_volume: InputPathType,
    output_file: str,
    debug: bool = False,
    check_opts: bool = False,
) -> MriStats2seg:
    """
    Build parameters.

    Args:
        stat_file: Stat file in an MRI format.
        segmentation_volume: Segmentation volume file.
        output_file: Output file.
        debug: Turn on debugging.
        check_opts: Don't run anything, just check options and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriStats2seg = {
        "@type": "freesurfer/mri_stats2seg",
        "stat_file": stat_file,
        "segmentation_volume": segmentation_volume,
        "output_file": output_file,
        "debug": debug,
        "check_opts": check_opts,
    }
    return params

def mri_stats2seg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriStats2seg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stat_file", None) is None:
        raise StyxValidationError("`stat_file` must not be None")
    if not isinstance(params["stat_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`stat_file` has the wrong type: Received `{type(params["stat_file"])}` expected `InputPathType`')
    if params.get("segmentation_volume", None) is None:
        raise StyxValidationError("`segmentation_volume` must not be None")
    if not isinstance(params["segmentation_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`segmentation_volume` has the wrong type: Received `{type(params["segmentation_volume"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_opts", None) is not None:
        if not isinstance(params["check_opts"], bool):
            raise StyxValidationError(f'`check_opts` has the wrong type: Received `{type(params["check_opts"])}` expected `bool`')

def mri_stats2seg_cargs(params: MriStats2seg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_stats2seg")
    cargs.append("--stat")
    cargs.append(execution.input_file(params["stat_file"]))
    cargs.append("--seg")
    cargs.append(execution.input_file(params["segmentation_volume"]))
    cargs.append("--o")
    cargs.append(params["output_file"])
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_opts"):
        cargs.append("--checkopts")
    return cargs

def mri_stats2seg_outputs(params: MriStats2seg, execution: Execution) -> MriStats2segOutputs:
    """Build the MriStats2segOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmented_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MriStats2segOutputs(
        root=root_v,
        segmented_output=segmented_output_v,
    )

def mri_stats2seg_execute(params: MriStats2seg, runner: Runner | None = None) -> MriStats2segOutputs:
    """
    mri_stats2seg

    A command-line tool for converting MRI statistical maps to segmented volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_stats2seg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_STATS2SEG_METADATA)
    execution.params(params)
    args = mri_stats2seg_cargs(params, execution)
    out = mri_stats2seg_outputs(params, execution)
    execution.run(args)
    return out

def mri_stats2seg(
    stat_file: InputPathType,
    segmentation_volume: InputPathType,
    output_file: str,
    debug: bool = False,
    check_opts: bool = False,
    runner: Runner | None = None,
) -> MriStats2segOutputs:
    """
    mri_stats2seg

    A command-line tool for converting MRI statistical maps to segmented volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        stat_file: Stat file in an MRI format.
        segmentation_volume: Segmentation volume file.
        output_file: Output file.
        debug: Turn on debugging.
        check_opts: Don't run anything, just check options and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_stats2seg_params(
        stat_file=stat_file,
        segmentation_volume=segmentation_volume,
        output_file=output_file,
        debug=debug,
        check_opts=check_opts,
    )
    return mri_stats2seg_execute(params, runner)

__all__ = [
    "MriStats2seg",
    "MriStats2segOutputs",
    "MRI_STATS2SEG_METADATA",
    "mri_stats2seg_cargs",
    "mri_stats2seg_outputs",
    "mri_stats2seg_params",
    "mri_stats2seg_execute",
    "mri_stats2seg_validate",
    "mri_stats2seg",
]