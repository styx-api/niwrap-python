# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_REMOVE_NECK_METADATA = Metadata(
    id="mri_remove_neck",
    name="mri_remove_neck",
    package="freesurfer",
)

MriRemoveNeck = typing.TypedDict(
    "MriRemoveNeck",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_remove_neck"]],
        "input_volume": InputPathType,
        "transform": InputPathType,
        "gca": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriRemoveNeckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Processed MRI volume with neck removed."""

def mri_remove_neck_params(
    input_volume: InputPathType,
    transform: InputPathType,
    gca: InputPathType,
    output_volume: str,
) -> MriRemoveNeck:
    """
    Build parameters.

    Args:
        input_volume: Input MRI volume.
        transform: Transformation matrix to register the volume.
        gca: GCA file needed for processing.
        output_volume: Output MRI volume with the neck removed.

    Returns:
        Parameter dictionary.
    """
    params: MriRemoveNeck = {
        "@type": "freesurfer/mri_remove_neck",
        "input_volume": input_volume,
        "transform": transform,
        "gca": gca,
        "output_volume": output_volume,
    }
    return params

def mri_remove_neck_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRemoveNeck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("gca", None) is None:
        raise StyxValidationError("`gca` must not be None")
    if not isinstance(params["gca"], (pathlib.Path, str)):
        raise StyxValidationError(f'`gca` has the wrong type: Received `{type(params["gca"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_remove_neck_cargs(params: MriRemoveNeck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_remove_neck")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(execution.input_file(params["gca"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_remove_neck_outputs(params: MriRemoveNeck, execution: Execution) -> MriRemoveNeckOutputs:
    """Build the MriRemoveNeckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriRemoveNeckOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_remove_neck_execute(params: MriRemoveNeck, runner: Runner | None = None) -> MriRemoveNeckOutputs:
    """
    mri_remove_neck

    Tool for removing neck from MRI volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_remove_neck_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_REMOVE_NECK_METADATA)
    execution.params(params)
    args = mri_remove_neck_cargs(params, execution)
    out = mri_remove_neck_outputs(params, execution)
    execution.run(args)
    return out

def mri_remove_neck(
    input_volume: InputPathType,
    transform: InputPathType,
    gca: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriRemoveNeckOutputs:
    """
    mri_remove_neck

    Tool for removing neck from MRI volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input MRI volume.
        transform: Transformation matrix to register the volume.
        gca: GCA file needed for processing.
        output_volume: Output MRI volume with the neck removed.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_remove_neck_params(
        input_volume=input_volume,
        transform=transform,
        gca=gca,
        output_volume=output_volume,
    )
    return mri_remove_neck_execute(params, runner)

__all__ = [
    "MriRemoveNeck",
    "MriRemoveNeckOutputs",
    "MRI_REMOVE_NECK_METADATA",
    "mri_remove_neck_cargs",
    "mri_remove_neck_outputs",
    "mri_remove_neck_params",
    "mri_remove_neck_execute",
    "mri_remove_neck_validate",
    "mri_remove_neck",
]