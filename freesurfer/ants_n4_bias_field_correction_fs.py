# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_N4_BIAS_FIELD_CORRECTION_FS_METADATA = Metadata(
    id="AntsN4BiasFieldCorrectionFs",
    name="AntsN4BiasFieldCorrectionFs",
    package="freesurfer",
)

AntsN4BiasFieldCorrectionFs = typing.TypedDict(
    "AntsN4BiasFieldCorrectionFs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/AntsN4BiasFieldCorrectionFs"]],
        "input_file": InputPathType,
        "output_file": str,
        "mask_file": typing.NotRequired[InputPathType],
        "shrink_factor": typing.NotRequired[int],
        "iterations": typing.NotRequired[list[float]],
        "output_dtype": typing.NotRequired[str],
        "replace_zeros": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class AntsN4BiasFieldCorrectionFsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_output_file: OutputPathType
    """Corrected volume output file."""

def ants_n4_bias_field_correction_fs_params(
    input_file: InputPathType,
    output_file: str,
    mask_file: InputPathType | None = None,
    shrink_factor: int | None = None,
    iterations: list[float] | None = None,
    output_dtype: str | None = None,
    replace_zeros: str | None = None,
) -> AntsN4BiasFieldCorrectionFs:
    """
    Build parameters.

    Args:
        input_file: Input volume file.
        output_file: Corrected output volume file.
        mask_file: Use image mask during correction.
        shrink_factor: Resample factor to decrease computation time. Default is 4.
        iterations: Number of resolutions and max iterations per resolution.\
            Default is `50 50 50 50`, which indicates 4 fitting levels with 50\
            iterations each.
        output_dtype: Corrected output data type. Can be float, uchar, or int.\
            Default is float.
        replace_zeros: Replace 0s with offset + scale*rand(). Values will be\
            remasked in the output if remask=1.

    Returns:
        Parameter dictionary.
    """
    params: AntsN4BiasFieldCorrectionFs = {
        "@type": "freesurfer/AntsN4BiasFieldCorrectionFs",
        "input_file": input_file,
        "output_file": output_file,
    }
    if mask_file is not None:
        params["mask_file"] = mask_file
    if shrink_factor is not None:
        params["shrink_factor"] = shrink_factor
    if iterations is not None:
        params["iterations"] = iterations
    if output_dtype is not None:
        params["output_dtype"] = output_dtype
    if replace_zeros is not None:
        params["replace_zeros"] = replace_zeros
    return params

def ants_n4_bias_field_correction_fs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsN4BiasFieldCorrectionFs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("shrink_factor", None) is not None:
        if not isinstance(params["shrink_factor"], int):
            raise StyxValidationError(f'`shrink_factor` has the wrong type: Received `{type(params["shrink_factor"])}` expected `int`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], list):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `list[float]`')
        for e in params["iterations"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("output_dtype", None) is not None:
        if not isinstance(params["output_dtype"], str):
            raise StyxValidationError(f'`output_dtype` has the wrong type: Received `{type(params["output_dtype"])}` expected `str`')
    if params.get("replace_zeros", None) is not None:
        if not isinstance(params["replace_zeros"], str):
            raise StyxValidationError(f'`replace_zeros` has the wrong type: Received `{type(params["replace_zeros"])}` expected `str`')

def ants_n4_bias_field_correction_fs_cargs(params: AntsN4BiasFieldCorrectionFs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("AntsN4BiasFieldCorrectionFs")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-o")
    cargs.append(params["output_file"])
    v_0 = params.get("mask_file")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("shrink_factor")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(str(v_1))
    v_2 = params.get("iterations")
    if v_2 is not None:
        cargs.append("-t")
        for item0 in v_2:
            cargs.append(str(item0))
    v_3 = params.get("output_dtype")
    if v_3 is not None:
        cargs.append("-d")
        cargs.append(v_3)
    v_4 = params.get("replace_zeros")
    if v_4 is not None:
        cargs.append("-r")
        cargs.append(v_4)
    return cargs

def ants_n4_bias_field_correction_fs_outputs(params: AntsN4BiasFieldCorrectionFs, execution: Execution) -> AntsN4BiasFieldCorrectionFsOutputs:
    """Build the AntsN4BiasFieldCorrectionFsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return AntsN4BiasFieldCorrectionFsOutputs(
        root=root_v,
        corrected_output_file=corrected_output_file_v,
    )

def ants_n4_bias_field_correction_fs_execute(params: AntsN4BiasFieldCorrectionFs, runner: Runner | None = None) -> AntsN4BiasFieldCorrectionFsOutputs:
    """
    AntsN4BiasFieldCorrectionFs

    Runs N4 (nonparameteric, nonuniform normalization) retrospective bias correction on an image. This program wraps the AntsN4BiasFieldCorrection utility available in the ANTs package.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_n4_bias_field_correction_fs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_N4_BIAS_FIELD_CORRECTION_FS_METADATA)
    execution.params(params)
    args = ants_n4_bias_field_correction_fs_cargs(params, execution)
    out = ants_n4_bias_field_correction_fs_outputs(params, execution)
    execution.run(args)
    return out

def ants_n4_bias_field_correction_fs(
    input_file: InputPathType,
    output_file: str,
    mask_file: InputPathType | None = None,
    shrink_factor: int | None = None,
    iterations: list[float] | None = None,
    output_dtype: str | None = None,
    replace_zeros: str | None = None,
    runner: Runner | None = None,
) -> AntsN4BiasFieldCorrectionFsOutputs:
    """
    AntsN4BiasFieldCorrectionFs

    Runs N4 (nonparameteric, nonuniform normalization) retrospective bias correction on an image. This program wraps the AntsN4BiasFieldCorrection utility available in the ANTs package.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input volume file.
        output_file: Corrected output volume file.
        mask_file: Use image mask during correction.
        shrink_factor: Resample factor to decrease computation time. Default is 4.
        iterations: Number of resolutions and max iterations per resolution.\
            Default is `50 50 50 50`, which indicates 4 fitting levels with 50\
            iterations each.
        output_dtype: Corrected output data type. Can be float, uchar, or int.\
            Default is float.
        replace_zeros: Replace 0s with offset + scale*rand(). Values will be\
            remasked in the output if remask=1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_n4_bias_field_correction_fs_params(
        input_file=input_file,
        output_file=output_file,
        mask_file=mask_file,
        shrink_factor=shrink_factor,
        iterations=iterations,
        output_dtype=output_dtype,
        replace_zeros=replace_zeros,
    )
    return ants_n4_bias_field_correction_fs_execute(params, runner)

__all__ = [
    "AntsN4BiasFieldCorrectionFs",
    "AntsN4BiasFieldCorrectionFsOutputs",
    "ANTS_N4_BIAS_FIELD_CORRECTION_FS_METADATA",
    "ants_n4_bias_field_correction_fs_cargs",
    "ants_n4_bias_field_correction_fs_outputs",
    "ants_n4_bias_field_correction_fs_params",
    "ants_n4_bias_field_correction_fs_execute",
    "ants_n4_bias_field_correction_fs_validate",
    "ants_n4_bias_field_correction_fs",
]