# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

OCT_REGISTER_MOSAIC_METADATA = Metadata(
    id="oct_register_mosaic",
    name="oct_register_mosaic",
    package="freesurfer",
)

OctRegisterMosaic = typing.TypedDict(
    "OctRegisterMosaic",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/oct_register_mosaic"]],
        "tiles_or_mosaic_list": list[str],
        "output_volume": str,
        "downsample": typing.NotRequired[float],
        "weight_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class OctRegisterMosaicOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_output: OutputPathType
    """The registered output volume"""

def oct_register_mosaic_params(
    tiles_or_mosaic_list: list[str],
    output_volume: str,
    downsample: float | None = None,
    weight_file: InputPathType | None = None,
) -> OctRegisterMosaic:
    """
    Build parameters.

    Args:
        tiles_or_mosaic_list: OCT tile images to be registered or a file listing\
            the mosaic tiles
        output_volume: Output registered volume
        downsample: Use Gaussian downsampling specified number of times
        weight_file: File with tile weights to use in tile averaging

    Returns:
        Parameter dictionary.
    """
    params: OctRegisterMosaic = {
        "@type": "freesurfer/oct_register_mosaic",
        "tiles_or_mosaic_list": tiles_or_mosaic_list,
        "output_volume": output_volume,
    }
    if downsample is not None:
        params["downsample"] = downsample
    if weight_file is not None:
        params["weight_file"] = weight_file
    return params

def oct_register_mosaic_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid OctRegisterMosaic."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tiles_or_mosaic_list", None) is None:
        raise StyxValidationError("`tiles_or_mosaic_list` must not be None")
    if not isinstance(params["tiles_or_mosaic_list"], list):
        raise StyxValidationError(f'`tiles_or_mosaic_list` has the wrong type: Received `{type(params["tiles_or_mosaic_list"])}` expected `list[str]`')
    for e in params["tiles_or_mosaic_list"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`tiles_or_mosaic_list` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("downsample", None) is not None:
        if not isinstance(params["downsample"], (float, int)):
            raise StyxValidationError(f'`downsample` has the wrong type: Received `{type(params["downsample"])}` expected `float`')
    if params.get("weight_file", None) is not None:
        if not isinstance(params["weight_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weight_file` has the wrong type: Received `{type(params["weight_file"])}` expected `InputPathType`')

def oct_register_mosaic_cargs(params: OctRegisterMosaic, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("oct_register_mosaic")
    for item0 in params["tiles_or_mosaic_list"]:
        cargs.append(item0)
    cargs.append(params["output_volume"])
    v_0 = params.get("downsample")
    if v_0 is not None:
        cargs.append("-D")
        cargs.append(str(v_0))
    v_1 = params.get("weight_file")
    if v_1 is not None:
        cargs.append("-W")
        cargs.append(execution.input_file(v_1))
    return cargs

def oct_register_mosaic_outputs(params: OctRegisterMosaic, execution: Execution) -> OctRegisterMosaicOutputs:
    """Build the OctRegisterMosaicOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_output_v: OutputPathType = execution.output_file(params["output_volume"])
    return OctRegisterMosaicOutputs(
        root=root_v,
        registered_output=registered_output_v,
    )

def oct_register_mosaic_execute(params: OctRegisterMosaic, runner: Runner | None = None) -> OctRegisterMosaicOutputs:
    """
    oct_register_mosaic

    Tool for registering multiple OCT (Optical Coherence Tomography) tiles or a mosaic list into a single output volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    oct_register_mosaic_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(OCT_REGISTER_MOSAIC_METADATA)
    execution.params(params)
    args = oct_register_mosaic_cargs(params, execution)
    out = oct_register_mosaic_outputs(params, execution)
    execution.run(args)
    return out

def oct_register_mosaic(
    tiles_or_mosaic_list: list[str],
    output_volume: str,
    downsample: float | None = None,
    weight_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> OctRegisterMosaicOutputs:
    """
    oct_register_mosaic

    Tool for registering multiple OCT (Optical Coherence Tomography) tiles or a mosaic list into a single output volume

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        tiles_or_mosaic_list: OCT tile images to be registered or a file listing\
            the mosaic tiles
        output_volume: Output registered volume
        downsample: Use Gaussian downsampling specified number of times
        weight_file: File with tile weights to use in tile averaging
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = oct_register_mosaic_params(
        tiles_or_mosaic_list=tiles_or_mosaic_list,
        output_volume=output_volume,
        downsample=downsample,
        weight_file=weight_file,
    )
    return oct_register_mosaic_execute(params, runner)

__all__ = [
    "OctRegisterMosaic",
    "OctRegisterMosaicOutputs",
    "OCT_REGISTER_MOSAIC_METADATA",
    "oct_register_mosaic_cargs",
    "oct_register_mosaic_outputs",
    "oct_register_mosaic_params",
    "oct_register_mosaic_execute",
    "oct_register_mosaic_validate",
    "oct_register_mosaic",
]