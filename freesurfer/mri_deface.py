# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_DEFACE_METADATA = Metadata(
    id="mri_deface",
    name="mri_deface",
    package="freesurfer",
)

MriDeface = typing.TypedDict(
    "MriDeface",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_deface"]],
        "input_volume": InputPathType,
        "brain_template": InputPathType,
        "face_template": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriDefaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    defaced_output_file: OutputPathType
    """The resulting defaced MRI volume."""

def mri_deface_params(
    input_volume: InputPathType,
    brain_template: InputPathType,
    face_template: InputPathType,
    output_volume: str,
) -> MriDeface:
    """
    Build parameters.

    Args:
        input_volume: The input volume to be defaced (e.g. anatomical MRI image).
        brain_template: Template volume of the brain to be used for defacing.
        face_template: Template volume of the face to be used for defacing.
        output_volume: The output volume path for the defaced image.

    Returns:
        Parameter dictionary.
    """
    params: MriDeface = {
        "@type": "freesurfer/mri_deface",
        "input_volume": input_volume,
        "brain_template": brain_template,
        "face_template": face_template,
        "output_volume": output_volume,
    }
    return params

def mri_deface_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriDeface."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("brain_template", None) is None:
        raise StyxValidationError("`brain_template` must not be None")
    if not isinstance(params["brain_template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain_template` has the wrong type: Received `{type(params["brain_template"])}` expected `InputPathType`')
    if params.get("face_template", None) is None:
        raise StyxValidationError("`face_template` must not be None")
    if not isinstance(params["face_template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`face_template` has the wrong type: Received `{type(params["face_template"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_deface_cargs(params: MriDeface, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_deface")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["brain_template"]))
    cargs.append(execution.input_file(params["face_template"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_deface_outputs(params: MriDeface, execution: Execution) -> MriDefaceOutputs:
    """Build the MriDefaceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    defaced_output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriDefaceOutputs(
        root=root_v,
        defaced_output_file=defaced_output_file_v,
    )

def mri_deface_execute(params: MriDeface, runner: Runner | None = None) -> MriDefaceOutputs:
    """
    mri_deface

    MRI Deface utility for removing facial features from MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_deface_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_DEFACE_METADATA)
    execution.params(params)
    args = mri_deface_cargs(params, execution)
    out = mri_deface_outputs(params, execution)
    execution.run(args)
    return out

def mri_deface(
    input_volume: InputPathType,
    brain_template: InputPathType,
    face_template: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriDefaceOutputs:
    """
    mri_deface

    MRI Deface utility for removing facial features from MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: The input volume to be defaced (e.g. anatomical MRI image).
        brain_template: Template volume of the brain to be used for defacing.
        face_template: Template volume of the face to be used for defacing.
        output_volume: The output volume path for the defaced image.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_deface_params(
        input_volume=input_volume,
        brain_template=brain_template,
        face_template=face_template,
        output_volume=output_volume,
    )
    return mri_deface_execute(params, runner)

__all__ = [
    "MriDeface",
    "MriDefaceOutputs",
    "MRI_DEFACE_METADATA",
    "mri_deface_cargs",
    "mri_deface_outputs",
    "mri_deface_params",
    "mri_deface_execute",
    "mri_deface_validate",
    "mri_deface",
]