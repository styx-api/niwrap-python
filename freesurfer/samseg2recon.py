# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SAMSEG2RECON_METADATA = Metadata(
    id="samseg2recon",
    name="samseg2recon",
    package="freesurfer",
)

Samseg2recon = typing.TypedDict(
    "Samseg2recon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/samseg2recon"]],
        "subject": str,
        "samseg_dir": typing.NotRequired[str],
        "no_cc": typing.NotRequired[bool],
        "fill": typing.NotRequired[bool],
        "normalization2": typing.NotRequired[bool],
        "uchar": typing.NotRequired[bool],
        "no_keep_exc": typing.NotRequired[bool],
        "long_tp": typing.NotRequired[float],
        "base": typing.NotRequired[bool],
        "mask_file": typing.NotRequired[InputPathType],
        "from_recon_all": typing.NotRequired[bool],
        "force_update": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Samseg2reconOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    filled_mgz: OutputPathType
    """Filled MRI volume for the subject, if --fill is used."""
    norm_mgz: OutputPathType
    """Normalized MRI volume created from brain.mgz if --normalization2 is used."""
    orig_mgz: OutputPathType
    """Original MRI conformed volume."""

def samseg2recon_params(
    subject: str,
    samseg_dir: str | None = None,
    no_cc: bool = False,
    fill: bool = False,
    normalization2: bool = False,
    uchar: bool = False,
    no_keep_exc: bool = False,
    long_tp: float | None = None,
    base: bool = False,
    mask_file: InputPathType | None = None,
    from_recon_all: bool = False,
    force_update: bool = False,
) -> Samseg2recon:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        samseg_dir: Output directory from samseg or samseg-long. Defaults to\
            subject/mri/samseg if not supplied
        no_cc: Do not do corpus callosum segmentation
        fill: Use samseg to create the filled.mgz used for tessellation
        normalization2: Seg brain.mgz to norm.mgz
        uchar: Convert to uchar
        no_keep_exc: Do not keep extracerebral segmentations
        long_tp: Process specific time point (TP) number from samsegdir
        base: Process base, will find folder called base in samsegdir
        mask_file: Use provided mask as brainmask instead of computing from seg
        from_recon_all: Indicates execution from recon-all, preventing overwrite of\
            rawavg.mgz and orig.mgz
        force_update: Force update of the subject directory

    Returns:
        Parameter dictionary.
    """
    params: Samseg2recon = {
        "@type": "freesurfer/samseg2recon",
        "subject": subject,
        "no_cc": no_cc,
        "fill": fill,
        "normalization2": normalization2,
        "uchar": uchar,
        "no_keep_exc": no_keep_exc,
        "base": base,
        "from_recon_all": from_recon_all,
        "force_update": force_update,
    }
    if samseg_dir is not None:
        params["samseg_dir"] = samseg_dir
    if long_tp is not None:
        params["long_tp"] = long_tp
    if mask_file is not None:
        params["mask_file"] = mask_file
    return params

def samseg2recon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Samseg2recon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("samseg_dir", None) is not None:
        if not isinstance(params["samseg_dir"], str):
            raise StyxValidationError(f'`samseg_dir` has the wrong type: Received `{type(params["samseg_dir"])}` expected `str`')
    if params.get("no_cc", None) is not None:
        if not isinstance(params["no_cc"], bool):
            raise StyxValidationError(f'`no_cc` has the wrong type: Received `{type(params["no_cc"])}` expected `bool`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], bool):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `bool`')
    if params.get("normalization2", None) is not None:
        if not isinstance(params["normalization2"], bool):
            raise StyxValidationError(f'`normalization2` has the wrong type: Received `{type(params["normalization2"])}` expected `bool`')
    if params.get("uchar", None) is not None:
        if not isinstance(params["uchar"], bool):
            raise StyxValidationError(f'`uchar` has the wrong type: Received `{type(params["uchar"])}` expected `bool`')
    if params.get("no_keep_exc", None) is not None:
        if not isinstance(params["no_keep_exc"], bool):
            raise StyxValidationError(f'`no_keep_exc` has the wrong type: Received `{type(params["no_keep_exc"])}` expected `bool`')
    if params.get("long_tp", None) is not None:
        if not isinstance(params["long_tp"], (float, int)):
            raise StyxValidationError(f'`long_tp` has the wrong type: Received `{type(params["long_tp"])}` expected `float`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], bool):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `bool`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("from_recon_all", None) is not None:
        if not isinstance(params["from_recon_all"], bool):
            raise StyxValidationError(f'`from_recon_all` has the wrong type: Received `{type(params["from_recon_all"])}` expected `bool`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')

def samseg2recon_cargs(params: Samseg2recon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("samseg2recon")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("samseg_dir")
    if v_0 is not None:
        cargs.append("--samseg")
        cargs.append(v_0)
    if params.get("no_cc"):
        cargs.append("--no-cc")
    if params.get("fill"):
        cargs.append("--fill")
    if params.get("normalization2"):
        cargs.append("--normalization2")
    if params.get("uchar"):
        cargs.append("--uchar")
    if params.get("no_keep_exc"):
        cargs.append("--no-keep-exc")
    v_1 = params.get("long_tp")
    if v_1 is not None:
        cargs.append("--long")
        cargs.append(str(v_1))
    if params.get("base"):
        cargs.append("--base")
    v_2 = params.get("mask_file")
    if v_2 is not None:
        cargs.append("--m")
        cargs.append(execution.input_file(v_2))
    if params.get("from_recon_all"):
        cargs.append("--from-recon-all")
    if params.get("force_update"):
        cargs.append("--force-update")
    return cargs

def samseg2recon_outputs(params: Samseg2recon, execution: Execution) -> Samseg2reconOutputs:
    """Build the Samseg2reconOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    filled_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/filled.mgz')
    norm_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/norm.mgz')
    orig_mgz_v: OutputPathType = execution.output_file(f'{params["subject"]}/mri/orig/001.mgz')
    return Samseg2reconOutputs(
        root=root_v,
        filled_mgz=filled_mgz_v,
        norm_mgz=norm_mgz_v,
        orig_mgz=orig_mgz_v,
    )

def samseg2recon_execute(params: Samseg2recon, runner: Runner | None = None) -> Samseg2reconOutputs:
    """
    samseg2recon

    Creates and populates a subjects directory for use with recon-all from SAMSEG outputs.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    samseg2recon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SAMSEG2RECON_METADATA)
    execution.params(params)
    args = samseg2recon_cargs(params, execution)
    out = samseg2recon_outputs(params, execution)
    execution.run(args)
    return out

def samseg2recon(
    subject: str,
    samseg_dir: str | None = None,
    no_cc: bool = False,
    fill: bool = False,
    normalization2: bool = False,
    uchar: bool = False,
    no_keep_exc: bool = False,
    long_tp: float | None = None,
    base: bool = False,
    mask_file: InputPathType | None = None,
    from_recon_all: bool = False,
    force_update: bool = False,
    runner: Runner | None = None,
) -> Samseg2reconOutputs:
    """
    samseg2recon

    Creates and populates a subjects directory for use with recon-all from SAMSEG outputs.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        samseg_dir: Output directory from samseg or samseg-long. Defaults to\
            subject/mri/samseg if not supplied
        no_cc: Do not do corpus callosum segmentation
        fill: Use samseg to create the filled.mgz used for tessellation
        normalization2: Seg brain.mgz to norm.mgz
        uchar: Convert to uchar
        no_keep_exc: Do not keep extracerebral segmentations
        long_tp: Process specific time point (TP) number from samsegdir
        base: Process base, will find folder called base in samsegdir
        mask_file: Use provided mask as brainmask instead of computing from seg
        from_recon_all: Indicates execution from recon-all, preventing overwrite of\
            rawavg.mgz and orig.mgz
        force_update: Force update of the subject directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = samseg2recon_params(
        subject=subject,
        samseg_dir=samseg_dir,
        no_cc=no_cc,
        fill=fill,
        normalization2=normalization2,
        uchar=uchar,
        no_keep_exc=no_keep_exc,
        long_tp=long_tp,
        base=base,
        mask_file=mask_file,
        from_recon_all=from_recon_all,
        force_update=force_update,
    )
    return samseg2recon_execute(params, runner)

__all__ = [
    "Samseg2recon",
    "Samseg2reconOutputs",
    "SAMSEG2RECON_METADATA",
    "samseg2recon_cargs",
    "samseg2recon_outputs",
    "samseg2recon_params",
    "samseg2recon_execute",
    "samseg2recon_validate",
    "samseg2recon",
]