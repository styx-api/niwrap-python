# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_HEAD_METADATA = Metadata(
    id="mri_head",
    name="mri_head",
    package="freesurfer",
)

MriHead = typing.TypedDict(
    "MriHead",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_head"]],
        "identify": typing.NotRequired[bool],
        "read": typing.NotRequired[bool],
        "filename": typing.NotRequired[str],
        "help": typing.NotRequired[bool],
        "usage": typing.NotRequired[bool],
        "question_mark_help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriHeadOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_head_params(
    identify: bool = False,
    read: bool = False,
    filename: str | None = None,
    help: bool = False,
    usage: bool = False,
    question_mark_help: bool = False,
) -> MriHead:
    """
    Build parameters.

    Args:
        identify: Identify the MRI file
        read: Read the MRI file
        filename: Filename for identification or reading
        help: Display help information
        usage: Display usage information
        question_mark_help: Display help or usage information

    Returns:
        Parameter dictionary.
    """
    params: MriHead = {
        "@type": "freesurfer/mri_head",
        "identify": identify,
        "read": read,
        "help": help,
        "usage": usage,
        "question_mark_help": question_mark_help,
    }
    if filename is not None:
        params["filename"] = filename
    return params

def mri_head_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriHead."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("identify", None) is not None:
        if not isinstance(params["identify"], bool):
            raise StyxValidationError(f'`identify` has the wrong type: Received `{type(params["identify"])}` expected `bool`')
    if params.get("read", None) is not None:
        if not isinstance(params["read"], bool):
            raise StyxValidationError(f'`read` has the wrong type: Received `{type(params["read"])}` expected `bool`')
    if params.get("filename", None) is not None:
        if not isinstance(params["filename"], str):
            raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("usage", None) is not None:
        if not isinstance(params["usage"], bool):
            raise StyxValidationError(f'`usage` has the wrong type: Received `{type(params["usage"])}` expected `bool`')
    if params.get("question_mark_help", None) is not None:
        if not isinstance(params["question_mark_help"], bool):
            raise StyxValidationError(f'`question_mark_help` has the wrong type: Received `{type(params["question_mark_help"])}` expected `bool`')

def mri_head_cargs(params: MriHead, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_head")
    if params.get("identify"):
        cargs.append("-identify")
    if params.get("read"):
        cargs.append("-read")
    v_0 = params.get("filename")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("help"):
        cargs.append("-h")
    if params.get("usage"):
        cargs.append("-u")
    if params.get("question_mark_help"):
        cargs.append("-?")
    return cargs

def mri_head_outputs(params: MriHead, execution: Execution) -> MriHeadOutputs:
    """Build the MriHeadOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriHeadOutputs(
        root=root_v,
    )

def mri_head_execute(params: MriHead, runner: Runner | None = None) -> MriHeadOutputs:
    """
    mri_head

    No description

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_head_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_HEAD_METADATA)
    execution.params(params)
    args = mri_head_cargs(params, execution)
    out = mri_head_outputs(params, execution)
    execution.run(args)
    return out

def mri_head(
    identify: bool = False,
    read: bool = False,
    filename: str | None = None,
    help: bool = False,
    usage: bool = False,
    question_mark_help: bool = False,
    runner: Runner | None = None,
) -> MriHeadOutputs:
    """
    mri_head

    No description

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        identify: Identify the MRI file
        read: Read the MRI file
        filename: Filename for identification or reading
        help: Display help information
        usage: Display usage information
        question_mark_help: Display help or usage information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_head_params(
        identify=identify,
        read=read,
        filename=filename,
        help=help,
        usage=usage,
        question_mark_help=question_mark_help,
    )
    return mri_head_execute(params, runner)

__all__ = [
    "MriHead",
    "MriHeadOutputs",
    "MRI_HEAD_METADATA",
    "mri_head_cargs",
    "mri_head_outputs",
    "mri_head_params",
    "mri_head_execute",
    "mri_head_validate",
    "mri_head",
]