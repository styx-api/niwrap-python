# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS2RGB_METADATA = Metadata(
    id="mris2rgb",
    name="mris2rgb",
    package="freesurfer",
)

Mris2rgb = typing.TypedDict(
    "Mris2rgb",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris2rgb"]],
        "library_path": str,
    },
)

@dataclasses.dataclass
class Mris2rgbOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris2rgb_params(
    library_path: str,
) -> Mris2rgb:
    """
    Build parameters.

    Args:
        library_path:

    Returns:
        Parameter dictionary.
    """
    params: Mris2rgb = {
        "@type": "freesurfer/mris2rgb",
        "library_path": library_path,
    }
    return params

def mris2rgb_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mris2rgb."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("library_path", None) is None:
        raise StyxValidationError("`library_path` must not be None")
    if not isinstance(params["library_path"], str):
        raise StyxValidationError(f'`library_path` has the wrong type: Received `{type(params["library_path"])}` expected `str`')

def mris2rgb_cargs(params: Mris2rgb, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris2rgb")
    cargs.append("export LD_LIBRARY_PATH=")
    cargs.append(params["library_path"])
    return cargs

def mris2rgb_outputs(params: Mris2rgb, execution: Execution) -> Mris2rgbOutputs:
    """Build the Mris2rgbOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Mris2rgbOutputs(
        root=root_v,
    )

def mris2rgb_execute(params: Mris2rgb, runner: Runner | None = None) -> Mris2rgbOutputs:
    """
    mris2rgb

    A tool from FreeSurfer for converting MRI surface files to RGB images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris2rgb_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS2RGB_METADATA)
    execution.params(params)
    args = mris2rgb_cargs(params, execution)
    out = mris2rgb_outputs(params, execution)
    execution.run(args)
    return out

def mris2rgb(
    library_path: str,
    runner: Runner | None = None,
) -> Mris2rgbOutputs:
    """
    mris2rgb

    A tool from FreeSurfer for converting MRI surface files to RGB images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        library_path:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris2rgb_params(
        library_path=library_path,
    )
    return mris2rgb_execute(params, runner)

__all__ = [
    "Mris2rgb",
    "Mris2rgbOutputs",
    "MRIS2RGB_METADATA",
    "mris2rgb_cargs",
    "mris2rgb_outputs",
    "mris2rgb_params",
    "mris2rgb_execute",
    "mris2rgb_validate",
    "mris2rgb",
]