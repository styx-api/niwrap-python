# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKHEADSURF_METADATA = Metadata(
    id="mkheadsurf",
    name="mkheadsurf",
    package="freesurfer",
)

Mkheadsurf = typing.TypedDict(
    "Mkheadsurf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mkheadsurf"]],
        "input_vol": InputPathType,
        "output_vol": str,
        "output_surf": str,
        "subject_id": str,
        "nsmooth": typing.NotRequired[float],
        "noseghead": typing.NotRequired[bool],
        "thresh1": typing.NotRequired[float],
        "thresh2": typing.NotRequired[float],
        "nhitsmin": typing.NotRequired[float],
        "ndilate": typing.NotRequired[float],
        "nerode": typing.NotRequired[float],
        "fillval": typing.NotRequired[float],
        "fhi": typing.NotRequired[float],
        "no_rescale": typing.NotRequired[bool],
        "no_fill_holes_islands": typing.NotRequired[bool],
        "or_mask": typing.NotRequired[InputPathType],
        "tessellation_method": typing.NotRequired[str],
        "inflate": typing.NotRequired[bool],
        "curv": typing.NotRequired[bool],
        "srcvol": typing.NotRequired[str],
        "headvol": typing.NotRequired[str],
        "headsurf": typing.NotRequired[str],
        "smheadsurf": typing.NotRequired[str],
        "hemi": typing.NotRequired[str],
        "subjects_dir": typing.NotRequired[str],
        "umask": typing.NotRequired[float],
        "logfile": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MkheadsurfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mkheadsurf_params(
    input_vol: InputPathType,
    output_vol: str,
    output_surf: str,
    subject_id: str,
    nsmooth: float | None = None,
    noseghead: bool = False,
    thresh1: float | None = None,
    thresh2: float | None = None,
    nhitsmin: float | None = None,
    ndilate: float | None = None,
    nerode: float | None = None,
    fillval: float | None = None,
    fhi: float | None = None,
    no_rescale: bool = False,
    no_fill_holes_islands: bool = False,
    or_mask: InputPathType | None = None,
    tessellation_method: str | None = None,
    inflate: bool = False,
    curv: bool = False,
    srcvol: str | None = None,
    headvol: str | None = None,
    headsurf: str | None = None,
    smheadsurf: str | None = None,
    hemi: str | None = None,
    subjects_dir: str | None = None,
    umask: float | None = None,
    logfile: str | None = None,
) -> Mkheadsurf:
    """
    Build parameters.

    Args:
        input_vol: Input volume
        output_vol: Output volume
        output_surf: Output surface
        subject_id: Subject ID
        nsmooth: Number of smoothing iterations (default 10)
        noseghead: Do not segment the head, only tessellate and smooth existing
        thresh1: Threshold 1 value (default 20)
        thresh2: Threshold 2 value (default 20)
        nhitsmin: Minimum number of hits (default 2)
        ndilate: Number of dilations (default 0)
        nerode: Number of erosions (default 0)
        fillval: Fill value (default 1)
        fhi: FHI for MRIchangeType; default is to use default in mri_seghead
        no_rescale: Do not rescale input when converting to uchar
        no_fill_holes_islands: Do not fill holes and remove islands
        or_mask: Include all voxels in ormask in the head seg
        tessellation_method: Tessellation method using mri_tessellate or mri_mc\
            (default is -mc)
        inflate: Inflate and compute sulc
        curv: Compute curv with smoothing
        srcvol: Source volume ID (default is T1)
        headvol: Head volume ID (default is seghead)
        headsurf: Head surface ID (default is seghead)
        smheadsurf: Smoothed head surface ID (default is smseghead)
        hemi: Hemisphere (default is lh)
        subjects_dir: Subjects directory (default is SUBJECTS_DIR)
        umask: Umask setting (default is 2)
        logfile: Log file

    Returns:
        Parameter dictionary.
    """
    params: Mkheadsurf = {
        "@type": "freesurfer/mkheadsurf",
        "input_vol": input_vol,
        "output_vol": output_vol,
        "output_surf": output_surf,
        "subject_id": subject_id,
        "noseghead": noseghead,
        "no_rescale": no_rescale,
        "no_fill_holes_islands": no_fill_holes_islands,
        "inflate": inflate,
        "curv": curv,
    }
    if nsmooth is not None:
        params["nsmooth"] = nsmooth
    if thresh1 is not None:
        params["thresh1"] = thresh1
    if thresh2 is not None:
        params["thresh2"] = thresh2
    if nhitsmin is not None:
        params["nhitsmin"] = nhitsmin
    if ndilate is not None:
        params["ndilate"] = ndilate
    if nerode is not None:
        params["nerode"] = nerode
    if fillval is not None:
        params["fillval"] = fillval
    if fhi is not None:
        params["fhi"] = fhi
    if or_mask is not None:
        params["or_mask"] = or_mask
    if tessellation_method is not None:
        params["tessellation_method"] = tessellation_method
    if srcvol is not None:
        params["srcvol"] = srcvol
    if headvol is not None:
        params["headvol"] = headvol
    if headsurf is not None:
        params["headsurf"] = headsurf
    if smheadsurf is not None:
        params["smheadsurf"] = smheadsurf
    if hemi is not None:
        params["hemi"] = hemi
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if umask is not None:
        params["umask"] = umask
    if logfile is not None:
        params["logfile"] = logfile
    return params

def mkheadsurf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mkheadsurf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_vol", None) is None:
        raise StyxValidationError("`input_vol` must not be None")
    if not isinstance(params["input_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vol` has the wrong type: Received `{type(params["input_vol"])}` expected `InputPathType`')
    if params.get("output_vol", None) is None:
        raise StyxValidationError("`output_vol` must not be None")
    if not isinstance(params["output_vol"], str):
        raise StyxValidationError(f'`output_vol` has the wrong type: Received `{type(params["output_vol"])}` expected `str`')
    if params.get("output_surf", None) is None:
        raise StyxValidationError("`output_surf` must not be None")
    if not isinstance(params["output_surf"], str):
        raise StyxValidationError(f'`output_surf` has the wrong type: Received `{type(params["output_surf"])}` expected `str`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("nsmooth", None) is not None:
        if not isinstance(params["nsmooth"], (float, int)):
            raise StyxValidationError(f'`nsmooth` has the wrong type: Received `{type(params["nsmooth"])}` expected `float`')
    if params.get("noseghead", None) is not None:
        if not isinstance(params["noseghead"], bool):
            raise StyxValidationError(f'`noseghead` has the wrong type: Received `{type(params["noseghead"])}` expected `bool`')
    if params.get("thresh1", None) is not None:
        if not isinstance(params["thresh1"], (float, int)):
            raise StyxValidationError(f'`thresh1` has the wrong type: Received `{type(params["thresh1"])}` expected `float`')
    if params.get("thresh2", None) is not None:
        if not isinstance(params["thresh2"], (float, int)):
            raise StyxValidationError(f'`thresh2` has the wrong type: Received `{type(params["thresh2"])}` expected `float`')
    if params.get("nhitsmin", None) is not None:
        if not isinstance(params["nhitsmin"], (float, int)):
            raise StyxValidationError(f'`nhitsmin` has the wrong type: Received `{type(params["nhitsmin"])}` expected `float`')
    if params.get("ndilate", None) is not None:
        if not isinstance(params["ndilate"], (float, int)):
            raise StyxValidationError(f'`ndilate` has the wrong type: Received `{type(params["ndilate"])}` expected `float`')
    if params.get("nerode", None) is not None:
        if not isinstance(params["nerode"], (float, int)):
            raise StyxValidationError(f'`nerode` has the wrong type: Received `{type(params["nerode"])}` expected `float`')
    if params.get("fillval", None) is not None:
        if not isinstance(params["fillval"], (float, int)):
            raise StyxValidationError(f'`fillval` has the wrong type: Received `{type(params["fillval"])}` expected `float`')
    if params.get("fhi", None) is not None:
        if not isinstance(params["fhi"], (float, int)):
            raise StyxValidationError(f'`fhi` has the wrong type: Received `{type(params["fhi"])}` expected `float`')
    if params.get("no_rescale", None) is not None:
        if not isinstance(params["no_rescale"], bool):
            raise StyxValidationError(f'`no_rescale` has the wrong type: Received `{type(params["no_rescale"])}` expected `bool`')
    if params.get("no_fill_holes_islands", None) is not None:
        if not isinstance(params["no_fill_holes_islands"], bool):
            raise StyxValidationError(f'`no_fill_holes_islands` has the wrong type: Received `{type(params["no_fill_holes_islands"])}` expected `bool`')
    if params.get("or_mask", None) is not None:
        if not isinstance(params["or_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`or_mask` has the wrong type: Received `{type(params["or_mask"])}` expected `InputPathType`')
    if params.get("tessellation_method", None) is not None:
        if not isinstance(params["tessellation_method"], str):
            raise StyxValidationError(f'`tessellation_method` has the wrong type: Received `{type(params["tessellation_method"])}` expected `str`')
    if params.get("inflate", None) is not None:
        if not isinstance(params["inflate"], bool):
            raise StyxValidationError(f'`inflate` has the wrong type: Received `{type(params["inflate"])}` expected `bool`')
    if params.get("curv", None) is not None:
        if not isinstance(params["curv"], bool):
            raise StyxValidationError(f'`curv` has the wrong type: Received `{type(params["curv"])}` expected `bool`')
    if params.get("srcvol", None) is not None:
        if not isinstance(params["srcvol"], str):
            raise StyxValidationError(f'`srcvol` has the wrong type: Received `{type(params["srcvol"])}` expected `str`')
    if params.get("headvol", None) is not None:
        if not isinstance(params["headvol"], str):
            raise StyxValidationError(f'`headvol` has the wrong type: Received `{type(params["headvol"])}` expected `str`')
    if params.get("headsurf", None) is not None:
        if not isinstance(params["headsurf"], str):
            raise StyxValidationError(f'`headsurf` has the wrong type: Received `{type(params["headsurf"])}` expected `str`')
    if params.get("smheadsurf", None) is not None:
        if not isinstance(params["smheadsurf"], str):
            raise StyxValidationError(f'`smheadsurf` has the wrong type: Received `{type(params["smheadsurf"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], (float, int)):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `float`')
    if params.get("logfile", None) is not None:
        if not isinstance(params["logfile"], str):
            raise StyxValidationError(f'`logfile` has the wrong type: Received `{type(params["logfile"])}` expected `str`')

def mkheadsurf_cargs(params: Mkheadsurf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mkheadsurf")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_vol"]))
    cargs.append("-o")
    cargs.append(params["output_vol"])
    cargs.append("-surf")
    cargs.append(params["output_surf"])
    cargs.append("-s")
    cargs.append(params["subject_id"])
    v_0 = params.get("nsmooth")
    if v_0 is not None:
        cargs.append("-nsmooth")
        cargs.append(str(v_0))
    if params.get("noseghead"):
        cargs.append("-noseghead")
    v_1 = params.get("thresh1")
    if v_1 is not None:
        cargs.append("-thresh1")
        cargs.append(str(v_1))
    v_2 = params.get("thresh2")
    if v_2 is not None:
        cargs.append("-thresh2")
        cargs.append(str(v_2))
    v_3 = params.get("nhitsmin")
    if v_3 is not None:
        cargs.append("-nhitsmin")
        cargs.append(str(v_3))
    v_4 = params.get("ndilate")
    if v_4 is not None:
        cargs.append("-ndilate")
        cargs.append(str(v_4))
    v_5 = params.get("nerode")
    if v_5 is not None:
        cargs.append("-nerode")
        cargs.append(str(v_5))
    v_6 = params.get("fillval")
    if v_6 is not None:
        cargs.append("-fillval")
        cargs.append(str(v_6))
    v_7 = params.get("fhi")
    if v_7 is not None:
        cargs.append("-fhi")
        cargs.append(str(v_7))
    if params.get("no_rescale"):
        cargs.append("-no-rescale")
    if params.get("no_fill_holes_islands"):
        cargs.append("-no-fill-holes-islands")
    v_8 = params.get("or_mask")
    if v_8 is not None:
        cargs.append("-or-mask")
        cargs.append(execution.input_file(v_8))
    v_9 = params.get("tessellation_method")
    if v_9 is not None:
        cargs.append("-tess")
        cargs.append(v_9)
    if params.get("inflate"):
        cargs.append("-inflate")
    if params.get("curv"):
        cargs.append("-curv")
    v_10 = params.get("srcvol")
    if v_10 is not None:
        cargs.append("-srcvol")
        cargs.append(v_10)
    v_11 = params.get("headvol")
    if v_11 is not None:
        cargs.append("-headvol")
        cargs.append(v_11)
    v_12 = params.get("headsurf")
    if v_12 is not None:
        cargs.append("-headsurf")
        cargs.append(v_12)
    v_13 = params.get("smheadsurf")
    if v_13 is not None:
        cargs.append("-smheadsurf")
        cargs.append(v_13)
    v_14 = params.get("hemi")
    if v_14 is not None:
        cargs.append("-hemi")
        cargs.append(v_14)
    v_15 = params.get("subjects_dir")
    if v_15 is not None:
        cargs.append("-sd")
        cargs.append(v_15)
    v_16 = params.get("umask")
    if v_16 is not None:
        cargs.append("-umask")
        cargs.append(str(v_16))
    v_17 = params.get("logfile")
    if v_17 is not None:
        cargs.append("-log")
        cargs.append(v_17)
    return cargs

def mkheadsurf_outputs(params: Mkheadsurf, execution: Execution) -> MkheadsurfOutputs:
    """Build the MkheadsurfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MkheadsurfOutputs(
        root=root_v,
    )

def mkheadsurf_execute(params: Mkheadsurf, runner: Runner | None = None) -> MkheadsurfOutputs:
    """
    mkheadsurf

    Segment and create a surface representation of the head for visualization and further processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mkheadsurf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKHEADSURF_METADATA)
    execution.params(params)
    args = mkheadsurf_cargs(params, execution)
    out = mkheadsurf_outputs(params, execution)
    execution.run(args)
    return out

def mkheadsurf(
    input_vol: InputPathType,
    output_vol: str,
    output_surf: str,
    subject_id: str,
    nsmooth: float | None = None,
    noseghead: bool = False,
    thresh1: float | None = None,
    thresh2: float | None = None,
    nhitsmin: float | None = None,
    ndilate: float | None = None,
    nerode: float | None = None,
    fillval: float | None = None,
    fhi: float | None = None,
    no_rescale: bool = False,
    no_fill_holes_islands: bool = False,
    or_mask: InputPathType | None = None,
    tessellation_method: str | None = None,
    inflate: bool = False,
    curv: bool = False,
    srcvol: str | None = None,
    headvol: str | None = None,
    headsurf: str | None = None,
    smheadsurf: str | None = None,
    hemi: str | None = None,
    subjects_dir: str | None = None,
    umask: float | None = None,
    logfile: str | None = None,
    runner: Runner | None = None,
) -> MkheadsurfOutputs:
    """
    mkheadsurf

    Segment and create a surface representation of the head for visualization and further processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_vol: Input volume
        output_vol: Output volume
        output_surf: Output surface
        subject_id: Subject ID
        nsmooth: Number of smoothing iterations (default 10)
        noseghead: Do not segment the head, only tessellate and smooth existing
        thresh1: Threshold 1 value (default 20)
        thresh2: Threshold 2 value (default 20)
        nhitsmin: Minimum number of hits (default 2)
        ndilate: Number of dilations (default 0)
        nerode: Number of erosions (default 0)
        fillval: Fill value (default 1)
        fhi: FHI for MRIchangeType; default is to use default in mri_seghead
        no_rescale: Do not rescale input when converting to uchar
        no_fill_holes_islands: Do not fill holes and remove islands
        or_mask: Include all voxels in ormask in the head seg
        tessellation_method: Tessellation method using mri_tessellate or mri_mc\
            (default is -mc)
        inflate: Inflate and compute sulc
        curv: Compute curv with smoothing
        srcvol: Source volume ID (default is T1)
        headvol: Head volume ID (default is seghead)
        headsurf: Head surface ID (default is seghead)
        smheadsurf: Smoothed head surface ID (default is smseghead)
        hemi: Hemisphere (default is lh)
        subjects_dir: Subjects directory (default is SUBJECTS_DIR)
        umask: Umask setting (default is 2)
        logfile: Log file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mkheadsurf_params(
        input_vol=input_vol,
        output_vol=output_vol,
        output_surf=output_surf,
        subject_id=subject_id,
        nsmooth=nsmooth,
        noseghead=noseghead,
        thresh1=thresh1,
        thresh2=thresh2,
        nhitsmin=nhitsmin,
        ndilate=ndilate,
        nerode=nerode,
        fillval=fillval,
        fhi=fhi,
        no_rescale=no_rescale,
        no_fill_holes_islands=no_fill_holes_islands,
        or_mask=or_mask,
        tessellation_method=tessellation_method,
        inflate=inflate,
        curv=curv,
        srcvol=srcvol,
        headvol=headvol,
        headsurf=headsurf,
        smheadsurf=smheadsurf,
        hemi=hemi,
        subjects_dir=subjects_dir,
        umask=umask,
        logfile=logfile,
    )
    return mkheadsurf_execute(params, runner)

__all__ = [
    "Mkheadsurf",
    "MkheadsurfOutputs",
    "MKHEADSURF_METADATA",
    "mkheadsurf_cargs",
    "mkheadsurf_outputs",
    "mkheadsurf_params",
    "mkheadsurf_execute",
    "mkheadsurf_validate",
    "mkheadsurf",
]