# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_MOTION_METADATA = Metadata(
    id="dmri_motion",
    name="dmri_motion",
    package="freesurfer",
)

DmriMotion = typing.TypedDict(
    "DmriMotion",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_motion"]],
        "outfile": InputPathType,
        "outf": typing.NotRequired[InputPathType],
        "mat": typing.NotRequired[InputPathType],
        "dwi": typing.NotRequired[list[InputPathType]],
        "bval": typing.NotRequired[list[InputPathType]],
        "threshold": typing.NotRequired[float],
        "diffusivity": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriMotionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    motion_measures_out: OutputPathType
    """Output file of motion measures"""
    frame_by_frame_out: OutputPathType | None
    """Output frame-by-frame motion measures file"""

def dmri_motion_params(
    outfile: InputPathType,
    outf: InputPathType | None = None,
    mat: InputPathType | None = None,
    dwi: list[InputPathType] | None = None,
    bval: list[InputPathType] | None = None,
    threshold: float | None = None,
    diffusivity: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
    version: bool = False,
) -> DmriMotion:
    """
    Build parameters.

    Args:
        outfile: Output text file of motion measures
        outf: Output text file of frame-by-frame motion measures
        mat: Input text file of volume-to-baseline affine transformations
        dwi: Input DWI scan(s), unprocessed
        bval: Input b-value table(s), one per scan
        threshold: Low-b image intensity threshold
        diffusivity: Nominal diffusivity
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit
        help: Print out information on how to use this program
        version: Print out version and exit

    Returns:
        Parameter dictionary.
    """
    params: DmriMotion = {
        "@type": "freesurfer/dmri_motion",
        "outfile": outfile,
        "debug": debug,
        "checkopts": checkopts,
        "help": help,
        "version": version,
    }
    if outf is not None:
        params["outf"] = outf
    if mat is not None:
        params["mat"] = mat
    if dwi is not None:
        params["dwi"] = dwi
    if bval is not None:
        params["bval"] = bval
    if threshold is not None:
        params["threshold"] = threshold
    if diffusivity is not None:
        params["diffusivity"] = diffusivity
    return params

def dmri_motion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriMotion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `InputPathType`')
    if params.get("outf", None) is not None:
        if not isinstance(params["outf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`outf` has the wrong type: Received `{type(params["outf"])}` expected `InputPathType`')
    if params.get("mat", None) is not None:
        if not isinstance(params["mat"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mat` has the wrong type: Received `{type(params["mat"])}` expected `InputPathType`')
    if params.get("dwi", None) is not None:
        if not isinstance(params["dwi"], list):
            raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(params["dwi"])}` expected `list[InputPathType]`')
        for e in params["dwi"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("bval", None) is not None:
        if not isinstance(params["bval"], list):
            raise StyxValidationError(f'`bval` has the wrong type: Received `{type(params["bval"])}` expected `list[InputPathType]`')
        for e_2 in params["bval"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`bval` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("diffusivity", None) is not None:
        if not isinstance(params["diffusivity"], (float, int)):
            raise StyxValidationError(f'`diffusivity` has the wrong type: Received `{type(params["diffusivity"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def dmri_motion_cargs(params: DmriMotion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_motion")
    cargs.append("--out")
    cargs.append(execution.input_file(params["outfile"]))
    v_0 = params.get("outf")
    if v_0 is not None:
        cargs.append("--outf")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mat")
    if v_1 is not None:
        cargs.append("--mat")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("dwi")
    if v_2 is not None:
        cargs.append("--dwi")
        for item0 in v_2:
            cargs.append(execution.input_file(item0))
    v_3 = params.get("bval")
    if v_3 is not None:
        cargs.append("--bval")
        for item1 in v_3:
            cargs.append(execution.input_file(item1))
    v_4 = params.get("threshold")
    if v_4 is not None:
        cargs.append("--T")
        cargs.append(str(v_4))
    v_5 = params.get("diffusivity")
    if v_5 is not None:
        cargs.append("--D")
        cargs.append(str(v_5))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def dmri_motion_outputs(params: DmriMotion, execution: Execution) -> DmriMotionOutputs:
    """Build the DmriMotionOutputs object for this tool."""
    frame_by_frame_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    motion_measures_out_v: OutputPathType = execution.output_file(params["outfile"])
    __v0 = params.get("outf")
    if __v0 is not None:
        frame_by_frame_out_v = execution.output_file((__v0 if __v0 is not None else ""))
    return DmriMotionOutputs(
        root=root_v,
        motion_measures_out=motion_measures_out_v,
        frame_by_frame_out=frame_by_frame_out_v,
    )

def dmri_motion_execute(params: DmriMotion, runner: Runner | None = None) -> DmriMotionOutputs:
    """
    dmri_motion

    A tool for calculating motion measures from DWI scans.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_motion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_MOTION_METADATA)
    execution.params(params)
    args = dmri_motion_cargs(params, execution)
    out = dmri_motion_outputs(params, execution)
    execution.run(args)
    return out

def dmri_motion(
    outfile: InputPathType,
    outf: InputPathType | None = None,
    mat: InputPathType | None = None,
    dwi: list[InputPathType] | None = None,
    bval: list[InputPathType] | None = None,
    threshold: float | None = None,
    diffusivity: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DmriMotionOutputs:
    """
    dmri_motion

    A tool for calculating motion measures from DWI scans.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        outfile: Output text file of motion measures
        outf: Output text file of frame-by-frame motion measures
        mat: Input text file of volume-to-baseline affine transformations
        dwi: Input DWI scan(s), unprocessed
        bval: Input b-value table(s), one per scan
        threshold: Low-b image intensity threshold
        diffusivity: Nominal diffusivity
        debug: Turn on debugging
        checkopts: Don't run anything, just check options and exit
        help: Print out information on how to use this program
        version: Print out version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_motion_params(
        outfile=outfile,
        outf=outf,
        mat=mat,
        dwi=dwi,
        bval=bval,
        threshold=threshold,
        diffusivity=diffusivity,
        debug=debug,
        checkopts=checkopts,
        help=help,
        version=version,
    )
    return dmri_motion_execute(params, runner)

__all__ = [
    "DmriMotion",
    "DmriMotionOutputs",
    "DMRI_MOTION_METADATA",
    "dmri_motion_cargs",
    "dmri_motion_outputs",
    "dmri_motion_params",
    "dmri_motion_execute",
    "dmri_motion_validate",
    "dmri_motion",
]