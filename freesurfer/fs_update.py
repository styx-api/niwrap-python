# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_UPDATE_METADATA = Metadata(
    id="fs_update",
    name="fs_update",
    package="freesurfer",
)

FsUpdate = typing.TypedDict(
    "FsUpdate",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_update"]],
        "update_path": typing.NotRequired[str],
        "help_short": typing.NotRequired[bool],
        "help_medium": typing.NotRequired[bool],
        "help_long": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsUpdateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fs_update_params(
    update_path: str | None = None,
    help_short: bool = False,
    help_medium: bool = False,
    help_long: bool = False,
) -> FsUpdate:
    """
    Build parameters.

    Args:
        update_path: Path to specific files or directories to update, copied\
            recursively
        help_short: Show help
        help_medium:
        help_long:

    Returns:
        Parameter dictionary.
    """
    params: FsUpdate = {
        "@type": "freesurfer/fs_update",
        "help_short": help_short,
        "help_medium": help_medium,
        "help_long": help_long,
    }
    if update_path is not None:
        params["update_path"] = update_path
    return params

def fs_update_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsUpdate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("update_path", None) is not None:
        if not isinstance(params["update_path"], str):
            raise StyxValidationError(f'`update_path` has the wrong type: Received `{type(params["update_path"])}` expected `str`')
    if params.get("help_short", None) is not None:
        if not isinstance(params["help_short"], bool):
            raise StyxValidationError(f'`help_short` has the wrong type: Received `{type(params["help_short"])}` expected `bool`')
    if params.get("help_medium", None) is not None:
        if not isinstance(params["help_medium"], bool):
            raise StyxValidationError(f'`help_medium` has the wrong type: Received `{type(params["help_medium"])}` expected `bool`')
    if params.get("help_long", None) is not None:
        if not isinstance(params["help_long"], bool):
            raise StyxValidationError(f'`help_long` has the wrong type: Received `{type(params["help_long"])}` expected `bool`')

def fs_update_cargs(params: FsUpdate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_update")
    v_0 = params.get("update_path")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("help_short"):
        cargs.append("-h")
    if params.get("help_medium"):
        cargs.append("-help")
    if params.get("help_long"):
        cargs.append("--help")
    return cargs

def fs_update_outputs(params: FsUpdate, execution: Execution) -> FsUpdateOutputs:
    """Build the FsUpdateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FsUpdateOutputs(
        root=root_v,
    )

def fs_update_execute(params: FsUpdate, runner: Runner | None = None) -> FsUpdateOutputs:
    """
    fs_update

    Utility to update the FreeSurfer installation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_update_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_UPDATE_METADATA)
    execution.params(params)
    args = fs_update_cargs(params, execution)
    out = fs_update_outputs(params, execution)
    execution.run(args)
    return out

def fs_update(
    update_path: str | None = None,
    help_short: bool = False,
    help_medium: bool = False,
    help_long: bool = False,
    runner: Runner | None = None,
) -> FsUpdateOutputs:
    """
    fs_update

    Utility to update the FreeSurfer installation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        update_path: Path to specific files or directories to update, copied\
            recursively
        help_short: Show help
        help_medium:
        help_long:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_update_params(
        update_path=update_path,
        help_short=help_short,
        help_medium=help_medium,
        help_long=help_long,
    )
    return fs_update_execute(params, runner)

__all__ = [
    "FsUpdate",
    "FsUpdateOutputs",
    "FS_UPDATE_METADATA",
    "fs_update_cargs",
    "fs_update_outputs",
    "fs_update_params",
    "fs_update_execute",
    "fs_update_validate",
    "fs_update",
]