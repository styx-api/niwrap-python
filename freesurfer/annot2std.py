# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANNOT2STD_METADATA = Metadata(
    id="annot2std",
    name="annot2std",
    package="freesurfer",
)

Annot2std = typing.TypedDict(
    "Annot2std",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/annot2std"]],
        "output_annot_path": str,
        "subjects": list[str],
        "fsgd_file": typing.NotRequired[InputPathType],
        "subject_list_file": typing.NotRequired[InputPathType],
        "target": typing.NotRequired[str],
        "left_hemisphere": typing.NotRequired[bool],
        "right_hemisphere": typing.NotRequired[bool],
        "xhemi": typing.NotRequired[bool],
        "surfreg": typing.NotRequired[str],
        "srcsurfreg": typing.NotRequired[str],
        "trgsurfreg": typing.NotRequired[str],
        "annotname": typing.NotRequired[str],
        "aparc": typing.NotRequired[bool],
        "a2009s": typing.NotRequired[bool],
        "segmentation": typing.NotRequired[str],
        "stack": typing.NotRequired[str],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Annot2stdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_annot_file: OutputPathType
    """Main output annotation file"""
    out_prob_map_file: OutputPathType
    """Output annotation probability map file"""
    output_surface_segmentation: OutputPathType | None
    """Output surface segmentation file"""

def annot2std_params(
    output_annot_path: str,
    subjects: list[str],
    fsgd_file: InputPathType | None = None,
    subject_list_file: InputPathType | None = None,
    target: str | None = None,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    xhemi: bool = False,
    surfreg: str | None = None,
    srcsurfreg: str | None = None,
    trgsurfreg: str | None = None,
    annotname: str | None = None,
    aparc: bool = False,
    a2009s: bool = False,
    segmentation: str | None = None,
    stack: str | None = None,
    help: bool = False,
    version: bool = False,
) -> Annot2std:
    """
    Build parameters.

    Args:
        output_annot_path: Full output annotation path (also creates\
            outannotpath.p.mgh)
        subjects: Input subject(s), specify each subject with --s subj
        fsgd_file: FSGD file for group descriptor
        subject_list_file: Subject list file
        target: Target subject (e.g., fsaverage)
        left_hemisphere: Use left hemisphere
        right_hemisphere: Use right hemisphere
        xhemi: For interhemispheric analysis
        surfreg: Surface registration type (default is sphere.reg)
        srcsurfreg: Source surface registration type (default is sphere.reg)
        trgsurfreg: Target surface registration type (default is sphere.reg)
        annotname: Input annotation name (?h.annotname.annot)
        aparc: Annotation name set to aparc
        a2009s: Annotation name set to aparc.a2009s
        segmentation: Save output as a surface segmentation (2 frames, second = p)
        stack: Stack of individual annotations as segmentation
        help: Display help
        version: Display version

    Returns:
        Parameter dictionary.
    """
    params: Annot2std = {
        "@type": "freesurfer/annot2std",
        "output_annot_path": output_annot_path,
        "subjects": subjects,
        "left_hemisphere": left_hemisphere,
        "right_hemisphere": right_hemisphere,
        "xhemi": xhemi,
        "aparc": aparc,
        "a2009s": a2009s,
        "help": help,
        "version": version,
    }
    if fsgd_file is not None:
        params["fsgd_file"] = fsgd_file
    if subject_list_file is not None:
        params["subject_list_file"] = subject_list_file
    if target is not None:
        params["target"] = target
    if surfreg is not None:
        params["surfreg"] = surfreg
    if srcsurfreg is not None:
        params["srcsurfreg"] = srcsurfreg
    if trgsurfreg is not None:
        params["trgsurfreg"] = trgsurfreg
    if annotname is not None:
        params["annotname"] = annotname
    if segmentation is not None:
        params["segmentation"] = segmentation
    if stack is not None:
        params["stack"] = stack
    return params

def annot2std_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Annot2std."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_annot_path", None) is None:
        raise StyxValidationError("`output_annot_path` must not be None")
    if not isinstance(params["output_annot_path"], str):
        raise StyxValidationError(f'`output_annot_path` has the wrong type: Received `{type(params["output_annot_path"])}` expected `str`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("fsgd_file", None) is not None:
        if not isinstance(params["fsgd_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fsgd_file` has the wrong type: Received `{type(params["fsgd_file"])}` expected `InputPathType`')
    if params.get("subject_list_file", None) is not None:
        if not isinstance(params["subject_list_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`subject_list_file` has the wrong type: Received `{type(params["subject_list_file"])}` expected `InputPathType`')
    if params.get("target", None) is not None:
        if not isinstance(params["target"], str):
            raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `str`')
    if params.get("left_hemisphere", None) is not None:
        if not isinstance(params["left_hemisphere"], bool):
            raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(params["left_hemisphere"])}` expected `bool`')
    if params.get("right_hemisphere", None) is not None:
        if not isinstance(params["right_hemisphere"], bool):
            raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(params["right_hemisphere"])}` expected `bool`')
    if params.get("xhemi", None) is not None:
        if not isinstance(params["xhemi"], bool):
            raise StyxValidationError(f'`xhemi` has the wrong type: Received `{type(params["xhemi"])}` expected `bool`')
    if params.get("surfreg", None) is not None:
        if not isinstance(params["surfreg"], str):
            raise StyxValidationError(f'`surfreg` has the wrong type: Received `{type(params["surfreg"])}` expected `str`')
    if params.get("srcsurfreg", None) is not None:
        if not isinstance(params["srcsurfreg"], str):
            raise StyxValidationError(f'`srcsurfreg` has the wrong type: Received `{type(params["srcsurfreg"])}` expected `str`')
    if params.get("trgsurfreg", None) is not None:
        if not isinstance(params["trgsurfreg"], str):
            raise StyxValidationError(f'`trgsurfreg` has the wrong type: Received `{type(params["trgsurfreg"])}` expected `str`')
    if params.get("annotname", None) is not None:
        if not isinstance(params["annotname"], str):
            raise StyxValidationError(f'`annotname` has the wrong type: Received `{type(params["annotname"])}` expected `str`')
    if params.get("aparc", None) is not None:
        if not isinstance(params["aparc"], bool):
            raise StyxValidationError(f'`aparc` has the wrong type: Received `{type(params["aparc"])}` expected `bool`')
    if params.get("a2009s", None) is not None:
        if not isinstance(params["a2009s"], bool):
            raise StyxValidationError(f'`a2009s` has the wrong type: Received `{type(params["a2009s"])}` expected `bool`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], str):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `str`')
    if params.get("stack", None) is not None:
        if not isinstance(params["stack"], str):
            raise StyxValidationError(f'`stack` has the wrong type: Received `{type(params["stack"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def annot2std_cargs(params: Annot2std, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("annot2std")
    cargs.append("--o")
    cargs.append(params["output_annot_path"])
    cargs.append("--s")
    for item0 in params["subjects"]:
        cargs.append(item0)
    v_0 = params.get("fsgd_file")
    if v_0 is not None:
        cargs.append("--fsgd")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("subject_list_file")
    if v_1 is not None:
        cargs.append("--f")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("target")
    if v_2 is not None:
        cargs.append("--t")
        cargs.append(v_2)
    if params.get("left_hemisphere"):
        cargs.append("--lh")
    if params.get("right_hemisphere"):
        cargs.append("--rh")
    if params.get("xhemi"):
        cargs.append("--xhemi")
    v_3 = params.get("surfreg")
    if v_3 is not None:
        cargs.append("--surfreg")
        cargs.append(v_3)
    v_4 = params.get("srcsurfreg")
    if v_4 is not None:
        cargs.append("--srcsurfreg")
        cargs.append(v_4)
    v_5 = params.get("trgsurfreg")
    if v_5 is not None:
        cargs.append("--trgsurfreg")
        cargs.append(v_5)
    v_6 = params.get("annotname")
    if v_6 is not None:
        cargs.append("--a")
        cargs.append(v_6)
    if params.get("aparc"):
        cargs.append("--aparc")
    if params.get("a2009s"):
        cargs.append("--a2009s")
    v_7 = params.get("segmentation")
    if v_7 is not None:
        cargs.append("--seg")
        cargs.append(v_7)
    v_8 = params.get("stack")
    if v_8 is not None:
        cargs.append("--stack")
        cargs.append(v_8)
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def annot2std_outputs(params: Annot2std, execution: Execution) -> Annot2stdOutputs:
    """Build the Annot2stdOutputs object for this tool."""
    output_surface_segmentation_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_annot_file_v: OutputPathType = execution.output_file(params["output_annot_path"])
    out_prob_map_file_v: OutputPathType = execution.output_file(f'{params["output_annot_path"]}.p.mgh')
    __v0 = params.get("segmentation")
    if __v0 is not None:
        output_surface_segmentation_v = execution.output_file((__v0 if __v0 is not None else ""))
    return Annot2stdOutputs(
        root=root_v,
        out_annot_file=out_annot_file_v,
        out_prob_map_file=out_prob_map_file_v,
        output_surface_segmentation=output_surface_segmentation_v,
    )

def annot2std_execute(params: Annot2std, runner: Runner | None = None) -> Annot2stdOutputs:
    """
    annot2std

    Creates an average annotation in a standard space based on transforming the annotations of the individual subjects to the standard space through the surface registration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    annot2std_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANNOT2STD_METADATA)
    execution.params(params)
    args = annot2std_cargs(params, execution)
    out = annot2std_outputs(params, execution)
    execution.run(args)
    return out

def annot2std(
    output_annot_path: str,
    subjects: list[str],
    fsgd_file: InputPathType | None = None,
    subject_list_file: InputPathType | None = None,
    target: str | None = None,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    xhemi: bool = False,
    surfreg: str | None = None,
    srcsurfreg: str | None = None,
    trgsurfreg: str | None = None,
    annotname: str | None = None,
    aparc: bool = False,
    a2009s: bool = False,
    segmentation: str | None = None,
    stack: str | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Annot2stdOutputs:
    """
    annot2std

    Creates an average annotation in a standard space based on transforming the annotations of the individual subjects to the standard space through the surface registration.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_annot_path: Full output annotation path (also creates\
            outannotpath.p.mgh)
        subjects: Input subject(s), specify each subject with --s subj
        fsgd_file: FSGD file for group descriptor
        subject_list_file: Subject list file
        target: Target subject (e.g., fsaverage)
        left_hemisphere: Use left hemisphere
        right_hemisphere: Use right hemisphere
        xhemi: For interhemispheric analysis
        surfreg: Surface registration type (default is sphere.reg)
        srcsurfreg: Source surface registration type (default is sphere.reg)
        trgsurfreg: Target surface registration type (default is sphere.reg)
        annotname: Input annotation name (?h.annotname.annot)
        aparc: Annotation name set to aparc
        a2009s: Annotation name set to aparc.a2009s
        segmentation: Save output as a surface segmentation (2 frames, second = p)
        stack: Stack of individual annotations as segmentation
        help: Display help
        version: Display version
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = annot2std_params(
        output_annot_path=output_annot_path,
        subjects=subjects,
        fsgd_file=fsgd_file,
        subject_list_file=subject_list_file,
        target=target,
        left_hemisphere=left_hemisphere,
        right_hemisphere=right_hemisphere,
        xhemi=xhemi,
        surfreg=surfreg,
        srcsurfreg=srcsurfreg,
        trgsurfreg=trgsurfreg,
        annotname=annotname,
        aparc=aparc,
        a2009s=a2009s,
        segmentation=segmentation,
        stack=stack,
        help=help,
        version=version,
    )
    return annot2std_execute(params, runner)

__all__ = [
    "Annot2std",
    "Annot2stdOutputs",
    "ANNOT2STD_METADATA",
    "annot2std_cargs",
    "annot2std_outputs",
    "annot2std_params",
    "annot2std_execute",
    "annot2std_validate",
    "annot2std",
]