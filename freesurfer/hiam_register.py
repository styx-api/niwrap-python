# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HIAM_REGISTER_METADATA = Metadata(
    id="hiam_register",
    name="hiam_register",
    package="freesurfer",
)

HiamRegister = typing.TypedDict(
    "HiamRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/hiam_register"]],
        "input_surface": InputPathType,
        "average_surface": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class HiamRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_surface: OutputPathType
    """The output surface after registration."""

def hiam_register_params(
    input_surface: InputPathType,
    average_surface: InputPathType,
    output_surface: str,
) -> HiamRegister:
    """
    Build parameters.

    Args:
        input_surface: The input surface to be registered.
        average_surface: The average surface to register against.
        output_surface: The path where the output registered surface will be saved.

    Returns:
        Parameter dictionary.
    """
    params: HiamRegister = {
        "@type": "freesurfer/hiam_register",
        "input_surface": input_surface,
        "average_surface": average_surface,
        "output_surface": output_surface,
    }
    return params

def hiam_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HiamRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("average_surface", None) is None:
        raise StyxValidationError("`average_surface` must not be None")
    if not isinstance(params["average_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`average_surface` has the wrong type: Received `{type(params["average_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def hiam_register_cargs(params: HiamRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("hiam_register")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["average_surface"]))
    cargs.append(params["output_surface"])
    return cargs

def hiam_register_outputs(params: HiamRegister, execution: Execution) -> HiamRegisterOutputs:
    """Build the HiamRegisterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return HiamRegisterOutputs(
        root=root_v,
        registered_surface=registered_surface_v,
    )

def hiam_register_execute(params: HiamRegister, runner: Runner | None = None) -> HiamRegisterOutputs:
    """
    hiam_register

    This program registers a surface with an average surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    hiam_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HIAM_REGISTER_METADATA)
    execution.params(params)
    args = hiam_register_cargs(params, execution)
    out = hiam_register_outputs(params, execution)
    execution.run(args)
    return out

def hiam_register(
    input_surface: InputPathType,
    average_surface: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> HiamRegisterOutputs:
    """
    hiam_register

    This program registers a surface with an average surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: The input surface to be registered.
        average_surface: The average surface to register against.
        output_surface: The path where the output registered surface will be saved.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = hiam_register_params(
        input_surface=input_surface,
        average_surface=average_surface,
        output_surface=output_surface,
    )
    return hiam_register_execute(params, runner)

__all__ = [
    "HiamRegister",
    "HiamRegisterOutputs",
    "HIAM_REGISTER_METADATA",
    "hiam_register_cargs",
    "hiam_register_outputs",
    "hiam_register_params",
    "hiam_register_execute",
    "hiam_register_validate",
    "hiam_register",
]