# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MERGE_PARCELLATIONS_METADATA = Metadata(
    id="mris_merge_parcellations",
    name="mris_merge_parcellations",
    package="freesurfer",
)

MrisMergeParcellations = typing.TypedDict(
    "MrisMergeParcellations",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_merge_parcellations"]],
        "surface": InputPathType,
        "label1": InputPathType,
        "label2": InputPathType,
        "annot_name": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisMergeParcellationsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_merge_parcellations_params(
    surface: InputPathType,
    label1: InputPathType,
    label2: InputPathType,
    annot_name: str | None = None,
) -> MrisMergeParcellations:
    """
    Build parameters.

    Args:
        surface: Surface file to analyze
        label1: First label file
        label2: Second label file
        annot_name: Compute pairwise Hausdorff distance between all annotations

    Returns:
        Parameter dictionary.
    """
    params: MrisMergeParcellations = {
        "@type": "freesurfer/mris_merge_parcellations",
        "surface": surface,
        "label1": label1,
        "label2": label2,
    }
    if annot_name is not None:
        params["annot_name"] = annot_name
    return params

def mris_merge_parcellations_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMergeParcellations."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("label1", None) is None:
        raise StyxValidationError("`label1` must not be None")
    if not isinstance(params["label1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label1` has the wrong type: Received `{type(params["label1"])}` expected `InputPathType`')
    if params.get("label2", None) is None:
        raise StyxValidationError("`label2` must not be None")
    if not isinstance(params["label2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label2` has the wrong type: Received `{type(params["label2"])}` expected `InputPathType`')
    if params.get("annot_name", None) is not None:
        if not isinstance(params["annot_name"], str):
            raise StyxValidationError(f'`annot_name` has the wrong type: Received `{type(params["annot_name"])}` expected `str`')

def mris_merge_parcellations_cargs(params: MrisMergeParcellations, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_merge_parcellations")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["label1"]))
    cargs.append(execution.input_file(params["label2"]))
    v_0 = params.get("annot_name")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(v_0)
    return cargs

def mris_merge_parcellations_outputs(params: MrisMergeParcellations, execution: Execution) -> MrisMergeParcellationsOutputs:
    """Build the MrisMergeParcellationsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisMergeParcellationsOutputs(
        root=root_v,
    )

def mris_merge_parcellations_execute(params: MrisMergeParcellations, runner: Runner | None = None) -> MrisMergeParcellationsOutputs:
    """
    mris_merge_parcellations

    This program computes the Hausdorff distance between two labels on the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_merge_parcellations_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MERGE_PARCELLATIONS_METADATA)
    execution.params(params)
    args = mris_merge_parcellations_cargs(params, execution)
    out = mris_merge_parcellations_outputs(params, execution)
    execution.run(args)
    return out

def mris_merge_parcellations(
    surface: InputPathType,
    label1: InputPathType,
    label2: InputPathType,
    annot_name: str | None = None,
    runner: Runner | None = None,
) -> MrisMergeParcellationsOutputs:
    """
    mris_merge_parcellations

    This program computes the Hausdorff distance between two labels on the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Surface file to analyze
        label1: First label file
        label2: Second label file
        annot_name: Compute pairwise Hausdorff distance between all annotations
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_merge_parcellations_params(
        surface=surface,
        label1=label1,
        label2=label2,
        annot_name=annot_name,
    )
    return mris_merge_parcellations_execute(params, runner)

__all__ = [
    "MrisMergeParcellations",
    "MrisMergeParcellationsOutputs",
    "MRIS_MERGE_PARCELLATIONS_METADATA",
    "mris_merge_parcellations_cargs",
    "mris_merge_parcellations_outputs",
    "mris_merge_parcellations_params",
    "mris_merge_parcellations_execute",
    "mris_merge_parcellations_validate",
    "mris_merge_parcellations",
]