# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COPY_VALUES_METADATA = Metadata(
    id="mri_copy_values",
    name="mri_copy_values",
    package="freesurfer",
)

MriCopyValues = typing.TypedDict(
    "MriCopyValues",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_copy_values"]],
        "source_volume": InputPathType,
        "target_volume": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriCopyValuesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume with copied values from source."""

def mri_copy_values_params(
    source_volume: InputPathType,
    target_volume: InputPathType,
    output_volume: str,
) -> MriCopyValues:
    """
    Build parameters.

    Args:
        source_volume: Source volume from which values are copied.
        target_volume: Target volume to which values are copied.
        output_volume: Output volume where the result will be stored.

    Returns:
        Parameter dictionary.
    """
    params: MriCopyValues = {
        "@type": "freesurfer/mri_copy_values",
        "source_volume": source_volume,
        "target_volume": target_volume,
        "output_volume": output_volume,
    }
    return params

def mri_copy_values_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCopyValues."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_volume", None) is None:
        raise StyxValidationError("`source_volume` must not be None")
    if not isinstance(params["source_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["source_volume"])}` expected `InputPathType`')
    if params.get("target_volume", None) is None:
        raise StyxValidationError("`target_volume` must not be None")
    if not isinstance(params["target_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["target_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_copy_values_cargs(params: MriCopyValues, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_copy_values")
    cargs.append(execution.input_file(params["source_volume"]))
    cargs.append(execution.input_file(params["target_volume"]))
    cargs.append(params["output_volume"])
    return cargs

def mri_copy_values_outputs(params: MriCopyValues, execution: Execution) -> MriCopyValuesOutputs:
    """Build the MriCopyValuesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriCopyValuesOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_copy_values_execute(params: MriCopyValues, runner: Runner | None = None) -> MriCopyValuesOutputs:
    """
    mri_copy_values

    No description

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_copy_values_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COPY_VALUES_METADATA)
    execution.params(params)
    args = mri_copy_values_cargs(params, execution)
    out = mri_copy_values_outputs(params, execution)
    execution.run(args)
    return out

def mri_copy_values(
    source_volume: InputPathType,
    target_volume: InputPathType,
    output_volume: str,
    runner: Runner | None = None,
) -> MriCopyValuesOutputs:
    """
    mri_copy_values

    No description

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_volume: Source volume from which values are copied.
        target_volume: Target volume to which values are copied.
        output_volume: Output volume where the result will be stored.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_copy_values_params(
        source_volume=source_volume,
        target_volume=target_volume,
        output_volume=output_volume,
    )
    return mri_copy_values_execute(params, runner)

__all__ = [
    "MriCopyValues",
    "MriCopyValuesOutputs",
    "MRI_COPY_VALUES_METADATA",
    "mri_copy_values_cargs",
    "mri_copy_values_outputs",
    "mri_copy_values_params",
    "mri_copy_values_execute",
    "mri_copy_values_validate",
    "mri_copy_values",
]