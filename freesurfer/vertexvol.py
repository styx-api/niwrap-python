# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VERTEXVOL_METADATA = Metadata(
    id="vertexvol",
    name="vertexvol",
    package="freesurfer",
)

Vertexvol = typing.TypedDict(
    "Vertexvol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vertexvol"]],
        "subject": str,
        "left_hemisphere": typing.NotRequired[bool],
        "right_hemisphere": typing.NotRequired[bool],
        "output_file": typing.NotRequired[str],
        "use_th3": typing.NotRequired[bool],
        "no_th3": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VertexvolOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType | None
    """Output file containing vertex-wise volume"""

def vertexvol_params(
    subject: str,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    output_file: str | None = None,
    use_th3: bool = False,
    no_th3: bool = False,
) -> Vertexvol:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        left_hemisphere: Select left hemisphere
        right_hemisphere: Select right hemisphere
        output_file: Output file name, default is ?h.volume
        use_th3: Use TH3 method for computation
        no_th3: Don't use TH3 method for computation

    Returns:
        Parameter dictionary.
    """
    params: Vertexvol = {
        "@type": "freesurfer/vertexvol",
        "subject": subject,
        "left_hemisphere": left_hemisphere,
        "right_hemisphere": right_hemisphere,
        "use_th3": use_th3,
        "no_th3": no_th3,
    }
    if output_file is not None:
        params["output_file"] = output_file
    return params

def vertexvol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Vertexvol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("left_hemisphere", None) is not None:
        if not isinstance(params["left_hemisphere"], bool):
            raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(params["left_hemisphere"])}` expected `bool`')
    if params.get("right_hemisphere", None) is not None:
        if not isinstance(params["right_hemisphere"], bool):
            raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(params["right_hemisphere"])}` expected `bool`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("use_th3", None) is not None:
        if not isinstance(params["use_th3"], bool):
            raise StyxValidationError(f'`use_th3` has the wrong type: Received `{type(params["use_th3"])}` expected `bool`')
    if params.get("no_th3", None) is not None:
        if not isinstance(params["no_th3"], bool):
            raise StyxValidationError(f'`no_th3` has the wrong type: Received `{type(params["no_th3"])}` expected `bool`')

def vertexvol_cargs(params: Vertexvol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vertexvol")
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("left_hemisphere"):
        cargs.append("--lh")
    if params.get("right_hemisphere"):
        cargs.append("--rh")
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    if params.get("use_th3"):
        cargs.append("--th3")
    if params.get("no_th3"):
        cargs.append("--no-th3")
    return cargs

def vertexvol_outputs(params: Vertexvol, execution: Execution) -> VertexvolOutputs:
    """Build the VertexvolOutputs object for this tool."""
    output_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_volume_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return VertexvolOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def vertexvol_execute(params: Vertexvol, runner: Runner | None = None) -> VertexvolOutputs:
    """
    vertexvol

    Computes vertex-wise volume (and mid.area)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vertexvol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VERTEXVOL_METADATA)
    execution.params(params)
    args = vertexvol_cargs(params, execution)
    out = vertexvol_outputs(params, execution)
    execution.run(args)
    return out

def vertexvol(
    subject: str,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    output_file: str | None = None,
    use_th3: bool = False,
    no_th3: bool = False,
    runner: Runner | None = None,
) -> VertexvolOutputs:
    """
    vertexvol

    Computes vertex-wise volume (and mid.area)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        left_hemisphere: Select left hemisphere
        right_hemisphere: Select right hemisphere
        output_file: Output file name, default is ?h.volume
        use_th3: Use TH3 method for computation
        no_th3: Don't use TH3 method for computation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vertexvol_params(
        subject=subject,
        left_hemisphere=left_hemisphere,
        right_hemisphere=right_hemisphere,
        output_file=output_file,
        use_th3=use_th3,
        no_th3=no_th3,
    )
    return vertexvol_execute(params, runner)

__all__ = [
    "Vertexvol",
    "VertexvolOutputs",
    "VERTEXVOL_METADATA",
    "vertexvol_cargs",
    "vertexvol_outputs",
    "vertexvol_params",
    "vertexvol_execute",
    "vertexvol_validate",
    "vertexvol",
]