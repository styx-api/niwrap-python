# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSPALM_METADATA = Metadata(
    id="fspalm",
    name="fspalm",
    package="freesurfer",
)

Fspalm = typing.TypedDict(
    "Fspalm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fspalm"]],
        "glmdir": str,
        "cft": float,
        "cwp": float,
        "onetail": typing.NotRequired[bool],
        "twotail": typing.NotRequired[bool],
        "name": typing.NotRequired[str],
        "iters": typing.NotRequired[float],
        "monly": typing.NotRequired[bool],
        "pponly": typing.NotRequired[bool],
        "octave": typing.NotRequired[bool],
        "centroid": typing.NotRequired[bool],
        "2spaces": typing.NotRequired[bool],
        "3spaces": typing.NotRequired[bool],
        "pargs": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FspalmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fspalm_params(
    glmdir: str,
    cft: float,
    cwp: float,
    onetail: bool = False,
    twotail: bool = False,
    name: str | None = None,
    iters: float | None = None,
    monly: bool = False,
    pponly: bool = False,
    octave: bool = False,
    centroid: bool = False,
    v_2spaces: bool = False,
    v_3spaces: bool = False,
    pargs: str | None = None,
) -> Fspalm:
    """
    Build parameters.

    Args:
        glmdir: The mri_glmfit directory to prepare
        cft: Voxel-wise cluster forming threshold (CFT), -log10(p)
        cwp: Clusterwise p-value threshold
        onetail: Perform a one-tailed test
        twotail: Perform a two-tailed test. NOTE: changes CFT
        name: Name of palm subdirectory (default="palm")
        iters: Number of iterations
        monly: Only create matlab file, do not run
        pponly: Only perform post-processing
        octave: Run with octave, not matlab
        centroid: Add --centroid flag to mri_surfcluster post-processing
        v_2spaces: Bonferroni-correct for 2 spaces
        v_3spaces: Bonferroni-correct for 3 spaces
        pargs: Supply additional args to be passed to the palm function

    Returns:
        Parameter dictionary.
    """
    params: Fspalm = {
        "@type": "freesurfer/fspalm",
        "glmdir": glmdir,
        "cft": cft,
        "cwp": cwp,
        "onetail": onetail,
        "twotail": twotail,
        "monly": monly,
        "pponly": pponly,
        "octave": octave,
        "centroid": centroid,
        "2spaces": v_2spaces,
        "3spaces": v_3spaces,
    }
    if name is not None:
        params["name"] = name
    if iters is not None:
        params["iters"] = iters
    if pargs is not None:
        params["pargs"] = pargs
    return params

def fspalm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fspalm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("glmdir", None) is None:
        raise StyxValidationError("`glmdir` must not be None")
    if not isinstance(params["glmdir"], str):
        raise StyxValidationError(f'`glmdir` has the wrong type: Received `{type(params["glmdir"])}` expected `str`')
    if params.get("cft", None) is None:
        raise StyxValidationError("`cft` must not be None")
    if not isinstance(params["cft"], (float, int)):
        raise StyxValidationError(f'`cft` has the wrong type: Received `{type(params["cft"])}` expected `float`')
    if params.get("cwp", None) is None:
        raise StyxValidationError("`cwp` must not be None")
    if not isinstance(params["cwp"], (float, int)):
        raise StyxValidationError(f'`cwp` has the wrong type: Received `{type(params["cwp"])}` expected `float`')
    if params.get("onetail", None) is not None:
        if not isinstance(params["onetail"], bool):
            raise StyxValidationError(f'`onetail` has the wrong type: Received `{type(params["onetail"])}` expected `bool`')
    if params.get("twotail", None) is not None:
        if not isinstance(params["twotail"], bool):
            raise StyxValidationError(f'`twotail` has the wrong type: Received `{type(params["twotail"])}` expected `bool`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("iters", None) is not None:
        if not isinstance(params["iters"], (float, int)):
            raise StyxValidationError(f'`iters` has the wrong type: Received `{type(params["iters"])}` expected `float`')
    if params.get("monly", None) is not None:
        if not isinstance(params["monly"], bool):
            raise StyxValidationError(f'`monly` has the wrong type: Received `{type(params["monly"])}` expected `bool`')
    if params.get("pponly", None) is not None:
        if not isinstance(params["pponly"], bool):
            raise StyxValidationError(f'`pponly` has the wrong type: Received `{type(params["pponly"])}` expected `bool`')
    if params.get("octave", None) is not None:
        if not isinstance(params["octave"], bool):
            raise StyxValidationError(f'`octave` has the wrong type: Received `{type(params["octave"])}` expected `bool`')
    if params.get("centroid", None) is not None:
        if not isinstance(params["centroid"], bool):
            raise StyxValidationError(f'`centroid` has the wrong type: Received `{type(params["centroid"])}` expected `bool`')
    if params.get("2spaces", None) is not None:
        if not isinstance(params["2spaces"], bool):
            raise StyxValidationError(f'`2spaces` has the wrong type: Received `{type(params["2spaces"])}` expected `bool`')
    if params.get("3spaces", None) is not None:
        if not isinstance(params["3spaces"], bool):
            raise StyxValidationError(f'`3spaces` has the wrong type: Received `{type(params["3spaces"])}` expected `bool`')
    if params.get("pargs", None) is not None:
        if not isinstance(params["pargs"], str):
            raise StyxValidationError(f'`pargs` has the wrong type: Received `{type(params["pargs"])}` expected `str`')

def fspalm_cargs(params: Fspalm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fspalm")
    cargs.append("--glmdir")
    cargs.append(params["glmdir"])
    cargs.append("--cft")
    cargs.append(str(params["cft"]))
    cargs.append("--cwp")
    cargs.append(str(params["cwp"]))
    if params.get("onetail"):
        cargs.append("--onetail")
    if params.get("twotail"):
        cargs.append("--twotail")
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("--name")
        cargs.append(v_0)
    v_1 = params.get("iters")
    if v_1 is not None:
        cargs.append("--iters")
        cargs.append(str(v_1))
    if params.get("monly"):
        cargs.append("--monly")
    if params.get("pponly"):
        cargs.append("--pponly")
    if params.get("octave"):
        cargs.append("--octave")
    if params.get("centroid"):
        cargs.append("--centroid")
    if params.get("2spaces"):
        cargs.append("--2spaces")
    if params.get("3spaces"):
        cargs.append("--3spaces")
    v_2 = params.get("pargs")
    if v_2 is not None:
        cargs.append("--pargs")
        cargs.append(v_2)
    return cargs

def fspalm_outputs(params: Fspalm, execution: Execution) -> FspalmOutputs:
    """Build the FspalmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FspalmOutputs(
        root=root_v,
    )

def fspalm_execute(params: Fspalm, runner: Runner | None = None) -> FspalmOutputs:
    """
    fspalm

    Prepares and analyzes the output of mri_glmfit for Permutation Analysis of Linear Models (PALM) to correct for multiple comparisons.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fspalm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSPALM_METADATA)
    execution.params(params)
    args = fspalm_cargs(params, execution)
    out = fspalm_outputs(params, execution)
    execution.run(args)
    return out

def fspalm(
    glmdir: str,
    cft: float,
    cwp: float,
    onetail: bool = False,
    twotail: bool = False,
    name: str | None = None,
    iters: float | None = None,
    monly: bool = False,
    pponly: bool = False,
    octave: bool = False,
    centroid: bool = False,
    v_2spaces: bool = False,
    v_3spaces: bool = False,
    pargs: str | None = None,
    runner: Runner | None = None,
) -> FspalmOutputs:
    """
    fspalm

    Prepares and analyzes the output of mri_glmfit for Permutation Analysis of Linear Models (PALM) to correct for multiple comparisons.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        glmdir: The mri_glmfit directory to prepare
        cft: Voxel-wise cluster forming threshold (CFT), -log10(p)
        cwp: Clusterwise p-value threshold
        onetail: Perform a one-tailed test
        twotail: Perform a two-tailed test. NOTE: changes CFT
        name: Name of palm subdirectory (default="palm")
        iters: Number of iterations
        monly: Only create matlab file, do not run
        pponly: Only perform post-processing
        octave: Run with octave, not matlab
        centroid: Add --centroid flag to mri_surfcluster post-processing
        v_2spaces: Bonferroni-correct for 2 spaces
        v_3spaces: Bonferroni-correct for 3 spaces
        pargs: Supply additional args to be passed to the palm function
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fspalm_params(
        glmdir=glmdir,
        cft=cft,
        cwp=cwp,
        onetail=onetail,
        twotail=twotail,
        name=name,
        iters=iters,
        monly=monly,
        pponly=pponly,
        octave=octave,
        centroid=centroid,
        v_2spaces=v_2spaces,
        v_3spaces=v_3spaces,
        pargs=pargs,
    )
    return fspalm_execute(params, runner)

__all__ = [
    "Fspalm",
    "FspalmOutputs",
    "FSPALM_METADATA",
    "fspalm_cargs",
    "fspalm_outputs",
    "fspalm_params",
    "fspalm_execute",
    "fspalm_validate",
    "fspalm",
]