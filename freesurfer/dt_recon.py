# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DT_RECON_METADATA = Metadata(
    id="dt_recon",
    name="dt_recon",
    package="freesurfer",
)

DtRecon = typing.TypedDict(
    "DtRecon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dt_recon"]],
        "input_volume": InputPathType,
        "bvals_bvecs": typing.NotRequired[str],
        "subject_id": str,
        "output_dir": str,
        "info_dump": typing.NotRequired[InputPathType],
        "ec_reference": typing.NotRequired[float],
        "no_ec_flag": typing.NotRequired[bool],
        "no_reg_flag": typing.NotRequired[bool],
        "register_file": typing.NotRequired[InputPathType],
        "no_tal_flag": typing.NotRequired[bool],
        "subjects_dir": typing.NotRequired[str],
        "save_ec_residuals_flag": typing.NotRequired[bool],
        "pca_analysis_flag": typing.NotRequired[bool],
        "mask_prune_threshold": typing.NotRequired[float],
        "init_spm_flag": typing.NotRequired[bool],
        "init_fsl_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DtReconOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    dwi_nifti: OutputPathType
    """Converted input to NIFTI format."""
    dwi_eddy_corrected: OutputPathType
    """DWI after eddy-current correction."""
    tensor_map: OutputPathType
    """Tensor map."""
    fa_map: OutputPathType
    """Fractional anisotropy (FA) map."""
    fa_tal_map: OutputPathType
    """FA map in Talairach space."""
    register_file_output: OutputPathType
    """Registration file."""
    mask_file: OutputPathType
    """Mask file."""

def dt_recon_params(
    input_volume: InputPathType,
    subject_id: str,
    output_dir: str,
    bvals_bvecs: str | None = None,
    info_dump: InputPathType | None = None,
    ec_reference: float | None = None,
    no_ec_flag: bool = False,
    no_reg_flag: bool = False,
    register_file: InputPathType | None = None,
    no_tal_flag: bool = False,
    subjects_dir: str | None = None,
    save_ec_residuals_flag: bool = False,
    pca_analysis_flag: bool = False,
    mask_prune_threshold: float | None = None,
    init_spm_flag: bool = False,
    init_fsl_flag: bool = False,
    debug_flag: bool = False,
    version_flag: bool = False,
) -> DtRecon:
    """
    Build parameters.

    Args:
        input_volume: Input volume (DWI data).
        subject_id: Subject ID.
        output_dir: Output directory.
        bvals_bvecs: B-values and B-vectors files.
        info_dump: Use info dump created by unpacksdcmdir or dcmunpack.
        ec_reference: Use specified time points as 0-based reference for eddy\
            current correction.
        no_ec_flag: Turn off eddy/motion correction.
        no_reg_flag: Do not register to subject or resample to talairach.
        register_file: Supply a register.lta file instead of registering.
        no_tal_flag: Do not resample FA to talairach space.
        subjects_dir: Specify subjects directory (default env SUBJECTS_DIR).
        save_ec_residuals_flag: Save residual error (dwires and eres).
        pca_analysis_flag: Run PCA/SVD analysis on eres (saves in pca-eres dir).
        mask_prune_threshold: Set threshold for masking (default is FLT_MIN).
        init_spm_flag: Initialize BBR with SPM instead of coreg (requires MATLAB).
        init_fsl_flag: Initialize BBR with FSL instead of coreg.
        debug_flag: Print out lots of info.
        version_flag: Print version of this script and exit.

    Returns:
        Parameter dictionary.
    """
    params: DtRecon = {
        "@type": "freesurfer/dt_recon",
        "input_volume": input_volume,
        "subject_id": subject_id,
        "output_dir": output_dir,
        "no_ec_flag": no_ec_flag,
        "no_reg_flag": no_reg_flag,
        "no_tal_flag": no_tal_flag,
        "save_ec_residuals_flag": save_ec_residuals_flag,
        "pca_analysis_flag": pca_analysis_flag,
        "init_spm_flag": init_spm_flag,
        "init_fsl_flag": init_fsl_flag,
        "debug_flag": debug_flag,
        "version_flag": version_flag,
    }
    if bvals_bvecs is not None:
        params["bvals_bvecs"] = bvals_bvecs
    if info_dump is not None:
        params["info_dump"] = info_dump
    if ec_reference is not None:
        params["ec_reference"] = ec_reference
    if register_file is not None:
        params["register_file"] = register_file
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if mask_prune_threshold is not None:
        params["mask_prune_threshold"] = mask_prune_threshold
    return params

def dt_recon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DtRecon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("bvals_bvecs", None) is not None:
        if not isinstance(params["bvals_bvecs"], str):
            raise StyxValidationError(f'`bvals_bvecs` has the wrong type: Received `{type(params["bvals_bvecs"])}` expected `str`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("info_dump", None) is not None:
        if not isinstance(params["info_dump"], (pathlib.Path, str)):
            raise StyxValidationError(f'`info_dump` has the wrong type: Received `{type(params["info_dump"])}` expected `InputPathType`')
    if params.get("ec_reference", None) is not None:
        if not isinstance(params["ec_reference"], (float, int)):
            raise StyxValidationError(f'`ec_reference` has the wrong type: Received `{type(params["ec_reference"])}` expected `float`')
    if params.get("no_ec_flag", None) is not None:
        if not isinstance(params["no_ec_flag"], bool):
            raise StyxValidationError(f'`no_ec_flag` has the wrong type: Received `{type(params["no_ec_flag"])}` expected `bool`')
    if params.get("no_reg_flag", None) is not None:
        if not isinstance(params["no_reg_flag"], bool):
            raise StyxValidationError(f'`no_reg_flag` has the wrong type: Received `{type(params["no_reg_flag"])}` expected `bool`')
    if params.get("register_file", None) is not None:
        if not isinstance(params["register_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`register_file` has the wrong type: Received `{type(params["register_file"])}` expected `InputPathType`')
    if params.get("no_tal_flag", None) is not None:
        if not isinstance(params["no_tal_flag"], bool):
            raise StyxValidationError(f'`no_tal_flag` has the wrong type: Received `{type(params["no_tal_flag"])}` expected `bool`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("save_ec_residuals_flag", None) is not None:
        if not isinstance(params["save_ec_residuals_flag"], bool):
            raise StyxValidationError(f'`save_ec_residuals_flag` has the wrong type: Received `{type(params["save_ec_residuals_flag"])}` expected `bool`')
    if params.get("pca_analysis_flag", None) is not None:
        if not isinstance(params["pca_analysis_flag"], bool):
            raise StyxValidationError(f'`pca_analysis_flag` has the wrong type: Received `{type(params["pca_analysis_flag"])}` expected `bool`')
    if params.get("mask_prune_threshold", None) is not None:
        if not isinstance(params["mask_prune_threshold"], (float, int)):
            raise StyxValidationError(f'`mask_prune_threshold` has the wrong type: Received `{type(params["mask_prune_threshold"])}` expected `float`')
    if params.get("init_spm_flag", None) is not None:
        if not isinstance(params["init_spm_flag"], bool):
            raise StyxValidationError(f'`init_spm_flag` has the wrong type: Received `{type(params["init_spm_flag"])}` expected `bool`')
    if params.get("init_fsl_flag", None) is not None:
        if not isinstance(params["init_fsl_flag"], bool):
            raise StyxValidationError(f'`init_fsl_flag` has the wrong type: Received `{type(params["init_fsl_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def dt_recon_cargs(params: DtRecon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dt_recon")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    v_0 = params.get("bvals_bvecs")
    if v_0 is not None:
        cargs.append("--b")
        cargs.append(v_0)
    cargs.append("--s")
    cargs.append(params["subject_id"])
    cargs.append("--o")
    cargs.append(params["output_dir"])
    v_1 = params.get("info_dump")
    if v_1 is not None:
        cargs.append("--info-dump")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("ec_reference")
    if v_2 is not None:
        cargs.append("--ecref")
        cargs.append(str(v_2))
    if params.get("no_ec_flag"):
        cargs.append("--no-ec")
    if params.get("no_reg_flag"):
        cargs.append("--no-reg")
    v_3 = params.get("register_file")
    if v_3 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_3))
    if params.get("no_tal_flag"):
        cargs.append("--no-tal")
    v_4 = params.get("subjects_dir")
    if v_4 is not None:
        cargs.append("--sd")
        cargs.append(v_4)
    if params.get("save_ec_residuals_flag"):
        cargs.append("--eres-save")
    if params.get("pca_analysis_flag"):
        cargs.append("--pca")
    v_5 = params.get("mask_prune_threshold")
    if v_5 is not None:
        cargs.append("--prune_thr")
        cargs.append(str(v_5))
    if params.get("init_spm_flag"):
        cargs.append("--init-spm")
    if params.get("init_fsl_flag"):
        cargs.append("--init-fsl")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("version_flag"):
        cargs.append("--version")
    return cargs

def dt_recon_outputs(params: DtRecon, execution: Execution) -> DtReconOutputs:
    """Build the DtReconOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    dwi_nifti_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/dwi.nii.gz')
    dwi_eddy_corrected_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/dwi-ec.nii.gz')
    tensor_map_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/tensor.nii.gz')
    fa_map_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/fa.nii.gz')
    fa_tal_map_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/fa-tal.nii.gz')
    register_file_output_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/register.lta')
    mask_file_v: OutputPathType = execution.output_file(f'{params["output_dir"]}/mask.nii.gz')
    return DtReconOutputs(
        root=root_v,
        dwi_nifti=dwi_nifti_v,
        dwi_eddy_corrected=dwi_eddy_corrected_v,
        tensor_map=tensor_map_v,
        fa_map=fa_map_v,
        fa_tal_map=fa_tal_map_v,
        register_file_output=register_file_output_v,
        mask_file=mask_file_v,
    )

def dt_recon_execute(params: DtRecon, runner: Runner | None = None) -> DtReconOutputs:
    """
    dt_recon

    Performs DTI reconstruction from the raw DWI input files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dt_recon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DT_RECON_METADATA)
    execution.params(params)
    args = dt_recon_cargs(params, execution)
    out = dt_recon_outputs(params, execution)
    execution.run(args)
    return out

def dt_recon(
    input_volume: InputPathType,
    subject_id: str,
    output_dir: str,
    bvals_bvecs: str | None = None,
    info_dump: InputPathType | None = None,
    ec_reference: float | None = None,
    no_ec_flag: bool = False,
    no_reg_flag: bool = False,
    register_file: InputPathType | None = None,
    no_tal_flag: bool = False,
    subjects_dir: str | None = None,
    save_ec_residuals_flag: bool = False,
    pca_analysis_flag: bool = False,
    mask_prune_threshold: float | None = None,
    init_spm_flag: bool = False,
    init_fsl_flag: bool = False,
    debug_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> DtReconOutputs:
    """
    dt_recon

    Performs DTI reconstruction from the raw DWI input files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume (DWI data).
        subject_id: Subject ID.
        output_dir: Output directory.
        bvals_bvecs: B-values and B-vectors files.
        info_dump: Use info dump created by unpacksdcmdir or dcmunpack.
        ec_reference: Use specified time points as 0-based reference for eddy\
            current correction.
        no_ec_flag: Turn off eddy/motion correction.
        no_reg_flag: Do not register to subject or resample to talairach.
        register_file: Supply a register.lta file instead of registering.
        no_tal_flag: Do not resample FA to talairach space.
        subjects_dir: Specify subjects directory (default env SUBJECTS_DIR).
        save_ec_residuals_flag: Save residual error (dwires and eres).
        pca_analysis_flag: Run PCA/SVD analysis on eres (saves in pca-eres dir).
        mask_prune_threshold: Set threshold for masking (default is FLT_MIN).
        init_spm_flag: Initialize BBR with SPM instead of coreg (requires MATLAB).
        init_fsl_flag: Initialize BBR with FSL instead of coreg.
        debug_flag: Print out lots of info.
        version_flag: Print version of this script and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dt_recon_params(
        input_volume=input_volume,
        subject_id=subject_id,
        output_dir=output_dir,
        bvals_bvecs=bvals_bvecs,
        info_dump=info_dump,
        ec_reference=ec_reference,
        no_ec_flag=no_ec_flag,
        no_reg_flag=no_reg_flag,
        register_file=register_file,
        no_tal_flag=no_tal_flag,
        subjects_dir=subjects_dir,
        save_ec_residuals_flag=save_ec_residuals_flag,
        pca_analysis_flag=pca_analysis_flag,
        mask_prune_threshold=mask_prune_threshold,
        init_spm_flag=init_spm_flag,
        init_fsl_flag=init_fsl_flag,
        debug_flag=debug_flag,
        version_flag=version_flag,
    )
    return dt_recon_execute(params, runner)

__all__ = [
    "DtRecon",
    "DtReconOutputs",
    "DT_RECON_METADATA",
    "dt_recon_cargs",
    "dt_recon_outputs",
    "dt_recon_params",
    "dt_recon_execute",
    "dt_recon_validate",
    "dt_recon",
]