# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RCA_BASE_INIT_METADATA = Metadata(
    id="rca-base-init",
    name="rca-base-init",
    package="freesurfer",
)

RcaBaseInit = typing.TypedDict(
    "RcaBaseInit",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rca-base-init"]],
        "log_file": typing.NotRequired[str],
        "status_file": typing.NotRequired[str],
        "cmd_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RcaBaseInitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rca_base_init_params(
    log_file: str | None = None,
    status_file: str | None = None,
    cmd_file: str | None = None,
) -> RcaBaseInit:
    """
    Build parameters.

    Args:
        log_file: Path to the local log file for output.
        status_file: Path to the status file to append logs.
        cmd_file: Path to the command file to append execution commands.

    Returns:
        Parameter dictionary.
    """
    params: RcaBaseInit = {
        "@type": "freesurfer/rca-base-init",
    }
    if log_file is not None:
        params["log_file"] = log_file
    if status_file is not None:
        params["status_file"] = status_file
    if cmd_file is not None:
        params["cmd_file"] = cmd_file
    return params

def rca_base_init_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RcaBaseInit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("status_file", None) is not None:
        if not isinstance(params["status_file"], str):
            raise StyxValidationError(f'`status_file` has the wrong type: Received `{type(params["status_file"])}` expected `str`')
    if params.get("cmd_file", None) is not None:
        if not isinstance(params["cmd_file"], str):
            raise StyxValidationError(f'`cmd_file` has the wrong type: Received `{type(params["cmd_file"])}` expected `str`')

def rca_base_init_cargs(params: RcaBaseInit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rca-base-init")
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-init")
        cargs.append(v_0)
    v_1 = params.get("status_file")
    if v_1 is not None:
        cargs.append(v_1)
    v_2 = params.get("cmd_file")
    if v_2 is not None:
        cargs.append(v_2)
    return cargs

def rca_base_init_outputs(params: RcaBaseInit, execution: Execution) -> RcaBaseInitOutputs:
    """Build the RcaBaseInitOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RcaBaseInitOutputs(
        root=root_v,
    )

def rca_base_init_execute(params: RcaBaseInit, runner: Runner | None = None) -> RcaBaseInitOutputs:
    """
    rca-base-init

    Initialize base subject for recon-all processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rca_base_init_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RCA_BASE_INIT_METADATA)
    execution.params(params)
    args = rca_base_init_cargs(params, execution)
    out = rca_base_init_outputs(params, execution)
    execution.run(args)
    return out

def rca_base_init(
    log_file: str | None = None,
    status_file: str | None = None,
    cmd_file: str | None = None,
    runner: Runner | None = None,
) -> RcaBaseInitOutputs:
    """
    rca-base-init

    Initialize base subject for recon-all processing

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        log_file: Path to the local log file for output.
        status_file: Path to the status file to append logs.
        cmd_file: Path to the command file to append execution commands.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rca_base_init_params(
        log_file=log_file,
        status_file=status_file,
        cmd_file=cmd_file,
    )
    return rca_base_init_execute(params, runner)

__all__ = [
    "RcaBaseInit",
    "RcaBaseInitOutputs",
    "RCA_BASE_INIT_METADATA",
    "rca_base_init_cargs",
    "rca_base_init_outputs",
    "rca_base_init_params",
    "rca_base_init_execute",
    "rca_base_init_validate",
    "rca_base_init",
]