# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WMEDITS2SURF_METADATA = Metadata(
    id="wmedits2surf",
    name="wmedits2surf",
    package="freesurfer",
)

Wmedits2surf = typing.TypedDict(
    "Wmedits2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/wmedits2surf"]],
        "subject": str,
        "self": typing.NotRequired[bool],
        "overwrite": typing.NotRequired[bool],
        "tmp_dir": typing.NotRequired[str],
        "cleanup": typing.NotRequired[bool],
        "no_cleanup": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "lh": typing.NotRequired[bool],
        "rh": typing.NotRequired[bool],
        "no_surfs": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Wmedits2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_wmerase: OutputPathType
    """Left hemisphere white matter erase surface file."""
    rh_wmerase: OutputPathType
    """Right hemisphere white matter erase surface file."""
    lh_wmfill: OutputPathType
    """Left hemisphere white matter fill surface file."""
    rh_wmfill: OutputPathType
    """Right hemisphere white matter fill surface file."""
    wm_erase_stats: OutputPathType
    """Statistics on the number of voxels erased."""
    wm_fill_stats: OutputPathType
    """Statistics on the number of voxels filled."""

def wmedits2surf_params(
    subject: str,
    self: bool = False,
    overwrite: bool = False,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
    debug: bool = False,
    lh: bool = False,
    rh: bool = False,
    no_surfs: bool = False,
) -> Wmedits2surf:
    """
    Build parameters.

    Args:
        subject: Subject identifier.
        self: Output will be on self instead of fsaverage.
        overwrite: Force overwriting of existing results.
        tmp_dir: Temporary directory.
        cleanup: Cleanup temporary files after execution.
        no_cleanup: Do not cleanup temporary files after execution.
        debug: Debug mode.
        lh: Only do left hemisphere.
        rh: Only do right hemisphere.
        no_surfs: Do not compute surfaces, only count stats.

    Returns:
        Parameter dictionary.
    """
    params: Wmedits2surf = {
        "@type": "freesurfer/wmedits2surf",
        "subject": subject,
        "self": self,
        "overwrite": overwrite,
        "cleanup": cleanup,
        "no_cleanup": no_cleanup,
        "debug": debug,
        "lh": lh,
        "rh": rh,
        "no_surfs": no_surfs,
    }
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def wmedits2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Wmedits2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("self", None) is not None:
        if not isinstance(params["self"], bool):
            raise StyxValidationError(f'`self` has the wrong type: Received `{type(params["self"])}` expected `bool`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')
    if params.get("cleanup", None) is not None:
        if not isinstance(params["cleanup"], bool):
            raise StyxValidationError(f'`cleanup` has the wrong type: Received `{type(params["cleanup"])}` expected `bool`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("lh", None) is not None:
        if not isinstance(params["lh"], bool):
            raise StyxValidationError(f'`lh` has the wrong type: Received `{type(params["lh"])}` expected `bool`')
    if params.get("rh", None) is not None:
        if not isinstance(params["rh"], bool):
            raise StyxValidationError(f'`rh` has the wrong type: Received `{type(params["rh"])}` expected `bool`')
    if params.get("no_surfs", None) is not None:
        if not isinstance(params["no_surfs"], bool):
            raise StyxValidationError(f'`no_surfs` has the wrong type: Received `{type(params["no_surfs"])}` expected `bool`')

def wmedits2surf_cargs(params: Wmedits2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wmedits2surf")
    cargs.append("--s")
    cargs.append(params["subject"])
    if params.get("self"):
        cargs.append("--self")
    if params.get("overwrite"):
        cargs.append("--overwrite")
    v_0 = params.get("tmp_dir")
    if v_0 is not None:
        cargs.append("--tmp")
        cargs.append(v_0)
    if params.get("cleanup"):
        cargs.append("--cleanup")
    if params.get("no_cleanup"):
        cargs.append("--no-cleanup")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("lh"):
        cargs.append("--lh")
    if params.get("rh"):
        cargs.append("--rh")
    if params.get("no_surfs"):
        cargs.append("--no-surfs")
    return cargs

def wmedits2surf_outputs(params: Wmedits2surf, execution: Execution) -> Wmedits2surfOutputs:
    """Build the Wmedits2surfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    lh_wmerase_v: OutputPathType = execution.output_file("subject/surf/lh.wmerase.fsa.mgh")
    rh_wmerase_v: OutputPathType = execution.output_file("subject/surf/rh.wmerase.fsa.mgh")
    lh_wmfill_v: OutputPathType = execution.output_file("subject/surf/lh.wmfill.fsa.mgh")
    rh_wmfill_v: OutputPathType = execution.output_file("subject/surf/rh.wmfill.fsa.mgh")
    wm_erase_stats_v: OutputPathType = execution.output_file("subject/stats/wm.erase.dat")
    wm_fill_stats_v: OutputPathType = execution.output_file("subject/stats/wm.fill.dat")
    return Wmedits2surfOutputs(
        root=root_v,
        lh_wmerase=lh_wmerase_v,
        rh_wmerase=rh_wmerase_v,
        lh_wmfill=lh_wmfill_v,
        rh_wmfill=rh_wmfill_v,
        wm_erase_stats=wm_erase_stats_v,
        wm_fill_stats=wm_fill_stats_v,
    )

def wmedits2surf_execute(params: Wmedits2surf, runner: Runner | None = None) -> Wmedits2surfOutputs:
    """
    wmedits2surf

    Computes binary maps of surface locations where the wm.mgz has been edited. Creates files for each hemisphere for each type of edit.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    wmedits2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WMEDITS2SURF_METADATA)
    execution.params(params)
    args = wmedits2surf_cargs(params, execution)
    out = wmedits2surf_outputs(params, execution)
    execution.run(args)
    return out

def wmedits2surf(
    subject: str,
    self: bool = False,
    overwrite: bool = False,
    tmp_dir: str | None = None,
    cleanup: bool = False,
    no_cleanup: bool = False,
    debug: bool = False,
    lh: bool = False,
    rh: bool = False,
    no_surfs: bool = False,
    runner: Runner | None = None,
) -> Wmedits2surfOutputs:
    """
    wmedits2surf

    Computes binary maps of surface locations where the wm.mgz has been edited. Creates files for each hemisphere for each type of edit.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier.
        self: Output will be on self instead of fsaverage.
        overwrite: Force overwriting of existing results.
        tmp_dir: Temporary directory.
        cleanup: Cleanup temporary files after execution.
        no_cleanup: Do not cleanup temporary files after execution.
        debug: Debug mode.
        lh: Only do left hemisphere.
        rh: Only do right hemisphere.
        no_surfs: Do not compute surfaces, only count stats.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = wmedits2surf_params(
        subject=subject,
        self=self,
        overwrite=overwrite,
        tmp_dir=tmp_dir,
        cleanup=cleanup,
        no_cleanup=no_cleanup,
        debug=debug,
        lh=lh,
        rh=rh,
        no_surfs=no_surfs,
    )
    return wmedits2surf_execute(params, runner)

__all__ = [
    "Wmedits2surf",
    "Wmedits2surfOutputs",
    "WMEDITS2SURF_METADATA",
    "wmedits2surf_cargs",
    "wmedits2surf_outputs",
    "wmedits2surf_params",
    "wmedits2surf_execute",
    "wmedits2surf_validate",
    "wmedits2surf",
]