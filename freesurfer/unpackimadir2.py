# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACKIMADIR2_METADATA = Metadata(
    id="unpackimadir2",
    name="unpackimadir2",
    package="freesurfer",
)

Unpackimadir2 = typing.TypedDict(
    "Unpackimadir2",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpackimadir2"]],
        "directory": InputPathType,
    },
)

@dataclasses.dataclass
class Unpackimadir2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def unpackimadir2_params(
    directory: InputPathType,
) -> Unpackimadir2:
    """
    Build parameters.

    Args:
        directory: Image directory to unpack.

    Returns:
        Parameter dictionary.
    """
    params: Unpackimadir2 = {
        "@type": "freesurfer/unpackimadir2",
        "directory": directory,
    }
    return params

def unpackimadir2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Unpackimadir2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("directory", None) is None:
        raise StyxValidationError("`directory` must not be None")
    if not isinstance(params["directory"], (pathlib.Path, str)):
        raise StyxValidationError(f'`directory` has the wrong type: Received `{type(params["directory"])}` expected `InputPathType`')

def unpackimadir2_cargs(params: Unpackimadir2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpackimadir2")
    cargs.append(execution.input_file(params["directory"]))
    return cargs

def unpackimadir2_outputs(params: Unpackimadir2, execution: Execution) -> Unpackimadir2Outputs:
    """Build the Unpackimadir2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Unpackimadir2Outputs(
        root=root_v,
    )

def unpackimadir2_execute(params: Unpackimadir2, runner: Runner | None = None) -> Unpackimadir2Outputs:
    """
    unpackimadir2

    Tool to unpack image directories, from FreeSurfer suite.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpackimadir2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACKIMADIR2_METADATA)
    execution.params(params)
    args = unpackimadir2_cargs(params, execution)
    out = unpackimadir2_outputs(params, execution)
    execution.run(args)
    return out

def unpackimadir2(
    directory: InputPathType,
    runner: Runner | None = None,
) -> Unpackimadir2Outputs:
    """
    unpackimadir2

    Tool to unpack image directories, from FreeSurfer suite.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        directory: Image directory to unpack.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpackimadir2_params(
        directory=directory,
    )
    return unpackimadir2_execute(params, runner)

__all__ = [
    "Unpackimadir2",
    "Unpackimadir2Outputs",
    "UNPACKIMADIR2_METADATA",
    "unpackimadir2_cargs",
    "unpackimadir2_outputs",
    "unpackimadir2_params",
    "unpackimadir2_execute",
    "unpackimadir2_validate",
    "unpackimadir2",
]