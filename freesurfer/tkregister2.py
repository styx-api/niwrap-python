# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TKREGISTER2_METADATA = Metadata(
    id="tkregister2",
    name="tkregister2",
    package="freesurfer",
)

Tkregister2 = typing.TypedDict(
    "Tkregister2",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tkregister2"]],
        "fixed_volume": InputPathType,
        "moving_volume": InputPathType,
        "reg_file": InputPathType,
        "noedit": typing.NotRequired[bool],
        "lta": typing.NotRequired[bool],
        "surf_reg": typing.NotRequired[bool],
        "reg_only": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Tkregister2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_reg_file: OutputPathType
    """Resulting registration file"""

def tkregister2_params(
    fixed_volume: InputPathType,
    moving_volume: InputPathType,
    reg_file: InputPathType,
    noedit: bool = False,
    lta: bool = False,
    surf_reg: bool = False,
    reg_only: bool = False,
    help: bool = False,
) -> Tkregister2:
    """
    Build parameters.

    Args:
        fixed_volume: Fixed volume (e.g., anatomical image)
        moving_volume: Moving volume (e.g., functional image)
        reg_file: Registration file to be saved or loaded
        noedit: Run in no-edit mode, useful for scripting.
        lta: Use LTA format for registration file.
        surf_reg: Use surface registration.
        reg_only: Don't show GUI, just save registration.
        help: Display help information.

    Returns:
        Parameter dictionary.
    """
    params: Tkregister2 = {
        "@type": "freesurfer/tkregister2",
        "fixed_volume": fixed_volume,
        "moving_volume": moving_volume,
        "reg_file": reg_file,
        "noedit": noedit,
        "lta": lta,
        "surf_reg": surf_reg,
        "reg_only": reg_only,
        "help": help,
    }
    return params

def tkregister2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tkregister2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed_volume", None) is None:
        raise StyxValidationError("`fixed_volume` must not be None")
    if not isinstance(params["fixed_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_volume` has the wrong type: Received `{type(params["fixed_volume"])}` expected `InputPathType`')
    if params.get("moving_volume", None) is None:
        raise StyxValidationError("`moving_volume` must not be None")
    if not isinstance(params["moving_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_volume` has the wrong type: Received `{type(params["moving_volume"])}` expected `InputPathType`')
    if params.get("reg_file", None) is None:
        raise StyxValidationError("`reg_file` must not be None")
    if not isinstance(params["reg_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reg_file` has the wrong type: Received `{type(params["reg_file"])}` expected `InputPathType`')
    if params.get("noedit", None) is not None:
        if not isinstance(params["noedit"], bool):
            raise StyxValidationError(f'`noedit` has the wrong type: Received `{type(params["noedit"])}` expected `bool`')
    if params.get("lta", None) is not None:
        if not isinstance(params["lta"], bool):
            raise StyxValidationError(f'`lta` has the wrong type: Received `{type(params["lta"])}` expected `bool`')
    if params.get("surf_reg", None) is not None:
        if not isinstance(params["surf_reg"], bool):
            raise StyxValidationError(f'`surf_reg` has the wrong type: Received `{type(params["surf_reg"])}` expected `bool`')
    if params.get("reg_only", None) is not None:
        if not isinstance(params["reg_only"], bool):
            raise StyxValidationError(f'`reg_only` has the wrong type: Received `{type(params["reg_only"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def tkregister2_cargs(params: Tkregister2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tkregister2")
    cargs.append(execution.input_file(params["fixed_volume"]))
    cargs.append(execution.input_file(params["moving_volume"]))
    cargs.append(execution.input_file(params["reg_file"]))
    if params.get("noedit"):
        cargs.append("--noedit")
    if params.get("lta"):
        cargs.append("--lta")
    if params.get("surf_reg"):
        cargs.append("--surf")
    if params.get("reg_only"):
        cargs.append("--regonly")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def tkregister2_outputs(params: Tkregister2, execution: Execution) -> Tkregister2Outputs:
    """Build the Tkregister2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_reg_file_v: OutputPathType = execution.output_file(params["reg_file"])
    return Tkregister2Outputs(
        root=root_v,
        output_reg_file=output_reg_file_v,
    )

def tkregister2_execute(params: Tkregister2, runner: Runner | None = None) -> Tkregister2Outputs:
    """
    tkregister2

    tkregister2 is a tool from FreeSurfer used for registration of MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tkregister2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TKREGISTER2_METADATA)
    execution.params(params)
    args = tkregister2_cargs(params, execution)
    out = tkregister2_outputs(params, execution)
    execution.run(args)
    return out

def tkregister2(
    fixed_volume: InputPathType,
    moving_volume: InputPathType,
    reg_file: InputPathType,
    noedit: bool = False,
    lta: bool = False,
    surf_reg: bool = False,
    reg_only: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> Tkregister2Outputs:
    """
    tkregister2

    tkregister2 is a tool from FreeSurfer used for registration of MRI images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        fixed_volume: Fixed volume (e.g., anatomical image)
        moving_volume: Moving volume (e.g., functional image)
        reg_file: Registration file to be saved or loaded
        noedit: Run in no-edit mode, useful for scripting.
        lta: Use LTA format for registration file.
        surf_reg: Use surface registration.
        reg_only: Don't show GUI, just save registration.
        help: Display help information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tkregister2_params(
        fixed_volume=fixed_volume,
        moving_volume=moving_volume,
        reg_file=reg_file,
        noedit=noedit,
        lta=lta,
        surf_reg=surf_reg,
        reg_only=reg_only,
        help=help,
    )
    return tkregister2_execute(params, runner)

__all__ = [
    "Tkregister2",
    "Tkregister2Outputs",
    "TKREGISTER2_METADATA",
    "tkregister2_cargs",
    "tkregister2_outputs",
    "tkregister2_params",
    "tkregister2_execute",
    "tkregister2_validate",
    "tkregister2",
]