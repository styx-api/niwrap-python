# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_WM_VOLUME_METADATA = Metadata(
    id="mris_wm_volume",
    name="mris_wm_volume",
    package="freesurfer",
)

MrisWmVolume = typing.TypedDict(
    "MrisWmVolume",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_wm_volume"]],
        "subject": str,
        "hemi": str,
        "subjects_dir": typing.NotRequired[str],
        "whitesurfname": typing.NotRequired[str],
        "asegname": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisWmVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_wm_volume_params(
    subject: str,
    hemi: str,
    subjects_dir: str | None = None,
    whitesurfname: str | None = None,
    asegname: str | None = None,
    verbose: bool = False,
) -> MrisWmVolume:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        hemi: Hemisphere to compute volume for (e.g., lh or rh)
        subjects_dir: The SUBJECTS_DIR where the subject data is stored
        whitesurfname: Name of the white surface file
        asegname: Name of the aseg file
        verbose: Output more messages

    Returns:
        Parameter dictionary.
    """
    params: MrisWmVolume = {
        "@type": "freesurfer/mris_wm_volume",
        "subject": subject,
        "hemi": hemi,
        "verbose": verbose,
    }
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if whitesurfname is not None:
        params["whitesurfname"] = whitesurfname
    if asegname is not None:
        params["asegname"] = asegname
    return params

def mris_wm_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisWmVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("whitesurfname", None) is not None:
        if not isinstance(params["whitesurfname"], str):
            raise StyxValidationError(f'`whitesurfname` has the wrong type: Received `{type(params["whitesurfname"])}` expected `str`')
    if params.get("asegname", None) is not None:
        if not isinstance(params["asegname"], str):
            raise StyxValidationError(f'`asegname` has the wrong type: Received `{type(params["asegname"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def mris_wm_volume_cargs(params: MrisWmVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_wm_volume")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    v_0 = params.get("subjects_dir")
    if v_0 is not None:
        cargs.append("-SDIR")
        cargs.append(v_0)
    v_1 = params.get("whitesurfname")
    if v_1 is not None:
        cargs.append("-white")
        cargs.append(v_1)
    v_2 = params.get("asegname")
    if v_2 is not None:
        cargs.append("-aseg")
        cargs.append(v_2)
    if params.get("verbose"):
        cargs.append("-v")
    return cargs

def mris_wm_volume_outputs(params: MrisWmVolume, execution: Execution) -> MrisWmVolumeOutputs:
    """Build the MrisWmVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisWmVolumeOutputs(
        root=root_v,
    )

def mris_wm_volume_execute(params: MrisWmVolume, runner: Runner | None = None) -> MrisWmVolumeOutputs:
    """
    mris_wm_volume

    Computes the volume of the enclosed hemisphere white matter surface, ignoring non-white matter voxels in the aseg.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_wm_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_WM_VOLUME_METADATA)
    execution.params(params)
    args = mris_wm_volume_cargs(params, execution)
    out = mris_wm_volume_outputs(params, execution)
    execution.run(args)
    return out

def mris_wm_volume(
    subject: str,
    hemi: str,
    subjects_dir: str | None = None,
    whitesurfname: str | None = None,
    asegname: str | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> MrisWmVolumeOutputs:
    """
    mris_wm_volume

    Computes the volume of the enclosed hemisphere white matter surface, ignoring non-white matter voxels in the aseg.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        hemi: Hemisphere to compute volume for (e.g., lh or rh)
        subjects_dir: The SUBJECTS_DIR where the subject data is stored
        whitesurfname: Name of the white surface file
        asegname: Name of the aseg file
        verbose: Output more messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_wm_volume_params(
        subject=subject,
        hemi=hemi,
        subjects_dir=subjects_dir,
        whitesurfname=whitesurfname,
        asegname=asegname,
        verbose=verbose,
    )
    return mris_wm_volume_execute(params, runner)

__all__ = [
    "MrisWmVolume",
    "MrisWmVolumeOutputs",
    "MRIS_WM_VOLUME_METADATA",
    "mris_wm_volume_cargs",
    "mris_wm_volume_outputs",
    "mris_wm_volume_params",
    "mris_wm_volume_execute",
    "mris_wm_volume_validate",
    "mris_wm_volume",
]