# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IREPIFITVOL_GLNX64_METADATA = Metadata(
    id="irepifitvol.glnx64",
    name="irepifitvol.glnx64",
    package="freesurfer",
)

IrepifitvolGlnx64 = typing.TypedDict(
    "IrepifitvolGlnx64",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/irepifitvol.glnx64"]],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class IrepifitvolGlnx64Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """The fitted volume output from the irepifitvol process."""

def irepifitvol_glnx64_params(
    input_file: InputPathType,
    output_file: str,
) -> IrepifitvolGlnx64:
    """
    Build parameters.

    Args:
        input_file: The input volume file to be processed.
        output_file: The path to save the output volume file after fitting.

    Returns:
        Parameter dictionary.
    """
    params: IrepifitvolGlnx64 = {
        "@type": "freesurfer/irepifitvol.glnx64",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def irepifitvol_glnx64_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IrepifitvolGlnx64."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def irepifitvol_glnx64_cargs(params: IrepifitvolGlnx64, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("irepifitvol.glnx64")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def irepifitvol_glnx64_outputs(params: IrepifitvolGlnx64, execution: Execution) -> IrepifitvolGlnx64Outputs:
    """Build the IrepifitvolGlnx64Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["output_file"])
    return IrepifitvolGlnx64Outputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def irepifitvol_glnx64_execute(params: IrepifitvolGlnx64, runner: Runner | None = None) -> IrepifitvolGlnx64Outputs:
    """
    irepifitvol.glnx64

    This tool is a part of the FreeSurfer toolkit, designed for certain volume fitting tasks.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    irepifitvol_glnx64_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IREPIFITVOL_GLNX64_METADATA)
    execution.params(params)
    args = irepifitvol_glnx64_cargs(params, execution)
    out = irepifitvol_glnx64_outputs(params, execution)
    execution.run(args)
    return out

def irepifitvol_glnx64(
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> IrepifitvolGlnx64Outputs:
    """
    irepifitvol.glnx64

    This tool is a part of the FreeSurfer toolkit, designed for certain volume fitting tasks.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: The input volume file to be processed.
        output_file: The path to save the output volume file after fitting.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = irepifitvol_glnx64_params(
        input_file=input_file,
        output_file=output_file,
    )
    return irepifitvol_glnx64_execute(params, runner)

__all__ = [
    "IrepifitvolGlnx64",
    "IrepifitvolGlnx64Outputs",
    "IREPIFITVOL_GLNX64_METADATA",
    "irepifitvol_glnx64_cargs",
    "irepifitvol_glnx64_outputs",
    "irepifitvol_glnx64_params",
    "irepifitvol_glnx64_execute",
    "irepifitvol_glnx64_validate",
    "irepifitvol_glnx64",
]