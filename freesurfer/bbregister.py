# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BBREGISTER_METADATA = Metadata(
    id="bbregister",
    name="bbregister",
    package="freesurfer",
)

Bbregister = typing.TypedDict(
    "Bbregister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bbregister"]],
        "subject": str,
        "moveable_volume": InputPathType,
        "reg_file": str,
        "contrast_type_t1": typing.NotRequired[bool],
        "contrast_type_t2": typing.NotRequired[bool],
        "vsm": typing.NotRequired[InputPathType],
        "init_coreg": typing.NotRequired[bool],
        "no_coreg_ref_mask": typing.NotRequired[bool],
        "init_header": typing.NotRequired[bool],
        "init_reg": typing.NotRequired[InputPathType],
        "intvol": typing.NotRequired[InputPathType],
        "mid_frame": typing.NotRequired[bool],
        "frame_no": typing.NotRequired[float],
        "template_out": typing.NotRequired[str],
        "o_outvol": typing.NotRequired[str],
        "s_from_reg": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BbregisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reg_output: OutputPathType
    """Output registration file in dat or lta format."""
    out_volume: OutputPathType | None
    """Resampled moveable volume."""

def bbregister_params(
    subject: str,
    moveable_volume: InputPathType,
    reg_file: str,
    contrast_type_t1: bool = False,
    contrast_type_t2: bool = False,
    vsm: InputPathType | None = None,
    init_coreg: bool = False,
    no_coreg_ref_mask: bool = False,
    init_header: bool = False,
    init_reg: InputPathType | None = None,
    intvol: InputPathType | None = None,
    mid_frame: bool = False,
    frame_no: float | None = None,
    template_out: str | None = None,
    o_outvol: str | None = None,
    s_from_reg: bool = False,
) -> Bbregister:
    """
    Build parameters.

    Args:
        subject: FreeSurfer subject name as found in $SUBJECTS_DIR.
        moveable_volume: "Moveable" volume template for cross-modal volume. E.g.,\
            fMRI volume used for motion correction.
        reg_file: Output FreeSurfer registration file (tkregister-style or LTA\
            format).
        contrast_type_t1: Assume T1 contrast, White Matter brighter than Grey\
            Matter.
        contrast_type_t2: Assume T2 contrast, Grey Matter brighter than White\
            Matter. Same as --bold and --dti.
        vsm: Include B0 distortion correction. A voxel shift map can be created\
            with the epidewarp.fsl script.
        init_coreg: Initialize with FreeSurfer mri_coreg program.
        no_coreg_ref_mask: Do not use aparc+aseg.mgz as a reference mask.
        init_header: Use geometry info for close voxel-to-voxel registration.\
            Typically valid if acquired in same session.
        init_reg: Supply an initial registration matrix; can be LTA format.
        intvol: Volume used as an intermediate during registration. Useful for\
            partial field-of-view volumes.
        mid_frame: Register to middle frame (not with --frame option).
        frame_no: Register to specified frame (default is 0=1st).
        template_out: Save template (beneficial with --frame).
        o_outvol: Resample moveable volume and save as specified output volume.
        s_from_reg: Get subject name from registration file.

    Returns:
        Parameter dictionary.
    """
    params: Bbregister = {
        "@type": "freesurfer/bbregister",
        "subject": subject,
        "moveable_volume": moveable_volume,
        "reg_file": reg_file,
        "contrast_type_t1": contrast_type_t1,
        "contrast_type_t2": contrast_type_t2,
        "init_coreg": init_coreg,
        "no_coreg_ref_mask": no_coreg_ref_mask,
        "init_header": init_header,
        "mid_frame": mid_frame,
        "s_from_reg": s_from_reg,
    }
    if vsm is not None:
        params["vsm"] = vsm
    if init_reg is not None:
        params["init_reg"] = init_reg
    if intvol is not None:
        params["intvol"] = intvol
    if frame_no is not None:
        params["frame_no"] = frame_no
    if template_out is not None:
        params["template_out"] = template_out
    if o_outvol is not None:
        params["o_outvol"] = o_outvol
    return params

def bbregister_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bbregister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("moveable_volume", None) is None:
        raise StyxValidationError("`moveable_volume` must not be None")
    if not isinstance(params["moveable_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moveable_volume` has the wrong type: Received `{type(params["moveable_volume"])}` expected `InputPathType`')
    if params.get("reg_file", None) is None:
        raise StyxValidationError("`reg_file` must not be None")
    if not isinstance(params["reg_file"], str):
        raise StyxValidationError(f'`reg_file` has the wrong type: Received `{type(params["reg_file"])}` expected `str`')
    if params.get("contrast_type_t1", None) is not None:
        if not isinstance(params["contrast_type_t1"], bool):
            raise StyxValidationError(f'`contrast_type_t1` has the wrong type: Received `{type(params["contrast_type_t1"])}` expected `bool`')
    if params.get("contrast_type_t2", None) is not None:
        if not isinstance(params["contrast_type_t2"], bool):
            raise StyxValidationError(f'`contrast_type_t2` has the wrong type: Received `{type(params["contrast_type_t2"])}` expected `bool`')
    if params.get("vsm", None) is not None:
        if not isinstance(params["vsm"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vsm` has the wrong type: Received `{type(params["vsm"])}` expected `InputPathType`')
    if params.get("init_coreg", None) is not None:
        if not isinstance(params["init_coreg"], bool):
            raise StyxValidationError(f'`init_coreg` has the wrong type: Received `{type(params["init_coreg"])}` expected `bool`')
    if params.get("no_coreg_ref_mask", None) is not None:
        if not isinstance(params["no_coreg_ref_mask"], bool):
            raise StyxValidationError(f'`no_coreg_ref_mask` has the wrong type: Received `{type(params["no_coreg_ref_mask"])}` expected `bool`')
    if params.get("init_header", None) is not None:
        if not isinstance(params["init_header"], bool):
            raise StyxValidationError(f'`init_header` has the wrong type: Received `{type(params["init_header"])}` expected `bool`')
    if params.get("init_reg", None) is not None:
        if not isinstance(params["init_reg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_reg` has the wrong type: Received `{type(params["init_reg"])}` expected `InputPathType`')
    if params.get("intvol", None) is not None:
        if not isinstance(params["intvol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`intvol` has the wrong type: Received `{type(params["intvol"])}` expected `InputPathType`')
    if params.get("mid_frame", None) is not None:
        if not isinstance(params["mid_frame"], bool):
            raise StyxValidationError(f'`mid_frame` has the wrong type: Received `{type(params["mid_frame"])}` expected `bool`')
    if params.get("frame_no", None) is not None:
        if not isinstance(params["frame_no"], (float, int)):
            raise StyxValidationError(f'`frame_no` has the wrong type: Received `{type(params["frame_no"])}` expected `float`')
    if params.get("template_out", None) is not None:
        if not isinstance(params["template_out"], str):
            raise StyxValidationError(f'`template_out` has the wrong type: Received `{type(params["template_out"])}` expected `str`')
    if params.get("o_outvol", None) is not None:
        if not isinstance(params["o_outvol"], str):
            raise StyxValidationError(f'`o_outvol` has the wrong type: Received `{type(params["o_outvol"])}` expected `str`')
    if params.get("s_from_reg", None) is not None:
        if not isinstance(params["s_from_reg"], bool):
            raise StyxValidationError(f'`s_from_reg` has the wrong type: Received `{type(params["s_from_reg"])}` expected `bool`')

def bbregister_cargs(params: Bbregister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bbregister")
    cargs.append("--s")
    cargs.append(params["subject"])
    cargs.append("--mov")
    cargs.append(execution.input_file(params["moveable_volume"]))
    cargs.append("--reg")
    cargs.append(params["reg_file"])
    if params.get("contrast_type_t1"):
        cargs.append("--t1")
    if params.get("contrast_type_t2"):
        cargs.append("--t2")
    v_0 = params.get("vsm")
    if v_0 is not None:
        cargs.append("--vsm")
        cargs.append(execution.input_file(v_0))
    if params.get("init_coreg"):
        cargs.append("--init-coreg")
    if params.get("no_coreg_ref_mask"):
        cargs.append("--no-coreg-ref-mask")
    if params.get("init_header"):
        cargs.append("--init-header")
    v_1 = params.get("init_reg")
    if v_1 is not None:
        cargs.append("--init-reg")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("intvol")
    if v_2 is not None:
        cargs.append("--int")
        cargs.append(execution.input_file(v_2))
    if params.get("mid_frame"):
        cargs.append("--mid-frame")
    v_3 = params.get("frame_no")
    if v_3 is not None:
        cargs.append("--frame")
        cargs.append(str(v_3))
    v_4 = params.get("template_out")
    if v_4 is not None:
        cargs.append("--template-out")
        cargs.append(v_4)
    v_5 = params.get("o_outvol")
    if v_5 is not None:
        cargs.append("--o")
        cargs.append(v_5)
    if params.get("s_from_reg"):
        cargs.append("--s-from-reg")
    return cargs

def bbregister_outputs(params: Bbregister, execution: Execution) -> BbregisterOutputs:
    """Build the BbregisterOutputs object for this tool."""
    out_volume_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    reg_output_v: OutputPathType = execution.output_file(params["reg_file"])
    __v0 = params.get("o_outvol")
    if __v0 is not None:
        out_volume_v = execution.output_file((__v0 if __v0 is not None else ""))
    return BbregisterOutputs(
        root=root_v,
        reg_output=reg_output_v,
        out_volume=out_volume_v,
    )

def bbregister_execute(params: Bbregister, runner: Runner | None = None) -> BbregisterOutputs:
    """
    bbregister

    Performs within-subject, cross-modal registration using a boundary-based cost function.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bbregister_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BBREGISTER_METADATA)
    execution.params(params)
    args = bbregister_cargs(params, execution)
    out = bbregister_outputs(params, execution)
    execution.run(args)
    return out

def bbregister(
    subject: str,
    moveable_volume: InputPathType,
    reg_file: str,
    contrast_type_t1: bool = False,
    contrast_type_t2: bool = False,
    vsm: InputPathType | None = None,
    init_coreg: bool = False,
    no_coreg_ref_mask: bool = False,
    init_header: bool = False,
    init_reg: InputPathType | None = None,
    intvol: InputPathType | None = None,
    mid_frame: bool = False,
    frame_no: float | None = None,
    template_out: str | None = None,
    o_outvol: str | None = None,
    s_from_reg: bool = False,
    runner: Runner | None = None,
) -> BbregisterOutputs:
    """
    bbregister

    Performs within-subject, cross-modal registration using a boundary-based cost function.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: FreeSurfer subject name as found in $SUBJECTS_DIR.
        moveable_volume: "Moveable" volume template for cross-modal volume. E.g.,\
            fMRI volume used for motion correction.
        reg_file: Output FreeSurfer registration file (tkregister-style or LTA\
            format).
        contrast_type_t1: Assume T1 contrast, White Matter brighter than Grey\
            Matter.
        contrast_type_t2: Assume T2 contrast, Grey Matter brighter than White\
            Matter. Same as --bold and --dti.
        vsm: Include B0 distortion correction. A voxel shift map can be created\
            with the epidewarp.fsl script.
        init_coreg: Initialize with FreeSurfer mri_coreg program.
        no_coreg_ref_mask: Do not use aparc+aseg.mgz as a reference mask.
        init_header: Use geometry info for close voxel-to-voxel registration.\
            Typically valid if acquired in same session.
        init_reg: Supply an initial registration matrix; can be LTA format.
        intvol: Volume used as an intermediate during registration. Useful for\
            partial field-of-view volumes.
        mid_frame: Register to middle frame (not with --frame option).
        frame_no: Register to specified frame (default is 0=1st).
        template_out: Save template (beneficial with --frame).
        o_outvol: Resample moveable volume and save as specified output volume.
        s_from_reg: Get subject name from registration file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bbregister_params(
        subject=subject,
        moveable_volume=moveable_volume,
        reg_file=reg_file,
        contrast_type_t1=contrast_type_t1,
        contrast_type_t2=contrast_type_t2,
        vsm=vsm,
        init_coreg=init_coreg,
        no_coreg_ref_mask=no_coreg_ref_mask,
        init_header=init_header,
        init_reg=init_reg,
        intvol=intvol,
        mid_frame=mid_frame,
        frame_no=frame_no,
        template_out=template_out,
        o_outvol=o_outvol,
        s_from_reg=s_from_reg,
    )
    return bbregister_execute(params, runner)

__all__ = [
    "Bbregister",
    "BbregisterOutputs",
    "BBREGISTER_METADATA",
    "bbregister_cargs",
    "bbregister_outputs",
    "bbregister_params",
    "bbregister_execute",
    "bbregister_validate",
    "bbregister",
]