# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRK_TOOLS_METADATA = Metadata(
    id="trk_tools",
    name="trk_tools",
    package="freesurfer",
)

TrkTools = typing.TypedDict(
    "TrkTools",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/trk_tools"]],
        "reference_image": InputPathType,
        "input_trk": InputPathType,
        "output_trk": typing.NotRequired[str],
        "output_image": typing.NotRequired[str],
        "update_header": typing.NotRequired[bool],
        "output_vtk": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class TrkToolsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    trk_output_file: OutputPathType | None
    """The processed output TRK file"""
    image_output_file: OutputPathType | None
    """Image exported from TRK file"""
    vtk_output_file: OutputPathType | None
    """VTK file containing streamlines"""

def trk_tools_params(
    reference_image: InputPathType,
    input_trk: InputPathType,
    output_trk: str | None = None,
    output_image: str | None = None,
    update_header: bool = False,
    output_vtk: str | None = None,
) -> TrkTools:
    """
    Build parameters.

    Args:
        reference_image: Reference image for TRK processing
        input_trk: Input TRK file to be processed
        output_trk: Output TRK file
        output_image: Export TRK into an image
        update_header: Update TRK header with reference image
        output_vtk: Output streamlines in VTK format

    Returns:
        Parameter dictionary.
    """
    params: TrkTools = {
        "@type": "freesurfer/trk_tools",
        "reference_image": reference_image,
        "input_trk": input_trk,
        "update_header": update_header,
    }
    if output_trk is not None:
        params["output_trk"] = output_trk
    if output_image is not None:
        params["output_image"] = output_image
    if output_vtk is not None:
        params["output_vtk"] = output_vtk
    return params

def trk_tools_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TrkTools."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("input_trk", None) is None:
        raise StyxValidationError("`input_trk` must not be None")
    if not isinstance(params["input_trk"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_trk` has the wrong type: Received `{type(params["input_trk"])}` expected `InputPathType`')
    if params.get("output_trk", None) is not None:
        if not isinstance(params["output_trk"], str):
            raise StyxValidationError(f'`output_trk` has the wrong type: Received `{type(params["output_trk"])}` expected `str`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("update_header", None) is not None:
        if not isinstance(params["update_header"], bool):
            raise StyxValidationError(f'`update_header` has the wrong type: Received `{type(params["update_header"])}` expected `bool`')
    if params.get("output_vtk", None) is not None:
        if not isinstance(params["output_vtk"], str):
            raise StyxValidationError(f'`output_vtk` has the wrong type: Received `{type(params["output_vtk"])}` expected `str`')

def trk_tools_cargs(params: TrkTools, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("trk_tools")
    cargs.append("-i")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("-f")
    cargs.append(execution.input_file(params["input_trk"]))
    v_0 = params.get("output_trk")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("output_image")
    if v_1 is not None:
        cargs.append("-e")
        cargs.append(v_1)
    if params.get("update_header"):
        cargs.append("-u")
    v_2 = params.get("output_vtk")
    if v_2 is not None:
        cargs.append("-v")
        cargs.append(v_2)
    return cargs

def trk_tools_outputs(params: TrkTools, execution: Execution) -> TrkToolsOutputs:
    """Build the TrkToolsOutputs object for this tool."""
    trk_output_file_v: OutputPathType | None = None
    image_output_file_v: OutputPathType | None = None
    vtk_output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_trk")
    if __v0 is not None:
        trk_output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_image")
    if __v1 is not None:
        image_output_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("output_vtk")
    if __v2 is not None:
        vtk_output_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    return TrkToolsOutputs(
        root=root_v,
        trk_output_file=trk_output_file_v,
        image_output_file=image_output_file_v,
        vtk_output_file=vtk_output_file_v,
    )

def trk_tools_execute(params: TrkTools, runner: Runner | None = None) -> TrkToolsOutputs:
    """
    trk_tools

    Tool for processing TRK files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    trk_tools_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRK_TOOLS_METADATA)
    execution.params(params)
    args = trk_tools_cargs(params, execution)
    out = trk_tools_outputs(params, execution)
    execution.run(args)
    return out

def trk_tools(
    reference_image: InputPathType,
    input_trk: InputPathType,
    output_trk: str | None = None,
    output_image: str | None = None,
    update_header: bool = False,
    output_vtk: str | None = None,
    runner: Runner | None = None,
) -> TrkToolsOutputs:
    """
    trk_tools

    Tool for processing TRK files

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        reference_image: Reference image for TRK processing
        input_trk: Input TRK file to be processed
        output_trk: Output TRK file
        output_image: Export TRK into an image
        update_header: Update TRK header with reference image
        output_vtk: Output streamlines in VTK format
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = trk_tools_params(
        reference_image=reference_image,
        input_trk=input_trk,
        output_trk=output_trk,
        output_image=output_image,
        update_header=update_header,
        output_vtk=output_vtk,
    )
    return trk_tools_execute(params, runner)

__all__ = [
    "TrkTools",
    "TrkToolsOutputs",
    "TRK_TOOLS_METADATA",
    "trk_tools_cargs",
    "trk_tools_outputs",
    "trk_tools_params",
    "trk_tools_execute",
    "trk_tools_validate",
    "trk_tools",
]