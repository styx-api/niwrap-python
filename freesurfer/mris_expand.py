# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_EXPAND_METADATA = Metadata(
    id="mris_expand",
    name="mris_expand",
    package="freesurfer",
)

MrisExpand = typing.TypedDict(
    "MrisExpand",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_expand"]],
        "input_surface": InputPathType,
        "expansion_distance": float,
        "output_surface": str,
        "thickness": typing.NotRequired[bool],
        "label": typing.NotRequired[str],
        "tmap": typing.NotRequired[str],
        "tmap_random": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisExpandOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface_file: OutputPathType
    """Expanded output surface file"""

def mris_expand_params(
    input_surface: InputPathType,
    expansion_distance: float,
    output_surface: str,
    thickness: bool = False,
    label: str | None = None,
    tmap: str | None = None,
    tmap_random: str | None = None,
) -> MrisExpand:
    """
    Build parameters.

    Args:
        input_surface: Input surface file (e.g. lh.white)
        expansion_distance: Expansion distance in mm
        output_surface: Output surface file
        thickness: Use thickness for expansion
        label: Use label file for expansion
        tmap: Use a prespecified map of percent thickness to compute the target\
            locations for expansion
        tmap_random: Create a random target distance map with Gaussian sampling for\
            the target locations

    Returns:
        Parameter dictionary.
    """
    params: MrisExpand = {
        "@type": "freesurfer/mris_expand",
        "input_surface": input_surface,
        "expansion_distance": expansion_distance,
        "output_surface": output_surface,
        "thickness": thickness,
    }
    if label is not None:
        params["label"] = label
    if tmap is not None:
        params["tmap"] = tmap
    if tmap_random is not None:
        params["tmap_random"] = tmap_random
    return params

def mris_expand_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisExpand."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("expansion_distance", None) is None:
        raise StyxValidationError("`expansion_distance` must not be None")
    if not isinstance(params["expansion_distance"], (float, int)):
        raise StyxValidationError(f'`expansion_distance` has the wrong type: Received `{type(params["expansion_distance"])}` expected `float`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("thickness", None) is not None:
        if not isinstance(params["thickness"], bool):
            raise StyxValidationError(f'`thickness` has the wrong type: Received `{type(params["thickness"])}` expected `bool`')
    if params.get("label", None) is not None:
        if not isinstance(params["label"], str):
            raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `str`')
    if params.get("tmap", None) is not None:
        if not isinstance(params["tmap"], str):
            raise StyxValidationError(f'`tmap` has the wrong type: Received `{type(params["tmap"])}` expected `str`')
    if params.get("tmap_random", None) is not None:
        if not isinstance(params["tmap_random"], str):
            raise StyxValidationError(f'`tmap_random` has the wrong type: Received `{type(params["tmap_random"])}` expected `str`')

def mris_expand_cargs(params: MrisExpand, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_expand")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(str(params["expansion_distance"]))
    cargs.append(params["output_surface"])
    if params.get("thickness"):
        cargs.append("-thickness")
    v_0 = params.get("label")
    if v_0 is not None:
        cargs.append("-label")
        cargs.append(v_0)
    v_1 = params.get("tmap")
    if v_1 is not None:
        cargs.append("-tmap")
        cargs.append(v_1)
    v_2 = params.get("tmap_random")
    if v_2 is not None:
        cargs.append("-tmap random")
        cargs.append(v_2)
    return cargs

def mris_expand_outputs(params: MrisExpand, execution: Execution) -> MrisExpandOutputs:
    """Build the MrisExpandOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_file_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisExpandOutputs(
        root=root_v,
        output_surface_file=output_surface_file_v,
    )

def mris_expand_execute(params: MrisExpand, runner: Runner | None = None) -> MrisExpandOutputs:
    """
    mris_expand

    Expand a given surface by a specified distance

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_expand_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_EXPAND_METADATA)
    execution.params(params)
    args = mris_expand_cargs(params, execution)
    out = mris_expand_outputs(params, execution)
    execution.run(args)
    return out

def mris_expand(
    input_surface: InputPathType,
    expansion_distance: float,
    output_surface: str,
    thickness: bool = False,
    label: str | None = None,
    tmap: str | None = None,
    tmap_random: str | None = None,
    runner: Runner | None = None,
) -> MrisExpandOutputs:
    """
    mris_expand

    Expand a given surface by a specified distance

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file (e.g. lh.white)
        expansion_distance: Expansion distance in mm
        output_surface: Output surface file
        thickness: Use thickness for expansion
        label: Use label file for expansion
        tmap: Use a prespecified map of percent thickness to compute the target\
            locations for expansion
        tmap_random: Create a random target distance map with Gaussian sampling for\
            the target locations
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_expand_params(
        input_surface=input_surface,
        expansion_distance=expansion_distance,
        output_surface=output_surface,
        thickness=thickness,
        label=label,
        tmap=tmap,
        tmap_random=tmap_random,
    )
    return mris_expand_execute(params, runner)

__all__ = [
    "MrisExpand",
    "MrisExpandOutputs",
    "MRIS_EXPAND_METADATA",
    "mris_expand_cargs",
    "mris_expand_outputs",
    "mris_expand_params",
    "mris_expand_execute",
    "mris_expand_validate",
    "mris_expand",
]