# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LONG_NORMALIZE_METADATA = Metadata(
    id="mri_long_normalize",
    name="mri_long_normalize",
    package="freesurfer",
)

MriLongNormalize = typing.TypedDict(
    "MriLongNormalize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_long_normalize"]],
        "input_vol": InputPathType,
        "base_tp_file": InputPathType,
        "output_vol": str,
        "normalization_iters": typing.NotRequired[int],
        "disable_1d": typing.NotRequired[bool],
        "smooth_bias": typing.NotRequired[float],
        "aseg": typing.NotRequired[InputPathType],
        "debug_gvx": typing.NotRequired[list[float]],
        "debug_gx": typing.NotRequired[list[float]],
        "reading": typing.NotRequired[list[str]],
        "print_usage": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriLongNormalizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output MRI volume file."""

def mri_long_normalize_params(
    input_vol: InputPathType,
    base_tp_file: InputPathType,
    output_vol: str,
    normalization_iters: int | None = None,
    disable_1d: bool = False,
    smooth_bias: float | None = None,
    aseg: InputPathType | None = None,
    debug_gvx: list[float] | None = None,
    debug_gx: list[float] | None = None,
    reading: list[str] | None = None,
    print_usage: bool = False,
) -> MriLongNormalize:
    """
    Build parameters.

    Args:
        input_vol: Input MRI volume file.
        base_tp_file: Base time point file.
        output_vol: Output MRI volume file.
        normalization_iters: Use n 3D normalization iterations (default is 2).
        disable_1d: Disable 1D normalization.
        smooth_bias: Smooth the bias field.
        aseg: Aseg file specification.
        debug_gvx: For debugging: specify Gvx, Gvy, Gvz.
        debug_gx: For debugging: specify Gx, Gy, Gz.
        reading: For reading: specify control points and bias field.
        print_usage: Print usage information.

    Returns:
        Parameter dictionary.
    """
    params: MriLongNormalize = {
        "@type": "freesurfer/mri_long_normalize",
        "input_vol": input_vol,
        "base_tp_file": base_tp_file,
        "output_vol": output_vol,
        "disable_1d": disable_1d,
        "print_usage": print_usage,
    }
    if normalization_iters is not None:
        params["normalization_iters"] = normalization_iters
    if smooth_bias is not None:
        params["smooth_bias"] = smooth_bias
    if aseg is not None:
        params["aseg"] = aseg
    if debug_gvx is not None:
        params["debug_gvx"] = debug_gvx
    if debug_gx is not None:
        params["debug_gx"] = debug_gx
    if reading is not None:
        params["reading"] = reading
    return params

def mri_long_normalize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLongNormalize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_vol", None) is None:
        raise StyxValidationError("`input_vol` must not be None")
    if not isinstance(params["input_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vol` has the wrong type: Received `{type(params["input_vol"])}` expected `InputPathType`')
    if params.get("base_tp_file", None) is None:
        raise StyxValidationError("`base_tp_file` must not be None")
    if not isinstance(params["base_tp_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`base_tp_file` has the wrong type: Received `{type(params["base_tp_file"])}` expected `InputPathType`')
    if params.get("output_vol", None) is None:
        raise StyxValidationError("`output_vol` must not be None")
    if not isinstance(params["output_vol"], str):
        raise StyxValidationError(f'`output_vol` has the wrong type: Received `{type(params["output_vol"])}` expected `str`')
    if params.get("normalization_iters", None) is not None:
        if not isinstance(params["normalization_iters"], int):
            raise StyxValidationError(f'`normalization_iters` has the wrong type: Received `{type(params["normalization_iters"])}` expected `int`')
    if params.get("disable_1d", None) is not None:
        if not isinstance(params["disable_1d"], bool):
            raise StyxValidationError(f'`disable_1d` has the wrong type: Received `{type(params["disable_1d"])}` expected `bool`')
    if params.get("smooth_bias", None) is not None:
        if not isinstance(params["smooth_bias"], (float, int)):
            raise StyxValidationError(f'`smooth_bias` has the wrong type: Received `{type(params["smooth_bias"])}` expected `float`')
    if params.get("aseg", None) is not None:
        if not isinstance(params["aseg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`aseg` has the wrong type: Received `{type(params["aseg"])}` expected `InputPathType`')
    if params.get("debug_gvx", None) is not None:
        if not isinstance(params["debug_gvx"], list):
            raise StyxValidationError(f'`debug_gvx` has the wrong type: Received `{type(params["debug_gvx"])}` expected `list[float]`')
        if not (3 <= len(params["debug_gvx"]) <= 3):
            raise StyxValidationError("Parameter `debug_gvx` must contain between 3 and 3 elements (inclusive)")
        for e in params["debug_gvx"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`debug_gvx` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("debug_gx", None) is not None:
        if not isinstance(params["debug_gx"], list):
            raise StyxValidationError(f'`debug_gx` has the wrong type: Received `{type(params["debug_gx"])}` expected `list[float]`')
        if not (3 <= len(params["debug_gx"]) <= 3):
            raise StyxValidationError("Parameter `debug_gx` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["debug_gx"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`debug_gx` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("reading", None) is not None:
        if not isinstance(params["reading"], list):
            raise StyxValidationError(f'`reading` has the wrong type: Received `{type(params["reading"])}` expected `list[str]`')
        if not (2 <= len(params["reading"]) <= 2):
            raise StyxValidationError("Parameter `reading` must contain between 2 and 2 elements (inclusive)")
        for e_3 in params["reading"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`reading` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("print_usage", None) is not None:
        if not isinstance(params["print_usage"], bool):
            raise StyxValidationError(f'`print_usage` has the wrong type: Received `{type(params["print_usage"])}` expected `bool`')

def mri_long_normalize_cargs(params: MriLongNormalize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_long_normalize")
    cargs.append(execution.input_file(params["input_vol"]))
    cargs.append(execution.input_file(params["base_tp_file"]))
    cargs.append(params["output_vol"])
    v_0 = params.get("normalization_iters")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    if params.get("disable_1d"):
        cargs.append("-no1d")
    v_1 = params.get("smooth_bias")
    if v_1 is not None:
        cargs.append("-sigma")
        cargs.append(str(v_1))
    v_2 = params.get("aseg")
    if v_2 is not None:
        cargs.append("-a")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("debug_gvx")
    if v_3 is not None:
        cargs.append("-v")
        for item0 in v_3:
            cargs.append(str(item0))
    v_4 = params.get("debug_gx")
    if v_4 is not None:
        cargs.append("-d")
        for item1 in v_4:
            cargs.append(str(item1))
    v_5 = params.get("reading")
    if v_5 is not None:
        cargs.append("-r")
        for item2 in v_5:
            cargs.append(item2)
    if params.get("print_usage"):
        cargs.append("-u")
    return cargs

def mri_long_normalize_outputs(params: MriLongNormalize, execution: Execution) -> MriLongNormalizeOutputs:
    """Build the MriLongNormalizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_vol"])
    return MriLongNormalizeOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_long_normalize_execute(params: MriLongNormalize, runner: Runner | None = None) -> MriLongNormalizeOutputs:
    """
    mri_long_normalize

    Tool to normalize the white-matter of MRI volumes, optionally based on control points.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_long_normalize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LONG_NORMALIZE_METADATA)
    execution.params(params)
    args = mri_long_normalize_cargs(params, execution)
    out = mri_long_normalize_outputs(params, execution)
    execution.run(args)
    return out

def mri_long_normalize(
    input_vol: InputPathType,
    base_tp_file: InputPathType,
    output_vol: str,
    normalization_iters: int | None = None,
    disable_1d: bool = False,
    smooth_bias: float | None = None,
    aseg: InputPathType | None = None,
    debug_gvx: list[float] | None = None,
    debug_gx: list[float] | None = None,
    reading: list[str] | None = None,
    print_usage: bool = False,
    runner: Runner | None = None,
) -> MriLongNormalizeOutputs:
    """
    mri_long_normalize

    Tool to normalize the white-matter of MRI volumes, optionally based on control points.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_vol: Input MRI volume file.
        base_tp_file: Base time point file.
        output_vol: Output MRI volume file.
        normalization_iters: Use n 3D normalization iterations (default is 2).
        disable_1d: Disable 1D normalization.
        smooth_bias: Smooth the bias field.
        aseg: Aseg file specification.
        debug_gvx: For debugging: specify Gvx, Gvy, Gvz.
        debug_gx: For debugging: specify Gx, Gy, Gz.
        reading: For reading: specify control points and bias field.
        print_usage: Print usage information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_long_normalize_params(
        input_vol=input_vol,
        base_tp_file=base_tp_file,
        output_vol=output_vol,
        normalization_iters=normalization_iters,
        disable_1d=disable_1d,
        smooth_bias=smooth_bias,
        aseg=aseg,
        debug_gvx=debug_gvx,
        debug_gx=debug_gx,
        reading=reading,
        print_usage=print_usage,
    )
    return mri_long_normalize_execute(params, runner)

__all__ = [
    "MriLongNormalize",
    "MriLongNormalizeOutputs",
    "MRI_LONG_NORMALIZE_METADATA",
    "mri_long_normalize_cargs",
    "mri_long_normalize_outputs",
    "mri_long_normalize_params",
    "mri_long_normalize_execute",
    "mri_long_normalize_validate",
    "mri_long_normalize",
]