# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GLMFIT_SIM_METADATA = Metadata(
    id="mri_glmfit-sim",
    name="mri_glmfit-sim",
    package="freesurfer",
)

MriGlmfitSim = typing.TypedDict(
    "MriGlmfitSim",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_glmfit-sim"]],
        "glmdir": str,
        "cwp": typing.NotRequired[float],
        "mczsim": typing.NotRequired[str],
        "mczsim_dir": typing.NotRequired[str],
        "mczsim_label": typing.NotRequired[str],
        "perm": typing.NotRequired[str],
        "perm_resid": typing.NotRequired[bool],
        "perm_signflip": typing.NotRequired[bool],
        "grf": typing.NotRequired[str],
        "spaces_2": typing.NotRequired[bool],
        "spaces_3": typing.NotRequired[bool],
        "overwrite": typing.NotRequired[bool],
        "bg": typing.NotRequired[float],
        "sleep": typing.NotRequired[float],
        "a2009s": typing.NotRequired[bool],
        "annot": typing.NotRequired[str],
        "log": typing.NotRequired[str],
        "base": typing.NotRequired[str],
        "no_sim": typing.NotRequired[str],
        "seed": typing.NotRequired[float],
        "fwhm_override": typing.NotRequired[float],
        "fwhm_add": typing.NotRequired[float],
        "uniform": typing.NotRequired[list[float]],
        "no_out_annot": typing.NotRequired[bool],
        "no_cluster_mean": typing.NotRequired[bool],
        "y_file": typing.NotRequired[InputPathType],
        "centroid": typing.NotRequired[bool],
        "spatial_sum": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriGlmfitSimOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sig_voxel_mgh: OutputPathType | None
    """Sig volume corrected for voxel-wise multiple comparisons."""
    sig_cluster_mgh: OutputPathType | None
    """Sig volume corrected for cluster-wise multiple comparisons."""
    sig_cluster_summary: OutputPathType | None
    """Cluster summary table."""
    y_ocn_dat: OutputPathType | None
    """Summary of the input (y) over each cluster."""
    sig_ocn_mgh: OutputPathType | None
    """Output cluster number volume."""
    sig_ocn_annot: OutputPathType | None
    """Cluster annotation for surfaces."""
    sig_masked_mgh: OutputPathType | None
    """Original sig volume masked to show only clusters."""

def mri_glmfit_sim_params(
    glmdir: str,
    cwp: float | None = None,
    mczsim: str | None = None,
    mczsim_dir: str | None = None,
    mczsim_label: str | None = None,
    perm: str | None = None,
    perm_resid: bool = False,
    perm_signflip: bool = False,
    grf: str | None = None,
    spaces_2: bool = False,
    spaces_3: bool = False,
    overwrite: bool = False,
    bg: float | None = None,
    sleep: float | None = None,
    a2009s: bool = False,
    annot: str | None = None,
    log: str | None = None,
    base: str | None = None,
    no_sim: str | None = None,
    seed: float | None = None,
    fwhm_override: float | None = None,
    fwhm_add: float | None = None,
    uniform: list[float] | None = None,
    no_out_annot: bool = False,
    no_cluster_mean: bool = False,
    y_file: InputPathType | None = None,
    centroid: bool = False,
    spatial_sum: bool = False,
    help: bool = False,
) -> MriGlmfitSim:
    """
    Build parameters.

    Args:
        glmdir: Path to GLM directory.
        cwp: Cluster-wise p-value threshold, default is 0.05.
        mczsim: Use pre-computed z-based Monte Carlo simulations. Requires\
            vwthreshold and sign.
        mczsim_dir: Directory for custom Monte Carlo simulations, default is\
            FREESURFER_HOME/average/mult-comp-cor.
        mczsim_label: Label for custom Monte Carlo simulations, default is cortex.
        perm: Use permutation simulation with nsim iterations, cluster forming\
            threshold (CFT), and sign.
        perm_resid: Use permutation on the residual instead of raw data for\
            non-orthogonal designs.
        perm_signflip: Use sign flipping instead of shuffling for permutation.
        grf: Use Gaussian Random Fields (GRF) method, with vwthreshold and sign.
        spaces_2: Apply additional Bonferroni correction across 2 spaces.
        spaces_3: Apply additional Bonferroni correction across 3 spaces.
        overwrite: Delete previous CSDs.
        bg: Divide simulation into njobs and put in background.
        sleep: Number of seconds to sleep between background polls.
        a2009s: Use aparc.a2009s instead of aparc for region of vertex max.
        annot: Use specific annotation for region of vertex max.
        log: Specify logfile, default is csdbase.mri_glmfit-sim.log.
        base: Override csdbase name.
        no_sim: Do not simulate, only run cluster.
        seed: Set simulation random number generator seed.
        fwhm_override: Override fwhm in glmdir.
        fwhm_add: Add fwhmAdd to the estimated fwhm.
        uniform: Use uniform PDF instead of gaussian, specify min and max.
        no_out_annot: Do not output a cluster annotation.
        no_cluster_mean: Do not compute means of each subject in each cluster.
        y_file: Specify the GLM input y file.
        centroid: Report the coordinates/annotation of the centroid instead of max.
        spatial_sum: Compute the sum over voxels in the cluster rather than the\
            average.
        help: Show the help message and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriGlmfitSim = {
        "@type": "freesurfer/mri_glmfit-sim",
        "glmdir": glmdir,
        "perm_resid": perm_resid,
        "perm_signflip": perm_signflip,
        "spaces_2": spaces_2,
        "spaces_3": spaces_3,
        "overwrite": overwrite,
        "a2009s": a2009s,
        "no_out_annot": no_out_annot,
        "no_cluster_mean": no_cluster_mean,
        "centroid": centroid,
        "spatial_sum": spatial_sum,
        "help": help,
    }
    if cwp is not None:
        params["cwp"] = cwp
    if mczsim is not None:
        params["mczsim"] = mczsim
    if mczsim_dir is not None:
        params["mczsim_dir"] = mczsim_dir
    if mczsim_label is not None:
        params["mczsim_label"] = mczsim_label
    if perm is not None:
        params["perm"] = perm
    if grf is not None:
        params["grf"] = grf
    if bg is not None:
        params["bg"] = bg
    if sleep is not None:
        params["sleep"] = sleep
    if annot is not None:
        params["annot"] = annot
    if log is not None:
        params["log"] = log
    if base is not None:
        params["base"] = base
    if no_sim is not None:
        params["no_sim"] = no_sim
    if seed is not None:
        params["seed"] = seed
    if fwhm_override is not None:
        params["fwhm_override"] = fwhm_override
    if fwhm_add is not None:
        params["fwhm_add"] = fwhm_add
    if uniform is not None:
        params["uniform"] = uniform
    if y_file is not None:
        params["y_file"] = y_file
    return params

def mri_glmfit_sim_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGlmfitSim."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("glmdir", None) is None:
        raise StyxValidationError("`glmdir` must not be None")
    if not isinstance(params["glmdir"], str):
        raise StyxValidationError(f'`glmdir` has the wrong type: Received `{type(params["glmdir"])}` expected `str`')
    if params.get("cwp", None) is not None:
        if not isinstance(params["cwp"], (float, int)):
            raise StyxValidationError(f'`cwp` has the wrong type: Received `{type(params["cwp"])}` expected `float`')
    if params.get("mczsim", None) is not None:
        if not isinstance(params["mczsim"], str):
            raise StyxValidationError(f'`mczsim` has the wrong type: Received `{type(params["mczsim"])}` expected `str`')
    if params.get("mczsim_dir", None) is not None:
        if not isinstance(params["mczsim_dir"], str):
            raise StyxValidationError(f'`mczsim_dir` has the wrong type: Received `{type(params["mczsim_dir"])}` expected `str`')
    if params.get("mczsim_label", None) is not None:
        if not isinstance(params["mczsim_label"], str):
            raise StyxValidationError(f'`mczsim_label` has the wrong type: Received `{type(params["mczsim_label"])}` expected `str`')
    if params.get("perm", None) is not None:
        if not isinstance(params["perm"], str):
            raise StyxValidationError(f'`perm` has the wrong type: Received `{type(params["perm"])}` expected `str`')
    if params.get("perm_resid", None) is not None:
        if not isinstance(params["perm_resid"], bool):
            raise StyxValidationError(f'`perm_resid` has the wrong type: Received `{type(params["perm_resid"])}` expected `bool`')
    if params.get("perm_signflip", None) is not None:
        if not isinstance(params["perm_signflip"], bool):
            raise StyxValidationError(f'`perm_signflip` has the wrong type: Received `{type(params["perm_signflip"])}` expected `bool`')
    if params.get("grf", None) is not None:
        if not isinstance(params["grf"], str):
            raise StyxValidationError(f'`grf` has the wrong type: Received `{type(params["grf"])}` expected `str`')
    if params.get("spaces_2", None) is not None:
        if not isinstance(params["spaces_2"], bool):
            raise StyxValidationError(f'`spaces_2` has the wrong type: Received `{type(params["spaces_2"])}` expected `bool`')
    if params.get("spaces_3", None) is not None:
        if not isinstance(params["spaces_3"], bool):
            raise StyxValidationError(f'`spaces_3` has the wrong type: Received `{type(params["spaces_3"])}` expected `bool`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')
    if params.get("bg", None) is not None:
        if not isinstance(params["bg"], (float, int)):
            raise StyxValidationError(f'`bg` has the wrong type: Received `{type(params["bg"])}` expected `float`')
    if params.get("sleep", None) is not None:
        if not isinstance(params["sleep"], (float, int)):
            raise StyxValidationError(f'`sleep` has the wrong type: Received `{type(params["sleep"])}` expected `float`')
    if params.get("a2009s", None) is not None:
        if not isinstance(params["a2009s"], bool):
            raise StyxValidationError(f'`a2009s` has the wrong type: Received `{type(params["a2009s"])}` expected `bool`')
    if params.get("annot", None) is not None:
        if not isinstance(params["annot"], str):
            raise StyxValidationError(f'`annot` has the wrong type: Received `{type(params["annot"])}` expected `str`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], str):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `str`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], str):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `str`')
    if params.get("no_sim", None) is not None:
        if not isinstance(params["no_sim"], str):
            raise StyxValidationError(f'`no_sim` has the wrong type: Received `{type(params["no_sim"])}` expected `str`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (float, int)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `float`')
    if params.get("fwhm_override", None) is not None:
        if not isinstance(params["fwhm_override"], (float, int)):
            raise StyxValidationError(f'`fwhm_override` has the wrong type: Received `{type(params["fwhm_override"])}` expected `float`')
    if params.get("fwhm_add", None) is not None:
        if not isinstance(params["fwhm_add"], (float, int)):
            raise StyxValidationError(f'`fwhm_add` has the wrong type: Received `{type(params["fwhm_add"])}` expected `float`')
    if params.get("uniform", None) is not None:
        if not isinstance(params["uniform"], list):
            raise StyxValidationError(f'`uniform` has the wrong type: Received `{type(params["uniform"])}` expected `list[float]`')
        if len(params["uniform"]) > 2:
            raise StyxValidationError("Parameter `uniform` must contain at most 2 elements")
        for e in params["uniform"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`uniform` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("no_out_annot", None) is not None:
        if not isinstance(params["no_out_annot"], bool):
            raise StyxValidationError(f'`no_out_annot` has the wrong type: Received `{type(params["no_out_annot"])}` expected `bool`')
    if params.get("no_cluster_mean", None) is not None:
        if not isinstance(params["no_cluster_mean"], bool):
            raise StyxValidationError(f'`no_cluster_mean` has the wrong type: Received `{type(params["no_cluster_mean"])}` expected `bool`')
    if params.get("y_file", None) is not None:
        if not isinstance(params["y_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`y_file` has the wrong type: Received `{type(params["y_file"])}` expected `InputPathType`')
    if params.get("centroid", None) is not None:
        if not isinstance(params["centroid"], bool):
            raise StyxValidationError(f'`centroid` has the wrong type: Received `{type(params["centroid"])}` expected `bool`')
    if params.get("spatial_sum", None) is not None:
        if not isinstance(params["spatial_sum"], bool):
            raise StyxValidationError(f'`spatial_sum` has the wrong type: Received `{type(params["spatial_sum"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_glmfit_sim_cargs(params: MriGlmfitSim, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_glmfit-sim")
    cargs.append("--glmdir")
    cargs.append(params["glmdir"])
    v_0 = params.get("cwp")
    if v_0 is not None:
        cargs.append("--cwp")
        cargs.append(str(v_0))
    v_1 = params.get("mczsim")
    if v_1 is not None:
        cargs.append("--mczsim")
        cargs.append(v_1)
    v_2 = params.get("mczsim_dir")
    if v_2 is not None:
        cargs.append("--mczsim-dir")
        cargs.append(v_2)
    v_3 = params.get("mczsim_label")
    if v_3 is not None:
        cargs.append("--mczsim-label")
        cargs.append(v_3)
    v_4 = params.get("perm")
    if v_4 is not None:
        cargs.append("--perm")
        cargs.append(v_4)
    if params.get("perm_resid"):
        cargs.append("--perm-resid")
    if params.get("perm_signflip"):
        cargs.append("--perm-signflip")
    v_5 = params.get("grf")
    if v_5 is not None:
        cargs.append("--grf")
        cargs.append(v_5)
    if params.get("spaces_2"):
        cargs.append("--2spaces")
    if params.get("spaces_3"):
        cargs.append("--3spaces")
    if params.get("overwrite"):
        cargs.append("--overwrite")
    v_6 = params.get("bg")
    if v_6 is not None:
        cargs.append("--bg")
        cargs.append(str(v_6))
    v_7 = params.get("sleep")
    if v_7 is not None:
        cargs.append("--sleep")
        cargs.append(str(v_7))
    if params.get("a2009s"):
        cargs.append("--a2009s")
    v_8 = params.get("annot")
    if v_8 is not None:
        cargs.append("--annot")
        cargs.append(v_8)
    v_9 = params.get("log")
    if v_9 is not None:
        cargs.append("--log")
        cargs.append(v_9)
    v_10 = params.get("base")
    if v_10 is not None:
        cargs.append("--base")
        cargs.append(v_10)
    v_11 = params.get("no_sim")
    if v_11 is not None:
        cargs.append("--no-sim")
        cargs.append(v_11)
    v_12 = params.get("seed")
    if v_12 is not None:
        cargs.append("--seed")
        cargs.append(str(v_12))
    v_13 = params.get("fwhm_override")
    if v_13 is not None:
        cargs.append("--fwhm-override")
        cargs.append(str(v_13))
    v_14 = params.get("fwhm_add")
    if v_14 is not None:
        cargs.append("--fwhm-add")
        cargs.append(str(v_14))
    v_15 = params.get("uniform")
    if v_15 is not None:
        cargs.append("--uniform")
        for item0 in v_15:
            cargs.append(str(item0))
    if params.get("no_out_annot"):
        cargs.append("--no-out-annot")
    if params.get("no_cluster_mean"):
        cargs.append("--no-cluster-mean")
    v_16 = params.get("y_file")
    if v_16 is not None:
        cargs.append("--y")
        cargs.append(execution.input_file(v_16))
    if params.get("centroid"):
        cargs.append("--centroid")
    if params.get("spatial_sum"):
        cargs.append("--spatial-sum")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_glmfit_sim_outputs(params: MriGlmfitSim, execution: Execution) -> MriGlmfitSimOutputs:
    """Build the MriGlmfitSimOutputs object for this tool."""
    sig_voxel_mgh_v: OutputPathType | None = None
    sig_cluster_mgh_v: OutputPathType | None = None
    sig_cluster_summary_v: OutputPathType | None = None
    y_ocn_dat_v: OutputPathType | None = None
    sig_ocn_mgh_v: OutputPathType | None = None
    sig_ocn_annot_v: OutputPathType | None = None
    sig_masked_mgh_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("base")
    if __v0 is not None:
        sig_voxel_mgh_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.sig.voxel.mgh')
    __v1 = params.get("base")
    if __v1 is not None:
        sig_cluster_mgh_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.sig.cluster.mgh')
    __v2 = params.get("base")
    if __v2 is not None:
        sig_cluster_summary_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.sig.cluster.summary')
    __v3 = params.get("base")
    if __v3 is not None:
        y_ocn_dat_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}.y.ocn.dat')
    __v4 = params.get("base")
    if __v4 is not None:
        sig_ocn_mgh_v = execution.output_file(f'{(__v4 if __v4 is not None else "")}.sig.ocn.mgh')
    __v5 = params.get("base")
    if __v5 is not None:
        sig_ocn_annot_v = execution.output_file(f'{(__v5 if __v5 is not None else "")}.sig.ocn.annot')
    __v6 = params.get("base")
    if __v6 is not None:
        sig_masked_mgh_v = execution.output_file(f'{(__v6 if __v6 is not None else "")}.sig.masked.mgh')
    return MriGlmfitSimOutputs(
        root=root_v,
        sig_voxel_mgh=sig_voxel_mgh_v,
        sig_cluster_mgh=sig_cluster_mgh_v,
        sig_cluster_summary=sig_cluster_summary_v,
        y_ocn_dat=y_ocn_dat_v,
        sig_ocn_mgh=sig_ocn_mgh_v,
        sig_ocn_annot=sig_ocn_annot_v,
        sig_masked_mgh=sig_masked_mgh_v,
    )

def mri_glmfit_sim_execute(params: MriGlmfitSim, runner: Runner | None = None) -> MriGlmfitSimOutputs:
    """
    mri_glmfit-sim

    A tool to run corrections for multiple comparisons on volumes or surfaces, using various methods including Monte Carlo simulation, permutation, and Gaussian Random Fields.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_glmfit_sim_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GLMFIT_SIM_METADATA)
    execution.params(params)
    args = mri_glmfit_sim_cargs(params, execution)
    out = mri_glmfit_sim_outputs(params, execution)
    execution.run(args)
    return out

def mri_glmfit_sim(
    glmdir: str,
    cwp: float | None = None,
    mczsim: str | None = None,
    mczsim_dir: str | None = None,
    mczsim_label: str | None = None,
    perm: str | None = None,
    perm_resid: bool = False,
    perm_signflip: bool = False,
    grf: str | None = None,
    spaces_2: bool = False,
    spaces_3: bool = False,
    overwrite: bool = False,
    bg: float | None = None,
    sleep: float | None = None,
    a2009s: bool = False,
    annot: str | None = None,
    log: str | None = None,
    base: str | None = None,
    no_sim: str | None = None,
    seed: float | None = None,
    fwhm_override: float | None = None,
    fwhm_add: float | None = None,
    uniform: list[float] | None = None,
    no_out_annot: bool = False,
    no_cluster_mean: bool = False,
    y_file: InputPathType | None = None,
    centroid: bool = False,
    spatial_sum: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MriGlmfitSimOutputs:
    """
    mri_glmfit-sim

    A tool to run corrections for multiple comparisons on volumes or surfaces, using various methods including Monte Carlo simulation, permutation, and Gaussian Random Fields.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        glmdir: Path to GLM directory.
        cwp: Cluster-wise p-value threshold, default is 0.05.
        mczsim: Use pre-computed z-based Monte Carlo simulations. Requires\
            vwthreshold and sign.
        mczsim_dir: Directory for custom Monte Carlo simulations, default is\
            FREESURFER_HOME/average/mult-comp-cor.
        mczsim_label: Label for custom Monte Carlo simulations, default is cortex.
        perm: Use permutation simulation with nsim iterations, cluster forming\
            threshold (CFT), and sign.
        perm_resid: Use permutation on the residual instead of raw data for\
            non-orthogonal designs.
        perm_signflip: Use sign flipping instead of shuffling for permutation.
        grf: Use Gaussian Random Fields (GRF) method, with vwthreshold and sign.
        spaces_2: Apply additional Bonferroni correction across 2 spaces.
        spaces_3: Apply additional Bonferroni correction across 3 spaces.
        overwrite: Delete previous CSDs.
        bg: Divide simulation into njobs and put in background.
        sleep: Number of seconds to sleep between background polls.
        a2009s: Use aparc.a2009s instead of aparc for region of vertex max.
        annot: Use specific annotation for region of vertex max.
        log: Specify logfile, default is csdbase.mri_glmfit-sim.log.
        base: Override csdbase name.
        no_sim: Do not simulate, only run cluster.
        seed: Set simulation random number generator seed.
        fwhm_override: Override fwhm in glmdir.
        fwhm_add: Add fwhmAdd to the estimated fwhm.
        uniform: Use uniform PDF instead of gaussian, specify min and max.
        no_out_annot: Do not output a cluster annotation.
        no_cluster_mean: Do not compute means of each subject in each cluster.
        y_file: Specify the GLM input y file.
        centroid: Report the coordinates/annotation of the centroid instead of max.
        spatial_sum: Compute the sum over voxels in the cluster rather than the\
            average.
        help: Show the help message and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_glmfit_sim_params(
        glmdir=glmdir,
        cwp=cwp,
        mczsim=mczsim,
        mczsim_dir=mczsim_dir,
        mczsim_label=mczsim_label,
        perm=perm,
        perm_resid=perm_resid,
        perm_signflip=perm_signflip,
        grf=grf,
        spaces_2=spaces_2,
        spaces_3=spaces_3,
        overwrite=overwrite,
        bg=bg,
        sleep=sleep,
        a2009s=a2009s,
        annot=annot,
        log=log,
        base=base,
        no_sim=no_sim,
        seed=seed,
        fwhm_override=fwhm_override,
        fwhm_add=fwhm_add,
        uniform=uniform,
        no_out_annot=no_out_annot,
        no_cluster_mean=no_cluster_mean,
        y_file=y_file,
        centroid=centroid,
        spatial_sum=spatial_sum,
        help=help,
    )
    return mri_glmfit_sim_execute(params, runner)

__all__ = [
    "MriGlmfitSim",
    "MriGlmfitSimOutputs",
    "MRI_GLMFIT_SIM_METADATA",
    "mri_glmfit_sim_cargs",
    "mri_glmfit_sim_outputs",
    "mri_glmfit_sim_params",
    "mri_glmfit_sim_execute",
    "mri_glmfit_sim_validate",
    "mri_glmfit_sim",
]