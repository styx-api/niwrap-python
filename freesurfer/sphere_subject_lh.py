# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHERE_SUBJECT_LH_METADATA = Metadata(
    id="sphere_subject-lh",
    name="sphere_subject-lh",
    package="freesurfer",
)

SphereSubjectLh = typing.TypedDict(
    "SphereSubjectLh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sphere_subject-lh"]],
        "license_file": InputPathType,
    },
)

@dataclasses.dataclass
class SphereSubjectLhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def sphere_subject_lh_params(
    license_file: InputPathType,
) -> SphereSubjectLh:
    """
    Build parameters.

    Args:
        license_file:

    Returns:
        Parameter dictionary.
    """
    params: SphereSubjectLh = {
        "@type": "freesurfer/sphere_subject-lh",
        "license_file": license_file,
    }
    return params

def sphere_subject_lh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SphereSubjectLh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("license_file", None) is None:
        raise StyxValidationError("`license_file` must not be None")
    if not isinstance(params["license_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`license_file` has the wrong type: Received `{type(params["license_file"])}` expected `InputPathType`')

def sphere_subject_lh_cargs(params: SphereSubjectLh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sphere_subject-lh")
    cargs.append("-lh")
    cargs.append(execution.input_file(params["license_file"]))
    return cargs

def sphere_subject_lh_outputs(params: SphereSubjectLh, execution: Execution) -> SphereSubjectLhOutputs:
    """Build the SphereSubjectLhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SphereSubjectLhOutputs(
        root=root_v,
    )

def sphere_subject_lh_execute(params: SphereSubjectLh, runner: Runner | None = None) -> SphereSubjectLhOutputs:
    """
    sphere_subject-lh

    Tool for processing spherical representations in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sphere_subject_lh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHERE_SUBJECT_LH_METADATA)
    execution.params(params)
    args = sphere_subject_lh_cargs(params, execution)
    out = sphere_subject_lh_outputs(params, execution)
    execution.run(args)
    return out

def sphere_subject_lh(
    license_file: InputPathType,
    runner: Runner | None = None,
) -> SphereSubjectLhOutputs:
    """
    sphere_subject-lh

    Tool for processing spherical representations in FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        license_file:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sphere_subject_lh_params(
        license_file=license_file,
    )
    return sphere_subject_lh_execute(params, runner)

__all__ = [
    "SphereSubjectLh",
    "SphereSubjectLhOutputs",
    "SPHERE_SUBJECT_LH_METADATA",
    "sphere_subject_lh_cargs",
    "sphere_subject_lh_outputs",
    "sphere_subject_lh_params",
    "sphere_subject_lh_execute",
    "sphere_subject_lh_validate",
    "sphere_subject_lh",
]