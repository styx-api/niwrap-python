# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIX_SUBJECT_RH_METADATA = Metadata(
    id="fix_subject-rh",
    name="fix_subject-rh",
    package="freesurfer",
)

FixSubjectRh = typing.TypedDict(
    "FixSubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fix_subject-rh"]],
        "input_directory": str,
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FixSubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fix_subject_rh_params(
    input_directory: str,
    help_flag: bool = False,
) -> FixSubjectRh:
    """
    Build parameters.

    Args:
        input_directory: Directory where the subject's right hemisphere data is\
            located.
        help_flag: Displays help information for fix_subject-rh.

    Returns:
        Parameter dictionary.
    """
    params: FixSubjectRh = {
        "@type": "freesurfer/fix_subject-rh",
        "input_directory": input_directory,
        "help_flag": help_flag,
    }
    return params

def fix_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixSubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_directory", None) is None:
        raise StyxValidationError("`input_directory` must not be None")
    if not isinstance(params["input_directory"], str):
        raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def fix_subject_rh_cargs(params: FixSubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fix_subject-rh")
    cargs.append("-rh")
    cargs.append(params["input_directory"])
    if params.get("help_flag"):
        cargs.append("--help")
    return cargs

def fix_subject_rh_outputs(params: FixSubjectRh, execution: Execution) -> FixSubjectRhOutputs:
    """Build the FixSubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixSubjectRhOutputs(
        root=root_v,
    )

def fix_subject_rh_execute(params: FixSubjectRh, runner: Runner | None = None) -> FixSubjectRhOutputs:
    """
    fix_subject-rh

    A tool from FreeSurfer that performs operations on the right hemisphere data within a specified directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fix_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIX_SUBJECT_RH_METADATA)
    execution.params(params)
    args = fix_subject_rh_cargs(params, execution)
    out = fix_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def fix_subject_rh(
    input_directory: str,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> FixSubjectRhOutputs:
    """
    fix_subject-rh

    A tool from FreeSurfer that performs operations on the right hemisphere data within a specified directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_directory: Directory where the subject's right hemisphere data is\
            located.
        help_flag: Displays help information for fix_subject-rh.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fix_subject_rh_params(
        input_directory=input_directory,
        help_flag=help_flag,
    )
    return fix_subject_rh_execute(params, runner)

__all__ = [
    "FixSubjectRh",
    "FixSubjectRhOutputs",
    "FIX_SUBJECT_RH_METADATA",
    "fix_subject_rh_cargs",
    "fix_subject_rh_outputs",
    "fix_subject_rh_params",
    "fix_subject_rh_execute",
    "fix_subject_rh_validate",
    "fix_subject_rh",
]