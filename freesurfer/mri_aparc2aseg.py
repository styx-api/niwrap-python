# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_APARC2ASEG_METADATA = Metadata(
    id="mri_aparc2aseg",
    name="mri_aparc2aseg",
    package="freesurfer",
)

MriAparc2aseg = typing.TypedDict(
    "MriAparc2aseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_aparc2aseg"]],
        "subject": typing.NotRequired[str],
        "output_volfile": typing.NotRequired[str],
        "old_ribbon": typing.NotRequired[bool],
        "new_ribbon": typing.NotRequired[bool],
        "a2005s": typing.NotRequired[bool],
        "a2009s": typing.NotRequired[bool],
        "annot_name": typing.NotRequired[str],
        "annot_table": typing.NotRequired[str],
        "base_offset": typing.NotRequired[float],
        "label_wm": typing.NotRequired[bool],
        "wmparc_dmax": typing.NotRequired[float],
        "rip_unknown": typing.NotRequired[bool],
        "hypo_as_wm": typing.NotRequired[bool],
        "no_fix_parahip": typing.NotRequired[bool],
        "smooth_normals": typing.NotRequired[float],
        "crs_test": typing.NotRequired[str],
        "left_hemisphere": typing.NotRequired[bool],
        "right_hemisphere": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriAparc2asegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Output segmentation volume."""

def mri_aparc2aseg_params(
    subject: str | None = None,
    output_volfile: str | None = None,
    old_ribbon: bool = False,
    new_ribbon: bool = False,
    a2005s: bool = False,
    a2009s: bool = False,
    annot_name: str | None = None,
    annot_table: str | None = None,
    base_offset: float | None = None,
    label_wm: bool = False,
    wmparc_dmax: float | None = None,
    rip_unknown: bool = False,
    hypo_as_wm: bool = False,
    no_fix_parahip: bool = False,
    smooth_normals: float | None = None,
    crs_test: str | None = None,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    threads: float | None = None,
    help: bool = False,
    version: bool = False,
) -> MriAparc2aseg:
    """
    Build parameters.

    Args:
        subject: Name of the subject as found in the SUBJECTS_DIR.
        output_volfile: Full path of file to save the output segmentation in.\
            Default is mri/aparc+aseg.mgz
        old_ribbon: Use mri/hemi.ribbon.mgz as a mask for the cortex
        new_ribbon: Mask cortical voxels with mri/ribbon.mgz. Same as --volmask
        a2005s: Use ?h.aparc.a2005s.annot. Output will be aparc.a2005s+aseg.mgz.
        a2009s: Use ?h.aparc.a2009s.annot. Output will be aparc.a2009s+aseg.mgz.
        annot_name: Use annotname surface annotation. By default, uses\
            ?h.aparc.annot.
        annot_table: Annotation table file. Default is\
            $FREESURFER_HOME/Simple_surface_labels2009.txt
        base_offset: Add offset to all segmentation ids.
        label_wm: Re-assign white matter voxel labels to the closest cortical point\
            if within distance.
        wmparc_dmax: Max distance (mm) from cortex to be labeled as gyral WM\
            (default 5mm)
        rip_unknown: Do not label WM based on 'unknown' cortical label
        hypo_as_wm: Label hypointensities as WM
        no_fix_parahip: Do not remove unconnected regions from WM parahip
        smooth_normals: Change number of surface normal smoothing steps (default is\
            10).
        crs_test: Test mapping of column, row, slice
        left_hemisphere: Only process the left hemisphere
        right_hemisphere: Only process the right hemisphere
        threads: Run in parallel with the specified number of threads
        help: Print out information on how to use this program
        version: Print out version and exit

    Returns:
        Parameter dictionary.
    """
    params: MriAparc2aseg = {
        "@type": "freesurfer/mri_aparc2aseg",
        "old_ribbon": old_ribbon,
        "new_ribbon": new_ribbon,
        "a2005s": a2005s,
        "a2009s": a2009s,
        "label_wm": label_wm,
        "rip_unknown": rip_unknown,
        "hypo_as_wm": hypo_as_wm,
        "no_fix_parahip": no_fix_parahip,
        "left_hemisphere": left_hemisphere,
        "right_hemisphere": right_hemisphere,
        "help": help,
        "version": version,
    }
    if subject is not None:
        params["subject"] = subject
    if output_volfile is not None:
        params["output_volfile"] = output_volfile
    if annot_name is not None:
        params["annot_name"] = annot_name
    if annot_table is not None:
        params["annot_table"] = annot_table
    if base_offset is not None:
        params["base_offset"] = base_offset
    if wmparc_dmax is not None:
        params["wmparc_dmax"] = wmparc_dmax
    if smooth_normals is not None:
        params["smooth_normals"] = smooth_normals
    if crs_test is not None:
        params["crs_test"] = crs_test
    if threads is not None:
        params["threads"] = threads
    return params

def mri_aparc2aseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAparc2aseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("output_volfile", None) is not None:
        if not isinstance(params["output_volfile"], str):
            raise StyxValidationError(f'`output_volfile` has the wrong type: Received `{type(params["output_volfile"])}` expected `str`')
    if params.get("old_ribbon", None) is not None:
        if not isinstance(params["old_ribbon"], bool):
            raise StyxValidationError(f'`old_ribbon` has the wrong type: Received `{type(params["old_ribbon"])}` expected `bool`')
    if params.get("new_ribbon", None) is not None:
        if not isinstance(params["new_ribbon"], bool):
            raise StyxValidationError(f'`new_ribbon` has the wrong type: Received `{type(params["new_ribbon"])}` expected `bool`')
    if params.get("a2005s", None) is not None:
        if not isinstance(params["a2005s"], bool):
            raise StyxValidationError(f'`a2005s` has the wrong type: Received `{type(params["a2005s"])}` expected `bool`')
    if params.get("a2009s", None) is not None:
        if not isinstance(params["a2009s"], bool):
            raise StyxValidationError(f'`a2009s` has the wrong type: Received `{type(params["a2009s"])}` expected `bool`')
    if params.get("annot_name", None) is not None:
        if not isinstance(params["annot_name"], str):
            raise StyxValidationError(f'`annot_name` has the wrong type: Received `{type(params["annot_name"])}` expected `str`')
    if params.get("annot_table", None) is not None:
        if not isinstance(params["annot_table"], str):
            raise StyxValidationError(f'`annot_table` has the wrong type: Received `{type(params["annot_table"])}` expected `str`')
    if params.get("base_offset", None) is not None:
        if not isinstance(params["base_offset"], (float, int)):
            raise StyxValidationError(f'`base_offset` has the wrong type: Received `{type(params["base_offset"])}` expected `float`')
    if params.get("label_wm", None) is not None:
        if not isinstance(params["label_wm"], bool):
            raise StyxValidationError(f'`label_wm` has the wrong type: Received `{type(params["label_wm"])}` expected `bool`')
    if params.get("wmparc_dmax", None) is not None:
        if not isinstance(params["wmparc_dmax"], (float, int)):
            raise StyxValidationError(f'`wmparc_dmax` has the wrong type: Received `{type(params["wmparc_dmax"])}` expected `float`')
    if params.get("rip_unknown", None) is not None:
        if not isinstance(params["rip_unknown"], bool):
            raise StyxValidationError(f'`rip_unknown` has the wrong type: Received `{type(params["rip_unknown"])}` expected `bool`')
    if params.get("hypo_as_wm", None) is not None:
        if not isinstance(params["hypo_as_wm"], bool):
            raise StyxValidationError(f'`hypo_as_wm` has the wrong type: Received `{type(params["hypo_as_wm"])}` expected `bool`')
    if params.get("no_fix_parahip", None) is not None:
        if not isinstance(params["no_fix_parahip"], bool):
            raise StyxValidationError(f'`no_fix_parahip` has the wrong type: Received `{type(params["no_fix_parahip"])}` expected `bool`')
    if params.get("smooth_normals", None) is not None:
        if not isinstance(params["smooth_normals"], (float, int)):
            raise StyxValidationError(f'`smooth_normals` has the wrong type: Received `{type(params["smooth_normals"])}` expected `float`')
    if params.get("crs_test", None) is not None:
        if not isinstance(params["crs_test"], str):
            raise StyxValidationError(f'`crs_test` has the wrong type: Received `{type(params["crs_test"])}` expected `str`')
    if params.get("left_hemisphere", None) is not None:
        if not isinstance(params["left_hemisphere"], bool):
            raise StyxValidationError(f'`left_hemisphere` has the wrong type: Received `{type(params["left_hemisphere"])}` expected `bool`')
    if params.get("right_hemisphere", None) is not None:
        if not isinstance(params["right_hemisphere"], bool):
            raise StyxValidationError(f'`right_hemisphere` has the wrong type: Received `{type(params["right_hemisphere"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_aparc2aseg_cargs(params: MriAparc2aseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_aparc2aseg")
    v_0 = params.get("subject")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    v_1 = params.get("output_volfile")
    if v_1 is not None:
        cargs.append("--o")
        cargs.append(v_1)
    if params.get("old_ribbon"):
        cargs.append("--old-ribbon")
    if params.get("new_ribbon"):
        cargs.append("--new-ribbon")
    if params.get("a2005s"):
        cargs.append("--a2005s")
    if params.get("a2009s"):
        cargs.append("--a2009s")
    v_2 = params.get("annot_name")
    if v_2 is not None:
        cargs.append("--annot")
        cargs.append(v_2)
    v_3 = params.get("annot_table")
    if v_3 is not None:
        cargs.append("--annot-table")
        cargs.append(v_3)
    v_4 = params.get("base_offset")
    if v_4 is not None:
        cargs.append("--base-offset")
        cargs.append(str(v_4))
    if params.get("label_wm"):
        cargs.append("--labelwm")
    v_5 = params.get("wmparc_dmax")
    if v_5 is not None:
        cargs.append("--wmparc-dmax")
        cargs.append(str(v_5))
    if params.get("rip_unknown"):
        cargs.append("--rip-unknown")
    if params.get("hypo_as_wm"):
        cargs.append("--hypo-as-wm")
    if params.get("no_fix_parahip"):
        cargs.append("--no-fix-parahip")
    v_6 = params.get("smooth_normals")
    if v_6 is not None:
        cargs.append("--smooth_normals")
        cargs.append(str(v_6))
    v_7 = params.get("crs_test")
    if v_7 is not None:
        cargs.append("--crs-test")
        cargs.append(v_7)
    if params.get("left_hemisphere"):
        cargs.append("--lh")
    if params.get("right_hemisphere"):
        cargs.append("--rh")
    v_8 = params.get("threads")
    if v_8 is not None:
        cargs.append("--threads")
        cargs.append(str(v_8))
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_aparc2aseg_outputs(params: MriAparc2aseg, execution: Execution) -> MriAparc2asegOutputs:
    """Build the MriAparc2asegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file("mri/aparc+aseg.mgz")
    return MriAparc2asegOutputs(
        root=root_v,
        output_volume=output_volume_v,
    )

def mri_aparc2aseg_execute(params: MriAparc2aseg, runner: Runner | None = None) -> MriAparc2asegOutputs:
    """
    mri_aparc2aseg

    Maps the cortical labels from the automatic cortical parcellation to the automatic segmentation volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_aparc2aseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_APARC2ASEG_METADATA)
    execution.params(params)
    args = mri_aparc2aseg_cargs(params, execution)
    out = mri_aparc2aseg_outputs(params, execution)
    execution.run(args)
    return out

def mri_aparc2aseg(
    subject: str | None = None,
    output_volfile: str | None = None,
    old_ribbon: bool = False,
    new_ribbon: bool = False,
    a2005s: bool = False,
    a2009s: bool = False,
    annot_name: str | None = None,
    annot_table: str | None = None,
    base_offset: float | None = None,
    label_wm: bool = False,
    wmparc_dmax: float | None = None,
    rip_unknown: bool = False,
    hypo_as_wm: bool = False,
    no_fix_parahip: bool = False,
    smooth_normals: float | None = None,
    crs_test: str | None = None,
    left_hemisphere: bool = False,
    right_hemisphere: bool = False,
    threads: float | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriAparc2asegOutputs:
    """
    mri_aparc2aseg

    Maps the cortical labels from the automatic cortical parcellation to the automatic segmentation volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Name of the subject as found in the SUBJECTS_DIR.
        output_volfile: Full path of file to save the output segmentation in.\
            Default is mri/aparc+aseg.mgz
        old_ribbon: Use mri/hemi.ribbon.mgz as a mask for the cortex
        new_ribbon: Mask cortical voxels with mri/ribbon.mgz. Same as --volmask
        a2005s: Use ?h.aparc.a2005s.annot. Output will be aparc.a2005s+aseg.mgz.
        a2009s: Use ?h.aparc.a2009s.annot. Output will be aparc.a2009s+aseg.mgz.
        annot_name: Use annotname surface annotation. By default, uses\
            ?h.aparc.annot.
        annot_table: Annotation table file. Default is\
            $FREESURFER_HOME/Simple_surface_labels2009.txt
        base_offset: Add offset to all segmentation ids.
        label_wm: Re-assign white matter voxel labels to the closest cortical point\
            if within distance.
        wmparc_dmax: Max distance (mm) from cortex to be labeled as gyral WM\
            (default 5mm)
        rip_unknown: Do not label WM based on 'unknown' cortical label
        hypo_as_wm: Label hypointensities as WM
        no_fix_parahip: Do not remove unconnected regions from WM parahip
        smooth_normals: Change number of surface normal smoothing steps (default is\
            10).
        crs_test: Test mapping of column, row, slice
        left_hemisphere: Only process the left hemisphere
        right_hemisphere: Only process the right hemisphere
        threads: Run in parallel with the specified number of threads
        help: Print out information on how to use this program
        version: Print out version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_aparc2aseg_params(
        subject=subject,
        output_volfile=output_volfile,
        old_ribbon=old_ribbon,
        new_ribbon=new_ribbon,
        a2005s=a2005s,
        a2009s=a2009s,
        annot_name=annot_name,
        annot_table=annot_table,
        base_offset=base_offset,
        label_wm=label_wm,
        wmparc_dmax=wmparc_dmax,
        rip_unknown=rip_unknown,
        hypo_as_wm=hypo_as_wm,
        no_fix_parahip=no_fix_parahip,
        smooth_normals=smooth_normals,
        crs_test=crs_test,
        left_hemisphere=left_hemisphere,
        right_hemisphere=right_hemisphere,
        threads=threads,
        help=help,
        version=version,
    )
    return mri_aparc2aseg_execute(params, runner)

__all__ = [
    "MriAparc2aseg",
    "MriAparc2asegOutputs",
    "MRI_APARC2ASEG_METADATA",
    "mri_aparc2aseg_cargs",
    "mri_aparc2aseg_outputs",
    "mri_aparc2aseg_params",
    "mri_aparc2aseg_execute",
    "mri_aparc2aseg_validate",
    "mri_aparc2aseg",
]