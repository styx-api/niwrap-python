# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_VOLSMOOTH_METADATA = Metadata(
    id="mris_volsmooth",
    name="mris_volsmooth",
    package="freesurfer",
)

MrisVolsmooth = typing.TypedDict(
    "MrisVolsmooth",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_volsmooth"]],
        "input_volume": InputPathType,
        "output_volume": str,
        "registration": InputPathType,
        "projfrac": typing.NotRequired[float],
        "projfrac_avg": typing.NotRequired[str],
        "fill_ribbon": typing.NotRequired[bool],
        "surf_out": typing.NotRequired[str],
        "fwhm": typing.NotRequired[float],
        "niters": typing.NotRequired[float],
        "vol_fwhm": typing.NotRequired[float],
        "log": typing.NotRequired[str],
        "nocleanup": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisVolsmoothOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outvol_file: OutputPathType
    """Output volume file after surface-based smoothing."""
    lh_surface_output: OutputPathType | None
    """Left hemisphere smoothed surface output."""
    rh_surface_output: OutputPathType | None
    """Right hemisphere smoothed surface output."""

def mris_volsmooth_params(
    input_volume: InputPathType,
    output_volume: str,
    registration: InputPathType,
    projfrac: float | None = None,
    projfrac_avg: str | None = None,
    fill_ribbon: bool = False,
    surf_out: str | None = None,
    fwhm: float | None = None,
    niters: float | None = None,
    vol_fwhm: float | None = None,
    log: str | None = None,
    nocleanup: bool = False,
    debug: bool = False,
) -> MrisVolsmooth:
    """
    Build parameters.

    Args:
        input_volume: Source volume with values that will be smoothed on the\
            surface.
        output_volume: Output volume.
        registration: TKRegister-style registration matrix that maps between the\
            input/output volumes and the FreeSurfer surface anatomical.
        projfrac: Fraction of thickness to project along surface normal.
        projfrac_avg: Average sampling along normal, specified by min, max, and\
            delta.
        fill_ribbon: Fill ribbon.
        surf_out: Save smoothed surfaces as basename.?h.mgh
        fwhm: Surface smoothing by full-width/half-max in mm.
        niters: Specify surface smoothing by number of nearest neighbor smoothing\
            iterations.
        vol_fwhm: Volume smoothing outside of the surface. Surface voxels and\
            non-surface voxels are smoothed separately.
        log: Explicitly set log file.
        nocleanup: Do not delete temporary files.
        debug: Turn on debugging.

    Returns:
        Parameter dictionary.
    """
    params: MrisVolsmooth = {
        "@type": "freesurfer/mris_volsmooth",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "registration": registration,
        "fill_ribbon": fill_ribbon,
        "nocleanup": nocleanup,
        "debug": debug,
    }
    if projfrac is not None:
        params["projfrac"] = projfrac
    if projfrac_avg is not None:
        params["projfrac_avg"] = projfrac_avg
    if surf_out is not None:
        params["surf_out"] = surf_out
    if fwhm is not None:
        params["fwhm"] = fwhm
    if niters is not None:
        params["niters"] = niters
    if vol_fwhm is not None:
        params["vol_fwhm"] = vol_fwhm
    if log is not None:
        params["log"] = log
    return params

def mris_volsmooth_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisVolsmooth."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("registration", None) is None:
        raise StyxValidationError("`registration` must not be None")
    if not isinstance(params["registration"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration` has the wrong type: Received `{type(params["registration"])}` expected `InputPathType`')
    if params.get("projfrac", None) is not None:
        if not isinstance(params["projfrac"], (float, int)):
            raise StyxValidationError(f'`projfrac` has the wrong type: Received `{type(params["projfrac"])}` expected `float`')
    if params.get("projfrac_avg", None) is not None:
        if not isinstance(params["projfrac_avg"], str):
            raise StyxValidationError(f'`projfrac_avg` has the wrong type: Received `{type(params["projfrac_avg"])}` expected `str`')
    if params.get("fill_ribbon", None) is not None:
        if not isinstance(params["fill_ribbon"], bool):
            raise StyxValidationError(f'`fill_ribbon` has the wrong type: Received `{type(params["fill_ribbon"])}` expected `bool`')
    if params.get("surf_out", None) is not None:
        if not isinstance(params["surf_out"], str):
            raise StyxValidationError(f'`surf_out` has the wrong type: Received `{type(params["surf_out"])}` expected `str`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("niters", None) is not None:
        if not isinstance(params["niters"], (float, int)):
            raise StyxValidationError(f'`niters` has the wrong type: Received `{type(params["niters"])}` expected `float`')
    if params.get("vol_fwhm", None) is not None:
        if not isinstance(params["vol_fwhm"], (float, int)):
            raise StyxValidationError(f'`vol_fwhm` has the wrong type: Received `{type(params["vol_fwhm"])}` expected `float`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], str):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `str`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def mris_volsmooth_cargs(params: MrisVolsmooth, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_volsmooth")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("-o")
    cargs.append(params["output_volume"])
    cargs.append("-reg")
    cargs.append(execution.input_file(params["registration"]))
    v_0 = params.get("projfrac")
    if v_0 is not None:
        cargs.append("--projfrac")
        cargs.append(str(v_0))
    v_1 = params.get("projfrac_avg")
    if v_1 is not None:
        cargs.append("--projfrac-avg")
        cargs.append(v_1)
    if params.get("fill_ribbon"):
        cargs.append("--fill-ribbon")
    v_2 = params.get("surf_out")
    if v_2 is not None:
        cargs.append("--surf-out")
        cargs.append(v_2)
    v_3 = params.get("fwhm")
    if v_3 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_3))
    v_4 = params.get("niters")
    if v_4 is not None:
        cargs.append("--niters")
        cargs.append(str(v_4))
    v_5 = params.get("vol_fwhm")
    if v_5 is not None:
        cargs.append("--vol-fwhm")
        cargs.append(str(v_5))
    v_6 = params.get("log")
    if v_6 is not None:
        cargs.append("--log")
        cargs.append(v_6)
    if params.get("nocleanup"):
        cargs.append("--nocleanup")
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def mris_volsmooth_outputs(params: MrisVolsmooth, execution: Execution) -> MrisVolsmoothOutputs:
    """Build the MrisVolsmoothOutputs object for this tool."""
    lh_surface_output_v: OutputPathType | None = None
    rh_surface_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    outvol_file_v: OutputPathType = execution.output_file(f'{params["output_volume"]}.mgh')
    __v0 = params.get("surf_out")
    if __v0 is not None:
        lh_surface_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.lh.mgh')
    __v1 = params.get("surf_out")
    if __v1 is not None:
        rh_surface_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.rh.mgh')
    return MrisVolsmoothOutputs(
        root=root_v,
        outvol_file=outvol_file_v,
        lh_surface_output=lh_surface_output_v,
        rh_surface_output=rh_surface_output_v,
    )

def mris_volsmooth_execute(params: MrisVolsmooth, runner: Runner | None = None) -> MrisVolsmoothOutputs:
    """
    mris_volsmooth

    Performs surface-based smoothing inside a volume by sampling a volume to a surface, smoothing on the surface, then replacing the surface voxels in the volume with values that were smoothed.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_volsmooth_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_VOLSMOOTH_METADATA)
    execution.params(params)
    args = mris_volsmooth_cargs(params, execution)
    out = mris_volsmooth_outputs(params, execution)
    execution.run(args)
    return out

def mris_volsmooth(
    input_volume: InputPathType,
    output_volume: str,
    registration: InputPathType,
    projfrac: float | None = None,
    projfrac_avg: str | None = None,
    fill_ribbon: bool = False,
    surf_out: str | None = None,
    fwhm: float | None = None,
    niters: float | None = None,
    vol_fwhm: float | None = None,
    log: str | None = None,
    nocleanup: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> MrisVolsmoothOutputs:
    """
    mris_volsmooth

    Performs surface-based smoothing inside a volume by sampling a volume to a surface, smoothing on the surface, then replacing the surface voxels in the volume with values that were smoothed.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Source volume with values that will be smoothed on the\
            surface.
        output_volume: Output volume.
        registration: TKRegister-style registration matrix that maps between the\
            input/output volumes and the FreeSurfer surface anatomical.
        projfrac: Fraction of thickness to project along surface normal.
        projfrac_avg: Average sampling along normal, specified by min, max, and\
            delta.
        fill_ribbon: Fill ribbon.
        surf_out: Save smoothed surfaces as basename.?h.mgh
        fwhm: Surface smoothing by full-width/half-max in mm.
        niters: Specify surface smoothing by number of nearest neighbor smoothing\
            iterations.
        vol_fwhm: Volume smoothing outside of the surface. Surface voxels and\
            non-surface voxels are smoothed separately.
        log: Explicitly set log file.
        nocleanup: Do not delete temporary files.
        debug: Turn on debugging.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_volsmooth_params(
        input_volume=input_volume,
        output_volume=output_volume,
        registration=registration,
        projfrac=projfrac,
        projfrac_avg=projfrac_avg,
        fill_ribbon=fill_ribbon,
        surf_out=surf_out,
        fwhm=fwhm,
        niters=niters,
        vol_fwhm=vol_fwhm,
        log=log,
        nocleanup=nocleanup,
        debug=debug,
    )
    return mris_volsmooth_execute(params, runner)

__all__ = [
    "MrisVolsmooth",
    "MrisVolsmoothOutputs",
    "MRIS_VOLSMOOTH_METADATA",
    "mris_volsmooth_cargs",
    "mris_volsmooth_outputs",
    "mris_volsmooth_params",
    "mris_volsmooth_execute",
    "mris_volsmooth_validate",
    "mris_volsmooth",
]