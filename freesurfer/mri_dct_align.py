# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_DCT_ALIGN_METADATA = Metadata(
    id="mri_dct_align",
    name="mri_dct_align",
    package="freesurfer",
)

MriDctAlign = typing.TypedDict(
    "MriDctAlign",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_dct_align"]],
        "source": InputPathType,
        "destination": InputPathType,
        "output_xform": str,
    },
)

@dataclasses.dataclass
class MriDctAlignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xform_file: OutputPathType
    """Transformation file resulting from the alignment"""

def mri_dct_align_params(
    source: InputPathType,
    destination: InputPathType,
    output_xform: str,
) -> MriDctAlign:
    """
    Build parameters.

    Args:
        source: Source image file
        destination: Destination image file
        output_xform: Output transformation file

    Returns:
        Parameter dictionary.
    """
    params: MriDctAlign = {
        "@type": "freesurfer/mri_dct_align",
        "source": source,
        "destination": destination,
        "output_xform": output_xform,
    }
    return params

def mri_dct_align_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriDctAlign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("destination", None) is None:
        raise StyxValidationError("`destination` must not be None")
    if not isinstance(params["destination"], (pathlib.Path, str)):
        raise StyxValidationError(f'`destination` has the wrong type: Received `{type(params["destination"])}` expected `InputPathType`')
    if params.get("output_xform", None) is None:
        raise StyxValidationError("`output_xform` must not be None")
    if not isinstance(params["output_xform"], str):
        raise StyxValidationError(f'`output_xform` has the wrong type: Received `{type(params["output_xform"])}` expected `str`')

def mri_dct_align_cargs(params: MriDctAlign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_dct_align")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["destination"]))
    cargs.append(params["output_xform"])
    return cargs

def mri_dct_align_outputs(params: MriDctAlign, execution: Execution) -> MriDctAlignOutputs:
    """Build the MriDctAlignOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xform_file_v: OutputPathType = execution.output_file(params["output_xform"])
    return MriDctAlignOutputs(
        root=root_v,
        output_xform_file=output_xform_file_v,
    )

def mri_dct_align_execute(params: MriDctAlign, runner: Runner | None = None) -> MriDctAlignOutputs:
    """
    mri_dct_align

    MRI DCT alignment tool from FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_dct_align_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_DCT_ALIGN_METADATA)
    execution.params(params)
    args = mri_dct_align_cargs(params, execution)
    out = mri_dct_align_outputs(params, execution)
    execution.run(args)
    return out

def mri_dct_align(
    source: InputPathType,
    destination: InputPathType,
    output_xform: str,
    runner: Runner | None = None,
) -> MriDctAlignOutputs:
    """
    mri_dct_align

    MRI DCT alignment tool from FreeSurfer

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source: Source image file
        destination: Destination image file
        output_xform: Output transformation file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_dct_align_params(
        source=source,
        destination=destination,
        output_xform=output_xform,
    )
    return mri_dct_align_execute(params, runner)

__all__ = [
    "MriDctAlign",
    "MriDctAlignOutputs",
    "MRI_DCT_ALIGN_METADATA",
    "mri_dct_align_cargs",
    "mri_dct_align_outputs",
    "mri_dct_align_params",
    "mri_dct_align_execute",
    "mri_dct_align_validate",
    "mri_dct_align",
]