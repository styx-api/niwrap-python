# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHERE_SUBJECT_METADATA = Metadata(
    id="sphere_subject",
    name="sphere_subject",
    package="freesurfer",
)

SphereSubject = typing.TypedDict(
    "SphereSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sphere_subject"]],
        "input_dir": str,
        "output_file": str,
        "license_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SphereSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_result: OutputPathType
    """Output of the sphere_subject execution."""

def sphere_subject_params(
    input_dir: str,
    output_file: str,
    license_file: str | None = None,
) -> SphereSubject:
    """
    Build parameters.

    Args:
        input_dir: Input subject directory
        output_file: Output file for results
        license_file: Path to FreeSurfer license file

    Returns:
        Parameter dictionary.
    """
    params: SphereSubject = {
        "@type": "freesurfer/sphere_subject",
        "input_dir": input_dir,
        "output_file": output_file,
    }
    if license_file is not None:
        params["license_file"] = license_file
    return params

def sphere_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SphereSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dir", None) is None:
        raise StyxValidationError("`input_dir` must not be None")
    if not isinstance(params["input_dir"], str):
        raise StyxValidationError(f'`input_dir` has the wrong type: Received `{type(params["input_dir"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("license_file", None) is not None:
        if not isinstance(params["license_file"], str):
            raise StyxValidationError(f'`license_file` has the wrong type: Received `{type(params["license_file"])}` expected `str`')

def sphere_subject_cargs(params: SphereSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sphere_subject")
    cargs.append(params["input_dir"])
    cargs.append(params["output_file"])
    v_0 = params.get("license_file")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def sphere_subject_outputs(params: SphereSubject, execution: Execution) -> SphereSubjectOutputs:
    """Build the SphereSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_result_v: OutputPathType = execution.output_file(params["output_file"])
    return SphereSubjectOutputs(
        root=root_v,
        output_result=output_result_v,
    )

def sphere_subject_execute(params: SphereSubject, runner: Runner | None = None) -> SphereSubjectOutputs:
    """
    sphere_subject

    A FreeSurfer tool for processing subject data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sphere_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHERE_SUBJECT_METADATA)
    execution.params(params)
    args = sphere_subject_cargs(params, execution)
    out = sphere_subject_outputs(params, execution)
    execution.run(args)
    return out

def sphere_subject(
    input_dir: str,
    output_file: str,
    license_file: str | None = None,
    runner: Runner | None = None,
) -> SphereSubjectOutputs:
    """
    sphere_subject

    A FreeSurfer tool for processing subject data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_dir: Input subject directory
        output_file: Output file for results
        license_file: Path to FreeSurfer license file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sphere_subject_params(
        input_dir=input_dir,
        output_file=output_file,
        license_file=license_file,
    )
    return sphere_subject_execute(params, runner)

__all__ = [
    "SphereSubject",
    "SphereSubjectOutputs",
    "SPHERE_SUBJECT_METADATA",
    "sphere_subject_cargs",
    "sphere_subject_outputs",
    "sphere_subject_params",
    "sphere_subject_execute",
    "sphere_subject_validate",
    "sphere_subject",
]