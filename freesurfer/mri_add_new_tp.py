# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_ADD_NEW_TP_METADATA = Metadata(
    id="mri_add_new_tp",
    name="mri_add_new_tp",
    package="freesurfer",
)

MriAddNewTp = typing.TypedDict(
    "MriAddNewTp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_add_new_tp"]],
        "base_id": str,
        "newtp_id": str,
    },
)

@dataclasses.dataclass
class MriAddNewTpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_add_new_tp_params(
    base_id: str,
    newtp_id: str,
) -> MriAddNewTp:
    """
    Build parameters.

    Args:
        base_id: The ID of the base template.
        newtp_id: The ID of the new time point to be added.

    Returns:
        Parameter dictionary.
    """
    params: MriAddNewTp = {
        "@type": "freesurfer/mri_add_new_tp",
        "base_id": base_id,
        "newtp_id": newtp_id,
    }
    return params

def mri_add_new_tp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAddNewTp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base_id", None) is None:
        raise StyxValidationError("`base_id` must not be None")
    if not isinstance(params["base_id"], str):
        raise StyxValidationError(f'`base_id` has the wrong type: Received `{type(params["base_id"])}` expected `str`')
    if params.get("newtp_id", None) is None:
        raise StyxValidationError("`newtp_id` must not be None")
    if not isinstance(params["newtp_id"], str):
        raise StyxValidationError(f'`newtp_id` has the wrong type: Received `{type(params["newtp_id"])}` expected `str`')

def mri_add_new_tp_cargs(params: MriAddNewTp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_add_new_tp")
    cargs.append(params["base_id"])
    cargs.append(params["newtp_id"])
    return cargs

def mri_add_new_tp_outputs(params: MriAddNewTp, execution: Execution) -> MriAddNewTpOutputs:
    """Build the MriAddNewTpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriAddNewTpOutputs(
        root=root_v,
    )

def mri_add_new_tp_execute(params: MriAddNewTp, runner: Runner | None = None) -> MriAddNewTpOutputs:
    """
    mri_add_new_tp

    Adds a new time point to the base/template without re-creating the base. Only the new time point needs to be run longitudinally.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_add_new_tp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_ADD_NEW_TP_METADATA)
    execution.params(params)
    args = mri_add_new_tp_cargs(params, execution)
    out = mri_add_new_tp_outputs(params, execution)
    execution.run(args)
    return out

def mri_add_new_tp(
    base_id: str,
    newtp_id: str,
    runner: Runner | None = None,
) -> MriAddNewTpOutputs:
    """
    mri_add_new_tp

    Adds a new time point to the base/template without re-creating the base. Only the new time point needs to be run longitudinally.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        base_id: The ID of the base template.
        newtp_id: The ID of the new time point to be added.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_add_new_tp_params(
        base_id=base_id,
        newtp_id=newtp_id,
    )
    return mri_add_new_tp_execute(params, runner)

__all__ = [
    "MriAddNewTp",
    "MriAddNewTpOutputs",
    "MRI_ADD_NEW_TP_METADATA",
    "mri_add_new_tp_cargs",
    "mri_add_new_tp_outputs",
    "mri_add_new_tp_params",
    "mri_add_new_tp_execute",
    "mri_add_new_tp_validate",
    "mri_add_new_tp",
]