# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_AA_SHRINKWRAP_METADATA = Metadata(
    id="mris_AA_shrinkwrap",
    name="mris_AA_shrinkwrap",
    package="freesurfer",
)

MrisAaShrinkwrap = typing.TypedDict(
    "MrisAaShrinkwrap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_AA_shrinkwrap"]],
        "t1_vol": InputPathType,
        "pd_vol": InputPathType,
        "output_dir": str,
        "omit_self_intersection": typing.NotRequired[bool],
        "create_curvature_area": typing.NotRequired[bool],
        "average_curvature": typing.NotRequired[float],
        "white_only": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisAaShrinkwrapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_aa_shrinkwrap_params(
    t1_vol: InputPathType,
    pd_vol: InputPathType,
    output_dir: str,
    omit_self_intersection: bool = False,
    create_curvature_area: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
) -> MrisAaShrinkwrap:
    """
    Build parameters.

    Args:
        t1_vol: T1 volume file
        pd_vol: PD volume file
        output_dir: Output directory
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface
        create_curvature_area: Create curvature and area files from white matter\
            surface
        average_curvature: Average curvature values a specified number of times\
            (default=10)
        white_only: Only generate white matter surface

    Returns:
        Parameter dictionary.
    """
    params: MrisAaShrinkwrap = {
        "@type": "freesurfer/mris_AA_shrinkwrap",
        "t1_vol": t1_vol,
        "pd_vol": pd_vol,
        "output_dir": output_dir,
        "omit_self_intersection": omit_self_intersection,
        "create_curvature_area": create_curvature_area,
        "white_only": white_only,
    }
    if average_curvature is not None:
        params["average_curvature"] = average_curvature
    return params

def mris_aa_shrinkwrap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisAaShrinkwrap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("t1_vol", None) is None:
        raise StyxValidationError("`t1_vol` must not be None")
    if not isinstance(params["t1_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`t1_vol` has the wrong type: Received `{type(params["t1_vol"])}` expected `InputPathType`')
    if params.get("pd_vol", None) is None:
        raise StyxValidationError("`pd_vol` must not be None")
    if not isinstance(params["pd_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pd_vol` has the wrong type: Received `{type(params["pd_vol"])}` expected `InputPathType`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("omit_self_intersection", None) is not None:
        if not isinstance(params["omit_self_intersection"], bool):
            raise StyxValidationError(f'`omit_self_intersection` has the wrong type: Received `{type(params["omit_self_intersection"])}` expected `bool`')
    if params.get("create_curvature_area", None) is not None:
        if not isinstance(params["create_curvature_area"], bool):
            raise StyxValidationError(f'`create_curvature_area` has the wrong type: Received `{type(params["create_curvature_area"])}` expected `bool`')
    if params.get("average_curvature", None) is not None:
        if not isinstance(params["average_curvature"], (float, int)):
            raise StyxValidationError(f'`average_curvature` has the wrong type: Received `{type(params["average_curvature"])}` expected `float`')
    if params.get("white_only", None) is not None:
        if not isinstance(params["white_only"], bool):
            raise StyxValidationError(f'`white_only` has the wrong type: Received `{type(params["white_only"])}` expected `bool`')

def mris_aa_shrinkwrap_cargs(params: MrisAaShrinkwrap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_AA_shrinkwrap")
    cargs.append(execution.input_file(params["t1_vol"]))
    cargs.append(execution.input_file(params["pd_vol"]))
    cargs.append(params["output_dir"])
    if params.get("omit_self_intersection"):
        cargs.append("-q")
    if params.get("create_curvature_area"):
        cargs.append("-c")
    v_0 = params.get("average_curvature")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    if params.get("white_only"):
        cargs.append("-whiteonly")
    return cargs

def mris_aa_shrinkwrap_outputs(params: MrisAaShrinkwrap, execution: Execution) -> MrisAaShrinkwrapOutputs:
    """Build the MrisAaShrinkwrapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisAaShrinkwrapOutputs(
        root=root_v,
    )

def mris_aa_shrinkwrap_execute(params: MrisAaShrinkwrap, runner: Runner | None = None) -> MrisAaShrinkwrapOutputs:
    """
    mris_AA_shrinkwrap

    This program positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface and generates surface files for these surfaces as well as a 'curvature' file for the cortical thickness, and a surface file which approximates layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_aa_shrinkwrap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_AA_SHRINKWRAP_METADATA)
    execution.params(params)
    args = mris_aa_shrinkwrap_cargs(params, execution)
    out = mris_aa_shrinkwrap_outputs(params, execution)
    execution.run(args)
    return out

def mris_aa_shrinkwrap(
    t1_vol: InputPathType,
    pd_vol: InputPathType,
    output_dir: str,
    omit_self_intersection: bool = False,
    create_curvature_area: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
    runner: Runner | None = None,
) -> MrisAaShrinkwrapOutputs:
    """
    mris_AA_shrinkwrap

    This program positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface and generates surface files for these surfaces as well as a 'curvature' file for the cortical thickness, and a surface file which approximates layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        t1_vol: T1 volume file
        pd_vol: PD volume file
        output_dir: Output directory
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface
        create_curvature_area: Create curvature and area files from white matter\
            surface
        average_curvature: Average curvature values a specified number of times\
            (default=10)
        white_only: Only generate white matter surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_aa_shrinkwrap_params(
        t1_vol=t1_vol,
        pd_vol=pd_vol,
        output_dir=output_dir,
        omit_self_intersection=omit_self_intersection,
        create_curvature_area=create_curvature_area,
        average_curvature=average_curvature,
        white_only=white_only,
    )
    return mris_aa_shrinkwrap_execute(params, runner)

__all__ = [
    "MrisAaShrinkwrap",
    "MrisAaShrinkwrapOutputs",
    "MRIS_AA_SHRINKWRAP_METADATA",
    "mris_aa_shrinkwrap_cargs",
    "mris_aa_shrinkwrap_outputs",
    "mris_aa_shrinkwrap_params",
    "mris_aa_shrinkwrap_execute",
    "mris_aa_shrinkwrap_validate",
    "mris_aa_shrinkwrap",
]