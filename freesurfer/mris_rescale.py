# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_RESCALE_METADATA = Metadata(
    id="mris_rescale",
    name="mris_rescale",
    package="freesurfer",
)

MrisRescale = typing.TypedDict(
    "MrisRescale",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_rescale"]],
        "input_surface": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class MrisRescaleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    rescaled_output_surface: OutputPathType
    """The file containing the rescaled surface."""

def mris_rescale_params(
    input_surface: InputPathType,
    output_surface: str,
) -> MrisRescale:
    """
    Build parameters.

    Args:
        input_surface: The input surface file to be rescaled.
        output_surface: The output surface file after rescaling.

    Returns:
        Parameter dictionary.
    """
    params: MrisRescale = {
        "@type": "freesurfer/mris_rescale",
        "input_surface": input_surface,
        "output_surface": output_surface,
    }
    return params

def mris_rescale_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRescale."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def mris_rescale_cargs(params: MrisRescale, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_rescale")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_surface"])
    return cargs

def mris_rescale_outputs(params: MrisRescale, execution: Execution) -> MrisRescaleOutputs:
    """Build the MrisRescaleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    rescaled_output_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisRescaleOutputs(
        root=root_v,
        rescaled_output_surface=rescaled_output_surface_v,
    )

def mris_rescale_execute(params: MrisRescale, runner: Runner | None = None) -> MrisRescaleOutputs:
    """
    mris_rescale

    This program will rescale a surface representation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_rescale_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_RESCALE_METADATA)
    execution.params(params)
    args = mris_rescale_cargs(params, execution)
    out = mris_rescale_outputs(params, execution)
    execution.run(args)
    return out

def mris_rescale(
    input_surface: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> MrisRescaleOutputs:
    """
    mris_rescale

    This program will rescale a surface representation

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: The input surface file to be rescaled.
        output_surface: The output surface file after rescaling.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_rescale_params(
        input_surface=input_surface,
        output_surface=output_surface,
    )
    return mris_rescale_execute(params, runner)

__all__ = [
    "MrisRescale",
    "MrisRescaleOutputs",
    "MRIS_RESCALE_METADATA",
    "mris_rescale_cargs",
    "mris_rescale_outputs",
    "mris_rescale_params",
    "mris_rescale_execute",
    "mris_rescale_validate",
    "mris_rescale",
]