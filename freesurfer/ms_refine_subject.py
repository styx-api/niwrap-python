# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MS_REFINE_SUBJECT_METADATA = Metadata(
    id="ms_refine_subject",
    name="ms_refine_subject",
    package="freesurfer",
)

MsRefineSubject = typing.TypedDict(
    "MsRefineSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/ms_refine_subject"]],
        "subjects_dir": str,
    },
)

@dataclasses.dataclass
class MsRefineSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def ms_refine_subject_params(
    subjects_dir: str,
) -> MsRefineSubject:
    """
    Build parameters.

    Args:
        subjects_dir: Directory containing the subject data (e.g.\
            /usr/local/freesurfer/subjects)

    Returns:
        Parameter dictionary.
    """
    params: MsRefineSubject = {
        "@type": "freesurfer/ms_refine_subject",
        "subjects_dir": subjects_dir,
    }
    return params

def ms_refine_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MsRefineSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')

def ms_refine_subject_cargs(params: MsRefineSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ms_refine_subject")
    cargs.append(params["subjects_dir"])
    return cargs

def ms_refine_subject_outputs(params: MsRefineSubject, execution: Execution) -> MsRefineSubjectOutputs:
    """Build the MsRefineSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MsRefineSubjectOutputs(
        root=root_v,
    )

def ms_refine_subject_execute(params: MsRefineSubject, runner: Runner | None = None) -> MsRefineSubjectOutputs:
    """
    ms_refine_subject

    Unknown

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ms_refine_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MS_REFINE_SUBJECT_METADATA)
    execution.params(params)
    args = ms_refine_subject_cargs(params, execution)
    out = ms_refine_subject_outputs(params, execution)
    execution.run(args)
    return out

def ms_refine_subject(
    subjects_dir: str,
    runner: Runner | None = None,
) -> MsRefineSubjectOutputs:
    """
    ms_refine_subject

    Unknown

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects_dir: Directory containing the subject data (e.g.\
            /usr/local/freesurfer/subjects)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ms_refine_subject_params(
        subjects_dir=subjects_dir,
    )
    return ms_refine_subject_execute(params, runner)

__all__ = [
    "MsRefineSubject",
    "MsRefineSubjectOutputs",
    "MS_REFINE_SUBJECT_METADATA",
    "ms_refine_subject_cargs",
    "ms_refine_subject_outputs",
    "ms_refine_subject_params",
    "ms_refine_subject_execute",
    "ms_refine_subject_validate",
    "ms_refine_subject",
]