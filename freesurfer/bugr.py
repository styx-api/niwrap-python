# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BUGR_METADATA = Metadata(
    id="bugr",
    name="bugr",
    package="freesurfer",
)

Bugr = typing.TypedDict(
    "Bugr",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/bugr"]],
        "subject_name": str,
        "command_line": str,
        "error_message": str,
        "log_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class BugrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def bugr_params(
    subject_name: str,
    command_line: str,
    error_message: str,
    log_file: InputPathType | None = None,
) -> Bugr:
    """
    Build parameters.

    Args:
        subject_name: Subject name to include in the bug report
        command_line: The entire command-line executed
        error_message: The error message generated
        log_file: Log file path of the subject's recon-all process

    Returns:
        Parameter dictionary.
    """
    params: Bugr = {
        "@type": "freesurfer/bugr",
        "subject_name": subject_name,
        "command_line": command_line,
        "error_message": error_message,
    }
    if log_file is not None:
        params["log_file"] = log_file
    return params

def bugr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bugr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("command_line", None) is None:
        raise StyxValidationError("`command_line` must not be None")
    if not isinstance(params["command_line"], str):
        raise StyxValidationError(f'`command_line` has the wrong type: Received `{type(params["command_line"])}` expected `str`')
    if params.get("error_message", None) is None:
        raise StyxValidationError("`error_message` must not be None")
    if not isinstance(params["error_message"], str):
        raise StyxValidationError(f'`error_message` has the wrong type: Received `{type(params["error_message"])}` expected `str`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `InputPathType`')

def bugr_cargs(params: Bugr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bugr")
    cargs.append("-subject")
    cargs.append(params["subject_name"])
    cargs.append("-command")
    cargs.append(params["command_line"])
    cargs.append("-error")
    cargs.append(params["error_message"])
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("-log")
        cargs.append(execution.input_file(v_0))
    return cargs

def bugr_outputs(params: Bugr, execution: Execution) -> BugrOutputs:
    """Build the BugrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BugrOutputs(
        root=root_v,
    )

def bugr_execute(params: Bugr, runner: Runner | None = None) -> BugrOutputs:
    """
    bugr

    Utility for generating and reporting FreeSurfer bugs

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bugr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BUGR_METADATA)
    execution.params(params)
    args = bugr_cargs(params, execution)
    out = bugr_outputs(params, execution)
    execution.run(args)
    return out

def bugr(
    subject_name: str,
    command_line: str,
    error_message: str,
    log_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> BugrOutputs:
    """
    bugr

    Utility for generating and reporting FreeSurfer bugs

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Subject name to include in the bug report
        command_line: The entire command-line executed
        error_message: The error message generated
        log_file: Log file path of the subject's recon-all process
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bugr_params(
        subject_name=subject_name,
        command_line=command_line,
        error_message=error_message,
        log_file=log_file,
    )
    return bugr_execute(params, runner)

__all__ = [
    "Bugr",
    "BugrOutputs",
    "BUGR_METADATA",
    "bugr_cargs",
    "bugr_outputs",
    "bugr_params",
    "bugr_execute",
    "bugr_validate",
    "bugr",
]