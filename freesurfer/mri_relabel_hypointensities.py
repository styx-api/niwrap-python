# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RELABEL_HYPOINTENSITIES_METADATA = Metadata(
    id="mri_relabel_hypointensities",
    name="mri_relabel_hypointensities",
    package="freesurfer",
)

MriRelabelHypointensities = typing.TypedDict(
    "MriRelabelHypointensities",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_relabel_hypointensities"]],
        "input_aseg": InputPathType,
        "surface_directory": str,
        "output_aseg": str,
    },
)

@dataclasses.dataclass
class MriRelabelHypointensitiesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_aseg_file: OutputPathType
    """The relabeled output aseg file"""

def mri_relabel_hypointensities_params(
    input_aseg: InputPathType,
    surface_directory: str,
    output_aseg: str,
) -> MriRelabelHypointensities:
    """
    Build parameters.

    Args:
        input_aseg: Input aseg file
        surface_directory: Directory containing surfaces
        output_aseg: Output aseg file

    Returns:
        Parameter dictionary.
    """
    params: MriRelabelHypointensities = {
        "@type": "freesurfer/mri_relabel_hypointensities",
        "input_aseg": input_aseg,
        "surface_directory": surface_directory,
        "output_aseg": output_aseg,
    }
    return params

def mri_relabel_hypointensities_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRelabelHypointensities."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_aseg", None) is None:
        raise StyxValidationError("`input_aseg` must not be None")
    if not isinstance(params["input_aseg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_aseg` has the wrong type: Received `{type(params["input_aseg"])}` expected `InputPathType`')
    if params.get("surface_directory", None) is None:
        raise StyxValidationError("`surface_directory` must not be None")
    if not isinstance(params["surface_directory"], str):
        raise StyxValidationError(f'`surface_directory` has the wrong type: Received `{type(params["surface_directory"])}` expected `str`')
    if params.get("output_aseg", None) is None:
        raise StyxValidationError("`output_aseg` must not be None")
    if not isinstance(params["output_aseg"], str):
        raise StyxValidationError(f'`output_aseg` has the wrong type: Received `{type(params["output_aseg"])}` expected `str`')

def mri_relabel_hypointensities_cargs(params: MriRelabelHypointensities, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_relabel_hypointensities")
    cargs.append(execution.input_file(params["input_aseg"]))
    cargs.append(params["surface_directory"])
    cargs.append(params["output_aseg"])
    return cargs

def mri_relabel_hypointensities_outputs(params: MriRelabelHypointensities, execution: Execution) -> MriRelabelHypointensitiesOutputs:
    """Build the MriRelabelHypointensitiesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_aseg_file_v: OutputPathType = execution.output_file(params["output_aseg"])
    return MriRelabelHypointensitiesOutputs(
        root=root_v,
        output_aseg_file=output_aseg_file_v,
    )

def mri_relabel_hypointensities_execute(params: MriRelabelHypointensities, runner: Runner | None = None) -> MriRelabelHypointensitiesOutputs:
    """
    mri_relabel_hypointensities

    Tool for relabeling hypointensities in FreeSurfer's aseg files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_relabel_hypointensities_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RELABEL_HYPOINTENSITIES_METADATA)
    execution.params(params)
    args = mri_relabel_hypointensities_cargs(params, execution)
    out = mri_relabel_hypointensities_outputs(params, execution)
    execution.run(args)
    return out

def mri_relabel_hypointensities(
    input_aseg: InputPathType,
    surface_directory: str,
    output_aseg: str,
    runner: Runner | None = None,
) -> MriRelabelHypointensitiesOutputs:
    """
    mri_relabel_hypointensities

    Tool for relabeling hypointensities in FreeSurfer's aseg files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_aseg: Input aseg file
        surface_directory: Directory containing surfaces
        output_aseg: Output aseg file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_relabel_hypointensities_params(
        input_aseg=input_aseg,
        surface_directory=surface_directory,
        output_aseg=output_aseg,
    )
    return mri_relabel_hypointensities_execute(params, runner)

__all__ = [
    "MriRelabelHypointensities",
    "MriRelabelHypointensitiesOutputs",
    "MRI_RELABEL_HYPOINTENSITIES_METADATA",
    "mri_relabel_hypointensities_cargs",
    "mri_relabel_hypointensities_outputs",
    "mri_relabel_hypointensities_params",
    "mri_relabel_hypointensities_execute",
    "mri_relabel_hypointensities_validate",
    "mri_relabel_hypointensities",
]