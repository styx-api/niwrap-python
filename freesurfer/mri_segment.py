# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEGMENT_METADATA = Metadata(
    id="mri_segment",
    name="mri_segment",
    package="freesurfer",
)

MriSegment = typing.TypedDict(
    "MriSegment",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_segment"]],
        "in_vol": InputPathType,
        "out_vol": str,
        "no1d_remove": typing.NotRequired[float],
        "slope": typing.NotRequired[float],
        "pslope": typing.NotRequired[float],
        "nslope": typing.NotRequired[float],
        "debug_voxel": typing.NotRequired[list[float]],
        "auto": typing.NotRequired[bool],
        "noauto": typing.NotRequired[bool],
        "log": typing.NotRequired[bool],
        "keep": typing.NotRequired[bool],
        "gray_hi": typing.NotRequired[float],
        "wm_low": typing.NotRequired[float],
        "wm_low_factor": typing.NotRequired[float],
        "wm_hi": typing.NotRequired[float],
        "nseg": typing.NotRequired[float],
        "thicken": typing.NotRequired[bool],
        "fillbg": typing.NotRequired[bool],
        "fillv": typing.NotRequired[bool],
        "blur_sigma": typing.NotRequired[float],
        "iterations": typing.NotRequired[float],
        "thin_strand_limit": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
        "threshold": typing.NotRequired[float],
        "extract_options": typing.NotRequired[InputPathType],
        "wsize": typing.NotRequired[float],
        "wsizemm": typing.NotRequired[float],
        "polvw_size": typing.NotRequired[float],
        "polv_len": typing.NotRequired[float],
        "datfile": typing.NotRequired[InputPathType],
        "segmentation": typing.NotRequired[str],
        "diagno": typing.NotRequired[float],
        "diag_write": typing.NotRequired[bool],
        "diag_verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSegmentOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume: OutputPathType
    """Segmented white matter output volume"""
    log_file: OutputPathType
    """Log file from segmentation process"""

def mri_segment_params(
    in_vol: InputPathType,
    out_vol: str,
    no1d_remove: float | None = None,
    slope: float | None = None,
    pslope: float | None = None,
    nslope: float | None = None,
    debug_voxel: list[float] | None = None,
    auto: bool = False,
    noauto: bool = False,
    log: bool = False,
    keep: bool = False,
    gray_hi: float | None = None,
    wm_low: float | None = None,
    wm_low_factor: float | None = None,
    wm_hi: float | None = None,
    nseg: float | None = None,
    thicken: bool = False,
    fillbg: bool = False,
    fillv: bool = False,
    blur_sigma: float | None = None,
    iterations: float | None = None,
    thin_strand_limit: float | None = None,
    verbose: bool = False,
    threshold: float | None = None,
    extract_options: InputPathType | None = None,
    wsize: float | None = None,
    wsizemm: float | None = None,
    polvw_size: float | None = None,
    polv_len: float | None = None,
    datfile: InputPathType | None = None,
    segmentation: str | None = None,
    diagno: float | None = None,
    diag_write: bool = False,
    diag_verbose: bool = False,
) -> MriSegment:
    """
    Build parameters.

    Args:
        in_vol: Input volume to be segmented
        out_vol: Output volume after segmentation
        no1d_remove: Don't run code that removes 1D strands from segmentation
        slope: Set the curvature slope (both n and p)
        pslope: Set the curvature pslope (default=1.0)
        nslope: Set the curvature nslope (default=1.0)
        debug_voxel: Set voxel for debugging
        auto: Automatically detect class statistics (default)
        noauto: Don't automatically detect class statistics
        log: Log to ./segment.dat
        keep: Keep wm edits, maintains all values of 1 and 255
        gray_hi: Set the gray matter high limit (default=100.000)
        wm_low: Set the white matter low limit (default=80.000)
        wm_low_factor: wm_low = f*gray_mean + (1-f)*white_mean;
        wm_hi: Set the white matter high limit (default=125.000)
        nseg: Thicken the n largest thin strands (default=20)
        thicken: Toggle thickening step (default=ON)
        fillbg: Toggle filling of the basal ganglia (default=OFF)
        fillv: Toggle filling of the ventricles (default=OFF)
        blur_sigma: Set blur sigma (default=0.25)
        iterations: Set # iterations of border classification (default=1)
        thin_strand_limit: Set limit to thin strands in mm (default=4)
        verbose: Verbose
        threshold: Set % threshold (default=0.80)
        extract_options: Extract options from filename
        wsize: Set wsize (default=11 voxels)
        wsizemm: Set wsize based on mm instead of voxels
        polvw_size: Set wsize for plane of least variance (default=5)
        polv_len: Set length for plane of least variance (default=3)
        datfile: Set datfile (default is segment.dat)
        segmentation: Use segmentation to set thresholds
        diagno: Set diagnostic number
        diag_write: Set diagnostic write
        diag_verbose: Set diagnostic verbose

    Returns:
        Parameter dictionary.
    """
    params: MriSegment = {
        "@type": "freesurfer/mri_segment",
        "in_vol": in_vol,
        "out_vol": out_vol,
        "auto": auto,
        "noauto": noauto,
        "log": log,
        "keep": keep,
        "thicken": thicken,
        "fillbg": fillbg,
        "fillv": fillv,
        "verbose": verbose,
        "diag_write": diag_write,
        "diag_verbose": diag_verbose,
    }
    if no1d_remove is not None:
        params["no1d_remove"] = no1d_remove
    if slope is not None:
        params["slope"] = slope
    if pslope is not None:
        params["pslope"] = pslope
    if nslope is not None:
        params["nslope"] = nslope
    if debug_voxel is not None:
        params["debug_voxel"] = debug_voxel
    if gray_hi is not None:
        params["gray_hi"] = gray_hi
    if wm_low is not None:
        params["wm_low"] = wm_low
    if wm_low_factor is not None:
        params["wm_low_factor"] = wm_low_factor
    if wm_hi is not None:
        params["wm_hi"] = wm_hi
    if nseg is not None:
        params["nseg"] = nseg
    if blur_sigma is not None:
        params["blur_sigma"] = blur_sigma
    if iterations is not None:
        params["iterations"] = iterations
    if thin_strand_limit is not None:
        params["thin_strand_limit"] = thin_strand_limit
    if threshold is not None:
        params["threshold"] = threshold
    if extract_options is not None:
        params["extract_options"] = extract_options
    if wsize is not None:
        params["wsize"] = wsize
    if wsizemm is not None:
        params["wsizemm"] = wsizemm
    if polvw_size is not None:
        params["polvw_size"] = polvw_size
    if polv_len is not None:
        params["polv_len"] = polv_len
    if datfile is not None:
        params["datfile"] = datfile
    if segmentation is not None:
        params["segmentation"] = segmentation
    if diagno is not None:
        params["diagno"] = diagno
    return params

def mri_segment_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegment."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_vol", None) is None:
        raise StyxValidationError("`in_vol` must not be None")
    if not isinstance(params["in_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_vol` has the wrong type: Received `{type(params["in_vol"])}` expected `InputPathType`')
    if params.get("out_vol", None) is None:
        raise StyxValidationError("`out_vol` must not be None")
    if not isinstance(params["out_vol"], str):
        raise StyxValidationError(f'`out_vol` has the wrong type: Received `{type(params["out_vol"])}` expected `str`')
    if params.get("no1d_remove", None) is not None:
        if not isinstance(params["no1d_remove"], (float, int)):
            raise StyxValidationError(f'`no1d_remove` has the wrong type: Received `{type(params["no1d_remove"])}` expected `float`')
    if params.get("slope", None) is not None:
        if not isinstance(params["slope"], (float, int)):
            raise StyxValidationError(f'`slope` has the wrong type: Received `{type(params["slope"])}` expected `float`')
    if params.get("pslope", None) is not None:
        if not isinstance(params["pslope"], (float, int)):
            raise StyxValidationError(f'`pslope` has the wrong type: Received `{type(params["pslope"])}` expected `float`')
    if params.get("nslope", None) is not None:
        if not isinstance(params["nslope"], (float, int)):
            raise StyxValidationError(f'`nslope` has the wrong type: Received `{type(params["nslope"])}` expected `float`')
    if params.get("debug_voxel", None) is not None:
        if not isinstance(params["debug_voxel"], list):
            raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(params["debug_voxel"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel` must contain between 3 and 3 elements (inclusive)")
        for e in params["debug_voxel"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`debug_voxel` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("auto", None) is not None:
        if not isinstance(params["auto"], bool):
            raise StyxValidationError(f'`auto` has the wrong type: Received `{type(params["auto"])}` expected `bool`')
    if params.get("noauto", None) is not None:
        if not isinstance(params["noauto"], bool):
            raise StyxValidationError(f'`noauto` has the wrong type: Received `{type(params["noauto"])}` expected `bool`')
    if params.get("log", None) is not None:
        if not isinstance(params["log"], bool):
            raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `bool`')
    if params.get("keep", None) is not None:
        if not isinstance(params["keep"], bool):
            raise StyxValidationError(f'`keep` has the wrong type: Received `{type(params["keep"])}` expected `bool`')
    if params.get("gray_hi", None) is not None:
        if not isinstance(params["gray_hi"], (float, int)):
            raise StyxValidationError(f'`gray_hi` has the wrong type: Received `{type(params["gray_hi"])}` expected `float`')
    if params.get("wm_low", None) is not None:
        if not isinstance(params["wm_low"], (float, int)):
            raise StyxValidationError(f'`wm_low` has the wrong type: Received `{type(params["wm_low"])}` expected `float`')
    if params.get("wm_low_factor", None) is not None:
        if not isinstance(params["wm_low_factor"], (float, int)):
            raise StyxValidationError(f'`wm_low_factor` has the wrong type: Received `{type(params["wm_low_factor"])}` expected `float`')
    if params.get("wm_hi", None) is not None:
        if not isinstance(params["wm_hi"], (float, int)):
            raise StyxValidationError(f'`wm_hi` has the wrong type: Received `{type(params["wm_hi"])}` expected `float`')
    if params.get("nseg", None) is not None:
        if not isinstance(params["nseg"], (float, int)):
            raise StyxValidationError(f'`nseg` has the wrong type: Received `{type(params["nseg"])}` expected `float`')
    if params.get("thicken", None) is not None:
        if not isinstance(params["thicken"], bool):
            raise StyxValidationError(f'`thicken` has the wrong type: Received `{type(params["thicken"])}` expected `bool`')
    if params.get("fillbg", None) is not None:
        if not isinstance(params["fillbg"], bool):
            raise StyxValidationError(f'`fillbg` has the wrong type: Received `{type(params["fillbg"])}` expected `bool`')
    if params.get("fillv", None) is not None:
        if not isinstance(params["fillv"], bool):
            raise StyxValidationError(f'`fillv` has the wrong type: Received `{type(params["fillv"])}` expected `bool`')
    if params.get("blur_sigma", None) is not None:
        if not isinstance(params["blur_sigma"], (float, int)):
            raise StyxValidationError(f'`blur_sigma` has the wrong type: Received `{type(params["blur_sigma"])}` expected `float`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], (float, int)):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `float`')
    if params.get("thin_strand_limit", None) is not None:
        if not isinstance(params["thin_strand_limit"], (float, int)):
            raise StyxValidationError(f'`thin_strand_limit` has the wrong type: Received `{type(params["thin_strand_limit"])}` expected `float`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("extract_options", None) is not None:
        if not isinstance(params["extract_options"], (pathlib.Path, str)):
            raise StyxValidationError(f'`extract_options` has the wrong type: Received `{type(params["extract_options"])}` expected `InputPathType`')
    if params.get("wsize", None) is not None:
        if not isinstance(params["wsize"], (float, int)):
            raise StyxValidationError(f'`wsize` has the wrong type: Received `{type(params["wsize"])}` expected `float`')
    if params.get("wsizemm", None) is not None:
        if not isinstance(params["wsizemm"], (float, int)):
            raise StyxValidationError(f'`wsizemm` has the wrong type: Received `{type(params["wsizemm"])}` expected `float`')
    if params.get("polvw_size", None) is not None:
        if not isinstance(params["polvw_size"], (float, int)):
            raise StyxValidationError(f'`polvw_size` has the wrong type: Received `{type(params["polvw_size"])}` expected `float`')
    if params.get("polv_len", None) is not None:
        if not isinstance(params["polv_len"], (float, int)):
            raise StyxValidationError(f'`polv_len` has the wrong type: Received `{type(params["polv_len"])}` expected `float`')
    if params.get("datfile", None) is not None:
        if not isinstance(params["datfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`datfile` has the wrong type: Received `{type(params["datfile"])}` expected `InputPathType`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], str):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `str`')
    if params.get("diagno", None) is not None:
        if not isinstance(params["diagno"], (float, int)):
            raise StyxValidationError(f'`diagno` has the wrong type: Received `{type(params["diagno"])}` expected `float`')
    if params.get("diag_write", None) is not None:
        if not isinstance(params["diag_write"], bool):
            raise StyxValidationError(f'`diag_write` has the wrong type: Received `{type(params["diag_write"])}` expected `bool`')
    if params.get("diag_verbose", None) is not None:
        if not isinstance(params["diag_verbose"], bool):
            raise StyxValidationError(f'`diag_verbose` has the wrong type: Received `{type(params["diag_verbose"])}` expected `bool`')

def mri_segment_cargs(params: MriSegment, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_segment")
    cargs.append(execution.input_file(params["in_vol"]))
    cargs.append(params["out_vol"])
    v_0 = params.get("no1d_remove")
    if v_0 is not None:
        cargs.append("-no1d_remove")
        cargs.append(str(v_0))
    v_1 = params.get("slope")
    if v_1 is not None:
        cargs.append("-slope")
        cargs.append(str(v_1))
    v_2 = params.get("pslope")
    if v_2 is not None:
        cargs.append("-pslope")
        cargs.append(str(v_2))
    v_3 = params.get("nslope")
    if v_3 is not None:
        cargs.append("-nslope")
        cargs.append(str(v_3))
    v_4 = params.get("debug_voxel")
    if v_4 is not None:
        cargs.append("-debug_voxel")
        for item0 in v_4:
            cargs.append(str(item0))
    if params.get("auto"):
        cargs.append("-auto")
    if params.get("noauto"):
        cargs.append("-noauto")
    if params.get("log"):
        cargs.append("-log")
    if params.get("keep"):
        cargs.append("-keep")
    v_5 = params.get("gray_hi")
    if v_5 is not None:
        cargs.append("-ghi")
        cargs.append(str(v_5))
    v_6 = params.get("wm_low")
    if v_6 is not None:
        cargs.append("-wlo")
        cargs.append(str(v_6))
    v_7 = params.get("wm_low_factor")
    if v_7 is not None:
        cargs.append("-wm_low_factor")
        cargs.append(str(v_7))
    v_8 = params.get("wm_hi")
    if v_8 is not None:
        cargs.append("-whi")
        cargs.append(str(v_8))
    v_9 = params.get("nseg")
    if v_9 is not None:
        cargs.append("-nseg")
        cargs.append(str(v_9))
    if params.get("thicken"):
        cargs.append("-thicken")
    if params.get("fillbg"):
        cargs.append("-fillbg")
    if params.get("fillv"):
        cargs.append("-fillv")
    v_10 = params.get("blur_sigma")
    if v_10 is not None:
        cargs.append("-b")
        cargs.append(str(v_10))
    v_11 = params.get("iterations")
    if v_11 is not None:
        cargs.append("-n")
        cargs.append(str(v_11))
    v_12 = params.get("thin_strand_limit")
    if v_12 is not None:
        cargs.append("-t")
        cargs.append(str(v_12))
    if params.get("verbose"):
        cargs.append("-v")
    v_13 = params.get("threshold")
    if v_13 is not None:
        cargs.append("-p")
        cargs.append(str(v_13))
    v_14 = params.get("extract_options")
    if v_14 is not None:
        cargs.append("-x")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("wsize")
    if v_15 is not None:
        cargs.append("-w")
        cargs.append(str(v_15))
    v_16 = params.get("wsizemm")
    if v_16 is not None:
        cargs.append("-wsizemm")
        cargs.append(str(v_16))
    v_17 = params.get("polvw_size")
    if v_17 is not None:
        cargs.append("-polvwsize")
        cargs.append(str(v_17))
    v_18 = params.get("polv_len")
    if v_18 is not None:
        cargs.append("-polvlen")
        cargs.append(str(v_18))
    v_19 = params.get("datfile")
    if v_19 is not None:
        cargs.append("-dat")
        cargs.append(execution.input_file(v_19))
    v_20 = params.get("segmentation")
    if v_20 is not None:
        cargs.append("-seg")
        cargs.append(v_20)
    v_21 = params.get("diagno")
    if v_21 is not None:
        cargs.append("-diagno")
        cargs.append(str(v_21))
    if params.get("diag_write"):
        cargs.append("-diag-write")
    if params.get("diag_verbose"):
        cargs.append("-diag-verbose")
    return cargs

def mri_segment_outputs(params: MriSegment, execution: Execution) -> MriSegmentOutputs:
    """Build the MriSegmentOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_v: OutputPathType = execution.output_file(params["out_vol"])
    log_file_v: OutputPathType = execution.output_file("./segment.dat")
    return MriSegmentOutputs(
        root=root_v,
        output_volume=output_volume_v,
        log_file=log_file_v,
    )

def mri_segment_execute(params: MriSegment, runner: Runner | None = None) -> MriSegmentOutputs:
    """
    mri_segment

    Segments white matter from the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_segment_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEGMENT_METADATA)
    execution.params(params)
    args = mri_segment_cargs(params, execution)
    out = mri_segment_outputs(params, execution)
    execution.run(args)
    return out

def mri_segment(
    in_vol: InputPathType,
    out_vol: str,
    no1d_remove: float | None = None,
    slope: float | None = None,
    pslope: float | None = None,
    nslope: float | None = None,
    debug_voxel: list[float] | None = None,
    auto: bool = False,
    noauto: bool = False,
    log: bool = False,
    keep: bool = False,
    gray_hi: float | None = None,
    wm_low: float | None = None,
    wm_low_factor: float | None = None,
    wm_hi: float | None = None,
    nseg: float | None = None,
    thicken: bool = False,
    fillbg: bool = False,
    fillv: bool = False,
    blur_sigma: float | None = None,
    iterations: float | None = None,
    thin_strand_limit: float | None = None,
    verbose: bool = False,
    threshold: float | None = None,
    extract_options: InputPathType | None = None,
    wsize: float | None = None,
    wsizemm: float | None = None,
    polvw_size: float | None = None,
    polv_len: float | None = None,
    datfile: InputPathType | None = None,
    segmentation: str | None = None,
    diagno: float | None = None,
    diag_write: bool = False,
    diag_verbose: bool = False,
    runner: Runner | None = None,
) -> MriSegmentOutputs:
    """
    mri_segment

    Segments white matter from the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        in_vol: Input volume to be segmented
        out_vol: Output volume after segmentation
        no1d_remove: Don't run code that removes 1D strands from segmentation
        slope: Set the curvature slope (both n and p)
        pslope: Set the curvature pslope (default=1.0)
        nslope: Set the curvature nslope (default=1.0)
        debug_voxel: Set voxel for debugging
        auto: Automatically detect class statistics (default)
        noauto: Don't automatically detect class statistics
        log: Log to ./segment.dat
        keep: Keep wm edits, maintains all values of 1 and 255
        gray_hi: Set the gray matter high limit (default=100.000)
        wm_low: Set the white matter low limit (default=80.000)
        wm_low_factor: wm_low = f*gray_mean + (1-f)*white_mean;
        wm_hi: Set the white matter high limit (default=125.000)
        nseg: Thicken the n largest thin strands (default=20)
        thicken: Toggle thickening step (default=ON)
        fillbg: Toggle filling of the basal ganglia (default=OFF)
        fillv: Toggle filling of the ventricles (default=OFF)
        blur_sigma: Set blur sigma (default=0.25)
        iterations: Set # iterations of border classification (default=1)
        thin_strand_limit: Set limit to thin strands in mm (default=4)
        verbose: Verbose
        threshold: Set % threshold (default=0.80)
        extract_options: Extract options from filename
        wsize: Set wsize (default=11 voxels)
        wsizemm: Set wsize based on mm instead of voxels
        polvw_size: Set wsize for plane of least variance (default=5)
        polv_len: Set length for plane of least variance (default=3)
        datfile: Set datfile (default is segment.dat)
        segmentation: Use segmentation to set thresholds
        diagno: Set diagnostic number
        diag_write: Set diagnostic write
        diag_verbose: Set diagnostic verbose
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_segment_params(
        in_vol=in_vol,
        out_vol=out_vol,
        no1d_remove=no1d_remove,
        slope=slope,
        pslope=pslope,
        nslope=nslope,
        debug_voxel=debug_voxel,
        auto=auto,
        noauto=noauto,
        log=log,
        keep=keep,
        gray_hi=gray_hi,
        wm_low=wm_low,
        wm_low_factor=wm_low_factor,
        wm_hi=wm_hi,
        nseg=nseg,
        thicken=thicken,
        fillbg=fillbg,
        fillv=fillv,
        blur_sigma=blur_sigma,
        iterations=iterations,
        thin_strand_limit=thin_strand_limit,
        verbose=verbose,
        threshold=threshold,
        extract_options=extract_options,
        wsize=wsize,
        wsizemm=wsizemm,
        polvw_size=polvw_size,
        polv_len=polv_len,
        datfile=datfile,
        segmentation=segmentation,
        diagno=diagno,
        diag_write=diag_write,
        diag_verbose=diag_verbose,
    )
    return mri_segment_execute(params, runner)

__all__ = [
    "MriSegment",
    "MriSegmentOutputs",
    "MRI_SEGMENT_METADATA",
    "mri_segment_cargs",
    "mri_segment_outputs",
    "mri_segment_params",
    "mri_segment_execute",
    "mri_segment_validate",
    "mri_segment",
]