# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_TRANSFORM_METADATA = Metadata(
    id="mris_transform",
    name="mris_transform",
    package="freesurfer",
)

MrisTransform = typing.TypedDict(
    "MrisTransform",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_transform"]],
        "input_surface": InputPathType,
        "transform": InputPathType,
        "output_surface": str,
        "trx_src": typing.NotRequired[InputPathType],
        "trx_dst": typing.NotRequired[InputPathType],
        "is_inverse": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisTransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_output_surface: OutputPathType
    """Transformed output surface file."""

def mris_transform_params(
    input_surface: InputPathType,
    transform: InputPathType,
    output_surface: str,
    trx_src: InputPathType | None = None,
    trx_dst: InputPathType | None = None,
    is_inverse: bool = False,
) -> MrisTransform:
    """
    Build parameters.

    Args:
        input_surface: Input surface file, e.g., lh.pial.
        transform: Image-to-image transform file, e.g., LTA or M3Z.
        output_surface: Output surface file, e.g., lh.out.pial.
        trx_src: Specify the source geometry if the transform was created by\
            MNI/mritotal or FSL/flirt.
        trx_dst: Specify the destination geometry if the transform does not include\
            this information or the path in the M3Z is invalid.
        is_inverse: Use this option when using a transform from destination to\
            source space.

    Returns:
        Parameter dictionary.
    """
    params: MrisTransform = {
        "@type": "freesurfer/mris_transform",
        "input_surface": input_surface,
        "transform": transform,
        "output_surface": output_surface,
        "is_inverse": is_inverse,
    }
    if trx_src is not None:
        params["trx_src"] = trx_src
    if trx_dst is not None:
        params["trx_dst"] = trx_dst
    return params

def mris_transform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisTransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("trx_src", None) is not None:
        if not isinstance(params["trx_src"], (pathlib.Path, str)):
            raise StyxValidationError(f'`trx_src` has the wrong type: Received `{type(params["trx_src"])}` expected `InputPathType`')
    if params.get("trx_dst", None) is not None:
        if not isinstance(params["trx_dst"], (pathlib.Path, str)):
            raise StyxValidationError(f'`trx_dst` has the wrong type: Received `{type(params["trx_dst"])}` expected `InputPathType`')
    if params.get("is_inverse", None) is not None:
        if not isinstance(params["is_inverse"], bool):
            raise StyxValidationError(f'`is_inverse` has the wrong type: Received `{type(params["is_inverse"])}` expected `bool`')

def mris_transform_cargs(params: MrisTransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_transform")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["transform"]))
    cargs.append(params["output_surface"])
    v_0 = params.get("trx_src")
    if v_0 is not None:
        cargs.append("--trx-src")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("trx_dst")
    if v_1 is not None:
        cargs.append("--trx-dst")
        cargs.append(execution.input_file(v_1))
    if params.get("is_inverse"):
        cargs.append("--is-inverse")
    return cargs

def mris_transform_outputs(params: MrisTransform, execution: Execution) -> MrisTransformOutputs:
    """Build the MrisTransformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_output_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisTransformOutputs(
        root=root_v,
        transformed_output_surface=transformed_output_surface_v,
    )

def mris_transform_execute(params: MrisTransform, runner: Runner | None = None) -> MrisTransformOutputs:
    """
    mris_transform

    A tool to transform surfaces from one space to another using image transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_transform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_TRANSFORM_METADATA)
    execution.params(params)
    args = mris_transform_cargs(params, execution)
    out = mris_transform_outputs(params, execution)
    execution.run(args)
    return out

def mris_transform(
    input_surface: InputPathType,
    transform: InputPathType,
    output_surface: str,
    trx_src: InputPathType | None = None,
    trx_dst: InputPathType | None = None,
    is_inverse: bool = False,
    runner: Runner | None = None,
) -> MrisTransformOutputs:
    """
    mris_transform

    A tool to transform surfaces from one space to another using image transforms.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file, e.g., lh.pial.
        transform: Image-to-image transform file, e.g., LTA or M3Z.
        output_surface: Output surface file, e.g., lh.out.pial.
        trx_src: Specify the source geometry if the transform was created by\
            MNI/mritotal or FSL/flirt.
        trx_dst: Specify the destination geometry if the transform does not include\
            this information or the path in the M3Z is invalid.
        is_inverse: Use this option when using a transform from destination to\
            source space.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_transform_params(
        input_surface=input_surface,
        transform=transform,
        output_surface=output_surface,
        trx_src=trx_src,
        trx_dst=trx_dst,
        is_inverse=is_inverse,
    )
    return mris_transform_execute(params, runner)

__all__ = [
    "MrisTransform",
    "MrisTransformOutputs",
    "MRIS_TRANSFORM_METADATA",
    "mris_transform_cargs",
    "mris_transform_outputs",
    "mris_transform_params",
    "mris_transform_execute",
    "mris_transform_validate",
    "mris_transform",
]