# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MIDEFACE_METADATA = Metadata(
    id="mideface",
    name="mideface",
    package="freesurfer",
)

Mideface = typing.TypedDict(
    "Mideface",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mideface"]],
        "input_volume": InputPathType,
        "output_volume": str,
        "facemask": typing.NotRequired[InputPathType],
        "output_dir": typing.NotRequired[str],
        "exclusion_mask": typing.NotRequired[InputPathType],
        "samseg_ndilations": typing.NotRequired[float],
        "samseg_json": typing.NotRequired[str],
        "samseg_fast": typing.NotRequired[bool],
        "no_samseg_fast": typing.NotRequired[bool],
        "init_reg": typing.NotRequired[InputPathType],
        "synthseg_ndilations": typing.NotRequired[float],
        "fill_const": typing.NotRequired[list[float]],
        "fill_zero": typing.NotRequired[bool],
        "fhi": typing.NotRequired[float],
        "no_ears": typing.NotRequired[bool],
        "back_of_head": typing.NotRequired[bool],
        "forehead": typing.NotRequired[bool],
        "pics": typing.NotRequired[bool],
        "code": typing.NotRequired[str],
        "image_convert": typing.NotRequired[str],
        "no_post": typing.NotRequired[bool],
        "threads": typing.NotRequired[float],
        "force": typing.NotRequired[bool],
        "output_format": typing.NotRequired[str],
        "atlas": typing.NotRequired[str],
        "expert": typing.NotRequired[str],
        "display_no": typing.NotRequired[float],
        "apply_volume": typing.NotRequired[str],
        "check_volume": typing.NotRequired[InputPathType],
        "check_output_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MidefaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    defaced_output: OutputPathType
    """Defaced output volume"""
    facemask_output: OutputPathType | None
    """Applied facemask file"""

def mideface_params(
    input_volume: InputPathType,
    output_volume: str,
    facemask: InputPathType | None = None,
    output_dir: str | None = None,
    exclusion_mask: InputPathType | None = None,
    samseg_ndilations: float | None = None,
    samseg_json: str | None = None,
    samseg_fast: bool = False,
    no_samseg_fast: bool = False,
    init_reg: InputPathType | None = None,
    synthseg_ndilations: float | None = None,
    fill_const: list[float] | None = None,
    fill_zero: bool = False,
    fhi: float | None = None,
    no_ears: bool = False,
    back_of_head: bool = False,
    forehead: bool = False,
    pics: bool = False,
    code: str | None = None,
    image_convert: str | None = None,
    no_post: bool = False,
    threads: float | None = None,
    force: bool = False,
    output_format: str | None = None,
    atlas: str | None = None,
    expert: str | None = None,
    display_no: float | None = None,
    apply_volume: str | None = None,
    check_volume: InputPathType | None = None,
    check_output_file: InputPathType | None = None,
) -> Mideface:
    """
    Build parameters.

    Args:
        input_volume: Volume to deface
        output_volume: Defaced output volume
        facemask: Facemask to apply
        output_dir: Directory for outputs, activates PostHeadSurf
        exclusion_mask: Mask to exclude certain regions from defacing
        samseg_ndilations: Number of dilations for Samseg segmentation
        samseg_json: JSON configuration for Samseg
        samseg_fast: Configure Samseg to run quickly
        no_samseg_fast: Do not configure Samseg to run quickly
        init_reg: Initial registration file for Samseg
        synthseg_ndilations: Number of dilations for Synthseg segmentation
        fill_const: Constants for filling regions
        fill_zero: Fill regions with zero
        fhi: FHI value for MRIchangeType()
        no_ears: Do not include ears in the defacing
        back_of_head: Include back of head in defacing
        forehead: Include forehead in defacing (risks removing brain)
        pics: Take pictures of the defaced result
        code: Embed code name in pictures
        image_convert: Path to ImageMagick convert binary for pictures
        no_post: Do not make a head surface after defacing
        threads: Number of threads to use
        force: Force reprocessing (only applicable if output directory is used)
        output_format: Output file format
        atlas: Directory containing atlas files
        expert: Additional expert options
        display_no: Xvfb display number for taking pictures
        apply_volume: Apply midface output to a second volume
        check_volume: Volume to check if defaced
        check_output_file: Optional output file for check result

    Returns:
        Parameter dictionary.
    """
    params: Mideface = {
        "@type": "freesurfer/mideface",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "samseg_fast": samseg_fast,
        "no_samseg_fast": no_samseg_fast,
        "fill_zero": fill_zero,
        "no_ears": no_ears,
        "back_of_head": back_of_head,
        "forehead": forehead,
        "pics": pics,
        "no_post": no_post,
        "force": force,
    }
    if facemask is not None:
        params["facemask"] = facemask
    if output_dir is not None:
        params["output_dir"] = output_dir
    if exclusion_mask is not None:
        params["exclusion_mask"] = exclusion_mask
    if samseg_ndilations is not None:
        params["samseg_ndilations"] = samseg_ndilations
    if samseg_json is not None:
        params["samseg_json"] = samseg_json
    if init_reg is not None:
        params["init_reg"] = init_reg
    if synthseg_ndilations is not None:
        params["synthseg_ndilations"] = synthseg_ndilations
    if fill_const is not None:
        params["fill_const"] = fill_const
    if fhi is not None:
        params["fhi"] = fhi
    if code is not None:
        params["code"] = code
    if image_convert is not None:
        params["image_convert"] = image_convert
    if threads is not None:
        params["threads"] = threads
    if output_format is not None:
        params["output_format"] = output_format
    if atlas is not None:
        params["atlas"] = atlas
    if expert is not None:
        params["expert"] = expert
    if display_no is not None:
        params["display_no"] = display_no
    if apply_volume is not None:
        params["apply_volume"] = apply_volume
    if check_volume is not None:
        params["check_volume"] = check_volume
    if check_output_file is not None:
        params["check_output_file"] = check_output_file
    return params

def mideface_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mideface."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("facemask", None) is not None:
        if not isinstance(params["facemask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`facemask` has the wrong type: Received `{type(params["facemask"])}` expected `InputPathType`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("exclusion_mask", None) is not None:
        if not isinstance(params["exclusion_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`exclusion_mask` has the wrong type: Received `{type(params["exclusion_mask"])}` expected `InputPathType`')
    if params.get("samseg_ndilations", None) is not None:
        if not isinstance(params["samseg_ndilations"], (float, int)):
            raise StyxValidationError(f'`samseg_ndilations` has the wrong type: Received `{type(params["samseg_ndilations"])}` expected `float`')
    if params.get("samseg_json", None) is not None:
        if not isinstance(params["samseg_json"], str):
            raise StyxValidationError(f'`samseg_json` has the wrong type: Received `{type(params["samseg_json"])}` expected `str`')
    if params.get("samseg_fast", None) is not None:
        if not isinstance(params["samseg_fast"], bool):
            raise StyxValidationError(f'`samseg_fast` has the wrong type: Received `{type(params["samseg_fast"])}` expected `bool`')
    if params.get("no_samseg_fast", None) is not None:
        if not isinstance(params["no_samseg_fast"], bool):
            raise StyxValidationError(f'`no_samseg_fast` has the wrong type: Received `{type(params["no_samseg_fast"])}` expected `bool`')
    if params.get("init_reg", None) is not None:
        if not isinstance(params["init_reg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_reg` has the wrong type: Received `{type(params["init_reg"])}` expected `InputPathType`')
    if params.get("synthseg_ndilations", None) is not None:
        if not isinstance(params["synthseg_ndilations"], (float, int)):
            raise StyxValidationError(f'`synthseg_ndilations` has the wrong type: Received `{type(params["synthseg_ndilations"])}` expected `float`')
    if params.get("fill_const", None) is not None:
        if not isinstance(params["fill_const"], list):
            raise StyxValidationError(f'`fill_const` has the wrong type: Received `{type(params["fill_const"])}` expected `list[float]`')
        if not (2 <= len(params["fill_const"]) <= 2):
            raise StyxValidationError("Parameter `fill_const` must contain between 2 and 2 elements (inclusive)")
        for e in params["fill_const"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`fill_const` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("fill_zero", None) is not None:
        if not isinstance(params["fill_zero"], bool):
            raise StyxValidationError(f'`fill_zero` has the wrong type: Received `{type(params["fill_zero"])}` expected `bool`')
    if params.get("fhi", None) is not None:
        if not isinstance(params["fhi"], (float, int)):
            raise StyxValidationError(f'`fhi` has the wrong type: Received `{type(params["fhi"])}` expected `float`')
    if params.get("no_ears", None) is not None:
        if not isinstance(params["no_ears"], bool):
            raise StyxValidationError(f'`no_ears` has the wrong type: Received `{type(params["no_ears"])}` expected `bool`')
    if params.get("back_of_head", None) is not None:
        if not isinstance(params["back_of_head"], bool):
            raise StyxValidationError(f'`back_of_head` has the wrong type: Received `{type(params["back_of_head"])}` expected `bool`')
    if params.get("forehead", None) is not None:
        if not isinstance(params["forehead"], bool):
            raise StyxValidationError(f'`forehead` has the wrong type: Received `{type(params["forehead"])}` expected `bool`')
    if params.get("pics", None) is not None:
        if not isinstance(params["pics"], bool):
            raise StyxValidationError(f'`pics` has the wrong type: Received `{type(params["pics"])}` expected `bool`')
    if params.get("code", None) is not None:
        if not isinstance(params["code"], str):
            raise StyxValidationError(f'`code` has the wrong type: Received `{type(params["code"])}` expected `str`')
    if params.get("image_convert", None) is not None:
        if not isinstance(params["image_convert"], str):
            raise StyxValidationError(f'`image_convert` has the wrong type: Received `{type(params["image_convert"])}` expected `str`')
    if params.get("no_post", None) is not None:
        if not isinstance(params["no_post"], bool):
            raise StyxValidationError(f'`no_post` has the wrong type: Received `{type(params["no_post"])}` expected `bool`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("output_format", None) is not None:
        if not isinstance(params["output_format"], str):
            raise StyxValidationError(f'`output_format` has the wrong type: Received `{type(params["output_format"])}` expected `str`')
    if params.get("atlas", None) is not None:
        if not isinstance(params["atlas"], str):
            raise StyxValidationError(f'`atlas` has the wrong type: Received `{type(params["atlas"])}` expected `str`')
    if params.get("expert", None) is not None:
        if not isinstance(params["expert"], str):
            raise StyxValidationError(f'`expert` has the wrong type: Received `{type(params["expert"])}` expected `str`')
    if params.get("display_no", None) is not None:
        if not isinstance(params["display_no"], (float, int)):
            raise StyxValidationError(f'`display_no` has the wrong type: Received `{type(params["display_no"])}` expected `float`')
    if params.get("apply_volume", None) is not None:
        if not isinstance(params["apply_volume"], str):
            raise StyxValidationError(f'`apply_volume` has the wrong type: Received `{type(params["apply_volume"])}` expected `str`')
    if params.get("check_volume", None) is not None:
        if not isinstance(params["check_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`check_volume` has the wrong type: Received `{type(params["check_volume"])}` expected `InputPathType`')
    if params.get("check_output_file", None) is not None:
        if not isinstance(params["check_output_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`check_output_file` has the wrong type: Received `{type(params["check_output_file"])}` expected `InputPathType`')

def mideface_cargs(params: Mideface, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mideface")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--o")
    cargs.append(params["output_volume"])
    v_0 = params.get("facemask")
    if v_0 is not None:
        cargs.append("--facemask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("output_dir")
    if v_1 is not None:
        cargs.append("--odir")
        cargs.append(v_1)
    v_2 = params.get("exclusion_mask")
    if v_2 is not None:
        cargs.append("--xmask")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("samseg_ndilations")
    if v_3 is not None:
        cargs.append("--xmask-samseg")
        cargs.append(str(v_3))
    v_4 = params.get("samseg_json")
    if v_4 is not None:
        cargs.append("--samseg-json")
        cargs.append(v_4)
    if params.get("samseg_fast"):
        cargs.append("--samseg-fast")
    if params.get("no_samseg_fast"):
        cargs.append("--no-samseg-fast")
    v_5 = params.get("init_reg")
    if v_5 is not None:
        cargs.append("--init-reg")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("synthseg_ndilations")
    if v_6 is not None:
        cargs.append("--xmask-synthseg")
        cargs.append(str(v_6))
    v_7 = params.get("fill_const")
    if v_7 is not None:
        cargs.append("--fill-const")
        for item0 in v_7:
            cargs.append(str(item0))
    if params.get("fill_zero"):
        cargs.append("--fill-zero")
    v_8 = params.get("fhi")
    if v_8 is not None:
        cargs.append("--fhi")
        cargs.append(str(v_8))
    if params.get("no_ears"):
        cargs.append("--no-ears")
    if params.get("back_of_head"):
        cargs.append("--back-of-head")
    if params.get("forehead"):
        cargs.append("--forehead")
    if params.get("pics"):
        cargs.append("--pics")
    v_9 = params.get("code")
    if v_9 is not None:
        cargs.append("--code")
        cargs.append(v_9)
    v_10 = params.get("image_convert")
    if v_10 is not None:
        cargs.append("--imconvert")
        cargs.append(v_10)
    if params.get("no_post"):
        cargs.append("--no-post")
    v_11 = params.get("threads")
    if v_11 is not None:
        cargs.append("--threads")
        cargs.append(str(v_11))
    if params.get("force"):
        cargs.append("--force")
    v_12 = params.get("output_format")
    if v_12 is not None:
        cargs.append("--nii --nii.gz --mgz")
        cargs.append(v_12)
    v_13 = params.get("atlas")
    if v_13 is not None:
        cargs.append("--atlas")
        cargs.append(v_13)
    v_14 = params.get("expert")
    if v_14 is not None:
        cargs.append("--expert")
        cargs.append(v_14)
    v_15 = params.get("display_no")
    if v_15 is not None:
        cargs.append("--display")
        cargs.append(str(v_15))
    v_16 = params.get("apply_volume")
    if v_16 is not None:
        cargs.append("--apply")
        cargs.append(v_16)
    v_17 = params.get("check_volume")
    if v_17 is not None:
        cargs.append("--check")
        cargs.append(execution.input_file(v_17))
    v_18 = params.get("check_output_file")
    if v_18 is not None:
        cargs.append("--check")
        cargs.append(execution.input_file(v_18))
    return cargs

def mideface_outputs(params: Mideface, execution: Execution) -> MidefaceOutputs:
    """Build the MidefaceOutputs object for this tool."""
    facemask_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    defaced_output_v: OutputPathType = execution.output_file(params["output_volume"])
    __v0 = params.get("facemask")
    if __v0 is not None:
        facemask_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MidefaceOutputs(
        root=root_v,
        defaced_output=defaced_output_v,
        facemask_output=facemask_output_v,
    )

def mideface_execute(params: Mideface, runner: Runner | None = None) -> MidefaceOutputs:
    """
    mideface

    Minimally invasive defacing tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mideface_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MIDEFACE_METADATA)
    execution.params(params)
    args = mideface_cargs(params, execution)
    out = mideface_outputs(params, execution)
    execution.run(args)
    return out

def mideface(
    input_volume: InputPathType,
    output_volume: str,
    facemask: InputPathType | None = None,
    output_dir: str | None = None,
    exclusion_mask: InputPathType | None = None,
    samseg_ndilations: float | None = None,
    samseg_json: str | None = None,
    samseg_fast: bool = False,
    no_samseg_fast: bool = False,
    init_reg: InputPathType | None = None,
    synthseg_ndilations: float | None = None,
    fill_const: list[float] | None = None,
    fill_zero: bool = False,
    fhi: float | None = None,
    no_ears: bool = False,
    back_of_head: bool = False,
    forehead: bool = False,
    pics: bool = False,
    code: str | None = None,
    image_convert: str | None = None,
    no_post: bool = False,
    threads: float | None = None,
    force: bool = False,
    output_format: str | None = None,
    atlas: str | None = None,
    expert: str | None = None,
    display_no: float | None = None,
    apply_volume: str | None = None,
    check_volume: InputPathType | None = None,
    check_output_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> MidefaceOutputs:
    """
    mideface

    Minimally invasive defacing tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Volume to deface
        output_volume: Defaced output volume
        facemask: Facemask to apply
        output_dir: Directory for outputs, activates PostHeadSurf
        exclusion_mask: Mask to exclude certain regions from defacing
        samseg_ndilations: Number of dilations for Samseg segmentation
        samseg_json: JSON configuration for Samseg
        samseg_fast: Configure Samseg to run quickly
        no_samseg_fast: Do not configure Samseg to run quickly
        init_reg: Initial registration file for Samseg
        synthseg_ndilations: Number of dilations for Synthseg segmentation
        fill_const: Constants for filling regions
        fill_zero: Fill regions with zero
        fhi: FHI value for MRIchangeType()
        no_ears: Do not include ears in the defacing
        back_of_head: Include back of head in defacing
        forehead: Include forehead in defacing (risks removing brain)
        pics: Take pictures of the defaced result
        code: Embed code name in pictures
        image_convert: Path to ImageMagick convert binary for pictures
        no_post: Do not make a head surface after defacing
        threads: Number of threads to use
        force: Force reprocessing (only applicable if output directory is used)
        output_format: Output file format
        atlas: Directory containing atlas files
        expert: Additional expert options
        display_no: Xvfb display number for taking pictures
        apply_volume: Apply midface output to a second volume
        check_volume: Volume to check if defaced
        check_output_file: Optional output file for check result
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mideface_params(
        input_volume=input_volume,
        output_volume=output_volume,
        facemask=facemask,
        output_dir=output_dir,
        exclusion_mask=exclusion_mask,
        samseg_ndilations=samseg_ndilations,
        samseg_json=samseg_json,
        samseg_fast=samseg_fast,
        no_samseg_fast=no_samseg_fast,
        init_reg=init_reg,
        synthseg_ndilations=synthseg_ndilations,
        fill_const=fill_const,
        fill_zero=fill_zero,
        fhi=fhi,
        no_ears=no_ears,
        back_of_head=back_of_head,
        forehead=forehead,
        pics=pics,
        code=code,
        image_convert=image_convert,
        no_post=no_post,
        threads=threads,
        force=force,
        output_format=output_format,
        atlas=atlas,
        expert=expert,
        display_no=display_no,
        apply_volume=apply_volume,
        check_volume=check_volume,
        check_output_file=check_output_file,
    )
    return mideface_execute(params, runner)

__all__ = [
    "Mideface",
    "MidefaceOutputs",
    "MIDEFACE_METADATA",
    "mideface_cargs",
    "mideface_outputs",
    "mideface_params",
    "mideface_execute",
    "mideface_validate",
    "mideface",
]