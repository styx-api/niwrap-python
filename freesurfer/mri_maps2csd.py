# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MAPS2CSD_METADATA = Metadata(
    id="mri_maps2csd",
    name="mri_maps2csd",
    package="freesurfer",
)

MriMaps2csd = typing.TypedDict(
    "MriMaps2csd",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_maps2csd"]],
        "input_files": list[str],
        "csd_file": typing.NotRequired[str],
        "pdf_file": typing.NotRequired[str],
        "subject_hemi_surf": typing.NotRequired[str],
        "thresh": typing.NotRequired[float],
        "sign": typing.NotRequired[str],
        "csd_apply_file": typing.NotRequired[str],
        "apply_out": typing.NotRequired[str],
        "subjects_dir": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriMaps2csdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_maps2csd_params(
    input_files: list[str],
    csd_file: str | None = None,
    pdf_file: str | None = None,
    subject_hemi_surf: str | None = None,
    thresh: float | None = None,
    sign: str | None = None,
    csd_apply_file: str | None = None,
    apply_out: str | None = None,
    subjects_dir: str | None = None,
    debug: bool = False,
    checkopts: bool = False,
) -> MriMaps2csd:
    """
    Build parameters.

    Args:
        input_files: Input file(s) or specify them directly on the command line
        csd_file: Output CSD file
        pdf_file: PDF file created from CSD
        subject_hemi_surf: Subject name, hemisphere, and surface specification
        thresh: Threshold for cluster-forming (-log10 scale)
        sign: Sign adjustment for threshold (+1, -1 or 0)
        csd_apply_file: Apply a CSD file to inputs, return p-value of max cluster
        apply_out:
        subjects_dir: Subjects directory
        debug: Turn on debugging
        checkopts: Don't run, just check options then exit

    Returns:
        Parameter dictionary.
    """
    params: MriMaps2csd = {
        "@type": "freesurfer/mri_maps2csd",
        "input_files": input_files,
        "debug": debug,
        "checkopts": checkopts,
    }
    if csd_file is not None:
        params["csd_file"] = csd_file
    if pdf_file is not None:
        params["pdf_file"] = pdf_file
    if subject_hemi_surf is not None:
        params["subject_hemi_surf"] = subject_hemi_surf
    if thresh is not None:
        params["thresh"] = thresh
    if sign is not None:
        params["sign"] = sign
    if csd_apply_file is not None:
        params["csd_apply_file"] = csd_apply_file
    if apply_out is not None:
        params["apply_out"] = apply_out
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    return params

def mri_maps2csd_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMaps2csd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[str]`')
    for e in params["input_files"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("csd_file", None) is not None:
        if not isinstance(params["csd_file"], str):
            raise StyxValidationError(f'`csd_file` has the wrong type: Received `{type(params["csd_file"])}` expected `str`')
    if params.get("pdf_file", None) is not None:
        if not isinstance(params["pdf_file"], str):
            raise StyxValidationError(f'`pdf_file` has the wrong type: Received `{type(params["pdf_file"])}` expected `str`')
    if params.get("subject_hemi_surf", None) is not None:
        if not isinstance(params["subject_hemi_surf"], str):
            raise StyxValidationError(f'`subject_hemi_surf` has the wrong type: Received `{type(params["subject_hemi_surf"])}` expected `str`')
    if params.get("thresh", None) is not None:
        if not isinstance(params["thresh"], (float, int)):
            raise StyxValidationError(f'`thresh` has the wrong type: Received `{type(params["thresh"])}` expected `float`')
    if params.get("sign", None) is not None:
        if not isinstance(params["sign"], str):
            raise StyxValidationError(f'`sign` has the wrong type: Received `{type(params["sign"])}` expected `str`')
    if params.get("csd_apply_file", None) is not None:
        if not isinstance(params["csd_apply_file"], str):
            raise StyxValidationError(f'`csd_apply_file` has the wrong type: Received `{type(params["csd_apply_file"])}` expected `str`')
    if params.get("apply_out", None) is not None:
        if not isinstance(params["apply_out"], str):
            raise StyxValidationError(f'`apply_out` has the wrong type: Received `{type(params["apply_out"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')

def mri_maps2csd_cargs(params: MriMaps2csd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_maps2csd")
    cargs.append("--i")
    for item0 in params["input_files"]:
        cargs.append(item0)
    v_0 = params.get("csd_file")
    if v_0 is not None:
        cargs.append("--csd")
        cargs.append(v_0)
    v_1 = params.get("pdf_file")
    if v_1 is not None:
        cargs.append("--pdf")
        cargs.append(v_1)
    v_2 = params.get("subject_hemi_surf")
    if v_2 is not None:
        cargs.append("--s")
        cargs.append(v_2)
    v_3 = params.get("thresh")
    if v_3 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_3))
    v_4 = params.get("sign")
    if v_4 is not None:
        cargs.append("--sign")
        cargs.append(v_4)
    v_5 = params.get("csd_apply_file")
    if v_5 is not None:
        cargs.append("--csd-apply")
        cargs.append(v_5)
    v_6 = params.get("apply_out")
    if v_6 is not None:
        cargs.append(v_6)
    v_7 = params.get("subjects_dir")
    if v_7 is not None:
        cargs.append("--sd")
        cargs.append(v_7)
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    return cargs

def mri_maps2csd_outputs(params: MriMaps2csd, execution: Execution) -> MriMaps2csdOutputs:
    """Build the MriMaps2csdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriMaps2csdOutputs(
        root=root_v,
    )

def mri_maps2csd_execute(params: MriMaps2csd, runner: Runner | None = None) -> MriMaps2csdOutputs:
    """
    mri_maps2csd

    Tool to create CSD files and PDFs from input maps and apply them.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_maps2csd_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MAPS2CSD_METADATA)
    execution.params(params)
    args = mri_maps2csd_cargs(params, execution)
    out = mri_maps2csd_outputs(params, execution)
    execution.run(args)
    return out

def mri_maps2csd(
    input_files: list[str],
    csd_file: str | None = None,
    pdf_file: str | None = None,
    subject_hemi_surf: str | None = None,
    thresh: float | None = None,
    sign: str | None = None,
    csd_apply_file: str | None = None,
    apply_out: str | None = None,
    subjects_dir: str | None = None,
    debug: bool = False,
    checkopts: bool = False,
    runner: Runner | None = None,
) -> MriMaps2csdOutputs:
    """
    mri_maps2csd

    Tool to create CSD files and PDFs from input maps and apply them.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input file(s) or specify them directly on the command line
        csd_file: Output CSD file
        pdf_file: PDF file created from CSD
        subject_hemi_surf: Subject name, hemisphere, and surface specification
        thresh: Threshold for cluster-forming (-log10 scale)
        sign: Sign adjustment for threshold (+1, -1 or 0)
        csd_apply_file: Apply a CSD file to inputs, return p-value of max cluster
        apply_out:
        subjects_dir: Subjects directory
        debug: Turn on debugging
        checkopts: Don't run, just check options then exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_maps2csd_params(
        input_files=input_files,
        csd_file=csd_file,
        pdf_file=pdf_file,
        subject_hemi_surf=subject_hemi_surf,
        thresh=thresh,
        sign=sign,
        csd_apply_file=csd_apply_file,
        apply_out=apply_out,
        subjects_dir=subjects_dir,
        debug=debug,
        checkopts=checkopts,
    )
    return mri_maps2csd_execute(params, runner)

__all__ = [
    "MriMaps2csd",
    "MriMaps2csdOutputs",
    "MRI_MAPS2CSD_METADATA",
    "mri_maps2csd_cargs",
    "mri_maps2csd_outputs",
    "mri_maps2csd_params",
    "mri_maps2csd_execute",
    "mri_maps2csd_validate",
    "mri_maps2csd",
]