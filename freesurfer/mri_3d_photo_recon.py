# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_3D_PHOTO_RECON_METADATA = Metadata(
    id="mri_3d_photo_recon",
    name="mri_3d_photo_recon",
    package="freesurfer",
)

Mri3dPhotoRecon = typing.TypedDict(
    "Mri3dPhotoRecon",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_3d_photo_recon"]],
        "input_photo_dir": list[InputPathType],
        "input_segmentation_dir": list[InputPathType],
        "slice_thickness": float,
        "photo_resolution": float,
        "output_directory": str,
        "ref_mask": typing.NotRequired[InputPathType],
        "ref_surface": typing.NotRequired[InputPathType],
        "ref_soft_mask": typing.NotRequired[InputPathType],
        "mesh_reorient_with_indices": typing.NotRequired[str],
        "photos_posterior_side": typing.NotRequired[bool],
        "order_posterior_to_anterior": typing.NotRequired[bool],
        "allow_z_stretch": typing.NotRequired[bool],
        "rigid_only_for_photos": typing.NotRequired[bool],
        "gpu_index": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class Mri3dPhotoReconOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reconstructed_volume: OutputPathType
    """Reconstructed photo volume output file"""
    reference_data: OutputPathType
    """Output reference data"""

def mri_3d_photo_recon_params(
    input_photo_dir: list[InputPathType],
    input_segmentation_dir: list[InputPathType],
    slice_thickness: float,
    photo_resolution: float,
    output_directory: str,
    ref_mask: InputPathType | None = None,
    ref_surface: InputPathType | None = None,
    ref_soft_mask: InputPathType | None = None,
    mesh_reorient_with_indices: str | None = None,
    photos_posterior_side: bool = False,
    order_posterior_to_anterior: bool = False,
    allow_z_stretch: bool = False,
    rigid_only_for_photos: bool = False,
    gpu_index: float | None = None,
) -> Mri3dPhotoRecon:
    """
    Build parameters.

    Args:
        input_photo_dir: Directory with input photos (required)
        input_segmentation_dir: Directory with input slab masks / segmentations\
            (required)
        slice_thickness: Slice thickness in mm
        photo_resolution: Resolution of the photos in mm
        output_directory: Output directory with reconstructed photo volume and\
            reference
        ref_mask: Reference binary mask
        ref_surface: Reference surface file
        ref_soft_mask: Reference soft mask
        mesh_reorient_with_indices: Vertex indices of frontal pole, occipital pole,\
            and top of central sulcus, separated with commas, for mesh alignment
        photos_posterior_side: Use when photos are taken of posterior side of slabs\
            (default is anterior side)
        order_posterior_to_anterior: Use when photos are ordered from posterior to\
            anterior (default is anterior to posterior)
        allow_z_stretch: Use to adjust the slice thickness to best match the\
            reference. You should probably *never* use this with soft references\
            (ref_soft_mask)
        rigid_only_for_photos: Switch on if you want photos to deform only rigidly\
            (not affine)
        gpu_index: Index of GPU to use

    Returns:
        Parameter dictionary.
    """
    params: Mri3dPhotoRecon = {
        "@type": "freesurfer/mri_3d_photo_recon",
        "input_photo_dir": input_photo_dir,
        "input_segmentation_dir": input_segmentation_dir,
        "slice_thickness": slice_thickness,
        "photo_resolution": photo_resolution,
        "output_directory": output_directory,
        "photos_posterior_side": photos_posterior_side,
        "order_posterior_to_anterior": order_posterior_to_anterior,
        "allow_z_stretch": allow_z_stretch,
        "rigid_only_for_photos": rigid_only_for_photos,
    }
    if ref_mask is not None:
        params["ref_mask"] = ref_mask
    if ref_surface is not None:
        params["ref_surface"] = ref_surface
    if ref_soft_mask is not None:
        params["ref_soft_mask"] = ref_soft_mask
    if mesh_reorient_with_indices is not None:
        params["mesh_reorient_with_indices"] = mesh_reorient_with_indices
    if gpu_index is not None:
        params["gpu_index"] = gpu_index
    return params

def mri_3d_photo_recon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mri3dPhotoRecon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_photo_dir", None) is None:
        raise StyxValidationError("`input_photo_dir` must not be None")
    if not isinstance(params["input_photo_dir"], list):
        raise StyxValidationError(f'`input_photo_dir` has the wrong type: Received `{type(params["input_photo_dir"])}` expected `list[InputPathType]`')
    for e in params["input_photo_dir"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_photo_dir` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("input_segmentation_dir", None) is None:
        raise StyxValidationError("`input_segmentation_dir` must not be None")
    if not isinstance(params["input_segmentation_dir"], list):
        raise StyxValidationError(f'`input_segmentation_dir` has the wrong type: Received `{type(params["input_segmentation_dir"])}` expected `list[InputPathType]`')
    for e_2 in params["input_segmentation_dir"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_segmentation_dir` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("slice_thickness", None) is None:
        raise StyxValidationError("`slice_thickness` must not be None")
    if not isinstance(params["slice_thickness"], (float, int)):
        raise StyxValidationError(f'`slice_thickness` has the wrong type: Received `{type(params["slice_thickness"])}` expected `float`')
    if params.get("photo_resolution", None) is None:
        raise StyxValidationError("`photo_resolution` must not be None")
    if not isinstance(params["photo_resolution"], (float, int)):
        raise StyxValidationError(f'`photo_resolution` has the wrong type: Received `{type(params["photo_resolution"])}` expected `float`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("ref_mask", None) is not None:
        if not isinstance(params["ref_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ref_mask` has the wrong type: Received `{type(params["ref_mask"])}` expected `InputPathType`')
    if params.get("ref_surface", None) is not None:
        if not isinstance(params["ref_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ref_surface` has the wrong type: Received `{type(params["ref_surface"])}` expected `InputPathType`')
    if params.get("ref_soft_mask", None) is not None:
        if not isinstance(params["ref_soft_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ref_soft_mask` has the wrong type: Received `{type(params["ref_soft_mask"])}` expected `InputPathType`')
    if params.get("mesh_reorient_with_indices", None) is not None:
        if not isinstance(params["mesh_reorient_with_indices"], str):
            raise StyxValidationError(f'`mesh_reorient_with_indices` has the wrong type: Received `{type(params["mesh_reorient_with_indices"])}` expected `str`')
    if params.get("photos_posterior_side", None) is not None:
        if not isinstance(params["photos_posterior_side"], bool):
            raise StyxValidationError(f'`photos_posterior_side` has the wrong type: Received `{type(params["photos_posterior_side"])}` expected `bool`')
    if params.get("order_posterior_to_anterior", None) is not None:
        if not isinstance(params["order_posterior_to_anterior"], bool):
            raise StyxValidationError(f'`order_posterior_to_anterior` has the wrong type: Received `{type(params["order_posterior_to_anterior"])}` expected `bool`')
    if params.get("allow_z_stretch", None) is not None:
        if not isinstance(params["allow_z_stretch"], bool):
            raise StyxValidationError(f'`allow_z_stretch` has the wrong type: Received `{type(params["allow_z_stretch"])}` expected `bool`')
    if params.get("rigid_only_for_photos", None) is not None:
        if not isinstance(params["rigid_only_for_photos"], bool):
            raise StyxValidationError(f'`rigid_only_for_photos` has the wrong type: Received `{type(params["rigid_only_for_photos"])}` expected `bool`')
    if params.get("gpu_index", None) is not None:
        if not isinstance(params["gpu_index"], (float, int)):
            raise StyxValidationError(f'`gpu_index` has the wrong type: Received `{type(params["gpu_index"])}` expected `float`')

def mri_3d_photo_recon_cargs(params: Mri3dPhotoRecon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_3d_photo_recon")
    cargs.append("--input_photo_dir")
    for item0 in params["input_photo_dir"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--input_segmentation_dir")
    for item1 in params["input_segmentation_dir"]:
        cargs.append(execution.input_file(item1))
    cargs.append("--slice_thickness")
    cargs.append(str(params["slice_thickness"]))
    cargs.append("--photo_resolution")
    cargs.append(str(params["photo_resolution"]))
    cargs.append("--output_directory")
    cargs.append(params["output_directory"])
    v_0 = params.get("ref_mask")
    if v_0 is not None:
        cargs.append("--ref_mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("ref_surface")
    if v_1 is not None:
        cargs.append("--ref_surface")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("ref_soft_mask")
    if v_2 is not None:
        cargs.append("--ref_soft_mask")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("mesh_reorient_with_indices")
    if v_3 is not None:
        cargs.append("--mesh_reorient_with_indices")
        cargs.append(v_3)
    if params.get("photos_posterior_side"):
        cargs.append("--photos_of_posterior_side")
    if params.get("order_posterior_to_anterior"):
        cargs.append("--order_posterior_to_anterior")
    if params.get("allow_z_stretch"):
        cargs.append("--allow_z_stretch")
    if params.get("rigid_only_for_photos"):
        cargs.append("--rigid_only_for_photos")
    v_4 = params.get("gpu_index")
    if v_4 is not None:
        cargs.append("--gpu")
        cargs.append(str(v_4))
    return cargs

def mri_3d_photo_recon_outputs(params: Mri3dPhotoRecon, execution: Execution) -> Mri3dPhotoReconOutputs:
    """Build the Mri3dPhotoReconOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reconstructed_volume_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/reconstructed_volume.nii.gz')
    reference_data_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/reference_data.nii.gz')
    return Mri3dPhotoReconOutputs(
        root=root_v,
        reconstructed_volume=reconstructed_volume_v,
        reference_data=reference_data_v,
    )

def mri_3d_photo_recon_execute(params: Mri3dPhotoRecon, runner: Runner | None = None) -> Mri3dPhotoReconOutputs:
    """
    mri_3d_photo_recon

    Code for 3D photo reconstruction (Tregidgo, et al., MICCAI 2020)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_3d_photo_recon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_3D_PHOTO_RECON_METADATA)
    execution.params(params)
    args = mri_3d_photo_recon_cargs(params, execution)
    out = mri_3d_photo_recon_outputs(params, execution)
    execution.run(args)
    return out

def mri_3d_photo_recon(
    input_photo_dir: list[InputPathType],
    input_segmentation_dir: list[InputPathType],
    slice_thickness: float,
    photo_resolution: float,
    output_directory: str,
    ref_mask: InputPathType | None = None,
    ref_surface: InputPathType | None = None,
    ref_soft_mask: InputPathType | None = None,
    mesh_reorient_with_indices: str | None = None,
    photos_posterior_side: bool = False,
    order_posterior_to_anterior: bool = False,
    allow_z_stretch: bool = False,
    rigid_only_for_photos: bool = False,
    gpu_index: float | None = None,
    runner: Runner | None = None,
) -> Mri3dPhotoReconOutputs:
    """
    mri_3d_photo_recon

    Code for 3D photo reconstruction (Tregidgo, et al., MICCAI 2020)

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_photo_dir: Directory with input photos (required)
        input_segmentation_dir: Directory with input slab masks / segmentations\
            (required)
        slice_thickness: Slice thickness in mm
        photo_resolution: Resolution of the photos in mm
        output_directory: Output directory with reconstructed photo volume and\
            reference
        ref_mask: Reference binary mask
        ref_surface: Reference surface file
        ref_soft_mask: Reference soft mask
        mesh_reorient_with_indices: Vertex indices of frontal pole, occipital pole,\
            and top of central sulcus, separated with commas, for mesh alignment
        photos_posterior_side: Use when photos are taken of posterior side of slabs\
            (default is anterior side)
        order_posterior_to_anterior: Use when photos are ordered from posterior to\
            anterior (default is anterior to posterior)
        allow_z_stretch: Use to adjust the slice thickness to best match the\
            reference. You should probably *never* use this with soft references\
            (ref_soft_mask)
        rigid_only_for_photos: Switch on if you want photos to deform only rigidly\
            (not affine)
        gpu_index: Index of GPU to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_3d_photo_recon_params(
        input_photo_dir=input_photo_dir,
        input_segmentation_dir=input_segmentation_dir,
        slice_thickness=slice_thickness,
        photo_resolution=photo_resolution,
        output_directory=output_directory,
        ref_mask=ref_mask,
        ref_surface=ref_surface,
        ref_soft_mask=ref_soft_mask,
        mesh_reorient_with_indices=mesh_reorient_with_indices,
        photos_posterior_side=photos_posterior_side,
        order_posterior_to_anterior=order_posterior_to_anterior,
        allow_z_stretch=allow_z_stretch,
        rigid_only_for_photos=rigid_only_for_photos,
        gpu_index=gpu_index,
    )
    return mri_3d_photo_recon_execute(params, runner)

__all__ = [
    "Mri3dPhotoRecon",
    "Mri3dPhotoReconOutputs",
    "MRI_3D_PHOTO_RECON_METADATA",
    "mri_3d_photo_recon_cargs",
    "mri_3d_photo_recon_outputs",
    "mri_3d_photo_recon_params",
    "mri_3d_photo_recon_execute",
    "mri_3d_photo_recon_validate",
    "mri_3d_photo_recon",
]