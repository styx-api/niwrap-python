# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

THICKDIFFMAP_METADATA = Metadata(
    id="thickdiffmap",
    name="thickdiffmap",
    package="freesurfer",
)

Thickdiffmap = typing.TypedDict(
    "Thickdiffmap",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/thickdiffmap"]],
        "subjscan1": InputPathType,
        "subjscan2": InputPathType,
        "commonsubj": str,
        "hemi": str,
        "steps": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class ThickdiffmapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def thickdiffmap_params(
    subjscan1: InputPathType,
    subjscan2: InputPathType,
    commonsubj: str,
    hemi: str,
    steps: list[str] | None = None,
) -> Thickdiffmap:
    """
    Build parameters.

    Args:
        subjscan1: First scan of a subject
        subjscan2: Second (later) scan of the same subject
        commonsubj: Subject to use as the common template
        hemi: Hemisphere to process
        steps: Stages of processing

    Returns:
        Parameter dictionary.
    """
    params: Thickdiffmap = {
        "@type": "freesurfer/thickdiffmap",
        "subjscan1": subjscan1,
        "subjscan2": subjscan2,
        "commonsubj": commonsubj,
        "hemi": hemi,
    }
    if steps is not None:
        params["steps"] = steps
    return params

def thickdiffmap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Thickdiffmap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjscan1", None) is None:
        raise StyxValidationError("`subjscan1` must not be None")
    if not isinstance(params["subjscan1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subjscan1` has the wrong type: Received `{type(params["subjscan1"])}` expected `InputPathType`')
    if params.get("subjscan2", None) is None:
        raise StyxValidationError("`subjscan2` must not be None")
    if not isinstance(params["subjscan2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subjscan2` has the wrong type: Received `{type(params["subjscan2"])}` expected `InputPathType`')
    if params.get("commonsubj", None) is None:
        raise StyxValidationError("`commonsubj` must not be None")
    if not isinstance(params["commonsubj"], str):
        raise StyxValidationError(f'`commonsubj` has the wrong type: Received `{type(params["commonsubj"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("steps", None) is not None:
        if not isinstance(params["steps"], list):
            raise StyxValidationError(f'`steps` has the wrong type: Received `{type(params["steps"])}` expected `list[str]`')
        for e in params["steps"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`steps` has the wrong type: Received `{type(e)}` expected `list[str]`')

def thickdiffmap_cargs(params: Thickdiffmap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("thickdiffmap")
    cargs.append(execution.input_file(params["subjscan1"]))
    cargs.append(execution.input_file(params["subjscan2"]))
    cargs.append(params["commonsubj"])
    cargs.append(params["hemi"])
    v_0 = params.get("steps")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    return cargs

def thickdiffmap_outputs(params: Thickdiffmap, execution: Execution) -> ThickdiffmapOutputs:
    """Build the ThickdiffmapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ThickdiffmapOutputs(
        root=root_v,
    )

def thickdiffmap_execute(params: Thickdiffmap, runner: Runner | None = None) -> ThickdiffmapOutputs:
    """
    thickdiffmap

    Compute and analyze cortical thickness difference maps

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    thickdiffmap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(THICKDIFFMAP_METADATA)
    execution.params(params)
    args = thickdiffmap_cargs(params, execution)
    out = thickdiffmap_outputs(params, execution)
    execution.run(args)
    return out

def thickdiffmap(
    subjscan1: InputPathType,
    subjscan2: InputPathType,
    commonsubj: str,
    hemi: str,
    steps: list[str] | None = None,
    runner: Runner | None = None,
) -> ThickdiffmapOutputs:
    """
    thickdiffmap

    Compute and analyze cortical thickness difference maps

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjscan1: First scan of a subject
        subjscan2: Second (later) scan of the same subject
        commonsubj: Subject to use as the common template
        hemi: Hemisphere to process
        steps: Stages of processing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = thickdiffmap_params(
        subjscan1=subjscan1,
        subjscan2=subjscan2,
        commonsubj=commonsubj,
        hemi=hemi,
        steps=steps,
    )
    return thickdiffmap_execute(params, runner)

__all__ = [
    "Thickdiffmap",
    "ThickdiffmapOutputs",
    "THICKDIFFMAP_METADATA",
    "thickdiffmap_cargs",
    "thickdiffmap_outputs",
    "thickdiffmap_params",
    "thickdiffmap_execute",
    "thickdiffmap_validate",
    "thickdiffmap",
]