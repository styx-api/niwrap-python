# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CHECK_SUBJECT_METADATA = Metadata(
    id="check_subject",
    name="check_subject",
    package="freesurfer",
)

CheckSubject = typing.TypedDict(
    "CheckSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/check_subject"]],
        "subject_dir": str,
    },
)

@dataclasses.dataclass
class CheckSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def check_subject_params(
    subject_dir: str,
) -> CheckSubject:
    """
    Build parameters.

    Args:
        subject_dir: Path to the subject directory to check.

    Returns:
        Parameter dictionary.
    """
    params: CheckSubject = {
        "@type": "freesurfer/check_subject",
        "subject_dir": subject_dir,
    }
    return params

def check_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CheckSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')

def check_subject_cargs(params: CheckSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("check_subject")
    cargs.append(params["subject_dir"])
    return cargs

def check_subject_outputs(params: CheckSubject, execution: Execution) -> CheckSubjectOutputs:
    """Build the CheckSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CheckSubjectOutputs(
        root=root_v,
    )

def check_subject_execute(params: CheckSubject, runner: Runner | None = None) -> CheckSubjectOutputs:
    """
    check_subject

    Checks a subject directory for the existence of a surf directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    check_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CHECK_SUBJECT_METADATA)
    execution.params(params)
    args = check_subject_cargs(params, execution)
    out = check_subject_outputs(params, execution)
    execution.run(args)
    return out

def check_subject(
    subject_dir: str,
    runner: Runner | None = None,
) -> CheckSubjectOutputs:
    """
    check_subject

    Checks a subject directory for the existence of a surf directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Path to the subject directory to check.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = check_subject_params(
        subject_dir=subject_dir,
    )
    return check_subject_execute(params, runner)

__all__ = [
    "CheckSubject",
    "CheckSubjectOutputs",
    "CHECK_SUBJECT_METADATA",
    "check_subject_cargs",
    "check_subject_outputs",
    "check_subject_params",
    "check_subject_execute",
    "check_subject_validate",
    "check_subject",
]