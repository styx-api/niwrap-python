# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_Z2P_METADATA = Metadata(
    id="mri_z2p",
    name="mri_z2p",
    package="freesurfer",
)

MriZ2p = typing.TypedDict(
    "MriZ2p",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_z2p"]],
        "z_volume": InputPathType,
        "p_volume": str,
        "sig_volume": str,
        "mask_volume": typing.NotRequired[InputPathType],
        "two_sided": typing.NotRequired[bool],
        "one_sided": typing.NotRequired[bool],
        "signed": typing.NotRequired[bool],
        "feat": typing.NotRequired[str],
        "feat_format": typing.NotRequired[str],
        "nii_format": typing.NotRequired[bool],
        "niigz_format": typing.NotRequired[bool],
        "mgh_format": typing.NotRequired[bool],
        "mgz_format": typing.NotRequired[bool],
        "img_format": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "check_opts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriZ2pOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_p_volume: OutputPathType
    """Output P volume file."""
    output_sig_volume: OutputPathType
    """Output significance volume file."""

def mri_z2p_params(
    z_volume: InputPathType,
    p_volume: str,
    sig_volume: str,
    mask_volume: InputPathType | None = None,
    two_sided: bool = False,
    one_sided: bool = False,
    signed: bool = False,
    feat: str | None = None,
    feat_format: str | None = None,
    nii_format: bool = False,
    niigz_format: bool = False,
    mgh_format: bool = False,
    mgz_format: bool = False,
    img_format: bool = False,
    debug: bool = False,
    check_opts: bool = False,
) -> MriZ2p:
    """
    Build parameters.

    Args:
        z_volume: Z volume file
        p_volume: P volume file
        sig_volume: Significance volume file
        mask_volume: Mask volume file
        two_sided: Assume a two-sided, unsigned test (keeps sign of input).
        one_sided: Assume a one-sided, signed test.
        signed: Two-sided/signed p-value (p = 2*(1-p)).
        feat: Convert all z-stats and zf-stats to sigs in the specified directory.
        feat_format: Use specified format for output (e.g., nii, nii.gz, mgh).
        nii_format: Use NIfTI output format.
        niigz_format: Use compressed NIfTI output format.
        mgh_format: Use MGH output format.
        mgz_format: Use MGZ output format.
        img_format: Use Analyze output format.
        debug: Turn on debugging.
        check_opts: Don't run anything, just check options and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriZ2p = {
        "@type": "freesurfer/mri_z2p",
        "z_volume": z_volume,
        "p_volume": p_volume,
        "sig_volume": sig_volume,
        "two_sided": two_sided,
        "one_sided": one_sided,
        "signed": signed,
        "nii_format": nii_format,
        "niigz_format": niigz_format,
        "mgh_format": mgh_format,
        "mgz_format": mgz_format,
        "img_format": img_format,
        "debug": debug,
        "check_opts": check_opts,
    }
    if mask_volume is not None:
        params["mask_volume"] = mask_volume
    if feat is not None:
        params["feat"] = feat
    if feat_format is not None:
        params["feat_format"] = feat_format
    return params

def mri_z2p_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriZ2p."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("z_volume", None) is None:
        raise StyxValidationError("`z_volume` must not be None")
    if not isinstance(params["z_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`z_volume` has the wrong type: Received `{type(params["z_volume"])}` expected `InputPathType`')
    if params.get("p_volume", None) is None:
        raise StyxValidationError("`p_volume` must not be None")
    if not isinstance(params["p_volume"], str):
        raise StyxValidationError(f'`p_volume` has the wrong type: Received `{type(params["p_volume"])}` expected `str`')
    if params.get("sig_volume", None) is None:
        raise StyxValidationError("`sig_volume` must not be None")
    if not isinstance(params["sig_volume"], str):
        raise StyxValidationError(f'`sig_volume` has the wrong type: Received `{type(params["sig_volume"])}` expected `str`')
    if params.get("mask_volume", None) is not None:
        if not isinstance(params["mask_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `InputPathType`')
    if params.get("two_sided", None) is not None:
        if not isinstance(params["two_sided"], bool):
            raise StyxValidationError(f'`two_sided` has the wrong type: Received `{type(params["two_sided"])}` expected `bool`')
    if params.get("one_sided", None) is not None:
        if not isinstance(params["one_sided"], bool):
            raise StyxValidationError(f'`one_sided` has the wrong type: Received `{type(params["one_sided"])}` expected `bool`')
    if params.get("signed", None) is not None:
        if not isinstance(params["signed"], bool):
            raise StyxValidationError(f'`signed` has the wrong type: Received `{type(params["signed"])}` expected `bool`')
    if params.get("feat", None) is not None:
        if not isinstance(params["feat"], str):
            raise StyxValidationError(f'`feat` has the wrong type: Received `{type(params["feat"])}` expected `str`')
    if params.get("feat_format", None) is not None:
        if not isinstance(params["feat_format"], str):
            raise StyxValidationError(f'`feat_format` has the wrong type: Received `{type(params["feat_format"])}` expected `str`')
    if params.get("nii_format", None) is not None:
        if not isinstance(params["nii_format"], bool):
            raise StyxValidationError(f'`nii_format` has the wrong type: Received `{type(params["nii_format"])}` expected `bool`')
    if params.get("niigz_format", None) is not None:
        if not isinstance(params["niigz_format"], bool):
            raise StyxValidationError(f'`niigz_format` has the wrong type: Received `{type(params["niigz_format"])}` expected `bool`')
    if params.get("mgh_format", None) is not None:
        if not isinstance(params["mgh_format"], bool):
            raise StyxValidationError(f'`mgh_format` has the wrong type: Received `{type(params["mgh_format"])}` expected `bool`')
    if params.get("mgz_format", None) is not None:
        if not isinstance(params["mgz_format"], bool):
            raise StyxValidationError(f'`mgz_format` has the wrong type: Received `{type(params["mgz_format"])}` expected `bool`')
    if params.get("img_format", None) is not None:
        if not isinstance(params["img_format"], bool):
            raise StyxValidationError(f'`img_format` has the wrong type: Received `{type(params["img_format"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_opts", None) is not None:
        if not isinstance(params["check_opts"], bool):
            raise StyxValidationError(f'`check_opts` has the wrong type: Received `{type(params["check_opts"])}` expected `bool`')

def mri_z2p_cargs(params: MriZ2p, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_z2p")
    cargs.append("--z")
    cargs.append(execution.input_file(params["z_volume"]))
    cargs.append("--p")
    cargs.append(params["p_volume"])
    cargs.append("--log10p")
    cargs.append(params["sig_volume"])
    v_0 = params.get("mask_volume")
    if v_0 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_0))
    if params.get("two_sided"):
        cargs.append("--two-sided")
    if params.get("one_sided"):
        cargs.append("--one-sided")
    if params.get("signed"):
        cargs.append("--signed")
    v_1 = params.get("feat")
    if v_1 is not None:
        cargs.append("--feat")
        cargs.append(v_1)
    v_2 = params.get("feat_format")
    if v_2 is not None:
        cargs.append("--featfmt")
        cargs.append(v_2)
    if params.get("nii_format"):
        cargs.append("--nii")
    if params.get("niigz_format"):
        cargs.append("--nii.gz")
    if params.get("mgh_format"):
        cargs.append("--mgh")
    if params.get("mgz_format"):
        cargs.append("--mgz")
    if params.get("img_format"):
        cargs.append("--img")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_opts"):
        cargs.append("--checkopts")
    return cargs

def mri_z2p_outputs(params: MriZ2p, execution: Execution) -> MriZ2pOutputs:
    """Build the MriZ2pOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_p_volume_v: OutputPathType = execution.output_file(params["p_volume"])
    output_sig_volume_v: OutputPathType = execution.output_file(params["sig_volume"])
    return MriZ2pOutputs(
        root=root_v,
        output_p_volume=output_p_volume_v,
        output_sig_volume=output_sig_volume_v,
    )

def mri_z2p_execute(params: MriZ2p, runner: Runner | None = None) -> MriZ2pOutputs:
    """
    mri_z2p

    Converts Z-statistic volumes to P-value volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_z2p_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_Z2P_METADATA)
    execution.params(params)
    args = mri_z2p_cargs(params, execution)
    out = mri_z2p_outputs(params, execution)
    execution.run(args)
    return out

def mri_z2p(
    z_volume: InputPathType,
    p_volume: str,
    sig_volume: str,
    mask_volume: InputPathType | None = None,
    two_sided: bool = False,
    one_sided: bool = False,
    signed: bool = False,
    feat: str | None = None,
    feat_format: str | None = None,
    nii_format: bool = False,
    niigz_format: bool = False,
    mgh_format: bool = False,
    mgz_format: bool = False,
    img_format: bool = False,
    debug: bool = False,
    check_opts: bool = False,
    runner: Runner | None = None,
) -> MriZ2pOutputs:
    """
    mri_z2p

    Converts Z-statistic volumes to P-value volumes.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        z_volume: Z volume file
        p_volume: P volume file
        sig_volume: Significance volume file
        mask_volume: Mask volume file
        two_sided: Assume a two-sided, unsigned test (keeps sign of input).
        one_sided: Assume a one-sided, signed test.
        signed: Two-sided/signed p-value (p = 2*(1-p)).
        feat: Convert all z-stats and zf-stats to sigs in the specified directory.
        feat_format: Use specified format for output (e.g., nii, nii.gz, mgh).
        nii_format: Use NIfTI output format.
        niigz_format: Use compressed NIfTI output format.
        mgh_format: Use MGH output format.
        mgz_format: Use MGZ output format.
        img_format: Use Analyze output format.
        debug: Turn on debugging.
        check_opts: Don't run anything, just check options and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_z2p_params(
        z_volume=z_volume,
        p_volume=p_volume,
        sig_volume=sig_volume,
        mask_volume=mask_volume,
        two_sided=two_sided,
        one_sided=one_sided,
        signed=signed,
        feat=feat,
        feat_format=feat_format,
        nii_format=nii_format,
        niigz_format=niigz_format,
        mgh_format=mgh_format,
        mgz_format=mgz_format,
        img_format=img_format,
        debug=debug,
        check_opts=check_opts,
    )
    return mri_z2p_execute(params, runner)

__all__ = [
    "MriZ2p",
    "MriZ2pOutputs",
    "MRI_Z2P_METADATA",
    "mri_z2p_cargs",
    "mri_z2p_outputs",
    "mri_z2p_params",
    "mri_z2p_execute",
    "mri_z2p_validate",
    "mri_z2p",
]