# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COMPUTE_INTERRATER_VARIABILITY_CSH_METADATA = Metadata(
    id="compute_interrater_variability.csh",
    name="compute_interrater_variability.csh",
    package="freesurfer",
)

ComputeInterraterVariabilityCsh = typing.TypedDict(
    "ComputeInterraterVariabilityCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/compute_interrater_variability.csh"]],
        "label_vol1": InputPathType,
        "label_vol2": InputPathType,
        "output_prefix": str,
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ComputeInterraterVariabilityCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file_1: OutputPathType
    """Output file containing mean Hausdorff distance."""
    output_file_2: OutputPathType
    """Output file containing max Hausdorff distance."""
    output_file_3: OutputPathType
    """Output file containing label volume difference, Dice, and Jaccard overlap measures."""

def compute_interrater_variability_csh_params(
    label_vol1: InputPathType,
    label_vol2: InputPathType,
    output_prefix: str,
    version: bool = False,
    help: bool = False,
) -> ComputeInterraterVariabilityCsh:
    """
    Build parameters.

    Args:
        label_vol1: Label volume from rater 1.
        label_vol2: Label volume from rater 2.
        output_prefix: Prefix for the output text files containing results. A total\
            of three files will be produced.
        version: Print version information and exit.
        help: Print help information and exit.

    Returns:
        Parameter dictionary.
    """
    params: ComputeInterraterVariabilityCsh = {
        "@type": "freesurfer/compute_interrater_variability.csh",
        "label_vol1": label_vol1,
        "label_vol2": label_vol2,
        "output_prefix": output_prefix,
        "version": version,
        "help": help,
    }
    return params

def compute_interrater_variability_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ComputeInterraterVariabilityCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_vol1", None) is None:
        raise StyxValidationError("`label_vol1` must not be None")
    if not isinstance(params["label_vol1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_vol1` has the wrong type: Received `{type(params["label_vol1"])}` expected `InputPathType`')
    if params.get("label_vol2", None) is None:
        raise StyxValidationError("`label_vol2` must not be None")
    if not isinstance(params["label_vol2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_vol2` has the wrong type: Received `{type(params["label_vol2"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def compute_interrater_variability_csh_cargs(params: ComputeInterraterVariabilityCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("compute_interrater_variability.csh")
    cargs.append("--vol1")
    cargs.append(execution.input_file(params["label_vol1"]))
    cargs.append("--vol2")
    cargs.append(execution.input_file(params["label_vol2"]))
    cargs.append("--out")
    cargs.append(params["output_prefix"])
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def compute_interrater_variability_csh_outputs(params: ComputeInterraterVariabilityCsh, execution: Execution) -> ComputeInterraterVariabilityCshOutputs:
    """Build the ComputeInterraterVariabilityCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_1_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_file1.txt')
    output_file_2_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_file2.txt')
    output_file_3_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_file3.txt')
    return ComputeInterraterVariabilityCshOutputs(
        root=root_v,
        output_file_1=output_file_1_v,
        output_file_2=output_file_2_v,
        output_file_3=output_file_3_v,
    )

def compute_interrater_variability_csh_execute(params: ComputeInterraterVariabilityCsh, runner: Runner | None = None) -> ComputeInterraterVariabilityCshOutputs:
    """
    compute_interrater_variability.csh

    Computes the interrater variability between label volumes from different raters or time points using several metrics.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    compute_interrater_variability_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COMPUTE_INTERRATER_VARIABILITY_CSH_METADATA)
    execution.params(params)
    args = compute_interrater_variability_csh_cargs(params, execution)
    out = compute_interrater_variability_csh_outputs(params, execution)
    execution.run(args)
    return out

def compute_interrater_variability_csh(
    label_vol1: InputPathType,
    label_vol2: InputPathType,
    output_prefix: str,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> ComputeInterraterVariabilityCshOutputs:
    """
    compute_interrater_variability.csh

    Computes the interrater variability between label volumes from different raters or time points using several metrics.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label_vol1: Label volume from rater 1.
        label_vol2: Label volume from rater 2.
        output_prefix: Prefix for the output text files containing results. A total\
            of three files will be produced.
        version: Print version information and exit.
        help: Print help information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = compute_interrater_variability_csh_params(
        label_vol1=label_vol1,
        label_vol2=label_vol2,
        output_prefix=output_prefix,
        version=version,
        help=help,
    )
    return compute_interrater_variability_csh_execute(params, runner)

__all__ = [
    "ComputeInterraterVariabilityCsh",
    "ComputeInterraterVariabilityCshOutputs",
    "COMPUTE_INTERRATER_VARIABILITY_CSH_METADATA",
    "compute_interrater_variability_csh_cargs",
    "compute_interrater_variability_csh_outputs",
    "compute_interrater_variability_csh_params",
    "compute_interrater_variability_csh_execute",
    "compute_interrater_variability_csh_validate",
    "compute_interrater_variability_csh",
]