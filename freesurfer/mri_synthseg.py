# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SYNTHSEG_METADATA = Metadata(
    id="mri_synthseg",
    name="mri_synthseg",
    package="freesurfer",
)

MriSynthseg = typing.TypedDict(
    "MriSynthseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_synthseg"]],
        "input_image": InputPathType,
        "output_segmentation": str,
        "cortex_parcellation": typing.NotRequired[bool],
        "robust_prediction": typing.NotRequired[bool],
        "fast_prediction": typing.NotRequired[bool],
        "clip_ct": typing.NotRequired[bool],
        "output_volume": typing.NotRequired[str],
        "output_qc": typing.NotRequired[str],
        "output_posteriors": typing.NotRequired[str],
        "resampled_images": typing.NotRequired[str],
        "image_patch_size": typing.NotRequired[list[float]],
        "threads": typing.NotRequired[float],
        "cpu": typing.NotRequired[bool],
        "version_1": typing.NotRequired[bool],
        "photo_synthseg": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriSynthsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_segmentation_file: OutputPathType
    """Segmentation output(s)"""
    output_volume_csv: OutputPathType | None
    """CSV file with volumes for all structures and subjects"""
    output_qc_csv: OutputPathType | None
    """CSV file with QC scores for all subjects"""
    output_posteriors_file: OutputPathType | None
    """Posteriors output(s)"""
    resampled_images_file: OutputPathType | None
    """Resampled image(s)"""

def mri_synthseg_params(
    input_image: InputPathType,
    output_segmentation: str,
    cortex_parcellation: bool = False,
    robust_prediction: bool = False,
    fast_prediction: bool = False,
    clip_ct: bool = False,
    output_volume: str | None = None,
    output_qc: str | None = None,
    output_posteriors: str | None = None,
    resampled_images: str | None = None,
    image_patch_size: list[float] | None = None,
    threads: float | None = None,
    cpu: bool = False,
    version_1: bool = False,
    photo_synthseg: str | None = None,
) -> MriSynthseg:
    """
    Build parameters.

    Args:
        input_image: Image(s) to segment. Can be a path to an image or to a folder.
        output_segmentation: Segmentation output(s). Must be a folder if --i\
            designates a folder.
        cortex_parcellation: Perform cortex parcellation.
        robust_prediction: Use robust predictions (slower).
        fast_prediction: Bypass some processing for faster prediction.
        clip_ct: Clip CT scans in Hounsfield scale to [0, 80].
        output_volume: Output CSV file with volumes for all structures and\
            subjects.
        output_qc: Output CSV file with QC scores for all subjects.
        output_posteriors: Posteriors output(s). Must be a folder if --i designates\
            a folder.
        resampled_images: Resampled image(s). Must be a folder if --i is a folder.
        image_patch_size: Only analyse an image patch of the given size.
        threads: Number of cores to be used. Default is 1.
        cpu: Enforce running with CPU rather than GPU.
        version_1: Use SynthSeg 1.0 (updated 25/06/22).
        photo_synthseg: Photo-SynthSeg: segment 3D reconstructed stack of coronal\
            dissection photos of the cerebrum; must be left, right, or both.

    Returns:
        Parameter dictionary.
    """
    params: MriSynthseg = {
        "@type": "freesurfer/mri_synthseg",
        "input_image": input_image,
        "output_segmentation": output_segmentation,
        "cortex_parcellation": cortex_parcellation,
        "robust_prediction": robust_prediction,
        "fast_prediction": fast_prediction,
        "clip_ct": clip_ct,
        "cpu": cpu,
        "version_1": version_1,
    }
    if output_volume is not None:
        params["output_volume"] = output_volume
    if output_qc is not None:
        params["output_qc"] = output_qc
    if output_posteriors is not None:
        params["output_posteriors"] = output_posteriors
    if resampled_images is not None:
        params["resampled_images"] = resampled_images
    if image_patch_size is not None:
        params["image_patch_size"] = image_patch_size
    if threads is not None:
        params["threads"] = threads
    if photo_synthseg is not None:
        params["photo_synthseg"] = photo_synthseg
    return params

def mri_synthseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSynthseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_segmentation", None) is None:
        raise StyxValidationError("`output_segmentation` must not be None")
    if not isinstance(params["output_segmentation"], str):
        raise StyxValidationError(f'`output_segmentation` has the wrong type: Received `{type(params["output_segmentation"])}` expected `str`')
    if params.get("cortex_parcellation", None) is not None:
        if not isinstance(params["cortex_parcellation"], bool):
            raise StyxValidationError(f'`cortex_parcellation` has the wrong type: Received `{type(params["cortex_parcellation"])}` expected `bool`')
    if params.get("robust_prediction", None) is not None:
        if not isinstance(params["robust_prediction"], bool):
            raise StyxValidationError(f'`robust_prediction` has the wrong type: Received `{type(params["robust_prediction"])}` expected `bool`')
    if params.get("fast_prediction", None) is not None:
        if not isinstance(params["fast_prediction"], bool):
            raise StyxValidationError(f'`fast_prediction` has the wrong type: Received `{type(params["fast_prediction"])}` expected `bool`')
    if params.get("clip_ct", None) is not None:
        if not isinstance(params["clip_ct"], bool):
            raise StyxValidationError(f'`clip_ct` has the wrong type: Received `{type(params["clip_ct"])}` expected `bool`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("output_qc", None) is not None:
        if not isinstance(params["output_qc"], str):
            raise StyxValidationError(f'`output_qc` has the wrong type: Received `{type(params["output_qc"])}` expected `str`')
    if params.get("output_posteriors", None) is not None:
        if not isinstance(params["output_posteriors"], str):
            raise StyxValidationError(f'`output_posteriors` has the wrong type: Received `{type(params["output_posteriors"])}` expected `str`')
    if params.get("resampled_images", None) is not None:
        if not isinstance(params["resampled_images"], str):
            raise StyxValidationError(f'`resampled_images` has the wrong type: Received `{type(params["resampled_images"])}` expected `str`')
    if params.get("image_patch_size", None) is not None:
        if not isinstance(params["image_patch_size"], list):
            raise StyxValidationError(f'`image_patch_size` has the wrong type: Received `{type(params["image_patch_size"])}` expected `list[float]`')
        for e in params["image_patch_size"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`image_patch_size` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("cpu", None) is not None:
        if not isinstance(params["cpu"], bool):
            raise StyxValidationError(f'`cpu` has the wrong type: Received `{type(params["cpu"])}` expected `bool`')
    if params.get("version_1", None) is not None:
        if not isinstance(params["version_1"], bool):
            raise StyxValidationError(f'`version_1` has the wrong type: Received `{type(params["version_1"])}` expected `bool`')
    if params.get("photo_synthseg", None) is not None:
        if not isinstance(params["photo_synthseg"], str):
            raise StyxValidationError(f'`photo_synthseg` has the wrong type: Received `{type(params["photo_synthseg"])}` expected `str`')

def mri_synthseg_cargs(params: MriSynthseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_synthseg")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_segmentation"])
    if params.get("cortex_parcellation"):
        cargs.append("--parc")
    if params.get("robust_prediction"):
        cargs.append("--robust")
    if params.get("fast_prediction"):
        cargs.append("--fast")
    if params.get("clip_ct"):
        cargs.append("--ct")
    v_0 = params.get("output_volume")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("output_qc")
    if v_1 is not None:
        cargs.append(v_1)
    v_2 = params.get("output_posteriors")
    if v_2 is not None:
        cargs.append(v_2)
    v_3 = params.get("resampled_images")
    if v_3 is not None:
        cargs.append(v_3)
    v_4 = params.get("image_patch_size")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append(str(item0))
    v_5 = params.get("threads")
    if v_5 is not None:
        cargs.append(str(v_5))
    if params.get("cpu"):
        cargs.append("--cpu")
    if params.get("version_1"):
        cargs.append("--v1")
    v_6 = params.get("photo_synthseg")
    if v_6 is not None:
        cargs.append(v_6)
    return cargs

def mri_synthseg_outputs(params: MriSynthseg, execution: Execution) -> MriSynthsegOutputs:
    """Build the MriSynthsegOutputs object for this tool."""
    output_volume_csv_v: OutputPathType | None = None
    output_qc_csv_v: OutputPathType | None = None
    output_posteriors_file_v: OutputPathType | None = None
    resampled_images_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_segmentation_file_v: OutputPathType = execution.output_file(params["output_segmentation"])
    __v0 = params.get("output_volume")
    if __v0 is not None:
        output_volume_csv_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_qc")
    if __v1 is not None:
        output_qc_csv_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("output_posteriors")
    if __v2 is not None:
        output_posteriors_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("resampled_images")
    if __v3 is not None:
        resampled_images_file_v = execution.output_file((__v3 if __v3 is not None else ""))
    return MriSynthsegOutputs(
        root=root_v,
        output_segmentation_file=output_segmentation_file_v,
        output_volume_csv=output_volume_csv_v,
        output_qc_csv=output_qc_csv_v,
        output_posteriors_file=output_posteriors_file_v,
        resampled_images_file=resampled_images_file_v,
    )

def mri_synthseg_execute(params: MriSynthseg, runner: Runner | None = None) -> MriSynthsegOutputs:
    """
    mri_synthseg

    SynthSeg is a tool for brain image segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_synthseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SYNTHSEG_METADATA)
    execution.params(params)
    args = mri_synthseg_cargs(params, execution)
    out = mri_synthseg_outputs(params, execution)
    execution.run(args)
    return out

def mri_synthseg(
    input_image: InputPathType,
    output_segmentation: str,
    cortex_parcellation: bool = False,
    robust_prediction: bool = False,
    fast_prediction: bool = False,
    clip_ct: bool = False,
    output_volume: str | None = None,
    output_qc: str | None = None,
    output_posteriors: str | None = None,
    resampled_images: str | None = None,
    image_patch_size: list[float] | None = None,
    threads: float | None = None,
    cpu: bool = False,
    version_1: bool = False,
    photo_synthseg: str | None = None,
    runner: Runner | None = None,
) -> MriSynthsegOutputs:
    """
    mri_synthseg

    SynthSeg is a tool for brain image segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: Image(s) to segment. Can be a path to an image or to a folder.
        output_segmentation: Segmentation output(s). Must be a folder if --i\
            designates a folder.
        cortex_parcellation: Perform cortex parcellation.
        robust_prediction: Use robust predictions (slower).
        fast_prediction: Bypass some processing for faster prediction.
        clip_ct: Clip CT scans in Hounsfield scale to [0, 80].
        output_volume: Output CSV file with volumes for all structures and\
            subjects.
        output_qc: Output CSV file with QC scores for all subjects.
        output_posteriors: Posteriors output(s). Must be a folder if --i designates\
            a folder.
        resampled_images: Resampled image(s). Must be a folder if --i is a folder.
        image_patch_size: Only analyse an image patch of the given size.
        threads: Number of cores to be used. Default is 1.
        cpu: Enforce running with CPU rather than GPU.
        version_1: Use SynthSeg 1.0 (updated 25/06/22).
        photo_synthseg: Photo-SynthSeg: segment 3D reconstructed stack of coronal\
            dissection photos of the cerebrum; must be left, right, or both.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_synthseg_params(
        input_image=input_image,
        output_segmentation=output_segmentation,
        cortex_parcellation=cortex_parcellation,
        robust_prediction=robust_prediction,
        fast_prediction=fast_prediction,
        clip_ct=clip_ct,
        output_volume=output_volume,
        output_qc=output_qc,
        output_posteriors=output_posteriors,
        resampled_images=resampled_images,
        image_patch_size=image_patch_size,
        threads=threads,
        cpu=cpu,
        version_1=version_1,
        photo_synthseg=photo_synthseg,
    )
    return mri_synthseg_execute(params, runner)

__all__ = [
    "MriSynthseg",
    "MriSynthsegOutputs",
    "MRI_SYNTHSEG_METADATA",
    "mri_synthseg_cargs",
    "mri_synthseg_outputs",
    "mri_synthseg_params",
    "mri_synthseg_execute",
    "mri_synthseg_validate",
    "mri_synthseg",
]