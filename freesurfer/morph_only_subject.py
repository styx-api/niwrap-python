# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_ONLY_SUBJECT_METADATA = Metadata(
    id="morph_only_subject",
    name="morph_only_subject",
    package="freesurfer",
)

MorphOnlySubject = typing.TypedDict(
    "MorphOnlySubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/morph_only_subject"]],
        "placeholder_input": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MorphOnlySubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def morph_only_subject_params(
    placeholder_input: str | None = None,
) -> MorphOnlySubject:
    """
    Build parameters.

    Args:
        placeholder_input: Placeholder input argument for morph_only_subject tool.

    Returns:
        Parameter dictionary.
    """
    params: MorphOnlySubject = {
        "@type": "freesurfer/morph_only_subject",
    }
    if placeholder_input is not None:
        params["placeholder_input"] = placeholder_input
    return params

def morph_only_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphOnlySubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("placeholder_input", None) is not None:
        if not isinstance(params["placeholder_input"], str):
            raise StyxValidationError(f'`placeholder_input` has the wrong type: Received `{type(params["placeholder_input"])}` expected `str`')

def morph_only_subject_cargs(params: MorphOnlySubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_only_subject")
    v_0 = params.get("placeholder_input")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def morph_only_subject_outputs(params: MorphOnlySubject, execution: Execution) -> MorphOnlySubjectOutputs:
    """Build the MorphOnlySubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MorphOnlySubjectOutputs(
        root=root_v,
    )

def morph_only_subject_execute(params: MorphOnlySubject, runner: Runner | None = None) -> MorphOnlySubjectOutputs:
    """
    morph_only_subject

    A placeholder descriptor for morph_only_subject tool as help content is unavailable.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_only_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_ONLY_SUBJECT_METADATA)
    execution.params(params)
    args = morph_only_subject_cargs(params, execution)
    out = morph_only_subject_outputs(params, execution)
    execution.run(args)
    return out

def morph_only_subject(
    placeholder_input: str | None = None,
    runner: Runner | None = None,
) -> MorphOnlySubjectOutputs:
    """
    morph_only_subject

    A placeholder descriptor for morph_only_subject tool as help content is unavailable.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        placeholder_input: Placeholder input argument for morph_only_subject tool.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_only_subject_params(
        placeholder_input=placeholder_input,
    )
    return morph_only_subject_execute(params, runner)

__all__ = [
    "MorphOnlySubject",
    "MorphOnlySubjectOutputs",
    "MORPH_ONLY_SUBJECT_METADATA",
    "morph_only_subject_cargs",
    "morph_only_subject_outputs",
    "morph_only_subject_params",
    "morph_only_subject_execute",
    "morph_only_subject_validate",
    "morph_only_subject",
]