# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_ROTATE_METADATA = Metadata(
    id="mris_rotate",
    name="mris_rotate",
    package="freesurfer",
)

MrisRotate = typing.TypedDict(
    "MrisRotate",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_rotate"]],
        "input_surface": InputPathType,
        "alpha_deg": float,
        "beta_deg": float,
        "gamma_deg": float,
        "output_surface": str,
        "regfile": typing.NotRequired[InputPathType],
        "invalidate_geometry": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisRotateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    rotated_surface: OutputPathType
    """The rotated output surface."""

def mris_rotate_params(
    input_surface: InputPathType,
    alpha_deg: float,
    beta_deg: float,
    gamma_deg: float,
    output_surface: str,
    regfile: InputPathType | None = None,
    invalidate_geometry: bool = False,
) -> MrisRotate:
    """
    Build parameters.

    Args:
        input_surface: Input surface file to be rotated.
        alpha_deg: Rotation angle in degrees around the X-axis.
        beta_deg: Rotation angle in degrees around the Y-axis.
        gamma_deg: Rotation angle in degrees around the Z-axis.
        output_surface: Output surface file after rotation.
        regfile: Extract angles from registration file, ignores alpha, beta, gamma.
        invalidate_geometry: Invalidate volume geometry in output.

    Returns:
        Parameter dictionary.
    """
    params: MrisRotate = {
        "@type": "freesurfer/mris_rotate",
        "input_surface": input_surface,
        "alpha_deg": alpha_deg,
        "beta_deg": beta_deg,
        "gamma_deg": gamma_deg,
        "output_surface": output_surface,
        "invalidate_geometry": invalidate_geometry,
    }
    if regfile is not None:
        params["regfile"] = regfile
    return params

def mris_rotate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRotate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("alpha_deg", None) is None:
        raise StyxValidationError("`alpha_deg` must not be None")
    if not isinstance(params["alpha_deg"], (float, int)):
        raise StyxValidationError(f'`alpha_deg` has the wrong type: Received `{type(params["alpha_deg"])}` expected `float`')
    if params.get("beta_deg", None) is None:
        raise StyxValidationError("`beta_deg` must not be None")
    if not isinstance(params["beta_deg"], (float, int)):
        raise StyxValidationError(f'`beta_deg` has the wrong type: Received `{type(params["beta_deg"])}` expected `float`')
    if params.get("gamma_deg", None) is None:
        raise StyxValidationError("`gamma_deg` must not be None")
    if not isinstance(params["gamma_deg"], (float, int)):
        raise StyxValidationError(f'`gamma_deg` has the wrong type: Received `{type(params["gamma_deg"])}` expected `float`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("regfile", None) is not None:
        if not isinstance(params["regfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`regfile` has the wrong type: Received `{type(params["regfile"])}` expected `InputPathType`')
    if params.get("invalidate_geometry", None) is not None:
        if not isinstance(params["invalidate_geometry"], bool):
            raise StyxValidationError(f'`invalidate_geometry` has the wrong type: Received `{type(params["invalidate_geometry"])}` expected `bool`')

def mris_rotate_cargs(params: MrisRotate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_rotate")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(str(params["alpha_deg"]))
    cargs.append(str(params["beta_deg"]))
    cargs.append(str(params["gamma_deg"]))
    cargs.append(params["output_surface"])
    v_0 = params.get("regfile")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_0))
    if params.get("invalidate_geometry"):
        cargs.append("-n")
    return cargs

def mris_rotate_outputs(params: MrisRotate, execution: Execution) -> MrisRotateOutputs:
    """Build the MrisRotateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    rotated_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    return MrisRotateOutputs(
        root=root_v,
        rotated_surface=rotated_surface_v,
    )

def mris_rotate_execute(params: MrisRotate, runner: Runner | None = None) -> MrisRotateOutputs:
    """
    mris_rotate

    Rotate a surface given three angles.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_rotate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_ROTATE_METADATA)
    execution.params(params)
    args = mris_rotate_cargs(params, execution)
    out = mris_rotate_outputs(params, execution)
    execution.run(args)
    return out

def mris_rotate(
    input_surface: InputPathType,
    alpha_deg: float,
    beta_deg: float,
    gamma_deg: float,
    output_surface: str,
    regfile: InputPathType | None = None,
    invalidate_geometry: bool = False,
    runner: Runner | None = None,
) -> MrisRotateOutputs:
    """
    mris_rotate

    Rotate a surface given three angles.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file to be rotated.
        alpha_deg: Rotation angle in degrees around the X-axis.
        beta_deg: Rotation angle in degrees around the Y-axis.
        gamma_deg: Rotation angle in degrees around the Z-axis.
        output_surface: Output surface file after rotation.
        regfile: Extract angles from registration file, ignores alpha, beta, gamma.
        invalidate_geometry: Invalidate volume geometry in output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_rotate_params(
        input_surface=input_surface,
        alpha_deg=alpha_deg,
        beta_deg=beta_deg,
        gamma_deg=gamma_deg,
        output_surface=output_surface,
        regfile=regfile,
        invalidate_geometry=invalidate_geometry,
    )
    return mris_rotate_execute(params, runner)

__all__ = [
    "MrisRotate",
    "MrisRotateOutputs",
    "MRIS_ROTATE_METADATA",
    "mris_rotate_cargs",
    "mris_rotate_outputs",
    "mris_rotate_params",
    "mris_rotate_execute",
    "mris_rotate_validate",
    "mris_rotate",
]