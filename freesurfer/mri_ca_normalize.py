# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CA_NORMALIZE_METADATA = Metadata(
    id="mri_ca_normalize",
    name="mri_ca_normalize",
    package="freesurfer",
)

MriCaNormalize = typing.TypedDict(
    "MriCaNormalize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_ca_normalize"]],
        "input_brain_volumes": list[InputPathType],
        "atlas_file": InputPathType,
        "xform_file": InputPathType,
        "output_volumes": list[str],
        "seg_file": typing.NotRequired[InputPathType],
        "sigma_value": typing.NotRequired[float],
        "fsamples_file": typing.NotRequired[InputPathType],
        "dilate_iters": typing.NotRequired[float],
        "nsamples_file": typing.NotRequired[InputPathType],
        "mask_vol": typing.NotRequired[InputPathType],
        "control_points_file": typing.NotRequired[InputPathType],
        "fonly_file": typing.NotRequired[InputPathType],
        "diag_file": typing.NotRequired[InputPathType],
        "debug_voxel_coords": typing.NotRequired[list[float]],
        "debug_node_coords": typing.NotRequired[list[float]],
        "tr_value": typing.NotRequired[float],
        "te_value": typing.NotRequired[float],
        "alpha_value": typing.NotRequired[float],
        "example_mri_vol": typing.NotRequired[InputPathType],
        "extra_norm_pctl": typing.NotRequired[float],
        "prior_threshold": typing.NotRequired[float],
        "n_regions": typing.NotRequired[float],
        "verbose_value": typing.NotRequired[float],
        "top_percent": typing.NotRequired[float],
        "novar_flag": typing.NotRequired[bool],
        "renorm_file": typing.NotRequired[InputPathType],
        "flash_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCaNormalizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    normalized_output: OutputPathType
    """Normalized output volume in mgh format."""

def mri_ca_normalize_params(
    input_brain_volumes: list[InputPathType],
    atlas_file: InputPathType,
    xform_file: InputPathType,
    output_volumes: list[str],
    seg_file: InputPathType | None = None,
    sigma_value: float | None = None,
    fsamples_file: InputPathType | None = None,
    dilate_iters: float | None = None,
    nsamples_file: InputPathType | None = None,
    mask_vol: InputPathType | None = None,
    control_points_file: InputPathType | None = None,
    fonly_file: InputPathType | None = None,
    diag_file: InputPathType | None = None,
    debug_voxel_coords: list[float] | None = None,
    debug_node_coords: list[float] | None = None,
    tr_value: float | None = None,
    te_value: float | None = None,
    alpha_value: float | None = None,
    example_mri_vol: InputPathType | None = None,
    extra_norm_pctl: float | None = None,
    prior_threshold: float | None = None,
    n_regions: float | None = None,
    verbose_value: float | None = None,
    top_percent: float | None = None,
    novar_flag: bool = False,
    renorm_file: InputPathType | None = None,
    flash_flag: bool = False,
) -> MriCaNormalize:
    """
    Build parameters.

    Args:
        input_brain_volumes: Input brain volume(s). Can specify multiple inputs.
        atlas_file: Atlas file in GCA format.
        xform_file: Transform file in LTA format.
        output_volumes: Output volume(s) in either mgh or mgz format. Can specify\
            multiple outputs.
        seg_file: Aseg file to help normalization.
        sigma_value: Smoothing sigma for bias field if control points specified\
            (default=4).
        fsamples_file: Write control points to filename.
        dilate_iters: Dilate the brain mask niters times before masking.
        nsamples_file: Write transformed normalization control points to filename.
        mask_vol: Use mri_vol to mask input.
        control_points_file: Define control points from filename.
        fonly_file: Only use control points from filename.
        diag_file: Write to log file.
        debug_voxel_coords: Debug voxel. Needs x, y, z coordinates.
        debug_node_coords: Debug node. Needs x, y, z coordinates.
        tr_value: Set TR in msec.
        te_value: Set TE in msec.
        alpha_value: Set alpha in radians.
        example_mri_vol: Use T1 (mri_vol) and segmentation as example.
        extra_norm_pctl: Use 1+pct and 1-pct to widen the range of T1 values.
        prior_threshold: Use prior threshold t (default=.6).
        n_regions: Use n regions/struct for normalization.
        verbose_value: Used for debugging and diagnostics.
        top_percent: Use top p percent (default=.25) white matter points as control\
            points.
        novar_flag: Do not use variance estimates.
        renorm_file: Renormalize using predicted intensity values in mri_vol.
        flash_flag: Use FLASH forward model to predict intensity values.

    Returns:
        Parameter dictionary.
    """
    params: MriCaNormalize = {
        "@type": "freesurfer/mri_ca_normalize",
        "input_brain_volumes": input_brain_volumes,
        "atlas_file": atlas_file,
        "xform_file": xform_file,
        "output_volumes": output_volumes,
        "novar_flag": novar_flag,
        "flash_flag": flash_flag,
    }
    if seg_file is not None:
        params["seg_file"] = seg_file
    if sigma_value is not None:
        params["sigma_value"] = sigma_value
    if fsamples_file is not None:
        params["fsamples_file"] = fsamples_file
    if dilate_iters is not None:
        params["dilate_iters"] = dilate_iters
    if nsamples_file is not None:
        params["nsamples_file"] = nsamples_file
    if mask_vol is not None:
        params["mask_vol"] = mask_vol
    if control_points_file is not None:
        params["control_points_file"] = control_points_file
    if fonly_file is not None:
        params["fonly_file"] = fonly_file
    if diag_file is not None:
        params["diag_file"] = diag_file
    if debug_voxel_coords is not None:
        params["debug_voxel_coords"] = debug_voxel_coords
    if debug_node_coords is not None:
        params["debug_node_coords"] = debug_node_coords
    if tr_value is not None:
        params["tr_value"] = tr_value
    if te_value is not None:
        params["te_value"] = te_value
    if alpha_value is not None:
        params["alpha_value"] = alpha_value
    if example_mri_vol is not None:
        params["example_mri_vol"] = example_mri_vol
    if extra_norm_pctl is not None:
        params["extra_norm_pctl"] = extra_norm_pctl
    if prior_threshold is not None:
        params["prior_threshold"] = prior_threshold
    if n_regions is not None:
        params["n_regions"] = n_regions
    if verbose_value is not None:
        params["verbose_value"] = verbose_value
    if top_percent is not None:
        params["top_percent"] = top_percent
    if renorm_file is not None:
        params["renorm_file"] = renorm_file
    return params

def mri_ca_normalize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCaNormalize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_brain_volumes", None) is None:
        raise StyxValidationError("`input_brain_volumes` must not be None")
    if not isinstance(params["input_brain_volumes"], list):
        raise StyxValidationError(f'`input_brain_volumes` has the wrong type: Received `{type(params["input_brain_volumes"])}` expected `list[InputPathType]`')
    for e in params["input_brain_volumes"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_brain_volumes` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("atlas_file", None) is None:
        raise StyxValidationError("`atlas_file` must not be None")
    if not isinstance(params["atlas_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_file` has the wrong type: Received `{type(params["atlas_file"])}` expected `InputPathType`')
    if params.get("xform_file", None) is None:
        raise StyxValidationError("`xform_file` must not be None")
    if not isinstance(params["xform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xform_file` has the wrong type: Received `{type(params["xform_file"])}` expected `InputPathType`')
    if params.get("output_volumes", None) is None:
        raise StyxValidationError("`output_volumes` must not be None")
    if not isinstance(params["output_volumes"], list):
        raise StyxValidationError(f'`output_volumes` has the wrong type: Received `{type(params["output_volumes"])}` expected `list[str]`')
    for e_2 in params["output_volumes"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`output_volumes` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("seg_file", None) is not None:
        if not isinstance(params["seg_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seg_file` has the wrong type: Received `{type(params["seg_file"])}` expected `InputPathType`')
    if params.get("sigma_value", None) is not None:
        if not isinstance(params["sigma_value"], (float, int)):
            raise StyxValidationError(f'`sigma_value` has the wrong type: Received `{type(params["sigma_value"])}` expected `float`')
    if params.get("fsamples_file", None) is not None:
        if not isinstance(params["fsamples_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fsamples_file` has the wrong type: Received `{type(params["fsamples_file"])}` expected `InputPathType`')
    if params.get("dilate_iters", None) is not None:
        if not isinstance(params["dilate_iters"], (float, int)):
            raise StyxValidationError(f'`dilate_iters` has the wrong type: Received `{type(params["dilate_iters"])}` expected `float`')
    if params.get("nsamples_file", None) is not None:
        if not isinstance(params["nsamples_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nsamples_file` has the wrong type: Received `{type(params["nsamples_file"])}` expected `InputPathType`')
    if params.get("mask_vol", None) is not None:
        if not isinstance(params["mask_vol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_vol` has the wrong type: Received `{type(params["mask_vol"])}` expected `InputPathType`')
    if params.get("control_points_file", None) is not None:
        if not isinstance(params["control_points_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`control_points_file` has the wrong type: Received `{type(params["control_points_file"])}` expected `InputPathType`')
    if params.get("fonly_file", None) is not None:
        if not isinstance(params["fonly_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fonly_file` has the wrong type: Received `{type(params["fonly_file"])}` expected `InputPathType`')
    if params.get("diag_file", None) is not None:
        if not isinstance(params["diag_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`diag_file` has the wrong type: Received `{type(params["diag_file"])}` expected `InputPathType`')
    if params.get("debug_voxel_coords", None) is not None:
        if not isinstance(params["debug_voxel_coords"], list):
            raise StyxValidationError(f'`debug_voxel_coords` has the wrong type: Received `{type(params["debug_voxel_coords"])}` expected `list[float]`')
        if not (3 <= len(params["debug_voxel_coords"]) <= 3):
            raise StyxValidationError("Parameter `debug_voxel_coords` must contain between 3 and 3 elements (inclusive)")
        for e_3 in params["debug_voxel_coords"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`debug_voxel_coords` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("debug_node_coords", None) is not None:
        if not isinstance(params["debug_node_coords"], list):
            raise StyxValidationError(f'`debug_node_coords` has the wrong type: Received `{type(params["debug_node_coords"])}` expected `list[float]`')
        if not (3 <= len(params["debug_node_coords"]) <= 3):
            raise StyxValidationError("Parameter `debug_node_coords` must contain between 3 and 3 elements (inclusive)")
        for e_4 in params["debug_node_coords"]:
            if not isinstance(e_4, (float, int)):
                raise StyxValidationError(f'`debug_node_coords` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
    if params.get("tr_value", None) is not None:
        if not isinstance(params["tr_value"], (float, int)):
            raise StyxValidationError(f'`tr_value` has the wrong type: Received `{type(params["tr_value"])}` expected `float`')
    if params.get("te_value", None) is not None:
        if not isinstance(params["te_value"], (float, int)):
            raise StyxValidationError(f'`te_value` has the wrong type: Received `{type(params["te_value"])}` expected `float`')
    if params.get("alpha_value", None) is not None:
        if not isinstance(params["alpha_value"], (float, int)):
            raise StyxValidationError(f'`alpha_value` has the wrong type: Received `{type(params["alpha_value"])}` expected `float`')
    if params.get("example_mri_vol", None) is not None:
        if not isinstance(params["example_mri_vol"], (pathlib.Path, str)):
            raise StyxValidationError(f'`example_mri_vol` has the wrong type: Received `{type(params["example_mri_vol"])}` expected `InputPathType`')
    if params.get("extra_norm_pctl", None) is not None:
        if not isinstance(params["extra_norm_pctl"], (float, int)):
            raise StyxValidationError(f'`extra_norm_pctl` has the wrong type: Received `{type(params["extra_norm_pctl"])}` expected `float`')
    if params.get("prior_threshold", None) is not None:
        if not isinstance(params["prior_threshold"], (float, int)):
            raise StyxValidationError(f'`prior_threshold` has the wrong type: Received `{type(params["prior_threshold"])}` expected `float`')
    if params.get("n_regions", None) is not None:
        if not isinstance(params["n_regions"], (float, int)):
            raise StyxValidationError(f'`n_regions` has the wrong type: Received `{type(params["n_regions"])}` expected `float`')
    if params.get("verbose_value", None) is not None:
        if not isinstance(params["verbose_value"], (float, int)):
            raise StyxValidationError(f'`verbose_value` has the wrong type: Received `{type(params["verbose_value"])}` expected `float`')
    if params.get("top_percent", None) is not None:
        if not isinstance(params["top_percent"], (float, int)):
            raise StyxValidationError(f'`top_percent` has the wrong type: Received `{type(params["top_percent"])}` expected `float`')
    if params.get("novar_flag", None) is not None:
        if not isinstance(params["novar_flag"], bool):
            raise StyxValidationError(f'`novar_flag` has the wrong type: Received `{type(params["novar_flag"])}` expected `bool`')
    if params.get("renorm_file", None) is not None:
        if not isinstance(params["renorm_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`renorm_file` has the wrong type: Received `{type(params["renorm_file"])}` expected `InputPathType`')
    if params.get("flash_flag", None) is not None:
        if not isinstance(params["flash_flag"], bool):
            raise StyxValidationError(f'`flash_flag` has the wrong type: Received `{type(params["flash_flag"])}` expected `bool`')

def mri_ca_normalize_cargs(params: MriCaNormalize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_ca_normalize")
    for item0 in params["input_brain_volumes"]:
        cargs.append(execution.input_file(item0))
    cargs.append(execution.input_file(params["atlas_file"]))
    cargs.append(execution.input_file(params["xform_file"]))
    for item1 in params["output_volumes"]:
        cargs.append(item1)
    v_0 = params.get("seg_file")
    if v_0 is not None:
        cargs.append("-seg")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("sigma_value")
    if v_1 is not None:
        cargs.append("-sigma")
        cargs.append(str(v_1))
    v_2 = params.get("fsamples_file")
    if v_2 is not None:
        cargs.append("-fsamples")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("dilate_iters")
    if v_3 is not None:
        cargs.append("-dilate")
        cargs.append(str(v_3))
    v_4 = params.get("nsamples_file")
    if v_4 is not None:
        cargs.append("-nsamples")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("mask_vol")
    if v_5 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("control_points_file")
    if v_6 is not None:
        cargs.append("-f")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("fonly_file")
    if v_7 is not None:
        cargs.append("-fonly")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("diag_file")
    if v_8 is not None:
        cargs.append("-diag")
        cargs.append(execution.input_file(v_8))
    v_9 = params.get("debug_voxel_coords")
    if v_9 is not None:
        cargs.append("-debug_voxel")
        for item2 in v_9:
            cargs.append(str(item2))
    v_10 = params.get("debug_node_coords")
    if v_10 is not None:
        cargs.append("-debug_node")
        for item3 in v_10:
            cargs.append(str(item3))
    v_11 = params.get("tr_value")
    if v_11 is not None:
        cargs.append("-tr")
        cargs.append(str(v_11))
    v_12 = params.get("te_value")
    if v_12 is not None:
        cargs.append("-te")
        cargs.append(str(v_12))
    v_13 = params.get("alpha_value")
    if v_13 is not None:
        cargs.append("-alpha")
        cargs.append(str(v_13))
    v_14 = params.get("example_mri_vol")
    if v_14 is not None:
        cargs.append("-example")
        cargs.append(execution.input_file(v_14))
    v_15 = params.get("extra_norm_pctl")
    if v_15 is not None:
        cargs.append("-extra_norm")
        cargs.append(str(v_15))
    v_16 = params.get("prior_threshold")
    if v_16 is not None:
        cargs.append("-prior")
        cargs.append(str(v_16))
    v_17 = params.get("n_regions")
    if v_17 is not None:
        cargs.append("-n")
        cargs.append(str(v_17))
    v_18 = params.get("verbose_value")
    if v_18 is not None:
        cargs.append("-v")
        cargs.append(str(v_18))
    v_19 = params.get("top_percent")
    if v_19 is not None:
        cargs.append("-p")
        cargs.append(str(v_19))
    if params.get("novar_flag"):
        cargs.append("-novar")
    v_20 = params.get("renorm_file")
    if v_20 is not None:
        cargs.append("-renorm")
        cargs.append(execution.input_file(v_20))
    if params.get("flash_flag"):
        cargs.append("-flash")
    return cargs

def mri_ca_normalize_outputs(params: MriCaNormalize, execution: Execution) -> MriCaNormalizeOutputs:
    """Build the MriCaNormalizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    normalized_output_v: OutputPathType = execution.output_file("output.mgh")
    return MriCaNormalizeOutputs(
        root=root_v,
        normalized_output=normalized_output_v,
    )

def mri_ca_normalize_execute(params: MriCaNormalize, runner: Runner | None = None) -> MriCaNormalizeOutputs:
    """
    mri_ca_normalize

    This program creates a normalized volume using the brain volume and an input gca file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_ca_normalize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CA_NORMALIZE_METADATA)
    execution.params(params)
    args = mri_ca_normalize_cargs(params, execution)
    out = mri_ca_normalize_outputs(params, execution)
    execution.run(args)
    return out

def mri_ca_normalize(
    input_brain_volumes: list[InputPathType],
    atlas_file: InputPathType,
    xform_file: InputPathType,
    output_volumes: list[str],
    seg_file: InputPathType | None = None,
    sigma_value: float | None = None,
    fsamples_file: InputPathType | None = None,
    dilate_iters: float | None = None,
    nsamples_file: InputPathType | None = None,
    mask_vol: InputPathType | None = None,
    control_points_file: InputPathType | None = None,
    fonly_file: InputPathType | None = None,
    diag_file: InputPathType | None = None,
    debug_voxel_coords: list[float] | None = None,
    debug_node_coords: list[float] | None = None,
    tr_value: float | None = None,
    te_value: float | None = None,
    alpha_value: float | None = None,
    example_mri_vol: InputPathType | None = None,
    extra_norm_pctl: float | None = None,
    prior_threshold: float | None = None,
    n_regions: float | None = None,
    verbose_value: float | None = None,
    top_percent: float | None = None,
    novar_flag: bool = False,
    renorm_file: InputPathType | None = None,
    flash_flag: bool = False,
    runner: Runner | None = None,
) -> MriCaNormalizeOutputs:
    """
    mri_ca_normalize

    This program creates a normalized volume using the brain volume and an input gca file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_brain_volumes: Input brain volume(s). Can specify multiple inputs.
        atlas_file: Atlas file in GCA format.
        xform_file: Transform file in LTA format.
        output_volumes: Output volume(s) in either mgh or mgz format. Can specify\
            multiple outputs.
        seg_file: Aseg file to help normalization.
        sigma_value: Smoothing sigma for bias field if control points specified\
            (default=4).
        fsamples_file: Write control points to filename.
        dilate_iters: Dilate the brain mask niters times before masking.
        nsamples_file: Write transformed normalization control points to filename.
        mask_vol: Use mri_vol to mask input.
        control_points_file: Define control points from filename.
        fonly_file: Only use control points from filename.
        diag_file: Write to log file.
        debug_voxel_coords: Debug voxel. Needs x, y, z coordinates.
        debug_node_coords: Debug node. Needs x, y, z coordinates.
        tr_value: Set TR in msec.
        te_value: Set TE in msec.
        alpha_value: Set alpha in radians.
        example_mri_vol: Use T1 (mri_vol) and segmentation as example.
        extra_norm_pctl: Use 1+pct and 1-pct to widen the range of T1 values.
        prior_threshold: Use prior threshold t (default=.6).
        n_regions: Use n regions/struct for normalization.
        verbose_value: Used for debugging and diagnostics.
        top_percent: Use top p percent (default=.25) white matter points as control\
            points.
        novar_flag: Do not use variance estimates.
        renorm_file: Renormalize using predicted intensity values in mri_vol.
        flash_flag: Use FLASH forward model to predict intensity values.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_ca_normalize_params(
        input_brain_volumes=input_brain_volumes,
        atlas_file=atlas_file,
        xform_file=xform_file,
        output_volumes=output_volumes,
        seg_file=seg_file,
        sigma_value=sigma_value,
        fsamples_file=fsamples_file,
        dilate_iters=dilate_iters,
        nsamples_file=nsamples_file,
        mask_vol=mask_vol,
        control_points_file=control_points_file,
        fonly_file=fonly_file,
        diag_file=diag_file,
        debug_voxel_coords=debug_voxel_coords,
        debug_node_coords=debug_node_coords,
        tr_value=tr_value,
        te_value=te_value,
        alpha_value=alpha_value,
        example_mri_vol=example_mri_vol,
        extra_norm_pctl=extra_norm_pctl,
        prior_threshold=prior_threshold,
        n_regions=n_regions,
        verbose_value=verbose_value,
        top_percent=top_percent,
        novar_flag=novar_flag,
        renorm_file=renorm_file,
        flash_flag=flash_flag,
    )
    return mri_ca_normalize_execute(params, runner)

__all__ = [
    "MriCaNormalize",
    "MriCaNormalizeOutputs",
    "MRI_CA_NORMALIZE_METADATA",
    "mri_ca_normalize_cargs",
    "mri_ca_normalize_outputs",
    "mri_ca_normalize_params",
    "mri_ca_normalize_execute",
    "mri_ca_normalize_validate",
    "mri_ca_normalize",
]