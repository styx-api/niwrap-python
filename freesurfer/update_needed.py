# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UPDATE_NEEDED_METADATA = Metadata(
    id="UpdateNeeded",
    name="UpdateNeeded",
    package="freesurfer",
)

UpdateNeeded = typing.TypedDict(
    "UpdateNeeded",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/UpdateNeeded"]],
        "target_file": InputPathType,
        "source_file": InputPathType,
        "additional_source_files": typing.NotRequired[list[InputPathType]],
    },
)

@dataclasses.dataclass
class UpdateNeededOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def update_needed_params(
    target_file: InputPathType,
    source_file: InputPathType,
    additional_source_files: list[InputPathType] | None = None,
) -> UpdateNeeded:
    """
    Build parameters.

    Args:
        target_file: The target file that needs to be updated.
        source_file: The primary source file for updating the target file.
        additional_source_files: Additional source files for updating the target\
            file.

    Returns:
        Parameter dictionary.
    """
    params: UpdateNeeded = {
        "@type": "freesurfer/UpdateNeeded",
        "target_file": target_file,
        "source_file": source_file,
    }
    if additional_source_files is not None:
        params["additional_source_files"] = additional_source_files
    return params

def update_needed_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UpdateNeeded."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("target_file", None) is None:
        raise StyxValidationError("`target_file` must not be None")
    if not isinstance(params["target_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_file` has the wrong type: Received `{type(params["target_file"])}` expected `InputPathType`')
    if params.get("source_file", None) is None:
        raise StyxValidationError("`source_file` must not be None")
    if not isinstance(params["source_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source_file` has the wrong type: Received `{type(params["source_file"])}` expected `InputPathType`')
    if params.get("additional_source_files", None) is not None:
        if not isinstance(params["additional_source_files"], list):
            raise StyxValidationError(f'`additional_source_files` has the wrong type: Received `{type(params["additional_source_files"])}` expected `list[InputPathType]`')
        for e in params["additional_source_files"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`additional_source_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def update_needed_cargs(params: UpdateNeeded, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("UpdateNeeded")
    cargs.append(execution.input_file(params["target_file"]))
    cargs.append(execution.input_file(params["source_file"]))
    v_0 = params.get("additional_source_files")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(execution.input_file(item0))
    return cargs

def update_needed_outputs(params: UpdateNeeded, execution: Execution) -> UpdateNeededOutputs:
    """Build the UpdateNeededOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return UpdateNeededOutputs(
        root=root_v,
    )

def update_needed_execute(params: UpdateNeeded, runner: Runner | None = None) -> UpdateNeededOutputs:
    """
    UpdateNeeded

    A command-line tool to update a target file based on one or more source files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    update_needed_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UPDATE_NEEDED_METADATA)
    execution.params(params)
    args = update_needed_cargs(params, execution)
    out = update_needed_outputs(params, execution)
    execution.run(args)
    return out

def update_needed(
    target_file: InputPathType,
    source_file: InputPathType,
    additional_source_files: list[InputPathType] | None = None,
    runner: Runner | None = None,
) -> UpdateNeededOutputs:
    """
    UpdateNeeded

    A command-line tool to update a target file based on one or more source files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        target_file: The target file that needs to be updated.
        source_file: The primary source file for updating the target file.
        additional_source_files: Additional source files for updating the target\
            file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = update_needed_params(
        target_file=target_file,
        source_file=source_file,
        additional_source_files=additional_source_files,
    )
    return update_needed_execute(params, runner)

__all__ = [
    "UpdateNeeded",
    "UpdateNeededOutputs",
    "UPDATE_NEEDED_METADATA",
    "update_needed_cargs",
    "update_needed_outputs",
    "update_needed_params",
    "update_needed_execute",
    "update_needed_validate",
    "update_needed",
]