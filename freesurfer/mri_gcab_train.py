# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GCAB_TRAIN_METADATA = Metadata(
    id="mri_gcab_train",
    name="mri_gcab_train",
    package="freesurfer",
)

MriGcabTrain = typing.TypedDict(
    "MriGcabTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gcab_train"]],
        "removed_info": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriGcabTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_gcab_train_params(
    removed_info: str | None = None,
) -> MriGcabTrain:
    """
    Build parameters.

    Args:
        removed_info: Command has been removed from the current version of\
            FreeSurfer. For further information, please contact the support mailing\
            list.

    Returns:
        Parameter dictionary.
    """
    params: MriGcabTrain = {
        "@type": "freesurfer/mri_gcab_train",
    }
    if removed_info is not None:
        params["removed_info"] = removed_info
    return params

def mri_gcab_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGcabTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("removed_info", None) is not None:
        if not isinstance(params["removed_info"], str):
            raise StyxValidationError(f'`removed_info` has the wrong type: Received `{type(params["removed_info"])}` expected `str`')

def mri_gcab_train_cargs(params: MriGcabTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gcab_train")
    v_0 = params.get("removed_info")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def mri_gcab_train_outputs(params: MriGcabTrain, execution: Execution) -> MriGcabTrainOutputs:
    """Build the MriGcabTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriGcabTrainOutputs(
        root=root_v,
    )

def mri_gcab_train_execute(params: MriGcabTrain, runner: Runner | None = None) -> MriGcabTrainOutputs:
    """
    mri_gcab_train

    Previously used command in FreeSurfer for training with Gaussian Classifier Atlas-based (GCAB) modeling; has been removed in the current version.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gcab_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GCAB_TRAIN_METADATA)
    execution.params(params)
    args = mri_gcab_train_cargs(params, execution)
    out = mri_gcab_train_outputs(params, execution)
    execution.run(args)
    return out

def mri_gcab_train(
    removed_info: str | None = None,
    runner: Runner | None = None,
) -> MriGcabTrainOutputs:
    """
    mri_gcab_train

    Previously used command in FreeSurfer for training with Gaussian Classifier Atlas-based (GCAB) modeling; has been removed in the current version.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        removed_info: Command has been removed from the current version of\
            FreeSurfer. For further information, please contact the support mailing\
            list.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gcab_train_params(
        removed_info=removed_info,
    )
    return mri_gcab_train_execute(params, runner)

__all__ = [
    "MriGcabTrain",
    "MriGcabTrainOutputs",
    "MRI_GCAB_TRAIN_METADATA",
    "mri_gcab_train_cargs",
    "mri_gcab_train_outputs",
    "mri_gcab_train_params",
    "mri_gcab_train_execute",
    "mri_gcab_train_validate",
    "mri_gcab_train",
]