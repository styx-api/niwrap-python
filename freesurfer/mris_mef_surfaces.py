# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MEF_SURFACES_METADATA = Metadata(
    id="mris_mef_surfaces",
    name="mris_mef_surfaces",
    package="freesurfer",
)

MrisMefSurfaces = typing.TypedDict(
    "MrisMefSurfaces",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_mef_surfaces"]],
        "subject_name": str,
        "hemisphere": str,
        "omit_self_intersection": typing.NotRequired[bool],
        "curvature": typing.NotRequired[bool],
        "average_curvature": typing.NotRequired[float],
        "white_only": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisMefSurfacesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_mef_surfaces_params(
    subject_name: str,
    hemisphere: str,
    omit_self_intersection: bool = False,
    curvature: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
) -> MrisMefSurfaces:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject
        hemisphere: Hemisphere (e.g., left or right)
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface.
        curvature: Create curvature and area files from the white matter surface.
        average_curvature: Average curvature values specified number of times,\
            default is 10.
        white_only: Only generate the white matter surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisMefSurfaces = {
        "@type": "freesurfer/mris_mef_surfaces",
        "subject_name": subject_name,
        "hemisphere": hemisphere,
        "omit_self_intersection": omit_self_intersection,
        "curvature": curvature,
        "white_only": white_only,
    }
    if average_curvature is not None:
        params["average_curvature"] = average_curvature
    return params

def mris_mef_surfaces_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMefSurfaces."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("omit_self_intersection", None) is not None:
        if not isinstance(params["omit_self_intersection"], bool):
            raise StyxValidationError(f'`omit_self_intersection` has the wrong type: Received `{type(params["omit_self_intersection"])}` expected `bool`')
    if params.get("curvature", None) is not None:
        if not isinstance(params["curvature"], bool):
            raise StyxValidationError(f'`curvature` has the wrong type: Received `{type(params["curvature"])}` expected `bool`')
    if params.get("average_curvature", None) is not None:
        if not isinstance(params["average_curvature"], (float, int)):
            raise StyxValidationError(f'`average_curvature` has the wrong type: Received `{type(params["average_curvature"])}` expected `float`')
    if params.get("white_only", None) is not None:
        if not isinstance(params["white_only"], bool):
            raise StyxValidationError(f'`white_only` has the wrong type: Received `{type(params["white_only"])}` expected `bool`')

def mris_mef_surfaces_cargs(params: MrisMefSurfaces, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_mef_surfaces")
    cargs.append(params["subject_name"])
    cargs.append(params["hemisphere"])
    if params.get("omit_self_intersection"):
        cargs.append("-q")
    if params.get("curvature"):
        cargs.append("-c")
    v_0 = params.get("average_curvature")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    if params.get("white_only"):
        cargs.append("-whiteonly")
    return cargs

def mris_mef_surfaces_outputs(params: MrisMefSurfaces, execution: Execution) -> MrisMefSurfacesOutputs:
    """Build the MrisMefSurfacesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisMefSurfacesOutputs(
        root=root_v,
    )

def mris_mef_surfaces_execute(params: MrisMefSurfaces, runner: Runner | None = None) -> MrisMefSurfacesOutputs:
    """
    mris_mef_surfaces

    Positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface and generates surface files for these surfaces. Also generates 'curvature' file for cortical thickness and a surface file approximating layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_mef_surfaces_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MEF_SURFACES_METADATA)
    execution.params(params)
    args = mris_mef_surfaces_cargs(params, execution)
    out = mris_mef_surfaces_outputs(params, execution)
    execution.run(args)
    return out

def mris_mef_surfaces(
    subject_name: str,
    hemisphere: str,
    omit_self_intersection: bool = False,
    curvature: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
    runner: Runner | None = None,
) -> MrisMefSurfacesOutputs:
    """
    mris_mef_surfaces

    Positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface and generates surface files for these surfaces. Also generates 'curvature' file for cortical thickness and a surface file approximating layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject
        hemisphere: Hemisphere (e.g., left or right)
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface.
        curvature: Create curvature and area files from the white matter surface.
        average_curvature: Average curvature values specified number of times,\
            default is 10.
        white_only: Only generate the white matter surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_mef_surfaces_params(
        subject_name=subject_name,
        hemisphere=hemisphere,
        omit_self_intersection=omit_self_intersection,
        curvature=curvature,
        average_curvature=average_curvature,
        white_only=white_only,
    )
    return mris_mef_surfaces_execute(params, runner)

__all__ = [
    "MrisMefSurfaces",
    "MrisMefSurfacesOutputs",
    "MRIS_MEF_SURFACES_METADATA",
    "mris_mef_surfaces_cargs",
    "mris_mef_surfaces_outputs",
    "mris_mef_surfaces_params",
    "mris_mef_surfaces_execute",
    "mris_mef_surfaces_validate",
    "mris_mef_surfaces",
]