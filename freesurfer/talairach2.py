# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TALAIRACH2_METADATA = Metadata(
    id="talairach2",
    name="talairach2",
    package="freesurfer",
)

Talairach2 = typing.TypedDict(
    "Talairach2",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/talairach2"]],
        "subject_id": str,
        "mgz_flag": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Talairach2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def talairach2_params(
    subject_id: str,
    mgz_flag: str | None = None,
) -> Talairach2:
    """
    Build parameters.

    Args:
        subject_id: Subject identifier for the talairach transformation.
        mgz_flag: Flag to indicate whether mgz format is used.

    Returns:
        Parameter dictionary.
    """
    params: Talairach2 = {
        "@type": "freesurfer/talairach2",
        "subject_id": subject_id,
    }
    if mgz_flag is not None:
        params["mgz_flag"] = mgz_flag
    return params

def talairach2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Talairach2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("mgz_flag", None) is not None:
        if not isinstance(params["mgz_flag"], str):
            raise StyxValidationError(f'`mgz_flag` has the wrong type: Received `{type(params["mgz_flag"])}` expected `str`')

def talairach2_cargs(params: Talairach2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("talairach2")
    cargs.append(params["subject_id"])
    v_0 = params.get("mgz_flag")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def talairach2_outputs(params: Talairach2, execution: Execution) -> Talairach2Outputs:
    """Build the Talairach2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Talairach2Outputs(
        root=root_v,
    )

def talairach2_execute(params: Talairach2, runner: Runner | None = None) -> Talairach2Outputs:
    """
    talairach2

    Tool for processing and converting talairach transformation files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    talairach2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TALAIRACH2_METADATA)
    execution.params(params)
    args = talairach2_cargs(params, execution)
    out = talairach2_outputs(params, execution)
    execution.run(args)
    return out

def talairach2(
    subject_id: str,
    mgz_flag: str | None = None,
    runner: Runner | None = None,
) -> Talairach2Outputs:
    """
    talairach2

    Tool for processing and converting talairach transformation files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_id: Subject identifier for the talairach transformation.
        mgz_flag: Flag to indicate whether mgz format is used.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = talairach2_params(
        subject_id=subject_id,
        mgz_flag=mgz_flag,
    )
    return talairach2_execute(params, runner)

__all__ = [
    "Talairach2",
    "Talairach2Outputs",
    "TALAIRACH2_METADATA",
    "talairach2_cargs",
    "talairach2_outputs",
    "talairach2_params",
    "talairach2_execute",
    "talairach2_validate",
    "talairach2",
]