# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TABLE2MAP_METADATA = Metadata(
    id="table2map",
    name="table2map",
    package="freesurfer",
)

Table2map = typing.TypedDict(
    "Table2map",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/table2map"]],
        "input_table": InputPathType,
        "output_map": str,
        "segmentation": typing.NotRequired[InputPathType],
        "parcellation": typing.NotRequired[InputPathType],
        "columns": typing.NotRequired[list[str]],
        "lookup_table": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class Table2mapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def table2map_params(
    input_table: InputPathType,
    output_map: str,
    segmentation: InputPathType | None = None,
    parcellation: InputPathType | None = None,
    columns: list[str] | None = None,
    lookup_table: InputPathType | None = None,
) -> Table2map:
    """
    Build parameters.

    Args:
        input_table: Input table.
        output_map: Output map.
        segmentation: Segmentation to map to.
        parcellation: Parcellation to map to.
        columns: Table columns to map. All are included by default.
        lookup_table: Alternative lookup table.

    Returns:
        Parameter dictionary.
    """
    params: Table2map = {
        "@type": "freesurfer/table2map",
        "input_table": input_table,
        "output_map": output_map,
    }
    if segmentation is not None:
        params["segmentation"] = segmentation
    if parcellation is not None:
        params["parcellation"] = parcellation
    if columns is not None:
        params["columns"] = columns
    if lookup_table is not None:
        params["lookup_table"] = lookup_table
    return params

def table2map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Table2map."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_table", None) is None:
        raise StyxValidationError("`input_table` must not be None")
    if not isinstance(params["input_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_table` has the wrong type: Received `{type(params["input_table"])}` expected `InputPathType`')
    if params.get("output_map", None) is None:
        raise StyxValidationError("`output_map` must not be None")
    if not isinstance(params["output_map"], str):
        raise StyxValidationError(f'`output_map` has the wrong type: Received `{type(params["output_map"])}` expected `str`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `InputPathType`')
    if params.get("parcellation", None) is not None:
        if not isinstance(params["parcellation"], (pathlib.Path, str)):
            raise StyxValidationError(f'`parcellation` has the wrong type: Received `{type(params["parcellation"])}` expected `InputPathType`')
    if params.get("columns", None) is not None:
        if not isinstance(params["columns"], list):
            raise StyxValidationError(f'`columns` has the wrong type: Received `{type(params["columns"])}` expected `list[str]`')
        for e in params["columns"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`columns` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("lookup_table", None) is not None:
        if not isinstance(params["lookup_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lookup_table` has the wrong type: Received `{type(params["lookup_table"])}` expected `InputPathType`')

def table2map_cargs(params: Table2map, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("table2map")
    cargs.append("-t")
    cargs.append(execution.input_file(params["input_table"]))
    cargs.append("-o")
    cargs.append(params["output_map"])
    v_0 = params.get("segmentation")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("parcellation")
    if v_1 is not None:
        cargs.append("-p")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("columns")
    if v_2 is not None:
        cargs.append("-c")
        for item0 in v_2:
            cargs.append(item0)
    v_3 = params.get("lookup_table")
    if v_3 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_3))
    return cargs

def table2map_outputs(params: Table2map, execution: Execution) -> Table2mapOutputs:
    """Build the Table2mapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Table2mapOutputs(
        root=root_v,
    )

def table2map_execute(params: Table2map, runner: Runner | None = None) -> Table2mapOutputs:
    """
    table2map

    A tool to map data from a table onto an output map, optionally using segmentation or parcellation files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    table2map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TABLE2MAP_METADATA)
    execution.params(params)
    args = table2map_cargs(params, execution)
    out = table2map_outputs(params, execution)
    execution.run(args)
    return out

def table2map(
    input_table: InputPathType,
    output_map: str,
    segmentation: InputPathType | None = None,
    parcellation: InputPathType | None = None,
    columns: list[str] | None = None,
    lookup_table: InputPathType | None = None,
    runner: Runner | None = None,
) -> Table2mapOutputs:
    """
    table2map

    A tool to map data from a table onto an output map, optionally using segmentation or parcellation files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_table: Input table.
        output_map: Output map.
        segmentation: Segmentation to map to.
        parcellation: Parcellation to map to.
        columns: Table columns to map. All are included by default.
        lookup_table: Alternative lookup table.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = table2map_params(
        input_table=input_table,
        output_map=output_map,
        segmentation=segmentation,
        parcellation=parcellation,
        columns=columns,
        lookup_table=lookup_table,
    )
    return table2map_execute(params, runner)

__all__ = [
    "Table2map",
    "Table2mapOutputs",
    "TABLE2MAP_METADATA",
    "table2map_cargs",
    "table2map_outputs",
    "table2map_params",
    "table2map_execute",
    "table2map_validate",
    "table2map",
]