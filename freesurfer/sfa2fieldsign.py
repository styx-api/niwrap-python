# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SFA2FIELDSIGN_METADATA = Metadata(
    id="sfa2fieldsign",
    name="sfa2fieldsign",
    package="freesurfer",
)

Sfa2fieldsign = typing.TypedDict(
    "Sfa2fieldsign",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/sfa2fieldsign"]],
        "sfadir": str,
        "register_dat": str,
        "threshold": typing.NotRequired[float],
        "fwhm": typing.NotRequired[float],
        "proj_frac": typing.NotRequired[float],
        "occip": typing.NotRequired[bool],
        "patch": typing.NotRequired[str],
        "osd": typing.NotRequired[str],
        "lh": typing.NotRequired[bool],
        "rh": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Sfa2fieldsignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fsig_bin: OutputPathType | None
    """Intersection of polar and eccentricity thresholded fieldsigns"""
    eccen_masked: OutputPathType | None
    """Eccentricity angle (rad) volume masked by fieldsign bin"""
    polar_masked: OutputPathType | None
    """Polar angle (rad) volume masked by fieldsign bin"""
    left_eccen_masked_mgh: OutputPathType | None
    """Masked eccentricity angle sampled on the left hemisphere surface"""
    left_polar_masked_mgh: OutputPathType | None
    """Masked polar angle sampled on the left hemisphere surface"""
    left_fieldsign_masked_mgh: OutputPathType | None
    """Masked fieldsign map on left hemispehre"""
    right_eccen_masked_mgh: OutputPathType | None
    """Masked eccentricity angle sampled on the right hemisphere surface"""
    right_polar_masked_mgh: OutputPathType | None
    """Masked polar angle sampled on the right hemisphere surface"""
    right_fieldsign_masked_mgh: OutputPathType | None
    """Masked fieldsign map on the right hemisphere"""

def sfa2fieldsign_params(
    sfadir: str,
    register_dat: str,
    threshold: float | None = None,
    fwhm: float | None = None,
    proj_frac: float | None = None,
    occip: bool = False,
    patch: str | None = None,
    osd: str | None = None,
    lh: bool = False,
    rh: bool = False,
) -> Sfa2fieldsign:
    """
    Build parameters.

    Args:
        sfadir: Output directory of sfa-sess
        register_dat: Registration file
        threshold: Sigthresh threshold (Default: 2)
        fwhm: Full width at half maximum (FWHM) (Default: 10mm)
        proj_frac: Projection fraction (Default: 0.5)
        occip: Use rh.occip.patch.flat
        patch: Use specific patch (?)h.patch
        osd: Directory under SFA to put output (Default: fieldsign)
        lh: Process left hemisphere only
        rh: Process right hemisphere only

    Returns:
        Parameter dictionary.
    """
    params: Sfa2fieldsign = {
        "@type": "freesurfer/sfa2fieldsign",
        "sfadir": sfadir,
        "register_dat": register_dat,
        "occip": occip,
        "lh": lh,
        "rh": rh,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if fwhm is not None:
        params["fwhm"] = fwhm
    if proj_frac is not None:
        params["proj_frac"] = proj_frac
    if patch is not None:
        params["patch"] = patch
    if osd is not None:
        params["osd"] = osd
    return params

def sfa2fieldsign_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sfa2fieldsign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sfadir", None) is None:
        raise StyxValidationError("`sfadir` must not be None")
    if not isinstance(params["sfadir"], str):
        raise StyxValidationError(f'`sfadir` has the wrong type: Received `{type(params["sfadir"])}` expected `str`')
    if params.get("register_dat", None) is None:
        raise StyxValidationError("`register_dat` must not be None")
    if not isinstance(params["register_dat"], str):
        raise StyxValidationError(f'`register_dat` has the wrong type: Received `{type(params["register_dat"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("proj_frac", None) is not None:
        if not isinstance(params["proj_frac"], (float, int)):
            raise StyxValidationError(f'`proj_frac` has the wrong type: Received `{type(params["proj_frac"])}` expected `float`')
    if params.get("occip", None) is not None:
        if not isinstance(params["occip"], bool):
            raise StyxValidationError(f'`occip` has the wrong type: Received `{type(params["occip"])}` expected `bool`')
    if params.get("patch", None) is not None:
        if not isinstance(params["patch"], str):
            raise StyxValidationError(f'`patch` has the wrong type: Received `{type(params["patch"])}` expected `str`')
    if params.get("osd", None) is not None:
        if not isinstance(params["osd"], str):
            raise StyxValidationError(f'`osd` has the wrong type: Received `{type(params["osd"])}` expected `str`')
    if params.get("lh", None) is not None:
        if not isinstance(params["lh"], bool):
            raise StyxValidationError(f'`lh` has the wrong type: Received `{type(params["lh"])}` expected `bool`')
    if params.get("rh", None) is not None:
        if not isinstance(params["rh"], bool):
            raise StyxValidationError(f'`rh` has the wrong type: Received `{type(params["rh"])}` expected `bool`')

def sfa2fieldsign_cargs(params: Sfa2fieldsign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sfa2fieldsign")
    cargs.append("--sfa")
    cargs.append(params["sfadir"])
    cargs.append("--reg")
    cargs.append(params["register_dat"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_0))
    v_1 = params.get("fwhm")
    if v_1 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_1))
    v_2 = params.get("proj_frac")
    if v_2 is not None:
        cargs.append("--proj-frac")
        cargs.append(str(v_2))
    if params.get("occip"):
        cargs.append("--occip")
    v_3 = params.get("patch")
    if v_3 is not None:
        cargs.append("--patch")
        cargs.append(v_3)
    v_4 = params.get("osd")
    if v_4 is not None:
        cargs.append("--osd")
        cargs.append(v_4)
    if params.get("lh"):
        cargs.append("--lh")
    if params.get("rh"):
        cargs.append("--rh")
    return cargs

def sfa2fieldsign_outputs(params: Sfa2fieldsign, execution: Execution) -> Sfa2fieldsignOutputs:
    """Build the Sfa2fieldsignOutputs object for this tool."""
    fsig_bin_v: OutputPathType | None = None
    eccen_masked_v: OutputPathType | None = None
    polar_masked_v: OutputPathType | None = None
    left_eccen_masked_mgh_v: OutputPathType | None = None
    left_polar_masked_mgh_v: OutputPathType | None = None
    left_fieldsign_masked_mgh_v: OutputPathType | None = None
    right_eccen_masked_mgh_v: OutputPathType | None = None
    right_polar_masked_mgh_v: OutputPathType | None = None
    right_fieldsign_masked_mgh_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("osd")
    if __v0 is not None:
        fsig_bin_v = execution.output_file(f'{params["sfadir"]}/{(__v0 if __v0 is not None else "")}/fsig.bin.nii')
    __v1 = params.get("osd")
    if __v1 is not None:
        eccen_masked_v = execution.output_file(f'{params["sfadir"]}/{(__v1 if __v1 is not None else "")}/eccen.masked.nii')
    __v2 = params.get("osd")
    if __v2 is not None:
        polar_masked_v = execution.output_file(f'{params["sfadir"]}/{(__v2 if __v2 is not None else "")}/polar.masked.nii')
    __v3 = params.get("osd")
    if __v3 is not None:
        left_eccen_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v3 if __v3 is not None else "")}/lh.eccen.masked.mgh')
    __v4 = params.get("osd")
    if __v4 is not None:
        left_polar_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v4 if __v4 is not None else "")}/lh.polar.masked.mgh')
    __v5 = params.get("osd")
    if __v5 is not None:
        left_fieldsign_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v5 if __v5 is not None else "")}/lh.fieldsign.masked.mgh')
    __v6 = params.get("osd")
    if __v6 is not None:
        right_eccen_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v6 if __v6 is not None else "")}/rh.eccen.masked.mgh')
    __v7 = params.get("osd")
    if __v7 is not None:
        right_polar_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v7 if __v7 is not None else "")}/rh.polar.masked.mgh')
    __v8 = params.get("osd")
    if __v8 is not None:
        right_fieldsign_masked_mgh_v = execution.output_file(f'{params["sfadir"]}/{(__v8 if __v8 is not None else "")}/rh.fieldsign.masked.mgh')
    return Sfa2fieldsignOutputs(
        root=root_v,
        fsig_bin=fsig_bin_v,
        eccen_masked=eccen_masked_v,
        polar_masked=polar_masked_v,
        left_eccen_masked_mgh=left_eccen_masked_mgh_v,
        left_polar_masked_mgh=left_polar_masked_mgh_v,
        left_fieldsign_masked_mgh=left_fieldsign_masked_mgh_v,
        right_eccen_masked_mgh=right_eccen_masked_mgh_v,
        right_polar_masked_mgh=right_polar_masked_mgh_v,
        right_fieldsign_masked_mgh=right_fieldsign_masked_mgh_v,
    )

def sfa2fieldsign_execute(params: Sfa2fieldsign, runner: Runner | None = None) -> Sfa2fieldsignOutputs:
    """
    sfa2fieldsign

    Computes fieldsign map from sfa-sess output. Masks the angle volumes and samples them to the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sfa2fieldsign_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SFA2FIELDSIGN_METADATA)
    execution.params(params)
    args = sfa2fieldsign_cargs(params, execution)
    out = sfa2fieldsign_outputs(params, execution)
    execution.run(args)
    return out

def sfa2fieldsign(
    sfadir: str,
    register_dat: str,
    threshold: float | None = None,
    fwhm: float | None = None,
    proj_frac: float | None = None,
    occip: bool = False,
    patch: str | None = None,
    osd: str | None = None,
    lh: bool = False,
    rh: bool = False,
    runner: Runner | None = None,
) -> Sfa2fieldsignOutputs:
    """
    sfa2fieldsign

    Computes fieldsign map from sfa-sess output. Masks the angle volumes and samples them to the surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        sfadir: Output directory of sfa-sess
        register_dat: Registration file
        threshold: Sigthresh threshold (Default: 2)
        fwhm: Full width at half maximum (FWHM) (Default: 10mm)
        proj_frac: Projection fraction (Default: 0.5)
        occip: Use rh.occip.patch.flat
        patch: Use specific patch (?)h.patch
        osd: Directory under SFA to put output (Default: fieldsign)
        lh: Process left hemisphere only
        rh: Process right hemisphere only
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sfa2fieldsign_params(
        sfadir=sfadir,
        register_dat=register_dat,
        threshold=threshold,
        fwhm=fwhm,
        proj_frac=proj_frac,
        occip=occip,
        patch=patch,
        osd=osd,
        lh=lh,
        rh=rh,
    )
    return sfa2fieldsign_execute(params, runner)

__all__ = [
    "Sfa2fieldsign",
    "Sfa2fieldsignOutputs",
    "SFA2FIELDSIGN_METADATA",
    "sfa2fieldsign_cargs",
    "sfa2fieldsign_outputs",
    "sfa2fieldsign_params",
    "sfa2fieldsign_execute",
    "sfa2fieldsign_validate",
    "sfa2fieldsign",
]