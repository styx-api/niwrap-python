# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_NLFILTER_METADATA = Metadata(
    id="mri_nlfilter",
    name="mri_nlfilter",
    package="freesurfer",
)

MriNlfilter = typing.TypedDict(
    "MriNlfilter",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_nlfilter"]],
        "input_image": InputPathType,
        "output_image": str,
        "blur_sigma": typing.NotRequired[float],
        "gaussian_sigma": typing.NotRequired[float],
        "mean_flag": typing.NotRequired[bool],
        "window_size": typing.NotRequired[float],
        "cplov_flag": typing.NotRequired[bool],
        "minmax_flag": typing.NotRequired[bool],
        "no_offsets_flag": typing.NotRequired[bool],
        "no_crop_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriNlfilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The processed image output file."""

def mri_nlfilter_params(
    input_image: InputPathType,
    output_image: str,
    blur_sigma: float | None = None,
    gaussian_sigma: float | None = None,
    mean_flag: bool = False,
    window_size: float | None = None,
    cplov_flag: bool = False,
    minmax_flag: bool = False,
    no_offsets_flag: bool = False,
    no_crop_flag: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
) -> MriNlfilter:
    """
    Build parameters.

    Args:
        input_image: The input image file to be processed.
        output_image: The output image file where the processed image will be\
            saved.
        blur_sigma: Specify sigma of the blurring kernel. Default is 0.500.
        gaussian_sigma: Filter with Gaussian instead of median. Requires sigma\
            value.
        mean_flag: Filter with mean instead of median.
        window_size: Specify window size used for offset calculation. Default is 3.
        cplov_flag: Filter with cplov.
        minmax_flag: Filter with minmax.
        no_offsets_flag: Don't use offsets, just apply standard filters.
        no_crop_flag: Don't crop to >0 region of image.
        version_flag: Display version number.
        help_flag: Display help message.

    Returns:
        Parameter dictionary.
    """
    params: MriNlfilter = {
        "@type": "freesurfer/mri_nlfilter",
        "input_image": input_image,
        "output_image": output_image,
        "mean_flag": mean_flag,
        "cplov_flag": cplov_flag,
        "minmax_flag": minmax_flag,
        "no_offsets_flag": no_offsets_flag,
        "no_crop_flag": no_crop_flag,
        "version_flag": version_flag,
        "help_flag": help_flag,
    }
    if blur_sigma is not None:
        params["blur_sigma"] = blur_sigma
    if gaussian_sigma is not None:
        params["gaussian_sigma"] = gaussian_sigma
    if window_size is not None:
        params["window_size"] = window_size
    return params

def mri_nlfilter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriNlfilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("blur_sigma", None) is not None:
        if not isinstance(params["blur_sigma"], (float, int)):
            raise StyxValidationError(f'`blur_sigma` has the wrong type: Received `{type(params["blur_sigma"])}` expected `float`')
    if params.get("gaussian_sigma", None) is not None:
        if not isinstance(params["gaussian_sigma"], (float, int)):
            raise StyxValidationError(f'`gaussian_sigma` has the wrong type: Received `{type(params["gaussian_sigma"])}` expected `float`')
    if params.get("mean_flag", None) is not None:
        if not isinstance(params["mean_flag"], bool):
            raise StyxValidationError(f'`mean_flag` has the wrong type: Received `{type(params["mean_flag"])}` expected `bool`')
    if params.get("window_size", None) is not None:
        if not isinstance(params["window_size"], (float, int)):
            raise StyxValidationError(f'`window_size` has the wrong type: Received `{type(params["window_size"])}` expected `float`')
    if params.get("cplov_flag", None) is not None:
        if not isinstance(params["cplov_flag"], bool):
            raise StyxValidationError(f'`cplov_flag` has the wrong type: Received `{type(params["cplov_flag"])}` expected `bool`')
    if params.get("minmax_flag", None) is not None:
        if not isinstance(params["minmax_flag"], bool):
            raise StyxValidationError(f'`minmax_flag` has the wrong type: Received `{type(params["minmax_flag"])}` expected `bool`')
    if params.get("no_offsets_flag", None) is not None:
        if not isinstance(params["no_offsets_flag"], bool):
            raise StyxValidationError(f'`no_offsets_flag` has the wrong type: Received `{type(params["no_offsets_flag"])}` expected `bool`')
    if params.get("no_crop_flag", None) is not None:
        if not isinstance(params["no_crop_flag"], bool):
            raise StyxValidationError(f'`no_crop_flag` has the wrong type: Received `{type(params["no_crop_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def mri_nlfilter_cargs(params: MriNlfilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_nlfilter")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    v_0 = params.get("blur_sigma")
    if v_0 is not None:
        cargs.append("-blur")
        cargs.append(str(v_0))
    v_1 = params.get("gaussian_sigma")
    if v_1 is not None:
        cargs.append("-gaussian")
        cargs.append(str(v_1))
    if params.get("mean_flag"):
        cargs.append("-mean")
    v_2 = params.get("window_size")
    if v_2 is not None:
        cargs.append("-w")
        cargs.append(str(v_2))
    if params.get("cplov_flag"):
        cargs.append("-cplov")
    if params.get("minmax_flag"):
        cargs.append("-minmax")
    if params.get("no_offsets_flag"):
        cargs.append("-n")
    if params.get("no_crop_flag"):
        cargs.append("-nc")
    if params.get("version_flag"):
        cargs.append("--version")
    if params.get("help_flag"):
        cargs.append("--help")
    return cargs

def mri_nlfilter_outputs(params: MriNlfilter, execution: Execution) -> MriNlfilterOutputs:
    """Build the MriNlfilterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_image"])
    return MriNlfilterOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_nlfilter_execute(params: MriNlfilter, runner: Runner | None = None) -> MriNlfilterOutputs:
    """
    mri_nlfilter

    This program processes an image using a nonlocal filter and writes the results to an output file. It supports different filtering methods such as median, Gaussian, and mean.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_nlfilter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_NLFILTER_METADATA)
    execution.params(params)
    args = mri_nlfilter_cargs(params, execution)
    out = mri_nlfilter_outputs(params, execution)
    execution.run(args)
    return out

def mri_nlfilter(
    input_image: InputPathType,
    output_image: str,
    blur_sigma: float | None = None,
    gaussian_sigma: float | None = None,
    mean_flag: bool = False,
    window_size: float | None = None,
    cplov_flag: bool = False,
    minmax_flag: bool = False,
    no_offsets_flag: bool = False,
    no_crop_flag: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> MriNlfilterOutputs:
    """
    mri_nlfilter

    This program processes an image using a nonlocal filter and writes the results to an output file. It supports different filtering methods such as median, Gaussian, and mean.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: The input image file to be processed.
        output_image: The output image file where the processed image will be\
            saved.
        blur_sigma: Specify sigma of the blurring kernel. Default is 0.500.
        gaussian_sigma: Filter with Gaussian instead of median. Requires sigma\
            value.
        mean_flag: Filter with mean instead of median.
        window_size: Specify window size used for offset calculation. Default is 3.
        cplov_flag: Filter with cplov.
        minmax_flag: Filter with minmax.
        no_offsets_flag: Don't use offsets, just apply standard filters.
        no_crop_flag: Don't crop to >0 region of image.
        version_flag: Display version number.
        help_flag: Display help message.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_nlfilter_params(
        input_image=input_image,
        output_image=output_image,
        blur_sigma=blur_sigma,
        gaussian_sigma=gaussian_sigma,
        mean_flag=mean_flag,
        window_size=window_size,
        cplov_flag=cplov_flag,
        minmax_flag=minmax_flag,
        no_offsets_flag=no_offsets_flag,
        no_crop_flag=no_crop_flag,
        version_flag=version_flag,
        help_flag=help_flag,
    )
    return mri_nlfilter_execute(params, runner)

__all__ = [
    "MriNlfilter",
    "MriNlfilterOutputs",
    "MRI_NLFILTER_METADATA",
    "mri_nlfilter_cargs",
    "mri_nlfilter_outputs",
    "mri_nlfilter_params",
    "mri_nlfilter_execute",
    "mri_nlfilter_validate",
    "mri_nlfilter",
]