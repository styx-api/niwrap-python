# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSDCMDECOMPRESS_METADATA = Metadata(
    id="fsdcmdecompress",
    name="fsdcmdecompress",
    package="freesurfer",
)

Fsdcmdecompress = typing.TypedDict(
    "Fsdcmdecompress",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsdcmdecompress"]],
        "indcmfile": InputPathType,
        "outdcmfile": str,
        "dcmtk": typing.NotRequired[bool],
        "jpeg": typing.NotRequired[bool],
        "rle": typing.NotRequired[bool],
        "gdcm": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsdcmdecompressOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType
    """The decompressed DICOM file"""

def fsdcmdecompress_params(
    indcmfile: InputPathType,
    outdcmfile: str,
    dcmtk: bool = False,
    jpeg: bool = False,
    rle: bool = False,
    gdcm: bool = False,
) -> Fsdcmdecompress:
    """
    Build parameters.

    Args:
        indcmfile: Input DICOM file to decompress
        outdcmfile: Output decompressed DICOM file
        dcmtk: Use DCMTK for decompression (either dcmdrle.fs or dcmdjpeg.fs)
        jpeg: DICOM is JPEG compressed (ignored without --dcmtk)
        rle: DICOM is RLE compressed (ignored without --dcmtk)
        gdcm: Use GDCM for decompression (default)

    Returns:
        Parameter dictionary.
    """
    params: Fsdcmdecompress = {
        "@type": "freesurfer/fsdcmdecompress",
        "indcmfile": indcmfile,
        "outdcmfile": outdcmfile,
        "dcmtk": dcmtk,
        "jpeg": jpeg,
        "rle": rle,
        "gdcm": gdcm,
    }
    return params

def fsdcmdecompress_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fsdcmdecompress."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("indcmfile", None) is None:
        raise StyxValidationError("`indcmfile` must not be None")
    if not isinstance(params["indcmfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`indcmfile` has the wrong type: Received `{type(params["indcmfile"])}` expected `InputPathType`')
    if params.get("outdcmfile", None) is None:
        raise StyxValidationError("`outdcmfile` must not be None")
    if not isinstance(params["outdcmfile"], str):
        raise StyxValidationError(f'`outdcmfile` has the wrong type: Received `{type(params["outdcmfile"])}` expected `str`')
    if params.get("dcmtk", None) is not None:
        if not isinstance(params["dcmtk"], bool):
            raise StyxValidationError(f'`dcmtk` has the wrong type: Received `{type(params["dcmtk"])}` expected `bool`')
    if params.get("jpeg", None) is not None:
        if not isinstance(params["jpeg"], bool):
            raise StyxValidationError(f'`jpeg` has the wrong type: Received `{type(params["jpeg"])}` expected `bool`')
    if params.get("rle", None) is not None:
        if not isinstance(params["rle"], bool):
            raise StyxValidationError(f'`rle` has the wrong type: Received `{type(params["rle"])}` expected `bool`')
    if params.get("gdcm", None) is not None:
        if not isinstance(params["gdcm"], bool):
            raise StyxValidationError(f'`gdcm` has the wrong type: Received `{type(params["gdcm"])}` expected `bool`')

def fsdcmdecompress_cargs(params: Fsdcmdecompress, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsdcmdecompress")
    cargs.append("--i")
    cargs.append(execution.input_file(params["indcmfile"]))
    cargs.append("--o")
    cargs.append(params["outdcmfile"])
    if params.get("dcmtk"):
        cargs.append("--dcmtk")
    if params.get("jpeg"):
        cargs.append("--jpeg")
    if params.get("rle"):
        cargs.append("--rle")
    if params.get("gdcm"):
        cargs.append("--gdcm")
    return cargs

def fsdcmdecompress_outputs(params: Fsdcmdecompress, execution: Execution) -> FsdcmdecompressOutputs:
    """Build the FsdcmdecompressOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_file_v: OutputPathType = execution.output_file(params["outdcmfile"])
    return FsdcmdecompressOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def fsdcmdecompress_execute(params: Fsdcmdecompress, runner: Runner | None = None) -> FsdcmdecompressOutputs:
    """
    fsdcmdecompress

    A tool for decompressing DICOM files using GDCM or DCMTK.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsdcmdecompress_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSDCMDECOMPRESS_METADATA)
    execution.params(params)
    args = fsdcmdecompress_cargs(params, execution)
    out = fsdcmdecompress_outputs(params, execution)
    execution.run(args)
    return out

def fsdcmdecompress(
    indcmfile: InputPathType,
    outdcmfile: str,
    dcmtk: bool = False,
    jpeg: bool = False,
    rle: bool = False,
    gdcm: bool = False,
    runner: Runner | None = None,
) -> FsdcmdecompressOutputs:
    """
    fsdcmdecompress

    A tool for decompressing DICOM files using GDCM or DCMTK.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        indcmfile: Input DICOM file to decompress
        outdcmfile: Output decompressed DICOM file
        dcmtk: Use DCMTK for decompression (either dcmdrle.fs or dcmdjpeg.fs)
        jpeg: DICOM is JPEG compressed (ignored without --dcmtk)
        rle: DICOM is RLE compressed (ignored without --dcmtk)
        gdcm: Use GDCM for decompression (default)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsdcmdecompress_params(
        indcmfile=indcmfile,
        outdcmfile=outdcmfile,
        dcmtk=dcmtk,
        jpeg=jpeg,
        rle=rle,
        gdcm=gdcm,
    )
    return fsdcmdecompress_execute(params, runner)

__all__ = [
    "Fsdcmdecompress",
    "FsdcmdecompressOutputs",
    "FSDCMDECOMPRESS_METADATA",
    "fsdcmdecompress_cargs",
    "fsdcmdecompress_outputs",
    "fsdcmdecompress_params",
    "fsdcmdecompress_execute",
    "fsdcmdecompress_validate",
    "fsdcmdecompress",
]