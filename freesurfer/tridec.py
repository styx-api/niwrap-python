# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRIDEC_METADATA = Metadata(
    id="tridec",
    name="tridec",
    package="freesurfer",
)

Tridec = typing.TypedDict(
    "Tridec",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tridec"]],
        "subject_name": str,
        "fine_file": InputPathType,
        "ico_file": InputPathType,
        "out_file": str,
    },
)

@dataclasses.dataclass
class TridecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file from tridec processing."""

def tridec_params(
    subject_name: str,
    fine_file: InputPathType,
    ico_file: InputPathType,
    out_file: str,
) -> Tridec:
    """
    Build parameters.

    Args:
        subject_name: Name of the subject being processed.
        fine_file: Fine file input for tridec.
        ico_file: ICO file input for tridec.
        out_file: Output file for tridec processing result.

    Returns:
        Parameter dictionary.
    """
    params: Tridec = {
        "@type": "freesurfer/tridec",
        "subject_name": subject_name,
        "fine_file": fine_file,
        "ico_file": ico_file,
        "out_file": out_file,
    }
    return params

def tridec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tridec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("fine_file", None) is None:
        raise StyxValidationError("`fine_file` must not be None")
    if not isinstance(params["fine_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fine_file` has the wrong type: Received `{type(params["fine_file"])}` expected `InputPathType`')
    if params.get("ico_file", None) is None:
        raise StyxValidationError("`ico_file` must not be None")
    if not isinstance(params["ico_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ico_file` has the wrong type: Received `{type(params["ico_file"])}` expected `InputPathType`')
    if params.get("out_file", None) is None:
        raise StyxValidationError("`out_file` must not be None")
    if not isinstance(params["out_file"], str):
        raise StyxValidationError(f'`out_file` has the wrong type: Received `{type(params["out_file"])}` expected `str`')

def tridec_cargs(params: Tridec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tridec")
    cargs.append(params["subject_name"])
    cargs.append(execution.input_file(params["fine_file"]))
    cargs.append(execution.input_file(params["ico_file"]))
    cargs.append(params["out_file"])
    return cargs

def tridec_outputs(params: Tridec, execution: Execution) -> TridecOutputs:
    """Build the TridecOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["out_file"])
    return TridecOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def tridec_execute(params: Tridec, runner: Runner | None = None) -> TridecOutputs:
    """
    tridec

    Tridec tool for processing brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tridec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRIDEC_METADATA)
    execution.params(params)
    args = tridec_cargs(params, execution)
    out = tridec_outputs(params, execution)
    execution.run(args)
    return out

def tridec(
    subject_name: str,
    fine_file: InputPathType,
    ico_file: InputPathType,
    out_file: str,
    runner: Runner | None = None,
) -> TridecOutputs:
    """
    tridec

    Tridec tool for processing brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: Name of the subject being processed.
        fine_file: Fine file input for tridec.
        ico_file: ICO file input for tridec.
        out_file: Output file for tridec processing result.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tridec_params(
        subject_name=subject_name,
        fine_file=fine_file,
        ico_file=ico_file,
        out_file=out_file,
    )
    return tridec_execute(params, runner)

__all__ = [
    "Tridec",
    "TridecOutputs",
    "TRIDEC_METADATA",
    "tridec_cargs",
    "tridec_outputs",
    "tridec_params",
    "tridec_execute",
    "tridec_validate",
    "tridec",
]