# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_EXTRACT_LARGEST_CC_METADATA = Metadata(
    id="mri_extract_largest_CC",
    name="mri_extract_largest_CC",
    package="freesurfer",
)

MriExtractLargestCcHemisphere = typing.Literal["lh"] | typing.Literal["rh"]

MriExtractLargestCc = typing.TypedDict(
    "MriExtractLargestCc",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_extract_largest_CC"]],
        "input_volume": InputPathType,
        "output_volume": str,
        "threshold": typing.NotRequired[float],
        "hemisphere": typing.NotRequired[MriExtractLargestCcHemisphere],
        "largest_cc_in_bg": typing.NotRequired[bool],
        "original_volume": typing.NotRequired[InputPathType],
        "label_value": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MriExtractLargestCcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """The output volume with the largest connected component extracted."""

def mri_extract_largest_cc_params(
    input_volume: InputPathType,
    output_volume: str,
    threshold: float | None = None,
    hemisphere: MriExtractLargestCcHemisphere | None = None,
    largest_cc_in_bg: bool = False,
    original_volume: InputPathType | None = None,
    label_value: float | None = None,
) -> MriExtractLargestCc:
    """
    Build parameters.

    Args:
        input_volume: Input volume.
        output_volume: Output volume.
        threshold: Threshold for object.
        hemisphere: Set the target value corresponding to lh (255) or rh (127).
        largest_cc_in_bg: Find the largest CC in the background.
        original_volume: Clone values from original volume into output (used with\
            -I).
        label_value: Perform connected components on voxels with specified label\
            value.

    Returns:
        Parameter dictionary.
    """
    params: MriExtractLargestCc = {
        "@type": "freesurfer/mri_extract_largest_CC",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "largest_cc_in_bg": largest_cc_in_bg,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    if original_volume is not None:
        params["original_volume"] = original_volume
    if label_value is not None:
        params["label_value"] = label_value
    return params

def mri_extract_largest_cc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriExtractLargestCc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `MriExtractLargestCcHemisphere`')
        if params["hemisphere"] not in ["lh", "rh"]:
            raise StyxValidationError("Parameter `hemisphere` must be one of [\"lh\", \"rh\"]")
    if params.get("largest_cc_in_bg", None) is not None:
        if not isinstance(params["largest_cc_in_bg"], bool):
            raise StyxValidationError(f'`largest_cc_in_bg` has the wrong type: Received `{type(params["largest_cc_in_bg"])}` expected `bool`')
    if params.get("original_volume", None) is not None:
        if not isinstance(params["original_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`original_volume` has the wrong type: Received `{type(params["original_volume"])}` expected `InputPathType`')
    if params.get("label_value", None) is not None:
        if not isinstance(params["label_value"], (float, int)):
            raise StyxValidationError(f'`label_value` has the wrong type: Received `{type(params["label_value"])}` expected `float`')

def mri_extract_largest_cc_cargs(params: MriExtractLargestCc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_extract_largest_CC")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-T")
        cargs.append(str(v_0))
    v_1 = params.get("hemisphere")
    if v_1 is not None:
        cargs.append("-hemi")
        cargs.append(str(v_1))
    if params.get("largest_cc_in_bg"):
        cargs.append("-I")
    v_2 = params.get("original_volume")
    if v_2 is not None:
        cargs.append("-O")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("label_value")
    if v_3 is not None:
        cargs.append("-L")
        cargs.append(str(v_3))
    return cargs

def mri_extract_largest_cc_outputs(params: MriExtractLargestCc, execution: Execution) -> MriExtractLargestCcOutputs:
    """Build the MriExtractLargestCcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriExtractLargestCcOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_extract_largest_cc_execute(params: MriExtractLargestCc, runner: Runner | None = None) -> MriExtractLargestCcOutputs:
    """
    mri_extract_largest_CC

    This program extracts the largest connected component of the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_extract_largest_cc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_EXTRACT_LARGEST_CC_METADATA)
    execution.params(params)
    args = mri_extract_largest_cc_cargs(params, execution)
    out = mri_extract_largest_cc_outputs(params, execution)
    execution.run(args)
    return out

def mri_extract_largest_cc(
    input_volume: InputPathType,
    output_volume: str,
    threshold: float | None = None,
    hemisphere: MriExtractLargestCcHemisphere | None = None,
    largest_cc_in_bg: bool = False,
    original_volume: InputPathType | None = None,
    label_value: float | None = None,
    runner: Runner | None = None,
) -> MriExtractLargestCcOutputs:
    """
    mri_extract_largest_CC

    This program extracts the largest connected component of the input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume.
        output_volume: Output volume.
        threshold: Threshold for object.
        hemisphere: Set the target value corresponding to lh (255) or rh (127).
        largest_cc_in_bg: Find the largest CC in the background.
        original_volume: Clone values from original volume into output (used with\
            -I).
        label_value: Perform connected components on voxels with specified label\
            value.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_extract_largest_cc_params(
        input_volume=input_volume,
        output_volume=output_volume,
        threshold=threshold,
        hemisphere=hemisphere,
        largest_cc_in_bg=largest_cc_in_bg,
        original_volume=original_volume,
        label_value=label_value,
    )
    return mri_extract_largest_cc_execute(params, runner)

__all__ = [
    "MriExtractLargestCc",
    "MriExtractLargestCcOutputs",
    "MRI_EXTRACT_LARGEST_CC_METADATA",
    "mri_extract_largest_cc_cargs",
    "mri_extract_largest_cc_outputs",
    "mri_extract_largest_cc_params",
    "mri_extract_largest_cc_execute",
    "mri_extract_largest_cc_validate",
    "mri_extract_largest_cc",
]