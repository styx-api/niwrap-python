# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_PARCELLATE_CONNECTIVITY_METADATA = Metadata(
    id="mris_parcellate_connectivity",
    name="mris_parcellate_connectivity",
    package="freesurfer",
)

MrisParcellateConnectivity = typing.TypedDict(
    "MrisParcellateConnectivity",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_parcellate_connectivity"]],
        "smooth_iterations": typing.NotRequired[float],
        "input_surface": InputPathType,
        "input_correlations": InputPathType,
        "output_parcellation": str,
    },
)

@dataclasses.dataclass
class MrisParcellateConnectivityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    parcellation_output: OutputPathType
    """The resulting output parcellation."""

def mris_parcellate_connectivity_params(
    input_surface: InputPathType,
    input_correlations: InputPathType,
    output_parcellation: str,
    smooth_iterations: float | None = None,
) -> MrisParcellateConnectivity:
    """
    Build parameters.

    Args:
        input_surface: Input surface file.
        input_correlations: Input correlations file.
        output_parcellation: Output parcellation file.
        smooth_iterations: Number of averaging iterations for smoothing correlation\
            matrix.

    Returns:
        Parameter dictionary.
    """
    params: MrisParcellateConnectivity = {
        "@type": "freesurfer/mris_parcellate_connectivity",
        "input_surface": input_surface,
        "input_correlations": input_correlations,
        "output_parcellation": output_parcellation,
    }
    if smooth_iterations is not None:
        params["smooth_iterations"] = smooth_iterations
    return params

def mris_parcellate_connectivity_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisParcellateConnectivity."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("smooth_iterations", None) is not None:
        if not isinstance(params["smooth_iterations"], (float, int)):
            raise StyxValidationError(f'`smooth_iterations` has the wrong type: Received `{type(params["smooth_iterations"])}` expected `float`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("input_correlations", None) is None:
        raise StyxValidationError("`input_correlations` must not be None")
    if not isinstance(params["input_correlations"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_correlations` has the wrong type: Received `{type(params["input_correlations"])}` expected `InputPathType`')
    if params.get("output_parcellation", None) is None:
        raise StyxValidationError("`output_parcellation` must not be None")
    if not isinstance(params["output_parcellation"], str):
        raise StyxValidationError(f'`output_parcellation` has the wrong type: Received `{type(params["output_parcellation"])}` expected `str`')

def mris_parcellate_connectivity_cargs(params: MrisParcellateConnectivity, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_parcellate_connectivity")
    v_0 = params.get("smooth_iterations")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(execution.input_file(params["input_correlations"]))
    cargs.append(params["output_parcellation"])
    return cargs

def mris_parcellate_connectivity_outputs(params: MrisParcellateConnectivity, execution: Execution) -> MrisParcellateConnectivityOutputs:
    """Build the MrisParcellateConnectivityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    parcellation_output_v: OutputPathType = execution.output_file(params["output_parcellation"])
    return MrisParcellateConnectivityOutputs(
        root=root_v,
        parcellation_output=parcellation_output_v,
    )

def mris_parcellate_connectivity_execute(params: MrisParcellateConnectivity, runner: Runner | None = None) -> MrisParcellateConnectivityOutputs:
    """
    mris_parcellate_connectivity

    A tool to parcellate brain connectivity using surface and correlation data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_parcellate_connectivity_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_PARCELLATE_CONNECTIVITY_METADATA)
    execution.params(params)
    args = mris_parcellate_connectivity_cargs(params, execution)
    out = mris_parcellate_connectivity_outputs(params, execution)
    execution.run(args)
    return out

def mris_parcellate_connectivity(
    input_surface: InputPathType,
    input_correlations: InputPathType,
    output_parcellation: str,
    smooth_iterations: float | None = None,
    runner: Runner | None = None,
) -> MrisParcellateConnectivityOutputs:
    """
    mris_parcellate_connectivity

    A tool to parcellate brain connectivity using surface and correlation data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file.
        input_correlations: Input correlations file.
        output_parcellation: Output parcellation file.
        smooth_iterations: Number of averaging iterations for smoothing correlation\
            matrix.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_parcellate_connectivity_params(
        input_surface=input_surface,
        input_correlations=input_correlations,
        output_parcellation=output_parcellation,
        smooth_iterations=smooth_iterations,
    )
    return mris_parcellate_connectivity_execute(params, runner)

__all__ = [
    "MrisParcellateConnectivity",
    "MrisParcellateConnectivityOutputs",
    "MRIS_PARCELLATE_CONNECTIVITY_METADATA",
    "mris_parcellate_connectivity_cargs",
    "mris_parcellate_connectivity_outputs",
    "mris_parcellate_connectivity_params",
    "mris_parcellate_connectivity_execute",
    "mris_parcellate_connectivity_validate",
    "mris_parcellate_connectivity",
]