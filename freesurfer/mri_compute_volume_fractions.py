# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_VOLUME_FRACTIONS_METADATA = Metadata(
    id="mri_compute_volume_fractions",
    name="mri_compute_volume_fractions",
    package="freesurfer",
)

MriComputeVolumeFractions = typing.TypedDict(
    "MriComputeVolumeFractions",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_volume_fractions"]],
        "output_stem": str,
        "registration_file": typing.NotRequired[InputPathType],
        "regheader": typing.NotRequired[str],
        "usf": typing.NotRequired[float],
        "resolution": typing.NotRequired[float],
        "resmm": typing.NotRequired[float],
        "segmentation_file": typing.NotRequired[InputPathType],
        "wsurf": typing.NotRequired[str],
        "psurf": typing.NotRequired[str],
        "no_aseg": typing.NotRequired[bool],
        "stackfile": typing.NotRequired[str],
        "gmfile": typing.NotRequired[str],
        "no_fill_csf": typing.NotRequired[bool],
        "dilation": typing.NotRequired[float],
        "out_seg": typing.NotRequired[str],
        "ttseg": typing.NotRequired[str],
        "ttseg_ctab": typing.NotRequired[str],
        "mgz_format": typing.NotRequired[bool],
        "mgh_format": typing.NotRequired[bool],
        "nii_format": typing.NotRequired[bool],
        "nii_gz_format": typing.NotRequired[bool],
        "ttype_head": typing.NotRequired[bool],
        "vg_thresh": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
        "checkopts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriComputeVolumeFractionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_cortex: OutputPathType
    """Output cortex volume file."""
    output_subcort_gm: OutputPathType
    """Output subcortical gray matter volume file."""
    output_wm: OutputPathType
    """Output white matter volume file."""
    output_csf: OutputPathType
    """Output cerebrospinal fluid volume file."""

def mri_compute_volume_fractions_params(
    output_stem: str,
    registration_file: InputPathType | None = None,
    regheader: str | None = None,
    usf: float | None = None,
    resolution: float | None = None,
    resmm: float | None = None,
    segmentation_file: InputPathType | None = None,
    wsurf: str | None = None,
    psurf: str | None = None,
    no_aseg: bool = False,
    stackfile: str | None = None,
    gmfile: str | None = None,
    no_fill_csf: bool = False,
    dilation: float | None = None,
    out_seg: str | None = None,
    ttseg: str | None = None,
    ttseg_ctab: str | None = None,
    mgz_format: bool = False,
    mgh_format: bool = False,
    nii_format: bool = False,
    nii_gz_format: bool = False,
    ttype_head: bool = False,
    vg_thresh: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
) -> MriComputeVolumeFractions:
    """
    Build parameters.

    Args:
        output_stem: Output stem for generated files (e.g. cortex, subcort_gm, wm,\
            csf). Files will be saved with this stem in different formats based on\
            the selected flags.
        registration_file: Registration file (can be LTA or reg.dat). If using\
            reg.dat, a template volume is needed.
        regheader: Specify the subject for regheader.
        usf: Set anatomical upsample factor (default is 2).
        resolution: Resolution setting. Sets USF to round(1/res).
        resmm: Set functional upsampling resolution (default is aseg->xsize/(USF)).
        segmentation_file: Use specified segmentation file instead of aseg.mgz.
        wsurf: Specify the white surface (default is 'white').
        psurf: Specify the pial surface (default is 'pial').
        no_aseg: Do not include aseg in processing (useful for testing).
        stackfile: Put cortex, subcortical GM, WM, CSF into a single multi-frame\
            file.
        gmfile: Put cortex + subcortical GM into a single-frame file.
        no_fill_csf: Do not attempt to fill voxels surrounding segmentation with\
            extracerebral CSF segmentation.
        dilation: For xCSF fill, dilate by specified number (default is 3). Use -1\
            to fill the entire volume.
        out_seg: Save segmentation (after adding xCSF voxels).
        ttseg: Save tissue type segmentation.
        ttseg_ctab: Save tissue type segmentation color table.
        mgz_format: Use MGZ format.
        mgh_format: Use MGH format.
        nii_format: Use NII format.
        nii_gz_format: Use NII.GZ format.
        ttype_head: Use default+head instead of default tissue type info for\
            segmentation.
        vg_thresh: Threshold for 'ERROR: LTAconcat(): LTAs 0 and 1 do not match'.
        debug: Turn on debugging mode.
        checkopts: Do not run anything, just check options and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriComputeVolumeFractions = {
        "@type": "freesurfer/mri_compute_volume_fractions",
        "output_stem": output_stem,
        "no_aseg": no_aseg,
        "no_fill_csf": no_fill_csf,
        "mgz_format": mgz_format,
        "mgh_format": mgh_format,
        "nii_format": nii_format,
        "nii_gz_format": nii_gz_format,
        "ttype_head": ttype_head,
        "debug": debug,
        "checkopts": checkopts,
    }
    if registration_file is not None:
        params["registration_file"] = registration_file
    if regheader is not None:
        params["regheader"] = regheader
    if usf is not None:
        params["usf"] = usf
    if resolution is not None:
        params["resolution"] = resolution
    if resmm is not None:
        params["resmm"] = resmm
    if segmentation_file is not None:
        params["segmentation_file"] = segmentation_file
    if wsurf is not None:
        params["wsurf"] = wsurf
    if psurf is not None:
        params["psurf"] = psurf
    if stackfile is not None:
        params["stackfile"] = stackfile
    if gmfile is not None:
        params["gmfile"] = gmfile
    if dilation is not None:
        params["dilation"] = dilation
    if out_seg is not None:
        params["out_seg"] = out_seg
    if ttseg is not None:
        params["ttseg"] = ttseg
    if ttseg_ctab is not None:
        params["ttseg_ctab"] = ttseg_ctab
    if vg_thresh is not None:
        params["vg_thresh"] = vg_thresh
    return params

def mri_compute_volume_fractions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeVolumeFractions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_stem", None) is None:
        raise StyxValidationError("`output_stem` must not be None")
    if not isinstance(params["output_stem"], str):
        raise StyxValidationError(f'`output_stem` has the wrong type: Received `{type(params["output_stem"])}` expected `str`')
    if params.get("registration_file", None) is not None:
        if not isinstance(params["registration_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("regheader", None) is not None:
        if not isinstance(params["regheader"], str):
            raise StyxValidationError(f'`regheader` has the wrong type: Received `{type(params["regheader"])}` expected `str`')
    if params.get("usf", None) is not None:
        if not isinstance(params["usf"], (float, int)):
            raise StyxValidationError(f'`usf` has the wrong type: Received `{type(params["usf"])}` expected `float`')
    if params.get("resolution", None) is not None:
        if not isinstance(params["resolution"], (float, int)):
            raise StyxValidationError(f'`resolution` has the wrong type: Received `{type(params["resolution"])}` expected `float`')
    if params.get("resmm", None) is not None:
        if not isinstance(params["resmm"], (float, int)):
            raise StyxValidationError(f'`resmm` has the wrong type: Received `{type(params["resmm"])}` expected `float`')
    if params.get("segmentation_file", None) is not None:
        if not isinstance(params["segmentation_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`segmentation_file` has the wrong type: Received `{type(params["segmentation_file"])}` expected `InputPathType`')
    if params.get("wsurf", None) is not None:
        if not isinstance(params["wsurf"], str):
            raise StyxValidationError(f'`wsurf` has the wrong type: Received `{type(params["wsurf"])}` expected `str`')
    if params.get("psurf", None) is not None:
        if not isinstance(params["psurf"], str):
            raise StyxValidationError(f'`psurf` has the wrong type: Received `{type(params["psurf"])}` expected `str`')
    if params.get("no_aseg", None) is not None:
        if not isinstance(params["no_aseg"], bool):
            raise StyxValidationError(f'`no_aseg` has the wrong type: Received `{type(params["no_aseg"])}` expected `bool`')
    if params.get("stackfile", None) is not None:
        if not isinstance(params["stackfile"], str):
            raise StyxValidationError(f'`stackfile` has the wrong type: Received `{type(params["stackfile"])}` expected `str`')
    if params.get("gmfile", None) is not None:
        if not isinstance(params["gmfile"], str):
            raise StyxValidationError(f'`gmfile` has the wrong type: Received `{type(params["gmfile"])}` expected `str`')
    if params.get("no_fill_csf", None) is not None:
        if not isinstance(params["no_fill_csf"], bool):
            raise StyxValidationError(f'`no_fill_csf` has the wrong type: Received `{type(params["no_fill_csf"])}` expected `bool`')
    if params.get("dilation", None) is not None:
        if not isinstance(params["dilation"], (float, int)):
            raise StyxValidationError(f'`dilation` has the wrong type: Received `{type(params["dilation"])}` expected `float`')
    if params.get("out_seg", None) is not None:
        if not isinstance(params["out_seg"], str):
            raise StyxValidationError(f'`out_seg` has the wrong type: Received `{type(params["out_seg"])}` expected `str`')
    if params.get("ttseg", None) is not None:
        if not isinstance(params["ttseg"], str):
            raise StyxValidationError(f'`ttseg` has the wrong type: Received `{type(params["ttseg"])}` expected `str`')
    if params.get("ttseg_ctab", None) is not None:
        if not isinstance(params["ttseg_ctab"], str):
            raise StyxValidationError(f'`ttseg_ctab` has the wrong type: Received `{type(params["ttseg_ctab"])}` expected `str`')
    if params.get("mgz_format", None) is not None:
        if not isinstance(params["mgz_format"], bool):
            raise StyxValidationError(f'`mgz_format` has the wrong type: Received `{type(params["mgz_format"])}` expected `bool`')
    if params.get("mgh_format", None) is not None:
        if not isinstance(params["mgh_format"], bool):
            raise StyxValidationError(f'`mgh_format` has the wrong type: Received `{type(params["mgh_format"])}` expected `bool`')
    if params.get("nii_format", None) is not None:
        if not isinstance(params["nii_format"], bool):
            raise StyxValidationError(f'`nii_format` has the wrong type: Received `{type(params["nii_format"])}` expected `bool`')
    if params.get("nii_gz_format", None) is not None:
        if not isinstance(params["nii_gz_format"], bool):
            raise StyxValidationError(f'`nii_gz_format` has the wrong type: Received `{type(params["nii_gz_format"])}` expected `bool`')
    if params.get("ttype_head", None) is not None:
        if not isinstance(params["ttype_head"], bool):
            raise StyxValidationError(f'`ttype_head` has the wrong type: Received `{type(params["ttype_head"])}` expected `bool`')
    if params.get("vg_thresh", None) is not None:
        if not isinstance(params["vg_thresh"], (float, int)):
            raise StyxValidationError(f'`vg_thresh` has the wrong type: Received `{type(params["vg_thresh"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("checkopts", None) is not None:
        if not isinstance(params["checkopts"], bool):
            raise StyxValidationError(f'`checkopts` has the wrong type: Received `{type(params["checkopts"])}` expected `bool`')

def mri_compute_volume_fractions_cargs(params: MriComputeVolumeFractions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_volume_fractions")
    cargs.append("--o")
    cargs.append(params["output_stem"])
    v_0 = params.get("registration_file")
    if v_0 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("regheader")
    if v_1 is not None:
        cargs.append("--regheader")
        cargs.append(v_1)
    v_2 = params.get("usf")
    if v_2 is not None:
        cargs.append("--usf")
        cargs.append(str(v_2))
    v_3 = params.get("resolution")
    if v_3 is not None:
        cargs.append("--r")
        cargs.append(str(v_3))
    v_4 = params.get("resmm")
    if v_4 is not None:
        cargs.append("--resmm")
        cargs.append(str(v_4))
    v_5 = params.get("segmentation_file")
    if v_5 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("wsurf")
    if v_6 is not None:
        cargs.append("--wsurf")
        cargs.append(v_6)
    v_7 = params.get("psurf")
    if v_7 is not None:
        cargs.append("--psurf")
        cargs.append(v_7)
    if params.get("no_aseg"):
        cargs.append("--no-aseg")
    v_8 = params.get("stackfile")
    if v_8 is not None:
        cargs.append("--stack")
        cargs.append(v_8)
    v_9 = params.get("gmfile")
    if v_9 is not None:
        cargs.append("--gm")
        cargs.append(v_9)
    if params.get("no_fill_csf"):
        cargs.append("--no-fill-csf")
    v_10 = params.get("dilation")
    if v_10 is not None:
        cargs.append("--dil")
        cargs.append(str(v_10))
    v_11 = params.get("out_seg")
    if v_11 is not None:
        cargs.append("--out-seg")
        cargs.append(v_11)
    v_12 = params.get("ttseg")
    if v_12 is not None:
        cargs.append("--ttseg")
        cargs.append(v_12)
    v_13 = params.get("ttseg_ctab")
    if v_13 is not None:
        cargs.append("--ttseg-ctab")
        cargs.append(v_13)
    if params.get("mgz_format"):
        cargs.append("--mgz")
    if params.get("mgh_format"):
        cargs.append("--mgh")
    if params.get("nii_format"):
        cargs.append("--nii")
    if params.get("nii_gz_format"):
        cargs.append("--nii.gz")
    if params.get("ttype_head"):
        cargs.append("--ttype+head")
    v_14 = params.get("vg_thresh")
    if v_14 is not None:
        cargs.append("--vg-thresh")
        cargs.append(str(v_14))
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("checkopts"):
        cargs.append("--checkopts")
    return cargs

def mri_compute_volume_fractions_outputs(params: MriComputeVolumeFractions, execution: Execution) -> MriComputeVolumeFractionsOutputs:
    """Build the MriComputeVolumeFractionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_cortex_v: OutputPathType = execution.output_file(f'{params["output_stem"]}.cortex.mgz')
    output_subcort_gm_v: OutputPathType = execution.output_file(f'{params["output_stem"]}.subcort_gm.mgz')
    output_wm_v: OutputPathType = execution.output_file(f'{params["output_stem"]}.wm.mgz')
    output_csf_v: OutputPathType = execution.output_file(f'{params["output_stem"]}.csf.mgz')
    return MriComputeVolumeFractionsOutputs(
        root=root_v,
        output_cortex=output_cortex_v,
        output_subcort_gm=output_subcort_gm_v,
        output_wm=output_wm_v,
        output_csf=output_csf_v,
    )

def mri_compute_volume_fractions_execute(params: MriComputeVolumeFractions, runner: Runner | None = None) -> MriComputeVolumeFractionsOutputs:
    """
    mri_compute_volume_fractions

    Computes partial volume fractions for cortex, subcortical GM, WM and CSF

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_volume_fractions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_VOLUME_FRACTIONS_METADATA)
    execution.params(params)
    args = mri_compute_volume_fractions_cargs(params, execution)
    out = mri_compute_volume_fractions_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_volume_fractions(
    output_stem: str,
    registration_file: InputPathType | None = None,
    regheader: str | None = None,
    usf: float | None = None,
    resolution: float | None = None,
    resmm: float | None = None,
    segmentation_file: InputPathType | None = None,
    wsurf: str | None = None,
    psurf: str | None = None,
    no_aseg: bool = False,
    stackfile: str | None = None,
    gmfile: str | None = None,
    no_fill_csf: bool = False,
    dilation: float | None = None,
    out_seg: str | None = None,
    ttseg: str | None = None,
    ttseg_ctab: str | None = None,
    mgz_format: bool = False,
    mgh_format: bool = False,
    nii_format: bool = False,
    nii_gz_format: bool = False,
    ttype_head: bool = False,
    vg_thresh: float | None = None,
    debug: bool = False,
    checkopts: bool = False,
    runner: Runner | None = None,
) -> MriComputeVolumeFractionsOutputs:
    """
    mri_compute_volume_fractions

    Computes partial volume fractions for cortex, subcortical GM, WM and CSF

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_stem: Output stem for generated files (e.g. cortex, subcort_gm, wm,\
            csf). Files will be saved with this stem in different formats based on\
            the selected flags.
        registration_file: Registration file (can be LTA or reg.dat). If using\
            reg.dat, a template volume is needed.
        regheader: Specify the subject for regheader.
        usf: Set anatomical upsample factor (default is 2).
        resolution: Resolution setting. Sets USF to round(1/res).
        resmm: Set functional upsampling resolution (default is aseg->xsize/(USF)).
        segmentation_file: Use specified segmentation file instead of aseg.mgz.
        wsurf: Specify the white surface (default is 'white').
        psurf: Specify the pial surface (default is 'pial').
        no_aseg: Do not include aseg in processing (useful for testing).
        stackfile: Put cortex, subcortical GM, WM, CSF into a single multi-frame\
            file.
        gmfile: Put cortex + subcortical GM into a single-frame file.
        no_fill_csf: Do not attempt to fill voxels surrounding segmentation with\
            extracerebral CSF segmentation.
        dilation: For xCSF fill, dilate by specified number (default is 3). Use -1\
            to fill the entire volume.
        out_seg: Save segmentation (after adding xCSF voxels).
        ttseg: Save tissue type segmentation.
        ttseg_ctab: Save tissue type segmentation color table.
        mgz_format: Use MGZ format.
        mgh_format: Use MGH format.
        nii_format: Use NII format.
        nii_gz_format: Use NII.GZ format.
        ttype_head: Use default+head instead of default tissue type info for\
            segmentation.
        vg_thresh: Threshold for 'ERROR: LTAconcat(): LTAs 0 and 1 do not match'.
        debug: Turn on debugging mode.
        checkopts: Do not run anything, just check options and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_volume_fractions_params(
        output_stem=output_stem,
        registration_file=registration_file,
        regheader=regheader,
        usf=usf,
        resolution=resolution,
        resmm=resmm,
        segmentation_file=segmentation_file,
        wsurf=wsurf,
        psurf=psurf,
        no_aseg=no_aseg,
        stackfile=stackfile,
        gmfile=gmfile,
        no_fill_csf=no_fill_csf,
        dilation=dilation,
        out_seg=out_seg,
        ttseg=ttseg,
        ttseg_ctab=ttseg_ctab,
        mgz_format=mgz_format,
        mgh_format=mgh_format,
        nii_format=nii_format,
        nii_gz_format=nii_gz_format,
        ttype_head=ttype_head,
        vg_thresh=vg_thresh,
        debug=debug,
        checkopts=checkopts,
    )
    return mri_compute_volume_fractions_execute(params, runner)

__all__ = [
    "MriComputeVolumeFractions",
    "MriComputeVolumeFractionsOutputs",
    "MRI_COMPUTE_VOLUME_FRACTIONS_METADATA",
    "mri_compute_volume_fractions_cargs",
    "mri_compute_volume_fractions_outputs",
    "mri_compute_volume_fractions_params",
    "mri_compute_volume_fractions_execute",
    "mri_compute_volume_fractions_validate",
    "mri_compute_volume_fractions",
]