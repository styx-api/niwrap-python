# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_SAVE_HISTOGRAMS_METADATA = Metadata(
    id="dmri_saveHistograms",
    name="dmri_saveHistograms",
    package="freesurfer",
)

DmriSaveHistograms = typing.TypedDict(
    "DmriSaveHistograms",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_saveHistograms"]],
        "parcellation": InputPathType,
        "number_of_bundles": float,
        "vtk_bundle_list": list[InputPathType],
        "output_csv": str,
        "brain_bundle_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DmriSaveHistogramsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    histogram_csv: OutputPathType
    """CSV file containing the output histograms."""

def dmri_save_histograms_params(
    parcellation: InputPathType,
    number_of_bundles: float,
    vtk_bundle_list: list[InputPathType],
    output_csv: str,
    brain_bundle_flag: bool = False,
) -> DmriSaveHistograms:
    """
    Build parameters.

    Args:
        parcellation: Parcellation file for the tractography data.
        number_of_bundles: Number of bundles in the tractography data.
        vtk_bundle_list: List of VTK bundles for creating histograms.
        output_csv: Output CSV file to save histograms.
        brain_bundle_flag: Brain Bundle flag.

    Returns:
        Parameter dictionary.
    """
    params: DmriSaveHistograms = {
        "@type": "freesurfer/dmri_saveHistograms",
        "parcellation": parcellation,
        "number_of_bundles": number_of_bundles,
        "vtk_bundle_list": vtk_bundle_list,
        "output_csv": output_csv,
        "brain_bundle_flag": brain_bundle_flag,
    }
    return params

def dmri_save_histograms_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriSaveHistograms."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("parcellation", None) is None:
        raise StyxValidationError("`parcellation` must not be None")
    if not isinstance(params["parcellation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`parcellation` has the wrong type: Received `{type(params["parcellation"])}` expected `InputPathType`')
    if params.get("number_of_bundles", None) is None:
        raise StyxValidationError("`number_of_bundles` must not be None")
    if not isinstance(params["number_of_bundles"], (float, int)):
        raise StyxValidationError(f'`number_of_bundles` has the wrong type: Received `{type(params["number_of_bundles"])}` expected `float`')
    if params.get("vtk_bundle_list", None) is None:
        raise StyxValidationError("`vtk_bundle_list` must not be None")
    if not isinstance(params["vtk_bundle_list"], list):
        raise StyxValidationError(f'`vtk_bundle_list` has the wrong type: Received `{type(params["vtk_bundle_list"])}` expected `list[InputPathType]`')
    for e in params["vtk_bundle_list"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`vtk_bundle_list` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_csv", None) is None:
        raise StyxValidationError("`output_csv` must not be None")
    if not isinstance(params["output_csv"], str):
        raise StyxValidationError(f'`output_csv` has the wrong type: Received `{type(params["output_csv"])}` expected `str`')
    if params.get("brain_bundle_flag", None) is not None:
        if not isinstance(params["brain_bundle_flag"], bool):
            raise StyxValidationError(f'`brain_bundle_flag` has the wrong type: Received `{type(params["brain_bundle_flag"])}` expected `bool`')

def dmri_save_histograms_cargs(params: DmriSaveHistograms, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_saveHistograms")
    cargs.append("-p")
    cargs.append(execution.input_file(params["parcellation"]))
    cargs.append("-f")
    cargs.append(str(params["number_of_bundles"]))
    for item0 in params["vtk_bundle_list"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-o")
    cargs.append(params["output_csv"])
    if params.get("brain_bundle_flag"):
        cargs.append("-bb")
    return cargs

def dmri_save_histograms_outputs(params: DmriSaveHistograms, execution: Execution) -> DmriSaveHistogramsOutputs:
    """Build the DmriSaveHistogramsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    histogram_csv_v: OutputPathType = execution.output_file("histograms.csv")
    return DmriSaveHistogramsOutputs(
        root=root_v,
        histogram_csv=histogram_csv_v,
    )

def dmri_save_histograms_execute(params: DmriSaveHistograms, runner: Runner | None = None) -> DmriSaveHistogramsOutputs:
    """
    dmri_saveHistograms

    A tool to save histograms from diffusion MRI tractography data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_save_histograms_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_SAVE_HISTOGRAMS_METADATA)
    execution.params(params)
    args = dmri_save_histograms_cargs(params, execution)
    out = dmri_save_histograms_outputs(params, execution)
    execution.run(args)
    return out

def dmri_save_histograms(
    parcellation: InputPathType,
    number_of_bundles: float,
    vtk_bundle_list: list[InputPathType],
    output_csv: str,
    brain_bundle_flag: bool = False,
    runner: Runner | None = None,
) -> DmriSaveHistogramsOutputs:
    """
    dmri_saveHistograms

    A tool to save histograms from diffusion MRI tractography data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        parcellation: Parcellation file for the tractography data.
        number_of_bundles: Number of bundles in the tractography data.
        vtk_bundle_list: List of VTK bundles for creating histograms.
        output_csv: Output CSV file to save histograms.
        brain_bundle_flag: Brain Bundle flag.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_save_histograms_params(
        parcellation=parcellation,
        number_of_bundles=number_of_bundles,
        vtk_bundle_list=vtk_bundle_list,
        output_csv=output_csv,
        brain_bundle_flag=brain_bundle_flag,
    )
    return dmri_save_histograms_execute(params, runner)

__all__ = [
    "DmriSaveHistograms",
    "DmriSaveHistogramsOutputs",
    "DMRI_SAVE_HISTOGRAMS_METADATA",
    "dmri_save_histograms_cargs",
    "dmri_save_histograms_outputs",
    "dmri_save_histograms_params",
    "dmri_save_histograms_execute",
    "dmri_save_histograms_validate",
    "dmri_save_histograms",
]