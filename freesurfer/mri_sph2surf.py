# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SPH2SURF_METADATA = Metadata(
    id="mri-sph2surf",
    name="mri-sph2surf",
    package="freesurfer",
)

MriSph2surf = typing.TypedDict(
    "MriSph2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri-sph2surf"]],
        "instem": str,
        "outstem": str,
        "hemi": str,
        "subject": str,
        "offset": typing.NotRequired[float],
        "svitdir": typing.NotRequired[str],
        "umask": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSph2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output surface data file in the form outstem-lh.w (or rh)."""

def mri_sph2surf_params(
    instem: str,
    outstem: str,
    hemi: str,
    subject: str,
    offset: float | None = None,
    svitdir: str | None = None,
    umask: str | None = None,
    verbose: bool = False,
    version: bool = False,
) -> MriSph2surf:
    """
    Build parameters.

    Args:
        instem: Input stem of a bfloat file. The full input file name must take the\
            form instem-lh_000.bfloat (or rh).
        outstem: Output stem for the resulting file. The output file will have the\
            name outstem-lh.w (or rh).
        hemi: Specifies the hemisphere for processing. Acceptable values are 'lh'\
            or 'rh'.
        subject: Specifies the subject identifier for the FreeSurfer processing\
            pipeline.
        offset: Zero-based plane/frame number. Default is 0.
        svitdir: Directory for svit. Default is\
            '/usr/local/freesurfer/subjects/subject/svit'.
        umask: Specifies a new user mask.
        verbose: Enable verbose output.
        version: Show version information.

    Returns:
        Parameter dictionary.
    """
    params: MriSph2surf = {
        "@type": "freesurfer/mri-sph2surf",
        "instem": instem,
        "outstem": outstem,
        "hemi": hemi,
        "subject": subject,
        "verbose": verbose,
        "version": version,
    }
    if offset is not None:
        params["offset"] = offset
    if svitdir is not None:
        params["svitdir"] = svitdir
    if umask is not None:
        params["umask"] = umask
    return params

def mri_sph2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSph2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("instem", None) is None:
        raise StyxValidationError("`instem` must not be None")
    if not isinstance(params["instem"], str):
        raise StyxValidationError(f'`instem` has the wrong type: Received `{type(params["instem"])}` expected `str`')
    if params.get("outstem", None) is None:
        raise StyxValidationError("`outstem` must not be None")
    if not isinstance(params["outstem"], str):
        raise StyxValidationError(f'`outstem` has the wrong type: Received `{type(params["outstem"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("offset", None) is not None:
        if not isinstance(params["offset"], (float, int)):
            raise StyxValidationError(f'`offset` has the wrong type: Received `{type(params["offset"])}` expected `float`')
    if params.get("svitdir", None) is not None:
        if not isinstance(params["svitdir"], str):
            raise StyxValidationError(f'`svitdir` has the wrong type: Received `{type(params["svitdir"])}` expected `str`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_sph2surf_cargs(params: MriSph2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri-sph2surf")
    cargs.append("-i")
    cargs.append(params["instem"])
    cargs.append("-o")
    cargs.append(params["outstem"])
    cargs.append("-hemi")
    cargs.append(params["hemi"])
    cargs.append("-s")
    cargs.append(params["subject"])
    v_0 = params.get("offset")
    if v_0 is not None:
        cargs.append("-offset")
        cargs.append(str(v_0))
    v_1 = params.get("svitdir")
    if v_1 is not None:
        cargs.append("-svitdir")
        cargs.append(v_1)
    v_2 = params.get("umask")
    if v_2 is not None:
        cargs.append("-umask")
        cargs.append(v_2)
    if params.get("verbose"):
        cargs.append("-verbose")
    if params.get("version"):
        cargs.append("-version")
    return cargs

def mri_sph2surf_outputs(params: MriSph2surf, execution: Execution) -> MriSph2surfOutputs:
    """Build the MriSph2surfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["outstem"]}-{params["hemi"]}.w')
    return MriSph2surfOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_sph2surf_execute(params: MriSph2surf, runner: Runner | None = None) -> MriSph2surfOutputs:
    """
    mri-sph2surf

    Converts spherical functional data to surface data in the FreeSurfer processing pipeline.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_sph2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SPH2SURF_METADATA)
    execution.params(params)
    args = mri_sph2surf_cargs(params, execution)
    out = mri_sph2surf_outputs(params, execution)
    execution.run(args)
    return out

def mri_sph2surf(
    instem: str,
    outstem: str,
    hemi: str,
    subject: str,
    offset: float | None = None,
    svitdir: str | None = None,
    umask: str | None = None,
    verbose: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriSph2surfOutputs:
    """
    mri-sph2surf

    Converts spherical functional data to surface data in the FreeSurfer processing pipeline.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        instem: Input stem of a bfloat file. The full input file name must take the\
            form instem-lh_000.bfloat (or rh).
        outstem: Output stem for the resulting file. The output file will have the\
            name outstem-lh.w (or rh).
        hemi: Specifies the hemisphere for processing. Acceptable values are 'lh'\
            or 'rh'.
        subject: Specifies the subject identifier for the FreeSurfer processing\
            pipeline.
        offset: Zero-based plane/frame number. Default is 0.
        svitdir: Directory for svit. Default is\
            '/usr/local/freesurfer/subjects/subject/svit'.
        umask: Specifies a new user mask.
        verbose: Enable verbose output.
        version: Show version information.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_sph2surf_params(
        instem=instem,
        outstem=outstem,
        hemi=hemi,
        subject=subject,
        offset=offset,
        svitdir=svitdir,
        umask=umask,
        verbose=verbose,
        version=version,
    )
    return mri_sph2surf_execute(params, runner)

__all__ = [
    "MriSph2surf",
    "MriSph2surfOutputs",
    "MRI_SPH2SURF_METADATA",
    "mri_sph2surf_cargs",
    "mri_sph2surf_outputs",
    "mri_sph2surf_params",
    "mri_sph2surf_execute",
    "mri_sph2surf_validate",
    "mri_sph2surf",
]