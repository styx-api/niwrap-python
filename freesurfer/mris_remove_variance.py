# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_REMOVE_VARIANCE_METADATA = Metadata(
    id="mris_remove_variance",
    name="mris_remove_variance",
    package="freesurfer",
)

MrisRemoveVariance = typing.TypedDict(
    "MrisRemoveVariance",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_remove_variance"]],
        "input_surface_file": InputPathType,
        "curvature_file": InputPathType,
        "curvature_file_to_remove": InputPathType,
        "output_curvature_file": str,
    },
)

@dataclasses.dataclass
class MrisRemoveVarianceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resulting_curvature_file: OutputPathType
    """The resulting curvature file after removing variance."""

def mris_remove_variance_params(
    input_surface_file: InputPathType,
    curvature_file: InputPathType,
    curvature_file_to_remove: InputPathType,
    output_curvature_file: str,
) -> MrisRemoveVariance:
    """
    Build parameters.

    Args:
        input_surface_file: The input surface file.
        curvature_file: The curvature file.
        curvature_file_to_remove: The curvature file to remove from the input\
            curvature.
        output_curvature_file: The output curvature file.

    Returns:
        Parameter dictionary.
    """
    params: MrisRemoveVariance = {
        "@type": "freesurfer/mris_remove_variance",
        "input_surface_file": input_surface_file,
        "curvature_file": curvature_file,
        "curvature_file_to_remove": curvature_file_to_remove,
        "output_curvature_file": output_curvature_file,
    }
    return params

def mris_remove_variance_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisRemoveVariance."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface_file", None) is None:
        raise StyxValidationError("`input_surface_file` must not be None")
    if not isinstance(params["input_surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface_file` has the wrong type: Received `{type(params["input_surface_file"])}` expected `InputPathType`')
    if params.get("curvature_file", None) is None:
        raise StyxValidationError("`curvature_file` must not be None")
    if not isinstance(params["curvature_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`curvature_file` has the wrong type: Received `{type(params["curvature_file"])}` expected `InputPathType`')
    if params.get("curvature_file_to_remove", None) is None:
        raise StyxValidationError("`curvature_file_to_remove` must not be None")
    if not isinstance(params["curvature_file_to_remove"], (pathlib.Path, str)):
        raise StyxValidationError(f'`curvature_file_to_remove` has the wrong type: Received `{type(params["curvature_file_to_remove"])}` expected `InputPathType`')
    if params.get("output_curvature_file", None) is None:
        raise StyxValidationError("`output_curvature_file` must not be None")
    if not isinstance(params["output_curvature_file"], str):
        raise StyxValidationError(f'`output_curvature_file` has the wrong type: Received `{type(params["output_curvature_file"])}` expected `str`')

def mris_remove_variance_cargs(params: MrisRemoveVariance, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_remove_variance")
    cargs.append(execution.input_file(params["input_surface_file"]))
    cargs.append(execution.input_file(params["curvature_file"]))
    cargs.append(execution.input_file(params["curvature_file_to_remove"]))
    cargs.append(params["output_curvature_file"])
    return cargs

def mris_remove_variance_outputs(params: MrisRemoveVariance, execution: Execution) -> MrisRemoveVarianceOutputs:
    """Build the MrisRemoveVarianceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    resulting_curvature_file_v: OutputPathType = execution.output_file(params["output_curvature_file"])
    return MrisRemoveVarianceOutputs(
        root=root_v,
        resulting_curvature_file=resulting_curvature_file_v,
    )

def mris_remove_variance_execute(params: MrisRemoveVariance, runner: Runner | None = None) -> MrisRemoveVarianceOutputs:
    """
    mris_remove_variance

    This program removes the linear component of the variance accounted for by one curvature vector from another.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_remove_variance_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_REMOVE_VARIANCE_METADATA)
    execution.params(params)
    args = mris_remove_variance_cargs(params, execution)
    out = mris_remove_variance_outputs(params, execution)
    execution.run(args)
    return out

def mris_remove_variance(
    input_surface_file: InputPathType,
    curvature_file: InputPathType,
    curvature_file_to_remove: InputPathType,
    output_curvature_file: str,
    runner: Runner | None = None,
) -> MrisRemoveVarianceOutputs:
    """
    mris_remove_variance

    This program removes the linear component of the variance accounted for by one curvature vector from another.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface_file: The input surface file.
        curvature_file: The curvature file.
        curvature_file_to_remove: The curvature file to remove from the input\
            curvature.
        output_curvature_file: The output curvature file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_remove_variance_params(
        input_surface_file=input_surface_file,
        curvature_file=curvature_file,
        curvature_file_to_remove=curvature_file_to_remove,
        output_curvature_file=output_curvature_file,
    )
    return mris_remove_variance_execute(params, runner)

__all__ = [
    "MrisRemoveVariance",
    "MrisRemoveVarianceOutputs",
    "MRIS_REMOVE_VARIANCE_METADATA",
    "mris_remove_variance_cargs",
    "mris_remove_variance_outputs",
    "mris_remove_variance_params",
    "mris_remove_variance_execute",
    "mris_remove_variance_validate",
    "mris_remove_variance",
]