# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_EXTRACT_PATCHES_METADATA = Metadata(
    id="mris_extract_patches",
    name="mris_extract_patches",
    package="freesurfer",
)

MrisExtractPatches = typing.TypedDict(
    "MrisExtractPatches",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_extract_patches"]],
        "subject": str,
        "output_dir": str,
    },
)

@dataclasses.dataclass
class MrisExtractPatchesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_extract_patches_params(
    subject: str,
    output_dir: str,
) -> MrisExtractPatches:
    """
    Build parameters.

    Args:
        subject: Subject from which patches are to be extracted
        output_dir: Output directory where patches will be saved

    Returns:
        Parameter dictionary.
    """
    params: MrisExtractPatches = {
        "@type": "freesurfer/mris_extract_patches",
        "subject": subject,
        "output_dir": output_dir,
    }
    return params

def mris_extract_patches_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisExtractPatches."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')

def mris_extract_patches_cargs(params: MrisExtractPatches, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_extract_patches")
    cargs.append(params["subject"])
    cargs.append(params["output_dir"])
    return cargs

def mris_extract_patches_outputs(params: MrisExtractPatches, execution: Execution) -> MrisExtractPatchesOutputs:
    """Build the MrisExtractPatchesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisExtractPatchesOutputs(
        root=root_v,
    )

def mris_extract_patches_execute(params: MrisExtractPatches, runner: Runner | None = None) -> MrisExtractPatchesOutputs:
    """
    mris_extract_patches

    A tool for extracting patches from brain surfaces.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_extract_patches_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_EXTRACT_PATCHES_METADATA)
    execution.params(params)
    args = mris_extract_patches_cargs(params, execution)
    out = mris_extract_patches_outputs(params, execution)
    execution.run(args)
    return out

def mris_extract_patches(
    subject: str,
    output_dir: str,
    runner: Runner | None = None,
) -> MrisExtractPatchesOutputs:
    """
    mris_extract_patches

    A tool for extracting patches from brain surfaces.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject from which patches are to be extracted
        output_dir: Output directory where patches will be saved
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_extract_patches_params(
        subject=subject,
        output_dir=output_dir,
    )
    return mris_extract_patches_execute(params, runner)

__all__ = [
    "MrisExtractPatches",
    "MrisExtractPatchesOutputs",
    "MRIS_EXTRACT_PATCHES_METADATA",
    "mris_extract_patches_cargs",
    "mris_extract_patches_outputs",
    "mris_extract_patches_params",
    "mris_extract_patches_execute",
    "mris_extract_patches_validate",
    "mris_extract_patches",
]