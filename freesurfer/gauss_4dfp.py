# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GAUSS_4DFP_METADATA = Metadata(
    id="gauss_4dfp",
    name="gauss_4dfp",
    package="freesurfer",
)

Gauss4dfp = typing.TypedDict(
    "Gauss4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/gauss_4dfp"]],
        "input_file": str,
        "f_half": float,
        "output_root": typing.NotRequired[str],
        "endian_flag": typing.NotRequired[str],
        "wrap_flag": typing.NotRequired[bool],
        "differentiate_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Gauss4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output file in 4dfp format."""

def gauss_4dfp_params(
    input_file: str,
    f_half: float,
    output_root: str | None = None,
    endian_flag: str | None = None,
    wrap_flag: bool = False,
    differentiate_flag: bool = False,
) -> Gauss4dfp:
    """
    Build parameters.

    Args:
        input_file: Input 4dfp or conc file to be processed.
        f_half: Half frequency in 1/cm for the Gaussian filter application (f_half\
            is the half frequency in 1/cm).
        output_root: Root name for the output file. Default is\
            <inroot>_g<10*f_half>.
        endian_flag: Specify output endian; 'b' for big or 'l' for little endian.\
            Default is input endian.
        wrap_flag: Suppress x and y padding (wrap-around).
        differentiate_flag: Apply differentiation.

    Returns:
        Parameter dictionary.
    """
    params: Gauss4dfp = {
        "@type": "freesurfer/gauss_4dfp",
        "input_file": input_file,
        "f_half": f_half,
        "wrap_flag": wrap_flag,
        "differentiate_flag": differentiate_flag,
    }
    if output_root is not None:
        params["output_root"] = output_root
    if endian_flag is not None:
        params["endian_flag"] = endian_flag
    return params

def gauss_4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gauss4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], str):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `str`')
    if params.get("f_half", None) is None:
        raise StyxValidationError("`f_half` must not be None")
    if not isinstance(params["f_half"], (float, int)):
        raise StyxValidationError(f'`f_half` has the wrong type: Received `{type(params["f_half"])}` expected `float`')
    if params.get("output_root", None) is not None:
        if not isinstance(params["output_root"], str):
            raise StyxValidationError(f'`output_root` has the wrong type: Received `{type(params["output_root"])}` expected `str`')
    if params.get("endian_flag", None) is not None:
        if not isinstance(params["endian_flag"], str):
            raise StyxValidationError(f'`endian_flag` has the wrong type: Received `{type(params["endian_flag"])}` expected `str`')
    if params.get("wrap_flag", None) is not None:
        if not isinstance(params["wrap_flag"], bool):
            raise StyxValidationError(f'`wrap_flag` has the wrong type: Received `{type(params["wrap_flag"])}` expected `bool`')
    if params.get("differentiate_flag", None) is not None:
        if not isinstance(params["differentiate_flag"], bool):
            raise StyxValidationError(f'`differentiate_flag` has the wrong type: Received `{type(params["differentiate_flag"])}` expected `bool`')

def gauss_4dfp_cargs(params: Gauss4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gauss_4dfp")
    cargs.append(params["input_file"])
    cargs.append(str(params["f_half"]))
    v_0 = params.get("output_root")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("endian_flag")
    if v_1 is not None:
        cargs.append("-@")
        cargs.append(v_1)
    if params.get("wrap_flag"):
        cargs.append("-w")
    if params.get("differentiate_flag"):
        cargs.append("-d")
    return cargs

def gauss_4dfp_outputs(params: Gauss4dfp, execution: Execution) -> Gauss4dfpOutputs:
    """Build the Gauss4dfpOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_root")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.4dfp.ifh')
    return Gauss4dfpOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def gauss_4dfp_execute(params: Gauss4dfp, runner: Runner | None = None) -> Gauss4dfpOutputs:
    """
    gauss_4dfp

    Applies a Gaussian filter to 4dfp or conc input files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gauss_4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GAUSS_4DFP_METADATA)
    execution.params(params)
    args = gauss_4dfp_cargs(params, execution)
    out = gauss_4dfp_outputs(params, execution)
    execution.run(args)
    return out

def gauss_4dfp(
    input_file: str,
    f_half: float,
    output_root: str | None = None,
    endian_flag: str | None = None,
    wrap_flag: bool = False,
    differentiate_flag: bool = False,
    runner: Runner | None = None,
) -> Gauss4dfpOutputs:
    """
    gauss_4dfp

    Applies a Gaussian filter to 4dfp or conc input files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input 4dfp or conc file to be processed.
        f_half: Half frequency in 1/cm for the Gaussian filter application (f_half\
            is the half frequency in 1/cm).
        output_root: Root name for the output file. Default is\
            <inroot>_g<10*f_half>.
        endian_flag: Specify output endian; 'b' for big or 'l' for little endian.\
            Default is input endian.
        wrap_flag: Suppress x and y padding (wrap-around).
        differentiate_flag: Apply differentiation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gauss_4dfp_params(
        input_file=input_file,
        f_half=f_half,
        output_root=output_root,
        endian_flag=endian_flag,
        wrap_flag=wrap_flag,
        differentiate_flag=differentiate_flag,
    )
    return gauss_4dfp_execute(params, runner)

__all__ = [
    "Gauss4dfp",
    "Gauss4dfpOutputs",
    "GAUSS_4DFP_METADATA",
    "gauss_4dfp_cargs",
    "gauss_4dfp_outputs",
    "gauss_4dfp_params",
    "gauss_4dfp_execute",
    "gauss_4dfp_validate",
    "gauss_4dfp",
]