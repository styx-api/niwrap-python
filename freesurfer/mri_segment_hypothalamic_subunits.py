# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEGMENT_HYPOTHALAMIC_SUBUNITS_METADATA = Metadata(
    id="mri_segment_hypothalamic_subunits",
    name="mri_segment_hypothalamic_subunits",
    package="freesurfer",
)

MriSegmentHypothalamicSubunits = typing.TypedDict(
    "MriSegmentHypothalamicSubunits",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_segment_hypothalamic_subunits"]],
        "subjects": typing.NotRequired[list[str]],
        "subjects_dir": typing.NotRequired[str],
        "write_posteriors": typing.NotRequired[bool],
        "image_input": typing.NotRequired[str],
        "output": typing.NotRequired[str],
        "posteriors": typing.NotRequired[str],
        "resample": typing.NotRequired[str],
        "volume_output": typing.NotRequired[str],
        "crop_size": typing.NotRequired[list[float]],
        "threads": typing.NotRequired[float],
        "cpu": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriSegmentHypothalamicSubunitsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmentation_output_files: OutputPathType | None
    """Segmentation output(s) in T1 mode."""
    posteriors_output: OutputPathType | None
    """Posteriors output(s)."""
    resampled_output: OutputPathType | None
    """Resampled image(s) output."""
    volume_output_csv: OutputPathType | None
    """CSV file with volumes for all structures and subjects."""

def mri_segment_hypothalamic_subunits_params(
    subjects: list[str] | None = None,
    subjects_dir: str | None = None,
    write_posteriors: bool = False,
    image_input: str | None = None,
    output: str | None = None,
    posteriors: str | None = None,
    resample: str | None = None,
    volume_output: str | None = None,
    crop_size: list[float] | None = None,
    threads: float | None = None,
    cpu: bool = False,
) -> MriSegmentHypothalamicSubunits:
    """
    Build parameters.

    Args:
        subjects: (required in FS mode) Name of one or several subjects in\
            $SUBJECTS_DIR on which to run the module, assuming recon-all has been\
            run on the specified subjects.
        subjects_dir: (FS mode, optional) Override current $SUBJECTS_DIR.
        write_posteriors: (FS mode, optional) Save posteriors; default is False.
        image_input: (required in T1 mode) Image(s) to segment. Can be a path to a\
            single image or to a folder.
        output: (required in T1 mode) Segmentation output(s). Must be a folder if\
            --i designates a folder.
        posteriors: (T1 mode, optional) Posteriors output(s). Must be a folder if\
            --i designates a folder.
        resample: (T1 mode, optional) Resampled image(s). Must be a folder if --i\
            designates a folder.
        volume_output: (T1 mode, optional) Output CSV file with volumes for all\
            structures and subjects.
        crop_size: (both modes, optional) Size of the central patch to analyse\
            (must be divisible by 8). The whole image is analysed by default.
        threads: (both modes, optional) Number of cores to be used. Default uses 1\
            core.
        cpu: (both modes, optional) Enforce running with CPU rather than GPU.

    Returns:
        Parameter dictionary.
    """
    params: MriSegmentHypothalamicSubunits = {
        "@type": "freesurfer/mri_segment_hypothalamic_subunits",
        "write_posteriors": write_posteriors,
        "cpu": cpu,
    }
    if subjects is not None:
        params["subjects"] = subjects
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if image_input is not None:
        params["image_input"] = image_input
    if output is not None:
        params["output"] = output
    if posteriors is not None:
        params["posteriors"] = posteriors
    if resample is not None:
        params["resample"] = resample
    if volume_output is not None:
        params["volume_output"] = volume_output
    if crop_size is not None:
        params["crop_size"] = crop_size
    if threads is not None:
        params["threads"] = threads
    return params

def mri_segment_hypothalamic_subunits_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegmentHypothalamicSubunits."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is not None:
        if not isinstance(params["subjects"], list):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
        for e in params["subjects"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("write_posteriors", None) is not None:
        if not isinstance(params["write_posteriors"], bool):
            raise StyxValidationError(f'`write_posteriors` has the wrong type: Received `{type(params["write_posteriors"])}` expected `bool`')
    if params.get("image_input", None) is not None:
        if not isinstance(params["image_input"], str):
            raise StyxValidationError(f'`image_input` has the wrong type: Received `{type(params["image_input"])}` expected `str`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("posteriors", None) is not None:
        if not isinstance(params["posteriors"], str):
            raise StyxValidationError(f'`posteriors` has the wrong type: Received `{type(params["posteriors"])}` expected `str`')
    if params.get("resample", None) is not None:
        if not isinstance(params["resample"], str):
            raise StyxValidationError(f'`resample` has the wrong type: Received `{type(params["resample"])}` expected `str`')
    if params.get("volume_output", None) is not None:
        if not isinstance(params["volume_output"], str):
            raise StyxValidationError(f'`volume_output` has the wrong type: Received `{type(params["volume_output"])}` expected `str`')
    if params.get("crop_size", None) is not None:
        if not isinstance(params["crop_size"], list):
            raise StyxValidationError(f'`crop_size` has the wrong type: Received `{type(params["crop_size"])}` expected `list[float]`')
        for e_2 in params["crop_size"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`crop_size` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("cpu", None) is not None:
        if not isinstance(params["cpu"], bool):
            raise StyxValidationError(f'`cpu` has the wrong type: Received `{type(params["cpu"])}` expected `bool`')

def mri_segment_hypothalamic_subunits_cargs(params: MriSegmentHypothalamicSubunits, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_segment_hypothalamic_subunits")
    v_0 = params.get("subjects")
    if v_0 is not None:
        cargs.append("--s")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("subjects_dir")
    if v_1 is not None:
        cargs.append("--sd")
        cargs.append(v_1)
    if params.get("write_posteriors"):
        cargs.append("--write_posteriors")
    v_2 = params.get("image_input")
    if v_2 is not None:
        cargs.append("--i")
        cargs.append(v_2)
    v_3 = params.get("output")
    if v_3 is not None:
        cargs.append("--o")
        cargs.append(v_3)
    v_4 = params.get("posteriors")
    if v_4 is not None:
        cargs.append("--post")
        cargs.append(v_4)
    v_5 = params.get("resample")
    if v_5 is not None:
        cargs.append("--resample")
        cargs.append(v_5)
    v_6 = params.get("volume_output")
    if v_6 is not None:
        cargs.append("--vol")
        cargs.append(v_6)
    v_7 = params.get("crop_size")
    if v_7 is not None:
        cargs.append("--crop")
        for item1 in v_7:
            cargs.append(str(item1))
    v_8 = params.get("threads")
    if v_8 is not None:
        cargs.append("--threads")
        cargs.append(str(v_8))
    if params.get("cpu"):
        cargs.append("--cpu")
    return cargs

def mri_segment_hypothalamic_subunits_outputs(params: MriSegmentHypothalamicSubunits, execution: Execution) -> MriSegmentHypothalamicSubunitsOutputs:
    """Build the MriSegmentHypothalamicSubunitsOutputs object for this tool."""
    segmentation_output_files_v: OutputPathType | None = None
    posteriors_output_v: OutputPathType | None = None
    resampled_output_v: OutputPathType | None = None
    volume_output_csv_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        segmentation_output_files_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("posteriors")
    if __v1 is not None:
        posteriors_output_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("resample")
    if __v2 is not None:
        resampled_output_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("volume_output")
    if __v3 is not None:
        volume_output_csv_v = execution.output_file((__v3 if __v3 is not None else ""))
    return MriSegmentHypothalamicSubunitsOutputs(
        root=root_v,
        segmentation_output_files=segmentation_output_files_v,
        posteriors_output=posteriors_output_v,
        resampled_output=resampled_output_v,
        volume_output_csv=volume_output_csv_v,
    )

def mri_segment_hypothalamic_subunits_execute(params: MriSegmentHypothalamicSubunits, runner: Runner | None = None) -> MriSegmentHypothalamicSubunitsOutputs:
    """
    mri_segment_hypothalamic_subunits

    This module segments hypothalamic subunits and can be run in two modes: on FreeSurfer subjects or on any T1-weighted scan(s) of approximately 1mm resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_segment_hypothalamic_subunits_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEGMENT_HYPOTHALAMIC_SUBUNITS_METADATA)
    execution.params(params)
    args = mri_segment_hypothalamic_subunits_cargs(params, execution)
    out = mri_segment_hypothalamic_subunits_outputs(params, execution)
    execution.run(args)
    return out

def mri_segment_hypothalamic_subunits(
    subjects: list[str] | None = None,
    subjects_dir: str | None = None,
    write_posteriors: bool = False,
    image_input: str | None = None,
    output: str | None = None,
    posteriors: str | None = None,
    resample: str | None = None,
    volume_output: str | None = None,
    crop_size: list[float] | None = None,
    threads: float | None = None,
    cpu: bool = False,
    runner: Runner | None = None,
) -> MriSegmentHypothalamicSubunitsOutputs:
    """
    mri_segment_hypothalamic_subunits

    This module segments hypothalamic subunits and can be run in two modes: on FreeSurfer subjects or on any T1-weighted scan(s) of approximately 1mm resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: (required in FS mode) Name of one or several subjects in\
            $SUBJECTS_DIR on which to run the module, assuming recon-all has been\
            run on the specified subjects.
        subjects_dir: (FS mode, optional) Override current $SUBJECTS_DIR.
        write_posteriors: (FS mode, optional) Save posteriors; default is False.
        image_input: (required in T1 mode) Image(s) to segment. Can be a path to a\
            single image or to a folder.
        output: (required in T1 mode) Segmentation output(s). Must be a folder if\
            --i designates a folder.
        posteriors: (T1 mode, optional) Posteriors output(s). Must be a folder if\
            --i designates a folder.
        resample: (T1 mode, optional) Resampled image(s). Must be a folder if --i\
            designates a folder.
        volume_output: (T1 mode, optional) Output CSV file with volumes for all\
            structures and subjects.
        crop_size: (both modes, optional) Size of the central patch to analyse\
            (must be divisible by 8). The whole image is analysed by default.
        threads: (both modes, optional) Number of cores to be used. Default uses 1\
            core.
        cpu: (both modes, optional) Enforce running with CPU rather than GPU.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_segment_hypothalamic_subunits_params(
        subjects=subjects,
        subjects_dir=subjects_dir,
        write_posteriors=write_posteriors,
        image_input=image_input,
        output=output,
        posteriors=posteriors,
        resample=resample,
        volume_output=volume_output,
        crop_size=crop_size,
        threads=threads,
        cpu=cpu,
    )
    return mri_segment_hypothalamic_subunits_execute(params, runner)

__all__ = [
    "MriSegmentHypothalamicSubunits",
    "MriSegmentHypothalamicSubunitsOutputs",
    "MRI_SEGMENT_HYPOTHALAMIC_SUBUNITS_METADATA",
    "mri_segment_hypothalamic_subunits_cargs",
    "mri_segment_hypothalamic_subunits_outputs",
    "mri_segment_hypothalamic_subunits_params",
    "mri_segment_hypothalamic_subunits_execute",
    "mri_segment_hypothalamic_subunits_validate",
    "mri_segment_hypothalamic_subunits",
]