# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INFLATE_SUBJECT_RH_METADATA = Metadata(
    id="inflate_subject-rh",
    name="inflate_subject-rh",
    package="freesurfer",
)

InflateSubjectRh = typing.TypedDict(
    "InflateSubjectRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/inflate_subject-rh"]],
        "arguments": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class InflateSubjectRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def inflate_subject_rh_params(
    arguments: str | None = None,
) -> InflateSubjectRh:
    """
    Build parameters.

    Args:
        arguments: Arguments for the command. Placeholder due to lack of detailed\
            help text.

    Returns:
        Parameter dictionary.
    """
    params: InflateSubjectRh = {
        "@type": "freesurfer/inflate_subject-rh",
    }
    if arguments is not None:
        params["arguments"] = arguments
    return params

def inflate_subject_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid InflateSubjectRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("arguments", None) is not None:
        if not isinstance(params["arguments"], str):
            raise StyxValidationError(f'`arguments` has the wrong type: Received `{type(params["arguments"])}` expected `str`')

def inflate_subject_rh_cargs(params: InflateSubjectRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("inflate_subject-rh")
    v_0 = params.get("arguments")
    if v_0 is not None:
        cargs.append("-rh")
        cargs.append(v_0)
    return cargs

def inflate_subject_rh_outputs(params: InflateSubjectRh, execution: Execution) -> InflateSubjectRhOutputs:
    """Build the InflateSubjectRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return InflateSubjectRhOutputs(
        root=root_v,
    )

def inflate_subject_rh_execute(params: InflateSubjectRh, runner: Runner | None = None) -> InflateSubjectRhOutputs:
    """
    inflate_subject-rh

    Freesurfer command to perform an operation on the right hemisphere of a subject's brain image data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    inflate_subject_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INFLATE_SUBJECT_RH_METADATA)
    execution.params(params)
    args = inflate_subject_rh_cargs(params, execution)
    out = inflate_subject_rh_outputs(params, execution)
    execution.run(args)
    return out

def inflate_subject_rh(
    arguments: str | None = None,
    runner: Runner | None = None,
) -> InflateSubjectRhOutputs:
    """
    inflate_subject-rh

    Freesurfer command to perform an operation on the right hemisphere of a subject's brain image data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        arguments: Arguments for the command. Placeholder due to lack of detailed\
            help text.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = inflate_subject_rh_params(
        arguments=arguments,
    )
    return inflate_subject_rh_execute(params, runner)

__all__ = [
    "InflateSubjectRh",
    "InflateSubjectRhOutputs",
    "INFLATE_SUBJECT_RH_METADATA",
    "inflate_subject_rh_cargs",
    "inflate_subject_rh_outputs",
    "inflate_subject_rh_params",
    "inflate_subject_rh_execute",
    "inflate_subject_rh_validate",
    "inflate_subject_rh",
]