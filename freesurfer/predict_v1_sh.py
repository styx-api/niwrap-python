# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PREDICT_V1_SH_METADATA = Metadata(
    id="predict_v1.sh",
    name="predict_v1.sh",
    package="freesurfer",
)

PredictV1Sh = typing.TypedDict(
    "PredictV1Sh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/predict_v1.sh"]],
        "template": typing.NotRequired[str],
        "inflated_surface_flag": typing.NotRequired[bool],
        "hemisphere": typing.NotRequired[str],
        "print_mode_flag": typing.NotRequired[bool],
        "subjects": list[str],
        "usage_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PredictV1ShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def predict_v1_sh_params(
    subjects: list[str],
    template: str | None = None,
    inflated_surface_flag: bool = False,
    hemisphere: str | None = None,
    print_mode_flag: bool = False,
    usage_flag: bool = False,
) -> PredictV1Sh:
    """
    Build parameters.

    Args:
        subjects: Subjects for prediction
        template: Target image for registration (exvivo or invivo [default])
        inflated_surface_flag: Don't use inflated surface as initial registration\
            (backward compatibility)
        hemisphere: Hemisphere (rh or lh) default is both hemispheres
        print_mode_flag: Print mode (do not run commands, just print them)
        usage_flag: Print usage

    Returns:
        Parameter dictionary.
    """
    params: PredictV1Sh = {
        "@type": "freesurfer/predict_v1.sh",
        "subjects": subjects,
        "inflated_surface_flag": inflated_surface_flag,
        "print_mode_flag": print_mode_flag,
        "usage_flag": usage_flag,
    }
    if template is not None:
        params["template"] = template
    if hemisphere is not None:
        params["hemisphere"] = hemisphere
    return params

def predict_v1_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PredictV1Sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], str):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `str`')
    if params.get("inflated_surface_flag", None) is not None:
        if not isinstance(params["inflated_surface_flag"], bool):
            raise StyxValidationError(f'`inflated_surface_flag` has the wrong type: Received `{type(params["inflated_surface_flag"])}` expected `bool`')
    if params.get("hemisphere", None) is not None:
        if not isinstance(params["hemisphere"], str):
            raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("print_mode_flag", None) is not None:
        if not isinstance(params["print_mode_flag"], bool):
            raise StyxValidationError(f'`print_mode_flag` has the wrong type: Received `{type(params["print_mode_flag"])}` expected `bool`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("usage_flag", None) is not None:
        if not isinstance(params["usage_flag"], bool):
            raise StyxValidationError(f'`usage_flag` has the wrong type: Received `{type(params["usage_flag"])}` expected `bool`')

def predict_v1_sh_cargs(params: PredictV1Sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("predict_v1.sh")
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(v_0)
    if params.get("inflated_surface_flag"):
        cargs.append("-i")
    v_1 = params.get("hemisphere")
    if v_1 is not None:
        cargs.append("-h")
        cargs.append(v_1)
    if params.get("print_mode_flag"):
        cargs.append("-p")
    for item0 in params["subjects"]:
        cargs.append(item0)
    if params.get("usage_flag"):
        cargs.append("-u")
    return cargs

def predict_v1_sh_outputs(params: PredictV1Sh, execution: Execution) -> PredictV1ShOutputs:
    """Build the PredictV1ShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PredictV1ShOutputs(
        root=root_v,
    )

def predict_v1_sh_execute(params: PredictV1Sh, runner: Runner | None = None) -> PredictV1ShOutputs:
    """
    predict_v1.sh

    A script for predicting brain images using registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    predict_v1_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PREDICT_V1_SH_METADATA)
    execution.params(params)
    args = predict_v1_sh_cargs(params, execution)
    out = predict_v1_sh_outputs(params, execution)
    execution.run(args)
    return out

def predict_v1_sh(
    subjects: list[str],
    template: str | None = None,
    inflated_surface_flag: bool = False,
    hemisphere: str | None = None,
    print_mode_flag: bool = False,
    usage_flag: bool = False,
    runner: Runner | None = None,
) -> PredictV1ShOutputs:
    """
    predict_v1.sh

    A script for predicting brain images using registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: Subjects for prediction
        template: Target image for registration (exvivo or invivo [default])
        inflated_surface_flag: Don't use inflated surface as initial registration\
            (backward compatibility)
        hemisphere: Hemisphere (rh or lh) default is both hemispheres
        print_mode_flag: Print mode (do not run commands, just print them)
        usage_flag: Print usage
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = predict_v1_sh_params(
        subjects=subjects,
        template=template,
        inflated_surface_flag=inflated_surface_flag,
        hemisphere=hemisphere,
        print_mode_flag=print_mode_flag,
        usage_flag=usage_flag,
    )
    return predict_v1_sh_execute(params, runner)

__all__ = [
    "PredictV1Sh",
    "PredictV1ShOutputs",
    "PREDICT_V1_SH_METADATA",
    "predict_v1_sh_cargs",
    "predict_v1_sh_outputs",
    "predict_v1_sh_params",
    "predict_v1_sh_execute",
    "predict_v1_sh_validate",
    "predict_v1_sh",
]