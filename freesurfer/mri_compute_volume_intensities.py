# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_COMPUTE_VOLUME_INTENSITIES_METADATA = Metadata(
    id="mri_compute_volume_intensities",
    name="mri_compute_volume_intensities",
    package="freesurfer",
)

MriComputeVolumeIntensities = typing.TypedDict(
    "MriComputeVolumeIntensities",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_compute_volume_intensities"]],
        "input_intensity": InputPathType,
        "volume_fraction_stem": str,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MriComputeVolumeIntensitiesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_volume_file: OutputPathType
    """Computed output volume file"""

def mri_compute_volume_intensities_params(
    input_intensity: InputPathType,
    volume_fraction_stem: str,
    output_volume: str,
) -> MriComputeVolumeIntensities:
    """
    Build parameters.

    Args:
        input_intensity: Input intensity volume
        volume_fraction_stem: Volume fraction stem
        output_volume: Output volume file

    Returns:
        Parameter dictionary.
    """
    params: MriComputeVolumeIntensities = {
        "@type": "freesurfer/mri_compute_volume_intensities",
        "input_intensity": input_intensity,
        "volume_fraction_stem": volume_fraction_stem,
        "output_volume": output_volume,
    }
    return params

def mri_compute_volume_intensities_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriComputeVolumeIntensities."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_intensity", None) is None:
        raise StyxValidationError("`input_intensity` must not be None")
    if not isinstance(params["input_intensity"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_intensity` has the wrong type: Received `{type(params["input_intensity"])}` expected `InputPathType`')
    if params.get("volume_fraction_stem", None) is None:
        raise StyxValidationError("`volume_fraction_stem` must not be None")
    if not isinstance(params["volume_fraction_stem"], str):
        raise StyxValidationError(f'`volume_fraction_stem` has the wrong type: Received `{type(params["volume_fraction_stem"])}` expected `str`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mri_compute_volume_intensities_cargs(params: MriComputeVolumeIntensities, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_compute_volume_intensities")
    cargs.append(execution.input_file(params["input_intensity"]))
    cargs.append(params["volume_fraction_stem"])
    cargs.append(params["output_volume"])
    return cargs

def mri_compute_volume_intensities_outputs(params: MriComputeVolumeIntensities, execution: Execution) -> MriComputeVolumeIntensitiesOutputs:
    """Build the MriComputeVolumeIntensitiesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_volume_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriComputeVolumeIntensitiesOutputs(
        root=root_v,
        output_volume_file=output_volume_file_v,
    )

def mri_compute_volume_intensities_execute(params: MriComputeVolumeIntensities, runner: Runner | None = None) -> MriComputeVolumeIntensitiesOutputs:
    """
    mri_compute_volume_intensities

    A tool to compute volume intensities for a given input intensity volume and volume fraction stem.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_compute_volume_intensities_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_COMPUTE_VOLUME_INTENSITIES_METADATA)
    execution.params(params)
    args = mri_compute_volume_intensities_cargs(params, execution)
    out = mri_compute_volume_intensities_outputs(params, execution)
    execution.run(args)
    return out

def mri_compute_volume_intensities(
    input_intensity: InputPathType,
    volume_fraction_stem: str,
    output_volume: str,
    runner: Runner | None = None,
) -> MriComputeVolumeIntensitiesOutputs:
    """
    mri_compute_volume_intensities

    A tool to compute volume intensities for a given input intensity volume and volume fraction stem.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_intensity: Input intensity volume
        volume_fraction_stem: Volume fraction stem
        output_volume: Output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_compute_volume_intensities_params(
        input_intensity=input_intensity,
        volume_fraction_stem=volume_fraction_stem,
        output_volume=output_volume,
    )
    return mri_compute_volume_intensities_execute(params, runner)

__all__ = [
    "MriComputeVolumeIntensities",
    "MriComputeVolumeIntensitiesOutputs",
    "MRI_COMPUTE_VOLUME_INTENSITIES_METADATA",
    "mri_compute_volume_intensities_cargs",
    "mri_compute_volume_intensities_outputs",
    "mri_compute_volume_intensities_params",
    "mri_compute_volume_intensities_execute",
    "mri_compute_volume_intensities_validate",
    "mri_compute_volume_intensities",
]