# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_FUNC2SPH_METADATA = Metadata(
    id="mri-func2sph",
    name="mri-func2sph",
    package="freesurfer",
)

MriFunc2sphHemisphere = typing.Literal["lh"] | typing.Literal["rh"]

MriFunc2sph = typing.TypedDict(
    "MriFunc2sph",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri-func2sph"]],
        "instem": str,
        "outstem": str,
        "hemisphere": MriFunc2sphHemisphere,
        "fvitdir": str,
        "hole_filling_iters": typing.NotRequired[float],
        "icosahedron_size": typing.NotRequired[float],
        "input_type": typing.NotRequired[str],
        "umask": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriFunc2sphOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_func2sph_params(
    instem: str,
    outstem: str,
    hemisphere: MriFunc2sphHemisphere,
    fvitdir: str,
    hole_filling_iters: float | None = None,
    icosahedron_size: float | None = None,
    input_type: str | None = None,
    umask: str | None = None,
) -> MriFunc2sph:
    """
    Build parameters.

    Args:
        instem: Input file stem
        outstem: Output file stem
        hemisphere: Hemisphere to process, can be 'lh' or 'rh'
        fvitdir: Functional vertex information directory
        hole_filling_iters: Number of hole-filling iterations
        icosahedron_size: Size of the icosahedron
        input_type: Type of input data, will be auto-detected if not specified
        umask: New umask value

    Returns:
        Parameter dictionary.
    """
    params: MriFunc2sph = {
        "@type": "freesurfer/mri-func2sph",
        "instem": instem,
        "outstem": outstem,
        "hemisphere": hemisphere,
        "fvitdir": fvitdir,
    }
    if hole_filling_iters is not None:
        params["hole_filling_iters"] = hole_filling_iters
    if icosahedron_size is not None:
        params["icosahedron_size"] = icosahedron_size
    if input_type is not None:
        params["input_type"] = input_type
    if umask is not None:
        params["umask"] = umask
    return params

def mri_func2sph_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriFunc2sph."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("instem", None) is None:
        raise StyxValidationError("`instem` must not be None")
    if not isinstance(params["instem"], str):
        raise StyxValidationError(f'`instem` has the wrong type: Received `{type(params["instem"])}` expected `str`')
    if params.get("outstem", None) is None:
        raise StyxValidationError("`outstem` must not be None")
    if not isinstance(params["outstem"], str):
        raise StyxValidationError(f'`outstem` has the wrong type: Received `{type(params["outstem"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `MriFunc2sphHemisphere`')
    if params["hemisphere"] not in ["lh", "rh"]:
        raise StyxValidationError("Parameter `hemisphere` must be one of [\"lh\", \"rh\"]")
    if params.get("fvitdir", None) is None:
        raise StyxValidationError("`fvitdir` must not be None")
    if not isinstance(params["fvitdir"], str):
        raise StyxValidationError(f'`fvitdir` has the wrong type: Received `{type(params["fvitdir"])}` expected `str`')
    if params.get("hole_filling_iters", None) is not None:
        if not isinstance(params["hole_filling_iters"], (float, int)):
            raise StyxValidationError(f'`hole_filling_iters` has the wrong type: Received `{type(params["hole_filling_iters"])}` expected `float`')
    if params.get("icosahedron_size", None) is not None:
        if not isinstance(params["icosahedron_size"], (float, int)):
            raise StyxValidationError(f'`icosahedron_size` has the wrong type: Received `{type(params["icosahedron_size"])}` expected `float`')
    if params.get("input_type", None) is not None:
        if not isinstance(params["input_type"], str):
            raise StyxValidationError(f'`input_type` has the wrong type: Received `{type(params["input_type"])}` expected `str`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')

def mri_func2sph_cargs(params: MriFunc2sph, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri-func2sph")
    cargs.append("-i")
    cargs.append(params["instem"])
    cargs.append("-o")
    cargs.append(params["outstem"])
    cargs.append("-hemi")
    cargs.append(str(params["hemisphere"]))
    cargs.append("-fvitdir")
    cargs.append(params["fvitdir"])
    v_0 = params.get("hole_filling_iters")
    if v_0 is not None:
        cargs.append("-niters")
        cargs.append(str(v_0))
    v_1 = params.get("icosahedron_size")
    if v_1 is not None:
        cargs.append("-icosize")
        cargs.append(str(v_1))
    v_2 = params.get("input_type")
    if v_2 is not None:
        cargs.append("-intype")
        cargs.append(v_2)
    v_3 = params.get("umask")
    if v_3 is not None:
        cargs.append("-umask")
        cargs.append(v_3)
    return cargs

def mri_func2sph_outputs(params: MriFunc2sph, execution: Execution) -> MriFunc2sphOutputs:
    """Build the MriFunc2sphOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriFunc2sphOutputs(
        root=root_v,
    )

def mri_func2sph_execute(params: MriFunc2sph, runner: Runner | None = None) -> MriFunc2sphOutputs:
    """
    mri-func2sph

    Maps functional data from volume space to spherical surface space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_func2sph_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_FUNC2SPH_METADATA)
    execution.params(params)
    args = mri_func2sph_cargs(params, execution)
    out = mri_func2sph_outputs(params, execution)
    execution.run(args)
    return out

def mri_func2sph(
    instem: str,
    outstem: str,
    hemisphere: MriFunc2sphHemisphere,
    fvitdir: str,
    hole_filling_iters: float | None = None,
    icosahedron_size: float | None = None,
    input_type: str | None = None,
    umask: str | None = None,
    runner: Runner | None = None,
) -> MriFunc2sphOutputs:
    """
    mri-func2sph

    Maps functional data from volume space to spherical surface space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        instem: Input file stem
        outstem: Output file stem
        hemisphere: Hemisphere to process, can be 'lh' or 'rh'
        fvitdir: Functional vertex information directory
        hole_filling_iters: Number of hole-filling iterations
        icosahedron_size: Size of the icosahedron
        input_type: Type of input data, will be auto-detected if not specified
        umask: New umask value
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_func2sph_params(
        instem=instem,
        outstem=outstem,
        hemisphere=hemisphere,
        fvitdir=fvitdir,
        hole_filling_iters=hole_filling_iters,
        icosahedron_size=icosahedron_size,
        input_type=input_type,
        umask=umask,
    )
    return mri_func2sph_execute(params, runner)

__all__ = [
    "MriFunc2sph",
    "MriFunc2sphOutputs",
    "MRI_FUNC2SPH_METADATA",
    "mri_func2sph_cargs",
    "mri_func2sph_outputs",
    "mri_func2sph_params",
    "mri_func2sph_execute",
    "mri_func2sph_validate",
    "mri_func2sph",
]