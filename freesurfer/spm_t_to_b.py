# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPM_T_TO_B_METADATA = Metadata(
    id="spm_t_to_b",
    name="spm_t_to_b",
    package="freesurfer",
)

SpmTToB = typing.TypedDict(
    "SpmTToB",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/spm_t_to_b"]],
        "spm_stem_format": str,
        "bshort_stem": str,
    },
)

@dataclasses.dataclass
class SpmTToBOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def spm_t_to_b_params(
    spm_stem_format: str,
    bshort_stem: str,
) -> SpmTToB:
    """
    Build parameters.

    Args:
        spm_stem_format: Input SPM stem format.
        bshort_stem: Output bshort stem.

    Returns:
        Parameter dictionary.
    """
    params: SpmTToB = {
        "@type": "freesurfer/spm_t_to_b",
        "spm_stem_format": spm_stem_format,
        "bshort_stem": bshort_stem,
    }
    return params

def spm_t_to_b_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpmTToB."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spm_stem_format", None) is None:
        raise StyxValidationError("`spm_stem_format` must not be None")
    if not isinstance(params["spm_stem_format"], str):
        raise StyxValidationError(f'`spm_stem_format` has the wrong type: Received `{type(params["spm_stem_format"])}` expected `str`')
    if params.get("bshort_stem", None) is None:
        raise StyxValidationError("`bshort_stem` must not be None")
    if not isinstance(params["bshort_stem"], str):
        raise StyxValidationError(f'`bshort_stem` has the wrong type: Received `{type(params["bshort_stem"])}` expected `str`')

def spm_t_to_b_cargs(params: SpmTToB, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("spm_t_to_b")
    cargs.append(params["spm_stem_format"])
    cargs.append(params["bshort_stem"])
    return cargs

def spm_t_to_b_outputs(params: SpmTToB, execution: Execution) -> SpmTToBOutputs:
    """Build the SpmTToBOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SpmTToBOutputs(
        root=root_v,
    )

def spm_t_to_b_execute(params: SpmTToB, runner: Runner | None = None) -> SpmTToBOutputs:
    """
    spm_t_to_b

    Converts SPM format to Bshort format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spm_t_to_b_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPM_T_TO_B_METADATA)
    execution.params(params)
    args = spm_t_to_b_cargs(params, execution)
    out = spm_t_to_b_outputs(params, execution)
    execution.run(args)
    return out

def spm_t_to_b(
    spm_stem_format: str,
    bshort_stem: str,
    runner: Runner | None = None,
) -> SpmTToBOutputs:
    """
    spm_t_to_b

    Converts SPM format to Bshort format.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        spm_stem_format: Input SPM stem format.
        bshort_stem: Output bshort stem.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spm_t_to_b_params(
        spm_stem_format=spm_stem_format,
        bshort_stem=bshort_stem,
    )
    return spm_t_to_b_execute(params, runner)

__all__ = [
    "SpmTToB",
    "SpmTToBOutputs",
    "SPM_T_TO_B_METADATA",
    "spm_t_to_b_cargs",
    "spm_t_to_b_outputs",
    "spm_t_to_b_params",
    "spm_t_to_b_execute",
    "spm_t_to_b_validate",
    "spm_t_to_b",
]