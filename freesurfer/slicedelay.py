# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SLICEDELAY_METADATA = Metadata(
    id="slicedelay",
    name="slicedelay",
    package="freesurfer",
)

SlicedelayOrder = typing.Literal["up"] | typing.Literal["down"] | typing.Literal["odd"] | typing.Literal["even"] | typing.Literal["siemens"]

Slicedelay = typing.TypedDict(
    "Slicedelay",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/slicedelay"]],
        "slicedelayfile": str,
        "nslices": float,
        "order": SlicedelayOrder,
        "ngroups": float,
    },
)

@dataclasses.dataclass
class SlicedelayOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    slicedelayfile: OutputPathType
    """The generated slice delay file"""

def slicedelay_params(
    slicedelayfile: str,
    nslices: float,
    order: SlicedelayOrder,
    ngroups: float,
) -> Slicedelay:
    """
    Build parameters.

    Args:
        slicedelayfile:
        nslices:
        order:
        ngroups:

    Returns:
        Parameter dictionary.
    """
    params: Slicedelay = {
        "@type": "freesurfer/slicedelay",
        "slicedelayfile": slicedelayfile,
        "nslices": nslices,
        "order": order,
        "ngroups": ngroups,
    }
    return params

def slicedelay_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Slicedelay."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("slicedelayfile", None) is None:
        raise StyxValidationError("`slicedelayfile` must not be None")
    if not isinstance(params["slicedelayfile"], str):
        raise StyxValidationError(f'`slicedelayfile` has the wrong type: Received `{type(params["slicedelayfile"])}` expected `str`')
    if params.get("nslices", None) is None:
        raise StyxValidationError("`nslices` must not be None")
    if not isinstance(params["nslices"], (float, int)):
        raise StyxValidationError(f'`nslices` has the wrong type: Received `{type(params["nslices"])}` expected `float`')
    if params.get("order", None) is None:
        raise StyxValidationError("`order` must not be None")
    if not isinstance(params["order"], str):
        raise StyxValidationError(f'`order` has the wrong type: Received `{type(params["order"])}` expected `SlicedelayOrder`')
    if params["order"] not in ["up", "down", "odd", "even", "siemens"]:
        raise StyxValidationError("Parameter `order` must be one of [\"up\", \"down\", \"odd\", \"even\", \"siemens\"]")
    if params.get("ngroups", None) is None:
        raise StyxValidationError("`ngroups` must not be None")
    if not isinstance(params["ngroups"], (float, int)):
        raise StyxValidationError(f'`ngroups` has the wrong type: Received `{type(params["ngroups"])}` expected `float`')

def slicedelay_cargs(params: Slicedelay, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("slicedelay")
    cargs.append("--o")
    cargs.append(params["slicedelayfile"])
    cargs.append("--nslices")
    cargs.append(str(params["nslices"]))
    cargs.append("--order")
    cargs.append(str(params["order"]))
    cargs.append("--ngroups")
    cargs.append(str(params["ngroups"]))
    return cargs

def slicedelay_outputs(params: Slicedelay, execution: Execution) -> SlicedelayOutputs:
    """Build the SlicedelayOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    slicedelayfile_v: OutputPathType = execution.output_file(params["slicedelayfile"])
    return SlicedelayOutputs(
        root=root_v,
        slicedelayfile=slicedelayfile_v,
    )

def slicedelay_execute(params: Slicedelay, runner: Runner | None = None) -> SlicedelayOutputs:
    """
    slicedelay

    Creates an FSL custom slice delay file for use with slicetimer for slice-time correction of fMRI

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    slicedelay_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SLICEDELAY_METADATA)
    execution.params(params)
    args = slicedelay_cargs(params, execution)
    out = slicedelay_outputs(params, execution)
    execution.run(args)
    return out

def slicedelay(
    slicedelayfile: str,
    nslices: float,
    order: SlicedelayOrder,
    ngroups: float,
    runner: Runner | None = None,
) -> SlicedelayOutputs:
    """
    slicedelay

    Creates an FSL custom slice delay file for use with slicetimer for slice-time correction of fMRI

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        slicedelayfile:
        nslices:
        order:
        ngroups:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = slicedelay_params(
        slicedelayfile=slicedelayfile,
        nslices=nslices,
        order=order,
        ngroups=ngroups,
    )
    return slicedelay_execute(params, runner)

__all__ = [
    "Slicedelay",
    "SlicedelayOutputs",
    "SLICEDELAY_METADATA",
    "slicedelay_cargs",
    "slicedelay_outputs",
    "slicedelay_params",
    "slicedelay_execute",
    "slicedelay_validate",
    "slicedelay",
]