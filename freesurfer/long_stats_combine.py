# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_STATS_COMBINE_METADATA = Metadata(
    id="long_stats_combine",
    name="long_stats_combine",
    package="freesurfer",
)

LongStatsCombine = typing.TypedDict(
    "LongStatsCombine",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_stats_combine"]],
        "qdec": InputPathType,
        "stats": str,
        "measure": str,
        "subject_dir": str,
        "output_qdec": str,
        "output_stats": typing.NotRequired[str],
        "input_stats": typing.NotRequired[InputPathType],
        "cross_sectional": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LongStatsCombineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_qdec_file: OutputPathType
    """Output long qdec table"""
    output_stacked_stats_file: OutputPathType | None
    """Stacked stats table for all subjects, all time points"""

def long_stats_combine_params(
    qdec: InputPathType,
    stats: str,
    measure: str,
    subject_dir: str,
    output_qdec: str,
    output_stats: str | None = None,
    input_stats: InputPathType | None = None,
    cross_sectional: bool = False,
) -> LongStatsCombine:
    """
    Build parameters.

    Args:
        qdec: QDEC table file specifying the subjects and time points. File has\
            first columns: fsid fsid-base
        stats: The stats file, e.g. aseg.stats or lh.aparc.stats
        measure: The stats measure (e.g. volume, thickness, mean, std)
        subject_dir: Full path to FreeSurfer subjects directory
        output_qdec: File name of output long qdec table
        output_stats: File name to output stacked stats table (all subjects, all\
            time points)
        input_stats: File name of stacked stats table (same order as qdec), instead\
            of using --stats and --meas
        cross_sectional: Use cross sectional results (for testing only)

    Returns:
        Parameter dictionary.
    """
    params: LongStatsCombine = {
        "@type": "freesurfer/long_stats_combine",
        "qdec": qdec,
        "stats": stats,
        "measure": measure,
        "subject_dir": subject_dir,
        "output_qdec": output_qdec,
        "cross_sectional": cross_sectional,
    }
    if output_stats is not None:
        params["output_stats"] = output_stats
    if input_stats is not None:
        params["input_stats"] = input_stats
    return params

def long_stats_combine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongStatsCombine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec", None) is None:
        raise StyxValidationError("`qdec` must not be None")
    if not isinstance(params["qdec"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec` has the wrong type: Received `{type(params["qdec"])}` expected `InputPathType`')
    if params.get("stats", None) is None:
        raise StyxValidationError("`stats` must not be None")
    if not isinstance(params["stats"], str):
        raise StyxValidationError(f'`stats` has the wrong type: Received `{type(params["stats"])}` expected `str`')
    if params.get("measure", None) is None:
        raise StyxValidationError("`measure` must not be None")
    if not isinstance(params["measure"], str):
        raise StyxValidationError(f'`measure` has the wrong type: Received `{type(params["measure"])}` expected `str`')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("output_qdec", None) is None:
        raise StyxValidationError("`output_qdec` must not be None")
    if not isinstance(params["output_qdec"], str):
        raise StyxValidationError(f'`output_qdec` has the wrong type: Received `{type(params["output_qdec"])}` expected `str`')
    if params.get("output_stats", None) is not None:
        if not isinstance(params["output_stats"], str):
            raise StyxValidationError(f'`output_stats` has the wrong type: Received `{type(params["output_stats"])}` expected `str`')
    if params.get("input_stats", None) is not None:
        if not isinstance(params["input_stats"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_stats` has the wrong type: Received `{type(params["input_stats"])}` expected `InputPathType`')
    if params.get("cross_sectional", None) is not None:
        if not isinstance(params["cross_sectional"], bool):
            raise StyxValidationError(f'`cross_sectional` has the wrong type: Received `{type(params["cross_sectional"])}` expected `bool`')

def long_stats_combine_cargs(params: LongStatsCombine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_stats_combine")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec"]))
    cargs.append("--stats")
    cargs.append(params["stats"])
    cargs.append("--meas")
    cargs.append(params["measure"])
    cargs.append("--sd")
    cargs.append(params["subject_dir"])
    cargs.append("--outqdec")
    cargs.append(params["output_qdec"])
    v_0 = params.get("output_stats")
    if v_0 is not None:
        cargs.append("--outstats")
        cargs.append(v_0)
    v_1 = params.get("input_stats")
    if v_1 is not None:
        cargs.append("--instats")
        cargs.append(execution.input_file(v_1))
    if params.get("cross_sectional"):
        cargs.append("--cross")
    return cargs

def long_stats_combine_outputs(params: LongStatsCombine, execution: Execution) -> LongStatsCombineOutputs:
    """Build the LongStatsCombineOutputs object for this tool."""
    output_stacked_stats_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_qdec_file_v: OutputPathType = execution.output_file(params["output_qdec"])
    __v0 = params.get("output_stats")
    if __v0 is not None:
        output_stacked_stats_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return LongStatsCombineOutputs(
        root=root_v,
        output_qdec_file=output_qdec_file_v,
        output_stacked_stats_file=output_stacked_stats_file_v,
    )

def long_stats_combine_execute(params: LongStatsCombine, runner: Runner | None = None) -> LongStatsCombineOutputs:
    """
    long_stats_combine

    Adds columns from stats into longitudinal qdec table, using longitudinally processed results.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_stats_combine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_STATS_COMBINE_METADATA)
    execution.params(params)
    args = long_stats_combine_cargs(params, execution)
    out = long_stats_combine_outputs(params, execution)
    execution.run(args)
    return out

def long_stats_combine(
    qdec: InputPathType,
    stats: str,
    measure: str,
    subject_dir: str,
    output_qdec: str,
    output_stats: str | None = None,
    input_stats: InputPathType | None = None,
    cross_sectional: bool = False,
    runner: Runner | None = None,
) -> LongStatsCombineOutputs:
    """
    long_stats_combine

    Adds columns from stats into longitudinal qdec table, using longitudinally processed results.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec: QDEC table file specifying the subjects and time points. File has\
            first columns: fsid fsid-base
        stats: The stats file, e.g. aseg.stats or lh.aparc.stats
        measure: The stats measure (e.g. volume, thickness, mean, std)
        subject_dir: Full path to FreeSurfer subjects directory
        output_qdec: File name of output long qdec table
        output_stats: File name to output stacked stats table (all subjects, all\
            time points)
        input_stats: File name of stacked stats table (same order as qdec), instead\
            of using --stats and --meas
        cross_sectional: Use cross sectional results (for testing only)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_stats_combine_params(
        qdec=qdec,
        stats=stats,
        measure=measure,
        subject_dir=subject_dir,
        output_qdec=output_qdec,
        output_stats=output_stats,
        input_stats=input_stats,
        cross_sectional=cross_sectional,
    )
    return long_stats_combine_execute(params, runner)

__all__ = [
    "LongStatsCombine",
    "LongStatsCombineOutputs",
    "LONG_STATS_COMBINE_METADATA",
    "long_stats_combine_cargs",
    "long_stats_combine_outputs",
    "long_stats_combine_params",
    "long_stats_combine_execute",
    "long_stats_combine_validate",
    "long_stats_combine",
]