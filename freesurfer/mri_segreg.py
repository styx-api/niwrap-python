# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_SEGREG_METADATA = Metadata(
    id="mri_segreg",
    name="mri_segreg",
    package="freesurfer",
)

MriSegreg = typing.TypedDict(
    "MriSegreg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_segreg"]],
        "input_file": InputPathType,
        "output_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriSegregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output file generated by mri_segreg"""

def mri_segreg_params(
    input_file: InputPathType,
    output_file: str = "output.mgz",
) -> MriSegreg:
    """
    Build parameters.

    Args:
        input_file: Input image file. All formats accepted by mri_convert can be\
            used.
        output_file: Output file for the processed image

    Returns:
        Parameter dictionary.
    """
    params: MriSegreg = {
        "@type": "freesurfer/mri_segreg",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def mri_segreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriSegreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_segreg_cargs(params: MriSegreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_segreg")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params.get("output_file", "output.mgz"))
    return cargs

def mri_segreg_outputs(params: MriSegreg, execution: Execution) -> MriSegregOutputs:
    """Build the MriSegregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params.get("output_file", "output.mgz"))
    return MriSegregOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def mri_segreg_execute(params: MriSegreg, runner: Runner | None = None) -> MriSegregOutputs:
    """
    mri_segreg

    MRI Segregation tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_segreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_SEGREG_METADATA)
    execution.params(params)
    args = mri_segreg_cargs(params, execution)
    out = mri_segreg_outputs(params, execution)
    execution.run(args)
    return out

def mri_segreg(
    input_file: InputPathType,
    output_file: str = "output.mgz",
    runner: Runner | None = None,
) -> MriSegregOutputs:
    """
    mri_segreg

    MRI Segregation tool

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input image file. All formats accepted by mri_convert can be\
            used.
        output_file: Output file for the processed image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_segreg_params(
        input_file=input_file,
        output_file=output_file,
    )
    return mri_segreg_execute(params, runner)

__all__ = [
    "MriSegreg",
    "MriSegregOutputs",
    "MRI_SEGREG_METADATA",
    "mri_segreg_cargs",
    "mri_segreg_outputs",
    "mri_segreg_params",
    "mri_segreg_execute",
    "mri_segreg_validate",
    "mri_segreg",
]