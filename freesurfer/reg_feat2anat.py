# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_FEAT2ANAT_METADATA = Metadata(
    id="reg-feat2anat",
    name="reg-feat2anat",
    package="freesurfer",
)

RegFeat2anat = typing.TypedDict(
    "RegFeat2anat",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reg-feat2anat"]],
        "feat_dir": str,
        "subject_id": str,
        "overwrite_exf2std": typing.NotRequired[bool],
        "manual": typing.NotRequired[bool],
        "manxfm_type": typing.NotRequired[str],
        "dof": typing.NotRequired[int],
        "bins": typing.NotRequired[int],
        "cost": typing.NotRequired[str],
        "max_angle": typing.NotRequired[float],
        "bet": typing.NotRequired[bool],
        "title": typing.NotRequired[str],
        "no_bbr": typing.NotRequired[bool],
        "spm": typing.NotRequired[bool],
        "no_inorm": typing.NotRequired[bool],
        "fmov": typing.NotRequired[str],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RegFeat2anatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    anat2std_register: OutputPathType
    """Init FS reg from anat to FSL-standard"""
    std2anat_fsl_mat: OutputPathType
    """Init FSL reg from FSL-standard to anat"""
    exf2anat_init_fsl_mat: OutputPathType
    """Init FSL reg from example_func to anat"""
    exf2anat_fsl_mat: OutputPathType
    """Final FSL reg from example_func to anat"""
    anat2exf_register: OutputPathType
    """Final FS reg from example_func to anat"""
    register_dat: OutputPathType
    """Same as anat2exf.register.dat"""
    example_func2standard_mat: OutputPathType
    """Replacement for feat-generated"""
    log_file: OutputPathType
    """Log file for registration"""

def reg_feat2anat_params(
    feat_dir: str,
    subject_id: str,
    overwrite_exf2std: bool = False,
    manual: bool = False,
    manxfm_type: str | None = None,
    dof: int | None = None,
    bins: int | None = None,
    cost: str | None = None,
    max_angle: float | None = None,
    bet: bool = False,
    title: str | None = None,
    no_bbr: bool = False,
    spm: bool = False,
    no_inorm: bool = False,
    fmov: str | None = None,
    debug: bool = False,
) -> RegFeat2anat:
    """
    Build parameters.

    Args:
        feat_dir: Directory in which to find example_func.
        subject_id: FreeSurfer subject identifier as found in SUBJECTS_DIR.
        overwrite_exf2std: Replace Feat-generated example_func2standard.
        manual: Interactively view/edit registration.
        manxfm_type: Interactively view/edit registration for xfm type. xfmtype can\
            be: func2anat, std2anat, or func2std.
        dof: FLIRT degrees of freedom (default is 6).
        bins: FLIRT number of bins (default is 256).
        cost: FLIRT cost function (default is corratio).
        max_angle: FLIRT maximum search angle (default is 90).
        bet: Run betfunc on example_func (not with FSL 4.0).
        title: Title for tkregister window.
        no_bbr: Do not use boundary-based registration.
        spm: Use SPM instead of FLIRT, 6 dof only.
        no_inorm: Do not inorm when running tkregister2.
        fmov: fmov argument for tkregister2.
        debug: Turn on debugging.

    Returns:
        Parameter dictionary.
    """
    params: RegFeat2anat = {
        "@type": "freesurfer/reg-feat2anat",
        "feat_dir": feat_dir,
        "subject_id": subject_id,
        "overwrite_exf2std": overwrite_exf2std,
        "manual": manual,
        "bet": bet,
        "no_bbr": no_bbr,
        "spm": spm,
        "no_inorm": no_inorm,
        "debug": debug,
    }
    if manxfm_type is not None:
        params["manxfm_type"] = manxfm_type
    if dof is not None:
        params["dof"] = dof
    if bins is not None:
        params["bins"] = bins
    if cost is not None:
        params["cost"] = cost
    if max_angle is not None:
        params["max_angle"] = max_angle
    if title is not None:
        params["title"] = title
    if fmov is not None:
        params["fmov"] = fmov
    return params

def reg_feat2anat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegFeat2anat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_dir", None) is None:
        raise StyxValidationError("`feat_dir` must not be None")
    if not isinstance(params["feat_dir"], str):
        raise StyxValidationError(f'`feat_dir` has the wrong type: Received `{type(params["feat_dir"])}` expected `str`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')
    if params.get("overwrite_exf2std", None) is not None:
        if not isinstance(params["overwrite_exf2std"], bool):
            raise StyxValidationError(f'`overwrite_exf2std` has the wrong type: Received `{type(params["overwrite_exf2std"])}` expected `bool`')
    if params.get("manual", None) is not None:
        if not isinstance(params["manual"], bool):
            raise StyxValidationError(f'`manual` has the wrong type: Received `{type(params["manual"])}` expected `bool`')
    if params.get("manxfm_type", None) is not None:
        if not isinstance(params["manxfm_type"], str):
            raise StyxValidationError(f'`manxfm_type` has the wrong type: Received `{type(params["manxfm_type"])}` expected `str`')
    if params.get("dof", None) is not None:
        if not isinstance(params["dof"], int):
            raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `int`')
    if params.get("bins", None) is not None:
        if not isinstance(params["bins"], int):
            raise StyxValidationError(f'`bins` has the wrong type: Received `{type(params["bins"])}` expected `int`')
    if params.get("cost", None) is not None:
        if not isinstance(params["cost"], str):
            raise StyxValidationError(f'`cost` has the wrong type: Received `{type(params["cost"])}` expected `str`')
    if params.get("max_angle", None) is not None:
        if not isinstance(params["max_angle"], (float, int)):
            raise StyxValidationError(f'`max_angle` has the wrong type: Received `{type(params["max_angle"])}` expected `float`')
    if params.get("bet", None) is not None:
        if not isinstance(params["bet"], bool):
            raise StyxValidationError(f'`bet` has the wrong type: Received `{type(params["bet"])}` expected `bool`')
    if params.get("title", None) is not None:
        if not isinstance(params["title"], str):
            raise StyxValidationError(f'`title` has the wrong type: Received `{type(params["title"])}` expected `str`')
    if params.get("no_bbr", None) is not None:
        if not isinstance(params["no_bbr"], bool):
            raise StyxValidationError(f'`no_bbr` has the wrong type: Received `{type(params["no_bbr"])}` expected `bool`')
    if params.get("spm", None) is not None:
        if not isinstance(params["spm"], bool):
            raise StyxValidationError(f'`spm` has the wrong type: Received `{type(params["spm"])}` expected `bool`')
    if params.get("no_inorm", None) is not None:
        if not isinstance(params["no_inorm"], bool):
            raise StyxValidationError(f'`no_inorm` has the wrong type: Received `{type(params["no_inorm"])}` expected `bool`')
    if params.get("fmov", None) is not None:
        if not isinstance(params["fmov"], str):
            raise StyxValidationError(f'`fmov` has the wrong type: Received `{type(params["fmov"])}` expected `str`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def reg_feat2anat_cargs(params: RegFeat2anat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg-feat2anat")
    cargs.append("--feat")
    cargs.append(params["feat_dir"])
    cargs.append("--subject")
    cargs.append(params["subject_id"])
    if params.get("overwrite_exf2std"):
        cargs.append("--overwrite-exf2std")
    if params.get("manual"):
        cargs.append("--manual")
    v_0 = params.get("manxfm_type")
    if v_0 is not None:
        cargs.append("--manxfm")
        cargs.append(v_0)
    v_1 = params.get("dof")
    if v_1 is not None:
        cargs.append("--dof")
        cargs.append(str(v_1))
    v_2 = params.get("bins")
    if v_2 is not None:
        cargs.append("--bins")
        cargs.append(str(v_2))
    v_3 = params.get("cost")
    if v_3 is not None:
        cargs.append("--cost")
        cargs.append(v_3)
    v_4 = params.get("max_angle")
    if v_4 is not None:
        cargs.append("--maxangle")
        cargs.append(str(v_4))
    if params.get("bet"):
        cargs.append("--bet")
    v_5 = params.get("title")
    if v_5 is not None:
        cargs.append("--title")
        cargs.append(v_5)
    if params.get("no_bbr"):
        cargs.append("--no-bbr")
    if params.get("spm"):
        cargs.append("--spm")
    if params.get("no_inorm"):
        cargs.append("--no-inorm")
    v_6 = params.get("fmov")
    if v_6 is not None:
        cargs.append("--fmov")
        cargs.append(v_6)
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def reg_feat2anat_outputs(params: RegFeat2anat, execution: Execution) -> RegFeat2anatOutputs:
    """Build the RegFeat2anatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    anat2std_register_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/anat2std.register.dat")
    std2anat_fsl_mat_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/std2anat.fsl.mat")
    exf2anat_init_fsl_mat_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/exf2anat.init.fsl.mat")
    exf2anat_fsl_mat_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/exf2anat.fsl.mat")
    anat2exf_register_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/anat2exf.register.dat")
    register_dat_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/register.dat")
    example_func2standard_mat_v: OutputPathType = execution.output_file("featdir/reg/freesurfer/example_func2standard.mat")
    log_file_v: OutputPathType = execution.output_file("reg/freesurfer/reg-feat2anat.log")
    return RegFeat2anatOutputs(
        root=root_v,
        anat2std_register=anat2std_register_v,
        std2anat_fsl_mat=std2anat_fsl_mat_v,
        exf2anat_init_fsl_mat=exf2anat_init_fsl_mat_v,
        exf2anat_fsl_mat=exf2anat_fsl_mat_v,
        anat2exf_register=anat2exf_register_v,
        register_dat=register_dat_v,
        example_func2standard_mat=example_func2standard_mat_v,
        log_file=log_file_v,
    )

def reg_feat2anat_execute(params: RegFeat2anat, runner: Runner | None = None) -> RegFeat2anatOutputs:
    """
    reg-feat2anat

    Registers FSL-Feat example_func to FreeSurfer anatomical data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_feat2anat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_FEAT2ANAT_METADATA)
    execution.params(params)
    args = reg_feat2anat_cargs(params, execution)
    out = reg_feat2anat_outputs(params, execution)
    execution.run(args)
    return out

def reg_feat2anat(
    feat_dir: str,
    subject_id: str,
    overwrite_exf2std: bool = False,
    manual: bool = False,
    manxfm_type: str | None = None,
    dof: int | None = None,
    bins: int | None = None,
    cost: str | None = None,
    max_angle: float | None = None,
    bet: bool = False,
    title: str | None = None,
    no_bbr: bool = False,
    spm: bool = False,
    no_inorm: bool = False,
    fmov: str | None = None,
    debug: bool = False,
    runner: Runner | None = None,
) -> RegFeat2anatOutputs:
    """
    reg-feat2anat

    Registers FSL-Feat example_func to FreeSurfer anatomical data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        feat_dir: Directory in which to find example_func.
        subject_id: FreeSurfer subject identifier as found in SUBJECTS_DIR.
        overwrite_exf2std: Replace Feat-generated example_func2standard.
        manual: Interactively view/edit registration.
        manxfm_type: Interactively view/edit registration for xfm type. xfmtype can\
            be: func2anat, std2anat, or func2std.
        dof: FLIRT degrees of freedom (default is 6).
        bins: FLIRT number of bins (default is 256).
        cost: FLIRT cost function (default is corratio).
        max_angle: FLIRT maximum search angle (default is 90).
        bet: Run betfunc on example_func (not with FSL 4.0).
        title: Title for tkregister window.
        no_bbr: Do not use boundary-based registration.
        spm: Use SPM instead of FLIRT, 6 dof only.
        no_inorm: Do not inorm when running tkregister2.
        fmov: fmov argument for tkregister2.
        debug: Turn on debugging.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_feat2anat_params(
        feat_dir=feat_dir,
        subject_id=subject_id,
        overwrite_exf2std=overwrite_exf2std,
        manual=manual,
        manxfm_type=manxfm_type,
        dof=dof,
        bins=bins,
        cost=cost,
        max_angle=max_angle,
        bet=bet,
        title=title,
        no_bbr=no_bbr,
        spm=spm,
        no_inorm=no_inorm,
        fmov=fmov,
        debug=debug,
    )
    return reg_feat2anat_execute(params, runner)

__all__ = [
    "RegFeat2anat",
    "RegFeat2anatOutputs",
    "REG_FEAT2ANAT_METADATA",
    "reg_feat2anat_cargs",
    "reg_feat2anat_outputs",
    "reg_feat2anat_params",
    "reg_feat2anat_execute",
    "reg_feat2anat_validate",
    "reg_feat2anat",
]