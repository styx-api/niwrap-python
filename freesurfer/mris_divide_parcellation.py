# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_DIVIDE_PARCELLATION_METADATA = Metadata(
    id="mris_divide_parcellation",
    name="mris_divide_parcellation",
    package="freesurfer",
)

MrisDivideParcellation = typing.TypedDict(
    "MrisDivideParcellation",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_divide_parcellation"]],
        "subject": str,
        "hemi": str,
        "sourceannot": InputPathType,
        "splitfile_or_areathresh": str,
        "outannot": str,
        "scale": typing.NotRequired[float],
        "label_name": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisDivideParcellationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outannot_file: OutputPathType
    """The resulting annotation file with divided labels."""

def mris_divide_parcellation_params(
    subject: str,
    hemi: str,
    sourceannot: InputPathType,
    splitfile_or_areathresh: str,
    outannot: str,
    scale: float | None = None,
    label_name: str | None = None,
) -> MrisDivideParcellation:
    """
    Build parameters.

    Args:
        subject: The subject identifier.
        hemi: The hemisphere identifier, e.g., left (lh) or right (rh).
        sourceannot: The source annotation file.
        splitfile_or_areathresh: Either a splitfile specifying divisions or an area\
            threshold in mm^2.
        outannot: The output annotation file name.
        scale: Specify offset scaling for rgb values (default=20).
        label_name: Only process the label <label name> (not implemented yet).

    Returns:
        Parameter dictionary.
    """
    params: MrisDivideParcellation = {
        "@type": "freesurfer/mris_divide_parcellation",
        "subject": subject,
        "hemi": hemi,
        "sourceannot": sourceannot,
        "splitfile_or_areathresh": splitfile_or_areathresh,
        "outannot": outannot,
    }
    if scale is not None:
        params["scale"] = scale
    if label_name is not None:
        params["label_name"] = label_name
    return params

def mris_divide_parcellation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisDivideParcellation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("sourceannot", None) is None:
        raise StyxValidationError("`sourceannot` must not be None")
    if not isinstance(params["sourceannot"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sourceannot` has the wrong type: Received `{type(params["sourceannot"])}` expected `InputPathType`')
    if params.get("splitfile_or_areathresh", None) is None:
        raise StyxValidationError("`splitfile_or_areathresh` must not be None")
    if not isinstance(params["splitfile_or_areathresh"], str):
        raise StyxValidationError(f'`splitfile_or_areathresh` has the wrong type: Received `{type(params["splitfile_or_areathresh"])}` expected `str`')
    if params.get("outannot", None) is None:
        raise StyxValidationError("`outannot` must not be None")
    if not isinstance(params["outannot"], str):
        raise StyxValidationError(f'`outannot` has the wrong type: Received `{type(params["outannot"])}` expected `str`')
    if params.get("scale", None) is not None:
        if not isinstance(params["scale"], (float, int)):
            raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `float`')
    if params.get("label_name", None) is not None:
        if not isinstance(params["label_name"], str):
            raise StyxValidationError(f'`label_name` has the wrong type: Received `{type(params["label_name"])}` expected `str`')

def mris_divide_parcellation_cargs(params: MrisDivideParcellation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_divide_parcellation")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["sourceannot"]))
    cargs.append(params["splitfile_or_areathresh"])
    cargs.append(params["outannot"])
    v_0 = params.get("scale")
    if v_0 is not None:
        cargs.append("-scale")
        cargs.append(str(v_0))
    v_1 = params.get("label_name")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(v_1)
    return cargs

def mris_divide_parcellation_outputs(params: MrisDivideParcellation, execution: Execution) -> MrisDivideParcellationOutputs:
    """Build the MrisDivideParcellationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outannot_file_v: OutputPathType = execution.output_file(params["outannot"])
    return MrisDivideParcellationOutputs(
        root=root_v,
        outannot_file=outannot_file_v,
    )

def mris_divide_parcellation_execute(params: MrisDivideParcellation, runner: Runner | None = None) -> MrisDivideParcellationOutputs:
    """
    mris_divide_parcellation

    Divides one or more parcellations into divisions perpendicular to the long axis of the label.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_divide_parcellation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_DIVIDE_PARCELLATION_METADATA)
    execution.params(params)
    args = mris_divide_parcellation_cargs(params, execution)
    out = mris_divide_parcellation_outputs(params, execution)
    execution.run(args)
    return out

def mris_divide_parcellation(
    subject: str,
    hemi: str,
    sourceannot: InputPathType,
    splitfile_or_areathresh: str,
    outannot: str,
    scale: float | None = None,
    label_name: str | None = None,
    runner: Runner | None = None,
) -> MrisDivideParcellationOutputs:
    """
    mris_divide_parcellation

    Divides one or more parcellations into divisions perpendicular to the long axis of the label.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject identifier.
        hemi: The hemisphere identifier, e.g., left (lh) or right (rh).
        sourceannot: The source annotation file.
        splitfile_or_areathresh: Either a splitfile specifying divisions or an area\
            threshold in mm^2.
        outannot: The output annotation file name.
        scale: Specify offset scaling for rgb values (default=20).
        label_name: Only process the label <label name> (not implemented yet).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_divide_parcellation_params(
        subject=subject,
        hemi=hemi,
        sourceannot=sourceannot,
        splitfile_or_areathresh=splitfile_or_areathresh,
        outannot=outannot,
        scale=scale,
        label_name=label_name,
    )
    return mris_divide_parcellation_execute(params, runner)

__all__ = [
    "MrisDivideParcellation",
    "MrisDivideParcellationOutputs",
    "MRIS_DIVIDE_PARCELLATION_METADATA",
    "mris_divide_parcellation_cargs",
    "mris_divide_parcellation_outputs",
    "mris_divide_parcellation_params",
    "mris_divide_parcellation_execute",
    "mris_divide_parcellation_validate",
    "mris_divide_parcellation",
]