# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LIST_OTL_LABELS_METADATA = Metadata(
    id="list_otl_labels",
    name="list_otl_labels",
    package="freesurfer",
)

ListOtlLabels = typing.TypedDict(
    "ListOtlLabels",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/list_otl_labels"]],
        "input_file": InputPathType,
    },
)

@dataclasses.dataclass
class ListOtlLabelsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def list_otl_labels_params(
    input_file: InputPathType,
) -> ListOtlLabels:
    """
    Build parameters.

    Args:
        input_file:

    Returns:
        Parameter dictionary.
    """
    params: ListOtlLabels = {
        "@type": "freesurfer/list_otl_labels",
        "input_file": input_file,
    }
    return params

def list_otl_labels_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ListOtlLabels."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def list_otl_labels_cargs(params: ListOtlLabels, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("list_otl_labels")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    return cargs

def list_otl_labels_outputs(params: ListOtlLabels, execution: Execution) -> ListOtlLabelsOutputs:
    """Build the ListOtlLabelsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ListOtlLabelsOutputs(
        root=root_v,
    )

def list_otl_labels_execute(params: ListOtlLabels, runner: Runner | None = None) -> ListOtlLabelsOutputs:
    """
    list_otl_labels

    Tool for listing labels in an imaging file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    list_otl_labels_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LIST_OTL_LABELS_METADATA)
    execution.params(params)
    args = list_otl_labels_cargs(params, execution)
    out = list_otl_labels_outputs(params, execution)
    execution.run(args)
    return out

def list_otl_labels(
    input_file: InputPathType,
    runner: Runner | None = None,
) -> ListOtlLabelsOutputs:
    """
    list_otl_labels

    Tool for listing labels in an imaging file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = list_otl_labels_params(
        input_file=input_file,
    )
    return list_otl_labels_execute(params, runner)

__all__ = [
    "ListOtlLabels",
    "ListOtlLabelsOutputs",
    "LIST_OTL_LABELS_METADATA",
    "list_otl_labels_cargs",
    "list_otl_labels_outputs",
    "list_otl_labels_params",
    "list_otl_labels_execute",
    "list_otl_labels_validate",
    "list_otl_labels",
]