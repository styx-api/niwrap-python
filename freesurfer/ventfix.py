# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VENTFIX_METADATA = Metadata(
    id="ventfix",
    name="ventfix",
    package="freesurfer",
)

Ventfix = typing.TypedDict(
    "Ventfix",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/ventfix"]],
        "subject_dir": str,
        "option1": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VentfixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fixed_ventricles: OutputPathType
    """Output image with fixed ventricles"""

def ventfix_params(
    subject_dir: str,
    option1: str | None = None,
) -> Ventfix:
    """
    Build parameters.

    Args:
        subject_dir: Path to the subject's directory containing MRI scans.
        option1: Description of option 1

    Returns:
        Parameter dictionary.
    """
    params: Ventfix = {
        "@type": "freesurfer/ventfix",
        "subject_dir": subject_dir,
    }
    if option1 is not None:
        params["option1"] = option1
    return params

def ventfix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ventfix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("option1", None) is not None:
        if not isinstance(params["option1"], str):
            raise StyxValidationError(f'`option1` has the wrong type: Received `{type(params["option1"])}` expected `str`')

def ventfix_cargs(params: Ventfix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ventfix")
    cargs.append(params["subject_dir"])
    v_0 = params.get("option1")
    if v_0 is not None:
        cargs.append("--option1")
        cargs.append(v_0)
    return cargs

def ventfix_outputs(params: Ventfix, execution: Execution) -> VentfixOutputs:
    """Build the VentfixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fixed_ventricles_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}/fixed_ventricles.nii.gz')
    return VentfixOutputs(
        root=root_v,
        fixed_ventricles=fixed_ventricles_v,
    )

def ventfix_execute(params: Ventfix, runner: Runner | None = None) -> VentfixOutputs:
    """
    ventfix

    Tool for fixing ventricles in MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ventfix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VENTFIX_METADATA)
    execution.params(params)
    args = ventfix_cargs(params, execution)
    out = ventfix_outputs(params, execution)
    execution.run(args)
    return out

def ventfix(
    subject_dir: str,
    option1: str | None = None,
    runner: Runner | None = None,
) -> VentfixOutputs:
    """
    ventfix

    Tool for fixing ventricles in MRI scans

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Path to the subject's directory containing MRI scans.
        option1: Description of option 1
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ventfix_params(
        subject_dir=subject_dir,
        option1=option1,
    )
    return ventfix_execute(params, runner)

__all__ = [
    "Ventfix",
    "VentfixOutputs",
    "VENTFIX_METADATA",
    "ventfix_cargs",
    "ventfix_outputs",
    "ventfix_params",
    "ventfix_execute",
    "ventfix_validate",
    "ventfix",
]