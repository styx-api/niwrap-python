# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DMRI_COLORED_FA_METADATA = Metadata(
    id="dmri_coloredFA",
    name="dmri_coloredFA",
    package="freesurfer",
)

DmriColoredFa = typing.TypedDict(
    "DmriColoredFa",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dmri_coloredFA"]],
        "input_volume": InputPathType,
        "output_volume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class DmriColoredFaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_colored_fa: OutputPathType
    """The output colored FA image."""

def dmri_colored_fa_params(
    input_volume: InputPathType,
    output_volume: str = "colored_FA",
) -> DmriColoredFa:
    """
    Build parameters.

    Args:
        input_volume: Input diffusion MRI volume.
        output_volume: Output colored FA image.

    Returns:
        Parameter dictionary.
    """
    params: DmriColoredFa = {
        "@type": "freesurfer/dmri_coloredFA",
        "input_volume": input_volume,
        "output_volume": output_volume,
    }
    return params

def dmri_colored_fa_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DmriColoredFa."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def dmri_colored_fa_cargs(params: DmriColoredFa, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dmri_coloredFA")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params.get("output_volume", "colored_FA"))
    return cargs

def dmri_colored_fa_outputs(params: DmriColoredFa, execution: Execution) -> DmriColoredFaOutputs:
    """Build the DmriColoredFaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_colored_fa_v: OutputPathType = execution.output_file(f'{params.get("output_volume", "colored_FA")}.nii.gz')
    return DmriColoredFaOutputs(
        root=root_v,
        output_colored_fa=output_colored_fa_v,
    )

def dmri_colored_fa_execute(params: DmriColoredFa, runner: Runner | None = None) -> DmriColoredFaOutputs:
    """
    dmri_coloredFA

    Tool for generating colored FA maps from diffusion MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dmri_colored_fa_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DMRI_COLORED_FA_METADATA)
    execution.params(params)
    args = dmri_colored_fa_cargs(params, execution)
    out = dmri_colored_fa_outputs(params, execution)
    execution.run(args)
    return out

def dmri_colored_fa(
    input_volume: InputPathType,
    output_volume: str = "colored_FA",
    runner: Runner | None = None,
) -> DmriColoredFaOutputs:
    """
    dmri_coloredFA

    Tool for generating colored FA maps from diffusion MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input diffusion MRI volume.
        output_volume: Output colored FA image.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dmri_colored_fa_params(
        input_volume=input_volume,
        output_volume=output_volume,
    )
    return dmri_colored_fa_execute(params, runner)

__all__ = [
    "DmriColoredFa",
    "DmriColoredFaOutputs",
    "DMRI_COLORED_FA_METADATA",
    "dmri_colored_fa_cargs",
    "dmri_colored_fa_outputs",
    "dmri_colored_fa_params",
    "dmri_colored_fa_execute",
    "dmri_colored_fa_validate",
    "dmri_colored_fa",
]