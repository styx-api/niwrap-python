# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MULTISCALE_STATS_METADATA = Metadata(
    id="mris_multiscale_stats",
    name="mris_multiscale_stats",
    package="freesurfer",
)

MrisMultiscaleStats = typing.TypedDict(
    "MrisMultiscaleStats",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_multiscale_stats"]],
        "output_subject": str,
        "hemi": str,
        "surf": InputPathType,
        "curv": InputPathType,
        "class1_subjects": list[str],
        "class2_subjects": list[str],
    },
)

@dataclasses.dataclass
class MrisMultiscaleStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_multiscale_stats_params(
    output_subject: str,
    hemi: str,
    surf: InputPathType,
    curv: InputPathType,
    class1_subjects: list[str],
    class2_subjects: list[str],
) -> MrisMultiscaleStats:
    """
    Build parameters.

    Args:
        output_subject: The output subject identifier.
        hemi: Specify which hemisphere to use.
        surf: A spherical surface file suitable for computing geodesics.
        curv: The curvature file to be processed.
        class1_subjects: List of subjects from one class.
        class2_subjects: List of subjects from another class.

    Returns:
        Parameter dictionary.
    """
    params: MrisMultiscaleStats = {
        "@type": "freesurfer/mris_multiscale_stats",
        "output_subject": output_subject,
        "hemi": hemi,
        "surf": surf,
        "curv": curv,
        "class1_subjects": class1_subjects,
        "class2_subjects": class2_subjects,
    }
    return params

def mris_multiscale_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMultiscaleStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_subject", None) is None:
        raise StyxValidationError("`output_subject` must not be None")
    if not isinstance(params["output_subject"], str):
        raise StyxValidationError(f'`output_subject` has the wrong type: Received `{type(params["output_subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surf", None) is None:
        raise StyxValidationError("`surf` must not be None")
    if not isinstance(params["surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `InputPathType`')
    if params.get("curv", None) is None:
        raise StyxValidationError("`curv` must not be None")
    if not isinstance(params["curv"], (pathlib.Path, str)):
        raise StyxValidationError(f'`curv` has the wrong type: Received `{type(params["curv"])}` expected `InputPathType`')
    if params.get("class1_subjects", None) is None:
        raise StyxValidationError("`class1_subjects` must not be None")
    if not isinstance(params["class1_subjects"], list):
        raise StyxValidationError(f'`class1_subjects` has the wrong type: Received `{type(params["class1_subjects"])}` expected `list[str]`')
    for e in params["class1_subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`class1_subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("class2_subjects", None) is None:
        raise StyxValidationError("`class2_subjects` must not be None")
    if not isinstance(params["class2_subjects"], list):
        raise StyxValidationError(f'`class2_subjects` has the wrong type: Received `{type(params["class2_subjects"])}` expected `list[str]`')
    for e_2 in params["class2_subjects"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`class2_subjects` has the wrong type: Received `{type(e_2)}` expected `list[str]`')

def mris_multiscale_stats_cargs(params: MrisMultiscaleStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_multiscale_stats")
    cargs.append("-o")
    cargs.append(params["output_subject"])
    cargs.append(params["hemi"])
    cargs.append(execution.input_file(params["surf"]))
    cargs.append(execution.input_file(params["curv"]))
    for item0 in params["class1_subjects"]:
        cargs.append(item0)
    for item1 in params["class2_subjects"]:
        cargs.append(item1)
    return cargs

def mris_multiscale_stats_outputs(params: MrisMultiscaleStats, execution: Execution) -> MrisMultiscaleStatsOutputs:
    """Build the MrisMultiscaleStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisMultiscaleStatsOutputs(
        root=root_v,
    )

def mris_multiscale_stats_execute(params: MrisMultiscaleStats, runner: Runner | None = None) -> MrisMultiscaleStatsOutputs:
    """
    mris_multiscale_stats

    Compute the autocorrelation function of a curvature file using multiscale statistical techniques.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_multiscale_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MULTISCALE_STATS_METADATA)
    execution.params(params)
    args = mris_multiscale_stats_cargs(params, execution)
    out = mris_multiscale_stats_outputs(params, execution)
    execution.run(args)
    return out

def mris_multiscale_stats(
    output_subject: str,
    hemi: str,
    surf: InputPathType,
    curv: InputPathType,
    class1_subjects: list[str],
    class2_subjects: list[str],
    runner: Runner | None = None,
) -> MrisMultiscaleStatsOutputs:
    """
    mris_multiscale_stats

    Compute the autocorrelation function of a curvature file using multiscale statistical techniques.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_subject: The output subject identifier.
        hemi: Specify which hemisphere to use.
        surf: A spherical surface file suitable for computing geodesics.
        curv: The curvature file to be processed.
        class1_subjects: List of subjects from one class.
        class2_subjects: List of subjects from another class.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_multiscale_stats_params(
        output_subject=output_subject,
        hemi=hemi,
        surf=surf,
        curv=curv,
        class1_subjects=class1_subjects,
        class2_subjects=class2_subjects,
    )
    return mris_multiscale_stats_execute(params, runner)

__all__ = [
    "MrisMultiscaleStats",
    "MrisMultiscaleStatsOutputs",
    "MRIS_MULTISCALE_STATS_METADATA",
    "mris_multiscale_stats_cargs",
    "mris_multiscale_stats_outputs",
    "mris_multiscale_stats_params",
    "mris_multiscale_stats_execute",
    "mris_multiscale_stats_validate",
    "mris_multiscale_stats",
]