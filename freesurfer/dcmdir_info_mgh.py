# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCMDIR_INFO_MGH_METADATA = Metadata(
    id="dcmdir-info-mgh",
    name="dcmdir-info-mgh",
    package="freesurfer",
)

DcmdirInfoMgh = typing.TypedDict(
    "DcmdirInfoMgh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/dcmdir-info-mgh"]],
        "dicomdir": str,
        "unpackdir": typing.NotRequired[str],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "nopre": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DcmdirInfoMghOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dcmdir_info_mgh_params(
    dicomdir: str,
    unpackdir: str | None = None,
    version: bool = False,
    help: bool = False,
    nopre: bool = False,
) -> DcmdirInfoMgh:
    """
    Build parameters.

    Args:
        dicomdir: Input DICOM directory.
        unpackdir: Directory where the unpacked data will be stored (optional). If\
            specified, DICOM files are converted to MGZ format.
        version: Print version and exit
        help: Print help and exit
        nopre: Do not assume filenames use the NNNNNN- prefix convention

    Returns:
        Parameter dictionary.
    """
    params: DcmdirInfoMgh = {
        "@type": "freesurfer/dcmdir-info-mgh",
        "dicomdir": dicomdir,
        "version": version,
        "help": help,
        "nopre": nopre,
    }
    if unpackdir is not None:
        params["unpackdir"] = unpackdir
    return params

def dcmdir_info_mgh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DcmdirInfoMgh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dicomdir", None) is None:
        raise StyxValidationError("`dicomdir` must not be None")
    if not isinstance(params["dicomdir"], str):
        raise StyxValidationError(f'`dicomdir` has the wrong type: Received `{type(params["dicomdir"])}` expected `str`')
    if params.get("unpackdir", None) is not None:
        if not isinstance(params["unpackdir"], str):
            raise StyxValidationError(f'`unpackdir` has the wrong type: Received `{type(params["unpackdir"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("nopre", None) is not None:
        if not isinstance(params["nopre"], bool):
            raise StyxValidationError(f'`nopre` has the wrong type: Received `{type(params["nopre"])}` expected `bool`')

def dcmdir_info_mgh_cargs(params: DcmdirInfoMgh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcmdir-info-mgh")
    cargs.append("-mgh")
    cargs.append(params["dicomdir"])
    v_0 = params.get("unpackdir")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    if params.get("nopre"):
        cargs.append("--nopre")
    return cargs

def dcmdir_info_mgh_outputs(params: DcmdirInfoMgh, execution: Execution) -> DcmdirInfoMghOutputs:
    """Build the DcmdirInfoMghOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DcmdirInfoMghOutputs(
        root=root_v,
    )

def dcmdir_info_mgh_execute(params: DcmdirInfoMgh, runner: Runner | None = None) -> DcmdirInfoMghOutputs:
    """
    dcmdir-info-mgh

    Scans a DICOM directory and extracts information about each series.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcmdir_info_mgh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCMDIR_INFO_MGH_METADATA)
    execution.params(params)
    args = dcmdir_info_mgh_cargs(params, execution)
    out = dcmdir_info_mgh_outputs(params, execution)
    execution.run(args)
    return out

def dcmdir_info_mgh(
    dicomdir: str,
    unpackdir: str | None = None,
    version: bool = False,
    help: bool = False,
    nopre: bool = False,
    runner: Runner | None = None,
) -> DcmdirInfoMghOutputs:
    """
    dcmdir-info-mgh

    Scans a DICOM directory and extracts information about each series.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        dicomdir: Input DICOM directory.
        unpackdir: Directory where the unpacked data will be stored (optional). If\
            specified, DICOM files are converted to MGZ format.
        version: Print version and exit
        help: Print help and exit
        nopre: Do not assume filenames use the NNNNNN- prefix convention
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcmdir_info_mgh_params(
        dicomdir=dicomdir,
        unpackdir=unpackdir,
        version=version,
        help=help,
        nopre=nopre,
    )
    return dcmdir_info_mgh_execute(params, runner)

__all__ = [
    "DcmdirInfoMgh",
    "DcmdirInfoMghOutputs",
    "DCMDIR_INFO_MGH_METADATA",
    "dcmdir_info_mgh_cargs",
    "dcmdir_info_mgh_outputs",
    "dcmdir_info_mgh_params",
    "dcmdir_info_mgh_execute",
    "dcmdir_info_mgh_validate",
    "dcmdir_info_mgh",
]