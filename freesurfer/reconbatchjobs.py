# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RECONBATCHJOBS_METADATA = Metadata(
    id="reconbatchjobs",
    name="reconbatchjobs",
    package="freesurfer",
)

Reconbatchjobs = typing.TypedDict(
    "Reconbatchjobs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/reconbatchjobs"]],
        "logfile": str,
        "cmdfiles": list[str],
    },
)

@dataclasses.dataclass
class ReconbatchjobsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def reconbatchjobs_params(
    logfile: str,
    cmdfiles: list[str],
) -> Reconbatchjobs:
    """
    Build parameters.

    Args:
        logfile: Log file to capture output of batch jobs
        cmdfiles: Command files for batch processing

    Returns:
        Parameter dictionary.
    """
    params: Reconbatchjobs = {
        "@type": "freesurfer/reconbatchjobs",
        "logfile": logfile,
        "cmdfiles": cmdfiles,
    }
    return params

def reconbatchjobs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Reconbatchjobs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("logfile", None) is None:
        raise StyxValidationError("`logfile` must not be None")
    if not isinstance(params["logfile"], str):
        raise StyxValidationError(f'`logfile` has the wrong type: Received `{type(params["logfile"])}` expected `str`')
    if params.get("cmdfiles", None) is None:
        raise StyxValidationError("`cmdfiles` must not be None")
    if not isinstance(params["cmdfiles"], list):
        raise StyxValidationError(f'`cmdfiles` has the wrong type: Received `{type(params["cmdfiles"])}` expected `list[str]`')
    for e in params["cmdfiles"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`cmdfiles` has the wrong type: Received `{type(e)}` expected `list[str]`')

def reconbatchjobs_cargs(params: Reconbatchjobs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reconbatchjobs")
    cargs.append(params["logfile"])
    for item0 in params["cmdfiles"]:
        cargs.append(item0)
    return cargs

def reconbatchjobs_outputs(params: Reconbatchjobs, execution: Execution) -> ReconbatchjobsOutputs:
    """Build the ReconbatchjobsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ReconbatchjobsOutputs(
        root=root_v,
    )

def reconbatchjobs_execute(params: Reconbatchjobs, runner: Runner | None = None) -> ReconbatchjobsOutputs:
    """
    reconbatchjobs

    Batch job processor for reconstruction scripts

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reconbatchjobs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RECONBATCHJOBS_METADATA)
    execution.params(params)
    args = reconbatchjobs_cargs(params, execution)
    out = reconbatchjobs_outputs(params, execution)
    execution.run(args)
    return out

def reconbatchjobs(
    logfile: str,
    cmdfiles: list[str],
    runner: Runner | None = None,
) -> ReconbatchjobsOutputs:
    """
    reconbatchjobs

    Batch job processor for reconstruction scripts

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        logfile: Log file to capture output of batch jobs
        cmdfiles: Command files for batch processing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reconbatchjobs_params(
        logfile=logfile,
        cmdfiles=cmdfiles,
    )
    return reconbatchjobs_execute(params, runner)

__all__ = [
    "Reconbatchjobs",
    "ReconbatchjobsOutputs",
    "RECONBATCHJOBS_METADATA",
    "reconbatchjobs_cargs",
    "reconbatchjobs_outputs",
    "reconbatchjobs_params",
    "reconbatchjobs_execute",
    "reconbatchjobs_validate",
    "reconbatchjobs",
]