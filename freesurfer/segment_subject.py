# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_SUBJECT_METADATA = Metadata(
    id="segment_subject",
    name="segment_subject",
    package="freesurfer",
)

SegmentSubject = typing.TypedDict(
    "SegmentSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segment_subject"]],
        "input_volume": InputPathType,
        "output_xfm": str,
        "log_file": typing.NotRequired[str],
        "help_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SegmentSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xfm_file: OutputPathType
    """Output transformation file"""

def segment_subject_params(
    input_volume: InputPathType,
    output_xfm: str,
    log_file: str | None = None,
    help_flag: bool = False,
    debug_flag: bool = False,
    version_flag: bool = False,
) -> SegmentSubject:
    """
    Build parameters.

    Args:
        input_volume: Input volume
        output_xfm: Output transformation file
        log_file: Log file. Default is outdir/talarach.log
        help_flag: Print help and exit
        debug_flag: Turn on debugging
        version_flag: Print version and exit

    Returns:
        Parameter dictionary.
    """
    params: SegmentSubject = {
        "@type": "freesurfer/segment_subject",
        "input_volume": input_volume,
        "output_xfm": output_xfm,
        "help_flag": help_flag,
        "debug_flag": debug_flag,
        "version_flag": version_flag,
    }
    if log_file is not None:
        params["log_file"] = log_file
    return params

def segment_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_xfm", None) is None:
        raise StyxValidationError("`output_xfm` must not be None")
    if not isinstance(params["output_xfm"], str):
        raise StyxValidationError(f'`output_xfm` has the wrong type: Received `{type(params["output_xfm"])}` expected `str`')
    if params.get("log_file", None) is not None:
        if not isinstance(params["log_file"], str):
            raise StyxValidationError(f'`log_file` has the wrong type: Received `{type(params["log_file"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')

def segment_subject_cargs(params: SegmentSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segment_subject")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--xfm")
    cargs.append(params["output_xfm"])
    v_0 = params.get("log_file")
    if v_0 is not None:
        cargs.append("--log")
        cargs.append(v_0)
    if params.get("help_flag"):
        cargs.append("--help")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("version_flag"):
        cargs.append("--version")
    return cargs

def segment_subject_outputs(params: SegmentSubject, execution: Execution) -> SegmentSubjectOutputs:
    """Build the SegmentSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xfm_file_v: OutputPathType = execution.output_file(params["output_xfm"])
    return SegmentSubjectOutputs(
        root=root_v,
        output_xfm_file=output_xfm_file_v,
    )

def segment_subject_execute(params: SegmentSubject, runner: Runner | None = None) -> SegmentSubjectOutputs:
    """
    segment_subject

    Front-end for MINCs mritotal to compute the Talairach transform that maps the input volume to the MNI305 space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_SUBJECT_METADATA)
    execution.params(params)
    args = segment_subject_cargs(params, execution)
    out = segment_subject_outputs(params, execution)
    execution.run(args)
    return out

def segment_subject(
    input_volume: InputPathType,
    output_xfm: str,
    log_file: str | None = None,
    help_flag: bool = False,
    debug_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> SegmentSubjectOutputs:
    """
    segment_subject

    Front-end for MINCs mritotal to compute the Talairach transform that maps the input volume to the MNI305 space.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume
        output_xfm: Output transformation file
        log_file: Log file. Default is outdir/talarach.log
        help_flag: Print help and exit
        debug_flag: Turn on debugging
        version_flag: Print version and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_subject_params(
        input_volume=input_volume,
        output_xfm=output_xfm,
        log_file=log_file,
        help_flag=help_flag,
        debug_flag=debug_flag,
        version_flag=version_flag,
    )
    return segment_subject_execute(params, runner)

__all__ = [
    "SegmentSubject",
    "SegmentSubjectOutputs",
    "SEGMENT_SUBJECT_METADATA",
    "segment_subject_cargs",
    "segment_subject_outputs",
    "segment_subject_params",
    "segment_subject_execute",
    "segment_subject_validate",
    "segment_subject",
]