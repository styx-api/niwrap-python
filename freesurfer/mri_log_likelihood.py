# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_LOG_LIKELIHOOD_METADATA = Metadata(
    id="mri_log_likelihood",
    name="mri_log_likelihood",
    package="freesurfer",
)

MriLogLikelihood = typing.TypedDict(
    "MriLogLikelihood",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_log_likelihood"]],
        "input_brain_images": list[InputPathType],
        "atlas_file": InputPathType,
        "transform_file": InputPathType,
    },
)

@dataclasses.dataclass
class MriLogLikelihoodOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_log_likelihood_params(
    input_brain_images: list[InputPathType],
    atlas_file: InputPathType,
    transform_file: InputPathType,
) -> MriLogLikelihood:
    """
    Build parameters.

    Args:
        input_brain_images: List of input brain images
        atlas_file: Atlas image file
        transform_file: Transform file for registration

    Returns:
        Parameter dictionary.
    """
    params: MriLogLikelihood = {
        "@type": "freesurfer/mri_log_likelihood",
        "input_brain_images": input_brain_images,
        "atlas_file": atlas_file,
        "transform_file": transform_file,
    }
    return params

def mri_log_likelihood_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriLogLikelihood."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_brain_images", None) is None:
        raise StyxValidationError("`input_brain_images` must not be None")
    if not isinstance(params["input_brain_images"], list):
        raise StyxValidationError(f'`input_brain_images` has the wrong type: Received `{type(params["input_brain_images"])}` expected `list[InputPathType]`')
    for e in params["input_brain_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_brain_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("atlas_file", None) is None:
        raise StyxValidationError("`atlas_file` must not be None")
    if not isinstance(params["atlas_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_file` has the wrong type: Received `{type(params["atlas_file"])}` expected `InputPathType`')
    if params.get("transform_file", None) is None:
        raise StyxValidationError("`transform_file` must not be None")
    if not isinstance(params["transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform_file"])}` expected `InputPathType`')

def mri_log_likelihood_cargs(params: MriLogLikelihood, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_log_likelihood")
    for item0 in params["input_brain_images"]:
        cargs.append(execution.input_file(item0))
    cargs.append(execution.input_file(params["atlas_file"]))
    cargs.append(execution.input_file(params["transform_file"]))
    return cargs

def mri_log_likelihood_outputs(params: MriLogLikelihood, execution: Execution) -> MriLogLikelihoodOutputs:
    """Build the MriLogLikelihoodOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriLogLikelihoodOutputs(
        root=root_v,
    )

def mri_log_likelihood_execute(params: MriLogLikelihood, runner: Runner | None = None) -> MriLogLikelihoodOutputs:
    """
    mri_log_likelihood

    MRI log likelihood calculation tool for brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_log_likelihood_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_LOG_LIKELIHOOD_METADATA)
    execution.params(params)
    args = mri_log_likelihood_cargs(params, execution)
    out = mri_log_likelihood_outputs(params, execution)
    execution.run(args)
    return out

def mri_log_likelihood(
    input_brain_images: list[InputPathType],
    atlas_file: InputPathType,
    transform_file: InputPathType,
    runner: Runner | None = None,
) -> MriLogLikelihoodOutputs:
    """
    mri_log_likelihood

    MRI log likelihood calculation tool for brain images.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_brain_images: List of input brain images
        atlas_file: Atlas image file
        transform_file: Transform file for registration
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_log_likelihood_params(
        input_brain_images=input_brain_images,
        atlas_file=atlas_file,
        transform_file=transform_file,
    )
    return mri_log_likelihood_execute(params, runner)

__all__ = [
    "MriLogLikelihood",
    "MriLogLikelihoodOutputs",
    "MRI_LOG_LIKELIHOOD_METADATA",
    "mri_log_likelihood_cargs",
    "mri_log_likelihood_outputs",
    "mri_log_likelihood_params",
    "mri_log_likelihood_execute",
    "mri_log_likelihood_validate",
    "mri_log_likelihood",
]