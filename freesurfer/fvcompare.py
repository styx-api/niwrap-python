# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FVCOMPARE_METADATA = Metadata(
    id="fvcompare",
    name="fvcompare",
    package="freesurfer",
)

Fvcompare = typing.TypedDict(
    "Fvcompare",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fvcompare"]],
        "subject1": str,
        "subject2": str,
        "subject_dir1": typing.NotRequired[str],
        "subject_dir2": typing.NotRequired[str],
        "name1": typing.NotRequired[str],
        "name2": typing.NotRequired[str],
        "color1": typing.NotRequired[str],
        "volume": typing.NotRequired[str],
        "segmentation": typing.NotRequired[str],
        "aseg": typing.NotRequired[bool],
        "no_seg": typing.NotRequired[bool],
        "left_hemi": typing.NotRequired[bool],
        "right_hemi": typing.NotRequired[bool],
        "no_surf": typing.NotRequired[bool],
        "gray_levels": typing.NotRequired[list[float]],
        "cursor_position": typing.NotRequired[list[float]],
        "zoom_level": typing.NotRequired[float],
        "annotation": typing.NotRequired[str],
        "aparc": typing.NotRequired[bool],
        "inflated": typing.NotRequired[bool],
        "white": typing.NotRequired[bool],
        "orig": typing.NotRequired[bool],
        "surf_name": typing.NotRequired[str],
        "pointset": typing.NotRequired[InputPathType],
        "wot2": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FvcompareOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fvcompare_params(
    subject1: str,
    subject2: str,
    subject_dir1: str | None = None,
    subject_dir2: str | None = None,
    name1: str | None = None,
    name2: str | None = None,
    color1: str | None = None,
    volume: str | None = None,
    segmentation: str | None = None,
    aseg: bool = False,
    no_seg: bool = False,
    left_hemi: bool = False,
    right_hemi: bool = False,
    no_surf: bool = False,
    gray_levels: list[float] | None = None,
    cursor_position: list[float] | None = None,
    zoom_level: float | None = None,
    annotation: str | None = None,
    aparc: bool = False,
    inflated: bool = False,
    white: bool = False,
    orig: bool = False,
    surf_name: str | None = None,
    pointset: InputPathType | None = None,
    wot2: bool = False,
) -> Fvcompare:
    """
    Build parameters.

    Args:
        subject1: Subject 1 identifier
        subject2: Subject 2 identifier
        subject_dir1: Directory path for Subject 1
        subject_dir2: Directory path for Subject 2
        name1: Name associated with Subject 1 (default: s1)
        name2: Name associated with Subject 2 (default: s2)
        color1: Set surface colors for Subject 1
        volume: Volume name found in subject/mri (default: brainmask.mgz, can have\
            multiple)
        segmentation: Segmentation name found in subject/mri (default:\
            aparc+aseg.mgz, can have multiple)
        aseg: Add aseg.mgz to segmentation list
        no_seg: Do not display segmentations
        left_hemi: Only display left hemisphere
        right_hemi: Only display right hemisphere
        no_surf: Do not display surfaces
        gray_levels: Set gray scale levels
        cursor_position: Place cursor at given location and center Field of View
        zoom_level: Set zoom level
        annotation: Load annotation onto surfaces
        aparc: Load aparc.annot onto surfaces
        inflated: Load inflated surfaces in addition to white and pial
        white: Only show white surfaces
        orig: Only show orig.nofix and orig surfaces
        surf_name: Only show specified surface name
        pointset: Load point set file
        wot2: Include the ?h.woT2.pial surfs

    Returns:
        Parameter dictionary.
    """
    params: Fvcompare = {
        "@type": "freesurfer/fvcompare",
        "subject1": subject1,
        "subject2": subject2,
        "aseg": aseg,
        "no_seg": no_seg,
        "left_hemi": left_hemi,
        "right_hemi": right_hemi,
        "no_surf": no_surf,
        "aparc": aparc,
        "inflated": inflated,
        "white": white,
        "orig": orig,
        "wot2": wot2,
    }
    if subject_dir1 is not None:
        params["subject_dir1"] = subject_dir1
    if subject_dir2 is not None:
        params["subject_dir2"] = subject_dir2
    if name1 is not None:
        params["name1"] = name1
    if name2 is not None:
        params["name2"] = name2
    if color1 is not None:
        params["color1"] = color1
    if volume is not None:
        params["volume"] = volume
    if segmentation is not None:
        params["segmentation"] = segmentation
    if gray_levels is not None:
        params["gray_levels"] = gray_levels
    if cursor_position is not None:
        params["cursor_position"] = cursor_position
    if zoom_level is not None:
        params["zoom_level"] = zoom_level
    if annotation is not None:
        params["annotation"] = annotation
    if surf_name is not None:
        params["surf_name"] = surf_name
    if pointset is not None:
        params["pointset"] = pointset
    return params

def fvcompare_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fvcompare."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject1", None) is None:
        raise StyxValidationError("`subject1` must not be None")
    if not isinstance(params["subject1"], str):
        raise StyxValidationError(f'`subject1` has the wrong type: Received `{type(params["subject1"])}` expected `str`')
    if params.get("subject2", None) is None:
        raise StyxValidationError("`subject2` must not be None")
    if not isinstance(params["subject2"], str):
        raise StyxValidationError(f'`subject2` has the wrong type: Received `{type(params["subject2"])}` expected `str`')
    if params.get("subject_dir1", None) is not None:
        if not isinstance(params["subject_dir1"], str):
            raise StyxValidationError(f'`subject_dir1` has the wrong type: Received `{type(params["subject_dir1"])}` expected `str`')
    if params.get("subject_dir2", None) is not None:
        if not isinstance(params["subject_dir2"], str):
            raise StyxValidationError(f'`subject_dir2` has the wrong type: Received `{type(params["subject_dir2"])}` expected `str`')
    if params.get("name1", None) is not None:
        if not isinstance(params["name1"], str):
            raise StyxValidationError(f'`name1` has the wrong type: Received `{type(params["name1"])}` expected `str`')
    if params.get("name2", None) is not None:
        if not isinstance(params["name2"], str):
            raise StyxValidationError(f'`name2` has the wrong type: Received `{type(params["name2"])}` expected `str`')
    if params.get("color1", None) is not None:
        if not isinstance(params["color1"], str):
            raise StyxValidationError(f'`color1` has the wrong type: Received `{type(params["color1"])}` expected `str`')
    if params.get("volume", None) is not None:
        if not isinstance(params["volume"], str):
            raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `str`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], str):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `str`')
    if params.get("aseg", None) is not None:
        if not isinstance(params["aseg"], bool):
            raise StyxValidationError(f'`aseg` has the wrong type: Received `{type(params["aseg"])}` expected `bool`')
    if params.get("no_seg", None) is not None:
        if not isinstance(params["no_seg"], bool):
            raise StyxValidationError(f'`no_seg` has the wrong type: Received `{type(params["no_seg"])}` expected `bool`')
    if params.get("left_hemi", None) is not None:
        if not isinstance(params["left_hemi"], bool):
            raise StyxValidationError(f'`left_hemi` has the wrong type: Received `{type(params["left_hemi"])}` expected `bool`')
    if params.get("right_hemi", None) is not None:
        if not isinstance(params["right_hemi"], bool):
            raise StyxValidationError(f'`right_hemi` has the wrong type: Received `{type(params["right_hemi"])}` expected `bool`')
    if params.get("no_surf", None) is not None:
        if not isinstance(params["no_surf"], bool):
            raise StyxValidationError(f'`no_surf` has the wrong type: Received `{type(params["no_surf"])}` expected `bool`')
    if params.get("gray_levels", None) is not None:
        if not isinstance(params["gray_levels"], list):
            raise StyxValidationError(f'`gray_levels` has the wrong type: Received `{type(params["gray_levels"])}` expected `list[float]`')
        if not (2 <= len(params["gray_levels"]) <= 2):
            raise StyxValidationError("Parameter `gray_levels` must contain between 2 and 2 elements (inclusive)")
        for e in params["gray_levels"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`gray_levels` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("cursor_position", None) is not None:
        if not isinstance(params["cursor_position"], list):
            raise StyxValidationError(f'`cursor_position` has the wrong type: Received `{type(params["cursor_position"])}` expected `list[float]`')
        if not (3 <= len(params["cursor_position"]) <= 3):
            raise StyxValidationError("Parameter `cursor_position` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["cursor_position"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`cursor_position` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("zoom_level", None) is not None:
        if not isinstance(params["zoom_level"], (float, int)):
            raise StyxValidationError(f'`zoom_level` has the wrong type: Received `{type(params["zoom_level"])}` expected `float`')
    if params.get("annotation", None) is not None:
        if not isinstance(params["annotation"], str):
            raise StyxValidationError(f'`annotation` has the wrong type: Received `{type(params["annotation"])}` expected `str`')
    if params.get("aparc", None) is not None:
        if not isinstance(params["aparc"], bool):
            raise StyxValidationError(f'`aparc` has the wrong type: Received `{type(params["aparc"])}` expected `bool`')
    if params.get("inflated", None) is not None:
        if not isinstance(params["inflated"], bool):
            raise StyxValidationError(f'`inflated` has the wrong type: Received `{type(params["inflated"])}` expected `bool`')
    if params.get("white", None) is not None:
        if not isinstance(params["white"], bool):
            raise StyxValidationError(f'`white` has the wrong type: Received `{type(params["white"])}` expected `bool`')
    if params.get("orig", None) is not None:
        if not isinstance(params["orig"], bool):
            raise StyxValidationError(f'`orig` has the wrong type: Received `{type(params["orig"])}` expected `bool`')
    if params.get("surf_name", None) is not None:
        if not isinstance(params["surf_name"], str):
            raise StyxValidationError(f'`surf_name` has the wrong type: Received `{type(params["surf_name"])}` expected `str`')
    if params.get("pointset", None) is not None:
        if not isinstance(params["pointset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`pointset` has the wrong type: Received `{type(params["pointset"])}` expected `InputPathType`')
    if params.get("wot2", None) is not None:
        if not isinstance(params["wot2"], bool):
            raise StyxValidationError(f'`wot2` has the wrong type: Received `{type(params["wot2"])}` expected `bool`')

def fvcompare_cargs(params: Fvcompare, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fvcompare")
    cargs.append("--s1")
    cargs.append(params["subject1"])
    cargs.append("--s2")
    cargs.append(params["subject2"])
    v_0 = params.get("subject_dir1")
    if v_0 is not None:
        cargs.append("--sd1")
        cargs.append(v_0)
    v_1 = params.get("subject_dir2")
    if v_1 is not None:
        cargs.append("--sd2")
        cargs.append(v_1)
    v_2 = params.get("name1")
    if v_2 is not None:
        cargs.append("--n1")
        cargs.append(v_2)
    v_3 = params.get("name2")
    if v_3 is not None:
        cargs.append("--n2")
        cargs.append(v_3)
    v_4 = params.get("color1")
    if v_4 is not None:
        cargs.append("--c1")
        cargs.append(v_4)
    v_5 = params.get("volume")
    if v_5 is not None:
        cargs.append("--vol")
        cargs.append(v_5)
    v_6 = params.get("segmentation")
    if v_6 is not None:
        cargs.append("--seg")
        cargs.append(v_6)
    if params.get("aseg"):
        cargs.append("--aseg")
    if params.get("no_seg"):
        cargs.append("--no-seg")
    if params.get("left_hemi"):
        cargs.append("--lh")
    if params.get("right_hemi"):
        cargs.append("--rh")
    if params.get("no_surf"):
        cargs.append("--no-surf")
    v_7 = params.get("gray_levels")
    if v_7 is not None:
        cargs.append("--gray")
        for item0 in v_7:
            cargs.append(str(item0))
    v_8 = params.get("cursor_position")
    if v_8 is not None:
        cargs.append("--crs")
        for item1 in v_8:
            cargs.append(str(item1))
    v_9 = params.get("zoom_level")
    if v_9 is not None:
        cargs.append("--zoom")
        cargs.append(str(v_9))
    v_10 = params.get("annotation")
    if v_10 is not None:
        cargs.append("--annot")
        cargs.append(v_10)
    if params.get("aparc"):
        cargs.append("--aparc")
    if params.get("inflated"):
        cargs.append("--inflated")
    if params.get("white"):
        cargs.append("--white")
    if params.get("orig"):
        cargs.append("--orig")
    v_11 = params.get("surf_name")
    if v_11 is not None:
        cargs.append("--surf")
        cargs.append(v_11)
    v_12 = params.get("pointset")
    if v_12 is not None:
        cargs.append("--p")
        cargs.append(execution.input_file(v_12))
    if params.get("wot2"):
        cargs.append("--wot2")
    return cargs

def fvcompare_outputs(params: Fvcompare, execution: Execution) -> FvcompareOutputs:
    """Build the FvcompareOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FvcompareOutputs(
        root=root_v,
    )

def fvcompare_execute(params: Fvcompare, runner: Runner | None = None) -> FvcompareOutputs:
    """
    fvcompare

    Simultaneously loads volume, segmentation, and surface data from two subjects in freeview, for comparing across time or different analysis methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fvcompare_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FVCOMPARE_METADATA)
    execution.params(params)
    args = fvcompare_cargs(params, execution)
    out = fvcompare_outputs(params, execution)
    execution.run(args)
    return out

def fvcompare(
    subject1: str,
    subject2: str,
    subject_dir1: str | None = None,
    subject_dir2: str | None = None,
    name1: str | None = None,
    name2: str | None = None,
    color1: str | None = None,
    volume: str | None = None,
    segmentation: str | None = None,
    aseg: bool = False,
    no_seg: bool = False,
    left_hemi: bool = False,
    right_hemi: bool = False,
    no_surf: bool = False,
    gray_levels: list[float] | None = None,
    cursor_position: list[float] | None = None,
    zoom_level: float | None = None,
    annotation: str | None = None,
    aparc: bool = False,
    inflated: bool = False,
    white: bool = False,
    orig: bool = False,
    surf_name: str | None = None,
    pointset: InputPathType | None = None,
    wot2: bool = False,
    runner: Runner | None = None,
) -> FvcompareOutputs:
    """
    fvcompare

    Simultaneously loads volume, segmentation, and surface data from two subjects in freeview, for comparing across time or different analysis methods.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject1: Subject 1 identifier
        subject2: Subject 2 identifier
        subject_dir1: Directory path for Subject 1
        subject_dir2: Directory path for Subject 2
        name1: Name associated with Subject 1 (default: s1)
        name2: Name associated with Subject 2 (default: s2)
        color1: Set surface colors for Subject 1
        volume: Volume name found in subject/mri (default: brainmask.mgz, can have\
            multiple)
        segmentation: Segmentation name found in subject/mri (default:\
            aparc+aseg.mgz, can have multiple)
        aseg: Add aseg.mgz to segmentation list
        no_seg: Do not display segmentations
        left_hemi: Only display left hemisphere
        right_hemi: Only display right hemisphere
        no_surf: Do not display surfaces
        gray_levels: Set gray scale levels
        cursor_position: Place cursor at given location and center Field of View
        zoom_level: Set zoom level
        annotation: Load annotation onto surfaces
        aparc: Load aparc.annot onto surfaces
        inflated: Load inflated surfaces in addition to white and pial
        white: Only show white surfaces
        orig: Only show orig.nofix and orig surfaces
        surf_name: Only show specified surface name
        pointset: Load point set file
        wot2: Include the ?h.woT2.pial surfs
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fvcompare_params(
        subject1=subject1,
        subject2=subject2,
        subject_dir1=subject_dir1,
        subject_dir2=subject_dir2,
        name1=name1,
        name2=name2,
        color1=color1,
        volume=volume,
        segmentation=segmentation,
        aseg=aseg,
        no_seg=no_seg,
        left_hemi=left_hemi,
        right_hemi=right_hemi,
        no_surf=no_surf,
        gray_levels=gray_levels,
        cursor_position=cursor_position,
        zoom_level=zoom_level,
        annotation=annotation,
        aparc=aparc,
        inflated=inflated,
        white=white,
        orig=orig,
        surf_name=surf_name,
        pointset=pointset,
        wot2=wot2,
    )
    return fvcompare_execute(params, runner)

__all__ = [
    "Fvcompare",
    "FvcompareOutputs",
    "FVCOMPARE_METADATA",
    "fvcompare_cargs",
    "fvcompare_outputs",
    "fvcompare_params",
    "fvcompare_execute",
    "fvcompare_validate",
    "fvcompare",
]