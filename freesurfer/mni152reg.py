# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MNI152REG_METADATA = Metadata(
    id="mni152reg",
    name="mni152reg",
    package="freesurfer",
)

Mni152reg = typing.TypedDict(
    "Mni152reg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mni152reg"]],
        "subject": str,
        "register_1mm": typing.NotRequired[bool],
        "output": typing.NotRequired[str],
        "symmetric": typing.NotRequired[bool],
        "save_volume": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Mni152regOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reg_matrix_2mm: OutputPathType
    """Output registration matrix for standard 2mm target"""
    reg_matrix_1mm: OutputPathType
    """Output registration matrix for 1mm target when --1 flag is used"""

def mni152reg_params(
    subject: str,
    register_1mm: bool = False,
    output: str | None = None,
    symmetric: bool = False,
    save_volume: bool = False,
) -> Mni152reg:
    """
    Build parameters.

    Args:
        subject: FreeSurfer subject ID
        register_1mm: Register to 1mm target (instead of 2mm)
        output: Explicitly set output registration file
        symmetric: Register to FSL symmetric target
        save_volume: Sample original to output space

    Returns:
        Parameter dictionary.
    """
    params: Mni152reg = {
        "@type": "freesurfer/mni152reg",
        "subject": subject,
        "register_1mm": register_1mm,
        "symmetric": symmetric,
        "save_volume": save_volume,
    }
    if output is not None:
        params["output"] = output
    return params

def mni152reg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mni152reg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("register_1mm", None) is not None:
        if not isinstance(params["register_1mm"], bool):
            raise StyxValidationError(f'`register_1mm` has the wrong type: Received `{type(params["register_1mm"])}` expected `bool`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("symmetric", None) is not None:
        if not isinstance(params["symmetric"], bool):
            raise StyxValidationError(f'`symmetric` has the wrong type: Received `{type(params["symmetric"])}` expected `bool`')
    if params.get("save_volume", None) is not None:
        if not isinstance(params["save_volume"], bool):
            raise StyxValidationError(f'`save_volume` has the wrong type: Received `{type(params["save_volume"])}` expected `bool`')

def mni152reg_cargs(params: Mni152reg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mni152reg")
    cargs.append("-s")
    cargs.append(params["subject"])
    if params.get("register_1mm"):
        cargs.append("--1")
    v_0 = params.get("output")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    if params.get("symmetric"):
        cargs.append("--sym")
    if params.get("save_volume"):
        cargs.append("--save-vol")
    return cargs

def mni152reg_outputs(params: Mni152reg, execution: Execution) -> Mni152regOutputs:
    """Build the Mni152regOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reg_matrix_2mm_v: OutputPathType = execution.output_file(f'$SUBJECTS_DIR/{params["subject"]}/mri/transforms/reg.mni152.2mm.dat')
    reg_matrix_1mm_v: OutputPathType = execution.output_file(f'$SUBJECTS_DIR/{params["subject"]}/mri/transforms/reg.mni152.1mm.dat')
    return Mni152regOutputs(
        root=root_v,
        reg_matrix_2mm=reg_matrix_2mm_v,
        reg_matrix_1mm=reg_matrix_1mm_v,
    )

def mni152reg_execute(params: Mni152reg, runner: Runner | None = None) -> Mni152regOutputs:
    """
    mni152reg

    Registers the FreeSurfer subject to the FSL MNI 152 brain to create a tkregister-style registration matrix.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mni152reg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MNI152REG_METADATA)
    execution.params(params)
    args = mni152reg_cargs(params, execution)
    out = mni152reg_outputs(params, execution)
    execution.run(args)
    return out

def mni152reg(
    subject: str,
    register_1mm: bool = False,
    output: str | None = None,
    symmetric: bool = False,
    save_volume: bool = False,
    runner: Runner | None = None,
) -> Mni152regOutputs:
    """
    mni152reg

    Registers the FreeSurfer subject to the FSL MNI 152 brain to create a tkregister-style registration matrix.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: FreeSurfer subject ID
        register_1mm: Register to 1mm target (instead of 2mm)
        output: Explicitly set output registration file
        symmetric: Register to FSL symmetric target
        save_volume: Sample original to output space
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mni152reg_params(
        subject=subject,
        register_1mm=register_1mm,
        output=output,
        symmetric=symmetric,
        save_volume=save_volume,
    )
    return mni152reg_execute(params, runner)

__all__ = [
    "Mni152reg",
    "Mni152regOutputs",
    "MNI152REG_METADATA",
    "mni152reg_cargs",
    "mni152reg_outputs",
    "mni152reg_params",
    "mni152reg_execute",
    "mni152reg_validate",
    "mni152reg",
]