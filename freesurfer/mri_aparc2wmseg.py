# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_APARC2WMSEG_METADATA = Metadata(
    id="mri_aparc2wmseg",
    name="mri_aparc2wmseg",
    package="freesurfer",
)

MriAparc2wmseg = typing.TypedDict(
    "MriAparc2wmseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_aparc2wmseg"]],
        "subject": str,
        "wmseg_file": str,
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriAparc2wmsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_aparc2wmseg_params(
    subject: str,
    wmseg_file: str,
    help: bool = False,
    version: bool = False,
) -> MriAparc2wmseg:
    """
    Build parameters.

    Args:
        subject: The subject identifier used in FreeSurfer.
        wmseg_file: File to store the output white matter segmentation.
        help: Print out information on how to use this program.
        version: Print out version and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriAparc2wmseg = {
        "@type": "freesurfer/mri_aparc2wmseg",
        "subject": subject,
        "wmseg_file": wmseg_file,
        "help": help,
        "version": version,
    }
    return params

def mri_aparc2wmseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAparc2wmseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("wmseg_file", None) is None:
        raise StyxValidationError("`wmseg_file` must not be None")
    if not isinstance(params["wmseg_file"], str):
        raise StyxValidationError(f'`wmseg_file` has the wrong type: Received `{type(params["wmseg_file"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mri_aparc2wmseg_cargs(params: MriAparc2wmseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_aparc2wmseg")
    cargs.append("--s")
    cargs.append(params["subject"])
    cargs.append("--wmseg")
    cargs.append(params["wmseg_file"])
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mri_aparc2wmseg_outputs(params: MriAparc2wmseg, execution: Execution) -> MriAparc2wmsegOutputs:
    """Build the MriAparc2wmsegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriAparc2wmsegOutputs(
        root=root_v,
    )

def mri_aparc2wmseg_execute(params: MriAparc2wmseg, runner: Runner | None = None) -> MriAparc2wmsegOutputs:
    """
    mri_aparc2wmseg

    A tool to convert aparc+aseg.mgz annotations into a white matter segmentation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_aparc2wmseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_APARC2WMSEG_METADATA)
    execution.params(params)
    args = mri_aparc2wmseg_cargs(params, execution)
    out = mri_aparc2wmseg_outputs(params, execution)
    execution.run(args)
    return out

def mri_aparc2wmseg(
    subject: str,
    wmseg_file: str,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MriAparc2wmsegOutputs:
    """
    mri_aparc2wmseg

    A tool to convert aparc+aseg.mgz annotations into a white matter segmentation file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: The subject identifier used in FreeSurfer.
        wmseg_file: File to store the output white matter segmentation.
        help: Print out information on how to use this program.
        version: Print out version and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_aparc2wmseg_params(
        subject=subject,
        wmseg_file=wmseg_file,
        help=help,
        version=version,
    )
    return mri_aparc2wmseg_execute(params, runner)

__all__ = [
    "MriAparc2wmseg",
    "MriAparc2wmsegOutputs",
    "MRI_APARC2WMSEG_METADATA",
    "mri_aparc2wmseg_cargs",
    "mri_aparc2wmseg_outputs",
    "mri_aparc2wmseg_params",
    "mri_aparc2wmseg_execute",
    "mri_aparc2wmseg_validate",
    "mri_aparc2wmseg",
]