# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

T4IMGS_4DFP_METADATA = Metadata(
    id="t4imgs_4dfp",
    name="t4imgs_4dfp",
    package="freesurfer",
)

T4imgs4dfp = typing.TypedDict(
    "T4imgs4dfp",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/t4imgs_4dfp"]],
        "sqrt_normalize": typing.NotRequired[bool],
        "cubic_spline": typing.NotRequired[bool],
        "output_nan": typing.NotRequired[bool],
        "convert_t4": typing.NotRequired[bool],
        "nearest_neighbor": typing.NotRequired[bool],
        "output_111_space": typing.NotRequired[bool],
        "output_222_space": typing.NotRequired[bool],
        "output_333n_space": typing.NotRequired[str],
        "duplicate_dimensions": typing.NotRequired[str],
        "big_endian": typing.NotRequired[bool],
        "little_endian": typing.NotRequired[bool],
        "input_images": list[InputPathType],
        "output_image": str,
    },
)

@dataclasses.dataclass
class T4imgs4dfpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transformed_image: OutputPathType
    """Transformed 4dfp image as output."""

def t4imgs_4dfp_params(
    input_images: list[InputPathType],
    output_image: str,
    sqrt_normalize: bool = False,
    cubic_spline: bool = False,
    output_nan: bool = False,
    convert_t4: bool = False,
    nearest_neighbor: bool = False,
    output_111_space: bool = False,
    output_222_space: bool = False,
    output_333n_space: str | None = None,
    duplicate_dimensions: str | None = None,
    big_endian: bool = False,
    little_endian: bool = False,
) -> T4imgs4dfp:
    """
    Build parameters.

    Args:
        input_images: Input list of 4dfp images.
        output_image: Output file name for the transformed image.
        sqrt_normalize: Normalize by sqrt(n) rather than n (for z images).
        cubic_spline: Interpolate by 3D cubic spline (default is 3D linear).
        output_nan: Output NaN (default 0.0) for undefined values.
        convert_t4: Internally convert to_711-2A_t4->to_711-2B_t4.
        nearest_neighbor: Use nearest neighbor interpolation.
        output_111_space: Output in 111 space instead of default 333.0 space.
        output_222_space: Output in 222 space instead of default 333.0 space.
        output_333n_space: Output in 333.n space (y shifted up by n pixels).
        duplicate_dimensions: Duplicate dimensions of specified image.
        big_endian: Output in big endian format.
        little_endian: Output in little endian format.

    Returns:
        Parameter dictionary.
    """
    params: T4imgs4dfp = {
        "@type": "freesurfer/t4imgs_4dfp",
        "input_images": input_images,
        "output_image": output_image,
        "sqrt_normalize": sqrt_normalize,
        "cubic_spline": cubic_spline,
        "output_nan": output_nan,
        "convert_t4": convert_t4,
        "nearest_neighbor": nearest_neighbor,
        "output_111_space": output_111_space,
        "output_222_space": output_222_space,
        "big_endian": big_endian,
        "little_endian": little_endian,
    }
    if output_333n_space is not None:
        params["output_333n_space"] = output_333n_space
    if duplicate_dimensions is not None:
        params["duplicate_dimensions"] = duplicate_dimensions
    return params

def t4imgs_4dfp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid T4imgs4dfp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sqrt_normalize", None) is not None:
        if not isinstance(params["sqrt_normalize"], bool):
            raise StyxValidationError(f'`sqrt_normalize` has the wrong type: Received `{type(params["sqrt_normalize"])}` expected `bool`')
    if params.get("cubic_spline", None) is not None:
        if not isinstance(params["cubic_spline"], bool):
            raise StyxValidationError(f'`cubic_spline` has the wrong type: Received `{type(params["cubic_spline"])}` expected `bool`')
    if params.get("output_nan", None) is not None:
        if not isinstance(params["output_nan"], bool):
            raise StyxValidationError(f'`output_nan` has the wrong type: Received `{type(params["output_nan"])}` expected `bool`')
    if params.get("convert_t4", None) is not None:
        if not isinstance(params["convert_t4"], bool):
            raise StyxValidationError(f'`convert_t4` has the wrong type: Received `{type(params["convert_t4"])}` expected `bool`')
    if params.get("nearest_neighbor", None) is not None:
        if not isinstance(params["nearest_neighbor"], bool):
            raise StyxValidationError(f'`nearest_neighbor` has the wrong type: Received `{type(params["nearest_neighbor"])}` expected `bool`')
    if params.get("output_111_space", None) is not None:
        if not isinstance(params["output_111_space"], bool):
            raise StyxValidationError(f'`output_111_space` has the wrong type: Received `{type(params["output_111_space"])}` expected `bool`')
    if params.get("output_222_space", None) is not None:
        if not isinstance(params["output_222_space"], bool):
            raise StyxValidationError(f'`output_222_space` has the wrong type: Received `{type(params["output_222_space"])}` expected `bool`')
    if params.get("output_333n_space", None) is not None:
        if not isinstance(params["output_333n_space"], str):
            raise StyxValidationError(f'`output_333n_space` has the wrong type: Received `{type(params["output_333n_space"])}` expected `str`')
    if params.get("duplicate_dimensions", None) is not None:
        if not isinstance(params["duplicate_dimensions"], str):
            raise StyxValidationError(f'`duplicate_dimensions` has the wrong type: Received `{type(params["duplicate_dimensions"])}` expected `str`')
    if params.get("big_endian", None) is not None:
        if not isinstance(params["big_endian"], bool):
            raise StyxValidationError(f'`big_endian` has the wrong type: Received `{type(params["big_endian"])}` expected `bool`')
    if params.get("little_endian", None) is not None:
        if not isinstance(params["little_endian"], bool):
            raise StyxValidationError(f'`little_endian` has the wrong type: Received `{type(params["little_endian"])}` expected `bool`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')

def t4imgs_4dfp_cargs(params: T4imgs4dfp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("t4imgs_4dfp")
    if params.get("sqrt_normalize"):
        cargs.append("-z")
    if params.get("cubic_spline"):
        cargs.append("-s")
    if params.get("output_nan"):
        cargs.append("-N")
    if params.get("convert_t4"):
        cargs.append("-B")
    if params.get("nearest_neighbor"):
        cargs.append("-n")
    if params.get("output_111_space"):
        cargs.append("-O111")
    if params.get("output_222_space"):
        cargs.append("-O222")
    v_0 = params.get("output_333n_space")
    if v_0 is not None:
        cargs.append("-O333.n")
        cargs.append(v_0)
    v_1 = params.get("duplicate_dimensions")
    if v_1 is not None:
        cargs.append("-O")
        cargs.append(v_1)
    if params.get("big_endian"):
        cargs.append("-@b")
    if params.get("little_endian"):
        cargs.append("-@l")
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_image"])
    return cargs

def t4imgs_4dfp_outputs(params: T4imgs4dfp, execution: Execution) -> T4imgs4dfpOutputs:
    """Build the T4imgs4dfpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    transformed_image_v: OutputPathType = execution.output_file(f'{params["output_image"]}.4dfp.img')
    return T4imgs4dfpOutputs(
        root=root_v,
        transformed_image=transformed_image_v,
    )

def t4imgs_4dfp_execute(params: T4imgs4dfp, runner: Runner | None = None) -> T4imgs4dfpOutputs:
    """
    t4imgs_4dfp

    Freesurfer tool for transforming images according to a specified T4 file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    t4imgs_4dfp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(T4IMGS_4DFP_METADATA)
    execution.params(params)
    args = t4imgs_4dfp_cargs(params, execution)
    out = t4imgs_4dfp_outputs(params, execution)
    execution.run(args)
    return out

def t4imgs_4dfp(
    input_images: list[InputPathType],
    output_image: str,
    sqrt_normalize: bool = False,
    cubic_spline: bool = False,
    output_nan: bool = False,
    convert_t4: bool = False,
    nearest_neighbor: bool = False,
    output_111_space: bool = False,
    output_222_space: bool = False,
    output_333n_space: str | None = None,
    duplicate_dimensions: str | None = None,
    big_endian: bool = False,
    little_endian: bool = False,
    runner: Runner | None = None,
) -> T4imgs4dfpOutputs:
    """
    t4imgs_4dfp

    Freesurfer tool for transforming images according to a specified T4 file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_images: Input list of 4dfp images.
        output_image: Output file name for the transformed image.
        sqrt_normalize: Normalize by sqrt(n) rather than n (for z images).
        cubic_spline: Interpolate by 3D cubic spline (default is 3D linear).
        output_nan: Output NaN (default 0.0) for undefined values.
        convert_t4: Internally convert to_711-2A_t4->to_711-2B_t4.
        nearest_neighbor: Use nearest neighbor interpolation.
        output_111_space: Output in 111 space instead of default 333.0 space.
        output_222_space: Output in 222 space instead of default 333.0 space.
        output_333n_space: Output in 333.n space (y shifted up by n pixels).
        duplicate_dimensions: Duplicate dimensions of specified image.
        big_endian: Output in big endian format.
        little_endian: Output in little endian format.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = t4imgs_4dfp_params(
        input_images=input_images,
        output_image=output_image,
        sqrt_normalize=sqrt_normalize,
        cubic_spline=cubic_spline,
        output_nan=output_nan,
        convert_t4=convert_t4,
        nearest_neighbor=nearest_neighbor,
        output_111_space=output_111_space,
        output_222_space=output_222_space,
        output_333n_space=output_333n_space,
        duplicate_dimensions=duplicate_dimensions,
        big_endian=big_endian,
        little_endian=little_endian,
    )
    return t4imgs_4dfp_execute(params, runner)

__all__ = [
    "T4imgs4dfp",
    "T4imgs4dfpOutputs",
    "T4IMGS_4DFP_METADATA",
    "t4imgs_4dfp_cargs",
    "t4imgs_4dfp_outputs",
    "t4imgs_4dfp_params",
    "t4imgs_4dfp_execute",
    "t4imgs_4dfp_validate",
    "t4imgs_4dfp",
]