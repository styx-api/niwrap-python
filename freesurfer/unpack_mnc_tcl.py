# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACK_MNC_TCL_METADATA = Metadata(
    id="unpack_mnc.tcl",
    name="unpack_mnc.tcl",
    package="freesurfer",
)

UnpackMncTcl = typing.TypedDict(
    "UnpackMncTcl",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpack_mnc.tcl"]],
        "verbose": typing.NotRequired[bool],
        "output_dir": typing.NotRequired[str],
        "input_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class UnpackMncTclOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    unpacked_file: OutputPathType | None
    """Unpacked output MINC file"""

def unpack_mnc_tcl_params(
    verbose: bool = False,
    output_dir: str | None = None,
    input_file: InputPathType | None = None,
) -> UnpackMncTcl:
    """
    Build parameters.

    Args:
        verbose: Verbose output messages
        output_dir: Output directory for unpacked files
        input_file: Specify a custom input file for unpacking

    Returns:
        Parameter dictionary.
    """
    params: UnpackMncTcl = {
        "@type": "freesurfer/unpack_mnc.tcl",
        "verbose": verbose,
    }
    if output_dir is not None:
        params["output_dir"] = output_dir
    if input_file is not None:
        params["input_file"] = input_file
    return params

def unpack_mnc_tcl_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UnpackMncTcl."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("input_file", None) is not None:
        if not isinstance(params["input_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def unpack_mnc_tcl_cargs(params: UnpackMncTcl, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpack_mnc.tcl")
    if params.get("verbose"):
        cargs.append("-v")
    v_0 = params.get("output_dir")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("input_file")
    if v_1 is not None:
        cargs.append("-i")
        cargs.append(execution.input_file(v_1))
    return cargs

def unpack_mnc_tcl_outputs(params: UnpackMncTcl, execution: Execution) -> UnpackMncTclOutputs:
    """Build the UnpackMncTclOutputs object for this tool."""
    unpacked_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        unpacked_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/unpacked_data.mnc')
    return UnpackMncTclOutputs(
        root=root_v,
        unpacked_file=unpacked_file_v,
    )

def unpack_mnc_tcl_execute(params: UnpackMncTcl, runner: Runner | None = None) -> UnpackMncTclOutputs:
    """
    unpack_mnc.tcl

    A tool for unpacking MINC format images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpack_mnc_tcl_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACK_MNC_TCL_METADATA)
    execution.params(params)
    args = unpack_mnc_tcl_cargs(params, execution)
    out = unpack_mnc_tcl_outputs(params, execution)
    execution.run(args)
    return out

def unpack_mnc_tcl(
    verbose: bool = False,
    output_dir: str | None = None,
    input_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> UnpackMncTclOutputs:
    """
    unpack_mnc.tcl

    A tool for unpacking MINC format images

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        verbose: Verbose output messages
        output_dir: Output directory for unpacked files
        input_file: Specify a custom input file for unpacking
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpack_mnc_tcl_params(
        verbose=verbose,
        output_dir=output_dir,
        input_file=input_file,
    )
    return unpack_mnc_tcl_execute(params, runner)

__all__ = [
    "UnpackMncTcl",
    "UnpackMncTclOutputs",
    "UNPACK_MNC_TCL_METADATA",
    "unpack_mnc_tcl_cargs",
    "unpack_mnc_tcl_outputs",
    "unpack_mnc_tcl_params",
    "unpack_mnc_tcl_execute",
    "unpack_mnc_tcl_validate",
    "unpack_mnc_tcl",
]