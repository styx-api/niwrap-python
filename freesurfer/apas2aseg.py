# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APAS2ASEG_METADATA = Metadata(
    id="apas2aseg",
    name="apas2aseg",
    package="freesurfer",
)

Apas2aseg = typing.TypedDict(
    "Apas2aseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/apas2aseg"]],
        "subject": typing.NotRequired[str],
        "input_aparc_aseg": typing.NotRequired[InputPathType],
        "output_seg": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Apas2asegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_seg_file: OutputPathType | None
    """The output segmentation file resulting from the conversion process."""

def apas2aseg_params(
    subject: str | None = None,
    input_aparc_aseg: InputPathType | None = None,
    output_seg: str | None = None,
) -> Apas2aseg:
    """
    Build parameters.

    Args:
        subject: Subject identifier specifying the directory where the subject's\
            data is stored.
        input_aparc_aseg: Input aparc+aseg.mgz file to be converted.
        output_seg: Output file for the new segmentation (e.g., apas-aseg.mgz).

    Returns:
        Parameter dictionary.
    """
    params: Apas2aseg = {
        "@type": "freesurfer/apas2aseg",
    }
    if subject is not None:
        params["subject"] = subject
    if input_aparc_aseg is not None:
        params["input_aparc_aseg"] = input_aparc_aseg
    if output_seg is not None:
        params["output_seg"] = output_seg
    return params

def apas2aseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Apas2aseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("input_aparc_aseg", None) is not None:
        if not isinstance(params["input_aparc_aseg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_aparc_aseg` has the wrong type: Received `{type(params["input_aparc_aseg"])}` expected `InputPathType`')
    if params.get("output_seg", None) is not None:
        if not isinstance(params["output_seg"], str):
            raise StyxValidationError(f'`output_seg` has the wrong type: Received `{type(params["output_seg"])}` expected `str`')

def apas2aseg_cargs(params: Apas2aseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("apas2aseg")
    v_0 = params.get("subject")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    v_1 = params.get("input_aparc_aseg")
    if v_1 is not None:
        cargs.append("--i")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("output_seg")
    if v_2 is not None:
        cargs.append("--o")
        cargs.append(v_2)
    return cargs

def apas2aseg_outputs(params: Apas2aseg, execution: Execution) -> Apas2asegOutputs:
    """Build the Apas2asegOutputs object for this tool."""
    output_seg_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_seg")
    if __v0 is not None:
        output_seg_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return Apas2asegOutputs(
        root=root_v,
        output_seg_file=output_seg_file_v,
    )

def apas2aseg_execute(params: Apas2aseg, runner: Runner | None = None) -> Apas2asegOutputs:
    """
    apas2aseg

    Converts aparc+aseg.mgz into aseg.mgz-like format by replacing specific cortical segmentations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    apas2aseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APAS2ASEG_METADATA)
    execution.params(params)
    args = apas2aseg_cargs(params, execution)
    out = apas2aseg_outputs(params, execution)
    execution.run(args)
    return out

def apas2aseg(
    subject: str | None = None,
    input_aparc_aseg: InputPathType | None = None,
    output_seg: str | None = None,
    runner: Runner | None = None,
) -> Apas2asegOutputs:
    """
    apas2aseg

    Converts aparc+aseg.mgz into aseg.mgz-like format by replacing specific cortical segmentations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier specifying the directory where the subject's\
            data is stored.
        input_aparc_aseg: Input aparc+aseg.mgz file to be converted.
        output_seg: Output file for the new segmentation (e.g., apas-aseg.mgz).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = apas2aseg_params(
        subject=subject,
        input_aparc_aseg=input_aparc_aseg,
        output_seg=output_seg,
    )
    return apas2aseg_execute(params, runner)

__all__ = [
    "Apas2aseg",
    "Apas2asegOutputs",
    "APAS2ASEG_METADATA",
    "apas2aseg_cargs",
    "apas2aseg_outputs",
    "apas2aseg_params",
    "apas2aseg_execute",
    "apas2aseg_validate",
    "apas2aseg",
]