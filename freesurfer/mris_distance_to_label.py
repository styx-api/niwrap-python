# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_DISTANCE_TO_LABEL_METADATA = Metadata(
    id="mris_distance_to_label",
    name="mris_distance_to_label",
    package="freesurfer",
)

MrisDistanceToLabel = typing.TypedDict(
    "MrisDistanceToLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_distance_to_label"]],
        "hemisphere": str,
        "subject_1": str,
    },
)

@dataclasses.dataclass
class MrisDistanceToLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_distance_to_label_params(
    hemisphere: str,
    subject_1: str,
) -> MrisDistanceToLabel:
    """
    Build parameters.

    Args:
        hemisphere: Hemisphere to process (e.g., 'lh' for left hemisphere or 'rh'\
            for right hemisphere)
        subject_1: Subject identifier or path to the subject directory

    Returns:
        Parameter dictionary.
    """
    params: MrisDistanceToLabel = {
        "@type": "freesurfer/mris_distance_to_label",
        "hemisphere": hemisphere,
        "subject_1": subject_1,
    }
    return params

def mris_distance_to_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisDistanceToLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("subject_1", None) is None:
        raise StyxValidationError("`subject_1` must not be None")
    if not isinstance(params["subject_1"], str):
        raise StyxValidationError(f'`subject_1` has the wrong type: Received `{type(params["subject_1"])}` expected `str`')

def mris_distance_to_label_cargs(params: MrisDistanceToLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_distance_to_label")
    cargs.append(params["hemisphere"])
    cargs.append(params["subject_1"])
    return cargs

def mris_distance_to_label_outputs(params: MrisDistanceToLabel, execution: Execution) -> MrisDistanceToLabelOutputs:
    """Build the MrisDistanceToLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisDistanceToLabelOutputs(
        root=root_v,
    )

def mris_distance_to_label_execute(params: MrisDistanceToLabel, runner: Runner | None = None) -> MrisDistanceToLabelOutputs:
    """
    mris_distance_to_label

    A tool for measuring the distance between vertices on a surface and a labeled region.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_distance_to_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_DISTANCE_TO_LABEL_METADATA)
    execution.params(params)
    args = mris_distance_to_label_cargs(params, execution)
    out = mris_distance_to_label_outputs(params, execution)
    execution.run(args)
    return out

def mris_distance_to_label(
    hemisphere: str,
    subject_1: str,
    runner: Runner | None = None,
) -> MrisDistanceToLabelOutputs:
    """
    mris_distance_to_label

    A tool for measuring the distance between vertices on a surface and a labeled region.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        hemisphere: Hemisphere to process (e.g., 'lh' for left hemisphere or 'rh'\
            for right hemisphere)
        subject_1: Subject identifier or path to the subject directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_distance_to_label_params(
        hemisphere=hemisphere,
        subject_1=subject_1,
    )
    return mris_distance_to_label_execute(params, runner)

__all__ = [
    "MrisDistanceToLabel",
    "MrisDistanceToLabelOutputs",
    "MRIS_DISTANCE_TO_LABEL_METADATA",
    "mris_distance_to_label_cargs",
    "mris_distance_to_label_outputs",
    "mris_distance_to_label_params",
    "mris_distance_to_label_execute",
    "mris_distance_to_label_validate",
    "mris_distance_to_label",
]