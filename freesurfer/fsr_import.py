# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSR_IMPORT_METADATA = Metadata(
    id="fsr-import",
    name="fsr-import",
    package="freesurfer",
)

FsrImport = typing.TypedDict(
    "FsrImport",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fsr-import"]],
        "outdir": str,
        "t1w_input": typing.NotRequired[list[InputPathType]],
        "t2w_input": typing.NotRequired[list[InputPathType]],
        "flair_input": typing.NotRequired[list[InputPathType]],
        "custom_mode_input": typing.NotRequired[list[str]],
        "force_update": typing.NotRequired[bool],
        "no_conform": typing.NotRequired[bool],
        "hires": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsrImportOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_t1w: OutputPathType
    """Output directory for T1-weighted images"""
    out_t2w: OutputPathType
    """Output directory for T2-weighted images"""
    out_flair: OutputPathType
    """Output directory for FLAIR images"""

def fsr_import_params(
    outdir: str,
    t1w_input: list[InputPathType] | None = None,
    t2w_input: list[InputPathType] | None = None,
    flair_input: list[InputPathType] | None = None,
    custom_mode_input: list[str] | None = None,
    force_update: bool = False,
    no_conform: bool = False,
    hires: bool = False,
) -> FsrImport:
    """
    Build parameters.

    Args:
        outdir: Root directory for output data
        t1w_input: Input T1-weighted image files
        t2w_input: Input T2-weighted image files
        flair_input: Input FLAIR image files
        custom_mode_input: Custom modality image file with specified mode name (not\
            t1w, t2w, or flair)
        force_update: Update files regardless of timestamp
        no_conform: Do not conform inputs to 1mm, 256 dimensions
        hires: Same as --no-conform

    Returns:
        Parameter dictionary.
    """
    params: FsrImport = {
        "@type": "freesurfer/fsr-import",
        "outdir": outdir,
        "force_update": force_update,
        "no_conform": no_conform,
        "hires": hires,
    }
    if t1w_input is not None:
        params["t1w_input"] = t1w_input
    if t2w_input is not None:
        params["t2w_input"] = t2w_input
    if flair_input is not None:
        params["flair_input"] = flair_input
    if custom_mode_input is not None:
        params["custom_mode_input"] = custom_mode_input
    return params

def fsr_import_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsrImport."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("outdir", None) is None:
        raise StyxValidationError("`outdir` must not be None")
    if not isinstance(params["outdir"], str):
        raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("t1w_input", None) is not None:
        if not isinstance(params["t1w_input"], list):
            raise StyxValidationError(f'`t1w_input` has the wrong type: Received `{type(params["t1w_input"])}` expected `list[InputPathType]`')
        for e in params["t1w_input"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`t1w_input` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("t2w_input", None) is not None:
        if not isinstance(params["t2w_input"], list):
            raise StyxValidationError(f'`t2w_input` has the wrong type: Received `{type(params["t2w_input"])}` expected `list[InputPathType]`')
        for e_2 in params["t2w_input"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`t2w_input` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("flair_input", None) is not None:
        if not isinstance(params["flair_input"], list):
            raise StyxValidationError(f'`flair_input` has the wrong type: Received `{type(params["flair_input"])}` expected `list[InputPathType]`')
        for e_3 in params["flair_input"]:
            if not isinstance(e_3, (pathlib.Path, str)):
                raise StyxValidationError(f'`flair_input` has the wrong type: Received `{type(e_3)}` expected `list[InputPathType]`')
    if params.get("custom_mode_input", None) is not None:
        if not isinstance(params["custom_mode_input"], list):
            raise StyxValidationError(f'`custom_mode_input` has the wrong type: Received `{type(params["custom_mode_input"])}` expected `list[str]`')
        for e_4 in params["custom_mode_input"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`custom_mode_input` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("force_update", None) is not None:
        if not isinstance(params["force_update"], bool):
            raise StyxValidationError(f'`force_update` has the wrong type: Received `{type(params["force_update"])}` expected `bool`')
    if params.get("no_conform", None) is not None:
        if not isinstance(params["no_conform"], bool):
            raise StyxValidationError(f'`no_conform` has the wrong type: Received `{type(params["no_conform"])}` expected `bool`')
    if params.get("hires", None) is not None:
        if not isinstance(params["hires"], bool):
            raise StyxValidationError(f'`hires` has the wrong type: Received `{type(params["hires"])}` expected `bool`')

def fsr_import_cargs(params: FsrImport, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsr-import")
    cargs.append("--o")
    cargs.append(params["outdir"])
    v_0 = params.get("t1w_input")
    if v_0 is not None:
        cargs.append("--t1w")
        for item0 in v_0:
            cargs.append(execution.input_file(item0))
    v_1 = params.get("t2w_input")
    if v_1 is not None:
        cargs.append("--t2w")
        for item1 in v_1:
            cargs.append(execution.input_file(item1))
    v_2 = params.get("flair_input")
    if v_2 is not None:
        cargs.append("--flair")
        for item2 in v_2:
            cargs.append(execution.input_file(item2))
    v_3 = params.get("custom_mode_input")
    if v_3 is not None:
        cargs.append("--mode")
        for item3 in v_3:
            cargs.append(item3)
    if params.get("force_update"):
        cargs.append("--force-update")
    if params.get("no_conform"):
        cargs.append("--no-conform")
    if params.get("hires"):
        cargs.append("--hires")
    return cargs

def fsr_import_outputs(params: FsrImport, execution: Execution) -> FsrImportOutputs:
    """Build the FsrImportOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_t1w_v: OutputPathType = execution.output_file(f'{params["outdir"]}/t1w')
    out_t2w_v: OutputPathType = execution.output_file(f'{params["outdir"]}/t2w')
    out_flair_v: OutputPathType = execution.output_file(f'{params["outdir"]}/flair_input')
    return FsrImportOutputs(
        root=root_v,
        out_t1w=out_t1w_v,
        out_t2w=out_t2w_v,
        out_flair=out_flair_v,
    )

def fsr_import_execute(params: FsrImport, runner: Runner | None = None) -> FsrImportOutputs:
    """
    fsr-import

    Copies/converts data into a directory structure for samseg-expected format

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsr_import_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSR_IMPORT_METADATA)
    execution.params(params)
    args = fsr_import_cargs(params, execution)
    out = fsr_import_outputs(params, execution)
    execution.run(args)
    return out

def fsr_import(
    outdir: str,
    t1w_input: list[InputPathType] | None = None,
    t2w_input: list[InputPathType] | None = None,
    flair_input: list[InputPathType] | None = None,
    custom_mode_input: list[str] | None = None,
    force_update: bool = False,
    no_conform: bool = False,
    hires: bool = False,
    runner: Runner | None = None,
) -> FsrImportOutputs:
    """
    fsr-import

    Copies/converts data into a directory structure for samseg-expected format

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        outdir: Root directory for output data
        t1w_input: Input T1-weighted image files
        t2w_input: Input T2-weighted image files
        flair_input: Input FLAIR image files
        custom_mode_input: Custom modality image file with specified mode name (not\
            t1w, t2w, or flair)
        force_update: Update files regardless of timestamp
        no_conform: Do not conform inputs to 1mm, 256 dimensions
        hires: Same as --no-conform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsr_import_params(
        outdir=outdir,
        t1w_input=t1w_input,
        t2w_input=t2w_input,
        flair_input=flair_input,
        custom_mode_input=custom_mode_input,
        force_update=force_update,
        no_conform=no_conform,
        hires=hires,
    )
    return fsr_import_execute(params, runner)

__all__ = [
    "FsrImport",
    "FsrImportOutputs",
    "FSR_IMPORT_METADATA",
    "fsr_import_cargs",
    "fsr_import_outputs",
    "fsr_import_params",
    "fsr_import_execute",
    "fsr_import_validate",
    "fsr_import",
]