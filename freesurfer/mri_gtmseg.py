# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_GTMSEG_METADATA = Metadata(
    id="mri_gtmseg",
    name="mri_gtmseg",
    package="freesurfer",
)

MriGtmseg = typing.TypedDict(
    "MriGtmseg",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_gtmseg"]],
        "output_volume": str,
        "source_subject": str,
        "internal_usf": typing.NotRequired[float],
        "apas_file": typing.NotRequired[InputPathType],
        "context_annotation": typing.NotRequired[list[str]],
        "subseg_wm": typing.NotRequired[bool],
        "wm_annotation": typing.NotRequired[list[str]],
        "dmax": typing.NotRequired[float],
        "keep_hypo": typing.NotRequired[bool],
        "keep_cc": typing.NotRequired[bool],
        "ctab": typing.NotRequired[InputPathType],
        "lhminmax": typing.NotRequired[list[float]],
        "rhminmax": typing.NotRequired[list[float]],
        "output_usf": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "threads_max": typing.NotRequired[bool],
        "threads_max_1": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "check_opts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriGtmsegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_gtmseg_params(
    output_volume: str,
    source_subject: str,
    internal_usf: float | None = None,
    apas_file: InputPathType | None = None,
    context_annotation: list[str] | None = None,
    subseg_wm: bool = False,
    wm_annotation: list[str] | None = None,
    dmax: float | None = None,
    keep_hypo: bool = False,
    keep_cc: bool = False,
    ctab: InputPathType | None = None,
    lhminmax: list[float] | None = None,
    rhminmax: list[float] | None = None,
    output_usf: float | None = None,
    threads: float | None = None,
    threads_max: bool = False,
    threads_max_1: bool = False,
    debug: bool = False,
    check_opts: bool = False,
) -> MriGtmseg:
    """
    Build parameters.

    Args:
        output_volume: Output volume (output will be subject/mri/outvol)
        source_subject: Source subject
        internal_usf: Upsampling factor (default 2)
        apas_file: Defines extra-cerebral and subcortical segmentations\
            (apas+head.mgz)
        context_annotation: Use annotation to segment cortex\
            (aparc.annot,1000,2000)
        subseg_wm: Turn on segmenting of WM into smaller parts (off by default)
        wm_annotation: Use annotation to subsegment white matter
        dmax: Distance from cortex for white matter segmentation to be considered\
            'unsegmented' (default 5.000000)
        keep_hypo: Do not convert white matter hypointensities to a white matter\
            label
        keep_cc: Do not convert corpus callosum to a white matter label
        ctab: Copy items in ctab.lut into master ctab merging or overwriting what\
            is there
        lhminmax: For defining left hemisphere ribbon in APAS (default: 1000 1900)
        rhminmax: For defining right hemisphere ribbon in APAS (default: 2000 2900)
        output_usf: Set actual output resolution. Default is to be the same as the\
            --internal-usf
        threads: Use N threads (with Open MP)
        threads_max: Use the maximum allowable number of threads for this computer
        threads_max_1: Use one less than the maximum allowable number of threads\
            for this computer
        debug: Turn on debugging
        check_opts: Don't run anything, just check options and exit

    Returns:
        Parameter dictionary.
    """
    params: MriGtmseg = {
        "@type": "freesurfer/mri_gtmseg",
        "output_volume": output_volume,
        "source_subject": source_subject,
        "subseg_wm": subseg_wm,
        "keep_hypo": keep_hypo,
        "keep_cc": keep_cc,
        "threads_max": threads_max,
        "threads_max_1": threads_max_1,
        "debug": debug,
        "check_opts": check_opts,
    }
    if internal_usf is not None:
        params["internal_usf"] = internal_usf
    if apas_file is not None:
        params["apas_file"] = apas_file
    if context_annotation is not None:
        params["context_annotation"] = context_annotation
    if wm_annotation is not None:
        params["wm_annotation"] = wm_annotation
    if dmax is not None:
        params["dmax"] = dmax
    if ctab is not None:
        params["ctab"] = ctab
    if lhminmax is not None:
        params["lhminmax"] = lhminmax
    if rhminmax is not None:
        params["rhminmax"] = rhminmax
    if output_usf is not None:
        params["output_usf"] = output_usf
    if threads is not None:
        params["threads"] = threads
    return params

def mri_gtmseg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriGtmseg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("source_subject", None) is None:
        raise StyxValidationError("`source_subject` must not be None")
    if not isinstance(params["source_subject"], str):
        raise StyxValidationError(f'`source_subject` has the wrong type: Received `{type(params["source_subject"])}` expected `str`')
    if params.get("internal_usf", None) is not None:
        if not isinstance(params["internal_usf"], (float, int)):
            raise StyxValidationError(f'`internal_usf` has the wrong type: Received `{type(params["internal_usf"])}` expected `float`')
    if params.get("apas_file", None) is not None:
        if not isinstance(params["apas_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`apas_file` has the wrong type: Received `{type(params["apas_file"])}` expected `InputPathType`')
    if params.get("context_annotation", None) is not None:
        if not isinstance(params["context_annotation"], list):
            raise StyxValidationError(f'`context_annotation` has the wrong type: Received `{type(params["context_annotation"])}` expected `list[str]`')
        if not (3 <= len(params["context_annotation"]) <= 3):
            raise StyxValidationError("Parameter `context_annotation` must contain between 3 and 3 elements (inclusive)")
        for e in params["context_annotation"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`context_annotation` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("subseg_wm", None) is not None:
        if not isinstance(params["subseg_wm"], bool):
            raise StyxValidationError(f'`subseg_wm` has the wrong type: Received `{type(params["subseg_wm"])}` expected `bool`')
    if params.get("wm_annotation", None) is not None:
        if not isinstance(params["wm_annotation"], list):
            raise StyxValidationError(f'`wm_annotation` has the wrong type: Received `{type(params["wm_annotation"])}` expected `list[str]`')
        if not (3 <= len(params["wm_annotation"]) <= 3):
            raise StyxValidationError("Parameter `wm_annotation` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["wm_annotation"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`wm_annotation` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("dmax", None) is not None:
        if not isinstance(params["dmax"], (float, int)):
            raise StyxValidationError(f'`dmax` has the wrong type: Received `{type(params["dmax"])}` expected `float`')
    if params.get("keep_hypo", None) is not None:
        if not isinstance(params["keep_hypo"], bool):
            raise StyxValidationError(f'`keep_hypo` has the wrong type: Received `{type(params["keep_hypo"])}` expected `bool`')
    if params.get("keep_cc", None) is not None:
        if not isinstance(params["keep_cc"], bool):
            raise StyxValidationError(f'`keep_cc` has the wrong type: Received `{type(params["keep_cc"])}` expected `bool`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `InputPathType`')
    if params.get("lhminmax", None) is not None:
        if not isinstance(params["lhminmax"], list):
            raise StyxValidationError(f'`lhminmax` has the wrong type: Received `{type(params["lhminmax"])}` expected `list[float]`')
        if not (2 <= len(params["lhminmax"]) <= 2):
            raise StyxValidationError("Parameter `lhminmax` must contain between 2 and 2 elements (inclusive)")
        for e_3 in params["lhminmax"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`lhminmax` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("rhminmax", None) is not None:
        if not isinstance(params["rhminmax"], list):
            raise StyxValidationError(f'`rhminmax` has the wrong type: Received `{type(params["rhminmax"])}` expected `list[float]`')
        if not (2 <= len(params["rhminmax"]) <= 2):
            raise StyxValidationError("Parameter `rhminmax` must contain between 2 and 2 elements (inclusive)")
        for e_4 in params["rhminmax"]:
            if not isinstance(e_4, (float, int)):
                raise StyxValidationError(f'`rhminmax` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
    if params.get("output_usf", None) is not None:
        if not isinstance(params["output_usf"], (float, int)):
            raise StyxValidationError(f'`output_usf` has the wrong type: Received `{type(params["output_usf"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("threads_max", None) is not None:
        if not isinstance(params["threads_max"], bool):
            raise StyxValidationError(f'`threads_max` has the wrong type: Received `{type(params["threads_max"])}` expected `bool`')
    if params.get("threads_max_1", None) is not None:
        if not isinstance(params["threads_max_1"], bool):
            raise StyxValidationError(f'`threads_max_1` has the wrong type: Received `{type(params["threads_max_1"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("check_opts", None) is not None:
        if not isinstance(params["check_opts"], bool):
            raise StyxValidationError(f'`check_opts` has the wrong type: Received `{type(params["check_opts"])}` expected `bool`')

def mri_gtmseg_cargs(params: MriGtmseg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_gtmseg")
    cargs.append("--o")
    cargs.append(params["output_volume"])
    cargs.append("--s")
    cargs.append(params["source_subject"])
    v_0 = params.get("internal_usf")
    if v_0 is not None:
        cargs.append("--internal-usf")
        cargs.append(str(v_0))
    v_1 = params.get("apas_file")
    if v_1 is not None:
        cargs.append("--apas")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("context_annotation")
    if v_2 is not None:
        cargs.append("--ctx-annot")
        for item0 in v_2:
            cargs.append(item0)
    if params.get("subseg_wm"):
        cargs.append("--subseg-wm")
    v_3 = params.get("wm_annotation")
    if v_3 is not None:
        cargs.append("--wm-annot")
        for item1 in v_3:
            cargs.append(item1)
    v_4 = params.get("dmax")
    if v_4 is not None:
        cargs.append("--dmax")
        cargs.append(str(v_4))
    if params.get("keep_hypo"):
        cargs.append("--keep-hypo")
    if params.get("keep_cc"):
        cargs.append("--keep-cc")
    v_5 = params.get("ctab")
    if v_5 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("lhminmax")
    if v_6 is not None:
        cargs.append("--lhminmax")
        for item2 in v_6:
            cargs.append(str(item2))
    v_7 = params.get("rhminmax")
    if v_7 is not None:
        cargs.append("--rhminmax")
        for item3 in v_7:
            cargs.append(str(item3))
    v_8 = params.get("output_usf")
    if v_8 is not None:
        cargs.append("--output-usf")
        cargs.append(str(v_8))
    v_9 = params.get("threads")
    if v_9 is not None:
        cargs.append("--threads")
        cargs.append(str(v_9))
    if params.get("threads_max"):
        cargs.append("--threads-max")
    if params.get("threads_max_1"):
        cargs.append("--threads-max-1")
    if params.get("debug"):
        cargs.append("--debug")
    if params.get("check_opts"):
        cargs.append("--checkopts")
    return cargs

def mri_gtmseg_outputs(params: MriGtmseg, execution: Execution) -> MriGtmsegOutputs:
    """Build the MriGtmsegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriGtmsegOutputs(
        root=root_v,
    )

def mri_gtmseg_execute(params: MriGtmseg, runner: Runner | None = None) -> MriGtmsegOutputs:
    """
    mri_gtmseg

    Creates a segmentation that can be used with the geometric transfer matrix (GTM).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_gtmseg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_GTMSEG_METADATA)
    execution.params(params)
    args = mri_gtmseg_cargs(params, execution)
    out = mri_gtmseg_outputs(params, execution)
    execution.run(args)
    return out

def mri_gtmseg(
    output_volume: str,
    source_subject: str,
    internal_usf: float | None = None,
    apas_file: InputPathType | None = None,
    context_annotation: list[str] | None = None,
    subseg_wm: bool = False,
    wm_annotation: list[str] | None = None,
    dmax: float | None = None,
    keep_hypo: bool = False,
    keep_cc: bool = False,
    ctab: InputPathType | None = None,
    lhminmax: list[float] | None = None,
    rhminmax: list[float] | None = None,
    output_usf: float | None = None,
    threads: float | None = None,
    threads_max: bool = False,
    threads_max_1: bool = False,
    debug: bool = False,
    check_opts: bool = False,
    runner: Runner | None = None,
) -> MriGtmsegOutputs:
    """
    mri_gtmseg

    Creates a segmentation that can be used with the geometric transfer matrix (GTM).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        output_volume: Output volume (output will be subject/mri/outvol)
        source_subject: Source subject
        internal_usf: Upsampling factor (default 2)
        apas_file: Defines extra-cerebral and subcortical segmentations\
            (apas+head.mgz)
        context_annotation: Use annotation to segment cortex\
            (aparc.annot,1000,2000)
        subseg_wm: Turn on segmenting of WM into smaller parts (off by default)
        wm_annotation: Use annotation to subsegment white matter
        dmax: Distance from cortex for white matter segmentation to be considered\
            'unsegmented' (default 5.000000)
        keep_hypo: Do not convert white matter hypointensities to a white matter\
            label
        keep_cc: Do not convert corpus callosum to a white matter label
        ctab: Copy items in ctab.lut into master ctab merging or overwriting what\
            is there
        lhminmax: For defining left hemisphere ribbon in APAS (default: 1000 1900)
        rhminmax: For defining right hemisphere ribbon in APAS (default: 2000 2900)
        output_usf: Set actual output resolution. Default is to be the same as the\
            --internal-usf
        threads: Use N threads (with Open MP)
        threads_max: Use the maximum allowable number of threads for this computer
        threads_max_1: Use one less than the maximum allowable number of threads\
            for this computer
        debug: Turn on debugging
        check_opts: Don't run anything, just check options and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_gtmseg_params(
        output_volume=output_volume,
        source_subject=source_subject,
        internal_usf=internal_usf,
        apas_file=apas_file,
        context_annotation=context_annotation,
        subseg_wm=subseg_wm,
        wm_annotation=wm_annotation,
        dmax=dmax,
        keep_hypo=keep_hypo,
        keep_cc=keep_cc,
        ctab=ctab,
        lhminmax=lhminmax,
        rhminmax=rhminmax,
        output_usf=output_usf,
        threads=threads,
        threads_max=threads_max,
        threads_max_1=threads_max_1,
        debug=debug,
        check_opts=check_opts,
    )
    return mri_gtmseg_execute(params, runner)

__all__ = [
    "MriGtmseg",
    "MriGtmsegOutputs",
    "MRI_GTMSEG_METADATA",
    "mri_gtmseg_cargs",
    "mri_gtmseg_outputs",
    "mri_gtmseg_params",
    "mri_gtmseg_execute",
    "mri_gtmseg_validate",
    "mri_gtmseg",
]