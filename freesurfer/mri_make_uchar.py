# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_MAKE_UCHAR_METADATA = Metadata(
    id="mri_make_uchar",
    name="mri_make_uchar",
    package="freesurfer",
)

MriMakeUchar = typing.TypedDict(
    "MriMakeUchar",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_make_uchar"]],
        "input_volume": InputPathType,
        "talairach_xform": InputPathType,
        "output_volume": str,
        "first_percentile": typing.NotRequired[float],
        "wm_percentile": typing.NotRequired[float],
        "max_radius": typing.NotRequired[float],
        "cumulative_histo": typing.NotRequired[str],
        "vradvol": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriMakeUcharOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume from mri_make_uchar processing"""

def mri_make_uchar_params(
    input_volume: InputPathType,
    talairach_xform: InputPathType,
    output_volume: str,
    first_percentile: float | None = None,
    wm_percentile: float | None = None,
    max_radius: float | None = None,
    cumulative_histo: str | None = None,
    vradvol: str | None = None,
) -> MriMakeUchar:
    """
    Build parameters.

    Args:
        input_volume: Input volume file.
        talairach_xform: Talairach transformation file.
        output_volume: Output volume file.
        first_percentile: First percentile for histogram calculation (default\
            0.01).
        wm_percentile: White matter percentile for histogram calculation (default\
            0.9).
        max_radius: Maximum radius for voxel consideration (default 50).
        cumulative_histo: Cumulative histogram file.
        vradvol: Volume file where everything outside MAX_R is set to 0.

    Returns:
        Parameter dictionary.
    """
    params: MriMakeUchar = {
        "@type": "freesurfer/mri_make_uchar",
        "input_volume": input_volume,
        "talairach_xform": talairach_xform,
        "output_volume": output_volume,
    }
    if first_percentile is not None:
        params["first_percentile"] = first_percentile
    if wm_percentile is not None:
        params["wm_percentile"] = wm_percentile
    if max_radius is not None:
        params["max_radius"] = max_radius
    if cumulative_histo is not None:
        params["cumulative_histo"] = cumulative_histo
    if vradvol is not None:
        params["vradvol"] = vradvol
    return params

def mri_make_uchar_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriMakeUchar."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("talairach_xform", None) is None:
        raise StyxValidationError("`talairach_xform` must not be None")
    if not isinstance(params["talairach_xform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`talairach_xform` has the wrong type: Received `{type(params["talairach_xform"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("first_percentile", None) is not None:
        if not isinstance(params["first_percentile"], (float, int)):
            raise StyxValidationError(f'`first_percentile` has the wrong type: Received `{type(params["first_percentile"])}` expected `float`')
    if params.get("wm_percentile", None) is not None:
        if not isinstance(params["wm_percentile"], (float, int)):
            raise StyxValidationError(f'`wm_percentile` has the wrong type: Received `{type(params["wm_percentile"])}` expected `float`')
    if params.get("max_radius", None) is not None:
        if not isinstance(params["max_radius"], (float, int)):
            raise StyxValidationError(f'`max_radius` has the wrong type: Received `{type(params["max_radius"])}` expected `float`')
    if params.get("cumulative_histo", None) is not None:
        if not isinstance(params["cumulative_histo"], str):
            raise StyxValidationError(f'`cumulative_histo` has the wrong type: Received `{type(params["cumulative_histo"])}` expected `str`')
    if params.get("vradvol", None) is not None:
        if not isinstance(params["vradvol"], str):
            raise StyxValidationError(f'`vradvol` has the wrong type: Received `{type(params["vradvol"])}` expected `str`')

def mri_make_uchar_cargs(params: MriMakeUchar, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_make_uchar")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["talairach_xform"]))
    cargs.append(params["output_volume"])
    v_0 = params.get("first_percentile")
    if v_0 is not None:
        cargs.append("-f")
        cargs.append(str(v_0))
    v_1 = params.get("wm_percentile")
    if v_1 is not None:
        cargs.append("-w")
        cargs.append(str(v_1))
    v_2 = params.get("max_radius")
    if v_2 is not None:
        cargs.append("-r")
        cargs.append(str(v_2))
    v_3 = params.get("cumulative_histo")
    if v_3 is not None:
        cargs.append("-h")
        cargs.append(v_3)
    v_4 = params.get("vradvol")
    if v_4 is not None:
        cargs.append("-v")
        cargs.append(v_4)
    return cargs

def mri_make_uchar_outputs(params: MriMakeUchar, execution: Execution) -> MriMakeUcharOutputs:
    """Build the MriMakeUcharOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriMakeUcharOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mri_make_uchar_execute(params: MriMakeUchar, runner: Runner | None = None) -> MriMakeUcharOutputs:
    """
    mri_make_uchar

    Tool to adjust intensity of brain MRI images using a Talairach transformation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_make_uchar_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_MAKE_UCHAR_METADATA)
    execution.params(params)
    args = mri_make_uchar_cargs(params, execution)
    out = mri_make_uchar_outputs(params, execution)
    execution.run(args)
    return out

def mri_make_uchar(
    input_volume: InputPathType,
    talairach_xform: InputPathType,
    output_volume: str,
    first_percentile: float | None = None,
    wm_percentile: float | None = None,
    max_radius: float | None = None,
    cumulative_histo: str | None = None,
    vradvol: str | None = None,
    runner: Runner | None = None,
) -> MriMakeUcharOutputs:
    """
    mri_make_uchar

    Tool to adjust intensity of brain MRI images using a Talairach transformation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file.
        talairach_xform: Talairach transformation file.
        output_volume: Output volume file.
        first_percentile: First percentile for histogram calculation (default\
            0.01).
        wm_percentile: White matter percentile for histogram calculation (default\
            0.9).
        max_radius: Maximum radius for voxel consideration (default 50).
        cumulative_histo: Cumulative histogram file.
        vradvol: Volume file where everything outside MAX_R is set to 0.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_make_uchar_params(
        input_volume=input_volume,
        talairach_xform=talairach_xform,
        output_volume=output_volume,
        first_percentile=first_percentile,
        wm_percentile=wm_percentile,
        max_radius=max_radius,
        cumulative_histo=cumulative_histo,
        vradvol=vradvol,
    )
    return mri_make_uchar_execute(params, runner)

__all__ = [
    "MriMakeUchar",
    "MriMakeUcharOutputs",
    "MRI_MAKE_UCHAR_METADATA",
    "mri_make_uchar_cargs",
    "mri_make_uchar_outputs",
    "mri_make_uchar_params",
    "mri_make_uchar_execute",
    "mri_make_uchar_validate",
    "mri_make_uchar",
]