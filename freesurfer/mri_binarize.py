# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_BINARIZE_METADATA = Metadata(
    id="mri_binarize",
    name="mri_binarize",
    package="freesurfer",
)

MriBinarize = typing.TypedDict(
    "MriBinarize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_binarize"]],
        "input_volume": InputPathType,
        "output_volume": str,
        "min_threshold": typing.NotRequired[float],
        "max_threshold": typing.NotRequired[float],
        "pct_threshold": typing.NotRequired[float],
        "rmin": typing.NotRequired[float],
        "rmax": typing.NotRequired[float],
        "fdr_threshold": typing.NotRequired[float],
        "match_values": typing.NotRequired[list[float]],
        "replace_values": typing.NotRequired[list[float]],
        "binval": typing.NotRequired[float],
        "binval_not": typing.NotRequired[float],
        "frame": typing.NotRequired[float],
        "merge_volume": typing.NotRequired[InputPathType],
        "mask_volume": typing.NotRequired[InputPathType],
        "mask_threshold": typing.NotRequired[float],
        "surf_name": typing.NotRequired[str],
        "surf_smooth": typing.NotRequired[float],
        "threads": typing.NotRequired[float],
        "ctx_wm_flag": typing.NotRequired[bool],
        "all_wm_flag": typing.NotRequired[bool],
        "ventricles_flag": typing.NotRequired[bool],
        "wm_vcsf_flag": typing.NotRequired[bool],
        "gm_flag": typing.NotRequired[bool],
        "subcort_gm_flag": typing.NotRequired[bool],
        "scm_lh_flag": typing.NotRequired[bool],
        "scm_rh_flag": typing.NotRequired[bool],
        "zero_edges_flag": typing.NotRequired[bool],
        "zero_slice_edges_flag": typing.NotRequired[bool],
        "dilate_vertex": typing.NotRequired[str],
        "remove_islands_flag": typing.NotRequired[bool],
        "fill_holes_flag": typing.NotRequired[bool],
        "noverbose_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriBinarizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_volume: OutputPathType
    """The resulting binarized volume."""

def mri_binarize_params(
    input_volume: InputPathType,
    output_volume: str,
    min_threshold: float | None = None,
    max_threshold: float | None = None,
    pct_threshold: float | None = None,
    rmin: float | None = None,
    rmax: float | None = None,
    fdr_threshold: float | None = None,
    match_values: list[float] | None = None,
    replace_values: list[float] | None = None,
    binval: float | None = None,
    binval_not: float | None = None,
    frame: float | None = None,
    merge_volume: InputPathType | None = None,
    mask_volume: InputPathType | None = None,
    mask_threshold: float | None = None,
    surf_name: str | None = None,
    surf_smooth: float | None = None,
    threads: float | None = None,
    ctx_wm_flag: bool = False,
    all_wm_flag: bool = False,
    ventricles_flag: bool = False,
    wm_vcsf_flag: bool = False,
    gm_flag: bool = False,
    subcort_gm_flag: bool = False,
    scm_lh_flag: bool = False,
    scm_rh_flag: bool = False,
    zero_edges_flag: bool = False,
    zero_slice_edges_flag: bool = False,
    dilate_vertex: str | None = None,
    remove_islands_flag: bool = False,
    fill_holes_flag: bool = False,
    noverbose_flag: bool = False,
    debug_flag: bool = False,
) -> MriBinarize:
    """
    Build parameters.

    Args:
        input_volume: Input volume to be binarized.
        output_volume: Path to output volume.
        min_threshold: Minimum threshold (default is -inf).
        max_threshold: Maximum threshold (default is +inf).
        pct_threshold: Set threshold to capture top P% of voxels.
        rmin: Compute min threshold based on rmin times global mean.
        rmax: Compute max threshold based on rmax times global mean.
        fdr_threshold: Compute min threshold based on FDR.
        match_values: Binarize based on matching values.
        replace_values: Replace voxels with specified values. Format: V1 V2
        binval: Set voxel value within threshold to specified value (default is 1).
        binval_not: Set voxel value outside threshold range to specified value\
            (default is 0).
        frame: Use specific frame of the input. 0-based index.
        merge_volume: Merge with another volume. Must be the same dimensions as\
            input volume.
        mask_volume: Mask input with a specified mask volume.
        mask_threshold: Set threshold for mask volume (default is 0.5).
        surf_name: Create a surface mesh from the binarization.
        surf_smooth: Smooth the surface mesh iteratively, specifying the number of\
            iterations.
        threads: Specify number of threads to use.
        ctx_wm_flag: Set match values for cerebral white matter.
        all_wm_flag: Set match values for all white matter.
        ventricles_flag: Set match values for ventricles and choroid.
        wm_vcsf_flag: Match for WM and ventricular CSF.
        gm_flag: Match for all WM, VCSF and background, then invert.
        subcort_gm_flag: Match for subcortical gray matter.
        scm_lh_flag: Subcortical mass for left hemisphere.
        scm_rh_flag: Subcortical mass for right hemisphere.
        zero_edges_flag: Set edge voxels to zero.
        zero_slice_edges_flag: Set edge slice voxels to zero.
        dilate_vertex: Dilate vertex to a specific target area.
        remove_islands_flag: Remove islands in the mask.
        fill_holes_flag: Remove holes in the mask.
        noverbose_flag: Suppress verbose output.
        debug_flag: Enable debugging output.

    Returns:
        Parameter dictionary.
    """
    params: MriBinarize = {
        "@type": "freesurfer/mri_binarize",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "ctx_wm_flag": ctx_wm_flag,
        "all_wm_flag": all_wm_flag,
        "ventricles_flag": ventricles_flag,
        "wm_vcsf_flag": wm_vcsf_flag,
        "gm_flag": gm_flag,
        "subcort_gm_flag": subcort_gm_flag,
        "scm_lh_flag": scm_lh_flag,
        "scm_rh_flag": scm_rh_flag,
        "zero_edges_flag": zero_edges_flag,
        "zero_slice_edges_flag": zero_slice_edges_flag,
        "remove_islands_flag": remove_islands_flag,
        "fill_holes_flag": fill_holes_flag,
        "noverbose_flag": noverbose_flag,
        "debug_flag": debug_flag,
    }
    if min_threshold is not None:
        params["min_threshold"] = min_threshold
    if max_threshold is not None:
        params["max_threshold"] = max_threshold
    if pct_threshold is not None:
        params["pct_threshold"] = pct_threshold
    if rmin is not None:
        params["rmin"] = rmin
    if rmax is not None:
        params["rmax"] = rmax
    if fdr_threshold is not None:
        params["fdr_threshold"] = fdr_threshold
    if match_values is not None:
        params["match_values"] = match_values
    if replace_values is not None:
        params["replace_values"] = replace_values
    if binval is not None:
        params["binval"] = binval
    if binval_not is not None:
        params["binval_not"] = binval_not
    if frame is not None:
        params["frame"] = frame
    if merge_volume is not None:
        params["merge_volume"] = merge_volume
    if mask_volume is not None:
        params["mask_volume"] = mask_volume
    if mask_threshold is not None:
        params["mask_threshold"] = mask_threshold
    if surf_name is not None:
        params["surf_name"] = surf_name
    if surf_smooth is not None:
        params["surf_smooth"] = surf_smooth
    if threads is not None:
        params["threads"] = threads
    if dilate_vertex is not None:
        params["dilate_vertex"] = dilate_vertex
    return params

def mri_binarize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriBinarize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("min_threshold", None) is not None:
        if not isinstance(params["min_threshold"], (float, int)):
            raise StyxValidationError(f'`min_threshold` has the wrong type: Received `{type(params["min_threshold"])}` expected `float`')
    if params.get("max_threshold", None) is not None:
        if not isinstance(params["max_threshold"], (float, int)):
            raise StyxValidationError(f'`max_threshold` has the wrong type: Received `{type(params["max_threshold"])}` expected `float`')
    if params.get("pct_threshold", None) is not None:
        if not isinstance(params["pct_threshold"], (float, int)):
            raise StyxValidationError(f'`pct_threshold` has the wrong type: Received `{type(params["pct_threshold"])}` expected `float`')
    if params.get("rmin", None) is not None:
        if not isinstance(params["rmin"], (float, int)):
            raise StyxValidationError(f'`rmin` has the wrong type: Received `{type(params["rmin"])}` expected `float`')
    if params.get("rmax", None) is not None:
        if not isinstance(params["rmax"], (float, int)):
            raise StyxValidationError(f'`rmax` has the wrong type: Received `{type(params["rmax"])}` expected `float`')
    if params.get("fdr_threshold", None) is not None:
        if not isinstance(params["fdr_threshold"], (float, int)):
            raise StyxValidationError(f'`fdr_threshold` has the wrong type: Received `{type(params["fdr_threshold"])}` expected `float`')
    if params.get("match_values", None) is not None:
        if not isinstance(params["match_values"], list):
            raise StyxValidationError(f'`match_values` has the wrong type: Received `{type(params["match_values"])}` expected `list[float]`')
        for e in params["match_values"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`match_values` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("replace_values", None) is not None:
        if not isinstance(params["replace_values"], list):
            raise StyxValidationError(f'`replace_values` has the wrong type: Received `{type(params["replace_values"])}` expected `list[float]`')
        for e_2 in params["replace_values"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`replace_values` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("binval", None) is not None:
        if not isinstance(params["binval"], (float, int)):
            raise StyxValidationError(f'`binval` has the wrong type: Received `{type(params["binval"])}` expected `float`')
    if params.get("binval_not", None) is not None:
        if not isinstance(params["binval_not"], (float, int)):
            raise StyxValidationError(f'`binval_not` has the wrong type: Received `{type(params["binval_not"])}` expected `float`')
    if params.get("frame", None) is not None:
        if not isinstance(params["frame"], (float, int)):
            raise StyxValidationError(f'`frame` has the wrong type: Received `{type(params["frame"])}` expected `float`')
    if params.get("merge_volume", None) is not None:
        if not isinstance(params["merge_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`merge_volume` has the wrong type: Received `{type(params["merge_volume"])}` expected `InputPathType`')
    if params.get("mask_volume", None) is not None:
        if not isinstance(params["mask_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `InputPathType`')
    if params.get("mask_threshold", None) is not None:
        if not isinstance(params["mask_threshold"], (float, int)):
            raise StyxValidationError(f'`mask_threshold` has the wrong type: Received `{type(params["mask_threshold"])}` expected `float`')
    if params.get("surf_name", None) is not None:
        if not isinstance(params["surf_name"], str):
            raise StyxValidationError(f'`surf_name` has the wrong type: Received `{type(params["surf_name"])}` expected `str`')
    if params.get("surf_smooth", None) is not None:
        if not isinstance(params["surf_smooth"], (float, int)):
            raise StyxValidationError(f'`surf_smooth` has the wrong type: Received `{type(params["surf_smooth"])}` expected `float`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params["threads"])}` expected `float`')
    if params.get("ctx_wm_flag", None) is not None:
        if not isinstance(params["ctx_wm_flag"], bool):
            raise StyxValidationError(f'`ctx_wm_flag` has the wrong type: Received `{type(params["ctx_wm_flag"])}` expected `bool`')
    if params.get("all_wm_flag", None) is not None:
        if not isinstance(params["all_wm_flag"], bool):
            raise StyxValidationError(f'`all_wm_flag` has the wrong type: Received `{type(params["all_wm_flag"])}` expected `bool`')
    if params.get("ventricles_flag", None) is not None:
        if not isinstance(params["ventricles_flag"], bool):
            raise StyxValidationError(f'`ventricles_flag` has the wrong type: Received `{type(params["ventricles_flag"])}` expected `bool`')
    if params.get("wm_vcsf_flag", None) is not None:
        if not isinstance(params["wm_vcsf_flag"], bool):
            raise StyxValidationError(f'`wm_vcsf_flag` has the wrong type: Received `{type(params["wm_vcsf_flag"])}` expected `bool`')
    if params.get("gm_flag", None) is not None:
        if not isinstance(params["gm_flag"], bool):
            raise StyxValidationError(f'`gm_flag` has the wrong type: Received `{type(params["gm_flag"])}` expected `bool`')
    if params.get("subcort_gm_flag", None) is not None:
        if not isinstance(params["subcort_gm_flag"], bool):
            raise StyxValidationError(f'`subcort_gm_flag` has the wrong type: Received `{type(params["subcort_gm_flag"])}` expected `bool`')
    if params.get("scm_lh_flag", None) is not None:
        if not isinstance(params["scm_lh_flag"], bool):
            raise StyxValidationError(f'`scm_lh_flag` has the wrong type: Received `{type(params["scm_lh_flag"])}` expected `bool`')
    if params.get("scm_rh_flag", None) is not None:
        if not isinstance(params["scm_rh_flag"], bool):
            raise StyxValidationError(f'`scm_rh_flag` has the wrong type: Received `{type(params["scm_rh_flag"])}` expected `bool`')
    if params.get("zero_edges_flag", None) is not None:
        if not isinstance(params["zero_edges_flag"], bool):
            raise StyxValidationError(f'`zero_edges_flag` has the wrong type: Received `{type(params["zero_edges_flag"])}` expected `bool`')
    if params.get("zero_slice_edges_flag", None) is not None:
        if not isinstance(params["zero_slice_edges_flag"], bool):
            raise StyxValidationError(f'`zero_slice_edges_flag` has the wrong type: Received `{type(params["zero_slice_edges_flag"])}` expected `bool`')
    if params.get("dilate_vertex", None) is not None:
        if not isinstance(params["dilate_vertex"], str):
            raise StyxValidationError(f'`dilate_vertex` has the wrong type: Received `{type(params["dilate_vertex"])}` expected `str`')
    if params.get("remove_islands_flag", None) is not None:
        if not isinstance(params["remove_islands_flag"], bool):
            raise StyxValidationError(f'`remove_islands_flag` has the wrong type: Received `{type(params["remove_islands_flag"])}` expected `bool`')
    if params.get("fill_holes_flag", None) is not None:
        if not isinstance(params["fill_holes_flag"], bool):
            raise StyxValidationError(f'`fill_holes_flag` has the wrong type: Received `{type(params["fill_holes_flag"])}` expected `bool`')
    if params.get("noverbose_flag", None) is not None:
        if not isinstance(params["noverbose_flag"], bool):
            raise StyxValidationError(f'`noverbose_flag` has the wrong type: Received `{type(params["noverbose_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')

def mri_binarize_cargs(params: MriBinarize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_binarize")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--o")
    cargs.append(params["output_volume"])
    v_0 = params.get("min_threshold")
    if v_0 is not None:
        cargs.append("--min")
        cargs.append(str(v_0))
    v_1 = params.get("max_threshold")
    if v_1 is not None:
        cargs.append("--max")
        cargs.append(str(v_1))
    v_2 = params.get("pct_threshold")
    if v_2 is not None:
        cargs.append("--pct")
        cargs.append(str(v_2))
    v_3 = params.get("rmin")
    if v_3 is not None:
        cargs.append("--rmin")
        cargs.append(str(v_3))
    v_4 = params.get("rmax")
    if v_4 is not None:
        cargs.append("--rmax")
        cargs.append(str(v_4))
    v_5 = params.get("fdr_threshold")
    if v_5 is not None:
        cargs.append("--fdr")
        cargs.append(str(v_5))
    v_6 = params.get("match_values")
    if v_6 is not None:
        cargs.append("--match")
        for item0 in v_6:
            cargs.append(str(item0))
    v_7 = params.get("replace_values")
    if v_7 is not None:
        cargs.append("--replace")
        for item1 in v_7:
            cargs.append(str(item1))
    v_8 = params.get("binval")
    if v_8 is not None:
        cargs.append("--binval")
        cargs.append(str(v_8))
    v_9 = params.get("binval_not")
    if v_9 is not None:
        cargs.append("--binvalnot")
        cargs.append(str(v_9))
    v_10 = params.get("frame")
    if v_10 is not None:
        cargs.append("--frame")
        cargs.append(str(v_10))
    v_11 = params.get("merge_volume")
    if v_11 is not None:
        cargs.append("--merge")
        cargs.append(execution.input_file(v_11))
    v_12 = params.get("mask_volume")
    if v_12 is not None:
        cargs.append("--mask")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("mask_threshold")
    if v_13 is not None:
        cargs.append("--mask-thresh")
        cargs.append(str(v_13))
    v_14 = params.get("surf_name")
    if v_14 is not None:
        cargs.append("--surf")
        cargs.append(v_14)
    v_15 = params.get("surf_smooth")
    if v_15 is not None:
        cargs.append("--surf-smooth")
        cargs.append(str(v_15))
    v_16 = params.get("threads")
    if v_16 is not None:
        cargs.append("--threads")
        cargs.append(str(v_16))
    if params.get("ctx_wm_flag"):
        cargs.append("--ctx-wm")
    if params.get("all_wm_flag"):
        cargs.append("--all-wm")
    if params.get("ventricles_flag"):
        cargs.append("--ventricles")
    if params.get("wm_vcsf_flag"):
        cargs.append("--wm+vcsf")
    if params.get("gm_flag"):
        cargs.append("--gm")
    if params.get("subcort_gm_flag"):
        cargs.append("--subcort-gm")
    if params.get("scm_lh_flag"):
        cargs.append("--scm-lh")
    if params.get("scm_rh_flag"):
        cargs.append("--scm-rh")
    if params.get("zero_edges_flag"):
        cargs.append("--zero-edges")
    if params.get("zero_slice_edges_flag"):
        cargs.append("--zero-slice-edges")
    v_17 = params.get("dilate_vertex")
    if v_17 is not None:
        cargs.append("--dilate-vertex")
        cargs.append(v_17)
    if params.get("remove_islands_flag"):
        cargs.append("--remove-islands")
    if params.get("fill_holes_flag"):
        cargs.append("--fill-holes")
    if params.get("noverbose_flag"):
        cargs.append("--noverbose")
    if params.get("debug_flag"):
        cargs.append("--debug")
    return cargs

def mri_binarize_outputs(params: MriBinarize, execution: Execution) -> MriBinarizeOutputs:
    """Build the MriBinarizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_volume_v: OutputPathType = execution.output_file(params["output_volume"])
    return MriBinarizeOutputs(
        root=root_v,
        out_volume=out_volume_v,
    )

def mri_binarize_execute(params: MriBinarize, runner: Runner | None = None) -> MriBinarizeOutputs:
    """
    mri_binarize

    A program to binarize a volume or volume-encoded surface file, with options to merge and manipulate binarized output.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_binarize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_BINARIZE_METADATA)
    execution.params(params)
    args = mri_binarize_cargs(params, execution)
    out = mri_binarize_outputs(params, execution)
    execution.run(args)
    return out

def mri_binarize(
    input_volume: InputPathType,
    output_volume: str,
    min_threshold: float | None = None,
    max_threshold: float | None = None,
    pct_threshold: float | None = None,
    rmin: float | None = None,
    rmax: float | None = None,
    fdr_threshold: float | None = None,
    match_values: list[float] | None = None,
    replace_values: list[float] | None = None,
    binval: float | None = None,
    binval_not: float | None = None,
    frame: float | None = None,
    merge_volume: InputPathType | None = None,
    mask_volume: InputPathType | None = None,
    mask_threshold: float | None = None,
    surf_name: str | None = None,
    surf_smooth: float | None = None,
    threads: float | None = None,
    ctx_wm_flag: bool = False,
    all_wm_flag: bool = False,
    ventricles_flag: bool = False,
    wm_vcsf_flag: bool = False,
    gm_flag: bool = False,
    subcort_gm_flag: bool = False,
    scm_lh_flag: bool = False,
    scm_rh_flag: bool = False,
    zero_edges_flag: bool = False,
    zero_slice_edges_flag: bool = False,
    dilate_vertex: str | None = None,
    remove_islands_flag: bool = False,
    fill_holes_flag: bool = False,
    noverbose_flag: bool = False,
    debug_flag: bool = False,
    runner: Runner | None = None,
) -> MriBinarizeOutputs:
    """
    mri_binarize

    A program to binarize a volume or volume-encoded surface file, with options to merge and manipulate binarized output.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume to be binarized.
        output_volume: Path to output volume.
        min_threshold: Minimum threshold (default is -inf).
        max_threshold: Maximum threshold (default is +inf).
        pct_threshold: Set threshold to capture top P% of voxels.
        rmin: Compute min threshold based on rmin times global mean.
        rmax: Compute max threshold based on rmax times global mean.
        fdr_threshold: Compute min threshold based on FDR.
        match_values: Binarize based on matching values.
        replace_values: Replace voxels with specified values. Format: V1 V2
        binval: Set voxel value within threshold to specified value (default is 1).
        binval_not: Set voxel value outside threshold range to specified value\
            (default is 0).
        frame: Use specific frame of the input. 0-based index.
        merge_volume: Merge with another volume. Must be the same dimensions as\
            input volume.
        mask_volume: Mask input with a specified mask volume.
        mask_threshold: Set threshold for mask volume (default is 0.5).
        surf_name: Create a surface mesh from the binarization.
        surf_smooth: Smooth the surface mesh iteratively, specifying the number of\
            iterations.
        threads: Specify number of threads to use.
        ctx_wm_flag: Set match values for cerebral white matter.
        all_wm_flag: Set match values for all white matter.
        ventricles_flag: Set match values for ventricles and choroid.
        wm_vcsf_flag: Match for WM and ventricular CSF.
        gm_flag: Match for all WM, VCSF and background, then invert.
        subcort_gm_flag: Match for subcortical gray matter.
        scm_lh_flag: Subcortical mass for left hemisphere.
        scm_rh_flag: Subcortical mass for right hemisphere.
        zero_edges_flag: Set edge voxels to zero.
        zero_slice_edges_flag: Set edge slice voxels to zero.
        dilate_vertex: Dilate vertex to a specific target area.
        remove_islands_flag: Remove islands in the mask.
        fill_holes_flag: Remove holes in the mask.
        noverbose_flag: Suppress verbose output.
        debug_flag: Enable debugging output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_binarize_params(
        input_volume=input_volume,
        output_volume=output_volume,
        min_threshold=min_threshold,
        max_threshold=max_threshold,
        pct_threshold=pct_threshold,
        rmin=rmin,
        rmax=rmax,
        fdr_threshold=fdr_threshold,
        match_values=match_values,
        replace_values=replace_values,
        binval=binval,
        binval_not=binval_not,
        frame=frame,
        merge_volume=merge_volume,
        mask_volume=mask_volume,
        mask_threshold=mask_threshold,
        surf_name=surf_name,
        surf_smooth=surf_smooth,
        threads=threads,
        ctx_wm_flag=ctx_wm_flag,
        all_wm_flag=all_wm_flag,
        ventricles_flag=ventricles_flag,
        wm_vcsf_flag=wm_vcsf_flag,
        gm_flag=gm_flag,
        subcort_gm_flag=subcort_gm_flag,
        scm_lh_flag=scm_lh_flag,
        scm_rh_flag=scm_rh_flag,
        zero_edges_flag=zero_edges_flag,
        zero_slice_edges_flag=zero_slice_edges_flag,
        dilate_vertex=dilate_vertex,
        remove_islands_flag=remove_islands_flag,
        fill_holes_flag=fill_holes_flag,
        noverbose_flag=noverbose_flag,
        debug_flag=debug_flag,
    )
    return mri_binarize_execute(params, runner)

__all__ = [
    "MriBinarize",
    "MriBinarizeOutputs",
    "MRI_BINARIZE_METADATA",
    "mri_binarize_cargs",
    "mri_binarize_outputs",
    "mri_binarize_params",
    "mri_binarize_execute",
    "mri_binarize_validate",
    "mri_binarize",
]