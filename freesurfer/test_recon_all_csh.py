# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TEST_RECON_ALL_CSH_METADATA = Metadata(
    id="test_recon-all.csh",
    name="test_recon-all.csh",
    package="freesurfer",
)

TestReconAllCsh = typing.TypedDict(
    "TestReconAllCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/test_recon-all.csh"]],
        "reference_subj_source_dir": typing.NotRequired[str],
        "reference_subjid": typing.NotRequired[str],
        "test_subject_dest_dir": typing.NotRequired[str],
        "test_subjid": typing.NotRequired[str],
        "freesurfer_home": typing.NotRequired[str],
        "norecon": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TestReconAllCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    recon_all_output: OutputPathType
    """Output from the recon-all utility."""
    mri_diff_output: OutputPathType
    """Output from the mri_diff utility."""
    mri_compute_seg_overlap_output: OutputPathType
    """Output from the mri_compute_seg_overlap utility."""
    mris_diff_output: OutputPathType
    """Output from the mris_diff utility."""
    mri_surf2surf_output: OutputPathType
    """Output from the mri_surf2surf utility."""
    mris_compute_parc_overlap_output: OutputPathType
    """Output from the mris_compute_parc_overlap utility."""
    diff_output: OutputPathType
    """Output from the diff utility."""
    asegstatsdiff_output: OutputPathType
    """Output from the asegstatsdiff utility."""
    aparcstatsdiff_output: OutputPathType
    """Output from the aparcstatsdiff utility."""

def test_recon_all_csh_params(
    reference_subj_source_dir: str | None = None,
    reference_subjid: str | None = None,
    test_subject_dest_dir: str | None = None,
    test_subjid: str | None = None,
    freesurfer_home: str | None = None,
    norecon: bool = False,
) -> TestReconAllCsh:
    """
    Build parameters.

    Args:
        reference_subj_source_dir: Directory of the reference subject source.
        reference_subjid: ID of the reference subject.
        test_subject_dest_dir: Directory for the test subject destination.
        test_subjid: ID of the test subject.
        freesurfer_home: Path to the FreeSurfer installation directory.
        norecon: Flag to indicate that recon-all should not be run.

    Returns:
        Parameter dictionary.
    """
    params: TestReconAllCsh = {
        "@type": "freesurfer/test_recon-all.csh",
        "norecon": norecon,
    }
    if reference_subj_source_dir is not None:
        params["reference_subj_source_dir"] = reference_subj_source_dir
    if reference_subjid is not None:
        params["reference_subjid"] = reference_subjid
    if test_subject_dest_dir is not None:
        params["test_subject_dest_dir"] = test_subject_dest_dir
    if test_subjid is not None:
        params["test_subjid"] = test_subjid
    if freesurfer_home is not None:
        params["freesurfer_home"] = freesurfer_home
    return params

def test_recon_all_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TestReconAllCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("reference_subj_source_dir", None) is not None:
        if not isinstance(params["reference_subj_source_dir"], str):
            raise StyxValidationError(f'`reference_subj_source_dir` has the wrong type: Received `{type(params["reference_subj_source_dir"])}` expected `str`')
    if params.get("reference_subjid", None) is not None:
        if not isinstance(params["reference_subjid"], str):
            raise StyxValidationError(f'`reference_subjid` has the wrong type: Received `{type(params["reference_subjid"])}` expected `str`')
    if params.get("test_subject_dest_dir", None) is not None:
        if not isinstance(params["test_subject_dest_dir"], str):
            raise StyxValidationError(f'`test_subject_dest_dir` has the wrong type: Received `{type(params["test_subject_dest_dir"])}` expected `str`')
    if params.get("test_subjid", None) is not None:
        if not isinstance(params["test_subjid"], str):
            raise StyxValidationError(f'`test_subjid` has the wrong type: Received `{type(params["test_subjid"])}` expected `str`')
    if params.get("freesurfer_home", None) is not None:
        if not isinstance(params["freesurfer_home"], str):
            raise StyxValidationError(f'`freesurfer_home` has the wrong type: Received `{type(params["freesurfer_home"])}` expected `str`')
    if params.get("norecon", None) is not None:
        if not isinstance(params["norecon"], bool):
            raise StyxValidationError(f'`norecon` has the wrong type: Received `{type(params["norecon"])}` expected `bool`')

def test_recon_all_csh_cargs(params: TestReconAllCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("test_recon-all.csh")
    v_0 = params.get("reference_subj_source_dir")
    if v_0 is not None:
        cargs.append("-rsd")
        cargs.append(v_0)
    v_1 = params.get("reference_subjid")
    if v_1 is not None:
        cargs.append("-rs")
        cargs.append(v_1)
    v_2 = params.get("test_subject_dest_dir")
    if v_2 is not None:
        cargs.append("-tsd")
        cargs.append(v_2)
    v_3 = params.get("test_subjid")
    if v_3 is not None:
        cargs.append("-ts")
        cargs.append(v_3)
    v_4 = params.get("freesurfer_home")
    if v_4 is not None:
        cargs.append("-fshome")
        cargs.append(v_4)
    if params.get("norecon"):
        cargs.append("-norecon")
    return cargs

def test_recon_all_csh_outputs(params: TestReconAllCsh, execution: Execution) -> TestReconAllCshOutputs:
    """Build the TestReconAllCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    recon_all_output_v: OutputPathType = execution.output_file("recon_all_output.txt")
    mri_diff_output_v: OutputPathType = execution.output_file("mri_diff_output.txt")
    mri_compute_seg_overlap_output_v: OutputPathType = execution.output_file("mri_compute_seg_overlap_output.txt")
    mris_diff_output_v: OutputPathType = execution.output_file("mris_diff_output.txt")
    mri_surf2surf_output_v: OutputPathType = execution.output_file("mri_surf2surf_output.txt")
    mris_compute_parc_overlap_output_v: OutputPathType = execution.output_file("mris_compute_parc_overlap_output.txt")
    diff_output_v: OutputPathType = execution.output_file("diff_output.txt")
    asegstatsdiff_output_v: OutputPathType = execution.output_file("asegstatsdiff_output.txt")
    aparcstatsdiff_output_v: OutputPathType = execution.output_file("aparcstatsdiff_output.txt")
    return TestReconAllCshOutputs(
        root=root_v,
        recon_all_output=recon_all_output_v,
        mri_diff_output=mri_diff_output_v,
        mri_compute_seg_overlap_output=mri_compute_seg_overlap_output_v,
        mris_diff_output=mris_diff_output_v,
        mri_surf2surf_output=mri_surf2surf_output_v,
        mris_compute_parc_overlap_output=mris_compute_parc_overlap_output_v,
        diff_output=diff_output_v,
        asegstatsdiff_output=asegstatsdiff_output_v,
        aparcstatsdiff_output=aparcstatsdiff_output_v,
    )

def test_recon_all_csh_execute(params: TestReconAllCsh, runner: Runner | None = None) -> TestReconAllCshOutputs:
    """
    test_recon-all.csh

    Script for testing recon-all and other utilities with FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    test_recon_all_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TEST_RECON_ALL_CSH_METADATA)
    execution.params(params)
    args = test_recon_all_csh_cargs(params, execution)
    out = test_recon_all_csh_outputs(params, execution)
    execution.run(args)
    return out

def test_recon_all_csh(
    reference_subj_source_dir: str | None = None,
    reference_subjid: str | None = None,
    test_subject_dest_dir: str | None = None,
    test_subjid: str | None = None,
    freesurfer_home: str | None = None,
    norecon: bool = False,
    runner: Runner | None = None,
) -> TestReconAllCshOutputs:
    """
    test_recon-all.csh

    Script for testing recon-all and other utilities with FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        reference_subj_source_dir: Directory of the reference subject source.
        reference_subjid: ID of the reference subject.
        test_subject_dest_dir: Directory for the test subject destination.
        test_subjid: ID of the test subject.
        freesurfer_home: Path to the FreeSurfer installation directory.
        norecon: Flag to indicate that recon-all should not be run.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = test_recon_all_csh_params(
        reference_subj_source_dir=reference_subj_source_dir,
        reference_subjid=reference_subjid,
        test_subject_dest_dir=test_subject_dest_dir,
        test_subjid=test_subjid,
        freesurfer_home=freesurfer_home,
        norecon=norecon,
    )
    return test_recon_all_csh_execute(params, runner)

__all__ = [
    "TestReconAllCsh",
    "TestReconAllCshOutputs",
    "TEST_RECON_ALL_CSH_METADATA",
    "test_recon_all_csh_cargs",
    "test_recon_all_csh_outputs",
    "test_recon_all_csh_params",
    "test_recon_all_csh_execute",
    "test_recon_all_csh_validate",
    "test_recon_all_csh",
]