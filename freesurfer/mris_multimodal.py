# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MULTIMODAL_METADATA = Metadata(
    id="mris_multimodal",
    name="mris_multimodal",
    package="freesurfer",
)

MrisMultimodal = typing.TypedDict(
    "MrisMultimodal",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_multimodal"]],
        "input_surface": InputPathType,
        "target_surface": InputPathType,
        "output_surface": str,
        "fill_holes": typing.NotRequired[bool],
        "curvature": typing.NotRequired[bool],
        "thickness": typing.NotRequired[bool],
        "annotation_output": str,
        "overlay_output": str,
        "csv_output": str,
        "vtk_output": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisMultimodalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    processed_output_surface: OutputPathType
    """Processed output surface file."""
    annotation_output_file: OutputPathType
    """Output file containing annotation data."""
    overlay_output_file: OutputPathType
    """Output file containing overlay data."""
    csv_output_file: OutputPathType
    """Output CSV file."""

def mris_multimodal_params(
    input_surface: InputPathType,
    target_surface: InputPathType,
    output_surface: str,
    annotation_output: str,
    overlay_output: str,
    csv_output: str,
    fill_holes: bool = False,
    curvature: bool = False,
    thickness: bool = False,
    vtk_output: bool = False,
) -> MrisMultimodal:
    """
    Build parameters.

    Args:
        input_surface: Input surface file.
        target_surface: Target surface file.
        output_surface: Output surface file.
        annotation_output: Output file for annotation data.
        overlay_output: Output file for overlay data.
        csv_output: Output CSV file.
        fill_holes: Flag to fill holes in the surface.
        curvature: Flag to process curvature data.
        thickness: Flag to process thickness data.
        vtk_output: Flag to output VTK file format.

    Returns:
        Parameter dictionary.
    """
    params: MrisMultimodal = {
        "@type": "freesurfer/mris_multimodal",
        "input_surface": input_surface,
        "target_surface": target_surface,
        "output_surface": output_surface,
        "annotation_output": annotation_output,
        "overlay_output": overlay_output,
        "csv_output": csv_output,
        "fill_holes": fill_holes,
        "curvature": curvature,
        "thickness": thickness,
        "vtk_output": vtk_output,
    }
    return params

def mris_multimodal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMultimodal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("target_surface", None) is None:
        raise StyxValidationError("`target_surface` must not be None")
    if not isinstance(params["target_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_surface` has the wrong type: Received `{type(params["target_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("fill_holes", None) is not None:
        if not isinstance(params["fill_holes"], bool):
            raise StyxValidationError(f'`fill_holes` has the wrong type: Received `{type(params["fill_holes"])}` expected `bool`')
    if params.get("curvature", None) is not None:
        if not isinstance(params["curvature"], bool):
            raise StyxValidationError(f'`curvature` has the wrong type: Received `{type(params["curvature"])}` expected `bool`')
    if params.get("thickness", None) is not None:
        if not isinstance(params["thickness"], bool):
            raise StyxValidationError(f'`thickness` has the wrong type: Received `{type(params["thickness"])}` expected `bool`')
    if params.get("annotation_output", None) is None:
        raise StyxValidationError("`annotation_output` must not be None")
    if not isinstance(params["annotation_output"], str):
        raise StyxValidationError(f'`annotation_output` has the wrong type: Received `{type(params["annotation_output"])}` expected `str`')
    if params.get("overlay_output", None) is None:
        raise StyxValidationError("`overlay_output` must not be None")
    if not isinstance(params["overlay_output"], str):
        raise StyxValidationError(f'`overlay_output` has the wrong type: Received `{type(params["overlay_output"])}` expected `str`')
    if params.get("csv_output", None) is None:
        raise StyxValidationError("`csv_output` must not be None")
    if not isinstance(params["csv_output"], str):
        raise StyxValidationError(f'`csv_output` has the wrong type: Received `{type(params["csv_output"])}` expected `str`')
    if params.get("vtk_output", None) is not None:
        if not isinstance(params["vtk_output"], bool):
            raise StyxValidationError(f'`vtk_output` has the wrong type: Received `{type(params["vtk_output"])}` expected `bool`')

def mris_multimodal_cargs(params: MrisMultimodal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_multimodal")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append("-t")
    cargs.append(execution.input_file(params["target_surface"]))
    cargs.append("-o")
    cargs.append(params["output_surface"])
    if params.get("fill_holes"):
        cargs.append("-fillHoles")
    if params.get("curvature"):
        cargs.append("--curvature")
    if params.get("thickness"):
        cargs.append("--thickness")
    cargs.append("-a")
    cargs.append(params["annotation_output"])
    cargs.append("-v")
    cargs.append(params["overlay_output"])
    cargs.append("-c")
    cargs.append(params["csv_output"])
    if params.get("vtk_output"):
        cargs.append("-vtk")
    return cargs

def mris_multimodal_outputs(params: MrisMultimodal, execution: Execution) -> MrisMultimodalOutputs:
    """Build the MrisMultimodalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    processed_output_surface_v: OutputPathType = execution.output_file(params["output_surface"])
    annotation_output_file_v: OutputPathType = execution.output_file(params["annotation_output"])
    overlay_output_file_v: OutputPathType = execution.output_file(params["overlay_output"])
    csv_output_file_v: OutputPathType = execution.output_file(params["csv_output"])
    return MrisMultimodalOutputs(
        root=root_v,
        processed_output_surface=processed_output_surface_v,
        annotation_output_file=annotation_output_file_v,
        overlay_output_file=overlay_output_file_v,
        csv_output_file=csv_output_file_v,
    )

def mris_multimodal_execute(params: MrisMultimodal, runner: Runner | None = None) -> MrisMultimodalOutputs:
    """
    mris_multimodal

    A FreeSurfer tool for processing multimodal surface data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_multimodal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MULTIMODAL_METADATA)
    execution.params(params)
    args = mris_multimodal_cargs(params, execution)
    out = mris_multimodal_outputs(params, execution)
    execution.run(args)
    return out

def mris_multimodal(
    input_surface: InputPathType,
    target_surface: InputPathType,
    output_surface: str,
    annotation_output: str,
    overlay_output: str,
    csv_output: str,
    fill_holes: bool = False,
    curvature: bool = False,
    thickness: bool = False,
    vtk_output: bool = False,
    runner: Runner | None = None,
) -> MrisMultimodalOutputs:
    """
    mris_multimodal

    A FreeSurfer tool for processing multimodal surface data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file.
        target_surface: Target surface file.
        output_surface: Output surface file.
        annotation_output: Output file for annotation data.
        overlay_output: Output file for overlay data.
        csv_output: Output CSV file.
        fill_holes: Flag to fill holes in the surface.
        curvature: Flag to process curvature data.
        thickness: Flag to process thickness data.
        vtk_output: Flag to output VTK file format.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_multimodal_params(
        input_surface=input_surface,
        target_surface=target_surface,
        output_surface=output_surface,
        annotation_output=annotation_output,
        overlay_output=overlay_output,
        csv_output=csv_output,
        fill_holes=fill_holes,
        curvature=curvature,
        thickness=thickness,
        vtk_output=vtk_output,
    )
    return mris_multimodal_execute(params, runner)

__all__ = [
    "MrisMultimodal",
    "MrisMultimodalOutputs",
    "MRIS_MULTIMODAL_METADATA",
    "mris_multimodal_cargs",
    "mris_multimodal_outputs",
    "mris_multimodal_params",
    "mris_multimodal_execute",
    "mris_multimodal_validate",
    "mris_multimodal",
]