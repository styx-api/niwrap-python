# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GROUPSTATSDIFF_METADATA = Metadata(
    id="groupstatsdiff",
    name="groupstatsdiff",
    package="freesurfer",
)

Groupstatsdiff = typing.TypedDict(
    "Groupstatsdiff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/groupstatsdiff"]],
        "group1_dir": str,
        "group2_dir": str,
        "output_dir": str,
        "no_maps": typing.NotRequired[bool],
        "osgm": typing.NotRequired[bool],
        "no_common": typing.NotRequired[bool],
        "allow_subj_diff": typing.NotRequired[bool],
        "no_area": typing.NotRequired[bool],
        "no_volume": typing.NotRequired[bool],
        "no_ba": typing.NotRequired[bool],
        "no_aparcstats": typing.NotRequired[bool],
        "no_asegstats": typing.NotRequired[bool],
        "no_wparcstats": typing.NotRequired[bool],
        "no_stats": typing.NotRequired[bool],
        "no_prune": typing.NotRequired[bool],
        "fwhm_value": typing.NotRequired[float],
        "subjects_dir1": typing.NotRequired[str],
        "subjects_dir2": typing.NotRequired[str],
        "no_dice": typing.NotRequired[bool],
        "dice_ctab": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class GroupstatsdiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def groupstatsdiff_params(
    group1_dir: str,
    group2_dir: str,
    output_dir: str,
    no_maps: bool = False,
    osgm: bool = False,
    no_common: bool = False,
    allow_subj_diff: bool = False,
    no_area: bool = False,
    no_volume: bool = False,
    no_ba: bool = False,
    no_aparcstats: bool = False,
    no_asegstats: bool = False,
    no_wparcstats: bool = False,
    no_stats: bool = False,
    no_prune: bool = False,
    fwhm_value: float | None = None,
    subjects_dir1: str | None = None,
    subjects_dir2: str | None = None,
    no_dice: bool = False,
    dice_ctab: str | None = None,
) -> Groupstatsdiff:
    """
    Build parameters.

    Args:
        group1_dir: Output folder from groupstats for group 1
        group2_dir: Output folder from groupstats for group 2
        output_dir: Output folder for the difference analysis
        no_maps: Only analyze ROI data, no maps
        osgm: Use OSGM instead of native FSGD
        no_common: Do not select common segments when running stattablediff
        allow_subj_diff: Allow list of subjects to be different between the two\
            analyses
        no_area: Do not compute area differences
        no_volume: Do not compute volume differences
        no_ba: Do not compute differences for BA labels
        no_aparcstats: Do not do aparcstats
        no_asegstats: Do not do asegstats
        no_wparcstats: Do not do wmparcstats
        no_stats: Do not do any ROI stats
        no_prune: Do not prune when running mri_glmfit
        fwhm_value: Override the FWHM from group analysis
        subjects_dir1: Subjects directory 1 for computing dice (default is parent\
            dir of groupdir)
        subjects_dir2: Subjects directory 2 for computing dice (default is parent\
            dir of groupdir)
        no_dice: Do not compute dice
        dice_ctab: CTAB to use for dice computation (default is\
            /usr/local/freesurfer/ASegStatsLUT.txt)

    Returns:
        Parameter dictionary.
    """
    params: Groupstatsdiff = {
        "@type": "freesurfer/groupstatsdiff",
        "group1_dir": group1_dir,
        "group2_dir": group2_dir,
        "output_dir": output_dir,
        "no_maps": no_maps,
        "osgm": osgm,
        "no_common": no_common,
        "allow_subj_diff": allow_subj_diff,
        "no_area": no_area,
        "no_volume": no_volume,
        "no_ba": no_ba,
        "no_aparcstats": no_aparcstats,
        "no_asegstats": no_asegstats,
        "no_wparcstats": no_wparcstats,
        "no_stats": no_stats,
        "no_prune": no_prune,
        "no_dice": no_dice,
    }
    if fwhm_value is not None:
        params["fwhm_value"] = fwhm_value
    if subjects_dir1 is not None:
        params["subjects_dir1"] = subjects_dir1
    if subjects_dir2 is not None:
        params["subjects_dir2"] = subjects_dir2
    if dice_ctab is not None:
        params["dice_ctab"] = dice_ctab
    return params

def groupstatsdiff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Groupstatsdiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("group1_dir", None) is None:
        raise StyxValidationError("`group1_dir` must not be None")
    if not isinstance(params["group1_dir"], str):
        raise StyxValidationError(f'`group1_dir` has the wrong type: Received `{type(params["group1_dir"])}` expected `str`')
    if params.get("group2_dir", None) is None:
        raise StyxValidationError("`group2_dir` must not be None")
    if not isinstance(params["group2_dir"], str):
        raise StyxValidationError(f'`group2_dir` has the wrong type: Received `{type(params["group2_dir"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("no_maps", None) is not None:
        if not isinstance(params["no_maps"], bool):
            raise StyxValidationError(f'`no_maps` has the wrong type: Received `{type(params["no_maps"])}` expected `bool`')
    if params.get("osgm", None) is not None:
        if not isinstance(params["osgm"], bool):
            raise StyxValidationError(f'`osgm` has the wrong type: Received `{type(params["osgm"])}` expected `bool`')
    if params.get("no_common", None) is not None:
        if not isinstance(params["no_common"], bool):
            raise StyxValidationError(f'`no_common` has the wrong type: Received `{type(params["no_common"])}` expected `bool`')
    if params.get("allow_subj_diff", None) is not None:
        if not isinstance(params["allow_subj_diff"], bool):
            raise StyxValidationError(f'`allow_subj_diff` has the wrong type: Received `{type(params["allow_subj_diff"])}` expected `bool`')
    if params.get("no_area", None) is not None:
        if not isinstance(params["no_area"], bool):
            raise StyxValidationError(f'`no_area` has the wrong type: Received `{type(params["no_area"])}` expected `bool`')
    if params.get("no_volume", None) is not None:
        if not isinstance(params["no_volume"], bool):
            raise StyxValidationError(f'`no_volume` has the wrong type: Received `{type(params["no_volume"])}` expected `bool`')
    if params.get("no_ba", None) is not None:
        if not isinstance(params["no_ba"], bool):
            raise StyxValidationError(f'`no_ba` has the wrong type: Received `{type(params["no_ba"])}` expected `bool`')
    if params.get("no_aparcstats", None) is not None:
        if not isinstance(params["no_aparcstats"], bool):
            raise StyxValidationError(f'`no_aparcstats` has the wrong type: Received `{type(params["no_aparcstats"])}` expected `bool`')
    if params.get("no_asegstats", None) is not None:
        if not isinstance(params["no_asegstats"], bool):
            raise StyxValidationError(f'`no_asegstats` has the wrong type: Received `{type(params["no_asegstats"])}` expected `bool`')
    if params.get("no_wparcstats", None) is not None:
        if not isinstance(params["no_wparcstats"], bool):
            raise StyxValidationError(f'`no_wparcstats` has the wrong type: Received `{type(params["no_wparcstats"])}` expected `bool`')
    if params.get("no_stats", None) is not None:
        if not isinstance(params["no_stats"], bool):
            raise StyxValidationError(f'`no_stats` has the wrong type: Received `{type(params["no_stats"])}` expected `bool`')
    if params.get("no_prune", None) is not None:
        if not isinstance(params["no_prune"], bool):
            raise StyxValidationError(f'`no_prune` has the wrong type: Received `{type(params["no_prune"])}` expected `bool`')
    if params.get("fwhm_value", None) is not None:
        if not isinstance(params["fwhm_value"], (float, int)):
            raise StyxValidationError(f'`fwhm_value` has the wrong type: Received `{type(params["fwhm_value"])}` expected `float`')
    if params.get("subjects_dir1", None) is not None:
        if not isinstance(params["subjects_dir1"], str):
            raise StyxValidationError(f'`subjects_dir1` has the wrong type: Received `{type(params["subjects_dir1"])}` expected `str`')
    if params.get("subjects_dir2", None) is not None:
        if not isinstance(params["subjects_dir2"], str):
            raise StyxValidationError(f'`subjects_dir2` has the wrong type: Received `{type(params["subjects_dir2"])}` expected `str`')
    if params.get("no_dice", None) is not None:
        if not isinstance(params["no_dice"], bool):
            raise StyxValidationError(f'`no_dice` has the wrong type: Received `{type(params["no_dice"])}` expected `bool`')
    if params.get("dice_ctab", None) is not None:
        if not isinstance(params["dice_ctab"], str):
            raise StyxValidationError(f'`dice_ctab` has the wrong type: Received `{type(params["dice_ctab"])}` expected `str`')

def groupstatsdiff_cargs(params: Groupstatsdiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("groupstatsdiff")
    cargs.append("-g1")
    cargs.append(params["group1_dir"])
    cargs.append("-g2")
    cargs.append(params["group2_dir"])
    cargs.append("-o")
    cargs.append(params["output_dir"])
    if params.get("no_maps"):
        cargs.append("--no-maps")
    if params.get("osgm"):
        cargs.append("--osgm")
    if params.get("no_common"):
        cargs.append("--no-common")
    if params.get("allow_subj_diff"):
        cargs.append("--allow-subj-diff")
    if params.get("no_area"):
        cargs.append("--no-area")
    if params.get("no_volume"):
        cargs.append("--no-volume")
    if params.get("no_ba"):
        cargs.append("--no-ba")
    if params.get("no_aparcstats"):
        cargs.append("--no-aparcstats")
    if params.get("no_asegstats"):
        cargs.append("--no-asegstats")
    if params.get("no_wparcstats"):
        cargs.append("--no-wparcstats")
    if params.get("no_stats"):
        cargs.append("--no-stats")
    if params.get("no_prune"):
        cargs.append("--no-prune")
    v_0 = params.get("fwhm_value")
    if v_0 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_0))
    v_1 = params.get("subjects_dir1")
    if v_1 is not None:
        cargs.append("--sd1")
        cargs.append(v_1)
    v_2 = params.get("subjects_dir2")
    if v_2 is not None:
        cargs.append("--sd2")
        cargs.append(v_2)
    if params.get("no_dice"):
        cargs.append("--no-dice")
    v_3 = params.get("dice_ctab")
    if v_3 is not None:
        cargs.append("--dice-ctab")
        cargs.append(v_3)
    return cargs

def groupstatsdiff_outputs(params: Groupstatsdiff, execution: Execution) -> GroupstatsdiffOutputs:
    """Build the GroupstatsdiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GroupstatsdiffOutputs(
        root=root_v,
    )

def groupstatsdiff_execute(params: Groupstatsdiff, runner: Runner | None = None) -> GroupstatsdiffOutputs:
    """
    groupstatsdiff

    Evaluate the differences between two groupstats outputs from recon-all analyses in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    groupstatsdiff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GROUPSTATSDIFF_METADATA)
    execution.params(params)
    args = groupstatsdiff_cargs(params, execution)
    out = groupstatsdiff_outputs(params, execution)
    execution.run(args)
    return out

def groupstatsdiff(
    group1_dir: str,
    group2_dir: str,
    output_dir: str,
    no_maps: bool = False,
    osgm: bool = False,
    no_common: bool = False,
    allow_subj_diff: bool = False,
    no_area: bool = False,
    no_volume: bool = False,
    no_ba: bool = False,
    no_aparcstats: bool = False,
    no_asegstats: bool = False,
    no_wparcstats: bool = False,
    no_stats: bool = False,
    no_prune: bool = False,
    fwhm_value: float | None = None,
    subjects_dir1: str | None = None,
    subjects_dir2: str | None = None,
    no_dice: bool = False,
    dice_ctab: str | None = None,
    runner: Runner | None = None,
) -> GroupstatsdiffOutputs:
    """
    groupstatsdiff

    Evaluate the differences between two groupstats outputs from recon-all analyses in Freesurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        group1_dir: Output folder from groupstats for group 1
        group2_dir: Output folder from groupstats for group 2
        output_dir: Output folder for the difference analysis
        no_maps: Only analyze ROI data, no maps
        osgm: Use OSGM instead of native FSGD
        no_common: Do not select common segments when running stattablediff
        allow_subj_diff: Allow list of subjects to be different between the two\
            analyses
        no_area: Do not compute area differences
        no_volume: Do not compute volume differences
        no_ba: Do not compute differences for BA labels
        no_aparcstats: Do not do aparcstats
        no_asegstats: Do not do asegstats
        no_wparcstats: Do not do wmparcstats
        no_stats: Do not do any ROI stats
        no_prune: Do not prune when running mri_glmfit
        fwhm_value: Override the FWHM from group analysis
        subjects_dir1: Subjects directory 1 for computing dice (default is parent\
            dir of groupdir)
        subjects_dir2: Subjects directory 2 for computing dice (default is parent\
            dir of groupdir)
        no_dice: Do not compute dice
        dice_ctab: CTAB to use for dice computation (default is\
            /usr/local/freesurfer/ASegStatsLUT.txt)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = groupstatsdiff_params(
        group1_dir=group1_dir,
        group2_dir=group2_dir,
        output_dir=output_dir,
        no_maps=no_maps,
        osgm=osgm,
        no_common=no_common,
        allow_subj_diff=allow_subj_diff,
        no_area=no_area,
        no_volume=no_volume,
        no_ba=no_ba,
        no_aparcstats=no_aparcstats,
        no_asegstats=no_asegstats,
        no_wparcstats=no_wparcstats,
        no_stats=no_stats,
        no_prune=no_prune,
        fwhm_value=fwhm_value,
        subjects_dir1=subjects_dir1,
        subjects_dir2=subjects_dir2,
        no_dice=no_dice,
        dice_ctab=dice_ctab,
    )
    return groupstatsdiff_execute(params, runner)

__all__ = [
    "Groupstatsdiff",
    "GroupstatsdiffOutputs",
    "GROUPSTATSDIFF_METADATA",
    "groupstatsdiff_cargs",
    "groupstatsdiff_outputs",
    "groupstatsdiff_params",
    "groupstatsdiff_execute",
    "groupstatsdiff_validate",
    "groupstatsdiff",
]