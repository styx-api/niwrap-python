# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_SUBJECT_METADATA = Metadata(
    id="label_subject",
    name="label_subject",
    package="freesurfer",
)

LabelSubject = typing.TypedDict(
    "LabelSubject",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/label_subject"]],
        "nu_file": typing.NotRequired[InputPathType],
        "orig_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LabelSubjectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    converted_mnc: OutputPathType
    """Converted MNC file from the original MRI data"""

def label_subject_params(
    nu_file: InputPathType | None = None,
    orig_dir: str | None = None,
) -> LabelSubject:
    """
    Build parameters.

    Args:
        nu_file: Path to the nu.mgz file. If not provided, defaults to nu.mgz.
        orig_dir: The original MRI data directory. If the nu file is not found, it\
            will search in the given directory.

    Returns:
        Parameter dictionary.
    """
    params: LabelSubject = {
        "@type": "freesurfer/label_subject",
    }
    if nu_file is not None:
        params["nu_file"] = nu_file
    if orig_dir is not None:
        params["orig_dir"] = orig_dir
    return params

def label_subject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelSubject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("nu_file", None) is not None:
        if not isinstance(params["nu_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nu_file` has the wrong type: Received `{type(params["nu_file"])}` expected `InputPathType`')
    if params.get("orig_dir", None) is not None:
        if not isinstance(params["orig_dir"], str):
            raise StyxValidationError(f'`orig_dir` has the wrong type: Received `{type(params["orig_dir"])}` expected `str`')

def label_subject_cargs(params: LabelSubject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label_subject")
    v_0 = params.get("nu_file")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("orig_dir")
    if v_1 is not None:
        cargs.append(v_1)
    return cargs

def label_subject_outputs(params: LabelSubject, execution: Execution) -> LabelSubjectOutputs:
    """Build the LabelSubjectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    converted_mnc_v: OutputPathType = execution.output_file("/tmp/nu.mnc")
    return LabelSubjectOutputs(
        root=root_v,
        converted_mnc=converted_mnc_v,
    )

def label_subject_execute(params: LabelSubject, runner: Runner | None = None) -> LabelSubjectOutputs:
    """
    label_subject

    A tool for labeling subject MRI data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_subject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_SUBJECT_METADATA)
    execution.params(params)
    args = label_subject_cargs(params, execution)
    out = label_subject_outputs(params, execution)
    execution.run(args)
    return out

def label_subject(
    nu_file: InputPathType | None = None,
    orig_dir: str | None = None,
    runner: Runner | None = None,
) -> LabelSubjectOutputs:
    """
    label_subject

    A tool for labeling subject MRI data in FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        nu_file: Path to the nu.mgz file. If not provided, defaults to nu.mgz.
        orig_dir: The original MRI data directory. If the nu file is not found, it\
            will search in the given directory.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_subject_params(
        nu_file=nu_file,
        orig_dir=orig_dir,
    )
    return label_subject_execute(params, runner)

__all__ = [
    "LabelSubject",
    "LabelSubjectOutputs",
    "LABEL_SUBJECT_METADATA",
    "label_subject_cargs",
    "label_subject_outputs",
    "label_subject_params",
    "label_subject_execute",
    "label_subject_validate",
    "label_subject",
]