# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIX_SUBJECT_CORRECTED_RH_METADATA = Metadata(
    id="fix_subject_corrected-rh",
    name="fix_subject_corrected-rh",
    package="freesurfer",
)

FixSubjectCorrectedRh = typing.TypedDict(
    "FixSubjectCorrectedRh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fix_subject_corrected-rh"]],
        "subject_dir": str,
    },
)

@dataclasses.dataclass
class FixSubjectCorrectedRhOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    log_file: OutputPathType
    """Log file generated during the execution"""

def fix_subject_corrected_rh_params(
    subject_dir: str,
) -> FixSubjectCorrectedRh:
    """
    Build parameters.

    Args:
        subject_dir:

    Returns:
        Parameter dictionary.
    """
    params: FixSubjectCorrectedRh = {
        "@type": "freesurfer/fix_subject_corrected-rh",
        "subject_dir": subject_dir,
    }
    return params

def fix_subject_corrected_rh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixSubjectCorrectedRh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')

def fix_subject_corrected_rh_cargs(params: FixSubjectCorrectedRh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fix_subject_corrected-rh")
    cargs.append("-rh")
    cargs.append(params["subject_dir"])
    return cargs

def fix_subject_corrected_rh_outputs(params: FixSubjectCorrectedRh, execution: Execution) -> FixSubjectCorrectedRhOutputs:
    """Build the FixSubjectCorrectedRhOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    log_file_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}/scripts_output.log')
    return FixSubjectCorrectedRhOutputs(
        root=root_v,
        log_file=log_file_v,
    )

def fix_subject_corrected_rh_execute(params: FixSubjectCorrectedRh, runner: Runner | None = None) -> FixSubjectCorrectedRhOutputs:
    """
    fix_subject_corrected-rh

    A tool related to FreeSurfer subject correction

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fix_subject_corrected_rh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIX_SUBJECT_CORRECTED_RH_METADATA)
    execution.params(params)
    args = fix_subject_corrected_rh_cargs(params, execution)
    out = fix_subject_corrected_rh_outputs(params, execution)
    execution.run(args)
    return out

def fix_subject_corrected_rh(
    subject_dir: str,
    runner: Runner | None = None,
) -> FixSubjectCorrectedRhOutputs:
    """
    fix_subject_corrected-rh

    A tool related to FreeSurfer subject correction

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fix_subject_corrected_rh_params(
        subject_dir=subject_dir,
    )
    return fix_subject_corrected_rh_execute(params, runner)

__all__ = [
    "FixSubjectCorrectedRh",
    "FixSubjectCorrectedRhOutputs",
    "FIX_SUBJECT_CORRECTED_RH_METADATA",
    "fix_subject_corrected_rh_cargs",
    "fix_subject_corrected_rh_outputs",
    "fix_subject_corrected_rh_params",
    "fix_subject_corrected_rh_execute",
    "fix_subject_corrected_rh_validate",
    "fix_subject_corrected_rh",
]