# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RTVIEW_METADATA = Metadata(
    id="rtview",
    name="rtview",
    package="freesurfer",
)

Rtview = typing.TypedDict(
    "Rtview",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/rtview"]],
        "subject": typing.NotRequired[str],
        "hemi": typing.NotRequired[str],
        "left_hemi": typing.NotRequired[bool],
        "right_hemi": typing.NotRequired[bool],
        "eccen": typing.NotRequired[bool],
        "polar": typing.NotRequired[bool],
        "real_file": typing.NotRequired[InputPathType],
        "imag_file": typing.NotRequired[InputPathType],
        "fsig_file": typing.NotRequired[InputPathType],
        "reg_file": typing.NotRequired[InputPathType],
        "flat_display": typing.NotRequired[bool],
        "patch": typing.NotRequired[str],
        "tcl_file": typing.NotRequired[InputPathType],
        "no_cleanup": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RtviewOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rtview_params(
    subject: str | None = None,
    hemi: str | None = None,
    left_hemi: bool = False,
    right_hemi: bool = False,
    eccen: bool = False,
    polar: bool = False,
    real_file: InputPathType | None = None,
    imag_file: InputPathType | None = None,
    fsig_file: InputPathType | None = None,
    reg_file: InputPathType | None = None,
    flat_display: bool = False,
    patch: str | None = None,
    tcl_file: InputPathType | None = None,
    no_cleanup: bool = False,
) -> Rtview:
    """
    Build parameters.

    Args:
        subject: Subject to use as display
        hemi: Hemisphere to display: 'lh' for left hemisphere or 'rh' for right\
            hemisphere
        left_hemi: Display left hemisphere
        right_hemi: Display right hemisphere
        eccen: Display eccentricity data
        polar: Display polar data
        real_file: File containing real (cosine) values
        imag_file: File containing imaginary (sine) values
        fsig_file: File containing significance values
        reg_file: Registration file for when real/imag/fsig are volumes
        flat_display: Display on occip.patch.flat
        patch: Display on specified patchname
        tcl_file: Use your own TCL command file
        no_cleanup: Don't delete temporary directory

    Returns:
        Parameter dictionary.
    """
    params: Rtview = {
        "@type": "freesurfer/rtview",
        "left_hemi": left_hemi,
        "right_hemi": right_hemi,
        "eccen": eccen,
        "polar": polar,
        "flat_display": flat_display,
        "no_cleanup": no_cleanup,
    }
    if subject is not None:
        params["subject"] = subject
    if hemi is not None:
        params["hemi"] = hemi
    if real_file is not None:
        params["real_file"] = real_file
    if imag_file is not None:
        params["imag_file"] = imag_file
    if fsig_file is not None:
        params["fsig_file"] = fsig_file
    if reg_file is not None:
        params["reg_file"] = reg_file
    if patch is not None:
        params["patch"] = patch
    if tcl_file is not None:
        params["tcl_file"] = tcl_file
    return params

def rtview_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Rtview."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("left_hemi", None) is not None:
        if not isinstance(params["left_hemi"], bool):
            raise StyxValidationError(f'`left_hemi` has the wrong type: Received `{type(params["left_hemi"])}` expected `bool`')
    if params.get("right_hemi", None) is not None:
        if not isinstance(params["right_hemi"], bool):
            raise StyxValidationError(f'`right_hemi` has the wrong type: Received `{type(params["right_hemi"])}` expected `bool`')
    if params.get("eccen", None) is not None:
        if not isinstance(params["eccen"], bool):
            raise StyxValidationError(f'`eccen` has the wrong type: Received `{type(params["eccen"])}` expected `bool`')
    if params.get("polar", None) is not None:
        if not isinstance(params["polar"], bool):
            raise StyxValidationError(f'`polar` has the wrong type: Received `{type(params["polar"])}` expected `bool`')
    if params.get("real_file", None) is not None:
        if not isinstance(params["real_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`real_file` has the wrong type: Received `{type(params["real_file"])}` expected `InputPathType`')
    if params.get("imag_file", None) is not None:
        if not isinstance(params["imag_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`imag_file` has the wrong type: Received `{type(params["imag_file"])}` expected `InputPathType`')
    if params.get("fsig_file", None) is not None:
        if not isinstance(params["fsig_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fsig_file` has the wrong type: Received `{type(params["fsig_file"])}` expected `InputPathType`')
    if params.get("reg_file", None) is not None:
        if not isinstance(params["reg_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reg_file` has the wrong type: Received `{type(params["reg_file"])}` expected `InputPathType`')
    if params.get("flat_display", None) is not None:
        if not isinstance(params["flat_display"], bool):
            raise StyxValidationError(f'`flat_display` has the wrong type: Received `{type(params["flat_display"])}` expected `bool`')
    if params.get("patch", None) is not None:
        if not isinstance(params["patch"], str):
            raise StyxValidationError(f'`patch` has the wrong type: Received `{type(params["patch"])}` expected `str`')
    if params.get("tcl_file", None) is not None:
        if not isinstance(params["tcl_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tcl_file` has the wrong type: Received `{type(params["tcl_file"])}` expected `InputPathType`')
    if params.get("no_cleanup", None) is not None:
        if not isinstance(params["no_cleanup"], bool):
            raise StyxValidationError(f'`no_cleanup` has the wrong type: Received `{type(params["no_cleanup"])}` expected `bool`')

def rtview_cargs(params: Rtview, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rtview")
    v_0 = params.get("subject")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    v_1 = params.get("hemi")
    if v_1 is not None:
        cargs.append("--hemi")
        cargs.append(v_1)
    if params.get("left_hemi"):
        cargs.append("--lh")
    if params.get("right_hemi"):
        cargs.append("--rh")
    if params.get("eccen"):
        cargs.append("--eccen")
    if params.get("polar"):
        cargs.append("--polar")
    v_2 = params.get("real_file")
    if v_2 is not None:
        cargs.append("--real")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("imag_file")
    if v_3 is not None:
        cargs.append("--imag")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("fsig_file")
    if v_4 is not None:
        cargs.append("--fsig")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("reg_file")
    if v_5 is not None:
        cargs.append("--reg")
        cargs.append(execution.input_file(v_5))
    if params.get("flat_display"):
        cargs.append("--flat")
    v_6 = params.get("patch")
    if v_6 is not None:
        cargs.append("--patch")
        cargs.append(v_6)
    v_7 = params.get("tcl_file")
    if v_7 is not None:
        cargs.append("--tcl")
        cargs.append(execution.input_file(v_7))
    if params.get("no_cleanup"):
        cargs.append("--no-cleanup")
    return cargs

def rtview_outputs(params: Rtview, execution: Execution) -> RtviewOutputs:
    """Build the RtviewOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RtviewOutputs(
        root=root_v,
    )

def rtview_execute(params: Rtview, runner: Runner | None = None) -> RtviewOutputs:
    """
    rtview

    View FSFAST version 5 retinotopy data using the color wheel. This is a front-end for tksurfer, setting up the environment for using the color wheel.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rtview_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RTVIEW_METADATA)
    execution.params(params)
    args = rtview_cargs(params, execution)
    out = rtview_outputs(params, execution)
    execution.run(args)
    return out

def rtview(
    subject: str | None = None,
    hemi: str | None = None,
    left_hemi: bool = False,
    right_hemi: bool = False,
    eccen: bool = False,
    polar: bool = False,
    real_file: InputPathType | None = None,
    imag_file: InputPathType | None = None,
    fsig_file: InputPathType | None = None,
    reg_file: InputPathType | None = None,
    flat_display: bool = False,
    patch: str | None = None,
    tcl_file: InputPathType | None = None,
    no_cleanup: bool = False,
    runner: Runner | None = None,
) -> RtviewOutputs:
    """
    rtview

    View FSFAST version 5 retinotopy data using the color wheel. This is a front-end for tksurfer, setting up the environment for using the color wheel.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject to use as display
        hemi: Hemisphere to display: 'lh' for left hemisphere or 'rh' for right\
            hemisphere
        left_hemi: Display left hemisphere
        right_hemi: Display right hemisphere
        eccen: Display eccentricity data
        polar: Display polar data
        real_file: File containing real (cosine) values
        imag_file: File containing imaginary (sine) values
        fsig_file: File containing significance values
        reg_file: Registration file for when real/imag/fsig are volumes
        flat_display: Display on occip.patch.flat
        patch: Display on specified patchname
        tcl_file: Use your own TCL command file
        no_cleanup: Don't delete temporary directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rtview_params(
        subject=subject,
        hemi=hemi,
        left_hemi=left_hemi,
        right_hemi=right_hemi,
        eccen=eccen,
        polar=polar,
        real_file=real_file,
        imag_file=imag_file,
        fsig_file=fsig_file,
        reg_file=reg_file,
        flat_display=flat_display,
        patch=patch,
        tcl_file=tcl_file,
        no_cleanup=no_cleanup,
    )
    return rtview_execute(params, runner)

__all__ = [
    "Rtview",
    "RtviewOutputs",
    "RTVIEW_METADATA",
    "rtview_cargs",
    "rtview_outputs",
    "rtview_params",
    "rtview_execute",
    "rtview_validate",
    "rtview",
]