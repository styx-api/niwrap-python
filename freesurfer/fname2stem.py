# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FNAME2STEM_METADATA = Metadata(
    id="fname2stem",
    name="fname2stem",
    package="freesurfer",
)

Fname2stem = typing.TypedDict(
    "Fname2stem",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fname2stem"]],
        "filename": str,
    },
)

@dataclasses.dataclass
class Fname2stemOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fname2stem_params(
    filename: str,
) -> Fname2stem:
    """
    Build parameters.

    Args:
        filename: The name of the file to convert to a stem. The file does not need\
            to exist.

    Returns:
        Parameter dictionary.
    """
    params: Fname2stem = {
        "@type": "freesurfer/fname2stem",
        "filename": filename,
    }
    return params

def fname2stem_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fname2stem."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename", None) is None:
        raise StyxValidationError("`filename` must not be None")
    if not isinstance(params["filename"], str):
        raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')

def fname2stem_cargs(params: Fname2stem, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fname2stem")
    cargs.append(params["filename"])
    return cargs

def fname2stem_outputs(params: Fname2stem, execution: Execution) -> Fname2stemOutputs:
    """Build the Fname2stemOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Fname2stemOutputs(
        root=root_v,
    )

def fname2stem_execute(params: Fname2stem, runner: Runner | None = None) -> Fname2stemOutputs:
    """
    fname2stem

    Converts the name of a file to a stem. Example: f.mgh, f.nii, f.nii.gz would return f.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fname2stem_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FNAME2STEM_METADATA)
    execution.params(params)
    args = fname2stem_cargs(params, execution)
    out = fname2stem_outputs(params, execution)
    execution.run(args)
    return out

def fname2stem(
    filename: str,
    runner: Runner | None = None,
) -> Fname2stemOutputs:
    """
    fname2stem

    Converts the name of a file to a stem. Example: f.mgh, f.nii, f.nii.gz would return f.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        filename: The name of the file to convert to a stem. The file does not need\
            to exist.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fname2stem_params(
        filename=filename,
    )
    return fname2stem_execute(params, runner)

__all__ = [
    "Fname2stem",
    "Fname2stemOutputs",
    "FNAME2STEM_METADATA",
    "fname2stem_cargs",
    "fname2stem_outputs",
    "fname2stem_params",
    "fname2stem_execute",
    "fname2stem_validate",
    "fname2stem",
]