# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ISOLATE_LABELS_CSH_METADATA = Metadata(
    id="isolate_labels.csh",
    name="isolate_labels.csh",
    package="freesurfer",
)

IsolateLabelsCsh = typing.TypedDict(
    "IsolateLabelsCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/isolate_labels.csh"]],
        "label_volume": InputPathType,
        "output_prefix": str,
        "label_option": typing.NotRequired[str],
        "lowercase_label_option": typing.NotRequired[str],
        "version": typing.NotRequired[bool],
        "keepval": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class IsolateLabelsCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def isolate_labels_csh_params(
    label_volume: InputPathType,
    output_prefix: str,
    label_option: str | None = None,
    lowercase_label_option: str | None = None,
    version: bool = False,
    keepval: bool = False,
    help: bool = False,
) -> IsolateLabelsCsh:
    """
    Build parameters.

    Args:
        label_volume: Label volume to be analyzed
        output_prefix: Prefix of binary label file(s)
        label_option: The particular label to be analyzed; default is all labels.
        lowercase_label_option: The particular label to be analyzed; default is all\
            labels.
        version: Print version and exit
        keepval: Keeps original label values
        help: Print help and exit

    Returns:
        Parameter dictionary.
    """
    params: IsolateLabelsCsh = {
        "@type": "freesurfer/isolate_labels.csh",
        "label_volume": label_volume,
        "output_prefix": output_prefix,
        "version": version,
        "keepval": keepval,
        "help": help,
    }
    if label_option is not None:
        params["label_option"] = label_option
    if lowercase_label_option is not None:
        params["lowercase_label_option"] = lowercase_label_option
    return params

def isolate_labels_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IsolateLabelsCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_volume", None) is None:
        raise StyxValidationError("`label_volume` must not be None")
    if not isinstance(params["label_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_volume` has the wrong type: Received `{type(params["label_volume"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("label_option", None) is not None:
        if not isinstance(params["label_option"], str):
            raise StyxValidationError(f'`label_option` has the wrong type: Received `{type(params["label_option"])}` expected `str`')
    if params.get("lowercase_label_option", None) is not None:
        if not isinstance(params["lowercase_label_option"], str):
            raise StyxValidationError(f'`lowercase_label_option` has the wrong type: Received `{type(params["lowercase_label_option"])}` expected `str`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("keepval", None) is not None:
        if not isinstance(params["keepval"], bool):
            raise StyxValidationError(f'`keepval` has the wrong type: Received `{type(params["keepval"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def isolate_labels_csh_cargs(params: IsolateLabelsCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("isolate_labels.csh")
    cargs.append("--vol")
    cargs.append(execution.input_file(params["label_volume"]))
    cargs.append("--outprefix")
    cargs.append(params["output_prefix"])
    v_0 = params.get("label_option")
    if v_0 is not None:
        cargs.append("--L")
        cargs.append(v_0)
    v_1 = params.get("lowercase_label_option")
    if v_1 is not None:
        cargs.append("--l")
        cargs.append(v_1)
    if params.get("version"):
        cargs.append("--version")
    if params.get("keepval"):
        cargs.append("--keepval")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def isolate_labels_csh_outputs(params: IsolateLabelsCsh, execution: Execution) -> IsolateLabelsCshOutputs:
    """Build the IsolateLabelsCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return IsolateLabelsCshOutputs(
        root=root_v,
    )

def isolate_labels_csh_execute(params: IsolateLabelsCsh, runner: Runner | None = None) -> IsolateLabelsCshOutputs:
    """
    isolate_labels.csh

    Tool to separate out a particular or all labels into individual binary files for subsequent shape analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    isolate_labels_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ISOLATE_LABELS_CSH_METADATA)
    execution.params(params)
    args = isolate_labels_csh_cargs(params, execution)
    out = isolate_labels_csh_outputs(params, execution)
    execution.run(args)
    return out

def isolate_labels_csh(
    label_volume: InputPathType,
    output_prefix: str,
    label_option: str | None = None,
    lowercase_label_option: str | None = None,
    version: bool = False,
    keepval: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> IsolateLabelsCshOutputs:
    """
    isolate_labels.csh

    Tool to separate out a particular or all labels into individual binary files for subsequent shape analysis.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        label_volume: Label volume to be analyzed
        output_prefix: Prefix of binary label file(s)
        label_option: The particular label to be analyzed; default is all labels.
        lowercase_label_option: The particular label to be analyzed; default is all\
            labels.
        version: Print version and exit
        keepval: Keeps original label values
        help: Print help and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = isolate_labels_csh_params(
        label_volume=label_volume,
        output_prefix=output_prefix,
        label_option=label_option,
        lowercase_label_option=lowercase_label_option,
        version=version,
        keepval=keepval,
        help=help,
    )
    return isolate_labels_csh_execute(params, runner)

__all__ = [
    "IsolateLabelsCsh",
    "IsolateLabelsCshOutputs",
    "ISOLATE_LABELS_CSH_METADATA",
    "isolate_labels_csh_cargs",
    "isolate_labels_csh_outputs",
    "isolate_labels_csh_params",
    "isolate_labels_csh_execute",
    "isolate_labels_csh_validate",
    "isolate_labels_csh",
]