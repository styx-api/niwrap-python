# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TKSURFERFV_METADATA = Metadata(
    id="tksurferfv",
    name="tksurferfv",
    package="freesurfer",
)

Tksurferfv = typing.TypedDict(
    "Tksurferfv",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/tksurferfv"]],
        "subject": str,
        "hemi": str,
        "surface": str,
        "tksurfer": typing.NotRequired[bool],
        "all_surfaces": typing.NotRequired[bool],
        "vgl": typing.NotRequired[bool],
        "no_vgl": typing.NotRequired[bool],
        "no_outline": typing.NotRequired[bool],
        "neuro_orientation": typing.NotRequired[bool],
        "rotate_around_cursor": typing.NotRequired[bool],
        "heat_scale": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class TksurferfvOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tksurferfv_params(
    subject: str,
    hemi: str,
    surface: str,
    tksurfer_2: bool = False,
    all_surfaces: bool = False,
    vgl: bool = False,
    no_vgl: bool = False,
    no_outline: bool = False,
    neuro_orientation: bool = False,
    rotate_around_cursor: bool = False,
    heat_scale: str | None = None,
) -> Tksurferfv:
    """
    Build parameters.

    Args:
        subject: Subject identifier.
        hemi: Hemisphere (e.g., lh or rh).
        surface: Surface type.
        tksurfer_2: Use tksurfer instead of freeview.
        all_surfaces: Load white, pial, and inflated surfaces.
        vgl: Run freeview with /usr/pubsw/bin/vglrun.
        no_vgl: Do not run freeview with /usr/pubsw/bin/vglrun.
        no_outline: Do not show annotations as outlines.
        neuro_orientation: Use neurological orientation instead of radiological.
        rotate_around_cursor: Rotate around cursor in 3D view.
        heat_scale: Overlay heat scale (options: linear, linearopaque, piecewise,\
            min_to_max).

    Returns:
        Parameter dictionary.
    """
    params: Tksurferfv = {
        "@type": "freesurfer/tksurferfv",
        "subject": subject,
        "hemi": hemi,
        "surface": surface,
        "tksurfer": tksurfer_2,
        "all_surfaces": all_surfaces,
        "vgl": vgl,
        "no_vgl": no_vgl,
        "no_outline": no_outline,
        "neuro_orientation": neuro_orientation,
        "rotate_around_cursor": rotate_around_cursor,
    }
    if heat_scale is not None:
        params["heat_scale"] = heat_scale
    return params

def tksurferfv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tksurferfv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("tksurfer", None) is not None:
        if not isinstance(params["tksurfer"], bool):
            raise StyxValidationError(f'`tksurfer` has the wrong type: Received `{type(params["tksurfer"])}` expected `bool`')
    if params.get("all_surfaces", None) is not None:
        if not isinstance(params["all_surfaces"], bool):
            raise StyxValidationError(f'`all_surfaces` has the wrong type: Received `{type(params["all_surfaces"])}` expected `bool`')
    if params.get("vgl", None) is not None:
        if not isinstance(params["vgl"], bool):
            raise StyxValidationError(f'`vgl` has the wrong type: Received `{type(params["vgl"])}` expected `bool`')
    if params.get("no_vgl", None) is not None:
        if not isinstance(params["no_vgl"], bool):
            raise StyxValidationError(f'`no_vgl` has the wrong type: Received `{type(params["no_vgl"])}` expected `bool`')
    if params.get("no_outline", None) is not None:
        if not isinstance(params["no_outline"], bool):
            raise StyxValidationError(f'`no_outline` has the wrong type: Received `{type(params["no_outline"])}` expected `bool`')
    if params.get("neuro_orientation", None) is not None:
        if not isinstance(params["neuro_orientation"], bool):
            raise StyxValidationError(f'`neuro_orientation` has the wrong type: Received `{type(params["neuro_orientation"])}` expected `bool`')
    if params.get("rotate_around_cursor", None) is not None:
        if not isinstance(params["rotate_around_cursor"], bool):
            raise StyxValidationError(f'`rotate_around_cursor` has the wrong type: Received `{type(params["rotate_around_cursor"])}` expected `bool`')
    if params.get("heat_scale", None) is not None:
        if not isinstance(params["heat_scale"], str):
            raise StyxValidationError(f'`heat_scale` has the wrong type: Received `{type(params["heat_scale"])}` expected `str`')

def tksurferfv_cargs(params: Tksurferfv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tksurferfv")
    cargs.append(params["subject"])
    cargs.append(params["hemi"])
    cargs.append(params["surface"])
    if params.get("tksurfer"):
        cargs.append("-tksurfer")
    if params.get("all_surfaces"):
        cargs.append("-all")
    if params.get("vgl"):
        cargs.append("-vgl")
    if params.get("no_vgl"):
        cargs.append("-no-vgl")
    if params.get("no_outline"):
        cargs.append("-no-outline")
    if params.get("neuro_orientation"):
        cargs.append("-neuro")
    if params.get("rotate_around_cursor"):
        cargs.append("-rca")
    v_0 = params.get("heat_scale")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def tksurferfv_outputs(params: Tksurferfv, execution: Execution) -> TksurferfvOutputs:
    """Build the TksurferfvOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TksurferfvOutputs(
        root=root_v,
    )

def tksurferfv_execute(params: Tksurferfv, runner: Runner | None = None) -> TksurferfvOutputs:
    """
    tksurferfv

    A script that runs freeview with arguments similar to tksurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tksurferfv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TKSURFERFV_METADATA)
    execution.params(params)
    args = tksurferfv_cargs(params, execution)
    out = tksurferfv_outputs(params, execution)
    execution.run(args)
    return out

def tksurferfv(
    subject: str,
    hemi: str,
    surface: str,
    tksurfer_2: bool = False,
    all_surfaces: bool = False,
    vgl: bool = False,
    no_vgl: bool = False,
    no_outline: bool = False,
    neuro_orientation: bool = False,
    rotate_around_cursor: bool = False,
    heat_scale: str | None = None,
    runner: Runner | None = None,
) -> TksurferfvOutputs:
    """
    tksurferfv

    A script that runs freeview with arguments similar to tksurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier.
        hemi: Hemisphere (e.g., lh or rh).
        surface: Surface type.
        tksurfer_2: Use tksurfer instead of freeview.
        all_surfaces: Load white, pial, and inflated surfaces.
        vgl: Run freeview with /usr/pubsw/bin/vglrun.
        no_vgl: Do not run freeview with /usr/pubsw/bin/vglrun.
        no_outline: Do not show annotations as outlines.
        neuro_orientation: Use neurological orientation instead of radiological.
        rotate_around_cursor: Rotate around cursor in 3D view.
        heat_scale: Overlay heat scale (options: linear, linearopaque, piecewise,\
            min_to_max).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tksurferfv_params(
        subject=subject,
        hemi=hemi,
        surface=surface,
        tksurfer_2=tksurfer_2,
        all_surfaces=all_surfaces,
        vgl=vgl,
        no_vgl=no_vgl,
        no_outline=no_outline,
        neuro_orientation=neuro_orientation,
        rotate_around_cursor=rotate_around_cursor,
        heat_scale=heat_scale,
    )
    return tksurferfv_execute(params, runner)

__all__ = [
    "Tksurferfv",
    "TksurferfvOutputs",
    "TKSURFERFV_METADATA",
    "tksurferfv_cargs",
    "tksurferfv_outputs",
    "tksurferfv_params",
    "tksurferfv_execute",
    "tksurferfv_validate",
    "tksurferfv",
]