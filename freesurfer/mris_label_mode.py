# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_LABEL_MODE_METADATA = Metadata(
    id="mris_label_mode",
    name="mris_label_mode",
    package="freesurfer",
)

MrisLabelMode = typing.TypedDict(
    "MrisLabelMode",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_label_mode"]],
        "input_curv_file": InputPathType,
        "hemi": str,
        "surface": str,
        "subject": list[str],
        "output_curv_file": str,
        "summary_statistics": typing.NotRequired[bool],
        "statistics_cond": typing.NotRequired[str],
        "output_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrisLabelModeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mris_label_mode_params(
    input_curv_file: InputPathType,
    hemi: str,
    surface: str,
    subject: list[str],
    output_curv_file: str,
    summary_statistics: bool = False,
    statistics_cond: str | None = None,
    output_directory: str | None = None,
) -> MrisLabelMode:
    """
    Build parameters.

    Args:
        input_curv_file: Input curvature file
        hemi: Hemisphere
        surface: Surface name
        subject: Subject name(s)
        output_curv_file: Output curvature file
        summary_statistics: Generate summary statistics
        statistics_cond: Condition number for summary statistics
        output_directory: Override the last subject as the output surface

    Returns:
        Parameter dictionary.
    """
    params: MrisLabelMode = {
        "@type": "freesurfer/mris_label_mode",
        "input_curv_file": input_curv_file,
        "hemi": hemi,
        "surface": surface,
        "subject": subject,
        "output_curv_file": output_curv_file,
        "summary_statistics": summary_statistics,
    }
    if statistics_cond is not None:
        params["statistics_cond"] = statistics_cond
    if output_directory is not None:
        params["output_directory"] = output_directory
    return params

def mris_label_mode_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisLabelMode."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_curv_file", None) is None:
        raise StyxValidationError("`input_curv_file` must not be None")
    if not isinstance(params["input_curv_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_curv_file` has the wrong type: Received `{type(params["input_curv_file"])}` expected `InputPathType`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], list):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `list[str]`')
    for e in params["subject"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output_curv_file", None) is None:
        raise StyxValidationError("`output_curv_file` must not be None")
    if not isinstance(params["output_curv_file"], str):
        raise StyxValidationError(f'`output_curv_file` has the wrong type: Received `{type(params["output_curv_file"])}` expected `str`')
    if params.get("summary_statistics", None) is not None:
        if not isinstance(params["summary_statistics"], bool):
            raise StyxValidationError(f'`summary_statistics` has the wrong type: Received `{type(params["summary_statistics"])}` expected `bool`')
    if params.get("statistics_cond", None) is not None:
        if not isinstance(params["statistics_cond"], str):
            raise StyxValidationError(f'`statistics_cond` has the wrong type: Received `{type(params["statistics_cond"])}` expected `str`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def mris_label_mode_cargs(params: MrisLabelMode, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_label_mode")
    cargs.append(execution.input_file(params["input_curv_file"]))
    cargs.append(params["hemi"])
    cargs.append(params["surface"])
    for item0 in params["subject"]:
        cargs.append(item0)
    cargs.append(params["output_curv_file"])
    if params.get("summary_statistics"):
        cargs.append("-s")
    v_0 = params.get("statistics_cond")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("output_directory")
    if v_1 is not None:
        cargs.append("-o")
        cargs.append(v_1)
    return cargs

def mris_label_mode_outputs(params: MrisLabelMode, execution: Execution) -> MrisLabelModeOutputs:
    """Build the MrisLabelModeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrisLabelModeOutputs(
        root=root_v,
    )

def mris_label_mode_execute(params: MrisLabelMode, runner: Runner | None = None) -> MrisLabelModeOutputs:
    """
    mris_label_mode

    This program will add a template into an average surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_label_mode_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_LABEL_MODE_METADATA)
    execution.params(params)
    args = mris_label_mode_cargs(params, execution)
    out = mris_label_mode_outputs(params, execution)
    execution.run(args)
    return out

def mris_label_mode(
    input_curv_file: InputPathType,
    hemi: str,
    surface: str,
    subject: list[str],
    output_curv_file: str,
    summary_statistics: bool = False,
    statistics_cond: str | None = None,
    output_directory: str | None = None,
    runner: Runner | None = None,
) -> MrisLabelModeOutputs:
    """
    mris_label_mode

    This program will add a template into an average surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_curv_file: Input curvature file
        hemi: Hemisphere
        surface: Surface name
        subject: Subject name(s)
        output_curv_file: Output curvature file
        summary_statistics: Generate summary statistics
        statistics_cond: Condition number for summary statistics
        output_directory: Override the last subject as the output surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_label_mode_params(
        input_curv_file=input_curv_file,
        hemi=hemi,
        surface=surface,
        subject=subject,
        output_curv_file=output_curv_file,
        summary_statistics=summary_statistics,
        statistics_cond=statistics_cond,
        output_directory=output_directory,
    )
    return mris_label_mode_execute(params, runner)

__all__ = [
    "MrisLabelMode",
    "MrisLabelModeOutputs",
    "MRIS_LABEL_MODE_METADATA",
    "mris_label_mode_cargs",
    "mris_label_mode_outputs",
    "mris_label_mode_params",
    "mris_label_mode_execute",
    "mris_label_mode_validate",
    "mris_label_mode",
]