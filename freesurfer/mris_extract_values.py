# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_EXTRACT_VALUES_METADATA = Metadata(
    id="mris_extract_values",
    name="mris_extract_values",
    package="freesurfer",
)

MrisExtractValues = typing.TypedDict(
    "MrisExtractValues",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_extract_values"]],
        "surface": InputPathType,
        "overlay": InputPathType,
        "annotation": InputPathType,
        "csvfile": str,
        "num_images": float,
        "image_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class MrisExtractValuesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_csv: OutputPathType
    """The CSV file generated by the tool"""

def mris_extract_values_params(
    surface: InputPathType,
    overlay: InputPathType,
    annotation: InputPathType,
    csvfile: str,
    num_images: float,
    image_files: list[InputPathType],
) -> MrisExtractValues:
    """
    Build parameters.

    Args:
        surface:
        overlay:
        annotation:
        csvfile:
        num_images:
        image_files:

    Returns:
        Parameter dictionary.
    """
    params: MrisExtractValues = {
        "@type": "freesurfer/mris_extract_values",
        "surface": surface,
        "overlay": overlay,
        "annotation": annotation,
        "csvfile": csvfile,
        "num_images": num_images,
        "image_files": image_files,
    }
    return params

def mris_extract_values_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisExtractValues."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("overlay", None) is None:
        raise StyxValidationError("`overlay` must not be None")
    if not isinstance(params["overlay"], (pathlib.Path, str)):
        raise StyxValidationError(f'`overlay` has the wrong type: Received `{type(params["overlay"])}` expected `InputPathType`')
    if params.get("annotation", None) is None:
        raise StyxValidationError("`annotation` must not be None")
    if not isinstance(params["annotation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`annotation` has the wrong type: Received `{type(params["annotation"])}` expected `InputPathType`')
    if params.get("csvfile", None) is None:
        raise StyxValidationError("`csvfile` must not be None")
    if not isinstance(params["csvfile"], str):
        raise StyxValidationError(f'`csvfile` has the wrong type: Received `{type(params["csvfile"])}` expected `str`')
    if params.get("num_images", None) is None:
        raise StyxValidationError("`num_images` must not be None")
    if not isinstance(params["num_images"], (float, int)):
        raise StyxValidationError(f'`num_images` has the wrong type: Received `{type(params["num_images"])}` expected `float`')
    if params.get("image_files", None) is None:
        raise StyxValidationError("`image_files` must not be None")
    if not isinstance(params["image_files"], list):
        raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(params["image_files"])}` expected `list[InputPathType]`')
    for e in params["image_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def mris_extract_values_cargs(params: MrisExtractValues, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_extract_values")
    cargs.append("-i")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append("-v")
    cargs.append(execution.input_file(params["overlay"]))
    cargs.append("-a")
    cargs.append(execution.input_file(params["annotation"]))
    cargs.append("-o")
    cargs.append(params["csvfile"])
    cargs.append("-m")
    cargs.append(str(params["num_images"]))
    cargs.append("--images")
    for item0 in params["image_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def mris_extract_values_outputs(params: MrisExtractValues, execution: Execution) -> MrisExtractValuesOutputs:
    """Build the MrisExtractValuesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_csv_v: OutputPathType = execution.output_file(params["csvfile"])
    return MrisExtractValuesOutputs(
        root=root_v,
        output_csv=output_csv_v,
    )

def mris_extract_values_execute(params: MrisExtractValues, runner: Runner | None = None) -> MrisExtractValuesOutputs:
    """
    mris_extract_values

    Extracts values from surface, overlay, and annotation files and outputs them to a CSV file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_extract_values_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_EXTRACT_VALUES_METADATA)
    execution.params(params)
    args = mris_extract_values_cargs(params, execution)
    out = mris_extract_values_outputs(params, execution)
    execution.run(args)
    return out

def mris_extract_values(
    surface: InputPathType,
    overlay: InputPathType,
    annotation: InputPathType,
    csvfile: str,
    num_images: float,
    image_files: list[InputPathType],
    runner: Runner | None = None,
) -> MrisExtractValuesOutputs:
    """
    mris_extract_values

    Extracts values from surface, overlay, and annotation files and outputs them to a CSV file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface:
        overlay:
        annotation:
        csvfile:
        num_images:
        image_files:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_extract_values_params(
        surface=surface,
        overlay=overlay,
        annotation=annotation,
        csvfile=csvfile,
        num_images=num_images,
        image_files=image_files,
    )
    return mris_extract_values_execute(params, runner)

__all__ = [
    "MrisExtractValues",
    "MrisExtractValuesOutputs",
    "MRIS_EXTRACT_VALUES_METADATA",
    "mris_extract_values_cargs",
    "mris_extract_values_outputs",
    "mris_extract_values_params",
    "mris_extract_values_execute",
    "mris_extract_values_validate",
    "mris_extract_values",
]