# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_CHECK_VERSION_METADATA = Metadata(
    id="fs-check-version",
    name="fs-check-version",
    package="freesurfer",
)

FsCheckVersion = typing.TypedDict(
    "FsCheckVersion",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs-check-version"]],
        "subjects_dir": str,
        "outfile": str,
        "subject": typing.NotRequired[str],
        "require_match": typing.NotRequired[bool],
        "no_require_match": typing.NotRequired[bool],
        "test": typing.NotRequired[bool],
        "test_debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FsCheckVersionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output text file with a 1 if the version matches, otherwise 0"""

def fs_check_version_params(
    subjects_dir: str,
    outfile: str,
    subject: str | None = None,
    require_match: bool = False,
    no_require_match: bool = False,
    test: bool = False,
    test_debug: bool = False,
) -> FsCheckVersion:
    """
    Build parameters.

    Args:
        subjects_dir: Subjects directory path
        outfile: Output file path where result of version check will be written
        subject: Subject name (optional)
        require_match: Set REQUIRE_FS_MATCH for testing
        no_require_match: Unset REQUIRE_FS_MATCH for testing
        test: Go through permutations for testing
        test_debug: Go through permutations for debugging

    Returns:
        Parameter dictionary.
    """
    params: FsCheckVersion = {
        "@type": "freesurfer/fs-check-version",
        "subjects_dir": subjects_dir,
        "outfile": outfile,
        "require_match": require_match,
        "no_require_match": no_require_match,
        "test": test,
        "test_debug": test_debug,
    }
    if subject is not None:
        params["subject"] = subject
    return params

def fs_check_version_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsCheckVersion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("subject", None) is not None:
        if not isinstance(params["subject"], str):
            raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("require_match", None) is not None:
        if not isinstance(params["require_match"], bool):
            raise StyxValidationError(f'`require_match` has the wrong type: Received `{type(params["require_match"])}` expected `bool`')
    if params.get("no_require_match", None) is not None:
        if not isinstance(params["no_require_match"], bool):
            raise StyxValidationError(f'`no_require_match` has the wrong type: Received `{type(params["no_require_match"])}` expected `bool`')
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')
    if params.get("test_debug", None) is not None:
        if not isinstance(params["test_debug"], bool):
            raise StyxValidationError(f'`test_debug` has the wrong type: Received `{type(params["test_debug"])}` expected `bool`')

def fs_check_version_cargs(params: FsCheckVersion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs-check-version")
    cargs.append("--sd")
    cargs.append(params["subjects_dir"])
    cargs.append("--o")
    cargs.append(params["outfile"])
    v_0 = params.get("subject")
    if v_0 is not None:
        cargs.append("--s")
        cargs.append(v_0)
    if params.get("require_match"):
        cargs.append("--require-match")
    if params.get("no_require_match"):
        cargs.append("--no-require-match")
    if params.get("test"):
        cargs.append("--test")
    if params.get("test_debug"):
        cargs.append("--test-debug")
    return cargs

def fs_check_version_outputs(params: FsCheckVersion, execution: Execution) -> FsCheckVersionOutputs:
    """Build the FsCheckVersionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["outfile"])
    return FsCheckVersionOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def fs_check_version_execute(params: FsCheckVersion, runner: Runner | None = None) -> FsCheckVersionOutputs:
    """
    fs-check-version

    Script to manage which version of FreeSurfer can be used to analyze data ensuring consistency with the desired version.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_check_version_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_CHECK_VERSION_METADATA)
    execution.params(params)
    args = fs_check_version_cargs(params, execution)
    out = fs_check_version_outputs(params, execution)
    execution.run(args)
    return out

def fs_check_version(
    subjects_dir: str,
    outfile: str,
    subject: str | None = None,
    require_match: bool = False,
    no_require_match: bool = False,
    test: bool = False,
    test_debug: bool = False,
    runner: Runner | None = None,
) -> FsCheckVersionOutputs:
    """
    fs-check-version

    Script to manage which version of FreeSurfer can be used to analyze data ensuring consistency with the desired version.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects_dir: Subjects directory path
        outfile: Output file path where result of version check will be written
        subject: Subject name (optional)
        require_match: Set REQUIRE_FS_MATCH for testing
        no_require_match: Unset REQUIRE_FS_MATCH for testing
        test: Go through permutations for testing
        test_debug: Go through permutations for debugging
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_check_version_params(
        subjects_dir=subjects_dir,
        outfile=outfile,
        subject=subject,
        require_match=require_match,
        no_require_match=no_require_match,
        test=test,
        test_debug=test_debug,
    )
    return fs_check_version_execute(params, runner)

__all__ = [
    "FsCheckVersion",
    "FsCheckVersionOutputs",
    "FS_CHECK_VERSION_METADATA",
    "fs_check_version_cargs",
    "fs_check_version_outputs",
    "fs_check_version_params",
    "fs_check_version_execute",
    "fs_check_version_validate",
    "fs_check_version",
]