# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_REORIENT_LR_CSH_METADATA = Metadata(
    id="mri_reorient_LR.csh",
    name="mri_reorient_LR.csh",
    package="freesurfer",
)

MriReorientLrCsh = typing.TypedDict(
    "MriReorientLrCsh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_reorient_LR.csh"]],
        "input_vol": InputPathType,
        "output_vol": str,
        "display_result": typing.NotRequired[bool],
        "clean_files": typing.NotRequired[bool],
        "output_registration": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriReorientLrCshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reoriented_vol: OutputPathType
    """Reoriented volume"""

def mri_reorient_lr_csh_params(
    input_vol: InputPathType,
    output_vol: str,
    display_result: bool = False,
    clean_files: bool = False,
    output_registration: bool = False,
    version: bool = False,
    help: bool = False,
) -> MriReorientLrCsh:
    """
    Build parameters.

    Args:
        input_vol: Input file to be reoriented
        output_vol: Reoriented input file
        display_result: Display registration result using FreeView
        clean_files: Delete all auxiliary and registration files
        output_registration: Write out the registration file that is applied to the\
            reoriented input file (fslmat or lta)
        version: Print version and exit
        help: Print help and exit

    Returns:
        Parameter dictionary.
    """
    params: MriReorientLrCsh = {
        "@type": "freesurfer/mri_reorient_LR.csh",
        "input_vol": input_vol,
        "output_vol": output_vol,
        "display_result": display_result,
        "clean_files": clean_files,
        "output_registration": output_registration,
        "version": version,
        "help": help,
    }
    return params

def mri_reorient_lr_csh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriReorientLrCsh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_vol", None) is None:
        raise StyxValidationError("`input_vol` must not be None")
    if not isinstance(params["input_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_vol` has the wrong type: Received `{type(params["input_vol"])}` expected `InputPathType`')
    if params.get("output_vol", None) is None:
        raise StyxValidationError("`output_vol` must not be None")
    if not isinstance(params["output_vol"], str):
        raise StyxValidationError(f'`output_vol` has the wrong type: Received `{type(params["output_vol"])}` expected `str`')
    if params.get("display_result", None) is not None:
        if not isinstance(params["display_result"], bool):
            raise StyxValidationError(f'`display_result` has the wrong type: Received `{type(params["display_result"])}` expected `bool`')
    if params.get("clean_files", None) is not None:
        if not isinstance(params["clean_files"], bool):
            raise StyxValidationError(f'`clean_files` has the wrong type: Received `{type(params["clean_files"])}` expected `bool`')
    if params.get("output_registration", None) is not None:
        if not isinstance(params["output_registration"], bool):
            raise StyxValidationError(f'`output_registration` has the wrong type: Received `{type(params["output_registration"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def mri_reorient_lr_csh_cargs(params: MriReorientLrCsh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_reorient_LR.csh")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_vol"]))
    cargs.append("--o")
    cargs.append(params["output_vol"])
    if params.get("display_result"):
        cargs.append("--disp")
    if params.get("clean_files"):
        cargs.append("--clean")
    if params.get("output_registration"):
        cargs.append("--outreg")
    if params.get("version"):
        cargs.append("--version")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def mri_reorient_lr_csh_outputs(params: MriReorientLrCsh, execution: Execution) -> MriReorientLrCshOutputs:
    """Build the MriReorientLrCshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reoriented_vol_v: OutputPathType = execution.output_file(params["output_vol"])
    return MriReorientLrCshOutputs(
        root=root_v,
        reoriented_vol=reoriented_vol_v,
    )

def mri_reorient_lr_csh_execute(params: MriReorientLrCsh, runner: Runner | None = None) -> MriReorientLrCshOutputs:
    """
    mri_reorient_LR.csh

    A script to reorient MRI volumes from left-right orientation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_reorient_lr_csh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_REORIENT_LR_CSH_METADATA)
    execution.params(params)
    args = mri_reorient_lr_csh_cargs(params, execution)
    out = mri_reorient_lr_csh_outputs(params, execution)
    execution.run(args)
    return out

def mri_reorient_lr_csh(
    input_vol: InputPathType,
    output_vol: str,
    display_result: bool = False,
    clean_files: bool = False,
    output_registration: bool = False,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> MriReorientLrCshOutputs:
    """
    mri_reorient_LR.csh

    A script to reorient MRI volumes from left-right orientation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_vol: Input file to be reoriented
        output_vol: Reoriented input file
        display_result: Display registration result using FreeView
        clean_files: Delete all auxiliary and registration files
        output_registration: Write out the registration file that is applied to the\
            reoriented input file (fslmat or lta)
        version: Print version and exit
        help: Print help and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_reorient_lr_csh_params(
        input_vol=input_vol,
        output_vol=output_vol,
        display_result=display_result,
        clean_files=clean_files,
        output_registration=output_registration,
        version=version,
        help=help,
    )
    return mri_reorient_lr_csh_execute(params, runner)

__all__ = [
    "MriReorientLrCsh",
    "MriReorientLrCshOutputs",
    "MRI_REORIENT_LR_CSH_METADATA",
    "mri_reorient_lr_csh_cargs",
    "mri_reorient_lr_csh_outputs",
    "mri_reorient_lr_csh_params",
    "mri_reorient_lr_csh_execute",
    "mri_reorient_lr_csh_validate",
    "mri_reorient_lr_csh",
]