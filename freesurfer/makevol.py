# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKEVOL_METADATA = Metadata(
    id="makevol",
    name="makevol",
    package="freesurfer",
)

Makevol = typing.TypedDict(
    "Makevol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/makevol"]],
        "filename": typing.NotRequired[str],
        "width": typing.NotRequired[int],
        "height": typing.NotRequired[int],
        "depth": typing.NotRequired[int],
        "sizex": typing.NotRequired[float],
        "sizey": typing.NotRequired[float],
        "sizez": typing.NotRequired[float],
        "set_method": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MakevolOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """The created volume file."""

def makevol_params(
    filename: str | None = None,
    width: int | None = None,
    height: int | None = None,
    depth: int | None = None,
    sizex: float | None = None,
    sizey: float | None = None,
    sizez: float | None = None,
    set_method: str | None = None,
) -> Makevol:
    """
    Build parameters.

    Args:
        filename: Write volume to the given file name, implying type.
        width: Use integer WIDTH as the x dimension.
        height: Use integer HEIGHT as the y dimension.
        depth: Use integer DEPTH as the z dimension.
        sizex: Use float SIZEX as the x resolution.
        sizey: Use float SIZEY as the y resolution.
        sizez: Use float SIZEZ as the z resolution.
        set_method: Use METHOD to fill the values. Methods: xyz, random, constant.

    Returns:
        Parameter dictionary.
    """
    params: Makevol = {
        "@type": "freesurfer/makevol",
    }
    if filename is not None:
        params["filename"] = filename
    if width is not None:
        params["width"] = width
    if height is not None:
        params["height"] = height
    if depth is not None:
        params["depth"] = depth
    if sizex is not None:
        params["sizex"] = sizex
    if sizey is not None:
        params["sizey"] = sizey
    if sizez is not None:
        params["sizez"] = sizez
    if set_method is not None:
        params["set_method"] = set_method
    return params

def makevol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Makevol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename", None) is not None:
        if not isinstance(params["filename"], str):
            raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')
    if params.get("width", None) is not None:
        if not isinstance(params["width"], int):
            raise StyxValidationError(f'`width` has the wrong type: Received `{type(params["width"])}` expected `int`')
    if params.get("height", None) is not None:
        if not isinstance(params["height"], int):
            raise StyxValidationError(f'`height` has the wrong type: Received `{type(params["height"])}` expected `int`')
    if params.get("depth", None) is not None:
        if not isinstance(params["depth"], int):
            raise StyxValidationError(f'`depth` has the wrong type: Received `{type(params["depth"])}` expected `int`')
    if params.get("sizex", None) is not None:
        if not isinstance(params["sizex"], (float, int)):
            raise StyxValidationError(f'`sizex` has the wrong type: Received `{type(params["sizex"])}` expected `float`')
    if params.get("sizey", None) is not None:
        if not isinstance(params["sizey"], (float, int)):
            raise StyxValidationError(f'`sizey` has the wrong type: Received `{type(params["sizey"])}` expected `float`')
    if params.get("sizez", None) is not None:
        if not isinstance(params["sizez"], (float, int)):
            raise StyxValidationError(f'`sizez` has the wrong type: Received `{type(params["sizez"])}` expected `float`')
    if params.get("set_method", None) is not None:
        if not isinstance(params["set_method"], str):
            raise StyxValidationError(f'`set_method` has the wrong type: Received `{type(params["set_method"])}` expected `str`')

def makevol_cargs(params: Makevol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("makevol")
    v_0 = params.get("filename")
    if v_0 is not None:
        cargs.append("-f")
        cargs.append(v_0)
    v_1 = params.get("width")
    if v_1 is not None:
        cargs.append("-x")
        cargs.append(str(v_1))
    v_2 = params.get("height")
    if v_2 is not None:
        cargs.append("-y")
        cargs.append(str(v_2))
    v_3 = params.get("depth")
    if v_3 is not None:
        cargs.append("-z")
        cargs.append(str(v_3))
    v_4 = params.get("sizex")
    if v_4 is not None:
        cargs.append("--sizex")
        cargs.append(str(v_4))
    v_5 = params.get("sizey")
    if v_5 is not None:
        cargs.append("--sizey")
        cargs.append(str(v_5))
    v_6 = params.get("sizez")
    if v_6 is not None:
        cargs.append("--sizez")
        cargs.append(str(v_6))
    v_7 = params.get("set_method")
    if v_7 is not None:
        cargs.append("--set-method")
        cargs.append(v_7)
    return cargs

def makevol_outputs(params: Makevol, execution: Execution) -> MakevolOutputs:
    """Build the MakevolOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("filename")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MakevolOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def makevol_execute(params: Makevol, runner: Runner | None = None) -> MakevolOutputs:
    """
    makevol

    A tool to create a volume with given parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    makevol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKEVOL_METADATA)
    execution.params(params)
    args = makevol_cargs(params, execution)
    out = makevol_outputs(params, execution)
    execution.run(args)
    return out

def makevol(
    filename: str | None = None,
    width: int | None = None,
    height: int | None = None,
    depth: int | None = None,
    sizex: float | None = None,
    sizey: float | None = None,
    sizez: float | None = None,
    set_method: str | None = None,
    runner: Runner | None = None,
) -> MakevolOutputs:
    """
    makevol

    A tool to create a volume with given parameters.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        filename: Write volume to the given file name, implying type.
        width: Use integer WIDTH as the x dimension.
        height: Use integer HEIGHT as the y dimension.
        depth: Use integer DEPTH as the z dimension.
        sizex: Use float SIZEX as the x resolution.
        sizey: Use float SIZEY as the y resolution.
        sizez: Use float SIZEZ as the z resolution.
        set_method: Use METHOD to fill the values. Methods: xyz, random, constant.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = makevol_params(
        filename=filename,
        width=width,
        height=height,
        depth=depth,
        sizex=sizex,
        sizey=sizey,
        sizez=sizez,
        set_method=set_method,
    )
    return makevol_execute(params, runner)

__all__ = [
    "Makevol",
    "MakevolOutputs",
    "MAKEVOL_METADATA",
    "makevol_cargs",
    "makevol_outputs",
    "makevol_params",
    "makevol_execute",
    "makevol_validate",
    "makevol",
]