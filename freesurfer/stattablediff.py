# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

STATTABLEDIFF_METADATA = Metadata(
    id="stattablediff",
    name="stattablediff",
    package="freesurfer",
)

Stattablediff = typing.TypedDict(
    "Stattablediff",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/stattablediff"]],
        "t1": InputPathType,
        "t2": InputPathType,
        "output": str,
        "percent_diff": typing.NotRequired[bool],
        "percent_diff_t1": typing.NotRequired[bool],
        "percent_diff_t2": typing.NotRequired[bool],
        "multiply": typing.NotRequired[float],
        "divide": typing.NotRequired[float],
        "common": typing.NotRequired[bool],
        "remove_exvivo": typing.NotRequired[bool],
        "diff_subjects": typing.NotRequired[bool],
        "noreplace53": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class StattablediffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_diff_table: OutputPathType
    """Generated table of differences"""

def stattablediff_params(
    t1: InputPathType,
    t2: InputPathType,
    output: str,
    percent_diff: bool = False,
    percent_diff_t1: bool = False,
    percent_diff_t2: bool = False,
    multiply: float | None = None,
    divide: float | None = None,
    common: bool = False,
    remove_exvivo: bool = False,
    diff_subjects: bool = False,
    noreplace53: bool = False,
) -> Stattablediff:
    """
    Build parameters.

    Args:
        t1: Input table 1 (output of asegstats2table or aparcstats2table)
        t2: Input table 2 (output of asegstats2table or aparcstats2table)
        output: Output file for the table of differences
        percent_diff: Compute percent difference with respect to mean of tables
        percent_diff_t1: Compute percent difference with respect to table 1
        percent_diff_t2: Compute percent difference with respect to table 2
        multiply: Multiply by mulval
        divide: Divide by divval
        common: Compute difference on common segments (may reorder)
        remove_exvivo: Remove the string '_exvivo' from the column header
        diff_subjects: Compare subjects with different names
        noreplace53: Do not replace 5.3 structure names with later names

    Returns:
        Parameter dictionary.
    """
    params: Stattablediff = {
        "@type": "freesurfer/stattablediff",
        "t1": t1,
        "t2": t2,
        "output": output,
        "percent_diff": percent_diff,
        "percent_diff_t1": percent_diff_t1,
        "percent_diff_t2": percent_diff_t2,
        "common": common,
        "remove_exvivo": remove_exvivo,
        "diff_subjects": diff_subjects,
        "noreplace53": noreplace53,
    }
    if multiply is not None:
        params["multiply"] = multiply
    if divide is not None:
        params["divide"] = divide
    return params

def stattablediff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Stattablediff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("t1", None) is None:
        raise StyxValidationError("`t1` must not be None")
    if not isinstance(params["t1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`t1` has the wrong type: Received `{type(params["t1"])}` expected `InputPathType`')
    if params.get("t2", None) is None:
        raise StyxValidationError("`t2` must not be None")
    if not isinstance(params["t2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`t2` has the wrong type: Received `{type(params["t2"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("percent_diff", None) is not None:
        if not isinstance(params["percent_diff"], bool):
            raise StyxValidationError(f'`percent_diff` has the wrong type: Received `{type(params["percent_diff"])}` expected `bool`')
    if params.get("percent_diff_t1", None) is not None:
        if not isinstance(params["percent_diff_t1"], bool):
            raise StyxValidationError(f'`percent_diff_t1` has the wrong type: Received `{type(params["percent_diff_t1"])}` expected `bool`')
    if params.get("percent_diff_t2", None) is not None:
        if not isinstance(params["percent_diff_t2"], bool):
            raise StyxValidationError(f'`percent_diff_t2` has the wrong type: Received `{type(params["percent_diff_t2"])}` expected `bool`')
    if params.get("multiply", None) is not None:
        if not isinstance(params["multiply"], (float, int)):
            raise StyxValidationError(f'`multiply` has the wrong type: Received `{type(params["multiply"])}` expected `float`')
    if params.get("divide", None) is not None:
        if not isinstance(params["divide"], (float, int)):
            raise StyxValidationError(f'`divide` has the wrong type: Received `{type(params["divide"])}` expected `float`')
    if params.get("common", None) is not None:
        if not isinstance(params["common"], bool):
            raise StyxValidationError(f'`common` has the wrong type: Received `{type(params["common"])}` expected `bool`')
    if params.get("remove_exvivo", None) is not None:
        if not isinstance(params["remove_exvivo"], bool):
            raise StyxValidationError(f'`remove_exvivo` has the wrong type: Received `{type(params["remove_exvivo"])}` expected `bool`')
    if params.get("diff_subjects", None) is not None:
        if not isinstance(params["diff_subjects"], bool):
            raise StyxValidationError(f'`diff_subjects` has the wrong type: Received `{type(params["diff_subjects"])}` expected `bool`')
    if params.get("noreplace53", None) is not None:
        if not isinstance(params["noreplace53"], bool):
            raise StyxValidationError(f'`noreplace53` has the wrong type: Received `{type(params["noreplace53"])}` expected `bool`')

def stattablediff_cargs(params: Stattablediff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("stattablediff")
    cargs.append(execution.input_file(params["t1"]))
    cargs.append(execution.input_file(params["t2"]))
    cargs.append(params["output"])
    if params.get("percent_diff"):
        cargs.append("--percent")
    if params.get("percent_diff_t1"):
        cargs.append("--percent1")
    if params.get("percent_diff_t2"):
        cargs.append("--percent2")
    v_0 = params.get("multiply")
    if v_0 is not None:
        cargs.append("--mul")
        cargs.append(str(v_0))
    v_1 = params.get("divide")
    if v_1 is not None:
        cargs.append("--div")
        cargs.append(str(v_1))
    if params.get("common"):
        cargs.append("--common")
    if params.get("remove_exvivo"):
        cargs.append("--rm-exvivo")
    if params.get("diff_subjects"):
        cargs.append("--diff-subjs")
    if params.get("noreplace53"):
        cargs.append("--noreplace53")
    return cargs

def stattablediff_outputs(params: Stattablediff, execution: Execution) -> StattablediffOutputs:
    """Build the StattablediffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_diff_table_v: OutputPathType = execution.output_file(params["output"])
    return StattablediffOutputs(
        root=root_v,
        output_diff_table=output_diff_table_v,
    )

def stattablediff_execute(params: Stattablediff, runner: Runner | None = None) -> StattablediffOutputs:
    """
    stattablediff

    Creates a table of the differences between two stats tables.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    stattablediff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(STATTABLEDIFF_METADATA)
    execution.params(params)
    args = stattablediff_cargs(params, execution)
    out = stattablediff_outputs(params, execution)
    execution.run(args)
    return out

def stattablediff(
    t1: InputPathType,
    t2: InputPathType,
    output: str,
    percent_diff: bool = False,
    percent_diff_t1: bool = False,
    percent_diff_t2: bool = False,
    multiply: float | None = None,
    divide: float | None = None,
    common: bool = False,
    remove_exvivo: bool = False,
    diff_subjects: bool = False,
    noreplace53: bool = False,
    runner: Runner | None = None,
) -> StattablediffOutputs:
    """
    stattablediff

    Creates a table of the differences between two stats tables.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        t1: Input table 1 (output of asegstats2table or aparcstats2table)
        t2: Input table 2 (output of asegstats2table or aparcstats2table)
        output: Output file for the table of differences
        percent_diff: Compute percent difference with respect to mean of tables
        percent_diff_t1: Compute percent difference with respect to table 1
        percent_diff_t2: Compute percent difference with respect to table 2
        multiply: Multiply by mulval
        divide: Divide by divval
        common: Compute difference on common segments (may reorder)
        remove_exvivo: Remove the string '_exvivo' from the column header
        diff_subjects: Compare subjects with different names
        noreplace53: Do not replace 5.3 structure names with later names
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = stattablediff_params(
        t1=t1,
        t2=t2,
        output=output,
        percent_diff=percent_diff,
        percent_diff_t1=percent_diff_t1,
        percent_diff_t2=percent_diff_t2,
        multiply=multiply,
        divide=divide,
        common=common,
        remove_exvivo=remove_exvivo,
        diff_subjects=diff_subjects,
        noreplace53=noreplace53,
    )
    return stattablediff_execute(params, runner)

__all__ = [
    "Stattablediff",
    "StattablediffOutputs",
    "STATTABLEDIFF_METADATA",
    "stattablediff_cargs",
    "stattablediff_outputs",
    "stattablediff_params",
    "stattablediff_execute",
    "stattablediff_validate",
    "stattablediff",
]