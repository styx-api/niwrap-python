# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURF2VOL_METADATA = Metadata(
    id="surf2vol",
    name="surf2vol",
    package="freesurfer",
)

Surf2vol = typing.TypedDict(
    "Surf2vol",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/surf2vol"]],
        "fixed_surface": InputPathType,
        "moving_surface": InputPathType,
        "fixed_mri": InputPathType,
        "moving_mri": InputPathType,
        "output_file": typing.NotRequired[str],
        "output_field": typing.NotRequired[str],
        "output_affine": typing.NotRequired[str],
        "output_surf": typing.NotRequired[str],
        "output_surf_affine": typing.NotRequired[str],
        "output_mesh": typing.NotRequired[str],
        "spacing_x": typing.NotRequired[float],
        "spacing_y": typing.NotRequired[float],
        "spacing_z": typing.NotRequired[float],
        "poisson_ratio": typing.NotRequired[float],
        "dirty_factor": typing.NotRequired[float],
        "debug_output": typing.NotRequired[bool],
        "cache_transform": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Surf2volOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Default output of the deformation process."""
    output_field: OutputPathType | None
    """Field output of the deformation process."""

def surf2vol_params(
    fixed_surface: InputPathType,
    moving_surface: InputPathType,
    fixed_mri: InputPathType,
    moving_mri: InputPathType,
    output_file: str | None = None,
    output_field: str | None = None,
    output_affine: str | None = None,
    output_surf: str | None = None,
    output_surf_affine: str | None = None,
    output_mesh: str | None = None,
    spacing_x: float | None = None,
    spacing_y: float | None = None,
    spacing_z: float | None = None,
    poisson_ratio: float | None = None,
    dirty_factor: float | None = None,
    debug_output: bool = False,
    cache_transform: str | None = None,
) -> Surf2vol:
    """
    Build parameters.

    Args:
        fixed_surface: File path for the main fixed surface.
        moving_surface: File path for the main moving surface.
        fixed_mri: Fixed volume file.
        moving_mri: Moving volume file.
        output_file: Output file for the result, default is out.mgz.
        output_field: Output field file, default is out_field.mgz.
        output_affine: Path for the output affine file.
        output_surf: Root file name for output surfaces which will have indices\
            appended for each surface.
        output_surf_affine: Root file name for output surfaces with affine\
            transformations.
        output_mesh: File path for the output mesh.
        spacing_x: Specifies the x spacing for the deformation grid.
        spacing_y: Specifies the y spacing for the deformation grid.
        spacing_z: Specifies the z spacing for the deformation grid.
        poisson_ratio: Poisson ratio for material properties, default is 0.3.
        dirty_factor: Factor for dirty regions, between 0 and 1.
        debug_output: Enable debug output, writing a morph file at each iteration.
        cache_transform: Path to save transformation cache for reusing in\
            subsequent runs.

    Returns:
        Parameter dictionary.
    """
    params: Surf2vol = {
        "@type": "freesurfer/surf2vol",
        "fixed_surface": fixed_surface,
        "moving_surface": moving_surface,
        "fixed_mri": fixed_mri,
        "moving_mri": moving_mri,
        "debug_output": debug_output,
    }
    if output_file is not None:
        params["output_file"] = output_file
    if output_field is not None:
        params["output_field"] = output_field
    if output_affine is not None:
        params["output_affine"] = output_affine
    if output_surf is not None:
        params["output_surf"] = output_surf
    if output_surf_affine is not None:
        params["output_surf_affine"] = output_surf_affine
    if output_mesh is not None:
        params["output_mesh"] = output_mesh
    if spacing_x is not None:
        params["spacing_x"] = spacing_x
    if spacing_y is not None:
        params["spacing_y"] = spacing_y
    if spacing_z is not None:
        params["spacing_z"] = spacing_z
    if poisson_ratio is not None:
        params["poisson_ratio"] = poisson_ratio
    if dirty_factor is not None:
        params["dirty_factor"] = dirty_factor
    if cache_transform is not None:
        params["cache_transform"] = cache_transform
    return params

def surf2vol_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Surf2vol."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed_surface", None) is None:
        raise StyxValidationError("`fixed_surface` must not be None")
    if not isinstance(params["fixed_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_surface` has the wrong type: Received `{type(params["fixed_surface"])}` expected `InputPathType`')
    if params.get("moving_surface", None) is None:
        raise StyxValidationError("`moving_surface` must not be None")
    if not isinstance(params["moving_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_surface` has the wrong type: Received `{type(params["moving_surface"])}` expected `InputPathType`')
    if params.get("fixed_mri", None) is None:
        raise StyxValidationError("`fixed_mri` must not be None")
    if not isinstance(params["fixed_mri"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_mri` has the wrong type: Received `{type(params["fixed_mri"])}` expected `InputPathType`')
    if params.get("moving_mri", None) is None:
        raise StyxValidationError("`moving_mri` must not be None")
    if not isinstance(params["moving_mri"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_mri` has the wrong type: Received `{type(params["moving_mri"])}` expected `InputPathType`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("output_field", None) is not None:
        if not isinstance(params["output_field"], str):
            raise StyxValidationError(f'`output_field` has the wrong type: Received `{type(params["output_field"])}` expected `str`')
    if params.get("output_affine", None) is not None:
        if not isinstance(params["output_affine"], str):
            raise StyxValidationError(f'`output_affine` has the wrong type: Received `{type(params["output_affine"])}` expected `str`')
    if params.get("output_surf", None) is not None:
        if not isinstance(params["output_surf"], str):
            raise StyxValidationError(f'`output_surf` has the wrong type: Received `{type(params["output_surf"])}` expected `str`')
    if params.get("output_surf_affine", None) is not None:
        if not isinstance(params["output_surf_affine"], str):
            raise StyxValidationError(f'`output_surf_affine` has the wrong type: Received `{type(params["output_surf_affine"])}` expected `str`')
    if params.get("output_mesh", None) is not None:
        if not isinstance(params["output_mesh"], str):
            raise StyxValidationError(f'`output_mesh` has the wrong type: Received `{type(params["output_mesh"])}` expected `str`')
    if params.get("spacing_x", None) is not None:
        if not isinstance(params["spacing_x"], (float, int)):
            raise StyxValidationError(f'`spacing_x` has the wrong type: Received `{type(params["spacing_x"])}` expected `float`')
    if params.get("spacing_y", None) is not None:
        if not isinstance(params["spacing_y"], (float, int)):
            raise StyxValidationError(f'`spacing_y` has the wrong type: Received `{type(params["spacing_y"])}` expected `float`')
    if params.get("spacing_z", None) is not None:
        if not isinstance(params["spacing_z"], (float, int)):
            raise StyxValidationError(f'`spacing_z` has the wrong type: Received `{type(params["spacing_z"])}` expected `float`')
    if params.get("poisson_ratio", None) is not None:
        if not isinstance(params["poisson_ratio"], (float, int)):
            raise StyxValidationError(f'`poisson_ratio` has the wrong type: Received `{type(params["poisson_ratio"])}` expected `float`')
        if params["poisson_ratio"] > 0.5:
            raise StyxValidationError("Parameter `poisson_ratio` must be at most 0.5")
    if params.get("dirty_factor", None) is not None:
        if not isinstance(params["dirty_factor"], (float, int)):
            raise StyxValidationError(f'`dirty_factor` has the wrong type: Received `{type(params["dirty_factor"])}` expected `float`')
        if not (0 <= params["dirty_factor"] <= 1):
            raise StyxValidationError("Parameter `dirty_factor` must be between 0 and 1 (inclusive)")
    if params.get("debug_output", None) is not None:
        if not isinstance(params["debug_output"], bool):
            raise StyxValidationError(f'`debug_output` has the wrong type: Received `{type(params["debug_output"])}` expected `bool`')
    if params.get("cache_transform", None) is not None:
        if not isinstance(params["cache_transform"], str):
            raise StyxValidationError(f'`cache_transform` has the wrong type: Received `{type(params["cache_transform"])}` expected `str`')

def surf2vol_cargs(params: Surf2vol, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("surf2vol")
    cargs.append("-fixed_surf")
    cargs.append(execution.input_file(params["fixed_surface"]))
    cargs.append("-moving_surf")
    cargs.append(execution.input_file(params["moving_surface"]))
    cargs.append("-fixed_mri")
    cargs.append(execution.input_file(params["fixed_mri"]))
    cargs.append("-moving_mri")
    cargs.append(execution.input_file(params["moving_mri"]))
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append("-out")
        cargs.append(v_0)
    v_1 = params.get("output_field")
    if v_1 is not None:
        cargs.append("-out_field")
        cargs.append(v_1)
    v_2 = params.get("output_affine")
    if v_2 is not None:
        cargs.append("-out_affine")
        cargs.append(v_2)
    v_3 = params.get("output_surf")
    if v_3 is not None:
        cargs.append("-out_surf")
        cargs.append(v_3)
    v_4 = params.get("output_surf_affine")
    if v_4 is not None:
        cargs.append("-out_surf_affine")
        cargs.append(v_4)
    v_5 = params.get("output_mesh")
    if v_5 is not None:
        cargs.append("-out_mesh")
        cargs.append(v_5)
    v_6 = params.get("spacing_x")
    if v_6 is not None:
        cargs.append("-spacing_x")
        cargs.append(str(v_6))
    v_7 = params.get("spacing_y")
    if v_7 is not None:
        cargs.append("-spacing_y")
        cargs.append(str(v_7))
    v_8 = params.get("spacing_z")
    if v_8 is not None:
        cargs.append("-spacing_z")
        cargs.append(str(v_8))
    v_9 = params.get("poisson_ratio")
    if v_9 is not None:
        cargs.append("-poisson")
        cargs.append(str(v_9))
    v_10 = params.get("dirty_factor")
    if v_10 is not None:
        cargs.append("-dirty")
        cargs.append(str(v_10))
    if params.get("debug_output"):
        cargs.append("-dbg_output")
    v_11 = params.get("cache_transform")
    if v_11 is not None:
        cargs.append("-cache_transform")
        cargs.append(v_11)
    return cargs

def surf2vol_outputs(params: Surf2vol, execution: Execution) -> Surf2volOutputs:
    """Build the Surf2volOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    output_field_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_field")
    if __v1 is not None:
        output_field_v = execution.output_file((__v1 if __v1 is not None else ""))
    return Surf2volOutputs(
        root=root_v,
        output_file=output_file_v,
        output_field=output_field_v,
    )

def surf2vol_execute(params: Surf2vol, runner: Runner | None = None) -> Surf2volOutputs:
    """
    surf2vol

    Diffuse surface deformation to volumes using surface and MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surf2vol_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURF2VOL_METADATA)
    execution.params(params)
    args = surf2vol_cargs(params, execution)
    out = surf2vol_outputs(params, execution)
    execution.run(args)
    return out

def surf2vol(
    fixed_surface: InputPathType,
    moving_surface: InputPathType,
    fixed_mri: InputPathType,
    moving_mri: InputPathType,
    output_file: str | None = None,
    output_field: str | None = None,
    output_affine: str | None = None,
    output_surf: str | None = None,
    output_surf_affine: str | None = None,
    output_mesh: str | None = None,
    spacing_x: float | None = None,
    spacing_y: float | None = None,
    spacing_z: float | None = None,
    poisson_ratio: float | None = None,
    dirty_factor: float | None = None,
    debug_output: bool = False,
    cache_transform: str | None = None,
    runner: Runner | None = None,
) -> Surf2volOutputs:
    """
    surf2vol

    Diffuse surface deformation to volumes using surface and MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        fixed_surface: File path for the main fixed surface.
        moving_surface: File path for the main moving surface.
        fixed_mri: Fixed volume file.
        moving_mri: Moving volume file.
        output_file: Output file for the result, default is out.mgz.
        output_field: Output field file, default is out_field.mgz.
        output_affine: Path for the output affine file.
        output_surf: Root file name for output surfaces which will have indices\
            appended for each surface.
        output_surf_affine: Root file name for output surfaces with affine\
            transformations.
        output_mesh: File path for the output mesh.
        spacing_x: Specifies the x spacing for the deformation grid.
        spacing_y: Specifies the y spacing for the deformation grid.
        spacing_z: Specifies the z spacing for the deformation grid.
        poisson_ratio: Poisson ratio for material properties, default is 0.3.
        dirty_factor: Factor for dirty regions, between 0 and 1.
        debug_output: Enable debug output, writing a morph file at each iteration.
        cache_transform: Path to save transformation cache for reusing in\
            subsequent runs.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surf2vol_params(
        fixed_surface=fixed_surface,
        moving_surface=moving_surface,
        fixed_mri=fixed_mri,
        moving_mri=moving_mri,
        output_file=output_file,
        output_field=output_field,
        output_affine=output_affine,
        output_surf=output_surf,
        output_surf_affine=output_surf_affine,
        output_mesh=output_mesh,
        spacing_x=spacing_x,
        spacing_y=spacing_y,
        spacing_z=spacing_z,
        poisson_ratio=poisson_ratio,
        dirty_factor=dirty_factor,
        debug_output=debug_output,
        cache_transform=cache_transform,
    )
    return surf2vol_execute(params, runner)

__all__ = [
    "Surf2vol",
    "Surf2volOutputs",
    "SURF2VOL_METADATA",
    "surf2vol_cargs",
    "surf2vol_outputs",
    "surf2vol_params",
    "surf2vol_execute",
    "surf2vol_validate",
    "surf2vol",
]