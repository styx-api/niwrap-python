# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOL2SUBFIELD_METADATA = Metadata(
    id="vol2subfield",
    name="vol2subfield",
    package="freesurfer",
)

Vol2subfield = typing.TypedDict(
    "Vol2subfield",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/vol2subfield"]],
        "input_volume": InputPathType,
        "subfield_volume": InputPathType,
        "registration_file": InputPathType,
        "output_volume": typing.NotRequired[str],
        "output_registration": typing.NotRequired[str],
        "stats_output": typing.NotRequired[str],
        "avgwf_output": typing.NotRequired[str],
        "avgwfvol_output": typing.NotRequired[str],
        "color_table": typing.NotRequired[InputPathType],
        "interpolation_nearest": typing.NotRequired[bool],
        "interpolation_trilin": typing.NotRequired[bool],
        "interpolation_cubic": typing.NotRequired[bool],
        "tmp_directory": typing.NotRequired[str],
        "preset_subfield_lh_hippoamyg": typing.NotRequired[bool],
        "preset_subfield_rh_hippoamyg": typing.NotRequired[bool],
        "preset_subfield_lh_hbt": typing.NotRequired[bool],
        "preset_subfield_rh_hbt": typing.NotRequired[bool],
        "preset_subfield_thalamus": typing.NotRequired[bool],
        "preset_subfield_brainstem": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Vol2subfieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mapped_output_volume: OutputPathType | None
    """Output volume mapped into subfield volume space"""
    output_registration_file: OutputPathType | None
    """Output registration file"""
    segmentation_stats_file: OutputPathType | None
    """Statistics output file generated by mri_segstats"""
    average_waveform_file: OutputPathType | None
    """Average waveform output file"""
    average_waveform_volume_file: OutputPathType | None
    """Average waveform volume output file"""

def vol2subfield_params(
    input_volume: InputPathType,
    subfield_volume: InputPathType,
    registration_file: InputPathType,
    output_volume: str | None = None,
    output_registration: str | None = None,
    stats_output: str | None = None,
    avgwf_output: str | None = None,
    avgwfvol_output: str | None = None,
    color_table: InputPathType | None = None,
    interpolation_nearest: bool = False,
    interpolation_trilin: bool = False,
    interpolation_cubic: bool = False,
    tmp_directory: str | None = None,
    preset_subfield_lh_hippoamyg: bool = False,
    preset_subfield_rh_hippoamyg: bool = False,
    preset_subfield_lh_hbt: bool = False,
    preset_subfield_rh_hbt: bool = False,
    preset_subfield_thalamus: bool = False,
    preset_subfield_brainstem: bool = False,
) -> Vol2subfield:
    """
    Build parameters.

    Args:
        input_volume: Input volume
        subfield_volume: Subfield volume (full path or relative to subject/mri)
        registration_file: Registration that maps input volume to conformed space
        output_volume: Output volume
        output_registration: Registration between input volume and subfield
        stats_output: Run mri_segstats with --sum output to this file
        avgwf_output: Run mri_segstats with --avgwf output to this file
        avgwfvol_output: Run mri_segstats with --avgwfvol output to this file
        color_table: Color table to use with mri_segstats
        interpolation_nearest: Use nearest neighbor interpolation
        interpolation_trilin: Use triliear interpolation
        interpolation_cubic: Use cubic interpolation
        tmp_directory: Temporary directory for debugging
        preset_subfield_lh_hippoamyg: Set subfield to lh.hippoAmygLabels-T1.v21.mgz
        preset_subfield_rh_hippoamyg: Set subfield to rh.hippoAmygLabels-T1.v21.mgz
        preset_subfield_lh_hbt: Set subfield to lh.hippoAmygLabels-T1.v21.HBT.mgz
        preset_subfield_rh_hbt: Set subfield to rh.hippoAmygLabels-T1.v21.HBT.mgz
        preset_subfield_thalamus: Set subfield to ThalamicNuclei.v10.T1.mgz
        preset_subfield_brainstem: Set subfield to brainstemSsLabels.v12.mgz

    Returns:
        Parameter dictionary.
    """
    params: Vol2subfield = {
        "@type": "freesurfer/vol2subfield",
        "input_volume": input_volume,
        "subfield_volume": subfield_volume,
        "registration_file": registration_file,
        "interpolation_nearest": interpolation_nearest,
        "interpolation_trilin": interpolation_trilin,
        "interpolation_cubic": interpolation_cubic,
        "preset_subfield_lh_hippoamyg": preset_subfield_lh_hippoamyg,
        "preset_subfield_rh_hippoamyg": preset_subfield_rh_hippoamyg,
        "preset_subfield_lh_hbt": preset_subfield_lh_hbt,
        "preset_subfield_rh_hbt": preset_subfield_rh_hbt,
        "preset_subfield_thalamus": preset_subfield_thalamus,
        "preset_subfield_brainstem": preset_subfield_brainstem,
    }
    if output_volume is not None:
        params["output_volume"] = output_volume
    if output_registration is not None:
        params["output_registration"] = output_registration
    if stats_output is not None:
        params["stats_output"] = stats_output
    if avgwf_output is not None:
        params["avgwf_output"] = avgwf_output
    if avgwfvol_output is not None:
        params["avgwfvol_output"] = avgwfvol_output
    if color_table is not None:
        params["color_table"] = color_table
    if tmp_directory is not None:
        params["tmp_directory"] = tmp_directory
    return params

def vol2subfield_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Vol2subfield."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("subfield_volume", None) is None:
        raise StyxValidationError("`subfield_volume` must not be None")
    if not isinstance(params["subfield_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subfield_volume` has the wrong type: Received `{type(params["subfield_volume"])}` expected `InputPathType`')
    if params.get("registration_file", None) is None:
        raise StyxValidationError("`registration_file` must not be None")
    if not isinstance(params["registration_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params["registration_file"])}` expected `InputPathType`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("output_registration", None) is not None:
        if not isinstance(params["output_registration"], str):
            raise StyxValidationError(f'`output_registration` has the wrong type: Received `{type(params["output_registration"])}` expected `str`')
    if params.get("stats_output", None) is not None:
        if not isinstance(params["stats_output"], str):
            raise StyxValidationError(f'`stats_output` has the wrong type: Received `{type(params["stats_output"])}` expected `str`')
    if params.get("avgwf_output", None) is not None:
        if not isinstance(params["avgwf_output"], str):
            raise StyxValidationError(f'`avgwf_output` has the wrong type: Received `{type(params["avgwf_output"])}` expected `str`')
    if params.get("avgwfvol_output", None) is not None:
        if not isinstance(params["avgwfvol_output"], str):
            raise StyxValidationError(f'`avgwfvol_output` has the wrong type: Received `{type(params["avgwfvol_output"])}` expected `str`')
    if params.get("color_table", None) is not None:
        if not isinstance(params["color_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`color_table` has the wrong type: Received `{type(params["color_table"])}` expected `InputPathType`')
    if params.get("interpolation_nearest", None) is not None:
        if not isinstance(params["interpolation_nearest"], bool):
            raise StyxValidationError(f'`interpolation_nearest` has the wrong type: Received `{type(params["interpolation_nearest"])}` expected `bool`')
    if params.get("interpolation_trilin", None) is not None:
        if not isinstance(params["interpolation_trilin"], bool):
            raise StyxValidationError(f'`interpolation_trilin` has the wrong type: Received `{type(params["interpolation_trilin"])}` expected `bool`')
    if params.get("interpolation_cubic", None) is not None:
        if not isinstance(params["interpolation_cubic"], bool):
            raise StyxValidationError(f'`interpolation_cubic` has the wrong type: Received `{type(params["interpolation_cubic"])}` expected `bool`')
    if params.get("tmp_directory", None) is not None:
        if not isinstance(params["tmp_directory"], str):
            raise StyxValidationError(f'`tmp_directory` has the wrong type: Received `{type(params["tmp_directory"])}` expected `str`')
    if params.get("preset_subfield_lh_hippoamyg", None) is not None:
        if not isinstance(params["preset_subfield_lh_hippoamyg"], bool):
            raise StyxValidationError(f'`preset_subfield_lh_hippoamyg` has the wrong type: Received `{type(params["preset_subfield_lh_hippoamyg"])}` expected `bool`')
    if params.get("preset_subfield_rh_hippoamyg", None) is not None:
        if not isinstance(params["preset_subfield_rh_hippoamyg"], bool):
            raise StyxValidationError(f'`preset_subfield_rh_hippoamyg` has the wrong type: Received `{type(params["preset_subfield_rh_hippoamyg"])}` expected `bool`')
    if params.get("preset_subfield_lh_hbt", None) is not None:
        if not isinstance(params["preset_subfield_lh_hbt"], bool):
            raise StyxValidationError(f'`preset_subfield_lh_hbt` has the wrong type: Received `{type(params["preset_subfield_lh_hbt"])}` expected `bool`')
    if params.get("preset_subfield_rh_hbt", None) is not None:
        if not isinstance(params["preset_subfield_rh_hbt"], bool):
            raise StyxValidationError(f'`preset_subfield_rh_hbt` has the wrong type: Received `{type(params["preset_subfield_rh_hbt"])}` expected `bool`')
    if params.get("preset_subfield_thalamus", None) is not None:
        if not isinstance(params["preset_subfield_thalamus"], bool):
            raise StyxValidationError(f'`preset_subfield_thalamus` has the wrong type: Received `{type(params["preset_subfield_thalamus"])}` expected `bool`')
    if params.get("preset_subfield_brainstem", None) is not None:
        if not isinstance(params["preset_subfield_brainstem"], bool):
            raise StyxValidationError(f'`preset_subfield_brainstem` has the wrong type: Received `{type(params["preset_subfield_brainstem"])}` expected `bool`')

def vol2subfield_cargs(params: Vol2subfield, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("vol2subfield")
    cargs.append("--i")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append("--sf")
    cargs.append(execution.input_file(params["subfield_volume"]))
    cargs.append("--reg")
    cargs.append(execution.input_file(params["registration_file"]))
    v_0 = params.get("output_volume")
    if v_0 is not None:
        cargs.append("--o")
        cargs.append(v_0)
    v_1 = params.get("output_registration")
    if v_1 is not None:
        cargs.append("--outreg")
        cargs.append(v_1)
    v_2 = params.get("stats_output")
    if v_2 is not None:
        cargs.append("--stats")
        cargs.append(v_2)
    v_3 = params.get("avgwf_output")
    if v_3 is not None:
        cargs.append("--avgwf")
        cargs.append(v_3)
    v_4 = params.get("avgwfvol_output")
    if v_4 is not None:
        cargs.append("--avgwfvol")
        cargs.append(v_4)
    v_5 = params.get("color_table")
    if v_5 is not None:
        cargs.append("--ctab")
        cargs.append(execution.input_file(v_5))
    if params.get("interpolation_nearest"):
        cargs.append("--nearest")
    if params.get("interpolation_trilin"):
        cargs.append("--trilin")
    if params.get("interpolation_cubic"):
        cargs.append("--cubic")
    v_6 = params.get("tmp_directory")
    if v_6 is not None:
        cargs.append("--tmp")
        cargs.append(v_6)
    if params.get("preset_subfield_lh_hippoamyg"):
        cargs.append("--lh.hippoamyg")
    if params.get("preset_subfield_rh_hippoamyg"):
        cargs.append("--rh.hippoamyg")
    if params.get("preset_subfield_lh_hbt"):
        cargs.append("--lh.hbt")
    if params.get("preset_subfield_rh_hbt"):
        cargs.append("--rh.hbt")
    if params.get("preset_subfield_thalamus"):
        cargs.append("--thalamus")
    if params.get("preset_subfield_brainstem"):
        cargs.append("--brainstem")
    return cargs

def vol2subfield_outputs(params: Vol2subfield, execution: Execution) -> Vol2subfieldOutputs:
    """Build the Vol2subfieldOutputs object for this tool."""
    mapped_output_volume_v: OutputPathType | None = None
    output_registration_file_v: OutputPathType | None = None
    segmentation_stats_file_v: OutputPathType | None = None
    average_waveform_file_v: OutputPathType | None = None
    average_waveform_volume_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_volume")
    if __v0 is not None:
        mapped_output_volume_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_registration")
    if __v1 is not None:
        output_registration_file_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("stats_output")
    if __v2 is not None:
        segmentation_stats_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("avgwf_output")
    if __v3 is not None:
        average_waveform_file_v = execution.output_file((__v3 if __v3 is not None else ""))
    __v4 = params.get("avgwfvol_output")
    if __v4 is not None:
        average_waveform_volume_file_v = execution.output_file((__v4 if __v4 is not None else ""))
    return Vol2subfieldOutputs(
        root=root_v,
        mapped_output_volume=mapped_output_volume_v,
        output_registration_file=output_registration_file_v,
        segmentation_stats_file=segmentation_stats_file_v,
        average_waveform_file=average_waveform_file_v,
        average_waveform_volume_file=average_waveform_volume_file_v,
    )

def vol2subfield_execute(params: Vol2subfield, runner: Runner | None = None) -> Vol2subfieldOutputs:
    """
    vol2subfield

    A tool for integrating arbitrary volumes with volumes that share a RAS space with the orig volume in the FreeSurfer mri folder.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vol2subfield_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOL2SUBFIELD_METADATA)
    execution.params(params)
    args = vol2subfield_cargs(params, execution)
    out = vol2subfield_outputs(params, execution)
    execution.run(args)
    return out

def vol2subfield(
    input_volume: InputPathType,
    subfield_volume: InputPathType,
    registration_file: InputPathType,
    output_volume: str | None = None,
    output_registration: str | None = None,
    stats_output: str | None = None,
    avgwf_output: str | None = None,
    avgwfvol_output: str | None = None,
    color_table: InputPathType | None = None,
    interpolation_nearest: bool = False,
    interpolation_trilin: bool = False,
    interpolation_cubic: bool = False,
    tmp_directory: str | None = None,
    preset_subfield_lh_hippoamyg: bool = False,
    preset_subfield_rh_hippoamyg: bool = False,
    preset_subfield_lh_hbt: bool = False,
    preset_subfield_rh_hbt: bool = False,
    preset_subfield_thalamus: bool = False,
    preset_subfield_brainstem: bool = False,
    runner: Runner | None = None,
) -> Vol2subfieldOutputs:
    """
    vol2subfield

    A tool for integrating arbitrary volumes with volumes that share a RAS space with the orig volume in the FreeSurfer mri folder.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume
        subfield_volume: Subfield volume (full path or relative to subject/mri)
        registration_file: Registration that maps input volume to conformed space
        output_volume: Output volume
        output_registration: Registration between input volume and subfield
        stats_output: Run mri_segstats with --sum output to this file
        avgwf_output: Run mri_segstats with --avgwf output to this file
        avgwfvol_output: Run mri_segstats with --avgwfvol output to this file
        color_table: Color table to use with mri_segstats
        interpolation_nearest: Use nearest neighbor interpolation
        interpolation_trilin: Use triliear interpolation
        interpolation_cubic: Use cubic interpolation
        tmp_directory: Temporary directory for debugging
        preset_subfield_lh_hippoamyg: Set subfield to lh.hippoAmygLabels-T1.v21.mgz
        preset_subfield_rh_hippoamyg: Set subfield to rh.hippoAmygLabels-T1.v21.mgz
        preset_subfield_lh_hbt: Set subfield to lh.hippoAmygLabels-T1.v21.HBT.mgz
        preset_subfield_rh_hbt: Set subfield to rh.hippoAmygLabels-T1.v21.HBT.mgz
        preset_subfield_thalamus: Set subfield to ThalamicNuclei.v10.T1.mgz
        preset_subfield_brainstem: Set subfield to brainstemSsLabels.v12.mgz
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vol2subfield_params(
        input_volume=input_volume,
        subfield_volume=subfield_volume,
        registration_file=registration_file,
        output_volume=output_volume,
        output_registration=output_registration,
        stats_output=stats_output,
        avgwf_output=avgwf_output,
        avgwfvol_output=avgwfvol_output,
        color_table=color_table,
        interpolation_nearest=interpolation_nearest,
        interpolation_trilin=interpolation_trilin,
        interpolation_cubic=interpolation_cubic,
        tmp_directory=tmp_directory,
        preset_subfield_lh_hippoamyg=preset_subfield_lh_hippoamyg,
        preset_subfield_rh_hippoamyg=preset_subfield_rh_hippoamyg,
        preset_subfield_lh_hbt=preset_subfield_lh_hbt,
        preset_subfield_rh_hbt=preset_subfield_rh_hbt,
        preset_subfield_thalamus=preset_subfield_thalamus,
        preset_subfield_brainstem=preset_subfield_brainstem,
    )
    return vol2subfield_execute(params, runner)

__all__ = [
    "Vol2subfield",
    "Vol2subfieldOutputs",
    "VOL2SUBFIELD_METADATA",
    "vol2subfield_cargs",
    "vol2subfield_outputs",
    "vol2subfield_params",
    "vol2subfield_execute",
    "vol2subfield_validate",
    "vol2subfield",
]