# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AVI2TALXFM_METADATA = Metadata(
    id="avi2talxfm",
    name="avi2talxfm",
    package="freesurfer",
)

Avi2talxfm = typing.TypedDict(
    "Avi2talxfm",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/avi2talxfm"]],
        "input_volume": InputPathType,
        "target_volume": InputPathType,
        "vox2vox_transform": InputPathType,
        "output_xfm": str,
    },
)

@dataclasses.dataclass
class Avi2talxfmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_xfm_file: OutputPathType
    """Resulting MNI transform file"""

def avi2talxfm_params(
    input_volume: InputPathType,
    target_volume: InputPathType,
    vox2vox_transform: InputPathType,
    output_xfm: str,
) -> Avi2talxfm:
    """
    Build parameters.

    Args:
        input_volume: Input volume file
        target_volume: Target volume file
        vox2vox_transform: Voxel-to-voxel transform file
        output_xfm: Output MNI transform file

    Returns:
        Parameter dictionary.
    """
    params: Avi2talxfm = {
        "@type": "freesurfer/avi2talxfm",
        "input_volume": input_volume,
        "target_volume": target_volume,
        "vox2vox_transform": vox2vox_transform,
        "output_xfm": output_xfm,
    }
    return params

def avi2talxfm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Avi2talxfm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("target_volume", None) is None:
        raise StyxValidationError("`target_volume` must not be None")
    if not isinstance(params["target_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["target_volume"])}` expected `InputPathType`')
    if params.get("vox2vox_transform", None) is None:
        raise StyxValidationError("`vox2vox_transform` must not be None")
    if not isinstance(params["vox2vox_transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vox2vox_transform` has the wrong type: Received `{type(params["vox2vox_transform"])}` expected `InputPathType`')
    if params.get("output_xfm", None) is None:
        raise StyxValidationError("`output_xfm` must not be None")
    if not isinstance(params["output_xfm"], str):
        raise StyxValidationError(f'`output_xfm` has the wrong type: Received `{type(params["output_xfm"])}` expected `str`')

def avi2talxfm_cargs(params: Avi2talxfm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("avi2talxfm")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(execution.input_file(params["target_volume"]))
    cargs.append(execution.input_file(params["vox2vox_transform"]))
    cargs.append(params["output_xfm"])
    return cargs

def avi2talxfm_outputs(params: Avi2talxfm, execution: Execution) -> Avi2talxfmOutputs:
    """Build the Avi2talxfmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_xfm_file_v: OutputPathType = execution.output_file(params["output_xfm"])
    return Avi2talxfmOutputs(
        root=root_v,
        output_xfm_file=output_xfm_file_v,
    )

def avi2talxfm_execute(params: Avi2talxfm, runner: Runner | None = None) -> Avi2talxfmOutputs:
    """
    avi2talxfm

    Convert voxel-to-voxel transform to MNI transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    avi2talxfm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AVI2TALXFM_METADATA)
    execution.params(params)
    args = avi2talxfm_cargs(params, execution)
    out = avi2talxfm_outputs(params, execution)
    execution.run(args)
    return out

def avi2talxfm(
    input_volume: InputPathType,
    target_volume: InputPathType,
    vox2vox_transform: InputPathType,
    output_xfm: str,
    runner: Runner | None = None,
) -> Avi2talxfmOutputs:
    """
    avi2talxfm

    Convert voxel-to-voxel transform to MNI transform.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume file
        target_volume: Target volume file
        vox2vox_transform: Voxel-to-voxel transform file
        output_xfm: Output MNI transform file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = avi2talxfm_params(
        input_volume=input_volume,
        target_volume=target_volume,
        vox2vox_transform=vox2vox_transform,
        output_xfm=output_xfm,
    )
    return avi2talxfm_execute(params, runner)

__all__ = [
    "Avi2talxfm",
    "Avi2talxfmOutputs",
    "AVI2TALXFM_METADATA",
    "avi2talxfm_cargs",
    "avi2talxfm_outputs",
    "avi2talxfm_params",
    "avi2talxfm_execute",
    "avi2talxfm_validate",
    "avi2talxfm",
]