# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_CVS_REGISTER_METADATA = Metadata(
    id="mri_cvs_register",
    name="mri_cvs_register",
    package="freesurfer",
)

MriCvsRegister = typing.TypedDict(
    "MriCvsRegister",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_cvs_register"]],
        "mov_subjid": str,
        "template_subjid": typing.NotRequired[str],
        "templatedir": typing.NotRequired[str],
        "mni_flag": typing.NotRequired[bool],
        "outdir": typing.NotRequired[str],
        "asegfname": typing.NotRequired[str],
        "voltype": typing.NotRequired[str],
        "step1_flag": typing.NotRequired[bool],
        "step2_flag": typing.NotRequired[bool],
        "step3_flag": typing.NotRequired[bool],
        "noaseg_flag": typing.NotRequired[bool],
        "nointensity_flag": typing.NotRequired[bool],
        "hemi_flag": typing.NotRequired[bool],
        "masktargethemi_flag": typing.NotRequired[bool],
        "maskmovinghemi_flag": typing.NotRequired[bool],
        "nocleanup_flag": typing.NotRequired[bool],
        "keepelreg_flag": typing.NotRequired[bool],
        "keepallm3z_flag": typing.NotRequired[bool],
        "cleanall_flag": typing.NotRequired[bool],
        "cleansurfreg_flag": typing.NotRequired[bool],
        "cleanelreg_flag": typing.NotRequired[bool],
        "cleanvolreg_flag": typing.NotRequired[bool],
        "m3d_flag": typing.NotRequired[bool],
        "openmp": typing.NotRequired[int],
        "nolog_flag": typing.NotRequired[bool],
        "version_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriCvsRegisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    final_cvs_morph: OutputPathType | None
    """The final morph that combines correspondences recovered in all registration steps."""
    final_cvs_morphed_norm: OutputPathType | None
    """The CVS morphed norm.mgz file; final result of CVS with all registration steps contributions."""
    final_cvs_morphed_aseg: OutputPathType | None
    """The CVS morphed aseg.mgz file with all registration steps contributions."""
    step1_morphed_aseg: OutputPathType | None
    """Morphed aseg.mgz file result of CVS up to step (3)(i) and contains elatic morph and aseg-based nonlinear registration."""
    step1_morphed_norm: OutputPathType | None
    """Morphed norm.mgz file result of CVS up to step (3)(i) and contains elatic morph and aseg-based nonlinear registration."""

def mri_cvs_register_params(
    mov_subjid: str,
    template_subjid: str | None = None,
    templatedir: str | None = None,
    mni_flag: bool = False,
    outdir: str | None = None,
    asegfname: str | None = None,
    voltype: str | None = None,
    step1_flag: bool = False,
    step2_flag: bool = False,
    step3_flag: bool = False,
    noaseg_flag: bool = False,
    nointensity_flag: bool = False,
    hemi_flag: bool = False,
    masktargethemi_flag: bool = False,
    maskmovinghemi_flag: bool = False,
    nocleanup_flag: bool = False,
    keepelreg_flag: bool = False,
    keepallm3z_flag: bool = False,
    cleanall_flag: bool = False,
    cleansurfreg_flag: bool = False,
    cleanelreg_flag: bool = False,
    cleanvolreg_flag: bool = False,
    m3d_flag: bool = False,
    openmp: int | None = None,
    nolog_flag: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
) -> MriCvsRegister:
    """
    Build parameters.

    Args:
        mov_subjid: FreeSurfer subject name as found in $SUBJECTS_DIR. This\
            identifies the subject that is to be moved / registered to the target.
        template_subjid: FreeSurfer subject name as found in $SUBJECTS_DIR (or\
            --templatedir). This identifies the subject that is to be kept fixed\
            (template).
        templatedir: Directory of the template subject's SUBJECTS_DIR if different\
            from that of the moving subject.
        mni_flag: Use the CVS atlas in MNI152 space as a target for registration.
        outdir: Name of the output directory where all the registration results are\
            written.
        asegfname: Name of the segmentation volume used in volumetric registration\
            step. Do not use the file extension.
        voltype: Changes the input from norm.mgz to voltype.mgz.
        step1_flag: Only do step 1 (spherical registration).
        step2_flag: Only do step 2 (elastic registration).
        step3_flag: Only do step 3 (volumetric registration).
        noaseg_flag: Do not use aseg volumes in the volumetric registration\
            pipeline.
        nointensity_flag: Do not use intensity volumes in the volumetric\
            registration pipeline.
        hemi_flag: Run CVS registration only on one hemisphere.
        masktargethemi_flag: Use with --hemi when hemi is registered to full brain\
            target.
        maskmovinghemi_flag: Use with --hemi when full brain is registered to\
            single hemi target.
        nocleanup_flag: Do not delete temporary files.
        keepelreg_flag: Do not delete elastic registration outcomes.
        keepallm3z_flag: Do not delete m3z morph files computed during CVS.
        cleanall_flag: Overwrite / recompute all CVS-related morphs.
        cleansurfreg_flag: Overwrite/recompute CVS-related surface registration\
            morphs.
        cleanelreg_flag: Overwrite / recompute CVS-related elastic registration\
            morph.
        cleanvolreg_flag: Overwrite / recompute CVS-related volumetric morphs.
        m3d_flag: Use m3d instead of m3z for registration morphs.
        openmp: Assign the number of nodes for openmp runs.
        nolog_flag: Do not produce a log file.
        version_flag: Print version and exit.
        help_flag: Print help and exit.

    Returns:
        Parameter dictionary.
    """
    params: MriCvsRegister = {
        "@type": "freesurfer/mri_cvs_register",
        "mov_subjid": mov_subjid,
        "mni_flag": mni_flag,
        "step1_flag": step1_flag,
        "step2_flag": step2_flag,
        "step3_flag": step3_flag,
        "noaseg_flag": noaseg_flag,
        "nointensity_flag": nointensity_flag,
        "hemi_flag": hemi_flag,
        "masktargethemi_flag": masktargethemi_flag,
        "maskmovinghemi_flag": maskmovinghemi_flag,
        "nocleanup_flag": nocleanup_flag,
        "keepelreg_flag": keepelreg_flag,
        "keepallm3z_flag": keepallm3z_flag,
        "cleanall_flag": cleanall_flag,
        "cleansurfreg_flag": cleansurfreg_flag,
        "cleanelreg_flag": cleanelreg_flag,
        "cleanvolreg_flag": cleanvolreg_flag,
        "m3d_flag": m3d_flag,
        "nolog_flag": nolog_flag,
        "version_flag": version_flag,
        "help_flag": help_flag,
    }
    if template_subjid is not None:
        params["template_subjid"] = template_subjid
    if templatedir is not None:
        params["templatedir"] = templatedir
    if outdir is not None:
        params["outdir"] = outdir
    if asegfname is not None:
        params["asegfname"] = asegfname
    if voltype is not None:
        params["voltype"] = voltype
    if openmp is not None:
        params["openmp"] = openmp
    return params

def mri_cvs_register_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriCvsRegister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mov_subjid", None) is None:
        raise StyxValidationError("`mov_subjid` must not be None")
    if not isinstance(params["mov_subjid"], str):
        raise StyxValidationError(f'`mov_subjid` has the wrong type: Received `{type(params["mov_subjid"])}` expected `str`')
    if params.get("template_subjid", None) is not None:
        if not isinstance(params["template_subjid"], str):
            raise StyxValidationError(f'`template_subjid` has the wrong type: Received `{type(params["template_subjid"])}` expected `str`')
    if params.get("templatedir", None) is not None:
        if not isinstance(params["templatedir"], str):
            raise StyxValidationError(f'`templatedir` has the wrong type: Received `{type(params["templatedir"])}` expected `str`')
    if params.get("mni_flag", None) is not None:
        if not isinstance(params["mni_flag"], bool):
            raise StyxValidationError(f'`mni_flag` has the wrong type: Received `{type(params["mni_flag"])}` expected `bool`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("asegfname", None) is not None:
        if not isinstance(params["asegfname"], str):
            raise StyxValidationError(f'`asegfname` has the wrong type: Received `{type(params["asegfname"])}` expected `str`')
    if params.get("voltype", None) is not None:
        if not isinstance(params["voltype"], str):
            raise StyxValidationError(f'`voltype` has the wrong type: Received `{type(params["voltype"])}` expected `str`')
    if params.get("step1_flag", None) is not None:
        if not isinstance(params["step1_flag"], bool):
            raise StyxValidationError(f'`step1_flag` has the wrong type: Received `{type(params["step1_flag"])}` expected `bool`')
    if params.get("step2_flag", None) is not None:
        if not isinstance(params["step2_flag"], bool):
            raise StyxValidationError(f'`step2_flag` has the wrong type: Received `{type(params["step2_flag"])}` expected `bool`')
    if params.get("step3_flag", None) is not None:
        if not isinstance(params["step3_flag"], bool):
            raise StyxValidationError(f'`step3_flag` has the wrong type: Received `{type(params["step3_flag"])}` expected `bool`')
    if params.get("noaseg_flag", None) is not None:
        if not isinstance(params["noaseg_flag"], bool):
            raise StyxValidationError(f'`noaseg_flag` has the wrong type: Received `{type(params["noaseg_flag"])}` expected `bool`')
    if params.get("nointensity_flag", None) is not None:
        if not isinstance(params["nointensity_flag"], bool):
            raise StyxValidationError(f'`nointensity_flag` has the wrong type: Received `{type(params["nointensity_flag"])}` expected `bool`')
    if params.get("hemi_flag", None) is not None:
        if not isinstance(params["hemi_flag"], bool):
            raise StyxValidationError(f'`hemi_flag` has the wrong type: Received `{type(params["hemi_flag"])}` expected `bool`')
    if params.get("masktargethemi_flag", None) is not None:
        if not isinstance(params["masktargethemi_flag"], bool):
            raise StyxValidationError(f'`masktargethemi_flag` has the wrong type: Received `{type(params["masktargethemi_flag"])}` expected `bool`')
    if params.get("maskmovinghemi_flag", None) is not None:
        if not isinstance(params["maskmovinghemi_flag"], bool):
            raise StyxValidationError(f'`maskmovinghemi_flag` has the wrong type: Received `{type(params["maskmovinghemi_flag"])}` expected `bool`')
    if params.get("nocleanup_flag", None) is not None:
        if not isinstance(params["nocleanup_flag"], bool):
            raise StyxValidationError(f'`nocleanup_flag` has the wrong type: Received `{type(params["nocleanup_flag"])}` expected `bool`')
    if params.get("keepelreg_flag", None) is not None:
        if not isinstance(params["keepelreg_flag"], bool):
            raise StyxValidationError(f'`keepelreg_flag` has the wrong type: Received `{type(params["keepelreg_flag"])}` expected `bool`')
    if params.get("keepallm3z_flag", None) is not None:
        if not isinstance(params["keepallm3z_flag"], bool):
            raise StyxValidationError(f'`keepallm3z_flag` has the wrong type: Received `{type(params["keepallm3z_flag"])}` expected `bool`')
    if params.get("cleanall_flag", None) is not None:
        if not isinstance(params["cleanall_flag"], bool):
            raise StyxValidationError(f'`cleanall_flag` has the wrong type: Received `{type(params["cleanall_flag"])}` expected `bool`')
    if params.get("cleansurfreg_flag", None) is not None:
        if not isinstance(params["cleansurfreg_flag"], bool):
            raise StyxValidationError(f'`cleansurfreg_flag` has the wrong type: Received `{type(params["cleansurfreg_flag"])}` expected `bool`')
    if params.get("cleanelreg_flag", None) is not None:
        if not isinstance(params["cleanelreg_flag"], bool):
            raise StyxValidationError(f'`cleanelreg_flag` has the wrong type: Received `{type(params["cleanelreg_flag"])}` expected `bool`')
    if params.get("cleanvolreg_flag", None) is not None:
        if not isinstance(params["cleanvolreg_flag"], bool):
            raise StyxValidationError(f'`cleanvolreg_flag` has the wrong type: Received `{type(params["cleanvolreg_flag"])}` expected `bool`')
    if params.get("m3d_flag", None) is not None:
        if not isinstance(params["m3d_flag"], bool):
            raise StyxValidationError(f'`m3d_flag` has the wrong type: Received `{type(params["m3d_flag"])}` expected `bool`')
    if params.get("openmp", None) is not None:
        if not isinstance(params["openmp"], int):
            raise StyxValidationError(f'`openmp` has the wrong type: Received `{type(params["openmp"])}` expected `int`')
    if params.get("nolog_flag", None) is not None:
        if not isinstance(params["nolog_flag"], bool):
            raise StyxValidationError(f'`nolog_flag` has the wrong type: Received `{type(params["nolog_flag"])}` expected `bool`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def mri_cvs_register_cargs(params: MriCvsRegister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_cvs_register")
    cargs.append("--mov")
    cargs.append(params["mov_subjid"])
    v_0 = params.get("template_subjid")
    if v_0 is not None:
        cargs.append("--template")
        cargs.append(v_0)
    v_1 = params.get("templatedir")
    if v_1 is not None:
        cargs.append("--templatedir")
        cargs.append(v_1)
    if params.get("mni_flag"):
        cargs.append("--mni")
    v_2 = params.get("outdir")
    if v_2 is not None:
        cargs.append("--outdir")
        cargs.append(v_2)
    v_3 = params.get("asegfname")
    if v_3 is not None:
        cargs.append("--asegfname")
        cargs.append(v_3)
    v_4 = params.get("voltype")
    if v_4 is not None:
        cargs.append("--voltype")
        cargs.append(v_4)
    if params.get("step1_flag"):
        cargs.append("--step1")
    if params.get("step2_flag"):
        cargs.append("--step2")
    if params.get("step3_flag"):
        cargs.append("--step3")
    if params.get("noaseg_flag"):
        cargs.append("--noaseg")
    if params.get("nointensity_flag"):
        cargs.append("--nointensity")
    if params.get("hemi_flag"):
        cargs.append("--hemi")
    if params.get("masktargethemi_flag"):
        cargs.append("--masktargethemi")
    if params.get("maskmovinghemi_flag"):
        cargs.append("--maskmovinghemi")
    if params.get("nocleanup_flag"):
        cargs.append("--nocleanup")
    if params.get("keepelreg_flag"):
        cargs.append("--keepelreg")
    if params.get("keepallm3z_flag"):
        cargs.append("--keepallm3z")
    if params.get("cleanall_flag"):
        cargs.append("--cleanall")
    if params.get("cleansurfreg_flag"):
        cargs.append("--cleansurfreg")
    if params.get("cleanelreg_flag"):
        cargs.append("--cleanelreg")
    if params.get("cleanvolreg_flag"):
        cargs.append("--cleanvolreg")
    if params.get("m3d_flag"):
        cargs.append("--m3d")
    v_5 = params.get("openmp")
    if v_5 is not None:
        cargs.append("--openmp")
        cargs.append(str(v_5))
    if params.get("nolog_flag"):
        cargs.append("--nolog")
    if params.get("version_flag"):
        cargs.append("--version")
    if params.get("help_flag"):
        cargs.append("--help")
    return cargs

def mri_cvs_register_outputs(params: MriCvsRegister, execution: Execution) -> MriCvsRegisterOutputs:
    """Build the MriCvsRegisterOutputs object for this tool."""
    final_cvs_morph_v: OutputPathType | None = None
    final_cvs_morphed_norm_v: OutputPathType | None = None
    final_cvs_morphed_aseg_v: OutputPathType | None = None
    step1_morphed_aseg_v: OutputPathType | None = None
    step1_morphed_norm_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outdir")
    if __v0 is not None:
        final_cvs_morph_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/final_CVSmorph_toTEMPLATE.m3z')
    __v1 = params.get("outdir")
    if __v1 is not None:
        final_cvs_morphed_norm_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/final_CVSmorphed_toTEMPLATE_norm.mgz')
    __v2 = params.get("outdir")
    if __v2 is not None:
        final_cvs_morphed_aseg_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}/final_CVSmorphed_toTEMPLATE_aseg.mgz')
    __v3 = params.get("outdir")
    if __v3 is not None:
        step1_morphed_aseg_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}/step1_CVSmorphed_toTEMPLATE_aseg.mgz')
    __v4 = params.get("outdir")
    if __v4 is not None:
        step1_morphed_norm_v = execution.output_file(f'{(__v4 if __v4 is not None else "")}/step1_CVSmorphed_toTEMPLATE_norm.mgz')
    return MriCvsRegisterOutputs(
        root=root_v,
        final_cvs_morph=final_cvs_morph_v,
        final_cvs_morphed_norm=final_cvs_morphed_norm_v,
        final_cvs_morphed_aseg=final_cvs_morphed_aseg_v,
        step1_morphed_aseg=step1_morphed_aseg_v,
        step1_morphed_norm=step1_morphed_norm_v,
    )

def mri_cvs_register_execute(params: MriCvsRegister, runner: Runner | None = None) -> MriCvsRegisterOutputs:
    """
    mri_cvs_register

    Combined Volume and Surface Registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_cvs_register_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_CVS_REGISTER_METADATA)
    execution.params(params)
    args = mri_cvs_register_cargs(params, execution)
    out = mri_cvs_register_outputs(params, execution)
    execution.run(args)
    return out

def mri_cvs_register(
    mov_subjid: str,
    template_subjid: str | None = None,
    templatedir: str | None = None,
    mni_flag: bool = False,
    outdir: str | None = None,
    asegfname: str | None = None,
    voltype: str | None = None,
    step1_flag: bool = False,
    step2_flag: bool = False,
    step3_flag: bool = False,
    noaseg_flag: bool = False,
    nointensity_flag: bool = False,
    hemi_flag: bool = False,
    masktargethemi_flag: bool = False,
    maskmovinghemi_flag: bool = False,
    nocleanup_flag: bool = False,
    keepelreg_flag: bool = False,
    keepallm3z_flag: bool = False,
    cleanall_flag: bool = False,
    cleansurfreg_flag: bool = False,
    cleanelreg_flag: bool = False,
    cleanvolreg_flag: bool = False,
    m3d_flag: bool = False,
    openmp: int | None = None,
    nolog_flag: bool = False,
    version_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> MriCvsRegisterOutputs:
    """
    mri_cvs_register

    Combined Volume and Surface Registration

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        mov_subjid: FreeSurfer subject name as found in $SUBJECTS_DIR. This\
            identifies the subject that is to be moved / registered to the target.
        template_subjid: FreeSurfer subject name as found in $SUBJECTS_DIR (or\
            --templatedir). This identifies the subject that is to be kept fixed\
            (template).
        templatedir: Directory of the template subject's SUBJECTS_DIR if different\
            from that of the moving subject.
        mni_flag: Use the CVS atlas in MNI152 space as a target for registration.
        outdir: Name of the output directory where all the registration results are\
            written.
        asegfname: Name of the segmentation volume used in volumetric registration\
            step. Do not use the file extension.
        voltype: Changes the input from norm.mgz to voltype.mgz.
        step1_flag: Only do step 1 (spherical registration).
        step2_flag: Only do step 2 (elastic registration).
        step3_flag: Only do step 3 (volumetric registration).
        noaseg_flag: Do not use aseg volumes in the volumetric registration\
            pipeline.
        nointensity_flag: Do not use intensity volumes in the volumetric\
            registration pipeline.
        hemi_flag: Run CVS registration only on one hemisphere.
        masktargethemi_flag: Use with --hemi when hemi is registered to full brain\
            target.
        maskmovinghemi_flag: Use with --hemi when full brain is registered to\
            single hemi target.
        nocleanup_flag: Do not delete temporary files.
        keepelreg_flag: Do not delete elastic registration outcomes.
        keepallm3z_flag: Do not delete m3z morph files computed during CVS.
        cleanall_flag: Overwrite / recompute all CVS-related morphs.
        cleansurfreg_flag: Overwrite/recompute CVS-related surface registration\
            morphs.
        cleanelreg_flag: Overwrite / recompute CVS-related elastic registration\
            morph.
        cleanvolreg_flag: Overwrite / recompute CVS-related volumetric morphs.
        m3d_flag: Use m3d instead of m3z for registration morphs.
        openmp: Assign the number of nodes for openmp runs.
        nolog_flag: Do not produce a log file.
        version_flag: Print version and exit.
        help_flag: Print help and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_cvs_register_params(
        mov_subjid=mov_subjid,
        template_subjid=template_subjid,
        templatedir=templatedir,
        mni_flag=mni_flag,
        outdir=outdir,
        asegfname=asegfname,
        voltype=voltype,
        step1_flag=step1_flag,
        step2_flag=step2_flag,
        step3_flag=step3_flag,
        noaseg_flag=noaseg_flag,
        nointensity_flag=nointensity_flag,
        hemi_flag=hemi_flag,
        masktargethemi_flag=masktargethemi_flag,
        maskmovinghemi_flag=maskmovinghemi_flag,
        nocleanup_flag=nocleanup_flag,
        keepelreg_flag=keepelreg_flag,
        keepallm3z_flag=keepallm3z_flag,
        cleanall_flag=cleanall_flag,
        cleansurfreg_flag=cleansurfreg_flag,
        cleanelreg_flag=cleanelreg_flag,
        cleanvolreg_flag=cleanvolreg_flag,
        m3d_flag=m3d_flag,
        openmp=openmp,
        nolog_flag=nolog_flag,
        version_flag=version_flag,
        help_flag=help_flag,
    )
    return mri_cvs_register_execute(params, runner)

__all__ = [
    "MriCvsRegister",
    "MriCvsRegisterOutputs",
    "MRI_CVS_REGISTER_METADATA",
    "mri_cvs_register_cargs",
    "mri_cvs_register_outputs",
    "mri_cvs_register_params",
    "mri_cvs_register_execute",
    "mri_cvs_register_validate",
    "mri_cvs_register",
]