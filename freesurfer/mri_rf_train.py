# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RF_TRAIN_METADATA = Metadata(
    id="mri_rf_train",
    name="mri_rf_train",
    package="freesurfer",
)

MriRfTrain = typing.TypedDict(
    "MriRfTrain",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_rf_train"]],
        "seg_volume": str,
        "atlas_transform": str,
        "mask_volume": typing.NotRequired[str],
        "node_spacing": typing.NotRequired[float],
        "prior_spacing": typing.NotRequired[float],
        "input_training_data": typing.NotRequired[list[str]],
        "sanity_check": typing.NotRequired[bool],
        "subjects": list[str],
        "output_rfa": str,
    },
)

@dataclasses.dataclass
class MriRfTrainOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_rf_train_params(
    seg_volume: str,
    atlas_transform: str,
    subjects: list[str],
    output_rfa: str,
    mask_volume: str | None = None,
    node_spacing: float | None = None,
    prior_spacing: float | None = None,
    input_training_data: list[str] | None = None,
    sanity_check: bool = False,
) -> MriRfTrain:
    """
    Build parameters.

    Args:
        seg_volume: Segmentation volume (path relative to $subject/mri).
        atlas_transform: Atlas transform (path relative to\
            $subject/mri/transforms).
        subjects: Input subjects.
        output_rfa: Output RFA filename.
        mask_volume: Use volname as a mask (path relative to $subject/mri).
        node_spacing: Spacing of classifiers in canonical space.
        prior_spacing: Spacing of class priors in canonical space.
        input_training_data: Specifying training data (path relative to\
            $subject/mri). Can specify multiple inputs. If not specified, 'orig' is\
            used.
        sanity_check: Conduct sanity-check of labels for obvious edit errors.

    Returns:
        Parameter dictionary.
    """
    params: MriRfTrain = {
        "@type": "freesurfer/mri_rf_train",
        "seg_volume": seg_volume,
        "atlas_transform": atlas_transform,
        "subjects": subjects,
        "output_rfa": output_rfa,
        "sanity_check": sanity_check,
    }
    if mask_volume is not None:
        params["mask_volume"] = mask_volume
    if node_spacing is not None:
        params["node_spacing"] = node_spacing
    if prior_spacing is not None:
        params["prior_spacing"] = prior_spacing
    if input_training_data is not None:
        params["input_training_data"] = input_training_data
    return params

def mri_rf_train_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRfTrain."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("seg_volume", None) is None:
        raise StyxValidationError("`seg_volume` must not be None")
    if not isinstance(params["seg_volume"], str):
        raise StyxValidationError(f'`seg_volume` has the wrong type: Received `{type(params["seg_volume"])}` expected `str`')
    if params.get("atlas_transform", None) is None:
        raise StyxValidationError("`atlas_transform` must not be None")
    if not isinstance(params["atlas_transform"], str):
        raise StyxValidationError(f'`atlas_transform` has the wrong type: Received `{type(params["atlas_transform"])}` expected `str`')
    if params.get("mask_volume", None) is not None:
        if not isinstance(params["mask_volume"], str):
            raise StyxValidationError(f'`mask_volume` has the wrong type: Received `{type(params["mask_volume"])}` expected `str`')
    if params.get("node_spacing", None) is not None:
        if not isinstance(params["node_spacing"], (float, int)):
            raise StyxValidationError(f'`node_spacing` has the wrong type: Received `{type(params["node_spacing"])}` expected `float`')
    if params.get("prior_spacing", None) is not None:
        if not isinstance(params["prior_spacing"], (float, int)):
            raise StyxValidationError(f'`prior_spacing` has the wrong type: Received `{type(params["prior_spacing"])}` expected `float`')
    if params.get("input_training_data", None) is not None:
        if not isinstance(params["input_training_data"], list):
            raise StyxValidationError(f'`input_training_data` has the wrong type: Received `{type(params["input_training_data"])}` expected `list[str]`')
        for e in params["input_training_data"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`input_training_data` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("sanity_check", None) is not None:
        if not isinstance(params["sanity_check"], bool):
            raise StyxValidationError(f'`sanity_check` has the wrong type: Received `{type(params["sanity_check"])}` expected `bool`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e_2 in params["subjects"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("output_rfa", None) is None:
        raise StyxValidationError("`output_rfa` must not be None")
    if not isinstance(params["output_rfa"], str):
        raise StyxValidationError(f'`output_rfa` has the wrong type: Received `{type(params["output_rfa"])}` expected `str`')

def mri_rf_train_cargs(params: MriRfTrain, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_rf_train")
    cargs.append("-seg")
    cargs.append(params["seg_volume"])
    cargs.append("-xform")
    cargs.append(params["atlas_transform"])
    v_0 = params.get("mask_volume")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(v_0)
    v_1 = params.get("node_spacing")
    if v_1 is not None:
        cargs.append("-node_spacing")
        cargs.append(str(v_1))
    v_2 = params.get("prior_spacing")
    if v_2 is not None:
        cargs.append("-prior_spacing")
        cargs.append(str(v_2))
    v_3 = params.get("input_training_data")
    if v_3 is not None:
        cargs.append("-input")
        for item0 in v_3:
            cargs.append(item0)
    if params.get("sanity_check"):
        cargs.append("-check")
    for item1 in params["subjects"]:
        cargs.append(item1)
    cargs.append(params["output_rfa"])
    return cargs

def mri_rf_train_outputs(params: MriRfTrain, execution: Execution) -> MriRfTrainOutputs:
    """Build the MriRfTrainOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriRfTrainOutputs(
        root=root_v,
    )

def mri_rf_train_execute(params: MriRfTrain, runner: Runner | None = None) -> MriRfTrainOutputs:
    """
    mri_rf_train

    Trains GCA data with multiple subjects using MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_rf_train_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RF_TRAIN_METADATA)
    execution.params(params)
    args = mri_rf_train_cargs(params, execution)
    out = mri_rf_train_outputs(params, execution)
    execution.run(args)
    return out

def mri_rf_train(
    seg_volume: str,
    atlas_transform: str,
    subjects: list[str],
    output_rfa: str,
    mask_volume: str | None = None,
    node_spacing: float | None = None,
    prior_spacing: float | None = None,
    input_training_data: list[str] | None = None,
    sanity_check: bool = False,
    runner: Runner | None = None,
) -> MriRfTrainOutputs:
    """
    mri_rf_train

    Trains GCA data with multiple subjects using MRI data.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        seg_volume: Segmentation volume (path relative to $subject/mri).
        atlas_transform: Atlas transform (path relative to\
            $subject/mri/transforms).
        subjects: Input subjects.
        output_rfa: Output RFA filename.
        mask_volume: Use volname as a mask (path relative to $subject/mri).
        node_spacing: Spacing of classifiers in canonical space.
        prior_spacing: Spacing of class priors in canonical space.
        input_training_data: Specifying training data (path relative to\
            $subject/mri). Can specify multiple inputs. If not specified, 'orig' is\
            used.
        sanity_check: Conduct sanity-check of labels for obvious edit errors.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_rf_train_params(
        seg_volume=seg_volume,
        atlas_transform=atlas_transform,
        subjects=subjects,
        output_rfa=output_rfa,
        mask_volume=mask_volume,
        node_spacing=node_spacing,
        prior_spacing=prior_spacing,
        input_training_data=input_training_data,
        sanity_check=sanity_check,
    )
    return mri_rf_train_execute(params, runner)

__all__ = [
    "MriRfTrain",
    "MriRfTrainOutputs",
    "MRI_RF_TRAIN_METADATA",
    "mri_rf_train_cargs",
    "mri_rf_train_outputs",
    "mri_rf_train_params",
    "mri_rf_train_execute",
    "mri_rf_train_validate",
    "mri_rf_train",
]