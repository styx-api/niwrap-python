# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CBLUMWMGYRI_METADATA = Metadata(
    id="cblumwmgyri",
    name="cblumwmgyri",
    package="freesurfer",
)

Cblumwmgyri = typing.TypedDict(
    "Cblumwmgyri",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/cblumwmgyri"]],
        "subject": str,
        "source_seg": typing.NotRequired[InputPathType],
        "n_erodes_dilates": typing.NotRequired[float],
        "out_seg": typing.NotRequired[str],
        "no_segstats": typing.NotRequired[bool],
        "subjects_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CblumwmgyriOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_seg_file: OutputPathType | None
    """Output segmentation file after processing"""

def cblumwmgyri_params(
    subject: str,
    source_seg: InputPathType | None = None,
    n_erodes_dilates: float | None = None,
    out_seg: str | None = None,
    no_segstats: bool = False,
    subjects_dir: str | None = None,
) -> Cblumwmgyri:
    """
    Build parameters.

    Args:
        subject: Subject identifier
        source_seg: Source segmentation file (default: aparc+aseg.mgz)
        n_erodes_dilates: Number of erosions/dilations (default: 2)
        out_seg: Output segmentation file (default: sourceseg+cblumwmgyri.mgz)
        no_segstats: Do not compute segmentation statistics
        subjects_dir: Subjects directory

    Returns:
        Parameter dictionary.
    """
    params: Cblumwmgyri = {
        "@type": "freesurfer/cblumwmgyri",
        "subject": subject,
        "no_segstats": no_segstats,
    }
    if source_seg is not None:
        params["source_seg"] = source_seg
    if n_erodes_dilates is not None:
        params["n_erodes_dilates"] = n_erodes_dilates
    if out_seg is not None:
        params["out_seg"] = out_seg
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    return params

def cblumwmgyri_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Cblumwmgyri."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params["subject"])}` expected `str`')
    if params.get("source_seg", None) is not None:
        if not isinstance(params["source_seg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`source_seg` has the wrong type: Received `{type(params["source_seg"])}` expected `InputPathType`')
    if params.get("n_erodes_dilates", None) is not None:
        if not isinstance(params["n_erodes_dilates"], (float, int)):
            raise StyxValidationError(f'`n_erodes_dilates` has the wrong type: Received `{type(params["n_erodes_dilates"])}` expected `float`')
    if params.get("out_seg", None) is not None:
        if not isinstance(params["out_seg"], str):
            raise StyxValidationError(f'`out_seg` has the wrong type: Received `{type(params["out_seg"])}` expected `str`')
    if params.get("no_segstats", None) is not None:
        if not isinstance(params["no_segstats"], bool):
            raise StyxValidationError(f'`no_segstats` has the wrong type: Received `{type(params["no_segstats"])}` expected `bool`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')

def cblumwmgyri_cargs(params: Cblumwmgyri, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cblumwmgyri")
    cargs.append("--s")
    cargs.append(params["subject"])
    v_0 = params.get("source_seg")
    if v_0 is not None:
        cargs.append("--seg")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("n_erodes_dilates")
    if v_1 is not None:
        cargs.append("--n")
        cargs.append(str(v_1))
    v_2 = params.get("out_seg")
    if v_2 is not None:
        cargs.append("--o")
        cargs.append(v_2)
    if params.get("no_segstats"):
        cargs.append("--no-segstats")
    v_3 = params.get("subjects_dir")
    if v_3 is not None:
        cargs.append("--sd")
        cargs.append(v_3)
    return cargs

def cblumwmgyri_outputs(params: Cblumwmgyri, execution: Execution) -> CblumwmgyriOutputs:
    """Build the CblumwmgyriOutputs object for this tool."""
    output_seg_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_seg")
    if __v0 is not None:
        output_seg_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return CblumwmgyriOutputs(
        root=root_v,
        output_seg_file=output_seg_file_v,
    )

def cblumwmgyri_execute(params: Cblumwmgyri, runner: Runner | None = None) -> CblumwmgyriOutputs:
    """
    cblumwmgyri

    Segments cerebellar white matter into gyral and core components using geometrical constraints.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cblumwmgyri_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CBLUMWMGYRI_METADATA)
    execution.params(params)
    args = cblumwmgyri_cargs(params, execution)
    out = cblumwmgyri_outputs(params, execution)
    execution.run(args)
    return out

def cblumwmgyri(
    subject: str,
    source_seg: InputPathType | None = None,
    n_erodes_dilates: float | None = None,
    out_seg: str | None = None,
    no_segstats: bool = False,
    subjects_dir: str | None = None,
    runner: Runner | None = None,
) -> CblumwmgyriOutputs:
    """
    cblumwmgyri

    Segments cerebellar white matter into gyral and core components using geometrical constraints.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject: Subject identifier
        source_seg: Source segmentation file (default: aparc+aseg.mgz)
        n_erodes_dilates: Number of erosions/dilations (default: 2)
        out_seg: Output segmentation file (default: sourceseg+cblumwmgyri.mgz)
        no_segstats: Do not compute segmentation statistics
        subjects_dir: Subjects directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cblumwmgyri_params(
        subject=subject,
        source_seg=source_seg,
        n_erodes_dilates=n_erodes_dilates,
        out_seg=out_seg,
        no_segstats=no_segstats,
        subjects_dir=subjects_dir,
    )
    return cblumwmgyri_execute(params, runner)

__all__ = [
    "Cblumwmgyri",
    "CblumwmgyriOutputs",
    "CBLUMWMGYRI_METADATA",
    "cblumwmgyri_cargs",
    "cblumwmgyri_outputs",
    "cblumwmgyri_params",
    "cblumwmgyri_execute",
    "cblumwmgyri_validate",
    "cblumwmgyri",
]