# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_AVERAGE_VOLUME_METADATA = Metadata(
    id="make_average_volume",
    name="make_average_volume",
    package="freesurfer",
)

MakeAverageVolume = typing.TypedDict(
    "MakeAverageVolume",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/make_average_volume"]],
        "subjects": list[str],
        "fsgd": typing.NotRequired[InputPathType],
        "out": typing.NotRequired[str],
        "topdir": typing.NotRequired[str],
        "xform": typing.NotRequired[str],
        "sdir": typing.NotRequired[str],
        "sd_flag": typing.NotRequired[bool],
        "force_flag": typing.NotRequired[bool],
        "keep_all_orig_flag": typing.NotRequired[bool],
        "no_aseg_flag": typing.NotRequired[bool],
        "ctab": typing.NotRequired[str],
        "ctab_default_flag": typing.NotRequired[bool],
        "echo_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "nocleanup_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MakeAverageVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def make_average_volume_params(
    subjects: list[str],
    fsgd: InputPathType | None = None,
    out: str | None = None,
    topdir: str | None = None,
    xform: str | None = None,
    sdir: str | None = None,
    sd_flag: bool = False,
    force_flag: bool = False,
    keep_all_orig_flag: bool = False,
    no_aseg_flag: bool = False,
    ctab: str | None = None,
    ctab_default_flag: bool = False,
    echo_flag: bool = False,
    debug_flag: bool = False,
    nocleanup_flag: bool = False,
) -> MakeAverageVolume:
    """
    Build parameters.

    Args:
        subjects: List of subjects to include in the average.
        fsgd: File containing subject list for averaging.
        out: Output average subject name. Default is 'average'.
        topdir: Directory to put data and link to SUBJECTS_DIR.
        xform: Transformation name to use, default is talairach.xfm.
        sdir: Use specified SUBJECTS_DIR instead of the environment's one.
        sd_flag: Same as --sdir.
        force_flag: Overwrite existing average subject data.
        keep_all_orig_flag: Concatenate all original volumes into mri/orig.all.mgz.
        no_aseg_flag: Do not create 'average' aseg.
        ctab: Embed colortable into segmentations.
        ctab_default_flag: Embed FreeSurferColorLUT.txt into segmentations.
        echo_flag: Enable command echo for debugging.
        debug_flag: Same as --echo for debugging.
        nocleanup_flag: Do not delete temporary files.

    Returns:
        Parameter dictionary.
    """
    params: MakeAverageVolume = {
        "@type": "freesurfer/make_average_volume",
        "subjects": subjects,
        "sd_flag": sd_flag,
        "force_flag": force_flag,
        "keep_all_orig_flag": keep_all_orig_flag,
        "no_aseg_flag": no_aseg_flag,
        "ctab_default_flag": ctab_default_flag,
        "echo_flag": echo_flag,
        "debug_flag": debug_flag,
        "nocleanup_flag": nocleanup_flag,
    }
    if fsgd is not None:
        params["fsgd"] = fsgd
    if out is not None:
        params["out"] = out
    if topdir is not None:
        params["topdir"] = topdir
    if xform is not None:
        params["xform"] = xform
    if sdir is not None:
        params["sdir"] = sdir
    if ctab is not None:
        params["ctab"] = ctab
    return params

def make_average_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakeAverageVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], list):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `list[str]`')
    for e in params["subjects"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("fsgd", None) is not None:
        if not isinstance(params["fsgd"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fsgd` has the wrong type: Received `{type(params["fsgd"])}` expected `InputPathType`')
    if params.get("out", None) is not None:
        if not isinstance(params["out"], str):
            raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')
    if params.get("topdir", None) is not None:
        if not isinstance(params["topdir"], str):
            raise StyxValidationError(f'`topdir` has the wrong type: Received `{type(params["topdir"])}` expected `str`')
    if params.get("xform", None) is not None:
        if not isinstance(params["xform"], str):
            raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `str`')
    if params.get("sdir", None) is not None:
        if not isinstance(params["sdir"], str):
            raise StyxValidationError(f'`sdir` has the wrong type: Received `{type(params["sdir"])}` expected `str`')
    if params.get("sd_flag", None) is not None:
        if not isinstance(params["sd_flag"], bool):
            raise StyxValidationError(f'`sd_flag` has the wrong type: Received `{type(params["sd_flag"])}` expected `bool`')
    if params.get("force_flag", None) is not None:
        if not isinstance(params["force_flag"], bool):
            raise StyxValidationError(f'`force_flag` has the wrong type: Received `{type(params["force_flag"])}` expected `bool`')
    if params.get("keep_all_orig_flag", None) is not None:
        if not isinstance(params["keep_all_orig_flag"], bool):
            raise StyxValidationError(f'`keep_all_orig_flag` has the wrong type: Received `{type(params["keep_all_orig_flag"])}` expected `bool`')
    if params.get("no_aseg_flag", None) is not None:
        if not isinstance(params["no_aseg_flag"], bool):
            raise StyxValidationError(f'`no_aseg_flag` has the wrong type: Received `{type(params["no_aseg_flag"])}` expected `bool`')
    if params.get("ctab", None) is not None:
        if not isinstance(params["ctab"], str):
            raise StyxValidationError(f'`ctab` has the wrong type: Received `{type(params["ctab"])}` expected `str`')
    if params.get("ctab_default_flag", None) is not None:
        if not isinstance(params["ctab_default_flag"], bool):
            raise StyxValidationError(f'`ctab_default_flag` has the wrong type: Received `{type(params["ctab_default_flag"])}` expected `bool`')
    if params.get("echo_flag", None) is not None:
        if not isinstance(params["echo_flag"], bool):
            raise StyxValidationError(f'`echo_flag` has the wrong type: Received `{type(params["echo_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("nocleanup_flag", None) is not None:
        if not isinstance(params["nocleanup_flag"], bool):
            raise StyxValidationError(f'`nocleanup_flag` has the wrong type: Received `{type(params["nocleanup_flag"])}` expected `bool`')

def make_average_volume_cargs(params: MakeAverageVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_average_volume")
    cargs.append("--subjects")
    for item0 in params["subjects"]:
        cargs.append(item0)
    v_0 = params.get("fsgd")
    if v_0 is not None:
        cargs.append("--fsgd")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("out")
    if v_1 is not None:
        cargs.append("--out")
        cargs.append(v_1)
    v_2 = params.get("topdir")
    if v_2 is not None:
        cargs.append("--topdir")
        cargs.append(v_2)
    v_3 = params.get("xform")
    if v_3 is not None:
        cargs.append("--xform")
        cargs.append(v_3)
    v_4 = params.get("sdir")
    if v_4 is not None:
        cargs.append("--sdir")
        cargs.append(v_4)
    if params.get("sd_flag"):
        cargs.append("--sd")
    if params.get("force_flag"):
        cargs.append("--force")
    if params.get("keep_all_orig_flag"):
        cargs.append("--keep-all-orig")
    if params.get("no_aseg_flag"):
        cargs.append("--no-aseg")
    v_5 = params.get("ctab")
    if v_5 is not None:
        cargs.append("--ctab")
        cargs.append(v_5)
    if params.get("ctab_default_flag"):
        cargs.append("--ctab-default")
    if params.get("echo_flag"):
        cargs.append("--echo")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("nocleanup_flag"):
        cargs.append("--nocleanup")
    return cargs

def make_average_volume_outputs(params: MakeAverageVolume, execution: Execution) -> MakeAverageVolumeOutputs:
    """Build the MakeAverageVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MakeAverageVolumeOutputs(
        root=root_v,
    )

def make_average_volume_execute(params: MakeAverageVolume, runner: Runner | None = None) -> MakeAverageVolumeOutputs:
    """
    make_average_volume

    Creates average volumes from a set of subjects.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_average_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_AVERAGE_VOLUME_METADATA)
    execution.params(params)
    args = make_average_volume_cargs(params, execution)
    out = make_average_volume_outputs(params, execution)
    execution.run(args)
    return out

def make_average_volume(
    subjects: list[str],
    fsgd: InputPathType | None = None,
    out: str | None = None,
    topdir: str | None = None,
    xform: str | None = None,
    sdir: str | None = None,
    sd_flag: bool = False,
    force_flag: bool = False,
    keep_all_orig_flag: bool = False,
    no_aseg_flag: bool = False,
    ctab: str | None = None,
    ctab_default_flag: bool = False,
    echo_flag: bool = False,
    debug_flag: bool = False,
    nocleanup_flag: bool = False,
    runner: Runner | None = None,
) -> MakeAverageVolumeOutputs:
    """
    make_average_volume

    Creates average volumes from a set of subjects.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subjects: List of subjects to include in the average.
        fsgd: File containing subject list for averaging.
        out: Output average subject name. Default is 'average'.
        topdir: Directory to put data and link to SUBJECTS_DIR.
        xform: Transformation name to use, default is talairach.xfm.
        sdir: Use specified SUBJECTS_DIR instead of the environment's one.
        sd_flag: Same as --sdir.
        force_flag: Overwrite existing average subject data.
        keep_all_orig_flag: Concatenate all original volumes into mri/orig.all.mgz.
        no_aseg_flag: Do not create 'average' aseg.
        ctab: Embed colortable into segmentations.
        ctab_default_flag: Embed FreeSurferColorLUT.txt into segmentations.
        echo_flag: Enable command echo for debugging.
        debug_flag: Same as --echo for debugging.
        nocleanup_flag: Do not delete temporary files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_average_volume_params(
        subjects=subjects,
        fsgd=fsgd,
        out=out,
        topdir=topdir,
        xform=xform,
        sdir=sdir,
        sd_flag=sd_flag,
        force_flag=force_flag,
        keep_all_orig_flag=keep_all_orig_flag,
        no_aseg_flag=no_aseg_flag,
        ctab=ctab,
        ctab_default_flag=ctab_default_flag,
        echo_flag=echo_flag,
        debug_flag=debug_flag,
        nocleanup_flag=nocleanup_flag,
    )
    return make_average_volume_execute(params, runner)

__all__ = [
    "MakeAverageVolume",
    "MakeAverageVolumeOutputs",
    "MAKE_AVERAGE_VOLUME_METADATA",
    "make_average_volume_cargs",
    "make_average_volume_outputs",
    "make_average_volume_params",
    "make_average_volume_execute",
    "make_average_volume_validate",
    "make_average_volume",
]