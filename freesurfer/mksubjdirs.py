# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKSUBJDIRS_METADATA = Metadata(
    id="mksubjdirs",
    name="mksubjdirs",
    package="freesurfer",
)

Mksubjdirs = typing.TypedDict(
    "Mksubjdirs",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mksubjdirs"]],
        "subj_name": str,
        "mode": typing.NotRequired[str],
        "parents": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "selinux_context": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MksubjdirsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mksubjdirs_params(
    subj_name: str,
    mode: str | None = None,
    parents: bool = False,
    verbose: bool = False,
    selinux_context: bool = False,
    help: bool = False,
    version: bool = False,
) -> Mksubjdirs:
    """
    Build parameters.

    Args:
        subj_name: Name of the subject directory to create.
        mode: Set file mode (as in chmod), not a=rwx - umask
        parents: No error if existing, make parent directories as needed
        verbose: Print a message for each created directory
        selinux_context: Set SELinux security context of each created directory to\
            the default type
        help: Display help and exit
        version: Output version information and exit

    Returns:
        Parameter dictionary.
    """
    params: Mksubjdirs = {
        "@type": "freesurfer/mksubjdirs",
        "subj_name": subj_name,
        "parents": parents,
        "verbose": verbose,
        "selinux_context": selinux_context,
        "help": help,
        "version": version,
    }
    if mode is not None:
        params["mode"] = mode
    return params

def mksubjdirs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mksubjdirs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subj_name", None) is None:
        raise StyxValidationError("`subj_name` must not be None")
    if not isinstance(params["subj_name"], str):
        raise StyxValidationError(f'`subj_name` has the wrong type: Received `{type(params["subj_name"])}` expected `str`')
    if params.get("mode", None) is not None:
        if not isinstance(params["mode"], str):
            raise StyxValidationError(f'`mode` has the wrong type: Received `{type(params["mode"])}` expected `str`')
    if params.get("parents", None) is not None:
        if not isinstance(params["parents"], bool):
            raise StyxValidationError(f'`parents` has the wrong type: Received `{type(params["parents"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("selinux_context", None) is not None:
        if not isinstance(params["selinux_context"], bool):
            raise StyxValidationError(f'`selinux_context` has the wrong type: Received `{type(params["selinux_context"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def mksubjdirs_cargs(params: Mksubjdirs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mksubjdirs")
    cargs.append(params["subj_name"])
    v_0 = params.get("mode")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(v_0)
    if params.get("parents"):
        cargs.append("-p")
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("selinux_context"):
        cargs.append("-Z")
    if params.get("help"):
        cargs.append("--help")
    if params.get("version"):
        cargs.append("--version")
    return cargs

def mksubjdirs_outputs(params: Mksubjdirs, execution: Execution) -> MksubjdirsOutputs:
    """Build the MksubjdirsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MksubjdirsOutputs(
        root=root_v,
    )

def mksubjdirs_execute(params: Mksubjdirs, runner: Runner | None = None) -> MksubjdirsOutputs:
    """
    mksubjdirs

    A command-line tool to create subject directories.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mksubjdirs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKSUBJDIRS_METADATA)
    execution.params(params)
    args = mksubjdirs_cargs(params, execution)
    out = mksubjdirs_outputs(params, execution)
    execution.run(args)
    return out

def mksubjdirs(
    subj_name: str,
    mode: str | None = None,
    parents: bool = False,
    verbose: bool = False,
    selinux_context: bool = False,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MksubjdirsOutputs:
    """
    mksubjdirs

    A command-line tool to create subject directories.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subj_name: Name of the subject directory to create.
        mode: Set file mode (as in chmod), not a=rwx - umask
        parents: No error if existing, make parent directories as needed
        verbose: Print a message for each created directory
        selinux_context: Set SELinux security context of each created directory to\
            the default type
        help: Display help and exit
        version: Output version information and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mksubjdirs_params(
        subj_name=subj_name,
        mode=mode,
        parents=parents,
        verbose=verbose,
        selinux_context=selinux_context,
        help=help,
        version=version,
    )
    return mksubjdirs_execute(params, runner)

__all__ = [
    "Mksubjdirs",
    "MksubjdirsOutputs",
    "MKSUBJDIRS_METADATA",
    "mksubjdirs_cargs",
    "mksubjdirs_outputs",
    "mksubjdirs_params",
    "mksubjdirs_execute",
    "mksubjdirs_validate",
    "mksubjdirs",
]