# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRISP_PAINT_METADATA = Metadata(
    id="mrisp_paint",
    name="mrisp_paint",
    package="freesurfer",
)

MrispPaint = typing.TypedDict(
    "MrispPaint",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mrisp_paint"]],
        "template_file": InputPathType,
        "input_surface": InputPathType,
        "output_name": str,
        "subjects_dir": typing.NotRequired[str],
        "vertex_coords": typing.NotRequired[str],
        "average_flag": typing.NotRequired[float],
        "normalize_flag": typing.NotRequired[bool],
        "frame_number": typing.NotRequired[float],
        "square_root_flag": typing.NotRequired[bool],
        "variance_params": typing.NotRequired[str],
        "usage_flag": typing.NotRequired[bool],
        "birn_info_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
        "diag_vertex": typing.NotRequired[float],
        "version_flag": typing.NotRequired[bool],
        "diag_write_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrispPaintOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file containing a surface-worth of per-vertex values in 'curvature' format."""

def mrisp_paint_params(
    template_file: InputPathType,
    input_surface: InputPathType,
    output_name: str,
    subjects_dir: str | None = None,
    vertex_coords: str | None = None,
    average_flag: float | None = None,
    normalize_flag: bool = False,
    frame_number: float | None = None,
    square_root_flag: bool = False,
    variance_params: str | None = None,
    usage_flag: bool = False,
    birn_info_flag: bool = False,
    help_flag: bool = False,
    diag_vertex: float | None = None,
    version_flag: bool = False,
    diag_write_flag: bool = False,
) -> MrispPaint:
    """
    Build parameters.

    Args:
        template_file: Full path to the template file. Template may contain\
            multiple parameters. Example: 'somepath/mytemplate.tif#1'.
        input_surface: Full path to the input surface file, which provides the grid\
            onto which the template data is sampled.
        output_name: Output file name. Saves the surface-worth of per-vertex\
            values.
        subjects_dir: Set the SUBJECTS_DIR. Default: use environment variable.
        vertex_coords: Treat overlay as a surface and write it into a 3 frame\
            parameterization.
        average_flag: Average curvature patterns a given number of times.
        normalize_flag: Normalize curvature by variance.
        frame_number: Paint the specified frame number to the output file. Default:\
            0.
        square_root_flag: Take the square-root of the output variable.
        variance_params: Generate variance map. Requires subject name, hemisphere,\
            and field number.
        usage_flag: Print usage.
        birn_info_flag: Print BIRN-standard program information.
        help_flag: Print help message.
        diag_vertex: Invoke diagnostics for a specific vertex number.
        version_flag: Print version information.
        diag_write_flag: Write some diagnostics (DIAG_WRITE).

    Returns:
        Parameter dictionary.
    """
    params: MrispPaint = {
        "@type": "freesurfer/mrisp_paint",
        "template_file": template_file,
        "input_surface": input_surface,
        "output_name": output_name,
        "normalize_flag": normalize_flag,
        "square_root_flag": square_root_flag,
        "usage_flag": usage_flag,
        "birn_info_flag": birn_info_flag,
        "help_flag": help_flag,
        "version_flag": version_flag,
        "diag_write_flag": diag_write_flag,
    }
    if subjects_dir is not None:
        params["subjects_dir"] = subjects_dir
    if vertex_coords is not None:
        params["vertex_coords"] = vertex_coords
    if average_flag is not None:
        params["average_flag"] = average_flag
    if frame_number is not None:
        params["frame_number"] = frame_number
    if variance_params is not None:
        params["variance_params"] = variance_params
    if diag_vertex is not None:
        params["diag_vertex"] = diag_vertex
    return params

def mrisp_paint_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrispPaint."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template_file", None) is None:
        raise StyxValidationError("`template_file` must not be None")
    if not isinstance(params["template_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_file` has the wrong type: Received `{type(params["template_file"])}` expected `InputPathType`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("subjects_dir", None) is not None:
        if not isinstance(params["subjects_dir"], str):
            raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("vertex_coords", None) is not None:
        if not isinstance(params["vertex_coords"], str):
            raise StyxValidationError(f'`vertex_coords` has the wrong type: Received `{type(params["vertex_coords"])}` expected `str`')
    if params.get("average_flag", None) is not None:
        if not isinstance(params["average_flag"], (float, int)):
            raise StyxValidationError(f'`average_flag` has the wrong type: Received `{type(params["average_flag"])}` expected `float`')
    if params.get("normalize_flag", None) is not None:
        if not isinstance(params["normalize_flag"], bool):
            raise StyxValidationError(f'`normalize_flag` has the wrong type: Received `{type(params["normalize_flag"])}` expected `bool`')
    if params.get("frame_number", None) is not None:
        if not isinstance(params["frame_number"], (float, int)):
            raise StyxValidationError(f'`frame_number` has the wrong type: Received `{type(params["frame_number"])}` expected `float`')
    if params.get("square_root_flag", None) is not None:
        if not isinstance(params["square_root_flag"], bool):
            raise StyxValidationError(f'`square_root_flag` has the wrong type: Received `{type(params["square_root_flag"])}` expected `bool`')
    if params.get("variance_params", None) is not None:
        if not isinstance(params["variance_params"], str):
            raise StyxValidationError(f'`variance_params` has the wrong type: Received `{type(params["variance_params"])}` expected `str`')
    if params.get("usage_flag", None) is not None:
        if not isinstance(params["usage_flag"], bool):
            raise StyxValidationError(f'`usage_flag` has the wrong type: Received `{type(params["usage_flag"])}` expected `bool`')
    if params.get("birn_info_flag", None) is not None:
        if not isinstance(params["birn_info_flag"], bool):
            raise StyxValidationError(f'`birn_info_flag` has the wrong type: Received `{type(params["birn_info_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("diag_vertex", None) is not None:
        if not isinstance(params["diag_vertex"], (float, int)):
            raise StyxValidationError(f'`diag_vertex` has the wrong type: Received `{type(params["diag_vertex"])}` expected `float`')
    if params.get("version_flag", None) is not None:
        if not isinstance(params["version_flag"], bool):
            raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params["version_flag"])}` expected `bool`')
    if params.get("diag_write_flag", None) is not None:
        if not isinstance(params["diag_write_flag"], bool):
            raise StyxValidationError(f'`diag_write_flag` has the wrong type: Received `{type(params["diag_write_flag"])}` expected `bool`')

def mrisp_paint_cargs(params: MrispPaint, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrisp_paint")
    cargs.append(execution.input_file(params["template_file"]))
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_name"])
    v_0 = params.get("subjects_dir")
    if v_0 is not None:
        cargs.append("-SDIR")
        cargs.append(v_0)
    v_1 = params.get("vertex_coords")
    if v_1 is not None:
        cargs.append("-coords")
        cargs.append(v_1)
    v_2 = params.get("average_flag")
    if v_2 is not None:
        cargs.append("-A")
        cargs.append(str(v_2))
    if params.get("normalize_flag"):
        cargs.append("-N")
    v_3 = params.get("frame_number")
    if v_3 is not None:
        cargs.append("-f")
        cargs.append(str(v_3))
    if params.get("square_root_flag"):
        cargs.append("-S")
    v_4 = params.get("variance_params")
    if v_4 is not None:
        cargs.append("-variance")
        cargs.append(v_4)
    if params.get("usage_flag"):
        cargs.append("-?")
    if params.get("birn_info_flag"):
        cargs.append("--all-info")
    if params.get("help_flag"):
        cargs.append("--help")
    v_5 = params.get("diag_vertex")
    if v_5 is not None:
        cargs.append("-V")
        cargs.append(str(v_5))
    if params.get("version_flag"):
        cargs.append("--version")
    if params.get("diag_write_flag"):
        cargs.append("-W")
    return cargs

def mrisp_paint_outputs(params: MrispPaint, execution: Execution) -> MrispPaintOutputs:
    """Build the MrispPaintOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_name"])
    return MrispPaintOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mrisp_paint_execute(params: MrispPaint, runner: Runner | None = None) -> MrispPaintOutputs:
    """
    mrisp_paint

    A tool for extracting arrays from a surface-registration template file and sampling them onto a surface mesh.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrisp_paint_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRISP_PAINT_METADATA)
    execution.params(params)
    args = mrisp_paint_cargs(params, execution)
    out = mrisp_paint_outputs(params, execution)
    execution.run(args)
    return out

def mrisp_paint(
    template_file: InputPathType,
    input_surface: InputPathType,
    output_name: str,
    subjects_dir: str | None = None,
    vertex_coords: str | None = None,
    average_flag: float | None = None,
    normalize_flag: bool = False,
    frame_number: float | None = None,
    square_root_flag: bool = False,
    variance_params: str | None = None,
    usage_flag: bool = False,
    birn_info_flag: bool = False,
    help_flag: bool = False,
    diag_vertex: float | None = None,
    version_flag: bool = False,
    diag_write_flag: bool = False,
    runner: Runner | None = None,
) -> MrispPaintOutputs:
    """
    mrisp_paint

    A tool for extracting arrays from a surface-registration template file and sampling them onto a surface mesh.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        template_file: Full path to the template file. Template may contain\
            multiple parameters. Example: 'somepath/mytemplate.tif#1'.
        input_surface: Full path to the input surface file, which provides the grid\
            onto which the template data is sampled.
        output_name: Output file name. Saves the surface-worth of per-vertex\
            values.
        subjects_dir: Set the SUBJECTS_DIR. Default: use environment variable.
        vertex_coords: Treat overlay as a surface and write it into a 3 frame\
            parameterization.
        average_flag: Average curvature patterns a given number of times.
        normalize_flag: Normalize curvature by variance.
        frame_number: Paint the specified frame number to the output file. Default:\
            0.
        square_root_flag: Take the square-root of the output variable.
        variance_params: Generate variance map. Requires subject name, hemisphere,\
            and field number.
        usage_flag: Print usage.
        birn_info_flag: Print BIRN-standard program information.
        help_flag: Print help message.
        diag_vertex: Invoke diagnostics for a specific vertex number.
        version_flag: Print version information.
        diag_write_flag: Write some diagnostics (DIAG_WRITE).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrisp_paint_params(
        template_file=template_file,
        input_surface=input_surface,
        output_name=output_name,
        subjects_dir=subjects_dir,
        vertex_coords=vertex_coords,
        average_flag=average_flag,
        normalize_flag=normalize_flag,
        frame_number=frame_number,
        square_root_flag=square_root_flag,
        variance_params=variance_params,
        usage_flag=usage_flag,
        birn_info_flag=birn_info_flag,
        help_flag=help_flag,
        diag_vertex=diag_vertex,
        version_flag=version_flag,
        diag_write_flag=diag_write_flag,
    )
    return mrisp_paint_execute(params, runner)

__all__ = [
    "MrispPaint",
    "MrispPaintOutputs",
    "MRISP_PAINT_METADATA",
    "mrisp_paint_cargs",
    "mrisp_paint_outputs",
    "mrisp_paint_params",
    "mrisp_paint_execute",
    "mrisp_paint_validate",
    "mrisp_paint",
]