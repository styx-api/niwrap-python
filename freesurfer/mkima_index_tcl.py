# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MKIMA_INDEX_TCL_METADATA = Metadata(
    id="mkima_index.tcl",
    name="mkima_index.tcl",
    package="freesurfer",
)

MkimaIndexTcl = typing.TypedDict(
    "MkimaIndexTcl",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mkima_index.tcl"]],
        "input_file": InputPathType,
        "output_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MkimaIndexTclOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output file resulting from mkima index operations."""

def mkima_index_tcl_params(
    input_file: InputPathType,
    output_flag: bool = False,
) -> MkimaIndexTcl:
    """
    Build parameters.

    Args:
        input_file: Input file for mkima index operations.
        output_flag: Flag to specify output for mkima index operation.

    Returns:
        Parameter dictionary.
    """
    params: MkimaIndexTcl = {
        "@type": "freesurfer/mkima_index.tcl",
        "input_file": input_file,
        "output_flag": output_flag,
    }
    return params

def mkima_index_tcl_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MkimaIndexTcl."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_flag", None) is not None:
        if not isinstance(params["output_flag"], bool):
            raise StyxValidationError(f'`output_flag` has the wrong type: Received `{type(params["output_flag"])}` expected `bool`')

def mkima_index_tcl_cargs(params: MkimaIndexTcl, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mkima_index.tcl")
    cargs.append(execution.input_file(params["input_file"]))
    if params.get("output_flag"):
        cargs.append("-o")
    return cargs

def mkima_index_tcl_outputs(params: MkimaIndexTcl, execution: Execution) -> MkimaIndexTclOutputs:
    """Build the MkimaIndexTclOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["input_file"]}_index_output')
    return MkimaIndexTclOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def mkima_index_tcl_execute(params: MkimaIndexTcl, runner: Runner | None = None) -> MkimaIndexTclOutputs:
    """
    mkima_index.tcl

    A command-line tool for handling mkima index operations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mkima_index_tcl_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MKIMA_INDEX_TCL_METADATA)
    execution.params(params)
    args = mkima_index_tcl_cargs(params, execution)
    out = mkima_index_tcl_outputs(params, execution)
    execution.run(args)
    return out

def mkima_index_tcl(
    input_file: InputPathType,
    output_flag: bool = False,
    runner: Runner | None = None,
) -> MkimaIndexTclOutputs:
    """
    mkima_index.tcl

    A command-line tool for handling mkima index operations.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input file for mkima index operations.
        output_flag: Flag to specify output for mkima index operation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mkima_index_tcl_params(
        input_file=input_file,
        output_flag=output_flag,
    )
    return mkima_index_tcl_execute(params, runner)

__all__ = [
    "MkimaIndexTcl",
    "MkimaIndexTclOutputs",
    "MKIMA_INDEX_TCL_METADATA",
    "mkima_index_tcl_cargs",
    "mkima_index_tcl_outputs",
    "mkima_index_tcl_params",
    "mkima_index_tcl_execute",
    "mkima_index_tcl_validate",
    "mkima_index_tcl",
]