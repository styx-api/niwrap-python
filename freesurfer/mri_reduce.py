# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_REDUCE_METADATA = Metadata(
    id="mri_reduce",
    name="mri_reduce",
    package="freesurfer",
)

MriReduce = typing.TypedDict(
    "MriReduce",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_reduce"]],
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MriReduceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reduced_output: OutputPathType
    """Reduced MRI file output"""

def mri_reduce_params(
    input_file: InputPathType,
    output_file: str,
) -> MriReduce:
    """
    Build parameters.

    Args:
        input_file: Input MRI file
        output_file: Output filename for the reduced MRI file

    Returns:
        Parameter dictionary.
    """
    params: MriReduce = {
        "@type": "freesurfer/mri_reduce",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def mri_reduce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriReduce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def mri_reduce_cargs(params: MriReduce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_reduce")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def mri_reduce_outputs(params: MriReduce, execution: Execution) -> MriReduceOutputs:
    """Build the MriReduceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reduced_output_v: OutputPathType = execution.output_file(params["output_file"])
    return MriReduceOutputs(
        root=root_v,
        reduced_output=reduced_output_v,
    )

def mri_reduce_execute(params: MriReduce, runner: Runner | None = None) -> MriReduceOutputs:
    """
    mri_reduce

    A tool to reduce MRI file dimensions

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_reduce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_REDUCE_METADATA)
    execution.params(params)
    args = mri_reduce_cargs(params, execution)
    out = mri_reduce_outputs(params, execution)
    execution.run(args)
    return out

def mri_reduce(
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> MriReduceOutputs:
    """
    mri_reduce

    A tool to reduce MRI file dimensions

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input MRI file
        output_file: Output filename for the reduced MRI file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_reduce_params(
        input_file=input_file,
        output_file=output_file,
    )
    return mri_reduce_execute(params, runner)

__all__ = [
    "MriReduce",
    "MriReduceOutputs",
    "MRI_REDUCE_METADATA",
    "mri_reduce_cargs",
    "mri_reduce_outputs",
    "mri_reduce_params",
    "mri_reduce_execute",
    "mri_reduce_validate",
    "mri_reduce",
]