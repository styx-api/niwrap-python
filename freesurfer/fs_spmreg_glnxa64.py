# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FS_SPMREG_GLNXA64_METADATA = Metadata(
    id="fs_spmreg.glnxa64",
    name="fs_spmreg.glnxa64",
    package="freesurfer",
)

FsSpmregGlnxa64 = typing.TypedDict(
    "FsSpmregGlnxa64",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/fs_spmreg.glnxa64"]],
        "input_volume": InputPathType,
        "output_matrix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FsSpmregGlnxa64Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_matrix_file: OutputPathType
    """Output registration matrix file"""

def fs_spmreg_glnxa64_params(
    input_volume: InputPathType,
    output_matrix: str = "output.mat",
) -> FsSpmregGlnxa64:
    """
    Build parameters.

    Args:
        input_volume: Input anatomical volume
        output_matrix: Output registration matrix

    Returns:
        Parameter dictionary.
    """
    params: FsSpmregGlnxa64 = {
        "@type": "freesurfer/fs_spmreg.glnxa64",
        "input_volume": input_volume,
        "output_matrix": output_matrix,
    }
    return params

def fs_spmreg_glnxa64_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FsSpmregGlnxa64."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_matrix", None) is not None:
        if not isinstance(params["output_matrix"], str):
            raise StyxValidationError(f'`output_matrix` has the wrong type: Received `{type(params["output_matrix"])}` expected `str`')

def fs_spmreg_glnxa64_cargs(params: FsSpmregGlnxa64, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fs_spmreg.glnxa64")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params.get("output_matrix", "output.mat"))
    return cargs

def fs_spmreg_glnxa64_outputs(params: FsSpmregGlnxa64, execution: Execution) -> FsSpmregGlnxa64Outputs:
    """Build the FsSpmregGlnxa64Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_matrix_file_v: OutputPathType = execution.output_file(params.get("output_matrix", "output.mat"))
    return FsSpmregGlnxa64Outputs(
        root=root_v,
        output_matrix_file=output_matrix_file_v,
    )

def fs_spmreg_glnxa64_execute(params: FsSpmregGlnxa64, runner: Runner | None = None) -> FsSpmregGlnxa64Outputs:
    """
    fs_spmreg.glnxa64

    fs_spmreg is a tool for registration using SPM within FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fs_spmreg_glnxa64_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FS_SPMREG_GLNXA64_METADATA)
    execution.params(params)
    args = fs_spmreg_glnxa64_cargs(params, execution)
    out = fs_spmreg_glnxa64_outputs(params, execution)
    execution.run(args)
    return out

def fs_spmreg_glnxa64(
    input_volume: InputPathType,
    output_matrix: str = "output.mat",
    runner: Runner | None = None,
) -> FsSpmregGlnxa64Outputs:
    """
    fs_spmreg.glnxa64

    fs_spmreg is a tool for registration using SPM within FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input anatomical volume
        output_matrix: Output registration matrix
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fs_spmreg_glnxa64_params(
        input_volume=input_volume,
        output_matrix=output_matrix,
    )
    return fs_spmreg_glnxa64_execute(params, runner)

__all__ = [
    "FsSpmregGlnxa64",
    "FsSpmregGlnxa64Outputs",
    "FS_SPMREG_GLNXA64_METADATA",
    "fs_spmreg_glnxa64_cargs",
    "fs_spmreg_glnxa64_outputs",
    "fs_spmreg_glnxa64_params",
    "fs_spmreg_glnxa64_execute",
    "fs_spmreg_glnxa64_validate",
    "fs_spmreg_glnxa64",
]