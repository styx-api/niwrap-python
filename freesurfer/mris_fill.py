# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_FILL_METADATA = Metadata(
    id="mris_fill",
    name="mris_fill",
    package="freesurfer",
)

MrisFill = typing.TypedDict(
    "MrisFill",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_fill"]],
        "resolution": typing.NotRequired[float],
        "conform": typing.NotRequired[bool],
        "input_surface": InputPathType,
        "output_volume": str,
    },
)

@dataclasses.dataclass
class MrisFillOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    filled_volume: OutputPathType
    """The resulting filled volume output."""

def mris_fill_params(
    input_surface: InputPathType,
    output_volume: str,
    resolution: float | None = None,
    conform: bool = False,
) -> MrisFill:
    """
    Build parameters.

    Args:
        input_surface: Input surface file
        output_volume: Output volume file
        resolution: Set the resolution of the output volume (default = 0.250\
            mm/voxel)
        conform: Conform the volume before writing

    Returns:
        Parameter dictionary.
    """
    params: MrisFill = {
        "@type": "freesurfer/mris_fill",
        "input_surface": input_surface,
        "output_volume": output_volume,
        "conform": conform,
    }
    if resolution is not None:
        params["resolution"] = resolution
    return params

def mris_fill_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisFill."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("resolution", None) is not None:
        if not isinstance(params["resolution"], (float, int)):
            raise StyxValidationError(f'`resolution` has the wrong type: Received `{type(params["resolution"])}` expected `float`')
    if params.get("conform", None) is not None:
        if not isinstance(params["conform"], bool):
            raise StyxValidationError(f'`conform` has the wrong type: Received `{type(params["conform"])}` expected `bool`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')

def mris_fill_cargs(params: MrisFill, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_fill")
    v_0 = params.get("resolution")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(str(v_0))
    if params.get("conform"):
        cargs.append("-c")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_volume"])
    return cargs

def mris_fill_outputs(params: MrisFill, execution: Execution) -> MrisFillOutputs:
    """Build the MrisFillOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    filled_volume_v: OutputPathType = execution.output_file(params["output_volume"])
    return MrisFillOutputs(
        root=root_v,
        filled_volume=filled_volume_v,
    )

def mris_fill_execute(params: MrisFill, runner: Runner | None = None) -> MrisFillOutputs:
    """
    mris_fill

    A tool that floodfills the interior of a surface and writes the results into a volume of specified resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_fill_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_FILL_METADATA)
    execution.params(params)
    args = mris_fill_cargs(params, execution)
    out = mris_fill_outputs(params, execution)
    execution.run(args)
    return out

def mris_fill(
    input_surface: InputPathType,
    output_volume: str,
    resolution: float | None = None,
    conform: bool = False,
    runner: Runner | None = None,
) -> MrisFillOutputs:
    """
    mris_fill

    A tool that floodfills the interior of a surface and writes the results into a volume of specified resolution.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input surface file
        output_volume: Output volume file
        resolution: Set the resolution of the output volume (default = 0.250\
            mm/voxel)
        conform: Conform the volume before writing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_fill_params(
        input_surface=input_surface,
        output_volume=output_volume,
        resolution=resolution,
        conform=conform,
    )
    return mris_fill_execute(params, runner)

__all__ = [
    "MrisFill",
    "MrisFillOutputs",
    "MRIS_FILL_METADATA",
    "mris_fill_cargs",
    "mris_fill_outputs",
    "mris_fill_params",
    "mris_fill_execute",
    "mris_fill_validate",
    "mris_fill",
]