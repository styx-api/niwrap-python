# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_HA_T1_SH_METADATA = Metadata(
    id="segmentHA_T1.sh",
    name="segmentHA_T1.sh",
    package="freesurfer",
)

SegmentHaT1Sh = typing.TypedDict(
    "SegmentHaT1Sh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segmentHA_T1.sh"]],
        "input_image": InputPathType,
        "output_directory": str,
        "brain_mask": typing.NotRequired[InputPathType],
        "verbose": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SegmentHaT1ShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    hippocampus_aseg: OutputPathType
    """Segmented hippocampal subfields."""
    amygdala_aseg: OutputPathType
    """Segmented amygdalar subfields."""

def segment_ha_t1_sh_params(
    input_image: InputPathType,
    output_directory: str,
    brain_mask: InputPathType | None = None,
    verbose: bool = False,
    debug: bool = False,
) -> SegmentHaT1Sh:
    """
    Build parameters.

    Args:
        input_image: The input T1-weighted MRI image for hippocampal/amygdalar\
            segmentation.
        output_directory: The directory where the output will be saved.
        brain_mask: Use a specific brain mask for segmentation.
        verbose: Increase the verbosity of the output.
        debug: Enable debugging mode.

    Returns:
        Parameter dictionary.
    """
    params: SegmentHaT1Sh = {
        "@type": "freesurfer/segmentHA_T1.sh",
        "input_image": input_image,
        "output_directory": output_directory,
        "verbose": verbose,
        "debug": debug,
    }
    if brain_mask is not None:
        params["brain_mask"] = brain_mask
    return params

def segment_ha_t1_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentHaT1Sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("brain_mask", None) is not None:
        if not isinstance(params["brain_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`brain_mask` has the wrong type: Received `{type(params["brain_mask"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def segment_ha_t1_sh_cargs(params: SegmentHaT1Sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segmentHA_T1.sh")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_directory"])
    v_0 = params.get("brain_mask")
    if v_0 is not None:
        cargs.append("--brainmask")
        cargs.append(execution.input_file(v_0))
    if params.get("verbose"):
        cargs.append("--verbose")
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def segment_ha_t1_sh_outputs(params: SegmentHaT1Sh, execution: Execution) -> SegmentHaT1ShOutputs:
    """Build the SegmentHaT1ShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    hippocampus_aseg_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/hippocampus_aseg.mgz')
    amygdala_aseg_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/amygdala_aseg.mgz')
    return SegmentHaT1ShOutputs(
        root=root_v,
        hippocampus_aseg=hippocampus_aseg_v,
        amygdala_aseg=amygdala_aseg_v,
    )

def segment_ha_t1_sh_execute(params: SegmentHaT1Sh, runner: Runner | None = None) -> SegmentHaT1ShOutputs:
    """
    segmentHA_T1.sh

    Tool for hippocampal/amygdalar subfield segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_ha_t1_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_HA_T1_SH_METADATA)
    execution.params(params)
    args = segment_ha_t1_sh_cargs(params, execution)
    out = segment_ha_t1_sh_outputs(params, execution)
    execution.run(args)
    return out

def segment_ha_t1_sh(
    input_image: InputPathType,
    output_directory: str,
    brain_mask: InputPathType | None = None,
    verbose: bool = False,
    debug: bool = False,
    runner: Runner | None = None,
) -> SegmentHaT1ShOutputs:
    """
    segmentHA_T1.sh

    Tool for hippocampal/amygdalar subfield segmentation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_image: The input T1-weighted MRI image for hippocampal/amygdalar\
            segmentation.
        output_directory: The directory where the output will be saved.
        brain_mask: Use a specific brain mask for segmentation.
        verbose: Increase the verbosity of the output.
        debug: Enable debugging mode.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_ha_t1_sh_params(
        input_image=input_image,
        output_directory=output_directory,
        brain_mask=brain_mask,
        verbose=verbose,
        debug=debug,
    )
    return segment_ha_t1_sh_execute(params, runner)

__all__ = [
    "SegmentHaT1Sh",
    "SegmentHaT1ShOutputs",
    "SEGMENT_HA_T1_SH_METADATA",
    "segment_ha_t1_sh_cargs",
    "segment_ha_t1_sh_outputs",
    "segment_ha_t1_sh_params",
    "segment_ha_t1_sh_execute",
    "segment_ha_t1_sh_validate",
    "segment_ha_t1_sh",
]