# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_MS_REFINE_METADATA = Metadata(
    id="mris_ms_refine",
    name="mris_ms_refine",
    package="freesurfer",
)

MrisMsRefine = typing.TypedDict(
    "MrisMsRefine",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_ms_refine"]],
        "subject_name": str,
        "hemisphere": str,
        "xform": InputPathType,
        "flash_files": list[InputPathType],
        "residuals": InputPathType,
        "omit_self_intersection": typing.NotRequired[bool],
        "create_curvature_files": typing.NotRequired[bool],
        "average_curvature": typing.NotRequired[float],
        "white_only": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MrisMsRefineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    white_surface: OutputPathType
    """Generated white matter surface file"""
    pial_surface: OutputPathType
    """Generated gray matter surface file"""
    curvature_file: OutputPathType
    """Curvature file for cortical thickness"""
    layerIV_surface: OutputPathType
    """Surface file approximating layer IV of the cortical sheet"""

def mris_ms_refine_params(
    subject_name: str,
    hemisphere: str,
    xform: InputPathType,
    flash_files: list[InputPathType],
    residuals: InputPathType,
    omit_self_intersection: bool = False,
    create_curvature_files: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
) -> MrisMsRefine:
    """
    Build parameters.

    Args:
        subject_name: The name of the subject
        hemisphere: The hemisphere to process ('lh' or 'rh')
        xform: The transform file
        flash_files: Flash images
        residuals: Residuals file
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface.
        create_curvature_files: Create curvature and area files from white matter\
            surface.
        average_curvature: Average curvature values a specified number of times.
        white_only: Only generate white matter surface.

    Returns:
        Parameter dictionary.
    """
    params: MrisMsRefine = {
        "@type": "freesurfer/mris_ms_refine",
        "subject_name": subject_name,
        "hemisphere": hemisphere,
        "xform": xform,
        "flash_files": flash_files,
        "residuals": residuals,
        "omit_self_intersection": omit_self_intersection,
        "create_curvature_files": create_curvature_files,
        "white_only": white_only,
    }
    if average_curvature is not None:
        params["average_curvature"] = average_curvature
    return params

def mris_ms_refine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisMsRefine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_name", None) is None:
        raise StyxValidationError("`subject_name` must not be None")
    if not isinstance(params["subject_name"], str):
        raise StyxValidationError(f'`subject_name` has the wrong type: Received `{type(params["subject_name"])}` expected `str`')
    if params.get("hemisphere", None) is None:
        raise StyxValidationError("`hemisphere` must not be None")
    if not isinstance(params["hemisphere"], str):
        raise StyxValidationError(f'`hemisphere` has the wrong type: Received `{type(params["hemisphere"])}` expected `str`')
    if params.get("xform", None) is None:
        raise StyxValidationError("`xform` must not be None")
    if not isinstance(params["xform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xform` has the wrong type: Received `{type(params["xform"])}` expected `InputPathType`')
    if params.get("flash_files", None) is None:
        raise StyxValidationError("`flash_files` must not be None")
    if not isinstance(params["flash_files"], list):
        raise StyxValidationError(f'`flash_files` has the wrong type: Received `{type(params["flash_files"])}` expected `list[InputPathType]`')
    for e in params["flash_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`flash_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("residuals", None) is None:
        raise StyxValidationError("`residuals` must not be None")
    if not isinstance(params["residuals"], (pathlib.Path, str)):
        raise StyxValidationError(f'`residuals` has the wrong type: Received `{type(params["residuals"])}` expected `InputPathType`')
    if params.get("omit_self_intersection", None) is not None:
        if not isinstance(params["omit_self_intersection"], bool):
            raise StyxValidationError(f'`omit_self_intersection` has the wrong type: Received `{type(params["omit_self_intersection"])}` expected `bool`')
    if params.get("create_curvature_files", None) is not None:
        if not isinstance(params["create_curvature_files"], bool):
            raise StyxValidationError(f'`create_curvature_files` has the wrong type: Received `{type(params["create_curvature_files"])}` expected `bool`')
    if params.get("average_curvature", None) is not None:
        if not isinstance(params["average_curvature"], (float, int)):
            raise StyxValidationError(f'`average_curvature` has the wrong type: Received `{type(params["average_curvature"])}` expected `float`')
    if params.get("white_only", None) is not None:
        if not isinstance(params["white_only"], bool):
            raise StyxValidationError(f'`white_only` has the wrong type: Received `{type(params["white_only"])}` expected `bool`')

def mris_ms_refine_cargs(params: MrisMsRefine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_ms_refine")
    cargs.append(params["subject_name"])
    cargs.append(params["hemisphere"])
    cargs.append(execution.input_file(params["xform"]))
    for item0 in params["flash_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append(execution.input_file(params["residuals"]))
    if params.get("omit_self_intersection"):
        cargs.append("-q")
    if params.get("create_curvature_files"):
        cargs.append("-c")
    v_0 = params.get("average_curvature")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(str(v_0))
    if params.get("white_only"):
        cargs.append("-whiteonly")
    return cargs

def mris_ms_refine_outputs(params: MrisMsRefine, execution: Execution) -> MrisMsRefineOutputs:
    """Build the MrisMsRefineOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    white_surface_v: OutputPathType = execution.output_file(f'{params["subject_name"]}/{params["hemisphere"]}.white')
    pial_surface_v: OutputPathType = execution.output_file(f'{params["subject_name"]}/{params["hemisphere"]}.pial')
    curvature_file_v: OutputPathType = execution.output_file(f'{params["subject_name"]}/{params["hemisphere"]}.curv')
    layerIV_surface_v: OutputPathType = execution.output_file(f'{params["subject_name"]}/{params["hemisphere"]}.layerIV')
    return MrisMsRefineOutputs(
        root=root_v,
        white_surface=white_surface_v,
        pial_surface=pial_surface_v,
        curvature_file=curvature_file_v,
        layerIV_surface=layerIV_surface_v,
    )

def mris_ms_refine_execute(params: MrisMsRefine, runner: Runner | None = None) -> MrisMsRefineOutputs:
    """
    mris_ms_refine

    This program positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface. It generates surface files for these surfaces as well as a 'curvature' file for the cortical thickness, and a surface file which approximates layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_ms_refine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_MS_REFINE_METADATA)
    execution.params(params)
    args = mris_ms_refine_cargs(params, execution)
    out = mris_ms_refine_outputs(params, execution)
    execution.run(args)
    return out

def mris_ms_refine(
    subject_name: str,
    hemisphere: str,
    xform: InputPathType,
    flash_files: list[InputPathType],
    residuals: InputPathType,
    omit_self_intersection: bool = False,
    create_curvature_files: bool = False,
    average_curvature: float | None = None,
    white_only: bool = False,
    runner: Runner | None = None,
) -> MrisMsRefineOutputs:
    """
    mris_ms_refine

    This program positions the tessellation of the cortical surface at the white matter surface, then the gray matter surface. It generates surface files for these surfaces as well as a 'curvature' file for the cortical thickness, and a surface file which approximates layer IV of the cortical sheet.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_name: The name of the subject
        hemisphere: The hemisphere to process ('lh' or 'rh')
        xform: The transform file
        flash_files: Flash images
        residuals: Residuals file
        omit_self_intersection: Omit self-intersection and only generate gray/white\
            surface.
        create_curvature_files: Create curvature and area files from white matter\
            surface.
        average_curvature: Average curvature values a specified number of times.
        white_only: Only generate white matter surface.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_ms_refine_params(
        subject_name=subject_name,
        hemisphere=hemisphere,
        xform=xform,
        flash_files=flash_files,
        residuals=residuals,
        omit_self_intersection=omit_self_intersection,
        create_curvature_files=create_curvature_files,
        average_curvature=average_curvature,
        white_only=white_only,
    )
    return mris_ms_refine_execute(params, runner)

__all__ = [
    "MrisMsRefine",
    "MrisMsRefineOutputs",
    "MRIS_MS_REFINE_METADATA",
    "mris_ms_refine_cargs",
    "mris_ms_refine_outputs",
    "mris_ms_refine_params",
    "mris_ms_refine_execute",
    "mris_ms_refine_validate",
    "mris_ms_refine",
]