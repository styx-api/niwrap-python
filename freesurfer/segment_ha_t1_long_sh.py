# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SEGMENT_HA_T1_LONG_SH_METADATA = Metadata(
    id="segmentHA_T1_long.sh",
    name="segmentHA_T1_long.sh",
    package="freesurfer",
)

SegmentHaT1LongSh = typing.TypedDict(
    "SegmentHaT1LongSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/segmentHA_T1_long.sh"]],
        "subject_dir": str,
        "subject_id": str,
    },
)

@dataclasses.dataclass
class SegmentHaT1LongShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dir: OutputPathType
    """Output directory containing segmentation results"""

def segment_ha_t1_long_sh_params(
    subject_dir: str,
    subject_id: str,
) -> SegmentHaT1LongSh:
    """
    Build parameters.

    Args:
        subject_dir: Directory containing subject data
        subject_id: Identifier for the subject within the subject directory

    Returns:
        Parameter dictionary.
    """
    params: SegmentHaT1LongSh = {
        "@type": "freesurfer/segmentHA_T1_long.sh",
        "subject_dir": subject_dir,
        "subject_id": subject_id,
    }
    return params

def segment_ha_t1_long_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SegmentHaT1LongSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("subject_id", None) is None:
        raise StyxValidationError("`subject_id` must not be None")
    if not isinstance(params["subject_id"], str):
        raise StyxValidationError(f'`subject_id` has the wrong type: Received `{type(params["subject_id"])}` expected `str`')

def segment_ha_t1_long_sh_cargs(params: SegmentHaT1LongSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("segmentHA_T1_long.sh")
    cargs.append(params["subject_dir"])
    cargs.append(params["subject_id"])
    return cargs

def segment_ha_t1_long_sh_outputs(params: SegmentHaT1LongSh, execution: Execution) -> SegmentHaT1LongShOutputs:
    """Build the SegmentHaT1LongShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dir_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}/{params["subject_id"]}_long_segment/output')
    return SegmentHaT1LongShOutputs(
        root=root_v,
        output_dir=output_dir_v,
    )

def segment_ha_t1_long_sh_execute(params: SegmentHaT1LongSh, runner: Runner | None = None) -> SegmentHaT1LongShOutputs:
    """
    segmentHA_T1_long.sh

    A script for longitudinal segmentation of the hippocampal/amygdala regions

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    segment_ha_t1_long_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SEGMENT_HA_T1_LONG_SH_METADATA)
    execution.params(params)
    args = segment_ha_t1_long_sh_cargs(params, execution)
    out = segment_ha_t1_long_sh_outputs(params, execution)
    execution.run(args)
    return out

def segment_ha_t1_long_sh(
    subject_dir: str,
    subject_id: str,
    runner: Runner | None = None,
) -> SegmentHaT1LongShOutputs:
    """
    segmentHA_T1_long.sh

    A script for longitudinal segmentation of the hippocampal/amygdala regions

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_dir: Directory containing subject data
        subject_id: Identifier for the subject within the subject directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = segment_ha_t1_long_sh_params(
        subject_dir=subject_dir,
        subject_id=subject_id,
    )
    return segment_ha_t1_long_sh_execute(params, runner)

__all__ = [
    "SegmentHaT1LongSh",
    "SegmentHaT1LongShOutputs",
    "SEGMENT_HA_T1_LONG_SH_METADATA",
    "segment_ha_t1_long_sh_cargs",
    "segment_ha_t1_long_sh_outputs",
    "segment_ha_t1_long_sh_params",
    "segment_ha_t1_long_sh_execute",
    "segment_ha_t1_long_sh_validate",
    "segment_ha_t1_long_sh",
]