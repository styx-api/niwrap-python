# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_REVERSE_METADATA = Metadata(
    id="mris_reverse",
    name="mris_reverse",
    package="freesurfer",
)

MrisReverse = typing.TypedDict(
    "MrisReverse",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_reverse"]],
        "input_surface": InputPathType,
        "output_surface": str,
    },
)

@dataclasses.dataclass
class MrisReverseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reversed_surface: OutputPathType
    """Reversed cortical surface file."""

def mris_reverse_params(
    input_surface: InputPathType,
    output_surface: str,
) -> MrisReverse:
    """
    Build parameters.

    Args:
        input_surface: Input cortical surface file.
        output_surface: Output cortical surface file.

    Returns:
        Parameter dictionary.
    """
    params: MrisReverse = {
        "@type": "freesurfer/mris_reverse",
        "input_surface": input_surface,
        "output_surface": output_surface,
    }
    return params

def mris_reverse_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisReverse."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')

def mris_reverse_cargs(params: MrisReverse, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_reverse")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append(params["output_surface"])
    return cargs

def mris_reverse_outputs(params: MrisReverse, execution: Execution) -> MrisReverseOutputs:
    """Build the MrisReverseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reversed_surface_v: OutputPathType = execution.output_file(f'{params["output_surface"]}.surf')
    return MrisReverseOutputs(
        root=root_v,
        reversed_surface=reversed_surface_v,
    )

def mris_reverse_execute(params: MrisReverse, runner: Runner | None = None) -> MrisReverseOutputs:
    """
    mris_reverse

    This tool reverses a cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_reverse_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_REVERSE_METADATA)
    execution.params(params)
    args = mris_reverse_cargs(params, execution)
    out = mris_reverse_outputs(params, execution)
    execution.run(args)
    return out

def mris_reverse(
    input_surface: InputPathType,
    output_surface: str,
    runner: Runner | None = None,
) -> MrisReverseOutputs:
    """
    mris_reverse

    This tool reverses a cortical surface.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_surface: Input cortical surface file.
        output_surface: Output cortical surface file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_reverse_params(
        input_surface=input_surface,
        output_surface=output_surface,
    )
    return mris_reverse_execute(params, runner)

__all__ = [
    "MrisReverse",
    "MrisReverseOutputs",
    "MRIS_REVERSE_METADATA",
    "mris_reverse_cargs",
    "mris_reverse_outputs",
    "mris_reverse_params",
    "mris_reverse_execute",
    "mris_reverse_validate",
    "mris_reverse",
]