# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_RF_LONG_LABEL_METADATA = Metadata(
    id="mri_rf_long_label",
    name="mri_rf_long_label",
    package="freesurfer",
)

MriRfLongLabel = typing.TypedDict(
    "MriRfLongLabel",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_rf_long_label"]],
        "help_flag": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriRfLongLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_rf_long_label_params(
    help_flag: str | None = None,
) -> MriRfLongLabel:
    """
    Build parameters.

    Args:
        help_flag: Displays a message that the tool has been removed.

    Returns:
        Parameter dictionary.
    """
    params: MriRfLongLabel = {
        "@type": "freesurfer/mri_rf_long_label",
    }
    if help_flag is not None:
        params["help_flag"] = help_flag
    return params

def mri_rf_long_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriRfLongLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], str):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `str`')

def mri_rf_long_label_cargs(params: MriRfLongLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_rf_long_label")
    v_0 = params.get("help_flag")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def mri_rf_long_label_outputs(params: MriRfLongLabel, execution: Execution) -> MriRfLongLabelOutputs:
    """Build the MriRfLongLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriRfLongLabelOutputs(
        root=root_v,
    )

def mri_rf_long_label_execute(params: MriRfLongLabel, runner: Runner | None = None) -> MriRfLongLabelOutputs:
    """
    mri_rf_long_label

    The mri_rf_long_label tool has been removed from this version of FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_rf_long_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_RF_LONG_LABEL_METADATA)
    execution.params(params)
    args = mri_rf_long_label_cargs(params, execution)
    out = mri_rf_long_label_outputs(params, execution)
    execution.run(args)
    return out

def mri_rf_long_label(
    help_flag: str | None = None,
    runner: Runner | None = None,
) -> MriRfLongLabelOutputs:
    """
    mri_rf_long_label

    The mri_rf_long_label tool has been removed from this version of FreeSurfer.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        help_flag: Displays a message that the tool has been removed.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_rf_long_label_params(
        help_flag=help_flag,
    )
    return mri_rf_long_label_execute(params, runner)

__all__ = [
    "MriRfLongLabel",
    "MriRfLongLabelOutputs",
    "MRI_RF_LONG_LABEL_METADATA",
    "mri_rf_long_label_cargs",
    "mri_rf_long_label_outputs",
    "mri_rf_long_label_params",
    "mri_rf_long_label_execute",
    "mri_rf_long_label_validate",
    "mri_rf_long_label",
]