# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPLINE3_TEST_METADATA = Metadata(
    id="Spline3_test",
    name="Spline3_test",
    package="freesurfer",
)

Spline3Test = typing.TypedDict(
    "Spline3Test",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/Spline3_test"]],
        "x_values": list[float],
        "y_values": list[float],
        "x_new_values": list[float],
    },
)

@dataclasses.dataclass
class Spline3TestOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_ynew: OutputPathType
    """Interpolated y values at new x positions."""
    output_ynewarr: OutputPathType
    """Array format of interpolated y values at new x positions."""

def spline3_test_params(
    x_values: list[float],
    y_values: list[float],
    x_new_values: list[float],
) -> Spline3Test:
    """
    Build parameters.

    Args:
        x_values: Original x values for interpolation.
        y_values: Original y values corresponding to x values.
        x_new_values: New x values where interpolation is evaluated.

    Returns:
        Parameter dictionary.
    """
    params: Spline3Test = {
        "@type": "freesurfer/Spline3_test",
        "x_values": x_values,
        "y_values": y_values,
        "x_new_values": x_new_values,
    }
    return params

def spline3_test_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Spline3Test."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("x_values", None) is None:
        raise StyxValidationError("`x_values` must not be None")
    if not isinstance(params["x_values"], list):
        raise StyxValidationError(f'`x_values` has the wrong type: Received `{type(params["x_values"])}` expected `list[float]`')
    for e in params["x_values"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`x_values` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("y_values", None) is None:
        raise StyxValidationError("`y_values` must not be None")
    if not isinstance(params["y_values"], list):
        raise StyxValidationError(f'`y_values` has the wrong type: Received `{type(params["y_values"])}` expected `list[float]`')
    for e_2 in params["y_values"]:
        if not isinstance(e_2, (float, int)):
            raise StyxValidationError(f'`y_values` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("x_new_values", None) is None:
        raise StyxValidationError("`x_new_values` must not be None")
    if not isinstance(params["x_new_values"], list):
        raise StyxValidationError(f'`x_new_values` has the wrong type: Received `{type(params["x_new_values"])}` expected `list[float]`')
    for e_3 in params["x_new_values"]:
        if not isinstance(e_3, (float, int)):
            raise StyxValidationError(f'`x_new_values` has the wrong type: Received `{type(e_3)}` expected `list[float]`')

def spline3_test_cargs(params: Spline3Test, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("Spline3_test")
    for item0 in params["x_values"]:
        cargs.append(str(item0))
    for item1 in params["y_values"]:
        cargs.append(str(item1))
    for item2 in params["x_new_values"]:
        cargs.append(str(item2))
    return cargs

def spline3_test_outputs(params: Spline3Test, execution: Execution) -> Spline3TestOutputs:
    """Build the Spline3TestOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_ynew_v: OutputPathType = execution.output_file("Ynew.txt")
    output_ynewarr_v: OutputPathType = execution.output_file("Ynewarr.txt")
    return Spline3TestOutputs(
        root=root_v,
        output_ynew=output_ynew_v,
        output_ynewarr=output_ynewarr_v,
    )

def spline3_test_execute(params: Spline3Test, runner: Runner | None = None) -> Spline3TestOutputs:
    """
    Spline3_test

    A tool for cubic spline interpolation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spline3_test_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPLINE3_TEST_METADATA)
    execution.params(params)
    args = spline3_test_cargs(params, execution)
    out = spline3_test_outputs(params, execution)
    execution.run(args)
    return out

def spline3_test(
    x_values: list[float],
    y_values: list[float],
    x_new_values: list[float],
    runner: Runner | None = None,
) -> Spline3TestOutputs:
    """
    Spline3_test

    A tool for cubic spline interpolation.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        x_values: Original x values for interpolation.
        y_values: Original y values corresponding to x values.
        x_new_values: New x values where interpolation is evaluated.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spline3_test_params(
        x_values=x_values,
        y_values=y_values,
        x_new_values=x_new_values,
    )
    return spline3_test_execute(params, runner)

__all__ = [
    "Spline3Test",
    "Spline3TestOutputs",
    "SPLINE3_TEST_METADATA",
    "spline3_test_cargs",
    "spline3_test_outputs",
    "spline3_test_params",
    "spline3_test_execute",
    "spline3_test_validate",
    "spline3_test",
]