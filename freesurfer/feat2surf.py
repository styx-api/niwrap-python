# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FEAT2SURF_METADATA = Metadata(
    id="feat2surf",
    name="feat2surf",
    package="freesurfer",
)

Feat2surf = typing.TypedDict(
    "Feat2surf",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/feat2surf"]],
        "feat_dirs": list[str],
        "feat_dirfile": typing.NotRequired[InputPathType],
        "proj_frac": typing.NotRequired[float],
        "hemi": typing.NotRequired[str],
        "target": typing.NotRequired[str],
        "surf": typing.NotRequired[str],
        "cope_only": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "nolog_flag": typing.NotRequired[bool],
        "out_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Feat2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    lh_output: OutputPathType | None
    """Output statistics on the left hemisphere for the subject"""
    rh_output: OutputPathType | None
    """Output statistics on the right hemisphere for the subject"""
    lh_target_output: OutputPathType | None
    """Output statistics on the left hemisphere for the target subject"""
    rh_target_output: OutputPathType | None
    """Output statistics on the right hemisphere for the target subject"""

def feat2surf_params(
    feat_dirs: list[str],
    feat_dirfile: InputPathType | None = None,
    proj_frac: float | None = None,
    hemi: str | None = None,
    target: str | None = None,
    surf: str | None = None,
    cope_only: bool = False,
    debug_flag: bool = False,
    nolog_flag: bool = False,
    out_dir: str | None = None,
) -> Feat2surf:
    """
    Build parameters.

    Args:
        feat_dirs: Directory where Feat results are stored. Can specify multiple\
            directories.
        feat_dirfile: File with a list of Feat directories.
        proj_frac: Sample functional a fraction of the cortical thickness normal to\
            the surface. Default is 0.
        hemi: Run on specified hemisphere (lh or rh) only. Default is both\
            hemispheres.
        target: Subject used to define common surface space. Default is fsaverage.
        surf: Surface to resample to. Default is white.
        cope_only: Only map the copes and varcopes to the common surface space
        debug_flag: Turn on debugging
        nolog_flag: Do not create a log file
        out_dir: Output directory to use instead of default feat/reg_surf-?h/stats.

    Returns:
        Parameter dictionary.
    """
    params: Feat2surf = {
        "@type": "freesurfer/feat2surf",
        "feat_dirs": feat_dirs,
        "cope_only": cope_only,
        "debug_flag": debug_flag,
        "nolog_flag": nolog_flag,
    }
    if feat_dirfile is not None:
        params["feat_dirfile"] = feat_dirfile
    if proj_frac is not None:
        params["proj_frac"] = proj_frac
    if hemi is not None:
        params["hemi"] = hemi
    if target is not None:
        params["target"] = target
    if surf is not None:
        params["surf"] = surf
    if out_dir is not None:
        params["out_dir"] = out_dir
    return params

def feat2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Feat2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_dirs", None) is None:
        raise StyxValidationError("`feat_dirs` must not be None")
    if not isinstance(params["feat_dirs"], list):
        raise StyxValidationError(f'`feat_dirs` has the wrong type: Received `{type(params["feat_dirs"])}` expected `list[str]`')
    for e in params["feat_dirs"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`feat_dirs` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("feat_dirfile", None) is not None:
        if not isinstance(params["feat_dirfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`feat_dirfile` has the wrong type: Received `{type(params["feat_dirfile"])}` expected `InputPathType`')
    if params.get("proj_frac", None) is not None:
        if not isinstance(params["proj_frac"], (float, int)):
            raise StyxValidationError(f'`proj_frac` has the wrong type: Received `{type(params["proj_frac"])}` expected `float`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("target", None) is not None:
        if not isinstance(params["target"], str):
            raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `str`')
    if params.get("cope_only", None) is not None:
        if not isinstance(params["cope_only"], bool):
            raise StyxValidationError(f'`cope_only` has the wrong type: Received `{type(params["cope_only"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("nolog_flag", None) is not None:
        if not isinstance(params["nolog_flag"], bool):
            raise StyxValidationError(f'`nolog_flag` has the wrong type: Received `{type(params["nolog_flag"])}` expected `bool`')
    if params.get("out_dir", None) is not None:
        if not isinstance(params["out_dir"], str):
            raise StyxValidationError(f'`out_dir` has the wrong type: Received `{type(params["out_dir"])}` expected `str`')

def feat2surf_cargs(params: Feat2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("feat2surf")
    cargs.append("--feat")
    for item0 in params["feat_dirs"]:
        cargs.append(item0)
    v_0 = params.get("feat_dirfile")
    if v_0 is not None:
        cargs.append("--featdirfile")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("proj_frac")
    if v_1 is not None:
        cargs.append("--projfrac")
        cargs.append(str(v_1))
    v_2 = params.get("hemi")
    if v_2 is not None:
        cargs.append("--hemi")
        cargs.append(v_2)
    v_3 = params.get("target")
    if v_3 is not None:
        cargs.append("--target")
        cargs.append(v_3)
    v_4 = params.get("surf")
    if v_4 is not None:
        cargs.append("--surf")
        cargs.append(v_4)
    if params.get("cope_only"):
        cargs.append("--cope-only")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("nolog_flag"):
        cargs.append("--nolog")
    v_5 = params.get("out_dir")
    if v_5 is not None:
        cargs.append("--out")
        cargs.append(v_5)
    return cargs

def feat2surf_outputs(params: Feat2surf, execution: Execution) -> Feat2surfOutputs:
    """Build the Feat2surfOutputs object for this tool."""
    lh_output_v: OutputPathType | None = None
    rh_output_v: OutputPathType | None = None
    lh_target_output_v: OutputPathType | None = None
    rh_target_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_dir")
    if __v0 is not None:
        lh_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/reg_surf-lh-Subject/stats')
    __v1 = params.get("out_dir")
    if __v1 is not None:
        rh_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/reg_surf-rh-Subject/stats')
    __v2 = params.get("out_dir")
    if __v2 is not None:
        lh_target_output_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}/reg_surf-lh-targid/stats')
    __v3 = params.get("out_dir")
    if __v3 is not None:
        rh_target_output_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}/reg_surf-rh-targid/stats')
    return Feat2surfOutputs(
        root=root_v,
        lh_output=lh_output_v,
        rh_output=rh_output_v,
        lh_target_output=lh_target_output_v,
        rh_target_output=rh_target_output_v,
    )

def feat2surf_execute(params: Feat2surf, runner: Runner | None = None) -> Feat2surfOutputs:
    """
    feat2surf

    Resamples Feat statistics onto the surface of the subject and onto a stereo-taxic surface atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    feat2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FEAT2SURF_METADATA)
    execution.params(params)
    args = feat2surf_cargs(params, execution)
    out = feat2surf_outputs(params, execution)
    execution.run(args)
    return out

def feat2surf(
    feat_dirs: list[str],
    feat_dirfile: InputPathType | None = None,
    proj_frac: float | None = None,
    hemi: str | None = None,
    target: str | None = None,
    surf: str | None = None,
    cope_only: bool = False,
    debug_flag: bool = False,
    nolog_flag: bool = False,
    out_dir: str | None = None,
    runner: Runner | None = None,
) -> Feat2surfOutputs:
    """
    feat2surf

    Resamples Feat statistics onto the surface of the subject and onto a stereo-taxic surface atlas.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        feat_dirs: Directory where Feat results are stored. Can specify multiple\
            directories.
        feat_dirfile: File with a list of Feat directories.
        proj_frac: Sample functional a fraction of the cortical thickness normal to\
            the surface. Default is 0.
        hemi: Run on specified hemisphere (lh or rh) only. Default is both\
            hemispheres.
        target: Subject used to define common surface space. Default is fsaverage.
        surf: Surface to resample to. Default is white.
        cope_only: Only map the copes and varcopes to the common surface space
        debug_flag: Turn on debugging
        nolog_flag: Do not create a log file
        out_dir: Output directory to use instead of default feat/reg_surf-?h/stats.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = feat2surf_params(
        feat_dirs=feat_dirs,
        feat_dirfile=feat_dirfile,
        proj_frac=proj_frac,
        hemi=hemi,
        target=target,
        surf=surf,
        cope_only=cope_only,
        debug_flag=debug_flag,
        nolog_flag=nolog_flag,
        out_dir=out_dir,
    )
    return feat2surf_execute(params, runner)

__all__ = [
    "Feat2surf",
    "Feat2surfOutputs",
    "FEAT2SURF_METADATA",
    "feat2surf_cargs",
    "feat2surf_outputs",
    "feat2surf_params",
    "feat2surf_execute",
    "feat2surf_validate",
    "feat2surf",
]