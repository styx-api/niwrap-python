# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_TESSELLATE_METADATA = Metadata(
    id="mri_tessellate",
    name="mri_tessellate",
    package="freesurfer",
)

MriTessellate = typing.TypedDict(
    "MriTessellate",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_tessellate"]],
        "input_volume": InputPathType,
        "label_value": int,
        "output_surf": str,
        "different_labels": typing.NotRequired[bool],
        "max_vertices": typing.NotRequired[int],
        "real_ras": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MriTessellateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface_file: OutputPathType
    """Output file of the tessellation surface, typically ?h.orig."""

def mri_tessellate_params(
    input_volume: InputPathType,
    label_value: int,
    output_surf: str,
    different_labels: bool = False,
    max_vertices: int | None = None,
    real_ras: bool = False,
) -> MriTessellate:
    """
    Build parameters.

    Args:
        input_volume: Input volume (e.g., a filled MRI image).
        label_value: Label value for the tessellation. Integer value, if input is\
            filled volume, 127 is rh, 255 is lh.
        output_surf: Binary surface of the tessellation, output file.
        different_labels: Tessellate the surface of all voxels with different\
            labels.
        max_vertices: Set the max number of vertices to nvertices, and the max\
            number of faces to (2 * nvertices).
        real_ras: Save surface with real RAS coordinates where c_(r,a,s) != 0.

    Returns:
        Parameter dictionary.
    """
    params: MriTessellate = {
        "@type": "freesurfer/mri_tessellate",
        "input_volume": input_volume,
        "label_value": label_value,
        "output_surf": output_surf,
        "different_labels": different_labels,
        "real_ras": real_ras,
    }
    if max_vertices is not None:
        params["max_vertices"] = max_vertices
    return params

def mri_tessellate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriTessellate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("label_value", None) is None:
        raise StyxValidationError("`label_value` must not be None")
    if not isinstance(params["label_value"], int):
        raise StyxValidationError(f'`label_value` has the wrong type: Received `{type(params["label_value"])}` expected `int`')
    if params.get("output_surf", None) is None:
        raise StyxValidationError("`output_surf` must not be None")
    if not isinstance(params["output_surf"], str):
        raise StyxValidationError(f'`output_surf` has the wrong type: Received `{type(params["output_surf"])}` expected `str`')
    if params.get("different_labels", None) is not None:
        if not isinstance(params["different_labels"], bool):
            raise StyxValidationError(f'`different_labels` has the wrong type: Received `{type(params["different_labels"])}` expected `bool`')
    if params.get("max_vertices", None) is not None:
        if not isinstance(params["max_vertices"], int):
            raise StyxValidationError(f'`max_vertices` has the wrong type: Received `{type(params["max_vertices"])}` expected `int`')
    if params.get("real_ras", None) is not None:
        if not isinstance(params["real_ras"], bool):
            raise StyxValidationError(f'`real_ras` has the wrong type: Received `{type(params["real_ras"])}` expected `bool`')

def mri_tessellate_cargs(params: MriTessellate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_tessellate")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(str(params["label_value"]))
    cargs.append(params["output_surf"])
    if params.get("different_labels"):
        cargs.append("-a")
    v_0 = params.get("max_vertices")
    if v_0 is not None:
        cargs.append("-maxv")
        cargs.append(str(v_0))
    if params.get("real_ras"):
        cargs.append("-n")
    return cargs

def mri_tessellate_outputs(params: MriTessellate, execution: Execution) -> MriTessellateOutputs:
    """Build the MriTessellateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_surface_file_v: OutputPathType = execution.output_file(params["output_surf"])
    return MriTessellateOutputs(
        root=root_v,
        output_surface_file=output_surface_file_v,
    )

def mri_tessellate_execute(params: MriTessellate, runner: Runner | None = None) -> MriTessellateOutputs:
    """
    mri_tessellate

    This program creates a surface by tessellating a given input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_tessellate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_TESSELLATE_METADATA)
    execution.params(params)
    args = mri_tessellate_cargs(params, execution)
    out = mri_tessellate_outputs(params, execution)
    execution.run(args)
    return out

def mri_tessellate(
    input_volume: InputPathType,
    label_value: int,
    output_surf: str,
    different_labels: bool = False,
    max_vertices: int | None = None,
    real_ras: bool = False,
    runner: Runner | None = None,
) -> MriTessellateOutputs:
    """
    mri_tessellate

    This program creates a surface by tessellating a given input volume.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_volume: Input volume (e.g., a filled MRI image).
        label_value: Label value for the tessellation. Integer value, if input is\
            filled volume, 127 is rh, 255 is lh.
        output_surf: Binary surface of the tessellation, output file.
        different_labels: Tessellate the surface of all voxels with different\
            labels.
        max_vertices: Set the max number of vertices to nvertices, and the max\
            number of faces to (2 * nvertices).
        real_ras: Save surface with real RAS coordinates where c_(r,a,s) != 0.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_tessellate_params(
        input_volume=input_volume,
        label_value=label_value,
        output_surf=output_surf,
        different_labels=different_labels,
        max_vertices=max_vertices,
        real_ras=real_ras,
    )
    return mri_tessellate_execute(params, runner)

__all__ = [
    "MriTessellate",
    "MriTessellateOutputs",
    "MRI_TESSELLATE_METADATA",
    "mri_tessellate_cargs",
    "mri_tessellate_outputs",
    "mri_tessellate_params",
    "mri_tessellate_execute",
    "mri_tessellate_validate",
    "mri_tessellate",
]