# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNPACKMINCDIR_METADATA = Metadata(
    id="unpackmincdir",
    name="unpackmincdir",
    package="freesurfer",
)

Unpackmincdir = typing.TypedDict(
    "Unpackmincdir",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/unpackmincdir"]],
        "source_directory": str,
        "target_directory": str,
        "scan_sequence_info": typing.NotRequired[str],
        "functional_sequence": typing.NotRequired[str],
        "functional_subdirectory": typing.NotRequired[str],
        "minc_only": typing.NotRequired[bool],
        "no_copy": typing.NotRequired[bool],
        "umask": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class UnpackmincdirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def unpackmincdir_params(
    source_directory: str,
    target_directory: str,
    scan_sequence_info: str | None = None,
    functional_sequence: str | None = None,
    functional_subdirectory: str | None = None,
    minc_only: bool = False,
    no_copy: bool = False,
    umask: str | None = None,
) -> Unpackmincdir:
    """
    Build parameters.

    Args:
        source_directory: Source directory containing the MINC files.
        target_directory: Target directory to unpack the contents to.
        scan_sequence_info: Scan sequence directives file, e.g.,\
            freesurfer_alpha/scanseq.info.
        functional_sequence: Use seqname for functionals (example:\
            ep2d_fid_ts_20b2604).
        functional_subdirectory: Functional subdirectory, e.g., bold.
        minc_only: Do not unpack into bshorts.
        no_copy: Create directories but do not copy/convert data.
        umask: Unix file permission mask (default: 22).

    Returns:
        Parameter dictionary.
    """
    params: Unpackmincdir = {
        "@type": "freesurfer/unpackmincdir",
        "source_directory": source_directory,
        "target_directory": target_directory,
        "minc_only": minc_only,
        "no_copy": no_copy,
    }
    if scan_sequence_info is not None:
        params["scan_sequence_info"] = scan_sequence_info
    if functional_sequence is not None:
        params["functional_sequence"] = functional_sequence
    if functional_subdirectory is not None:
        params["functional_subdirectory"] = functional_subdirectory
    if umask is not None:
        params["umask"] = umask
    return params

def unpackmincdir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Unpackmincdir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_directory", None) is None:
        raise StyxValidationError("`source_directory` must not be None")
    if not isinstance(params["source_directory"], str):
        raise StyxValidationError(f'`source_directory` has the wrong type: Received `{type(params["source_directory"])}` expected `str`')
    if params.get("target_directory", None) is None:
        raise StyxValidationError("`target_directory` must not be None")
    if not isinstance(params["target_directory"], str):
        raise StyxValidationError(f'`target_directory` has the wrong type: Received `{type(params["target_directory"])}` expected `str`')
    if params.get("scan_sequence_info", None) is not None:
        if not isinstance(params["scan_sequence_info"], str):
            raise StyxValidationError(f'`scan_sequence_info` has the wrong type: Received `{type(params["scan_sequence_info"])}` expected `str`')
    if params.get("functional_sequence", None) is not None:
        if not isinstance(params["functional_sequence"], str):
            raise StyxValidationError(f'`functional_sequence` has the wrong type: Received `{type(params["functional_sequence"])}` expected `str`')
    if params.get("functional_subdirectory", None) is not None:
        if not isinstance(params["functional_subdirectory"], str):
            raise StyxValidationError(f'`functional_subdirectory` has the wrong type: Received `{type(params["functional_subdirectory"])}` expected `str`')
    if params.get("minc_only", None) is not None:
        if not isinstance(params["minc_only"], bool):
            raise StyxValidationError(f'`minc_only` has the wrong type: Received `{type(params["minc_only"])}` expected `bool`')
    if params.get("no_copy", None) is not None:
        if not isinstance(params["no_copy"], bool):
            raise StyxValidationError(f'`no_copy` has the wrong type: Received `{type(params["no_copy"])}` expected `bool`')
    if params.get("umask", None) is not None:
        if not isinstance(params["umask"], str):
            raise StyxValidationError(f'`umask` has the wrong type: Received `{type(params["umask"])}` expected `str`')

def unpackmincdir_cargs(params: Unpackmincdir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unpackmincdir")
    cargs.append("-src")
    cargs.append(params["source_directory"])
    cargs.append("-targ")
    cargs.append(params["target_directory"])
    v_0 = params.get("scan_sequence_info")
    if v_0 is not None:
        cargs.append("-scanseqinfo")
        cargs.append(v_0)
    v_1 = params.get("functional_sequence")
    if v_1 is not None:
        cargs.append("-funcseq")
        cargs.append(v_1)
    v_2 = params.get("functional_subdirectory")
    if v_2 is not None:
        cargs.append("-fsd")
        cargs.append(v_2)
    if params.get("minc_only"):
        cargs.append("-minconly")
    if params.get("no_copy"):
        cargs.append("-nocopy")
    v_3 = params.get("umask")
    if v_3 is not None:
        cargs.append("-umask")
        cargs.append(v_3)
    return cargs

def unpackmincdir_outputs(params: Unpackmincdir, execution: Execution) -> UnpackmincdirOutputs:
    """Build the UnpackmincdirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return UnpackmincdirOutputs(
        root=root_v,
    )

def unpackmincdir_execute(params: Unpackmincdir, runner: Runner | None = None) -> UnpackmincdirOutputs:
    """
    unpackmincdir

    Tool for unpacking directories with MINC files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unpackmincdir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNPACKMINCDIR_METADATA)
    execution.params(params)
    args = unpackmincdir_cargs(params, execution)
    out = unpackmincdir_outputs(params, execution)
    execution.run(args)
    return out

def unpackmincdir(
    source_directory: str,
    target_directory: str,
    scan_sequence_info: str | None = None,
    functional_sequence: str | None = None,
    functional_subdirectory: str | None = None,
    minc_only: bool = False,
    no_copy: bool = False,
    umask: str | None = None,
    runner: Runner | None = None,
) -> UnpackmincdirOutputs:
    """
    unpackmincdir

    Tool for unpacking directories with MINC files.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        source_directory: Source directory containing the MINC files.
        target_directory: Target directory to unpack the contents to.
        scan_sequence_info: Scan sequence directives file, e.g.,\
            freesurfer_alpha/scanseq.info.
        functional_sequence: Use seqname for functionals (example:\
            ep2d_fid_ts_20b2604).
        functional_subdirectory: Functional subdirectory, e.g., bold.
        minc_only: Do not unpack into bshorts.
        no_copy: Create directories but do not copy/convert data.
        umask: Unix file permission mask (default: 22).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unpackmincdir_params(
        source_directory=source_directory,
        target_directory=target_directory,
        scan_sequence_info=scan_sequence_info,
        functional_sequence=functional_sequence,
        functional_subdirectory=functional_subdirectory,
        minc_only=minc_only,
        no_copy=no_copy,
        umask=umask,
    )
    return unpackmincdir_execute(params, runner)

__all__ = [
    "Unpackmincdir",
    "UnpackmincdirOutputs",
    "UNPACKMINCDIR_METADATA",
    "unpackmincdir_cargs",
    "unpackmincdir_outputs",
    "unpackmincdir_params",
    "unpackmincdir_execute",
    "unpackmincdir_validate",
    "unpackmincdir",
]