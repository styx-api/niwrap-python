# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRIS_SKELETONIZE_METADATA = Metadata(
    id="mris_skeletonize",
    name="mris_skeletonize",
    package="freesurfer",
)

MrisSkeletonize = typing.TypedDict(
    "MrisSkeletonize",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mris_skeletonize"]],
        "surface": str,
        "surfvals": str,
        "mask": str,
        "k1": typing.NotRequired[bool],
        "curv_nonmaxsup": typing.NotRequired[bool],
        "gyrus": typing.NotRequired[bool],
        "sulcus": typing.NotRequired[bool],
        "outdir": typing.NotRequired[str],
        "sphere": typing.NotRequired[str],
        "pointset": typing.NotRequired[str],
        "label": typing.NotRequired[str],
        "nbrsize": typing.NotRequired[float],
        "threshold": typing.NotRequired[float],
        "cluster": typing.NotRequired[float],
        "fwhm": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MrisSkeletonizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    skeleton_output: OutputPathType | None
    """Output skeletonized mask."""
    pointset_output: OutputPathType | None
    """Output point set of the skeleton."""
    skeleton_label: OutputPathType | None
    """Label file for the skeletonized output."""

def mris_skeletonize_params(
    surface: str,
    surfvals: str,
    mask: str,
    k1: bool = False,
    curv_nonmaxsup: bool = False,
    gyrus: bool = False,
    sulcus: bool = False,
    outdir: str | None = None,
    sphere: str | None = None,
    pointset: str | None = None,
    label: str | None = None,
    nbrsize: float | None = None,
    threshold: float | None = None,
    cluster: float | None = None,
    fwhm: float | None = None,
) -> MrisSkeletonize:
    """
    Build parameters.

    Args:
        surface: Path to the surface file.
        surfvals: Pass input explicitly rather than computing it.
        mask: Final skeletonized mask file.
        k1: Use k1 from surface (not with --curv-nonmaxsup).
        curv_nonmaxsup: Use curvature H computed from surface with non-max\
            suppression (not with --k1).
        gyrus: Skeletonize the crowns of the gyri.
        sulcus: Skeletonize the fundi of the sulci.
        outdir: Directory where all outputs will be saved.
        sphere: Sphere path, only needed for nonmax suppression.
        pointset: Point set of the skeleton (PointSet.json).
        label: Surface label of the skeleton (label file path).
        nbrsize: Neighborhood size for 2nd FF (default is 2).
        threshold: Used to create initial mask that will be skeletonized (typically\
            about 0.3).
        cluster: Cluster the thresholded input and keep the largest nkeep clusters.
        fwhm: Smooth surface values by this FWHM.

    Returns:
        Parameter dictionary.
    """
    params: MrisSkeletonize = {
        "@type": "freesurfer/mris_skeletonize",
        "surface": surface,
        "surfvals": surfvals,
        "mask": mask,
        "k1": k1,
        "curv_nonmaxsup": curv_nonmaxsup,
        "gyrus": gyrus,
        "sulcus": sulcus,
    }
    if outdir is not None:
        params["outdir"] = outdir
    if sphere is not None:
        params["sphere"] = sphere
    if pointset is not None:
        params["pointset"] = pointset
    if label is not None:
        params["label"] = label
    if nbrsize is not None:
        params["nbrsize"] = nbrsize
    if threshold is not None:
        params["threshold"] = threshold
    if cluster is not None:
        params["cluster"] = cluster
    if fwhm is not None:
        params["fwhm"] = fwhm
    return params

def mris_skeletonize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MrisSkeletonize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("surfvals", None) is None:
        raise StyxValidationError("`surfvals` must not be None")
    if not isinstance(params["surfvals"], str):
        raise StyxValidationError(f'`surfvals` has the wrong type: Received `{type(params["surfvals"])}` expected `str`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], str):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `str`')
    if params.get("k1", None) is not None:
        if not isinstance(params["k1"], bool):
            raise StyxValidationError(f'`k1` has the wrong type: Received `{type(params["k1"])}` expected `bool`')
    if params.get("curv_nonmaxsup", None) is not None:
        if not isinstance(params["curv_nonmaxsup"], bool):
            raise StyxValidationError(f'`curv_nonmaxsup` has the wrong type: Received `{type(params["curv_nonmaxsup"])}` expected `bool`')
    if params.get("gyrus", None) is not None:
        if not isinstance(params["gyrus"], bool):
            raise StyxValidationError(f'`gyrus` has the wrong type: Received `{type(params["gyrus"])}` expected `bool`')
    if params.get("sulcus", None) is not None:
        if not isinstance(params["sulcus"], bool):
            raise StyxValidationError(f'`sulcus` has the wrong type: Received `{type(params["sulcus"])}` expected `bool`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("sphere", None) is not None:
        if not isinstance(params["sphere"], str):
            raise StyxValidationError(f'`sphere` has the wrong type: Received `{type(params["sphere"])}` expected `str`')
    if params.get("pointset", None) is not None:
        if not isinstance(params["pointset"], str):
            raise StyxValidationError(f'`pointset` has the wrong type: Received `{type(params["pointset"])}` expected `str`')
    if params.get("label", None) is not None:
        if not isinstance(params["label"], str):
            raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `str`')
    if params.get("nbrsize", None) is not None:
        if not isinstance(params["nbrsize"], (float, int)):
            raise StyxValidationError(f'`nbrsize` has the wrong type: Received `{type(params["nbrsize"])}` expected `float`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("cluster", None) is not None:
        if not isinstance(params["cluster"], (float, int)):
            raise StyxValidationError(f'`cluster` has the wrong type: Received `{type(params["cluster"])}` expected `float`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')

def mris_skeletonize_cargs(params: MrisSkeletonize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mris_skeletonize")
    cargs.append("--surf")
    cargs.append(params["surface"])
    cargs.append("--surfvals")
    cargs.append(params["surfvals"])
    cargs.append("--mask")
    cargs.append(params["mask"])
    if params.get("k1"):
        cargs.append("--k1")
    if params.get("curv_nonmaxsup"):
        cargs.append("--curv-nonmaxsup")
    if params.get("gyrus"):
        cargs.append("--gyrus")
    if params.get("sulcus"):
        cargs.append("--sulcus")
    v_0 = params.get("outdir")
    if v_0 is not None:
        cargs.append("--outdir")
        cargs.append(v_0)
    v_1 = params.get("sphere")
    if v_1 is not None:
        cargs.append("--sphere")
        cargs.append(v_1)
    v_2 = params.get("pointset")
    if v_2 is not None:
        cargs.append("--ps")
        cargs.append(v_2)
    v_3 = params.get("label")
    if v_3 is not None:
        cargs.append("--label")
        cargs.append(v_3)
    v_4 = params.get("nbrsize")
    if v_4 is not None:
        cargs.append("--nbrsize")
        cargs.append(str(v_4))
    v_5 = params.get("threshold")
    if v_5 is not None:
        cargs.append("--threshold")
        cargs.append(str(v_5))
    v_6 = params.get("cluster")
    if v_6 is not None:
        cargs.append("--cluster")
        cargs.append(str(v_6))
    v_7 = params.get("fwhm")
    if v_7 is not None:
        cargs.append("--fwhm")
        cargs.append(str(v_7))
    return cargs

def mris_skeletonize_outputs(params: MrisSkeletonize, execution: Execution) -> MrisSkeletonizeOutputs:
    """Build the MrisSkeletonizeOutputs object for this tool."""
    skeleton_output_v: OutputPathType | None = None
    pointset_output_v: OutputPathType | None = None
    skeleton_label_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outdir")
    if __v0 is not None:
        skeleton_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/skeleton.mgz')
    __v1 = params.get("outdir")
    if __v1 is not None:
        pointset_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/PointSet.json')
    __v2 = params.get("outdir")
    if __v2 is not None:
        skeleton_label_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}/skeleton_label.mgz')
    return MrisSkeletonizeOutputs(
        root=root_v,
        skeleton_output=skeleton_output_v,
        pointset_output=pointset_output_v,
        skeleton_label=skeleton_label_v,
    )

def mris_skeletonize_execute(params: MrisSkeletonize, runner: Runner | None = None) -> MrisSkeletonizeOutputs:
    """
    mris_skeletonize

    Computes the skeleton of gyri (ie, the crowns) or sulci (ie, the fundi).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mris_skeletonize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRIS_SKELETONIZE_METADATA)
    execution.params(params)
    args = mris_skeletonize_cargs(params, execution)
    out = mris_skeletonize_outputs(params, execution)
    execution.run(args)
    return out

def mris_skeletonize(
    surface: str,
    surfvals: str,
    mask: str,
    k1: bool = False,
    curv_nonmaxsup: bool = False,
    gyrus: bool = False,
    sulcus: bool = False,
    outdir: str | None = None,
    sphere: str | None = None,
    pointset: str | None = None,
    label: str | None = None,
    nbrsize: float | None = None,
    threshold: float | None = None,
    cluster: float | None = None,
    fwhm: float | None = None,
    runner: Runner | None = None,
) -> MrisSkeletonizeOutputs:
    """
    mris_skeletonize

    Computes the skeleton of gyri (ie, the crowns) or sulci (ie, the fundi).

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        surface: Path to the surface file.
        surfvals: Pass input explicitly rather than computing it.
        mask: Final skeletonized mask file.
        k1: Use k1 from surface (not with --curv-nonmaxsup).
        curv_nonmaxsup: Use curvature H computed from surface with non-max\
            suppression (not with --k1).
        gyrus: Skeletonize the crowns of the gyri.
        sulcus: Skeletonize the fundi of the sulci.
        outdir: Directory where all outputs will be saved.
        sphere: Sphere path, only needed for nonmax suppression.
        pointset: Point set of the skeleton (PointSet.json).
        label: Surface label of the skeleton (label file path).
        nbrsize: Neighborhood size for 2nd FF (default is 2).
        threshold: Used to create initial mask that will be skeletonized (typically\
            about 0.3).
        cluster: Cluster the thresholded input and keep the largest nkeep clusters.
        fwhm: Smooth surface values by this FWHM.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mris_skeletonize_params(
        surface=surface,
        surfvals=surfvals,
        mask=mask,
        k1=k1,
        curv_nonmaxsup=curv_nonmaxsup,
        gyrus=gyrus,
        sulcus=sulcus,
        outdir=outdir,
        sphere=sphere,
        pointset=pointset,
        label=label,
        nbrsize=nbrsize,
        threshold=threshold,
        cluster=cluster,
        fwhm=fwhm,
    )
    return mris_skeletonize_execute(params, runner)

__all__ = [
    "MrisSkeletonize",
    "MrisSkeletonizeOutputs",
    "MRIS_SKELETONIZE_METADATA",
    "mris_skeletonize_cargs",
    "mris_skeletonize_outputs",
    "mris_skeletonize_params",
    "mris_skeletonize_execute",
    "mris_skeletonize_validate",
    "mris_skeletonize",
]