# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_AND_METADATA = Metadata(
    id="mri_and",
    name="mri_and",
    package="freesurfer",
)

MriAnd = typing.TypedDict(
    "MriAnd",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_and"]],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class MriAndOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_and_params(
    input_files: list[InputPathType],
) -> MriAnd:
    """
    Build parameters.

    Args:
        input_files: Input volume files

    Returns:
        Parameter dictionary.
    """
    params: MriAnd = {
        "@type": "freesurfer/mri_and",
        "input_files": input_files,
    }
    return params

def mri_and_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriAnd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def mri_and_cargs(params: MriAnd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_and")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def mri_and_outputs(params: MriAnd, execution: Execution) -> MriAndOutputs:
    """Build the MriAndOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriAndOutputs(
        root=root_v,
    )

def mri_and_execute(params: MriAnd, runner: Runner | None = None) -> MriAndOutputs:
    """
    mri_and

    Performs a logical voxel-wise AND on a series of volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_and_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_AND_METADATA)
    execution.params(params)
    args = mri_and_cargs(params, execution)
    out = mri_and_outputs(params, execution)
    execution.run(args)
    return out

def mri_and(
    input_files: list[InputPathType],
    runner: Runner | None = None,
) -> MriAndOutputs:
    """
    mri_and

    Performs a logical voxel-wise AND on a series of volumes

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_files: Input volume files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_and_params(
        input_files=input_files,
    )
    return mri_and_execute(params, runner)

__all__ = [
    "MriAnd",
    "MriAndOutputs",
    "MRI_AND_METADATA",
    "mri_and_cargs",
    "mri_and_outputs",
    "mri_and_params",
    "mri_and_execute",
    "mri_and_validate",
    "mri_and",
]