# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CHECK_RECONS_SH_METADATA = Metadata(
    id="check_recons.sh",
    name="check_recons.sh",
    package="freesurfer",
)

CheckReconsSh = typing.TypedDict(
    "CheckReconsSh",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/check_recons.sh"]],
        "subject_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CheckReconsShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def check_recons_sh_params(
    subject_directory: str | None = None,
) -> CheckReconsSh:
    """
    Build parameters.

    Args:
        subject_directory: Directory where subjects are being processed. If not\
            specified, uses SUBJECTS_DIR.

    Returns:
        Parameter dictionary.
    """
    params: CheckReconsSh = {
        "@type": "freesurfer/check_recons.sh",
    }
    if subject_directory is not None:
        params["subject_directory"] = subject_directory
    return params

def check_recons_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CheckReconsSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_directory", None) is not None:
        if not isinstance(params["subject_directory"], str):
            raise StyxValidationError(f'`subject_directory` has the wrong type: Received `{type(params["subject_directory"])}` expected `str`')

def check_recons_sh_cargs(params: CheckReconsSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("check_recons.sh")
    v_0 = params.get("subject_directory")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def check_recons_sh_outputs(params: CheckReconsSh, execution: Execution) -> CheckReconsShOutputs:
    """Build the CheckReconsShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CheckReconsShOutputs(
        root=root_v,
    )

def check_recons_sh_execute(params: CheckReconsSh, runner: Runner | None = None) -> CheckReconsShOutputs:
    """
    check_recons.sh

    Checks the status of subjects being processed by recon-all in the SUBJECTS_DIR or a specified directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    check_recons_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CHECK_RECONS_SH_METADATA)
    execution.params(params)
    args = check_recons_sh_cargs(params, execution)
    out = check_recons_sh_outputs(params, execution)
    execution.run(args)
    return out

def check_recons_sh(
    subject_directory: str | None = None,
    runner: Runner | None = None,
) -> CheckReconsShOutputs:
    """
    check_recons.sh

    Checks the status of subjects being processed by recon-all in the SUBJECTS_DIR or a specified directory.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        subject_directory: Directory where subjects are being processed. If not\
            specified, uses SUBJECTS_DIR.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = check_recons_sh_params(
        subject_directory=subject_directory,
    )
    return check_recons_sh_execute(params, runner)

__all__ = [
    "CheckReconsSh",
    "CheckReconsShOutputs",
    "CHECK_RECONS_SH_METADATA",
    "check_recons_sh_cargs",
    "check_recons_sh_outputs",
    "check_recons_sh_params",
    "check_recons_sh_execute",
    "check_recons_sh_validate",
    "check_recons_sh",
]