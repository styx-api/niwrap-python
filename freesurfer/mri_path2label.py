# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRI_PATH2LABEL_METADATA = Metadata(
    id="mri_path2label",
    name="mri_path2label",
    package="freesurfer",
)

MriPath2label = typing.TypedDict(
    "MriPath2label",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/mri_path2label"]],
        "input_file": str,
        "output_file": str,
        "single": typing.NotRequired[bool],
        "path_to_label": typing.NotRequired[bool],
        "label_to_path": typing.NotRequired[bool],
        "connect": typing.NotRequired[list[str]],
        "fill": typing.NotRequired[list[str]],
        "confillx": typing.NotRequired[list[str]],
        "confill": typing.NotRequired[list[str]],
        "source_file": typing.NotRequired[str],
        "dest_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MriPath2labelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mri_path2label_params(
    input_file: str,
    output_file: str,
    single: bool = False,
    path_to_label: bool = False,
    label_to_path: bool = False,
    connect: list[str] | None = None,
    fill: list[str] | None = None,
    confillx: list[str] | None = None,
    confill: list[str] | None = None,
    source_file: str | None = None,
    dest_file: str | None = None,
) -> MriPath2label:
    """
    Build parameters.

    Args:
        input_file: Input file, either a path or label file.
        output_file: Output file, either a path or label file.
        single: Only convert a single path, and do not use sentinel values.
        path_to_label: Treat input as a path and output a label.
        label_to_path: Treat input as a label and output a path.
        connect: Connect path; input and output must be paths; requires subject and\
            hemi.
        fill: Fill already closed, connected path; input must be a path, output\
            must be a label; requires subject, hemi, and seedvtx.
        confillx: Connect and fill path; input must be a path, output must be a\
            label; requires surface_fname and seedvtx.
        confill: Connect and fill path; input must be a path, output must be a\
            label; requires subject, hemi, and seedvtx.
        source_file: The path file, if path2label.
        dest_file: The label file, if path2label.

    Returns:
        Parameter dictionary.
    """
    params: MriPath2label = {
        "@type": "freesurfer/mri_path2label",
        "input_file": input_file,
        "output_file": output_file,
        "single": single,
        "path_to_label": path_to_label,
        "label_to_path": label_to_path,
    }
    if connect is not None:
        params["connect"] = connect
    if fill is not None:
        params["fill"] = fill
    if confillx is not None:
        params["confillx"] = confillx
    if confill is not None:
        params["confill"] = confill
    if source_file is not None:
        params["source_file"] = source_file
    if dest_file is not None:
        params["dest_file"] = dest_file
    return params

def mri_path2label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MriPath2label."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], str):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("single", None) is not None:
        if not isinstance(params["single"], bool):
            raise StyxValidationError(f'`single` has the wrong type: Received `{type(params["single"])}` expected `bool`')
    if params.get("path_to_label", None) is not None:
        if not isinstance(params["path_to_label"], bool):
            raise StyxValidationError(f'`path_to_label` has the wrong type: Received `{type(params["path_to_label"])}` expected `bool`')
    if params.get("label_to_path", None) is not None:
        if not isinstance(params["label_to_path"], bool):
            raise StyxValidationError(f'`label_to_path` has the wrong type: Received `{type(params["label_to_path"])}` expected `bool`')
    if params.get("connect", None) is not None:
        if not isinstance(params["connect"], list):
            raise StyxValidationError(f'`connect` has the wrong type: Received `{type(params["connect"])}` expected `list[str]`')
        for e in params["connect"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`connect` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], list):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `list[str]`')
        for e_2 in params["fill"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`fill` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("confillx", None) is not None:
        if not isinstance(params["confillx"], list):
            raise StyxValidationError(f'`confillx` has the wrong type: Received `{type(params["confillx"])}` expected `list[str]`')
        for e_3 in params["confillx"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`confillx` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("confill", None) is not None:
        if not isinstance(params["confill"], list):
            raise StyxValidationError(f'`confill` has the wrong type: Received `{type(params["confill"])}` expected `list[str]`')
        for e_4 in params["confill"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`confill` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("source_file", None) is not None:
        if not isinstance(params["source_file"], str):
            raise StyxValidationError(f'`source_file` has the wrong type: Received `{type(params["source_file"])}` expected `str`')
    if params.get("dest_file", None) is not None:
        if not isinstance(params["dest_file"], str):
            raise StyxValidationError(f'`dest_file` has the wrong type: Received `{type(params["dest_file"])}` expected `str`')

def mri_path2label_cargs(params: MriPath2label, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mri_path2label")
    cargs.append(params["input_file"])
    cargs.append(params["output_file"])
    if params.get("single"):
        cargs.append("--single")
    if params.get("path_to_label"):
        cargs.append("--path2label")
    if params.get("label_to_path"):
        cargs.append("--label2path")
    v_0 = params.get("connect")
    if v_0 is not None:
        cargs.append("--connect")
        for item0 in v_0:
            cargs.append(item0)
    v_1 = params.get("fill")
    if v_1 is not None:
        cargs.append("--fill")
        for item1 in v_1:
            cargs.append(item1)
    v_2 = params.get("confillx")
    if v_2 is not None:
        cargs.append("--confillx")
        for item2 in v_2:
            cargs.append(item2)
    v_3 = params.get("confill")
    if v_3 is not None:
        cargs.append("--confill")
        for item3 in v_3:
            cargs.append(item3)
    v_4 = params.get("source_file")
    if v_4 is not None:
        cargs.append("--i")
        cargs.append(v_4)
    v_5 = params.get("dest_file")
    if v_5 is not None:
        cargs.append("--o")
        cargs.append(v_5)
    return cargs

def mri_path2label_outputs(params: MriPath2label, execution: Execution) -> MriPath2labelOutputs:
    """Build the MriPath2labelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MriPath2labelOutputs(
        root=root_v,
    )

def mri_path2label_execute(params: MriPath2label, runner: Runner | None = None) -> MriPath2labelOutputs:
    """
    mri_path2label

    Converts a path file to a label or a label file to a path file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mri_path2label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRI_PATH2LABEL_METADATA)
    execution.params(params)
    args = mri_path2label_cargs(params, execution)
    out = mri_path2label_outputs(params, execution)
    execution.run(args)
    return out

def mri_path2label(
    input_file: str,
    output_file: str,
    single: bool = False,
    path_to_label: bool = False,
    label_to_path: bool = False,
    connect: list[str] | None = None,
    fill: list[str] | None = None,
    confillx: list[str] | None = None,
    confill: list[str] | None = None,
    source_file: str | None = None,
    dest_file: str | None = None,
    runner: Runner | None = None,
) -> MriPath2labelOutputs:
    """
    mri_path2label

    Converts a path file to a label or a label file to a path file.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        input_file: Input file, either a path or label file.
        output_file: Output file, either a path or label file.
        single: Only convert a single path, and do not use sentinel values.
        path_to_label: Treat input as a path and output a label.
        label_to_path: Treat input as a label and output a path.
        connect: Connect path; input and output must be paths; requires subject and\
            hemi.
        fill: Fill already closed, connected path; input must be a path, output\
            must be a label; requires subject, hemi, and seedvtx.
        confillx: Connect and fill path; input must be a path, output must be a\
            label; requires surface_fname and seedvtx.
        confill: Connect and fill path; input must be a path, output must be a\
            label; requires subject, hemi, and seedvtx.
        source_file: The path file, if path2label.
        dest_file: The label file, if path2label.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mri_path2label_params(
        input_file=input_file,
        output_file=output_file,
        single=single,
        path_to_label=path_to_label,
        label_to_path=label_to_path,
        connect=connect,
        fill=fill,
        confillx=confillx,
        confill=confill,
        source_file=source_file,
        dest_file=dest_file,
    )
    return mri_path2label_execute(params, runner)

__all__ = [
    "MriPath2label",
    "MriPath2labelOutputs",
    "MRI_PATH2LABEL_METADATA",
    "mri_path2label_cargs",
    "mri_path2label_outputs",
    "mri_path2label_params",
    "mri_path2label_execute",
    "mri_path2label_validate",
    "mri_path2label",
]