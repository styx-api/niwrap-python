# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LONG_STATS_TPS_METADATA = Metadata(
    id="long_stats_tps",
    name="long_stats_tps",
    package="freesurfer",
)

LongStatsTps = typing.TypedDict(
    "LongStatsTps",
    {
        "@type": typing.NotRequired[typing.Literal["freesurfer/long_stats_tps"]],
        "qdec_table": InputPathType,
        "stats_file": str,
        "measure": str,
        "subjects_dir": str,
        "time_point": float,
        "output_file": str,
        "qcolumn": typing.NotRequired[str],
        "cross_sectional": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LongStatsTpsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stacked_results: OutputPathType
    """File containing stacked results for individual time points."""

def long_stats_tps_params(
    qdec_table: InputPathType,
    stats_file: str,
    measure: str,
    subjects_dir: str,
    time_point: float,
    output_file: str,
    qcolumn: str | None = None,
    cross_sectional: bool = False,
) -> LongStatsTps:
    """
    Build parameters.

    Args:
        qdec_table: qdec.table.dat file with first columns: fsid fsid-base
        stats_file: Stats file without path: e.g. aseg.stats or lh.aparc.stats
        measure: Stats measure, e.g. volume, thickness, mean, std
        subjects_dir: Full path to FreeSurfer subjects directory
        time_point: Time point number
        output_file: File name of output
        qcolumn: Select a column from the qdec table itself (then --stats, --meas\
            and --sd are not necessary)
        cross_sectional: Use cross sectional results (for testing only)

    Returns:
        Parameter dictionary.
    """
    params: LongStatsTps = {
        "@type": "freesurfer/long_stats_tps",
        "qdec_table": qdec_table,
        "stats_file": stats_file,
        "measure": measure,
        "subjects_dir": subjects_dir,
        "time_point": time_point,
        "output_file": output_file,
        "cross_sectional": cross_sectional,
    }
    if qcolumn is not None:
        params["qcolumn"] = qcolumn
    return params

def long_stats_tps_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LongStatsTps."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("qdec_table", None) is None:
        raise StyxValidationError("`qdec_table` must not be None")
    if not isinstance(params["qdec_table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`qdec_table` has the wrong type: Received `{type(params["qdec_table"])}` expected `InputPathType`')
    if params.get("stats_file", None) is None:
        raise StyxValidationError("`stats_file` must not be None")
    if not isinstance(params["stats_file"], str):
        raise StyxValidationError(f'`stats_file` has the wrong type: Received `{type(params["stats_file"])}` expected `str`')
    if params.get("measure", None) is None:
        raise StyxValidationError("`measure` must not be None")
    if not isinstance(params["measure"], str):
        raise StyxValidationError(f'`measure` has the wrong type: Received `{type(params["measure"])}` expected `str`')
    if params.get("subjects_dir", None) is None:
        raise StyxValidationError("`subjects_dir` must not be None")
    if not isinstance(params["subjects_dir"], str):
        raise StyxValidationError(f'`subjects_dir` has the wrong type: Received `{type(params["subjects_dir"])}` expected `str`')
    if params.get("time_point", None) is None:
        raise StyxValidationError("`time_point` must not be None")
    if not isinstance(params["time_point"], (float, int)):
        raise StyxValidationError(f'`time_point` has the wrong type: Received `{type(params["time_point"])}` expected `float`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("qcolumn", None) is not None:
        if not isinstance(params["qcolumn"], str):
            raise StyxValidationError(f'`qcolumn` has the wrong type: Received `{type(params["qcolumn"])}` expected `str`')
    if params.get("cross_sectional", None) is not None:
        if not isinstance(params["cross_sectional"], bool):
            raise StyxValidationError(f'`cross_sectional` has the wrong type: Received `{type(params["cross_sectional"])}` expected `bool`')

def long_stats_tps_cargs(params: LongStatsTps, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("long_stats_tps")
    cargs.append("--qdec")
    cargs.append(execution.input_file(params["qdec_table"]))
    cargs.append("--stats")
    cargs.append(params["stats_file"])
    cargs.append("--meas")
    cargs.append(params["measure"])
    cargs.append("--sd")
    cargs.append(params["subjects_dir"])
    cargs.append("--tp")
    cargs.append(str(params["time_point"]))
    cargs.append("--out")
    cargs.append(params["output_file"])
    v_0 = params.get("qcolumn")
    if v_0 is not None:
        cargs.append("--qcol")
        cargs.append(v_0)
    if params.get("cross_sectional"):
        cargs.append("--cross")
    return cargs

def long_stats_tps_outputs(params: LongStatsTps, execution: Execution) -> LongStatsTpsOutputs:
    """Build the LongStatsTpsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stacked_results_v: OutputPathType = execution.output_file(params["output_file"])
    return LongStatsTpsOutputs(
        root=root_v,
        stacked_results=stacked_results_v,
    )

def long_stats_tps_execute(params: LongStatsTps, runner: Runner | None = None) -> LongStatsTpsOutputs:
    """
    long_stats_tps

    Stack results for individual time points based on longitudinal qdec table.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    long_stats_tps_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LONG_STATS_TPS_METADATA)
    execution.params(params)
    args = long_stats_tps_cargs(params, execution)
    out = long_stats_tps_outputs(params, execution)
    execution.run(args)
    return out

def long_stats_tps(
    qdec_table: InputPathType,
    stats_file: str,
    measure: str,
    subjects_dir: str,
    time_point: float,
    output_file: str,
    qcolumn: str | None = None,
    cross_sectional: bool = False,
    runner: Runner | None = None,
) -> LongStatsTpsOutputs:
    """
    long_stats_tps

    Stack results for individual time points based on longitudinal qdec table.

    Author: FreeSurfer Developers

    URL: https://github.com/freesurfer/freesurfer

    Args:
        qdec_table: qdec.table.dat file with first columns: fsid fsid-base
        stats_file: Stats file without path: e.g. aseg.stats or lh.aparc.stats
        measure: Stats measure, e.g. volume, thickness, mean, std
        subjects_dir: Full path to FreeSurfer subjects directory
        time_point: Time point number
        output_file: File name of output
        qcolumn: Select a column from the qdec table itself (then --stats, --meas\
            and --sd are not necessary)
        cross_sectional: Use cross sectional results (for testing only)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = long_stats_tps_params(
        qdec_table=qdec_table,
        stats_file=stats_file,
        measure=measure,
        subjects_dir=subjects_dir,
        time_point=time_point,
        output_file=output_file,
        qcolumn=qcolumn,
        cross_sectional=cross_sectional,
    )
    return long_stats_tps_execute(params, runner)

__all__ = [
    "LongStatsTps",
    "LongStatsTpsOutputs",
    "LONG_STATS_TPS_METADATA",
    "long_stats_tps_cargs",
    "long_stats_tps_outputs",
    "long_stats_tps_params",
    "long_stats_tps_execute",
    "long_stats_tps_validate",
    "long_stats_tps",
]