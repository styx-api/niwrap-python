# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_ALADIN_METADATA = Metadata(
    id="reg_aladin",
    name="reg_aladin",
    package="niftyreg",
)

RegAladin = typing.TypedDict(
    "RegAladin",
    {
        "@type": typing.NotRequired[typing.Literal["niftyreg/reg_aladin"]],
        "reference_image": InputPathType,
        "floating_image": InputPathType,
        "symmetric": typing.NotRequired[bool],
        "output_affine": typing.NotRequired[str],
        "rigid_only": typing.NotRequired[bool],
        "direct_affine": typing.NotRequired[bool],
        "smooth_ref": typing.NotRequired[float],
        "smooth_float": typing.NotRequired[float],
        "num_levels": typing.NotRequired[float],
        "first_levels": typing.NotRequired[float],
        "use_nifti_origin": typing.NotRequired[bool],
        "percent_block": typing.NotRequired[float],
        "percent_inlier": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class RegAladinOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_affine_file: OutputPathType
    """File containing the output affine transformation"""

def reg_aladin_params(
    reference_image: InputPathType,
    floating_image: InputPathType,
    symmetric: bool = False,
    output_affine: str | None = None,
    rigid_only: bool = False,
    direct_affine: bool = False,
    smooth_ref: float | None = None,
    smooth_float: float | None = None,
    num_levels: float | None = None,
    first_levels: float | None = None,
    use_nifti_origin: bool = False,
    percent_block: float | None = None,
    percent_inlier: float | None = None,
) -> RegAladin:
    """
    Build parameters.

    Args:
        reference_image: Filename of the reference (target) image
        floating_image: Filename of the floating (source) image
        symmetric: Uses symmetric version of the algorithm
        output_affine: Filename which contains the output affine transformation
        rigid_only: To perform a rigid registration only
        direct_affine: Directly optimize 12 DoF affine
        smooth_ref: Smooth the reference image using the specified sigma (mm)
        smooth_float: Smooth the floating image using the specified sigma (mm)
        num_levels: Number of levels to perform
        first_levels: Only perform the first levels
        use_nifti_origin: Use the NIFTI header origins to initialize the\
            translation
        percent_block: Percentage of block to use
        percent_inlier: Percentage of inlier for the LTS

    Returns:
        Parameter dictionary.
    """
    params: RegAladin = {
        "@type": "niftyreg/reg_aladin",
        "reference_image": reference_image,
        "floating_image": floating_image,
        "symmetric": symmetric,
        "rigid_only": rigid_only,
        "direct_affine": direct_affine,
        "use_nifti_origin": use_nifti_origin,
    }
    if output_affine is not None:
        params["output_affine"] = output_affine
    if smooth_ref is not None:
        params["smooth_ref"] = smooth_ref
    if smooth_float is not None:
        params["smooth_float"] = smooth_float
    if num_levels is not None:
        params["num_levels"] = num_levels
    if first_levels is not None:
        params["first_levels"] = first_levels
    if percent_block is not None:
        params["percent_block"] = percent_block
    if percent_inlier is not None:
        params["percent_inlier"] = percent_inlier
    return params

def reg_aladin_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegAladin."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("floating_image", None) is None:
        raise StyxValidationError("`floating_image` must not be None")
    if not isinstance(params["floating_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`floating_image` has the wrong type: Received `{type(params["floating_image"])}` expected `InputPathType`')
    if params.get("symmetric", None) is not None:
        if not isinstance(params["symmetric"], bool):
            raise StyxValidationError(f'`symmetric` has the wrong type: Received `{type(params["symmetric"])}` expected `bool`')
    if params.get("output_affine", None) is not None:
        if not isinstance(params["output_affine"], str):
            raise StyxValidationError(f'`output_affine` has the wrong type: Received `{type(params["output_affine"])}` expected `str`')
    if params.get("rigid_only", None) is not None:
        if not isinstance(params["rigid_only"], bool):
            raise StyxValidationError(f'`rigid_only` has the wrong type: Received `{type(params["rigid_only"])}` expected `bool`')
    if params.get("direct_affine", None) is not None:
        if not isinstance(params["direct_affine"], bool):
            raise StyxValidationError(f'`direct_affine` has the wrong type: Received `{type(params["direct_affine"])}` expected `bool`')
    if params.get("smooth_ref", None) is not None:
        if not isinstance(params["smooth_ref"], (float, int)):
            raise StyxValidationError(f'`smooth_ref` has the wrong type: Received `{type(params["smooth_ref"])}` expected `float`')
    if params.get("smooth_float", None) is not None:
        if not isinstance(params["smooth_float"], (float, int)):
            raise StyxValidationError(f'`smooth_float` has the wrong type: Received `{type(params["smooth_float"])}` expected `float`')
    if params.get("num_levels", None) is not None:
        if not isinstance(params["num_levels"], (float, int)):
            raise StyxValidationError(f'`num_levels` has the wrong type: Received `{type(params["num_levels"])}` expected `float`')
    if params.get("first_levels", None) is not None:
        if not isinstance(params["first_levels"], (float, int)):
            raise StyxValidationError(f'`first_levels` has the wrong type: Received `{type(params["first_levels"])}` expected `float`')
    if params.get("use_nifti_origin", None) is not None:
        if not isinstance(params["use_nifti_origin"], bool):
            raise StyxValidationError(f'`use_nifti_origin` has the wrong type: Received `{type(params["use_nifti_origin"])}` expected `bool`')
    if params.get("percent_block", None) is not None:
        if not isinstance(params["percent_block"], (float, int)):
            raise StyxValidationError(f'`percent_block` has the wrong type: Received `{type(params["percent_block"])}` expected `float`')
    if params.get("percent_inlier", None) is not None:
        if not isinstance(params["percent_inlier"], (float, int)):
            raise StyxValidationError(f'`percent_inlier` has the wrong type: Received `{type(params["percent_inlier"])}` expected `float`')

def reg_aladin_cargs(params: RegAladin, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg_aladin")
    cargs.append("-ref")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("-flo")
    cargs.append(execution.input_file(params["floating_image"]))
    if params.get("symmetric"):
        cargs.append("-sym")
    v_0 = params.get("output_affine")
    if v_0 is not None:
        cargs.append("-aff")
        cargs.append(v_0)
    if params.get("rigid_only"):
        cargs.append("-rigOnly")
    if params.get("direct_affine"):
        cargs.append("-affDirect")
    v_1 = params.get("smooth_ref")
    if v_1 is not None:
        cargs.append("-smooR")
        cargs.append(str(v_1))
    v_2 = params.get("smooth_float")
    if v_2 is not None:
        cargs.append("-smooF")
        cargs.append(str(v_2))
    v_3 = params.get("num_levels")
    if v_3 is not None:
        cargs.append("-ln")
        cargs.append(str(v_3))
    v_4 = params.get("first_levels")
    if v_4 is not None:
        cargs.append("-lp")
        cargs.append(str(v_4))
    if params.get("use_nifti_origin"):
        cargs.append("-nac")
    v_5 = params.get("percent_block")
    if v_5 is not None:
        cargs.append("-%v")
        cargs.append(str(v_5))
    v_6 = params.get("percent_inlier")
    if v_6 is not None:
        cargs.append("-%i")
        cargs.append(str(v_6))
    return cargs

def reg_aladin_outputs(params: RegAladin, execution: Execution) -> RegAladinOutputs:
    """Build the RegAladinOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_affine_file_v: OutputPathType = execution.output_file("outputAffine.txt")
    return RegAladinOutputs(
        root=root_v,
        output_affine_file=output_affine_file_v,
    )

def reg_aladin_execute(params: RegAladin, runner: Runner | None = None) -> RegAladinOutputs:
    """
    reg_aladin

    Block Matching algorithm for global registration based on "Reconstructing a 3D structure from serial histological sections", Image and Vision Computing, 2001

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_aladin_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_ALADIN_METADATA)
    execution.params(params)
    args = reg_aladin_cargs(params, execution)
    out = reg_aladin_outputs(params, execution)
    execution.run(args)
    return out

def reg_aladin(
    reference_image: InputPathType,
    floating_image: InputPathType,
    symmetric: bool = False,
    output_affine: str | None = None,
    rigid_only: bool = False,
    direct_affine: bool = False,
    smooth_ref: float | None = None,
    smooth_float: float | None = None,
    num_levels: float | None = None,
    first_levels: float | None = None,
    use_nifti_origin: bool = False,
    percent_block: float | None = None,
    percent_inlier: float | None = None,
    runner: Runner | None = None,
) -> RegAladinOutputs:
    """
    reg_aladin

    Block Matching algorithm for global registration based on "Reconstructing a 3D structure from serial histological sections", Image and Vision Computing, 2001

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        reference_image: Filename of the reference (target) image
        floating_image: Filename of the floating (source) image
        symmetric: Uses symmetric version of the algorithm
        output_affine: Filename which contains the output affine transformation
        rigid_only: To perform a rigid registration only
        direct_affine: Directly optimize 12 DoF affine
        smooth_ref: Smooth the reference image using the specified sigma (mm)
        smooth_float: Smooth the floating image using the specified sigma (mm)
        num_levels: Number of levels to perform
        first_levels: Only perform the first levels
        use_nifti_origin: Use the NIFTI header origins to initialize the\
            translation
        percent_block: Percentage of block to use
        percent_inlier: Percentage of inlier for the LTS
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_aladin_params(
        reference_image=reference_image,
        floating_image=floating_image,
        symmetric=symmetric,
        output_affine=output_affine,
        rigid_only=rigid_only,
        direct_affine=direct_affine,
        smooth_ref=smooth_ref,
        smooth_float=smooth_float,
        num_levels=num_levels,
        first_levels=first_levels,
        use_nifti_origin=use_nifti_origin,
        percent_block=percent_block,
        percent_inlier=percent_inlier,
    )
    return reg_aladin_execute(params, runner)

__all__ = [
    "RegAladin",
    "RegAladinOutputs",
    "REG_ALADIN_METADATA",
    "reg_aladin_cargs",
    "reg_aladin_outputs",
    "reg_aladin_params",
    "reg_aladin_execute",
    "reg_aladin_validate",
    "reg_aladin",
]