# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_TOOLS_METADATA = Metadata(
    id="reg_tools",
    name="reg_tools",
    package="niftyreg",
)

RegTools = typing.TypedDict(
    "RegTools",
    {
        "@type": typing.NotRequired[typing.Literal["niftyreg/reg_tools"]],
        "input_image": InputPathType,
        "output_image": typing.NotRequired[str],
        "add_value_or_image": typing.NotRequired[str],
        "sub_value_or_image": typing.NotRequired[str],
        "mul_value_or_image": typing.NotRequired[str],
        "div_value_or_image": typing.NotRequired[str],
        "smooth_value": typing.NotRequired[float],
        "smooth_gaussian": typing.NotRequired[list[float]],
        "rms_image": typing.NotRequired[InputPathType],
        "binarize": typing.NotRequired[bool],
        "threshold_value": typing.NotRequired[float],
        "nan_mask_image": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class RegToolsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType | None
    """File containing the output image"""

def reg_tools_params(
    input_image: InputPathType,
    output_image: str | None = None,
    add_value_or_image: str | None = None,
    sub_value_or_image: str | None = None,
    mul_value_or_image: str | None = None,
    div_value_or_image: str | None = None,
    smooth_value: float | None = None,
    smooth_gaussian: list[float] | None = None,
    rms_image: InputPathType | None = None,
    binarize: bool = False,
    threshold_value: float | None = None,
    nan_mask_image: InputPathType | None = None,
) -> RegTools:
    """
    Build parameters.

    Args:
        input_image: Filename of the input image
        output_image: Filename of the output image
        add_value_or_image: This image (or value) is added to the input
        sub_value_or_image: This image (or value) is subtracted from the input
        mul_value_or_image: This image (or value) is multiplied with the input
        div_value_or_image: This image (or value) is divided by the input
        smooth_value: The input image is smoothed using a B-spline curve
        smooth_gaussian: The input image is smoothed using a Gaussian kernel
        rms_image: Compute the mean RMS between the input image and this image
        binarize: Binarize the input image (val!=0?val=1:val=0)
        threshold_value: Threshold the input image (val<thr?val=0:val=1)
        nan_mask_image: This image is used to mask the input image. Voxels outside\
            of the mask are set to NaN

    Returns:
        Parameter dictionary.
    """
    params: RegTools = {
        "@type": "niftyreg/reg_tools",
        "input_image": input_image,
        "binarize": binarize,
    }
    if output_image is not None:
        params["output_image"] = output_image
    if add_value_or_image is not None:
        params["add_value_or_image"] = add_value_or_image
    if sub_value_or_image is not None:
        params["sub_value_or_image"] = sub_value_or_image
    if mul_value_or_image is not None:
        params["mul_value_or_image"] = mul_value_or_image
    if div_value_or_image is not None:
        params["div_value_or_image"] = div_value_or_image
    if smooth_value is not None:
        params["smooth_value"] = smooth_value
    if smooth_gaussian is not None:
        params["smooth_gaussian"] = smooth_gaussian
    if rms_image is not None:
        params["rms_image"] = rms_image
    if threshold_value is not None:
        params["threshold_value"] = threshold_value
    if nan_mask_image is not None:
        params["nan_mask_image"] = nan_mask_image
    return params

def reg_tools_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegTools."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("add_value_or_image", None) is not None:
        if not isinstance(params["add_value_or_image"], str):
            raise StyxValidationError(f'`add_value_or_image` has the wrong type: Received `{type(params["add_value_or_image"])}` expected `str`')
    if params.get("sub_value_or_image", None) is not None:
        if not isinstance(params["sub_value_or_image"], str):
            raise StyxValidationError(f'`sub_value_or_image` has the wrong type: Received `{type(params["sub_value_or_image"])}` expected `str`')
    if params.get("mul_value_or_image", None) is not None:
        if not isinstance(params["mul_value_or_image"], str):
            raise StyxValidationError(f'`mul_value_or_image` has the wrong type: Received `{type(params["mul_value_or_image"])}` expected `str`')
    if params.get("div_value_or_image", None) is not None:
        if not isinstance(params["div_value_or_image"], str):
            raise StyxValidationError(f'`div_value_or_image` has the wrong type: Received `{type(params["div_value_or_image"])}` expected `str`')
    if params.get("smooth_value", None) is not None:
        if not isinstance(params["smooth_value"], (float, int)):
            raise StyxValidationError(f'`smooth_value` has the wrong type: Received `{type(params["smooth_value"])}` expected `float`')
    if params.get("smooth_gaussian", None) is not None:
        if not isinstance(params["smooth_gaussian"], list):
            raise StyxValidationError(f'`smooth_gaussian` has the wrong type: Received `{type(params["smooth_gaussian"])}` expected `list[float]`')
        if not (3 <= len(params["smooth_gaussian"]) <= 3):
            raise StyxValidationError("Parameter `smooth_gaussian` must contain between 3 and 3 elements (inclusive)")
        for e in params["smooth_gaussian"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`smooth_gaussian` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("rms_image", None) is not None:
        if not isinstance(params["rms_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`rms_image` has the wrong type: Received `{type(params["rms_image"])}` expected `InputPathType`')
    if params.get("binarize", None) is not None:
        if not isinstance(params["binarize"], bool):
            raise StyxValidationError(f'`binarize` has the wrong type: Received `{type(params["binarize"])}` expected `bool`')
    if params.get("threshold_value", None) is not None:
        if not isinstance(params["threshold_value"], (float, int)):
            raise StyxValidationError(f'`threshold_value` has the wrong type: Received `{type(params["threshold_value"])}` expected `float`')
    if params.get("nan_mask_image", None) is not None:
        if not isinstance(params["nan_mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nan_mask_image` has the wrong type: Received `{type(params["nan_mask_image"])}` expected `InputPathType`')

def reg_tools_cargs(params: RegTools, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg_tools")
    cargs.append("-in")
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("output_image")
    if v_0 is not None:
        cargs.append("-out")
        cargs.append(v_0)
    v_1 = params.get("add_value_or_image")
    if v_1 is not None:
        cargs.append("-add")
        cargs.append(v_1)
    v_2 = params.get("sub_value_or_image")
    if v_2 is not None:
        cargs.append("-sub")
        cargs.append(v_2)
    v_3 = params.get("mul_value_or_image")
    if v_3 is not None:
        cargs.append("-mul")
        cargs.append(v_3)
    v_4 = params.get("div_value_or_image")
    if v_4 is not None:
        cargs.append("-div")
        cargs.append(v_4)
    v_5 = params.get("smooth_value")
    if v_5 is not None:
        cargs.append("-smo")
        cargs.append(str(v_5))
    v_6 = params.get("smooth_gaussian")
    if v_6 is not None:
        cargs.append("-smoG")
        for item0 in v_6:
            cargs.append(str(item0))
    v_7 = params.get("rms_image")
    if v_7 is not None:
        cargs.append("-rms")
        cargs.append(execution.input_file(v_7))
    if params.get("binarize"):
        cargs.append("-bin")
    v_8 = params.get("threshold_value")
    if v_8 is not None:
        cargs.append("-thr")
        cargs.append(str(v_8))
    v_9 = params.get("nan_mask_image")
    if v_9 is not None:
        cargs.append("-nan")
        cargs.append(execution.input_file(v_9))
    return cargs

def reg_tools_outputs(params: RegTools, execution: Execution) -> RegToolsOutputs:
    """Build the RegToolsOutputs object for this tool."""
    output_image_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_image")
    if __v0 is not None:
        output_image_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return RegToolsOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
    )

def reg_tools_execute(params: RegTools, runner: Runner | None = None) -> RegToolsOutputs:
    """
    reg_tools

    A versatile tool for manipulating and processing medical images.

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_tools_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_TOOLS_METADATA)
    execution.params(params)
    args = reg_tools_cargs(params, execution)
    out = reg_tools_outputs(params, execution)
    execution.run(args)
    return out

def reg_tools(
    input_image: InputPathType,
    output_image: str | None = None,
    add_value_or_image: str | None = None,
    sub_value_or_image: str | None = None,
    mul_value_or_image: str | None = None,
    div_value_or_image: str | None = None,
    smooth_value: float | None = None,
    smooth_gaussian: list[float] | None = None,
    rms_image: InputPathType | None = None,
    binarize: bool = False,
    threshold_value: float | None = None,
    nan_mask_image: InputPathType | None = None,
    runner: Runner | None = None,
) -> RegToolsOutputs:
    """
    reg_tools

    A versatile tool for manipulating and processing medical images.

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        input_image: Filename of the input image
        output_image: Filename of the output image
        add_value_or_image: This image (or value) is added to the input
        sub_value_or_image: This image (or value) is subtracted from the input
        mul_value_or_image: This image (or value) is multiplied with the input
        div_value_or_image: This image (or value) is divided by the input
        smooth_value: The input image is smoothed using a B-spline curve
        smooth_gaussian: The input image is smoothed using a Gaussian kernel
        rms_image: Compute the mean RMS between the input image and this image
        binarize: Binarize the input image (val!=0?val=1:val=0)
        threshold_value: Threshold the input image (val<thr?val=0:val=1)
        nan_mask_image: This image is used to mask the input image. Voxels outside\
            of the mask are set to NaN
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_tools_params(
        input_image=input_image,
        output_image=output_image,
        add_value_or_image=add_value_or_image,
        sub_value_or_image=sub_value_or_image,
        mul_value_or_image=mul_value_or_image,
        div_value_or_image=div_value_or_image,
        smooth_value=smooth_value,
        smooth_gaussian=smooth_gaussian,
        rms_image=rms_image,
        binarize=binarize,
        threshold_value=threshold_value,
        nan_mask_image=nan_mask_image,
    )
    return reg_tools_execute(params, runner)

__all__ = [
    "RegTools",
    "RegToolsOutputs",
    "REG_TOOLS_METADATA",
    "reg_tools_cargs",
    "reg_tools_outputs",
    "reg_tools_params",
    "reg_tools_execute",
    "reg_tools_validate",
    "reg_tools",
]