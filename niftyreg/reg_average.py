# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_AVERAGE_METADATA = Metadata(
    id="reg_average",
    name="reg_average",
    package="niftyreg",
)

RegAverage = typing.TypedDict(
    "RegAverage",
    {
        "@type": typing.NotRequired[typing.Literal["niftyreg/reg_average"]],
        "output_file": str,
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class RegAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file_location: OutputPathType
    """The averaged output file (image or affine transformation)"""

def reg_average_params(
    output_file: str,
    input_files: list[InputPathType],
) -> RegAverage:
    """
    Build parameters.

    Args:
        output_file: Filename of the output image or affine transformation
        input_files: Input file names (images or affine matrices) to be averaged

    Returns:
        Parameter dictionary.
    """
    params: RegAverage = {
        "@type": "niftyreg/reg_average",
        "output_file": output_file,
        "input_files": input_files,
    }
    return params

def reg_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def reg_average_cargs(params: RegAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg_average")
    cargs.append(params["output_file"])
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def reg_average_outputs(params: RegAverage, execution: Execution) -> RegAverageOutputs:
    """Build the RegAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_location_v: OutputPathType = execution.output_file(params["output_file"])
    return RegAverageOutputs(
        root=root_v,
        output_file_location=output_file_location_v,
    )

def reg_average_execute(params: RegAverage, runner: Runner | None = None) -> RegAverageOutputs:
    """
    reg_average

    Command line program to average either images or affine transformations

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_AVERAGE_METADATA)
    execution.params(params)
    args = reg_average_cargs(params, execution)
    out = reg_average_outputs(params, execution)
    execution.run(args)
    return out

def reg_average(
    output_file: str,
    input_files: list[InputPathType],
    runner: Runner | None = None,
) -> RegAverageOutputs:
    """
    reg_average

    Command line program to average either images or affine transformations

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        output_file: Filename of the output image or affine transformation
        input_files: Input file names (images or affine matrices) to be averaged
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_average_params(
        output_file=output_file,
        input_files=input_files,
    )
    return reg_average_execute(params, runner)

__all__ = [
    "RegAverage",
    "RegAverageOutputs",
    "REG_AVERAGE_METADATA",
    "reg_average_cargs",
    "reg_average_outputs",
    "reg_average_params",
    "reg_average_execute",
    "reg_average_validate",
    "reg_average",
]