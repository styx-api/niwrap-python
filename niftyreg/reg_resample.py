# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REG_RESAMPLE_METADATA = Metadata(
    id="reg_resample",
    name="reg_resample",
    package="niftyreg",
)

RegResample = typing.TypedDict(
    "RegResample",
    {
        "@type": typing.NotRequired[typing.Literal["niftyreg/reg_resample"]],
        "reference_image": InputPathType,
        "floating_image": InputPathType,
        "affine_transform": typing.NotRequired[InputPathType],
        "flirt_affine_transform": typing.NotRequired[InputPathType],
        "control_point_grid": typing.NotRequired[InputPathType],
        "deformation_field": typing.NotRequired[InputPathType],
        "resampled_image": typing.NotRequired[str],
        "resampled_blank": typing.NotRequired[str],
        "nearest_neighbor": typing.NotRequired[bool],
        "linear_interpolation": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RegResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_resampled_image: OutputPathType | None
    """File containing the resampled image"""
    output_resampled_blank: OutputPathType | None
    """File containing the resampled blank grid"""

def reg_resample_params(
    reference_image: InputPathType,
    floating_image: InputPathType,
    affine_transform: InputPathType | None = None,
    flirt_affine_transform: InputPathType | None = None,
    control_point_grid: InputPathType | None = None,
    deformation_field: InputPathType | None = None,
    resampled_image: str | None = None,
    resampled_blank: str | None = None,
    nearest_neighbor: bool = False,
    linear_interpolation: bool = False,
) -> RegResample:
    """
    Build parameters.

    Args:
        reference_image: Filename of the reference image
        floating_image: Filename of the floating image
        affine_transform: Filename which contains an affine transformation\
            (Affine*Reference=Floating)
        flirt_affine_transform: Filename which contains a radiological flirt affine\
            transformation
        control_point_grid: Filename of the control point grid image (from reg_f3d)
        deformation_field: Filename of the deformation field image (from\
            reg_transform)
        resampled_image: Filename of the resampled image
        resampled_blank: Filename of the resampled blank grid
        nearest_neighbor: Use a Nearest Neighbor interpolation for the source\
            resampling (cubic spline by default)
        linear_interpolation: Use a Linear interpolation for the source resampling\
            (cubic spline by default)

    Returns:
        Parameter dictionary.
    """
    params: RegResample = {
        "@type": "niftyreg/reg_resample",
        "reference_image": reference_image,
        "floating_image": floating_image,
        "nearest_neighbor": nearest_neighbor,
        "linear_interpolation": linear_interpolation,
    }
    if affine_transform is not None:
        params["affine_transform"] = affine_transform
    if flirt_affine_transform is not None:
        params["flirt_affine_transform"] = flirt_affine_transform
    if control_point_grid is not None:
        params["control_point_grid"] = control_point_grid
    if deformation_field is not None:
        params["deformation_field"] = deformation_field
    if resampled_image is not None:
        params["resampled_image"] = resampled_image
    if resampled_blank is not None:
        params["resampled_blank"] = resampled_blank
    return params

def reg_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RegResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("floating_image", None) is None:
        raise StyxValidationError("`floating_image` must not be None")
    if not isinstance(params["floating_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`floating_image` has the wrong type: Received `{type(params["floating_image"])}` expected `InputPathType`')
    if params.get("affine_transform", None) is not None:
        if not isinstance(params["affine_transform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`affine_transform` has the wrong type: Received `{type(params["affine_transform"])}` expected `InputPathType`')
    if params.get("flirt_affine_transform", None) is not None:
        if not isinstance(params["flirt_affine_transform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`flirt_affine_transform` has the wrong type: Received `{type(params["flirt_affine_transform"])}` expected `InputPathType`')
    if params.get("control_point_grid", None) is not None:
        if not isinstance(params["control_point_grid"], (pathlib.Path, str)):
            raise StyxValidationError(f'`control_point_grid` has the wrong type: Received `{type(params["control_point_grid"])}` expected `InputPathType`')
    if params.get("deformation_field", None) is not None:
        if not isinstance(params["deformation_field"], (pathlib.Path, str)):
            raise StyxValidationError(f'`deformation_field` has the wrong type: Received `{type(params["deformation_field"])}` expected `InputPathType`')
    if params.get("resampled_image", None) is not None:
        if not isinstance(params["resampled_image"], str):
            raise StyxValidationError(f'`resampled_image` has the wrong type: Received `{type(params["resampled_image"])}` expected `str`')
    if params.get("resampled_blank", None) is not None:
        if not isinstance(params["resampled_blank"], str):
            raise StyxValidationError(f'`resampled_blank` has the wrong type: Received `{type(params["resampled_blank"])}` expected `str`')
    if params.get("nearest_neighbor", None) is not None:
        if not isinstance(params["nearest_neighbor"], bool):
            raise StyxValidationError(f'`nearest_neighbor` has the wrong type: Received `{type(params["nearest_neighbor"])}` expected `bool`')
    if params.get("linear_interpolation", None) is not None:
        if not isinstance(params["linear_interpolation"], bool):
            raise StyxValidationError(f'`linear_interpolation` has the wrong type: Received `{type(params["linear_interpolation"])}` expected `bool`')

def reg_resample_cargs(params: RegResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("reg_resample")
    cargs.append("-ref")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("-flo")
    cargs.append(execution.input_file(params["floating_image"]))
    v_0 = params.get("affine_transform")
    if v_0 is not None:
        cargs.append("-aff")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("flirt_affine_transform")
    if v_1 is not None:
        cargs.append("-affFlirt")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("control_point_grid")
    if v_2 is not None:
        cargs.append("-cpp")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("deformation_field")
    if v_3 is not None:
        cargs.append("-def")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("resampled_image")
    if v_4 is not None:
        cargs.append("-res")
        cargs.append(v_4)
    v_5 = params.get("resampled_blank")
    if v_5 is not None:
        cargs.append("-blank")
        cargs.append(v_5)
    if params.get("nearest_neighbor"):
        cargs.append("-NN")
    if params.get("linear_interpolation"):
        cargs.append("-LIN")
    return cargs

def reg_resample_outputs(params: RegResample, execution: Execution) -> RegResampleOutputs:
    """Build the RegResampleOutputs object for this tool."""
    output_resampled_image_v: OutputPathType | None = None
    output_resampled_blank_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("resampled_image")
    if __v0 is not None:
        output_resampled_image_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("resampled_blank")
    if __v1 is not None:
        output_resampled_blank_v = execution.output_file((__v1 if __v1 is not None else ""))
    return RegResampleOutputs(
        root=root_v,
        output_resampled_image=output_resampled_image_v,
        output_resampled_blank=output_resampled_blank_v,
    )

def reg_resample_execute(params: RegResample, runner: Runner | None = None) -> RegResampleOutputs:
    """
    reg_resample

    Tool for resampling floating images to the reference image space using different transformations.

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reg_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REG_RESAMPLE_METADATA)
    execution.params(params)
    args = reg_resample_cargs(params, execution)
    out = reg_resample_outputs(params, execution)
    execution.run(args)
    return out

def reg_resample(
    reference_image: InputPathType,
    floating_image: InputPathType,
    affine_transform: InputPathType | None = None,
    flirt_affine_transform: InputPathType | None = None,
    control_point_grid: InputPathType | None = None,
    deformation_field: InputPathType | None = None,
    resampled_image: str | None = None,
    resampled_blank: str | None = None,
    nearest_neighbor: bool = False,
    linear_interpolation: bool = False,
    runner: Runner | None = None,
) -> RegResampleOutputs:
    """
    reg_resample

    Tool for resampling floating images to the reference image space using different transformations.

    Author: NiftyReg Developers

    URL: http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftyReg

    Args:
        reference_image: Filename of the reference image
        floating_image: Filename of the floating image
        affine_transform: Filename which contains an affine transformation\
            (Affine*Reference=Floating)
        flirt_affine_transform: Filename which contains a radiological flirt affine\
            transformation
        control_point_grid: Filename of the control point grid image (from reg_f3d)
        deformation_field: Filename of the deformation field image (from\
            reg_transform)
        resampled_image: Filename of the resampled image
        resampled_blank: Filename of the resampled blank grid
        nearest_neighbor: Use a Nearest Neighbor interpolation for the source\
            resampling (cubic spline by default)
        linear_interpolation: Use a Linear interpolation for the source resampling\
            (cubic spline by default)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reg_resample_params(
        reference_image=reference_image,
        floating_image=floating_image,
        affine_transform=affine_transform,
        flirt_affine_transform=flirt_affine_transform,
        control_point_grid=control_point_grid,
        deformation_field=deformation_field,
        resampled_image=resampled_image,
        resampled_blank=resampled_blank,
        nearest_neighbor=nearest_neighbor,
        linear_interpolation=linear_interpolation,
    )
    return reg_resample_execute(params, runner)

__all__ = [
    "RegResample",
    "RegResampleOutputs",
    "REG_RESAMPLE_METADATA",
    "reg_resample_cargs",
    "reg_resample_outputs",
    "reg_resample_params",
    "reg_resample_execute",
    "reg_resample_validate",
    "reg_resample",
]