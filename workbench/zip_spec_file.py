# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ZIP_SPEC_FILE_METADATA = Metadata(
    id="zip_spec_file",
    name="ZIP A SPEC FILE AND ITS DATA FILES",
    package="workbench",
)

ZipSpecFile = typing.TypedDict(
    "ZipSpecFile",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/zip_spec_file"]],
        "spec_file": str,
        "extract_folder": str,
        "zip_file": str,
        "base_dir": typing.NotRequired[str],
        "skip_missing": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ZipSpecFileOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def zip_spec_file_params(
    spec_file: str,
    extract_folder: str,
    zip_file: str,
    base_dir: str | None = None,
    skip_missing: bool = False,
) -> ZipSpecFile:
    """
    Build parameters.

    Args:
        spec_file: the specification file to add to zip file
        extract_folder: the name of the folder created when the zip file is\
            unzipped
        zip_file: out - the zip file that will be created
        base_dir: specify a directory that all data files are somewhere within,\
            this will become the root of the zipfile's directory structure
        skip_missing: any missing files will generate only warnings, and the zip\
            file will be created anyway

    Returns:
        Parameter dictionary.
    """
    params: ZipSpecFile = {
        "@type": "workbench/zip_spec_file",
        "spec_file": spec_file,
        "extract_folder": extract_folder,
        "zip_file": zip_file,
        "skip_missing": skip_missing,
    }
    if base_dir is not None:
        params["base_dir"] = base_dir
    return params

def zip_spec_file_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ZipSpecFile."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spec_file", None) is None:
        raise StyxValidationError("`spec_file` must not be None")
    if not isinstance(params["spec_file"], str):
        raise StyxValidationError(f'`spec_file` has the wrong type: Received `{type(params["spec_file"])}` expected `str`')
    if params.get("extract_folder", None) is None:
        raise StyxValidationError("`extract_folder` must not be None")
    if not isinstance(params["extract_folder"], str):
        raise StyxValidationError(f'`extract_folder` has the wrong type: Received `{type(params["extract_folder"])}` expected `str`')
    if params.get("zip_file", None) is None:
        raise StyxValidationError("`zip_file` must not be None")
    if not isinstance(params["zip_file"], str):
        raise StyxValidationError(f'`zip_file` has the wrong type: Received `{type(params["zip_file"])}` expected `str`')
    if params.get("base_dir", None) is not None:
        if not isinstance(params["base_dir"], str):
            raise StyxValidationError(f'`base_dir` has the wrong type: Received `{type(params["base_dir"])}` expected `str`')
    if params.get("skip_missing", None) is not None:
        if not isinstance(params["skip_missing"], bool):
            raise StyxValidationError(f'`skip_missing` has the wrong type: Received `{type(params["skip_missing"])}` expected `bool`')

def zip_spec_file_cargs(params: ZipSpecFile, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-zip-spec-file")
    cargs.append(params["spec_file"])
    cargs.append(params["extract_folder"])
    cargs.append(params["zip_file"])
    v_0 = params.get("base_dir")
    if v_0 is not None:
        cargs.append("-base-dir")
        cargs.append(v_0)
    if params.get("skip_missing"):
        cargs.append("-skip-missing")
    return cargs

def zip_spec_file_outputs(params: ZipSpecFile, execution: Execution) -> ZipSpecFileOutputs:
    """Build the ZipSpecFileOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ZipSpecFileOutputs(
        root=root_v,
    )

def zip_spec_file_execute(params: ZipSpecFile, runner: Runner | None = None) -> ZipSpecFileOutputs:
    """
    ZIP A SPEC FILE AND ITS DATA FILES

    If zip-file already exists, it will be overwritten.  If -base-dir is not specified, the directory containing the spec file is used for the base directory.  The spec file must contain only relative paths, and no data files may be outside the base directory.  Scene files inside spec files are not checked for what files they reference, ensure that all data files referenced by the scene files are also referenced by the spec file.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    zip_spec_file_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ZIP_SPEC_FILE_METADATA)
    execution.params(params)
    args = zip_spec_file_cargs(params, execution)
    out = zip_spec_file_outputs(params, execution)
    execution.run(args)
    return out

def zip_spec_file(
    spec_file: str,
    extract_folder: str,
    zip_file: str,
    base_dir: str | None = None,
    skip_missing: bool = False,
    runner: Runner | None = None,
) -> ZipSpecFileOutputs:
    """
    ZIP A SPEC FILE AND ITS DATA FILES

    If zip-file already exists, it will be overwritten.  If -base-dir is not specified, the directory containing the spec file is used for the base directory.  The spec file must contain only relative paths, and no data files may be outside the base directory.  Scene files inside spec files are not checked for what files they reference, ensure that all data files referenced by the scene files are also referenced by the spec file.

    Args:
        spec_file: the specification file to add to zip file
        extract_folder: the name of the folder created when the zip file is\
            unzipped
        zip_file: out - the zip file that will be created
        base_dir: specify a directory that all data files are somewhere within,\
            this will become the root of the zipfile's directory structure
        skip_missing: any missing files will generate only warnings, and the zip\
            file will be created anyway
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = zip_spec_file_params(
        spec_file=spec_file,
        extract_folder=extract_folder,
        zip_file=zip_file,
        base_dir=base_dir,
        skip_missing=skip_missing,
    )
    return zip_spec_file_execute(params, runner)

__all__ = [
    "ZipSpecFile",
    "ZipSpecFileOutputs",
    "ZIP_SPEC_FILE_METADATA",
    "zip_spec_file_cargs",
    "zip_spec_file_outputs",
    "zip_spec_file_params",
    "zip_spec_file_execute",
    "zip_spec_file_validate",
    "zip_spec_file",
]