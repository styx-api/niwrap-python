# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_RESTRICT_DENSE_MAP_METADATA = Metadata(
    id="cifti_restrict_dense_map",
    name="EXCLUDE BRAINORDINATES FROM A CIFTI FILE",
    package="workbench",
)

CiftiRestrictDenseMap = typing.TypedDict(
    "CiftiRestrictDenseMap",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_restrict_dense_map"]],
        "cifti_in": InputPathType,
        "direction": str,
        "cifti_out": str,
        "cifti_roi": typing.NotRequired[InputPathType],
        "left_roi": typing.NotRequired[InputPathType],
        "right_roi": typing.NotRequired[InputPathType],
        "cerebellum_roi": typing.NotRequired[InputPathType],
        "vol_roi": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiRestrictDenseMapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti"""

def cifti_restrict_dense_map_params(
    cifti_in: InputPathType,
    direction: str,
    cifti_out: str,
    cifti_roi: InputPathType | None = None,
    left_roi: InputPathType | None = None,
    right_roi: InputPathType | None = None,
    cerebellum_roi: InputPathType | None = None,
    vol_roi: InputPathType | None = None,
) -> CiftiRestrictDenseMap:
    """
    Build parameters.

    Args:
        cifti_in: the input cifti
        direction: which dimension to change the mapping on (integer, 'ROW', or\
            'COLUMN')
        cifti_out: the output cifti
        cifti_roi: cifti file containing combined rois
        left_roi: vertices to use from left hemisphere
        right_roi: vertices to use from right hemisphere
        cerebellum_roi: vertices to use from cerebellum
        vol_roi: voxels to use

    Returns:
        Parameter dictionary.
    """
    params: CiftiRestrictDenseMap = {
        "@type": "workbench/cifti_restrict_dense_map",
        "cifti_in": cifti_in,
        "direction": direction,
        "cifti_out": cifti_out,
    }
    if cifti_roi is not None:
        params["cifti_roi"] = cifti_roi
    if left_roi is not None:
        params["left_roi"] = left_roi
    if right_roi is not None:
        params["right_roi"] = right_roi
    if cerebellum_roi is not None:
        params["cerebellum_roi"] = cerebellum_roi
    if vol_roi is not None:
        params["vol_roi"] = vol_roi
    return params

def cifti_restrict_dense_map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiRestrictDenseMap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("cifti_roi", None) is not None:
        if not isinstance(params["cifti_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti_roi` has the wrong type: Received `{type(params["cifti_roi"])}` expected `InputPathType`')
    if params.get("left_roi", None) is not None:
        if not isinstance(params["left_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`left_roi` has the wrong type: Received `{type(params["left_roi"])}` expected `InputPathType`')
    if params.get("right_roi", None) is not None:
        if not isinstance(params["right_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`right_roi` has the wrong type: Received `{type(params["right_roi"])}` expected `InputPathType`')
    if params.get("cerebellum_roi", None) is not None:
        if not isinstance(params["cerebellum_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cerebellum_roi` has the wrong type: Received `{type(params["cerebellum_roi"])}` expected `InputPathType`')
    if params.get("vol_roi", None) is not None:
        if not isinstance(params["vol_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_roi` has the wrong type: Received `{type(params["vol_roi"])}` expected `InputPathType`')

def cifti_restrict_dense_map_cargs(params: CiftiRestrictDenseMap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-restrict-dense-map")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("cifti_roi")
    if v_0 is not None:
        cargs.append("-cifti-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("left_roi")
    if v_1 is not None:
        cargs.append("-left-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("right_roi")
    if v_2 is not None:
        cargs.append("-right-roi")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("cerebellum_roi")
    if v_3 is not None:
        cargs.append("-cerebellum-roi")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("vol_roi")
    if v_4 is not None:
        cargs.append("-vol-roi")
        cargs.append(execution.input_file(v_4))
    return cargs

def cifti_restrict_dense_map_outputs(params: CiftiRestrictDenseMap, execution: Execution) -> CiftiRestrictDenseMapOutputs:
    """Build the CiftiRestrictDenseMapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiRestrictDenseMapOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_restrict_dense_map_execute(params: CiftiRestrictDenseMap, runner: Runner | None = None) -> CiftiRestrictDenseMapOutputs:
    """
    EXCLUDE BRAINORDINATES FROM A CIFTI FILE

    Writes a modified version of <cifti-in>, where all brainordinates outside the specified roi(s) are removed from the file.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  If -cifti-roi is specified, no other -*-roi option may be specified.  If not using -cifti-roi, any -*-roi options not present will discard the relevant structure, if present in the input file.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_restrict_dense_map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_RESTRICT_DENSE_MAP_METADATA)
    execution.params(params)
    args = cifti_restrict_dense_map_cargs(params, execution)
    out = cifti_restrict_dense_map_outputs(params, execution)
    execution.run(args)
    return out

def cifti_restrict_dense_map(
    cifti_in: InputPathType,
    direction: str,
    cifti_out: str,
    cifti_roi: InputPathType | None = None,
    left_roi: InputPathType | None = None,
    right_roi: InputPathType | None = None,
    cerebellum_roi: InputPathType | None = None,
    vol_roi: InputPathType | None = None,
    runner: Runner | None = None,
) -> CiftiRestrictDenseMapOutputs:
    """
    EXCLUDE BRAINORDINATES FROM A CIFTI FILE

    Writes a modified version of <cifti-in>, where all brainordinates outside the specified roi(s) are removed from the file.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  If -cifti-roi is specified, no other -*-roi option may be specified.  If not using -cifti-roi, any -*-roi options not present will discard the relevant structure, if present in the input file.

    Args:
        cifti_in: the input cifti
        direction: which dimension to change the mapping on (integer, 'ROW', or\
            'COLUMN')
        cifti_out: the output cifti
        cifti_roi: cifti file containing combined rois
        left_roi: vertices to use from left hemisphere
        right_roi: vertices to use from right hemisphere
        cerebellum_roi: vertices to use from cerebellum
        vol_roi: voxels to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_restrict_dense_map_params(
        cifti_in=cifti_in,
        direction=direction,
        cifti_out=cifti_out,
        cifti_roi=cifti_roi,
        left_roi=left_roi,
        right_roi=right_roi,
        cerebellum_roi=cerebellum_roi,
        vol_roi=vol_roi,
    )
    return cifti_restrict_dense_map_execute(params, runner)

__all__ = [
    "CiftiRestrictDenseMap",
    "CiftiRestrictDenseMapOutputs",
    "CIFTI_RESTRICT_DENSE_MAP_METADATA",
    "cifti_restrict_dense_map_cargs",
    "cifti_restrict_dense_map_outputs",
    "cifti_restrict_dense_map_params",
    "cifti_restrict_dense_map_execute",
    "cifti_restrict_dense_map_validate",
    "cifti_restrict_dense_map",
]