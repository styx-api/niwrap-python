# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_TRANSPOSE_METADATA = Metadata(
    id="cifti_transpose",
    name="TRANSPOSE A CIFTI FILE",
    package="workbench",
)

CiftiTranspose = typing.TypedDict(
    "CiftiTranspose",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_transpose"]],
        "cifti_in": InputPathType,
        "cifti_out": str,
        "mem_limit": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class CiftiTransposeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_transpose_params(
    cifti_in: InputPathType,
    cifti_out: str,
    mem_limit: float | None = None,
) -> CiftiTranspose:
    """
    Build parameters.

    Args:
        cifti_in: the input cifti file
        cifti_out: the output cifti file
        mem_limit: restrict memory usage

    Returns:
        Parameter dictionary.
    """
    params: CiftiTranspose = {
        "@type": "workbench/cifti_transpose",
        "cifti_in": cifti_in,
        "cifti_out": cifti_out,
    }
    if mem_limit is not None:
        params["mem_limit"] = mem_limit
    return params

def cifti_transpose_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiTranspose."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("mem_limit", None) is not None:
        if not isinstance(params["mem_limit"], (float, int)):
            raise StyxValidationError(f'`mem_limit` has the wrong type: Received `{type(params["mem_limit"])}` expected `float`')

def cifti_transpose_cargs(params: CiftiTranspose, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-transpose")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["cifti_out"])
    v_0 = params.get("mem_limit")
    if v_0 is not None:
        cargs.append("-mem-limit")
        cargs.append(str(v_0))
    return cargs

def cifti_transpose_outputs(params: CiftiTranspose, execution: Execution) -> CiftiTransposeOutputs:
    """Build the CiftiTransposeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiTransposeOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_transpose_execute(params: CiftiTranspose, runner: Runner | None = None) -> CiftiTransposeOutputs:
    """
    TRANSPOSE A CIFTI FILE

    The input must be a 2-dimensional cifti file.  The output is a cifti file where every row in the input is a column in the output.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_transpose_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_TRANSPOSE_METADATA)
    execution.params(params)
    args = cifti_transpose_cargs(params, execution)
    out = cifti_transpose_outputs(params, execution)
    execution.run(args)
    return out

def cifti_transpose(
    cifti_in: InputPathType,
    cifti_out: str,
    mem_limit: float | None = None,
    runner: Runner | None = None,
) -> CiftiTransposeOutputs:
    """
    TRANSPOSE A CIFTI FILE

    The input must be a 2-dimensional cifti file.  The output is a cifti file where every row in the input is a column in the output.

    Args:
        cifti_in: the input cifti file
        cifti_out: the output cifti file
        mem_limit: restrict memory usage
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_transpose_params(
        cifti_in=cifti_in,
        cifti_out=cifti_out,
        mem_limit=mem_limit,
    )
    return cifti_transpose_execute(params, runner)

__all__ = [
    "CiftiTranspose",
    "CiftiTransposeOutputs",
    "CIFTI_TRANSPOSE_METADATA",
    "cifti_transpose_cargs",
    "cifti_transpose_outputs",
    "cifti_transpose_params",
    "cifti_transpose_execute",
    "cifti_transpose_validate",
    "cifti_transpose",
]