# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_GRADIENT_METADATA = Metadata(
    id="volume_gradient",
    name="GRADIENT OF A VOLUME FILE",
    package="workbench",
)

class VolumeGradientPresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class VolumeGradientVectors(typing.TypedDict):
    vector_volume_out: str
    """the vectors as a volume file"""

VolumeGradient = typing.TypedDict(
    "VolumeGradient",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_gradient"]],
        "volume_in": InputPathType,
        "volume_out": str,
        "presmooth": typing.NotRequired[VolumeGradientPresmooth],
        "roi": typing.NotRequired[InputPathType],
        "vectors": typing.NotRequired[VolumeGradientVectors],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeGradientOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output gradient magnitude volume"""
    vector_volume_out: OutputPathType | None
    """the vectors as a volume file"""

def volume_gradient_params(
    volume_in: InputPathType,
    volume_out: str,
    presmooth: VolumeGradientPresmooth | None = None,
    roi: InputPathType | None = None,
    vectors: VolumeGradientVectors | None = None,
    subvolume: str | None = None,
) -> VolumeGradient:
    """
    Build parameters.

    Args:
        volume_in: the input volume
        volume_out: the output gradient magnitude volume
        presmooth: smooth the volume before computing the gradient
        roi: select a region of interest to take the gradient of
        vectors: output vectors
        subvolume: select a single subvolume to take the gradient of

    Returns:
        Parameter dictionary.
    """
    params: VolumeGradient = {
        "@type": "workbench/volume_gradient",
        "volume_in": volume_in,
        "volume_out": volume_out,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if vectors is not None:
        params["vectors"] = vectors
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_gradient_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeGradient."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("vectors", None) is not None:
        if not isinstance(params["vectors"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["vectors"])}\'')
        if params["vectors"].get("vector_volume_out", None) is None:
            raise StyxValidationError("`vector_volume_out` must not be None")
        if not isinstance(params["vectors"]["vector_volume_out"], str):
            raise StyxValidationError(f'`vector_volume_out` has the wrong type: Received `{type(params["vectors"]["vector_volume_out"])}` expected `str`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_gradient_cargs(params: VolumeGradient, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-gradient")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("vectors")
    if v_2 is not None:
        cargs.append("-vectors")
        cargs.append(v_2["vector_volume_out"])
    v_3 = params.get("subvolume")
    if v_3 is not None:
        cargs.append("-subvolume")
        cargs.append(v_3)
    return cargs

def volume_gradient_outputs(params: VolumeGradient, execution: Execution) -> VolumeGradientOutputs:
    """Build the VolumeGradientOutputs object for this tool."""
    vector_volume_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    __v0 = params.get("vectors")
    if __v0 is not None:
        vector_volume_out_v = execution.output_file(__v0["vector_volume_out"])
    return VolumeGradientOutputs(
        root=root_v,
        volume_out=volume_out_v,
        vector_volume_out=vector_volume_out_v,
    )

def volume_gradient_execute(params: VolumeGradient, runner: Runner | None = None) -> VolumeGradientOutputs:
    """
    GRADIENT OF A VOLUME FILE

    Computes the gradient of the volume by doing linear regressions for each voxel, considering only its face neighbors unless too few face neighbors exist.  The gradient vector is constructed from the partial derivatives of the resulting linear function, and the magnitude of this vector is the output.  If specified, the volume vector output is arranged with the x, y, and z components from a subvolume as consecutive subvolumes.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_gradient_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_GRADIENT_METADATA)
    execution.params(params)
    args = volume_gradient_cargs(params, execution)
    out = volume_gradient_outputs(params, execution)
    execution.run(args)
    return out

def volume_gradient(
    volume_in: InputPathType,
    volume_out: str,
    presmooth: VolumeGradientPresmooth | None = None,
    roi: InputPathType | None = None,
    vectors: VolumeGradientVectors | None = None,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeGradientOutputs:
    """
    GRADIENT OF A VOLUME FILE

    Computes the gradient of the volume by doing linear regressions for each voxel, considering only its face neighbors unless too few face neighbors exist.  The gradient vector is constructed from the partial derivatives of the resulting linear function, and the magnitude of this vector is the output.  If specified, the volume vector output is arranged with the x, y, and z components from a subvolume as consecutive subvolumes.

    Args:
        volume_in: the input volume
        volume_out: the output gradient magnitude volume
        presmooth: smooth the volume before computing the gradient
        roi: select a region of interest to take the gradient of
        vectors: output vectors
        subvolume: select a single subvolume to take the gradient of
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_gradient_params(
        volume_in=volume_in,
        volume_out=volume_out,
        presmooth=presmooth,
        roi=roi,
        vectors=vectors,
        subvolume=subvolume,
    )
    return volume_gradient_execute(params, runner)

__all__ = [
    "VolumeGradient",
    "VolumeGradientOutputs",
    "VOLUME_GRADIENT_METADATA",
    "volume_gradient_cargs",
    "volume_gradient_outputs",
    "volume_gradient_params",
    "volume_gradient_execute",
    "volume_gradient_validate",
    "volume_gradient",
]