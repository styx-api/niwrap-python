# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_FILL_HOLES_METADATA = Metadata(
    id="volume_fill_holes",
    name="FILL HOLES IN AN ROI VOLUME",
    package="workbench",
)

VolumeFillHoles = typing.TypedDict(
    "VolumeFillHoles",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_fill_holes"]],
        "volume_in": InputPathType,
        "volume_out": str,
    },
)

@dataclasses.dataclass
class VolumeFillHolesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output ROI volume"""

def volume_fill_holes_params(
    volume_in: InputPathType,
    volume_out: str,
) -> VolumeFillHoles:
    """
    Build parameters.

    Args:
        volume_in: the input ROI volume
        volume_out: the output ROI volume

    Returns:
        Parameter dictionary.
    """
    params: VolumeFillHoles = {
        "@type": "workbench/volume_fill_holes",
        "volume_in": volume_in,
        "volume_out": volume_out,
    }
    return params

def volume_fill_holes_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeFillHoles."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')

def volume_fill_holes_cargs(params: VolumeFillHoles, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-fill-holes")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["volume_out"])
    return cargs

def volume_fill_holes_outputs(params: VolumeFillHoles, execution: Execution) -> VolumeFillHolesOutputs:
    """Build the VolumeFillHolesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeFillHolesOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_fill_holes_execute(params: VolumeFillHoles, runner: Runner | None = None) -> VolumeFillHolesOutputs:
    """
    FILL HOLES IN AN ROI VOLUME

    Finds all face-connected parts that are not included in the ROI, and fills all but the largest one with ones.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_fill_holes_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_FILL_HOLES_METADATA)
    execution.params(params)
    args = volume_fill_holes_cargs(params, execution)
    out = volume_fill_holes_outputs(params, execution)
    execution.run(args)
    return out

def volume_fill_holes(
    volume_in: InputPathType,
    volume_out: str,
    runner: Runner | None = None,
) -> VolumeFillHolesOutputs:
    """
    FILL HOLES IN AN ROI VOLUME

    Finds all face-connected parts that are not included in the ROI, and fills all but the largest one with ones.

    Args:
        volume_in: the input ROI volume
        volume_out: the output ROI volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_fill_holes_params(
        volume_in=volume_in,
        volume_out=volume_out,
    )
    return volume_fill_holes_execute(params, runner)

__all__ = [
    "VolumeFillHoles",
    "VolumeFillHolesOutputs",
    "VOLUME_FILL_HOLES_METADATA",
    "volume_fill_holes_cargs",
    "volume_fill_holes_outputs",
    "volume_fill_holes_params",
    "volume_fill_holes_execute",
    "volume_fill_holes_validate",
    "volume_fill_holes",
]