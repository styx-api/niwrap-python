# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_COPY_EXTENSIONS_METADATA = Metadata(
    id="volume_copy_extensions",
    name="COPY EXTENDED DATA TO ANOTHER VOLUME FILE",
    package="workbench",
)

VolumeCopyExtensions = typing.TypedDict(
    "VolumeCopyExtensions",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_copy_extensions"]],
        "data_volume": InputPathType,
        "extension_volume": InputPathType,
        "volume_out": str,
        "drop_unknown": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VolumeCopyExtensionsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_copy_extensions_params(
    data_volume: InputPathType,
    extension_volume: InputPathType,
    volume_out: str,
    drop_unknown: bool = False,
) -> VolumeCopyExtensions:
    """
    Build parameters.

    Args:
        data_volume: the volume file containing the voxel data to use
        extension_volume: the volume file containing the extensions to use
        volume_out: the output volume
        drop_unknown: don't copy extensions that workbench doesn't understand

    Returns:
        Parameter dictionary.
    """
    params: VolumeCopyExtensions = {
        "@type": "workbench/volume_copy_extensions",
        "data_volume": data_volume,
        "extension_volume": extension_volume,
        "volume_out": volume_out,
        "drop_unknown": drop_unknown,
    }
    return params

def volume_copy_extensions_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeCopyExtensions."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("data_volume", None) is None:
        raise StyxValidationError("`data_volume` must not be None")
    if not isinstance(params["data_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`data_volume` has the wrong type: Received `{type(params["data_volume"])}` expected `InputPathType`')
    if params.get("extension_volume", None) is None:
        raise StyxValidationError("`extension_volume` must not be None")
    if not isinstance(params["extension_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`extension_volume` has the wrong type: Received `{type(params["extension_volume"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("drop_unknown", None) is not None:
        if not isinstance(params["drop_unknown"], bool):
            raise StyxValidationError(f'`drop_unknown` has the wrong type: Received `{type(params["drop_unknown"])}` expected `bool`')

def volume_copy_extensions_cargs(params: VolumeCopyExtensions, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-copy-extensions")
    cargs.append(execution.input_file(params["data_volume"]))
    cargs.append(execution.input_file(params["extension_volume"]))
    cargs.append(params["volume_out"])
    if params.get("drop_unknown"):
        cargs.append("-drop-unknown")
    return cargs

def volume_copy_extensions_outputs(params: VolumeCopyExtensions, execution: Execution) -> VolumeCopyExtensionsOutputs:
    """Build the VolumeCopyExtensionsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeCopyExtensionsOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_copy_extensions_execute(params: VolumeCopyExtensions, runner: Runner | None = None) -> VolumeCopyExtensionsOutputs:
    """
    COPY EXTENDED DATA TO ANOTHER VOLUME FILE

    This command copies the information in a volume file that isn't a critical part of the standard header or data matrix, e.g. map names, palette settings, label tables.  If -drop-unknown is not specified, it also copies similar kinds of information set by other software.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_copy_extensions_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_COPY_EXTENSIONS_METADATA)
    execution.params(params)
    args = volume_copy_extensions_cargs(params, execution)
    out = volume_copy_extensions_outputs(params, execution)
    execution.run(args)
    return out

def volume_copy_extensions(
    data_volume: InputPathType,
    extension_volume: InputPathType,
    volume_out: str,
    drop_unknown: bool = False,
    runner: Runner | None = None,
) -> VolumeCopyExtensionsOutputs:
    """
    COPY EXTENDED DATA TO ANOTHER VOLUME FILE

    This command copies the information in a volume file that isn't a critical part of the standard header or data matrix, e.g. map names, palette settings, label tables.  If -drop-unknown is not specified, it also copies similar kinds of information set by other software.

    Args:
        data_volume: the volume file containing the voxel data to use
        extension_volume: the volume file containing the extensions to use
        volume_out: the output volume
        drop_unknown: don't copy extensions that workbench doesn't understand
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_copy_extensions_params(
        data_volume=data_volume,
        extension_volume=extension_volume,
        volume_out=volume_out,
        drop_unknown=drop_unknown,
    )
    return volume_copy_extensions_execute(params, runner)

__all__ = [
    "VolumeCopyExtensions",
    "VolumeCopyExtensionsOutputs",
    "VOLUME_COPY_EXTENSIONS_METADATA",
    "volume_copy_extensions_cargs",
    "volume_copy_extensions_outputs",
    "volume_copy_extensions_params",
    "volume_copy_extensions_execute",
    "volume_copy_extensions_validate",
    "volume_copy_extensions",
]