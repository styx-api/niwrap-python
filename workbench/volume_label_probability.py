# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_LABEL_PROBABILITY_METADATA = Metadata(
    id="volume_label_probability",
    name="FIND FREQUENCY OF VOLUME LABELS",
    package="workbench",
)

VolumeLabelProbability = typing.TypedDict(
    "VolumeLabelProbability",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_label_probability"]],
        "label_maps": InputPathType,
        "probability_out": str,
        "exclude_unlabeled": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VolumeLabelProbabilityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    probability_out: OutputPathType
    """the relative frequencies of each label at each voxel"""

def volume_label_probability_params(
    label_maps: InputPathType,
    probability_out: str,
    exclude_unlabeled: bool = False,
) -> VolumeLabelProbability:
    """
    Build parameters.

    Args:
        label_maps: volume label file containing individual label maps from many\
            subjects
        probability_out: the relative frequencies of each label at each voxel
        exclude_unlabeled: don't make a probability map of the unlabeled key

    Returns:
        Parameter dictionary.
    """
    params: VolumeLabelProbability = {
        "@type": "workbench/volume_label_probability",
        "label_maps": label_maps,
        "probability_out": probability_out,
        "exclude_unlabeled": exclude_unlabeled,
    }
    return params

def volume_label_probability_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeLabelProbability."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_maps", None) is None:
        raise StyxValidationError("`label_maps` must not be None")
    if not isinstance(params["label_maps"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_maps` has the wrong type: Received `{type(params["label_maps"])}` expected `InputPathType`')
    if params.get("probability_out", None) is None:
        raise StyxValidationError("`probability_out` must not be None")
    if not isinstance(params["probability_out"], str):
        raise StyxValidationError(f'`probability_out` has the wrong type: Received `{type(params["probability_out"])}` expected `str`')
    if params.get("exclude_unlabeled", None) is not None:
        if not isinstance(params["exclude_unlabeled"], bool):
            raise StyxValidationError(f'`exclude_unlabeled` has the wrong type: Received `{type(params["exclude_unlabeled"])}` expected `bool`')

def volume_label_probability_cargs(params: VolumeLabelProbability, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-label-probability")
    cargs.append(execution.input_file(params["label_maps"]))
    cargs.append(params["probability_out"])
    if params.get("exclude_unlabeled"):
        cargs.append("-exclude-unlabeled")
    return cargs

def volume_label_probability_outputs(params: VolumeLabelProbability, execution: Execution) -> VolumeLabelProbabilityOutputs:
    """Build the VolumeLabelProbabilityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    probability_out_v: OutputPathType = execution.output_file(params["probability_out"])
    return VolumeLabelProbabilityOutputs(
        root=root_v,
        probability_out=probability_out_v,
    )

def volume_label_probability_execute(params: VolumeLabelProbability, runner: Runner | None = None) -> VolumeLabelProbabilityOutputs:
    """
    FIND FREQUENCY OF VOLUME LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that voxel, divided by the number of input maps.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_label_probability_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_LABEL_PROBABILITY_METADATA)
    execution.params(params)
    args = volume_label_probability_cargs(params, execution)
    out = volume_label_probability_outputs(params, execution)
    execution.run(args)
    return out

def volume_label_probability(
    label_maps: InputPathType,
    probability_out: str,
    exclude_unlabeled: bool = False,
    runner: Runner | None = None,
) -> VolumeLabelProbabilityOutputs:
    """
    FIND FREQUENCY OF VOLUME LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that voxel, divided by the number of input maps.

    Args:
        label_maps: volume label file containing individual label maps from many\
            subjects
        probability_out: the relative frequencies of each label at each voxel
        exclude_unlabeled: don't make a probability map of the unlabeled key
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_label_probability_params(
        label_maps=label_maps,
        probability_out=probability_out,
        exclude_unlabeled=exclude_unlabeled,
    )
    return volume_label_probability_execute(params, runner)

__all__ = [
    "VolumeLabelProbability",
    "VolumeLabelProbabilityOutputs",
    "VOLUME_LABEL_PROBABILITY_METADATA",
    "volume_label_probability_cargs",
    "volume_label_probability_outputs",
    "volume_label_probability_params",
    "volume_label_probability_execute",
    "volume_label_probability_validate",
    "volume_label_probability",
]