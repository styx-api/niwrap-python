# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_ROIS_FROM_EXTREMA_METADATA = Metadata(
    id="metric_rois_from_extrema",
    name="CREATE METRIC ROI MAPS FROM EXTREMA MAPS",
    package="workbench",
)

MetricRoisFromExtrema = typing.TypedDict(
    "MetricRoisFromExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_rois_from_extrema"]],
        "surface": InputPathType,
        "metric": InputPathType,
        "limit": float,
        "metric_out": str,
        "gaussian": typing.NotRequired[float],
        "roi": typing.NotRequired[InputPathType],
        "overlap_logic": typing.NotRequired[str],
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricRoisFromExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric file"""

def metric_rois_from_extrema_params(
    surface: InputPathType,
    metric: InputPathType,
    limit: float,
    metric_out: str,
    gaussian: float | None = None,
    roi: InputPathType | None = None,
    overlap_logic: str | None = None,
    column: str | None = None,
) -> MetricRoisFromExtrema:
    """
    Build parameters.

    Args:
        surface: the surface to use for geodesic distance
        metric: the input metric file
        limit: geodesic distance limit from vertex, in mm
        metric_out: the output metric file
        gaussian: generate a gaussian kernel instead of a flat ROI
        roi: select a region of interest to use
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        column: select a single input column to use

    Returns:
        Parameter dictionary.
    """
    params: MetricRoisFromExtrema = {
        "@type": "workbench/metric_rois_from_extrema",
        "surface": surface,
        "metric": metric,
        "limit": limit,
        "metric_out": metric_out,
    }
    if gaussian is not None:
        params["gaussian"] = gaussian
    if roi is not None:
        params["roi"] = roi
    if overlap_logic is not None:
        params["overlap_logic"] = overlap_logic
    if column is not None:
        params["column"] = column
    return params

def metric_rois_from_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricRoisFromExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `InputPathType`')
    if params.get("limit", None) is None:
        raise StyxValidationError("`limit` must not be None")
    if not isinstance(params["limit"], (float, int)):
        raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("gaussian", None) is not None:
        if not isinstance(params["gaussian"], (float, int)):
            raise StyxValidationError(f'`gaussian` has the wrong type: Received `{type(params["gaussian"])}` expected `float`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("overlap_logic", None) is not None:
        if not isinstance(params["overlap_logic"], str):
            raise StyxValidationError(f'`overlap_logic` has the wrong type: Received `{type(params["overlap_logic"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def metric_rois_from_extrema_cargs(params: MetricRoisFromExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-rois-from-extrema")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric"]))
    cargs.append(str(params["limit"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("gaussian")
    if v_0 is not None:
        cargs.append("-gaussian")
        cargs.append(str(v_0))
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("overlap_logic")
    if v_2 is not None:
        cargs.append("-overlap-logic")
        cargs.append(v_2)
    v_3 = params.get("column")
    if v_3 is not None:
        cargs.append("-column")
        cargs.append(v_3)
    return cargs

def metric_rois_from_extrema_outputs(params: MetricRoisFromExtrema, execution: Execution) -> MetricRoisFromExtremaOutputs:
    """Build the MetricRoisFromExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricRoisFromExtremaOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_rois_from_extrema_execute(params: MetricRoisFromExtrema, runner: Runner | None = None) -> MetricRoisFromExtremaOutputs:
    """
    CREATE METRIC ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_rois_from_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_ROIS_FROM_EXTREMA_METADATA)
    execution.params(params)
    args = metric_rois_from_extrema_cargs(params, execution)
    out = metric_rois_from_extrema_outputs(params, execution)
    execution.run(args)
    return out

def metric_rois_from_extrema(
    surface: InputPathType,
    metric: InputPathType,
    limit: float,
    metric_out: str,
    gaussian: float | None = None,
    roi: InputPathType | None = None,
    overlap_logic: str | None = None,
    column: str | None = None,
    runner: Runner | None = None,
) -> MetricRoisFromExtremaOutputs:
    """
    CREATE METRIC ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        surface: the surface to use for geodesic distance
        metric: the input metric file
        limit: geodesic distance limit from vertex, in mm
        metric_out: the output metric file
        gaussian: generate a gaussian kernel instead of a flat ROI
        roi: select a region of interest to use
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        column: select a single input column to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_rois_from_extrema_params(
        surface=surface,
        metric=metric,
        limit=limit,
        metric_out=metric_out,
        gaussian=gaussian,
        roi=roi,
        overlap_logic=overlap_logic,
        column=column,
    )
    return metric_rois_from_extrema_execute(params, runner)

__all__ = [
    "MetricRoisFromExtrema",
    "MetricRoisFromExtremaOutputs",
    "METRIC_ROIS_FROM_EXTREMA_METADATA",
    "metric_rois_from_extrema_cargs",
    "metric_rois_from_extrema_outputs",
    "metric_rois_from_extrema_params",
    "metric_rois_from_extrema_execute",
    "metric_rois_from_extrema_validate",
    "metric_rois_from_extrema",
]