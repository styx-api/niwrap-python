# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_INFORMATION_METADATA = Metadata(
    id="surface_information",
    name="DISPLAY INFORMATION ABOUT A SURFACE",
    package="workbench",
)

SurfaceInformation = typing.TypedDict(
    "SurfaceInformation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_information"]],
        "surface_file": InputPathType,
    },
)

@dataclasses.dataclass
class SurfaceInformationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_information_params(
    surface_file: InputPathType,
) -> SurfaceInformation:
    """
    Build parameters.

    Args:
        surface_file: Surface for which information is displayed

    Returns:
        Parameter dictionary.
    """
    params: SurfaceInformation = {
        "@type": "workbench/surface_information",
        "surface_file": surface_file,
    }
    return params

def surface_information_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceInformation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_file", None) is None:
        raise StyxValidationError("`surface_file` must not be None")
    if not isinstance(params["surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_file` has the wrong type: Received `{type(params["surface_file"])}` expected `InputPathType`')

def surface_information_cargs(params: SurfaceInformation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-information")
    cargs.append(execution.input_file(params["surface_file"]))
    return cargs

def surface_information_outputs(params: SurfaceInformation, execution: Execution) -> SurfaceInformationOutputs:
    """Build the SurfaceInformationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceInformationOutputs(
        root=root_v,
    )

def surface_information_execute(params: SurfaceInformation, runner: Runner | None = None) -> SurfaceInformationOutputs:
    """
    DISPLAY INFORMATION ABOUT A SURFACE

    Information about surface is displayed including vertices, 
triangles, bounding box, and spacing.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_information_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_INFORMATION_METADATA)
    execution.params(params)
    args = surface_information_cargs(params, execution)
    out = surface_information_outputs(params, execution)
    execution.run(args)
    return out

def surface_information(
    surface_file: InputPathType,
    runner: Runner | None = None,
) -> SurfaceInformationOutputs:
    """
    DISPLAY INFORMATION ABOUT A SURFACE

    Information about surface is displayed including vertices, 
triangles, bounding box, and spacing.

    Args:
        surface_file: Surface for which information is displayed
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_information_params(
        surface_file=surface_file,
    )
    return surface_information_execute(params, runner)

__all__ = [
    "SurfaceInformation",
    "SurfaceInformationOutputs",
    "SURFACE_INFORMATION_METADATA",
    "surface_information_cargs",
    "surface_information_outputs",
    "surface_information_params",
    "surface_information_execute",
    "surface_information_validate",
    "surface_information",
]