# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_CHANGE_MAPPING_METADATA = Metadata(
    id="cifti_change_mapping",
    name="CONVERT TO SCALAR, COPY MAPPING, ETC",
    package="workbench",
)

class CiftiChangeMappingSeries(typing.TypedDict):
    step: float
    """increment between series points"""
    start: float
    """start value of the series"""
    unit: typing.NotRequired[str]
    """select unit for series (default SECOND)"""

class CiftiChangeMappingScalar(typing.TypedDict):
    name_file: typing.NotRequired[str]
    """specify names for the maps"""

class CiftiChangeMappingFromCifti(typing.TypedDict):
    template_cifti: InputPathType
    """a cifti file containing the desired mapping"""
    direction: str
    """which direction to copy the mapping from"""

CiftiChangeMapping = typing.TypedDict(
    "CiftiChangeMapping",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_change_mapping"]],
        "data_cifti": InputPathType,
        "direction": str,
        "cifti_out": str,
        "series": typing.NotRequired[CiftiChangeMappingSeries],
        "scalar": typing.NotRequired[CiftiChangeMappingScalar],
        "from_cifti": typing.NotRequired[CiftiChangeMappingFromCifti],
    },
)

@dataclasses.dataclass
class CiftiChangeMappingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_change_mapping_params(
    data_cifti: InputPathType,
    direction: str,
    cifti_out: str,
    series: CiftiChangeMappingSeries | None = None,
    scalar: CiftiChangeMappingScalar | None = None,
    from_cifti: CiftiChangeMappingFromCifti | None = None,
) -> CiftiChangeMapping:
    """
    Build parameters.

    Args:
        data_cifti: the cifti file to use the data from
        direction: which direction on <data-cifti> to replace the mapping
        cifti_out: the output cifti file
        series: set the mapping to series
        scalar: set the mapping to scalar
        from_cifti: copy mapping from another cifti file

    Returns:
        Parameter dictionary.
    """
    params: CiftiChangeMapping = {
        "@type": "workbench/cifti_change_mapping",
        "data_cifti": data_cifti,
        "direction": direction,
        "cifti_out": cifti_out,
    }
    if series is not None:
        params["series"] = series
    if scalar is not None:
        params["scalar"] = scalar
    if from_cifti is not None:
        params["from_cifti"] = from_cifti
    return params

def cifti_change_mapping_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiChangeMapping."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("data_cifti", None) is None:
        raise StyxValidationError("`data_cifti` must not be None")
    if not isinstance(params["data_cifti"], (pathlib.Path, str)):
        raise StyxValidationError(f'`data_cifti` has the wrong type: Received `{type(params["data_cifti"])}` expected `InputPathType`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("series", None) is not None:
        if not isinstance(params["series"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["series"])}\'')
        if params["series"].get("step", None) is None:
            raise StyxValidationError("`step` must not be None")
        if not isinstance(params["series"]["step"], (float, int)):
            raise StyxValidationError(f'`step` has the wrong type: Received `{type(params["series"]["step"])}` expected `float`')
        if params["series"].get("start", None) is None:
            raise StyxValidationError("`start` must not be None")
        if not isinstance(params["series"]["start"], (float, int)):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["series"]["start"])}` expected `float`')
        if params["series"].get("unit", None) is not None:
            if not isinstance(params["series"]["unit"], str):
                raise StyxValidationError(f'`unit` has the wrong type: Received `{type(params["series"]["unit"])}` expected `str`')
    if params.get("scalar", None) is not None:
        if not isinstance(params["scalar"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["scalar"])}\'')
        if params["scalar"].get("name_file", None) is not None:
            if not isinstance(params["scalar"]["name_file"], str):
                raise StyxValidationError(f'`name_file` has the wrong type: Received `{type(params["scalar"]["name_file"])}` expected `str`')
    if params.get("from_cifti", None) is not None:
        if not isinstance(params["from_cifti"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["from_cifti"])}\'')
        if params["from_cifti"].get("template_cifti", None) is None:
            raise StyxValidationError("`template_cifti` must not be None")
        if not isinstance(params["from_cifti"]["template_cifti"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template_cifti` has the wrong type: Received `{type(params["from_cifti"]["template_cifti"])}` expected `InputPathType`')
        if params["from_cifti"].get("direction", None) is None:
            raise StyxValidationError("`direction` must not be None")
        if not isinstance(params["from_cifti"]["direction"], str):
            raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["from_cifti"]["direction"])}` expected `str`')

def cifti_change_mapping_cargs(params: CiftiChangeMapping, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-change-mapping")
    cargs.append(execution.input_file(params["data_cifti"]))
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("series")
    if v_0 is not None:
        cargs.append("-series")
        cargs.append(str(v_0["step"]))
        cargs.append(str(v_0["start"]))
        v_1 = v_0.get("unit")
        if v_1 is not None:
            cargs.append("-unit")
            cargs.append(v_1)
    v_2 = params.get("scalar")
    if v_2 is not None:
        cargs.append("-scalar")
        v_3 = v_2.get("name_file")
        if v_3 is not None:
            cargs.append("-name-file")
            cargs.append(v_3)
    v_4 = params.get("from_cifti")
    if v_4 is not None:
        cargs.append("-from-cifti")
        cargs.append(execution.input_file(v_4["template_cifti"]))
        cargs.append(v_4["direction"])
    return cargs

def cifti_change_mapping_outputs(params: CiftiChangeMapping, execution: Execution) -> CiftiChangeMappingOutputs:
    """Build the CiftiChangeMappingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiChangeMappingOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_change_mapping_execute(params: CiftiChangeMapping, runner: Runner | None = None) -> CiftiChangeMappingOutputs:
    """
    CONVERT TO SCALAR, COPY MAPPING, ETC

    Take an existing cifti file and change one of the mappings.  Exactly one of -series, -scalar, or -from-cifti must be specified.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

The argument to -unit must be one of the following:

SECOND
HERTZ
METER
RADIAN

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_change_mapping_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_CHANGE_MAPPING_METADATA)
    execution.params(params)
    args = cifti_change_mapping_cargs(params, execution)
    out = cifti_change_mapping_outputs(params, execution)
    execution.run(args)
    return out

def cifti_change_mapping(
    data_cifti: InputPathType,
    direction: str,
    cifti_out: str,
    series: CiftiChangeMappingSeries | None = None,
    scalar: CiftiChangeMappingScalar | None = None,
    from_cifti: CiftiChangeMappingFromCifti | None = None,
    runner: Runner | None = None,
) -> CiftiChangeMappingOutputs:
    """
    CONVERT TO SCALAR, COPY MAPPING, ETC

    Take an existing cifti file and change one of the mappings.  Exactly one of -series, -scalar, or -from-cifti must be specified.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

The argument to -unit must be one of the following:

SECOND
HERTZ
METER
RADIAN

    Args:
        data_cifti: the cifti file to use the data from
        direction: which direction on <data-cifti> to replace the mapping
        cifti_out: the output cifti file
        series: set the mapping to series
        scalar: set the mapping to scalar
        from_cifti: copy mapping from another cifti file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_change_mapping_params(
        data_cifti=data_cifti,
        direction=direction,
        cifti_out=cifti_out,
        series=series,
        scalar=scalar,
        from_cifti=from_cifti,
    )
    return cifti_change_mapping_execute(params, runner)

__all__ = [
    "CiftiChangeMapping",
    "CiftiChangeMappingOutputs",
    "CIFTI_CHANGE_MAPPING_METADATA",
    "cifti_change_mapping_cargs",
    "cifti_change_mapping_outputs",
    "cifti_change_mapping_params",
    "cifti_change_mapping_execute",
    "cifti_change_mapping_validate",
    "cifti_change_mapping",
]