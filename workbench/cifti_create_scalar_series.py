# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_CREATE_SCALAR_SERIES_METADATA = Metadata(
    id="cifti_create_scalar_series",
    name="IMPORT SERIES DATA INTO CIFTI",
    package="workbench",
)

class CiftiCreateScalarSeriesSeries(typing.TypedDict):
    unit: str
    """the unit to use"""
    start: float
    """the value at the first series point"""
    step: float
    """the interval between series points"""

CiftiCreateScalarSeries = typing.TypedDict(
    "CiftiCreateScalarSeries",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_create_scalar_series"]],
        "input": str,
        "cifti_out": str,
        "transpose": typing.NotRequired[bool],
        "name_file": typing.NotRequired[str],
        "series": typing.NotRequired[CiftiCreateScalarSeriesSeries],
    },
)

@dataclasses.dataclass
class CiftiCreateScalarSeriesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_create_scalar_series_params(
    input_: str,
    cifti_out: str,
    transpose: bool = False,
    name_file: str | None = None,
    series: CiftiCreateScalarSeriesSeries | None = None,
) -> CiftiCreateScalarSeries:
    """
    Build parameters.

    Args:
        input_: input file
        cifti_out: output cifti file
        transpose: use if the rows of the text file are along the scalar dimension
        name_file: use a text file to set names on scalar dimension
        series: set the units and values of the series

    Returns:
        Parameter dictionary.
    """
    params: CiftiCreateScalarSeries = {
        "@type": "workbench/cifti_create_scalar_series",
        "input": input_,
        "cifti_out": cifti_out,
        "transpose": transpose,
    }
    if name_file is not None:
        params["name_file"] = name_file
    if series is not None:
        params["series"] = series
    return params

def cifti_create_scalar_series_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiCreateScalarSeries."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], str):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("transpose", None) is not None:
        if not isinstance(params["transpose"], bool):
            raise StyxValidationError(f'`transpose` has the wrong type: Received `{type(params["transpose"])}` expected `bool`')
    if params.get("name_file", None) is not None:
        if not isinstance(params["name_file"], str):
            raise StyxValidationError(f'`name_file` has the wrong type: Received `{type(params["name_file"])}` expected `str`')
    if params.get("series", None) is not None:
        if not isinstance(params["series"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["series"])}\'')
        if params["series"].get("unit", None) is None:
            raise StyxValidationError("`unit` must not be None")
        if not isinstance(params["series"]["unit"], str):
            raise StyxValidationError(f'`unit` has the wrong type: Received `{type(params["series"]["unit"])}` expected `str`')
        if params["series"].get("start", None) is None:
            raise StyxValidationError("`start` must not be None")
        if not isinstance(params["series"]["start"], (float, int)):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["series"]["start"])}` expected `float`')
        if params["series"].get("step", None) is None:
            raise StyxValidationError("`step` must not be None")
        if not isinstance(params["series"]["step"], (float, int)):
            raise StyxValidationError(f'`step` has the wrong type: Received `{type(params["series"]["step"])}` expected `float`')

def cifti_create_scalar_series_cargs(params: CiftiCreateScalarSeries, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-create-scalar-series")
    cargs.append(params["input"])
    cargs.append(params["cifti_out"])
    if params.get("transpose"):
        cargs.append("-transpose")
    v_0 = params.get("name_file")
    if v_0 is not None:
        cargs.append("-name-file")
        cargs.append(v_0)
    v_1 = params.get("series")
    if v_1 is not None:
        cargs.append("-series")
        cargs.append(v_1["unit"])
        cargs.append(str(v_1["start"]))
        cargs.append(str(v_1["step"]))
    return cargs

def cifti_create_scalar_series_outputs(params: CiftiCreateScalarSeries, execution: Execution) -> CiftiCreateScalarSeriesOutputs:
    """Build the CiftiCreateScalarSeriesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiCreateScalarSeriesOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_create_scalar_series_execute(params: CiftiCreateScalarSeries, runner: Runner | None = None) -> CiftiCreateScalarSeriesOutputs:
    """
    IMPORT SERIES DATA INTO CIFTI

    Convert a text file containing series of equal length into a cifti file.  The text file should have lines made up of numbers separated by whitespace, with no extra newlines between lines.

The <unit> argument must be one of the following:

SECOND
HERTZ
METER
RADIAN

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_create_scalar_series_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_CREATE_SCALAR_SERIES_METADATA)
    execution.params(params)
    args = cifti_create_scalar_series_cargs(params, execution)
    out = cifti_create_scalar_series_outputs(params, execution)
    execution.run(args)
    return out

def cifti_create_scalar_series(
    input_: str,
    cifti_out: str,
    transpose: bool = False,
    name_file: str | None = None,
    series: CiftiCreateScalarSeriesSeries | None = None,
    runner: Runner | None = None,
) -> CiftiCreateScalarSeriesOutputs:
    """
    IMPORT SERIES DATA INTO CIFTI

    Convert a text file containing series of equal length into a cifti file.  The text file should have lines made up of numbers separated by whitespace, with no extra newlines between lines.

The <unit> argument must be one of the following:

SECOND
HERTZ
METER
RADIAN

    Args:
        input_: input file
        cifti_out: output cifti file
        transpose: use if the rows of the text file are along the scalar dimension
        name_file: use a text file to set names on scalar dimension
        series: set the units and values of the series
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_create_scalar_series_params(
        input_=input_,
        cifti_out=cifti_out,
        transpose=transpose,
        name_file=name_file,
        series=series,
    )
    return cifti_create_scalar_series_execute(params, runner)

__all__ = [
    "CiftiCreateScalarSeries",
    "CiftiCreateScalarSeriesOutputs",
    "CIFTI_CREATE_SCALAR_SERIES_METADATA",
    "cifti_create_scalar_series_cargs",
    "cifti_create_scalar_series_outputs",
    "cifti_create_scalar_series_params",
    "cifti_create_scalar_series_execute",
    "cifti_create_scalar_series_validate",
    "cifti_create_scalar_series",
]