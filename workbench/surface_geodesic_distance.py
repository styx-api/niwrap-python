# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_GEODESIC_DISTANCE_METADATA = Metadata(
    id="surface_geodesic_distance",
    name="COMPUTE GEODESIC DISTANCE FROM ONE VERTEX TO THE ENTIRE SURFACE",
    package="workbench",
)

SurfaceGeodesicDistance = typing.TypedDict(
    "SurfaceGeodesicDistance",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_geodesic_distance"]],
        "surface": InputPathType,
        "vertex": int,
        "metric_out": str,
        "naive": typing.NotRequired[bool],
        "limit": typing.NotRequired[float],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SurfaceGeodesicDistanceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def surface_geodesic_distance_params(
    surface: InputPathType,
    vertex: int,
    metric_out: str,
    naive: bool = False,
    limit: float | None = None,
    corrected_areas: InputPathType | None = None,
) -> SurfaceGeodesicDistance:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        vertex: the vertex to compute geodesic distance from
        metric_out: the output metric
        naive: use only neighbors, don't crawl triangles (not recommended)
        limit: stop at a certain distance
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: SurfaceGeodesicDistance = {
        "@type": "workbench/surface_geodesic_distance",
        "surface": surface,
        "vertex": vertex,
        "metric_out": metric_out,
        "naive": naive,
    }
    if limit is not None:
        params["limit"] = limit
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def surface_geodesic_distance_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceGeodesicDistance."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("vertex", None) is None:
        raise StyxValidationError("`vertex` must not be None")
    if not isinstance(params["vertex"], int):
        raise StyxValidationError(f'`vertex` has the wrong type: Received `{type(params["vertex"])}` expected `int`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("naive", None) is not None:
        if not isinstance(params["naive"], bool):
            raise StyxValidationError(f'`naive` has the wrong type: Received `{type(params["naive"])}` expected `bool`')
    if params.get("limit", None) is not None:
        if not isinstance(params["limit"], (float, int)):
            raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def surface_geodesic_distance_cargs(params: SurfaceGeodesicDistance, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-geodesic-distance")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["vertex"]))
    cargs.append(params["metric_out"])
    if params.get("naive"):
        cargs.append("-naive")
    v_0 = params.get("limit")
    if v_0 is not None:
        cargs.append("-limit")
        cargs.append(str(v_0))
    v_1 = params.get("corrected_areas")
    if v_1 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_1))
    return cargs

def surface_geodesic_distance_outputs(params: SurfaceGeodesicDistance, execution: Execution) -> SurfaceGeodesicDistanceOutputs:
    """Build the SurfaceGeodesicDistanceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return SurfaceGeodesicDistanceOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def surface_geodesic_distance_execute(params: SurfaceGeodesicDistance, runner: Runner | None = None) -> SurfaceGeodesicDistanceOutputs:
    """
    COMPUTE GEODESIC DISTANCE FROM ONE VERTEX TO THE ENTIRE SURFACE

    Unless -limit is specified, computes the geodesic distance from the specified vertex to all others.  The result is output as a single column metric file, with a value of -1 for vertices that the distance was not computed for.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_geodesic_distance_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_GEODESIC_DISTANCE_METADATA)
    execution.params(params)
    args = surface_geodesic_distance_cargs(params, execution)
    out = surface_geodesic_distance_outputs(params, execution)
    execution.run(args)
    return out

def surface_geodesic_distance(
    surface: InputPathType,
    vertex: int,
    metric_out: str,
    naive: bool = False,
    limit: float | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> SurfaceGeodesicDistanceOutputs:
    """
    COMPUTE GEODESIC DISTANCE FROM ONE VERTEX TO THE ENTIRE SURFACE

    Unless -limit is specified, computes the geodesic distance from the specified vertex to all others.  The result is output as a single column metric file, with a value of -1 for vertices that the distance was not computed for.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        surface: the surface to compute on
        vertex: the vertex to compute geodesic distance from
        metric_out: the output metric
        naive: use only neighbors, don't crawl triangles (not recommended)
        limit: stop at a certain distance
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_geodesic_distance_params(
        surface=surface,
        vertex=vertex,
        metric_out=metric_out,
        naive=naive,
        limit=limit,
        corrected_areas=corrected_areas,
    )
    return surface_geodesic_distance_execute(params, runner)

__all__ = [
    "SurfaceGeodesicDistance",
    "SurfaceGeodesicDistanceOutputs",
    "SURFACE_GEODESIC_DISTANCE_METADATA",
    "surface_geodesic_distance_cargs",
    "surface_geodesic_distance_outputs",
    "surface_geodesic_distance_params",
    "surface_geodesic_distance_execute",
    "surface_geodesic_distance_validate",
    "surface_geodesic_distance",
]