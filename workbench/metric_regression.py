# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_REGRESSION_METADATA = Metadata(
    id="metric_regression",
    name="REGRESS SPATIAL MAP OUT OF A METRIC FILE",
    package="workbench",
)

class MetricRegressionRemove(typing.TypedDict):
    metric: InputPathType
    """the metric file to use"""
    remove_column: typing.NotRequired[str]
    """select a column to use, rather than all"""

class MetricRegressionKeep(typing.TypedDict):
    metric: InputPathType
    """the metric file to use"""
    keep_column: typing.NotRequired[str]
    """select a column to use, rather than all"""

MetricRegression = typing.TypedDict(
    "MetricRegression",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_regression"]],
        "metric_in": InputPathType,
        "metric_out": str,
        "roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "remove": list[MetricRegressionRemove],
        "keep": list[MetricRegressionKeep],
    },
)

@dataclasses.dataclass
class MetricRegressionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_regression_params(
    metric_in: InputPathType,
    metric_out: str,
    remove: list[MetricRegressionRemove],
    keep: list[MetricRegressionKeep],
    roi: InputPathType | None = None,
    column: str | None = None,
) -> MetricRegression:
    """
    Build parameters.

    Args:
        metric_in: the metric to regress from
        metric_out: the output metric
        remove: specify a metric to regress out
        keep: specify a metric to include in regression, but not remove
        roi: only regress inside an roi
        column: select a single column to regress from

    Returns:
        Parameter dictionary.
    """
    params: MetricRegression = {
        "@type": "workbench/metric_regression",
        "metric_in": metric_in,
        "metric_out": metric_out,
        "remove": remove,
        "keep": keep,
    }
    if roi is not None:
        params["roi"] = roi
    if column is not None:
        params["column"] = column
    return params

def metric_regression_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricRegression."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("remove", None) is None:
        raise StyxValidationError("`remove` must not be None")
    if not isinstance(params["remove"], list):
        raise StyxValidationError(f'`remove` has the wrong type: Received `{type(params["remove"])}` expected `list[MetricRegressionRemove]`')
    for e in params["remove"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("metric", None) is None:
            raise StyxValidationError("`metric` must not be None")
        if not isinstance(e["metric"], (pathlib.Path, str)):
            raise StyxValidationError(f'`metric` has the wrong type: Received `{type(e["metric"])}` expected `InputPathType`')
        if e.get("remove_column", None) is not None:
            if not isinstance(e["remove_column"], str):
                raise StyxValidationError(f'`remove_column` has the wrong type: Received `{type(e["remove_column"])}` expected `str`')
    if params.get("keep", None) is None:
        raise StyxValidationError("`keep` must not be None")
    if not isinstance(params["keep"], list):
        raise StyxValidationError(f'`keep` has the wrong type: Received `{type(params["keep"])}` expected `list[MetricRegressionKeep]`')
    for e_2 in params["keep"]:
        if not isinstance(e_2, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
        if e_2.get("metric", None) is None:
            raise StyxValidationError("`metric` must not be None")
        if not isinstance(e_2["metric"], (pathlib.Path, str)):
            raise StyxValidationError(f'`metric` has the wrong type: Received `{type(e_2["metric"])}` expected `InputPathType`')
        if e_2.get("keep_column", None) is not None:
            if not isinstance(e_2["keep_column"], str):
                raise StyxValidationError(f'`keep_column` has the wrong type: Received `{type(e_2["keep_column"])}` expected `str`')

def metric_regression_cargs(params: MetricRegression, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-regression")
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    for item0 in params["remove"]:
        cargs.append("-remove")
        cargs.append(execution.input_file(item0["metric"]))
        v_2 = item0.get("remove_column")
        if v_2 is not None:
            cargs.append("-remove-column")
            cargs.append(v_2)
    for item1 in params["keep"]:
        cargs.append("-keep")
        cargs.append(execution.input_file(item1["metric"]))
        v_3 = item1.get("keep_column")
        if v_3 is not None:
            cargs.append("-keep-column")
            cargs.append(v_3)
    return cargs

def metric_regression_outputs(params: MetricRegression, execution: Execution) -> MetricRegressionOutputs:
    """Build the MetricRegressionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricRegressionOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_regression_execute(params: MetricRegression, runner: Runner | None = None) -> MetricRegressionOutputs:
    """
    REGRESS SPATIAL MAP OUT OF A METRIC FILE

    For each regressor, its mean across the surface is subtracted from its data.  Each input map is then regressed against these, and a constant term.  The resulting regressed slopes of all regressors specified with -remove are multiplied with their respective regressor maps, and these are subtracted from the input map.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_regression_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_REGRESSION_METADATA)
    execution.params(params)
    args = metric_regression_cargs(params, execution)
    out = metric_regression_outputs(params, execution)
    execution.run(args)
    return out

def metric_regression(
    metric_in: InputPathType,
    metric_out: str,
    remove: list[MetricRegressionRemove],
    keep: list[MetricRegressionKeep],
    roi: InputPathType | None = None,
    column: str | None = None,
    runner: Runner | None = None,
) -> MetricRegressionOutputs:
    """
    REGRESS SPATIAL MAP OUT OF A METRIC FILE

    For each regressor, its mean across the surface is subtracted from its data.  Each input map is then regressed against these, and a constant term.  The resulting regressed slopes of all regressors specified with -remove are multiplied with their respective regressor maps, and these are subtracted from the input map.

    Args:
        metric_in: the metric to regress from
        metric_out: the output metric
        remove: specify a metric to regress out
        keep: specify a metric to include in regression, but not remove
        roi: only regress inside an roi
        column: select a single column to regress from
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_regression_params(
        metric_in=metric_in,
        metric_out=metric_out,
        remove=remove,
        keep=keep,
        roi=roi,
        column=column,
    )
    return metric_regression_execute(params, runner)

__all__ = [
    "MetricRegression",
    "MetricRegressionOutputs",
    "METRIC_REGRESSION_METADATA",
    "metric_regression_cargs",
    "metric_regression_outputs",
    "metric_regression_params",
    "metric_regression_execute",
    "metric_regression_validate",
    "metric_regression",
]