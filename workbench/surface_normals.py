# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_NORMALS_METADATA = Metadata(
    id="surface_normals",
    name="OUTPUT VERTEX NORMALS AS METRIC FILE",
    package="workbench",
)

SurfaceNormals = typing.TypedDict(
    "SurfaceNormals",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_normals"]],
        "surface": InputPathType,
        "metric_out": str,
    },
)

@dataclasses.dataclass
class SurfaceNormalsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the normal vectors"""

def surface_normals_params(
    surface: InputPathType,
    metric_out: str,
) -> SurfaceNormals:
    """
    Build parameters.

    Args:
        surface: the surface to output the normals of
        metric_out: the normal vectors

    Returns:
        Parameter dictionary.
    """
    params: SurfaceNormals = {
        "@type": "workbench/surface_normals",
        "surface": surface,
        "metric_out": metric_out,
    }
    return params

def surface_normals_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceNormals."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')

def surface_normals_cargs(params: SurfaceNormals, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-normals")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["metric_out"])
    return cargs

def surface_normals_outputs(params: SurfaceNormals, execution: Execution) -> SurfaceNormalsOutputs:
    """Build the SurfaceNormalsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return SurfaceNormalsOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def surface_normals_execute(params: SurfaceNormals, runner: Runner | None = None) -> SurfaceNormalsOutputs:
    """
    OUTPUT VERTEX NORMALS AS METRIC FILE

    Computes the normal vectors of the surface file, and outputs them as a 3 column metric file.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_normals_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_NORMALS_METADATA)
    execution.params(params)
    args = surface_normals_cargs(params, execution)
    out = surface_normals_outputs(params, execution)
    execution.run(args)
    return out

def surface_normals(
    surface: InputPathType,
    metric_out: str,
    runner: Runner | None = None,
) -> SurfaceNormalsOutputs:
    """
    OUTPUT VERTEX NORMALS AS METRIC FILE

    Computes the normal vectors of the surface file, and outputs them as a 3 column metric file.

    Args:
        surface: the surface to output the normals of
        metric_out: the normal vectors
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_normals_params(
        surface=surface,
        metric_out=metric_out,
    )
    return surface_normals_execute(params, runner)

__all__ = [
    "SurfaceNormals",
    "SurfaceNormalsOutputs",
    "SURFACE_NORMALS_METADATA",
    "surface_normals_cargs",
    "surface_normals_outputs",
    "surface_normals_params",
    "surface_normals_execute",
    "surface_normals_validate",
    "surface_normals",
]