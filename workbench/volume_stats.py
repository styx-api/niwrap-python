# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_STATS_METADATA = Metadata(
    id="volume_stats",
    name="SPATIAL STATISTICS ON A VOLUME FILE",
    package="workbench",
)

class VolumeStatsRoi(typing.TypedDict):
    roi_volume: InputPathType
    """the roi, as a volume file"""
    match_maps: typing.NotRequired[bool]
    """each subvolume of input uses the corresponding subvolume from the roi file"""

VolumeStats = typing.TypedDict(
    "VolumeStats",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_stats"]],
        "volume_in": InputPathType,
        "reduce": typing.NotRequired[str],
        "percentile": typing.NotRequired[float],
        "subvolume": typing.NotRequired[str],
        "roi": typing.NotRequired[VolumeStatsRoi],
        "show_map_name": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VolumeStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def volume_stats_params(
    volume_in: InputPathType,
    reduce: str | None = None,
    percentile: float | None = None,
    subvolume: str | None = None,
    roi: VolumeStatsRoi | None = None,
    show_map_name: bool = False,
) -> VolumeStats:
    """
    Build parameters.

    Args:
        volume_in: the input volume
        reduce: use a reduction operation
        percentile: give the value at a percentile
        subvolume: only display output for one subvolume
        roi: only consider data inside an roi
        show_map_name: print map index and name before each output

    Returns:
        Parameter dictionary.
    """
    params: VolumeStats = {
        "@type": "workbench/volume_stats",
        "volume_in": volume_in,
        "show_map_name": show_map_name,
    }
    if reduce is not None:
        params["reduce"] = reduce
    if percentile is not None:
        params["percentile"] = percentile
    if subvolume is not None:
        params["subvolume"] = subvolume
    if roi is not None:
        params["roi"] = roi
    return params

def volume_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("reduce", None) is not None:
        if not isinstance(params["reduce"], str):
            raise StyxValidationError(f'`reduce` has the wrong type: Received `{type(params["reduce"])}` expected `str`')
    if params.get("percentile", None) is not None:
        if not isinstance(params["percentile"], (float, int)):
            raise StyxValidationError(f'`percentile` has the wrong type: Received `{type(params["percentile"])}` expected `float`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi"])}\'')
        if params["roi"].get("roi_volume", None) is None:
            raise StyxValidationError("`roi_volume` must not be None")
        if not isinstance(params["roi"]["roi_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi_volume` has the wrong type: Received `{type(params["roi"]["roi_volume"])}` expected `InputPathType`')
        if params["roi"].get("match_maps", None) is not None:
            if not isinstance(params["roi"]["match_maps"], bool):
                raise StyxValidationError(f'`match_maps` has the wrong type: Received `{type(params["roi"]["match_maps"])}` expected `bool`')
    if params.get("show_map_name", None) is not None:
        if not isinstance(params["show_map_name"], bool):
            raise StyxValidationError(f'`show_map_name` has the wrong type: Received `{type(params["show_map_name"])}` expected `bool`')

def volume_stats_cargs(params: VolumeStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-stats")
    cargs.append(execution.input_file(params["volume_in"]))
    v_0 = params.get("reduce")
    if v_0 is not None:
        cargs.append("-reduce")
        cargs.append(v_0)
    v_1 = params.get("percentile")
    if v_1 is not None:
        cargs.append("-percentile")
        cargs.append(str(v_1))
    v_2 = params.get("subvolume")
    if v_2 is not None:
        cargs.append("-subvolume")
        cargs.append(v_2)
    v_3 = params.get("roi")
    if v_3 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_3["roi_volume"]))
        if v_3.get("match_maps"):
            cargs.append("-match-maps")
    if params.get("show_map_name"):
        cargs.append("-show-map-name")
    return cargs

def volume_stats_outputs(params: VolumeStats, execution: Execution) -> VolumeStatsOutputs:
    """Build the VolumeStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolumeStatsOutputs(
        root=root_v,
    )

def volume_stats_execute(params: VolumeStats, runner: Runner | None = None) -> VolumeStatsOutputs:
    """
    SPATIAL STATISTICS ON A VOLUME FILE

    For each subvolume of the input, a line of text is printed, resulting from the specified reduction or percentile operation.  Use -subvolume to only give output for a single subvolume.  If the -roi option is used without -match-maps, then each line will contain as many numbers as there are maps in the ROI file, separated by tab characters.  Exactly one of -reduce or -percentile must be specified.

The argument to the -reduce option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_STATS_METADATA)
    execution.params(params)
    args = volume_stats_cargs(params, execution)
    out = volume_stats_outputs(params, execution)
    execution.run(args)
    return out

def volume_stats(
    volume_in: InputPathType,
    reduce: str | None = None,
    percentile: float | None = None,
    subvolume: str | None = None,
    roi: VolumeStatsRoi | None = None,
    show_map_name: bool = False,
    runner: Runner | None = None,
) -> VolumeStatsOutputs:
    """
    SPATIAL STATISTICS ON A VOLUME FILE

    For each subvolume of the input, a line of text is printed, resulting from the specified reduction or percentile operation.  Use -subvolume to only give output for a single subvolume.  If the -roi option is used without -match-maps, then each line will contain as many numbers as there are maps in the ROI file, separated by tab characters.  Exactly one of -reduce or -percentile must be specified.

The argument to the -reduce option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        volume_in: the input volume
        reduce: use a reduction operation
        percentile: give the value at a percentile
        subvolume: only display output for one subvolume
        roi: only consider data inside an roi
        show_map_name: print map index and name before each output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_stats_params(
        volume_in=volume_in,
        reduce=reduce,
        percentile=percentile,
        subvolume=subvolume,
        roi=roi,
        show_map_name=show_map_name,
    )
    return volume_stats_execute(params, runner)

__all__ = [
    "VolumeStats",
    "VolumeStatsOutputs",
    "VOLUME_STATS_METADATA",
    "volume_stats_cargs",
    "volume_stats_outputs",
    "volume_stats_params",
    "volume_stats_execute",
    "volume_stats_validate",
    "volume_stats",
]