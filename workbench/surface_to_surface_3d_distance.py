# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_TO_SURFACE_3D_DISTANCE_METADATA = Metadata(
    id="surface_to_surface_3d_distance",
    name="COMPUTE DISTANCE BETWEEN CORRESPONDING VERTICES",
    package="workbench",
)

class SurfaceToSurface3dDistanceVectors(typing.TypedDict):
    vectors_out: str
    """the output vectors"""

SurfaceToSurface3dDistance = typing.TypedDict(
    "SurfaceToSurface3dDistance",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_to_surface_3d_distance"]],
        "surface_comp": InputPathType,
        "surface_ref": InputPathType,
        "dists_out": str,
        "vectors": typing.NotRequired[SurfaceToSurface3dDistanceVectors],
    },
)

@dataclasses.dataclass
class SurfaceToSurface3dDistanceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    dists_out: OutputPathType
    """the output distances"""
    vectors_out: OutputPathType | None
    """the output vectors"""

def surface_to_surface_3d_distance_params(
    surface_comp: InputPathType,
    surface_ref: InputPathType,
    dists_out: str,
    vectors: SurfaceToSurface3dDistanceVectors | None = None,
) -> SurfaceToSurface3dDistance:
    """
    Build parameters.

    Args:
        surface_comp: the surface to compare to the reference
        surface_ref: the surface to use as the reference
        dists_out: the output distances
        vectors: output the displacement vectors

    Returns:
        Parameter dictionary.
    """
    params: SurfaceToSurface3dDistance = {
        "@type": "workbench/surface_to_surface_3d_distance",
        "surface_comp": surface_comp,
        "surface_ref": surface_ref,
        "dists_out": dists_out,
    }
    if vectors is not None:
        params["vectors"] = vectors
    return params

def surface_to_surface_3d_distance_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceToSurface3dDistance."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_comp", None) is None:
        raise StyxValidationError("`surface_comp` must not be None")
    if not isinstance(params["surface_comp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_comp` has the wrong type: Received `{type(params["surface_comp"])}` expected `InputPathType`')
    if params.get("surface_ref", None) is None:
        raise StyxValidationError("`surface_ref` must not be None")
    if not isinstance(params["surface_ref"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_ref` has the wrong type: Received `{type(params["surface_ref"])}` expected `InputPathType`')
    if params.get("dists_out", None) is None:
        raise StyxValidationError("`dists_out` must not be None")
    if not isinstance(params["dists_out"], str):
        raise StyxValidationError(f'`dists_out` has the wrong type: Received `{type(params["dists_out"])}` expected `str`')
    if params.get("vectors", None) is not None:
        if not isinstance(params["vectors"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["vectors"])}\'')
        if params["vectors"].get("vectors_out", None) is None:
            raise StyxValidationError("`vectors_out` must not be None")
        if not isinstance(params["vectors"]["vectors_out"], str):
            raise StyxValidationError(f'`vectors_out` has the wrong type: Received `{type(params["vectors"]["vectors_out"])}` expected `str`')

def surface_to_surface_3d_distance_cargs(params: SurfaceToSurface3dDistance, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-to-surface-3d-distance")
    cargs.append(execution.input_file(params["surface_comp"]))
    cargs.append(execution.input_file(params["surface_ref"]))
    cargs.append(params["dists_out"])
    v_0 = params.get("vectors")
    if v_0 is not None:
        cargs.append("-vectors")
        cargs.append(v_0["vectors_out"])
    return cargs

def surface_to_surface_3d_distance_outputs(params: SurfaceToSurface3dDistance, execution: Execution) -> SurfaceToSurface3dDistanceOutputs:
    """Build the SurfaceToSurface3dDistanceOutputs object for this tool."""
    vectors_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    dists_out_v: OutputPathType = execution.output_file(params["dists_out"])
    __v0 = params.get("vectors")
    if __v0 is not None:
        vectors_out_v = execution.output_file(__v0["vectors_out"])
    return SurfaceToSurface3dDistanceOutputs(
        root=root_v,
        dists_out=dists_out_v,
        vectors_out=vectors_out_v,
    )

def surface_to_surface_3d_distance_execute(params: SurfaceToSurface3dDistance, runner: Runner | None = None) -> SurfaceToSurface3dDistanceOutputs:
    """
    COMPUTE DISTANCE BETWEEN CORRESPONDING VERTICES

    Computes the vector difference between the vertices of each surface with the same index, as (comp - ref), and output the magnitudes, and optionally the displacement vectors.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_to_surface_3d_distance_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_TO_SURFACE_3D_DISTANCE_METADATA)
    execution.params(params)
    args = surface_to_surface_3d_distance_cargs(params, execution)
    out = surface_to_surface_3d_distance_outputs(params, execution)
    execution.run(args)
    return out

def surface_to_surface_3d_distance(
    surface_comp: InputPathType,
    surface_ref: InputPathType,
    dists_out: str,
    vectors: SurfaceToSurface3dDistanceVectors | None = None,
    runner: Runner | None = None,
) -> SurfaceToSurface3dDistanceOutputs:
    """
    COMPUTE DISTANCE BETWEEN CORRESPONDING VERTICES

    Computes the vector difference between the vertices of each surface with the same index, as (comp - ref), and output the magnitudes, and optionally the displacement vectors.

    Args:
        surface_comp: the surface to compare to the reference
        surface_ref: the surface to use as the reference
        dists_out: the output distances
        vectors: output the displacement vectors
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_to_surface_3d_distance_params(
        surface_comp=surface_comp,
        surface_ref=surface_ref,
        dists_out=dists_out,
        vectors=vectors,
    )
    return surface_to_surface_3d_distance_execute(params, runner)

__all__ = [
    "SurfaceToSurface3dDistance",
    "SurfaceToSurface3dDistanceOutputs",
    "SURFACE_TO_SURFACE_3D_DISTANCE_METADATA",
    "surface_to_surface_3d_distance_cargs",
    "surface_to_surface_3d_distance_outputs",
    "surface_to_surface_3d_distance_params",
    "surface_to_surface_3d_distance_execute",
    "surface_to_surface_3d_distance_validate",
    "surface_to_surface_3d_distance",
]