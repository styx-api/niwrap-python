# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_COMPONENTS_TO_FRAMES_METADATA = Metadata(
    id="volume_components_to_frames",
    name="CONVERT RGB/COMPLEX VOLUME TO FRAMES",
    package="workbench",
)

VolumeComponentsToFrames = typing.TypedDict(
    "VolumeComponentsToFrames",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_components_to_frames"]],
        "input": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class VolumeComponentsToFramesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the input volume converted to multiple frames of scalar type"""

def volume_components_to_frames_params(
    input_: InputPathType,
    output: str,
) -> VolumeComponentsToFrames:
    """
    Build parameters.

    Args:
        input_: the RGB/complex-type volume
        output: the input volume converted to multiple frames of scalar type

    Returns:
        Parameter dictionary.
    """
    params: VolumeComponentsToFrames = {
        "@type": "workbench/volume_components_to_frames",
        "input": input_,
        "output": output,
    }
    return params

def volume_components_to_frames_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeComponentsToFrames."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def volume_components_to_frames_cargs(params: VolumeComponentsToFrames, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-components-to-frames")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["output"])
    return cargs

def volume_components_to_frames_outputs(params: VolumeComponentsToFrames, execution: Execution) -> VolumeComponentsToFramesOutputs:
    """Build the VolumeComponentsToFramesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return VolumeComponentsToFramesOutputs(
        root=root_v,
        output=output_v,
    )

def volume_components_to_frames_execute(params: VolumeComponentsToFrames, runner: Runner | None = None) -> VolumeComponentsToFramesOutputs:
    """
    CONVERT RGB/COMPLEX VOLUME TO FRAMES

    RGB and complex datatypes are not always well supported, this command allows separating them into standard subvolumes for better support.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_components_to_frames_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_COMPONENTS_TO_FRAMES_METADATA)
    execution.params(params)
    args = volume_components_to_frames_cargs(params, execution)
    out = volume_components_to_frames_outputs(params, execution)
    execution.run(args)
    return out

def volume_components_to_frames(
    input_: InputPathType,
    output: str,
    runner: Runner | None = None,
) -> VolumeComponentsToFramesOutputs:
    """
    CONVERT RGB/COMPLEX VOLUME TO FRAMES

    RGB and complex datatypes are not always well supported, this command allows separating them into standard subvolumes for better support.

    Args:
        input_: the RGB/complex-type volume
        output: the input volume converted to multiple frames of scalar type
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_components_to_frames_params(
        input_=input_,
        output=output,
    )
    return volume_components_to_frames_execute(params, runner)

__all__ = [
    "VolumeComponentsToFrames",
    "VolumeComponentsToFramesOutputs",
    "VOLUME_COMPONENTS_TO_FRAMES_METADATA",
    "volume_components_to_frames_cargs",
    "volume_components_to_frames_outputs",
    "volume_components_to_frames_params",
    "volume_components_to_frames_execute",
    "volume_components_to_frames_validate",
    "volume_components_to_frames",
]