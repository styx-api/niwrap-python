# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_MERGE_PARCELS_METADATA = Metadata(
    id="cifti_merge_parcels",
    name="MERGE CIFTI FILES ALONG PARCELS DIMENSION",
    package="workbench",
)

CiftiMergeParcels = typing.TypedDict(
    "CiftiMergeParcels",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_merge_parcels"]],
        "direction": str,
        "cifti_out": str,
        "cifti": list[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiMergeParcelsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_merge_parcels_params(
    direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
) -> CiftiMergeParcels:
    """
    Build parameters.

    Args:
        direction: which dimension to merge along (integer, 'ROW', or 'COLUMN')
        cifti_out: the output cifti file
        cifti: specify an input cifti file

    Returns:
        Parameter dictionary.
    """
    params: CiftiMergeParcels = {
        "@type": "workbench/cifti_merge_parcels",
        "direction": direction,
        "cifti_out": cifti_out,
        "cifti": cifti,
    }
    return params

def cifti_merge_parcels_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiMergeParcels."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], list):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `list[InputPathType]`')
    for e in params["cifti"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def cifti_merge_parcels_cargs(params: CiftiMergeParcels, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-merge-parcels")
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    for item0 in params["cifti"]:
        cargs.append("-cifti")
        cargs.append(execution.input_file(item0))
    return cargs

def cifti_merge_parcels_outputs(params: CiftiMergeParcels, execution: Execution) -> CiftiMergeParcelsOutputs:
    """Build the CiftiMergeParcelsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiMergeParcelsOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_merge_parcels_execute(params: CiftiMergeParcels, runner: Runner | None = None) -> CiftiMergeParcelsOutputs:
    """
    MERGE CIFTI FILES ALONG PARCELS DIMENSION

    The input cifti files must have matching mappings along the direction not specified, and the mapping along the specified direction must be parcels.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_merge_parcels_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_MERGE_PARCELS_METADATA)
    execution.params(params)
    args = cifti_merge_parcels_cargs(params, execution)
    out = cifti_merge_parcels_outputs(params, execution)
    execution.run(args)
    return out

def cifti_merge_parcels(
    direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
    runner: Runner | None = None,
) -> CiftiMergeParcelsOutputs:
    """
    MERGE CIFTI FILES ALONG PARCELS DIMENSION

    The input cifti files must have matching mappings along the direction not specified, and the mapping along the specified direction must be parcels.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

    Args:
        direction: which dimension to merge along (integer, 'ROW', or 'COLUMN')
        cifti_out: the output cifti file
        cifti: specify an input cifti file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_merge_parcels_params(
        direction=direction,
        cifti_out=cifti_out,
        cifti=cifti,
    )
    return cifti_merge_parcels_execute(params, runner)

__all__ = [
    "CiftiMergeParcels",
    "CiftiMergeParcelsOutputs",
    "CIFTI_MERGE_PARCELS_METADATA",
    "cifti_merge_parcels_cargs",
    "cifti_merge_parcels_outputs",
    "cifti_merge_parcels_params",
    "cifti_merge_parcels_execute",
    "cifti_merge_parcels_validate",
    "cifti_merge_parcels",
]