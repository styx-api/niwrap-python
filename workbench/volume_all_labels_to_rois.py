# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_ALL_LABELS_TO_ROIS_METADATA = Metadata(
    id="volume_all_labels_to_rois",
    name="MAKE ROIS FROM ALL LABELS IN A VOLUME FRAME",
    package="workbench",
)

VolumeAllLabelsToRois = typing.TypedDict(
    "VolumeAllLabelsToRois",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_all_labels_to_rois"]],
        "label_in": InputPathType,
        "map": str,
        "volume_out": str,
    },
)

@dataclasses.dataclass
class VolumeAllLabelsToRoisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume file"""

def volume_all_labels_to_rois_params(
    label_in: InputPathType,
    map: str,
    volume_out: str,
) -> VolumeAllLabelsToRois:
    """
    Build parameters.

    Args:
        label_in: the input volume label file
        map: the number or name of the label map to use
        volume_out: the output volume file

    Returns:
        Parameter dictionary.
    """
    params: VolumeAllLabelsToRois = {
        "@type": "workbench/volume_all_labels_to_rois",
        "label_in": label_in,
        "map": map,
        "volume_out": volume_out,
    }
    return params

def volume_all_labels_to_rois_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeAllLabelsToRois."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')

def volume_all_labels_to_rois_cargs(params: VolumeAllLabelsToRois, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-all-labels-to-rois")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["map"])
    cargs.append(params["volume_out"])
    return cargs

def volume_all_labels_to_rois_outputs(params: VolumeAllLabelsToRois, execution: Execution) -> VolumeAllLabelsToRoisOutputs:
    """Build the VolumeAllLabelsToRoisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeAllLabelsToRoisOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_all_labels_to_rois_execute(params: VolumeAllLabelsToRois, runner: Runner | None = None) -> VolumeAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A VOLUME FRAME

    The output volume has a frame for each label in the specified input frame, other than the ??? label, each of which contains an ROI of all voxels that are set to the corresponding label.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_all_labels_to_rois_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_ALL_LABELS_TO_ROIS_METADATA)
    execution.params(params)
    args = volume_all_labels_to_rois_cargs(params, execution)
    out = volume_all_labels_to_rois_outputs(params, execution)
    execution.run(args)
    return out

def volume_all_labels_to_rois(
    label_in: InputPathType,
    map: str,
    volume_out: str,
    runner: Runner | None = None,
) -> VolumeAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A VOLUME FRAME

    The output volume has a frame for each label in the specified input frame, other than the ??? label, each of which contains an ROI of all voxels that are set to the corresponding label.

    Args:
        label_in: the input volume label file
        map: the number or name of the label map to use
        volume_out: the output volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_all_labels_to_rois_params(
        label_in=label_in,
        map=map,
        volume_out=volume_out,
    )
    return volume_all_labels_to_rois_execute(params, runner)

__all__ = [
    "VolumeAllLabelsToRois",
    "VolumeAllLabelsToRoisOutputs",
    "VOLUME_ALL_LABELS_TO_ROIS_METADATA",
    "volume_all_labels_to_rois_cargs",
    "volume_all_labels_to_rois_outputs",
    "volume_all_labels_to_rois_params",
    "volume_all_labels_to_rois_execute",
    "volume_all_labels_to_rois_validate",
    "volume_all_labels_to_rois",
]