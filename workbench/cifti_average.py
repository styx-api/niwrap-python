# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_AVERAGE_METADATA = Metadata(
    id="cifti_average",
    name="AVERAGE CIFTI FILES",
    package="workbench",
)

class CiftiAverageExcludeOutliers(typing.TypedDict):
    sigma_below: float
    """number of standard deviations below the mean to include"""
    sigma_above: float
    """number of standard deviations above the mean to include"""

class CiftiAverageCifti(typing.TypedDict):
    cifti_in: InputPathType
    """the input cifti file"""
    weight: typing.NotRequired[float]
    """give a weight for this file"""

CiftiAverage = typing.TypedDict(
    "CiftiAverage",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_average"]],
        "cifti_out": str,
        "exclude_outliers": typing.NotRequired[CiftiAverageExcludeOutliers],
        "mem_limit": typing.NotRequired[float],
        "cifti": list[CiftiAverageCifti],
    },
)

@dataclasses.dataclass
class CiftiAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_average_params(
    cifti_out: str,
    cifti: list[CiftiAverageCifti],
    exclude_outliers: CiftiAverageExcludeOutliers | None = None,
    mem_limit: float | None = None,
) -> CiftiAverage:
    """
    Build parameters.

    Args:
        cifti_out: output cifti file
        cifti: specify an input file
        exclude_outliers: exclude outliers by standard deviation of each element\
            across files
        mem_limit: restrict memory used for file reading efficiency

    Returns:
        Parameter dictionary.
    """
    params: CiftiAverage = {
        "@type": "workbench/cifti_average",
        "cifti_out": cifti_out,
        "cifti": cifti,
    }
    if exclude_outliers is not None:
        params["exclude_outliers"] = exclude_outliers
    if mem_limit is not None:
        params["mem_limit"] = mem_limit
    return params

def cifti_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("exclude_outliers", None) is not None:
        if not isinstance(params["exclude_outliers"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_outliers"])}\'')
        if params["exclude_outliers"].get("sigma_below", None) is None:
            raise StyxValidationError("`sigma_below` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_below"], (float, int)):
            raise StyxValidationError(f'`sigma_below` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_below"])}` expected `float`')
        if params["exclude_outliers"].get("sigma_above", None) is None:
            raise StyxValidationError("`sigma_above` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_above"], (float, int)):
            raise StyxValidationError(f'`sigma_above` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_above"])}` expected `float`')
    if params.get("mem_limit", None) is not None:
        if not isinstance(params["mem_limit"], (float, int)):
            raise StyxValidationError(f'`mem_limit` has the wrong type: Received `{type(params["mem_limit"])}` expected `float`')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], list):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `list[CiftiAverageCifti]`')
    for e in params["cifti"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("cifti_in", None) is None:
            raise StyxValidationError("`cifti_in` must not be None")
        if not isinstance(e["cifti_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(e["cifti_in"])}` expected `InputPathType`')
        if e.get("weight", None) is not None:
            if not isinstance(e["weight"], (float, int)):
                raise StyxValidationError(f'`weight` has the wrong type: Received `{type(e["weight"])}` expected `float`')

def cifti_average_cargs(params: CiftiAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-average")
    cargs.append(params["cifti_out"])
    v_0 = params.get("exclude_outliers")
    if v_0 is not None:
        cargs.append("-exclude-outliers")
        cargs.append(str(v_0["sigma_below"]))
        cargs.append(str(v_0["sigma_above"]))
    v_1 = params.get("mem_limit")
    if v_1 is not None:
        cargs.append("-mem-limit")
        cargs.append(str(v_1))
    for item0 in params["cifti"]:
        cargs.append("-cifti")
        cargs.append(execution.input_file(item0["cifti_in"]))
        v_2 = item0.get("weight")
        if v_2 is not None:
            cargs.append("-weight")
            cargs.append(str(v_2))
    return cargs

def cifti_average_outputs(params: CiftiAverage, execution: Execution) -> CiftiAverageOutputs:
    """Build the CiftiAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiAverageOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_average_execute(params: CiftiAverage, runner: Runner | None = None) -> CiftiAverageOutputs:
    """
    AVERAGE CIFTI FILES

    Averages cifti files together.  Files without -weight specified are given a weight of 1.  If -exclude-outliers is specified, at each element, the data across all files is taken as a set, its unweighted mean and sample standard deviation are found, and values outside the specified number of standard deviations are excluded from the (potentially weighted) average at that element.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_AVERAGE_METADATA)
    execution.params(params)
    args = cifti_average_cargs(params, execution)
    out = cifti_average_outputs(params, execution)
    execution.run(args)
    return out

def cifti_average(
    cifti_out: str,
    cifti: list[CiftiAverageCifti],
    exclude_outliers: CiftiAverageExcludeOutliers | None = None,
    mem_limit: float | None = None,
    runner: Runner | None = None,
) -> CiftiAverageOutputs:
    """
    AVERAGE CIFTI FILES

    Averages cifti files together.  Files without -weight specified are given a weight of 1.  If -exclude-outliers is specified, at each element, the data across all files is taken as a set, its unweighted mean and sample standard deviation are found, and values outside the specified number of standard deviations are excluded from the (potentially weighted) average at that element.

    Args:
        cifti_out: output cifti file
        cifti: specify an input file
        exclude_outliers: exclude outliers by standard deviation of each element\
            across files
        mem_limit: restrict memory used for file reading efficiency
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_average_params(
        cifti_out=cifti_out,
        cifti=cifti,
        exclude_outliers=exclude_outliers,
        mem_limit=mem_limit,
    )
    return cifti_average_execute(params, runner)

__all__ = [
    "CiftiAverage",
    "CiftiAverageOutputs",
    "CIFTI_AVERAGE_METADATA",
    "cifti_average_cargs",
    "cifti_average_outputs",
    "cifti_average_params",
    "cifti_average_execute",
    "cifti_average_validate",
    "cifti_average",
]