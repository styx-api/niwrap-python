# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_ROIS_FROM_EXTREMA_METADATA = Metadata(
    id="volume_rois_from_extrema",
    name="CREATE VOLUME ROI MAPS FROM EXTREMA MAPS",
    package="workbench",
)

VolumeRoisFromExtrema = typing.TypedDict(
    "VolumeRoisFromExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_rois_from_extrema"]],
        "volume_in": InputPathType,
        "limit": float,
        "volume_out": str,
        "gaussian": typing.NotRequired[float],
        "roi": typing.NotRequired[InputPathType],
        "overlap_logic": typing.NotRequired[str],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeRoisFromExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_rois_from_extrema_params(
    volume_in: InputPathType,
    limit: float,
    volume_out: str,
    gaussian: float | None = None,
    roi: InputPathType | None = None,
    overlap_logic: str | None = None,
    subvolume: str | None = None,
) -> VolumeRoisFromExtrema:
    """
    Build parameters.

    Args:
        volume_in: the input volume
        limit: distance limit from voxel center, in mm
        volume_out: the output volume
        gaussian: generate a gaussian kernel instead of a flat ROI
        roi: select a region of interest to use
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        subvolume: select a single subvolume to take the gradient of

    Returns:
        Parameter dictionary.
    """
    params: VolumeRoisFromExtrema = {
        "@type": "workbench/volume_rois_from_extrema",
        "volume_in": volume_in,
        "limit": limit,
        "volume_out": volume_out,
    }
    if gaussian is not None:
        params["gaussian"] = gaussian
    if roi is not None:
        params["roi"] = roi
    if overlap_logic is not None:
        params["overlap_logic"] = overlap_logic
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_rois_from_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeRoisFromExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("limit", None) is None:
        raise StyxValidationError("`limit` must not be None")
    if not isinstance(params["limit"], (float, int)):
        raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("gaussian", None) is not None:
        if not isinstance(params["gaussian"], (float, int)):
            raise StyxValidationError(f'`gaussian` has the wrong type: Received `{type(params["gaussian"])}` expected `float`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("overlap_logic", None) is not None:
        if not isinstance(params["overlap_logic"], str):
            raise StyxValidationError(f'`overlap_logic` has the wrong type: Received `{type(params["overlap_logic"])}` expected `str`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_rois_from_extrema_cargs(params: VolumeRoisFromExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-rois-from-extrema")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(str(params["limit"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("gaussian")
    if v_0 is not None:
        cargs.append("-gaussian")
        cargs.append(str(v_0))
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("overlap_logic")
    if v_2 is not None:
        cargs.append("-overlap-logic")
        cargs.append(v_2)
    v_3 = params.get("subvolume")
    if v_3 is not None:
        cargs.append("-subvolume")
        cargs.append(v_3)
    return cargs

def volume_rois_from_extrema_outputs(params: VolumeRoisFromExtrema, execution: Execution) -> VolumeRoisFromExtremaOutputs:
    """Build the VolumeRoisFromExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeRoisFromExtremaOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_rois_from_extrema_execute(params: VolumeRoisFromExtrema, runner: Runner | None = None) -> VolumeRoisFromExtremaOutputs:
    """
    CREATE VOLUME ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_rois_from_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_ROIS_FROM_EXTREMA_METADATA)
    execution.params(params)
    args = volume_rois_from_extrema_cargs(params, execution)
    out = volume_rois_from_extrema_outputs(params, execution)
    execution.run(args)
    return out

def volume_rois_from_extrema(
    volume_in: InputPathType,
    limit: float,
    volume_out: str,
    gaussian: float | None = None,
    roi: InputPathType | None = None,
    overlap_logic: str | None = None,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeRoisFromExtremaOutputs:
    """
    CREATE VOLUME ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        volume_in: the input volume
        limit: distance limit from voxel center, in mm
        volume_out: the output volume
        gaussian: generate a gaussian kernel instead of a flat ROI
        roi: select a region of interest to use
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        subvolume: select a single subvolume to take the gradient of
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_rois_from_extrema_params(
        volume_in=volume_in,
        limit=limit,
        volume_out=volume_out,
        gaussian=gaussian,
        roi=roi,
        overlap_logic=overlap_logic,
        subvolume=subvolume,
    )
    return volume_rois_from_extrema_execute(params, runner)

__all__ = [
    "VolumeRoisFromExtrema",
    "VolumeRoisFromExtremaOutputs",
    "VOLUME_ROIS_FROM_EXTREMA_METADATA",
    "volume_rois_from_extrema_cargs",
    "volume_rois_from_extrema_outputs",
    "volume_rois_from_extrema_params",
    "volume_rois_from_extrema_execute",
    "volume_rois_from_extrema_validate",
    "volume_rois_from_extrema",
]