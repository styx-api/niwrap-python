# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_TO_ROIS_METADATA = Metadata(
    id="border_to_rois",
    name="MAKE METRIC ROIS FROM BORDERS",
    package="workbench",
)

BorderToRois = typing.TypedDict(
    "BorderToRois",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_to_rois"]],
        "surface": InputPathType,
        "border_file": InputPathType,
        "metric_out": str,
        "border": typing.NotRequired[str],
        "inverse": typing.NotRequired[bool],
        "include_border": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BorderToRoisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric file"""

def border_to_rois_params(
    surface: InputPathType,
    border_file: InputPathType,
    metric_out: str,
    border: str | None = None,
    inverse: bool = False,
    include_border: bool = False,
) -> BorderToRois:
    """
    Build parameters.

    Args:
        surface: the surface the borders are drawn on
        border_file: the border file
        metric_out: the output metric file
        border: create ROI for only one border
        inverse: use inverse selection (outside border)
        include_border: include vertices the border is closest to

    Returns:
        Parameter dictionary.
    """
    params: BorderToRois = {
        "@type": "workbench/border_to_rois",
        "surface": surface,
        "border_file": border_file,
        "metric_out": metric_out,
        "inverse": inverse,
        "include_border": include_border,
    }
    if border is not None:
        params["border"] = border
    return params

def border_to_rois_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderToRois."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("border_file", None) is None:
        raise StyxValidationError("`border_file` must not be None")
    if not isinstance(params["border_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`border_file` has the wrong type: Received `{type(params["border_file"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("border", None) is not None:
        if not isinstance(params["border"], str):
            raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `str`')
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')
    if params.get("include_border", None) is not None:
        if not isinstance(params["include_border"], bool):
            raise StyxValidationError(f'`include_border` has the wrong type: Received `{type(params["include_border"])}` expected `bool`')

def border_to_rois_cargs(params: BorderToRois, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-to-rois")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["border_file"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("border")
    if v_0 is not None:
        cargs.append("-border")
        cargs.append(v_0)
    if params.get("inverse"):
        cargs.append("-inverse")
    if params.get("include_border"):
        cargs.append("-include-border")
    return cargs

def border_to_rois_outputs(params: BorderToRois, execution: Execution) -> BorderToRoisOutputs:
    """Build the BorderToRoisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return BorderToRoisOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def border_to_rois_execute(params: BorderToRois, runner: Runner | None = None) -> BorderToRoisOutputs:
    """
    MAKE METRIC ROIS FROM BORDERS

    By default, draws ROIs inside all borders in the border file, as separate metric columns.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_to_rois_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_TO_ROIS_METADATA)
    execution.params(params)
    args = border_to_rois_cargs(params, execution)
    out = border_to_rois_outputs(params, execution)
    execution.run(args)
    return out

def border_to_rois(
    surface: InputPathType,
    border_file: InputPathType,
    metric_out: str,
    border: str | None = None,
    inverse: bool = False,
    include_border: bool = False,
    runner: Runner | None = None,
) -> BorderToRoisOutputs:
    """
    MAKE METRIC ROIS FROM BORDERS

    By default, draws ROIs inside all borders in the border file, as separate metric columns.

    Args:
        surface: the surface the borders are drawn on
        border_file: the border file
        metric_out: the output metric file
        border: create ROI for only one border
        inverse: use inverse selection (outside border)
        include_border: include vertices the border is closest to
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_to_rois_params(
        surface=surface,
        border_file=border_file,
        metric_out=metric_out,
        border=border,
        inverse=inverse,
        include_border=include_border,
    )
    return border_to_rois_execute(params, runner)

__all__ = [
    "BorderToRois",
    "BorderToRoisOutputs",
    "BORDER_TO_ROIS_METADATA",
    "border_to_rois_cargs",
    "border_to_rois_outputs",
    "border_to_rois_params",
    "border_to_rois_execute",
    "border_to_rois_validate",
    "border_to_rois",
]