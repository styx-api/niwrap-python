# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_ERODE_METADATA = Metadata(
    id="label_erode",
    name="ERODE A LABEL FILE",
    package="workbench",
)

LabelErode = typing.TypedDict(
    "LabelErode",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_erode"]],
        "label": InputPathType,
        "surface": InputPathType,
        "erode_dist": float,
        "label_out": str,
        "roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class LabelErodeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label file"""

def label_erode_params(
    label: InputPathType,
    surface: InputPathType,
    erode_dist: float,
    label_out: str,
    roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
) -> LabelErode:
    """
    Build parameters.

    Args:
        label: the input label
        surface: the surface to erode on
        erode_dist: distance in mm to erode the labels
        label_out: the output label file
        roi: assume values outside this roi are labeled
        column: select a single column to erode
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: LabelErode = {
        "@type": "workbench/label_erode",
        "label": label,
        "surface": surface,
        "erode_dist": erode_dist,
        "label_out": label_out,
    }
    if roi is not None:
        params["roi"] = roi
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def label_erode_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelErode."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("erode_dist", None) is None:
        raise StyxValidationError("`erode_dist` must not be None")
    if not isinstance(params["erode_dist"], (float, int)):
        raise StyxValidationError(f'`erode_dist` has the wrong type: Received `{type(params["erode_dist"])}` expected `float`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def label_erode_cargs(params: LabelErode, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-erode")
    cargs.append(execution.input_file(params["label"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["erode_dist"]))
    cargs.append(params["label_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    v_2 = params.get("corrected_areas")
    if v_2 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_2))
    return cargs

def label_erode_outputs(params: LabelErode, execution: Execution) -> LabelErodeOutputs:
    """Build the LabelErodeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return LabelErodeOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def label_erode_execute(params: LabelErode, runner: Runner | None = None) -> LabelErodeOutputs:
    """
    ERODE A LABEL FILE

    Around each vertex that is unlabeled, set surrounding vertices to unlabeled.  The surrounding vertices are all immediate neighbors and all vertices within the specified distance.

Note that the -corrected-areas option uses an approximate correction for distance along the surface.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_erode_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_ERODE_METADATA)
    execution.params(params)
    args = label_erode_cargs(params, execution)
    out = label_erode_outputs(params, execution)
    execution.run(args)
    return out

def label_erode(
    label: InputPathType,
    surface: InputPathType,
    erode_dist: float,
    label_out: str,
    roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> LabelErodeOutputs:
    """
    ERODE A LABEL FILE

    Around each vertex that is unlabeled, set surrounding vertices to unlabeled.  The surrounding vertices are all immediate neighbors and all vertices within the specified distance.

Note that the -corrected-areas option uses an approximate correction for distance along the surface.

    Args:
        label: the input label
        surface: the surface to erode on
        erode_dist: distance in mm to erode the labels
        label_out: the output label file
        roi: assume values outside this roi are labeled
        column: select a single column to erode
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_erode_params(
        label=label,
        surface=surface,
        erode_dist=erode_dist,
        label_out=label_out,
        roi=roi,
        column=column,
        corrected_areas=corrected_areas,
    )
    return label_erode_execute(params, runner)

__all__ = [
    "LabelErode",
    "LabelErodeOutputs",
    "LABEL_ERODE_METADATA",
    "label_erode_cargs",
    "label_erode_outputs",
    "label_erode_params",
    "label_erode_execute",
    "label_erode_validate",
    "label_erode",
]