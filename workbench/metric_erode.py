# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_ERODE_METADATA = Metadata(
    id="metric_erode",
    name="ERODE A METRIC FILE",
    package="workbench",
)

MetricErode = typing.TypedDict(
    "MetricErode",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_erode"]],
        "metric": InputPathType,
        "surface": InputPathType,
        "distance": float,
        "metric_out": str,
        "roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MetricErodeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_erode_params(
    metric: InputPathType,
    surface: InputPathType,
    distance: float,
    metric_out: str,
    roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
) -> MetricErode:
    """
    Build parameters.

    Args:
        metric: the metric file to erode
        surface: the surface to compute on
        distance: distance in mm to erode
        metric_out: the output metric
        roi: assume values outside this roi are nonzero
        column: select a single column to erode
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: MetricErode = {
        "@type": "workbench/metric_erode",
        "metric": metric,
        "surface": surface,
        "distance": distance,
        "metric_out": metric_out,
    }
    if roi is not None:
        params["roi"] = roi
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_erode_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricErode."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def metric_erode_cargs(params: MetricErode, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-erode")
    cargs.append(execution.input_file(params["metric"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    v_2 = params.get("corrected_areas")
    if v_2 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_2))
    return cargs

def metric_erode_outputs(params: MetricErode, execution: Execution) -> MetricErodeOutputs:
    """Build the MetricErodeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricErodeOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_erode_execute(params: MetricErode, runner: Runner | None = None) -> MetricErodeOutputs:
    """
    ERODE A METRIC FILE

    Around each vertex with a value of zero, set surrounding vertices to zero.  The surrounding vertices are all immediate neighbors and all vertices within the specified distance.

Note that the -corrected-areas option uses an approximate correction for distance along the surface.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_erode_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_ERODE_METADATA)
    execution.params(params)
    args = metric_erode_cargs(params, execution)
    out = metric_erode_outputs(params, execution)
    execution.run(args)
    return out

def metric_erode(
    metric: InputPathType,
    surface: InputPathType,
    distance: float,
    metric_out: str,
    roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> MetricErodeOutputs:
    """
    ERODE A METRIC FILE

    Around each vertex with a value of zero, set surrounding vertices to zero.  The surrounding vertices are all immediate neighbors and all vertices within the specified distance.

Note that the -corrected-areas option uses an approximate correction for distance along the surface.

    Args:
        metric: the metric file to erode
        surface: the surface to compute on
        distance: distance in mm to erode
        metric_out: the output metric
        roi: assume values outside this roi are nonzero
        column: select a single column to erode
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_erode_params(
        metric=metric,
        surface=surface,
        distance=distance,
        metric_out=metric_out,
        roi=roi,
        column=column,
        corrected_areas=corrected_areas,
    )
    return metric_erode_execute(params, runner)

__all__ = [
    "MetricErode",
    "MetricErodeOutputs",
    "METRIC_ERODE_METADATA",
    "metric_erode_cargs",
    "metric_erode_outputs",
    "metric_erode_params",
    "metric_erode_execute",
    "metric_erode_validate",
    "metric_erode",
]