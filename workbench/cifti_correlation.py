# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_CORRELATION_METADATA = Metadata(
    id="cifti_correlation",
    name="GENERATE CORRELATION OF ROWS IN A CIFTI FILE",
    package="workbench",
)

class CiftiCorrelationRoiOverride(typing.TypedDict):
    left_roi: typing.NotRequired[InputPathType]
    """use an roi for left hempsphere"""
    right_roi: typing.NotRequired[InputPathType]
    """use an roi for right hempsphere"""
    cerebellum_roi: typing.NotRequired[InputPathType]
    """use an roi for cerebellum"""
    vol_roi: typing.NotRequired[InputPathType]
    """use an roi for volume"""
    cifti_roi: typing.NotRequired[InputPathType]
    """use a cifti file for combined rois"""

CiftiCorrelation = typing.TypedDict(
    "CiftiCorrelation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_correlation"]],
        "cifti": InputPathType,
        "cifti_out": str,
        "roi_override": typing.NotRequired[CiftiCorrelationRoiOverride],
        "weights": typing.NotRequired[str],
        "fisher_z": typing.NotRequired[bool],
        "no_demean": typing.NotRequired[bool],
        "covariance": typing.NotRequired[bool],
        "mem_limit": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class CiftiCorrelationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_correlation_params(
    cifti: InputPathType,
    cifti_out: str,
    roi_override: CiftiCorrelationRoiOverride | None = None,
    weights: str | None = None,
    fisher_z: bool = False,
    no_demean: bool = False,
    covariance: bool = False,
    mem_limit: float | None = None,
) -> CiftiCorrelation:
    """
    Build parameters.

    Args:
        cifti: input cifti file
        cifti_out: output cifti file
        roi_override: perform correlation from a subset of rows to all rows
        weights: specify column weights
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        no_demean: instead of correlation, do dot product of rows, then normalize\
            by diagonal
        covariance: compute covariance instead of correlation
        mem_limit: restrict memory usage

    Returns:
        Parameter dictionary.
    """
    params: CiftiCorrelation = {
        "@type": "workbench/cifti_correlation",
        "cifti": cifti,
        "cifti_out": cifti_out,
        "fisher_z": fisher_z,
        "no_demean": no_demean,
        "covariance": covariance,
    }
    if roi_override is not None:
        params["roi_override"] = roi_override
    if weights is not None:
        params["weights"] = weights
    if mem_limit is not None:
        params["mem_limit"] = mem_limit
    return params

def cifti_correlation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiCorrelation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `InputPathType`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("roi_override", None) is not None:
        if not isinstance(params["roi_override"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi_override"])}\'')
        if params["roi_override"].get("left_roi", None) is not None:
            if not isinstance(params["roi_override"]["left_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`left_roi` has the wrong type: Received `{type(params["roi_override"]["left_roi"])}` expected `InputPathType`')
        if params["roi_override"].get("right_roi", None) is not None:
            if not isinstance(params["roi_override"]["right_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`right_roi` has the wrong type: Received `{type(params["roi_override"]["right_roi"])}` expected `InputPathType`')
        if params["roi_override"].get("cerebellum_roi", None) is not None:
            if not isinstance(params["roi_override"]["cerebellum_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`cerebellum_roi` has the wrong type: Received `{type(params["roi_override"]["cerebellum_roi"])}` expected `InputPathType`')
        if params["roi_override"].get("vol_roi", None) is not None:
            if not isinstance(params["roi_override"]["vol_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`vol_roi` has the wrong type: Received `{type(params["roi_override"]["vol_roi"])}` expected `InputPathType`')
        if params["roi_override"].get("cifti_roi", None) is not None:
            if not isinstance(params["roi_override"]["cifti_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`cifti_roi` has the wrong type: Received `{type(params["roi_override"]["cifti_roi"])}` expected `InputPathType`')
    if params.get("weights", None) is not None:
        if not isinstance(params["weights"], str):
            raise StyxValidationError(f'`weights` has the wrong type: Received `{type(params["weights"])}` expected `str`')
    if params.get("fisher_z", None) is not None:
        if not isinstance(params["fisher_z"], bool):
            raise StyxValidationError(f'`fisher_z` has the wrong type: Received `{type(params["fisher_z"])}` expected `bool`')
    if params.get("no_demean", None) is not None:
        if not isinstance(params["no_demean"], bool):
            raise StyxValidationError(f'`no_demean` has the wrong type: Received `{type(params["no_demean"])}` expected `bool`')
    if params.get("covariance", None) is not None:
        if not isinstance(params["covariance"], bool):
            raise StyxValidationError(f'`covariance` has the wrong type: Received `{type(params["covariance"])}` expected `bool`')
    if params.get("mem_limit", None) is not None:
        if not isinstance(params["mem_limit"], (float, int)):
            raise StyxValidationError(f'`mem_limit` has the wrong type: Received `{type(params["mem_limit"])}` expected `float`')

def cifti_correlation_cargs(params: CiftiCorrelation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-correlation")
    cargs.append(execution.input_file(params["cifti"]))
    cargs.append(params["cifti_out"])
    v_0 = params.get("roi_override")
    if v_0 is not None:
        cargs.append("-roi-override")
        v_1 = v_0.get("left_roi")
        if v_1 is not None:
            cargs.append("-left-roi")
            cargs.append(execution.input_file(v_1))
        v_2 = v_0.get("right_roi")
        if v_2 is not None:
            cargs.append("-right-roi")
            cargs.append(execution.input_file(v_2))
        v_3 = v_0.get("cerebellum_roi")
        if v_3 is not None:
            cargs.append("-cerebellum-roi")
            cargs.append(execution.input_file(v_3))
        v_4 = v_0.get("vol_roi")
        if v_4 is not None:
            cargs.append("-vol-roi")
            cargs.append(execution.input_file(v_4))
        v_5 = v_0.get("cifti_roi")
        if v_5 is not None:
            cargs.append("-cifti-roi")
            cargs.append(execution.input_file(v_5))
    v_6 = params.get("weights")
    if v_6 is not None:
        cargs.append("-weights")
        cargs.append(v_6)
    if params.get("fisher_z"):
        cargs.append("-fisher-z")
    if params.get("no_demean"):
        cargs.append("-no-demean")
    if params.get("covariance"):
        cargs.append("-covariance")
    v_7 = params.get("mem_limit")
    if v_7 is not None:
        cargs.append("-mem-limit")
        cargs.append(str(v_7))
    return cargs

def cifti_correlation_outputs(params: CiftiCorrelation, execution: Execution) -> CiftiCorrelationOutputs:
    """Build the CiftiCorrelationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiCorrelationOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_correlation_execute(params: CiftiCorrelation, runner: Runner | None = None) -> CiftiCorrelationOutputs:
    """
    GENERATE CORRELATION OF ROWS IN A CIFTI FILE

    For each row (or each row inside an roi if -roi-override is specified), correlate to all other rows.  The -cifti-roi suboption to -roi-override may not be specified with any other -*-roi suboption, but you may specify the other -*-roi suboptions together.

When using the -fisher-z option, the output is NOT a Z-score, it is artanh(r), to do further math on this output, consider using -cifti-math.

Restricting the memory usage will make it calculate the output in chunks, and if the input file size is more than 70% of the memory limit, it will also read through the input file as rows are required, resulting in several passes through the input file (once per chunk).  Memory limit does not need to be an integer, you may also specify 0 to calculate a single output row at a time (this may be very slow).

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_correlation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_CORRELATION_METADATA)
    execution.params(params)
    args = cifti_correlation_cargs(params, execution)
    out = cifti_correlation_outputs(params, execution)
    execution.run(args)
    return out

def cifti_correlation(
    cifti: InputPathType,
    cifti_out: str,
    roi_override: CiftiCorrelationRoiOverride | None = None,
    weights: str | None = None,
    fisher_z: bool = False,
    no_demean: bool = False,
    covariance: bool = False,
    mem_limit: float | None = None,
    runner: Runner | None = None,
) -> CiftiCorrelationOutputs:
    """
    GENERATE CORRELATION OF ROWS IN A CIFTI FILE

    For each row (or each row inside an roi if -roi-override is specified), correlate to all other rows.  The -cifti-roi suboption to -roi-override may not be specified with any other -*-roi suboption, but you may specify the other -*-roi suboptions together.

When using the -fisher-z option, the output is NOT a Z-score, it is artanh(r), to do further math on this output, consider using -cifti-math.

Restricting the memory usage will make it calculate the output in chunks, and if the input file size is more than 70% of the memory limit, it will also read through the input file as rows are required, resulting in several passes through the input file (once per chunk).  Memory limit does not need to be an integer, you may also specify 0 to calculate a single output row at a time (this may be very slow).

    Args:
        cifti: input cifti file
        cifti_out: output cifti file
        roi_override: perform correlation from a subset of rows to all rows
        weights: specify column weights
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        no_demean: instead of correlation, do dot product of rows, then normalize\
            by diagonal
        covariance: compute covariance instead of correlation
        mem_limit: restrict memory usage
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_correlation_params(
        cifti=cifti,
        cifti_out=cifti_out,
        roi_override=roi_override,
        weights=weights,
        fisher_z=fisher_z,
        no_demean=no_demean,
        covariance=covariance,
        mem_limit=mem_limit,
    )
    return cifti_correlation_execute(params, runner)

__all__ = [
    "CiftiCorrelation",
    "CiftiCorrelationOutputs",
    "CIFTI_CORRELATION_METADATA",
    "cifti_correlation_cargs",
    "cifti_correlation_outputs",
    "cifti_correlation_params",
    "cifti_correlation_execute",
    "cifti_correlation_validate",
    "cifti_correlation",
]