# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_LABEL_TO_SURFACE_MAPPING_METADATA = Metadata(
    id="volume_label_to_surface_mapping",
    name="MAP A LABEL VOLUME TO A SURFACE LABEL FILE",
    package="workbench",
)

class VolumeLabelToSurfaceMappingRibbonConstrained(typing.TypedDict):
    inner_surf: InputPathType
    """the inner surface of the ribbon"""
    outer_surf: InputPathType
    """the outer surface of the ribbon"""
    volume_roi: typing.NotRequired[InputPathType]
    """use a volume roi"""
    dilate_missing: typing.NotRequired[float]
    """use dilation for small vertices that 'missed' the geometry tests"""
    voxel_subdiv: typing.NotRequired[int]
    """voxel divisions while estimating voxel weights"""
    thin_columns: typing.NotRequired[bool]
    """use non-overlapping polyhedra"""

VolumeLabelToSurfaceMapping = typing.TypedDict(
    "VolumeLabelToSurfaceMapping",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_label_to_surface_mapping"]],
        "volume": InputPathType,
        "surface": InputPathType,
        "label_out": str,
        "ribbon_constrained": typing.NotRequired[VolumeLabelToSurfaceMappingRibbonConstrained],
        "subvol_select": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeLabelToSurfaceMappingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output gifti label file"""

def volume_label_to_surface_mapping_params(
    volume: InputPathType,
    surface: InputPathType,
    label_out: str,
    ribbon_constrained: VolumeLabelToSurfaceMappingRibbonConstrained | None = None,
    subvol_select: str | None = None,
) -> VolumeLabelToSurfaceMapping:
    """
    Build parameters.

    Args:
        volume: the volume to map data from
        surface: the surface to map the data onto
        label_out: the output gifti label file
        ribbon_constrained: use ribbon constrained mapping algorithm
        subvol_select: select a single subvolume to map

    Returns:
        Parameter dictionary.
    """
    params: VolumeLabelToSurfaceMapping = {
        "@type": "workbench/volume_label_to_surface_mapping",
        "volume": volume,
        "surface": surface,
        "label_out": label_out,
    }
    if ribbon_constrained is not None:
        params["ribbon_constrained"] = ribbon_constrained
    if subvol_select is not None:
        params["subvol_select"] = subvol_select
    return params

def volume_label_to_surface_mapping_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeLabelToSurfaceMapping."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("ribbon_constrained", None) is not None:
        if not isinstance(params["ribbon_constrained"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["ribbon_constrained"])}\'')
        if params["ribbon_constrained"].get("inner_surf", None) is None:
            raise StyxValidationError("`inner_surf` must not be None")
        if not isinstance(params["ribbon_constrained"]["inner_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`inner_surf` has the wrong type: Received `{type(params["ribbon_constrained"]["inner_surf"])}` expected `InputPathType`')
        if params["ribbon_constrained"].get("outer_surf", None) is None:
            raise StyxValidationError("`outer_surf` must not be None")
        if not isinstance(params["ribbon_constrained"]["outer_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`outer_surf` has the wrong type: Received `{type(params["ribbon_constrained"]["outer_surf"])}` expected `InputPathType`')
        if params["ribbon_constrained"].get("volume_roi", None) is not None:
            if not isinstance(params["ribbon_constrained"]["volume_roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`volume_roi` has the wrong type: Received `{type(params["ribbon_constrained"]["volume_roi"])}` expected `InputPathType`')
        if params["ribbon_constrained"].get("dilate_missing", None) is not None:
            if not isinstance(params["ribbon_constrained"]["dilate_missing"], (float, int)):
                raise StyxValidationError(f'`dilate_missing` has the wrong type: Received `{type(params["ribbon_constrained"]["dilate_missing"])}` expected `float`')
        if params["ribbon_constrained"].get("voxel_subdiv", None) is not None:
            if not isinstance(params["ribbon_constrained"]["voxel_subdiv"], int):
                raise StyxValidationError(f'`voxel_subdiv` has the wrong type: Received `{type(params["ribbon_constrained"]["voxel_subdiv"])}` expected `int`')
        if params["ribbon_constrained"].get("thin_columns", None) is not None:
            if not isinstance(params["ribbon_constrained"]["thin_columns"], bool):
                raise StyxValidationError(f'`thin_columns` has the wrong type: Received `{type(params["ribbon_constrained"]["thin_columns"])}` expected `bool`')
    if params.get("subvol_select", None) is not None:
        if not isinstance(params["subvol_select"], str):
            raise StyxValidationError(f'`subvol_select` has the wrong type: Received `{type(params["subvol_select"])}` expected `str`')

def volume_label_to_surface_mapping_cargs(params: VolumeLabelToSurfaceMapping, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-label-to-surface-mapping")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["label_out"])
    v_0 = params.get("ribbon_constrained")
    if v_0 is not None:
        cargs.append("-ribbon-constrained")
        cargs.append(execution.input_file(v_0["inner_surf"]))
        cargs.append(execution.input_file(v_0["outer_surf"]))
        v_1 = v_0.get("volume_roi")
        if v_1 is not None:
            cargs.append("-volume-roi")
            cargs.append(execution.input_file(v_1))
        v_2 = v_0.get("dilate_missing")
        if v_2 is not None:
            cargs.append("-dilate-missing")
            cargs.append(str(v_2))
        v_3 = v_0.get("voxel_subdiv")
        if v_3 is not None:
            cargs.append("-voxel-subdiv")
            cargs.append(str(v_3))
        if v_0.get("thin_columns"):
            cargs.append("-thin-columns")
    v_4 = params.get("subvol_select")
    if v_4 is not None:
        cargs.append("-subvol-select")
        cargs.append(v_4)
    return cargs

def volume_label_to_surface_mapping_outputs(params: VolumeLabelToSurfaceMapping, execution: Execution) -> VolumeLabelToSurfaceMappingOutputs:
    """Build the VolumeLabelToSurfaceMappingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return VolumeLabelToSurfaceMappingOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def volume_label_to_surface_mapping_execute(params: VolumeLabelToSurfaceMapping, runner: Runner | None = None) -> VolumeLabelToSurfaceMappingOutputs:
    """
    MAP A LABEL VOLUME TO A SURFACE LABEL FILE

    Map label volume data to a surface.  If -ribbon-constrained is not specified, uses the enclosing voxel method.  The ribbon mapping method constructs a polyhedron from the vertex's neighbors on each surface, and estimates the amount of this polyhedron's volume that falls inside any nearby voxels, to use as the weights for a popularity comparison.  If -thin-columns is specified, the polyhedron uses the edge midpoints and triangle centroids, so that neighboring vertices do not have overlapping polyhedra.  This may require increasing -voxel-subdiv to get enough samples in each voxel to reliably land inside these smaller polyhedra.  The volume ROI is useful to exclude partial volume effects of voxels the surfaces pass through, and will cause the mapping to ignore voxels that don't have a positive value in the mask.  The subdivision number specifies how it approximates the amount of the volume the polyhedron intersects, by splitting each voxel into NxNxN pieces, and checking whether the center of each piece is inside the polyhedron.  If you have very large voxels, consider increasing this if you get unexpected unlabeled vertices in your output.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_label_to_surface_mapping_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_LABEL_TO_SURFACE_MAPPING_METADATA)
    execution.params(params)
    args = volume_label_to_surface_mapping_cargs(params, execution)
    out = volume_label_to_surface_mapping_outputs(params, execution)
    execution.run(args)
    return out

def volume_label_to_surface_mapping(
    volume: InputPathType,
    surface: InputPathType,
    label_out: str,
    ribbon_constrained: VolumeLabelToSurfaceMappingRibbonConstrained | None = None,
    subvol_select: str | None = None,
    runner: Runner | None = None,
) -> VolumeLabelToSurfaceMappingOutputs:
    """
    MAP A LABEL VOLUME TO A SURFACE LABEL FILE

    Map label volume data to a surface.  If -ribbon-constrained is not specified, uses the enclosing voxel method.  The ribbon mapping method constructs a polyhedron from the vertex's neighbors on each surface, and estimates the amount of this polyhedron's volume that falls inside any nearby voxels, to use as the weights for a popularity comparison.  If -thin-columns is specified, the polyhedron uses the edge midpoints and triangle centroids, so that neighboring vertices do not have overlapping polyhedra.  This may require increasing -voxel-subdiv to get enough samples in each voxel to reliably land inside these smaller polyhedra.  The volume ROI is useful to exclude partial volume effects of voxels the surfaces pass through, and will cause the mapping to ignore voxels that don't have a positive value in the mask.  The subdivision number specifies how it approximates the amount of the volume the polyhedron intersects, by splitting each voxel into NxNxN pieces, and checking whether the center of each piece is inside the polyhedron.  If you have very large voxels, consider increasing this if you get unexpected unlabeled vertices in your output.

    Args:
        volume: the volume to map data from
        surface: the surface to map the data onto
        label_out: the output gifti label file
        ribbon_constrained: use ribbon constrained mapping algorithm
        subvol_select: select a single subvolume to map
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_label_to_surface_mapping_params(
        volume=volume,
        surface=surface,
        label_out=label_out,
        ribbon_constrained=ribbon_constrained,
        subvol_select=subvol_select,
    )
    return volume_label_to_surface_mapping_execute(params, runner)

__all__ = [
    "VolumeLabelToSurfaceMapping",
    "VolumeLabelToSurfaceMappingOutputs",
    "VOLUME_LABEL_TO_SURFACE_MAPPING_METADATA",
    "volume_label_to_surface_mapping_cargs",
    "volume_label_to_surface_mapping_outputs",
    "volume_label_to_surface_mapping_params",
    "volume_label_to_surface_mapping_execute",
    "volume_label_to_surface_mapping_validate",
    "volume_label_to_surface_mapping",
]