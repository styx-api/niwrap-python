# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_MERGE_METADATA = Metadata(
    id="volume_merge",
    name="MERGE VOLUME FILES INTO A NEW FILE",
    package="workbench",
)

class VolumeMergeUpTo(typing.TypedDict):
    last_subvol: str
    """the number or name of the last subvolume to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class VolumeMergeSubvolume(typing.TypedDict):
    subvol: str
    """the subvolume number or name"""
    up_to: typing.NotRequired[VolumeMergeUpTo]
    """use an inclusive range of subvolumes"""

class VolumeMergeVolume(typing.TypedDict):
    volume_in: InputPathType
    """a volume file to use subvolumes from"""
    subvolume: list[VolumeMergeSubvolume]
    """select a single subvolume to use"""

VolumeMerge = typing.TypedDict(
    "VolumeMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_merge"]],
        "volume_out": str,
        "volume": list[VolumeMergeVolume],
    },
)

@dataclasses.dataclass
class VolumeMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume file"""

def volume_merge_params(
    volume_out: str,
    volume: list[VolumeMergeVolume],
) -> VolumeMerge:
    """
    Build parameters.

    Args:
        volume_out: the output volume file
        volume: specify an input volume file

    Returns:
        Parameter dictionary.
    """
    params: VolumeMerge = {
        "@type": "workbench/volume_merge",
        "volume_out": volume_out,
        "volume": volume,
    }
    return params

def volume_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], list):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `list[VolumeMergeVolume]`')
    for e in params["volume"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("volume_in", None) is None:
            raise StyxValidationError("`volume_in` must not be None")
        if not isinstance(e["volume_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(e["volume_in"])}` expected `InputPathType`')
        if e.get("subvolume", None) is None:
            raise StyxValidationError("`subvolume` must not be None")
        if not isinstance(e["subvolume"], list):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(e["subvolume"])}` expected `list[VolumeMergeSubvolume]`')
        for e_2 in e["subvolume"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("subvol", None) is None:
                raise StyxValidationError("`subvol` must not be None")
            if not isinstance(e_2["subvol"], str):
                raise StyxValidationError(f'`subvol` has the wrong type: Received `{type(e_2["subvol"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_subvol", None) is None:
                    raise StyxValidationError("`last_subvol` must not be None")
                if not isinstance(e_2["up_to"]["last_subvol"], str):
                    raise StyxValidationError(f'`last_subvol` has the wrong type: Received `{type(e_2["up_to"]["last_subvol"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def volume_merge_cargs(params: VolumeMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-merge")
    cargs.append(params["volume_out"])
    for item0 in params["volume"]:
        cargs.append("-volume")
        cargs.append(execution.input_file(item0["volume_in"]))
        for item1 in item0["subvolume"]:
            cargs.append("-subvolume")
            cargs.append(item1["subvol"])
            v_0 = item1.get("up_to")
            if v_0 is not None:
                cargs.append("-up-to")
                cargs.append(v_0["last_subvol"])
                if v_0.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def volume_merge_outputs(params: VolumeMerge, execution: Execution) -> VolumeMergeOutputs:
    """Build the VolumeMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeMergeOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_merge_execute(params: VolumeMerge, runner: Runner | None = None) -> VolumeMergeOutputs:
    """
    MERGE VOLUME FILES INTO A NEW FILE

    Takes one or more volume files and constructs a new volume file by concatenating subvolumes from them.  The input volume files must have the same volume space.

Example: wb_command -volume-merge out.nii -volume first.nii -subvolume 1 -volume second.nii

This example would take the first subvolume from first.nii, followed by all subvolumes from second.nii, and write these to out.nii.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_MERGE_METADATA)
    execution.params(params)
    args = volume_merge_cargs(params, execution)
    out = volume_merge_outputs(params, execution)
    execution.run(args)
    return out

def volume_merge(
    volume_out: str,
    volume: list[VolumeMergeVolume],
    runner: Runner | None = None,
) -> VolumeMergeOutputs:
    """
    MERGE VOLUME FILES INTO A NEW FILE

    Takes one or more volume files and constructs a new volume file by concatenating subvolumes from them.  The input volume files must have the same volume space.

Example: wb_command -volume-merge out.nii -volume first.nii -subvolume 1 -volume second.nii

This example would take the first subvolume from first.nii, followed by all subvolumes from second.nii, and write these to out.nii.

    Args:
        volume_out: the output volume file
        volume: specify an input volume file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_merge_params(
        volume_out=volume_out,
        volume=volume,
    )
    return volume_merge_execute(params, runner)

__all__ = [
    "VolumeMerge",
    "VolumeMergeOutputs",
    "VOLUME_MERGE_METADATA",
    "volume_merge_cargs",
    "volume_merge_outputs",
    "volume_merge_params",
    "volume_merge_execute",
    "volume_merge_validate",
    "volume_merge",
]