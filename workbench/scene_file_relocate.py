# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SCENE_FILE_RELOCATE_METADATA = Metadata(
    id="scene_file_relocate",
    name="RECREATE SCENE FILE IN NEW LOCATION",
    package="workbench",
)

SceneFileRelocate = typing.TypedDict(
    "SceneFileRelocate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/scene_file_relocate"]],
        "input_scene": str,
        "output_scene": str,
    },
)

@dataclasses.dataclass
class SceneFileRelocateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def scene_file_relocate_params(
    input_scene: str,
    output_scene: str,
) -> SceneFileRelocate:
    """
    Build parameters.

    Args:
        input_scene: the scene file to use
        output_scene: output - the new scene file to create

    Returns:
        Parameter dictionary.
    """
    params: SceneFileRelocate = {
        "@type": "workbench/scene_file_relocate",
        "input_scene": input_scene,
        "output_scene": output_scene,
    }
    return params

def scene_file_relocate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SceneFileRelocate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_scene", None) is None:
        raise StyxValidationError("`input_scene` must not be None")
    if not isinstance(params["input_scene"], str):
        raise StyxValidationError(f'`input_scene` has the wrong type: Received `{type(params["input_scene"])}` expected `str`')
    if params.get("output_scene", None) is None:
        raise StyxValidationError("`output_scene` must not be None")
    if not isinstance(params["output_scene"], str):
        raise StyxValidationError(f'`output_scene` has the wrong type: Received `{type(params["output_scene"])}` expected `str`')

def scene_file_relocate_cargs(params: SceneFileRelocate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-scene-file-relocate")
    cargs.append(params["input_scene"])
    cargs.append(params["output_scene"])
    return cargs

def scene_file_relocate_outputs(params: SceneFileRelocate, execution: Execution) -> SceneFileRelocateOutputs:
    """Build the SceneFileRelocateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SceneFileRelocateOutputs(
        root=root_v,
    )

def scene_file_relocate_execute(params: SceneFileRelocate, runner: Runner | None = None) -> SceneFileRelocateOutputs:
    """
    RECREATE SCENE FILE IN NEW LOCATION

    Scene files contain internal relative paths, such that moving or copying a scene file will cause it to lose track of the files it refers to.  This command makes a modified copy of the scene file, changing the relative paths to refer to the new relative locations of the files.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    scene_file_relocate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SCENE_FILE_RELOCATE_METADATA)
    execution.params(params)
    args = scene_file_relocate_cargs(params, execution)
    out = scene_file_relocate_outputs(params, execution)
    execution.run(args)
    return out

def scene_file_relocate(
    input_scene: str,
    output_scene: str,
    runner: Runner | None = None,
) -> SceneFileRelocateOutputs:
    """
    RECREATE SCENE FILE IN NEW LOCATION

    Scene files contain internal relative paths, such that moving or copying a scene file will cause it to lose track of the files it refers to.  This command makes a modified copy of the scene file, changing the relative paths to refer to the new relative locations of the files.

    Args:
        input_scene: the scene file to use
        output_scene: output - the new scene file to create
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = scene_file_relocate_params(
        input_scene=input_scene,
        output_scene=output_scene,
    )
    return scene_file_relocate_execute(params, runner)

__all__ = [
    "SceneFileRelocate",
    "SceneFileRelocateOutputs",
    "SCENE_FILE_RELOCATE_METADATA",
    "scene_file_relocate_cargs",
    "scene_file_relocate_outputs",
    "scene_file_relocate_params",
    "scene_file_relocate_execute",
    "scene_file_relocate_validate",
    "scene_file_relocate",
]