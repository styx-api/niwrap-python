# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_VERTEX_AREAS_METADATA = Metadata(
    id="surface_vertex_areas",
    name="MEASURE SURFACE AREA EACH VERTEX IS RESPONSIBLE FOR",
    package="workbench",
)

SurfaceVertexAreas = typing.TypedDict(
    "SurfaceVertexAreas",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_vertex_areas"]],
        "surface": InputPathType,
        "metric": str,
    },
)

@dataclasses.dataclass
class SurfaceVertexAreasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric: OutputPathType
    """the output metric"""

def surface_vertex_areas_params(
    surface: InputPathType,
    metric: str,
) -> SurfaceVertexAreas:
    """
    Build parameters.

    Args:
        surface: the surface to measure
        metric: the output metric

    Returns:
        Parameter dictionary.
    """
    params: SurfaceVertexAreas = {
        "@type": "workbench/surface_vertex_areas",
        "surface": surface,
        "metric": metric,
    }
    return params

def surface_vertex_areas_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceVertexAreas."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], str):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')

def surface_vertex_areas_cargs(params: SurfaceVertexAreas, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-vertex-areas")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["metric"])
    return cargs

def surface_vertex_areas_outputs(params: SurfaceVertexAreas, execution: Execution) -> SurfaceVertexAreasOutputs:
    """Build the SurfaceVertexAreasOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_v: OutputPathType = execution.output_file(params["metric"])
    return SurfaceVertexAreasOutputs(
        root=root_v,
        metric=metric_v,
    )

def surface_vertex_areas_execute(params: SurfaceVertexAreas, runner: Runner | None = None) -> SurfaceVertexAreasOutputs:
    """
    MEASURE SURFACE AREA EACH VERTEX IS RESPONSIBLE FOR

    Each vertex gets one third of the area of each triangle it is a part of.  Units are mm^2.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_vertex_areas_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_VERTEX_AREAS_METADATA)
    execution.params(params)
    args = surface_vertex_areas_cargs(params, execution)
    out = surface_vertex_areas_outputs(params, execution)
    execution.run(args)
    return out

def surface_vertex_areas(
    surface: InputPathType,
    metric: str,
    runner: Runner | None = None,
) -> SurfaceVertexAreasOutputs:
    """
    MEASURE SURFACE AREA EACH VERTEX IS RESPONSIBLE FOR

    Each vertex gets one third of the area of each triangle it is a part of.  Units are mm^2.

    Args:
        surface: the surface to measure
        metric: the output metric
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_vertex_areas_params(
        surface=surface,
        metric=metric,
    )
    return surface_vertex_areas_execute(params, runner)

__all__ = [
    "SurfaceVertexAreas",
    "SurfaceVertexAreasOutputs",
    "SURFACE_VERTEX_AREAS_METADATA",
    "surface_vertex_areas_cargs",
    "surface_vertex_areas_outputs",
    "surface_vertex_areas_params",
    "surface_vertex_areas_execute",
    "surface_vertex_areas_validate",
    "surface_vertex_areas",
]