# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_MERGE_METADATA = Metadata(
    id="cifti_merge",
    name="MERGE OR SPLIT ON SERIES, SCALAR, OR LABEL DIMENSIONS",
    package="workbench",
)

class CiftiMergeUpTo(typing.TypedDict):
    last_index: str
    """the number or name of the last index to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class CiftiMergeIndex(typing.TypedDict):
    index: str
    """the index number (starting from 1), or name"""
    up_to: typing.NotRequired[CiftiMergeUpTo]
    """use an inclusive range of indices"""

class CiftiMergeCifti(typing.TypedDict):
    cifti_in: InputPathType
    """a cifti file to use data from"""
    index: list[CiftiMergeIndex]
    """select a single index to use"""

CiftiMerge = typing.TypedDict(
    "CiftiMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_merge"]],
        "cifti_out": str,
        "direction": typing.NotRequired[str],
        "mem_limit": typing.NotRequired[float],
        "cifti": list[CiftiMergeCifti],
    },
)

@dataclasses.dataclass
class CiftiMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_merge_params(
    cifti_out: str,
    cifti: list[CiftiMergeCifti],
    direction: str | None = None,
    mem_limit: float | None = None,
) -> CiftiMerge:
    """
    Build parameters.

    Args:
        cifti_out: output cifti file
        cifti: specify an input cifti file
        direction: merge in a direction other than along rows
        mem_limit: restrict memory used for file reading efficiency

    Returns:
        Parameter dictionary.
    """
    params: CiftiMerge = {
        "@type": "workbench/cifti_merge",
        "cifti_out": cifti_out,
        "cifti": cifti,
    }
    if direction is not None:
        params["direction"] = direction
    if mem_limit is not None:
        params["mem_limit"] = mem_limit
    return params

def cifti_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("direction", None) is not None:
        if not isinstance(params["direction"], str):
            raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("mem_limit", None) is not None:
        if not isinstance(params["mem_limit"], (float, int)):
            raise StyxValidationError(f'`mem_limit` has the wrong type: Received `{type(params["mem_limit"])}` expected `float`')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], list):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `list[CiftiMergeCifti]`')
    for e in params["cifti"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("cifti_in", None) is None:
            raise StyxValidationError("`cifti_in` must not be None")
        if not isinstance(e["cifti_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(e["cifti_in"])}` expected `InputPathType`')
        if e.get("index", None) is None:
            raise StyxValidationError("`index` must not be None")
        if not isinstance(e["index"], list):
            raise StyxValidationError(f'`index` has the wrong type: Received `{type(e["index"])}` expected `list[CiftiMergeIndex]`')
        for e_2 in e["index"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("index", None) is None:
                raise StyxValidationError("`index` must not be None")
            if not isinstance(e_2["index"], str):
                raise StyxValidationError(f'`index` has the wrong type: Received `{type(e_2["index"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_index", None) is None:
                    raise StyxValidationError("`last_index` must not be None")
                if not isinstance(e_2["up_to"]["last_index"], str):
                    raise StyxValidationError(f'`last_index` has the wrong type: Received `{type(e_2["up_to"]["last_index"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def cifti_merge_cargs(params: CiftiMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-merge")
    cargs.append(params["cifti_out"])
    v_0 = params.get("direction")
    if v_0 is not None:
        cargs.append("-direction")
        cargs.append(v_0)
    v_1 = params.get("mem_limit")
    if v_1 is not None:
        cargs.append("-mem-limit")
        cargs.append(str(v_1))
    for item0 in params["cifti"]:
        cargs.append("-cifti")
        cargs.append(execution.input_file(item0["cifti_in"]))
        for item1 in item0["index"]:
            cargs.append("-index")
            cargs.append(item1["index"])
            v_2 = item1.get("up_to")
            if v_2 is not None:
                cargs.append("-up-to")
                cargs.append(v_2["last_index"])
                if v_2.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def cifti_merge_outputs(params: CiftiMerge, execution: Execution) -> CiftiMergeOutputs:
    """Build the CiftiMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiMergeOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_merge_execute(params: CiftiMerge, runner: Runner | None = None) -> CiftiMergeOutputs:
    """
    MERGE OR SPLIT ON SERIES, SCALAR, OR LABEL DIMENSIONS

    Given input CIFTI files for which mappings along the selected direction are the same type, all either series, scalars, or labels, and the other dimensions are index-compatible, this command concatenates the data in the specified indices/ranges along the selected direction (by default, on typical 2D cifti, concatenate horizontally, so rows become longer).  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

Example: wb_command -cifti-merge out.dtseries.nii -cifti first.dtseries.nii -index 1 -cifti second.dtseries.nii

This example would take the first column from first.dtseries.nii, followed by all columns from second.dtseries.nii, and write these columns to out.dtseries.nii.  

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_MERGE_METADATA)
    execution.params(params)
    args = cifti_merge_cargs(params, execution)
    out = cifti_merge_outputs(params, execution)
    execution.run(args)
    return out

def cifti_merge(
    cifti_out: str,
    cifti: list[CiftiMergeCifti],
    direction: str | None = None,
    mem_limit: float | None = None,
    runner: Runner | None = None,
) -> CiftiMergeOutputs:
    """
    MERGE OR SPLIT ON SERIES, SCALAR, OR LABEL DIMENSIONS

    Given input CIFTI files for which mappings along the selected direction are the same type, all either series, scalars, or labels, and the other dimensions are index-compatible, this command concatenates the data in the specified indices/ranges along the selected direction (by default, on typical 2D cifti, concatenate horizontally, so rows become longer).  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

Example: wb_command -cifti-merge out.dtseries.nii -cifti first.dtseries.nii -index 1 -cifti second.dtseries.nii

This example would take the first column from first.dtseries.nii, followed by all columns from second.dtseries.nii, and write these columns to out.dtseries.nii.  

    Args:
        cifti_out: output cifti file
        cifti: specify an input cifti file
        direction: merge in a direction other than along rows
        mem_limit: restrict memory used for file reading efficiency
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_merge_params(
        cifti_out=cifti_out,
        cifti=cifti,
        direction=direction,
        mem_limit=mem_limit,
    )
    return cifti_merge_execute(params, runner)

__all__ = [
    "CiftiMerge",
    "CiftiMergeOutputs",
    "CIFTI_MERGE_METADATA",
    "cifti_merge_cargs",
    "cifti_merge_outputs",
    "cifti_merge_params",
    "cifti_merge_execute",
    "cifti_merge_validate",
    "cifti_merge",
]