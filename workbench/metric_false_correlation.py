# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_FALSE_CORRELATION_METADATA = Metadata(
    id="metric_false_correlation",
    name="COMPARE CORRELATION LOCALLY AND ACROSS/THROUGH SULCI/GYRI",
    package="workbench",
)

MetricFalseCorrelation = typing.TypedDict(
    "MetricFalseCorrelation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_false_correlation"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "3_d_dist": float,
        "geo_outer": float,
        "geo_inner": float,
        "metric_out": str,
        "roi": typing.NotRequired[InputPathType],
        "dump_text": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricFalseCorrelationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_false_correlation_params(
    surface: InputPathType,
    metric_in: InputPathType,
    v_3_d_dist: float,
    geo_outer: float,
    geo_inner: float,
    metric_out: str,
    roi: InputPathType | None = None,
    dump_text: str | None = None,
) -> MetricFalseCorrelation:
    """
    Build parameters.

    Args:
        surface: the surface to compute geodesic and 3D distance with
        metric_in: the metric to correlate
        v_3_d_dist: maximum 3D distance to check around each vertex
        geo_outer: maximum geodesic distance to use for neighboring correlation
        geo_inner: minimum geodesic distance to use for neighboring correlation
        metric_out: the output metric
        roi: select a region of interest that has data
        dump_text: dump the raw measures used to a text file

    Returns:
        Parameter dictionary.
    """
    params: MetricFalseCorrelation = {
        "@type": "workbench/metric_false_correlation",
        "surface": surface,
        "metric_in": metric_in,
        "3_d_dist": v_3_d_dist,
        "geo_outer": geo_outer,
        "geo_inner": geo_inner,
        "metric_out": metric_out,
    }
    if roi is not None:
        params["roi"] = roi
    if dump_text is not None:
        params["dump_text"] = dump_text
    return params

def metric_false_correlation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricFalseCorrelation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("3_d_dist", None) is None:
        raise StyxValidationError("`3_d_dist` must not be None")
    if not isinstance(params["3_d_dist"], (float, int)):
        raise StyxValidationError(f'`3_d_dist` has the wrong type: Received `{type(params["3_d_dist"])}` expected `float`')
    if params.get("geo_outer", None) is None:
        raise StyxValidationError("`geo_outer` must not be None")
    if not isinstance(params["geo_outer"], (float, int)):
        raise StyxValidationError(f'`geo_outer` has the wrong type: Received `{type(params["geo_outer"])}` expected `float`')
    if params.get("geo_inner", None) is None:
        raise StyxValidationError("`geo_inner` must not be None")
    if not isinstance(params["geo_inner"], (float, int)):
        raise StyxValidationError(f'`geo_inner` has the wrong type: Received `{type(params["geo_inner"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("dump_text", None) is not None:
        if not isinstance(params["dump_text"], str):
            raise StyxValidationError(f'`dump_text` has the wrong type: Received `{type(params["dump_text"])}` expected `str`')

def metric_false_correlation_cargs(params: MetricFalseCorrelation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-false-correlation")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(str(params["3_d_dist"]))
    cargs.append(str(params["geo_outer"]))
    cargs.append(str(params["geo_inner"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("dump_text")
    if v_1 is not None:
        cargs.append("-dump-text")
        cargs.append(v_1)
    return cargs

def metric_false_correlation_outputs(params: MetricFalseCorrelation, execution: Execution) -> MetricFalseCorrelationOutputs:
    """Build the MetricFalseCorrelationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricFalseCorrelationOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_false_correlation_execute(params: MetricFalseCorrelation, runner: Runner | None = None) -> MetricFalseCorrelationOutputs:
    """
    COMPARE CORRELATION LOCALLY AND ACROSS/THROUGH SULCI/GYRI

    For each vertex, compute the average correlation within a range of geodesic distances that don't cross a sulcus/gyrus, and the correlation to the closest vertex crossing a sulcus/gyrus.  A vertex is considered to cross a sulcus/gyrus if the 3D distance is less than a third of the geodesic distance.  The output file contains the ratio between these correlations, and some additional maps to help explain the ratio.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_false_correlation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_FALSE_CORRELATION_METADATA)
    execution.params(params)
    args = metric_false_correlation_cargs(params, execution)
    out = metric_false_correlation_outputs(params, execution)
    execution.run(args)
    return out

def metric_false_correlation(
    surface: InputPathType,
    metric_in: InputPathType,
    v_3_d_dist: float,
    geo_outer: float,
    geo_inner: float,
    metric_out: str,
    roi: InputPathType | None = None,
    dump_text: str | None = None,
    runner: Runner | None = None,
) -> MetricFalseCorrelationOutputs:
    """
    COMPARE CORRELATION LOCALLY AND ACROSS/THROUGH SULCI/GYRI

    For each vertex, compute the average correlation within a range of geodesic distances that don't cross a sulcus/gyrus, and the correlation to the closest vertex crossing a sulcus/gyrus.  A vertex is considered to cross a sulcus/gyrus if the 3D distance is less than a third of the geodesic distance.  The output file contains the ratio between these correlations, and some additional maps to help explain the ratio.

    Args:
        surface: the surface to compute geodesic and 3D distance with
        metric_in: the metric to correlate
        v_3_d_dist: maximum 3D distance to check around each vertex
        geo_outer: maximum geodesic distance to use for neighboring correlation
        geo_inner: minimum geodesic distance to use for neighboring correlation
        metric_out: the output metric
        roi: select a region of interest that has data
        dump_text: dump the raw measures used to a text file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_false_correlation_params(
        surface=surface,
        metric_in=metric_in,
        v_3_d_dist=v_3_d_dist,
        geo_outer=geo_outer,
        geo_inner=geo_inner,
        metric_out=metric_out,
        roi=roi,
        dump_text=dump_text,
    )
    return metric_false_correlation_execute(params, runner)

__all__ = [
    "MetricFalseCorrelation",
    "MetricFalseCorrelationOutputs",
    "METRIC_FALSE_CORRELATION_METADATA",
    "metric_false_correlation_cargs",
    "metric_false_correlation_outputs",
    "metric_false_correlation_params",
    "metric_false_correlation_execute",
    "metric_false_correlation_validate",
    "metric_false_correlation",
]