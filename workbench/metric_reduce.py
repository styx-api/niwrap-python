# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_REDUCE_METADATA = Metadata(
    id="metric_reduce",
    name="PERFORM REDUCTION OPERATION ACROSS METRIC COLUMNS",
    package="workbench",
)

class MetricReduceExcludeOutliers(typing.TypedDict):
    sigma_below: float
    """number of standard deviations below the mean to include"""
    sigma_above: float
    """number of standard deviations above the mean to include"""

MetricReduce = typing.TypedDict(
    "MetricReduce",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_reduce"]],
        "metric_in": InputPathType,
        "operation": str,
        "metric_out": str,
        "exclude_outliers": typing.NotRequired[MetricReduceExcludeOutliers],
        "only_numeric": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MetricReduceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_reduce_params(
    metric_in: InputPathType,
    operation: str,
    metric_out: str,
    exclude_outliers: MetricReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
) -> MetricReduce:
    """
    Build parameters.

    Args:
        metric_in: the metric to reduce
        operation: the reduction operator to use
        metric_out: the output metric
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values

    Returns:
        Parameter dictionary.
    """
    params: MetricReduce = {
        "@type": "workbench/metric_reduce",
        "metric_in": metric_in,
        "operation": operation,
        "metric_out": metric_out,
        "only_numeric": only_numeric,
    }
    if exclude_outliers is not None:
        params["exclude_outliers"] = exclude_outliers
    return params

def metric_reduce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricReduce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("exclude_outliers", None) is not None:
        if not isinstance(params["exclude_outliers"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_outliers"])}\'')
        if params["exclude_outliers"].get("sigma_below", None) is None:
            raise StyxValidationError("`sigma_below` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_below"], (float, int)):
            raise StyxValidationError(f'`sigma_below` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_below"])}` expected `float`')
        if params["exclude_outliers"].get("sigma_above", None) is None:
            raise StyxValidationError("`sigma_above` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_above"], (float, int)):
            raise StyxValidationError(f'`sigma_above` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_above"])}` expected `float`')
    if params.get("only_numeric", None) is not None:
        if not isinstance(params["only_numeric"], bool):
            raise StyxValidationError(f'`only_numeric` has the wrong type: Received `{type(params["only_numeric"])}` expected `bool`')

def metric_reduce_cargs(params: MetricReduce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-reduce")
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["operation"])
    cargs.append(params["metric_out"])
    v_0 = params.get("exclude_outliers")
    if v_0 is not None:
        cargs.append("-exclude-outliers")
        cargs.append(str(v_0["sigma_below"]))
        cargs.append(str(v_0["sigma_above"]))
    if params.get("only_numeric"):
        cargs.append("-only-numeric")
    return cargs

def metric_reduce_outputs(params: MetricReduce, execution: Execution) -> MetricReduceOutputs:
    """Build the MetricReduceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricReduceOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_reduce_execute(params: MetricReduce, runner: Runner | None = None) -> MetricReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ACROSS METRIC COLUMNS

    For each surface vertex, takes the data across columns as a vector, and performs the specified reduction on it, putting the result into the single output column at that vertex.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_reduce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_REDUCE_METADATA)
    execution.params(params)
    args = metric_reduce_cargs(params, execution)
    out = metric_reduce_outputs(params, execution)
    execution.run(args)
    return out

def metric_reduce(
    metric_in: InputPathType,
    operation: str,
    metric_out: str,
    exclude_outliers: MetricReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
    runner: Runner | None = None,
) -> MetricReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ACROSS METRIC COLUMNS

    For each surface vertex, takes the data across columns as a vector, and performs the specified reduction on it, putting the result into the single output column at that vertex.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        metric_in: the metric to reduce
        operation: the reduction operator to use
        metric_out: the output metric
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_reduce_params(
        metric_in=metric_in,
        operation=operation,
        metric_out=metric_out,
        exclude_outliers=exclude_outliers,
        only_numeric=only_numeric,
    )
    return metric_reduce_execute(params, runner)

__all__ = [
    "MetricReduce",
    "MetricReduceOutputs",
    "METRIC_REDUCE_METADATA",
    "metric_reduce_cargs",
    "metric_reduce_outputs",
    "metric_reduce_params",
    "metric_reduce_execute",
    "metric_reduce_validate",
    "metric_reduce",
]