# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_MERGE_DENSE_METADATA = Metadata(
    id="cifti_merge_dense",
    name="MERGE CIFTI FILES ALONG DENSE DIMENSION",
    package="workbench",
)

CiftiMergeDense = typing.TypedDict(
    "CiftiMergeDense",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_merge_dense"]],
        "direction": str,
        "cifti_out": str,
        "label_collision": typing.NotRequired[str],
        "cifti": list[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiMergeDenseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_merge_dense_params(
    direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
    label_collision: str | None = None,
) -> CiftiMergeDense:
    """
    Build parameters.

    Args:
        direction: which dimension to merge along, ROW or COLUMN
        cifti_out: the output cifti file
        cifti: specify an input cifti file
        label_collision: how to handle conflicts between label keys

    Returns:
        Parameter dictionary.
    """
    params: CiftiMergeDense = {
        "@type": "workbench/cifti_merge_dense",
        "direction": direction,
        "cifti_out": cifti_out,
        "cifti": cifti,
    }
    if label_collision is not None:
        params["label_collision"] = label_collision
    return params

def cifti_merge_dense_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiMergeDense."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("label_collision", None) is not None:
        if not isinstance(params["label_collision"], str):
            raise StyxValidationError(f'`label_collision` has the wrong type: Received `{type(params["label_collision"])}` expected `str`')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], list):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `list[InputPathType]`')
    for e in params["cifti"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def cifti_merge_dense_cargs(params: CiftiMergeDense, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-merge-dense")
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("label_collision")
    if v_0 is not None:
        cargs.append("-label-collision")
        cargs.append(v_0)
    for item0 in params["cifti"]:
        cargs.append("-cifti")
        cargs.append(execution.input_file(item0))
    return cargs

def cifti_merge_dense_outputs(params: CiftiMergeDense, execution: Execution) -> CiftiMergeDenseOutputs:
    """Build the CiftiMergeDenseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiMergeDenseOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_merge_dense_execute(params: CiftiMergeDense, runner: Runner | None = None) -> CiftiMergeDenseOutputs:
    """
    MERGE CIFTI FILES ALONG DENSE DIMENSION

    The input cifti files must have matching mappings along the direction not specified, and the mapping along the specified direction must be brain models.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_merge_dense_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_MERGE_DENSE_METADATA)
    execution.params(params)
    args = cifti_merge_dense_cargs(params, execution)
    out = cifti_merge_dense_outputs(params, execution)
    execution.run(args)
    return out

def cifti_merge_dense(
    direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
    label_collision: str | None = None,
    runner: Runner | None = None,
) -> CiftiMergeDenseOutputs:
    """
    MERGE CIFTI FILES ALONG DENSE DIMENSION

    The input cifti files must have matching mappings along the direction not specified, and the mapping along the specified direction must be brain models.

    Args:
        direction: which dimension to merge along, ROW or COLUMN
        cifti_out: the output cifti file
        cifti: specify an input cifti file
        label_collision: how to handle conflicts between label keys
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_merge_dense_params(
        direction=direction,
        cifti_out=cifti_out,
        cifti=cifti,
        label_collision=label_collision,
    )
    return cifti_merge_dense_execute(params, runner)

__all__ = [
    "CiftiMergeDense",
    "CiftiMergeDenseOutputs",
    "CIFTI_MERGE_DENSE_METADATA",
    "cifti_merge_dense_cargs",
    "cifti_merge_dense_outputs",
    "cifti_merge_dense_params",
    "cifti_merge_dense_execute",
    "cifti_merge_dense_validate",
    "cifti_merge_dense",
]