# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_SMOOTHING_METADATA = Metadata(
    id="volume_smoothing",
    name="SMOOTH A VOLUME FILE",
    package="workbench",
)

VolumeSmoothing = typing.TypedDict(
    "VolumeSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_smoothing"]],
        "volume_in": InputPathType,
        "kernel": float,
        "volume_out": str,
        "fwhm": typing.NotRequired[bool],
        "roi": typing.NotRequired[InputPathType],
        "fix_zeros": typing.NotRequired[bool],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_smoothing_params(
    volume_in: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    roi: InputPathType | None = None,
    fix_zeros: bool = False,
    subvolume: str | None = None,
) -> VolumeSmoothing:
    """
    Build parameters.

    Args:
        volume_in: the volume to smooth
        kernel: the size of the gaussian smoothing kernel in mm, as sigma by\
            default
        volume_out: the output volume
        fwhm: kernel size is FWHM, not sigma
        roi: smooth only from data within an ROI
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume to smooth

    Returns:
        Parameter dictionary.
    """
    params: VolumeSmoothing = {
        "@type": "workbench/volume_smoothing",
        "volume_in": volume_in,
        "kernel": kernel,
        "volume_out": volume_out,
        "fwhm": fwhm,
        "fix_zeros": fix_zeros,
    }
    if roi is not None:
        params["roi"] = roi
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("kernel", None) is None:
        raise StyxValidationError("`kernel` must not be None")
    if not isinstance(params["kernel"], (float, int)):
        raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["kernel"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], bool):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("fix_zeros", None) is not None:
        if not isinstance(params["fix_zeros"], bool):
            raise StyxValidationError(f'`fix_zeros` has the wrong type: Received `{type(params["fix_zeros"])}` expected `bool`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_smoothing_cargs(params: VolumeSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-smoothing")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(str(params["kernel"]))
    cargs.append(params["volume_out"])
    if params.get("fwhm"):
        cargs.append("-fwhm")
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    if params.get("fix_zeros"):
        cargs.append("-fix-zeros")
    v_1 = params.get("subvolume")
    if v_1 is not None:
        cargs.append("-subvolume")
        cargs.append(v_1)
    return cargs

def volume_smoothing_outputs(params: VolumeSmoothing, execution: Execution) -> VolumeSmoothingOutputs:
    """Build the VolumeSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeSmoothingOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_smoothing_execute(params: VolumeSmoothing, runner: Runner | None = None) -> VolumeSmoothingOutputs:
    """
    SMOOTH A VOLUME FILE

    Gaussian smoothing for volumes.  By default, smooths all subvolumes with no ROI, if ROI is given, only positive voxels in the ROI volume have their values used, and all other voxels are set to zero.  Smoothing a non-orthogonal volume will be significantly slower, because the operation cannot be separated into 1-dimensional smoothings without distorting the kernel shape.

The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the voxel.  This is useful for zeros that indicate lack of information, preventing them from pulling down the intensity of nearby voxels, while giving the zero an extrapolated value.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_SMOOTHING_METADATA)
    execution.params(params)
    args = volume_smoothing_cargs(params, execution)
    out = volume_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def volume_smoothing(
    volume_in: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    roi: InputPathType | None = None,
    fix_zeros: bool = False,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeSmoothingOutputs:
    """
    SMOOTH A VOLUME FILE

    Gaussian smoothing for volumes.  By default, smooths all subvolumes with no ROI, if ROI is given, only positive voxels in the ROI volume have their values used, and all other voxels are set to zero.  Smoothing a non-orthogonal volume will be significantly slower, because the operation cannot be separated into 1-dimensional smoothings without distorting the kernel shape.

The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the voxel.  This is useful for zeros that indicate lack of information, preventing them from pulling down the intensity of nearby voxels, while giving the zero an extrapolated value.

    Args:
        volume_in: the volume to smooth
        kernel: the size of the gaussian smoothing kernel in mm, as sigma by\
            default
        volume_out: the output volume
        fwhm: kernel size is FWHM, not sigma
        roi: smooth only from data within an ROI
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume to smooth
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_smoothing_params(
        volume_in=volume_in,
        kernel=kernel,
        volume_out=volume_out,
        fwhm=fwhm,
        roi=roi,
        fix_zeros=fix_zeros,
        subvolume=subvolume,
    )
    return volume_smoothing_execute(params, runner)

__all__ = [
    "VolumeSmoothing",
    "VolumeSmoothingOutputs",
    "VOLUME_SMOOTHING_METADATA",
    "volume_smoothing_cargs",
    "volume_smoothing_outputs",
    "volume_smoothing_params",
    "volume_smoothing_execute",
    "volume_smoothing_validate",
    "volume_smoothing",
]