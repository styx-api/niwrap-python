# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_FILL_HOLES_METADATA = Metadata(
    id="metric_fill_holes",
    name="FILL HOLES IN AN ROI METRIC",
    package="workbench",
)

MetricFillHoles = typing.TypedDict(
    "MetricFillHoles",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_fill_holes"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "metric_out": str,
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MetricFillHolesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output ROI metric"""

def metric_fill_holes_params(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    corrected_areas: InputPathType | None = None,
) -> MetricFillHoles:
    """
    Build parameters.

    Args:
        surface: the surface to use for neighbor information
        metric_in: the input ROI metric
        metric_out: the output ROI metric
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: MetricFillHoles = {
        "@type": "workbench/metric_fill_holes",
        "surface": surface,
        "metric_in": metric_in,
        "metric_out": metric_out,
    }
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_fill_holes_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricFillHoles."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def metric_fill_holes_cargs(params: MetricFillHoles, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-fill-holes")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("corrected_areas")
    if v_0 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_0))
    return cargs

def metric_fill_holes_outputs(params: MetricFillHoles, execution: Execution) -> MetricFillHolesOutputs:
    """Build the MetricFillHolesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricFillHolesOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_fill_holes_execute(params: MetricFillHoles, runner: Runner | None = None) -> MetricFillHolesOutputs:
    """
    FILL HOLES IN AN ROI METRIC

    Finds all connected areas that are not included in the ROI, and writes ones into all but the largest one, in terms of surface area.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_fill_holes_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_FILL_HOLES_METADATA)
    execution.params(params)
    args = metric_fill_holes_cargs(params, execution)
    out = metric_fill_holes_outputs(params, execution)
    execution.run(args)
    return out

def metric_fill_holes(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> MetricFillHolesOutputs:
    """
    FILL HOLES IN AN ROI METRIC

    Finds all connected areas that are not included in the ROI, and writes ones into all but the largest one, in terms of surface area.

    Args:
        surface: the surface to use for neighbor information
        metric_in: the input ROI metric
        metric_out: the output ROI metric
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_fill_holes_params(
        surface=surface,
        metric_in=metric_in,
        metric_out=metric_out,
        corrected_areas=corrected_areas,
    )
    return metric_fill_holes_execute(params, runner)

__all__ = [
    "MetricFillHoles",
    "MetricFillHolesOutputs",
    "METRIC_FILL_HOLES_METADATA",
    "metric_fill_holes_cargs",
    "metric_fill_holes_outputs",
    "metric_fill_holes_params",
    "metric_fill_holes_execute",
    "metric_fill_holes_validate",
    "metric_fill_holes",
]