# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_EXTREMA_METADATA = Metadata(
    id="cifti_extrema",
    name="FIND EXTREMA IN A CIFTI FILE",
    package="workbench",
)

class CiftiExtremaThreshold(typing.TypedDict):
    low: float
    """the largest value to consider for being a minimum"""
    high: float
    """the smallest value to consider for being a maximum"""

CiftiExtrema = typing.TypedDict(
    "CiftiExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_extrema"]],
        "cifti": InputPathType,
        "surface_distance": float,
        "volume_distance": float,
        "direction": str,
        "cifti_out": str,
        "left_surface": typing.NotRequired[InputPathType],
        "right_surface": typing.NotRequired[InputPathType],
        "cerebellum_surface": typing.NotRequired[InputPathType],
        "surface_presmooth": typing.NotRequired[float],
        "volume_presmooth": typing.NotRequired[float],
        "presmooth_fwhm": typing.NotRequired[bool],
        "threshold": typing.NotRequired[CiftiExtremaThreshold],
        "merged_volume": typing.NotRequired[bool],
        "sum_maps": typing.NotRequired[bool],
        "consolidate_mode": typing.NotRequired[bool],
        "only_maxima": typing.NotRequired[bool],
        "only_minima": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti"""

def cifti_extrema_params(
    cifti: InputPathType,
    surface_distance: float,
    volume_distance: float,
    direction: str,
    cifti_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
    surface_presmooth: float | None = None,
    volume_presmooth: float | None = None,
    presmooth_fwhm: bool = False,
    threshold: CiftiExtremaThreshold | None = None,
    merged_volume: bool = False,
    sum_maps: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
) -> CiftiExtrema:
    """
    Build parameters.

    Args:
        cifti: the input cifti
        surface_distance: the minimum distance between extrema of the same type,\
            for surface components
        volume_distance: the minimum distance between extrema of the same type, for\
            volume components
        direction: which dimension to find extrema along, ROW or COLUMN
        cifti_out: the output cifti
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use
        surface_presmooth: smooth on the surface before finding extrema
        volume_presmooth: smooth volume components before finding extrema
        presmooth_fwhm: smoothing kernel distances are FWHM, not sigma
        threshold: ignore small extrema
        merged_volume: treat volume components as if they were a single component
        sum_maps: output the sum of the extrema maps instead of each map separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima

    Returns:
        Parameter dictionary.
    """
    params: CiftiExtrema = {
        "@type": "workbench/cifti_extrema",
        "cifti": cifti,
        "surface_distance": surface_distance,
        "volume_distance": volume_distance,
        "direction": direction,
        "cifti_out": cifti_out,
        "presmooth_fwhm": presmooth_fwhm,
        "merged_volume": merged_volume,
        "sum_maps": sum_maps,
        "consolidate_mode": consolidate_mode,
        "only_maxima": only_maxima,
        "only_minima": only_minima,
    }
    if left_surface is not None:
        params["left_surface"] = left_surface
    if right_surface is not None:
        params["right_surface"] = right_surface
    if cerebellum_surface is not None:
        params["cerebellum_surface"] = cerebellum_surface
    if surface_presmooth is not None:
        params["surface_presmooth"] = surface_presmooth
    if volume_presmooth is not None:
        params["volume_presmooth"] = volume_presmooth
    if threshold is not None:
        params["threshold"] = threshold
    return params

def cifti_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `InputPathType`')
    if params.get("surface_distance", None) is None:
        raise StyxValidationError("`surface_distance` must not be None")
    if not isinstance(params["surface_distance"], (float, int)):
        raise StyxValidationError(f'`surface_distance` has the wrong type: Received `{type(params["surface_distance"])}` expected `float`')
    if params.get("volume_distance", None) is None:
        raise StyxValidationError("`volume_distance` must not be None")
    if not isinstance(params["volume_distance"], (float, int)):
        raise StyxValidationError(f'`volume_distance` has the wrong type: Received `{type(params["volume_distance"])}` expected `float`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("left_surface", None) is not None:
        if not isinstance(params["left_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`left_surface` has the wrong type: Received `{type(params["left_surface"])}` expected `InputPathType`')
    if params.get("right_surface", None) is not None:
        if not isinstance(params["right_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`right_surface` has the wrong type: Received `{type(params["right_surface"])}` expected `InputPathType`')
    if params.get("cerebellum_surface", None) is not None:
        if not isinstance(params["cerebellum_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cerebellum_surface` has the wrong type: Received `{type(params["cerebellum_surface"])}` expected `InputPathType`')
    if params.get("surface_presmooth", None) is not None:
        if not isinstance(params["surface_presmooth"], (float, int)):
            raise StyxValidationError(f'`surface_presmooth` has the wrong type: Received `{type(params["surface_presmooth"])}` expected `float`')
    if params.get("volume_presmooth", None) is not None:
        if not isinstance(params["volume_presmooth"], (float, int)):
            raise StyxValidationError(f'`volume_presmooth` has the wrong type: Received `{type(params["volume_presmooth"])}` expected `float`')
    if params.get("presmooth_fwhm", None) is not None:
        if not isinstance(params["presmooth_fwhm"], bool):
            raise StyxValidationError(f'`presmooth_fwhm` has the wrong type: Received `{type(params["presmooth_fwhm"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["threshold"])}\'')
        if params["threshold"].get("low", None) is None:
            raise StyxValidationError("`low` must not be None")
        if not isinstance(params["threshold"]["low"], (float, int)):
            raise StyxValidationError(f'`low` has the wrong type: Received `{type(params["threshold"]["low"])}` expected `float`')
        if params["threshold"].get("high", None) is None:
            raise StyxValidationError("`high` must not be None")
        if not isinstance(params["threshold"]["high"], (float, int)):
            raise StyxValidationError(f'`high` has the wrong type: Received `{type(params["threshold"]["high"])}` expected `float`')
    if params.get("merged_volume", None) is not None:
        if not isinstance(params["merged_volume"], bool):
            raise StyxValidationError(f'`merged_volume` has the wrong type: Received `{type(params["merged_volume"])}` expected `bool`')
    if params.get("sum_maps", None) is not None:
        if not isinstance(params["sum_maps"], bool):
            raise StyxValidationError(f'`sum_maps` has the wrong type: Received `{type(params["sum_maps"])}` expected `bool`')
    if params.get("consolidate_mode", None) is not None:
        if not isinstance(params["consolidate_mode"], bool):
            raise StyxValidationError(f'`consolidate_mode` has the wrong type: Received `{type(params["consolidate_mode"])}` expected `bool`')
    if params.get("only_maxima", None) is not None:
        if not isinstance(params["only_maxima"], bool):
            raise StyxValidationError(f'`only_maxima` has the wrong type: Received `{type(params["only_maxima"])}` expected `bool`')
    if params.get("only_minima", None) is not None:
        if not isinstance(params["only_minima"], bool):
            raise StyxValidationError(f'`only_minima` has the wrong type: Received `{type(params["only_minima"])}` expected `bool`')

def cifti_extrema_cargs(params: CiftiExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-extrema")
    cargs.append(execution.input_file(params["cifti"]))
    cargs.append(str(params["surface_distance"]))
    cargs.append(str(params["volume_distance"]))
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("left_surface")
    if v_0 is not None:
        cargs.append("-left-surface")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("right_surface")
    if v_1 is not None:
        cargs.append("-right-surface")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("cerebellum_surface")
    if v_2 is not None:
        cargs.append("-cerebellum-surface")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("surface_presmooth")
    if v_3 is not None:
        cargs.append("-surface-presmooth")
        cargs.append(str(v_3))
    v_4 = params.get("volume_presmooth")
    if v_4 is not None:
        cargs.append("-volume-presmooth")
        cargs.append(str(v_4))
    if params.get("presmooth_fwhm"):
        cargs.append("-presmooth-fwhm")
    v_5 = params.get("threshold")
    if v_5 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_5["low"]))
        cargs.append(str(v_5["high"]))
    if params.get("merged_volume"):
        cargs.append("-merged-volume")
    if params.get("sum_maps"):
        cargs.append("-sum-maps")
    if params.get("consolidate_mode"):
        cargs.append("-consolidate-mode")
    if params.get("only_maxima"):
        cargs.append("-only-maxima")
    if params.get("only_minima"):
        cargs.append("-only-minima")
    return cargs

def cifti_extrema_outputs(params: CiftiExtrema, execution: Execution) -> CiftiExtremaOutputs:
    """Build the CiftiExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiExtremaOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_extrema_execute(params: CiftiExtrema, runner: Runner | None = None) -> CiftiExtremaOutputs:
    """
    FIND EXTREMA IN A CIFTI FILE

    Finds spatial locations in a cifti file that have more extreme values than all nearby locations in the same component (surface or volume structure).  The input cifti file must have a brain models mapping along the specified direction.  COLUMN is the direction that works on dtseries and dscalar.  For dconn, if it is symmetric use COLUMN, otherwise use ROW.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_EXTREMA_METADATA)
    execution.params(params)
    args = cifti_extrema_cargs(params, execution)
    out = cifti_extrema_outputs(params, execution)
    execution.run(args)
    return out

def cifti_extrema(
    cifti: InputPathType,
    surface_distance: float,
    volume_distance: float,
    direction: str,
    cifti_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
    surface_presmooth: float | None = None,
    volume_presmooth: float | None = None,
    presmooth_fwhm: bool = False,
    threshold: CiftiExtremaThreshold | None = None,
    merged_volume: bool = False,
    sum_maps: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
    runner: Runner | None = None,
) -> CiftiExtremaOutputs:
    """
    FIND EXTREMA IN A CIFTI FILE

    Finds spatial locations in a cifti file that have more extreme values than all nearby locations in the same component (surface or volume structure).  The input cifti file must have a brain models mapping along the specified direction.  COLUMN is the direction that works on dtseries and dscalar.  For dconn, if it is symmetric use COLUMN, otherwise use ROW.

    Args:
        cifti: the input cifti
        surface_distance: the minimum distance between extrema of the same type,\
            for surface components
        volume_distance: the minimum distance between extrema of the same type, for\
            volume components
        direction: which dimension to find extrema along, ROW or COLUMN
        cifti_out: the output cifti
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use
        surface_presmooth: smooth on the surface before finding extrema
        volume_presmooth: smooth volume components before finding extrema
        presmooth_fwhm: smoothing kernel distances are FWHM, not sigma
        threshold: ignore small extrema
        merged_volume: treat volume components as if they were a single component
        sum_maps: output the sum of the extrema maps instead of each map separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_extrema_params(
        cifti=cifti,
        surface_distance=surface_distance,
        volume_distance=volume_distance,
        direction=direction,
        cifti_out=cifti_out,
        left_surface=left_surface,
        right_surface=right_surface,
        cerebellum_surface=cerebellum_surface,
        surface_presmooth=surface_presmooth,
        volume_presmooth=volume_presmooth,
        presmooth_fwhm=presmooth_fwhm,
        threshold=threshold,
        merged_volume=merged_volume,
        sum_maps=sum_maps,
        consolidate_mode=consolidate_mode,
        only_maxima=only_maxima,
        only_minima=only_minima,
    )
    return cifti_extrema_execute(params, runner)

__all__ = [
    "CiftiExtrema",
    "CiftiExtremaOutputs",
    "CIFTI_EXTREMA_METADATA",
    "cifti_extrema_cargs",
    "cifti_extrema_outputs",
    "cifti_extrema_params",
    "cifti_extrema_execute",
    "cifti_extrema_validate",
    "cifti_extrema",
]