# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_FLIP_LR_METADATA = Metadata(
    id="surface_flip_lr",
    name="MIRROR A SURFACE THROUGH THE YZ PLANE",
    package="workbench",
)

SurfaceFlipLr = typing.TypedDict(
    "SurfaceFlipLr",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_flip_lr"]],
        "surface": InputPathType,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceFlipLrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the output flipped surface"""

def surface_flip_lr_params(
    surface: InputPathType,
    surface_out: str,
) -> SurfaceFlipLr:
    """
    Build parameters.

    Args:
        surface: the surface to flip
        surface_out: the output flipped surface

    Returns:
        Parameter dictionary.
    """
    params: SurfaceFlipLr = {
        "@type": "workbench/surface_flip_lr",
        "surface": surface,
        "surface_out": surface_out,
    }
    return params

def surface_flip_lr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceFlipLr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_flip_lr_cargs(params: SurfaceFlipLr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-flip-lr")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_flip_lr_outputs(params: SurfaceFlipLr, execution: Execution) -> SurfaceFlipLrOutputs:
    """Build the SurfaceFlipLrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceFlipLrOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_flip_lr_execute(params: SurfaceFlipLr, runner: Runner | None = None) -> SurfaceFlipLrOutputs:
    """
    MIRROR A SURFACE THROUGH THE YZ PLANE

    This command negates the x coordinate of each vertex, and flips the surface normals, so that you have a surface of opposite handedness with the same features and vertex correspondence, with normals consistent with the original surface.  That is, if the input surface has normals facing outward, the output surface will also have normals facing outward.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_flip_lr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_FLIP_LR_METADATA)
    execution.params(params)
    args = surface_flip_lr_cargs(params, execution)
    out = surface_flip_lr_outputs(params, execution)
    execution.run(args)
    return out

def surface_flip_lr(
    surface: InputPathType,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceFlipLrOutputs:
    """
    MIRROR A SURFACE THROUGH THE YZ PLANE

    This command negates the x coordinate of each vertex, and flips the surface normals, so that you have a surface of opposite handedness with the same features and vertex correspondence, with normals consistent with the original surface.  That is, if the input surface has normals facing outward, the output surface will also have normals facing outward.

    Args:
        surface: the surface to flip
        surface_out: the output flipped surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_flip_lr_params(
        surface=surface,
        surface_out=surface_out,
    )
    return surface_flip_lr_execute(params, runner)

__all__ = [
    "SurfaceFlipLr",
    "SurfaceFlipLrOutputs",
    "SURFACE_FLIP_LR_METADATA",
    "surface_flip_lr_cargs",
    "surface_flip_lr_outputs",
    "surface_flip_lr_params",
    "surface_flip_lr_execute",
    "surface_flip_lr_validate",
    "surface_flip_lr",
]