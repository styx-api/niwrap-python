# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_ROIS_TO_BORDER_METADATA = Metadata(
    id="metric_rois_to_border",
    name="DRAW BORDERS AROUND METRIC ROIS",
    package="workbench",
)

MetricRoisToBorder = typing.TypedDict(
    "MetricRoisToBorder",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_rois_to_border"]],
        "surface": InputPathType,
        "metric": InputPathType,
        "class_name": str,
        "border_out": str,
        "placement": typing.NotRequired[float],
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricRoisToBorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    border_out: OutputPathType
    """the output border file"""

def metric_rois_to_border_params(
    surface: InputPathType,
    metric: InputPathType,
    class_name: str,
    border_out: str,
    placement: float | None = None,
    column: str | None = None,
) -> MetricRoisToBorder:
    """
    Build parameters.

    Args:
        surface: the surface to use for neighbor information
        metric: the input metric containing ROIs
        class_name: the name to use for the class of the output borders
        border_out: the output border file
        placement: set how far along the edge border points are drawn
        column: select a single column

    Returns:
        Parameter dictionary.
    """
    params: MetricRoisToBorder = {
        "@type": "workbench/metric_rois_to_border",
        "surface": surface,
        "metric": metric,
        "class_name": class_name,
        "border_out": border_out,
    }
    if placement is not None:
        params["placement"] = placement
    if column is not None:
        params["column"] = column
    return params

def metric_rois_to_border_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricRoisToBorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `InputPathType`')
    if params.get("class_name", None) is None:
        raise StyxValidationError("`class_name` must not be None")
    if not isinstance(params["class_name"], str):
        raise StyxValidationError(f'`class_name` has the wrong type: Received `{type(params["class_name"])}` expected `str`')
    if params.get("border_out", None) is None:
        raise StyxValidationError("`border_out` must not be None")
    if not isinstance(params["border_out"], str):
        raise StyxValidationError(f'`border_out` has the wrong type: Received `{type(params["border_out"])}` expected `str`')
    if params.get("placement", None) is not None:
        if not isinstance(params["placement"], (float, int)):
            raise StyxValidationError(f'`placement` has the wrong type: Received `{type(params["placement"])}` expected `float`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def metric_rois_to_border_cargs(params: MetricRoisToBorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-rois-to-border")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric"]))
    cargs.append(params["class_name"])
    cargs.append(params["border_out"])
    v_0 = params.get("placement")
    if v_0 is not None:
        cargs.append("-placement")
        cargs.append(str(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    return cargs

def metric_rois_to_border_outputs(params: MetricRoisToBorder, execution: Execution) -> MetricRoisToBorderOutputs:
    """Build the MetricRoisToBorderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    border_out_v: OutputPathType = execution.output_file(params["border_out"])
    return MetricRoisToBorderOutputs(
        root=root_v,
        border_out=border_out_v,
    )

def metric_rois_to_border_execute(params: MetricRoisToBorder, runner: Runner | None = None) -> MetricRoisToBorderOutputs:
    """
    DRAW BORDERS AROUND METRIC ROIS

    For each ROI column, finds all edges on the mesh that cross the boundary of the ROI, and draws borders through them.  By default, this is done on all columns in the input file, using the map name as the name for the border.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_rois_to_border_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_ROIS_TO_BORDER_METADATA)
    execution.params(params)
    args = metric_rois_to_border_cargs(params, execution)
    out = metric_rois_to_border_outputs(params, execution)
    execution.run(args)
    return out

def metric_rois_to_border(
    surface: InputPathType,
    metric: InputPathType,
    class_name: str,
    border_out: str,
    placement: float | None = None,
    column: str | None = None,
    runner: Runner | None = None,
) -> MetricRoisToBorderOutputs:
    """
    DRAW BORDERS AROUND METRIC ROIS

    For each ROI column, finds all edges on the mesh that cross the boundary of the ROI, and draws borders through them.  By default, this is done on all columns in the input file, using the map name as the name for the border.

    Args:
        surface: the surface to use for neighbor information
        metric: the input metric containing ROIs
        class_name: the name to use for the class of the output borders
        border_out: the output border file
        placement: set how far along the edge border points are drawn
        column: select a single column
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_rois_to_border_params(
        surface=surface,
        metric=metric,
        class_name=class_name,
        border_out=border_out,
        placement=placement,
        column=column,
    )
    return metric_rois_to_border_execute(params, runner)

__all__ = [
    "MetricRoisToBorder",
    "MetricRoisToBorderOutputs",
    "METRIC_ROIS_TO_BORDER_METADATA",
    "metric_rois_to_border_cargs",
    "metric_rois_to_border_outputs",
    "metric_rois_to_border_params",
    "metric_rois_to_border_execute",
    "metric_rois_to_border_validate",
    "metric_rois_to_border",
]