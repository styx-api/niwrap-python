# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_ADJACENCY_METADATA = Metadata(
    id="cifti_label_adjacency",
    name="MAKE ADJACENCY MATRIX OF A CIFTI LABEL FILE",
    package="workbench",
)

CiftiLabelAdjacency = typing.TypedDict(
    "CiftiLabelAdjacency",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_adjacency"]],
        "label_in": InputPathType,
        "adjacency_out": str,
        "left_surface": typing.NotRequired[InputPathType],
        "right_surface": typing.NotRequired[InputPathType],
        "cerebellum_surface": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiLabelAdjacencyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    adjacency_out: OutputPathType
    """the output cifti pconn adjacency matrix"""

def cifti_label_adjacency_params(
    label_in: InputPathType,
    adjacency_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
) -> CiftiLabelAdjacency:
    """
    Build parameters.

    Args:
        label_in: the input cifti label file
        adjacency_out: the output cifti pconn adjacency matrix
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelAdjacency = {
        "@type": "workbench/cifti_label_adjacency",
        "label_in": label_in,
        "adjacency_out": adjacency_out,
    }
    if left_surface is not None:
        params["left_surface"] = left_surface
    if right_surface is not None:
        params["right_surface"] = right_surface
    if cerebellum_surface is not None:
        params["cerebellum_surface"] = cerebellum_surface
    return params

def cifti_label_adjacency_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelAdjacency."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("adjacency_out", None) is None:
        raise StyxValidationError("`adjacency_out` must not be None")
    if not isinstance(params["adjacency_out"], str):
        raise StyxValidationError(f'`adjacency_out` has the wrong type: Received `{type(params["adjacency_out"])}` expected `str`')
    if params.get("left_surface", None) is not None:
        if not isinstance(params["left_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`left_surface` has the wrong type: Received `{type(params["left_surface"])}` expected `InputPathType`')
    if params.get("right_surface", None) is not None:
        if not isinstance(params["right_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`right_surface` has the wrong type: Received `{type(params["right_surface"])}` expected `InputPathType`')
    if params.get("cerebellum_surface", None) is not None:
        if not isinstance(params["cerebellum_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cerebellum_surface` has the wrong type: Received `{type(params["cerebellum_surface"])}` expected `InputPathType`')

def cifti_label_adjacency_cargs(params: CiftiLabelAdjacency, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-adjacency")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["adjacency_out"])
    v_0 = params.get("left_surface")
    if v_0 is not None:
        cargs.append("-left-surface")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("right_surface")
    if v_1 is not None:
        cargs.append("-right-surface")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("cerebellum_surface")
    if v_2 is not None:
        cargs.append("-cerebellum-surface")
        cargs.append(execution.input_file(v_2))
    return cargs

def cifti_label_adjacency_outputs(params: CiftiLabelAdjacency, execution: Execution) -> CiftiLabelAdjacencyOutputs:
    """Build the CiftiLabelAdjacencyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    adjacency_out_v: OutputPathType = execution.output_file(params["adjacency_out"])
    return CiftiLabelAdjacencyOutputs(
        root=root_v,
        adjacency_out=adjacency_out_v,
    )

def cifti_label_adjacency_execute(params: CiftiLabelAdjacency, runner: Runner | None = None) -> CiftiLabelAdjacencyOutputs:
    """
    MAKE ADJACENCY MATRIX OF A CIFTI LABEL FILE

    Find face-adjacent voxels and connected vertices that have different label values, and count them for each pair.  Put the resulting counts into a parcellated connectivity file, with the diagonal being zero.  This gives a rough estimate of how long or expansive the border between two labels is.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_adjacency_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_ADJACENCY_METADATA)
    execution.params(params)
    args = cifti_label_adjacency_cargs(params, execution)
    out = cifti_label_adjacency_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_adjacency(
    label_in: InputPathType,
    adjacency_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
    runner: Runner | None = None,
) -> CiftiLabelAdjacencyOutputs:
    """
    MAKE ADJACENCY MATRIX OF A CIFTI LABEL FILE

    Find face-adjacent voxels and connected vertices that have different label values, and count them for each pair.  Put the resulting counts into a parcellated connectivity file, with the diagonal being zero.  This gives a rough estimate of how long or expansive the border between two labels is.

    Args:
        label_in: the input cifti label file
        adjacency_out: the output cifti pconn adjacency matrix
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_adjacency_params(
        label_in=label_in,
        adjacency_out=adjacency_out,
        left_surface=left_surface,
        right_surface=right_surface,
        cerebellum_surface=cerebellum_surface,
    )
    return cifti_label_adjacency_execute(params, runner)

__all__ = [
    "CiftiLabelAdjacency",
    "CiftiLabelAdjacencyOutputs",
    "CIFTI_LABEL_ADJACENCY_METADATA",
    "cifti_label_adjacency_cargs",
    "cifti_label_adjacency_outputs",
    "cifti_label_adjacency_params",
    "cifti_label_adjacency_execute",
    "cifti_label_adjacency_validate",
    "cifti_label_adjacency",
]