# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_REMOVE_ISLANDS_METADATA = Metadata(
    id="metric_remove_islands",
    name="REMOVE ISLANDS FROM AN ROI METRIC",
    package="workbench",
)

MetricRemoveIslands = typing.TypedDict(
    "MetricRemoveIslands",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_remove_islands"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "metric_out": str,
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MetricRemoveIslandsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output ROI metric"""

def metric_remove_islands_params(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    corrected_areas: InputPathType | None = None,
) -> MetricRemoveIslands:
    """
    Build parameters.

    Args:
        surface: the surface to use for neighbor information
        metric_in: the input ROI metric
        metric_out: the output ROI metric
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: MetricRemoveIslands = {
        "@type": "workbench/metric_remove_islands",
        "surface": surface,
        "metric_in": metric_in,
        "metric_out": metric_out,
    }
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_remove_islands_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricRemoveIslands."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def metric_remove_islands_cargs(params: MetricRemoveIslands, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-remove-islands")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("corrected_areas")
    if v_0 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_0))
    return cargs

def metric_remove_islands_outputs(params: MetricRemoveIslands, execution: Execution) -> MetricRemoveIslandsOutputs:
    """Build the MetricRemoveIslandsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricRemoveIslandsOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_remove_islands_execute(params: MetricRemoveIslands, runner: Runner | None = None) -> MetricRemoveIslandsOutputs:
    """
    REMOVE ISLANDS FROM AN ROI METRIC

    Finds all connected areas in the ROI, and zeros out all but the largest one, in terms of surface area.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_remove_islands_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_REMOVE_ISLANDS_METADATA)
    execution.params(params)
    args = metric_remove_islands_cargs(params, execution)
    out = metric_remove_islands_outputs(params, execution)
    execution.run(args)
    return out

def metric_remove_islands(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> MetricRemoveIslandsOutputs:
    """
    REMOVE ISLANDS FROM AN ROI METRIC

    Finds all connected areas in the ROI, and zeros out all but the largest one, in terms of surface area.

    Args:
        surface: the surface to use for neighbor information
        metric_in: the input ROI metric
        metric_out: the output ROI metric
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_remove_islands_params(
        surface=surface,
        metric_in=metric_in,
        metric_out=metric_out,
        corrected_areas=corrected_areas,
    )
    return metric_remove_islands_execute(params, runner)

__all__ = [
    "MetricRemoveIslands",
    "MetricRemoveIslandsOutputs",
    "METRIC_REMOVE_ISLANDS_METADATA",
    "metric_remove_islands_cargs",
    "metric_remove_islands_outputs",
    "metric_remove_islands_params",
    "metric_remove_islands_execute",
    "metric_remove_islands_validate",
    "metric_remove_islands",
]