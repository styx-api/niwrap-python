# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_DILATE_METADATA = Metadata(
    id="label_dilate",
    name="DILATE A LABEL FILE",
    package="workbench",
)

LabelDilate = typing.TypedDict(
    "LabelDilate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_dilate"]],
        "label": InputPathType,
        "surface": InputPathType,
        "dilate_dist": float,
        "label_out": str,
        "bad_vertex_roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class LabelDilateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label file"""

def label_dilate_params(
    label: InputPathType,
    surface: InputPathType,
    dilate_dist: float,
    label_out: str,
    bad_vertex_roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
) -> LabelDilate:
    """
    Build parameters.

    Args:
        label: the input label
        surface: the surface to dilate on
        dilate_dist: distance in mm to dilate the labels
        label_out: the output label file
        bad_vertex_roi: specify an roi of vertices to overwrite, rather than\
            vertices with the unlabeled key
        column: select a single column to dilate
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: LabelDilate = {
        "@type": "workbench/label_dilate",
        "label": label,
        "surface": surface,
        "dilate_dist": dilate_dist,
        "label_out": label_out,
    }
    if bad_vertex_roi is not None:
        params["bad_vertex_roi"] = bad_vertex_roi
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def label_dilate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelDilate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("dilate_dist", None) is None:
        raise StyxValidationError("`dilate_dist` must not be None")
    if not isinstance(params["dilate_dist"], (float, int)):
        raise StyxValidationError(f'`dilate_dist` has the wrong type: Received `{type(params["dilate_dist"])}` expected `float`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("bad_vertex_roi", None) is not None:
        if not isinstance(params["bad_vertex_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bad_vertex_roi` has the wrong type: Received `{type(params["bad_vertex_roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def label_dilate_cargs(params: LabelDilate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-dilate")
    cargs.append(execution.input_file(params["label"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["dilate_dist"]))
    cargs.append(params["label_out"])
    v_0 = params.get("bad_vertex_roi")
    if v_0 is not None:
        cargs.append("-bad-vertex-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    v_2 = params.get("corrected_areas")
    if v_2 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_2))
    return cargs

def label_dilate_outputs(params: LabelDilate, execution: Execution) -> LabelDilateOutputs:
    """Build the LabelDilateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return LabelDilateOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def label_dilate_execute(params: LabelDilate, runner: Runner | None = None) -> LabelDilateOutputs:
    """
    DILATE A LABEL FILE

    Fills in label information for all vertices designated as bad, up to the specified distance away from other labels.  If -bad-vertex-roi is specified, all vertices, including those with the unlabeled key, are good, except for vertices with a positive value in the ROI.  If it is not specified, only vertices with the unlabeled key are bad.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_dilate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_DILATE_METADATA)
    execution.params(params)
    args = label_dilate_cargs(params, execution)
    out = label_dilate_outputs(params, execution)
    execution.run(args)
    return out

def label_dilate(
    label: InputPathType,
    surface: InputPathType,
    dilate_dist: float,
    label_out: str,
    bad_vertex_roi: InputPathType | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> LabelDilateOutputs:
    """
    DILATE A LABEL FILE

    Fills in label information for all vertices designated as bad, up to the specified distance away from other labels.  If -bad-vertex-roi is specified, all vertices, including those with the unlabeled key, are good, except for vertices with a positive value in the ROI.  If it is not specified, only vertices with the unlabeled key are bad.

    Args:
        label: the input label
        surface: the surface to dilate on
        dilate_dist: distance in mm to dilate the labels
        label_out: the output label file
        bad_vertex_roi: specify an roi of vertices to overwrite, rather than\
            vertices with the unlabeled key
        column: select a single column to dilate
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_dilate_params(
        label=label,
        surface=surface,
        dilate_dist=dilate_dist,
        label_out=label_out,
        bad_vertex_roi=bad_vertex_roi,
        column=column,
        corrected_areas=corrected_areas,
    )
    return label_dilate_execute(params, runner)

__all__ = [
    "LabelDilate",
    "LabelDilateOutputs",
    "LABEL_DILATE_METADATA",
    "label_dilate_cargs",
    "label_dilate_outputs",
    "label_dilate_params",
    "label_dilate_execute",
    "label_dilate_validate",
    "label_dilate",
]