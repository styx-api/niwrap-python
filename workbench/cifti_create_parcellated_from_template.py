# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_CREATE_PARCELLATED_FROM_TEMPLATE_METADATA = Metadata(
    id="cifti_create_parcellated_from_template",
    name="MATCH PARCELS TO TEMPLATE BY NAME",
    package="workbench",
)

CiftiCreateParcellatedFromTemplate = typing.TypedDict(
    "CiftiCreateParcellatedFromTemplate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_create_parcellated_from_template"]],
        "cifti_template": InputPathType,
        "modify_direction": str,
        "cifti_out": str,
        "fill_value": typing.NotRequired[float],
        "cifti": list[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiCreateParcellatedFromTemplateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_create_parcellated_from_template_params(
    cifti_template: InputPathType,
    modify_direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
    fill_value: float | None = None,
) -> CiftiCreateParcellatedFromTemplate:
    """
    Build parameters.

    Args:
        cifti_template: a cifti file with the template parcel mapping along column
        modify_direction: which dimension of the output file should match the\
            template (integer, 'ROW', or 'COLUMN')
        cifti_out: the output cifti file
        cifti: specify an input cifti file
        fill_value: specify value to be used in parcels that don't match

    Returns:
        Parameter dictionary.
    """
    params: CiftiCreateParcellatedFromTemplate = {
        "@type": "workbench/cifti_create_parcellated_from_template",
        "cifti_template": cifti_template,
        "modify_direction": modify_direction,
        "cifti_out": cifti_out,
        "cifti": cifti,
    }
    if fill_value is not None:
        params["fill_value"] = fill_value
    return params

def cifti_create_parcellated_from_template_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiCreateParcellatedFromTemplate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_template", None) is None:
        raise StyxValidationError("`cifti_template` must not be None")
    if not isinstance(params["cifti_template"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_template` has the wrong type: Received `{type(params["cifti_template"])}` expected `InputPathType`')
    if params.get("modify_direction", None) is None:
        raise StyxValidationError("`modify_direction` must not be None")
    if not isinstance(params["modify_direction"], str):
        raise StyxValidationError(f'`modify_direction` has the wrong type: Received `{type(params["modify_direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("fill_value", None) is not None:
        if not isinstance(params["fill_value"], (float, int)):
            raise StyxValidationError(f'`fill_value` has the wrong type: Received `{type(params["fill_value"])}` expected `float`')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], list):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `list[InputPathType]`')
    for e in params["cifti"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def cifti_create_parcellated_from_template_cargs(params: CiftiCreateParcellatedFromTemplate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-create-parcellated-from-template")
    cargs.append(execution.input_file(params["cifti_template"]))
    cargs.append(params["modify_direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("fill_value")
    if v_0 is not None:
        cargs.append("-fill-value")
        cargs.append(str(v_0))
    for item0 in params["cifti"]:
        cargs.append("-cifti")
        cargs.append(execution.input_file(item0))
    return cargs

def cifti_create_parcellated_from_template_outputs(params: CiftiCreateParcellatedFromTemplate, execution: Execution) -> CiftiCreateParcellatedFromTemplateOutputs:
    """Build the CiftiCreateParcellatedFromTemplateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiCreateParcellatedFromTemplateOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_create_parcellated_from_template_execute(params: CiftiCreateParcellatedFromTemplate, runner: Runner | None = None) -> CiftiCreateParcellatedFromTemplateOutputs:
    """
    MATCH PARCELS TO TEMPLATE BY NAME

    For each parcel name in the template mapping, find that name in an input cifti file and use its data in the output file.  All input cifti files must have a parcels mapping along <modify-direction> and matching mappings along other dimensions.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_create_parcellated_from_template_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_CREATE_PARCELLATED_FROM_TEMPLATE_METADATA)
    execution.params(params)
    args = cifti_create_parcellated_from_template_cargs(params, execution)
    out = cifti_create_parcellated_from_template_outputs(params, execution)
    execution.run(args)
    return out

def cifti_create_parcellated_from_template(
    cifti_template: InputPathType,
    modify_direction: str,
    cifti_out: str,
    cifti: list[InputPathType],
    fill_value: float | None = None,
    runner: Runner | None = None,
) -> CiftiCreateParcellatedFromTemplateOutputs:
    """
    MATCH PARCELS TO TEMPLATE BY NAME

    For each parcel name in the template mapping, find that name in an input cifti file and use its data in the output file.  All input cifti files must have a parcels mapping along <modify-direction> and matching mappings along other dimensions.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.

    Args:
        cifti_template: a cifti file with the template parcel mapping along column
        modify_direction: which dimension of the output file should match the\
            template (integer, 'ROW', or 'COLUMN')
        cifti_out: the output cifti file
        cifti: specify an input cifti file
        fill_value: specify value to be used in parcels that don't match
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_create_parcellated_from_template_params(
        cifti_template=cifti_template,
        modify_direction=modify_direction,
        cifti_out=cifti_out,
        cifti=cifti,
        fill_value=fill_value,
    )
    return cifti_create_parcellated_from_template_execute(params, runner)

__all__ = [
    "CiftiCreateParcellatedFromTemplate",
    "CiftiCreateParcellatedFromTemplateOutputs",
    "CIFTI_CREATE_PARCELLATED_FROM_TEMPLATE_METADATA",
    "cifti_create_parcellated_from_template_cargs",
    "cifti_create_parcellated_from_template_outputs",
    "cifti_create_parcellated_from_template_params",
    "cifti_create_parcellated_from_template_execute",
    "cifti_create_parcellated_from_template_validate",
    "cifti_create_parcellated_from_template",
]