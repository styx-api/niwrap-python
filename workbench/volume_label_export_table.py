# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_LABEL_EXPORT_TABLE_METADATA = Metadata(
    id="volume_label_export_table",
    name="EXPORT LABEL TABLE FROM VOLUME AS TEXT",
    package="workbench",
)

VolumeLabelExportTable = typing.TypedDict(
    "VolumeLabelExportTable",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_label_export_table"]],
        "label_in": InputPathType,
        "map": str,
        "table_out": str,
        "hierarchy": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeLabelExportTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def volume_label_export_table_params(
    label_in: InputPathType,
    map: str,
    table_out: str,
    hierarchy: str | None = None,
) -> VolumeLabelExportTable:
    """
    Build parameters.

    Args:
        label_in: the input volume label file
        map: the number or name of the label map to use
        table_out: output - the output text file
        hierarchy: export the hierarchy as json

    Returns:
        Parameter dictionary.
    """
    params: VolumeLabelExportTable = {
        "@type": "workbench/volume_label_export_table",
        "label_in": label_in,
        "map": map,
        "table_out": table_out,
    }
    if hierarchy is not None:
        params["hierarchy"] = hierarchy
    return params

def volume_label_export_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeLabelExportTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("table_out", None) is None:
        raise StyxValidationError("`table_out` must not be None")
    if not isinstance(params["table_out"], str):
        raise StyxValidationError(f'`table_out` has the wrong type: Received `{type(params["table_out"])}` expected `str`')
    if params.get("hierarchy", None) is not None:
        if not isinstance(params["hierarchy"], str):
            raise StyxValidationError(f'`hierarchy` has the wrong type: Received `{type(params["hierarchy"])}` expected `str`')

def volume_label_export_table_cargs(params: VolumeLabelExportTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-label-export-table")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["map"])
    cargs.append(params["table_out"])
    v_0 = params.get("hierarchy")
    if v_0 is not None:
        cargs.append("-hierarchy")
        cargs.append(v_0)
    return cargs

def volume_label_export_table_outputs(params: VolumeLabelExportTable, execution: Execution) -> VolumeLabelExportTableOutputs:
    """Build the VolumeLabelExportTableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolumeLabelExportTableOutputs(
        root=root_v,
    )

def volume_label_export_table_execute(params: VolumeLabelExportTable, runner: Runner | None = None) -> VolumeLabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM VOLUME AS TEXT

    Takes the label table from the volume label map, and writes it to a text format matching what is expected by -volume-label-import.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_label_export_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_LABEL_EXPORT_TABLE_METADATA)
    execution.params(params)
    args = volume_label_export_table_cargs(params, execution)
    out = volume_label_export_table_outputs(params, execution)
    execution.run(args)
    return out

def volume_label_export_table(
    label_in: InputPathType,
    map: str,
    table_out: str,
    hierarchy: str | None = None,
    runner: Runner | None = None,
) -> VolumeLabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM VOLUME AS TEXT

    Takes the label table from the volume label map, and writes it to a text format matching what is expected by -volume-label-import.

    Args:
        label_in: the input volume label file
        map: the number or name of the label map to use
        table_out: output - the output text file
        hierarchy: export the hierarchy as json
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_label_export_table_params(
        label_in=label_in,
        map=map,
        table_out=table_out,
        hierarchy=hierarchy,
    )
    return volume_label_export_table_execute(params, runner)

__all__ = [
    "VolumeLabelExportTable",
    "VolumeLabelExportTableOutputs",
    "VOLUME_LABEL_EXPORT_TABLE_METADATA",
    "volume_label_export_table_cargs",
    "volume_label_export_table_outputs",
    "volume_label_export_table_params",
    "volume_label_export_table_execute",
    "volume_label_export_table_validate",
    "volume_label_export_table",
]