# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_CUT_RESAMPLE_METADATA = Metadata(
    id="surface_cut_resample",
    name="RESAMPLE A CUT SURFACE",
    package="workbench",
)

SurfaceCutResample = typing.TypedDict(
    "SurfaceCutResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_cut_resample"]],
        "surface_in": InputPathType,
        "current_sphere": InputPathType,
        "new_sphere": InputPathType,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceCutResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the output surface file"""

def surface_cut_resample_params(
    surface_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    surface_out: str,
) -> SurfaceCutResample:
    """
    Build parameters.

    Args:
        surface_in: the surface file to resample
        current_sphere: a sphere surface with the mesh that the input surface is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        surface_out: the output surface file

    Returns:
        Parameter dictionary.
    """
    params: SurfaceCutResample = {
        "@type": "workbench/surface_cut_resample",
        "surface_in": surface_in,
        "current_sphere": current_sphere,
        "new_sphere": new_sphere,
        "surface_out": surface_out,
    }
    return params

def surface_cut_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceCutResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_in", None) is None:
        raise StyxValidationError("`surface_in` must not be None")
    if not isinstance(params["surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in` has the wrong type: Received `{type(params["surface_in"])}` expected `InputPathType`')
    if params.get("current_sphere", None) is None:
        raise StyxValidationError("`current_sphere` must not be None")
    if not isinstance(params["current_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`current_sphere` has the wrong type: Received `{type(params["current_sphere"])}` expected `InputPathType`')
    if params.get("new_sphere", None) is None:
        raise StyxValidationError("`new_sphere` must not be None")
    if not isinstance(params["new_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_sphere` has the wrong type: Received `{type(params["new_sphere"])}` expected `InputPathType`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_cut_resample_cargs(params: SurfaceCutResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-cut-resample")
    cargs.append(execution.input_file(params["surface_in"]))
    cargs.append(execution.input_file(params["current_sphere"]))
    cargs.append(execution.input_file(params["new_sphere"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_cut_resample_outputs(params: SurfaceCutResample, execution: Execution) -> SurfaceCutResampleOutputs:
    """Build the SurfaceCutResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceCutResampleOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_cut_resample_execute(params: SurfaceCutResample, runner: Runner | None = None) -> SurfaceCutResampleOutputs:
    """
    RESAMPLE A CUT SURFACE

    Resamples a surface file, given two spherical surfaces that are in register.  Barycentric resampling is used, because it is usually better for resampling surfaces, and because it is needed to figure out the new topology anyway.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_cut_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_CUT_RESAMPLE_METADATA)
    execution.params(params)
    args = surface_cut_resample_cargs(params, execution)
    out = surface_cut_resample_outputs(params, execution)
    execution.run(args)
    return out

def surface_cut_resample(
    surface_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceCutResampleOutputs:
    """
    RESAMPLE A CUT SURFACE

    Resamples a surface file, given two spherical surfaces that are in register.  Barycentric resampling is used, because it is usually better for resampling surfaces, and because it is needed to figure out the new topology anyway.

    Args:
        surface_in: the surface file to resample
        current_sphere: a sphere surface with the mesh that the input surface is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        surface_out: the output surface file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_cut_resample_params(
        surface_in=surface_in,
        current_sphere=current_sphere,
        new_sphere=new_sphere,
        surface_out=surface_out,
    )
    return surface_cut_resample_execute(params, runner)

__all__ = [
    "SurfaceCutResample",
    "SurfaceCutResampleOutputs",
    "SURFACE_CUT_RESAMPLE_METADATA",
    "surface_cut_resample_cargs",
    "surface_cut_resample_outputs",
    "surface_cut_resample_params",
    "surface_cut_resample_execute",
    "surface_cut_resample_validate",
    "surface_cut_resample",
]