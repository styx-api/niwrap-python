# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_RESAMPLE_METADATA = Metadata(
    id="volume_resample",
    name="TRANSFORM AND RESAMPLE A VOLUME FILE",
    package="workbench",
)

class VolumeResampleFlirt(typing.TypedDict):
    source_volume: str
    """the source volume used when generating the affine"""
    target_volume: str
    """the target volume used when generating the affine"""

class VolumeResampleAffine(typing.TypedDict):
    affine: str
    """the affine file to use"""
    flirt: typing.NotRequired[VolumeResampleFlirt]
    """MUST be used if affine is a flirt affine"""

class VolumeResampleAffineSeries(typing.TypedDict):
    affine_series: str
    """text file containing 12 or 16 numbers per line, each being a row-major flattened affine"""
    flirt: typing.NotRequired[VolumeResampleFlirt]
    """MUST be used if the affines are flirt affines"""

class VolumeResampleWarp(typing.TypedDict):
    warpfield: str
    """the warpfield file"""
    fnirt: typing.NotRequired[str]
    """MUST be used if using a fnirt warpfield"""

VolumeResample = typing.TypedDict(
    "VolumeResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_resample"]],
        "volume_in": InputPathType,
        "volume_space": str,
        "method": str,
        "volume_out": str,
        "background": typing.NotRequired[float],
        "affine": list[VolumeResampleAffine],
        "affine_series": list[VolumeResampleAffineSeries],
        "warp": list[VolumeResampleWarp],
    },
)

@dataclasses.dataclass
class VolumeResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_resample_params(
    volume_in: InputPathType,
    volume_space: str,
    method: str,
    volume_out: str,
    affine: list[VolumeResampleAffine],
    affine_series: list[VolumeResampleAffineSeries],
    warp: list[VolumeResampleWarp],
    background: float | None = None,
) -> VolumeResample:
    """
    Build parameters.

    Args:
        volume_in: volume to resample
        volume_space: a volume file in the volume space you want for the output
        method: the resampling method
        volume_out: the output volume
        affine: add an affine transform
        affine_series: add an independent affine per-frame
        warp: add a nonlinear warpfield transform
        background: use a specified value for locations outside the FoV of the\
            input image or warpfield(s)

    Returns:
        Parameter dictionary.
    """
    params: VolumeResample = {
        "@type": "workbench/volume_resample",
        "volume_in": volume_in,
        "volume_space": volume_space,
        "method": method,
        "volume_out": volume_out,
        "affine": affine,
        "affine_series": affine_series,
        "warp": warp,
    }
    if background is not None:
        params["background"] = background
    return params

def volume_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("volume_space", None) is None:
        raise StyxValidationError("`volume_space` must not be None")
    if not isinstance(params["volume_space"], str):
        raise StyxValidationError(f'`volume_space` has the wrong type: Received `{type(params["volume_space"])}` expected `str`')
    if params.get("method", None) is None:
        raise StyxValidationError("`method` must not be None")
    if not isinstance(params["method"], str):
        raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("background", None) is not None:
        if not isinstance(params["background"], (float, int)):
            raise StyxValidationError(f'`background` has the wrong type: Received `{type(params["background"])}` expected `float`')
    if params.get("affine", None) is None:
        raise StyxValidationError("`affine` must not be None")
    if not isinstance(params["affine"], list):
        raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `list[VolumeResampleAffine]`')
    for e in params["affine"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("affine", None) is None:
            raise StyxValidationError("`affine` must not be None")
        if not isinstance(e["affine"], str):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(e["affine"])}` expected `str`')
        if e.get("flirt", None) is not None:
            if not isinstance(e["flirt"], dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e["flirt"])}\'')
            if e["flirt"].get("source_volume", None) is None:
                raise StyxValidationError("`source_volume` must not be None")
            if not isinstance(e["flirt"]["source_volume"], str):
                raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(e["flirt"]["source_volume"])}` expected `str`')
            if e["flirt"].get("target_volume", None) is None:
                raise StyxValidationError("`target_volume` must not be None")
            if not isinstance(e["flirt"]["target_volume"], str):
                raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(e["flirt"]["target_volume"])}` expected `str`')
    if params.get("affine_series", None) is None:
        raise StyxValidationError("`affine_series` must not be None")
    if not isinstance(params["affine_series"], list):
        raise StyxValidationError(f'`affine_series` has the wrong type: Received `{type(params["affine_series"])}` expected `list[VolumeResampleAffineSeries]`')
    for e_2 in params["affine_series"]:
        if not isinstance(e_2, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
        if e_2.get("affine_series", None) is None:
            raise StyxValidationError("`affine_series` must not be None")
        if not isinstance(e_2["affine_series"], str):
            raise StyxValidationError(f'`affine_series` has the wrong type: Received `{type(e_2["affine_series"])}` expected `str`')
        if e_2.get("flirt", None) is not None:
            if not isinstance(e_2["flirt"], dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["flirt"])}\'')
            if e_2["flirt"].get("source_volume", None) is None:
                raise StyxValidationError("`source_volume` must not be None")
            if not isinstance(e_2["flirt"]["source_volume"], str):
                raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(e_2["flirt"]["source_volume"])}` expected `str`')
            if e_2["flirt"].get("target_volume", None) is None:
                raise StyxValidationError("`target_volume` must not be None")
            if not isinstance(e_2["flirt"]["target_volume"], str):
                raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(e_2["flirt"]["target_volume"])}` expected `str`')
    if params.get("warp", None) is None:
        raise StyxValidationError("`warp` must not be None")
    if not isinstance(params["warp"], list):
        raise StyxValidationError(f'`warp` has the wrong type: Received `{type(params["warp"])}` expected `list[VolumeResampleWarp]`')
    for e_3 in params["warp"]:
        if not isinstance(e_3, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
        if e_3.get("warpfield", None) is None:
            raise StyxValidationError("`warpfield` must not be None")
        if not isinstance(e_3["warpfield"], str):
            raise StyxValidationError(f'`warpfield` has the wrong type: Received `{type(e_3["warpfield"])}` expected `str`')
        if e_3.get("fnirt", None) is not None:
            if not isinstance(e_3["fnirt"], str):
                raise StyxValidationError(f'`fnirt` has the wrong type: Received `{type(e_3["fnirt"])}` expected `str`')

def volume_resample_cargs(params: VolumeResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-resample")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["volume_space"])
    cargs.append(params["method"])
    cargs.append(params["volume_out"])
    v_0 = params.get("background")
    if v_0 is not None:
        cargs.append("-background")
        cargs.append(str(v_0))
    for item0 in params["affine"]:
        cargs.append("-affine")
        cargs.append(item0["affine"])
        v_1 = item0.get("flirt")
        if v_1 is not None:
            cargs.append("-flirt")
            cargs.append(v_1["source_volume"])
            cargs.append(v_1["target_volume"])
    for item1 in params["affine_series"]:
        cargs.append("-affine-series")
        cargs.append(item1["affine_series"])
        v_2 = item1.get("flirt")
        if v_2 is not None:
            cargs.append("-flirt")
            cargs.append(v_2["source_volume"])
            cargs.append(v_2["target_volume"])
    for item2 in params["warp"]:
        cargs.append("-warp")
        cargs.append(item2["warpfield"])
        v_3 = item2.get("fnirt")
        if v_3 is not None:
            cargs.append("-fnirt")
            cargs.append(v_3)
    return cargs

def volume_resample_outputs(params: VolumeResample, execution: Execution) -> VolumeResampleOutputs:
    """Build the VolumeResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeResampleOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_resample_execute(params: VolumeResample, runner: Runner | None = None) -> VolumeResampleOutputs:
    """
    TRANSFORM AND RESAMPLE A VOLUME FILE

    Resample a volume file with an arbitrary list of transformations.  You may specify -affine, -warp, and -affine-series multiple times each, and they will be used in the order specified.  For instance, for rigid motion correction followed by nonlinear atlas registration, specify -affine-series first, then -warp.  The recommended methods are CUBIC (cubic spline) for most data, and ENCLOSING_VOXEL for label data.  The parameter <method> must be one of:

CUBIC
ENCLOSING_VOXEL
TRILINEAR

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_RESAMPLE_METADATA)
    execution.params(params)
    args = volume_resample_cargs(params, execution)
    out = volume_resample_outputs(params, execution)
    execution.run(args)
    return out

def volume_resample(
    volume_in: InputPathType,
    volume_space: str,
    method: str,
    volume_out: str,
    affine: list[VolumeResampleAffine],
    affine_series: list[VolumeResampleAffineSeries],
    warp: list[VolumeResampleWarp],
    background: float | None = None,
    runner: Runner | None = None,
) -> VolumeResampleOutputs:
    """
    TRANSFORM AND RESAMPLE A VOLUME FILE

    Resample a volume file with an arbitrary list of transformations.  You may specify -affine, -warp, and -affine-series multiple times each, and they will be used in the order specified.  For instance, for rigid motion correction followed by nonlinear atlas registration, specify -affine-series first, then -warp.  The recommended methods are CUBIC (cubic spline) for most data, and ENCLOSING_VOXEL for label data.  The parameter <method> must be one of:

CUBIC
ENCLOSING_VOXEL
TRILINEAR

    Args:
        volume_in: volume to resample
        volume_space: a volume file in the volume space you want for the output
        method: the resampling method
        volume_out: the output volume
        affine: add an affine transform
        affine_series: add an independent affine per-frame
        warp: add a nonlinear warpfield transform
        background: use a specified value for locations outside the FoV of the\
            input image or warpfield(s)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_resample_params(
        volume_in=volume_in,
        volume_space=volume_space,
        method=method,
        volume_out=volume_out,
        affine=affine,
        affine_series=affine_series,
        warp=warp,
        background=background,
    )
    return volume_resample_execute(params, runner)

__all__ = [
    "VolumeResample",
    "VolumeResampleOutputs",
    "VOLUME_RESAMPLE_METADATA",
    "volume_resample_cargs",
    "volume_resample_outputs",
    "volume_resample_params",
    "volume_resample_execute",
    "volume_resample_validate",
    "volume_resample",
]