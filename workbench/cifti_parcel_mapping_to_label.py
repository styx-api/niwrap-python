# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_PARCEL_MAPPING_TO_LABEL_METADATA = Metadata(
    id="cifti_parcel_mapping_to_label",
    name="CREATE DLABEL FROM PARCELLATED FILE",
    package="workbench",
)

CiftiParcelMappingToLabel = typing.TypedDict(
    "CiftiParcelMappingToLabel",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_parcel_mapping_to_label"]],
        "cifti_in": InputPathType,
        "direction": str,
        "template_cifti": InputPathType,
        "dlabel_out": str,
    },
)

@dataclasses.dataclass
class CiftiParcelMappingToLabelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    dlabel_out: OutputPathType
    """the output dense label file"""

def cifti_parcel_mapping_to_label_params(
    cifti_in: InputPathType,
    direction: str,
    template_cifti: InputPathType,
    dlabel_out: str,
) -> CiftiParcelMappingToLabel:
    """
    Build parameters.

    Args:
        cifti_in: the input parcellated file
        direction: which dimension to take the parcel map from, ROW or COLUMN
        template_cifti: a cifti file with the desired dense mapping along column
        dlabel_out: the output dense label file

    Returns:
        Parameter dictionary.
    """
    params: CiftiParcelMappingToLabel = {
        "@type": "workbench/cifti_parcel_mapping_to_label",
        "cifti_in": cifti_in,
        "direction": direction,
        "template_cifti": template_cifti,
        "dlabel_out": dlabel_out,
    }
    return params

def cifti_parcel_mapping_to_label_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiParcelMappingToLabel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("template_cifti", None) is None:
        raise StyxValidationError("`template_cifti` must not be None")
    if not isinstance(params["template_cifti"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_cifti` has the wrong type: Received `{type(params["template_cifti"])}` expected `InputPathType`')
    if params.get("dlabel_out", None) is None:
        raise StyxValidationError("`dlabel_out` must not be None")
    if not isinstance(params["dlabel_out"], str):
        raise StyxValidationError(f'`dlabel_out` has the wrong type: Received `{type(params["dlabel_out"])}` expected `str`')

def cifti_parcel_mapping_to_label_cargs(params: CiftiParcelMappingToLabel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-parcel-mapping-to-label")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["direction"])
    cargs.append(execution.input_file(params["template_cifti"]))
    cargs.append(params["dlabel_out"])
    return cargs

def cifti_parcel_mapping_to_label_outputs(params: CiftiParcelMappingToLabel, execution: Execution) -> CiftiParcelMappingToLabelOutputs:
    """Build the CiftiParcelMappingToLabelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    dlabel_out_v: OutputPathType = execution.output_file(params["dlabel_out"])
    return CiftiParcelMappingToLabelOutputs(
        root=root_v,
        dlabel_out=dlabel_out_v,
    )

def cifti_parcel_mapping_to_label_execute(params: CiftiParcelMappingToLabel, runner: Runner | None = None) -> CiftiParcelMappingToLabelOutputs:
    """
    CREATE DLABEL FROM PARCELLATED FILE

    This command will output a dlabel file, useful for doing the same parcellation to another dense file.

For ptseries, pscalar, plabel, pconn, and pdconn, using COLUMN for <direction> will work.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_parcel_mapping_to_label_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_PARCEL_MAPPING_TO_LABEL_METADATA)
    execution.params(params)
    args = cifti_parcel_mapping_to_label_cargs(params, execution)
    out = cifti_parcel_mapping_to_label_outputs(params, execution)
    execution.run(args)
    return out

def cifti_parcel_mapping_to_label(
    cifti_in: InputPathType,
    direction: str,
    template_cifti: InputPathType,
    dlabel_out: str,
    runner: Runner | None = None,
) -> CiftiParcelMappingToLabelOutputs:
    """
    CREATE DLABEL FROM PARCELLATED FILE

    This command will output a dlabel file, useful for doing the same parcellation to another dense file.

For ptseries, pscalar, plabel, pconn, and pdconn, using COLUMN for <direction> will work.

    Args:
        cifti_in: the input parcellated file
        direction: which dimension to take the parcel map from, ROW or COLUMN
        template_cifti: a cifti file with the desired dense mapping along column
        dlabel_out: the output dense label file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_parcel_mapping_to_label_params(
        cifti_in=cifti_in,
        direction=direction,
        template_cifti=template_cifti,
        dlabel_out=dlabel_out,
    )
    return cifti_parcel_mapping_to_label_execute(params, runner)

__all__ = [
    "CiftiParcelMappingToLabel",
    "CiftiParcelMappingToLabelOutputs",
    "CIFTI_PARCEL_MAPPING_TO_LABEL_METADATA",
    "cifti_parcel_mapping_to_label_cargs",
    "cifti_parcel_mapping_to_label_outputs",
    "cifti_parcel_mapping_to_label_params",
    "cifti_parcel_mapping_to_label_execute",
    "cifti_parcel_mapping_to_label_validate",
    "cifti_parcel_mapping_to_label",
]