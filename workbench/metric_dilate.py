# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_DILATE_METADATA = Metadata(
    id="metric_dilate",
    name="DILATE A METRIC FILE",
    package="workbench",
)

MetricDilate = typing.TypedDict(
    "MetricDilate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_dilate"]],
        "metric": InputPathType,
        "surface": InputPathType,
        "distance": float,
        "metric_out": str,
        "bad_vertex_roi": typing.NotRequired[InputPathType],
        "data_roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "nearest": typing.NotRequired[bool],
        "linear": typing.NotRequired[bool],
        "exponent": typing.NotRequired[float],
        "corrected_areas": typing.NotRequired[InputPathType],
        "legacy_cutoff": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MetricDilateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_dilate_params(
    metric: InputPathType,
    surface: InputPathType,
    distance: float,
    metric_out: str,
    bad_vertex_roi: InputPathType | None = None,
    data_roi: InputPathType | None = None,
    column: str | None = None,
    nearest: bool = False,
    linear: bool = False,
    exponent: float | None = None,
    corrected_areas: InputPathType | None = None,
    legacy_cutoff: bool = False,
) -> MetricDilate:
    """
    Build parameters.

    Args:
        metric: the metric to dilate
        surface: the surface to compute on
        distance: distance in mm to dilate
        metric_out: the output metric
        bad_vertex_roi: specify an roi of vertices to overwrite, rather than\
            vertices with value zero
        data_roi: specify an roi of where there is data
        column: select a single column to dilate
        nearest: use the nearest good value instead of a weighted average
        linear: fill in values with linear interpolation along strongest gradient
        exponent: use a different exponent in the weighting function
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        legacy_cutoff: use the v1.3.2 method of choosing how many vertices to use\
            when calulating the dilated value with weighted method

    Returns:
        Parameter dictionary.
    """
    params: MetricDilate = {
        "@type": "workbench/metric_dilate",
        "metric": metric,
        "surface": surface,
        "distance": distance,
        "metric_out": metric_out,
        "nearest": nearest,
        "linear": linear,
        "legacy_cutoff": legacy_cutoff,
    }
    if bad_vertex_roi is not None:
        params["bad_vertex_roi"] = bad_vertex_roi
    if data_roi is not None:
        params["data_roi"] = data_roi
    if column is not None:
        params["column"] = column
    if exponent is not None:
        params["exponent"] = exponent
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_dilate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricDilate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("bad_vertex_roi", None) is not None:
        if not isinstance(params["bad_vertex_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bad_vertex_roi` has the wrong type: Received `{type(params["bad_vertex_roi"])}` expected `InputPathType`')
    if params.get("data_roi", None) is not None:
        if not isinstance(params["data_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`data_roi` has the wrong type: Received `{type(params["data_roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("nearest", None) is not None:
        if not isinstance(params["nearest"], bool):
            raise StyxValidationError(f'`nearest` has the wrong type: Received `{type(params["nearest"])}` expected `bool`')
    if params.get("linear", None) is not None:
        if not isinstance(params["linear"], bool):
            raise StyxValidationError(f'`linear` has the wrong type: Received `{type(params["linear"])}` expected `bool`')
    if params.get("exponent", None) is not None:
        if not isinstance(params["exponent"], (float, int)):
            raise StyxValidationError(f'`exponent` has the wrong type: Received `{type(params["exponent"])}` expected `float`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("legacy_cutoff", None) is not None:
        if not isinstance(params["legacy_cutoff"], bool):
            raise StyxValidationError(f'`legacy_cutoff` has the wrong type: Received `{type(params["legacy_cutoff"])}` expected `bool`')

def metric_dilate_cargs(params: MetricDilate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-dilate")
    cargs.append(execution.input_file(params["metric"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("bad_vertex_roi")
    if v_0 is not None:
        cargs.append("-bad-vertex-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("data_roi")
    if v_1 is not None:
        cargs.append("-data-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("column")
    if v_2 is not None:
        cargs.append("-column")
        cargs.append(v_2)
    if params.get("nearest"):
        cargs.append("-nearest")
    if params.get("linear"):
        cargs.append("-linear")
    v_3 = params.get("exponent")
    if v_3 is not None:
        cargs.append("-exponent")
        cargs.append(str(v_3))
    v_4 = params.get("corrected_areas")
    if v_4 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_4))
    if params.get("legacy_cutoff"):
        cargs.append("-legacy-cutoff")
    return cargs

def metric_dilate_outputs(params: MetricDilate, execution: Execution) -> MetricDilateOutputs:
    """Build the MetricDilateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricDilateOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_dilate_execute(params: MetricDilate, runner: Runner | None = None) -> MetricDilateOutputs:
    """
    DILATE A METRIC FILE

    For all metric vertices that are designated as bad, if they neighbor a non-bad vertex with data or are within the specified distance of such a vertex, replace the value with a distance-based weighted average of nearby non-bad vertices that have data, otherwise set the value to zero.  No matter how small <distance> is, dilation will always use at least the immediate neighbor vertices.  If -nearest is specified, it will use the value from the closest non-bad vertex with data within range instead of a weighted average.

If -bad-vertex-roi is specified, all vertices with a positive ROI value are bad.  If it is not specified, only vertices that have data, with a value of zero, are bad.  If -data-roi is not specified, all vertices are assumed to have data.

Note that the -corrected-areas option uses an approximate correction for the change in distances along a group average surface.

To get the behavior of version 1.3.2 or earlier, use '-legacy-cutoff -exponent 2'.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_dilate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_DILATE_METADATA)
    execution.params(params)
    args = metric_dilate_cargs(params, execution)
    out = metric_dilate_outputs(params, execution)
    execution.run(args)
    return out

def metric_dilate(
    metric: InputPathType,
    surface: InputPathType,
    distance: float,
    metric_out: str,
    bad_vertex_roi: InputPathType | None = None,
    data_roi: InputPathType | None = None,
    column: str | None = None,
    nearest: bool = False,
    linear: bool = False,
    exponent: float | None = None,
    corrected_areas: InputPathType | None = None,
    legacy_cutoff: bool = False,
    runner: Runner | None = None,
) -> MetricDilateOutputs:
    """
    DILATE A METRIC FILE

    For all metric vertices that are designated as bad, if they neighbor a non-bad vertex with data or are within the specified distance of such a vertex, replace the value with a distance-based weighted average of nearby non-bad vertices that have data, otherwise set the value to zero.  No matter how small <distance> is, dilation will always use at least the immediate neighbor vertices.  If -nearest is specified, it will use the value from the closest non-bad vertex with data within range instead of a weighted average.

If -bad-vertex-roi is specified, all vertices with a positive ROI value are bad.  If it is not specified, only vertices that have data, with a value of zero, are bad.  If -data-roi is not specified, all vertices are assumed to have data.

Note that the -corrected-areas option uses an approximate correction for the change in distances along a group average surface.

To get the behavior of version 1.3.2 or earlier, use '-legacy-cutoff -exponent 2'.

    Args:
        metric: the metric to dilate
        surface: the surface to compute on
        distance: distance in mm to dilate
        metric_out: the output metric
        bad_vertex_roi: specify an roi of vertices to overwrite, rather than\
            vertices with value zero
        data_roi: specify an roi of where there is data
        column: select a single column to dilate
        nearest: use the nearest good value instead of a weighted average
        linear: fill in values with linear interpolation along strongest gradient
        exponent: use a different exponent in the weighting function
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        legacy_cutoff: use the v1.3.2 method of choosing how many vertices to use\
            when calulating the dilated value with weighted method
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_dilate_params(
        metric=metric,
        surface=surface,
        distance=distance,
        metric_out=metric_out,
        bad_vertex_roi=bad_vertex_roi,
        data_roi=data_roi,
        column=column,
        nearest=nearest,
        linear=linear,
        exponent=exponent,
        corrected_areas=corrected_areas,
        legacy_cutoff=legacy_cutoff,
    )
    return metric_dilate_execute(params, runner)

__all__ = [
    "MetricDilate",
    "MetricDilateOutputs",
    "METRIC_DILATE_METADATA",
    "metric_dilate_cargs",
    "metric_dilate_outputs",
    "metric_dilate_params",
    "metric_dilate_execute",
    "metric_dilate_validate",
    "metric_dilate",
]