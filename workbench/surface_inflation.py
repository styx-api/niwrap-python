# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_INFLATION_METADATA = Metadata(
    id="surface_inflation",
    name="SURFACE INFLATION",
    package="workbench",
)

SurfaceInflation = typing.TypedDict(
    "SurfaceInflation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_inflation"]],
        "anatomical_surface_in": InputPathType,
        "surface_in": InputPathType,
        "number_of_smoothing_cycles": int,
        "smoothing_strength": float,
        "smoothing_iterations": int,
        "inflation_factor": float,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceInflationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """output surface file"""

def surface_inflation_params(
    anatomical_surface_in: InputPathType,
    surface_in: InputPathType,
    number_of_smoothing_cycles: int,
    smoothing_strength: float,
    smoothing_iterations: int,
    inflation_factor: float,
    surface_out: str,
) -> SurfaceInflation:
    """
    Build parameters.

    Args:
        anatomical_surface_in: the anatomical surface
        surface_in: the surface file to inflate
        number_of_smoothing_cycles: number of smoothing cycles
        smoothing_strength: smoothing strength (ranges [0.0 - 1.0])
        smoothing_iterations: smoothing iterations
        inflation_factor: inflation factor
        surface_out: output surface file

    Returns:
        Parameter dictionary.
    """
    params: SurfaceInflation = {
        "@type": "workbench/surface_inflation",
        "anatomical_surface_in": anatomical_surface_in,
        "surface_in": surface_in,
        "number_of_smoothing_cycles": number_of_smoothing_cycles,
        "smoothing_strength": smoothing_strength,
        "smoothing_iterations": smoothing_iterations,
        "inflation_factor": inflation_factor,
        "surface_out": surface_out,
    }
    return params

def surface_inflation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceInflation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("anatomical_surface_in", None) is None:
        raise StyxValidationError("`anatomical_surface_in` must not be None")
    if not isinstance(params["anatomical_surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`anatomical_surface_in` has the wrong type: Received `{type(params["anatomical_surface_in"])}` expected `InputPathType`')
    if params.get("surface_in", None) is None:
        raise StyxValidationError("`surface_in` must not be None")
    if not isinstance(params["surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in` has the wrong type: Received `{type(params["surface_in"])}` expected `InputPathType`')
    if params.get("number_of_smoothing_cycles", None) is None:
        raise StyxValidationError("`number_of_smoothing_cycles` must not be None")
    if not isinstance(params["number_of_smoothing_cycles"], int):
        raise StyxValidationError(f'`number_of_smoothing_cycles` has the wrong type: Received `{type(params["number_of_smoothing_cycles"])}` expected `int`')
    if params.get("smoothing_strength", None) is None:
        raise StyxValidationError("`smoothing_strength` must not be None")
    if not isinstance(params["smoothing_strength"], (float, int)):
        raise StyxValidationError(f'`smoothing_strength` has the wrong type: Received `{type(params["smoothing_strength"])}` expected `float`')
    if params.get("smoothing_iterations", None) is None:
        raise StyxValidationError("`smoothing_iterations` must not be None")
    if not isinstance(params["smoothing_iterations"], int):
        raise StyxValidationError(f'`smoothing_iterations` has the wrong type: Received `{type(params["smoothing_iterations"])}` expected `int`')
    if params.get("inflation_factor", None) is None:
        raise StyxValidationError("`inflation_factor` must not be None")
    if not isinstance(params["inflation_factor"], (float, int)):
        raise StyxValidationError(f'`inflation_factor` has the wrong type: Received `{type(params["inflation_factor"])}` expected `float`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_inflation_cargs(params: SurfaceInflation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-inflation")
    cargs.append(execution.input_file(params["anatomical_surface_in"]))
    cargs.append(execution.input_file(params["surface_in"]))
    cargs.append(str(params["number_of_smoothing_cycles"]))
    cargs.append(str(params["smoothing_strength"]))
    cargs.append(str(params["smoothing_iterations"]))
    cargs.append(str(params["inflation_factor"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_inflation_outputs(params: SurfaceInflation, execution: Execution) -> SurfaceInflationOutputs:
    """Build the SurfaceInflationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceInflationOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_inflation_execute(params: SurfaceInflation, runner: Runner | None = None) -> SurfaceInflationOutputs:
    """
    SURFACE INFLATION

    Inflate a surface by performing cycles that consist of smoothing  followed by inflation (to correct shrinkage caused by smoothing).

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_inflation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_INFLATION_METADATA)
    execution.params(params)
    args = surface_inflation_cargs(params, execution)
    out = surface_inflation_outputs(params, execution)
    execution.run(args)
    return out

def surface_inflation(
    anatomical_surface_in: InputPathType,
    surface_in: InputPathType,
    number_of_smoothing_cycles: int,
    smoothing_strength: float,
    smoothing_iterations: int,
    inflation_factor: float,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceInflationOutputs:
    """
    SURFACE INFLATION

    Inflate a surface by performing cycles that consist of smoothing  followed by inflation (to correct shrinkage caused by smoothing).

    Args:
        anatomical_surface_in: the anatomical surface
        surface_in: the surface file to inflate
        number_of_smoothing_cycles: number of smoothing cycles
        smoothing_strength: smoothing strength (ranges [0.0 - 1.0])
        smoothing_iterations: smoothing iterations
        inflation_factor: inflation factor
        surface_out: output surface file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_inflation_params(
        anatomical_surface_in=anatomical_surface_in,
        surface_in=surface_in,
        number_of_smoothing_cycles=number_of_smoothing_cycles,
        smoothing_strength=smoothing_strength,
        smoothing_iterations=smoothing_iterations,
        inflation_factor=inflation_factor,
        surface_out=surface_out,
    )
    return surface_inflation_execute(params, runner)

__all__ = [
    "SurfaceInflation",
    "SurfaceInflationOutputs",
    "SURFACE_INFLATION_METADATA",
    "surface_inflation_cargs",
    "surface_inflation_outputs",
    "surface_inflation_params",
    "surface_inflation_execute",
    "surface_inflation_validate",
    "surface_inflation",
]