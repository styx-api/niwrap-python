# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_AVERAGE_METADATA = Metadata(
    id="surface_average",
    name="AVERAGE SURFACE FILES TOGETHER",
    package="workbench",
)

class SurfaceAverageStddev(typing.TypedDict):
    stddev_metric_out: str
    """the output metric for 3D sample standard deviation"""

class SurfaceAverageUncertainty(typing.TypedDict):
    uncert_metric_out: str
    """the output metric for uncertainty"""

class SurfaceAverageSurf(typing.TypedDict):
    surface: InputPathType
    """a surface file to average"""
    weight: typing.NotRequired[float]
    """specify a weighted average"""

SurfaceAverage = typing.TypedDict(
    "SurfaceAverage",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_average"]],
        "surface_out": str,
        "stddev": typing.NotRequired[SurfaceAverageStddev],
        "uncertainty": typing.NotRequired[SurfaceAverageUncertainty],
        "surf": list[SurfaceAverageSurf],
    },
)

@dataclasses.dataclass
class SurfaceAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the output averaged surface"""
    stddev_metric_out: OutputPathType | None
    """the output metric for 3D sample standard deviation"""
    uncert_metric_out: OutputPathType | None
    """the output metric for uncertainty"""

def surface_average_params(
    surface_out: str,
    surf: list[SurfaceAverageSurf],
    stddev: SurfaceAverageStddev | None = None,
    uncertainty: SurfaceAverageUncertainty | None = None,
) -> SurfaceAverage:
    """
    Build parameters.

    Args:
        surface_out: the output averaged surface
        surf: specify a surface to include in the average
        stddev: compute 3D sample standard deviation
        uncertainty: compute caret5 'uncertainty'

    Returns:
        Parameter dictionary.
    """
    params: SurfaceAverage = {
        "@type": "workbench/surface_average",
        "surface_out": surface_out,
        "surf": surf,
    }
    if stddev is not None:
        params["stddev"] = stddev
    if uncertainty is not None:
        params["uncertainty"] = uncertainty
    return params

def surface_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')
    if params.get("stddev", None) is not None:
        if not isinstance(params["stddev"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["stddev"])}\'')
        if params["stddev"].get("stddev_metric_out", None) is None:
            raise StyxValidationError("`stddev_metric_out` must not be None")
        if not isinstance(params["stddev"]["stddev_metric_out"], str):
            raise StyxValidationError(f'`stddev_metric_out` has the wrong type: Received `{type(params["stddev"]["stddev_metric_out"])}` expected `str`')
    if params.get("uncertainty", None) is not None:
        if not isinstance(params["uncertainty"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["uncertainty"])}\'')
        if params["uncertainty"].get("uncert_metric_out", None) is None:
            raise StyxValidationError("`uncert_metric_out` must not be None")
        if not isinstance(params["uncertainty"]["uncert_metric_out"], str):
            raise StyxValidationError(f'`uncert_metric_out` has the wrong type: Received `{type(params["uncertainty"]["uncert_metric_out"])}` expected `str`')
    if params.get("surf", None) is None:
        raise StyxValidationError("`surf` must not be None")
    if not isinstance(params["surf"], list):
        raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params["surf"])}` expected `list[SurfaceAverageSurf]`')
    for e in params["surf"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("surface", None) is None:
            raise StyxValidationError("`surface` must not be None")
        if not isinstance(e["surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(e["surface"])}` expected `InputPathType`')
        if e.get("weight", None) is not None:
            if not isinstance(e["weight"], (float, int)):
                raise StyxValidationError(f'`weight` has the wrong type: Received `{type(e["weight"])}` expected `float`')

def surface_average_cargs(params: SurfaceAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-average")
    cargs.append(params["surface_out"])
    v_0 = params.get("stddev")
    if v_0 is not None:
        cargs.append("-stddev")
        cargs.append(v_0["stddev_metric_out"])
    v_1 = params.get("uncertainty")
    if v_1 is not None:
        cargs.append("-uncertainty")
        cargs.append(v_1["uncert_metric_out"])
    for item0 in params["surf"]:
        cargs.append("-surf")
        cargs.append(execution.input_file(item0["surface"]))
        v_2 = item0.get("weight")
        if v_2 is not None:
            cargs.append("-weight")
            cargs.append(str(v_2))
    return cargs

def surface_average_outputs(params: SurfaceAverage, execution: Execution) -> SurfaceAverageOutputs:
    """Build the SurfaceAverageOutputs object for this tool."""
    stddev_metric_out_v: OutputPathType | None = None
    uncert_metric_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    __v0 = params.get("stddev")
    if __v0 is not None:
        stddev_metric_out_v = execution.output_file(__v0["stddev_metric_out"])
    __v1 = params.get("uncertainty")
    if __v1 is not None:
        uncert_metric_out_v = execution.output_file(__v1["uncert_metric_out"])
    return SurfaceAverageOutputs(
        root=root_v,
        surface_out=surface_out_v,
        stddev_metric_out=stddev_metric_out_v,
        uncert_metric_out=uncert_metric_out_v,
    )

def surface_average_execute(params: SurfaceAverage, runner: Runner | None = None) -> SurfaceAverageOutputs:
    """
    AVERAGE SURFACE FILES TOGETHER

    The 3D sample standard deviation is computed as 'sqrt(sum(squaredlength(xyz - mean(xyz)))/(n - 1))'.

Uncertainty is a legacy measure used in caret5, and is computed as 'sum(length(xyz - mean(xyz)))/n'.

When weights are used, the 3D sample standard deviation treats them as reliability weights.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_AVERAGE_METADATA)
    execution.params(params)
    args = surface_average_cargs(params, execution)
    out = surface_average_outputs(params, execution)
    execution.run(args)
    return out

def surface_average(
    surface_out: str,
    surf: list[SurfaceAverageSurf],
    stddev: SurfaceAverageStddev | None = None,
    uncertainty: SurfaceAverageUncertainty | None = None,
    runner: Runner | None = None,
) -> SurfaceAverageOutputs:
    """
    AVERAGE SURFACE FILES TOGETHER

    The 3D sample standard deviation is computed as 'sqrt(sum(squaredlength(xyz - mean(xyz)))/(n - 1))'.

Uncertainty is a legacy measure used in caret5, and is computed as 'sum(length(xyz - mean(xyz)))/n'.

When weights are used, the 3D sample standard deviation treats them as reliability weights.

    Args:
        surface_out: the output averaged surface
        surf: specify a surface to include in the average
        stddev: compute 3D sample standard deviation
        uncertainty: compute caret5 'uncertainty'
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_average_params(
        surface_out=surface_out,
        surf=surf,
        stddev=stddev,
        uncertainty=uncertainty,
    )
    return surface_average_execute(params, runner)

__all__ = [
    "SurfaceAverage",
    "SurfaceAverageOutputs",
    "SURFACE_AVERAGE_METADATA",
    "surface_average_cargs",
    "surface_average_outputs",
    "surface_average_params",
    "surface_average_execute",
    "surface_average_validate",
    "surface_average",
]