# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIGNED_DISTANCE_TO_SURFACE_METADATA = Metadata(
    id="signed_distance_to_surface",
    name="COMPUTE SIGNED DISTANCE FROM ONE SURFACE TO ANOTHER",
    package="workbench",
)

SignedDistanceToSurface = typing.TypedDict(
    "SignedDistanceToSurface",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/signed_distance_to_surface"]],
        "surface_comp": InputPathType,
        "surface_ref": InputPathType,
        "metric": str,
        "winding": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SignedDistanceToSurfaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric: OutputPathType
    """the output metric"""

def signed_distance_to_surface_params(
    surface_comp: InputPathType,
    surface_ref: InputPathType,
    metric: str,
    winding: str | None = None,
) -> SignedDistanceToSurface:
    """
    Build parameters.

    Args:
        surface_comp: the comparison surface to measure the signed distance on
        surface_ref: the reference surface that defines the signed distance\
            function
        metric: the output metric
        winding: winding method for point inside surface test

    Returns:
        Parameter dictionary.
    """
    params: SignedDistanceToSurface = {
        "@type": "workbench/signed_distance_to_surface",
        "surface_comp": surface_comp,
        "surface_ref": surface_ref,
        "metric": metric,
    }
    if winding is not None:
        params["winding"] = winding
    return params

def signed_distance_to_surface_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SignedDistanceToSurface."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_comp", None) is None:
        raise StyxValidationError("`surface_comp` must not be None")
    if not isinstance(params["surface_comp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_comp` has the wrong type: Received `{type(params["surface_comp"])}` expected `InputPathType`')
    if params.get("surface_ref", None) is None:
        raise StyxValidationError("`surface_ref` must not be None")
    if not isinstance(params["surface_ref"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_ref` has the wrong type: Received `{type(params["surface_ref"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], str):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')
    if params.get("winding", None) is not None:
        if not isinstance(params["winding"], str):
            raise StyxValidationError(f'`winding` has the wrong type: Received `{type(params["winding"])}` expected `str`')

def signed_distance_to_surface_cargs(params: SignedDistanceToSurface, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-signed-distance-to-surface")
    cargs.append(execution.input_file(params["surface_comp"]))
    cargs.append(execution.input_file(params["surface_ref"]))
    cargs.append(params["metric"])
    v_0 = params.get("winding")
    if v_0 is not None:
        cargs.append("-winding")
        cargs.append(v_0)
    return cargs

def signed_distance_to_surface_outputs(params: SignedDistanceToSurface, execution: Execution) -> SignedDistanceToSurfaceOutputs:
    """Build the SignedDistanceToSurfaceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_v: OutputPathType = execution.output_file(params["metric"])
    return SignedDistanceToSurfaceOutputs(
        root=root_v,
        metric=metric_v,
    )

def signed_distance_to_surface_execute(params: SignedDistanceToSurface, runner: Runner | None = None) -> SignedDistanceToSurfaceOutputs:
    """
    COMPUTE SIGNED DISTANCE FROM ONE SURFACE TO ANOTHER

    Compute the signed distance function of the reference surface at every vertex on the comparison surface.  NOTE: this relation is NOT symmetric, the line from a vertex to the closest point on the 'ref' surface (the one that defines the signed distance function) will only align with the normal of the 'ref' surface.  Valid specifiers for winding methods are as follows:

EVEN_ODD (default)
NEGATIVE
NONZERO
NORMALS

The NORMALS method uses the normals of triangles and edges, or the closest triangle hit by a ray from the point.  This method may be slightly faster, but is only reliable for a closed surface that does not cross through itself.  All other methods count entry (positive) and exit (negative) crossings of a vertical ray from the point, then counts as inside if the total is odd, negative, or nonzero, respectively.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    signed_distance_to_surface_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIGNED_DISTANCE_TO_SURFACE_METADATA)
    execution.params(params)
    args = signed_distance_to_surface_cargs(params, execution)
    out = signed_distance_to_surface_outputs(params, execution)
    execution.run(args)
    return out

def signed_distance_to_surface(
    surface_comp: InputPathType,
    surface_ref: InputPathType,
    metric: str,
    winding: str | None = None,
    runner: Runner | None = None,
) -> SignedDistanceToSurfaceOutputs:
    """
    COMPUTE SIGNED DISTANCE FROM ONE SURFACE TO ANOTHER

    Compute the signed distance function of the reference surface at every vertex on the comparison surface.  NOTE: this relation is NOT symmetric, the line from a vertex to the closest point on the 'ref' surface (the one that defines the signed distance function) will only align with the normal of the 'ref' surface.  Valid specifiers for winding methods are as follows:

EVEN_ODD (default)
NEGATIVE
NONZERO
NORMALS

The NORMALS method uses the normals of triangles and edges, or the closest triangle hit by a ray from the point.  This method may be slightly faster, but is only reliable for a closed surface that does not cross through itself.  All other methods count entry (positive) and exit (negative) crossings of a vertical ray from the point, then counts as inside if the total is odd, negative, or nonzero, respectively.

    Args:
        surface_comp: the comparison surface to measure the signed distance on
        surface_ref: the reference surface that defines the signed distance\
            function
        metric: the output metric
        winding: winding method for point inside surface test
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = signed_distance_to_surface_params(
        surface_comp=surface_comp,
        surface_ref=surface_ref,
        metric=metric,
        winding=winding,
    )
    return signed_distance_to_surface_execute(params, runner)

__all__ = [
    "SignedDistanceToSurface",
    "SignedDistanceToSurfaceOutputs",
    "SIGNED_DISTANCE_TO_SURFACE_METADATA",
    "signed_distance_to_surface_cargs",
    "signed_distance_to_surface_outputs",
    "signed_distance_to_surface_params",
    "signed_distance_to_surface_execute",
    "signed_distance_to_surface_validate",
    "signed_distance_to_surface",
]