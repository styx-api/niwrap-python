# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_TO_VOLUME_MAPPING_METADATA = Metadata(
    id="label_to_volume_mapping",
    name="MAP LABEL FILE TO VOLUME",
    package="workbench",
)

class LabelToVolumeMappingRibbonConstrained(typing.TypedDict):
    inner_surf: InputPathType
    """the inner surface of the ribbon"""
    outer_surf: InputPathType
    """the outer surface of the ribbon"""
    voxel_subdiv: typing.NotRequired[int]
    """voxel divisions while estimating voxel weights"""
    greedy: typing.NotRequired[bool]
    """also put labels in voxels with less than 50% partial volume (legacy behavior)"""
    thick_columns: typing.NotRequired[bool]
    """use overlapping columns (legacy method)"""

LabelToVolumeMapping = typing.TypedDict(
    "LabelToVolumeMapping",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_to_volume_mapping"]],
        "label": InputPathType,
        "surface": InputPathType,
        "volume_space": InputPathType,
        "volume_out": str,
        "nearest_vertex": typing.NotRequired[float],
        "ribbon_constrained": typing.NotRequired[LabelToVolumeMappingRibbonConstrained],
    },
)

@dataclasses.dataclass
class LabelToVolumeMappingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume file"""

def label_to_volume_mapping_params(
    label: InputPathType,
    surface: InputPathType,
    volume_space: InputPathType,
    volume_out: str,
    nearest_vertex: float | None = None,
    ribbon_constrained: LabelToVolumeMappingRibbonConstrained | None = None,
) -> LabelToVolumeMapping:
    """
    Build parameters.

    Args:
        label: the input label file
        surface: the surface to use coordinates from
        volume_space: a volume file in the desired output volume space
        volume_out: the output volume file
        nearest_vertex: use the label from the vertex closest to the voxel center
        ribbon_constrained: use ribbon constrained mapping algorithm

    Returns:
        Parameter dictionary.
    """
    params: LabelToVolumeMapping = {
        "@type": "workbench/label_to_volume_mapping",
        "label": label,
        "surface": surface,
        "volume_space": volume_space,
        "volume_out": volume_out,
    }
    if nearest_vertex is not None:
        params["nearest_vertex"] = nearest_vertex
    if ribbon_constrained is not None:
        params["ribbon_constrained"] = ribbon_constrained
    return params

def label_to_volume_mapping_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelToVolumeMapping."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("volume_space", None) is None:
        raise StyxValidationError("`volume_space` must not be None")
    if not isinstance(params["volume_space"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_space` has the wrong type: Received `{type(params["volume_space"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("nearest_vertex", None) is not None:
        if not isinstance(params["nearest_vertex"], (float, int)):
            raise StyxValidationError(f'`nearest_vertex` has the wrong type: Received `{type(params["nearest_vertex"])}` expected `float`')
    if params.get("ribbon_constrained", None) is not None:
        if not isinstance(params["ribbon_constrained"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["ribbon_constrained"])}\'')
        if params["ribbon_constrained"].get("inner_surf", None) is None:
            raise StyxValidationError("`inner_surf` must not be None")
        if not isinstance(params["ribbon_constrained"]["inner_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`inner_surf` has the wrong type: Received `{type(params["ribbon_constrained"]["inner_surf"])}` expected `InputPathType`')
        if params["ribbon_constrained"].get("outer_surf", None) is None:
            raise StyxValidationError("`outer_surf` must not be None")
        if not isinstance(params["ribbon_constrained"]["outer_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`outer_surf` has the wrong type: Received `{type(params["ribbon_constrained"]["outer_surf"])}` expected `InputPathType`')
        if params["ribbon_constrained"].get("voxel_subdiv", None) is not None:
            if not isinstance(params["ribbon_constrained"]["voxel_subdiv"], int):
                raise StyxValidationError(f'`voxel_subdiv` has the wrong type: Received `{type(params["ribbon_constrained"]["voxel_subdiv"])}` expected `int`')
        if params["ribbon_constrained"].get("greedy", None) is not None:
            if not isinstance(params["ribbon_constrained"]["greedy"], bool):
                raise StyxValidationError(f'`greedy` has the wrong type: Received `{type(params["ribbon_constrained"]["greedy"])}` expected `bool`')
        if params["ribbon_constrained"].get("thick_columns", None) is not None:
            if not isinstance(params["ribbon_constrained"]["thick_columns"], bool):
                raise StyxValidationError(f'`thick_columns` has the wrong type: Received `{type(params["ribbon_constrained"]["thick_columns"])}` expected `bool`')

def label_to_volume_mapping_cargs(params: LabelToVolumeMapping, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-to-volume-mapping")
    cargs.append(execution.input_file(params["label"]))
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["volume_space"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("nearest_vertex")
    if v_0 is not None:
        cargs.append("-nearest-vertex")
        cargs.append(str(v_0))
    v_1 = params.get("ribbon_constrained")
    if v_1 is not None:
        cargs.append("-ribbon-constrained")
        cargs.append(execution.input_file(v_1["inner_surf"]))
        cargs.append(execution.input_file(v_1["outer_surf"]))
        v_2 = v_1.get("voxel_subdiv")
        if v_2 is not None:
            cargs.append("-voxel-subdiv")
            cargs.append(str(v_2))
        if v_1.get("greedy"):
            cargs.append("-greedy")
        if v_1.get("thick_columns"):
            cargs.append("-thick-columns")
    return cargs

def label_to_volume_mapping_outputs(params: LabelToVolumeMapping, execution: Execution) -> LabelToVolumeMappingOutputs:
    """Build the LabelToVolumeMappingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return LabelToVolumeMappingOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def label_to_volume_mapping_execute(params: LabelToVolumeMapping, runner: Runner | None = None) -> LabelToVolumeMappingOutputs:
    """
    MAP LABEL FILE TO VOLUME

    Maps labels from a gifti label file into a volume file.  You must specify exactly one mapping method option.  The -nearest-vertex method uses the label from the vertex closest to the voxel center.  The -ribbon-constrained method uses the same method as in -volume-to-surface-mapping, then uses the weights in reverse, with popularity logic to decide on a label to use.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_to_volume_mapping_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_TO_VOLUME_MAPPING_METADATA)
    execution.params(params)
    args = label_to_volume_mapping_cargs(params, execution)
    out = label_to_volume_mapping_outputs(params, execution)
    execution.run(args)
    return out

def label_to_volume_mapping(
    label: InputPathType,
    surface: InputPathType,
    volume_space: InputPathType,
    volume_out: str,
    nearest_vertex: float | None = None,
    ribbon_constrained: LabelToVolumeMappingRibbonConstrained | None = None,
    runner: Runner | None = None,
) -> LabelToVolumeMappingOutputs:
    """
    MAP LABEL FILE TO VOLUME

    Maps labels from a gifti label file into a volume file.  You must specify exactly one mapping method option.  The -nearest-vertex method uses the label from the vertex closest to the voxel center.  The -ribbon-constrained method uses the same method as in -volume-to-surface-mapping, then uses the weights in reverse, with popularity logic to decide on a label to use.

    Args:
        label: the input label file
        surface: the surface to use coordinates from
        volume_space: a volume file in the desired output volume space
        volume_out: the output volume file
        nearest_vertex: use the label from the vertex closest to the voxel center
        ribbon_constrained: use ribbon constrained mapping algorithm
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_to_volume_mapping_params(
        label=label,
        surface=surface,
        volume_space=volume_space,
        volume_out=volume_out,
        nearest_vertex=nearest_vertex,
        ribbon_constrained=ribbon_constrained,
    )
    return label_to_volume_mapping_execute(params, runner)

__all__ = [
    "LabelToVolumeMapping",
    "LabelToVolumeMappingOutputs",
    "LABEL_TO_VOLUME_MAPPING_METADATA",
    "label_to_volume_mapping_cargs",
    "label_to_volume_mapping_outputs",
    "label_to_volume_mapping_params",
    "label_to_volume_mapping_execute",
    "label_to_volume_mapping_validate",
    "label_to_volume_mapping",
]