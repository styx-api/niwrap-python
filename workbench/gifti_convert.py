# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GIFTI_CONVERT_METADATA = Metadata(
    id="gifti_convert",
    name="CONVERT A GIFTI FILE TO A DIFFERENT ENCODING",
    package="workbench",
)

GiftiConvert = typing.TypedDict(
    "GiftiConvert",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/gifti_convert"]],
        "gifti_encoding": str,
        "input_gifti_file": str,
        "output_gifti_file": str,
    },
)

@dataclasses.dataclass
class GiftiConvertOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def gifti_convert_params(
    gifti_encoding: str,
    input_gifti_file: str,
    output_gifti_file: str,
) -> GiftiConvert:
    """
    Build parameters.

    Args:
        gifti_encoding: what the output encoding should be
        input_gifti_file: the input gifti file
        output_gifti_file: output - the output gifti file

    Returns:
        Parameter dictionary.
    """
    params: GiftiConvert = {
        "@type": "workbench/gifti_convert",
        "gifti_encoding": gifti_encoding,
        "input_gifti_file": input_gifti_file,
        "output_gifti_file": output_gifti_file,
    }
    return params

def gifti_convert_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GiftiConvert."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gifti_encoding", None) is None:
        raise StyxValidationError("`gifti_encoding` must not be None")
    if not isinstance(params["gifti_encoding"], str):
        raise StyxValidationError(f'`gifti_encoding` has the wrong type: Received `{type(params["gifti_encoding"])}` expected `str`')
    if params.get("input_gifti_file", None) is None:
        raise StyxValidationError("`input_gifti_file` must not be None")
    if not isinstance(params["input_gifti_file"], str):
        raise StyxValidationError(f'`input_gifti_file` has the wrong type: Received `{type(params["input_gifti_file"])}` expected `str`')
    if params.get("output_gifti_file", None) is None:
        raise StyxValidationError("`output_gifti_file` must not be None")
    if not isinstance(params["output_gifti_file"], str):
        raise StyxValidationError(f'`output_gifti_file` has the wrong type: Received `{type(params["output_gifti_file"])}` expected `str`')

def gifti_convert_cargs(params: GiftiConvert, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-gifti-convert")
    cargs.append(params["gifti_encoding"])
    cargs.append(params["input_gifti_file"])
    cargs.append(params["output_gifti_file"])
    return cargs

def gifti_convert_outputs(params: GiftiConvert, execution: Execution) -> GiftiConvertOutputs:
    """Build the GiftiConvertOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GiftiConvertOutputs(
        root=root_v,
    )

def gifti_convert_execute(params: GiftiConvert, runner: Runner | None = None) -> GiftiConvertOutputs:
    """
    CONVERT A GIFTI FILE TO A DIFFERENT ENCODING

    The value of <gifti-encoding> must be one of the following:

ASCII
BASE64_BINARY
GZIP_BASE64_BINARY
EXTERNAL_FILE_BINARY

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gifti_convert_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GIFTI_CONVERT_METADATA)
    execution.params(params)
    args = gifti_convert_cargs(params, execution)
    out = gifti_convert_outputs(params, execution)
    execution.run(args)
    return out

def gifti_convert(
    gifti_encoding: str,
    input_gifti_file: str,
    output_gifti_file: str,
    runner: Runner | None = None,
) -> GiftiConvertOutputs:
    """
    CONVERT A GIFTI FILE TO A DIFFERENT ENCODING

    The value of <gifti-encoding> must be one of the following:

ASCII
BASE64_BINARY
GZIP_BASE64_BINARY
EXTERNAL_FILE_BINARY

    Args:
        gifti_encoding: what the output encoding should be
        input_gifti_file: the input gifti file
        output_gifti_file: output - the output gifti file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gifti_convert_params(
        gifti_encoding=gifti_encoding,
        input_gifti_file=input_gifti_file,
        output_gifti_file=output_gifti_file,
    )
    return gifti_convert_execute(params, runner)

__all__ = [
    "GiftiConvert",
    "GiftiConvertOutputs",
    "GIFTI_CONVERT_METADATA",
    "gifti_convert_cargs",
    "gifti_convert_outputs",
    "gifti_convert_params",
    "gifti_convert_execute",
    "gifti_convert_validate",
    "gifti_convert",
]