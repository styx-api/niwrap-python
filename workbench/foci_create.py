# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FOCI_CREATE_METADATA = Metadata(
    id="foci_create",
    name="CREATE A FOCI FILE",
    package="workbench",
)

class FociCreateClass(typing.TypedDict):
    class_name: str
    """name of class"""
    foci_list_file: str
    """text file containing foci names, coordinates, and colors"""
    surface: InputPathType
    """surface file for projection of foci list file"""

FociCreate = typing.TypedDict(
    "FociCreate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/foci_create"]],
        "output": str,
        "class": list[FociCreateClass],
    },
)

@dataclasses.dataclass
class FociCreateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output foci file"""

def foci_create_params(
    output: str,
    class_: list[FociCreateClass],
) -> FociCreate:
    """
    Build parameters.

    Args:
        output: the output foci file
        class_: specify class input data

    Returns:
        Parameter dictionary.
    """
    params: FociCreate = {
        "@type": "workbench/foci_create",
        "output": output,
        "class": class_,
    }
    return params

def foci_create_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FociCreate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("class", None) is None:
        raise StyxValidationError("`class` must not be None")
    if not isinstance(params["class"], list):
        raise StyxValidationError(f'`class` has the wrong type: Received `{type(params["class"])}` expected `list[FociCreateClass]`')
    for e in params["class"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("class_name", None) is None:
            raise StyxValidationError("`class_name` must not be None")
        if not isinstance(e["class_name"], str):
            raise StyxValidationError(f'`class_name` has the wrong type: Received `{type(e["class_name"])}` expected `str`')
        if e.get("foci_list_file", None) is None:
            raise StyxValidationError("`foci_list_file` must not be None")
        if not isinstance(e["foci_list_file"], str):
            raise StyxValidationError(f'`foci_list_file` has the wrong type: Received `{type(e["foci_list_file"])}` expected `str`')
        if e.get("surface", None) is None:
            raise StyxValidationError("`surface` must not be None")
        if not isinstance(e["surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(e["surface"])}` expected `InputPathType`')

def foci_create_cargs(params: FociCreate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-foci-create")
    cargs.append(params["output"])
    for item0 in params["class"]:
        cargs.append("-class")
        cargs.append(item0["class_name"])
        cargs.append(item0["foci_list_file"])
        cargs.append(execution.input_file(item0["surface"]))
    return cargs

def foci_create_outputs(params: FociCreate, execution: Execution) -> FociCreateOutputs:
    """Build the FociCreateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return FociCreateOutputs(
        root=root_v,
        output=output_v,
    )

def foci_create_execute(params: FociCreate, runner: Runner | None = None) -> FociCreateOutputs:
    """
    CREATE A FOCI FILE

    Creates a foci file from names, coordinates, and RGB values in a text file.  The text file must have the following format (2 lines per focus):

<focus-name>
<red> <green> <blue> <x> <y> <z> 
...

Foci names are specified on a separate line from their coordinates and color, in order to let foci names contain spaces.  Whitespace is trimmed from both ends of the foci name, but is kept if it is in the middle of a name.  The values of <red>, <green>, <blue> and must be integers from 0 to 255, and will specify the color the foci is drawn as.

Foci are grouped into classes and the name for the class is specified using the <class-name> parameter.

All foci within one text file must be associated with the structure contained in the <surface> parameter and are projected to that surface.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    foci_create_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FOCI_CREATE_METADATA)
    execution.params(params)
    args = foci_create_cargs(params, execution)
    out = foci_create_outputs(params, execution)
    execution.run(args)
    return out

def foci_create(
    output: str,
    class_: list[FociCreateClass],
    runner: Runner | None = None,
) -> FociCreateOutputs:
    """
    CREATE A FOCI FILE

    Creates a foci file from names, coordinates, and RGB values in a text file.  The text file must have the following format (2 lines per focus):

<focus-name>
<red> <green> <blue> <x> <y> <z> 
...

Foci names are specified on a separate line from their coordinates and color, in order to let foci names contain spaces.  Whitespace is trimmed from both ends of the foci name, but is kept if it is in the middle of a name.  The values of <red>, <green>, <blue> and must be integers from 0 to 255, and will specify the color the foci is drawn as.

Foci are grouped into classes and the name for the class is specified using the <class-name> parameter.

All foci within one text file must be associated with the structure contained in the <surface> parameter and are projected to that surface.

    Args:
        output: the output foci file
        class_: specify class input data
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = foci_create_params(
        output=output,
        class_=class_,
    )
    return foci_create_execute(params, runner)

__all__ = [
    "FociCreate",
    "FociCreateOutputs",
    "FOCI_CREATE_METADATA",
    "foci_create_cargs",
    "foci_create_outputs",
    "foci_create_params",
    "foci_create_execute",
    "foci_create_validate",
    "foci_create",
]