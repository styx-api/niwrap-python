# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONVERT_AFFINE_METADATA = Metadata(
    id="convert_affine",
    name="CONVERT AN AFFINE FILE BETWEEN CONVENTIONS",
    package="workbench",
)

class ConvertAffineFromWorld(typing.TypedDict):
    input: str
    """the input affine"""
    inverse: typing.NotRequired[bool]
    """for files that use 'target to source' convention"""

class ConvertAffineFromFlirt(typing.TypedDict):
    input: str
    """the input affine"""
    source_volume: str
    """the source volume used when generating the input affine"""
    target_volume: str
    """the target volume used when generating the input affine"""

class ConvertAffineToWorld(typing.TypedDict):
    output: str
    """output - the output affine"""
    inverse: typing.NotRequired[bool]
    """write file using 'target to source' convention"""

class ConvertAffineToFlirt(typing.TypedDict):
    output: str
    """output - the output affine"""
    source_volume: str
    """the volume you want to apply the transform to"""
    target_volume: str
    """the target space you want the transformed volume to match"""

ConvertAffine = typing.TypedDict(
    "ConvertAffine",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/convert_affine"]],
        "from_world": typing.NotRequired[ConvertAffineFromWorld],
        "from_itk": typing.NotRequired[str],
        "from_flirt": typing.NotRequired[ConvertAffineFromFlirt],
        "to_world": typing.NotRequired[ConvertAffineToWorld],
        "to_itk": typing.NotRequired[str],
        "to_flirt": list[ConvertAffineToFlirt],
    },
)

@dataclasses.dataclass
class ConvertAffineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def convert_affine_params(
    to_flirt: list[ConvertAffineToFlirt],
    from_world: ConvertAffineFromWorld | None = None,
    from_itk: str | None = None,
    from_flirt: ConvertAffineFromFlirt | None = None,
    to_world: ConvertAffineToWorld | None = None,
    to_itk: str | None = None,
) -> ConvertAffine:
    """
    Build parameters.

    Args:
        to_flirt: write output as a flirt matrix
        from_world: input is a NIFTI 'world' affine
        from_itk: input is an ITK matrix
        from_flirt: input is a flirt matrix
        to_world: write output as a NIFTI 'world' affine
        to_itk: write output as an ITK affine

    Returns:
        Parameter dictionary.
    """
    params: ConvertAffine = {
        "@type": "workbench/convert_affine",
        "to_flirt": to_flirt,
    }
    if from_world is not None:
        params["from_world"] = from_world
    if from_itk is not None:
        params["from_itk"] = from_itk
    if from_flirt is not None:
        params["from_flirt"] = from_flirt
    if to_world is not None:
        params["to_world"] = to_world
    if to_itk is not None:
        params["to_itk"] = to_itk
    return params

def convert_affine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ConvertAffine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("from_world", None) is not None:
        if not isinstance(params["from_world"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["from_world"])}\'')
        if params["from_world"].get("input", None) is None:
            raise StyxValidationError("`input` must not be None")
        if not isinstance(params["from_world"]["input"], str):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["from_world"]["input"])}` expected `str`')
        if params["from_world"].get("inverse", None) is not None:
            if not isinstance(params["from_world"]["inverse"], bool):
                raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["from_world"]["inverse"])}` expected `bool`')
    if params.get("from_itk", None) is not None:
        if not isinstance(params["from_itk"], str):
            raise StyxValidationError(f'`from_itk` has the wrong type: Received `{type(params["from_itk"])}` expected `str`')
    if params.get("from_flirt", None) is not None:
        if not isinstance(params["from_flirt"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["from_flirt"])}\'')
        if params["from_flirt"].get("input", None) is None:
            raise StyxValidationError("`input` must not be None")
        if not isinstance(params["from_flirt"]["input"], str):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["from_flirt"]["input"])}` expected `str`')
        if params["from_flirt"].get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(params["from_flirt"]["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["from_flirt"]["source_volume"])}` expected `str`')
        if params["from_flirt"].get("target_volume", None) is None:
            raise StyxValidationError("`target_volume` must not be None")
        if not isinstance(params["from_flirt"]["target_volume"], str):
            raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["from_flirt"]["target_volume"])}` expected `str`')
    if params.get("to_world", None) is not None:
        if not isinstance(params["to_world"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["to_world"])}\'')
        if params["to_world"].get("output", None) is None:
            raise StyxValidationError("`output` must not be None")
        if not isinstance(params["to_world"]["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["to_world"]["output"])}` expected `str`')
        if params["to_world"].get("inverse", None) is not None:
            if not isinstance(params["to_world"]["inverse"], bool):
                raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["to_world"]["inverse"])}` expected `bool`')
    if params.get("to_itk", None) is not None:
        if not isinstance(params["to_itk"], str):
            raise StyxValidationError(f'`to_itk` has the wrong type: Received `{type(params["to_itk"])}` expected `str`')
    if params.get("to_flirt", None) is None:
        raise StyxValidationError("`to_flirt` must not be None")
    if not isinstance(params["to_flirt"], list):
        raise StyxValidationError(f'`to_flirt` has the wrong type: Received `{type(params["to_flirt"])}` expected `list[ConvertAffineToFlirt]`')
    for e in params["to_flirt"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("output", None) is None:
            raise StyxValidationError("`output` must not be None")
        if not isinstance(e["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(e["output"])}` expected `str`')
        if e.get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(e["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(e["source_volume"])}` expected `str`')
        if e.get("target_volume", None) is None:
            raise StyxValidationError("`target_volume` must not be None")
        if not isinstance(e["target_volume"], str):
            raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(e["target_volume"])}` expected `str`')

def convert_affine_cargs(params: ConvertAffine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-convert-affine")
    v_0 = params.get("from_world")
    if v_0 is not None:
        cargs.append("-from-world")
        cargs.append(v_0["input"])
        if v_0.get("inverse"):
            cargs.append("-inverse")
    v_1 = params.get("from_itk")
    if v_1 is not None:
        cargs.append("-from-itk")
        cargs.append(v_1)
    v_2 = params.get("from_flirt")
    if v_2 is not None:
        cargs.append("-from-flirt")
        cargs.append(v_2["input"])
        cargs.append(v_2["source_volume"])
        cargs.append(v_2["target_volume"])
    v_3 = params.get("to_world")
    if v_3 is not None:
        cargs.append("-to-world")
        cargs.append(v_3["output"])
        if v_3.get("inverse"):
            cargs.append("-inverse")
    v_4 = params.get("to_itk")
    if v_4 is not None:
        cargs.append("-to-itk")
        cargs.append(v_4)
    for item0 in params["to_flirt"]:
        cargs.append("-to-flirt")
        cargs.append(item0["output"])
        cargs.append(item0["source_volume"])
        cargs.append(item0["target_volume"])
    return cargs

def convert_affine_outputs(params: ConvertAffine, execution: Execution) -> ConvertAffineOutputs:
    """Build the ConvertAffineOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ConvertAffineOutputs(
        root=root_v,
    )

def convert_affine_execute(params: ConvertAffine, runner: Runner | None = None) -> ConvertAffineOutputs:
    """
    CONVERT AN AFFINE FILE BETWEEN CONVENTIONS

    NIFTI world matrices can be used directly on mm coordinates via matrix multiplication, they use the NIFTI coordinate system, that is, positive X is right, positive Y is anterior, and positive Z is superior.  Note that wb_command assumes that world matrices transform source coordinates to target coordinates, while other tools may use affines that transform target coordinates to source coordinates.

The ITK format is used by ANTS.

You must specify exactly one -from option, but you may specify multiple -to options, and -to-flirt may be specified more than once.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    convert_affine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONVERT_AFFINE_METADATA)
    execution.params(params)
    args = convert_affine_cargs(params, execution)
    out = convert_affine_outputs(params, execution)
    execution.run(args)
    return out

def convert_affine(
    to_flirt: list[ConvertAffineToFlirt],
    from_world: ConvertAffineFromWorld | None = None,
    from_itk: str | None = None,
    from_flirt: ConvertAffineFromFlirt | None = None,
    to_world: ConvertAffineToWorld | None = None,
    to_itk: str | None = None,
    runner: Runner | None = None,
) -> ConvertAffineOutputs:
    """
    CONVERT AN AFFINE FILE BETWEEN CONVENTIONS

    NIFTI world matrices can be used directly on mm coordinates via matrix multiplication, they use the NIFTI coordinate system, that is, positive X is right, positive Y is anterior, and positive Z is superior.  Note that wb_command assumes that world matrices transform source coordinates to target coordinates, while other tools may use affines that transform target coordinates to source coordinates.

The ITK format is used by ANTS.

You must specify exactly one -from option, but you may specify multiple -to options, and -to-flirt may be specified more than once.

    Args:
        to_flirt: write output as a flirt matrix
        from_world: input is a NIFTI 'world' affine
        from_itk: input is an ITK matrix
        from_flirt: input is a flirt matrix
        to_world: write output as a NIFTI 'world' affine
        to_itk: write output as an ITK affine
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = convert_affine_params(
        to_flirt=to_flirt,
        from_world=from_world,
        from_itk=from_itk,
        from_flirt=from_flirt,
        to_world=to_world,
        to_itk=to_itk,
    )
    return convert_affine_execute(params, runner)

__all__ = [
    "ConvertAffine",
    "ConvertAffineOutputs",
    "CONVERT_AFFINE_METADATA",
    "convert_affine_cargs",
    "convert_affine_outputs",
    "convert_affine_params",
    "convert_affine_execute",
    "convert_affine_validate",
    "convert_affine",
]