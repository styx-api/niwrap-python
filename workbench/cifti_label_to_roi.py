# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_TO_ROI_METADATA = Metadata(
    id="cifti_label_to_roi",
    name="MAKE A CIFTI LABEL INTO AN ROI",
    package="workbench",
)

CiftiLabelToRoi = typing.TypedDict(
    "CiftiLabelToRoi",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_to_roi"]],
        "label_in": InputPathType,
        "scalar_out": str,
        "name": typing.NotRequired[str],
        "key": typing.NotRequired[int],
        "map": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CiftiLabelToRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    scalar_out: OutputPathType
    """the output cifti scalar file"""

def cifti_label_to_roi_params(
    label_in: InputPathType,
    scalar_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
) -> CiftiLabelToRoi:
    """
    Build parameters.

    Args:
        label_in: the input cifti label file
        scalar_out: the output cifti scalar file
        name: select label by name
        key: select label by key
        map: select a single label map to use

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelToRoi = {
        "@type": "workbench/cifti_label_to_roi",
        "label_in": label_in,
        "scalar_out": scalar_out,
    }
    if name is not None:
        params["name"] = name
    if key is not None:
        params["key"] = key
    if map is not None:
        params["map"] = map
    return params

def cifti_label_to_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelToRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("scalar_out", None) is None:
        raise StyxValidationError("`scalar_out` must not be None")
    if not isinstance(params["scalar_out"], str):
        raise StyxValidationError(f'`scalar_out` has the wrong type: Received `{type(params["scalar_out"])}` expected `str`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("key", None) is not None:
        if not isinstance(params["key"], int):
            raise StyxValidationError(f'`key` has the wrong type: Received `{type(params["key"])}` expected `int`')
    if params.get("map", None) is not None:
        if not isinstance(params["map"], str):
            raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')

def cifti_label_to_roi_cargs(params: CiftiLabelToRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-to-roi")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["scalar_out"])
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("-name")
        cargs.append(v_0)
    v_1 = params.get("key")
    if v_1 is not None:
        cargs.append("-key")
        cargs.append(str(v_1))
    v_2 = params.get("map")
    if v_2 is not None:
        cargs.append("-map")
        cargs.append(v_2)
    return cargs

def cifti_label_to_roi_outputs(params: CiftiLabelToRoi, execution: Execution) -> CiftiLabelToRoiOutputs:
    """Build the CiftiLabelToRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    scalar_out_v: OutputPathType = execution.output_file(params["scalar_out"])
    return CiftiLabelToRoiOutputs(
        root=root_v,
        scalar_out=scalar_out_v,
    )

def cifti_label_to_roi_execute(params: CiftiLabelToRoi, runner: Runner | None = None) -> CiftiLabelToRoiOutputs:
    """
    MAKE A CIFTI LABEL INTO AN ROI

    For each map in <label-in>, a map is created in <scalar-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_to_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_TO_ROI_METADATA)
    execution.params(params)
    args = cifti_label_to_roi_cargs(params, execution)
    out = cifti_label_to_roi_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_to_roi(
    label_in: InputPathType,
    scalar_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
    runner: Runner | None = None,
) -> CiftiLabelToRoiOutputs:
    """
    MAKE A CIFTI LABEL INTO AN ROI

    For each map in <label-in>, a map is created in <scalar-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        label_in: the input cifti label file
        scalar_out: the output cifti scalar file
        name: select label by name
        key: select label by key
        map: select a single label map to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_to_roi_params(
        label_in=label_in,
        scalar_out=scalar_out,
        name=name,
        key=key,
        map=map,
    )
    return cifti_label_to_roi_execute(params, runner)

__all__ = [
    "CiftiLabelToRoi",
    "CiftiLabelToRoiOutputs",
    "CIFTI_LABEL_TO_ROI_METADATA",
    "cifti_label_to_roi_cargs",
    "cifti_label_to_roi_outputs",
    "cifti_label_to_roi_params",
    "cifti_label_to_roi_execute",
    "cifti_label_to_roi_validate",
    "cifti_label_to_roi",
]