# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_TFCE_METADATA = Metadata(
    id="volume_tfce",
    name="DO TFCE ON A VOLUME FILE",
    package="workbench",
)

class VolumeTfcePresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """smoothing kernel size is FWHM, not sigma"""

class VolumeTfceParameters(typing.TypedDict):
    e: float
    """exponent for cluster volume (default 0.5)"""
    h: float
    """exponent for threshold value (default 2.0)"""

VolumeTfce = typing.TypedDict(
    "VolumeTfce",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_tfce"]],
        "volume_in": InputPathType,
        "volume_out": str,
        "presmooth": typing.NotRequired[VolumeTfcePresmooth],
        "roi": typing.NotRequired[InputPathType],
        "parameters": typing.NotRequired[VolumeTfceParameters],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeTfceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_tfce_params(
    volume_in: InputPathType,
    volume_out: str,
    presmooth: VolumeTfcePresmooth | None = None,
    roi: InputPathType | None = None,
    parameters: VolumeTfceParameters | None = None,
    subvolume: str | None = None,
) -> VolumeTfce:
    """
    Build parameters.

    Args:
        volume_in: the volume to run TFCE on
        volume_out: the output volume
        presmooth: smooth the volume before running TFCE
        roi: select a region of interest to run TFCE on
        parameters: set parameters for TFCE integral
        subvolume: select a single subvolume

    Returns:
        Parameter dictionary.
    """
    params: VolumeTfce = {
        "@type": "workbench/volume_tfce",
        "volume_in": volume_in,
        "volume_out": volume_out,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if parameters is not None:
        params["parameters"] = parameters
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_tfce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeTfce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("parameters", None) is not None:
        if not isinstance(params["parameters"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["parameters"])}\'')
        if params["parameters"].get("e", None) is None:
            raise StyxValidationError("`e` must not be None")
        if not isinstance(params["parameters"]["e"], (float, int)):
            raise StyxValidationError(f'`e` has the wrong type: Received `{type(params["parameters"]["e"])}` expected `float`')
        if params["parameters"].get("h", None) is None:
            raise StyxValidationError("`h` must not be None")
        if not isinstance(params["parameters"]["h"], (float, int)):
            raise StyxValidationError(f'`h` has the wrong type: Received `{type(params["parameters"]["h"])}` expected `float`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_tfce_cargs(params: VolumeTfce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-tfce")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("parameters")
    if v_2 is not None:
        cargs.append("-parameters")
        cargs.append(str(v_2["e"]))
        cargs.append(str(v_2["h"]))
    v_3 = params.get("subvolume")
    if v_3 is not None:
        cargs.append("-subvolume")
        cargs.append(v_3)
    return cargs

def volume_tfce_outputs(params: VolumeTfce, execution: Execution) -> VolumeTfceOutputs:
    """Build the VolumeTfceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeTfceOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_tfce_execute(params: VolumeTfce, runner: Runner | None = None) -> VolumeTfceOutputs:
    """
    DO TFCE ON A VOLUME FILE

    This command does not do any statistical analysis.  Please use something like PALM if you are just trying to do statistics on your data.

Threshold-free cluster enhancement is a method to increase the relative value of regions that would form clusters in a standard thresholding test.  This is accomplished by evaluating the integral of:

e(h, p)^E * h^H * dh

at each voxel p, where h ranges from 0 to the maximum value in the data, and e(h, p) is the extent of the cluster containing voxel p at threshold h.  Negative values are similarly enhanced by negating the data, running the same process, and negating the result.

This method is explained in: Smith SM, Nichols TE., "Threshold-free cluster enhancement: addressing problems of smoothing, threshold dependence and localisation in cluster inference." Neuroimage. 2009 Jan 1;44(1):83-98. PMID: 18501637

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_tfce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_TFCE_METADATA)
    execution.params(params)
    args = volume_tfce_cargs(params, execution)
    out = volume_tfce_outputs(params, execution)
    execution.run(args)
    return out

def volume_tfce(
    volume_in: InputPathType,
    volume_out: str,
    presmooth: VolumeTfcePresmooth | None = None,
    roi: InputPathType | None = None,
    parameters: VolumeTfceParameters | None = None,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeTfceOutputs:
    """
    DO TFCE ON A VOLUME FILE

    This command does not do any statistical analysis.  Please use something like PALM if you are just trying to do statistics on your data.

Threshold-free cluster enhancement is a method to increase the relative value of regions that would form clusters in a standard thresholding test.  This is accomplished by evaluating the integral of:

e(h, p)^E * h^H * dh

at each voxel p, where h ranges from 0 to the maximum value in the data, and e(h, p) is the extent of the cluster containing voxel p at threshold h.  Negative values are similarly enhanced by negating the data, running the same process, and negating the result.

This method is explained in: Smith SM, Nichols TE., "Threshold-free cluster enhancement: addressing problems of smoothing, threshold dependence and localisation in cluster inference." Neuroimage. 2009 Jan 1;44(1):83-98. PMID: 18501637

    Args:
        volume_in: the volume to run TFCE on
        volume_out: the output volume
        presmooth: smooth the volume before running TFCE
        roi: select a region of interest to run TFCE on
        parameters: set parameters for TFCE integral
        subvolume: select a single subvolume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_tfce_params(
        volume_in=volume_in,
        volume_out=volume_out,
        presmooth=presmooth,
        roi=roi,
        parameters=parameters,
        subvolume=subvolume,
    )
    return volume_tfce_execute(params, runner)

__all__ = [
    "VolumeTfce",
    "VolumeTfceOutputs",
    "VOLUME_TFCE_METADATA",
    "volume_tfce_cargs",
    "volume_tfce_outputs",
    "volume_tfce_params",
    "volume_tfce_execute",
    "volume_tfce_validate",
    "volume_tfce",
]