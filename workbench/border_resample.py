# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_RESAMPLE_METADATA = Metadata(
    id="border_resample",
    name="RESAMPLE A BORDER FILE TO A DIFFERENT MESH",
    package="workbench",
)

BorderResample = typing.TypedDict(
    "BorderResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_resample"]],
        "border_in": InputPathType,
        "current_sphere": InputPathType,
        "new_sphere": InputPathType,
        "border_out": str,
    },
)

@dataclasses.dataclass
class BorderResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    border_out: OutputPathType
    """the output border file"""

def border_resample_params(
    border_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    border_out: str,
) -> BorderResample:
    """
    Build parameters.

    Args:
        border_in: the border file to resample
        current_sphere: a sphere surface with the mesh that the metric is currently\
            on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        border_out: the output border file

    Returns:
        Parameter dictionary.
    """
    params: BorderResample = {
        "@type": "workbench/border_resample",
        "border_in": border_in,
        "current_sphere": current_sphere,
        "new_sphere": new_sphere,
        "border_out": border_out,
    }
    return params

def border_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("border_in", None) is None:
        raise StyxValidationError("`border_in` must not be None")
    if not isinstance(params["border_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`border_in` has the wrong type: Received `{type(params["border_in"])}` expected `InputPathType`')
    if params.get("current_sphere", None) is None:
        raise StyxValidationError("`current_sphere` must not be None")
    if not isinstance(params["current_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`current_sphere` has the wrong type: Received `{type(params["current_sphere"])}` expected `InputPathType`')
    if params.get("new_sphere", None) is None:
        raise StyxValidationError("`new_sphere` must not be None")
    if not isinstance(params["new_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_sphere` has the wrong type: Received `{type(params["new_sphere"])}` expected `InputPathType`')
    if params.get("border_out", None) is None:
        raise StyxValidationError("`border_out` must not be None")
    if not isinstance(params["border_out"], str):
        raise StyxValidationError(f'`border_out` has the wrong type: Received `{type(params["border_out"])}` expected `str`')

def border_resample_cargs(params: BorderResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-resample")
    cargs.append(execution.input_file(params["border_in"]))
    cargs.append(execution.input_file(params["current_sphere"]))
    cargs.append(execution.input_file(params["new_sphere"]))
    cargs.append(params["border_out"])
    return cargs

def border_resample_outputs(params: BorderResample, execution: Execution) -> BorderResampleOutputs:
    """Build the BorderResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    border_out_v: OutputPathType = execution.output_file(params["border_out"])
    return BorderResampleOutputs(
        root=root_v,
        border_out=border_out_v,
    )

def border_resample_execute(params: BorderResample, runner: Runner | None = None) -> BorderResampleOutputs:
    """
    RESAMPLE A BORDER FILE TO A DIFFERENT MESH

    Resamples a border file, given two spherical surfaces that are in register.  Only borders that have the same structure as current-sphere will be resampled.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_RESAMPLE_METADATA)
    execution.params(params)
    args = border_resample_cargs(params, execution)
    out = border_resample_outputs(params, execution)
    execution.run(args)
    return out

def border_resample(
    border_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    border_out: str,
    runner: Runner | None = None,
) -> BorderResampleOutputs:
    """
    RESAMPLE A BORDER FILE TO A DIFFERENT MESH

    Resamples a border file, given two spherical surfaces that are in register.  Only borders that have the same structure as current-sphere will be resampled.

    Args:
        border_in: the border file to resample
        current_sphere: a sphere surface with the mesh that the metric is currently\
            on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        border_out: the output border file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_resample_params(
        border_in=border_in,
        current_sphere=current_sphere,
        new_sphere=new_sphere,
        border_out=border_out,
    )
    return border_resample_execute(params, runner)

__all__ = [
    "BorderResample",
    "BorderResampleOutputs",
    "BORDER_RESAMPLE_METADATA",
    "border_resample_cargs",
    "border_resample_outputs",
    "border_resample_params",
    "border_resample_execute",
    "border_resample_validate",
    "border_resample",
]