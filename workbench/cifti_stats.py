# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_STATS_METADATA = Metadata(
    id="cifti_stats",
    name="STATISTICS ALONG CIFTI COLUMNS",
    package="workbench",
)

class CiftiStatsRoi(typing.TypedDict):
    roi_cifti: InputPathType
    """the roi, as a cifti file"""
    match_maps: typing.NotRequired[bool]
    """each column of input uses the corresponding column from the roi file"""

CiftiStats = typing.TypedDict(
    "CiftiStats",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_stats"]],
        "cifti_in": InputPathType,
        "reduce": typing.NotRequired[str],
        "percentile": typing.NotRequired[float],
        "column": typing.NotRequired[int],
        "roi": typing.NotRequired[CiftiStatsRoi],
        "show_map_name": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cifti_stats_params(
    cifti_in: InputPathType,
    reduce: str | None = None,
    percentile: float | None = None,
    column: int | None = None,
    roi: CiftiStatsRoi | None = None,
    show_map_name: bool = False,
) -> CiftiStats:
    """
    Build parameters.

    Args:
        cifti_in: the input cifti
        reduce: use a reduction operation
        percentile: give the value at a percentile
        column: only display output for one column
        roi: only consider data inside an roi
        show_map_name: print column index and name before each output

    Returns:
        Parameter dictionary.
    """
    params: CiftiStats = {
        "@type": "workbench/cifti_stats",
        "cifti_in": cifti_in,
        "show_map_name": show_map_name,
    }
    if reduce is not None:
        params["reduce"] = reduce
    if percentile is not None:
        params["percentile"] = percentile
    if column is not None:
        params["column"] = column
    if roi is not None:
        params["roi"] = roi
    return params

def cifti_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("reduce", None) is not None:
        if not isinstance(params["reduce"], str):
            raise StyxValidationError(f'`reduce` has the wrong type: Received `{type(params["reduce"])}` expected `str`')
    if params.get("percentile", None) is not None:
        if not isinstance(params["percentile"], (float, int)):
            raise StyxValidationError(f'`percentile` has the wrong type: Received `{type(params["percentile"])}` expected `float`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], int):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `int`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi"])}\'')
        if params["roi"].get("roi_cifti", None) is None:
            raise StyxValidationError("`roi_cifti` must not be None")
        if not isinstance(params["roi"]["roi_cifti"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi_cifti` has the wrong type: Received `{type(params["roi"]["roi_cifti"])}` expected `InputPathType`')
        if params["roi"].get("match_maps", None) is not None:
            if not isinstance(params["roi"]["match_maps"], bool):
                raise StyxValidationError(f'`match_maps` has the wrong type: Received `{type(params["roi"]["match_maps"])}` expected `bool`')
    if params.get("show_map_name", None) is not None:
        if not isinstance(params["show_map_name"], bool):
            raise StyxValidationError(f'`show_map_name` has the wrong type: Received `{type(params["show_map_name"])}` expected `bool`')

def cifti_stats_cargs(params: CiftiStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-stats")
    cargs.append(execution.input_file(params["cifti_in"]))
    v_0 = params.get("reduce")
    if v_0 is not None:
        cargs.append("-reduce")
        cargs.append(v_0)
    v_1 = params.get("percentile")
    if v_1 is not None:
        cargs.append("-percentile")
        cargs.append(str(v_1))
    v_2 = params.get("column")
    if v_2 is not None:
        cargs.append("-column")
        cargs.append(str(v_2))
    v_3 = params.get("roi")
    if v_3 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_3["roi_cifti"]))
        if v_3.get("match_maps"):
            cargs.append("-match-maps")
    if params.get("show_map_name"):
        cargs.append("-show-map-name")
    return cargs

def cifti_stats_outputs(params: CiftiStats, execution: Execution) -> CiftiStatsOutputs:
    """Build the CiftiStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CiftiStatsOutputs(
        root=root_v,
    )

def cifti_stats_execute(params: CiftiStats, runner: Runner | None = None) -> CiftiStatsOutputs:
    """
    STATISTICS ALONG CIFTI COLUMNS

    For each column of the input, a line of text is printed, resulting from the specified reduction or percentile operation.  If -roi is specified without -match-maps, then each line will contain as many numbers as there are maps in the ROI file, separated by tab characters.  Use -column to only give output for a single data column.  Exactly one of -reduce or -percentile must be specified.

The argument to the -reduce option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_STATS_METADATA)
    execution.params(params)
    args = cifti_stats_cargs(params, execution)
    out = cifti_stats_outputs(params, execution)
    execution.run(args)
    return out

def cifti_stats(
    cifti_in: InputPathType,
    reduce: str | None = None,
    percentile: float | None = None,
    column: int | None = None,
    roi: CiftiStatsRoi | None = None,
    show_map_name: bool = False,
    runner: Runner | None = None,
) -> CiftiStatsOutputs:
    """
    STATISTICS ALONG CIFTI COLUMNS

    For each column of the input, a line of text is printed, resulting from the specified reduction or percentile operation.  If -roi is specified without -match-maps, then each line will contain as many numbers as there are maps in the ROI file, separated by tab characters.  Use -column to only give output for a single data column.  Exactly one of -reduce or -percentile must be specified.

The argument to the -reduce option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        cifti_in: the input cifti
        reduce: use a reduction operation
        percentile: give the value at a percentile
        column: only display output for one column
        roi: only consider data inside an roi
        show_map_name: print column index and name before each output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_stats_params(
        cifti_in=cifti_in,
        reduce=reduce,
        percentile=percentile,
        column=column,
        roi=roi,
        show_map_name=show_map_name,
    )
    return cifti_stats_execute(params, runner)

__all__ = [
    "CiftiStats",
    "CiftiStatsOutputs",
    "CIFTI_STATS_METADATA",
    "cifti_stats_cargs",
    "cifti_stats_outputs",
    "cifti_stats_params",
    "cifti_stats_execute",
    "cifti_stats_validate",
    "cifti_stats",
]