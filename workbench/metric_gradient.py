# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_GRADIENT_METADATA = Metadata(
    id="metric_gradient",
    name="SURFACE GRADIENT OF A METRIC FILE",
    package="workbench",
)

class MetricGradientPresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class MetricGradientRoi(typing.TypedDict):
    roi_metric: InputPathType
    """the area to take the gradient within, as a metric"""
    match_columns: typing.NotRequired[bool]
    """for each input column, use the corresponding column from the roi"""

class MetricGradientVectors(typing.TypedDict):
    vector_metric_out: str
    """the vectors as a metric file"""

MetricGradient = typing.TypedDict(
    "MetricGradient",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_gradient"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "metric_out": str,
        "presmooth": typing.NotRequired[MetricGradientPresmooth],
        "roi": typing.NotRequired[MetricGradientRoi],
        "vectors": typing.NotRequired[MetricGradientVectors],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
        "average_normals": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MetricGradientOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the magnitude of the gradient"""
    vector_metric_out: OutputPathType | None
    """the vectors as a metric file"""

def metric_gradient_params(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    presmooth: MetricGradientPresmooth | None = None,
    roi: MetricGradientRoi | None = None,
    vectors: MetricGradientVectors | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    average_normals: bool = False,
) -> MetricGradient:
    """
    Build parameters.

    Args:
        surface: the surface to compute the gradient on
        metric_in: the metric to compute the gradient of
        metric_out: the magnitude of the gradient
        presmooth: smooth the metric before computing the gradient
        roi: select a region of interest to take the gradient of
        vectors: output gradient vectors
        column: select a single column to compute the gradient of
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        average_normals: average the normals of each vertex with its neighbors\
            before using them to compute the gradient

    Returns:
        Parameter dictionary.
    """
    params: MetricGradient = {
        "@type": "workbench/metric_gradient",
        "surface": surface,
        "metric_in": metric_in,
        "metric_out": metric_out,
        "average_normals": average_normals,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if vectors is not None:
        params["vectors"] = vectors
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_gradient_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricGradient."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi"])}\'')
        if params["roi"].get("roi_metric", None) is None:
            raise StyxValidationError("`roi_metric` must not be None")
        if not isinstance(params["roi"]["roi_metric"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi_metric` has the wrong type: Received `{type(params["roi"]["roi_metric"])}` expected `InputPathType`')
        if params["roi"].get("match_columns", None) is not None:
            if not isinstance(params["roi"]["match_columns"], bool):
                raise StyxValidationError(f'`match_columns` has the wrong type: Received `{type(params["roi"]["match_columns"])}` expected `bool`')
    if params.get("vectors", None) is not None:
        if not isinstance(params["vectors"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["vectors"])}\'')
        if params["vectors"].get("vector_metric_out", None) is None:
            raise StyxValidationError("`vector_metric_out` must not be None")
        if not isinstance(params["vectors"]["vector_metric_out"], str):
            raise StyxValidationError(f'`vector_metric_out` has the wrong type: Received `{type(params["vectors"]["vector_metric_out"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("average_normals", None) is not None:
        if not isinstance(params["average_normals"], bool):
            raise StyxValidationError(f'`average_normals` has the wrong type: Received `{type(params["average_normals"])}` expected `bool`')

def metric_gradient_cargs(params: MetricGradient, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-gradient")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1["roi_metric"]))
        if v_1.get("match_columns"):
            cargs.append("-match-columns")
    v_2 = params.get("vectors")
    if v_2 is not None:
        cargs.append("-vectors")
        cargs.append(v_2["vector_metric_out"])
    v_3 = params.get("column")
    if v_3 is not None:
        cargs.append("-column")
        cargs.append(v_3)
    v_4 = params.get("corrected_areas")
    if v_4 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_4))
    if params.get("average_normals"):
        cargs.append("-average-normals")
    return cargs

def metric_gradient_outputs(params: MetricGradient, execution: Execution) -> MetricGradientOutputs:
    """Build the MetricGradientOutputs object for this tool."""
    vector_metric_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    __v0 = params.get("vectors")
    if __v0 is not None:
        vector_metric_out_v = execution.output_file(__v0["vector_metric_out"])
    return MetricGradientOutputs(
        root=root_v,
        metric_out=metric_out_v,
        vector_metric_out=vector_metric_out_v,
    )

def metric_gradient_execute(params: MetricGradient, runner: Runner | None = None) -> MetricGradientOutputs:
    """
    SURFACE GRADIENT OF A METRIC FILE

    At each vertex, the immediate neighbors are unfolded onto a plane tangent to the surface at the vertex (specifically, perpendicular to the normal).  The gradient is computed using a regression between the unfolded positions of the vertices and their values.  The gradient is then given by the slopes of the regression, and reconstructed as a 3D gradient vector.  By default, takes the gradient of all columns, with no presmoothing, across the whole surface, without averaging the normals of the surface among neighbors.

When using -corrected-areas, note that it is an approximate correction.  Doing smoothing on individual surfaces before averaging/gradient is preferred, when possible, in order to make use of the original surface structure.

Specifying an ROI will restrict the gradient to only use data from where the ROI metric is positive, and output zeros anywhere the ROI metric is not positive.

By default, the first column of the roi metric is used for all input columns.  When -match-columns is specified to the -roi option, the input and roi metrics must have the same number of columns, and for each input column's index, the same column index is used in the roi metric.  If the -match-columns option to -roi is used while the -column option is also used, the number of columns of the roi metric must match the input metric, and it will use the roi column with the index of the selected input column.

The vector output metric is organized such that the X, Y, and Z components from a single input column are consecutive columns.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_gradient_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_GRADIENT_METADATA)
    execution.params(params)
    args = metric_gradient_cargs(params, execution)
    out = metric_gradient_outputs(params, execution)
    execution.run(args)
    return out

def metric_gradient(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    presmooth: MetricGradientPresmooth | None = None,
    roi: MetricGradientRoi | None = None,
    vectors: MetricGradientVectors | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    average_normals: bool = False,
    runner: Runner | None = None,
) -> MetricGradientOutputs:
    """
    SURFACE GRADIENT OF A METRIC FILE

    At each vertex, the immediate neighbors are unfolded onto a plane tangent to the surface at the vertex (specifically, perpendicular to the normal).  The gradient is computed using a regression between the unfolded positions of the vertices and their values.  The gradient is then given by the slopes of the regression, and reconstructed as a 3D gradient vector.  By default, takes the gradient of all columns, with no presmoothing, across the whole surface, without averaging the normals of the surface among neighbors.

When using -corrected-areas, note that it is an approximate correction.  Doing smoothing on individual surfaces before averaging/gradient is preferred, when possible, in order to make use of the original surface structure.

Specifying an ROI will restrict the gradient to only use data from where the ROI metric is positive, and output zeros anywhere the ROI metric is not positive.

By default, the first column of the roi metric is used for all input columns.  When -match-columns is specified to the -roi option, the input and roi metrics must have the same number of columns, and for each input column's index, the same column index is used in the roi metric.  If the -match-columns option to -roi is used while the -column option is also used, the number of columns of the roi metric must match the input metric, and it will use the roi column with the index of the selected input column.

The vector output metric is organized such that the X, Y, and Z components from a single input column are consecutive columns.

    Args:
        surface: the surface to compute the gradient on
        metric_in: the metric to compute the gradient of
        metric_out: the magnitude of the gradient
        presmooth: smooth the metric before computing the gradient
        roi: select a region of interest to take the gradient of
        vectors: output gradient vectors
        column: select a single column to compute the gradient of
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        average_normals: average the normals of each vertex with its neighbors\
            before using them to compute the gradient
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_gradient_params(
        surface=surface,
        metric_in=metric_in,
        metric_out=metric_out,
        presmooth=presmooth,
        roi=roi,
        vectors=vectors,
        column=column,
        corrected_areas=corrected_areas,
        average_normals=average_normals,
    )
    return metric_gradient_execute(params, runner)

__all__ = [
    "MetricGradient",
    "MetricGradientOutputs",
    "METRIC_GRADIENT_METADATA",
    "metric_gradient_cargs",
    "metric_gradient_outputs",
    "metric_gradient_params",
    "metric_gradient_execute",
    "metric_gradient_validate",
    "metric_gradient",
]