# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_MERGE_METADATA = Metadata(
    id="border_merge",
    name="MERGE BORDER FILES INTO A NEW FILE",
    package="workbench",
)

class BorderMergeUpTo(typing.TypedDict):
    last_border: str
    """the number or name of the last column to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class BorderMergeSelect(typing.TypedDict):
    border: str
    """the border number or name"""
    up_to: typing.NotRequired[BorderMergeUpTo]
    """use an inclusive range of borders"""

class BorderMergeBorder(typing.TypedDict):
    border_file_in: InputPathType
    """a border file to use borders from"""
    select: list[BorderMergeSelect]
    """select a single border to use"""

BorderMerge = typing.TypedDict(
    "BorderMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_merge"]],
        "border_file_out": str,
        "border": list[BorderMergeBorder],
    },
)

@dataclasses.dataclass
class BorderMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    border_file_out: OutputPathType
    """the output border file"""

def border_merge_params(
    border_file_out: str,
    border: list[BorderMergeBorder],
) -> BorderMerge:
    """
    Build parameters.

    Args:
        border_file_out: the output border file
        border: specify an input border file

    Returns:
        Parameter dictionary.
    """
    params: BorderMerge = {
        "@type": "workbench/border_merge",
        "border_file_out": border_file_out,
        "border": border,
    }
    return params

def border_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("border_file_out", None) is None:
        raise StyxValidationError("`border_file_out` must not be None")
    if not isinstance(params["border_file_out"], str):
        raise StyxValidationError(f'`border_file_out` has the wrong type: Received `{type(params["border_file_out"])}` expected `str`')
    if params.get("border", None) is None:
        raise StyxValidationError("`border` must not be None")
    if not isinstance(params["border"], list):
        raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `list[BorderMergeBorder]`')
    for e in params["border"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("border_file_in", None) is None:
            raise StyxValidationError("`border_file_in` must not be None")
        if not isinstance(e["border_file_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`border_file_in` has the wrong type: Received `{type(e["border_file_in"])}` expected `InputPathType`')
        if e.get("select", None) is None:
            raise StyxValidationError("`select` must not be None")
        if not isinstance(e["select"], list):
            raise StyxValidationError(f'`select` has the wrong type: Received `{type(e["select"])}` expected `list[BorderMergeSelect]`')
        for e_2 in e["select"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("border", None) is None:
                raise StyxValidationError("`border` must not be None")
            if not isinstance(e_2["border"], str):
                raise StyxValidationError(f'`border` has the wrong type: Received `{type(e_2["border"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_border", None) is None:
                    raise StyxValidationError("`last_border` must not be None")
                if not isinstance(e_2["up_to"]["last_border"], str):
                    raise StyxValidationError(f'`last_border` has the wrong type: Received `{type(e_2["up_to"]["last_border"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def border_merge_cargs(params: BorderMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-merge")
    cargs.append(params["border_file_out"])
    for item0 in params["border"]:
        cargs.append("-border")
        cargs.append(execution.input_file(item0["border_file_in"]))
        for item1 in item0["select"]:
            cargs.append("-select")
            cargs.append(item1["border"])
            v_0 = item1.get("up_to")
            if v_0 is not None:
                cargs.append("-up-to")
                cargs.append(v_0["last_border"])
                if v_0.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def border_merge_outputs(params: BorderMerge, execution: Execution) -> BorderMergeOutputs:
    """Build the BorderMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    border_file_out_v: OutputPathType = execution.output_file(params["border_file_out"])
    return BorderMergeOutputs(
        root=root_v,
        border_file_out=border_file_out_v,
    )

def border_merge_execute(params: BorderMerge, runner: Runner | None = None) -> BorderMergeOutputs:
    """
    MERGE BORDER FILES INTO A NEW FILE

    Takes one or more border files and makes a new border file from the borders in them.

Example: wb_command -border-merge out.border -border first.border -select 1 -border second.border

This example would take the first border from first.border, followed by all borders from second.border, and write these to out.border.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_MERGE_METADATA)
    execution.params(params)
    args = border_merge_cargs(params, execution)
    out = border_merge_outputs(params, execution)
    execution.run(args)
    return out

def border_merge(
    border_file_out: str,
    border: list[BorderMergeBorder],
    runner: Runner | None = None,
) -> BorderMergeOutputs:
    """
    MERGE BORDER FILES INTO A NEW FILE

    Takes one or more border files and makes a new border file from the borders in them.

Example: wb_command -border-merge out.border -border first.border -select 1 -border second.border

This example would take the first border from first.border, followed by all borders from second.border, and write these to out.border.

    Args:
        border_file_out: the output border file
        border: specify an input border file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_merge_params(
        border_file_out=border_file_out,
        border=border,
    )
    return border_merge_execute(params, runner)

__all__ = [
    "BorderMerge",
    "BorderMergeOutputs",
    "BORDER_MERGE_METADATA",
    "border_merge_cargs",
    "border_merge_outputs",
    "border_merge_params",
    "border_merge_execute",
    "border_merge_validate",
    "border_merge",
]