# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_MASK_METADATA = Metadata(
    id="metric_mask",
    name="MASK A METRIC FILE",
    package="workbench",
)

MetricMask = typing.TypedDict(
    "MetricMask",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_mask"]],
        "metric": InputPathType,
        "mask": InputPathType,
        "metric_out": str,
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricMaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_mask_params(
    metric: InputPathType,
    mask: InputPathType,
    metric_out: str,
    column: str | None = None,
) -> MetricMask:
    """
    Build parameters.

    Args:
        metric: the input metric
        mask: the mask metric
        metric_out: the output metric
        column: select a single column

    Returns:
        Parameter dictionary.
    """
    params: MetricMask = {
        "@type": "workbench/metric_mask",
        "metric": metric,
        "mask": mask,
        "metric_out": metric_out,
    }
    if column is not None:
        params["column"] = column
    return params

def metric_mask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricMask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def metric_mask_cargs(params: MetricMask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-mask")
    cargs.append(execution.input_file(params["metric"]))
    cargs.append(execution.input_file(params["mask"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("column")
    if v_0 is not None:
        cargs.append("-column")
        cargs.append(v_0)
    return cargs

def metric_mask_outputs(params: MetricMask, execution: Execution) -> MetricMaskOutputs:
    """Build the MetricMaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricMaskOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_mask_execute(params: MetricMask, runner: Runner | None = None) -> MetricMaskOutputs:
    """
    MASK A METRIC FILE

    By default, the output metric is a copy of the input metric, but with zeros wherever the mask metric is zero or negative.  if -column is specified, the output contains only one column, the masked version of the specified input column.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_mask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_MASK_METADATA)
    execution.params(params)
    args = metric_mask_cargs(params, execution)
    out = metric_mask_outputs(params, execution)
    execution.run(args)
    return out

def metric_mask(
    metric: InputPathType,
    mask: InputPathType,
    metric_out: str,
    column: str | None = None,
    runner: Runner | None = None,
) -> MetricMaskOutputs:
    """
    MASK A METRIC FILE

    By default, the output metric is a copy of the input metric, but with zeros wherever the mask metric is zero or negative.  if -column is specified, the output contains only one column, the masked version of the specified input column.

    Args:
        metric: the input metric
        mask: the mask metric
        metric_out: the output metric
        column: select a single column
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_mask_params(
        metric=metric,
        mask=mask,
        metric_out=metric_out,
        column=column,
    )
    return metric_mask_execute(params, runner)

__all__ = [
    "MetricMask",
    "MetricMaskOutputs",
    "METRIC_MASK_METADATA",
    "metric_mask_cargs",
    "metric_mask_outputs",
    "metric_mask_params",
    "metric_mask_execute",
    "metric_mask_validate",
    "metric_mask",
]