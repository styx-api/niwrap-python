# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_ROI_AVERAGE_METADATA = Metadata(
    id="cifti_roi_average",
    name="AVERAGE ROWS IN A SINGLE CIFTI FILE",
    package="workbench",
)

CiftiRoiAverage = typing.TypedDict(
    "CiftiRoiAverage",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_roi_average"]],
        "cifti_in": InputPathType,
        "text_out": str,
        "cifti_roi": typing.NotRequired[InputPathType],
        "left_roi": typing.NotRequired[InputPathType],
        "right_roi": typing.NotRequired[InputPathType],
        "cerebellum_roi": typing.NotRequired[InputPathType],
        "vol_roi": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class CiftiRoiAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cifti_roi_average_params(
    cifti_in: InputPathType,
    text_out: str,
    cifti_roi: InputPathType | None = None,
    left_roi: InputPathType | None = None,
    right_roi: InputPathType | None = None,
    cerebellum_roi: InputPathType | None = None,
    vol_roi: InputPathType | None = None,
) -> CiftiRoiAverage:
    """
    Build parameters.

    Args:
        cifti_in: the cifti file to average rows from
        text_out: output text file of the average values
        cifti_roi: cifti file containing combined rois
        left_roi: vertices to use from left hemisphere
        right_roi: vertices to use from right hemisphere
        cerebellum_roi: vertices to use from cerebellum
        vol_roi: voxels to use

    Returns:
        Parameter dictionary.
    """
    params: CiftiRoiAverage = {
        "@type": "workbench/cifti_roi_average",
        "cifti_in": cifti_in,
        "text_out": text_out,
    }
    if cifti_roi is not None:
        params["cifti_roi"] = cifti_roi
    if left_roi is not None:
        params["left_roi"] = left_roi
    if right_roi is not None:
        params["right_roi"] = right_roi
    if cerebellum_roi is not None:
        params["cerebellum_roi"] = cerebellum_roi
    if vol_roi is not None:
        params["vol_roi"] = vol_roi
    return params

def cifti_roi_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiRoiAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("text_out", None) is None:
        raise StyxValidationError("`text_out` must not be None")
    if not isinstance(params["text_out"], str):
        raise StyxValidationError(f'`text_out` has the wrong type: Received `{type(params["text_out"])}` expected `str`')
    if params.get("cifti_roi", None) is not None:
        if not isinstance(params["cifti_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti_roi` has the wrong type: Received `{type(params["cifti_roi"])}` expected `InputPathType`')
    if params.get("left_roi", None) is not None:
        if not isinstance(params["left_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`left_roi` has the wrong type: Received `{type(params["left_roi"])}` expected `InputPathType`')
    if params.get("right_roi", None) is not None:
        if not isinstance(params["right_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`right_roi` has the wrong type: Received `{type(params["right_roi"])}` expected `InputPathType`')
    if params.get("cerebellum_roi", None) is not None:
        if not isinstance(params["cerebellum_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cerebellum_roi` has the wrong type: Received `{type(params["cerebellum_roi"])}` expected `InputPathType`')
    if params.get("vol_roi", None) is not None:
        if not isinstance(params["vol_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_roi` has the wrong type: Received `{type(params["vol_roi"])}` expected `InputPathType`')

def cifti_roi_average_cargs(params: CiftiRoiAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-roi-average")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["text_out"])
    v_0 = params.get("cifti_roi")
    if v_0 is not None:
        cargs.append("-cifti-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("left_roi")
    if v_1 is not None:
        cargs.append("-left-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("right_roi")
    if v_2 is not None:
        cargs.append("-right-roi")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("cerebellum_roi")
    if v_3 is not None:
        cargs.append("-cerebellum-roi")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("vol_roi")
    if v_4 is not None:
        cargs.append("-vol-roi")
        cargs.append(execution.input_file(v_4))
    return cargs

def cifti_roi_average_outputs(params: CiftiRoiAverage, execution: Execution) -> CiftiRoiAverageOutputs:
    """Build the CiftiRoiAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CiftiRoiAverageOutputs(
        root=root_v,
    )

def cifti_roi_average_execute(params: CiftiRoiAverage, runner: Runner | None = None) -> CiftiRoiAverageOutputs:
    """
    AVERAGE ROWS IN A SINGLE CIFTI FILE

    Average the rows that are within the specified ROIs, and write the resulting average row to a text file, separated by newlines.  If -cifti-roi is specified, -left-roi, -right-roi, -cerebellum-roi, and -vol-roi must not be specified.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_roi_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_ROI_AVERAGE_METADATA)
    execution.params(params)
    args = cifti_roi_average_cargs(params, execution)
    out = cifti_roi_average_outputs(params, execution)
    execution.run(args)
    return out

def cifti_roi_average(
    cifti_in: InputPathType,
    text_out: str,
    cifti_roi: InputPathType | None = None,
    left_roi: InputPathType | None = None,
    right_roi: InputPathType | None = None,
    cerebellum_roi: InputPathType | None = None,
    vol_roi: InputPathType | None = None,
    runner: Runner | None = None,
) -> CiftiRoiAverageOutputs:
    """
    AVERAGE ROWS IN A SINGLE CIFTI FILE

    Average the rows that are within the specified ROIs, and write the resulting average row to a text file, separated by newlines.  If -cifti-roi is specified, -left-roi, -right-roi, -cerebellum-roi, and -vol-roi must not be specified.

    Args:
        cifti_in: the cifti file to average rows from
        text_out: output text file of the average values
        cifti_roi: cifti file containing combined rois
        left_roi: vertices to use from left hemisphere
        right_roi: vertices to use from right hemisphere
        cerebellum_roi: vertices to use from cerebellum
        vol_roi: voxels to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_roi_average_params(
        cifti_in=cifti_in,
        text_out=text_out,
        cifti_roi=cifti_roi,
        left_roi=left_roi,
        right_roi=right_roi,
        cerebellum_roi=cerebellum_roi,
        vol_roi=vol_roi,
    )
    return cifti_roi_average_execute(params, runner)

__all__ = [
    "CiftiRoiAverage",
    "CiftiRoiAverageOutputs",
    "CIFTI_ROI_AVERAGE_METADATA",
    "cifti_roi_average_cargs",
    "cifti_roi_average_outputs",
    "cifti_roi_average_params",
    "cifti_roi_average_execute",
    "cifti_roi_average_validate",
    "cifti_roi_average",
]