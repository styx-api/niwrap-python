# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_SET_COORDINATES_METADATA = Metadata(
    id="surface_set_coordinates",
    name="MODIFY COORDINATES OF A SURFACE",
    package="workbench",
)

SurfaceSetCoordinates = typing.TypedDict(
    "SurfaceSetCoordinates",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_set_coordinates"]],
        "surface_in": InputPathType,
        "coord_metric": InputPathType,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceSetCoordinatesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the new surface"""

def surface_set_coordinates_params(
    surface_in: InputPathType,
    coord_metric: InputPathType,
    surface_out: str,
) -> SurfaceSetCoordinates:
    """
    Build parameters.

    Args:
        surface_in: the surface to use for the topology
        coord_metric: the new coordinates, as a 3-column metric file
        surface_out: the new surface

    Returns:
        Parameter dictionary.
    """
    params: SurfaceSetCoordinates = {
        "@type": "workbench/surface_set_coordinates",
        "surface_in": surface_in,
        "coord_metric": coord_metric,
        "surface_out": surface_out,
    }
    return params

def surface_set_coordinates_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceSetCoordinates."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_in", None) is None:
        raise StyxValidationError("`surface_in` must not be None")
    if not isinstance(params["surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in` has the wrong type: Received `{type(params["surface_in"])}` expected `InputPathType`')
    if params.get("coord_metric", None) is None:
        raise StyxValidationError("`coord_metric` must not be None")
    if not isinstance(params["coord_metric"], (pathlib.Path, str)):
        raise StyxValidationError(f'`coord_metric` has the wrong type: Received `{type(params["coord_metric"])}` expected `InputPathType`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_set_coordinates_cargs(params: SurfaceSetCoordinates, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-set-coordinates")
    cargs.append(execution.input_file(params["surface_in"]))
    cargs.append(execution.input_file(params["coord_metric"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_set_coordinates_outputs(params: SurfaceSetCoordinates, execution: Execution) -> SurfaceSetCoordinatesOutputs:
    """Build the SurfaceSetCoordinatesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceSetCoordinatesOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_set_coordinates_execute(params: SurfaceSetCoordinates, runner: Runner | None = None) -> SurfaceSetCoordinatesOutputs:
    """
    MODIFY COORDINATES OF A SURFACE

    Takes the topology from an existing surface file, and uses values from a metric file as coordinates to construct a new surface file.

See -surface-coordinates-to-metric for how to get surface coordinates as a metric file, such that you can then modify them via metric commands, etc.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_set_coordinates_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_SET_COORDINATES_METADATA)
    execution.params(params)
    args = surface_set_coordinates_cargs(params, execution)
    out = surface_set_coordinates_outputs(params, execution)
    execution.run(args)
    return out

def surface_set_coordinates(
    surface_in: InputPathType,
    coord_metric: InputPathType,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceSetCoordinatesOutputs:
    """
    MODIFY COORDINATES OF A SURFACE

    Takes the topology from an existing surface file, and uses values from a metric file as coordinates to construct a new surface file.

See -surface-coordinates-to-metric for how to get surface coordinates as a metric file, such that you can then modify them via metric commands, etc.

    Args:
        surface_in: the surface to use for the topology
        coord_metric: the new coordinates, as a 3-column metric file
        surface_out: the new surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_set_coordinates_params(
        surface_in=surface_in,
        coord_metric=coord_metric,
        surface_out=surface_out,
    )
    return surface_set_coordinates_execute(params, runner)

__all__ = [
    "SurfaceSetCoordinates",
    "SurfaceSetCoordinatesOutputs",
    "SURFACE_SET_COORDINATES_METADATA",
    "surface_set_coordinates_cargs",
    "surface_set_coordinates_outputs",
    "surface_set_coordinates_params",
    "surface_set_coordinates_execute",
    "surface_set_coordinates_validate",
    "surface_set_coordinates",
]