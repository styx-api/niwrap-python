# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_MASK_METADATA = Metadata(
    id="label_mask",
    name="MASK A LABEL FILE",
    package="workbench",
)

LabelMask = typing.TypedDict(
    "LabelMask",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_mask"]],
        "label": InputPathType,
        "mask": InputPathType,
        "label_out": str,
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LabelMaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label file"""

def label_mask_params(
    label: InputPathType,
    mask: InputPathType,
    label_out: str,
    column: str | None = None,
) -> LabelMask:
    """
    Build parameters.

    Args:
        label: the label file to mask
        mask: the mask metric
        label_out: the output label file
        column: select a single column

    Returns:
        Parameter dictionary.
    """
    params: LabelMask = {
        "@type": "workbench/label_mask",
        "label": label,
        "mask": mask,
        "label_out": label_out,
    }
    if column is not None:
        params["column"] = column
    return params

def label_mask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelMask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def label_mask_cargs(params: LabelMask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-mask")
    cargs.append(execution.input_file(params["label"]))
    cargs.append(execution.input_file(params["mask"]))
    cargs.append(params["label_out"])
    v_0 = params.get("column")
    if v_0 is not None:
        cargs.append("-column")
        cargs.append(v_0)
    return cargs

def label_mask_outputs(params: LabelMask, execution: Execution) -> LabelMaskOutputs:
    """Build the LabelMaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return LabelMaskOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def label_mask_execute(params: LabelMask, runner: Runner | None = None) -> LabelMaskOutputs:
    """
    MASK A LABEL FILE

    By default, the output label is a copy of the input label, but with the 'unused' label wherever the mask metric is zero or negative.  if -column is specified, the output contains only one column, the masked version of the specified input column.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_mask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_MASK_METADATA)
    execution.params(params)
    args = label_mask_cargs(params, execution)
    out = label_mask_outputs(params, execution)
    execution.run(args)
    return out

def label_mask(
    label: InputPathType,
    mask: InputPathType,
    label_out: str,
    column: str | None = None,
    runner: Runner | None = None,
) -> LabelMaskOutputs:
    """
    MASK A LABEL FILE

    By default, the output label is a copy of the input label, but with the 'unused' label wherever the mask metric is zero or negative.  if -column is specified, the output contains only one column, the masked version of the specified input column.

    Args:
        label: the label file to mask
        mask: the mask metric
        label_out: the output label file
        column: select a single column
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_mask_params(
        label=label,
        mask=mask,
        label_out=label_out,
        column=column,
    )
    return label_mask_execute(params, runner)

__all__ = [
    "LabelMask",
    "LabelMaskOutputs",
    "LABEL_MASK_METADATA",
    "label_mask_cargs",
    "label_mask_outputs",
    "label_mask_params",
    "label_mask_execute",
    "label_mask_validate",
    "label_mask",
]