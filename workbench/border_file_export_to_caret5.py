# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_FILE_EXPORT_TO_CARET5_METADATA = Metadata(
    id="border_file_export_to_caret5",
    name="EXPORT BORDER FILE TO CARET5 FILE FORMAT",
    package="workbench",
)

BorderFileExportToCaret5 = typing.TypedDict(
    "BorderFileExportToCaret5",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_file_export_to_caret5"]],
        "border_file": str,
        "output_file_prefix": str,
        "surface": list[InputPathType],
    },
)

@dataclasses.dataclass
class BorderFileExportToCaret5Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def border_file_export_to_caret5_params(
    border_file: str,
    output_file_prefix: str,
    surface: list[InputPathType],
) -> BorderFileExportToCaret5:
    """
    Build parameters.

    Args:
        border_file: workbench border file
        output_file_prefix: prefix for name of output caret5\
            border/borderproj/bordercolor files
        surface: specify an input surface

    Returns:
        Parameter dictionary.
    """
    params: BorderFileExportToCaret5 = {
        "@type": "workbench/border_file_export_to_caret5",
        "border_file": border_file,
        "output_file_prefix": output_file_prefix,
        "surface": surface,
    }
    return params

def border_file_export_to_caret5_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderFileExportToCaret5."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("border_file", None) is None:
        raise StyxValidationError("`border_file` must not be None")
    if not isinstance(params["border_file"], str):
        raise StyxValidationError(f'`border_file` has the wrong type: Received `{type(params["border_file"])}` expected `str`')
    if params.get("output_file_prefix", None) is None:
        raise StyxValidationError("`output_file_prefix` must not be None")
    if not isinstance(params["output_file_prefix"], str):
        raise StyxValidationError(f'`output_file_prefix` has the wrong type: Received `{type(params["output_file_prefix"])}` expected `str`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], list):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `list[InputPathType]`')
    for e in params["surface"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def border_file_export_to_caret5_cargs(params: BorderFileExportToCaret5, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-file-export-to-caret5")
    cargs.append(params["border_file"])
    cargs.append(params["output_file_prefix"])
    for item0 in params["surface"]:
        cargs.append("-surface")
        cargs.append(execution.input_file(item0))
    return cargs

def border_file_export_to_caret5_outputs(params: BorderFileExportToCaret5, execution: Execution) -> BorderFileExportToCaret5Outputs:
    """Build the BorderFileExportToCaret5Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BorderFileExportToCaret5Outputs(
        root=root_v,
    )

def border_file_export_to_caret5_execute(params: BorderFileExportToCaret5, runner: Runner | None = None) -> BorderFileExportToCaret5Outputs:
    """
    EXPORT BORDER FILE TO CARET5 FILE FORMAT

    A Workbench border file may contain borders for multiple structures and borders that are both projected and unprojected.  It also contains a color table for the borders. 

Caret5 has both border (unprojected) and border projection (projected) files.  In addition, each Caret5 border or border projection file typically contains data for a single structure. Caret5 also uses a border color file that associates colors with the names of the borders. 

This command will try to output both Caret5 border and border projection files.  Each output border/border projection file will contains data for one structure so there may be many files created.  The structure name is included in the name of each border or border projection file that is created. 

One Caret5 border color file will also be produced by this command. 

Providing surface(s) as input parameters is optional, but recommended. Surfaces may be needed to create both projected and/or unprojected coordinates of borders.  If there is a failure to produce an output border or border projection due to a missing surface with the matching structure, an error message will be displayed and some output files will not be created. 

When writing new files, this command will overwrite a file with the same name. 

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_file_export_to_caret5_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_FILE_EXPORT_TO_CARET5_METADATA)
    execution.params(params)
    args = border_file_export_to_caret5_cargs(params, execution)
    out = border_file_export_to_caret5_outputs(params, execution)
    execution.run(args)
    return out

def border_file_export_to_caret5(
    border_file: str,
    output_file_prefix: str,
    surface: list[InputPathType],
    runner: Runner | None = None,
) -> BorderFileExportToCaret5Outputs:
    """
    EXPORT BORDER FILE TO CARET5 FILE FORMAT

    A Workbench border file may contain borders for multiple structures and borders that are both projected and unprojected.  It also contains a color table for the borders. 

Caret5 has both border (unprojected) and border projection (projected) files.  In addition, each Caret5 border or border projection file typically contains data for a single structure. Caret5 also uses a border color file that associates colors with the names of the borders. 

This command will try to output both Caret5 border and border projection files.  Each output border/border projection file will contains data for one structure so there may be many files created.  The structure name is included in the name of each border or border projection file that is created. 

One Caret5 border color file will also be produced by this command. 

Providing surface(s) as input parameters is optional, but recommended. Surfaces may be needed to create both projected and/or unprojected coordinates of borders.  If there is a failure to produce an output border or border projection due to a missing surface with the matching structure, an error message will be displayed and some output files will not be created. 

When writing new files, this command will overwrite a file with the same name. 

    Args:
        border_file: workbench border file
        output_file_prefix: prefix for name of output caret5\
            border/borderproj/bordercolor files
        surface: specify an input surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_file_export_to_caret5_params(
        border_file=border_file,
        output_file_prefix=output_file_prefix,
        surface=surface,
    )
    return border_file_export_to_caret5_execute(params, runner)

__all__ = [
    "BorderFileExportToCaret5",
    "BorderFileExportToCaret5Outputs",
    "BORDER_FILE_EXPORT_TO_CARET5_METADATA",
    "border_file_export_to_caret5_cargs",
    "border_file_export_to_caret5_outputs",
    "border_file_export_to_caret5_params",
    "border_file_export_to_caret5_execute",
    "border_file_export_to_caret5_validate",
    "border_file_export_to_caret5",
]