# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_MERGE_METADATA = Metadata(
    id="label_merge",
    name="MERGE LABEL FILES INTO A NEW FILE",
    package="workbench",
)

class LabelMergeUpTo(typing.TypedDict):
    last_column: str
    """the number or name of the last column to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class LabelMergeColumn(typing.TypedDict):
    column: str
    """the column number or name"""
    up_to: typing.NotRequired[LabelMergeUpTo]
    """use an inclusive range of columns"""

class LabelMergeLabel(typing.TypedDict):
    label_in: InputPathType
    """a label file to use columns from"""
    column: list[LabelMergeColumn]
    """select a single column to use"""

LabelMerge = typing.TypedDict(
    "LabelMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_merge"]],
        "label_out": str,
        "label": list[LabelMergeLabel],
    },
)

@dataclasses.dataclass
class LabelMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label"""

def label_merge_params(
    label_out: str,
    label: list[LabelMergeLabel],
) -> LabelMerge:
    """
    Build parameters.

    Args:
        label_out: the output label
        label: specify an input label

    Returns:
        Parameter dictionary.
    """
    params: LabelMerge = {
        "@type": "workbench/label_merge",
        "label_out": label_out,
        "label": label,
    }
    return params

def label_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], list):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `list[LabelMergeLabel]`')
    for e in params["label"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("label_in", None) is None:
            raise StyxValidationError("`label_in` must not be None")
        if not isinstance(e["label_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(e["label_in"])}` expected `InputPathType`')
        if e.get("column", None) is None:
            raise StyxValidationError("`column` must not be None")
        if not isinstance(e["column"], list):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(e["column"])}` expected `list[LabelMergeColumn]`')
        for e_2 in e["column"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("column", None) is None:
                raise StyxValidationError("`column` must not be None")
            if not isinstance(e_2["column"], str):
                raise StyxValidationError(f'`column` has the wrong type: Received `{type(e_2["column"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_column", None) is None:
                    raise StyxValidationError("`last_column` must not be None")
                if not isinstance(e_2["up_to"]["last_column"], str):
                    raise StyxValidationError(f'`last_column` has the wrong type: Received `{type(e_2["up_to"]["last_column"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def label_merge_cargs(params: LabelMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-merge")
    cargs.append(params["label_out"])
    for item0 in params["label"]:
        cargs.append("-label")
        cargs.append(execution.input_file(item0["label_in"]))
        for item1 in item0["column"]:
            cargs.append("-column")
            cargs.append(item1["column"])
            v_0 = item1.get("up_to")
            if v_0 is not None:
                cargs.append("-up-to")
                cargs.append(v_0["last_column"])
                if v_0.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def label_merge_outputs(params: LabelMerge, execution: Execution) -> LabelMergeOutputs:
    """Build the LabelMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return LabelMergeOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def label_merge_execute(params: LabelMerge, runner: Runner | None = None) -> LabelMergeOutputs:
    """
    MERGE LABEL FILES INTO A NEW FILE

    Takes one or more label files and constructs a new label file by concatenating columns from them.  The input files must have the same number of vertices and the same structure.

Example: wb_command -label-merge out.label.gii -label first.label.gii -column 1 -label second.label.gii

This example would take the first column from first.label.gii and all subvolumes from second.label.gii, and write these to out.label.gii.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_MERGE_METADATA)
    execution.params(params)
    args = label_merge_cargs(params, execution)
    out = label_merge_outputs(params, execution)
    execution.run(args)
    return out

def label_merge(
    label_out: str,
    label: list[LabelMergeLabel],
    runner: Runner | None = None,
) -> LabelMergeOutputs:
    """
    MERGE LABEL FILES INTO A NEW FILE

    Takes one or more label files and constructs a new label file by concatenating columns from them.  The input files must have the same number of vertices and the same structure.

Example: wb_command -label-merge out.label.gii -label first.label.gii -column 1 -label second.label.gii

This example would take the first column from first.label.gii and all subvolumes from second.label.gii, and write these to out.label.gii.

    Args:
        label_out: the output label
        label: specify an input label
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_merge_params(
        label_out=label_out,
        label=label,
    )
    return label_merge_execute(params, runner)

__all__ = [
    "LabelMerge",
    "LabelMergeOutputs",
    "LABEL_MERGE_METADATA",
    "label_merge_cargs",
    "label_merge_outputs",
    "label_merge_params",
    "label_merge_execute",
    "label_merge_validate",
    "label_merge",
]