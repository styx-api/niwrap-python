# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_APPLY_AFFINE_METADATA = Metadata(
    id="surface_apply_affine",
    name="APPLY AFFINE TRANSFORM TO SURFACE FILE",
    package="workbench",
)

class SurfaceApplyAffineFlirt(typing.TypedDict):
    source_volume: str
    """the source volume used when generating the affine"""
    target_volume: str
    """the target volume used when generating the affine"""

SurfaceApplyAffine = typing.TypedDict(
    "SurfaceApplyAffine",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_apply_affine"]],
        "in_surf": InputPathType,
        "affine": str,
        "out_surf": str,
        "flirt": typing.NotRequired[SurfaceApplyAffineFlirt],
    },
)

@dataclasses.dataclass
class SurfaceApplyAffineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_surf: OutputPathType
    """the output transformed surface"""

def surface_apply_affine_params(
    in_surf: InputPathType,
    affine: str,
    out_surf: str,
    flirt: SurfaceApplyAffineFlirt | None = None,
) -> SurfaceApplyAffine:
    """
    Build parameters.

    Args:
        in_surf: the surface to transform
        affine: the affine file
        out_surf: the output transformed surface
        flirt: MUST be used if affine is a flirt affine

    Returns:
        Parameter dictionary.
    """
    params: SurfaceApplyAffine = {
        "@type": "workbench/surface_apply_affine",
        "in_surf": in_surf,
        "affine": affine,
        "out_surf": out_surf,
    }
    if flirt is not None:
        params["flirt"] = flirt
    return params

def surface_apply_affine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceApplyAffine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_surf", None) is None:
        raise StyxValidationError("`in_surf` must not be None")
    if not isinstance(params["in_surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_surf` has the wrong type: Received `{type(params["in_surf"])}` expected `InputPathType`')
    if params.get("affine", None) is None:
        raise StyxValidationError("`affine` must not be None")
    if not isinstance(params["affine"], str):
        raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `str`')
    if params.get("out_surf", None) is None:
        raise StyxValidationError("`out_surf` must not be None")
    if not isinstance(params["out_surf"], str):
        raise StyxValidationError(f'`out_surf` has the wrong type: Received `{type(params["out_surf"])}` expected `str`')
    if params.get("flirt", None) is not None:
        if not isinstance(params["flirt"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["flirt"])}\'')
        if params["flirt"].get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(params["flirt"]["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["flirt"]["source_volume"])}` expected `str`')
        if params["flirt"].get("target_volume", None) is None:
            raise StyxValidationError("`target_volume` must not be None")
        if not isinstance(params["flirt"]["target_volume"], str):
            raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["flirt"]["target_volume"])}` expected `str`')

def surface_apply_affine_cargs(params: SurfaceApplyAffine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-apply-affine")
    cargs.append(execution.input_file(params["in_surf"]))
    cargs.append(params["affine"])
    cargs.append(params["out_surf"])
    v_0 = params.get("flirt")
    if v_0 is not None:
        cargs.append("-flirt")
        cargs.append(v_0["source_volume"])
        cargs.append(v_0["target_volume"])
    return cargs

def surface_apply_affine_outputs(params: SurfaceApplyAffine, execution: Execution) -> SurfaceApplyAffineOutputs:
    """Build the SurfaceApplyAffineOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_surf_v: OutputPathType = execution.output_file(params["out_surf"])
    return SurfaceApplyAffineOutputs(
        root=root_v,
        out_surf=out_surf_v,
    )

def surface_apply_affine_execute(params: SurfaceApplyAffine, runner: Runner | None = None) -> SurfaceApplyAffineOutputs:
    """
    APPLY AFFINE TRANSFORM TO SURFACE FILE

    For flirt matrices, you must use the -flirt option, because flirt matrices are not a complete description of the coordinate transform they represent.  If the -flirt option is not present, the affine must be a nifti 'world' affine, which can be obtained with the -convert-affine command, or aff_conv from the 4dfp suite.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_apply_affine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_APPLY_AFFINE_METADATA)
    execution.params(params)
    args = surface_apply_affine_cargs(params, execution)
    out = surface_apply_affine_outputs(params, execution)
    execution.run(args)
    return out

def surface_apply_affine(
    in_surf: InputPathType,
    affine: str,
    out_surf: str,
    flirt: SurfaceApplyAffineFlirt | None = None,
    runner: Runner | None = None,
) -> SurfaceApplyAffineOutputs:
    """
    APPLY AFFINE TRANSFORM TO SURFACE FILE

    For flirt matrices, you must use the -flirt option, because flirt matrices are not a complete description of the coordinate transform they represent.  If the -flirt option is not present, the affine must be a nifti 'world' affine, which can be obtained with the -convert-affine command, or aff_conv from the 4dfp suite.

    Args:
        in_surf: the surface to transform
        affine: the affine file
        out_surf: the output transformed surface
        flirt: MUST be used if affine is a flirt affine
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_apply_affine_params(
        in_surf=in_surf,
        affine=affine,
        out_surf=out_surf,
        flirt=flirt,
    )
    return surface_apply_affine_execute(params, runner)

__all__ = [
    "SurfaceApplyAffine",
    "SurfaceApplyAffineOutputs",
    "SURFACE_APPLY_AFFINE_METADATA",
    "surface_apply_affine_cargs",
    "surface_apply_affine_outputs",
    "surface_apply_affine_params",
    "surface_apply_affine_execute",
    "surface_apply_affine_validate",
    "surface_apply_affine",
]