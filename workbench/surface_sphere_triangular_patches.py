# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_SPHERE_TRIANGULAR_PATCHES_METADATA = Metadata(
    id="surface_sphere_triangular_patches",
    name="DIVIDE STANDARD SPHERE INTO PATCHES",
    package="workbench",
)

SurfaceSphereTriangularPatches = typing.TypedDict(
    "SurfaceSphereTriangularPatches",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_sphere_triangular_patches"]],
        "sphere": InputPathType,
        "divisions": int,
        "text_out": str,
    },
)

@dataclasses.dataclass
class SurfaceSphereTriangularPatchesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_sphere_triangular_patches_params(
    sphere: InputPathType,
    divisions: int,
    text_out: str,
) -> SurfaceSphereTriangularPatches:
    """
    Build parameters.

    Args:
        sphere: an undistorted, regularly divided icosahedral sphere
        divisions: how many pieces to divide each icosahedral edge into, must\
            divide perfectly into the given sphere
        text_out: output - text file for the vertex numbers of the patches

    Returns:
        Parameter dictionary.
    """
    params: SurfaceSphereTriangularPatches = {
        "@type": "workbench/surface_sphere_triangular_patches",
        "sphere": sphere,
        "divisions": divisions,
        "text_out": text_out,
    }
    return params

def surface_sphere_triangular_patches_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceSphereTriangularPatches."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sphere", None) is None:
        raise StyxValidationError("`sphere` must not be None")
    if not isinstance(params["sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere` has the wrong type: Received `{type(params["sphere"])}` expected `InputPathType`')
    if params.get("divisions", None) is None:
        raise StyxValidationError("`divisions` must not be None")
    if not isinstance(params["divisions"], int):
        raise StyxValidationError(f'`divisions` has the wrong type: Received `{type(params["divisions"])}` expected `int`')
    if params.get("text_out", None) is None:
        raise StyxValidationError("`text_out` must not be None")
    if not isinstance(params["text_out"], str):
        raise StyxValidationError(f'`text_out` has the wrong type: Received `{type(params["text_out"])}` expected `str`')

def surface_sphere_triangular_patches_cargs(params: SurfaceSphereTriangularPatches, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-sphere-triangular-patches")
    cargs.append(execution.input_file(params["sphere"]))
    cargs.append(str(params["divisions"]))
    cargs.append(params["text_out"])
    return cargs

def surface_sphere_triangular_patches_outputs(params: SurfaceSphereTriangularPatches, execution: Execution) -> SurfaceSphereTriangularPatchesOutputs:
    """Build the SurfaceSphereTriangularPatchesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceSphereTriangularPatchesOutputs(
        root=root_v,
    )

def surface_sphere_triangular_patches_execute(params: SurfaceSphereTriangularPatches, runner: Runner | None = None) -> SurfaceSphereTriangularPatchesOutputs:
    """
    DIVIDE STANDARD SPHERE INTO PATCHES

    Divide the given undistorted sphere into equally-sized triangular patches.  Patches overlap by a border of 1 vertex.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_sphere_triangular_patches_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_SPHERE_TRIANGULAR_PATCHES_METADATA)
    execution.params(params)
    args = surface_sphere_triangular_patches_cargs(params, execution)
    out = surface_sphere_triangular_patches_outputs(params, execution)
    execution.run(args)
    return out

def surface_sphere_triangular_patches(
    sphere: InputPathType,
    divisions: int,
    text_out: str,
    runner: Runner | None = None,
) -> SurfaceSphereTriangularPatchesOutputs:
    """
    DIVIDE STANDARD SPHERE INTO PATCHES

    Divide the given undistorted sphere into equally-sized triangular patches.  Patches overlap by a border of 1 vertex.

    Args:
        sphere: an undistorted, regularly divided icosahedral sphere
        divisions: how many pieces to divide each icosahedral edge into, must\
            divide perfectly into the given sphere
        text_out: output - text file for the vertex numbers of the patches
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_sphere_triangular_patches_params(
        sphere=sphere,
        divisions=divisions,
        text_out=text_out,
    )
    return surface_sphere_triangular_patches_execute(params, runner)

__all__ = [
    "SurfaceSphereTriangularPatches",
    "SurfaceSphereTriangularPatchesOutputs",
    "SURFACE_SPHERE_TRIANGULAR_PATCHES_METADATA",
    "surface_sphere_triangular_patches_cargs",
    "surface_sphere_triangular_patches_outputs",
    "surface_sphere_triangular_patches_params",
    "surface_sphere_triangular_patches_execute",
    "surface_sphere_triangular_patches_validate",
    "surface_sphere_triangular_patches",
]