# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_VECTOR_OPERATION_METADATA = Metadata(
    id="metric_vector_operation",
    name="DO A VECTOR OPERATION ON METRIC FILES",
    package="workbench",
)

MetricVectorOperation = typing.TypedDict(
    "MetricVectorOperation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_vector_operation"]],
        "vectors_a": InputPathType,
        "vectors_b": InputPathType,
        "operation": str,
        "metric_out": str,
        "normalize_a": typing.NotRequired[bool],
        "normalize_b": typing.NotRequired[bool],
        "normalize_output": typing.NotRequired[bool],
        "magnitude": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MetricVectorOperationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output file"""

def metric_vector_operation_params(
    vectors_a: InputPathType,
    vectors_b: InputPathType,
    operation: str,
    metric_out: str,
    normalize_a: bool = False,
    normalize_b: bool = False,
    normalize_output: bool = False,
    magnitude: bool = False,
) -> MetricVectorOperation:
    """
    Build parameters.

    Args:
        vectors_a: first vector input file
        vectors_b: second vector input file
        operation: what vector operation to do
        metric_out: the output file
        normalize_a: normalize vectors of first input
        normalize_b: normalize vectors of second input
        normalize_output: normalize output vectors (not valid for dot product)
        magnitude: output the magnitude of the result (not valid for dot product)

    Returns:
        Parameter dictionary.
    """
    params: MetricVectorOperation = {
        "@type": "workbench/metric_vector_operation",
        "vectors_a": vectors_a,
        "vectors_b": vectors_b,
        "operation": operation,
        "metric_out": metric_out,
        "normalize_a": normalize_a,
        "normalize_b": normalize_b,
        "normalize_output": normalize_output,
        "magnitude": magnitude,
    }
    return params

def metric_vector_operation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricVectorOperation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vectors_a", None) is None:
        raise StyxValidationError("`vectors_a` must not be None")
    if not isinstance(params["vectors_a"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vectors_a` has the wrong type: Received `{type(params["vectors_a"])}` expected `InputPathType`')
    if params.get("vectors_b", None) is None:
        raise StyxValidationError("`vectors_b` must not be None")
    if not isinstance(params["vectors_b"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vectors_b` has the wrong type: Received `{type(params["vectors_b"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("normalize_a", None) is not None:
        if not isinstance(params["normalize_a"], bool):
            raise StyxValidationError(f'`normalize_a` has the wrong type: Received `{type(params["normalize_a"])}` expected `bool`')
    if params.get("normalize_b", None) is not None:
        if not isinstance(params["normalize_b"], bool):
            raise StyxValidationError(f'`normalize_b` has the wrong type: Received `{type(params["normalize_b"])}` expected `bool`')
    if params.get("normalize_output", None) is not None:
        if not isinstance(params["normalize_output"], bool):
            raise StyxValidationError(f'`normalize_output` has the wrong type: Received `{type(params["normalize_output"])}` expected `bool`')
    if params.get("magnitude", None) is not None:
        if not isinstance(params["magnitude"], bool):
            raise StyxValidationError(f'`magnitude` has the wrong type: Received `{type(params["magnitude"])}` expected `bool`')

def metric_vector_operation_cargs(params: MetricVectorOperation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-vector-operation")
    cargs.append(execution.input_file(params["vectors_a"]))
    cargs.append(execution.input_file(params["vectors_b"]))
    cargs.append(params["operation"])
    cargs.append(params["metric_out"])
    if params.get("normalize_a"):
        cargs.append("-normalize-a")
    if params.get("normalize_b"):
        cargs.append("-normalize-b")
    if params.get("normalize_output"):
        cargs.append("-normalize-output")
    if params.get("magnitude"):
        cargs.append("-magnitude")
    return cargs

def metric_vector_operation_outputs(params: MetricVectorOperation, execution: Execution) -> MetricVectorOperationOutputs:
    """Build the MetricVectorOperationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricVectorOperationOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_vector_operation_execute(params: MetricVectorOperation, runner: Runner | None = None) -> MetricVectorOperationOutputs:
    """
    DO A VECTOR OPERATION ON METRIC FILES

    Does a vector operation on two metric files (that must have a multiple of 3 columns).  Either of the inputs may have multiple vectors (more than 3 columns), but not both (at least one must have exactly 3 columns).  The -magnitude and -normalize-output options may not be specified together, or with an operation that returns a scalar (dot product).  The <operation> parameter must be one of the following:

DOT
CROSS
ADD
SUBTRACT

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_vector_operation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_VECTOR_OPERATION_METADATA)
    execution.params(params)
    args = metric_vector_operation_cargs(params, execution)
    out = metric_vector_operation_outputs(params, execution)
    execution.run(args)
    return out

def metric_vector_operation(
    vectors_a: InputPathType,
    vectors_b: InputPathType,
    operation: str,
    metric_out: str,
    normalize_a: bool = False,
    normalize_b: bool = False,
    normalize_output: bool = False,
    magnitude: bool = False,
    runner: Runner | None = None,
) -> MetricVectorOperationOutputs:
    """
    DO A VECTOR OPERATION ON METRIC FILES

    Does a vector operation on two metric files (that must have a multiple of 3 columns).  Either of the inputs may have multiple vectors (more than 3 columns), but not both (at least one must have exactly 3 columns).  The -magnitude and -normalize-output options may not be specified together, or with an operation that returns a scalar (dot product).  The <operation> parameter must be one of the following:

DOT
CROSS
ADD
SUBTRACT

    Args:
        vectors_a: first vector input file
        vectors_b: second vector input file
        operation: what vector operation to do
        metric_out: the output file
        normalize_a: normalize vectors of first input
        normalize_b: normalize vectors of second input
        normalize_output: normalize output vectors (not valid for dot product)
        magnitude: output the magnitude of the result (not valid for dot product)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_vector_operation_params(
        vectors_a=vectors_a,
        vectors_b=vectors_b,
        operation=operation,
        metric_out=metric_out,
        normalize_a=normalize_a,
        normalize_b=normalize_b,
        normalize_output=normalize_output,
        magnitude=magnitude,
    )
    return metric_vector_operation_execute(params, runner)

__all__ = [
    "MetricVectorOperation",
    "MetricVectorOperationOutputs",
    "METRIC_VECTOR_OPERATION_METADATA",
    "metric_vector_operation_cargs",
    "metric_vector_operation_outputs",
    "metric_vector_operation_params",
    "metric_vector_operation_execute",
    "metric_vector_operation_validate",
    "metric_vector_operation",
]