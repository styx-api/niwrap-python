# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FOCI_LIST_COORDS_METADATA = Metadata(
    id="foci_list_coords",
    name="OUTPUT FOCI COORDINATES IN A TEXT FILE",
    package="workbench",
)

FociListCoords = typing.TypedDict(
    "FociListCoords",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/foci_list_coords"]],
        "foci_file": InputPathType,
        "coord_file_out": str,
        "names_out": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FociListCoordsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def foci_list_coords_params(
    foci_file: InputPathType,
    coord_file_out: str,
    names_out: str | None = None,
) -> FociListCoords:
    """
    Build parameters.

    Args:
        foci_file: input foci file
        coord_file_out: output - the output coordinate text file
        names_out: output the foci names

    Returns:
        Parameter dictionary.
    """
    params: FociListCoords = {
        "@type": "workbench/foci_list_coords",
        "foci_file": foci_file,
        "coord_file_out": coord_file_out,
    }
    if names_out is not None:
        params["names_out"] = names_out
    return params

def foci_list_coords_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FociListCoords."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("foci_file", None) is None:
        raise StyxValidationError("`foci_file` must not be None")
    if not isinstance(params["foci_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`foci_file` has the wrong type: Received `{type(params["foci_file"])}` expected `InputPathType`')
    if params.get("coord_file_out", None) is None:
        raise StyxValidationError("`coord_file_out` must not be None")
    if not isinstance(params["coord_file_out"], str):
        raise StyxValidationError(f'`coord_file_out` has the wrong type: Received `{type(params["coord_file_out"])}` expected `str`')
    if params.get("names_out", None) is not None:
        if not isinstance(params["names_out"], str):
            raise StyxValidationError(f'`names_out` has the wrong type: Received `{type(params["names_out"])}` expected `str`')

def foci_list_coords_cargs(params: FociListCoords, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-foci-list-coords")
    cargs.append(execution.input_file(params["foci_file"]))
    cargs.append(params["coord_file_out"])
    v_0 = params.get("names_out")
    if v_0 is not None:
        cargs.append("-names-out")
        cargs.append(v_0)
    return cargs

def foci_list_coords_outputs(params: FociListCoords, execution: Execution) -> FociListCoordsOutputs:
    """Build the FociListCoordsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FociListCoordsOutputs(
        root=root_v,
    )

def foci_list_coords_execute(params: FociListCoords, runner: Runner | None = None) -> FociListCoordsOutputs:
    """
    OUTPUT FOCI COORDINATES IN A TEXT FILE

    Output the coordinates for every focus in the foci file, and optionally the focus names in a second text file.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    foci_list_coords_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FOCI_LIST_COORDS_METADATA)
    execution.params(params)
    args = foci_list_coords_cargs(params, execution)
    out = foci_list_coords_outputs(params, execution)
    execution.run(args)
    return out

def foci_list_coords(
    foci_file: InputPathType,
    coord_file_out: str,
    names_out: str | None = None,
    runner: Runner | None = None,
) -> FociListCoordsOutputs:
    """
    OUTPUT FOCI COORDINATES IN A TEXT FILE

    Output the coordinates for every focus in the foci file, and optionally the focus names in a second text file.

    Args:
        foci_file: input foci file
        coord_file_out: output - the output coordinate text file
        names_out: output the foci names
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = foci_list_coords_params(
        foci_file=foci_file,
        coord_file_out=coord_file_out,
        names_out=names_out,
    )
    return foci_list_coords_execute(params, runner)

__all__ = [
    "FociListCoords",
    "FociListCoordsOutputs",
    "FOCI_LIST_COORDS_METADATA",
    "foci_list_coords_cargs",
    "foci_list_coords_outputs",
    "foci_list_coords_params",
    "foci_list_coords_execute",
    "foci_list_coords_validate",
    "foci_list_coords",
]