# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_ALL_LABELS_TO_ROIS_METADATA = Metadata(
    id="cifti_all_labels_to_rois",
    name="MAKE ROIS FROM ALL LABELS IN A CIFTI LABEL MAP",
    package="workbench",
)

CiftiAllLabelsToRois = typing.TypedDict(
    "CiftiAllLabelsToRois",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_all_labels_to_rois"]],
        "label_in": InputPathType,
        "map": str,
        "cifti_out": str,
    },
)

@dataclasses.dataclass
class CiftiAllLabelsToRoisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_all_labels_to_rois_params(
    label_in: InputPathType,
    map: str,
    cifti_out: str,
) -> CiftiAllLabelsToRois:
    """
    Build parameters.

    Args:
        label_in: the input cifti label file
        map: the number or name of the label map to use
        cifti_out: the output cifti file

    Returns:
        Parameter dictionary.
    """
    params: CiftiAllLabelsToRois = {
        "@type": "workbench/cifti_all_labels_to_rois",
        "label_in": label_in,
        "map": map,
        "cifti_out": cifti_out,
    }
    return params

def cifti_all_labels_to_rois_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiAllLabelsToRois."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')

def cifti_all_labels_to_rois_cargs(params: CiftiAllLabelsToRois, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-all-labels-to-rois")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["map"])
    cargs.append(params["cifti_out"])
    return cargs

def cifti_all_labels_to_rois_outputs(params: CiftiAllLabelsToRois, execution: Execution) -> CiftiAllLabelsToRoisOutputs:
    """Build the CiftiAllLabelsToRoisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiAllLabelsToRoisOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_all_labels_to_rois_execute(params: CiftiAllLabelsToRois, runner: Runner | None = None) -> CiftiAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A CIFTI LABEL MAP

    The output cifti file is a dscalar file with a column (map) for each label in the specified input map, other than the ??? label, each of which contains a binary ROI of all brainordinates that are set to the corresponding label.

Most of the time, specifying '1' for the <map> argument will do what is desired.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_all_labels_to_rois_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_ALL_LABELS_TO_ROIS_METADATA)
    execution.params(params)
    args = cifti_all_labels_to_rois_cargs(params, execution)
    out = cifti_all_labels_to_rois_outputs(params, execution)
    execution.run(args)
    return out

def cifti_all_labels_to_rois(
    label_in: InputPathType,
    map: str,
    cifti_out: str,
    runner: Runner | None = None,
) -> CiftiAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A CIFTI LABEL MAP

    The output cifti file is a dscalar file with a column (map) for each label in the specified input map, other than the ??? label, each of which contains a binary ROI of all brainordinates that are set to the corresponding label.

Most of the time, specifying '1' for the <map> argument will do what is desired.

    Args:
        label_in: the input cifti label file
        map: the number or name of the label map to use
        cifti_out: the output cifti file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_all_labels_to_rois_params(
        label_in=label_in,
        map=map,
        cifti_out=cifti_out,
    )
    return cifti_all_labels_to_rois_execute(params, runner)

__all__ = [
    "CiftiAllLabelsToRois",
    "CiftiAllLabelsToRoisOutputs",
    "CIFTI_ALL_LABELS_TO_ROIS_METADATA",
    "cifti_all_labels_to_rois_cargs",
    "cifti_all_labels_to_rois_outputs",
    "cifti_all_labels_to_rois_params",
    "cifti_all_labels_to_rois_execute",
    "cifti_all_labels_to_rois_validate",
    "cifti_all_labels_to_rois",
]