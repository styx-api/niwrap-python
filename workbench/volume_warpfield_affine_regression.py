# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_WARPFIELD_AFFINE_REGRESSION_METADATA = Metadata(
    id="volume_warpfield_affine_regression",
    name="REGRESS AFFINE FROM WARPFIELD",
    package="workbench",
)

class VolumeWarpfieldAffineRegressionFlirtOut(typing.TypedDict):
    source_volume: str
    """the volume you want to apply the transform to"""
    target_volume: str
    """the target space you want the transformed volume to match"""

VolumeWarpfieldAffineRegression = typing.TypedDict(
    "VolumeWarpfieldAffineRegression",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_warpfield_affine_regression"]],
        "warpfield": str,
        "affine_out": str,
        "roi": typing.NotRequired[InputPathType],
        "fnirt": typing.NotRequired[str],
        "flirt_out": typing.NotRequired[VolumeWarpfieldAffineRegressionFlirtOut],
    },
)

@dataclasses.dataclass
class VolumeWarpfieldAffineRegressionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def volume_warpfield_affine_regression_params(
    warpfield: str,
    affine_out: str,
    roi: InputPathType | None = None,
    fnirt: str | None = None,
    flirt_out: VolumeWarpfieldAffineRegressionFlirtOut | None = None,
) -> VolumeWarpfieldAffineRegression:
    """
    Build parameters.

    Args:
        warpfield: the input warpfield
        affine_out: output - the output affine file
        roi: only consider voxels within a mask (e.g., a brain mask)
        fnirt: input is a fnirt warpfield
        flirt_out: write output as a flirt matrix rather than a world coordinate\
            transform

    Returns:
        Parameter dictionary.
    """
    params: VolumeWarpfieldAffineRegression = {
        "@type": "workbench/volume_warpfield_affine_regression",
        "warpfield": warpfield,
        "affine_out": affine_out,
    }
    if roi is not None:
        params["roi"] = roi
    if fnirt is not None:
        params["fnirt"] = fnirt
    if flirt_out is not None:
        params["flirt_out"] = flirt_out
    return params

def volume_warpfield_affine_regression_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeWarpfieldAffineRegression."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("warpfield", None) is None:
        raise StyxValidationError("`warpfield` must not be None")
    if not isinstance(params["warpfield"], str):
        raise StyxValidationError(f'`warpfield` has the wrong type: Received `{type(params["warpfield"])}` expected `str`')
    if params.get("affine_out", None) is None:
        raise StyxValidationError("`affine_out` must not be None")
    if not isinstance(params["affine_out"], str):
        raise StyxValidationError(f'`affine_out` has the wrong type: Received `{type(params["affine_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("fnirt", None) is not None:
        if not isinstance(params["fnirt"], str):
            raise StyxValidationError(f'`fnirt` has the wrong type: Received `{type(params["fnirt"])}` expected `str`')
    if params.get("flirt_out", None) is not None:
        if not isinstance(params["flirt_out"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["flirt_out"])}\'')
        if params["flirt_out"].get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(params["flirt_out"]["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["flirt_out"]["source_volume"])}` expected `str`')
        if params["flirt_out"].get("target_volume", None) is None:
            raise StyxValidationError("`target_volume` must not be None")
        if not isinstance(params["flirt_out"]["target_volume"], str):
            raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["flirt_out"]["target_volume"])}` expected `str`')

def volume_warpfield_affine_regression_cargs(params: VolumeWarpfieldAffineRegression, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-warpfield-affine-regression")
    cargs.append(params["warpfield"])
    cargs.append(params["affine_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("fnirt")
    if v_1 is not None:
        cargs.append("-fnirt")
        cargs.append(v_1)
    v_2 = params.get("flirt_out")
    if v_2 is not None:
        cargs.append("-flirt-out")
        cargs.append(v_2["source_volume"])
        cargs.append(v_2["target_volume"])
    return cargs

def volume_warpfield_affine_regression_outputs(params: VolumeWarpfieldAffineRegression, execution: Execution) -> VolumeWarpfieldAffineRegressionOutputs:
    """Build the VolumeWarpfieldAffineRegressionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolumeWarpfieldAffineRegressionOutputs(
        root=root_v,
    )

def volume_warpfield_affine_regression_execute(params: VolumeWarpfieldAffineRegression, runner: Runner | None = None) -> VolumeWarpfieldAffineRegressionOutputs:
    """
    REGRESS AFFINE FROM WARPFIELD

    For all voxels in the warpfield, do a regression that predicts the post-warp coordinate from the source coordinate.  When -roi is specified, only consider voxels with a value greater than 0 in <roi-vol>.

The default is to expect the warpfield to be in relative world coordinates (mm space), and to write the output as a world affine (mm space to mm space).  If you are using FSL-created files and utilities, specify -fnirt and -flirt-out as needed, as their coordinate conventions are different.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_warpfield_affine_regression_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_WARPFIELD_AFFINE_REGRESSION_METADATA)
    execution.params(params)
    args = volume_warpfield_affine_regression_cargs(params, execution)
    out = volume_warpfield_affine_regression_outputs(params, execution)
    execution.run(args)
    return out

def volume_warpfield_affine_regression(
    warpfield: str,
    affine_out: str,
    roi: InputPathType | None = None,
    fnirt: str | None = None,
    flirt_out: VolumeWarpfieldAffineRegressionFlirtOut | None = None,
    runner: Runner | None = None,
) -> VolumeWarpfieldAffineRegressionOutputs:
    """
    REGRESS AFFINE FROM WARPFIELD

    For all voxels in the warpfield, do a regression that predicts the post-warp coordinate from the source coordinate.  When -roi is specified, only consider voxels with a value greater than 0 in <roi-vol>.

The default is to expect the warpfield to be in relative world coordinates (mm space), and to write the output as a world affine (mm space to mm space).  If you are using FSL-created files and utilities, specify -fnirt and -flirt-out as needed, as their coordinate conventions are different.

    Args:
        warpfield: the input warpfield
        affine_out: output - the output affine file
        roi: only consider voxels within a mask (e.g., a brain mask)
        fnirt: input is a fnirt warpfield
        flirt_out: write output as a flirt matrix rather than a world coordinate\
            transform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_warpfield_affine_regression_params(
        warpfield=warpfield,
        affine_out=affine_out,
        roi=roi,
        fnirt=fnirt,
        flirt_out=flirt_out,
    )
    return volume_warpfield_affine_regression_execute(params, runner)

__all__ = [
    "VolumeWarpfieldAffineRegression",
    "VolumeWarpfieldAffineRegressionOutputs",
    "VOLUME_WARPFIELD_AFFINE_REGRESSION_METADATA",
    "volume_warpfield_affine_regression_cargs",
    "volume_warpfield_affine_regression_outputs",
    "volume_warpfield_affine_regression_params",
    "volume_warpfield_affine_regression_execute",
    "volume_warpfield_affine_regression_validate",
    "volume_warpfield_affine_regression",
]