# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_REMOVE_ISLANDS_METADATA = Metadata(
    id="volume_remove_islands",
    name="REMOVE ISLANDS FROM AN ROI VOLUME",
    package="workbench",
)

VolumeRemoveIslands = typing.TypedDict(
    "VolumeRemoveIslands",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_remove_islands"]],
        "volume_in": InputPathType,
        "volume_out": str,
    },
)

@dataclasses.dataclass
class VolumeRemoveIslandsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output ROI volume"""

def volume_remove_islands_params(
    volume_in: InputPathType,
    volume_out: str,
) -> VolumeRemoveIslands:
    """
    Build parameters.

    Args:
        volume_in: the input ROI volume
        volume_out: the output ROI volume

    Returns:
        Parameter dictionary.
    """
    params: VolumeRemoveIslands = {
        "@type": "workbench/volume_remove_islands",
        "volume_in": volume_in,
        "volume_out": volume_out,
    }
    return params

def volume_remove_islands_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeRemoveIslands."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')

def volume_remove_islands_cargs(params: VolumeRemoveIslands, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-remove-islands")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["volume_out"])
    return cargs

def volume_remove_islands_outputs(params: VolumeRemoveIslands, execution: Execution) -> VolumeRemoveIslandsOutputs:
    """Build the VolumeRemoveIslandsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeRemoveIslandsOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_remove_islands_execute(params: VolumeRemoveIslands, runner: Runner | None = None) -> VolumeRemoveIslandsOutputs:
    """
    REMOVE ISLANDS FROM AN ROI VOLUME

    Finds all face-connected parts of the ROI, and zeros out all but the largest one.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_remove_islands_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_REMOVE_ISLANDS_METADATA)
    execution.params(params)
    args = volume_remove_islands_cargs(params, execution)
    out = volume_remove_islands_outputs(params, execution)
    execution.run(args)
    return out

def volume_remove_islands(
    volume_in: InputPathType,
    volume_out: str,
    runner: Runner | None = None,
) -> VolumeRemoveIslandsOutputs:
    """
    REMOVE ISLANDS FROM AN ROI VOLUME

    Finds all face-connected parts of the ROI, and zeros out all but the largest one.

    Args:
        volume_in: the input ROI volume
        volume_out: the output ROI volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_remove_islands_params(
        volume_in=volume_in,
        volume_out=volume_out,
    )
    return volume_remove_islands_execute(params, runner)

__all__ = [
    "VolumeRemoveIslands",
    "VolumeRemoveIslandsOutputs",
    "VOLUME_REMOVE_ISLANDS_METADATA",
    "volume_remove_islands_cargs",
    "volume_remove_islands_outputs",
    "volume_remove_islands_params",
    "volume_remove_islands_execute",
    "volume_remove_islands_validate",
    "volume_remove_islands",
]