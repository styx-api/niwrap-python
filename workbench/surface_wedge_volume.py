# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_WEDGE_VOLUME_METADATA = Metadata(
    id="surface_wedge_volume",
    name="MEASURE PER-VERTEX VOLUME BETWEEN SURFACES",
    package="workbench",
)

SurfaceWedgeVolume = typing.TypedDict(
    "SurfaceWedgeVolume",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_wedge_volume"]],
        "inner_surface": InputPathType,
        "outer_surface": InputPathType,
        "metric": str,
    },
)

@dataclasses.dataclass
class SurfaceWedgeVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric: OutputPathType
    """the output metric"""

def surface_wedge_volume_params(
    inner_surface: InputPathType,
    outer_surface: InputPathType,
    metric: str,
) -> SurfaceWedgeVolume:
    """
    Build parameters.

    Args:
        inner_surface: the inner surface
        outer_surface: the outer surface
        metric: the output metric

    Returns:
        Parameter dictionary.
    """
    params: SurfaceWedgeVolume = {
        "@type": "workbench/surface_wedge_volume",
        "inner_surface": inner_surface,
        "outer_surface": outer_surface,
        "metric": metric,
    }
    return params

def surface_wedge_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceWedgeVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inner_surface", None) is None:
        raise StyxValidationError("`inner_surface` must not be None")
    if not isinstance(params["inner_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inner_surface` has the wrong type: Received `{type(params["inner_surface"])}` expected `InputPathType`')
    if params.get("outer_surface", None) is None:
        raise StyxValidationError("`outer_surface` must not be None")
    if not isinstance(params["outer_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outer_surface` has the wrong type: Received `{type(params["outer_surface"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], str):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')

def surface_wedge_volume_cargs(params: SurfaceWedgeVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-wedge-volume")
    cargs.append(execution.input_file(params["inner_surface"]))
    cargs.append(execution.input_file(params["outer_surface"]))
    cargs.append(params["metric"])
    return cargs

def surface_wedge_volume_outputs(params: SurfaceWedgeVolume, execution: Execution) -> SurfaceWedgeVolumeOutputs:
    """Build the SurfaceWedgeVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_v: OutputPathType = execution.output_file(params["metric"])
    return SurfaceWedgeVolumeOutputs(
        root=root_v,
        metric=metric_v,
    )

def surface_wedge_volume_execute(params: SurfaceWedgeVolume, runner: Runner | None = None) -> SurfaceWedgeVolumeOutputs:
    """
    MEASURE PER-VERTEX VOLUME BETWEEN SURFACES

    Compute the volume of each vertex's area from one surface to another.  The surfaces must have vertex correspondence, and have consistent triangle orientation.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_wedge_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_WEDGE_VOLUME_METADATA)
    execution.params(params)
    args = surface_wedge_volume_cargs(params, execution)
    out = surface_wedge_volume_outputs(params, execution)
    execution.run(args)
    return out

def surface_wedge_volume(
    inner_surface: InputPathType,
    outer_surface: InputPathType,
    metric: str,
    runner: Runner | None = None,
) -> SurfaceWedgeVolumeOutputs:
    """
    MEASURE PER-VERTEX VOLUME BETWEEN SURFACES

    Compute the volume of each vertex's area from one surface to another.  The surfaces must have vertex correspondence, and have consistent triangle orientation.

    Args:
        inner_surface: the inner surface
        outer_surface: the outer surface
        metric: the output metric
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_wedge_volume_params(
        inner_surface=inner_surface,
        outer_surface=outer_surface,
        metric=metric,
    )
    return surface_wedge_volume_execute(params, runner)

__all__ = [
    "SurfaceWedgeVolume",
    "SurfaceWedgeVolumeOutputs",
    "SURFACE_WEDGE_VOLUME_METADATA",
    "surface_wedge_volume_cargs",
    "surface_wedge_volume_outputs",
    "surface_wedge_volume_params",
    "surface_wedge_volume_execute",
    "surface_wedge_volume_validate",
    "surface_wedge_volume",
]