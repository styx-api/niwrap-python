# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_RESAMPLE_METADATA = Metadata(
    id="surface_resample",
    name="RESAMPLE A SURFACE TO A DIFFERENT MESH",
    package="workbench",
)

class SurfaceResampleAreaSurfs(typing.TypedDict):
    current_area: InputPathType
    """a relevant surface with <current-sphere> mesh"""
    new_area: InputPathType
    """a relevant surface with <new-sphere> mesh"""

SurfaceResample = typing.TypedDict(
    "SurfaceResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_resample"]],
        "surface_in": InputPathType,
        "current_sphere": InputPathType,
        "new_sphere": InputPathType,
        "method": str,
        "surface_out": str,
        "area_surfs": typing.NotRequired[SurfaceResampleAreaSurfs],
        "area_metrics": typing.NotRequired[SurfaceResampleAreaSurfs],
        "bypass_sphere_check": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SurfaceResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the output surface file"""

def surface_resample_params(
    surface_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    method: str,
    surface_out: str,
    area_surfs: SurfaceResampleAreaSurfs | None = None,
    area_metrics: SurfaceResampleAreaSurfs | None = None,
    bypass_sphere_check: bool = False,
) -> SurfaceResample:
    """
    Build parameters.

    Args:
        surface_in: the surface file to resample
        current_sphere: a sphere surface with the mesh that the input surface is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        method: the method name
        surface_out: the output surface file
        area_surfs: specify surfaces to do vertex area correction based on
        area_metrics: specify vertex area metrics to do area correction based on
        bypass_sphere_check: ADVANCED: allow the current and new 'spheres' to have\
            arbitrary shape as long as they follow the same contour

    Returns:
        Parameter dictionary.
    """
    params: SurfaceResample = {
        "@type": "workbench/surface_resample",
        "surface_in": surface_in,
        "current_sphere": current_sphere,
        "new_sphere": new_sphere,
        "method": method,
        "surface_out": surface_out,
        "bypass_sphere_check": bypass_sphere_check,
    }
    if area_surfs is not None:
        params["area_surfs"] = area_surfs
    if area_metrics is not None:
        params["area_metrics"] = area_metrics
    return params

def surface_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_in", None) is None:
        raise StyxValidationError("`surface_in` must not be None")
    if not isinstance(params["surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in` has the wrong type: Received `{type(params["surface_in"])}` expected `InputPathType`')
    if params.get("current_sphere", None) is None:
        raise StyxValidationError("`current_sphere` must not be None")
    if not isinstance(params["current_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`current_sphere` has the wrong type: Received `{type(params["current_sphere"])}` expected `InputPathType`')
    if params.get("new_sphere", None) is None:
        raise StyxValidationError("`new_sphere` must not be None")
    if not isinstance(params["new_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_sphere` has the wrong type: Received `{type(params["new_sphere"])}` expected `InputPathType`')
    if params.get("method", None) is None:
        raise StyxValidationError("`method` must not be None")
    if not isinstance(params["method"], str):
        raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')
    if params.get("area_surfs", None) is not None:
        if not isinstance(params["area_surfs"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["area_surfs"])}\'')
        if params["area_surfs"].get("current_area", None) is None:
            raise StyxValidationError("`current_area` must not be None")
        if not isinstance(params["area_surfs"]["current_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_area` has the wrong type: Received `{type(params["area_surfs"]["current_area"])}` expected `InputPathType`')
        if params["area_surfs"].get("new_area", None) is None:
            raise StyxValidationError("`new_area` must not be None")
        if not isinstance(params["area_surfs"]["new_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_area` has the wrong type: Received `{type(params["area_surfs"]["new_area"])}` expected `InputPathType`')
    if params.get("area_metrics", None) is not None:
        if not isinstance(params["area_metrics"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["area_metrics"])}\'')
        if params["area_metrics"].get("current_area", None) is None:
            raise StyxValidationError("`current_area` must not be None")
        if not isinstance(params["area_metrics"]["current_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_area` has the wrong type: Received `{type(params["area_metrics"]["current_area"])}` expected `InputPathType`')
        if params["area_metrics"].get("new_area", None) is None:
            raise StyxValidationError("`new_area` must not be None")
        if not isinstance(params["area_metrics"]["new_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_area` has the wrong type: Received `{type(params["area_metrics"]["new_area"])}` expected `InputPathType`')
    if params.get("bypass_sphere_check", None) is not None:
        if not isinstance(params["bypass_sphere_check"], bool):
            raise StyxValidationError(f'`bypass_sphere_check` has the wrong type: Received `{type(params["bypass_sphere_check"])}` expected `bool`')

def surface_resample_cargs(params: SurfaceResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-resample")
    cargs.append(execution.input_file(params["surface_in"]))
    cargs.append(execution.input_file(params["current_sphere"]))
    cargs.append(execution.input_file(params["new_sphere"]))
    cargs.append(params["method"])
    cargs.append(params["surface_out"])
    v_0 = params.get("area_surfs")
    if v_0 is not None:
        cargs.append("-area-surfs")
        cargs.append(execution.input_file(v_0["current_area"]))
        cargs.append(execution.input_file(v_0["new_area"]))
    v_1 = params.get("area_metrics")
    if v_1 is not None:
        cargs.append("-area-metrics")
        cargs.append(execution.input_file(v_1["current_area"]))
        cargs.append(execution.input_file(v_1["new_area"]))
    if params.get("bypass_sphere_check"):
        cargs.append("-bypass-sphere-check")
    return cargs

def surface_resample_outputs(params: SurfaceResample, execution: Execution) -> SurfaceResampleOutputs:
    """Build the SurfaceResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceResampleOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_resample_execute(params: SurfaceResample, runner: Runner | None = None) -> SurfaceResampleOutputs:
    """
    RESAMPLE A SURFACE TO A DIFFERENT MESH

    Resamples a surface file, given two spherical surfaces that are in register.  If ADAP_BARY_AREA is used, exactly one of -area-surfs or -area-metrics must be specified.  This method is not generally recommended for surface resampling, but is provided for completeness.

The BARYCENTRIC method is generally recommended for anatomical surfaces, in order to minimize smoothing.

For cut surfaces (including flatmaps), use -surface-cut-resample.

Instead of resampling a spherical surface, the -surface-sphere-project-unproject command is recommended.

The <method> argument must be one of the following:

ADAP_BARY_AREA
BARYCENTRIC


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_RESAMPLE_METADATA)
    execution.params(params)
    args = surface_resample_cargs(params, execution)
    out = surface_resample_outputs(params, execution)
    execution.run(args)
    return out

def surface_resample(
    surface_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    method: str,
    surface_out: str,
    area_surfs: SurfaceResampleAreaSurfs | None = None,
    area_metrics: SurfaceResampleAreaSurfs | None = None,
    bypass_sphere_check: bool = False,
    runner: Runner | None = None,
) -> SurfaceResampleOutputs:
    """
    RESAMPLE A SURFACE TO A DIFFERENT MESH

    Resamples a surface file, given two spherical surfaces that are in register.  If ADAP_BARY_AREA is used, exactly one of -area-surfs or -area-metrics must be specified.  This method is not generally recommended for surface resampling, but is provided for completeness.

The BARYCENTRIC method is generally recommended for anatomical surfaces, in order to minimize smoothing.

For cut surfaces (including flatmaps), use -surface-cut-resample.

Instead of resampling a spherical surface, the -surface-sphere-project-unproject command is recommended.

The <method> argument must be one of the following:

ADAP_BARY_AREA
BARYCENTRIC


    Args:
        surface_in: the surface file to resample
        current_sphere: a sphere surface with the mesh that the input surface is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        method: the method name
        surface_out: the output surface file
        area_surfs: specify surfaces to do vertex area correction based on
        area_metrics: specify vertex area metrics to do area correction based on
        bypass_sphere_check: ADVANCED: allow the current and new 'spheres' to have\
            arbitrary shape as long as they follow the same contour
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_resample_params(
        surface_in=surface_in,
        current_sphere=current_sphere,
        new_sphere=new_sphere,
        method=method,
        surface_out=surface_out,
        area_surfs=area_surfs,
        area_metrics=area_metrics,
        bypass_sphere_check=bypass_sphere_check,
    )
    return surface_resample_execute(params, runner)

__all__ = [
    "SurfaceResample",
    "SurfaceResampleOutputs",
    "SURFACE_RESAMPLE_METADATA",
    "surface_resample_cargs",
    "surface_resample_outputs",
    "surface_resample_params",
    "surface_resample_execute",
    "surface_resample_validate",
    "surface_resample",
]