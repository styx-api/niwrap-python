# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_IMPORT_METADATA = Metadata(
    id="cifti_label_import",
    name="MAKE A CIFTI LABEL FILE FROM A CIFTI FILE",
    package="workbench",
)

CiftiLabelImport = typing.TypedDict(
    "CiftiLabelImport",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_import"]],
        "input": InputPathType,
        "label_list_file": str,
        "output": str,
        "discard_others": typing.NotRequired[bool],
        "unlabeled_value": typing.NotRequired[int],
        "drop_unused_labels": typing.NotRequired[bool],
        "hierarchy": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CiftiLabelImportOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output cifti label file"""

def cifti_label_import_params(
    input_: InputPathType,
    label_list_file: str,
    output: str,
    discard_others: bool = False,
    unlabeled_value: int | None = None,
    drop_unused_labels: bool = False,
    hierarchy: str | None = None,
) -> CiftiLabelImport:
    """
    Build parameters.

    Args:
        input_: the input cifti file
        label_list_file: text file containing the values and names for labels
        output: the output cifti label file
        discard_others: set any values not mentioned in the label list to the ???\
            label
        unlabeled_value: set the value that will be interpreted as unlabeled
        drop_unused_labels: remove any unused label values from the label table
        hierarchy: read label name hierarchy from a json file

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelImport = {
        "@type": "workbench/cifti_label_import",
        "input": input_,
        "label_list_file": label_list_file,
        "output": output,
        "discard_others": discard_others,
        "drop_unused_labels": drop_unused_labels,
    }
    if unlabeled_value is not None:
        params["unlabeled_value"] = unlabeled_value
    if hierarchy is not None:
        params["hierarchy"] = hierarchy
    return params

def cifti_label_import_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelImport."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("label_list_file", None) is None:
        raise StyxValidationError("`label_list_file` must not be None")
    if not isinstance(params["label_list_file"], str):
        raise StyxValidationError(f'`label_list_file` has the wrong type: Received `{type(params["label_list_file"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("discard_others", None) is not None:
        if not isinstance(params["discard_others"], bool):
            raise StyxValidationError(f'`discard_others` has the wrong type: Received `{type(params["discard_others"])}` expected `bool`')
    if params.get("unlabeled_value", None) is not None:
        if not isinstance(params["unlabeled_value"], int):
            raise StyxValidationError(f'`unlabeled_value` has the wrong type: Received `{type(params["unlabeled_value"])}` expected `int`')
    if params.get("drop_unused_labels", None) is not None:
        if not isinstance(params["drop_unused_labels"], bool):
            raise StyxValidationError(f'`drop_unused_labels` has the wrong type: Received `{type(params["drop_unused_labels"])}` expected `bool`')
    if params.get("hierarchy", None) is not None:
        if not isinstance(params["hierarchy"], str):
            raise StyxValidationError(f'`hierarchy` has the wrong type: Received `{type(params["hierarchy"])}` expected `str`')

def cifti_label_import_cargs(params: CiftiLabelImport, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-import")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["label_list_file"])
    cargs.append(params["output"])
    if params.get("discard_others"):
        cargs.append("-discard-others")
    v_0 = params.get("unlabeled_value")
    if v_0 is not None:
        cargs.append("-unlabeled-value")
        cargs.append(str(v_0))
    if params.get("drop_unused_labels"):
        cargs.append("-drop-unused-labels")
    v_1 = params.get("hierarchy")
    if v_1 is not None:
        cargs.append("-hierarchy")
        cargs.append(v_1)
    return cargs

def cifti_label_import_outputs(params: CiftiLabelImport, execution: Execution) -> CiftiLabelImportOutputs:
    """Build the CiftiLabelImportOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return CiftiLabelImportOutputs(
        root=root_v,
        output=output_v,
    )

def cifti_label_import_execute(params: CiftiLabelImport, runner: Runner | None = None) -> CiftiLabelImportOutputs:
    """
    MAKE A CIFTI LABEL FILE FROM A CIFTI FILE

    Creates a cifti label file from a cifti file with label-like values.  You may specify the empty string (use "") for <label-list-file>, which will be treated as if it is an empty file.  The label list file must have the following format (2 lines per label):

<labelname>
<key> <red> <green> <blue> <alpha>
...

Label names are specified on a separate line from their value and color, in order to let label names contain spaces.  Whitespace is trimmed from both ends of the label name, but is kept if it is in the middle of a label.  Do not specify the "unlabeled" key in the file, it is assumed that 0 means not labeled unless -unlabeled-value is specified.  The value of <key> specifies what value in the imported file should be used as this label (these same key values are also used in the output file).  The values of <red>, <green>, <blue> and <alpha> must be integers from 0 to 255, and will specify the color the label is drawn as (alpha of 255 means fully opaque, which is probably what you want).

By default, it will create new label names with names like LABEL_5 for any values encountered that are not mentioned in the list file, specify -discard-others to instead set these values to the "unlabeled" key.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_import_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_IMPORT_METADATA)
    execution.params(params)
    args = cifti_label_import_cargs(params, execution)
    out = cifti_label_import_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_import(
    input_: InputPathType,
    label_list_file: str,
    output: str,
    discard_others: bool = False,
    unlabeled_value: int | None = None,
    drop_unused_labels: bool = False,
    hierarchy: str | None = None,
    runner: Runner | None = None,
) -> CiftiLabelImportOutputs:
    """
    MAKE A CIFTI LABEL FILE FROM A CIFTI FILE

    Creates a cifti label file from a cifti file with label-like values.  You may specify the empty string (use "") for <label-list-file>, which will be treated as if it is an empty file.  The label list file must have the following format (2 lines per label):

<labelname>
<key> <red> <green> <blue> <alpha>
...

Label names are specified on a separate line from their value and color, in order to let label names contain spaces.  Whitespace is trimmed from both ends of the label name, but is kept if it is in the middle of a label.  Do not specify the "unlabeled" key in the file, it is assumed that 0 means not labeled unless -unlabeled-value is specified.  The value of <key> specifies what value in the imported file should be used as this label (these same key values are also used in the output file).  The values of <red>, <green>, <blue> and <alpha> must be integers from 0 to 255, and will specify the color the label is drawn as (alpha of 255 means fully opaque, which is probably what you want).

By default, it will create new label names with names like LABEL_5 for any values encountered that are not mentioned in the list file, specify -discard-others to instead set these values to the "unlabeled" key.

    Args:
        input_: the input cifti file
        label_list_file: text file containing the values and names for labels
        output: the output cifti label file
        discard_others: set any values not mentioned in the label list to the ???\
            label
        unlabeled_value: set the value that will be interpreted as unlabeled
        drop_unused_labels: remove any unused label values from the label table
        hierarchy: read label name hierarchy from a json file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_import_params(
        input_=input_,
        label_list_file=label_list_file,
        output=output,
        discard_others=discard_others,
        unlabeled_value=unlabeled_value,
        drop_unused_labels=drop_unused_labels,
        hierarchy=hierarchy,
    )
    return cifti_label_import_execute(params, runner)

__all__ = [
    "CiftiLabelImport",
    "CiftiLabelImportOutputs",
    "CIFTI_LABEL_IMPORT_METADATA",
    "cifti_label_import_cargs",
    "cifti_label_import_outputs",
    "cifti_label_import_params",
    "cifti_label_import_execute",
    "cifti_label_import_validate",
    "cifti_label_import",
]