# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_REORDER_METADATA = Metadata(
    id="cifti_reorder",
    name="REORDER THE PARCELS OR SCALAR/LABEL MAPS IN A CIFTI FILE",
    package="workbench",
)

CiftiReorder = typing.TypedDict(
    "CiftiReorder",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_reorder"]],
        "cifti_in": InputPathType,
        "direction": str,
        "reorder_list": str,
        "cifti_out": str,
    },
)

@dataclasses.dataclass
class CiftiReorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the reordered cifti file"""

def cifti_reorder_params(
    cifti_in: InputPathType,
    direction: str,
    reorder_list: str,
    cifti_out: str,
) -> CiftiReorder:
    """
    Build parameters.

    Args:
        cifti_in: input cifti file
        direction: which dimension to reorder along, ROW or COLUMN
        reorder_list: a text file containing the desired order transformation
        cifti_out: the reordered cifti file

    Returns:
        Parameter dictionary.
    """
    params: CiftiReorder = {
        "@type": "workbench/cifti_reorder",
        "cifti_in": cifti_in,
        "direction": direction,
        "reorder_list": reorder_list,
        "cifti_out": cifti_out,
    }
    return params

def cifti_reorder_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiReorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("reorder_list", None) is None:
        raise StyxValidationError("`reorder_list` must not be None")
    if not isinstance(params["reorder_list"], str):
        raise StyxValidationError(f'`reorder_list` has the wrong type: Received `{type(params["reorder_list"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')

def cifti_reorder_cargs(params: CiftiReorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-reorder")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["direction"])
    cargs.append(params["reorder_list"])
    cargs.append(params["cifti_out"])
    return cargs

def cifti_reorder_outputs(params: CiftiReorder, execution: Execution) -> CiftiReorderOutputs:
    """Build the CiftiReorderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiReorderOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_reorder_execute(params: CiftiReorder, runner: Runner | None = None) -> CiftiReorderOutputs:
    """
    REORDER THE PARCELS OR SCALAR/LABEL MAPS IN A CIFTI FILE

    The mapping along the specified direction must be parcels, scalars, or labels.  For pscalar or ptseries, use COLUMN to reorder the parcels.  For dlabel, use ROW.  The <reorder-list> file must contain 1-based indices separated by whitespace (spaces, newlines, tabs, etc), with as many indices as <cifti-in> has along the specified dimension.  These indices specify which current index should end up in that position, for instance, if the current order is 'A B C D', and the desired order is 'D A B C', the text file should contain '4 1 2 3'.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_reorder_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_REORDER_METADATA)
    execution.params(params)
    args = cifti_reorder_cargs(params, execution)
    out = cifti_reorder_outputs(params, execution)
    execution.run(args)
    return out

def cifti_reorder(
    cifti_in: InputPathType,
    direction: str,
    reorder_list: str,
    cifti_out: str,
    runner: Runner | None = None,
) -> CiftiReorderOutputs:
    """
    REORDER THE PARCELS OR SCALAR/LABEL MAPS IN A CIFTI FILE

    The mapping along the specified direction must be parcels, scalars, or labels.  For pscalar or ptseries, use COLUMN to reorder the parcels.  For dlabel, use ROW.  The <reorder-list> file must contain 1-based indices separated by whitespace (spaces, newlines, tabs, etc), with as many indices as <cifti-in> has along the specified dimension.  These indices specify which current index should end up in that position, for instance, if the current order is 'A B C D', and the desired order is 'D A B C', the text file should contain '4 1 2 3'.

    Args:
        cifti_in: input cifti file
        direction: which dimension to reorder along, ROW or COLUMN
        reorder_list: a text file containing the desired order transformation
        cifti_out: the reordered cifti file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_reorder_params(
        cifti_in=cifti_in,
        direction=direction,
        reorder_list=reorder_list,
        cifti_out=cifti_out,
    )
    return cifti_reorder_execute(params, runner)

__all__ = [
    "CiftiReorder",
    "CiftiReorderOutputs",
    "CIFTI_REORDER_METADATA",
    "cifti_reorder_cargs",
    "cifti_reorder_outputs",
    "cifti_reorder_params",
    "cifti_reorder_execute",
    "cifti_reorder_validate",
    "cifti_reorder",
]