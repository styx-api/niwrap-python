# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_GEODESIC_DISTANCE_SPARSE_TEXT_METADATA = Metadata(
    id="surface_geodesic_distance_sparse_text",
    name="OUTPUT GEODESIC DISTANCES AS TEXT",
    package="workbench",
)

SurfaceGeodesicDistanceSparseText = typing.TypedDict(
    "SurfaceGeodesicDistanceSparseText",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_geodesic_distance_sparse_text"]],
        "surface": InputPathType,
        "limit": float,
        "text_out": str,
        "corrected_areas": typing.NotRequired[InputPathType],
        "naive": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SurfaceGeodesicDistanceSparseTextOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_geodesic_distance_sparse_text_params(
    surface: InputPathType,
    limit: float,
    text_out: str,
    corrected_areas: InputPathType | None = None,
    naive: bool = False,
) -> SurfaceGeodesicDistanceSparseText:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        limit: the geodesic distance limit in mm
        text_out: output - the output text file
        corrected_areas: vertex areas to use to correct the distances on a\
            group-average surface
        naive: use only neighbors, don't crawl triangles (not recommended)

    Returns:
        Parameter dictionary.
    """
    params: SurfaceGeodesicDistanceSparseText = {
        "@type": "workbench/surface_geodesic_distance_sparse_text",
        "surface": surface,
        "limit": limit,
        "text_out": text_out,
        "naive": naive,
    }
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def surface_geodesic_distance_sparse_text_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceGeodesicDistanceSparseText."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("limit", None) is None:
        raise StyxValidationError("`limit` must not be None")
    if not isinstance(params["limit"], (float, int)):
        raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("text_out", None) is None:
        raise StyxValidationError("`text_out` must not be None")
    if not isinstance(params["text_out"], str):
        raise StyxValidationError(f'`text_out` has the wrong type: Received `{type(params["text_out"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("naive", None) is not None:
        if not isinstance(params["naive"], bool):
            raise StyxValidationError(f'`naive` has the wrong type: Received `{type(params["naive"])}` expected `bool`')

def surface_geodesic_distance_sparse_text_cargs(params: SurfaceGeodesicDistanceSparseText, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-geodesic-distance-sparse-text")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["limit"]))
    cargs.append(params["text_out"])
    v_0 = params.get("corrected_areas")
    if v_0 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_0))
    if params.get("naive"):
        cargs.append("-naive")
    return cargs

def surface_geodesic_distance_sparse_text_outputs(params: SurfaceGeodesicDistanceSparseText, execution: Execution) -> SurfaceGeodesicDistanceSparseTextOutputs:
    """Build the SurfaceGeodesicDistanceSparseTextOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceGeodesicDistanceSparseTextOutputs(
        root=root_v,
    )

def surface_geodesic_distance_sparse_text_execute(params: SurfaceGeodesicDistanceSparseText, runner: Runner | None = None) -> SurfaceGeodesicDistanceSparseTextOutputs:
    """
    OUTPUT GEODESIC DISTANCES AS TEXT

    Similar to -geodesic-distance-all-to-all, but outputs in a text format that only lists the in-range vertices.  Each line in the output text file is a list of pairs, like '<vertex>,<distance>,<vertex>,<distance>,...' in csv format.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_geodesic_distance_sparse_text_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_GEODESIC_DISTANCE_SPARSE_TEXT_METADATA)
    execution.params(params)
    args = surface_geodesic_distance_sparse_text_cargs(params, execution)
    out = surface_geodesic_distance_sparse_text_outputs(params, execution)
    execution.run(args)
    return out

def surface_geodesic_distance_sparse_text(
    surface: InputPathType,
    limit: float,
    text_out: str,
    corrected_areas: InputPathType | None = None,
    naive: bool = False,
    runner: Runner | None = None,
) -> SurfaceGeodesicDistanceSparseTextOutputs:
    """
    OUTPUT GEODESIC DISTANCES AS TEXT

    Similar to -geodesic-distance-all-to-all, but outputs in a text format that only lists the in-range vertices.  Each line in the output text file is a list of pairs, like '<vertex>,<distance>,<vertex>,<distance>,...' in csv format.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        surface: the surface to compute on
        limit: the geodesic distance limit in mm
        text_out: output - the output text file
        corrected_areas: vertex areas to use to correct the distances on a\
            group-average surface
        naive: use only neighbors, don't crawl triangles (not recommended)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_geodesic_distance_sparse_text_params(
        surface=surface,
        limit=limit,
        text_out=text_out,
        corrected_areas=corrected_areas,
        naive=naive,
    )
    return surface_geodesic_distance_sparse_text_execute(params, runner)

__all__ = [
    "SurfaceGeodesicDistanceSparseText",
    "SurfaceGeodesicDistanceSparseTextOutputs",
    "SURFACE_GEODESIC_DISTANCE_SPARSE_TEXT_METADATA",
    "surface_geodesic_distance_sparse_text_cargs",
    "surface_geodesic_distance_sparse_text_outputs",
    "surface_geodesic_distance_sparse_text_params",
    "surface_geodesic_distance_sparse_text_execute",
    "surface_geodesic_distance_sparse_text_validate",
    "surface_geodesic_distance_sparse_text",
]