# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_PAIRWISE_CORRELATION_METADATA = Metadata(
    id="cifti_pairwise_correlation",
    name="CORRELATE PAIRED ROWS BETWEEN TWO CIFTI FILES",
    package="workbench",
)

CiftiPairwiseCorrelation = typing.TypedDict(
    "CiftiPairwiseCorrelation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_pairwise_correlation"]],
        "cifti_a": InputPathType,
        "cifti_b": InputPathType,
        "cifti_out": str,
        "fisher_z": typing.NotRequired[bool],
        "override_mapping_check": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiPairwiseCorrelationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_pairwise_correlation_params(
    cifti_a: InputPathType,
    cifti_b: InputPathType,
    cifti_out: str,
    fisher_z: bool = False,
    override_mapping_check: bool = False,
) -> CiftiPairwiseCorrelation:
    """
    Build parameters.

    Args:
        cifti_a: first input cifti file
        cifti_b: second input cifti file
        cifti_out: output cifti file
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        override_mapping_check: don't check the mappings for compatibility, only\
            check length

    Returns:
        Parameter dictionary.
    """
    params: CiftiPairwiseCorrelation = {
        "@type": "workbench/cifti_pairwise_correlation",
        "cifti_a": cifti_a,
        "cifti_b": cifti_b,
        "cifti_out": cifti_out,
        "fisher_z": fisher_z,
        "override_mapping_check": override_mapping_check,
    }
    return params

def cifti_pairwise_correlation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiPairwiseCorrelation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_a", None) is None:
        raise StyxValidationError("`cifti_a` must not be None")
    if not isinstance(params["cifti_a"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_a` has the wrong type: Received `{type(params["cifti_a"])}` expected `InputPathType`')
    if params.get("cifti_b", None) is None:
        raise StyxValidationError("`cifti_b` must not be None")
    if not isinstance(params["cifti_b"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_b` has the wrong type: Received `{type(params["cifti_b"])}` expected `InputPathType`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("fisher_z", None) is not None:
        if not isinstance(params["fisher_z"], bool):
            raise StyxValidationError(f'`fisher_z` has the wrong type: Received `{type(params["fisher_z"])}` expected `bool`')
    if params.get("override_mapping_check", None) is not None:
        if not isinstance(params["override_mapping_check"], bool):
            raise StyxValidationError(f'`override_mapping_check` has the wrong type: Received `{type(params["override_mapping_check"])}` expected `bool`')

def cifti_pairwise_correlation_cargs(params: CiftiPairwiseCorrelation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-pairwise-correlation")
    cargs.append(execution.input_file(params["cifti_a"]))
    cargs.append(execution.input_file(params["cifti_b"]))
    cargs.append(params["cifti_out"])
    if params.get("fisher_z"):
        cargs.append("-fisher-z")
    if params.get("override_mapping_check"):
        cargs.append("-override-mapping-check")
    return cargs

def cifti_pairwise_correlation_outputs(params: CiftiPairwiseCorrelation, execution: Execution) -> CiftiPairwiseCorrelationOutputs:
    """Build the CiftiPairwiseCorrelationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiPairwiseCorrelationOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_pairwise_correlation_execute(params: CiftiPairwiseCorrelation, runner: Runner | None = None) -> CiftiPairwiseCorrelationOutputs:
    """
    CORRELATE PAIRED ROWS BETWEEN TWO CIFTI FILES

    For each row in <cifti-a>, correlate it with the same row in <cifti-b>, and put the result in the same row of <cifti-out>, which has only one column.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_pairwise_correlation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_PAIRWISE_CORRELATION_METADATA)
    execution.params(params)
    args = cifti_pairwise_correlation_cargs(params, execution)
    out = cifti_pairwise_correlation_outputs(params, execution)
    execution.run(args)
    return out

def cifti_pairwise_correlation(
    cifti_a: InputPathType,
    cifti_b: InputPathType,
    cifti_out: str,
    fisher_z: bool = False,
    override_mapping_check: bool = False,
    runner: Runner | None = None,
) -> CiftiPairwiseCorrelationOutputs:
    """
    CORRELATE PAIRED ROWS BETWEEN TWO CIFTI FILES

    For each row in <cifti-a>, correlate it with the same row in <cifti-b>, and put the result in the same row of <cifti-out>, which has only one column.

    Args:
        cifti_a: first input cifti file
        cifti_b: second input cifti file
        cifti_out: output cifti file
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        override_mapping_check: don't check the mappings for compatibility, only\
            check length
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_pairwise_correlation_params(
        cifti_a=cifti_a,
        cifti_b=cifti_b,
        cifti_out=cifti_out,
        fisher_z=fisher_z,
        override_mapping_check=override_mapping_check,
    )
    return cifti_pairwise_correlation_execute(params, runner)

__all__ = [
    "CiftiPairwiseCorrelation",
    "CiftiPairwiseCorrelationOutputs",
    "CIFTI_PAIRWISE_CORRELATION_METADATA",
    "cifti_pairwise_correlation_cargs",
    "cifti_pairwise_correlation_outputs",
    "cifti_pairwise_correlation_params",
    "cifti_pairwise_correlation_execute",
    "cifti_pairwise_correlation_validate",
    "cifti_pairwise_correlation",
]