# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_LABEL_TO_ROI_METADATA = Metadata(
    id="volume_label_to_roi",
    name="MAKE A VOLUME LABEL INTO AN ROI VOLUME",
    package="workbench",
)

VolumeLabelToRoi = typing.TypedDict(
    "VolumeLabelToRoi",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_label_to_roi"]],
        "label_in": InputPathType,
        "volume_out": str,
        "name": typing.NotRequired[str],
        "key": typing.NotRequired[int],
        "map": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeLabelToRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume file"""

def volume_label_to_roi_params(
    label_in: InputPathType,
    volume_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
) -> VolumeLabelToRoi:
    """
    Build parameters.

    Args:
        label_in: the input volume label file
        volume_out: the output volume file
        name: select label by name
        key: select label by key
        map: select a single label map to use

    Returns:
        Parameter dictionary.
    """
    params: VolumeLabelToRoi = {
        "@type": "workbench/volume_label_to_roi",
        "label_in": label_in,
        "volume_out": volume_out,
    }
    if name is not None:
        params["name"] = name
    if key is not None:
        params["key"] = key
    if map is not None:
        params["map"] = map
    return params

def volume_label_to_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeLabelToRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("key", None) is not None:
        if not isinstance(params["key"], int):
            raise StyxValidationError(f'`key` has the wrong type: Received `{type(params["key"])}` expected `int`')
    if params.get("map", None) is not None:
        if not isinstance(params["map"], str):
            raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')

def volume_label_to_roi_cargs(params: VolumeLabelToRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-label-to-roi")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("-name")
        cargs.append(v_0)
    v_1 = params.get("key")
    if v_1 is not None:
        cargs.append("-key")
        cargs.append(str(v_1))
    v_2 = params.get("map")
    if v_2 is not None:
        cargs.append("-map")
        cargs.append(v_2)
    return cargs

def volume_label_to_roi_outputs(params: VolumeLabelToRoi, execution: Execution) -> VolumeLabelToRoiOutputs:
    """Build the VolumeLabelToRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeLabelToRoiOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_label_to_roi_execute(params: VolumeLabelToRoi, runner: Runner | None = None) -> VolumeLabelToRoiOutputs:
    """
    MAKE A VOLUME LABEL INTO AN ROI VOLUME

    For each map in <label-in>, a map is created in <volume-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_label_to_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_LABEL_TO_ROI_METADATA)
    execution.params(params)
    args = volume_label_to_roi_cargs(params, execution)
    out = volume_label_to_roi_outputs(params, execution)
    execution.run(args)
    return out

def volume_label_to_roi(
    label_in: InputPathType,
    volume_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
    runner: Runner | None = None,
) -> VolumeLabelToRoiOutputs:
    """
    MAKE A VOLUME LABEL INTO AN ROI VOLUME

    For each map in <label-in>, a map is created in <volume-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        label_in: the input volume label file
        volume_out: the output volume file
        name: select label by name
        key: select label by key
        map: select a single label map to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_label_to_roi_params(
        label_in=label_in,
        volume_out=volume_out,
        name=name,
        key=key,
        map=map,
    )
    return volume_label_to_roi_execute(params, runner)

__all__ = [
    "VolumeLabelToRoi",
    "VolumeLabelToRoiOutputs",
    "VOLUME_LABEL_TO_ROI_METADATA",
    "volume_label_to_roi_cargs",
    "volume_label_to_roi_outputs",
    "volume_label_to_roi_params",
    "volume_label_to_roi_execute",
    "volume_label_to_roi_validate",
    "volume_label_to_roi",
]