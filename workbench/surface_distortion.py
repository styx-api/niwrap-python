# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_DISTORTION_METADATA = Metadata(
    id="surface_distortion",
    name="MEASURE DISTORTION BETWEEN SURFACES",
    package="workbench",
)

class SurfaceDistortionSmooth(typing.TypedDict):
    sigma: float
    """the size of the smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class SurfaceDistortionMatchSurfaceArea(typing.TypedDict):
    roi: typing.NotRequired[InputPathType]
    """only use the surface area within a given ROI (e.g., to exclude the medial wall)"""

class SurfaceDistortionLocalAffineMethod(typing.TypedDict):
    log2: typing.NotRequired[bool]
    """apply base-2 log transform"""

SurfaceDistortion = typing.TypedDict(
    "SurfaceDistortion",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_distortion"]],
        "surface_reference": InputPathType,
        "surface_distorted": InputPathType,
        "metric_out": str,
        "smooth": typing.NotRequired[SurfaceDistortionSmooth],
        "match_surface_area": typing.NotRequired[SurfaceDistortionMatchSurfaceArea],
        "caret5_method": typing.NotRequired[bool],
        "edge_method": typing.NotRequired[bool],
        "local_affine_method": typing.NotRequired[SurfaceDistortionLocalAffineMethod],
    },
)

@dataclasses.dataclass
class SurfaceDistortionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output distortion metric"""

def surface_distortion_params(
    surface_reference: InputPathType,
    surface_distorted: InputPathType,
    metric_out: str,
    smooth: SurfaceDistortionSmooth | None = None,
    match_surface_area: SurfaceDistortionMatchSurfaceArea | None = None,
    caret5_method: bool = False,
    edge_method: bool = False,
    local_affine_method: SurfaceDistortionLocalAffineMethod | None = None,
) -> SurfaceDistortion:
    """
    Build parameters.

    Args:
        surface_reference: the reference surface
        surface_distorted: the distorted surface
        metric_out: the output distortion metric
        smooth: smooth the area data
        match_surface_area: isotropically rescale the distorted surface so that it\
            has the same surface area as the reference surface
        caret5_method: use the surface distortion method from caret5
        edge_method: calculate distortion of edge lengths rather than areas
        local_affine_method: calculate distortion by the local affines between\
            triangles

    Returns:
        Parameter dictionary.
    """
    params: SurfaceDistortion = {
        "@type": "workbench/surface_distortion",
        "surface_reference": surface_reference,
        "surface_distorted": surface_distorted,
        "metric_out": metric_out,
        "caret5_method": caret5_method,
        "edge_method": edge_method,
    }
    if smooth is not None:
        params["smooth"] = smooth
    if match_surface_area is not None:
        params["match_surface_area"] = match_surface_area
    if local_affine_method is not None:
        params["local_affine_method"] = local_affine_method
    return params

def surface_distortion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceDistortion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_reference", None) is None:
        raise StyxValidationError("`surface_reference` must not be None")
    if not isinstance(params["surface_reference"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_reference` has the wrong type: Received `{type(params["surface_reference"])}` expected `InputPathType`')
    if params.get("surface_distorted", None) is None:
        raise StyxValidationError("`surface_distorted` must not be None")
    if not isinstance(params["surface_distorted"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_distorted` has the wrong type: Received `{type(params["surface_distorted"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("smooth", None) is not None:
        if not isinstance(params["smooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["smooth"])}\'')
        if params["smooth"].get("sigma", None) is None:
            raise StyxValidationError("`sigma` must not be None")
        if not isinstance(params["smooth"]["sigma"], (float, int)):
            raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["smooth"]["sigma"])}` expected `float`')
        if params["smooth"].get("fwhm", None) is not None:
            if not isinstance(params["smooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["smooth"]["fwhm"])}` expected `bool`')
    if params.get("match_surface_area", None) is not None:
        if not isinstance(params["match_surface_area"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["match_surface_area"])}\'')
        if params["match_surface_area"].get("roi", None) is not None:
            if not isinstance(params["match_surface_area"]["roi"], (pathlib.Path, str)):
                raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["match_surface_area"]["roi"])}` expected `InputPathType`')
    if params.get("caret5_method", None) is not None:
        if not isinstance(params["caret5_method"], bool):
            raise StyxValidationError(f'`caret5_method` has the wrong type: Received `{type(params["caret5_method"])}` expected `bool`')
    if params.get("edge_method", None) is not None:
        if not isinstance(params["edge_method"], bool):
            raise StyxValidationError(f'`edge_method` has the wrong type: Received `{type(params["edge_method"])}` expected `bool`')
    if params.get("local_affine_method", None) is not None:
        if not isinstance(params["local_affine_method"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["local_affine_method"])}\'')
        if params["local_affine_method"].get("log2", None) is not None:
            if not isinstance(params["local_affine_method"]["log2"], bool):
                raise StyxValidationError(f'`log2` has the wrong type: Received `{type(params["local_affine_method"]["log2"])}` expected `bool`')

def surface_distortion_cargs(params: SurfaceDistortion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-distortion")
    cargs.append(execution.input_file(params["surface_reference"]))
    cargs.append(execution.input_file(params["surface_distorted"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("smooth")
    if v_0 is not None:
        cargs.append("-smooth")
        cargs.append(str(v_0["sigma"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("match_surface_area")
    if v_1 is not None:
        cargs.append("-match-surface-area")
        v_2 = v_1.get("roi")
        if v_2 is not None:
            cargs.append("-roi")
            cargs.append(execution.input_file(v_2))
    if params.get("caret5_method"):
        cargs.append("-caret5-method")
    if params.get("edge_method"):
        cargs.append("-edge-method")
    v_3 = params.get("local_affine_method")
    if v_3 is not None:
        cargs.append("-local-affine-method")
        if v_3.get("log2"):
            cargs.append("-log2")
    return cargs

def surface_distortion_outputs(params: SurfaceDistortion, execution: Execution) -> SurfaceDistortionOutputs:
    """Build the SurfaceDistortionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return SurfaceDistortionOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def surface_distortion_execute(params: SurfaceDistortion, runner: Runner | None = None) -> SurfaceDistortionOutputs:
    """
    MEASURE DISTORTION BETWEEN SURFACES

    This command, when not using -caret5-method, -edge-method, or -local-affine-method, is equivalent to using -surface-vertex-areas on each surface, smoothing both output metrics with the GEO_GAUSS_EQUAL method on the surface they came from if -smooth is specified, and then using the formula 'ln(distorted/reference)/ln(2)' on the smoothed results.

When using -caret5-method, it uses the surface distortion method from caret5, which takes the base 2 log of the ratio of tile areas, then averages those results at each vertex, and then smooths the result on the reference surface.

When using -edge-method, the -smooth option is ignored, and the output at each vertex is the average of 'abs(ln(refEdge/distortEdge)/ln(2))' over all edges connected to the vertex.

When using -local-affine-method, the -smooth option is ignored.  The output is two columns, the first is the area distortion ratio, and the second is anisotropic strain.  These are calculated by an affine transform between matching triangles, and then averaged across the triangles of a vertex.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_distortion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_DISTORTION_METADATA)
    execution.params(params)
    args = surface_distortion_cargs(params, execution)
    out = surface_distortion_outputs(params, execution)
    execution.run(args)
    return out

def surface_distortion(
    surface_reference: InputPathType,
    surface_distorted: InputPathType,
    metric_out: str,
    smooth: SurfaceDistortionSmooth | None = None,
    match_surface_area: SurfaceDistortionMatchSurfaceArea | None = None,
    caret5_method: bool = False,
    edge_method: bool = False,
    local_affine_method: SurfaceDistortionLocalAffineMethod | None = None,
    runner: Runner | None = None,
) -> SurfaceDistortionOutputs:
    """
    MEASURE DISTORTION BETWEEN SURFACES

    This command, when not using -caret5-method, -edge-method, or -local-affine-method, is equivalent to using -surface-vertex-areas on each surface, smoothing both output metrics with the GEO_GAUSS_EQUAL method on the surface they came from if -smooth is specified, and then using the formula 'ln(distorted/reference)/ln(2)' on the smoothed results.

When using -caret5-method, it uses the surface distortion method from caret5, which takes the base 2 log of the ratio of tile areas, then averages those results at each vertex, and then smooths the result on the reference surface.

When using -edge-method, the -smooth option is ignored, and the output at each vertex is the average of 'abs(ln(refEdge/distortEdge)/ln(2))' over all edges connected to the vertex.

When using -local-affine-method, the -smooth option is ignored.  The output is two columns, the first is the area distortion ratio, and the second is anisotropic strain.  These are calculated by an affine transform between matching triangles, and then averaged across the triangles of a vertex.

    Args:
        surface_reference: the reference surface
        surface_distorted: the distorted surface
        metric_out: the output distortion metric
        smooth: smooth the area data
        match_surface_area: isotropically rescale the distorted surface so that it\
            has the same surface area as the reference surface
        caret5_method: use the surface distortion method from caret5
        edge_method: calculate distortion of edge lengths rather than areas
        local_affine_method: calculate distortion by the local affines between\
            triangles
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_distortion_params(
        surface_reference=surface_reference,
        surface_distorted=surface_distorted,
        metric_out=metric_out,
        smooth=smooth,
        match_surface_area=match_surface_area,
        caret5_method=caret5_method,
        edge_method=edge_method,
        local_affine_method=local_affine_method,
    )
    return surface_distortion_execute(params, runner)

__all__ = [
    "SurfaceDistortion",
    "SurfaceDistortionOutputs",
    "SURFACE_DISTORTION_METADATA",
    "surface_distortion_cargs",
    "surface_distortion_outputs",
    "surface_distortion_params",
    "surface_distortion_execute",
    "surface_distortion_validate",
    "surface_distortion",
]