# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SCENE_FILE_UPDATE_METADATA = Metadata(
    id="scene_file_update",
    name="UPDATE SCENE FILE",
    package="workbench",
)

SceneFileUpdate = typing.TypedDict(
    "SceneFileUpdate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/scene_file_update"]],
        "input_scene_file": str,
        "output_scene_file": str,
        "scene_name_or_number": str,
        "fix_map_palette_settings": typing.NotRequired[bool],
        "remove_missing_files": typing.NotRequired[bool],
        "error": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "copy_map_one_palette": list[str],
        "data_file_add": list[str],
        "data_file_remove": list[str],
    },
)

@dataclasses.dataclass
class SceneFileUpdateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def scene_file_update_params(
    input_scene_file: str,
    output_scene_file: str,
    scene_name_or_number: str,
    copy_map_one_palette: list[str],
    data_file_add: list[str],
    data_file_remove: list[str],
    fix_map_palette_settings: bool = False,
    remove_missing_files: bool = False,
    error: bool = False,
    verbose: bool = False,
) -> SceneFileUpdate:
    """
    Build parameters.

    Args:
        input_scene_file: the input scene file
        output_scene_file: the new scene file to create
        scene_name_or_number: name or number (starting at one) of the scene in the\
            scene file
        copy_map_one_palette: Copy palettes settings from first map to all maps in\
            a data file
        data_file_add: Add a data file to scene's loaded files
        data_file_remove: Remove a data file from scene's loaded files
        fix_map_palette_settings: Fix palette settings for files with change in\
            number of maps
        remove_missing_files: Remove missing files from SpecFile
        error: Abort command if there is an error performing any of the operations\
            on the scene file
        verbose: Print names of files that have palettes updated

    Returns:
        Parameter dictionary.
    """
    params: SceneFileUpdate = {
        "@type": "workbench/scene_file_update",
        "input_scene_file": input_scene_file,
        "output_scene_file": output_scene_file,
        "scene_name_or_number": scene_name_or_number,
        "copy_map_one_palette": copy_map_one_palette,
        "data_file_add": data_file_add,
        "data_file_remove": data_file_remove,
        "fix_map_palette_settings": fix_map_palette_settings,
        "remove_missing_files": remove_missing_files,
        "error": error,
        "verbose": verbose,
    }
    return params

def scene_file_update_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SceneFileUpdate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_scene_file", None) is None:
        raise StyxValidationError("`input_scene_file` must not be None")
    if not isinstance(params["input_scene_file"], str):
        raise StyxValidationError(f'`input_scene_file` has the wrong type: Received `{type(params["input_scene_file"])}` expected `str`')
    if params.get("output_scene_file", None) is None:
        raise StyxValidationError("`output_scene_file` must not be None")
    if not isinstance(params["output_scene_file"], str):
        raise StyxValidationError(f'`output_scene_file` has the wrong type: Received `{type(params["output_scene_file"])}` expected `str`')
    if params.get("scene_name_or_number", None) is None:
        raise StyxValidationError("`scene_name_or_number` must not be None")
    if not isinstance(params["scene_name_or_number"], str):
        raise StyxValidationError(f'`scene_name_or_number` has the wrong type: Received `{type(params["scene_name_or_number"])}` expected `str`')
    if params.get("fix_map_palette_settings", None) is not None:
        if not isinstance(params["fix_map_palette_settings"], bool):
            raise StyxValidationError(f'`fix_map_palette_settings` has the wrong type: Received `{type(params["fix_map_palette_settings"])}` expected `bool`')
    if params.get("remove_missing_files", None) is not None:
        if not isinstance(params["remove_missing_files"], bool):
            raise StyxValidationError(f'`remove_missing_files` has the wrong type: Received `{type(params["remove_missing_files"])}` expected `bool`')
    if params.get("error", None) is not None:
        if not isinstance(params["error"], bool):
            raise StyxValidationError(f'`error` has the wrong type: Received `{type(params["error"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("copy_map_one_palette", None) is None:
        raise StyxValidationError("`copy_map_one_palette` must not be None")
    if not isinstance(params["copy_map_one_palette"], list):
        raise StyxValidationError(f'`copy_map_one_palette` has the wrong type: Received `{type(params["copy_map_one_palette"])}` expected `list[str]`')
    for e in params["copy_map_one_palette"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`copy_map_one_palette` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("data_file_add", None) is None:
        raise StyxValidationError("`data_file_add` must not be None")
    if not isinstance(params["data_file_add"], list):
        raise StyxValidationError(f'`data_file_add` has the wrong type: Received `{type(params["data_file_add"])}` expected `list[str]`')
    for e_2 in params["data_file_add"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`data_file_add` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("data_file_remove", None) is None:
        raise StyxValidationError("`data_file_remove` must not be None")
    if not isinstance(params["data_file_remove"], list):
        raise StyxValidationError(f'`data_file_remove` has the wrong type: Received `{type(params["data_file_remove"])}` expected `list[str]`')
    for e_3 in params["data_file_remove"]:
        if not isinstance(e_3, str):
            raise StyxValidationError(f'`data_file_remove` has the wrong type: Received `{type(e_3)}` expected `list[str]`')

def scene_file_update_cargs(params: SceneFileUpdate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-scene-file-update")
    cargs.append(params["input_scene_file"])
    cargs.append(params["output_scene_file"])
    cargs.append(params["scene_name_or_number"])
    if params.get("fix_map_palette_settings"):
        cargs.append("-fix-map-palette-settings")
    if params.get("remove_missing_files"):
        cargs.append("-remove-missing-files")
    if params.get("error"):
        cargs.append("-error")
    if params.get("verbose"):
        cargs.append("-verbose")
    for item0 in params["copy_map_one_palette"]:
        cargs.append("-copy-map-one-palette")
        cargs.append(item0)
    for item1 in params["data_file_add"]:
        cargs.append("-data-file-add")
        cargs.append(item1)
    for item2 in params["data_file_remove"]:
        cargs.append("-data-file-remove")
        cargs.append(item2)
    return cargs

def scene_file_update_outputs(params: SceneFileUpdate, execution: Execution) -> SceneFileUpdateOutputs:
    """Build the SceneFileUpdateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SceneFileUpdateOutputs(
        root=root_v,
    )

def scene_file_update_execute(params: SceneFileUpdate, runner: Runner | None = None) -> SceneFileUpdateOutputs:
    """
    UPDATE SCENE FILE

    This command will update a scene for specific changes in data files.

"-fix-map-palette-settings" will find all data files that have had a change in the number of maps since the scene scene was created.  If the file has its "Apply to All Maps" property enabled, the palette setting in the first map is copied to all maps in the file.  Note: This modifies the palette settings for the file in the scene (data file is NOT modified).

"-copy-map-one-palette" will copy the palette settings from the first map to all other maps in a data file.  This option is typically used when the number of maps in a data file changes.  It changes the palette settings in the scene that are applied to the data file when the scene is loaded (the data file is not modified).  The name of the data file specified on the command line is matched to the end of file names in the scene.  This allows matching multiple files if their names end with the same characters.  It also allows including a relative path when there is more than one file with the same name but in different paths and only one of the files to be updated.

"-remove-missing-files" Any files that fail to load when the scene is read will be removed from the scene.  Thus, if one deletes files prior to running with this option, the deleted files are removed from the scene.  

"-error" If this option is provided and there is an error while performing any of the scene operations, the command will immediately cease processing and the output scene file will not be created.   Otherwise any errors will be listed after the command finishes.


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    scene_file_update_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SCENE_FILE_UPDATE_METADATA)
    execution.params(params)
    args = scene_file_update_cargs(params, execution)
    out = scene_file_update_outputs(params, execution)
    execution.run(args)
    return out

def scene_file_update(
    input_scene_file: str,
    output_scene_file: str,
    scene_name_or_number: str,
    copy_map_one_palette: list[str],
    data_file_add: list[str],
    data_file_remove: list[str],
    fix_map_palette_settings: bool = False,
    remove_missing_files: bool = False,
    error: bool = False,
    verbose: bool = False,
    runner: Runner | None = None,
) -> SceneFileUpdateOutputs:
    """
    UPDATE SCENE FILE

    This command will update a scene for specific changes in data files.

"-fix-map-palette-settings" will find all data files that have had a change in the number of maps since the scene scene was created.  If the file has its "Apply to All Maps" property enabled, the palette setting in the first map is copied to all maps in the file.  Note: This modifies the palette settings for the file in the scene (data file is NOT modified).

"-copy-map-one-palette" will copy the palette settings from the first map to all other maps in a data file.  This option is typically used when the number of maps in a data file changes.  It changes the palette settings in the scene that are applied to the data file when the scene is loaded (the data file is not modified).  The name of the data file specified on the command line is matched to the end of file names in the scene.  This allows matching multiple files if their names end with the same characters.  It also allows including a relative path when there is more than one file with the same name but in different paths and only one of the files to be updated.

"-remove-missing-files" Any files that fail to load when the scene is read will be removed from the scene.  Thus, if one deletes files prior to running with this option, the deleted files are removed from the scene.  

"-error" If this option is provided and there is an error while performing any of the scene operations, the command will immediately cease processing and the output scene file will not be created.   Otherwise any errors will be listed after the command finishes.


    Args:
        input_scene_file: the input scene file
        output_scene_file: the new scene file to create
        scene_name_or_number: name or number (starting at one) of the scene in the\
            scene file
        copy_map_one_palette: Copy palettes settings from first map to all maps in\
            a data file
        data_file_add: Add a data file to scene's loaded files
        data_file_remove: Remove a data file from scene's loaded files
        fix_map_palette_settings: Fix palette settings for files with change in\
            number of maps
        remove_missing_files: Remove missing files from SpecFile
        error: Abort command if there is an error performing any of the operations\
            on the scene file
        verbose: Print names of files that have palettes updated
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = scene_file_update_params(
        input_scene_file=input_scene_file,
        output_scene_file=output_scene_file,
        scene_name_or_number=scene_name_or_number,
        copy_map_one_palette=copy_map_one_palette,
        data_file_add=data_file_add,
        data_file_remove=data_file_remove,
        fix_map_palette_settings=fix_map_palette_settings,
        remove_missing_files=remove_missing_files,
        error=error,
        verbose=verbose,
    )
    return scene_file_update_execute(params, runner)

__all__ = [
    "SceneFileUpdate",
    "SceneFileUpdateOutputs",
    "SCENE_FILE_UPDATE_METADATA",
    "scene_file_update_cargs",
    "scene_file_update_outputs",
    "scene_file_update_params",
    "scene_file_update_execute",
    "scene_file_update_validate",
    "scene_file_update",
]