# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GIFTI_ALL_LABELS_TO_ROIS_METADATA = Metadata(
    id="gifti_all_labels_to_rois",
    name="MAKE ROIS FROM ALL LABELS IN A GIFTI COLUMN",
    package="workbench",
)

GiftiAllLabelsToRois = typing.TypedDict(
    "GiftiAllLabelsToRois",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/gifti_all_labels_to_rois"]],
        "label_in": InputPathType,
        "map": str,
        "metric_out": str,
    },
)

@dataclasses.dataclass
class GiftiAllLabelsToRoisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric file"""

def gifti_all_labels_to_rois_params(
    label_in: InputPathType,
    map: str,
    metric_out: str,
) -> GiftiAllLabelsToRois:
    """
    Build parameters.

    Args:
        label_in: the input gifti label file
        map: the number or name of the label map to use
        metric_out: the output metric file

    Returns:
        Parameter dictionary.
    """
    params: GiftiAllLabelsToRois = {
        "@type": "workbench/gifti_all_labels_to_rois",
        "label_in": label_in,
        "map": map,
        "metric_out": metric_out,
    }
    return params

def gifti_all_labels_to_rois_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GiftiAllLabelsToRois."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')

def gifti_all_labels_to_rois_cargs(params: GiftiAllLabelsToRois, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-gifti-all-labels-to-rois")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["map"])
    cargs.append(params["metric_out"])
    return cargs

def gifti_all_labels_to_rois_outputs(params: GiftiAllLabelsToRois, execution: Execution) -> GiftiAllLabelsToRoisOutputs:
    """Build the GiftiAllLabelsToRoisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return GiftiAllLabelsToRoisOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def gifti_all_labels_to_rois_execute(params: GiftiAllLabelsToRois, runner: Runner | None = None) -> GiftiAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A GIFTI COLUMN

    The output metric file has a column for each label in the specified input map, other than the ??? label, each of which contains an ROI of all vertices that are set to the corresponding label.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gifti_all_labels_to_rois_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GIFTI_ALL_LABELS_TO_ROIS_METADATA)
    execution.params(params)
    args = gifti_all_labels_to_rois_cargs(params, execution)
    out = gifti_all_labels_to_rois_outputs(params, execution)
    execution.run(args)
    return out

def gifti_all_labels_to_rois(
    label_in: InputPathType,
    map: str,
    metric_out: str,
    runner: Runner | None = None,
) -> GiftiAllLabelsToRoisOutputs:
    """
    MAKE ROIS FROM ALL LABELS IN A GIFTI COLUMN

    The output metric file has a column for each label in the specified input map, other than the ??? label, each of which contains an ROI of all vertices that are set to the corresponding label.

    Args:
        label_in: the input gifti label file
        map: the number or name of the label map to use
        metric_out: the output metric file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gifti_all_labels_to_rois_params(
        label_in=label_in,
        map=map,
        metric_out=metric_out,
    )
    return gifti_all_labels_to_rois_execute(params, runner)

__all__ = [
    "GiftiAllLabelsToRois",
    "GiftiAllLabelsToRoisOutputs",
    "GIFTI_ALL_LABELS_TO_ROIS_METADATA",
    "gifti_all_labels_to_rois_cargs",
    "gifti_all_labels_to_rois_outputs",
    "gifti_all_labels_to_rois_params",
    "gifti_all_labels_to_rois_execute",
    "gifti_all_labels_to_rois_validate",
    "gifti_all_labels_to_rois",
]