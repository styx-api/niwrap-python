# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_SMOOTHING_METADATA = Metadata(
    id="metric_smoothing",
    name="SMOOTH A METRIC FILE",
    package="workbench",
)

class MetricSmoothingRoi(typing.TypedDict):
    roi_metric: InputPathType
    """the roi to smooth within, as a metric"""
    match_columns: typing.NotRequired[bool]
    """for each input column, use the corresponding column from the roi"""

MetricSmoothing = typing.TypedDict(
    "MetricSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_smoothing"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "smoothing_kernel": float,
        "metric_out": str,
        "fwhm": typing.NotRequired[bool],
        "roi": typing.NotRequired[MetricSmoothingRoi],
        "fix_zeros": typing.NotRequired[bool],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
        "method": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_smoothing_params(
    surface: InputPathType,
    metric_in: InputPathType,
    smoothing_kernel: float,
    metric_out: str,
    fwhm: bool = False,
    roi: MetricSmoothingRoi | None = None,
    fix_zeros: bool = False,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    method: str | None = None,
) -> MetricSmoothing:
    """
    Build parameters.

    Args:
        surface: the surface to smooth on
        metric_in: the metric to smooth
        smoothing_kernel: the size of the gaussian smoothing kernel in mm, as sigma\
            by default
        metric_out: the output metric
        fwhm: kernel size is FWHM, not sigma
        roi: select a region of interest to smooth
        fix_zeros: treat zero values as not being data
        column: select a single column to smooth
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        method: select smoothing method, default GEO_GAUSS_AREA

    Returns:
        Parameter dictionary.
    """
    params: MetricSmoothing = {
        "@type": "workbench/metric_smoothing",
        "surface": surface,
        "metric_in": metric_in,
        "smoothing_kernel": smoothing_kernel,
        "metric_out": metric_out,
        "fwhm": fwhm,
        "fix_zeros": fix_zeros,
    }
    if roi is not None:
        params["roi"] = roi
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    if method is not None:
        params["method"] = method
    return params

def metric_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("smoothing_kernel", None) is None:
        raise StyxValidationError("`smoothing_kernel` must not be None")
    if not isinstance(params["smoothing_kernel"], (float, int)):
        raise StyxValidationError(f'`smoothing_kernel` has the wrong type: Received `{type(params["smoothing_kernel"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], bool):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi"])}\'')
        if params["roi"].get("roi_metric", None) is None:
            raise StyxValidationError("`roi_metric` must not be None")
        if not isinstance(params["roi"]["roi_metric"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi_metric` has the wrong type: Received `{type(params["roi"]["roi_metric"])}` expected `InputPathType`')
        if params["roi"].get("match_columns", None) is not None:
            if not isinstance(params["roi"]["match_columns"], bool):
                raise StyxValidationError(f'`match_columns` has the wrong type: Received `{type(params["roi"]["match_columns"])}` expected `bool`')
    if params.get("fix_zeros", None) is not None:
        if not isinstance(params["fix_zeros"], bool):
            raise StyxValidationError(f'`fix_zeros` has the wrong type: Received `{type(params["fix_zeros"])}` expected `bool`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("method", None) is not None:
        if not isinstance(params["method"], str):
            raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')

def metric_smoothing_cargs(params: MetricSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-smoothing")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(str(params["smoothing_kernel"]))
    cargs.append(params["metric_out"])
    if params.get("fwhm"):
        cargs.append("-fwhm")
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0["roi_metric"]))
        if v_0.get("match_columns"):
            cargs.append("-match-columns")
    if params.get("fix_zeros"):
        cargs.append("-fix-zeros")
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    v_2 = params.get("corrected_areas")
    if v_2 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("method")
    if v_3 is not None:
        cargs.append("-method")
        cargs.append(v_3)
    return cargs

def metric_smoothing_outputs(params: MetricSmoothing, execution: Execution) -> MetricSmoothingOutputs:
    """Build the MetricSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricSmoothingOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_smoothing_execute(params: MetricSmoothing, runner: Runner | None = None) -> MetricSmoothingOutputs:
    """
    SMOOTH A METRIC FILE

    Smooth a metric file on a surface.  By default, smooths all input columns on the entire surface, specify -column to use only one input column, and -roi to smooth only where the roi metric is greater than 0, outputting zeros elsewhere.

When using -roi, input data outside the ROI is not used to compute the smoothed values.  By default, the first column of the roi metric is used for all input columns.  When -match-columns is specified to the -roi option, the input and roi metrics must have the same number of columns, and for each input column's index, the same column index is used in the roi metric.  If the -match-columns option to -roi is used while the -column option is also used, the number of columns must match between the roi and input metric, and it will use the roi column with the index of the selected input column.

The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the vertex.  This is useful for zeros that indicate lack of information, preventing them from pulling down the intensity of nearby vertices, while giving the zero an extrapolated value.

The -corrected-areas option is intended for when it is unavoidable to smooth on a group average surface, it is only an approximate correction for the reduction of structure in a group average surface.  It is better to smooth the data on individuals before averaging, when feasible.

Valid values for <method> are:

GEO_GAUSS_AREA - uses a geodesic gaussian kernel, and normalizes based on vertex area in order to work more reliably on irregular surfaces

GEO_GAUSS_EQUAL - uses a geodesic gaussian kernel, and normalizes assuming each vertex has equal importance

GEO_GAUSS - matches geodesic gaussian smoothing from caret5, but does not check kernels for having unequal importance

The GEO_GAUSS_AREA method is the default because it is usually the correct choice.  GEO_GAUSS_EQUAL may be the correct choice when the sum of vertex values is more meaningful then the surface integral (sum of values .* areas), for instance when smoothing vertex areas (the sum is the total surface area, while the surface integral is the sum of squares of the vertex areas).  The GEO_GAUSS method is not recommended, it exists mainly to replicate methods of studies done with caret5's geodesic smoothing.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_SMOOTHING_METADATA)
    execution.params(params)
    args = metric_smoothing_cargs(params, execution)
    out = metric_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def metric_smoothing(
    surface: InputPathType,
    metric_in: InputPathType,
    smoothing_kernel: float,
    metric_out: str,
    fwhm: bool = False,
    roi: MetricSmoothingRoi | None = None,
    fix_zeros: bool = False,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    method: str | None = None,
    runner: Runner | None = None,
) -> MetricSmoothingOutputs:
    """
    SMOOTH A METRIC FILE

    Smooth a metric file on a surface.  By default, smooths all input columns on the entire surface, specify -column to use only one input column, and -roi to smooth only where the roi metric is greater than 0, outputting zeros elsewhere.

When using -roi, input data outside the ROI is not used to compute the smoothed values.  By default, the first column of the roi metric is used for all input columns.  When -match-columns is specified to the -roi option, the input and roi metrics must have the same number of columns, and for each input column's index, the same column index is used in the roi metric.  If the -match-columns option to -roi is used while the -column option is also used, the number of columns must match between the roi and input metric, and it will use the roi column with the index of the selected input column.

The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the vertex.  This is useful for zeros that indicate lack of information, preventing them from pulling down the intensity of nearby vertices, while giving the zero an extrapolated value.

The -corrected-areas option is intended for when it is unavoidable to smooth on a group average surface, it is only an approximate correction for the reduction of structure in a group average surface.  It is better to smooth the data on individuals before averaging, when feasible.

Valid values for <method> are:

GEO_GAUSS_AREA - uses a geodesic gaussian kernel, and normalizes based on vertex area in order to work more reliably on irregular surfaces

GEO_GAUSS_EQUAL - uses a geodesic gaussian kernel, and normalizes assuming each vertex has equal importance

GEO_GAUSS - matches geodesic gaussian smoothing from caret5, but does not check kernels for having unequal importance

The GEO_GAUSS_AREA method is the default because it is usually the correct choice.  GEO_GAUSS_EQUAL may be the correct choice when the sum of vertex values is more meaningful then the surface integral (sum of values .* areas), for instance when smoothing vertex areas (the sum is the total surface area, while the surface integral is the sum of squares of the vertex areas).  The GEO_GAUSS method is not recommended, it exists mainly to replicate methods of studies done with caret5's geodesic smoothing.

    Args:
        surface: the surface to smooth on
        metric_in: the metric to smooth
        smoothing_kernel: the size of the gaussian smoothing kernel in mm, as sigma\
            by default
        metric_out: the output metric
        fwhm: kernel size is FWHM, not sigma
        roi: select a region of interest to smooth
        fix_zeros: treat zero values as not being data
        column: select a single column to smooth
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        method: select smoothing method, default GEO_GAUSS_AREA
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_smoothing_params(
        surface=surface,
        metric_in=metric_in,
        smoothing_kernel=smoothing_kernel,
        metric_out=metric_out,
        fwhm=fwhm,
        roi=roi,
        fix_zeros=fix_zeros,
        column=column,
        corrected_areas=corrected_areas,
        method=method,
    )
    return metric_smoothing_execute(params, runner)

__all__ = [
    "MetricSmoothing",
    "MetricSmoothingOutputs",
    "METRIC_SMOOTHING_METADATA",
    "metric_smoothing_cargs",
    "metric_smoothing_outputs",
    "metric_smoothing_params",
    "metric_smoothing_execute",
    "metric_smoothing_validate",
    "metric_smoothing",
]