# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_LENGTH_METADATA = Metadata(
    id="border_length",
    name="REPORT LENGTH OF BORDERS",
    package="workbench",
)

BorderLength = typing.TypedDict(
    "BorderLength",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_length"]],
        "border": InputPathType,
        "surface": InputPathType,
        "corrected_areas": typing.NotRequired[InputPathType],
        "separate_pieces": typing.NotRequired[bool],
        "hide_border_name": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BorderLengthOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def border_length_params(
    border: InputPathType,
    surface: InputPathType,
    corrected_areas: InputPathType | None = None,
    separate_pieces: bool = False,
    hide_border_name: bool = False,
) -> BorderLength:
    """
    Build parameters.

    Args:
        border: the input border file
        surface: the surface to measure the borders on
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        separate_pieces: report lengths for multi-part borders as separate numbers
        hide_border_name: don't print border name before each output

    Returns:
        Parameter dictionary.
    """
    params: BorderLength = {
        "@type": "workbench/border_length",
        "border": border,
        "surface": surface,
        "separate_pieces": separate_pieces,
        "hide_border_name": hide_border_name,
    }
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def border_length_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderLength."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("border", None) is None:
        raise StyxValidationError("`border` must not be None")
    if not isinstance(params["border"], (pathlib.Path, str)):
        raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("separate_pieces", None) is not None:
        if not isinstance(params["separate_pieces"], bool):
            raise StyxValidationError(f'`separate_pieces` has the wrong type: Received `{type(params["separate_pieces"])}` expected `bool`')
    if params.get("hide_border_name", None) is not None:
        if not isinstance(params["hide_border_name"], bool):
            raise StyxValidationError(f'`hide_border_name` has the wrong type: Received `{type(params["hide_border_name"])}` expected `bool`')

def border_length_cargs(params: BorderLength, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-length")
    cargs.append(execution.input_file(params["border"]))
    cargs.append(execution.input_file(params["surface"]))
    v_0 = params.get("corrected_areas")
    if v_0 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_0))
    if params.get("separate_pieces"):
        cargs.append("-separate-pieces")
    if params.get("hide_border_name"):
        cargs.append("-hide-border-name")
    return cargs

def border_length_outputs(params: BorderLength, execution: Execution) -> BorderLengthOutputs:
    """Build the BorderLengthOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BorderLengthOutputs(
        root=root_v,
    )

def border_length_execute(params: BorderLength, runner: Runner | None = None) -> BorderLengthOutputs:
    """
    REPORT LENGTH OF BORDERS

    For each border, print its length along the surface, in mm.  If a border has multiple parts, their lengths are summed before printing, unless -separate-pieces is specified.

The -corrected-areas option is intended for when the length is not meaningfully measurable on individual surfaces, it is only an approximate correction for the reduction in structure of a group average surface.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_length_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_LENGTH_METADATA)
    execution.params(params)
    args = border_length_cargs(params, execution)
    out = border_length_outputs(params, execution)
    execution.run(args)
    return out

def border_length(
    border: InputPathType,
    surface: InputPathType,
    corrected_areas: InputPathType | None = None,
    separate_pieces: bool = False,
    hide_border_name: bool = False,
    runner: Runner | None = None,
) -> BorderLengthOutputs:
    """
    REPORT LENGTH OF BORDERS

    For each border, print its length along the surface, in mm.  If a border has multiple parts, their lengths are summed before printing, unless -separate-pieces is specified.

The -corrected-areas option is intended for when the length is not meaningfully measurable on individual surfaces, it is only an approximate correction for the reduction in structure of a group average surface.

    Args:
        border: the input border file
        surface: the surface to measure the borders on
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        separate_pieces: report lengths for multi-part borders as separate numbers
        hide_border_name: don't print border name before each output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_length_params(
        border=border,
        surface=surface,
        corrected_areas=corrected_areas,
        separate_pieces=separate_pieces,
        hide_border_name=hide_border_name,
    )
    return border_length_execute(params, runner)

__all__ = [
    "BorderLength",
    "BorderLengthOutputs",
    "BORDER_LENGTH_METADATA",
    "border_length_cargs",
    "border_length_outputs",
    "border_length_params",
    "border_length_execute",
    "border_length_validate",
    "border_length",
]