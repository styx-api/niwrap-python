# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_FIND_CLUSTERS_METADATA = Metadata(
    id="volume_find_clusters",
    name="FILTER CLUSTERS BY VOLUME",
    package="workbench",
)

VolumeFindClusters = typing.TypedDict(
    "VolumeFindClusters",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_find_clusters"]],
        "volume_in": InputPathType,
        "value_threshold": float,
        "minimum_volume": float,
        "volume_out": str,
        "less_than": typing.NotRequired[bool],
        "roi": typing.NotRequired[InputPathType],
        "subvolume": typing.NotRequired[str],
        "size_ratio": typing.NotRequired[float],
        "distance": typing.NotRequired[float],
        "start": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class VolumeFindClustersOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_find_clusters_params(
    volume_in: InputPathType,
    value_threshold: float,
    minimum_volume: float,
    volume_out: str,
    less_than: bool = False,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
    size_ratio: float | None = None,
    distance: float | None = None,
    start: int | None = None,
) -> VolumeFindClusters:
    """
    Build parameters.

    Args:
        volume_in: the input volume
        value_threshold: threshold for data values
        minimum_volume: threshold for cluster volume, in mm^3
        volume_out: the output volume
        less_than: find values less than <value-threshold>, rather than greater
        roi: select a region of interest
        subvolume: select a single subvolume
        size_ratio: ignore clusters smaller than a given fraction of the largest\
            cluster in map
        distance: ignore clusters further than a given distance from the largest\
            cluster
        start: start labeling clusters from a value other than 1

    Returns:
        Parameter dictionary.
    """
    params: VolumeFindClusters = {
        "@type": "workbench/volume_find_clusters",
        "volume_in": volume_in,
        "value_threshold": value_threshold,
        "minimum_volume": minimum_volume,
        "volume_out": volume_out,
        "less_than": less_than,
    }
    if roi is not None:
        params["roi"] = roi
    if subvolume is not None:
        params["subvolume"] = subvolume
    if size_ratio is not None:
        params["size_ratio"] = size_ratio
    if distance is not None:
        params["distance"] = distance
    if start is not None:
        params["start"] = start
    return params

def volume_find_clusters_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeFindClusters."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("value_threshold", None) is None:
        raise StyxValidationError("`value_threshold` must not be None")
    if not isinstance(params["value_threshold"], (float, int)):
        raise StyxValidationError(f'`value_threshold` has the wrong type: Received `{type(params["value_threshold"])}` expected `float`')
    if params.get("minimum_volume", None) is None:
        raise StyxValidationError("`minimum_volume` must not be None")
    if not isinstance(params["minimum_volume"], (float, int)):
        raise StyxValidationError(f'`minimum_volume` has the wrong type: Received `{type(params["minimum_volume"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("less_than", None) is not None:
        if not isinstance(params["less_than"], bool):
            raise StyxValidationError(f'`less_than` has the wrong type: Received `{type(params["less_than"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')
    if params.get("size_ratio", None) is not None:
        if not isinstance(params["size_ratio"], (float, int)):
            raise StyxValidationError(f'`size_ratio` has the wrong type: Received `{type(params["size_ratio"])}` expected `float`')
    if params.get("distance", None) is not None:
        if not isinstance(params["distance"], (float, int)):
            raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("start", None) is not None:
        if not isinstance(params["start"], int):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["start"])}` expected `int`')

def volume_find_clusters_cargs(params: VolumeFindClusters, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-find-clusters")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(str(params["value_threshold"]))
    cargs.append(str(params["minimum_volume"]))
    cargs.append(params["volume_out"])
    if params.get("less_than"):
        cargs.append("-less-than")
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("subvolume")
    if v_1 is not None:
        cargs.append("-subvolume")
        cargs.append(v_1)
    v_2 = params.get("size_ratio")
    if v_2 is not None:
        cargs.append("-size-ratio")
        cargs.append(str(v_2))
    v_3 = params.get("distance")
    if v_3 is not None:
        cargs.append("-distance")
        cargs.append(str(v_3))
    v_4 = params.get("start")
    if v_4 is not None:
        cargs.append("-start")
        cargs.append(str(v_4))
    return cargs

def volume_find_clusters_outputs(params: VolumeFindClusters, execution: Execution) -> VolumeFindClustersOutputs:
    """Build the VolumeFindClustersOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeFindClustersOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_find_clusters_execute(params: VolumeFindClusters, runner: Runner | None = None) -> VolumeFindClustersOutputs:
    """
    FILTER CLUSTERS BY VOLUME

    Outputs a volume with nonzero integers for all voxels within a large enough cluster, and zeros elsewhere.  The integers denote cluster membership (by default, first cluster found will use value 1, second cluster 2, etc).  Cluster values are not reused across frames of the output, but instead keep counting up.  By default, values greater than <value-threshold> are considered to be in a cluster, use -less-than to test for values less than the threshold.  To apply this as a mask to the data, or to do more complicated thresholding, see -volume-math.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_find_clusters_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_FIND_CLUSTERS_METADATA)
    execution.params(params)
    args = volume_find_clusters_cargs(params, execution)
    out = volume_find_clusters_outputs(params, execution)
    execution.run(args)
    return out

def volume_find_clusters(
    volume_in: InputPathType,
    value_threshold: float,
    minimum_volume: float,
    volume_out: str,
    less_than: bool = False,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
    size_ratio: float | None = None,
    distance: float | None = None,
    start: int | None = None,
    runner: Runner | None = None,
) -> VolumeFindClustersOutputs:
    """
    FILTER CLUSTERS BY VOLUME

    Outputs a volume with nonzero integers for all voxels within a large enough cluster, and zeros elsewhere.  The integers denote cluster membership (by default, first cluster found will use value 1, second cluster 2, etc).  Cluster values are not reused across frames of the output, but instead keep counting up.  By default, values greater than <value-threshold> are considered to be in a cluster, use -less-than to test for values less than the threshold.  To apply this as a mask to the data, or to do more complicated thresholding, see -volume-math.

    Args:
        volume_in: the input volume
        value_threshold: threshold for data values
        minimum_volume: threshold for cluster volume, in mm^3
        volume_out: the output volume
        less_than: find values less than <value-threshold>, rather than greater
        roi: select a region of interest
        subvolume: select a single subvolume
        size_ratio: ignore clusters smaller than a given fraction of the largest\
            cluster in map
        distance: ignore clusters further than a given distance from the largest\
            cluster
        start: start labeling clusters from a value other than 1
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_find_clusters_params(
        volume_in=volume_in,
        value_threshold=value_threshold,
        minimum_volume=minimum_volume,
        volume_out=volume_out,
        less_than=less_than,
        roi=roi,
        subvolume=subvolume,
        size_ratio=size_ratio,
        distance=distance,
        start=start,
    )
    return volume_find_clusters_execute(params, runner)

__all__ = [
    "VolumeFindClusters",
    "VolumeFindClustersOutputs",
    "VOLUME_FIND_CLUSTERS_METADATA",
    "volume_find_clusters_cargs",
    "volume_find_clusters_outputs",
    "volume_find_clusters_params",
    "volume_find_clusters_execute",
    "volume_find_clusters_validate",
    "volume_find_clusters",
]