# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_FLIP_NORMALS_METADATA = Metadata(
    id="surface_flip_normals",
    name="FLIP ALL TILES ON A SURFACE",
    package="workbench",
)

SurfaceFlipNormals = typing.TypedDict(
    "SurfaceFlipNormals",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_flip_normals"]],
        "surface": InputPathType,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceFlipNormalsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """the output surface"""

def surface_flip_normals_params(
    surface: InputPathType,
    surface_out: str,
) -> SurfaceFlipNormals:
    """
    Build parameters.

    Args:
        surface: the surface to flip the normals of
        surface_out: the output surface

    Returns:
        Parameter dictionary.
    """
    params: SurfaceFlipNormals = {
        "@type": "workbench/surface_flip_normals",
        "surface": surface,
        "surface_out": surface_out,
    }
    return params

def surface_flip_normals_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceFlipNormals."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_flip_normals_cargs(params: SurfaceFlipNormals, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-flip-normals")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_flip_normals_outputs(params: SurfaceFlipNormals, execution: Execution) -> SurfaceFlipNormalsOutputs:
    """Build the SurfaceFlipNormalsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceFlipNormalsOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_flip_normals_execute(params: SurfaceFlipNormals, runner: Runner | None = None) -> SurfaceFlipNormalsOutputs:
    """
    FLIP ALL TILES ON A SURFACE

    Flips all triangles on a surface, resulting in surface normals being flipped the other direction (inward vs outward).  If you transform a surface with an affine that has negative determinant, or a warpfield that similarly flips the surface, you may end up with a surface that has normals pointing inwards, which may have display problems.  Using this command will solve that problem.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_flip_normals_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_FLIP_NORMALS_METADATA)
    execution.params(params)
    args = surface_flip_normals_cargs(params, execution)
    out = surface_flip_normals_outputs(params, execution)
    execution.run(args)
    return out

def surface_flip_normals(
    surface: InputPathType,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceFlipNormalsOutputs:
    """
    FLIP ALL TILES ON A SURFACE

    Flips all triangles on a surface, resulting in surface normals being flipped the other direction (inward vs outward).  If you transform a surface with an affine that has negative determinant, or a warpfield that similarly flips the surface, you may end up with a surface that has normals pointing inwards, which may have display problems.  Using this command will solve that problem.

    Args:
        surface: the surface to flip the normals of
        surface_out: the output surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_flip_normals_params(
        surface=surface,
        surface_out=surface_out,
    )
    return surface_flip_normals_execute(params, runner)

__all__ = [
    "SurfaceFlipNormals",
    "SurfaceFlipNormalsOutputs",
    "SURFACE_FLIP_NORMALS_METADATA",
    "surface_flip_normals_cargs",
    "surface_flip_normals_outputs",
    "surface_flip_normals_params",
    "surface_flip_normals_execute",
    "surface_flip_normals_validate",
    "surface_flip_normals",
]