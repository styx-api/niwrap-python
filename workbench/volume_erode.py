# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_ERODE_METADATA = Metadata(
    id="volume_erode",
    name="ERODE A VOLUME FILE",
    package="workbench",
)

VolumeErode = typing.TypedDict(
    "VolumeErode",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_erode"]],
        "volume": InputPathType,
        "distance": float,
        "volume_out": str,
        "roi": typing.NotRequired[InputPathType],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeErodeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_erode_params(
    volume: InputPathType,
    distance: float,
    volume_out: str,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
) -> VolumeErode:
    """
    Build parameters.

    Args:
        volume: the volume to erode
        distance: distance in mm to erode
        volume_out: the output volume
        roi: assume voxels outside this roi are nonzero
        subvolume: select a single subvolume to dilate

    Returns:
        Parameter dictionary.
    """
    params: VolumeErode = {
        "@type": "workbench/volume_erode",
        "volume": volume,
        "distance": distance,
        "volume_out": volume_out,
    }
    if roi is not None:
        params["roi"] = roi
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_erode_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeErode."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_erode_cargs(params: VolumeErode, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-erode")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("subvolume")
    if v_1 is not None:
        cargs.append("-subvolume")
        cargs.append(v_1)
    return cargs

def volume_erode_outputs(params: VolumeErode, execution: Execution) -> VolumeErodeOutputs:
    """Build the VolumeErodeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeErodeOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_erode_execute(params: VolumeErode, runner: Runner | None = None) -> VolumeErodeOutputs:
    """
    ERODE A VOLUME FILE

    Around each voxel with a value of zero, set surrounding voxels to zero.  The surrounding voxels are all face neighbors and all voxels within the specified distance (center to center).

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_erode_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_ERODE_METADATA)
    execution.params(params)
    args = volume_erode_cargs(params, execution)
    out = volume_erode_outputs(params, execution)
    execution.run(args)
    return out

def volume_erode(
    volume: InputPathType,
    distance: float,
    volume_out: str,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeErodeOutputs:
    """
    ERODE A VOLUME FILE

    Around each voxel with a value of zero, set surrounding voxels to zero.  The surrounding voxels are all face neighbors and all voxels within the specified distance (center to center).

    Args:
        volume: the volume to erode
        distance: distance in mm to erode
        volume_out: the output volume
        roi: assume voxels outside this roi are nonzero
        subvolume: select a single subvolume to dilate
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_erode_params(
        volume=volume,
        distance=distance,
        volume_out=volume_out,
        roi=roi,
        subvolume=subvolume,
    )
    return volume_erode_execute(params, runner)

__all__ = [
    "VolumeErode",
    "VolumeErodeOutputs",
    "VOLUME_ERODE_METADATA",
    "volume_erode_cargs",
    "volume_erode_outputs",
    "volume_erode_params",
    "volume_erode_execute",
    "volume_erode_validate",
    "volume_erode",
]