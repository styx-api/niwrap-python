# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_DILATE_METADATA = Metadata(
    id="volume_dilate",
    name="DILATE A VOLUME FILE",
    package="workbench",
)

class VolumeDilatePresmooth(typing.TypedDict):
    kernel: float
    """the size of gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class VolumeDilateGradExtrapolate(typing.TypedDict):
    presmooth: typing.NotRequired[VolumeDilatePresmooth]
    """apply presmoothing before computing gradient vectors, not recommended"""

VolumeDilate = typing.TypedDict(
    "VolumeDilate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_dilate"]],
        "volume": InputPathType,
        "distance": float,
        "method": str,
        "volume_out": str,
        "exponent": typing.NotRequired[float],
        "bad_voxel_roi": typing.NotRequired[InputPathType],
        "data_roi": typing.NotRequired[InputPathType],
        "subvolume": typing.NotRequired[str],
        "legacy_cutoff": typing.NotRequired[bool],
        "grad_extrapolate": typing.NotRequired[VolumeDilateGradExtrapolate],
    },
)

@dataclasses.dataclass
class VolumeDilateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_dilate_params(
    volume: InputPathType,
    distance: float,
    method: str,
    volume_out: str,
    exponent: float | None = None,
    bad_voxel_roi: InputPathType | None = None,
    data_roi: InputPathType | None = None,
    subvolume: str | None = None,
    legacy_cutoff: bool = False,
    grad_extrapolate: VolumeDilateGradExtrapolate | None = None,
) -> VolumeDilate:
    """
    Build parameters.

    Args:
        volume: the volume to dilate
        distance: distance in mm to dilate
        method: dilation method to use
        volume_out: the output volume
        exponent: use a different exponent in the weighting function
        bad_voxel_roi: specify an roi of voxels to overwrite, rather than voxels\
            with value zero
        data_roi: specify an roi of where there is data
        subvolume: select a single subvolume to dilate
        legacy_cutoff: use the v1.3.2 method of excluding voxels further than the\
            dilation distance when calculating the dilated value
        grad_extrapolate: additionally use the gradient to extrapolate, intended to\
            be used with WEIGHTED

    Returns:
        Parameter dictionary.
    """
    params: VolumeDilate = {
        "@type": "workbench/volume_dilate",
        "volume": volume,
        "distance": distance,
        "method": method,
        "volume_out": volume_out,
        "legacy_cutoff": legacy_cutoff,
    }
    if exponent is not None:
        params["exponent"] = exponent
    if bad_voxel_roi is not None:
        params["bad_voxel_roi"] = bad_voxel_roi
    if data_roi is not None:
        params["data_roi"] = data_roi
    if subvolume is not None:
        params["subvolume"] = subvolume
    if grad_extrapolate is not None:
        params["grad_extrapolate"] = grad_extrapolate
    return params

def volume_dilate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeDilate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("method", None) is None:
        raise StyxValidationError("`method` must not be None")
    if not isinstance(params["method"], str):
        raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("exponent", None) is not None:
        if not isinstance(params["exponent"], (float, int)):
            raise StyxValidationError(f'`exponent` has the wrong type: Received `{type(params["exponent"])}` expected `float`')
    if params.get("bad_voxel_roi", None) is not None:
        if not isinstance(params["bad_voxel_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bad_voxel_roi` has the wrong type: Received `{type(params["bad_voxel_roi"])}` expected `InputPathType`')
    if params.get("data_roi", None) is not None:
        if not isinstance(params["data_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`data_roi` has the wrong type: Received `{type(params["data_roi"])}` expected `InputPathType`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')
    if params.get("legacy_cutoff", None) is not None:
        if not isinstance(params["legacy_cutoff"], bool):
            raise StyxValidationError(f'`legacy_cutoff` has the wrong type: Received `{type(params["legacy_cutoff"])}` expected `bool`')
    if params.get("grad_extrapolate", None) is not None:
        if not isinstance(params["grad_extrapolate"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["grad_extrapolate"])}\'')
        if params["grad_extrapolate"].get("presmooth", None) is not None:
            if not isinstance(params["grad_extrapolate"]["presmooth"], dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(params["grad_extrapolate"]["presmooth"])}\'')
            if params["grad_extrapolate"]["presmooth"].get("kernel", None) is None:
                raise StyxValidationError("`kernel` must not be None")
            if not isinstance(params["grad_extrapolate"]["presmooth"]["kernel"], (float, int)):
                raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["grad_extrapolate"]["presmooth"]["kernel"])}` expected `float`')
            if params["grad_extrapolate"]["presmooth"].get("fwhm", None) is not None:
                if not isinstance(params["grad_extrapolate"]["presmooth"]["fwhm"], bool):
                    raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["grad_extrapolate"]["presmooth"]["fwhm"])}` expected `bool`')

def volume_dilate_cargs(params: VolumeDilate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-dilate")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["method"])
    cargs.append(params["volume_out"])
    v_0 = params.get("exponent")
    if v_0 is not None:
        cargs.append("-exponent")
        cargs.append(str(v_0))
    v_1 = params.get("bad_voxel_roi")
    if v_1 is not None:
        cargs.append("-bad-voxel-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("data_roi")
    if v_2 is not None:
        cargs.append("-data-roi")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("subvolume")
    if v_3 is not None:
        cargs.append("-subvolume")
        cargs.append(v_3)
    if params.get("legacy_cutoff"):
        cargs.append("-legacy-cutoff")
    v_4 = params.get("grad_extrapolate")
    if v_4 is not None:
        cargs.append("-grad-extrapolate")
        v_5 = v_4.get("presmooth")
        if v_5 is not None:
            cargs.append("-presmooth")
            cargs.append(str(v_5["kernel"]))
            if v_5.get("fwhm"):
                cargs.append("-fwhm")
    return cargs

def volume_dilate_outputs(params: VolumeDilate, execution: Execution) -> VolumeDilateOutputs:
    """Build the VolumeDilateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeDilateOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_dilate_execute(params: VolumeDilate, runner: Runner | None = None) -> VolumeDilateOutputs:
    """
    DILATE A VOLUME FILE

    For all voxels that are designated as bad, if they neighbor a non-bad voxel with data or are within the specified distance of such a voxel, replace the value in the bad voxel with a value calculated from nearby non-bad voxels that have data, otherwise set the value to zero.  No matter how small <distance> is, dilation will always use at least the face neighbor voxels.

By default, voxels that have data with the value 0 are bad, specify -bad-voxel-roi to only count voxels as bad if they are selected by the roi.  If -data-roi is not specified, all voxels are assumed to have data.

To get the behavior of version 1.3.2 or earlier, use '-legacy-cutoff -exponent 2'.

Valid values for <method> are:

NEAREST - use the value from the nearest good voxel
WEIGHTED - use a weighted average based on distance

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_dilate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_DILATE_METADATA)
    execution.params(params)
    args = volume_dilate_cargs(params, execution)
    out = volume_dilate_outputs(params, execution)
    execution.run(args)
    return out

def volume_dilate(
    volume: InputPathType,
    distance: float,
    method: str,
    volume_out: str,
    exponent: float | None = None,
    bad_voxel_roi: InputPathType | None = None,
    data_roi: InputPathType | None = None,
    subvolume: str | None = None,
    legacy_cutoff: bool = False,
    grad_extrapolate: VolumeDilateGradExtrapolate | None = None,
    runner: Runner | None = None,
) -> VolumeDilateOutputs:
    """
    DILATE A VOLUME FILE

    For all voxels that are designated as bad, if they neighbor a non-bad voxel with data or are within the specified distance of such a voxel, replace the value in the bad voxel with a value calculated from nearby non-bad voxels that have data, otherwise set the value to zero.  No matter how small <distance> is, dilation will always use at least the face neighbor voxels.

By default, voxels that have data with the value 0 are bad, specify -bad-voxel-roi to only count voxels as bad if they are selected by the roi.  If -data-roi is not specified, all voxels are assumed to have data.

To get the behavior of version 1.3.2 or earlier, use '-legacy-cutoff -exponent 2'.

Valid values for <method> are:

NEAREST - use the value from the nearest good voxel
WEIGHTED - use a weighted average based on distance

    Args:
        volume: the volume to dilate
        distance: distance in mm to dilate
        method: dilation method to use
        volume_out: the output volume
        exponent: use a different exponent in the weighting function
        bad_voxel_roi: specify an roi of voxels to overwrite, rather than voxels\
            with value zero
        data_roi: specify an roi of where there is data
        subvolume: select a single subvolume to dilate
        legacy_cutoff: use the v1.3.2 method of excluding voxels further than the\
            dilation distance when calculating the dilated value
        grad_extrapolate: additionally use the gradient to extrapolate, intended to\
            be used with WEIGHTED
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_dilate_params(
        volume=volume,
        distance=distance,
        method=method,
        volume_out=volume_out,
        exponent=exponent,
        bad_voxel_roi=bad_voxel_roi,
        data_roi=data_roi,
        subvolume=subvolume,
        legacy_cutoff=legacy_cutoff,
        grad_extrapolate=grad_extrapolate,
    )
    return volume_dilate_execute(params, runner)

__all__ = [
    "VolumeDilate",
    "VolumeDilateOutputs",
    "VOLUME_DILATE_METADATA",
    "volume_dilate_cargs",
    "volume_dilate_outputs",
    "volume_dilate_params",
    "volume_dilate_execute",
    "volume_dilate_validate",
    "volume_dilate",
]