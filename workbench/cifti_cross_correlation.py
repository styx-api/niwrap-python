# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_CROSS_CORRELATION_METADATA = Metadata(
    id="cifti_cross_correlation",
    name="CORRELATE A CIFTI FILE WITH ANOTHER CIFTI FILE",
    package="workbench",
)

CiftiCrossCorrelation = typing.TypedDict(
    "CiftiCrossCorrelation",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_cross_correlation"]],
        "cifti_a": InputPathType,
        "cifti_b": InputPathType,
        "cifti_out": str,
        "weights": typing.NotRequired[str],
        "fisher_z": typing.NotRequired[bool],
        "mem_limit": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class CiftiCrossCorrelationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""

def cifti_cross_correlation_params(
    cifti_a: InputPathType,
    cifti_b: InputPathType,
    cifti_out: str,
    weights: str | None = None,
    fisher_z: bool = False,
    mem_limit: float | None = None,
) -> CiftiCrossCorrelation:
    """
    Build parameters.

    Args:
        cifti_a: first input cifti file
        cifti_b: second input cifti file
        cifti_out: output cifti file
        weights: specify column weights
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        mem_limit: restrict memory usage

    Returns:
        Parameter dictionary.
    """
    params: CiftiCrossCorrelation = {
        "@type": "workbench/cifti_cross_correlation",
        "cifti_a": cifti_a,
        "cifti_b": cifti_b,
        "cifti_out": cifti_out,
        "fisher_z": fisher_z,
    }
    if weights is not None:
        params["weights"] = weights
    if mem_limit is not None:
        params["mem_limit"] = mem_limit
    return params

def cifti_cross_correlation_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiCrossCorrelation."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_a", None) is None:
        raise StyxValidationError("`cifti_a` must not be None")
    if not isinstance(params["cifti_a"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_a` has the wrong type: Received `{type(params["cifti_a"])}` expected `InputPathType`')
    if params.get("cifti_b", None) is None:
        raise StyxValidationError("`cifti_b` must not be None")
    if not isinstance(params["cifti_b"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_b` has the wrong type: Received `{type(params["cifti_b"])}` expected `InputPathType`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("weights", None) is not None:
        if not isinstance(params["weights"], str):
            raise StyxValidationError(f'`weights` has the wrong type: Received `{type(params["weights"])}` expected `str`')
    if params.get("fisher_z", None) is not None:
        if not isinstance(params["fisher_z"], bool):
            raise StyxValidationError(f'`fisher_z` has the wrong type: Received `{type(params["fisher_z"])}` expected `bool`')
    if params.get("mem_limit", None) is not None:
        if not isinstance(params["mem_limit"], (float, int)):
            raise StyxValidationError(f'`mem_limit` has the wrong type: Received `{type(params["mem_limit"])}` expected `float`')

def cifti_cross_correlation_cargs(params: CiftiCrossCorrelation, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-cross-correlation")
    cargs.append(execution.input_file(params["cifti_a"]))
    cargs.append(execution.input_file(params["cifti_b"]))
    cargs.append(params["cifti_out"])
    v_0 = params.get("weights")
    if v_0 is not None:
        cargs.append("-weights")
        cargs.append(v_0)
    if params.get("fisher_z"):
        cargs.append("-fisher-z")
    v_1 = params.get("mem_limit")
    if v_1 is not None:
        cargs.append("-mem-limit")
        cargs.append(str(v_1))
    return cargs

def cifti_cross_correlation_outputs(params: CiftiCrossCorrelation, execution: Execution) -> CiftiCrossCorrelationOutputs:
    """Build the CiftiCrossCorrelationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiCrossCorrelationOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_cross_correlation_execute(params: CiftiCrossCorrelation, runner: Runner | None = None) -> CiftiCrossCorrelationOutputs:
    """
    CORRELATE A CIFTI FILE WITH ANOTHER CIFTI FILE

    Correlates every row in <cifti-a> with every row in <cifti-b>.  The mapping along columns in <cifti-b> becomes the mapping along rows in the output.

When using the -fisher-z option, the output is NOT a Z-score, it is artanh(r), to do further math on this output, consider using -cifti-math.

Restricting the memory usage will make it calculate the output in chunks, by reading through <cifti-b> multiple times.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_cross_correlation_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_CROSS_CORRELATION_METADATA)
    execution.params(params)
    args = cifti_cross_correlation_cargs(params, execution)
    out = cifti_cross_correlation_outputs(params, execution)
    execution.run(args)
    return out

def cifti_cross_correlation(
    cifti_a: InputPathType,
    cifti_b: InputPathType,
    cifti_out: str,
    weights: str | None = None,
    fisher_z: bool = False,
    mem_limit: float | None = None,
    runner: Runner | None = None,
) -> CiftiCrossCorrelationOutputs:
    """
    CORRELATE A CIFTI FILE WITH ANOTHER CIFTI FILE

    Correlates every row in <cifti-a> with every row in <cifti-b>.  The mapping along columns in <cifti-b> becomes the mapping along rows in the output.

When using the -fisher-z option, the output is NOT a Z-score, it is artanh(r), to do further math on this output, consider using -cifti-math.

Restricting the memory usage will make it calculate the output in chunks, by reading through <cifti-b> multiple times.

    Args:
        cifti_a: first input cifti file
        cifti_b: second input cifti file
        cifti_out: output cifti file
        weights: specify column weights
        fisher_z: apply fisher small z transform (ie, artanh) to correlation
        mem_limit: restrict memory usage
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_cross_correlation_params(
        cifti_a=cifti_a,
        cifti_b=cifti_b,
        cifti_out=cifti_out,
        weights=weights,
        fisher_z=fisher_z,
        mem_limit=mem_limit,
    )
    return cifti_cross_correlation_execute(params, runner)

__all__ = [
    "CiftiCrossCorrelation",
    "CiftiCrossCorrelationOutputs",
    "CIFTI_CROSS_CORRELATION_METADATA",
    "cifti_cross_correlation_cargs",
    "cifti_cross_correlation_outputs",
    "cifti_cross_correlation_params",
    "cifti_cross_correlation_execute",
    "cifti_cross_correlation_validate",
    "cifti_cross_correlation",
]