# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_EXTREMA_METADATA = Metadata(
    id="metric_extrema",
    name="FIND EXTREMA IN A METRIC FILE",
    package="workbench",
)

class MetricExtremaPresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class MetricExtremaThreshold(typing.TypedDict):
    low: float
    """the largest value to consider for being a minimum"""
    high: float
    """the smallest value to consider for being a maximum"""

MetricExtrema = typing.TypedDict(
    "MetricExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_extrema"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "distance": float,
        "metric_out": str,
        "presmooth": typing.NotRequired[MetricExtremaPresmooth],
        "roi": typing.NotRequired[InputPathType],
        "threshold": typing.NotRequired[MetricExtremaThreshold],
        "sum_columns": typing.NotRequired[bool],
        "consolidate_mode": typing.NotRequired[bool],
        "only_maxima": typing.NotRequired[bool],
        "only_minima": typing.NotRequired[bool],
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MetricExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output extrema metric"""

def metric_extrema_params(
    surface: InputPathType,
    metric_in: InputPathType,
    distance: float,
    metric_out: str,
    presmooth: MetricExtremaPresmooth | None = None,
    roi: InputPathType | None = None,
    threshold: MetricExtremaThreshold | None = None,
    sum_columns: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
    column: str | None = None,
) -> MetricExtrema:
    """
    Build parameters.

    Args:
        surface: the surface to use for distance information
        metric_in: the metric to find the extrema of
        distance: the minimum distance between identified extrema of the same type
        metric_out: the output extrema metric
        presmooth: smooth the metric before finding extrema
        roi: ignore values outside the selected area
        threshold: ignore small extrema
        sum_columns: output the sum of the extrema columns instead of each column\
            separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima
        column: select a single column to find extrema in

    Returns:
        Parameter dictionary.
    """
    params: MetricExtrema = {
        "@type": "workbench/metric_extrema",
        "surface": surface,
        "metric_in": metric_in,
        "distance": distance,
        "metric_out": metric_out,
        "sum_columns": sum_columns,
        "consolidate_mode": consolidate_mode,
        "only_maxima": only_maxima,
        "only_minima": only_minima,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if threshold is not None:
        params["threshold"] = threshold
    if column is not None:
        params["column"] = column
    return params

def metric_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["threshold"])}\'')
        if params["threshold"].get("low", None) is None:
            raise StyxValidationError("`low` must not be None")
        if not isinstance(params["threshold"]["low"], (float, int)):
            raise StyxValidationError(f'`low` has the wrong type: Received `{type(params["threshold"]["low"])}` expected `float`')
        if params["threshold"].get("high", None) is None:
            raise StyxValidationError("`high` must not be None")
        if not isinstance(params["threshold"]["high"], (float, int)):
            raise StyxValidationError(f'`high` has the wrong type: Received `{type(params["threshold"]["high"])}` expected `float`')
    if params.get("sum_columns", None) is not None:
        if not isinstance(params["sum_columns"], bool):
            raise StyxValidationError(f'`sum_columns` has the wrong type: Received `{type(params["sum_columns"])}` expected `bool`')
    if params.get("consolidate_mode", None) is not None:
        if not isinstance(params["consolidate_mode"], bool):
            raise StyxValidationError(f'`consolidate_mode` has the wrong type: Received `{type(params["consolidate_mode"])}` expected `bool`')
    if params.get("only_maxima", None) is not None:
        if not isinstance(params["only_maxima"], bool):
            raise StyxValidationError(f'`only_maxima` has the wrong type: Received `{type(params["only_maxima"])}` expected `bool`')
    if params.get("only_minima", None) is not None:
        if not isinstance(params["only_minima"], bool):
            raise StyxValidationError(f'`only_minima` has the wrong type: Received `{type(params["only_minima"])}` expected `bool`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def metric_extrema_cargs(params: MetricExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-extrema")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("threshold")
    if v_2 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_2["low"]))
        cargs.append(str(v_2["high"]))
    if params.get("sum_columns"):
        cargs.append("-sum-columns")
    if params.get("consolidate_mode"):
        cargs.append("-consolidate-mode")
    if params.get("only_maxima"):
        cargs.append("-only-maxima")
    if params.get("only_minima"):
        cargs.append("-only-minima")
    v_3 = params.get("column")
    if v_3 is not None:
        cargs.append("-column")
        cargs.append(v_3)
    return cargs

def metric_extrema_outputs(params: MetricExtrema, execution: Execution) -> MetricExtremaOutputs:
    """Build the MetricExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricExtremaOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_extrema_execute(params: MetricExtrema, runner: Runner | None = None) -> MetricExtremaOutputs:
    """
    FIND EXTREMA IN A METRIC FILE

    Finds extrema in a metric file, such that no two extrema of the same type are within <distance> of each other.  The extrema are labeled as -1 for minima, 1 for maxima, 0 otherwise.  If -only-maxima or -only-minima is specified, then it will ignore extrema not of the specified type.  These options are mutually exclusive.

If -roi is specified, not only is data outside the roi not used, but any vertex on the edge of the ROI will never be counted as an extrema, in case the ROI cuts across a gradient, which would otherwise generate extrema where there should be none.

If -sum-columns is specified, these extrema columns are summed, and the output has a single column with this result.

By default, a datapoint is an extrema only if it is more extreme than every other datapoint that is within <distance> from it.  If -consolidate-mode is used, it instead starts by finding all datapoints that are more extreme than their immediate neighbors, then while there are any extrema within <distance> of each other, take the two extrema closest to each other and merge them into one by a weighted average based on how many original extrema have been merged into each.

By default, all input columns are used with no smoothing, use -column to specify a single column to use, and -presmooth to smooth the input before finding the extrema.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_EXTREMA_METADATA)
    execution.params(params)
    args = metric_extrema_cargs(params, execution)
    out = metric_extrema_outputs(params, execution)
    execution.run(args)
    return out

def metric_extrema(
    surface: InputPathType,
    metric_in: InputPathType,
    distance: float,
    metric_out: str,
    presmooth: MetricExtremaPresmooth | None = None,
    roi: InputPathType | None = None,
    threshold: MetricExtremaThreshold | None = None,
    sum_columns: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
    column: str | None = None,
    runner: Runner | None = None,
) -> MetricExtremaOutputs:
    """
    FIND EXTREMA IN A METRIC FILE

    Finds extrema in a metric file, such that no two extrema of the same type are within <distance> of each other.  The extrema are labeled as -1 for minima, 1 for maxima, 0 otherwise.  If -only-maxima or -only-minima is specified, then it will ignore extrema not of the specified type.  These options are mutually exclusive.

If -roi is specified, not only is data outside the roi not used, but any vertex on the edge of the ROI will never be counted as an extrema, in case the ROI cuts across a gradient, which would otherwise generate extrema where there should be none.

If -sum-columns is specified, these extrema columns are summed, and the output has a single column with this result.

By default, a datapoint is an extrema only if it is more extreme than every other datapoint that is within <distance> from it.  If -consolidate-mode is used, it instead starts by finding all datapoints that are more extreme than their immediate neighbors, then while there are any extrema within <distance> of each other, take the two extrema closest to each other and merge them into one by a weighted average based on how many original extrema have been merged into each.

By default, all input columns are used with no smoothing, use -column to specify a single column to use, and -presmooth to smooth the input before finding the extrema.

    Args:
        surface: the surface to use for distance information
        metric_in: the metric to find the extrema of
        distance: the minimum distance between identified extrema of the same type
        metric_out: the output extrema metric
        presmooth: smooth the metric before finding extrema
        roi: ignore values outside the selected area
        threshold: ignore small extrema
        sum_columns: output the sum of the extrema columns instead of each column\
            separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima
        column: select a single column to find extrema in
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_extrema_params(
        surface=surface,
        metric_in=metric_in,
        distance=distance,
        metric_out=metric_out,
        presmooth=presmooth,
        roi=roi,
        threshold=threshold,
        sum_columns=sum_columns,
        consolidate_mode=consolidate_mode,
        only_maxima=only_maxima,
        only_minima=only_minima,
        column=column,
    )
    return metric_extrema_execute(params, runner)

__all__ = [
    "MetricExtrema",
    "MetricExtremaOutputs",
    "METRIC_EXTREMA_METADATA",
    "metric_extrema_cargs",
    "metric_extrema_outputs",
    "metric_extrema_params",
    "metric_extrema_execute",
    "metric_extrema_validate",
    "metric_extrema",
]