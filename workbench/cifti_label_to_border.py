# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_TO_BORDER_METADATA = Metadata(
    id="cifti_label_to_border",
    name="DRAW BORDERS AROUND CIFTI LABELS",
    package="workbench",
)

class CiftiLabelToBorderBorder(typing.TypedDict):
    surface: InputPathType
    """the surface to use for neighbor and structure information"""
    border_out: str
    """the output border file"""

CiftiLabelToBorder = typing.TypedDict(
    "CiftiLabelToBorder",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_to_border"]],
        "cifti_in": InputPathType,
        "placement": typing.NotRequired[float],
        "column": typing.NotRequired[str],
        "border": list[CiftiLabelToBorderBorder],
    },
)

@dataclasses.dataclass
class CiftiLabelToBorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    border_out: list[OutputPathType]
    """the output border file"""

def cifti_label_to_border_params(
    cifti_in: InputPathType,
    border: list[CiftiLabelToBorderBorder],
    placement: float | None = None,
    column: str | None = None,
) -> CiftiLabelToBorder:
    """
    Build parameters.

    Args:
        cifti_in: the input cifti dlabel file
        border: specify output file for a surface structure
        placement: set how far along the edge border points are drawn
        column: select a single column

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelToBorder = {
        "@type": "workbench/cifti_label_to_border",
        "cifti_in": cifti_in,
        "border": border,
    }
    if placement is not None:
        params["placement"] = placement
    if column is not None:
        params["column"] = column
    return params

def cifti_label_to_border_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelToBorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("placement", None) is not None:
        if not isinstance(params["placement"], (float, int)):
            raise StyxValidationError(f'`placement` has the wrong type: Received `{type(params["placement"])}` expected `float`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("border", None) is None:
        raise StyxValidationError("`border` must not be None")
    if not isinstance(params["border"], list):
        raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `list[CiftiLabelToBorderBorder]`')
    for e in params["border"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("surface", None) is None:
            raise StyxValidationError("`surface` must not be None")
        if not isinstance(e["surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surface` has the wrong type: Received `{type(e["surface"])}` expected `InputPathType`')
        if e.get("border_out", None) is None:
            raise StyxValidationError("`border_out` must not be None")
        if not isinstance(e["border_out"], str):
            raise StyxValidationError(f'`border_out` has the wrong type: Received `{type(e["border_out"])}` expected `str`')

def cifti_label_to_border_cargs(params: CiftiLabelToBorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-to-border")
    cargs.append(execution.input_file(params["cifti_in"]))
    v_0 = params.get("placement")
    if v_0 is not None:
        cargs.append("-placement")
        cargs.append(str(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    for item0 in params["border"]:
        cargs.append("-border")
        cargs.append(execution.input_file(item0["surface"]))
        cargs.append(item0["border_out"])
    return cargs

def cifti_label_to_border_outputs(params: CiftiLabelToBorder, execution: Execution) -> CiftiLabelToBorderOutputs:
    """Build the CiftiLabelToBorderOutputs object for this tool."""
    border_out_v: list[OutputPathType] = []
    root_v: OutputPathType = execution.output_file(".")
    for __o0 in params["border"]:
        border_out_v.append(execution.output_file(__o0["border_out"]))
    return CiftiLabelToBorderOutputs(
        root=root_v,
        border_out=border_out_v,
    )

def cifti_label_to_border_execute(params: CiftiLabelToBorder, runner: Runner | None = None) -> CiftiLabelToBorderOutputs:
    """
    DRAW BORDERS AROUND CIFTI LABELS

    For each surface, takes the labels on the matching structure and draws borders around the labels.  Use -column to only draw borders around one label map.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_to_border_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_TO_BORDER_METADATA)
    execution.params(params)
    args = cifti_label_to_border_cargs(params, execution)
    out = cifti_label_to_border_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_to_border(
    cifti_in: InputPathType,
    border: list[CiftiLabelToBorderBorder],
    placement: float | None = None,
    column: str | None = None,
    runner: Runner | None = None,
) -> CiftiLabelToBorderOutputs:
    """
    DRAW BORDERS AROUND CIFTI LABELS

    For each surface, takes the labels on the matching structure and draws borders around the labels.  Use -column to only draw borders around one label map.

    Args:
        cifti_in: the input cifti dlabel file
        border: specify output file for a surface structure
        placement: set how far along the edge border points are drawn
        column: select a single column
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_to_border_params(
        cifti_in=cifti_in,
        border=border,
        placement=placement,
        column=column,
    )
    return cifti_label_to_border_execute(params, runner)

__all__ = [
    "CiftiLabelToBorder",
    "CiftiLabelToBorderOutputs",
    "CIFTI_LABEL_TO_BORDER_METADATA",
    "cifti_label_to_border_cargs",
    "cifti_label_to_border_outputs",
    "cifti_label_to_border_params",
    "cifti_label_to_border_execute",
    "cifti_label_to_border_validate",
    "cifti_label_to_border",
]