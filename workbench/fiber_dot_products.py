# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIBER_DOT_PRODUCTS_METADATA = Metadata(
    id="fiber_dot_products",
    name="COMPUTE DOT PRODUCTS OF FIBER ORIENTATIONS WITH SURFACE NORMALS",
    package="workbench",
)

FiberDotProducts = typing.TypedDict(
    "FiberDotProducts",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/fiber_dot_products"]],
        "white_surf": InputPathType,
        "fiber_file": InputPathType,
        "max_dist": float,
        "direction": str,
        "dot_metric": str,
        "f_metric": str,
    },
)

@dataclasses.dataclass
class FiberDotProductsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    dot_metric: OutputPathType
    """the metric of dot products"""
    f_metric: OutputPathType
    """a metric of the f values of the fiber distributions"""

def fiber_dot_products_params(
    white_surf: InputPathType,
    fiber_file: InputPathType,
    max_dist: float,
    direction: str,
    dot_metric: str,
    f_metric: str,
) -> FiberDotProducts:
    """
    Build parameters.

    Args:
        white_surf: the white/gray boundary surface
        fiber_file: the fiber orientation file
        max_dist: the maximum distance from any surface vertex a fiber population\
            may be, in mm
        direction: test against surface for whether a fiber population should be\
            used
        dot_metric: the metric of dot products
        f_metric: a metric of the f values of the fiber distributions

    Returns:
        Parameter dictionary.
    """
    params: FiberDotProducts = {
        "@type": "workbench/fiber_dot_products",
        "white_surf": white_surf,
        "fiber_file": fiber_file,
        "max_dist": max_dist,
        "direction": direction,
        "dot_metric": dot_metric,
        "f_metric": f_metric,
    }
    return params

def fiber_dot_products_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FiberDotProducts."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("white_surf", None) is None:
        raise StyxValidationError("`white_surf` must not be None")
    if not isinstance(params["white_surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`white_surf` has the wrong type: Received `{type(params["white_surf"])}` expected `InputPathType`')
    if params.get("fiber_file", None) is None:
        raise StyxValidationError("`fiber_file` must not be None")
    if not isinstance(params["fiber_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fiber_file` has the wrong type: Received `{type(params["fiber_file"])}` expected `InputPathType`')
    if params.get("max_dist", None) is None:
        raise StyxValidationError("`max_dist` must not be None")
    if not isinstance(params["max_dist"], (float, int)):
        raise StyxValidationError(f'`max_dist` has the wrong type: Received `{type(params["max_dist"])}` expected `float`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("dot_metric", None) is None:
        raise StyxValidationError("`dot_metric` must not be None")
    if not isinstance(params["dot_metric"], str):
        raise StyxValidationError(f'`dot_metric` has the wrong type: Received `{type(params["dot_metric"])}` expected `str`')
    if params.get("f_metric", None) is None:
        raise StyxValidationError("`f_metric` must not be None")
    if not isinstance(params["f_metric"], str):
        raise StyxValidationError(f'`f_metric` has the wrong type: Received `{type(params["f_metric"])}` expected `str`')

def fiber_dot_products_cargs(params: FiberDotProducts, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-fiber-dot-products")
    cargs.append(execution.input_file(params["white_surf"]))
    cargs.append(execution.input_file(params["fiber_file"]))
    cargs.append(str(params["max_dist"]))
    cargs.append(params["direction"])
    cargs.append(params["dot_metric"])
    cargs.append(params["f_metric"])
    return cargs

def fiber_dot_products_outputs(params: FiberDotProducts, execution: Execution) -> FiberDotProductsOutputs:
    """Build the FiberDotProductsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    dot_metric_v: OutputPathType = execution.output_file(params["dot_metric"])
    f_metric_v: OutputPathType = execution.output_file(params["f_metric"])
    return FiberDotProductsOutputs(
        root=root_v,
        dot_metric=dot_metric_v,
        f_metric=f_metric_v,
    )

def fiber_dot_products_execute(params: FiberDotProducts, runner: Runner | None = None) -> FiberDotProductsOutputs:
    """
    COMPUTE DOT PRODUCTS OF FIBER ORIENTATIONS WITH SURFACE NORMALS

    For each vertex, this command finds the closest fiber population that satisfies the <direction> test, and computes the absolute value of the dot product of the surface normal and the normalized mean direction of each fiber.  The <direction> test must be one of INSIDE, OUTSIDE, or ANY, which causes the command to only use fiber populations that are inside the surface, outside the surface, or to not care which direction it is from the surface.  Each fiber population is output in a separate metric column.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fiber_dot_products_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIBER_DOT_PRODUCTS_METADATA)
    execution.params(params)
    args = fiber_dot_products_cargs(params, execution)
    out = fiber_dot_products_outputs(params, execution)
    execution.run(args)
    return out

def fiber_dot_products(
    white_surf: InputPathType,
    fiber_file: InputPathType,
    max_dist: float,
    direction: str,
    dot_metric: str,
    f_metric: str,
    runner: Runner | None = None,
) -> FiberDotProductsOutputs:
    """
    COMPUTE DOT PRODUCTS OF FIBER ORIENTATIONS WITH SURFACE NORMALS

    For each vertex, this command finds the closest fiber population that satisfies the <direction> test, and computes the absolute value of the dot product of the surface normal and the normalized mean direction of each fiber.  The <direction> test must be one of INSIDE, OUTSIDE, or ANY, which causes the command to only use fiber populations that are inside the surface, outside the surface, or to not care which direction it is from the surface.  Each fiber population is output in a separate metric column.

    Args:
        white_surf: the white/gray boundary surface
        fiber_file: the fiber orientation file
        max_dist: the maximum distance from any surface vertex a fiber population\
            may be, in mm
        direction: test against surface for whether a fiber population should be\
            used
        dot_metric: the metric of dot products
        f_metric: a metric of the f values of the fiber distributions
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fiber_dot_products_params(
        white_surf=white_surf,
        fiber_file=fiber_file,
        max_dist=max_dist,
        direction=direction,
        dot_metric=dot_metric,
        f_metric=f_metric,
    )
    return fiber_dot_products_execute(params, runner)

__all__ = [
    "FiberDotProducts",
    "FiberDotProductsOutputs",
    "FIBER_DOT_PRODUCTS_METADATA",
    "fiber_dot_products_cargs",
    "fiber_dot_products_outputs",
    "fiber_dot_products_params",
    "fiber_dot_products_execute",
    "fiber_dot_products_validate",
    "fiber_dot_products",
]