# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_APPLY_WARPFIELD_METADATA = Metadata(
    id="surface_apply_warpfield",
    name="APPLY WARPFIELD TO SURFACE FILE",
    package="workbench",
)

SurfaceApplyWarpfield = typing.TypedDict(
    "SurfaceApplyWarpfield",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_apply_warpfield"]],
        "in_surf": InputPathType,
        "warpfield": str,
        "out_surf": str,
        "fnirt": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SurfaceApplyWarpfieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_surf: OutputPathType
    """the output transformed surface"""

def surface_apply_warpfield_params(
    in_surf: InputPathType,
    warpfield: str,
    out_surf: str,
    fnirt: str | None = None,
) -> SurfaceApplyWarpfield:
    """
    Build parameters.

    Args:
        in_surf: the surface to transform
        warpfield: the INVERSE warpfield
        out_surf: the output transformed surface
        fnirt: MUST be used if using a fnirt warpfield

    Returns:
        Parameter dictionary.
    """
    params: SurfaceApplyWarpfield = {
        "@type": "workbench/surface_apply_warpfield",
        "in_surf": in_surf,
        "warpfield": warpfield,
        "out_surf": out_surf,
    }
    if fnirt is not None:
        params["fnirt"] = fnirt
    return params

def surface_apply_warpfield_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceApplyWarpfield."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_surf", None) is None:
        raise StyxValidationError("`in_surf` must not be None")
    if not isinstance(params["in_surf"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_surf` has the wrong type: Received `{type(params["in_surf"])}` expected `InputPathType`')
    if params.get("warpfield", None) is None:
        raise StyxValidationError("`warpfield` must not be None")
    if not isinstance(params["warpfield"], str):
        raise StyxValidationError(f'`warpfield` has the wrong type: Received `{type(params["warpfield"])}` expected `str`')
    if params.get("out_surf", None) is None:
        raise StyxValidationError("`out_surf` must not be None")
    if not isinstance(params["out_surf"], str):
        raise StyxValidationError(f'`out_surf` has the wrong type: Received `{type(params["out_surf"])}` expected `str`')
    if params.get("fnirt", None) is not None:
        if not isinstance(params["fnirt"], str):
            raise StyxValidationError(f'`fnirt` has the wrong type: Received `{type(params["fnirt"])}` expected `str`')

def surface_apply_warpfield_cargs(params: SurfaceApplyWarpfield, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-apply-warpfield")
    cargs.append(execution.input_file(params["in_surf"]))
    cargs.append(params["warpfield"])
    cargs.append(params["out_surf"])
    v_0 = params.get("fnirt")
    if v_0 is not None:
        cargs.append("-fnirt")
        cargs.append(v_0)
    return cargs

def surface_apply_warpfield_outputs(params: SurfaceApplyWarpfield, execution: Execution) -> SurfaceApplyWarpfieldOutputs:
    """Build the SurfaceApplyWarpfieldOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_surf_v: OutputPathType = execution.output_file(params["out_surf"])
    return SurfaceApplyWarpfieldOutputs(
        root=root_v,
        out_surf=out_surf_v,
    )

def surface_apply_warpfield_execute(params: SurfaceApplyWarpfield, runner: Runner | None = None) -> SurfaceApplyWarpfieldOutputs:
    """
    APPLY WARPFIELD TO SURFACE FILE

    NOTE: warping a surface requires the INVERSE of the warpfield used to warp the volume it lines up with.  The header of the forward warp is needed by the -fnirt option in order to correctly interpret the displacements in the fnirt warpfield.

If the -fnirt option is not present, the warpfield must be a nifti 'world' warpfield, which can be obtained with the -convert-warpfield command.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_apply_warpfield_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_APPLY_WARPFIELD_METADATA)
    execution.params(params)
    args = surface_apply_warpfield_cargs(params, execution)
    out = surface_apply_warpfield_outputs(params, execution)
    execution.run(args)
    return out

def surface_apply_warpfield(
    in_surf: InputPathType,
    warpfield: str,
    out_surf: str,
    fnirt: str | None = None,
    runner: Runner | None = None,
) -> SurfaceApplyWarpfieldOutputs:
    """
    APPLY WARPFIELD TO SURFACE FILE

    NOTE: warping a surface requires the INVERSE of the warpfield used to warp the volume it lines up with.  The header of the forward warp is needed by the -fnirt option in order to correctly interpret the displacements in the fnirt warpfield.

If the -fnirt option is not present, the warpfield must be a nifti 'world' warpfield, which can be obtained with the -convert-warpfield command.

    Args:
        in_surf: the surface to transform
        warpfield: the INVERSE warpfield
        out_surf: the output transformed surface
        fnirt: MUST be used if using a fnirt warpfield
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_apply_warpfield_params(
        in_surf=in_surf,
        warpfield=warpfield,
        out_surf=out_surf,
        fnirt=fnirt,
    )
    return surface_apply_warpfield_execute(params, runner)

__all__ = [
    "SurfaceApplyWarpfield",
    "SurfaceApplyWarpfieldOutputs",
    "SURFACE_APPLY_WARPFIELD_METADATA",
    "surface_apply_warpfield_cargs",
    "surface_apply_warpfield_outputs",
    "surface_apply_warpfield_params",
    "surface_apply_warpfield_execute",
    "surface_apply_warpfield_validate",
    "surface_apply_warpfield",
]