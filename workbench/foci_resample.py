# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FOCI_RESAMPLE_METADATA = Metadata(
    id="foci_resample",
    name="PROJECT FOCI TO A DIFFERENT SURFACE",
    package="workbench",
)

class FociResampleLeftSurfaces(typing.TypedDict):
    current_surf: InputPathType
    """the surface the foci are currently projected on"""
    new_surf: InputPathType
    """the surface to project the foci onto"""

FociResample = typing.TypedDict(
    "FociResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/foci_resample"]],
        "foci_in": InputPathType,
        "foci_out": str,
        "left_surfaces": typing.NotRequired[FociResampleLeftSurfaces],
        "right_surfaces": typing.NotRequired[FociResampleLeftSurfaces],
        "cerebellum_surfaces": typing.NotRequired[FociResampleLeftSurfaces],
        "discard_distance_from_surface": typing.NotRequired[bool],
        "restore_xyz": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FociResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    foci_out: OutputPathType
    """the output foci file"""

def foci_resample_params(
    foci_in: InputPathType,
    foci_out: str,
    left_surfaces: FociResampleLeftSurfaces | None = None,
    right_surfaces: FociResampleLeftSurfaces | None = None,
    cerebellum_surfaces: FociResampleLeftSurfaces | None = None,
    discard_distance_from_surface: bool = False,
    restore_xyz: bool = False,
) -> FociResample:
    """
    Build parameters.

    Args:
        foci_in: the input foci file
        foci_out: the output foci file
        left_surfaces: the left surfaces for resampling
        right_surfaces: the right surfaces for resampling
        cerebellum_surfaces: the cerebellum surfaces for resampling
        discard_distance_from_surface: ignore the distance the foci are above or\
            below the current surface
        restore_xyz: put the original xyz coordinates into the foci, rather than\
            the coordinates obtained from unprojection

    Returns:
        Parameter dictionary.
    """
    params: FociResample = {
        "@type": "workbench/foci_resample",
        "foci_in": foci_in,
        "foci_out": foci_out,
        "discard_distance_from_surface": discard_distance_from_surface,
        "restore_xyz": restore_xyz,
    }
    if left_surfaces is not None:
        params["left_surfaces"] = left_surfaces
    if right_surfaces is not None:
        params["right_surfaces"] = right_surfaces
    if cerebellum_surfaces is not None:
        params["cerebellum_surfaces"] = cerebellum_surfaces
    return params

def foci_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FociResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("foci_in", None) is None:
        raise StyxValidationError("`foci_in` must not be None")
    if not isinstance(params["foci_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`foci_in` has the wrong type: Received `{type(params["foci_in"])}` expected `InputPathType`')
    if params.get("foci_out", None) is None:
        raise StyxValidationError("`foci_out` must not be None")
    if not isinstance(params["foci_out"], str):
        raise StyxValidationError(f'`foci_out` has the wrong type: Received `{type(params["foci_out"])}` expected `str`')
    if params.get("left_surfaces", None) is not None:
        if not isinstance(params["left_surfaces"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["left_surfaces"])}\'')
        if params["left_surfaces"].get("current_surf", None) is None:
            raise StyxValidationError("`current_surf` must not be None")
        if not isinstance(params["left_surfaces"]["current_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_surf` has the wrong type: Received `{type(params["left_surfaces"]["current_surf"])}` expected `InputPathType`')
        if params["left_surfaces"].get("new_surf", None) is None:
            raise StyxValidationError("`new_surf` must not be None")
        if not isinstance(params["left_surfaces"]["new_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_surf` has the wrong type: Received `{type(params["left_surfaces"]["new_surf"])}` expected `InputPathType`')
    if params.get("right_surfaces", None) is not None:
        if not isinstance(params["right_surfaces"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["right_surfaces"])}\'')
        if params["right_surfaces"].get("current_surf", None) is None:
            raise StyxValidationError("`current_surf` must not be None")
        if not isinstance(params["right_surfaces"]["current_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_surf` has the wrong type: Received `{type(params["right_surfaces"]["current_surf"])}` expected `InputPathType`')
        if params["right_surfaces"].get("new_surf", None) is None:
            raise StyxValidationError("`new_surf` must not be None")
        if not isinstance(params["right_surfaces"]["new_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_surf` has the wrong type: Received `{type(params["right_surfaces"]["new_surf"])}` expected `InputPathType`')
    if params.get("cerebellum_surfaces", None) is not None:
        if not isinstance(params["cerebellum_surfaces"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["cerebellum_surfaces"])}\'')
        if params["cerebellum_surfaces"].get("current_surf", None) is None:
            raise StyxValidationError("`current_surf` must not be None")
        if not isinstance(params["cerebellum_surfaces"]["current_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_surf` has the wrong type: Received `{type(params["cerebellum_surfaces"]["current_surf"])}` expected `InputPathType`')
        if params["cerebellum_surfaces"].get("new_surf", None) is None:
            raise StyxValidationError("`new_surf` must not be None")
        if not isinstance(params["cerebellum_surfaces"]["new_surf"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_surf` has the wrong type: Received `{type(params["cerebellum_surfaces"]["new_surf"])}` expected `InputPathType`')
    if params.get("discard_distance_from_surface", None) is not None:
        if not isinstance(params["discard_distance_from_surface"], bool):
            raise StyxValidationError(f'`discard_distance_from_surface` has the wrong type: Received `{type(params["discard_distance_from_surface"])}` expected `bool`')
    if params.get("restore_xyz", None) is not None:
        if not isinstance(params["restore_xyz"], bool):
            raise StyxValidationError(f'`restore_xyz` has the wrong type: Received `{type(params["restore_xyz"])}` expected `bool`')

def foci_resample_cargs(params: FociResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-foci-resample")
    cargs.append(execution.input_file(params["foci_in"]))
    cargs.append(params["foci_out"])
    v_0 = params.get("left_surfaces")
    if v_0 is not None:
        cargs.append("-left-surfaces")
        cargs.append(execution.input_file(v_0["current_surf"]))
        cargs.append(execution.input_file(v_0["new_surf"]))
    v_1 = params.get("right_surfaces")
    if v_1 is not None:
        cargs.append("-right-surfaces")
        cargs.append(execution.input_file(v_1["current_surf"]))
        cargs.append(execution.input_file(v_1["new_surf"]))
    v_2 = params.get("cerebellum_surfaces")
    if v_2 is not None:
        cargs.append("-cerebellum-surfaces")
        cargs.append(execution.input_file(v_2["current_surf"]))
        cargs.append(execution.input_file(v_2["new_surf"]))
    if params.get("discard_distance_from_surface"):
        cargs.append("-discard-distance-from-surface")
    if params.get("restore_xyz"):
        cargs.append("-restore-xyz")
    return cargs

def foci_resample_outputs(params: FociResample, execution: Execution) -> FociResampleOutputs:
    """Build the FociResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    foci_out_v: OutputPathType = execution.output_file(params["foci_out"])
    return FociResampleOutputs(
        root=root_v,
        foci_out=foci_out_v,
    )

def foci_resample_execute(params: FociResample, runner: Runner | None = None) -> FociResampleOutputs:
    """
    PROJECT FOCI TO A DIFFERENT SURFACE

    Unprojects foci from the <current-surf> for the structure, then projects them to <new-surf>.  If the foci have meaningful distances above or below the surface, use anatomical surfaces.  If the foci should be on the surface, use registered spheres and the options -discard-distance-from-surface and -restore-xyz.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    foci_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FOCI_RESAMPLE_METADATA)
    execution.params(params)
    args = foci_resample_cargs(params, execution)
    out = foci_resample_outputs(params, execution)
    execution.run(args)
    return out

def foci_resample(
    foci_in: InputPathType,
    foci_out: str,
    left_surfaces: FociResampleLeftSurfaces | None = None,
    right_surfaces: FociResampleLeftSurfaces | None = None,
    cerebellum_surfaces: FociResampleLeftSurfaces | None = None,
    discard_distance_from_surface: bool = False,
    restore_xyz: bool = False,
    runner: Runner | None = None,
) -> FociResampleOutputs:
    """
    PROJECT FOCI TO A DIFFERENT SURFACE

    Unprojects foci from the <current-surf> for the structure, then projects them to <new-surf>.  If the foci have meaningful distances above or below the surface, use anatomical surfaces.  If the foci should be on the surface, use registered spheres and the options -discard-distance-from-surface and -restore-xyz.

    Args:
        foci_in: the input foci file
        foci_out: the output foci file
        left_surfaces: the left surfaces for resampling
        right_surfaces: the right surfaces for resampling
        cerebellum_surfaces: the cerebellum surfaces for resampling
        discard_distance_from_surface: ignore the distance the foci are above or\
            below the current surface
        restore_xyz: put the original xyz coordinates into the foci, rather than\
            the coordinates obtained from unprojection
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = foci_resample_params(
        foci_in=foci_in,
        foci_out=foci_out,
        left_surfaces=left_surfaces,
        right_surfaces=right_surfaces,
        cerebellum_surfaces=cerebellum_surfaces,
        discard_distance_from_surface=discard_distance_from_surface,
        restore_xyz=restore_xyz,
    )
    return foci_resample_execute(params, runner)

__all__ = [
    "FociResample",
    "FociResampleOutputs",
    "FOCI_RESAMPLE_METADATA",
    "foci_resample_cargs",
    "foci_resample_outputs",
    "foci_resample_params",
    "foci_resample_execute",
    "foci_resample_validate",
    "foci_resample",
]