# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_DISTORTION_METADATA = Metadata(
    id="volume_distortion",
    name="CALCULATE VOLUME WARPFIELD DISTORTION",
    package="workbench",
)

VolumeDistortion = typing.TypedDict(
    "VolumeDistortion",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_distortion"]],
        "warpfield": str,
        "volume_out": str,
        "fnirt": typing.NotRequired[str],
        "circular": typing.NotRequired[bool],
        "log2": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VolumeDistortionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output distortion measures"""

def volume_distortion_params(
    warpfield: str,
    volume_out: str,
    fnirt: str | None = None,
    circular: bool = False,
    log2: bool = False,
) -> VolumeDistortion:
    """
    Build parameters.

    Args:
        warpfield: the warpfield to compute the distortion of
        volume_out: the output distortion measures
        fnirt: MUST be used if using a fnirt warpfield
        circular: use the circle-based formula for the anisotropic measure
        log2: apply base-2 log transform

    Returns:
        Parameter dictionary.
    """
    params: VolumeDistortion = {
        "@type": "workbench/volume_distortion",
        "warpfield": warpfield,
        "volume_out": volume_out,
        "circular": circular,
        "log2": log2,
    }
    if fnirt is not None:
        params["fnirt"] = fnirt
    return params

def volume_distortion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeDistortion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("warpfield", None) is None:
        raise StyxValidationError("`warpfield` must not be None")
    if not isinstance(params["warpfield"], str):
        raise StyxValidationError(f'`warpfield` has the wrong type: Received `{type(params["warpfield"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("fnirt", None) is not None:
        if not isinstance(params["fnirt"], str):
            raise StyxValidationError(f'`fnirt` has the wrong type: Received `{type(params["fnirt"])}` expected `str`')
    if params.get("circular", None) is not None:
        if not isinstance(params["circular"], bool):
            raise StyxValidationError(f'`circular` has the wrong type: Received `{type(params["circular"])}` expected `bool`')
    if params.get("log2", None) is not None:
        if not isinstance(params["log2"], bool):
            raise StyxValidationError(f'`log2` has the wrong type: Received `{type(params["log2"])}` expected `bool`')

def volume_distortion_cargs(params: VolumeDistortion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-distortion")
    cargs.append(params["warpfield"])
    cargs.append(params["volume_out"])
    v_0 = params.get("fnirt")
    if v_0 is not None:
        cargs.append("-fnirt")
        cargs.append(v_0)
    if params.get("circular"):
        cargs.append("-circular")
    if params.get("log2"):
        cargs.append("-log2")
    return cargs

def volume_distortion_outputs(params: VolumeDistortion, execution: Execution) -> VolumeDistortionOutputs:
    """Build the VolumeDistortionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeDistortionOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_distortion_execute(params: VolumeDistortion, runner: Runner | None = None) -> VolumeDistortionOutputs:
    """
    CALCULATE VOLUME WARPFIELD DISTORTION

    Calculates isotropic and anisotropic distortions in the volume warpfield.  At each voxel, the gradient of the absolute warpfield is computed to obtain the local affine transforms for each voxel (jacobian matrices), and strain tensors are derived from them.  The isotropic component (volumetric expansion ratio) is the product of the three principal strains.  The default measure ('elongation') for the anisotropic component is the largest principal strain divided by the smallest.

The -circular option instead calculates the anisotropic component by transforming the principal strains into log space, considering them as x-values of points on a circle 120 degrees apart, finds the circle's diameter, and transforms that back to a ratio.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_distortion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_DISTORTION_METADATA)
    execution.params(params)
    args = volume_distortion_cargs(params, execution)
    out = volume_distortion_outputs(params, execution)
    execution.run(args)
    return out

def volume_distortion(
    warpfield: str,
    volume_out: str,
    fnirt: str | None = None,
    circular: bool = False,
    log2: bool = False,
    runner: Runner | None = None,
) -> VolumeDistortionOutputs:
    """
    CALCULATE VOLUME WARPFIELD DISTORTION

    Calculates isotropic and anisotropic distortions in the volume warpfield.  At each voxel, the gradient of the absolute warpfield is computed to obtain the local affine transforms for each voxel (jacobian matrices), and strain tensors are derived from them.  The isotropic component (volumetric expansion ratio) is the product of the three principal strains.  The default measure ('elongation') for the anisotropic component is the largest principal strain divided by the smallest.

The -circular option instead calculates the anisotropic component by transforming the principal strains into log space, considering them as x-values of points on a circle 120 degrees apart, finds the circle's diameter, and transforms that back to a ratio.

    Args:
        warpfield: the warpfield to compute the distortion of
        volume_out: the output distortion measures
        fnirt: MUST be used if using a fnirt warpfield
        circular: use the circle-based formula for the anisotropic measure
        log2: apply base-2 log transform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_distortion_params(
        warpfield=warpfield,
        volume_out=volume_out,
        fnirt=fnirt,
        circular=circular,
        log2=log2,
    )
    return volume_distortion_execute(params, runner)

__all__ = [
    "VolumeDistortion",
    "VolumeDistortionOutputs",
    "VOLUME_DISTORTION_METADATA",
    "volume_distortion_cargs",
    "volume_distortion_outputs",
    "volume_distortion_params",
    "volume_distortion_execute",
    "volume_distortion_validate",
    "volume_distortion",
]