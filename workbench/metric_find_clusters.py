# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_FIND_CLUSTERS_METADATA = Metadata(
    id="metric_find_clusters",
    name="FILTER CLUSTERS BY SURFACE AREA",
    package="workbench",
)

MetricFindClusters = typing.TypedDict(
    "MetricFindClusters",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_find_clusters"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "value_threshold": float,
        "minimum_area": float,
        "metric_out": str,
        "less_than": typing.NotRequired[bool],
        "roi": typing.NotRequired[InputPathType],
        "corrected_areas": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "size_ratio": typing.NotRequired[float],
        "distance": typing.NotRequired[float],
        "start": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class MetricFindClustersOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_find_clusters_params(
    surface: InputPathType,
    metric_in: InputPathType,
    value_threshold: float,
    minimum_area: float,
    metric_out: str,
    less_than: bool = False,
    roi: InputPathType | None = None,
    corrected_areas: InputPathType | None = None,
    column: str | None = None,
    size_ratio: float | None = None,
    distance: float | None = None,
    start: int | None = None,
) -> MetricFindClusters:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        metric_in: the input metric
        value_threshold: threshold for data values
        minimum_area: threshold for cluster area, in mm^2
        metric_out: the output metric
        less_than: find values less than <value-threshold>, rather than greater
        roi: select a region of interest
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        column: select a single column
        size_ratio: ignore clusters smaller than a given fraction of the largest\
            cluster in map
        distance: ignore clusters further than a given distance from the largest\
            cluster
        start: start labeling clusters from a value other than 1

    Returns:
        Parameter dictionary.
    """
    params: MetricFindClusters = {
        "@type": "workbench/metric_find_clusters",
        "surface": surface,
        "metric_in": metric_in,
        "value_threshold": value_threshold,
        "minimum_area": minimum_area,
        "metric_out": metric_out,
        "less_than": less_than,
    }
    if roi is not None:
        params["roi"] = roi
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    if column is not None:
        params["column"] = column
    if size_ratio is not None:
        params["size_ratio"] = size_ratio
    if distance is not None:
        params["distance"] = distance
    if start is not None:
        params["start"] = start
    return params

def metric_find_clusters_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricFindClusters."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("value_threshold", None) is None:
        raise StyxValidationError("`value_threshold` must not be None")
    if not isinstance(params["value_threshold"], (float, int)):
        raise StyxValidationError(f'`value_threshold` has the wrong type: Received `{type(params["value_threshold"])}` expected `float`')
    if params.get("minimum_area", None) is None:
        raise StyxValidationError("`minimum_area` must not be None")
    if not isinstance(params["minimum_area"], (float, int)):
        raise StyxValidationError(f'`minimum_area` has the wrong type: Received `{type(params["minimum_area"])}` expected `float`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("less_than", None) is not None:
        if not isinstance(params["less_than"], bool):
            raise StyxValidationError(f'`less_than` has the wrong type: Received `{type(params["less_than"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("size_ratio", None) is not None:
        if not isinstance(params["size_ratio"], (float, int)):
            raise StyxValidationError(f'`size_ratio` has the wrong type: Received `{type(params["size_ratio"])}` expected `float`')
    if params.get("distance", None) is not None:
        if not isinstance(params["distance"], (float, int)):
            raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("start", None) is not None:
        if not isinstance(params["start"], int):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["start"])}` expected `int`')

def metric_find_clusters_cargs(params: MetricFindClusters, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-find-clusters")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(str(params["value_threshold"]))
    cargs.append(str(params["minimum_area"]))
    cargs.append(params["metric_out"])
    if params.get("less_than"):
        cargs.append("-less-than")
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("corrected_areas")
    if v_1 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("column")
    if v_2 is not None:
        cargs.append("-column")
        cargs.append(v_2)
    v_3 = params.get("size_ratio")
    if v_3 is not None:
        cargs.append("-size-ratio")
        cargs.append(str(v_3))
    v_4 = params.get("distance")
    if v_4 is not None:
        cargs.append("-distance")
        cargs.append(str(v_4))
    v_5 = params.get("start")
    if v_5 is not None:
        cargs.append("-start")
        cargs.append(str(v_5))
    return cargs

def metric_find_clusters_outputs(params: MetricFindClusters, execution: Execution) -> MetricFindClustersOutputs:
    """Build the MetricFindClustersOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricFindClustersOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_find_clusters_execute(params: MetricFindClusters, runner: Runner | None = None) -> MetricFindClustersOutputs:
    """
    FILTER CLUSTERS BY SURFACE AREA

    Outputs a metric with nonzero integers for all vertices within a large enough cluster, and zeros elsewhere.  The integers denote cluster membership (by default, first cluster found will use value 1, second cluster 2, etc).  Cluster values are not reused across maps of the output, but instead keep counting up.  By default, values greater than <value-threshold> are considered to be in a cluster, use -less-than to test for values less than the threshold.  To apply this as a mask to the data, or to do more complicated thresholding, see -metric-math.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_find_clusters_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_FIND_CLUSTERS_METADATA)
    execution.params(params)
    args = metric_find_clusters_cargs(params, execution)
    out = metric_find_clusters_outputs(params, execution)
    execution.run(args)
    return out

def metric_find_clusters(
    surface: InputPathType,
    metric_in: InputPathType,
    value_threshold: float,
    minimum_area: float,
    metric_out: str,
    less_than: bool = False,
    roi: InputPathType | None = None,
    corrected_areas: InputPathType | None = None,
    column: str | None = None,
    size_ratio: float | None = None,
    distance: float | None = None,
    start: int | None = None,
    runner: Runner | None = None,
) -> MetricFindClustersOutputs:
    """
    FILTER CLUSTERS BY SURFACE AREA

    Outputs a metric with nonzero integers for all vertices within a large enough cluster, and zeros elsewhere.  The integers denote cluster membership (by default, first cluster found will use value 1, second cluster 2, etc).  Cluster values are not reused across maps of the output, but instead keep counting up.  By default, values greater than <value-threshold> are considered to be in a cluster, use -less-than to test for values less than the threshold.  To apply this as a mask to the data, or to do more complicated thresholding, see -metric-math.

    Args:
        surface: the surface to compute on
        metric_in: the input metric
        value_threshold: threshold for data values
        minimum_area: threshold for cluster area, in mm^2
        metric_out: the output metric
        less_than: find values less than <value-threshold>, rather than greater
        roi: select a region of interest
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        column: select a single column
        size_ratio: ignore clusters smaller than a given fraction of the largest\
            cluster in map
        distance: ignore clusters further than a given distance from the largest\
            cluster
        start: start labeling clusters from a value other than 1
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_find_clusters_params(
        surface=surface,
        metric_in=metric_in,
        value_threshold=value_threshold,
        minimum_area=minimum_area,
        metric_out=metric_out,
        less_than=less_than,
        roi=roi,
        corrected_areas=corrected_areas,
        column=column,
        size_ratio=size_ratio,
        distance=distance,
        start=start,
    )
    return metric_find_clusters_execute(params, runner)

__all__ = [
    "MetricFindClusters",
    "MetricFindClustersOutputs",
    "METRIC_FIND_CLUSTERS_METADATA",
    "metric_find_clusters_cargs",
    "metric_find_clusters_outputs",
    "metric_find_clusters_params",
    "metric_find_clusters_execute",
    "metric_find_clusters_validate",
    "metric_find_clusters",
]