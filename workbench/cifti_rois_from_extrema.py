# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_ROIS_FROM_EXTREMA_METADATA = Metadata(
    id="cifti_rois_from_extrema",
    name="CREATE CIFTI ROI MAPS FROM EXTREMA MAPS",
    package="workbench",
)

class CiftiRoisFromExtremaGaussian(typing.TypedDict):
    surf_sigma: float
    """the sigma for the surface gaussian kernel, in mm"""
    vol_sigma: float
    """the sigma for the volume gaussian kernel, in mm"""

CiftiRoisFromExtrema = typing.TypedDict(
    "CiftiRoisFromExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_rois_from_extrema"]],
        "cifti": InputPathType,
        "surf_limit": float,
        "vol_limit": float,
        "direction": str,
        "cifti_out": str,
        "left_surface": typing.NotRequired[InputPathType],
        "right_surface": typing.NotRequired[InputPathType],
        "cerebellum_surface": typing.NotRequired[InputPathType],
        "gaussian": typing.NotRequired[CiftiRoisFromExtremaGaussian],
        "overlap_logic": typing.NotRequired[str],
        "merged_volume": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiRoisFromExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti"""

def cifti_rois_from_extrema_params(
    cifti: InputPathType,
    surf_limit: float,
    vol_limit: float,
    direction: str,
    cifti_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
    gaussian: CiftiRoisFromExtremaGaussian | None = None,
    overlap_logic: str | None = None,
    merged_volume: bool = False,
) -> CiftiRoisFromExtrema:
    """
    Build parameters.

    Args:
        cifti: the input cifti
        surf_limit: geodesic distance limit from vertex, in mm
        vol_limit: euclidean distance limit from voxel center, in mm
        direction: which dimension an extrema map is along, ROW or COLUMN
        cifti_out: the output cifti
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use
        gaussian: generate gaussian kernels instead of flat ROIs
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        merged_volume: treat volume components as if they were a single component

    Returns:
        Parameter dictionary.
    """
    params: CiftiRoisFromExtrema = {
        "@type": "workbench/cifti_rois_from_extrema",
        "cifti": cifti,
        "surf_limit": surf_limit,
        "vol_limit": vol_limit,
        "direction": direction,
        "cifti_out": cifti_out,
        "merged_volume": merged_volume,
    }
    if left_surface is not None:
        params["left_surface"] = left_surface
    if right_surface is not None:
        params["right_surface"] = right_surface
    if cerebellum_surface is not None:
        params["cerebellum_surface"] = cerebellum_surface
    if gaussian is not None:
        params["gaussian"] = gaussian
    if overlap_logic is not None:
        params["overlap_logic"] = overlap_logic
    return params

def cifti_rois_from_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiRoisFromExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `InputPathType`')
    if params.get("surf_limit", None) is None:
        raise StyxValidationError("`surf_limit` must not be None")
    if not isinstance(params["surf_limit"], (float, int)):
        raise StyxValidationError(f'`surf_limit` has the wrong type: Received `{type(params["surf_limit"])}` expected `float`')
    if params.get("vol_limit", None) is None:
        raise StyxValidationError("`vol_limit` must not be None")
    if not isinstance(params["vol_limit"], (float, int)):
        raise StyxValidationError(f'`vol_limit` has the wrong type: Received `{type(params["vol_limit"])}` expected `float`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("left_surface", None) is not None:
        if not isinstance(params["left_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`left_surface` has the wrong type: Received `{type(params["left_surface"])}` expected `InputPathType`')
    if params.get("right_surface", None) is not None:
        if not isinstance(params["right_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`right_surface` has the wrong type: Received `{type(params["right_surface"])}` expected `InputPathType`')
    if params.get("cerebellum_surface", None) is not None:
        if not isinstance(params["cerebellum_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cerebellum_surface` has the wrong type: Received `{type(params["cerebellum_surface"])}` expected `InputPathType`')
    if params.get("gaussian", None) is not None:
        if not isinstance(params["gaussian"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["gaussian"])}\'')
        if params["gaussian"].get("surf_sigma", None) is None:
            raise StyxValidationError("`surf_sigma` must not be None")
        if not isinstance(params["gaussian"]["surf_sigma"], (float, int)):
            raise StyxValidationError(f'`surf_sigma` has the wrong type: Received `{type(params["gaussian"]["surf_sigma"])}` expected `float`')
        if params["gaussian"].get("vol_sigma", None) is None:
            raise StyxValidationError("`vol_sigma` must not be None")
        if not isinstance(params["gaussian"]["vol_sigma"], (float, int)):
            raise StyxValidationError(f'`vol_sigma` has the wrong type: Received `{type(params["gaussian"]["vol_sigma"])}` expected `float`')
    if params.get("overlap_logic", None) is not None:
        if not isinstance(params["overlap_logic"], str):
            raise StyxValidationError(f'`overlap_logic` has the wrong type: Received `{type(params["overlap_logic"])}` expected `str`')
    if params.get("merged_volume", None) is not None:
        if not isinstance(params["merged_volume"], bool):
            raise StyxValidationError(f'`merged_volume` has the wrong type: Received `{type(params["merged_volume"])}` expected `bool`')

def cifti_rois_from_extrema_cargs(params: CiftiRoisFromExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-rois-from-extrema")
    cargs.append(execution.input_file(params["cifti"]))
    cargs.append(str(params["surf_limit"]))
    cargs.append(str(params["vol_limit"]))
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("left_surface")
    if v_0 is not None:
        cargs.append("-left-surface")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("right_surface")
    if v_1 is not None:
        cargs.append("-right-surface")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("cerebellum_surface")
    if v_2 is not None:
        cargs.append("-cerebellum-surface")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("gaussian")
    if v_3 is not None:
        cargs.append("-gaussian")
        cargs.append(str(v_3["surf_sigma"]))
        cargs.append(str(v_3["vol_sigma"]))
    v_4 = params.get("overlap_logic")
    if v_4 is not None:
        cargs.append("-overlap-logic")
        cargs.append(v_4)
    if params.get("merged_volume"):
        cargs.append("-merged-volume")
    return cargs

def cifti_rois_from_extrema_outputs(params: CiftiRoisFromExtrema, execution: Execution) -> CiftiRoisFromExtremaOutputs:
    """Build the CiftiRoisFromExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiRoisFromExtremaOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_rois_from_extrema_execute(params: CiftiRoisFromExtrema, runner: Runner | None = None) -> CiftiRoisFromExtremaOutputs:
    """
    CREATE CIFTI ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_rois_from_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_ROIS_FROM_EXTREMA_METADATA)
    execution.params(params)
    args = cifti_rois_from_extrema_cargs(params, execution)
    out = cifti_rois_from_extrema_outputs(params, execution)
    execution.run(args)
    return out

def cifti_rois_from_extrema(
    cifti: InputPathType,
    surf_limit: float,
    vol_limit: float,
    direction: str,
    cifti_out: str,
    left_surface: InputPathType | None = None,
    right_surface: InputPathType | None = None,
    cerebellum_surface: InputPathType | None = None,
    gaussian: CiftiRoisFromExtremaGaussian | None = None,
    overlap_logic: str | None = None,
    merged_volume: bool = False,
    runner: Runner | None = None,
) -> CiftiRoisFromExtremaOutputs:
    """
    CREATE CIFTI ROI MAPS FROM EXTREMA MAPS

    For each nonzero value in each map, make a map with an ROI around that location.  If the -gaussian option is specified, then normalized gaussian kernels are output instead of ROIs.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        cifti: the input cifti
        surf_limit: geodesic distance limit from vertex, in mm
        vol_limit: euclidean distance limit from voxel center, in mm
        direction: which dimension an extrema map is along, ROW or COLUMN
        cifti_out: the output cifti
        left_surface: specify the left surface to use
        right_surface: specify the right surface to use
        cerebellum_surface: specify the cerebellum surface to use
        gaussian: generate gaussian kernels instead of flat ROIs
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        merged_volume: treat volume components as if they were a single component
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_rois_from_extrema_params(
        cifti=cifti,
        surf_limit=surf_limit,
        vol_limit=vol_limit,
        direction=direction,
        cifti_out=cifti_out,
        left_surface=left_surface,
        right_surface=right_surface,
        cerebellum_surface=cerebellum_surface,
        gaussian=gaussian,
        overlap_logic=overlap_logic,
        merged_volume=merged_volume,
    )
    return cifti_rois_from_extrema_execute(params, runner)

__all__ = [
    "CiftiRoisFromExtrema",
    "CiftiRoisFromExtremaOutputs",
    "CIFTI_ROIS_FROM_EXTREMA_METADATA",
    "cifti_rois_from_extrema_cargs",
    "cifti_rois_from_extrema_outputs",
    "cifti_rois_from_extrema_params",
    "cifti_rois_from_extrema_execute",
    "cifti_rois_from_extrema_validate",
    "cifti_rois_from_extrema",
]