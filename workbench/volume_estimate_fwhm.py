# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_ESTIMATE_FWHM_METADATA = Metadata(
    id="volume_estimate_fwhm",
    name="ESTIMATE FWHM SMOOTHNESS OF A VOLUME",
    package="workbench",
)

class VolumeEstimateFwhmWholeFile(typing.TypedDict):
    demean: typing.NotRequired[bool]
    """subtract the mean image before estimating smoothness"""

VolumeEstimateFwhm = typing.TypedDict(
    "VolumeEstimateFwhm",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_estimate_fwhm"]],
        "volume": InputPathType,
        "roi": typing.NotRequired[InputPathType],
        "subvolume": typing.NotRequired[str],
        "whole_file": typing.NotRequired[VolumeEstimateFwhmWholeFile],
    },
)

@dataclasses.dataclass
class VolumeEstimateFwhmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def volume_estimate_fwhm_params(
    volume: InputPathType,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
    whole_file: VolumeEstimateFwhmWholeFile | None = None,
) -> VolumeEstimateFwhm:
    """
    Build parameters.

    Args:
        volume: the input volume
        roi: use only data within an ROI
        subvolume: select a single subvolume to estimate smoothness of
        whole_file: estimate for the whole file at once, not each subvolume\
            separately

    Returns:
        Parameter dictionary.
    """
    params: VolumeEstimateFwhm = {
        "@type": "workbench/volume_estimate_fwhm",
        "volume": volume,
    }
    if roi is not None:
        params["roi"] = roi
    if subvolume is not None:
        params["subvolume"] = subvolume
    if whole_file is not None:
        params["whole_file"] = whole_file
    return params

def volume_estimate_fwhm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeEstimateFwhm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')
    if params.get("whole_file", None) is not None:
        if not isinstance(params["whole_file"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["whole_file"])}\'')
        if params["whole_file"].get("demean", None) is not None:
            if not isinstance(params["whole_file"]["demean"], bool):
                raise StyxValidationError(f'`demean` has the wrong type: Received `{type(params["whole_file"]["demean"])}` expected `bool`')

def volume_estimate_fwhm_cargs(params: VolumeEstimateFwhm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-estimate-fwhm")
    cargs.append(execution.input_file(params["volume"]))
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("subvolume")
    if v_1 is not None:
        cargs.append("-subvolume")
        cargs.append(v_1)
    v_2 = params.get("whole_file")
    if v_2 is not None:
        cargs.append("-whole-file")
        if v_2.get("demean"):
            cargs.append("-demean")
    return cargs

def volume_estimate_fwhm_outputs(params: VolumeEstimateFwhm, execution: Execution) -> VolumeEstimateFwhmOutputs:
    """Build the VolumeEstimateFwhmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolumeEstimateFwhmOutputs(
        root=root_v,
    )

def volume_estimate_fwhm_execute(params: VolumeEstimateFwhm, runner: Runner | None = None) -> VolumeEstimateFwhmOutputs:
    """
    ESTIMATE FWHM SMOOTHNESS OF A VOLUME

    Estimates the smoothness of the input volume in X, Y, and Z directions separately, printing the estimates to standard output, in mm as FWHM.  If -subvolume or -whole-file are not specified, each subvolume is estimated and displayed separately.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_estimate_fwhm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_ESTIMATE_FWHM_METADATA)
    execution.params(params)
    args = volume_estimate_fwhm_cargs(params, execution)
    out = volume_estimate_fwhm_outputs(params, execution)
    execution.run(args)
    return out

def volume_estimate_fwhm(
    volume: InputPathType,
    roi: InputPathType | None = None,
    subvolume: str | None = None,
    whole_file: VolumeEstimateFwhmWholeFile | None = None,
    runner: Runner | None = None,
) -> VolumeEstimateFwhmOutputs:
    """
    ESTIMATE FWHM SMOOTHNESS OF A VOLUME

    Estimates the smoothness of the input volume in X, Y, and Z directions separately, printing the estimates to standard output, in mm as FWHM.  If -subvolume or -whole-file are not specified, each subvolume is estimated and displayed separately.

    Args:
        volume: the input volume
        roi: use only data within an ROI
        subvolume: select a single subvolume to estimate smoothness of
        whole_file: estimate for the whole file at once, not each subvolume\
            separately
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_estimate_fwhm_params(
        volume=volume,
        roi=roi,
        subvolume=subvolume,
        whole_file=whole_file,
    )
    return volume_estimate_fwhm_execute(params, runner)

__all__ = [
    "VolumeEstimateFwhm",
    "VolumeEstimateFwhmOutputs",
    "VOLUME_ESTIMATE_FWHM_METADATA",
    "volume_estimate_fwhm_cargs",
    "volume_estimate_fwhm_outputs",
    "volume_estimate_fwhm_params",
    "volume_estimate_fwhm_execute",
    "volume_estimate_fwhm_validate",
    "volume_estimate_fwhm",
]