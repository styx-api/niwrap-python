# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_TO_BORDER_METADATA = Metadata(
    id="label_to_border",
    name="DRAW BORDERS AROUND LABELS",
    package="workbench",
)

LabelToBorder = typing.TypedDict(
    "LabelToBorder",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_to_border"]],
        "surface": InputPathType,
        "label_in": InputPathType,
        "border_out": str,
        "placement": typing.NotRequired[float],
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LabelToBorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    border_out: OutputPathType
    """the output border file"""

def label_to_border_params(
    surface: InputPathType,
    label_in: InputPathType,
    border_out: str,
    placement: float | None = None,
    column: str | None = None,
) -> LabelToBorder:
    """
    Build parameters.

    Args:
        surface: the surface to use for neighbor information
        label_in: the input label file
        border_out: the output border file
        placement: set how far along the edge border points are drawn
        column: select a single column

    Returns:
        Parameter dictionary.
    """
    params: LabelToBorder = {
        "@type": "workbench/label_to_border",
        "surface": surface,
        "label_in": label_in,
        "border_out": border_out,
    }
    if placement is not None:
        params["placement"] = placement
    if column is not None:
        params["column"] = column
    return params

def label_to_border_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelToBorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("border_out", None) is None:
        raise StyxValidationError("`border_out` must not be None")
    if not isinstance(params["border_out"], str):
        raise StyxValidationError(f'`border_out` has the wrong type: Received `{type(params["border_out"])}` expected `str`')
    if params.get("placement", None) is not None:
        if not isinstance(params["placement"], (float, int)):
            raise StyxValidationError(f'`placement` has the wrong type: Received `{type(params["placement"])}` expected `float`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def label_to_border_cargs(params: LabelToBorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-to-border")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["border_out"])
    v_0 = params.get("placement")
    if v_0 is not None:
        cargs.append("-placement")
        cargs.append(str(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    return cargs

def label_to_border_outputs(params: LabelToBorder, execution: Execution) -> LabelToBorderOutputs:
    """Build the LabelToBorderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    border_out_v: OutputPathType = execution.output_file(params["border_out"])
    return LabelToBorderOutputs(
        root=root_v,
        border_out=border_out_v,
    )

def label_to_border_execute(params: LabelToBorder, runner: Runner | None = None) -> LabelToBorderOutputs:
    """
    DRAW BORDERS AROUND LABELS

    For each label, finds all edges on the mesh that cross the boundary of the label, and draws borders through them.  By default, this is done on all columns in the input file, using the map name as the class name for the border.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_to_border_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_TO_BORDER_METADATA)
    execution.params(params)
    args = label_to_border_cargs(params, execution)
    out = label_to_border_outputs(params, execution)
    execution.run(args)
    return out

def label_to_border(
    surface: InputPathType,
    label_in: InputPathType,
    border_out: str,
    placement: float | None = None,
    column: str | None = None,
    runner: Runner | None = None,
) -> LabelToBorderOutputs:
    """
    DRAW BORDERS AROUND LABELS

    For each label, finds all edges on the mesh that cross the boundary of the label, and draws borders through them.  By default, this is done on all columns in the input file, using the map name as the class name for the border.

    Args:
        surface: the surface to use for neighbor information
        label_in: the input label file
        border_out: the output border file
        placement: set how far along the edge border points are drawn
        column: select a single column
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_to_border_params(
        surface=surface,
        label_in=label_in,
        border_out=border_out,
        placement=placement,
        column=column,
    )
    return label_to_border_execute(params, runner)

__all__ = [
    "LabelToBorder",
    "LabelToBorderOutputs",
    "LABEL_TO_BORDER_METADATA",
    "label_to_border_cargs",
    "label_to_border_outputs",
    "label_to_border_params",
    "label_to_border_execute",
    "label_to_border_validate",
    "label_to_border",
]