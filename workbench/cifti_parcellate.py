# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_PARCELLATE_METADATA = Metadata(
    id="cifti_parcellate",
    name="PARCELLATE A CIFTI FILE",
    package="workbench",
)

class CiftiParcellateSpatialWeights(typing.TypedDict):
    left_area_surf: typing.NotRequired[InputPathType]
    """use a surface for left vertex areas"""
    right_area_surf: typing.NotRequired[InputPathType]
    """use a surface for right vertex areas"""
    cerebellum_area_surf: typing.NotRequired[InputPathType]
    """use a surface for cerebellum vertex areas"""
    left_area_metric: typing.NotRequired[InputPathType]
    """use a metric file for left vertex weights"""
    right_area_metric: typing.NotRequired[InputPathType]
    """use a metric file for right vertex weights"""
    cerebellum_area_metric: typing.NotRequired[InputPathType]
    """use a metric file for cerebellum vertex weights"""

class CiftiParcellateExcludeOutliers(typing.TypedDict):
    sigma_below: float
    """number of standard deviations below the mean to include"""
    sigma_above: float
    """number of standard deviations above the mean to include"""

class CiftiParcellateNonemptyMaskOut(typing.TypedDict):
    mask_out: str
    """the output mask file"""

CiftiParcellate = typing.TypedDict(
    "CiftiParcellate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_parcellate"]],
        "cifti_in": InputPathType,
        "cifti_label": InputPathType,
        "direction": str,
        "cifti_out": str,
        "spatial_weights": typing.NotRequired[CiftiParcellateSpatialWeights],
        "cifti_weights": typing.NotRequired[InputPathType],
        "method": typing.NotRequired[str],
        "exclude_outliers": typing.NotRequired[CiftiParcellateExcludeOutliers],
        "only_numeric": typing.NotRequired[bool],
        "fill_value": typing.NotRequired[float],
        "nonempty_mask_out": typing.NotRequired[CiftiParcellateNonemptyMaskOut],
        "legacy_mode": typing.NotRequired[bool],
        "include_empty": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiParcellateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """output cifti file"""
    mask_out: OutputPathType | None
    """the output mask file"""

def cifti_parcellate_params(
    cifti_in: InputPathType,
    cifti_label: InputPathType,
    direction: str,
    cifti_out: str,
    spatial_weights: CiftiParcellateSpatialWeights | None = None,
    cifti_weights: InputPathType | None = None,
    method: str | None = None,
    exclude_outliers: CiftiParcellateExcludeOutliers | None = None,
    only_numeric: bool = False,
    fill_value: float | None = None,
    nonempty_mask_out: CiftiParcellateNonemptyMaskOut | None = None,
    legacy_mode: bool = False,
    include_empty: bool = False,
) -> CiftiParcellate:
    """
    Build parameters.

    Args:
        cifti_in: the cifti file to parcellate
        cifti_label: a cifti label file to use for the parcellation
        direction: which mapping to parcellate (integer, ROW, or COLUMN)
        cifti_out: output cifti file
        spatial_weights: use voxel volume and either vertex areas or metric files\
            as weights
        cifti_weights: use a cifti file containing weights
        method: specify method of parcellation (default MEAN, or MODE if label\
            data)
        exclude_outliers: exclude non-numeric values and outliers from each parcel\
            by standard deviation
        only_numeric: exclude non-numeric values
        fill_value: specify value to use in empty parcels (default 0)
        nonempty_mask_out: output a matching pscalar file that has 0s in empty\
            parcels, and 1s elsewhere
        legacy_mode: use the old behavior, parcels are defined by the intersection\
            between labels and valid data, and empty parcels are discarded
        include_empty: deprecated: now the default behavior

    Returns:
        Parameter dictionary.
    """
    params: CiftiParcellate = {
        "@type": "workbench/cifti_parcellate",
        "cifti_in": cifti_in,
        "cifti_label": cifti_label,
        "direction": direction,
        "cifti_out": cifti_out,
        "only_numeric": only_numeric,
        "legacy_mode": legacy_mode,
        "include_empty": include_empty,
    }
    if spatial_weights is not None:
        params["spatial_weights"] = spatial_weights
    if cifti_weights is not None:
        params["cifti_weights"] = cifti_weights
    if method is not None:
        params["method"] = method
    if exclude_outliers is not None:
        params["exclude_outliers"] = exclude_outliers
    if fill_value is not None:
        params["fill_value"] = fill_value
    if nonempty_mask_out is not None:
        params["nonempty_mask_out"] = nonempty_mask_out
    return params

def cifti_parcellate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiParcellate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("cifti_label", None) is None:
        raise StyxValidationError("`cifti_label` must not be None")
    if not isinstance(params["cifti_label"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_label` has the wrong type: Received `{type(params["cifti_label"])}` expected `InputPathType`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("spatial_weights", None) is not None:
        if not isinstance(params["spatial_weights"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["spatial_weights"])}\'')
        if params["spatial_weights"].get("left_area_surf", None) is not None:
            if not isinstance(params["spatial_weights"]["left_area_surf"], (pathlib.Path, str)):
                raise StyxValidationError(f'`left_area_surf` has the wrong type: Received `{type(params["spatial_weights"]["left_area_surf"])}` expected `InputPathType`')
        if params["spatial_weights"].get("right_area_surf", None) is not None:
            if not isinstance(params["spatial_weights"]["right_area_surf"], (pathlib.Path, str)):
                raise StyxValidationError(f'`right_area_surf` has the wrong type: Received `{type(params["spatial_weights"]["right_area_surf"])}` expected `InputPathType`')
        if params["spatial_weights"].get("cerebellum_area_surf", None) is not None:
            if not isinstance(params["spatial_weights"]["cerebellum_area_surf"], (pathlib.Path, str)):
                raise StyxValidationError(f'`cerebellum_area_surf` has the wrong type: Received `{type(params["spatial_weights"]["cerebellum_area_surf"])}` expected `InputPathType`')
        if params["spatial_weights"].get("left_area_metric", None) is not None:
            if not isinstance(params["spatial_weights"]["left_area_metric"], (pathlib.Path, str)):
                raise StyxValidationError(f'`left_area_metric` has the wrong type: Received `{type(params["spatial_weights"]["left_area_metric"])}` expected `InputPathType`')
        if params["spatial_weights"].get("right_area_metric", None) is not None:
            if not isinstance(params["spatial_weights"]["right_area_metric"], (pathlib.Path, str)):
                raise StyxValidationError(f'`right_area_metric` has the wrong type: Received `{type(params["spatial_weights"]["right_area_metric"])}` expected `InputPathType`')
        if params["spatial_weights"].get("cerebellum_area_metric", None) is not None:
            if not isinstance(params["spatial_weights"]["cerebellum_area_metric"], (pathlib.Path, str)):
                raise StyxValidationError(f'`cerebellum_area_metric` has the wrong type: Received `{type(params["spatial_weights"]["cerebellum_area_metric"])}` expected `InputPathType`')
    if params.get("cifti_weights", None) is not None:
        if not isinstance(params["cifti_weights"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cifti_weights` has the wrong type: Received `{type(params["cifti_weights"])}` expected `InputPathType`')
    if params.get("method", None) is not None:
        if not isinstance(params["method"], str):
            raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')
    if params.get("exclude_outliers", None) is not None:
        if not isinstance(params["exclude_outliers"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_outliers"])}\'')
        if params["exclude_outliers"].get("sigma_below", None) is None:
            raise StyxValidationError("`sigma_below` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_below"], (float, int)):
            raise StyxValidationError(f'`sigma_below` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_below"])}` expected `float`')
        if params["exclude_outliers"].get("sigma_above", None) is None:
            raise StyxValidationError("`sigma_above` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_above"], (float, int)):
            raise StyxValidationError(f'`sigma_above` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_above"])}` expected `float`')
    if params.get("only_numeric", None) is not None:
        if not isinstance(params["only_numeric"], bool):
            raise StyxValidationError(f'`only_numeric` has the wrong type: Received `{type(params["only_numeric"])}` expected `bool`')
    if params.get("fill_value", None) is not None:
        if not isinstance(params["fill_value"], (float, int)):
            raise StyxValidationError(f'`fill_value` has the wrong type: Received `{type(params["fill_value"])}` expected `float`')
    if params.get("nonempty_mask_out", None) is not None:
        if not isinstance(params["nonempty_mask_out"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["nonempty_mask_out"])}\'')
        if params["nonempty_mask_out"].get("mask_out", None) is None:
            raise StyxValidationError("`mask_out` must not be None")
        if not isinstance(params["nonempty_mask_out"]["mask_out"], str):
            raise StyxValidationError(f'`mask_out` has the wrong type: Received `{type(params["nonempty_mask_out"]["mask_out"])}` expected `str`')
    if params.get("legacy_mode", None) is not None:
        if not isinstance(params["legacy_mode"], bool):
            raise StyxValidationError(f'`legacy_mode` has the wrong type: Received `{type(params["legacy_mode"])}` expected `bool`')
    if params.get("include_empty", None) is not None:
        if not isinstance(params["include_empty"], bool):
            raise StyxValidationError(f'`include_empty` has the wrong type: Received `{type(params["include_empty"])}` expected `bool`')

def cifti_parcellate_cargs(params: CiftiParcellate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-parcellate")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(execution.input_file(params["cifti_label"]))
    cargs.append(params["direction"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("spatial_weights")
    if v_0 is not None:
        cargs.append("-spatial-weights")
        v_1 = v_0.get("left_area_surf")
        if v_1 is not None:
            cargs.append("-left-area-surf")
            cargs.append(execution.input_file(v_1))
        v_2 = v_0.get("right_area_surf")
        if v_2 is not None:
            cargs.append("-right-area-surf")
            cargs.append(execution.input_file(v_2))
        v_3 = v_0.get("cerebellum_area_surf")
        if v_3 is not None:
            cargs.append("-cerebellum-area-surf")
            cargs.append(execution.input_file(v_3))
        v_4 = v_0.get("left_area_metric")
        if v_4 is not None:
            cargs.append("-left-area-metric")
            cargs.append(execution.input_file(v_4))
        v_5 = v_0.get("right_area_metric")
        if v_5 is not None:
            cargs.append("-right-area-metric")
            cargs.append(execution.input_file(v_5))
        v_6 = v_0.get("cerebellum_area_metric")
        if v_6 is not None:
            cargs.append("-cerebellum-area-metric")
            cargs.append(execution.input_file(v_6))
    v_7 = params.get("cifti_weights")
    if v_7 is not None:
        cargs.append("-cifti-weights")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("method")
    if v_8 is not None:
        cargs.append("-method")
        cargs.append(v_8)
    v_9 = params.get("exclude_outliers")
    if v_9 is not None:
        cargs.append("-exclude-outliers")
        cargs.append(str(v_9["sigma_below"]))
        cargs.append(str(v_9["sigma_above"]))
    if params.get("only_numeric"):
        cargs.append("-only-numeric")
    v_10 = params.get("fill_value")
    if v_10 is not None:
        cargs.append("-fill-value")
        cargs.append(str(v_10))
    v_11 = params.get("nonempty_mask_out")
    if v_11 is not None:
        cargs.append("-nonempty-mask-out")
        cargs.append(v_11["mask_out"])
    if params.get("legacy_mode"):
        cargs.append("-legacy-mode")
    if params.get("include_empty"):
        cargs.append("-include-empty")
    return cargs

def cifti_parcellate_outputs(params: CiftiParcellate, execution: Execution) -> CiftiParcellateOutputs:
    """Build the CiftiParcellateOutputs object for this tool."""
    mask_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    __v0 = params.get("nonempty_mask_out")
    if __v0 is not None:
        mask_out_v = execution.output_file(__v0["mask_out"])
    return CiftiParcellateOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
        mask_out=mask_out_v,
    )

def cifti_parcellate_execute(params: CiftiParcellate, runner: Runner | None = None) -> CiftiParcellateOutputs:
    """
    PARCELLATE A CIFTI FILE

    Each label (other than the unlabeled key) in the cifti label file will be treated as a parcel, and all rows or columns of data within the parcel are averaged together to form the parcel's output row or column.  If -legacy-mode is specified, parcels will be defined as the overlap between a label and the data, with no errors for missing data vertices or voxels, and empty parcels discarded.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  For dtseries or dscalar, use COLUMN.  If you are parcellating a dconn in both directions, parcellating by ROW first will use much less memory.

NOTE: the parcels in the output file are sorted by the numeric label keys, in ascending order.

The parameter to the -method option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data

The -*-weights options are mutually exclusive and may only be used with MEAN (default), SUM, STDEV, SAMPSTDEV, VARIANCE, MEDIAN, or MODE (default for label data).

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_parcellate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_PARCELLATE_METADATA)
    execution.params(params)
    args = cifti_parcellate_cargs(params, execution)
    out = cifti_parcellate_outputs(params, execution)
    execution.run(args)
    return out

def cifti_parcellate(
    cifti_in: InputPathType,
    cifti_label: InputPathType,
    direction: str,
    cifti_out: str,
    spatial_weights: CiftiParcellateSpatialWeights | None = None,
    cifti_weights: InputPathType | None = None,
    method: str | None = None,
    exclude_outliers: CiftiParcellateExcludeOutliers | None = None,
    only_numeric: bool = False,
    fill_value: float | None = None,
    nonempty_mask_out: CiftiParcellateNonemptyMaskOut | None = None,
    legacy_mode: bool = False,
    include_empty: bool = False,
    runner: Runner | None = None,
) -> CiftiParcellateOutputs:
    """
    PARCELLATE A CIFTI FILE

    Each label (other than the unlabeled key) in the cifti label file will be treated as a parcel, and all rows or columns of data within the parcel are averaged together to form the parcel's output row or column.  If -legacy-mode is specified, parcels will be defined as the overlap between a label and the data, with no errors for missing data vertices or voxels, and empty parcels discarded.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  For dtseries or dscalar, use COLUMN.  If you are parcellating a dconn in both directions, parcellating by ROW first will use much less memory.

NOTE: the parcels in the output file are sorted by the numeric label keys, in ascending order.

The parameter to the -method option must be one of the following:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data

The -*-weights options are mutually exclusive and may only be used with MEAN (default), SUM, STDEV, SAMPSTDEV, VARIANCE, MEDIAN, or MODE (default for label data).

    Args:
        cifti_in: the cifti file to parcellate
        cifti_label: a cifti label file to use for the parcellation
        direction: which mapping to parcellate (integer, ROW, or COLUMN)
        cifti_out: output cifti file
        spatial_weights: use voxel volume and either vertex areas or metric files\
            as weights
        cifti_weights: use a cifti file containing weights
        method: specify method of parcellation (default MEAN, or MODE if label\
            data)
        exclude_outliers: exclude non-numeric values and outliers from each parcel\
            by standard deviation
        only_numeric: exclude non-numeric values
        fill_value: specify value to use in empty parcels (default 0)
        nonempty_mask_out: output a matching pscalar file that has 0s in empty\
            parcels, and 1s elsewhere
        legacy_mode: use the old behavior, parcels are defined by the intersection\
            between labels and valid data, and empty parcels are discarded
        include_empty: deprecated: now the default behavior
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_parcellate_params(
        cifti_in=cifti_in,
        cifti_label=cifti_label,
        direction=direction,
        cifti_out=cifti_out,
        spatial_weights=spatial_weights,
        cifti_weights=cifti_weights,
        method=method,
        exclude_outliers=exclude_outliers,
        only_numeric=only_numeric,
        fill_value=fill_value,
        nonempty_mask_out=nonempty_mask_out,
        legacy_mode=legacy_mode,
        include_empty=include_empty,
    )
    return cifti_parcellate_execute(params, runner)

__all__ = [
    "CiftiParcellate",
    "CiftiParcellateOutputs",
    "CIFTI_PARCELLATE_METADATA",
    "cifti_parcellate_cargs",
    "cifti_parcellate_outputs",
    "cifti_parcellate_params",
    "cifti_parcellate_execute",
    "cifti_parcellate_validate",
    "cifti_parcellate",
]