# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_REDUCE_METADATA = Metadata(
    id="cifti_reduce",
    name="PERFORM REDUCTION OPERATION ON A CIFTI FILE",
    package="workbench",
)

class CiftiReduceExcludeOutliers(typing.TypedDict):
    sigma_below: float
    """number of standard deviations below the mean to include"""
    sigma_above: float
    """number of standard deviations above the mean to include"""

CiftiReduce = typing.TypedDict(
    "CiftiReduce",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_reduce"]],
        "cifti_in": InputPathType,
        "operation": str,
        "cifti_out": str,
        "direction": typing.NotRequired[str],
        "exclude_outliers": typing.NotRequired[CiftiReduceExcludeOutliers],
        "only_numeric": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiReduceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output cifti file"""

def cifti_reduce_params(
    cifti_in: InputPathType,
    operation: str,
    cifti_out: str,
    direction: str | None = None,
    exclude_outliers: CiftiReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
) -> CiftiReduce:
    """
    Build parameters.

    Args:
        cifti_in: the cifti file to reduce
        operation: the reduction operator to use
        cifti_out: the output cifti file
        direction: specify what direction to reduce along
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values

    Returns:
        Parameter dictionary.
    """
    params: CiftiReduce = {
        "@type": "workbench/cifti_reduce",
        "cifti_in": cifti_in,
        "operation": operation,
        "cifti_out": cifti_out,
        "only_numeric": only_numeric,
    }
    if direction is not None:
        params["direction"] = direction
    if exclude_outliers is not None:
        params["exclude_outliers"] = exclude_outliers
    return params

def cifti_reduce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiReduce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("direction", None) is not None:
        if not isinstance(params["direction"], str):
            raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("exclude_outliers", None) is not None:
        if not isinstance(params["exclude_outliers"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_outliers"])}\'')
        if params["exclude_outliers"].get("sigma_below", None) is None:
            raise StyxValidationError("`sigma_below` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_below"], (float, int)):
            raise StyxValidationError(f'`sigma_below` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_below"])}` expected `float`')
        if params["exclude_outliers"].get("sigma_above", None) is None:
            raise StyxValidationError("`sigma_above` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_above"], (float, int)):
            raise StyxValidationError(f'`sigma_above` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_above"])}` expected `float`')
    if params.get("only_numeric", None) is not None:
        if not isinstance(params["only_numeric"], bool):
            raise StyxValidationError(f'`only_numeric` has the wrong type: Received `{type(params["only_numeric"])}` expected `bool`')

def cifti_reduce_cargs(params: CiftiReduce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-reduce")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["operation"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("direction")
    if v_0 is not None:
        cargs.append("-direction")
        cargs.append(v_0)
    v_1 = params.get("exclude_outliers")
    if v_1 is not None:
        cargs.append("-exclude-outliers")
        cargs.append(str(v_1["sigma_below"]))
        cargs.append(str(v_1["sigma_above"]))
    if params.get("only_numeric"):
        cargs.append("-only-numeric")
    return cargs

def cifti_reduce_outputs(params: CiftiReduce, execution: Execution) -> CiftiReduceOutputs:
    """Build the CiftiReduceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiReduceOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_reduce_execute(params: CiftiReduce, runner: Runner | None = None) -> CiftiReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ON A CIFTI FILE

    For the specified direction (default ROW), perform a reduction operation along that direction.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_reduce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_REDUCE_METADATA)
    execution.params(params)
    args = cifti_reduce_cargs(params, execution)
    out = cifti_reduce_outputs(params, execution)
    execution.run(args)
    return out

def cifti_reduce(
    cifti_in: InputPathType,
    operation: str,
    cifti_out: str,
    direction: str | None = None,
    exclude_outliers: CiftiReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
    runner: Runner | None = None,
) -> CiftiReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ON A CIFTI FILE

    For the specified direction (default ROW), perform a reduction operation along that direction.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        cifti_in: the cifti file to reduce
        operation: the reduction operator to use
        cifti_out: the output cifti file
        direction: specify what direction to reduce along
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_reduce_params(
        cifti_in=cifti_in,
        operation=operation,
        cifti_out=cifti_out,
        direction=direction,
        exclude_outliers=exclude_outliers,
        only_numeric=only_numeric,
    )
    return cifti_reduce_execute(params, runner)

__all__ = [
    "CiftiReduce",
    "CiftiReduceOutputs",
    "CIFTI_REDUCE_METADATA",
    "cifti_reduce_cargs",
    "cifti_reduce_outputs",
    "cifti_reduce_params",
    "cifti_reduce_execute",
    "cifti_reduce_validate",
    "cifti_reduce",
]