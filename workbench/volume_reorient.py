# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_REORIENT_METADATA = Metadata(
    id="volume_reorient",
    name="CHANGE VOXEL ORDER OF A VOLUME FILE",
    package="workbench",
)

VolumeReorient = typing.TypedDict(
    "VolumeReorient",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_reorient"]],
        "volume": InputPathType,
        "orient_string": str,
        "volume_out": str,
    },
)

@dataclasses.dataclass
class VolumeReorientOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def volume_reorient_params(
    volume: InputPathType,
    orient_string: str,
    volume_out: str,
) -> VolumeReorient:
    """
    Build parameters.

    Args:
        volume: the volume to reorient
        orient_string: the desired orientation
        volume_out: out - the reoriented volume

    Returns:
        Parameter dictionary.
    """
    params: VolumeReorient = {
        "@type": "workbench/volume_reorient",
        "volume": volume,
        "orient_string": orient_string,
        "volume_out": volume_out,
    }
    return params

def volume_reorient_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeReorient."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("orient_string", None) is None:
        raise StyxValidationError("`orient_string` must not be None")
    if not isinstance(params["orient_string"], str):
        raise StyxValidationError(f'`orient_string` has the wrong type: Received `{type(params["orient_string"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')

def volume_reorient_cargs(params: VolumeReorient, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-reorient")
    cargs.append(execution.input_file(params["volume"]))
    cargs.append(params["orient_string"])
    cargs.append(params["volume_out"])
    return cargs

def volume_reorient_outputs(params: VolumeReorient, execution: Execution) -> VolumeReorientOutputs:
    """Build the VolumeReorientOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolumeReorientOutputs(
        root=root_v,
    )

def volume_reorient_execute(params: VolumeReorient, runner: Runner | None = None) -> VolumeReorientOutputs:
    """
    CHANGE VOXEL ORDER OF A VOLUME FILE

    Changes the voxel order and the header spacing/origin information such that the value of any spatial point is unchanged.  Orientation strings look like 'LPI', which means first index is left to right, second is posterior to anterior, and third is inferior to superior.  The valid characters are:

L      left to right
R      right to left
P      posterior to anterior
A      anterior to posterior
I      inferior to superior
S      superior to inferior

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_reorient_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_REORIENT_METADATA)
    execution.params(params)
    args = volume_reorient_cargs(params, execution)
    out = volume_reorient_outputs(params, execution)
    execution.run(args)
    return out

def volume_reorient(
    volume: InputPathType,
    orient_string: str,
    volume_out: str,
    runner: Runner | None = None,
) -> VolumeReorientOutputs:
    """
    CHANGE VOXEL ORDER OF A VOLUME FILE

    Changes the voxel order and the header spacing/origin information such that the value of any spatial point is unchanged.  Orientation strings look like 'LPI', which means first index is left to right, second is posterior to anterior, and third is inferior to superior.  The valid characters are:

L      left to right
R      right to left
P      posterior to anterior
A      anterior to posterior
I      inferior to superior
S      superior to inferior

    Args:
        volume: the volume to reorient
        orient_string: the desired orientation
        volume_out: out - the reoriented volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_reorient_params(
        volume=volume,
        orient_string=orient_string,
        volume_out=volume_out,
    )
    return volume_reorient_execute(params, runner)

__all__ = [
    "VolumeReorient",
    "VolumeReorientOutputs",
    "VOLUME_REORIENT_METADATA",
    "volume_reorient_cargs",
    "volume_reorient_outputs",
    "volume_reorient_params",
    "volume_reorient_execute",
    "volume_reorient_validate",
    "volume_reorient",
]