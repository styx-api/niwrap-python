# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_TFCE_METADATA = Metadata(
    id="metric_tfce",
    name="DO TFCE ON A METRIC FILE",
    package="workbench",
)

class MetricTfcePresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class MetricTfceParameters(typing.TypedDict):
    e: float
    """exponent for cluster area (default 1.0)"""
    h: float
    """exponent for threshold value (default 2.0)"""

MetricTfce = typing.TypedDict(
    "MetricTfce",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_tfce"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "metric_out": str,
        "presmooth": typing.NotRequired[MetricTfcePresmooth],
        "roi": typing.NotRequired[InputPathType],
        "parameters": typing.NotRequired[MetricTfceParameters],
        "column": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MetricTfceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_tfce_params(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    presmooth: MetricTfcePresmooth | None = None,
    roi: InputPathType | None = None,
    parameters: MetricTfceParameters | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
) -> MetricTfce:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        metric_in: the metric to run TFCE on
        metric_out: the output metric
        presmooth: smooth the metric before running TFCE
        roi: select a region of interest to run TFCE on
        parameters: set parameters for TFCE integral
        column: select a single column
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: MetricTfce = {
        "@type": "workbench/metric_tfce",
        "surface": surface,
        "metric_in": metric_in,
        "metric_out": metric_out,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if parameters is not None:
        params["parameters"] = parameters
    if column is not None:
        params["column"] = column
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def metric_tfce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricTfce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("parameters", None) is not None:
        if not isinstance(params["parameters"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["parameters"])}\'')
        if params["parameters"].get("e", None) is None:
            raise StyxValidationError("`e` must not be None")
        if not isinstance(params["parameters"]["e"], (float, int)):
            raise StyxValidationError(f'`e` has the wrong type: Received `{type(params["parameters"]["e"])}` expected `float`')
        if params["parameters"].get("h", None) is None:
            raise StyxValidationError("`h` must not be None")
        if not isinstance(params["parameters"]["h"], (float, int)):
            raise StyxValidationError(f'`h` has the wrong type: Received `{type(params["parameters"]["h"])}` expected `float`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def metric_tfce_cargs(params: MetricTfce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-tfce")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("parameters")
    if v_2 is not None:
        cargs.append("-parameters")
        cargs.append(str(v_2["e"]))
        cargs.append(str(v_2["h"]))
    v_3 = params.get("column")
    if v_3 is not None:
        cargs.append("-column")
        cargs.append(v_3)
    v_4 = params.get("corrected_areas")
    if v_4 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_4))
    return cargs

def metric_tfce_outputs(params: MetricTfce, execution: Execution) -> MetricTfceOutputs:
    """Build the MetricTfceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricTfceOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_tfce_execute(params: MetricTfce, runner: Runner | None = None) -> MetricTfceOutputs:
    """
    DO TFCE ON A METRIC FILE

    This command does not do any statistical analysis.  Please use something like PALM if you are just trying to do statistics on your data.

Threshold-free cluster enhancement is a method to increase the relative value of regions that would form clusters in a standard thresholding test.  This is accomplished by evaluating the integral of:

e(h, p)^E * h^H * dh

at each vertex p, where h ranges from 0 to the maximum value in the data, and e(h, p) is the extent of the cluster containing vertex p at threshold h.  Negative values are similarly enhanced by negating the data, running the same process, and negating the result.

When using -presmooth with -corrected-areas, note that it is an approximate correction within the smoothing algorithm (the TFCE correction is exact).  Doing smoothing on individual surfaces before averaging/TFCE is preferred, when possible, in order to better tie the smoothing kernel size to the original feature size.

The TFCE method is explained in: Smith SM, Nichols TE., "Threshold-free cluster enhancement: addressing problems of smoothing, threshold dependence and localisation in cluster inference." Neuroimage. 2009 Jan 1;44(1):83-98. PMID: 18501637

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_tfce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_TFCE_METADATA)
    execution.params(params)
    args = metric_tfce_cargs(params, execution)
    out = metric_tfce_outputs(params, execution)
    execution.run(args)
    return out

def metric_tfce(
    surface: InputPathType,
    metric_in: InputPathType,
    metric_out: str,
    presmooth: MetricTfcePresmooth | None = None,
    roi: InputPathType | None = None,
    parameters: MetricTfceParameters | None = None,
    column: str | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> MetricTfceOutputs:
    """
    DO TFCE ON A METRIC FILE

    This command does not do any statistical analysis.  Please use something like PALM if you are just trying to do statistics on your data.

Threshold-free cluster enhancement is a method to increase the relative value of regions that would form clusters in a standard thresholding test.  This is accomplished by evaluating the integral of:

e(h, p)^E * h^H * dh

at each vertex p, where h ranges from 0 to the maximum value in the data, and e(h, p) is the extent of the cluster containing vertex p at threshold h.  Negative values are similarly enhanced by negating the data, running the same process, and negating the result.

When using -presmooth with -corrected-areas, note that it is an approximate correction within the smoothing algorithm (the TFCE correction is exact).  Doing smoothing on individual surfaces before averaging/TFCE is preferred, when possible, in order to better tie the smoothing kernel size to the original feature size.

The TFCE method is explained in: Smith SM, Nichols TE., "Threshold-free cluster enhancement: addressing problems of smoothing, threshold dependence and localisation in cluster inference." Neuroimage. 2009 Jan 1;44(1):83-98. PMID: 18501637

    Args:
        surface: the surface to compute on
        metric_in: the metric to run TFCE on
        metric_out: the output metric
        presmooth: smooth the metric before running TFCE
        roi: select a region of interest to run TFCE on
        parameters: set parameters for TFCE integral
        column: select a single column
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_tfce_params(
        surface=surface,
        metric_in=metric_in,
        metric_out=metric_out,
        presmooth=presmooth,
        roi=roi,
        parameters=parameters,
        column=column,
        corrected_areas=corrected_areas,
    )
    return metric_tfce_execute(params, runner)

__all__ = [
    "MetricTfce",
    "MetricTfceOutputs",
    "METRIC_TFCE_METADATA",
    "metric_tfce_cargs",
    "metric_tfce_outputs",
    "metric_tfce_params",
    "metric_tfce_execute",
    "metric_tfce_validate",
    "metric_tfce",
]