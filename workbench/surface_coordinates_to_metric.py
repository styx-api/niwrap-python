# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_COORDINATES_TO_METRIC_METADATA = Metadata(
    id="surface_coordinates_to_metric",
    name="MAKE METRIC FILE OF SURFACE COORDINATES",
    package="workbench",
)

SurfaceCoordinatesToMetric = typing.TypedDict(
    "SurfaceCoordinatesToMetric",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_coordinates_to_metric"]],
        "surface": InputPathType,
        "metric_out": str,
    },
)

@dataclasses.dataclass
class SurfaceCoordinatesToMetricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def surface_coordinates_to_metric_params(
    surface: InputPathType,
    metric_out: str,
) -> SurfaceCoordinatesToMetric:
    """
    Build parameters.

    Args:
        surface: the surface to use the coordinates of
        metric_out: the output metric

    Returns:
        Parameter dictionary.
    """
    params: SurfaceCoordinatesToMetric = {
        "@type": "workbench/surface_coordinates_to_metric",
        "surface": surface,
        "metric_out": metric_out,
    }
    return params

def surface_coordinates_to_metric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceCoordinatesToMetric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')

def surface_coordinates_to_metric_cargs(params: SurfaceCoordinatesToMetric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-coordinates-to-metric")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["metric_out"])
    return cargs

def surface_coordinates_to_metric_outputs(params: SurfaceCoordinatesToMetric, execution: Execution) -> SurfaceCoordinatesToMetricOutputs:
    """Build the SurfaceCoordinatesToMetricOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return SurfaceCoordinatesToMetricOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def surface_coordinates_to_metric_execute(params: SurfaceCoordinatesToMetric, runner: Runner | None = None) -> SurfaceCoordinatesToMetricOutputs:
    """
    MAKE METRIC FILE OF SURFACE COORDINATES

    Puts the coordinates of the surface into a 3-map metric file, as x, y, z.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_coordinates_to_metric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_COORDINATES_TO_METRIC_METADATA)
    execution.params(params)
    args = surface_coordinates_to_metric_cargs(params, execution)
    out = surface_coordinates_to_metric_outputs(params, execution)
    execution.run(args)
    return out

def surface_coordinates_to_metric(
    surface: InputPathType,
    metric_out: str,
    runner: Runner | None = None,
) -> SurfaceCoordinatesToMetricOutputs:
    """
    MAKE METRIC FILE OF SURFACE COORDINATES

    Puts the coordinates of the surface into a 3-map metric file, as x, y, z.

    Args:
        surface: the surface to use the coordinates of
        metric_out: the output metric
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_coordinates_to_metric_params(
        surface=surface,
        metric_out=metric_out,
    )
    return surface_coordinates_to_metric_execute(params, runner)

__all__ = [
    "SurfaceCoordinatesToMetric",
    "SurfaceCoordinatesToMetricOutputs",
    "SURFACE_COORDINATES_TO_METRIC_METADATA",
    "surface_coordinates_to_metric_cargs",
    "surface_coordinates_to_metric_outputs",
    "surface_coordinates_to_metric_params",
    "surface_coordinates_to_metric_execute",
    "surface_coordinates_to_metric_validate",
    "surface_coordinates_to_metric",
]