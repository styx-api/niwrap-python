# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_CORTEX_LAYER_METADATA = Metadata(
    id="surface_cortex_layer",
    name="CREATE SURFACE APPROXIMATING A CORTICAL LAYER",
    package="workbench",
)

class SurfaceCortexLayerPlacementOut(typing.TypedDict):
    placement_metric: str
    """output metric"""

SurfaceCortexLayer = typing.TypedDict(
    "SurfaceCortexLayer",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_cortex_layer"]],
        "white_surface": InputPathType,
        "pial_surface": InputPathType,
        "location": float,
        "out_surface": str,
        "placement_out": typing.NotRequired[SurfaceCortexLayerPlacementOut],
    },
)

@dataclasses.dataclass
class SurfaceCortexLayerOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_surface: OutputPathType
    """the output surface"""
    placement_metric: OutputPathType | None
    """output metric"""

def surface_cortex_layer_params(
    white_surface: InputPathType,
    pial_surface: InputPathType,
    location: float,
    out_surface: str,
    placement_out: SurfaceCortexLayerPlacementOut | None = None,
) -> SurfaceCortexLayer:
    """
    Build parameters.

    Args:
        white_surface: the white matter surface
        pial_surface: the pial surface
        location: what volume fraction to place the layer at
        out_surface: the output surface
        placement_out: output the placement as a volume fraction from pial to white

    Returns:
        Parameter dictionary.
    """
    params: SurfaceCortexLayer = {
        "@type": "workbench/surface_cortex_layer",
        "white_surface": white_surface,
        "pial_surface": pial_surface,
        "location": location,
        "out_surface": out_surface,
    }
    if placement_out is not None:
        params["placement_out"] = placement_out
    return params

def surface_cortex_layer_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceCortexLayer."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("white_surface", None) is None:
        raise StyxValidationError("`white_surface` must not be None")
    if not isinstance(params["white_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`white_surface` has the wrong type: Received `{type(params["white_surface"])}` expected `InputPathType`')
    if params.get("pial_surface", None) is None:
        raise StyxValidationError("`pial_surface` must not be None")
    if not isinstance(params["pial_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pial_surface` has the wrong type: Received `{type(params["pial_surface"])}` expected `InputPathType`')
    if params.get("location", None) is None:
        raise StyxValidationError("`location` must not be None")
    if not isinstance(params["location"], (float, int)):
        raise StyxValidationError(f'`location` has the wrong type: Received `{type(params["location"])}` expected `float`')
    if params.get("out_surface", None) is None:
        raise StyxValidationError("`out_surface` must not be None")
    if not isinstance(params["out_surface"], str):
        raise StyxValidationError(f'`out_surface` has the wrong type: Received `{type(params["out_surface"])}` expected `str`')
    if params.get("placement_out", None) is not None:
        if not isinstance(params["placement_out"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["placement_out"])}\'')
        if params["placement_out"].get("placement_metric", None) is None:
            raise StyxValidationError("`placement_metric` must not be None")
        if not isinstance(params["placement_out"]["placement_metric"], str):
            raise StyxValidationError(f'`placement_metric` has the wrong type: Received `{type(params["placement_out"]["placement_metric"])}` expected `str`')

def surface_cortex_layer_cargs(params: SurfaceCortexLayer, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-cortex-layer")
    cargs.append(execution.input_file(params["white_surface"]))
    cargs.append(execution.input_file(params["pial_surface"]))
    cargs.append(str(params["location"]))
    cargs.append(params["out_surface"])
    v_0 = params.get("placement_out")
    if v_0 is not None:
        cargs.append("-placement-out")
        cargs.append(v_0["placement_metric"])
    return cargs

def surface_cortex_layer_outputs(params: SurfaceCortexLayer, execution: Execution) -> SurfaceCortexLayerOutputs:
    """Build the SurfaceCortexLayerOutputs object for this tool."""
    placement_metric_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_surface_v: OutputPathType = execution.output_file(params["out_surface"])
    __v0 = params.get("placement_out")
    if __v0 is not None:
        placement_metric_v = execution.output_file(__v0["placement_metric"])
    return SurfaceCortexLayerOutputs(
        root=root_v,
        out_surface=out_surface_v,
        placement_metric=placement_metric_v,
    )

def surface_cortex_layer_execute(params: SurfaceCortexLayer, runner: Runner | None = None) -> SurfaceCortexLayerOutputs:
    """
    CREATE SURFACE APPROXIMATING A CORTICAL LAYER

    The input surfaces must have vertex correspondence.  The output surface is generated by placing vertices between the two surfaces such that the enclosed volume within any small patch of the new and white surfaces is the given fraction of the volume of the same patch between the pial and white surfaces (i.e., specifying 0 would give the white surface, 1 would give the pial surface).  

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_cortex_layer_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_CORTEX_LAYER_METADATA)
    execution.params(params)
    args = surface_cortex_layer_cargs(params, execution)
    out = surface_cortex_layer_outputs(params, execution)
    execution.run(args)
    return out

def surface_cortex_layer(
    white_surface: InputPathType,
    pial_surface: InputPathType,
    location: float,
    out_surface: str,
    placement_out: SurfaceCortexLayerPlacementOut | None = None,
    runner: Runner | None = None,
) -> SurfaceCortexLayerOutputs:
    """
    CREATE SURFACE APPROXIMATING A CORTICAL LAYER

    The input surfaces must have vertex correspondence.  The output surface is generated by placing vertices between the two surfaces such that the enclosed volume within any small patch of the new and white surfaces is the given fraction of the volume of the same patch between the pial and white surfaces (i.e., specifying 0 would give the white surface, 1 would give the pial surface).  

    Args:
        white_surface: the white matter surface
        pial_surface: the pial surface
        location: what volume fraction to place the layer at
        out_surface: the output surface
        placement_out: output the placement as a volume fraction from pial to white
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_cortex_layer_params(
        white_surface=white_surface,
        pial_surface=pial_surface,
        location=location,
        out_surface=out_surface,
        placement_out=placement_out,
    )
    return surface_cortex_layer_execute(params, runner)

__all__ = [
    "SurfaceCortexLayer",
    "SurfaceCortexLayerOutputs",
    "SURFACE_CORTEX_LAYER_METADATA",
    "surface_cortex_layer_cargs",
    "surface_cortex_layer_outputs",
    "surface_cortex_layer_params",
    "surface_cortex_layer_execute",
    "surface_cortex_layer_validate",
    "surface_cortex_layer",
]