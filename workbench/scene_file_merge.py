# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SCENE_FILE_MERGE_METADATA = Metadata(
    id="scene_file_merge",
    name="REARRANGE SCENES INTO A NEW FILE",
    package="workbench",
)

class SceneFileMergeUpTo(typing.TypedDict):
    last_column: str
    """the number or name of the last scene to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class SceneFileMergeScene(typing.TypedDict):
    scene: str
    """the scene number or name"""
    up_to: typing.NotRequired[SceneFileMergeUpTo]
    """use an inclusive range of scenes"""

class SceneFileMergeSceneFile(typing.TypedDict):
    scene_file: str
    """the input scene file"""
    scene: list[SceneFileMergeScene]
    """specify a scene to use"""

SceneFileMerge = typing.TypedDict(
    "SceneFileMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/scene_file_merge"]],
        "scene_file_out": str,
        "scene_file": list[SceneFileMergeSceneFile],
    },
)

@dataclasses.dataclass
class SceneFileMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def scene_file_merge_params(
    scene_file_out: str,
    scene_file: list[SceneFileMergeSceneFile],
) -> SceneFileMerge:
    """
    Build parameters.

    Args:
        scene_file_out: output - the output scene file
        scene_file: specify a scene file to use scenes from

    Returns:
        Parameter dictionary.
    """
    params: SceneFileMerge = {
        "@type": "workbench/scene_file_merge",
        "scene_file_out": scene_file_out,
        "scene_file": scene_file,
    }
    return params

def scene_file_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SceneFileMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scene_file_out", None) is None:
        raise StyxValidationError("`scene_file_out` must not be None")
    if not isinstance(params["scene_file_out"], str):
        raise StyxValidationError(f'`scene_file_out` has the wrong type: Received `{type(params["scene_file_out"])}` expected `str`')
    if params.get("scene_file", None) is None:
        raise StyxValidationError("`scene_file` must not be None")
    if not isinstance(params["scene_file"], list):
        raise StyxValidationError(f'`scene_file` has the wrong type: Received `{type(params["scene_file"])}` expected `list[SceneFileMergeSceneFile]`')
    for e in params["scene_file"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("scene_file", None) is None:
            raise StyxValidationError("`scene_file` must not be None")
        if not isinstance(e["scene_file"], str):
            raise StyxValidationError(f'`scene_file` has the wrong type: Received `{type(e["scene_file"])}` expected `str`')
        if e.get("scene", None) is None:
            raise StyxValidationError("`scene` must not be None")
        if not isinstance(e["scene"], list):
            raise StyxValidationError(f'`scene` has the wrong type: Received `{type(e["scene"])}` expected `list[SceneFileMergeScene]`')
        for e_2 in e["scene"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("scene", None) is None:
                raise StyxValidationError("`scene` must not be None")
            if not isinstance(e_2["scene"], str):
                raise StyxValidationError(f'`scene` has the wrong type: Received `{type(e_2["scene"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_column", None) is None:
                    raise StyxValidationError("`last_column` must not be None")
                if not isinstance(e_2["up_to"]["last_column"], str):
                    raise StyxValidationError(f'`last_column` has the wrong type: Received `{type(e_2["up_to"]["last_column"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def scene_file_merge_cargs(params: SceneFileMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-scene-file-merge")
    cargs.append(params["scene_file_out"])
    for item0 in params["scene_file"]:
        cargs.append("-scene-file")
        cargs.append(item0["scene_file"])
        for item1 in item0["scene"]:
            cargs.append("-scene")
            cargs.append(item1["scene"])
            v_0 = item1.get("up_to")
            if v_0 is not None:
                cargs.append("-up-to")
                cargs.append(v_0["last_column"])
                if v_0.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def scene_file_merge_outputs(params: SceneFileMerge, execution: Execution) -> SceneFileMergeOutputs:
    """Build the SceneFileMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SceneFileMergeOutputs(
        root=root_v,
    )

def scene_file_merge_execute(params: SceneFileMerge, runner: Runner | None = None) -> SceneFileMergeOutputs:
    """
    REARRANGE SCENES INTO A NEW FILE

    Takes one or more scene files and constructs a new scene file by concatenating specified scenes from them.

Example: wb_command -scene-file-merge out.scene -scene-file first.scene -scene 1 -scene-file second.scene

This example would take the first scene from first.scene, followed by all scenes from second.scene, and write these scenes to out.scene.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    scene_file_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SCENE_FILE_MERGE_METADATA)
    execution.params(params)
    args = scene_file_merge_cargs(params, execution)
    out = scene_file_merge_outputs(params, execution)
    execution.run(args)
    return out

def scene_file_merge(
    scene_file_out: str,
    scene_file: list[SceneFileMergeSceneFile],
    runner: Runner | None = None,
) -> SceneFileMergeOutputs:
    """
    REARRANGE SCENES INTO A NEW FILE

    Takes one or more scene files and constructs a new scene file by concatenating specified scenes from them.

Example: wb_command -scene-file-merge out.scene -scene-file first.scene -scene 1 -scene-file second.scene

This example would take the first scene from first.scene, followed by all scenes from second.scene, and write these scenes to out.scene.

    Args:
        scene_file_out: output - the output scene file
        scene_file: specify a scene file to use scenes from
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = scene_file_merge_params(
        scene_file_out=scene_file_out,
        scene_file=scene_file,
    )
    return scene_file_merge_execute(params, runner)

__all__ = [
    "SceneFileMerge",
    "SceneFileMergeOutputs",
    "SCENE_FILE_MERGE_METADATA",
    "scene_file_merge_cargs",
    "scene_file_merge_outputs",
    "scene_file_merge_params",
    "scene_file_merge_execute",
    "scene_file_merge_validate",
    "scene_file_merge",
]