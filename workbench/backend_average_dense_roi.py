# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BACKEND_AVERAGE_DENSE_ROI_METADATA = Metadata(
    id="backend_average_dense_roi",
    name="CONNECTOME DB BACKEND COMMAND FOR CIFTI AVERAGE DENSE ROI",
    package="workbench",
)

BackendAverageDenseRoi = typing.TypedDict(
    "BackendAverageDenseRoi",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/backend_average_dense_roi"]],
        "index_list": str,
        "out_file": str,
    },
)

@dataclasses.dataclass
class BackendAverageDenseRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def backend_average_dense_roi_params(
    index_list: str,
    out_file: str,
) -> BackendAverageDenseRoi:
    """
    Build parameters.

    Args:
        index_list: comma separated list of cifti indexes to average
        out_file: file to write the average row to

    Returns:
        Parameter dictionary.
    """
    params: BackendAverageDenseRoi = {
        "@type": "workbench/backend_average_dense_roi",
        "index_list": index_list,
        "out_file": out_file,
    }
    return params

def backend_average_dense_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BackendAverageDenseRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("index_list", None) is None:
        raise StyxValidationError("`index_list` must not be None")
    if not isinstance(params["index_list"], str):
        raise StyxValidationError(f'`index_list` has the wrong type: Received `{type(params["index_list"])}` expected `str`')
    if params.get("out_file", None) is None:
        raise StyxValidationError("`out_file` must not be None")
    if not isinstance(params["out_file"], str):
        raise StyxValidationError(f'`out_file` has the wrong type: Received `{type(params["out_file"])}` expected `str`')

def backend_average_dense_roi_cargs(params: BackendAverageDenseRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-backend-average-dense-roi")
    cargs.append(params["index_list"])
    cargs.append(params["out_file"])
    return cargs

def backend_average_dense_roi_outputs(params: BackendAverageDenseRoi, execution: Execution) -> BackendAverageDenseRoiOutputs:
    """Build the BackendAverageDenseRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BackendAverageDenseRoiOutputs(
        root=root_v,
    )

def backend_average_dense_roi_execute(params: BackendAverageDenseRoi, runner: Runner | None = None) -> BackendAverageDenseRoiOutputs:
    """
    CONNECTOME DB BACKEND COMMAND FOR CIFTI AVERAGE DENSE ROI

    This command is probably not the one you are looking for, try -cifti-average-dense-roi.  It takes the list of cifti files to average from standard input, and writes its output as little endian, 32-bit integer of row size followed by the row as 32-bit floats.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    backend_average_dense_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BACKEND_AVERAGE_DENSE_ROI_METADATA)
    execution.params(params)
    args = backend_average_dense_roi_cargs(params, execution)
    out = backend_average_dense_roi_outputs(params, execution)
    execution.run(args)
    return out

def backend_average_dense_roi(
    index_list: str,
    out_file: str,
    runner: Runner | None = None,
) -> BackendAverageDenseRoiOutputs:
    """
    CONNECTOME DB BACKEND COMMAND FOR CIFTI AVERAGE DENSE ROI

    This command is probably not the one you are looking for, try -cifti-average-dense-roi.  It takes the list of cifti files to average from standard input, and writes its output as little endian, 32-bit integer of row size followed by the row as 32-bit floats.

    Args:
        index_list: comma separated list of cifti indexes to average
        out_file: file to write the average row to
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = backend_average_dense_roi_params(
        index_list=index_list,
        out_file=out_file,
    )
    return backend_average_dense_roi_execute(params, runner)

__all__ = [
    "BackendAverageDenseRoi",
    "BackendAverageDenseRoiOutputs",
    "BACKEND_AVERAGE_DENSE_ROI_METADATA",
    "backend_average_dense_roi_cargs",
    "backend_average_dense_roi_outputs",
    "backend_average_dense_roi_params",
    "backend_average_dense_roi_execute",
    "backend_average_dense_roi_validate",
    "backend_average_dense_roi",
]