# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANNOTATION_RESAMPLE_METADATA = Metadata(
    id="annotation_resample",
    name="RESAMPLE AN ANNOTATION FILE TO DIFFERENT MESHES",
    package="workbench",
)

class AnnotationResampleSurfacePair(typing.TypedDict):
    source_surface: InputPathType
    """the midthickness surface of the current mesh the annotations use"""
    target_surface: InputPathType
    """the midthickness surface of the mesh the annotations should be transferred to"""

AnnotationResample = typing.TypedDict(
    "AnnotationResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/annotation_resample"]],
        "annotation_in": InputPathType,
        "annotation_out": str,
        "surface_pair": list[AnnotationResampleSurfacePair],
    },
)

@dataclasses.dataclass
class AnnotationResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def annotation_resample_params(
    annotation_in: InputPathType,
    annotation_out: str,
    surface_pair: list[AnnotationResampleSurfacePair],
) -> AnnotationResample:
    """
    Build parameters.

    Args:
        annotation_in: the annotation file to resample
        annotation_out: name of resampled annotation file
        surface_pair: pair of surfaces for resampling surface annotations for one\
            structure

    Returns:
        Parameter dictionary.
    """
    params: AnnotationResample = {
        "@type": "workbench/annotation_resample",
        "annotation_in": annotation_in,
        "annotation_out": annotation_out,
        "surface_pair": surface_pair,
    }
    return params

def annotation_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AnnotationResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("annotation_in", None) is None:
        raise StyxValidationError("`annotation_in` must not be None")
    if not isinstance(params["annotation_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`annotation_in` has the wrong type: Received `{type(params["annotation_in"])}` expected `InputPathType`')
    if params.get("annotation_out", None) is None:
        raise StyxValidationError("`annotation_out` must not be None")
    if not isinstance(params["annotation_out"], str):
        raise StyxValidationError(f'`annotation_out` has the wrong type: Received `{type(params["annotation_out"])}` expected `str`')
    if params.get("surface_pair", None) is None:
        raise StyxValidationError("`surface_pair` must not be None")
    if not isinstance(params["surface_pair"], list):
        raise StyxValidationError(f'`surface_pair` has the wrong type: Received `{type(params["surface_pair"])}` expected `list[AnnotationResampleSurfacePair]`')
    for e in params["surface_pair"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("source_surface", None) is None:
            raise StyxValidationError("`source_surface` must not be None")
        if not isinstance(e["source_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`source_surface` has the wrong type: Received `{type(e["source_surface"])}` expected `InputPathType`')
        if e.get("target_surface", None) is None:
            raise StyxValidationError("`target_surface` must not be None")
        if not isinstance(e["target_surface"], (pathlib.Path, str)):
            raise StyxValidationError(f'`target_surface` has the wrong type: Received `{type(e["target_surface"])}` expected `InputPathType`')

def annotation_resample_cargs(params: AnnotationResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-annotation-resample")
    cargs.append(execution.input_file(params["annotation_in"]))
    cargs.append(params["annotation_out"])
    for item0 in params["surface_pair"]:
        cargs.append("-surface-pair")
        cargs.append(execution.input_file(item0["source_surface"]))
        cargs.append(execution.input_file(item0["target_surface"]))
    return cargs

def annotation_resample_outputs(params: AnnotationResample, execution: Execution) -> AnnotationResampleOutputs:
    """Build the AnnotationResampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AnnotationResampleOutputs(
        root=root_v,
    )

def annotation_resample_execute(params: AnnotationResample, runner: Runner | None = None) -> AnnotationResampleOutputs:
    """
    RESAMPLE AN ANNOTATION FILE TO DIFFERENT MESHES

    Resample an annotation file from the source mesh to the target mesh.

Only annotations in surface space are modified, no changes are made to annotations in other spaces.  The -surface-pair option may be repeated for additional structures used by surface space annotations.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    annotation_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANNOTATION_RESAMPLE_METADATA)
    execution.params(params)
    args = annotation_resample_cargs(params, execution)
    out = annotation_resample_outputs(params, execution)
    execution.run(args)
    return out

def annotation_resample(
    annotation_in: InputPathType,
    annotation_out: str,
    surface_pair: list[AnnotationResampleSurfacePair],
    runner: Runner | None = None,
) -> AnnotationResampleOutputs:
    """
    RESAMPLE AN ANNOTATION FILE TO DIFFERENT MESHES

    Resample an annotation file from the source mesh to the target mesh.

Only annotations in surface space are modified, no changes are made to annotations in other spaces.  The -surface-pair option may be repeated for additional structures used by surface space annotations.

    Args:
        annotation_in: the annotation file to resample
        annotation_out: name of resampled annotation file
        surface_pair: pair of surfaces for resampling surface annotations for one\
            structure
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = annotation_resample_params(
        annotation_in=annotation_in,
        annotation_out=annotation_out,
        surface_pair=surface_pair,
    )
    return annotation_resample_execute(params, runner)

__all__ = [
    "AnnotationResample",
    "AnnotationResampleOutputs",
    "ANNOTATION_RESAMPLE_METADATA",
    "annotation_resample_cargs",
    "annotation_resample_outputs",
    "annotation_resample_params",
    "annotation_resample_execute",
    "annotation_resample_validate",
    "annotation_resample",
]