# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_SPHERE_PROJECT_UNPROJECT_METADATA = Metadata(
    id="surface_sphere_project_unproject",
    name="COPY REGISTRATION DEFORMATIONS TO DIFFERENT SPHERE",
    package="workbench",
)

SurfaceSphereProjectUnproject = typing.TypedDict(
    "SurfaceSphereProjectUnproject",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_sphere_project_unproject"]],
        "sphere_in": InputPathType,
        "sphere_project_to": InputPathType,
        "sphere_unproject_from": InputPathType,
        "sphere_out": str,
    },
)

@dataclasses.dataclass
class SurfaceSphereProjectUnprojectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sphere_out: OutputPathType
    """the output sphere"""

def surface_sphere_project_unproject_params(
    sphere_in: InputPathType,
    sphere_project_to: InputPathType,
    sphere_unproject_from: InputPathType,
    sphere_out: str,
) -> SurfaceSphereProjectUnproject:
    """
    Build parameters.

    Args:
        sphere_in: a sphere with the desired output mesh
        sphere_project_to: a sphere that aligns with sphere-in
        sphere_unproject_from: <sphere-project-to> deformed to the desired output\
            space
        sphere_out: the output sphere

    Returns:
        Parameter dictionary.
    """
    params: SurfaceSphereProjectUnproject = {
        "@type": "workbench/surface_sphere_project_unproject",
        "sphere_in": sphere_in,
        "sphere_project_to": sphere_project_to,
        "sphere_unproject_from": sphere_unproject_from,
        "sphere_out": sphere_out,
    }
    return params

def surface_sphere_project_unproject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceSphereProjectUnproject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sphere_in", None) is None:
        raise StyxValidationError("`sphere_in` must not be None")
    if not isinstance(params["sphere_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere_in` has the wrong type: Received `{type(params["sphere_in"])}` expected `InputPathType`')
    if params.get("sphere_project_to", None) is None:
        raise StyxValidationError("`sphere_project_to` must not be None")
    if not isinstance(params["sphere_project_to"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere_project_to` has the wrong type: Received `{type(params["sphere_project_to"])}` expected `InputPathType`')
    if params.get("sphere_unproject_from", None) is None:
        raise StyxValidationError("`sphere_unproject_from` must not be None")
    if not isinstance(params["sphere_unproject_from"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere_unproject_from` has the wrong type: Received `{type(params["sphere_unproject_from"])}` expected `InputPathType`')
    if params.get("sphere_out", None) is None:
        raise StyxValidationError("`sphere_out` must not be None")
    if not isinstance(params["sphere_out"], str):
        raise StyxValidationError(f'`sphere_out` has the wrong type: Received `{type(params["sphere_out"])}` expected `str`')

def surface_sphere_project_unproject_cargs(params: SurfaceSphereProjectUnproject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-sphere-project-unproject")
    cargs.append(execution.input_file(params["sphere_in"]))
    cargs.append(execution.input_file(params["sphere_project_to"]))
    cargs.append(execution.input_file(params["sphere_unproject_from"]))
    cargs.append(params["sphere_out"])
    return cargs

def surface_sphere_project_unproject_outputs(params: SurfaceSphereProjectUnproject, execution: Execution) -> SurfaceSphereProjectUnprojectOutputs:
    """Build the SurfaceSphereProjectUnprojectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    sphere_out_v: OutputPathType = execution.output_file(params["sphere_out"])
    return SurfaceSphereProjectUnprojectOutputs(
        root=root_v,
        sphere_out=sphere_out_v,
    )

def surface_sphere_project_unproject_execute(params: SurfaceSphereProjectUnproject, runner: Runner | None = None) -> SurfaceSphereProjectUnprojectOutputs:
    """
    COPY REGISTRATION DEFORMATIONS TO DIFFERENT SPHERE

    Background: A surface registration starts with an input sphere, and moves its vertices around on the sphere until it matches the template data.  This means that the registration deformation is actually represented as the difference between two separate files - the starting sphere, and the registered sphere.  Since the starting sphere of the registration may not have vertex correspondence to any other sphere (often, it is a native sphere), it can be inconvenient to manipulate or compare these deformations across subjects, etc.

The purpose of this command is to be able to apply these deformations onto a new sphere of the user's choice, to make it easier to compare or manipulate them.  Common uses are to concatenate two successive separate registrations (e.g. Human to Chimpanzee, and then Chimpanzee to Macaque) or inversion (for dedrifting or symmetric registration schemes).

<sphere-in> must already be considered to be in alignment with one of the two ends of the registration (if your registration is Human to Chimpanzee, <sphere-in> must be in register with either Human or Chimpanzee).  The 'project-to' sphere must be the side of the registration that is aligned with <sphere-in> (if your registration is Human to Chimpanzee, and <sphere-in> is aligned with Human, then 'project-to' should be the original Human sphere).  The 'unproject-from' sphere must be the remaining sphere of the registration (original vs deformed/registered).  The output is as if you had run the same registration with <sphere-in> as the starting sphere, in the direction of deforming the 'project-to' sphere to create the 'unproject-from' sphere.

Note that this command cannot check for you what spheres are aligned with other spheres, and using the wrong spheres or in the incorrect order will not necessarily cause an error message.  In some cases, it may be useful to use a new, arbitrary sphere as the input, which can be created with the -surface-create-sphere command.

Example 1: You have a Human to Chimpanzee registration, and a Chimpanzee to Macaque registration, and want to combine them.  If you use the Human sphere registered to Chimpanzee as sphere-in, the Chimpanzee standard sphere as project-to, and the Chimpanzee sphere registered to Macaque as unproject-from, the output will be the Human sphere in register with the Macaque.

Example 2: You have a Human to Chimpanzee registration, but what you really want is the inverse, that is, the sphere as if you had run the registration from Chimpanzee to Human.  If you use the Chimpanzee standard sphere as sphere-in, the Human sphere registered to Chimpanzee as project-to, and the standard Human sphere as unproject-from, the output will be the Chimpanzee sphere in register with the Human.

Technical details: Each vertex of <sphere-in> is projected to a triangle of <sphere-project-to>, and its new position is determined by the position of the corresponding triangle in <sphere-unproject-from>.  The output is a sphere with the topology of <sphere-in>, but coordinates shifted by the deformation from <sphere-project-to> to <sphere-unproject-from>.  <sphere-project-to> and <sphere-unproject-from> must have the same topology as each other, but <sphere-in> may have any topology.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_sphere_project_unproject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_SPHERE_PROJECT_UNPROJECT_METADATA)
    execution.params(params)
    args = surface_sphere_project_unproject_cargs(params, execution)
    out = surface_sphere_project_unproject_outputs(params, execution)
    execution.run(args)
    return out

def surface_sphere_project_unproject(
    sphere_in: InputPathType,
    sphere_project_to: InputPathType,
    sphere_unproject_from: InputPathType,
    sphere_out: str,
    runner: Runner | None = None,
) -> SurfaceSphereProjectUnprojectOutputs:
    """
    COPY REGISTRATION DEFORMATIONS TO DIFFERENT SPHERE

    Background: A surface registration starts with an input sphere, and moves its vertices around on the sphere until it matches the template data.  This means that the registration deformation is actually represented as the difference between two separate files - the starting sphere, and the registered sphere.  Since the starting sphere of the registration may not have vertex correspondence to any other sphere (often, it is a native sphere), it can be inconvenient to manipulate or compare these deformations across subjects, etc.

The purpose of this command is to be able to apply these deformations onto a new sphere of the user's choice, to make it easier to compare or manipulate them.  Common uses are to concatenate two successive separate registrations (e.g. Human to Chimpanzee, and then Chimpanzee to Macaque) or inversion (for dedrifting or symmetric registration schemes).

<sphere-in> must already be considered to be in alignment with one of the two ends of the registration (if your registration is Human to Chimpanzee, <sphere-in> must be in register with either Human or Chimpanzee).  The 'project-to' sphere must be the side of the registration that is aligned with <sphere-in> (if your registration is Human to Chimpanzee, and <sphere-in> is aligned with Human, then 'project-to' should be the original Human sphere).  The 'unproject-from' sphere must be the remaining sphere of the registration (original vs deformed/registered).  The output is as if you had run the same registration with <sphere-in> as the starting sphere, in the direction of deforming the 'project-to' sphere to create the 'unproject-from' sphere.

Note that this command cannot check for you what spheres are aligned with other spheres, and using the wrong spheres or in the incorrect order will not necessarily cause an error message.  In some cases, it may be useful to use a new, arbitrary sphere as the input, which can be created with the -surface-create-sphere command.

Example 1: You have a Human to Chimpanzee registration, and a Chimpanzee to Macaque registration, and want to combine them.  If you use the Human sphere registered to Chimpanzee as sphere-in, the Chimpanzee standard sphere as project-to, and the Chimpanzee sphere registered to Macaque as unproject-from, the output will be the Human sphere in register with the Macaque.

Example 2: You have a Human to Chimpanzee registration, but what you really want is the inverse, that is, the sphere as if you had run the registration from Chimpanzee to Human.  If you use the Chimpanzee standard sphere as sphere-in, the Human sphere registered to Chimpanzee as project-to, and the standard Human sphere as unproject-from, the output will be the Chimpanzee sphere in register with the Human.

Technical details: Each vertex of <sphere-in> is projected to a triangle of <sphere-project-to>, and its new position is determined by the position of the corresponding triangle in <sphere-unproject-from>.  The output is a sphere with the topology of <sphere-in>, but coordinates shifted by the deformation from <sphere-project-to> to <sphere-unproject-from>.  <sphere-project-to> and <sphere-unproject-from> must have the same topology as each other, but <sphere-in> may have any topology.

    Args:
        sphere_in: a sphere with the desired output mesh
        sphere_project_to: a sphere that aligns with sphere-in
        sphere_unproject_from: <sphere-project-to> deformed to the desired output\
            space
        sphere_out: the output sphere
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_sphere_project_unproject_params(
        sphere_in=sphere_in,
        sphere_project_to=sphere_project_to,
        sphere_unproject_from=sphere_unproject_from,
        sphere_out=sphere_out,
    )
    return surface_sphere_project_unproject_execute(params, runner)

__all__ = [
    "SurfaceSphereProjectUnproject",
    "SurfaceSphereProjectUnprojectOutputs",
    "SURFACE_SPHERE_PROJECT_UNPROJECT_METADATA",
    "surface_sphere_project_unproject_cargs",
    "surface_sphere_project_unproject_outputs",
    "surface_sphere_project_unproject_params",
    "surface_sphere_project_unproject_execute",
    "surface_sphere_project_unproject_validate",
    "surface_sphere_project_unproject",
]