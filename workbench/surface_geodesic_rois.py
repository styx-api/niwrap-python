# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_GEODESIC_ROIS_METADATA = Metadata(
    id="surface_geodesic_rois",
    name="DRAW GEODESIC LIMITED ROIS AT VERTICES",
    package="workbench",
)

SurfaceGeodesicRois = typing.TypedDict(
    "SurfaceGeodesicRois",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_geodesic_rois"]],
        "surface": InputPathType,
        "limit": float,
        "vertex_list_file": str,
        "metric_out": str,
        "gaussian": typing.NotRequired[float],
        "overlap_logic": typing.NotRequired[str],
        "names": typing.NotRequired[str],
        "corrected_areas": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SurfaceGeodesicRoisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def surface_geodesic_rois_params(
    surface: InputPathType,
    limit: float,
    vertex_list_file: str,
    metric_out: str,
    gaussian: float | None = None,
    overlap_logic: str | None = None,
    names: str | None = None,
    corrected_areas: InputPathType | None = None,
) -> SurfaceGeodesicRois:
    """
    Build parameters.

    Args:
        surface: the surface to draw on
        limit: geodesic distance limit from vertex, in mm
        vertex_list_file: a text file containing the vertices to draw ROIs around
        metric_out: the output metric
        gaussian: generate a gaussian kernel instead of a flat ROI
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        names: name the columns from text file
        corrected_areas: vertex areas to use instead of computing them from the\
            surface

    Returns:
        Parameter dictionary.
    """
    params: SurfaceGeodesicRois = {
        "@type": "workbench/surface_geodesic_rois",
        "surface": surface,
        "limit": limit,
        "vertex_list_file": vertex_list_file,
        "metric_out": metric_out,
    }
    if gaussian is not None:
        params["gaussian"] = gaussian
    if overlap_logic is not None:
        params["overlap_logic"] = overlap_logic
    if names is not None:
        params["names"] = names
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def surface_geodesic_rois_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceGeodesicRois."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("limit", None) is None:
        raise StyxValidationError("`limit` must not be None")
    if not isinstance(params["limit"], (float, int)):
        raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("vertex_list_file", None) is None:
        raise StyxValidationError("`vertex_list_file` must not be None")
    if not isinstance(params["vertex_list_file"], str):
        raise StyxValidationError(f'`vertex_list_file` has the wrong type: Received `{type(params["vertex_list_file"])}` expected `str`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("gaussian", None) is not None:
        if not isinstance(params["gaussian"], (float, int)):
            raise StyxValidationError(f'`gaussian` has the wrong type: Received `{type(params["gaussian"])}` expected `float`')
    if params.get("overlap_logic", None) is not None:
        if not isinstance(params["overlap_logic"], str):
            raise StyxValidationError(f'`overlap_logic` has the wrong type: Received `{type(params["overlap_logic"])}` expected `str`')
    if params.get("names", None) is not None:
        if not isinstance(params["names"], str):
            raise StyxValidationError(f'`names` has the wrong type: Received `{type(params["names"])}` expected `str`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')

def surface_geodesic_rois_cargs(params: SurfaceGeodesicRois, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-geodesic-rois")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(str(params["limit"]))
    cargs.append(params["vertex_list_file"])
    cargs.append(params["metric_out"])
    v_0 = params.get("gaussian")
    if v_0 is not None:
        cargs.append("-gaussian")
        cargs.append(str(v_0))
    v_1 = params.get("overlap_logic")
    if v_1 is not None:
        cargs.append("-overlap-logic")
        cargs.append(v_1)
    v_2 = params.get("names")
    if v_2 is not None:
        cargs.append("-names")
        cargs.append(v_2)
    v_3 = params.get("corrected_areas")
    if v_3 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_3))
    return cargs

def surface_geodesic_rois_outputs(params: SurfaceGeodesicRois, execution: Execution) -> SurfaceGeodesicRoisOutputs:
    """Build the SurfaceGeodesicRoisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return SurfaceGeodesicRoisOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def surface_geodesic_rois_execute(params: SurfaceGeodesicRois, runner: Runner | None = None) -> SurfaceGeodesicRoisOutputs:
    """
    DRAW GEODESIC LIMITED ROIS AT VERTICES

    For each vertex in the list file, a column in the output metric is created, and an ROI around that vertex is drawn in that column.  Each metric column will have zeros outside the geodesic distance spacified by <limit>, and by default will have a value of 1.0 inside it.  If the -gaussian option is specified, the values inside the ROI will instead form a gaussian with the specified value of sigma, normalized so that the sum of the nonzero values in the metric column is 1.0.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_geodesic_rois_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_GEODESIC_ROIS_METADATA)
    execution.params(params)
    args = surface_geodesic_rois_cargs(params, execution)
    out = surface_geodesic_rois_outputs(params, execution)
    execution.run(args)
    return out

def surface_geodesic_rois(
    surface: InputPathType,
    limit: float,
    vertex_list_file: str,
    metric_out: str,
    gaussian: float | None = None,
    overlap_logic: str | None = None,
    names: str | None = None,
    corrected_areas: InputPathType | None = None,
    runner: Runner | None = None,
) -> SurfaceGeodesicRoisOutputs:
    """
    DRAW GEODESIC LIMITED ROIS AT VERTICES

    For each vertex in the list file, a column in the output metric is created, and an ROI around that vertex is drawn in that column.  Each metric column will have zeros outside the geodesic distance spacified by <limit>, and by default will have a value of 1.0 inside it.  If the -gaussian option is specified, the values inside the ROI will instead form a gaussian with the specified value of sigma, normalized so that the sum of the nonzero values in the metric column is 1.0.  The <method> argument to -overlap-logic must be one of ALLOW, CLOSEST, or EXCLUDE.  ALLOW is the default, and means that ROIs are treated independently and may overlap.  CLOSEST means that ROIs may not overlap, and that no ROI contains vertices that are closer to a different seed vertex.  EXCLUDE means that ROIs may not overlap, and that any vertex within range of more than one ROI does not belong to any ROI.

    Args:
        surface: the surface to draw on
        limit: geodesic distance limit from vertex, in mm
        vertex_list_file: a text file containing the vertices to draw ROIs around
        metric_out: the output metric
        gaussian: generate a gaussian kernel instead of a flat ROI
        overlap_logic: how to handle overlapping ROIs, default ALLOW
        names: name the columns from text file
        corrected_areas: vertex areas to use instead of computing them from the\
            surface
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_geodesic_rois_params(
        surface=surface,
        limit=limit,
        vertex_list_file=vertex_list_file,
        metric_out=metric_out,
        gaussian=gaussian,
        overlap_logic=overlap_logic,
        names=names,
        corrected_areas=corrected_areas,
    )
    return surface_geodesic_rois_execute(params, runner)

__all__ = [
    "SurfaceGeodesicRois",
    "SurfaceGeodesicRoisOutputs",
    "SURFACE_GEODESIC_ROIS_METADATA",
    "surface_geodesic_rois_cargs",
    "surface_geodesic_rois_outputs",
    "surface_geodesic_rois_params",
    "surface_geodesic_rois_execute",
    "surface_geodesic_rois_validate",
    "surface_geodesic_rois",
]