# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_AFFINE_REGRESSION_METADATA = Metadata(
    id="surface_affine_regression",
    name="REGRESS THE AFFINE TRANSFORM BETWEEN SURFACES ON THE SAME MESH",
    package="workbench",
)

SurfaceAffineRegression = typing.TypedDict(
    "SurfaceAffineRegression",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_affine_regression"]],
        "source": InputPathType,
        "target": InputPathType,
        "affine_out": str,
    },
)

@dataclasses.dataclass
class SurfaceAffineRegressionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_affine_regression_params(
    source: InputPathType,
    target: InputPathType,
    affine_out: str,
) -> SurfaceAffineRegression:
    """
    Build parameters.

    Args:
        source: the surface to warp
        target: the surface to match the coordinates of
        affine_out: output - the output affine file

    Returns:
        Parameter dictionary.
    """
    params: SurfaceAffineRegression = {
        "@type": "workbench/surface_affine_regression",
        "source": source,
        "target": target,
        "affine_out": affine_out,
    }
    return params

def surface_affine_regression_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceAffineRegression."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("affine_out", None) is None:
        raise StyxValidationError("`affine_out` must not be None")
    if not isinstance(params["affine_out"], str):
        raise StyxValidationError(f'`affine_out` has the wrong type: Received `{type(params["affine_out"])}` expected `str`')

def surface_affine_regression_cargs(params: SurfaceAffineRegression, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-affine-regression")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["target"]))
    cargs.append(params["affine_out"])
    return cargs

def surface_affine_regression_outputs(params: SurfaceAffineRegression, execution: Execution) -> SurfaceAffineRegressionOutputs:
    """Build the SurfaceAffineRegressionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceAffineRegressionOutputs(
        root=root_v,
    )

def surface_affine_regression_execute(params: SurfaceAffineRegression, runner: Runner | None = None) -> SurfaceAffineRegressionOutputs:
    """
    REGRESS THE AFFINE TRANSFORM BETWEEN SURFACES ON THE SAME MESH

    Use linear regression to compute an affine that minimizes the sum of squares of the coordinate differences between the target surface and the warped source surface.  Note that this has a bias to shrink the surface that is being warped.  The output is written as a NIFTI 'world' matrix, see -convert-affine to convert it for use in other software.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_affine_regression_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_AFFINE_REGRESSION_METADATA)
    execution.params(params)
    args = surface_affine_regression_cargs(params, execution)
    out = surface_affine_regression_outputs(params, execution)
    execution.run(args)
    return out

def surface_affine_regression(
    source: InputPathType,
    target: InputPathType,
    affine_out: str,
    runner: Runner | None = None,
) -> SurfaceAffineRegressionOutputs:
    """
    REGRESS THE AFFINE TRANSFORM BETWEEN SURFACES ON THE SAME MESH

    Use linear regression to compute an affine that minimizes the sum of squares of the coordinate differences between the target surface and the warped source surface.  Note that this has a bias to shrink the surface that is being warped.  The output is written as a NIFTI 'world' matrix, see -convert-affine to convert it for use in other software.

    Args:
        source: the surface to warp
        target: the surface to match the coordinates of
        affine_out: output - the output affine file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_affine_regression_params(
        source=source,
        target=target,
        affine_out=affine_out,
    )
    return surface_affine_regression_execute(params, runner)

__all__ = [
    "SurfaceAffineRegression",
    "SurfaceAffineRegressionOutputs",
    "SURFACE_AFFINE_REGRESSION_METADATA",
    "surface_affine_regression_cargs",
    "surface_affine_regression_outputs",
    "surface_affine_regression_params",
    "surface_affine_regression_execute",
    "surface_affine_regression_validate",
    "surface_affine_regression",
]