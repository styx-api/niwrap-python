# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_EXPORT_TABLE_METADATA = Metadata(
    id="label_export_table",
    name="EXPORT LABEL TABLE FROM GIFTI AS TEXT",
    package="workbench",
)

LabelExportTable = typing.TypedDict(
    "LabelExportTable",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_export_table"]],
        "label_in": InputPathType,
        "table_out": str,
        "hierarchy": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LabelExportTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def label_export_table_params(
    label_in: InputPathType,
    table_out: str,
    hierarchy: str | None = None,
) -> LabelExportTable:
    """
    Build parameters.

    Args:
        label_in: the input label file
        table_out: output - the output text file
        hierarchy: export the hierarchy as json

    Returns:
        Parameter dictionary.
    """
    params: LabelExportTable = {
        "@type": "workbench/label_export_table",
        "label_in": label_in,
        "table_out": table_out,
    }
    if hierarchy is not None:
        params["hierarchy"] = hierarchy
    return params

def label_export_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelExportTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("table_out", None) is None:
        raise StyxValidationError("`table_out` must not be None")
    if not isinstance(params["table_out"], str):
        raise StyxValidationError(f'`table_out` has the wrong type: Received `{type(params["table_out"])}` expected `str`')
    if params.get("hierarchy", None) is not None:
        if not isinstance(params["hierarchy"], str):
            raise StyxValidationError(f'`hierarchy` has the wrong type: Received `{type(params["hierarchy"])}` expected `str`')

def label_export_table_cargs(params: LabelExportTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-export-table")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["table_out"])
    v_0 = params.get("hierarchy")
    if v_0 is not None:
        cargs.append("-hierarchy")
        cargs.append(v_0)
    return cargs

def label_export_table_outputs(params: LabelExportTable, execution: Execution) -> LabelExportTableOutputs:
    """Build the LabelExportTableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return LabelExportTableOutputs(
        root=root_v,
    )

def label_export_table_execute(params: LabelExportTable, runner: Runner | None = None) -> LabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM GIFTI AS TEXT

    Takes the label table from the gifti label file, and writes it to a text format matching what is expected by -metric-label-import.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_export_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_EXPORT_TABLE_METADATA)
    execution.params(params)
    args = label_export_table_cargs(params, execution)
    out = label_export_table_outputs(params, execution)
    execution.run(args)
    return out

def label_export_table(
    label_in: InputPathType,
    table_out: str,
    hierarchy: str | None = None,
    runner: Runner | None = None,
) -> LabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM GIFTI AS TEXT

    Takes the label table from the gifti label file, and writes it to a text format matching what is expected by -metric-label-import.

    Args:
        label_in: the input label file
        table_out: output - the output text file
        hierarchy: export the hierarchy as json
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_export_table_params(
        label_in=label_in,
        table_out=table_out,
        hierarchy=hierarchy,
    )
    return label_export_table_execute(params, runner)

__all__ = [
    "LabelExportTable",
    "LabelExportTableOutputs",
    "LABEL_EXPORT_TABLE_METADATA",
    "label_export_table_cargs",
    "label_export_table_outputs",
    "label_export_table_params",
    "label_export_table_execute",
    "label_export_table_validate",
    "label_export_table",
]