# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GIFTI_LABEL_ADD_PREFIX_METADATA = Metadata(
    id="gifti_label_add_prefix",
    name="ADD PREFIX TO ALL LABEL NAMES IN A GIFTI LABEL FILE",
    package="workbench",
)

GiftiLabelAddPrefix = typing.TypedDict(
    "GiftiLabelAddPrefix",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/gifti_label_add_prefix"]],
        "label_in": InputPathType,
        "prefix": str,
        "label_out": str,
    },
)

@dataclasses.dataclass
class GiftiLabelAddPrefixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label file"""

def gifti_label_add_prefix_params(
    label_in: InputPathType,
    prefix: str,
    label_out: str,
) -> GiftiLabelAddPrefix:
    """
    Build parameters.

    Args:
        label_in: the input label file
        prefix: the prefix string to add
        label_out: the output label file

    Returns:
        Parameter dictionary.
    """
    params: GiftiLabelAddPrefix = {
        "@type": "workbench/gifti_label_add_prefix",
        "label_in": label_in,
        "prefix": prefix,
        "label_out": label_out,
    }
    return params

def gifti_label_add_prefix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GiftiLabelAddPrefix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')

def gifti_label_add_prefix_cargs(params: GiftiLabelAddPrefix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-gifti-label-add-prefix")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["prefix"])
    cargs.append(params["label_out"])
    return cargs

def gifti_label_add_prefix_outputs(params: GiftiLabelAddPrefix, execution: Execution) -> GiftiLabelAddPrefixOutputs:
    """Build the GiftiLabelAddPrefixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return GiftiLabelAddPrefixOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def gifti_label_add_prefix_execute(params: GiftiLabelAddPrefix, runner: Runner | None = None) -> GiftiLabelAddPrefixOutputs:
    """
    ADD PREFIX TO ALL LABEL NAMES IN A GIFTI LABEL FILE

    For each label other than '???', prepend <prefix> to the label name.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gifti_label_add_prefix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GIFTI_LABEL_ADD_PREFIX_METADATA)
    execution.params(params)
    args = gifti_label_add_prefix_cargs(params, execution)
    out = gifti_label_add_prefix_outputs(params, execution)
    execution.run(args)
    return out

def gifti_label_add_prefix(
    label_in: InputPathType,
    prefix: str,
    label_out: str,
    runner: Runner | None = None,
) -> GiftiLabelAddPrefixOutputs:
    """
    ADD PREFIX TO ALL LABEL NAMES IN A GIFTI LABEL FILE

    For each label other than '???', prepend <prefix> to the label name.

    Args:
        label_in: the input label file
        prefix: the prefix string to add
        label_out: the output label file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gifti_label_add_prefix_params(
        label_in=label_in,
        prefix=prefix,
        label_out=label_out,
    )
    return gifti_label_add_prefix_execute(params, runner)

__all__ = [
    "GiftiLabelAddPrefix",
    "GiftiLabelAddPrefixOutputs",
    "GIFTI_LABEL_ADD_PREFIX_METADATA",
    "gifti_label_add_prefix_cargs",
    "gifti_label_add_prefix_outputs",
    "gifti_label_add_prefix_params",
    "gifti_label_add_prefix_execute",
    "gifti_label_add_prefix_validate",
    "gifti_label_add_prefix",
]