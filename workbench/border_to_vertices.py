# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_TO_VERTICES_METADATA = Metadata(
    id="border_to_vertices",
    name="DRAW BORDERS AS VERTICES IN A METRIC FILE",
    package="workbench",
)

BorderToVertices = typing.TypedDict(
    "BorderToVertices",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_to_vertices"]],
        "surface": InputPathType,
        "border_file": InputPathType,
        "metric_out": str,
        "border": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class BorderToVerticesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric file"""

def border_to_vertices_params(
    surface: InputPathType,
    border_file: InputPathType,
    metric_out: str,
    border: str | None = None,
) -> BorderToVertices:
    """
    Build parameters.

    Args:
        surface: the surface the borders are drawn on
        border_file: the border file
        metric_out: the output metric file
        border: create ROI for only one border

    Returns:
        Parameter dictionary.
    """
    params: BorderToVertices = {
        "@type": "workbench/border_to_vertices",
        "surface": surface,
        "border_file": border_file,
        "metric_out": metric_out,
    }
    if border is not None:
        params["border"] = border
    return params

def border_to_vertices_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderToVertices."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("border_file", None) is None:
        raise StyxValidationError("`border_file` must not be None")
    if not isinstance(params["border_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`border_file` has the wrong type: Received `{type(params["border_file"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("border", None) is not None:
        if not isinstance(params["border"], str):
            raise StyxValidationError(f'`border` has the wrong type: Received `{type(params["border"])}` expected `str`')

def border_to_vertices_cargs(params: BorderToVertices, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-to-vertices")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["border_file"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("border")
    if v_0 is not None:
        cargs.append("-border")
        cargs.append(v_0)
    return cargs

def border_to_vertices_outputs(params: BorderToVertices, execution: Execution) -> BorderToVerticesOutputs:
    """Build the BorderToVerticesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return BorderToVerticesOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def border_to_vertices_execute(params: BorderToVertices, runner: Runner | None = None) -> BorderToVerticesOutputs:
    """
    DRAW BORDERS AS VERTICES IN A METRIC FILE

    Outputs a metric with 1s on vertices that follow a border, and 0s elsewhere.  By default, a separate metric column is created for each border.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_to_vertices_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_TO_VERTICES_METADATA)
    execution.params(params)
    args = border_to_vertices_cargs(params, execution)
    out = border_to_vertices_outputs(params, execution)
    execution.run(args)
    return out

def border_to_vertices(
    surface: InputPathType,
    border_file: InputPathType,
    metric_out: str,
    border: str | None = None,
    runner: Runner | None = None,
) -> BorderToVerticesOutputs:
    """
    DRAW BORDERS AS VERTICES IN A METRIC FILE

    Outputs a metric with 1s on vertices that follow a border, and 0s elsewhere.  By default, a separate metric column is created for each border.

    Args:
        surface: the surface the borders are drawn on
        border_file: the border file
        metric_out: the output metric file
        border: create ROI for only one border
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_to_vertices_params(
        surface=surface,
        border_file=border_file,
        metric_out=metric_out,
        border=border,
    )
    return border_to_vertices_execute(params, runner)

__all__ = [
    "BorderToVertices",
    "BorderToVerticesOutputs",
    "BORDER_TO_VERTICES_METADATA",
    "border_to_vertices_cargs",
    "border_to_vertices_outputs",
    "border_to_vertices_params",
    "border_to_vertices_execute",
    "border_to_vertices_validate",
    "border_to_vertices",
]