# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_MERGE_METADATA = Metadata(
    id="metric_merge",
    name="MERGE METRIC FILES INTO A NEW FILE",
    package="workbench",
)

class MetricMergeUpTo(typing.TypedDict):
    last_column: str
    """the number or name of the last column to include"""
    reverse: typing.NotRequired[bool]
    """use the range in reverse order"""

class MetricMergeColumn(typing.TypedDict):
    column: str
    """the column number or name"""
    up_to: typing.NotRequired[MetricMergeUpTo]
    """use an inclusive range of columns"""

class MetricMergeMetric(typing.TypedDict):
    metric_in: InputPathType
    """a metric file to use columns from"""
    column: list[MetricMergeColumn]
    """select a single column to use"""

MetricMerge = typing.TypedDict(
    "MetricMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_merge"]],
        "metric_out": str,
        "metric": list[MetricMergeMetric],
    },
)

@dataclasses.dataclass
class MetricMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_merge_params(
    metric_out: str,
    metric: list[MetricMergeMetric],
) -> MetricMerge:
    """
    Build parameters.

    Args:
        metric_out: the output metric
        metric: specify an input metric

    Returns:
        Parameter dictionary.
    """
    params: MetricMerge = {
        "@type": "workbench/metric_merge",
        "metric_out": metric_out,
        "metric": metric,
    }
    return params

def metric_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], list):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `list[MetricMergeMetric]`')
    for e in params["metric"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("metric_in", None) is None:
            raise StyxValidationError("`metric_in` must not be None")
        if not isinstance(e["metric_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(e["metric_in"])}` expected `InputPathType`')
        if e.get("column", None) is None:
            raise StyxValidationError("`column` must not be None")
        if not isinstance(e["column"], list):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(e["column"])}` expected `list[MetricMergeColumn]`')
        for e_2 in e["column"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("column", None) is None:
                raise StyxValidationError("`column` must not be None")
            if not isinstance(e_2["column"], str):
                raise StyxValidationError(f'`column` has the wrong type: Received `{type(e_2["column"])}` expected `str`')
            if e_2.get("up_to", None) is not None:
                if not isinstance(e_2["up_to"], dict):
                    raise StyxValidationError(f'Params object has the wrong type \'{type(e_2["up_to"])}\'')
                if e_2["up_to"].get("last_column", None) is None:
                    raise StyxValidationError("`last_column` must not be None")
                if not isinstance(e_2["up_to"]["last_column"], str):
                    raise StyxValidationError(f'`last_column` has the wrong type: Received `{type(e_2["up_to"]["last_column"])}` expected `str`')
                if e_2["up_to"].get("reverse", None) is not None:
                    if not isinstance(e_2["up_to"]["reverse"], bool):
                        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(e_2["up_to"]["reverse"])}` expected `bool`')

def metric_merge_cargs(params: MetricMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-merge")
    cargs.append(params["metric_out"])
    for item0 in params["metric"]:
        cargs.append("-metric")
        cargs.append(execution.input_file(item0["metric_in"]))
        for item1 in item0["column"]:
            cargs.append("-column")
            cargs.append(item1["column"])
            v_0 = item1.get("up_to")
            if v_0 is not None:
                cargs.append("-up-to")
                cargs.append(v_0["last_column"])
                if v_0.get("reverse"):
                    cargs.append("-reverse")
    return cargs

def metric_merge_outputs(params: MetricMerge, execution: Execution) -> MetricMergeOutputs:
    """Build the MetricMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricMergeOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_merge_execute(params: MetricMerge, runner: Runner | None = None) -> MetricMergeOutputs:
    """
    MERGE METRIC FILES INTO A NEW FILE

    Takes one or more metric files and constructs a new metric file by concatenating columns from them.  The input metric files must have the same number of vertices and same structure.

Example: wb_command -metric-merge out.func.gii -metric first.func.gii -column 1 -metric second.func.gii

This example would take the first column from first.func.gii, followed by all columns from second.func.gii, and write these columns to out.func.gii.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_MERGE_METADATA)
    execution.params(params)
    args = metric_merge_cargs(params, execution)
    out = metric_merge_outputs(params, execution)
    execution.run(args)
    return out

def metric_merge(
    metric_out: str,
    metric: list[MetricMergeMetric],
    runner: Runner | None = None,
) -> MetricMergeOutputs:
    """
    MERGE METRIC FILES INTO A NEW FILE

    Takes one or more metric files and constructs a new metric file by concatenating columns from them.  The input metric files must have the same number of vertices and same structure.

Example: wb_command -metric-merge out.func.gii -metric first.func.gii -column 1 -metric second.func.gii

This example would take the first column from first.func.gii, followed by all columns from second.func.gii, and write these columns to out.func.gii.

    Args:
        metric_out: the output metric
        metric: specify an input metric
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_merge_params(
        metric_out=metric_out,
        metric=metric,
    )
    return metric_merge_execute(params, runner)

__all__ = [
    "MetricMerge",
    "MetricMergeOutputs",
    "METRIC_MERGE_METADATA",
    "metric_merge_cargs",
    "metric_merge_outputs",
    "metric_merge_params",
    "metric_merge_execute",
    "metric_merge_validate",
    "metric_merge",
]