# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_EXPORT_TABLE_METADATA = Metadata(
    id="cifti_label_export_table",
    name="EXPORT LABEL TABLE FROM CIFTI AS TEXT",
    package="workbench",
)

CiftiLabelExportTable = typing.TypedDict(
    "CiftiLabelExportTable",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_export_table"]],
        "label_in": InputPathType,
        "map": str,
        "table_out": str,
        "hierarchy": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CiftiLabelExportTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cifti_label_export_table_params(
    label_in: InputPathType,
    map: str,
    table_out: str,
    hierarchy: str | None = None,
) -> CiftiLabelExportTable:
    """
    Build parameters.

    Args:
        label_in: the input cifti label file
        map: the number or name of the label map to use
        table_out: output - the output text file
        hierarchy: export the hierarchy as json

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelExportTable = {
        "@type": "workbench/cifti_label_export_table",
        "label_in": label_in,
        "map": map,
        "table_out": table_out,
    }
    if hierarchy is not None:
        params["hierarchy"] = hierarchy
    return params

def cifti_label_export_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelExportTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("table_out", None) is None:
        raise StyxValidationError("`table_out` must not be None")
    if not isinstance(params["table_out"], str):
        raise StyxValidationError(f'`table_out` has the wrong type: Received `{type(params["table_out"])}` expected `str`')
    if params.get("hierarchy", None) is not None:
        if not isinstance(params["hierarchy"], str):
            raise StyxValidationError(f'`hierarchy` has the wrong type: Received `{type(params["hierarchy"])}` expected `str`')

def cifti_label_export_table_cargs(params: CiftiLabelExportTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-export-table")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["map"])
    cargs.append(params["table_out"])
    v_0 = params.get("hierarchy")
    if v_0 is not None:
        cargs.append("-hierarchy")
        cargs.append(v_0)
    return cargs

def cifti_label_export_table_outputs(params: CiftiLabelExportTable, execution: Execution) -> CiftiLabelExportTableOutputs:
    """Build the CiftiLabelExportTableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CiftiLabelExportTableOutputs(
        root=root_v,
    )

def cifti_label_export_table_execute(params: CiftiLabelExportTable, runner: Runner | None = None) -> CiftiLabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM CIFTI AS TEXT

    Takes the label table from the cifti label map, and writes it to a text format matching what is expected by -cifti-label-import.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_export_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_EXPORT_TABLE_METADATA)
    execution.params(params)
    args = cifti_label_export_table_cargs(params, execution)
    out = cifti_label_export_table_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_export_table(
    label_in: InputPathType,
    map: str,
    table_out: str,
    hierarchy: str | None = None,
    runner: Runner | None = None,
) -> CiftiLabelExportTableOutputs:
    """
    EXPORT LABEL TABLE FROM CIFTI AS TEXT

    Takes the label table from the cifti label map, and writes it to a text format matching what is expected by -cifti-label-import.

    Args:
        label_in: the input cifti label file
        map: the number or name of the label map to use
        table_out: output - the output text file
        hierarchy: export the hierarchy as json
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_export_table_params(
        label_in=label_in,
        map=map,
        table_out=table_out,
        hierarchy=hierarchy,
    )
    return cifti_label_export_table_execute(params, runner)

__all__ = [
    "CiftiLabelExportTable",
    "CiftiLabelExportTableOutputs",
    "CIFTI_LABEL_EXPORT_TABLE_METADATA",
    "cifti_label_export_table_cargs",
    "cifti_label_export_table_outputs",
    "cifti_label_export_table_params",
    "cifti_label_export_table_execute",
    "cifti_label_export_table_validate",
    "cifti_label_export_table",
]