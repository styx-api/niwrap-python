# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_GENERATE_INFLATED_METADATA = Metadata(
    id="surface_generate_inflated",
    name="SURFACE GENERATE INFLATED",
    package="workbench",
)

SurfaceGenerateInflated = typing.TypedDict(
    "SurfaceGenerateInflated",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_generate_inflated"]],
        "anatomical_surface_in": InputPathType,
        "inflated_surface_out": str,
        "very_inflated_surface_out": str,
        "iterations_scale": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SurfaceGenerateInflatedOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    inflated_surface_out: OutputPathType
    """the output inflated surface"""
    very_inflated_surface_out: OutputPathType
    """the output very inflated surface"""

def surface_generate_inflated_params(
    anatomical_surface_in: InputPathType,
    inflated_surface_out: str,
    very_inflated_surface_out: str,
    iterations_scale: float | None = None,
) -> SurfaceGenerateInflated:
    """
    Build parameters.

    Args:
        anatomical_surface_in: the anatomical surface
        inflated_surface_out: the output inflated surface
        very_inflated_surface_out: the output very inflated surface
        iterations_scale: optional iterations scaling

    Returns:
        Parameter dictionary.
    """
    params: SurfaceGenerateInflated = {
        "@type": "workbench/surface_generate_inflated",
        "anatomical_surface_in": anatomical_surface_in,
        "inflated_surface_out": inflated_surface_out,
        "very_inflated_surface_out": very_inflated_surface_out,
    }
    if iterations_scale is not None:
        params["iterations_scale"] = iterations_scale
    return params

def surface_generate_inflated_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceGenerateInflated."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("anatomical_surface_in", None) is None:
        raise StyxValidationError("`anatomical_surface_in` must not be None")
    if not isinstance(params["anatomical_surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`anatomical_surface_in` has the wrong type: Received `{type(params["anatomical_surface_in"])}` expected `InputPathType`')
    if params.get("inflated_surface_out", None) is None:
        raise StyxValidationError("`inflated_surface_out` must not be None")
    if not isinstance(params["inflated_surface_out"], str):
        raise StyxValidationError(f'`inflated_surface_out` has the wrong type: Received `{type(params["inflated_surface_out"])}` expected `str`')
    if params.get("very_inflated_surface_out", None) is None:
        raise StyxValidationError("`very_inflated_surface_out` must not be None")
    if not isinstance(params["very_inflated_surface_out"], str):
        raise StyxValidationError(f'`very_inflated_surface_out` has the wrong type: Received `{type(params["very_inflated_surface_out"])}` expected `str`')
    if params.get("iterations_scale", None) is not None:
        if not isinstance(params["iterations_scale"], (float, int)):
            raise StyxValidationError(f'`iterations_scale` has the wrong type: Received `{type(params["iterations_scale"])}` expected `float`')

def surface_generate_inflated_cargs(params: SurfaceGenerateInflated, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-generate-inflated")
    cargs.append(execution.input_file(params["anatomical_surface_in"]))
    cargs.append(params["inflated_surface_out"])
    cargs.append(params["very_inflated_surface_out"])
    v_0 = params.get("iterations_scale")
    if v_0 is not None:
        cargs.append("-iterations-scale")
        cargs.append(str(v_0))
    return cargs

def surface_generate_inflated_outputs(params: SurfaceGenerateInflated, execution: Execution) -> SurfaceGenerateInflatedOutputs:
    """Build the SurfaceGenerateInflatedOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    inflated_surface_out_v: OutputPathType = execution.output_file(params["inflated_surface_out"])
    very_inflated_surface_out_v: OutputPathType = execution.output_file(params["very_inflated_surface_out"])
    return SurfaceGenerateInflatedOutputs(
        root=root_v,
        inflated_surface_out=inflated_surface_out_v,
        very_inflated_surface_out=very_inflated_surface_out_v,
    )

def surface_generate_inflated_execute(params: SurfaceGenerateInflated, runner: Runner | None = None) -> SurfaceGenerateInflatedOutputs:
    """
    SURFACE GENERATE INFLATED

    Generate inflated and very inflated surfaces. The output surfaces are 'matched' (have same XYZ range) to the anatomical surface. In most cases, an iterations-scale of 1.0 (default) is sufficient.  However, if the surface contains a large number of vertices (150,000), try an iterations-scale of 2.5.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_generate_inflated_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_GENERATE_INFLATED_METADATA)
    execution.params(params)
    args = surface_generate_inflated_cargs(params, execution)
    out = surface_generate_inflated_outputs(params, execution)
    execution.run(args)
    return out

def surface_generate_inflated(
    anatomical_surface_in: InputPathType,
    inflated_surface_out: str,
    very_inflated_surface_out: str,
    iterations_scale: float | None = None,
    runner: Runner | None = None,
) -> SurfaceGenerateInflatedOutputs:
    """
    SURFACE GENERATE INFLATED

    Generate inflated and very inflated surfaces. The output surfaces are 'matched' (have same XYZ range) to the anatomical surface. In most cases, an iterations-scale of 1.0 (default) is sufficient.  However, if the surface contains a large number of vertices (150,000), try an iterations-scale of 2.5.

    Args:
        anatomical_surface_in: the anatomical surface
        inflated_surface_out: the output inflated surface
        very_inflated_surface_out: the output very inflated surface
        iterations_scale: optional iterations scaling
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_generate_inflated_params(
        anatomical_surface_in=anatomical_surface_in,
        inflated_surface_out=inflated_surface_out,
        very_inflated_surface_out=very_inflated_surface_out,
        iterations_scale=iterations_scale,
    )
    return surface_generate_inflated_execute(params, runner)

__all__ = [
    "SurfaceGenerateInflated",
    "SurfaceGenerateInflatedOutputs",
    "SURFACE_GENERATE_INFLATED_METADATA",
    "surface_generate_inflated_cargs",
    "surface_generate_inflated_outputs",
    "surface_generate_inflated_params",
    "surface_generate_inflated_execute",
    "surface_generate_inflated_validate",
    "surface_generate_inflated",
]