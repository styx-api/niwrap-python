# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_REPLACE_STRUCTURE_METADATA = Metadata(
    id="cifti_replace_structure",
    name="REPLACE DATA IN A STRUCTURE IN A CIFTI FILE",
    package="workbench",
)

class CiftiReplaceStructureVolumeAll(typing.TypedDict):
    volume: InputPathType
    """the input volume"""
    from_cropped: typing.NotRequired[bool]
    """the input is cropped to the size of the data"""

class CiftiReplaceStructureLabel(typing.TypedDict):
    structure: str
    """the structure to replace the data of"""
    label: InputPathType
    """the input label file"""

class CiftiReplaceStructureMetric(typing.TypedDict):
    structure: str
    """the structure to replace the data of"""
    metric: InputPathType
    """the input metric"""

class CiftiReplaceStructureVolume(typing.TypedDict):
    structure: str
    """the structure to replace the data of"""
    volume: InputPathType
    """the input volume"""
    from_cropped: typing.NotRequired[bool]
    """the input is cropped to the size of the component"""

CiftiReplaceStructure = typing.TypedDict(
    "CiftiReplaceStructure",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_replace_structure"]],
        "cifti": str,
        "direction": str,
        "volume_all": typing.NotRequired[CiftiReplaceStructureVolumeAll],
        "discard_unused_labels": typing.NotRequired[bool],
        "label_collision": typing.NotRequired[str],
        "label": list[CiftiReplaceStructureLabel],
        "metric": list[CiftiReplaceStructureMetric],
        "volume": list[CiftiReplaceStructureVolume],
    },
)

@dataclasses.dataclass
class CiftiReplaceStructureOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cifti_replace_structure_params(
    cifti: str,
    direction: str,
    label: list[CiftiReplaceStructureLabel],
    metric: list[CiftiReplaceStructureMetric],
    volume: list[CiftiReplaceStructureVolume],
    volume_all: CiftiReplaceStructureVolumeAll | None = None,
    discard_unused_labels: bool = False,
    label_collision: str | None = None,
) -> CiftiReplaceStructure:
    """
    Build parameters.

    Args:
        cifti: the cifti to modify
        direction: which dimension to interpret as a single map, ROW or COLUMN
        label: replace the data in a surface label component
        metric: replace the data in a surface component
        volume: replace the data in a volume component
        volume_all: replace the data in all volume components
        discard_unused_labels: when operating on a dlabel file, drop any unused\
            label keys from the label table
        label_collision: how to handle conflicts between label keys

    Returns:
        Parameter dictionary.
    """
    params: CiftiReplaceStructure = {
        "@type": "workbench/cifti_replace_structure",
        "cifti": cifti,
        "direction": direction,
        "label": label,
        "metric": metric,
        "volume": volume,
        "discard_unused_labels": discard_unused_labels,
    }
    if volume_all is not None:
        params["volume_all"] = volume_all
    if label_collision is not None:
        params["label_collision"] = label_collision
    return params

def cifti_replace_structure_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiReplaceStructure."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti", None) is None:
        raise StyxValidationError("`cifti` must not be None")
    if not isinstance(params["cifti"], str):
        raise StyxValidationError(f'`cifti` has the wrong type: Received `{type(params["cifti"])}` expected `str`')
    if params.get("direction", None) is None:
        raise StyxValidationError("`direction` must not be None")
    if not isinstance(params["direction"], str):
        raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `str`')
    if params.get("volume_all", None) is not None:
        if not isinstance(params["volume_all"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["volume_all"])}\'')
        if params["volume_all"].get("volume", None) is None:
            raise StyxValidationError("`volume` must not be None")
        if not isinstance(params["volume_all"]["volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume_all"]["volume"])}` expected `InputPathType`')
        if params["volume_all"].get("from_cropped", None) is not None:
            if not isinstance(params["volume_all"]["from_cropped"], bool):
                raise StyxValidationError(f'`from_cropped` has the wrong type: Received `{type(params["volume_all"]["from_cropped"])}` expected `bool`')
    if params.get("discard_unused_labels", None) is not None:
        if not isinstance(params["discard_unused_labels"], bool):
            raise StyxValidationError(f'`discard_unused_labels` has the wrong type: Received `{type(params["discard_unused_labels"])}` expected `bool`')
    if params.get("label_collision", None) is not None:
        if not isinstance(params["label_collision"], str):
            raise StyxValidationError(f'`label_collision` has the wrong type: Received `{type(params["label_collision"])}` expected `str`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], list):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `list[CiftiReplaceStructureLabel]`')
    for e in params["label"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("structure", None) is None:
            raise StyxValidationError("`structure` must not be None")
        if not isinstance(e["structure"], str):
            raise StyxValidationError(f'`structure` has the wrong type: Received `{type(e["structure"])}` expected `str`')
        if e.get("label", None) is None:
            raise StyxValidationError("`label` must not be None")
        if not isinstance(e["label"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label` has the wrong type: Received `{type(e["label"])}` expected `InputPathType`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], list):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `list[CiftiReplaceStructureMetric]`')
    for e_2 in params["metric"]:
        if not isinstance(e_2, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
        if e_2.get("structure", None) is None:
            raise StyxValidationError("`structure` must not be None")
        if not isinstance(e_2["structure"], str):
            raise StyxValidationError(f'`structure` has the wrong type: Received `{type(e_2["structure"])}` expected `str`')
        if e_2.get("metric", None) is None:
            raise StyxValidationError("`metric` must not be None")
        if not isinstance(e_2["metric"], (pathlib.Path, str)):
            raise StyxValidationError(f'`metric` has the wrong type: Received `{type(e_2["metric"])}` expected `InputPathType`')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], list):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `list[CiftiReplaceStructureVolume]`')
    for e_3 in params["volume"]:
        if not isinstance(e_3, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
        if e_3.get("structure", None) is None:
            raise StyxValidationError("`structure` must not be None")
        if not isinstance(e_3["structure"], str):
            raise StyxValidationError(f'`structure` has the wrong type: Received `{type(e_3["structure"])}` expected `str`')
        if e_3.get("volume", None) is None:
            raise StyxValidationError("`volume` must not be None")
        if not isinstance(e_3["volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`volume` has the wrong type: Received `{type(e_3["volume"])}` expected `InputPathType`')
        if e_3.get("from_cropped", None) is not None:
            if not isinstance(e_3["from_cropped"], bool):
                raise StyxValidationError(f'`from_cropped` has the wrong type: Received `{type(e_3["from_cropped"])}` expected `bool`')

def cifti_replace_structure_cargs(params: CiftiReplaceStructure, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-replace-structure")
    cargs.append(params["cifti"])
    cargs.append(params["direction"])
    v_0 = params.get("volume_all")
    if v_0 is not None:
        cargs.append("-volume-all")
        cargs.append(execution.input_file(v_0["volume"]))
        if v_0.get("from_cropped"):
            cargs.append("-from-cropped")
    if params.get("discard_unused_labels"):
        cargs.append("-discard-unused-labels")
    v_1 = params.get("label_collision")
    if v_1 is not None:
        cargs.append("-label-collision")
        cargs.append(v_1)
    for item0 in params["label"]:
        cargs.append("-label")
        cargs.append(item0["structure"])
        cargs.append(execution.input_file(item0["label"]))
    for item1 in params["metric"]:
        cargs.append("-metric")
        cargs.append(item1["structure"])
        cargs.append(execution.input_file(item1["metric"]))
    for item2 in params["volume"]:
        cargs.append("-volume")
        cargs.append(item2["structure"])
        cargs.append(execution.input_file(item2["volume"]))
        if item2.get("from_cropped"):
            cargs.append("-from-cropped")
    return cargs

def cifti_replace_structure_outputs(params: CiftiReplaceStructure, execution: Execution) -> CiftiReplaceStructureOutputs:
    """Build the CiftiReplaceStructureOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CiftiReplaceStructureOutputs(
        root=root_v,
    )

def cifti_replace_structure_execute(params: CiftiReplaceStructure, runner: Runner | None = None) -> CiftiReplaceStructureOutputs:
    """
    REPLACE DATA IN A STRUCTURE IN A CIFTI FILE

    This is a fairly low-level command, you probably want to use -cifti-create-dense-from-template instead.

You must specify at least one of -metric, -label, -volume, or -volume-all for this command to do anything.  Input volumes must line up with the output of -cifti-separate.  For dtseries/dscalar, use COLUMN, and if your dconn matrix will be fully symmetric, COLUMN is more efficient.  The -volume-all option must not be specified when using a -volume option.  A -metric option must not be specified when using a -label option, and is not recommended on a label-type cifti file.  For each <structure> argument, use one of the following strings:

CORTEX_LEFT
CORTEX_RIGHT
CEREBELLUM
ACCUMBENS_LEFT
ACCUMBENS_RIGHT
ALL_GREY_MATTER
ALL_WHITE_MATTER
AMYGDALA_LEFT
AMYGDALA_RIGHT
BRAIN_STEM
CAUDATE_LEFT
CAUDATE_RIGHT
CEREBELLAR_WHITE_MATTER_LEFT
CEREBELLAR_WHITE_MATTER_RIGHT
CEREBELLUM_LEFT
CEREBELLUM_RIGHT
CEREBRAL_WHITE_MATTER_LEFT
CEREBRAL_WHITE_MATTER_RIGHT
CORTEX
DIENCEPHALON_VENTRAL_LEFT
DIENCEPHALON_VENTRAL_RIGHT
HIPPOCAMPUS_LEFT
HIPPOCAMPUS_RIGHT
HIPPOCAMPUS_DENTATE_LEFT
HIPPOCAMPUS_DENTATE_RIGHT
INVALID
OTHER
OTHER_GREY_MATTER
OTHER_WHITE_MATTER
PALLIDUM_LEFT
PALLIDUM_RIGHT
PUTAMEN_LEFT
PUTAMEN_RIGHT
THALAMUS_LEFT
THALAMUS_RIGHT

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_replace_structure_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_REPLACE_STRUCTURE_METADATA)
    execution.params(params)
    args = cifti_replace_structure_cargs(params, execution)
    out = cifti_replace_structure_outputs(params, execution)
    execution.run(args)
    return out

def cifti_replace_structure(
    cifti: str,
    direction: str,
    label: list[CiftiReplaceStructureLabel],
    metric: list[CiftiReplaceStructureMetric],
    volume: list[CiftiReplaceStructureVolume],
    volume_all: CiftiReplaceStructureVolumeAll | None = None,
    discard_unused_labels: bool = False,
    label_collision: str | None = None,
    runner: Runner | None = None,
) -> CiftiReplaceStructureOutputs:
    """
    REPLACE DATA IN A STRUCTURE IN A CIFTI FILE

    This is a fairly low-level command, you probably want to use -cifti-create-dense-from-template instead.

You must specify at least one of -metric, -label, -volume, or -volume-all for this command to do anything.  Input volumes must line up with the output of -cifti-separate.  For dtseries/dscalar, use COLUMN, and if your dconn matrix will be fully symmetric, COLUMN is more efficient.  The -volume-all option must not be specified when using a -volume option.  A -metric option must not be specified when using a -label option, and is not recommended on a label-type cifti file.  For each <structure> argument, use one of the following strings:

CORTEX_LEFT
CORTEX_RIGHT
CEREBELLUM
ACCUMBENS_LEFT
ACCUMBENS_RIGHT
ALL_GREY_MATTER
ALL_WHITE_MATTER
AMYGDALA_LEFT
AMYGDALA_RIGHT
BRAIN_STEM
CAUDATE_LEFT
CAUDATE_RIGHT
CEREBELLAR_WHITE_MATTER_LEFT
CEREBELLAR_WHITE_MATTER_RIGHT
CEREBELLUM_LEFT
CEREBELLUM_RIGHT
CEREBRAL_WHITE_MATTER_LEFT
CEREBRAL_WHITE_MATTER_RIGHT
CORTEX
DIENCEPHALON_VENTRAL_LEFT
DIENCEPHALON_VENTRAL_RIGHT
HIPPOCAMPUS_LEFT
HIPPOCAMPUS_RIGHT
HIPPOCAMPUS_DENTATE_LEFT
HIPPOCAMPUS_DENTATE_RIGHT
INVALID
OTHER
OTHER_GREY_MATTER
OTHER_WHITE_MATTER
PALLIDUM_LEFT
PALLIDUM_RIGHT
PUTAMEN_LEFT
PUTAMEN_RIGHT
THALAMUS_LEFT
THALAMUS_RIGHT

    Args:
        cifti: the cifti to modify
        direction: which dimension to interpret as a single map, ROW or COLUMN
        label: replace the data in a surface label component
        metric: replace the data in a surface component
        volume: replace the data in a volume component
        volume_all: replace the data in all volume components
        discard_unused_labels: when operating on a dlabel file, drop any unused\
            label keys from the label table
        label_collision: how to handle conflicts between label keys
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_replace_structure_params(
        cifti=cifti,
        direction=direction,
        label=label,
        metric=metric,
        volume=volume,
        volume_all=volume_all,
        discard_unused_labels=discard_unused_labels,
        label_collision=label_collision,
    )
    return cifti_replace_structure_execute(params, runner)

__all__ = [
    "CiftiReplaceStructure",
    "CiftiReplaceStructureOutputs",
    "CIFTI_REPLACE_STRUCTURE_METADATA",
    "cifti_replace_structure_cargs",
    "cifti_replace_structure_outputs",
    "cifti_replace_structure_params",
    "cifti_replace_structure_execute",
    "cifti_replace_structure_validate",
    "cifti_replace_structure",
]