# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_MODIFY_SPHERE_METADATA = Metadata(
    id="surface_modify_sphere",
    name="CHANGE RADIUS AND OPTIONALLY RECENTER A SPHERE",
    package="workbench",
)

SurfaceModifySphere = typing.TypedDict(
    "SurfaceModifySphere",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_modify_sphere"]],
        "sphere_in": InputPathType,
        "radius": float,
        "sphere_out": str,
        "recenter": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SurfaceModifySphereOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sphere_out: OutputPathType
    """the output sphere"""

def surface_modify_sphere_params(
    sphere_in: InputPathType,
    radius: float,
    sphere_out: str,
    recenter: bool = False,
) -> SurfaceModifySphere:
    """
    Build parameters.

    Args:
        sphere_in: the sphere to modify
        radius: the radius the output sphere should have
        sphere_out: the output sphere
        recenter: recenter the sphere by means of the bounding box

    Returns:
        Parameter dictionary.
    """
    params: SurfaceModifySphere = {
        "@type": "workbench/surface_modify_sphere",
        "sphere_in": sphere_in,
        "radius": radius,
        "sphere_out": sphere_out,
        "recenter": recenter,
    }
    return params

def surface_modify_sphere_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceModifySphere."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sphere_in", None) is None:
        raise StyxValidationError("`sphere_in` must not be None")
    if not isinstance(params["sphere_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere_in` has the wrong type: Received `{type(params["sphere_in"])}` expected `InputPathType`')
    if params.get("radius", None) is None:
        raise StyxValidationError("`radius` must not be None")
    if not isinstance(params["radius"], (float, int)):
        raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')
    if params.get("sphere_out", None) is None:
        raise StyxValidationError("`sphere_out` must not be None")
    if not isinstance(params["sphere_out"], str):
        raise StyxValidationError(f'`sphere_out` has the wrong type: Received `{type(params["sphere_out"])}` expected `str`')
    if params.get("recenter", None) is not None:
        if not isinstance(params["recenter"], bool):
            raise StyxValidationError(f'`recenter` has the wrong type: Received `{type(params["recenter"])}` expected `bool`')

def surface_modify_sphere_cargs(params: SurfaceModifySphere, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-modify-sphere")
    cargs.append(execution.input_file(params["sphere_in"]))
    cargs.append(str(params["radius"]))
    cargs.append(params["sphere_out"])
    if params.get("recenter"):
        cargs.append("-recenter")
    return cargs

def surface_modify_sphere_outputs(params: SurfaceModifySphere, execution: Execution) -> SurfaceModifySphereOutputs:
    """Build the SurfaceModifySphereOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    sphere_out_v: OutputPathType = execution.output_file(params["sphere_out"])
    return SurfaceModifySphereOutputs(
        root=root_v,
        sphere_out=sphere_out_v,
    )

def surface_modify_sphere_execute(params: SurfaceModifySphere, runner: Runner | None = None) -> SurfaceModifySphereOutputs:
    """
    CHANGE RADIUS AND OPTIONALLY RECENTER A SPHERE

    This command may be useful if you have used -surface-resample to resample a sphere, which can suffer from problems generally not present in -surface-sphere-project-unproject.  If the sphere should already be centered around the origin, using -recenter may still shift it slightly before changing the radius, which is likely to be undesireable.

If <sphere-in> is not close to spherical, or not centered around the origin and -recenter is not used, a warning is printed.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_modify_sphere_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_MODIFY_SPHERE_METADATA)
    execution.params(params)
    args = surface_modify_sphere_cargs(params, execution)
    out = surface_modify_sphere_outputs(params, execution)
    execution.run(args)
    return out

def surface_modify_sphere(
    sphere_in: InputPathType,
    radius: float,
    sphere_out: str,
    recenter: bool = False,
    runner: Runner | None = None,
) -> SurfaceModifySphereOutputs:
    """
    CHANGE RADIUS AND OPTIONALLY RECENTER A SPHERE

    This command may be useful if you have used -surface-resample to resample a sphere, which can suffer from problems generally not present in -surface-sphere-project-unproject.  If the sphere should already be centered around the origin, using -recenter may still shift it slightly before changing the radius, which is likely to be undesireable.

If <sphere-in> is not close to spherical, or not centered around the origin and -recenter is not used, a warning is printed.

    Args:
        sphere_in: the sphere to modify
        radius: the radius the output sphere should have
        sphere_out: the output sphere
        recenter: recenter the sphere by means of the bounding box
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_modify_sphere_params(
        sphere_in=sphere_in,
        radius=radius,
        sphere_out=sphere_out,
        recenter=recenter,
    )
    return surface_modify_sphere_execute(params, runner)

__all__ = [
    "SurfaceModifySphere",
    "SurfaceModifySphereOutputs",
    "SURFACE_MODIFY_SPHERE_METADATA",
    "surface_modify_sphere_cargs",
    "surface_modify_sphere_outputs",
    "surface_modify_sphere_params",
    "surface_modify_sphere_execute",
    "surface_modify_sphere_validate",
    "surface_modify_sphere",
]