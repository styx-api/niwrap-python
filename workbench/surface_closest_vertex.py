# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_CLOSEST_VERTEX_METADATA = Metadata(
    id="surface_closest_vertex",
    name="FIND CLOSEST SURFACE VERTEX TO COORDINATES",
    package="workbench",
)

SurfaceClosestVertex = typing.TypedDict(
    "SurfaceClosestVertex",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_closest_vertex"]],
        "surface": InputPathType,
        "coord_list_file": str,
        "vertex_list_out": str,
    },
)

@dataclasses.dataclass
class SurfaceClosestVertexOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_closest_vertex_params(
    surface: InputPathType,
    coord_list_file: str,
    vertex_list_out: str,
) -> SurfaceClosestVertex:
    """
    Build parameters.

    Args:
        surface: the surface to use
        coord_list_file: text file with coordinates
        vertex_list_out: output - the output text file with vertex numbers

    Returns:
        Parameter dictionary.
    """
    params: SurfaceClosestVertex = {
        "@type": "workbench/surface_closest_vertex",
        "surface": surface,
        "coord_list_file": coord_list_file,
        "vertex_list_out": vertex_list_out,
    }
    return params

def surface_closest_vertex_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceClosestVertex."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("coord_list_file", None) is None:
        raise StyxValidationError("`coord_list_file` must not be None")
    if not isinstance(params["coord_list_file"], str):
        raise StyxValidationError(f'`coord_list_file` has the wrong type: Received `{type(params["coord_list_file"])}` expected `str`')
    if params.get("vertex_list_out", None) is None:
        raise StyxValidationError("`vertex_list_out` must not be None")
    if not isinstance(params["vertex_list_out"], str):
        raise StyxValidationError(f'`vertex_list_out` has the wrong type: Received `{type(params["vertex_list_out"])}` expected `str`')

def surface_closest_vertex_cargs(params: SurfaceClosestVertex, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-closest-vertex")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["coord_list_file"])
    cargs.append(params["vertex_list_out"])
    return cargs

def surface_closest_vertex_outputs(params: SurfaceClosestVertex, execution: Execution) -> SurfaceClosestVertexOutputs:
    """Build the SurfaceClosestVertexOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceClosestVertexOutputs(
        root=root_v,
    )

def surface_closest_vertex_execute(params: SurfaceClosestVertex, runner: Runner | None = None) -> SurfaceClosestVertexOutputs:
    """
    FIND CLOSEST SURFACE VERTEX TO COORDINATES

    For each coordinate XYZ triple, find the closest vertex in the surface, and output its vertex number into a text file.  The input file should only use whitespace to separate coordinates (spaces, newlines, tabs), for instance:

20 30 25
30 -20 10

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_closest_vertex_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_CLOSEST_VERTEX_METADATA)
    execution.params(params)
    args = surface_closest_vertex_cargs(params, execution)
    out = surface_closest_vertex_outputs(params, execution)
    execution.run(args)
    return out

def surface_closest_vertex(
    surface: InputPathType,
    coord_list_file: str,
    vertex_list_out: str,
    runner: Runner | None = None,
) -> SurfaceClosestVertexOutputs:
    """
    FIND CLOSEST SURFACE VERTEX TO COORDINATES

    For each coordinate XYZ triple, find the closest vertex in the surface, and output its vertex number into a text file.  The input file should only use whitespace to separate coordinates (spaces, newlines, tabs), for instance:

20 30 25
30 -20 10

    Args:
        surface: the surface to use
        coord_list_file: text file with coordinates
        vertex_list_out: output - the output text file with vertex numbers
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_closest_vertex_params(
        surface=surface,
        coord_list_file=coord_list_file,
        vertex_list_out=vertex_list_out,
    )
    return surface_closest_vertex_execute(params, runner)

__all__ = [
    "SurfaceClosestVertex",
    "SurfaceClosestVertexOutputs",
    "SURFACE_CLOSEST_VERTEX_METADATA",
    "surface_closest_vertex_cargs",
    "surface_closest_vertex_outputs",
    "surface_closest_vertex_params",
    "surface_closest_vertex_execute",
    "surface_closest_vertex_validate",
    "surface_closest_vertex",
]