# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ZIP_SCENE_FILE_METADATA = Metadata(
    id="zip_scene_file",
    name="ZIP A SCENE FILE AND ITS DATA FILES",
    package="workbench",
)

ZipSceneFile = typing.TypedDict(
    "ZipSceneFile",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/zip_scene_file"]],
        "scene_file": str,
        "extract_folder": str,
        "zip_file": str,
        "base_dir": typing.NotRequired[str],
        "skip_missing": typing.NotRequired[bool],
        "write_scene_file": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ZipSceneFileOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def zip_scene_file_params(
    scene_file: str,
    extract_folder: str,
    zip_file: str,
    base_dir: str | None = None,
    skip_missing: bool = False,
    write_scene_file: bool = False,
) -> ZipSceneFile:
    """
    Build parameters.

    Args:
        scene_file: the scene file to make the zip file from
        extract_folder: the name of the folder created when the zip file is\
            unzipped
        zip_file: out - the zip file that will be created
        base_dir: specify a directory that all data files are somewhere within,\
            this will become the root of the zipfile's directory structure
        skip_missing: any missing files will generate only warnings, and the zip\
            file will be created anyway
        write_scene_file: rewrite the scene file before zipping, to store a new\
            base path or fix extra '..'s in paths that might break

    Returns:
        Parameter dictionary.
    """
    params: ZipSceneFile = {
        "@type": "workbench/zip_scene_file",
        "scene_file": scene_file,
        "extract_folder": extract_folder,
        "zip_file": zip_file,
        "skip_missing": skip_missing,
        "write_scene_file": write_scene_file,
    }
    if base_dir is not None:
        params["base_dir"] = base_dir
    return params

def zip_scene_file_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ZipSceneFile."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scene_file", None) is None:
        raise StyxValidationError("`scene_file` must not be None")
    if not isinstance(params["scene_file"], str):
        raise StyxValidationError(f'`scene_file` has the wrong type: Received `{type(params["scene_file"])}` expected `str`')
    if params.get("extract_folder", None) is None:
        raise StyxValidationError("`extract_folder` must not be None")
    if not isinstance(params["extract_folder"], str):
        raise StyxValidationError(f'`extract_folder` has the wrong type: Received `{type(params["extract_folder"])}` expected `str`')
    if params.get("zip_file", None) is None:
        raise StyxValidationError("`zip_file` must not be None")
    if not isinstance(params["zip_file"], str):
        raise StyxValidationError(f'`zip_file` has the wrong type: Received `{type(params["zip_file"])}` expected `str`')
    if params.get("base_dir", None) is not None:
        if not isinstance(params["base_dir"], str):
            raise StyxValidationError(f'`base_dir` has the wrong type: Received `{type(params["base_dir"])}` expected `str`')
    if params.get("skip_missing", None) is not None:
        if not isinstance(params["skip_missing"], bool):
            raise StyxValidationError(f'`skip_missing` has the wrong type: Received `{type(params["skip_missing"])}` expected `bool`')
    if params.get("write_scene_file", None) is not None:
        if not isinstance(params["write_scene_file"], bool):
            raise StyxValidationError(f'`write_scene_file` has the wrong type: Received `{type(params["write_scene_file"])}` expected `bool`')

def zip_scene_file_cargs(params: ZipSceneFile, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-zip-scene-file")
    cargs.append(params["scene_file"])
    cargs.append(params["extract_folder"])
    cargs.append(params["zip_file"])
    v_0 = params.get("base_dir")
    if v_0 is not None:
        cargs.append("-base-dir")
        cargs.append(v_0)
    if params.get("skip_missing"):
        cargs.append("-skip-missing")
    if params.get("write_scene_file"):
        cargs.append("-write-scene-file")
    return cargs

def zip_scene_file_outputs(params: ZipSceneFile, execution: Execution) -> ZipSceneFileOutputs:
    """Build the ZipSceneFileOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ZipSceneFileOutputs(
        root=root_v,
    )

def zip_scene_file_execute(params: ZipSceneFile, runner: Runner | None = None) -> ZipSceneFileOutputs:
    """
    ZIP A SCENE FILE AND ITS DATA FILES

    If zip-file already exists, it will be overwritten.  If -base-dir is not specified, the base directory will be automatically set to the lowest level directory containing all files.  The scene file must contain only relative paths, and no data files may be outside the base directory.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    zip_scene_file_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ZIP_SCENE_FILE_METADATA)
    execution.params(params)
    args = zip_scene_file_cargs(params, execution)
    out = zip_scene_file_outputs(params, execution)
    execution.run(args)
    return out

def zip_scene_file(
    scene_file: str,
    extract_folder: str,
    zip_file: str,
    base_dir: str | None = None,
    skip_missing: bool = False,
    write_scene_file: bool = False,
    runner: Runner | None = None,
) -> ZipSceneFileOutputs:
    """
    ZIP A SCENE FILE AND ITS DATA FILES

    If zip-file already exists, it will be overwritten.  If -base-dir is not specified, the base directory will be automatically set to the lowest level directory containing all files.  The scene file must contain only relative paths, and no data files may be outside the base directory.

    Args:
        scene_file: the scene file to make the zip file from
        extract_folder: the name of the folder created when the zip file is\
            unzipped
        zip_file: out - the zip file that will be created
        base_dir: specify a directory that all data files are somewhere within,\
            this will become the root of the zipfile's directory structure
        skip_missing: any missing files will generate only warnings, and the zip\
            file will be created anyway
        write_scene_file: rewrite the scene file before zipping, to store a new\
            base path or fix extra '..'s in paths that might break
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = zip_scene_file_params(
        scene_file=scene_file,
        extract_folder=extract_folder,
        zip_file=zip_file,
        base_dir=base_dir,
        skip_missing=skip_missing,
        write_scene_file=write_scene_file,
    )
    return zip_scene_file_execute(params, runner)

__all__ = [
    "ZipSceneFile",
    "ZipSceneFileOutputs",
    "ZIP_SCENE_FILE_METADATA",
    "zip_scene_file_cargs",
    "zip_scene_file_outputs",
    "zip_scene_file_params",
    "zip_scene_file_execute",
    "zip_scene_file_validate",
    "zip_scene_file",
]