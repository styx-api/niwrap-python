# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_VECTOR_TOWARD_ROI_METADATA = Metadata(
    id="metric_vector_toward_roi",
    name="FIND IF VECTORS POINT TOWARD AN ROI",
    package="workbench",
)

MetricVectorTowardRoi = typing.TypedDict(
    "MetricVectorTowardRoi",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_vector_toward_roi"]],
        "surface": InputPathType,
        "target_roi": InputPathType,
        "metric_out": str,
        "roi": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class MetricVectorTowardRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric"""

def metric_vector_toward_roi_params(
    surface: InputPathType,
    target_roi: InputPathType,
    metric_out: str,
    roi: InputPathType | None = None,
) -> MetricVectorTowardRoi:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        target_roi: the roi to find the shortest path to
        metric_out: the output metric
        roi: don't compute for vertices outside an roi

    Returns:
        Parameter dictionary.
    """
    params: MetricVectorTowardRoi = {
        "@type": "workbench/metric_vector_toward_roi",
        "surface": surface,
        "target_roi": target_roi,
        "metric_out": metric_out,
    }
    if roi is not None:
        params["roi"] = roi
    return params

def metric_vector_toward_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricVectorTowardRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("target_roi", None) is None:
        raise StyxValidationError("`target_roi` must not be None")
    if not isinstance(params["target_roi"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target_roi` has the wrong type: Received `{type(params["target_roi"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')

def metric_vector_toward_roi_cargs(params: MetricVectorTowardRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-vector-toward-roi")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["target_roi"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    return cargs

def metric_vector_toward_roi_outputs(params: MetricVectorTowardRoi, execution: Execution) -> MetricVectorTowardRoiOutputs:
    """Build the MetricVectorTowardRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return MetricVectorTowardRoiOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def metric_vector_toward_roi_execute(params: MetricVectorTowardRoi, runner: Runner | None = None) -> MetricVectorTowardRoiOutputs:
    """
    FIND IF VECTORS POINT TOWARD AN ROI

    At each vertex, compute the vector along the start of the shortest path to the ROI.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_vector_toward_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_VECTOR_TOWARD_ROI_METADATA)
    execution.params(params)
    args = metric_vector_toward_roi_cargs(params, execution)
    out = metric_vector_toward_roi_outputs(params, execution)
    execution.run(args)
    return out

def metric_vector_toward_roi(
    surface: InputPathType,
    target_roi: InputPathType,
    metric_out: str,
    roi: InputPathType | None = None,
    runner: Runner | None = None,
) -> MetricVectorTowardRoiOutputs:
    """
    FIND IF VECTORS POINT TOWARD AN ROI

    At each vertex, compute the vector along the start of the shortest path to the ROI.

    Args:
        surface: the surface to compute on
        target_roi: the roi to find the shortest path to
        metric_out: the output metric
        roi: don't compute for vertices outside an roi
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_vector_toward_roi_params(
        surface=surface,
        target_roi=target_roi,
        metric_out=metric_out,
        roi=roi,
    )
    return metric_vector_toward_roi_execute(params, runner)

__all__ = [
    "MetricVectorTowardRoi",
    "MetricVectorTowardRoiOutputs",
    "METRIC_VECTOR_TOWARD_ROI_METADATA",
    "metric_vector_toward_roi_cargs",
    "metric_vector_toward_roi_outputs",
    "metric_vector_toward_roi_params",
    "metric_vector_toward_roi_execute",
    "metric_vector_toward_roi_validate",
    "metric_vector_toward_roi",
]