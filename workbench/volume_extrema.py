# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_EXTREMA_METADATA = Metadata(
    id="volume_extrema",
    name="FIND EXTREMA IN A VOLUME FILE",
    package="workbench",
)

class VolumeExtremaPresmooth(typing.TypedDict):
    kernel: float
    """the size of the gaussian smoothing kernel in mm, as sigma by default"""
    fwhm: typing.NotRequired[bool]
    """kernel size is FWHM, not sigma"""

class VolumeExtremaThreshold(typing.TypedDict):
    low: float
    """the largest value to consider for being a minimum"""
    high: float
    """the smallest value to consider for being a maximum"""

VolumeExtrema = typing.TypedDict(
    "VolumeExtrema",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_extrema"]],
        "volume_in": InputPathType,
        "distance": float,
        "volume_out": str,
        "presmooth": typing.NotRequired[VolumeExtremaPresmooth],
        "roi": typing.NotRequired[InputPathType],
        "threshold": typing.NotRequired[VolumeExtremaThreshold],
        "sum_subvols": typing.NotRequired[bool],
        "consolidate_mode": typing.NotRequired[bool],
        "only_maxima": typing.NotRequired[bool],
        "only_minima": typing.NotRequired[bool],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeExtremaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output extrema volume"""

def volume_extrema_params(
    volume_in: InputPathType,
    distance: float,
    volume_out: str,
    presmooth: VolumeExtremaPresmooth | None = None,
    roi: InputPathType | None = None,
    threshold: VolumeExtremaThreshold | None = None,
    sum_subvols: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
    subvolume: str | None = None,
) -> VolumeExtrema:
    """
    Build parameters.

    Args:
        volume_in: volume file to find the extrema of
        distance: the minimum distance between identified extrema of the same type
        volume_out: the output extrema volume
        presmooth: smooth the volume before finding extrema
        roi: ignore values outside the selected area
        threshold: ignore small extrema
        sum_subvols: output the sum of the extrema subvolumes instead of each\
            subvolume separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima
        subvolume: select a single subvolume to find extrema in

    Returns:
        Parameter dictionary.
    """
    params: VolumeExtrema = {
        "@type": "workbench/volume_extrema",
        "volume_in": volume_in,
        "distance": distance,
        "volume_out": volume_out,
        "sum_subvols": sum_subvols,
        "consolidate_mode": consolidate_mode,
        "only_maxima": only_maxima,
        "only_minima": only_minima,
    }
    if presmooth is not None:
        params["presmooth"] = presmooth
    if roi is not None:
        params["roi"] = roi
    if threshold is not None:
        params["threshold"] = threshold
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_extrema_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeExtrema."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("distance", None) is None:
        raise StyxValidationError("`distance` must not be None")
    if not isinstance(params["distance"], (float, int)):
        raise StyxValidationError(f'`distance` has the wrong type: Received `{type(params["distance"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("presmooth", None) is not None:
        if not isinstance(params["presmooth"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["presmooth"])}\'')
        if params["presmooth"].get("kernel", None) is None:
            raise StyxValidationError("`kernel` must not be None")
        if not isinstance(params["presmooth"]["kernel"], (float, int)):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["presmooth"]["kernel"])}` expected `float`')
        if params["presmooth"].get("fwhm", None) is not None:
            if not isinstance(params["presmooth"]["fwhm"], bool):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["presmooth"]["fwhm"])}` expected `bool`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["threshold"])}\'')
        if params["threshold"].get("low", None) is None:
            raise StyxValidationError("`low` must not be None")
        if not isinstance(params["threshold"]["low"], (float, int)):
            raise StyxValidationError(f'`low` has the wrong type: Received `{type(params["threshold"]["low"])}` expected `float`')
        if params["threshold"].get("high", None) is None:
            raise StyxValidationError("`high` must not be None")
        if not isinstance(params["threshold"]["high"], (float, int)):
            raise StyxValidationError(f'`high` has the wrong type: Received `{type(params["threshold"]["high"])}` expected `float`')
    if params.get("sum_subvols", None) is not None:
        if not isinstance(params["sum_subvols"], bool):
            raise StyxValidationError(f'`sum_subvols` has the wrong type: Received `{type(params["sum_subvols"])}` expected `bool`')
    if params.get("consolidate_mode", None) is not None:
        if not isinstance(params["consolidate_mode"], bool):
            raise StyxValidationError(f'`consolidate_mode` has the wrong type: Received `{type(params["consolidate_mode"])}` expected `bool`')
    if params.get("only_maxima", None) is not None:
        if not isinstance(params["only_maxima"], bool):
            raise StyxValidationError(f'`only_maxima` has the wrong type: Received `{type(params["only_maxima"])}` expected `bool`')
    if params.get("only_minima", None) is not None:
        if not isinstance(params["only_minima"], bool):
            raise StyxValidationError(f'`only_minima` has the wrong type: Received `{type(params["only_minima"])}` expected `bool`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_extrema_cargs(params: VolumeExtrema, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-extrema")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(str(params["distance"]))
    cargs.append(params["volume_out"])
    v_0 = params.get("presmooth")
    if v_0 is not None:
        cargs.append("-presmooth")
        cargs.append(str(v_0["kernel"]))
        if v_0.get("fwhm"):
            cargs.append("-fwhm")
    v_1 = params.get("roi")
    if v_1 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("threshold")
    if v_2 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_2["low"]))
        cargs.append(str(v_2["high"]))
    if params.get("sum_subvols"):
        cargs.append("-sum-subvols")
    if params.get("consolidate_mode"):
        cargs.append("-consolidate-mode")
    if params.get("only_maxima"):
        cargs.append("-only-maxima")
    if params.get("only_minima"):
        cargs.append("-only-minima")
    v_3 = params.get("subvolume")
    if v_3 is not None:
        cargs.append("-subvolume")
        cargs.append(v_3)
    return cargs

def volume_extrema_outputs(params: VolumeExtrema, execution: Execution) -> VolumeExtremaOutputs:
    """Build the VolumeExtremaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeExtremaOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_extrema_execute(params: VolumeExtrema, runner: Runner | None = None) -> VolumeExtremaOutputs:
    """
    FIND EXTREMA IN A VOLUME FILE

    Finds extrema in a volume file, such that no two extrema of the same type are within <distance> of each other.  The extrema are labeled as -1 for minima, 1 for maxima, 0 otherwise.  If -only-maxima or -only-minima is specified, then it will ignore extrema not of the specified type.  These options are mutually exclusive.

If -sum-subvols is specified, these extrema subvolumes are summed, and the output has a single subvolume with this result.

By default, a datapoint is an extrema only if it is more extreme than every other datapoint that is within <distance> from it.  If -consolidate-mode is used, it instead starts by finding all datapoints that are more extreme than their immediate neighbors, then while there are any extrema within <distance> of each other, take the two extrema closest to each other and merge them into one by a weighted average based on how many original extrema have been merged into each.

By default, all input subvolumes are used with no smoothing, use -subvolume to specify a single subvolume to use, and -presmooth to smooth the input before finding the extrema.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_extrema_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_EXTREMA_METADATA)
    execution.params(params)
    args = volume_extrema_cargs(params, execution)
    out = volume_extrema_outputs(params, execution)
    execution.run(args)
    return out

def volume_extrema(
    volume_in: InputPathType,
    distance: float,
    volume_out: str,
    presmooth: VolumeExtremaPresmooth | None = None,
    roi: InputPathType | None = None,
    threshold: VolumeExtremaThreshold | None = None,
    sum_subvols: bool = False,
    consolidate_mode: bool = False,
    only_maxima: bool = False,
    only_minima: bool = False,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeExtremaOutputs:
    """
    FIND EXTREMA IN A VOLUME FILE

    Finds extrema in a volume file, such that no two extrema of the same type are within <distance> of each other.  The extrema are labeled as -1 for minima, 1 for maxima, 0 otherwise.  If -only-maxima or -only-minima is specified, then it will ignore extrema not of the specified type.  These options are mutually exclusive.

If -sum-subvols is specified, these extrema subvolumes are summed, and the output has a single subvolume with this result.

By default, a datapoint is an extrema only if it is more extreme than every other datapoint that is within <distance> from it.  If -consolidate-mode is used, it instead starts by finding all datapoints that are more extreme than their immediate neighbors, then while there are any extrema within <distance> of each other, take the two extrema closest to each other and merge them into one by a weighted average based on how many original extrema have been merged into each.

By default, all input subvolumes are used with no smoothing, use -subvolume to specify a single subvolume to use, and -presmooth to smooth the input before finding the extrema.

    Args:
        volume_in: volume file to find the extrema of
        distance: the minimum distance between identified extrema of the same type
        volume_out: the output extrema volume
        presmooth: smooth the volume before finding extrema
        roi: ignore values outside the selected area
        threshold: ignore small extrema
        sum_subvols: output the sum of the extrema subvolumes instead of each\
            subvolume separately
        consolidate_mode: use consolidation of local minima instead of a large\
            neighborhood
        only_maxima: only find the maxima
        only_minima: only find the minima
        subvolume: select a single subvolume to find extrema in
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_extrema_params(
        volume_in=volume_in,
        distance=distance,
        volume_out=volume_out,
        presmooth=presmooth,
        roi=roi,
        threshold=threshold,
        sum_subvols=sum_subvols,
        consolidate_mode=consolidate_mode,
        only_maxima=only_maxima,
        only_minima=only_minima,
        subvolume=subvolume,
    )
    return volume_extrema_execute(params, runner)

__all__ = [
    "VolumeExtrema",
    "VolumeExtremaOutputs",
    "VOLUME_EXTREMA_METADATA",
    "volume_extrema_cargs",
    "volume_extrema_outputs",
    "volume_extrema_params",
    "volume_extrema_execute",
    "volume_extrema_validate",
    "volume_extrema",
]