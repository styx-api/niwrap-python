# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CREATE_SIGNED_DISTANCE_VOLUME_METADATA = Metadata(
    id="create_signed_distance_volume",
    name="CREATE SIGNED DISTANCE VOLUME FROM SURFACE",
    package="workbench",
)

class CreateSignedDistanceVolumeRoiOut(typing.TypedDict):
    roi_vol: str
    """the output roi volume"""

CreateSignedDistanceVolume = typing.TypedDict(
    "CreateSignedDistanceVolume",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/create_signed_distance_volume"]],
        "surface": InputPathType,
        "refspace": str,
        "outvol": str,
        "roi_out": typing.NotRequired[CreateSignedDistanceVolumeRoiOut],
        "fill_value": typing.NotRequired[float],
        "exact_limit": typing.NotRequired[float],
        "approx_limit": typing.NotRequired[float],
        "approx_neighborhood": typing.NotRequired[int],
        "winding": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CreateSignedDistanceVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outvol: OutputPathType
    """the output volume"""
    roi_vol: OutputPathType | None
    """the output roi volume"""

def create_signed_distance_volume_params(
    surface: InputPathType,
    refspace: str,
    outvol: str,
    roi_out: CreateSignedDistanceVolumeRoiOut | None = None,
    fill_value: float | None = None,
    exact_limit: float | None = None,
    approx_limit: float | None = None,
    approx_neighborhood: int | None = None,
    winding: str | None = None,
) -> CreateSignedDistanceVolume:
    """
    Build parameters.

    Args:
        surface: the input surface
        refspace: a volume in the desired output space (dims, spacing, origin)
        outvol: the output volume
        roi_out: output an roi volume of where the output has a computed value
        fill_value: specify a value to put in all voxels that don't get assigned a\
            distance
        exact_limit: specify distance for exact output
        approx_limit: specify distance for approximate output
        approx_neighborhood: voxel neighborhood for approximate calculation
        winding: winding method for point inside surface test

    Returns:
        Parameter dictionary.
    """
    params: CreateSignedDistanceVolume = {
        "@type": "workbench/create_signed_distance_volume",
        "surface": surface,
        "refspace": refspace,
        "outvol": outvol,
    }
    if roi_out is not None:
        params["roi_out"] = roi_out
    if fill_value is not None:
        params["fill_value"] = fill_value
    if exact_limit is not None:
        params["exact_limit"] = exact_limit
    if approx_limit is not None:
        params["approx_limit"] = approx_limit
    if approx_neighborhood is not None:
        params["approx_neighborhood"] = approx_neighborhood
    if winding is not None:
        params["winding"] = winding
    return params

def create_signed_distance_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CreateSignedDistanceVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("refspace", None) is None:
        raise StyxValidationError("`refspace` must not be None")
    if not isinstance(params["refspace"], str):
        raise StyxValidationError(f'`refspace` has the wrong type: Received `{type(params["refspace"])}` expected `str`')
    if params.get("outvol", None) is None:
        raise StyxValidationError("`outvol` must not be None")
    if not isinstance(params["outvol"], str):
        raise StyxValidationError(f'`outvol` has the wrong type: Received `{type(params["outvol"])}` expected `str`')
    if params.get("roi_out", None) is not None:
        if not isinstance(params["roi_out"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["roi_out"])}\'')
        if params["roi_out"].get("roi_vol", None) is None:
            raise StyxValidationError("`roi_vol` must not be None")
        if not isinstance(params["roi_out"]["roi_vol"], str):
            raise StyxValidationError(f'`roi_vol` has the wrong type: Received `{type(params["roi_out"]["roi_vol"])}` expected `str`')
    if params.get("fill_value", None) is not None:
        if not isinstance(params["fill_value"], (float, int)):
            raise StyxValidationError(f'`fill_value` has the wrong type: Received `{type(params["fill_value"])}` expected `float`')
    if params.get("exact_limit", None) is not None:
        if not isinstance(params["exact_limit"], (float, int)):
            raise StyxValidationError(f'`exact_limit` has the wrong type: Received `{type(params["exact_limit"])}` expected `float`')
    if params.get("approx_limit", None) is not None:
        if not isinstance(params["approx_limit"], (float, int)):
            raise StyxValidationError(f'`approx_limit` has the wrong type: Received `{type(params["approx_limit"])}` expected `float`')
    if params.get("approx_neighborhood", None) is not None:
        if not isinstance(params["approx_neighborhood"], int):
            raise StyxValidationError(f'`approx_neighborhood` has the wrong type: Received `{type(params["approx_neighborhood"])}` expected `int`')
    if params.get("winding", None) is not None:
        if not isinstance(params["winding"], str):
            raise StyxValidationError(f'`winding` has the wrong type: Received `{type(params["winding"])}` expected `str`')

def create_signed_distance_volume_cargs(params: CreateSignedDistanceVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-create-signed-distance-volume")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["refspace"])
    cargs.append(params["outvol"])
    v_0 = params.get("roi_out")
    if v_0 is not None:
        cargs.append("-roi-out")
        cargs.append(v_0["roi_vol"])
    v_1 = params.get("fill_value")
    if v_1 is not None:
        cargs.append("-fill-value")
        cargs.append(str(v_1))
    v_2 = params.get("exact_limit")
    if v_2 is not None:
        cargs.append("-exact-limit")
        cargs.append(str(v_2))
    v_3 = params.get("approx_limit")
    if v_3 is not None:
        cargs.append("-approx-limit")
        cargs.append(str(v_3))
    v_4 = params.get("approx_neighborhood")
    if v_4 is not None:
        cargs.append("-approx-neighborhood")
        cargs.append(str(v_4))
    v_5 = params.get("winding")
    if v_5 is not None:
        cargs.append("-winding")
        cargs.append(v_5)
    return cargs

def create_signed_distance_volume_outputs(params: CreateSignedDistanceVolume, execution: Execution) -> CreateSignedDistanceVolumeOutputs:
    """Build the CreateSignedDistanceVolumeOutputs object for this tool."""
    roi_vol_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    outvol_v: OutputPathType = execution.output_file(params["outvol"])
    __v0 = params.get("roi_out")
    if __v0 is not None:
        roi_vol_v = execution.output_file(__v0["roi_vol"])
    return CreateSignedDistanceVolumeOutputs(
        root=root_v,
        outvol=outvol_v,
        roi_vol=roi_vol_v,
    )

def create_signed_distance_volume_execute(params: CreateSignedDistanceVolume, runner: Runner | None = None) -> CreateSignedDistanceVolumeOutputs:
    """
    CREATE SIGNED DISTANCE VOLUME FROM SURFACE

    Computes the signed distance function of the surface.  Exact distance is calculated by finding the closest point on any surface triangle to the center of the voxel.  Approximate distance is calculated starting with these distances, using dijkstra's method with a neighborhood of voxels.  Specifying too small of an exact distance may produce unexpected results.  Valid specifiers for winding methods are as follows:

EVEN_ODD (default)
NEGATIVE
NONZERO
NORMALS

The NORMALS method uses the normals of triangles and edges, or the closest triangle hit by a ray from the point.  This method may be slightly faster, but is only reliable for a closed surface that does not cross through itself.  All other methods count entry (positive) and exit (negative) crossings of a vertical ray from the point, then counts as inside if the total is odd, negative, or nonzero, respectively.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    create_signed_distance_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CREATE_SIGNED_DISTANCE_VOLUME_METADATA)
    execution.params(params)
    args = create_signed_distance_volume_cargs(params, execution)
    out = create_signed_distance_volume_outputs(params, execution)
    execution.run(args)
    return out

def create_signed_distance_volume(
    surface: InputPathType,
    refspace: str,
    outvol: str,
    roi_out: CreateSignedDistanceVolumeRoiOut | None = None,
    fill_value: float | None = None,
    exact_limit: float | None = None,
    approx_limit: float | None = None,
    approx_neighborhood: int | None = None,
    winding: str | None = None,
    runner: Runner | None = None,
) -> CreateSignedDistanceVolumeOutputs:
    """
    CREATE SIGNED DISTANCE VOLUME FROM SURFACE

    Computes the signed distance function of the surface.  Exact distance is calculated by finding the closest point on any surface triangle to the center of the voxel.  Approximate distance is calculated starting with these distances, using dijkstra's method with a neighborhood of voxels.  Specifying too small of an exact distance may produce unexpected results.  Valid specifiers for winding methods are as follows:

EVEN_ODD (default)
NEGATIVE
NONZERO
NORMALS

The NORMALS method uses the normals of triangles and edges, or the closest triangle hit by a ray from the point.  This method may be slightly faster, but is only reliable for a closed surface that does not cross through itself.  All other methods count entry (positive) and exit (negative) crossings of a vertical ray from the point, then counts as inside if the total is odd, negative, or nonzero, respectively.

    Args:
        surface: the input surface
        refspace: a volume in the desired output space (dims, spacing, origin)
        outvol: the output volume
        roi_out: output an roi volume of where the output has a computed value
        fill_value: specify a value to put in all voxels that don't get assigned a\
            distance
        exact_limit: specify distance for exact output
        approx_limit: specify distance for approximate output
        approx_neighborhood: voxel neighborhood for approximate calculation
        winding: winding method for point inside surface test
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = create_signed_distance_volume_params(
        surface=surface,
        refspace=refspace,
        outvol=outvol,
        roi_out=roi_out,
        fill_value=fill_value,
        exact_limit=exact_limit,
        approx_limit=approx_limit,
        approx_neighborhood=approx_neighborhood,
        winding=winding,
    )
    return create_signed_distance_volume_execute(params, runner)

__all__ = [
    "CreateSignedDistanceVolume",
    "CreateSignedDistanceVolumeOutputs",
    "CREATE_SIGNED_DISTANCE_VOLUME_METADATA",
    "create_signed_distance_volume_cargs",
    "create_signed_distance_volume_outputs",
    "create_signed_distance_volume_params",
    "create_signed_distance_volume_execute",
    "create_signed_distance_volume_validate",
    "create_signed_distance_volume",
]