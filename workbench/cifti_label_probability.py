# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_PROBABILITY_METADATA = Metadata(
    id="cifti_label_probability",
    name="FIND FREQUENCY OF CIFTI LABELS",
    package="workbench",
)

CiftiLabelProbability = typing.TypedDict(
    "CiftiLabelProbability",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_probability"]],
        "label_maps": InputPathType,
        "probability_dscalar_out": str,
        "exclude_unlabeled": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CiftiLabelProbabilityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    probability_dscalar_out: OutputPathType
    """the relative frequencies of each label at each vertex/voxel"""

def cifti_label_probability_params(
    label_maps: InputPathType,
    probability_dscalar_out: str,
    exclude_unlabeled: bool = False,
) -> CiftiLabelProbability:
    """
    Build parameters.

    Args:
        label_maps: cifti dlabel file containing individual label maps from many\
            subjects
        probability_dscalar_out: the relative frequencies of each label at each\
            vertex/voxel
        exclude_unlabeled: don't make a probability map of the unlabeled key

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelProbability = {
        "@type": "workbench/cifti_label_probability",
        "label_maps": label_maps,
        "probability_dscalar_out": probability_dscalar_out,
        "exclude_unlabeled": exclude_unlabeled,
    }
    return params

def cifti_label_probability_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelProbability."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_maps", None) is None:
        raise StyxValidationError("`label_maps` must not be None")
    if not isinstance(params["label_maps"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_maps` has the wrong type: Received `{type(params["label_maps"])}` expected `InputPathType`')
    if params.get("probability_dscalar_out", None) is None:
        raise StyxValidationError("`probability_dscalar_out` must not be None")
    if not isinstance(params["probability_dscalar_out"], str):
        raise StyxValidationError(f'`probability_dscalar_out` has the wrong type: Received `{type(params["probability_dscalar_out"])}` expected `str`')
    if params.get("exclude_unlabeled", None) is not None:
        if not isinstance(params["exclude_unlabeled"], bool):
            raise StyxValidationError(f'`exclude_unlabeled` has the wrong type: Received `{type(params["exclude_unlabeled"])}` expected `bool`')

def cifti_label_probability_cargs(params: CiftiLabelProbability, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-probability")
    cargs.append(execution.input_file(params["label_maps"]))
    cargs.append(params["probability_dscalar_out"])
    if params.get("exclude_unlabeled"):
        cargs.append("-exclude-unlabeled")
    return cargs

def cifti_label_probability_outputs(params: CiftiLabelProbability, execution: Execution) -> CiftiLabelProbabilityOutputs:
    """Build the CiftiLabelProbabilityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    probability_dscalar_out_v: OutputPathType = execution.output_file(params["probability_dscalar_out"])
    return CiftiLabelProbabilityOutputs(
        root=root_v,
        probability_dscalar_out=probability_dscalar_out_v,
    )

def cifti_label_probability_execute(params: CiftiLabelProbability, runner: Runner | None = None) -> CiftiLabelProbabilityOutputs:
    """
    FIND FREQUENCY OF CIFTI LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that vertex/voxel, divided by the number of input maps.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_probability_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_PROBABILITY_METADATA)
    execution.params(params)
    args = cifti_label_probability_cargs(params, execution)
    out = cifti_label_probability_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_probability(
    label_maps: InputPathType,
    probability_dscalar_out: str,
    exclude_unlabeled: bool = False,
    runner: Runner | None = None,
) -> CiftiLabelProbabilityOutputs:
    """
    FIND FREQUENCY OF CIFTI LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that vertex/voxel, divided by the number of input maps.

    Args:
        label_maps: cifti dlabel file containing individual label maps from many\
            subjects
        probability_dscalar_out: the relative frequencies of each label at each\
            vertex/voxel
        exclude_unlabeled: don't make a probability map of the unlabeled key
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_probability_params(
        label_maps=label_maps,
        probability_dscalar_out=probability_dscalar_out,
        exclude_unlabeled=exclude_unlabeled,
    )
    return cifti_label_probability_execute(params, runner)

__all__ = [
    "CiftiLabelProbability",
    "CiftiLabelProbabilityOutputs",
    "CIFTI_LABEL_PROBABILITY_METADATA",
    "cifti_label_probability_cargs",
    "cifti_label_probability_outputs",
    "cifti_label_probability_params",
    "cifti_label_probability_execute",
    "cifti_label_probability_validate",
    "cifti_label_probability",
]