# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METADATA_STRING_REPLACE_METADATA = Metadata(
    id="metadata_string_replace",
    name="REPLACE A STRING IN ALL METADATA OF A FILE",
    package="workbench",
)

MetadataStringReplace = typing.TypedDict(
    "MetadataStringReplace",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metadata_string_replace"]],
        "input_file": str,
        "find_string": str,
        "replace_string": str,
        "output_file": str,
        "case_insensitive": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MetadataStringReplaceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def metadata_string_replace_params(
    input_file: str,
    find_string: str,
    replace_string: str,
    output_file: str,
    case_insensitive: bool = False,
) -> MetadataStringReplace:
    """
    Build parameters.

    Args:
        input_file: the file to replace metadata in
        find_string: the string to find
        replace_string: the string to replace <find-string> with
        output_file: output - the name to save the modified file as
        case_insensitive: match with case variation also

    Returns:
        Parameter dictionary.
    """
    params: MetadataStringReplace = {
        "@type": "workbench/metadata_string_replace",
        "input_file": input_file,
        "find_string": find_string,
        "replace_string": replace_string,
        "output_file": output_file,
        "case_insensitive": case_insensitive,
    }
    return params

def metadata_string_replace_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetadataStringReplace."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], str):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `str`')
    if params.get("find_string", None) is None:
        raise StyxValidationError("`find_string` must not be None")
    if not isinstance(params["find_string"], str):
        raise StyxValidationError(f'`find_string` has the wrong type: Received `{type(params["find_string"])}` expected `str`')
    if params.get("replace_string", None) is None:
        raise StyxValidationError("`replace_string` must not be None")
    if not isinstance(params["replace_string"], str):
        raise StyxValidationError(f'`replace_string` has the wrong type: Received `{type(params["replace_string"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("case_insensitive", None) is not None:
        if not isinstance(params["case_insensitive"], bool):
            raise StyxValidationError(f'`case_insensitive` has the wrong type: Received `{type(params["case_insensitive"])}` expected `bool`')

def metadata_string_replace_cargs(params: MetadataStringReplace, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metadata-string-replace")
    cargs.append(params["input_file"])
    cargs.append(params["find_string"])
    cargs.append(params["replace_string"])
    cargs.append(params["output_file"])
    if params.get("case_insensitive"):
        cargs.append("-case-insensitive")
    return cargs

def metadata_string_replace_outputs(params: MetadataStringReplace, execution: Execution) -> MetadataStringReplaceOutputs:
    """Build the MetadataStringReplaceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MetadataStringReplaceOutputs(
        root=root_v,
    )

def metadata_string_replace_execute(params: MetadataStringReplace, runner: Runner | None = None) -> MetadataStringReplaceOutputs:
    """
    REPLACE A STRING IN ALL METADATA OF A FILE

    Replaces all occurrences of <find-string> in the metadata and map names of <input-file> with <replace-string>.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metadata_string_replace_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METADATA_STRING_REPLACE_METADATA)
    execution.params(params)
    args = metadata_string_replace_cargs(params, execution)
    out = metadata_string_replace_outputs(params, execution)
    execution.run(args)
    return out

def metadata_string_replace(
    input_file: str,
    find_string: str,
    replace_string: str,
    output_file: str,
    case_insensitive: bool = False,
    runner: Runner | None = None,
) -> MetadataStringReplaceOutputs:
    """
    REPLACE A STRING IN ALL METADATA OF A FILE

    Replaces all occurrences of <find-string> in the metadata and map names of <input-file> with <replace-string>.

    Args:
        input_file: the file to replace metadata in
        find_string: the string to find
        replace_string: the string to replace <find-string> with
        output_file: output - the name to save the modified file as
        case_insensitive: match with case variation also
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metadata_string_replace_params(
        input_file=input_file,
        find_string=find_string,
        replace_string=replace_string,
        output_file=output_file,
        case_insensitive=case_insensitive,
    )
    return metadata_string_replace_execute(params, runner)

__all__ = [
    "MetadataStringReplace",
    "MetadataStringReplaceOutputs",
    "METADATA_STRING_REPLACE_METADATA",
    "metadata_string_replace_cargs",
    "metadata_string_replace_outputs",
    "metadata_string_replace_params",
    "metadata_string_replace_execute",
    "metadata_string_replace_validate",
    "metadata_string_replace",
]