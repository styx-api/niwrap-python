# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_LABEL_MODIFY_KEYS_METADATA = Metadata(
    id="volume_label_modify_keys",
    name="CHANGE KEY VALUES IN A VOLUME LABEL FILE",
    package="workbench",
)

VolumeLabelModifyKeys = typing.TypedDict(
    "VolumeLabelModifyKeys",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_label_modify_keys"]],
        "volume_in": InputPathType,
        "remap_file": str,
        "volume_out": str,
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeLabelModifyKeysOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume label file"""

def volume_label_modify_keys_params(
    volume_in: InputPathType,
    remap_file: str,
    volume_out: str,
    subvolume: str | None = None,
) -> VolumeLabelModifyKeys:
    """
    Build parameters.

    Args:
        volume_in: the input volume label file
        remap_file: text file with old and new key values
        volume_out: the output volume label file
        subvolume: select a single subvolume

    Returns:
        Parameter dictionary.
    """
    params: VolumeLabelModifyKeys = {
        "@type": "workbench/volume_label_modify_keys",
        "volume_in": volume_in,
        "remap_file": remap_file,
        "volume_out": volume_out,
    }
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_label_modify_keys_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeLabelModifyKeys."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("remap_file", None) is None:
        raise StyxValidationError("`remap_file` must not be None")
    if not isinstance(params["remap_file"], str):
        raise StyxValidationError(f'`remap_file` has the wrong type: Received `{type(params["remap_file"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_label_modify_keys_cargs(params: VolumeLabelModifyKeys, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-label-modify-keys")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["remap_file"])
    cargs.append(params["volume_out"])
    v_0 = params.get("subvolume")
    if v_0 is not None:
        cargs.append("-subvolume")
        cargs.append(v_0)
    return cargs

def volume_label_modify_keys_outputs(params: VolumeLabelModifyKeys, execution: Execution) -> VolumeLabelModifyKeysOutputs:
    """Build the VolumeLabelModifyKeysOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeLabelModifyKeysOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_label_modify_keys_execute(params: VolumeLabelModifyKeys, runner: Runner | None = None) -> VolumeLabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A VOLUME LABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_label_modify_keys_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_LABEL_MODIFY_KEYS_METADATA)
    execution.params(params)
    args = volume_label_modify_keys_cargs(params, execution)
    out = volume_label_modify_keys_outputs(params, execution)
    execution.run(args)
    return out

def volume_label_modify_keys(
    volume_in: InputPathType,
    remap_file: str,
    volume_out: str,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeLabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A VOLUME LABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        volume_in: the input volume label file
        remap_file: text file with old and new key values
        volume_out: the output volume label file
        subvolume: select a single subvolume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_label_modify_keys_params(
        volume_in=volume_in,
        remap_file=remap_file,
        volume_out=volume_out,
        subvolume=subvolume,
    )
    return volume_label_modify_keys_execute(params, runner)

__all__ = [
    "VolumeLabelModifyKeys",
    "VolumeLabelModifyKeysOutputs",
    "VOLUME_LABEL_MODIFY_KEYS_METADATA",
    "volume_label_modify_keys_cargs",
    "volume_label_modify_keys_outputs",
    "volume_label_modify_keys_params",
    "volume_label_modify_keys_execute",
    "volume_label_modify_keys_validate",
    "volume_label_modify_keys",
]