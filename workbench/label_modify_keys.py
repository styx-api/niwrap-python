# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_MODIFY_KEYS_METADATA = Metadata(
    id="label_modify_keys",
    name="CHANGE KEY VALUES IN A LABEL FILE",
    package="workbench",
)

LabelModifyKeys = typing.TypedDict(
    "LabelModifyKeys",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_modify_keys"]],
        "label_in": InputPathType,
        "remap_file": str,
        "label_out": str,
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class LabelModifyKeysOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """output label file"""

def label_modify_keys_params(
    label_in: InputPathType,
    remap_file: str,
    label_out: str,
    column: str | None = None,
) -> LabelModifyKeys:
    """
    Build parameters.

    Args:
        label_in: the input label file
        remap_file: text file with old and new key values
        label_out: output label file
        column: select a single column to use

    Returns:
        Parameter dictionary.
    """
    params: LabelModifyKeys = {
        "@type": "workbench/label_modify_keys",
        "label_in": label_in,
        "remap_file": remap_file,
        "label_out": label_out,
    }
    if column is not None:
        params["column"] = column
    return params

def label_modify_keys_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelModifyKeys."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("remap_file", None) is None:
        raise StyxValidationError("`remap_file` must not be None")
    if not isinstance(params["remap_file"], str):
        raise StyxValidationError(f'`remap_file` has the wrong type: Received `{type(params["remap_file"])}` expected `str`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def label_modify_keys_cargs(params: LabelModifyKeys, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-modify-keys")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["remap_file"])
    cargs.append(params["label_out"])
    v_0 = params.get("column")
    if v_0 is not None:
        cargs.append("-column")
        cargs.append(v_0)
    return cargs

def label_modify_keys_outputs(params: LabelModifyKeys, execution: Execution) -> LabelModifyKeysOutputs:
    """Build the LabelModifyKeysOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    return LabelModifyKeysOutputs(
        root=root_v,
        label_out=label_out_v,
    )

def label_modify_keys_execute(params: LabelModifyKeys, runner: Runner | None = None) -> LabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A LABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_modify_keys_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_MODIFY_KEYS_METADATA)
    execution.params(params)
    args = label_modify_keys_cargs(params, execution)
    out = label_modify_keys_outputs(params, execution)
    execution.run(args)
    return out

def label_modify_keys(
    label_in: InputPathType,
    remap_file: str,
    label_out: str,
    column: str | None = None,
    runner: Runner | None = None,
) -> LabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A LABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        label_in: the input label file
        remap_file: text file with old and new key values
        label_out: output label file
        column: select a single column to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_modify_keys_params(
        label_in=label_in,
        remap_file=remap_file,
        label_out=label_out,
        column=column,
    )
    return label_modify_keys_execute(params, runner)

__all__ = [
    "LabelModifyKeys",
    "LabelModifyKeysOutputs",
    "LABEL_MODIFY_KEYS_METADATA",
    "label_modify_keys_cargs",
    "label_modify_keys_outputs",
    "label_modify_keys_params",
    "label_modify_keys_execute",
    "label_modify_keys_validate",
    "label_modify_keys",
]