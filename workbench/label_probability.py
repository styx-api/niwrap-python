# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_PROBABILITY_METADATA = Metadata(
    id="label_probability",
    name="FIND FREQUENCY OF SURFACE LABELS",
    package="workbench",
)

LabelProbability = typing.TypedDict(
    "LabelProbability",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_probability"]],
        "label_maps": InputPathType,
        "probability_metric_out": str,
        "exclude_unlabeled": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LabelProbabilityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    probability_metric_out: OutputPathType
    """the relative frequencies of each label at each vertex"""

def label_probability_params(
    label_maps: InputPathType,
    probability_metric_out: str,
    exclude_unlabeled: bool = False,
) -> LabelProbability:
    """
    Build parameters.

    Args:
        label_maps: label file containing individual label maps from many subjects
        probability_metric_out: the relative frequencies of each label at each\
            vertex
        exclude_unlabeled: don't make a probability map of the unlabeled key

    Returns:
        Parameter dictionary.
    """
    params: LabelProbability = {
        "@type": "workbench/label_probability",
        "label_maps": label_maps,
        "probability_metric_out": probability_metric_out,
        "exclude_unlabeled": exclude_unlabeled,
    }
    return params

def label_probability_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelProbability."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_maps", None) is None:
        raise StyxValidationError("`label_maps` must not be None")
    if not isinstance(params["label_maps"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_maps` has the wrong type: Received `{type(params["label_maps"])}` expected `InputPathType`')
    if params.get("probability_metric_out", None) is None:
        raise StyxValidationError("`probability_metric_out` must not be None")
    if not isinstance(params["probability_metric_out"], str):
        raise StyxValidationError(f'`probability_metric_out` has the wrong type: Received `{type(params["probability_metric_out"])}` expected `str`')
    if params.get("exclude_unlabeled", None) is not None:
        if not isinstance(params["exclude_unlabeled"], bool):
            raise StyxValidationError(f'`exclude_unlabeled` has the wrong type: Received `{type(params["exclude_unlabeled"])}` expected `bool`')

def label_probability_cargs(params: LabelProbability, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-probability")
    cargs.append(execution.input_file(params["label_maps"]))
    cargs.append(params["probability_metric_out"])
    if params.get("exclude_unlabeled"):
        cargs.append("-exclude-unlabeled")
    return cargs

def label_probability_outputs(params: LabelProbability, execution: Execution) -> LabelProbabilityOutputs:
    """Build the LabelProbabilityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    probability_metric_out_v: OutputPathType = execution.output_file(params["probability_metric_out"])
    return LabelProbabilityOutputs(
        root=root_v,
        probability_metric_out=probability_metric_out_v,
    )

def label_probability_execute(params: LabelProbability, runner: Runner | None = None) -> LabelProbabilityOutputs:
    """
    FIND FREQUENCY OF SURFACE LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that vertex, divided by the number of input maps.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_probability_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_PROBABILITY_METADATA)
    execution.params(params)
    args = label_probability_cargs(params, execution)
    out = label_probability_outputs(params, execution)
    execution.run(args)
    return out

def label_probability(
    label_maps: InputPathType,
    probability_metric_out: str,
    exclude_unlabeled: bool = False,
    runner: Runner | None = None,
) -> LabelProbabilityOutputs:
    """
    FIND FREQUENCY OF SURFACE LABELS

    This command outputs a set of soft ROIs, one for each label in the input, where the value is how many of the input maps had that label at that vertex, divided by the number of input maps.

    Args:
        label_maps: label file containing individual label maps from many subjects
        probability_metric_out: the relative frequencies of each label at each\
            vertex
        exclude_unlabeled: don't make a probability map of the unlabeled key
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_probability_params(
        label_maps=label_maps,
        probability_metric_out=probability_metric_out,
        exclude_unlabeled=exclude_unlabeled,
    )
    return label_probability_execute(params, runner)

__all__ = [
    "LabelProbability",
    "LabelProbabilityOutputs",
    "LABEL_PROBABILITY_METADATA",
    "label_probability_cargs",
    "label_probability_outputs",
    "label_probability_params",
    "label_probability_execute",
    "label_probability_validate",
    "label_probability",
]