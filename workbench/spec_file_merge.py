# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPEC_FILE_MERGE_METADATA = Metadata(
    id="spec_file_merge",
    name="MERGE TWO SPEC FILES INTO ONE",
    package="workbench",
)

SpecFileMerge = typing.TypedDict(
    "SpecFileMerge",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/spec_file_merge"]],
        "spec_1": str,
        "spec_2": str,
        "out_spec": str,
    },
)

@dataclasses.dataclass
class SpecFileMergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def spec_file_merge_params(
    spec_1: str,
    spec_2: str,
    out_spec: str,
) -> SpecFileMerge:
    """
    Build parameters.

    Args:
        spec_1: first spec file to merge
        spec_2: second spec file to merge
        out_spec: output - output spec file

    Returns:
        Parameter dictionary.
    """
    params: SpecFileMerge = {
        "@type": "workbench/spec_file_merge",
        "spec_1": spec_1,
        "spec_2": spec_2,
        "out_spec": out_spec,
    }
    return params

def spec_file_merge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpecFileMerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spec_1", None) is None:
        raise StyxValidationError("`spec_1` must not be None")
    if not isinstance(params["spec_1"], str):
        raise StyxValidationError(f'`spec_1` has the wrong type: Received `{type(params["spec_1"])}` expected `str`')
    if params.get("spec_2", None) is None:
        raise StyxValidationError("`spec_2` must not be None")
    if not isinstance(params["spec_2"], str):
        raise StyxValidationError(f'`spec_2` has the wrong type: Received `{type(params["spec_2"])}` expected `str`')
    if params.get("out_spec", None) is None:
        raise StyxValidationError("`out_spec` must not be None")
    if not isinstance(params["out_spec"], str):
        raise StyxValidationError(f'`out_spec` has the wrong type: Received `{type(params["out_spec"])}` expected `str`')

def spec_file_merge_cargs(params: SpecFileMerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-spec-file-merge")
    cargs.append(params["spec_1"])
    cargs.append(params["spec_2"])
    cargs.append(params["out_spec"])
    return cargs

def spec_file_merge_outputs(params: SpecFileMerge, execution: Execution) -> SpecFileMergeOutputs:
    """Build the SpecFileMergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SpecFileMergeOutputs(
        root=root_v,
    )

def spec_file_merge_execute(params: SpecFileMerge, runner: Runner | None = None) -> SpecFileMergeOutputs:
    """
    MERGE TWO SPEC FILES INTO ONE

    The output spec file contains every file that is in either of the input spec files.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spec_file_merge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPEC_FILE_MERGE_METADATA)
    execution.params(params)
    args = spec_file_merge_cargs(params, execution)
    out = spec_file_merge_outputs(params, execution)
    execution.run(args)
    return out

def spec_file_merge(
    spec_1: str,
    spec_2: str,
    out_spec: str,
    runner: Runner | None = None,
) -> SpecFileMergeOutputs:
    """
    MERGE TWO SPEC FILES INTO ONE

    The output spec file contains every file that is in either of the input spec files.

    Args:
        spec_1: first spec file to merge
        spec_2: second spec file to merge
        out_spec: output - output spec file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spec_file_merge_params(
        spec_1=spec_1,
        spec_2=spec_2,
        out_spec=out_spec,
    )
    return spec_file_merge_execute(params, runner)

__all__ = [
    "SpecFileMerge",
    "SpecFileMergeOutputs",
    "SPEC_FILE_MERGE_METADATA",
    "spec_file_merge_cargs",
    "spec_file_merge_outputs",
    "spec_file_merge_params",
    "spec_file_merge_execute",
    "spec_file_merge_validate",
    "spec_file_merge",
]