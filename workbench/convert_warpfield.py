# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONVERT_WARPFIELD_METADATA = Metadata(
    id="convert_warpfield",
    name="CONVERT A WARPFIELD BETWEEN CONVENTIONS",
    package="workbench",
)

class ConvertWarpfieldFromWorld(typing.TypedDict):
    input: str
    """the input warpfield"""
    absolute: typing.NotRequired[bool]
    """warpfield was written in absolute convention, rather than relative"""

class ConvertWarpfieldFromFnirt(typing.TypedDict):
    input: str
    """the input warpfield"""
    source_volume: str
    """the source volume used when generating the input warpfield"""
    absolute: typing.NotRequired[bool]
    """warpfield was written in absolute convention, rather than relative"""

class ConvertWarpfieldToFnirt(typing.TypedDict):
    output: str
    """output - the output warpfield"""
    source_volume: str
    """the volume you want to apply the warpfield to"""

ConvertWarpfield = typing.TypedDict(
    "ConvertWarpfield",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/convert_warpfield"]],
        "from_world": typing.NotRequired[ConvertWarpfieldFromWorld],
        "from_itk": typing.NotRequired[str],
        "from_fnirt": typing.NotRequired[ConvertWarpfieldFromFnirt],
        "to_world": typing.NotRequired[str],
        "to_itk": typing.NotRequired[str],
        "to_fnirt": list[ConvertWarpfieldToFnirt],
    },
)

@dataclasses.dataclass
class ConvertWarpfieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def convert_warpfield_params(
    to_fnirt: list[ConvertWarpfieldToFnirt],
    from_world: ConvertWarpfieldFromWorld | None = None,
    from_itk: str | None = None,
    from_fnirt: ConvertWarpfieldFromFnirt | None = None,
    to_world: str | None = None,
    to_itk: str | None = None,
) -> ConvertWarpfield:
    """
    Build parameters.

    Args:
        to_fnirt: write output as a fnirt warpfield
        from_world: input is a NIFTI 'world' warpfield
        from_itk: input is an ITK warpfield
        from_fnirt: input is a fnirt warpfield
        to_world: write output as a NIFTI 'world' warpfield
        to_itk: write output as an ITK warpfield

    Returns:
        Parameter dictionary.
    """
    params: ConvertWarpfield = {
        "@type": "workbench/convert_warpfield",
        "to_fnirt": to_fnirt,
    }
    if from_world is not None:
        params["from_world"] = from_world
    if from_itk is not None:
        params["from_itk"] = from_itk
    if from_fnirt is not None:
        params["from_fnirt"] = from_fnirt
    if to_world is not None:
        params["to_world"] = to_world
    if to_itk is not None:
        params["to_itk"] = to_itk
    return params

def convert_warpfield_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ConvertWarpfield."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("from_world", None) is not None:
        if not isinstance(params["from_world"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["from_world"])}\'')
        if params["from_world"].get("input", None) is None:
            raise StyxValidationError("`input` must not be None")
        if not isinstance(params["from_world"]["input"], str):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["from_world"]["input"])}` expected `str`')
        if params["from_world"].get("absolute", None) is not None:
            if not isinstance(params["from_world"]["absolute"], bool):
                raise StyxValidationError(f'`absolute` has the wrong type: Received `{type(params["from_world"]["absolute"])}` expected `bool`')
    if params.get("from_itk", None) is not None:
        if not isinstance(params["from_itk"], str):
            raise StyxValidationError(f'`from_itk` has the wrong type: Received `{type(params["from_itk"])}` expected `str`')
    if params.get("from_fnirt", None) is not None:
        if not isinstance(params["from_fnirt"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["from_fnirt"])}\'')
        if params["from_fnirt"].get("input", None) is None:
            raise StyxValidationError("`input` must not be None")
        if not isinstance(params["from_fnirt"]["input"], str):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["from_fnirt"]["input"])}` expected `str`')
        if params["from_fnirt"].get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(params["from_fnirt"]["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(params["from_fnirt"]["source_volume"])}` expected `str`')
        if params["from_fnirt"].get("absolute", None) is not None:
            if not isinstance(params["from_fnirt"]["absolute"], bool):
                raise StyxValidationError(f'`absolute` has the wrong type: Received `{type(params["from_fnirt"]["absolute"])}` expected `bool`')
    if params.get("to_world", None) is not None:
        if not isinstance(params["to_world"], str):
            raise StyxValidationError(f'`to_world` has the wrong type: Received `{type(params["to_world"])}` expected `str`')
    if params.get("to_itk", None) is not None:
        if not isinstance(params["to_itk"], str):
            raise StyxValidationError(f'`to_itk` has the wrong type: Received `{type(params["to_itk"])}` expected `str`')
    if params.get("to_fnirt", None) is None:
        raise StyxValidationError("`to_fnirt` must not be None")
    if not isinstance(params["to_fnirt"], list):
        raise StyxValidationError(f'`to_fnirt` has the wrong type: Received `{type(params["to_fnirt"])}` expected `list[ConvertWarpfieldToFnirt]`')
    for e in params["to_fnirt"]:
        if not isinstance(e, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
        if e.get("output", None) is None:
            raise StyxValidationError("`output` must not be None")
        if not isinstance(e["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(e["output"])}` expected `str`')
        if e.get("source_volume", None) is None:
            raise StyxValidationError("`source_volume` must not be None")
        if not isinstance(e["source_volume"], str):
            raise StyxValidationError(f'`source_volume` has the wrong type: Received `{type(e["source_volume"])}` expected `str`')

def convert_warpfield_cargs(params: ConvertWarpfield, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-convert-warpfield")
    v_0 = params.get("from_world")
    if v_0 is not None:
        cargs.append("-from-world")
        cargs.append(v_0["input"])
        if v_0.get("absolute"):
            cargs.append("-absolute")
    v_1 = params.get("from_itk")
    if v_1 is not None:
        cargs.append("-from-itk")
        cargs.append(v_1)
    v_2 = params.get("from_fnirt")
    if v_2 is not None:
        cargs.append("-from-fnirt")
        cargs.append(v_2["input"])
        cargs.append(v_2["source_volume"])
        if v_2.get("absolute"):
            cargs.append("-absolute")
    v_3 = params.get("to_world")
    if v_3 is not None:
        cargs.append("-to-world")
        cargs.append(v_3)
    v_4 = params.get("to_itk")
    if v_4 is not None:
        cargs.append("-to-itk")
        cargs.append(v_4)
    for item0 in params["to_fnirt"]:
        cargs.append("-to-fnirt")
        cargs.append(item0["output"])
        cargs.append(item0["source_volume"])
    return cargs

def convert_warpfield_outputs(params: ConvertWarpfield, execution: Execution) -> ConvertWarpfieldOutputs:
    """Build the ConvertWarpfieldOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ConvertWarpfieldOutputs(
        root=root_v,
    )

def convert_warpfield_execute(params: ConvertWarpfield, runner: Runner | None = None) -> ConvertWarpfieldOutputs:
    """
    CONVERT A WARPFIELD BETWEEN CONVENTIONS

    NIFTI world warpfields can be used directly on mm coordinates via sampling the three subvolumes at the coordinate and adding the sampled values to the coordinate vector.  They use the NIFTI coordinate system, that is, X is left to right, Y is posterior to anterior, and Z is inferior to superior.

NOTE: this command does not invert the warpfield, and to warp a surface, you must use the inverse of the warpfield that warps the corresponding volume.

The ITK format is used by ANTS.

You must specify exactly one -from option, but you may specify multiple -to options, and -to-fnirt may be specified more than once.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    convert_warpfield_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONVERT_WARPFIELD_METADATA)
    execution.params(params)
    args = convert_warpfield_cargs(params, execution)
    out = convert_warpfield_outputs(params, execution)
    execution.run(args)
    return out

def convert_warpfield(
    to_fnirt: list[ConvertWarpfieldToFnirt],
    from_world: ConvertWarpfieldFromWorld | None = None,
    from_itk: str | None = None,
    from_fnirt: ConvertWarpfieldFromFnirt | None = None,
    to_world: str | None = None,
    to_itk: str | None = None,
    runner: Runner | None = None,
) -> ConvertWarpfieldOutputs:
    """
    CONVERT A WARPFIELD BETWEEN CONVENTIONS

    NIFTI world warpfields can be used directly on mm coordinates via sampling the three subvolumes at the coordinate and adding the sampled values to the coordinate vector.  They use the NIFTI coordinate system, that is, X is left to right, Y is posterior to anterior, and Z is inferior to superior.

NOTE: this command does not invert the warpfield, and to warp a surface, you must use the inverse of the warpfield that warps the corresponding volume.

The ITK format is used by ANTS.

You must specify exactly one -from option, but you may specify multiple -to options, and -to-fnirt may be specified more than once.

    Args:
        to_fnirt: write output as a fnirt warpfield
        from_world: input is a NIFTI 'world' warpfield
        from_itk: input is an ITK warpfield
        from_fnirt: input is a fnirt warpfield
        to_world: write output as a NIFTI 'world' warpfield
        to_itk: write output as an ITK warpfield
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = convert_warpfield_params(
        to_fnirt=to_fnirt,
        from_world=from_world,
        from_itk=from_itk,
        from_fnirt=from_fnirt,
        to_world=to_world,
        to_itk=to_itk,
    )
    return convert_warpfield_execute(params, runner)

__all__ = [
    "ConvertWarpfield",
    "ConvertWarpfieldOutputs",
    "CONVERT_WARPFIELD_METADATA",
    "convert_warpfield_cargs",
    "convert_warpfield_outputs",
    "convert_warpfield_params",
    "convert_warpfield_execute",
    "convert_warpfield_validate",
    "convert_warpfield",
]