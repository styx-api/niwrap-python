# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_PARCEL_RESAMPLING_GENERIC_METADATA = Metadata(
    id="volume_parcel_resampling_generic",
    name="SMOOTH AND RESAMPLE VOLUME PARCELS FROM DIFFERENT VOLUME SPACE",
    package="workbench",
)

VolumeParcelResamplingGeneric = typing.TypedDict(
    "VolumeParcelResamplingGeneric",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_parcel_resampling_generic"]],
        "volume_in": InputPathType,
        "cur_parcels": InputPathType,
        "new_parcels": InputPathType,
        "kernel": float,
        "volume_out": str,
        "fwhm": typing.NotRequired[bool],
        "fix_zeros": typing.NotRequired[bool],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeParcelResamplingGenericOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """output volume"""

def volume_parcel_resampling_generic_params(
    volume_in: InputPathType,
    cur_parcels: InputPathType,
    new_parcels: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    fix_zeros: bool = False,
    subvolume: str | None = None,
) -> VolumeParcelResamplingGeneric:
    """
    Build parameters.

    Args:
        volume_in: the input data volume
        cur_parcels: label volume of where the parcels currently are
        new_parcels: label volume of where the parcels should be
        kernel: gaussian kernel size in mm to smooth by during resampling, as sigma\
            by default
        volume_out: output volume
        fwhm: smoothing kernel size is FWHM, not sigma
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume as input

    Returns:
        Parameter dictionary.
    """
    params: VolumeParcelResamplingGeneric = {
        "@type": "workbench/volume_parcel_resampling_generic",
        "volume_in": volume_in,
        "cur_parcels": cur_parcels,
        "new_parcels": new_parcels,
        "kernel": kernel,
        "volume_out": volume_out,
        "fwhm": fwhm,
        "fix_zeros": fix_zeros,
    }
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_parcel_resampling_generic_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeParcelResamplingGeneric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("cur_parcels", None) is None:
        raise StyxValidationError("`cur_parcels` must not be None")
    if not isinstance(params["cur_parcels"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cur_parcels` has the wrong type: Received `{type(params["cur_parcels"])}` expected `InputPathType`')
    if params.get("new_parcels", None) is None:
        raise StyxValidationError("`new_parcels` must not be None")
    if not isinstance(params["new_parcels"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_parcels` has the wrong type: Received `{type(params["new_parcels"])}` expected `InputPathType`')
    if params.get("kernel", None) is None:
        raise StyxValidationError("`kernel` must not be None")
    if not isinstance(params["kernel"], (float, int)):
        raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["kernel"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], bool):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `bool`')
    if params.get("fix_zeros", None) is not None:
        if not isinstance(params["fix_zeros"], bool):
            raise StyxValidationError(f'`fix_zeros` has the wrong type: Received `{type(params["fix_zeros"])}` expected `bool`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_parcel_resampling_generic_cargs(params: VolumeParcelResamplingGeneric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-parcel-resampling-generic")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(execution.input_file(params["cur_parcels"]))
    cargs.append(execution.input_file(params["new_parcels"]))
    cargs.append(str(params["kernel"]))
    cargs.append(params["volume_out"])
    if params.get("fwhm"):
        cargs.append("-fwhm")
    if params.get("fix_zeros"):
        cargs.append("-fix-zeros")
    v_0 = params.get("subvolume")
    if v_0 is not None:
        cargs.append("-subvolume")
        cargs.append(v_0)
    return cargs

def volume_parcel_resampling_generic_outputs(params: VolumeParcelResamplingGeneric, execution: Execution) -> VolumeParcelResamplingGenericOutputs:
    """Build the VolumeParcelResamplingGenericOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeParcelResamplingGenericOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_parcel_resampling_generic_execute(params: VolumeParcelResamplingGeneric, runner: Runner | None = None) -> VolumeParcelResamplingGenericOutputs:
    """
    SMOOTH AND RESAMPLE VOLUME PARCELS FROM DIFFERENT VOLUME SPACE

    Smooths and resamples the region inside each label in cur-parcels to the region of the same label name in new-parcels.  Any voxels in the output label region but outside the input label region will be extrapolated from nearby data.  The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the voxel, and after smoothing is complete, it will check for any remaining values of zero, and fill them in with extrapolated values.  The output volume will use the volume space of new-parcels, which does not need to be in the same volume space as the input.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_parcel_resampling_generic_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_PARCEL_RESAMPLING_GENERIC_METADATA)
    execution.params(params)
    args = volume_parcel_resampling_generic_cargs(params, execution)
    out = volume_parcel_resampling_generic_outputs(params, execution)
    execution.run(args)
    return out

def volume_parcel_resampling_generic(
    volume_in: InputPathType,
    cur_parcels: InputPathType,
    new_parcels: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    fix_zeros: bool = False,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeParcelResamplingGenericOutputs:
    """
    SMOOTH AND RESAMPLE VOLUME PARCELS FROM DIFFERENT VOLUME SPACE

    Smooths and resamples the region inside each label in cur-parcels to the region of the same label name in new-parcels.  Any voxels in the output label region but outside the input label region will be extrapolated from nearby data.  The -fix-zeros option causes the smoothing to not use an input value if it is zero, but still write a smoothed value to the voxel, and after smoothing is complete, it will check for any remaining values of zero, and fill them in with extrapolated values.  The output volume will use the volume space of new-parcels, which does not need to be in the same volume space as the input.

    Args:
        volume_in: the input data volume
        cur_parcels: label volume of where the parcels currently are
        new_parcels: label volume of where the parcels should be
        kernel: gaussian kernel size in mm to smooth by during resampling, as sigma\
            by default
        volume_out: output volume
        fwhm: smoothing kernel size is FWHM, not sigma
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume as input
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_parcel_resampling_generic_params(
        volume_in=volume_in,
        cur_parcels=cur_parcels,
        new_parcels=new_parcels,
        kernel=kernel,
        volume_out=volume_out,
        fwhm=fwhm,
        fix_zeros=fix_zeros,
        subvolume=subvolume,
    )
    return volume_parcel_resampling_generic_execute(params, runner)

__all__ = [
    "VolumeParcelResamplingGeneric",
    "VolumeParcelResamplingGenericOutputs",
    "VOLUME_PARCEL_RESAMPLING_GENERIC_METADATA",
    "volume_parcel_resampling_generic_cargs",
    "volume_parcel_resampling_generic_outputs",
    "volume_parcel_resampling_generic_params",
    "volume_parcel_resampling_generic_execute",
    "volume_parcel_resampling_generic_validate",
    "volume_parcel_resampling_generic",
]