# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_MATCH_METADATA = Metadata(
    id="surface_match",
    name="SURFACE MATCH",
    package="workbench",
)

SurfaceMatch = typing.TypedDict(
    "SurfaceMatch",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_match"]],
        "match_surface_file": InputPathType,
        "input_surface_file": InputPathType,
        "output_surface_name": str,
    },
)

@dataclasses.dataclass
class SurfaceMatchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def surface_match_params(
    match_surface_file: InputPathType,
    input_surface_file: InputPathType,
    output_surface_name: str,
) -> SurfaceMatch:
    """
    Build parameters.

    Args:
        match_surface_file: Match (Reference) Surface
        input_surface_file: File containing surface that will be transformed
        output_surface_name: Surface File after transformation

    Returns:
        Parameter dictionary.
    """
    params: SurfaceMatch = {
        "@type": "workbench/surface_match",
        "match_surface_file": match_surface_file,
        "input_surface_file": input_surface_file,
        "output_surface_name": output_surface_name,
    }
    return params

def surface_match_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceMatch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("match_surface_file", None) is None:
        raise StyxValidationError("`match_surface_file` must not be None")
    if not isinstance(params["match_surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`match_surface_file` has the wrong type: Received `{type(params["match_surface_file"])}` expected `InputPathType`')
    if params.get("input_surface_file", None) is None:
        raise StyxValidationError("`input_surface_file` must not be None")
    if not isinstance(params["input_surface_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface_file` has the wrong type: Received `{type(params["input_surface_file"])}` expected `InputPathType`')
    if params.get("output_surface_name", None) is None:
        raise StyxValidationError("`output_surface_name` must not be None")
    if not isinstance(params["output_surface_name"], str):
        raise StyxValidationError(f'`output_surface_name` has the wrong type: Received `{type(params["output_surface_name"])}` expected `str`')

def surface_match_cargs(params: SurfaceMatch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-match")
    cargs.append(execution.input_file(params["match_surface_file"]))
    cargs.append(execution.input_file(params["input_surface_file"]))
    cargs.append(params["output_surface_name"])
    return cargs

def surface_match_outputs(params: SurfaceMatch, execution: Execution) -> SurfaceMatchOutputs:
    """Build the SurfaceMatchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SurfaceMatchOutputs(
        root=root_v,
    )

def surface_match_execute(params: SurfaceMatch, runner: Runner | None = None) -> SurfaceMatchOutputs:
    """
    SURFACE MATCH

    The Input Surface File will be transformed so that its coordinate ranges (bounding box) match that of the Match Surface File

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_match_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_MATCH_METADATA)
    execution.params(params)
    args = surface_match_cargs(params, execution)
    out = surface_match_outputs(params, execution)
    execution.run(args)
    return out

def surface_match(
    match_surface_file: InputPathType,
    input_surface_file: InputPathType,
    output_surface_name: str,
    runner: Runner | None = None,
) -> SurfaceMatchOutputs:
    """
    SURFACE MATCH

    The Input Surface File will be transformed so that its coordinate ranges (bounding box) match that of the Match Surface File

    Args:
        match_surface_file: Match (Reference) Surface
        input_surface_file: File containing surface that will be transformed
        output_surface_name: Surface File after transformation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_match_params(
        match_surface_file=match_surface_file,
        input_surface_file=input_surface_file,
        output_surface_name=output_surface_name,
    )
    return surface_match_execute(params, runner)

__all__ = [
    "SurfaceMatch",
    "SurfaceMatchOutputs",
    "SURFACE_MATCH_METADATA",
    "surface_match_cargs",
    "surface_match_outputs",
    "surface_match_params",
    "surface_match_execute",
    "surface_match_validate",
    "surface_match",
]