# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_REDUCE_METADATA = Metadata(
    id="volume_reduce",
    name="PERFORM REDUCTION OPERATION ACROSS SUBVOLUMES",
    package="workbench",
)

class VolumeReduceExcludeOutliers(typing.TypedDict):
    sigma_below: float
    """number of standard deviations below the mean to include"""
    sigma_above: float
    """number of standard deviations above the mean to include"""

VolumeReduce = typing.TypedDict(
    "VolumeReduce",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_reduce"]],
        "volume_in": InputPathType,
        "operation": str,
        "volume_out": str,
        "exclude_outliers": typing.NotRequired[VolumeReduceExcludeOutliers],
        "only_numeric": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VolumeReduceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_reduce_params(
    volume_in: InputPathType,
    operation: str,
    volume_out: str,
    exclude_outliers: VolumeReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
) -> VolumeReduce:
    """
    Build parameters.

    Args:
        volume_in: the volume file to reduce
        operation: the reduction operator to use
        volume_out: the output volume
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values

    Returns:
        Parameter dictionary.
    """
    params: VolumeReduce = {
        "@type": "workbench/volume_reduce",
        "volume_in": volume_in,
        "operation": operation,
        "volume_out": volume_out,
        "only_numeric": only_numeric,
    }
    if exclude_outliers is not None:
        params["exclude_outliers"] = exclude_outliers
    return params

def volume_reduce_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeReduce."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume_in", None) is None:
        raise StyxValidationError("`volume_in` must not be None")
    if not isinstance(params["volume_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume_in` has the wrong type: Received `{type(params["volume_in"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("exclude_outliers", None) is not None:
        if not isinstance(params["exclude_outliers"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["exclude_outliers"])}\'')
        if params["exclude_outliers"].get("sigma_below", None) is None:
            raise StyxValidationError("`sigma_below` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_below"], (float, int)):
            raise StyxValidationError(f'`sigma_below` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_below"])}` expected `float`')
        if params["exclude_outliers"].get("sigma_above", None) is None:
            raise StyxValidationError("`sigma_above` must not be None")
        if not isinstance(params["exclude_outliers"]["sigma_above"], (float, int)):
            raise StyxValidationError(f'`sigma_above` has the wrong type: Received `{type(params["exclude_outliers"]["sigma_above"])}` expected `float`')
    if params.get("only_numeric", None) is not None:
        if not isinstance(params["only_numeric"], bool):
            raise StyxValidationError(f'`only_numeric` has the wrong type: Received `{type(params["only_numeric"])}` expected `bool`')

def volume_reduce_cargs(params: VolumeReduce, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-reduce")
    cargs.append(execution.input_file(params["volume_in"]))
    cargs.append(params["operation"])
    cargs.append(params["volume_out"])
    v_0 = params.get("exclude_outliers")
    if v_0 is not None:
        cargs.append("-exclude-outliers")
        cargs.append(str(v_0["sigma_below"]))
        cargs.append(str(v_0["sigma_above"]))
    if params.get("only_numeric"):
        cargs.append("-only-numeric")
    return cargs

def volume_reduce_outputs(params: VolumeReduce, execution: Execution) -> VolumeReduceOutputs:
    """Build the VolumeReduceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeReduceOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_reduce_execute(params: VolumeReduce, runner: Runner | None = None) -> VolumeReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ACROSS SUBVOLUMES

    For each voxel, takes the data across subvolumes as a vector, and performs the specified reduction on it, putting the result into the single output volume at that voxel.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_reduce_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_REDUCE_METADATA)
    execution.params(params)
    args = volume_reduce_cargs(params, execution)
    out = volume_reduce_outputs(params, execution)
    execution.run(args)
    return out

def volume_reduce(
    volume_in: InputPathType,
    operation: str,
    volume_out: str,
    exclude_outliers: VolumeReduceExcludeOutliers | None = None,
    only_numeric: bool = False,
    runner: Runner | None = None,
) -> VolumeReduceOutputs:
    """
    PERFORM REDUCTION OPERATION ACROSS SUBVOLUMES

    For each voxel, takes the data across subvolumes as a vector, and performs the specified reduction on it, putting the result into the single output volume at that voxel.  The reduction operators are as follows:

MAX: the maximum value
MIN: the minimum value
INDEXMAX: the 1-based index of the maximum value
INDEXMIN: the 1-based index of the minimum value
SUM: add all values
PRODUCT: multiply all values
MEAN: the mean of the data
STDEV: the standard deviation (N denominator)
SAMPSTDEV: the sample standard deviation (N-1 denominator)
VARIANCE: the variance of the data
TSNR: mean divided by sample standard deviation (N-1 denominator)
COV: sample standard deviation (N-1 denominator) divided by mean
L2NORM: square root of sum of squares
MEDIAN: the median of the data
MODE: the mode of the data
COUNT_NONZERO: the number of nonzero elements in the data


    Args:
        volume_in: the volume file to reduce
        operation: the reduction operator to use
        volume_out: the output volume
        exclude_outliers: exclude non-numeric values and outliers by standard\
            deviation
        only_numeric: exclude non-numeric values
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_reduce_params(
        volume_in=volume_in,
        operation=operation,
        volume_out=volume_out,
        exclude_outliers=exclude_outliers,
        only_numeric=only_numeric,
    )
    return volume_reduce_execute(params, runner)

__all__ = [
    "VolumeReduce",
    "VolumeReduceOutputs",
    "VOLUME_REDUCE_METADATA",
    "volume_reduce_cargs",
    "volume_reduce_outputs",
    "volume_reduce_params",
    "volume_reduce_execute",
    "volume_reduce_validate",
    "volume_reduce",
]