# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BORDER_EXPORT_COLOR_TABLE_METADATA = Metadata(
    id="border_export_color_table",
    name="WRITE BORDER NAMES AND COLORS AS TEXT",
    package="workbench",
)

BorderExportColorTable = typing.TypedDict(
    "BorderExportColorTable",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/border_export_color_table"]],
        "border_file": InputPathType,
        "table_out": str,
        "class_colors": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BorderExportColorTableOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def border_export_color_table_params(
    border_file: InputPathType,
    table_out: str,
    class_colors: bool = False,
) -> BorderExportColorTable:
    """
    Build parameters.

    Args:
        border_file: the input border file
        table_out: output - the output text file
        class_colors: use class colors instead of the name colors

    Returns:
        Parameter dictionary.
    """
    params: BorderExportColorTable = {
        "@type": "workbench/border_export_color_table",
        "border_file": border_file,
        "table_out": table_out,
        "class_colors": class_colors,
    }
    return params

def border_export_color_table_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BorderExportColorTable."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("border_file", None) is None:
        raise StyxValidationError("`border_file` must not be None")
    if not isinstance(params["border_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`border_file` has the wrong type: Received `{type(params["border_file"])}` expected `InputPathType`')
    if params.get("table_out", None) is None:
        raise StyxValidationError("`table_out` must not be None")
    if not isinstance(params["table_out"], str):
        raise StyxValidationError(f'`table_out` has the wrong type: Received `{type(params["table_out"])}` expected `str`')
    if params.get("class_colors", None) is not None:
        if not isinstance(params["class_colors"], bool):
            raise StyxValidationError(f'`class_colors` has the wrong type: Received `{type(params["class_colors"])}` expected `bool`')

def border_export_color_table_cargs(params: BorderExportColorTable, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-border-export-color-table")
    cargs.append(execution.input_file(params["border_file"]))
    cargs.append(params["table_out"])
    if params.get("class_colors"):
        cargs.append("-class-colors")
    return cargs

def border_export_color_table_outputs(params: BorderExportColorTable, execution: Execution) -> BorderExportColorTableOutputs:
    """Build the BorderExportColorTableOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BorderExportColorTableOutputs(
        root=root_v,
    )

def border_export_color_table_execute(params: BorderExportColorTable, runner: Runner | None = None) -> BorderExportColorTableOutputs:
    """
    WRITE BORDER NAMES AND COLORS AS TEXT

    Takes the names and colors of each border, and writes it to the same format as -metric-label-import expects.  By default, the borders are colored by border name, specify -class-colors to color them by class instead.  The key values start at 1 and follow the order of the borders in the file.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    border_export_color_table_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BORDER_EXPORT_COLOR_TABLE_METADATA)
    execution.params(params)
    args = border_export_color_table_cargs(params, execution)
    out = border_export_color_table_outputs(params, execution)
    execution.run(args)
    return out

def border_export_color_table(
    border_file: InputPathType,
    table_out: str,
    class_colors: bool = False,
    runner: Runner | None = None,
) -> BorderExportColorTableOutputs:
    """
    WRITE BORDER NAMES AND COLORS AS TEXT

    Takes the names and colors of each border, and writes it to the same format as -metric-label-import expects.  By default, the borders are colored by border name, specify -class-colors to color them by class instead.  The key values start at 1 and follow the order of the borders in the file.

    Args:
        border_file: the input border file
        table_out: output - the output text file
        class_colors: use class colors instead of the name colors
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = border_export_color_table_params(
        border_file=border_file,
        table_out=table_out,
        class_colors=class_colors,
    )
    return border_export_color_table_execute(params, runner)

__all__ = [
    "BorderExportColorTable",
    "BorderExportColorTableOutputs",
    "BORDER_EXPORT_COLOR_TABLE_METADATA",
    "border_export_color_table_cargs",
    "border_export_color_table_outputs",
    "border_export_color_table_params",
    "border_export_color_table_execute",
    "border_export_color_table_validate",
    "border_export_color_table",
]