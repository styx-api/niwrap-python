# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPEC_FILE_RELOCATE_METADATA = Metadata(
    id="spec_file_relocate",
    name="RECREATE SPEC FILE IN NEW LOCATION",
    package="workbench",
)

SpecFileRelocate = typing.TypedDict(
    "SpecFileRelocate",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/spec_file_relocate"]],
        "input_spec": str,
        "output_spec": str,
    },
)

@dataclasses.dataclass
class SpecFileRelocateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def spec_file_relocate_params(
    input_spec: str,
    output_spec: str,
) -> SpecFileRelocate:
    """
    Build parameters.

    Args:
        input_spec: the spec file to use
        output_spec: output - the new spec file to create

    Returns:
        Parameter dictionary.
    """
    params: SpecFileRelocate = {
        "@type": "workbench/spec_file_relocate",
        "input_spec": input_spec,
        "output_spec": output_spec,
    }
    return params

def spec_file_relocate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpecFileRelocate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_spec", None) is None:
        raise StyxValidationError("`input_spec` must not be None")
    if not isinstance(params["input_spec"], str):
        raise StyxValidationError(f'`input_spec` has the wrong type: Received `{type(params["input_spec"])}` expected `str`')
    if params.get("output_spec", None) is None:
        raise StyxValidationError("`output_spec` must not be None")
    if not isinstance(params["output_spec"], str):
        raise StyxValidationError(f'`output_spec` has the wrong type: Received `{type(params["output_spec"])}` expected `str`')

def spec_file_relocate_cargs(params: SpecFileRelocate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-spec-file-relocate")
    cargs.append(params["input_spec"])
    cargs.append(params["output_spec"])
    return cargs

def spec_file_relocate_outputs(params: SpecFileRelocate, execution: Execution) -> SpecFileRelocateOutputs:
    """Build the SpecFileRelocateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SpecFileRelocateOutputs(
        root=root_v,
    )

def spec_file_relocate_execute(params: SpecFileRelocate, runner: Runner | None = None) -> SpecFileRelocateOutputs:
    """
    RECREATE SPEC FILE IN NEW LOCATION

    Spec files contain internal relative paths, such that moving or copying a spec file will cause it to lose track of the files it refers to.  This command makes a modified copy of the spec file, changing the relative paths to refer to the new relative locations of the files.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spec_file_relocate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPEC_FILE_RELOCATE_METADATA)
    execution.params(params)
    args = spec_file_relocate_cargs(params, execution)
    out = spec_file_relocate_outputs(params, execution)
    execution.run(args)
    return out

def spec_file_relocate(
    input_spec: str,
    output_spec: str,
    runner: Runner | None = None,
) -> SpecFileRelocateOutputs:
    """
    RECREATE SPEC FILE IN NEW LOCATION

    Spec files contain internal relative paths, such that moving or copying a spec file will cause it to lose track of the files it refers to.  This command makes a modified copy of the spec file, changing the relative paths to refer to the new relative locations of the files.

    Args:
        input_spec: the spec file to use
        output_spec: output - the new spec file to create
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spec_file_relocate_params(
        input_spec=input_spec,
        output_spec=output_spec,
    )
    return spec_file_relocate_execute(params, runner)

__all__ = [
    "SpecFileRelocate",
    "SpecFileRelocateOutputs",
    "SPEC_FILE_RELOCATE_METADATA",
    "spec_file_relocate_cargs",
    "spec_file_relocate_outputs",
    "spec_file_relocate_params",
    "spec_file_relocate_execute",
    "spec_file_relocate_validate",
    "spec_file_relocate",
]