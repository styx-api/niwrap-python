# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOLUME_PARCEL_SMOOTHING_METADATA = Metadata(
    id="volume_parcel_smoothing",
    name="SMOOTH PARCELS IN A VOLUME SEPARATELY",
    package="workbench",
)

VolumeParcelSmoothing = typing.TypedDict(
    "VolumeParcelSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/volume_parcel_smoothing"]],
        "data_volume": InputPathType,
        "label_volume": InputPathType,
        "kernel": float,
        "volume_out": str,
        "fwhm": typing.NotRequired[bool],
        "fix_zeros": typing.NotRequired[bool],
        "subvolume": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolumeParcelSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    volume_out: OutputPathType
    """the output volume"""

def volume_parcel_smoothing_params(
    data_volume: InputPathType,
    label_volume: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    fix_zeros: bool = False,
    subvolume: str | None = None,
) -> VolumeParcelSmoothing:
    """
    Build parameters.

    Args:
        data_volume: the volume to smooth
        label_volume: a label volume containing the parcels to smooth
        kernel: the size of the gaussian smoothing kernel in mm, as sigma by\
            default
        volume_out: the output volume
        fwhm: smoothing kernel size is FWHM, not sigma
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume to smooth

    Returns:
        Parameter dictionary.
    """
    params: VolumeParcelSmoothing = {
        "@type": "workbench/volume_parcel_smoothing",
        "data_volume": data_volume,
        "label_volume": label_volume,
        "kernel": kernel,
        "volume_out": volume_out,
        "fwhm": fwhm,
        "fix_zeros": fix_zeros,
    }
    if subvolume is not None:
        params["subvolume"] = subvolume
    return params

def volume_parcel_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolumeParcelSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("data_volume", None) is None:
        raise StyxValidationError("`data_volume` must not be None")
    if not isinstance(params["data_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`data_volume` has the wrong type: Received `{type(params["data_volume"])}` expected `InputPathType`')
    if params.get("label_volume", None) is None:
        raise StyxValidationError("`label_volume` must not be None")
    if not isinstance(params["label_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_volume` has the wrong type: Received `{type(params["label_volume"])}` expected `InputPathType`')
    if params.get("kernel", None) is None:
        raise StyxValidationError("`kernel` must not be None")
    if not isinstance(params["kernel"], (float, int)):
        raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["kernel"])}` expected `float`')
    if params.get("volume_out", None) is None:
        raise StyxValidationError("`volume_out` must not be None")
    if not isinstance(params["volume_out"], str):
        raise StyxValidationError(f'`volume_out` has the wrong type: Received `{type(params["volume_out"])}` expected `str`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], bool):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `bool`')
    if params.get("fix_zeros", None) is not None:
        if not isinstance(params["fix_zeros"], bool):
            raise StyxValidationError(f'`fix_zeros` has the wrong type: Received `{type(params["fix_zeros"])}` expected `bool`')
    if params.get("subvolume", None) is not None:
        if not isinstance(params["subvolume"], str):
            raise StyxValidationError(f'`subvolume` has the wrong type: Received `{type(params["subvolume"])}` expected `str`')

def volume_parcel_smoothing_cargs(params: VolumeParcelSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-volume-parcel-smoothing")
    cargs.append(execution.input_file(params["data_volume"]))
    cargs.append(execution.input_file(params["label_volume"]))
    cargs.append(str(params["kernel"]))
    cargs.append(params["volume_out"])
    if params.get("fwhm"):
        cargs.append("-fwhm")
    if params.get("fix_zeros"):
        cargs.append("-fix-zeros")
    v_0 = params.get("subvolume")
    if v_0 is not None:
        cargs.append("-subvolume")
        cargs.append(v_0)
    return cargs

def volume_parcel_smoothing_outputs(params: VolumeParcelSmoothing, execution: Execution) -> VolumeParcelSmoothingOutputs:
    """Build the VolumeParcelSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    volume_out_v: OutputPathType = execution.output_file(params["volume_out"])
    return VolumeParcelSmoothingOutputs(
        root=root_v,
        volume_out=volume_out_v,
    )

def volume_parcel_smoothing_execute(params: VolumeParcelSmoothing, runner: Runner | None = None) -> VolumeParcelSmoothingOutputs:
    """
    SMOOTH PARCELS IN A VOLUME SEPARATELY

    The volume is smoothed within each label in the label volume using data only from within the label.  Equivalent to running volume smoothing with ROIs matching each label separately, then adding the resulting volumes, but faster.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    volume_parcel_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOLUME_PARCEL_SMOOTHING_METADATA)
    execution.params(params)
    args = volume_parcel_smoothing_cargs(params, execution)
    out = volume_parcel_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def volume_parcel_smoothing(
    data_volume: InputPathType,
    label_volume: InputPathType,
    kernel: float,
    volume_out: str,
    fwhm: bool = False,
    fix_zeros: bool = False,
    subvolume: str | None = None,
    runner: Runner | None = None,
) -> VolumeParcelSmoothingOutputs:
    """
    SMOOTH PARCELS IN A VOLUME SEPARATELY

    The volume is smoothed within each label in the label volume using data only from within the label.  Equivalent to running volume smoothing with ROIs matching each label separately, then adding the resulting volumes, but faster.

    Args:
        data_volume: the volume to smooth
        label_volume: a label volume containing the parcels to smooth
        kernel: the size of the gaussian smoothing kernel in mm, as sigma by\
            default
        volume_out: the output volume
        fwhm: smoothing kernel size is FWHM, not sigma
        fix_zeros: treat zero values as not being data
        subvolume: select a single subvolume to smooth
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = volume_parcel_smoothing_params(
        data_volume=data_volume,
        label_volume=label_volume,
        kernel=kernel,
        volume_out=volume_out,
        fwhm=fwhm,
        fix_zeros=fix_zeros,
        subvolume=subvolume,
    )
    return volume_parcel_smoothing_execute(params, runner)

__all__ = [
    "VolumeParcelSmoothing",
    "VolumeParcelSmoothingOutputs",
    "VOLUME_PARCEL_SMOOTHING_METADATA",
    "volume_parcel_smoothing_cargs",
    "volume_parcel_smoothing_outputs",
    "volume_parcel_smoothing_params",
    "volume_parcel_smoothing_execute",
    "volume_parcel_smoothing_validate",
    "volume_parcel_smoothing",
]