# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GIFTI_LABEL_TO_ROI_METADATA = Metadata(
    id="gifti_label_to_roi",
    name="MAKE A GIFTI LABEL INTO AN ROI METRIC",
    package="workbench",
)

GiftiLabelToRoi = typing.TypedDict(
    "GiftiLabelToRoi",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/gifti_label_to_roi"]],
        "label_in": InputPathType,
        "metric_out": str,
        "name": typing.NotRequired[str],
        "key": typing.NotRequired[int],
        "map": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class GiftiLabelToRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    metric_out: OutputPathType
    """the output metric file"""

def gifti_label_to_roi_params(
    label_in: InputPathType,
    metric_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
) -> GiftiLabelToRoi:
    """
    Build parameters.

    Args:
        label_in: the input gifti label file
        metric_out: the output metric file
        name: select label by name
        key: select label by key
        map: select a single label map to use

    Returns:
        Parameter dictionary.
    """
    params: GiftiLabelToRoi = {
        "@type": "workbench/gifti_label_to_roi",
        "label_in": label_in,
        "metric_out": metric_out,
    }
    if name is not None:
        params["name"] = name
    if key is not None:
        params["key"] = key
    if map is not None:
        params["map"] = map
    return params

def gifti_label_to_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GiftiLabelToRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("metric_out", None) is None:
        raise StyxValidationError("`metric_out` must not be None")
    if not isinstance(params["metric_out"], str):
        raise StyxValidationError(f'`metric_out` has the wrong type: Received `{type(params["metric_out"])}` expected `str`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("key", None) is not None:
        if not isinstance(params["key"], int):
            raise StyxValidationError(f'`key` has the wrong type: Received `{type(params["key"])}` expected `int`')
    if params.get("map", None) is not None:
        if not isinstance(params["map"], str):
            raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')

def gifti_label_to_roi_cargs(params: GiftiLabelToRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-gifti-label-to-roi")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(params["metric_out"])
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("-name")
        cargs.append(v_0)
    v_1 = params.get("key")
    if v_1 is not None:
        cargs.append("-key")
        cargs.append(str(v_1))
    v_2 = params.get("map")
    if v_2 is not None:
        cargs.append("-map")
        cargs.append(v_2)
    return cargs

def gifti_label_to_roi_outputs(params: GiftiLabelToRoi, execution: Execution) -> GiftiLabelToRoiOutputs:
    """Build the GiftiLabelToRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    metric_out_v: OutputPathType = execution.output_file(params["metric_out"])
    return GiftiLabelToRoiOutputs(
        root=root_v,
        metric_out=metric_out_v,
    )

def gifti_label_to_roi_execute(params: GiftiLabelToRoi, runner: Runner | None = None) -> GiftiLabelToRoiOutputs:
    """
    MAKE A GIFTI LABEL INTO AN ROI METRIC

    For each map in <label-in>, a map is created in <metric-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gifti_label_to_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GIFTI_LABEL_TO_ROI_METADATA)
    execution.params(params)
    args = gifti_label_to_roi_cargs(params, execution)
    out = gifti_label_to_roi_outputs(params, execution)
    execution.run(args)
    return out

def gifti_label_to_roi(
    label_in: InputPathType,
    metric_out: str,
    name: str | None = None,
    key: int | None = None,
    map: str | None = None,
    runner: Runner | None = None,
) -> GiftiLabelToRoiOutputs:
    """
    MAKE A GIFTI LABEL INTO AN ROI METRIC

    For each map in <label-in>, a map is created in <metric-out> where all locations labeled with <label-name> or with a key of <label-key> are given a value of 1, and all other locations are given 0.  Exactly one of -name and -key must be specified.  Specify -map to use only one map from <label-in>.

    Args:
        label_in: the input gifti label file
        metric_out: the output metric file
        name: select label by name
        key: select label by key
        map: select a single label map to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gifti_label_to_roi_params(
        label_in=label_in,
        metric_out=metric_out,
        name=name,
        key=key,
        map=map,
    )
    return gifti_label_to_roi_execute(params, runner)

__all__ = [
    "GiftiLabelToRoi",
    "GiftiLabelToRoiOutputs",
    "GIFTI_LABEL_TO_ROI_METADATA",
    "gifti_label_to_roi_cargs",
    "gifti_label_to_roi_outputs",
    "gifti_label_to_roi_params",
    "gifti_label_to_roi_execute",
    "gifti_label_to_roi_validate",
    "gifti_label_to_roi",
]