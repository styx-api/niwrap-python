# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METRIC_ESTIMATE_FWHM_METADATA = Metadata(
    id="metric_estimate_fwhm",
    name="ESTIMATE FWHM SMOOTHNESS OF A METRIC FILE",
    package="workbench",
)

class MetricEstimateFwhmWholeFile(typing.TypedDict):
    demean: typing.NotRequired[bool]
    """subtract the mean image before estimating smoothness"""

MetricEstimateFwhm = typing.TypedDict(
    "MetricEstimateFwhm",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metric_estimate_fwhm"]],
        "surface": InputPathType,
        "metric_in": InputPathType,
        "roi": typing.NotRequired[InputPathType],
        "column": typing.NotRequired[str],
        "whole_file": typing.NotRequired[MetricEstimateFwhmWholeFile],
    },
)

@dataclasses.dataclass
class MetricEstimateFwhmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def metric_estimate_fwhm_params(
    surface: InputPathType,
    metric_in: InputPathType,
    roi: InputPathType | None = None,
    column: str | None = None,
    whole_file: MetricEstimateFwhmWholeFile | None = None,
) -> MetricEstimateFwhm:
    """
    Build parameters.

    Args:
        surface: the surface to use for distance and neighbor information
        metric_in: the input metric
        roi: use only data within an ROI
        column: select a single column to estimate smoothness of
        whole_file: estimate for the whole file at once, not each column separately

    Returns:
        Parameter dictionary.
    """
    params: MetricEstimateFwhm = {
        "@type": "workbench/metric_estimate_fwhm",
        "surface": surface,
        "metric_in": metric_in,
    }
    if roi is not None:
        params["roi"] = roi
    if column is not None:
        params["column"] = column
    if whole_file is not None:
        params["whole_file"] = whole_file
    return params

def metric_estimate_fwhm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetricEstimateFwhm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("metric_in", None) is None:
        raise StyxValidationError("`metric_in` must not be None")
    if not isinstance(params["metric_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`metric_in` has the wrong type: Received `{type(params["metric_in"])}` expected `InputPathType`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')
    if params.get("whole_file", None) is not None:
        if not isinstance(params["whole_file"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["whole_file"])}\'')
        if params["whole_file"].get("demean", None) is not None:
            if not isinstance(params["whole_file"]["demean"], bool):
                raise StyxValidationError(f'`demean` has the wrong type: Received `{type(params["whole_file"]["demean"])}` expected `bool`')

def metric_estimate_fwhm_cargs(params: MetricEstimateFwhm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metric-estimate-fwhm")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(execution.input_file(params["metric_in"]))
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("column")
    if v_1 is not None:
        cargs.append("-column")
        cargs.append(v_1)
    v_2 = params.get("whole_file")
    if v_2 is not None:
        cargs.append("-whole-file")
        if v_2.get("demean"):
            cargs.append("-demean")
    return cargs

def metric_estimate_fwhm_outputs(params: MetricEstimateFwhm, execution: Execution) -> MetricEstimateFwhmOutputs:
    """Build the MetricEstimateFwhmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MetricEstimateFwhmOutputs(
        root=root_v,
    )

def metric_estimate_fwhm_execute(params: MetricEstimateFwhm, runner: Runner | None = None) -> MetricEstimateFwhmOutputs:
    """
    ESTIMATE FWHM SMOOTHNESS OF A METRIC FILE

    Estimates the smoothness of the metric columns, printing the estimates to standard output.  These estimates ignore variation in vertex spacing.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metric_estimate_fwhm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METRIC_ESTIMATE_FWHM_METADATA)
    execution.params(params)
    args = metric_estimate_fwhm_cargs(params, execution)
    out = metric_estimate_fwhm_outputs(params, execution)
    execution.run(args)
    return out

def metric_estimate_fwhm(
    surface: InputPathType,
    metric_in: InputPathType,
    roi: InputPathType | None = None,
    column: str | None = None,
    whole_file: MetricEstimateFwhmWholeFile | None = None,
    runner: Runner | None = None,
) -> MetricEstimateFwhmOutputs:
    """
    ESTIMATE FWHM SMOOTHNESS OF A METRIC FILE

    Estimates the smoothness of the metric columns, printing the estimates to standard output.  These estimates ignore variation in vertex spacing.

    Args:
        surface: the surface to use for distance and neighbor information
        metric_in: the input metric
        roi: use only data within an ROI
        column: select a single column to estimate smoothness of
        whole_file: estimate for the whole file at once, not each column separately
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metric_estimate_fwhm_params(
        surface=surface,
        metric_in=metric_in,
        roi=roi,
        column=column,
        whole_file=whole_file,
    )
    return metric_estimate_fwhm_execute(params, runner)

__all__ = [
    "MetricEstimateFwhm",
    "MetricEstimateFwhmOutputs",
    "METRIC_ESTIMATE_FWHM_METADATA",
    "metric_estimate_fwhm_cargs",
    "metric_estimate_fwhm_outputs",
    "metric_estimate_fwhm_params",
    "metric_estimate_fwhm_execute",
    "metric_estimate_fwhm_validate",
    "metric_estimate_fwhm",
]