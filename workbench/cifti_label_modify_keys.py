# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CIFTI_LABEL_MODIFY_KEYS_METADATA = Metadata(
    id="cifti_label_modify_keys",
    name="CHANGE KEY VALUES IN A DLABEL FILE",
    package="workbench",
)

CiftiLabelModifyKeys = typing.TypedDict(
    "CiftiLabelModifyKeys",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/cifti_label_modify_keys"]],
        "cifti_in": InputPathType,
        "remap_file": str,
        "cifti_out": str,
        "column": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CiftiLabelModifyKeysOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """the output dlabel file"""

def cifti_label_modify_keys_params(
    cifti_in: InputPathType,
    remap_file: str,
    cifti_out: str,
    column: str | None = None,
) -> CiftiLabelModifyKeys:
    """
    Build parameters.

    Args:
        cifti_in: the input dlabel file
        remap_file: text file with old and new key values
        cifti_out: the output dlabel file
        column: select a single column to use

    Returns:
        Parameter dictionary.
    """
    params: CiftiLabelModifyKeys = {
        "@type": "workbench/cifti_label_modify_keys",
        "cifti_in": cifti_in,
        "remap_file": remap_file,
        "cifti_out": cifti_out,
    }
    if column is not None:
        params["column"] = column
    return params

def cifti_label_modify_keys_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CiftiLabelModifyKeys."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cifti_in", None) is None:
        raise StyxValidationError("`cifti_in` must not be None")
    if not isinstance(params["cifti_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cifti_in` has the wrong type: Received `{type(params["cifti_in"])}` expected `InputPathType`')
    if params.get("remap_file", None) is None:
        raise StyxValidationError("`remap_file` must not be None")
    if not isinstance(params["remap_file"], str):
        raise StyxValidationError(f'`remap_file` has the wrong type: Received `{type(params["remap_file"])}` expected `str`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], str):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `str`')

def cifti_label_modify_keys_cargs(params: CiftiLabelModifyKeys, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-cifti-label-modify-keys")
    cargs.append(execution.input_file(params["cifti_in"]))
    cargs.append(params["remap_file"])
    cargs.append(params["cifti_out"])
    v_0 = params.get("column")
    if v_0 is not None:
        cargs.append("-column")
        cargs.append(v_0)
    return cargs

def cifti_label_modify_keys_outputs(params: CiftiLabelModifyKeys, execution: Execution) -> CiftiLabelModifyKeysOutputs:
    """Build the CiftiLabelModifyKeysOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return CiftiLabelModifyKeysOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def cifti_label_modify_keys_execute(params: CiftiLabelModifyKeys, runner: Runner | None = None) -> CiftiLabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A DLABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cifti_label_modify_keys_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CIFTI_LABEL_MODIFY_KEYS_METADATA)
    execution.params(params)
    args = cifti_label_modify_keys_cargs(params, execution)
    out = cifti_label_modify_keys_outputs(params, execution)
    execution.run(args)
    return out

def cifti_label_modify_keys(
    cifti_in: InputPathType,
    remap_file: str,
    cifti_out: str,
    column: str | None = None,
    runner: Runner | None = None,
) -> CiftiLabelModifyKeysOutputs:
    """
    CHANGE KEY VALUES IN A DLABEL FILE

    <remap-file> should have lines of the form 'oldkey newkey', like so:

3 5
5 8
8 2

This would change the current label with key '3' to use the key '5' instead, 5 would use 8, and 8 would use 2.  Any collision in key values results in the label that was not specified in the remap file getting remapped to an otherwise unused key.  Remapping more than one key to the same new key, or the same key to more than one new key, results in an error.  This will not change the appearance of the file when displayed, as it will change the key values in the data at the same time.

    Args:
        cifti_in: the input dlabel file
        remap_file: text file with old and new key values
        cifti_out: the output dlabel file
        column: select a single column to use
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cifti_label_modify_keys_params(
        cifti_in=cifti_in,
        remap_file=remap_file,
        cifti_out=cifti_out,
        column=column,
    )
    return cifti_label_modify_keys_execute(params, runner)

__all__ = [
    "CiftiLabelModifyKeys",
    "CiftiLabelModifyKeysOutputs",
    "CIFTI_LABEL_MODIFY_KEYS_METADATA",
    "cifti_label_modify_keys_cargs",
    "cifti_label_modify_keys_outputs",
    "cifti_label_modify_keys_params",
    "cifti_label_modify_keys_execute",
    "cifti_label_modify_keys_validate",
    "cifti_label_modify_keys",
]