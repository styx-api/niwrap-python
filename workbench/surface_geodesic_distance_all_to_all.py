# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_GEODESIC_DISTANCE_ALL_TO_ALL_METADATA = Metadata(
    id="surface_geodesic_distance_all_to_all",
    name="COMPUTE GEODESIC DISTANCES FROM ALL VERTICES",
    package="workbench",
)

SurfaceGeodesicDistanceAllToAll = typing.TypedDict(
    "SurfaceGeodesicDistanceAllToAll",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_geodesic_distance_all_to_all"]],
        "surface": InputPathType,
        "cifti_out": str,
        "roi": typing.NotRequired[InputPathType],
        "limit": typing.NotRequired[float],
        "corrected_areas": typing.NotRequired[InputPathType],
        "naive": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SurfaceGeodesicDistanceAllToAllOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cifti_out: OutputPathType
    """single-hemisphere dconn containing the distances"""

def surface_geodesic_distance_all_to_all_params(
    surface: InputPathType,
    cifti_out: str,
    roi: InputPathType | None = None,
    limit: float | None = None,
    corrected_areas: InputPathType | None = None,
    naive: bool = False,
) -> SurfaceGeodesicDistanceAllToAll:
    """
    Build parameters.

    Args:
        surface: the surface to compute on
        cifti_out: single-hemisphere dconn containing the distances
        roi: only output distances for vertices inside an ROI
        limit: stop at a specified distance
        corrected_areas: vertex areas to use to correct the distances on a\
            group-average surface
        naive: use only neighbors, don't crawl triangles (not recommended)

    Returns:
        Parameter dictionary.
    """
    params: SurfaceGeodesicDistanceAllToAll = {
        "@type": "workbench/surface_geodesic_distance_all_to_all",
        "surface": surface,
        "cifti_out": cifti_out,
        "naive": naive,
    }
    if roi is not None:
        params["roi"] = roi
    if limit is not None:
        params["limit"] = limit
    if corrected_areas is not None:
        params["corrected_areas"] = corrected_areas
    return params

def surface_geodesic_distance_all_to_all_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceGeodesicDistanceAllToAll."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')
    if params.get("cifti_out", None) is None:
        raise StyxValidationError("`cifti_out` must not be None")
    if not isinstance(params["cifti_out"], str):
        raise StyxValidationError(f'`cifti_out` has the wrong type: Received `{type(params["cifti_out"])}` expected `str`')
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("limit", None) is not None:
        if not isinstance(params["limit"], (float, int)):
            raise StyxValidationError(f'`limit` has the wrong type: Received `{type(params["limit"])}` expected `float`')
    if params.get("corrected_areas", None) is not None:
        if not isinstance(params["corrected_areas"], (pathlib.Path, str)):
            raise StyxValidationError(f'`corrected_areas` has the wrong type: Received `{type(params["corrected_areas"])}` expected `InputPathType`')
    if params.get("naive", None) is not None:
        if not isinstance(params["naive"], bool):
            raise StyxValidationError(f'`naive` has the wrong type: Received `{type(params["naive"])}` expected `bool`')

def surface_geodesic_distance_all_to_all_cargs(params: SurfaceGeodesicDistanceAllToAll, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-geodesic-distance-all-to-all")
    cargs.append(execution.input_file(params["surface"]))
    cargs.append(params["cifti_out"])
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("-roi")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("limit")
    if v_1 is not None:
        cargs.append("-limit")
        cargs.append(str(v_1))
    v_2 = params.get("corrected_areas")
    if v_2 is not None:
        cargs.append("-corrected-areas")
        cargs.append(execution.input_file(v_2))
    if params.get("naive"):
        cargs.append("-naive")
    return cargs

def surface_geodesic_distance_all_to_all_outputs(params: SurfaceGeodesicDistanceAllToAll, execution: Execution) -> SurfaceGeodesicDistanceAllToAllOutputs:
    """Build the SurfaceGeodesicDistanceAllToAllOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cifti_out_v: OutputPathType = execution.output_file(params["cifti_out"])
    return SurfaceGeodesicDistanceAllToAllOutputs(
        root=root_v,
        cifti_out=cifti_out_v,
    )

def surface_geodesic_distance_all_to_all_execute(params: SurfaceGeodesicDistanceAllToAll, runner: Runner | None = None) -> SurfaceGeodesicDistanceAllToAllOutputs:
    """
    COMPUTE GEODESIC DISTANCES FROM ALL VERTICES

    Computes geodesic distance from every vertex to every vertex, outputting a single-hemisphere dconn file.  If you are only interested in a few vertices, see -surface-geodesic-distance.  When -limit is specified, any vertex beyond the limit is assigned the value -1.

The -roi option makes the output file smaller by not outputting distances to or from vertices outside the ROI, but paths are still allowed to go outside the ROI when finding distances to other vertices.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_geodesic_distance_all_to_all_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_GEODESIC_DISTANCE_ALL_TO_ALL_METADATA)
    execution.params(params)
    args = surface_geodesic_distance_all_to_all_cargs(params, execution)
    out = surface_geodesic_distance_all_to_all_outputs(params, execution)
    execution.run(args)
    return out

def surface_geodesic_distance_all_to_all(
    surface: InputPathType,
    cifti_out: str,
    roi: InputPathType | None = None,
    limit: float | None = None,
    corrected_areas: InputPathType | None = None,
    naive: bool = False,
    runner: Runner | None = None,
) -> SurfaceGeodesicDistanceAllToAllOutputs:
    """
    COMPUTE GEODESIC DISTANCES FROM ALL VERTICES

    Computes geodesic distance from every vertex to every vertex, outputting a single-hemisphere dconn file.  If you are only interested in a few vertices, see -surface-geodesic-distance.  When -limit is specified, any vertex beyond the limit is assigned the value -1.

The -roi option makes the output file smaller by not outputting distances to or from vertices outside the ROI, but paths are still allowed to go outside the ROI when finding distances to other vertices.

The -corrected-areas option should be used when the input is a group average surface - group average surfaces have significantly less surface area than individual surfaces do, and therefore distances measured on them would be smaller than measuring them on individual surfaces.  In this case, the input to this option should be a group average of the output of -surface-vertex-areas for each subject.

If -naive is not specified, the algorithm uses not just immediate neighbors, but also neighbors derived from crawling across pairs of triangles that share an edge.

    Args:
        surface: the surface to compute on
        cifti_out: single-hemisphere dconn containing the distances
        roi: only output distances for vertices inside an ROI
        limit: stop at a specified distance
        corrected_areas: vertex areas to use to correct the distances on a\
            group-average surface
        naive: use only neighbors, don't crawl triangles (not recommended)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_geodesic_distance_all_to_all_params(
        surface=surface,
        cifti_out=cifti_out,
        roi=roi,
        limit=limit,
        corrected_areas=corrected_areas,
        naive=naive,
    )
    return surface_geodesic_distance_all_to_all_execute(params, runner)

__all__ = [
    "SurfaceGeodesicDistanceAllToAll",
    "SurfaceGeodesicDistanceAllToAllOutputs",
    "SURFACE_GEODESIC_DISTANCE_ALL_TO_ALL_METADATA",
    "surface_geodesic_distance_all_to_all_cargs",
    "surface_geodesic_distance_all_to_all_outputs",
    "surface_geodesic_distance_all_to_all_params",
    "surface_geodesic_distance_all_to_all_execute",
    "surface_geodesic_distance_all_to_all_validate",
    "surface_geodesic_distance_all_to_all",
]