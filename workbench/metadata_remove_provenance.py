# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

METADATA_REMOVE_PROVENANCE_METADATA = Metadata(
    id="metadata_remove_provenance",
    name="REMOVE PROVENANCE INFORMATION FROM FILE METADATA",
    package="workbench",
)

MetadataRemoveProvenance = typing.TypedDict(
    "MetadataRemoveProvenance",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/metadata_remove_provenance"]],
        "input_file": str,
        "output_file": str,
    },
)

@dataclasses.dataclass
class MetadataRemoveProvenanceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def metadata_remove_provenance_params(
    input_file: str,
    output_file: str,
) -> MetadataRemoveProvenance:
    """
    Build parameters.

    Args:
        input_file: the file to remove provenance information from
        output_file: output - the name to save the modified file as

    Returns:
        Parameter dictionary.
    """
    params: MetadataRemoveProvenance = {
        "@type": "workbench/metadata_remove_provenance",
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def metadata_remove_provenance_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MetadataRemoveProvenance."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], str):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `str`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def metadata_remove_provenance_cargs(params: MetadataRemoveProvenance, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-metadata-remove-provenance")
    cargs.append(params["input_file"])
    cargs.append(params["output_file"])
    return cargs

def metadata_remove_provenance_outputs(params: MetadataRemoveProvenance, execution: Execution) -> MetadataRemoveProvenanceOutputs:
    """Build the MetadataRemoveProvenanceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MetadataRemoveProvenanceOutputs(
        root=root_v,
    )

def metadata_remove_provenance_execute(params: MetadataRemoveProvenance, runner: Runner | None = None) -> MetadataRemoveProvenanceOutputs:
    """
    REMOVE PROVENANCE INFORMATION FROM FILE METADATA

    Removes the provenance metadata fields added by workbench during processing.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    metadata_remove_provenance_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(METADATA_REMOVE_PROVENANCE_METADATA)
    execution.params(params)
    args = metadata_remove_provenance_cargs(params, execution)
    out = metadata_remove_provenance_outputs(params, execution)
    execution.run(args)
    return out

def metadata_remove_provenance(
    input_file: str,
    output_file: str,
    runner: Runner | None = None,
) -> MetadataRemoveProvenanceOutputs:
    """
    REMOVE PROVENANCE INFORMATION FROM FILE METADATA

    Removes the provenance metadata fields added by workbench during processing.

    Args:
        input_file: the file to remove provenance information from
        output_file: output - the name to save the modified file as
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = metadata_remove_provenance_params(
        input_file=input_file,
        output_file=output_file,
    )
    return metadata_remove_provenance_execute(params, runner)

__all__ = [
    "MetadataRemoveProvenance",
    "MetadataRemoveProvenanceOutputs",
    "METADATA_REMOVE_PROVENANCE_METADATA",
    "metadata_remove_provenance_cargs",
    "metadata_remove_provenance_outputs",
    "metadata_remove_provenance_params",
    "metadata_remove_provenance_execute",
    "metadata_remove_provenance_validate",
    "metadata_remove_provenance",
]