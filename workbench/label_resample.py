# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL_RESAMPLE_METADATA = Metadata(
    id="label_resample",
    name="RESAMPLE A LABEL FILE TO A DIFFERENT MESH",
    package="workbench",
)

class LabelResampleAreaSurfs(typing.TypedDict):
    current_area: InputPathType
    """a relevant anatomical surface with <current-sphere> mesh"""
    new_area: InputPathType
    """a relevant anatomical surface with <new-sphere> mesh"""

class LabelResampleValidRoiOut(typing.TypedDict):
    roi_out: str
    """the output roi as a metric"""

LabelResample = typing.TypedDict(
    "LabelResample",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/label_resample"]],
        "label_in": InputPathType,
        "current_sphere": InputPathType,
        "new_sphere": InputPathType,
        "method": str,
        "label_out": str,
        "area_surfs": typing.NotRequired[LabelResampleAreaSurfs],
        "area_metrics": typing.NotRequired[LabelResampleAreaSurfs],
        "current_roi": typing.NotRequired[InputPathType],
        "valid_roi_out": typing.NotRequired[LabelResampleValidRoiOut],
        "largest": typing.NotRequired[bool],
        "bypass_sphere_check": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LabelResampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_out: OutputPathType
    """the output label file"""
    roi_out: OutputPathType | None
    """the output roi as a metric"""

def label_resample_params(
    label_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    method: str,
    label_out: str,
    area_surfs: LabelResampleAreaSurfs | None = None,
    area_metrics: LabelResampleAreaSurfs | None = None,
    current_roi: InputPathType | None = None,
    valid_roi_out: LabelResampleValidRoiOut | None = None,
    largest: bool = False,
    bypass_sphere_check: bool = False,
) -> LabelResample:
    """
    Build parameters.

    Args:
        label_in: the label file to resample
        current_sphere: a sphere surface with the mesh that the label file is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        method: the method name
        label_out: the output label file
        area_surfs: specify surfaces to do vertex area correction based on
        area_metrics: specify vertex area metrics to do area correction based on
        current_roi: use an input roi on the current mesh to exclude non-data\
            vertices
        valid_roi_out: output the ROI of vertices that got data from valid source\
            vertices
        largest: use only the label of the vertex with the largest weight
        bypass_sphere_check: ADVANCED: allow the current and new 'spheres' to have\
            arbitrary shape as long as they follow the same contour

    Returns:
        Parameter dictionary.
    """
    params: LabelResample = {
        "@type": "workbench/label_resample",
        "label_in": label_in,
        "current_sphere": current_sphere,
        "new_sphere": new_sphere,
        "method": method,
        "label_out": label_out,
        "largest": largest,
        "bypass_sphere_check": bypass_sphere_check,
    }
    if area_surfs is not None:
        params["area_surfs"] = area_surfs
    if area_metrics is not None:
        params["area_metrics"] = area_metrics
    if current_roi is not None:
        params["current_roi"] = current_roi
    if valid_roi_out is not None:
        params["valid_roi_out"] = valid_roi_out
    return params

def label_resample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LabelResample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("label_in", None) is None:
        raise StyxValidationError("`label_in` must not be None")
    if not isinstance(params["label_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`label_in` has the wrong type: Received `{type(params["label_in"])}` expected `InputPathType`')
    if params.get("current_sphere", None) is None:
        raise StyxValidationError("`current_sphere` must not be None")
    if not isinstance(params["current_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`current_sphere` has the wrong type: Received `{type(params["current_sphere"])}` expected `InputPathType`')
    if params.get("new_sphere", None) is None:
        raise StyxValidationError("`new_sphere` must not be None")
    if not isinstance(params["new_sphere"], (pathlib.Path, str)):
        raise StyxValidationError(f'`new_sphere` has the wrong type: Received `{type(params["new_sphere"])}` expected `InputPathType`')
    if params.get("method", None) is None:
        raise StyxValidationError("`method` must not be None")
    if not isinstance(params["method"], str):
        raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `str`')
    if params.get("label_out", None) is None:
        raise StyxValidationError("`label_out` must not be None")
    if not isinstance(params["label_out"], str):
        raise StyxValidationError(f'`label_out` has the wrong type: Received `{type(params["label_out"])}` expected `str`')
    if params.get("area_surfs", None) is not None:
        if not isinstance(params["area_surfs"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["area_surfs"])}\'')
        if params["area_surfs"].get("current_area", None) is None:
            raise StyxValidationError("`current_area` must not be None")
        if not isinstance(params["area_surfs"]["current_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_area` has the wrong type: Received `{type(params["area_surfs"]["current_area"])}` expected `InputPathType`')
        if params["area_surfs"].get("new_area", None) is None:
            raise StyxValidationError("`new_area` must not be None")
        if not isinstance(params["area_surfs"]["new_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_area` has the wrong type: Received `{type(params["area_surfs"]["new_area"])}` expected `InputPathType`')
    if params.get("area_metrics", None) is not None:
        if not isinstance(params["area_metrics"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["area_metrics"])}\'')
        if params["area_metrics"].get("current_area", None) is None:
            raise StyxValidationError("`current_area` must not be None")
        if not isinstance(params["area_metrics"]["current_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_area` has the wrong type: Received `{type(params["area_metrics"]["current_area"])}` expected `InputPathType`')
        if params["area_metrics"].get("new_area", None) is None:
            raise StyxValidationError("`new_area` must not be None")
        if not isinstance(params["area_metrics"]["new_area"], (pathlib.Path, str)):
            raise StyxValidationError(f'`new_area` has the wrong type: Received `{type(params["area_metrics"]["new_area"])}` expected `InputPathType`')
    if params.get("current_roi", None) is not None:
        if not isinstance(params["current_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`current_roi` has the wrong type: Received `{type(params["current_roi"])}` expected `InputPathType`')
    if params.get("valid_roi_out", None) is not None:
        if not isinstance(params["valid_roi_out"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["valid_roi_out"])}\'')
        if params["valid_roi_out"].get("roi_out", None) is None:
            raise StyxValidationError("`roi_out` must not be None")
        if not isinstance(params["valid_roi_out"]["roi_out"], str):
            raise StyxValidationError(f'`roi_out` has the wrong type: Received `{type(params["valid_roi_out"]["roi_out"])}` expected `str`')
    if params.get("largest", None) is not None:
        if not isinstance(params["largest"], bool):
            raise StyxValidationError(f'`largest` has the wrong type: Received `{type(params["largest"])}` expected `bool`')
    if params.get("bypass_sphere_check", None) is not None:
        if not isinstance(params["bypass_sphere_check"], bool):
            raise StyxValidationError(f'`bypass_sphere_check` has the wrong type: Received `{type(params["bypass_sphere_check"])}` expected `bool`')

def label_resample_cargs(params: LabelResample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-label-resample")
    cargs.append(execution.input_file(params["label_in"]))
    cargs.append(execution.input_file(params["current_sphere"]))
    cargs.append(execution.input_file(params["new_sphere"]))
    cargs.append(params["method"])
    cargs.append(params["label_out"])
    v_0 = params.get("area_surfs")
    if v_0 is not None:
        cargs.append("-area-surfs")
        cargs.append(execution.input_file(v_0["current_area"]))
        cargs.append(execution.input_file(v_0["new_area"]))
    v_1 = params.get("area_metrics")
    if v_1 is not None:
        cargs.append("-area-metrics")
        cargs.append(execution.input_file(v_1["current_area"]))
        cargs.append(execution.input_file(v_1["new_area"]))
    v_2 = params.get("current_roi")
    if v_2 is not None:
        cargs.append("-current-roi")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("valid_roi_out")
    if v_3 is not None:
        cargs.append("-valid-roi-out")
        cargs.append(v_3["roi_out"])
    if params.get("largest"):
        cargs.append("-largest")
    if params.get("bypass_sphere_check"):
        cargs.append("-bypass-sphere-check")
    return cargs

def label_resample_outputs(params: LabelResample, execution: Execution) -> LabelResampleOutputs:
    """Build the LabelResampleOutputs object for this tool."""
    roi_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    label_out_v: OutputPathType = execution.output_file(params["label_out"])
    __v0 = params.get("valid_roi_out")
    if __v0 is not None:
        roi_out_v = execution.output_file(__v0["roi_out"])
    return LabelResampleOutputs(
        root=root_v,
        label_out=label_out_v,
        roi_out=roi_out_v,
    )

def label_resample_execute(params: LabelResample, runner: Runner | None = None) -> LabelResampleOutputs:
    """
    RESAMPLE A LABEL FILE TO A DIFFERENT MESH

    Resamples a label file, given two spherical surfaces that are in register.  If ADAP_BARY_AREA is used, exactly one of -area-surfs or -area-metrics must be specified.

The ADAP_BARY_AREA method is recommended for label data, because it should be better at resolving vertices that are near multiple labels, or in case of downsampling.  Midthickness surfaces are recommended for the vertex areas for most data.

The -largest option results in nearest vertex behavior when used with BARYCENTRIC, as it uses the value of the source vertex that has the largest weight.

When -largest is not specified, the vertex weights are summed according to which label they correspond to, and the label with the largest sum is used.

The <method> argument must be one of the following:

ADAP_BARY_AREA
BARYCENTRIC


    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label_resample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL_RESAMPLE_METADATA)
    execution.params(params)
    args = label_resample_cargs(params, execution)
    out = label_resample_outputs(params, execution)
    execution.run(args)
    return out

def label_resample(
    label_in: InputPathType,
    current_sphere: InputPathType,
    new_sphere: InputPathType,
    method: str,
    label_out: str,
    area_surfs: LabelResampleAreaSurfs | None = None,
    area_metrics: LabelResampleAreaSurfs | None = None,
    current_roi: InputPathType | None = None,
    valid_roi_out: LabelResampleValidRoiOut | None = None,
    largest: bool = False,
    bypass_sphere_check: bool = False,
    runner: Runner | None = None,
) -> LabelResampleOutputs:
    """
    RESAMPLE A LABEL FILE TO A DIFFERENT MESH

    Resamples a label file, given two spherical surfaces that are in register.  If ADAP_BARY_AREA is used, exactly one of -area-surfs or -area-metrics must be specified.

The ADAP_BARY_AREA method is recommended for label data, because it should be better at resolving vertices that are near multiple labels, or in case of downsampling.  Midthickness surfaces are recommended for the vertex areas for most data.

The -largest option results in nearest vertex behavior when used with BARYCENTRIC, as it uses the value of the source vertex that has the largest weight.

When -largest is not specified, the vertex weights are summed according to which label they correspond to, and the label with the largest sum is used.

The <method> argument must be one of the following:

ADAP_BARY_AREA
BARYCENTRIC


    Args:
        label_in: the label file to resample
        current_sphere: a sphere surface with the mesh that the label file is\
            currently on
        new_sphere: a sphere surface that is in register with <current-sphere> and\
            has the desired output mesh
        method: the method name
        label_out: the output label file
        area_surfs: specify surfaces to do vertex area correction based on
        area_metrics: specify vertex area metrics to do area correction based on
        current_roi: use an input roi on the current mesh to exclude non-data\
            vertices
        valid_roi_out: output the ROI of vertices that got data from valid source\
            vertices
        largest: use only the label of the vertex with the largest weight
        bypass_sphere_check: ADVANCED: allow the current and new 'spheres' to have\
            arbitrary shape as long as they follow the same contour
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label_resample_params(
        label_in=label_in,
        current_sphere=current_sphere,
        new_sphere=new_sphere,
        method=method,
        label_out=label_out,
        area_surfs=area_surfs,
        area_metrics=area_metrics,
        current_roi=current_roi,
        valid_roi_out=valid_roi_out,
        largest=largest,
        bypass_sphere_check=bypass_sphere_check,
    )
    return label_resample_execute(params, runner)

__all__ = [
    "LabelResample",
    "LabelResampleOutputs",
    "LABEL_RESAMPLE_METADATA",
    "label_resample_cargs",
    "label_resample_outputs",
    "label_resample_params",
    "label_resample_execute",
    "label_resample_validate",
    "label_resample",
]