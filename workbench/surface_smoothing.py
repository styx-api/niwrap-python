# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_SMOOTHING_METADATA = Metadata(
    id="surface_smoothing",
    name="SURFACE SMOOTHING",
    package="workbench",
)

SurfaceSmoothing = typing.TypedDict(
    "SurfaceSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["workbench/surface_smoothing"]],
        "surface_in": InputPathType,
        "smoothing_strength": float,
        "smoothing_iterations": int,
        "surface_out": str,
    },
)

@dataclasses.dataclass
class SurfaceSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    surface_out: OutputPathType
    """output surface file"""

def surface_smoothing_params(
    surface_in: InputPathType,
    smoothing_strength: float,
    smoothing_iterations: int,
    surface_out: str,
) -> SurfaceSmoothing:
    """
    Build parameters.

    Args:
        surface_in: the surface file to smooth
        smoothing_strength: smoothing strength (ranges [0.0 - 1.0])
        smoothing_iterations: smoothing iterations
        surface_out: output surface file

    Returns:
        Parameter dictionary.
    """
    params: SurfaceSmoothing = {
        "@type": "workbench/surface_smoothing",
        "surface_in": surface_in,
        "smoothing_strength": smoothing_strength,
        "smoothing_iterations": smoothing_iterations,
        "surface_out": surface_out,
    }
    return params

def surface_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface_in", None) is None:
        raise StyxValidationError("`surface_in` must not be None")
    if not isinstance(params["surface_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_in` has the wrong type: Received `{type(params["surface_in"])}` expected `InputPathType`')
    if params.get("smoothing_strength", None) is None:
        raise StyxValidationError("`smoothing_strength` must not be None")
    if not isinstance(params["smoothing_strength"], (float, int)):
        raise StyxValidationError(f'`smoothing_strength` has the wrong type: Received `{type(params["smoothing_strength"])}` expected `float`')
    if params.get("smoothing_iterations", None) is None:
        raise StyxValidationError("`smoothing_iterations` must not be None")
    if not isinstance(params["smoothing_iterations"], int):
        raise StyxValidationError(f'`smoothing_iterations` has the wrong type: Received `{type(params["smoothing_iterations"])}` expected `int`')
    if params.get("surface_out", None) is None:
        raise StyxValidationError("`surface_out` must not be None")
    if not isinstance(params["surface_out"], str):
        raise StyxValidationError(f'`surface_out` has the wrong type: Received `{type(params["surface_out"])}` expected `str`')

def surface_smoothing_cargs(params: SurfaceSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wb_command")
    cargs.append("-surface-smoothing")
    cargs.append(execution.input_file(params["surface_in"]))
    cargs.append(str(params["smoothing_strength"]))
    cargs.append(str(params["smoothing_iterations"]))
    cargs.append(params["surface_out"])
    return cargs

def surface_smoothing_outputs(params: SurfaceSmoothing, execution: Execution) -> SurfaceSmoothingOutputs:
    """Build the SurfaceSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    surface_out_v: OutputPathType = execution.output_file(params["surface_out"])
    return SurfaceSmoothingOutputs(
        root=root_v,
        surface_out=surface_out_v,
    )

def surface_smoothing_execute(params: SurfaceSmoothing, runner: Runner | None = None) -> SurfaceSmoothingOutputs:
    """
    SURFACE SMOOTHING

    Smooths a surface by averaging vertex coordinates with those of the neighboring vertices.

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_SMOOTHING_METADATA)
    execution.params(params)
    args = surface_smoothing_cargs(params, execution)
    out = surface_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def surface_smoothing(
    surface_in: InputPathType,
    smoothing_strength: float,
    smoothing_iterations: int,
    surface_out: str,
    runner: Runner | None = None,
) -> SurfaceSmoothingOutputs:
    """
    SURFACE SMOOTHING

    Smooths a surface by averaging vertex coordinates with those of the neighboring vertices.

    Args:
        surface_in: the surface file to smooth
        smoothing_strength: smoothing strength (ranges [0.0 - 1.0])
        smoothing_iterations: smoothing iterations
        surface_out: output surface file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_smoothing_params(
        surface_in=surface_in,
        smoothing_strength=smoothing_strength,
        smoothing_iterations=smoothing_iterations,
        surface_out=surface_out,
    )
    return surface_smoothing_execute(params, runner)

__all__ = [
    "SurfaceSmoothing",
    "SurfaceSmoothingOutputs",
    "SURFACE_SMOOTHING_METADATA",
    "surface_smoothing_cargs",
    "surface_smoothing_outputs",
    "surface_smoothing_params",
    "surface_smoothing_execute",
    "surface_smoothing_validate",
    "surface_smoothing",
]