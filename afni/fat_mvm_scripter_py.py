# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_MVM_SCRIPTER_PY_METADATA = Metadata(
    id="fat_mvm_scripter.py",
    name="fat_mvm_scripter.py",
    package="afni",
)

FatMvmScripterPy = typing.TypedDict(
    "FatMvmScripterPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_mvm_scripter.py"]],
        "prefix": str,
        "table": InputPathType,
        "log": InputPathType,
        "vars": typing.NotRequired[str],
        "file_vars": typing.NotRequired[InputPathType],
        "Pars": typing.NotRequired[str],
        "file_Pars": typing.NotRequired[InputPathType],
        "rois": typing.NotRequired[str],
        "file_rois": typing.NotRequired[InputPathType],
        "no_posthoc": typing.NotRequired[bool],
        "NA_warn_off": typing.NotRequired[bool],
        "subnet_pref": typing.NotRequired[str],
        "cat_pair_off": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatMvmScripterPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    generated_script: OutputPathType
    """Script for running 3dMVM, using the prescribed variables along with each individual parameter."""
    results_file: OutputPathType
    """Text file of the test results."""

def fat_mvm_scripter_py_params(
    prefix: str,
    table: InputPathType,
    log: InputPathType,
    vars: str | None = None,
    file_vars: InputPathType | None = None,
    Pars: str | None = None,
    file_Pars: InputPathType | None = None,
    rois: str | None = None,
    file_rois: InputPathType | None = None,
    no_posthoc: bool = False,
    NA_warn_off: bool = False,
    subnet_pref: str | None = None,
    cat_pair_off: bool = False,
) -> FatMvmScripterPy:
    """
    Build parameters.

    Args:
        prefix: Output prefix for the script file, which will ultimately create a\
            PREFIX_MVM.txt file of statistical results from 3dMVM.
        table: Text file containing columns of subject data, one subject per row,\
            formatted as a *_MVMtbl.txt output by fat_mvm_prep.py.
        log: File formatted according to fat_mvm_prep.py containing commented\
            headings and lists of cross-group ROIs and parameters.
        vars: List of variables for the 3dMVM model. Names must be separated with\
            whitespace. Categorical variables will be detected automatically by the\
            presence of nonnumeric characters in their columns.
        file_vars: Second method for supplying a list of variables for 3dMVM.\
            VAR_FILE is a text file with a single column of variable names.
        Pars: List of parameters (names of matrices) to run in distinct 3dMVM\
            models. Names must be separated with whitespace.
        file_Pars: Second method for supplying a list of parameters for 3dMVM runs.\
            PAR_FILE is a text file with a single column of parameter names.
        rois: Optional command to select a subset of available network ROIs. Names\
            must be separated with whitespace.
        file_rois: Second method for supplying a subset of ROIs for 3dMVM runs.\
            ROI_FILE is a text file with a single column of variable names.
        no_posthoc: Switch to turn off the automatic generation of per-ROI post hoc\
            tests.
        NA_warn_off: Switch to turn off the automatic warnings as the data table is\
            created. 3dMVM will excise subjects with NA values, so there shouldn't\
            be NA values in columns you want to model.
        subnet_pref: Name SUBPR for the new table file that is created when a\
            subnetwork list of ROIs is used.
        cat_pair_off: Switch to turn off the test for categorical variables\
            undergoing posthoc testing.

    Returns:
        Parameter dictionary.
    """
    params: FatMvmScripterPy = {
        "@type": "afni/fat_mvm_scripter.py",
        "prefix": prefix,
        "table": table,
        "log": log,
        "no_posthoc": no_posthoc,
        "NA_warn_off": NA_warn_off,
        "cat_pair_off": cat_pair_off,
    }
    if vars is not None:
        params["vars"] = vars
    if file_vars is not None:
        params["file_vars"] = file_vars
    if Pars is not None:
        params["Pars"] = Pars
    if file_Pars is not None:
        params["file_Pars"] = file_Pars
    if rois is not None:
        params["rois"] = rois
    if file_rois is not None:
        params["file_rois"] = file_rois
    if subnet_pref is not None:
        params["subnet_pref"] = subnet_pref
    return params

def fat_mvm_scripter_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatMvmScripterPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("table", None) is None:
        raise StyxValidationError("`table` must not be None")
    if not isinstance(params["table"], (pathlib.Path, str)):
        raise StyxValidationError(f'`table` has the wrong type: Received `{type(params["table"])}` expected `InputPathType`')
    if params.get("log", None) is None:
        raise StyxValidationError("`log` must not be None")
    if not isinstance(params["log"], (pathlib.Path, str)):
        raise StyxValidationError(f'`log` has the wrong type: Received `{type(params["log"])}` expected `InputPathType`')
    if params.get("vars", None) is not None:
        if not isinstance(params["vars"], str):
            raise StyxValidationError(f'`vars` has the wrong type: Received `{type(params["vars"])}` expected `str`')
    if params.get("file_vars", None) is not None:
        if not isinstance(params["file_vars"], (pathlib.Path, str)):
            raise StyxValidationError(f'`file_vars` has the wrong type: Received `{type(params["file_vars"])}` expected `InputPathType`')
    if params.get("Pars", None) is not None:
        if not isinstance(params["Pars"], str):
            raise StyxValidationError(f'`Pars` has the wrong type: Received `{type(params["Pars"])}` expected `str`')
    if params.get("file_Pars", None) is not None:
        if not isinstance(params["file_Pars"], (pathlib.Path, str)):
            raise StyxValidationError(f'`file_Pars` has the wrong type: Received `{type(params["file_Pars"])}` expected `InputPathType`')
    if params.get("rois", None) is not None:
        if not isinstance(params["rois"], str):
            raise StyxValidationError(f'`rois` has the wrong type: Received `{type(params["rois"])}` expected `str`')
    if params.get("file_rois", None) is not None:
        if not isinstance(params["file_rois"], (pathlib.Path, str)):
            raise StyxValidationError(f'`file_rois` has the wrong type: Received `{type(params["file_rois"])}` expected `InputPathType`')
    if params.get("no_posthoc", None) is not None:
        if not isinstance(params["no_posthoc"], bool):
            raise StyxValidationError(f'`no_posthoc` has the wrong type: Received `{type(params["no_posthoc"])}` expected `bool`')
    if params.get("NA_warn_off", None) is not None:
        if not isinstance(params["NA_warn_off"], bool):
            raise StyxValidationError(f'`NA_warn_off` has the wrong type: Received `{type(params["NA_warn_off"])}` expected `bool`')
    if params.get("subnet_pref", None) is not None:
        if not isinstance(params["subnet_pref"], str):
            raise StyxValidationError(f'`subnet_pref` has the wrong type: Received `{type(params["subnet_pref"])}` expected `str`')
    if params.get("cat_pair_off", None) is not None:
        if not isinstance(params["cat_pair_off"], bool):
            raise StyxValidationError(f'`cat_pair_off` has the wrong type: Received `{type(params["cat_pair_off"])}` expected `bool`')

def fat_mvm_scripter_py_cargs(params: FatMvmScripterPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_mvm_scripter.py")
    cargs.append("--prefix")
    cargs.append(params["prefix"])
    cargs.append("--table")
    cargs.append(execution.input_file(params["table"]))
    cargs.append("--log")
    cargs.append(execution.input_file(params["log"]))
    v_0 = params.get("vars")
    if v_0 is not None:
        cargs.append("--vars")
        cargs.append(v_0)
    v_1 = params.get("file_vars")
    if v_1 is not None:
        cargs.append("--file_vars")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("Pars")
    if v_2 is not None:
        cargs.append("--Pars")
        cargs.append(v_2)
    v_3 = params.get("file_Pars")
    if v_3 is not None:
        cargs.append("--File_Pars")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("rois")
    if v_4 is not None:
        cargs.append("--rois")
        cargs.append(v_4)
    v_5 = params.get("file_rois")
    if v_5 is not None:
        cargs.append("--file_rois")
        cargs.append(execution.input_file(v_5))
    if params.get("no_posthoc"):
        cargs.append("--no_posthoc")
    if params.get("NA_warn_off"):
        cargs.append("--NA_warn_off")
    v_6 = params.get("subnet_pref")
    if v_6 is not None:
        cargs.append("--subnet_pref")
        cargs.append(v_6)
    if params.get("cat_pair_off"):
        cargs.append("--cat_pair_off")
    return cargs

def fat_mvm_scripter_py_outputs(params: FatMvmScripterPy, execution: Execution) -> FatMvmScripterPyOutputs:
    """Build the FatMvmScripterPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    generated_script_v: OutputPathType = execution.output_file(f'{params["prefix"]}_scri.tcsh')
    results_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}_MVM.txt')
    return FatMvmScripterPyOutputs(
        root=root_v,
        generated_script=generated_script_v,
        results_file=results_file_v,
    )

def fat_mvm_scripter_py_execute(params: FatMvmScripterPy, runner: Runner | None = None) -> FatMvmScripterPyOutputs:
    """
    fat_mvm_scripter.py

    Automated tool to create command scripts for 3dMVM statistical modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_mvm_scripter_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_MVM_SCRIPTER_PY_METADATA)
    execution.params(params)
    args = fat_mvm_scripter_py_cargs(params, execution)
    out = fat_mvm_scripter_py_outputs(params, execution)
    execution.run(args)
    return out

def fat_mvm_scripter_py(
    prefix: str,
    table: InputPathType,
    log: InputPathType,
    vars: str | None = None,
    file_vars: InputPathType | None = None,
    Pars: str | None = None,
    file_Pars: InputPathType | None = None,
    rois: str | None = None,
    file_rois: InputPathType | None = None,
    no_posthoc: bool = False,
    NA_warn_off: bool = False,
    subnet_pref: str | None = None,
    cat_pair_off: bool = False,
    runner: Runner | None = None,
) -> FatMvmScripterPyOutputs:
    """
    fat_mvm_scripter.py

    Automated tool to create command scripts for 3dMVM statistical modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output prefix for the script file, which will ultimately create a\
            PREFIX_MVM.txt file of statistical results from 3dMVM.
        table: Text file containing columns of subject data, one subject per row,\
            formatted as a *_MVMtbl.txt output by fat_mvm_prep.py.
        log: File formatted according to fat_mvm_prep.py containing commented\
            headings and lists of cross-group ROIs and parameters.
        vars: List of variables for the 3dMVM model. Names must be separated with\
            whitespace. Categorical variables will be detected automatically by the\
            presence of nonnumeric characters in their columns.
        file_vars: Second method for supplying a list of variables for 3dMVM.\
            VAR_FILE is a text file with a single column of variable names.
        Pars: List of parameters (names of matrices) to run in distinct 3dMVM\
            models. Names must be separated with whitespace.
        file_Pars: Second method for supplying a list of parameters for 3dMVM runs.\
            PAR_FILE is a text file with a single column of parameter names.
        rois: Optional command to select a subset of available network ROIs. Names\
            must be separated with whitespace.
        file_rois: Second method for supplying a subset of ROIs for 3dMVM runs.\
            ROI_FILE is a text file with a single column of variable names.
        no_posthoc: Switch to turn off the automatic generation of per-ROI post hoc\
            tests.
        NA_warn_off: Switch to turn off the automatic warnings as the data table is\
            created. 3dMVM will excise subjects with NA values, so there shouldn't\
            be NA values in columns you want to model.
        subnet_pref: Name SUBPR for the new table file that is created when a\
            subnetwork list of ROIs is used.
        cat_pair_off: Switch to turn off the test for categorical variables\
            undergoing posthoc testing.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_mvm_scripter_py_params(
        prefix=prefix,
        table=table,
        log=log,
        vars=vars,
        file_vars=file_vars,
        Pars=Pars,
        file_Pars=file_Pars,
        rois=rois,
        file_rois=file_rois,
        no_posthoc=no_posthoc,
        NA_warn_off=NA_warn_off,
        subnet_pref=subnet_pref,
        cat_pair_off=cat_pair_off,
    )
    return fat_mvm_scripter_py_execute(params, runner)

__all__ = [
    "FatMvmScripterPy",
    "FatMvmScripterPyOutputs",
    "FAT_MVM_SCRIPTER_PY_METADATA",
    "fat_mvm_scripter_py_cargs",
    "fat_mvm_scripter_py_outputs",
    "fat_mvm_scripter_py_params",
    "fat_mvm_scripter_py_execute",
    "fat_mvm_scripter_py_validate",
    "fat_mvm_scripter_py",
]