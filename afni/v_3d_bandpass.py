# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_BANDPASS_METADATA = Metadata(
    id="3dBandpass",
    name="3dBandpass",
    package="afni",
)

V3dBandpassOutputtype = typing.Literal["NIFTI"] | typing.Literal["AFNI"] | typing.Literal["NIFTI_GZ"]

V3dBandpass = typing.TypedDict(
    "V3dBandpass",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dBandpass"]],
        "prefix": typing.NotRequired[str],
        "automask": typing.NotRequired[bool],
        "blur": typing.NotRequired[float],
        "despike": typing.NotRequired[bool],
        "highpass": float,
        "lowpass": float,
        "in_file": InputPathType,
        "localPV": typing.NotRequired[float],
        "mask": typing.NotRequired[InputPathType],
        "nfft": typing.NotRequired[int],
        "no_detrend": typing.NotRequired[bool],
        "normalize": typing.NotRequired[bool],
        "notrans": typing.NotRequired[bool],
        "orthogonalize_dset": typing.NotRequired[InputPathType],
        "orthogonalize_file": typing.NotRequired[list[InputPathType]],
        "outputtype": typing.NotRequired[V3dBandpassOutputtype],
        "tr": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dBandpassOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Output file from 3dbandpass."""

def v_3d_bandpass_params(
    highpass: float,
    lowpass: float,
    in_file: InputPathType,
    prefix: str | None = None,
    automask: bool = False,
    blur: float | None = None,
    despike: bool = False,
    localPV: float | None = None,
    mask: InputPathType | None = None,
    nfft: int | None = None,
    no_detrend: bool = False,
    normalize: bool = False,
    notrans: bool = False,
    orthogonalize_dset: InputPathType | None = None,
    orthogonalize_file: list[InputPathType] | None = None,
    outputtype: V3dBandpassOutputtype | None = None,
    tr: float | None = None,
) -> V3dBandpass:
    """
    Build parameters.

    Args:
        highpass: Highpass.
        lowpass: Lowpass.
        in_file: Input file to 3dbandpass.
        prefix: Prefix for output file.
        automask: Create a mask from the input dataset.
        blur: Blur (inside the mask only) with a filter width (fwhm) of 'fff'\
            millimeters.
        despike: Despike each time series before other processing. hopefully, you\
            don't actually need to do this, which is why it is optional.
        localPV: Replace each vector by the local principal vector (aka first\
            singular vector) from a neighborhood of radius 'rrr' millimeters. note\
            that the pv time series is l2 normalized. this option is mostly for bob\
            cox to have fun with.
        mask: Mask file.
        nfft: Set the fft length [must be a legal value].
        no_detrend: Skip the quadratic detrending of the input that occurs before\
            the fft-based bandpassing. you would only want to do this if the\
            dataset had been detrended already in some other program.
        normalize: Make all output time series have l2 norm = 1 (i.e., sum of\
            squares = 1).
        notrans: Don't check for initial positive transients in the data. the test\
            is a little slow, so skipping it is ok, if you know the data time\
            series are transient-free.
        orthogonalize_dset: Orthogonalize each voxel to the corresponding voxel\
            time series in dataset 'fset', which must have the same spatial and\
            temporal grid structure as the main input dataset. at present, only one\
            '-dsort' option is allowed.
        orthogonalize_file: Also orthogonalize input to columns in f.1d. multiple\
            '-ort' options are allowed.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        tr: Set time step (tr) in sec [default=from dataset header].

    Returns:
        Parameter dictionary.
    """
    params: V3dBandpass = {
        "@type": "afni/3dBandpass",
        "highpass": highpass,
        "lowpass": lowpass,
        "in_file": in_file,
        "automask": automask,
        "despike": despike,
        "no_detrend": no_detrend,
        "normalize": normalize,
        "notrans": notrans,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if blur is not None:
        params["blur"] = blur
    if localPV is not None:
        params["localPV"] = localPV
    if mask is not None:
        params["mask"] = mask
    if nfft is not None:
        params["nfft"] = nfft
    if orthogonalize_dset is not None:
        params["orthogonalize_dset"] = orthogonalize_dset
    if orthogonalize_file is not None:
        params["orthogonalize_file"] = orthogonalize_file
    if outputtype is not None:
        params["outputtype"] = outputtype
    if tr is not None:
        params["tr"] = tr
    return params

def v_3d_bandpass_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dBandpass."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("blur", None) is not None:
        if not isinstance(params["blur"], (float, int)):
            raise StyxValidationError(f'`blur` has the wrong type: Received `{type(params["blur"])}` expected `float`')
    if params.get("despike", None) is not None:
        if not isinstance(params["despike"], bool):
            raise StyxValidationError(f'`despike` has the wrong type: Received `{type(params["despike"])}` expected `bool`')
    if params.get("highpass", None) is None:
        raise StyxValidationError("`highpass` must not be None")
    if not isinstance(params["highpass"], (float, int)):
        raise StyxValidationError(f'`highpass` has the wrong type: Received `{type(params["highpass"])}` expected `float`')
    if params.get("lowpass", None) is None:
        raise StyxValidationError("`lowpass` must not be None")
    if not isinstance(params["lowpass"], (float, int)):
        raise StyxValidationError(f'`lowpass` has the wrong type: Received `{type(params["lowpass"])}` expected `float`')
    if params.get("in_file", None) is None:
        raise StyxValidationError("`in_file` must not be None")
    if not isinstance(params["in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_file` has the wrong type: Received `{type(params["in_file"])}` expected `InputPathType`')
    if params.get("localPV", None) is not None:
        if not isinstance(params["localPV"], (float, int)):
            raise StyxValidationError(f'`localPV` has the wrong type: Received `{type(params["localPV"])}` expected `float`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("nfft", None) is not None:
        if not isinstance(params["nfft"], int):
            raise StyxValidationError(f'`nfft` has the wrong type: Received `{type(params["nfft"])}` expected `int`')
    if params.get("no_detrend", None) is not None:
        if not isinstance(params["no_detrend"], bool):
            raise StyxValidationError(f'`no_detrend` has the wrong type: Received `{type(params["no_detrend"])}` expected `bool`')
    if params.get("normalize", None) is not None:
        if not isinstance(params["normalize"], bool):
            raise StyxValidationError(f'`normalize` has the wrong type: Received `{type(params["normalize"])}` expected `bool`')
    if params.get("notrans", None) is not None:
        if not isinstance(params["notrans"], bool):
            raise StyxValidationError(f'`notrans` has the wrong type: Received `{type(params["notrans"])}` expected `bool`')
    if params.get("orthogonalize_dset", None) is not None:
        if not isinstance(params["orthogonalize_dset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`orthogonalize_dset` has the wrong type: Received `{type(params["orthogonalize_dset"])}` expected `InputPathType`')
    if params.get("orthogonalize_file", None) is not None:
        if not isinstance(params["orthogonalize_file"], list):
            raise StyxValidationError(f'`orthogonalize_file` has the wrong type: Received `{type(params["orthogonalize_file"])}` expected `list[InputPathType]`')
        for e in params["orthogonalize_file"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`orthogonalize_file` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("outputtype", None) is not None:
        if not isinstance(params["outputtype"], str):
            raise StyxValidationError(f'`outputtype` has the wrong type: Received `{type(params["outputtype"])}` expected `V3dBandpassOutputtype`')
        if params["outputtype"] not in ["NIFTI", "AFNI", "NIFTI_GZ"]:
            raise StyxValidationError("Parameter `outputtype` must be one of [\"NIFTI\", \"AFNI\", \"NIFTI_GZ\"]")
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')

def v_3d_bandpass_cargs(params: V3dBandpass, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dBandpass")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("automask"):
        cargs.append("-automask")
    v_1 = params.get("blur")
    if v_1 is not None:
        cargs.append("-blur")
        cargs.append(str(v_1))
    if params.get("despike"):
        cargs.append("-despike")
    cargs.append(str(params["highpass"]))
    cargs.append(str(params["lowpass"]))
    cargs.append(execution.input_file(params["in_file"]))
    v_2 = params.get("localPV")
    if v_2 is not None:
        cargs.append("-localPV")
        cargs.append(str(v_2))
    v_3 = params.get("mask")
    if v_3 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("nfft")
    if v_4 is not None:
        cargs.append("-nfft")
        cargs.append(str(v_4))
    if params.get("no_detrend"):
        cargs.append("-nodetrend")
    if params.get("normalize"):
        cargs.append("-norm")
    if params.get("notrans"):
        cargs.append("-notrans")
    v_5 = params.get("orthogonalize_dset")
    if v_5 is not None:
        cargs.append("-dsort")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("orthogonalize_file")
    if v_6 is not None:
        cargs.append("-ort")
        for item0 in v_6:
            cargs.append(execution.input_file(item0))
    v_7 = params.get("outputtype")
    if v_7 is not None:
        cargs.append(str(v_7))
    v_8 = params.get("tr")
    if v_8 is not None:
        cargs.append("-dt")
        cargs.append(str(v_8))
    return cargs

def v_3d_bandpass_outputs(params: V3dBandpass, execution: Execution) -> V3dBandpassOutputs:
    """Build the V3dBandpassOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return V3dBandpassOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def v_3d_bandpass_execute(params: V3dBandpass, runner: Runner | None = None) -> V3dBandpassOutputs:
    """
    3dBandpass

    Program to lowpass and/or highpass each voxel time series in a dataset, offering more/different options than Fourier.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_bandpass_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_BANDPASS_METADATA)
    execution.params(params)
    args = v_3d_bandpass_cargs(params, execution)
    out = v_3d_bandpass_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_bandpass(
    highpass: float,
    lowpass: float,
    in_file: InputPathType,
    prefix: str | None = None,
    automask: bool = False,
    blur: float | None = None,
    despike: bool = False,
    localPV: float | None = None,
    mask: InputPathType | None = None,
    nfft: int | None = None,
    no_detrend: bool = False,
    normalize: bool = False,
    notrans: bool = False,
    orthogonalize_dset: InputPathType | None = None,
    orthogonalize_file: list[InputPathType] | None = None,
    outputtype: V3dBandpassOutputtype | None = None,
    tr: float | None = None,
    runner: Runner | None = None,
) -> V3dBandpassOutputs:
    """
    3dBandpass

    Program to lowpass and/or highpass each voxel time series in a dataset, offering more/different options than Fourier.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        highpass: Highpass.
        lowpass: Lowpass.
        in_file: Input file to 3dbandpass.
        prefix: Prefix for output file.
        automask: Create a mask from the input dataset.
        blur: Blur (inside the mask only) with a filter width (fwhm) of 'fff'\
            millimeters.
        despike: Despike each time series before other processing. hopefully, you\
            don't actually need to do this, which is why it is optional.
        localPV: Replace each vector by the local principal vector (aka first\
            singular vector) from a neighborhood of radius 'rrr' millimeters. note\
            that the pv time series is l2 normalized. this option is mostly for bob\
            cox to have fun with.
        mask: Mask file.
        nfft: Set the fft length [must be a legal value].
        no_detrend: Skip the quadratic detrending of the input that occurs before\
            the fft-based bandpassing. you would only want to do this if the\
            dataset had been detrended already in some other program.
        normalize: Make all output time series have l2 norm = 1 (i.e., sum of\
            squares = 1).
        notrans: Don't check for initial positive transients in the data. the test\
            is a little slow, so skipping it is ok, if you know the data time\
            series are transient-free.
        orthogonalize_dset: Orthogonalize each voxel to the corresponding voxel\
            time series in dataset 'fset', which must have the same spatial and\
            temporal grid structure as the main input dataset. at present, only one\
            '-dsort' option is allowed.
        orthogonalize_file: Also orthogonalize input to columns in f.1d. multiple\
            '-ort' options are allowed.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        tr: Set time step (tr) in sec [default=from dataset header].
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_bandpass_params(
        highpass=highpass,
        lowpass=lowpass,
        in_file=in_file,
        prefix=prefix,
        automask=automask,
        blur=blur,
        despike=despike,
        localPV=localPV,
        mask=mask,
        nfft=nfft,
        no_detrend=no_detrend,
        normalize=normalize,
        notrans=notrans,
        orthogonalize_dset=orthogonalize_dset,
        orthogonalize_file=orthogonalize_file,
        outputtype=outputtype,
        tr=tr,
    )
    return v_3d_bandpass_execute(params, runner)

__all__ = [
    "V3dBandpass",
    "V3dBandpassOutputs",
    "V_3D_BANDPASS_METADATA",
    "v_3d_bandpass_cargs",
    "v_3d_bandpass_outputs",
    "v_3d_bandpass_params",
    "v_3d_bandpass_execute",
    "v_3d_bandpass_validate",
    "v_3d_bandpass",
]