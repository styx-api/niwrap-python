# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1DTRANSPOSE_METADATA = Metadata(
    id="1dtranspose",
    name="1dtranspose",
    package="afni",
)

V1dtranspose = typing.TypedDict(
    "V1dtranspose",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dtranspose"]],
        "infile": InputPathType,
        "outfile": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V1dtransposeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType | None
    """Transposed output file"""

def v_1dtranspose_params(
    infile: InputPathType,
    outfile: str | None = None,
) -> V1dtranspose:
    """
    Build parameters.

    Args:
        infile: Input file (e.g. data.1D)
        outfile: Output file (e.g. transposed_data.1D), or '-' to write to stdout

    Returns:
        Parameter dictionary.
    """
    params: V1dtranspose = {
        "@type": "afni/1dtranspose",
        "infile": infile,
    }
    if outfile is not None:
        params["outfile"] = outfile
    return params

def v_1dtranspose_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dtranspose."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is not None:
        if not isinstance(params["outfile"], str):
            raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def v_1dtranspose_cargs(params: V1dtranspose, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dtranspose")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("outfile")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def v_1dtranspose_outputs(params: V1dtranspose, execution: Execution) -> V1dtransposeOutputs:
    """Build the V1dtransposeOutputs object for this tool."""
    outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outfile")
    if __v0 is not None:
        outfile_v = execution.output_file((__v0 if __v0 is not None else ""))
    return V1dtransposeOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def v_1dtranspose_execute(params: V1dtranspose, runner: Runner | None = None) -> V1dtransposeOutputs:
    """
    1dtranspose

    Transpose an AFNI *.1D file (ASCII list of numbers arranged in columns).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1dtranspose_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1DTRANSPOSE_METADATA)
    execution.params(params)
    args = v_1dtranspose_cargs(params, execution)
    out = v_1dtranspose_outputs(params, execution)
    execution.run(args)
    return out

def v_1dtranspose(
    infile: InputPathType,
    outfile: str | None = None,
    runner: Runner | None = None,
) -> V1dtransposeOutputs:
    """
    1dtranspose

    Transpose an AFNI *.1D file (ASCII list of numbers arranged in columns).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input file (e.g. data.1D)
        outfile: Output file (e.g. transposed_data.1D), or '-' to write to stdout
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1dtranspose_params(
        infile=infile,
        outfile=outfile,
    )
    return v_1dtranspose_execute(params, runner)

__all__ = [
    "V1dtranspose",
    "V1dtransposeOutputs",
    "V_1DTRANSPOSE_METADATA",
    "v_1dtranspose_cargs",
    "v_1dtranspose_outputs",
    "v_1dtranspose_params",
    "v_1dtranspose_execute",
    "v_1dtranspose_validate",
    "v_1dtranspose",
]