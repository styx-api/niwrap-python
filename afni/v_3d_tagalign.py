# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TAGALIGN_METADATA = Metadata(
    id="3dTagalign",
    name="3dTagalign",
    package="afni",
)

V3dTagalign = typing.TypedDict(
    "V3dTagalign",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTagalign"]],
        "input_dataset": InputPathType,
        "master_dataset": InputPathType,
        "tagset_file": typing.NotRequired[InputPathType],
        "no_keep_tags": typing.NotRequired[bool],
        "matvec_file": typing.NotRequired[str],
        "rotate": typing.NotRequired[bool],
        "affine": typing.NotRequired[bool],
        "rotscl": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
        "dummy": typing.NotRequired[bool],
        "linear_interpolation": typing.NotRequired[bool],
        "cubic_interpolation": typing.NotRequired[bool],
        "nearest_neighbor_interpolation": typing.NotRequired[bool],
        "quintic_interpolation": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dTagalignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset_head: OutputPathType | None
    """Output dataset in AFNI format (.HEAD)"""
    output_dataset_brick: OutputPathType | None
    """Output dataset in AFNI format (.BRIK)"""
    output_matvec_file: OutputPathType | None
    """Output transformation matrix and vector file"""

def v_3d_tagalign_params(
    input_dataset: InputPathType,
    master_dataset: InputPathType,
    tagset_file: InputPathType | None = None,
    no_keep_tags: bool = False,
    matvec_file: str | None = None,
    rotate: bool = False,
    affine: bool = False,
    rotscl: bool = False,
    prefix: str | None = None,
    verbose: bool = False,
    dummy: bool = False,
    linear_interpolation: bool = False,
    cubic_interpolation: bool = False,
    nearest_neighbor_interpolation: bool = False,
    quintic_interpolation: bool = False,
) -> V3dTagalign:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset to align
        master_dataset: Use dataset 'mset' as the master dataset. This option is\
            mandatory.
        tagset_file: Use the tagset in the .tag file instead of dset.
        no_keep_tags: Don't put transformed locations of dset's tags into the\
            output dataset [default = keep tags].
        matvec_file: Write the matrix+vector transformation to file 'mfile'. This\
            can be used with 3dWarp's '-matvec_in2out' option to align other\
            datasets in the same way (e.g., functional datasets).
        rotate: Compute the transformation as a rotation + shift (default).
        affine: Compute the transformation as a general affine map, where the\
            matrix is a general 3x3 matrix.
        rotscl: Compute transformation as a rotation times an isotropic scaling;\
            where matrix is an orthogonal matrix times a scalar.
        prefix: Specify the prefix for the output dataset.
        verbose: Print progress reports.
        dummy: Don't actually rotate the dataset, just compute the transformation\
            matrix and vector. If '-matvec' is used, the mfile will be written.
        linear_interpolation: Use linear interpolation method.
        cubic_interpolation: Use cubic interpolation method (default).
        nearest_neighbor_interpolation: Use nearest neighbour interpolation method.
        quintic_interpolation: Use quintic interpolation method.

    Returns:
        Parameter dictionary.
    """
    params: V3dTagalign = {
        "@type": "afni/3dTagalign",
        "input_dataset": input_dataset,
        "master_dataset": master_dataset,
        "no_keep_tags": no_keep_tags,
        "rotate": rotate,
        "affine": affine,
        "rotscl": rotscl,
        "verbose": verbose,
        "dummy": dummy,
        "linear_interpolation": linear_interpolation,
        "cubic_interpolation": cubic_interpolation,
        "nearest_neighbor_interpolation": nearest_neighbor_interpolation,
        "quintic_interpolation": quintic_interpolation,
    }
    if tagset_file is not None:
        params["tagset_file"] = tagset_file
    if matvec_file is not None:
        params["matvec_file"] = matvec_file
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_tagalign_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTagalign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("master_dataset", None) is None:
        raise StyxValidationError("`master_dataset` must not be None")
    if not isinstance(params["master_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`master_dataset` has the wrong type: Received `{type(params["master_dataset"])}` expected `InputPathType`')
    if params.get("tagset_file", None) is not None:
        if not isinstance(params["tagset_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tagset_file` has the wrong type: Received `{type(params["tagset_file"])}` expected `InputPathType`')
    if params.get("no_keep_tags", None) is not None:
        if not isinstance(params["no_keep_tags"], bool):
            raise StyxValidationError(f'`no_keep_tags` has the wrong type: Received `{type(params["no_keep_tags"])}` expected `bool`')
    if params.get("matvec_file", None) is not None:
        if not isinstance(params["matvec_file"], str):
            raise StyxValidationError(f'`matvec_file` has the wrong type: Received `{type(params["matvec_file"])}` expected `str`')
    if params.get("rotate", None) is not None:
        if not isinstance(params["rotate"], bool):
            raise StyxValidationError(f'`rotate` has the wrong type: Received `{type(params["rotate"])}` expected `bool`')
    if params.get("affine", None) is not None:
        if not isinstance(params["affine"], bool):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `bool`')
    if params.get("rotscl", None) is not None:
        if not isinstance(params["rotscl"], bool):
            raise StyxValidationError(f'`rotscl` has the wrong type: Received `{type(params["rotscl"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("dummy", None) is not None:
        if not isinstance(params["dummy"], bool):
            raise StyxValidationError(f'`dummy` has the wrong type: Received `{type(params["dummy"])}` expected `bool`')
    if params.get("linear_interpolation", None) is not None:
        if not isinstance(params["linear_interpolation"], bool):
            raise StyxValidationError(f'`linear_interpolation` has the wrong type: Received `{type(params["linear_interpolation"])}` expected `bool`')
    if params.get("cubic_interpolation", None) is not None:
        if not isinstance(params["cubic_interpolation"], bool):
            raise StyxValidationError(f'`cubic_interpolation` has the wrong type: Received `{type(params["cubic_interpolation"])}` expected `bool`')
    if params.get("nearest_neighbor_interpolation", None) is not None:
        if not isinstance(params["nearest_neighbor_interpolation"], bool):
            raise StyxValidationError(f'`nearest_neighbor_interpolation` has the wrong type: Received `{type(params["nearest_neighbor_interpolation"])}` expected `bool`')
    if params.get("quintic_interpolation", None) is not None:
        if not isinstance(params["quintic_interpolation"], bool):
            raise StyxValidationError(f'`quintic_interpolation` has the wrong type: Received `{type(params["quintic_interpolation"])}` expected `bool`')

def v_3d_tagalign_cargs(params: V3dTagalign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTagalign")
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append("-master")
    cargs.append(execution.input_file(params["master_dataset"]))
    v_0 = params.get("tagset_file")
    if v_0 is not None:
        cargs.append("-tagset")
        cargs.append(execution.input_file(v_0))
    if params.get("no_keep_tags"):
        cargs.append("-nokeeptags")
    v_1 = params.get("matvec_file")
    if v_1 is not None:
        cargs.append("-matvec")
        cargs.append(v_1)
    if params.get("rotate"):
        cargs.append("-rotate")
    if params.get("affine"):
        cargs.append("-affine")
    if params.get("rotscl"):
        cargs.append("-rotscl")
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("dummy"):
        cargs.append("-dummy")
    if params.get("linear_interpolation"):
        cargs.append("-linear")
    if params.get("cubic_interpolation"):
        cargs.append("-cubic")
    if params.get("nearest_neighbor_interpolation"):
        cargs.append("-NN")
    if params.get("quintic_interpolation"):
        cargs.append("-quintic")
    return cargs

def v_3d_tagalign_outputs(params: V3dTagalign, execution: Execution) -> V3dTagalignOutputs:
    """Build the V3dTagalignOutputs object for this tool."""
    output_dataset_head_v: OutputPathType | None = None
    output_dataset_brick_v: OutputPathType | None = None
    output_matvec_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_dataset_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_dataset_brick_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    __v2 = params.get("matvec_file")
    if __v2 is not None:
        output_matvec_file_v = execution.output_file((__v2 if __v2 is not None else ""))
    return V3dTagalignOutputs(
        root=root_v,
        output_dataset_head=output_dataset_head_v,
        output_dataset_brick=output_dataset_brick_v,
        output_matvec_file=output_matvec_file_v,
    )

def v_3d_tagalign_execute(params: V3dTagalign, runner: Runner | None = None) -> V3dTagalignOutputs:
    """
    3dTagalign

    Rotates/translates dataset 'dset' to be aligned with the master using the tagsets embedded in their .HEAD files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_tagalign_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TAGALIGN_METADATA)
    execution.params(params)
    args = v_3d_tagalign_cargs(params, execution)
    out = v_3d_tagalign_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_tagalign(
    input_dataset: InputPathType,
    master_dataset: InputPathType,
    tagset_file: InputPathType | None = None,
    no_keep_tags: bool = False,
    matvec_file: str | None = None,
    rotate: bool = False,
    affine: bool = False,
    rotscl: bool = False,
    prefix: str | None = None,
    verbose: bool = False,
    dummy: bool = False,
    linear_interpolation: bool = False,
    cubic_interpolation: bool = False,
    nearest_neighbor_interpolation: bool = False,
    quintic_interpolation: bool = False,
    runner: Runner | None = None,
) -> V3dTagalignOutputs:
    """
    3dTagalign

    Rotates/translates dataset 'dset' to be aligned with the master using the tagsets embedded in their .HEAD files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset to align
        master_dataset: Use dataset 'mset' as the master dataset. This option is\
            mandatory.
        tagset_file: Use the tagset in the .tag file instead of dset.
        no_keep_tags: Don't put transformed locations of dset's tags into the\
            output dataset [default = keep tags].
        matvec_file: Write the matrix+vector transformation to file 'mfile'. This\
            can be used with 3dWarp's '-matvec_in2out' option to align other\
            datasets in the same way (e.g., functional datasets).
        rotate: Compute the transformation as a rotation + shift (default).
        affine: Compute the transformation as a general affine map, where the\
            matrix is a general 3x3 matrix.
        rotscl: Compute transformation as a rotation times an isotropic scaling;\
            where matrix is an orthogonal matrix times a scalar.
        prefix: Specify the prefix for the output dataset.
        verbose: Print progress reports.
        dummy: Don't actually rotate the dataset, just compute the transformation\
            matrix and vector. If '-matvec' is used, the mfile will be written.
        linear_interpolation: Use linear interpolation method.
        cubic_interpolation: Use cubic interpolation method (default).
        nearest_neighbor_interpolation: Use nearest neighbour interpolation method.
        quintic_interpolation: Use quintic interpolation method.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_tagalign_params(
        input_dataset=input_dataset,
        master_dataset=master_dataset,
        tagset_file=tagset_file,
        no_keep_tags=no_keep_tags,
        matvec_file=matvec_file,
        rotate=rotate,
        affine=affine,
        rotscl=rotscl,
        prefix=prefix,
        verbose=verbose,
        dummy=dummy,
        linear_interpolation=linear_interpolation,
        cubic_interpolation=cubic_interpolation,
        nearest_neighbor_interpolation=nearest_neighbor_interpolation,
        quintic_interpolation=quintic_interpolation,
    )
    return v_3d_tagalign_execute(params, runner)

__all__ = [
    "V3dTagalign",
    "V3dTagalignOutputs",
    "V_3D_TAGALIGN_METADATA",
    "v_3d_tagalign_cargs",
    "v_3d_tagalign_outputs",
    "v_3d_tagalign_params",
    "v_3d_tagalign_execute",
    "v_3d_tagalign_validate",
    "v_3d_tagalign",
]