# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

STRBLAST_METADATA = Metadata(
    id="strblast",
    name="strblast",
    package="afni",
)

Strblast = typing.TypedDict(
    "Strblast",
    {
        "@type": typing.NotRequired[typing.Literal["afni/strblast"]],
        "targetstring": str,
        "input_files": list[InputPathType],
        "new_char": typing.NotRequired[str],
        "new_string": typing.NotRequired[str],
        "unescape": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class StrblastOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def strblast_params(
    targetstring: str,
    input_files: list[InputPathType],
    new_char: str | None = None,
    new_string: str | None = None,
    unescape: bool = False,
    quiet: bool = False,
    help: bool = False,
) -> Strblast:
    """
    Build parameters.

    Args:
        targetstring: Target string to search for in the input files.
        input_files: Input files to search for the target string.
        new_char: Replace TARGETSTRING with CHAR (repeated)
        new_string: Replace TARGETSTRING with STRING
        unescape: Parse TARGETSTRING for escaped characters (includes '\t', '\n',\
            '\r')
        quiet: Do not report files with no strings found. Use -quiet -quiet to\
            avoid any reporting.
        help: Show help message and exit.

    Returns:
        Parameter dictionary.
    """
    params: Strblast = {
        "@type": "afni/strblast",
        "targetstring": targetstring,
        "input_files": input_files,
        "unescape": unescape,
        "quiet": quiet,
        "help": help,
    }
    if new_char is not None:
        params["new_char"] = new_char
    if new_string is not None:
        params["new_string"] = new_string
    return params

def strblast_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Strblast."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("targetstring", None) is None:
        raise StyxValidationError("`targetstring` must not be None")
    if not isinstance(params["targetstring"], str):
        raise StyxValidationError(f'`targetstring` has the wrong type: Received `{type(params["targetstring"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("new_char", None) is not None:
        if not isinstance(params["new_char"], str):
            raise StyxValidationError(f'`new_char` has the wrong type: Received `{type(params["new_char"])}` expected `str`')
    if params.get("new_string", None) is not None:
        if not isinstance(params["new_string"], str):
            raise StyxValidationError(f'`new_string` has the wrong type: Received `{type(params["new_string"])}` expected `str`')
    if params.get("unescape", None) is not None:
        if not isinstance(params["unescape"], bool):
            raise StyxValidationError(f'`unescape` has the wrong type: Received `{type(params["unescape"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def strblast_cargs(params: Strblast, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("strblast")
    cargs.append(params["targetstring"])
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("new_char")
    if v_0 is not None:
        cargs.append("-new_char")
        cargs.append(v_0)
    v_1 = params.get("new_string")
    if v_1 is not None:
        cargs.append("-new_string")
        cargs.append(v_1)
    if params.get("unescape"):
        cargs.append("-unescape")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def strblast_outputs(params: Strblast, execution: Execution) -> StrblastOutputs:
    """Build the StrblastOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return StrblastOutputs(
        root=root_v,
    )

def strblast_execute(params: Strblast, runner: Runner | None = None) -> StrblastOutputs:
    """
    strblast

    Finds exact copies of the target string in each of the input files, and replaces all characters with some junk string.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    strblast_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(STRBLAST_METADATA)
    execution.params(params)
    args = strblast_cargs(params, execution)
    out = strblast_outputs(params, execution)
    execution.run(args)
    return out

def strblast(
    targetstring: str,
    input_files: list[InputPathType],
    new_char: str | None = None,
    new_string: str | None = None,
    unescape: bool = False,
    quiet: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> StrblastOutputs:
    """
    strblast

    Finds exact copies of the target string in each of the input files, and replaces all characters with some junk string.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        targetstring: Target string to search for in the input files.
        input_files: Input files to search for the target string.
        new_char: Replace TARGETSTRING with CHAR (repeated)
        new_string: Replace TARGETSTRING with STRING
        unescape: Parse TARGETSTRING for escaped characters (includes '\t', '\n',\
            '\r')
        quiet: Do not report files with no strings found. Use -quiet -quiet to\
            avoid any reporting.
        help: Show help message and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = strblast_params(
        targetstring=targetstring,
        input_files=input_files,
        new_char=new_char,
        new_string=new_string,
        unescape=unescape,
        quiet=quiet,
        help=help,
    )
    return strblast_execute(params, runner)

__all__ = [
    "Strblast",
    "StrblastOutputs",
    "STRBLAST_METADATA",
    "strblast_cargs",
    "strblast_outputs",
    "strblast_params",
    "strblast_execute",
    "strblast_validate",
    "strblast",
]