# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ZCUTUP_METADATA = Metadata(
    id="3dZcutup",
    name="3dZcutup",
    package="afni",
)

V3dZcutup = typing.TypedDict(
    "V3dZcutup",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dZcutup"]],
        "keep_slices": str,
        "prefix": typing.NotRequired[str],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dZcutupOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_head: OutputPathType | None
    """The output dataset HEAD file"""
    output_brik: OutputPathType | None
    """The output dataset BRIK file"""

def v_3d_zcutup_params(
    keep_slices: str,
    dataset: InputPathType,
    prefix: str | None = None,
) -> V3dZcutup:
    """
    Build parameters.

    Args:
        keep_slices: Keep slices numbered 'b' through 't', inclusive. This is a\
            mandatory option. Slice numbers start at 0.
        dataset: The input dataset (e.g., epi07+orig). You can use a sub-brick\
            selector on the input dataset.
        prefix: Write result into dataset with the given prefix [default =\
            'zcutup']

    Returns:
        Parameter dictionary.
    """
    params: V3dZcutup = {
        "@type": "afni/3dZcutup",
        "keep_slices": keep_slices,
        "dataset": dataset,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_zcutup_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dZcutup."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("keep_slices", None) is None:
        raise StyxValidationError("`keep_slices` must not be None")
    if not isinstance(params["keep_slices"], str):
        raise StyxValidationError(f'`keep_slices` has the wrong type: Received `{type(params["keep_slices"])}` expected `str`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def v_3d_zcutup_cargs(params: V3dZcutup, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dZcutup")
    cargs.append("-keep")
    cargs.append(params["keep_slices"])
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def v_3d_zcutup_outputs(params: V3dZcutup, execution: Execution) -> V3dZcutupOutputs:
    """Build the V3dZcutupOutputs object for this tool."""
    output_head_v: OutputPathType | None = None
    output_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    return V3dZcutupOutputs(
        root=root_v,
        output_head=output_head_v,
        output_brik=output_brik_v,
    )

def v_3d_zcutup_execute(params: V3dZcutup, runner: Runner | None = None) -> V3dZcutupOutputs:
    """
    3dZcutup

    Cut slices off a dataset in its z-direction and write a new dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_zcutup_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ZCUTUP_METADATA)
    execution.params(params)
    args = v_3d_zcutup_cargs(params, execution)
    out = v_3d_zcutup_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_zcutup(
    keep_slices: str,
    dataset: InputPathType,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dZcutupOutputs:
    """
    3dZcutup

    Cut slices off a dataset in its z-direction and write a new dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        keep_slices: Keep slices numbered 'b' through 't', inclusive. This is a\
            mandatory option. Slice numbers start at 0.
        dataset: The input dataset (e.g., epi07+orig). You can use a sub-brick\
            selector on the input dataset.
        prefix: Write result into dataset with the given prefix [default =\
            'zcutup']
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_zcutup_params(
        keep_slices=keep_slices,
        dataset=dataset,
        prefix=prefix,
    )
    return v_3d_zcutup_execute(params, runner)

__all__ = [
    "V3dZcutup",
    "V3dZcutupOutputs",
    "V_3D_ZCUTUP_METADATA",
    "v_3d_zcutup_cargs",
    "v_3d_zcutup_outputs",
    "v_3d_zcutup_params",
    "v_3d_zcutup_execute",
    "v_3d_zcutup_validate",
    "v_3d_zcutup",
]