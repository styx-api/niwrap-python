# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DMATCALC_METADATA = Metadata(
    id="3dmatcalc",
    name="3dmatcalc",
    package="afni",
)

V3dmatcalc = typing.TypedDict(
    "V3dmatcalc",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dmatcalc"]],
        "input_dataset": InputPathType,
        "input_matrix": InputPathType,
        "output_dataset": str,
        "mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class V3dmatcalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_header: OutputPathType
    """Output dataset header file."""
    output_brick: OutputPathType
    """Output dataset brick file."""

def v_3dmatcalc_params(
    input_dataset: InputPathType,
    input_matrix: InputPathType,
    output_dataset: str,
    mask: InputPathType | None = None,
) -> V3dmatcalc:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset to be processed.
        input_matrix: The matrix to be applied, specified as a .1D file or as an\
            expression in the syntax of 1dmatcalc.
        output_dataset: Prefix for the output dataset.
        mask: Apply the matrix only to voxels in the mask; other voxels will be set\
            to all zeroes.

    Returns:
        Parameter dictionary.
    """
    params: V3dmatcalc = {
        "@type": "afni/3dmatcalc",
        "input_dataset": input_dataset,
        "input_matrix": input_matrix,
        "output_dataset": output_dataset,
    }
    if mask is not None:
        params["mask"] = mask
    return params

def v_3dmatcalc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dmatcalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("input_matrix", None) is None:
        raise StyxValidationError("`input_matrix` must not be None")
    if not isinstance(params["input_matrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_matrix` has the wrong type: Received `{type(params["input_matrix"])}` expected `InputPathType`')
    if params.get("output_dataset", None) is None:
        raise StyxValidationError("`output_dataset` must not be None")
    if not isinstance(params["output_dataset"], str):
        raise StyxValidationError(f'`output_dataset` has the wrong type: Received `{type(params["output_dataset"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')

def v_3dmatcalc_cargs(params: V3dmatcalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dmatcalc")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append("-matrix")
    cargs.append(execution.input_file(params["input_matrix"]))
    cargs.append("-prefix")
    cargs.append(params["output_dataset"])
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    return cargs

def v_3dmatcalc_outputs(params: V3dmatcalc, execution: Execution) -> V3dmatcalcOutputs:
    """Build the V3dmatcalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_header_v: OutputPathType = execution.output_file(f'{params["output_dataset"]}+tlrc.HEAD')
    output_brick_v: OutputPathType = execution.output_file(f'{params["output_dataset"]}+tlrc.BRIK')
    return V3dmatcalcOutputs(
        root=root_v,
        output_header=output_header_v,
        output_brick=output_brick_v,
    )

def v_3dmatcalc_execute(params: V3dmatcalc, runner: Runner | None = None) -> V3dmatcalcOutputs:
    """
    3dmatcalc

    Apply a matrix to a dataset, voxel-by-voxel, to produce a new dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dmatcalc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DMATCALC_METADATA)
    execution.params(params)
    args = v_3dmatcalc_cargs(params, execution)
    out = v_3dmatcalc_outputs(params, execution)
    execution.run(args)
    return out

def v_3dmatcalc(
    input_dataset: InputPathType,
    input_matrix: InputPathType,
    output_dataset: str,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dmatcalcOutputs:
    """
    3dmatcalc

    Apply a matrix to a dataset, voxel-by-voxel, to produce a new dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset to be processed.
        input_matrix: The matrix to be applied, specified as a .1D file or as an\
            expression in the syntax of 1dmatcalc.
        output_dataset: Prefix for the output dataset.
        mask: Apply the matrix only to voxels in the mask; other voxels will be set\
            to all zeroes.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dmatcalc_params(
        input_dataset=input_dataset,
        input_matrix=input_matrix,
        output_dataset=output_dataset,
        mask=mask,
    )
    return v_3dmatcalc_execute(params, runner)

__all__ = [
    "V3dmatcalc",
    "V3dmatcalcOutputs",
    "V_3DMATCALC_METADATA",
    "v_3dmatcalc_cargs",
    "v_3dmatcalc_outputs",
    "v_3dmatcalc_params",
    "v_3dmatcalc_execute",
    "v_3dmatcalc_validate",
    "v_3dmatcalc",
]