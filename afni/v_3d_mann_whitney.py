# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_MANN_WHITNEY_METADATA = Metadata(
    id="3dMannWhitney",
    name="3dMannWhitney",
    package="afni",
)

V3dMannWhitney = typing.TypedDict(
    "V3dMannWhitney",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dMannWhitney"]],
        "dset1_x": list[str],
        "dset2_y": list[str],
        "output_prefix": str,
        "workmem": typing.NotRequired[int],
        "voxel_num": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class V3dMannWhitneyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_mann_whitney_params(
    dset1_x: list[str],
    dset2_y: list[str],
    output_prefix: str,
    workmem: int | None = None,
    voxel_num: int | None = None,
) -> V3dMannWhitney:
    """
    Build parameters.

    Args:
        dset1_x: Data set for X observations. Must specify 1 and only 1 sub-brick.
        dset2_y: Data set for Y observations. Must specify 1 and only 1 sub-brick.
        output_prefix: Estimated population delta and Wilcoxon-Mann-Whitney\
            statistics written to file.
        workmem: Number of megabytes of RAM to use for statistical workspace.
        voxel_num: Screen output for voxel # num.

    Returns:
        Parameter dictionary.
    """
    params: V3dMannWhitney = {
        "@type": "afni/3dMannWhitney",
        "dset1_x": dset1_x,
        "dset2_y": dset2_y,
        "output_prefix": output_prefix,
    }
    if workmem is not None:
        params["workmem"] = workmem
    if voxel_num is not None:
        params["voxel_num"] = voxel_num
    return params

def v_3d_mann_whitney_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dMannWhitney."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset1_x", None) is None:
        raise StyxValidationError("`dset1_x` must not be None")
    if not isinstance(params["dset1_x"], list):
        raise StyxValidationError(f'`dset1_x` has the wrong type: Received `{type(params["dset1_x"])}` expected `list[str]`')
    for e in params["dset1_x"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`dset1_x` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("dset2_y", None) is None:
        raise StyxValidationError("`dset2_y` must not be None")
    if not isinstance(params["dset2_y"], list):
        raise StyxValidationError(f'`dset2_y` has the wrong type: Received `{type(params["dset2_y"])}` expected `list[str]`')
    for e_2 in params["dset2_y"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`dset2_y` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("workmem", None) is not None:
        if not isinstance(params["workmem"], int):
            raise StyxValidationError(f'`workmem` has the wrong type: Received `{type(params["workmem"])}` expected `int`')
    if params.get("voxel_num", None) is not None:
        if not isinstance(params["voxel_num"], int):
            raise StyxValidationError(f'`voxel_num` has the wrong type: Received `{type(params["voxel_num"])}` expected `int`')

def v_3d_mann_whitney_cargs(params: V3dMannWhitney, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dMannWhitney")
    cargs.append("-dset 1")
    for item0 in params["dset1_x"]:
        cargs.append(item0)
    cargs.append("-dset 2")
    for item1 in params["dset2_y"]:
        cargs.append(item1)
    cargs.append("-out")
    cargs.append(params["output_prefix"])
    v_0 = params.get("workmem")
    if v_0 is not None:
        cargs.append("-workmem")
        cargs.append(str(v_0))
    v_1 = params.get("voxel_num")
    if v_1 is not None:
        cargs.append("-voxel")
        cargs.append(str(v_1))
    return cargs

def v_3d_mann_whitney_outputs(params: V3dMannWhitney, execution: Execution) -> V3dMannWhitneyOutputs:
    """Build the V3dMannWhitneyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dMannWhitneyOutputs(
        root=root_v,
    )

def v_3d_mann_whitney_execute(params: V3dMannWhitney, runner: Runner | None = None) -> V3dMannWhitneyOutputs:
    """
    3dMannWhitney

    Performs nonparametric Mann-Whitney two-sample test.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_mann_whitney_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_MANN_WHITNEY_METADATA)
    execution.params(params)
    args = v_3d_mann_whitney_cargs(params, execution)
    out = v_3d_mann_whitney_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_mann_whitney(
    dset1_x: list[str],
    dset2_y: list[str],
    output_prefix: str,
    workmem: int | None = None,
    voxel_num: int | None = None,
    runner: Runner | None = None,
) -> V3dMannWhitneyOutputs:
    """
    3dMannWhitney

    Performs nonparametric Mann-Whitney two-sample test.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset1_x: Data set for X observations. Must specify 1 and only 1 sub-brick.
        dset2_y: Data set for Y observations. Must specify 1 and only 1 sub-brick.
        output_prefix: Estimated population delta and Wilcoxon-Mann-Whitney\
            statistics written to file.
        workmem: Number of megabytes of RAM to use for statistical workspace.
        voxel_num: Screen output for voxel # num.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_mann_whitney_params(
        dset1_x=dset1_x,
        dset2_y=dset2_y,
        output_prefix=output_prefix,
        workmem=workmem,
        voxel_num=voxel_num,
    )
    return v_3d_mann_whitney_execute(params, runner)

__all__ = [
    "V3dMannWhitney",
    "V3dMannWhitneyOutputs",
    "V_3D_MANN_WHITNEY_METADATA",
    "v_3d_mann_whitney_cargs",
    "v_3d_mann_whitney_outputs",
    "v_3d_mann_whitney_params",
    "v_3d_mann_whitney_execute",
    "v_3d_mann_whitney_validate",
    "v_3d_mann_whitney",
]