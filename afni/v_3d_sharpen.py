# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_SHARPEN_METADATA = Metadata(
    id="3dSharpen",
    name="3dSharpen",
    package="afni",
)

V3dSharpen = typing.TypedDict(
    "V3dSharpen",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dSharpen"]],
        "sharpening_factor": typing.NotRequired[float],
        "input_dataset": InputPathType,
        "output_prefix": str,
    },
)

@dataclasses.dataclass
class V3dSharpenOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType
    """Sharpened output dataset."""

def v_3d_sharpen_params(
    input_dataset: InputPathType,
    output_prefix: str,
    sharpening_factor: float | None = None,
) -> V3dSharpen:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (e.g., input.nii.gz)
        output_prefix: Name of the output dataset (e.g., output.nii.gz) which will\
            be in floating point format.
        sharpening_factor: Sharpening factor, between 0.1 and 0.9 (inclusive).\
            Larger values mean more sharpening.

    Returns:
        Parameter dictionary.
    """
    params: V3dSharpen = {
        "@type": "afni/3dSharpen",
        "input_dataset": input_dataset,
        "output_prefix": output_prefix,
    }
    if sharpening_factor is not None:
        params["sharpening_factor"] = sharpening_factor
    return params

def v_3d_sharpen_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dSharpen."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("sharpening_factor", None) is not None:
        if not isinstance(params["sharpening_factor"], (float, int)):
            raise StyxValidationError(f'`sharpening_factor` has the wrong type: Received `{type(params["sharpening_factor"])}` expected `float`')
        if not (0.1 <= params["sharpening_factor"] <= 0.9):
            raise StyxValidationError("Parameter `sharpening_factor` must be between 0.1 and 0.9 (inclusive)")
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def v_3d_sharpen_cargs(params: V3dSharpen, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dSharpen")
    v_0 = params.get("sharpening_factor")
    if v_0 is not None:
        cargs.append("-phi")
        cargs.append(str(v_0))
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append("-prefix")
    cargs.append(params["output_prefix"])
    return cargs

def v_3d_sharpen_outputs(params: V3dSharpen, execution: Execution) -> V3dSharpenOutputs:
    """Build the V3dSharpenOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dataset_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}.nii.gz')
    return V3dSharpenOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def v_3d_sharpen_execute(params: V3dSharpen, runner: Runner | None = None) -> V3dSharpenOutputs:
    """
    3dSharpen

    Applies a simple 3D sharpening filter to the positive values in the #0 volume of the input dataset, and writes out a new dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_sharpen_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_SHARPEN_METADATA)
    execution.params(params)
    args = v_3d_sharpen_cargs(params, execution)
    out = v_3d_sharpen_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_sharpen(
    input_dataset: InputPathType,
    output_prefix: str,
    sharpening_factor: float | None = None,
    runner: Runner | None = None,
) -> V3dSharpenOutputs:
    """
    3dSharpen

    Applies a simple 3D sharpening filter to the positive values in the #0 volume of the input dataset, and writes out a new dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (e.g., input.nii.gz)
        output_prefix: Name of the output dataset (e.g., output.nii.gz) which will\
            be in floating point format.
        sharpening_factor: Sharpening factor, between 0.1 and 0.9 (inclusive).\
            Larger values mean more sharpening.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_sharpen_params(
        input_dataset=input_dataset,
        output_prefix=output_prefix,
        sharpening_factor=sharpening_factor,
    )
    return v_3d_sharpen_execute(params, runner)

__all__ = [
    "V3dSharpen",
    "V3dSharpenOutputs",
    "V_3D_SHARPEN_METADATA",
    "v_3d_sharpen_cargs",
    "v_3d_sharpen_outputs",
    "v_3d_sharpen_params",
    "v_3d_sharpen_execute",
    "v_3d_sharpen_validate",
    "v_3d_sharpen",
]