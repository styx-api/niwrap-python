# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOL_CENTER_METADATA = Metadata(
    id="@VolCenter",
    name="@VolCenter",
    package="afni",
)

VolCenter = typing.TypedDict(
    "VolCenter",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@VolCenter"]],
        "dset": InputPathType,
        "orient": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class VolCenterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def vol_center_params(
    dset: InputPathType,
    orient: str | None = None,
) -> VolCenter:
    """
    Build parameters.

    Args:
        dset: Input volume dataset
        orient: Output coordinate system orientation (e.g., RAI)

    Returns:
        Parameter dictionary.
    """
    params: VolCenter = {
        "@type": "afni/@VolCenter",
        "dset": dset,
    }
    if orient is not None:
        params["orient"] = orient
    return params

def vol_center_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid VolCenter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')
    if params.get("orient", None) is not None:
        if not isinstance(params["orient"], str):
            raise StyxValidationError(f'`orient` has the wrong type: Received `{type(params["orient"])}` expected `str`')

def vol_center_cargs(params: VolCenter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@VolCenter")
    cargs.append("-dset")
    cargs.append(execution.input_file(params["dset"]))
    v_0 = params.get("orient")
    if v_0 is not None:
        cargs.append("-or")
        cargs.append(v_0)
    return cargs

def vol_center_outputs(params: VolCenter, execution: Execution) -> VolCenterOutputs:
    """Build the VolCenterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return VolCenterOutputs(
        root=root_v,
    )

def vol_center_execute(params: VolCenter, runner: Runner | None = None) -> VolCenterOutputs:
    """
    @VolCenter

    Tool to return the center of volume for a given dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vol_center_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOL_CENTER_METADATA)
    execution.params(params)
    args = vol_center_cargs(params, execution)
    out = vol_center_outputs(params, execution)
    execution.run(args)
    return out

def vol_center(
    dset: InputPathType,
    orient: str | None = None,
    runner: Runner | None = None,
) -> VolCenterOutputs:
    """
    @VolCenter

    Tool to return the center of volume for a given dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset: Input volume dataset
        orient: Output coordinate system orientation (e.g., RAI)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vol_center_params(
        dset=dset,
        orient=orient,
    )
    return vol_center_execute(params, runner)

__all__ = [
    "VolCenter",
    "VolCenterOutputs",
    "VOL_CENTER_METADATA",
    "vol_center_cargs",
    "vol_center_outputs",
    "vol_center_params",
    "vol_center_execute",
    "vol_center_validate",
    "vol_center",
]