# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAST_ROI_METADATA = Metadata(
    id="@fast_roi",
    name="@fast_roi",
    package="afni",
)

FastRoi = typing.TypedDict(
    "FastRoi",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@fast_roi"]],
        "region": list[str],
        "drawn_roi": typing.NotRequired[InputPathType],
        "anat": InputPathType,
        "anat_ns": typing.NotRequired[InputPathType],
        "base": InputPathType,
        "roi_grid": InputPathType,
        "prefix": str,
        "time": typing.NotRequired[bool],
        "twopass": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FastRoiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    roi_output: OutputPathType
    """ROI output volume with the specified prefix."""

def fast_roi_params(
    region: list[str],
    anat: InputPathType,
    base: InputPathType,
    roi_grid: InputPathType,
    prefix: str,
    drawn_roi: InputPathType | None = None,
    anat_ns: InputPathType | None = None,
    time: bool = False,
    twopass: bool = False,
    help: bool = False,
) -> FastRoi:
    """
    Build parameters.

    Args:
        region: Symbolic atlas-based region name. Use repeated instances to specify\
            a mask of numerous regions. Each region is assigned a power of 2\
            integer in the output mask.
        anat: ANAT is the volume to be put in standard space. If ANAT is already in\
            TLRC space, there is no need for -base option.
        base: Name of the reference TLRC volume.
        roi_grid: The volume that defines the final ROI's grid.
        prefix: Prefix used to tag the names the ROIs output.
        drawn_roi: A user drawn ROI in standard (tlrc) space. This ROI gets added\
            with the REGION ROI.
        anat_ns: Same as -anat, but indicates that the skull has been removed\
            already.
        time: Output elapsed time reports.
        twopass: Make TLRC transformation more robust. Use it if TLRC transform\
            step fails.
        help: Output help message.

    Returns:
        Parameter dictionary.
    """
    params: FastRoi = {
        "@type": "afni/@fast_roi",
        "region": region,
        "anat": anat,
        "base": base,
        "roi_grid": roi_grid,
        "prefix": prefix,
        "time": time,
        "twopass": twopass,
        "help": help,
    }
    if drawn_roi is not None:
        params["drawn_roi"] = drawn_roi
    if anat_ns is not None:
        params["anat_ns"] = anat_ns
    return params

def fast_roi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FastRoi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("region", None) is None:
        raise StyxValidationError("`region` must not be None")
    if not isinstance(params["region"], list):
        raise StyxValidationError(f'`region` has the wrong type: Received `{type(params["region"])}` expected `list[str]`')
    for e in params["region"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`region` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("drawn_roi", None) is not None:
        if not isinstance(params["drawn_roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`drawn_roi` has the wrong type: Received `{type(params["drawn_roi"])}` expected `InputPathType`')
    if params.get("anat", None) is None:
        raise StyxValidationError("`anat` must not be None")
    if not isinstance(params["anat"], (pathlib.Path, str)):
        raise StyxValidationError(f'`anat` has the wrong type: Received `{type(params["anat"])}` expected `InputPathType`')
    if params.get("anat_ns", None) is not None:
        if not isinstance(params["anat_ns"], (pathlib.Path, str)):
            raise StyxValidationError(f'`anat_ns` has the wrong type: Received `{type(params["anat_ns"])}` expected `InputPathType`')
    if params.get("base", None) is None:
        raise StyxValidationError("`base` must not be None")
    if not isinstance(params["base"], (pathlib.Path, str)):
        raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `InputPathType`')
    if params.get("roi_grid", None) is None:
        raise StyxValidationError("`roi_grid` must not be None")
    if not isinstance(params["roi_grid"], (pathlib.Path, str)):
        raise StyxValidationError(f'`roi_grid` has the wrong type: Received `{type(params["roi_grid"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("time", None) is not None:
        if not isinstance(params["time"], bool):
            raise StyxValidationError(f'`time` has the wrong type: Received `{type(params["time"])}` expected `bool`')
    if params.get("twopass", None) is not None:
        if not isinstance(params["twopass"], bool):
            raise StyxValidationError(f'`twopass` has the wrong type: Received `{type(params["twopass"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def fast_roi_cargs(params: FastRoi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@fast_roi")
    cargs.append("-region")
    for item0 in params["region"]:
        cargs.append(item0)
    v_0 = params.get("drawn_roi")
    if v_0 is not None:
        cargs.append("-drawn_roi")
        cargs.append(execution.input_file(v_0))
    cargs.append("-anat")
    cargs.append(execution.input_file(params["anat"]))
    v_1 = params.get("anat_ns")
    if v_1 is not None:
        cargs.append("-anat_ns")
        cargs.append(execution.input_file(v_1))
    cargs.append("-base")
    cargs.append(execution.input_file(params["base"]))
    cargs.append("-roi_grid")
    cargs.append(execution.input_file(params["roi_grid"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("time"):
        cargs.append("--time")
    if params.get("twopass"):
        cargs.append("--twopass")
    if params.get("help"):
        cargs.append("--help")
    return cargs

def fast_roi_outputs(params: FastRoi, execution: Execution) -> FastRoiOutputs:
    """Build the FastRoiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    roi_output_v: OutputPathType = execution.output_file(f'ROI.{params["prefix"]}+orig')
    return FastRoiOutputs(
        root=root_v,
        roi_output=roi_output_v,
    )

def fast_roi_execute(params: FastRoi, runner: Runner | None = None) -> FastRoiOutputs:
    """
    @fast_roi

    Creates Atlas-based ROI masked in ANAT's original space. The script executes rapidly for realtime fMRI applications.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fast_roi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAST_ROI_METADATA)
    execution.params(params)
    args = fast_roi_cargs(params, execution)
    out = fast_roi_outputs(params, execution)
    execution.run(args)
    return out

def fast_roi(
    region: list[str],
    anat: InputPathType,
    base: InputPathType,
    roi_grid: InputPathType,
    prefix: str,
    drawn_roi: InputPathType | None = None,
    anat_ns: InputPathType | None = None,
    time: bool = False,
    twopass: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> FastRoiOutputs:
    """
    @fast_roi

    Creates Atlas-based ROI masked in ANAT's original space. The script executes rapidly for realtime fMRI applications.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        region: Symbolic atlas-based region name. Use repeated instances to specify\
            a mask of numerous regions. Each region is assigned a power of 2\
            integer in the output mask.
        anat: ANAT is the volume to be put in standard space. If ANAT is already in\
            TLRC space, there is no need for -base option.
        base: Name of the reference TLRC volume.
        roi_grid: The volume that defines the final ROI's grid.
        prefix: Prefix used to tag the names the ROIs output.
        drawn_roi: A user drawn ROI in standard (tlrc) space. This ROI gets added\
            with the REGION ROI.
        anat_ns: Same as -anat, but indicates that the skull has been removed\
            already.
        time: Output elapsed time reports.
        twopass: Make TLRC transformation more robust. Use it if TLRC transform\
            step fails.
        help: Output help message.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fast_roi_params(
        region=region,
        anat=anat,
        base=base,
        roi_grid=roi_grid,
        prefix=prefix,
        drawn_roi=drawn_roi,
        anat_ns=anat_ns,
        time=time,
        twopass=twopass,
        help=help,
    )
    return fast_roi_execute(params, runner)

__all__ = [
    "FastRoi",
    "FastRoiOutputs",
    "FAST_ROI_METADATA",
    "fast_roi_cargs",
    "fast_roi_outputs",
    "fast_roi_params",
    "fast_roi_execute",
    "fast_roi_validate",
    "fast_roi",
]