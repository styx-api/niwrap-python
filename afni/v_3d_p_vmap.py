# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_P_VMAP_METADATA = Metadata(
    id="3dPVmap",
    name="3dPVmap",
    package="afni",
)

V3dPVmap = typing.TypedDict(
    "V3dPVmap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dPVmap"]],
        "prefix": typing.NotRequired[str],
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[bool],
        "inputdataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dPVmapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outbrik: OutputPathType | None
    """Output PVmap file"""
    outhead: OutputPathType | None
    """Output PVmap header file"""
    pc_vectors: OutputPathType | None
    """Principal component time series vectors"""

def v_3d_p_vmap_params(
    inputdataset: InputPathType,
    prefix: str | None = None,
    mask: InputPathType | None = None,
    automask: bool = False,
) -> V3dPVmap:
    """
    Build parameters.

    Args:
        inputdataset: Input dataset (e.g., fred.nii)
        prefix: Output prefix for generated files
        mask: Mask dataset (e.g., brainmask.nii)
        automask: Automatically generate a mask from the input dataset

    Returns:
        Parameter dictionary.
    """
    params: V3dPVmap = {
        "@type": "afni/3dPVmap",
        "inputdataset": inputdataset,
        "automask": automask,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if mask is not None:
        params["mask"] = mask
    return params

def v_3d_p_vmap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dPVmap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("inputdataset", None) is None:
        raise StyxValidationError("`inputdataset` must not be None")
    if not isinstance(params["inputdataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputdataset` has the wrong type: Received `{type(params["inputdataset"])}` expected `InputPathType`')

def v_3d_p_vmap_cargs(params: V3dPVmap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dPVmap")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    if params.get("automask"):
        cargs.append("-automask")
    cargs.append(execution.input_file(params["inputdataset"]))
    return cargs

def v_3d_p_vmap_outputs(params: V3dPVmap, execution: Execution) -> V3dPVmapOutputs:
    """Build the V3dPVmapOutputs object for this tool."""
    outbrik_v: OutputPathType | None = None
    outhead_v: OutputPathType | None = None
    pc_vectors_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        outbrik_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.BRIK')
    __v1 = params.get("prefix")
    if __v1 is not None:
        outhead_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.HEAD')
    __v2 = params.get("prefix")
    if __v2 is not None:
        pc_vectors_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.1D')
    return V3dPVmapOutputs(
        root=root_v,
        outbrik=outbrik_v,
        outhead=outhead_v,
        pc_vectors=pc_vectors_v,
    )

def v_3d_p_vmap_execute(params: V3dPVmap, runner: Runner | None = None) -> V3dPVmapOutputs:
    """
    3dPVmap

    Computes the first two principal component vectors of a time series dataset, then outputs the R-squared coefficient of each voxel time series with these first two components.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_p_vmap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_P_VMAP_METADATA)
    execution.params(params)
    args = v_3d_p_vmap_cargs(params, execution)
    out = v_3d_p_vmap_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_p_vmap(
    inputdataset: InputPathType,
    prefix: str | None = None,
    mask: InputPathType | None = None,
    automask: bool = False,
    runner: Runner | None = None,
) -> V3dPVmapOutputs:
    """
    3dPVmap

    Computes the first two principal component vectors of a time series dataset, then outputs the R-squared coefficient of each voxel time series with these first two components.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inputdataset: Input dataset (e.g., fred.nii)
        prefix: Output prefix for generated files
        mask: Mask dataset (e.g., brainmask.nii)
        automask: Automatically generate a mask from the input dataset
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_p_vmap_params(
        inputdataset=inputdataset,
        prefix=prefix,
        mask=mask,
        automask=automask,
    )
    return v_3d_p_vmap_execute(params, runner)

__all__ = [
    "V3dPVmap",
    "V3dPVmapOutputs",
    "V_3D_P_VMAP_METADATA",
    "v_3d_p_vmap_cargs",
    "v_3d_p_vmap_outputs",
    "v_3d_p_vmap_params",
    "v_3d_p_vmap_execute",
    "v_3d_p_vmap_validate",
    "v_3d_p_vmap",
]