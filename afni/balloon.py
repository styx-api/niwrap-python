# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BALLOON_METADATA = Metadata(
    id="balloon",
    name="balloon",
    package="afni",
)

Balloon = typing.TypedDict(
    "Balloon",
    {
        "@type": typing.NotRequired[typing.Literal["afni/balloon"]],
        "tr": float,
        "num_scans": int,
        "event_times": InputPathType,
        "t_rise": typing.NotRequired[list[float]],
        "t_fall": typing.NotRequired[list[float]],
        "t_sustain": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class BalloonOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def balloon_params(
    tr: float,
    num_scans: int,
    event_times: InputPathType,
    t_rise: list[float] | None = None,
    t_fall: list[float] | None = None,
    t_sustain: list[float] | None = None,
) -> Balloon:
    """
    Build parameters.

    Args:
        tr: Scan repetition time in seconds (TR), the interval at which the output\
            curve will be sampled.
        num_scans: Number of scans (N), the output curve will comprise this number\
            of samples.
        event_times: The name of a file containing the event timings, in seconds,\
            as ASCII strings separated by white space, with time 0 being the time\
            at which the initial scan occurred.
        t_rise: Haemodynamic rise time in seconds (typically between 4s and 6s).
        t_fall: Haemodynamic fall time in seconds (typically between 4s and 6s).
        t_sustain: Haemodynamic sustain time in seconds (typically between 0s and\
            4s).

    Returns:
        Parameter dictionary.
    """
    params: Balloon = {
        "@type": "afni/balloon",
        "tr": tr,
        "num_scans": num_scans,
        "event_times": event_times,
    }
    if t_rise is not None:
        params["t_rise"] = t_rise
    if t_fall is not None:
        params["t_fall"] = t_fall
    if t_sustain is not None:
        params["t_sustain"] = t_sustain
    return params

def balloon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Balloon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tr", None) is None:
        raise StyxValidationError("`tr` must not be None")
    if not isinstance(params["tr"], (float, int)):
        raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("num_scans", None) is None:
        raise StyxValidationError("`num_scans` must not be None")
    if not isinstance(params["num_scans"], int):
        raise StyxValidationError(f'`num_scans` has the wrong type: Received `{type(params["num_scans"])}` expected `int`')
    if params.get("event_times", None) is None:
        raise StyxValidationError("`event_times` must not be None")
    if not isinstance(params["event_times"], (pathlib.Path, str)):
        raise StyxValidationError(f'`event_times` has the wrong type: Received `{type(params["event_times"])}` expected `InputPathType`')
    if params.get("t_rise", None) is not None:
        if not isinstance(params["t_rise"], list):
            raise StyxValidationError(f'`t_rise` has the wrong type: Received `{type(params["t_rise"])}` expected `list[float]`')
        if len(params["t_rise"]) > 1:
            raise StyxValidationError("Parameter `t_rise` must contain at most 1 element")
        for e in params["t_rise"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`t_rise` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("t_fall", None) is not None:
        if not isinstance(params["t_fall"], list):
            raise StyxValidationError(f'`t_fall` has the wrong type: Received `{type(params["t_fall"])}` expected `list[float]`')
        if len(params["t_fall"]) > 1:
            raise StyxValidationError("Parameter `t_fall` must contain at most 1 element")
        for e_2 in params["t_fall"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`t_fall` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("t_sustain", None) is not None:
        if not isinstance(params["t_sustain"], list):
            raise StyxValidationError(f'`t_sustain` has the wrong type: Received `{type(params["t_sustain"])}` expected `list[float]`')
        if len(params["t_sustain"]) > 1:
            raise StyxValidationError("Parameter `t_sustain` must contain at most 1 element")
        for e_3 in params["t_sustain"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`t_sustain` has the wrong type: Received `{type(e_3)}` expected `list[float]`')

def balloon_cargs(params: Balloon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("balloon")
    cargs.append(str(params["tr"]))
    cargs.append(str(params["num_scans"]))
    cargs.append(execution.input_file(params["event_times"]))
    v_0 = params.get("t_rise")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(str(item0))
    v_1 = params.get("t_fall")
    if v_1 is not None:
        for item1 in v_1:
            cargs.append(str(item1))
    v_2 = params.get("t_sustain")
    if v_2 is not None:
        for item2 in v_2:
            cargs.append(str(item2))
    return cargs

def balloon_outputs(params: Balloon, execution: Execution) -> BalloonOutputs:
    """Build the BalloonOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BalloonOutputs(
        root=root_v,
    )

def balloon_execute(params: Balloon, runner: Runner | None = None) -> BalloonOutputs:
    """
    balloon

    Simulation of haemodynamic response using the balloon model. Based on the theoretical model proposed by Buxton et al. (1998).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    balloon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BALLOON_METADATA)
    execution.params(params)
    args = balloon_cargs(params, execution)
    out = balloon_outputs(params, execution)
    execution.run(args)
    return out

def balloon(
    tr: float,
    num_scans: int,
    event_times: InputPathType,
    t_rise: list[float] | None = None,
    t_fall: list[float] | None = None,
    t_sustain: list[float] | None = None,
    runner: Runner | None = None,
) -> BalloonOutputs:
    """
    balloon

    Simulation of haemodynamic response using the balloon model. Based on the theoretical model proposed by Buxton et al. (1998).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        tr: Scan repetition time in seconds (TR), the interval at which the output\
            curve will be sampled.
        num_scans: Number of scans (N), the output curve will comprise this number\
            of samples.
        event_times: The name of a file containing the event timings, in seconds,\
            as ASCII strings separated by white space, with time 0 being the time\
            at which the initial scan occurred.
        t_rise: Haemodynamic rise time in seconds (typically between 4s and 6s).
        t_fall: Haemodynamic fall time in seconds (typically between 4s and 6s).
        t_sustain: Haemodynamic sustain time in seconds (typically between 0s and\
            4s).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = balloon_params(
        tr=tr,
        num_scans=num_scans,
        event_times=event_times,
        t_rise=t_rise,
        t_fall=t_fall,
        t_sustain=t_sustain,
    )
    return balloon_execute(params, runner)

__all__ = [
    "Balloon",
    "BalloonOutputs",
    "BALLOON_METADATA",
    "balloon_cargs",
    "balloon_outputs",
    "balloon_params",
    "balloon_execute",
    "balloon_validate",
    "balloon",
]