# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NO_POUND_METADATA = Metadata(
    id="@NoPound",
    name="@NoPound",
    package="afni",
)

NoPound = typing.TypedDict(
    "NoPound",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@NoPound"]],
        "afni_files": list[str],
    },
)

@dataclasses.dataclass
class NoPoundOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def no_pound_params(
    afni_files: list[str],
) -> NoPound:
    """
    Build parameters.

    Args:
        afni_files: List of AFNI files where # characters should be replaced with\
            -.

    Returns:
        Parameter dictionary.
    """
    params: NoPound = {
        "@type": "afni/@NoPound",
        "afni_files": afni_files,
    }
    return params

def no_pound_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NoPound."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("afni_files", None) is None:
        raise StyxValidationError("`afni_files` must not be None")
    if not isinstance(params["afni_files"], list):
        raise StyxValidationError(f'`afni_files` has the wrong type: Received `{type(params["afni_files"])}` expected `list[str]`')
    for e in params["afni_files"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`afni_files` has the wrong type: Received `{type(e)}` expected `list[str]`')

def no_pound_cargs(params: NoPound, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@NoPound")
    for item0 in params["afni_files"]:
        cargs.append(item0)
    return cargs

def no_pound_outputs(params: NoPound, execution: Execution) -> NoPoundOutputs:
    """Build the NoPoundOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return NoPoundOutputs(
        root=root_v,
    )

def no_pound_execute(params: NoPound, runner: Runner | None = None) -> NoPoundOutputs:
    """
    @NoPound

    Replaces all # characters in AFNI filenames with a -.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    no_pound_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NO_POUND_METADATA)
    execution.params(params)
    args = no_pound_cargs(params, execution)
    out = no_pound_outputs(params, execution)
    execution.run(args)
    return out

def no_pound(
    afni_files: list[str],
    runner: Runner | None = None,
) -> NoPoundOutputs:
    """
    @NoPound

    Replaces all # characters in AFNI filenames with a -.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        afni_files: List of AFNI files where # characters should be replaced with\
            -.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = no_pound_params(
        afni_files=afni_files,
    )
    return no_pound_execute(params, runner)

__all__ = [
    "NoPound",
    "NoPoundOutputs",
    "NO_POUND_METADATA",
    "no_pound_cargs",
    "no_pound_outputs",
    "no_pound_params",
    "no_pound_execute",
    "no_pound_validate",
    "no_pound",
]