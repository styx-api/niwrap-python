# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QUICKSPEC_METADATA = Metadata(
    id="quickspec",
    name="quickspec",
    package="afni",
)

Quickspec = typing.TypedDict(
    "Quickspec",
    {
        "@type": typing.NotRequired[typing.Literal["afni/quickspec"]],
        "tn": list[str],
        "tsn": list[str],
        "tsnad": typing.NotRequired[list[str]],
        "tsnadm": typing.NotRequired[list[str]],
        "tsnadl": typing.NotRequired[list[str]],
        "spec": typing.NotRequired[str],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class QuickspecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_specfile: OutputPathType | None
    """The spec file output."""

def quickspec_params(
    tn: list[str],
    tsn: list[str],
    tsnad: list[str] | None = None,
    tsnadm: list[str] | None = None,
    tsnadl: list[str] | None = None,
    spec: str | None = None,
    help: bool = False,
) -> Quickspec:
    """
    Build parameters.

    Args:
        tn: Specify surface type and name.
        tsn: Specify surface type, state, and name.
        tsnad: Specify surface type, state, name, anatomical correctness, and Local\
            Domain Parent.
        tsnadm: Specify surface type, state, name, anatomical correctness, Local\
            Domain Parent, and node marker file.
        tsnadl: Specify surface type, state, name, anatomical correctness, Local\
            Domain Parent, and label dataset file.
        spec: Name of spec file output. Default is quick.spec.
        help: Display help message.

    Returns:
        Parameter dictionary.
    """
    params: Quickspec = {
        "@type": "afni/quickspec",
        "tn": tn,
        "tsn": tsn,
        "help": help,
    }
    if tsnad is not None:
        params["tsnad"] = tsnad
    if tsnadm is not None:
        params["tsnadm"] = tsnadm
    if tsnadl is not None:
        params["tsnadl"] = tsnadl
    if spec is not None:
        params["spec"] = spec
    return params

def quickspec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Quickspec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tn", None) is None:
        raise StyxValidationError("`tn` must not be None")
    if not isinstance(params["tn"], list):
        raise StyxValidationError(f'`tn` has the wrong type: Received `{type(params["tn"])}` expected `list[str]`')
    for e in params["tn"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`tn` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("tsn", None) is None:
        raise StyxValidationError("`tsn` must not be None")
    if not isinstance(params["tsn"], list):
        raise StyxValidationError(f'`tsn` has the wrong type: Received `{type(params["tsn"])}` expected `list[str]`')
    for e_2 in params["tsn"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`tsn` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("tsnad", None) is not None:
        if not isinstance(params["tsnad"], list):
            raise StyxValidationError(f'`tsnad` has the wrong type: Received `{type(params["tsnad"])}` expected `list[str]`')
        for e_3 in params["tsnad"]:
            if not isinstance(e_3, str):
                raise StyxValidationError(f'`tsnad` has the wrong type: Received `{type(e_3)}` expected `list[str]`')
    if params.get("tsnadm", None) is not None:
        if not isinstance(params["tsnadm"], list):
            raise StyxValidationError(f'`tsnadm` has the wrong type: Received `{type(params["tsnadm"])}` expected `list[str]`')
        for e_4 in params["tsnadm"]:
            if not isinstance(e_4, str):
                raise StyxValidationError(f'`tsnadm` has the wrong type: Received `{type(e_4)}` expected `list[str]`')
    if params.get("tsnadl", None) is not None:
        if not isinstance(params["tsnadl"], list):
            raise StyxValidationError(f'`tsnadl` has the wrong type: Received `{type(params["tsnadl"])}` expected `list[str]`')
        for e_5 in params["tsnadl"]:
            if not isinstance(e_5, str):
                raise StyxValidationError(f'`tsnadl` has the wrong type: Received `{type(e_5)}` expected `list[str]`')
    if params.get("spec", None) is not None:
        if not isinstance(params["spec"], str):
            raise StyxValidationError(f'`spec` has the wrong type: Received `{type(params["spec"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def quickspec_cargs(params: Quickspec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("quickspec")
    cargs.append("-tn")
    for item0 in params["tn"]:
        cargs.append(item0)
    cargs.append("-tsn")
    for item1 in params["tsn"]:
        cargs.append(item1)
    v_0 = params.get("tsnad")
    if v_0 is not None:
        cargs.append("-tsnad")
        for item2 in v_0:
            cargs.append(item2)
    v_1 = params.get("tsnadm")
    if v_1 is not None:
        cargs.append("-tsnadm")
        for item3 in v_1:
            cargs.append(item3)
    v_2 = params.get("tsnadl")
    if v_2 is not None:
        cargs.append("-tsnadl")
        for item4 in v_2:
            cargs.append(item4)
    v_3 = params.get("spec")
    if v_3 is not None:
        cargs.append("-spec")
        cargs.append(v_3)
    if params.get("help"):
        cargs.append("-h")
    return cargs

def quickspec_outputs(params: Quickspec, execution: Execution) -> QuickspecOutputs:
    """Build the QuickspecOutputs object for this tool."""
    out_specfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("spec")
    if __v0 is not None:
        out_specfile_v = execution.output_file((__v0 if __v0 is not None else ""))
    return QuickspecOutputs(
        root=root_v,
        out_specfile=out_specfile_v,
    )

def quickspec_execute(params: Quickspec, runner: Runner | None = None) -> QuickspecOutputs:
    """
    quickspec

    A quick and dirty way of loading a surface into SUMA or command line programs using a spec file.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    quickspec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QUICKSPEC_METADATA)
    execution.params(params)
    args = quickspec_cargs(params, execution)
    out = quickspec_outputs(params, execution)
    execution.run(args)
    return out

def quickspec(
    tn: list[str],
    tsn: list[str],
    tsnad: list[str] | None = None,
    tsnadm: list[str] | None = None,
    tsnadl: list[str] | None = None,
    spec: str | None = None,
    help: bool = False,
    runner: Runner | None = None,
) -> QuickspecOutputs:
    """
    quickspec

    A quick and dirty way of loading a surface into SUMA or command line programs using a spec file.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        tn: Specify surface type and name.
        tsn: Specify surface type, state, and name.
        tsnad: Specify surface type, state, name, anatomical correctness, and Local\
            Domain Parent.
        tsnadm: Specify surface type, state, name, anatomical correctness, Local\
            Domain Parent, and node marker file.
        tsnadl: Specify surface type, state, name, anatomical correctness, Local\
            Domain Parent, and label dataset file.
        spec: Name of spec file output. Default is quick.spec.
        help: Display help message.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = quickspec_params(
        tn=tn,
        tsn=tsn,
        tsnad=tsnad,
        tsnadm=tsnadm,
        tsnadl=tsnadl,
        spec=spec,
        help=help,
    )
    return quickspec_execute(params, runner)

__all__ = [
    "Quickspec",
    "QuickspecOutputs",
    "QUICKSPEC_METADATA",
    "quickspec_cargs",
    "quickspec_outputs",
    "quickspec_params",
    "quickspec_execute",
    "quickspec_validate",
    "quickspec",
]