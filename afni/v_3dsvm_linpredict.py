# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DSVM_LINPREDICT_METADATA = Metadata(
    id="3dsvm_linpredict",
    name="3dsvm_linpredict",
    package="afni",
)

V3dsvmLinpredict = typing.TypedDict(
    "V3dsvmLinpredict",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dsvm_linpredict"]],
        "mask_dataset": typing.NotRequired[InputPathType],
        "weight_vector": InputPathType,
        "input_dataset": str,
    },
)

@dataclasses.dataclass
class V3dsvmLinpredictOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout_output: OutputPathType
    """The result is a number printed to stdout"""

def v_3dsvm_linpredict_params(
    weight_vector: InputPathType,
    input_dataset: str,
    mask_dataset: InputPathType | None = None,
) -> V3dsvmLinpredict:
    """
    Build parameters.

    Args:
        weight_vector: Weight vector dataset
        input_dataset: Input dataset, potentially with sub-brick and/or sub-range\
            selectors
        mask_dataset: Dataset to be used as a mask. Only voxels with nonzero values\
            in 'mset' will be averaged from 'dataset'. The mask dataset and the\
            input dataset must have the same number of voxels.

    Returns:
        Parameter dictionary.
    """
    params: V3dsvmLinpredict = {
        "@type": "afni/3dsvm_linpredict",
        "weight_vector": weight_vector,
        "input_dataset": input_dataset,
    }
    if mask_dataset is not None:
        params["mask_dataset"] = mask_dataset
    return params

def v_3dsvm_linpredict_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dsvmLinpredict."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask_dataset", None) is not None:
        if not isinstance(params["mask_dataset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_dataset` has the wrong type: Received `{type(params["mask_dataset"])}` expected `InputPathType`')
    if params.get("weight_vector", None) is None:
        raise StyxValidationError("`weight_vector` must not be None")
    if not isinstance(params["weight_vector"], (pathlib.Path, str)):
        raise StyxValidationError(f'`weight_vector` has the wrong type: Received `{type(params["weight_vector"])}` expected `InputPathType`')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], str):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `str`')

def v_3dsvm_linpredict_cargs(params: V3dsvmLinpredict, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dsvm_linpredict")
    v_0 = params.get("mask_dataset")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    cargs.append(execution.input_file(params["weight_vector"]))
    cargs.append(params["input_dataset"])
    return cargs

def v_3dsvm_linpredict_outputs(params: V3dsvmLinpredict, execution: Execution) -> V3dsvmLinpredictOutputs:
    """Build the V3dsvmLinpredictOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stdout_output_v: OutputPathType = execution.output_file("stdout")
    return V3dsvmLinpredictOutputs(
        root=root_v,
        stdout_output=stdout_output_v,
    )

def v_3dsvm_linpredict_execute(params: V3dsvmLinpredict, runner: Runner | None = None) -> V3dsvmLinpredictOutputs:
    """
    3dsvm_linpredict

    Linear prediction for weights from 3dsvm

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dsvm_linpredict_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DSVM_LINPREDICT_METADATA)
    execution.params(params)
    args = v_3dsvm_linpredict_cargs(params, execution)
    out = v_3dsvm_linpredict_outputs(params, execution)
    execution.run(args)
    return out

def v_3dsvm_linpredict(
    weight_vector: InputPathType,
    input_dataset: str,
    mask_dataset: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dsvmLinpredictOutputs:
    """
    3dsvm_linpredict

    Linear prediction for weights from 3dsvm

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        weight_vector: Weight vector dataset
        input_dataset: Input dataset, potentially with sub-brick and/or sub-range\
            selectors
        mask_dataset: Dataset to be used as a mask. Only voxels with nonzero values\
            in 'mset' will be averaged from 'dataset'. The mask dataset and the\
            input dataset must have the same number of voxels.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dsvm_linpredict_params(
        weight_vector=weight_vector,
        input_dataset=input_dataset,
        mask_dataset=mask_dataset,
    )
    return v_3dsvm_linpredict_execute(params, runner)

__all__ = [
    "V3dsvmLinpredict",
    "V3dsvmLinpredictOutputs",
    "V_3DSVM_LINPREDICT_METADATA",
    "v_3dsvm_linpredict_cargs",
    "v_3dsvm_linpredict_outputs",
    "v_3dsvm_linpredict_params",
    "v_3dsvm_linpredict_execute",
    "v_3dsvm_linpredict_validate",
    "v_3dsvm_linpredict",
]