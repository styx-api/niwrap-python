# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CHECK_FOR_AFNI_DSET_METADATA = Metadata(
    id="@CheckForAfniDset",
    name="@CheckForAfniDset",
    package="afni",
)

CheckForAfniDset = typing.TypedDict(
    "CheckForAfniDset",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@CheckForAfniDset"]],
        "dataset_name": str,
    },
)

@dataclasses.dataclass
class CheckForAfniDsetOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_status: OutputPathType
    """Text file containing the status code of the dataset"""

def check_for_afni_dset_params(
    dataset_name: str,
) -> CheckForAfniDset:
    """
    Build parameters.

    Args:
        dataset_name: Path to the AFNI dataset (e.g., /Data/stuff/Hello+orig.HEAD)

    Returns:
        Parameter dictionary.
    """
    params: CheckForAfniDset = {
        "@type": "afni/@CheckForAfniDset",
        "dataset_name": dataset_name,
    }
    return params

def check_for_afni_dset_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CheckForAfniDset."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset_name", None) is None:
        raise StyxValidationError("`dataset_name` must not be None")
    if not isinstance(params["dataset_name"], str):
        raise StyxValidationError(f'`dataset_name` has the wrong type: Received `{type(params["dataset_name"])}` expected `str`')

def check_for_afni_dset_cargs(params: CheckForAfniDset, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@CheckForAfniDset")
    cargs.append(params["dataset_name"])
    return cargs

def check_for_afni_dset_outputs(params: CheckForAfniDset, execution: Execution) -> CheckForAfniDsetOutputs:
    """Build the CheckForAfniDsetOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_status_v: OutputPathType = execution.output_file(f'{params["dataset_name"]}_status.txt')
    return CheckForAfniDsetOutputs(
        root=root_v,
        output_status=output_status_v,
    )

def check_for_afni_dset_execute(params: CheckForAfniDset, runner: Runner | None = None) -> CheckForAfniDsetOutputs:
    """
    @CheckForAfniDset

    Check for the existence of AFNI datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    check_for_afni_dset_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CHECK_FOR_AFNI_DSET_METADATA)
    execution.params(params)
    args = check_for_afni_dset_cargs(params, execution)
    out = check_for_afni_dset_outputs(params, execution)
    execution.run(args)
    return out

def check_for_afni_dset(
    dataset_name: str,
    runner: Runner | None = None,
) -> CheckForAfniDsetOutputs:
    """
    @CheckForAfniDset

    Check for the existence of AFNI datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset_name: Path to the AFNI dataset (e.g., /Data/stuff/Hello+orig.HEAD)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = check_for_afni_dset_params(
        dataset_name=dataset_name,
    )
    return check_for_afni_dset_execute(params, runner)

__all__ = [
    "CheckForAfniDset",
    "CheckForAfniDsetOutputs",
    "CHECK_FOR_AFNI_DSET_METADATA",
    "check_for_afni_dset_cargs",
    "check_for_afni_dset_outputs",
    "check_for_afni_dset_params",
    "check_for_afni_dset_execute",
    "check_for_afni_dset_validate",
    "check_for_afni_dset",
]