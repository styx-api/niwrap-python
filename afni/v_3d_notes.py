# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_NOTES_METADATA = Metadata(
    id="3dNotes",
    name="3dNotes",
    package="afni",
)

V3dNotes = typing.TypedDict(
    "V3dNotes",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dNotes"]],
        "add_note": typing.NotRequired[str],
        "append_history": typing.NotRequired[str],
        "replace_history": typing.NotRequired[str],
        "delete_note": typing.NotRequired[float],
        "print_notes": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dNotesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_notes_params(
    dataset: InputPathType,
    add_note: str | None = None,
    append_history: str | None = None,
    replace_history: str | None = None,
    delete_note: float | None = None,
    print_notes: bool = False,
    help: bool = False,
) -> V3dNotes:
    """
    Build parameters.

    Args:
        dataset: AFNI compatible dataset [required].
        add_note: Add the string 'str' to the list of notes.
        append_history: Append the string 'str' to the dataset's history. This can\
            only appear once on the command line.
        replace_history: Replace any existing history note with 'str'. This option\
            cannot be used with '-h'.
        delete_note: Deletes note number num.
        print_notes: Print to stdout the expanded notes.
        help: Displays this help screen.

    Returns:
        Parameter dictionary.
    """
    params: V3dNotes = {
        "@type": "afni/3dNotes",
        "dataset": dataset,
        "print_notes": print_notes,
        "help": help,
    }
    if add_note is not None:
        params["add_note"] = add_note
    if append_history is not None:
        params["append_history"] = append_history
    if replace_history is not None:
        params["replace_history"] = replace_history
    if delete_note is not None:
        params["delete_note"] = delete_note
    return params

def v_3d_notes_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dNotes."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("add_note", None) is not None:
        if not isinstance(params["add_note"], str):
            raise StyxValidationError(f'`add_note` has the wrong type: Received `{type(params["add_note"])}` expected `str`')
    if params.get("append_history", None) is not None:
        if not isinstance(params["append_history"], str):
            raise StyxValidationError(f'`append_history` has the wrong type: Received `{type(params["append_history"])}` expected `str`')
    if params.get("replace_history", None) is not None:
        if not isinstance(params["replace_history"], str):
            raise StyxValidationError(f'`replace_history` has the wrong type: Received `{type(params["replace_history"])}` expected `str`')
    if params.get("delete_note", None) is not None:
        if not isinstance(params["delete_note"], (float, int)):
            raise StyxValidationError(f'`delete_note` has the wrong type: Received `{type(params["delete_note"])}` expected `float`')
    if params.get("print_notes", None) is not None:
        if not isinstance(params["print_notes"], bool):
            raise StyxValidationError(f'`print_notes` has the wrong type: Received `{type(params["print_notes"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def v_3d_notes_cargs(params: V3dNotes, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dNotes")
    v_0 = params.get("add_note")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(v_0)
    v_1 = params.get("append_history")
    if v_1 is not None:
        cargs.append("-h")
        cargs.append(v_1)
    v_2 = params.get("replace_history")
    if v_2 is not None:
        cargs.append("-HH")
        cargs.append(v_2)
    v_3 = params.get("delete_note")
    if v_3 is not None:
        cargs.append("-d")
        cargs.append(str(v_3))
    if params.get("print_notes"):
        cargs.append("-ses")
    if params.get("help"):
        cargs.append("-help")
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def v_3d_notes_outputs(params: V3dNotes, execution: Execution) -> V3dNotesOutputs:
    """Build the V3dNotesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dNotesOutputs(
        root=root_v,
    )

def v_3d_notes_execute(params: V3dNotes, runner: Runner | None = None) -> V3dNotesOutputs:
    """
    3dNotes

    A program to add, delete and show notes for AFNI datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_notes_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_NOTES_METADATA)
    execution.params(params)
    args = v_3d_notes_cargs(params, execution)
    out = v_3d_notes_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_notes(
    dataset: InputPathType,
    add_note: str | None = None,
    append_history: str | None = None,
    replace_history: str | None = None,
    delete_note: float | None = None,
    print_notes: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> V3dNotesOutputs:
    """
    3dNotes

    A program to add, delete and show notes for AFNI datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: AFNI compatible dataset [required].
        add_note: Add the string 'str' to the list of notes.
        append_history: Append the string 'str' to the dataset's history. This can\
            only appear once on the command line.
        replace_history: Replace any existing history note with 'str'. This option\
            cannot be used with '-h'.
        delete_note: Deletes note number num.
        print_notes: Print to stdout the expanded notes.
        help: Displays this help screen.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_notes_params(
        dataset=dataset,
        add_note=add_note,
        append_history=append_history,
        replace_history=replace_history,
        delete_note=delete_note,
        print_notes=print_notes,
        help=help,
    )
    return v_3d_notes_execute(params, runner)

__all__ = [
    "V3dNotes",
    "V3dNotesOutputs",
    "V_3D_NOTES_METADATA",
    "v_3d_notes_cargs",
    "v_3d_notes_outputs",
    "v_3d_notes_params",
    "v_3d_notes_execute",
    "v_3d_notes_validate",
    "v_3d_notes",
]