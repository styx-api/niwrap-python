# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_ORIENT_METADATA = Metadata(
    id="@GetAfniOrient",
    name="@GetAfniOrient",
    package="afni",
)

GetAfniOrient = typing.TypedDict(
    "GetAfniOrient",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@GetAfniOrient"]],
        "exploratory": typing.NotRequired[bool],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class GetAfniOrientOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_orient_code: OutputPathType
    """File containing the orientation code"""

def get_afni_orient_params(
    infile: InputPathType,
    exploratory: bool = False,
) -> GetAfniOrient:
    """
    Build parameters.

    Args:
        infile: Input AFNI dataset (e.g. Hello+orig.HEAD)
        exploratory: Exploratory flag for additional functionalities

    Returns:
        Parameter dictionary.
    """
    params: GetAfniOrient = {
        "@type": "afni/@GetAfniOrient",
        "infile": infile,
        "exploratory": exploratory,
    }
    return params

def get_afni_orient_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniOrient."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("exploratory", None) is not None:
        if not isinstance(params["exploratory"], bool):
            raise StyxValidationError(f'`exploratory` has the wrong type: Received `{type(params["exploratory"])}` expected `bool`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def get_afni_orient_cargs(params: GetAfniOrient, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@GetAfniOrient")
    if params.get("exploratory"):
        cargs.append("-exp")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def get_afni_orient_outputs(params: GetAfniOrient, execution: Execution) -> GetAfniOrientOutputs:
    """Build the GetAfniOrientOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_orient_code_v: OutputPathType = execution.output_file(f'{params["infile"]}_orient_code.txt')
    return GetAfniOrientOutputs(
        root=root_v,
        output_orient_code=output_orient_code_v,
    )

def get_afni_orient_execute(params: GetAfniOrient, runner: Runner | None = None) -> GetAfniOrientOutputs:
    """
    @GetAfniOrient

    Returns the orient code of AFNI datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_orient_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_ORIENT_METADATA)
    execution.params(params)
    args = get_afni_orient_cargs(params, execution)
    out = get_afni_orient_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_orient(
    infile: InputPathType,
    exploratory: bool = False,
    runner: Runner | None = None,
) -> GetAfniOrientOutputs:
    """
    @GetAfniOrient

    Returns the orient code of AFNI datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input AFNI dataset (e.g. Hello+orig.HEAD)
        exploratory: Exploratory flag for additional functionalities
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_orient_params(
        infile=infile,
        exploratory=exploratory,
    )
    return get_afni_orient_execute(params, runner)

__all__ = [
    "GetAfniOrient",
    "GetAfniOrientOutputs",
    "GET_AFNI_ORIENT_METADATA",
    "get_afni_orient_cargs",
    "get_afni_orient_outputs",
    "get_afni_orient_params",
    "get_afni_orient_execute",
    "get_afni_orient_validate",
    "get_afni_orient",
]