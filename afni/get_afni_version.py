# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_VERSION_METADATA = Metadata(
    id="@get.afni.version",
    name="@get.afni.version",
    package="afni",
)

GetAfniVersion = typing.TypedDict(
    "GetAfniVersion",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@get.afni.version"]],
        "version": str,
    },
)

@dataclasses.dataclass
class GetAfniVersionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    src_dir: OutputPathType
    """Directory containing the downloaded AFNI source code for the specified version."""

def get_afni_version_params(
    version: str,
) -> GetAfniVersion:
    """
    Build parameters.

    Args:
        version: AFNI version number to get (e.g., 16.0.01)

    Returns:
        Parameter dictionary.
    """
    params: GetAfniVersion = {
        "@type": "afni/@get.afni.version",
        "version": version,
    }
    return params

def get_afni_version_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniVersion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("version", None) is None:
        raise StyxValidationError("`version` must not be None")
    if not isinstance(params["version"], str):
        raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `str`')

def get_afni_version_cargs(params: GetAfniVersion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@get.afni.version")
    cargs.append(params["version"])
    return cargs

def get_afni_version_outputs(params: GetAfniVersion, execution: Execution) -> GetAfniVersionOutputs:
    """Build the GetAfniVersionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    src_dir_v: OutputPathType = execution.output_file(f'AFNI_{params["version"]}/AFNI/src')
    return GetAfniVersionOutputs(
        root=root_v,
        src_dir=src_dir_v,
    )

def get_afni_version_execute(params: GetAfniVersion, runner: Runner | None = None) -> GetAfniVersionOutputs:
    """
    @get.afni.version

    Downloads the source code for a specified AFNI version.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_version_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_VERSION_METADATA)
    execution.params(params)
    args = get_afni_version_cargs(params, execution)
    out = get_afni_version_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_version(
    version: str,
    runner: Runner | None = None,
) -> GetAfniVersionOutputs:
    """
    @get.afni.version

    Downloads the source code for a specified AFNI version.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        version: AFNI version number to get (e.g., 16.0.01)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_version_params(
        version=version,
    )
    return get_afni_version_execute(params, runner)

__all__ = [
    "GetAfniVersion",
    "GetAfniVersionOutputs",
    "GET_AFNI_VERSION_METADATA",
    "get_afni_version_cargs",
    "get_afni_version_outputs",
    "get_afni_version_params",
    "get_afni_version_execute",
    "get_afni_version_validate",
    "get_afni_version",
]