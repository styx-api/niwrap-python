# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DJPEG_METADATA = Metadata(
    id="djpeg",
    name="djpeg",
    package="afni",
)

Djpeg = typing.TypedDict(
    "Djpeg",
    {
        "@type": typing.NotRequired[typing.Literal["afni/djpeg"]],
        "input_file": InputPathType,
        "output_file": str,
        "gray": typing.NotRequired[bool],
        "fast_dct": typing.NotRequired[bool],
        "one_pixel_height": typing.NotRequired[bool],
        "pseudo_pixel_ratio": typing.NotRequired[bool],
        "crop_region": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class DjpegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType
    """Output image file"""

def djpeg_params(
    input_file: InputPathType,
    output_file: str,
    gray: bool = False,
    fast_dct: bool = False,
    one_pixel_height: bool = False,
    pseudo_pixel_ratio: bool = False,
    crop_region: str | None = None,
) -> Djpeg:
    """
    Build parameters.

    Args:
        input_file: Input JPEG file (e.g. image.jpg)
        output_file: Output image file (e.g. image.ppm)
        gray: Force grayscale output
        fast_dct: Prevent dithering of output
        one_pixel_height: Force one-pixel modulation flag
        pseudo_pixel_ratio: Force pseudo-pixel ratio flag
        crop_region: Crop region (syntax: WxH+X+Y, e.g., 100x100+10+10)

    Returns:
        Parameter dictionary.
    """
    params: Djpeg = {
        "@type": "afni/djpeg",
        "input_file": input_file,
        "output_file": output_file,
        "gray": gray,
        "fast_dct": fast_dct,
        "one_pixel_height": one_pixel_height,
        "pseudo_pixel_ratio": pseudo_pixel_ratio,
    }
    if crop_region is not None:
        params["crop_region"] = crop_region
    return params

def djpeg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Djpeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("gray", None) is not None:
        if not isinstance(params["gray"], bool):
            raise StyxValidationError(f'`gray` has the wrong type: Received `{type(params["gray"])}` expected `bool`')
    if params.get("fast_dct", None) is not None:
        if not isinstance(params["fast_dct"], bool):
            raise StyxValidationError(f'`fast_dct` has the wrong type: Received `{type(params["fast_dct"])}` expected `bool`')
    if params.get("one_pixel_height", None) is not None:
        if not isinstance(params["one_pixel_height"], bool):
            raise StyxValidationError(f'`one_pixel_height` has the wrong type: Received `{type(params["one_pixel_height"])}` expected `bool`')
    if params.get("pseudo_pixel_ratio", None) is not None:
        if not isinstance(params["pseudo_pixel_ratio"], bool):
            raise StyxValidationError(f'`pseudo_pixel_ratio` has the wrong type: Received `{type(params["pseudo_pixel_ratio"])}` expected `bool`')
    if params.get("crop_region", None) is not None:
        if not isinstance(params["crop_region"], str):
            raise StyxValidationError(f'`crop_region` has the wrong type: Received `{type(params["crop_region"])}` expected `str`')

def djpeg_cargs(params: Djpeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("djpeg")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    if params.get("gray"):
        cargs.append("-grayscale")
    if params.get("fast_dct"):
        cargs.append("-fast")
    if params.get("one_pixel_height"):
        cargs.append("-onepixel")
    if params.get("pseudo_pixel_ratio"):
        cargs.append("-236")
    v_0 = params.get("crop_region")
    if v_0 is not None:
        cargs.append("-crop")
        cargs.append(v_0)
    return cargs

def djpeg_outputs(params: Djpeg, execution: Execution) -> DjpegOutputs:
    """Build the DjpegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_v: OutputPathType = execution.output_file(params["output_file"])
    return DjpegOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def djpeg_execute(params: Djpeg, runner: Runner | None = None) -> DjpegOutputs:
    """
    djpeg

    Decompress a JPEG file to an image file

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    djpeg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DJPEG_METADATA)
    execution.params(params)
    args = djpeg_cargs(params, execution)
    out = djpeg_outputs(params, execution)
    execution.run(args)
    return out

def djpeg(
    input_file: InputPathType,
    output_file: str,
    gray: bool = False,
    fast_dct: bool = False,
    one_pixel_height: bool = False,
    pseudo_pixel_ratio: bool = False,
    crop_region: str | None = None,
    runner: Runner | None = None,
) -> DjpegOutputs:
    """
    djpeg

    Decompress a JPEG file to an image file

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Input JPEG file (e.g. image.jpg)
        output_file: Output image file (e.g. image.ppm)
        gray: Force grayscale output
        fast_dct: Prevent dithering of output
        one_pixel_height: Force one-pixel modulation flag
        pseudo_pixel_ratio: Force pseudo-pixel ratio flag
        crop_region: Crop region (syntax: WxH+X+Y, e.g., 100x100+10+10)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = djpeg_params(
        input_file=input_file,
        output_file=output_file,
        gray=gray,
        fast_dct=fast_dct,
        one_pixel_height=one_pixel_height,
        pseudo_pixel_ratio=pseudo_pixel_ratio,
        crop_region=crop_region,
    )
    return djpeg_execute(params, runner)

__all__ = [
    "Djpeg",
    "DjpegOutputs",
    "DJPEG_METADATA",
    "djpeg_cargs",
    "djpeg_outputs",
    "djpeg_params",
    "djpeg_execute",
    "djpeg_validate",
    "djpeg",
]