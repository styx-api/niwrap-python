# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_PLUG_DIFF_METADATA = Metadata(
    id="@make_plug_diff",
    name="@make_plug_diff",
    package="afni",
)

MakePlugDiff = typing.TypedDict(
    "MakePlugDiff",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@make_plug_diff"]],
        "vtk_dir": str,
        "xm_dir": str,
        "afni_src_dir": str,
        "afni_bin_dir": str,
        "comments": typing.NotRequired[bool],
        "linux": typing.NotRequired[bool],
        "diff_dir": str,
    },
)

@dataclasses.dataclass
class MakePlugDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def make_plug_diff_params(
    vtk_dir: str,
    xm_dir: str,
    afni_src_dir: str,
    afni_bin_dir: str,
    diff_dir: str,
    comments: bool = False,
    linux: bool = False,
) -> MakePlugDiff:
    """
    Build parameters.

    Args:
        vtk_dir: Directory where vtk is installed
        xm_dir: Directory where motif is installed
        afni_src_dir: Full path to AFNI's src/ directory
        afni_bin_dir: Path, relative to ASRCDIR, to abin
        diff_dir: Name of directory containing diffusion code
        comments: Output comments only
        linux: Flag for doing linuxy things

    Returns:
        Parameter dictionary.
    """
    params: MakePlugDiff = {
        "@type": "afni/@make_plug_diff",
        "vtk_dir": vtk_dir,
        "xm_dir": xm_dir,
        "afni_src_dir": afni_src_dir,
        "afni_bin_dir": afni_bin_dir,
        "diff_dir": diff_dir,
        "comments": comments,
        "linux": linux,
    }
    return params

def make_plug_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakePlugDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vtk_dir", None) is None:
        raise StyxValidationError("`vtk_dir` must not be None")
    if not isinstance(params["vtk_dir"], str):
        raise StyxValidationError(f'`vtk_dir` has the wrong type: Received `{type(params["vtk_dir"])}` expected `str`')
    if params.get("xm_dir", None) is None:
        raise StyxValidationError("`xm_dir` must not be None")
    if not isinstance(params["xm_dir"], str):
        raise StyxValidationError(f'`xm_dir` has the wrong type: Received `{type(params["xm_dir"])}` expected `str`')
    if params.get("afni_src_dir", None) is None:
        raise StyxValidationError("`afni_src_dir` must not be None")
    if not isinstance(params["afni_src_dir"], str):
        raise StyxValidationError(f'`afni_src_dir` has the wrong type: Received `{type(params["afni_src_dir"])}` expected `str`')
    if params.get("afni_bin_dir", None) is None:
        raise StyxValidationError("`afni_bin_dir` must not be None")
    if not isinstance(params["afni_bin_dir"], str):
        raise StyxValidationError(f'`afni_bin_dir` has the wrong type: Received `{type(params["afni_bin_dir"])}` expected `str`')
    if params.get("comments", None) is not None:
        if not isinstance(params["comments"], bool):
            raise StyxValidationError(f'`comments` has the wrong type: Received `{type(params["comments"])}` expected `bool`')
    if params.get("linux", None) is not None:
        if not isinstance(params["linux"], bool):
            raise StyxValidationError(f'`linux` has the wrong type: Received `{type(params["linux"])}` expected `bool`')
    if params.get("diff_dir", None) is None:
        raise StyxValidationError("`diff_dir` must not be None")
    if not isinstance(params["diff_dir"], str):
        raise StyxValidationError(f'`diff_dir` has the wrong type: Received `{type(params["diff_dir"])}` expected `str`')

def make_plug_diff_cargs(params: MakePlugDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@make_plug_diff")
    cargs.append("-vtk")
    cargs.append(params["vtk_dir"])
    cargs.append("-xm")
    cargs.append(params["xm_dir"])
    cargs.append("-asrc")
    cargs.append(params["afni_src_dir"])
    cargs.append("-abin")
    cargs.append(params["afni_bin_dir"])
    if params.get("comments"):
        cargs.append("-comments")
    if params.get("linux"):
        cargs.append("-linux")
    cargs.append("-diff")
    cargs.append(params["diff_dir"])
    return cargs

def make_plug_diff_outputs(params: MakePlugDiff, execution: Execution) -> MakePlugDiffOutputs:
    """Build the MakePlugDiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MakePlugDiffOutputs(
        root=root_v,
    )

def make_plug_diff_execute(params: MakePlugDiff, runner: Runner | None = None) -> MakePlugDiffOutputs:
    """
    @make_plug_diff

    Compiles AFNI's diffusion plugin.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_plug_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_PLUG_DIFF_METADATA)
    execution.params(params)
    args = make_plug_diff_cargs(params, execution)
    out = make_plug_diff_outputs(params, execution)
    execution.run(args)
    return out

def make_plug_diff(
    vtk_dir: str,
    xm_dir: str,
    afni_src_dir: str,
    afni_bin_dir: str,
    diff_dir: str,
    comments: bool = False,
    linux: bool = False,
    runner: Runner | None = None,
) -> MakePlugDiffOutputs:
    """
    @make_plug_diff

    Compiles AFNI's diffusion plugin.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        vtk_dir: Directory where vtk is installed
        xm_dir: Directory where motif is installed
        afni_src_dir: Full path to AFNI's src/ directory
        afni_bin_dir: Path, relative to ASRCDIR, to abin
        diff_dir: Name of directory containing diffusion code
        comments: Output comments only
        linux: Flag for doing linuxy things
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_plug_diff_params(
        vtk_dir=vtk_dir,
        xm_dir=xm_dir,
        afni_src_dir=afni_src_dir,
        afni_bin_dir=afni_bin_dir,
        diff_dir=diff_dir,
        comments=comments,
        linux=linux,
    )
    return make_plug_diff_execute(params, runner)

__all__ = [
    "MakePlugDiff",
    "MakePlugDiffOutputs",
    "MAKE_PLUG_DIFF_METADATA",
    "make_plug_diff_cargs",
    "make_plug_diff_outputs",
    "make_plug_diff_params",
    "make_plug_diff_execute",
    "make_plug_diff_validate",
    "make_plug_diff",
]