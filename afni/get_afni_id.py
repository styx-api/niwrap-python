# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_ID_METADATA = Metadata(
    id="@GetAfniID",
    name="@GetAfniID",
    package="afni",
)

GetAfniId = typing.TypedDict(
    "GetAfniId",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@GetAfniID"]],
        "dset": InputPathType,
    },
)

@dataclasses.dataclass
class GetAfniIdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    unique_id: OutputPathType
    """Unique identifier of the dataset"""

def get_afni_id_params(
    dset: InputPathType,
) -> GetAfniId:
    """
    Build parameters.

    Args:
        dset: Dataset for which the unique identifier is to be returned.

    Returns:
        Parameter dictionary.
    """
    params: GetAfniId = {
        "@type": "afni/@GetAfniID",
        "dset": dset,
    }
    return params

def get_afni_id_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniId."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')

def get_afni_id_cargs(params: GetAfniId, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@GetAfniID")
    cargs.append(execution.input_file(params["dset"]))
    return cargs

def get_afni_id_outputs(params: GetAfniId, execution: Execution) -> GetAfniIdOutputs:
    """Build the GetAfniIdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    unique_id_v: OutputPathType = execution.output_file("stdout")
    return GetAfniIdOutputs(
        root=root_v,
        unique_id=unique_id_v,
    )

def get_afni_id_execute(params: GetAfniId, runner: Runner | None = None) -> GetAfniIdOutputs:
    """
    @GetAfniID

    Returns the unique identifier of a dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_id_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_ID_METADATA)
    execution.params(params)
    args = get_afni_id_cargs(params, execution)
    out = get_afni_id_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_id(
    dset: InputPathType,
    runner: Runner | None = None,
) -> GetAfniIdOutputs:
    """
    @GetAfniID

    Returns the unique identifier of a dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset: Dataset for which the unique identifier is to be returned.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_id_params(
        dset=dset,
    )
    return get_afni_id_execute(params, runner)

__all__ = [
    "GetAfniId",
    "GetAfniIdOutputs",
    "GET_AFNI_ID_METADATA",
    "get_afni_id_cargs",
    "get_afni_id_outputs",
    "get_afni_id_params",
    "get_afni_id_execute",
    "get_afni_id_validate",
    "get_afni_id",
]