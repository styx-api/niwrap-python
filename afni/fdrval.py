# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FDRVAL_METADATA = Metadata(
    id="fdrval",
    name="fdrval",
    package="afni",
)

Fdrval = typing.TypedDict(
    "Fdrval",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fdrval"]],
        "dset": InputPathType,
        "sub": float,
        "val_list": list[float],
        "pval": typing.NotRequired[bool],
        "ponly": typing.NotRequired[bool],
        "qonly": typing.NotRequired[bool],
        "qinput": typing.NotRequired[bool],
        "inverse": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FdrvalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """Computed q-values or p-values for the given thresholds"""

def fdrval_params(
    dset: InputPathType,
    sub: float,
    val_list: list[float],
    pval: bool = False,
    ponly: bool = False,
    qonly: bool = False,
    qinput: bool = False,
    inverse: bool = False,
) -> Fdrval:
    """
    Build parameters.

    Args:
        dset: Input dataset
        sub: Sub-brick number
        val_list: List of threshold values
        pval: Output the p-value (on the same line, after q)
        ponly: Don't output q-values, just p-values
        qonly: Don't output p-values, just q-values
        qinput: The 'val' inputs are taken to be q-values and then the outputs are\
            the corresponding statistical thresholds.
        inverse: Inverse of the usual operation. 'Val' inputs must be between 0 and\
            1 (exclusive). Cannot be used with '-ponly' or '-pval'.

    Returns:
        Parameter dictionary.
    """
    params: Fdrval = {
        "@type": "afni/fdrval",
        "dset": dset,
        "sub": sub,
        "val_list": val_list,
        "pval": pval,
        "ponly": ponly,
        "qonly": qonly,
        "qinput": qinput,
        "inverse": inverse,
    }
    return params

def fdrval_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fdrval."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')
    if params.get("sub", None) is None:
        raise StyxValidationError("`sub` must not be None")
    if not isinstance(params["sub"], (float, int)):
        raise StyxValidationError(f'`sub` has the wrong type: Received `{type(params["sub"])}` expected `float`')
    if params.get("val_list", None) is None:
        raise StyxValidationError("`val_list` must not be None")
    if not isinstance(params["val_list"], list):
        raise StyxValidationError(f'`val_list` has the wrong type: Received `{type(params["val_list"])}` expected `list[float]`')
    for e in params["val_list"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`val_list` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("pval", None) is not None:
        if not isinstance(params["pval"], bool):
            raise StyxValidationError(f'`pval` has the wrong type: Received `{type(params["pval"])}` expected `bool`')
    if params.get("ponly", None) is not None:
        if not isinstance(params["ponly"], bool):
            raise StyxValidationError(f'`ponly` has the wrong type: Received `{type(params["ponly"])}` expected `bool`')
    if params.get("qonly", None) is not None:
        if not isinstance(params["qonly"], bool):
            raise StyxValidationError(f'`qonly` has the wrong type: Received `{type(params["qonly"])}` expected `bool`')
    if params.get("qinput", None) is not None:
        if not isinstance(params["qinput"], bool):
            raise StyxValidationError(f'`qinput` has the wrong type: Received `{type(params["qinput"])}` expected `bool`')
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')

def fdrval_cargs(params: Fdrval, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fdrval")
    cargs.append(execution.input_file(params["dset"]))
    cargs.append(str(params["sub"]))
    for item0 in params["val_list"]:
        cargs.append(str(item0))
    if params.get("pval"):
        cargs.append("-pval")
    if params.get("ponly"):
        cargs.append("-ponly")
    if params.get("qonly"):
        cargs.append("-qonly")
    if params.get("qinput"):
        cargs.append("-qinput")
    if params.get("inverse"):
        cargs.append("-inverse")
    return cargs

def fdrval_outputs(params: Fdrval, execution: Execution) -> FdrvalOutputs:
    """Build the FdrvalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file("stdout.txt")
    return FdrvalOutputs(
        root=root_v,
        output=output_v,
    )

def fdrval_execute(params: Fdrval, runner: Runner | None = None) -> FdrvalOutputs:
    """
    fdrval

    Computes q-values from FDR curve data stored in dataset headers

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fdrval_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FDRVAL_METADATA)
    execution.params(params)
    args = fdrval_cargs(params, execution)
    out = fdrval_outputs(params, execution)
    execution.run(args)
    return out

def fdrval(
    dset: InputPathType,
    sub: float,
    val_list: list[float],
    pval: bool = False,
    ponly: bool = False,
    qonly: bool = False,
    qinput: bool = False,
    inverse: bool = False,
    runner: Runner | None = None,
) -> FdrvalOutputs:
    """
    fdrval

    Computes q-values from FDR curve data stored in dataset headers

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset: Input dataset
        sub: Sub-brick number
        val_list: List of threshold values
        pval: Output the p-value (on the same line, after q)
        ponly: Don't output q-values, just p-values
        qonly: Don't output p-values, just q-values
        qinput: The 'val' inputs are taken to be q-values and then the outputs are\
            the corresponding statistical thresholds.
        inverse: Inverse of the usual operation. 'Val' inputs must be between 0 and\
            1 (exclusive). Cannot be used with '-ponly' or '-pval'.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fdrval_params(
        dset=dset,
        sub=sub,
        val_list=val_list,
        pval=pval,
        ponly=ponly,
        qonly=qonly,
        qinput=qinput,
        inverse=inverse,
    )
    return fdrval_execute(params, runner)

__all__ = [
    "Fdrval",
    "FdrvalOutputs",
    "FDRVAL_METADATA",
    "fdrval_cargs",
    "fdrval_outputs",
    "fdrval_params",
    "fdrval_execute",
    "fdrval_validate",
    "fdrval",
]