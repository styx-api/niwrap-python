# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_AUTOBOX_METADATA = Metadata(
    id="3dAutobox",
    name="3dAutobox",
    package="afni",
)

V3dAutobox = typing.TypedDict(
    "V3dAutobox",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dAutobox"]],
        "input": InputPathType,
        "prefix": typing.NotRequired[str],
        "alt_input": typing.NotRequired[InputPathType],
        "noclust": typing.NotRequired[bool],
        "extent": typing.NotRequired[bool],
        "extent_ijk": typing.NotRequired[bool],
        "extent_ijk_to_file": typing.NotRequired[str],
        "extent_ijk_midslice": typing.NotRequired[bool],
        "extent_ijkord": typing.NotRequired[bool],
        "extent_ijkord_to_file": typing.NotRequired[str],
        "extent_xyz_to_file": typing.NotRequired[str],
        "extent_xyz_midslice": typing.NotRequired[bool],
        "npad": typing.NotRequired[float],
        "npad_safety_on": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dAutoboxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_autobox_params(
    input_: InputPathType,
    prefix: str | None = None,
    alt_input: InputPathType | None = None,
    noclust: bool = False,
    extent: bool = False,
    extent_ijk: bool = False,
    extent_ijk_to_file: str | None = None,
    extent_ijk_midslice: bool = False,
    extent_ijkord: bool = False,
    extent_ijkord_to_file: str | None = None,
    extent_xyz_to_file: str | None = None,
    extent_xyz_midslice: bool = False,
    npad: float | None = None,
    npad_safety_on: bool = False,
) -> V3dAutobox:
    """
    Build parameters.

    Args:
        input_: Input dataset
        prefix: Crop the input dataset to the size of the box, and write an output\
            dataset with PREFIX for the name. If not used, no new volume is written\
            out.
        alt_input: An alternate way to specify the input dataset.
        noclust: Don't do any clustering to find the box. Any non-zero voxel will\
            be preserved in the cropped volume.
        extent: Write to standard out the spatial extent of the box.
        extent_ijk: Write out the 6 auto bbox ijk slice numbers to screen: imin\
            imax jmin jmax kmin kmax.
        extent_ijk_to_file: Write out the 6 auto bbox ijk slice numbers to a\
            simple-formatted text file FF: imin imax jmin jmax kmin kmax.
        extent_ijk_midslice: Write out the 3 ijk midslices of the autobox to the\
            screen: imid jmid kmid.
        extent_ijkord: Write out the 6 auto bbox ijk slice numbers to screen in a\
            particular order and format. Useful for scripting.
        extent_ijkord_to_file: Write out the 6 auto bbox ijk slice numbers to a\
            file in a particular order and format. Useful for 3dcalc expressions.
        extent_xyz_to_file: Write out the 6 auto bbox xyz coordinates to a\
            simple-formatted text file GG: xmin xmax ymin ymax zmin zmax.
        extent_xyz_midslice: Write out the 3 xyz midslices of the autobox to the\
            screen: xmid ymid zmid.
        npad: Number of extra voxels to pad on each side of box.
        npad_safety_on: Constrain npad-ded extents to be within dataset.

    Returns:
        Parameter dictionary.
    """
    params: V3dAutobox = {
        "@type": "afni/3dAutobox",
        "input": input_,
        "noclust": noclust,
        "extent": extent,
        "extent_ijk": extent_ijk,
        "extent_ijk_midslice": extent_ijk_midslice,
        "extent_ijkord": extent_ijkord,
        "extent_xyz_midslice": extent_xyz_midslice,
        "npad_safety_on": npad_safety_on,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if alt_input is not None:
        params["alt_input"] = alt_input
    if extent_ijk_to_file is not None:
        params["extent_ijk_to_file"] = extent_ijk_to_file
    if extent_ijkord_to_file is not None:
        params["extent_ijkord_to_file"] = extent_ijkord_to_file
    if extent_xyz_to_file is not None:
        params["extent_xyz_to_file"] = extent_xyz_to_file
    if npad is not None:
        params["npad"] = npad
    return params

def v_3d_autobox_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dAutobox."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("alt_input", None) is not None:
        if not isinstance(params["alt_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`alt_input` has the wrong type: Received `{type(params["alt_input"])}` expected `InputPathType`')
    if params.get("noclust", None) is not None:
        if not isinstance(params["noclust"], bool):
            raise StyxValidationError(f'`noclust` has the wrong type: Received `{type(params["noclust"])}` expected `bool`')
    if params.get("extent", None) is not None:
        if not isinstance(params["extent"], bool):
            raise StyxValidationError(f'`extent` has the wrong type: Received `{type(params["extent"])}` expected `bool`')
    if params.get("extent_ijk", None) is not None:
        if not isinstance(params["extent_ijk"], bool):
            raise StyxValidationError(f'`extent_ijk` has the wrong type: Received `{type(params["extent_ijk"])}` expected `bool`')
    if params.get("extent_ijk_to_file", None) is not None:
        if not isinstance(params["extent_ijk_to_file"], str):
            raise StyxValidationError(f'`extent_ijk_to_file` has the wrong type: Received `{type(params["extent_ijk_to_file"])}` expected `str`')
    if params.get("extent_ijk_midslice", None) is not None:
        if not isinstance(params["extent_ijk_midslice"], bool):
            raise StyxValidationError(f'`extent_ijk_midslice` has the wrong type: Received `{type(params["extent_ijk_midslice"])}` expected `bool`')
    if params.get("extent_ijkord", None) is not None:
        if not isinstance(params["extent_ijkord"], bool):
            raise StyxValidationError(f'`extent_ijkord` has the wrong type: Received `{type(params["extent_ijkord"])}` expected `bool`')
    if params.get("extent_ijkord_to_file", None) is not None:
        if not isinstance(params["extent_ijkord_to_file"], str):
            raise StyxValidationError(f'`extent_ijkord_to_file` has the wrong type: Received `{type(params["extent_ijkord_to_file"])}` expected `str`')
    if params.get("extent_xyz_to_file", None) is not None:
        if not isinstance(params["extent_xyz_to_file"], str):
            raise StyxValidationError(f'`extent_xyz_to_file` has the wrong type: Received `{type(params["extent_xyz_to_file"])}` expected `str`')
    if params.get("extent_xyz_midslice", None) is not None:
        if not isinstance(params["extent_xyz_midslice"], bool):
            raise StyxValidationError(f'`extent_xyz_midslice` has the wrong type: Received `{type(params["extent_xyz_midslice"])}` expected `bool`')
    if params.get("npad", None) is not None:
        if not isinstance(params["npad"], (float, int)):
            raise StyxValidationError(f'`npad` has the wrong type: Received `{type(params["npad"])}` expected `float`')
    if params.get("npad_safety_on", None) is not None:
        if not isinstance(params["npad_safety_on"], bool):
            raise StyxValidationError(f'`npad_safety_on` has the wrong type: Received `{type(params["npad_safety_on"])}` expected `bool`')

def v_3d_autobox_cargs(params: V3dAutobox, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dAutobox")
    cargs.append(execution.input_file(params["input"]))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("alt_input")
    if v_1 is not None:
        cargs.append("-input")
        cargs.append(execution.input_file(v_1))
    if params.get("noclust"):
        cargs.append("-noclust")
    if params.get("extent"):
        cargs.append("-extent")
    if params.get("extent_ijk"):
        cargs.append("-extent_ijk")
    v_2 = params.get("extent_ijk_to_file")
    if v_2 is not None:
        cargs.append("-extent_ijk_to_file")
        cargs.append(v_2)
    if params.get("extent_ijk_midslice"):
        cargs.append("-extent_ijk_midslice")
    if params.get("extent_ijkord"):
        cargs.append("-extent_ijkord")
    v_3 = params.get("extent_ijkord_to_file")
    if v_3 is not None:
        cargs.append("-extent_ijkord_to_file")
        cargs.append(v_3)
    v_4 = params.get("extent_xyz_to_file")
    if v_4 is not None:
        cargs.append("-extent_xyz_to_file")
        cargs.append(v_4)
    if params.get("extent_xyz_midslice"):
        cargs.append("-extent_xyz_midslice")
    v_5 = params.get("npad")
    if v_5 is not None:
        cargs.append("-npad")
        cargs.append(str(v_5))
    if params.get("npad_safety_on"):
        cargs.append("-npad_safety_on")
    return cargs

def v_3d_autobox_outputs(params: V3dAutobox, execution: Execution) -> V3dAutoboxOutputs:
    """Build the V3dAutoboxOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dAutoboxOutputs(
        root=root_v,
    )

def v_3d_autobox_execute(params: V3dAutobox, runner: Runner | None = None) -> V3dAutoboxOutputs:
    """
    3dAutobox

    Computes size of a box that fits around the volume. Can also be used to crop the volume to that box.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_autobox_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_AUTOBOX_METADATA)
    execution.params(params)
    args = v_3d_autobox_cargs(params, execution)
    out = v_3d_autobox_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_autobox(
    input_: InputPathType,
    prefix: str | None = None,
    alt_input: InputPathType | None = None,
    noclust: bool = False,
    extent: bool = False,
    extent_ijk: bool = False,
    extent_ijk_to_file: str | None = None,
    extent_ijk_midslice: bool = False,
    extent_ijkord: bool = False,
    extent_ijkord_to_file: str | None = None,
    extent_xyz_to_file: str | None = None,
    extent_xyz_midslice: bool = False,
    npad: float | None = None,
    npad_safety_on: bool = False,
    runner: Runner | None = None,
) -> V3dAutoboxOutputs:
    """
    3dAutobox

    Computes size of a box that fits around the volume. Can also be used to crop the volume to that box.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Input dataset
        prefix: Crop the input dataset to the size of the box, and write an output\
            dataset with PREFIX for the name. If not used, no new volume is written\
            out.
        alt_input: An alternate way to specify the input dataset.
        noclust: Don't do any clustering to find the box. Any non-zero voxel will\
            be preserved in the cropped volume.
        extent: Write to standard out the spatial extent of the box.
        extent_ijk: Write out the 6 auto bbox ijk slice numbers to screen: imin\
            imax jmin jmax kmin kmax.
        extent_ijk_to_file: Write out the 6 auto bbox ijk slice numbers to a\
            simple-formatted text file FF: imin imax jmin jmax kmin kmax.
        extent_ijk_midslice: Write out the 3 ijk midslices of the autobox to the\
            screen: imid jmid kmid.
        extent_ijkord: Write out the 6 auto bbox ijk slice numbers to screen in a\
            particular order and format. Useful for scripting.
        extent_ijkord_to_file: Write out the 6 auto bbox ijk slice numbers to a\
            file in a particular order and format. Useful for 3dcalc expressions.
        extent_xyz_to_file: Write out the 6 auto bbox xyz coordinates to a\
            simple-formatted text file GG: xmin xmax ymin ymax zmin zmax.
        extent_xyz_midslice: Write out the 3 xyz midslices of the autobox to the\
            screen: xmid ymid zmid.
        npad: Number of extra voxels to pad on each side of box.
        npad_safety_on: Constrain npad-ded extents to be within dataset.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_autobox_params(
        input_=input_,
        prefix=prefix,
        alt_input=alt_input,
        noclust=noclust,
        extent=extent,
        extent_ijk=extent_ijk,
        extent_ijk_to_file=extent_ijk_to_file,
        extent_ijk_midslice=extent_ijk_midslice,
        extent_ijkord=extent_ijkord,
        extent_ijkord_to_file=extent_ijkord_to_file,
        extent_xyz_to_file=extent_xyz_to_file,
        extent_xyz_midslice=extent_xyz_midslice,
        npad=npad,
        npad_safety_on=npad_safety_on,
    )
    return v_3d_autobox_execute(params, runner)

__all__ = [
    "V3dAutobox",
    "V3dAutoboxOutputs",
    "V_3D_AUTOBOX_METADATA",
    "v_3d_autobox_cargs",
    "v_3d_autobox_outputs",
    "v_3d_autobox_params",
    "v_3d_autobox_execute",
    "v_3d_autobox_validate",
    "v_3d_autobox",
]