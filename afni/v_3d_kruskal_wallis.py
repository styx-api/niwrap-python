# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_KRUSKAL_WALLIS_METADATA = Metadata(
    id="3dKruskalWallis",
    name="3dKruskalWallis",
    package="afni",
)

V3dKruskalWallis = typing.TypedDict(
    "V3dKruskalWallis",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dKruskalWallis"]],
        "levels": int,
        "datasets": list[str],
        "workmem": typing.NotRequired[int],
        "voxel": typing.NotRequired[int],
        "output": str,
    },
)

@dataclasses.dataclass
class V3dKruskalWallisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile_prefix: OutputPathType
    """Output file containing Kruskal-Wallis statistics"""

def v_3d_kruskal_wallis_params(
    levels: int,
    datasets: list[str],
    output: str,
    workmem: int | None = None,
    voxel: int | None = None,
) -> V3dKruskalWallis:
    """
    Build parameters.

    Args:
        levels: Number of treatments
        datasets: Data set for treatment #1 through to treatment #s. Specify\
            sub-brick if more than one present.
        output: Kruskal-Wallis statistics are written to file prefixname
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel: Screen output for voxel # num

    Returns:
        Parameter dictionary.
    """
    params: V3dKruskalWallis = {
        "@type": "afni/3dKruskalWallis",
        "levels": levels,
        "datasets": datasets,
        "output": output,
    }
    if workmem is not None:
        params["workmem"] = workmem
    if voxel is not None:
        params["voxel"] = voxel
    return params

def v_3d_kruskal_wallis_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dKruskalWallis."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("levels", None) is None:
        raise StyxValidationError("`levels` must not be None")
    if not isinstance(params["levels"], int):
        raise StyxValidationError(f'`levels` has the wrong type: Received `{type(params["levels"])}` expected `int`')
    if params["levels"] < 2:
        raise StyxValidationError("Parameter `levels` must be at least 2")
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[str]`')
    for e in params["datasets"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("workmem", None) is not None:
        if not isinstance(params["workmem"], int):
            raise StyxValidationError(f'`workmem` has the wrong type: Received `{type(params["workmem"])}` expected `int`')
    if params.get("voxel", None) is not None:
        if not isinstance(params["voxel"], int):
            raise StyxValidationError(f'`voxel` has the wrong type: Received `{type(params["voxel"])}` expected `int`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def v_3d_kruskal_wallis_cargs(params: V3dKruskalWallis, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dKruskalWallis")
    cargs.append("-levels")
    cargs.append(str(params["levels"]))
    cargs.append("-dset")
    for item0 in params["datasets"]:
        cargs.append(item0)
    v_0 = params.get("workmem")
    if v_0 is not None:
        cargs.append("-workmem")
        cargs.append(str(v_0))
    v_1 = params.get("voxel")
    if v_1 is not None:
        cargs.append("-voxel")
        cargs.append(str(v_1))
    cargs.append("-out")
    cargs.append(params["output"])
    return cargs

def v_3d_kruskal_wallis_outputs(params: V3dKruskalWallis, execution: Execution) -> V3dKruskalWallisOutputs:
    """Build the V3dKruskalWallisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_prefix_v: OutputPathType = execution.output_file(f'{params["output"]}+tlrc')
    return V3dKruskalWallisOutputs(
        root=root_v,
        outfile_prefix=outfile_prefix_v,
    )

def v_3d_kruskal_wallis_execute(params: V3dKruskalWallis, runner: Runner | None = None) -> V3dKruskalWallisOutputs:
    """
    3dKruskalWallis

    This program performs nonparametric Kruskal-Wallis test for comparison of multiple treatments.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_kruskal_wallis_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_KRUSKAL_WALLIS_METADATA)
    execution.params(params)
    args = v_3d_kruskal_wallis_cargs(params, execution)
    out = v_3d_kruskal_wallis_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_kruskal_wallis(
    levels: int,
    datasets: list[str],
    output: str,
    workmem: int | None = None,
    voxel: int | None = None,
    runner: Runner | None = None,
) -> V3dKruskalWallisOutputs:
    """
    3dKruskalWallis

    This program performs nonparametric Kruskal-Wallis test for comparison of multiple treatments.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        levels: Number of treatments
        datasets: Data set for treatment #1 through to treatment #s. Specify\
            sub-brick if more than one present.
        output: Kruskal-Wallis statistics are written to file prefixname
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel: Screen output for voxel # num
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_kruskal_wallis_params(
        levels=levels,
        datasets=datasets,
        output=output,
        workmem=workmem,
        voxel=voxel,
    )
    return v_3d_kruskal_wallis_execute(params, runner)

__all__ = [
    "V3dKruskalWallis",
    "V3dKruskalWallisOutputs",
    "V_3D_KRUSKAL_WALLIS_METADATA",
    "v_3d_kruskal_wallis_cargs",
    "v_3d_kruskal_wallis_outputs",
    "v_3d_kruskal_wallis_params",
    "v_3d_kruskal_wallis_execute",
    "v_3d_kruskal_wallis_validate",
    "v_3d_kruskal_wallis",
]