# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REORDER_METADATA = Metadata(
    id="@Reorder",
    name="@Reorder",
    package="afni",
)

Reorder = typing.TypedDict(
    "Reorder",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@Reorder"]],
        "input_dataset": InputPathType,
        "mapfile": InputPathType,
        "prefix": str,
        "offset": typing.NotRequired[float],
        "save_work": typing.NotRequired[bool],
        "test": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ReorderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType
    """Reordered output dataset"""

def reorder_params(
    input_dataset: InputPathType,
    mapfile: InputPathType,
    prefix: str,
    offset: float | None = None,
    save_work: bool = False,
    test: bool = False,
    help: bool = False,
) -> Reorder:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset to reorder (e.g. EPI+tlrc)
        mapfile: TR to event mapping file (e.g. events.txt)
        prefix: Prefix for the output dataset
        offset: Offset mapfile TR indices by OFFSET (in TRs)
        save_work: Do not delete work directory (reorder.work.dir) at the end
        test: Just report sub-bricks, do not create datasets
        help: Show help message

    Returns:
        Parameter dictionary.
    """
    params: Reorder = {
        "@type": "afni/@Reorder",
        "input_dataset": input_dataset,
        "mapfile": mapfile,
        "prefix": prefix,
        "save_work": save_work,
        "test": test,
        "help": help,
    }
    if offset is not None:
        params["offset"] = offset
    return params

def reorder_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Reorder."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("mapfile", None) is None:
        raise StyxValidationError("`mapfile` must not be None")
    if not isinstance(params["mapfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mapfile` has the wrong type: Received `{type(params["mapfile"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("offset", None) is not None:
        if not isinstance(params["offset"], (float, int)):
            raise StyxValidationError(f'`offset` has the wrong type: Received `{type(params["offset"])}` expected `float`')
    if params.get("save_work", None) is not None:
        if not isinstance(params["save_work"], bool):
            raise StyxValidationError(f'`save_work` has the wrong type: Received `{type(params["save_work"])}` expected `bool`')
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def reorder_cargs(params: Reorder, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@Reorder")
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append(execution.input_file(params["mapfile"]))
    cargs.append(params["prefix"])
    v_0 = params.get("offset")
    if v_0 is not None:
        cargs.append("-offset")
        cargs.append(str(v_0))
    if params.get("save_work"):
        cargs.append("-save_work")
    if params.get("test"):
        cargs.append("-test")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def reorder_outputs(params: Reorder, execution: Execution) -> ReorderOutputs:
    """Build the ReorderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dataset_v: OutputPathType = execution.output_file(f'{params["prefix"]}+tlrc')
    return ReorderOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def reorder_execute(params: Reorder, runner: Runner | None = None) -> ReorderOutputs:
    """
    @Reorder

    Reorder sub-bricks of a dataset based on event mapping. Works similarly to the Reorder plugin.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    reorder_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REORDER_METADATA)
    execution.params(params)
    args = reorder_cargs(params, execution)
    out = reorder_outputs(params, execution)
    execution.run(args)
    return out

def reorder(
    input_dataset: InputPathType,
    mapfile: InputPathType,
    prefix: str,
    offset: float | None = None,
    save_work: bool = False,
    test: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> ReorderOutputs:
    """
    @Reorder

    Reorder sub-bricks of a dataset based on event mapping. Works similarly to the Reorder plugin.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset to reorder (e.g. EPI+tlrc)
        mapfile: TR to event mapping file (e.g. events.txt)
        prefix: Prefix for the output dataset
        offset: Offset mapfile TR indices by OFFSET (in TRs)
        save_work: Do not delete work directory (reorder.work.dir) at the end
        test: Just report sub-bricks, do not create datasets
        help: Show help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = reorder_params(
        input_dataset=input_dataset,
        mapfile=mapfile,
        prefix=prefix,
        offset=offset,
        save_work=save_work,
        test=test,
        help=help,
    )
    return reorder_execute(params, runner)

__all__ = [
    "Reorder",
    "ReorderOutputs",
    "REORDER_METADATA",
    "reorder_cargs",
    "reorder_outputs",
    "reorder_params",
    "reorder_execute",
    "reorder_validate",
    "reorder",
]