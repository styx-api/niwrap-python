# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_DEPTH_MAP_METADATA = Metadata(
    id="3dDepthMap",
    name="3dDepthMap",
    package="afni",
)

V3dDepthMap = typing.TypedDict(
    "V3dDepthMap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dDepthMap"]],
        "input_dataset": InputPathType,
        "output_prefix": str,
        "mask": typing.NotRequired[InputPathType],
        "dist_squared": typing.NotRequired[bool],
        "ignore_voxdims": typing.NotRequired[bool],
        "rimify": typing.NotRequired[float],
        "zeros_are_zero": typing.NotRequired[bool],
        "zeros_are_neg": typing.NotRequired[bool],
        "nz_are_neg": typing.NotRequired[bool],
        "bounds_are_not_zero": typing.NotRequired[bool],
        "only2D": typing.NotRequired[str],
        "binary_only": typing.NotRequired[bool],
        "verbosity": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dDepthMapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Main output file"""

def v_3d_depth_map_params(
    input_dataset: InputPathType,
    output_prefix: str,
    mask: InputPathType | None = None,
    dist_squared: bool = False,
    ignore_voxdims: bool = False,
    rimify: float | None = None,
    zeros_are_zero: bool = False,
    zeros_are_neg: bool = False,
    nz_are_neg: bool = False,
    bounds_are_not_zero: bool = False,
    only2D: str | None = None,
    binary_only: bool = False,
    verbosity: float | None = None,
) -> V3dDepthMap:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset
        output_prefix: Output prefix name
        mask: Mask dataset, applied after the EDT has been calculated
        dist_squared: Output EDT values as distance squared
        ignore_voxdims: Ignore voxel dimensions, producing outputs as if each voxel\
            dimension was unity
        rimify: Output a map of each ROI's boundary layer up to thickness RIM
        zeros_are_zero: EDT values only reported within nonzero locations of the\
            input dataset
        zeros_are_neg: EDT values in the zero/background regions will be negative
        nz_are_neg: EDT values in the nonzero ROI regions will be negative
        bounds_are_not_zero: Treat FOV boundaries for nonzero ROIs as open (i.e.,\
            continue infinitely)
        only2D: Run EDT in 2D along the specified plane (axi|cor|sag)
        binary_only: Treat the input as a binary mask for a faster calculation
        verbosity: Manage verbosity when running code (default: 1)

    Returns:
        Parameter dictionary.
    """
    params: V3dDepthMap = {
        "@type": "afni/3dDepthMap",
        "input_dataset": input_dataset,
        "output_prefix": output_prefix,
        "dist_squared": dist_squared,
        "ignore_voxdims": ignore_voxdims,
        "zeros_are_zero": zeros_are_zero,
        "zeros_are_neg": zeros_are_neg,
        "nz_are_neg": nz_are_neg,
        "bounds_are_not_zero": bounds_are_not_zero,
        "binary_only": binary_only,
    }
    if mask is not None:
        params["mask"] = mask
    if rimify is not None:
        params["rimify"] = rimify
    if only2D is not None:
        params["only2D"] = only2D
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def v_3d_depth_map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dDepthMap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("dist_squared", None) is not None:
        if not isinstance(params["dist_squared"], bool):
            raise StyxValidationError(f'`dist_squared` has the wrong type: Received `{type(params["dist_squared"])}` expected `bool`')
    if params.get("ignore_voxdims", None) is not None:
        if not isinstance(params["ignore_voxdims"], bool):
            raise StyxValidationError(f'`ignore_voxdims` has the wrong type: Received `{type(params["ignore_voxdims"])}` expected `bool`')
    if params.get("rimify", None) is not None:
        if not isinstance(params["rimify"], (float, int)):
            raise StyxValidationError(f'`rimify` has the wrong type: Received `{type(params["rimify"])}` expected `float`')
    if params.get("zeros_are_zero", None) is not None:
        if not isinstance(params["zeros_are_zero"], bool):
            raise StyxValidationError(f'`zeros_are_zero` has the wrong type: Received `{type(params["zeros_are_zero"])}` expected `bool`')
    if params.get("zeros_are_neg", None) is not None:
        if not isinstance(params["zeros_are_neg"], bool):
            raise StyxValidationError(f'`zeros_are_neg` has the wrong type: Received `{type(params["zeros_are_neg"])}` expected `bool`')
    if params.get("nz_are_neg", None) is not None:
        if not isinstance(params["nz_are_neg"], bool):
            raise StyxValidationError(f'`nz_are_neg` has the wrong type: Received `{type(params["nz_are_neg"])}` expected `bool`')
    if params.get("bounds_are_not_zero", None) is not None:
        if not isinstance(params["bounds_are_not_zero"], bool):
            raise StyxValidationError(f'`bounds_are_not_zero` has the wrong type: Received `{type(params["bounds_are_not_zero"])}` expected `bool`')
    if params.get("only2D", None) is not None:
        if not isinstance(params["only2D"], str):
            raise StyxValidationError(f'`only2D` has the wrong type: Received `{type(params["only2D"])}` expected `str`')
    if params.get("binary_only", None) is not None:
        if not isinstance(params["binary_only"], bool):
            raise StyxValidationError(f'`binary_only` has the wrong type: Received `{type(params["binary_only"])}` expected `bool`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], (float, int)):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `float`')

def v_3d_depth_map_cargs(params: V3dDepthMap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dDepthMap")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append("-prefix")
    cargs.append(params["output_prefix"])
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("dist_squared"):
        cargs.append("-dist_sq")
    if params.get("ignore_voxdims"):
        cargs.append("-ignore_voxdims")
    v_1 = params.get("rimify")
    if v_1 is not None:
        cargs.append("-rimify")
        cargs.append(str(v_1))
    if params.get("zeros_are_zero"):
        cargs.append("-zeros_are_zero")
    if params.get("zeros_are_neg"):
        cargs.append("-zeros_are_neg")
    if params.get("nz_are_neg"):
        cargs.append("-nz_are_neg")
    if params.get("bounds_are_not_zero"):
        cargs.append("-bounds_are_not_zero")
    v_2 = params.get("only2D")
    if v_2 is not None:
        cargs.append("-only2D")
        cargs.append(v_2)
    if params.get("binary_only"):
        cargs.append("-binary_only")
    v_3 = params.get("verbosity")
    if v_3 is not None:
        cargs.append("-verb")
        cargs.append(str(v_3))
    return cargs

def v_3d_depth_map_outputs(params: V3dDepthMap, execution: Execution) -> V3dDepthMapOutputs:
    """Build the V3dDepthMapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}.nii.gz')
    return V3dDepthMapOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_depth_map_execute(params: V3dDepthMap, runner: Runner | None = None) -> V3dDepthMapOutputs:
    """
    3dDepthMap

    Calculates the Euclidean Distance Transform (EDT) for 3D volumes, allowing computation of ROI depth maps and applying various adjustments like masking and rimification.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_depth_map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_DEPTH_MAP_METADATA)
    execution.params(params)
    args = v_3d_depth_map_cargs(params, execution)
    out = v_3d_depth_map_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_depth_map(
    input_dataset: InputPathType,
    output_prefix: str,
    mask: InputPathType | None = None,
    dist_squared: bool = False,
    ignore_voxdims: bool = False,
    rimify: float | None = None,
    zeros_are_zero: bool = False,
    zeros_are_neg: bool = False,
    nz_are_neg: bool = False,
    bounds_are_not_zero: bool = False,
    only2D: str | None = None,
    binary_only: bool = False,
    verbosity: float | None = None,
    runner: Runner | None = None,
) -> V3dDepthMapOutputs:
    """
    3dDepthMap

    Calculates the Euclidean Distance Transform (EDT) for 3D volumes, allowing computation of ROI depth maps and applying various adjustments like masking and rimification.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset
        output_prefix: Output prefix name
        mask: Mask dataset, applied after the EDT has been calculated
        dist_squared: Output EDT values as distance squared
        ignore_voxdims: Ignore voxel dimensions, producing outputs as if each voxel\
            dimension was unity
        rimify: Output a map of each ROI's boundary layer up to thickness RIM
        zeros_are_zero: EDT values only reported within nonzero locations of the\
            input dataset
        zeros_are_neg: EDT values in the zero/background regions will be negative
        nz_are_neg: EDT values in the nonzero ROI regions will be negative
        bounds_are_not_zero: Treat FOV boundaries for nonzero ROIs as open (i.e.,\
            continue infinitely)
        only2D: Run EDT in 2D along the specified plane (axi|cor|sag)
        binary_only: Treat the input as a binary mask for a faster calculation
        verbosity: Manage verbosity when running code (default: 1)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_depth_map_params(
        input_dataset=input_dataset,
        output_prefix=output_prefix,
        mask=mask,
        dist_squared=dist_squared,
        ignore_voxdims=ignore_voxdims,
        rimify=rimify,
        zeros_are_zero=zeros_are_zero,
        zeros_are_neg=zeros_are_neg,
        nz_are_neg=nz_are_neg,
        bounds_are_not_zero=bounds_are_not_zero,
        only2D=only2D,
        binary_only=binary_only,
        verbosity=verbosity,
    )
    return v_3d_depth_map_execute(params, runner)

__all__ = [
    "V3dDepthMap",
    "V3dDepthMapOutputs",
    "V_3D_DEPTH_MAP_METADATA",
    "v_3d_depth_map_cargs",
    "v_3d_depth_map_outputs",
    "v_3d_depth_map_params",
    "v_3d_depth_map_execute",
    "v_3d_depth_map_validate",
    "v_3d_depth_map",
]