# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FLOAT_SCAN_METADATA = Metadata(
    id="float_scan",
    name="float_scan",
    package="afni",
)

FloatScan = typing.TypedDict(
    "FloatScan",
    {
        "@type": typing.NotRequired[typing.Literal["afni/float_scan"]],
        "fix_illegal_values": typing.NotRequired[bool],
        "verbose_mode": typing.NotRequired[bool],
        "skip_count": typing.NotRequired[int],
        "input_file": InputPathType,
    },
)

@dataclasses.dataclass
class FloatScanOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout_file: OutputPathType
    """Output file with illegal values replaced by 0 when -fix flag is used"""

def float_scan_params(
    input_file: InputPathType,
    fix_illegal_values: bool = False,
    verbose_mode: bool = False,
    skip_count: int | None = None,
) -> FloatScan:
    """
    Build parameters.

    Args:
        input_file: Input file containing IEEE floating point numbers
        fix_illegal_values: Writes a copy of the input file to stdout, replacing\
            illegal values with 0.
        verbose_mode: Verbose mode: print out index of each illegal value.
        skip_count: Skip the first n floating point locations (i.e., the first 4*n\
            bytes) in the file

    Returns:
        Parameter dictionary.
    """
    params: FloatScan = {
        "@type": "afni/float_scan",
        "input_file": input_file,
        "fix_illegal_values": fix_illegal_values,
        "verbose_mode": verbose_mode,
    }
    if skip_count is not None:
        params["skip_count"] = skip_count
    return params

def float_scan_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FloatScan."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fix_illegal_values", None) is not None:
        if not isinstance(params["fix_illegal_values"], bool):
            raise StyxValidationError(f'`fix_illegal_values` has the wrong type: Received `{type(params["fix_illegal_values"])}` expected `bool`')
    if params.get("verbose_mode", None) is not None:
        if not isinstance(params["verbose_mode"], bool):
            raise StyxValidationError(f'`verbose_mode` has the wrong type: Received `{type(params["verbose_mode"])}` expected `bool`')
    if params.get("skip_count", None) is not None:
        if not isinstance(params["skip_count"], int):
            raise StyxValidationError(f'`skip_count` has the wrong type: Received `{type(params["skip_count"])}` expected `int`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def float_scan_cargs(params: FloatScan, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("float_scan")
    if params.get("fix_illegal_values"):
        cargs.append("-fix")
    if params.get("verbose_mode"):
        cargs.append("-v")
    v_0 = params.get("skip_count")
    if v_0 is not None:
        cargs.append("-skip")
        cargs.append(str(v_0))
    cargs.append(execution.input_file(params["input_file"]))
    return cargs

def float_scan_outputs(params: FloatScan, execution: Execution) -> FloatScanOutputs:
    """Build the FloatScanOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stdout_file_v: OutputPathType = execution.output_file("stdout")
    return FloatScanOutputs(
        root=root_v,
        stdout_file=stdout_file_v,
    )

def float_scan_execute(params: FloatScan, runner: Runner | None = None) -> FloatScanOutputs:
    """
    float_scan

    Scans the input file of IEEE floating point numbers for illegal values: infinities and not-a-number (NaN) values.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    float_scan_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FLOAT_SCAN_METADATA)
    execution.params(params)
    args = float_scan_cargs(params, execution)
    out = float_scan_outputs(params, execution)
    execution.run(args)
    return out

def float_scan(
    input_file: InputPathType,
    fix_illegal_values: bool = False,
    verbose_mode: bool = False,
    skip_count: int | None = None,
    runner: Runner | None = None,
) -> FloatScanOutputs:
    """
    float_scan

    Scans the input file of IEEE floating point numbers for illegal values: infinities and not-a-number (NaN) values.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Input file containing IEEE floating point numbers
        fix_illegal_values: Writes a copy of the input file to stdout, replacing\
            illegal values with 0.
        verbose_mode: Verbose mode: print out index of each illegal value.
        skip_count: Skip the first n floating point locations (i.e., the first 4*n\
            bytes) in the file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = float_scan_params(
        input_file=input_file,
        fix_illegal_values=fix_illegal_values,
        verbose_mode=verbose_mode,
        skip_count=skip_count,
    )
    return float_scan_execute(params, runner)

__all__ = [
    "FloatScan",
    "FloatScanOutputs",
    "FLOAT_SCAN_METADATA",
    "float_scan_cargs",
    "float_scan_outputs",
    "float_scan_params",
    "float_scan_execute",
    "float_scan_validate",
    "float_scan",
]