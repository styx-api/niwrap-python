# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

APSEARCH_METADATA = Metadata(
    id="apsearch",
    name="apsearch",
    package="afni",
)

Apsearch = typing.TypedDict(
    "Apsearch",
    {
        "@type": typing.NotRequired[typing.Literal["afni/apsearch"]],
        "search_term": str,
        "file_output": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ApsearchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """File containing search results"""

def apsearch_params(
    search_term: str,
    file_output: str | None = None,
    verbose: bool = False,
) -> Apsearch:
    """
    Build parameters.

    Args:
        search_term: Term to search
        file_output: File to save the search results
        verbose: Print detailed information during search

    Returns:
        Parameter dictionary.
    """
    params: Apsearch = {
        "@type": "afni/apsearch",
        "search_term": search_term,
        "verbose": verbose,
    }
    if file_output is not None:
        params["file_output"] = file_output
    return params

def apsearch_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Apsearch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("search_term", None) is None:
        raise StyxValidationError("`search_term` must not be None")
    if not isinstance(params["search_term"], str):
        raise StyxValidationError(f'`search_term` has the wrong type: Received `{type(params["search_term"])}` expected `str`')
    if params.get("file_output", None) is not None:
        if not isinstance(params["file_output"], str):
            raise StyxValidationError(f'`file_output` has the wrong type: Received `{type(params["file_output"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def apsearch_cargs(params: Apsearch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("apsearch")
    cargs.append(params["search_term"])
    v_0 = params.get("file_output")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("verbose"):
        cargs.append("-v")
    return cargs

def apsearch_outputs(params: Apsearch, execution: Execution) -> ApsearchOutputs:
    """Build the ApsearchOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("file_output")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return ApsearchOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def apsearch_execute(params: Apsearch, runner: Runner | None = None) -> ApsearchOutputs:
    """
    apsearch

    A tool for searching applications.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    apsearch_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(APSEARCH_METADATA)
    execution.params(params)
    args = apsearch_cargs(params, execution)
    out = apsearch_outputs(params, execution)
    execution.run(args)
    return out

def apsearch(
    search_term: str,
    file_output: str | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> ApsearchOutputs:
    """
    apsearch

    A tool for searching applications.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        search_term: Term to search
        file_output: File to save the search results
        verbose: Print detailed information during search
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = apsearch_params(
        search_term=search_term,
        file_output=file_output,
        verbose=verbose,
    )
    return apsearch_execute(params, runner)

__all__ = [
    "Apsearch",
    "ApsearchOutputs",
    "APSEARCH_METADATA",
    "apsearch_cargs",
    "apsearch_outputs",
    "apsearch_params",
    "apsearch_execute",
    "apsearch_validate",
    "apsearch",
]