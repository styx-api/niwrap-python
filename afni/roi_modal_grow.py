# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ROI_MODAL_GROW_METADATA = Metadata(
    id="@ROI_modal_grow",
    name="@ROI_modal_grow",
    package="afni",
)

RoiModalGrow = typing.TypedDict(
    "RoiModalGrow",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ROI_modal_grow"]],
        "input_dset": InputPathType,
        "niters": float,
        "outdir": typing.NotRequired[str],
        "mask": typing.NotRequired[InputPathType],
        "prefix": typing.NotRequired[str],
        "neighborhood_type": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class RoiModalGrowOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Final output dataset"""

def roi_modal_grow_params(
    input_dset: InputPathType,
    niters: float,
    outdir: str | None = None,
    mask: InputPathType | None = None,
    prefix: str | None = None,
    neighborhood_type: int | None = None,
) -> RoiModalGrow:
    """
    Build parameters.

    Args:
        input_dset: Required input dataset. This dataset should be a set of integer\
            values, assuming approximate isotropic voxels.
        niters: Number of iterations for modal growth, generally making sense for\
            values from about 1-10.
        outdir: Directory name for output. All output goes to this directory.\
            Default is rmgrow.
        mask: Mask dataset at the same grid as the input dataset. Could be a\
            dilated version of the original mask or a larger region like a cortical\
            ribbon mask. Not required but often desirable.
        prefix: Base name of the final output dataset, i.e., baseprefix.nii.gz.\
            Default is rmg, so output would be rmg.nii.gz.
        neighborhood_type: Neighborhood type used in finding mode. 1 - facing\
            neighbors, 2 - edges, 3 - corners.

    Returns:
        Parameter dictionary.
    """
    params: RoiModalGrow = {
        "@type": "afni/@ROI_modal_grow",
        "input_dset": input_dset,
        "niters": niters,
    }
    if outdir is not None:
        params["outdir"] = outdir
    if mask is not None:
        params["mask"] = mask
    if prefix is not None:
        params["prefix"] = prefix
    if neighborhood_type is not None:
        params["neighborhood_type"] = neighborhood_type
    return params

def roi_modal_grow_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RoiModalGrow."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dset", None) is None:
        raise StyxValidationError("`input_dset` must not be None")
    if not isinstance(params["input_dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dset` has the wrong type: Received `{type(params["input_dset"])}` expected `InputPathType`')
    if params.get("niters", None) is None:
        raise StyxValidationError("`niters` must not be None")
    if not isinstance(params["niters"], (float, int)):
        raise StyxValidationError(f'`niters` has the wrong type: Received `{type(params["niters"])}` expected `float`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("neighborhood_type", None) is not None:
        if not isinstance(params["neighborhood_type"], int):
            raise StyxValidationError(f'`neighborhood_type` has the wrong type: Received `{type(params["neighborhood_type"])}` expected `int`')
        if not (1 <= params["neighborhood_type"] <= 3):
            raise StyxValidationError("Parameter `neighborhood_type` must be between 1 and 3 (inclusive)")

def roi_modal_grow_cargs(params: RoiModalGrow, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ROI_modal_grow")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_dset"]))
    cargs.append("-niters")
    cargs.append(str(params["niters"]))
    v_0 = params.get("outdir")
    if v_0 is not None:
        cargs.append("-outdir")
        cargs.append(v_0)
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    v_3 = params.get("neighborhood_type")
    if v_3 is not None:
        cargs.append("-NN")
        cargs.append(str(v_3))
    return cargs

def roi_modal_grow_outputs(params: RoiModalGrow, execution: Execution) -> RoiModalGrowOutputs:
    """Build the RoiModalGrowOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outdir")
    if __v0 is not None:
        __v1 = params.get("prefix")
        if __v1 is not None:
            output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/{(__v1 if __v1 is not None else "")}.nii.gz')
    return RoiModalGrowOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def roi_modal_grow_execute(params: RoiModalGrow, runner: Runner | None = None) -> RoiModalGrowOutputs:
    """
    @ROI_modal_grow

    Script to grow a set of regions in a volumetric dataset using modal smoothing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    roi_modal_grow_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ROI_MODAL_GROW_METADATA)
    execution.params(params)
    args = roi_modal_grow_cargs(params, execution)
    out = roi_modal_grow_outputs(params, execution)
    execution.run(args)
    return out

def roi_modal_grow(
    input_dset: InputPathType,
    niters: float,
    outdir: str | None = None,
    mask: InputPathType | None = None,
    prefix: str | None = None,
    neighborhood_type: int | None = None,
    runner: Runner | None = None,
) -> RoiModalGrowOutputs:
    """
    @ROI_modal_grow

    Script to grow a set of regions in a volumetric dataset using modal smoothing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dset: Required input dataset. This dataset should be a set of integer\
            values, assuming approximate isotropic voxels.
        niters: Number of iterations for modal growth, generally making sense for\
            values from about 1-10.
        outdir: Directory name for output. All output goes to this directory.\
            Default is rmgrow.
        mask: Mask dataset at the same grid as the input dataset. Could be a\
            dilated version of the original mask or a larger region like a cortical\
            ribbon mask. Not required but often desirable.
        prefix: Base name of the final output dataset, i.e., baseprefix.nii.gz.\
            Default is rmg, so output would be rmg.nii.gz.
        neighborhood_type: Neighborhood type used in finding mode. 1 - facing\
            neighbors, 2 - edges, 3 - corners.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = roi_modal_grow_params(
        input_dset=input_dset,
        niters=niters,
        outdir=outdir,
        mask=mask,
        prefix=prefix,
        neighborhood_type=neighborhood_type,
    )
    return roi_modal_grow_execute(params, runner)

__all__ = [
    "RoiModalGrow",
    "RoiModalGrowOutputs",
    "ROI_MODAL_GROW_METADATA",
    "roi_modal_grow_cargs",
    "roi_modal_grow_outputs",
    "roi_modal_grow_params",
    "roi_modal_grow_execute",
    "roi_modal_grow_validate",
    "roi_modal_grow",
]