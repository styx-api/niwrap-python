# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CLUST_EXP_CAT_LAB_METADATA = Metadata(
    id="@ClustExp_CatLab",
    name="@ClustExp_CatLab",
    package="afni",
)

ClustExpCatLab = typing.TypedDict(
    "ClustExpCatLab",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ClustExp_CatLab"]],
        "prefix": str,
        "input_file": InputPathType,
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ClustExpCatLabOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output data set concatenating labeled subbriks"""

def clust_exp_cat_lab_params(
    prefix: str,
    input_file: InputPathType,
    help: bool = False,
) -> ClustExpCatLab:
    """
    Build parameters.

    Args:
        prefix: Output file name
        input_file: Name of file containing the labels and data sets table (e.g.\
            subjects.csv)
        help: Show help information

    Returns:
        Parameter dictionary.
    """
    params: ClustExpCatLab = {
        "@type": "afni/@ClustExp_CatLab",
        "prefix": prefix,
        "input_file": input_file,
        "help": help,
    }
    return params

def clust_exp_cat_lab_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ClustExpCatLab."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def clust_exp_cat_lab_cargs(params: ClustExpCatLab, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ClustExp_CatLab")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_file"]))
    if params.get("help"):
        cargs.append("-help")
    return cargs

def clust_exp_cat_lab_outputs(params: ClustExpCatLab, execution: Execution) -> ClustExpCatLabOutputs:
    """Build the ClustExpCatLabOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}.nii.gz')
    return ClustExpCatLabOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def clust_exp_cat_lab_execute(params: ClustExpCatLab, runner: Runner | None = None) -> ClustExpCatLabOutputs:
    """
    @ClustExp_CatLab

    Helper script to concatenate and label a group of data sets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    clust_exp_cat_lab_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CLUST_EXP_CAT_LAB_METADATA)
    execution.params(params)
    args = clust_exp_cat_lab_cargs(params, execution)
    out = clust_exp_cat_lab_outputs(params, execution)
    execution.run(args)
    return out

def clust_exp_cat_lab(
    prefix: str,
    input_file: InputPathType,
    help: bool = False,
    runner: Runner | None = None,
) -> ClustExpCatLabOutputs:
    """
    @ClustExp_CatLab

    Helper script to concatenate and label a group of data sets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output file name
        input_file: Name of file containing the labels and data sets table (e.g.\
            subjects.csv)
        help: Show help information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = clust_exp_cat_lab_params(
        prefix=prefix,
        input_file=input_file,
        help=help,
    )
    return clust_exp_cat_lab_execute(params, runner)

__all__ = [
    "ClustExpCatLab",
    "ClustExpCatLabOutputs",
    "CLUST_EXP_CAT_LAB_METADATA",
    "clust_exp_cat_lab_cargs",
    "clust_exp_cat_lab_outputs",
    "clust_exp_cat_lab_params",
    "clust_exp_cat_lab_execute",
    "clust_exp_cat_lab_validate",
    "clust_exp_cat_lab",
]