# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TFIM_METADATA = Metadata(
    id="tfim",
    name="tfim",
    package="afni",
)

Tfim = typing.TypedDict(
    "Tfim",
    {
        "@type": typing.NotRequired[typing.Literal["afni/tfim"]],
        "prefix": typing.NotRequired[str],
        "pthresh": typing.NotRequired[float],
        "eqcorr": typing.NotRequired[float],
        "paired": typing.NotRequired[bool],
        "set1_images": list[InputPathType],
        "set2_images": list[InputPathType],
        "base1_value": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class TfimOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    diff_output: OutputPathType | None
    """Difference image output. Default prefix is 'tfim'."""
    tspm_output: OutputPathType | None
    """T-statistic of difference. Default prefix is 'tfim'."""
    corr_output: OutputPathType | None
    """Equivalent correlation statistic output. Written if -eqcorr is used."""

def tfim_params(
    set1_images: list[InputPathType],
    set2_images: list[InputPathType],
    prefix: str | None = None,
    pthresh: float | None = None,
    eqcorr: float | None = None,
    paired: bool = False,
    base1_value: float | None = None,
) -> Tfim:
    """
    Build parameters.

    Args:
        set1_images: First set of image files.
        set2_images: Second set of image files.
        prefix: Prefix for output filenames. Default is 'tfim'.
        pthresh: Significance level (per voxel) to threshold the output with.\
            Voxels with t-statistic less significant than this will have their diff\
            output zeroed. Default is no threshold.
        eqcorr: Write out the equivalent correlation statistic. The number 'dval'\
            is the value to use for 'dof'. Default is not to write this file.
        paired: Compare -set1 and -set2 using a paired sample t-test. Illegal with\
            the -base1 option.
        base1_value: Base value for the first set of images. Used for Usage 2.

    Returns:
        Parameter dictionary.
    """
    params: Tfim = {
        "@type": "afni/tfim",
        "set1_images": set1_images,
        "set2_images": set2_images,
        "paired": paired,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if pthresh is not None:
        params["pthresh"] = pthresh
    if eqcorr is not None:
        params["eqcorr"] = eqcorr
    if base1_value is not None:
        params["base1_value"] = base1_value
    return params

def tfim_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tfim."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("pthresh", None) is not None:
        if not isinstance(params["pthresh"], (float, int)):
            raise StyxValidationError(f'`pthresh` has the wrong type: Received `{type(params["pthresh"])}` expected `float`')
        if not (0 <= params["pthresh"] <= 1):
            raise StyxValidationError("Parameter `pthresh` must be between 0 and 1 (inclusive)")
    if params.get("eqcorr", None) is not None:
        if not isinstance(params["eqcorr"], (float, int)):
            raise StyxValidationError(f'`eqcorr` has the wrong type: Received `{type(params["eqcorr"])}` expected `float`')
    if params.get("paired", None) is not None:
        if not isinstance(params["paired"], bool):
            raise StyxValidationError(f'`paired` has the wrong type: Received `{type(params["paired"])}` expected `bool`')
    if params.get("set1_images", None) is None:
        raise StyxValidationError("`set1_images` must not be None")
    if not isinstance(params["set1_images"], list):
        raise StyxValidationError(f'`set1_images` has the wrong type: Received `{type(params["set1_images"])}` expected `list[InputPathType]`')
    for e in params["set1_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`set1_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("set2_images", None) is None:
        raise StyxValidationError("`set2_images` must not be None")
    if not isinstance(params["set2_images"], list):
        raise StyxValidationError(f'`set2_images` has the wrong type: Received `{type(params["set2_images"])}` expected `list[InputPathType]`')
    for e_2 in params["set2_images"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`set2_images` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("base1_value", None) is not None:
        if not isinstance(params["base1_value"], (float, int)):
            raise StyxValidationError(f'`base1_value` has the wrong type: Received `{type(params["base1_value"])}` expected `float`')

def tfim_cargs(params: Tfim, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tfim")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("pthresh")
    if v_1 is not None:
        cargs.append("-pthresh")
        cargs.append(str(v_1))
    v_2 = params.get("eqcorr")
    if v_2 is not None:
        cargs.append("-eqcorr")
        cargs.append(str(v_2))
    if params.get("paired"):
        cargs.append("-paired")
    cargs.append("-set1")
    for item0 in params["set1_images"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-set2")
    for item1 in params["set2_images"]:
        cargs.append(execution.input_file(item1))
    v_3 = params.get("base1_value")
    if v_3 is not None:
        cargs.append("-base1")
        cargs.append(str(v_3))
    return cargs

def tfim_outputs(params: Tfim, execution: Execution) -> TfimOutputs:
    """Build the TfimOutputs object for this tool."""
    diff_output_v: OutputPathType | None = None
    tspm_output_v: OutputPathType | None = None
    corr_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        diff_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.diff')
    __v1 = params.get("prefix")
    if __v1 is not None:
        tspm_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.tspm')
    __v2 = params.get("prefix")
    if __v2 is not None:
        corr_output_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.corr')
    return TfimOutputs(
        root=root_v,
        diff_output=diff_output_v,
        tspm_output=tspm_output_v,
        corr_output=corr_output_v,
    )

def tfim_execute(params: Tfim, runner: Runner | None = None) -> TfimOutputs:
    """
    tfim

    MCW TFIM: t-tests on sets of functional images

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tfim_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TFIM_METADATA)
    execution.params(params)
    args = tfim_cargs(params, execution)
    out = tfim_outputs(params, execution)
    execution.run(args)
    return out

def tfim(
    set1_images: list[InputPathType],
    set2_images: list[InputPathType],
    prefix: str | None = None,
    pthresh: float | None = None,
    eqcorr: float | None = None,
    paired: bool = False,
    base1_value: float | None = None,
    runner: Runner | None = None,
) -> TfimOutputs:
    """
    tfim

    MCW TFIM: t-tests on sets of functional images

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        set1_images: First set of image files.
        set2_images: Second set of image files.
        prefix: Prefix for output filenames. Default is 'tfim'.
        pthresh: Significance level (per voxel) to threshold the output with.\
            Voxels with t-statistic less significant than this will have their diff\
            output zeroed. Default is no threshold.
        eqcorr: Write out the equivalent correlation statistic. The number 'dval'\
            is the value to use for 'dof'. Default is not to write this file.
        paired: Compare -set1 and -set2 using a paired sample t-test. Illegal with\
            the -base1 option.
        base1_value: Base value for the first set of images. Used for Usage 2.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tfim_params(
        set1_images=set1_images,
        set2_images=set2_images,
        prefix=prefix,
        pthresh=pthresh,
        eqcorr=eqcorr,
        paired=paired,
        base1_value=base1_value,
    )
    return tfim_execute(params, runner)

__all__ = [
    "Tfim",
    "TfimOutputs",
    "TFIM_METADATA",
    "tfim_cargs",
    "tfim_outputs",
    "tfim_params",
    "tfim_execute",
    "tfim_validate",
    "tfim",
]