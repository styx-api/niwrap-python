# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_RANK_METADATA = Metadata(
    id="3dRank",
    name="3dRank",
    package="afni",
)

V3dRank = typing.TypedDict(
    "V3dRank",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dRank"]],
        "input_datasets": list[InputPathType],
        "output_prefix": typing.NotRequired[str],
        "version_info": typing.NotRequired[bool],
        "help_info": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dRankOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset_head: OutputPathType | None
    """Main output dataset in AFNI format (.HEAD file)"""
    output_dataset_brik: OutputPathType | None
    """Main output dataset in AFNI format (.BRIK file)"""
    rank_map_file: OutputPathType | None
    """Rank map 1D file showing the mapping from the rank to the integral values in the input dataset"""

def v_3d_rank_params(
    input_datasets: list[InputPathType],
    output_prefix: str | None = None,
    version_info: bool = False,
    help_info: bool = False,
) -> V3dRank:
    """
    Build parameters.

    Args:
        input_datasets: Input datasets. Acceptable data types are: byte, short, and\
            floats.
        output_prefix: Output prefix. If you have multiple datasets on input, the\
            prefix is preceded by r00., r01., etc. If no prefix is given, the\
            default is rank.DATASET1, rank.DATASET2, etc.
        version_info: Print author and version info
        help_info: Print this help screen

    Returns:
        Parameter dictionary.
    """
    params: V3dRank = {
        "@type": "afni/3dRank",
        "input_datasets": input_datasets,
        "version_info": version_info,
        "help_info": help_info,
    }
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    return params

def v_3d_rank_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dRank."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_datasets", None) is None:
        raise StyxValidationError("`input_datasets` must not be None")
    if not isinstance(params["input_datasets"], list):
        raise StyxValidationError(f'`input_datasets` has the wrong type: Received `{type(params["input_datasets"])}` expected `list[InputPathType]`')
    for e in params["input_datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("version_info", None) is not None:
        if not isinstance(params["version_info"], bool):
            raise StyxValidationError(f'`version_info` has the wrong type: Received `{type(params["version_info"])}` expected `bool`')
    if params.get("help_info", None) is not None:
        if not isinstance(params["help_info"], bool):
            raise StyxValidationError(f'`help_info` has the wrong type: Received `{type(params["help_info"])}` expected `bool`')

def v_3d_rank_cargs(params: V3dRank, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dRank")
    for item0 in params["input_datasets"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("output_prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("version_info"):
        cargs.append("-ver")
    if params.get("help_info"):
        cargs.append("-help")
    return cargs

def v_3d_rank_outputs(params: V3dRank, execution: Execution) -> V3dRankOutputs:
    """Build the V3dRankOutputs object for this tool."""
    output_dataset_head_v: OutputPathType | None = None
    output_dataset_brik_v: OutputPathType | None = None
    rank_map_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_prefix")
    if __v0 is not None:
        output_dataset_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.HEAD')
    __v1 = params.get("output_prefix")
    if __v1 is not None:
        output_dataset_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.BRIK')
    __v2 = params.get("output_prefix")
    if __v2 is not None:
        rank_map_file_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.1D')
    return V3dRankOutputs(
        root=root_v,
        output_dataset_head=output_dataset_head_v,
        output_dataset_brik=output_dataset_brik_v,
        rank_map_file=rank_map_file_v,
    )

def v_3d_rank_execute(params: V3dRank, runner: Runner | None = None) -> V3dRankOutputs:
    """
    3dRank

    Replaces voxel values by their rank in the set of values collected over all voxels in all input datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_rank_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_RANK_METADATA)
    execution.params(params)
    args = v_3d_rank_cargs(params, execution)
    out = v_3d_rank_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_rank(
    input_datasets: list[InputPathType],
    output_prefix: str | None = None,
    version_info: bool = False,
    help_info: bool = False,
    runner: Runner | None = None,
) -> V3dRankOutputs:
    """
    3dRank

    Replaces voxel values by their rank in the set of values collected over all voxels in all input datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_datasets: Input datasets. Acceptable data types are: byte, short, and\
            floats.
        output_prefix: Output prefix. If you have multiple datasets on input, the\
            prefix is preceded by r00., r01., etc. If no prefix is given, the\
            default is rank.DATASET1, rank.DATASET2, etc.
        version_info: Print author and version info
        help_info: Print this help screen
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_rank_params(
        input_datasets=input_datasets,
        output_prefix=output_prefix,
        version_info=version_info,
        help_info=help_info,
    )
    return v_3d_rank_execute(params, runner)

__all__ = [
    "V3dRank",
    "V3dRankOutputs",
    "V_3D_RANK_METADATA",
    "v_3d_rank_cargs",
    "v_3d_rank_outputs",
    "v_3d_rank_params",
    "v_3d_rank_execute",
    "v_3d_rank_validate",
    "v_3d_rank",
]