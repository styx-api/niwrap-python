# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMUPSAM_METADATA = Metadata(
    id="imupsam",
    name="imupsam",
    package="afni",
)

Imupsam = typing.TypedDict(
    "Imupsam",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imupsam"]],
        "ascii_flag": typing.NotRequired[bool],
        "factor": int,
        "input_image": InputPathType,
        "output_image": str,
    },
)

@dataclasses.dataclass
class ImupsamOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType
    """Upsampled image output file"""

def imupsam_params(
    factor: int,
    input_image: InputPathType,
    output_image: str,
    ascii_flag: bool = False,
) -> Imupsam:
    """
    Build parameters.

    Args:
        factor: Upsampling factor; must be an integer in the range 2 to 30
        input_image: Path of the input 2D image file
        output_image: Path of the output upsampled image file. Use '-' to write to\
            stdout.
        ascii_flag: Write the result in ASCII format: all numbers for the file are\
            output, with no header info

    Returns:
        Parameter dictionary.
    """
    params: Imupsam = {
        "@type": "afni/imupsam",
        "factor": factor,
        "input_image": input_image,
        "output_image": output_image,
        "ascii_flag": ascii_flag,
    }
    return params

def imupsam_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imupsam."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ascii_flag", None) is not None:
        if not isinstance(params["ascii_flag"], bool):
            raise StyxValidationError(f'`ascii_flag` has the wrong type: Received `{type(params["ascii_flag"])}` expected `bool`')
    if params.get("factor", None) is None:
        raise StyxValidationError("`factor` must not be None")
    if not isinstance(params["factor"], int):
        raise StyxValidationError(f'`factor` has the wrong type: Received `{type(params["factor"])}` expected `int`')
    if not (2 <= params["factor"] <= 30):
        raise StyxValidationError("Parameter `factor` must be between 2 and 30 (inclusive)")
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')

def imupsam_cargs(params: Imupsam, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imupsam")
    if params.get("ascii_flag"):
        cargs.append("-A")
    cargs.append(str(params["factor"]))
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    return cargs

def imupsam_outputs(params: Imupsam, execution: Execution) -> ImupsamOutputs:
    """Build the ImupsamOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_file_v: OutputPathType = execution.output_file(params["output_image"])
    return ImupsamOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
    )

def imupsam_execute(params: Imupsam, runner: Runner | None = None) -> ImupsamOutputs:
    """
    imupsam

    Upsamples a 2D image by a specified factor

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imupsam_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMUPSAM_METADATA)
    execution.params(params)
    args = imupsam_cargs(params, execution)
    out = imupsam_outputs(params, execution)
    execution.run(args)
    return out

def imupsam(
    factor: int,
    input_image: InputPathType,
    output_image: str,
    ascii_flag: bool = False,
    runner: Runner | None = None,
) -> ImupsamOutputs:
    """
    imupsam

    Upsamples a 2D image by a specified factor

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        factor: Upsampling factor; must be an integer in the range 2 to 30
        input_image: Path of the input 2D image file
        output_image: Path of the output upsampled image file. Use '-' to write to\
            stdout.
        ascii_flag: Write the result in ASCII format: all numbers for the file are\
            output, with no header info
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imupsam_params(
        factor=factor,
        input_image=input_image,
        output_image=output_image,
        ascii_flag=ascii_flag,
    )
    return imupsam_execute(params, runner)

__all__ = [
    "Imupsam",
    "ImupsamOutputs",
    "IMUPSAM_METADATA",
    "imupsam_cargs",
    "imupsam_outputs",
    "imupsam_params",
    "imupsam_execute",
    "imupsam_validate",
    "imupsam",
]