# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SAMP_BIAS_METADATA = Metadata(
    id="SampBias",
    name="SampBias",
    package="afni",
)

SampBias = typing.TypedDict(
    "SampBias",
    {
        "@type": typing.NotRequired[typing.Literal["afni/SampBias"]],
        "specfile": InputPathType,
        "surfname": str,
        "plimit": typing.NotRequired[float],
        "dlimit": typing.NotRequired[float],
        "outfile": str,
        "prefix": typing.NotRequired[str],
        "segdo": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SampBiasOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_1D: OutputPathType
    """Output results in .1D format"""
    out_prefix: OutputPathType | None
    """Output results in a proper surface-based dataset."""

def samp_bias_params(
    specfile: InputPathType,
    surfname: str,
    outfile: str,
    plimit: float | None = None,
    dlimit: float | None = None,
    prefix: str | None = None,
    segdo: str | None = None,
) -> SampBias:
    """
    Build parameters.

    Args:
        specfile: Spec file containing input surfaces.
        surfname: Name of input surface.
        outfile: Output results in .1D format.
        plimit: Maximum length of path along surface in mm. Default is 50 mm.
        dlimit: Maximum length of euclidean distance in mm. Default is 1000 mm.
        prefix: Output results into a proper surface-based dataset.
        segdo: Output a displayable object file that contains segments between\
            paired nodes.

    Returns:
        Parameter dictionary.
    """
    params: SampBias = {
        "@type": "afni/SampBias",
        "specfile": specfile,
        "surfname": surfname,
        "outfile": outfile,
    }
    if plimit is not None:
        params["plimit"] = plimit
    if dlimit is not None:
        params["dlimit"] = dlimit
    if prefix is not None:
        params["prefix"] = prefix
    if segdo is not None:
        params["segdo"] = segdo
    return params

def samp_bias_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SampBias."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("specfile", None) is None:
        raise StyxValidationError("`specfile` must not be None")
    if not isinstance(params["specfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`specfile` has the wrong type: Received `{type(params["specfile"])}` expected `InputPathType`')
    if params.get("surfname", None) is None:
        raise StyxValidationError("`surfname` must not be None")
    if not isinstance(params["surfname"], str):
        raise StyxValidationError(f'`surfname` has the wrong type: Received `{type(params["surfname"])}` expected `str`')
    if params.get("plimit", None) is not None:
        if not isinstance(params["plimit"], (float, int)):
            raise StyxValidationError(f'`plimit` has the wrong type: Received `{type(params["plimit"])}` expected `float`')
    if params.get("dlimit", None) is not None:
        if not isinstance(params["dlimit"], (float, int)):
            raise StyxValidationError(f'`dlimit` has the wrong type: Received `{type(params["dlimit"])}` expected `float`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("segdo", None) is not None:
        if not isinstance(params["segdo"], str):
            raise StyxValidationError(f'`segdo` has the wrong type: Received `{type(params["segdo"])}` expected `str`')

def samp_bias_cargs(params: SampBias, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SampBias")
    cargs.append("-spec")
    cargs.append(execution.input_file(params["specfile"]))
    cargs.append("-surf")
    cargs.append(params["surfname"])
    v_0 = params.get("plimit")
    if v_0 is not None:
        cargs.append("-plimit")
        cargs.append(str(v_0))
    v_1 = params.get("dlimit")
    if v_1 is not None:
        cargs.append("-dlimit")
        cargs.append(str(v_1))
    cargs.append("-out")
    cargs.append(params["outfile"])
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    v_3 = params.get("segdo")
    if v_3 is not None:
        cargs.append("-segdo")
        cargs.append(v_3)
    return cargs

def samp_bias_outputs(params: SampBias, execution: Execution) -> SampBiasOutputs:
    """Build the SampBiasOutputs object for this tool."""
    out_prefix_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_1D_v: OutputPathType = execution.output_file(f'{params["outfile"]}.1D')
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_prefix_v = execution.output_file((__v0 if __v0 is not None else ""))
    return SampBiasOutputs(
        root=root_v,
        out_1D=out_1D_v,
        out_prefix=out_prefix_v,
    )

def samp_bias_execute(params: SampBias, runner: Runner | None = None) -> SampBiasOutputs:
    """
    SampBias

    SampBias is a tool for sampling bias resultant segments between paired nodes on anatomical surfaces.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    samp_bias_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SAMP_BIAS_METADATA)
    execution.params(params)
    args = samp_bias_cargs(params, execution)
    out = samp_bias_outputs(params, execution)
    execution.run(args)
    return out

def samp_bias(
    specfile: InputPathType,
    surfname: str,
    outfile: str,
    plimit: float | None = None,
    dlimit: float | None = None,
    prefix: str | None = None,
    segdo: str | None = None,
    runner: Runner | None = None,
) -> SampBiasOutputs:
    """
    SampBias

    SampBias is a tool for sampling bias resultant segments between paired nodes on anatomical surfaces.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        specfile: Spec file containing input surfaces.
        surfname: Name of input surface.
        outfile: Output results in .1D format.
        plimit: Maximum length of path along surface in mm. Default is 50 mm.
        dlimit: Maximum length of euclidean distance in mm. Default is 1000 mm.
        prefix: Output results into a proper surface-based dataset.
        segdo: Output a displayable object file that contains segments between\
            paired nodes.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = samp_bias_params(
        specfile=specfile,
        surfname=surfname,
        outfile=outfile,
        plimit=plimit,
        dlimit=dlimit,
        prefix=prefix,
        segdo=segdo,
    )
    return samp_bias_execute(params, runner)

__all__ = [
    "SampBias",
    "SampBiasOutputs",
    "SAMP_BIAS_METADATA",
    "samp_bias_cargs",
    "samp_bias_outputs",
    "samp_bias_params",
    "samp_bias_execute",
    "samp_bias_validate",
    "samp_bias",
]