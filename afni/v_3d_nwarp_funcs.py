# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_NWARP_FUNCS_METADATA = Metadata(
    id="3dNwarpFuncs",
    name="3dNwarpFuncs",
    package="afni",
)

V3dNwarpFuncs = typing.TypedDict(
    "V3dNwarpFuncs",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dNwarpFuncs"]],
        "input_warp": InputPathType,
        "output_prefix": str,
        "bulk_flag": typing.NotRequired[bool],
        "shear_flag": typing.NotRequired[bool],
        "vorticity_flag": typing.NotRequired[bool],
        "all_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dNwarpFuncsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """The output dataset with the computed functions."""

def v_3d_nwarp_funcs_params(
    input_warp: InputPathType,
    output_prefix: str,
    bulk_flag: bool = False,
    shear_flag: bool = False,
    vorticity_flag: bool = False,
    all_flag: bool = False,
) -> V3dNwarpFuncs:
    """
    Build parameters.

    Args:
        input_warp: 'www' is the name of the 3D warp dataset (mandatory option).
        output_prefix: 'ppp' is the name of the new output dataset.
        bulk_flag: Compute the (fractional) bulk volume change (Jacobian\
            determinant minus 1).
        shear_flag: Compute the shear energy.
        vorticity_flag: Compute the vorticity energy.
        all_flag: Compute all 3 functions: bulk, shear, and vorticity.

    Returns:
        Parameter dictionary.
    """
    params: V3dNwarpFuncs = {
        "@type": "afni/3dNwarpFuncs",
        "input_warp": input_warp,
        "output_prefix": output_prefix,
        "bulk_flag": bulk_flag,
        "shear_flag": shear_flag,
        "vorticity_flag": vorticity_flag,
        "all_flag": all_flag,
    }
    return params

def v_3d_nwarp_funcs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dNwarpFuncs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_warp", None) is None:
        raise StyxValidationError("`input_warp` must not be None")
    if not isinstance(params["input_warp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_warp` has the wrong type: Received `{type(params["input_warp"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("bulk_flag", None) is not None:
        if not isinstance(params["bulk_flag"], bool):
            raise StyxValidationError(f'`bulk_flag` has the wrong type: Received `{type(params["bulk_flag"])}` expected `bool`')
    if params.get("shear_flag", None) is not None:
        if not isinstance(params["shear_flag"], bool):
            raise StyxValidationError(f'`shear_flag` has the wrong type: Received `{type(params["shear_flag"])}` expected `bool`')
    if params.get("vorticity_flag", None) is not None:
        if not isinstance(params["vorticity_flag"], bool):
            raise StyxValidationError(f'`vorticity_flag` has the wrong type: Received `{type(params["vorticity_flag"])}` expected `bool`')
    if params.get("all_flag", None) is not None:
        if not isinstance(params["all_flag"], bool):
            raise StyxValidationError(f'`all_flag` has the wrong type: Received `{type(params["all_flag"])}` expected `bool`')

def v_3d_nwarp_funcs_cargs(params: V3dNwarpFuncs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dNwarpFuncs")
    cargs.append("-nwarp")
    cargs.append(execution.input_file(params["input_warp"]))
    cargs.append("-prefix")
    cargs.append(params["output_prefix"])
    if params.get("bulk_flag"):
        cargs.append("-bulk")
    if params.get("shear_flag"):
        cargs.append("-shear")
    if params.get("vorticity_flag"):
        cargs.append("-vorticity")
    if params.get("all_flag"):
        cargs.append("-all")
    return cargs

def v_3d_nwarp_funcs_outputs(params: V3dNwarpFuncs, execution: Execution) -> V3dNwarpFuncsOutputs:
    """Build the V3dNwarpFuncsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_output.nii.gz')
    return V3dNwarpFuncsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_nwarp_funcs_execute(params: V3dNwarpFuncs, runner: Runner | None = None) -> V3dNwarpFuncsOutputs:
    """
    3dNwarpFuncs

    Compute functions of 3D warp displacements, such as bulk volume change, shear energy, and vorticity energy.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_nwarp_funcs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_NWARP_FUNCS_METADATA)
    execution.params(params)
    args = v_3d_nwarp_funcs_cargs(params, execution)
    out = v_3d_nwarp_funcs_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_nwarp_funcs(
    input_warp: InputPathType,
    output_prefix: str,
    bulk_flag: bool = False,
    shear_flag: bool = False,
    vorticity_flag: bool = False,
    all_flag: bool = False,
    runner: Runner | None = None,
) -> V3dNwarpFuncsOutputs:
    """
    3dNwarpFuncs

    Compute functions of 3D warp displacements, such as bulk volume change, shear energy, and vorticity energy.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_warp: 'www' is the name of the 3D warp dataset (mandatory option).
        output_prefix: 'ppp' is the name of the new output dataset.
        bulk_flag: Compute the (fractional) bulk volume change (Jacobian\
            determinant minus 1).
        shear_flag: Compute the shear energy.
        vorticity_flag: Compute the vorticity energy.
        all_flag: Compute all 3 functions: bulk, shear, and vorticity.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_nwarp_funcs_params(
        input_warp=input_warp,
        output_prefix=output_prefix,
        bulk_flag=bulk_flag,
        shear_flag=shear_flag,
        vorticity_flag=vorticity_flag,
        all_flag=all_flag,
    )
    return v_3d_nwarp_funcs_execute(params, runner)

__all__ = [
    "V3dNwarpFuncs",
    "V3dNwarpFuncsOutputs",
    "V_3D_NWARP_FUNCS_METADATA",
    "v_3d_nwarp_funcs_cargs",
    "v_3d_nwarp_funcs_outputs",
    "v_3d_nwarp_funcs_params",
    "v_3d_nwarp_funcs_execute",
    "v_3d_nwarp_funcs_validate",
    "v_3d_nwarp_funcs",
]