# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_MVM_GRIDCONV_PY_METADATA = Metadata(
    id="fat_mvm_gridconv.py",
    name="fat_mvm_gridconv.py",
    package="afni",
)

FatMvmGridconvPy = typing.TypedDict(
    "FatMvmGridconvPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_mvm_gridconv.py"]],
        "matrix_files": typing.NotRequired[str],
        "list_file": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class FatMvmGridconvPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fat_mvm_gridconv_py_params(
    matrix_files: str | None = None,
    list_file: InputPathType | None = None,
) -> FatMvmGridconvPy:
    """
    Build parameters.

    Args:
        matrix_files: Provide the set of matrix (*.grid) files as a searchable\
            path. This can be a globbable entry in quotes containing wildcard\
            characters.
        list_file: Provide the matrix (*.grid) files by explicit path in a text\
            file. The LIST text file must contain at least one column (path to\
            subject matrix file) with an optional second column (output file\
            names). If no second column is given, the default '_MOD.grid' postfix\
            is applied.

    Returns:
        Parameter dictionary.
    """
    params: FatMvmGridconvPy = {
        "@type": "afni/fat_mvm_gridconv.py",
    }
    if matrix_files is not None:
        params["matrix_files"] = matrix_files
    if list_file is not None:
        params["list_file"] = list_file
    return params

def fat_mvm_gridconv_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatMvmGridconvPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("matrix_files", None) is not None:
        if not isinstance(params["matrix_files"], str):
            raise StyxValidationError(f'`matrix_files` has the wrong type: Received `{type(params["matrix_files"])}` expected `str`')
    if params.get("list_file", None) is not None:
        if not isinstance(params["list_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`list_file` has the wrong type: Received `{type(params["list_file"])}` expected `InputPathType`')

def fat_mvm_gridconv_py_cargs(params: FatMvmGridconvPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_mvm_gridconv.py")
    v_0 = params.get("matrix_files")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(v_0)
    v_1 = params.get("list_file")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_1))
    return cargs

def fat_mvm_gridconv_py_outputs(params: FatMvmGridconvPy, execution: Execution) -> FatMvmGridconvPyOutputs:
    """Build the FatMvmGridconvPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FatMvmGridconvPyOutputs(
        root=root_v,
    )

def fat_mvm_gridconv_py_execute(params: FatMvmGridconvPy, runner: Runner | None = None) -> FatMvmGridconvPyOutputs:
    """
    fat_mvm_gridconv.py

    Preprocess 'old school' *.grid files for statistical modeling using 3dMVM.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_mvm_gridconv_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_MVM_GRIDCONV_PY_METADATA)
    execution.params(params)
    args = fat_mvm_gridconv_py_cargs(params, execution)
    out = fat_mvm_gridconv_py_outputs(params, execution)
    execution.run(args)
    return out

def fat_mvm_gridconv_py(
    matrix_files: str | None = None,
    list_file: InputPathType | None = None,
    runner: Runner | None = None,
) -> FatMvmGridconvPyOutputs:
    """
    fat_mvm_gridconv.py

    Preprocess 'old school' *.grid files for statistical modeling using 3dMVM.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        matrix_files: Provide the set of matrix (*.grid) files as a searchable\
            path. This can be a globbable entry in quotes containing wildcard\
            characters.
        list_file: Provide the matrix (*.grid) files by explicit path in a text\
            file. The LIST text file must contain at least one column (path to\
            subject matrix file) with an optional second column (output file\
            names). If no second column is given, the default '_MOD.grid' postfix\
            is applied.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_mvm_gridconv_py_params(
        matrix_files=matrix_files,
        list_file=list_file,
    )
    return fat_mvm_gridconv_py_execute(params, runner)

__all__ = [
    "FatMvmGridconvPy",
    "FatMvmGridconvPyOutputs",
    "FAT_MVM_GRIDCONV_PY_METADATA",
    "fat_mvm_gridconv_py_cargs",
    "fat_mvm_gridconv_py_outputs",
    "fat_mvm_gridconv_py_params",
    "fat_mvm_gridconv_py_execute",
    "fat_mvm_gridconv_py_validate",
    "fat_mvm_gridconv_py",
]