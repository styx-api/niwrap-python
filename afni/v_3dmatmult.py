# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DMATMULT_METADATA = Metadata(
    id="3dmatmult",
    name="3dmatmult",
    package="afni",
)

V3dmatmult = typing.TypedDict(
    "V3dmatmult",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dmatmult"]],
        "inputA": InputPathType,
        "inputB": InputPathType,
        "prefix": str,
        "datum": typing.NotRequired[str],
        "verb": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dmatmultOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output dataset from the matrix multiplication"""

def v_3dmatmult_params(
    inputA: InputPathType,
    inputB: InputPathType,
    prefix: str,
    datum: str | None = None,
    verb: float | None = None,
) -> V3dmatmult:
    """
    Build parameters.

    Args:
        inputA: Specify first (matrix) dataset
        inputB: Specify second (matrix) dataset
        prefix: Specify output dataset prefix
        datum: Specify output data type ('byte', 'short', 'float')
        verb: Specify verbosity level

    Returns:
        Parameter dictionary.
    """
    params: V3dmatmult = {
        "@type": "afni/3dmatmult",
        "inputA": inputA,
        "inputB": inputB,
        "prefix": prefix,
    }
    if datum is not None:
        params["datum"] = datum
    if verb is not None:
        params["verb"] = verb
    return params

def v_3dmatmult_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dmatmult."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputA", None) is None:
        raise StyxValidationError("`inputA` must not be None")
    if not isinstance(params["inputA"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputA` has the wrong type: Received `{type(params["inputA"])}` expected `InputPathType`')
    if params.get("inputB", None) is None:
        raise StyxValidationError("`inputB` must not be None")
    if not isinstance(params["inputB"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inputB` has the wrong type: Received `{type(params["inputB"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("datum", None) is not None:
        if not isinstance(params["datum"], str):
            raise StyxValidationError(f'`datum` has the wrong type: Received `{type(params["datum"])}` expected `str`')
    if params.get("verb", None) is not None:
        if not isinstance(params["verb"], (float, int)):
            raise StyxValidationError(f'`verb` has the wrong type: Received `{type(params["verb"])}` expected `float`')

def v_3dmatmult_cargs(params: V3dmatmult, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dmatmult")
    cargs.append("-inputA")
    cargs.append(execution.input_file(params["inputA"]))
    cargs.append("-inputB")
    cargs.append(execution.input_file(params["inputB"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("datum")
    if v_0 is not None:
        cargs.append("-datum")
        cargs.append(v_0)
    v_1 = params.get("verb")
    if v_1 is not None:
        cargs.append("-verb")
        cargs.append(str(v_1))
    return cargs

def v_3dmatmult_outputs(params: V3dmatmult, execution: Execution) -> V3dmatmultOutputs:
    """Build the V3dmatmultOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["prefix"])
    return V3dmatmultOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3dmatmult_execute(params: V3dmatmult, runner: Runner | None = None) -> V3dmatmultOutputs:
    """
    3dmatmult

    Multiply AFNI datasets slice-by-slice as matrices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dmatmult_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DMATMULT_METADATA)
    execution.params(params)
    args = v_3dmatmult_cargs(params, execution)
    out = v_3dmatmult_outputs(params, execution)
    execution.run(args)
    return out

def v_3dmatmult(
    inputA: InputPathType,
    inputB: InputPathType,
    prefix: str,
    datum: str | None = None,
    verb: float | None = None,
    runner: Runner | None = None,
) -> V3dmatmultOutputs:
    """
    3dmatmult

    Multiply AFNI datasets slice-by-slice as matrices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inputA: Specify first (matrix) dataset
        inputB: Specify second (matrix) dataset
        prefix: Specify output dataset prefix
        datum: Specify output data type ('byte', 'short', 'float')
        verb: Specify verbosity level
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dmatmult_params(
        inputA=inputA,
        inputB=inputB,
        prefix=prefix,
        datum=datum,
        verb=verb,
    )
    return v_3dmatmult_execute(params, runner)

__all__ = [
    "V3dmatmult",
    "V3dmatmultOutputs",
    "V_3DMATMULT_METADATA",
    "v_3dmatmult_cargs",
    "v_3dmatmult_outputs",
    "v_3dmatmult_params",
    "v_3dmatmult_execute",
    "v_3dmatmult_validate",
    "v_3dmatmult",
]