# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HELP_AFNI_METADATA = Metadata(
    id="@help.AFNI",
    name="@help.AFNI",
    package="afni",
)

HelpAfni = typing.TypedDict(
    "HelpAfni",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@help.AFNI"]],
        "match": typing.NotRequired[str],
        "lynx": typing.NotRequired[bool],
        "vi": typing.NotRequired[bool],
        "less": typing.NotRequired[bool],
        "nedit": typing.NotRequired[bool],
        "noview": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class HelpAfniOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def help_afni_params(
    match: str | None = None,
    lynx: bool = False,
    vi: bool = False,
    less: bool = False,
    nedit: bool = False,
    noview: bool = False,
) -> HelpAfni:
    """
    Build parameters.

    Args:
        match: Looks for occurrence of each word in the list in the help file. For\
            a match with multiple words, all the words must be on the same line of\
            text in the help file.
        lynx: Set viewer to lynx
        vi: Set viewer to vi
        less: Set viewer to less (default)
        nedit: Set viewer to nedit
        noview: Set viewer to no view

    Returns:
        Parameter dictionary.
    """
    params: HelpAfni = {
        "@type": "afni/@help.AFNI",
        "lynx": lynx,
        "vi": vi,
        "less": less,
        "nedit": nedit,
        "noview": noview,
    }
    if match is not None:
        params["match"] = match
    return params

def help_afni_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HelpAfni."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("match", None) is not None:
        if not isinstance(params["match"], str):
            raise StyxValidationError(f'`match` has the wrong type: Received `{type(params["match"])}` expected `str`')
    if params.get("lynx", None) is not None:
        if not isinstance(params["lynx"], bool):
            raise StyxValidationError(f'`lynx` has the wrong type: Received `{type(params["lynx"])}` expected `bool`')
    if params.get("vi", None) is not None:
        if not isinstance(params["vi"], bool):
            raise StyxValidationError(f'`vi` has the wrong type: Received `{type(params["vi"])}` expected `bool`')
    if params.get("less", None) is not None:
        if not isinstance(params["less"], bool):
            raise StyxValidationError(f'`less` has the wrong type: Received `{type(params["less"])}` expected `bool`')
    if params.get("nedit", None) is not None:
        if not isinstance(params["nedit"], bool):
            raise StyxValidationError(f'`nedit` has the wrong type: Received `{type(params["nedit"])}` expected `bool`')
    if params.get("noview", None) is not None:
        if not isinstance(params["noview"], bool):
            raise StyxValidationError(f'`noview` has the wrong type: Received `{type(params["noview"])}` expected `bool`')

def help_afni_cargs(params: HelpAfni, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@help.AFNI")
    v_0 = params.get("match")
    if v_0 is not None:
        cargs.append("-match")
        cargs.append(v_0)
    if params.get("lynx"):
        cargs.append("-lynx")
    if params.get("vi"):
        cargs.append("-vi")
    if params.get("less"):
        cargs.append("-less")
    if params.get("nedit"):
        cargs.append("-nedit")
    if params.get("noview"):
        cargs.append("-noview")
    return cargs

def help_afni_outputs(params: HelpAfni, execution: Execution) -> HelpAfniOutputs:
    """Build the HelpAfniOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return HelpAfniOutputs(
        root=root_v,
    )

def help_afni_execute(params: HelpAfni, runner: Runner | None = None) -> HelpAfniOutputs:
    """
    @help.AFNI

    A script to retrieve and search AFNI's help page for all programs

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    help_afni_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HELP_AFNI_METADATA)
    execution.params(params)
    args = help_afni_cargs(params, execution)
    out = help_afni_outputs(params, execution)
    execution.run(args)
    return out

def help_afni(
    match: str | None = None,
    lynx: bool = False,
    vi: bool = False,
    less: bool = False,
    nedit: bool = False,
    noview: bool = False,
    runner: Runner | None = None,
) -> HelpAfniOutputs:
    """
    @help.AFNI

    A script to retrieve and search AFNI's help page for all programs

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        match: Looks for occurrence of each word in the list in the help file. For\
            a match with multiple words, all the words must be on the same line of\
            text in the help file.
        lynx: Set viewer to lynx
        vi: Set viewer to vi
        less: Set viewer to less (default)
        nedit: Set viewer to nedit
        noview: Set viewer to no view
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = help_afni_params(
        match=match,
        lynx=lynx,
        vi=vi,
        less=less,
        nedit=nedit,
        noview=noview,
    )
    return help_afni_execute(params, runner)

__all__ = [
    "HelpAfni",
    "HelpAfniOutputs",
    "HELP_AFNI_METADATA",
    "help_afni_cargs",
    "help_afni_outputs",
    "help_afni_params",
    "help_afni_execute",
    "help_afni_validate",
    "help_afni",
]