# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DSETSTAT2P_METADATA = Metadata(
    id="dsetstat2p",
    name="dsetstat2p",
    package="afni",
)

Dsetstat2p = typing.TypedDict(
    "Dsetstat2p",
    {
        "@type": typing.NotRequired[typing.Literal["afni/dsetstat2p"]],
        "dataset": str,
        "statval": float,
        "bisided": typing.NotRequired[bool],
        "two_sided": typing.NotRequired[bool],
        "one_sided": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Dsetstat2pOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output p-value or stat value"""

def dsetstat2p_params(
    dataset: str,
    statval: float,
    bisided: bool = False,
    two_sided: bool = False,
    one_sided: bool = False,
    quiet: bool = False,
) -> Dsetstat2p:
    """
    Build parameters.

    Args:
        dataset: Specify a dataset DDD and, if it has multiple sub-bricks, the\
            [i]th subbrick with the statistic of interest MUST be selected\
            explicitly; note the use of quotation marks around the brick selector\
            (because of the square-brackets). Note that 'i' can be either a number\
            of a string label selector.
        statval: Input stat-value S, which MUST be in the interval [0, infinity).
        bisided: Choose one-sided or bi-sided/two-sided testing
        two_sided: Choose one-sided or bi-sided/two-sided testing
        one_sided: Choose one-sided or bi-sided/two-sided testing
        quiet: An optional flag so that output ONLY the final statistic value\
            output to standard output; this can be then be viewed, redirected to a\
            text file or saved as a shell variable. (Default: display supplementary\
            text.)

    Returns:
        Parameter dictionary.
    """
    params: Dsetstat2p = {
        "@type": "afni/dsetstat2p",
        "dataset": dataset,
        "statval": statval,
        "bisided": bisided,
        "two_sided": two_sided,
        "one_sided": one_sided,
        "quiet": quiet,
    }
    return params

def dsetstat2p_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dsetstat2p."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], str):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `str`')
    if params.get("statval", None) is None:
        raise StyxValidationError("`statval` must not be None")
    if not isinstance(params["statval"], (float, int)):
        raise StyxValidationError(f'`statval` has the wrong type: Received `{type(params["statval"])}` expected `float`')
    if params["statval"] < 0:
        raise StyxValidationError("Parameter `statval` must be at least 0")
    if params.get("bisided", None) is not None:
        if not isinstance(params["bisided"], bool):
            raise StyxValidationError(f'`bisided` has the wrong type: Received `{type(params["bisided"])}` expected `bool`')
    if params.get("two_sided", None) is not None:
        if not isinstance(params["two_sided"], bool):
            raise StyxValidationError(f'`two_sided` has the wrong type: Received `{type(params["two_sided"])}` expected `bool`')
    if params.get("one_sided", None) is not None:
        if not isinstance(params["one_sided"], bool):
            raise StyxValidationError(f'`one_sided` has the wrong type: Received `{type(params["one_sided"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def dsetstat2p_cargs(params: Dsetstat2p, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dsetstat2p")
    cargs.append(params["dataset"])
    cargs.append(str(params["statval"]))
    if params.get("bisided"):
        cargs.append("-bisided")
    if params.get("two_sided"):
        cargs.append("-2sided")
    if params.get("one_sided"):
        cargs.append("-1sided")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def dsetstat2p_outputs(params: Dsetstat2p, execution: Execution) -> Dsetstat2pOutputs:
    """Build the Dsetstat2pOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("output.txt")
    return Dsetstat2pOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def dsetstat2p_execute(params: Dsetstat2p, runner: Runner | None = None) -> Dsetstat2pOutputs:
    """
    dsetstat2p

    Converts a statistic to a p-value with reference to a particular dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dsetstat2p_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DSETSTAT2P_METADATA)
    execution.params(params)
    args = dsetstat2p_cargs(params, execution)
    out = dsetstat2p_outputs(params, execution)
    execution.run(args)
    return out

def dsetstat2p(
    dataset: str,
    statval: float,
    bisided: bool = False,
    two_sided: bool = False,
    one_sided: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> Dsetstat2pOutputs:
    """
    dsetstat2p

    Converts a statistic to a p-value with reference to a particular dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Specify a dataset DDD and, if it has multiple sub-bricks, the\
            [i]th subbrick with the statistic of interest MUST be selected\
            explicitly; note the use of quotation marks around the brick selector\
            (because of the square-brackets). Note that 'i' can be either a number\
            of a string label selector.
        statval: Input stat-value S, which MUST be in the interval [0, infinity).
        bisided: Choose one-sided or bi-sided/two-sided testing
        two_sided: Choose one-sided or bi-sided/two-sided testing
        one_sided: Choose one-sided or bi-sided/two-sided testing
        quiet: An optional flag so that output ONLY the final statistic value\
            output to standard output; this can be then be viewed, redirected to a\
            text file or saved as a shell variable. (Default: display supplementary\
            text.)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dsetstat2p_params(
        dataset=dataset,
        statval=statval,
        bisided=bisided,
        two_sided=two_sided,
        one_sided=one_sided,
        quiet=quiet,
    )
    return dsetstat2p_execute(params, runner)

__all__ = [
    "Dsetstat2p",
    "Dsetstat2pOutputs",
    "DSETSTAT2P_METADATA",
    "dsetstat2p_cargs",
    "dsetstat2p_outputs",
    "dsetstat2p_params",
    "dsetstat2p_execute",
    "dsetstat2p_validate",
    "dsetstat2p",
]