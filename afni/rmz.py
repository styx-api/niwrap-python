# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RMZ_METADATA = Metadata(
    id="rmz",
    name="rmz",
    package="afni",
)

Rmz = typing.TypedDict(
    "Rmz",
    {
        "@type": typing.NotRequired[typing.Literal["afni/rmz"]],
        "quiet": typing.NotRequired[bool],
        "hash_flag": typing.NotRequired[float],
        "keep_flag": typing.NotRequired[bool],
        "filenames": list[InputPathType],
    },
)

@dataclasses.dataclass
class RmzOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def rmz_params(
    filenames: list[InputPathType],
    quiet: bool = False,
    hash_flag: float | None = None,
    keep_flag: bool = False,
) -> Rmz:
    """
    Build parameters.

    Args:
        filenames: Files to zero out and remove
        quiet: Quiet mode
        hash_flag: Number of times to zero out the files
        keep_flag: Keep the files instead of removing them

    Returns:
        Parameter dictionary.
    """
    params: Rmz = {
        "@type": "afni/rmz",
        "filenames": filenames,
        "quiet": quiet,
        "keep_flag": keep_flag,
    }
    if hash_flag is not None:
        params["hash_flag"] = hash_flag
    return params

def rmz_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Rmz."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("hash_flag", None) is not None:
        if not isinstance(params["hash_flag"], (float, int)):
            raise StyxValidationError(f'`hash_flag` has the wrong type: Received `{type(params["hash_flag"])}` expected `float`')
    if params.get("keep_flag", None) is not None:
        if not isinstance(params["keep_flag"], bool):
            raise StyxValidationError(f'`keep_flag` has the wrong type: Received `{type(params["keep_flag"])}` expected `bool`')
    if params.get("filenames", None) is None:
        raise StyxValidationError("`filenames` must not be None")
    if not isinstance(params["filenames"], list):
        raise StyxValidationError(f'`filenames` has the wrong type: Received `{type(params["filenames"])}` expected `list[InputPathType]`')
    for e in params["filenames"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`filenames` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def rmz_cargs(params: Rmz, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rmz")
    if params.get("quiet"):
        cargs.append("-q")
    v_0 = params.get("hash_flag")
    if v_0 is not None:
        cargs.append("-#")
        cargs.append(str(v_0))
    if params.get("keep_flag"):
        cargs.append("-k")
    for item0 in params["filenames"]:
        cargs.append(execution.input_file(item0))
    return cargs

def rmz_outputs(params: Rmz, execution: Execution) -> RmzOutputs:
    """Build the RmzOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RmzOutputs(
        root=root_v,
    )

def rmz_execute(params: Rmz, runner: Runner | None = None) -> RmzOutputs:
    """
    rmz

    Zeros out files before removing them

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rmz_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RMZ_METADATA)
    execution.params(params)
    args = rmz_cargs(params, execution)
    out = rmz_outputs(params, execution)
    execution.run(args)
    return out

def rmz(
    filenames: list[InputPathType],
    quiet: bool = False,
    hash_flag: float | None = None,
    keep_flag: bool = False,
    runner: Runner | None = None,
) -> RmzOutputs:
    """
    rmz

    Zeros out files before removing them

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        filenames: Files to zero out and remove
        quiet: Quiet mode
        hash_flag: Number of times to zero out the files
        keep_flag: Keep the files instead of removing them
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rmz_params(
        filenames=filenames,
        quiet=quiet,
        hash_flag=hash_flag,
        keep_flag=keep_flag,
    )
    return rmz_execute(params, runner)

__all__ = [
    "Rmz",
    "RmzOutputs",
    "RMZ_METADATA",
    "rmz_cargs",
    "rmz_outputs",
    "rmz_params",
    "rmz_execute",
    "rmz_validate",
    "rmz",
]