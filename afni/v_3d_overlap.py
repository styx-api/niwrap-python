# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_OVERLAP_METADATA = Metadata(
    id="3dOverlap",
    name="3dOverlap",
    package="afni",
)

V3dOverlap = typing.TypedDict(
    "V3dOverlap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dOverlap"]],
        "dataset1": InputPathType,
        "dataset2": list[InputPathType],
        "save_prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dOverlapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_brik: OutputPathType | None
    """BRIK file with count of overlaps at each voxel (if -save is used)"""
    output_head: OutputPathType | None
    """HEAD file with count of overlaps at each voxel (if -save is used)"""

def v_3d_overlap_params(
    dataset1: InputPathType,
    dataset2: list[InputPathType],
    save_prefix: str | None = None,
) -> V3dOverlap:
    """
    Build parameters.

    Args:
        dataset1: First input dataset (e.g. dset1+orig)
        dataset2: Second input dataset (e.g. dset2+orig)
        save_prefix: Save the count of overlaps at each voxel into a dataset with\
            the given prefix

    Returns:
        Parameter dictionary.
    """
    params: V3dOverlap = {
        "@type": "afni/3dOverlap",
        "dataset1": dataset1,
        "dataset2": dataset2,
    }
    if save_prefix is not None:
        params["save_prefix"] = save_prefix
    return params

def v_3d_overlap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dOverlap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset1", None) is None:
        raise StyxValidationError("`dataset1` must not be None")
    if not isinstance(params["dataset1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset1` has the wrong type: Received `{type(params["dataset1"])}` expected `InputPathType`')
    if params.get("dataset2", None) is None:
        raise StyxValidationError("`dataset2` must not be None")
    if not isinstance(params["dataset2"], list):
        raise StyxValidationError(f'`dataset2` has the wrong type: Received `{type(params["dataset2"])}` expected `list[InputPathType]`')
    for e in params["dataset2"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`dataset2` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("save_prefix", None) is not None:
        if not isinstance(params["save_prefix"], str):
            raise StyxValidationError(f'`save_prefix` has the wrong type: Received `{type(params["save_prefix"])}` expected `str`')

def v_3d_overlap_cargs(params: V3dOverlap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dOverlap")
    cargs.append(execution.input_file(params["dataset1"]))
    for item0 in params["dataset2"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("save_prefix")
    if v_0 is not None:
        cargs.append("-save")
        cargs.append(v_0)
    return cargs

def v_3d_overlap_outputs(params: V3dOverlap, execution: Execution) -> V3dOverlapOutputs:
    """Build the V3dOverlapOutputs object for this tool."""
    output_brik_v: OutputPathType | None = None
    output_head_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("save_prefix")
    if __v0 is not None:
        output_brik_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.BRIK')
    __v1 = params.get("save_prefix")
    if __v1 is not None:
        output_head_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.HEAD')
    return V3dOverlapOutputs(
        root=root_v,
        output_brik=output_brik_v,
        output_head=output_head_v,
    )

def v_3d_overlap_execute(params: V3dOverlap, runner: Runner | None = None) -> V3dOverlapOutputs:
    """
    3dOverlap

    Counts the number of voxels that are nonzero in all input datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_overlap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_OVERLAP_METADATA)
    execution.params(params)
    args = v_3d_overlap_cargs(params, execution)
    out = v_3d_overlap_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_overlap(
    dataset1: InputPathType,
    dataset2: list[InputPathType],
    save_prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dOverlapOutputs:
    """
    3dOverlap

    Counts the number of voxels that are nonzero in all input datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset1: First input dataset (e.g. dset1+orig)
        dataset2: Second input dataset (e.g. dset2+orig)
        save_prefix: Save the count of overlaps at each voxel into a dataset with\
            the given prefix
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_overlap_params(
        dataset1=dataset1,
        dataset2=dataset2,
        save_prefix=save_prefix,
    )
    return v_3d_overlap_execute(params, runner)

__all__ = [
    "V3dOverlap",
    "V3dOverlapOutputs",
    "V_3D_OVERLAP_METADATA",
    "v_3d_overlap_cargs",
    "v_3d_overlap_outputs",
    "v_3d_overlap_params",
    "v_3d_overlap_execute",
    "v_3d_overlap_validate",
    "v_3d_overlap",
]