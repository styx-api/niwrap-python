# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_DESPIKE_METADATA = Metadata(
    id="3dDespike",
    name="3dDespike",
    package="afni",
)

V3dDespike = typing.TypedDict(
    "V3dDespike",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dDespike"]],
        "prefix": typing.NotRequired[str],
        "in_file": InputPathType,
    },
)

@dataclasses.dataclass
class V3dDespikeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Output file."""

def v_3d_despike_params(
    in_file: InputPathType,
    prefix: str | None = None,
) -> V3dDespike:
    """
    Build parameters.

    Args:
        in_file: Input file to 3ddespike.
        prefix: Prefix for output file.

    Returns:
        Parameter dictionary.
    """
    params: V3dDespike = {
        "@type": "afni/3dDespike",
        "in_file": in_file,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_despike_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dDespike."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("in_file", None) is None:
        raise StyxValidationError("`in_file` must not be None")
    if not isinstance(params["in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_file` has the wrong type: Received `{type(params["in_file"])}` expected `InputPathType`')

def v_3d_despike_cargs(params: V3dDespike, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dDespike")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    cargs.append(execution.input_file(params["in_file"]))
    return cargs

def v_3d_despike_outputs(params: V3dDespike, execution: Execution) -> V3dDespikeOutputs:
    """Build the V3dDespikeOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return V3dDespikeOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def v_3d_despike_execute(params: V3dDespike, runner: Runner | None = None) -> V3dDespikeOutputs:
    """
    3dDespike

    Removes 'spikes' from the 3D+time input dataset and writes a new dataset with the spike values replaced by something more pleasing to the eye.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_despike_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_DESPIKE_METADATA)
    execution.params(params)
    args = v_3d_despike_cargs(params, execution)
    out = v_3d_despike_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_despike(
    in_file: InputPathType,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dDespikeOutputs:
    """
    3dDespike

    Removes 'spikes' from the 3D+time input dataset and writes a new dataset with the spike values replaced by something more pleasing to the eye.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_file: Input file to 3ddespike.
        prefix: Prefix for output file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_despike_params(
        in_file=in_file,
        prefix=prefix,
    )
    return v_3d_despike_execute(params, runner)

__all__ = [
    "V3dDespike",
    "V3dDespikeOutputs",
    "V_3D_DESPIKE_METADATA",
    "v_3d_despike_cargs",
    "v_3d_despike_outputs",
    "v_3d_despike_params",
    "v_3d_despike_execute",
    "v_3d_despike_validate",
    "v_3d_despike",
]