# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NIML_FEEDME_METADATA = Metadata(
    id="niml_feedme",
    name="niml_feedme",
    package="afni",
)

NimlFeedme = typing.TypedDict(
    "NimlFeedme",
    {
        "@type": typing.NotRequired[typing.Literal["afni/niml_feedme"]],
        "host": typing.NotRequired[str],
        "interval": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
        "accum": typing.NotRequired[bool],
        "target_dataset": typing.NotRequired[str],
        "drive_cmds": typing.NotRequired[list[str]],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class NimlFeedmeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def niml_feedme_params(
    dataset: InputPathType,
    host: str | None = None,
    interval: float | None = None,
    verbose: bool = False,
    accum: bool = False,
    target_dataset: str | None = None,
    drive_cmds: list[str] | None = None,
) -> NimlFeedme:
    """
    Build parameters.

    Args:
        dataset: Input dataset to be sent to AFNI.
        host: Send data, via TCP/IP, to AFNI running on the computer system\
            'sname'. By default, uses the current system (localhost), if you don't\
            use this option.
        interval: Tries to maintain an inter-transmit interval of 'ms'\
            milliseconds. The default is 1000 msec per volume.
        verbose: Be (very) talkative about actions.
        accum: Send sub-bricks so that they accumulate in AFNI. The default is to\
            create only a 1 volume dataset inside AFNI, and each sub-brick just\
            replaces that one volume when it is received.
        target_dataset: Change the dataset name transmitted to AFNI from\
            'niml_feedme' to 'nam'.
        drive_cmds: Send 'cmd' as a DRIVE_AFNI command. If cmd contains blanks, it\
            must be in 'quotes'. Multiple -drive options may be used. These\
            commands will be sent to AFNI just after the first volume is\
            transmitted. See file README.driver for a list of commands.

    Returns:
        Parameter dictionary.
    """
    params: NimlFeedme = {
        "@type": "afni/niml_feedme",
        "dataset": dataset,
        "verbose": verbose,
        "accum": accum,
    }
    if host is not None:
        params["host"] = host
    if interval is not None:
        params["interval"] = interval
    if target_dataset is not None:
        params["target_dataset"] = target_dataset
    if drive_cmds is not None:
        params["drive_cmds"] = drive_cmds
    return params

def niml_feedme_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NimlFeedme."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("host", None) is not None:
        if not isinstance(params["host"], str):
            raise StyxValidationError(f'`host` has the wrong type: Received `{type(params["host"])}` expected `str`')
    if params.get("interval", None) is not None:
        if not isinstance(params["interval"], (float, int)):
            raise StyxValidationError(f'`interval` has the wrong type: Received `{type(params["interval"])}` expected `float`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("accum", None) is not None:
        if not isinstance(params["accum"], bool):
            raise StyxValidationError(f'`accum` has the wrong type: Received `{type(params["accum"])}` expected `bool`')
    if params.get("target_dataset", None) is not None:
        if not isinstance(params["target_dataset"], str):
            raise StyxValidationError(f'`target_dataset` has the wrong type: Received `{type(params["target_dataset"])}` expected `str`')
    if params.get("drive_cmds", None) is not None:
        if not isinstance(params["drive_cmds"], list):
            raise StyxValidationError(f'`drive_cmds` has the wrong type: Received `{type(params["drive_cmds"])}` expected `list[str]`')
        for e in params["drive_cmds"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`drive_cmds` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def niml_feedme_cargs(params: NimlFeedme, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("niml_feedme")
    v_0 = params.get("host")
    if v_0 is not None:
        cargs.append("-host")
        cargs.append(v_0)
    v_1 = params.get("interval")
    if v_1 is not None:
        cargs.append("-dt")
        cargs.append(str(v_1))
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("accum"):
        cargs.append("-accum")
    v_2 = params.get("target_dataset")
    if v_2 is not None:
        cargs.append("-target")
        cargs.append(v_2)
    v_3 = params.get("drive_cmds")
    if v_3 is not None:
        cargs.append("-drive")
        for item0 in v_3:
            cargs.append(item0)
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def niml_feedme_outputs(params: NimlFeedme, execution: Execution) -> NimlFeedmeOutputs:
    """Build the NimlFeedmeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return NimlFeedmeOutputs(
        root=root_v,
    )

def niml_feedme_execute(params: NimlFeedme, runner: Runner | None = None) -> NimlFeedmeOutputs:
    """
    niml_feedme

    Sends volumes from the dataset to AFNI via the NIML socket interface.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    niml_feedme_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NIML_FEEDME_METADATA)
    execution.params(params)
    args = niml_feedme_cargs(params, execution)
    out = niml_feedme_outputs(params, execution)
    execution.run(args)
    return out

def niml_feedme(
    dataset: InputPathType,
    host: str | None = None,
    interval: float | None = None,
    verbose: bool = False,
    accum: bool = False,
    target_dataset: str | None = None,
    drive_cmds: list[str] | None = None,
    runner: Runner | None = None,
) -> NimlFeedmeOutputs:
    """
    niml_feedme

    Sends volumes from the dataset to AFNI via the NIML socket interface.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset to be sent to AFNI.
        host: Send data, via TCP/IP, to AFNI running on the computer system\
            'sname'. By default, uses the current system (localhost), if you don't\
            use this option.
        interval: Tries to maintain an inter-transmit interval of 'ms'\
            milliseconds. The default is 1000 msec per volume.
        verbose: Be (very) talkative about actions.
        accum: Send sub-bricks so that they accumulate in AFNI. The default is to\
            create only a 1 volume dataset inside AFNI, and each sub-brick just\
            replaces that one volume when it is received.
        target_dataset: Change the dataset name transmitted to AFNI from\
            'niml_feedme' to 'nam'.
        drive_cmds: Send 'cmd' as a DRIVE_AFNI command. If cmd contains blanks, it\
            must be in 'quotes'. Multiple -drive options may be used. These\
            commands will be sent to AFNI just after the first volume is\
            transmitted. See file README.driver for a list of commands.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = niml_feedme_params(
        dataset=dataset,
        host=host,
        interval=interval,
        verbose=verbose,
        accum=accum,
        target_dataset=target_dataset,
        drive_cmds=drive_cmds,
    )
    return niml_feedme_execute(params, runner)

__all__ = [
    "NimlFeedme",
    "NimlFeedmeOutputs",
    "NIML_FEEDME_METADATA",
    "niml_feedme_cargs",
    "niml_feedme_outputs",
    "niml_feedme_params",
    "niml_feedme_execute",
    "niml_feedme_validate",
    "niml_feedme",
]