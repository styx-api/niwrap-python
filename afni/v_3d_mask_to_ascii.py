# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_MASK_TO_ASCII_METADATA = Metadata(
    id="3dMaskToASCII",
    name="3dMaskToASCII",
    package="afni",
)

V3dMaskToAscii = typing.TypedDict(
    "V3dMaskToAscii",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dMaskToASCII"]],
        "tobin_flag": typing.NotRequired[bool],
        "dataset": InputPathType,
        "outputfile": str,
    },
)

@dataclasses.dataclass
class V3dMaskToAsciiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outputfile: OutputPathType
    """Output file where ASCII string mask or binary mask will be written."""

def v_3d_mask_to_ascii_params(
    dataset: InputPathType,
    outputfile: str,
    tobin_flag: bool = False,
) -> V3dMaskToAscii:
    """
    Build parameters.

    Args:
        dataset: Input dataset (e.g. mask.nii.gz)
        outputfile: Output file where ASCII string mask or binary mask will be\
            written.
        tobin_flag: Read ASCII mask, expand it to byte-valued dataset, and write to\
            stdout.

    Returns:
        Parameter dictionary.
    """
    params: V3dMaskToAscii = {
        "@type": "afni/3dMaskToASCII",
        "dataset": dataset,
        "outputfile": outputfile,
        "tobin_flag": tobin_flag,
    }
    return params

def v_3d_mask_to_ascii_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dMaskToAscii."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tobin_flag", None) is not None:
        if not isinstance(params["tobin_flag"], bool):
            raise StyxValidationError(f'`tobin_flag` has the wrong type: Received `{type(params["tobin_flag"])}` expected `bool`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')
    if params.get("outputfile", None) is None:
        raise StyxValidationError("`outputfile` must not be None")
    if not isinstance(params["outputfile"], str):
        raise StyxValidationError(f'`outputfile` has the wrong type: Received `{type(params["outputfile"])}` expected `str`')

def v_3d_mask_to_ascii_cargs(params: V3dMaskToAscii, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dMaskToASCII")
    if params.get("tobin_flag"):
        cargs.append("-tobin")
    cargs.append(execution.input_file(params["dataset"]))
    cargs.append("> ")
    cargs.append(params["outputfile"])
    return cargs

def v_3d_mask_to_ascii_outputs(params: V3dMaskToAscii, execution: Execution) -> V3dMaskToAsciiOutputs:
    """Build the V3dMaskToAsciiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outputfile_v: OutputPathType = execution.output_file(params["outputfile"])
    return V3dMaskToAsciiOutputs(
        root=root_v,
        outputfile=outputfile_v,
    )

def v_3d_mask_to_ascii_execute(params: V3dMaskToAscii, runner: Runner | None = None) -> V3dMaskToAsciiOutputs:
    """
    3dMaskToASCII

    Converts a byte-valued 0/1 dataset into an ASCII string, or vice versa.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_mask_to_ascii_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_MASK_TO_ASCII_METADATA)
    execution.params(params)
    args = v_3d_mask_to_ascii_cargs(params, execution)
    out = v_3d_mask_to_ascii_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_mask_to_ascii(
    dataset: InputPathType,
    outputfile: str,
    tobin_flag: bool = False,
    runner: Runner | None = None,
) -> V3dMaskToAsciiOutputs:
    """
    3dMaskToASCII

    Converts a byte-valued 0/1 dataset into an ASCII string, or vice versa.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset (e.g. mask.nii.gz)
        outputfile: Output file where ASCII string mask or binary mask will be\
            written.
        tobin_flag: Read ASCII mask, expand it to byte-valued dataset, and write to\
            stdout.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_mask_to_ascii_params(
        dataset=dataset,
        outputfile=outputfile,
        tobin_flag=tobin_flag,
    )
    return v_3d_mask_to_ascii_execute(params, runner)

__all__ = [
    "V3dMaskToAscii",
    "V3dMaskToAsciiOutputs",
    "V_3D_MASK_TO_ASCII_METADATA",
    "v_3d_mask_to_ascii_cargs",
    "v_3d_mask_to_ascii_outputs",
    "v_3d_mask_to_ascii_params",
    "v_3d_mask_to_ascii_execute",
    "v_3d_mask_to_ascii_validate",
    "v_3d_mask_to_ascii",
]