# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1D_FLAG_MOTION_METADATA = Metadata(
    id="1dFlagMotion",
    name="1dFlagMotion",
    package="afni",
)

V1dFlagMotion = typing.TypedDict(
    "V1dFlagMotion",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dFlagMotion"]],
        "input_motion_file": InputPathType,
        "max_translation": typing.NotRequired[float],
        "max_rotation": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V1dFlagMotionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_points: OutputPathType
    """List of points exceeding the motion bounds in 1D format"""

def v_1d_flag_motion_params(
    input_motion_file: InputPathType,
    max_translation: float | None = None,
    max_rotation: float | None = None,
) -> V1dFlagMotion:
    """
    Build parameters.

    Args:
        input_motion_file: Input file with EXACTLY 6 columns: roll pitch yaw\
            delta-SI delta-LR delta-AP (angles in degrees followed by translations\
            in mm)
        max_translation: Maximum translation allowed in any direction (defaults to\
            1.5mm)
        max_rotation: Maximum rotation allowed in any direction (defaults to 1.25\
            degrees)

    Returns:
        Parameter dictionary.
    """
    params: V1dFlagMotion = {
        "@type": "afni/1dFlagMotion",
        "input_motion_file": input_motion_file,
    }
    if max_translation is not None:
        params["max_translation"] = max_translation
    if max_rotation is not None:
        params["max_rotation"] = max_rotation
    return params

def v_1d_flag_motion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dFlagMotion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_motion_file", None) is None:
        raise StyxValidationError("`input_motion_file` must not be None")
    if not isinstance(params["input_motion_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_motion_file` has the wrong type: Received `{type(params["input_motion_file"])}` expected `InputPathType`')
    if params.get("max_translation", None) is not None:
        if not isinstance(params["max_translation"], (float, int)):
            raise StyxValidationError(f'`max_translation` has the wrong type: Received `{type(params["max_translation"])}` expected `float`')
    if params.get("max_rotation", None) is not None:
        if not isinstance(params["max_rotation"], (float, int)):
            raise StyxValidationError(f'`max_rotation` has the wrong type: Received `{type(params["max_rotation"])}` expected `float`')

def v_1d_flag_motion_cargs(params: V1dFlagMotion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dFlagMotion")
    cargs.append(execution.input_file(params["input_motion_file"]))
    v_0 = params.get("max_translation")
    if v_0 is not None:
        cargs.append("-MaxTrans")
        cargs.append(str(v_0))
    v_1 = params.get("max_rotation")
    if v_1 is not None:
        cargs.append("-MaxRot")
        cargs.append(str(v_1))
    return cargs

def v_1d_flag_motion_outputs(params: V1dFlagMotion, execution: Execution) -> V1dFlagMotionOutputs:
    """Build the V1dFlagMotionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_points_v: OutputPathType = execution.output_file("output_motion_points.1D")
    return V1dFlagMotionOutputs(
        root=root_v,
        output_points=output_points_v,
    )

def v_1d_flag_motion_execute(params: V1dFlagMotion, runner: Runner | None = None) -> V1dFlagMotionOutputs:
    """
    1dFlagMotion

    Produces a list of time points with excessive motion relative to the previous time point.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1d_flag_motion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1D_FLAG_MOTION_METADATA)
    execution.params(params)
    args = v_1d_flag_motion_cargs(params, execution)
    out = v_1d_flag_motion_outputs(params, execution)
    execution.run(args)
    return out

def v_1d_flag_motion(
    input_motion_file: InputPathType,
    max_translation: float | None = None,
    max_rotation: float | None = None,
    runner: Runner | None = None,
) -> V1dFlagMotionOutputs:
    """
    1dFlagMotion

    Produces a list of time points with excessive motion relative to the previous time point.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_motion_file: Input file with EXACTLY 6 columns: roll pitch yaw\
            delta-SI delta-LR delta-AP (angles in degrees followed by translations\
            in mm)
        max_translation: Maximum translation allowed in any direction (defaults to\
            1.5mm)
        max_rotation: Maximum rotation allowed in any direction (defaults to 1.25\
            degrees)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1d_flag_motion_params(
        input_motion_file=input_motion_file,
        max_translation=max_translation,
        max_rotation=max_rotation,
    )
    return v_1d_flag_motion_execute(params, runner)

__all__ = [
    "V1dFlagMotion",
    "V1dFlagMotionOutputs",
    "V_1D_FLAG_MOTION_METADATA",
    "v_1d_flag_motion_cargs",
    "v_1d_flag_motion_outputs",
    "v_1d_flag_motion_params",
    "v_1d_flag_motion_execute",
    "v_1d_flag_motion_validate",
    "v_1d_flag_motion",
]