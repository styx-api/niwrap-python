# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DIFF_FILES_METADATA = Metadata(
    id="@diff.files",
    name="@diff.files",
    package="afni",
)

DiffFiles = typing.TypedDict(
    "DiffFiles",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@diff.files"]],
        "files": list[str],
        "old_dir": str,
        "diff_opts": typing.NotRequired[str],
        "diff_prog": typing.NotRequired[str],
        "ignore_missing": typing.NotRequired[bool],
        "longlist": typing.NotRequired[bool],
        "save": typing.NotRequired[bool],
        "show": typing.NotRequired[bool],
        "xxdiff": typing.NotRequired[bool],
        "X_flag": typing.NotRequired[bool],
        "verbosity": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class DiffFilesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def diff_files_params(
    files: list[str],
    old_dir: str,
    diff_opts: str | None = None,
    diff_prog: str | None = None,
    ignore_missing: bool = False,
    longlist: bool = False,
    save: bool = False,
    show: bool = False,
    xxdiff: bool = False,
    X_flag: bool = False,
    verbosity: float | None = None,
) -> DiffFiles:
    """
    Build parameters.

    Args:
        files: List of files to compare
        old_dir: Directory containing the files to compare against
        diff_opts: Add options to diff command (e.g., -w)
        diff_prog: Display diffs using a specified program (e.g., meld, xxdiff)
        ignore_missing: Continue even if files are missing
        longlist: Run 'ls -l' on both directories instead of listing files
        save: Create PDFs of diffs
        show: Show diffs using 'diff'
        xxdiff: Show diffs using 'xxdiff'
        X_flag: Implies -xxdiff and -ignore_missing
        verbosity: Set verbosity level (2 or 3)

    Returns:
        Parameter dictionary.
    """
    params: DiffFiles = {
        "@type": "afni/@diff.files",
        "files": files,
        "old_dir": old_dir,
        "ignore_missing": ignore_missing,
        "longlist": longlist,
        "save": save,
        "show": show,
        "xxdiff": xxdiff,
        "X_flag": X_flag,
    }
    if diff_opts is not None:
        params["diff_opts"] = diff_opts
    if diff_prog is not None:
        params["diff_prog"] = diff_prog
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def diff_files_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DiffFiles."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("files", None) is None:
        raise StyxValidationError("`files` must not be None")
    if not isinstance(params["files"], list):
        raise StyxValidationError(f'`files` has the wrong type: Received `{type(params["files"])}` expected `list[str]`')
    for e in params["files"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`files` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("old_dir", None) is None:
        raise StyxValidationError("`old_dir` must not be None")
    if not isinstance(params["old_dir"], str):
        raise StyxValidationError(f'`old_dir` has the wrong type: Received `{type(params["old_dir"])}` expected `str`')
    if params.get("diff_opts", None) is not None:
        if not isinstance(params["diff_opts"], str):
            raise StyxValidationError(f'`diff_opts` has the wrong type: Received `{type(params["diff_opts"])}` expected `str`')
    if params.get("diff_prog", None) is not None:
        if not isinstance(params["diff_prog"], str):
            raise StyxValidationError(f'`diff_prog` has the wrong type: Received `{type(params["diff_prog"])}` expected `str`')
    if params.get("ignore_missing", None) is not None:
        if not isinstance(params["ignore_missing"], bool):
            raise StyxValidationError(f'`ignore_missing` has the wrong type: Received `{type(params["ignore_missing"])}` expected `bool`')
    if params.get("longlist", None) is not None:
        if not isinstance(params["longlist"], bool):
            raise StyxValidationError(f'`longlist` has the wrong type: Received `{type(params["longlist"])}` expected `bool`')
    if params.get("save", None) is not None:
        if not isinstance(params["save"], bool):
            raise StyxValidationError(f'`save` has the wrong type: Received `{type(params["save"])}` expected `bool`')
    if params.get("show", None) is not None:
        if not isinstance(params["show"], bool):
            raise StyxValidationError(f'`show` has the wrong type: Received `{type(params["show"])}` expected `bool`')
    if params.get("xxdiff", None) is not None:
        if not isinstance(params["xxdiff"], bool):
            raise StyxValidationError(f'`xxdiff` has the wrong type: Received `{type(params["xxdiff"])}` expected `bool`')
    if params.get("X_flag", None) is not None:
        if not isinstance(params["X_flag"], bool):
            raise StyxValidationError(f'`X_flag` has the wrong type: Received `{type(params["X_flag"])}` expected `bool`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], (float, int)):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `float`')
        if not (1 <= params["verbosity"] <= 3):
            raise StyxValidationError("Parameter `verbosity` must be between 1 and 3 (inclusive)")

def diff_files_cargs(params: DiffFiles, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@diff.files")
    for item0 in params["files"]:
        cargs.append(item0)
    cargs.append(params["old_dir"])
    v_0 = params.get("diff_opts")
    if v_0 is not None:
        cargs.append("-diff_opts")
        cargs.append(v_0)
    v_1 = params.get("diff_prog")
    if v_1 is not None:
        cargs.append("-diff_prog")
        cargs.append(v_1)
    if params.get("ignore_missing"):
        cargs.append("-ignore_missing")
    if params.get("longlist"):
        cargs.append("-longlist")
    if params.get("save"):
        cargs.append("-save")
    if params.get("show"):
        cargs.append("-show")
    if params.get("xxdiff"):
        cargs.append("-xxdiff")
    if params.get("X_flag"):
        cargs.append("-X")
    v_2 = params.get("verbosity")
    if v_2 is not None:
        cargs.append("-verb")
        cargs.append(str(v_2))
    return cargs

def diff_files_outputs(params: DiffFiles, execution: Execution) -> DiffFilesOutputs:
    """Build the DiffFilesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DiffFilesOutputs(
        root=root_v,
    )

def diff_files_execute(params: DiffFiles, runner: Runner | None = None) -> DiffFilesOutputs:
    """
    @diff.files

    Show file differences (between specified files and those in another directory)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    diff_files_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DIFF_FILES_METADATA)
    execution.params(params)
    args = diff_files_cargs(params, execution)
    out = diff_files_outputs(params, execution)
    execution.run(args)
    return out

def diff_files(
    files: list[str],
    old_dir: str,
    diff_opts: str | None = None,
    diff_prog: str | None = None,
    ignore_missing: bool = False,
    longlist: bool = False,
    save: bool = False,
    show: bool = False,
    xxdiff: bool = False,
    X_flag: bool = False,
    verbosity: float | None = None,
    runner: Runner | None = None,
) -> DiffFilesOutputs:
    """
    @diff.files

    Show file differences (between specified files and those in another directory)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        files: List of files to compare
        old_dir: Directory containing the files to compare against
        diff_opts: Add options to diff command (e.g., -w)
        diff_prog: Display diffs using a specified program (e.g., meld, xxdiff)
        ignore_missing: Continue even if files are missing
        longlist: Run 'ls -l' on both directories instead of listing files
        save: Create PDFs of diffs
        show: Show diffs using 'diff'
        xxdiff: Show diffs using 'xxdiff'
        X_flag: Implies -xxdiff and -ignore_missing
        verbosity: Set verbosity level (2 or 3)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = diff_files_params(
        files=files,
        old_dir=old_dir,
        diff_opts=diff_opts,
        diff_prog=diff_prog,
        ignore_missing=ignore_missing,
        longlist=longlist,
        save=save,
        show=show,
        xxdiff=xxdiff,
        X_flag=X_flag,
        verbosity=verbosity,
    )
    return diff_files_execute(params, runner)

__all__ = [
    "DiffFiles",
    "DiffFilesOutputs",
    "DIFF_FILES_METADATA",
    "diff_files_cargs",
    "diff_files_outputs",
    "diff_files_params",
    "diff_files_execute",
    "diff_files_validate",
    "diff_files",
]