# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DJUNCT_SSW_INTERMED_EDGE_IMGS_METADATA = Metadata(
    id="@djunct_ssw_intermed_edge_imgs",
    name="@djunct_ssw_intermed_edge_imgs",
    package="afni",
)

DjunctSswIntermedEdgeImgs = typing.TypedDict(
    "DjunctSswIntermedEdgeImgs",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@djunct_ssw_intermed_edge_imgs"]],
        "prefix": str,
        "ulay": InputPathType,
        "olay": InputPathType,
        "box_focus_slices": typing.NotRequired[str],
        "montgap": typing.NotRequired[str],
        "cbar": typing.NotRequired[str],
        "ulay_range": typing.NotRequired[str],
        "montx": typing.NotRequired[str],
        "monty": typing.NotRequired[str],
        "help_view": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "no_clean": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DjunctSswIntermedEdgeImgsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def djunct_ssw_intermed_edge_imgs_params(
    prefix: str,
    ulay: InputPathType,
    olay: InputPathType,
    box_focus_slices: str | None = None,
    montgap: str | None = None,
    cbar: str | None = None,
    ulay_range: str | None = None,
    montx: str | None = None,
    monty: str | None = None,
    help_view: bool = False,
    help: bool = False,
    version: bool = False,
    no_clean: bool = False,
) -> DjunctSswIntermedEdgeImgs:
    """
    Build parameters.

    Args:
        prefix: Prefix for generated output files
        ulay: Underlay dataset
        olay: Overlay dataset
        box_focus_slices: Slices of interest for focus box
        montgap: Gap between montage slices
        cbar: Color bar specification for AFNI
        ulay_range: Range for underlay data mapping
        montx: Number of slices along x dimension in montage
        monty: Number of slices along y dimension in montage
        help_view: View help file in viewer
        help: Displays help information
        version: Displays version information
        no_clean: Don't clean up intermediate files

    Returns:
        Parameter dictionary.
    """
    params: DjunctSswIntermedEdgeImgs = {
        "@type": "afni/@djunct_ssw_intermed_edge_imgs",
        "prefix": prefix,
        "ulay": ulay,
        "olay": olay,
        "help_view": help_view,
        "help": help,
        "version": version,
        "no_clean": no_clean,
    }
    if box_focus_slices is not None:
        params["box_focus_slices"] = box_focus_slices
    if montgap is not None:
        params["montgap"] = montgap
    if cbar is not None:
        params["cbar"] = cbar
    if ulay_range is not None:
        params["ulay_range"] = ulay_range
    if montx is not None:
        params["montx"] = montx
    if monty is not None:
        params["monty"] = monty
    return params

def djunct_ssw_intermed_edge_imgs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DjunctSswIntermedEdgeImgs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("ulay", None) is None:
        raise StyxValidationError("`ulay` must not be None")
    if not isinstance(params["ulay"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ulay` has the wrong type: Received `{type(params["ulay"])}` expected `InputPathType`')
    if params.get("olay", None) is None:
        raise StyxValidationError("`olay` must not be None")
    if not isinstance(params["olay"], (pathlib.Path, str)):
        raise StyxValidationError(f'`olay` has the wrong type: Received `{type(params["olay"])}` expected `InputPathType`')
    if params.get("box_focus_slices", None) is not None:
        if not isinstance(params["box_focus_slices"], str):
            raise StyxValidationError(f'`box_focus_slices` has the wrong type: Received `{type(params["box_focus_slices"])}` expected `str`')
    if params.get("montgap", None) is not None:
        if not isinstance(params["montgap"], str):
            raise StyxValidationError(f'`montgap` has the wrong type: Received `{type(params["montgap"])}` expected `str`')
    if params.get("cbar", None) is not None:
        if not isinstance(params["cbar"], str):
            raise StyxValidationError(f'`cbar` has the wrong type: Received `{type(params["cbar"])}` expected `str`')
    if params.get("ulay_range", None) is not None:
        if not isinstance(params["ulay_range"], str):
            raise StyxValidationError(f'`ulay_range` has the wrong type: Received `{type(params["ulay_range"])}` expected `str`')
    if params.get("montx", None) is not None:
        if not isinstance(params["montx"], str):
            raise StyxValidationError(f'`montx` has the wrong type: Received `{type(params["montx"])}` expected `str`')
    if params.get("monty", None) is not None:
        if not isinstance(params["monty"], str):
            raise StyxValidationError(f'`monty` has the wrong type: Received `{type(params["monty"])}` expected `str`')
    if params.get("help_view", None) is not None:
        if not isinstance(params["help_view"], bool):
            raise StyxValidationError(f'`help_view` has the wrong type: Received `{type(params["help_view"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')

def djunct_ssw_intermed_edge_imgs_cargs(params: DjunctSswIntermedEdgeImgs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@djunct_ssw_intermed_edge_imgs")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-ulay")
    cargs.append(execution.input_file(params["ulay"]))
    cargs.append("-olay")
    cargs.append(execution.input_file(params["olay"]))
    v_0 = params.get("box_focus_slices")
    if v_0 is not None:
        cargs.append("-box_focus_slices")
        cargs.append(v_0)
    v_1 = params.get("montgap")
    if v_1 is not None:
        cargs.append("-montgap")
        cargs.append(v_1)
    v_2 = params.get("cbar")
    if v_2 is not None:
        cargs.append("-cbar")
        cargs.append(v_2)
    v_3 = params.get("ulay_range")
    if v_3 is not None:
        cargs.append("-ulay_range")
        cargs.append(v_3)
    v_4 = params.get("montx")
    if v_4 is not None:
        cargs.append("-montx")
        cargs.append(v_4)
    v_5 = params.get("monty")
    if v_5 is not None:
        cargs.append("-monty")
        cargs.append(v_5)
    if params.get("help_view"):
        cargs.append("-hview")
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-ver")
    if params.get("no_clean"):
        cargs.append("-no_clean")
    return cargs

def djunct_ssw_intermed_edge_imgs_outputs(params: DjunctSswIntermedEdgeImgs, execution: Execution) -> DjunctSswIntermedEdgeImgsOutputs:
    """Build the DjunctSswIntermedEdgeImgsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DjunctSswIntermedEdgeImgsOutputs(
        root=root_v,
    )

def djunct_ssw_intermed_edge_imgs_execute(params: DjunctSswIntermedEdgeImgs, runner: Runner | None = None) -> DjunctSswIntermedEdgeImgsOutputs:
    """
    @djunct_ssw_intermed_edge_imgs

    Helper script to generate intermediate edge images for SSW-related processing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    djunct_ssw_intermed_edge_imgs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DJUNCT_SSW_INTERMED_EDGE_IMGS_METADATA)
    execution.params(params)
    args = djunct_ssw_intermed_edge_imgs_cargs(params, execution)
    out = djunct_ssw_intermed_edge_imgs_outputs(params, execution)
    execution.run(args)
    return out

def djunct_ssw_intermed_edge_imgs(
    prefix: str,
    ulay: InputPathType,
    olay: InputPathType,
    box_focus_slices: str | None = None,
    montgap: str | None = None,
    cbar: str | None = None,
    ulay_range: str | None = None,
    montx: str | None = None,
    monty: str | None = None,
    help_view: bool = False,
    help: bool = False,
    version: bool = False,
    no_clean: bool = False,
    runner: Runner | None = None,
) -> DjunctSswIntermedEdgeImgsOutputs:
    """
    @djunct_ssw_intermed_edge_imgs

    Helper script to generate intermediate edge images for SSW-related processing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix for generated output files
        ulay: Underlay dataset
        olay: Overlay dataset
        box_focus_slices: Slices of interest for focus box
        montgap: Gap between montage slices
        cbar: Color bar specification for AFNI
        ulay_range: Range for underlay data mapping
        montx: Number of slices along x dimension in montage
        monty: Number of slices along y dimension in montage
        help_view: View help file in viewer
        help: Displays help information
        version: Displays version information
        no_clean: Don't clean up intermediate files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = djunct_ssw_intermed_edge_imgs_params(
        prefix=prefix,
        ulay=ulay,
        olay=olay,
        box_focus_slices=box_focus_slices,
        montgap=montgap,
        cbar=cbar,
        ulay_range=ulay_range,
        montx=montx,
        monty=monty,
        help_view=help_view,
        help=help,
        version=version,
        no_clean=no_clean,
    )
    return djunct_ssw_intermed_edge_imgs_execute(params, runner)

__all__ = [
    "DjunctSswIntermedEdgeImgs",
    "DjunctSswIntermedEdgeImgsOutputs",
    "DJUNCT_SSW_INTERMED_EDGE_IMGS_METADATA",
    "djunct_ssw_intermed_edge_imgs_cargs",
    "djunct_ssw_intermed_edge_imgs_outputs",
    "djunct_ssw_intermed_edge_imgs_params",
    "djunct_ssw_intermed_edge_imgs_execute",
    "djunct_ssw_intermed_edge_imgs_validate",
    "djunct_ssw_intermed_edge_imgs",
]