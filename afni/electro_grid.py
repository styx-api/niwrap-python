# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ELECTRO_GRID_METADATA = Metadata(
    id="@ElectroGrid",
    name="@ElectroGrid",
    package="afni",
)

ElectroGrid = typing.TypedDict(
    "ElectroGrid",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ElectroGrid"]],
        "strip": typing.NotRequired[int],
        "grid": typing.NotRequired[list[int]],
        "prefix": typing.NotRequired[str],
        "coords": typing.NotRequired[InputPathType],
        "with_markers": typing.NotRequired[bool],
        "echo": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ElectroGridOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_surface: OutputPathType | None
    """Output surface file"""

def electro_grid_params(
    strip: int | None = None,
    grid: list[int] | None = None,
    prefix: str | None = None,
    coords: InputPathType | None = None,
    with_markers: bool = False,
    echo: bool = False,
) -> ElectroGrid:
    """
    Build parameters.

    Args:
        strip: Make an Nx strip (array) of electrodes.
        grid: Make an Nx by Ny grid of electrodes. A node at (i,j) has a node ID =\
            i+Nx*j with 0<=i<Nx and 0<=j<=Ny
        prefix: Use PREFIX for the output surface.
        coords: Specify the coordinates of the nodes on the grid, or the array.\
            XYZ.1D should have three columns, with each row specifying the\
            coordinates of one node.
        with_markers: Add markers to the surface at each electrode.
        echo: Set echo.

    Returns:
        Parameter dictionary.
    """
    params: ElectroGrid = {
        "@type": "afni/@ElectroGrid",
        "with_markers": with_markers,
        "echo": echo,
    }
    if strip is not None:
        params["strip"] = strip
    if grid is not None:
        params["grid"] = grid
    if prefix is not None:
        params["prefix"] = prefix
    if coords is not None:
        params["coords"] = coords
    return params

def electro_grid_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ElectroGrid."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("strip", None) is not None:
        if not isinstance(params["strip"], int):
            raise StyxValidationError(f'`strip` has the wrong type: Received `{type(params["strip"])}` expected `int`')
    if params.get("grid", None) is not None:
        if not isinstance(params["grid"], list):
            raise StyxValidationError(f'`grid` has the wrong type: Received `{type(params["grid"])}` expected `list[int]`')
        if not (2 <= len(params["grid"]) <= 2):
            raise StyxValidationError("Parameter `grid` must contain between 2 and 2 elements (inclusive)")
        for e in params["grid"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`grid` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("coords", None) is not None:
        if not isinstance(params["coords"], (pathlib.Path, str)):
            raise StyxValidationError(f'`coords` has the wrong type: Received `{type(params["coords"])}` expected `InputPathType`')
    if params.get("with_markers", None) is not None:
        if not isinstance(params["with_markers"], bool):
            raise StyxValidationError(f'`with_markers` has the wrong type: Received `{type(params["with_markers"])}` expected `bool`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')

def electro_grid_cargs(params: ElectroGrid, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ElectroGrid")
    v_0 = params.get("strip")
    if v_0 is not None:
        cargs.append("-strip")
        cargs.append(str(v_0))
    v_1 = params.get("grid")
    if v_1 is not None:
        cargs.append("-grid")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    v_3 = params.get("coords")
    if v_3 is not None:
        cargs.append("-coords")
        cargs.append(execution.input_file(v_3))
    if params.get("with_markers"):
        cargs.append("-with_markers")
    if params.get("echo"):
        cargs.append("-echo")
    return cargs

def electro_grid_outputs(params: ElectroGrid, execution: Execution) -> ElectroGridOutputs:
    """Build the ElectroGridOutputs object for this tool."""
    output_surface_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_surface_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.gii')
    return ElectroGridOutputs(
        root=root_v,
        output_surface=output_surface_v,
    )

def electro_grid_execute(params: ElectroGrid, runner: Runner | None = None) -> ElectroGridOutputs:
    """
    @ElectroGrid

    Creates a mesh representation of an electrode grid for use with SUMA

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    electro_grid_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ELECTRO_GRID_METADATA)
    execution.params(params)
    args = electro_grid_cargs(params, execution)
    out = electro_grid_outputs(params, execution)
    execution.run(args)
    return out

def electro_grid(
    strip: int | None = None,
    grid: list[int] | None = None,
    prefix: str | None = None,
    coords: InputPathType | None = None,
    with_markers: bool = False,
    echo: bool = False,
    runner: Runner | None = None,
) -> ElectroGridOutputs:
    """
    @ElectroGrid

    Creates a mesh representation of an electrode grid for use with SUMA

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        strip: Make an Nx strip (array) of electrodes.
        grid: Make an Nx by Ny grid of electrodes. A node at (i,j) has a node ID =\
            i+Nx*j with 0<=i<Nx and 0<=j<=Ny
        prefix: Use PREFIX for the output surface.
        coords: Specify the coordinates of the nodes on the grid, or the array.\
            XYZ.1D should have three columns, with each row specifying the\
            coordinates of one node.
        with_markers: Add markers to the surface at each electrode.
        echo: Set echo.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = electro_grid_params(
        strip=strip,
        grid=grid,
        prefix=prefix,
        coords=coords,
        with_markers=with_markers,
        echo=echo,
    )
    return electro_grid_execute(params, runner)

__all__ = [
    "ElectroGrid",
    "ElectroGridOutputs",
    "ELECTRO_GRID_METADATA",
    "electro_grid_cargs",
    "electro_grid_outputs",
    "electro_grid_params",
    "electro_grid_execute",
    "electro_grid_validate",
    "electro_grid",
]