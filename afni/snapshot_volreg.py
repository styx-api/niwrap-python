# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SNAPSHOT_VOLREG_METADATA = Metadata(
    id="@snapshot_volreg",
    name="@snapshot_volreg",
    package="afni",
)

SnapshotVolreg = typing.TypedDict(
    "SnapshotVolreg",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@snapshot_volreg"]],
        "anatdataset": InputPathType,
        "epidataset": InputPathType,
        "jname": typing.NotRequired[str],
        "xdisplay": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SnapshotVolregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_jpeg: OutputPathType | None
    """JPEG image showing the edges of the EPI dataset overlayed on the anatomical dataset"""

def snapshot_volreg_params(
    anatdataset: InputPathType,
    epidataset: InputPathType,
    jname: str | None = None,
    xdisplay: str | None = None,
) -> SnapshotVolreg:
    """
    Build parameters.

    Args:
        anatdataset: Anatomical dataset file
        epidataset: EPI dataset file
        jname: Name for the output JPEG file
        xdisplay: Display number of an already running Xvfb instance

    Returns:
        Parameter dictionary.
    """
    params: SnapshotVolreg = {
        "@type": "afni/@snapshot_volreg",
        "anatdataset": anatdataset,
        "epidataset": epidataset,
    }
    if jname is not None:
        params["jname"] = jname
    if xdisplay is not None:
        params["xdisplay"] = xdisplay
    return params

def snapshot_volreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SnapshotVolreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("anatdataset", None) is None:
        raise StyxValidationError("`anatdataset` must not be None")
    if not isinstance(params["anatdataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`anatdataset` has the wrong type: Received `{type(params["anatdataset"])}` expected `InputPathType`')
    if params.get("epidataset", None) is None:
        raise StyxValidationError("`epidataset` must not be None")
    if not isinstance(params["epidataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`epidataset` has the wrong type: Received `{type(params["epidataset"])}` expected `InputPathType`')
    if params.get("jname", None) is not None:
        if not isinstance(params["jname"], str):
            raise StyxValidationError(f'`jname` has the wrong type: Received `{type(params["jname"])}` expected `str`')
    if params.get("xdisplay", None) is not None:
        if not isinstance(params["xdisplay"], str):
            raise StyxValidationError(f'`xdisplay` has the wrong type: Received `{type(params["xdisplay"])}` expected `str`')

def snapshot_volreg_cargs(params: SnapshotVolreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@snapshot_volreg")
    cargs.append(execution.input_file(params["anatdataset"]))
    cargs.append(execution.input_file(params["epidataset"]))
    v_0 = params.get("jname")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("xdisplay")
    if v_1 is not None:
        cargs.append(v_1)
    return cargs

def snapshot_volreg_outputs(params: SnapshotVolreg, execution: Execution) -> SnapshotVolregOutputs:
    """Build the SnapshotVolregOutputs object for this tool."""
    output_jpeg_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("jname")
    if __v0 is not None:
        output_jpeg_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.jpg')
    return SnapshotVolregOutputs(
        root=root_v,
        output_jpeg=output_jpeg_v,
    )

def snapshot_volreg_execute(params: SnapshotVolreg, runner: Runner | None = None) -> SnapshotVolregOutputs:
    """
    @snapshot_volreg

    Create a JPEG image showing the edges of an EPI dataset overlayed on an anatomical dataset to judge 3D registration quality.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    snapshot_volreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SNAPSHOT_VOLREG_METADATA)
    execution.params(params)
    args = snapshot_volreg_cargs(params, execution)
    out = snapshot_volreg_outputs(params, execution)
    execution.run(args)
    return out

def snapshot_volreg(
    anatdataset: InputPathType,
    epidataset: InputPathType,
    jname: str | None = None,
    xdisplay: str | None = None,
    runner: Runner | None = None,
) -> SnapshotVolregOutputs:
    """
    @snapshot_volreg

    Create a JPEG image showing the edges of an EPI dataset overlayed on an anatomical dataset to judge 3D registration quality.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        anatdataset: Anatomical dataset file
        epidataset: EPI dataset file
        jname: Name for the output JPEG file
        xdisplay: Display number of an already running Xvfb instance
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = snapshot_volreg_params(
        anatdataset=anatdataset,
        epidataset=epidataset,
        jname=jname,
        xdisplay=xdisplay,
    )
    return snapshot_volreg_execute(params, runner)

__all__ = [
    "SnapshotVolreg",
    "SnapshotVolregOutputs",
    "SNAPSHOT_VOLREG_METADATA",
    "snapshot_volreg_cargs",
    "snapshot_volreg_outputs",
    "snapshot_volreg_params",
    "snapshot_volreg_execute",
    "snapshot_volreg_validate",
    "snapshot_volreg",
]