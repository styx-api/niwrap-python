# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAND_METADATA = Metadata(
    id="imand",
    name="imand",
    package="afni",
)

Imand = typing.TypedDict(
    "Imand",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imand"]],
        "threshold": typing.NotRequired[float],
        "input_images": list[InputPathType],
        "output_image": str,
    },
)

@dataclasses.dataclass
class ImandOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """The resulting output image file."""

def imand_params(
    input_images: list[InputPathType],
    output_image: str,
    threshold: float | None = None,
) -> Imand:
    """
    Build parameters.

    Args:
        input_images: Input images to be processed. Multiple input images can be\
            specified.
        output_image: Output image file.
        threshold: Threshold value; only pixels above this value will be output.\
            Optional.

    Returns:
        Parameter dictionary.
    """
    params: Imand = {
        "@type": "afni/imand",
        "input_images": input_images,
        "output_image": output_image,
    }
    if threshold is not None:
        params["threshold"] = threshold
    return params

def imand_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imand."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')

def imand_cargs(params: Imand, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imand")
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_0))
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_image"])
    return cargs

def imand_outputs(params: Imand, execution: Execution) -> ImandOutputs:
    """Build the ImandOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["output_image"])
    return ImandOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def imand_execute(params: Imand, runner: Runner | None = None) -> ImandOutputs:
    """
    imand

    Image AND operation tool. Only pixels nonzero in all input images (and above the threshold, if given) will be output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imand_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAND_METADATA)
    execution.params(params)
    args = imand_cargs(params, execution)
    out = imand_outputs(params, execution)
    execution.run(args)
    return out

def imand(
    input_images: list[InputPathType],
    output_image: str,
    threshold: float | None = None,
    runner: Runner | None = None,
) -> ImandOutputs:
    """
    imand

    Image AND operation tool. Only pixels nonzero in all input images (and above the threshold, if given) will be output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_images: Input images to be processed. Multiple input images can be\
            specified.
        output_image: Output image file.
        threshold: Threshold value; only pixels above this value will be output.\
            Optional.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imand_params(
        input_images=input_images,
        output_image=output_image,
        threshold=threshold,
    )
    return imand_execute(params, runner)

__all__ = [
    "Imand",
    "ImandOutputs",
    "IMAND_METADATA",
    "imand_cargs",
    "imand_outputs",
    "imand_params",
    "imand_execute",
    "imand_validate",
    "imand",
]