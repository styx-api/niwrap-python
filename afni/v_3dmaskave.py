# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DMASKAVE_METADATA = Metadata(
    id="3dmaskave",
    name="3dmaskave",
    package="afni",
)

V3dmaskaveOutputtype = typing.Literal["NIFTI"] | typing.Literal["AFNI"] | typing.Literal["NIFTI_GZ"]

V3dmaskave = typing.TypedDict(
    "V3dmaskave",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dmaskave"]],
        "in_file": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "num_threads": typing.NotRequired[int],
        "outputtype": typing.NotRequired[V3dmaskaveOutputtype],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dmaskaveOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType
    """Output image file name."""

def v_3dmaskave_params(
    in_file: InputPathType,
    mask: InputPathType | None = None,
    num_threads: int | None = None,
    outputtype: V3dmaskaveOutputtype | None = None,
    quiet: bool = False,
) -> V3dmaskave:
    """
    Build parameters.

    Args:
        in_file: Input file to 3dmaskave.
        mask: Matrix to align input file.
        num_threads: Set number of threads.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        quiet: Matrix to align input file.

    Returns:
        Parameter dictionary.
    """
    params: V3dmaskave = {
        "@type": "afni/3dmaskave",
        "in_file": in_file,
        "quiet": quiet,
    }
    if mask is not None:
        params["mask"] = mask
    if num_threads is not None:
        params["num_threads"] = num_threads
    if outputtype is not None:
        params["outputtype"] = outputtype
    return params

def v_3dmaskave_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dmaskave."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_file", None) is None:
        raise StyxValidationError("`in_file` must not be None")
    if not isinstance(params["in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_file` has the wrong type: Received `{type(params["in_file"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], int):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `int`')
    if params.get("outputtype", None) is not None:
        if not isinstance(params["outputtype"], str):
            raise StyxValidationError(f'`outputtype` has the wrong type: Received `{type(params["outputtype"])}` expected `V3dmaskaveOutputtype`')
        if params["outputtype"] not in ["NIFTI", "AFNI", "NIFTI_GZ"]:
            raise StyxValidationError("Parameter `outputtype` must be one of [\"NIFTI\", \"AFNI\", \"NIFTI_GZ\"]")
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_3dmaskave_cargs(params: V3dmaskave, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dmaskave")
    cargs.append(execution.input_file(params["in_file"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("num_threads")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("outputtype")
    if v_2 is not None:
        cargs.append(str(v_2))
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def v_3dmaskave_outputs(params: V3dmaskave, execution: Execution) -> V3dmaskaveOutputs:
    """Build the V3dmaskaveOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_file_v: OutputPathType = execution.output_file(f'{params["in_file"]}_maskave.1D')
    out_file_v = execution.output_file("out_file")
    return V3dmaskaveOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def v_3dmaskave_execute(params: V3dmaskave, runner: Runner | None = None) -> V3dmaskaveOutputs:
    """
    3dmaskave

    Computes average of all voxels in the input dataset which satisfy the criterion in the options list

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dmaskave_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DMASKAVE_METADATA)
    execution.params(params)
    args = v_3dmaskave_cargs(params, execution)
    out = v_3dmaskave_outputs(params, execution)
    execution.run(args)
    return out

def v_3dmaskave(
    in_file: InputPathType,
    mask: InputPathType | None = None,
    num_threads: int | None = None,
    outputtype: V3dmaskaveOutputtype | None = None,
    quiet: bool = False,
    runner: Runner | None = None,
) -> V3dmaskaveOutputs:
    """
    3dmaskave

    Computes average of all voxels in the input dataset which satisfy the criterion in the options list

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_file: Input file to 3dmaskave.
        mask: Matrix to align input file.
        num_threads: Set number of threads.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        quiet: Matrix to align input file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dmaskave_params(
        in_file=in_file,
        mask=mask,
        num_threads=num_threads,
        outputtype=outputtype,
        quiet=quiet,
    )
    return v_3dmaskave_execute(params, runner)

__all__ = [
    "V3dmaskave",
    "V3dmaskaveOutputs",
    "V_3DMASKAVE_METADATA",
    "v_3dmaskave_cargs",
    "v_3dmaskave_outputs",
    "v_3dmaskave_params",
    "v_3dmaskave_execute",
    "v_3dmaskave_validate",
    "v_3dmaskave",
]