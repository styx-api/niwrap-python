# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMROTATE_METADATA = Metadata(
    id="imrotate",
    name="imrotate",
    package="afni",
)

Imrotate = typing.TypedDict(
    "Imrotate",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imrotate"]],
        "linear_interpolation": typing.NotRequired[bool],
        "fourier_interpolation": typing.NotRequired[bool],
        "dx": float,
        "dy": float,
        "phi": float,
        "input_image": InputPathType,
        "output_image": str,
    },
)

@dataclasses.dataclass
class ImrotateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType
    """Path to the output image file"""

def imrotate_params(
    dx: float,
    dy: float,
    phi: float,
    input_image: InputPathType,
    output_image: str,
    linear_interpolation: bool = False,
    fourier_interpolation: bool = False,
) -> Imrotate:
    """
    Build parameters.

    Args:
        dx: Pixels to shift rightwards (can be non-integer)
        dy: Pixels to shift downwards (can be non-integer)
        phi: Degrees to rotate clockwise
        input_image: Input image file
        output_image: Output image file
        linear_interpolation: Use bilinear interpolation (default is bicubic)
        fourier_interpolation: Use Fourier interpolation

    Returns:
        Parameter dictionary.
    """
    params: Imrotate = {
        "@type": "afni/imrotate",
        "dx": dx,
        "dy": dy,
        "phi": phi,
        "input_image": input_image,
        "output_image": output_image,
        "linear_interpolation": linear_interpolation,
        "fourier_interpolation": fourier_interpolation,
    }
    return params

def imrotate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imrotate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("linear_interpolation", None) is not None:
        if not isinstance(params["linear_interpolation"], bool):
            raise StyxValidationError(f'`linear_interpolation` has the wrong type: Received `{type(params["linear_interpolation"])}` expected `bool`')
    if params.get("fourier_interpolation", None) is not None:
        if not isinstance(params["fourier_interpolation"], bool):
            raise StyxValidationError(f'`fourier_interpolation` has the wrong type: Received `{type(params["fourier_interpolation"])}` expected `bool`')
    if params.get("dx", None) is None:
        raise StyxValidationError("`dx` must not be None")
    if not isinstance(params["dx"], (float, int)):
        raise StyxValidationError(f'`dx` has the wrong type: Received `{type(params["dx"])}` expected `float`')
    if params.get("dy", None) is None:
        raise StyxValidationError("`dy` must not be None")
    if not isinstance(params["dy"], (float, int)):
        raise StyxValidationError(f'`dy` has the wrong type: Received `{type(params["dy"])}` expected `float`')
    if params.get("phi", None) is None:
        raise StyxValidationError("`phi` must not be None")
    if not isinstance(params["phi"], (float, int)):
        raise StyxValidationError(f'`phi` has the wrong type: Received `{type(params["phi"])}` expected `float`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')

def imrotate_cargs(params: Imrotate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imrotate")
    if params.get("linear_interpolation"):
        cargs.append("-linear")
    if params.get("fourier_interpolation"):
        cargs.append("-Fourier")
    cargs.append(str(params["dx"]))
    cargs.append(str(params["dy"]))
    cargs.append(str(params["phi"]))
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    return cargs

def imrotate_outputs(params: Imrotate, execution: Execution) -> ImrotateOutputs:
    """Build the ImrotateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_file_v: OutputPathType = execution.output_file(params["output_image"])
    return ImrotateOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
    )

def imrotate_execute(params: Imrotate, runner: Runner | None = None) -> ImrotateOutputs:
    """
    imrotate

    Shifts and rotates an image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imrotate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMROTATE_METADATA)
    execution.params(params)
    args = imrotate_cargs(params, execution)
    out = imrotate_outputs(params, execution)
    execution.run(args)
    return out

def imrotate(
    dx: float,
    dy: float,
    phi: float,
    input_image: InputPathType,
    output_image: str,
    linear_interpolation: bool = False,
    fourier_interpolation: bool = False,
    runner: Runner | None = None,
) -> ImrotateOutputs:
    """
    imrotate

    Shifts and rotates an image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dx: Pixels to shift rightwards (can be non-integer)
        dy: Pixels to shift downwards (can be non-integer)
        phi: Degrees to rotate clockwise
        input_image: Input image file
        output_image: Output image file
        linear_interpolation: Use bilinear interpolation (default is bicubic)
        fourier_interpolation: Use Fourier interpolation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imrotate_params(
        dx=dx,
        dy=dy,
        phi=phi,
        input_image=input_image,
        output_image=output_image,
        linear_interpolation=linear_interpolation,
        fourier_interpolation=fourier_interpolation,
    )
    return imrotate_execute(params, runner)

__all__ = [
    "Imrotate",
    "ImrotateOutputs",
    "IMROTATE_METADATA",
    "imrotate_cargs",
    "imrotate_outputs",
    "imrotate_params",
    "imrotate_execute",
    "imrotate_validate",
    "imrotate",
]