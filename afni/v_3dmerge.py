# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DMERGE_METADATA = Metadata(
    id="3dmerge",
    name="3dmerge",
    package="afni",
)

V3dmerge = typing.TypedDict(
    "V3dmerge",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dmerge"]],
        "input_files": list[InputPathType],
        "output_file": str,
        "blur_fwhm": typing.NotRequired[float],
        "threshold": typing.NotRequired[float],
        "clust": typing.NotRequired[list[float]],
        "dindex": typing.NotRequired[float],
        "tindex": typing.NotRequired[float],
        "absolute": typing.NotRequired[bool],
        "dxyz": typing.NotRequired[bool],
        "gmean": typing.NotRequired[bool],
        "gmax": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dmergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType
    """Output dataset file"""

def v_3dmerge_params(
    input_files: list[InputPathType],
    output_file: str,
    blur_fwhm: float | None = None,
    threshold: float | None = None,
    clust: list[float] | None = None,
    dindex: float | None = None,
    tindex: float | None = None,
    absolute: bool = False,
    dxyz: bool = False,
    gmean: bool = False,
    gmax: bool = False,
    quiet: bool = False,
) -> V3dmerge:
    """
    Build parameters.

    Args:
        input_files: Input dataset files
        output_file: Output dataset prefix
        blur_fwhm: Gaussian blur with FWHM in mm
        threshold: Threshold data to censor the intensities; only valid for 'fith',\
            'fico', or 'fitt' datasets
        clust: Form clusters with connection distance and clip off data not in\
            clusters of a minimum volume
        dindex: Specify sub-brick #j as the data source
        tindex: Specify sub-brick #k as the threshold source
        absolute: Take absolute values of intensities
        dxyz: Force cluster editing to behave as if all voxel dimensions were set\
            to 1 mm
        gmean: Combine datasets by averaging intensities (default)
        gmax: Combine datasets by taking max intensity
        quiet: Reduce the number of messages shown

    Returns:
        Parameter dictionary.
    """
    params: V3dmerge = {
        "@type": "afni/3dmerge",
        "input_files": input_files,
        "output_file": output_file,
        "absolute": absolute,
        "dxyz": dxyz,
        "gmean": gmean,
        "gmax": gmax,
        "quiet": quiet,
    }
    if blur_fwhm is not None:
        params["blur_fwhm"] = blur_fwhm
    if threshold is not None:
        params["threshold"] = threshold
    if clust is not None:
        params["clust"] = clust
    if dindex is not None:
        params["dindex"] = dindex
    if tindex is not None:
        params["tindex"] = tindex
    return params

def v_3dmerge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dmerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("blur_fwhm", None) is not None:
        if not isinstance(params["blur_fwhm"], (float, int)):
            raise StyxValidationError(f'`blur_fwhm` has the wrong type: Received `{type(params["blur_fwhm"])}` expected `float`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("clust", None) is not None:
        if not isinstance(params["clust"], list):
            raise StyxValidationError(f'`clust` has the wrong type: Received `{type(params["clust"])}` expected `list[float]`')
        if not (2 <= len(params["clust"]) <= 2):
            raise StyxValidationError("Parameter `clust` must contain between 2 and 2 elements (inclusive)")
        for e_2 in params["clust"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`clust` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("dindex", None) is not None:
        if not isinstance(params["dindex"], (float, int)):
            raise StyxValidationError(f'`dindex` has the wrong type: Received `{type(params["dindex"])}` expected `float`')
    if params.get("tindex", None) is not None:
        if not isinstance(params["tindex"], (float, int)):
            raise StyxValidationError(f'`tindex` has the wrong type: Received `{type(params["tindex"])}` expected `float`')
    if params.get("absolute", None) is not None:
        if not isinstance(params["absolute"], bool):
            raise StyxValidationError(f'`absolute` has the wrong type: Received `{type(params["absolute"])}` expected `bool`')
    if params.get("dxyz", None) is not None:
        if not isinstance(params["dxyz"], bool):
            raise StyxValidationError(f'`dxyz` has the wrong type: Received `{type(params["dxyz"])}` expected `bool`')
    if params.get("gmean", None) is not None:
        if not isinstance(params["gmean"], bool):
            raise StyxValidationError(f'`gmean` has the wrong type: Received `{type(params["gmean"])}` expected `bool`')
    if params.get("gmax", None) is not None:
        if not isinstance(params["gmax"], bool):
            raise StyxValidationError(f'`gmax` has the wrong type: Received `{type(params["gmax"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_3dmerge_cargs(params: V3dmerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dmerge")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-prefix")
    cargs.append(params["output_file"])
    v_0 = params.get("blur_fwhm")
    if v_0 is not None:
        cargs.append("-1blur_fwhm")
        cargs.append(str(v_0))
    v_1 = params.get("threshold")
    if v_1 is not None:
        cargs.append("-1thresh")
        cargs.append(str(v_1))
    v_2 = params.get("clust")
    if v_2 is not None:
        cargs.append("-1clust")
        for item1 in v_2:
            cargs.append(str(item1))
    v_3 = params.get("dindex")
    if v_3 is not None:
        cargs.append("-1dindex")
        cargs.append(str(v_3))
    v_4 = params.get("tindex")
    if v_4 is not None:
        cargs.append("-1tindex")
        cargs.append(str(v_4))
    if params.get("absolute"):
        cargs.append("-1abs")
    if params.get("dxyz"):
        cargs.append("-dxyz=1")
    if params.get("gmean"):
        cargs.append("-gmean")
    if params.get("gmax"):
        cargs.append("-gmax")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def v_3dmerge_outputs(params: V3dmerge, execution: Execution) -> V3dmergeOutputs:
    """Build the V3dmergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dataset_v: OutputPathType = execution.output_file(params["output_file"])
    return V3dmergeOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def v_3dmerge_execute(params: V3dmerge, runner: Runner | None = None) -> V3dmergeOutputs:
    """
    3dmerge

    3dmerge edits and merges 3D datasets by applying various operations like thresholding, blurring, clustering, and more.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dmerge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DMERGE_METADATA)
    execution.params(params)
    args = v_3dmerge_cargs(params, execution)
    out = v_3dmerge_outputs(params, execution)
    execution.run(args)
    return out

def v_3dmerge(
    input_files: list[InputPathType],
    output_file: str,
    blur_fwhm: float | None = None,
    threshold: float | None = None,
    clust: list[float] | None = None,
    dindex: float | None = None,
    tindex: float | None = None,
    absolute: bool = False,
    dxyz: bool = False,
    gmean: bool = False,
    gmax: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> V3dmergeOutputs:
    """
    3dmerge

    3dmerge edits and merges 3D datasets by applying various operations like thresholding, blurring, clustering, and more.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input dataset files
        output_file: Output dataset prefix
        blur_fwhm: Gaussian blur with FWHM in mm
        threshold: Threshold data to censor the intensities; only valid for 'fith',\
            'fico', or 'fitt' datasets
        clust: Form clusters with connection distance and clip off data not in\
            clusters of a minimum volume
        dindex: Specify sub-brick #j as the data source
        tindex: Specify sub-brick #k as the threshold source
        absolute: Take absolute values of intensities
        dxyz: Force cluster editing to behave as if all voxel dimensions were set\
            to 1 mm
        gmean: Combine datasets by averaging intensities (default)
        gmax: Combine datasets by taking max intensity
        quiet: Reduce the number of messages shown
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dmerge_params(
        input_files=input_files,
        output_file=output_file,
        blur_fwhm=blur_fwhm,
        threshold=threshold,
        clust=clust,
        dindex=dindex,
        tindex=tindex,
        absolute=absolute,
        dxyz=dxyz,
        gmean=gmean,
        gmax=gmax,
        quiet=quiet,
    )
    return v_3dmerge_execute(params, runner)

__all__ = [
    "V3dmerge",
    "V3dmergeOutputs",
    "V_3DMERGE_METADATA",
    "v_3dmerge_cargs",
    "v_3dmerge_outputs",
    "v_3dmerge_params",
    "v_3dmerge_execute",
    "v_3dmerge_validate",
    "v_3dmerge",
]