# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ENTROPY_METADATA = Metadata(
    id="3dEntropy",
    name="3dEntropy",
    package="afni",
)

V3dEntropy = typing.TypedDict(
    "V3dEntropy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dEntropy"]],
        "zskip": typing.NotRequired[bool],
        "input_dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dEntropyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_entropy_params(
    input_dataset: InputPathType,
    zskip: bool = False,
) -> V3dEntropy:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (stored as 16 bit shorts)
        zskip: Skip 0 values in the entropy computation

    Returns:
        Parameter dictionary.
    """
    params: V3dEntropy = {
        "@type": "afni/3dEntropy",
        "input_dataset": input_dataset,
        "zskip": zskip,
    }
    return params

def v_3d_entropy_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dEntropy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("zskip", None) is not None:
        if not isinstance(params["zskip"], bool):
            raise StyxValidationError(f'`zskip` has the wrong type: Received `{type(params["zskip"])}` expected `bool`')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')

def v_3d_entropy_cargs(params: V3dEntropy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dEntropy")
    if params.get("zskip"):
        cargs.append("-zskip")
    cargs.append(execution.input_file(params["input_dataset"]))
    return cargs

def v_3d_entropy_outputs(params: V3dEntropy, execution: Execution) -> V3dEntropyOutputs:
    """Build the V3dEntropyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dEntropyOutputs(
        root=root_v,
    )

def v_3d_entropy_execute(params: V3dEntropy, runner: Runner | None = None) -> V3dEntropyOutputs:
    """
    3dEntropy

    Computes entropy for a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_entropy_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ENTROPY_METADATA)
    execution.params(params)
    args = v_3d_entropy_cargs(params, execution)
    out = v_3d_entropy_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_entropy(
    input_dataset: InputPathType,
    zskip: bool = False,
    runner: Runner | None = None,
) -> V3dEntropyOutputs:
    """
    3dEntropy

    Computes entropy for a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (stored as 16 bit shorts)
        zskip: Skip 0 values in the entropy computation
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_entropy_params(
        input_dataset=input_dataset,
        zskip=zskip,
    )
    return v_3d_entropy_execute(params, runner)

__all__ = [
    "V3dEntropy",
    "V3dEntropyOutputs",
    "V_3D_ENTROPY_METADATA",
    "v_3d_entropy_cargs",
    "v_3d_entropy_outputs",
    "v_3d_entropy_params",
    "v_3d_entropy_execute",
    "v_3d_entropy_validate",
    "v_3d_entropy",
]