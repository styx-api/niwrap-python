# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_ROI_ROW_PY_METADATA = Metadata(
    id="fat_roi_row.py",
    name="fat_roi_row.py",
    package="afni",
)

FatRoiRowPy = typing.TypedDict(
    "FatRoiRowPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_roi_row.py"]],
        "roi": str,
        "matrix_files": typing.NotRequired[str],
        "list_file": typing.NotRequired[InputPathType],
        "extern_labs_no": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatRoiRowPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Selected ROI row output file in .row format."""

def fat_roi_row_py_params(
    roi: str,
    matrix_files: str | None = None,
    list_file: InputPathType | None = None,
    extern_labs_no: bool = False,
) -> FatRoiRowPy:
    """
    Build parameters.

    Args:
        roi: Specify which ROI's row of connectivity you want to select out. If\
            labeltables were used, you may select the ROI by either the string\
            label or the ROI mask number.
        matrix_files: Provide the set of matrix (*.grid or *.netcc) files by\
            searchable path. This can be a globbable entry in quotes containing\
            wildcard characters.
        list_file: Provide the set of matrix (*.grid or *.netcc) files by explicit\
            path in a text file. The LIST text file must contain at least one\
            column with the path to subject matrix file.
        extern_labs_no: Switch to turn off the writing/usage of user-defined labels\
            in the *.grid/*.netcc files.

    Returns:
        Parameter dictionary.
    """
    params: FatRoiRowPy = {
        "@type": "afni/fat_roi_row.py",
        "roi": roi,
        "extern_labs_no": extern_labs_no,
    }
    if matrix_files is not None:
        params["matrix_files"] = matrix_files
    if list_file is not None:
        params["list_file"] = list_file
    return params

def fat_roi_row_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatRoiRowPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("roi", None) is None:
        raise StyxValidationError("`roi` must not be None")
    if not isinstance(params["roi"], str):
        raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `str`')
    if params.get("matrix_files", None) is not None:
        if not isinstance(params["matrix_files"], str):
            raise StyxValidationError(f'`matrix_files` has the wrong type: Received `{type(params["matrix_files"])}` expected `str`')
    if params.get("list_file", None) is not None:
        if not isinstance(params["list_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`list_file` has the wrong type: Received `{type(params["list_file"])}` expected `InputPathType`')
    if params.get("extern_labs_no", None) is not None:
        if not isinstance(params["extern_labs_no"], bool):
            raise StyxValidationError(f'`extern_labs_no` has the wrong type: Received `{type(params["extern_labs_no"])}` expected `bool`')

def fat_roi_row_py_cargs(params: FatRoiRowPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_roi_row.py")
    cargs.append("-r")
    cargs.append(params["roi"])
    v_0 = params.get("matrix_files")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(v_0)
    v_1 = params.get("list_file")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_1))
    if params.get("extern_labs_no"):
        cargs.append("-E")
    return cargs

def fat_roi_row_py_outputs(params: FatRoiRowPy, execution: Execution) -> FatRoiRowPyOutputs:
    """Build the FatRoiRowPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["roi"]}_selected.row')
    return FatRoiRowPyOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def fat_roi_row_py_execute(params: FatRoiRowPy, runner: Runner | None = None) -> FatRoiRowPyOutputs:
    """
    fat_roi_row.py

    Select a single ROI's row out of a connectivity matrix file (*.grid or *.netcc) for viewing and/or further analysis.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_roi_row_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_ROI_ROW_PY_METADATA)
    execution.params(params)
    args = fat_roi_row_py_cargs(params, execution)
    out = fat_roi_row_py_outputs(params, execution)
    execution.run(args)
    return out

def fat_roi_row_py(
    roi: str,
    matrix_files: str | None = None,
    list_file: InputPathType | None = None,
    extern_labs_no: bool = False,
    runner: Runner | None = None,
) -> FatRoiRowPyOutputs:
    """
    fat_roi_row.py

    Select a single ROI's row out of a connectivity matrix file (*.grid or *.netcc) for viewing and/or further analysis.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        roi: Specify which ROI's row of connectivity you want to select out. If\
            labeltables were used, you may select the ROI by either the string\
            label or the ROI mask number.
        matrix_files: Provide the set of matrix (*.grid or *.netcc) files by\
            searchable path. This can be a globbable entry in quotes containing\
            wildcard characters.
        list_file: Provide the set of matrix (*.grid or *.netcc) files by explicit\
            path in a text file. The LIST text file must contain at least one\
            column with the path to subject matrix file.
        extern_labs_no: Switch to turn off the writing/usage of user-defined labels\
            in the *.grid/*.netcc files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_roi_row_py_params(
        roi=roi,
        matrix_files=matrix_files,
        list_file=list_file,
        extern_labs_no=extern_labs_no,
    )
    return fat_roi_row_py_execute(params, runner)

__all__ = [
    "FatRoiRowPy",
    "FatRoiRowPyOutputs",
    "FAT_ROI_ROW_PY_METADATA",
    "fat_roi_row_py_cargs",
    "fat_roi_row_py_outputs",
    "fat_roi_row_py_params",
    "fat_roi_row_py_execute",
    "fat_roi_row_py_validate",
    "fat_roi_row_py",
]