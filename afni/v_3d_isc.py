# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ISC_METADATA = Metadata(
    id="3dISC",
    name="3dISC",
    package="afni",
)

V3dIscIoFunctions = typing.Literal["AFNI"] | typing.Literal["R"]

V3dIsc = typing.TypedDict(
    "V3dIsc",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dISC"]],
        "outfile_prefix": str,
        "num_jobs": typing.NotRequired[float],
        "mask_file": typing.NotRequired[InputPathType],
        "model_structure": str,
        "qvar_centers": typing.NotRequired[str],
        "quantitative_vars": typing.NotRequired[str],
        "fisher_transform": typing.NotRequired[bool],
        "io_functions": typing.NotRequired[V3dIscIoFunctions],
        "data_table": str,
    },
)

@dataclasses.dataclass
class V3dIscOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    isc_output: OutputPathType
    """Main output ISC file"""
    tstat_output: OutputPathType
    """T-statistic of ISC"""

def v_3d_isc_params(
    outfile_prefix: str,
    model_structure: str,
    data_table: str,
    num_jobs: float | None = None,
    mask_file: InputPathType | None = None,
    qvar_centers: str | None = None,
    quantitative_vars: str | None = None,
    fisher_transform: bool = False,
    io_functions: V3dIscIoFunctions | None = None,
) -> V3dIsc:
    """
    Build parameters.

    Args:
        outfile_prefix: Output file name. For AFNI format, provide prefix only,\
            with no view+suffix needed.
        model_structure: Specify the model structure for all the variables. The\
            expression FORMULA with more than one variable has to be surrounded\
            within quotes.
        data_table: List the data structure with a header as the first line. Has to\
            occur last in the script.
        num_jobs: Specify the number of jobs to run concurrently. Choose 1 for a\
            single-processor computer.
        mask_file: Process voxels inside this mask only. Default is no masking.
        qvar_centers: Specify centering values for quantitative variables\
            identified under -qVars. Multiple centers are separated by commas\
            without spaces and should be within quotes.
        quantitative_vars: Identify quantitative variables (or covariates). The\
            list should be comma-separated and within quotes.
        fisher_transform: Perform Fisher transformation on the response variable\
            (input files) if it is a correlation value.
        io_functions: Use AFNI's C io functions (default) or R's io functions.

    Returns:
        Parameter dictionary.
    """
    params: V3dIsc = {
        "@type": "afni/3dISC",
        "outfile_prefix": outfile_prefix,
        "model_structure": model_structure,
        "data_table": data_table,
        "fisher_transform": fisher_transform,
    }
    if num_jobs is not None:
        params["num_jobs"] = num_jobs
    if mask_file is not None:
        params["mask_file"] = mask_file
    if qvar_centers is not None:
        params["qvar_centers"] = qvar_centers
    if quantitative_vars is not None:
        params["quantitative_vars"] = quantitative_vars
    if io_functions is not None:
        params["io_functions"] = io_functions
    return params

def v_3d_isc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dIsc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("outfile_prefix", None) is None:
        raise StyxValidationError("`outfile_prefix` must not be None")
    if not isinstance(params["outfile_prefix"], str):
        raise StyxValidationError(f'`outfile_prefix` has the wrong type: Received `{type(params["outfile_prefix"])}` expected `str`')
    if params.get("num_jobs", None) is not None:
        if not isinstance(params["num_jobs"], (float, int)):
            raise StyxValidationError(f'`num_jobs` has the wrong type: Received `{type(params["num_jobs"])}` expected `float`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("model_structure", None) is None:
        raise StyxValidationError("`model_structure` must not be None")
    if not isinstance(params["model_structure"], str):
        raise StyxValidationError(f'`model_structure` has the wrong type: Received `{type(params["model_structure"])}` expected `str`')
    if params.get("qvar_centers", None) is not None:
        if not isinstance(params["qvar_centers"], str):
            raise StyxValidationError(f'`qvar_centers` has the wrong type: Received `{type(params["qvar_centers"])}` expected `str`')
    if params.get("quantitative_vars", None) is not None:
        if not isinstance(params["quantitative_vars"], str):
            raise StyxValidationError(f'`quantitative_vars` has the wrong type: Received `{type(params["quantitative_vars"])}` expected `str`')
    if params.get("fisher_transform", None) is not None:
        if not isinstance(params["fisher_transform"], bool):
            raise StyxValidationError(f'`fisher_transform` has the wrong type: Received `{type(params["fisher_transform"])}` expected `bool`')
    if params.get("io_functions", None) is not None:
        if not isinstance(params["io_functions"], str):
            raise StyxValidationError(f'`io_functions` has the wrong type: Received `{type(params["io_functions"])}` expected `V3dIscIoFunctions`')
        if params["io_functions"] not in ["AFNI", "R"]:
            raise StyxValidationError("Parameter `io_functions` must be one of [\"AFNI\", \"R\"]")
    if params.get("data_table", None) is None:
        raise StyxValidationError("`data_table` must not be None")
    if not isinstance(params["data_table"], str):
        raise StyxValidationError(f'`data_table` has the wrong type: Received `{type(params["data_table"])}` expected `str`')

def v_3d_isc_cargs(params: V3dIsc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dISC")
    cargs.append("-prefix")
    cargs.append(params["outfile_prefix"])
    v_0 = params.get("num_jobs")
    if v_0 is not None:
        cargs.append("-jobs")
        cargs.append(str(v_0))
    v_1 = params.get("mask_file")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    cargs.append("-model")
    cargs.append(params["model_structure"])
    v_2 = params.get("qvar_centers")
    if v_2 is not None:
        cargs.append("-qVarCenters")
        cargs.append(v_2)
    v_3 = params.get("quantitative_vars")
    if v_3 is not None:
        cargs.append("-qVars")
        cargs.append(v_3)
    if params.get("fisher_transform"):
        cargs.append("-r2z")
    v_4 = params.get("io_functions")
    if v_4 is not None:
        cargs.append("-cio")
        cargs.append(str(v_4))
    cargs.append("-dataTable")
    cargs.append(params["data_table"])
    return cargs

def v_3d_isc_outputs(params: V3dIsc, execution: Execution) -> V3dIscOutputs:
    """Build the V3dIscOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    isc_output_v: OutputPathType = execution.output_file(f'{params["outfile_prefix"]}_ISC.nii')
    tstat_output_v: OutputPathType = execution.output_file(f'{params["outfile_prefix"]}_tstat.nii')
    return V3dIscOutputs(
        root=root_v,
        isc_output=isc_output_v,
        tstat_output=tstat_output_v,
    )

def v_3d_isc_execute(params: V3dIsc, runner: Runner | None = None) -> V3dIscOutputs:
    """
    3dISC

    Program for Voxelwise Inter-Subject Correlation (ISC) Analysis using linear mixed-effects modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_isc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ISC_METADATA)
    execution.params(params)
    args = v_3d_isc_cargs(params, execution)
    out = v_3d_isc_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_isc(
    outfile_prefix: str,
    model_structure: str,
    data_table: str,
    num_jobs: float | None = None,
    mask_file: InputPathType | None = None,
    qvar_centers: str | None = None,
    quantitative_vars: str | None = None,
    fisher_transform: bool = False,
    io_functions: V3dIscIoFunctions | None = None,
    runner: Runner | None = None,
) -> V3dIscOutputs:
    """
    3dISC

    Program for Voxelwise Inter-Subject Correlation (ISC) Analysis using linear mixed-effects modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        outfile_prefix: Output file name. For AFNI format, provide prefix only,\
            with no view+suffix needed.
        model_structure: Specify the model structure for all the variables. The\
            expression FORMULA with more than one variable has to be surrounded\
            within quotes.
        data_table: List the data structure with a header as the first line. Has to\
            occur last in the script.
        num_jobs: Specify the number of jobs to run concurrently. Choose 1 for a\
            single-processor computer.
        mask_file: Process voxels inside this mask only. Default is no masking.
        qvar_centers: Specify centering values for quantitative variables\
            identified under -qVars. Multiple centers are separated by commas\
            without spaces and should be within quotes.
        quantitative_vars: Identify quantitative variables (or covariates). The\
            list should be comma-separated and within quotes.
        fisher_transform: Perform Fisher transformation on the response variable\
            (input files) if it is a correlation value.
        io_functions: Use AFNI's C io functions (default) or R's io functions.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_isc_params(
        outfile_prefix=outfile_prefix,
        model_structure=model_structure,
        data_table=data_table,
        num_jobs=num_jobs,
        mask_file=mask_file,
        qvar_centers=qvar_centers,
        quantitative_vars=quantitative_vars,
        fisher_transform=fisher_transform,
        io_functions=io_functions,
    )
    return v_3d_isc_execute(params, runner)

__all__ = [
    "V3dIsc",
    "V3dIscOutputs",
    "V_3D_ISC_METADATA",
    "v_3d_isc_cargs",
    "v_3d_isc_outputs",
    "v_3d_isc_params",
    "v_3d_isc_execute",
    "v_3d_isc_validate",
    "v_3d_isc",
]