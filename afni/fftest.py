# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FFTEST_METADATA = Metadata(
    id="fftest",
    name="fftest",
    package="afni",
)

Fftest = typing.TypedDict(
    "Fftest",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fftest"]],
        "length": float,
        "num_tests": float,
        "vector_size": float,
        "quiet_mode": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FftestOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fftest_params(
    length: float,
    num_tests: float,
    vector_size: float,
    quiet_mode: bool = False,
) -> Fftest:
    """
    Build parameters.

    Args:
        length: Length of the test
        num_tests: Number of tests to run
        vector_size: Vector size for the test
        quiet_mode: Quiet mode

    Returns:
        Parameter dictionary.
    """
    params: Fftest = {
        "@type": "afni/fftest",
        "length": length,
        "num_tests": num_tests,
        "vector_size": vector_size,
        "quiet_mode": quiet_mode,
    }
    return params

def fftest_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fftest."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("length", None) is None:
        raise StyxValidationError("`length` must not be None")
    if not isinstance(params["length"], (float, int)):
        raise StyxValidationError(f'`length` has the wrong type: Received `{type(params["length"])}` expected `float`')
    if params.get("num_tests", None) is None:
        raise StyxValidationError("`num_tests` must not be None")
    if not isinstance(params["num_tests"], (float, int)):
        raise StyxValidationError(f'`num_tests` has the wrong type: Received `{type(params["num_tests"])}` expected `float`')
    if params.get("vector_size", None) is None:
        raise StyxValidationError("`vector_size` must not be None")
    if not isinstance(params["vector_size"], (float, int)):
        raise StyxValidationError(f'`vector_size` has the wrong type: Received `{type(params["vector_size"])}` expected `float`')
    if params.get("quiet_mode", None) is not None:
        if not isinstance(params["quiet_mode"], bool):
            raise StyxValidationError(f'`quiet_mode` has the wrong type: Received `{type(params["quiet_mode"])}` expected `bool`')

def fftest_cargs(params: Fftest, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fftest")
    cargs.append(str(params["length"]))
    cargs.append(str(params["num_tests"]))
    cargs.append(str(params["vector_size"]))
    if params.get("quiet_mode"):
        cargs.append("-q")
    return cargs

def fftest_outputs(params: Fftest, execution: Execution) -> FftestOutputs:
    """Build the FftestOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FftestOutputs(
        root=root_v,
    )

def fftest_execute(params: Fftest, runner: Runner | None = None) -> FftestOutputs:
    """
    fftest

    A command line tool for testing purposes

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fftest_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FFTEST_METADATA)
    execution.params(params)
    args = fftest_cargs(params, execution)
    out = fftest_outputs(params, execution)
    execution.run(args)
    return out

def fftest(
    length: float,
    num_tests: float,
    vector_size: float,
    quiet_mode: bool = False,
    runner: Runner | None = None,
) -> FftestOutputs:
    """
    fftest

    A command line tool for testing purposes

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        length: Length of the test
        num_tests: Number of tests to run
        vector_size: Vector size for the test
        quiet_mode: Quiet mode
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fftest_params(
        length=length,
        num_tests=num_tests,
        vector_size=vector_size,
        quiet_mode=quiet_mode,
    )
    return fftest_execute(params, runner)

__all__ = [
    "Fftest",
    "FftestOutputs",
    "FFTEST_METADATA",
    "fftest_cargs",
    "fftest_outputs",
    "fftest_params",
    "fftest_execute",
    "fftest_validate",
    "fftest",
]