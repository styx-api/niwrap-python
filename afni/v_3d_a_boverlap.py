# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_A_BOVERLAP_METADATA = Metadata(
    id="3dABoverlap",
    name="3dABoverlap",
    package="afni",
)

V3dABoverlap = typing.TypedDict(
    "V3dABoverlap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dABoverlap"]],
        "dataset_a": InputPathType,
        "dataset_b": InputPathType,
        "no_automask": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dABoverlapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_a_boverlap_params(
    dataset_a: InputPathType,
    dataset_b: InputPathType,
    no_automask: bool = False,
    quiet: bool = False,
    verbose: bool = False,
) -> V3dABoverlap:
    """
    Build parameters.

    Args:
        dataset_a: First input dataset
        dataset_b: Second input dataset
        no_automask: Consider input datasets as masks (automask does not work on\
            mask datasets)
        quiet: Be as quiet as possible (without being entirely mute)
        verbose: Print out some progress reports (to stderr)

    Returns:
        Parameter dictionary.
    """
    params: V3dABoverlap = {
        "@type": "afni/3dABoverlap",
        "dataset_a": dataset_a,
        "dataset_b": dataset_b,
        "no_automask": no_automask,
        "quiet": quiet,
        "verbose": verbose,
    }
    return params

def v_3d_a_boverlap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dABoverlap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset_a", None) is None:
        raise StyxValidationError("`dataset_a` must not be None")
    if not isinstance(params["dataset_a"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset_a` has the wrong type: Received `{type(params["dataset_a"])}` expected `InputPathType`')
    if params.get("dataset_b", None) is None:
        raise StyxValidationError("`dataset_b` must not be None")
    if not isinstance(params["dataset_b"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset_b` has the wrong type: Received `{type(params["dataset_b"])}` expected `InputPathType`')
    if params.get("no_automask", None) is not None:
        if not isinstance(params["no_automask"], bool):
            raise StyxValidationError(f'`no_automask` has the wrong type: Received `{type(params["no_automask"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def v_3d_a_boverlap_cargs(params: V3dABoverlap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dABoverlap")
    cargs.append(execution.input_file(params["dataset_a"]))
    cargs.append(execution.input_file(params["dataset_b"]))
    if params.get("no_automask"):
        cargs.append("-no_automask")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("verbose"):
        cargs.append("-verb")
    return cargs

def v_3d_a_boverlap_outputs(params: V3dABoverlap, execution: Execution) -> V3dABoverlapOutputs:
    """Build the V3dABoverlapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dABoverlapOutputs(
        root=root_v,
    )

def v_3d_a_boverlap_execute(params: V3dABoverlap, runner: Runner | None = None) -> V3dABoverlapOutputs:
    """
    3dABoverlap

    Counts various metrics about how the automasks of datasets A and B overlap or don't overlap

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_a_boverlap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_A_BOVERLAP_METADATA)
    execution.params(params)
    args = v_3d_a_boverlap_cargs(params, execution)
    out = v_3d_a_boverlap_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_a_boverlap(
    dataset_a: InputPathType,
    dataset_b: InputPathType,
    no_automask: bool = False,
    quiet: bool = False,
    verbose: bool = False,
    runner: Runner | None = None,
) -> V3dABoverlapOutputs:
    """
    3dABoverlap

    Counts various metrics about how the automasks of datasets A and B overlap or don't overlap

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset_a: First input dataset
        dataset_b: Second input dataset
        no_automask: Consider input datasets as masks (automask does not work on\
            mask datasets)
        quiet: Be as quiet as possible (without being entirely mute)
        verbose: Print out some progress reports (to stderr)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_a_boverlap_params(
        dataset_a=dataset_a,
        dataset_b=dataset_b,
        no_automask=no_automask,
        quiet=quiet,
        verbose=verbose,
    )
    return v_3d_a_boverlap_execute(params, runner)

__all__ = [
    "V3dABoverlap",
    "V3dABoverlapOutputs",
    "V_3D_A_BOVERLAP_METADATA",
    "v_3d_a_boverlap_cargs",
    "v_3d_a_boverlap_outputs",
    "v_3d_a_boverlap_params",
    "v_3d_a_boverlap_execute",
    "v_3d_a_boverlap_validate",
    "v_3d_a_boverlap",
]