# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_PERIODOGRAM_METADATA = Metadata(
    id="3dPeriodogram",
    name="3dPeriodogram",
    package="afni",
)

V3dPeriodogram = typing.TypedDict(
    "V3dPeriodogram",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dPeriodogram"]],
        "prefix": typing.NotRequired[str],
        "taper": typing.NotRequired[float],
        "nfft": typing.NotRequired[float],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dPeriodogramOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_header: OutputPathType | None
    """Output dataset header file"""
    output_brick: OutputPathType | None
    """Output dataset brick file"""

def v_3d_periodogram_params(
    dataset: InputPathType,
    prefix: str | None = None,
    taper: float | None = None,
    nfft: float | None = None,
) -> V3dPeriodogram:
    """
    Build parameters.

    Args:
        dataset: Input dataset
        prefix: Prefix for the output dataset
        taper: Fraction of data to taper
        nfft: Set FFT length to a specific number of points

    Returns:
        Parameter dictionary.
    """
    params: V3dPeriodogram = {
        "@type": "afni/3dPeriodogram",
        "dataset": dataset,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if taper is not None:
        params["taper"] = taper
    if nfft is not None:
        params["nfft"] = nfft
    return params

def v_3d_periodogram_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dPeriodogram."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("taper", None) is not None:
        if not isinstance(params["taper"], (float, int)):
            raise StyxValidationError(f'`taper` has the wrong type: Received `{type(params["taper"])}` expected `float`')
        if not (0 <= params["taper"] <= 1):
            raise StyxValidationError("Parameter `taper` must be between 0 and 1 (inclusive)")
    if params.get("nfft", None) is not None:
        if not isinstance(params["nfft"], (float, int)):
            raise StyxValidationError(f'`nfft` has the wrong type: Received `{type(params["nfft"])}` expected `float`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def v_3d_periodogram_cargs(params: V3dPeriodogram, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dPeriodogram")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("taper")
    if v_1 is not None:
        cargs.append("-taper")
        cargs.append(str(v_1))
    v_2 = params.get("nfft")
    if v_2 is not None:
        cargs.append("-nfft")
        cargs.append(str(v_2))
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def v_3d_periodogram_outputs(params: V3dPeriodogram, execution: Execution) -> V3dPeriodogramOutputs:
    """Build the V3dPeriodogramOutputs object for this tool."""
    output_header_v: OutputPathType | None = None
    output_brick_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_header_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_brick_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.BRIK')
    return V3dPeriodogramOutputs(
        root=root_v,
        output_header=output_header_v,
        output_brick=output_brick_v,
    )

def v_3d_periodogram_execute(params: V3dPeriodogram, runner: Runner | None = None) -> V3dPeriodogramOutputs:
    """
    3dPeriodogram

    Computes the periodogram of each voxel time series. The periodogram is a crude estimate of the power spectrum.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_periodogram_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_PERIODOGRAM_METADATA)
    execution.params(params)
    args = v_3d_periodogram_cargs(params, execution)
    out = v_3d_periodogram_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_periodogram(
    dataset: InputPathType,
    prefix: str | None = None,
    taper: float | None = None,
    nfft: float | None = None,
    runner: Runner | None = None,
) -> V3dPeriodogramOutputs:
    """
    3dPeriodogram

    Computes the periodogram of each voxel time series. The periodogram is a crude estimate of the power spectrum.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset
        prefix: Prefix for the output dataset
        taper: Fraction of data to taper
        nfft: Set FFT length to a specific number of points
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_periodogram_params(
        dataset=dataset,
        prefix=prefix,
        taper=taper,
        nfft=nfft,
    )
    return v_3d_periodogram_execute(params, runner)

__all__ = [
    "V3dPeriodogram",
    "V3dPeriodogramOutputs",
    "V_3D_PERIODOGRAM_METADATA",
    "v_3d_periodogram_cargs",
    "v_3d_periodogram_outputs",
    "v_3d_periodogram_params",
    "v_3d_periodogram_execute",
    "v_3d_periodogram_validate",
    "v_3d_periodogram",
]