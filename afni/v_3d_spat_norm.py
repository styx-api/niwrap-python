# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_SPAT_NORM_METADATA = Metadata(
    id="3dSpatNorm",
    name="3dSpatNorm",
    package="afni",
)

V3dSpatNorm = typing.TypedDict(
    "V3dSpatNorm",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dSpatNorm"]],
        "dataset": InputPathType,
        "prefix": typing.NotRequired[str],
        "orig_space": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "monkey": typing.NotRequired[bool],
        "marmot": typing.NotRequired[bool],
        "rat": typing.NotRequired[bool],
        "human": typing.NotRequired[bool],
        "bottom_cuts": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dSpatNormOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_head: OutputPathType | None
    """Output dataset (HEAD file)"""
    out_brik: OutputPathType | None
    """Output dataset (BRIK file)"""

def v_3d_spat_norm_params(
    dataset: InputPathType,
    prefix: str | None = None,
    orig_space: bool = False,
    verbose: bool = False,
    monkey: bool = False,
    marmot: bool = False,
    rat: bool = False,
    human: bool = False,
    bottom_cuts: str | None = None,
) -> V3dSpatNorm:
    """
    Build parameters.

    Args:
        dataset: Input dataset
        prefix: Write output dataset using 'ppp' for the prefix
        orig_space: Write output dataset using the same grid as dataset
        verbose: Write out progress reports
        monkey: Monkey business
        marmot: Marmoset head
        rat: Rat head
        human: Bone head (default)
        bottom_cuts: Make approximate cuts at the bottom to shave non-brain areas.\
            CUTFLAGS is a string of characters indicating which sides to cut: 'A'\
            for anterior, 'P' for posterior, 'R' for right, 'L' for left. Example:\
            -bottom_cuts APLR

    Returns:
        Parameter dictionary.
    """
    params: V3dSpatNorm = {
        "@type": "afni/3dSpatNorm",
        "dataset": dataset,
        "orig_space": orig_space,
        "verbose": verbose,
        "monkey": monkey,
        "marmot": marmot,
        "rat": rat,
        "human": human,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if bottom_cuts is not None:
        params["bottom_cuts"] = bottom_cuts
    return params

def v_3d_spat_norm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dSpatNorm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("orig_space", None) is not None:
        if not isinstance(params["orig_space"], bool):
            raise StyxValidationError(f'`orig_space` has the wrong type: Received `{type(params["orig_space"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("monkey", None) is not None:
        if not isinstance(params["monkey"], bool):
            raise StyxValidationError(f'`monkey` has the wrong type: Received `{type(params["monkey"])}` expected `bool`')
    if params.get("marmot", None) is not None:
        if not isinstance(params["marmot"], bool):
            raise StyxValidationError(f'`marmot` has the wrong type: Received `{type(params["marmot"])}` expected `bool`')
    if params.get("rat", None) is not None:
        if not isinstance(params["rat"], bool):
            raise StyxValidationError(f'`rat` has the wrong type: Received `{type(params["rat"])}` expected `bool`')
    if params.get("human", None) is not None:
        if not isinstance(params["human"], bool):
            raise StyxValidationError(f'`human` has the wrong type: Received `{type(params["human"])}` expected `bool`')
    if params.get("bottom_cuts", None) is not None:
        if not isinstance(params["bottom_cuts"], str):
            raise StyxValidationError(f'`bottom_cuts` has the wrong type: Received `{type(params["bottom_cuts"])}` expected `str`')

def v_3d_spat_norm_cargs(params: V3dSpatNorm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dSpatNorm")
    cargs.append(execution.input_file(params["dataset"]))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("orig_space"):
        cargs.append("-orig_space")
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("monkey"):
        cargs.append("-monkey")
    if params.get("marmot"):
        cargs.append("-marmost")
    if params.get("rat"):
        cargs.append("-rat")
    if params.get("human"):
        cargs.append("-human")
    v_1 = params.get("bottom_cuts")
    if v_1 is not None:
        cargs.append("-bottom_cuts")
        cargs.append(v_1)
    return cargs

def v_3d_spat_norm_outputs(params: V3dSpatNorm, execution: Execution) -> V3dSpatNormOutputs:
    """Build the V3dSpatNormOutputs object for this tool."""
    out_head_v: OutputPathType | None = None
    out_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        out_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    return V3dSpatNormOutputs(
        root=root_v,
        out_head=out_head_v,
        out_brik=out_brik_v,
    )

def v_3d_spat_norm_execute(params: V3dSpatNorm, runner: Runner | None = None) -> V3dSpatNormOutputs:
    """
    3dSpatNorm

    An obsolete tool for spatial normalization

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_spat_norm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_SPAT_NORM_METADATA)
    execution.params(params)
    args = v_3d_spat_norm_cargs(params, execution)
    out = v_3d_spat_norm_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_spat_norm(
    dataset: InputPathType,
    prefix: str | None = None,
    orig_space: bool = False,
    verbose: bool = False,
    monkey: bool = False,
    marmot: bool = False,
    rat: bool = False,
    human: bool = False,
    bottom_cuts: str | None = None,
    runner: Runner | None = None,
) -> V3dSpatNormOutputs:
    """
    3dSpatNorm

    An obsolete tool for spatial normalization

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset
        prefix: Write output dataset using 'ppp' for the prefix
        orig_space: Write output dataset using the same grid as dataset
        verbose: Write out progress reports
        monkey: Monkey business
        marmot: Marmoset head
        rat: Rat head
        human: Bone head (default)
        bottom_cuts: Make approximate cuts at the bottom to shave non-brain areas.\
            CUTFLAGS is a string of characters indicating which sides to cut: 'A'\
            for anterior, 'P' for posterior, 'R' for right, 'L' for left. Example:\
            -bottom_cuts APLR
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_spat_norm_params(
        dataset=dataset,
        prefix=prefix,
        orig_space=orig_space,
        verbose=verbose,
        monkey=monkey,
        marmot=marmot,
        rat=rat,
        human=human,
        bottom_cuts=bottom_cuts,
    )
    return v_3d_spat_norm_execute(params, runner)

__all__ = [
    "V3dSpatNorm",
    "V3dSpatNormOutputs",
    "V_3D_SPAT_NORM_METADATA",
    "v_3d_spat_norm_cargs",
    "v_3d_spat_norm_outputs",
    "v_3d_spat_norm_params",
    "v_3d_spat_norm_execute",
    "v_3d_spat_norm_validate",
    "v_3d_spat_norm",
]