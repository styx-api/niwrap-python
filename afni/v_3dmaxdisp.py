# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DMAXDISP_METADATA = Metadata(
    id="3dmaxdisp",
    name="3dmaxdisp",
    package="afni",
)

V3dmaxdisp = typing.TypedDict(
    "V3dmaxdisp",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dmaxdisp"]],
        "inset": InputPathType,
        "matrix": InputPathType,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dmaxdispOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    displacement_output: OutputPathType
    """Results showing average and maximum displacements."""

def v_3dmaxdisp_params(
    inset: InputPathType,
    matrix: InputPathType,
    verbose: bool = False,
) -> V3dmaxdisp:
    """
    Build parameters.

    Args:
        inset: Input dataset file used to form the mask over which displacements\
            will be computed.
        matrix: 3x4 affine transformation matrix file applied to the coordinates of\
            the voxels in the dataset mask.
        verbose: Print a few progress reports.

    Returns:
        Parameter dictionary.
    """
    params: V3dmaxdisp = {
        "@type": "afni/3dmaxdisp",
        "inset": inset,
        "matrix": matrix,
        "verbose": verbose,
    }
    return params

def v_3dmaxdisp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dmaxdisp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("matrix", None) is None:
        raise StyxValidationError("`matrix` must not be None")
    if not isinstance(params["matrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`matrix` has the wrong type: Received `{type(params["matrix"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def v_3dmaxdisp_cargs(params: V3dmaxdisp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dmaxdisp")
    cargs.append("-inset")
    cargs.append(execution.input_file(params["inset"]))
    cargs.append("-matrix")
    cargs.append(execution.input_file(params["matrix"]))
    if params.get("verbose"):
        cargs.append("-verb")
    return cargs

def v_3dmaxdisp_outputs(params: V3dmaxdisp, execution: Execution) -> V3dmaxdispOutputs:
    """Build the V3dmaxdispOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    displacement_output_v: OutputPathType = execution.output_file("stdout")
    return V3dmaxdispOutputs(
        root=root_v,
        displacement_output=displacement_output_v,
    )

def v_3dmaxdisp_execute(params: V3dmaxdisp, runner: Runner | None = None) -> V3dmaxdispOutputs:
    """
    3dmaxdisp

    Reads in a 3D dataset and a DICOM-based affine matrix to output the average and maximum displacement applied to the edge voxels of the 3D dataset's automask.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dmaxdisp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DMAXDISP_METADATA)
    execution.params(params)
    args = v_3dmaxdisp_cargs(params, execution)
    out = v_3dmaxdisp_outputs(params, execution)
    execution.run(args)
    return out

def v_3dmaxdisp(
    inset: InputPathType,
    matrix: InputPathType,
    verbose: bool = False,
    runner: Runner | None = None,
) -> V3dmaxdispOutputs:
    """
    3dmaxdisp

    Reads in a 3D dataset and a DICOM-based affine matrix to output the average and maximum displacement applied to the edge voxels of the 3D dataset's automask.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inset: Input dataset file used to form the mask over which displacements\
            will be computed.
        matrix: 3x4 affine transformation matrix file applied to the coordinates of\
            the voxels in the dataset mask.
        verbose: Print a few progress reports.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dmaxdisp_params(
        inset=inset,
        matrix=matrix,
        verbose=verbose,
    )
    return v_3dmaxdisp_execute(params, runner)

__all__ = [
    "V3dmaxdisp",
    "V3dmaxdispOutputs",
    "V_3DMAXDISP_METADATA",
    "v_3dmaxdisp_cargs",
    "v_3dmaxdisp_outputs",
    "v_3dmaxdisp_params",
    "v_3dmaxdisp_execute",
    "v_3dmaxdisp_validate",
    "v_3dmaxdisp",
]