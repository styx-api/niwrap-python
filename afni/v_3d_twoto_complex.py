# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TWOTO_COMPLEX_METADATA = Metadata(
    id="3dTwotoComplex",
    name="3dTwotoComplex",
    package="afni",
)

V3dTwotoComplex = typing.TypedDict(
    "V3dTwotoComplex",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTwotoComplex"]],
        "dataset1": InputPathType,
        "dataset2": typing.NotRequired[InputPathType],
        "prefix": typing.NotRequired[str],
        "ri": typing.NotRequired[bool],
        "mp": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class V3dTwotoComplexOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_brick: OutputPathType | None
    """Output complex-valued dataset with prefix"""
    out_head: OutputPathType | None
    """Header for the complex-valued dataset"""

def v_3d_twoto_complex_params(
    dataset1: InputPathType,
    dataset2: InputPathType | None = None,
    prefix: str | None = None,
    ri: bool = False,
    mp: bool = False,
    mask: InputPathType | None = None,
) -> V3dTwotoComplex:
    """
    Build parameters.

    Args:
        dataset1: Input dataset (either as 1 dataset with 2 sub-bricks or 2\
            separate datasets)
        dataset2: Second input dataset (optional if 2 sub-bricks in the first\
            dataset)
        prefix: Prefix for the output dataset [default='cmplx']
        ri: Specify that the 2 inputs are real and imaginary parts [this is the\
            default]
        mp: Specify that the 2 inputs are magnitude and phase [phase is in radians]
        mask: Only output nonzero values where the mask dataset is nonzero

    Returns:
        Parameter dictionary.
    """
    params: V3dTwotoComplex = {
        "@type": "afni/3dTwotoComplex",
        "dataset1": dataset1,
        "ri": ri,
        "mp": mp,
    }
    if dataset2 is not None:
        params["dataset2"] = dataset2
    if prefix is not None:
        params["prefix"] = prefix
    if mask is not None:
        params["mask"] = mask
    return params

def v_3d_twoto_complex_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTwotoComplex."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset1", None) is None:
        raise StyxValidationError("`dataset1` must not be None")
    if not isinstance(params["dataset1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset1` has the wrong type: Received `{type(params["dataset1"])}` expected `InputPathType`')
    if params.get("dataset2", None) is not None:
        if not isinstance(params["dataset2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`dataset2` has the wrong type: Received `{type(params["dataset2"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("ri", None) is not None:
        if not isinstance(params["ri"], bool):
            raise StyxValidationError(f'`ri` has the wrong type: Received `{type(params["ri"])}` expected `bool`')
    if params.get("mp", None) is not None:
        if not isinstance(params["mp"], bool):
            raise StyxValidationError(f'`mp` has the wrong type: Received `{type(params["mp"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')

def v_3d_twoto_complex_cargs(params: V3dTwotoComplex, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTwotoComplex")
    cargs.append(execution.input_file(params["dataset1"]))
    v_0 = params.get("dataset2")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("prefix")
    if v_1 is not None:
        cargs.append("-prefix")
        cargs.append(v_1)
    if params.get("ri"):
        cargs.append("-RI")
    if params.get("mp"):
        cargs.append("-MP")
    v_2 = params.get("mask")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    return cargs

def v_3d_twoto_complex_outputs(params: V3dTwotoComplex, execution: Execution) -> V3dTwotoComplexOutputs:
    """Build the V3dTwotoComplexOutputs object for this tool."""
    out_brick_v: OutputPathType | None = None
    out_head_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_brick_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.BRIK')
    __v1 = params.get("prefix")
    if __v1 is not None:
        out_head_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.HEAD')
    return V3dTwotoComplexOutputs(
        root=root_v,
        out_brick=out_brick_v,
        out_head=out_head_v,
    )

def v_3d_twoto_complex_execute(params: V3dTwotoComplex, runner: Runner | None = None) -> V3dTwotoComplexOutputs:
    """
    3dTwotoComplex

    Converts 2 sub-bricks of input to a complex-valued dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_twoto_complex_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TWOTO_COMPLEX_METADATA)
    execution.params(params)
    args = v_3d_twoto_complex_cargs(params, execution)
    out = v_3d_twoto_complex_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_twoto_complex(
    dataset1: InputPathType,
    dataset2: InputPathType | None = None,
    prefix: str | None = None,
    ri: bool = False,
    mp: bool = False,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dTwotoComplexOutputs:
    """
    3dTwotoComplex

    Converts 2 sub-bricks of input to a complex-valued dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset1: Input dataset (either as 1 dataset with 2 sub-bricks or 2\
            separate datasets)
        dataset2: Second input dataset (optional if 2 sub-bricks in the first\
            dataset)
        prefix: Prefix for the output dataset [default='cmplx']
        ri: Specify that the 2 inputs are real and imaginary parts [this is the\
            default]
        mp: Specify that the 2 inputs are magnitude and phase [phase is in radians]
        mask: Only output nonzero values where the mask dataset is nonzero
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_twoto_complex_params(
        dataset1=dataset1,
        dataset2=dataset2,
        prefix=prefix,
        ri=ri,
        mp=mp,
        mask=mask,
    )
    return v_3d_twoto_complex_execute(params, runner)

__all__ = [
    "V3dTwotoComplex",
    "V3dTwotoComplexOutputs",
    "V_3D_TWOTO_COMPLEX_METADATA",
    "v_3d_twoto_complex_cargs",
    "v_3d_twoto_complex_outputs",
    "v_3d_twoto_complex_params",
    "v_3d_twoto_complex_execute",
    "v_3d_twoto_complex_validate",
    "v_3d_twoto_complex",
]