# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NOISY_SKULL_STRIP_METADATA = Metadata(
    id="@NoisySkullStrip",
    name="@NoisySkullStrip",
    package="afni",
)

NoisySkullStrip = typing.TypedDict(
    "NoisySkullStrip",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@NoisySkullStrip"]],
        "input_file": InputPathType,
        "keep_tmp": typing.NotRequired[bool],
        "3dskullstrip_opts": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class NoisySkullStripOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    anat_ns: OutputPathType
    """Skull stripped version of ANAT"""
    anat_air: OutputPathType
    """Special mask output - air"""
    anat_skl: OutputPathType
    """Special mask output - skull"""
    anat_lsp: OutputPathType
    """Volume used to threshold 'air' out of the volume to be stripped"""

def noisy_skull_strip_params(
    input_file: InputPathType,
    keep_tmp: bool = False,
    v_3dskullstrip_opts: str | None = None,
) -> NoisySkullStrip:
    """
    Build parameters.

    Args:
        input_file: The anatomical dataset
        keep_tmp: Do not erase temporary files at the end.
        v_3dskullstrip_opts: Anything following this option is passed to\
            3dSkullStrip

    Returns:
        Parameter dictionary.
    """
    params: NoisySkullStrip = {
        "@type": "afni/@NoisySkullStrip",
        "input_file": input_file,
        "keep_tmp": keep_tmp,
    }
    if v_3dskullstrip_opts is not None:
        params["3dskullstrip_opts"] = v_3dskullstrip_opts
    return params

def noisy_skull_strip_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NoisySkullStrip."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("keep_tmp", None) is not None:
        if not isinstance(params["keep_tmp"], bool):
            raise StyxValidationError(f'`keep_tmp` has the wrong type: Received `{type(params["keep_tmp"])}` expected `bool`')
    if params.get("3dskullstrip_opts", None) is not None:
        if not isinstance(params["3dskullstrip_opts"], str):
            raise StyxValidationError(f'`3dskullstrip_opts` has the wrong type: Received `{type(params["3dskullstrip_opts"])}` expected `str`')

def noisy_skull_strip_cargs(params: NoisySkullStrip, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@NoisySkullStrip")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_file"]))
    if params.get("keep_tmp"):
        cargs.append("-keep_tmp")
    v_0 = params.get("3dskullstrip_opts")
    if v_0 is not None:
        cargs.append("-3dSkullStrip_opts")
        cargs.append(v_0)
    return cargs

def noisy_skull_strip_outputs(params: NoisySkullStrip, execution: Execution) -> NoisySkullStripOutputs:
    """Build the NoisySkullStripOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    anat_ns_v: OutputPathType = execution.output_file(f'{params["input_file"]}.ns')
    anat_air_v: OutputPathType = execution.output_file(f'{params["input_file"]}.air')
    anat_skl_v: OutputPathType = execution.output_file(f'{params["input_file"]}.skl')
    anat_lsp_v: OutputPathType = execution.output_file(f'{params["input_file"]}.lsp')
    return NoisySkullStripOutputs(
        root=root_v,
        anat_ns=anat_ns_v,
        anat_air=anat_air_v,
        anat_skl=anat_skl_v,
        anat_lsp=anat_lsp_v,
    )

def noisy_skull_strip_execute(params: NoisySkullStrip, runner: Runner | None = None) -> NoisySkullStripOutputs:
    """
    @NoisySkullStrip

    Strips the skull of anatomical datasets with low SNR.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    noisy_skull_strip_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NOISY_SKULL_STRIP_METADATA)
    execution.params(params)
    args = noisy_skull_strip_cargs(params, execution)
    out = noisy_skull_strip_outputs(params, execution)
    execution.run(args)
    return out

def noisy_skull_strip(
    input_file: InputPathType,
    keep_tmp: bool = False,
    v_3dskullstrip_opts: str | None = None,
    runner: Runner | None = None,
) -> NoisySkullStripOutputs:
    """
    @NoisySkullStrip

    Strips the skull of anatomical datasets with low SNR.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: The anatomical dataset
        keep_tmp: Do not erase temporary files at the end.
        v_3dskullstrip_opts: Anything following this option is passed to\
            3dSkullStrip
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = noisy_skull_strip_params(
        input_file=input_file,
        keep_tmp=keep_tmp,
        v_3dskullstrip_opts=v_3dskullstrip_opts,
    )
    return noisy_skull_strip_execute(params, runner)

__all__ = [
    "NoisySkullStrip",
    "NoisySkullStripOutputs",
    "NOISY_SKULL_STRIP_METADATA",
    "noisy_skull_strip_cargs",
    "noisy_skull_strip_outputs",
    "noisy_skull_strip_params",
    "noisy_skull_strip_execute",
    "noisy_skull_strip_validate",
    "noisy_skull_strip",
]