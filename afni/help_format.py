# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HELP_FORMAT_METADATA = Metadata(
    id="help_format",
    name="help_format",
    package="afni",
)

HelpFormat = typing.TypedDict(
    "HelpFormat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/help_format"]],
        "stdin": str,
    },
)

@dataclasses.dataclass
class HelpFormatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    formatted_output: OutputPathType
    """The formatted text with hyperlinks"""

def help_format_params(
    stdin: str,
) -> HelpFormat:
    """
    Build parameters.

    Args:
        stdin: Standard input text to be formatted

    Returns:
        Parameter dictionary.
    """
    params: HelpFormat = {
        "@type": "afni/help_format",
        "stdin": stdin,
    }
    return params

def help_format_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid HelpFormat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stdin", None) is None:
        raise StyxValidationError("`stdin` must not be None")
    if not isinstance(params["stdin"], str):
        raise StyxValidationError(f'`stdin` has the wrong type: Received `{type(params["stdin"])}` expected `str`')

def help_format_cargs(params: HelpFormat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("help_format")
    cargs.append(params["stdin"])
    return cargs

def help_format_outputs(params: HelpFormat, execution: Execution) -> HelpFormatOutputs:
    """Build the HelpFormatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    formatted_output_v: OutputPathType = execution.output_file("formatted_output.html")
    return HelpFormatOutputs(
        root=root_v,
        formatted_output=formatted_output_v,
    )

def help_format_execute(params: HelpFormat, runner: Runner | None = None) -> HelpFormatOutputs:
    """
    help_format

    Formats text by converting URLs into HTML hyperlinks

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    help_format_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HELP_FORMAT_METADATA)
    execution.params(params)
    args = help_format_cargs(params, execution)
    out = help_format_outputs(params, execution)
    execution.run(args)
    return out

def help_format(
    stdin: str,
    runner: Runner | None = None,
) -> HelpFormatOutputs:
    """
    help_format

    Formats text by converting URLs into HTML hyperlinks

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        stdin: Standard input text to be formatted
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = help_format_params(
        stdin=stdin,
    )
    return help_format_execute(params, runner)

__all__ = [
    "HelpFormat",
    "HelpFormatOutputs",
    "HELP_FORMAT_METADATA",
    "help_format_cargs",
    "help_format_outputs",
    "help_format_params",
    "help_format_execute",
    "help_format_validate",
    "help_format",
]