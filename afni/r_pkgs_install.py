# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

R_PKGS_INSTALL_METADATA = Metadata(
    id="rPkgsInstall",
    name="rPkgsInstall",
    package="afni",
)

RPkgsInstall = typing.TypedDict(
    "RPkgsInstall",
    {
        "@type": typing.NotRequired[typing.Literal["afni/rPkgsInstall"]],
        "packages": str,
        "download_site": typing.NotRequired[str],
        "check": typing.NotRequired[bool],
        "update": typing.NotRequired[bool],
        "remove": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RPkgsInstallOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_packages: OutputPathType
    """Output R packages after installation, update, or removal"""

def r_pkgs_install_params(
    packages: str,
    download_site: str | None = None,
    check: bool = False,
    update: bool = False,
    remove: bool = False,
) -> RPkgsInstall:
    """
    Build parameters.

    Args:
        packages: List of R packages to install, update, or remove. Use 'ALL' to\
            refer to all AFNI-required packages.
        download_site: Specify the package repository website. Default is\
            'http://cloud.r-project.org'.
        check: Verify whether the specified R packages are installed on the\
            computer without installing/updating/removing them.
        update: Update the specified R packages. If packages are not installed,\
            they will be installed.
        remove: Remove the specified R packages from the system.

    Returns:
        Parameter dictionary.
    """
    params: RPkgsInstall = {
        "@type": "afni/rPkgsInstall",
        "packages": packages,
        "check": check,
        "update": update,
        "remove": remove,
    }
    if download_site is not None:
        params["download_site"] = download_site
    return params

def r_pkgs_install_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RPkgsInstall."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("packages", None) is None:
        raise StyxValidationError("`packages` must not be None")
    if not isinstance(params["packages"], str):
        raise StyxValidationError(f'`packages` has the wrong type: Received `{type(params["packages"])}` expected `str`')
    if params.get("download_site", None) is not None:
        if not isinstance(params["download_site"], str):
            raise StyxValidationError(f'`download_site` has the wrong type: Received `{type(params["download_site"])}` expected `str`')
    if params.get("check", None) is not None:
        if not isinstance(params["check"], bool):
            raise StyxValidationError(f'`check` has the wrong type: Received `{type(params["check"])}` expected `bool`')
    if params.get("update", None) is not None:
        if not isinstance(params["update"], bool):
            raise StyxValidationError(f'`update` has the wrong type: Received `{type(params["update"])}` expected `bool`')
    if params.get("remove", None) is not None:
        if not isinstance(params["remove"], bool):
            raise StyxValidationError(f'`remove` has the wrong type: Received `{type(params["remove"])}` expected `bool`')

def r_pkgs_install_cargs(params: RPkgsInstall, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("rPkgsInstall")
    cargs.append("-pkgs")
    cargs.append(params["packages"])
    v_0 = params.get("download_site")
    if v_0 is not None:
        cargs.append("-site")
        cargs.append(v_0)
    if params.get("check"):
        cargs.append("-check")
    if params.get("update"):
        cargs.append("-update")
    if params.get("remove"):
        cargs.append("-remove")
    return cargs

def r_pkgs_install_outputs(params: RPkgsInstall, execution: Execution) -> RPkgsInstallOutputs:
    """Build the RPkgsInstallOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_packages_v: OutputPathType = execution.output_file(params["packages"])
    return RPkgsInstallOutputs(
        root=root_v,
        output_packages=output_packages_v,
    )

def r_pkgs_install_execute(params: RPkgsInstall, runner: Runner | None = None) -> RPkgsInstallOutputs:
    """
    rPkgsInstall

    A tool for installing, checking, updating, or removing R packages for AFNI.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    r_pkgs_install_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(R_PKGS_INSTALL_METADATA)
    execution.params(params)
    args = r_pkgs_install_cargs(params, execution)
    out = r_pkgs_install_outputs(params, execution)
    execution.run(args)
    return out

def r_pkgs_install(
    packages: str,
    download_site: str | None = None,
    check: bool = False,
    update: bool = False,
    remove: bool = False,
    runner: Runner | None = None,
) -> RPkgsInstallOutputs:
    """
    rPkgsInstall

    A tool for installing, checking, updating, or removing R packages for AFNI.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        packages: List of R packages to install, update, or remove. Use 'ALL' to\
            refer to all AFNI-required packages.
        download_site: Specify the package repository website. Default is\
            'http://cloud.r-project.org'.
        check: Verify whether the specified R packages are installed on the\
            computer without installing/updating/removing them.
        update: Update the specified R packages. If packages are not installed,\
            they will be installed.
        remove: Remove the specified R packages from the system.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = r_pkgs_install_params(
        packages=packages,
        download_site=download_site,
        check=check,
        update=update,
        remove=remove,
    )
    return r_pkgs_install_execute(params, runner)

__all__ = [
    "RPkgsInstall",
    "RPkgsInstallOutputs",
    "R_PKGS_INSTALL_METADATA",
    "r_pkgs_install_cargs",
    "r_pkgs_install_outputs",
    "r_pkgs_install_params",
    "r_pkgs_install_execute",
    "r_pkgs_install_validate",
    "r_pkgs_install",
]