# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FROM3D_METADATA = Metadata(
    id="from3d",
    name="from3d",
    package="afni",
)

From3d = typing.TypedDict(
    "From3d",
    {
        "@type": typing.NotRequired[typing.Literal["afni/from3d"]],
        "verbose": typing.NotRequired[bool],
        "nsize": typing.NotRequired[bool],
        "raw": typing.NotRequired[bool],
        "float": typing.NotRequired[bool],
        "zfirst": typing.NotRequired[float],
        "zlast": typing.NotRequired[float],
        "tfirst": typing.NotRequired[float],
        "tlast": typing.NotRequired[float],
        "input": InputPathType,
        "prefix": str,
    },
)

@dataclasses.dataclass
class From3dOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def from3d_params(
    input_: InputPathType,
    prefix: str,
    verbose: bool = False,
    nsize: bool = False,
    raw: bool = False,
    float_: bool = False,
    zfirst: float | None = None,
    zlast: float | None = None,
    tfirst: float | None = None,
    tlast: float | None = None,
) -> From3d:
    """
    Build parameters.

    Args:
        input_: Read 3D dataset from file 'fname'
        prefix: Write 2D images using prefix 'rname'
        verbose: Print out verbose information during the run
        nsize: Adjust size of 2D data file to be NxN, by padding with zeros, where\
            N is a power of 2.
        raw: Write images in 'raw' format (just the data bytes). There will be no\
            header information saying what the image dimensions are.
        float_: Write images as floats, no matter what they are in the dataset\
            itself.
        zfirst: Set 'num' = number of first z slice to be extracted (default = 1).
        zlast: Set 'num' = number of last z slice to be extracted (default =\
            largest).
        tfirst: Set 'num' = number of first time slice to be extracted (default =\
            1).
        tlast: Set 'num' = number of last time slice to be extracted (default =\
            largest).

    Returns:
        Parameter dictionary.
    """
    params: From3d = {
        "@type": "afni/from3d",
        "input": input_,
        "prefix": prefix,
        "verbose": verbose,
        "nsize": nsize,
        "raw": raw,
        "float": float_,
    }
    if zfirst is not None:
        params["zfirst"] = zfirst
    if zlast is not None:
        params["zlast"] = zlast
    if tfirst is not None:
        params["tfirst"] = tfirst
    if tlast is not None:
        params["tlast"] = tlast
    return params

def from3d_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid From3d."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("nsize", None) is not None:
        if not isinstance(params["nsize"], bool):
            raise StyxValidationError(f'`nsize` has the wrong type: Received `{type(params["nsize"])}` expected `bool`')
    if params.get("raw", None) is not None:
        if not isinstance(params["raw"], bool):
            raise StyxValidationError(f'`raw` has the wrong type: Received `{type(params["raw"])}` expected `bool`')
    if params.get("float", None) is not None:
        if not isinstance(params["float"], bool):
            raise StyxValidationError(f'`float` has the wrong type: Received `{type(params["float"])}` expected `bool`')
    if params.get("zfirst", None) is not None:
        if not isinstance(params["zfirst"], (float, int)):
            raise StyxValidationError(f'`zfirst` has the wrong type: Received `{type(params["zfirst"])}` expected `float`')
    if params.get("zlast", None) is not None:
        if not isinstance(params["zlast"], (float, int)):
            raise StyxValidationError(f'`zlast` has the wrong type: Received `{type(params["zlast"])}` expected `float`')
    if params.get("tfirst", None) is not None:
        if not isinstance(params["tfirst"], (float, int)):
            raise StyxValidationError(f'`tfirst` has the wrong type: Received `{type(params["tfirst"])}` expected `float`')
    if params.get("tlast", None) is not None:
        if not isinstance(params["tlast"], (float, int)):
            raise StyxValidationError(f'`tlast` has the wrong type: Received `{type(params["tlast"])}` expected `float`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def from3d_cargs(params: From3d, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("from3d")
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("nsize"):
        cargs.append("-nsize")
    if params.get("raw"):
        cargs.append("-raw")
    if params.get("float"):
        cargs.append("-float")
    v_0 = params.get("zfirst")
    if v_0 is not None:
        cargs.append("-zfirst")
        cargs.append(str(v_0))
    v_1 = params.get("zlast")
    if v_1 is not None:
        cargs.append("-zlast")
        cargs.append(str(v_1))
    v_2 = params.get("tfirst")
    if v_2 is not None:
        cargs.append("-tfirst")
        cargs.append(str(v_2))
    v_3 = params.get("tlast")
    if v_3 is not None:
        cargs.append("-tlast")
        cargs.append(str(v_3))
    cargs.append("-input")
    cargs.append(execution.input_file(params["input"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    return cargs

def from3d_outputs(params: From3d, execution: Execution) -> From3dOutputs:
    """Build the From3dOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return From3dOutputs(
        root=root_v,
    )

def from3d_execute(params: From3d, runner: Runner | None = None) -> From3dOutputs:
    """
    from3d

    Extract 2D image files from a 3D AFNI dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    from3d_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FROM3D_METADATA)
    execution.params(params)
    args = from3d_cargs(params, execution)
    out = from3d_outputs(params, execution)
    execution.run(args)
    return out

def from3d(
    input_: InputPathType,
    prefix: str,
    verbose: bool = False,
    nsize: bool = False,
    raw: bool = False,
    float_: bool = False,
    zfirst: float | None = None,
    zlast: float | None = None,
    tfirst: float | None = None,
    tlast: float | None = None,
    runner: Runner | None = None,
) -> From3dOutputs:
    """
    from3d

    Extract 2D image files from a 3D AFNI dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Read 3D dataset from file 'fname'
        prefix: Write 2D images using prefix 'rname'
        verbose: Print out verbose information during the run
        nsize: Adjust size of 2D data file to be NxN, by padding with zeros, where\
            N is a power of 2.
        raw: Write images in 'raw' format (just the data bytes). There will be no\
            header information saying what the image dimensions are.
        float_: Write images as floats, no matter what they are in the dataset\
            itself.
        zfirst: Set 'num' = number of first z slice to be extracted (default = 1).
        zlast: Set 'num' = number of last z slice to be extracted (default =\
            largest).
        tfirst: Set 'num' = number of first time slice to be extracted (default =\
            1).
        tlast: Set 'num' = number of last time slice to be extracted (default =\
            largest).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = from3d_params(
        input_=input_,
        prefix=prefix,
        verbose=verbose,
        nsize=nsize,
        raw=raw,
        float_=float_,
        zfirst=zfirst,
        zlast=zlast,
        tfirst=tfirst,
        tlast=tlast,
    )
    return from3d_execute(params, runner)

__all__ = [
    "From3d",
    "From3dOutputs",
    "FROM3D_METADATA",
    "from3d_cargs",
    "from3d_outputs",
    "from3d_params",
    "from3d_execute",
    "from3d_validate",
    "from3d",
]