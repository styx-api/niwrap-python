# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TOUTCOUNT_METADATA = Metadata(
    id="3dToutcount",
    name="3dToutcount",
    package="afni",
)

V3dToutcount = typing.TypedDict(
    "V3dToutcount",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dToutcount"]],
        "input_dataset": str,
        "output_prefix": typing.NotRequired[str],
        "mask_dataset": typing.NotRequired[str],
        "q_threshold": typing.NotRequired[float],
        "autoclip": typing.NotRequired[bool],
        "automask": typing.NotRequired[bool],
        "fraction": typing.NotRequired[bool],
        "range": typing.NotRequired[bool],
        "polort_order": typing.NotRequired[float],
        "legendre": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dToutcountOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_afni_head: OutputPathType | None
    """Output dataset in AFNI format (HEAD file)."""
    output_afni_brik: OutputPathType | None
    """Output dataset in AFNI format (BRIK file)."""

def v_3d_toutcount_params(
    input_dataset: str,
    output_prefix: str | None = None,
    mask_dataset: str | None = None,
    q_threshold: float | None = None,
    autoclip: bool = False,
    automask: bool = False,
    fraction: bool = False,
    range_: bool = False,
    polort_order: float | None = None,
    legendre: bool = False,
) -> V3dToutcount:
    """
    Build parameters.

    Args:
        input_dataset: Input 3D+time dataset (e.g. dataset+orig)
        output_prefix: Prefix of the new dataset saved with the outlier Q values,\
            applicable with the -save option
        mask_dataset: Only count voxels in the provided mask dataset
        q_threshold: Use 'q' instead of 0.001 in the calculation of alpha. Must be\
            within range 0 < q < 1.
        autoclip: Clip off 'small' voxels (as in 3dClipLevel). Cannot use with\
            -mask.
        automask: Automatically mask the dataset. Cannot use with -mask.
        fraction: Output the fraction of (masked) voxels which are outliers at each\
            time point, instead of the count.
        range_: Print out median+3.5*MAD of outlier count with each time point.
        polort_order: Detrend each voxel time series with polynomials of order\
            'nn'. Default value is 0, which removes the median.
        legendre: Use Legendre polynomials for detrending (also allows -polort >\
            3).

    Returns:
        Parameter dictionary.
    """
    params: V3dToutcount = {
        "@type": "afni/3dToutcount",
        "input_dataset": input_dataset,
        "autoclip": autoclip,
        "automask": automask,
        "fraction": fraction,
        "range": range_,
        "legendre": legendre,
    }
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    if mask_dataset is not None:
        params["mask_dataset"] = mask_dataset
    if q_threshold is not None:
        params["q_threshold"] = q_threshold
    if polort_order is not None:
        params["polort_order"] = polort_order
    return params

def v_3d_toutcount_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dToutcount."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], str):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `str`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("mask_dataset", None) is not None:
        if not isinstance(params["mask_dataset"], str):
            raise StyxValidationError(f'`mask_dataset` has the wrong type: Received `{type(params["mask_dataset"])}` expected `str`')
    if params.get("q_threshold", None) is not None:
        if not isinstance(params["q_threshold"], (float, int)):
            raise StyxValidationError(f'`q_threshold` has the wrong type: Received `{type(params["q_threshold"])}` expected `float`')
        if not (0 <= params["q_threshold"] <= 1):
            raise StyxValidationError("Parameter `q_threshold` must be between 0 and 1 (inclusive)")
    if params.get("autoclip", None) is not None:
        if not isinstance(params["autoclip"], bool):
            raise StyxValidationError(f'`autoclip` has the wrong type: Received `{type(params["autoclip"])}` expected `bool`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("fraction", None) is not None:
        if not isinstance(params["fraction"], bool):
            raise StyxValidationError(f'`fraction` has the wrong type: Received `{type(params["fraction"])}` expected `bool`')
    if params.get("range", None) is not None:
        if not isinstance(params["range"], bool):
            raise StyxValidationError(f'`range` has the wrong type: Received `{type(params["range"])}` expected `bool`')
    if params.get("polort_order", None) is not None:
        if not isinstance(params["polort_order"], (float, int)):
            raise StyxValidationError(f'`polort_order` has the wrong type: Received `{type(params["polort_order"])}` expected `float`')
    if params.get("legendre", None) is not None:
        if not isinstance(params["legendre"], bool):
            raise StyxValidationError(f'`legendre` has the wrong type: Received `{type(params["legendre"])}` expected `bool`')

def v_3d_toutcount_cargs(params: V3dToutcount, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dToutcount")
    cargs.append(params["input_dataset"])
    v_0 = params.get("output_prefix")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("mask_dataset")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(v_1)
    v_2 = params.get("q_threshold")
    if v_2 is not None:
        cargs.append("-qthr")
        cargs.append(str(v_2))
    if params.get("autoclip"):
        cargs.append("-autoclip")
    if params.get("automask"):
        cargs.append("-automask")
    if params.get("fraction"):
        cargs.append("-fraction")
    if params.get("range"):
        cargs.append("-range")
    v_3 = params.get("polort_order")
    if v_3 is not None:
        cargs.append("-polort")
        cargs.append(str(v_3))
    if params.get("legendre"):
        cargs.append("-legendre")
    return cargs

def v_3d_toutcount_outputs(params: V3dToutcount, execution: Execution) -> V3dToutcountOutputs:
    """Build the V3dToutcountOutputs object for this tool."""
    output_afni_head_v: OutputPathType | None = None
    output_afni_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_prefix")
    if __v0 is not None:
        output_afni_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.HEAD')
    __v1 = params.get("output_prefix")
    if __v1 is not None:
        output_afni_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.BRIK')
    return V3dToutcountOutputs(
        root=root_v,
        output_afni_head=output_afni_head_v,
        output_afni_brik=output_afni_brik_v,
    )

def v_3d_toutcount_execute(params: V3dToutcount, runner: Runner | None = None) -> V3dToutcountOutputs:
    """
    3dToutcount

    Calculates the number of 'outliers' in a 3D+time dataset at each time point.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_toutcount_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TOUTCOUNT_METADATA)
    execution.params(params)
    args = v_3d_toutcount_cargs(params, execution)
    out = v_3d_toutcount_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_toutcount(
    input_dataset: str,
    output_prefix: str | None = None,
    mask_dataset: str | None = None,
    q_threshold: float | None = None,
    autoclip: bool = False,
    automask: bool = False,
    fraction: bool = False,
    range_: bool = False,
    polort_order: float | None = None,
    legendre: bool = False,
    runner: Runner | None = None,
) -> V3dToutcountOutputs:
    """
    3dToutcount

    Calculates the number of 'outliers' in a 3D+time dataset at each time point.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input 3D+time dataset (e.g. dataset+orig)
        output_prefix: Prefix of the new dataset saved with the outlier Q values,\
            applicable with the -save option
        mask_dataset: Only count voxels in the provided mask dataset
        q_threshold: Use 'q' instead of 0.001 in the calculation of alpha. Must be\
            within range 0 < q < 1.
        autoclip: Clip off 'small' voxels (as in 3dClipLevel). Cannot use with\
            -mask.
        automask: Automatically mask the dataset. Cannot use with -mask.
        fraction: Output the fraction of (masked) voxels which are outliers at each\
            time point, instead of the count.
        range_: Print out median+3.5*MAD of outlier count with each time point.
        polort_order: Detrend each voxel time series with polynomials of order\
            'nn'. Default value is 0, which removes the median.
        legendre: Use Legendre polynomials for detrending (also allows -polort >\
            3).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_toutcount_params(
        input_dataset=input_dataset,
        output_prefix=output_prefix,
        mask_dataset=mask_dataset,
        q_threshold=q_threshold,
        autoclip=autoclip,
        automask=automask,
        fraction=fraction,
        range_=range_,
        polort_order=polort_order,
        legendre=legendre,
    )
    return v_3d_toutcount_execute(params, runner)

__all__ = [
    "V3dToutcount",
    "V3dToutcountOutputs",
    "V_3D_TOUTCOUNT_METADATA",
    "v_3d_toutcount_cargs",
    "v_3d_toutcount_outputs",
    "v_3d_toutcount_params",
    "v_3d_toutcount_execute",
    "v_3d_toutcount_validate",
    "v_3d_toutcount",
]