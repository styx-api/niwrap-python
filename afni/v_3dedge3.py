# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DEDGE3_METADATA = Metadata(
    id="3dedge3",
    name="3dedge3",
    package="afni",
)

V3dedge3 = typing.TypedDict(
    "V3dedge3",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dedge3"]],
        "input_file": InputPathType,
        "verbose": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "datum": typing.NotRequired[str],
        "fscale": typing.NotRequired[bool],
        "gscale": typing.NotRequired[bool],
        "nscale": typing.NotRequired[bool],
        "scale_floats": typing.NotRequired[float],
        "automask": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dedge3Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output dataset"""

def v_3dedge3_params(
    input_file: InputPathType,
    verbose: bool = False,
    prefix: str | None = None,
    datum: str | None = None,
    fscale: bool = False,
    gscale: bool = False,
    nscale: bool = False,
    scale_floats: float | None = None,
    automask: bool = False,
) -> V3dedge3:
    """
    Build parameters.

    Args:
        input_file: Input dataset
        verbose: Print out some information along the way
        prefix: Sets the prefix of the output dataset
        datum: Sets the datum of the output dataset
        fscale: Force scaling of the output to the maximum integer range
        gscale: Same as '-fscale', but also forces each output sub-brick to get the\
            same scaling factor
        nscale: Don't do any scaling on output to byte or short datasets
        scale_floats: Multiply input by VAL, but only if the input datum is float.
        automask: For automatic internal calculation of a mask in the usual AFNI\
            way

    Returns:
        Parameter dictionary.
    """
    params: V3dedge3 = {
        "@type": "afni/3dedge3",
        "input_file": input_file,
        "verbose": verbose,
        "fscale": fscale,
        "gscale": gscale,
        "nscale": nscale,
        "automask": automask,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if datum is not None:
        params["datum"] = datum
    if scale_floats is not None:
        params["scale_floats"] = scale_floats
    return params

def v_3dedge3_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dedge3."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("datum", None) is not None:
        if not isinstance(params["datum"], str):
            raise StyxValidationError(f'`datum` has the wrong type: Received `{type(params["datum"])}` expected `str`')
    if params.get("fscale", None) is not None:
        if not isinstance(params["fscale"], bool):
            raise StyxValidationError(f'`fscale` has the wrong type: Received `{type(params["fscale"])}` expected `bool`')
    if params.get("gscale", None) is not None:
        if not isinstance(params["gscale"], bool):
            raise StyxValidationError(f'`gscale` has the wrong type: Received `{type(params["gscale"])}` expected `bool`')
    if params.get("nscale", None) is not None:
        if not isinstance(params["nscale"], bool):
            raise StyxValidationError(f'`nscale` has the wrong type: Received `{type(params["nscale"])}` expected `bool`')
    if params.get("scale_floats", None) is not None:
        if not isinstance(params["scale_floats"], (float, int)):
            raise StyxValidationError(f'`scale_floats` has the wrong type: Received `{type(params["scale_floats"])}` expected `float`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')

def v_3dedge3_cargs(params: V3dedge3, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dedge3")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_file"]))
    if params.get("verbose"):
        cargs.append("-verbose")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("datum")
    if v_1 is not None:
        cargs.append("-datum")
        cargs.append(v_1)
    if params.get("fscale"):
        cargs.append("-fscale")
    if params.get("gscale"):
        cargs.append("-gscale")
    if params.get("nscale"):
        cargs.append("-nscale")
    v_2 = params.get("scale_floats")
    if v_2 is not None:
        cargs.append("-scale_floats")
        cargs.append(str(v_2))
    if params.get("automask"):
        cargs.append("-automask")
    return cargs

def v_3dedge3_outputs(params: V3dedge3, execution: Execution) -> V3dedge3Outputs:
    """Build the V3dedge3Outputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return V3dedge3Outputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3dedge3_execute(params: V3dedge3, runner: Runner | None = None) -> V3dedge3Outputs:
    """
    3dedge3

    Does 3D Edge detection using the library 3DEdge

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dedge3_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DEDGE3_METADATA)
    execution.params(params)
    args = v_3dedge3_cargs(params, execution)
    out = v_3dedge3_outputs(params, execution)
    execution.run(args)
    return out

def v_3dedge3(
    input_file: InputPathType,
    verbose: bool = False,
    prefix: str | None = None,
    datum: str | None = None,
    fscale: bool = False,
    gscale: bool = False,
    nscale: bool = False,
    scale_floats: float | None = None,
    automask: bool = False,
    runner: Runner | None = None,
) -> V3dedge3Outputs:
    """
    3dedge3

    Does 3D Edge detection using the library 3DEdge

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Input dataset
        verbose: Print out some information along the way
        prefix: Sets the prefix of the output dataset
        datum: Sets the datum of the output dataset
        fscale: Force scaling of the output to the maximum integer range
        gscale: Same as '-fscale', but also forces each output sub-brick to get the\
            same scaling factor
        nscale: Don't do any scaling on output to byte or short datasets
        scale_floats: Multiply input by VAL, but only if the input datum is float.
        automask: For automatic internal calculation of a mask in the usual AFNI\
            way
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dedge3_params(
        input_file=input_file,
        verbose=verbose,
        prefix=prefix,
        datum=datum,
        fscale=fscale,
        gscale=gscale,
        nscale=nscale,
        scale_floats=scale_floats,
        automask=automask,
    )
    return v_3dedge3_execute(params, runner)

__all__ = [
    "V3dedge3",
    "V3dedge3Outputs",
    "V_3DEDGE3_METADATA",
    "v_3dedge3_cargs",
    "v_3dedge3_outputs",
    "v_3dedge3_params",
    "v_3dedge3_execute",
    "v_3dedge3_validate",
    "v_3dedge3",
]