# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ROI_CORR_MAT_METADATA = Metadata(
    id="@ROI_Corr_Mat",
    name="@ROI_Corr_Mat",
    package="afni",
)

RoiCorrMat = typing.TypedDict(
    "RoiCorrMat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ROI_Corr_Mat"]],
        "ts_vol": InputPathType,
        "roi_vol": InputPathType,
        "prefix": str,
        "roisel": typing.NotRequired[InputPathType],
        "zval": typing.NotRequired[bool],
        "mat_opt": typing.NotRequired[str],
        "dirty": typing.NotRequired[bool],
        "keep_tmp": typing.NotRequired[bool],
        "echo": typing.NotRequired[bool],
        "verb": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RoiCorrMatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    matrix_1d: OutputPathType
    """Correlation matrix in .1D format"""
    matrix_brick: OutputPathType
    """Correlation matrix in .BRIK format"""

def roi_corr_mat_params(
    ts_vol: InputPathType,
    roi_vol: InputPathType,
    prefix: str,
    roisel: InputPathType | None = None,
    zval: bool = False,
    mat_opt: str | None = None,
    dirty: bool = False,
    keep_tmp: bool = False,
    echo: bool = False,
    verb: bool = False,
) -> RoiCorrMat:
    """
    Build parameters.

    Args:
        ts_vol: Time series volume
        roi_vol: ROI volume
        prefix: Use output for a prefix
        roisel: Force processing of ROI label (integers) listed in ROISEL 1D file
        zval: Output a zscore version of the correlation matrix
        mat_opt: Output matrix in different manners
        dirty: Keep temporary files
        keep_tmp: Keep temporary files
        echo: Set echo (echo all commands to screen)
        verb: Verbose flag

    Returns:
        Parameter dictionary.
    """
    params: RoiCorrMat = {
        "@type": "afni/@ROI_Corr_Mat",
        "ts_vol": ts_vol,
        "roi_vol": roi_vol,
        "prefix": prefix,
        "zval": zval,
        "dirty": dirty,
        "keep_tmp": keep_tmp,
        "echo": echo,
        "verb": verb,
    }
    if roisel is not None:
        params["roisel"] = roisel
    if mat_opt is not None:
        params["mat_opt"] = mat_opt
    return params

def roi_corr_mat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RoiCorrMat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ts_vol", None) is None:
        raise StyxValidationError("`ts_vol` must not be None")
    if not isinstance(params["ts_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ts_vol` has the wrong type: Received `{type(params["ts_vol"])}` expected `InputPathType`')
    if params.get("roi_vol", None) is None:
        raise StyxValidationError("`roi_vol` must not be None")
    if not isinstance(params["roi_vol"], (pathlib.Path, str)):
        raise StyxValidationError(f'`roi_vol` has the wrong type: Received `{type(params["roi_vol"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("roisel", None) is not None:
        if not isinstance(params["roisel"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roisel` has the wrong type: Received `{type(params["roisel"])}` expected `InputPathType`')
    if params.get("zval", None) is not None:
        if not isinstance(params["zval"], bool):
            raise StyxValidationError(f'`zval` has the wrong type: Received `{type(params["zval"])}` expected `bool`')
    if params.get("mat_opt", None) is not None:
        if not isinstance(params["mat_opt"], str):
            raise StyxValidationError(f'`mat_opt` has the wrong type: Received `{type(params["mat_opt"])}` expected `str`')
    if params.get("dirty", None) is not None:
        if not isinstance(params["dirty"], bool):
            raise StyxValidationError(f'`dirty` has the wrong type: Received `{type(params["dirty"])}` expected `bool`')
    if params.get("keep_tmp", None) is not None:
        if not isinstance(params["keep_tmp"], bool):
            raise StyxValidationError(f'`keep_tmp` has the wrong type: Received `{type(params["keep_tmp"])}` expected `bool`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("verb", None) is not None:
        if not isinstance(params["verb"], bool):
            raise StyxValidationError(f'`verb` has the wrong type: Received `{type(params["verb"])}` expected `bool`')

def roi_corr_mat_cargs(params: RoiCorrMat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ROI_Corr_Mat")
    cargs.append("-ts")
    cargs.append(execution.input_file(params["ts_vol"]))
    cargs.append("-roi")
    cargs.append(execution.input_file(params["roi_vol"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("roisel")
    if v_0 is not None:
        cargs.append("-roisel")
        cargs.append(execution.input_file(v_0))
    if params.get("zval"):
        cargs.append("-zval")
    v_1 = params.get("mat_opt")
    if v_1 is not None:
        cargs.append("-mat")
        cargs.append(v_1)
    if params.get("dirty"):
        cargs.append("-dirty")
    if params.get("keep_tmp"):
        cargs.append("-keep_tmp")
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("verb"):
        cargs.append("-verb")
    return cargs

def roi_corr_mat_outputs(params: RoiCorrMat, execution: Execution) -> RoiCorrMatOutputs:
    """Build the RoiCorrMatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    matrix_1d_v: OutputPathType = execution.output_file(f'{params["prefix"]}_matrix.1D')
    matrix_brick_v: OutputPathType = execution.output_file(f'{params["prefix"]}_matrix.BRIK')
    return RoiCorrMatOutputs(
        root=root_v,
        matrix_1d=matrix_1d_v,
        matrix_brick=matrix_brick_v,
    )

def roi_corr_mat_execute(params: RoiCorrMat, runner: Runner | None = None) -> RoiCorrMatOutputs:
    """
    @ROI_Corr_Mat

    Script to produce an NxN ROI correlation matrix of N ROIs

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    roi_corr_mat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ROI_CORR_MAT_METADATA)
    execution.params(params)
    args = roi_corr_mat_cargs(params, execution)
    out = roi_corr_mat_outputs(params, execution)
    execution.run(args)
    return out

def roi_corr_mat(
    ts_vol: InputPathType,
    roi_vol: InputPathType,
    prefix: str,
    roisel: InputPathType | None = None,
    zval: bool = False,
    mat_opt: str | None = None,
    dirty: bool = False,
    keep_tmp: bool = False,
    echo: bool = False,
    verb: bool = False,
    runner: Runner | None = None,
) -> RoiCorrMatOutputs:
    """
    @ROI_Corr_Mat

    Script to produce an NxN ROI correlation matrix of N ROIs

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        ts_vol: Time series volume
        roi_vol: ROI volume
        prefix: Use output for a prefix
        roisel: Force processing of ROI label (integers) listed in ROISEL 1D file
        zval: Output a zscore version of the correlation matrix
        mat_opt: Output matrix in different manners
        dirty: Keep temporary files
        keep_tmp: Keep temporary files
        echo: Set echo (echo all commands to screen)
        verb: Verbose flag
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = roi_corr_mat_params(
        ts_vol=ts_vol,
        roi_vol=roi_vol,
        prefix=prefix,
        roisel=roisel,
        zval=zval,
        mat_opt=mat_opt,
        dirty=dirty,
        keep_tmp=keep_tmp,
        echo=echo,
        verb=verb,
    )
    return roi_corr_mat_execute(params, runner)

__all__ = [
    "RoiCorrMat",
    "RoiCorrMatOutputs",
    "ROI_CORR_MAT_METADATA",
    "roi_corr_mat_cargs",
    "roi_corr_mat_outputs",
    "roi_corr_mat_params",
    "roi_corr_mat_execute",
    "roi_corr_mat_validate",
    "roi_corr_mat",
]