# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAGES_EQUAL_METADATA = Metadata(
    id="images_equal",
    name="images_equal",
    package="afni",
)

ImagesEqual = typing.TypedDict(
    "ImagesEqual",
    {
        "@type": typing.NotRequired[typing.Literal["afni/images_equal"]],
        "file_a": InputPathType,
        "file_b": InputPathType,
        "all_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ImagesEqualOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    comparison_result: OutputPathType
    """Result of the image comparison: 1 if equal, 0 if not."""

def images_equal_params(
    file_a: InputPathType,
    file_b: InputPathType,
    all_flag: bool = False,
) -> ImagesEqual:
    """
    Build parameters.

    Args:
        file_a: First image file to compare.
        file_b: Second image file to compare.
        all_flag: Compare all images in the files; all must be equal for exit\
            status to be 1.

    Returns:
        Parameter dictionary.
    """
    params: ImagesEqual = {
        "@type": "afni/images_equal",
        "file_a": file_a,
        "file_b": file_b,
        "all_flag": all_flag,
    }
    return params

def images_equal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ImagesEqual."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("file_a", None) is None:
        raise StyxValidationError("`file_a` must not be None")
    if not isinstance(params["file_a"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file_a` has the wrong type: Received `{type(params["file_a"])}` expected `InputPathType`')
    if params.get("file_b", None) is None:
        raise StyxValidationError("`file_b` must not be None")
    if not isinstance(params["file_b"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file_b` has the wrong type: Received `{type(params["file_b"])}` expected `InputPathType`')
    if params.get("all_flag", None) is not None:
        if not isinstance(params["all_flag"], bool):
            raise StyxValidationError(f'`all_flag` has the wrong type: Received `{type(params["all_flag"])}` expected `bool`')

def images_equal_cargs(params: ImagesEqual, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("images_equal")
    cargs.append(execution.input_file(params["file_a"]))
    cargs.append(execution.input_file(params["file_b"]))
    if params.get("all_flag"):
        cargs.append("-all")
    return cargs

def images_equal_outputs(params: ImagesEqual, execution: Execution) -> ImagesEqualOutputs:
    """Build the ImagesEqualOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    comparison_result_v: OutputPathType = execution.output_file("comparison_result.txt")
    return ImagesEqualOutputs(
        root=root_v,
        comparison_result=comparison_result_v,
    )

def images_equal_execute(params: ImagesEqual, runner: Runner | None = None) -> ImagesEqualOutputs:
    """
    images_equal

    A simple program to test if two 2D images are identical.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    images_equal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAGES_EQUAL_METADATA)
    execution.params(params)
    args = images_equal_cargs(params, execution)
    out = images_equal_outputs(params, execution)
    execution.run(args)
    return out

def images_equal(
    file_a: InputPathType,
    file_b: InputPathType,
    all_flag: bool = False,
    runner: Runner | None = None,
) -> ImagesEqualOutputs:
    """
    images_equal

    A simple program to test if two 2D images are identical.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        file_a: First image file to compare.
        file_b: Second image file to compare.
        all_flag: Compare all images in the files; all must be equal for exit\
            status to be 1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = images_equal_params(
        file_a=file_a,
        file_b=file_b,
        all_flag=all_flag,
    )
    return images_equal_execute(params, runner)

__all__ = [
    "ImagesEqual",
    "ImagesEqualOutputs",
    "IMAGES_EQUAL_METADATA",
    "images_equal_cargs",
    "images_equal_outputs",
    "images_equal_params",
    "images_equal_execute",
    "images_equal_validate",
    "images_equal",
]