# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ACOST_METADATA = Metadata(
    id="3dAcost",
    name="3dAcost",
    package="afni",
)

V3dAcost = typing.TypedDict(
    "V3dAcost",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dAcost"]],
        "infile": InputPathType,
        "basefile": InputPathType,
        "outfile": str,
        "all_cost": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dAcostOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_head: OutputPathType
    """Output aligned dataset (HEAD file)"""
    output_brik: OutputPathType
    """Output aligned dataset (BRIK file)"""

def v_3d_acost_params(
    infile: InputPathType,
    basefile: InputPathType,
    outfile: str,
    all_cost: bool = False,
) -> V3dAcost:
    """
    Build parameters.

    Args:
        infile: Input dataset for allineation
        basefile: Base dataset for allineation
        outfile: Prefix for the output dataset
        all_cost: Prints all alignment cost metrics

    Returns:
        Parameter dictionary.
    """
    params: V3dAcost = {
        "@type": "afni/3dAcost",
        "infile": infile,
        "basefile": basefile,
        "outfile": outfile,
        "all_cost": all_cost,
    }
    return params

def v_3d_acost_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dAcost."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("basefile", None) is None:
        raise StyxValidationError("`basefile` must not be None")
    if not isinstance(params["basefile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`basefile` has the wrong type: Received `{type(params["basefile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("all_cost", None) is not None:
        if not isinstance(params["all_cost"], bool):
            raise StyxValidationError(f'`all_cost` has the wrong type: Received `{type(params["all_cost"])}` expected `bool`')

def v_3d_acost_cargs(params: V3dAcost, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dAcost")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-base")
    cargs.append(execution.input_file(params["basefile"]))
    cargs.append("-prefix")
    cargs.append(params["outfile"])
    if params.get("all_cost"):
        cargs.append("-allcostX")
    return cargs

def v_3d_acost_outputs(params: V3dAcost, execution: Execution) -> V3dAcostOutputs:
    """Build the V3dAcostOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_head_v: OutputPathType = execution.output_file(f'{params["outfile"]}+orig.HEAD')
    output_brik_v: OutputPathType = execution.output_file(f'{params["outfile"]}+orig.BRIK')
    return V3dAcostOutputs(
        root=root_v,
        output_head=output_head_v,
        output_brik=output_brik_v,
    )

def v_3d_acost_execute(params: V3dAcost, runner: Runner | None = None) -> V3dAcostOutputs:
    """
    3dAcost

    Allineate dataset to a base dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_acost_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ACOST_METADATA)
    execution.params(params)
    args = v_3d_acost_cargs(params, execution)
    out = v_3d_acost_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_acost(
    infile: InputPathType,
    basefile: InputPathType,
    outfile: str,
    all_cost: bool = False,
    runner: Runner | None = None,
) -> V3dAcostOutputs:
    """
    3dAcost

    Allineate dataset to a base dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input dataset for allineation
        basefile: Base dataset for allineation
        outfile: Prefix for the output dataset
        all_cost: Prints all alignment cost metrics
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_acost_params(
        infile=infile,
        basefile=basefile,
        outfile=outfile,
        all_cost=all_cost,
    )
    return v_3d_acost_execute(params, runner)

__all__ = [
    "V3dAcost",
    "V3dAcostOutputs",
    "V_3D_ACOST_METADATA",
    "v_3d_acost_cargs",
    "v_3d_acost_outputs",
    "v_3d_acost_params",
    "v_3d_acost_execute",
    "v_3d_acost_validate",
    "v_3d_acost",
]