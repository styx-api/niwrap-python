# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_2PERM_METADATA = Metadata(
    id="2perm",
    name="2perm",
    package="afni",
)

V2perm = typing.TypedDict(
    "V2perm",
    {
        "@type": typing.NotRequired[typing.Literal["afni/2perm"]],
        "prefix": typing.NotRequired[str],
        "comma": typing.NotRequired[bool],
        "bottom_int": float,
        "top_int": float,
        "subset1_size": typing.NotRequired[float],
        "subset2_size": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V2permOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    file_A: OutputPathType | None
    """First subset output file"""
    file_B: OutputPathType | None
    """Second subset output file"""

def v_2perm_params(
    bottom_int: float,
    top_int: float,
    prefix: str | None = None,
    comma: bool = False,
    subset1_size: float | None = None,
    subset2_size: float | None = None,
) -> V2perm:
    """
    Build parameters.

    Args:
        bottom_int: Bottom integer of the range
        top_int: Top integer of the range
        prefix: Prefix for output files (default 'AFNIroolz')
        comma: Write each file as a single row of comma-separated numbers
        subset1_size: Size of the first subset (optional, default is half the\
            range)
        subset2_size: Size of the second subset (optional, default is half the\
            range)

    Returns:
        Parameter dictionary.
    """
    params: V2perm = {
        "@type": "afni/2perm",
        "bottom_int": bottom_int,
        "top_int": top_int,
        "comma": comma,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if subset1_size is not None:
        params["subset1_size"] = subset1_size
    if subset2_size is not None:
        params["subset2_size"] = subset2_size
    return params

def v_2perm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V2perm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("comma", None) is not None:
        if not isinstance(params["comma"], bool):
            raise StyxValidationError(f'`comma` has the wrong type: Received `{type(params["comma"])}` expected `bool`')
    if params.get("bottom_int", None) is None:
        raise StyxValidationError("`bottom_int` must not be None")
    if not isinstance(params["bottom_int"], (float, int)):
        raise StyxValidationError(f'`bottom_int` has the wrong type: Received `{type(params["bottom_int"])}` expected `float`')
    if params.get("top_int", None) is None:
        raise StyxValidationError("`top_int` must not be None")
    if not isinstance(params["top_int"], (float, int)):
        raise StyxValidationError(f'`top_int` has the wrong type: Received `{type(params["top_int"])}` expected `float`')
    if params.get("subset1_size", None) is not None:
        if not isinstance(params["subset1_size"], (float, int)):
            raise StyxValidationError(f'`subset1_size` has the wrong type: Received `{type(params["subset1_size"])}` expected `float`')
    if params.get("subset2_size", None) is not None:
        if not isinstance(params["subset2_size"], (float, int)):
            raise StyxValidationError(f'`subset2_size` has the wrong type: Received `{type(params["subset2_size"])}` expected `float`')

def v_2perm_cargs(params: V2perm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("2perm")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("comma"):
        cargs.append("-comma")
    cargs.append(str(params["bottom_int"]))
    cargs.append(str(params["top_int"]))
    v_1 = params.get("subset1_size")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("subset2_size")
    if v_2 is not None:
        cargs.append(str(v_2))
    return cargs

def v_2perm_outputs(params: V2perm, execution: Execution) -> V2permOutputs:
    """Build the V2permOutputs object for this tool."""
    file_A_v: OutputPathType | None = None
    file_B_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        file_A_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_A')
    __v1 = params.get("prefix")
    if __v1 is not None:
        file_B_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}_B')
    return V2permOutputs(
        root=root_v,
        file_A=file_A_v,
        file_B=file_B_v,
    )

def v_2perm_execute(params: V2perm, runner: Runner | None = None) -> V2permOutputs:
    """
    2perm

    Generates two random non-overlapping subsets of a given set of integers.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_2perm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_2PERM_METADATA)
    execution.params(params)
    args = v_2perm_cargs(params, execution)
    out = v_2perm_outputs(params, execution)
    execution.run(args)
    return out

def v_2perm(
    bottom_int: float,
    top_int: float,
    prefix: str | None = None,
    comma: bool = False,
    subset1_size: float | None = None,
    subset2_size: float | None = None,
    runner: Runner | None = None,
) -> V2permOutputs:
    """
    2perm

    Generates two random non-overlapping subsets of a given set of integers.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        bottom_int: Bottom integer of the range
        top_int: Top integer of the range
        prefix: Prefix for output files (default 'AFNIroolz')
        comma: Write each file as a single row of comma-separated numbers
        subset1_size: Size of the first subset (optional, default is half the\
            range)
        subset2_size: Size of the second subset (optional, default is half the\
            range)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_2perm_params(
        bottom_int=bottom_int,
        top_int=top_int,
        prefix=prefix,
        comma=comma,
        subset1_size=subset1_size,
        subset2_size=subset2_size,
    )
    return v_2perm_execute(params, runner)

__all__ = [
    "V2perm",
    "V2permOutputs",
    "V_2PERM_METADATA",
    "v_2perm_cargs",
    "v_2perm_outputs",
    "v_2perm_params",
    "v_2perm_execute",
    "v_2perm_validate",
    "v_2perm",
]