# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PROMPT_POPUP_METADATA = Metadata(
    id="prompt_popup",
    name="prompt_popup",
    package="afni",
)

PromptPopup = typing.TypedDict(
    "PromptPopup",
    {
        "@type": typing.NotRequired[typing.Literal["afni/prompt_popup"]],
        "message": str,
        "message_pause": typing.NotRequired[str],
        "buttons": typing.NotRequired[list[str]],
        "buttons_b": typing.NotRequired[list[str]],
        "timeout": typing.NotRequired[float],
        "timeout_to": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class PromptPopupOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def prompt_popup_params(
    message: str,
    message_pause: str | None = None,
    buttons: list[str] | None = None,
    buttons_b: list[str] | None = None,
    timeout: float | None = None,
    timeout_to: float | None = None,
) -> PromptPopup:
    """
    Build parameters.

    Args:
        message: Pops a window prompting the user with MESSAGE. If MESSAGE is '-',\
            it is read from stdin.
        message_pause: Same as -message to match the old prompt_user.
        buttons: What do you want the buttons to say? You can give up to three\
            -button for three buttons. Returns integer 1, 2, or 3. If there is no\
            -button, there will be one button 'Ok'.
        buttons_b: Same as -button.
        timeout: Timeout in seconds of prompt message. Default answer is returned\
            if TT seconds elapse without user input.
        timeout_to: Same as -timeout TT.

    Returns:
        Parameter dictionary.
    """
    params: PromptPopup = {
        "@type": "afni/prompt_popup",
        "message": message,
    }
    if message_pause is not None:
        params["message_pause"] = message_pause
    if buttons is not None:
        params["buttons"] = buttons
    if buttons_b is not None:
        params["buttons_b"] = buttons_b
    if timeout is not None:
        params["timeout"] = timeout
    if timeout_to is not None:
        params["timeout_to"] = timeout_to
    return params

def prompt_popup_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PromptPopup."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("message", None) is None:
        raise StyxValidationError("`message` must not be None")
    if not isinstance(params["message"], str):
        raise StyxValidationError(f'`message` has the wrong type: Received `{type(params["message"])}` expected `str`')
    if params.get("message_pause", None) is not None:
        if not isinstance(params["message_pause"], str):
            raise StyxValidationError(f'`message_pause` has the wrong type: Received `{type(params["message_pause"])}` expected `str`')
    if params.get("buttons", None) is not None:
        if not isinstance(params["buttons"], list):
            raise StyxValidationError(f'`buttons` has the wrong type: Received `{type(params["buttons"])}` expected `list[str]`')
        for e in params["buttons"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`buttons` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("buttons_b", None) is not None:
        if not isinstance(params["buttons_b"], list):
            raise StyxValidationError(f'`buttons_b` has the wrong type: Received `{type(params["buttons_b"])}` expected `list[str]`')
        for e_2 in params["buttons_b"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`buttons_b` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("timeout", None) is not None:
        if not isinstance(params["timeout"], (float, int)):
            raise StyxValidationError(f'`timeout` has the wrong type: Received `{type(params["timeout"])}` expected `float`')
    if params.get("timeout_to", None) is not None:
        if not isinstance(params["timeout_to"], (float, int)):
            raise StyxValidationError(f'`timeout_to` has the wrong type: Received `{type(params["timeout_to"])}` expected `float`')

def prompt_popup_cargs(params: PromptPopup, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("prompt_popup")
    cargs.append("-message")
    cargs.append(params["message"])
    v_0 = params.get("message_pause")
    if v_0 is not None:
        cargs.append("-pause")
        cargs.append(v_0)
    v_1 = params.get("buttons")
    if v_1 is not None:
        cargs.append("-button")
        for item0 in v_1:
            cargs.append(item0)
    v_2 = params.get("buttons_b")
    if v_2 is not None:
        cargs.append("-b")
        for item1 in v_2:
            cargs.append(item1)
    v_3 = params.get("timeout")
    if v_3 is not None:
        cargs.append("-timeout")
        cargs.append(str(v_3))
    v_4 = params.get("timeout_to")
    if v_4 is not None:
        cargs.append("-to")
        cargs.append(str(v_4))
    return cargs

def prompt_popup_outputs(params: PromptPopup, execution: Execution) -> PromptPopupOutputs:
    """Build the PromptPopupOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PromptPopupOutputs(
        root=root_v,
    )

def prompt_popup_execute(params: PromptPopup, runner: Runner | None = None) -> PromptPopupOutputs:
    """
    prompt_popup

    A command-line tool that pops up a window prompting user interaction with a message and buttons.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    prompt_popup_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PROMPT_POPUP_METADATA)
    execution.params(params)
    args = prompt_popup_cargs(params, execution)
    out = prompt_popup_outputs(params, execution)
    execution.run(args)
    return out

def prompt_popup(
    message: str,
    message_pause: str | None = None,
    buttons: list[str] | None = None,
    buttons_b: list[str] | None = None,
    timeout: float | None = None,
    timeout_to: float | None = None,
    runner: Runner | None = None,
) -> PromptPopupOutputs:
    """
    prompt_popup

    A command-line tool that pops up a window prompting user interaction with a message and buttons.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        message: Pops a window prompting the user with MESSAGE. If MESSAGE is '-',\
            it is read from stdin.
        message_pause: Same as -message to match the old prompt_user.
        buttons: What do you want the buttons to say? You can give up to three\
            -button for three buttons. Returns integer 1, 2, or 3. If there is no\
            -button, there will be one button 'Ok'.
        buttons_b: Same as -button.
        timeout: Timeout in seconds of prompt message. Default answer is returned\
            if TT seconds elapse without user input.
        timeout_to: Same as -timeout TT.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = prompt_popup_params(
        message=message,
        message_pause=message_pause,
        buttons=buttons,
        buttons_b=buttons_b,
        timeout=timeout,
        timeout_to=timeout_to,
    )
    return prompt_popup_execute(params, runner)

__all__ = [
    "PromptPopup",
    "PromptPopupOutputs",
    "PROMPT_POPUP_METADATA",
    "prompt_popup_cargs",
    "prompt_popup_outputs",
    "prompt_popup_params",
    "prompt_popup_execute",
    "prompt_popup_validate",
    "prompt_popup",
]