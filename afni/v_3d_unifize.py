# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_UNIFIZE_METADATA = Metadata(
    id="3dUnifize",
    name="3dUnifize",
    package="afni",
)

V3dUnifizeOutputtype = typing.Literal["NIFTI"] | typing.Literal["AFNI"] | typing.Literal["NIFTI_GZ"]

V3dUnifize = typing.TypedDict(
    "V3dUnifize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dUnifize"]],
        "cl_frac": typing.NotRequired[float],
        "epi": typing.NotRequired[bool],
        "gm": typing.NotRequired[bool],
        "no_duplo": typing.NotRequired[bool],
        "num_threads": typing.NotRequired[int],
        "outputtype": typing.NotRequired[V3dUnifizeOutputtype],
        "quiet": typing.NotRequired[bool],
        "rbt": typing.NotRequired[list[float]],
        "prefix": typing.NotRequired[str],
        "scale_file": typing.NotRequired[InputPathType],
        "t2": typing.NotRequired[bool],
        "t2_up": typing.NotRequired[float],
        "urad": typing.NotRequired[float],
        "in_file": InputPathType,
    },
)

@dataclasses.dataclass
class V3dUnifizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Output image file name."""
    scale_file_outfile: OutputPathType | None
    """Scale factor file."""

def v_3d_unifize_params(
    in_file: InputPathType,
    cl_frac: float | None = None,
    epi: bool = False,
    gm: bool = False,
    no_duplo: bool = False,
    num_threads: int | None = None,
    outputtype: V3dUnifizeOutputtype | None = None,
    quiet: bool = False,
    rbt: list[float] | None = None,
    prefix: str | None = None,
    scale_file: InputPathType | None = None,
    t2: bool = False,
    t2_up: float | None = None,
    urad: float | None = None,
) -> V3dUnifize:
    """
    Build parameters.

    Args:
        in_file: Input file to 3dunifize.
        cl_frac: Option for afni experts only.set the automask 'clip level\
            fraction'. must be between 0.1 and 0.9. a small fraction means to make\
            the initial threshold for clipping (a la 3dcliplevel) smaller, which\
            will tend to make the mask larger. [default=0.1].
        epi: Assume the input dataset is a t2 (or t2\*) weighted epi time series.\
            after computing the scaling, apply it to all volumes (trs) in the input\
            dataset. that is, a given voxel will be scaled by the same factor at\
            each tr. this option also implies '-noduplo' and '-t2'.this option\
            turns off '-gm' if you turned it on.
        gm: Also scale to unifize 'gray matter' = lower intensity voxels (to aid in\
            registering images from different scanners).
        no_duplo: Do not use the 'duplo down' step; this can be useful for lower\
            resolution datasets.
        num_threads: Set number of threads.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        quiet: Don't print the progress messages.
        rbt: (a float, a float, a float). Option for afni experts only.specify the\
            3 parameters for the algorithm:r = radius; same as given by option\
            '-urad', [default=18.3]b = bottom percentile of normalizing data range,\
            [default=70.0]r = top percentile of normalizing data range,\
            [default=80.0].
        prefix: Output image file name.
        scale_file: Output file name to save the scale factor used at each voxel .
        t2: Treat the input as if it were t2-weighted, rather than t1-weighted.\
            this processing is done simply by inverting the image contrast,\
            processing it as if that result were t1-weighted, and then re-inverting\
            the results counts of voxel overlap, i.e., each voxel will contain the\
            number of masks that it is set in.
        t2_up: Option for afni experts only.set the upper percentile point used for\
            t2-t1 inversion. allowed to be anything between 90 and 100 (inclusive),\
            with default to 98.5 (for no good reason).
        urad: Sets the radius (in voxels) of the ball used for the sneaky trick.\
            default value is 18.3, and should be changed proportionally if the\
            dataset voxel size differs significantly from 1 mm.

    Returns:
        Parameter dictionary.
    """
    params: V3dUnifize = {
        "@type": "afni/3dUnifize",
        "in_file": in_file,
        "epi": epi,
        "gm": gm,
        "no_duplo": no_duplo,
        "quiet": quiet,
        "t2": t2,
    }
    if cl_frac is not None:
        params["cl_frac"] = cl_frac
    if num_threads is not None:
        params["num_threads"] = num_threads
    if outputtype is not None:
        params["outputtype"] = outputtype
    if rbt is not None:
        params["rbt"] = rbt
    if prefix is not None:
        params["prefix"] = prefix
    if scale_file is not None:
        params["scale_file"] = scale_file
    if t2_up is not None:
        params["t2_up"] = t2_up
    if urad is not None:
        params["urad"] = urad
    return params

def v_3d_unifize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dUnifize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cl_frac", None) is not None:
        if not isinstance(params["cl_frac"], (float, int)):
            raise StyxValidationError(f'`cl_frac` has the wrong type: Received `{type(params["cl_frac"])}` expected `float`')
    if params.get("epi", None) is not None:
        if not isinstance(params["epi"], bool):
            raise StyxValidationError(f'`epi` has the wrong type: Received `{type(params["epi"])}` expected `bool`')
    if params.get("gm", None) is not None:
        if not isinstance(params["gm"], bool):
            raise StyxValidationError(f'`gm` has the wrong type: Received `{type(params["gm"])}` expected `bool`')
    if params.get("no_duplo", None) is not None:
        if not isinstance(params["no_duplo"], bool):
            raise StyxValidationError(f'`no_duplo` has the wrong type: Received `{type(params["no_duplo"])}` expected `bool`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], int):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `int`')
    if params.get("outputtype", None) is not None:
        if not isinstance(params["outputtype"], str):
            raise StyxValidationError(f'`outputtype` has the wrong type: Received `{type(params["outputtype"])}` expected `V3dUnifizeOutputtype`')
        if params["outputtype"] not in ["NIFTI", "AFNI", "NIFTI_GZ"]:
            raise StyxValidationError("Parameter `outputtype` must be one of [\"NIFTI\", \"AFNI\", \"NIFTI_GZ\"]")
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("rbt", None) is not None:
        if not isinstance(params["rbt"], list):
            raise StyxValidationError(f'`rbt` has the wrong type: Received `{type(params["rbt"])}` expected `list[float]`')
        if not (3 <= len(params["rbt"]) <= 3):
            raise StyxValidationError("Parameter `rbt` must contain between 3 and 3 elements (inclusive)")
        for e in params["rbt"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`rbt` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("scale_file", None) is not None:
        if not isinstance(params["scale_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scale_file` has the wrong type: Received `{type(params["scale_file"])}` expected `InputPathType`')
    if params.get("t2", None) is not None:
        if not isinstance(params["t2"], bool):
            raise StyxValidationError(f'`t2` has the wrong type: Received `{type(params["t2"])}` expected `bool`')
    if params.get("t2_up", None) is not None:
        if not isinstance(params["t2_up"], (float, int)):
            raise StyxValidationError(f'`t2_up` has the wrong type: Received `{type(params["t2_up"])}` expected `float`')
    if params.get("urad", None) is not None:
        if not isinstance(params["urad"], (float, int)):
            raise StyxValidationError(f'`urad` has the wrong type: Received `{type(params["urad"])}` expected `float`')
    if params.get("in_file", None) is None:
        raise StyxValidationError("`in_file` must not be None")
    if not isinstance(params["in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_file` has the wrong type: Received `{type(params["in_file"])}` expected `InputPathType`')

def v_3d_unifize_cargs(params: V3dUnifize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dUnifize")
    v_0 = params.get("cl_frac")
    if v_0 is not None:
        cargs.append("-clfrac")
        cargs.append(str(v_0))
    if params.get("epi"):
        cargs.append("-EPI")
    if params.get("gm"):
        cargs.append("-GM")
    if params.get("no_duplo"):
        cargs.append("-noduplo")
    v_1 = params.get("num_threads")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("outputtype")
    if v_2 is not None:
        cargs.append(str(v_2))
    if params.get("quiet"):
        cargs.append("-quiet")
    v_3 = params.get("rbt")
    if v_3 is not None:
        cargs.append("-rbt")
        for item0 in v_3:
            cargs.append(str(item0))
    v_4 = params.get("prefix")
    if v_4 is not None:
        cargs.append("-prefix")
        cargs.append(v_4)
    v_5 = params.get("scale_file")
    if v_5 is not None:
        cargs.append("-ssave")
        cargs.append(execution.input_file(v_5))
    if params.get("t2"):
        cargs.append("-T2")
    v_6 = params.get("t2_up")
    if v_6 is not None:
        cargs.append("-T2up")
        cargs.append(str(v_6))
    v_7 = params.get("urad")
    if v_7 is not None:
        cargs.append("-Urad")
        cargs.append(str(v_7))
    cargs.append("-input")
    cargs.append(execution.input_file(params["in_file"]))
    return cargs

def v_3d_unifize_outputs(params: V3dUnifize, execution: Execution) -> V3dUnifizeOutputs:
    """Build the V3dUnifizeOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    scale_file_outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("scale_file")
    if __v1 is not None:
        scale_file_outfile_v = execution.output_file((__v1 if __v1 is not None else ""))
    return V3dUnifizeOutputs(
        root=root_v,
        out_file=out_file_v,
        scale_file_outfile=scale_file_outfile_v,
    )

def v_3d_unifize_execute(params: V3dUnifize, runner: Runner | None = None) -> V3dUnifizeOutputs:
    """
    3dUnifize

    3dUnifize - for uniformizing image intensity
* The input dataset is supposed to be a T1-weighted volume, possibly already skull-stripped (e.g., via 3dSkullStrip). However, this program can be a useful step to take BEFORE 3dSkullStrip, since the latter program can fail if the input volume is strongly shaded -- 3dUnifize will (mostly) remove such shading artifacts.
* The output dataset has the white matter (WM) intensity approximately uniformized across space, and scaled to peak at about 1000.
* The output dataset is always stored in float format!
* If the input dataset has more than 1 sub-brick, only sub-brick #0 will be processed!
* Want to correct EPI datasets for nonuniformity? You can try the new and experimental [Mar 2017] '-EPI' option.
* The principal motive for this program is for use in an image registration script, and it may or may not be useful otherwise.
* This program replaces the older (and very different) 3dUniformize, which is no longer maintained and may sublimate at any moment. (In other words, we do not recommend the use of 3dUniformize.)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_unifize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_UNIFIZE_METADATA)
    execution.params(params)
    args = v_3d_unifize_cargs(params, execution)
    out = v_3d_unifize_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_unifize(
    in_file: InputPathType,
    cl_frac: float | None = None,
    epi: bool = False,
    gm: bool = False,
    no_duplo: bool = False,
    num_threads: int | None = None,
    outputtype: V3dUnifizeOutputtype | None = None,
    quiet: bool = False,
    rbt: list[float] | None = None,
    prefix: str | None = None,
    scale_file: InputPathType | None = None,
    t2: bool = False,
    t2_up: float | None = None,
    urad: float | None = None,
    runner: Runner | None = None,
) -> V3dUnifizeOutputs:
    """
    3dUnifize

    3dUnifize - for uniformizing image intensity
* The input dataset is supposed to be a T1-weighted volume, possibly already skull-stripped (e.g., via 3dSkullStrip). However, this program can be a useful step to take BEFORE 3dSkullStrip, since the latter program can fail if the input volume is strongly shaded -- 3dUnifize will (mostly) remove such shading artifacts.
* The output dataset has the white matter (WM) intensity approximately uniformized across space, and scaled to peak at about 1000.
* The output dataset is always stored in float format!
* If the input dataset has more than 1 sub-brick, only sub-brick #0 will be processed!
* Want to correct EPI datasets for nonuniformity? You can try the new and experimental [Mar 2017] '-EPI' option.
* The principal motive for this program is for use in an image registration script, and it may or may not be useful otherwise.
* This program replaces the older (and very different) 3dUniformize, which is no longer maintained and may sublimate at any moment. (In other words, we do not recommend the use of 3dUniformize.)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_file: Input file to 3dunifize.
        cl_frac: Option for afni experts only.set the automask 'clip level\
            fraction'. must be between 0.1 and 0.9. a small fraction means to make\
            the initial threshold for clipping (a la 3dcliplevel) smaller, which\
            will tend to make the mask larger. [default=0.1].
        epi: Assume the input dataset is a t2 (or t2\*) weighted epi time series.\
            after computing the scaling, apply it to all volumes (trs) in the input\
            dataset. that is, a given voxel will be scaled by the same factor at\
            each tr. this option also implies '-noduplo' and '-t2'.this option\
            turns off '-gm' if you turned it on.
        gm: Also scale to unifize 'gray matter' = lower intensity voxels (to aid in\
            registering images from different scanners).
        no_duplo: Do not use the 'duplo down' step; this can be useful for lower\
            resolution datasets.
        num_threads: Set number of threads.
        outputtype: 'nifti' or 'afni' or 'nifti_gz'. Afni output filetype.
        quiet: Don't print the progress messages.
        rbt: (a float, a float, a float). Option for afni experts only.specify the\
            3 parameters for the algorithm:r = radius; same as given by option\
            '-urad', [default=18.3]b = bottom percentile of normalizing data range,\
            [default=70.0]r = top percentile of normalizing data range,\
            [default=80.0].
        prefix: Output image file name.
        scale_file: Output file name to save the scale factor used at each voxel .
        t2: Treat the input as if it were t2-weighted, rather than t1-weighted.\
            this processing is done simply by inverting the image contrast,\
            processing it as if that result were t1-weighted, and then re-inverting\
            the results counts of voxel overlap, i.e., each voxel will contain the\
            number of masks that it is set in.
        t2_up: Option for afni experts only.set the upper percentile point used for\
            t2-t1 inversion. allowed to be anything between 90 and 100 (inclusive),\
            with default to 98.5 (for no good reason).
        urad: Sets the radius (in voxels) of the ball used for the sneaky trick.\
            default value is 18.3, and should be changed proportionally if the\
            dataset voxel size differs significantly from 1 mm.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_unifize_params(
        in_file=in_file,
        cl_frac=cl_frac,
        epi=epi,
        gm=gm,
        no_duplo=no_duplo,
        num_threads=num_threads,
        outputtype=outputtype,
        quiet=quiet,
        rbt=rbt,
        prefix=prefix,
        scale_file=scale_file,
        t2=t2,
        t2_up=t2_up,
        urad=urad,
    )
    return v_3d_unifize_execute(params, runner)

__all__ = [
    "V3dUnifize",
    "V3dUnifizeOutputs",
    "V_3D_UNIFIZE_METADATA",
    "v_3d_unifize_cargs",
    "v_3d_unifize_outputs",
    "v_3d_unifize_params",
    "v_3d_unifize_execute",
    "v_3d_unifize_validate",
    "v_3d_unifize",
]