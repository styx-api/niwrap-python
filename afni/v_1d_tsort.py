# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1D_TSORT_METADATA = Metadata(
    id="1dTsort",
    name="1dTsort",
    package="afni",
)

V1dTsort = typing.TypedDict(
    "V1dTsort",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dTsort"]],
        "inc_order": typing.NotRequired[bool],
        "dec_order": typing.NotRequired[bool],
        "transpose": typing.NotRequired[bool],
        "column": typing.NotRequired[float],
        "imode": typing.NotRequired[bool],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class V1dTsortOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_1d_tsort_params(
    infile: InputPathType,
    inc_order: bool = False,
    dec_order: bool = False,
    transpose: bool = False,
    column: float | None = None,
    imode: bool = False,
) -> V1dTsort:
    """
    Build parameters.

    Args:
        infile: Input 1D file to be sorted.
        inc_order: Sort into increasing order [default]
        dec_order: Sort into decreasing order
        transpose: Transpose the file before output.
        column: Sort only on column #j (counting starts at 0), and carry the rest\
            of the columns with it.
        imode: Typecast all values to integers, return the mode in the input then\
            exit. No sorting results are returned.

    Returns:
        Parameter dictionary.
    """
    params: V1dTsort = {
        "@type": "afni/1dTsort",
        "infile": infile,
        "inc_order": inc_order,
        "dec_order": dec_order,
        "transpose": transpose,
        "imode": imode,
    }
    if column is not None:
        params["column"] = column
    return params

def v_1d_tsort_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dTsort."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inc_order", None) is not None:
        if not isinstance(params["inc_order"], bool):
            raise StyxValidationError(f'`inc_order` has the wrong type: Received `{type(params["inc_order"])}` expected `bool`')
    if params.get("dec_order", None) is not None:
        if not isinstance(params["dec_order"], bool):
            raise StyxValidationError(f'`dec_order` has the wrong type: Received `{type(params["dec_order"])}` expected `bool`')
    if params.get("transpose", None) is not None:
        if not isinstance(params["transpose"], bool):
            raise StyxValidationError(f'`transpose` has the wrong type: Received `{type(params["transpose"])}` expected `bool`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], (float, int)):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `float`')
    if params.get("imode", None) is not None:
        if not isinstance(params["imode"], bool):
            raise StyxValidationError(f'`imode` has the wrong type: Received `{type(params["imode"])}` expected `bool`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def v_1d_tsort_cargs(params: V1dTsort, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dTsort")
    if params.get("inc_order"):
        cargs.append("-inc")
    if params.get("dec_order"):
        cargs.append("-dec")
    if params.get("transpose"):
        cargs.append("-flip")
    v_0 = params.get("column")
    if v_0 is not None:
        cargs.append("-col")
        cargs.append(str(v_0))
    if params.get("imode"):
        cargs.append("-imode")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def v_1d_tsort_outputs(params: V1dTsort, execution: Execution) -> V1dTsortOutputs:
    """Build the V1dTsortOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V1dTsortOutputs(
        root=root_v,
    )

def v_1d_tsort_execute(params: V1dTsort, runner: Runner | None = None) -> V1dTsortOutputs:
    """
    1dTsort

    Sorts each column of the input 1D file and writes result to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1d_tsort_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1D_TSORT_METADATA)
    execution.params(params)
    args = v_1d_tsort_cargs(params, execution)
    out = v_1d_tsort_outputs(params, execution)
    execution.run(args)
    return out

def v_1d_tsort(
    infile: InputPathType,
    inc_order: bool = False,
    dec_order: bool = False,
    transpose: bool = False,
    column: float | None = None,
    imode: bool = False,
    runner: Runner | None = None,
) -> V1dTsortOutputs:
    """
    1dTsort

    Sorts each column of the input 1D file and writes result to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input 1D file to be sorted.
        inc_order: Sort into increasing order [default]
        dec_order: Sort into decreasing order
        transpose: Transpose the file before output.
        column: Sort only on column #j (counting starts at 0), and carry the rest\
            of the columns with it.
        imode: Typecast all values to integers, return the mode in the input then\
            exit. No sorting results are returned.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1d_tsort_params(
        infile=infile,
        inc_order=inc_order,
        dec_order=dec_order,
        transpose=transpose,
        column=column,
        imode=imode,
    )
    return v_1d_tsort_execute(params, runner)

__all__ = [
    "V1dTsort",
    "V1dTsortOutputs",
    "V_1D_TSORT_METADATA",
    "v_1d_tsort_cargs",
    "v_1d_tsort_outputs",
    "v_1d_tsort_params",
    "v_1d_tsort_execute",
    "v_1d_tsort_validate",
    "v_1d_tsort",
]