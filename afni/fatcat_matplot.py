# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FATCAT_MATPLOT_METADATA = Metadata(
    id="FATCAT_matplot",
    name="FATCAT_matplot",
    package="afni",
)

FatcatMatplot = typing.TypedDict(
    "FatcatMatplot",
    {
        "@type": typing.NotRequired[typing.Literal["afni/FATCAT_matplot"]],
        "directory": str,
        "shiny_folder": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatcatMatplotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fatcat_matplot_params(
    directory: str,
    shiny_folder: bool = False,
) -> FatcatMatplot:
    """
    Build parameters.

    Args:
        directory: Path to a folder containing .netcc and/or .grid files.
        shiny_folder: Use a custom shiny folder (for testing purposes).

    Returns:
        Parameter dictionary.
    """
    params: FatcatMatplot = {
        "@type": "afni/FATCAT_matplot",
        "directory": directory,
        "shiny_folder": shiny_folder,
    }
    return params

def fatcat_matplot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatcatMatplot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("directory", None) is None:
        raise StyxValidationError("`directory` must not be None")
    if not isinstance(params["directory"], str):
        raise StyxValidationError(f'`directory` has the wrong type: Received `{type(params["directory"])}` expected `str`')
    if params.get("shiny_folder", None) is not None:
        if not isinstance(params["shiny_folder"], bool):
            raise StyxValidationError(f'`shiny_folder` has the wrong type: Received `{type(params["shiny_folder"])}` expected `bool`')

def fatcat_matplot_cargs(params: FatcatMatplot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("FATCAT_matplot")
    cargs.append(params["directory"])
    if params.get("shiny_folder"):
        cargs.append("-ShinyFolder")
    return cargs

def fatcat_matplot_outputs(params: FatcatMatplot, execution: Execution) -> FatcatMatplotOutputs:
    """Build the FatcatMatplotOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FatcatMatplotOutputs(
        root=root_v,
    )

def fatcat_matplot_execute(params: FatcatMatplot, runner: Runner | None = None) -> FatcatMatplotOutputs:
    """
    FATCAT_matplot

    Launch a shiny app to visualize .netcc and/or .grid files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fatcat_matplot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FATCAT_MATPLOT_METADATA)
    execution.params(params)
    args = fatcat_matplot_cargs(params, execution)
    out = fatcat_matplot_outputs(params, execution)
    execution.run(args)
    return out

def fatcat_matplot(
    directory: str,
    shiny_folder: bool = False,
    runner: Runner | None = None,
) -> FatcatMatplotOutputs:
    """
    FATCAT_matplot

    Launch a shiny app to visualize .netcc and/or .grid files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        directory: Path to a folder containing .netcc and/or .grid files.
        shiny_folder: Use a custom shiny folder (for testing purposes).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fatcat_matplot_params(
        directory=directory,
        shiny_folder=shiny_folder,
    )
    return fatcat_matplot_execute(params, runner)

__all__ = [
    "FatcatMatplot",
    "FatcatMatplotOutputs",
    "FATCAT_MATPLOT_METADATA",
    "fatcat_matplot_cargs",
    "fatcat_matplot_outputs",
    "fatcat_matplot_params",
    "fatcat_matplot_execute",
    "fatcat_matplot_validate",
    "fatcat_matplot",
]