# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_BALL_MATCH_METADATA = Metadata(
    id="3dBallMatch",
    name="3dBallMatch",
    package="afni",
)

V3dBallMatch = typing.TypedDict(
    "V3dBallMatch",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dBallMatch"]],
        "input_dataset": InputPathType,
        "radius": typing.NotRequired[float],
        "dataset_option": typing.NotRequired[str],
        "ball_radius": typing.NotRequired[float],
        "spheroid_axes": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class V3dBallMatchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_stdout: OutputPathType
    """Output containing matching coordinates and related data"""

def v_3d_ball_match_params(
    input_dataset: InputPathType,
    radius: float | None = None,
    dataset_option: str | None = None,
    ball_radius: float | None = None,
    spheroid_axes: list[float] | None = None,
) -> V3dBallMatch:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (e.g., Fred.nii)
        radius: Radius of the 3D ball to match (in mm)
        dataset_option: Specifies the input dataset
        ball_radius: Set the radius of the 3D ball to match (mm)
        spheroid_axes: Match with a spheroid of revolution, with principal axis\
            radius 'a' and secondary axes radii 'b'

    Returns:
        Parameter dictionary.
    """
    params: V3dBallMatch = {
        "@type": "afni/3dBallMatch",
        "input_dataset": input_dataset,
    }
    if radius is not None:
        params["radius"] = radius
    if dataset_option is not None:
        params["dataset_option"] = dataset_option
    if ball_radius is not None:
        params["ball_radius"] = ball_radius
    if spheroid_axes is not None:
        params["spheroid_axes"] = spheroid_axes
    return params

def v_3d_ball_match_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dBallMatch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("radius", None) is not None:
        if not isinstance(params["radius"], (float, int)):
            raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')
    if params.get("dataset_option", None) is not None:
        if not isinstance(params["dataset_option"], str):
            raise StyxValidationError(f'`dataset_option` has the wrong type: Received `{type(params["dataset_option"])}` expected `str`')
    if params.get("ball_radius", None) is not None:
        if not isinstance(params["ball_radius"], (float, int)):
            raise StyxValidationError(f'`ball_radius` has the wrong type: Received `{type(params["ball_radius"])}` expected `float`')
    if params.get("spheroid_axes", None) is not None:
        if not isinstance(params["spheroid_axes"], list):
            raise StyxValidationError(f'`spheroid_axes` has the wrong type: Received `{type(params["spheroid_axes"])}` expected `list[float]`')
        if not (2 <= len(params["spheroid_axes"]) <= 2):
            raise StyxValidationError("Parameter `spheroid_axes` must contain between 2 and 2 elements (inclusive)")
        for e in params["spheroid_axes"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`spheroid_axes` has the wrong type: Received `{type(e)}` expected `list[float]`')

def v_3d_ball_match_cargs(params: V3dBallMatch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dBallMatch")
    cargs.append(execution.input_file(params["input_dataset"]))
    v_0 = params.get("radius")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("dataset_option")
    if v_1 is not None:
        cargs.append("-input")
        cargs.append(v_1)
    v_2 = params.get("ball_radius")
    if v_2 is not None:
        cargs.append("-ball")
        cargs.append(str(v_2))
    v_3 = params.get("spheroid_axes")
    if v_3 is not None:
        cargs.append("-spheroid")
        for item0 in v_3:
            cargs.append(str(item0))
    return cargs

def v_3d_ball_match_outputs(params: V3dBallMatch, execution: Execution) -> V3dBallMatchOutputs:
    """Build the V3dBallMatchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_stdout_v: OutputPathType = execution.output_file("stdout")
    return V3dBallMatchOutputs(
        root=root_v,
        output_stdout=output_stdout_v,
    )

def v_3d_ball_match_execute(params: V3dBallMatch, runner: Runner | None = None) -> V3dBallMatchOutputs:
    """
    3dBallMatch

    A tool to find a good match between a ball (filled sphere) of the given radius and a dataset to determine a crude approximate center of the brain quickly.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_ball_match_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_BALL_MATCH_METADATA)
    execution.params(params)
    args = v_3d_ball_match_cargs(params, execution)
    out = v_3d_ball_match_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_ball_match(
    input_dataset: InputPathType,
    radius: float | None = None,
    dataset_option: str | None = None,
    ball_radius: float | None = None,
    spheroid_axes: list[float] | None = None,
    runner: Runner | None = None,
) -> V3dBallMatchOutputs:
    """
    3dBallMatch

    A tool to find a good match between a ball (filled sphere) of the given radius and a dataset to determine a crude approximate center of the brain quickly.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (e.g., Fred.nii)
        radius: Radius of the 3D ball to match (in mm)
        dataset_option: Specifies the input dataset
        ball_radius: Set the radius of the 3D ball to match (mm)
        spheroid_axes: Match with a spheroid of revolution, with principal axis\
            radius 'a' and secondary axes radii 'b'
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_ball_match_params(
        input_dataset=input_dataset,
        radius=radius,
        dataset_option=dataset_option,
        ball_radius=ball_radius,
        spheroid_axes=spheroid_axes,
    )
    return v_3d_ball_match_execute(params, runner)

__all__ = [
    "V3dBallMatch",
    "V3dBallMatchOutputs",
    "V_3D_BALL_MATCH_METADATA",
    "v_3d_ball_match_cargs",
    "v_3d_ball_match_outputs",
    "v_3d_ball_match_params",
    "v_3d_ball_match_execute",
    "v_3d_ball_match_validate",
    "v_3d_ball_match",
]