# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_4SWAP_METADATA = Metadata(
    id="4swap",
    name="4swap",
    package="afni",
)

V4swap = typing.TypedDict(
    "V4swap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/4swap"]],
        "files": list[InputPathType],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V4swapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_4swap_params(
    files: list[InputPathType],
    quiet: bool = False,
) -> V4swap:
    """
    Build parameters.

    Args:
        files: List of files to process.
        quiet: Work quietly; suppress output messages.

    Returns:
        Parameter dictionary.
    """
    params: V4swap = {
        "@type": "afni/4swap",
        "files": files,
        "quiet": quiet,
    }
    return params

def v_4swap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V4swap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("files", None) is None:
        raise StyxValidationError("`files` must not be None")
    if not isinstance(params["files"], list):
        raise StyxValidationError(f'`files` has the wrong type: Received `{type(params["files"])}` expected `list[InputPathType]`')
    for e in params["files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_4swap_cargs(params: V4swap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("4swap")
    for item0 in params["files"]:
        cargs.append(execution.input_file(item0))
    if params.get("quiet"):
        cargs.append("-q")
    return cargs

def v_4swap_outputs(params: V4swap, execution: Execution) -> V4swapOutputs:
    """Build the V4swapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V4swapOutputs(
        root=root_v,
    )

def v_4swap_execute(params: V4swap, runner: Runner | None = None) -> V4swapOutputs:
    """
    4swap

    Swaps byte quadruples on the listed files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_4swap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_4SWAP_METADATA)
    execution.params(params)
    args = v_4swap_cargs(params, execution)
    out = v_4swap_outputs(params, execution)
    execution.run(args)
    return out

def v_4swap(
    files: list[InputPathType],
    quiet: bool = False,
    runner: Runner | None = None,
) -> V4swapOutputs:
    """
    4swap

    Swaps byte quadruples on the listed files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        files: List of files to process.
        quiet: Work quietly; suppress output messages.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_4swap_params(
        files=files,
        quiet=quiet,
    )
    return v_4swap_execute(params, runner)

__all__ = [
    "V4swap",
    "V4swapOutputs",
    "V_4SWAP_METADATA",
    "v_4swap_cargs",
    "v_4swap_outputs",
    "v_4swap_params",
    "v_4swap_execute",
    "v_4swap_validate",
    "v_4swap",
]