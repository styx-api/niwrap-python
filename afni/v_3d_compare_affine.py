# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_COMPARE_AFFINE_METADATA = Metadata(
    id="3dCompareAffine",
    name="3dCompareAffine",
    package="afni",
)

V3dCompareAffine = typing.TypedDict(
    "V3dCompareAffine",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dCompareAffine"]],
        "mask": typing.NotRequired[str],
        "dset": typing.NotRequired[InputPathType],
        "affine": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class V3dCompareAffineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output file containing comparison results of affine transformations"""

def v_3d_compare_affine_params(
    mask: str | None = None,
    dset: InputPathType | None = None,
    affine: list[str] | None = None,
) -> V3dCompareAffine:
    """
    Build parameters.

    Args:
        mask: Dataset containing non-zero voxels used as the region over which to\
            compare the affine transformations.
        dset: Dataset to compute an automask from it and use that mask as the\
            spatial region for comparison.
        affine: Input an affine transformation (file or 'MATRIX'). Multiple\
            '-affine' options can be used to input multiple files.

    Returns:
        Parameter dictionary.
    """
    params: V3dCompareAffine = {
        "@type": "afni/3dCompareAffine",
    }
    if mask is not None:
        params["mask"] = mask
    if dset is not None:
        params["dset"] = dset
    if affine is not None:
        params["affine"] = affine
    return params

def v_3d_compare_affine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dCompareAffine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], str):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `str`')
    if params.get("dset", None) is not None:
        if not isinstance(params["dset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')
    if params.get("affine", None) is not None:
        if not isinstance(params["affine"], list):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `list[str]`')
        for e in params["affine"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`affine` has the wrong type: Received `{type(e)}` expected `list[str]`')

def v_3d_compare_affine_cargs(params: V3dCompareAffine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dCompareAffine")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(v_0)
    v_1 = params.get("dset")
    if v_1 is not None:
        cargs.append("-dset")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("affine")
    if v_2 is not None:
        cargs.append("-affine")
        for item0 in v_2:
            cargs.append(item0)
    return cargs

def v_3d_compare_affine_outputs(params: V3dCompareAffine, execution: Execution) -> V3dCompareAffineOutputs:
    """Build the V3dCompareAffineOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file("[OUTPUT_PREFIX]_comparison.txt")
    return V3dCompareAffineOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def v_3d_compare_affine_execute(params: V3dCompareAffine, runner: Runner | None = None) -> V3dCompareAffineOutputs:
    """
    3dCompareAffine

    Compares two (or more) affine spatial transformations on a dataset and outputs measurements of their differences in spatial displacements.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_compare_affine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_COMPARE_AFFINE_METADATA)
    execution.params(params)
    args = v_3d_compare_affine_cargs(params, execution)
    out = v_3d_compare_affine_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_compare_affine(
    mask: str | None = None,
    dset: InputPathType | None = None,
    affine: list[str] | None = None,
    runner: Runner | None = None,
) -> V3dCompareAffineOutputs:
    """
    3dCompareAffine

    Compares two (or more) affine spatial transformations on a dataset and outputs measurements of their differences in spatial displacements.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        mask: Dataset containing non-zero voxels used as the region over which to\
            compare the affine transformations.
        dset: Dataset to compute an automask from it and use that mask as the\
            spatial region for comparison.
        affine: Input an affine transformation (file or 'MATRIX'). Multiple\
            '-affine' options can be used to input multiple files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_compare_affine_params(
        mask=mask,
        dset=dset,
        affine=affine,
    )
    return v_3d_compare_affine_execute(params, runner)

__all__ = [
    "V3dCompareAffine",
    "V3dCompareAffineOutputs",
    "V_3D_COMPARE_AFFINE_METADATA",
    "v_3d_compare_affine_cargs",
    "v_3d_compare_affine_outputs",
    "v_3d_compare_affine_params",
    "v_3d_compare_affine_execute",
    "v_3d_compare_affine_validate",
    "v_3d_compare_affine",
]