# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMCUTUP_METADATA = Metadata(
    id="imcutup",
    name="imcutup",
    package="afni",
)

Imcutup = typing.TypedDict(
    "Imcutup",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imcutup"]],
        "prefix": typing.NotRequired[str],
        "xynum": typing.NotRequired[bool],
        "yxnum": typing.NotRequired[bool],
        "xynum_format": typing.NotRequired[bool],
        "yxnum_format": typing.NotRequired[bool],
        "nx": int,
        "ny": int,
        "input_file": InputPathType,
    },
)

@dataclasses.dataclass
class ImcutupOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def imcutup_params(
    nx: int,
    ny: int,
    input_file: InputPathType,
    prefix: str | None = None,
    xynum: bool = False,
    yxnum: bool = False,
    xynum_format: bool = False,
    yxnum_format: bool = False,
) -> Imcutup:
    """
    Build parameters.

    Args:
        nx: Number of pixels along the x-dimension for the smaller images.
        ny: Number of pixels along the y-dimension for the smaller images.
        input_file: Input image filename. Must be a single 2D image.
        prefix: Prefix the output files with the provided string
        xynum: Number the output images in x-first, then y (default behavior)
        yxnum: Number the output images in y-first, then x
        xynum_format: 2D numbering in x.y format
        yxnum_format: 2D numbering in y.x format

    Returns:
        Parameter dictionary.
    """
    params: Imcutup = {
        "@type": "afni/imcutup",
        "nx": nx,
        "ny": ny,
        "input_file": input_file,
        "xynum": xynum,
        "yxnum": yxnum,
        "xynum_format": xynum_format,
        "yxnum_format": yxnum_format,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def imcutup_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imcutup."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("xynum", None) is not None:
        if not isinstance(params["xynum"], bool):
            raise StyxValidationError(f'`xynum` has the wrong type: Received `{type(params["xynum"])}` expected `bool`')
    if params.get("yxnum", None) is not None:
        if not isinstance(params["yxnum"], bool):
            raise StyxValidationError(f'`yxnum` has the wrong type: Received `{type(params["yxnum"])}` expected `bool`')
    if params.get("xynum_format", None) is not None:
        if not isinstance(params["xynum_format"], bool):
            raise StyxValidationError(f'`xynum_format` has the wrong type: Received `{type(params["xynum_format"])}` expected `bool`')
    if params.get("yxnum_format", None) is not None:
        if not isinstance(params["yxnum_format"], bool):
            raise StyxValidationError(f'`yxnum_format` has the wrong type: Received `{type(params["yxnum_format"])}` expected `bool`')
    if params.get("nx", None) is None:
        raise StyxValidationError("`nx` must not be None")
    if not isinstance(params["nx"], int):
        raise StyxValidationError(f'`nx` has the wrong type: Received `{type(params["nx"])}` expected `int`')
    if params.get("ny", None) is None:
        raise StyxValidationError("`ny` must not be None")
    if not isinstance(params["ny"], int):
        raise StyxValidationError(f'`ny` has the wrong type: Received `{type(params["ny"])}` expected `int`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def imcutup_cargs(params: Imcutup, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imcutup")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("xynum"):
        cargs.append("-xynum")
    if params.get("yxnum"):
        cargs.append("-yxnum")
    if params.get("xynum_format"):
        cargs.append("-x.ynum")
    if params.get("yxnum_format"):
        cargs.append("-y.xnum")
    cargs.append(str(params["nx"]))
    cargs.append(str(params["ny"]))
    cargs.append(execution.input_file(params["input_file"]))
    return cargs

def imcutup_outputs(params: Imcutup, execution: Execution) -> ImcutupOutputs:
    """Build the ImcutupOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ImcutupOutputs(
        root=root_v,
    )

def imcutup_execute(params: Imcutup, runner: Runner | None = None) -> ImcutupOutputs:
    """
    imcutup

    Breaks up larger images into smaller image files of user-defined size.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imcutup_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMCUTUP_METADATA)
    execution.params(params)
    args = imcutup_cargs(params, execution)
    out = imcutup_outputs(params, execution)
    execution.run(args)
    return out

def imcutup(
    nx: int,
    ny: int,
    input_file: InputPathType,
    prefix: str | None = None,
    xynum: bool = False,
    yxnum: bool = False,
    xynum_format: bool = False,
    yxnum_format: bool = False,
    runner: Runner | None = None,
) -> ImcutupOutputs:
    """
    imcutup

    Breaks up larger images into smaller image files of user-defined size.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        nx: Number of pixels along the x-dimension for the smaller images.
        ny: Number of pixels along the y-dimension for the smaller images.
        input_file: Input image filename. Must be a single 2D image.
        prefix: Prefix the output files with the provided string
        xynum: Number the output images in x-first, then y (default behavior)
        yxnum: Number the output images in y-first, then x
        xynum_format: 2D numbering in x.y format
        yxnum_format: 2D numbering in y.x format
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imcutup_params(
        nx=nx,
        ny=ny,
        input_file=input_file,
        prefix=prefix,
        xynum=xynum,
        yxnum=yxnum,
        xynum_format=xynum_format,
        yxnum_format=yxnum_format,
    )
    return imcutup_execute(params, runner)

__all__ = [
    "Imcutup",
    "ImcutupOutputs",
    "IMCUTUP_METADATA",
    "imcutup_cargs",
    "imcutup_outputs",
    "imcutup_params",
    "imcutup_execute",
    "imcutup_validate",
    "imcutup",
]