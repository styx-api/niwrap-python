# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMSTACK_METADATA = Metadata(
    id="imstack",
    name="imstack",
    package="afni",
)

ImstackDataType = typing.Literal["short"] | typing.Literal["float"]

Imstack = typing.TypedDict(
    "Imstack",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imstack"]],
        "image_files": list[InputPathType],
        "data_type": typing.NotRequired[ImstackDataType],
        "output_prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class ImstackOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_data_file: OutputPathType | None
    """Output data file"""
    output_header_file: OutputPathType | None
    """Output header file"""

def imstack_params(
    image_files: list[InputPathType],
    data_type: ImstackDataType | None = None,
    output_prefix: str | None = None,
) -> Imstack:
    """
    Build parameters.

    Args:
        image_files: Input image filenames
        data_type: Converts the output data file to be 'type', which is either\
            'short' or 'float'. The default type is the type of the first image.
        output_prefix: Names the output files to be 'name'.b'type' and 'name'.hdr.\
            The default name is 'obi-wan-kenobi'.

    Returns:
        Parameter dictionary.
    """
    params: Imstack = {
        "@type": "afni/imstack",
        "image_files": image_files,
    }
    if data_type is not None:
        params["data_type"] = data_type
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    return params

def imstack_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imstack."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_files", None) is None:
        raise StyxValidationError("`image_files` must not be None")
    if not isinstance(params["image_files"], list):
        raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(params["image_files"])}` expected `list[InputPathType]`')
    for e in params["image_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("data_type", None) is not None:
        if not isinstance(params["data_type"], str):
            raise StyxValidationError(f'`data_type` has the wrong type: Received `{type(params["data_type"])}` expected `ImstackDataType`')
        if params["data_type"] not in ["short", "float"]:
            raise StyxValidationError("Parameter `data_type` must be one of [\"short\", \"float\"]")
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def imstack_cargs(params: Imstack, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imstack")
    for item0 in params["image_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("data_type")
    if v_0 is not None:
        cargs.append("-datum")
        cargs.append(str(v_0))
    v_1 = params.get("output_prefix")
    if v_1 is not None:
        cargs.append("-prefix")
        cargs.append(v_1)
    return cargs

def imstack_outputs(params: Imstack, execution: Execution) -> ImstackOutputs:
    """Build the ImstackOutputs object for this tool."""
    output_data_file_v: OutputPathType | None = None
    output_header_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_prefix")
    if __v0 is not None:
        __v1 = params.get("data_type")
        if __v1 is not None:
            output_data_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.b{(__v1 if __v1 is not None else "")}')
    __v2 = params.get("output_prefix")
    if __v2 is not None:
        output_header_file_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.hdr')
    return ImstackOutputs(
        root=root_v,
        output_data_file=output_data_file_v,
        output_header_file=output_header_file_v,
    )

def imstack_execute(params: Imstack, runner: Runner | None = None) -> ImstackOutputs:
    """
    imstack

    Stacks up a set of 2D images into one big file (a la MGH).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imstack_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMSTACK_METADATA)
    execution.params(params)
    args = imstack_cargs(params, execution)
    out = imstack_outputs(params, execution)
    execution.run(args)
    return out

def imstack(
    image_files: list[InputPathType],
    data_type: ImstackDataType | None = None,
    output_prefix: str | None = None,
    runner: Runner | None = None,
) -> ImstackOutputs:
    """
    imstack

    Stacks up a set of 2D images into one big file (a la MGH).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        image_files: Input image filenames
        data_type: Converts the output data file to be 'type', which is either\
            'short' or 'float'. The default type is the type of the first image.
        output_prefix: Names the output files to be 'name'.b'type' and 'name'.hdr.\
            The default name is 'obi-wan-kenobi'.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imstack_params(
        image_files=image_files,
        data_type=data_type,
        output_prefix=output_prefix,
    )
    return imstack_execute(params, runner)

__all__ = [
    "Imstack",
    "ImstackOutputs",
    "IMSTACK_METADATA",
    "imstack_cargs",
    "imstack_outputs",
    "imstack_params",
    "imstack_execute",
    "imstack_validate",
    "imstack",
]