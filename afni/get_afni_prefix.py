# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_PREFIX_METADATA = Metadata(
    id="@GetAfniPrefix",
    name="@GetAfniPrefix",
    package="afni",
)

GetAfniPrefix = typing.TypedDict(
    "GetAfniPrefix",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@GetAfniPrefix"]],
        "name": InputPathType,
        "suffix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class GetAfniPrefixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def get_afni_prefix_params(
    name: InputPathType,
    suffix: str | None = None,
) -> GetAfniPrefix:
    """
    Build parameters.

    Args:
        name: Input file path for which the AFNI prefix will be extracted
        suffix: Suffix string to append to the returned prefix

    Returns:
        Parameter dictionary.
    """
    params: GetAfniPrefix = {
        "@type": "afni/@GetAfniPrefix",
        "name": name,
    }
    if suffix is not None:
        params["suffix"] = suffix
    return params

def get_afni_prefix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniPrefix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("name", None) is None:
        raise StyxValidationError("`name` must not be None")
    if not isinstance(params["name"], (pathlib.Path, str)):
        raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `InputPathType`')
    if params.get("suffix", None) is not None:
        if not isinstance(params["suffix"], str):
            raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')

def get_afni_prefix_cargs(params: GetAfniPrefix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@GetAfniPrefix")
    cargs.append(execution.input_file(params["name"]))
    v_0 = params.get("suffix")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def get_afni_prefix_outputs(params: GetAfniPrefix, execution: Execution) -> GetAfniPrefixOutputs:
    """Build the GetAfniPrefixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GetAfniPrefixOutputs(
        root=root_v,
    )

def get_afni_prefix_execute(params: GetAfniPrefix, runner: Runner | None = None) -> GetAfniPrefixOutputs:
    """
    @GetAfniPrefix

    A tool to extract AFNI prefix from a given file path

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_prefix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_PREFIX_METADATA)
    execution.params(params)
    args = get_afni_prefix_cargs(params, execution)
    out = get_afni_prefix_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_prefix(
    name: InputPathType,
    suffix: str | None = None,
    runner: Runner | None = None,
) -> GetAfniPrefixOutputs:
    """
    @GetAfniPrefix

    A tool to extract AFNI prefix from a given file path

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        name: Input file path for which the AFNI prefix will be extracted
        suffix: Suffix string to append to the returned prefix
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_prefix_params(
        name=name,
        suffix=suffix,
    )
    return get_afni_prefix_execute(params, runner)

__all__ = [
    "GetAfniPrefix",
    "GetAfniPrefixOutputs",
    "GET_AFNI_PREFIX_METADATA",
    "get_afni_prefix_cargs",
    "get_afni_prefix_outputs",
    "get_afni_prefix_params",
    "get_afni_prefix_execute",
    "get_afni_prefix_validate",
    "get_afni_prefix",
]