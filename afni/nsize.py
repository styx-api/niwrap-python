# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NSIZE_METADATA = Metadata(
    id="nsize",
    name="nsize",
    package="afni",
)

Nsize = typing.TypedDict(
    "Nsize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/nsize"]],
        "image_in": InputPathType,
        "image_out": str,
    },
)

@dataclasses.dataclass
class NsizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    image_out_file: OutputPathType
    """Zero padded output image file"""

def nsize_params(
    image_in: InputPathType,
    image_out: str,
) -> Nsize:
    """
    Build parameters.

    Args:
        image_in: Input image file
        image_out: Output padded image file

    Returns:
        Parameter dictionary.
    """
    params: Nsize = {
        "@type": "afni/nsize",
        "image_in": image_in,
        "image_out": image_out,
    }
    return params

def nsize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Nsize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_in", None) is None:
        raise StyxValidationError("`image_in` must not be None")
    if not isinstance(params["image_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image_in` has the wrong type: Received `{type(params["image_in"])}` expected `InputPathType`')
    if params.get("image_out", None) is None:
        raise StyxValidationError("`image_out` must not be None")
    if not isinstance(params["image_out"], str):
        raise StyxValidationError(f'`image_out` has the wrong type: Received `{type(params["image_out"])}` expected `str`')

def nsize_cargs(params: Nsize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("nsize")
    cargs.append(execution.input_file(params["image_in"]))
    cargs.append(params["image_out"])
    return cargs

def nsize_outputs(params: Nsize, execution: Execution) -> NsizeOutputs:
    """Build the NsizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    image_out_file_v: OutputPathType = execution.output_file(params["image_out"])
    return NsizeOutputs(
        root=root_v,
        image_out_file=image_out_file_v,
    )

def nsize_execute(params: Nsize, runner: Runner | None = None) -> NsizeOutputs:
    """
    nsize

    Zero pads an input image to the nearest larger NxN dimensions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    nsize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NSIZE_METADATA)
    execution.params(params)
    args = nsize_cargs(params, execution)
    out = nsize_outputs(params, execution)
    execution.run(args)
    return out

def nsize(
    image_in: InputPathType,
    image_out: str,
    runner: Runner | None = None,
) -> NsizeOutputs:
    """
    nsize

    Zero pads an input image to the nearest larger NxN dimensions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        image_in: Input image file
        image_out: Output padded image file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = nsize_params(
        image_in=image_in,
        image_out=image_out,
    )
    return nsize_execute(params, runner)

__all__ = [
    "Nsize",
    "NsizeOutputs",
    "NSIZE_METADATA",
    "nsize_cargs",
    "nsize_outputs",
    "nsize_params",
    "nsize_execute",
    "nsize_validate",
    "nsize",
]