# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SFIM_METADATA = Metadata(
    id="sfim",
    name="sfim",
    package="afni",
)

Sfim = typing.TypedDict(
    "Sfim",
    {
        "@type": typing.NotRequired[typing.Literal["afni/sfim"]],
        "input_images": list[InputPathType],
        "sfint_file": typing.NotRequired[str],
        "baseline_state": typing.NotRequired[str],
        "local_base_option": typing.NotRequired[bool],
        "output_prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SfimOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def sfim_params(
    input_images: list[InputPathType],
    sfint_file: str | None = None,
    baseline_state: str | None = None,
    local_base_option: bool = False,
    output_prefix: str | None = None,
) -> Sfim:
    """
    Build parameters.

    Args:
        input_images: Input image files in formats accepted by AFNI
        sfint_file: Filename which contains the interval definitions. Default is\
            'sfint'. Example: '3*# 5*rest 4*A 5*rest 4*B 5*rest 4*A 5*rest'
        baseline_state: Task state name to use as the baseline. Default is 'rest'.
        local_base_option: Flag to indicate if each non-base task state interval\
            should have the mean of the two nearest base intervals subtracted\
            instead of the grand mean of all the base task intervals.
        output_prefix: Prefix for output image filenames for all states. The i'th\
            interval with task state name 'fred' will be written to file\
            'pname.fred.i'. Default is 'sfim'.

    Returns:
        Parameter dictionary.
    """
    params: Sfim = {
        "@type": "afni/sfim",
        "input_images": input_images,
        "local_base_option": local_base_option,
    }
    if sfint_file is not None:
        params["sfint_file"] = sfint_file
    if baseline_state is not None:
        params["baseline_state"] = baseline_state
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    return params

def sfim_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sfim."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("sfint_file", None) is not None:
        if not isinstance(params["sfint_file"], str):
            raise StyxValidationError(f'`sfint_file` has the wrong type: Received `{type(params["sfint_file"])}` expected `str`')
    if params.get("baseline_state", None) is not None:
        if not isinstance(params["baseline_state"], str):
            raise StyxValidationError(f'`baseline_state` has the wrong type: Received `{type(params["baseline_state"])}` expected `str`')
    if params.get("local_base_option", None) is not None:
        if not isinstance(params["local_base_option"], bool):
            raise StyxValidationError(f'`local_base_option` has the wrong type: Received `{type(params["local_base_option"])}` expected `bool`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def sfim_cargs(params: Sfim, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sfim")
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("sfint_file")
    if v_0 is not None:
        cargs.append("-sfint")
        cargs.append(v_0)
    v_1 = params.get("baseline_state")
    if v_1 is not None:
        cargs.append("-base")
        cargs.append(v_1)
    if params.get("local_base_option"):
        cargs.append("-localbase")
    v_2 = params.get("output_prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    return cargs

def sfim_outputs(params: Sfim, execution: Execution) -> SfimOutputs:
    """Build the SfimOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SfimOutputs(
        root=root_v,
    )

def sfim_execute(params: Sfim, runner: Runner | None = None) -> SfimOutputs:
    """
    sfim

    Stepwise Functional IMages

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sfim_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SFIM_METADATA)
    execution.params(params)
    args = sfim_cargs(params, execution)
    out = sfim_outputs(params, execution)
    execution.run(args)
    return out

def sfim(
    input_images: list[InputPathType],
    sfint_file: str | None = None,
    baseline_state: str | None = None,
    local_base_option: bool = False,
    output_prefix: str | None = None,
    runner: Runner | None = None,
) -> SfimOutputs:
    """
    sfim

    Stepwise Functional IMages

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_images: Input image files in formats accepted by AFNI
        sfint_file: Filename which contains the interval definitions. Default is\
            'sfint'. Example: '3*# 5*rest 4*A 5*rest 4*B 5*rest 4*A 5*rest'
        baseline_state: Task state name to use as the baseline. Default is 'rest'.
        local_base_option: Flag to indicate if each non-base task state interval\
            should have the mean of the two nearest base intervals subtracted\
            instead of the grand mean of all the base task intervals.
        output_prefix: Prefix for output image filenames for all states. The i'th\
            interval with task state name 'fred' will be written to file\
            'pname.fred.i'. Default is 'sfim'.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sfim_params(
        input_images=input_images,
        sfint_file=sfint_file,
        baseline_state=baseline_state,
        local_base_option=local_base_option,
        output_prefix=output_prefix,
    )
    return sfim_execute(params, runner)

__all__ = [
    "Sfim",
    "SfimOutputs",
    "SFIM_METADATA",
    "sfim_cargs",
    "sfim_outputs",
    "sfim_params",
    "sfim_execute",
    "sfim_validate",
    "sfim",
]