# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BRAIN_SKIN_METADATA = Metadata(
    id="BrainSkin",
    name="BrainSkin",
    package="afni",
)

BrainSkin = typing.TypedDict(
    "BrainSkin",
    {
        "@type": typing.NotRequired[typing.Literal["afni/BrainSkin"]],
        "surface": str,
        "skingrid_volume": InputPathType,
        "prefix": str,
        "plimit": typing.NotRequired[float],
        "dlimit": typing.NotRequired[float],
        "segdo": typing.NotRequired[str],
        "voxelize": typing.NotRequired[str],
        "infill": typing.NotRequired[str],
        "out_file": typing.NotRequired[InputPathType],
        "vol_skin": typing.NotRequired[InputPathType],
        "vol_hull": typing.NotRequired[InputPathType],
        "no_zero_attraction": typing.NotRequired[bool],
        "node_dbg": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class BrainSkinOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stitch_surface: OutputPathType
    """A bunch of triangles for closing the surface."""
    initial_skin_surface: OutputPathType
    """Initial skin surface"""
    reduced_skin_surface: OutputPathType
    """Reduced mesh version of initial skin surface."""
    inflated_skin_surface: OutputPathType
    """Original surface inflated inside skin surface."""
    patching_voxels: OutputPathType
    """Surface patching voxels."""
    surf_voxels: OutputPathType
    """Voxels inside original surface"""
    skin_voxels: OutputPathType
    """Mix of ptchvox and surfvox."""
    infilled_voxels: OutputPathType
    """Skin vox dataset filled in."""
    node_pairs_results: OutputPathType
    """Results of computations for finding node pairs that span sulci."""
    inflating_surface_results: OutputPathType
    """Results of computations for inflating initial surface inside skin surface."""
    segments_display: OutputPathType
    """Segments between node pairs spanning sulci."""

def brain_skin_params(
    surface: str,
    skingrid_volume: InputPathType,
    prefix: str,
    plimit: float | None = None,
    dlimit: float | None = None,
    segdo: str | None = None,
    voxelize: str | None = None,
    infill: str | None = None,
    out_file: InputPathType | None = None,
    vol_skin: InputPathType | None = None,
    vol_hull: InputPathType | None = None,
    no_zero_attraction: bool = False,
    node_dbg: float | None = None,
) -> BrainSkin:
    """
    Build parameters.

    Args:
        surface: Surface to smooth or the domain over which DSET is defined.
        skingrid_volume: A high-res volume to provide a grid for voxelization\
            steps.
        prefix: Prefix to use for variety of output files.
        plimit: Maximum length of path along surface in mm for node pairing.
        dlimit: Maximum length of Euclidean distance in mm for node pairing.
        segdo: Output a displayable object file that contains segments between\
            paired nodes.
        voxelize: Voxelization method. Choose from: slow: Sure footed but slow,\
            fast: Faster and works OK, mask: Fastest and works OK too (default).
        infill: Infill method. Choose from: slow: proper infill, but not needed,\
            fast: brutish infill, all we need (default).
        out_file: Output intermediary results from skin forming step.
        vol_skin: Deform an Icosahedron to match the outer boundary of a mask\
            volume.
        vol_hull: Deform an Icosahedron to match the convex hull of a mask volume.
        no_zero_attraction: With vol_skin, the surface will try to shrink\
            aggressively, even if there is no promise of non-zero values below.
        node_dbg: Output debugging information for node N for -vol_skin and\
            -vol_hull options.

    Returns:
        Parameter dictionary.
    """
    params: BrainSkin = {
        "@type": "afni/BrainSkin",
        "surface": surface,
        "skingrid_volume": skingrid_volume,
        "prefix": prefix,
        "no_zero_attraction": no_zero_attraction,
    }
    if plimit is not None:
        params["plimit"] = plimit
    if dlimit is not None:
        params["dlimit"] = dlimit
    if segdo is not None:
        params["segdo"] = segdo
    if voxelize is not None:
        params["voxelize"] = voxelize
    if infill is not None:
        params["infill"] = infill
    if out_file is not None:
        params["out_file"] = out_file
    if vol_skin is not None:
        params["vol_skin"] = vol_skin
    if vol_hull is not None:
        params["vol_hull"] = vol_hull
    if node_dbg is not None:
        params["node_dbg"] = node_dbg
    return params

def brain_skin_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BrainSkin."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], str):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `str`')
    if params.get("skingrid_volume", None) is None:
        raise StyxValidationError("`skingrid_volume` must not be None")
    if not isinstance(params["skingrid_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`skingrid_volume` has the wrong type: Received `{type(params["skingrid_volume"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("plimit", None) is not None:
        if not isinstance(params["plimit"], (float, int)):
            raise StyxValidationError(f'`plimit` has the wrong type: Received `{type(params["plimit"])}` expected `float`')
    if params.get("dlimit", None) is not None:
        if not isinstance(params["dlimit"], (float, int)):
            raise StyxValidationError(f'`dlimit` has the wrong type: Received `{type(params["dlimit"])}` expected `float`')
    if params.get("segdo", None) is not None:
        if not isinstance(params["segdo"], str):
            raise StyxValidationError(f'`segdo` has the wrong type: Received `{type(params["segdo"])}` expected `str`')
    if params.get("voxelize", None) is not None:
        if not isinstance(params["voxelize"], str):
            raise StyxValidationError(f'`voxelize` has the wrong type: Received `{type(params["voxelize"])}` expected `str`')
    if params.get("infill", None) is not None:
        if not isinstance(params["infill"], str):
            raise StyxValidationError(f'`infill` has the wrong type: Received `{type(params["infill"])}` expected `str`')
    if params.get("out_file", None) is not None:
        if not isinstance(params["out_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`out_file` has the wrong type: Received `{type(params["out_file"])}` expected `InputPathType`')
    if params.get("vol_skin", None) is not None:
        if not isinstance(params["vol_skin"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_skin` has the wrong type: Received `{type(params["vol_skin"])}` expected `InputPathType`')
    if params.get("vol_hull", None) is not None:
        if not isinstance(params["vol_hull"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_hull` has the wrong type: Received `{type(params["vol_hull"])}` expected `InputPathType`')
    if params.get("no_zero_attraction", None) is not None:
        if not isinstance(params["no_zero_attraction"], bool):
            raise StyxValidationError(f'`no_zero_attraction` has the wrong type: Received `{type(params["no_zero_attraction"])}` expected `bool`')
    if params.get("node_dbg", None) is not None:
        if not isinstance(params["node_dbg"], (float, int)):
            raise StyxValidationError(f'`node_dbg` has the wrong type: Received `{type(params["node_dbg"])}` expected `float`')

def brain_skin_cargs(params: BrainSkin, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("BrainSkin")
    cargs.append(params["surface"])
    cargs.append("-skingrid")
    cargs.append(execution.input_file(params["skingrid_volume"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("plimit")
    if v_0 is not None:
        cargs.append("-plimit")
        cargs.append(str(v_0))
    v_1 = params.get("dlimit")
    if v_1 is not None:
        cargs.append("-dlimit")
        cargs.append(str(v_1))
    v_2 = params.get("segdo")
    if v_2 is not None:
        cargs.append("-segdo")
        cargs.append(v_2)
    v_3 = params.get("voxelize")
    if v_3 is not None:
        cargs.append("-voxelize")
        cargs.append(v_3)
    v_4 = params.get("infill")
    if v_4 is not None:
        cargs.append("-infill")
        cargs.append(v_4)
    v_5 = params.get("out_file")
    if v_5 is not None:
        cargs.append("-out")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("vol_skin")
    if v_6 is not None:
        cargs.append("-vol_skin")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("vol_hull")
    if v_7 is not None:
        cargs.append("-vol_hull")
        cargs.append(execution.input_file(v_7))
    if params.get("no_zero_attraction"):
        cargs.append("-no_zero_attraction")
    v_8 = params.get("node_dbg")
    if v_8 is not None:
        cargs.append("-node_dbg")
        cargs.append(str(v_8))
    return cargs

def brain_skin_outputs(params: BrainSkin, execution: Execution) -> BrainSkinOutputs:
    """Build the BrainSkinOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stitch_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}.stitch.gii')
    initial_skin_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}.skin.gii')
    reduced_skin_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}.skin_simp.gii')
    inflated_skin_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}.skin.isotopic.gii')
    patching_voxels_v: OutputPathType = execution.output_file(f'{params["prefix"]}.ptchvox+orig')
    surf_voxels_v: OutputPathType = execution.output_file(f'{params["prefix"]}.surfvox+orig')
    skin_voxels_v: OutputPathType = execution.output_file(f'{params["prefix"]}.skinvox+orig')
    infilled_voxels_v: OutputPathType = execution.output_file(f'{params["prefix"]}.infilled+orig')
    node_pairs_results_v: OutputPathType = execution.output_file(f'{params["prefix"]}.niml.dset')
    inflating_surface_results_v: OutputPathType = execution.output_file(f'{params["prefix"]}.areas.niml.dset')
    segments_display_v: OutputPathType = execution.output_file(f'{params["prefix"]}.1D.do')
    return BrainSkinOutputs(
        root=root_v,
        stitch_surface=stitch_surface_v,
        initial_skin_surface=initial_skin_surface_v,
        reduced_skin_surface=reduced_skin_surface_v,
        inflated_skin_surface=inflated_skin_surface_v,
        patching_voxels=patching_voxels_v,
        surf_voxels=surf_voxels_v,
        skin_voxels=skin_voxels_v,
        infilled_voxels=infilled_voxels_v,
        node_pairs_results=node_pairs_results_v,
        inflating_surface_results=inflating_surface_results_v,
        segments_display=segments_display_v,
    )

def brain_skin_execute(params: BrainSkin, runner: Runner | None = None) -> BrainSkinOutputs:
    """
    BrainSkin

    A program to create an unfolded surface that wraps the brain (skin) and Gyrification Indices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    brain_skin_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BRAIN_SKIN_METADATA)
    execution.params(params)
    args = brain_skin_cargs(params, execution)
    out = brain_skin_outputs(params, execution)
    execution.run(args)
    return out

def brain_skin(
    surface: str,
    skingrid_volume: InputPathType,
    prefix: str,
    plimit: float | None = None,
    dlimit: float | None = None,
    segdo: str | None = None,
    voxelize: str | None = None,
    infill: str | None = None,
    out_file: InputPathType | None = None,
    vol_skin: InputPathType | None = None,
    vol_hull: InputPathType | None = None,
    no_zero_attraction: bool = False,
    node_dbg: float | None = None,
    runner: Runner | None = None,
) -> BrainSkinOutputs:
    """
    BrainSkin

    A program to create an unfolded surface that wraps the brain (skin) and Gyrification Indices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        surface: Surface to smooth or the domain over which DSET is defined.
        skingrid_volume: A high-res volume to provide a grid for voxelization\
            steps.
        prefix: Prefix to use for variety of output files.
        plimit: Maximum length of path along surface in mm for node pairing.
        dlimit: Maximum length of Euclidean distance in mm for node pairing.
        segdo: Output a displayable object file that contains segments between\
            paired nodes.
        voxelize: Voxelization method. Choose from: slow: Sure footed but slow,\
            fast: Faster and works OK, mask: Fastest and works OK too (default).
        infill: Infill method. Choose from: slow: proper infill, but not needed,\
            fast: brutish infill, all we need (default).
        out_file: Output intermediary results from skin forming step.
        vol_skin: Deform an Icosahedron to match the outer boundary of a mask\
            volume.
        vol_hull: Deform an Icosahedron to match the convex hull of a mask volume.
        no_zero_attraction: With vol_skin, the surface will try to shrink\
            aggressively, even if there is no promise of non-zero values below.
        node_dbg: Output debugging information for node N for -vol_skin and\
            -vol_hull options.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = brain_skin_params(
        surface=surface,
        skingrid_volume=skingrid_volume,
        prefix=prefix,
        plimit=plimit,
        dlimit=dlimit,
        segdo=segdo,
        voxelize=voxelize,
        infill=infill,
        out_file=out_file,
        vol_skin=vol_skin,
        vol_hull=vol_hull,
        no_zero_attraction=no_zero_attraction,
        node_dbg=node_dbg,
    )
    return brain_skin_execute(params, runner)

__all__ = [
    "BrainSkin",
    "BrainSkinOutputs",
    "BRAIN_SKIN_METADATA",
    "brain_skin_cargs",
    "brain_skin_outputs",
    "brain_skin_params",
    "brain_skin_execute",
    "brain_skin_validate",
    "brain_skin",
]