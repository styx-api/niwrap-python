# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PROMPT_USER_METADATA = Metadata(
    id="prompt_user",
    name="prompt_user",
    package="afni",
)

PromptUser = typing.TypedDict(
    "PromptUser",
    {
        "@type": typing.NotRequired[typing.Literal["afni/prompt_user"]],
        "pause_message": str,
        "timeout": typing.NotRequired[float],
        "timeout_alias": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class PromptUserOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def prompt_user_params(
    pause_message: str,
    timeout: float | None = None,
    timeout_alias: float | None = None,
) -> PromptUser:
    """
    Build parameters.

    Args:
        pause_message: Pops a window prompting the user with MESSAGE. If MESSAGE is\
            '-', it is read from stdin.
        timeout: Timeout in seconds for the prompt message. Default answer is\
            returned if TT seconds elapse without user input.
        timeout_alias: Alias for -timeout

    Returns:
        Parameter dictionary.
    """
    params: PromptUser = {
        "@type": "afni/prompt_user",
        "pause_message": pause_message,
    }
    if timeout is not None:
        params["timeout"] = timeout
    if timeout_alias is not None:
        params["timeout_alias"] = timeout_alias
    return params

def prompt_user_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PromptUser."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("pause_message", None) is None:
        raise StyxValidationError("`pause_message` must not be None")
    if not isinstance(params["pause_message"], str):
        raise StyxValidationError(f'`pause_message` has the wrong type: Received `{type(params["pause_message"])}` expected `str`')
    if params.get("timeout", None) is not None:
        if not isinstance(params["timeout"], (float, int)):
            raise StyxValidationError(f'`timeout` has the wrong type: Received `{type(params["timeout"])}` expected `float`')
    if params.get("timeout_alias", None) is not None:
        if not isinstance(params["timeout_alias"], (float, int)):
            raise StyxValidationError(f'`timeout_alias` has the wrong type: Received `{type(params["timeout_alias"])}` expected `float`')

def prompt_user_cargs(params: PromptUser, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("prompt_user")
    cargs.append("<-pause>")
    cargs.append(params["pause_message"])
    v_0 = params.get("timeout")
    if v_0 is not None:
        cargs.append("-timeout")
        cargs.append(str(v_0))
    v_1 = params.get("timeout_alias")
    if v_1 is not None:
        cargs.append("-to")
        cargs.append(str(v_1))
    return cargs

def prompt_user_outputs(params: PromptUser, execution: Execution) -> PromptUserOutputs:
    """Build the PromptUserOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PromptUserOutputs(
        root=root_v,
    )

def prompt_user_execute(params: PromptUser, runner: Runner | None = None) -> PromptUserOutputs:
    """
    prompt_user

    Tool that prompts a window requesting user input with a custom message.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    prompt_user_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PROMPT_USER_METADATA)
    execution.params(params)
    args = prompt_user_cargs(params, execution)
    out = prompt_user_outputs(params, execution)
    execution.run(args)
    return out

def prompt_user(
    pause_message: str,
    timeout: float | None = None,
    timeout_alias: float | None = None,
    runner: Runner | None = None,
) -> PromptUserOutputs:
    """
    prompt_user

    Tool that prompts a window requesting user input with a custom message.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        pause_message: Pops a window prompting the user with MESSAGE. If MESSAGE is\
            '-', it is read from stdin.
        timeout: Timeout in seconds for the prompt message. Default answer is\
            returned if TT seconds elapse without user input.
        timeout_alias: Alias for -timeout
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = prompt_user_params(
        pause_message=pause_message,
        timeout=timeout,
        timeout_alias=timeout_alias,
    )
    return prompt_user_execute(params, runner)

__all__ = [
    "PromptUser",
    "PromptUserOutputs",
    "PROMPT_USER_METADATA",
    "prompt_user_cargs",
    "prompt_user_outputs",
    "prompt_user_params",
    "prompt_user_execute",
    "prompt_user_validate",
    "prompt_user",
]