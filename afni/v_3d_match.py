# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_MATCH_METADATA = Metadata(
    id="3dMatch",
    name="3dMatch",
    package="afni",
)

V3dMatch = typing.TypedDict(
    "V3dMatch",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dMatch"]],
        "inset": InputPathType,
        "refset": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "in_min": typing.NotRequired[float],
        "in_max": typing.NotRequired[float],
        "ref_min": typing.NotRequired[float],
        "ref_max": typing.NotRequired[float],
        "prefix": str,
        "only_dice_thr": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dMatchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    ref_brik: OutputPathType
    """AFNI BRIK file with the same number of subbricks as the refset file, with highest weighted correlation."""
    ref_coeff_vals: OutputPathType
    """Text file recording original indices and coefficients."""
    in_brik: OutputPathType
    """AFNI BRIK file with the same number of subbricks as the inset file, with highest weighted correlation."""
    in_coeff_vals: OutputPathType
    """Text file recording original indices and coefficients."""

def v_3d_match_params(
    inset: InputPathType,
    refset: InputPathType,
    prefix: str,
    mask: InputPathType | None = None,
    in_min: float | None = None,
    in_max: float | None = None,
    ref_min: float | None = None,
    ref_max: float | None = None,
    only_dice_thr: bool = False,
) -> V3dMatch:
    """
    Build parameters.

    Args:
        inset: File with M subbricks of data to match against another file.
        refset: File with N subbricks, serving as a reference for INPUT_FILE.
        prefix: Prefix for output name for BRIK/HEAD files and *_coeff.vals text\
            files.
        mask: A mask of regions to include in the correlation of datasets.
        in_min: Threshold below which values in INPUT_FILE will be zeroed during\
            analysis.
        in_max: Threshold above which values in INPUT_FILE will be zeroed during\
            analysis.
        ref_min: Threshold below which values in REF_FILE will be zeroed during\
            analysis.
        ref_max: Threshold above which values in REF_FILE will be zeroed during\
            analysis.
        only_dice_thr: Apply thresholding only during Dice evaluation, not during\
            spatial correlation.

    Returns:
        Parameter dictionary.
    """
    params: V3dMatch = {
        "@type": "afni/3dMatch",
        "inset": inset,
        "refset": refset,
        "prefix": prefix,
        "only_dice_thr": only_dice_thr,
    }
    if mask is not None:
        params["mask"] = mask
    if in_min is not None:
        params["in_min"] = in_min
    if in_max is not None:
        params["in_max"] = in_max
    if ref_min is not None:
        params["ref_min"] = ref_min
    if ref_max is not None:
        params["ref_max"] = ref_max
    return params

def v_3d_match_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dMatch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("refset", None) is None:
        raise StyxValidationError("`refset` must not be None")
    if not isinstance(params["refset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`refset` has the wrong type: Received `{type(params["refset"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("in_min", None) is not None:
        if not isinstance(params["in_min"], (float, int)):
            raise StyxValidationError(f'`in_min` has the wrong type: Received `{type(params["in_min"])}` expected `float`')
    if params.get("in_max", None) is not None:
        if not isinstance(params["in_max"], (float, int)):
            raise StyxValidationError(f'`in_max` has the wrong type: Received `{type(params["in_max"])}` expected `float`')
    if params.get("ref_min", None) is not None:
        if not isinstance(params["ref_min"], (float, int)):
            raise StyxValidationError(f'`ref_min` has the wrong type: Received `{type(params["ref_min"])}` expected `float`')
    if params.get("ref_max", None) is not None:
        if not isinstance(params["ref_max"], (float, int)):
            raise StyxValidationError(f'`ref_max` has the wrong type: Received `{type(params["ref_max"])}` expected `float`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("only_dice_thr", None) is not None:
        if not isinstance(params["only_dice_thr"], bool):
            raise StyxValidationError(f'`only_dice_thr` has the wrong type: Received `{type(params["only_dice_thr"])}` expected `bool`')

def v_3d_match_cargs(params: V3dMatch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dMatch")
    cargs.append("-inset")
    cargs.append(execution.input_file(params["inset"]))
    cargs.append("-refset")
    cargs.append(execution.input_file(params["refset"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("in_min")
    if v_1 is not None:
        cargs.append("-in_min")
        cargs.append(str(v_1))
    v_2 = params.get("in_max")
    if v_2 is not None:
        cargs.append("-in_max")
        cargs.append(str(v_2))
    v_3 = params.get("ref_min")
    if v_3 is not None:
        cargs.append("-ref_min")
        cargs.append(str(v_3))
    v_4 = params.get("ref_max")
    if v_4 is not None:
        cargs.append("-ref_max")
        cargs.append(str(v_4))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("only_dice_thr"):
        cargs.append("-only_dice_thr")
    return cargs

def v_3d_match_outputs(params: V3dMatch, execution: Execution) -> V3dMatchOutputs:
    """Build the V3dMatchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    ref_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}_REF+orig.BRIK')
    ref_coeff_vals_v: OutputPathType = execution.output_file(f'{params["prefix"]}_REF_coeff.vals')
    in_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}_IN+orig.BRIK')
    in_coeff_vals_v: OutputPathType = execution.output_file(f'{params["prefix"]}_IN_coeff.vals')
    return V3dMatchOutputs(
        root=root_v,
        ref_brik=ref_brik_v,
        ref_coeff_vals=ref_coeff_vals_v,
        in_brik=in_brik_v,
        in_coeff_vals=in_coeff_vals_v,
    )

def v_3d_match_execute(params: V3dMatch, runner: Runner | None = None) -> V3dMatchOutputs:
    """
    3dMatch

    Find similar subbricks and rearrange order to ease comparison. Part of FATCAT in AFNI.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_match_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_MATCH_METADATA)
    execution.params(params)
    args = v_3d_match_cargs(params, execution)
    out = v_3d_match_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_match(
    inset: InputPathType,
    refset: InputPathType,
    prefix: str,
    mask: InputPathType | None = None,
    in_min: float | None = None,
    in_max: float | None = None,
    ref_min: float | None = None,
    ref_max: float | None = None,
    only_dice_thr: bool = False,
    runner: Runner | None = None,
) -> V3dMatchOutputs:
    """
    3dMatch

    Find similar subbricks and rearrange order to ease comparison. Part of FATCAT in AFNI.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inset: File with M subbricks of data to match against another file.
        refset: File with N subbricks, serving as a reference for INPUT_FILE.
        prefix: Prefix for output name for BRIK/HEAD files and *_coeff.vals text\
            files.
        mask: A mask of regions to include in the correlation of datasets.
        in_min: Threshold below which values in INPUT_FILE will be zeroed during\
            analysis.
        in_max: Threshold above which values in INPUT_FILE will be zeroed during\
            analysis.
        ref_min: Threshold below which values in REF_FILE will be zeroed during\
            analysis.
        ref_max: Threshold above which values in REF_FILE will be zeroed during\
            analysis.
        only_dice_thr: Apply thresholding only during Dice evaluation, not during\
            spatial correlation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_match_params(
        inset=inset,
        refset=refset,
        prefix=prefix,
        mask=mask,
        in_min=in_min,
        in_max=in_max,
        ref_min=ref_min,
        ref_max=ref_max,
        only_dice_thr=only_dice_thr,
    )
    return v_3d_match_execute(params, runner)

__all__ = [
    "V3dMatch",
    "V3dMatchOutputs",
    "V_3D_MATCH_METADATA",
    "v_3d_match_cargs",
    "v_3d_match_outputs",
    "v_3d_match_params",
    "v_3d_match_execute",
    "v_3d_match_validate",
    "v_3d_match",
]