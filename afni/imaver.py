# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAVER_METADATA = Metadata(
    id="imaver",
    name="imaver",
    package="afni",
)

Imaver = typing.TypedDict(
    "Imaver",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imaver"]],
        "out_ave": typing.NotRequired[str],
        "out_sig": typing.NotRequired[str],
        "input_images": list[InputPathType],
    },
)

@dataclasses.dataclass
class ImaverOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_ave_output: OutputPathType | None
    """Output image file for averages (optional)"""
    out_sig_output: OutputPathType | None
    """Output image file for standard deviations (optional)"""

def imaver_params(
    input_images: list[InputPathType],
    out_ave: str | None = None,
    out_sig: str | None = None,
) -> Imaver:
    """
    Build parameters.

    Args:
        input_images: Input image files for processing
        out_ave: Output average image file. Use '-' to skip output.
        out_sig: Output standard deviation image file. Use '-' to skip output.

    Returns:
        Parameter dictionary.
    """
    params: Imaver = {
        "@type": "afni/imaver",
        "input_images": input_images,
    }
    if out_ave is not None:
        params["out_ave"] = out_ave
    if out_sig is not None:
        params["out_sig"] = out_sig
    return params

def imaver_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imaver."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("out_ave", None) is not None:
        if not isinstance(params["out_ave"], str):
            raise StyxValidationError(f'`out_ave` has the wrong type: Received `{type(params["out_ave"])}` expected `str`')
    if params.get("out_sig", None) is not None:
        if not isinstance(params["out_sig"], str):
            raise StyxValidationError(f'`out_sig` has the wrong type: Received `{type(params["out_sig"])}` expected `str`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def imaver_cargs(params: Imaver, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imaver")
    v_0 = params.get("out_ave")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("out_sig")
    if v_1 is not None:
        cargs.append(v_1)
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    return cargs

def imaver_outputs(params: Imaver, execution: Execution) -> ImaverOutputs:
    """Build the ImaverOutputs object for this tool."""
    out_ave_output_v: OutputPathType | None = None
    out_sig_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_ave")
    if __v0 is not None:
        out_ave_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("out_sig")
    if __v1 is not None:
        out_sig_output_v = execution.output_file((__v1 if __v1 is not None else ""))
    return ImaverOutputs(
        root=root_v,
        out_ave_output=out_ave_output_v,
        out_sig_output=out_sig_output_v,
    )

def imaver_execute(params: Imaver, runner: Runner | None = None) -> ImaverOutputs:
    """
    imaver

    Computes the mean and standard deviation, pixel-by-pixel, of a whole bunch of images.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imaver_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAVER_METADATA)
    execution.params(params)
    args = imaver_cargs(params, execution)
    out = imaver_outputs(params, execution)
    execution.run(args)
    return out

def imaver(
    input_images: list[InputPathType],
    out_ave: str | None = None,
    out_sig: str | None = None,
    runner: Runner | None = None,
) -> ImaverOutputs:
    """
    imaver

    Computes the mean and standard deviation, pixel-by-pixel, of a whole bunch of images.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_images: Input image files for processing
        out_ave: Output average image file. Use '-' to skip output.
        out_sig: Output standard deviation image file. Use '-' to skip output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imaver_params(
        input_images=input_images,
        out_ave=out_ave,
        out_sig=out_sig,
    )
    return imaver_execute(params, runner)

__all__ = [
    "Imaver",
    "ImaverOutputs",
    "IMAVER_METADATA",
    "imaver_cargs",
    "imaver_outputs",
    "imaver_params",
    "imaver_execute",
    "imaver_validate",
    "imaver",
]