# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AFNI_ORIENT_SIGN_METADATA = Metadata(
    id="@AfniOrientSign",
    name="@AfniOrientSign",
    package="afni",
)

AfniOrientSign = typing.TypedDict(
    "AfniOrientSign",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@AfniOrientSign"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class AfniOrientSignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output file containing the orientation signs of the dataset"""

def afni_orient_sign_params(
    infile: InputPathType,
) -> AfniOrientSign:
    """
    Build parameters.

    Args:
        infile:

    Returns:
        Parameter dictionary.
    """
    params: AfniOrientSign = {
        "@type": "afni/@AfniOrientSign",
        "infile": infile,
    }
    return params

def afni_orient_sign_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AfniOrientSign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def afni_orient_sign_cargs(params: AfniOrientSign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@AfniOrientSign")
    cargs.append("-orient")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def afni_orient_sign_outputs(params: AfniOrientSign, execution: Execution) -> AfniOrientSignOutputs:
    """Build the AfniOrientSignOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(f'{params["infile"]}_orient.txt')
    return AfniOrientSignOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def afni_orient_sign_execute(params: AfniOrientSign, runner: Runner | None = None) -> AfniOrientSignOutputs:
    """
    @AfniOrientSign

    A tool within the AFNI suite to determine the orientation signs of datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    afni_orient_sign_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AFNI_ORIENT_SIGN_METADATA)
    execution.params(params)
    args = afni_orient_sign_cargs(params, execution)
    out = afni_orient_sign_outputs(params, execution)
    execution.run(args)
    return out

def afni_orient_sign(
    infile: InputPathType,
    runner: Runner | None = None,
) -> AfniOrientSignOutputs:
    """
    @AfniOrientSign

    A tool within the AFNI suite to determine the orientation signs of datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = afni_orient_sign_params(
        infile=infile,
    )
    return afni_orient_sign_execute(params, runner)

__all__ = [
    "AfniOrientSign",
    "AfniOrientSignOutputs",
    "AFNI_ORIENT_SIGN_METADATA",
    "afni_orient_sign_cargs",
    "afni_orient_sign_outputs",
    "afni_orient_sign_params",
    "afni_orient_sign_execute",
    "afni_orient_sign_validate",
    "afni_orient_sign",
]