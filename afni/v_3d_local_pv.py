# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_LOCAL_PV_METADATA = Metadata(
    id="3dLocalPV",
    name="3dLocalPV",
    package="afni",
)

V3dLocalPv = typing.TypedDict(
    "V3dLocalPv",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dLocalPV"]],
        "input_dataset": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "prefix2": typing.NotRequired[str],
        "evprefix": typing.NotRequired[str],
        "neighborhood": typing.NotRequired[str],
        "despike": typing.NotRequired[bool],
        "polort": typing.NotRequired[float],
        "vnorm": typing.NotRequired[bool],
        "vproj": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dLocalPvOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    svd_vector_result: OutputPathType | None
    """SVD vector result dataset"""
    second_principal_vector: OutputPathType | None
    """Second principal vector dataset"""
    singular_value: OutputPathType | None
    """Singular value at each voxel dataset"""

def v_3d_local_pv_params(
    input_dataset: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    prefix: str | None = None,
    prefix2: str | None = None,
    evprefix: str | None = None,
    neighborhood: str | None = None,
    despike: bool = False,
    polort: float | None = None,
    vnorm: bool = False,
    vproj: str | None = None,
) -> V3dLocalPv:
    """
    Build parameters.

    Args:
        input_dataset: Input time series dataset
        mask: Restrict operations to this mask
        automask: Create a mask from the time series dataset
        prefix: Save SVD vector result into this new dataset [default = 'LocalPV']
        prefix2: Save second principal vector into this new dataset [default =\
            don't save it]
        evprefix: Save singular value at each voxel into this dataset [default =\
            don't save]
        neighborhood: Neighborhood definition (e.g., 'SPHERE(5)', 'TOHD(7)', etc.)
        despike: Remove time series spikes from input dataset
        polort: Detrending
        vnorm: Normalize data vectors [strongly recommended]
        vproj: Project central data time series onto local SVD vector; if followed\
            by '2', then the central data time series will be projected on the\
            2-dimensional subspace spanned by the first 2 principal SVD vectors.\
            [default: just output principal singular vector]

    Returns:
        Parameter dictionary.
    """
    params: V3dLocalPv = {
        "@type": "afni/3dLocalPV",
        "input_dataset": input_dataset,
        "automask": automask,
        "despike": despike,
        "vnorm": vnorm,
    }
    if mask is not None:
        params["mask"] = mask
    if prefix is not None:
        params["prefix"] = prefix
    if prefix2 is not None:
        params["prefix2"] = prefix2
    if evprefix is not None:
        params["evprefix"] = evprefix
    if neighborhood is not None:
        params["neighborhood"] = neighborhood
    if polort is not None:
        params["polort"] = polort
    if vproj is not None:
        params["vproj"] = vproj
    return params

def v_3d_local_pv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dLocalPv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("prefix2", None) is not None:
        if not isinstance(params["prefix2"], str):
            raise StyxValidationError(f'`prefix2` has the wrong type: Received `{type(params["prefix2"])}` expected `str`')
    if params.get("evprefix", None) is not None:
        if not isinstance(params["evprefix"], str):
            raise StyxValidationError(f'`evprefix` has the wrong type: Received `{type(params["evprefix"])}` expected `str`')
    if params.get("neighborhood", None) is not None:
        if not isinstance(params["neighborhood"], str):
            raise StyxValidationError(f'`neighborhood` has the wrong type: Received `{type(params["neighborhood"])}` expected `str`')
    if params.get("despike", None) is not None:
        if not isinstance(params["despike"], bool):
            raise StyxValidationError(f'`despike` has the wrong type: Received `{type(params["despike"])}` expected `bool`')
    if params.get("polort", None) is not None:
        if not isinstance(params["polort"], (float, int)):
            raise StyxValidationError(f'`polort` has the wrong type: Received `{type(params["polort"])}` expected `float`')
    if params.get("vnorm", None) is not None:
        if not isinstance(params["vnorm"], bool):
            raise StyxValidationError(f'`vnorm` has the wrong type: Received `{type(params["vnorm"])}` expected `bool`')
    if params.get("vproj", None) is not None:
        if not isinstance(params["vproj"], str):
            raise StyxValidationError(f'`vproj` has the wrong type: Received `{type(params["vproj"])}` expected `str`')

def v_3d_local_pv_cargs(params: V3dLocalPv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dLocalPV")
    cargs.append(execution.input_file(params["input_dataset"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("automask"):
        cargs.append("-automask")
    v_1 = params.get("prefix")
    if v_1 is not None:
        cargs.append("-prefix")
        cargs.append(v_1)
    v_2 = params.get("prefix2")
    if v_2 is not None:
        cargs.append("-prefix2")
        cargs.append(v_2)
    v_3 = params.get("evprefix")
    if v_3 is not None:
        cargs.append("-evprefix")
        cargs.append(v_3)
    v_4 = params.get("neighborhood")
    if v_4 is not None:
        cargs.append("-nbhd")
        cargs.append(v_4)
    if params.get("despike"):
        cargs.append("-despike")
    v_5 = params.get("polort")
    if v_5 is not None:
        cargs.append("-polort")
        cargs.append(str(v_5))
    if params.get("vnorm"):
        cargs.append("-vnorm")
    v_6 = params.get("vproj")
    if v_6 is not None:
        cargs.append("-vproj")
        cargs.append(v_6)
    return cargs

def v_3d_local_pv_outputs(params: V3dLocalPv, execution: Execution) -> V3dLocalPvOutputs:
    """Build the V3dLocalPvOutputs object for this tool."""
    svd_vector_result_v: OutputPathType | None = None
    second_principal_vector_v: OutputPathType | None = None
    singular_value_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        svd_vector_result_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    __v1 = params.get("prefix2")
    if __v1 is not None:
        second_principal_vector_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.nii.gz')
    __v2 = params.get("evprefix")
    if __v2 is not None:
        singular_value_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.nii.gz')
    return V3dLocalPvOutputs(
        root=root_v,
        svd_vector_result=svd_vector_result_v,
        second_principal_vector=second_principal_vector_v,
        singular_value=singular_value_v,
    )

def v_3d_local_pv_execute(params: V3dLocalPv, runner: Runner | None = None) -> V3dLocalPvOutputs:
    """
    3dLocalPV

    Computes the Singular Value Decomposition (SVD) of the time series from a neighborhood of each voxel in a 3D+time dataset, which serves as a smoothing method for the dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_local_pv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_LOCAL_PV_METADATA)
    execution.params(params)
    args = v_3d_local_pv_cargs(params, execution)
    out = v_3d_local_pv_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_local_pv(
    input_dataset: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    prefix: str | None = None,
    prefix2: str | None = None,
    evprefix: str | None = None,
    neighborhood: str | None = None,
    despike: bool = False,
    polort: float | None = None,
    vnorm: bool = False,
    vproj: str | None = None,
    runner: Runner | None = None,
) -> V3dLocalPvOutputs:
    """
    3dLocalPV

    Computes the Singular Value Decomposition (SVD) of the time series from a neighborhood of each voxel in a 3D+time dataset, which serves as a smoothing method for the dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input time series dataset
        mask: Restrict operations to this mask
        automask: Create a mask from the time series dataset
        prefix: Save SVD vector result into this new dataset [default = 'LocalPV']
        prefix2: Save second principal vector into this new dataset [default =\
            don't save it]
        evprefix: Save singular value at each voxel into this dataset [default =\
            don't save]
        neighborhood: Neighborhood definition (e.g., 'SPHERE(5)', 'TOHD(7)', etc.)
        despike: Remove time series spikes from input dataset
        polort: Detrending
        vnorm: Normalize data vectors [strongly recommended]
        vproj: Project central data time series onto local SVD vector; if followed\
            by '2', then the central data time series will be projected on the\
            2-dimensional subspace spanned by the first 2 principal SVD vectors.\
            [default: just output principal singular vector]
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_local_pv_params(
        input_dataset=input_dataset,
        mask=mask,
        automask=automask,
        prefix=prefix,
        prefix2=prefix2,
        evprefix=evprefix,
        neighborhood=neighborhood,
        despike=despike,
        polort=polort,
        vnorm=vnorm,
        vproj=vproj,
    )
    return v_3d_local_pv_execute(params, runner)

__all__ = [
    "V3dLocalPv",
    "V3dLocalPvOutputs",
    "V_3D_LOCAL_PV_METADATA",
    "v_3d_local_pv_cargs",
    "v_3d_local_pv_outputs",
    "v_3d_local_pv_params",
    "v_3d_local_pv_execute",
    "v_3d_local_pv_validate",
    "v_3d_local_pv",
]