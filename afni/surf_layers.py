# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURF_LAYERS_METADATA = Metadata(
    id="SurfLayers",
    name="SurfLayers",
    package="afni",
)

SurfLayers = typing.TypedDict(
    "SurfLayers",
    {
        "@type": typing.NotRequired[typing.Literal["afni/SurfLayers"]],
        "spec_dset": typing.NotRequired[InputPathType],
        "outdir": typing.NotRequired[str],
        "states": typing.NotRequired[str],
        "hemi": typing.NotRequired[str],
        "n_intermed_surfs": typing.NotRequired[float],
        "surf_a": typing.NotRequired[InputPathType],
        "surf_b": typing.NotRequired[InputPathType],
        "surf_intermed_pref": typing.NotRequired[str],
        "echo": typing.NotRequired[bool],
        "no_clean": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SurfLayersOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dir: OutputPathType | None
    """Directory containing output files"""

def surf_layers_params(
    spec_dset: InputPathType | None = None,
    outdir: str | None = None,
    states: str | None = None,
    hemi: str | None = None,
    n_intermed_surfs: float | None = None,
    surf_a: InputPathType | None = None,
    surf_b: InputPathType | None = None,
    surf_intermed_pref: str | None = None,
    echo: bool = False,
    no_clean: bool = False,
) -> SurfLayers:
    """
    Build parameters.

    Args:
        spec_dset: Dataset that is the SUMA specification file describing input\
            surfaces
        outdir: New directory for output (default: surflayers)
        states: Typically smoothwm, pial states to describe inner and outer\
            surfaces (default: 'smoothwm pial')
        hemi: Choose hemisphere: 'lh', 'rh', or 'lh rh' (for both)
        n_intermed_surfs: Total number of intermediate surfaces to create
        surf_a: Inner boundary surface by filename (e.g., smoothwm.gii)
        surf_b: Outer boundary surface by filename (e.g., pial.gii)
        surf_intermed_pref: Name for interpolated surfaces (default: isurf)
        echo: Run script with 'set echo' (i.e., verbosely)
        no_clean: Do not remove temp files (probably just for testing)

    Returns:
        Parameter dictionary.
    """
    params: SurfLayers = {
        "@type": "afni/SurfLayers",
        "echo": echo,
        "no_clean": no_clean,
    }
    if spec_dset is not None:
        params["spec_dset"] = spec_dset
    if outdir is not None:
        params["outdir"] = outdir
    if states is not None:
        params["states"] = states
    if hemi is not None:
        params["hemi"] = hemi
    if n_intermed_surfs is not None:
        params["n_intermed_surfs"] = n_intermed_surfs
    if surf_a is not None:
        params["surf_a"] = surf_a
    if surf_b is not None:
        params["surf_b"] = surf_b
    if surf_intermed_pref is not None:
        params["surf_intermed_pref"] = surf_intermed_pref
    return params

def surf_layers_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfLayers."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spec_dset", None) is not None:
        if not isinstance(params["spec_dset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`spec_dset` has the wrong type: Received `{type(params["spec_dset"])}` expected `InputPathType`')
    if params.get("outdir", None) is not None:
        if not isinstance(params["outdir"], str):
            raise StyxValidationError(f'`outdir` has the wrong type: Received `{type(params["outdir"])}` expected `str`')
    if params.get("states", None) is not None:
        if not isinstance(params["states"], str):
            raise StyxValidationError(f'`states` has the wrong type: Received `{type(params["states"])}` expected `str`')
    if params.get("hemi", None) is not None:
        if not isinstance(params["hemi"], str):
            raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params["hemi"])}` expected `str`')
    if params.get("n_intermed_surfs", None) is not None:
        if not isinstance(params["n_intermed_surfs"], (float, int)):
            raise StyxValidationError(f'`n_intermed_surfs` has the wrong type: Received `{type(params["n_intermed_surfs"])}` expected `float`')
    if params.get("surf_a", None) is not None:
        if not isinstance(params["surf_a"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surf_a` has the wrong type: Received `{type(params["surf_a"])}` expected `InputPathType`')
    if params.get("surf_b", None) is not None:
        if not isinstance(params["surf_b"], (pathlib.Path, str)):
            raise StyxValidationError(f'`surf_b` has the wrong type: Received `{type(params["surf_b"])}` expected `InputPathType`')
    if params.get("surf_intermed_pref", None) is not None:
        if not isinstance(params["surf_intermed_pref"], str):
            raise StyxValidationError(f'`surf_intermed_pref` has the wrong type: Received `{type(params["surf_intermed_pref"])}` expected `str`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')

def surf_layers_cargs(params: SurfLayers, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SurfLayers")
    v_0 = params.get("spec_dset")
    if v_0 is not None:
        cargs.append("-spec")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("outdir")
    if v_1 is not None:
        cargs.append("-outdir")
        cargs.append(v_1)
    v_2 = params.get("states")
    if v_2 is not None:
        cargs.append("-states")
        cargs.append(v_2)
    v_3 = params.get("hemi")
    if v_3 is not None:
        cargs.append("-hemi")
        cargs.append(v_3)
    v_4 = params.get("n_intermed_surfs")
    if v_4 is not None:
        cargs.append("-n_intermed_surfs")
        cargs.append(str(v_4))
    v_5 = params.get("surf_a")
    if v_5 is not None:
        cargs.append("-surf_A")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("surf_b")
    if v_6 is not None:
        cargs.append("-surf_B")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("surf_intermed_pref")
    if v_7 is not None:
        cargs.append("-surf_intermed_pref")
        cargs.append(v_7)
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("no_clean"):
        cargs.append("-no_clean")
    return cargs

def surf_layers_outputs(params: SurfLayers, execution: Execution) -> SurfLayersOutputs:
    """Build the SurfLayersOutputs object for this tool."""
    output_dir_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outdir")
    if __v0 is not None:
        output_dir_v = execution.output_file((__v0 if __v0 is not None else ""))
    return SurfLayersOutputs(
        root=root_v,
        output_dir=output_dir_v,
    )

def surf_layers_execute(params: SurfLayers, runner: Runner | None = None) -> SurfLayersOutputs:
    """
    SurfLayers

    Compute intermediate equi-distant surfaces between two boundary surfaces.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surf_layers_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURF_LAYERS_METADATA)
    execution.params(params)
    args = surf_layers_cargs(params, execution)
    out = surf_layers_outputs(params, execution)
    execution.run(args)
    return out

def surf_layers(
    spec_dset: InputPathType | None = None,
    outdir: str | None = None,
    states: str | None = None,
    hemi: str | None = None,
    n_intermed_surfs: float | None = None,
    surf_a: InputPathType | None = None,
    surf_b: InputPathType | None = None,
    surf_intermed_pref: str | None = None,
    echo: bool = False,
    no_clean: bool = False,
    runner: Runner | None = None,
) -> SurfLayersOutputs:
    """
    SurfLayers

    Compute intermediate equi-distant surfaces between two boundary surfaces.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        spec_dset: Dataset that is the SUMA specification file describing input\
            surfaces
        outdir: New directory for output (default: surflayers)
        states: Typically smoothwm, pial states to describe inner and outer\
            surfaces (default: 'smoothwm pial')
        hemi: Choose hemisphere: 'lh', 'rh', or 'lh rh' (for both)
        n_intermed_surfs: Total number of intermediate surfaces to create
        surf_a: Inner boundary surface by filename (e.g., smoothwm.gii)
        surf_b: Outer boundary surface by filename (e.g., pial.gii)
        surf_intermed_pref: Name for interpolated surfaces (default: isurf)
        echo: Run script with 'set echo' (i.e., verbosely)
        no_clean: Do not remove temp files (probably just for testing)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surf_layers_params(
        spec_dset=spec_dset,
        outdir=outdir,
        states=states,
        hemi=hemi,
        n_intermed_surfs=n_intermed_surfs,
        surf_a=surf_a,
        surf_b=surf_b,
        surf_intermed_pref=surf_intermed_pref,
        echo=echo,
        no_clean=no_clean,
    )
    return surf_layers_execute(params, runner)

__all__ = [
    "SurfLayers",
    "SurfLayersOutputs",
    "SURF_LAYERS_METADATA",
    "surf_layers_cargs",
    "surf_layers_outputs",
    "surf_layers_params",
    "surf_layers_execute",
    "surf_layers_validate",
    "surf_layers",
]