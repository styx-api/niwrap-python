# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ABIDS_TOOL_PY_METADATA = Metadata(
    id="abids_tool.py",
    name="abids_tool.py",
    package="afni",
)

AbidsToolPy = typing.TypedDict(
    "AbidsToolPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/abids_tool.py"]],
        "input_files": list[InputPathType],
        "tr_match": typing.NotRequired[bool],
        "add_tr": typing.NotRequired[bool],
        "add_slice_times": typing.NotRequired[bool],
        "copy_prefix": typing.NotRequired[list[str]],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AbidsToolPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def abids_tool_py_params(
    input_files: list[InputPathType],
    tr_match: bool = False,
    add_tr: bool = False,
    add_slice_times: bool = False,
    copy_prefix: list[str] | None = None,
    help_flag: bool = False,
) -> AbidsToolPy:
    """
    Build parameters.

    Args:
        input_files: At least one 3d+time dataset in NIFTI format.
        tr_match: Check if the TR in the json file matches the TR from input\
            dataset header. (Returns 1 if match)
        add_tr: Add the TR from the BIDS json file to the input dataset using\
            3drefit.
        add_slice_times: Add the slice times from the BIDS json file to the input\
            dataset using 3drefit.
        copy_prefix: Copy both the NIFTI dataset(s) and matching .json file(s) to\
            PREFIX. Must have the same number of prefixes as datasets.
        help_flag: Show help information and exit.

    Returns:
        Parameter dictionary.
    """
    params: AbidsToolPy = {
        "@type": "afni/abids_tool.py",
        "input_files": input_files,
        "tr_match": tr_match,
        "add_tr": add_tr,
        "add_slice_times": add_slice_times,
        "help_flag": help_flag,
    }
    if copy_prefix is not None:
        params["copy_prefix"] = copy_prefix
    return params

def abids_tool_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AbidsToolPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("tr_match", None) is not None:
        if not isinstance(params["tr_match"], bool):
            raise StyxValidationError(f'`tr_match` has the wrong type: Received `{type(params["tr_match"])}` expected `bool`')
    if params.get("add_tr", None) is not None:
        if not isinstance(params["add_tr"], bool):
            raise StyxValidationError(f'`add_tr` has the wrong type: Received `{type(params["add_tr"])}` expected `bool`')
    if params.get("add_slice_times", None) is not None:
        if not isinstance(params["add_slice_times"], bool):
            raise StyxValidationError(f'`add_slice_times` has the wrong type: Received `{type(params["add_slice_times"])}` expected `bool`')
    if params.get("copy_prefix", None) is not None:
        if not isinstance(params["copy_prefix"], list):
            raise StyxValidationError(f'`copy_prefix` has the wrong type: Received `{type(params["copy_prefix"])}` expected `list[str]`')
        for e_2 in params["copy_prefix"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`copy_prefix` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def abids_tool_py_cargs(params: AbidsToolPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("abids_tool.py")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    if params.get("tr_match"):
        cargs.append("-TR_match")
    if params.get("add_tr"):
        cargs.append("-add_TR")
    if params.get("add_slice_times"):
        cargs.append("-add_slice_times")
    v_0 = params.get("copy_prefix")
    if v_0 is not None:
        cargs.append("-copy")
        for item1 in v_0:
            cargs.append(item1)
    if params.get("help_flag"):
        cargs.append("-help")
    return cargs

def abids_tool_py_outputs(params: AbidsToolPy, execution: Execution) -> AbidsToolPyOutputs:
    """Build the AbidsToolPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AbidsToolPyOutputs(
        root=root_v,
    )

def abids_tool_py_execute(params: AbidsToolPy, runner: Runner | None = None) -> AbidsToolPyOutputs:
    """
    abids_tool.py

    A tool to work with BIDS formatted datasets created with dcm2niix_afni or dcm2niix, mainly to pull information from the matching JSON file and refit the input dataset using 3drefit.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    abids_tool_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ABIDS_TOOL_PY_METADATA)
    execution.params(params)
    args = abids_tool_py_cargs(params, execution)
    out = abids_tool_py_outputs(params, execution)
    execution.run(args)
    return out

def abids_tool_py(
    input_files: list[InputPathType],
    tr_match: bool = False,
    add_tr: bool = False,
    add_slice_times: bool = False,
    copy_prefix: list[str] | None = None,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> AbidsToolPyOutputs:
    """
    abids_tool.py

    A tool to work with BIDS formatted datasets created with dcm2niix_afni or dcm2niix, mainly to pull information from the matching JSON file and refit the input dataset using 3drefit.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: At least one 3d+time dataset in NIFTI format.
        tr_match: Check if the TR in the json file matches the TR from input\
            dataset header. (Returns 1 if match)
        add_tr: Add the TR from the BIDS json file to the input dataset using\
            3drefit.
        add_slice_times: Add the slice times from the BIDS json file to the input\
            dataset using 3drefit.
        copy_prefix: Copy both the NIFTI dataset(s) and matching .json file(s) to\
            PREFIX. Must have the same number of prefixes as datasets.
        help_flag: Show help information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = abids_tool_py_params(
        input_files=input_files,
        tr_match=tr_match,
        add_tr=add_tr,
        add_slice_times=add_slice_times,
        copy_prefix=copy_prefix,
        help_flag=help_flag,
    )
    return abids_tool_py_execute(params, runner)

__all__ = [
    "AbidsToolPy",
    "AbidsToolPyOutputs",
    "ABIDS_TOOL_PY_METADATA",
    "abids_tool_py_cargs",
    "abids_tool_py_outputs",
    "abids_tool_py_params",
    "abids_tool_py_execute",
    "abids_tool_py_validate",
    "abids_tool_py",
]