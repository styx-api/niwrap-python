# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

P2DSETSTAT_METADATA = Metadata(
    id="p2dsetstat",
    name="p2dsetstat",
    package="afni",
)

P2dsetstat = typing.TypedDict(
    "P2dsetstat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/p2dsetstat"]],
        "dataset": str,
        "pvalue": float,
        "bisided": typing.NotRequired[bool],
        "twosided": typing.NotRequired[bool],
        "onesided": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class P2dsetstatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stat_value: OutputPathType
    """The converted statistic value."""

def p2dsetstat_params(
    dataset: str,
    pvalue: float,
    bisided: bool = False,
    twosided: bool = False,
    onesided: bool = False,
    quiet: bool = False,
) -> P2dsetstat:
    """
    Build parameters.

    Args:
        dataset: Specify a dataset DDD and, if it has multiple sub-bricks, the\
            [i]th subbrick with the statistic of interest MUST be selected\
            explicitly; note the use of quotation marks around the brick selector\
            (because of the square-brackets). 'i' can be either a number or a\
            string label selector.
        pvalue: Input p-value P, which MUST be in the interval [0,1].
        bisided: Two-sided test.
        twosided: Two-sided test.
        onesided: One-sided test.
        quiet: Output only the final statistic value.

    Returns:
        Parameter dictionary.
    """
    params: P2dsetstat = {
        "@type": "afni/p2dsetstat",
        "dataset": dataset,
        "pvalue": pvalue,
        "bisided": bisided,
        "twosided": twosided,
        "onesided": onesided,
        "quiet": quiet,
    }
    return params

def p2dsetstat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid P2dsetstat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], str):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `str`')
    if params.get("pvalue", None) is None:
        raise StyxValidationError("`pvalue` must not be None")
    if not isinstance(params["pvalue"], (float, int)):
        raise StyxValidationError(f'`pvalue` has the wrong type: Received `{type(params["pvalue"])}` expected `float`')
    if not (0 <= params["pvalue"] <= 1):
        raise StyxValidationError("Parameter `pvalue` must be between 0 and 1 (inclusive)")
    if params.get("bisided", None) is not None:
        if not isinstance(params["bisided"], bool):
            raise StyxValidationError(f'`bisided` has the wrong type: Received `{type(params["bisided"])}` expected `bool`')
    if params.get("twosided", None) is not None:
        if not isinstance(params["twosided"], bool):
            raise StyxValidationError(f'`twosided` has the wrong type: Received `{type(params["twosided"])}` expected `bool`')
    if params.get("onesided", None) is not None:
        if not isinstance(params["onesided"], bool):
            raise StyxValidationError(f'`onesided` has the wrong type: Received `{type(params["onesided"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def p2dsetstat_cargs(params: P2dsetstat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("p2dsetstat")
    cargs.append("-inset")
    cargs.append(params["dataset"])
    cargs.append("-pval")
    cargs.append(str(params["pvalue"]))
    if params.get("bisided"):
        cargs.append("-bisided")
    if params.get("twosided"):
        cargs.append("-2sided")
    if params.get("onesided"):
        cargs.append("-1sided")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def p2dsetstat_outputs(params: P2dsetstat, execution: Execution) -> P2dsetstatOutputs:
    """Build the P2dsetstatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stat_value_v: OutputPathType = execution.output_file("stdout")
    return P2dsetstatOutputs(
        root=root_v,
        stat_value=stat_value_v,
    )

def p2dsetstat_execute(params: P2dsetstat, runner: Runner | None = None) -> P2dsetstatOutputs:
    """
    p2dsetstat

    Convert a p-value to a statistic of choice with reference to a specific dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    p2dsetstat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(P2DSETSTAT_METADATA)
    execution.params(params)
    args = p2dsetstat_cargs(params, execution)
    out = p2dsetstat_outputs(params, execution)
    execution.run(args)
    return out

def p2dsetstat(
    dataset: str,
    pvalue: float,
    bisided: bool = False,
    twosided: bool = False,
    onesided: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> P2dsetstatOutputs:
    """
    p2dsetstat

    Convert a p-value to a statistic of choice with reference to a specific dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Specify a dataset DDD and, if it has multiple sub-bricks, the\
            [i]th subbrick with the statistic of interest MUST be selected\
            explicitly; note the use of quotation marks around the brick selector\
            (because of the square-brackets). 'i' can be either a number or a\
            string label selector.
        pvalue: Input p-value P, which MUST be in the interval [0,1].
        bisided: Two-sided test.
        twosided: Two-sided test.
        onesided: One-sided test.
        quiet: Output only the final statistic value.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = p2dsetstat_params(
        dataset=dataset,
        pvalue=pvalue,
        bisided=bisided,
        twosided=twosided,
        onesided=onesided,
        quiet=quiet,
    )
    return p2dsetstat_execute(params, runner)

__all__ = [
    "P2dsetstat",
    "P2dsetstatOutputs",
    "P2DSETSTAT_METADATA",
    "p2dsetstat_cargs",
    "p2dsetstat_outputs",
    "p2dsetstat_params",
    "p2dsetstat_execute",
    "p2dsetstat_validate",
    "p2dsetstat",
]