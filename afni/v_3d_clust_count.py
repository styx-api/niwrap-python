# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_CLUST_COUNT_METADATA = Metadata(
    id="3dClustCount",
    name="3dClustCount",
    package="afni",
)

V3dClustCount = typing.TypedDict(
    "V3dClustCount",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dClustCount"]],
        "datasets": list[InputPathType],
        "prefix": typing.NotRequired[str],
        "final": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dClustCountOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    clustcount_niml: OutputPathType | None
    """Summed results file in NIML format."""
    clustcount_1D: OutputPathType | None
    """Summed results file in 1D format (when '-final' is used)."""
    final_clustcount_niml: OutputPathType | None
    """Summed results file in NIML format (when '-final' is used)."""

def v_3d_clust_count_params(
    datasets: list[InputPathType],
    prefix: str | None = None,
    final: bool = False,
    quiet: bool = False,
) -> V3dClustCount:
    """
    Build parameters.

    Args:
        datasets: Input datasets to be processed.
        prefix: Prefix of the filename into which results will be summed. Actual\
            filename will be 'sss.clustcount.niml'. If this file already exists,\
            results from the current run will be summed into the existing results\
            and the file then re-written.
        final: Output results in a format similar to 3dClustSim -- as 1D and NIML\
            formatted files with probabilities of various cluster sizes. This\
            option can be used without any input datasets to create final output\
            files from saved '.clustcount.niml' output file from earlier runs.
        quiet: Suppresses progress reports and other informational messages. Should\
            be placed first in the command line to quiet most messages.

    Returns:
        Parameter dictionary.
    """
    params: V3dClustCount = {
        "@type": "afni/3dClustCount",
        "datasets": datasets,
        "final": final,
        "quiet": quiet,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_clust_count_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dClustCount."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("final", None) is not None:
        if not isinstance(params["final"], bool):
            raise StyxValidationError(f'`final` has the wrong type: Received `{type(params["final"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_3d_clust_count_cargs(params: V3dClustCount, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dClustCount")
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("final"):
        cargs.append("-final")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def v_3d_clust_count_outputs(params: V3dClustCount, execution: Execution) -> V3dClustCountOutputs:
    """Build the V3dClustCountOutputs object for this tool."""
    clustcount_niml_v: OutputPathType | None = None
    clustcount_1D_v: OutputPathType | None = None
    final_clustcount_niml_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        clustcount_niml_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.clustcount.niml')
    __v1 = params.get("prefix")
    if __v1 is not None:
        clustcount_1D_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.1D')
    __v2 = params.get("prefix")
    if __v2 is not None:
        final_clustcount_niml_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.niml')
    return V3dClustCountOutputs(
        root=root_v,
        clustcount_niml=clustcount_niml_v,
        clustcount_1D=clustcount_1D_v,
        final_clustcount_niml=final_clustcount_niml_v,
    )

def v_3d_clust_count_execute(params: V3dClustCount, runner: Runner | None = None) -> V3dClustCountOutputs:
    """
    3dClustCount

    This program takes as input 1 or more datasets, thresholds them at various levels, and counts up the number of clusters of various sizes.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_clust_count_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_CLUST_COUNT_METADATA)
    execution.params(params)
    args = v_3d_clust_count_cargs(params, execution)
    out = v_3d_clust_count_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_clust_count(
    datasets: list[InputPathType],
    prefix: str | None = None,
    final: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> V3dClustCountOutputs:
    """
    3dClustCount

    This program takes as input 1 or more datasets, thresholds them at various levels, and counts up the number of clusters of various sizes.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        datasets: Input datasets to be processed.
        prefix: Prefix of the filename into which results will be summed. Actual\
            filename will be 'sss.clustcount.niml'. If this file already exists,\
            results from the current run will be summed into the existing results\
            and the file then re-written.
        final: Output results in a format similar to 3dClustSim -- as 1D and NIML\
            formatted files with probabilities of various cluster sizes. This\
            option can be used without any input datasets to create final output\
            files from saved '.clustcount.niml' output file from earlier runs.
        quiet: Suppresses progress reports and other informational messages. Should\
            be placed first in the command line to quiet most messages.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_clust_count_params(
        datasets=datasets,
        prefix=prefix,
        final=final,
        quiet=quiet,
    )
    return v_3d_clust_count_execute(params, runner)

__all__ = [
    "V3dClustCount",
    "V3dClustCountOutputs",
    "V_3D_CLUST_COUNT_METADATA",
    "v_3d_clust_count_cargs",
    "v_3d_clust_count_outputs",
    "v_3d_clust_count_params",
    "v_3d_clust_count_execute",
    "v_3d_clust_count_validate",
    "v_3d_clust_count",
]