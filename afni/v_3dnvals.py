# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DNVALS_METADATA = Metadata(
    id="3dnvals",
    name="3dnvals",
    package="afni",
)

V3dnvals = typing.TypedDict(
    "V3dnvals",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dnvals"]],
        "datasets": list[InputPathType],
        "all_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dnvalsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3dnvals_params(
    datasets: list[InputPathType],
    all_flag: bool = False,
    verbose_flag: bool = False,
) -> V3dnvals:
    """
    Build parameters.

    Args:
        datasets: Input 3D dataset(s)
        all_flag: Print out all 4 dimensions: Nx, Ny, Nz, Nvals
        verbose_flag: Print the header name of the dataset first

    Returns:
        Parameter dictionary.
    """
    params: V3dnvals = {
        "@type": "afni/3dnvals",
        "datasets": datasets,
        "all_flag": all_flag,
        "verbose_flag": verbose_flag,
    }
    return params

def v_3dnvals_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dnvals."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("all_flag", None) is not None:
        if not isinstance(params["all_flag"], bool):
            raise StyxValidationError(f'`all_flag` has the wrong type: Received `{type(params["all_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def v_3dnvals_cargs(params: V3dnvals, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dnvals")
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    if params.get("all_flag"):
        cargs.append("-all")
    if params.get("verbose_flag"):
        cargs.append("-verbose")
    return cargs

def v_3dnvals_outputs(params: V3dnvals, execution: Execution) -> V3dnvalsOutputs:
    """Build the V3dnvalsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dnvalsOutputs(
        root=root_v,
    )

def v_3dnvals_execute(params: V3dnvals, runner: Runner | None = None) -> V3dnvalsOutputs:
    """
    3dnvals

    Tool to print the number of sub-bricks in a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dnvals_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DNVALS_METADATA)
    execution.params(params)
    args = v_3dnvals_cargs(params, execution)
    out = v_3dnvals_outputs(params, execution)
    execution.run(args)
    return out

def v_3dnvals(
    datasets: list[InputPathType],
    all_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> V3dnvalsOutputs:
    """
    3dnvals

    Tool to print the number of sub-bricks in a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        datasets: Input 3D dataset(s)
        all_flag: Print out all 4 dimensions: Nx, Ny, Nz, Nvals
        verbose_flag: Print the header name of the dataset first
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dnvals_params(
        datasets=datasets,
        all_flag=all_flag,
        verbose_flag=verbose_flag,
    )
    return v_3dnvals_execute(params, runner)

__all__ = [
    "V3dnvals",
    "V3dnvalsOutputs",
    "V_3DNVALS_METADATA",
    "v_3dnvals_cargs",
    "v_3dnvals_outputs",
    "v_3dnvals_params",
    "v_3dnvals_execute",
    "v_3dnvals_validate",
    "v_3dnvals",
]