# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_MVM_PREP_PY_METADATA = Metadata(
    id="fat_mvm_prep.py",
    name="fat_mvm_prep.py",
    package="afni",
)

FatMvmPrepPy = typing.TypedDict(
    "FatMvmPrepPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_mvm_prep.py"]],
        "prefix": str,
        "csv_file": InputPathType,
        "matrix_files": typing.NotRequired[str],
        "list_match": typing.NotRequired[InputPathType],
        "unionize_rois": typing.NotRequired[bool],
        "na_warn_off": typing.NotRequired[bool],
        "extern_labels_no": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatMvmPrepPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mvmtbl: OutputPathType
    """Output tabular text file for 3dMVM."""
    mvmprep_log: OutputPathType
    """Log file detailing subject matching and ROI list."""

def fat_mvm_prep_py_params(
    prefix: str,
    csv_file: InputPathType,
    matrix_files: str | None = None,
    list_match: InputPathType | None = None,
    unionize_rois: bool = False,
    na_warn_off: bool = False,
    extern_labels_no: bool = False,
) -> FatMvmPrepPy:
    """
    Build parameters.

    Args:
        prefix: Prefix for output files.
        csv_file: Comma-separated variable (CSV) file for input.
        matrix_files: Set of matrix (*.grid or *.netcc) files by searchable path.
        list_match: Text file containing two columns: path to subject matrix file\
            and CSV IDs.
        unionize_rois: Make the ROI list as the union of elements across the group.
        na_warn_off: Turn off the automatic warnings as the data table is created.
        extern_labels_no: Turn off the writing/usage of user-defined labels in the\
            *.grid/*.netcc files.

    Returns:
        Parameter dictionary.
    """
    params: FatMvmPrepPy = {
        "@type": "afni/fat_mvm_prep.py",
        "prefix": prefix,
        "csv_file": csv_file,
        "unionize_rois": unionize_rois,
        "na_warn_off": na_warn_off,
        "extern_labels_no": extern_labels_no,
    }
    if matrix_files is not None:
        params["matrix_files"] = matrix_files
    if list_match is not None:
        params["list_match"] = list_match
    return params

def fat_mvm_prep_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatMvmPrepPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("csv_file", None) is None:
        raise StyxValidationError("`csv_file` must not be None")
    if not isinstance(params["csv_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`csv_file` has the wrong type: Received `{type(params["csv_file"])}` expected `InputPathType`')
    if params.get("matrix_files", None) is not None:
        if not isinstance(params["matrix_files"], str):
            raise StyxValidationError(f'`matrix_files` has the wrong type: Received `{type(params["matrix_files"])}` expected `str`')
    if params.get("list_match", None) is not None:
        if not isinstance(params["list_match"], (pathlib.Path, str)):
            raise StyxValidationError(f'`list_match` has the wrong type: Received `{type(params["list_match"])}` expected `InputPathType`')
    if params.get("unionize_rois", None) is not None:
        if not isinstance(params["unionize_rois"], bool):
            raise StyxValidationError(f'`unionize_rois` has the wrong type: Received `{type(params["unionize_rois"])}` expected `bool`')
    if params.get("na_warn_off", None) is not None:
        if not isinstance(params["na_warn_off"], bool):
            raise StyxValidationError(f'`na_warn_off` has the wrong type: Received `{type(params["na_warn_off"])}` expected `bool`')
    if params.get("extern_labels_no", None) is not None:
        if not isinstance(params["extern_labels_no"], bool):
            raise StyxValidationError(f'`extern_labels_no` has the wrong type: Received `{type(params["extern_labels_no"])}` expected `bool`')

def fat_mvm_prep_py_cargs(params: FatMvmPrepPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_mvm_prep.py")
    cargs.append("-p")
    cargs.append(params["prefix"])
    cargs.append("-c")
    cargs.append(execution.input_file(params["csv_file"]))
    v_0 = params.get("matrix_files")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(v_0)
    v_1 = params.get("list_match")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_1))
    if params.get("unionize_rois"):
        cargs.append("-u")
    if params.get("na_warn_off"):
        cargs.append("-N")
    if params.get("extern_labels_no"):
        cargs.append("-E")
    return cargs

def fat_mvm_prep_py_outputs(params: FatMvmPrepPy, execution: Execution) -> FatMvmPrepPyOutputs:
    """Build the FatMvmPrepPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    mvmtbl_v: OutputPathType = execution.output_file(f'{params["prefix"]}_MVMtbl.txt')
    mvmprep_log_v: OutputPathType = execution.output_file(f'{params["prefix"]}_MVMprep.log')
    return FatMvmPrepPyOutputs(
        root=root_v,
        mvmtbl=mvmtbl_v,
        mvmprep_log=mvmprep_log_v,
    )

def fat_mvm_prep_py_execute(params: FatMvmPrepPy, runner: Runner | None = None) -> FatMvmPrepPyOutputs:
    """
    fat_mvm_prep.py

    Combine FATCAT output with CSV data for statistical modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_mvm_prep_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_MVM_PREP_PY_METADATA)
    execution.params(params)
    args = fat_mvm_prep_py_cargs(params, execution)
    out = fat_mvm_prep_py_outputs(params, execution)
    execution.run(args)
    return out

def fat_mvm_prep_py(
    prefix: str,
    csv_file: InputPathType,
    matrix_files: str | None = None,
    list_match: InputPathType | None = None,
    unionize_rois: bool = False,
    na_warn_off: bool = False,
    extern_labels_no: bool = False,
    runner: Runner | None = None,
) -> FatMvmPrepPyOutputs:
    """
    fat_mvm_prep.py

    Combine FATCAT output with CSV data for statistical modeling.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix for output files.
        csv_file: Comma-separated variable (CSV) file for input.
        matrix_files: Set of matrix (*.grid or *.netcc) files by searchable path.
        list_match: Text file containing two columns: path to subject matrix file\
            and CSV IDs.
        unionize_rois: Make the ROI list as the union of elements across the group.
        na_warn_off: Turn off the automatic warnings as the data table is created.
        extern_labels_no: Turn off the writing/usage of user-defined labels in the\
            *.grid/*.netcc files.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_mvm_prep_py_params(
        prefix=prefix,
        csv_file=csv_file,
        matrix_files=matrix_files,
        list_match=list_match,
        unionize_rois=unionize_rois,
        na_warn_off=na_warn_off,
        extern_labels_no=extern_labels_no,
    )
    return fat_mvm_prep_py_execute(params, runner)

__all__ = [
    "FatMvmPrepPy",
    "FatMvmPrepPyOutputs",
    "FAT_MVM_PREP_PY_METADATA",
    "fat_mvm_prep_py_cargs",
    "fat_mvm_prep_py_outputs",
    "fat_mvm_prep_py_params",
    "fat_mvm_prep_py_execute",
    "fat_mvm_prep_py_validate",
    "fat_mvm_prep_py",
]