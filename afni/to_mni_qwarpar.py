# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TO_MNI_QWARPAR_METADATA = Metadata(
    id="@toMNI_Qwarpar",
    name="@toMNI_Qwarpar",
    package="afni",
)

ToMniQwarpar = typing.TypedDict(
    "ToMniQwarpar",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@toMNI_Qwarpar"]],
        "numcpu": float,
        "numjob": float,
    },
)

@dataclasses.dataclass
class ToMniQwarparOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output dataset created after processing."""

def to_mni_qwarpar_params(
    numcpu: float,
    numjob: float,
) -> ToMniQwarpar:
    """
    Build parameters.

    Args:
        numcpu: TOTAL NUMBER OF CPUS TO USE; should not exceed the number of CPUs\
            (cores) on the system.
        numjob: MAX NUMBER OF JOBS TO USE; often set to the same value as numcpu so\
            that 1 dataset is processed in 1 core.

    Returns:
        Parameter dictionary.
    """
    params: ToMniQwarpar = {
        "@type": "afni/@toMNI_Qwarpar",
        "numcpu": numcpu,
        "numjob": numjob,
    }
    return params

def to_mni_qwarpar_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ToMniQwarpar."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("numcpu", None) is None:
        raise StyxValidationError("`numcpu` must not be None")
    if not isinstance(params["numcpu"], (float, int)):
        raise StyxValidationError(f'`numcpu` has the wrong type: Received `{type(params["numcpu"])}` expected `float`')
    if params.get("numjob", None) is None:
        raise StyxValidationError("`numjob` must not be None")
    if not isinstance(params["numjob"], (float, int)):
        raise StyxValidationError(f'`numjob` has the wrong type: Received `{type(params["numjob"])}` expected `float`')

def to_mni_qwarpar_cargs(params: ToMniQwarpar, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@toMNI_Qwarpar")
    cargs.append(str(params["numcpu"]))
    cargs.append(str(params["numjob"]))
    return cargs

def to_mni_qwarpar_outputs(params: ToMniQwarpar, execution: Execution) -> ToMniQwarparOutputs:
    """Build the ToMniQwarparOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("uni+tlrc.HEAD")
    return ToMniQwarparOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def to_mni_qwarpar_execute(params: ToMniQwarpar, runner: Runner | None = None) -> ToMniQwarparOutputs:
    """
    @toMNI_Qwarpar

    Transforms datasets to MNI space, then collectively re-transforms them to produce a refined average.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    to_mni_qwarpar_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TO_MNI_QWARPAR_METADATA)
    execution.params(params)
    args = to_mni_qwarpar_cargs(params, execution)
    out = to_mni_qwarpar_outputs(params, execution)
    execution.run(args)
    return out

def to_mni_qwarpar(
    numcpu: float,
    numjob: float,
    runner: Runner | None = None,
) -> ToMniQwarparOutputs:
    """
    @toMNI_Qwarpar

    Transforms datasets to MNI space, then collectively re-transforms them to produce a refined average.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        numcpu: TOTAL NUMBER OF CPUS TO USE; should not exceed the number of CPUs\
            (cores) on the system.
        numjob: MAX NUMBER OF JOBS TO USE; often set to the same value as numcpu so\
            that 1 dataset is processed in 1 core.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = to_mni_qwarpar_params(
        numcpu=numcpu,
        numjob=numjob,
    )
    return to_mni_qwarpar_execute(params, runner)

__all__ = [
    "ToMniQwarpar",
    "ToMniQwarparOutputs",
    "TO_MNI_QWARPAR_METADATA",
    "to_mni_qwarpar_cargs",
    "to_mni_qwarpar_outputs",
    "to_mni_qwarpar_params",
    "to_mni_qwarpar_execute",
    "to_mni_qwarpar_validate",
    "to_mni_qwarpar",
]