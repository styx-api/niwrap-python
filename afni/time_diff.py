# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TIME_DIFF_METADATA = Metadata(
    id="@TimeDiff",
    name="@TimeDiff",
    package="afni",
)

TimeDiff = typing.TypedDict(
    "TimeDiff",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@TimeDiff"]],
        "file1": InputPathType,
        "file2": InputPathType,
    },
)

@dataclasses.dataclass
class TimeDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def time_diff_params(
    file1: InputPathType,
    file2: InputPathType,
) -> TimeDiff:
    """
    Build parameters.

    Args:
        file1: First file to compare (e.g., file1.txt)
        file2: Second file to compare (e.g., file2.txt)

    Returns:
        Parameter dictionary.
    """
    params: TimeDiff = {
        "@type": "afni/@TimeDiff",
        "file1": file1,
        "file2": file2,
    }
    return params

def time_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TimeDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("file1", None) is None:
        raise StyxValidationError("`file1` must not be None")
    if not isinstance(params["file1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file1` has the wrong type: Received `{type(params["file1"])}` expected `InputPathType`')
    if params.get("file2", None) is None:
        raise StyxValidationError("`file2` must not be None")
    if not isinstance(params["file2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file2` has the wrong type: Received `{type(params["file2"])}` expected `InputPathType`')

def time_diff_cargs(params: TimeDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@TimeDiff")
    cargs.append(execution.input_file(params["file1"]))
    cargs.append(execution.input_file(params["file2"]))
    return cargs

def time_diff_outputs(params: TimeDiff, execution: Execution) -> TimeDiffOutputs:
    """Build the TimeDiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TimeDiffOutputs(
        root=root_v,
    )

def time_diff_execute(params: TimeDiff, runner: Runner | None = None) -> TimeDiffOutputs:
    """
    @TimeDiff

    A tool to compare the modification times of two files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    time_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TIME_DIFF_METADATA)
    execution.params(params)
    args = time_diff_cargs(params, execution)
    out = time_diff_outputs(params, execution)
    execution.run(args)
    return out

def time_diff(
    file1: InputPathType,
    file2: InputPathType,
    runner: Runner | None = None,
) -> TimeDiffOutputs:
    """
    @TimeDiff

    A tool to compare the modification times of two files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        file1: First file to compare (e.g., file1.txt)
        file2: Second file to compare (e.g., file2.txt)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = time_diff_params(
        file1=file1,
        file2=file2,
    )
    return time_diff_execute(params, runner)

__all__ = [
    "TimeDiff",
    "TimeDiffOutputs",
    "TIME_DIFF_METADATA",
    "time_diff_cargs",
    "time_diff_outputs",
    "time_diff_params",
    "time_diff_execute",
    "time_diff_validate",
    "time_diff",
]