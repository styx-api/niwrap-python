# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AIV_METADATA = Metadata(
    id="aiv",
    name="aiv",
    package="afni",
)

Aiv = typing.TypedDict(
    "Aiv",
    {
        "@type": typing.NotRequired[typing.Literal["afni/aiv"]],
        "verbose": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "title": typing.NotRequired[str],
        "port": typing.NotRequired[float],
        "pad": typing.NotRequired[str],
        "input_images": list[InputPathType],
    },
)

@dataclasses.dataclass
class AivOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def aiv_params(
    input_images: list[InputPathType],
    verbose: bool = False,
    quiet: bool = False,
    title: str | None = None,
    port: float | None = None,
    pad: str | None = None,
) -> Aiv:
    """
    Build parameters.

    Args:
        input_images: Input image files (e.g., img1.jpg, img2.bmp).
        verbose: Print out the image filenames for progress tracking.
        quiet: Run the program in quiet mode.
        title: Specify the window title.
        port: Listen to TCP/IP port for incoming images.
        pad: Pad all input images to be the same size.

    Returns:
        Parameter dictionary.
    """
    params: Aiv = {
        "@type": "afni/aiv",
        "input_images": input_images,
        "verbose": verbose,
        "quiet": quiet,
    }
    if title is not None:
        params["title"] = title
    if port is not None:
        params["port"] = port
    if pad is not None:
        params["pad"] = pad
    return params

def aiv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aiv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("title", None) is not None:
        if not isinstance(params["title"], str):
            raise StyxValidationError(f'`title` has the wrong type: Received `{type(params["title"])}` expected `str`')
    if params.get("port", None) is not None:
        if not isinstance(params["port"], (float, int)):
            raise StyxValidationError(f'`port` has the wrong type: Received `{type(params["port"])}` expected `float`')
        if not (1024 <= params["port"] <= 65535):
            raise StyxValidationError("Parameter `port` must be between 1024 and 65535 (inclusive)")
    if params.get("pad", None) is not None:
        if not isinstance(params["pad"], str):
            raise StyxValidationError(f'`pad` has the wrong type: Received `{type(params["pad"])}` expected `str`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def aiv_cargs(params: Aiv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aiv")
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("quiet"):
        cargs.append("-q")
    v_0 = params.get("title")
    if v_0 is not None:
        cargs.append("-title")
        cargs.append(v_0)
    v_1 = params.get("port")
    if v_1 is not None:
        cargs.append("-p")
        cargs.append(str(v_1))
    v_2 = params.get("pad")
    if v_2 is not None:
        cargs.append("-pad")
        cargs.append(v_2)
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    return cargs

def aiv_outputs(params: Aiv, execution: Execution) -> AivOutputs:
    """Build the AivOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AivOutputs(
        root=root_v,
    )

def aiv_execute(params: Aiv, runner: Runner | None = None) -> AivOutputs:
    """
    aiv

    AFNI Image Viewer program. Shows the 2D images on the command line in an AFNI-like image viewer.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aiv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AIV_METADATA)
    execution.params(params)
    args = aiv_cargs(params, execution)
    out = aiv_outputs(params, execution)
    execution.run(args)
    return out

def aiv(
    input_images: list[InputPathType],
    verbose: bool = False,
    quiet: bool = False,
    title: str | None = None,
    port: float | None = None,
    pad: str | None = None,
    runner: Runner | None = None,
) -> AivOutputs:
    """
    aiv

    AFNI Image Viewer program. Shows the 2D images on the command line in an AFNI-like image viewer.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_images: Input image files (e.g., img1.jpg, img2.bmp).
        verbose: Print out the image filenames for progress tracking.
        quiet: Run the program in quiet mode.
        title: Specify the window title.
        port: Listen to TCP/IP port for incoming images.
        pad: Pad all input images to be the same size.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aiv_params(
        input_images=input_images,
        verbose=verbose,
        quiet=quiet,
        title=title,
        port=port,
        pad=pad,
    )
    return aiv_execute(params, runner)

__all__ = [
    "Aiv",
    "AivOutputs",
    "AIV_METADATA",
    "aiv_cargs",
    "aiv_outputs",
    "aiv_params",
    "aiv_execute",
    "aiv_validate",
    "aiv",
]