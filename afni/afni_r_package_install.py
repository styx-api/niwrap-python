# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AFNI_R_PACKAGE_INSTALL_METADATA = Metadata(
    id="@afni_R_package_install",
    name="@afni_R_package_install",
    package="afni",
)

AfniRPackageInstall = typing.TypedDict(
    "AfniRPackageInstall",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@afni_R_package_install"]],
        "afni": typing.NotRequired[bool],
        "shiny": typing.NotRequired[bool],
        "bayes_view": typing.NotRequired[bool],
        "circos": typing.NotRequired[bool],
        "custom_packages": typing.NotRequired[str],
        "mirror": typing.NotRequired[str],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AfniRPackageInstallOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    install_log: OutputPathType
    """A log file listing all installed R packages."""

def afni_r_package_install_params(
    afni: bool = False,
    shiny: bool = False,
    bayes_view: bool = False,
    circos: bool = False,
    custom_packages: str | None = None,
    mirror: str | None = None,
    help: bool = False,
) -> AfniRPackageInstall:
    """
    Build parameters.

    Args:
        afni: Install AFNI related R packages.
        shiny: Install AFNI related shiny app packages.
        bayes_view: Install R packages for bayes_view.
        circos: Install OmicCircos for FATCAT_matplot.
        custom_packages: Install custom R packages (space-separated list). Must\
            start and end with double quotes.
        mirror: Set the CRAN mirror to a custom URL.
        help: Show help message.

    Returns:
        Parameter dictionary.
    """
    params: AfniRPackageInstall = {
        "@type": "afni/@afni_R_package_install",
        "afni": afni,
        "shiny": shiny,
        "bayes_view": bayes_view,
        "circos": circos,
        "help": help,
    }
    if custom_packages is not None:
        params["custom_packages"] = custom_packages
    if mirror is not None:
        params["mirror"] = mirror
    return params

def afni_r_package_install_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AfniRPackageInstall."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("afni", None) is not None:
        if not isinstance(params["afni"], bool):
            raise StyxValidationError(f'`afni` has the wrong type: Received `{type(params["afni"])}` expected `bool`')
    if params.get("shiny", None) is not None:
        if not isinstance(params["shiny"], bool):
            raise StyxValidationError(f'`shiny` has the wrong type: Received `{type(params["shiny"])}` expected `bool`')
    if params.get("bayes_view", None) is not None:
        if not isinstance(params["bayes_view"], bool):
            raise StyxValidationError(f'`bayes_view` has the wrong type: Received `{type(params["bayes_view"])}` expected `bool`')
    if params.get("circos", None) is not None:
        if not isinstance(params["circos"], bool):
            raise StyxValidationError(f'`circos` has the wrong type: Received `{type(params["circos"])}` expected `bool`')
    if params.get("custom_packages", None) is not None:
        if not isinstance(params["custom_packages"], str):
            raise StyxValidationError(f'`custom_packages` has the wrong type: Received `{type(params["custom_packages"])}` expected `str`')
    if params.get("mirror", None) is not None:
        if not isinstance(params["mirror"], str):
            raise StyxValidationError(f'`mirror` has the wrong type: Received `{type(params["mirror"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def afni_r_package_install_cargs(params: AfniRPackageInstall, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@afni_R_package_install")
    if params.get("afni"):
        cargs.append("-afni")
    if params.get("shiny"):
        cargs.append("-shiny")
    if params.get("bayes_view"):
        cargs.append("-bayes_view")
    if params.get("circos"):
        cargs.append("-circos")
    v_0 = params.get("custom_packages")
    if v_0 is not None:
        cargs.append("-custom")
        cargs.append(v_0)
    v_1 = params.get("mirror")
    if v_1 is not None:
        cargs.append("-mirror")
        cargs.append(v_1)
    if params.get("help"):
        cargs.append("-help")
    return cargs

def afni_r_package_install_outputs(params: AfniRPackageInstall, execution: Execution) -> AfniRPackageInstallOutputs:
    """Build the AfniRPackageInstallOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    install_log_v: OutputPathType = execution.output_file("R_packages_installed.txt")
    return AfniRPackageInstallOutputs(
        root=root_v,
        install_log=install_log_v,
    )

def afni_r_package_install_execute(params: AfniRPackageInstall, runner: Runner | None = None) -> AfniRPackageInstallOutputs:
    """
    @afni_R_package_install

    Helper script to install R packages for various afni-ish purposes.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    afni_r_package_install_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AFNI_R_PACKAGE_INSTALL_METADATA)
    execution.params(params)
    args = afni_r_package_install_cargs(params, execution)
    out = afni_r_package_install_outputs(params, execution)
    execution.run(args)
    return out

def afni_r_package_install(
    afni: bool = False,
    shiny: bool = False,
    bayes_view: bool = False,
    circos: bool = False,
    custom_packages: str | None = None,
    mirror: str | None = None,
    help: bool = False,
    runner: Runner | None = None,
) -> AfniRPackageInstallOutputs:
    """
    @afni_R_package_install

    Helper script to install R packages for various afni-ish purposes.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        afni: Install AFNI related R packages.
        shiny: Install AFNI related shiny app packages.
        bayes_view: Install R packages for bayes_view.
        circos: Install OmicCircos for FATCAT_matplot.
        custom_packages: Install custom R packages (space-separated list). Must\
            start and end with double quotes.
        mirror: Set the CRAN mirror to a custom URL.
        help: Show help message.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = afni_r_package_install_params(
        afni=afni,
        shiny=shiny,
        bayes_view=bayes_view,
        circos=circos,
        custom_packages=custom_packages,
        mirror=mirror,
        help=help,
    )
    return afni_r_package_install_execute(params, runner)

__all__ = [
    "AfniRPackageInstall",
    "AfniRPackageInstallOutputs",
    "AFNI_R_PACKAGE_INSTALL_METADATA",
    "afni_r_package_install_cargs",
    "afni_r_package_install_outputs",
    "afni_r_package_install_params",
    "afni_r_package_install_execute",
    "afni_r_package_install_validate",
    "afni_r_package_install",
]