# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_POLYFIT_METADATA = Metadata(
    id="3dPolyfit",
    name="3dPolyfit",
    package="afni",
)

V3dPolyfit = typing.TypedDict(
    "V3dPolyfit",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dPolyfit"]],
        "input_dataset": InputPathType,
        "poly_order": typing.NotRequired[int],
        "blur": typing.NotRequired[float],
        "median_radius": typing.NotRequired[float],
        "output_prefix": typing.NotRequired[str],
        "resid_prefix": typing.NotRequired[str],
        "coeff_output": typing.NotRequired[str],
        "automask": typing.NotRequired[bool],
        "mask_dataset": typing.NotRequired[InputPathType],
        "mean_scale": typing.NotRequired[bool],
        "clip_box": typing.NotRequired[bool],
        "fit_method": typing.NotRequired[int],
        "base_dataset": typing.NotRequired[InputPathType],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dPolyfitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Fitted output dataset"""
    resid_file: OutputPathType | None
    """Residual dataset"""
    coeff_file: OutputPathType | None
    """Coefficient output file"""

def v_3d_polyfit_params(
    input_dataset: InputPathType,
    poly_order: int | None = None,
    blur: float | None = None,
    median_radius: float | None = None,
    output_prefix: str | None = None,
    resid_prefix: str | None = None,
    coeff_output: str | None = None,
    automask: bool = False,
    mask_dataset: InputPathType | None = None,
    mean_scale: bool = False,
    clip_box: bool = False,
    fit_method: int | None = None,
    base_dataset: InputPathType | None = None,
    verbose: bool = False,
) -> V3dPolyfit:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (e.g., data.nii.gz)
        poly_order: Maximum polynomial order (0..9); [default=3]; [n=0 is the\
            constant 1]; [n=-1 means only use volumes from '-base']
        blur: Gaussian blur input dataset (inside mask) with FWHM='f' (mm)
        median_radius: Radius (voxels) of preliminary median filter of input;\
            default is no blurring
        output_prefix: Use 'pp' for prefix of output dataset (the fit); default\
            prefix is 'Polyfit'; use NULL to skip this output
        resid_prefix: Use 'rr' for the prefix of the residual dataset; default is\
            not to output residuals
        coeff_output: Save coefficients of fit into text file cc.1D; default is not\
            to save these coefficients
        automask: Create a mask (a la 3dAutomask)
        mask_dataset: Create a mask from nonzero voxels in 'mset'; default is not\
            to use a mask
        mean_scale: Scale the mean value of the fit (inside the mask) to 1;\
            probably this option is not useful for anything
        clip_box: Clip fit values outside the rectilinear box containing the mask\
            to the edge of that box, to avoid weird artifacts
        fit_method: Set 'mm' to 2 for least squares fit; set it to 1 for L1 fit\
            [default method=2]; [Note that L1 fitting is slower than L2 fitting]
        base_dataset: In addition to the polynomial fit, also use the volumes in\
            dataset 'bb' as extra basis functions
        verbose: Print fun and useful progress reports

    Returns:
        Parameter dictionary.
    """
    params: V3dPolyfit = {
        "@type": "afni/3dPolyfit",
        "input_dataset": input_dataset,
        "automask": automask,
        "mean_scale": mean_scale,
        "clip_box": clip_box,
        "verbose": verbose,
    }
    if poly_order is not None:
        params["poly_order"] = poly_order
    if blur is not None:
        params["blur"] = blur
    if median_radius is not None:
        params["median_radius"] = median_radius
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    if resid_prefix is not None:
        params["resid_prefix"] = resid_prefix
    if coeff_output is not None:
        params["coeff_output"] = coeff_output
    if mask_dataset is not None:
        params["mask_dataset"] = mask_dataset
    if fit_method is not None:
        params["fit_method"] = fit_method
    if base_dataset is not None:
        params["base_dataset"] = base_dataset
    return params

def v_3d_polyfit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dPolyfit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("poly_order", None) is not None:
        if not isinstance(params["poly_order"], int):
            raise StyxValidationError(f'`poly_order` has the wrong type: Received `{type(params["poly_order"])}` expected `int`')
        if not (-1 <= params["poly_order"] <= 9):
            raise StyxValidationError("Parameter `poly_order` must be between -1 and 9 (inclusive)")
    if params.get("blur", None) is not None:
        if not isinstance(params["blur"], (float, int)):
            raise StyxValidationError(f'`blur` has the wrong type: Received `{type(params["blur"])}` expected `float`')
    if params.get("median_radius", None) is not None:
        if not isinstance(params["median_radius"], (float, int)):
            raise StyxValidationError(f'`median_radius` has the wrong type: Received `{type(params["median_radius"])}` expected `float`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("resid_prefix", None) is not None:
        if not isinstance(params["resid_prefix"], str):
            raise StyxValidationError(f'`resid_prefix` has the wrong type: Received `{type(params["resid_prefix"])}` expected `str`')
    if params.get("coeff_output", None) is not None:
        if not isinstance(params["coeff_output"], str):
            raise StyxValidationError(f'`coeff_output` has the wrong type: Received `{type(params["coeff_output"])}` expected `str`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("mask_dataset", None) is not None:
        if not isinstance(params["mask_dataset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_dataset` has the wrong type: Received `{type(params["mask_dataset"])}` expected `InputPathType`')
    if params.get("mean_scale", None) is not None:
        if not isinstance(params["mean_scale"], bool):
            raise StyxValidationError(f'`mean_scale` has the wrong type: Received `{type(params["mean_scale"])}` expected `bool`')
    if params.get("clip_box", None) is not None:
        if not isinstance(params["clip_box"], bool):
            raise StyxValidationError(f'`clip_box` has the wrong type: Received `{type(params["clip_box"])}` expected `bool`')
    if params.get("fit_method", None) is not None:
        if not isinstance(params["fit_method"], int):
            raise StyxValidationError(f'`fit_method` has the wrong type: Received `{type(params["fit_method"])}` expected `int`')
        if not (1 <= params["fit_method"] <= 2):
            raise StyxValidationError("Parameter `fit_method` must be between 1 and 2 (inclusive)")
    if params.get("base_dataset", None) is not None:
        if not isinstance(params["base_dataset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`base_dataset` has the wrong type: Received `{type(params["base_dataset"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def v_3d_polyfit_cargs(params: V3dPolyfit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dPolyfit")
    cargs.append(execution.input_file(params["input_dataset"]))
    v_0 = params.get("poly_order")
    if v_0 is not None:
        cargs.append("-nord")
        cargs.append(str(v_0))
    v_1 = params.get("blur")
    if v_1 is not None:
        cargs.append("-blur")
        cargs.append(str(v_1))
    v_2 = params.get("median_radius")
    if v_2 is not None:
        cargs.append("-mrad")
        cargs.append(str(v_2))
    v_3 = params.get("output_prefix")
    if v_3 is not None:
        cargs.append("-prefix")
        cargs.append(v_3)
    v_4 = params.get("resid_prefix")
    if v_4 is not None:
        cargs.append("-resid")
        cargs.append(v_4)
    v_5 = params.get("coeff_output")
    if v_5 is not None:
        cargs.append("-1Dcoef")
        cargs.append(v_5)
    if params.get("automask"):
        cargs.append("-automask")
    v_6 = params.get("mask_dataset")
    if v_6 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_6))
    if params.get("mean_scale"):
        cargs.append("-mone")
    if params.get("clip_box"):
        cargs.append("-mclip")
    v_7 = params.get("fit_method")
    if v_7 is not None:
        cargs.append("-meth")
        cargs.append(str(v_7))
    v_8 = params.get("base_dataset")
    if v_8 is not None:
        cargs.append("-base")
        cargs.append(execution.input_file(v_8))
    if params.get("verbose"):
        cargs.append("-verb")
    return cargs

def v_3d_polyfit_outputs(params: V3dPolyfit, execution: Execution) -> V3dPolyfitOutputs:
    """Build the V3dPolyfitOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    resid_file_v: OutputPathType | None = None
    coeff_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_prefix")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    __v1 = params.get("resid_prefix")
    if __v1 is not None:
        resid_file_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.nii.gz')
    __v2 = params.get("coeff_output")
    if __v2 is not None:
        coeff_file_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.1D')
    return V3dPolyfitOutputs(
        root=root_v,
        output_file=output_file_v,
        resid_file=resid_file_v,
        coeff_file=coeff_file_v,
    )

def v_3d_polyfit_execute(params: V3dPolyfit, runner: Runner | None = None) -> V3dPolyfitOutputs:
    """
    3dPolyfit

    Fits a polynomial in space to the input dataset and outputs that fitted dataset. You can also add your own basis datasets to the fitting mix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_polyfit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_POLYFIT_METADATA)
    execution.params(params)
    args = v_3d_polyfit_cargs(params, execution)
    out = v_3d_polyfit_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_polyfit(
    input_dataset: InputPathType,
    poly_order: int | None = None,
    blur: float | None = None,
    median_radius: float | None = None,
    output_prefix: str | None = None,
    resid_prefix: str | None = None,
    coeff_output: str | None = None,
    automask: bool = False,
    mask_dataset: InputPathType | None = None,
    mean_scale: bool = False,
    clip_box: bool = False,
    fit_method: int | None = None,
    base_dataset: InputPathType | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> V3dPolyfitOutputs:
    """
    3dPolyfit

    Fits a polynomial in space to the input dataset and outputs that fitted dataset. You can also add your own basis datasets to the fitting mix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (e.g., data.nii.gz)
        poly_order: Maximum polynomial order (0..9); [default=3]; [n=0 is the\
            constant 1]; [n=-1 means only use volumes from '-base']
        blur: Gaussian blur input dataset (inside mask) with FWHM='f' (mm)
        median_radius: Radius (voxels) of preliminary median filter of input;\
            default is no blurring
        output_prefix: Use 'pp' for prefix of output dataset (the fit); default\
            prefix is 'Polyfit'; use NULL to skip this output
        resid_prefix: Use 'rr' for the prefix of the residual dataset; default is\
            not to output residuals
        coeff_output: Save coefficients of fit into text file cc.1D; default is not\
            to save these coefficients
        automask: Create a mask (a la 3dAutomask)
        mask_dataset: Create a mask from nonzero voxels in 'mset'; default is not\
            to use a mask
        mean_scale: Scale the mean value of the fit (inside the mask) to 1;\
            probably this option is not useful for anything
        clip_box: Clip fit values outside the rectilinear box containing the mask\
            to the edge of that box, to avoid weird artifacts
        fit_method: Set 'mm' to 2 for least squares fit; set it to 1 for L1 fit\
            [default method=2]; [Note that L1 fitting is slower than L2 fitting]
        base_dataset: In addition to the polynomial fit, also use the volumes in\
            dataset 'bb' as extra basis functions
        verbose: Print fun and useful progress reports
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_polyfit_params(
        input_dataset=input_dataset,
        poly_order=poly_order,
        blur=blur,
        median_radius=median_radius,
        output_prefix=output_prefix,
        resid_prefix=resid_prefix,
        coeff_output=coeff_output,
        automask=automask,
        mask_dataset=mask_dataset,
        mean_scale=mean_scale,
        clip_box=clip_box,
        fit_method=fit_method,
        base_dataset=base_dataset,
        verbose=verbose,
    )
    return v_3d_polyfit_execute(params, runner)

__all__ = [
    "V3dPolyfit",
    "V3dPolyfitOutputs",
    "V_3D_POLYFIT_METADATA",
    "v_3d_polyfit_cargs",
    "v_3d_polyfit_outputs",
    "v_3d_polyfit_params",
    "v_3d_polyfit_execute",
    "v_3d_polyfit_validate",
    "v_3d_polyfit",
]