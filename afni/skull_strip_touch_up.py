# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SKULL_STRIP_TOUCH_UP_METADATA = Metadata(
    id="@SkullStrip_TouchUp",
    name="@SkullStrip_TouchUp",
    package="afni",
)

SkullStripTouchUp = typing.TypedDict(
    "SkullStripTouchUp",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@SkullStrip_TouchUp"]],
        "prefix": str,
        "brain_dataset": InputPathType,
        "head_dataset": InputPathType,
        "mask_out": typing.NotRequired[bool],
        "orig_dim": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SkullStripTouchUpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_folder: OutputPathType
    """Output folder containing the touch-up results"""
    output_mask: OutputPathType
    """Output binary mask (if -mask_out flag is used)"""

def skull_strip_touch_up_params(
    prefix: str,
    brain_dataset: InputPathType,
    head_dataset: InputPathType,
    mask_out: bool = False,
    orig_dim: bool = False,
    help: bool = False,
) -> SkullStripTouchUp:
    """
    Build parameters.

    Args:
        prefix: Output file and folder name
        brain_dataset: Skull stripped data set to touch up
        head_dataset: Whole head anatomical data set
        mask_out: Output a binary mask in addition to actual data
        orig_dim: Edit in the original image dimensions
        help: Show this help message

    Returns:
        Parameter dictionary.
    """
    params: SkullStripTouchUp = {
        "@type": "afni/@SkullStrip_TouchUp",
        "prefix": prefix,
        "brain_dataset": brain_dataset,
        "head_dataset": head_dataset,
        "mask_out": mask_out,
        "orig_dim": orig_dim,
        "help": help,
    }
    return params

def skull_strip_touch_up_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SkullStripTouchUp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("brain_dataset", None) is None:
        raise StyxValidationError("`brain_dataset` must not be None")
    if not isinstance(params["brain_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain_dataset` has the wrong type: Received `{type(params["brain_dataset"])}` expected `InputPathType`')
    if params.get("head_dataset", None) is None:
        raise StyxValidationError("`head_dataset` must not be None")
    if not isinstance(params["head_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`head_dataset` has the wrong type: Received `{type(params["head_dataset"])}` expected `InputPathType`')
    if params.get("mask_out", None) is not None:
        if not isinstance(params["mask_out"], bool):
            raise StyxValidationError(f'`mask_out` has the wrong type: Received `{type(params["mask_out"])}` expected `bool`')
    if params.get("orig_dim", None) is not None:
        if not isinstance(params["orig_dim"], bool):
            raise StyxValidationError(f'`orig_dim` has the wrong type: Received `{type(params["orig_dim"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def skull_strip_touch_up_cargs(params: SkullStripTouchUp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@SkullStrip_TouchUp")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-brain")
    cargs.append(execution.input_file(params["brain_dataset"]))
    cargs.append("-head")
    cargs.append(execution.input_file(params["head_dataset"]))
    if params.get("mask_out"):
        cargs.append("-mask_out")
    if params.get("orig_dim"):
        cargs.append("-orig_dim")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def skull_strip_touch_up_outputs(params: SkullStripTouchUp, execution: Execution) -> SkullStripTouchUpOutputs:
    """Build the SkullStripTouchUpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_folder_v: OutputPathType = execution.output_file(f'{params["prefix"]}_SS_touch_up')
    output_mask_v: OutputPathType = execution.output_file(f'{params["prefix"]}_mask.nii.gz')
    return SkullStripTouchUpOutputs(
        root=root_v,
        output_folder=output_folder_v,
        output_mask=output_mask_v,
    )

def skull_strip_touch_up_execute(params: SkullStripTouchUp, runner: Runner | None = None) -> SkullStripTouchUpOutputs:
    """
    @SkullStrip_TouchUp

    Helper program to touch up failed skull stripping by resampling data, allowing manual edits, and outputting corrected data.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    skull_strip_touch_up_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SKULL_STRIP_TOUCH_UP_METADATA)
    execution.params(params)
    args = skull_strip_touch_up_cargs(params, execution)
    out = skull_strip_touch_up_outputs(params, execution)
    execution.run(args)
    return out

def skull_strip_touch_up(
    prefix: str,
    brain_dataset: InputPathType,
    head_dataset: InputPathType,
    mask_out: bool = False,
    orig_dim: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> SkullStripTouchUpOutputs:
    """
    @SkullStrip_TouchUp

    Helper program to touch up failed skull stripping by resampling data, allowing manual edits, and outputting corrected data.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output file and folder name
        brain_dataset: Skull stripped data set to touch up
        head_dataset: Whole head anatomical data set
        mask_out: Output a binary mask in addition to actual data
        orig_dim: Edit in the original image dimensions
        help: Show this help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = skull_strip_touch_up_params(
        prefix=prefix,
        brain_dataset=brain_dataset,
        head_dataset=head_dataset,
        mask_out=mask_out,
        orig_dim=orig_dim,
        help=help,
    )
    return skull_strip_touch_up_execute(params, runner)

__all__ = [
    "SkullStripTouchUp",
    "SkullStripTouchUpOutputs",
    "SKULL_STRIP_TOUCH_UP_METADATA",
    "skull_strip_touch_up_cargs",
    "skull_strip_touch_up_outputs",
    "skull_strip_touch_up_params",
    "skull_strip_touch_up_execute",
    "skull_strip_touch_up_validate",
    "skull_strip_touch_up",
]