# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CAT_MATVEC_METADATA = Metadata(
    id="cat_matvec",
    name="cat_matvec",
    package="afni",
)

CatMatvec = typing.TypedDict(
    "CatMatvec",
    {
        "@type": typing.NotRequired[typing.Literal["afni/cat_matvec"]],
        "matrix_format": typing.NotRequired[bool],
        "oneline_format": typing.NotRequired[bool],
        "four_by_four_format": typing.NotRequired[bool],
        "matvec_spec": list[str],
    },
)

@dataclasses.dataclass
class CatMatvecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cat_matvec_params(
    matvec_spec: list[str],
    matrix_format: bool = False,
    oneline_format: bool = False,
    four_by_four_format: bool = False,
) -> CatMatvec:
    """
    Build parameters.

    Args:
        matvec_spec: Specifies the matrix transformation. Can take forms described\
            in the documentation.
        matrix_format: Indicates that the resulting matrix will be written in the\
            'MATRIX(...)' format (FORM 3).
        oneline_format: Option indicates that the resulting matrix will simply be\
            written as 12 numbers on one line.
        four_by_four_format: Output matrix in augmented form (last row is 0 0 0 1).\
            This option does not work with -MATRIX or -ONELINE.

    Returns:
        Parameter dictionary.
    """
    params: CatMatvec = {
        "@type": "afni/cat_matvec",
        "matvec_spec": matvec_spec,
        "matrix_format": matrix_format,
        "oneline_format": oneline_format,
        "four_by_four_format": four_by_four_format,
    }
    return params

def cat_matvec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CatMatvec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("matrix_format", None) is not None:
        if not isinstance(params["matrix_format"], bool):
            raise StyxValidationError(f'`matrix_format` has the wrong type: Received `{type(params["matrix_format"])}` expected `bool`')
    if params.get("oneline_format", None) is not None:
        if not isinstance(params["oneline_format"], bool):
            raise StyxValidationError(f'`oneline_format` has the wrong type: Received `{type(params["oneline_format"])}` expected `bool`')
    if params.get("four_by_four_format", None) is not None:
        if not isinstance(params["four_by_four_format"], bool):
            raise StyxValidationError(f'`four_by_four_format` has the wrong type: Received `{type(params["four_by_four_format"])}` expected `bool`')
    if params.get("matvec_spec", None) is None:
        raise StyxValidationError("`matvec_spec` must not be None")
    if not isinstance(params["matvec_spec"], list):
        raise StyxValidationError(f'`matvec_spec` has the wrong type: Received `{type(params["matvec_spec"])}` expected `list[str]`')
    for e in params["matvec_spec"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`matvec_spec` has the wrong type: Received `{type(e)}` expected `list[str]`')

def cat_matvec_cargs(params: CatMatvec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cat_matvec")
    if params.get("matrix_format"):
        cargs.append("-MATRIX")
    if params.get("oneline_format"):
        cargs.append("-ONELINE")
    if params.get("four_by_four_format"):
        cargs.append("-4x4")
    for item0 in params["matvec_spec"]:
        cargs.append(item0)
    return cargs

def cat_matvec_outputs(params: CatMatvec, execution: Execution) -> CatMatvecOutputs:
    """Build the CatMatvecOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CatMatvecOutputs(
        root=root_v,
    )

def cat_matvec_execute(params: CatMatvec, runner: Runner | None = None) -> CatMatvecOutputs:
    """
    cat_matvec

    Catenates 3D rotation+shift matrix+vector transformations.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cat_matvec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CAT_MATVEC_METADATA)
    execution.params(params)
    args = cat_matvec_cargs(params, execution)
    out = cat_matvec_outputs(params, execution)
    execution.run(args)
    return out

def cat_matvec(
    matvec_spec: list[str],
    matrix_format: bool = False,
    oneline_format: bool = False,
    four_by_four_format: bool = False,
    runner: Runner | None = None,
) -> CatMatvecOutputs:
    """
    cat_matvec

    Catenates 3D rotation+shift matrix+vector transformations.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        matvec_spec: Specifies the matrix transformation. Can take forms described\
            in the documentation.
        matrix_format: Indicates that the resulting matrix will be written in the\
            'MATRIX(...)' format (FORM 3).
        oneline_format: Option indicates that the resulting matrix will simply be\
            written as 12 numbers on one line.
        four_by_four_format: Output matrix in augmented form (last row is 0 0 0 1).\
            This option does not work with -MATRIX or -ONELINE.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cat_matvec_params(
        matvec_spec=matvec_spec,
        matrix_format=matrix_format,
        oneline_format=oneline_format,
        four_by_four_format=four_by_four_format,
    )
    return cat_matvec_execute(params, runner)

__all__ = [
    "CatMatvec",
    "CatMatvecOutputs",
    "CAT_MATVEC_METADATA",
    "cat_matvec_cargs",
    "cat_matvec_outputs",
    "cat_matvec_params",
    "cat_matvec_execute",
    "cat_matvec_validate",
    "cat_matvec",
]