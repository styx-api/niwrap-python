# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SUMA_F_SVOL_TO_BRIK_METADATA = Metadata(
    id="@SUMA_FSvolToBRIK",
    name="@SUMA_FSvolToBRIK",
    package="afni",
)

SumaFSvolToBrik = typing.TypedDict(
    "SumaFSvolToBrik",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@SUMA_FSvolToBRIK"]],
        "fs_vol_data": InputPathType,
        "prefix": str,
    },
)

@dataclasses.dataclass
class SumaFSvolToBrikOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_brik: OutputPathType
    """Output BRIK volume converted from FreeSurfer data"""
    out_head: OutputPathType
    """Header file for the output BRIK volume"""

def suma_f_svol_to_brik_params(
    fs_vol_data: InputPathType,
    prefix: str,
) -> SumaFSvolToBrik:
    """
    Build parameters.

    Args:
        fs_vol_data: Input FreeSurfer volume data (e.g. COR- images or .mgz volume)
        prefix: Prefix for output BRIK volume

    Returns:
        Parameter dictionary.
    """
    params: SumaFSvolToBrik = {
        "@type": "afni/@SUMA_FSvolToBRIK",
        "fs_vol_data": fs_vol_data,
        "prefix": prefix,
    }
    return params

def suma_f_svol_to_brik_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SumaFSvolToBrik."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fs_vol_data", None) is None:
        raise StyxValidationError("`fs_vol_data` must not be None")
    if not isinstance(params["fs_vol_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fs_vol_data` has the wrong type: Received `{type(params["fs_vol_data"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def suma_f_svol_to_brik_cargs(params: SumaFSvolToBrik, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@SUMA_FSvolToBRIK")
    cargs.append(execution.input_file(params["fs_vol_data"]))
    cargs.append(params["prefix"])
    return cargs

def suma_f_svol_to_brik_outputs(params: SumaFSvolToBrik, execution: Execution) -> SumaFSvolToBrikOutputs:
    """Build the SumaFSvolToBrikOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}.BRIK')
    out_head_v: OutputPathType = execution.output_file(f'{params["prefix"]}.HEAD')
    return SumaFSvolToBrikOutputs(
        root=root_v,
        out_brik=out_brik_v,
        out_head=out_head_v,
    )

def suma_f_svol_to_brik_execute(params: SumaFSvolToBrik, runner: Runner | None = None) -> SumaFSvolToBrikOutputs:
    """
    @SUMA_FSvolToBRIK

    A script to convert COR- or .mgz files from FreeSurfer to BRIK format

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    suma_f_svol_to_brik_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SUMA_F_SVOL_TO_BRIK_METADATA)
    execution.params(params)
    args = suma_f_svol_to_brik_cargs(params, execution)
    out = suma_f_svol_to_brik_outputs(params, execution)
    execution.run(args)
    return out

def suma_f_svol_to_brik(
    fs_vol_data: InputPathType,
    prefix: str,
    runner: Runner | None = None,
) -> SumaFSvolToBrikOutputs:
    """
    @SUMA_FSvolToBRIK

    A script to convert COR- or .mgz files from FreeSurfer to BRIK format

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        fs_vol_data: Input FreeSurfer volume data (e.g. COR- images or .mgz volume)
        prefix: Prefix for output BRIK volume
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = suma_f_svol_to_brik_params(
        fs_vol_data=fs_vol_data,
        prefix=prefix,
    )
    return suma_f_svol_to_brik_execute(params, runner)

__all__ = [
    "SumaFSvolToBrik",
    "SumaFSvolToBrikOutputs",
    "SUMA_F_SVOL_TO_BRIK_METADATA",
    "suma_f_svol_to_brik_cargs",
    "suma_f_svol_to_brik_outputs",
    "suma_f_svol_to_brik_params",
    "suma_f_svol_to_brik_execute",
    "suma_f_svol_to_brik_validate",
    "suma_f_svol_to_brik",
]