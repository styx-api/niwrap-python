# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_PROC_CONNEC_VIS_METADATA = Metadata(
    id="fat_proc_connec_vis",
    name="fat_proc_connec_vis",
    package="afni",
)

FatProcConnecVis = typing.TypedDict(
    "FatProcConnecVis",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_proc_connec_vis"]],
        "in_rois": str,
        "prefix": str,
        "prefix_file": typing.NotRequired[str],
        "tsmoo_kpb": typing.NotRequired[float],
        "tsmoo_niter": typing.NotRequired[float],
        "iso_opt": typing.NotRequired[str],
        "trackid_no_or": typing.NotRequired[bool],
        "output_tcat": typing.NotRequired[bool],
        "output_tstat": typing.NotRequired[bool],
        "wdir": typing.NotRequired[str],
        "no_clean": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatProcConnecVisOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cmd_txt: OutputPathType
    """Command text file output"""
    tcat_file: OutputPathType
    """Concatenated ROI masks multibrick file from the -output_tcat flag"""
    tstat_file: OutputPathType
    """Single brick file from 3dTstat operation on the tcat dataset, produced by the -output_tstat flag"""

def fat_proc_connec_vis_params(
    in_rois: str,
    prefix: str,
    prefix_file: str | None = None,
    tsmoo_kpb: float | None = None,
    tsmoo_niter: float | None = None,
    iso_opt: str | None = None,
    trackid_no_or: bool = False,
    output_tcat: bool = False,
    output_tstat: bool = False,
    wdir: str | None = None,
    no_clean: bool = False,
) -> FatProcConnecVis:
    """
    Build parameters.

    Args:
        in_rois: List of separate files, each with a single ROI volume mask; can\
            include wildcards, etc. to specify the list
        prefix: Directory to contain the output files: *cmd.txt and surface files\
            such as *.gii and *.niml.dset; the namebase of files within this\
            directory will be the default for the program, 'wmc'. The value PPP can\
            contain parts of a path in it.
        prefix_file: Prefix for the output files: *cmd.txt and surface files such\
            as *.gii and *.niml.dset; can include path steps; and can make one\
            level of a new directory. For example, if FFF were 'A/B', then the\
            program could make a new directory called 'A' if it didn't exist\
            already and populate it with individual files having the same prefix\
            'B'.
        tsmoo_kpb: 'KPB' parameter in IsoSurface program; default value is 0.01.
        tsmoo_niter: 'NITER' parameter in IsoSurface program; default value is 6.
        iso_opt: Input one of the 'iso* options' from IsoSurface program, such as\
            'isorois+dsets', 'mergerois', etc. Quotations around the entry may be\
            needed, especially if something like the '-mergerois [LAB_OUT]' route\
            is being followed. Default: isorois+dsets
        trackid_no_or: Use this option to have the program recognize the naming\
            convention of 3dTrackID output and to ignore the OR-logic ROIs,\
            including only the AND-logic (AKA pairwise) connections
        output_tcat: Flag to output the multibrick file of concatenated ROI masks;\
            note that the [0]th brick will be all zeros (it is just a placeholder).\
            So, if there are N ROI maps concatenated, there will be N+1 bricks in\
            the output dataset, which has the name PPP_tcat.nii.gz.
        output_tstat: Flag to output the single brick file from the 3dTstat\
            operation on the tcat dataset. If there were N ROI maps concatenated,\
            then the largest value should be N. The output file's name will be\
            PPP_tstat.nii.gz.
        wdir: Working directory prefix. The format is '__WDIR_connec_vis_PPP',\
            where PPP is the input prefix.
        no_clean: Optional switch to NOT remove the working directory (default is\
            to remove the working directory)

    Returns:
        Parameter dictionary.
    """
    params: FatProcConnecVis = {
        "@type": "afni/fat_proc_connec_vis",
        "in_rois": in_rois,
        "prefix": prefix,
        "trackid_no_or": trackid_no_or,
        "output_tcat": output_tcat,
        "output_tstat": output_tstat,
        "no_clean": no_clean,
    }
    if prefix_file is not None:
        params["prefix_file"] = prefix_file
    if tsmoo_kpb is not None:
        params["tsmoo_kpb"] = tsmoo_kpb
    if tsmoo_niter is not None:
        params["tsmoo_niter"] = tsmoo_niter
    if iso_opt is not None:
        params["iso_opt"] = iso_opt
    if wdir is not None:
        params["wdir"] = wdir
    return params

def fat_proc_connec_vis_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatProcConnecVis."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_rois", None) is None:
        raise StyxValidationError("`in_rois` must not be None")
    if not isinstance(params["in_rois"], str):
        raise StyxValidationError(f'`in_rois` has the wrong type: Received `{type(params["in_rois"])}` expected `str`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("prefix_file", None) is not None:
        if not isinstance(params["prefix_file"], str):
            raise StyxValidationError(f'`prefix_file` has the wrong type: Received `{type(params["prefix_file"])}` expected `str`')
    if params.get("tsmoo_kpb", None) is not None:
        if not isinstance(params["tsmoo_kpb"], (float, int)):
            raise StyxValidationError(f'`tsmoo_kpb` has the wrong type: Received `{type(params["tsmoo_kpb"])}` expected `float`')
    if params.get("tsmoo_niter", None) is not None:
        if not isinstance(params["tsmoo_niter"], (float, int)):
            raise StyxValidationError(f'`tsmoo_niter` has the wrong type: Received `{type(params["tsmoo_niter"])}` expected `float`')
    if params.get("iso_opt", None) is not None:
        if not isinstance(params["iso_opt"], str):
            raise StyxValidationError(f'`iso_opt` has the wrong type: Received `{type(params["iso_opt"])}` expected `str`')
    if params.get("trackid_no_or", None) is not None:
        if not isinstance(params["trackid_no_or"], bool):
            raise StyxValidationError(f'`trackid_no_or` has the wrong type: Received `{type(params["trackid_no_or"])}` expected `bool`')
    if params.get("output_tcat", None) is not None:
        if not isinstance(params["output_tcat"], bool):
            raise StyxValidationError(f'`output_tcat` has the wrong type: Received `{type(params["output_tcat"])}` expected `bool`')
    if params.get("output_tstat", None) is not None:
        if not isinstance(params["output_tstat"], bool):
            raise StyxValidationError(f'`output_tstat` has the wrong type: Received `{type(params["output_tstat"])}` expected `bool`')
    if params.get("wdir", None) is not None:
        if not isinstance(params["wdir"], str):
            raise StyxValidationError(f'`wdir` has the wrong type: Received `{type(params["wdir"])}` expected `str`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')

def fat_proc_connec_vis_cargs(params: FatProcConnecVis, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_proc_connec_vis")
    cargs.append(params["in_rois"])
    cargs.append(params["prefix"])
    v_0 = params.get("prefix_file")
    if v_0 is not None:
        cargs.append("-prefix_file")
        cargs.append(v_0)
    v_1 = params.get("tsmoo_kpb")
    if v_1 is not None:
        cargs.append("-tsmoo_kpb")
        cargs.append(str(v_1))
    v_2 = params.get("tsmoo_niter")
    if v_2 is not None:
        cargs.append("-tsmoo_niter")
        cargs.append(str(v_2))
    v_3 = params.get("iso_opt")
    if v_3 is not None:
        cargs.append("-iso_opt")
        cargs.append(v_3)
    if params.get("trackid_no_or"):
        cargs.append("-trackid_no_or")
    if params.get("output_tcat"):
        cargs.append("-output_tcat")
    if params.get("output_tstat"):
        cargs.append("-output_tstat")
    v_4 = params.get("wdir")
    if v_4 is not None:
        cargs.append("-wdir")
        cargs.append(v_4)
    if params.get("no_clean"):
        cargs.append("-no_clean")
    return cargs

def fat_proc_connec_vis_outputs(params: FatProcConnecVis, execution: Execution) -> FatProcConnecVisOutputs:
    """Build the FatProcConnecVisOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cmd_txt_v: OutputPathType = execution.output_file(f'{params["prefix"]}_cmd.txt')
    tcat_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}_tcat.nii.gz')
    tstat_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}_tstat.nii.gz')
    return FatProcConnecVisOutputs(
        root=root_v,
        cmd_txt=cmd_txt_v,
        tcat_file=tcat_file_v,
        tstat_file=tstat_file_v,
    )

def fat_proc_connec_vis_execute(params: FatProcConnecVis, runner: Runner | None = None) -> FatProcConnecVisOutputs:
    """
    fat_proc_connec_vis

    This program is for visualizing the volumetric output of tracking, mainly for the '-dump_rois ...' from 3dTrackID. It creates surface-ized views of the separate white matter connection maps (WMCs) which can be viewed simultaneously in 3D with SUMA.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_proc_connec_vis_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_PROC_CONNEC_VIS_METADATA)
    execution.params(params)
    args = fat_proc_connec_vis_cargs(params, execution)
    out = fat_proc_connec_vis_outputs(params, execution)
    execution.run(args)
    return out

def fat_proc_connec_vis(
    in_rois: str,
    prefix: str,
    prefix_file: str | None = None,
    tsmoo_kpb: float | None = None,
    tsmoo_niter: float | None = None,
    iso_opt: str | None = None,
    trackid_no_or: bool = False,
    output_tcat: bool = False,
    output_tstat: bool = False,
    wdir: str | None = None,
    no_clean: bool = False,
    runner: Runner | None = None,
) -> FatProcConnecVisOutputs:
    """
    fat_proc_connec_vis

    This program is for visualizing the volumetric output of tracking, mainly for the '-dump_rois ...' from 3dTrackID. It creates surface-ized views of the separate white matter connection maps (WMCs) which can be viewed simultaneously in 3D with SUMA.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_rois: List of separate files, each with a single ROI volume mask; can\
            include wildcards, etc. to specify the list
        prefix: Directory to contain the output files: *cmd.txt and surface files\
            such as *.gii and *.niml.dset; the namebase of files within this\
            directory will be the default for the program, 'wmc'. The value PPP can\
            contain parts of a path in it.
        prefix_file: Prefix for the output files: *cmd.txt and surface files such\
            as *.gii and *.niml.dset; can include path steps; and can make one\
            level of a new directory. For example, if FFF were 'A/B', then the\
            program could make a new directory called 'A' if it didn't exist\
            already and populate it with individual files having the same prefix\
            'B'.
        tsmoo_kpb: 'KPB' parameter in IsoSurface program; default value is 0.01.
        tsmoo_niter: 'NITER' parameter in IsoSurface program; default value is 6.
        iso_opt: Input one of the 'iso* options' from IsoSurface program, such as\
            'isorois+dsets', 'mergerois', etc. Quotations around the entry may be\
            needed, especially if something like the '-mergerois [LAB_OUT]' route\
            is being followed. Default: isorois+dsets
        trackid_no_or: Use this option to have the program recognize the naming\
            convention of 3dTrackID output and to ignore the OR-logic ROIs,\
            including only the AND-logic (AKA pairwise) connections
        output_tcat: Flag to output the multibrick file of concatenated ROI masks;\
            note that the [0]th brick will be all zeros (it is just a placeholder).\
            So, if there are N ROI maps concatenated, there will be N+1 bricks in\
            the output dataset, which has the name PPP_tcat.nii.gz.
        output_tstat: Flag to output the single brick file from the 3dTstat\
            operation on the tcat dataset. If there were N ROI maps concatenated,\
            then the largest value should be N. The output file's name will be\
            PPP_tstat.nii.gz.
        wdir: Working directory prefix. The format is '__WDIR_connec_vis_PPP',\
            where PPP is the input prefix.
        no_clean: Optional switch to NOT remove the working directory (default is\
            to remove the working directory)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_proc_connec_vis_params(
        in_rois=in_rois,
        prefix=prefix,
        prefix_file=prefix_file,
        tsmoo_kpb=tsmoo_kpb,
        tsmoo_niter=tsmoo_niter,
        iso_opt=iso_opt,
        trackid_no_or=trackid_no_or,
        output_tcat=output_tcat,
        output_tstat=output_tstat,
        wdir=wdir,
        no_clean=no_clean,
    )
    return fat_proc_connec_vis_execute(params, runner)

__all__ = [
    "FatProcConnecVis",
    "FatProcConnecVisOutputs",
    "FAT_PROC_CONNEC_VIS_METADATA",
    "fat_proc_connec_vis_cargs",
    "fat_proc_connec_vis_outputs",
    "fat_proc_connec_vis_params",
    "fat_proc_connec_vis_execute",
    "fat_proc_connec_vis_validate",
    "fat_proc_connec_vis",
]