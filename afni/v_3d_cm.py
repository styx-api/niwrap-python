# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_CM_METADATA = Metadata(
    id="3dCM",
    name="3dCM",
    package="afni",
)

V3dCm = typing.TypedDict(
    "V3dCm",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dCM"]],
        "dset": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[bool],
        "set_origin": typing.NotRequired[list[float]],
        "local_ijk": typing.NotRequired[bool],
        "roi_vals": typing.NotRequired[list[float]],
        "all_rois": typing.NotRequired[bool],
        "icent": typing.NotRequired[bool],
        "dcent": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dCmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout: list[str]
    """center of mass of the dataset"""

def v_3d_cm_params(
    dset: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    set_origin: list[float] | None = None,
    local_ijk: bool = False,
    roi_vals: list[float] | None = None,
    all_rois: bool = False,
    icent: bool = False,
    dcent: bool = False,
) -> V3dCm:
    """
    Build parameters.

    Args:
        dset: Input dataset.
        mask: Use the specified dataset as a mask. Only voxels with nonzero values\
            in 'mset' will be averaged from 'dataset'. Both datasets must have the\
            same number of voxels.
        automask: Generate the mask automatically.
        set_origin: After computing the CM of the dataset, set the origin fields in\
            the header so that the CM will be at (x,y,z) in DICOM coordinates.
        local_ijk: Output values as (i,j,k) in local orientation.
        roi_vals: Compute center of mass for each blob with specified voxel values.
        all_rois: Automatically find all ROI values and compute their centers of\
            mass.
        icent: Compute Internal Center, which finds the center voxel closest to the\
            center of mass.
        dcent: Compute Distance Center, the center voxel with the shortest average\
            distance to all other voxels. This is computationally expensive.

    Returns:
        Parameter dictionary.
    """
    params: V3dCm = {
        "@type": "afni/3dCM",
        "dset": dset,
        "automask": automask,
        "local_ijk": local_ijk,
        "all_rois": all_rois,
        "icent": icent,
        "dcent": dcent,
    }
    if mask is not None:
        params["mask"] = mask
    if set_origin is not None:
        params["set_origin"] = set_origin
    if roi_vals is not None:
        params["roi_vals"] = roi_vals
    return params

def v_3d_cm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dCm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("set_origin", None) is not None:
        if not isinstance(params["set_origin"], list):
            raise StyxValidationError(f'`set_origin` has the wrong type: Received `{type(params["set_origin"])}` expected `list[float]`')
        if not (3 <= len(params["set_origin"]) <= 3):
            raise StyxValidationError("Parameter `set_origin` must contain between 3 and 3 elements (inclusive)")
        for e in params["set_origin"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`set_origin` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("local_ijk", None) is not None:
        if not isinstance(params["local_ijk"], bool):
            raise StyxValidationError(f'`local_ijk` has the wrong type: Received `{type(params["local_ijk"])}` expected `bool`')
    if params.get("roi_vals", None) is not None:
        if not isinstance(params["roi_vals"], list):
            raise StyxValidationError(f'`roi_vals` has the wrong type: Received `{type(params["roi_vals"])}` expected `list[float]`')
        for e_2 in params["roi_vals"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`roi_vals` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("all_rois", None) is not None:
        if not isinstance(params["all_rois"], bool):
            raise StyxValidationError(f'`all_rois` has the wrong type: Received `{type(params["all_rois"])}` expected `bool`')
    if params.get("icent", None) is not None:
        if not isinstance(params["icent"], bool):
            raise StyxValidationError(f'`icent` has the wrong type: Received `{type(params["icent"])}` expected `bool`')
    if params.get("dcent", None) is not None:
        if not isinstance(params["dcent"], bool):
            raise StyxValidationError(f'`dcent` has the wrong type: Received `{type(params["dcent"])}` expected `bool`')

def v_3d_cm_cargs(params: V3dCm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dCM")
    cargs.append(execution.input_file(params["dset"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("automask"):
        cargs.append("-automask")
    v_1 = params.get("set_origin")
    if v_1 is not None:
        cargs.append("-set")
        for item0 in v_1:
            cargs.append(str(item0))
    if params.get("local_ijk"):
        cargs.append("-local_ijk")
    v_2 = params.get("roi_vals")
    if v_2 is not None:
        cargs.append("-roi_vals")
        for item1 in v_2:
            cargs.append(str(item1))
    if params.get("all_rois"):
        cargs.append("-all_rois")
    if params.get("icent"):
        cargs.append("-Icent")
    if params.get("dcent"):
        cargs.append("-Dcent")
    return cargs

def v_3d_cm_outputs(params: V3dCm, execution: Execution) -> V3dCmOutputs:
    """Build the V3dCmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dCmOutputs(
        root=root_v,
        stdout=[],
    )

def v_3d_cm_execute(params: V3dCm, runner: Runner | None = None) -> V3dCmOutputs:
    """
    3dCM

    Tool for computing the center of mass of a dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_cm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_CM_METADATA)
    execution.params(params)
    args = v_3d_cm_cargs(params, execution)
    out = v_3d_cm_outputs(params, execution)
    execution.run(args, handle_stdout=lambda s: out.stdout.append(s))
    return out

def v_3d_cm(
    dset: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    set_origin: list[float] | None = None,
    local_ijk: bool = False,
    roi_vals: list[float] | None = None,
    all_rois: bool = False,
    icent: bool = False,
    dcent: bool = False,
    runner: Runner | None = None,
) -> V3dCmOutputs:
    """
    3dCM

    Tool for computing the center of mass of a dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset: Input dataset.
        mask: Use the specified dataset as a mask. Only voxels with nonzero values\
            in 'mset' will be averaged from 'dataset'. Both datasets must have the\
            same number of voxels.
        automask: Generate the mask automatically.
        set_origin: After computing the CM of the dataset, set the origin fields in\
            the header so that the CM will be at (x,y,z) in DICOM coordinates.
        local_ijk: Output values as (i,j,k) in local orientation.
        roi_vals: Compute center of mass for each blob with specified voxel values.
        all_rois: Automatically find all ROI values and compute their centers of\
            mass.
        icent: Compute Internal Center, which finds the center voxel closest to the\
            center of mass.
        dcent: Compute Distance Center, the center voxel with the shortest average\
            distance to all other voxels. This is computationally expensive.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_cm_params(
        dset=dset,
        mask=mask,
        automask=automask,
        set_origin=set_origin,
        local_ijk=local_ijk,
        roi_vals=roi_vals,
        all_rois=all_rois,
        icent=icent,
        dcent=dcent,
    )
    return v_3d_cm_execute(params, runner)

__all__ = [
    "V3dCm",
    "V3dCmOutputs",
    "V_3D_CM_METADATA",
    "v_3d_cm_cargs",
    "v_3d_cm_outputs",
    "v_3d_cm_params",
    "v_3d_cm_execute",
    "v_3d_cm_validate",
    "v_3d_cm",
]