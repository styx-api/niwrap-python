# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_LOMB_SCARGLE_METADATA = Metadata(
    id="3dLombScargle",
    name="3dLombScargle",
    package="afni",
)

V3dLombScargle = typing.TypedDict(
    "V3dLombScargle",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dLombScargle"]],
        "prefix": str,
        "inset": InputPathType,
        "censor_1d": typing.NotRequired[InputPathType],
        "censor_string": typing.NotRequired[str],
        "mask_file": typing.NotRequired[InputPathType],
        "out_pow_spec": typing.NotRequired[bool],
        "nyquist_multiplier": typing.NotRequired[int],
        "nifti": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dLombScargleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    time_points: OutputPathType
    """1D file of the sampled time points (in units of seconds) of the analyzed data set"""
    frequency_points: OutputPathType
    """1D file of the frequency sample points (in units of 1/seconds) of the output periodogram/spectrum data set"""
    amplitude_spectrum: OutputPathType
    """Volumetric data set containing a LS-derived amplitude spectrum"""
    power_spectrum: OutputPathType
    """Volumetric data set containing a LS-derived power spectrum"""

def v_3d_lomb_scargle_params(
    prefix: str,
    inset: InputPathType,
    censor_1d: InputPathType | None = None,
    censor_string: str | None = None,
    mask_file: InputPathType | None = None,
    out_pow_spec: bool = False,
    nyquist_multiplier: int | None = None,
    nifti: bool = False,
) -> V3dLombScargle:
    """
    Build parameters.

    Args:
        prefix: Output prefix name for data volume, time point 1D file, and\
            frequency 1D file
        inset: Time series of volumes, a 4D volumetric data set
        censor_1d: Single row or column of 1s (keep) and 0s (censored) describing\
            which volumes of FILE are kept in the sampling and which are censored\
            out, respectively. The length of the list of numbers must be of the\
            same length as the number of volumes in FILE. If not entered, then the\
            program will look for subbricks of all-zeros and assume those are\
            censored out.
        censor_string: AFNI-style selector string of volumes to *keep* in the\
            analysis. Such as: '[0..4,7,10..$]'.
        mask_file: Optional, mask of volume to analyze; additionally, any voxel\
            with uniformly zero values across time will produce a zero-spectrum
        out_pow_spec: Switch to output the amplitude spectrum of the freqs instead\
            of the periodogram. In the formulation used here, for a time series of\
            length N, the power spectral value S is related to the amplitude value\
            X as: S = (X)**2. (Without this opt, default output is amplitude\
            spectrum.)
        nyquist_multiplier: L-S periodograms can include frequencies above what\
            would typically be considered Nyquist. By default, the maximum\
            frequency will be what f_N *would* have been if no censoring of points\
            had occurred. Acceptable values are >0. (This sets the 'hifac'\
            parameter).
        nifti: Switch to output *.nii.gz volume file (default format is BRIK/HEAD).

    Returns:
        Parameter dictionary.
    """
    params: V3dLombScargle = {
        "@type": "afni/3dLombScargle",
        "prefix": prefix,
        "inset": inset,
        "out_pow_spec": out_pow_spec,
        "nifti": nifti,
    }
    if censor_1d is not None:
        params["censor_1d"] = censor_1d
    if censor_string is not None:
        params["censor_string"] = censor_string
    if mask_file is not None:
        params["mask_file"] = mask_file
    if nyquist_multiplier is not None:
        params["nyquist_multiplier"] = nyquist_multiplier
    return params

def v_3d_lomb_scargle_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dLombScargle."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("censor_1d", None) is not None:
        if not isinstance(params["censor_1d"], (pathlib.Path, str)):
            raise StyxValidationError(f'`censor_1d` has the wrong type: Received `{type(params["censor_1d"])}` expected `InputPathType`')
    if params.get("censor_string", None) is not None:
        if not isinstance(params["censor_string"], str):
            raise StyxValidationError(f'`censor_string` has the wrong type: Received `{type(params["censor_string"])}` expected `str`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("out_pow_spec", None) is not None:
        if not isinstance(params["out_pow_spec"], bool):
            raise StyxValidationError(f'`out_pow_spec` has the wrong type: Received `{type(params["out_pow_spec"])}` expected `bool`')
    if params.get("nyquist_multiplier", None) is not None:
        if not isinstance(params["nyquist_multiplier"], int):
            raise StyxValidationError(f'`nyquist_multiplier` has the wrong type: Received `{type(params["nyquist_multiplier"])}` expected `int`')
    if params.get("nifti", None) is not None:
        if not isinstance(params["nifti"], bool):
            raise StyxValidationError(f'`nifti` has the wrong type: Received `{type(params["nifti"])}` expected `bool`')

def v_3d_lomb_scargle_cargs(params: V3dLombScargle, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dLombScargle")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-inset")
    cargs.append(execution.input_file(params["inset"]))
    v_0 = params.get("censor_1d")
    if v_0 is not None:
        cargs.append("-censor_1D")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("censor_string")
    if v_1 is not None:
        cargs.append("-censor_str")
        cargs.append(v_1)
    v_2 = params.get("mask_file")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    if params.get("out_pow_spec"):
        cargs.append("-out_pow_spec")
    v_3 = params.get("nyquist_multiplier")
    if v_3 is not None:
        cargs.append("-nyq_mult")
        cargs.append(str(v_3))
    if params.get("nifti"):
        cargs.append("-nifti")
    return cargs

def v_3d_lomb_scargle_outputs(params: V3dLombScargle, execution: Execution) -> V3dLombScargleOutputs:
    """Build the V3dLombScargleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    time_points_v: OutputPathType = execution.output_file(f'{params["prefix"]}_time.1D')
    frequency_points_v: OutputPathType = execution.output_file(f'{params["prefix"]}_freq.1D')
    amplitude_spectrum_v: OutputPathType = execution.output_file(f'{params["prefix"]}_amp+orig')
    power_spectrum_v: OutputPathType = execution.output_file(f'{params["prefix"]}_pow+orig')
    return V3dLombScargleOutputs(
        root=root_v,
        time_points=time_points_v,
        frequency_points=frequency_points_v,
        amplitude_spectrum=amplitude_spectrum_v,
        power_spectrum=power_spectrum_v,
    )

def v_3d_lomb_scargle_execute(params: V3dLombScargle, runner: Runner | None = None) -> V3dLombScargleOutputs:
    """
    3dLombScargle

    Make a periodogram or amplitude-spectrum of a time series that has a non-constant sampling rate

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_lomb_scargle_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_LOMB_SCARGLE_METADATA)
    execution.params(params)
    args = v_3d_lomb_scargle_cargs(params, execution)
    out = v_3d_lomb_scargle_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_lomb_scargle(
    prefix: str,
    inset: InputPathType,
    censor_1d: InputPathType | None = None,
    censor_string: str | None = None,
    mask_file: InputPathType | None = None,
    out_pow_spec: bool = False,
    nyquist_multiplier: int | None = None,
    nifti: bool = False,
    runner: Runner | None = None,
) -> V3dLombScargleOutputs:
    """
    3dLombScargle

    Make a periodogram or amplitude-spectrum of a time series that has a non-constant sampling rate

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output prefix name for data volume, time point 1D file, and\
            frequency 1D file
        inset: Time series of volumes, a 4D volumetric data set
        censor_1d: Single row or column of 1s (keep) and 0s (censored) describing\
            which volumes of FILE are kept in the sampling and which are censored\
            out, respectively. The length of the list of numbers must be of the\
            same length as the number of volumes in FILE. If not entered, then the\
            program will look for subbricks of all-zeros and assume those are\
            censored out.
        censor_string: AFNI-style selector string of volumes to *keep* in the\
            analysis. Such as: '[0..4,7,10..$]'.
        mask_file: Optional, mask of volume to analyze; additionally, any voxel\
            with uniformly zero values across time will produce a zero-spectrum
        out_pow_spec: Switch to output the amplitude spectrum of the freqs instead\
            of the periodogram. In the formulation used here, for a time series of\
            length N, the power spectral value S is related to the amplitude value\
            X as: S = (X)**2. (Without this opt, default output is amplitude\
            spectrum.)
        nyquist_multiplier: L-S periodograms can include frequencies above what\
            would typically be considered Nyquist. By default, the maximum\
            frequency will be what f_N *would* have been if no censoring of points\
            had occurred. Acceptable values are >0. (This sets the 'hifac'\
            parameter).
        nifti: Switch to output *.nii.gz volume file (default format is BRIK/HEAD).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_lomb_scargle_params(
        prefix=prefix,
        inset=inset,
        censor_1d=censor_1d,
        censor_string=censor_string,
        mask_file=mask_file,
        out_pow_spec=out_pow_spec,
        nyquist_multiplier=nyquist_multiplier,
        nifti=nifti,
    )
    return v_3d_lomb_scargle_execute(params, runner)

__all__ = [
    "V3dLombScargle",
    "V3dLombScargleOutputs",
    "V_3D_LOMB_SCARGLE_METADATA",
    "v_3d_lomb_scargle_cargs",
    "v_3d_lomb_scargle_outputs",
    "v_3d_lomb_scargle_params",
    "v_3d_lomb_scargle_execute",
    "v_3d_lomb_scargle_validate",
    "v_3d_lomb_scargle",
]