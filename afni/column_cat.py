# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COLUMN_CAT_METADATA = Metadata(
    id="column_cat",
    name="column_cat",
    package="afni",
)

ColumnCat = typing.TypedDict(
    "ColumnCat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/column_cat"]],
        "line_number": typing.NotRequired[float],
        "separator_string": typing.NotRequired[str],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class ColumnCatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Redirect output of concatenation to a file"""

def column_cat_params(
    input_files: list[InputPathType],
    line_number: float | None = None,
    separator_string: str | None = None,
) -> ColumnCat:
    """
    Build parameters.

    Args:
        input_files: Input files to be concatenated
        line_number: Print only the specified line number (1-based)
        separator_string: Use the specified string as a separator between columns

    Returns:
        Parameter dictionary.
    """
    params: ColumnCat = {
        "@type": "afni/column_cat",
        "input_files": input_files,
    }
    if line_number is not None:
        params["line_number"] = line_number
    if separator_string is not None:
        params["separator_string"] = separator_string
    return params

def column_cat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ColumnCat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("line_number", None) is not None:
        if not isinstance(params["line_number"], (float, int)):
            raise StyxValidationError(f'`line_number` has the wrong type: Received `{type(params["line_number"])}` expected `float`')
    if params.get("separator_string", None) is not None:
        if not isinstance(params["separator_string"], str):
            raise StyxValidationError(f'`separator_string` has the wrong type: Received `{type(params["separator_string"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def column_cat_cargs(params: ColumnCat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("column_cat")
    v_0 = params.get("line_number")
    if v_0 is not None:
        cargs.append("-line")
        cargs.append(str(v_0))
    v_1 = params.get("separator_string")
    if v_1 is not None:
        cargs.append("-sep")
        cargs.append(v_1)
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def column_cat_outputs(params: ColumnCat, execution: Execution) -> ColumnCatOutputs:
    """Build the ColumnCatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("output_file.txt")
    return ColumnCatOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def column_cat_execute(params: ColumnCat, runner: Runner | None = None) -> ColumnCatOutputs:
    """
    column_cat

    Catenate files horizontally. Each line of output is the concatenation of each current line from the input files, all on the same line, separated by a space or a user-defined separator.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    column_cat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COLUMN_CAT_METADATA)
    execution.params(params)
    args = column_cat_cargs(params, execution)
    out = column_cat_outputs(params, execution)
    execution.run(args)
    return out

def column_cat(
    input_files: list[InputPathType],
    line_number: float | None = None,
    separator_string: str | None = None,
    runner: Runner | None = None,
) -> ColumnCatOutputs:
    """
    column_cat

    Catenate files horizontally. Each line of output is the concatenation of each current line from the input files, all on the same line, separated by a space or a user-defined separator.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input files to be concatenated
        line_number: Print only the specified line number (1-based)
        separator_string: Use the specified string as a separator between columns
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = column_cat_params(
        input_files=input_files,
        line_number=line_number,
        separator_string=separator_string,
    )
    return column_cat_execute(params, runner)

__all__ = [
    "ColumnCat",
    "ColumnCatOutputs",
    "COLUMN_CAT_METADATA",
    "column_cat_cargs",
    "column_cat_outputs",
    "column_cat_params",
    "column_cat_execute",
    "column_cat_validate",
    "column_cat",
]