# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_2SWAP_METADATA = Metadata(
    id="2swap",
    name="2swap",
    package="afni",
)

V2swap = typing.TypedDict(
    "V2swap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/2swap"]],
        "quiet": typing.NotRequired[bool],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class V2swapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_2swap_params(
    input_files: list[InputPathType],
    quiet: bool = False,
) -> V2swap:
    """
    Build parameters.

    Args:
        input_files: Input files
        quiet: Work quietly

    Returns:
        Parameter dictionary.
    """
    params: V2swap = {
        "@type": "afni/2swap",
        "input_files": input_files,
        "quiet": quiet,
    }
    return params

def v_2swap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V2swap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def v_2swap_cargs(params: V2swap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("2swap")
    if params.get("quiet"):
        cargs.append("-q")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def v_2swap_outputs(params: V2swap, execution: Execution) -> V2swapOutputs:
    """Build the V2swapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V2swapOutputs(
        root=root_v,
    )

def v_2swap_execute(params: V2swap, runner: Runner | None = None) -> V2swapOutputs:
    """
    2swap

    Swaps byte pairs on the files listed

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_2swap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_2SWAP_METADATA)
    execution.params(params)
    args = v_2swap_cargs(params, execution)
    out = v_2swap_outputs(params, execution)
    execution.run(args)
    return out

def v_2swap(
    input_files: list[InputPathType],
    quiet: bool = False,
    runner: Runner | None = None,
) -> V2swapOutputs:
    """
    2swap

    Swaps byte pairs on the files listed

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input files
        quiet: Work quietly
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_2swap_params(
        input_files=input_files,
        quiet=quiet,
    )
    return v_2swap_execute(params, runner)

__all__ = [
    "V2swap",
    "V2swapOutputs",
    "V_2SWAP_METADATA",
    "v_2swap_cargs",
    "v_2swap_outputs",
    "v_2swap_params",
    "v_2swap_execute",
    "v_2swap_validate",
    "v_2swap",
]