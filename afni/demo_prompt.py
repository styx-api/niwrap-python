# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DEMO_PROMPT_METADATA = Metadata(
    id="@demo_prompt",
    name="@demo_prompt",
    package="afni",
)

DemoPrompt = typing.TypedDict(
    "DemoPrompt",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@demo_prompt"]],
        "message": str,
    },
)

@dataclasses.dataclass
class DemoPromptOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    status: OutputPathType
    """Status output: 0 if user presses OK, 1 if user cancels"""

def demo_prompt_params(
    message: str,
) -> DemoPrompt:
    """
    Build parameters.

    Args:
        message: The message to display in the prompt

    Returns:
        Parameter dictionary.
    """
    params: DemoPrompt = {
        "@type": "afni/@demo_prompt",
        "message": message,
    }
    return params

def demo_prompt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DemoPrompt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("message", None) is None:
        raise StyxValidationError("`message` must not be None")
    if not isinstance(params["message"], str):
        raise StyxValidationError(f'`message` has the wrong type: Received `{type(params["message"])}` expected `str`')

def demo_prompt_cargs(params: DemoPrompt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@demo_prompt")
    cargs.append(params["message"])
    return cargs

def demo_prompt_outputs(params: DemoPrompt, execution: Execution) -> DemoPromptOutputs:
    """Build the DemoPromptOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    status_v: OutputPathType = execution.output_file("status")
    return DemoPromptOutputs(
        root=root_v,
        status=status_v,
    )

def demo_prompt_execute(params: DemoPrompt, runner: Runner | None = None) -> DemoPromptOutputs:
    """
    @demo_prompt

    Prompts user with a message and waits for acknowledgment

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    demo_prompt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DEMO_PROMPT_METADATA)
    execution.params(params)
    args = demo_prompt_cargs(params, execution)
    out = demo_prompt_outputs(params, execution)
    execution.run(args)
    return out

def demo_prompt(
    message: str,
    runner: Runner | None = None,
) -> DemoPromptOutputs:
    """
    @demo_prompt

    Prompts user with a message and waits for acknowledgment

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        message: The message to display in the prompt
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = demo_prompt_params(
        message=message,
    )
    return demo_prompt_execute(params, runner)

__all__ = [
    "DemoPrompt",
    "DemoPromptOutputs",
    "DEMO_PROMPT_METADATA",
    "demo_prompt_cargs",
    "demo_prompt_outputs",
    "demo_prompt_params",
    "demo_prompt_execute",
    "demo_prompt_validate",
    "demo_prompt",
]