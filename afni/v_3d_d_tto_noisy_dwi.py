# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_D_TTO_NOISY_DWI_METADATA = Metadata(
    id="3dDTtoNoisyDWI",
    name="3dDTtoNoisyDWI",
    package="afni",
)

V3dDTtoNoisyDwi = typing.TypedDict(
    "V3dDTtoNoisyDwi",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dDTtoNoisyDWI"]],
        "dt_file": InputPathType,
        "grad_file": InputPathType,
        "noise_dwi": float,
        "noise_b0": typing.NotRequired[float],
        "prefix": str,
        "mask": typing.NotRequired[InputPathType],
        "bval": typing.NotRequired[float],
        "s0": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dDTtoNoisyDwiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dwi: OutputPathType
    """Synthetic set of DWI measures with noise. Contains N+1 bricks mimicking B0+DWI data."""

def v_3d_d_tto_noisy_dwi_params(
    dt_file: InputPathType,
    grad_file: InputPathType,
    noise_dwi: float,
    prefix: str,
    noise_b0: float | None = None,
    mask: InputPathType | None = None,
    bval: float | None = None,
    s0: float | None = None,
) -> V3dDTtoNoisyDwi:
    """
    Build parameters.

    Args:
        dt_file: Diffusion tensor file with six bricks of DT components ordered in\
            the AFNI manner: Dxx,Dxy,Dyy,Dxz,Dyz,Dzz.
        grad_file: Text file of gradients arranged in three columns. There should\
            be no row of all zeros representing the b=0 line.
        noise_dwi: Fractional value of noise in DWIs. FF = sigma/S0 = 1/SNR0. For\
            example, FF=0.05 corresponds to SNR0=20.
        prefix: Output file name prefix. Will have N+1 bricks when GRADFILE has N\
            rows of gradients.
        noise_b0: Optional fraction of Rician noise in the b=0 reference image. If\
            not provided, FF2=FF.
        mask: Optional mask within which to calculate uncertainty. Data should be\
            masked already otherwise.
        bval: Optional DW factor to use if DT values are scaled to something\
            physical. Default is BB=1.
        s0: Optional reference b=0 signal strength. Default value is SS=1000.

    Returns:
        Parameter dictionary.
    """
    params: V3dDTtoNoisyDwi = {
        "@type": "afni/3dDTtoNoisyDWI",
        "dt_file": dt_file,
        "grad_file": grad_file,
        "noise_dwi": noise_dwi,
        "prefix": prefix,
    }
    if noise_b0 is not None:
        params["noise_b0"] = noise_b0
    if mask is not None:
        params["mask"] = mask
    if bval is not None:
        params["bval"] = bval
    if s0 is not None:
        params["s0"] = s0
    return params

def v_3d_d_tto_noisy_dwi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dDTtoNoisyDwi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dt_file", None) is None:
        raise StyxValidationError("`dt_file` must not be None")
    if not isinstance(params["dt_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dt_file` has the wrong type: Received `{type(params["dt_file"])}` expected `InputPathType`')
    if params.get("grad_file", None) is None:
        raise StyxValidationError("`grad_file` must not be None")
    if not isinstance(params["grad_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`grad_file` has the wrong type: Received `{type(params["grad_file"])}` expected `InputPathType`')
    if params.get("noise_dwi", None) is None:
        raise StyxValidationError("`noise_dwi` must not be None")
    if not isinstance(params["noise_dwi"], (float, int)):
        raise StyxValidationError(f'`noise_dwi` has the wrong type: Received `{type(params["noise_dwi"])}` expected `float`')
    if params.get("noise_b0", None) is not None:
        if not isinstance(params["noise_b0"], (float, int)):
            raise StyxValidationError(f'`noise_b0` has the wrong type: Received `{type(params["noise_b0"])}` expected `float`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("bval", None) is not None:
        if not isinstance(params["bval"], (float, int)):
            raise StyxValidationError(f'`bval` has the wrong type: Received `{type(params["bval"])}` expected `float`')
    if params.get("s0", None) is not None:
        if not isinstance(params["s0"], (float, int)):
            raise StyxValidationError(f'`s0` has the wrong type: Received `{type(params["s0"])}` expected `float`')

def v_3d_d_tto_noisy_dwi_cargs(params: V3dDTtoNoisyDwi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dDTtoNoisyDWI")
    cargs.append(execution.input_file(params["dt_file"]))
    cargs.append(execution.input_file(params["grad_file"]))
    cargs.append("-noise_DWI")
    cargs.append(str(params["noise_dwi"]))
    v_0 = params.get("noise_b0")
    if v_0 is not None:
        cargs.append("-noise_B0")
        cargs.append(str(v_0))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("bval")
    if v_2 is not None:
        cargs.append("-bval")
        cargs.append(str(v_2))
    v_3 = params.get("s0")
    if v_3 is not None:
        cargs.append("-S0")
        cargs.append(str(v_3))
    return cargs

def v_3d_d_tto_noisy_dwi_outputs(params: V3dDTtoNoisyDwi, execution: Execution) -> V3dDTtoNoisyDwiOutputs:
    """Build the V3dDTtoNoisyDwiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dwi_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig')
    return V3dDTtoNoisyDwiOutputs(
        root=root_v,
        output_dwi=output_dwi_v,
    )

def v_3d_d_tto_noisy_dwi_execute(params: V3dDTtoNoisyDwi, runner: Runner | None = None) -> V3dDTtoNoisyDwiOutputs:
    """
    3dDTtoNoisyDWI

    Generate a synthetic set of DWI measures with a given SNR from an AFNI-style DT file and a set of gradients. This can be useful for simulations and testing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_d_tto_noisy_dwi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_D_TTO_NOISY_DWI_METADATA)
    execution.params(params)
    args = v_3d_d_tto_noisy_dwi_cargs(params, execution)
    out = v_3d_d_tto_noisy_dwi_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_d_tto_noisy_dwi(
    dt_file: InputPathType,
    grad_file: InputPathType,
    noise_dwi: float,
    prefix: str,
    noise_b0: float | None = None,
    mask: InputPathType | None = None,
    bval: float | None = None,
    s0: float | None = None,
    runner: Runner | None = None,
) -> V3dDTtoNoisyDwiOutputs:
    """
    3dDTtoNoisyDWI

    Generate a synthetic set of DWI measures with a given SNR from an AFNI-style DT file and a set of gradients. This can be useful for simulations and testing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dt_file: Diffusion tensor file with six bricks of DT components ordered in\
            the AFNI manner: Dxx,Dxy,Dyy,Dxz,Dyz,Dzz.
        grad_file: Text file of gradients arranged in three columns. There should\
            be no row of all zeros representing the b=0 line.
        noise_dwi: Fractional value of noise in DWIs. FF = sigma/S0 = 1/SNR0. For\
            example, FF=0.05 corresponds to SNR0=20.
        prefix: Output file name prefix. Will have N+1 bricks when GRADFILE has N\
            rows of gradients.
        noise_b0: Optional fraction of Rician noise in the b=0 reference image. If\
            not provided, FF2=FF.
        mask: Optional mask within which to calculate uncertainty. Data should be\
            masked already otherwise.
        bval: Optional DW factor to use if DT values are scaled to something\
            physical. Default is BB=1.
        s0: Optional reference b=0 signal strength. Default value is SS=1000.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_d_tto_noisy_dwi_params(
        dt_file=dt_file,
        grad_file=grad_file,
        noise_dwi=noise_dwi,
        prefix=prefix,
        noise_b0=noise_b0,
        mask=mask,
        bval=bval,
        s0=s0,
    )
    return v_3d_d_tto_noisy_dwi_execute(params, runner)

__all__ = [
    "V3dDTtoNoisyDwi",
    "V3dDTtoNoisyDwiOutputs",
    "V_3D_D_TTO_NOISY_DWI_METADATA",
    "v_3d_d_tto_noisy_dwi_cargs",
    "v_3d_d_tto_noisy_dwi_outputs",
    "v_3d_d_tto_noisy_dwi_params",
    "v_3d_d_tto_noisy_dwi_execute",
    "v_3d_d_tto_noisy_dwi_validate",
    "v_3d_d_tto_noisy_dwi",
]