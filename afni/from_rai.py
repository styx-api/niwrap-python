# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FROM_RAI_METADATA = Metadata(
    id="@FromRAI",
    name="@FromRAI",
    package="afni",
)

FromRai = typing.TypedDict(
    "FromRai",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@FromRAI"]],
        "rai_coordinates": list[float],
        "orientation": str,
    },
)

@dataclasses.dataclass
class FromRaiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def from_rai_params(
    rai_coordinates: list[float],
    orientation: str,
) -> FromRai:
    """
    Build parameters.

    Args:
        rai_coordinates:
        orientation:

    Returns:
        Parameter dictionary.
    """
    params: FromRai = {
        "@type": "afni/@FromRAI",
        "rai_coordinates": rai_coordinates,
        "orientation": orientation,
    }
    return params

def from_rai_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FromRai."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("rai_coordinates", None) is None:
        raise StyxValidationError("`rai_coordinates` must not be None")
    if not isinstance(params["rai_coordinates"], list):
        raise StyxValidationError(f'`rai_coordinates` has the wrong type: Received `{type(params["rai_coordinates"])}` expected `list[float]`')
    if not (3 <= len(params["rai_coordinates"]) <= 3):
        raise StyxValidationError("Parameter `rai_coordinates` must contain between 3 and 3 elements (inclusive)")
    for e in params["rai_coordinates"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`rai_coordinates` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("orientation", None) is None:
        raise StyxValidationError("`orientation` must not be None")
    if not isinstance(params["orientation"], str):
        raise StyxValidationError(f'`orientation` has the wrong type: Received `{type(params["orientation"])}` expected `str`')

def from_rai_cargs(params: FromRai, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@FromRAI")
    cargs.append("-xyz")
    for item0 in params["rai_coordinates"]:
        cargs.append(str(item0))
    cargs.append("-or")
    cargs.append(params["orientation"])
    return cargs

def from_rai_outputs(params: FromRai, execution: Execution) -> FromRaiOutputs:
    """Build the FromRaiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FromRaiOutputs(
        root=root_v,
    )

def from_rai_execute(params: FromRai, runner: Runner | None = None) -> FromRaiOutputs:
    """
    @FromRAI

    Changes the RAI coordinates to the specified orientation

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    from_rai_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FROM_RAI_METADATA)
    execution.params(params)
    args = from_rai_cargs(params, execution)
    out = from_rai_outputs(params, execution)
    execution.run(args)
    return out

def from_rai(
    rai_coordinates: list[float],
    orientation: str,
    runner: Runner | None = None,
) -> FromRaiOutputs:
    """
    @FromRAI

    Changes the RAI coordinates to the specified orientation

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        rai_coordinates:
        orientation:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = from_rai_params(
        rai_coordinates=rai_coordinates,
        orientation=orientation,
    )
    return from_rai_execute(params, runner)

__all__ = [
    "FromRai",
    "FromRaiOutputs",
    "FROM_RAI_METADATA",
    "from_rai_cargs",
    "from_rai_outputs",
    "from_rai_params",
    "from_rai_execute",
    "from_rai_validate",
    "from_rai",
]