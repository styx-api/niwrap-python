# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_CLIP_LEVEL_METADATA = Metadata(
    id="3dClipLevel",
    name="3dClipLevel",
    package="afni",
)

V3dClipLevel = typing.TypedDict(
    "V3dClipLevel",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dClipLevel"]],
        "dataset": InputPathType,
        "mfrac": typing.NotRequired[float],
        "doall": typing.NotRequired[bool],
        "grad": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dClipLevelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_clip_level_params(
    dataset: InputPathType,
    mfrac: float | None = None,
    doall: bool = False,
    grad: str | None = None,
) -> V3dClipLevel:
    """
    Build parameters.

    Args:
        dataset: Input dataset (e.g. dataset.nii.gz)
        mfrac: Use the number ff instead of 0.50 in the algorithm.
        doall: Apply the algorithm to each sub-brick separately.
        grad: Compute a 'gradual' clip level as a function of voxel position and\
            output to a dataset with the given prefix.

    Returns:
        Parameter dictionary.
    """
    params: V3dClipLevel = {
        "@type": "afni/3dClipLevel",
        "dataset": dataset,
        "doall": doall,
    }
    if mfrac is not None:
        params["mfrac"] = mfrac
    if grad is not None:
        params["grad"] = grad
    return params

def v_3d_clip_level_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dClipLevel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')
    if params.get("mfrac", None) is not None:
        if not isinstance(params["mfrac"], (float, int)):
            raise StyxValidationError(f'`mfrac` has the wrong type: Received `{type(params["mfrac"])}` expected `float`')
    if params.get("doall", None) is not None:
        if not isinstance(params["doall"], bool):
            raise StyxValidationError(f'`doall` has the wrong type: Received `{type(params["doall"])}` expected `bool`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], str):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `str`')

def v_3d_clip_level_cargs(params: V3dClipLevel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dClipLevel")
    cargs.append(execution.input_file(params["dataset"]))
    v_0 = params.get("mfrac")
    if v_0 is not None:
        cargs.append("-mfrac")
        cargs.append(str(v_0))
    if params.get("doall"):
        cargs.append("-doall")
    v_1 = params.get("grad")
    if v_1 is not None:
        cargs.append("-grad")
        cargs.append(v_1)
    return cargs

def v_3d_clip_level_outputs(params: V3dClipLevel, execution: Execution) -> V3dClipLevelOutputs:
    """Build the V3dClipLevelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dClipLevelOutputs(
        root=root_v,
    )

def v_3d_clip_level_execute(params: V3dClipLevel, runner: Runner | None = None) -> V3dClipLevelOutputs:
    """
    3dClipLevel

    Estimates the value at which to clip the anatomical dataset so that background regions are set to zero.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_clip_level_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_CLIP_LEVEL_METADATA)
    execution.params(params)
    args = v_3d_clip_level_cargs(params, execution)
    out = v_3d_clip_level_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_clip_level(
    dataset: InputPathType,
    mfrac: float | None = None,
    doall: bool = False,
    grad: str | None = None,
    runner: Runner | None = None,
) -> V3dClipLevelOutputs:
    """
    3dClipLevel

    Estimates the value at which to clip the anatomical dataset so that background regions are set to zero.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset (e.g. dataset.nii.gz)
        mfrac: Use the number ff instead of 0.50 in the algorithm.
        doall: Apply the algorithm to each sub-brick separately.
        grad: Compute a 'gradual' clip level as a function of voxel position and\
            output to a dataset with the given prefix.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_clip_level_params(
        dataset=dataset,
        mfrac=mfrac,
        doall=doall,
        grad=grad,
    )
    return v_3d_clip_level_execute(params, runner)

__all__ = [
    "V3dClipLevel",
    "V3dClipLevelOutputs",
    "V_3D_CLIP_LEVEL_METADATA",
    "v_3d_clip_level_cargs",
    "v_3d_clip_level_outputs",
    "v_3d_clip_level_params",
    "v_3d_clip_level_execute",
    "v_3d_clip_level_validate",
    "v_3d_clip_level",
]