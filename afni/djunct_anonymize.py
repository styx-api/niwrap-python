# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DJUNCT_ANONYMIZE_METADATA = Metadata(
    id="@djunct_anonymize",
    name="@djunct_anonymize",
    package="afni",
)

DjunctAnonymize = typing.TypedDict(
    "DjunctAnonymize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@djunct_anonymize"]],
        "input": InputPathType,
        "add_note": typing.NotRequired[str],
        "copy_to": typing.NotRequired[InputPathType],
        "overwrite": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DjunctAnonymizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def djunct_anonymize_params(
    input_: InputPathType,
    add_note: str | None = None,
    copy_to: InputPathType | None = None,
    overwrite: bool = False,
) -> DjunctAnonymize:
    """
    Build parameters.

    Args:
        input_: Input dataset
        add_note: Add a note to the history after anonymizing
        copy_to: Copy the input to a new file, which is then anonymized
        overwrite: Overwrite the existing file if using -copy_to

    Returns:
        Parameter dictionary.
    """
    params: DjunctAnonymize = {
        "@type": "afni/@djunct_anonymize",
        "input": input_,
        "overwrite": overwrite,
    }
    if add_note is not None:
        params["add_note"] = add_note
    if copy_to is not None:
        params["copy_to"] = copy_to
    return params

def djunct_anonymize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DjunctAnonymize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("add_note", None) is not None:
        if not isinstance(params["add_note"], str):
            raise StyxValidationError(f'`add_note` has the wrong type: Received `{type(params["add_note"])}` expected `str`')
    if params.get("copy_to", None) is not None:
        if not isinstance(params["copy_to"], (pathlib.Path, str)):
            raise StyxValidationError(f'`copy_to` has the wrong type: Received `{type(params["copy_to"])}` expected `InputPathType`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')

def djunct_anonymize_cargs(params: DjunctAnonymize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@djunct_anonymize")
    cargs.append(execution.input_file(params["input"]))
    v_0 = params.get("add_note")
    if v_0 is not None:
        cargs.append("-add_note")
        cargs.append(v_0)
    v_1 = params.get("copy_to")
    if v_1 is not None:
        cargs.append("-copy_to")
        cargs.append(execution.input_file(v_1))
    if params.get("overwrite"):
        cargs.append("-overwrite")
    return cargs

def djunct_anonymize_outputs(params: DjunctAnonymize, execution: Execution) -> DjunctAnonymizeOutputs:
    """Build the DjunctAnonymizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DjunctAnonymizeOutputs(
        root=root_v,
    )

def djunct_anonymize_execute(params: DjunctAnonymize, runner: Runner | None = None) -> DjunctAnonymizeOutputs:
    """
    @djunct_anonymize

    Helper program to anonymize files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    djunct_anonymize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DJUNCT_ANONYMIZE_METADATA)
    execution.params(params)
    args = djunct_anonymize_cargs(params, execution)
    out = djunct_anonymize_outputs(params, execution)
    execution.run(args)
    return out

def djunct_anonymize(
    input_: InputPathType,
    add_note: str | None = None,
    copy_to: InputPathType | None = None,
    overwrite: bool = False,
    runner: Runner | None = None,
) -> DjunctAnonymizeOutputs:
    """
    @djunct_anonymize

    Helper program to anonymize files

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Input dataset
        add_note: Add a note to the history after anonymizing
        copy_to: Copy the input to a new file, which is then anonymized
        overwrite: Overwrite the existing file if using -copy_to
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = djunct_anonymize_params(
        input_=input_,
        add_note=add_note,
        copy_to=copy_to,
        overwrite=overwrite,
    )
    return djunct_anonymize_execute(params, runner)

__all__ = [
    "DjunctAnonymize",
    "DjunctAnonymizeOutputs",
    "DJUNCT_ANONYMIZE_METADATA",
    "djunct_anonymize_cargs",
    "djunct_anonymize_outputs",
    "djunct_anonymize_params",
    "djunct_anonymize_execute",
    "djunct_anonymize_validate",
    "djunct_anonymize",
]