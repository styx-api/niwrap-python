# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ROW_FILLIN_METADATA = Metadata(
    id="3dRowFillin",
    name="3dRowFillin",
    package="afni",
)

V3dRowFillin = typing.TypedDict(
    "V3dRowFillin",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dRowFillin"]],
        "maxgap": typing.NotRequired[float],
        "dir": typing.NotRequired[str],
        "binary": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "input_dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dRowFillinOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_brik: OutputPathType | None
    """Output dataset in BRIK format"""
    output_head: OutputPathType | None
    """Output dataset in HEAD format"""

def v_3d_row_fillin_params(
    input_dataset: InputPathType,
    maxgap: float | None = None,
    dir: str | None = None,
    binary: bool = False,
    prefix: str | None = None,
) -> V3dRowFillin:
    """
    Build parameters.

    Args:
        input_dataset: Input 3D dataset (e.g., dataset+orig)
        maxgap: Set the maximum length of a blank region that will be filled in
        dir: Set the direction of fill, e.g., A-P, P-A, I-S, S-I, L-R, R-L, x, y,\
            z, XYZ.OR, XYZ.AND
        binary: Turn input dataset to binary (0 and 1) before filling in. Output\
            will also be binary.
        prefix: Set the prefix for the output dataset

    Returns:
        Parameter dictionary.
    """
    params: V3dRowFillin = {
        "@type": "afni/3dRowFillin",
        "input_dataset": input_dataset,
        "binary": binary,
    }
    if maxgap is not None:
        params["maxgap"] = maxgap
    if dir is not None:
        params["dir"] = dir
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_row_fillin_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dRowFillin."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("maxgap", None) is not None:
        if not isinstance(params["maxgap"], (float, int)):
            raise StyxValidationError(f'`maxgap` has the wrong type: Received `{type(params["maxgap"])}` expected `float`')
    if params.get("dir", None) is not None:
        if not isinstance(params["dir"], str):
            raise StyxValidationError(f'`dir` has the wrong type: Received `{type(params["dir"])}` expected `str`')
    if params.get("binary", None) is not None:
        if not isinstance(params["binary"], bool):
            raise StyxValidationError(f'`binary` has the wrong type: Received `{type(params["binary"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')

def v_3d_row_fillin_cargs(params: V3dRowFillin, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dRowFillin")
    v_0 = params.get("maxgap")
    if v_0 is not None:
        cargs.append("-maxgap")
        cargs.append(str(v_0))
    v_1 = params.get("dir")
    if v_1 is not None:
        cargs.append("-dir")
        cargs.append(v_1)
    if params.get("binary"):
        cargs.append("-binary")
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    cargs.append(execution.input_file(params["input_dataset"]))
    return cargs

def v_3d_row_fillin_outputs(params: V3dRowFillin, execution: Execution) -> V3dRowFillinOutputs:
    """Build the V3dRowFillinOutputs object for this tool."""
    output_brik_v: OutputPathType | None = None
    output_head_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_brik_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.BRIK')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_head_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.HEAD')
    return V3dRowFillinOutputs(
        root=root_v,
        output_brik=output_brik_v,
        output_head=output_head_v,
    )

def v_3d_row_fillin_execute(params: V3dRowFillin, runner: Runner | None = None) -> V3dRowFillinOutputs:
    """
    3dRowFillin

    Fills in blank regions in 1D rows extracted from a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_row_fillin_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ROW_FILLIN_METADATA)
    execution.params(params)
    args = v_3d_row_fillin_cargs(params, execution)
    out = v_3d_row_fillin_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_row_fillin(
    input_dataset: InputPathType,
    maxgap: float | None = None,
    dir: str | None = None,
    binary: bool = False,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dRowFillinOutputs:
    """
    3dRowFillin

    Fills in blank regions in 1D rows extracted from a 3D dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input 3D dataset (e.g., dataset+orig)
        maxgap: Set the maximum length of a blank region that will be filled in
        dir: Set the direction of fill, e.g., A-P, P-A, I-S, S-I, L-R, R-L, x, y,\
            z, XYZ.OR, XYZ.AND
        binary: Turn input dataset to binary (0 and 1) before filling in. Output\
            will also be binary.
        prefix: Set the prefix for the output dataset
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_row_fillin_params(
        input_dataset=input_dataset,
        maxgap=maxgap,
        dir=dir,
        binary=binary,
        prefix=prefix,
    )
    return v_3d_row_fillin_execute(params, runner)

__all__ = [
    "V3dRowFillin",
    "V3dRowFillinOutputs",
    "V_3D_ROW_FILLIN_METADATA",
    "v_3d_row_fillin_cargs",
    "v_3d_row_fillin_outputs",
    "v_3d_row_fillin_params",
    "v_3d_row_fillin_execute",
    "v_3d_row_fillin_validate",
    "v_3d_row_fillin",
]