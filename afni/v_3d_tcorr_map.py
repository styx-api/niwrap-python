# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TCORR_MAP_METADATA = Metadata(
    id="3dTcorrMap",
    name="3dTcorrMap",
    package="afni",
)

V3dTcorrMap = typing.TypedDict(
    "V3dTcorrMap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTcorrMap"]],
        "input": InputPathType,
        "seed": typing.NotRequired[InputPathType],
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[bool],
        "mean": typing.NotRequired[str],
        "zmean": typing.NotRequired[str],
        "qmean": typing.NotRequired[str],
        "pmean": typing.NotRequired[str],
        "thresh": typing.NotRequired[str],
        "varthresh": typing.NotRequired[str],
        "norm_varthresh": typing.NotRequired[str],
        "corrmap": typing.NotRequired[str],
        "corrmask": typing.NotRequired[bool],
        "aexpr": typing.NotRequired[str],
        "cexpr": typing.NotRequired[str],
        "sexpr": typing.NotRequired[str],
        "hist": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dTcorrMapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_tcorr_map_params(
    input_: InputPathType,
    seed: InputPathType | None = None,
    mask: InputPathType | None = None,
    automask: bool = False,
    mean: str | None = None,
    zmean: str | None = None,
    qmean: str | None = None,
    pmean: str | None = None,
    thresh: str | None = None,
    varthresh: str | None = None,
    norm_varthresh: str | None = None,
    corrmap: str | None = None,
    corrmask: bool = False,
    aexpr: str | None = None,
    cexpr: str | None = None,
    sexpr: str | None = None,
    hist: str | None = None,
) -> V3dTcorrMap:
    """
    Build parameters.

    Args:
        input_: Read 3D+time dataset 'dd'. This provides the time series to be\
            correlated en masse. This is a mandatory option.
        seed: Read 3D+time dataset 'bb'. It correlates the -seed voxel time series\
            with every voxel in the -input dataset.
        mask: Read dataset 'mmm' as a voxel mask.
        automask: Create a mask from the input dataset.
        mean: Save average correlations into dataset prefix 'pp'.
        zmean: Save tanh of mean arctanh(correlation) into 'pp'.
        qmean: Save RMS(correlation) into 'pp'.
        pmean: Save average of squared positive correlations into 'pp'.
        thresh: Save the COUNT of how many voxels survived thresholding at level\
            abs(correlation) >= tt.
        varthresh: Save the COUNT of how many voxels survive thresholding at\
            multiple levels abs(correlation) >= tt.
        norm_varthresh: Like '-VarThresh', but the output counts are 'Normalized'.
        corrmap: Output at each voxel the entire correlation map, into a dataset\
            with prefix 'pp'.
        corrmask: By default, -CorrMap outputs a sub-brick for EACH input dataset\
            voxel. Eliminates these sub-bricks using this option.
        aexpr: For each correlation 'r', compute the calc-style expression 'expr',\
            and average these values to get the output that goes into dataset\
            'ppp'.
        cexpr: As in '-Aexpr', but only average together nonzero values computed by\
            'expr'.
        sexpr: As above, but the sum of the expressions is computed rather than the\
            average.
        hist: For each voxel, save a histogram of the correlation coefficients into\
            dataset ppp.

    Returns:
        Parameter dictionary.
    """
    params: V3dTcorrMap = {
        "@type": "afni/3dTcorrMap",
        "input": input_,
        "automask": automask,
        "corrmask": corrmask,
    }
    if seed is not None:
        params["seed"] = seed
    if mask is not None:
        params["mask"] = mask
    if mean is not None:
        params["mean"] = mean
    if zmean is not None:
        params["zmean"] = zmean
    if qmean is not None:
        params["qmean"] = qmean
    if pmean is not None:
        params["pmean"] = pmean
    if thresh is not None:
        params["thresh"] = thresh
    if varthresh is not None:
        params["varthresh"] = varthresh
    if norm_varthresh is not None:
        params["norm_varthresh"] = norm_varthresh
    if corrmap is not None:
        params["corrmap"] = corrmap
    if aexpr is not None:
        params["aexpr"] = aexpr
    if cexpr is not None:
        params["cexpr"] = cexpr
    if sexpr is not None:
        params["sexpr"] = sexpr
    if hist is not None:
        params["hist"] = hist
    return params

def v_3d_tcorr_map_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTcorrMap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("mean", None) is not None:
        if not isinstance(params["mean"], str):
            raise StyxValidationError(f'`mean` has the wrong type: Received `{type(params["mean"])}` expected `str`')
    if params.get("zmean", None) is not None:
        if not isinstance(params["zmean"], str):
            raise StyxValidationError(f'`zmean` has the wrong type: Received `{type(params["zmean"])}` expected `str`')
    if params.get("qmean", None) is not None:
        if not isinstance(params["qmean"], str):
            raise StyxValidationError(f'`qmean` has the wrong type: Received `{type(params["qmean"])}` expected `str`')
    if params.get("pmean", None) is not None:
        if not isinstance(params["pmean"], str):
            raise StyxValidationError(f'`pmean` has the wrong type: Received `{type(params["pmean"])}` expected `str`')
    if params.get("thresh", None) is not None:
        if not isinstance(params["thresh"], str):
            raise StyxValidationError(f'`thresh` has the wrong type: Received `{type(params["thresh"])}` expected `str`')
    if params.get("varthresh", None) is not None:
        if not isinstance(params["varthresh"], str):
            raise StyxValidationError(f'`varthresh` has the wrong type: Received `{type(params["varthresh"])}` expected `str`')
    if params.get("norm_varthresh", None) is not None:
        if not isinstance(params["norm_varthresh"], str):
            raise StyxValidationError(f'`norm_varthresh` has the wrong type: Received `{type(params["norm_varthresh"])}` expected `str`')
    if params.get("corrmap", None) is not None:
        if not isinstance(params["corrmap"], str):
            raise StyxValidationError(f'`corrmap` has the wrong type: Received `{type(params["corrmap"])}` expected `str`')
    if params.get("corrmask", None) is not None:
        if not isinstance(params["corrmask"], bool):
            raise StyxValidationError(f'`corrmask` has the wrong type: Received `{type(params["corrmask"])}` expected `bool`')
    if params.get("aexpr", None) is not None:
        if not isinstance(params["aexpr"], str):
            raise StyxValidationError(f'`aexpr` has the wrong type: Received `{type(params["aexpr"])}` expected `str`')
    if params.get("cexpr", None) is not None:
        if not isinstance(params["cexpr"], str):
            raise StyxValidationError(f'`cexpr` has the wrong type: Received `{type(params["cexpr"])}` expected `str`')
    if params.get("sexpr", None) is not None:
        if not isinstance(params["sexpr"], str):
            raise StyxValidationError(f'`sexpr` has the wrong type: Received `{type(params["sexpr"])}` expected `str`')
    if params.get("hist", None) is not None:
        if not isinstance(params["hist"], str):
            raise StyxValidationError(f'`hist` has the wrong type: Received `{type(params["hist"])}` expected `str`')

def v_3d_tcorr_map_cargs(params: V3dTcorrMap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTcorrMap")
    cargs.append(execution.input_file(params["input"]))
    v_0 = params.get("seed")
    if v_0 is not None:
        cargs.append("-seed")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    if params.get("automask"):
        cargs.append("-automask")
    v_2 = params.get("mean")
    if v_2 is not None:
        cargs.append("-Mean")
        cargs.append(v_2)
    v_3 = params.get("zmean")
    if v_3 is not None:
        cargs.append("-Zmean")
        cargs.append(v_3)
    v_4 = params.get("qmean")
    if v_4 is not None:
        cargs.append("-Qmean")
        cargs.append(v_4)
    v_5 = params.get("pmean")
    if v_5 is not None:
        cargs.append("-Pmean")
        cargs.append(v_5)
    v_6 = params.get("thresh")
    if v_6 is not None:
        cargs.append("-Thresh")
        cargs.append(v_6)
    v_7 = params.get("varthresh")
    if v_7 is not None:
        cargs.append("-VarThresh")
        cargs.append(v_7)
    v_8 = params.get("norm_varthresh")
    if v_8 is not None:
        cargs.append("-VarThreshN")
        cargs.append(v_8)
    v_9 = params.get("corrmap")
    if v_9 is not None:
        cargs.append("-CorrMap")
        cargs.append(v_9)
    if params.get("corrmask"):
        cargs.append("-CorrMask")
    v_10 = params.get("aexpr")
    if v_10 is not None:
        cargs.append("-Aexpr")
        cargs.append(v_10)
    v_11 = params.get("cexpr")
    if v_11 is not None:
        cargs.append("-Cexpr")
        cargs.append(v_11)
    v_12 = params.get("sexpr")
    if v_12 is not None:
        cargs.append("-Sexpr")
        cargs.append(v_12)
    v_13 = params.get("hist")
    if v_13 is not None:
        cargs.append("-Hist")
        cargs.append(v_13)
    return cargs

def v_3d_tcorr_map_outputs(params: V3dTcorrMap, execution: Execution) -> V3dTcorrMapOutputs:
    """Build the V3dTcorrMapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dTcorrMapOutputs(
        root=root_v,
    )

def v_3d_tcorr_map_execute(params: V3dTcorrMap, runner: Runner | None = None) -> V3dTcorrMapOutputs:
    """
    3dTcorrMap

    AFNI program to compute correlation maps of input time series data

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_tcorr_map_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TCORR_MAP_METADATA)
    execution.params(params)
    args = v_3d_tcorr_map_cargs(params, execution)
    out = v_3d_tcorr_map_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_tcorr_map(
    input_: InputPathType,
    seed: InputPathType | None = None,
    mask: InputPathType | None = None,
    automask: bool = False,
    mean: str | None = None,
    zmean: str | None = None,
    qmean: str | None = None,
    pmean: str | None = None,
    thresh: str | None = None,
    varthresh: str | None = None,
    norm_varthresh: str | None = None,
    corrmap: str | None = None,
    corrmask: bool = False,
    aexpr: str | None = None,
    cexpr: str | None = None,
    sexpr: str | None = None,
    hist: str | None = None,
    runner: Runner | None = None,
) -> V3dTcorrMapOutputs:
    """
    3dTcorrMap

    AFNI program to compute correlation maps of input time series data

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Read 3D+time dataset 'dd'. This provides the time series to be\
            correlated en masse. This is a mandatory option.
        seed: Read 3D+time dataset 'bb'. It correlates the -seed voxel time series\
            with every voxel in the -input dataset.
        mask: Read dataset 'mmm' as a voxel mask.
        automask: Create a mask from the input dataset.
        mean: Save average correlations into dataset prefix 'pp'.
        zmean: Save tanh of mean arctanh(correlation) into 'pp'.
        qmean: Save RMS(correlation) into 'pp'.
        pmean: Save average of squared positive correlations into 'pp'.
        thresh: Save the COUNT of how many voxels survived thresholding at level\
            abs(correlation) >= tt.
        varthresh: Save the COUNT of how many voxels survive thresholding at\
            multiple levels abs(correlation) >= tt.
        norm_varthresh: Like '-VarThresh', but the output counts are 'Normalized'.
        corrmap: Output at each voxel the entire correlation map, into a dataset\
            with prefix 'pp'.
        corrmask: By default, -CorrMap outputs a sub-brick for EACH input dataset\
            voxel. Eliminates these sub-bricks using this option.
        aexpr: For each correlation 'r', compute the calc-style expression 'expr',\
            and average these values to get the output that goes into dataset\
            'ppp'.
        cexpr: As in '-Aexpr', but only average together nonzero values computed by\
            'expr'.
        sexpr: As above, but the sum of the expressions is computed rather than the\
            average.
        hist: For each voxel, save a histogram of the correlation coefficients into\
            dataset ppp.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_tcorr_map_params(
        input_=input_,
        seed=seed,
        mask=mask,
        automask=automask,
        mean=mean,
        zmean=zmean,
        qmean=qmean,
        pmean=pmean,
        thresh=thresh,
        varthresh=varthresh,
        norm_varthresh=norm_varthresh,
        corrmap=corrmap,
        corrmask=corrmask,
        aexpr=aexpr,
        cexpr=cexpr,
        sexpr=sexpr,
        hist=hist,
    )
    return v_3d_tcorr_map_execute(params, runner)

__all__ = [
    "V3dTcorrMap",
    "V3dTcorrMapOutputs",
    "V_3D_TCORR_MAP_METADATA",
    "v_3d_tcorr_map_cargs",
    "v_3d_tcorr_map_outputs",
    "v_3d_tcorr_map_params",
    "v_3d_tcorr_map_execute",
    "v_3d_tcorr_map_validate",
    "v_3d_tcorr_map",
]