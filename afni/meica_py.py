# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MEICA_PY_METADATA = Metadata(
    id="meica.py",
    name="meica.py",
    package="afni",
)

MeicaPy = typing.TypedDict(
    "MeicaPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/meica.py"]],
        "infile": InputPathType,
        "echo_times": str,
        "affine": str,
        "output_directory": str,
        "components": typing.NotRequired[float],
        "talairach": typing.NotRequired[bool],
        "threshold": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MeicaPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    cleaned_bold: OutputPathType
    """Cleaned BOLD image after ME-ICA processing"""
    components_output: OutputPathType
    """Independent components result of ICA"""

def meica_py_params(
    infile: InputPathType,
    echo_times: str,
    affine: str,
    output_directory: str,
    components: float | None = None,
    talairach: bool = False,
    threshold: float | None = None,
    debug: bool = False,
) -> MeicaPy:
    """
    Build parameters.

    Args:
        infile: Input image dataset (e.g. dataset.nii.gz)
        echo_times: Echo times (e.g. 15.0,30.0,45.0)
        affine: Affine registration matrix
        output_directory: Output directory
        components: Number of components for ICA
        talairach: Apply standard Talairach transformation
        threshold: Threshold value for masking
        debug: Enable debug mode

    Returns:
        Parameter dictionary.
    """
    params: MeicaPy = {
        "@type": "afni/meica.py",
        "infile": infile,
        "echo_times": echo_times,
        "affine": affine,
        "output_directory": output_directory,
        "talairach": talairach,
        "debug": debug,
    }
    if components is not None:
        params["components"] = components
    if threshold is not None:
        params["threshold"] = threshold
    return params

def meica_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MeicaPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("echo_times", None) is None:
        raise StyxValidationError("`echo_times` must not be None")
    if not isinstance(params["echo_times"], str):
        raise StyxValidationError(f'`echo_times` has the wrong type: Received `{type(params["echo_times"])}` expected `str`')
    if params.get("affine", None) is None:
        raise StyxValidationError("`affine` must not be None")
    if not isinstance(params["affine"], str):
        raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `str`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("components", None) is not None:
        if not isinstance(params["components"], (float, int)):
            raise StyxValidationError(f'`components` has the wrong type: Received `{type(params["components"])}` expected `float`')
    if params.get("talairach", None) is not None:
        if not isinstance(params["talairach"], bool):
            raise StyxValidationError(f'`talairach` has the wrong type: Received `{type(params["talairach"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def meica_py_cargs(params: MeicaPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("meica.py")
    cargs.append("-d")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-e")
    cargs.append(params["echo_times"])
    cargs.append("-a")
    cargs.append(params["affine"])
    cargs.append("-o")
    cargs.append(params["output_directory"])
    v_0 = params.get("components")
    if v_0 is not None:
        cargs.append("-c")
        cargs.append(str(v_0))
    if params.get("talairach"):
        cargs.append("-t")
    v_1 = params.get("threshold")
    if v_1 is not None:
        cargs.append("--thresh")
        cargs.append(str(v_1))
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def meica_py_outputs(params: MeicaPy, execution: Execution) -> MeicaPyOutputs:
    """Build the MeicaPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    cleaned_bold_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/cleaned_bold.nii.gz')
    components_output_v: OutputPathType = execution.output_file(f'{params["output_directory"]}/components.nii.gz')
    return MeicaPyOutputs(
        root=root_v,
        cleaned_bold=cleaned_bold_v,
        components_output=components_output_v,
    )

def meica_py_execute(params: MeicaPy, runner: Runner | None = None) -> MeicaPyOutputs:
    """
    meica.py

    Multi-Echo Independent Component Analysis for fMRI denoising

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    meica_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MEICA_PY_METADATA)
    execution.params(params)
    args = meica_py_cargs(params, execution)
    out = meica_py_outputs(params, execution)
    execution.run(args)
    return out

def meica_py(
    infile: InputPathType,
    echo_times: str,
    affine: str,
    output_directory: str,
    components: float | None = None,
    talairach: bool = False,
    threshold: float | None = None,
    debug: bool = False,
    runner: Runner | None = None,
) -> MeicaPyOutputs:
    """
    meica.py

    Multi-Echo Independent Component Analysis for fMRI denoising

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input image dataset (e.g. dataset.nii.gz)
        echo_times: Echo times (e.g. 15.0,30.0,45.0)
        affine: Affine registration matrix
        output_directory: Output directory
        components: Number of components for ICA
        talairach: Apply standard Talairach transformation
        threshold: Threshold value for masking
        debug: Enable debug mode
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = meica_py_params(
        infile=infile,
        echo_times=echo_times,
        affine=affine,
        output_directory=output_directory,
        components=components,
        talairach=talairach,
        threshold=threshold,
        debug=debug,
    )
    return meica_py_execute(params, runner)

__all__ = [
    "MeicaPy",
    "MeicaPyOutputs",
    "MEICA_PY_METADATA",
    "meica_py_cargs",
    "meica_py_outputs",
    "meica_py_params",
    "meica_py_execute",
    "meica_py_validate",
    "meica_py",
]