# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1DDOT_METADATA = Metadata(
    id="1ddot",
    name="1ddot",
    package="afni",
)

V1ddot = typing.TypedDict(
    "V1ddot",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1ddot"]],
        "one_flag": typing.NotRequired[bool],
        "dem_flag": typing.NotRequired[bool],
        "cov_flag": typing.NotRequired[bool],
        "inn_flag": typing.NotRequired[bool],
        "rank_flag": typing.NotRequired[bool],
        "terse_flag": typing.NotRequired[bool],
        "okzero_flag": typing.NotRequired[bool],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class V1ddotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout_output: OutputPathType
    """Output correlation or covariance matrix printed to stdout."""
    stdout: list[str]
    """output text file"""

def v_1ddot_params(
    input_files: list[InputPathType],
    one_flag: bool = False,
    dem_flag: bool = False,
    cov_flag: bool = False,
    inn_flag: bool = False,
    rank_flag: bool = False,
    terse_flag: bool = False,
    okzero_flag: bool = False,
) -> V1ddot:
    """
    Build parameters.

    Args:
        input_files: Input 1D files.
        one_flag: Make 1st vector be all 1's.
        dem_flag: Remove mean from all vectors (conflicts with '-one').
        cov_flag: Compute with covariance matrix instead of correlation.
        inn_flag: Compute with inner product matrix instead.
        rank_flag: Compute Spearman rank correlation instead (also implies\
            '-terse').
        terse_flag: Output only the correlation or covariance matrix without any\
            garnish.
        okzero_flag: Do not quit if a vector is all zeros. The correlation matrix\
            will have 0 where NaNs ought to go.

    Returns:
        Parameter dictionary.
    """
    params: V1ddot = {
        "@type": "afni/1ddot",
        "input_files": input_files,
        "one_flag": one_flag,
        "dem_flag": dem_flag,
        "cov_flag": cov_flag,
        "inn_flag": inn_flag,
        "rank_flag": rank_flag,
        "terse_flag": terse_flag,
        "okzero_flag": okzero_flag,
    }
    return params

def v_1ddot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1ddot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("one_flag", None) is not None:
        if not isinstance(params["one_flag"], bool):
            raise StyxValidationError(f'`one_flag` has the wrong type: Received `{type(params["one_flag"])}` expected `bool`')
    if params.get("dem_flag", None) is not None:
        if not isinstance(params["dem_flag"], bool):
            raise StyxValidationError(f'`dem_flag` has the wrong type: Received `{type(params["dem_flag"])}` expected `bool`')
    if params.get("cov_flag", None) is not None:
        if not isinstance(params["cov_flag"], bool):
            raise StyxValidationError(f'`cov_flag` has the wrong type: Received `{type(params["cov_flag"])}` expected `bool`')
    if params.get("inn_flag", None) is not None:
        if not isinstance(params["inn_flag"], bool):
            raise StyxValidationError(f'`inn_flag` has the wrong type: Received `{type(params["inn_flag"])}` expected `bool`')
    if params.get("rank_flag", None) is not None:
        if not isinstance(params["rank_flag"], bool):
            raise StyxValidationError(f'`rank_flag` has the wrong type: Received `{type(params["rank_flag"])}` expected `bool`')
    if params.get("terse_flag", None) is not None:
        if not isinstance(params["terse_flag"], bool):
            raise StyxValidationError(f'`terse_flag` has the wrong type: Received `{type(params["terse_flag"])}` expected `bool`')
    if params.get("okzero_flag", None) is not None:
        if not isinstance(params["okzero_flag"], bool):
            raise StyxValidationError(f'`okzero_flag` has the wrong type: Received `{type(params["okzero_flag"])}` expected `bool`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def v_1ddot_cargs(params: V1ddot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1ddot")
    if params.get("one_flag"):
        cargs.append("-one")
    if params.get("dem_flag"):
        cargs.append("-dem")
    if params.get("cov_flag"):
        cargs.append("-cov")
    if params.get("inn_flag"):
        cargs.append("-inn")
    if params.get("rank_flag"):
        cargs.append("-rank")
    if params.get("terse_flag"):
        cargs.append("-terse")
    if params.get("okzero_flag"):
        cargs.append("-okzero")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def v_1ddot_outputs(params: V1ddot, execution: Execution) -> V1ddotOutputs:
    """Build the V1ddotOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stdout_output_v: OutputPathType = execution.output_file("stdout.txt")
    return V1ddotOutputs(
        root=root_v,
        stdout_output=stdout_output_v,
        stdout=[],
    )

def v_1ddot_execute(params: V1ddot, runner: Runner | None = None) -> V1ddotOutputs:
    """
    1ddot

    Computes the correlation matrix of the input 1D files and their inverse correlation matrix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1ddot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1DDOT_METADATA)
    execution.params(params)
    args = v_1ddot_cargs(params, execution)
    out = v_1ddot_outputs(params, execution)
    execution.run(args, handle_stdout=lambda s: out.stdout.append(s))
    return out

def v_1ddot(
    input_files: list[InputPathType],
    one_flag: bool = False,
    dem_flag: bool = False,
    cov_flag: bool = False,
    inn_flag: bool = False,
    rank_flag: bool = False,
    terse_flag: bool = False,
    okzero_flag: bool = False,
    runner: Runner | None = None,
) -> V1ddotOutputs:
    """
    1ddot

    Computes the correlation matrix of the input 1D files and their inverse correlation matrix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input 1D files.
        one_flag: Make 1st vector be all 1's.
        dem_flag: Remove mean from all vectors (conflicts with '-one').
        cov_flag: Compute with covariance matrix instead of correlation.
        inn_flag: Compute with inner product matrix instead.
        rank_flag: Compute Spearman rank correlation instead (also implies\
            '-terse').
        terse_flag: Output only the correlation or covariance matrix without any\
            garnish.
        okzero_flag: Do not quit if a vector is all zeros. The correlation matrix\
            will have 0 where NaNs ought to go.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1ddot_params(
        input_files=input_files,
        one_flag=one_flag,
        dem_flag=dem_flag,
        cov_flag=cov_flag,
        inn_flag=inn_flag,
        rank_flag=rank_flag,
        terse_flag=terse_flag,
        okzero_flag=okzero_flag,
    )
    return v_1ddot_execute(params, runner)

__all__ = [
    "V1ddot",
    "V1ddotOutputs",
    "V_1DDOT_METADATA",
    "v_1ddot_cargs",
    "v_1ddot_outputs",
    "v_1ddot_params",
    "v_1ddot_execute",
    "v_1ddot_validate",
    "v_1ddot",
]