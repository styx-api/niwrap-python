# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_TRACT_COLORIZE_METADATA = Metadata(
    id="@fat_tract_colorize",
    name="@fat_tract_colorize",
    package="afni",
)

FatTractColorize = typing.TypedDict(
    "FatTractColorize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@fat_tract_colorize"]],
        "in_fa": InputPathType,
        "in_v1": InputPathType,
        "in_tracts": str,
        "prefix": str,
        "in_ulay": typing.NotRequired[InputPathType],
        "no_view": typing.NotRequired[bool],
        "only_view": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatTractColorizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_hue_volume: OutputPathType
    """HSL coloration volume file with four bricks from the V1 and FA volumes: Hue, Saturation, Luminosity, and Brightness"""
    output_iso_surface: OutputPathType
    """Slightly smoothed isosurface file made by IsoSurface"""
    output_iso_spec: OutputPathType
    """Spec file made by quickspec"""
    output_proj_surface: OutputPathType
    """Projection of appropriate coloration onto the surface"""

def fat_tract_colorize_params(
    in_fa: InputPathType,
    in_v1: InputPathType,
    in_tracts: str,
    prefix: str,
    in_ulay: InputPathType | None = None,
    no_view: bool = False,
    only_view: bool = False,
) -> FatTractColorize:
    """
    Build parameters.

    Args:
        in_fa: FA values of the DT fitting, used to modulate the brightness of the\
            RGB coloration.
        in_v1: First eigenvector of the DT fitting. A unit vector volume with 3\
            components (0-1 range).
        in_tracts: The INDIMAP or PAIRMAP file output by 3dTrackID, specifying the\
            subbrick if >1 (e.g., NAME_INDIMAP+orig'[0]').
        prefix: Prefix for all output files.
        in_ulay: Optional underlay dataset for AFNI/SUMA viewing. Default is to use\
            the FA dataset.
        no_view: Turn off auto-running of AFNI_SUMA commands to view the output\
            immediately.
        only_view: Only view the data with AFNI+SUMA, assuming the command has been\
            run before.

    Returns:
        Parameter dictionary.
    """
    params: FatTractColorize = {
        "@type": "afni/@fat_tract_colorize",
        "in_fa": in_fa,
        "in_v1": in_v1,
        "in_tracts": in_tracts,
        "prefix": prefix,
        "no_view": no_view,
        "only_view": only_view,
    }
    if in_ulay is not None:
        params["in_ulay"] = in_ulay
    return params

def fat_tract_colorize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatTractColorize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_fa", None) is None:
        raise StyxValidationError("`in_fa` must not be None")
    if not isinstance(params["in_fa"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_fa` has the wrong type: Received `{type(params["in_fa"])}` expected `InputPathType`')
    if params.get("in_v1", None) is None:
        raise StyxValidationError("`in_v1` must not be None")
    if not isinstance(params["in_v1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_v1` has the wrong type: Received `{type(params["in_v1"])}` expected `InputPathType`')
    if params.get("in_tracts", None) is None:
        raise StyxValidationError("`in_tracts` must not be None")
    if not isinstance(params["in_tracts"], str):
        raise StyxValidationError(f'`in_tracts` has the wrong type: Received `{type(params["in_tracts"])}` expected `str`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("in_ulay", None) is not None:
        if not isinstance(params["in_ulay"], (pathlib.Path, str)):
            raise StyxValidationError(f'`in_ulay` has the wrong type: Received `{type(params["in_ulay"])}` expected `InputPathType`')
    if params.get("no_view", None) is not None:
        if not isinstance(params["no_view"], bool):
            raise StyxValidationError(f'`no_view` has the wrong type: Received `{type(params["no_view"])}` expected `bool`')
    if params.get("only_view", None) is not None:
        if not isinstance(params["only_view"], bool):
            raise StyxValidationError(f'`only_view` has the wrong type: Received `{type(params["only_view"])}` expected `bool`')

def fat_tract_colorize_cargs(params: FatTractColorize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@fat_tract_colorize")
    cargs.append("-in_fa")
    cargs.append(execution.input_file(params["in_fa"]))
    cargs.append("-in_v1")
    cargs.append(execution.input_file(params["in_v1"]))
    cargs.append("-in_tracts")
    cargs.append(params["in_tracts"])
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("in_ulay")
    if v_0 is not None:
        cargs.append("-in_ulay")
        cargs.append(execution.input_file(v_0))
    if params.get("no_view"):
        cargs.append("-no_view")
    if params.get("only_view"):
        cargs.append("-only_view")
    return cargs

def fat_tract_colorize_outputs(params: FatTractColorize, execution: Execution) -> FatTractColorizeOutputs:
    """Build the FatTractColorizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_hue_volume_v: OutputPathType = execution.output_file(f'{params["prefix"]}_RGB_HUE.nii.gz')
    output_iso_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}_RGB_iso.ply')
    output_iso_spec_v: OutputPathType = execution.output_file(f'{params["prefix"]}_RGB_iso.spec')
    output_proj_surface_v: OutputPathType = execution.output_file(f'{params["prefix"]}_RGB.niml.dset')
    return FatTractColorizeOutputs(
        root=root_v,
        output_hue_volume=output_hue_volume_v,
        output_iso_surface=output_iso_surface_v,
        output_iso_spec=output_iso_spec_v,
        output_proj_surface=output_proj_surface_v,
    )

def fat_tract_colorize_execute(params: FatTractColorize, runner: Runner | None = None) -> FatTractColorizeOutputs:
    """
    @fat_tract_colorize

    Visualize tractographic output from 3dTrackID, particularly in probabilistic mode.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_tract_colorize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_TRACT_COLORIZE_METADATA)
    execution.params(params)
    args = fat_tract_colorize_cargs(params, execution)
    out = fat_tract_colorize_outputs(params, execution)
    execution.run(args)
    return out

def fat_tract_colorize(
    in_fa: InputPathType,
    in_v1: InputPathType,
    in_tracts: str,
    prefix: str,
    in_ulay: InputPathType | None = None,
    no_view: bool = False,
    only_view: bool = False,
    runner: Runner | None = None,
) -> FatTractColorizeOutputs:
    """
    @fat_tract_colorize

    Visualize tractographic output from 3dTrackID, particularly in probabilistic mode.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_fa: FA values of the DT fitting, used to modulate the brightness of the\
            RGB coloration.
        in_v1: First eigenvector of the DT fitting. A unit vector volume with 3\
            components (0-1 range).
        in_tracts: The INDIMAP or PAIRMAP file output by 3dTrackID, specifying the\
            subbrick if >1 (e.g., NAME_INDIMAP+orig'[0]').
        prefix: Prefix for all output files.
        in_ulay: Optional underlay dataset for AFNI/SUMA viewing. Default is to use\
            the FA dataset.
        no_view: Turn off auto-running of AFNI_SUMA commands to view the output\
            immediately.
        only_view: Only view the data with AFNI+SUMA, assuming the command has been\
            run before.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_tract_colorize_params(
        in_fa=in_fa,
        in_v1=in_v1,
        in_tracts=in_tracts,
        prefix=prefix,
        in_ulay=in_ulay,
        no_view=no_view,
        only_view=only_view,
    )
    return fat_tract_colorize_execute(params, runner)

__all__ = [
    "FatTractColorize",
    "FatTractColorizeOutputs",
    "FAT_TRACT_COLORIZE_METADATA",
    "fat_tract_colorize_cargs",
    "fat_tract_colorize_outputs",
    "fat_tract_colorize_params",
    "fat_tract_colorize_execute",
    "fat_tract_colorize_validate",
    "fat_tract_colorize",
]