# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

F_SLABEL2DSET_METADATA = Metadata(
    id="@FSlabel2dset",
    name="@FSlabel2dset",
    package="afni",
)

FSlabel2dset = typing.TypedDict(
    "FSlabel2dset",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@FSlabel2dset"]],
        "fs_label_file": InputPathType,
        "val": typing.NotRequired[float],
        "help": typing.NotRequired[bool],
        "echo": typing.NotRequired[bool],
        "keep_tmp": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FSlabel2dsetOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def f_slabel2dset_params(
    fs_label_file: InputPathType,
    val: float | None = None,
    help: bool = False,
    echo: bool = False,
    keep_tmp: bool = False,
) -> FSlabel2dset:
    """
    Build parameters.

    Args:
        fs_label_file: Specify the ASCII label file from FreeSurfer
        val: Assign integer VAL to the nodes in FS_LABEL_FILE (Default is 1)
        help: Display help message
        echo: Turn echo for debugging
        keep_tmp: Don't cleanup temp files

    Returns:
        Parameter dictionary.
    """
    params: FSlabel2dset = {
        "@type": "afni/@FSlabel2dset",
        "fs_label_file": fs_label_file,
        "help": help,
        "echo": echo,
        "keep_tmp": keep_tmp,
    }
    if val is not None:
        params["val"] = val
    return params

def f_slabel2dset_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FSlabel2dset."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fs_label_file", None) is None:
        raise StyxValidationError("`fs_label_file` must not be None")
    if not isinstance(params["fs_label_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fs_label_file` has the wrong type: Received `{type(params["fs_label_file"])}` expected `InputPathType`')
    if params.get("val", None) is not None:
        if not isinstance(params["val"], (float, int)):
            raise StyxValidationError(f'`val` has the wrong type: Received `{type(params["val"])}` expected `float`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("keep_tmp", None) is not None:
        if not isinstance(params["keep_tmp"], bool):
            raise StyxValidationError(f'`keep_tmp` has the wrong type: Received `{type(params["keep_tmp"])}` expected `bool`')

def f_slabel2dset_cargs(params: FSlabel2dset, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@FSlabel2dset")
    cargs.append("-fs")
    cargs.append(execution.input_file(params["fs_label_file"]))
    v_0 = params.get("val")
    if v_0 is not None:
        cargs.append("-val")
        cargs.append(str(v_0))
    if params.get("help"):
        cargs.append("-help")
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("keep_tmp"):
        cargs.append("-keep_tmp")
    return cargs

def f_slabel2dset_outputs(params: FSlabel2dset, execution: Execution) -> FSlabel2dsetOutputs:
    """Build the FSlabel2dsetOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FSlabel2dsetOutputs(
        root=root_v,
    )

def f_slabel2dset_execute(params: FSlabel2dset, runner: Runner | None = None) -> FSlabel2dsetOutputs:
    """
    @FSlabel2dset

    A script to convert a FreeSurfer ASCII label file into a SUMA dataset and a SUMA ROI

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    f_slabel2dset_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(F_SLABEL2DSET_METADATA)
    execution.params(params)
    args = f_slabel2dset_cargs(params, execution)
    out = f_slabel2dset_outputs(params, execution)
    execution.run(args)
    return out

def f_slabel2dset(
    fs_label_file: InputPathType,
    val: float | None = None,
    help: bool = False,
    echo: bool = False,
    keep_tmp: bool = False,
    runner: Runner | None = None,
) -> FSlabel2dsetOutputs:
    """
    @FSlabel2dset

    A script to convert a FreeSurfer ASCII label file into a SUMA dataset and a SUMA ROI

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        fs_label_file: Specify the ASCII label file from FreeSurfer
        val: Assign integer VAL to the nodes in FS_LABEL_FILE (Default is 1)
        help: Display help message
        echo: Turn echo for debugging
        keep_tmp: Don't cleanup temp files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = f_slabel2dset_params(
        fs_label_file=fs_label_file,
        val=val,
        help=help,
        echo=echo,
        keep_tmp=keep_tmp,
    )
    return f_slabel2dset_execute(params, runner)

__all__ = [
    "FSlabel2dset",
    "FSlabel2dsetOutputs",
    "F_SLABEL2DSET_METADATA",
    "f_slabel2dset_cargs",
    "f_slabel2dset_outputs",
    "f_slabel2dset_params",
    "f_slabel2dset_execute",
    "f_slabel2dset_validate",
    "f_slabel2dset",
]