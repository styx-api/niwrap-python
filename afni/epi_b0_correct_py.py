# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EPI_B0_CORRECT_PY_METADATA = Metadata(
    id="epi_b0_correct.py",
    name="epi_b0_correct.py",
    package="afni",
)

EpiB0CorrectPy = typing.TypedDict(
    "EpiB0CorrectPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/epi_b0_correct.py"]],
        "prefix": str,
        "input_freq": InputPathType,
        "input_epi": InputPathType,
        "input_mask": typing.NotRequired[InputPathType],
        "input_magn": typing.NotRequired[InputPathType],
        "input_anat": typing.NotRequired[InputPathType],
        "input_json": typing.NotRequired[InputPathType],
        "epi_pe_dir": str,
        "epi_pe_bwpp": typing.NotRequired[float],
        "epi_pe_echo_sp": typing.NotRequired[float],
        "epi_pe_vox_dim": typing.NotRequired[float],
        "scale_freq": typing.NotRequired[float],
        "out_cmds": typing.NotRequired[str],
        "out_pars": typing.NotRequired[str],
        "wdir_name": typing.NotRequired[str],
        "blur_sigma": typing.NotRequired[float],
        "do_recenter_freq": typing.NotRequired[str],
        "mask_dilate": typing.NotRequired[list[float]],
        "no_clean": typing.NotRequired[bool],
        "qc_box_focus_ulay": typing.NotRequired[bool],
        "no_qc_image": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "ver": typing.NotRequired[bool],
        "date": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class EpiB0CorrectPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    warp_dset: OutputPathType
    """Warp dataset containing the warp along the phase encode axis."""
    cmds_script: OutputPathType
    """Script of the commands used to generate the warp and process the EPI."""
    params_txt: OutputPathType
    """Text file of parameters input or derived from datasets."""
    unwarped_epi: OutputPathType
    """EPI dataset with estimated distortion correction applied."""
    qc_image_dir: OutputPathType
    """Directory containing QC images."""

def epi_b0_correct_py_params(
    prefix: str,
    input_freq: InputPathType,
    input_epi: InputPathType,
    epi_pe_dir: str,
    input_mask: InputPathType | None = None,
    input_magn: InputPathType | None = None,
    input_anat: InputPathType | None = None,
    input_json: InputPathType | None = None,
    epi_pe_bwpp: float | None = None,
    epi_pe_echo_sp: float | None = None,
    epi_pe_vox_dim: float | None = None,
    scale_freq: float | None = None,
    out_cmds: str | None = None,
    out_pars: str | None = None,
    wdir_name: str | None = None,
    blur_sigma: float | None = None,
    do_recenter_freq: str | None = None,
    mask_dilate: list[float] | None = None,
    no_clean: bool = False,
    qc_box_focus_ulay: bool = False,
    no_qc_image: bool = False,
    help: bool = False,
    ver: bool = False,
    date: bool = False,
) -> EpiB0CorrectPy:
    """
    Build parameters.

    Args:
        prefix: Prefix of output files; can include path.
        input_freq: Phase dataset (frequency volume); should be at similar\
            resolution and FOV as the EPI dataset; must be scaled appropriately.
        input_epi: EPI dataset to which B0 distortion correction will be applied.
        epi_pe_dir: Direction (axis) of phase encoding, e.g., AP, PA, RL, ...
        input_mask: Mask of brain volume.
        input_magn: Magnitude dataset from which to estimate brain mask; can be\
            used for QC imaging.
        input_anat: Anatomical dataset to be used as underlay for QC images\
            (optional).
        input_json: JSON file containing parameters about the EPI dataset.
        epi_pe_bwpp: Bandwidth per pixel (in Hz) in the EPI dataset along the phase\
            encode direction.
        epi_pe_echo_sp: Effective TE spacing of the phase encoded volume, in\
            seconds.
        epi_pe_vox_dim: Voxel size along the EPI dataset's phase encode axis, in\
            mm.
        scale_freq: Scale to apply to frequency volume to match units (def:\
            SF=1.0).
        out_cmds: Name for output script recording commands (def:\
            PREFIX_cmds.tcsh).
        out_pars: Name for output text file recording relevant parameters (def:\
            PREFIX_pars.txt).
        wdir_name: Working directory name (def: automatic name).
        blur_sigma: Amount of blurring to apply to masked phase encode dataset\
            (def: BS=9).
        do_recenter_freq: Method to recenter the phase volume within the brain mask\
            (def: MC=mode).
        mask_dilate: Erosion and dilation parameters for automask (when using\
            magnitude image).
        no_clean: Don't remove the temporary directory of intermediate files.
        qc_box_focus_ulay: Focus the QC images on an automask region of the\
            underlay dataset.
        no_qc_image: Don't generate QC images.
        help: Display program help in terminal.
        ver: Display program version number in terminal.
        date: Display date of program's last update in terminal.

    Returns:
        Parameter dictionary.
    """
    params: EpiB0CorrectPy = {
        "@type": "afni/epi_b0_correct.py",
        "prefix": prefix,
        "input_freq": input_freq,
        "input_epi": input_epi,
        "epi_pe_dir": epi_pe_dir,
        "no_clean": no_clean,
        "qc_box_focus_ulay": qc_box_focus_ulay,
        "no_qc_image": no_qc_image,
        "help": help,
        "ver": ver,
        "date": date,
    }
    if input_mask is not None:
        params["input_mask"] = input_mask
    if input_magn is not None:
        params["input_magn"] = input_magn
    if input_anat is not None:
        params["input_anat"] = input_anat
    if input_json is not None:
        params["input_json"] = input_json
    if epi_pe_bwpp is not None:
        params["epi_pe_bwpp"] = epi_pe_bwpp
    if epi_pe_echo_sp is not None:
        params["epi_pe_echo_sp"] = epi_pe_echo_sp
    if epi_pe_vox_dim is not None:
        params["epi_pe_vox_dim"] = epi_pe_vox_dim
    if scale_freq is not None:
        params["scale_freq"] = scale_freq
    if out_cmds is not None:
        params["out_cmds"] = out_cmds
    if out_pars is not None:
        params["out_pars"] = out_pars
    if wdir_name is not None:
        params["wdir_name"] = wdir_name
    if blur_sigma is not None:
        params["blur_sigma"] = blur_sigma
    if do_recenter_freq is not None:
        params["do_recenter_freq"] = do_recenter_freq
    if mask_dilate is not None:
        params["mask_dilate"] = mask_dilate
    return params

def epi_b0_correct_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid EpiB0CorrectPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("input_freq", None) is None:
        raise StyxValidationError("`input_freq` must not be None")
    if not isinstance(params["input_freq"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_freq` has the wrong type: Received `{type(params["input_freq"])}` expected `InputPathType`')
    if params.get("input_epi", None) is None:
        raise StyxValidationError("`input_epi` must not be None")
    if not isinstance(params["input_epi"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_epi` has the wrong type: Received `{type(params["input_epi"])}` expected `InputPathType`')
    if params.get("input_mask", None) is not None:
        if not isinstance(params["input_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_mask` has the wrong type: Received `{type(params["input_mask"])}` expected `InputPathType`')
    if params.get("input_magn", None) is not None:
        if not isinstance(params["input_magn"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_magn` has the wrong type: Received `{type(params["input_magn"])}` expected `InputPathType`')
    if params.get("input_anat", None) is not None:
        if not isinstance(params["input_anat"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_anat` has the wrong type: Received `{type(params["input_anat"])}` expected `InputPathType`')
    if params.get("input_json", None) is not None:
        if not isinstance(params["input_json"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_json` has the wrong type: Received `{type(params["input_json"])}` expected `InputPathType`')
    if params.get("epi_pe_dir", None) is None:
        raise StyxValidationError("`epi_pe_dir` must not be None")
    if not isinstance(params["epi_pe_dir"], str):
        raise StyxValidationError(f'`epi_pe_dir` has the wrong type: Received `{type(params["epi_pe_dir"])}` expected `str`')
    if params.get("epi_pe_bwpp", None) is not None:
        if not isinstance(params["epi_pe_bwpp"], (float, int)):
            raise StyxValidationError(f'`epi_pe_bwpp` has the wrong type: Received `{type(params["epi_pe_bwpp"])}` expected `float`')
    if params.get("epi_pe_echo_sp", None) is not None:
        if not isinstance(params["epi_pe_echo_sp"], (float, int)):
            raise StyxValidationError(f'`epi_pe_echo_sp` has the wrong type: Received `{type(params["epi_pe_echo_sp"])}` expected `float`')
    if params.get("epi_pe_vox_dim", None) is not None:
        if not isinstance(params["epi_pe_vox_dim"], (float, int)):
            raise StyxValidationError(f'`epi_pe_vox_dim` has the wrong type: Received `{type(params["epi_pe_vox_dim"])}` expected `float`')
    if params.get("scale_freq", None) is not None:
        if not isinstance(params["scale_freq"], (float, int)):
            raise StyxValidationError(f'`scale_freq` has the wrong type: Received `{type(params["scale_freq"])}` expected `float`')
    if params.get("out_cmds", None) is not None:
        if not isinstance(params["out_cmds"], str):
            raise StyxValidationError(f'`out_cmds` has the wrong type: Received `{type(params["out_cmds"])}` expected `str`')
    if params.get("out_pars", None) is not None:
        if not isinstance(params["out_pars"], str):
            raise StyxValidationError(f'`out_pars` has the wrong type: Received `{type(params["out_pars"])}` expected `str`')
    if params.get("wdir_name", None) is not None:
        if not isinstance(params["wdir_name"], str):
            raise StyxValidationError(f'`wdir_name` has the wrong type: Received `{type(params["wdir_name"])}` expected `str`')
    if params.get("blur_sigma", None) is not None:
        if not isinstance(params["blur_sigma"], (float, int)):
            raise StyxValidationError(f'`blur_sigma` has the wrong type: Received `{type(params["blur_sigma"])}` expected `float`')
    if params.get("do_recenter_freq", None) is not None:
        if not isinstance(params["do_recenter_freq"], str):
            raise StyxValidationError(f'`do_recenter_freq` has the wrong type: Received `{type(params["do_recenter_freq"])}` expected `str`')
    if params.get("mask_dilate", None) is not None:
        if not isinstance(params["mask_dilate"], list):
            raise StyxValidationError(f'`mask_dilate` has the wrong type: Received `{type(params["mask_dilate"])}` expected `list[float]`')
        for e in params["mask_dilate"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`mask_dilate` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')
    if params.get("qc_box_focus_ulay", None) is not None:
        if not isinstance(params["qc_box_focus_ulay"], bool):
            raise StyxValidationError(f'`qc_box_focus_ulay` has the wrong type: Received `{type(params["qc_box_focus_ulay"])}` expected `bool`')
    if params.get("no_qc_image", None) is not None:
        if not isinstance(params["no_qc_image"], bool):
            raise StyxValidationError(f'`no_qc_image` has the wrong type: Received `{type(params["no_qc_image"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("ver", None) is not None:
        if not isinstance(params["ver"], bool):
            raise StyxValidationError(f'`ver` has the wrong type: Received `{type(params["ver"])}` expected `bool`')
    if params.get("date", None) is not None:
        if not isinstance(params["date"], bool):
            raise StyxValidationError(f'`date` has the wrong type: Received `{type(params["date"])}` expected `bool`')

def epi_b0_correct_py_cargs(params: EpiB0CorrectPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("epi_b0_correct.py")
    cargs.append(params["prefix"])
    cargs.append("-in_freq")
    cargs.append(execution.input_file(params["input_freq"]))
    cargs.append("-in_epi")
    cargs.append(execution.input_file(params["input_epi"]))
    v_0 = params.get("input_mask")
    if v_0 is not None:
        cargs.append("-in_mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("input_magn")
    if v_1 is not None:
        cargs.append("-in_magn")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("input_anat")
    if v_2 is not None:
        cargs.append("-in_anat")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("input_json")
    if v_3 is not None:
        cargs.append("-in_epi_json")
        cargs.append(execution.input_file(v_3))
    cargs.append("-epi_pe_dir")
    cargs.append(params["epi_pe_dir"])
    v_4 = params.get("epi_pe_bwpp")
    if v_4 is not None:
        cargs.append("-epi_pe_bwpp")
        cargs.append(str(v_4))
    v_5 = params.get("epi_pe_echo_sp")
    if v_5 is not None:
        cargs.append("-epi_pe_echo_sp")
        cargs.append(str(v_5))
    v_6 = params.get("epi_pe_vox_dim")
    if v_6 is not None:
        cargs.append("-epi_pe_voxdim")
        cargs.append(str(v_6))
    v_7 = params.get("scale_freq")
    if v_7 is not None:
        cargs.append("-scale_freq")
        cargs.append(str(v_7))
    v_8 = params.get("out_cmds")
    if v_8 is not None:
        cargs.append("-out_cmds")
        cargs.append(v_8)
    v_9 = params.get("out_pars")
    if v_9 is not None:
        cargs.append("-out_pars")
        cargs.append(v_9)
    v_10 = params.get("wdir_name")
    if v_10 is not None:
        cargs.append("-wdir_name")
        cargs.append(v_10)
    v_11 = params.get("blur_sigma")
    if v_11 is not None:
        cargs.append("-blur_sigma")
        cargs.append(str(v_11))
    v_12 = params.get("do_recenter_freq")
    if v_12 is not None:
        cargs.append("-do_recenter_freq")
        cargs.append(v_12)
    v_13 = params.get("mask_dilate")
    if v_13 is not None:
        cargs.append("-mask_dilate")
        for item0 in v_13:
            cargs.append(str(item0))
    if params.get("no_clean"):
        cargs.append("-no_clean")
    if params.get("qc_box_focus_ulay"):
        cargs.append("-qc_box_focus_ulay")
    if params.get("no_qc_image"):
        cargs.append("-no_qc_image")
    if params.get("help"):
        cargs.append("-help")
    if params.get("ver"):
        cargs.append("-ver")
    if params.get("date"):
        cargs.append("-date")
    return cargs

def epi_b0_correct_py_outputs(params: EpiB0CorrectPy, execution: Execution) -> EpiB0CorrectPyOutputs:
    """Build the EpiB0CorrectPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    warp_dset_v: OutputPathType = execution.output_file(f'{params["prefix"]}_WARP.nii.gz')
    cmds_script_v: OutputPathType = execution.output_file(f'{params["prefix"]}_cmds.tcsh')
    params_txt_v: OutputPathType = execution.output_file(f'{params["prefix"]}_pars.txt')
    unwarped_epi_v: OutputPathType = execution.output_file(f'{params["prefix"]}_unwarped.nii.gz')
    qc_image_dir_v: OutputPathType = execution.output_file(f'{params["prefix"]}_QC/')
    return EpiB0CorrectPyOutputs(
        root=root_v,
        warp_dset=warp_dset_v,
        cmds_script=cmds_script_v,
        params_txt=params_txt_v,
        unwarped_epi=unwarped_epi_v,
        qc_image_dir=qc_image_dir_v,
    )

def epi_b0_correct_py_execute(params: EpiB0CorrectPy, runner: Runner | None = None) -> EpiB0CorrectPyOutputs:
    """
    epi_b0_correct.py

    B0 distortion correction tool using an acquired frequency (phase) image.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    epi_b0_correct_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EPI_B0_CORRECT_PY_METADATA)
    execution.params(params)
    args = epi_b0_correct_py_cargs(params, execution)
    out = epi_b0_correct_py_outputs(params, execution)
    execution.run(args)
    return out

def epi_b0_correct_py(
    prefix: str,
    input_freq: InputPathType,
    input_epi: InputPathType,
    epi_pe_dir: str,
    input_mask: InputPathType | None = None,
    input_magn: InputPathType | None = None,
    input_anat: InputPathType | None = None,
    input_json: InputPathType | None = None,
    epi_pe_bwpp: float | None = None,
    epi_pe_echo_sp: float | None = None,
    epi_pe_vox_dim: float | None = None,
    scale_freq: float | None = None,
    out_cmds: str | None = None,
    out_pars: str | None = None,
    wdir_name: str | None = None,
    blur_sigma: float | None = None,
    do_recenter_freq: str | None = None,
    mask_dilate: list[float] | None = None,
    no_clean: bool = False,
    qc_box_focus_ulay: bool = False,
    no_qc_image: bool = False,
    help: bool = False,
    ver: bool = False,
    date: bool = False,
    runner: Runner | None = None,
) -> EpiB0CorrectPyOutputs:
    """
    epi_b0_correct.py

    B0 distortion correction tool using an acquired frequency (phase) image.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix of output files; can include path.
        input_freq: Phase dataset (frequency volume); should be at similar\
            resolution and FOV as the EPI dataset; must be scaled appropriately.
        input_epi: EPI dataset to which B0 distortion correction will be applied.
        epi_pe_dir: Direction (axis) of phase encoding, e.g., AP, PA, RL, ...
        input_mask: Mask of brain volume.
        input_magn: Magnitude dataset from which to estimate brain mask; can be\
            used for QC imaging.
        input_anat: Anatomical dataset to be used as underlay for QC images\
            (optional).
        input_json: JSON file containing parameters about the EPI dataset.
        epi_pe_bwpp: Bandwidth per pixel (in Hz) in the EPI dataset along the phase\
            encode direction.
        epi_pe_echo_sp: Effective TE spacing of the phase encoded volume, in\
            seconds.
        epi_pe_vox_dim: Voxel size along the EPI dataset's phase encode axis, in\
            mm.
        scale_freq: Scale to apply to frequency volume to match units (def:\
            SF=1.0).
        out_cmds: Name for output script recording commands (def:\
            PREFIX_cmds.tcsh).
        out_pars: Name for output text file recording relevant parameters (def:\
            PREFIX_pars.txt).
        wdir_name: Working directory name (def: automatic name).
        blur_sigma: Amount of blurring to apply to masked phase encode dataset\
            (def: BS=9).
        do_recenter_freq: Method to recenter the phase volume within the brain mask\
            (def: MC=mode).
        mask_dilate: Erosion and dilation parameters for automask (when using\
            magnitude image).
        no_clean: Don't remove the temporary directory of intermediate files.
        qc_box_focus_ulay: Focus the QC images on an automask region of the\
            underlay dataset.
        no_qc_image: Don't generate QC images.
        help: Display program help in terminal.
        ver: Display program version number in terminal.
        date: Display date of program's last update in terminal.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = epi_b0_correct_py_params(
        prefix=prefix,
        input_freq=input_freq,
        input_epi=input_epi,
        epi_pe_dir=epi_pe_dir,
        input_mask=input_mask,
        input_magn=input_magn,
        input_anat=input_anat,
        input_json=input_json,
        epi_pe_bwpp=epi_pe_bwpp,
        epi_pe_echo_sp=epi_pe_echo_sp,
        epi_pe_vox_dim=epi_pe_vox_dim,
        scale_freq=scale_freq,
        out_cmds=out_cmds,
        out_pars=out_pars,
        wdir_name=wdir_name,
        blur_sigma=blur_sigma,
        do_recenter_freq=do_recenter_freq,
        mask_dilate=mask_dilate,
        no_clean=no_clean,
        qc_box_focus_ulay=qc_box_focus_ulay,
        no_qc_image=no_qc_image,
        help=help,
        ver=ver,
        date=date,
    )
    return epi_b0_correct_py_execute(params, runner)

__all__ = [
    "EpiB0CorrectPy",
    "EpiB0CorrectPyOutputs",
    "EPI_B0_CORRECT_PY_METADATA",
    "epi_b0_correct_py_cargs",
    "epi_b0_correct_py_outputs",
    "epi_b0_correct_py_params",
    "epi_b0_correct_py_execute",
    "epi_b0_correct_py_validate",
    "epi_b0_correct_py",
]