# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VECWARP_METADATA = Metadata(
    id="Vecwarp",
    name="Vecwarp",
    package="afni",
)

Vecwarp = typing.TypedDict(
    "Vecwarp",
    {
        "@type": typing.NotRequired[typing.Literal["afni/Vecwarp"]],
        "apar": typing.NotRequired[InputPathType],
        "matvec": typing.NotRequired[InputPathType],
        "forward": typing.NotRequired[bool],
        "backward": typing.NotRequired[bool],
        "input": typing.NotRequired[InputPathType],
        "output": typing.NotRequired[str],
        "force": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class VecwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """The output file containing the transformed 3-vectors."""

def vecwarp_params(
    apar: InputPathType | None = None,
    matvec: InputPathType | None = None,
    forward: bool = False,
    backward: bool = False,
    input_: InputPathType | None = None,
    output: str | None = None,
    force: bool = False,
) -> Vecwarp:
    """
    Build parameters.

    Args:
        apar: Use the AFNI dataset 'aaa' as the source of the transformation; this\
            dataset must be in +acpc or +tlrc coordinates, and must contain the\
            attributes WARP_TYPE and WARP_DATA which describe the forward\
            transformation from +orig coordinates to the 'aaa' coordinate system.
        matvec: Read an affine transformation matrix-vector from file 'mmm', which\
            must be in the specified format.
        forward: To apply the forward transformation. If neither -forward nor\
            -backward is given, -forward is the default.
        backward: To apply the backward transformation.
        input_: Read input 3-vectors from the file 'iii' (from stdin if 'iii' is\
            '-' or the -input option is missing).
        output: Write the output to file 'ooo' (to stdout if 'ooo' is '-', or if\
            the -output option is missing).
        force: If the output file already exists, use -force to overwrite it. If\
            -force is used, it must come before -output on the command line.

    Returns:
        Parameter dictionary.
    """
    params: Vecwarp = {
        "@type": "afni/Vecwarp",
        "forward": forward,
        "backward": backward,
        "force": force,
    }
    if apar is not None:
        params["apar"] = apar
    if matvec is not None:
        params["matvec"] = matvec
    if input_ is not None:
        params["input"] = input_
    if output is not None:
        params["output"] = output
    return params

def vecwarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Vecwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("apar", None) is not None:
        if not isinstance(params["apar"], (pathlib.Path, str)):
            raise StyxValidationError(f'`apar` has the wrong type: Received `{type(params["apar"])}` expected `InputPathType`')
    if params.get("matvec", None) is not None:
        if not isinstance(params["matvec"], (pathlib.Path, str)):
            raise StyxValidationError(f'`matvec` has the wrong type: Received `{type(params["matvec"])}` expected `InputPathType`')
    if params.get("forward", None) is not None:
        if not isinstance(params["forward"], bool):
            raise StyxValidationError(f'`forward` has the wrong type: Received `{type(params["forward"])}` expected `bool`')
    if params.get("backward", None) is not None:
        if not isinstance(params["backward"], bool):
            raise StyxValidationError(f'`backward` has the wrong type: Received `{type(params["backward"])}` expected `bool`')
    if params.get("input", None) is not None:
        if not isinstance(params["input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')

def vecwarp_cargs(params: Vecwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("Vecwarp")
    v_0 = params.get("apar")
    if v_0 is not None:
        cargs.append("-apar")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("matvec")
    if v_1 is not None:
        cargs.append("-matvec")
        cargs.append(execution.input_file(v_1))
    if params.get("forward"):
        cargs.append("-forward")
    if params.get("backward"):
        cargs.append("-backward")
    v_2 = params.get("input")
    if v_2 is not None:
        cargs.append("-input")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("output")
    if v_3 is not None:
        cargs.append("-output")
        cargs.append(v_3)
    if params.get("force"):
        cargs.append("-force")
    return cargs

def vecwarp_outputs(params: Vecwarp, execution: Execution) -> VecwarpOutputs:
    """Build the VecwarpOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return VecwarpOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def vecwarp_execute(params: Vecwarp, runner: Runner | None = None) -> VecwarpOutputs:
    """
    Vecwarp

    Transforms (warps) a list of 3-vectors into another list of 3-vectors according to the specified options.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    vecwarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VECWARP_METADATA)
    execution.params(params)
    args = vecwarp_cargs(params, execution)
    out = vecwarp_outputs(params, execution)
    execution.run(args)
    return out

def vecwarp(
    apar: InputPathType | None = None,
    matvec: InputPathType | None = None,
    forward: bool = False,
    backward: bool = False,
    input_: InputPathType | None = None,
    output: str | None = None,
    force: bool = False,
    runner: Runner | None = None,
) -> VecwarpOutputs:
    """
    Vecwarp

    Transforms (warps) a list of 3-vectors into another list of 3-vectors according to the specified options.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        apar: Use the AFNI dataset 'aaa' as the source of the transformation; this\
            dataset must be in +acpc or +tlrc coordinates, and must contain the\
            attributes WARP_TYPE and WARP_DATA which describe the forward\
            transformation from +orig coordinates to the 'aaa' coordinate system.
        matvec: Read an affine transformation matrix-vector from file 'mmm', which\
            must be in the specified format.
        forward: To apply the forward transformation. If neither -forward nor\
            -backward is given, -forward is the default.
        backward: To apply the backward transformation.
        input_: Read input 3-vectors from the file 'iii' (from stdin if 'iii' is\
            '-' or the -input option is missing).
        output: Write the output to file 'ooo' (to stdout if 'ooo' is '-', or if\
            the -output option is missing).
        force: If the output file already exists, use -force to overwrite it. If\
            -force is used, it must come before -output on the command line.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = vecwarp_params(
        apar=apar,
        matvec=matvec,
        forward=forward,
        backward=backward,
        input_=input_,
        output=output,
        force=force,
    )
    return vecwarp_execute(params, runner)

__all__ = [
    "Vecwarp",
    "VecwarpOutputs",
    "VECWARP_METADATA",
    "vecwarp_cargs",
    "vecwarp_outputs",
    "vecwarp_params",
    "vecwarp_execute",
    "vecwarp_validate",
    "vecwarp",
]