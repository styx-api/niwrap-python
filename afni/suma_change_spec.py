# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SUMA_CHANGE_SPEC_METADATA = Metadata(
    id="suma_change_spec",
    name="suma_change_spec",
    package="afni",
)

SumaChangeSpec = typing.TypedDict(
    "SumaChangeSpec",
    {
        "@type": typing.NotRequired[typing.Literal["afni/suma_change_spec"]],
        "input": InputPathType,
        "state": str,
        "domainparent": typing.NotRequired[str],
        "output": typing.NotRequired[str],
        "remove": typing.NotRequired[bool],
        "anatomical": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SumaChangeSpecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_spec: OutputPathType | None
    """New Spec file"""
    backup_spec: OutputPathType
    """Backup of the original Spec file"""

def suma_change_spec_params(
    input_: InputPathType,
    state: str,
    domainparent: str | None = None,
    output: str | None = None,
    remove: bool = False,
    anatomical: bool = False,
) -> SumaChangeSpec:
    """
    Build parameters.

    Args:
        input_: SUMA Spec file to change
        state: State within the Spec file to change
        domainparent: New Domain Parent for the state within the Spec file
        output: Name to which the new Spec file will be temporarily written
        remove: Remove the automatically created backup
        anatomical: Add 'Anatomical = Y' to the selected SurfaceState

    Returns:
        Parameter dictionary.
    """
    params: SumaChangeSpec = {
        "@type": "afni/suma_change_spec",
        "input": input_,
        "state": state,
        "remove": remove,
        "anatomical": anatomical,
    }
    if domainparent is not None:
        params["domainparent"] = domainparent
    if output is not None:
        params["output"] = output
    return params

def suma_change_spec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SumaChangeSpec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("state", None) is None:
        raise StyxValidationError("`state` must not be None")
    if not isinstance(params["state"], str):
        raise StyxValidationError(f'`state` has the wrong type: Received `{type(params["state"])}` expected `str`')
    if params.get("domainparent", None) is not None:
        if not isinstance(params["domainparent"], str):
            raise StyxValidationError(f'`domainparent` has the wrong type: Received `{type(params["domainparent"])}` expected `str`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("remove", None) is not None:
        if not isinstance(params["remove"], bool):
            raise StyxValidationError(f'`remove` has the wrong type: Received `{type(params["remove"])}` expected `bool`')
    if params.get("anatomical", None) is not None:
        if not isinstance(params["anatomical"], bool):
            raise StyxValidationError(f'`anatomical` has the wrong type: Received `{type(params["anatomical"])}` expected `bool`')

def suma_change_spec_cargs(params: SumaChangeSpec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("suma_change_spec")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["state"])
    v_0 = params.get("domainparent")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("output")
    if v_1 is not None:
        cargs.append(v_1)
    if params.get("remove"):
        cargs.append("-remove")
    if params.get("anatomical"):
        cargs.append("-anatomical")
    return cargs

def suma_change_spec_outputs(params: SumaChangeSpec, execution: Execution) -> SumaChangeSpecOutputs:
    """Build the SumaChangeSpecOutputs object for this tool."""
    output_spec_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        output_spec_v = execution.output_file((__v0 if __v0 is not None else ""))
    backup_spec_v: OutputPathType = execution.output_file(f'{params["input"]}.bkp')
    return SumaChangeSpecOutputs(
        root=root_v,
        output_spec=output_spec_v,
        backup_spec=backup_spec_v,
    )

def suma_change_spec_execute(params: SumaChangeSpec, runner: Runner | None = None) -> SumaChangeSpecOutputs:
    """
    suma_change_spec

    This program changes SUMA's surface specification (Spec) files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    suma_change_spec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SUMA_CHANGE_SPEC_METADATA)
    execution.params(params)
    args = suma_change_spec_cargs(params, execution)
    out = suma_change_spec_outputs(params, execution)
    execution.run(args)
    return out

def suma_change_spec(
    input_: InputPathType,
    state: str,
    domainparent: str | None = None,
    output: str | None = None,
    remove: bool = False,
    anatomical: bool = False,
    runner: Runner | None = None,
) -> SumaChangeSpecOutputs:
    """
    suma_change_spec

    This program changes SUMA's surface specification (Spec) files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: SUMA Spec file to change
        state: State within the Spec file to change
        domainparent: New Domain Parent for the state within the Spec file
        output: Name to which the new Spec file will be temporarily written
        remove: Remove the automatically created backup
        anatomical: Add 'Anatomical = Y' to the selected SurfaceState
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = suma_change_spec_params(
        input_=input_,
        state=state,
        domainparent=domainparent,
        output=output,
        remove=remove,
        anatomical=anatomical,
    )
    return suma_change_spec_execute(params, runner)

__all__ = [
    "SumaChangeSpec",
    "SumaChangeSpecOutputs",
    "SUMA_CHANGE_SPEC_METADATA",
    "suma_change_spec_cargs",
    "suma_change_spec_outputs",
    "suma_change_spec_params",
    "suma_change_spec_execute",
    "suma_change_spec_validate",
    "suma_change_spec",
]