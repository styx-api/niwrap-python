# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIND_AFNI_DSET_PATH_METADATA = Metadata(
    id="@FindAfniDsetPath",
    name="@FindAfniDsetPath",
    package="afni",
)

FindAfniDsetPath = typing.TypedDict(
    "FindAfniDsetPath",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@FindAfniDsetPath"]],
        "dsetname": str,
        "append_file": typing.NotRequired[bool],
        "full_path": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FindAfniDsetPathOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def find_afni_dset_path_params(
    dsetname: str,
    append_file: bool = False,
    full_path: bool = False,
    help: bool = False,
) -> FindAfniDsetPath:
    """
    Build parameters.

    Args:
        dsetname: Name of the dataset to search for
        append_file: Show the file appended to (even with atlas name)
        full_path: Print full path instead of '.'
        help: Display help message

    Returns:
        Parameter dictionary.
    """
    params: FindAfniDsetPath = {
        "@type": "afni/@FindAfniDsetPath",
        "dsetname": dsetname,
        "append_file": append_file,
        "full_path": full_path,
        "help": help,
    }
    return params

def find_afni_dset_path_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FindAfniDsetPath."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dsetname", None) is None:
        raise StyxValidationError("`dsetname` must not be None")
    if not isinstance(params["dsetname"], str):
        raise StyxValidationError(f'`dsetname` has the wrong type: Received `{type(params["dsetname"])}` expected `str`')
    if params.get("append_file", None) is not None:
        if not isinstance(params["append_file"], bool):
            raise StyxValidationError(f'`append_file` has the wrong type: Received `{type(params["append_file"])}` expected `bool`')
    if params.get("full_path", None) is not None:
        if not isinstance(params["full_path"], bool):
            raise StyxValidationError(f'`full_path` has the wrong type: Received `{type(params["full_path"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def find_afni_dset_path_cargs(params: FindAfniDsetPath, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@FindAfniDsetPath")
    cargs.append(params["dsetname"])
    if params.get("append_file"):
        cargs.append("-append_file")
    if params.get("full_path"):
        cargs.append("-full_path")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def find_afni_dset_path_outputs(params: FindAfniDsetPath, execution: Execution) -> FindAfniDsetPathOutputs:
    """Build the FindAfniDsetPathOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FindAfniDsetPathOutputs(
        root=root_v,
    )

def find_afni_dset_path_execute(params: FindAfniDsetPath, runner: Runner | None = None) -> FindAfniDsetPathOutputs:
    """
    @FindAfniDsetPath

    Searches various AFNI directories for a specified dataset and returns its path.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    find_afni_dset_path_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIND_AFNI_DSET_PATH_METADATA)
    execution.params(params)
    args = find_afni_dset_path_cargs(params, execution)
    out = find_afni_dset_path_outputs(params, execution)
    execution.run(args)
    return out

def find_afni_dset_path(
    dsetname: str,
    append_file: bool = False,
    full_path: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> FindAfniDsetPathOutputs:
    """
    @FindAfniDsetPath

    Searches various AFNI directories for a specified dataset and returns its path.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dsetname: Name of the dataset to search for
        append_file: Show the file appended to (even with atlas name)
        full_path: Print full path instead of '.'
        help: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = find_afni_dset_path_params(
        dsetname=dsetname,
        append_file=append_file,
        full_path=full_path,
        help=help,
    )
    return find_afni_dset_path_execute(params, runner)

__all__ = [
    "FindAfniDsetPath",
    "FindAfniDsetPathOutputs",
    "FIND_AFNI_DSET_PATH_METADATA",
    "find_afni_dset_path_cargs",
    "find_afni_dset_path_outputs",
    "find_afni_dset_path_params",
    "find_afni_dset_path_execute",
    "find_afni_dset_path_validate",
    "find_afni_dset_path",
]