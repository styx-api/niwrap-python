# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PARSE_FS_LT_LOG_PY_METADATA = Metadata(
    id="parse_fs_lt_log.py",
    name="parse_fs_lt_log.py",
    package="afni",
)

ParseFsLtLogPy = typing.TypedDict(
    "ParseFsLtLogPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/parse_fs_lt_log.py"]],
        "logfile": InputPathType,
        "labels": list[str],
        "show_orig": typing.NotRequired[bool],
        "show_all_orig": typing.NotRequired[bool],
        "verbosity": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class ParseFsLtLogPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def parse_fs_lt_log_py_params(
    logfile: InputPathType,
    labels: list[str],
    show_orig: bool = False,
    show_all_orig: bool = False,
    verbosity: float | None = None,
) -> ParseFsLtLogPy:
    """
    Build parameters.

    Args:
        logfile: Specify rank log file.
        labels: Specify a list of labels to search for.
        show_orig: Show original label indices.
        show_all_orig: Show all original label indices.
        verbosity: Specify verbosity level.

    Returns:
        Parameter dictionary.
    """
    params: ParseFsLtLogPy = {
        "@type": "afni/parse_fs_lt_log.py",
        "logfile": logfile,
        "labels": labels,
        "show_orig": show_orig,
        "show_all_orig": show_all_orig,
    }
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def parse_fs_lt_log_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ParseFsLtLogPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("logfile", None) is None:
        raise StyxValidationError("`logfile` must not be None")
    if not isinstance(params["logfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`logfile` has the wrong type: Received `{type(params["logfile"])}` expected `InputPathType`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], list):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `list[str]`')
    for e in params["labels"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`labels` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("show_orig", None) is not None:
        if not isinstance(params["show_orig"], bool):
            raise StyxValidationError(f'`show_orig` has the wrong type: Received `{type(params["show_orig"])}` expected `bool`')
    if params.get("show_all_orig", None) is not None:
        if not isinstance(params["show_all_orig"], bool):
            raise StyxValidationError(f'`show_all_orig` has the wrong type: Received `{type(params["show_all_orig"])}` expected `bool`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], (float, int)):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `float`')

def parse_fs_lt_log_py_cargs(params: ParseFsLtLogPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("parse_fs_lt_log.py")
    cargs.append("-logfile")
    cargs.append(execution.input_file(params["logfile"]))
    cargs.append("-labels")
    for item0 in params["labels"]:
        cargs.append(item0)
    if params.get("show_orig"):
        cargs.append("-show_orig")
    if params.get("show_all_orig"):
        cargs.append("-show_all_orig")
    v_0 = params.get("verbosity")
    if v_0 is not None:
        cargs.append("-verb")
        cargs.append(str(v_0))
    return cargs

def parse_fs_lt_log_py_outputs(params: ParseFsLtLogPy, execution: Execution) -> ParseFsLtLogPyOutputs:
    """Build the ParseFsLtLogPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ParseFsLtLogPyOutputs(
        root=root_v,
    )

def parse_fs_lt_log_py_execute(params: ParseFsLtLogPy, runner: Runner | None = None) -> ParseFsLtLogPyOutputs:
    """
    parse_fs_lt_log.py

    Parses FreeSurfer labeltable log file and retrieves labeltable indices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    parse_fs_lt_log_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PARSE_FS_LT_LOG_PY_METADATA)
    execution.params(params)
    args = parse_fs_lt_log_py_cargs(params, execution)
    out = parse_fs_lt_log_py_outputs(params, execution)
    execution.run(args)
    return out

def parse_fs_lt_log_py(
    logfile: InputPathType,
    labels: list[str],
    show_orig: bool = False,
    show_all_orig: bool = False,
    verbosity: float | None = None,
    runner: Runner | None = None,
) -> ParseFsLtLogPyOutputs:
    """
    parse_fs_lt_log.py

    Parses FreeSurfer labeltable log file and retrieves labeltable indices.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        logfile: Specify rank log file.
        labels: Specify a list of labels to search for.
        show_orig: Show original label indices.
        show_all_orig: Show all original label indices.
        verbosity: Specify verbosity level.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = parse_fs_lt_log_py_params(
        logfile=logfile,
        labels=labels,
        show_orig=show_orig,
        show_all_orig=show_all_orig,
        verbosity=verbosity,
    )
    return parse_fs_lt_log_py_execute(params, runner)

__all__ = [
    "ParseFsLtLogPy",
    "ParseFsLtLogPyOutputs",
    "PARSE_FS_LT_LOG_PY_METADATA",
    "parse_fs_lt_log_py_cargs",
    "parse_fs_lt_log_py_outputs",
    "parse_fs_lt_log_py_params",
    "parse_fs_lt_log_py_execute",
    "parse_fs_lt_log_py_validate",
    "parse_fs_lt_log_py",
]