# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_STAT_CLUST_METADATA = Metadata(
    id="3dStatClust",
    name="3dStatClust",
    package="afni",
)

V3dStatClust = typing.TypedDict(
    "V3dStatClust",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dStatClust"]],
        "prefix": typing.NotRequired[str],
        "session_dir": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
        "dist_euc": typing.NotRequired[bool],
        "dist_ind": typing.NotRequired[bool],
        "dist_cor": typing.NotRequired[bool],
        "thresh": str,
        "nclust": float,
        "datasets": list[str],
    },
)

@dataclasses.dataclass
class V3dStatClustOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_head: OutputPathType | None
    """Output dataset header"""
    output_brick: OutputPathType | None
    """Output dataset brick"""

def v_3d_stat_clust_params(
    thresh: str,
    nclust: float,
    datasets: list[str],
    prefix: str | None = None,
    session_dir: str | None = None,
    verbose: bool = False,
    dist_euc: bool = False,
    dist_ind: bool = False,
    dist_cor: bool = False,
) -> V3dStatClust:
    """
    Build parameters.

    Args:
        thresh: Threshold statistic from file tname. Only voxels whose threshold\
            statistic is greater than t in absolute value will be considered. If\
            file tname contains more than 1 sub-brick, the threshold stat.\
            sub-brick must be specified.
        nclust: Maximum number of clusters for output (= number of sub-bricks in\
            output dataset).
        datasets: Parameter datasets.
        prefix: Use 'pname' for the output dataset prefix name.
        session_dir: Use 'dir' for the output dataset session directory.
        verbose: Print out verbose output as the program proceeds.
        dist_euc: Calculate Euclidean distance between parameters
        dist_ind: Statistical distance for independent parameters
        dist_cor: Statistical distance for correlated parameters

    Returns:
        Parameter dictionary.
    """
    params: V3dStatClust = {
        "@type": "afni/3dStatClust",
        "thresh": thresh,
        "nclust": nclust,
        "datasets": datasets,
        "verbose": verbose,
        "dist_euc": dist_euc,
        "dist_ind": dist_ind,
        "dist_cor": dist_cor,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if session_dir is not None:
        params["session_dir"] = session_dir
    return params

def v_3d_stat_clust_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dStatClust."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("session_dir", None) is not None:
        if not isinstance(params["session_dir"], str):
            raise StyxValidationError(f'`session_dir` has the wrong type: Received `{type(params["session_dir"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("dist_euc", None) is not None:
        if not isinstance(params["dist_euc"], bool):
            raise StyxValidationError(f'`dist_euc` has the wrong type: Received `{type(params["dist_euc"])}` expected `bool`')
    if params.get("dist_ind", None) is not None:
        if not isinstance(params["dist_ind"], bool):
            raise StyxValidationError(f'`dist_ind` has the wrong type: Received `{type(params["dist_ind"])}` expected `bool`')
    if params.get("dist_cor", None) is not None:
        if not isinstance(params["dist_cor"], bool):
            raise StyxValidationError(f'`dist_cor` has the wrong type: Received `{type(params["dist_cor"])}` expected `bool`')
    if params.get("thresh", None) is None:
        raise StyxValidationError("`thresh` must not be None")
    if not isinstance(params["thresh"], str):
        raise StyxValidationError(f'`thresh` has the wrong type: Received `{type(params["thresh"])}` expected `str`')
    if params.get("nclust", None) is None:
        raise StyxValidationError("`nclust` must not be None")
    if not isinstance(params["nclust"], (float, int)):
        raise StyxValidationError(f'`nclust` has the wrong type: Received `{type(params["nclust"])}` expected `float`')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[str]`')
    for e in params["datasets"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[str]`')

def v_3d_stat_clust_cargs(params: V3dStatClust, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dStatClust")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("session_dir")
    if v_1 is not None:
        cargs.append("-session")
        cargs.append(v_1)
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("dist_euc"):
        cargs.append("-dist_euc")
    if params.get("dist_ind"):
        cargs.append("-dist_ind")
    if params.get("dist_cor"):
        cargs.append("-dist_cor")
    cargs.append("-thresh")
    cargs.append(params["thresh"])
    cargs.append("-nclust")
    cargs.append(str(params["nclust"]))
    for item0 in params["datasets"]:
        cargs.append(item0)
    return cargs

def v_3d_stat_clust_outputs(params: V3dStatClust, execution: Execution) -> V3dStatClustOutputs:
    """Build the V3dStatClustOutputs object for this tool."""
    output_head_v: OutputPathType | None = None
    output_brick_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_brick_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    return V3dStatClustOutputs(
        root=root_v,
        output_head=output_head_v,
        output_brick=output_brick_v,
    )

def v_3d_stat_clust_execute(params: V3dStatClust, runner: Runner | None = None) -> V3dStatClustOutputs:
    """
    3dStatClust

    Perform agglomerative hierarchical clustering for user specified parameter sub-bricks, for all voxels whose threshold statistic is above a user specified value.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_stat_clust_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_STAT_CLUST_METADATA)
    execution.params(params)
    args = v_3d_stat_clust_cargs(params, execution)
    out = v_3d_stat_clust_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_stat_clust(
    thresh: str,
    nclust: float,
    datasets: list[str],
    prefix: str | None = None,
    session_dir: str | None = None,
    verbose: bool = False,
    dist_euc: bool = False,
    dist_ind: bool = False,
    dist_cor: bool = False,
    runner: Runner | None = None,
) -> V3dStatClustOutputs:
    """
    3dStatClust

    Perform agglomerative hierarchical clustering for user specified parameter sub-bricks, for all voxels whose threshold statistic is above a user specified value.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        thresh: Threshold statistic from file tname. Only voxels whose threshold\
            statistic is greater than t in absolute value will be considered. If\
            file tname contains more than 1 sub-brick, the threshold stat.\
            sub-brick must be specified.
        nclust: Maximum number of clusters for output (= number of sub-bricks in\
            output dataset).
        datasets: Parameter datasets.
        prefix: Use 'pname' for the output dataset prefix name.
        session_dir: Use 'dir' for the output dataset session directory.
        verbose: Print out verbose output as the program proceeds.
        dist_euc: Calculate Euclidean distance between parameters
        dist_ind: Statistical distance for independent parameters
        dist_cor: Statistical distance for correlated parameters
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_stat_clust_params(
        thresh=thresh,
        nclust=nclust,
        datasets=datasets,
        prefix=prefix,
        session_dir=session_dir,
        verbose=verbose,
        dist_euc=dist_euc,
        dist_ind=dist_ind,
        dist_cor=dist_cor,
    )
    return v_3d_stat_clust_execute(params, runner)

__all__ = [
    "V3dStatClust",
    "V3dStatClustOutputs",
    "V_3D_STAT_CLUST_METADATA",
    "v_3d_stat_clust_cargs",
    "v_3d_stat_clust_outputs",
    "v_3d_stat_clust_params",
    "v_3d_stat_clust_execute",
    "v_3d_stat_clust_validate",
    "v_3d_stat_clust",
]