# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TCORRELATE_METADATA = Metadata(
    id="3dTcorrelate",
    name="3dTcorrelate",
    package="afni",
)

V3dTcorrelate = typing.TypedDict(
    "V3dTcorrelate",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTcorrelate"]],
        "xset": InputPathType,
        "yset": InputPathType,
        "pearson": typing.NotRequired[bool],
        "spearman": typing.NotRequired[bool],
        "quadrant": typing.NotRequired[bool],
        "ktaub": typing.NotRequired[bool],
        "covariance": typing.NotRequired[bool],
        "partial": typing.NotRequired[InputPathType],
        "ycoef": typing.NotRequired[bool],
        "fisher": typing.NotRequired[bool],
        "polort": typing.NotRequired[int],
        "ort": typing.NotRequired[InputPathType],
        "autoclip": typing.NotRequired[bool],
        "automask": typing.NotRequired[bool],
        "zcensor": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dTcorrelateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Output image file name."""

def v_3d_tcorrelate_params(
    xset: InputPathType,
    yset: InputPathType,
    pearson: bool = False,
    spearman: bool = False,
    quadrant: bool = False,
    ktaub: bool = False,
    covariance: bool = False,
    partial: InputPathType | None = None,
    ycoef: bool = False,
    fisher: bool = False,
    polort: int | None = None,
    ort: InputPathType | None = None,
    autoclip: bool = False,
    automask: bool = False,
    zcensor: bool = False,
    prefix: str | None = None,
) -> V3dTcorrelate:
    """
    Build parameters.

    Args:
        xset: Input xset.
        yset: Input yset.
        pearson: Correlation is the normal pearson correlation coefficient.
        spearman: Correlation is the Spearman (rank) correlation coefficient.
        quadrant: Correlation is the quadrant coefficient.
        ktaub: Correlation is Kendall's tau_b coefficient. For continuous or finely\
            discretized data, tau_b and rank correlation are nearly equivalent.
        covariance: Covariance instead of correlation. That would be Pearson\
            correlation without scaling by the product of the standard deviations.
        partial: Partial Pearson's correlation of X & Y, adjusting for Z (the\
            dataset provided here).
        ycoef: Least squares coefficient that best fits y(t) to x(t), after\
            detrending. That is, if yd(t) is the detrended y(t) and xd(t) is the\
            detrended x(t), then the ycoef value is from the OLSQ fit to xd(t) =\
            ycoef & y(t) + error.
        fisher: Apply the Fisher (inverse hyperbolic tangent) transformation to\
            correlation results. Does not make sense with ktaub, covariance, or\
            ycoef.
        polort: Remove polynomial trend of order m. Using m=-1 mean no detrending;\
            this is only useful fro data that has been preprocessed.
        ort: A 1D file. Also detrend using the columbs of the 1D file provided\
            here. Only one -ort option can be given, so if you would like to use\
            more than one, create a temporary file using 1dcat.
        autoclip: Clip off low-intensity regions in the two datasets, so that the\
            correlation is only computed between high-intensity (presumably brain)\
            voxels. The intensity level is determined the same way that 3dClipLevel\
            works.
        automask: Clip off low-intensity regions in the two datasets, so that the\
            correlation is only computed between high-intensity (presumably brain)\
            voxels. The intensity level is determined the same way that 3dClipLevel\
            works.
        zcensor: Omit (censor out) any time points where the xset volume is all\
            zero OR where the yset volume is all zero (in mask). Please note that\
            using -zcensor with any detrending is unlikely to be useful.
        prefix: Save output into a dataset with this prefix.

    Returns:
        Parameter dictionary.
    """
    params: V3dTcorrelate = {
        "@type": "afni/3dTcorrelate",
        "xset": xset,
        "yset": yset,
        "pearson": pearson,
        "spearman": spearman,
        "quadrant": quadrant,
        "ktaub": ktaub,
        "covariance": covariance,
        "ycoef": ycoef,
        "fisher": fisher,
        "autoclip": autoclip,
        "automask": automask,
        "zcensor": zcensor,
    }
    if partial is not None:
        params["partial"] = partial
    if polort is not None:
        params["polort"] = polort
    if ort is not None:
        params["ort"] = ort
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_tcorrelate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTcorrelate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("xset", None) is None:
        raise StyxValidationError("`xset` must not be None")
    if not isinstance(params["xset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`xset` has the wrong type: Received `{type(params["xset"])}` expected `InputPathType`')
    if params.get("yset", None) is None:
        raise StyxValidationError("`yset` must not be None")
    if not isinstance(params["yset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`yset` has the wrong type: Received `{type(params["yset"])}` expected `InputPathType`')
    if params.get("pearson", None) is not None:
        if not isinstance(params["pearson"], bool):
            raise StyxValidationError(f'`pearson` has the wrong type: Received `{type(params["pearson"])}` expected `bool`')
    if params.get("spearman", None) is not None:
        if not isinstance(params["spearman"], bool):
            raise StyxValidationError(f'`spearman` has the wrong type: Received `{type(params["spearman"])}` expected `bool`')
    if params.get("quadrant", None) is not None:
        if not isinstance(params["quadrant"], bool):
            raise StyxValidationError(f'`quadrant` has the wrong type: Received `{type(params["quadrant"])}` expected `bool`')
    if params.get("ktaub", None) is not None:
        if not isinstance(params["ktaub"], bool):
            raise StyxValidationError(f'`ktaub` has the wrong type: Received `{type(params["ktaub"])}` expected `bool`')
    if params.get("covariance", None) is not None:
        if not isinstance(params["covariance"], bool):
            raise StyxValidationError(f'`covariance` has the wrong type: Received `{type(params["covariance"])}` expected `bool`')
    if params.get("partial", None) is not None:
        if not isinstance(params["partial"], (pathlib.Path, str)):
            raise StyxValidationError(f'`partial` has the wrong type: Received `{type(params["partial"])}` expected `InputPathType`')
    if params.get("ycoef", None) is not None:
        if not isinstance(params["ycoef"], bool):
            raise StyxValidationError(f'`ycoef` has the wrong type: Received `{type(params["ycoef"])}` expected `bool`')
    if params.get("fisher", None) is not None:
        if not isinstance(params["fisher"], bool):
            raise StyxValidationError(f'`fisher` has the wrong type: Received `{type(params["fisher"])}` expected `bool`')
    if params.get("polort", None) is not None:
        if not isinstance(params["polort"], int):
            raise StyxValidationError(f'`polort` has the wrong type: Received `{type(params["polort"])}` expected `int`')
        if not (-1 <= params["polort"] <= 9):
            raise StyxValidationError("Parameter `polort` must be between -1 and 9 (inclusive)")
    if params.get("ort", None) is not None:
        if not isinstance(params["ort"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ort` has the wrong type: Received `{type(params["ort"])}` expected `InputPathType`')
    if params.get("autoclip", None) is not None:
        if not isinstance(params["autoclip"], bool):
            raise StyxValidationError(f'`autoclip` has the wrong type: Received `{type(params["autoclip"])}` expected `bool`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("zcensor", None) is not None:
        if not isinstance(params["zcensor"], bool):
            raise StyxValidationError(f'`zcensor` has the wrong type: Received `{type(params["zcensor"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def v_3d_tcorrelate_cargs(params: V3dTcorrelate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTcorrelate")
    cargs.append(execution.input_file(params["xset"]))
    cargs.append(execution.input_file(params["yset"]))
    if params.get("pearson"):
        cargs.append("-pearson")
    if params.get("spearman"):
        cargs.append("-spearman")
    if params.get("quadrant"):
        cargs.append("-quadrant")
    if params.get("ktaub"):
        cargs.append("-ktaub")
    if params.get("covariance"):
        cargs.append("-covariance")
    v_0 = params.get("partial")
    if v_0 is not None:
        cargs.append("-partial")
        cargs.append(execution.input_file(v_0))
    if params.get("ycoef"):
        cargs.append("-ycoef")
    if params.get("fisher"):
        cargs.append("-Fisher")
    v_1 = params.get("polort")
    if v_1 is not None:
        cargs.append("-polort")
        cargs.append(str(v_1))
    v_2 = params.get("ort")
    if v_2 is not None:
        cargs.append("-ort")
        cargs.append(execution.input_file(v_2))
    if params.get("autoclip"):
        cargs.append("-autoclip")
    if params.get("automask"):
        cargs.append("-automask")
    if params.get("zcensor"):
        cargs.append("-zcensor")
    v_3 = params.get("prefix")
    if v_3 is not None:
        cargs.append("-prefix")
        cargs.append(v_3)
    return cargs

def v_3d_tcorrelate_outputs(params: V3dTcorrelate, execution: Execution) -> V3dTcorrelateOutputs:
    """Build the V3dTcorrelateOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return V3dTcorrelateOutputs(
        root=root_v,
        out_file=out_file_v,
    )

def v_3d_tcorrelate_execute(params: V3dTcorrelate, runner: Runner | None = None) -> V3dTcorrelateOutputs:
    """
    3dTcorrelate

    3dTcorrelate. Computes the correlation coefficient between corresponding voxel time series in two input 3D+time datasets 'xset' and 'yset'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_tcorrelate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TCORRELATE_METADATA)
    execution.params(params)
    args = v_3d_tcorrelate_cargs(params, execution)
    out = v_3d_tcorrelate_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_tcorrelate(
    xset: InputPathType,
    yset: InputPathType,
    pearson: bool = False,
    spearman: bool = False,
    quadrant: bool = False,
    ktaub: bool = False,
    covariance: bool = False,
    partial: InputPathType | None = None,
    ycoef: bool = False,
    fisher: bool = False,
    polort: int | None = None,
    ort: InputPathType | None = None,
    autoclip: bool = False,
    automask: bool = False,
    zcensor: bool = False,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dTcorrelateOutputs:
    """
    3dTcorrelate

    3dTcorrelate. Computes the correlation coefficient between corresponding voxel time series in two input 3D+time datasets 'xset' and 'yset'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        xset: Input xset.
        yset: Input yset.
        pearson: Correlation is the normal pearson correlation coefficient.
        spearman: Correlation is the Spearman (rank) correlation coefficient.
        quadrant: Correlation is the quadrant coefficient.
        ktaub: Correlation is Kendall's tau_b coefficient. For continuous or finely\
            discretized data, tau_b and rank correlation are nearly equivalent.
        covariance: Covariance instead of correlation. That would be Pearson\
            correlation without scaling by the product of the standard deviations.
        partial: Partial Pearson's correlation of X & Y, adjusting for Z (the\
            dataset provided here).
        ycoef: Least squares coefficient that best fits y(t) to x(t), after\
            detrending. That is, if yd(t) is the detrended y(t) and xd(t) is the\
            detrended x(t), then the ycoef value is from the OLSQ fit to xd(t) =\
            ycoef & y(t) + error.
        fisher: Apply the Fisher (inverse hyperbolic tangent) transformation to\
            correlation results. Does not make sense with ktaub, covariance, or\
            ycoef.
        polort: Remove polynomial trend of order m. Using m=-1 mean no detrending;\
            this is only useful fro data that has been preprocessed.
        ort: A 1D file. Also detrend using the columbs of the 1D file provided\
            here. Only one -ort option can be given, so if you would like to use\
            more than one, create a temporary file using 1dcat.
        autoclip: Clip off low-intensity regions in the two datasets, so that the\
            correlation is only computed between high-intensity (presumably brain)\
            voxels. The intensity level is determined the same way that 3dClipLevel\
            works.
        automask: Clip off low-intensity regions in the two datasets, so that the\
            correlation is only computed between high-intensity (presumably brain)\
            voxels. The intensity level is determined the same way that 3dClipLevel\
            works.
        zcensor: Omit (censor out) any time points where the xset volume is all\
            zero OR where the yset volume is all zero (in mask). Please note that\
            using -zcensor with any detrending is unlikely to be useful.
        prefix: Save output into a dataset with this prefix.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_tcorrelate_params(
        xset=xset,
        yset=yset,
        pearson=pearson,
        spearman=spearman,
        quadrant=quadrant,
        ktaub=ktaub,
        covariance=covariance,
        partial=partial,
        ycoef=ycoef,
        fisher=fisher,
        polort=polort,
        ort=ort,
        autoclip=autoclip,
        automask=automask,
        zcensor=zcensor,
        prefix=prefix,
    )
    return v_3d_tcorrelate_execute(params, runner)

__all__ = [
    "V3dTcorrelate",
    "V3dTcorrelateOutputs",
    "V_3D_TCORRELATE_METADATA",
    "v_3d_tcorrelate_cargs",
    "v_3d_tcorrelate_outputs",
    "v_3d_tcorrelate_params",
    "v_3d_tcorrelate_execute",
    "v_3d_tcorrelate_validate",
    "v_3d_tcorrelate",
]