# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SERIAL_HELPER_METADATA = Metadata(
    id="serial_helper",
    name="serial_helper",
    package="afni",
)

SerialHelper = typing.TypedDict(
    "SerialHelper",
    {
        "@type": typing.NotRequired[typing.Literal["afni/serial_helper"]],
        "serial_port": str,
        "sock_num": typing.NotRequired[float],
        "mp_max": typing.NotRequired[float],
        "mp_min": typing.NotRequired[float],
        "num_extra": typing.NotRequired[float],
        "disp_all": typing.NotRequired[float],
        "debug": typing.NotRequired[float],
        "show_times": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "hist": typing.NotRequired[bool],
        "no_serial": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SerialHelperOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def serial_helper_params(
    serial_port: str,
    sock_num: float | None = None,
    mp_max: float | None = None,
    mp_min: float | None = None,
    num_extra: float | None = None,
    disp_all: float | None = None,
    debug: float | None = None,
    show_times: bool = False,
    help: bool = False,
    hist: bool = False,
    no_serial: bool = False,
    version: bool = False,
) -> SerialHelper:
    """
    Build parameters.

    Args:
        serial_port: Output serial port filename
        sock_num: Specify socket number to serve
        mp_max: Limit the maximum value of the MP data
        mp_min: Limit the minimum value of the MP data
        num_extra: Receive additional floats per TR
        disp_all: Receive NVOX*8 extra floats per TR
        debug: Set the debugging level (0-3)
        show_times: Show communication times
        help: Display this help information
        hist: Show the module history
        no_serial: Turn off serial port output
        version: Show the current version number

    Returns:
        Parameter dictionary.
    """
    params: SerialHelper = {
        "@type": "afni/serial_helper",
        "serial_port": serial_port,
        "show_times": show_times,
        "help": help,
        "hist": hist,
        "no_serial": no_serial,
        "version": version,
    }
    if sock_num is not None:
        params["sock_num"] = sock_num
    if mp_max is not None:
        params["mp_max"] = mp_max
    if mp_min is not None:
        params["mp_min"] = mp_min
    if num_extra is not None:
        params["num_extra"] = num_extra
    if disp_all is not None:
        params["disp_all"] = disp_all
    if debug is not None:
        params["debug"] = debug
    return params

def serial_helper_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SerialHelper."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("serial_port", None) is None:
        raise StyxValidationError("`serial_port` must not be None")
    if not isinstance(params["serial_port"], str):
        raise StyxValidationError(f'`serial_port` has the wrong type: Received `{type(params["serial_port"])}` expected `str`')
    if params.get("sock_num", None) is not None:
        if not isinstance(params["sock_num"], (float, int)):
            raise StyxValidationError(f'`sock_num` has the wrong type: Received `{type(params["sock_num"])}` expected `float`')
    if params.get("mp_max", None) is not None:
        if not isinstance(params["mp_max"], (float, int)):
            raise StyxValidationError(f'`mp_max` has the wrong type: Received `{type(params["mp_max"])}` expected `float`')
    if params.get("mp_min", None) is not None:
        if not isinstance(params["mp_min"], (float, int)):
            raise StyxValidationError(f'`mp_min` has the wrong type: Received `{type(params["mp_min"])}` expected `float`')
    if params.get("num_extra", None) is not None:
        if not isinstance(params["num_extra"], (float, int)):
            raise StyxValidationError(f'`num_extra` has the wrong type: Received `{type(params["num_extra"])}` expected `float`')
    if params.get("disp_all", None) is not None:
        if not isinstance(params["disp_all"], (float, int)):
            raise StyxValidationError(f'`disp_all` has the wrong type: Received `{type(params["disp_all"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], (float, int)):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `float`')
    if params.get("show_times", None) is not None:
        if not isinstance(params["show_times"], bool):
            raise StyxValidationError(f'`show_times` has the wrong type: Received `{type(params["show_times"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("hist", None) is not None:
        if not isinstance(params["hist"], bool):
            raise StyxValidationError(f'`hist` has the wrong type: Received `{type(params["hist"])}` expected `bool`')
    if params.get("no_serial", None) is not None:
        if not isinstance(params["no_serial"], bool):
            raise StyxValidationError(f'`no_serial` has the wrong type: Received `{type(params["no_serial"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')

def serial_helper_cargs(params: SerialHelper, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("serial_helper")
    cargs.append("-serial_port")
    cargs.append(params["serial_port"])
    v_0 = params.get("sock_num")
    if v_0 is not None:
        cargs.append("-sock_num")
        cargs.append(str(v_0))
    v_1 = params.get("mp_max")
    if v_1 is not None:
        cargs.append("-mp_max")
        cargs.append(str(v_1))
    v_2 = params.get("mp_min")
    if v_2 is not None:
        cargs.append("-mp_min")
        cargs.append(str(v_2))
    v_3 = params.get("num_extra")
    if v_3 is not None:
        cargs.append("-num_extra")
        cargs.append(str(v_3))
    v_4 = params.get("disp_all")
    if v_4 is not None:
        cargs.append("-disp_all")
        cargs.append(str(v_4))
    v_5 = params.get("debug")
    if v_5 is not None:
        cargs.append("-debug")
        cargs.append(str(v_5))
    if params.get("show_times"):
        cargs.append("-show_times")
    if params.get("help"):
        cargs.append("-help")
    if params.get("hist"):
        cargs.append("-hist")
    if params.get("no_serial"):
        cargs.append("-no_serial")
    if params.get("version"):
        cargs.append("-version")
    return cargs

def serial_helper_outputs(params: SerialHelper, execution: Execution) -> SerialHelperOutputs:
    """Build the SerialHelperOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SerialHelperOutputs(
        root=root_v,
    )

def serial_helper_execute(params: SerialHelper, runner: Runner | None = None) -> SerialHelperOutputs:
    """
    serial_helper

    Passes motion parameters from socket to serial port

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    serial_helper_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SERIAL_HELPER_METADATA)
    execution.params(params)
    args = serial_helper_cargs(params, execution)
    out = serial_helper_outputs(params, execution)
    execution.run(args)
    return out

def serial_helper(
    serial_port: str,
    sock_num: float | None = None,
    mp_max: float | None = None,
    mp_min: float | None = None,
    num_extra: float | None = None,
    disp_all: float | None = None,
    debug: float | None = None,
    show_times: bool = False,
    help: bool = False,
    hist: bool = False,
    no_serial: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> SerialHelperOutputs:
    """
    serial_helper

    Passes motion parameters from socket to serial port

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        serial_port: Output serial port filename
        sock_num: Specify socket number to serve
        mp_max: Limit the maximum value of the MP data
        mp_min: Limit the minimum value of the MP data
        num_extra: Receive additional floats per TR
        disp_all: Receive NVOX*8 extra floats per TR
        debug: Set the debugging level (0-3)
        show_times: Show communication times
        help: Display this help information
        hist: Show the module history
        no_serial: Turn off serial port output
        version: Show the current version number
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = serial_helper_params(
        serial_port=serial_port,
        sock_num=sock_num,
        mp_max=mp_max,
        mp_min=mp_min,
        num_extra=num_extra,
        disp_all=disp_all,
        debug=debug,
        show_times=show_times,
        help=help,
        hist=hist,
        no_serial=no_serial,
        version=version,
    )
    return serial_helper_execute(params, runner)

__all__ = [
    "SerialHelper",
    "SerialHelperOutputs",
    "SERIAL_HELPER_METADATA",
    "serial_helper_cargs",
    "serial_helper_outputs",
    "serial_helper_params",
    "serial_helper_execute",
    "serial_helper_validate",
    "serial_helper",
]