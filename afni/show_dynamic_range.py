# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SHOW_DYNAMIC_RANGE_METADATA = Metadata(
    id="@ShowDynamicRange",
    name="@ShowDynamicRange",
    package="afni",
)

ShowDynamicRange = typing.TypedDict(
    "ShowDynamicRange",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ShowDynamicRange"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class ShowDynamicRangeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    minpercchange_file: OutputPathType
    """Dataset showing the percent signal change that an increment of 1 digitized value in the time series corresponds to."""
    range_file: OutputPathType
    """Dataset showing the number of discrete levels used to represent the time series."""

def show_dynamic_range_params(
    infile: InputPathType,
) -> ShowDynamicRange:
    """
    Build parameters.

    Args:
        infile: Input EPI time series dataset (e.g. epi.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: ShowDynamicRange = {
        "@type": "afni/@ShowDynamicRange",
        "infile": infile,
    }
    return params

def show_dynamic_range_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ShowDynamicRange."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def show_dynamic_range_cargs(params: ShowDynamicRange, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ShowDynamicRange")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def show_dynamic_range_outputs(params: ShowDynamicRange, execution: Execution) -> ShowDynamicRangeOutputs:
    """Build the ShowDynamicRangeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    minpercchange_file_v: OutputPathType = execution.output_file(f'{params["infile"]}_minpercchange.nii.gz')
    range_file_v: OutputPathType = execution.output_file(f'{params["infile"]}.range.nii.gz')
    return ShowDynamicRangeOutputs(
        root=root_v,
        minpercchange_file=minpercchange_file_v,
        range_file=range_file_v,
    )

def show_dynamic_range_execute(params: ShowDynamicRange, runner: Runner | None = None) -> ShowDynamicRangeOutputs:
    """
    @ShowDynamicRange

    The script checks the dynamic range of the time series data at locations inside the brain.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    show_dynamic_range_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SHOW_DYNAMIC_RANGE_METADATA)
    execution.params(params)
    args = show_dynamic_range_cargs(params, execution)
    out = show_dynamic_range_outputs(params, execution)
    execution.run(args)
    return out

def show_dynamic_range(
    infile: InputPathType,
    runner: Runner | None = None,
) -> ShowDynamicRangeOutputs:
    """
    @ShowDynamicRange

    The script checks the dynamic range of the time series data at locations inside the brain.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input EPI time series dataset (e.g. epi.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = show_dynamic_range_params(
        infile=infile,
    )
    return show_dynamic_range_execute(params, runner)

__all__ = [
    "ShowDynamicRange",
    "ShowDynamicRangeOutputs",
    "SHOW_DYNAMIC_RANGE_METADATA",
    "show_dynamic_range_cargs",
    "show_dynamic_range_outputs",
    "show_dynamic_range_params",
    "show_dynamic_range_execute",
    "show_dynamic_range_validate",
    "show_dynamic_range",
]