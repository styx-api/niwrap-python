# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TOKENS_METADATA = Metadata(
    id="tokens",
    name="tokens",
    package="afni",
)

Tokens = typing.TypedDict(
    "Tokens",
    {
        "@type": typing.NotRequired[typing.Literal["afni/tokens"]],
        "infile": typing.NotRequired[InputPathType],
        "extra_char": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class TokensOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tokens_params(
    infile: InputPathType | None = None,
    extra_char: list[str] | None = None,
) -> Tokens:
    """
    Build parameters.

    Args:
        infile: Specify input file (stdin if none)
        extra_char: Specify extra character to count as valid. Can be used more\
            than once.

    Returns:
        Parameter dictionary.
    """
    params: Tokens = {
        "@type": "afni/tokens",
    }
    if infile is not None:
        params["infile"] = infile
    if extra_char is not None:
        params["extra_char"] = extra_char
    return params

def tokens_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tokens."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is not None:
        if not isinstance(params["infile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("extra_char", None) is not None:
        if not isinstance(params["extra_char"], list):
            raise StyxValidationError(f'`extra_char` has the wrong type: Received `{type(params["extra_char"])}` expected `list[str]`')
        for e in params["extra_char"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`extra_char` has the wrong type: Received `{type(e)}` expected `list[str]`')

def tokens_cargs(params: Tokens, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tokens")
    v_0 = params.get("infile")
    if v_0 is not None:
        cargs.append("-infile")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("extra_char")
    if v_1 is not None:
        cargs.append("-extra")
        for item0 in v_1:
            cargs.append(item0)
    return cargs

def tokens_outputs(params: Tokens, execution: Execution) -> TokensOutputs:
    """Build the TokensOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TokensOutputs(
        root=root_v,
    )

def tokens_execute(params: Tokens, runner: Runner | None = None) -> TokensOutputs:
    """
    tokens

    Token counting tool

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tokens_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TOKENS_METADATA)
    execution.params(params)
    args = tokens_cargs(params, execution)
    out = tokens_outputs(params, execution)
    execution.run(args)
    return out

def tokens(
    infile: InputPathType | None = None,
    extra_char: list[str] | None = None,
    runner: Runner | None = None,
) -> TokensOutputs:
    """
    tokens

    Token counting tool

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Specify input file (stdin if none)
        extra_char: Specify extra character to count as valid. Can be used more\
            than once.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tokens_params(
        infile=infile,
        extra_char=extra_char,
    )
    return tokens_execute(params, runner)

__all__ = [
    "Tokens",
    "TokensOutputs",
    "TOKENS_METADATA",
    "tokens_cargs",
    "tokens_outputs",
    "tokens_params",
    "tokens_execute",
    "tokens_validate",
    "tokens",
]