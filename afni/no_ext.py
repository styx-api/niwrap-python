# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NO_EXT_METADATA = Metadata(
    id="@NoExt",
    name="@NoExt",
    package="afni",
)

NoExt = typing.TypedDict(
    "NoExt",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@NoExt"]],
        "inputfile": str,
        "extensions": typing.NotRequired[list[str]],
    },
)

@dataclasses.dataclass
class NoExtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """File name with specified extensions removed"""

def no_ext_params(
    inputfile: str,
    extensions: list[str] | None = None,
) -> NoExt:
    """
    Build parameters.

    Args:
        inputfile: Input file name with extension
        extensions: Extensions to be removed

    Returns:
        Parameter dictionary.
    """
    params: NoExt = {
        "@type": "afni/@NoExt",
        "inputfile": inputfile,
    }
    if extensions is not None:
        params["extensions"] = extensions
    return params

def no_ext_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NoExt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inputfile", None) is None:
        raise StyxValidationError("`inputfile` must not be None")
    if not isinstance(params["inputfile"], str):
        raise StyxValidationError(f'`inputfile` has the wrong type: Received `{type(params["inputfile"])}` expected `str`')
    if params.get("extensions", None) is not None:
        if not isinstance(params["extensions"], list):
            raise StyxValidationError(f'`extensions` has the wrong type: Received `{type(params["extensions"])}` expected `list[str]`')
        for e in params["extensions"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`extensions` has the wrong type: Received `{type(e)}` expected `list[str]`')

def no_ext_cargs(params: NoExt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@NoExt")
    cargs.append(params["inputfile"])
    v_0 = params.get("extensions")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    return cargs

def no_ext_outputs(params: NoExt, execution: Execution) -> NoExtOutputs:
    """Build the NoExtOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file("output")
    return NoExtOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def no_ext_execute(params: NoExt, runner: Runner | None = None) -> NoExtOutputs:
    """
    @NoExt

    Tool for removing specified extensions from filenames

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    no_ext_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NO_EXT_METADATA)
    execution.params(params)
    args = no_ext_cargs(params, execution)
    out = no_ext_outputs(params, execution)
    execution.run(args)
    return out

def no_ext(
    inputfile: str,
    extensions: list[str] | None = None,
    runner: Runner | None = None,
) -> NoExtOutputs:
    """
    @NoExt

    Tool for removing specified extensions from filenames

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inputfile: Input file name with extension
        extensions: Extensions to be removed
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = no_ext_params(
        inputfile=inputfile,
        extensions=extensions,
    )
    return no_ext_execute(params, runner)

__all__ = [
    "NoExt",
    "NoExtOutputs",
    "NO_EXT_METADATA",
    "no_ext_cargs",
    "no_ext_outputs",
    "no_ext_params",
    "no_ext_execute",
    "no_ext_validate",
    "no_ext",
]