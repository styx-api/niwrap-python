# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_FRIEDMAN_METADATA = Metadata(
    id="3dFriedman",
    name="3dFriedman",
    package="afni",
)

V3dFriedman = typing.TypedDict(
    "V3dFriedman",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dFriedman"]],
        "levels": int,
        "datasets": list[InputPathType],
        "workmem": typing.NotRequired[int],
        "voxel_num": typing.NotRequired[int],
        "output_prefix": str,
    },
)

@dataclasses.dataclass
class V3dFriedmanOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_friedman_params(
    levels: int,
    datasets: list[InputPathType],
    output_prefix: str,
    workmem: int | None = None,
    voxel_num: int | None = None,
) -> V3dFriedman:
    """
    Build parameters.

    Args:
        levels: Number of treatments
        datasets: Data sets for each treatment
        output_prefix: Prefix for the output files
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel_num: Screen output for a specific voxel number

    Returns:
        Parameter dictionary.
    """
    params: V3dFriedman = {
        "@type": "afni/3dFriedman",
        "levels": levels,
        "datasets": datasets,
        "output_prefix": output_prefix,
    }
    if workmem is not None:
        params["workmem"] = workmem
    if voxel_num is not None:
        params["voxel_num"] = voxel_num
    return params

def v_3d_friedman_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dFriedman."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("levels", None) is None:
        raise StyxValidationError("`levels` must not be None")
    if not isinstance(params["levels"], int):
        raise StyxValidationError(f'`levels` has the wrong type: Received `{type(params["levels"])}` expected `int`')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("workmem", None) is not None:
        if not isinstance(params["workmem"], int):
            raise StyxValidationError(f'`workmem` has the wrong type: Received `{type(params["workmem"])}` expected `int`')
    if params.get("voxel_num", None) is not None:
        if not isinstance(params["voxel_num"], int):
            raise StyxValidationError(f'`voxel_num` has the wrong type: Received `{type(params["voxel_num"])}` expected `int`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def v_3d_friedman_cargs(params: V3dFriedman, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dFriedman")
    cargs.append(str(params["levels"]))
    cargs.append("-dset")
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("workmem")
    if v_0 is not None:
        cargs.append("-workmem")
        cargs.append(str(v_0))
    v_1 = params.get("voxel_num")
    if v_1 is not None:
        cargs.append("-voxel")
        cargs.append(str(v_1))
    cargs.append("-out")
    cargs.append(params["output_prefix"])
    return cargs

def v_3d_friedman_outputs(params: V3dFriedman, execution: Execution) -> V3dFriedmanOutputs:
    """Build the V3dFriedmanOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dFriedmanOutputs(
        root=root_v,
    )

def v_3d_friedman_execute(params: V3dFriedman, runner: Runner | None = None) -> V3dFriedmanOutputs:
    """
    3dFriedman

    Performs nonparametric Friedman test for randomized complete block design experiments

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_friedman_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_FRIEDMAN_METADATA)
    execution.params(params)
    args = v_3d_friedman_cargs(params, execution)
    out = v_3d_friedman_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_friedman(
    levels: int,
    datasets: list[InputPathType],
    output_prefix: str,
    workmem: int | None = None,
    voxel_num: int | None = None,
    runner: Runner | None = None,
) -> V3dFriedmanOutputs:
    """
    3dFriedman

    Performs nonparametric Friedman test for randomized complete block design experiments

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        levels: Number of treatments
        datasets: Data sets for each treatment
        output_prefix: Prefix for the output files
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel_num: Screen output for a specific voxel number
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_friedman_params(
        levels=levels,
        datasets=datasets,
        output_prefix=output_prefix,
        workmem=workmem,
        voxel_num=voxel_num,
    )
    return v_3d_friedman_execute(params, runner)

__all__ = [
    "V3dFriedman",
    "V3dFriedmanOutputs",
    "V_3D_FRIEDMAN_METADATA",
    "v_3d_friedman_cargs",
    "v_3d_friedman_outputs",
    "v_3d_friedman_params",
    "v_3d_friedman_execute",
    "v_3d_friedman_validate",
    "v_3d_friedman",
]