# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_PVAL_METADATA = Metadata(
    id="3dPval",
    name="3dPval",
    package="afni",
)

V3dPval = typing.TypedDict(
    "V3dPval",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dPval"]],
        "input_dataset": InputPathType,
        "zscore": typing.NotRequired[bool],
        "log2": typing.NotRequired[bool],
        "log10": typing.NotRequired[bool],
        "qval": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dPvalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output dataset with converted statistical values. Default output filename is 'Pval.nii.gz'."""

def v_3d_pval_params(
    input_dataset: InputPathType,
    zscore: bool = False,
    log2: bool = False,
    log10: bool = False,
    qval: bool = False,
    prefix: str | None = None,
) -> V3dPval:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (e.g., InputDataset.nii)
        zscore: Convert statistic to a z-score instead, an N(0,1) deviate that\
            represents the same p-value.
        log2: Convert statistic to -log2(p).
        log10: Convert statistic to -log10(p).
        qval: Convert statistic to a q-value (FDR) instead. This option only works\
            with datasets that have FDR curves inserted in their headers.
        prefix: Prefix name for output file (default name is 'Pval').

    Returns:
        Parameter dictionary.
    """
    params: V3dPval = {
        "@type": "afni/3dPval",
        "input_dataset": input_dataset,
        "zscore": zscore,
        "log2": log2,
        "log10": log10,
        "qval": qval,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_pval_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dPval."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("zscore", None) is not None:
        if not isinstance(params["zscore"], bool):
            raise StyxValidationError(f'`zscore` has the wrong type: Received `{type(params["zscore"])}` expected `bool`')
    if params.get("log2", None) is not None:
        if not isinstance(params["log2"], bool):
            raise StyxValidationError(f'`log2` has the wrong type: Received `{type(params["log2"])}` expected `bool`')
    if params.get("log10", None) is not None:
        if not isinstance(params["log10"], bool):
            raise StyxValidationError(f'`log10` has the wrong type: Received `{type(params["log10"])}` expected `bool`')
    if params.get("qval", None) is not None:
        if not isinstance(params["qval"], bool):
            raise StyxValidationError(f'`qval` has the wrong type: Received `{type(params["qval"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def v_3d_pval_cargs(params: V3dPval, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dPval")
    cargs.append(execution.input_file(params["input_dataset"]))
    if params.get("zscore"):
        cargs.append("-zscore")
    if params.get("log2"):
        cargs.append("-log2")
    if params.get("log10"):
        cargs.append("-log10")
    if params.get("qval"):
        cargs.append("-qval")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    return cargs

def v_3d_pval_outputs(params: V3dPval, execution: Execution) -> V3dPvalOutputs:
    """Build the V3dPvalOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return V3dPvalOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_pval_execute(params: V3dPval, runner: Runner | None = None) -> V3dPvalOutputs:
    """
    3dPval

    Convert a dataset's statistical sub-bricks to p-values or other statistical representations.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_pval_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_PVAL_METADATA)
    execution.params(params)
    args = v_3d_pval_cargs(params, execution)
    out = v_3d_pval_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_pval(
    input_dataset: InputPathType,
    zscore: bool = False,
    log2: bool = False,
    log10: bool = False,
    qval: bool = False,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dPvalOutputs:
    """
    3dPval

    Convert a dataset's statistical sub-bricks to p-values or other statistical representations.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (e.g., InputDataset.nii)
        zscore: Convert statistic to a z-score instead, an N(0,1) deviate that\
            represents the same p-value.
        log2: Convert statistic to -log2(p).
        log10: Convert statistic to -log10(p).
        qval: Convert statistic to a q-value (FDR) instead. This option only works\
            with datasets that have FDR curves inserted in their headers.
        prefix: Prefix name for output file (default name is 'Pval').
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_pval_params(
        input_dataset=input_dataset,
        zscore=zscore,
        log2=log2,
        log10=log10,
        qval=qval,
        prefix=prefix,
    )
    return v_3d_pval_execute(params, runner)

__all__ = [
    "V3dPval",
    "V3dPvalOutputs",
    "V_3D_PVAL_METADATA",
    "v_3d_pval_cargs",
    "v_3d_pval_outputs",
    "v_3d_pval_params",
    "v_3d_pval_execute",
    "v_3d_pval_validate",
    "v_3d_pval",
]