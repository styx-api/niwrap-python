# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DRENAME_METADATA = Metadata(
    id="3drename",
    name="3drename",
    package="afni",
)

V3drename = typing.TypedDict(
    "V3drename",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3drename"]],
        "old_prefix": str,
        "new_prefix": str,
    },
)

@dataclasses.dataclass
class V3drenameOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3drename_params(
    old_prefix: str,
    new_prefix: str,
) -> V3drename:
    """
    Build parameters.

    Args:
        old_prefix: Old prefix of the datasets to rename.
        new_prefix: New prefix for the datasets.

    Returns:
        Parameter dictionary.
    """
    params: V3drename = {
        "@type": "afni/3drename",
        "old_prefix": old_prefix,
        "new_prefix": new_prefix,
    }
    return params

def v_3drename_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3drename."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("old_prefix", None) is None:
        raise StyxValidationError("`old_prefix` must not be None")
    if not isinstance(params["old_prefix"], str):
        raise StyxValidationError(f'`old_prefix` has the wrong type: Received `{type(params["old_prefix"])}` expected `str`')
    if params.get("new_prefix", None) is None:
        raise StyxValidationError("`new_prefix` must not be None")
    if not isinstance(params["new_prefix"], str):
        raise StyxValidationError(f'`new_prefix` has the wrong type: Received `{type(params["new_prefix"])}` expected `str`')

def v_3drename_cargs(params: V3drename, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3drename")
    cargs.append(params["old_prefix"])
    cargs.append(params["new_prefix"])
    return cargs

def v_3drename_outputs(params: V3drename, execution: Execution) -> V3drenameOutputs:
    """Build the V3drenameOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3drenameOutputs(
        root=root_v,
    )

def v_3drename_execute(params: V3drename, runner: Runner | None = None) -> V3drenameOutputs:
    """
    3drename

    Tool to rename AFNI datasets by changing the dataset prefix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3drename_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DRENAME_METADATA)
    execution.params(params)
    args = v_3drename_cargs(params, execution)
    out = v_3drename_outputs(params, execution)
    execution.run(args)
    return out

def v_3drename(
    old_prefix: str,
    new_prefix: str,
    runner: Runner | None = None,
) -> V3drenameOutputs:
    """
    3drename

    Tool to rename AFNI datasets by changing the dataset prefix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        old_prefix: Old prefix of the datasets to rename.
        new_prefix: New prefix for the datasets.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3drename_params(
        old_prefix=old_prefix,
        new_prefix=new_prefix,
    )
    return v_3drename_execute(params, runner)

__all__ = [
    "V3drename",
    "V3drenameOutputs",
    "V_3DRENAME_METADATA",
    "v_3drename_cargs",
    "v_3drename_outputs",
    "v_3drename_params",
    "v_3drename_execute",
    "v_3drename_validate",
    "v_3drename",
]