# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_MODEL_PRF_METADATA = Metadata(
    id="get_afni_model_PRF",
    name="get_afni_model_PRF",
    package="afni",
)

GetAfniModelPrf = typing.TypedDict(
    "GetAfniModelPrf",
    {
        "@type": typing.NotRequired[typing.Literal["afni/get_afni_model_PRF"]],
        "amplitude": float,
        "x_coord": float,
        "y_coord": float,
        "sigma": float,
    },
)

@dataclasses.dataclass
class GetAfniModelPrfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def get_afni_model_prf_params(
    amplitude: float,
    x_coord: float,
    y_coord: float,
    sigma: float,
) -> GetAfniModelPrf:
    """
    Build parameters.

    Args:
        amplitude: Amplitude for the AFNI model
        x_coord: X-coordinate for the AFNI model
        y_coord: Y-coordinate for the AFNI model
        sigma: Sigma value for the AFNI model

    Returns:
        Parameter dictionary.
    """
    params: GetAfniModelPrf = {
        "@type": "afni/get_afni_model_PRF",
        "amplitude": amplitude,
        "x_coord": x_coord,
        "y_coord": y_coord,
        "sigma": sigma,
    }
    return params

def get_afni_model_prf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniModelPrf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("amplitude", None) is None:
        raise StyxValidationError("`amplitude` must not be None")
    if not isinstance(params["amplitude"], (float, int)):
        raise StyxValidationError(f'`amplitude` has the wrong type: Received `{type(params["amplitude"])}` expected `float`')
    if params.get("x_coord", None) is None:
        raise StyxValidationError("`x_coord` must not be None")
    if not isinstance(params["x_coord"], (float, int)):
        raise StyxValidationError(f'`x_coord` has the wrong type: Received `{type(params["x_coord"])}` expected `float`')
    if params.get("y_coord", None) is None:
        raise StyxValidationError("`y_coord` must not be None")
    if not isinstance(params["y_coord"], (float, int)):
        raise StyxValidationError(f'`y_coord` has the wrong type: Received `{type(params["y_coord"])}` expected `float`')
    if params.get("sigma", None) is None:
        raise StyxValidationError("`sigma` must not be None")
    if not isinstance(params["sigma"], (float, int)):
        raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')

def get_afni_model_prf_cargs(params: GetAfniModelPrf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("get_afni_model_PRF")
    cargs.append(str(params["amplitude"]))
    cargs.append(str(params["x_coord"]))
    cargs.append(str(params["y_coord"]))
    cargs.append(str(params["sigma"]))
    return cargs

def get_afni_model_prf_outputs(params: GetAfniModelPrf, execution: Execution) -> GetAfniModelPrfOutputs:
    """Build the GetAfniModelPrfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return GetAfniModelPrfOutputs(
        root=root_v,
    )

def get_afni_model_prf_execute(params: GetAfniModelPrf, runner: Runner | None = None) -> GetAfniModelPrfOutputs:
    """
    get_afni_model_PRF

    A tool to get AFNI model parameters assuming a PRF framework

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_model_prf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_MODEL_PRF_METADATA)
    execution.params(params)
    args = get_afni_model_prf_cargs(params, execution)
    out = get_afni_model_prf_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_model_prf(
    amplitude: float,
    x_coord: float,
    y_coord: float,
    sigma: float,
    runner: Runner | None = None,
) -> GetAfniModelPrfOutputs:
    """
    get_afni_model_PRF

    A tool to get AFNI model parameters assuming a PRF framework

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        amplitude: Amplitude for the AFNI model
        x_coord: X-coordinate for the AFNI model
        y_coord: Y-coordinate for the AFNI model
        sigma: Sigma value for the AFNI model
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_model_prf_params(
        amplitude=amplitude,
        x_coord=x_coord,
        y_coord=y_coord,
        sigma=sigma,
    )
    return get_afni_model_prf_execute(params, runner)

__all__ = [
    "GetAfniModelPrf",
    "GetAfniModelPrfOutputs",
    "GET_AFNI_MODEL_PRF_METADATA",
    "get_afni_model_prf_cargs",
    "get_afni_model_prf_outputs",
    "get_afni_model_prf_params",
    "get_afni_model_prf_execute",
    "get_afni_model_prf_validate",
    "get_afni_model_prf",
]