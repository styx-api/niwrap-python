# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHARM_RECO_METADATA = Metadata(
    id="SpharmReco",
    name="SpharmReco",
    package="afni",
)

SpharmReco = typing.TypedDict(
    "SpharmReco",
    {
        "@type": typing.NotRequired[typing.Literal["afni/SpharmReco"]],
        "input_surface": str,
        "decomposition_order": float,
        "bases_prefix": str,
        "coefficients": list[InputPathType],
        "output_prefix": typing.NotRequired[str],
        "output_surface": typing.NotRequired[list[str]],
        "debug": typing.NotRequired[float],
        "smoothing": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SpharmRecoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def spharm_reco_params(
    input_surface: str,
    decomposition_order: float,
    bases_prefix: str,
    coefficients: list[InputPathType],
    output_prefix: str | None = None,
    output_surface: list[str] | None = None,
    debug: float | None = None,
    smoothing: float | None = None,
) -> SpharmReco:
    """
    Build parameters.

    Args:
        input_surface: Surface that provides the topology of the mesh (nodes'\
            connections). TYPE specifies the input surface type.
        decomposition_order: Decomposition order
        bases_prefix: Prefix of files containing the bases functions (spherical\
            harmonics). These files are generated with SpharmDeco.
        coefficients: Coefficients files used to recompose data columns. Multiple\
            coefficient files can be specified by repeating the option.
        output_prefix: Write out the reconstructed data into dataset PREFIX. The\
            output contains N columns; one for each COEF file.
        output_surface: Write out a new surface with reconstructed coordinates.\
            Requires N to be a multiple of 3.
        debug: Debug levels (1-3)
        smoothing: Smoothing parameter (0 .. 0.001) weighing the contribution of\
            higher order harmonics

    Returns:
        Parameter dictionary.
    """
    params: SpharmReco = {
        "@type": "afni/SpharmReco",
        "input_surface": input_surface,
        "decomposition_order": decomposition_order,
        "bases_prefix": bases_prefix,
        "coefficients": coefficients,
    }
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    if output_surface is not None:
        params["output_surface"] = output_surface
    if debug is not None:
        params["debug"] = debug
    if smoothing is not None:
        params["smoothing"] = smoothing
    return params

def spharm_reco_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpharmReco."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], str):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `str`')
    if params.get("decomposition_order", None) is None:
        raise StyxValidationError("`decomposition_order` must not be None")
    if not isinstance(params["decomposition_order"], (float, int)):
        raise StyxValidationError(f'`decomposition_order` has the wrong type: Received `{type(params["decomposition_order"])}` expected `float`')
    if params.get("bases_prefix", None) is None:
        raise StyxValidationError("`bases_prefix` must not be None")
    if not isinstance(params["bases_prefix"], str):
        raise StyxValidationError(f'`bases_prefix` has the wrong type: Received `{type(params["bases_prefix"])}` expected `str`')
    if params.get("coefficients", None) is None:
        raise StyxValidationError("`coefficients` must not be None")
    if not isinstance(params["coefficients"], list):
        raise StyxValidationError(f'`coefficients` has the wrong type: Received `{type(params["coefficients"])}` expected `list[InputPathType]`')
    if len(params["coefficients"]) < 1:
        raise StyxValidationError("Parameter `coefficients` must contain at least 1 element")
    for e in params["coefficients"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`coefficients` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("output_surface", None) is not None:
        if not isinstance(params["output_surface"], list):
            raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `list[str]`')
        for e_2 in params["output_surface"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], (float, int)):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `float`')
    if params.get("smoothing", None) is not None:
        if not isinstance(params["smoothing"], (float, int)):
            raise StyxValidationError(f'`smoothing` has the wrong type: Received `{type(params["smoothing"])}` expected `float`')
        if not (0 <= params["smoothing"] <= 0.001):
            raise StyxValidationError("Parameter `smoothing` must be between 0 and 0.001 (inclusive)")

def spharm_reco_cargs(params: SpharmReco, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SpharmReco")
    cargs.append("-i_TYPE")
    cargs.append(params["input_surface"])
    cargs.append("-l")
    cargs.append(str(params["decomposition_order"]))
    cargs.append("-bases_prefix")
    cargs.append(params["bases_prefix"])
    cargs.append("-coef")
    for item0 in params["coefficients"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("output_prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("output_surface")
    if v_1 is not None:
        cargs.append("-o_TYPE")
        for item1 in v_1:
            cargs.append(item1)
    v_2 = params.get("debug")
    if v_2 is not None:
        cargs.append("-debug")
        cargs.append(str(v_2))
    v_3 = params.get("smoothing")
    if v_3 is not None:
        cargs.append("-sigma")
        cargs.append(str(v_3))
    return cargs

def spharm_reco_outputs(params: SpharmReco, execution: Execution) -> SpharmRecoOutputs:
    """Build the SpharmRecoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SpharmRecoOutputs(
        root=root_v,
    )

def spharm_reco_execute(params: SpharmReco, runner: Runner | None = None) -> SpharmRecoOutputs:
    """
    SpharmReco

    Spherical Harmonics Reconstruction from a set of harmonics and their corresponding coefficients.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spharm_reco_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHARM_RECO_METADATA)
    execution.params(params)
    args = spharm_reco_cargs(params, execution)
    out = spharm_reco_outputs(params, execution)
    execution.run(args)
    return out

def spharm_reco(
    input_surface: str,
    decomposition_order: float,
    bases_prefix: str,
    coefficients: list[InputPathType],
    output_prefix: str | None = None,
    output_surface: list[str] | None = None,
    debug: float | None = None,
    smoothing: float | None = None,
    runner: Runner | None = None,
) -> SpharmRecoOutputs:
    """
    SpharmReco

    Spherical Harmonics Reconstruction from a set of harmonics and their corresponding coefficients.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_surface: Surface that provides the topology of the mesh (nodes'\
            connections). TYPE specifies the input surface type.
        decomposition_order: Decomposition order
        bases_prefix: Prefix of files containing the bases functions (spherical\
            harmonics). These files are generated with SpharmDeco.
        coefficients: Coefficients files used to recompose data columns. Multiple\
            coefficient files can be specified by repeating the option.
        output_prefix: Write out the reconstructed data into dataset PREFIX. The\
            output contains N columns; one for each COEF file.
        output_surface: Write out a new surface with reconstructed coordinates.\
            Requires N to be a multiple of 3.
        debug: Debug levels (1-3)
        smoothing: Smoothing parameter (0 .. 0.001) weighing the contribution of\
            higher order harmonics
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spharm_reco_params(
        input_surface=input_surface,
        decomposition_order=decomposition_order,
        bases_prefix=bases_prefix,
        coefficients=coefficients,
        output_prefix=output_prefix,
        output_surface=output_surface,
        debug=debug,
        smoothing=smoothing,
    )
    return spharm_reco_execute(params, runner)

__all__ = [
    "SpharmReco",
    "SpharmRecoOutputs",
    "SPHARM_RECO_METADATA",
    "spharm_reco_cargs",
    "spharm_reco_outputs",
    "spharm_reco_params",
    "spharm_reco_execute",
    "spharm_reco_validate",
    "spharm_reco",
]