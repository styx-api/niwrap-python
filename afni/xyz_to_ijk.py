# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

XYZ_TO_IJK_METADATA = Metadata(
    id="@xyz_to_ijk",
    name="@xyz_to_ijk",
    package="afni",
)

XyzToIjk = typing.TypedDict(
    "XyzToIjk",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@xyz_to_ijk"]],
        "inset": InputPathType,
        "x_coord": float,
        "y_coord": float,
        "z_coord": float,
        "prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class XyzToIjkOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output file containing the translated indices"""

def xyz_to_ijk_params(
    inset: InputPathType,
    x_coord: float,
    y_coord: float,
    z_coord: float,
    prefix: str | None = None,
) -> XyzToIjk:
    """
    Build parameters.

    Args:
        inset: Volumetric file name (e.g. FILE.nii.gz)
        x_coord: Three coordinates (in units of the dataset, like mm)
        y_coord: Three coordinates (in units of the dataset, like mm)
        z_coord: Three coordinates (in units of the dataset, like mm)
        prefix: File name (including path) to output the three indices

    Returns:
        Parameter dictionary.
    """
    params: XyzToIjk = {
        "@type": "afni/@xyz_to_ijk",
        "inset": inset,
        "x_coord": x_coord,
        "y_coord": y_coord,
        "z_coord": z_coord,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def xyz_to_ijk_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid XyzToIjk."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("x_coord", None) is None:
        raise StyxValidationError("`x_coord` must not be None")
    if not isinstance(params["x_coord"], (float, int)):
        raise StyxValidationError(f'`x_coord` has the wrong type: Received `{type(params["x_coord"])}` expected `float`')
    if params.get("y_coord", None) is None:
        raise StyxValidationError("`y_coord` must not be None")
    if not isinstance(params["y_coord"], (float, int)):
        raise StyxValidationError(f'`y_coord` has the wrong type: Received `{type(params["y_coord"])}` expected `float`')
    if params.get("z_coord", None) is None:
        raise StyxValidationError("`z_coord` must not be None")
    if not isinstance(params["z_coord"], (float, int)):
        raise StyxValidationError(f'`z_coord` has the wrong type: Received `{type(params["z_coord"])}` expected `float`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def xyz_to_ijk_cargs(params: XyzToIjk, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@xyz_to_ijk")
    cargs.append("-inset")
    cargs.append(execution.input_file(params["inset"]))
    cargs.append("-xyz")
    cargs.append(str(params["x_coord"]))
    cargs.append(str(params["y_coord"]))
    cargs.append(str(params["z_coord"]))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    return cargs

def xyz_to_ijk_outputs(params: XyzToIjk, execution: Execution) -> XyzToIjkOutputs:
    """Build the XyzToIjkOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return XyzToIjkOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def xyz_to_ijk_execute(params: XyzToIjk, runner: Runner | None = None) -> XyzToIjkOutputs:
    """
    @xyz_to_ijk

    Helper script to convert (x, y, z) coordinates to (i, j, k) indices for a volumetric dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    xyz_to_ijk_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(XYZ_TO_IJK_METADATA)
    execution.params(params)
    args = xyz_to_ijk_cargs(params, execution)
    out = xyz_to_ijk_outputs(params, execution)
    execution.run(args)
    return out

def xyz_to_ijk(
    inset: InputPathType,
    x_coord: float,
    y_coord: float,
    z_coord: float,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> XyzToIjkOutputs:
    """
    @xyz_to_ijk

    Helper script to convert (x, y, z) coordinates to (i, j, k) indices for a volumetric dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        inset: Volumetric file name (e.g. FILE.nii.gz)
        x_coord: Three coordinates (in units of the dataset, like mm)
        y_coord: Three coordinates (in units of the dataset, like mm)
        z_coord: Three coordinates (in units of the dataset, like mm)
        prefix: File name (including path) to output the three indices
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = xyz_to_ijk_params(
        inset=inset,
        x_coord=x_coord,
        y_coord=y_coord,
        z_coord=z_coord,
        prefix=prefix,
    )
    return xyz_to_ijk_execute(params, runner)

__all__ = [
    "XyzToIjk",
    "XyzToIjkOutputs",
    "XYZ_TO_IJK_METADATA",
    "xyz_to_ijk_cargs",
    "xyz_to_ijk_outputs",
    "xyz_to_ijk_params",
    "xyz_to_ijk_execute",
    "xyz_to_ijk_validate",
    "xyz_to_ijk",
]