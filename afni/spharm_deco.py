# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHARM_DECO_METADATA = Metadata(
    id="SpharmDeco",
    name="SpharmDeco",
    package="afni",
)

SpharmDeco = typing.TypedDict(
    "SpharmDeco",
    {
        "@type": typing.NotRequired[typing.Literal["afni/SpharmDeco"]],
        "i_type_s": InputPathType,
        "unit_sph_label": str,
        "order_l": float,
        "i_type_sd": typing.NotRequired[list[InputPathType]],
        "data_d": typing.NotRequired[InputPathType],
        "bases_prefix": typing.NotRequired[str],
        "prefix": typing.NotRequired[str],
        "o_type_sdr": typing.NotRequired[list[InputPathType]],
        "debug": typing.NotRequired[float],
        "sigma": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class SpharmDecoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reconstructed_data: OutputPathType | None
    """Reconstructed data or surface files named based on PREFIX."""

def spharm_deco_params(
    i_type_s: InputPathType,
    unit_sph_label: str,
    order_l: float,
    i_type_sd: list[InputPathType] | None = None,
    data_d: InputPathType | None = None,
    bases_prefix: str | None = None,
    prefix: str | None = None,
    o_type_sdr: list[InputPathType] | None = None,
    debug: float | None = None,
    sigma: float | None = None,
) -> SpharmDeco:
    """
    Build parameters.

    Args:
        i_type_s: Unit sphere, isotopic to the surface domain over which the data\
            to be decomposed is defined.
        unit_sph_label: Label of the unit sphere.
        order_l: Decomposition order.
        i_type_sd: A surface whose node coordinates provide data vectors (X, Y, Z)\
            to be decomposed or a dataset whose columns are to be individually\
            decomposed. You can specify multiple surfaces to be processed.
        data_d: Data vectors to be decomposed.
        bases_prefix: Save the basis functions under the prefix BASES_PREFIX.
        prefix: Write out the reconstructed data into dataset PREFIX and write the\
            beta coefficients for each processed data column.
        o_type_sdr: Write out a new surface with reconstructed coordinates.
        debug: Debug levels (1-3)
        sigma: Smoothing parameter (0 .. 0.001) which weighs down the contribution\
            of higher order harmonics.

    Returns:
        Parameter dictionary.
    """
    params: SpharmDeco = {
        "@type": "afni/SpharmDeco",
        "i_type_s": i_type_s,
        "unit_sph_label": unit_sph_label,
        "order_l": order_l,
    }
    if i_type_sd is not None:
        params["i_type_sd"] = i_type_sd
    if data_d is not None:
        params["data_d"] = data_d
    if bases_prefix is not None:
        params["bases_prefix"] = bases_prefix
    if prefix is not None:
        params["prefix"] = prefix
    if o_type_sdr is not None:
        params["o_type_sdr"] = o_type_sdr
    if debug is not None:
        params["debug"] = debug
    if sigma is not None:
        params["sigma"] = sigma
    return params

def spharm_deco_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpharmDeco."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("i_type_s", None) is None:
        raise StyxValidationError("`i_type_s` must not be None")
    if not isinstance(params["i_type_s"], (pathlib.Path, str)):
        raise StyxValidationError(f'`i_type_s` has the wrong type: Received `{type(params["i_type_s"])}` expected `InputPathType`')
    if params.get("unit_sph_label", None) is None:
        raise StyxValidationError("`unit_sph_label` must not be None")
    if not isinstance(params["unit_sph_label"], str):
        raise StyxValidationError(f'`unit_sph_label` has the wrong type: Received `{type(params["unit_sph_label"])}` expected `str`')
    if params.get("order_l", None) is None:
        raise StyxValidationError("`order_l` must not be None")
    if not isinstance(params["order_l"], (float, int)):
        raise StyxValidationError(f'`order_l` has the wrong type: Received `{type(params["order_l"])}` expected `float`')
    if params.get("i_type_sd", None) is not None:
        if not isinstance(params["i_type_sd"], list):
            raise StyxValidationError(f'`i_type_sd` has the wrong type: Received `{type(params["i_type_sd"])}` expected `list[InputPathType]`')
        for e in params["i_type_sd"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`i_type_sd` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("data_d", None) is not None:
        if not isinstance(params["data_d"], (pathlib.Path, str)):
            raise StyxValidationError(f'`data_d` has the wrong type: Received `{type(params["data_d"])}` expected `InputPathType`')
    if params.get("bases_prefix", None) is not None:
        if not isinstance(params["bases_prefix"], str):
            raise StyxValidationError(f'`bases_prefix` has the wrong type: Received `{type(params["bases_prefix"])}` expected `str`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("o_type_sdr", None) is not None:
        if not isinstance(params["o_type_sdr"], list):
            raise StyxValidationError(f'`o_type_sdr` has the wrong type: Received `{type(params["o_type_sdr"])}` expected `list[InputPathType]`')
        for e_2 in params["o_type_sdr"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`o_type_sdr` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], (float, int)):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `float`')
    if params.get("sigma", None) is not None:
        if not isinstance(params["sigma"], (float, int)):
            raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')

def spharm_deco_cargs(params: SpharmDeco, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SpharmDeco")
    cargs.append(execution.input_file(params["i_type_s"]))
    cargs.append(params["unit_sph_label"])
    cargs.append(str(params["order_l"]))
    v_0 = params.get("i_type_sd")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(execution.input_file(item0))
    v_1 = params.get("data_d")
    if v_1 is not None:
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("bases_prefix")
    if v_2 is not None:
        cargs.append(v_2)
    v_3 = params.get("prefix")
    if v_3 is not None:
        cargs.append(v_3)
    v_4 = params.get("o_type_sdr")
    if v_4 is not None:
        for item1 in v_4:
            cargs.append(execution.input_file(item1))
    v_5 = params.get("debug")
    if v_5 is not None:
        cargs.append("-debug")
        cargs.append(str(v_5))
    v_6 = params.get("sigma")
    if v_6 is not None:
        cargs.append("-sigma")
        cargs.append(str(v_6))
    return cargs

def spharm_deco_outputs(params: SpharmDeco, execution: Execution) -> SpharmDecoOutputs:
    """Build the SpharmDecoOutputs object for this tool."""
    reconstructed_data_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        reconstructed_data_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_reconstructed')
    return SpharmDecoOutputs(
        root=root_v,
        reconstructed_data=reconstructed_data_v,
    )

def spharm_deco_execute(params: SpharmDeco, runner: Runner | None = None) -> SpharmDecoOutputs:
    """
    SpharmDeco

    Spherical Harmonics Decomposition of a surface's coordinates or data

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spharm_deco_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHARM_DECO_METADATA)
    execution.params(params)
    args = spharm_deco_cargs(params, execution)
    out = spharm_deco_outputs(params, execution)
    execution.run(args)
    return out

def spharm_deco(
    i_type_s: InputPathType,
    unit_sph_label: str,
    order_l: float,
    i_type_sd: list[InputPathType] | None = None,
    data_d: InputPathType | None = None,
    bases_prefix: str | None = None,
    prefix: str | None = None,
    o_type_sdr: list[InputPathType] | None = None,
    debug: float | None = None,
    sigma: float | None = None,
    runner: Runner | None = None,
) -> SpharmDecoOutputs:
    """
    SpharmDeco

    Spherical Harmonics Decomposition of a surface's coordinates or data

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        i_type_s: Unit sphere, isotopic to the surface domain over which the data\
            to be decomposed is defined.
        unit_sph_label: Label of the unit sphere.
        order_l: Decomposition order.
        i_type_sd: A surface whose node coordinates provide data vectors (X, Y, Z)\
            to be decomposed or a dataset whose columns are to be individually\
            decomposed. You can specify multiple surfaces to be processed.
        data_d: Data vectors to be decomposed.
        bases_prefix: Save the basis functions under the prefix BASES_PREFIX.
        prefix: Write out the reconstructed data into dataset PREFIX and write the\
            beta coefficients for each processed data column.
        o_type_sdr: Write out a new surface with reconstructed coordinates.
        debug: Debug levels (1-3)
        sigma: Smoothing parameter (0 .. 0.001) which weighs down the contribution\
            of higher order harmonics.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spharm_deco_params(
        i_type_s=i_type_s,
        unit_sph_label=unit_sph_label,
        order_l=order_l,
        i_type_sd=i_type_sd,
        data_d=data_d,
        bases_prefix=bases_prefix,
        prefix=prefix,
        o_type_sdr=o_type_sdr,
        debug=debug,
        sigma=sigma,
    )
    return spharm_deco_execute(params, runner)

__all__ = [
    "SpharmDeco",
    "SpharmDecoOutputs",
    "SPHARM_DECO_METADATA",
    "spharm_deco_cargs",
    "spharm_deco_outputs",
    "spharm_deco_params",
    "spharm_deco_execute",
    "spharm_deco_validate",
    "spharm_deco",
]