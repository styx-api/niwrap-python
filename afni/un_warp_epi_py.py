# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UN_WARP_EPI_PY_METADATA = Metadata(
    id="unWarpEPI.py",
    name="unWarpEPI.py",
    package="afni",
)

UnWarpEpiPy = typing.TypedDict(
    "UnWarpEpiPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/unWarpEPI.py"]],
        "forward": InputPathType,
        "reverse": InputPathType,
        "anat4warp": InputPathType,
        "data": str,
        "subjID": str,
        "giant_move": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class UnWarpEpiPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def un_warp_epi_py_params(
    forward: InputPathType,
    reverse: InputPathType,
    anat4warp: InputPathType,
    data: str,
    subjID: str,
    giant_move: bool = False,
) -> UnWarpEpiPy:
    """
    Build parameters.

    Args:
        forward: Calibration matching data to be corrected
        reverse: Calibration with opposing polarity to data to be corrected
        anat4warp: Reference anatomical dataset
        data: Data to be corrected (same polarity as forward calibration data).\
            Separate with commas if specifying multiple datasets.
        subjID: ID of subject to be corrected
        giant_move: Set giant_move option for align_epi_anat if final align of\
            anatomy to corrected EPI fails if datasets are far apart in space.

    Returns:
        Parameter dictionary.
    """
    params: UnWarpEpiPy = {
        "@type": "afni/unWarpEPI.py",
        "forward": forward,
        "reverse": reverse,
        "anat4warp": anat4warp,
        "data": data,
        "subjID": subjID,
        "giant_move": giant_move,
    }
    return params

def un_warp_epi_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UnWarpEpiPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("forward", None) is None:
        raise StyxValidationError("`forward` must not be None")
    if not isinstance(params["forward"], (pathlib.Path, str)):
        raise StyxValidationError(f'`forward` has the wrong type: Received `{type(params["forward"])}` expected `InputPathType`')
    if params.get("reverse", None) is None:
        raise StyxValidationError("`reverse` must not be None")
    if not isinstance(params["reverse"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reverse` has the wrong type: Received `{type(params["reverse"])}` expected `InputPathType`')
    if params.get("anat4warp", None) is None:
        raise StyxValidationError("`anat4warp` must not be None")
    if not isinstance(params["anat4warp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`anat4warp` has the wrong type: Received `{type(params["anat4warp"])}` expected `InputPathType`')
    if params.get("data", None) is None:
        raise StyxValidationError("`data` must not be None")
    if not isinstance(params["data"], str):
        raise StyxValidationError(f'`data` has the wrong type: Received `{type(params["data"])}` expected `str`')
    if params.get("subjID", None) is None:
        raise StyxValidationError("`subjID` must not be None")
    if not isinstance(params["subjID"], str):
        raise StyxValidationError(f'`subjID` has the wrong type: Received `{type(params["subjID"])}` expected `str`')
    if params.get("giant_move", None) is not None:
        if not isinstance(params["giant_move"], bool):
            raise StyxValidationError(f'`giant_move` has the wrong type: Received `{type(params["giant_move"])}` expected `bool`')

def un_warp_epi_py_cargs(params: UnWarpEpiPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unWarpEPI.py")
    cargs.append("-f")
    cargs.append(execution.input_file(params["forward"]))
    cargs.append("-r")
    cargs.append(execution.input_file(params["reverse"]))
    cargs.append("-a")
    cargs.append(execution.input_file(params["anat4warp"]))
    cargs.append("-d")
    cargs.append(params["data"])
    cargs.append("-s")
    cargs.append(params["subjID"])
    if params.get("giant_move"):
        cargs.append("-g")
    return cargs

def un_warp_epi_py_outputs(params: UnWarpEpiPy, execution: Execution) -> UnWarpEpiPyOutputs:
    """Build the UnWarpEpiPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return UnWarpEpiPyOutputs(
        root=root_v,
    )

def un_warp_epi_py_execute(params: UnWarpEpiPy, runner: Runner | None = None) -> UnWarpEpiPyOutputs:
    """
    unWarpEPI.py

    Routine to unwarp EPI data set using another data set with opposite polarity

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    un_warp_epi_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UN_WARP_EPI_PY_METADATA)
    execution.params(params)
    args = un_warp_epi_py_cargs(params, execution)
    out = un_warp_epi_py_outputs(params, execution)
    execution.run(args)
    return out

def un_warp_epi_py(
    forward: InputPathType,
    reverse: InputPathType,
    anat4warp: InputPathType,
    data: str,
    subjID: str,
    giant_move: bool = False,
    runner: Runner | None = None,
) -> UnWarpEpiPyOutputs:
    """
    unWarpEPI.py

    Routine to unwarp EPI data set using another data set with opposite polarity

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        forward: Calibration matching data to be corrected
        reverse: Calibration with opposing polarity to data to be corrected
        anat4warp: Reference anatomical dataset
        data: Data to be corrected (same polarity as forward calibration data).\
            Separate with commas if specifying multiple datasets.
        subjID: ID of subject to be corrected
        giant_move: Set giant_move option for align_epi_anat if final align of\
            anatomy to corrected EPI fails if datasets are far apart in space.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = un_warp_epi_py_params(
        forward=forward,
        reverse=reverse,
        anat4warp=anat4warp,
        data=data,
        subjID=subjID,
        giant_move=giant_move,
    )
    return un_warp_epi_py_execute(params, runner)

__all__ = [
    "UnWarpEpiPy",
    "UnWarpEpiPyOutputs",
    "UN_WARP_EPI_PY_METADATA",
    "un_warp_epi_py_cargs",
    "un_warp_epi_py_outputs",
    "un_warp_epi_py_params",
    "un_warp_epi_py_execute",
    "un_warp_epi_py_validate",
    "un_warp_epi_py",
]