# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RENAME_PANGA_METADATA = Metadata(
    id="@RenamePanga",
    name="@RenamePanga",
    package="afni",
)

RenamePanga = typing.TypedDict(
    "RenamePanga",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@RenamePanga"]],
        "dir_number": str,
        "first_image_number": str,
        "num_slices": float,
        "num_reps": float,
        "output_root": str,
        "keep_prefix": typing.NotRequired[bool],
        "interactive": typing.NotRequired[bool],
        "outliers_check": typing.NotRequired[bool],
        "slice_pattern": typing.NotRequired[str],
        "output_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class RenamePangaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    head_file: OutputPathType | None
    """Main output file (HEAD)"""
    brik_file: OutputPathType | None
    """Main output file (BRIK)"""
    log_file: OutputPathType | None
    """Log file created in the current directory"""

def rename_panga_params(
    dir_number: str,
    first_image_number: str,
    num_slices: float,
    num_reps: float,
    output_root: str,
    keep_prefix: bool = False,
    interactive: bool = False,
    outliers_check: bool = False,
    slice_pattern: str | None = None,
    output_directory: str | None = None,
) -> RenamePanga:
    """
    Build parameters.

    Args:
        dir_number: The directory number where the first image of the series is\
            stored
        first_image_number: The number of the first image in the series
        num_slices: The number of slices making up the imaged volume
        num_reps: The number of samples in your time series
        output_root: The prefix for the output brick
        keep_prefix: Forces @RenamePanga to use the prefix you designate without\
            modification
        interactive: Launches to3d in interactive mode. This allows you to double\
            check the automated settings.
        outliers_check: Performs outliers check and writes the outliers to a .1D\
            file placed in the output directory
        slice_pattern: Sets the slice acquisition pattern. The default option is\
            alt+z.
        output_directory: Directory where the output (bricks and 1D files) will be\
            stored. The default directory is ./afni

    Returns:
        Parameter dictionary.
    """
    params: RenamePanga = {
        "@type": "afni/@RenamePanga",
        "dir_number": dir_number,
        "first_image_number": first_image_number,
        "num_slices": num_slices,
        "num_reps": num_reps,
        "output_root": output_root,
        "keep_prefix": keep_prefix,
        "interactive": interactive,
        "outliers_check": outliers_check,
    }
    if slice_pattern is not None:
        params["slice_pattern"] = slice_pattern
    if output_directory is not None:
        params["output_directory"] = output_directory
    return params

def rename_panga_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RenamePanga."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dir_number", None) is None:
        raise StyxValidationError("`dir_number` must not be None")
    if not isinstance(params["dir_number"], str):
        raise StyxValidationError(f'`dir_number` has the wrong type: Received `{type(params["dir_number"])}` expected `str`')
    if params.get("first_image_number", None) is None:
        raise StyxValidationError("`first_image_number` must not be None")
    if not isinstance(params["first_image_number"], str):
        raise StyxValidationError(f'`first_image_number` has the wrong type: Received `{type(params["first_image_number"])}` expected `str`')
    if params.get("num_slices", None) is None:
        raise StyxValidationError("`num_slices` must not be None")
    if not isinstance(params["num_slices"], (float, int)):
        raise StyxValidationError(f'`num_slices` has the wrong type: Received `{type(params["num_slices"])}` expected `float`')
    if params.get("num_reps", None) is None:
        raise StyxValidationError("`num_reps` must not be None")
    if not isinstance(params["num_reps"], (float, int)):
        raise StyxValidationError(f'`num_reps` has the wrong type: Received `{type(params["num_reps"])}` expected `float`')
    if params.get("output_root", None) is None:
        raise StyxValidationError("`output_root` must not be None")
    if not isinstance(params["output_root"], str):
        raise StyxValidationError(f'`output_root` has the wrong type: Received `{type(params["output_root"])}` expected `str`')
    if params.get("keep_prefix", None) is not None:
        if not isinstance(params["keep_prefix"], bool):
            raise StyxValidationError(f'`keep_prefix` has the wrong type: Received `{type(params["keep_prefix"])}` expected `bool`')
    if params.get("interactive", None) is not None:
        if not isinstance(params["interactive"], bool):
            raise StyxValidationError(f'`interactive` has the wrong type: Received `{type(params["interactive"])}` expected `bool`')
    if params.get("outliers_check", None) is not None:
        if not isinstance(params["outliers_check"], bool):
            raise StyxValidationError(f'`outliers_check` has the wrong type: Received `{type(params["outliers_check"])}` expected `bool`')
    if params.get("slice_pattern", None) is not None:
        if not isinstance(params["slice_pattern"], str):
            raise StyxValidationError(f'`slice_pattern` has the wrong type: Received `{type(params["slice_pattern"])}` expected `str`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def rename_panga_cargs(params: RenamePanga, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@RenamePanga")
    cargs.append(params["dir_number"])
    cargs.append(params["first_image_number"])
    cargs.append(str(params["num_slices"]))
    cargs.append(str(params["num_reps"]))
    cargs.append(params["output_root"])
    if params.get("keep_prefix"):
        cargs.append("-kp")
    if params.get("interactive"):
        cargs.append("-i")
    if params.get("outliers_check"):
        cargs.append("-oc")
    v_0 = params.get("slice_pattern")
    if v_0 is not None:
        cargs.append("-sp")
        cargs.append(v_0)
    v_1 = params.get("output_directory")
    if v_1 is not None:
        cargs.append("-od")
        cargs.append(v_1)
    return cargs

def rename_panga_outputs(params: RenamePanga, execution: Execution) -> RenamePangaOutputs:
    """Build the RenamePangaOutputs object for this tool."""
    head_file_v: OutputPathType | None = None
    brik_file_v: OutputPathType | None = None
    log_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_directory")
    if __v0 is not None:
        head_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/{params["output_root"]}_r#.HEAD')
    __v1 = params.get("output_directory")
    if __v1 is not None:
        brik_file_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/{params["output_root"]}_r#.BRIK')
    __v2 = params.get("output_directory")
    if __v2 is not None:
        log_file_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}/MAPLOG_Panga')
    return RenamePangaOutputs(
        root=root_v,
        head_file=head_file_v,
        brik_file=brik_file_v,
        log_file=log_file_v,
    )

def rename_panga_execute(params: RenamePanga, runner: Runner | None = None) -> RenamePangaOutputs:
    """
    @RenamePanga

    Creates AFNI bricks from RealTime GE EPI series.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rename_panga_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RENAME_PANGA_METADATA)
    execution.params(params)
    args = rename_panga_cargs(params, execution)
    out = rename_panga_outputs(params, execution)
    execution.run(args)
    return out

def rename_panga(
    dir_number: str,
    first_image_number: str,
    num_slices: float,
    num_reps: float,
    output_root: str,
    keep_prefix: bool = False,
    interactive: bool = False,
    outliers_check: bool = False,
    slice_pattern: str | None = None,
    output_directory: str | None = None,
    runner: Runner | None = None,
) -> RenamePangaOutputs:
    """
    @RenamePanga

    Creates AFNI bricks from RealTime GE EPI series.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dir_number: The directory number where the first image of the series is\
            stored
        first_image_number: The number of the first image in the series
        num_slices: The number of slices making up the imaged volume
        num_reps: The number of samples in your time series
        output_root: The prefix for the output brick
        keep_prefix: Forces @RenamePanga to use the prefix you designate without\
            modification
        interactive: Launches to3d in interactive mode. This allows you to double\
            check the automated settings.
        outliers_check: Performs outliers check and writes the outliers to a .1D\
            file placed in the output directory
        slice_pattern: Sets the slice acquisition pattern. The default option is\
            alt+z.
        output_directory: Directory where the output (bricks and 1D files) will be\
            stored. The default directory is ./afni
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rename_panga_params(
        dir_number=dir_number,
        first_image_number=first_image_number,
        num_slices=num_slices,
        num_reps=num_reps,
        output_root=output_root,
        keep_prefix=keep_prefix,
        interactive=interactive,
        outliers_check=outliers_check,
        slice_pattern=slice_pattern,
        output_directory=output_directory,
    )
    return rename_panga_execute(params, runner)

__all__ = [
    "RenamePanga",
    "RenamePangaOutputs",
    "RENAME_PANGA_METADATA",
    "rename_panga_cargs",
    "rename_panga_outputs",
    "rename_panga_params",
    "rename_panga_execute",
    "rename_panga_validate",
    "rename_panga",
]