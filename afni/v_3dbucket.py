# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DBUCKET_METADATA = Metadata(
    id="3dbucket",
    name="3dbucket",
    package="afni",
)

V3dbucket = typing.TypedDict(
    "V3dbucket",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dbucket"]],
        "prefix": typing.NotRequired[str],
        "output": typing.NotRequired[str],
        "session": typing.NotRequired[str],
        "glueto": typing.NotRequired[str],
        "aglueto": typing.NotRequired[str],
        "dry": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "fbuc": typing.NotRequired[bool],
        "abuc": typing.NotRequired[bool],
        "input_files": list[str],
    },
)

@dataclasses.dataclass
class V3dbucketOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3dbucket_params(
    input_files: list[str],
    prefix: str | None = None,
    output: str | None = None,
    session: str | None = None,
    glueto: str | None = None,
    aglueto: str | None = None,
    dry: bool = False,
    verbose: bool = False,
    fbuc: bool = False,
    abuc: bool = False,
) -> V3dbucket:
    """
    Build parameters.

    Args:
        input_files: Input datasets with optional sub-brick selection.
        prefix: Use 'pname' for the output dataset prefix name.
        output: Use 'pname' for the output dataset prefix name.
        session: Use 'dir' for the output dataset session directory.\
            [default='./'=current working directory]
        glueto: Append bricks to the end of the 'fname' dataset.
        aglueto: If fname dataset does not exist, create it (like -prefix).\
            Otherwise append to fname (like -glueto).
        dry: Execute a 'dry run'; only print out what would be done.
        verbose: Print out some verbose output as the program proceeds.
        fbuc: Create a functional bucket.
        abuc: Create an anatomical bucket. If neither of these options is given,\
            the output type is determined from the first input type.

    Returns:
        Parameter dictionary.
    """
    params: V3dbucket = {
        "@type": "afni/3dbucket",
        "input_files": input_files,
        "dry": dry,
        "verbose": verbose,
        "fbuc": fbuc,
        "abuc": abuc,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if output is not None:
        params["output"] = output
    if session is not None:
        params["session"] = session
    if glueto is not None:
        params["glueto"] = glueto
    if aglueto is not None:
        params["aglueto"] = aglueto
    return params

def v_3dbucket_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dbucket."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("session", None) is not None:
        if not isinstance(params["session"], str):
            raise StyxValidationError(f'`session` has the wrong type: Received `{type(params["session"])}` expected `str`')
    if params.get("glueto", None) is not None:
        if not isinstance(params["glueto"], str):
            raise StyxValidationError(f'`glueto` has the wrong type: Received `{type(params["glueto"])}` expected `str`')
    if params.get("aglueto", None) is not None:
        if not isinstance(params["aglueto"], str):
            raise StyxValidationError(f'`aglueto` has the wrong type: Received `{type(params["aglueto"])}` expected `str`')
    if params.get("dry", None) is not None:
        if not isinstance(params["dry"], bool):
            raise StyxValidationError(f'`dry` has the wrong type: Received `{type(params["dry"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("fbuc", None) is not None:
        if not isinstance(params["fbuc"], bool):
            raise StyxValidationError(f'`fbuc` has the wrong type: Received `{type(params["fbuc"])}` expected `bool`')
    if params.get("abuc", None) is not None:
        if not isinstance(params["abuc"], bool):
            raise StyxValidationError(f'`abuc` has the wrong type: Received `{type(params["abuc"])}` expected `bool`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[str]`')
    for e in params["input_files"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[str]`')

def v_3dbucket_cargs(params: V3dbucket, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dbucket")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("output")
    if v_1 is not None:
        cargs.append("-output")
        cargs.append(v_1)
    v_2 = params.get("session")
    if v_2 is not None:
        cargs.append("-session")
        cargs.append(v_2)
    v_3 = params.get("glueto")
    if v_3 is not None:
        cargs.append("-glueto")
        cargs.append(v_3)
    v_4 = params.get("aglueto")
    if v_4 is not None:
        cargs.append("-aglueto")
        cargs.append(v_4)
    if params.get("dry"):
        cargs.append("-dry")
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("fbuc"):
        cargs.append("-fbuc")
    if params.get("abuc"):
        cargs.append("-abuc")
    for item0 in params["input_files"]:
        cargs.append(item0)
    return cargs

def v_3dbucket_outputs(params: V3dbucket, execution: Execution) -> V3dbucketOutputs:
    """Build the V3dbucketOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dbucketOutputs(
        root=root_v,
    )

def v_3dbucket_execute(params: V3dbucket, runner: Runner | None = None) -> V3dbucketOutputs:
    """
    3dbucket

    Concatenate sub-bricks from input datasets into one big bucket dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dbucket_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DBUCKET_METADATA)
    execution.params(params)
    args = v_3dbucket_cargs(params, execution)
    out = v_3dbucket_outputs(params, execution)
    execution.run(args)
    return out

def v_3dbucket(
    input_files: list[str],
    prefix: str | None = None,
    output: str | None = None,
    session: str | None = None,
    glueto: str | None = None,
    aglueto: str | None = None,
    dry: bool = False,
    verbose: bool = False,
    fbuc: bool = False,
    abuc: bool = False,
    runner: Runner | None = None,
) -> V3dbucketOutputs:
    """
    3dbucket

    Concatenate sub-bricks from input datasets into one big bucket dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input datasets with optional sub-brick selection.
        prefix: Use 'pname' for the output dataset prefix name.
        output: Use 'pname' for the output dataset prefix name.
        session: Use 'dir' for the output dataset session directory.\
            [default='./'=current working directory]
        glueto: Append bricks to the end of the 'fname' dataset.
        aglueto: If fname dataset does not exist, create it (like -prefix).\
            Otherwise append to fname (like -glueto).
        dry: Execute a 'dry run'; only print out what would be done.
        verbose: Print out some verbose output as the program proceeds.
        fbuc: Create a functional bucket.
        abuc: Create an anatomical bucket. If neither of these options is given,\
            the output type is determined from the first input type.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dbucket_params(
        input_files=input_files,
        prefix=prefix,
        output=output,
        session=session,
        glueto=glueto,
        aglueto=aglueto,
        dry=dry,
        verbose=verbose,
        fbuc=fbuc,
        abuc=abuc,
    )
    return v_3dbucket_execute(params, runner)

__all__ = [
    "V3dbucket",
    "V3dbucketOutputs",
    "V_3DBUCKET_METADATA",
    "v_3dbucket_cargs",
    "v_3dbucket_outputs",
    "v_3dbucket_params",
    "v_3dbucket_execute",
    "v_3dbucket_validate",
    "v_3dbucket",
]