# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_SYNTHESIZE_METADATA = Metadata(
    id="3dSynthesize",
    name="3dSynthesize",
    package="afni",
)

V3dSynthesizeCenfill = typing.Literal["zero"] | typing.Literal["nbhr"] | typing.Literal["none"]

V3dSynthesize = typing.TypedDict(
    "V3dSynthesize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dSynthesize"]],
        "c_bucket": InputPathType,
        "matrix": InputPathType,
        "select": str,
        "prefix": str,
        "dry_flag": typing.NotRequired[bool],
        "tr": typing.NotRequired[float],
        "cenfill": typing.NotRequired[V3dSynthesizeCenfill],
    },
)

@dataclasses.dataclass
class V3dSynthesizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3d_synthesize_params(
    c_bucket: InputPathType,
    matrix: InputPathType,
    select: str,
    prefix: str,
    dry_flag: bool = False,
    tr: float | None = None,
    cenfill: V3dSynthesizeCenfill | None = None,
) -> V3dSynthesize:
    """
    Build parameters.

    Args:
        c_bucket: Input dataset from 3dDeconvolve via the '-cbucket' option.
        matrix: Matrix file from 3dDeconvolve via the '-x1D' option.
        select: Select columns from the matrix and corresponding sub-bricks from\
            the cbucket. Can use forms like 'baseline', 'polort', 'allfunc',\
            'allstim', 'all', 'something', or numbers/ranges.
        prefix: Output result into dataset with the specified name.
        dry_flag: Don't compute the output, just check the inputs.
        tr: Set TR in the output to the specified value.
        cenfill: How censored time points from 3dDeconvolve will be filled\
            (options: 'zero', 'nbhr', 'none').

    Returns:
        Parameter dictionary.
    """
    params: V3dSynthesize = {
        "@type": "afni/3dSynthesize",
        "c_bucket": c_bucket,
        "matrix": matrix,
        "select": select,
        "prefix": prefix,
        "dry_flag": dry_flag,
    }
    if tr is not None:
        params["tr"] = tr
    if cenfill is not None:
        params["cenfill"] = cenfill
    return params

def v_3d_synthesize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dSynthesize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("c_bucket", None) is None:
        raise StyxValidationError("`c_bucket` must not be None")
    if not isinstance(params["c_bucket"], (pathlib.Path, str)):
        raise StyxValidationError(f'`c_bucket` has the wrong type: Received `{type(params["c_bucket"])}` expected `InputPathType`')
    if params.get("matrix", None) is None:
        raise StyxValidationError("`matrix` must not be None")
    if not isinstance(params["matrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`matrix` has the wrong type: Received `{type(params["matrix"])}` expected `InputPathType`')
    if params.get("select", None) is None:
        raise StyxValidationError("`select` must not be None")
    if not isinstance(params["select"], str):
        raise StyxValidationError(f'`select` has the wrong type: Received `{type(params["select"])}` expected `str`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("dry_flag", None) is not None:
        if not isinstance(params["dry_flag"], bool):
            raise StyxValidationError(f'`dry_flag` has the wrong type: Received `{type(params["dry_flag"])}` expected `bool`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("cenfill", None) is not None:
        if not isinstance(params["cenfill"], str):
            raise StyxValidationError(f'`cenfill` has the wrong type: Received `{type(params["cenfill"])}` expected `V3dSynthesizeCenfill`')
        if params["cenfill"] not in ["zero", "nbhr", "none"]:
            raise StyxValidationError("Parameter `cenfill` must be one of [\"zero\", \"nbhr\", \"none\"]")

def v_3d_synthesize_cargs(params: V3dSynthesize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dSynthesize")
    cargs.append("-cbucket")
    cargs.append(execution.input_file(params["c_bucket"]))
    cargs.append("-matrix")
    cargs.append(execution.input_file(params["matrix"]))
    cargs.append("-select")
    cargs.append(params["select"])
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("dry_flag"):
        cargs.append("-dry")
    v_0 = params.get("tr")
    if v_0 is not None:
        cargs.append("-TR")
        cargs.append(str(v_0))
    v_1 = params.get("cenfill")
    if v_1 is not None:
        cargs.append("-cenfill")
        cargs.append(str(v_1))
    return cargs

def v_3d_synthesize_outputs(params: V3dSynthesize, execution: Execution) -> V3dSynthesizeOutputs:
    """Build the V3dSynthesizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dSynthesizeOutputs(
        root=root_v,
    )

def v_3d_synthesize_execute(params: V3dSynthesize, runner: Runner | None = None) -> V3dSynthesizeOutputs:
    """
    3dSynthesize

    Reads a '-cbucket' dataset and a '.xmat.1D' matrix from 3dDeconvolve, and synthesizes a fit dataset using selected sub-bricks and matrix columns.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_synthesize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_SYNTHESIZE_METADATA)
    execution.params(params)
    args = v_3d_synthesize_cargs(params, execution)
    out = v_3d_synthesize_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_synthesize(
    c_bucket: InputPathType,
    matrix: InputPathType,
    select: str,
    prefix: str,
    dry_flag: bool = False,
    tr: float | None = None,
    cenfill: V3dSynthesizeCenfill | None = None,
    runner: Runner | None = None,
) -> V3dSynthesizeOutputs:
    """
    3dSynthesize

    Reads a '-cbucket' dataset and a '.xmat.1D' matrix from 3dDeconvolve, and synthesizes a fit dataset using selected sub-bricks and matrix columns.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        c_bucket: Input dataset from 3dDeconvolve via the '-cbucket' option.
        matrix: Matrix file from 3dDeconvolve via the '-x1D' option.
        select: Select columns from the matrix and corresponding sub-bricks from\
            the cbucket. Can use forms like 'baseline', 'polort', 'allfunc',\
            'allstim', 'all', 'something', or numbers/ranges.
        prefix: Output result into dataset with the specified name.
        dry_flag: Don't compute the output, just check the inputs.
        tr: Set TR in the output to the specified value.
        cenfill: How censored time points from 3dDeconvolve will be filled\
            (options: 'zero', 'nbhr', 'none').
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_synthesize_params(
        c_bucket=c_bucket,
        matrix=matrix,
        select=select,
        prefix=prefix,
        dry_flag=dry_flag,
        tr=tr,
        cenfill=cenfill,
    )
    return v_3d_synthesize_execute(params, runner)

__all__ = [
    "V3dSynthesize",
    "V3dSynthesizeOutputs",
    "V_3D_SYNTHESIZE_METADATA",
    "v_3d_synthesize_cargs",
    "v_3d_synthesize_outputs",
    "v_3d_synthesize_params",
    "v_3d_synthesize_execute",
    "v_3d_synthesize_validate",
    "v_3d_synthesize",
]