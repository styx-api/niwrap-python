# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_FWH_MX_METADATA = Metadata(
    id="3dFWHMx",
    name="3dFWHMx",
    package="afni",
)

V3dFwhMx = typing.TypedDict(
    "V3dFwhMx",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dFWHMx"]],
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[bool],
        "demed": typing.NotRequired[bool],
        "unif": typing.NotRequired[bool],
        "detrend": typing.NotRequired[float],
        "detprefix": typing.NotRequired[str],
        "geom": typing.NotRequired[bool],
        "arith": typing.NotRequired[bool],
        "combine": typing.NotRequired[bool],
        "out": typing.NotRequired[str],
        "compat": typing.NotRequired[bool],
        "acf": typing.NotRequired[str],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class V3dFwhMxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file: OutputPathType | None
    """Output file containing FWHM/ACF estimates"""
    detrended_dataset: OutputPathType | None
    """Detrended dataset file"""

def v_3d_fwh_mx_params(
    infile: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    demed: bool = False,
    unif: bool = False,
    detrend: float | None = None,
    detprefix: str | None = None,
    geom: bool = False,
    arith: bool = False,
    combine: bool = False,
    out: str | None = None,
    compat: bool = False,
    acf: str | None = None,
) -> V3dFwhMx:
    """
    Build parameters.

    Args:
        infile: Input dataset
        mask: Use only voxels that are nonzero in dataset 'mmm'
        automask: Compute a mask from THIS dataset
        demed: if the input dataset has more than one sub-brick then subtract the\
            median of each voxel's time series before processing FWHM
        unif: Normalize each voxel's time series to have the same MAD before\
            processing FWHM, implies -demed
        detrend: Detrend to order 'q'. If q is not given, the program picks\
            q=NT/30; -detrend disables -demed, and includes -unif
        detprefix: Save the detrended file into a dataset with prefix 'd'
        geom: Compute the final estimate as the geometric mean
        arith: Compute the final estimate as the arithmetic mean
        combine: Combine the final measurements along each axis into one result
        out: Write output to file 'ttt' (3 columns of numbers). If not given, the\
            sub-brick outputs are not written. Use '-out -' to write to stdout, if\
            desired
        compat: Be compatible with the older 3dFWHM
        acf: Compute the spatial autocorrelation of the data as a function of\
            radius, then fit that to a model and output the model parameters

    Returns:
        Parameter dictionary.
    """
    params: V3dFwhMx = {
        "@type": "afni/3dFWHMx",
        "infile": infile,
        "automask": automask,
        "demed": demed,
        "unif": unif,
        "geom": geom,
        "arith": arith,
        "combine": combine,
        "compat": compat,
    }
    if mask is not None:
        params["mask"] = mask
    if detrend is not None:
        params["detrend"] = detrend
    if detprefix is not None:
        params["detprefix"] = detprefix
    if out is not None:
        params["out"] = out
    if acf is not None:
        params["acf"] = acf
    return params

def v_3d_fwh_mx_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dFwhMx."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("demed", None) is not None:
        if not isinstance(params["demed"], bool):
            raise StyxValidationError(f'`demed` has the wrong type: Received `{type(params["demed"])}` expected `bool`')
    if params.get("unif", None) is not None:
        if not isinstance(params["unif"], bool):
            raise StyxValidationError(f'`unif` has the wrong type: Received `{type(params["unif"])}` expected `bool`')
    if params.get("detrend", None) is not None:
        if not isinstance(params["detrend"], (float, int)):
            raise StyxValidationError(f'`detrend` has the wrong type: Received `{type(params["detrend"])}` expected `float`')
    if params.get("detprefix", None) is not None:
        if not isinstance(params["detprefix"], str):
            raise StyxValidationError(f'`detprefix` has the wrong type: Received `{type(params["detprefix"])}` expected `str`')
    if params.get("geom", None) is not None:
        if not isinstance(params["geom"], bool):
            raise StyxValidationError(f'`geom` has the wrong type: Received `{type(params["geom"])}` expected `bool`')
    if params.get("arith", None) is not None:
        if not isinstance(params["arith"], bool):
            raise StyxValidationError(f'`arith` has the wrong type: Received `{type(params["arith"])}` expected `bool`')
    if params.get("combine", None) is not None:
        if not isinstance(params["combine"], bool):
            raise StyxValidationError(f'`combine` has the wrong type: Received `{type(params["combine"])}` expected `bool`')
    if params.get("out", None) is not None:
        if not isinstance(params["out"], str):
            raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')
    if params.get("compat", None) is not None:
        if not isinstance(params["compat"], bool):
            raise StyxValidationError(f'`compat` has the wrong type: Received `{type(params["compat"])}` expected `bool`')
    if params.get("acf", None) is not None:
        if not isinstance(params["acf"], str):
            raise StyxValidationError(f'`acf` has the wrong type: Received `{type(params["acf"])}` expected `str`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def v_3d_fwh_mx_cargs(params: V3dFwhMx, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dFWHMx")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("automask"):
        cargs.append("-automask")
    if params.get("demed"):
        cargs.append("-demed")
    if params.get("unif"):
        cargs.append("-unif")
    v_1 = params.get("detrend")
    if v_1 is not None:
        cargs.append("-detrend")
        cargs.append(str(v_1))
    v_2 = params.get("detprefix")
    if v_2 is not None:
        cargs.append("-detprefix")
        cargs.append(v_2)
    if params.get("geom"):
        cargs.append("-geom")
    if params.get("arith"):
        cargs.append("-arith")
    if params.get("combine"):
        cargs.append("-combine")
    v_3 = params.get("out")
    if v_3 is not None:
        cargs.append("-out")
        cargs.append(v_3)
    if params.get("compat"):
        cargs.append("-compat")
    v_4 = params.get("acf")
    if v_4 is not None:
        cargs.append("-acf")
        cargs.append(v_4)
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def v_3d_fwh_mx_outputs(params: V3dFwhMx, execution: Execution) -> V3dFwhMxOutputs:
    """Build the V3dFwhMxOutputs object for this tool."""
    out_file_v: OutputPathType | None = None
    detrended_dataset_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out")
    if __v0 is not None:
        out_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.1D')
    __v1 = params.get("detprefix")
    if __v1 is not None:
        detrended_dataset_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.nii.gz')
    return V3dFwhMxOutputs(
        root=root_v,
        out_file=out_file_v,
        detrended_dataset=detrended_dataset_v,
    )

def v_3d_fwh_mx_execute(params: V3dFwhMx, runner: Runner | None = None) -> V3dFwhMxOutputs:
    """
    3dFWHMx

    Compute Full Width at Half Maximum (FWHM) for FMRI datasets using AutoCorrelation Function (ACF)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_fwh_mx_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_FWH_MX_METADATA)
    execution.params(params)
    args = v_3d_fwh_mx_cargs(params, execution)
    out = v_3d_fwh_mx_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_fwh_mx(
    infile: InputPathType,
    mask: InputPathType | None = None,
    automask: bool = False,
    demed: bool = False,
    unif: bool = False,
    detrend: float | None = None,
    detprefix: str | None = None,
    geom: bool = False,
    arith: bool = False,
    combine: bool = False,
    out: str | None = None,
    compat: bool = False,
    acf: str | None = None,
    runner: Runner | None = None,
) -> V3dFwhMxOutputs:
    """
    3dFWHMx

    Compute Full Width at Half Maximum (FWHM) for FMRI datasets using AutoCorrelation Function (ACF)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input dataset
        mask: Use only voxels that are nonzero in dataset 'mmm'
        automask: Compute a mask from THIS dataset
        demed: if the input dataset has more than one sub-brick then subtract the\
            median of each voxel's time series before processing FWHM
        unif: Normalize each voxel's time series to have the same MAD before\
            processing FWHM, implies -demed
        detrend: Detrend to order 'q'. If q is not given, the program picks\
            q=NT/30; -detrend disables -demed, and includes -unif
        detprefix: Save the detrended file into a dataset with prefix 'd'
        geom: Compute the final estimate as the geometric mean
        arith: Compute the final estimate as the arithmetic mean
        combine: Combine the final measurements along each axis into one result
        out: Write output to file 'ttt' (3 columns of numbers). If not given, the\
            sub-brick outputs are not written. Use '-out -' to write to stdout, if\
            desired
        compat: Be compatible with the older 3dFWHM
        acf: Compute the spatial autocorrelation of the data as a function of\
            radius, then fit that to a model and output the model parameters
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_fwh_mx_params(
        infile=infile,
        mask=mask,
        automask=automask,
        demed=demed,
        unif=unif,
        detrend=detrend,
        detprefix=detprefix,
        geom=geom,
        arith=arith,
        combine=combine,
        out=out,
        compat=compat,
        acf=acf,
    )
    return v_3d_fwh_mx_execute(params, runner)

__all__ = [
    "V3dFwhMx",
    "V3dFwhMxOutputs",
    "V_3D_FWH_MX_METADATA",
    "v_3d_fwh_mx_cargs",
    "v_3d_fwh_mx_outputs",
    "v_3d_fwh_mx_params",
    "v_3d_fwh_mx_execute",
    "v_3d_fwh_mx_validate",
    "v_3d_fwh_mx",
]