# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ADJUNCT_IS_LABEL_PY_METADATA = Metadata(
    id="adjunct_is_label.py",
    name="adjunct_is_label.py",
    package="afni",
)

AdjunctIsLabelPy = typing.TypedDict(
    "AdjunctIsLabelPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/adjunct_is_label.py"]],
        "infile": InputPathType,
        "label": str,
    },
)

@dataclasses.dataclass
class AdjunctIsLabelPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def adjunct_is_label_py_params(
    infile: InputPathType,
    label: str,
) -> AdjunctIsLabelPy:
    """
    Build parameters.

    Args:
        infile: Input file for the adjunct_is_label script
        label: Output label generated by the script

    Returns:
        Parameter dictionary.
    """
    params: AdjunctIsLabelPy = {
        "@type": "afni/adjunct_is_label.py",
        "infile": infile,
        "label": label,
    }
    return params

def adjunct_is_label_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AdjunctIsLabelPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("label", None) is None:
        raise StyxValidationError("`label` must not be None")
    if not isinstance(params["label"], str):
        raise StyxValidationError(f'`label` has the wrong type: Received `{type(params["label"])}` expected `str`')

def adjunct_is_label_py_cargs(params: AdjunctIsLabelPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("adjunct_is_label.py")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(params["label"])
    return cargs

def adjunct_is_label_py_outputs(params: AdjunctIsLabelPy, execution: Execution) -> AdjunctIsLabelPyOutputs:
    """Build the AdjunctIsLabelPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AdjunctIsLabelPyOutputs(
        root=root_v,
    )

def adjunct_is_label_py_execute(params: AdjunctIsLabelPy, runner: Runner | None = None) -> AdjunctIsLabelPyOutputs:
    """
    adjunct_is_label.py

    A subsidiary script of the chauffeur_afni suite for label functionalities.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    adjunct_is_label_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ADJUNCT_IS_LABEL_PY_METADATA)
    execution.params(params)
    args = adjunct_is_label_py_cargs(params, execution)
    out = adjunct_is_label_py_outputs(params, execution)
    execution.run(args)
    return out

def adjunct_is_label_py(
    infile: InputPathType,
    label: str,
    runner: Runner | None = None,
) -> AdjunctIsLabelPyOutputs:
    """
    adjunct_is_label.py

    A subsidiary script of the chauffeur_afni suite for label functionalities.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input file for the adjunct_is_label script
        label: Output label generated by the script
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = adjunct_is_label_py_params(
        infile=infile,
        label=label,
    )
    return adjunct_is_label_py_execute(params, runner)

__all__ = [
    "AdjunctIsLabelPy",
    "AdjunctIsLabelPyOutputs",
    "ADJUNCT_IS_LABEL_PY_METADATA",
    "adjunct_is_label_py_cargs",
    "adjunct_is_label_py_outputs",
    "adjunct_is_label_py_params",
    "adjunct_is_label_py_execute",
    "adjunct_is_label_py_validate",
    "adjunct_is_label_py",
]