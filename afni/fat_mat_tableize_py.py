# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAT_MAT_TABLEIZE_PY_METADATA = Metadata(
    id="fat_mat_tableize.py",
    name="fat_mat_tableize.py",
    package="afni",
)

FatMatTableizePy = typing.TypedDict(
    "FatMatTableizePy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/fat_mat_tableize.py"]],
        "input_matrices": list[str],
        "input_csv": typing.NotRequired[InputPathType],
        "input_list": typing.NotRequired[InputPathType],
        "output_prefix": str,
        "parameters": typing.NotRequired[list[str]],
        "version": typing.NotRequired[bool],
        "date": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "help_short": typing.NotRequired[bool],
        "help_view": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FatMatTableizePyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_table: OutputPathType
    """Table file usable in AFNI group analysis programs."""
    output_log: OutputPathType
    """Log file reporting inputs, matching, and aspects of creating the table file."""

def fat_mat_tableize_py_params(
    input_matrices: list[str],
    output_prefix: str,
    input_csv: InputPathType | None = None,
    input_list: InputPathType | None = None,
    parameters: list[str] | None = None,
    version: bool = False,
    date: bool = False,
    help: bool = False,
    help_short: bool = False,
    help_view: bool = False,
) -> FatMatTableizePy:
    """
    Build parameters.

    Args:
        input_matrices: Names of *.netcc or *.grid files with matrices to be used\
            to make table; can be provided using wildcard chars.
        output_prefix: Output basename for the table and log files. Suffix and file\
            extensions will be added for the outputs.
        input_csv: Name of a CSV file to include in the table. The first column\
            must have subject ID labels that match with the input matrix files.
        input_list: File containing paths to subject matrices and optionally CSV\
            IDs for matching.
        parameters: List of matrices to be included in the table, identified by\
            their parameter name.
        version: Display current version.
        date: Display release/editing date of current version.
        help: Display help in terminal.
        help_short: Display help in terminal (short flag).
        help_view: Display help in a separate text editor.

    Returns:
        Parameter dictionary.
    """
    params: FatMatTableizePy = {
        "@type": "afni/fat_mat_tableize.py",
        "input_matrices": input_matrices,
        "output_prefix": output_prefix,
        "version": version,
        "date": date,
        "help": help,
        "help_short": help_short,
        "help_view": help_view,
    }
    if input_csv is not None:
        params["input_csv"] = input_csv
    if input_list is not None:
        params["input_list"] = input_list
    if parameters is not None:
        params["parameters"] = parameters
    return params

def fat_mat_tableize_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FatMatTableizePy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_matrices", None) is None:
        raise StyxValidationError("`input_matrices` must not be None")
    if not isinstance(params["input_matrices"], list):
        raise StyxValidationError(f'`input_matrices` has the wrong type: Received `{type(params["input_matrices"])}` expected `list[str]`')
    for e in params["input_matrices"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_matrices` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("input_csv", None) is not None:
        if not isinstance(params["input_csv"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_csv` has the wrong type: Received `{type(params["input_csv"])}` expected `InputPathType`')
    if params.get("input_list", None) is not None:
        if not isinstance(params["input_list"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_list` has the wrong type: Received `{type(params["input_list"])}` expected `InputPathType`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("parameters", None) is not None:
        if not isinstance(params["parameters"], list):
            raise StyxValidationError(f'`parameters` has the wrong type: Received `{type(params["parameters"])}` expected `list[str]`')
        for e_2 in params["parameters"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`parameters` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("date", None) is not None:
        if not isinstance(params["date"], bool):
            raise StyxValidationError(f'`date` has the wrong type: Received `{type(params["date"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("help_short", None) is not None:
        if not isinstance(params["help_short"], bool):
            raise StyxValidationError(f'`help_short` has the wrong type: Received `{type(params["help_short"])}` expected `bool`')
    if params.get("help_view", None) is not None:
        if not isinstance(params["help_view"], bool):
            raise StyxValidationError(f'`help_view` has the wrong type: Received `{type(params["help_view"])}` expected `bool`')

def fat_mat_tableize_py_cargs(params: FatMatTableizePy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fat_mat_tableize.py")
    cargs.append("-in_mat")
    for item0 in params["input_matrices"]:
        cargs.append(item0)
    v_0 = params.get("input_csv")
    if v_0 is not None:
        cargs.append("-in_csv")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("input_list")
    if v_1 is not None:
        cargs.append("-in_listfile")
        cargs.append(execution.input_file(v_1))
    cargs.append("-prefix")
    cargs.append(params["output_prefix"])
    v_2 = params.get("parameters")
    if v_2 is not None:
        cargs.append("-pars")
        for item1 in v_2:
            cargs.append(item1)
    if params.get("version"):
        cargs.append("-ver")
    if params.get("date"):
        cargs.append("-date")
    if params.get("help"):
        cargs.append("-help")
    if params.get("help_short"):
        cargs.append("-h")
    if params.get("help_view"):
        cargs.append("-hview")
    return cargs

def fat_mat_tableize_py_outputs(params: FatMatTableizePy, execution: Execution) -> FatMatTableizePyOutputs:
    """Build the FatMatTableizePyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_table_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_tbl.txt')
    output_log_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}_prep.log')
    return FatMatTableizePyOutputs(
        root=root_v,
        output_table=output_table_v,
        output_log=output_log_v,
    )

def fat_mat_tableize_py_execute(params: FatMatTableizePy, runner: Runner | None = None) -> FatMatTableizePyOutputs:
    """
    fat_mat_tableize.py

    Make tables for AFNI group analysis programs from 3dNetCorr (*.netcc) and 3dTrackID (*.grid) outputs, with optional additional subject information from CSV files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fat_mat_tableize_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAT_MAT_TABLEIZE_PY_METADATA)
    execution.params(params)
    args = fat_mat_tableize_py_cargs(params, execution)
    out = fat_mat_tableize_py_outputs(params, execution)
    execution.run(args)
    return out

def fat_mat_tableize_py(
    input_matrices: list[str],
    output_prefix: str,
    input_csv: InputPathType | None = None,
    input_list: InputPathType | None = None,
    parameters: list[str] | None = None,
    version: bool = False,
    date: bool = False,
    help: bool = False,
    help_short: bool = False,
    help_view: bool = False,
    runner: Runner | None = None,
) -> FatMatTableizePyOutputs:
    """
    fat_mat_tableize.py

    Make tables for AFNI group analysis programs from 3dNetCorr (*.netcc) and 3dTrackID (*.grid) outputs, with optional additional subject information from CSV files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_matrices: Names of *.netcc or *.grid files with matrices to be used\
            to make table; can be provided using wildcard chars.
        output_prefix: Output basename for the table and log files. Suffix and file\
            extensions will be added for the outputs.
        input_csv: Name of a CSV file to include in the table. The first column\
            must have subject ID labels that match with the input matrix files.
        input_list: File containing paths to subject matrices and optionally CSV\
            IDs for matching.
        parameters: List of matrices to be included in the table, identified by\
            their parameter name.
        version: Display current version.
        date: Display release/editing date of current version.
        help: Display help in terminal.
        help_short: Display help in terminal (short flag).
        help_view: Display help in a separate text editor.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fat_mat_tableize_py_params(
        input_matrices=input_matrices,
        output_prefix=output_prefix,
        input_csv=input_csv,
        input_list=input_list,
        parameters=parameters,
        version=version,
        date=date,
        help=help,
        help_short=help_short,
        help_view=help_view,
    )
    return fat_mat_tableize_py_execute(params, runner)

__all__ = [
    "FatMatTableizePy",
    "FatMatTableizePyOutputs",
    "FAT_MAT_TABLEIZE_PY_METADATA",
    "fat_mat_tableize_py_cargs",
    "fat_mat_tableize_py_outputs",
    "fat_mat_tableize_py_params",
    "fat_mat_tableize_py_execute",
    "fat_mat_tableize_py_validate",
    "fat_mat_tableize_py",
]