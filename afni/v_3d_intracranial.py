# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_INTRACRANIAL_METADATA = Metadata(
    id="3dIntracranial",
    name="3dIntracranial",
    package="afni",
)

V3dIntracranial = typing.TypedDict(
    "V3dIntracranial",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dIntracranial"]],
        "infile": InputPathType,
        "prefix": str,
        "min_val": typing.NotRequired[float],
        "max_val": typing.NotRequired[float],
        "min_conn": typing.NotRequired[float],
        "max_conn": typing.NotRequired[float],
        "no_smooth": typing.NotRequired[bool],
        "mask": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dIntracranialOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmented_image: OutputPathType
    """Output file containing segmented image"""

def v_3d_intracranial_params(
    infile: InputPathType,
    prefix: str,
    min_val: float | None = None,
    max_val: float | None = None,
    min_conn: float | None = None,
    max_conn: float | None = None,
    no_smooth: bool = False,
    mask: bool = False,
    quiet: bool = False,
) -> V3dIntracranial:
    """
    Build parameters.

    Args:
        infile: Filename of anat dataset to be segmented
        prefix: Prefix name for file to contain segmented image
        min_val: Minimum voxel intensity limit. Default is internal PDF estimate\
            for lower bound
        max_val: Maximum voxel intensity limit. Default is internal PDF estimate\
            for upper bound
        min_conn: Minimum voxel connectivity to enter. Default is 4
        max_conn: Maximum voxel connectivity to leave. Default is 2
        no_smooth: Suppress spatial smoothing of segmentation mask
        mask: Generate functional image mask (complement). Default is to generate\
            anatomical image
        quiet: Suppress output to screen

    Returns:
        Parameter dictionary.
    """
    params: V3dIntracranial = {
        "@type": "afni/3dIntracranial",
        "infile": infile,
        "prefix": prefix,
        "no_smooth": no_smooth,
        "mask": mask,
        "quiet": quiet,
    }
    if min_val is not None:
        params["min_val"] = min_val
    if max_val is not None:
        params["max_val"] = max_val
    if min_conn is not None:
        params["min_conn"] = min_conn
    if max_conn is not None:
        params["max_conn"] = max_conn
    return params

def v_3d_intracranial_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dIntracranial."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("min_val", None) is not None:
        if not isinstance(params["min_val"], (float, int)):
            raise StyxValidationError(f'`min_val` has the wrong type: Received `{type(params["min_val"])}` expected `float`')
    if params.get("max_val", None) is not None:
        if not isinstance(params["max_val"], (float, int)):
            raise StyxValidationError(f'`max_val` has the wrong type: Received `{type(params["max_val"])}` expected `float`')
    if params.get("min_conn", None) is not None:
        if not isinstance(params["min_conn"], (float, int)):
            raise StyxValidationError(f'`min_conn` has the wrong type: Received `{type(params["min_conn"])}` expected `float`')
    if params.get("max_conn", None) is not None:
        if not isinstance(params["max_conn"], (float, int)):
            raise StyxValidationError(f'`max_conn` has the wrong type: Received `{type(params["max_conn"])}` expected `float`')
    if params.get("no_smooth", None) is not None:
        if not isinstance(params["no_smooth"], bool):
            raise StyxValidationError(f'`no_smooth` has the wrong type: Received `{type(params["no_smooth"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], bool):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_3d_intracranial_cargs(params: V3dIntracranial, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dIntracranial")
    cargs.append("-anat")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("min_val")
    if v_0 is not None:
        cargs.append("-min_val")
        cargs.append(str(v_0))
    v_1 = params.get("max_val")
    if v_1 is not None:
        cargs.append("-max_val")
        cargs.append(str(v_1))
    v_2 = params.get("min_conn")
    if v_2 is not None:
        cargs.append("-min_conn")
        cargs.append(str(v_2))
    v_3 = params.get("max_conn")
    if v_3 is not None:
        cargs.append("-max_conn")
        cargs.append(str(v_3))
    if params.get("no_smooth"):
        cargs.append("-nosmooth")
    if params.get("mask"):
        cargs.append("-mask")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def v_3d_intracranial_outputs(params: V3dIntracranial, execution: Execution) -> V3dIntracranialOutputs:
    """Build the V3dIntracranialOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmented_image_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig')
    return V3dIntracranialOutputs(
        root=root_v,
        segmented_image=segmented_image_v,
    )

def v_3d_intracranial_execute(params: V3dIntracranial, runner: Runner | None = None) -> V3dIntracranialOutputs:
    """
    3dIntracranial

    Performs automatic segmentation of intracranial region.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_intracranial_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_INTRACRANIAL_METADATA)
    execution.params(params)
    args = v_3d_intracranial_cargs(params, execution)
    out = v_3d_intracranial_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_intracranial(
    infile: InputPathType,
    prefix: str,
    min_val: float | None = None,
    max_val: float | None = None,
    min_conn: float | None = None,
    max_conn: float | None = None,
    no_smooth: bool = False,
    mask: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> V3dIntracranialOutputs:
    """
    3dIntracranial

    Performs automatic segmentation of intracranial region.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Filename of anat dataset to be segmented
        prefix: Prefix name for file to contain segmented image
        min_val: Minimum voxel intensity limit. Default is internal PDF estimate\
            for lower bound
        max_val: Maximum voxel intensity limit. Default is internal PDF estimate\
            for upper bound
        min_conn: Minimum voxel connectivity to enter. Default is 4
        max_conn: Maximum voxel connectivity to leave. Default is 2
        no_smooth: Suppress spatial smoothing of segmentation mask
        mask: Generate functional image mask (complement). Default is to generate\
            anatomical image
        quiet: Suppress output to screen
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_intracranial_params(
        infile=infile,
        prefix=prefix,
        min_val=min_val,
        max_val=max_val,
        min_conn=min_conn,
        max_conn=max_conn,
        no_smooth=no_smooth,
        mask=mask,
        quiet=quiet,
    )
    return v_3d_intracranial_execute(params, runner)

__all__ = [
    "V3dIntracranial",
    "V3dIntracranialOutputs",
    "V_3D_INTRACRANIAL_METADATA",
    "v_3d_intracranial_cargs",
    "v_3d_intracranial_outputs",
    "v_3d_intracranial_params",
    "v_3d_intracranial_execute",
    "v_3d_intracranial_validate",
    "v_3d_intracranial",
]