# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_RE_HO_METADATA = Metadata(
    id="3dReHo",
    name="3dReHo",
    package="afni",
)

V3dReHo = typing.TypedDict(
    "V3dReHo",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dReHo"]],
        "prefix": str,
        "inset": InputPathType,
        "nneigh": typing.NotRequired[str],
        "chi_sq": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
        "neigh_rad": typing.NotRequired[float],
        "neigh_x": typing.NotRequired[float],
        "neigh_y": typing.NotRequired[float],
        "neigh_z": typing.NotRequired[float],
        "box_rad": typing.NotRequired[float],
        "box_x": typing.NotRequired[float],
        "box_y": typing.NotRequired[float],
        "box_z": typing.NotRequired[float],
        "in_rois": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class V3dReHoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reho_output: OutputPathType
    """Output file containing ReHo/Kendall's W value per voxel."""
    roi_reho_vals: OutputPathType
    """List of ROI ReHo values."""
    chi_square: OutputPathType
    """Optional output subbrick containing Friedman chi-square value per voxel."""
    roi_chi_vals: OutputPathType
    """ROI Chi-square values file."""

def v_3d_re_ho_params(
    prefix: str,
    inset: InputPathType,
    nneigh: str | None = None,
    chi_sq: bool = False,
    mask: InputPathType | None = None,
    neigh_rad: float | None = None,
    neigh_x: float | None = None,
    neigh_y: float | None = None,
    neigh_z: float | None = None,
    box_rad: float | None = None,
    box_x: float | None = None,
    box_y: float | None = None,
    box_z: float | None = None,
    in_rois: InputPathType | None = None,
) -> V3dReHo:
    """
    Build parameters.

    Args:
        prefix: Output file name part.
        inset: Time series input file.
        nneigh: Number of voxels in neighborhood, inclusive; can be 7 (for facewise\
            neighbors), 19 (for face- and edge-wise neighbors), 27 (for face-,\
            edge-, and node-wise neighbors). Default is 27.
        chi_sq: Switch to output Friedman chi-square value per voxel as a subbrick.
        mask: Include a whole brain mask within which to calculate ReHo. Otherwise,\
            data should be masked already.
        neigh_rad: Radius R of a desired neighborhood for voxelwise control, must\
            be a floating point number >1. Examples: R=2.0 -> V=33, R=2.3 -> V=57,\
            etc.
        neigh_x: Semi-radius length A for ellipsoidal neighborhood.
        neigh_y: Semi-radius length B for ellipsoidal neighborhood.
        neigh_z: Semi-radius length C for ellipsoidal neighborhood.
        box_rad: Cubic box radius BR centered on a given voxel for neighborhood\
            control. Examples: BR=1 -> V=27, BR=2 -> V=125, etc.
        box_x: Box volume neighborhood dimension X. Values put in get added in the\
            +/- directions of each axis.
        box_y: Box volume neighborhood dimension Y. Values put in get added in the\
            +/- directions of each axis.
        box_z: Box volume neighborhood dimension Z. Values put in get added in the\
            +/- directions of each axis.
        in_rois: Input a set of ROIs, each labeled with distinct integers. ReHo\
            will be calculated per ROI.

    Returns:
        Parameter dictionary.
    """
    params: V3dReHo = {
        "@type": "afni/3dReHo",
        "prefix": prefix,
        "inset": inset,
        "chi_sq": chi_sq,
    }
    if nneigh is not None:
        params["nneigh"] = nneigh
    if mask is not None:
        params["mask"] = mask
    if neigh_rad is not None:
        params["neigh_rad"] = neigh_rad
    if neigh_x is not None:
        params["neigh_x"] = neigh_x
    if neigh_y is not None:
        params["neigh_y"] = neigh_y
    if neigh_z is not None:
        params["neigh_z"] = neigh_z
    if box_rad is not None:
        params["box_rad"] = box_rad
    if box_x is not None:
        params["box_x"] = box_x
    if box_y is not None:
        params["box_y"] = box_y
    if box_z is not None:
        params["box_z"] = box_z
    if in_rois is not None:
        params["in_rois"] = in_rois
    return params

def v_3d_re_ho_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dReHo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("nneigh", None) is not None:
        if not isinstance(params["nneigh"], str):
            raise StyxValidationError(f'`nneigh` has the wrong type: Received `{type(params["nneigh"])}` expected `str`')
    if params.get("chi_sq", None) is not None:
        if not isinstance(params["chi_sq"], bool):
            raise StyxValidationError(f'`chi_sq` has the wrong type: Received `{type(params["chi_sq"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("neigh_rad", None) is not None:
        if not isinstance(params["neigh_rad"], (float, int)):
            raise StyxValidationError(f'`neigh_rad` has the wrong type: Received `{type(params["neigh_rad"])}` expected `float`')
    if params.get("neigh_x", None) is not None:
        if not isinstance(params["neigh_x"], (float, int)):
            raise StyxValidationError(f'`neigh_x` has the wrong type: Received `{type(params["neigh_x"])}` expected `float`')
    if params.get("neigh_y", None) is not None:
        if not isinstance(params["neigh_y"], (float, int)):
            raise StyxValidationError(f'`neigh_y` has the wrong type: Received `{type(params["neigh_y"])}` expected `float`')
    if params.get("neigh_z", None) is not None:
        if not isinstance(params["neigh_z"], (float, int)):
            raise StyxValidationError(f'`neigh_z` has the wrong type: Received `{type(params["neigh_z"])}` expected `float`')
    if params.get("box_rad", None) is not None:
        if not isinstance(params["box_rad"], (float, int)):
            raise StyxValidationError(f'`box_rad` has the wrong type: Received `{type(params["box_rad"])}` expected `float`')
    if params.get("box_x", None) is not None:
        if not isinstance(params["box_x"], (float, int)):
            raise StyxValidationError(f'`box_x` has the wrong type: Received `{type(params["box_x"])}` expected `float`')
    if params.get("box_y", None) is not None:
        if not isinstance(params["box_y"], (float, int)):
            raise StyxValidationError(f'`box_y` has the wrong type: Received `{type(params["box_y"])}` expected `float`')
    if params.get("box_z", None) is not None:
        if not isinstance(params["box_z"], (float, int)):
            raise StyxValidationError(f'`box_z` has the wrong type: Received `{type(params["box_z"])}` expected `float`')
    if params.get("in_rois", None) is not None:
        if not isinstance(params["in_rois"], (pathlib.Path, str)):
            raise StyxValidationError(f'`in_rois` has the wrong type: Received `{type(params["in_rois"])}` expected `InputPathType`')

def v_3d_re_ho_cargs(params: V3dReHo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dReHo")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-inset")
    cargs.append(execution.input_file(params["inset"]))
    v_0 = params.get("nneigh")
    if v_0 is not None:
        cargs.append("-nneigh")
        cargs.append(v_0)
    if params.get("chi_sq"):
        cargs.append("-chi_sq")
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("neigh_rad")
    if v_2 is not None:
        cargs.append("-neigh_RAD")
        cargs.append(str(v_2))
    v_3 = params.get("neigh_x")
    if v_3 is not None:
        cargs.append("-neigh_X")
        cargs.append(str(v_3))
    v_4 = params.get("neigh_y")
    if v_4 is not None:
        cargs.append("-neigh_Y")
        cargs.append(str(v_4))
    v_5 = params.get("neigh_z")
    if v_5 is not None:
        cargs.append("-neigh_Z")
        cargs.append(str(v_5))
    v_6 = params.get("box_rad")
    if v_6 is not None:
        cargs.append("-box_RAD")
        cargs.append(str(v_6))
    v_7 = params.get("box_x")
    if v_7 is not None:
        cargs.append("-box_X")
        cargs.append(str(v_7))
    v_8 = params.get("box_y")
    if v_8 is not None:
        cargs.append("-box_Y")
        cargs.append(str(v_8))
    v_9 = params.get("box_z")
    if v_9 is not None:
        cargs.append("-box_Z")
        cargs.append(str(v_9))
    v_10 = params.get("in_rois")
    if v_10 is not None:
        cargs.append("-in_rois")
        cargs.append(execution.input_file(v_10))
    return cargs

def v_3d_re_ho_outputs(params: V3dReHo, execution: Execution) -> V3dReHoOutputs:
    """Build the V3dReHoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    reho_output_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK')
    roi_reho_vals_v: OutputPathType = execution.output_file(f'{params["prefix"]}_ROI_reho.vals')
    chi_square_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK[1]')
    roi_chi_vals_v: OutputPathType = execution.output_file(f'{params["prefix"]}_ROI_reho.chi')
    return V3dReHoOutputs(
        root=root_v,
        reho_output=reho_output_v,
        roi_reho_vals=roi_reho_vals_v,
        chi_square=chi_square_v,
        roi_chi_vals=roi_chi_vals_v,
    )

def v_3d_re_ho_execute(params: V3dReHo, runner: Runner | None = None) -> V3dReHoOutputs:
    """
    3dReHo

    3dReHo calculates Kendall's W per voxel using neighborhood voxels from 4D time series data set.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_re_ho_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_RE_HO_METADATA)
    execution.params(params)
    args = v_3d_re_ho_cargs(params, execution)
    out = v_3d_re_ho_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_re_ho(
    prefix: str,
    inset: InputPathType,
    nneigh: str | None = None,
    chi_sq: bool = False,
    mask: InputPathType | None = None,
    neigh_rad: float | None = None,
    neigh_x: float | None = None,
    neigh_y: float | None = None,
    neigh_z: float | None = None,
    box_rad: float | None = None,
    box_x: float | None = None,
    box_y: float | None = None,
    box_z: float | None = None,
    in_rois: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dReHoOutputs:
    """
    3dReHo

    3dReHo calculates Kendall's W per voxel using neighborhood voxels from 4D time series data set.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output file name part.
        inset: Time series input file.
        nneigh: Number of voxels in neighborhood, inclusive; can be 7 (for facewise\
            neighbors), 19 (for face- and edge-wise neighbors), 27 (for face-,\
            edge-, and node-wise neighbors). Default is 27.
        chi_sq: Switch to output Friedman chi-square value per voxel as a subbrick.
        mask: Include a whole brain mask within which to calculate ReHo. Otherwise,\
            data should be masked already.
        neigh_rad: Radius R of a desired neighborhood for voxelwise control, must\
            be a floating point number >1. Examples: R=2.0 -> V=33, R=2.3 -> V=57,\
            etc.
        neigh_x: Semi-radius length A for ellipsoidal neighborhood.
        neigh_y: Semi-radius length B for ellipsoidal neighborhood.
        neigh_z: Semi-radius length C for ellipsoidal neighborhood.
        box_rad: Cubic box radius BR centered on a given voxel for neighborhood\
            control. Examples: BR=1 -> V=27, BR=2 -> V=125, etc.
        box_x: Box volume neighborhood dimension X. Values put in get added in the\
            +/- directions of each axis.
        box_y: Box volume neighborhood dimension Y. Values put in get added in the\
            +/- directions of each axis.
        box_z: Box volume neighborhood dimension Z. Values put in get added in the\
            +/- directions of each axis.
        in_rois: Input a set of ROIs, each labeled with distinct integers. ReHo\
            will be calculated per ROI.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_re_ho_params(
        prefix=prefix,
        inset=inset,
        nneigh=nneigh,
        chi_sq=chi_sq,
        mask=mask,
        neigh_rad=neigh_rad,
        neigh_x=neigh_x,
        neigh_y=neigh_y,
        neigh_z=neigh_z,
        box_rad=box_rad,
        box_x=box_x,
        box_y=box_y,
        box_z=box_z,
        in_rois=in_rois,
    )
    return v_3d_re_ho_execute(params, runner)

__all__ = [
    "V3dReHo",
    "V3dReHoOutputs",
    "V_3D_RE_HO_METADATA",
    "v_3d_re_ho_cargs",
    "v_3d_re_ho_outputs",
    "v_3d_re_ho_params",
    "v_3d_re_ho_execute",
    "v_3d_re_ho_validate",
    "v_3d_re_ho",
]