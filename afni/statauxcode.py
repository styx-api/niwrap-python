# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

STATAUXCODE_METADATA = Metadata(
    id="@statauxcode",
    name="@statauxcode",
    package="afni",
)

Statauxcode = typing.TypedDict(
    "Statauxcode",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@statauxcode"]],
        "code": str,
    },
)

@dataclasses.dataclass
class StatauxcodeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """Output file containing the result of the conversion"""

def statauxcode_params(
    code: str,
) -> Statauxcode:
    """
    Build parameters.

    Args:
        code: The statistical code or its numerical equivalent to be converted.

    Returns:
        Parameter dictionary.
    """
    params: Statauxcode = {
        "@type": "afni/@statauxcode",
        "code": code,
    }
    return params

def statauxcode_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Statauxcode."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("code", None) is None:
        raise StyxValidationError("`code` must not be None")
    if not isinstance(params["code"], str):
        raise StyxValidationError(f'`code` has the wrong type: Received `{type(params["code"])}` expected `str`')

def statauxcode_cargs(params: Statauxcode, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@statauxcode")
    cargs.append(params["code"])
    return cargs

def statauxcode_outputs(params: Statauxcode, execution: Execution) -> StatauxcodeOutputs:
    """Build the StatauxcodeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file("output.txt")
    return StatauxcodeOutputs(
        root=root_v,
        output=output_v,
    )

def statauxcode_execute(params: Statauxcode, runner: Runner | None = None) -> StatauxcodeOutputs:
    """
    @statauxcode

    Returns the name or number of a statistics code based on specified mappings.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    statauxcode_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(STATAUXCODE_METADATA)
    execution.params(params)
    args = statauxcode_cargs(params, execution)
    out = statauxcode_outputs(params, execution)
    execution.run(args)
    return out

def statauxcode(
    code: str,
    runner: Runner | None = None,
) -> StatauxcodeOutputs:
    """
    @statauxcode

    Returns the name or number of a statistics code based on specified mappings.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        code: The statistical code or its numerical equivalent to be converted.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = statauxcode_params(
        code=code,
    )
    return statauxcode_execute(params, runner)

__all__ = [
    "Statauxcode",
    "StatauxcodeOutputs",
    "STATAUXCODE_METADATA",
    "statauxcode_cargs",
    "statauxcode_outputs",
    "statauxcode_params",
    "statauxcode_execute",
    "statauxcode_validate",
    "statauxcode",
]