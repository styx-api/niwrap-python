# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DEDGEDOG_METADATA = Metadata(
    id="3dedgedog",
    name="3dedgedog",
    package="afni",
)

V3dedgedog = typing.TypedDict(
    "V3dedgedog",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dedgedog"]],
        "input": InputPathType,
        "prefix": str,
        "mask": typing.NotRequired[InputPathType],
        "automask": typing.NotRequired[str],
        "sigma_rad": typing.NotRequired[float],
        "sigma_nvox": typing.NotRequired[float],
        "ratio_sigma": typing.NotRequired[float],
        "output_intermed": typing.NotRequired[bool],
        "edge_bnd_nn": typing.NotRequired[float],
        "edge_bnd_side": typing.NotRequired[str],
        "edge_bnd_scale": typing.NotRequired[bool],
        "only2d": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dedgedogOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_edge: OutputPathType
    """Output edge dataset"""
    out_dog: OutputPathType
    """Output difference of Gaussian dataset"""
    out_edt2: OutputPathType
    """Output Euclidean Distance Transform squared dataset"""
    out_blur_inner: OutputPathType
    """Output inner Gaussian blurred dataset"""
    out_blur_outer: OutputPathType
    """Output outer Gaussian blurred dataset"""

def v_3dedgedog_params(
    input_: InputPathType,
    prefix: str,
    mask: InputPathType | None = None,
    automask: str | None = None,
    sigma_rad: float | None = None,
    sigma_nvox: float | None = None,
    ratio_sigma: float | None = None,
    output_intermed: bool = False,
    edge_bnd_nn: float | None = None,
    edge_bnd_side: str | None = None,
    edge_bnd_scale: bool = False,
    only2d: str | None = None,
) -> V3dedgedog:
    """
    Build parameters.

    Args:
        input_: Input dataset
        prefix: Output prefix name
        mask: Mask dataset applied after Euclidean Distance Transform calculation
        automask: Calculate mask automatically. Optionally, you can provide an\
            integer X to dilate the initial automask X times (e.g., -automask+X)
        sigma_rad: Radius for 'inner' Gaussian, in mm; must be greater than 0\
            (default: 1.40)
        sigma_nvox: Define radius for 'inner' Gaussian by providing a\
            multiplicative factor for voxel edge length greater than 0 (default:\
            use sigma_rad)
        ratio_sigma: Ratio of inner and outer Gaussian sigma values (default: 1.40)
        output_intermed: Output intermediate datasets: DOG, EDT2, BLURS (default:\
            not output)
        edge_bnd_nn: Nearest neighbor (NN) value for connectedness of boundaries;\
            must be 1 (face only), 2 (face+edge), or 3 (face+edge+node) (default:\
            1)
        edge_bnd_side: Specify boundary layer to use: NEG, POS, BOTH, BOTH_SIGN\
            (default: NEG)
        edge_bnd_scale: Scale edge values to have relative magnitude between 0 and\
            100 (default: edge locations have value=1)
        only2d: Calculate edges in 2D per plane specified by SLI: 'axi', 'cor',\
            'sag'

    Returns:
        Parameter dictionary.
    """
    params: V3dedgedog = {
        "@type": "afni/3dedgedog",
        "input": input_,
        "prefix": prefix,
        "output_intermed": output_intermed,
        "edge_bnd_scale": edge_bnd_scale,
    }
    if mask is not None:
        params["mask"] = mask
    if automask is not None:
        params["automask"] = automask
    if sigma_rad is not None:
        params["sigma_rad"] = sigma_rad
    if sigma_nvox is not None:
        params["sigma_nvox"] = sigma_nvox
    if ratio_sigma is not None:
        params["ratio_sigma"] = ratio_sigma
    if edge_bnd_nn is not None:
        params["edge_bnd_nn"] = edge_bnd_nn
    if edge_bnd_side is not None:
        params["edge_bnd_side"] = edge_bnd_side
    if only2d is not None:
        params["only2d"] = only2d
    return params

def v_3dedgedog_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dedgedog."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], str):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `str`')
    if params.get("sigma_rad", None) is not None:
        if not isinstance(params["sigma_rad"], (float, int)):
            raise StyxValidationError(f'`sigma_rad` has the wrong type: Received `{type(params["sigma_rad"])}` expected `float`')
    if params.get("sigma_nvox", None) is not None:
        if not isinstance(params["sigma_nvox"], (float, int)):
            raise StyxValidationError(f'`sigma_nvox` has the wrong type: Received `{type(params["sigma_nvox"])}` expected `float`')
    if params.get("ratio_sigma", None) is not None:
        if not isinstance(params["ratio_sigma"], (float, int)):
            raise StyxValidationError(f'`ratio_sigma` has the wrong type: Received `{type(params["ratio_sigma"])}` expected `float`')
    if params.get("output_intermed", None) is not None:
        if not isinstance(params["output_intermed"], bool):
            raise StyxValidationError(f'`output_intermed` has the wrong type: Received `{type(params["output_intermed"])}` expected `bool`')
    if params.get("edge_bnd_nn", None) is not None:
        if not isinstance(params["edge_bnd_nn"], (float, int)):
            raise StyxValidationError(f'`edge_bnd_nn` has the wrong type: Received `{type(params["edge_bnd_nn"])}` expected `float`')
    if params.get("edge_bnd_side", None) is not None:
        if not isinstance(params["edge_bnd_side"], str):
            raise StyxValidationError(f'`edge_bnd_side` has the wrong type: Received `{type(params["edge_bnd_side"])}` expected `str`')
    if params.get("edge_bnd_scale", None) is not None:
        if not isinstance(params["edge_bnd_scale"], bool):
            raise StyxValidationError(f'`edge_bnd_scale` has the wrong type: Received `{type(params["edge_bnd_scale"])}` expected `bool`')
    if params.get("only2d", None) is not None:
        if not isinstance(params["only2d"], str):
            raise StyxValidationError(f'`only2d` has the wrong type: Received `{type(params["only2d"])}` expected `str`')

def v_3dedgedog_cargs(params: V3dedgedog, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dedgedog")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["prefix"])
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("automask")
    if v_1 is not None:
        cargs.append("-automask")
        cargs.append(v_1)
    v_2 = params.get("sigma_rad")
    if v_2 is not None:
        cargs.append("-sigma_rad")
        cargs.append(str(v_2))
    v_3 = params.get("sigma_nvox")
    if v_3 is not None:
        cargs.append("-sigma_nvox")
        cargs.append(str(v_3))
    v_4 = params.get("ratio_sigma")
    if v_4 is not None:
        cargs.append("-ratio_sigma")
        cargs.append(str(v_4))
    if params.get("output_intermed"):
        cargs.append("-output_intermed")
    v_5 = params.get("edge_bnd_nn")
    if v_5 is not None:
        cargs.append("-edge_bnd_NN")
        cargs.append(str(v_5))
    v_6 = params.get("edge_bnd_side")
    if v_6 is not None:
        cargs.append("-edge_bnd_side")
        cargs.append(v_6)
    if params.get("edge_bnd_scale"):
        cargs.append("-edge_bnd_scale")
    v_7 = params.get("only2d")
    if v_7 is not None:
        cargs.append("-only2D")
        cargs.append(v_7)
    return cargs

def v_3dedgedog_outputs(params: V3dedgedog, execution: Execution) -> V3dedgedogOutputs:
    """Build the V3dedgedogOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_edge_v: OutputPathType = execution.output_file(f'{params["prefix"]}_edge.nii.gz')
    out_dog_v: OutputPathType = execution.output_file(f'{params["prefix"]}_dog.nii.gz')
    out_edt2_v: OutputPathType = execution.output_file(f'{params["prefix"]}_edt2.nii.gz')
    out_blur_inner_v: OutputPathType = execution.output_file(f'{params["prefix"]}_blur_inner.nii.gz')
    out_blur_outer_v: OutputPathType = execution.output_file(f'{params["prefix"]}_blur_outer.nii.gz')
    return V3dedgedogOutputs(
        root=root_v,
        out_edge=out_edge_v,
        out_dog=out_dog_v,
        out_edt2=out_edt2_v,
        out_blur_inner=out_blur_inner_v,
        out_blur_outer=out_blur_outer_v,
    )

def v_3dedgedog_execute(params: V3dedgedog, runner: Runner | None = None) -> V3dedgedogOutputs:
    """
    3dedgedog

    Calculate edges in an image using the Difference of Gaussians (DOG) method with extensions/tweaks of the Marr-Hildreth algorithm.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dedgedog_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DEDGEDOG_METADATA)
    execution.params(params)
    args = v_3dedgedog_cargs(params, execution)
    out = v_3dedgedog_outputs(params, execution)
    execution.run(args)
    return out

def v_3dedgedog(
    input_: InputPathType,
    prefix: str,
    mask: InputPathType | None = None,
    automask: str | None = None,
    sigma_rad: float | None = None,
    sigma_nvox: float | None = None,
    ratio_sigma: float | None = None,
    output_intermed: bool = False,
    edge_bnd_nn: float | None = None,
    edge_bnd_side: str | None = None,
    edge_bnd_scale: bool = False,
    only2d: str | None = None,
    runner: Runner | None = None,
) -> V3dedgedogOutputs:
    """
    3dedgedog

    Calculate edges in an image using the Difference of Gaussians (DOG) method with extensions/tweaks of the Marr-Hildreth algorithm.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Input dataset
        prefix: Output prefix name
        mask: Mask dataset applied after Euclidean Distance Transform calculation
        automask: Calculate mask automatically. Optionally, you can provide an\
            integer X to dilate the initial automask X times (e.g., -automask+X)
        sigma_rad: Radius for 'inner' Gaussian, in mm; must be greater than 0\
            (default: 1.40)
        sigma_nvox: Define radius for 'inner' Gaussian by providing a\
            multiplicative factor for voxel edge length greater than 0 (default:\
            use sigma_rad)
        ratio_sigma: Ratio of inner and outer Gaussian sigma values (default: 1.40)
        output_intermed: Output intermediate datasets: DOG, EDT2, BLURS (default:\
            not output)
        edge_bnd_nn: Nearest neighbor (NN) value for connectedness of boundaries;\
            must be 1 (face only), 2 (face+edge), or 3 (face+edge+node) (default:\
            1)
        edge_bnd_side: Specify boundary layer to use: NEG, POS, BOTH, BOTH_SIGN\
            (default: NEG)
        edge_bnd_scale: Scale edge values to have relative magnitude between 0 and\
            100 (default: edge locations have value=1)
        only2d: Calculate edges in 2D per plane specified by SLI: 'axi', 'cor',\
            'sag'
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dedgedog_params(
        input_=input_,
        prefix=prefix,
        mask=mask,
        automask=automask,
        sigma_rad=sigma_rad,
        sigma_nvox=sigma_nvox,
        ratio_sigma=ratio_sigma,
        output_intermed=output_intermed,
        edge_bnd_nn=edge_bnd_nn,
        edge_bnd_side=edge_bnd_side,
        edge_bnd_scale=edge_bnd_scale,
        only2d=only2d,
    )
    return v_3dedgedog_execute(params, runner)

__all__ = [
    "V3dedgedog",
    "V3dedgedogOutputs",
    "V_3DEDGEDOG_METADATA",
    "v_3dedgedog_cargs",
    "v_3dedgedog_outputs",
    "v_3dedgedog_params",
    "v_3dedgedog_execute",
    "v_3dedgedog_validate",
    "v_3dedgedog",
]