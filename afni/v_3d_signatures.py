# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_SIGNATURES_METADATA = Metadata(
    id="3dSignatures",
    name="3dSignatures",
    package="afni",
)

V3dSignatures = typing.TypedDict(
    "V3dSignatures",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dSignatures"]],
        "infile": InputPathType,
        "outfile": str,
        "segmentation": typing.NotRequired[bool],
        "filter": typing.NotRequired[bool],
        "threshold": typing.NotRequired[float],
        "smoothing": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dSignaturesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    results_file: OutputPathType
    """Main analysis results file"""

def v_3d_signatures_params(
    infile: InputPathType,
    outfile: str,
    segmentation: bool = False,
    filter: bool = False,
    threshold: float | None = None,
    smoothing: float | None = None,
) -> V3dSignatures:
    """
    Build parameters.

    Args:
        infile: Input file containing 3D genome data (e.g. genome_data.txt)
        outfile: Output file to store analysis results (e.g. analysis_results.txt)
        segmentation: Flag to apply genome segmentation
        filter: Flag to apply data filtering
        threshold: Threshold level for data filtering; default=0.5
        smoothing: Apply smoothing with specified kernel size

    Returns:
        Parameter dictionary.
    """
    params: V3dSignatures = {
        "@type": "afni/3dSignatures",
        "infile": infile,
        "outfile": outfile,
        "segmentation": segmentation,
        "filter": filter,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if smoothing is not None:
        params["smoothing"] = smoothing
    return params

def v_3d_signatures_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dSignatures."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("segmentation", None) is not None:
        if not isinstance(params["segmentation"], bool):
            raise StyxValidationError(f'`segmentation` has the wrong type: Received `{type(params["segmentation"])}` expected `bool`')
    if params.get("filter", None) is not None:
        if not isinstance(params["filter"], bool):
            raise StyxValidationError(f'`filter` has the wrong type: Received `{type(params["filter"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
        if not (0 <= params["threshold"] <= 1):
            raise StyxValidationError("Parameter `threshold` must be between 0 and 1 (inclusive)")
    if params.get("smoothing", None) is not None:
        if not isinstance(params["smoothing"], (float, int)):
            raise StyxValidationError(f'`smoothing` has the wrong type: Received `{type(params["smoothing"])}` expected `float`')

def v_3d_signatures_cargs(params: V3dSignatures, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dSignatures")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(params["outfile"])
    if params.get("segmentation"):
        cargs.append("--segmentation")
    if params.get("filter"):
        cargs.append("--filter")
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("--threshold")
        cargs.append(str(v_0))
    v_1 = params.get("smoothing")
    if v_1 is not None:
        cargs.append("--smoothing")
        cargs.append(str(v_1))
    return cargs

def v_3d_signatures_outputs(params: V3dSignatures, execution: Execution) -> V3dSignaturesOutputs:
    """Build the V3dSignaturesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    results_file_v: OutputPathType = execution.output_file(f'{params["outfile"]}.txt')
    return V3dSignaturesOutputs(
        root=root_v,
        results_file=results_file_v,
    )

def v_3d_signatures_execute(params: V3dSignatures, runner: Runner | None = None) -> V3dSignaturesOutputs:
    """
    3dSignatures

    3dSignatures analysis tool for 3D genome organization

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_signatures_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_SIGNATURES_METADATA)
    execution.params(params)
    args = v_3d_signatures_cargs(params, execution)
    out = v_3d_signatures_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_signatures(
    infile: InputPathType,
    outfile: str,
    segmentation: bool = False,
    filter: bool = False,
    threshold: float | None = None,
    smoothing: float | None = None,
    runner: Runner | None = None,
) -> V3dSignaturesOutputs:
    """
    3dSignatures

    3dSignatures analysis tool for 3D genome organization

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input file containing 3D genome data (e.g. genome_data.txt)
        outfile: Output file to store analysis results (e.g. analysis_results.txt)
        segmentation: Flag to apply genome segmentation
        filter: Flag to apply data filtering
        threshold: Threshold level for data filtering; default=0.5
        smoothing: Apply smoothing with specified kernel size
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_signatures_params(
        infile=infile,
        outfile=outfile,
        segmentation=segmentation,
        filter=filter,
        threshold=threshold,
        smoothing=smoothing,
    )
    return v_3d_signatures_execute(params, runner)

__all__ = [
    "V3dSignatures",
    "V3dSignaturesOutputs",
    "V_3D_SIGNATURES_METADATA",
    "v_3d_signatures_cargs",
    "v_3d_signatures_outputs",
    "v_3d_signatures_params",
    "v_3d_signatures_execute",
    "v_3d_signatures_validate",
    "v_3d_signatures",
]