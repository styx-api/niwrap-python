# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ADWARP_METADATA = Metadata(
    id="adwarp",
    name="adwarp",
    package="afni",
)

Adwarp = typing.TypedDict(
    "Adwarp",
    {
        "@type": typing.NotRequired[typing.Literal["afni/adwarp"]],
        "apar": InputPathType,
        "dpar": str,
        "prefix": typing.NotRequired[str],
        "dxyz": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "resam": typing.NotRequired[str],
        "thr": typing.NotRequired[str],
        "func": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class AdwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    header_output: OutputPathType | None
    """Output dataset header file"""
    brick_output: OutputPathType | None
    """Output dataset brick file"""

def adwarp_params(
    apar: InputPathType,
    dpar: str,
    prefix: str | None = None,
    dxyz: float | None = None,
    verbose: bool = False,
    force: bool = False,
    resam: str | None = None,
    thr: str | None = None,
    func: str | None = None,
) -> Adwarp:
    """
    Build parameters.

    Args:
        apar: Set the anat parent dataset (nonoptional)
        dpar: Set the data parent dataset (nonoptional). dset may contain a\
            sub-brick selector, e.g., -dpar 'dset+orig[2,5,7]'
        prefix: Set the prefix for the output dataset. Default is the prefix of\
            'dset'.
        dxyz: Set the grid spacing in the output dataset. Default is 1 mm.
        verbose: Print out progress reports.
        force: Write out result even if it means deleting an existing dataset.\
            Default is not to overwrite.
        resam: Set resampling mode for all sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation). Default is Li for all sub-bricks.
        thr: Set resampling mode for threshold sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation).
        func: Set resampling mode for functional sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation).

    Returns:
        Parameter dictionary.
    """
    params: Adwarp = {
        "@type": "afni/adwarp",
        "apar": apar,
        "dpar": dpar,
        "verbose": verbose,
        "force": force,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if dxyz is not None:
        params["dxyz"] = dxyz
    if resam is not None:
        params["resam"] = resam
    if thr is not None:
        params["thr"] = thr
    if func is not None:
        params["func"] = func
    return params

def adwarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Adwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("apar", None) is None:
        raise StyxValidationError("`apar` must not be None")
    if not isinstance(params["apar"], (pathlib.Path, str)):
        raise StyxValidationError(f'`apar` has the wrong type: Received `{type(params["apar"])}` expected `InputPathType`')
    if params.get("dpar", None) is None:
        raise StyxValidationError("`dpar` must not be None")
    if not isinstance(params["dpar"], str):
        raise StyxValidationError(f'`dpar` has the wrong type: Received `{type(params["dpar"])}` expected `str`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("dxyz", None) is not None:
        if not isinstance(params["dxyz"], (float, int)):
            raise StyxValidationError(f'`dxyz` has the wrong type: Received `{type(params["dxyz"])}` expected `float`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("resam", None) is not None:
        if not isinstance(params["resam"], str):
            raise StyxValidationError(f'`resam` has the wrong type: Received `{type(params["resam"])}` expected `str`')
    if params.get("thr", None) is not None:
        if not isinstance(params["thr"], str):
            raise StyxValidationError(f'`thr` has the wrong type: Received `{type(params["thr"])}` expected `str`')
    if params.get("func", None) is not None:
        if not isinstance(params["func"], str):
            raise StyxValidationError(f'`func` has the wrong type: Received `{type(params["func"])}` expected `str`')

def adwarp_cargs(params: Adwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("adwarp")
    cargs.append("-apar")
    cargs.append(execution.input_file(params["apar"]))
    cargs.append("-dpar")
    cargs.append(params["dpar"])
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("dxyz")
    if v_1 is not None:
        cargs.append("-dxyz")
        cargs.append(str(v_1))
    if params.get("verbose"):
        cargs.append("-verbose")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("resam")
    if v_2 is not None:
        cargs.append("-resam")
        cargs.append(v_2)
    v_3 = params.get("thr")
    if v_3 is not None:
        cargs.append("-thr")
        cargs.append(v_3)
    v_4 = params.get("func")
    if v_4 is not None:
        cargs.append("-func")
        cargs.append(v_4)
    return cargs

def adwarp_outputs(params: Adwarp, execution: Execution) -> AdwarpOutputs:
    """Build the AdwarpOutputs object for this tool."""
    header_output_v: OutputPathType | None = None
    brick_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        header_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        brick_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.BRIK')
    return AdwarpOutputs(
        root=root_v,
        header_output=header_output_v,
        brick_output=brick_output_v,
    )

def adwarp_execute(params: Adwarp, runner: Runner | None = None) -> AdwarpOutputs:
    """
    adwarp

    Resamples a 'data parent' dataset to the grid defined by an 'anat parent' dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    adwarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ADWARP_METADATA)
    execution.params(params)
    args = adwarp_cargs(params, execution)
    out = adwarp_outputs(params, execution)
    execution.run(args)
    return out

def adwarp(
    apar: InputPathType,
    dpar: str,
    prefix: str | None = None,
    dxyz: float | None = None,
    verbose: bool = False,
    force: bool = False,
    resam: str | None = None,
    thr: str | None = None,
    func: str | None = None,
    runner: Runner | None = None,
) -> AdwarpOutputs:
    """
    adwarp

    Resamples a 'data parent' dataset to the grid defined by an 'anat parent' dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        apar: Set the anat parent dataset (nonoptional)
        dpar: Set the data parent dataset (nonoptional). dset may contain a\
            sub-brick selector, e.g., -dpar 'dset+orig[2,5,7]'
        prefix: Set the prefix for the output dataset. Default is the prefix of\
            'dset'.
        dxyz: Set the grid spacing in the output dataset. Default is 1 mm.
        verbose: Print out progress reports.
        force: Write out result even if it means deleting an existing dataset.\
            Default is not to overwrite.
        resam: Set resampling mode for all sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation). Default is Li for all sub-bricks.
        thr: Set resampling mode for threshold sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation).
        func: Set resampling mode for functional sub-bricks. Modes: NN (Nearest\
            Neighbor), Li (Linear Interpolation), Cu (Cubic Interpolation), Bk\
            (Blocky Interpolation).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = adwarp_params(
        apar=apar,
        dpar=dpar,
        prefix=prefix,
        dxyz=dxyz,
        verbose=verbose,
        force=force,
        resam=resam,
        thr=thr,
        func=func,
    )
    return adwarp_execute(params, runner)

__all__ = [
    "Adwarp",
    "AdwarpOutputs",
    "ADWARP_METADATA",
    "adwarp_cargs",
    "adwarp_outputs",
    "adwarp_params",
    "adwarp_execute",
    "adwarp_validate",
    "adwarp",
]