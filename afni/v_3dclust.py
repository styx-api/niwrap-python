# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DCLUST_METADATA = Metadata(
    id="3dclust",
    name="3dclust",
    package="afni",
)

V3dclust = typing.TypedDict(
    "V3dclust",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dclust"]],
        "rmm": typing.NotRequired[float],
        "vmul": typing.NotRequired[float],
        "datasets": list[InputPathType],
        "nn1": typing.NotRequired[bool],
        "nn2": typing.NotRequired[bool],
        "nn3": typing.NotRequired[bool],
        "noabs": typing.NotRequired[bool],
        "summarize": typing.NotRequired[bool],
        "nosum": typing.NotRequired[bool],
        "verb": typing.NotRequired[bool],
        "oned_format": typing.NotRequired[bool],
        "no_oned_format": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "mni": typing.NotRequired[bool],
        "isovalue": typing.NotRequired[bool],
        "isomerge": typing.NotRequired[bool],
        "inmask": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "savemask": typing.NotRequired[str],
        "binary": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dclustOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    prefixed_output: OutputPathType | None
    """New dataset with clusters set to zero based on prefix."""
    ordered_mask_output: OutputPathType | None
    """Ordered mask dataset based on savemask prefix."""

def v_3dclust_params(
    datasets: list[InputPathType],
    rmm: float | None = None,
    vmul: float | None = None,
    nn1: bool = False,
    nn2: bool = False,
    nn3: bool = False,
    noabs: bool = False,
    summarize: bool = False,
    nosum: bool = False,
    verb: bool = False,
    oned_format: bool = False,
    no_oned_format: bool = False,
    quiet: bool = False,
    mni: bool = False,
    isovalue: bool = False,
    isomerge: bool = False,
    inmask: bool = False,
    prefix: str | None = None,
    savemask: str | None = None,
    binary: bool = False,
) -> V3dclust:
    """
    Build parameters.

    Args:
        datasets: Input dataset(s). More than one allowed, but only the first\
            sub-brick of the dataset.
        rmm: Cluster connection radius in millimeters.
        vmul: Minimum cluster volume in micro-liters or minimum number of voxels if\
            negative.
        nn1: 1st nearest-neighbor clustering (faces touching).
        nn2: 2nd nearest-neighbor clustering (edges touching).
        nn3: 3rd nearest-neighbor clustering (corners touching).
        noabs: Use the signed voxel intensities for calculations.
        summarize: Write out only the total nonzero voxel count and volume for each\
            dataset.
        nosum: Suppress printout of the totals.
        verb: Print out a progress report to stderr as computations proceed.
        oned_format: Write output in 1D format (default).
        no_oned_format: Do not write output in 1D format.
        quiet: Suppress all non-essential output.
        mni: Transform output xyz-coordinates from TLRC to MNI space if the input\
            dataset has the +tlrc view.
        isovalue: Clusters will be formed only from contiguous voxels that also\
            have the same value.
        isomerge: Clusters will be formed from each distinct value in the dataset;\
            spatial contiguity will not be used.
        inmask: Use an internal mask from the dataset to eliminate voxels before\
            clustering.
        prefix: Write a new dataset that is a copy of the input, but with all\
            voxels not in a cluster set to zero; provide a prefix for the new\
            dataset.
        savemask: Write a new dataset that is an ordered mask where the largest\
            cluster is labeled '1', the next largest '2', and so forth.
        binary: Turn the output of '-savemask' into a binary (0 or 1) mask.

    Returns:
        Parameter dictionary.
    """
    params: V3dclust = {
        "@type": "afni/3dclust",
        "datasets": datasets,
        "nn1": nn1,
        "nn2": nn2,
        "nn3": nn3,
        "noabs": noabs,
        "summarize": summarize,
        "nosum": nosum,
        "verb": verb,
        "oned_format": oned_format,
        "no_oned_format": no_oned_format,
        "quiet": quiet,
        "mni": mni,
        "isovalue": isovalue,
        "isomerge": isomerge,
        "inmask": inmask,
        "binary": binary,
    }
    if rmm is not None:
        params["rmm"] = rmm
    if vmul is not None:
        params["vmul"] = vmul
    if prefix is not None:
        params["prefix"] = prefix
    if savemask is not None:
        params["savemask"] = savemask
    return params

def v_3dclust_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dclust."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("rmm", None) is not None:
        if not isinstance(params["rmm"], (float, int)):
            raise StyxValidationError(f'`rmm` has the wrong type: Received `{type(params["rmm"])}` expected `float`')
    if params.get("vmul", None) is not None:
        if not isinstance(params["vmul"], (float, int)):
            raise StyxValidationError(f'`vmul` has the wrong type: Received `{type(params["vmul"])}` expected `float`')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("nn1", None) is not None:
        if not isinstance(params["nn1"], bool):
            raise StyxValidationError(f'`nn1` has the wrong type: Received `{type(params["nn1"])}` expected `bool`')
    if params.get("nn2", None) is not None:
        if not isinstance(params["nn2"], bool):
            raise StyxValidationError(f'`nn2` has the wrong type: Received `{type(params["nn2"])}` expected `bool`')
    if params.get("nn3", None) is not None:
        if not isinstance(params["nn3"], bool):
            raise StyxValidationError(f'`nn3` has the wrong type: Received `{type(params["nn3"])}` expected `bool`')
    if params.get("noabs", None) is not None:
        if not isinstance(params["noabs"], bool):
            raise StyxValidationError(f'`noabs` has the wrong type: Received `{type(params["noabs"])}` expected `bool`')
    if params.get("summarize", None) is not None:
        if not isinstance(params["summarize"], bool):
            raise StyxValidationError(f'`summarize` has the wrong type: Received `{type(params["summarize"])}` expected `bool`')
    if params.get("nosum", None) is not None:
        if not isinstance(params["nosum"], bool):
            raise StyxValidationError(f'`nosum` has the wrong type: Received `{type(params["nosum"])}` expected `bool`')
    if params.get("verb", None) is not None:
        if not isinstance(params["verb"], bool):
            raise StyxValidationError(f'`verb` has the wrong type: Received `{type(params["verb"])}` expected `bool`')
    if params.get("oned_format", None) is not None:
        if not isinstance(params["oned_format"], bool):
            raise StyxValidationError(f'`oned_format` has the wrong type: Received `{type(params["oned_format"])}` expected `bool`')
    if params.get("no_oned_format", None) is not None:
        if not isinstance(params["no_oned_format"], bool):
            raise StyxValidationError(f'`no_oned_format` has the wrong type: Received `{type(params["no_oned_format"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("mni", None) is not None:
        if not isinstance(params["mni"], bool):
            raise StyxValidationError(f'`mni` has the wrong type: Received `{type(params["mni"])}` expected `bool`')
    if params.get("isovalue", None) is not None:
        if not isinstance(params["isovalue"], bool):
            raise StyxValidationError(f'`isovalue` has the wrong type: Received `{type(params["isovalue"])}` expected `bool`')
    if params.get("isomerge", None) is not None:
        if not isinstance(params["isomerge"], bool):
            raise StyxValidationError(f'`isomerge` has the wrong type: Received `{type(params["isomerge"])}` expected `bool`')
    if params.get("inmask", None) is not None:
        if not isinstance(params["inmask"], bool):
            raise StyxValidationError(f'`inmask` has the wrong type: Received `{type(params["inmask"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("savemask", None) is not None:
        if not isinstance(params["savemask"], str):
            raise StyxValidationError(f'`savemask` has the wrong type: Received `{type(params["savemask"])}` expected `str`')
    if params.get("binary", None) is not None:
        if not isinstance(params["binary"], bool):
            raise StyxValidationError(f'`binary` has the wrong type: Received `{type(params["binary"])}` expected `bool`')

def v_3dclust_cargs(params: V3dclust, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dclust")
    v_0 = params.get("rmm")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("vmul")
    if v_1 is not None:
        cargs.append(str(v_1))
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    if params.get("nn1"):
        cargs.append("-NN1")
    if params.get("nn2"):
        cargs.append("-NN2")
    if params.get("nn3"):
        cargs.append("-NN3")
    if params.get("noabs"):
        cargs.append("-noabs")
    if params.get("summarize"):
        cargs.append("-summarize")
    if params.get("nosum"):
        cargs.append("-nosum")
    if params.get("verb"):
        cargs.append("-verb")
    if params.get("oned_format"):
        cargs.append("-1Dformat")
    if params.get("no_oned_format"):
        cargs.append("-no_1Dformat")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("mni"):
        cargs.append("-mni")
    if params.get("isovalue"):
        cargs.append("-isovalue")
    if params.get("isomerge"):
        cargs.append("-isomerge")
    if params.get("inmask"):
        cargs.append("-inmask")
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    v_3 = params.get("savemask")
    if v_3 is not None:
        cargs.append("-savemask")
        cargs.append(v_3)
    if params.get("binary"):
        cargs.append("-binary")
    return cargs

def v_3dclust_outputs(params: V3dclust, execution: Execution) -> V3dclustOutputs:
    """Build the V3dclustOutputs object for this tool."""
    prefixed_output_v: OutputPathType | None = None
    ordered_mask_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        prefixed_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    __v1 = params.get("savemask")
    if __v1 is not None:
        ordered_mask_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.nii.gz')
    return V3dclustOutputs(
        root=root_v,
        prefixed_output=prefixed_output_v,
        ordered_mask_output=ordered_mask_output_v,
    )

def v_3dclust_execute(params: V3dclust, runner: Runner | None = None) -> V3dclustOutputs:
    """
    3dclust

    Performs simple-minded cluster detection in 3D datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dclust_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DCLUST_METADATA)
    execution.params(params)
    args = v_3dclust_cargs(params, execution)
    out = v_3dclust_outputs(params, execution)
    execution.run(args)
    return out

def v_3dclust(
    datasets: list[InputPathType],
    rmm: float | None = None,
    vmul: float | None = None,
    nn1: bool = False,
    nn2: bool = False,
    nn3: bool = False,
    noabs: bool = False,
    summarize: bool = False,
    nosum: bool = False,
    verb: bool = False,
    oned_format: bool = False,
    no_oned_format: bool = False,
    quiet: bool = False,
    mni: bool = False,
    isovalue: bool = False,
    isomerge: bool = False,
    inmask: bool = False,
    prefix: str | None = None,
    savemask: str | None = None,
    binary: bool = False,
    runner: Runner | None = None,
) -> V3dclustOutputs:
    """
    3dclust

    Performs simple-minded cluster detection in 3D datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        datasets: Input dataset(s). More than one allowed, but only the first\
            sub-brick of the dataset.
        rmm: Cluster connection radius in millimeters.
        vmul: Minimum cluster volume in micro-liters or minimum number of voxels if\
            negative.
        nn1: 1st nearest-neighbor clustering (faces touching).
        nn2: 2nd nearest-neighbor clustering (edges touching).
        nn3: 3rd nearest-neighbor clustering (corners touching).
        noabs: Use the signed voxel intensities for calculations.
        summarize: Write out only the total nonzero voxel count and volume for each\
            dataset.
        nosum: Suppress printout of the totals.
        verb: Print out a progress report to stderr as computations proceed.
        oned_format: Write output in 1D format (default).
        no_oned_format: Do not write output in 1D format.
        quiet: Suppress all non-essential output.
        mni: Transform output xyz-coordinates from TLRC to MNI space if the input\
            dataset has the +tlrc view.
        isovalue: Clusters will be formed only from contiguous voxels that also\
            have the same value.
        isomerge: Clusters will be formed from each distinct value in the dataset;\
            spatial contiguity will not be used.
        inmask: Use an internal mask from the dataset to eliminate voxels before\
            clustering.
        prefix: Write a new dataset that is a copy of the input, but with all\
            voxels not in a cluster set to zero; provide a prefix for the new\
            dataset.
        savemask: Write a new dataset that is an ordered mask where the largest\
            cluster is labeled '1', the next largest '2', and so forth.
        binary: Turn the output of '-savemask' into a binary (0 or 1) mask.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dclust_params(
        datasets=datasets,
        rmm=rmm,
        vmul=vmul,
        nn1=nn1,
        nn2=nn2,
        nn3=nn3,
        noabs=noabs,
        summarize=summarize,
        nosum=nosum,
        verb=verb,
        oned_format=oned_format,
        no_oned_format=no_oned_format,
        quiet=quiet,
        mni=mni,
        isovalue=isovalue,
        isomerge=isomerge,
        inmask=inmask,
        prefix=prefix,
        savemask=savemask,
        binary=binary,
    )
    return v_3dclust_execute(params, runner)

__all__ = [
    "V3dclust",
    "V3dclustOutputs",
    "V_3DCLUST_METADATA",
    "v_3dclust_cargs",
    "v_3dclust_outputs",
    "v_3dclust_params",
    "v_3dclust_execute",
    "v_3dclust_validate",
    "v_3dclust",
]