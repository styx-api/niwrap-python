# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TO_MNI_AWARP_METADATA = Metadata(
    id="@toMNI_Awarp",
    name="@toMNI_Awarp",
    package="afni",
)

ToMniAwarp = typing.TypedDict(
    "ToMniAwarp",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@toMNI_Awarp"]],
        "directory": str,
        "datasets": list[InputPathType],
    },
)

@dataclasses.dataclass
class ToMniAwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def to_mni_awarp_params(
    directory: str,
    datasets: list[InputPathType],
) -> ToMniAwarp:
    """
    Build parameters.

    Args:
        directory: Name of the directory to be created where results will be\
            stored.
        datasets: List of datasets to be transformed.

    Returns:
        Parameter dictionary.
    """
    params: ToMniAwarp = {
        "@type": "afni/@toMNI_Awarp",
        "directory": directory,
        "datasets": datasets,
    }
    return params

def to_mni_awarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ToMniAwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("directory", None) is None:
        raise StyxValidationError("`directory` must not be None")
    if not isinstance(params["directory"], str):
        raise StyxValidationError(f'`directory` has the wrong type: Received `{type(params["directory"])}` expected `str`')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def to_mni_awarp_cargs(params: ToMniAwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@toMNI_Awarp")
    cargs.append(params["directory"])
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    return cargs

def to_mni_awarp_outputs(params: ToMniAwarp, execution: Execution) -> ToMniAwarpOutputs:
    """Build the ToMniAwarpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ToMniAwarpOutputs(
        root=root_v,
    )

def to_mni_awarp_execute(params: ToMniAwarp, runner: Runner | None = None) -> ToMniAwarpOutputs:
    """
    @toMNI_Awarp

    Transforms skull-stripped datasets to 1x1x1 mm MNI space using an affine transformation.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    to_mni_awarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TO_MNI_AWARP_METADATA)
    execution.params(params)
    args = to_mni_awarp_cargs(params, execution)
    out = to_mni_awarp_outputs(params, execution)
    execution.run(args)
    return out

def to_mni_awarp(
    directory: str,
    datasets: list[InputPathType],
    runner: Runner | None = None,
) -> ToMniAwarpOutputs:
    """
    @toMNI_Awarp

    Transforms skull-stripped datasets to 1x1x1 mm MNI space using an affine transformation.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        directory: Name of the directory to be created where results will be\
            stored.
        datasets: List of datasets to be transformed.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = to_mni_awarp_params(
        directory=directory,
        datasets=datasets,
    )
    return to_mni_awarp_execute(params, runner)

__all__ = [
    "ToMniAwarp",
    "ToMniAwarpOutputs",
    "TO_MNI_AWARP_METADATA",
    "to_mni_awarp_cargs",
    "to_mni_awarp_outputs",
    "to_mni_awarp_params",
    "to_mni_awarp_execute",
    "to_mni_awarp_validate",
    "to_mni_awarp",
]