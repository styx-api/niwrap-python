# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_DFT_METADATA = Metadata(
    id="3dDFT",
    name="3dDFT",
    package="afni",
)

V3dDft = typing.TypedDict(
    "V3dDft",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dDFT"]],
        "infile": InputPathType,
        "prefix": str,
        "abs_output": typing.NotRequired[bool],
        "nfft": typing.NotRequired[float],
        "detrend": typing.NotRequired[bool],
        "taper": typing.NotRequired[float],
        "inverse": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dDftOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output dataset file"""
    outheader: OutputPathType
    """Output dataset header file"""

def v_3d_dft_params(
    infile: InputPathType,
    prefix: str,
    abs_output: bool = False,
    nfft: float | None = None,
    detrend: bool = False,
    taper: float | None = None,
    inverse: bool = False,
) -> V3dDft:
    """
    Build parameters.

    Args:
        infile: Input dataset (complex- or float-valued)
        prefix: Prefix for the output file
        abs_output: Output float dataset = abs(DFT)
        nfft: DFT length (must be >= number of time points)
        detrend: Least-squares remove linear drift before DFT
        taper: Fraction (0 <= f <= 1) of data to taper at ends (Hamming taper)
        inverse: Perform the inverse DFT

    Returns:
        Parameter dictionary.
    """
    params: V3dDft = {
        "@type": "afni/3dDFT",
        "infile": infile,
        "prefix": prefix,
        "abs_output": abs_output,
        "detrend": detrend,
        "inverse": inverse,
    }
    if nfft is not None:
        params["nfft"] = nfft
    if taper is not None:
        params["taper"] = taper
    return params

def v_3d_dft_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dDft."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("abs_output", None) is not None:
        if not isinstance(params["abs_output"], bool):
            raise StyxValidationError(f'`abs_output` has the wrong type: Received `{type(params["abs_output"])}` expected `bool`')
    if params.get("nfft", None) is not None:
        if not isinstance(params["nfft"], (float, int)):
            raise StyxValidationError(f'`nfft` has the wrong type: Received `{type(params["nfft"])}` expected `float`')
    if params.get("detrend", None) is not None:
        if not isinstance(params["detrend"], bool):
            raise StyxValidationError(f'`detrend` has the wrong type: Received `{type(params["detrend"])}` expected `bool`')
    if params.get("taper", None) is not None:
        if not isinstance(params["taper"], (float, int)):
            raise StyxValidationError(f'`taper` has the wrong type: Received `{type(params["taper"])}` expected `float`')
        if not (0 <= params["taper"] <= 1):
            raise StyxValidationError("Parameter `taper` must be between 0 and 1 (inclusive)")
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')

def v_3d_dft_cargs(params: V3dDft, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dDFT")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("abs_output"):
        cargs.append("-abs")
    v_0 = params.get("nfft")
    if v_0 is not None:
        cargs.append("-nfft")
        cargs.append(str(v_0))
    if params.get("detrend"):
        cargs.append("-detrend")
    v_1 = params.get("taper")
    if v_1 is not None:
        cargs.append("-taper")
        cargs.append(str(v_1))
    if params.get("inverse"):
        cargs.append("-inverse")
    return cargs

def v_3d_dft_outputs(params: V3dDft, execution: Execution) -> V3dDftOutputs:
    """Build the V3dDftOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK')
    outheader_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.HEAD')
    return V3dDftOutputs(
        root=root_v,
        outfile=outfile_v,
        outheader=outheader_v,
    )

def v_3d_dft_execute(params: V3dDft, runner: Runner | None = None) -> V3dDftOutputs:
    """
    3dDFT

    Performs Discrete Fourier Transform (DFT) along the time axis of a complex- or float-valued dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_dft_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_DFT_METADATA)
    execution.params(params)
    args = v_3d_dft_cargs(params, execution)
    out = v_3d_dft_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_dft(
    infile: InputPathType,
    prefix: str,
    abs_output: bool = False,
    nfft: float | None = None,
    detrend: bool = False,
    taper: float | None = None,
    inverse: bool = False,
    runner: Runner | None = None,
) -> V3dDftOutputs:
    """
    3dDFT

    Performs Discrete Fourier Transform (DFT) along the time axis of a complex- or float-valued dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input dataset (complex- or float-valued)
        prefix: Prefix for the output file
        abs_output: Output float dataset = abs(DFT)
        nfft: DFT length (must be >= number of time points)
        detrend: Least-squares remove linear drift before DFT
        taper: Fraction (0 <= f <= 1) of data to taper at ends (Hamming taper)
        inverse: Perform the inverse DFT
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_dft_params(
        infile=infile,
        prefix=prefix,
        abs_output=abs_output,
        nfft=nfft,
        detrend=detrend,
        taper=taper,
        inverse=inverse,
    )
    return v_3d_dft_execute(params, runner)

__all__ = [
    "V3dDft",
    "V3dDftOutputs",
    "V_3D_DFT_METADATA",
    "v_3d_dft_cargs",
    "v_3d_dft_outputs",
    "v_3d_dft_params",
    "v_3d_dft_execute",
    "v_3d_dft_validate",
    "v_3d_dft",
]