# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IM2NIML_METADATA = Metadata(
    id="im2niml",
    name="im2niml",
    package="afni",
)

Im2niml = typing.TypedDict(
    "Im2niml",
    {
        "@type": typing.NotRequired[typing.Literal["afni/im2niml"]],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class Im2nimlOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    niml_output: OutputPathType
    """NIML element"""

def im2niml_params(
    input_files: list[InputPathType],
) -> Im2niml:
    """
    Build parameters.

    Args:
        input_files: Input image file(s) (e.g. image.jpg)

    Returns:
        Parameter dictionary.
    """
    params: Im2niml = {
        "@type": "afni/im2niml",
        "input_files": input_files,
    }
    return params

def im2niml_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Im2niml."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def im2niml_cargs(params: Im2niml, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("im2niml")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def im2niml_outputs(params: Im2niml, execution: Execution) -> Im2nimlOutputs:
    """Build the Im2nimlOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    niml_output_v: OutputPathType = execution.output_file("stdout")
    return Im2nimlOutputs(
        root=root_v,
        niml_output=niml_output_v,
    )

def im2niml_execute(params: Im2niml, runner: Runner | None = None) -> Im2nimlOutputs:
    """
    im2niml

    Converts the input image(s) to a text-based NIML element and writes the result to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    im2niml_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IM2NIML_METADATA)
    execution.params(params)
    args = im2niml_cargs(params, execution)
    out = im2niml_outputs(params, execution)
    execution.run(args)
    return out

def im2niml(
    input_files: list[InputPathType],
    runner: Runner | None = None,
) -> Im2nimlOutputs:
    """
    im2niml

    Converts the input image(s) to a text-based NIML element and writes the result to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input image file(s) (e.g. image.jpg)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = im2niml_params(
        input_files=input_files,
    )
    return im2niml_execute(params, runner)

__all__ = [
    "Im2niml",
    "Im2nimlOutputs",
    "IM2NIML_METADATA",
    "im2niml_cargs",
    "im2niml_outputs",
    "im2niml_params",
    "im2niml_execute",
    "im2niml_validate",
    "im2niml",
]