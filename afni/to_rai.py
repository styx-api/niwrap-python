# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TO_RAI_METADATA = Metadata(
    id="@ToRAI",
    name="@ToRAI",
    package="afni",
)

ToRai = typing.TypedDict(
    "ToRai",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ToRAI"]],
        "coordinates": list[float],
        "orientation": str,
    },
)

@dataclasses.dataclass
class ToRaiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def to_rai_params(
    coordinates: list[float],
    orientation: str,
) -> ToRai:
    """
    Build parameters.

    Args:
        coordinates:
        orientation:

    Returns:
        Parameter dictionary.
    """
    params: ToRai = {
        "@type": "afni/@ToRAI",
        "coordinates": coordinates,
        "orientation": orientation,
    }
    return params

def to_rai_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ToRai."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("coordinates", None) is None:
        raise StyxValidationError("`coordinates` must not be None")
    if not isinstance(params["coordinates"], list):
        raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(params["coordinates"])}` expected `list[float]`')
    if not (3 <= len(params["coordinates"]) <= 3):
        raise StyxValidationError("Parameter `coordinates` must contain between 3 and 3 elements (inclusive)")
    for e in params["coordinates"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("orientation", None) is None:
        raise StyxValidationError("`orientation` must not be None")
    if not isinstance(params["orientation"], str):
        raise StyxValidationError(f'`orientation` has the wrong type: Received `{type(params["orientation"])}` expected `str`')

def to_rai_cargs(params: ToRai, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ToRAI")
    cargs.append("-xyz")
    for item0 in params["coordinates"]:
        cargs.append(str(item0))
    cargs.append("-or")
    cargs.append(params["orientation"])
    return cargs

def to_rai_outputs(params: ToRai, execution: Execution) -> ToRaiOutputs:
    """Build the ToRaiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ToRaiOutputs(
        root=root_v,
    )

def to_rai_execute(params: ToRai, runner: Runner | None = None) -> ToRaiOutputs:
    """
    @ToRAI

    Tool to change the ORIENT coordinates to RAI

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    to_rai_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TO_RAI_METADATA)
    execution.params(params)
    args = to_rai_cargs(params, execution)
    out = to_rai_outputs(params, execution)
    execution.run(args)
    return out

def to_rai(
    coordinates: list[float],
    orientation: str,
    runner: Runner | None = None,
) -> ToRaiOutputs:
    """
    @ToRAI

    Tool to change the ORIENT coordinates to RAI

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        coordinates:
        orientation:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = to_rai_params(
        coordinates=coordinates,
        orientation=orientation,
    )
    return to_rai_execute(params, runner)

__all__ = [
    "ToRai",
    "ToRaiOutputs",
    "TO_RAI_METADATA",
    "to_rai_cargs",
    "to_rai_outputs",
    "to_rai_params",
    "to_rai_execute",
    "to_rai_validate",
    "to_rai",
]