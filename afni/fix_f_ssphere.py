# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIX_F_SSPHERE_METADATA = Metadata(
    id="@fix_FSsphere",
    name="@fix_FSsphere",
    package="afni",
)

FixFSsphere = typing.TypedDict(
    "FixFSsphere",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@fix_FSsphere"]],
        "spec_file": InputPathType,
        "sphere_file": InputPathType,
        "num_iterations": typing.NotRequired[int],
        "extent_lim": typing.NotRequired[float],
        "project_first": typing.NotRequired[bool],
        "keep_temp": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FixFSsphereOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_surface: OutputPathType
    """Corrected surface"""

def fix_f_ssphere_params(
    spec_file: InputPathType,
    sphere_file: InputPathType,
    num_iterations: int | None = None,
    extent_lim: float | None = None,
    project_first: bool = False,
    keep_temp: bool = False,
) -> FixFSsphere:
    """
    Build parameters.

    Args:
        spec_file: Spec file
        sphere_file: SPHERE.asc is the sphere to be used
        num_iterations: Number of local smoothing operations. Default is 3000
        extent_lim: Extent, in mm, by which troubled sections are fattened. Default\
            is 6
        project_first: Project to a sphere, before smoothing. Default is 0
        keep_temp: Keep temporary files

    Returns:
        Parameter dictionary.
    """
    params: FixFSsphere = {
        "@type": "afni/@fix_FSsphere",
        "spec_file": spec_file,
        "sphere_file": sphere_file,
        "project_first": project_first,
        "keep_temp": keep_temp,
    }
    if num_iterations is not None:
        params["num_iterations"] = num_iterations
    if extent_lim is not None:
        params["extent_lim"] = extent_lim
    return params

def fix_f_ssphere_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FixFSsphere."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spec_file", None) is None:
        raise StyxValidationError("`spec_file` must not be None")
    if not isinstance(params["spec_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`spec_file` has the wrong type: Received `{type(params["spec_file"])}` expected `InputPathType`')
    if params.get("sphere_file", None) is None:
        raise StyxValidationError("`sphere_file` must not be None")
    if not isinstance(params["sphere_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`sphere_file` has the wrong type: Received `{type(params["sphere_file"])}` expected `InputPathType`')
    if params.get("num_iterations", None) is not None:
        if not isinstance(params["num_iterations"], int):
            raise StyxValidationError(f'`num_iterations` has the wrong type: Received `{type(params["num_iterations"])}` expected `int`')
    if params.get("extent_lim", None) is not None:
        if not isinstance(params["extent_lim"], (float, int)):
            raise StyxValidationError(f'`extent_lim` has the wrong type: Received `{type(params["extent_lim"])}` expected `float`')
    if params.get("project_first", None) is not None:
        if not isinstance(params["project_first"], bool):
            raise StyxValidationError(f'`project_first` has the wrong type: Received `{type(params["project_first"])}` expected `bool`')
    if params.get("keep_temp", None) is not None:
        if not isinstance(params["keep_temp"], bool):
            raise StyxValidationError(f'`keep_temp` has the wrong type: Received `{type(params["keep_temp"])}` expected `bool`')

def fix_f_ssphere_cargs(params: FixFSsphere, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@fix_FSsphere")
    cargs.append("-spec")
    cargs.append(execution.input_file(params["spec_file"]))
    cargs.append("-sphere")
    cargs.append(execution.input_file(params["sphere_file"]))
    v_0 = params.get("num_iterations")
    if v_0 is not None:
        cargs.append("-niter")
        cargs.append(str(v_0))
    v_1 = params.get("extent_lim")
    if v_1 is not None:
        cargs.append("-lim")
        cargs.append(str(v_1))
    if params.get("project_first"):
        cargs.append("-project_first")
    if params.get("keep_temp"):
        cargs.append("-keep_temp")
    return cargs

def fix_f_ssphere_outputs(params: FixFSsphere, execution: Execution) -> FixFSsphereOutputs:
    """Build the FixFSsphereOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_surface_v: OutputPathType = execution.output_file("[SPHERE]_fxd.asc")
    return FixFSsphereOutputs(
        root=root_v,
        corrected_surface=corrected_surface_v,
    )

def fix_f_ssphere_execute(params: FixFSsphere, runner: Runner | None = None) -> FixFSsphereOutputs:
    """
    @fix_FSsphere

    Tool for fixing errors in FreeSurfer spherical surfaces

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fix_f_ssphere_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIX_F_SSPHERE_METADATA)
    execution.params(params)
    args = fix_f_ssphere_cargs(params, execution)
    out = fix_f_ssphere_outputs(params, execution)
    execution.run(args)
    return out

def fix_f_ssphere(
    spec_file: InputPathType,
    sphere_file: InputPathType,
    num_iterations: int | None = None,
    extent_lim: float | None = None,
    project_first: bool = False,
    keep_temp: bool = False,
    runner: Runner | None = None,
) -> FixFSsphereOutputs:
    """
    @fix_FSsphere

    Tool for fixing errors in FreeSurfer spherical surfaces

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        spec_file: Spec file
        sphere_file: SPHERE.asc is the sphere to be used
        num_iterations: Number of local smoothing operations. Default is 3000
        extent_lim: Extent, in mm, by which troubled sections are fattened. Default\
            is 6
        project_first: Project to a sphere, before smoothing. Default is 0
        keep_temp: Keep temporary files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fix_f_ssphere_params(
        spec_file=spec_file,
        sphere_file=sphere_file,
        num_iterations=num_iterations,
        extent_lim=extent_lim,
        project_first=project_first,
        keep_temp=keep_temp,
    )
    return fix_f_ssphere_execute(params, runner)

__all__ = [
    "FixFSsphere",
    "FixFSsphereOutputs",
    "FIX_F_SSPHERE_METADATA",
    "fix_f_ssphere_cargs",
    "fix_f_ssphere_outputs",
    "fix_f_ssphere_params",
    "fix_f_ssphere_execute",
    "fix_f_ssphere_validate",
    "fix_f_ssphere",
]