# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

QUOTIZE_METADATA = Metadata(
    id="quotize",
    name="quotize",
    package="afni",
)

Quotize = typing.TypedDict(
    "Quotize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/quotize"]],
        "name": str,
        "input_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class QuotizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    array_output: OutputPathType
    """Generated C array of strings file"""

def quotize_params(
    name: str,
    input_file: InputPathType,
    output_file: str,
) -> Quotize:
    """
    Build parameters.

    Args:
        name: The name to be used for the array of strings.
        input_file: Input text file to be converted.
        output_file: Output file which will contain the C array of strings.

    Returns:
        Parameter dictionary.
    """
    params: Quotize = {
        "@type": "afni/quotize",
        "name": name,
        "input_file": input_file,
        "output_file": output_file,
    }
    return params

def quotize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Quotize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("name", None) is None:
        raise StyxValidationError("`name` must not be None")
    if not isinstance(params["name"], str):
        raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def quotize_cargs(params: Quotize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("quotize")
    cargs.append(params["name"])
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    return cargs

def quotize_outputs(params: Quotize, execution: Execution) -> QuotizeOutputs:
    """Build the QuotizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    array_output_v: OutputPathType = execution.output_file(params["output_file"])
    return QuotizeOutputs(
        root=root_v,
        array_output=array_output_v,
    )

def quotize_execute(params: Quotize, runner: Runner | None = None) -> QuotizeOutputs:
    """
    quotize

    Turns a text file into a C array of strings initialized into an array 'char *name[]'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    quotize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(QUOTIZE_METADATA)
    execution.params(params)
    args = quotize_cargs(params, execution)
    out = quotize_outputs(params, execution)
    execution.run(args)
    return out

def quotize(
    name: str,
    input_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> QuotizeOutputs:
    """
    quotize

    Turns a text file into a C array of strings initialized into an array 'char *name[]'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        name: The name to be used for the array of strings.
        input_file: Input text file to be converted.
        output_file: Output file which will contain the C array of strings.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = quotize_params(
        name=name,
        input_file=input_file,
        output_file=output_file,
    )
    return quotize_execute(params, runner)

__all__ = [
    "Quotize",
    "QuotizeOutputs",
    "QUOTIZE_METADATA",
    "quotize_cargs",
    "quotize_outputs",
    "quotize_params",
    "quotize_execute",
    "quotize_validate",
    "quotize",
]