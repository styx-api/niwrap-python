# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1DSUM_METADATA = Metadata(
    id="1dsum",
    name="1dsum",
    package="afni",
)

V1dsum = typing.TypedDict(
    "V1dsum",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dsum"]],
        "input_files": list[InputPathType],
        "ignore_rows": typing.NotRequired[float],
        "use_rows": typing.NotRequired[float],
        "mean_flag": typing.NotRequired[bool],
        "nocomment_flag": typing.NotRequired[bool],
        "okempty_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V1dsumOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Sum or average of columns in the input files"""

def v_1dsum_params(
    input_files: list[InputPathType],
    ignore_rows: float | None = None,
    use_rows: float | None = None,
    mean_flag: bool = False,
    nocomment_flag: bool = False,
    okempty_flag: bool = False,
) -> V1dsum:
    """
    Build parameters.

    Args:
        input_files: Input ASCII files with numbers arranged in rows and columns.
        ignore_rows: Skip the first nn rows of each file
        use_rows: Use only mm rows from each file
        mean_flag: Compute the average instead of the sum
        nocomment_flag: Do not reproduce comments from the header of the first\
            input file to the output.
        okempty_flag: If encountering an empty 1D file, print 0 and exit quietly\
            instead of exiting with an error message.

    Returns:
        Parameter dictionary.
    """
    params: V1dsum = {
        "@type": "afni/1dsum",
        "input_files": input_files,
        "mean_flag": mean_flag,
        "nocomment_flag": nocomment_flag,
        "okempty_flag": okempty_flag,
    }
    if ignore_rows is not None:
        params["ignore_rows"] = ignore_rows
    if use_rows is not None:
        params["use_rows"] = use_rows
    return params

def v_1dsum_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dsum."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("ignore_rows", None) is not None:
        if not isinstance(params["ignore_rows"], (float, int)):
            raise StyxValidationError(f'`ignore_rows` has the wrong type: Received `{type(params["ignore_rows"])}` expected `float`')
    if params.get("use_rows", None) is not None:
        if not isinstance(params["use_rows"], (float, int)):
            raise StyxValidationError(f'`use_rows` has the wrong type: Received `{type(params["use_rows"])}` expected `float`')
    if params.get("mean_flag", None) is not None:
        if not isinstance(params["mean_flag"], bool):
            raise StyxValidationError(f'`mean_flag` has the wrong type: Received `{type(params["mean_flag"])}` expected `bool`')
    if params.get("nocomment_flag", None) is not None:
        if not isinstance(params["nocomment_flag"], bool):
            raise StyxValidationError(f'`nocomment_flag` has the wrong type: Received `{type(params["nocomment_flag"])}` expected `bool`')
    if params.get("okempty_flag", None) is not None:
        if not isinstance(params["okempty_flag"], bool):
            raise StyxValidationError(f'`okempty_flag` has the wrong type: Received `{type(params["okempty_flag"])}` expected `bool`')

def v_1dsum_cargs(params: V1dsum, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dsum")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("ignore_rows")
    if v_0 is not None:
        cargs.append("-ignore")
        cargs.append(str(v_0))
    v_1 = params.get("use_rows")
    if v_1 is not None:
        cargs.append("-use")
        cargs.append(str(v_1))
    if params.get("mean_flag"):
        cargs.append("-mean")
    if params.get("nocomment_flag"):
        cargs.append("-nocomment")
    if params.get("okempty_flag"):
        cargs.append("-OKempty")
    return cargs

def v_1dsum_outputs(params: V1dsum, execution: Execution) -> V1dsumOutputs:
    """Build the V1dsumOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("output.txt")
    return V1dsumOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_1dsum_execute(params: V1dsum, runner: Runner | None = None) -> V1dsumOutputs:
    """
    1dsum

    Sum or average columns of ASCII files with numbers arranged in rows and columns.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1dsum_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1DSUM_METADATA)
    execution.params(params)
    args = v_1dsum_cargs(params, execution)
    out = v_1dsum_outputs(params, execution)
    execution.run(args)
    return out

def v_1dsum(
    input_files: list[InputPathType],
    ignore_rows: float | None = None,
    use_rows: float | None = None,
    mean_flag: bool = False,
    nocomment_flag: bool = False,
    okempty_flag: bool = False,
    runner: Runner | None = None,
) -> V1dsumOutputs:
    """
    1dsum

    Sum or average columns of ASCII files with numbers arranged in rows and columns.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input ASCII files with numbers arranged in rows and columns.
        ignore_rows: Skip the first nn rows of each file
        use_rows: Use only mm rows from each file
        mean_flag: Compute the average instead of the sum
        nocomment_flag: Do not reproduce comments from the header of the first\
            input file to the output.
        okempty_flag: If encountering an empty 1D file, print 0 and exit quietly\
            instead of exiting with an error message.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1dsum_params(
        input_files=input_files,
        ignore_rows=ignore_rows,
        use_rows=use_rows,
        mean_flag=mean_flag,
        nocomment_flag=nocomment_flag,
        okempty_flag=okempty_flag,
    )
    return v_1dsum_execute(params, runner)

__all__ = [
    "V1dsum",
    "V1dsumOutputs",
    "V_1DSUM_METADATA",
    "v_1dsum_cargs",
    "v_1dsum_outputs",
    "v_1dsum_params",
    "v_1dsum_execute",
    "v_1dsum_validate",
    "v_1dsum",
]