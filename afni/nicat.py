# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NICAT_METADATA = Metadata(
    id="nicat",
    name="nicat",
    package="afni",
)

Nicat = typing.TypedDict(
    "Nicat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/nicat"]],
        "stream_spec": str,
        "reopen": typing.NotRequired[str],
        "copy_stream": typing.NotRequired[bool],
        "read_only": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class NicatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def nicat_params(
    stream_spec: str,
    reopen: str | None = None,
    copy_stream: bool = False,
    read_only: bool = False,
) -> Nicat:
    """
    Build parameters.

    Args:
        stream_spec: Stream specification (e.g., tcp:localhost:4444)
        reopen: Reopen the stream after connection to the stream specified by the\
            given value.
        copy_stream: Copy the stream to stdout instead; the 'streamspec' will be\
            opened for reading.
        read_only: Read the stream but don't copy to stdout.

    Returns:
        Parameter dictionary.
    """
    params: Nicat = {
        "@type": "afni/nicat",
        "stream_spec": stream_spec,
        "copy_stream": copy_stream,
        "read_only": read_only,
    }
    if reopen is not None:
        params["reopen"] = reopen
    return params

def nicat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Nicat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stream_spec", None) is None:
        raise StyxValidationError("`stream_spec` must not be None")
    if not isinstance(params["stream_spec"], str):
        raise StyxValidationError(f'`stream_spec` has the wrong type: Received `{type(params["stream_spec"])}` expected `str`')
    if params.get("reopen", None) is not None:
        if not isinstance(params["reopen"], str):
            raise StyxValidationError(f'`reopen` has the wrong type: Received `{type(params["reopen"])}` expected `str`')
    if params.get("copy_stream", None) is not None:
        if not isinstance(params["copy_stream"], bool):
            raise StyxValidationError(f'`copy_stream` has the wrong type: Received `{type(params["copy_stream"])}` expected `bool`')
    if params.get("read_only", None) is not None:
        if not isinstance(params["read_only"], bool):
            raise StyxValidationError(f'`read_only` has the wrong type: Received `{type(params["read_only"])}` expected `bool`')

def nicat_cargs(params: Nicat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("nicat")
    cargs.append(params["stream_spec"])
    v_0 = params.get("reopen")
    if v_0 is not None:
        cargs.append("-reopen")
        cargs.append(v_0)
    if params.get("copy_stream"):
        cargs.append("-r")
    if params.get("read_only"):
        cargs.append("-R")
    return cargs

def nicat_outputs(params: Nicat, execution: Execution) -> NicatOutputs:
    """Build the NicatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return NicatOutputs(
        root=root_v,
    )

def nicat_execute(params: Nicat, runner: Runner | None = None) -> NicatOutputs:
    """
    nicat

    Copies stdin to the NIML stream, which will be opened for writing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    nicat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NICAT_METADATA)
    execution.params(params)
    args = nicat_cargs(params, execution)
    out = nicat_outputs(params, execution)
    execution.run(args)
    return out

def nicat(
    stream_spec: str,
    reopen: str | None = None,
    copy_stream: bool = False,
    read_only: bool = False,
    runner: Runner | None = None,
) -> NicatOutputs:
    """
    nicat

    Copies stdin to the NIML stream, which will be opened for writing.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        stream_spec: Stream specification (e.g., tcp:localhost:4444)
        reopen: Reopen the stream after connection to the stream specified by the\
            given value.
        copy_stream: Copy the stream to stdout instead; the 'streamspec' will be\
            opened for reading.
        read_only: Read the stream but don't copy to stdout.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = nicat_params(
        stream_spec=stream_spec,
        reopen=reopen,
        copy_stream=copy_stream,
        read_only=read_only,
    )
    return nicat_execute(params, runner)

__all__ = [
    "Nicat",
    "NicatOutputs",
    "NICAT_METADATA",
    "nicat_cargs",
    "nicat_outputs",
    "nicat_params",
    "nicat_execute",
    "nicat_validate",
    "nicat",
]