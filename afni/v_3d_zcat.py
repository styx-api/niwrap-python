# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ZCAT_METADATA = Metadata(
    id="3dZcat",
    name="3dZcat",
    package="afni",
)

V3dZcatDatum = typing.Literal["byte"] | typing.Literal["short"] | typing.Literal["float"]

V3dZcat = typing.TypedDict(
    "V3dZcat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dZcat"]],
        "prefix": typing.NotRequired[str],
        "datum": typing.NotRequired[V3dZcatDatum],
        "fscale": typing.NotRequired[bool],
        "nscale": typing.NotRequired[bool],
        "verb": typing.NotRequired[bool],
        "frugal": typing.NotRequired[bool],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class V3dZcatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_head: OutputPathType | None
    """AFNI HEAD file of the output dataset"""
    out_brik: OutputPathType | None
    """AFNI BRIK file of the output dataset"""

def v_3d_zcat_params(
    input_files: list[InputPathType],
    prefix: str | None = None,
    datum: V3dZcatDatum | None = None,
    fscale: bool = False,
    nscale: bool = False,
    verb: bool = False,
    frugal: bool = False,
) -> V3dZcat:
    """
    Build parameters.

    Args:
        input_files: Input datasets
        prefix: Use 'pname' for the output dataset prefix name. [default='zcat']
        datum: Coerce the output data to be stored as the given type, which may be\
            byte, short, or float.
        fscale: Force scaling of the output to the maximum integer range. This only\
            has effect if the output datum is byte or short (either forced or\
            defaulted). This option is sometimes necessary to eliminate unpleasant\
            truncation artifacts.
        nscale: Don't do any scaling on output to byte or short datasets. This may\
            be especially useful when operating on mask datasets whose output\
            values are only 0's and 1's.
        verb: Print out some verbosity as the program proceeds.
        frugal: Be 'frugal' in the use of memory, at the cost of I/O time. Only\
            needed if the program runs out of memory. Note frugality cannot be\
            combined with NIFTI output

    Returns:
        Parameter dictionary.
    """
    params: V3dZcat = {
        "@type": "afni/3dZcat",
        "input_files": input_files,
        "fscale": fscale,
        "nscale": nscale,
        "verb": verb,
        "frugal": frugal,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if datum is not None:
        params["datum"] = datum
    return params

def v_3d_zcat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dZcat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("datum", None) is not None:
        if not isinstance(params["datum"], str):
            raise StyxValidationError(f'`datum` has the wrong type: Received `{type(params["datum"])}` expected `V3dZcatDatum`')
        if params["datum"] not in ["byte", "short", "float"]:
            raise StyxValidationError("Parameter `datum` must be one of [\"byte\", \"short\", \"float\"]")
    if params.get("fscale", None) is not None:
        if not isinstance(params["fscale"], bool):
            raise StyxValidationError(f'`fscale` has the wrong type: Received `{type(params["fscale"])}` expected `bool`')
    if params.get("nscale", None) is not None:
        if not isinstance(params["nscale"], bool):
            raise StyxValidationError(f'`nscale` has the wrong type: Received `{type(params["nscale"])}` expected `bool`')
    if params.get("verb", None) is not None:
        if not isinstance(params["verb"], bool):
            raise StyxValidationError(f'`verb` has the wrong type: Received `{type(params["verb"])}` expected `bool`')
    if params.get("frugal", None) is not None:
        if not isinstance(params["frugal"], bool):
            raise StyxValidationError(f'`frugal` has the wrong type: Received `{type(params["frugal"])}` expected `bool`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def v_3d_zcat_cargs(params: V3dZcat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dZcat")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("datum")
    if v_1 is not None:
        cargs.append("-datum")
        cargs.append(str(v_1))
    if params.get("fscale"):
        cargs.append("-fscale")
    if params.get("nscale"):
        cargs.append("-nscale")
    if params.get("verb"):
        cargs.append("-verb")
    if params.get("frugal"):
        cargs.append("-frugal")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def v_3d_zcat_outputs(params: V3dZcat, execution: Execution) -> V3dZcatOutputs:
    """Build the V3dZcatOutputs object for this tool."""
    out_head_v: OutputPathType | None = None
    out_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        out_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        out_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    return V3dZcatOutputs(
        root=root_v,
        out_head=out_head_v,
        out_brik=out_brik_v,
    )

def v_3d_zcat_execute(params: V3dZcat, runner: Runner | None = None) -> V3dZcatOutputs:
    """
    3dZcat

    Concatenates datasets in the slice (z) direction

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_zcat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ZCAT_METADATA)
    execution.params(params)
    args = v_3d_zcat_cargs(params, execution)
    out = v_3d_zcat_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_zcat(
    input_files: list[InputPathType],
    prefix: str | None = None,
    datum: V3dZcatDatum | None = None,
    fscale: bool = False,
    nscale: bool = False,
    verb: bool = False,
    frugal: bool = False,
    runner: Runner | None = None,
) -> V3dZcatOutputs:
    """
    3dZcat

    Concatenates datasets in the slice (z) direction

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input datasets
        prefix: Use 'pname' for the output dataset prefix name. [default='zcat']
        datum: Coerce the output data to be stored as the given type, which may be\
            byte, short, or float.
        fscale: Force scaling of the output to the maximum integer range. This only\
            has effect if the output datum is byte or short (either forced or\
            defaulted). This option is sometimes necessary to eliminate unpleasant\
            truncation artifacts.
        nscale: Don't do any scaling on output to byte or short datasets. This may\
            be especially useful when operating on mask datasets whose output\
            values are only 0's and 1's.
        verb: Print out some verbosity as the program proceeds.
        frugal: Be 'frugal' in the use of memory, at the cost of I/O time. Only\
            needed if the program runs out of memory. Note frugality cannot be\
            combined with NIFTI output
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_zcat_params(
        input_files=input_files,
        prefix=prefix,
        datum=datum,
        fscale=fscale,
        nscale=nscale,
        verb=verb,
        frugal=frugal,
    )
    return v_3d_zcat_execute(params, runner)

__all__ = [
    "V3dZcat",
    "V3dZcatOutputs",
    "V_3D_ZCAT_METADATA",
    "v_3d_zcat_cargs",
    "v_3d_zcat_outputs",
    "v_3d_zcat_params",
    "v_3d_zcat_execute",
    "v_3d_zcat_validate",
    "v_3d_zcat",
]