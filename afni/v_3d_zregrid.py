# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ZREGRID_METADATA = Metadata(
    id="3dZregrid",
    name="3dZregrid",
    package="afni",
)

V3dZregrid = typing.TypedDict(
    "V3dZregrid",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dZregrid"]],
        "z_thickness": typing.NotRequired[float],
        "slice_count": typing.NotRequired[float],
        "z_size": typing.NotRequired[float],
        "prefix": typing.NotRequired[str],
        "infile": InputPathType,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dZregridOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile_head: OutputPathType | None
    """Output dataset with new grid"""
    outfile_brik: OutputPathType | None
    """Output dataset with new grid"""

def v_3d_zregrid_params(
    infile: InputPathType,
    z_thickness: float | None = None,
    slice_count: float | None = None,
    z_size: float | None = None,
    prefix: str | None = None,
    verbose: bool = False,
) -> V3dZregrid:
    """
    Build parameters.

    Args:
        infile: Input dataset
        z_thickness: Set slice thickness to D mm
        slice_count: Set slice count to N
        z_size: Set thickness of dataset (center-to-center of first and last\
            slices) to Z mm
        prefix: Write result to dataset with prefix P
        verbose: Write progress reports to stderr

    Returns:
        Parameter dictionary.
    """
    params: V3dZregrid = {
        "@type": "afni/3dZregrid",
        "infile": infile,
        "verbose": verbose,
    }
    if z_thickness is not None:
        params["z_thickness"] = z_thickness
    if slice_count is not None:
        params["slice_count"] = slice_count
    if z_size is not None:
        params["z_size"] = z_size
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_zregrid_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dZregrid."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("z_thickness", None) is not None:
        if not isinstance(params["z_thickness"], (float, int)):
            raise StyxValidationError(f'`z_thickness` has the wrong type: Received `{type(params["z_thickness"])}` expected `float`')
    if params.get("slice_count", None) is not None:
        if not isinstance(params["slice_count"], (float, int)):
            raise StyxValidationError(f'`slice_count` has the wrong type: Received `{type(params["slice_count"])}` expected `float`')
    if params.get("z_size", None) is not None:
        if not isinstance(params["z_size"], (float, int)):
            raise StyxValidationError(f'`z_size` has the wrong type: Received `{type(params["z_size"])}` expected `float`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def v_3d_zregrid_cargs(params: V3dZregrid, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dZregrid")
    v_0 = params.get("z_thickness")
    if v_0 is not None:
        cargs.append("-dz")
        cargs.append(str(v_0))
    v_1 = params.get("slice_count")
    if v_1 is not None:
        cargs.append("-nz")
        cargs.append(str(v_1))
    v_2 = params.get("z_size")
    if v_2 is not None:
        cargs.append("-zsize")
        cargs.append(str(v_2))
    v_3 = params.get("prefix")
    if v_3 is not None:
        cargs.append("-prefix")
        cargs.append(v_3)
    cargs.append(execution.input_file(params["infile"]))
    if params.get("verbose"):
        cargs.append("-verb")
    return cargs

def v_3d_zregrid_outputs(params: V3dZregrid, execution: Execution) -> V3dZregridOutputs:
    """Build the V3dZregridOutputs object for this tool."""
    outfile_head_v: OutputPathType | None = None
    outfile_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        outfile_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+orig.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        outfile_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+orig.BRIK')
    return V3dZregridOutputs(
        root=root_v,
        outfile_head=outfile_head_v,
        outfile_brik=outfile_brik_v,
    )

def v_3d_zregrid_execute(params: V3dZregrid, runner: Runner | None = None) -> V3dZregridOutputs:
    """
    3dZregrid

    Alters the input dataset's slice thickness and/or number.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_zregrid_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ZREGRID_METADATA)
    execution.params(params)
    args = v_3d_zregrid_cargs(params, execution)
    out = v_3d_zregrid_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_zregrid(
    infile: InputPathType,
    z_thickness: float | None = None,
    slice_count: float | None = None,
    z_size: float | None = None,
    prefix: str | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> V3dZregridOutputs:
    """
    3dZregrid

    Alters the input dataset's slice thickness and/or number.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input dataset
        z_thickness: Set slice thickness to D mm
        slice_count: Set slice count to N
        z_size: Set thickness of dataset (center-to-center of first and last\
            slices) to Z mm
        prefix: Write result to dataset with prefix P
        verbose: Write progress reports to stderr
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_zregrid_params(
        infile=infile,
        z_thickness=z_thickness,
        slice_count=slice_count,
        z_size=z_size,
        prefix=prefix,
        verbose=verbose,
    )
    return v_3d_zregrid_execute(params, runner)

__all__ = [
    "V3dZregrid",
    "V3dZregridOutputs",
    "V_3D_ZREGRID_METADATA",
    "v_3d_zregrid_cargs",
    "v_3d_zregrid_outputs",
    "v_3d_zregrid_params",
    "v_3d_zregrid_execute",
    "v_3d_zregrid_validate",
    "v_3d_zregrid",
]