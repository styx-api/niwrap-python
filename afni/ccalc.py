# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CCALC_METADATA = Metadata(
    id="ccalc",
    name="ccalc",
    package="afni",
)

Ccalc = typing.TypedDict(
    "Ccalc",
    {
        "@type": typing.NotRequired[typing.Literal["afni/ccalc"]],
        "format": typing.NotRequired[str],
        "expr": str,
    },
)

@dataclasses.dataclass
class CcalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def ccalc_params(
    expr: str,
    format: str | None = None,
) -> Ccalc:
    """
    Build parameters.

    Args:
        expr: Evaluate an expression specified on command line, return answer and\
            quit
        format: Format output in a nice form. Choose from 'double', 'nice', 'int',\
            'rint', 'cint', 'fint', or custom format string (e.g., %n.mf)

    Returns:
        Parameter dictionary.
    """
    params: Ccalc = {
        "@type": "afni/ccalc",
        "expr": expr,
    }
    if format is not None:
        params["format"] = format
    return params

def ccalc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ccalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("format", None) is not None:
        if not isinstance(params["format"], str):
            raise StyxValidationError(f'`format` has the wrong type: Received `{type(params["format"])}` expected `str`')
    if params.get("expr", None) is None:
        raise StyxValidationError("`expr` must not be None")
    if not isinstance(params["expr"], str):
        raise StyxValidationError(f'`expr` has the wrong type: Received `{type(params["expr"])}` expected `str`')

def ccalc_cargs(params: Ccalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ccalc")
    v_0 = params.get("format")
    if v_0 is not None:
        cargs.append("-form")
        cargs.append(v_0)
    cargs.append("-eval")
    cargs.append(params["expr"])
    return cargs

def ccalc_outputs(params: Ccalc, execution: Execution) -> CcalcOutputs:
    """Build the CcalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CcalcOutputs(
        root=root_v,
    )

def ccalc_execute(params: Ccalc, runner: Runner | None = None) -> CcalcOutputs:
    """
    ccalc

    Command line calculator with formatted output options

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ccalc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CCALC_METADATA)
    execution.params(params)
    args = ccalc_cargs(params, execution)
    out = ccalc_outputs(params, execution)
    execution.run(args)
    return out

def ccalc(
    expr: str,
    format: str | None = None,
    runner: Runner | None = None,
) -> CcalcOutputs:
    """
    ccalc

    Command line calculator with formatted output options

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        expr: Evaluate an expression specified on command line, return answer and\
            quit
        format: Format output in a nice form. Choose from 'double', 'nice', 'int',\
            'rint', 'cint', 'fint', or custom format string (e.g., %n.mf)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ccalc_params(
        expr=expr,
        format=format,
    )
    return ccalc_execute(params, runner)

__all__ = [
    "Ccalc",
    "CcalcOutputs",
    "CCALC_METADATA",
    "ccalc_cargs",
    "ccalc_outputs",
    "ccalc_params",
    "ccalc_execute",
    "ccalc_validate",
    "ccalc",
]