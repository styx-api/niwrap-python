# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NEURO_DECONVOLVE_PY_METADATA = Metadata(
    id="neuro_deconvolve.py",
    name="neuro_deconvolve.py",
    package="afni",
)

NeuroDeconvolvePy = typing.TypedDict(
    "NeuroDeconvolvePy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/neuro_deconvolve.py"]],
        "input_file": InputPathType,
        "prefix": str,
        "script": str,
        "kernel": typing.NotRequired[str],
        "kernel_file": typing.NotRequired[str],
        "mask_dset": typing.NotRequired[InputPathType],
        "old_style": typing.NotRequired[bool],
        "tr": typing.NotRequired[float],
        "tr_nup": typing.NotRequired[float],
        "verbosity": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class NeuroDeconvolvePyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_head: OutputPathType
    """Main default output head file"""
    output_brik: OutputPathType
    """Main default output BRIK file"""
    kernel_file_out: OutputPathType | None
    """File storing the response kernel"""

def neuro_deconvolve_py_params(
    input_file: InputPathType,
    prefix: str,
    script: str,
    kernel: str | None = None,
    kernel_file: str | None = None,
    mask_dset: InputPathType | None = None,
    old_style: bool = False,
    tr: float | None = None,
    tr_nup: float | None = None,
    verbosity: float | None = None,
) -> NeuroDeconvolvePy:
    """
    Build parameters.

    Args:
        input_file: Set the data to deconvolve
        prefix: Set the prefix for output filenames
        script: Specify the name of the output script
        kernel: Set the response kernel
        kernel_file: Set the filename to store the kernel in; should be at the\
            upsampled TR
        mask_dset: Set a mask dataset for 3dTfitter to use
        old_style: Make old-style script (pre-2015.02.24) for 1D case
        tr: Set the scanner TR; needed for 1D formatted input files
        tr_nup: Upsample factor for TR; number of pieces each original TR is\
            divided into
        verbosity: Set the verbose level

    Returns:
        Parameter dictionary.
    """
    params: NeuroDeconvolvePy = {
        "@type": "afni/neuro_deconvolve.py",
        "input_file": input_file,
        "prefix": prefix,
        "script": script,
        "old_style": old_style,
    }
    if kernel is not None:
        params["kernel"] = kernel
    if kernel_file is not None:
        params["kernel_file"] = kernel_file
    if mask_dset is not None:
        params["mask_dset"] = mask_dset
    if tr is not None:
        params["tr"] = tr
    if tr_nup is not None:
        params["tr_nup"] = tr_nup
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def neuro_deconvolve_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NeuroDeconvolvePy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("script", None) is None:
        raise StyxValidationError("`script` must not be None")
    if not isinstance(params["script"], str):
        raise StyxValidationError(f'`script` has the wrong type: Received `{type(params["script"])}` expected `str`')
    if params.get("kernel", None) is not None:
        if not isinstance(params["kernel"], str):
            raise StyxValidationError(f'`kernel` has the wrong type: Received `{type(params["kernel"])}` expected `str`')
    if params.get("kernel_file", None) is not None:
        if not isinstance(params["kernel_file"], str):
            raise StyxValidationError(f'`kernel_file` has the wrong type: Received `{type(params["kernel_file"])}` expected `str`')
    if params.get("mask_dset", None) is not None:
        if not isinstance(params["mask_dset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_dset` has the wrong type: Received `{type(params["mask_dset"])}` expected `InputPathType`')
    if params.get("old_style", None) is not None:
        if not isinstance(params["old_style"], bool):
            raise StyxValidationError(f'`old_style` has the wrong type: Received `{type(params["old_style"])}` expected `bool`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("tr_nup", None) is not None:
        if not isinstance(params["tr_nup"], (float, int)):
            raise StyxValidationError(f'`tr_nup` has the wrong type: Received `{type(params["tr_nup"])}` expected `float`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], (float, int)):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `float`')

def neuro_deconvolve_py_cargs(params: NeuroDeconvolvePy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("neuro_deconvolve.py")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["prefix"])
    cargs.append(params["script"])
    v_0 = params.get("kernel")
    if v_0 is not None:
        cargs.append("-kernel")
        cargs.append(v_0)
    v_1 = params.get("kernel_file")
    if v_1 is not None:
        cargs.append("-kernel_file")
        cargs.append(v_1)
    v_2 = params.get("mask_dset")
    if v_2 is not None:
        cargs.append("-mask_dset")
        cargs.append(execution.input_file(v_2))
    if params.get("old_style"):
        cargs.append("-old")
    v_3 = params.get("tr")
    if v_3 is not None:
        cargs.append("-tr")
        cargs.append(str(v_3))
    v_4 = params.get("tr_nup")
    if v_4 is not None:
        cargs.append("-tr_nup")
        cargs.append(str(v_4))
    v_5 = params.get("verbosity")
    if v_5 is not None:
        cargs.append("-verb")
        cargs.append(str(v_5))
    return cargs

def neuro_deconvolve_py_outputs(params: NeuroDeconvolvePy, execution: Execution) -> NeuroDeconvolvePyOutputs:
    """Build the NeuroDeconvolvePyOutputs object for this tool."""
    kernel_file_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_head_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.HEAD')
    output_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK')
    __v0 = params.get("kernel_file")
    if __v0 is not None:
        kernel_file_out_v = execution.output_file((__v0 if __v0 is not None else ""))
    return NeuroDeconvolvePyOutputs(
        root=root_v,
        output_head=output_head_v,
        output_brik=output_brik_v,
        kernel_file_out=kernel_file_out_v,
    )

def neuro_deconvolve_py_execute(params: NeuroDeconvolvePy, runner: Runner | None = None) -> NeuroDeconvolvePyOutputs:
    """
    neuro_deconvolve.py

    Generate a script to apply 3dTfitter to deconvolve an MRI signal (BOLD response curve) into a neuro response curve.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    neuro_deconvolve_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NEURO_DECONVOLVE_PY_METADATA)
    execution.params(params)
    args = neuro_deconvolve_py_cargs(params, execution)
    out = neuro_deconvolve_py_outputs(params, execution)
    execution.run(args)
    return out

def neuro_deconvolve_py(
    input_file: InputPathType,
    prefix: str,
    script: str,
    kernel: str | None = None,
    kernel_file: str | None = None,
    mask_dset: InputPathType | None = None,
    old_style: bool = False,
    tr: float | None = None,
    tr_nup: float | None = None,
    verbosity: float | None = None,
    runner: Runner | None = None,
) -> NeuroDeconvolvePyOutputs:
    """
    neuro_deconvolve.py

    Generate a script to apply 3dTfitter to deconvolve an MRI signal (BOLD response curve) into a neuro response curve.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Set the data to deconvolve
        prefix: Set the prefix for output filenames
        script: Specify the name of the output script
        kernel: Set the response kernel
        kernel_file: Set the filename to store the kernel in; should be at the\
            upsampled TR
        mask_dset: Set a mask dataset for 3dTfitter to use
        old_style: Make old-style script (pre-2015.02.24) for 1D case
        tr: Set the scanner TR; needed for 1D formatted input files
        tr_nup: Upsample factor for TR; number of pieces each original TR is\
            divided into
        verbosity: Set the verbose level
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = neuro_deconvolve_py_params(
        input_file=input_file,
        prefix=prefix,
        script=script,
        kernel=kernel,
        kernel_file=kernel_file,
        mask_dset=mask_dset,
        old_style=old_style,
        tr=tr,
        tr_nup=tr_nup,
        verbosity=verbosity,
    )
    return neuro_deconvolve_py_execute(params, runner)

__all__ = [
    "NeuroDeconvolvePy",
    "NeuroDeconvolvePyOutputs",
    "NEURO_DECONVOLVE_PY_METADATA",
    "neuro_deconvolve_py_cargs",
    "neuro_deconvolve_py_outputs",
    "neuro_deconvolve_py_params",
    "neuro_deconvolve_py_execute",
    "neuro_deconvolve_py_validate",
    "neuro_deconvolve_py",
]