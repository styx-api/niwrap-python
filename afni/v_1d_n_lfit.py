# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1D_N_LFIT_METADATA = Metadata(
    id="1dNLfit",
    name="1dNLfit",
    package="afni",
)

V1dNLfit = typing.TypedDict(
    "V1dNLfit",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dNLfit"]],
        "expression": str,
        "independent_variable": str,
        "parameters": list[str],
        "dependent_data": InputPathType,
        "method": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class V1dNLfitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fit_results: OutputPathType
    """Results (fitted time series models) are written to stdout. Should be saved by '>' redirection."""

def v_1d_n_lfit_params(
    expression: str,
    independent_variable: str,
    parameters: list[str],
    dependent_data: InputPathType,
    method: int | None = None,
) -> V1dNLfit:
    """
    Build parameters.

    Args:
        expression: The expression for the fit. It must contain one symbol from 'a'\
            to 'z' which is marked as the independent variable by option '-indvar',\
            and at least one more symbol which is a parameter to be estimated.
        independent_variable: Indicates which variable in '-expr' is the\
            independent variable. All other symbols are parameters, which are\
            either fixed (constants) or variables to be estimated. Read the values\
            of the independent variable from 1D file.
        parameters: Set fixed value or estimating range for a particular symbol.\
            For a fixed value, it takes the form 'a=3.14'. For an estimated\
            parameter, it takes the form 'q=-sqrt(2):sqrt(2)'. All symbols in\
            '-expr' must have a corresponding '-param' option, EXCEPT for the\
            '-indvar' symbol.
        dependent_data: Read the values of the dependent variable (to be fitted to\
            '-expr') from 1D file. The file must have the same number of rows as\
            the '-indvar' file.
        method: Set the method for fitting: '1' for L1, '2' for L2 (default is L2).

    Returns:
        Parameter dictionary.
    """
    params: V1dNLfit = {
        "@type": "afni/1dNLfit",
        "expression": expression,
        "independent_variable": independent_variable,
        "parameters": parameters,
        "dependent_data": dependent_data,
    }
    if method is not None:
        params["method"] = method
    return params

def v_1d_n_lfit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dNLfit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("expression", None) is None:
        raise StyxValidationError("`expression` must not be None")
    if not isinstance(params["expression"], str):
        raise StyxValidationError(f'`expression` has the wrong type: Received `{type(params["expression"])}` expected `str`')
    if params.get("independent_variable", None) is None:
        raise StyxValidationError("`independent_variable` must not be None")
    if not isinstance(params["independent_variable"], str):
        raise StyxValidationError(f'`independent_variable` has the wrong type: Received `{type(params["independent_variable"])}` expected `str`')
    if params.get("parameters", None) is None:
        raise StyxValidationError("`parameters` must not be None")
    if not isinstance(params["parameters"], list):
        raise StyxValidationError(f'`parameters` has the wrong type: Received `{type(params["parameters"])}` expected `list[str]`')
    for e in params["parameters"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`parameters` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("dependent_data", None) is None:
        raise StyxValidationError("`dependent_data` must not be None")
    if not isinstance(params["dependent_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dependent_data` has the wrong type: Received `{type(params["dependent_data"])}` expected `InputPathType`')
    if params.get("method", None) is not None:
        if not isinstance(params["method"], int):
            raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `int`')

def v_1d_n_lfit_cargs(params: V1dNLfit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dNLfit")
    cargs.append("-expr")
    cargs.append(params["expression"])
    cargs.append("-indvar")
    cargs.append(params["independent_variable"])
    cargs.append("-param")
    for item0 in params["parameters"]:
        cargs.append(item0)
    cargs.append("-depdata")
    cargs.append(execution.input_file(params["dependent_data"]))
    v_0 = params.get("method")
    if v_0 is not None:
        cargs.append("-meth")
        cargs.append(str(v_0))
    return cargs

def v_1d_n_lfit_outputs(params: V1dNLfit, execution: Execution) -> V1dNLfitOutputs:
    """Build the V1dNLfitOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fit_results_v: OutputPathType = execution.output_file("stdout")
    return V1dNLfitOutputs(
        root=root_v,
        fit_results=fit_results_v,
    )

def v_1d_n_lfit_execute(params: V1dNLfit, runner: Runner | None = None) -> V1dNLfitOutputs:
    """
    1dNLfit

    Program to fit a model to a vector of data. The model is given by a symbolic expression, with parameters to be estimated.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1d_n_lfit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1D_N_LFIT_METADATA)
    execution.params(params)
    args = v_1d_n_lfit_cargs(params, execution)
    out = v_1d_n_lfit_outputs(params, execution)
    execution.run(args)
    return out

def v_1d_n_lfit(
    expression: str,
    independent_variable: str,
    parameters: list[str],
    dependent_data: InputPathType,
    method: int | None = None,
    runner: Runner | None = None,
) -> V1dNLfitOutputs:
    """
    1dNLfit

    Program to fit a model to a vector of data. The model is given by a symbolic expression, with parameters to be estimated.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        expression: The expression for the fit. It must contain one symbol from 'a'\
            to 'z' which is marked as the independent variable by option '-indvar',\
            and at least one more symbol which is a parameter to be estimated.
        independent_variable: Indicates which variable in '-expr' is the\
            independent variable. All other symbols are parameters, which are\
            either fixed (constants) or variables to be estimated. Read the values\
            of the independent variable from 1D file.
        parameters: Set fixed value or estimating range for a particular symbol.\
            For a fixed value, it takes the form 'a=3.14'. For an estimated\
            parameter, it takes the form 'q=-sqrt(2):sqrt(2)'. All symbols in\
            '-expr' must have a corresponding '-param' option, EXCEPT for the\
            '-indvar' symbol.
        dependent_data: Read the values of the dependent variable (to be fitted to\
            '-expr') from 1D file. The file must have the same number of rows as\
            the '-indvar' file.
        method: Set the method for fitting: '1' for L1, '2' for L2 (default is L2).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1d_n_lfit_params(
        expression=expression,
        independent_variable=independent_variable,
        parameters=parameters,
        dependent_data=dependent_data,
        method=method,
    )
    return v_1d_n_lfit_execute(params, runner)

__all__ = [
    "V1dNLfit",
    "V1dNLfitOutputs",
    "V_1D_N_LFIT_METADATA",
    "v_1d_n_lfit_cargs",
    "v_1d_n_lfit_outputs",
    "v_1d_n_lfit_params",
    "v_1d_n_lfit_execute",
    "v_1d_n_lfit_validate",
    "v_1d_n_lfit",
]