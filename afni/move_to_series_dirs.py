# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MOVE_TO_SERIES_DIRS_METADATA = Metadata(
    id="@move.to.series.dirs",
    name="@move.to.series.dirs",
    package="afni",
)

MoveToSeriesDirsAction = typing.Literal["copy"] | typing.Literal["move"]

MoveToSeriesDirs = typing.TypedDict(
    "MoveToSeriesDirs",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@move.to.series.dirs"]],
        "action": typing.NotRequired[MoveToSeriesDirsAction],
        "dprefix": typing.NotRequired[str],
        "tag": typing.NotRequired[str],
        "test": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "hist": typing.NotRequired[bool],
        "ver": typing.NotRequired[bool],
        "dicom_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class MoveToSeriesDirsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def move_to_series_dirs_params(
    dicom_files: list[InputPathType],
    action: MoveToSeriesDirsAction | None = None,
    dprefix: str | None = None,
    tag: str | None = None,
    test: bool = False,
    help: bool = False,
    hist: bool = False,
    ver: bool = False,
) -> MoveToSeriesDirs:
    """
    Build parameters.

    Args:
        dicom_files: Specify input DICOM files (e.g., IMG*)
        action: Specify action to perform: copy or move. Default is copy.
        dprefix: Specify directory root for output series directories. Default is\
            current directory.
        tag: Specify the DICOM tag to use for partitioning. Default is 0020,0011\
            (REL Series Number).
        test: Run in test mode, only show what would be done without actually\
            moving any files.
        help: Show help information.
        hist: Show modification history.
        ver: Show version number.

    Returns:
        Parameter dictionary.
    """
    params: MoveToSeriesDirs = {
        "@type": "afni/@move.to.series.dirs",
        "dicom_files": dicom_files,
        "test": test,
        "help": help,
        "hist": hist,
        "ver": ver,
    }
    if action is not None:
        params["action"] = action
    if dprefix is not None:
        params["dprefix"] = dprefix
    if tag is not None:
        params["tag"] = tag
    return params

def move_to_series_dirs_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MoveToSeriesDirs."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("action", None) is not None:
        if not isinstance(params["action"], str):
            raise StyxValidationError(f'`action` has the wrong type: Received `{type(params["action"])}` expected `MoveToSeriesDirsAction`')
        if params["action"] not in ["copy", "move"]:
            raise StyxValidationError("Parameter `action` must be one of [\"copy\", \"move\"]")
    if params.get("dprefix", None) is not None:
        if not isinstance(params["dprefix"], str):
            raise StyxValidationError(f'`dprefix` has the wrong type: Received `{type(params["dprefix"])}` expected `str`')
    if params.get("tag", None) is not None:
        if not isinstance(params["tag"], str):
            raise StyxValidationError(f'`tag` has the wrong type: Received `{type(params["tag"])}` expected `str`')
    if params.get("test", None) is not None:
        if not isinstance(params["test"], bool):
            raise StyxValidationError(f'`test` has the wrong type: Received `{type(params["test"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("hist", None) is not None:
        if not isinstance(params["hist"], bool):
            raise StyxValidationError(f'`hist` has the wrong type: Received `{type(params["hist"])}` expected `bool`')
    if params.get("ver", None) is not None:
        if not isinstance(params["ver"], bool):
            raise StyxValidationError(f'`ver` has the wrong type: Received `{type(params["ver"])}` expected `bool`')
    if params.get("dicom_files", None) is None:
        raise StyxValidationError("`dicom_files` must not be None")
    if not isinstance(params["dicom_files"], list):
        raise StyxValidationError(f'`dicom_files` has the wrong type: Received `{type(params["dicom_files"])}` expected `list[InputPathType]`')
    for e in params["dicom_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`dicom_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def move_to_series_dirs_cargs(params: MoveToSeriesDirs, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@move.to.series.dirs")
    v_0 = params.get("action")
    if v_0 is not None:
        cargs.append("-action")
        cargs.append(str(v_0))
    v_1 = params.get("dprefix")
    if v_1 is not None:
        cargs.append("-dprefix")
        cargs.append(v_1)
    v_2 = params.get("tag")
    if v_2 is not None:
        cargs.append("-tag")
        cargs.append(v_2)
    if params.get("test"):
        cargs.append("-test")
    if params.get("help"):
        cargs.append("-help")
    if params.get("hist"):
        cargs.append("-hist")
    if params.get("ver"):
        cargs.append("-ver")
    for item0 in params["dicom_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def move_to_series_dirs_outputs(params: MoveToSeriesDirs, execution: Execution) -> MoveToSeriesDirsOutputs:
    """Build the MoveToSeriesDirsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MoveToSeriesDirsOutputs(
        root=root_v,
    )

def move_to_series_dirs_execute(params: MoveToSeriesDirs, runner: Runner | None = None) -> MoveToSeriesDirsOutputs:
    """
    @move.to.series.dirs

    Partition DICOM files into series directories by copying or moving them to new series directories.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    move_to_series_dirs_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MOVE_TO_SERIES_DIRS_METADATA)
    execution.params(params)
    args = move_to_series_dirs_cargs(params, execution)
    out = move_to_series_dirs_outputs(params, execution)
    execution.run(args)
    return out

def move_to_series_dirs(
    dicom_files: list[InputPathType],
    action: MoveToSeriesDirsAction | None = None,
    dprefix: str | None = None,
    tag: str | None = None,
    test: bool = False,
    help: bool = False,
    hist: bool = False,
    ver: bool = False,
    runner: Runner | None = None,
) -> MoveToSeriesDirsOutputs:
    """
    @move.to.series.dirs

    Partition DICOM files into series directories by copying or moving them to new series directories.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dicom_files: Specify input DICOM files (e.g., IMG*)
        action: Specify action to perform: copy or move. Default is copy.
        dprefix: Specify directory root for output series directories. Default is\
            current directory.
        tag: Specify the DICOM tag to use for partitioning. Default is 0020,0011\
            (REL Series Number).
        test: Run in test mode, only show what would be done without actually\
            moving any files.
        help: Show help information.
        hist: Show modification history.
        ver: Show version number.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = move_to_series_dirs_params(
        dicom_files=dicom_files,
        action=action,
        dprefix=dprefix,
        tag=tag,
        test=test,
        help=help,
        hist=hist,
        ver=ver,
    )
    return move_to_series_dirs_execute(params, runner)

__all__ = [
    "MoveToSeriesDirs",
    "MoveToSeriesDirsOutputs",
    "MOVE_TO_SERIES_DIRS_METADATA",
    "move_to_series_dirs_cargs",
    "move_to_series_dirs_outputs",
    "move_to_series_dirs_params",
    "move_to_series_dirs_execute",
    "move_to_series_dirs_validate",
    "move_to_series_dirs",
]