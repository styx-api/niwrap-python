# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DICOM_TO_RAW_METADATA = Metadata(
    id="dicom_to_raw",
    name="dicom_to_raw",
    package="afni",
)

DicomToRaw = typing.TypedDict(
    "DicomToRaw",
    {
        "@type": typing.NotRequired[typing.Literal["afni/dicom_to_raw"]],
        "input_dicom": InputPathType,
    },
)

@dataclasses.dataclass
class DicomToRawOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_raw_file: OutputPathType
    """Output raw file(s)"""

def dicom_to_raw_params(
    input_dicom: InputPathType,
) -> DicomToRaw:
    """
    Build parameters.

    Args:
        input_dicom: Input DICOM file

    Returns:
        Parameter dictionary.
    """
    params: DicomToRaw = {
        "@type": "afni/dicom_to_raw",
        "input_dicom": input_dicom,
    }
    return params

def dicom_to_raw_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DicomToRaw."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dicom", None) is None:
        raise StyxValidationError("`input_dicom` must not be None")
    if not isinstance(params["input_dicom"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dicom` has the wrong type: Received `{type(params["input_dicom"])}` expected `InputPathType`')

def dicom_to_raw_cargs(params: DicomToRaw, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dicom_to_raw")
    cargs.append(execution.input_file(params["input_dicom"]))
    return cargs

def dicom_to_raw_outputs(params: DicomToRaw, execution: Execution) -> DicomToRawOutputs:
    """Build the DicomToRawOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_raw_file_v: OutputPathType = execution.output_file(f'{params["input_dicom"]}.raw.0001')
    return DicomToRawOutputs(
        root=root_v,
        output_raw_file=output_raw_file_v,
    )

def dicom_to_raw_execute(params: DicomToRaw, runner: Runner | None = None) -> DicomToRawOutputs:
    """
    dicom_to_raw

    Reads images from DICOM file and writes them to raw file(s).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dicom_to_raw_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DICOM_TO_RAW_METADATA)
    execution.params(params)
    args = dicom_to_raw_cargs(params, execution)
    out = dicom_to_raw_outputs(params, execution)
    execution.run(args)
    return out

def dicom_to_raw(
    input_dicom: InputPathType,
    runner: Runner | None = None,
) -> DicomToRawOutputs:
    """
    dicom_to_raw

    Reads images from DICOM file and writes them to raw file(s).

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dicom: Input DICOM file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dicom_to_raw_params(
        input_dicom=input_dicom,
    )
    return dicom_to_raw_execute(params, runner)

__all__ = [
    "DicomToRaw",
    "DicomToRawOutputs",
    "DICOM_TO_RAW_METADATA",
    "dicom_to_raw_cargs",
    "dicom_to_raw_outputs",
    "dicom_to_raw_params",
    "dicom_to_raw_execute",
    "dicom_to_raw_validate",
    "dicom_to_raw",
]