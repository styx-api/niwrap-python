# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ALLINEATE_METADATA = Metadata(
    id="3dAllineate",
    name="3dAllineate",
    package="afni",
)

V3dAllineate = typing.TypedDict(
    "V3dAllineate",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dAllineate"]],
        "source": InputPathType,
        "base": typing.NotRequired[InputPathType],
        "prefix": str,
        "param_save": typing.NotRequired[str],
        "param_apply": typing.NotRequired[str],
        "matrix_save": typing.NotRequired[str],
        "matrix_apply": typing.NotRequired[str],
        "cost": typing.NotRequired[str],
        "interp": typing.NotRequired[str],
        "final": typing.NotRequired[str],
        "nmatch": typing.NotRequired[float],
        "nopad": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dAllineateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_brik: OutputPathType
    """Output dataset brick file"""
    out_head: OutputPathType
    """Output dataset head file"""
    out_param_save: OutputPathType | None
    """File holding saved warp parameters"""
    out_matrix_save: OutputPathType | None
    """File holding saved matrix transformations"""

def v_3d_allineate_params(
    source: InputPathType,
    prefix: str,
    base: InputPathType | None = None,
    param_save: str | None = None,
    param_apply: str | None = None,
    matrix_save: str | None = None,
    matrix_apply: str | None = None,
    cost: str | None = None,
    interp: str | None = None,
    final: str | None = None,
    nmatch: float | None = None,
    nopad: bool = False,
    verbose: bool = False,
    quiet: bool = False,
) -> V3dAllineate:
    """
    Build parameters.

    Args:
        source: Source dataset file
        prefix: Output prefix
        base: Base dataset file
        param_save: Save the warp parameters in ASCII (.1D) format into file
        param_apply: Read warp parameters from file and apply them to the source\
            dataset
        matrix_save: Save the transformation matrix for each sub-brick into file
        matrix_apply: Use the matrices in file to define the spatial\
            transformations to be applied
        cost: Defines the 'cost' function that defines the matching between the\
            source and the base
        interp: Defines interpolation method to use during matching process
        final: Defines the interpolation mode used to create the output dataset
        nmatch: Use at most 'nnn' scattered points to match the datasets
        nopad: Do not use zero-padding on the base image
        verbose: Print out verbose progress reports
        quiet: Don't print out verbose stuff

    Returns:
        Parameter dictionary.
    """
    params: V3dAllineate = {
        "@type": "afni/3dAllineate",
        "source": source,
        "prefix": prefix,
        "nopad": nopad,
        "verbose": verbose,
        "quiet": quiet,
    }
    if base is not None:
        params["base"] = base
    if param_save is not None:
        params["param_save"] = param_save
    if param_apply is not None:
        params["param_apply"] = param_apply
    if matrix_save is not None:
        params["matrix_save"] = matrix_save
    if matrix_apply is not None:
        params["matrix_apply"] = matrix_apply
    if cost is not None:
        params["cost"] = cost
    if interp is not None:
        params["interp"] = interp
    if final is not None:
        params["final"] = final
    if nmatch is not None:
        params["nmatch"] = nmatch
    return params

def v_3d_allineate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dAllineate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("base", None) is not None:
        if not isinstance(params["base"], (pathlib.Path, str)):
            raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("param_save", None) is not None:
        if not isinstance(params["param_save"], str):
            raise StyxValidationError(f'`param_save` has the wrong type: Received `{type(params["param_save"])}` expected `str`')
    if params.get("param_apply", None) is not None:
        if not isinstance(params["param_apply"], str):
            raise StyxValidationError(f'`param_apply` has the wrong type: Received `{type(params["param_apply"])}` expected `str`')
    if params.get("matrix_save", None) is not None:
        if not isinstance(params["matrix_save"], str):
            raise StyxValidationError(f'`matrix_save` has the wrong type: Received `{type(params["matrix_save"])}` expected `str`')
    if params.get("matrix_apply", None) is not None:
        if not isinstance(params["matrix_apply"], str):
            raise StyxValidationError(f'`matrix_apply` has the wrong type: Received `{type(params["matrix_apply"])}` expected `str`')
    if params.get("cost", None) is not None:
        if not isinstance(params["cost"], str):
            raise StyxValidationError(f'`cost` has the wrong type: Received `{type(params["cost"])}` expected `str`')
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `str`')
    if params.get("final", None) is not None:
        if not isinstance(params["final"], str):
            raise StyxValidationError(f'`final` has the wrong type: Received `{type(params["final"])}` expected `str`')
    if params.get("nmatch", None) is not None:
        if not isinstance(params["nmatch"], (float, int)):
            raise StyxValidationError(f'`nmatch` has the wrong type: Received `{type(params["nmatch"])}` expected `float`')
    if params.get("nopad", None) is not None:
        if not isinstance(params["nopad"], bool):
            raise StyxValidationError(f'`nopad` has the wrong type: Received `{type(params["nopad"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')

def v_3d_allineate_cargs(params: V3dAllineate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dAllineate")
    cargs.append(execution.input_file(params["source"]))
    v_0 = params.get("base")
    if v_0 is not None:
        cargs.append("-base")
        cargs.append(execution.input_file(v_0))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_1 = params.get("param_save")
    if v_1 is not None:
        cargs.append("-1Dparam_save")
        cargs.append(v_1)
    v_2 = params.get("param_apply")
    if v_2 is not None:
        cargs.append("-1Dparam_apply")
        cargs.append(v_2)
    v_3 = params.get("matrix_save")
    if v_3 is not None:
        cargs.append("-1Dmatrix_save")
        cargs.append(v_3)
    v_4 = params.get("matrix_apply")
    if v_4 is not None:
        cargs.append("-1Dmatrix_apply")
        cargs.append(v_4)
    v_5 = params.get("cost")
    if v_5 is not None:
        cargs.append("-cost")
        cargs.append(v_5)
    v_6 = params.get("interp")
    if v_6 is not None:
        cargs.append("-interp")
        cargs.append(v_6)
    v_7 = params.get("final")
    if v_7 is not None:
        cargs.append("-final")
        cargs.append(v_7)
    v_8 = params.get("nmatch")
    if v_8 is not None:
        cargs.append("-nmatch")
        cargs.append(str(v_8))
    if params.get("nopad"):
        cargs.append("-nopad")
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("quiet"):
        cargs.append("-quiet")
    return cargs

def v_3d_allineate_outputs(params: V3dAllineate, execution: Execution) -> V3dAllineateOutputs:
    """Build the V3dAllineateOutputs object for this tool."""
    out_param_save_v: OutputPathType | None = None
    out_matrix_save_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK')
    out_head_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.HEAD')
    __v0 = params.get("param_save")
    if __v0 is not None:
        out_param_save_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("matrix_save")
    if __v1 is not None:
        out_matrix_save_v = execution.output_file((__v1 if __v1 is not None else ""))
    return V3dAllineateOutputs(
        root=root_v,
        out_brik=out_brik_v,
        out_head=out_head_v,
        out_param_save=out_param_save_v,
        out_matrix_save=out_matrix_save_v,
    )

def v_3d_allineate_execute(params: V3dAllineate, runner: Runner | None = None) -> V3dAllineateOutputs:
    """
    3dAllineate

    Program to align one dataset (the 'source') to a 'base' dataset using an affine (matrix) transformation of space.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_allineate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ALLINEATE_METADATA)
    execution.params(params)
    args = v_3d_allineate_cargs(params, execution)
    out = v_3d_allineate_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_allineate(
    source: InputPathType,
    prefix: str,
    base: InputPathType | None = None,
    param_save: str | None = None,
    param_apply: str | None = None,
    matrix_save: str | None = None,
    matrix_apply: str | None = None,
    cost: str | None = None,
    interp: str | None = None,
    final: str | None = None,
    nmatch: float | None = None,
    nopad: bool = False,
    verbose: bool = False,
    quiet: bool = False,
    runner: Runner | None = None,
) -> V3dAllineateOutputs:
    """
    3dAllineate

    Program to align one dataset (the 'source') to a 'base' dataset using an affine (matrix) transformation of space.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        source: Source dataset file
        prefix: Output prefix
        base: Base dataset file
        param_save: Save the warp parameters in ASCII (.1D) format into file
        param_apply: Read warp parameters from file and apply them to the source\
            dataset
        matrix_save: Save the transformation matrix for each sub-brick into file
        matrix_apply: Use the matrices in file to define the spatial\
            transformations to be applied
        cost: Defines the 'cost' function that defines the matching between the\
            source and the base
        interp: Defines interpolation method to use during matching process
        final: Defines the interpolation mode used to create the output dataset
        nmatch: Use at most 'nnn' scattered points to match the datasets
        nopad: Do not use zero-padding on the base image
        verbose: Print out verbose progress reports
        quiet: Don't print out verbose stuff
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_allineate_params(
        source=source,
        prefix=prefix,
        base=base,
        param_save=param_save,
        param_apply=param_apply,
        matrix_save=matrix_save,
        matrix_apply=matrix_apply,
        cost=cost,
        interp=interp,
        final=final,
        nmatch=nmatch,
        nopad=nopad,
        verbose=verbose,
        quiet=quiet,
    )
    return v_3d_allineate_execute(params, runner)

__all__ = [
    "V3dAllineate",
    "V3dAllineateOutputs",
    "V_3D_ALLINEATE_METADATA",
    "v_3d_allineate_cargs",
    "v_3d_allineate_outputs",
    "v_3d_allineate_params",
    "v_3d_allineate_execute",
    "v_3d_allineate_validate",
    "v_3d_allineate",
]