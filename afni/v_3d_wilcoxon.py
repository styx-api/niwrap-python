# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_WILCOXON_METADATA = Metadata(
    id="3dWilcoxon",
    name="3dWilcoxon",
    package="afni",
)

V3dWilcoxon = typing.TypedDict(
    "V3dWilcoxon",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dWilcoxon"]],
        "workmem": typing.NotRequired[float],
        "voxel": typing.NotRequired[float],
        "dset1_x": list[InputPathType],
        "dset2_y": list[InputPathType],
        "output_prefix": str,
    },
)

@dataclasses.dataclass
class V3dWilcoxonOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Estimated population delta and Wilcoxon signed-rank statistics"""

def v_3d_wilcoxon_params(
    dset1_x: list[InputPathType],
    dset2_y: list[InputPathType],
    output_prefix: str,
    workmem: float | None = None,
    voxel: float | None = None,
) -> V3dWilcoxon:
    """
    Build parameters.

    Args:
        dset1_x: Data set for X observations. The user must specify 1 and only 1\
            sub-brick with each -dset command.
        dset2_y: Data set for Y observations. The user must specify 1 and only 1\
            sub-brick with each -dset command.
        output_prefix: Estimated population delta and Wilcoxon signed-rank\
            statistics are written to file
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel: Screen output for voxel # num

    Returns:
        Parameter dictionary.
    """
    params: V3dWilcoxon = {
        "@type": "afni/3dWilcoxon",
        "dset1_x": dset1_x,
        "dset2_y": dset2_y,
        "output_prefix": output_prefix,
    }
    if workmem is not None:
        params["workmem"] = workmem
    if voxel is not None:
        params["voxel"] = voxel
    return params

def v_3d_wilcoxon_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dWilcoxon."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("workmem", None) is not None:
        if not isinstance(params["workmem"], (float, int)):
            raise StyxValidationError(f'`workmem` has the wrong type: Received `{type(params["workmem"])}` expected `float`')
    if params.get("voxel", None) is not None:
        if not isinstance(params["voxel"], (float, int)):
            raise StyxValidationError(f'`voxel` has the wrong type: Received `{type(params["voxel"])}` expected `float`')
    if params.get("dset1_x", None) is None:
        raise StyxValidationError("`dset1_x` must not be None")
    if not isinstance(params["dset1_x"], list):
        raise StyxValidationError(f'`dset1_x` has the wrong type: Received `{type(params["dset1_x"])}` expected `list[InputPathType]`')
    for e in params["dset1_x"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`dset1_x` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("dset2_y", None) is None:
        raise StyxValidationError("`dset2_y` must not be None")
    if not isinstance(params["dset2_y"], list):
        raise StyxValidationError(f'`dset2_y` has the wrong type: Received `{type(params["dset2_y"])}` expected `list[InputPathType]`')
    for e_2 in params["dset2_y"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`dset2_y` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def v_3d_wilcoxon_cargs(params: V3dWilcoxon, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dWilcoxon")
    v_0 = params.get("workmem")
    if v_0 is not None:
        cargs.append("-workmem")
        cargs.append(str(v_0))
    v_1 = params.get("voxel")
    if v_1 is not None:
        cargs.append("-voxel")
        cargs.append(str(v_1))
    cargs.append("-dset 1")
    for item0 in params["dset1_x"]:
        cargs.append(execution.input_file(item0))
    cargs.append("-dset 2")
    for item1 in params["dset2_y"]:
        cargs.append(execution.input_file(item1))
    cargs.append("-out")
    cargs.append(params["output_prefix"])
    return cargs

def v_3d_wilcoxon_outputs(params: V3dWilcoxon, execution: Execution) -> V3dWilcoxonOutputs:
    """Build the V3dWilcoxonOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_prefix"])
    return V3dWilcoxonOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_wilcoxon_execute(params: V3dWilcoxon, runner: Runner | None = None) -> V3dWilcoxonOutputs:
    """
    3dWilcoxon

    Nonparametric Wilcoxon signed-rank test for paired comparisons of two samples

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_wilcoxon_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_WILCOXON_METADATA)
    execution.params(params)
    args = v_3d_wilcoxon_cargs(params, execution)
    out = v_3d_wilcoxon_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_wilcoxon(
    dset1_x: list[InputPathType],
    dset2_y: list[InputPathType],
    output_prefix: str,
    workmem: float | None = None,
    voxel: float | None = None,
    runner: Runner | None = None,
) -> V3dWilcoxonOutputs:
    """
    3dWilcoxon

    Nonparametric Wilcoxon signed-rank test for paired comparisons of two samples

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset1_x: Data set for X observations. The user must specify 1 and only 1\
            sub-brick with each -dset command.
        dset2_y: Data set for Y observations. The user must specify 1 and only 1\
            sub-brick with each -dset command.
        output_prefix: Estimated population delta and Wilcoxon signed-rank\
            statistics are written to file
        workmem: Number of megabytes of RAM to use for statistical workspace
        voxel: Screen output for voxel # num
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_wilcoxon_params(
        dset1_x=dset1_x,
        dset2_y=dset2_y,
        output_prefix=output_prefix,
        workmem=workmem,
        voxel=voxel,
    )
    return v_3d_wilcoxon_execute(params, runner)

__all__ = [
    "V3dWilcoxon",
    "V3dWilcoxonOutputs",
    "V_3D_WILCOXON_METADATA",
    "v_3d_wilcoxon_cargs",
    "v_3d_wilcoxon_outputs",
    "v_3d_wilcoxon_params",
    "v_3d_wilcoxon_execute",
    "v_3d_wilcoxon_validate",
    "v_3d_wilcoxon",
]