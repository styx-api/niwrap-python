# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ALIGN_CENTERS_METADATA = Metadata(
    id="@Align_Centers",
    name="@Align_Centers",
    package="afni",
)

AlignCenters = typing.TypedDict(
    "AlignCenters",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@Align_Centers"]],
        "base": InputPathType,
        "dset": InputPathType,
        "children": typing.NotRequired[list[InputPathType]],
        "echo": typing.NotRequired[bool],
        "overwrite": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "matrix_only": typing.NotRequired[bool],
        "matrix_only_no_dset": typing.NotRequired[bool],
        "no_cp": typing.NotRequired[bool],
        "center_grid": typing.NotRequired[bool],
        "center_cm": typing.NotRequired[bool],
        "center_cm_no_amask": typing.NotRequired[bool],
        "shift_xform": typing.NotRequired[InputPathType],
        "shift_xform_inv": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class AlignCentersOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    transform_matrix: OutputPathType
    """Transform matrix needed for the shift applied to DSET."""
    shifted_dset: OutputPathType | None
    """Shifted Dataset aligned with the base volume."""
    shifted_child_dsets: OutputPathType | None
    """Shifted child datasets aligned with the base volume."""

def align_centers_params(
    base: InputPathType,
    dset: InputPathType,
    children: list[InputPathType] | None = None,
    echo: bool = False,
    overwrite: bool = False,
    prefix: str | None = None,
    matrix_only: bool = False,
    matrix_only_no_dset: bool = False,
    no_cp: bool = False,
    center_grid: bool = False,
    center_cm: bool = False,
    center_cm_no_amask: bool = False,
    shift_xform: InputPathType | None = None,
    shift_xform_inv: InputPathType | None = None,
) -> AlignCenters:
    """
    Build parameters.

    Args:
        base: Base volume, typically a template. Can also specify RAI coordinates\
            for center alignment.
        dset: Dataset to be aligned to BASE.
        children: Additional datasets (originally in register with DSET) that\
            should be shifted in the same way.
        echo: Echo all commands to terminal for debugging.
        overwrite: Overwrite existing output files.
        prefix: Custom prefix for the result files.
        matrix_only: Only output the transform needed to align the centers without\
            shifting any child volumes.
        matrix_only_no_dset: Like -1Dmat_only, but no datasets are created or\
            changed.
        no_cp: Do not create new data; shift existing ones. Use with caution.
        center_grid: Center is that of the volume's grid (default).
        center_cm: Center is the center of mass of the volume.
        center_cm_no_amask: Like -cm, but with no automask.
        shift_xform: Apply shift translation from a 1D file.
        shift_xform_inv: Apply inverse of shift translation from a 1D file.

    Returns:
        Parameter dictionary.
    """
    params: AlignCenters = {
        "@type": "afni/@Align_Centers",
        "base": base,
        "dset": dset,
        "echo": echo,
        "overwrite": overwrite,
        "matrix_only": matrix_only,
        "matrix_only_no_dset": matrix_only_no_dset,
        "no_cp": no_cp,
        "center_grid": center_grid,
        "center_cm": center_cm,
        "center_cm_no_amask": center_cm_no_amask,
    }
    if children is not None:
        params["children"] = children
    if prefix is not None:
        params["prefix"] = prefix
    if shift_xform is not None:
        params["shift_xform"] = shift_xform
    if shift_xform_inv is not None:
        params["shift_xform_inv"] = shift_xform_inv
    return params

def align_centers_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AlignCenters."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base", None) is None:
        raise StyxValidationError("`base` must not be None")
    if not isinstance(params["base"], (pathlib.Path, str)):
        raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `InputPathType`')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')
    if params.get("children", None) is not None:
        if not isinstance(params["children"], list):
            raise StyxValidationError(f'`children` has the wrong type: Received `{type(params["children"])}` expected `list[InputPathType]`')
        for e in params["children"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`children` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("matrix_only", None) is not None:
        if not isinstance(params["matrix_only"], bool):
            raise StyxValidationError(f'`matrix_only` has the wrong type: Received `{type(params["matrix_only"])}` expected `bool`')
    if params.get("matrix_only_no_dset", None) is not None:
        if not isinstance(params["matrix_only_no_dset"], bool):
            raise StyxValidationError(f'`matrix_only_no_dset` has the wrong type: Received `{type(params["matrix_only_no_dset"])}` expected `bool`')
    if params.get("no_cp", None) is not None:
        if not isinstance(params["no_cp"], bool):
            raise StyxValidationError(f'`no_cp` has the wrong type: Received `{type(params["no_cp"])}` expected `bool`')
    if params.get("center_grid", None) is not None:
        if not isinstance(params["center_grid"], bool):
            raise StyxValidationError(f'`center_grid` has the wrong type: Received `{type(params["center_grid"])}` expected `bool`')
    if params.get("center_cm", None) is not None:
        if not isinstance(params["center_cm"], bool):
            raise StyxValidationError(f'`center_cm` has the wrong type: Received `{type(params["center_cm"])}` expected `bool`')
    if params.get("center_cm_no_amask", None) is not None:
        if not isinstance(params["center_cm_no_amask"], bool):
            raise StyxValidationError(f'`center_cm_no_amask` has the wrong type: Received `{type(params["center_cm_no_amask"])}` expected `bool`')
    if params.get("shift_xform", None) is not None:
        if not isinstance(params["shift_xform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`shift_xform` has the wrong type: Received `{type(params["shift_xform"])}` expected `InputPathType`')
    if params.get("shift_xform_inv", None) is not None:
        if not isinstance(params["shift_xform_inv"], (pathlib.Path, str)):
            raise StyxValidationError(f'`shift_xform_inv` has the wrong type: Received `{type(params["shift_xform_inv"])}` expected `InputPathType`')

def align_centers_cargs(params: AlignCenters, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@Align_Centers")
    cargs.append(execution.input_file(params["base"]))
    cargs.append(execution.input_file(params["dset"]))
    v_0 = params.get("children")
    if v_0 is not None:
        cargs.append("-child")
        for item0 in v_0:
            cargs.append(execution.input_file(item0))
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("overwrite"):
        cargs.append("-overwrite")
    v_1 = params.get("prefix")
    if v_1 is not None:
        cargs.append("-prefix")
        cargs.append(v_1)
    if params.get("matrix_only"):
        cargs.append("-1Dmat_only")
    if params.get("matrix_only_no_dset"):
        cargs.append("-1Dmat_only_nodset")
    if params.get("no_cp"):
        cargs.append("-no_cp")
    if params.get("center_grid"):
        cargs.append("-grid")
    if params.get("center_cm"):
        cargs.append("-cm")
    if params.get("center_cm_no_amask"):
        cargs.append("-cm_no_amask")
    v_2 = params.get("shift_xform")
    if v_2 is not None:
        cargs.append("-shift_xform")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("shift_xform_inv")
    if v_3 is not None:
        cargs.append("-shift_xform_inv")
        cargs.append(execution.input_file(v_3))
    return cargs

def align_centers_outputs(params: AlignCenters, execution: Execution) -> AlignCentersOutputs:
    """Build the AlignCentersOutputs object for this tool."""
    shifted_dset_v: OutputPathType | None = None
    shifted_child_dsets_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    transform_matrix_v: OutputPathType = execution.output_file(f'{params["dset"]}_shft.1D')
    __v0 = params.get("prefix")
    if __v0 is not None:
        shifted_dset_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_shft+orig.BRIK')
    __v1 = params.get("prefix")
    if __v1 is not None:
        shifted_child_dsets_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}_child_shft+orig.BRIK')
    return AlignCentersOutputs(
        root=root_v,
        transform_matrix=transform_matrix_v,
        shifted_dset=shifted_dset_v,
        shifted_child_dsets=shifted_child_dsets_v,
    )

def align_centers_execute(params: AlignCenters, runner: Runner | None = None) -> AlignCentersOutputs:
    """
    @Align_Centers

    Moves the center of a dataset (DSET) to the center of a base volume (BASE) and optionally creates a transform matrix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    align_centers_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ALIGN_CENTERS_METADATA)
    execution.params(params)
    args = align_centers_cargs(params, execution)
    out = align_centers_outputs(params, execution)
    execution.run(args)
    return out

def align_centers(
    base: InputPathType,
    dset: InputPathType,
    children: list[InputPathType] | None = None,
    echo: bool = False,
    overwrite: bool = False,
    prefix: str | None = None,
    matrix_only: bool = False,
    matrix_only_no_dset: bool = False,
    no_cp: bool = False,
    center_grid: bool = False,
    center_cm: bool = False,
    center_cm_no_amask: bool = False,
    shift_xform: InputPathType | None = None,
    shift_xform_inv: InputPathType | None = None,
    runner: Runner | None = None,
) -> AlignCentersOutputs:
    """
    @Align_Centers

    Moves the center of a dataset (DSET) to the center of a base volume (BASE) and optionally creates a transform matrix.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        base: Base volume, typically a template. Can also specify RAI coordinates\
            for center alignment.
        dset: Dataset to be aligned to BASE.
        children: Additional datasets (originally in register with DSET) that\
            should be shifted in the same way.
        echo: Echo all commands to terminal for debugging.
        overwrite: Overwrite existing output files.
        prefix: Custom prefix for the result files.
        matrix_only: Only output the transform needed to align the centers without\
            shifting any child volumes.
        matrix_only_no_dset: Like -1Dmat_only, but no datasets are created or\
            changed.
        no_cp: Do not create new data; shift existing ones. Use with caution.
        center_grid: Center is that of the volume's grid (default).
        center_cm: Center is the center of mass of the volume.
        center_cm_no_amask: Like -cm, but with no automask.
        shift_xform: Apply shift translation from a 1D file.
        shift_xform_inv: Apply inverse of shift translation from a 1D file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = align_centers_params(
        base=base,
        dset=dset,
        children=children,
        echo=echo,
        overwrite=overwrite,
        prefix=prefix,
        matrix_only=matrix_only,
        matrix_only_no_dset=matrix_only_no_dset,
        no_cp=no_cp,
        center_grid=center_grid,
        center_cm=center_cm,
        center_cm_no_amask=center_cm_no_amask,
        shift_xform=shift_xform,
        shift_xform_inv=shift_xform_inv,
    )
    return align_centers_execute(params, runner)

__all__ = [
    "AlignCenters",
    "AlignCentersOutputs",
    "ALIGN_CENTERS_METADATA",
    "align_centers_cargs",
    "align_centers_outputs",
    "align_centers_params",
    "align_centers_execute",
    "align_centers_validate",
    "align_centers",
]