# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DCOPY_METADATA = Metadata(
    id="3dcopy",
    name="3dcopy",
    package="afni",
)

class V3dcopyOldPrefix(typing.TypedDict):
    old_prefix: str
    """Old dataset prefix (and view if specific dataset view is to be copied)."""
    view: typing.NotRequired[str]
    """Specified view (orig, acpc, tlrc)."""

V3dcopy = typing.TypedDict(
    "V3dcopy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dcopy"]],
        "verbose": typing.NotRequired[bool],
        "denote": typing.NotRequired[bool],
        "old_prefix": V3dcopyOldPrefix,
        "new_prefix": str,
    },
)

@dataclasses.dataclass
class V3dcopyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3dcopy_params(
    old_prefix: V3dcopyOldPrefix,
    new_prefix: str,
    verbose: bool = False,
    denote: bool = False,
) -> V3dcopy:
    """
    Build parameters.

    Args:
        old_prefix:
        new_prefix: New dataset prefix or directory path.
        verbose: Print progress reports.
        denote: Remove any Notes from the file.

    Returns:
        Parameter dictionary.
    """
    params: V3dcopy = {
        "@type": "afni/3dcopy",
        "old_prefix": old_prefix,
        "new_prefix": new_prefix,
        "verbose": verbose,
        "denote": denote,
    }
    return params

def v_3dcopy_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dcopy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("denote", None) is not None:
        if not isinstance(params["denote"], bool):
            raise StyxValidationError(f'`denote` has the wrong type: Received `{type(params["denote"])}` expected `bool`')
    if params.get("old_prefix", None) is None:
        raise StyxValidationError("`old_prefix` must not be None")
    if not isinstance(params["old_prefix"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["old_prefix"])}\'')
    if params["old_prefix"].get("old_prefix", None) is None:
        raise StyxValidationError("`old_prefix` must not be None")
    if not isinstance(params["old_prefix"]["old_prefix"], str):
        raise StyxValidationError(f'`old_prefix` has the wrong type: Received `{type(params["old_prefix"]["old_prefix"])}` expected `str`')
    if params["old_prefix"].get("view", None) is not None:
        if not isinstance(params["old_prefix"]["view"], str):
            raise StyxValidationError(f'`view` has the wrong type: Received `{type(params["old_prefix"]["view"])}` expected `str`')
    if params.get("new_prefix", None) is None:
        raise StyxValidationError("`new_prefix` must not be None")
    if not isinstance(params["new_prefix"], str):
        raise StyxValidationError(f'`new_prefix` has the wrong type: Received `{type(params["new_prefix"])}` expected `str`')

def v_3dcopy_cargs(params: V3dcopy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dcopy")
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("denote"):
        cargs.append("-denote")
    cargs.append("".join([params["old_prefix"]["old_prefix"], ("".join(["+", v_0]) if (v_0 := params["old_prefix"].get("view")) is not None else "")]))
    cargs.append(params["new_prefix"])
    return cargs

def v_3dcopy_outputs(params: V3dcopy, execution: Execution) -> V3dcopyOutputs:
    """Build the V3dcopyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dcopyOutputs(
        root=root_v,
    )

def v_3dcopy_execute(params: V3dcopy, runner: Runner | None = None) -> V3dcopyOutputs:
    """
    3dcopy

    3dcopy copies datasets with or without altering prefixes and converting formats.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dcopy_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DCOPY_METADATA)
    execution.params(params)
    args = v_3dcopy_cargs(params, execution)
    out = v_3dcopy_outputs(params, execution)
    execution.run(args)
    return out

def v_3dcopy(
    old_prefix: V3dcopyOldPrefix,
    new_prefix: str,
    verbose: bool = False,
    denote: bool = False,
    runner: Runner | None = None,
) -> V3dcopyOutputs:
    """
    3dcopy

    3dcopy copies datasets with or without altering prefixes and converting formats.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        old_prefix:
        new_prefix: New dataset prefix or directory path.
        verbose: Print progress reports.
        denote: Remove any Notes from the file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dcopy_params(
        old_prefix=old_prefix,
        new_prefix=new_prefix,
        verbose=verbose,
        denote=denote,
    )
    return v_3dcopy_execute(params, runner)

__all__ = [
    "V3dcopy",
    "V3dcopyOutputs",
    "V_3DCOPY_METADATA",
    "v_3dcopy_cargs",
    "v_3dcopy_outputs",
    "v_3dcopy_params",
    "v_3dcopy_execute",
    "v_3dcopy_validate",
    "v_3dcopy",
]