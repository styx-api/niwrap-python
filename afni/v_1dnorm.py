# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1DNORM_METADATA = Metadata(
    id="1dnorm",
    name="1dnorm",
    package="afni",
)

V1dnorm = typing.TypedDict(
    "V1dnorm",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dnorm"]],
        "infile": InputPathType,
        "outfile": str,
        "norm1": typing.NotRequired[bool],
        "normx": typing.NotRequired[bool],
        "demean": typing.NotRequired[bool],
        "demed": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V1dnormOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    normalized_output: OutputPathType
    """Normalized output AFNI *.1D file"""

def v_1dnorm_params(
    infile: InputPathType,
    outfile: str,
    norm1: bool = False,
    normx: bool = False,
    demean: bool = False,
    demed: bool = False,
) -> V1dnorm:
    """
    Build parameters.

    Args:
        infile: Input AFNI *.1D file (ASCII list of numbers arranged in columns);\
            if '-' input will be read from stdin.
        outfile: Output AFNI *.1D file (normalized); if '-' output will be written\
            to stdout.
        norm1: Normalize so sum of absolute values is 1 (L_1 norm)
        normx: Normalize so that max absolute value is 1 (L_infinity norm)
        demean: Subtract each column's mean before normalizing
        demed: Subtract each column's median before normalizing

    Returns:
        Parameter dictionary.
    """
    params: V1dnorm = {
        "@type": "afni/1dnorm",
        "infile": infile,
        "outfile": outfile,
        "norm1": norm1,
        "normx": normx,
        "demean": demean,
        "demed": demed,
    }
    return params

def v_1dnorm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dnorm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("norm1", None) is not None:
        if not isinstance(params["norm1"], bool):
            raise StyxValidationError(f'`norm1` has the wrong type: Received `{type(params["norm1"])}` expected `bool`')
    if params.get("normx", None) is not None:
        if not isinstance(params["normx"], bool):
            raise StyxValidationError(f'`normx` has the wrong type: Received `{type(params["normx"])}` expected `bool`')
    if params.get("demean", None) is not None:
        if not isinstance(params["demean"], bool):
            raise StyxValidationError(f'`demean` has the wrong type: Received `{type(params["demean"])}` expected `bool`')
    if params.get("demed", None) is not None:
        if not isinstance(params["demed"], bool):
            raise StyxValidationError(f'`demed` has the wrong type: Received `{type(params["demed"])}` expected `bool`')

def v_1dnorm_cargs(params: V1dnorm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dnorm")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(params["outfile"])
    if params.get("norm1"):
        cargs.append("-norm1")
    if params.get("normx"):
        cargs.append("-normx")
    if params.get("demean"):
        cargs.append("-demean")
    if params.get("demed"):
        cargs.append("-demed")
    return cargs

def v_1dnorm_outputs(params: V1dnorm, execution: Execution) -> V1dnormOutputs:
    """Build the V1dnormOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    normalized_output_v: OutputPathType = execution.output_file(params["outfile"])
    return V1dnormOutputs(
        root=root_v,
        normalized_output=normalized_output_v,
    )

def v_1dnorm_execute(params: V1dnorm, runner: Runner | None = None) -> V1dnormOutputs:
    """
    1dnorm

    Normalize columns of a 1D file (AFNI ASCII list of numbers)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1dnorm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1DNORM_METADATA)
    execution.params(params)
    args = v_1dnorm_cargs(params, execution)
    out = v_1dnorm_outputs(params, execution)
    execution.run(args)
    return out

def v_1dnorm(
    infile: InputPathType,
    outfile: str,
    norm1: bool = False,
    normx: bool = False,
    demean: bool = False,
    demed: bool = False,
    runner: Runner | None = None,
) -> V1dnormOutputs:
    """
    1dnorm

    Normalize columns of a 1D file (AFNI ASCII list of numbers)

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        infile: Input AFNI *.1D file (ASCII list of numbers arranged in columns);\
            if '-' input will be read from stdin.
        outfile: Output AFNI *.1D file (normalized); if '-' output will be written\
            to stdout.
        norm1: Normalize so sum of absolute values is 1 (L_1 norm)
        normx: Normalize so that max absolute value is 1 (L_infinity norm)
        demean: Subtract each column's mean before normalizing
        demed: Subtract each column's median before normalizing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1dnorm_params(
        infile=infile,
        outfile=outfile,
        norm1=norm1,
        normx=normx,
        demean=demean,
        demed=demed,
    )
    return v_1dnorm_execute(params, runner)

__all__ = [
    "V1dnorm",
    "V1dnormOutputs",
    "V_1DNORM_METADATA",
    "v_1dnorm_cargs",
    "v_1dnorm_outputs",
    "v_1dnorm_params",
    "v_1dnorm_execute",
    "v_1dnorm_validate",
    "v_1dnorm",
]