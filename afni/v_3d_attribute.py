# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_ATTRIBUTE_METADATA = Metadata(
    id="3dAttribute",
    name="3dAttribute",
    package="afni",
)

V3dAttribute = typing.TypedDict(
    "V3dAttribute",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dAttribute"]],
        "all": typing.NotRequired[bool],
        "name": typing.NotRequired[bool],
        "center": typing.NotRequired[bool],
        "ssep": typing.NotRequired[str],
        "sprep": typing.NotRequired[str],
        "quote": typing.NotRequired[bool],
        "aname": str,
        "dset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dAttributeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout: OutputPathType
    """Output of the attribute value"""

def v_3d_attribute_params(
    aname: str,
    dset: InputPathType,
    all: bool = False,
    name: bool = False,
    center: bool = False,
    ssep: str | None = None,
    sprep: str | None = None,
    quote: bool = False,
) -> V3dAttribute:
    """
    Build parameters.

    Args:
        aname: Attribute name to be printed from the dataset
        dset: Dataset from which the attribute value will be printed
        all: Print all attributes from the dataset
        name: Include attribute name in the output
        center: Print the center of volume in RAI coordinates
        ssep: Use string SSEP as a separator between strings for multiple\
            sub-bricks
        sprep: Use string SPREP to replace blank space in string attributes
        quote: Use single quote around each string

    Returns:
        Parameter dictionary.
    """
    params: V3dAttribute = {
        "@type": "afni/3dAttribute",
        "aname": aname,
        "dset": dset,
        "all": all,
        "name": name,
        "center": center,
        "quote": quote,
    }
    if ssep is not None:
        params["ssep"] = ssep
    if sprep is not None:
        params["sprep"] = sprep
    return params

def v_3d_attribute_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dAttribute."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("all", None) is not None:
        if not isinstance(params["all"], bool):
            raise StyxValidationError(f'`all` has the wrong type: Received `{type(params["all"])}` expected `bool`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], bool):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `bool`')
    if params.get("center", None) is not None:
        if not isinstance(params["center"], bool):
            raise StyxValidationError(f'`center` has the wrong type: Received `{type(params["center"])}` expected `bool`')
    if params.get("ssep", None) is not None:
        if not isinstance(params["ssep"], str):
            raise StyxValidationError(f'`ssep` has the wrong type: Received `{type(params["ssep"])}` expected `str`')
    if params.get("sprep", None) is not None:
        if not isinstance(params["sprep"], str):
            raise StyxValidationError(f'`sprep` has the wrong type: Received `{type(params["sprep"])}` expected `str`')
    if params.get("quote", None) is not None:
        if not isinstance(params["quote"], bool):
            raise StyxValidationError(f'`quote` has the wrong type: Received `{type(params["quote"])}` expected `bool`')
    if params.get("aname", None) is None:
        raise StyxValidationError("`aname` must not be None")
    if not isinstance(params["aname"], str):
        raise StyxValidationError(f'`aname` has the wrong type: Received `{type(params["aname"])}` expected `str`')
    if params.get("dset", None) is None:
        raise StyxValidationError("`dset` must not be None")
    if not isinstance(params["dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset` has the wrong type: Received `{type(params["dset"])}` expected `InputPathType`')

def v_3d_attribute_cargs(params: V3dAttribute, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dAttribute")
    if params.get("all"):
        cargs.append("-all")
    if params.get("name"):
        cargs.append("-name")
    if params.get("center"):
        cargs.append("-center")
    v_0 = params.get("ssep")
    if v_0 is not None:
        cargs.append("-ssep")
        cargs.append(v_0)
    v_1 = params.get("sprep")
    if v_1 is not None:
        cargs.append("-sprep")
        cargs.append(v_1)
    if params.get("quote"):
        cargs.append("-quote")
    cargs.append(params["aname"])
    cargs.append(execution.input_file(params["dset"]))
    return cargs

def v_3d_attribute_outputs(params: V3dAttribute, execution: Execution) -> V3dAttributeOutputs:
    """Build the V3dAttributeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stdout_v: OutputPathType = execution.output_file("stdout")
    return V3dAttributeOutputs(
        root=root_v,
        stdout=stdout_v,
    )

def v_3d_attribute_execute(params: V3dAttribute, runner: Runner | None = None) -> V3dAttributeOutputs:
    """
    3dAttribute

    Prints the value of the attribute 'aname' from the header of the dataset 'dset'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_attribute_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_ATTRIBUTE_METADATA)
    execution.params(params)
    args = v_3d_attribute_cargs(params, execution)
    out = v_3d_attribute_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_attribute(
    aname: str,
    dset: InputPathType,
    all: bool = False,
    name: bool = False,
    center: bool = False,
    ssep: str | None = None,
    sprep: str | None = None,
    quote: bool = False,
    runner: Runner | None = None,
) -> V3dAttributeOutputs:
    """
    3dAttribute

    Prints the value of the attribute 'aname' from the header of the dataset 'dset'.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        aname: Attribute name to be printed from the dataset
        dset: Dataset from which the attribute value will be printed
        all: Print all attributes from the dataset
        name: Include attribute name in the output
        center: Print the center of volume in RAI coordinates
        ssep: Use string SSEP as a separator between strings for multiple\
            sub-bricks
        sprep: Use string SPREP to replace blank space in string attributes
        quote: Use single quote around each string
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_attribute_params(
        aname=aname,
        dset=dset,
        all=all,
        name=name,
        center=center,
        ssep=ssep,
        sprep=sprep,
        quote=quote,
    )
    return v_3d_attribute_execute(params, runner)

__all__ = [
    "V3dAttribute",
    "V3dAttributeOutputs",
    "V_3D_ATTRIBUTE_METADATA",
    "v_3d_attribute_cargs",
    "v_3d_attribute_outputs",
    "v_3d_attribute_params",
    "v_3d_attribute_execute",
    "v_3d_attribute_validate",
    "v_3d_attribute",
]