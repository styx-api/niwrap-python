# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NP_METADATA = Metadata(
    id="@np",
    name="@np",
    package="afni",
)

Np = typing.TypedDict(
    "Np",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@np"]],
        "prefix": str,
    },
)

@dataclasses.dataclass
class NpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output text file with the appropriate new prefix."""

def np_params(
    prefix: str,
) -> Np:
    """
    Build parameters.

    Args:
        prefix: The prefix to be checked.

    Returns:
        Parameter dictionary.
    """
    params: Np = {
        "@type": "afni/@np",
        "prefix": prefix,
    }
    return params

def np_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Np."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def np_cargs(params: Np, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@np")
    cargs.append(params["prefix"])
    return cargs

def np_outputs(params: Np, execution: Execution) -> NpOutputs:
    """Build the NpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file("appropriate_prefix.txt")
    return NpOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def np_execute(params: Np, runner: Runner | None = None) -> NpOutputs:
    """
    @np

    Finds an appropriate new prefix to use, given the files you already have in your directory. It automatically creates a valid prefix when you are repeatedly running similar commands but do not want to delete previous output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    np_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NP_METADATA)
    execution.params(params)
    args = np_cargs(params, execution)
    out = np_outputs(params, execution)
    execution.run(args)
    return out

def np(
    prefix: str,
    runner: Runner | None = None,
) -> NpOutputs:
    """
    @np

    Finds an appropriate new prefix to use, given the files you already have in your directory. It automatically creates a valid prefix when you are repeatedly running similar commands but do not want to delete previous output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: The prefix to be checked.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = np_params(
        prefix=prefix,
    )
    return np_execute(params, runner)

__all__ = [
    "Np",
    "NpOutputs",
    "NP_METADATA",
    "np_cargs",
    "np_outputs",
    "np_params",
    "np_execute",
    "np_validate",
    "np",
]