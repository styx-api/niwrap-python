# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMMASK_METADATA = Metadata(
    id="immask",
    name="immask",
    package="afni",
)

Immask = typing.TypedDict(
    "Immask",
    {
        "@type": typing.NotRequired[typing.Literal["afni/immask"]],
        "threshold": typing.NotRequired[float],
        "mask_image": typing.NotRequired[InputPathType],
        "positive_only": typing.NotRequired[bool],
        "input_image": InputPathType,
        "output_image": str,
    },
)

@dataclasses.dataclass
class ImmaskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Masked output image"""

def immask_params(
    input_image: InputPathType,
    output_image: str,
    threshold: float | None = None,
    mask_image: InputPathType | None = None,
    positive_only: bool = False,
) -> Immask:
    """
    Build parameters.

    Args:
        input_image: Input image to be masked
        output_image: Output image after masking
        threshold: Threshold value; all pixels with absolute value below this will\
            be set to zero in the output image
        mask_image: Mask image; only locations that are nonzero in the mask image\
            will be nonzero in the output image
        positive_only: Use only positive pixels from input image

    Returns:
        Parameter dictionary.
    """
    params: Immask = {
        "@type": "afni/immask",
        "input_image": input_image,
        "output_image": output_image,
        "positive_only": positive_only,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if mask_image is not None:
        params["mask_image"] = mask_image
    return params

def immask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Immask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("positive_only", None) is not None:
        if not isinstance(params["positive_only"], bool):
            raise StyxValidationError(f'`positive_only` has the wrong type: Received `{type(params["positive_only"])}` expected `bool`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')

def immask_cargs(params: Immask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("immask")
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-thresh")
        cargs.append(str(v_0))
    v_1 = params.get("mask_image")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    if params.get("positive_only"):
        cargs.append("-pos")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    return cargs

def immask_outputs(params: Immask, execution: Execution) -> ImmaskOutputs:
    """Build the ImmaskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_image"])
    return ImmaskOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def immask_execute(params: Immask, runner: Runner | None = None) -> ImmaskOutputs:
    """
    immask

    Masks the input image based on specified criteria and produces the output image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    immask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMMASK_METADATA)
    execution.params(params)
    args = immask_cargs(params, execution)
    out = immask_outputs(params, execution)
    execution.run(args)
    return out

def immask(
    input_image: InputPathType,
    output_image: str,
    threshold: float | None = None,
    mask_image: InputPathType | None = None,
    positive_only: bool = False,
    runner: Runner | None = None,
) -> ImmaskOutputs:
    """
    immask

    Masks the input image based on specified criteria and produces the output image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_image: Input image to be masked
        output_image: Output image after masking
        threshold: Threshold value; all pixels with absolute value below this will\
            be set to zero in the output image
        mask_image: Mask image; only locations that are nonzero in the mask image\
            will be nonzero in the output image
        positive_only: Use only positive pixels from input image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = immask_params(
        input_image=input_image,
        output_image=output_image,
        threshold=threshold,
        mask_image=mask_image,
        positive_only=positive_only,
    )
    return immask_execute(params, runner)

__all__ = [
    "Immask",
    "ImmaskOutputs",
    "IMMASK_METADATA",
    "immask_cargs",
    "immask_outputs",
    "immask_params",
    "immask_execute",
    "immask_validate",
    "immask",
]