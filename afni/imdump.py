# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMDUMP_METADATA = Metadata(
    id="imdump",
    name="imdump",
    package="afni",
)

Imdump = typing.TypedDict(
    "Imdump",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imdump"]],
        "input_image": InputPathType,
    },
)

@dataclasses.dataclass
class ImdumpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stdout: OutputPathType
    """Nonzero pixels in the format: x-index y-index value, one pixel per line."""

def imdump_params(
    input_image: InputPathType,
) -> Imdump:
    """
    Build parameters.

    Args:
        input_image: Input image file to be processed.

    Returns:
        Parameter dictionary.
    """
    params: Imdump = {
        "@type": "afni/imdump",
        "input_image": input_image,
    }
    return params

def imdump_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imdump."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')

def imdump_cargs(params: Imdump, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imdump")
    cargs.append(execution.input_file(params["input_image"]))
    return cargs

def imdump_outputs(params: Imdump, execution: Execution) -> ImdumpOutputs:
    """Build the ImdumpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stdout_v: OutputPathType = execution.output_file("stdout.txt")
    return ImdumpOutputs(
        root=root_v,
        stdout=stdout_v,
    )

def imdump_execute(params: Imdump, runner: Runner | None = None) -> ImdumpOutputs:
    """
    imdump

    Prints out nonzero pixels in an image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imdump_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMDUMP_METADATA)
    execution.params(params)
    args = imdump_cargs(params, execution)
    out = imdump_outputs(params, execution)
    execution.run(args)
    return out

def imdump(
    input_image: InputPathType,
    runner: Runner | None = None,
) -> ImdumpOutputs:
    """
    imdump

    Prints out nonzero pixels in an image

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_image: Input image file to be processed.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imdump_params(
        input_image=input_image,
    )
    return imdump_execute(params, runner)

__all__ = [
    "Imdump",
    "ImdumpOutputs",
    "IMDUMP_METADATA",
    "imdump_cargs",
    "imdump_outputs",
    "imdump_params",
    "imdump_execute",
    "imdump_validate",
    "imdump",
]