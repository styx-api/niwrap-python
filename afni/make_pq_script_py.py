# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKE_PQ_SCRIPT_PY_METADATA = Metadata(
    id="make_pq_script.py",
    name="make_pq_script.py",
    package="afni",
)

MakePqScriptPy = typing.TypedDict(
    "MakePqScriptPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/make_pq_script.py"]],
        "dataset": InputPathType,
        "brick_index": float,
        "mask": InputPathType,
        "out_script": str,
    },
)

@dataclasses.dataclass
class MakePqScriptPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    script: OutputPathType
    """Generated output script"""

def make_pq_script_py_params(
    dataset: InputPathType,
    brick_index: float,
    mask: InputPathType,
    out_script: str,
) -> MakePqScriptPy:
    """
    Build parameters.

    Args:
        dataset: Input dataset (no sub-brick selectors)
        brick_index: Volume sub-brick for specific t-stat
        mask: Mask volume dataset
        out_script: Name for output script to write

    Returns:
        Parameter dictionary.
    """
    params: MakePqScriptPy = {
        "@type": "afni/make_pq_script.py",
        "dataset": dataset,
        "brick_index": brick_index,
        "mask": mask,
        "out_script": out_script,
    }
    return params

def make_pq_script_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MakePqScriptPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')
    if params.get("brick_index", None) is None:
        raise StyxValidationError("`brick_index` must not be None")
    if not isinstance(params["brick_index"], (float, int)):
        raise StyxValidationError(f'`brick_index` has the wrong type: Received `{type(params["brick_index"])}` expected `float`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("out_script", None) is None:
        raise StyxValidationError("`out_script` must not be None")
    if not isinstance(params["out_script"], str):
        raise StyxValidationError(f'`out_script` has the wrong type: Received `{type(params["out_script"])}` expected `str`')

def make_pq_script_py_cargs(params: MakePqScriptPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("make_pq_script.py")
    cargs.append(execution.input_file(params["dataset"]))
    cargs.append(str(params["brick_index"]))
    cargs.append(execution.input_file(params["mask"]))
    cargs.append(params["out_script"])
    return cargs

def make_pq_script_py_outputs(params: MakePqScriptPy, execution: Execution) -> MakePqScriptPyOutputs:
    """Build the MakePqScriptPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    script_v: OutputPathType = execution.output_file(params["out_script"])
    return MakePqScriptPyOutputs(
        root=root_v,
        script=script_v,
    )

def make_pq_script_py_execute(params: MakePqScriptPy, runner: Runner | None = None) -> MakePqScriptPyOutputs:
    """
    make_pq_script.py

    Creates a script to compute p-value and q-value curves.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    make_pq_script_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKE_PQ_SCRIPT_PY_METADATA)
    execution.params(params)
    args = make_pq_script_py_cargs(params, execution)
    out = make_pq_script_py_outputs(params, execution)
    execution.run(args)
    return out

def make_pq_script_py(
    dataset: InputPathType,
    brick_index: float,
    mask: InputPathType,
    out_script: str,
    runner: Runner | None = None,
) -> MakePqScriptPyOutputs:
    """
    make_pq_script.py

    Creates a script to compute p-value and q-value curves.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset (no sub-brick selectors)
        brick_index: Volume sub-brick for specific t-stat
        mask: Mask volume dataset
        out_script: Name for output script to write
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = make_pq_script_py_params(
        dataset=dataset,
        brick_index=brick_index,
        mask=mask,
        out_script=out_script,
    )
    return make_pq_script_py_execute(params, runner)

__all__ = [
    "MakePqScriptPy",
    "MakePqScriptPyOutputs",
    "MAKE_PQ_SCRIPT_PY_METADATA",
    "make_pq_script_py_cargs",
    "make_pq_script_py_outputs",
    "make_pq_script_py_params",
    "make_pq_script_py_execute",
    "make_pq_script_py_validate",
    "make_pq_script_py",
]