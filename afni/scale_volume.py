# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SCALE_VOLUME_METADATA = Metadata(
    id="@ScaleVolume",
    name="@ScaleVolume",
    package="afni",
)

ScaleVolume = typing.TypedDict(
    "ScaleVolume",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ScaleVolume"]],
        "input_dset": InputPathType,
        "prefix": str,
        "val_clip": typing.NotRequired[list[float]],
        "perc_clip": typing.NotRequired[list[float]],
        "scale_by_mean": typing.NotRequired[bool],
        "scale_by_median": typing.NotRequired[bool],
        "norm": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class ScaleVolumeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output scaled dataset"""

def scale_volume_params(
    input_dset: InputPathType,
    prefix: str,
    val_clip: list[float] | None = None,
    perc_clip: list[float] | None = None,
    scale_by_mean: bool = False,
    scale_by_median: bool = False,
    norm: bool = False,
    mask: InputPathType | None = None,
) -> ScaleVolume:
    """
    Build parameters.

    Args:
        input_dset: Dataset to scale
        prefix: Prefix of output
        val_clip: Min and Max of output dataset. Default V0 = 0 and V1 = 255
        perc_clip: Set lowest P0 percentile to Min and highest P1 percentile to\
            Max. Default P0 = 2 and P1 = 98
        scale_by_mean: Divide each sub-brick by mean of non-zero voxels
        scale_by_median: Divide each sub-brick by median of non-zero voxels
        norm: For each time series T, Tnorm = (T - mean(T)) / stdev(T)
        mask: Restrict to non-zero values of given mask dataset

    Returns:
        Parameter dictionary.
    """
    params: ScaleVolume = {
        "@type": "afni/@ScaleVolume",
        "input_dset": input_dset,
        "prefix": prefix,
        "scale_by_mean": scale_by_mean,
        "scale_by_median": scale_by_median,
        "norm": norm,
    }
    if val_clip is not None:
        params["val_clip"] = val_clip
    if perc_clip is not None:
        params["perc_clip"] = perc_clip
    if mask is not None:
        params["mask"] = mask
    return params

def scale_volume_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ScaleVolume."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dset", None) is None:
        raise StyxValidationError("`input_dset` must not be None")
    if not isinstance(params["input_dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dset` has the wrong type: Received `{type(params["input_dset"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("val_clip", None) is not None:
        if not isinstance(params["val_clip"], list):
            raise StyxValidationError(f'`val_clip` has the wrong type: Received `{type(params["val_clip"])}` expected `list[float]`')
        if not (2 <= len(params["val_clip"]) <= 2):
            raise StyxValidationError("Parameter `val_clip` must contain between 2 and 2 elements (inclusive)")
        for e in params["val_clip"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`val_clip` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("perc_clip", None) is not None:
        if not isinstance(params["perc_clip"], list):
            raise StyxValidationError(f'`perc_clip` has the wrong type: Received `{type(params["perc_clip"])}` expected `list[float]`')
        if not (2 <= len(params["perc_clip"]) <= 2):
            raise StyxValidationError("Parameter `perc_clip` must contain between 2 and 2 elements (inclusive)")
        for e_2 in params["perc_clip"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`perc_clip` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("scale_by_mean", None) is not None:
        if not isinstance(params["scale_by_mean"], bool):
            raise StyxValidationError(f'`scale_by_mean` has the wrong type: Received `{type(params["scale_by_mean"])}` expected `bool`')
    if params.get("scale_by_median", None) is not None:
        if not isinstance(params["scale_by_median"], bool):
            raise StyxValidationError(f'`scale_by_median` has the wrong type: Received `{type(params["scale_by_median"])}` expected `bool`')
    if params.get("norm", None) is not None:
        if not isinstance(params["norm"], bool):
            raise StyxValidationError(f'`norm` has the wrong type: Received `{type(params["norm"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')

def scale_volume_cargs(params: ScaleVolume, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ScaleVolume")
    cargs.append(execution.input_file(params["input_dset"]))
    cargs.append(params["prefix"])
    v_0 = params.get("val_clip")
    if v_0 is not None:
        cargs.append("-val_clip")
        for item0 in v_0:
            cargs.append(str(item0))
    v_1 = params.get("perc_clip")
    if v_1 is not None:
        cargs.append("-perc_clip")
        for item1 in v_1:
            cargs.append(str(item1))
    if params.get("scale_by_mean"):
        cargs.append("-scale_by_mean")
    if params.get("scale_by_median"):
        cargs.append("-scale_by_median")
    if params.get("norm"):
        cargs.append("-norm")
    v_2 = params.get("mask")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    return cargs

def scale_volume_outputs(params: ScaleVolume, execution: Execution) -> ScaleVolumeOutputs:
    """Build the ScaleVolumeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}_scaled')
    return ScaleVolumeOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def scale_volume_execute(params: ScaleVolume, runner: Runner | None = None) -> ScaleVolumeOutputs:
    """
    @ScaleVolume

    A tool to scale the volume of datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    scale_volume_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SCALE_VOLUME_METADATA)
    execution.params(params)
    args = scale_volume_cargs(params, execution)
    out = scale_volume_outputs(params, execution)
    execution.run(args)
    return out

def scale_volume(
    input_dset: InputPathType,
    prefix: str,
    val_clip: list[float] | None = None,
    perc_clip: list[float] | None = None,
    scale_by_mean: bool = False,
    scale_by_median: bool = False,
    norm: bool = False,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> ScaleVolumeOutputs:
    """
    @ScaleVolume

    A tool to scale the volume of datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dset: Dataset to scale
        prefix: Prefix of output
        val_clip: Min and Max of output dataset. Default V0 = 0 and V1 = 255
        perc_clip: Set lowest P0 percentile to Min and highest P1 percentile to\
            Max. Default P0 = 2 and P1 = 98
        scale_by_mean: Divide each sub-brick by mean of non-zero voxels
        scale_by_median: Divide each sub-brick by median of non-zero voxels
        norm: For each time series T, Tnorm = (T - mean(T)) / stdev(T)
        mask: Restrict to non-zero values of given mask dataset
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = scale_volume_params(
        input_dset=input_dset,
        prefix=prefix,
        val_clip=val_clip,
        perc_clip=perc_clip,
        scale_by_mean=scale_by_mean,
        scale_by_median=scale_by_median,
        norm=norm,
        mask=mask,
    )
    return scale_volume_execute(params, runner)

__all__ = [
    "ScaleVolume",
    "ScaleVolumeOutputs",
    "SCALE_VOLUME_METADATA",
    "scale_volume_cargs",
    "scale_volume_outputs",
    "scale_volume_params",
    "scale_volume_execute",
    "scale_volume_validate",
    "scale_volume",
]