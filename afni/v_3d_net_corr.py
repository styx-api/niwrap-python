# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_NET_CORR_METADATA = Metadata(
    id="3dNetCorr",
    name="3dNetCorr",
    package="afni",
)

V3dNetCorr = typing.TypedDict(
    "V3dNetCorr",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dNetCorr"]],
        "prefix": str,
        "inset": InputPathType,
        "in_rois": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "fish_z": typing.NotRequired[bool],
        "part_corr": typing.NotRequired[bool],
        "ts_out": typing.NotRequired[bool],
        "ts_label": typing.NotRequired[bool],
        "ts_indiv": typing.NotRequired[bool],
        "ts_wb_corr": typing.NotRequired[bool],
        "ts_wb_Z": typing.NotRequired[bool],
        "weight_ts": typing.NotRequired[InputPathType],
        "weight_corr": typing.NotRequired[InputPathType],
        "ts_wb_strlabel": typing.NotRequired[bool],
        "nifti": typing.NotRequired[bool],
        "output_mask_nonnull": typing.NotRequired[bool],
        "push_thru_many_zeros": typing.NotRequired[bool],
        "allow_roi_zeros": typing.NotRequired[bool],
        "automask_off": typing.NotRequired[bool],
        "ignore_LT": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dNetCorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_netcc: OutputPathType
    """Output correlation matrix file for network 000"""
    output_netts: OutputPathType
    """Output mean time series per ROI for network 000"""
    output_niml: OutputPathType
    """NIML/SUMA-esque file for visualizing connectivity info in a 3D brain for network 000"""
    output_roidat: OutputPathType
    """Columns contain information for each ROI in the used mask."""
    output_mask_nnull: OutputPathType
    """Mask of non-null time series"""
    output_indiv: OutputPathType
    """Directory containing individual time series files for network 000"""
    output_binary_mask_nnull: OutputPathType
    """Binary mask of the non-null time series"""

def v_3d_net_corr_params(
    prefix: str,
    inset: InputPathType,
    in_rois: InputPathType,
    mask: InputPathType | None = None,
    fish_z: bool = False,
    part_corr: bool = False,
    ts_out: bool = False,
    ts_label: bool = False,
    ts_indiv: bool = False,
    ts_wb_corr: bool = False,
    ts_wb_Z: bool = False,
    weight_ts: InputPathType | None = None,
    weight_corr: InputPathType | None = None,
    ts_wb_strlabel: bool = False,
    nifti: bool = False,
    output_mask_nonnull: bool = False,
    push_thru_many_zeros: bool = False,
    allow_roi_zeros: bool = False,
    automask_off: bool = False,
    ignore_LT: bool = False,
) -> V3dNetCorr:
    """
    Build parameters.

    Args:
        prefix: Output file name prefix
        inset: Time series file (4D data set)
        in_rois: Input a set of ROIs each labelled with distinct integers. Multiple\
            subbricks can be input, each will be treated as a separate network.
        mask: Whole brain mask within which to calculate correlation.
        fish_z: Output Fisher Z-transform matrix along with correlation matrix.
        part_corr: Output the partial correlation matrix.
        ts_out: Output the mean time series of the ROIs.
        ts_label: Insert the integer ROI label at the start of each line of the\
            *.netts file created.
        ts_indiv: Create a directory for each network that contains the average\
            time series for each ROI in individual files.
        ts_wb_corr: Perform whole brain correlation for each ROI's average time\
            series and output as Pearson 'r' values.
        ts_wb_Z: Perform whole brain correlation for each ROI's average time series\
            and output as Fisher transformed Z-scores.
        weight_ts: Input a 1D file of weights to be applied multiplicatively to\
            each ROI's average time series.
        weight_corr: Input a 1D file of weights to estimate a weighted Pearson\
            Correlation.
        ts_wb_strlabel: Apply string labels to the WB correlation/Z-score output\
            files.
        nifti: Output any correlation map files as NIFTI files.
        output_mask_nonnull: Output mask of non-null time series.
        push_thru_many_zeros: Push through the calculation even if any ROI contains\
            more than 10 percent of voxels with null time series.
        allow_roi_zeros: Allow ROIs to have all-zero time series.
        automask_off: Disable internal automasking of where time series are not\
            uniformly zero.
        ignore_LT: Ignore any label table labels in the '-in_rois' file.

    Returns:
        Parameter dictionary.
    """
    params: V3dNetCorr = {
        "@type": "afni/3dNetCorr",
        "prefix": prefix,
        "inset": inset,
        "in_rois": in_rois,
        "fish_z": fish_z,
        "part_corr": part_corr,
        "ts_out": ts_out,
        "ts_label": ts_label,
        "ts_indiv": ts_indiv,
        "ts_wb_corr": ts_wb_corr,
        "ts_wb_Z": ts_wb_Z,
        "ts_wb_strlabel": ts_wb_strlabel,
        "nifti": nifti,
        "output_mask_nonnull": output_mask_nonnull,
        "push_thru_many_zeros": push_thru_many_zeros,
        "allow_roi_zeros": allow_roi_zeros,
        "automask_off": automask_off,
        "ignore_LT": ignore_LT,
    }
    if mask is not None:
        params["mask"] = mask
    if weight_ts is not None:
        params["weight_ts"] = weight_ts
    if weight_corr is not None:
        params["weight_corr"] = weight_corr
    return params

def v_3d_net_corr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dNetCorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("inset", None) is None:
        raise StyxValidationError("`inset` must not be None")
    if not isinstance(params["inset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`inset` has the wrong type: Received `{type(params["inset"])}` expected `InputPathType`')
    if params.get("in_rois", None) is None:
        raise StyxValidationError("`in_rois` must not be None")
    if not isinstance(params["in_rois"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_rois` has the wrong type: Received `{type(params["in_rois"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("fish_z", None) is not None:
        if not isinstance(params["fish_z"], bool):
            raise StyxValidationError(f'`fish_z` has the wrong type: Received `{type(params["fish_z"])}` expected `bool`')
    if params.get("part_corr", None) is not None:
        if not isinstance(params["part_corr"], bool):
            raise StyxValidationError(f'`part_corr` has the wrong type: Received `{type(params["part_corr"])}` expected `bool`')
    if params.get("ts_out", None) is not None:
        if not isinstance(params["ts_out"], bool):
            raise StyxValidationError(f'`ts_out` has the wrong type: Received `{type(params["ts_out"])}` expected `bool`')
    if params.get("ts_label", None) is not None:
        if not isinstance(params["ts_label"], bool):
            raise StyxValidationError(f'`ts_label` has the wrong type: Received `{type(params["ts_label"])}` expected `bool`')
    if params.get("ts_indiv", None) is not None:
        if not isinstance(params["ts_indiv"], bool):
            raise StyxValidationError(f'`ts_indiv` has the wrong type: Received `{type(params["ts_indiv"])}` expected `bool`')
    if params.get("ts_wb_corr", None) is not None:
        if not isinstance(params["ts_wb_corr"], bool):
            raise StyxValidationError(f'`ts_wb_corr` has the wrong type: Received `{type(params["ts_wb_corr"])}` expected `bool`')
    if params.get("ts_wb_Z", None) is not None:
        if not isinstance(params["ts_wb_Z"], bool):
            raise StyxValidationError(f'`ts_wb_Z` has the wrong type: Received `{type(params["ts_wb_Z"])}` expected `bool`')
    if params.get("weight_ts", None) is not None:
        if not isinstance(params["weight_ts"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weight_ts` has the wrong type: Received `{type(params["weight_ts"])}` expected `InputPathType`')
    if params.get("weight_corr", None) is not None:
        if not isinstance(params["weight_corr"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weight_corr` has the wrong type: Received `{type(params["weight_corr"])}` expected `InputPathType`')
    if params.get("ts_wb_strlabel", None) is not None:
        if not isinstance(params["ts_wb_strlabel"], bool):
            raise StyxValidationError(f'`ts_wb_strlabel` has the wrong type: Received `{type(params["ts_wb_strlabel"])}` expected `bool`')
    if params.get("nifti", None) is not None:
        if not isinstance(params["nifti"], bool):
            raise StyxValidationError(f'`nifti` has the wrong type: Received `{type(params["nifti"])}` expected `bool`')
    if params.get("output_mask_nonnull", None) is not None:
        if not isinstance(params["output_mask_nonnull"], bool):
            raise StyxValidationError(f'`output_mask_nonnull` has the wrong type: Received `{type(params["output_mask_nonnull"])}` expected `bool`')
    if params.get("push_thru_many_zeros", None) is not None:
        if not isinstance(params["push_thru_many_zeros"], bool):
            raise StyxValidationError(f'`push_thru_many_zeros` has the wrong type: Received `{type(params["push_thru_many_zeros"])}` expected `bool`')
    if params.get("allow_roi_zeros", None) is not None:
        if not isinstance(params["allow_roi_zeros"], bool):
            raise StyxValidationError(f'`allow_roi_zeros` has the wrong type: Received `{type(params["allow_roi_zeros"])}` expected `bool`')
    if params.get("automask_off", None) is not None:
        if not isinstance(params["automask_off"], bool):
            raise StyxValidationError(f'`automask_off` has the wrong type: Received `{type(params["automask_off"])}` expected `bool`')
    if params.get("ignore_LT", None) is not None:
        if not isinstance(params["ignore_LT"], bool):
            raise StyxValidationError(f'`ignore_LT` has the wrong type: Received `{type(params["ignore_LT"])}` expected `bool`')

def v_3d_net_corr_cargs(params: V3dNetCorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dNetCorr")
    cargs.append(params["prefix"])
    cargs.append(execution.input_file(params["inset"]))
    cargs.append(execution.input_file(params["in_rois"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("fish_z"):
        cargs.append("-fish_z")
    if params.get("part_corr"):
        cargs.append("-part_corr")
    if params.get("ts_out"):
        cargs.append("-ts_out")
    if params.get("ts_label"):
        cargs.append("-ts_label")
    if params.get("ts_indiv"):
        cargs.append("-ts_indiv")
    if params.get("ts_wb_corr"):
        cargs.append("-ts_wb_corr")
    if params.get("ts_wb_Z"):
        cargs.append("-ts_wb_Z")
    v_1 = params.get("weight_ts")
    if v_1 is not None:
        cargs.append("-weight_ts")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("weight_corr")
    if v_2 is not None:
        cargs.append("-weight_corr")
        cargs.append(execution.input_file(v_2))
    if params.get("ts_wb_strlabel"):
        cargs.append("-ts_wb_strlabel")
    if params.get("nifti"):
        cargs.append("-nifti")
    if params.get("output_mask_nonnull"):
        cargs.append("-output_mask_nonnull")
    if params.get("push_thru_many_zeros"):
        cargs.append("-push_thru_many_zeros")
    if params.get("allow_roi_zeros"):
        cargs.append("-allow_roi_zeros")
    if params.get("automask_off"):
        cargs.append("-automask_off")
    if params.get("ignore_LT"):
        cargs.append("-ignore_LT")
    return cargs

def v_3d_net_corr_outputs(params: V3dNetCorr, execution: Execution) -> V3dNetCorrOutputs:
    """Build the V3dNetCorrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_netcc_v: OutputPathType = execution.output_file(f'{params["prefix"]}_000.netcc')
    output_netts_v: OutputPathType = execution.output_file(f'{params["prefix"]}_000.netts')
    output_niml_v: OutputPathType = execution.output_file(f'{params["prefix"]}_000.niml.dset')
    output_roidat_v: OutputPathType = execution.output_file(f'{params["prefix"]}.roidat')
    output_mask_nnull_v: OutputPathType = execution.output_file(f'{params["prefix"]}_mask_nnull')
    output_indiv_v: OutputPathType = execution.output_file(f'{params["prefix"]}_000_INDIV')
    output_binary_mask_nnull_v: OutputPathType = execution.output_file("PREFIX_mask_nnull")
    return V3dNetCorrOutputs(
        root=root_v,
        output_netcc=output_netcc_v,
        output_netts=output_netts_v,
        output_niml=output_niml_v,
        output_roidat=output_roidat_v,
        output_mask_nnull=output_mask_nnull_v,
        output_indiv=output_indiv_v,
        output_binary_mask_nnull=output_binary_mask_nnull_v,
    )

def v_3d_net_corr_execute(params: V3dNetCorr, runner: Runner | None = None) -> V3dNetCorrOutputs:
    """
    3dNetCorr

    Compute correlation matrix of a set of ROIs based on mean time series

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_net_corr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_NET_CORR_METADATA)
    execution.params(params)
    args = v_3d_net_corr_cargs(params, execution)
    out = v_3d_net_corr_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_net_corr(
    prefix: str,
    inset: InputPathType,
    in_rois: InputPathType,
    mask: InputPathType | None = None,
    fish_z: bool = False,
    part_corr: bool = False,
    ts_out: bool = False,
    ts_label: bool = False,
    ts_indiv: bool = False,
    ts_wb_corr: bool = False,
    ts_wb_Z: bool = False,
    weight_ts: InputPathType | None = None,
    weight_corr: InputPathType | None = None,
    ts_wb_strlabel: bool = False,
    nifti: bool = False,
    output_mask_nonnull: bool = False,
    push_thru_many_zeros: bool = False,
    allow_roi_zeros: bool = False,
    automask_off: bool = False,
    ignore_LT: bool = False,
    runner: Runner | None = None,
) -> V3dNetCorrOutputs:
    """
    3dNetCorr

    Compute correlation matrix of a set of ROIs based on mean time series

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output file name prefix
        inset: Time series file (4D data set)
        in_rois: Input a set of ROIs each labelled with distinct integers. Multiple\
            subbricks can be input, each will be treated as a separate network.
        mask: Whole brain mask within which to calculate correlation.
        fish_z: Output Fisher Z-transform matrix along with correlation matrix.
        part_corr: Output the partial correlation matrix.
        ts_out: Output the mean time series of the ROIs.
        ts_label: Insert the integer ROI label at the start of each line of the\
            *.netts file created.
        ts_indiv: Create a directory for each network that contains the average\
            time series for each ROI in individual files.
        ts_wb_corr: Perform whole brain correlation for each ROI's average time\
            series and output as Pearson 'r' values.
        ts_wb_Z: Perform whole brain correlation for each ROI's average time series\
            and output as Fisher transformed Z-scores.
        weight_ts: Input a 1D file of weights to be applied multiplicatively to\
            each ROI's average time series.
        weight_corr: Input a 1D file of weights to estimate a weighted Pearson\
            Correlation.
        ts_wb_strlabel: Apply string labels to the WB correlation/Z-score output\
            files.
        nifti: Output any correlation map files as NIFTI files.
        output_mask_nonnull: Output mask of non-null time series.
        push_thru_many_zeros: Push through the calculation even if any ROI contains\
            more than 10 percent of voxels with null time series.
        allow_roi_zeros: Allow ROIs to have all-zero time series.
        automask_off: Disable internal automasking of where time series are not\
            uniformly zero.
        ignore_LT: Ignore any label table labels in the '-in_rois' file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_net_corr_params(
        prefix=prefix,
        inset=inset,
        in_rois=in_rois,
        mask=mask,
        fish_z=fish_z,
        part_corr=part_corr,
        ts_out=ts_out,
        ts_label=ts_label,
        ts_indiv=ts_indiv,
        ts_wb_corr=ts_wb_corr,
        ts_wb_Z=ts_wb_Z,
        weight_ts=weight_ts,
        weight_corr=weight_corr,
        ts_wb_strlabel=ts_wb_strlabel,
        nifti=nifti,
        output_mask_nonnull=output_mask_nonnull,
        push_thru_many_zeros=push_thru_many_zeros,
        allow_roi_zeros=allow_roi_zeros,
        automask_off=automask_off,
        ignore_LT=ignore_LT,
    )
    return v_3d_net_corr_execute(params, runner)

__all__ = [
    "V3dNetCorr",
    "V3dNetCorrOutputs",
    "V_3D_NET_CORR_METADATA",
    "v_3d_net_corr_cargs",
    "v_3d_net_corr_outputs",
    "v_3d_net_corr_params",
    "v_3d_net_corr_execute",
    "v_3d_net_corr_validate",
    "v_3d_net_corr",
]