# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DICOM_HINFO_METADATA = Metadata(
    id="dicom_hinfo",
    name="dicom_hinfo",
    package="afni",
)

DicomHinfo = typing.TypedDict(
    "DicomHinfo",
    {
        "@type": typing.NotRequired[typing.Literal["afni/dicom_hinfo"]],
        "tag": list[str],
        "sepstr": typing.NotRequired[str],
        "full_entry": typing.NotRequired[bool],
        "no_name": typing.NotRequired[bool],
        "namelast": typing.NotRequired[bool],
        "files": list[InputPathType],
    },
)

@dataclasses.dataclass
class DicomHinfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dicom_hinfo_params(
    tag: list[str],
    files: list[InputPathType],
    sepstr: str | None = None,
    full_entry: bool = False,
    no_name: bool = False,
    namelast: bool = False,
) -> DicomHinfo:
    """
    Build parameters.

    Args:
        tag: Specify one or more DICOM tags to print, in the format aaaa,bbbb where\
            aaaa and bbbb are hexadecimal digits.
        files: DICOM file(s) to process.
        sepstr: Use the specified string to separate fields instead of space.
        full_entry: Output the full entry if it is more than one word or contains\
            white space.
        no_name: Omit the filename from the output.
        namelast: Place the filename last in the output instead of first.

    Returns:
        Parameter dictionary.
    """
    params: DicomHinfo = {
        "@type": "afni/dicom_hinfo",
        "tag": tag,
        "files": files,
        "full_entry": full_entry,
        "no_name": no_name,
        "namelast": namelast,
    }
    if sepstr is not None:
        params["sepstr"] = sepstr
    return params

def dicom_hinfo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DicomHinfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tag", None) is None:
        raise StyxValidationError("`tag` must not be None")
    if not isinstance(params["tag"], list):
        raise StyxValidationError(f'`tag` has the wrong type: Received `{type(params["tag"])}` expected `list[str]`')
    for e in params["tag"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`tag` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("sepstr", None) is not None:
        if not isinstance(params["sepstr"], str):
            raise StyxValidationError(f'`sepstr` has the wrong type: Received `{type(params["sepstr"])}` expected `str`')
    if params.get("full_entry", None) is not None:
        if not isinstance(params["full_entry"], bool):
            raise StyxValidationError(f'`full_entry` has the wrong type: Received `{type(params["full_entry"])}` expected `bool`')
    if params.get("no_name", None) is not None:
        if not isinstance(params["no_name"], bool):
            raise StyxValidationError(f'`no_name` has the wrong type: Received `{type(params["no_name"])}` expected `bool`')
    if params.get("namelast", None) is not None:
        if not isinstance(params["namelast"], bool):
            raise StyxValidationError(f'`namelast` has the wrong type: Received `{type(params["namelast"])}` expected `bool`')
    if params.get("files", None) is None:
        raise StyxValidationError("`files` must not be None")
    if not isinstance(params["files"], list):
        raise StyxValidationError(f'`files` has the wrong type: Received `{type(params["files"])}` expected `list[InputPathType]`')
    for e_2 in params["files"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`files` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')

def dicom_hinfo_cargs(params: DicomHinfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dicom_hinfo")
    cargs.append("-tag")
    for item0 in params["tag"]:
        cargs.append(item0)
    v_0 = params.get("sepstr")
    if v_0 is not None:
        cargs.append("-sepstr")
        cargs.append(v_0)
    if params.get("full_entry"):
        cargs.append("-full_entry")
    if params.get("no_name"):
        cargs.append("-no_name")
    if params.get("namelast"):
        cargs.append("-namelast")
    for item1 in params["files"]:
        cargs.append(execution.input_file(item1))
    return cargs

def dicom_hinfo_outputs(params: DicomHinfo, execution: Execution) -> DicomHinfoOutputs:
    """Build the DicomHinfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DicomHinfoOutputs(
        root=root_v,
    )

def dicom_hinfo_execute(params: DicomHinfo, runner: Runner | None = None) -> DicomHinfoOutputs:
    """
    dicom_hinfo

    Prints selected information from one or more DICOM files to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dicom_hinfo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DICOM_HINFO_METADATA)
    execution.params(params)
    args = dicom_hinfo_cargs(params, execution)
    out = dicom_hinfo_outputs(params, execution)
    execution.run(args)
    return out

def dicom_hinfo(
    tag: list[str],
    files: list[InputPathType],
    sepstr: str | None = None,
    full_entry: bool = False,
    no_name: bool = False,
    namelast: bool = False,
    runner: Runner | None = None,
) -> DicomHinfoOutputs:
    """
    dicom_hinfo

    Prints selected information from one or more DICOM files to stdout.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        tag: Specify one or more DICOM tags to print, in the format aaaa,bbbb where\
            aaaa and bbbb are hexadecimal digits.
        files: DICOM file(s) to process.
        sepstr: Use the specified string to separate fields instead of space.
        full_entry: Output the full entry if it is more than one word or contains\
            white space.
        no_name: Omit the filename from the output.
        namelast: Place the filename last in the output instead of first.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dicom_hinfo_params(
        tag=tag,
        files=files,
        sepstr=sepstr,
        full_entry=full_entry,
        no_name=no_name,
        namelast=namelast,
    )
    return dicom_hinfo_execute(params, runner)

__all__ = [
    "DicomHinfo",
    "DicomHinfoOutputs",
    "DICOM_HINFO_METADATA",
    "dicom_hinfo_cargs",
    "dicom_hinfo_outputs",
    "dicom_hinfo_params",
    "dicom_hinfo_execute",
    "dicom_hinfo_validate",
    "dicom_hinfo",
]