# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_LOCAL_UNIFIZE_METADATA = Metadata(
    id="3dLocalUnifize",
    name="3dLocalUnifize",
    package="afni",
)

V3dLocalUnifize = typing.TypedDict(
    "V3dLocalUnifize",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dLocalUnifize"]],
        "input": InputPathType,
        "output": str,
        "working_dir": typing.NotRequired[str],
        "echo": typing.NotRequired[bool],
        "no_clean": typing.NotRequired[bool],
        "local_rad": typing.NotRequired[float],
        "local_perc": typing.NotRequired[float],
        "local_mask": typing.NotRequired[str],
        "filter_thr": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class V3dLocalUnifizeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output dataset file"""

def v_3d_local_unifize_params(
    input_: InputPathType,
    output: str,
    working_dir: str | None = None,
    echo: bool = False,
    no_clean: bool = False,
    local_rad: float | None = None,
    local_perc: float | None = None,
    local_mask: str | None = None,
    filter_thr: float | None = None,
) -> V3dLocalUnifize:
    """
    Build parameters.

    Args:
        input_: Input dataset
        output: Output dataset name, including path
        working_dir: Name of temporary working directory (def: __wdir_LocalUni_,\
            plus a random alphanumeric str)
        echo: Run this program very verbosely (default: false)
        no_clean: Do not remove the working directory (default: remove it)
        local_rad: The spherical neighborhood's radius for the 3dLocalStat step\
            (default: -3)
        local_perc: The percentile used in the 3dLocalStat step, generating the\
            scaling volume (default: 50)
        local_mask: Provide the masking option for the 3dLocalStat step; to remove\
            any masking, put 'None' as the option value (default: "-automask")
        filter_thr: Ceiling on values in the final, scaled dataset; values <=0 turn\
            off this final filtering (default: 1.5)

    Returns:
        Parameter dictionary.
    """
    params: V3dLocalUnifize = {
        "@type": "afni/3dLocalUnifize",
        "input": input_,
        "output": output,
        "echo": echo,
        "no_clean": no_clean,
    }
    if working_dir is not None:
        params["working_dir"] = working_dir
    if local_rad is not None:
        params["local_rad"] = local_rad
    if local_perc is not None:
        params["local_perc"] = local_perc
    if local_mask is not None:
        params["local_mask"] = local_mask
    if filter_thr is not None:
        params["filter_thr"] = filter_thr
    return params

def v_3d_local_unifize_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dLocalUnifize."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("working_dir", None) is not None:
        if not isinstance(params["working_dir"], str):
            raise StyxValidationError(f'`working_dir` has the wrong type: Received `{type(params["working_dir"])}` expected `str`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')
    if params.get("local_rad", None) is not None:
        if not isinstance(params["local_rad"], (float, int)):
            raise StyxValidationError(f'`local_rad` has the wrong type: Received `{type(params["local_rad"])}` expected `float`')
    if params.get("local_perc", None) is not None:
        if not isinstance(params["local_perc"], (float, int)):
            raise StyxValidationError(f'`local_perc` has the wrong type: Received `{type(params["local_perc"])}` expected `float`')
    if params.get("local_mask", None) is not None:
        if not isinstance(params["local_mask"], str):
            raise StyxValidationError(f'`local_mask` has the wrong type: Received `{type(params["local_mask"])}` expected `str`')
    if params.get("filter_thr", None) is not None:
        if not isinstance(params["filter_thr"], (float, int)):
            raise StyxValidationError(f'`filter_thr` has the wrong type: Received `{type(params["filter_thr"])}` expected `float`')

def v_3d_local_unifize_cargs(params: V3dLocalUnifize, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dLocalUnifize")
    cargs.append(execution.input_file(params["input"]))
    cargs.append("-prefix")
    cargs.append(params["output"])
    v_0 = params.get("working_dir")
    if v_0 is not None:
        cargs.append("-wdir_name")
        cargs.append(v_0)
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("no_clean"):
        cargs.append("-no_clean")
    v_1 = params.get("local_rad")
    if v_1 is not None:
        cargs.append("-local_rad")
        cargs.append(str(v_1))
    v_2 = params.get("local_perc")
    if v_2 is not None:
        cargs.append("-local_perc")
        cargs.append(str(v_2))
    v_3 = params.get("local_mask")
    if v_3 is not None:
        cargs.append("-local_mask")
        cargs.append(v_3)
    v_4 = params.get("filter_thr")
    if v_4 is not None:
        cargs.append("-filter_thr")
        cargs.append(str(v_4))
    return cargs

def v_3d_local_unifize_outputs(params: V3dLocalUnifize, execution: Execution) -> V3dLocalUnifizeOutputs:
    """Build the V3dLocalUnifizeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output"])
    return V3dLocalUnifizeOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_local_unifize_execute(params: V3dLocalUnifize, runner: Runner | None = None) -> V3dLocalUnifizeOutputs:
    """
    3dLocalUnifize

    This program generates a 'unifized' output volume by estimating the median in the local neighborhood of each voxel and using that to scale each voxel's brightness.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_local_unifize_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_LOCAL_UNIFIZE_METADATA)
    execution.params(params)
    args = v_3d_local_unifize_cargs(params, execution)
    out = v_3d_local_unifize_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_local_unifize(
    input_: InputPathType,
    output: str,
    working_dir: str | None = None,
    echo: bool = False,
    no_clean: bool = False,
    local_rad: float | None = None,
    local_perc: float | None = None,
    local_mask: str | None = None,
    filter_thr: float | None = None,
    runner: Runner | None = None,
) -> V3dLocalUnifizeOutputs:
    """
    3dLocalUnifize

    This program generates a 'unifized' output volume by estimating the median in the local neighborhood of each voxel and using that to scale each voxel's brightness.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_: Input dataset
        output: Output dataset name, including path
        working_dir: Name of temporary working directory (def: __wdir_LocalUni_,\
            plus a random alphanumeric str)
        echo: Run this program very verbosely (default: false)
        no_clean: Do not remove the working directory (default: remove it)
        local_rad: The spherical neighborhood's radius for the 3dLocalStat step\
            (default: -3)
        local_perc: The percentile used in the 3dLocalStat step, generating the\
            scaling volume (default: 50)
        local_mask: Provide the masking option for the 3dLocalStat step; to remove\
            any masking, put 'None' as the option value (default: "-automask")
        filter_thr: Ceiling on values in the final, scaled dataset; values <=0 turn\
            off this final filtering (default: 1.5)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_local_unifize_params(
        input_=input_,
        output=output,
        working_dir=working_dir,
        echo=echo,
        no_clean=no_clean,
        local_rad=local_rad,
        local_perc=local_perc,
        local_mask=local_mask,
        filter_thr=filter_thr,
    )
    return v_3d_local_unifize_execute(params, runner)

__all__ = [
    "V3dLocalUnifize",
    "V3dLocalUnifizeOutputs",
    "V_3D_LOCAL_UNIFIZE_METADATA",
    "v_3d_local_unifize_cargs",
    "v_3d_local_unifize_outputs",
    "v_3d_local_unifize_params",
    "v_3d_local_unifize_execute",
    "v_3d_local_unifize_validate",
    "v_3d_local_unifize",
]