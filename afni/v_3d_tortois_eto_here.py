# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TORTOIS_ETO_HERE_METADATA = Metadata(
    id="3dTORTOISEtoHere",
    name="3dTORTOISEtoHere",
    package="afni",
)

V3dTortoisEtoHere = typing.TypedDict(
    "V3dTortoisEtoHere",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTORTOISEtoHere"]],
        "dt_tort": InputPathType,
        "prefix": str,
        "scale_factor": typing.NotRequired[float],
        "flip_x": typing.NotRequired[bool],
        "flip_y": typing.NotRequired[bool],
        "flip_z": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dTortoisEtoHereOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dt_file: OutputPathType
    """Output AFNI-style DT file with the following ordering of the 6 bricks: Dxx, Dxy, Dyy, Dxz, Dyz, Dzz."""

def v_3d_tortois_eto_here_params(
    dt_tort: InputPathType,
    prefix: str,
    scale_factor: float | None = None,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
) -> V3dTortoisEtoHere:
    """
    Build parameters.

    Args:
        dt_tort: Diffusion tensor file with six bricks of DT components ordered in\
            the TORTOISE manner (Dxx, Dyy, Dzz, Dxy, Dxz, Dyz).
        prefix: Output file name prefix. Will have N+1 bricks when GRADFILE has N\
            rows of gradients.
        scale_factor: Optional switch to rescale the DT elements, dividing by a\
            number X>0.
        flip_x: Change sign of the first element of (inner) eigenvectors.
        flip_y: Change sign of the second element of (inner) eigenvectors.
        flip_z: Change sign of the third element of (inner) eigenvectors.

    Returns:
        Parameter dictionary.
    """
    params: V3dTortoisEtoHere = {
        "@type": "afni/3dTORTOISEtoHere",
        "dt_tort": dt_tort,
        "prefix": prefix,
        "flip_x": flip_x,
        "flip_y": flip_y,
        "flip_z": flip_z,
    }
    if scale_factor is not None:
        params["scale_factor"] = scale_factor
    return params

def v_3d_tortois_eto_here_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTortoisEtoHere."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dt_tort", None) is None:
        raise StyxValidationError("`dt_tort` must not be None")
    if not isinstance(params["dt_tort"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dt_tort` has the wrong type: Received `{type(params["dt_tort"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("scale_factor", None) is not None:
        if not isinstance(params["scale_factor"], (float, int)):
            raise StyxValidationError(f'`scale_factor` has the wrong type: Received `{type(params["scale_factor"])}` expected `float`')
    if params.get("flip_x", None) is not None:
        if not isinstance(params["flip_x"], bool):
            raise StyxValidationError(f'`flip_x` has the wrong type: Received `{type(params["flip_x"])}` expected `bool`')
    if params.get("flip_y", None) is not None:
        if not isinstance(params["flip_y"], bool):
            raise StyxValidationError(f'`flip_y` has the wrong type: Received `{type(params["flip_y"])}` expected `bool`')
    if params.get("flip_z", None) is not None:
        if not isinstance(params["flip_z"], bool):
            raise StyxValidationError(f'`flip_z` has the wrong type: Received `{type(params["flip_z"])}` expected `bool`')

def v_3d_tortois_eto_here_cargs(params: V3dTortoisEtoHere, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTORTOISEtoHere")
    cargs.append("-dt_tort")
    cargs.append(execution.input_file(params["dt_tort"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("scale_factor")
    if v_0 is not None:
        cargs.append("-scale_fac")
        cargs.append(str(v_0))
    if params.get("flip_x"):
        cargs.append("-flip_x")
    if params.get("flip_y"):
        cargs.append("-flip_y")
    if params.get("flip_z"):
        cargs.append("-flip_z")
    return cargs

def v_3d_tortois_eto_here_outputs(params: V3dTortoisEtoHere, execution: Execution) -> V3dTortoisEtoHereOutputs:
    """Build the V3dTortoisEtoHereOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dt_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}.nii.gz')
    return V3dTortoisEtoHereOutputs(
        root=root_v,
        output_dt_file=output_dt_file_v,
    )

def v_3d_tortois_eto_here_execute(params: V3dTortoisEtoHere, runner: Runner | None = None) -> V3dTortoisEtoHereOutputs:
    """
    3dTORTOISEtoHere

    Convert standard TORTOISE DTs (diagonal-first format) to standard AFNI (lower triangular, row-wise) format.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_tortois_eto_here_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TORTOIS_ETO_HERE_METADATA)
    execution.params(params)
    args = v_3d_tortois_eto_here_cargs(params, execution)
    out = v_3d_tortois_eto_here_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_tortois_eto_here(
    dt_tort: InputPathType,
    prefix: str,
    scale_factor: float | None = None,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    runner: Runner | None = None,
) -> V3dTortoisEtoHereOutputs:
    """
    3dTORTOISEtoHere

    Convert standard TORTOISE DTs (diagonal-first format) to standard AFNI (lower triangular, row-wise) format.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dt_tort: Diffusion tensor file with six bricks of DT components ordered in\
            the TORTOISE manner (Dxx, Dyy, Dzz, Dxy, Dxz, Dyz).
        prefix: Output file name prefix. Will have N+1 bricks when GRADFILE has N\
            rows of gradients.
        scale_factor: Optional switch to rescale the DT elements, dividing by a\
            number X>0.
        flip_x: Change sign of the first element of (inner) eigenvectors.
        flip_y: Change sign of the second element of (inner) eigenvectors.
        flip_z: Change sign of the third element of (inner) eigenvectors.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_tortois_eto_here_params(
        dt_tort=dt_tort,
        prefix=prefix,
        scale_factor=scale_factor,
        flip_x=flip_x,
        flip_y=flip_y,
        flip_z=flip_z,
    )
    return v_3d_tortois_eto_here_execute(params, runner)

__all__ = [
    "V3dTortoisEtoHere",
    "V3dTortoisEtoHereOutputs",
    "V_3D_TORTOIS_ETO_HERE_METADATA",
    "v_3d_tortois_eto_here_cargs",
    "v_3d_tortois_eto_here_outputs",
    "v_3d_tortois_eto_here_params",
    "v_3d_tortois_eto_here_execute",
    "v_3d_tortois_eto_here_validate",
    "v_3d_tortois_eto_here",
]