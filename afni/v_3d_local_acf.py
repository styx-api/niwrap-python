# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_LOCAL_ACF_METADATA = Metadata(
    id="3dLocalACF",
    name="3dLocalACF",
    package="afni",
)

V3dLocalAcf = typing.TypedDict(
    "V3dLocalAcf",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dLocalACF"]],
        "prefix": str,
        "input_file": InputPathType,
        "neighborhood": typing.NotRequired[str],
        "mask_file": typing.NotRequired[InputPathType],
        "auto_mask": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dLocalAcfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output dataset with ACF estimates"""

def v_3d_local_acf_params(
    prefix: str,
    input_file: InputPathType,
    neighborhood: str | None = None,
    mask_file: InputPathType | None = None,
    auto_mask: bool = False,
) -> V3dLocalAcf:
    """
    Build parameters.

    Args:
        prefix: Prefix for output dataset
        input_file: Input time series dataset
        neighborhood: Neighborhood specification (e.g., SPHERE(25))
        mask_file: Dataset to mask the analysis
        auto_mask: Automatically generate brain mask from input dataset

    Returns:
        Parameter dictionary.
    """
    params: V3dLocalAcf = {
        "@type": "afni/3dLocalACF",
        "prefix": prefix,
        "input_file": input_file,
        "auto_mask": auto_mask,
    }
    if neighborhood is not None:
        params["neighborhood"] = neighborhood
    if mask_file is not None:
        params["mask_file"] = mask_file
    return params

def v_3d_local_acf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dLocalAcf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("neighborhood", None) is not None:
        if not isinstance(params["neighborhood"], str):
            raise StyxValidationError(f'`neighborhood` has the wrong type: Received `{type(params["neighborhood"])}` expected `str`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("auto_mask", None) is not None:
        if not isinstance(params["auto_mask"], bool):
            raise StyxValidationError(f'`auto_mask` has the wrong type: Received `{type(params["auto_mask"])}` expected `bool`')

def v_3d_local_acf_cargs(params: V3dLocalAcf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dLocalACF")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("neighborhood")
    if v_0 is not None:
        cargs.append("-nbhd")
        cargs.append(v_0)
    v_1 = params.get("mask_file")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    if params.get("auto_mask"):
        cargs.append("-automask")
    return cargs

def v_3d_local_acf_outputs(params: V3dLocalAcf, execution: Execution) -> V3dLocalAcfOutputs:
    """Build the V3dLocalAcfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}.nii.gz')
    return V3dLocalAcfOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_local_acf_execute(params: V3dLocalAcf, runner: Runner | None = None) -> V3dLocalAcfOutputs:
    """
    3dLocalACF

    Estimate the spatial AutoCorrelation Function (ACF) locally in a neighborhood around each voxel

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_local_acf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_LOCAL_ACF_METADATA)
    execution.params(params)
    args = v_3d_local_acf_cargs(params, execution)
    out = v_3d_local_acf_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_local_acf(
    prefix: str,
    input_file: InputPathType,
    neighborhood: str | None = None,
    mask_file: InputPathType | None = None,
    auto_mask: bool = False,
    runner: Runner | None = None,
) -> V3dLocalAcfOutputs:
    """
    3dLocalACF

    Estimate the spatial AutoCorrelation Function (ACF) locally in a neighborhood around each voxel

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix for output dataset
        input_file: Input time series dataset
        neighborhood: Neighborhood specification (e.g., SPHERE(25))
        mask_file: Dataset to mask the analysis
        auto_mask: Automatically generate brain mask from input dataset
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_local_acf_params(
        prefix=prefix,
        input_file=input_file,
        neighborhood=neighborhood,
        mask_file=mask_file,
        auto_mask=auto_mask,
    )
    return v_3d_local_acf_execute(params, runner)

__all__ = [
    "V3dLocalAcf",
    "V3dLocalAcfOutputs",
    "V_3D_LOCAL_ACF_METADATA",
    "v_3d_local_acf_cargs",
    "v_3d_local_acf_outputs",
    "v_3d_local_acf_params",
    "v_3d_local_acf_execute",
    "v_3d_local_acf_validate",
    "v_3d_local_acf",
]