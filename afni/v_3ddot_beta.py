# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DDOT_BETA_METADATA = Metadata(
    id="3ddot_beta",
    name="3ddot_beta",
    package="afni",
)

V3ddotBeta = typing.TypedDict(
    "V3ddotBeta",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3ddot_beta"]],
        "input_file": InputPathType,
        "prefix": str,
        "doeta2": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class V3ddotBetaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output text file containing the correlation-like matrix"""

def v_3ddot_beta_params(
    input_file: InputPathType,
    prefix: str,
    doeta2: bool = False,
    mask: InputPathType | None = None,
) -> V3ddotBeta:
    """
    Build parameters.

    Args:
        input_file: Input file with N bricks
        prefix: Output prefix for the result file
        doeta2: Required flag for performing eta2 tests
        mask: Optional mask file within which to take values

    Returns:
        Parameter dictionary.
    """
    params: V3ddotBeta = {
        "@type": "afni/3ddot_beta",
        "input_file": input_file,
        "prefix": prefix,
        "doeta2": doeta2,
    }
    if mask is not None:
        params["mask"] = mask
    return params

def v_3ddot_beta_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3ddotBeta."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("doeta2", None) is not None:
        if not isinstance(params["doeta2"], bool):
            raise StyxValidationError(f'`doeta2` has the wrong type: Received `{type(params["doeta2"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')

def v_3ddot_beta_cargs(params: V3ddotBeta, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3ddot_beta")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("doeta2"):
        cargs.append("-doeta2")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    return cargs

def v_3ddot_beta_outputs(params: V3ddotBeta, execution: Execution) -> V3ddotBetaOutputs:
    """Build the V3ddotBetaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}_eta2.dat')
    return V3ddotBetaOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3ddot_beta_execute(params: V3ddotBeta, runner: Runner | None = None) -> V3ddotBetaOutputs:
    """
    3ddot_beta

    Beta version of updating 3ddot, currently only performing eta2 tests and outputting a full matrix to a text file.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3ddot_beta_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DDOT_BETA_METADATA)
    execution.params(params)
    args = v_3ddot_beta_cargs(params, execution)
    out = v_3ddot_beta_outputs(params, execution)
    execution.run(args)
    return out

def v_3ddot_beta(
    input_file: InputPathType,
    prefix: str,
    doeta2: bool = False,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3ddotBetaOutputs:
    """
    3ddot_beta

    Beta version of updating 3ddot, currently only performing eta2 tests and outputting a full matrix to a text file.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Input file with N bricks
        prefix: Output prefix for the result file
        doeta2: Required flag for performing eta2 tests
        mask: Optional mask file within which to take values
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3ddot_beta_params(
        input_file=input_file,
        prefix=prefix,
        doeta2=doeta2,
        mask=mask,
    )
    return v_3ddot_beta_execute(params, runner)

__all__ = [
    "V3ddotBeta",
    "V3ddotBetaOutputs",
    "V_3DDOT_BETA_METADATA",
    "v_3ddot_beta_cargs",
    "v_3ddot_beta_outputs",
    "v_3ddot_beta_params",
    "v_3ddot_beta_execute",
    "v_3ddot_beta_validate",
    "v_3ddot_beta",
]