# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNIQ_IMAGES_METADATA = Metadata(
    id="uniq_images",
    name="uniq_images",
    package="afni",
)

UniqImages = typing.TypedDict(
    "UniqImages",
    {
        "@type": typing.NotRequired[typing.Literal["afni/uniq_images"]],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class UniqImagesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    unique_files_list: OutputPathType
    """Generated list of filenames with unique images"""

def uniq_images_params(
    input_files: list[InputPathType],
) -> UniqImages:
    """
    Build parameters.

    Args:
        input_files: List of image filenames to be processed

    Returns:
        Parameter dictionary.
    """
    params: UniqImages = {
        "@type": "afni/uniq_images",
        "input_files": input_files,
    }
    return params

def uniq_images_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid UniqImages."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def uniq_images_cargs(params: UniqImages, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("uniq_images")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def uniq_images_outputs(params: UniqImages, execution: Execution) -> UniqImagesOutputs:
    """Build the UniqImagesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    unique_files_list_v: OutputPathType = execution.output_file("unique_images_list.txt")
    return UniqImagesOutputs(
        root=root_v,
        unique_files_list=unique_files_list_v,
    )

def uniq_images_execute(params: UniqImages, runner: Runner | None = None) -> UniqImagesOutputs:
    """
    uniq_images

    Simple program to read in a list of image filenames, determine which files have unique images inside, and echo out only a list of the filenames with unique images.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    uniq_images_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNIQ_IMAGES_METADATA)
    execution.params(params)
    args = uniq_images_cargs(params, execution)
    out = uniq_images_outputs(params, execution)
    execution.run(args)
    return out

def uniq_images(
    input_files: list[InputPathType],
    runner: Runner | None = None,
) -> UniqImagesOutputs:
    """
    uniq_images

    Simple program to read in a list of image filenames, determine which files have unique images inside, and echo out only a list of the filenames with unique images.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: List of image filenames to be processed
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = uniq_images_params(
        input_files=input_files,
    )
    return uniq_images_execute(params, runner)

__all__ = [
    "UniqImages",
    "UniqImagesOutputs",
    "UNIQ_IMAGES_METADATA",
    "uniq_images_cargs",
    "uniq_images_outputs",
    "uniq_images_params",
    "uniq_images_execute",
    "uniq_images_validate",
    "uniq_images",
]