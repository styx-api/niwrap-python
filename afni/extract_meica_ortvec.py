# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EXTRACT_MEICA_ORTVEC_METADATA = Metadata(
    id="@extract_meica_ortvec",
    name="@extract_meica_ortvec",
    package="afni",
)

ExtractMeicaOrtvec = typing.TypedDict(
    "ExtractMeicaOrtvec",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@extract_meica_ortvec"]],
        "prefix": str,
        "meica_dir": typing.NotRequired[str],
        "reject_ignored": typing.NotRequired[int],
        "reject_midk": typing.NotRequired[int],
        "work_dir": typing.NotRequired[str],
        "verbosity": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class ExtractMeicaOrtvecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output 1D ortvec file"""

def extract_meica_ortvec_params(
    prefix: str,
    meica_dir: str | None = None,
    reject_ignored: int | None = None,
    reject_midk: int | None = None,
    work_dir: str | None = None,
    verbosity: str | None = None,
) -> ExtractMeicaOrtvec:
    """
    Build parameters.

    Args:
        prefix: Name for output 1D ortvec file
        meica_dir: Directory for MEICA files
        reject_ignored: Do we reject ignored components (0=keep, 1=reject), default\
            is 0
        reject_midk: Do we reject midk components (0=keep, 1=reject), default is 1
        work_dir: Sub-directory for work
        verbosity: Set verbosity level

    Returns:
        Parameter dictionary.
    """
    params: ExtractMeicaOrtvec = {
        "@type": "afni/@extract_meica_ortvec",
        "prefix": prefix,
    }
    if meica_dir is not None:
        params["meica_dir"] = meica_dir
    if reject_ignored is not None:
        params["reject_ignored"] = reject_ignored
    if reject_midk is not None:
        params["reject_midk"] = reject_midk
    if work_dir is not None:
        params["work_dir"] = work_dir
    if verbosity is not None:
        params["verbosity"] = verbosity
    return params

def extract_meica_ortvec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ExtractMeicaOrtvec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("meica_dir", None) is not None:
        if not isinstance(params["meica_dir"], str):
            raise StyxValidationError(f'`meica_dir` has the wrong type: Received `{type(params["meica_dir"])}` expected `str`')
    if params.get("reject_ignored", None) is not None:
        if not isinstance(params["reject_ignored"], int):
            raise StyxValidationError(f'`reject_ignored` has the wrong type: Received `{type(params["reject_ignored"])}` expected `int`')
        if not (0 <= params["reject_ignored"] <= 1):
            raise StyxValidationError("Parameter `reject_ignored` must be between 0 and 1 (inclusive)")
    if params.get("reject_midk", None) is not None:
        if not isinstance(params["reject_midk"], int):
            raise StyxValidationError(f'`reject_midk` has the wrong type: Received `{type(params["reject_midk"])}` expected `int`')
        if not (0 <= params["reject_midk"] <= 1):
            raise StyxValidationError("Parameter `reject_midk` must be between 0 and 1 (inclusive)")
    if params.get("work_dir", None) is not None:
        if not isinstance(params["work_dir"], str):
            raise StyxValidationError(f'`work_dir` has the wrong type: Received `{type(params["work_dir"])}` expected `str`')
    if params.get("verbosity", None) is not None:
        if not isinstance(params["verbosity"], str):
            raise StyxValidationError(f'`verbosity` has the wrong type: Received `{type(params["verbosity"])}` expected `str`')

def extract_meica_ortvec_cargs(params: ExtractMeicaOrtvec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@extract_meica_ortvec")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("meica_dir")
    if v_0 is not None:
        cargs.append("-meica_dir")
        cargs.append(v_0)
    v_1 = params.get("reject_ignored")
    if v_1 is not None:
        cargs.append("-reject_ignored")
        cargs.append(str(v_1))
    v_2 = params.get("reject_midk")
    if v_2 is not None:
        cargs.append("-reject_midk")
        cargs.append(str(v_2))
    v_3 = params.get("work_dir")
    if v_3 is not None:
        cargs.append("-work_dir")
        cargs.append(v_3)
    v_4 = params.get("verbosity")
    if v_4 is not None:
        cargs.append("-verb")
        cargs.append(v_4)
    return cargs

def extract_meica_ortvec_outputs(params: ExtractMeicaOrtvec, execution: Execution) -> ExtractMeicaOrtvecOutputs:
    """Build the ExtractMeicaOrtvecOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(f'{params["prefix"]}.1D')
    return ExtractMeicaOrtvecOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def extract_meica_ortvec_execute(params: ExtractMeicaOrtvec, runner: Runner | None = None) -> ExtractMeicaOrtvecOutputs:
    """
    @extract_meica_ortvec

    Project good MEICA components out of bad ones

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    extract_meica_ortvec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EXTRACT_MEICA_ORTVEC_METADATA)
    execution.params(params)
    args = extract_meica_ortvec_cargs(params, execution)
    out = extract_meica_ortvec_outputs(params, execution)
    execution.run(args)
    return out

def extract_meica_ortvec(
    prefix: str,
    meica_dir: str | None = None,
    reject_ignored: int | None = None,
    reject_midk: int | None = None,
    work_dir: str | None = None,
    verbosity: str | None = None,
    runner: Runner | None = None,
) -> ExtractMeicaOrtvecOutputs:
    """
    @extract_meica_ortvec

    Project good MEICA components out of bad ones

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Name for output 1D ortvec file
        meica_dir: Directory for MEICA files
        reject_ignored: Do we reject ignored components (0=keep, 1=reject), default\
            is 0
        reject_midk: Do we reject midk components (0=keep, 1=reject), default is 1
        work_dir: Sub-directory for work
        verbosity: Set verbosity level
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = extract_meica_ortvec_params(
        prefix=prefix,
        meica_dir=meica_dir,
        reject_ignored=reject_ignored,
        reject_midk=reject_midk,
        work_dir=work_dir,
        verbosity=verbosity,
    )
    return extract_meica_ortvec_execute(params, runner)

__all__ = [
    "ExtractMeicaOrtvec",
    "ExtractMeicaOrtvecOutputs",
    "EXTRACT_MEICA_ORTVEC_METADATA",
    "extract_meica_ortvec_cargs",
    "extract_meica_ortvec_outputs",
    "extract_meica_ortvec_params",
    "extract_meica_ortvec_execute",
    "extract_meica_ortvec_validate",
    "extract_meica_ortvec",
]