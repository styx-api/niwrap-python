# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GRAYPLOT_METADATA = Metadata(
    id="@grayplot",
    name="@grayplot",
    package="afni",
)

Grayplot = typing.TypedDict(
    "Grayplot",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@grayplot"]],
        "dirname": str,
        "pvorder": typing.NotRequired[bool],
        "peelorder": typing.NotRequired[bool],
        "ijkorder": typing.NotRequired[bool],
        "allorder": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class GrayplotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    grayplot_img: OutputPathType
    """Output grayplot image"""

def grayplot_params(
    dirname: str,
    pvorder: bool = False,
    peelorder: bool = False,
    ijkorder: bool = False,
    allorder: bool = False,
) -> Grayplot:
    """
    Build parameters.

    Args:
        dirname: Directory containing afni_proc.py results.
        pvorder: Within each partition, voxels are ordered by a simple similarity\
            measure.
        peelorder: Within each partition, voxels are ordered by how many 'peel'\
            operations are needed to reach a given voxel.
        ijkorder: Within each partition, voxels are ordered by the 3D index in\
            which they appear in the dataset.
        allorder: Create grayplots for all ordering methods.

    Returns:
        Parameter dictionary.
    """
    params: Grayplot = {
        "@type": "afni/@grayplot",
        "dirname": dirname,
        "pvorder": pvorder,
        "peelorder": peelorder,
        "ijkorder": ijkorder,
        "allorder": allorder,
    }
    return params

def grayplot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Grayplot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dirname", None) is None:
        raise StyxValidationError("`dirname` must not be None")
    if not isinstance(params["dirname"], str):
        raise StyxValidationError(f'`dirname` has the wrong type: Received `{type(params["dirname"])}` expected `str`')
    if params.get("pvorder", None) is not None:
        if not isinstance(params["pvorder"], bool):
            raise StyxValidationError(f'`pvorder` has the wrong type: Received `{type(params["pvorder"])}` expected `bool`')
    if params.get("peelorder", None) is not None:
        if not isinstance(params["peelorder"], bool):
            raise StyxValidationError(f'`peelorder` has the wrong type: Received `{type(params["peelorder"])}` expected `bool`')
    if params.get("ijkorder", None) is not None:
        if not isinstance(params["ijkorder"], bool):
            raise StyxValidationError(f'`ijkorder` has the wrong type: Received `{type(params["ijkorder"])}` expected `bool`')
    if params.get("allorder", None) is not None:
        if not isinstance(params["allorder"], bool):
            raise StyxValidationError(f'`allorder` has the wrong type: Received `{type(params["allorder"])}` expected `bool`')

def grayplot_cargs(params: Grayplot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@grayplot")
    cargs.append(params["dirname"])
    if params.get("pvorder"):
        cargs.append("-pvorder")
    if params.get("peelorder"):
        cargs.append("-peelorder")
    if params.get("ijkorder"):
        cargs.append("-ijkorder")
    if params.get("allorder"):
        cargs.append("-ALLorder")
    return cargs

def grayplot_outputs(params: Grayplot, execution: Execution) -> GrayplotOutputs:
    """Build the GrayplotOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    grayplot_img_v: OutputPathType = execution.output_file("Grayplot.errts.png")
    return GrayplotOutputs(
        root=root_v,
        grayplot_img=grayplot_img_v,
    )

def grayplot_execute(params: Grayplot, runner: Runner | None = None) -> GrayplotOutputs:
    """
    @grayplot

    Script to read files from an afni_proc.py results directory and produce a grayplot from the errts dataset(s), combined with a motion magnitude indicator graph.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    grayplot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GRAYPLOT_METADATA)
    execution.params(params)
    args = grayplot_cargs(params, execution)
    out = grayplot_outputs(params, execution)
    execution.run(args)
    return out

def grayplot(
    dirname: str,
    pvorder: bool = False,
    peelorder: bool = False,
    ijkorder: bool = False,
    allorder: bool = False,
    runner: Runner | None = None,
) -> GrayplotOutputs:
    """
    @grayplot

    Script to read files from an afni_proc.py results directory and produce a grayplot from the errts dataset(s), combined with a motion magnitude indicator graph.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dirname: Directory containing afni_proc.py results.
        pvorder: Within each partition, voxels are ordered by a simple similarity\
            measure.
        peelorder: Within each partition, voxels are ordered by how many 'peel'\
            operations are needed to reach a given voxel.
        ijkorder: Within each partition, voxels are ordered by the 3D index in\
            which they appear in the dataset.
        allorder: Create grayplots for all ordering methods.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = grayplot_params(
        dirname=dirname,
        pvorder=pvorder,
        peelorder=peelorder,
        ijkorder=ijkorder,
        allorder=allorder,
    )
    return grayplot_execute(params, runner)

__all__ = [
    "Grayplot",
    "GrayplotOutputs",
    "GRAYPLOT_METADATA",
    "grayplot_cargs",
    "grayplot_outputs",
    "grayplot_params",
    "grayplot_execute",
    "grayplot_validate",
    "grayplot",
]