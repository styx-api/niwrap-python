# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMSTAT_METADATA = Metadata(
    id="imstat",
    name="imstat",
    package="afni",
)

Imstat = typing.TypedDict(
    "Imstat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/imstat"]],
        "no_label": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "pixstat_prefix": typing.NotRequired[str],
        "image_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class ImstatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mean_output: OutputPathType | None
    """Mean of pixel-wise statistics for the collection of 2D images"""
    sdev_output: OutputPathType | None
    """Standard deviation of pixel-wise statistics for the collection of 2D images"""

def imstat_params(
    image_files: list[InputPathType],
    no_label: bool = False,
    quiet: bool = False,
    pixstat_prefix: str | None = None,
) -> Imstat:
    """
    Build parameters.

    Args:
        image_files: Input image file(s)
        no_label: Don't write labels on each file's summary line
        quiet: Don't print statistics for each file
        pixstat_prefix: If more than one image file is given, then 'prefix.mean'\
            and 'prefix.sdev' will be written as the pixel-wise statistics images\
            of the whole collection. These images will be in the 'flim' floating\
            point format. [This option only works on 2D images!]

    Returns:
        Parameter dictionary.
    """
    params: Imstat = {
        "@type": "afni/imstat",
        "image_files": image_files,
        "no_label": no_label,
        "quiet": quiet,
    }
    if pixstat_prefix is not None:
        params["pixstat_prefix"] = pixstat_prefix
    return params

def imstat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imstat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("no_label", None) is not None:
        if not isinstance(params["no_label"], bool):
            raise StyxValidationError(f'`no_label` has the wrong type: Received `{type(params["no_label"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("pixstat_prefix", None) is not None:
        if not isinstance(params["pixstat_prefix"], str):
            raise StyxValidationError(f'`pixstat_prefix` has the wrong type: Received `{type(params["pixstat_prefix"])}` expected `str`')
    if params.get("image_files", None) is None:
        raise StyxValidationError("`image_files` must not be None")
    if not isinstance(params["image_files"], list):
        raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(params["image_files"])}` expected `list[InputPathType]`')
    for e in params["image_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`image_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def imstat_cargs(params: Imstat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imstat")
    if params.get("no_label"):
        cargs.append("-nolabel")
    if params.get("quiet"):
        cargs.append("-quiet")
    v_0 = params.get("pixstat_prefix")
    if v_0 is not None:
        cargs.append("-pixstat")
        cargs.append(v_0)
    for item0 in params["image_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def imstat_outputs(params: Imstat, execution: Execution) -> ImstatOutputs:
    """Build the ImstatOutputs object for this tool."""
    mean_output_v: OutputPathType | None = None
    sdev_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("pixstat_prefix")
    if __v0 is not None:
        mean_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.mean')
    __v1 = params.get("pixstat_prefix")
    if __v1 is not None:
        sdev_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.sdev')
    return ImstatOutputs(
        root=root_v,
        mean_output=mean_output_v,
        sdev_output=sdev_output_v,
    )

def imstat_execute(params: Imstat, runner: Runner | None = None) -> ImstatOutputs:
    """
    imstat

    Calculation of statistics of one or more images

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imstat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMSTAT_METADATA)
    execution.params(params)
    args = imstat_cargs(params, execution)
    out = imstat_outputs(params, execution)
    execution.run(args)
    return out

def imstat(
    image_files: list[InputPathType],
    no_label: bool = False,
    quiet: bool = False,
    pixstat_prefix: str | None = None,
    runner: Runner | None = None,
) -> ImstatOutputs:
    """
    imstat

    Calculation of statistics of one or more images

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        image_files: Input image file(s)
        no_label: Don't write labels on each file's summary line
        quiet: Don't print statistics for each file
        pixstat_prefix: If more than one image file is given, then 'prefix.mean'\
            and 'prefix.sdev' will be written as the pixel-wise statistics images\
            of the whole collection. These images will be in the 'flim' floating\
            point format. [This option only works on 2D images!]
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imstat_params(
        image_files=image_files,
        no_label=no_label,
        quiet=quiet,
        pixstat_prefix=pixstat_prefix,
    )
    return imstat_execute(params, runner)

__all__ = [
    "Imstat",
    "ImstatOutputs",
    "IMSTAT_METADATA",
    "imstat_cargs",
    "imstat_outputs",
    "imstat_params",
    "imstat_execute",
    "imstat_validate",
    "imstat",
]