# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CJPEG_METADATA = Metadata(
    id="cjpeg",
    name="cjpeg",
    package="afni",
)

Cjpeg = typing.TypedDict(
    "Cjpeg",
    {
        "@type": typing.NotRequired[typing.Literal["afni/cjpeg"]],
        "quality": typing.NotRequired[float],
        "grayscale": typing.NotRequired[bool],
        "optimize": typing.NotRequired[bool],
        "baseline": typing.NotRequired[bool],
        "progressive": typing.NotRequired[bool],
        "outfile": str,
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class CjpegOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """The output JPEG file"""

def cjpeg_params(
    outfile: str,
    infile: InputPathType,
    quality: float | None = None,
    grayscale: bool = False,
    optimize: bool = False,
    baseline: bool = False,
    progressive: bool = False,
) -> Cjpeg:
    """
    Build parameters.

    Args:
        outfile: Output JPEG file
        infile: Input image file
        quality: Quality of JPEG image (0-100)
        grayscale: Create a grayscale JPEG file
        optimize: Optimize Huffman table
        baseline: Create a baseline JPEG file
        progressive: Create a progressive JPEG file

    Returns:
        Parameter dictionary.
    """
    params: Cjpeg = {
        "@type": "afni/cjpeg",
        "outfile": outfile,
        "infile": infile,
        "grayscale": grayscale,
        "optimize": optimize,
        "baseline": baseline,
        "progressive": progressive,
    }
    if quality is not None:
        params["quality"] = quality
    return params

def cjpeg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Cjpeg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("quality", None) is not None:
        if not isinstance(params["quality"], (float, int)):
            raise StyxValidationError(f'`quality` has the wrong type: Received `{type(params["quality"])}` expected `float`')
        if not (0 <= params["quality"] <= 100):
            raise StyxValidationError("Parameter `quality` must be between 0 and 100 (inclusive)")
    if params.get("grayscale", None) is not None:
        if not isinstance(params["grayscale"], bool):
            raise StyxValidationError(f'`grayscale` has the wrong type: Received `{type(params["grayscale"])}` expected `bool`')
    if params.get("optimize", None) is not None:
        if not isinstance(params["optimize"], bool):
            raise StyxValidationError(f'`optimize` has the wrong type: Received `{type(params["optimize"])}` expected `bool`')
    if params.get("baseline", None) is not None:
        if not isinstance(params["baseline"], bool):
            raise StyxValidationError(f'`baseline` has the wrong type: Received `{type(params["baseline"])}` expected `bool`')
    if params.get("progressive", None) is not None:
        if not isinstance(params["progressive"], bool):
            raise StyxValidationError(f'`progressive` has the wrong type: Received `{type(params["progressive"])}` expected `bool`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def cjpeg_cargs(params: Cjpeg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cjpeg")
    v_0 = params.get("quality")
    if v_0 is not None:
        cargs.append("-quality")
        cargs.append(str(v_0))
    if params.get("grayscale"):
        cargs.append("-grayscale")
    if params.get("optimize"):
        cargs.append("-optimize")
    if params.get("baseline"):
        cargs.append("-baseline")
    if params.get("progressive"):
        cargs.append("-progressive")
    cargs.append(params["outfile"])
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def cjpeg_outputs(params: Cjpeg, execution: Execution) -> CjpegOutputs:
    """Build the CjpegOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["outfile"])
    return CjpegOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def cjpeg_execute(params: Cjpeg, runner: Runner | None = None) -> CjpegOutputs:
    """
    cjpeg

    Compresses an image file to a JPEG file

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cjpeg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CJPEG_METADATA)
    execution.params(params)
    args = cjpeg_cargs(params, execution)
    out = cjpeg_outputs(params, execution)
    execution.run(args)
    return out

def cjpeg(
    outfile: str,
    infile: InputPathType,
    quality: float | None = None,
    grayscale: bool = False,
    optimize: bool = False,
    baseline: bool = False,
    progressive: bool = False,
    runner: Runner | None = None,
) -> CjpegOutputs:
    """
    cjpeg

    Compresses an image file to a JPEG file

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        outfile: Output JPEG file
        infile: Input image file
        quality: Quality of JPEG image (0-100)
        grayscale: Create a grayscale JPEG file
        optimize: Optimize Huffman table
        baseline: Create a baseline JPEG file
        progressive: Create a progressive JPEG file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cjpeg_params(
        outfile=outfile,
        infile=infile,
        quality=quality,
        grayscale=grayscale,
        optimize=optimize,
        baseline=baseline,
        progressive=progressive,
    )
    return cjpeg_execute(params, runner)

__all__ = [
    "Cjpeg",
    "CjpegOutputs",
    "CJPEG_METADATA",
    "cjpeg_cargs",
    "cjpeg_outputs",
    "cjpeg_params",
    "cjpeg_execute",
    "cjpeg_validate",
    "cjpeg",
]