# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANATICOR_METADATA = Metadata(
    id="@ANATICOR",
    name="@ANATICOR",
    package="afni",
)

Anaticor = typing.TypedDict(
    "Anaticor",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ANATICOR"]],
        "ts": InputPathType,
        "polort": str,
        "motion": InputPathType,
        "aseg": InputPathType,
        "prefix": str,
        "radius": typing.NotRequired[float],
        "view": typing.NotRequired[str],
        "nuisance": typing.NotRequired[InputPathType],
        "no_ventricles": typing.NotRequired[bool],
        "Rsq_WMe": typing.NotRequired[bool],
        "coverage": typing.NotRequired[bool],
        "verb": typing.NotRequired[bool],
        "dirty": typing.NotRequired[bool],
        "echo": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AnaticorOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_files: OutputPathType
    """Output files with the prefix specified by the -prefix option."""

def anaticor_params(
    ts: InputPathType,
    polort: str,
    motion: InputPathType,
    aseg: InputPathType,
    prefix: str,
    radius: float | None = None,
    view: str | None = None,
    nuisance: InputPathType | None = None,
    no_ventricles: bool = False,
    Rsq_WMe: bool = False,
    coverage: bool = False,
    verb: bool = False,
    dirty: bool = False,
    echo: bool = False,
) -> Anaticor:
    """
    Build parameters.

    Args:
        ts: Time series volume which should have already undergone preprocessing\
            steps such as despiking, RetroIcor, RVT correction, time shifting, and\
            volume registration.
        polort: Polynomial for linear trend removal. Use the same order as for\
            afni_proc.py
        motion: Head motion parameters from 3dvolreg, also created by afni_proc.py
        aseg: Aseg file from FreeSurfer's segmentation. This aseg volume must be in\
            register with the EPI time series.
        prefix: Use output (residual time series) for a prefix
        radius: The radius of a local sphere mask in mm. Default is 15 mm for high\
            resolution 1.7x1.7x3mm data.
        view: Set the view of the output data. Default is +orig. Choose from +orig,\
            +acpc, or +tlrc.
        nuisance: Other nuisance regressors. Each regressor is a column in .1D file
        no_ventricles: Do not include LVe regressor
        Rsq_WMe: Produce an explained variance map for WMeLOCAL regressor.
        coverage: Produce a spatial coverage map of WMeLOCAL regressor
        verb: Verbose flag
        dirty: Keep temporary files
        echo: Echo each script command for debugging

    Returns:
        Parameter dictionary.
    """
    params: Anaticor = {
        "@type": "afni/@ANATICOR",
        "ts": ts,
        "polort": polort,
        "motion": motion,
        "aseg": aseg,
        "prefix": prefix,
        "no_ventricles": no_ventricles,
        "Rsq_WMe": Rsq_WMe,
        "coverage": coverage,
        "verb": verb,
        "dirty": dirty,
        "echo": echo,
    }
    if radius is not None:
        params["radius"] = radius
    if view is not None:
        params["view"] = view
    if nuisance is not None:
        params["nuisance"] = nuisance
    return params

def anaticor_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Anaticor."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ts", None) is None:
        raise StyxValidationError("`ts` must not be None")
    if not isinstance(params["ts"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ts` has the wrong type: Received `{type(params["ts"])}` expected `InputPathType`')
    if params.get("polort", None) is None:
        raise StyxValidationError("`polort` must not be None")
    if not isinstance(params["polort"], str):
        raise StyxValidationError(f'`polort` has the wrong type: Received `{type(params["polort"])}` expected `str`')
    if params.get("motion", None) is None:
        raise StyxValidationError("`motion` must not be None")
    if not isinstance(params["motion"], (pathlib.Path, str)):
        raise StyxValidationError(f'`motion` has the wrong type: Received `{type(params["motion"])}` expected `InputPathType`')
    if params.get("aseg", None) is None:
        raise StyxValidationError("`aseg` must not be None")
    if not isinstance(params["aseg"], (pathlib.Path, str)):
        raise StyxValidationError(f'`aseg` has the wrong type: Received `{type(params["aseg"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("radius", None) is not None:
        if not isinstance(params["radius"], (float, int)):
            raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')
    if params.get("view", None) is not None:
        if not isinstance(params["view"], str):
            raise StyxValidationError(f'`view` has the wrong type: Received `{type(params["view"])}` expected `str`')
    if params.get("nuisance", None) is not None:
        if not isinstance(params["nuisance"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nuisance` has the wrong type: Received `{type(params["nuisance"])}` expected `InputPathType`')
    if params.get("no_ventricles", None) is not None:
        if not isinstance(params["no_ventricles"], bool):
            raise StyxValidationError(f'`no_ventricles` has the wrong type: Received `{type(params["no_ventricles"])}` expected `bool`')
    if params.get("Rsq_WMe", None) is not None:
        if not isinstance(params["Rsq_WMe"], bool):
            raise StyxValidationError(f'`Rsq_WMe` has the wrong type: Received `{type(params["Rsq_WMe"])}` expected `bool`')
    if params.get("coverage", None) is not None:
        if not isinstance(params["coverage"], bool):
            raise StyxValidationError(f'`coverage` has the wrong type: Received `{type(params["coverage"])}` expected `bool`')
    if params.get("verb", None) is not None:
        if not isinstance(params["verb"], bool):
            raise StyxValidationError(f'`verb` has the wrong type: Received `{type(params["verb"])}` expected `bool`')
    if params.get("dirty", None) is not None:
        if not isinstance(params["dirty"], bool):
            raise StyxValidationError(f'`dirty` has the wrong type: Received `{type(params["dirty"])}` expected `bool`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')

def anaticor_cargs(params: Anaticor, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ANATICOR")
    cargs.append("-ts")
    cargs.append(execution.input_file(params["ts"]))
    cargs.append("-polort")
    cargs.append(params["polort"])
    cargs.append("-motion")
    cargs.append(execution.input_file(params["motion"]))
    cargs.append(execution.input_file(params["aseg"]))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    v_0 = params.get("radius")
    if v_0 is not None:
        cargs.append("-radius")
        cargs.append(str(v_0))
    v_1 = params.get("view")
    if v_1 is not None:
        cargs.append(v_1)
    v_2 = params.get("nuisance")
    if v_2 is not None:
        cargs.append("-nuisance")
        cargs.append(execution.input_file(v_2))
    if params.get("no_ventricles"):
        cargs.append("-no_ventricles")
    if params.get("Rsq_WMe"):
        cargs.append("-Rsq_WMe")
    if params.get("coverage"):
        cargs.append("-coverage")
    if params.get("verb"):
        cargs.append("-verb")
    if params.get("dirty"):
        cargs.append("-dirty")
    if params.get("echo"):
        cargs.append("-echo")
    return cargs

def anaticor_outputs(params: Anaticor, execution: Execution) -> AnaticorOutputs:
    """Build the AnaticorOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_files_v: OutputPathType = execution.output_file(params["prefix"])
    return AnaticorOutputs(
        root=root_v,
        output_files=output_files_v,
    )

def anaticor_execute(params: Anaticor, runner: Runner | None = None) -> AnaticorOutputs:
    """
    @ANATICOR

    Script to produce a residual time series cleaned by ANATICOR model

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    anaticor_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANATICOR_METADATA)
    execution.params(params)
    args = anaticor_cargs(params, execution)
    out = anaticor_outputs(params, execution)
    execution.run(args)
    return out

def anaticor(
    ts: InputPathType,
    polort: str,
    motion: InputPathType,
    aseg: InputPathType,
    prefix: str,
    radius: float | None = None,
    view: str | None = None,
    nuisance: InputPathType | None = None,
    no_ventricles: bool = False,
    Rsq_WMe: bool = False,
    coverage: bool = False,
    verb: bool = False,
    dirty: bool = False,
    echo: bool = False,
    runner: Runner | None = None,
) -> AnaticorOutputs:
    """
    @ANATICOR

    Script to produce a residual time series cleaned by ANATICOR model

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        ts: Time series volume which should have already undergone preprocessing\
            steps such as despiking, RetroIcor, RVT correction, time shifting, and\
            volume registration.
        polort: Polynomial for linear trend removal. Use the same order as for\
            afni_proc.py
        motion: Head motion parameters from 3dvolreg, also created by afni_proc.py
        aseg: Aseg file from FreeSurfer's segmentation. This aseg volume must be in\
            register with the EPI time series.
        prefix: Use output (residual time series) for a prefix
        radius: The radius of a local sphere mask in mm. Default is 15 mm for high\
            resolution 1.7x1.7x3mm data.
        view: Set the view of the output data. Default is +orig. Choose from +orig,\
            +acpc, or +tlrc.
        nuisance: Other nuisance regressors. Each regressor is a column in .1D file
        no_ventricles: Do not include LVe regressor
        Rsq_WMe: Produce an explained variance map for WMeLOCAL regressor.
        coverage: Produce a spatial coverage map of WMeLOCAL regressor
        verb: Verbose flag
        dirty: Keep temporary files
        echo: Echo each script command for debugging
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = anaticor_params(
        ts=ts,
        polort=polort,
        motion=motion,
        aseg=aseg,
        prefix=prefix,
        radius=radius,
        view=view,
        nuisance=nuisance,
        no_ventricles=no_ventricles,
        Rsq_WMe=Rsq_WMe,
        coverage=coverage,
        verb=verb,
        dirty=dirty,
        echo=echo,
    )
    return anaticor_execute(params, runner)

__all__ = [
    "Anaticor",
    "AnaticorOutputs",
    "ANATICOR_METADATA",
    "anaticor_cargs",
    "anaticor_outputs",
    "anaticor_params",
    "anaticor_execute",
    "anaticor_validate",
    "anaticor",
]