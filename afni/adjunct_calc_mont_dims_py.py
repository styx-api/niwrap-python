# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ADJUNCT_CALC_MONT_DIMS_PY_METADATA = Metadata(
    id="adjunct_calc_mont_dims.py",
    name="adjunct_calc_mont_dims.py",
    package="afni",
)

AdjunctCalcMontDimsPy = typing.TypedDict(
    "AdjunctCalcMontDimsPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/adjunct_calc_mont_dims.py"]],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AdjunctCalcMontDimsPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def adjunct_calc_mont_dims_py_params(
    help: bool = False,
) -> AdjunctCalcMontDimsPy:
    """
    Build parameters.

    Args:
        help: Display help information

    Returns:
        Parameter dictionary.
    """
    params: AdjunctCalcMontDimsPy = {
        "@type": "afni/adjunct_calc_mont_dims.py",
        "help": help,
    }
    return params

def adjunct_calc_mont_dims_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AdjunctCalcMontDimsPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def adjunct_calc_mont_dims_py_cargs(params: AdjunctCalcMontDimsPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("adjunct_calc_mont_dims.py")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def adjunct_calc_mont_dims_py_outputs(params: AdjunctCalcMontDimsPy, execution: Execution) -> AdjunctCalcMontDimsPyOutputs:
    """Build the AdjunctCalcMontDimsPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AdjunctCalcMontDimsPyOutputs(
        root=root_v,
    )

def adjunct_calc_mont_dims_py_execute(params: AdjunctCalcMontDimsPy, runner: Runner | None = None) -> AdjunctCalcMontDimsPyOutputs:
    """
    adjunct_calc_mont_dims.py

    A helper function for the fat_proc* scripts

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    adjunct_calc_mont_dims_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ADJUNCT_CALC_MONT_DIMS_PY_METADATA)
    execution.params(params)
    args = adjunct_calc_mont_dims_py_cargs(params, execution)
    out = adjunct_calc_mont_dims_py_outputs(params, execution)
    execution.run(args)
    return out

def adjunct_calc_mont_dims_py(
    help: bool = False,
    runner: Runner | None = None,
) -> AdjunctCalcMontDimsPyOutputs:
    """
    adjunct_calc_mont_dims.py

    A helper function for the fat_proc* scripts

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        help: Display help information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = adjunct_calc_mont_dims_py_params(
        help=help,
    )
    return adjunct_calc_mont_dims_py_execute(params, runner)

__all__ = [
    "AdjunctCalcMontDimsPy",
    "AdjunctCalcMontDimsPyOutputs",
    "ADJUNCT_CALC_MONT_DIMS_PY_METADATA",
    "adjunct_calc_mont_dims_py_cargs",
    "adjunct_calc_mont_dims_py_outputs",
    "adjunct_calc_mont_dims_py_params",
    "adjunct_calc_mont_dims_py_execute",
    "adjunct_calc_mont_dims_py_validate",
    "adjunct_calc_mont_dims_py",
]