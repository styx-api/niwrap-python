# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CLUST_EXP_HIST_TABLE_PY_METADATA = Metadata(
    id="ClustExp_HistTable.py",
    name="ClustExp_HistTable.py",
    package="afni",
)

ClustExpHistTablePy = typing.TypedDict(
    "ClustExpHistTablePy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/ClustExp_HistTable.py"]],
        "stat_dset": InputPathType,
        "prefix": typing.NotRequired[str],
        "session": typing.NotRequired[str],
        "overwrite": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ClustExpHistTablePyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    group_table: OutputPathType | None
    """Table with information parsed from the statistics dataset history."""

def clust_exp_hist_table_py_params(
    stat_dset: InputPathType,
    prefix: str | None = None,
    session: str | None = None,
    overwrite: bool = False,
) -> ClustExpHistTablePy:
    """
    Build parameters.

    Args:
        stat_dset: Statistics dataset.
        prefix: Name for output (no path). Default is GroupOut.
        session: Output parent folder if you don't want the current working\
            directory. Default is ./
        overwrite: Remove previous folder with same PREFIX

    Returns:
        Parameter dictionary.
    """
    params: ClustExpHistTablePy = {
        "@type": "afni/ClustExp_HistTable.py",
        "stat_dset": stat_dset,
        "overwrite": overwrite,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if session is not None:
        params["session"] = session
    return params

def clust_exp_hist_table_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ClustExpHistTablePy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stat_dset", None) is None:
        raise StyxValidationError("`stat_dset` must not be None")
    if not isinstance(params["stat_dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`stat_dset` has the wrong type: Received `{type(params["stat_dset"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("session", None) is not None:
        if not isinstance(params["session"], str):
            raise StyxValidationError(f'`session` has the wrong type: Received `{type(params["session"])}` expected `str`')
    if params.get("overwrite", None) is not None:
        if not isinstance(params["overwrite"], bool):
            raise StyxValidationError(f'`overwrite` has the wrong type: Received `{type(params["overwrite"])}` expected `bool`')

def clust_exp_hist_table_py_cargs(params: ClustExpHistTablePy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ClustExp_HistTable.py")
    cargs.append("-StatDSET")
    cargs.append(execution.input_file(params["stat_dset"]))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("session")
    if v_1 is not None:
        cargs.append("-session")
        cargs.append(v_1)
    if params.get("overwrite"):
        cargs.append("-overwrite")
    return cargs

def clust_exp_hist_table_py_outputs(params: ClustExpHistTablePy, execution: Execution) -> ClustExpHistTablePyOutputs:
    """Build the ClustExpHistTablePyOutputs object for this tool."""
    group_table_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        group_table_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_GroupTable.csv')
    return ClustExpHistTablePyOutputs(
        root=root_v,
        group_table=group_table_v,
    )

def clust_exp_hist_table_py_execute(params: ClustExpHistTablePy, runner: Runner | None = None) -> ClustExpHistTablePyOutputs:
    """
    ClustExp_HistTable.py

    Script to extract the data table from history of datasets from 3dttest++, 3dMVM, or 3dLME.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    clust_exp_hist_table_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CLUST_EXP_HIST_TABLE_PY_METADATA)
    execution.params(params)
    args = clust_exp_hist_table_py_cargs(params, execution)
    out = clust_exp_hist_table_py_outputs(params, execution)
    execution.run(args)
    return out

def clust_exp_hist_table_py(
    stat_dset: InputPathType,
    prefix: str | None = None,
    session: str | None = None,
    overwrite: bool = False,
    runner: Runner | None = None,
) -> ClustExpHistTablePyOutputs:
    """
    ClustExp_HistTable.py

    Script to extract the data table from history of datasets from 3dttest++, 3dMVM, or 3dLME.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        stat_dset: Statistics dataset.
        prefix: Name for output (no path). Default is GroupOut.
        session: Output parent folder if you don't want the current working\
            directory. Default is ./
        overwrite: Remove previous folder with same PREFIX
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = clust_exp_hist_table_py_params(
        stat_dset=stat_dset,
        prefix=prefix,
        session=session,
        overwrite=overwrite,
    )
    return clust_exp_hist_table_py_execute(params, runner)

__all__ = [
    "ClustExpHistTablePy",
    "ClustExpHistTablePyOutputs",
    "CLUST_EXP_HIST_TABLE_PY_METADATA",
    "clust_exp_hist_table_py_cargs",
    "clust_exp_hist_table_py_outputs",
    "clust_exp_hist_table_py_params",
    "clust_exp_hist_table_py_execute",
    "clust_exp_hist_table_py_validate",
    "clust_exp_hist_table_py",
]