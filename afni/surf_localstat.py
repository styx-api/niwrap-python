# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURF_LOCALSTAT_METADATA = Metadata(
    id="SurfLocalstat",
    name="SurfLocalstat",
    package="afni",
)

SurfLocalstatStat = typing.Literal["mean"] | typing.Literal["mode"] | typing.Literal["num"] | typing.Literal["FWHM"] | typing.Literal["ALL"]

SurfLocalstat = typing.TypedDict(
    "SurfLocalstat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/SurfLocalstat"]],
        "hood": typing.NotRequired[float],
        "nbhd_rad": typing.NotRequired[float],
        "prefix": str,
        "stat": SurfLocalstatStat,
        "input_dataset": InputPathType,
        "surface": InputPathType,
    },
)

@dataclasses.dataclass
class SurfLocalstatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Computed local statistics dataset"""

def surf_localstat_params(
    prefix: str,
    stat: SurfLocalstatStat,
    input_dataset: InputPathType,
    surface: InputPathType,
    hood: float | None = None,
    nbhd_rad: float | None = None,
) -> SurfLocalstat:
    """
    Build parameters.

    Args:
        prefix: Prefix of output data set.
        stat: Compute the specified statistic on the values extracted from the\
            region around each voxel. Options: mean, mode, num, FWHM, ALL.
        input_dataset: Input dataset.
        surface: Input GIFTI surface file.
        hood: Neighborhood of nodes within the specified radius R.
        nbhd_rad: Distance from node n as measured by the shortest distance along\
            the mesh.

    Returns:
        Parameter dictionary.
    """
    params: SurfLocalstat = {
        "@type": "afni/SurfLocalstat",
        "prefix": prefix,
        "stat": stat,
        "input_dataset": input_dataset,
        "surface": surface,
    }
    if hood is not None:
        params["hood"] = hood
    if nbhd_rad is not None:
        params["nbhd_rad"] = nbhd_rad
    return params

def surf_localstat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfLocalstat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("hood", None) is not None:
        if not isinstance(params["hood"], (float, int)):
            raise StyxValidationError(f'`hood` has the wrong type: Received `{type(params["hood"])}` expected `float`')
    if params.get("nbhd_rad", None) is not None:
        if not isinstance(params["nbhd_rad"], (float, int)):
            raise StyxValidationError(f'`nbhd_rad` has the wrong type: Received `{type(params["nbhd_rad"])}` expected `float`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("stat", None) is None:
        raise StyxValidationError("`stat` must not be None")
    if not isinstance(params["stat"], str):
        raise StyxValidationError(f'`stat` has the wrong type: Received `{type(params["stat"])}` expected `SurfLocalstatStat`')
    if params["stat"] not in ["mean", "mode", "num", "FWHM", "ALL"]:
        raise StyxValidationError("Parameter `stat` must be one of [\"mean\", \"mode\", \"num\", \"FWHM\", \"ALL\"]")
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')
    if params.get("surface", None) is None:
        raise StyxValidationError("`surface` must not be None")
    if not isinstance(params["surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface` has the wrong type: Received `{type(params["surface"])}` expected `InputPathType`')

def surf_localstat_cargs(params: SurfLocalstat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SurfLocalstat")
    v_0 = params.get("hood")
    if v_0 is not None:
        cargs.append("-hood")
        cargs.append(str(v_0))
    v_1 = params.get("nbhd_rad")
    if v_1 is not None:
        cargs.append("-nbhd_rad")
        cargs.append(str(v_1))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-stat")
    cargs.append(str(params["stat"]))
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_dataset"]))
    cargs.append("-i_gii")
    cargs.append(execution.input_file(params["surface"]))
    return cargs

def surf_localstat_outputs(params: SurfLocalstat, execution: Execution) -> SurfLocalstatOutputs:
    """Build the SurfLocalstatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}.niml.dset')
    return SurfLocalstatOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def surf_localstat_execute(params: SurfLocalstat, runner: Runner | None = None) -> SurfLocalstatOutputs:
    """
    SurfLocalstat

    Compute local statistics on a surface mesh

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surf_localstat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURF_LOCALSTAT_METADATA)
    execution.params(params)
    args = surf_localstat_cargs(params, execution)
    out = surf_localstat_outputs(params, execution)
    execution.run(args)
    return out

def surf_localstat(
    prefix: str,
    stat: SurfLocalstatStat,
    input_dataset: InputPathType,
    surface: InputPathType,
    hood: float | None = None,
    nbhd_rad: float | None = None,
    runner: Runner | None = None,
) -> SurfLocalstatOutputs:
    """
    SurfLocalstat

    Compute local statistics on a surface mesh

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix of output data set.
        stat: Compute the specified statistic on the values extracted from the\
            region around each voxel. Options: mean, mode, num, FWHM, ALL.
        input_dataset: Input dataset.
        surface: Input GIFTI surface file.
        hood: Neighborhood of nodes within the specified radius R.
        nbhd_rad: Distance from node n as measured by the shortest distance along\
            the mesh.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surf_localstat_params(
        prefix=prefix,
        stat=stat,
        input_dataset=input_dataset,
        surface=surface,
        hood=hood,
        nbhd_rad=nbhd_rad,
    )
    return surf_localstat_execute(params, runner)

__all__ = [
    "SurfLocalstat",
    "SurfLocalstatOutputs",
    "SURF_LOCALSTAT_METADATA",
    "surf_localstat_cargs",
    "surf_localstat_outputs",
    "surf_localstat_params",
    "surf_localstat_execute",
    "surf_localstat_validate",
    "surf_localstat",
]