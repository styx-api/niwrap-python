# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PLUGOUT_IJK_METADATA = Metadata(
    id="plugout_ijk",
    name="plugout_ijk",
    package="afni",
)

PlugoutIjk = typing.TypedDict(
    "PlugoutIjk",
    {
        "@type": typing.NotRequired[typing.Literal["afni/plugout_ijk"]],
        "host": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
        "port": typing.NotRequired[float],
        "name": typing.NotRequired[str],
        "port_offset": typing.NotRequired[float],
        "port_quiet": typing.NotRequired[float],
        "port_bloc_offset": typing.NotRequired[float],
        "max_bloc": typing.NotRequired[bool],
        "max_bloc_quiet": typing.NotRequired[bool],
        "num_assigned_ports": typing.NotRequired[bool],
        "num_assigned_ports_quiet": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PlugoutIjkOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def plugout_ijk_params(
    host: str | None = None,
    verbose: bool = False,
    port: float | None = None,
    name: str | None = None,
    port_offset: float | None = None,
    port_quiet: float | None = None,
    port_bloc_offset: float | None = None,
    max_bloc: bool = False,
    max_bloc_quiet: bool = False,
    num_assigned_ports: bool = False,
    num_assigned_ports_quiet: bool = False,
) -> PlugoutIjk:
    """
    Build parameters.

    Args:
        host: Connect to AFNI running on the specified computer using TCP/IP.
        verbose: Verbose mode.
        port: Use TCP/IP port number 'pp'.
        name: Use the string 'sss' for the name that AFNI assigns to this plugout.
        port_offset: Provide a port offset to allow multiple instances of\
            communicating programs to operate on the same machine.
        port_quiet: Provide a port offset like -np, but more quiet in the face of\
            adversity.
        port_bloc_offset: Provide a port offset block for easier port management.
        max_bloc: Print the current value of MAX_BLOC and exit.
        max_bloc_quiet: Print MAX_BLOC value only and exit.
        num_assigned_ports: Print the number of assigned ports used by AFNI then\
            quit.
        num_assigned_ports_quiet: Prints the number of assigned ports quietly.

    Returns:
        Parameter dictionary.
    """
    params: PlugoutIjk = {
        "@type": "afni/plugout_ijk",
        "verbose": verbose,
        "max_bloc": max_bloc,
        "max_bloc_quiet": max_bloc_quiet,
        "num_assigned_ports": num_assigned_ports,
        "num_assigned_ports_quiet": num_assigned_ports_quiet,
    }
    if host is not None:
        params["host"] = host
    if port is not None:
        params["port"] = port
    if name is not None:
        params["name"] = name
    if port_offset is not None:
        params["port_offset"] = port_offset
    if port_quiet is not None:
        params["port_quiet"] = port_quiet
    if port_bloc_offset is not None:
        params["port_bloc_offset"] = port_bloc_offset
    return params

def plugout_ijk_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PlugoutIjk."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("host", None) is not None:
        if not isinstance(params["host"], str):
            raise StyxValidationError(f'`host` has the wrong type: Received `{type(params["host"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("port", None) is not None:
        if not isinstance(params["port"], (float, int)):
            raise StyxValidationError(f'`port` has the wrong type: Received `{type(params["port"])}` expected `float`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("port_offset", None) is not None:
        if not isinstance(params["port_offset"], (float, int)):
            raise StyxValidationError(f'`port_offset` has the wrong type: Received `{type(params["port_offset"])}` expected `float`')
        if not (1025 <= params["port_offset"] <= 65500):
            raise StyxValidationError("Parameter `port_offset` must be between 1025 and 65500 (inclusive)")
    if params.get("port_quiet", None) is not None:
        if not isinstance(params["port_quiet"], (float, int)):
            raise StyxValidationError(f'`port_quiet` has the wrong type: Received `{type(params["port_quiet"])}` expected `float`')
    if params.get("port_bloc_offset", None) is not None:
        if not isinstance(params["port_bloc_offset"], (float, int)):
            raise StyxValidationError(f'`port_bloc_offset` has the wrong type: Received `{type(params["port_bloc_offset"])}` expected `float`')
        if params["port_bloc_offset"] > 4000:
            raise StyxValidationError("Parameter `port_bloc_offset` must be at most 4000")
    if params.get("max_bloc", None) is not None:
        if not isinstance(params["max_bloc"], bool):
            raise StyxValidationError(f'`max_bloc` has the wrong type: Received `{type(params["max_bloc"])}` expected `bool`')
    if params.get("max_bloc_quiet", None) is not None:
        if not isinstance(params["max_bloc_quiet"], bool):
            raise StyxValidationError(f'`max_bloc_quiet` has the wrong type: Received `{type(params["max_bloc_quiet"])}` expected `bool`')
    if params.get("num_assigned_ports", None) is not None:
        if not isinstance(params["num_assigned_ports"], bool):
            raise StyxValidationError(f'`num_assigned_ports` has the wrong type: Received `{type(params["num_assigned_ports"])}` expected `bool`')
    if params.get("num_assigned_ports_quiet", None) is not None:
        if not isinstance(params["num_assigned_ports_quiet"], bool):
            raise StyxValidationError(f'`num_assigned_ports_quiet` has the wrong type: Received `{type(params["num_assigned_ports_quiet"])}` expected `bool`')

def plugout_ijk_cargs(params: PlugoutIjk, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("plugout_ijk")
    v_0 = params.get("host")
    if v_0 is not None:
        cargs.append("-host")
        cargs.append(v_0)
    if params.get("verbose"):
        cargs.append("-v")
    v_1 = params.get("port")
    if v_1 is not None:
        cargs.append("-port")
        cargs.append(str(v_1))
    v_2 = params.get("name")
    if v_2 is not None:
        cargs.append("-name")
        cargs.append(v_2)
    v_3 = params.get("port_offset")
    if v_3 is not None:
        cargs.append("-np")
        cargs.append(str(v_3))
    v_4 = params.get("port_quiet")
    if v_4 is not None:
        cargs.append("-npq")
        cargs.append(str(v_4))
    v_5 = params.get("port_bloc_offset")
    if v_5 is not None:
        cargs.append("-npb")
        cargs.append(str(v_5))
    if params.get("max_bloc"):
        cargs.append("-max_port_bloc")
    if params.get("max_bloc_quiet"):
        cargs.append("-max_port_bloc_quiet")
    if params.get("num_assigned_ports"):
        cargs.append("-num_assigned_ports")
    if params.get("num_assigned_ports_quiet"):
        cargs.append("-num_assigned_ports_quiet")
    return cargs

def plugout_ijk_outputs(params: PlugoutIjk, execution: Execution) -> PlugoutIjkOutputs:
    """Build the PlugoutIjkOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PlugoutIjkOutputs(
        root=root_v,
    )

def plugout_ijk_execute(params: PlugoutIjk, runner: Runner | None = None) -> PlugoutIjkOutputs:
    """
    plugout_ijk

    Connects to AFNI and sends (i,j,k) dataset indices to control the viewpoint.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    plugout_ijk_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PLUGOUT_IJK_METADATA)
    execution.params(params)
    args = plugout_ijk_cargs(params, execution)
    out = plugout_ijk_outputs(params, execution)
    execution.run(args)
    return out

def plugout_ijk(
    host: str | None = None,
    verbose: bool = False,
    port: float | None = None,
    name: str | None = None,
    port_offset: float | None = None,
    port_quiet: float | None = None,
    port_bloc_offset: float | None = None,
    max_bloc: bool = False,
    max_bloc_quiet: bool = False,
    num_assigned_ports: bool = False,
    num_assigned_ports_quiet: bool = False,
    runner: Runner | None = None,
) -> PlugoutIjkOutputs:
    """
    plugout_ijk

    Connects to AFNI and sends (i,j,k) dataset indices to control the viewpoint.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        host: Connect to AFNI running on the specified computer using TCP/IP.
        verbose: Verbose mode.
        port: Use TCP/IP port number 'pp'.
        name: Use the string 'sss' for the name that AFNI assigns to this plugout.
        port_offset: Provide a port offset to allow multiple instances of\
            communicating programs to operate on the same machine.
        port_quiet: Provide a port offset like -np, but more quiet in the face of\
            adversity.
        port_bloc_offset: Provide a port offset block for easier port management.
        max_bloc: Print the current value of MAX_BLOC and exit.
        max_bloc_quiet: Print MAX_BLOC value only and exit.
        num_assigned_ports: Print the number of assigned ports used by AFNI then\
            quit.
        num_assigned_ports_quiet: Prints the number of assigned ports quietly.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = plugout_ijk_params(
        host=host,
        verbose=verbose,
        port=port,
        name=name,
        port_offset=port_offset,
        port_quiet=port_quiet,
        port_bloc_offset=port_bloc_offset,
        max_bloc=max_bloc,
        max_bloc_quiet=max_bloc_quiet,
        num_assigned_ports=num_assigned_ports,
        num_assigned_ports_quiet=num_assigned_ports_quiet,
    )
    return plugout_ijk_execute(params, runner)

__all__ = [
    "PlugoutIjk",
    "PlugoutIjkOutputs",
    "PLUGOUT_IJK_METADATA",
    "plugout_ijk_cargs",
    "plugout_ijk_outputs",
    "plugout_ijk_params",
    "plugout_ijk_execute",
    "plugout_ijk_validate",
    "plugout_ijk",
]