# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FI_RDESIGN_METADATA = Metadata(
    id="FIRdesign",
    name="FIRdesign",
    package="afni",
)

FiRdesign = typing.TypedDict(
    "FiRdesign",
    {
        "@type": typing.NotRequired[typing.Literal["afni/FIRdesign"]],
        "fbot": float,
        "ftop": float,
        "ntap": float,
        "tr": typing.NotRequired[float],
        "alternative_band": typing.NotRequired[list[float]],
        "alternative_ntap": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class FiRdesignOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fi_rdesign_params(
    fbot: float,
    ftop: float,
    ntap: float,
    tr: float | None = None,
    alternative_band: list[float] | None = None,
    alternative_ntap: float | None = None,
) -> FiRdesign:
    """
    Build parameters.

    Args:
        fbot: Lowest frequency in the pass band.
        ftop: Highest frequency in the pass band, must be higher than fbot and <=\
            0.5/TR.
        ntap: Number of filter weights (AKA 'taps') to use, must be in the range\
            8..2000 (inclusive).
        tr: Set time grid spacing to 'dd' [default is 1.0]
        alternative_band: Alternative way to specify the passband
        alternative_ntap: Alternative way to specify the number of taps

    Returns:
        Parameter dictionary.
    """
    params: FiRdesign = {
        "@type": "afni/FIRdesign",
        "fbot": fbot,
        "ftop": ftop,
        "ntap": ntap,
    }
    if tr is not None:
        params["tr"] = tr
    if alternative_band is not None:
        params["alternative_band"] = alternative_band
    if alternative_ntap is not None:
        params["alternative_ntap"] = alternative_ntap
    return params

def fi_rdesign_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FiRdesign."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fbot", None) is None:
        raise StyxValidationError("`fbot` must not be None")
    if not isinstance(params["fbot"], (float, int)):
        raise StyxValidationError(f'`fbot` has the wrong type: Received `{type(params["fbot"])}` expected `float`')
    if params["fbot"] < 0:
        raise StyxValidationError("Parameter `fbot` must be at least 0")
    if params.get("ftop", None) is None:
        raise StyxValidationError("`ftop` must not be None")
    if not isinstance(params["ftop"], (float, int)):
        raise StyxValidationError(f'`ftop` has the wrong type: Received `{type(params["ftop"])}` expected `float`')
    if params["ftop"] < 0:
        raise StyxValidationError("Parameter `ftop` must be at least 0")
    if params.get("ntap", None) is None:
        raise StyxValidationError("`ntap` must not be None")
    if not isinstance(params["ntap"], (float, int)):
        raise StyxValidationError(f'`ntap` has the wrong type: Received `{type(params["ntap"])}` expected `float`')
    if not (8 <= params["ntap"] <= 2000):
        raise StyxValidationError("Parameter `ntap` must be between 8 and 2000 (inclusive)")
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("alternative_band", None) is not None:
        if not isinstance(params["alternative_band"], list):
            raise StyxValidationError(f'`alternative_band` has the wrong type: Received `{type(params["alternative_band"])}` expected `list[float]`')
        if len(params["alternative_band"]) > 2:
            raise StyxValidationError("Parameter `alternative_band` must contain at most 2 elements")
        for e in params["alternative_band"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`alternative_band` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("alternative_ntap", None) is not None:
        if not isinstance(params["alternative_ntap"], (float, int)):
            raise StyxValidationError(f'`alternative_ntap` has the wrong type: Received `{type(params["alternative_ntap"])}` expected `float`')
        if not (8 <= params["alternative_ntap"] <= 2000):
            raise StyxValidationError("Parameter `alternative_ntap` must be between 8 and 2000 (inclusive)")

def fi_rdesign_cargs(params: FiRdesign, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("FIRdesign")
    cargs.append(str(params["fbot"]))
    cargs.append(str(params["ftop"]))
    cargs.append(str(params["ntap"]))
    v_0 = params.get("tr")
    if v_0 is not None:
        cargs.append("-TR")
        cargs.append(str(v_0))
    v_1 = params.get("alternative_band")
    if v_1 is not None:
        cargs.append("-band")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("alternative_ntap")
    if v_2 is not None:
        cargs.append("-ntap")
        cargs.append(str(v_2))
    return cargs

def fi_rdesign_outputs(params: FiRdesign, execution: Execution) -> FiRdesignOutputs:
    """Build the FiRdesignOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FiRdesignOutputs(
        root=root_v,
    )

def fi_rdesign_execute(params: FiRdesign, runner: Runner | None = None) -> FiRdesignOutputs:
    """
    FIRdesign

    Uses the Remez algorithm to calculate the FIR filter weights for a bandpass filter; results are written to stdout in an unadorned (no header) column of numbers.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fi_rdesign_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FI_RDESIGN_METADATA)
    execution.params(params)
    args = fi_rdesign_cargs(params, execution)
    out = fi_rdesign_outputs(params, execution)
    execution.run(args)
    return out

def fi_rdesign(
    fbot: float,
    ftop: float,
    ntap: float,
    tr: float | None = None,
    alternative_band: list[float] | None = None,
    alternative_ntap: float | None = None,
    runner: Runner | None = None,
) -> FiRdesignOutputs:
    """
    FIRdesign

    Uses the Remez algorithm to calculate the FIR filter weights for a bandpass filter; results are written to stdout in an unadorned (no header) column of numbers.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        fbot: Lowest frequency in the pass band.
        ftop: Highest frequency in the pass band, must be higher than fbot and <=\
            0.5/TR.
        ntap: Number of filter weights (AKA 'taps') to use, must be in the range\
            8..2000 (inclusive).
        tr: Set time grid spacing to 'dd' [default is 1.0]
        alternative_band: Alternative way to specify the passband
        alternative_ntap: Alternative way to specify the number of taps
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fi_rdesign_params(
        fbot=fbot,
        ftop=ftop,
        ntap=ntap,
        tr=tr,
        alternative_band=alternative_band,
        alternative_ntap=alternative_ntap,
    )
    return fi_rdesign_execute(params, runner)

__all__ = [
    "FiRdesign",
    "FiRdesignOutputs",
    "FI_RDESIGN_METADATA",
    "fi_rdesign_cargs",
    "fi_rdesign_outputs",
    "fi_rdesign_params",
    "fi_rdesign_execute",
    "fi_rdesign_validate",
    "fi_rdesign",
]