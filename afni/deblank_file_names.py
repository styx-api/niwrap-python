# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DEBLANK_FILE_NAMES_METADATA = Metadata(
    id="@DeblankFileNames",
    name="@DeblankFileNames",
    package="afni",
)

DeblankFileNames = typing.TypedDict(
    "DeblankFileNames",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@DeblankFileNames"]],
        "move": typing.NotRequired[bool],
        "nobrac": typing.NotRequired[bool],
        "demo_set": typing.NotRequired[bool],
        "echo": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "files": typing.NotRequired[list[InputPathType]],
    },
)

@dataclasses.dataclass
class DeblankFileNamesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def deblank_file_names_params(
    move: bool = False,
    nobrac: bool = False,
    demo_set: bool = False,
    echo: bool = False,
    help: bool = False,
    files: list[InputPathType] | None = None,
) -> DeblankFileNames:
    """
    Build parameters.

    Args:
        move: Actually rename the files (opposite of -dry_run).
        nobrac: Do not replace () and [] in filenames, just spaces.
        demo_set: Create a toy directory with bad names for testing.
        echo: Turn on script echo.
        help: Display help message.
        files: Specify files to fix as opposed to letting it fix all the names in\
            the current directory.

    Returns:
        Parameter dictionary.
    """
    params: DeblankFileNames = {
        "@type": "afni/@DeblankFileNames",
        "move": move,
        "nobrac": nobrac,
        "demo_set": demo_set,
        "echo": echo,
        "help": help,
    }
    if files is not None:
        params["files"] = files
    return params

def deblank_file_names_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DeblankFileNames."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("move", None) is not None:
        if not isinstance(params["move"], bool):
            raise StyxValidationError(f'`move` has the wrong type: Received `{type(params["move"])}` expected `bool`')
    if params.get("nobrac", None) is not None:
        if not isinstance(params["nobrac"], bool):
            raise StyxValidationError(f'`nobrac` has the wrong type: Received `{type(params["nobrac"])}` expected `bool`')
    if params.get("demo_set", None) is not None:
        if not isinstance(params["demo_set"], bool):
            raise StyxValidationError(f'`demo_set` has the wrong type: Received `{type(params["demo_set"])}` expected `bool`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("files", None) is not None:
        if not isinstance(params["files"], list):
            raise StyxValidationError(f'`files` has the wrong type: Received `{type(params["files"])}` expected `list[InputPathType]`')
        for e in params["files"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def deblank_file_names_cargs(params: DeblankFileNames, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@DeblankFileNames")
    if params.get("move"):
        cargs.append("-move")
    if params.get("nobrac"):
        cargs.append("-nobrac")
    if params.get("demo_set"):
        cargs.append("-demo_set")
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("help"):
        cargs.append("-help")
    v_0 = params.get("files")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(execution.input_file(item0))
    return cargs

def deblank_file_names_outputs(params: DeblankFileNames, execution: Execution) -> DeblankFileNamesOutputs:
    """Build the DeblankFileNamesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DeblankFileNamesOutputs(
        root=root_v,
    )

def deblank_file_names_execute(params: DeblankFileNames, runner: Runner | None = None) -> DeblankFileNamesOutputs:
    """
    @DeblankFileNames

    A script to remove blanks and other annoying characters ([], ()) from filenames.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    deblank_file_names_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DEBLANK_FILE_NAMES_METADATA)
    execution.params(params)
    args = deblank_file_names_cargs(params, execution)
    out = deblank_file_names_outputs(params, execution)
    execution.run(args)
    return out

def deblank_file_names(
    move: bool = False,
    nobrac: bool = False,
    demo_set: bool = False,
    echo: bool = False,
    help: bool = False,
    files: list[InputPathType] | None = None,
    runner: Runner | None = None,
) -> DeblankFileNamesOutputs:
    """
    @DeblankFileNames

    A script to remove blanks and other annoying characters ([], ()) from filenames.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        move: Actually rename the files (opposite of -dry_run).
        nobrac: Do not replace () and [] in filenames, just spaces.
        demo_set: Create a toy directory with bad names for testing.
        echo: Turn on script echo.
        help: Display help message.
        files: Specify files to fix as opposed to letting it fix all the names in\
            the current directory.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = deblank_file_names_params(
        move=move,
        nobrac=nobrac,
        demo_set=demo_set,
        echo=echo,
        help=help,
        files=files,
    )
    return deblank_file_names_execute(params, runner)

__all__ = [
    "DeblankFileNames",
    "DeblankFileNamesOutputs",
    "DEBLANK_FILE_NAMES_METADATA",
    "deblank_file_names_cargs",
    "deblank_file_names_outputs",
    "deblank_file_names_params",
    "deblank_file_names_execute",
    "deblank_file_names_validate",
    "deblank_file_names",
]