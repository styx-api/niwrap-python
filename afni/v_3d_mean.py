# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_MEAN_METADATA = Metadata(
    id="3dMean",
    name="3dMean",
    package="afni",
)

V3dMean = typing.TypedDict(
    "V3dMean",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dMean"]],
        "input_files": list[InputPathType],
        "verbose": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "datum": typing.NotRequired[str],
        "fscale": typing.NotRequired[bool],
        "gscale": typing.NotRequired[bool],
        "nscale": typing.NotRequired[bool],
        "non_zero": typing.NotRequired[bool],
        "stdev": typing.NotRequired[bool],
        "sqr": typing.NotRequired[bool],
        "sum": typing.NotRequired[bool],
        "count": typing.NotRequired[bool],
        "max": typing.NotRequired[bool],
        "min": typing.NotRequired[bool],
        "absmax": typing.NotRequired[bool],
        "signed_absmax": typing.NotRequired[bool],
        "mask_inter": typing.NotRequired[bool],
        "mask_union": typing.NotRequired[bool],
        "weightset": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class V3dMeanOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output dataset"""

def v_3d_mean_params(
    input_files: list[InputPathType],
    verbose: bool = False,
    prefix: str | None = None,
    datum: str | None = None,
    fscale: bool = False,
    gscale: bool = False,
    nscale: bool = False,
    non_zero: bool = False,
    stdev: bool = False,
    sqr: bool = False,
    sum: bool = False,
    count: bool = False,
    max: bool = False,
    min: bool = False,
    absmax: bool = False,
    signed_absmax: bool = False,
    mask_inter: bool = False,
    mask_union: bool = False,
    weightset: InputPathType | None = None,
) -> V3dMean:
    """
    Build parameters.

    Args:
        input_files: Input datasets
        verbose: Print out some information along the way
        prefix: Sets the prefix of the output dataset
        datum: Sets the datum of the output dataset
        fscale: Force scaling of the output to the maximum integer range
        gscale: Force scaling of the output to the maximum integer range, with\
            uniform scaling factor for each sub-brick
        nscale: Don't do any scaling on output to byte or short datasets. Only use\
            if you want the output dataset to be integer-valued.
        non_zero: Use only non-zero values for calculation of mean, min, max, sum,\
            squares
        stdev: Calculate the standard deviation, sqrt(variance), instead of the\
            mean (cannot be used with -sqr, -sum or -non_zero)
        sqr: Average the squares, instead of the values
        sum: Just take the sum (don't divide by number of datasets)
        count: Compute only the count of non-zero voxels
        max: Find the maximum at each voxel
        min: Find the minimum at each voxel
        absmax: Find maximum absolute value at each voxel
        signed_absmax: Find extremes with maximum absolute value but preserve sign
        mask_inter: Create a simple intersection mask
        mask_union: Create a simple union mask
        weightset: Sum of N dsets will be weighted by N volume WSET. This weight\
            dataset must be of type float.

    Returns:
        Parameter dictionary.
    """
    params: V3dMean = {
        "@type": "afni/3dMean",
        "input_files": input_files,
        "verbose": verbose,
        "fscale": fscale,
        "gscale": gscale,
        "nscale": nscale,
        "non_zero": non_zero,
        "stdev": stdev,
        "sqr": sqr,
        "sum": sum,
        "count": count,
        "max": max,
        "min": min,
        "absmax": absmax,
        "signed_absmax": signed_absmax,
        "mask_inter": mask_inter,
        "mask_union": mask_union,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if datum is not None:
        params["datum"] = datum
    if weightset is not None:
        params["weightset"] = weightset
    return params

def v_3d_mean_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dMean."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("datum", None) is not None:
        if not isinstance(params["datum"], str):
            raise StyxValidationError(f'`datum` has the wrong type: Received `{type(params["datum"])}` expected `str`')
    if params.get("fscale", None) is not None:
        if not isinstance(params["fscale"], bool):
            raise StyxValidationError(f'`fscale` has the wrong type: Received `{type(params["fscale"])}` expected `bool`')
    if params.get("gscale", None) is not None:
        if not isinstance(params["gscale"], bool):
            raise StyxValidationError(f'`gscale` has the wrong type: Received `{type(params["gscale"])}` expected `bool`')
    if params.get("nscale", None) is not None:
        if not isinstance(params["nscale"], bool):
            raise StyxValidationError(f'`nscale` has the wrong type: Received `{type(params["nscale"])}` expected `bool`')
    if params.get("non_zero", None) is not None:
        if not isinstance(params["non_zero"], bool):
            raise StyxValidationError(f'`non_zero` has the wrong type: Received `{type(params["non_zero"])}` expected `bool`')
    if params.get("stdev", None) is not None:
        if not isinstance(params["stdev"], bool):
            raise StyxValidationError(f'`stdev` has the wrong type: Received `{type(params["stdev"])}` expected `bool`')
    if params.get("sqr", None) is not None:
        if not isinstance(params["sqr"], bool):
            raise StyxValidationError(f'`sqr` has the wrong type: Received `{type(params["sqr"])}` expected `bool`')
    if params.get("sum", None) is not None:
        if not isinstance(params["sum"], bool):
            raise StyxValidationError(f'`sum` has the wrong type: Received `{type(params["sum"])}` expected `bool`')
    if params.get("count", None) is not None:
        if not isinstance(params["count"], bool):
            raise StyxValidationError(f'`count` has the wrong type: Received `{type(params["count"])}` expected `bool`')
    if params.get("max", None) is not None:
        if not isinstance(params["max"], bool):
            raise StyxValidationError(f'`max` has the wrong type: Received `{type(params["max"])}` expected `bool`')
    if params.get("min", None) is not None:
        if not isinstance(params["min"], bool):
            raise StyxValidationError(f'`min` has the wrong type: Received `{type(params["min"])}` expected `bool`')
    if params.get("absmax", None) is not None:
        if not isinstance(params["absmax"], bool):
            raise StyxValidationError(f'`absmax` has the wrong type: Received `{type(params["absmax"])}` expected `bool`')
    if params.get("signed_absmax", None) is not None:
        if not isinstance(params["signed_absmax"], bool):
            raise StyxValidationError(f'`signed_absmax` has the wrong type: Received `{type(params["signed_absmax"])}` expected `bool`')
    if params.get("mask_inter", None) is not None:
        if not isinstance(params["mask_inter"], bool):
            raise StyxValidationError(f'`mask_inter` has the wrong type: Received `{type(params["mask_inter"])}` expected `bool`')
    if params.get("mask_union", None) is not None:
        if not isinstance(params["mask_union"], bool):
            raise StyxValidationError(f'`mask_union` has the wrong type: Received `{type(params["mask_union"])}` expected `bool`')
    if params.get("weightset", None) is not None:
        if not isinstance(params["weightset"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weightset` has the wrong type: Received `{type(params["weightset"])}` expected `InputPathType`')

def v_3d_mean_cargs(params: V3dMean, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dMean")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    if params.get("verbose"):
        cargs.append("-verbose")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    v_1 = params.get("datum")
    if v_1 is not None:
        cargs.append("-datum")
        cargs.append(v_1)
    if params.get("fscale"):
        cargs.append("-fscale")
    if params.get("gscale"):
        cargs.append("-gscale")
    if params.get("nscale"):
        cargs.append("-nscale")
    if params.get("non_zero"):
        cargs.append("-non_zero")
    if params.get("stdev"):
        cargs.append("-sd")
    if params.get("sqr"):
        cargs.append("-sqr")
    if params.get("sum"):
        cargs.append("-sum")
    if params.get("count"):
        cargs.append("-count")
    if params.get("max"):
        cargs.append("-max")
    if params.get("min"):
        cargs.append("-min")
    if params.get("absmax"):
        cargs.append("-absmax")
    if params.get("signed_absmax"):
        cargs.append("-signed_absmax")
    if params.get("mask_inter"):
        cargs.append("-mask_inter")
    if params.get("mask_union"):
        cargs.append("-mask_union")
    v_2 = params.get("weightset")
    if v_2 is not None:
        cargs.append("-weightset")
        cargs.append(execution.input_file(v_2))
    return cargs

def v_3d_mean_outputs(params: V3dMean, execution: Execution) -> V3dMeanOutputs:
    """Build the V3dMeanOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return V3dMeanOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def v_3d_mean_execute(params: V3dMean, runner: Runner | None = None) -> V3dMeanOutputs:
    """
    3dMean

    Takes the voxel-by-voxel mean of all input datasets; designed to be faster than 3dcalc.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_mean_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_MEAN_METADATA)
    execution.params(params)
    args = v_3d_mean_cargs(params, execution)
    out = v_3d_mean_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_mean(
    input_files: list[InputPathType],
    verbose: bool = False,
    prefix: str | None = None,
    datum: str | None = None,
    fscale: bool = False,
    gscale: bool = False,
    nscale: bool = False,
    non_zero: bool = False,
    stdev: bool = False,
    sqr: bool = False,
    sum: bool = False,
    count: bool = False,
    max: bool = False,
    min: bool = False,
    absmax: bool = False,
    signed_absmax: bool = False,
    mask_inter: bool = False,
    mask_union: bool = False,
    weightset: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dMeanOutputs:
    """
    3dMean

    Takes the voxel-by-voxel mean of all input datasets; designed to be faster than 3dcalc.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input datasets
        verbose: Print out some information along the way
        prefix: Sets the prefix of the output dataset
        datum: Sets the datum of the output dataset
        fscale: Force scaling of the output to the maximum integer range
        gscale: Force scaling of the output to the maximum integer range, with\
            uniform scaling factor for each sub-brick
        nscale: Don't do any scaling on output to byte or short datasets. Only use\
            if you want the output dataset to be integer-valued.
        non_zero: Use only non-zero values for calculation of mean, min, max, sum,\
            squares
        stdev: Calculate the standard deviation, sqrt(variance), instead of the\
            mean (cannot be used with -sqr, -sum or -non_zero)
        sqr: Average the squares, instead of the values
        sum: Just take the sum (don't divide by number of datasets)
        count: Compute only the count of non-zero voxels
        max: Find the maximum at each voxel
        min: Find the minimum at each voxel
        absmax: Find maximum absolute value at each voxel
        signed_absmax: Find extremes with maximum absolute value but preserve sign
        mask_inter: Create a simple intersection mask
        mask_union: Create a simple union mask
        weightset: Sum of N dsets will be weighted by N volume WSET. This weight\
            dataset must be of type float.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_mean_params(
        input_files=input_files,
        verbose=verbose,
        prefix=prefix,
        datum=datum,
        fscale=fscale,
        gscale=gscale,
        nscale=nscale,
        non_zero=non_zero,
        stdev=stdev,
        sqr=sqr,
        sum=sum,
        count=count,
        max=max,
        min=min,
        absmax=absmax,
        signed_absmax=signed_absmax,
        mask_inter=mask_inter,
        mask_union=mask_union,
        weightset=weightset,
    )
    return v_3d_mean_execute(params, runner)

__all__ = [
    "V3dMean",
    "V3dMeanOutputs",
    "V_3D_MEAN_METADATA",
    "v_3d_mean_cargs",
    "v_3d_mean_outputs",
    "v_3d_mean_params",
    "v_3d_mean_execute",
    "v_3d_mean_validate",
    "v_3d_mean",
]