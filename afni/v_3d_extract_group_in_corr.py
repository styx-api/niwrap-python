# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_EXTRACT_GROUP_IN_CORR_METADATA = Metadata(
    id="3dExtractGroupInCorr",
    name="3dExtractGroupInCorr",
    package="afni",
)

V3dExtractGroupInCorr = typing.TypedDict(
    "V3dExtractGroupInCorr",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dExtractGroupInCorr"]],
        "group_in_corr_file": InputPathType,
        "prefix": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dExtractGroupInCorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType | None
    """Output dataset reconstructed from GroupInCorr data"""

def v_3d_extract_group_in_corr_params(
    group_in_corr_file: InputPathType,
    prefix: str | None = None,
) -> V3dExtractGroupInCorr:
    """
    Build parameters.

    Args:
        group_in_corr_file: GroupInCorr file to extract datasets from (e.g.\
            AAA.grpincorr.niml)
        prefix: Prefix to prepend to dataset labels. Use 'NULL' to skip the use of\
            the prefix.

    Returns:
        Parameter dictionary.
    """
    params: V3dExtractGroupInCorr = {
        "@type": "afni/3dExtractGroupInCorr",
        "group_in_corr_file": group_in_corr_file,
    }
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_extract_group_in_corr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dExtractGroupInCorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("group_in_corr_file", None) is None:
        raise StyxValidationError("`group_in_corr_file` must not be None")
    if not isinstance(params["group_in_corr_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`group_in_corr_file` has the wrong type: Received `{type(params["group_in_corr_file"])}` expected `InputPathType`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')

def v_3d_extract_group_in_corr_cargs(params: V3dExtractGroupInCorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dExtractGroupInCorr")
    cargs.append(execution.input_file(params["group_in_corr_file"]))
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    return cargs

def v_3d_extract_group_in_corr_outputs(params: V3dExtractGroupInCorr, execution: Execution) -> V3dExtractGroupInCorrOutputs:
    """Build the V3dExtractGroupInCorrOutputs object for this tool."""
    output_dataset_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_dataset_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_[DATASET_LABEL].nii')
    return V3dExtractGroupInCorrOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def v_3d_extract_group_in_corr_execute(params: V3dExtractGroupInCorr, runner: Runner | None = None) -> V3dExtractGroupInCorrOutputs:
    """
    3dExtractGroupInCorr

    This program breaks the collection of images from a GroupInCorr file back into individual AFNI 3D+time datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_extract_group_in_corr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_EXTRACT_GROUP_IN_CORR_METADATA)
    execution.params(params)
    args = v_3d_extract_group_in_corr_cargs(params, execution)
    out = v_3d_extract_group_in_corr_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_extract_group_in_corr(
    group_in_corr_file: InputPathType,
    prefix: str | None = None,
    runner: Runner | None = None,
) -> V3dExtractGroupInCorrOutputs:
    """
    3dExtractGroupInCorr

    This program breaks the collection of images from a GroupInCorr file back into individual AFNI 3D+time datasets.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        group_in_corr_file: GroupInCorr file to extract datasets from (e.g.\
            AAA.grpincorr.niml)
        prefix: Prefix to prepend to dataset labels. Use 'NULL' to skip the use of\
            the prefix.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_extract_group_in_corr_params(
        group_in_corr_file=group_in_corr_file,
        prefix=prefix,
    )
    return v_3d_extract_group_in_corr_execute(params, runner)

__all__ = [
    "V3dExtractGroupInCorr",
    "V3dExtractGroupInCorrOutputs",
    "V_3D_EXTRACT_GROUP_IN_CORR_METADATA",
    "v_3d_extract_group_in_corr_cargs",
    "v_3d_extract_group_in_corr_outputs",
    "v_3d_extract_group_in_corr_params",
    "v_3d_extract_group_in_corr_execute",
    "v_3d_extract_group_in_corr_validate",
    "v_3d_extract_group_in_corr",
]