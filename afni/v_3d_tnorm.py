# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_TNORM_METADATA = Metadata(
    id="3dTnorm",
    name="3dTnorm",
    package="afni",
)

V3dTnorm = typing.TypedDict(
    "V3dTnorm",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dTnorm"]],
        "prefix": typing.NotRequired[str],
        "norm2": typing.NotRequired[bool],
        "normR": typing.NotRequired[bool],
        "norm1": typing.NotRequired[bool],
        "normx": typing.NotRequired[bool],
        "polort": typing.NotRequired[float],
        "L1fit": typing.NotRequired[bool],
        "input_dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dTnormOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType | None
    """Normalized output dataset"""

def v_3d_tnorm_params(
    input_dataset: InputPathType,
    prefix: str | None = None,
    norm2: bool = False,
    normR: bool = False,
    norm1: bool = False,
    normx: bool = False,
    polort: float | None = None,
    L1fit: bool = False,
) -> V3dTnorm:
    """
    Build parameters.

    Args:
        input_dataset: Input dataset (e.g. data.nii)
        prefix: Prefix for the output dataset
        norm2: L2 normalize (sum of squares = 1)
        normR: Normalize so sum of squares = number of time points
        norm1: L1 normalize (sum of absolute values = 1)
        normx: Scale so max absolute value = 1 (L_infinity norm)
        polort: Detrend with polynomials of order p before normalizing
        L1fit: Detrend with L1 regression (L2 is default)

    Returns:
        Parameter dictionary.
    """
    params: V3dTnorm = {
        "@type": "afni/3dTnorm",
        "input_dataset": input_dataset,
        "norm2": norm2,
        "normR": normR,
        "norm1": norm1,
        "normx": normx,
        "L1fit": L1fit,
    }
    if prefix is not None:
        params["prefix"] = prefix
    if polort is not None:
        params["polort"] = polort
    return params

def v_3d_tnorm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dTnorm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("norm2", None) is not None:
        if not isinstance(params["norm2"], bool):
            raise StyxValidationError(f'`norm2` has the wrong type: Received `{type(params["norm2"])}` expected `bool`')
    if params.get("normR", None) is not None:
        if not isinstance(params["normR"], bool):
            raise StyxValidationError(f'`normR` has the wrong type: Received `{type(params["normR"])}` expected `bool`')
    if params.get("norm1", None) is not None:
        if not isinstance(params["norm1"], bool):
            raise StyxValidationError(f'`norm1` has the wrong type: Received `{type(params["norm1"])}` expected `bool`')
    if params.get("normx", None) is not None:
        if not isinstance(params["normx"], bool):
            raise StyxValidationError(f'`normx` has the wrong type: Received `{type(params["normx"])}` expected `bool`')
    if params.get("polort", None) is not None:
        if not isinstance(params["polort"], (float, int)):
            raise StyxValidationError(f'`polort` has the wrong type: Received `{type(params["polort"])}` expected `float`')
    if params.get("L1fit", None) is not None:
        if not isinstance(params["L1fit"], bool):
            raise StyxValidationError(f'`L1fit` has the wrong type: Received `{type(params["L1fit"])}` expected `bool`')
    if params.get("input_dataset", None) is None:
        raise StyxValidationError("`input_dataset` must not be None")
    if not isinstance(params["input_dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dataset` has the wrong type: Received `{type(params["input_dataset"])}` expected `InputPathType`')

def v_3d_tnorm_cargs(params: V3dTnorm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dTnorm")
    v_0 = params.get("prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("norm2"):
        cargs.append("-norm2")
    if params.get("normR"):
        cargs.append("-normR")
    if params.get("norm1"):
        cargs.append("-norm1")
    if params.get("normx"):
        cargs.append("-normx")
    v_1 = params.get("polort")
    if v_1 is not None:
        cargs.append("-polort")
        cargs.append(str(v_1))
    if params.get("L1fit"):
        cargs.append("-L1fit")
    cargs.append(execution.input_file(params["input_dataset"]))
    return cargs

def v_3d_tnorm_outputs(params: V3dTnorm, execution: Execution) -> V3dTnormOutputs:
    """Build the V3dTnormOutputs object for this tool."""
    output_dataset_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_dataset_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii')
    return V3dTnormOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def v_3d_tnorm_execute(params: V3dTnorm, runner: Runner | None = None) -> V3dTnormOutputs:
    """
    3dTnorm

    Normalizes each voxel time series by multiplicative scaling

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_tnorm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_TNORM_METADATA)
    execution.params(params)
    args = v_3d_tnorm_cargs(params, execution)
    out = v_3d_tnorm_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_tnorm(
    input_dataset: InputPathType,
    prefix: str | None = None,
    norm2: bool = False,
    normR: bool = False,
    norm1: bool = False,
    normx: bool = False,
    polort: float | None = None,
    L1fit: bool = False,
    runner: Runner | None = None,
) -> V3dTnormOutputs:
    """
    3dTnorm

    Normalizes each voxel time series by multiplicative scaling

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dataset: Input dataset (e.g. data.nii)
        prefix: Prefix for the output dataset
        norm2: L2 normalize (sum of squares = 1)
        normR: Normalize so sum of squares = number of time points
        norm1: L1 normalize (sum of absolute values = 1)
        normx: Scale so max absolute value = 1 (L_infinity norm)
        polort: Detrend with polynomials of order p before normalizing
        L1fit: Detrend with L1 regression (L2 is default)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_tnorm_params(
        input_dataset=input_dataset,
        prefix=prefix,
        norm2=norm2,
        normR=normR,
        norm1=norm1,
        normx=normx,
        polort=polort,
        L1fit=L1fit,
    )
    return v_3d_tnorm_execute(params, runner)

__all__ = [
    "V3dTnorm",
    "V3dTnormOutputs",
    "V_3D_TNORM_METADATA",
    "v_3d_tnorm_cargs",
    "v_3d_tnorm_outputs",
    "v_3d_tnorm_params",
    "v_3d_tnorm_execute",
    "v_3d_tnorm_validate",
    "v_3d_tnorm",
]