# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ADJUNCT_COMBINE_STR_PY_METADATA = Metadata(
    id="adjunct_combine_str.py",
    name="adjunct_combine_str.py",
    package="afni",
)

AdjunctCombineStrPy = typing.TypedDict(
    "AdjunctCombineStrPy",
    {
        "@type": typing.NotRequired[typing.Literal["afni/adjunct_combine_str.py"]],
        "output_file": str,
        "upper_index": float,
        "string_selectors": list[str],
    },
)

@dataclasses.dataclass
class AdjunctCombineStrPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_selector_file: OutputPathType
    """The output file containing the new string selector"""

def adjunct_combine_str_py_params(
    output_file: str,
    upper_index: float,
    string_selectors: list[str],
) -> AdjunctCombineStrPy:
    """
    Build parameters.

    Args:
        output_file: An output file name
        upper_index: An int that is the upper index for the selector (-1 means to\
            use the max number in the input strings)
        string_selectors: One or more string selector strings of *goods* to keep

    Returns:
        Parameter dictionary.
    """
    params: AdjunctCombineStrPy = {
        "@type": "afni/adjunct_combine_str.py",
        "output_file": output_file,
        "upper_index": upper_index,
        "string_selectors": string_selectors,
    }
    return params

def adjunct_combine_str_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AdjunctCombineStrPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("upper_index", None) is None:
        raise StyxValidationError("`upper_index` must not be None")
    if not isinstance(params["upper_index"], (float, int)):
        raise StyxValidationError(f'`upper_index` has the wrong type: Received `{type(params["upper_index"])}` expected `float`')
    if params.get("string_selectors", None) is None:
        raise StyxValidationError("`string_selectors` must not be None")
    if not isinstance(params["string_selectors"], list):
        raise StyxValidationError(f'`string_selectors` has the wrong type: Received `{type(params["string_selectors"])}` expected `list[str]`')
    for e in params["string_selectors"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`string_selectors` has the wrong type: Received `{type(e)}` expected `list[str]`')

def adjunct_combine_str_py_cargs(params: AdjunctCombineStrPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("adjunct_combine_str.py")
    cargs.append(params["output_file"])
    cargs.append(str(params["upper_index"]))
    for item0 in params["string_selectors"]:
        cargs.append(item0)
    return cargs

def adjunct_combine_str_py_outputs(params: AdjunctCombineStrPy, execution: Execution) -> AdjunctCombineStrPyOutputs:
    """Build the AdjunctCombineStrPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_selector_file_v: OutputPathType = execution.output_file(params["output_file"])
    return AdjunctCombineStrPyOutputs(
        root=root_v,
        output_selector_file=output_selector_file_v,
    )

def adjunct_combine_str_py_execute(params: AdjunctCombineStrPy, runner: Runner | None = None) -> AdjunctCombineStrPyOutputs:
    """
    adjunct_combine_str.py

    A simple helper function for fat_proc* scripts that processes string selectors and outputs a new string selector.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    adjunct_combine_str_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ADJUNCT_COMBINE_STR_PY_METADATA)
    execution.params(params)
    args = adjunct_combine_str_py_cargs(params, execution)
    out = adjunct_combine_str_py_outputs(params, execution)
    execution.run(args)
    return out

def adjunct_combine_str_py(
    output_file: str,
    upper_index: float,
    string_selectors: list[str],
    runner: Runner | None = None,
) -> AdjunctCombineStrPyOutputs:
    """
    adjunct_combine_str.py

    A simple helper function for fat_proc* scripts that processes string selectors and outputs a new string selector.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        output_file: An output file name
        upper_index: An int that is the upper index for the selector (-1 means to\
            use the max number in the input strings)
        string_selectors: One or more string selector strings of *goods* to keep
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = adjunct_combine_str_py_params(
        output_file=output_file,
        upper_index=upper_index,
        string_selectors=string_selectors,
    )
    return adjunct_combine_str_py_execute(params, runner)

__all__ = [
    "AdjunctCombineStrPy",
    "AdjunctCombineStrPyOutputs",
    "ADJUNCT_COMBINE_STR_PY_METADATA",
    "adjunct_combine_str_py_cargs",
    "adjunct_combine_str_py_outputs",
    "adjunct_combine_str_py_params",
    "adjunct_combine_str_py_execute",
    "adjunct_combine_str_py_validate",
    "adjunct_combine_str_py",
]