# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_1D_APAR2MAT_METADATA = Metadata(
    id="1dApar2mat",
    name="1dApar2mat",
    package="afni",
)

V1dApar2mat = typing.TypedDict(
    "V1dApar2mat",
    {
        "@type": typing.NotRequired[typing.Literal["afni/1dApar2mat"]],
        "x_shift": float,
        "y_shift": float,
        "z_shift": float,
        "z_angle": float,
        "x_angle": float,
        "y_angle": float,
        "x_scale": float,
        "y_scale": float,
        "z_scale": float,
        "y_x_shear": float,
        "z_x_shear": float,
        "z_y_shear": float,
    },
)

@dataclasses.dataclass
class V1dApar2matOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_1d_apar2mat_params(
    x_shift: float,
    y_shift: float,
    z_shift: float,
    z_angle: float,
    x_angle: float,
    y_angle: float,
    x_scale: float,
    y_scale: float,
    z_scale: float,
    y_x_shear: float,
    z_x_shear: float,
    z_y_shear: float,
) -> V1dApar2mat:
    """
    Build parameters.

    Args:
        x_shift: x-shift in mm
        y_shift: y-shift in mm
        z_shift: z-shift in mm
        z_angle: z-angle (roll) in degrees
        x_angle: x-angle (pitch) in degrees
        y_angle: y-angle (yaw) in degrees
        x_scale: x-scale factor in [0.10,10.0]
        y_scale: y-scale factor in [0.10,10.0]
        z_scale: z-scale factor in [0.10,10.0]
        y_x_shear: y/x-shear factor in [-0.3333,0.3333]
        z_x_shear: z/x-shear factor in [-0.3333,0.3333]
        z_y_shear: z/y-shear factor in [-0.3333,0.3333]

    Returns:
        Parameter dictionary.
    """
    params: V1dApar2mat = {
        "@type": "afni/1dApar2mat",
        "x_shift": x_shift,
        "y_shift": y_shift,
        "z_shift": z_shift,
        "z_angle": z_angle,
        "x_angle": x_angle,
        "y_angle": y_angle,
        "x_scale": x_scale,
        "y_scale": y_scale,
        "z_scale": z_scale,
        "y_x_shear": y_x_shear,
        "z_x_shear": z_x_shear,
        "z_y_shear": z_y_shear,
    }
    return params

def v_1d_apar2mat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V1dApar2mat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("x_shift", None) is None:
        raise StyxValidationError("`x_shift` must not be None")
    if not isinstance(params["x_shift"], (float, int)):
        raise StyxValidationError(f'`x_shift` has the wrong type: Received `{type(params["x_shift"])}` expected `float`')
    if params.get("y_shift", None) is None:
        raise StyxValidationError("`y_shift` must not be None")
    if not isinstance(params["y_shift"], (float, int)):
        raise StyxValidationError(f'`y_shift` has the wrong type: Received `{type(params["y_shift"])}` expected `float`')
    if params.get("z_shift", None) is None:
        raise StyxValidationError("`z_shift` must not be None")
    if not isinstance(params["z_shift"], (float, int)):
        raise StyxValidationError(f'`z_shift` has the wrong type: Received `{type(params["z_shift"])}` expected `float`')
    if params.get("z_angle", None) is None:
        raise StyxValidationError("`z_angle` must not be None")
    if not isinstance(params["z_angle"], (float, int)):
        raise StyxValidationError(f'`z_angle` has the wrong type: Received `{type(params["z_angle"])}` expected `float`')
    if params.get("x_angle", None) is None:
        raise StyxValidationError("`x_angle` must not be None")
    if not isinstance(params["x_angle"], (float, int)):
        raise StyxValidationError(f'`x_angle` has the wrong type: Received `{type(params["x_angle"])}` expected `float`')
    if params.get("y_angle", None) is None:
        raise StyxValidationError("`y_angle` must not be None")
    if not isinstance(params["y_angle"], (float, int)):
        raise StyxValidationError(f'`y_angle` has the wrong type: Received `{type(params["y_angle"])}` expected `float`')
    if params.get("x_scale", None) is None:
        raise StyxValidationError("`x_scale` must not be None")
    if not isinstance(params["x_scale"], (float, int)):
        raise StyxValidationError(f'`x_scale` has the wrong type: Received `{type(params["x_scale"])}` expected `float`')
    if not (0.1 <= params["x_scale"] <= 10):
        raise StyxValidationError("Parameter `x_scale` must be between 0.1 and 10 (inclusive)")
    if params.get("y_scale", None) is None:
        raise StyxValidationError("`y_scale` must not be None")
    if not isinstance(params["y_scale"], (float, int)):
        raise StyxValidationError(f'`y_scale` has the wrong type: Received `{type(params["y_scale"])}` expected `float`')
    if not (0.1 <= params["y_scale"] <= 10):
        raise StyxValidationError("Parameter `y_scale` must be between 0.1 and 10 (inclusive)")
    if params.get("z_scale", None) is None:
        raise StyxValidationError("`z_scale` must not be None")
    if not isinstance(params["z_scale"], (float, int)):
        raise StyxValidationError(f'`z_scale` has the wrong type: Received `{type(params["z_scale"])}` expected `float`')
    if not (0.1 <= params["z_scale"] <= 10):
        raise StyxValidationError("Parameter `z_scale` must be between 0.1 and 10 (inclusive)")
    if params.get("y_x_shear", None) is None:
        raise StyxValidationError("`y_x_shear` must not be None")
    if not isinstance(params["y_x_shear"], (float, int)):
        raise StyxValidationError(f'`y_x_shear` has the wrong type: Received `{type(params["y_x_shear"])}` expected `float`')
    if not (-0.3333 <= params["y_x_shear"] <= 0.3333):
        raise StyxValidationError("Parameter `y_x_shear` must be between -0.3333 and 0.3333 (inclusive)")
    if params.get("z_x_shear", None) is None:
        raise StyxValidationError("`z_x_shear` must not be None")
    if not isinstance(params["z_x_shear"], (float, int)):
        raise StyxValidationError(f'`z_x_shear` has the wrong type: Received `{type(params["z_x_shear"])}` expected `float`')
    if not (-0.3333 <= params["z_x_shear"] <= 0.3333):
        raise StyxValidationError("Parameter `z_x_shear` must be between -0.3333 and 0.3333 (inclusive)")
    if params.get("z_y_shear", None) is None:
        raise StyxValidationError("`z_y_shear` must not be None")
    if not isinstance(params["z_y_shear"], (float, int)):
        raise StyxValidationError(f'`z_y_shear` has the wrong type: Received `{type(params["z_y_shear"])}` expected `float`')
    if not (-0.3333 <= params["z_y_shear"] <= 0.3333):
        raise StyxValidationError("Parameter `z_y_shear` must be between -0.3333 and 0.3333 (inclusive)")

def v_1d_apar2mat_cargs(params: V1dApar2mat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("1dApar2mat")
    cargs.append(str(params["x_shift"]))
    cargs.append(str(params["y_shift"]))
    cargs.append(str(params["z_shift"]))
    cargs.append(str(params["z_angle"]))
    cargs.append(str(params["x_angle"]))
    cargs.append(str(params["y_angle"]))
    cargs.append(str(params["x_scale"]))
    cargs.append(str(params["y_scale"]))
    cargs.append(str(params["z_scale"]))
    cargs.append(str(params["y_x_shear"]))
    cargs.append(str(params["z_x_shear"]))
    cargs.append(str(params["z_y_shear"]))
    return cargs

def v_1d_apar2mat_outputs(params: V1dApar2mat, execution: Execution) -> V1dApar2matOutputs:
    """Build the V1dApar2matOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V1dApar2matOutputs(
        root=root_v,
    )

def v_1d_apar2mat_execute(params: V1dApar2mat, runner: Runner | None = None) -> V1dApar2matOutputs:
    """
    1dApar2mat

    Computes the affine transformation matrix from the set of 3dAllineate parameters.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_1d_apar2mat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_1D_APAR2MAT_METADATA)
    execution.params(params)
    args = v_1d_apar2mat_cargs(params, execution)
    out = v_1d_apar2mat_outputs(params, execution)
    execution.run(args)
    return out

def v_1d_apar2mat(
    x_shift: float,
    y_shift: float,
    z_shift: float,
    z_angle: float,
    x_angle: float,
    y_angle: float,
    x_scale: float,
    y_scale: float,
    z_scale: float,
    y_x_shear: float,
    z_x_shear: float,
    z_y_shear: float,
    runner: Runner | None = None,
) -> V1dApar2matOutputs:
    """
    1dApar2mat

    Computes the affine transformation matrix from the set of 3dAllineate parameters.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        x_shift: x-shift in mm
        y_shift: y-shift in mm
        z_shift: z-shift in mm
        z_angle: z-angle (roll) in degrees
        x_angle: x-angle (pitch) in degrees
        y_angle: y-angle (yaw) in degrees
        x_scale: x-scale factor in [0.10,10.0]
        y_scale: y-scale factor in [0.10,10.0]
        z_scale: z-scale factor in [0.10,10.0]
        y_x_shear: y/x-shear factor in [-0.3333,0.3333]
        z_x_shear: z/x-shear factor in [-0.3333,0.3333]
        z_y_shear: z/y-shear factor in [-0.3333,0.3333]
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_1d_apar2mat_params(
        x_shift=x_shift,
        y_shift=y_shift,
        z_shift=z_shift,
        z_angle=z_angle,
        x_angle=x_angle,
        y_angle=y_angle,
        x_scale=x_scale,
        y_scale=y_scale,
        z_scale=z_scale,
        y_x_shear=y_x_shear,
        z_x_shear=z_x_shear,
        z_y_shear=z_y_shear,
    )
    return v_1d_apar2mat_execute(params, runner)

__all__ = [
    "V1dApar2mat",
    "V1dApar2matOutputs",
    "V_1D_APAR2MAT_METADATA",
    "v_1d_apar2mat_cargs",
    "v_1d_apar2mat_outputs",
    "v_1d_apar2mat_params",
    "v_1d_apar2mat_execute",
    "v_1d_apar2mat_validate",
    "v_1d_apar2mat",
]