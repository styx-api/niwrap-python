# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_RO_ISTATS_METADATA = Metadata(
    id="3dROIstats",
    name="3dROIstats",
    package="afni",
)

V3dRoIstats = typing.TypedDict(
    "V3dRoIstats",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dROIstats"]],
        "in_file": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "debug": typing.NotRequired[bool],
        "format1D": typing.NotRequired[bool],
        "format1DR": typing.NotRequired[bool],
        "mask_f2short": typing.NotRequired[bool],
        "mask_file": typing.NotRequired[InputPathType],
        "nobriklab": typing.NotRequired[bool],
        "nomeanout": typing.NotRequired[bool],
        "num_roi": typing.NotRequired[int],
        "quiet": typing.NotRequired[bool],
        "roisel": typing.NotRequired[InputPathType],
        "stat": typing.NotRequired[list[InputPathType]],
        "zerofill": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dRoIstatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stats: list[str]
    """There will be one line of output for every sub-brick of every input dataset. Across each line will be every statistic for every mask value. For instance, if there 3 mask values (1,2,3), then the columns Mean_1, Mean_2 and Mean_3 will refer to the means across each mask value, respectively. If 4 statistics are requested, then there will be 12 stats displayed on each line (4 for each mask region), besides the file and sub-brick number."""

def v_3d_ro_istats_params(
    in_file: InputPathType,
    mask: InputPathType | None = None,
    debug: bool = False,
    format1D: bool = False,
    format1DR: bool = False,
    mask_f2short: bool = False,
    mask_file: InputPathType | None = None,
    nobriklab: bool = False,
    nomeanout: bool = False,
    num_roi: int | None = None,
    quiet: bool = False,
    roisel: InputPathType | None = None,
    stat: list[InputPathType] | None = None,
    zerofill: str | None = None,
) -> V3dRoIstats:
    """
    Build parameters.

    Args:
        in_file: Input dataset.
        mask: Input mask.
        debug: Print debug information.
        format1D: Output results in a 1d format that includes commented labels.
        format1DR: Output results in a 1d format that includes uncommented labels.\
            may not work optimally with typical 1d functions, but is useful for r\
            functions.
        mask_f2short: Tells the program to convert a float mask to short integers,\
            by simple rounding.
        mask_file: Input mask.
        nobriklab: Do not print the sub-brick label next to its index.
        nomeanout: Do not include the (zero-inclusive) mean among computed stats.
        num_roi: Forces the assumption that the mask dataset's rois are denoted by\
            1 to n inclusive. normally, the program figures out the rois on its\
            own. this option is useful if a) you are certain that the mask dataset\
            has no values outside the range [0 n], b) there may be some rois\
            missing between [1 n] in the mask data-set and c) you want those\
            columns in the output any-way so the output lines up with the output\
            from other invocations of 3droistats.
        quiet: Execute quietly.
        roisel: Only considers rois denoted by values found in the specified file.\
            note that the order of the rois as specified in the file is not\
            preserved. so an sel.1d of '2 8 20' produces the same output as '8 20\
            2'.
        stat: A list of items which are 'mean' or 'sum' or 'voxels' or 'minmax' or\
            'sigma' or 'median' or 'mode' or 'summary' or 'zerominmax' or\
            'zerosigma' or 'zeromedian' or 'zeromode'. Statistics to compute.\
            options include: * mean = compute the mean using only non_zero voxels.\
            implies the opposite for the mean computed by default. * median =\
            compute the median of nonzero voxels * mode = compute the mode of\
            nonzero voxels. (integral valued sets only) * minmax = compute the\
            min/max of nonzero voxels * sum = compute the sum using only nonzero\
            voxels. * voxels = compute the number of nonzero voxels * sigma =\
            compute the standard deviation of nonzero voxelsstatistics that include\
            zero-valued voxels: * zerominmax = compute the min/max of all voxels. *\
            zerosigma = compute the standard deviation of all voxels. * zeromedian\
            = compute the median of all voxels. * zeromode = compute the mode of\
            all voxels. * summary = only output a summary line with the grand mean\
            across all briks in the input dataset. this option cannot be used with\
            nomeanout.more that one option can be specified.
        zerofill: For roi labels not found, use the provided string instead of a\
            '0' in the output file. only active if `num_roi` is enabled.

    Returns:
        Parameter dictionary.
    """
    params: V3dRoIstats = {
        "@type": "afni/3dROIstats",
        "in_file": in_file,
        "debug": debug,
        "format1D": format1D,
        "format1DR": format1DR,
        "mask_f2short": mask_f2short,
        "nobriklab": nobriklab,
        "nomeanout": nomeanout,
        "quiet": quiet,
    }
    if mask is not None:
        params["mask"] = mask
    if mask_file is not None:
        params["mask_file"] = mask_file
    if num_roi is not None:
        params["num_roi"] = num_roi
    if roisel is not None:
        params["roisel"] = roisel
    if stat is not None:
        params["stat"] = stat
    if zerofill is not None:
        params["zerofill"] = zerofill
    return params

def v_3d_ro_istats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dRoIstats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_file", None) is None:
        raise StyxValidationError("`in_file` must not be None")
    if not isinstance(params["in_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_file` has the wrong type: Received `{type(params["in_file"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("format1D", None) is not None:
        if not isinstance(params["format1D"], bool):
            raise StyxValidationError(f'`format1D` has the wrong type: Received `{type(params["format1D"])}` expected `bool`')
    if params.get("format1DR", None) is not None:
        if not isinstance(params["format1DR"], bool):
            raise StyxValidationError(f'`format1DR` has the wrong type: Received `{type(params["format1DR"])}` expected `bool`')
    if params.get("mask_f2short", None) is not None:
        if not isinstance(params["mask_f2short"], bool):
            raise StyxValidationError(f'`mask_f2short` has the wrong type: Received `{type(params["mask_f2short"])}` expected `bool`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("nobriklab", None) is not None:
        if not isinstance(params["nobriklab"], bool):
            raise StyxValidationError(f'`nobriklab` has the wrong type: Received `{type(params["nobriklab"])}` expected `bool`')
    if params.get("nomeanout", None) is not None:
        if not isinstance(params["nomeanout"], bool):
            raise StyxValidationError(f'`nomeanout` has the wrong type: Received `{type(params["nomeanout"])}` expected `bool`')
    if params.get("num_roi", None) is not None:
        if not isinstance(params["num_roi"], int):
            raise StyxValidationError(f'`num_roi` has the wrong type: Received `{type(params["num_roi"])}` expected `int`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("roisel", None) is not None:
        if not isinstance(params["roisel"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roisel` has the wrong type: Received `{type(params["roisel"])}` expected `InputPathType`')
    if params.get("stat", None) is not None:
        if not isinstance(params["stat"], list):
            raise StyxValidationError(f'`stat` has the wrong type: Received `{type(params["stat"])}` expected `list[InputPathType]`')
        for e in params["stat"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`stat` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("zerofill", None) is not None:
        if not isinstance(params["zerofill"], str):
            raise StyxValidationError(f'`zerofill` has the wrong type: Received `{type(params["zerofill"])}` expected `str`')

def v_3d_ro_istats_cargs(params: V3dRoIstats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dROIstats")
    cargs.append(execution.input_file(params["in_file"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("format1D"):
        cargs.append("-1Dformat")
    if params.get("format1DR"):
        cargs.append("-1DRformat")
    if params.get("mask_f2short"):
        cargs.append("-mask_f2short")
    v_1 = params.get("mask_file")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    if params.get("nobriklab"):
        cargs.append("-nobriklab")
    if params.get("nomeanout"):
        cargs.append("-nomeanout")
    v_2 = params.get("num_roi")
    if v_2 is not None:
        cargs.append("-numroi")
        cargs.append(str(v_2))
    if params.get("quiet"):
        cargs.append("-quiet")
    v_3 = params.get("roisel")
    if v_3 is not None:
        cargs.append("-roisel")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("stat")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append(execution.input_file(item0))
    v_5 = params.get("zerofill")
    if v_5 is not None:
        cargs.append("-zerofill")
        cargs.append(v_5)
    return cargs

def v_3d_ro_istats_outputs(params: V3dRoIstats, execution: Execution) -> V3dRoIstatsOutputs:
    """Build the V3dRoIstatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dRoIstatsOutputs(
        root=root_v,
        stats=[],
    )

def v_3d_ro_istats_execute(params: V3dRoIstats, runner: Runner | None = None) -> V3dRoIstatsOutputs:
    """
    3dROIstats

    Display statistics over masked regions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_ro_istats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_RO_ISTATS_METADATA)
    execution.params(params)
    args = v_3d_ro_istats_cargs(params, execution)
    out = v_3d_ro_istats_outputs(params, execution)
    execution.run(args, handle_stdout=lambda s: out.stats.append(s))
    return out

def v_3d_ro_istats(
    in_file: InputPathType,
    mask: InputPathType | None = None,
    debug: bool = False,
    format1D: bool = False,
    format1DR: bool = False,
    mask_f2short: bool = False,
    mask_file: InputPathType | None = None,
    nobriklab: bool = False,
    nomeanout: bool = False,
    num_roi: int | None = None,
    quiet: bool = False,
    roisel: InputPathType | None = None,
    stat: list[InputPathType] | None = None,
    zerofill: str | None = None,
    runner: Runner | None = None,
) -> V3dRoIstatsOutputs:
    """
    3dROIstats

    Display statistics over masked regions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        in_file: Input dataset.
        mask: Input mask.
        debug: Print debug information.
        format1D: Output results in a 1d format that includes commented labels.
        format1DR: Output results in a 1d format that includes uncommented labels.\
            may not work optimally with typical 1d functions, but is useful for r\
            functions.
        mask_f2short: Tells the program to convert a float mask to short integers,\
            by simple rounding.
        mask_file: Input mask.
        nobriklab: Do not print the sub-brick label next to its index.
        nomeanout: Do not include the (zero-inclusive) mean among computed stats.
        num_roi: Forces the assumption that the mask dataset's rois are denoted by\
            1 to n inclusive. normally, the program figures out the rois on its\
            own. this option is useful if a) you are certain that the mask dataset\
            has no values outside the range [0 n], b) there may be some rois\
            missing between [1 n] in the mask data-set and c) you want those\
            columns in the output any-way so the output lines up with the output\
            from other invocations of 3droistats.
        quiet: Execute quietly.
        roisel: Only considers rois denoted by values found in the specified file.\
            note that the order of the rois as specified in the file is not\
            preserved. so an sel.1d of '2 8 20' produces the same output as '8 20\
            2'.
        stat: A list of items which are 'mean' or 'sum' or 'voxels' or 'minmax' or\
            'sigma' or 'median' or 'mode' or 'summary' or 'zerominmax' or\
            'zerosigma' or 'zeromedian' or 'zeromode'. Statistics to compute.\
            options include: * mean = compute the mean using only non_zero voxels.\
            implies the opposite for the mean computed by default. * median =\
            compute the median of nonzero voxels * mode = compute the mode of\
            nonzero voxels. (integral valued sets only) * minmax = compute the\
            min/max of nonzero voxels * sum = compute the sum using only nonzero\
            voxels. * voxels = compute the number of nonzero voxels * sigma =\
            compute the standard deviation of nonzero voxelsstatistics that include\
            zero-valued voxels: * zerominmax = compute the min/max of all voxels. *\
            zerosigma = compute the standard deviation of all voxels. * zeromedian\
            = compute the median of all voxels. * zeromode = compute the mode of\
            all voxels. * summary = only output a summary line with the grand mean\
            across all briks in the input dataset. this option cannot be used with\
            nomeanout.more that one option can be specified.
        zerofill: For roi labels not found, use the provided string instead of a\
            '0' in the output file. only active if `num_roi` is enabled.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_ro_istats_params(
        in_file=in_file,
        mask=mask,
        debug=debug,
        format1D=format1D,
        format1DR=format1DR,
        mask_f2short=mask_f2short,
        mask_file=mask_file,
        nobriklab=nobriklab,
        nomeanout=nomeanout,
        num_roi=num_roi,
        quiet=quiet,
        roisel=roisel,
        stat=stat,
        zerofill=zerofill,
    )
    return v_3d_ro_istats_execute(params, runner)

__all__ = [
    "V3dRoIstats",
    "V3dRoIstatsOutputs",
    "V_3D_RO_ISTATS_METADATA",
    "v_3d_ro_istats_cargs",
    "v_3d_ro_istats_outputs",
    "v_3d_ro_istats_params",
    "v_3d_ro_istats_execute",
    "v_3d_ro_istats_validate",
    "v_3d_ro_istats",
]