# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SUMA_REPREFIXIZE_SPEC_METADATA = Metadata(
    id="@suma_reprefixize_spec",
    name="@suma_reprefixize_spec",
    package="afni",
)

SumaReprefixizeSpec = typing.TypedDict(
    "SumaReprefixizeSpec",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@suma_reprefixize_spec"]],
        "input_file": InputPathType,
        "prefix": str,
        "output_dir": str,
        "work_dir": str,
        "no_clean": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SumaReprefixizeSpecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def suma_reprefixize_spec_params(
    input_file: InputPathType,
    prefix: str,
    output_dir: str,
    work_dir: str,
    no_clean: bool = False,
) -> SumaReprefixizeSpec:
    """
    Build parameters.

    Args:
        input_file: Input SUMA specification file
        prefix: Prefix to be added to the file names
        output_dir: Output directory where the prefixed files will be saved
        work_dir: Working directory for temporary files
        no_clean: Flag to avoid cleaning temporary files

    Returns:
        Parameter dictionary.
    """
    params: SumaReprefixizeSpec = {
        "@type": "afni/@suma_reprefixize_spec",
        "input_file": input_file,
        "prefix": prefix,
        "output_dir": output_dir,
        "work_dir": work_dir,
        "no_clean": no_clean,
    }
    return params

def suma_reprefixize_spec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SumaReprefixizeSpec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("work_dir", None) is None:
        raise StyxValidationError("`work_dir` must not be None")
    if not isinstance(params["work_dir"], str):
        raise StyxValidationError(f'`work_dir` has the wrong type: Received `{type(params["work_dir"])}` expected `str`')
    if params.get("no_clean", None) is not None:
        if not isinstance(params["no_clean"], bool):
            raise StyxValidationError(f'`no_clean` has the wrong type: Received `{type(params["no_clean"])}` expected `bool`')

def suma_reprefixize_spec_cargs(params: SumaReprefixizeSpec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@suma_reprefixize_spec")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-preprefix")
    cargs.append(params["prefix"])
    cargs.append("-odir")
    cargs.append(params["output_dir"])
    cargs.append("-workdir")
    cargs.append(params["work_dir"])
    if params.get("no_clean"):
        cargs.append("-no_clean")
    return cargs

def suma_reprefixize_spec_outputs(params: SumaReprefixizeSpec, execution: Execution) -> SumaReprefixizeSpecOutputs:
    """Build the SumaReprefixizeSpecOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SumaReprefixizeSpecOutputs(
        root=root_v,
    )

def suma_reprefixize_spec_execute(params: SumaReprefixizeSpec, runner: Runner | None = None) -> SumaReprefixizeSpecOutputs:
    """
    @suma_reprefixize_spec

    A tool for prefixing and working with SUMA specification files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    suma_reprefixize_spec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SUMA_REPREFIXIZE_SPEC_METADATA)
    execution.params(params)
    args = suma_reprefixize_spec_cargs(params, execution)
    out = suma_reprefixize_spec_outputs(params, execution)
    execution.run(args)
    return out

def suma_reprefixize_spec(
    input_file: InputPathType,
    prefix: str,
    output_dir: str,
    work_dir: str,
    no_clean: bool = False,
    runner: Runner | None = None,
) -> SumaReprefixizeSpecOutputs:
    """
    @suma_reprefixize_spec

    A tool for prefixing and working with SUMA specification files.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_file: Input SUMA specification file
        prefix: Prefix to be added to the file names
        output_dir: Output directory where the prefixed files will be saved
        work_dir: Working directory for temporary files
        no_clean: Flag to avoid cleaning temporary files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = suma_reprefixize_spec_params(
        input_file=input_file,
        prefix=prefix,
        output_dir=output_dir,
        work_dir=work_dir,
        no_clean=no_clean,
    )
    return suma_reprefixize_spec_execute(params, runner)

__all__ = [
    "SumaReprefixizeSpec",
    "SumaReprefixizeSpecOutputs",
    "SUMA_REPREFIXIZE_SPEC_METADATA",
    "suma_reprefixize_spec_cargs",
    "suma_reprefixize_spec_outputs",
    "suma_reprefixize_spec_params",
    "suma_reprefixize_spec_execute",
    "suma_reprefixize_spec_validate",
    "suma_reprefixize_spec",
]