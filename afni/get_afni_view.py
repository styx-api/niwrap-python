# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GET_AFNI_VIEW_METADATA = Metadata(
    id="@GetAfniView",
    name="@GetAfniView",
    package="afni",
)

GetAfniView = typing.TypedDict(
    "GetAfniView",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@GetAfniView"]],
        "dataset_name": str,
    },
)

@dataclasses.dataclass
class GetAfniViewOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    afni_view: OutputPathType
    """The AFNI view extension retrieved from the dataset name"""

def get_afni_view_params(
    dataset_name: str,
) -> GetAfniView:
    """
    Build parameters.

    Args:
        dataset_name: Name of the dataset (including path) from which to retrieve\
            the AFNI view (+orig, +acpc, etc.)

    Returns:
        Parameter dictionary.
    """
    params: GetAfniView = {
        "@type": "afni/@GetAfniView",
        "dataset_name": dataset_name,
    }
    return params

def get_afni_view_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid GetAfniView."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset_name", None) is None:
        raise StyxValidationError("`dataset_name` must not be None")
    if not isinstance(params["dataset_name"], str):
        raise StyxValidationError(f'`dataset_name` has the wrong type: Received `{type(params["dataset_name"])}` expected `str`')

def get_afni_view_cargs(params: GetAfniView, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@GetAfniView")
    cargs.append(params["dataset_name"])
    return cargs

def get_afni_view_outputs(params: GetAfniView, execution: Execution) -> GetAfniViewOutputs:
    """Build the GetAfniViewOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    afni_view_v: OutputPathType = execution.output_file("view_extension.txt")
    return GetAfniViewOutputs(
        root=root_v,
        afni_view=afni_view_v,
    )

def get_afni_view_execute(params: GetAfniView, runner: Runner | None = None) -> GetAfniViewOutputs:
    """
    @GetAfniView

    A tool to retrieve the AFNI view of a given dataset name

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    get_afni_view_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GET_AFNI_VIEW_METADATA)
    execution.params(params)
    args = get_afni_view_cargs(params, execution)
    out = get_afni_view_outputs(params, execution)
    execution.run(args)
    return out

def get_afni_view(
    dataset_name: str,
    runner: Runner | None = None,
) -> GetAfniViewOutputs:
    """
    @GetAfniView

    A tool to retrieve the AFNI view of a given dataset name

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset_name: Name of the dataset (including path) from which to retrieve\
            the AFNI view (+orig, +acpc, etc.)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = get_afni_view_params(
        dataset_name=dataset_name,
    )
    return get_afni_view_execute(params, runner)

__all__ = [
    "GetAfniView",
    "GetAfniViewOutputs",
    "GET_AFNI_VIEW_METADATA",
    "get_afni_view_cargs",
    "get_afni_view_outputs",
    "get_afni_view_params",
    "get_afni_view_execute",
    "get_afni_view_validate",
    "get_afni_view",
]