# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SCRIPT_CHECK_METADATA = Metadata(
    id="@ScriptCheck",
    name="@ScriptCheck",
    package="afni",
)

ScriptCheck = typing.TypedDict(
    "ScriptCheck",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ScriptCheck"]],
        "clean": typing.NotRequired[bool],
        "suffix": typing.NotRequired[str],
        "scripts": list[InputPathType],
    },
)

@dataclasses.dataclass
class ScriptCheckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    uncleaned_file: OutputPathType
    """Uncleaned original file with specified suffix"""
    cleaned_file: OutputPathType
    """Cleaned file if -clean option is used"""

def script_check_params(
    scripts: list[InputPathType],
    clean: bool = False,
    suffix: str | None = None,
) -> ScriptCheck:
    """
    Build parameters.

    Args:
        scripts: Scripts to be checked for improperly terminated lines
        clean: Clean bad line breaks
        suffix: Rename uncleaned file with specified suffix. Default is .uncln

    Returns:
        Parameter dictionary.
    """
    params: ScriptCheck = {
        "@type": "afni/@ScriptCheck",
        "scripts": scripts,
        "clean": clean,
    }
    if suffix is not None:
        params["suffix"] = suffix
    return params

def script_check_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ScriptCheck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("clean", None) is not None:
        if not isinstance(params["clean"], bool):
            raise StyxValidationError(f'`clean` has the wrong type: Received `{type(params["clean"])}` expected `bool`')
    if params.get("suffix", None) is not None:
        if not isinstance(params["suffix"], str):
            raise StyxValidationError(f'`suffix` has the wrong type: Received `{type(params["suffix"])}` expected `str`')
    if params.get("scripts", None) is None:
        raise StyxValidationError("`scripts` must not be None")
    if not isinstance(params["scripts"], list):
        raise StyxValidationError(f'`scripts` has the wrong type: Received `{type(params["scripts"])}` expected `list[InputPathType]`')
    for e in params["scripts"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`scripts` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def script_check_cargs(params: ScriptCheck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ScriptCheck")
    if params.get("clean"):
        cargs.append("-clean")
    v_0 = params.get("suffix")
    if v_0 is not None:
        cargs.append("-suffix")
        cargs.append(v_0)
    for item0 in params["scripts"]:
        cargs.append(execution.input_file(item0))
    return cargs

def script_check_outputs(params: ScriptCheck, execution: Execution) -> ScriptCheckOutputs:
    """Build the ScriptCheckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    uncleaned_file_v: OutputPathType = execution.output_file("{SCRIPT}.uncln")
    cleaned_file_v: OutputPathType = execution.output_file("{SCRIPT}")
    return ScriptCheckOutputs(
        root=root_v,
        uncleaned_file=uncleaned_file_v,
        cleaned_file=cleaned_file_v,
    )

def script_check_execute(params: ScriptCheck, runner: Runner | None = None) -> ScriptCheckOutputs:
    """
    @ScriptCheck

    Checks scripts for improperly terminated lines and optionally cleans them.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    script_check_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SCRIPT_CHECK_METADATA)
    execution.params(params)
    args = script_check_cargs(params, execution)
    out = script_check_outputs(params, execution)
    execution.run(args)
    return out

def script_check(
    scripts: list[InputPathType],
    clean: bool = False,
    suffix: str | None = None,
    runner: Runner | None = None,
) -> ScriptCheckOutputs:
    """
    @ScriptCheck

    Checks scripts for improperly terminated lines and optionally cleans them.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        scripts: Scripts to be checked for improperly terminated lines
        clean: Clean bad line breaks
        suffix: Rename uncleaned file with specified suffix. Default is .uncln
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = script_check_params(
        scripts=scripts,
        clean=clean,
        suffix=suffix,
    )
    return script_check_execute(params, runner)

__all__ = [
    "ScriptCheck",
    "ScriptCheckOutputs",
    "SCRIPT_CHECK_METADATA",
    "script_check_cargs",
    "script_check_outputs",
    "script_check_params",
    "script_check_execute",
    "script_check_validate",
    "script_check",
]