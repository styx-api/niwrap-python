# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAP_TRACK_ID_METADATA = Metadata(
    id="map_TrackID",
    name="map_TrackID",
    package="afni",
)

MapTrackId = typing.TypedDict(
    "MapTrackId",
    {
        "@type": typing.NotRequired[typing.Literal["afni/map_TrackID"]],
        "prefix": str,
        "in_trk": InputPathType,
        "in_map": InputPathType,
        "reference": InputPathType,
        "verbose": typing.NotRequired[bool],
        "orig_zero": typing.NotRequired[bool],
        "line_only_num": typing.NotRequired[bool],
        "already_inv": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MapTrackIdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_trk_file: OutputPathType
    """Mapped track file to new space"""

def map_track_id_params(
    prefix: str,
    in_trk: InputPathType,
    in_map: InputPathType,
    reference: InputPathType,
    verbose: bool = False,
    orig_zero: bool = False,
    line_only_num: bool = False,
    already_inv: bool = False,
) -> MapTrackId:
    """
    Build parameters.

    Args:
        prefix: Prefix for the output track file.
        in_trk: The name of the *.trk file to be mapped.
        in_map: Single line of matrix values for the transformation.
        reference: 3D data set in the space to which the TRK file is being mapped.
        verbose: Verbose output.
        orig_zero: Put (0,0,0) as the origin in the output *.trk file.
        line_only_num: If your 1D_MATR file is just 12 numbers in a row.
        already_inv: If you have inverted the mapping or use another program than\
            3dAllineate.

    Returns:
        Parameter dictionary.
    """
    params: MapTrackId = {
        "@type": "afni/map_TrackID",
        "prefix": prefix,
        "in_trk": in_trk,
        "in_map": in_map,
        "reference": reference,
        "verbose": verbose,
        "orig_zero": orig_zero,
        "line_only_num": line_only_num,
        "already_inv": already_inv,
    }
    return params

def map_track_id_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MapTrackId."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("in_trk", None) is None:
        raise StyxValidationError("`in_trk` must not be None")
    if not isinstance(params["in_trk"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_trk` has the wrong type: Received `{type(params["in_trk"])}` expected `InputPathType`')
    if params.get("in_map", None) is None:
        raise StyxValidationError("`in_map` must not be None")
    if not isinstance(params["in_map"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_map` has the wrong type: Received `{type(params["in_map"])}` expected `InputPathType`')
    if params.get("reference", None) is None:
        raise StyxValidationError("`reference` must not be None")
    if not isinstance(params["reference"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference` has the wrong type: Received `{type(params["reference"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("orig_zero", None) is not None:
        if not isinstance(params["orig_zero"], bool):
            raise StyxValidationError(f'`orig_zero` has the wrong type: Received `{type(params["orig_zero"])}` expected `bool`')
    if params.get("line_only_num", None) is not None:
        if not isinstance(params["line_only_num"], bool):
            raise StyxValidationError(f'`line_only_num` has the wrong type: Received `{type(params["line_only_num"])}` expected `bool`')
    if params.get("already_inv", None) is not None:
        if not isinstance(params["already_inv"], bool):
            raise StyxValidationError(f'`already_inv` has the wrong type: Received `{type(params["already_inv"])}` expected `bool`')

def map_track_id_cargs(params: MapTrackId, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("map_TrackID")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-in_trk")
    cargs.append(execution.input_file(params["in_trk"]))
    cargs.append("-in_map")
    cargs.append(execution.input_file(params["in_map"]))
    cargs.append("-ref")
    cargs.append(execution.input_file(params["reference"]))
    if params.get("verbose"):
        cargs.append("-verb")
    if params.get("orig_zero"):
        cargs.append("-orig_zero")
    if params.get("line_only_num"):
        cargs.append("-line_only_num")
    if params.get("already_inv"):
        cargs.append("-already_inv")
    return cargs

def map_track_id_outputs(params: MapTrackId, execution: Execution) -> MapTrackIdOutputs:
    """Build the MapTrackIdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_trk_file_v: OutputPathType = execution.output_file(f'{params["prefix"]}.trk')
    return MapTrackIdOutputs(
        root=root_v,
        output_trk_file=output_trk_file_v,
    )

def map_track_id_execute(params: MapTrackId, runner: Runner | None = None) -> MapTrackIdOutputs:
    """
    map_TrackID

    Maps the track file (*.trk) output of 3dTrackID to another space using the 1Dmatrix_save info of 3dAllineate.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    map_track_id_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAP_TRACK_ID_METADATA)
    execution.params(params)
    args = map_track_id_cargs(params, execution)
    out = map_track_id_outputs(params, execution)
    execution.run(args)
    return out

def map_track_id(
    prefix: str,
    in_trk: InputPathType,
    in_map: InputPathType,
    reference: InputPathType,
    verbose: bool = False,
    orig_zero: bool = False,
    line_only_num: bool = False,
    already_inv: bool = False,
    runner: Runner | None = None,
) -> MapTrackIdOutputs:
    """
    map_TrackID

    Maps the track file (*.trk) output of 3dTrackID to another space using the 1Dmatrix_save info of 3dAllineate.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Prefix for the output track file.
        in_trk: The name of the *.trk file to be mapped.
        in_map: Single line of matrix values for the transformation.
        reference: 3D data set in the space to which the TRK file is being mapped.
        verbose: Verbose output.
        orig_zero: Put (0,0,0) as the origin in the output *.trk file.
        line_only_num: If your 1D_MATR file is just 12 numbers in a row.
        already_inv: If you have inverted the mapping or use another program than\
            3dAllineate.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = map_track_id_params(
        prefix=prefix,
        in_trk=in_trk,
        in_map=in_map,
        reference=reference,
        verbose=verbose,
        orig_zero=orig_zero,
        line_only_num=line_only_num,
        already_inv=already_inv,
    )
    return map_track_id_execute(params, runner)

__all__ = [
    "MapTrackId",
    "MapTrackIdOutputs",
    "MAP_TRACK_ID_METADATA",
    "map_track_id_cargs",
    "map_track_id_outputs",
    "map_track_id_params",
    "map_track_id_execute",
    "map_track_id_validate",
    "map_track_id",
]