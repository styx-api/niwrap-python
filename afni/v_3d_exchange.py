# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_EXCHANGE_METADATA = Metadata(
    id="3dExchange",
    name="3dExchange",
    package="afni",
)

V3dExchange = typing.TypedDict(
    "V3dExchange",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dExchange"]],
        "prefix": str,
        "infile": InputPathType,
        "mapfile": InputPathType,
        "version": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dExchangeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_head: OutputPathType
    """Output HEAD file"""
    output_brik: OutputPathType
    """Output BRIK file"""

def v_3d_exchange_params(
    prefix: str,
    infile: InputPathType,
    mapfile: InputPathType,
    version: bool = False,
    help: bool = False,
) -> V3dExchange:
    """
    Build parameters.

    Args:
        prefix: Output prefix
        infile: Input dataset. Acceptable data types are byte, short, and floats.
        mapfile: Mapping columns. Input values in the first column, output values\
            in the second column
        version: Print author and version info
        help: Print this help screen

    Returns:
        Parameter dictionary.
    """
    params: V3dExchange = {
        "@type": "afni/3dExchange",
        "prefix": prefix,
        "infile": infile,
        "mapfile": mapfile,
        "version": version,
        "help": help,
    }
    return params

def v_3d_exchange_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dExchange."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("mapfile", None) is None:
        raise StyxValidationError("`mapfile` must not be None")
    if not isinstance(params["mapfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mapfile` has the wrong type: Received `{type(params["mapfile"])}` expected `InputPathType`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def v_3d_exchange_cargs(params: V3dExchange, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dExchange")
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    cargs.append("-input")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-map")
    cargs.append(execution.input_file(params["mapfile"]))
    if params.get("version"):
        cargs.append("-ver")
    if params.get("help"):
        cargs.append("-help")
    return cargs

def v_3d_exchange_outputs(params: V3dExchange, execution: Execution) -> V3dExchangeOutputs:
    """Build the V3dExchangeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_head_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.HEAD')
    output_brik_v: OutputPathType = execution.output_file(f'{params["prefix"]}+orig.BRIK')
    return V3dExchangeOutputs(
        root=root_v,
        output_head=output_head_v,
        output_brik=output_brik_v,
    )

def v_3d_exchange_execute(params: V3dExchange, runner: Runner | None = None) -> V3dExchangeOutputs:
    """
    3dExchange

    Replaces voxel values using a mapping file with specified columns

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_exchange_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_EXCHANGE_METADATA)
    execution.params(params)
    args = v_3d_exchange_cargs(params, execution)
    out = v_3d_exchange_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_exchange(
    prefix: str,
    infile: InputPathType,
    mapfile: InputPathType,
    version: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> V3dExchangeOutputs:
    """
    3dExchange

    Replaces voxel values using a mapping file with specified columns

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        prefix: Output prefix
        infile: Input dataset. Acceptable data types are byte, short, and floats.
        mapfile: Mapping columns. Input values in the first column, output values\
            in the second column
        version: Print author and version info
        help: Print this help screen
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_exchange_params(
        prefix=prefix,
        infile=infile,
        mapfile=mapfile,
        version=version,
        help=help,
    )
    return v_3d_exchange_execute(params, runner)

__all__ = [
    "V3dExchange",
    "V3dExchangeOutputs",
    "V_3D_EXCHANGE_METADATA",
    "v_3d_exchange_cargs",
    "v_3d_exchange_outputs",
    "v_3d_exchange_params",
    "v_3d_exchange_execute",
    "v_3d_exchange_validate",
    "v_3d_exchange",
]