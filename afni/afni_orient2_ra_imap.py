# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AFNI_ORIENT2_RA_IMAP_METADATA = Metadata(
    id="@AfniOrient2RAImap",
    name="@AfniOrient2RAImap",
    package="afni",
)

AfniOrient2RaImap = typing.TypedDict(
    "AfniOrient2RaImap",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@AfniOrient2RAImap"]],
        "orientation_code": str,
    },
)

@dataclasses.dataclass
class AfniOrient2RaImapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def afni_orient2_ra_imap_params(
    orientation_code: str,
) -> AfniOrient2RaImap:
    """
    Build parameters.

    Args:
        orientation_code: Orientation code (e.g., RAI, LSP)

    Returns:
        Parameter dictionary.
    """
    params: AfniOrient2RaImap = {
        "@type": "afni/@AfniOrient2RAImap",
        "orientation_code": orientation_code,
    }
    return params

def afni_orient2_ra_imap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AfniOrient2RaImap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("orientation_code", None) is None:
        raise StyxValidationError("`orientation_code` must not be None")
    if not isinstance(params["orientation_code"], str):
        raise StyxValidationError(f'`orientation_code` has the wrong type: Received `{type(params["orientation_code"])}` expected `str`')

def afni_orient2_ra_imap_cargs(params: AfniOrient2RaImap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@AfniOrient2RAImap")
    cargs.append(params["orientation_code"])
    return cargs

def afni_orient2_ra_imap_outputs(params: AfniOrient2RaImap, execution: Execution) -> AfniOrient2RaImapOutputs:
    """Build the AfniOrient2RaImapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AfniOrient2RaImapOutputs(
        root=root_v,
    )

def afni_orient2_ra_imap_execute(params: AfniOrient2RaImap, runner: Runner | None = None) -> AfniOrient2RaImapOutputs:
    """
    @AfniOrient2RAImap

    Returns the index map for the RAI directions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    afni_orient2_ra_imap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AFNI_ORIENT2_RA_IMAP_METADATA)
    execution.params(params)
    args = afni_orient2_ra_imap_cargs(params, execution)
    out = afni_orient2_ra_imap_outputs(params, execution)
    execution.run(args)
    return out

def afni_orient2_ra_imap(
    orientation_code: str,
    runner: Runner | None = None,
) -> AfniOrient2RaImapOutputs:
    """
    @AfniOrient2RAImap

    Returns the index map for the RAI directions

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        orientation_code: Orientation code (e.g., RAI, LSP)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = afni_orient2_ra_imap_params(
        orientation_code=orientation_code,
    )
    return afni_orient2_ra_imap_execute(params, runner)

__all__ = [
    "AfniOrient2RaImap",
    "AfniOrient2RaImapOutputs",
    "AFNI_ORIENT2_RA_IMAP_METADATA",
    "afni_orient2_ra_imap_cargs",
    "afni_orient2_ra_imap_outputs",
    "afni_orient2_ra_imap_params",
    "afni_orient2_ra_imap_execute",
    "afni_orient2_ra_imap_validate",
    "afni_orient2_ra_imap",
]