# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RO_IGROW_METADATA = Metadata(
    id="ROIgrow",
    name="ROIgrow",
    package="afni",
)

RoIgrow = typing.TypedDict(
    "RoIgrow",
    {
        "@type": typing.NotRequired[typing.Literal["afni/ROIgrow"]],
        "input_surface": str,
        "roi_labels": str,
        "lim_distance": float,
        "output_prefix": typing.NotRequired[str],
        "full_list": typing.NotRequired[bool],
        "grow_from_edge": typing.NotRequired[bool],
        "insphere_diameter": typing.NotRequired[float],
        "inbox_edges": typing.NotRequired[list[float]],
    },
)

@dataclasses.dataclass
class RoIgrowOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """1D output dataset."""

def ro_igrow_params(
    input_surface: str,
    roi_labels: str,
    lim_distance: float,
    output_prefix: str | None = None,
    full_list: bool = False,
    grow_from_edge: bool = False,
    insphere_diameter: float | None = None,
    inbox_edges: list[float] | None = None,
) -> RoIgrow:
    """
    Build parameters.

    Args:
        input_surface: Specify input surface. You can also use -t* and -spec and\
            -surf methods to input surfaces.
        roi_labels: Data column containing integer labels of ROIs. Each integer\
            label gets grown separately.
        lim_distance: Distance to cover from each node. The units of LIM are those\
            of the surface's node coordinates. Distances are calculated along the\
            surface's mesh.
        output_prefix: Prefix of 1D output dataset. Default is ROIgrow
        full_list: Output a row for each node on the surface. Nodes not in the\
            grown ROI, receive a 0 for a label. This option is ONLY for use with\
            -roi_labels.
        grow_from_edge: Grow ROIs from their edges rather than the brute force\
            default. This might make the program faster on large ROIs and large\
            surfaces.
        insphere_diameter: Diameter of the sphere inside which nodes are added\
            instead of growing along the surface.
        inbox_edges: Use a box of edge widths E1, E2, E3 instead of DIA.

    Returns:
        Parameter dictionary.
    """
    params: RoIgrow = {
        "@type": "afni/ROIgrow",
        "input_surface": input_surface,
        "roi_labels": roi_labels,
        "lim_distance": lim_distance,
        "full_list": full_list,
        "grow_from_edge": grow_from_edge,
    }
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    if insphere_diameter is not None:
        params["insphere_diameter"] = insphere_diameter
    if inbox_edges is not None:
        params["inbox_edges"] = inbox_edges
    return params

def ro_igrow_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RoIgrow."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], str):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `str`')
    if params.get("roi_labels", None) is None:
        raise StyxValidationError("`roi_labels` must not be None")
    if not isinstance(params["roi_labels"], str):
        raise StyxValidationError(f'`roi_labels` has the wrong type: Received `{type(params["roi_labels"])}` expected `str`')
    if params.get("lim_distance", None) is None:
        raise StyxValidationError("`lim_distance` must not be None")
    if not isinstance(params["lim_distance"], (float, int)):
        raise StyxValidationError(f'`lim_distance` has the wrong type: Received `{type(params["lim_distance"])}` expected `float`')
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("full_list", None) is not None:
        if not isinstance(params["full_list"], bool):
            raise StyxValidationError(f'`full_list` has the wrong type: Received `{type(params["full_list"])}` expected `bool`')
    if params.get("grow_from_edge", None) is not None:
        if not isinstance(params["grow_from_edge"], bool):
            raise StyxValidationError(f'`grow_from_edge` has the wrong type: Received `{type(params["grow_from_edge"])}` expected `bool`')
    if params.get("insphere_diameter", None) is not None:
        if not isinstance(params["insphere_diameter"], (float, int)):
            raise StyxValidationError(f'`insphere_diameter` has the wrong type: Received `{type(params["insphere_diameter"])}` expected `float`')
    if params.get("inbox_edges", None) is not None:
        if not isinstance(params["inbox_edges"], list):
            raise StyxValidationError(f'`inbox_edges` has the wrong type: Received `{type(params["inbox_edges"])}` expected `list[float]`')
        if not (3 <= len(params["inbox_edges"]) <= 3):
            raise StyxValidationError("Parameter `inbox_edges` must contain between 3 and 3 elements (inclusive)")
        for e in params["inbox_edges"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`inbox_edges` has the wrong type: Received `{type(e)}` expected `list[float]`')

def ro_igrow_cargs(params: RoIgrow, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ROIgrow")
    cargs.append("-i_TYPE")
    cargs.append(params["input_surface"])
    cargs.append("-roi_labels")
    cargs.append(params["roi_labels"])
    cargs.append("-lim")
    cargs.append(str(params["lim_distance"]))
    v_0 = params.get("output_prefix")
    if v_0 is not None:
        cargs.append("-prefix")
        cargs.append(v_0)
    if params.get("full_list"):
        cargs.append("-full_list")
    if params.get("grow_from_edge"):
        cargs.append("-grow_from_edge")
    v_1 = params.get("insphere_diameter")
    if v_1 is not None:
        cargs.append("-insphere")
        cargs.append(str(v_1))
    v_2 = params.get("inbox_edges")
    if v_2 is not None:
        cargs.append("-inbox")
        for item0 in v_2:
            cargs.append(str(item0))
    return cargs

def ro_igrow_outputs(params: RoIgrow, execution: Execution) -> RoIgrowOutputs:
    """Build the RoIgrowOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_prefix")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.1D')
    return RoIgrowOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def ro_igrow_execute(params: RoIgrow, runner: Runner | None = None) -> RoIgrowOutputs:
    """
    ROIgrow

    A program to expand an ROI on the surface.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ro_igrow_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RO_IGROW_METADATA)
    execution.params(params)
    args = ro_igrow_cargs(params, execution)
    out = ro_igrow_outputs(params, execution)
    execution.run(args)
    return out

def ro_igrow(
    input_surface: str,
    roi_labels: str,
    lim_distance: float,
    output_prefix: str | None = None,
    full_list: bool = False,
    grow_from_edge: bool = False,
    insphere_diameter: float | None = None,
    inbox_edges: list[float] | None = None,
    runner: Runner | None = None,
) -> RoIgrowOutputs:
    """
    ROIgrow

    A program to expand an ROI on the surface.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_surface: Specify input surface. You can also use -t* and -spec and\
            -surf methods to input surfaces.
        roi_labels: Data column containing integer labels of ROIs. Each integer\
            label gets grown separately.
        lim_distance: Distance to cover from each node. The units of LIM are those\
            of the surface's node coordinates. Distances are calculated along the\
            surface's mesh.
        output_prefix: Prefix of 1D output dataset. Default is ROIgrow
        full_list: Output a row for each node on the surface. Nodes not in the\
            grown ROI, receive a 0 for a label. This option is ONLY for use with\
            -roi_labels.
        grow_from_edge: Grow ROIs from their edges rather than the brute force\
            default. This might make the program faster on large ROIs and large\
            surfaces.
        insphere_diameter: Diameter of the sphere inside which nodes are added\
            instead of growing along the surface.
        inbox_edges: Use a box of edge widths E1, E2, E3 instead of DIA.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ro_igrow_params(
        input_surface=input_surface,
        roi_labels=roi_labels,
        lim_distance=lim_distance,
        output_prefix=output_prefix,
        full_list=full_list,
        grow_from_edge=grow_from_edge,
        insphere_diameter=insphere_diameter,
        inbox_edges=inbox_edges,
    )
    return ro_igrow_execute(params, runner)

__all__ = [
    "RoIgrow",
    "RoIgrowOutputs",
    "RO_IGROW_METADATA",
    "ro_igrow_cargs",
    "ro_igrow_outputs",
    "ro_igrow_params",
    "ro_igrow_execute",
    "ro_igrow_validate",
    "ro_igrow",
]