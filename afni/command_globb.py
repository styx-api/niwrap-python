# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

COMMAND_GLOBB_METADATA = Metadata(
    id="@CommandGlobb",
    name="@CommandGlobb",
    package="afni",
)

CommandGlobb = typing.TypedDict(
    "CommandGlobb",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@CommandGlobb"]],
        "program_command": str,
        "output_dir": str,
        "extension": typing.NotRequired[str],
        "brick_list": list[str],
    },
)

@dataclasses.dataclass
class CommandGlobbOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def command_globb_params(
    program_command: str,
    output_dir: str,
    brick_list: list[str],
    extension: str | None = None,
) -> CommandGlobb:
    """
    Build parameters.

    Args:
        program_command: The entire command line for the program desired. The\
            command is best put between single quotes, do not use the \ to break a\
            long line within the quotes.
        output_dir: The output directory where the results will be saved.
        brick_list: A list of bricks (or anything) on which the program command\
            will be executed.
        extension: If the program requires a -prefix option, then you can specify\
            the extension which will get appended to the Brick names before +orig.

    Returns:
        Parameter dictionary.
    """
    params: CommandGlobb = {
        "@type": "afni/@CommandGlobb",
        "program_command": program_command,
        "output_dir": output_dir,
        "brick_list": brick_list,
    }
    if extension is not None:
        params["extension"] = extension
    return params

def command_globb_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CommandGlobb."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("program_command", None) is None:
        raise StyxValidationError("`program_command` must not be None")
    if not isinstance(params["program_command"], str):
        raise StyxValidationError(f'`program_command` has the wrong type: Received `{type(params["program_command"])}` expected `str`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("extension", None) is not None:
        if not isinstance(params["extension"], str):
            raise StyxValidationError(f'`extension` has the wrong type: Received `{type(params["extension"])}` expected `str`')
    if params.get("brick_list", None) is None:
        raise StyxValidationError("`brick_list` must not be None")
    if not isinstance(params["brick_list"], list):
        raise StyxValidationError(f'`brick_list` has the wrong type: Received `{type(params["brick_list"])}` expected `list[str]`')
    for e in params["brick_list"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`brick_list` has the wrong type: Received `{type(e)}` expected `list[str]`')

def command_globb_cargs(params: CommandGlobb, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@CommandGlobb")
    cargs.append("-com")
    cargs.append(params["program_command"])
    cargs.append("-session")
    cargs.append(params["output_dir"])
    v_0 = params.get("extension")
    if v_0 is not None:
        cargs.append("-newext")
        cargs.append(v_0)
    cargs.append("-list")
    for item0 in params["brick_list"]:
        cargs.append(item0)
    return cargs

def command_globb_outputs(params: CommandGlobb, execution: Execution) -> CommandGlobbOutputs:
    """Build the CommandGlobbOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CommandGlobbOutputs(
        root=root_v,
    )

def command_globb_execute(params: CommandGlobb, runner: Runner | None = None) -> CommandGlobbOutputs:
    """
    @CommandGlobb

    A command-line tool to execute a specified program command line on a list of input bricks.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    command_globb_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(COMMAND_GLOBB_METADATA)
    execution.params(params)
    args = command_globb_cargs(params, execution)
    out = command_globb_outputs(params, execution)
    execution.run(args)
    return out

def command_globb(
    program_command: str,
    output_dir: str,
    brick_list: list[str],
    extension: str | None = None,
    runner: Runner | None = None,
) -> CommandGlobbOutputs:
    """
    @CommandGlobb

    A command-line tool to execute a specified program command line on a list of input bricks.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        program_command: The entire command line for the program desired. The\
            command is best put between single quotes, do not use the \ to break a\
            long line within the quotes.
        output_dir: The output directory where the results will be saved.
        brick_list: A list of bricks (or anything) on which the program command\
            will be executed.
        extension: If the program requires a -prefix option, then you can specify\
            the extension which will get appended to the Brick names before +orig.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = command_globb_params(
        program_command=program_command,
        output_dir=output_dir,
        brick_list=brick_list,
        extension=extension,
    )
    return command_globb_execute(params, runner)

__all__ = [
    "CommandGlobb",
    "CommandGlobbOutputs",
    "COMMAND_GLOBB_METADATA",
    "command_globb_cargs",
    "command_globb_outputs",
    "command_globb_params",
    "command_globb_execute",
    "command_globb_validate",
    "command_globb",
]