# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_MEDIAN_FILTER_METADATA = Metadata(
    id="3dMedianFilter",
    name="3dMedianFilter",
    package="afni",
)

V3dMedianFilter = typing.TypedDict(
    "V3dMedianFilter",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dMedianFilter"]],
        "irad": typing.NotRequired[float],
        "iter": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "automask": typing.NotRequired[bool],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dMedianFilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_brik: OutputPathType | None
    """Output dataset is stored in float format."""
    output_head: OutputPathType | None
    """Output dataset header file."""

def v_3d_median_filter_params(
    dataset: InputPathType,
    irad: float | None = None,
    iter: float | None = None,
    verbose: bool = False,
    prefix: str | None = None,
    automask: bool = False,
) -> V3dMedianFilter:
    """
    Build parameters.

    Args:
        dataset: Input dataset
        irad: Radius in voxels of spherical regions
        iter: Iterate 'n' times [default=1]
        verbose: Be verbose during run
        prefix: Use 'pp' for prefix of output dataset
        automask: Create a mask (a la 3dAutomask)

    Returns:
        Parameter dictionary.
    """
    params: V3dMedianFilter = {
        "@type": "afni/3dMedianFilter",
        "dataset": dataset,
        "verbose": verbose,
        "automask": automask,
    }
    if irad is not None:
        params["irad"] = irad
    if iter is not None:
        params["iter"] = iter
    if prefix is not None:
        params["prefix"] = prefix
    return params

def v_3d_median_filter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dMedianFilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("irad", None) is not None:
        if not isinstance(params["irad"], (float, int)):
            raise StyxValidationError(f'`irad` has the wrong type: Received `{type(params["irad"])}` expected `float`')
    if params.get("iter", None) is not None:
        if not isinstance(params["iter"], (float, int)):
            raise StyxValidationError(f'`iter` has the wrong type: Received `{type(params["iter"])}` expected `float`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("automask", None) is not None:
        if not isinstance(params["automask"], bool):
            raise StyxValidationError(f'`automask` has the wrong type: Received `{type(params["automask"])}` expected `bool`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def v_3d_median_filter_cargs(params: V3dMedianFilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dMedianFilter")
    v_0 = params.get("irad")
    if v_0 is not None:
        cargs.append("-irad")
        cargs.append(str(v_0))
    v_1 = params.get("iter")
    if v_1 is not None:
        cargs.append("-iter")
        cargs.append(str(v_1))
    if params.get("verbose"):
        cargs.append("-verb")
    v_2 = params.get("prefix")
    if v_2 is not None:
        cargs.append("-prefix")
        cargs.append(v_2)
    if params.get("automask"):
        cargs.append("-automask")
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def v_3d_median_filter_outputs(params: V3dMedianFilter, execution: Execution) -> V3dMedianFilterOutputs:
    """Build the V3dMedianFilterOutputs object for this tool."""
    output_brik_v: OutputPathType | None = None
    output_head_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_brik_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}+tlrc.BRIK')
    __v1 = params.get("prefix")
    if __v1 is not None:
        output_head_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}+tlrc.HEAD')
    return V3dMedianFilterOutputs(
        root=root_v,
        output_brik=output_brik_v,
        output_head=output_head_v,
    )

def v_3d_median_filter_execute(params: V3dMedianFilter, runner: Runner | None = None) -> V3dMedianFilterOutputs:
    """
    3dMedianFilter

    Computes the median in a spherical neighborhood around each point in the input to produce the output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_median_filter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_MEDIAN_FILTER_METADATA)
    execution.params(params)
    args = v_3d_median_filter_cargs(params, execution)
    out = v_3d_median_filter_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_median_filter(
    dataset: InputPathType,
    irad: float | None = None,
    iter: float | None = None,
    verbose: bool = False,
    prefix: str | None = None,
    automask: bool = False,
    runner: Runner | None = None,
) -> V3dMedianFilterOutputs:
    """
    3dMedianFilter

    Computes the median in a spherical neighborhood around each point in the input to produce the output.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset
        irad: Radius in voxels of spherical regions
        iter: Iterate 'n' times [default=1]
        verbose: Be verbose during run
        prefix: Use 'pp' for prefix of output dataset
        automask: Create a mask (a la 3dAutomask)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_median_filter_params(
        dataset=dataset,
        irad=irad,
        iter=iter,
        verbose=verbose,
        prefix=prefix,
        automask=automask,
    )
    return v_3d_median_filter_execute(params, runner)

__all__ = [
    "V3dMedianFilter",
    "V3dMedianFilterOutputs",
    "V_3D_MEDIAN_FILTER_METADATA",
    "v_3d_median_filter_cargs",
    "v_3d_median_filter_outputs",
    "v_3d_median_filter_params",
    "v_3d_median_filter_execute",
    "v_3d_median_filter_validate",
    "v_3d_median_filter",
]