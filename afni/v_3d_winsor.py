# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_WINSOR_METADATA = Metadata(
    id="3dWinsor",
    name="3dWinsor",
    package="afni",
)

V3dWinsor = typing.TypedDict(
    "V3dWinsor",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dWinsor"]],
        "irad": typing.NotRequired[float],
        "cbot": typing.NotRequired[float],
        "ctop": typing.NotRequired[float],
        "nrep": typing.NotRequired[float],
        "keepzero": typing.NotRequired[bool],
        "clip": typing.NotRequired[float],
        "prefix": typing.NotRequired[str],
        "mask": typing.NotRequired[InputPathType],
        "dataset": InputPathType,
    },
)

@dataclasses.dataclass
class V3dWinsorOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile_head: OutputPathType | None
    """Output dataset with Winsorizing filter applied."""
    outfile_brik: OutputPathType | None
    """Output dataset with Winsorizing filter applied."""

def v_3d_winsor_params(
    dataset: InputPathType,
    irad: float | None = None,
    cbot: float | None = None,
    ctop: float | None = None,
    nrep: float | None = None,
    keepzero: bool = False,
    clip: float | None = None,
    prefix: str | None = None,
    mask: InputPathType | None = None,
) -> V3dWinsor:
    """
    Build parameters.

    Args:
        dataset: Input dataset to apply the filter on.
        irad: Include all points within 'distance' rr in the operation, where\
            distance is defined as sqrt(i*i+j*j+k*k), and (i,j,k) are voxel index\
            offsets.
        cbot: Set bottom clip index to bb.
        ctop: Set top clip index to tt.
        nrep: Repeat filter nn times. If nn < 0, means to repeat filter until less\
            than abs(n) voxels change.
        keepzero: Don't filter voxels that are zero.
        clip: Set voxels at or below 'xx' to zero.
        prefix: Use 'pp' as the prefix for the output dataset.
        mask: Use 'mmm' as a mask dataset - voxels NOT in the mask won't be\
            filtered.

    Returns:
        Parameter dictionary.
    """
    params: V3dWinsor = {
        "@type": "afni/3dWinsor",
        "dataset": dataset,
        "keepzero": keepzero,
    }
    if irad is not None:
        params["irad"] = irad
    if cbot is not None:
        params["cbot"] = cbot
    if ctop is not None:
        params["ctop"] = ctop
    if nrep is not None:
        params["nrep"] = nrep
    if clip is not None:
        params["clip"] = clip
    if prefix is not None:
        params["prefix"] = prefix
    if mask is not None:
        params["mask"] = mask
    return params

def v_3d_winsor_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dWinsor."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("irad", None) is not None:
        if not isinstance(params["irad"], (float, int)):
            raise StyxValidationError(f'`irad` has the wrong type: Received `{type(params["irad"])}` expected `float`')
    if params.get("cbot", None) is not None:
        if not isinstance(params["cbot"], (float, int)):
            raise StyxValidationError(f'`cbot` has the wrong type: Received `{type(params["cbot"])}` expected `float`')
    if params.get("ctop", None) is not None:
        if not isinstance(params["ctop"], (float, int)):
            raise StyxValidationError(f'`ctop` has the wrong type: Received `{type(params["ctop"])}` expected `float`')
    if params.get("nrep", None) is not None:
        if not isinstance(params["nrep"], (float, int)):
            raise StyxValidationError(f'`nrep` has the wrong type: Received `{type(params["nrep"])}` expected `float`')
    if params.get("keepzero", None) is not None:
        if not isinstance(params["keepzero"], bool):
            raise StyxValidationError(f'`keepzero` has the wrong type: Received `{type(params["keepzero"])}` expected `bool`')
    if params.get("clip", None) is not None:
        if not isinstance(params["clip"], (float, int)):
            raise StyxValidationError(f'`clip` has the wrong type: Received `{type(params["clip"])}` expected `float`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')

def v_3d_winsor_cargs(params: V3dWinsor, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dWinsor")
    v_0 = params.get("irad")
    if v_0 is not None:
        cargs.append("-irad")
        cargs.append(str(v_0))
    v_1 = params.get("cbot")
    if v_1 is not None:
        cargs.append("-cbot")
        cargs.append(str(v_1))
    v_2 = params.get("ctop")
    if v_2 is not None:
        cargs.append("-ctop")
        cargs.append(str(v_2))
    v_3 = params.get("nrep")
    if v_3 is not None:
        cargs.append("-nrep")
        cargs.append(str(v_3))
    if params.get("keepzero"):
        cargs.append("-keepzero")
    v_4 = params.get("clip")
    if v_4 is not None:
        cargs.append("-clip")
        cargs.append(str(v_4))
    v_5 = params.get("prefix")
    if v_5 is not None:
        cargs.append("-prefix")
        cargs.append(v_5)
    v_6 = params.get("mask")
    if v_6 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_6))
    cargs.append(execution.input_file(params["dataset"]))
    return cargs

def v_3d_winsor_outputs(params: V3dWinsor, execution: Execution) -> V3dWinsorOutputs:
    """Build the V3dWinsorOutputs object for this tool."""
    outfile_head_v: OutputPathType | None = None
    outfile_brik_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        outfile_head_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.HEAD')
    __v1 = params.get("prefix")
    if __v1 is not None:
        outfile_brik_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.BRIK')
    return V3dWinsorOutputs(
        root=root_v,
        outfile_head=outfile_head_v,
        outfile_brik=outfile_brik_v,
    )

def v_3d_winsor_execute(params: V3dWinsor, runner: Runner | None = None) -> V3dWinsorOutputs:
    """
    3dWinsor

    Apply a 3D 'Winsorizing' filter to a short-valued dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_winsor_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_WINSOR_METADATA)
    execution.params(params)
    args = v_3d_winsor_cargs(params, execution)
    out = v_3d_winsor_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_winsor(
    dataset: InputPathType,
    irad: float | None = None,
    cbot: float | None = None,
    ctop: float | None = None,
    nrep: float | None = None,
    keepzero: bool = False,
    clip: float | None = None,
    prefix: str | None = None,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> V3dWinsorOutputs:
    """
    3dWinsor

    Apply a 3D 'Winsorizing' filter to a short-valued dataset.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input dataset to apply the filter on.
        irad: Include all points within 'distance' rr in the operation, where\
            distance is defined as sqrt(i*i+j*j+k*k), and (i,j,k) are voxel index\
            offsets.
        cbot: Set bottom clip index to bb.
        ctop: Set top clip index to tt.
        nrep: Repeat filter nn times. If nn < 0, means to repeat filter until less\
            than abs(n) voxels change.
        keepzero: Don't filter voxels that are zero.
        clip: Set voxels at or below 'xx' to zero.
        prefix: Use 'pp' as the prefix for the output dataset.
        mask: Use 'mmm' as a mask dataset - voxels NOT in the mask won't be\
            filtered.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_winsor_params(
        dataset=dataset,
        irad=irad,
        cbot=cbot,
        ctop=ctop,
        nrep=nrep,
        keepzero=keepzero,
        clip=clip,
        prefix=prefix,
        mask=mask,
    )
    return v_3d_winsor_execute(params, runner)

__all__ = [
    "V3dWinsor",
    "V3dWinsorOutputs",
    "V_3D_WINSOR_METADATA",
    "v_3d_winsor_cargs",
    "v_3d_winsor_outputs",
    "v_3d_winsor_params",
    "v_3d_winsor_execute",
    "v_3d_winsor_validate",
    "v_3d_winsor",
]