# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CENTER_DISTANCE_METADATA = Metadata(
    id="@Center_Distance",
    name="@Center_Distance",
    package="afni",
)

CenterDistance = typing.TypedDict(
    "CenterDistance",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@Center_Distance"]],
        "dset1": InputPathType,
        "dset2": InputPathType,
    },
)

@dataclasses.dataclass
class CenterDistanceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    distance_output: OutputPathType
    """The calculated distance between the centers of DSET_1 and DSET_2"""

def center_distance_params(
    dset1: InputPathType,
    dset2: InputPathType,
) -> CenterDistance:
    """
    Build parameters.

    Args:
        dset1: First dataset file (e.g. file1.nii.gz)
        dset2: Second dataset file (e.g. file2.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: CenterDistance = {
        "@type": "afni/@Center_Distance",
        "dset1": dset1,
        "dset2": dset2,
    }
    return params

def center_distance_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CenterDistance."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dset1", None) is None:
        raise StyxValidationError("`dset1` must not be None")
    if not isinstance(params["dset1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset1` has the wrong type: Received `{type(params["dset1"])}` expected `InputPathType`')
    if params.get("dset2", None) is None:
        raise StyxValidationError("`dset2` must not be None")
    if not isinstance(params["dset2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dset2` has the wrong type: Received `{type(params["dset2"])}` expected `InputPathType`')

def center_distance_cargs(params: CenterDistance, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@Center_Distance")
    cargs.append("-dset")
    cargs.append(execution.input_file(params["dset1"]))
    cargs.append(execution.input_file(params["dset2"]))
    return cargs

def center_distance_outputs(params: CenterDistance, execution: Execution) -> CenterDistanceOutputs:
    """Build the CenterDistanceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    distance_output_v: OutputPathType = execution.output_file("distance.txt")
    return CenterDistanceOutputs(
        root=root_v,
        distance_output=distance_output_v,
    )

def center_distance_execute(params: CenterDistance, runner: Runner | None = None) -> CenterDistanceOutputs:
    """
    @Center_Distance

    Tool to calculate the distance between the centers of two datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    center_distance_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CENTER_DISTANCE_METADATA)
    execution.params(params)
    args = center_distance_cargs(params, execution)
    out = center_distance_outputs(params, execution)
    execution.run(args)
    return out

def center_distance(
    dset1: InputPathType,
    dset2: InputPathType,
    runner: Runner | None = None,
) -> CenterDistanceOutputs:
    """
    @Center_Distance

    Tool to calculate the distance between the centers of two datasets

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dset1: First dataset file (e.g. file1.nii.gz)
        dset2: Second dataset file (e.g. file2.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = center_distance_params(
        dset1=dset1,
        dset2=dset2,
    )
    return center_distance_execute(params, runner)

__all__ = [
    "CenterDistance",
    "CenterDistanceOutputs",
    "CENTER_DISTANCE_METADATA",
    "center_distance_cargs",
    "center_distance_outputs",
    "center_distance_params",
    "center_distance_execute",
    "center_distance_validate",
    "center_distance",
]