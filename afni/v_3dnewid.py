# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3DNEWID_METADATA = Metadata(
    id="3dnewid",
    name="3dnewid",
    package="afni",
)

V3dnewid = typing.TypedDict(
    "V3dnewid",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dnewid"]],
        "datasets": list[InputPathType],
        "fun": typing.NotRequired[float],
        "fun11": typing.NotRequired[bool],
        "int": typing.NotRequired[bool],
        "hash": typing.NotRequired[str],
        "MD5": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class V3dnewidOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def v_3dnewid_params(
    datasets: list[InputPathType],
    fun: float | None = None,
    fun11: bool = False,
    int_: bool = False,
    hash: str | None = None,
    MD5: str | None = None,
) -> V3dnewid:
    """
    Build parameters.

    Args:
        datasets: Input datasets to assign new ID codes.
        fun: Generate n randomly generated ID codes. If n is not present, 1 ID code\
            is printed.
        fun11: Generate an 11 character ID code for use in scripting
        int_: Generate a random positive integer between 1 million and 1 billion.
        hash: Generate a unique hash code of the provided string. The same string\
            produces the same hash code.
        MD5: Generate the MD5 hash of the provided string. Output should be the\
            same as the -hash output without the prefix and without the + and /\
            char substitutions.

    Returns:
        Parameter dictionary.
    """
    params: V3dnewid = {
        "@type": "afni/3dnewid",
        "datasets": datasets,
        "fun11": fun11,
        "int": int_,
    }
    if fun is not None:
        params["fun"] = fun
    if hash is not None:
        params["hash"] = hash
    if MD5 is not None:
        params["MD5"] = MD5
    return params

def v_3dnewid_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dnewid."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("datasets", None) is None:
        raise StyxValidationError("`datasets` must not be None")
    if not isinstance(params["datasets"], list):
        raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(params["datasets"])}` expected `list[InputPathType]`')
    for e in params["datasets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`datasets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("fun", None) is not None:
        if not isinstance(params["fun"], (float, int)):
            raise StyxValidationError(f'`fun` has the wrong type: Received `{type(params["fun"])}` expected `float`')
    if params.get("fun11", None) is not None:
        if not isinstance(params["fun11"], bool):
            raise StyxValidationError(f'`fun11` has the wrong type: Received `{type(params["fun11"])}` expected `bool`')
    if params.get("int", None) is not None:
        if not isinstance(params["int"], bool):
            raise StyxValidationError(f'`int` has the wrong type: Received `{type(params["int"])}` expected `bool`')
    if params.get("hash", None) is not None:
        if not isinstance(params["hash"], str):
            raise StyxValidationError(f'`hash` has the wrong type: Received `{type(params["hash"])}` expected `str`')
    if params.get("MD5", None) is not None:
        if not isinstance(params["MD5"], str):
            raise StyxValidationError(f'`MD5` has the wrong type: Received `{type(params["MD5"])}` expected `str`')

def v_3dnewid_cargs(params: V3dnewid, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dnewid")
    for item0 in params["datasets"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("fun")
    if v_0 is not None:
        cargs.append("-fun")
        cargs.append(str(v_0))
    if params.get("fun11"):
        cargs.append("-fun11")
    if params.get("int"):
        cargs.append("-int")
    v_1 = params.get("hash")
    if v_1 is not None:
        cargs.append("-hash")
        cargs.append(v_1)
    v_2 = params.get("MD5")
    if v_2 is not None:
        cargs.append("-MD5")
        cargs.append(v_2)
    return cargs

def v_3dnewid_outputs(params: V3dnewid, execution: Execution) -> V3dnewidOutputs:
    """Build the V3dnewidOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return V3dnewidOutputs(
        root=root_v,
    )

def v_3dnewid_execute(params: V3dnewid, runner: Runner | None = None) -> V3dnewidOutputs:
    """
    3dnewid

    Assigns a new ID code to a dataset, ensuring internal ID codes remain unique.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3dnewid_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3DNEWID_METADATA)
    execution.params(params)
    args = v_3dnewid_cargs(params, execution)
    out = v_3dnewid_outputs(params, execution)
    execution.run(args)
    return out

def v_3dnewid(
    datasets: list[InputPathType],
    fun: float | None = None,
    fun11: bool = False,
    int_: bool = False,
    hash: str | None = None,
    MD5: str | None = None,
    runner: Runner | None = None,
) -> V3dnewidOutputs:
    """
    3dnewid

    Assigns a new ID code to a dataset, ensuring internal ID codes remain unique.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        datasets: Input datasets to assign new ID codes.
        fun: Generate n randomly generated ID codes. If n is not present, 1 ID code\
            is printed.
        fun11: Generate an 11 character ID code for use in scripting
        int_: Generate a random positive integer between 1 million and 1 billion.
        hash: Generate a unique hash code of the provided string. The same string\
            produces the same hash code.
        MD5: Generate the MD5 hash of the provided string. Output should be the\
            same as the -hash output without the prefix and without the + and /\
            char substitutions.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3dnewid_params(
        datasets=datasets,
        fun=fun,
        fun11=fun11,
        int_=int_,
        hash=hash,
        MD5=MD5,
    )
    return v_3dnewid_execute(params, runner)

__all__ = [
    "V3dnewid",
    "V3dnewidOutputs",
    "V_3DNEWID_METADATA",
    "v_3dnewid_cargs",
    "v_3dnewid_outputs",
    "v_3dnewid_params",
    "v_3dnewid_execute",
    "v_3dnewid_validate",
    "v_3dnewid",
]