# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DICE_METRIC_METADATA = Metadata(
    id="@DiceMetric",
    name="@DiceMetric",
    package="afni",
)

DiceMetric = typing.TypedDict(
    "DiceMetric",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@DiceMetric"]],
        "base": InputPathType,
        "dsets": list[InputPathType],
        "max_roi": typing.NotRequired[float],
        "labeltable": typing.NotRequired[InputPathType],
        "forceoutput": typing.NotRequired[InputPathType],
        "echo": typing.NotRequired[bool],
        "save_match": typing.NotRequired[bool],
        "save_diff": typing.NotRequired[bool],
        "do_not_mask_by_base": typing.NotRequired[bool],
        "mask_by_base": typing.NotRequired[bool],
        "prefix": typing.NotRequired[str],
        "ignore_bad": typing.NotRequired[bool],
        "keep_tmp": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DiceMetricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dice_metric_params(
    base: InputPathType,
    dsets: list[InputPathType],
    max_roi: float | None = None,
    labeltable: InputPathType | None = None,
    forceoutput: InputPathType | None = None,
    echo: bool = False,
    save_match: bool = False,
    save_diff: bool = False,
    do_not_mask_by_base: bool = False,
    mask_by_base: bool = False,
    prefix: str | None = None,
    ignore_bad: bool = False,
    keep_tmp: bool = False,
) -> DiceMetric:
    """
    Build parameters.

    Args:
        base: Name of base (reference) segmentation
        dsets: Data sets for which the Dice Metric with BASE is computed. This\
            should be the last option on the command line.
        max_roi: The maximum possible ROI index. Default is 12 or based on LTFILE\
            if specified.
        labeltable: If given, the labeltable is used to set the default MAX_ROI\
            parameter. Also, this option forces an output for each key in the\
            LTFILE.
        forceoutput: If given, force output for each class in LTFILE.
        echo: Set echo.
        save_match: Save volume showing BASE*equals(BASE,DSET).
        save_diff: Save volume showing BASE*(1-equals(BASE,DSET)).
        do_not_mask_by_base: Do not mask dset by step(base) before computing Dice\
            coefficient.
        mask_by_base: Mask dset by the step(base) before computing Dice\
            coefficient.
        prefix: Use PREFIX for the output table. Default is separate results for\
            each dset to stdout.
        ignore_bad: Warn if encountering bad scenarios, but do not create a zero\
            entry.
        keep_tmp: Keep temporary files for debugging. Note that you should delete\
            temporary files before rerunning the script.

    Returns:
        Parameter dictionary.
    """
    params: DiceMetric = {
        "@type": "afni/@DiceMetric",
        "base": base,
        "dsets": dsets,
        "echo": echo,
        "save_match": save_match,
        "save_diff": save_diff,
        "do_not_mask_by_base": do_not_mask_by_base,
        "mask_by_base": mask_by_base,
        "ignore_bad": ignore_bad,
        "keep_tmp": keep_tmp,
    }
    if max_roi is not None:
        params["max_roi"] = max_roi
    if labeltable is not None:
        params["labeltable"] = labeltable
    if forceoutput is not None:
        params["forceoutput"] = forceoutput
    if prefix is not None:
        params["prefix"] = prefix
    return params

def dice_metric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DiceMetric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("base", None) is None:
        raise StyxValidationError("`base` must not be None")
    if not isinstance(params["base"], (pathlib.Path, str)):
        raise StyxValidationError(f'`base` has the wrong type: Received `{type(params["base"])}` expected `InputPathType`')
    if params.get("dsets", None) is None:
        raise StyxValidationError("`dsets` must not be None")
    if not isinstance(params["dsets"], list):
        raise StyxValidationError(f'`dsets` has the wrong type: Received `{type(params["dsets"])}` expected `list[InputPathType]`')
    for e in params["dsets"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`dsets` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("max_roi", None) is not None:
        if not isinstance(params["max_roi"], (float, int)):
            raise StyxValidationError(f'`max_roi` has the wrong type: Received `{type(params["max_roi"])}` expected `float`')
    if params.get("labeltable", None) is not None:
        if not isinstance(params["labeltable"], (pathlib.Path, str)):
            raise StyxValidationError(f'`labeltable` has the wrong type: Received `{type(params["labeltable"])}` expected `InputPathType`')
    if params.get("forceoutput", None) is not None:
        if not isinstance(params["forceoutput"], (pathlib.Path, str)):
            raise StyxValidationError(f'`forceoutput` has the wrong type: Received `{type(params["forceoutput"])}` expected `InputPathType`')
    if params.get("echo", None) is not None:
        if not isinstance(params["echo"], bool):
            raise StyxValidationError(f'`echo` has the wrong type: Received `{type(params["echo"])}` expected `bool`')
    if params.get("save_match", None) is not None:
        if not isinstance(params["save_match"], bool):
            raise StyxValidationError(f'`save_match` has the wrong type: Received `{type(params["save_match"])}` expected `bool`')
    if params.get("save_diff", None) is not None:
        if not isinstance(params["save_diff"], bool):
            raise StyxValidationError(f'`save_diff` has the wrong type: Received `{type(params["save_diff"])}` expected `bool`')
    if params.get("do_not_mask_by_base", None) is not None:
        if not isinstance(params["do_not_mask_by_base"], bool):
            raise StyxValidationError(f'`do_not_mask_by_base` has the wrong type: Received `{type(params["do_not_mask_by_base"])}` expected `bool`')
    if params.get("mask_by_base", None) is not None:
        if not isinstance(params["mask_by_base"], bool):
            raise StyxValidationError(f'`mask_by_base` has the wrong type: Received `{type(params["mask_by_base"])}` expected `bool`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("ignore_bad", None) is not None:
        if not isinstance(params["ignore_bad"], bool):
            raise StyxValidationError(f'`ignore_bad` has the wrong type: Received `{type(params["ignore_bad"])}` expected `bool`')
    if params.get("keep_tmp", None) is not None:
        if not isinstance(params["keep_tmp"], bool):
            raise StyxValidationError(f'`keep_tmp` has the wrong type: Received `{type(params["keep_tmp"])}` expected `bool`')

def dice_metric_cargs(params: DiceMetric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@DiceMetric")
    cargs.append("-base")
    cargs.append(execution.input_file(params["base"]))
    cargs.append("-dsets")
    for item0 in params["dsets"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("max_roi")
    if v_0 is not None:
        cargs.append("-max_N_roi")
        cargs.append(str(v_0))
    v_1 = params.get("labeltable")
    if v_1 is not None:
        cargs.append("-labeltable")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("forceoutput")
    if v_2 is not None:
        cargs.append("-forceoutput")
        cargs.append(execution.input_file(v_2))
    if params.get("echo"):
        cargs.append("-echo")
    if params.get("save_match"):
        cargs.append("-save_match")
    if params.get("save_diff"):
        cargs.append("-save_diff")
    if params.get("do_not_mask_by_base"):
        cargs.append("-do_not_mask_by_base")
    if params.get("mask_by_base"):
        cargs.append("-mask_by_base")
    v_3 = params.get("prefix")
    if v_3 is not None:
        cargs.append("-prefix")
        cargs.append(v_3)
    if params.get("ignore_bad"):
        cargs.append("-ignore_bad")
    if params.get("keep_tmp"):
        cargs.append("-keep_tmp")
    return cargs

def dice_metric_outputs(params: DiceMetric, execution: Execution) -> DiceMetricOutputs:
    """Build the DiceMetricOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DiceMetricOutputs(
        root=root_v,
    )

def dice_metric_execute(params: DiceMetric, runner: Runner | None = None) -> DiceMetricOutputs:
    """
    @DiceMetric

    Computes Dice Metric between BASE and each of the DSET volumes

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dice_metric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DICE_METRIC_METADATA)
    execution.params(params)
    args = dice_metric_cargs(params, execution)
    out = dice_metric_outputs(params, execution)
    execution.run(args)
    return out

def dice_metric(
    base: InputPathType,
    dsets: list[InputPathType],
    max_roi: float | None = None,
    labeltable: InputPathType | None = None,
    forceoutput: InputPathType | None = None,
    echo: bool = False,
    save_match: bool = False,
    save_diff: bool = False,
    do_not_mask_by_base: bool = False,
    mask_by_base: bool = False,
    prefix: str | None = None,
    ignore_bad: bool = False,
    keep_tmp: bool = False,
    runner: Runner | None = None,
) -> DiceMetricOutputs:
    """
    @DiceMetric

    Computes Dice Metric between BASE and each of the DSET volumes

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        base: Name of base (reference) segmentation
        dsets: Data sets for which the Dice Metric with BASE is computed. This\
            should be the last option on the command line.
        max_roi: The maximum possible ROI index. Default is 12 or based on LTFILE\
            if specified.
        labeltable: If given, the labeltable is used to set the default MAX_ROI\
            parameter. Also, this option forces an output for each key in the\
            LTFILE.
        forceoutput: If given, force output for each class in LTFILE.
        echo: Set echo.
        save_match: Save volume showing BASE*equals(BASE,DSET).
        save_diff: Save volume showing BASE*(1-equals(BASE,DSET)).
        do_not_mask_by_base: Do not mask dset by step(base) before computing Dice\
            coefficient.
        mask_by_base: Mask dset by the step(base) before computing Dice\
            coefficient.
        prefix: Use PREFIX for the output table. Default is separate results for\
            each dset to stdout.
        ignore_bad: Warn if encountering bad scenarios, but do not create a zero\
            entry.
        keep_tmp: Keep temporary files for debugging. Note that you should delete\
            temporary files before rerunning the script.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dice_metric_params(
        base=base,
        dsets=dsets,
        max_roi=max_roi,
        labeltable=labeltable,
        forceoutput=forceoutput,
        echo=echo,
        save_match=save_match,
        save_diff=save_diff,
        do_not_mask_by_base=do_not_mask_by_base,
        mask_by_base=mask_by_base,
        prefix=prefix,
        ignore_bad=ignore_bad,
        keep_tmp=keep_tmp,
    )
    return dice_metric_execute(params, runner)

__all__ = [
    "DiceMetric",
    "DiceMetricOutputs",
    "DICE_METRIC_METADATA",
    "dice_metric_cargs",
    "dice_metric_outputs",
    "dice_metric_params",
    "dice_metric_execute",
    "dice_metric_validate",
    "dice_metric",
]