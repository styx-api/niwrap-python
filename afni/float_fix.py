# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FLOAT_FIX_METADATA = Metadata(
    id="@float_fix",
    name="@float_fix",
    package="afni",
)

FloatFix = typing.TypedDict(
    "FloatFix",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@float_fix"]],
        "input_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class FloatFixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def float_fix_params(
    input_files: list[InputPathType],
) -> FloatFix:
    """
    Build parameters.

    Args:
        input_files: Input files to be checked for illegal IEEE floating point\
            values. Wildcards can be used, but filenames must end with .HEAD.

    Returns:
        Parameter dictionary.
    """
    params: FloatFix = {
        "@type": "afni/@float_fix",
        "input_files": input_files,
    }
    return params

def float_fix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FloatFix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def float_fix_cargs(params: FloatFix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@float_fix")
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def float_fix_outputs(params: FloatFix, execution: Execution) -> FloatFixOutputs:
    """Build the FloatFixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FloatFixOutputs(
        root=root_v,
    )

def float_fix_execute(params: FloatFix, runner: Runner | None = None) -> FloatFixOutputs:
    """
    @float_fix

    Check whether the input files have any IEEE floating point numbers for illegal values: infinities and not-a-number (NaN) values.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    float_fix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FLOAT_FIX_METADATA)
    execution.params(params)
    args = float_fix_cargs(params, execution)
    out = float_fix_outputs(params, execution)
    execution.run(args)
    return out

def float_fix(
    input_files: list[InputPathType],
    runner: Runner | None = None,
) -> FloatFixOutputs:
    """
    @float_fix

    Check whether the input files have any IEEE floating point numbers for illegal values: infinities and not-a-number (NaN) values.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_files: Input files to be checked for illegal IEEE floating point\
            values. Wildcards can be used, but filenames must end with .HEAD.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = float_fix_params(
        input_files=input_files,
    )
    return float_fix_execute(params, runner)

__all__ = [
    "FloatFix",
    "FloatFixOutputs",
    "FLOAT_FIX_METADATA",
    "float_fix_cargs",
    "float_fix_outputs",
    "float_fix_params",
    "float_fix_execute",
    "float_fix_validate",
    "float_fix",
]