# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ROI_DECLUSTER_METADATA = Metadata(
    id="@ROI_decluster",
    name="@ROI_decluster",
    package="afni",
)

RoiDecluster = typing.TypedDict(
    "RoiDecluster",
    {
        "@type": typing.NotRequired[typing.Literal["afni/@ROI_decluster"]],
        "input_dset": InputPathType,
        "output_dir": typing.NotRequired[str],
        "nvox_thresh": typing.NotRequired[float],
        "frac_thresh": typing.NotRequired[float],
        "prefix": typing.NotRequired[str],
        "neighborhood_type": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class RoiDeclusterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Final output dataset"""

def roi_decluster_params(
    input_dset: InputPathType,
    output_dir: str | None = None,
    nvox_thresh: float | None = None,
    frac_thresh: float | None = None,
    prefix: str | None = None,
    neighborhood_type: int | None = None,
) -> RoiDecluster:
    """
    Build parameters.

    Args:
        input_dset: Required input dataset. This dataset should be set of integer\
            values. The program mostly assumes approximate isotropic voxels.
        output_dir: Directory name for output. All output goes to this directory.
        nvox_thresh: Number of voxels in a cluster to keep
        frac_thresh: Fraction of voxels in a cluster to keep [0.0-1.0]
        prefix: Base name of final output dataset, i.e. baseprefix.nii.gz
        neighborhood_type: Neighborhood type using in finding mode: 1 - facing\
            neighbors, 2 - edges, 3 - corners

    Returns:
        Parameter dictionary.
    """
    params: RoiDecluster = {
        "@type": "afni/@ROI_decluster",
        "input_dset": input_dset,
    }
    if output_dir is not None:
        params["output_dir"] = output_dir
    if nvox_thresh is not None:
        params["nvox_thresh"] = nvox_thresh
    if frac_thresh is not None:
        params["frac_thresh"] = frac_thresh
    if prefix is not None:
        params["prefix"] = prefix
    if neighborhood_type is not None:
        params["neighborhood_type"] = neighborhood_type
    return params

def roi_decluster_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RoiDecluster."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_dset", None) is None:
        raise StyxValidationError("`input_dset` must not be None")
    if not isinstance(params["input_dset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dset` has the wrong type: Received `{type(params["input_dset"])}` expected `InputPathType`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("nvox_thresh", None) is not None:
        if not isinstance(params["nvox_thresh"], (float, int)):
            raise StyxValidationError(f'`nvox_thresh` has the wrong type: Received `{type(params["nvox_thresh"])}` expected `float`')
    if params.get("frac_thresh", None) is not None:
        if not isinstance(params["frac_thresh"], (float, int)):
            raise StyxValidationError(f'`frac_thresh` has the wrong type: Received `{type(params["frac_thresh"])}` expected `float`')
    if params.get("prefix", None) is not None:
        if not isinstance(params["prefix"], str):
            raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("neighborhood_type", None) is not None:
        if not isinstance(params["neighborhood_type"], int):
            raise StyxValidationError(f'`neighborhood_type` has the wrong type: Received `{type(params["neighborhood_type"])}` expected `int`')
        if not (1 <= params["neighborhood_type"] <= 3):
            raise StyxValidationError("Parameter `neighborhood_type` must be between 1 and 3 (inclusive)")

def roi_decluster_cargs(params: RoiDecluster, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("@ROI_decluster")
    cargs.append("-input")
    cargs.append(execution.input_file(params["input_dset"]))
    v_0 = params.get("output_dir")
    if v_0 is not None:
        cargs.append("-outdir")
        cargs.append(v_0)
    v_1 = params.get("nvox_thresh")
    if v_1 is not None:
        cargs.append("-nvox_thresh")
        cargs.append(str(v_1))
    v_2 = params.get("frac_thresh")
    if v_2 is not None:
        cargs.append("-frac_thresh")
        cargs.append(str(v_2))
    v_3 = params.get("prefix")
    if v_3 is not None:
        cargs.append("-prefix")
        cargs.append(v_3)
    v_4 = params.get("neighborhood_type")
    if v_4 is not None:
        cargs.append("-NN")
        cargs.append(str(v_4))
    return cargs

def roi_decluster_outputs(params: RoiDecluster, execution: Execution) -> RoiDeclusterOutputs:
    """Build the RoiDeclusterOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("prefix")
    if __v0 is not None:
        output_file_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return RoiDeclusterOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def roi_decluster_execute(params: RoiDecluster, runner: Runner | None = None) -> RoiDeclusterOutputs:
    """
    @ROI_decluster

    Script to remove small clusters or standalone voxels from an ROI/atlas dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    roi_decluster_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ROI_DECLUSTER_METADATA)
    execution.params(params)
    args = roi_decluster_cargs(params, execution)
    out = roi_decluster_outputs(params, execution)
    execution.run(args)
    return out

def roi_decluster(
    input_dset: InputPathType,
    output_dir: str | None = None,
    nvox_thresh: float | None = None,
    frac_thresh: float | None = None,
    prefix: str | None = None,
    neighborhood_type: int | None = None,
    runner: Runner | None = None,
) -> RoiDeclusterOutputs:
    """
    @ROI_decluster

    Script to remove small clusters or standalone voxels from an ROI/atlas dataset

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        input_dset: Required input dataset. This dataset should be set of integer\
            values. The program mostly assumes approximate isotropic voxels.
        output_dir: Directory name for output. All output goes to this directory.
        nvox_thresh: Number of voxels in a cluster to keep
        frac_thresh: Fraction of voxels in a cluster to keep [0.0-1.0]
        prefix: Base name of final output dataset, i.e. baseprefix.nii.gz
        neighborhood_type: Neighborhood type using in finding mode: 1 - facing\
            neighbors, 2 - edges, 3 - corners
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = roi_decluster_params(
        input_dset=input_dset,
        output_dir=output_dir,
        nvox_thresh=nvox_thresh,
        frac_thresh=frac_thresh,
        prefix=prefix,
        neighborhood_type=neighborhood_type,
    )
    return roi_decluster_execute(params, runner)

__all__ = [
    "RoiDecluster",
    "RoiDeclusterOutputs",
    "ROI_DECLUSTER_METADATA",
    "roi_decluster_cargs",
    "roi_decluster_outputs",
    "roi_decluster_params",
    "roi_decluster_execute",
    "roi_decluster_validate",
    "roi_decluster",
]