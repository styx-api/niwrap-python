# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_3D_RANKIZER_METADATA = Metadata(
    id="3dRankizer",
    name="3dRankizer",
    package="afni",
)

V3dRankizer = typing.TypedDict(
    "V3dRankizer",
    {
        "@type": typing.NotRequired[typing.Literal["afni/3dRankizer"]],
        "dataset": InputPathType,
        "base_rank": typing.NotRequired[float],
        "mask": typing.NotRequired[InputPathType],
        "prefix": str,
        "percentize": typing.NotRequired[bool],
        "percentize_mask": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class V3dRankizerOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dataset: OutputPathType
    """Output float-format dataset containing ranked voxel values"""

def v_3d_rankizer_params(
    dataset: InputPathType,
    prefix: str,
    base_rank: float | None = None,
    mask: InputPathType | None = None,
    percentize: bool = False,
    percentize_mask: bool = False,
) -> V3dRankizer:
    """
    Build parameters.

    Args:
        dataset: Input MRI dataset
        prefix: Write results into float-format output dataset
        base_rank: Set the 'base' rank instead of 1
        mask: Use the specified dataset as a mask. Only voxels with nonzero values\
            in this mask will be used from the input dataset. Voxels outside the\
            mask will get rank 0.
        percentize: Divide rank by the number of voxels in the dataset and multiply\
            by 100.0
        percentize_mask: Divide rank by the number of voxels in the mask and\
            multiply by 100.0

    Returns:
        Parameter dictionary.
    """
    params: V3dRankizer = {
        "@type": "afni/3dRankizer",
        "dataset": dataset,
        "prefix": prefix,
        "percentize": percentize,
        "percentize_mask": percentize_mask,
    }
    if base_rank is not None:
        params["base_rank"] = base_rank
    if mask is not None:
        params["mask"] = mask
    return params

def v_3d_rankizer_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V3dRankizer."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataset", None) is None:
        raise StyxValidationError("`dataset` must not be None")
    if not isinstance(params["dataset"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dataset` has the wrong type: Received `{type(params["dataset"])}` expected `InputPathType`')
    if params.get("base_rank", None) is not None:
        if not isinstance(params["base_rank"], (float, int)):
            raise StyxValidationError(f'`base_rank` has the wrong type: Received `{type(params["base_rank"])}` expected `float`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("prefix", None) is None:
        raise StyxValidationError("`prefix` must not be None")
    if not isinstance(params["prefix"], str):
        raise StyxValidationError(f'`prefix` has the wrong type: Received `{type(params["prefix"])}` expected `str`')
    if params.get("percentize", None) is not None:
        if not isinstance(params["percentize"], bool):
            raise StyxValidationError(f'`percentize` has the wrong type: Received `{type(params["percentize"])}` expected `bool`')
    if params.get("percentize_mask", None) is not None:
        if not isinstance(params["percentize_mask"], bool):
            raise StyxValidationError(f'`percentize_mask` has the wrong type: Received `{type(params["percentize_mask"])}` expected `bool`')

def v_3d_rankizer_cargs(params: V3dRankizer, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("3dRankizer")
    cargs.append(execution.input_file(params["dataset"]))
    v_0 = params.get("base_rank")
    if v_0 is not None:
        cargs.append("-brank")
        cargs.append(str(v_0))
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    cargs.append("-prefix")
    cargs.append(params["prefix"])
    if params.get("percentize"):
        cargs.append("-percentize")
    if params.get("percentize_mask"):
        cargs.append("-percentize_mask")
    return cargs

def v_3d_rankizer_outputs(params: V3dRankizer, execution: Execution) -> V3dRankizerOutputs:
    """Build the V3dRankizerOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dataset_v: OutputPathType = execution.output_file(f'{params["prefix"]}+tlrc.HEAD')
    return V3dRankizerOutputs(
        root=root_v,
        output_dataset=output_dataset_v,
    )

def v_3d_rankizer_execute(params: V3dRankizer, runner: Runner | None = None) -> V3dRankizerOutputs:
    """
    3dRankizer

    Tool to rank each voxel as sorted into increasing value. Ties get the average rank.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_3d_rankizer_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_3D_RANKIZER_METADATA)
    execution.params(params)
    args = v_3d_rankizer_cargs(params, execution)
    out = v_3d_rankizer_outputs(params, execution)
    execution.run(args)
    return out

def v_3d_rankizer(
    dataset: InputPathType,
    prefix: str,
    base_rank: float | None = None,
    mask: InputPathType | None = None,
    percentize: bool = False,
    percentize_mask: bool = False,
    runner: Runner | None = None,
) -> V3dRankizerOutputs:
    """
    3dRankizer

    Tool to rank each voxel as sorted into increasing value. Ties get the average rank.

    Author: AFNI Developers

    URL: https://afni.nimh.nih.gov/

    Args:
        dataset: Input MRI dataset
        prefix: Write results into float-format output dataset
        base_rank: Set the 'base' rank instead of 1
        mask: Use the specified dataset as a mask. Only voxels with nonzero values\
            in this mask will be used from the input dataset. Voxels outside the\
            mask will get rank 0.
        percentize: Divide rank by the number of voxels in the dataset and multiply\
            by 100.0
        percentize_mask: Divide rank by the number of voxels in the mask and\
            multiply by 100.0
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_3d_rankizer_params(
        dataset=dataset,
        prefix=prefix,
        base_rank=base_rank,
        mask=mask,
        percentize=percentize,
        percentize_mask=percentize_mask,
    )
    return v_3d_rankizer_execute(params, runner)

__all__ = [
    "V3dRankizer",
    "V3dRankizerOutputs",
    "V_3D_RANKIZER_METADATA",
    "v_3d_rankizer_cargs",
    "v_3d_rankizer_outputs",
    "v_3d_rankizer_params",
    "v_3d_rankizer_execute",
    "v_3d_rankizer_validate",
    "v_3d_rankizer",
]