# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WPNG_METADATA = Metadata(
    id="wpng",
    name="wpng",
    package="fsl",
)

Wpng = typing.TypedDict(
    "Wpng",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/wpng"]],
        "input_file": typing.NotRequired[InputPathType],
        "gamma": typing.NotRequired[float],
        "bgcolor": typing.NotRequired[str],
        "text_flag": typing.NotRequired[bool],
        "time_flag": typing.NotRequired[bool],
        "interlace_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class WpngOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Converted PNG file"""

def wpng_params(
    input_file: InputPathType | None = None,
    gamma: float | None = None,
    bgcolor: str | None = None,
    text_flag: bool = False,
    time_flag: bool = False,
    interlace_flag: bool = False,
) -> Wpng:
    """
    Build parameters.

    Args:
        input_file: Input PNM file (binary PGM 'P5', PPM 'P6' or PAM 'P8')
        gamma: Transfer-function exponent (``gamma'') of the image in\
            floating-point format (e.g., ``0.45455''). If image looks correct on\
            given display system, image gamma is equal to inverse of display-system\
            exponent, i.e., 1 / (LUT * CRT) (where LUT = lookup-table exponent and\
            CRT = CRT exponent; first varies, second is usually 2.2, all are\
            positive)
        bgcolor: Desired background color for alpha-channel images, in 7-character\
            hex RGB format (e.g., ``#ff7700'' for orange: same as HTML colors)
        text_flag: Prompt interactively for text info (tEXt chunks)
        time_flag: Include a tIME chunk (last modification time)
        interlace_flag: Write interlaced PNG image

    Returns:
        Parameter dictionary.
    """
    params: Wpng = {
        "@type": "fsl/wpng",
        "text_flag": text_flag,
        "time_flag": time_flag,
        "interlace_flag": interlace_flag,
    }
    if input_file is not None:
        params["input_file"] = input_file
    if gamma is not None:
        params["gamma"] = gamma
    if bgcolor is not None:
        params["bgcolor"] = bgcolor
    return params

def wpng_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Wpng."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is not None:
        if not isinstance(params["input_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("gamma", None) is not None:
        if not isinstance(params["gamma"], (float, int)):
            raise StyxValidationError(f'`gamma` has the wrong type: Received `{type(params["gamma"])}` expected `float`')
    if params.get("bgcolor", None) is not None:
        if not isinstance(params["bgcolor"], str):
            raise StyxValidationError(f'`bgcolor` has the wrong type: Received `{type(params["bgcolor"])}` expected `str`')
    if params.get("text_flag", None) is not None:
        if not isinstance(params["text_flag"], bool):
            raise StyxValidationError(f'`text_flag` has the wrong type: Received `{type(params["text_flag"])}` expected `bool`')
    if params.get("time_flag", None) is not None:
        if not isinstance(params["time_flag"], bool):
            raise StyxValidationError(f'`time_flag` has the wrong type: Received `{type(params["time_flag"])}` expected `bool`')
    if params.get("interlace_flag", None) is not None:
        if not isinstance(params["interlace_flag"], bool):
            raise StyxValidationError(f'`interlace_flag` has the wrong type: Received `{type(params["interlace_flag"])}` expected `bool`')

def wpng_cargs(params: Wpng, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("wpng")
    v_0 = params.get("input_file")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("gamma")
    if v_1 is not None:
        cargs.append("-gamma")
        cargs.append(str(v_1))
    v_2 = params.get("bgcolor")
    if v_2 is not None:
        cargs.append("-bgcolor")
        cargs.append(v_2)
    if params.get("text_flag"):
        cargs.append("-text")
    if params.get("time_flag"):
        cargs.append("-time")
    if params.get("interlace_flag"):
        cargs.append("-interlace")
    return cargs

def wpng_outputs(params: Wpng, execution: Execution) -> WpngOutputs:
    """Build the WpngOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("[INPUT_FILE_BASE_NAME].png")
    return WpngOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def wpng_execute(params: Wpng, runner: Runner | None = None) -> WpngOutputs:
    """
    wpng

    Simple PGM/PPM/PAM to PNG Converter

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    wpng_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WPNG_METADATA)
    execution.params(params)
    args = wpng_cargs(params, execution)
    out = wpng_outputs(params, execution)
    execution.run(args)
    return out

def wpng(
    input_file: InputPathType | None = None,
    gamma: float | None = None,
    bgcolor: str | None = None,
    text_flag: bool = False,
    time_flag: bool = False,
    interlace_flag: bool = False,
    runner: Runner | None = None,
) -> WpngOutputs:
    """
    wpng

    Simple PGM/PPM/PAM to PNG Converter

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input PNM file (binary PGM 'P5', PPM 'P6' or PAM 'P8')
        gamma: Transfer-function exponent (``gamma'') of the image in\
            floating-point format (e.g., ``0.45455''). If image looks correct on\
            given display system, image gamma is equal to inverse of display-system\
            exponent, i.e., 1 / (LUT * CRT) (where LUT = lookup-table exponent and\
            CRT = CRT exponent; first varies, second is usually 2.2, all are\
            positive)
        bgcolor: Desired background color for alpha-channel images, in 7-character\
            hex RGB format (e.g., ``#ff7700'' for orange: same as HTML colors)
        text_flag: Prompt interactively for text info (tEXt chunks)
        time_flag: Include a tIME chunk (last modification time)
        interlace_flag: Write interlaced PNG image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = wpng_params(
        input_file=input_file,
        gamma=gamma,
        bgcolor=bgcolor,
        text_flag=text_flag,
        time_flag=time_flag,
        interlace_flag=interlace_flag,
    )
    return wpng_execute(params, runner)

__all__ = [
    "Wpng",
    "WpngOutputs",
    "WPNG_METADATA",
    "wpng_cargs",
    "wpng_outputs",
    "wpng_params",
    "wpng_execute",
    "wpng_validate",
    "wpng",
]