# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLHD_METADATA = Metadata(
    id="fslhd",
    name="fslhd",
    package="fsl",
)

Fslhd = typing.TypedDict(
    "Fslhd",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslhd"]],
        "xml_flag": typing.NotRequired[bool],
        "input_file": InputPathType,
    },
)

@dataclasses.dataclass
class FslhdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslhd_params(
    input_file: InputPathType,
    xml_flag: bool = False,
) -> Fslhd:
    """
    Build parameters.

    Args:
        input_file: Input NIFTI file
        xml_flag: Print an XML-style NIFTI header

    Returns:
        Parameter dictionary.
    """
    params: Fslhd = {
        "@type": "fsl/fslhd",
        "input_file": input_file,
        "xml_flag": xml_flag,
    }
    return params

def fslhd_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslhd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("xml_flag", None) is not None:
        if not isinstance(params["xml_flag"], bool):
            raise StyxValidationError(f'`xml_flag` has the wrong type: Received `{type(params["xml_flag"])}` expected `bool`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')

def fslhd_cargs(params: Fslhd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslhd")
    if params.get("xml_flag"):
        cargs.append("-x")
    cargs.append(execution.input_file(params["input_file"]))
    return cargs

def fslhd_outputs(params: Fslhd, execution: Execution) -> FslhdOutputs:
    """Build the FslhdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslhdOutputs(
        root=root_v,
    )

def fslhd_execute(params: Fslhd, runner: Runner | None = None) -> FslhdOutputs:
    """
    fslhd

    Display header information from a NIFTI file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslhd_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLHD_METADATA)
    execution.params(params)
    args = fslhd_cargs(params, execution)
    out = fslhd_outputs(params, execution)
    execution.run(args)
    return out

def fslhd(
    input_file: InputPathType,
    xml_flag: bool = False,
    runner: Runner | None = None,
) -> FslhdOutputs:
    """
    fslhd

    Display header information from a NIFTI file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input NIFTI file
        xml_flag: Print an XML-style NIFTI header
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslhd_params(
        input_file=input_file,
        xml_flag=xml_flag,
    )
    return fslhd_execute(params, runner)

__all__ = [
    "Fslhd",
    "FslhdOutputs",
    "FSLHD_METADATA",
    "fslhd_cargs",
    "fslhd_outputs",
    "fslhd_params",
    "fslhd_execute",
    "fslhd_validate",
    "fslhd",
]