# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POSSUM_INTERPMOT_PY_METADATA = Metadata(
    id="possum_interpmot.py",
    name="possum_interpmot.py",
    package="fsl",
)

PossumInterpmotPy = typing.TypedDict(
    "PossumInterpmotPy",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/possum_interpmot.py"]],
        "motion_type": int,
        "tr": float,
        "tr_slice": float,
        "nslices": int,
        "nvols": int,
        "custom_motion_file": InputPathType,
        "output_file": str,
    },
)

@dataclasses.dataclass
class PossumInterpmotPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Interpolated motion output file"""

def possum_interpmot_py_params(
    motion_type: int,
    tr: float,
    tr_slice: float,
    nslices: int,
    nvols: int,
    custom_motion_file: InputPathType,
    output_file: str,
) -> PossumInterpmotPy:
    """
    Build parameters.

    Args:
        motion_type: Type of motion: 0 for continuous, 1 for between slices, 2 for\
            between volumes
        tr: Repetition time in seconds
        tr_slice: Slice repetition time in seconds
        nslices: Number of slices
        nvols: Number of volumes
        custom_motion_file: Custom motion file
        output_file: Output file

    Returns:
        Parameter dictionary.
    """
    params: PossumInterpmotPy = {
        "@type": "fsl/possum_interpmot.py",
        "motion_type": motion_type,
        "tr": tr,
        "tr_slice": tr_slice,
        "nslices": nslices,
        "nvols": nvols,
        "custom_motion_file": custom_motion_file,
        "output_file": output_file,
    }
    return params

def possum_interpmot_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PossumInterpmotPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("motion_type", None) is None:
        raise StyxValidationError("`motion_type` must not be None")
    if not isinstance(params["motion_type"], int):
        raise StyxValidationError(f'`motion_type` has the wrong type: Received `{type(params["motion_type"])}` expected `int`')
    if not (0 <= params["motion_type"] <= 2):
        raise StyxValidationError("Parameter `motion_type` must be between 0 and 2 (inclusive)")
    if params.get("tr", None) is None:
        raise StyxValidationError("`tr` must not be None")
    if not isinstance(params["tr"], (float, int)):
        raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("tr_slice", None) is None:
        raise StyxValidationError("`tr_slice` must not be None")
    if not isinstance(params["tr_slice"], (float, int)):
        raise StyxValidationError(f'`tr_slice` has the wrong type: Received `{type(params["tr_slice"])}` expected `float`')
    if params.get("nslices", None) is None:
        raise StyxValidationError("`nslices` must not be None")
    if not isinstance(params["nslices"], int):
        raise StyxValidationError(f'`nslices` has the wrong type: Received `{type(params["nslices"])}` expected `int`')
    if params.get("nvols", None) is None:
        raise StyxValidationError("`nvols` must not be None")
    if not isinstance(params["nvols"], int):
        raise StyxValidationError(f'`nvols` has the wrong type: Received `{type(params["nvols"])}` expected `int`')
    if params.get("custom_motion_file", None) is None:
        raise StyxValidationError("`custom_motion_file` must not be None")
    if not isinstance(params["custom_motion_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`custom_motion_file` has the wrong type: Received `{type(params["custom_motion_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def possum_interpmot_py_cargs(params: PossumInterpmotPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("possum_interpmot.py")
    cargs.append(str(params["motion_type"]))
    cargs.append(str(params["tr"]))
    cargs.append(str(params["tr_slice"]))
    cargs.append(str(params["nslices"]))
    cargs.append(str(params["nvols"]))
    cargs.append(execution.input_file(params["custom_motion_file"]))
    cargs.append(params["output_file"])
    return cargs

def possum_interpmot_py_outputs(params: PossumInterpmotPy, execution: Execution) -> PossumInterpmotPyOutputs:
    """Build the PossumInterpmotPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["output_file"])
    return PossumInterpmotPyOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def possum_interpmot_py_execute(params: PossumInterpmotPy, runner: Runner | None = None) -> PossumInterpmotPyOutputs:
    """
    possum_interpmot.py

    Position Interpolation for Movers and Shakers

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    possum_interpmot_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POSSUM_INTERPMOT_PY_METADATA)
    execution.params(params)
    args = possum_interpmot_py_cargs(params, execution)
    out = possum_interpmot_py_outputs(params, execution)
    execution.run(args)
    return out

def possum_interpmot_py(
    motion_type: int,
    tr: float,
    tr_slice: float,
    nslices: int,
    nvols: int,
    custom_motion_file: InputPathType,
    output_file: str,
    runner: Runner | None = None,
) -> PossumInterpmotPyOutputs:
    """
    possum_interpmot.py

    Position Interpolation for Movers and Shakers

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        motion_type: Type of motion: 0 for continuous, 1 for between slices, 2 for\
            between volumes
        tr: Repetition time in seconds
        tr_slice: Slice repetition time in seconds
        nslices: Number of slices
        nvols: Number of volumes
        custom_motion_file: Custom motion file
        output_file: Output file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = possum_interpmot_py_params(
        motion_type=motion_type,
        tr=tr,
        tr_slice=tr_slice,
        nslices=nslices,
        nvols=nvols,
        custom_motion_file=custom_motion_file,
        output_file=output_file,
    )
    return possum_interpmot_py_execute(params, runner)

__all__ = [
    "PossumInterpmotPy",
    "PossumInterpmotPyOutputs",
    "POSSUM_INTERPMOT_PY_METADATA",
    "possum_interpmot_py_cargs",
    "possum_interpmot_py_outputs",
    "possum_interpmot_py_params",
    "possum_interpmot_py_execute",
    "possum_interpmot_py_validate",
    "possum_interpmot_py",
]