# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SLICES_METADATA = Metadata(
    id="slices",
    name="slices",
    package="fsl",
)

Slices = typing.TypedDict(
    "Slices",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/slices"]],
        "primary_input": InputPathType,
        "secondary_input": typing.NotRequired[InputPathType],
        "scale_factor": typing.NotRequired[float],
        "intensity_range": typing.NotRequired[list[float]],
        "output_gif": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SlicesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def slices_params(
    primary_input: InputPathType,
    secondary_input: InputPathType | None = None,
    scale_factor: float | None = None,
    intensity_range: list[float] | None = None,
    output_gif: str | None = None,
) -> Slices:
    """
    Build parameters.

    Args:
        primary_input: Primary input image file (e.g. img1.nii.gz)
        secondary_input: Secondary input image file (e.g. img2.nii.gz)
        scale_factor: Scale factor to apply to images.
        intensity_range: Intensity range to consider (minimum and maximum values).
        output_gif: Output GIF file.

    Returns:
        Parameter dictionary.
    """
    params: Slices = {
        "@type": "fsl/slices",
        "primary_input": primary_input,
    }
    if secondary_input is not None:
        params["secondary_input"] = secondary_input
    if scale_factor is not None:
        params["scale_factor"] = scale_factor
    if intensity_range is not None:
        params["intensity_range"] = intensity_range
    if output_gif is not None:
        params["output_gif"] = output_gif
    return params

def slices_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Slices."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("primary_input", None) is None:
        raise StyxValidationError("`primary_input` must not be None")
    if not isinstance(params["primary_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`primary_input` has the wrong type: Received `{type(params["primary_input"])}` expected `InputPathType`')
    if params.get("secondary_input", None) is not None:
        if not isinstance(params["secondary_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`secondary_input` has the wrong type: Received `{type(params["secondary_input"])}` expected `InputPathType`')
    if params.get("scale_factor", None) is not None:
        if not isinstance(params["scale_factor"], (float, int)):
            raise StyxValidationError(f'`scale_factor` has the wrong type: Received `{type(params["scale_factor"])}` expected `float`')
    if params.get("intensity_range", None) is not None:
        if not isinstance(params["intensity_range"], list):
            raise StyxValidationError(f'`intensity_range` has the wrong type: Received `{type(params["intensity_range"])}` expected `list[float]`')
        if not (2 <= len(params["intensity_range"]) <= 2):
            raise StyxValidationError("Parameter `intensity_range` must contain between 2 and 2 elements (inclusive)")
        for e in params["intensity_range"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`intensity_range` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("output_gif", None) is not None:
        if not isinstance(params["output_gif"], str):
            raise StyxValidationError(f'`output_gif` has the wrong type: Received `{type(params["output_gif"])}` expected `str`')

def slices_cargs(params: Slices, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("slices")
    cargs.append(execution.input_file(params["primary_input"]))
    v_0 = params.get("secondary_input")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("scale_factor")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(str(v_1))
    v_2 = params.get("intensity_range")
    if v_2 is not None:
        cargs.append("-i")
        for item0 in v_2:
            cargs.append(str(item0))
    v_3 = params.get("output_gif")
    if v_3 is not None:
        cargs.append("-o")
        cargs.append(v_3)
    return cargs

def slices_outputs(params: Slices, execution: Execution) -> SlicesOutputs:
    """Build the SlicesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SlicesOutputs(
        root=root_v,
    )

def slices_execute(params: Slices, runner: Runner | None = None) -> SlicesOutputs:
    """
    slices

    Generate a set of slices from an image, possibly with some scaling and intensity range options, and save as a GIF.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    slices_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SLICES_METADATA)
    execution.params(params)
    args = slices_cargs(params, execution)
    out = slices_outputs(params, execution)
    execution.run(args)
    return out

def slices(
    primary_input: InputPathType,
    secondary_input: InputPathType | None = None,
    scale_factor: float | None = None,
    intensity_range: list[float] | None = None,
    output_gif: str | None = None,
    runner: Runner | None = None,
) -> SlicesOutputs:
    """
    slices

    Generate a set of slices from an image, possibly with some scaling and intensity range options, and save as a GIF.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        primary_input: Primary input image file (e.g. img1.nii.gz)
        secondary_input: Secondary input image file (e.g. img2.nii.gz)
        scale_factor: Scale factor to apply to images.
        intensity_range: Intensity range to consider (minimum and maximum values).
        output_gif: Output GIF file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = slices_params(
        primary_input=primary_input,
        secondary_input=secondary_input,
        scale_factor=scale_factor,
        intensity_range=intensity_range,
        output_gif=output_gif,
    )
    return slices_execute(params, runner)

__all__ = [
    "Slices",
    "SlicesOutputs",
    "SLICES_METADATA",
    "slices_cargs",
    "slices_outputs",
    "slices_params",
    "slices_execute",
    "slices_validate",
    "slices",
]