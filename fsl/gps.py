# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

GPS_METADATA = Metadata(
    id="gps",
    name="gps",
    package="fsl",
)

Gps = typing.TypedDict(
    "Gps",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/gps"]],
        "ndir": float,
        "optws": typing.NotRequired[bool],
        "output": typing.NotRequired[str],
        "ranseed": typing.NotRequired[float],
        "init": typing.NotRequired[InputPathType],
        "report": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class GpsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType | None
    """Output file with generated directions"""

def gps_params(
    ndir: float,
    optws: bool = False,
    output: str | None = None,
    ranseed: float | None = None,
    init: InputPathType | None = None,
    report: bool = False,
    verbose: bool = False,
    help: bool = False,
) -> Gps:
    """
    Build parameters.

    Args:
        ndir: Number of directions
        optws: Perform additional optimisation on the whole sphere (needed for\
            eddy)
        output: Name of output file (default: bvecs#.txt)
        ranseed: Seed random generator with supplied number
        init: File with bvecs to use as initialisation
        report: Report coulomb forces for initial configuration
        verbose: Switch on diagnostic messages
        help: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Gps = {
        "@type": "fsl/gps",
        "ndir": ndir,
        "optws": optws,
        "report": report,
        "verbose": verbose,
        "help": help,
    }
    if output is not None:
        params["output"] = output
    if ranseed is not None:
        params["ranseed"] = ranseed
    if init is not None:
        params["init"] = init
    return params

def gps_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Gps."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ndir", None) is None:
        raise StyxValidationError("`ndir` must not be None")
    if not isinstance(params["ndir"], (float, int)):
        raise StyxValidationError(f'`ndir` has the wrong type: Received `{type(params["ndir"])}` expected `float`')
    if params.get("optws", None) is not None:
        if not isinstance(params["optws"], bool):
            raise StyxValidationError(f'`optws` has the wrong type: Received `{type(params["optws"])}` expected `bool`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("ranseed", None) is not None:
        if not isinstance(params["ranseed"], (float, int)):
            raise StyxValidationError(f'`ranseed` has the wrong type: Received `{type(params["ranseed"])}` expected `float`')
    if params.get("init", None) is not None:
        if not isinstance(params["init"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init` has the wrong type: Received `{type(params["init"])}` expected `InputPathType`')
    if params.get("report", None) is not None:
        if not isinstance(params["report"], bool):
            raise StyxValidationError(f'`report` has the wrong type: Received `{type(params["report"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def gps_cargs(params: Gps, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("gps")
    cargs.append("--ndir")
    cargs.append(str(params["ndir"]))
    if params.get("optws"):
        cargs.append("--optws")
    v_0 = params.get("output")
    if v_0 is not None:
        cargs.append("--out")
        cargs.append(v_0)
    v_1 = params.get("ranseed")
    if v_1 is not None:
        cargs.append("--ranseed")
        cargs.append(str(v_1))
    v_2 = params.get("init")
    if v_2 is not None:
        cargs.append("--init")
        cargs.append(execution.input_file(v_2))
    if params.get("report"):
        cargs.append("--report")
    if params.get("verbose"):
        cargs.append("-v,--verbose")
    if params.get("help"):
        cargs.append("-h,--help")
    return cargs

def gps_outputs(params: Gps, execution: Execution) -> GpsOutputs:
    """Build the GpsOutputs object for this tool."""
    output_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        output_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return GpsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def gps_execute(params: Gps, runner: Runner | None = None) -> GpsOutputs:
    """
    gps

    Generate set of diffusion gradient directions

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    gps_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(GPS_METADATA)
    execution.params(params)
    args = gps_cargs(params, execution)
    out = gps_outputs(params, execution)
    execution.run(args)
    return out

def gps(
    ndir: float,
    optws: bool = False,
    output: str | None = None,
    ranseed: float | None = None,
    init: InputPathType | None = None,
    report: bool = False,
    verbose: bool = False,
    help: bool = False,
    runner: Runner | None = None,
) -> GpsOutputs:
    """
    gps

    Generate set of diffusion gradient directions

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        ndir: Number of directions
        optws: Perform additional optimisation on the whole sphere (needed for\
            eddy)
        output: Name of output file (default: bvecs#.txt)
        ranseed: Seed random generator with supplied number
        init: File with bvecs to use as initialisation
        report: Report coulomb forces for initial configuration
        verbose: Switch on diagnostic messages
        help: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = gps_params(
        ndir=ndir,
        optws=optws,
        output=output,
        ranseed=ranseed,
        init=init,
        report=report,
        verbose=verbose,
        help=help,
    )
    return gps_execute(params, runner)

__all__ = [
    "Gps",
    "GpsOutputs",
    "GPS_METADATA",
    "gps_cargs",
    "gps_outputs",
    "gps_params",
    "gps_execute",
    "gps_validate",
    "gps",
]