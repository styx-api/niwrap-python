# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLINTERLEAVE_METADATA = Metadata(
    id="fslinterleave",
    name="fslinterleave",
    package="fsl",
)

Fslinterleave = typing.TypedDict(
    "Fslinterleave",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslinterleave"]],
        "infile1": InputPathType,
        "infile2": InputPathType,
        "outfile": str,
        "reverse_slice_order_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FslinterleaveOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    interleaved_output: OutputPathType
    """Interleaved output image"""

def fslinterleave_params(
    infile1: InputPathType,
    infile2: InputPathType,
    outfile: str,
    reverse_slice_order_flag: bool = False,
) -> Fslinterleave:
    """
    Build parameters.

    Args:
        infile1: First input image
        infile2: Second input image
        outfile: Output interleaved image
        reverse_slice_order_flag: Reverse slice order

    Returns:
        Parameter dictionary.
    """
    params: Fslinterleave = {
        "@type": "fsl/fslinterleave",
        "infile1": infile1,
        "infile2": infile2,
        "outfile": outfile,
        "reverse_slice_order_flag": reverse_slice_order_flag,
    }
    return params

def fslinterleave_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslinterleave."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile1", None) is None:
        raise StyxValidationError("`infile1` must not be None")
    if not isinstance(params["infile1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile1` has the wrong type: Received `{type(params["infile1"])}` expected `InputPathType`')
    if params.get("infile2", None) is None:
        raise StyxValidationError("`infile2` must not be None")
    if not isinstance(params["infile2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile2` has the wrong type: Received `{type(params["infile2"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("reverse_slice_order_flag", None) is not None:
        if not isinstance(params["reverse_slice_order_flag"], bool):
            raise StyxValidationError(f'`reverse_slice_order_flag` has the wrong type: Received `{type(params["reverse_slice_order_flag"])}` expected `bool`')

def fslinterleave_cargs(params: Fslinterleave, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslinterleave")
    cargs.append(execution.input_file(params["infile1"]))
    cargs.append(execution.input_file(params["infile2"]))
    cargs.append(params["outfile"])
    if params.get("reverse_slice_order_flag"):
        cargs.append("-i")
    return cargs

def fslinterleave_outputs(params: Fslinterleave, execution: Execution) -> FslinterleaveOutputs:
    """Build the FslinterleaveOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    interleaved_output_v: OutputPathType = execution.output_file(f'{params["outfile"]}.nii.gz')
    return FslinterleaveOutputs(
        root=root_v,
        interleaved_output=interleaved_output_v,
    )

def fslinterleave_execute(params: Fslinterleave, runner: Runner | None = None) -> FslinterleaveOutputs:
    """
    fslinterleave

    Interleaves two input images slice-by-slice to produce an output image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslinterleave_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLINTERLEAVE_METADATA)
    execution.params(params)
    args = fslinterleave_cargs(params, execution)
    out = fslinterleave_outputs(params, execution)
    execution.run(args)
    return out

def fslinterleave(
    infile1: InputPathType,
    infile2: InputPathType,
    outfile: str,
    reverse_slice_order_flag: bool = False,
    runner: Runner | None = None,
) -> FslinterleaveOutputs:
    """
    fslinterleave

    Interleaves two input images slice-by-slice to produce an output image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile1: First input image
        infile2: Second input image
        outfile: Output interleaved image
        reverse_slice_order_flag: Reverse slice order
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslinterleave_params(
        infile1=infile1,
        infile2=infile2,
        outfile=outfile,
        reverse_slice_order_flag=reverse_slice_order_flag,
    )
    return fslinterleave_execute(params, runner)

__all__ = [
    "Fslinterleave",
    "FslinterleaveOutputs",
    "FSLINTERLEAVE_METADATA",
    "fslinterleave_cargs",
    "fslinterleave_outputs",
    "fslinterleave_params",
    "fslinterleave_execute",
    "fslinterleave_validate",
    "fslinterleave",
]