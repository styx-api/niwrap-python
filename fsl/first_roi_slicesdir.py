# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIRST_ROI_SLICESDIR_METADATA = Metadata(
    id="first_roi_slicesdir",
    name="first_roi_slicesdir",
    package="fsl",
)

FirstRoiSlicesdir = typing.TypedDict(
    "FirstRoiSlicesdir",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/first_roi_slicesdir"]],
        "input_t1_images": str,
        "input_label_images": str,
    },
)

@dataclasses.dataclass
class FirstRoiSlicesdirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    t1_slicesdir: OutputPathType
    """Generated slice directory for the input T1 images"""
    label_slicesdir: OutputPathType
    """Generated slice directory for the input label images"""

def first_roi_slicesdir_params(
    input_t1_images: str,
    input_label_images: str,
) -> FirstRoiSlicesdir:
    """
    Build parameters.

    Args:
        input_t1_images: Input T1-weighted images of the brain (pattern-matched);\
            for example, *_t1.nii.gz
        input_label_images: Input label images corresponding to the T1 images\
            (pattern-matched); for example, *_L_Hipp_first.nii.gz

    Returns:
        Parameter dictionary.
    """
    params: FirstRoiSlicesdir = {
        "@type": "fsl/first_roi_slicesdir",
        "input_t1_images": input_t1_images,
        "input_label_images": input_label_images,
    }
    return params

def first_roi_slicesdir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FirstRoiSlicesdir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_t1_images", None) is None:
        raise StyxValidationError("`input_t1_images` must not be None")
    if not isinstance(params["input_t1_images"], str):
        raise StyxValidationError(f'`input_t1_images` has the wrong type: Received `{type(params["input_t1_images"])}` expected `str`')
    if params.get("input_label_images", None) is None:
        raise StyxValidationError("`input_label_images` must not be None")
    if not isinstance(params["input_label_images"], str):
        raise StyxValidationError(f'`input_label_images` has the wrong type: Received `{type(params["input_label_images"])}` expected `str`')

def first_roi_slicesdir_cargs(params: FirstRoiSlicesdir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("first_roi_slicesdir")
    cargs.append(params["input_t1_images"])
    cargs.append(params["input_label_images"])
    return cargs

def first_roi_slicesdir_outputs(params: FirstRoiSlicesdir, execution: Execution) -> FirstRoiSlicesdirOutputs:
    """Build the FirstRoiSlicesdirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    t1_slicesdir_v: OutputPathType = execution.output_file(f'{params["input_t1_images"]}_slicesdir')
    label_slicesdir_v: OutputPathType = execution.output_file(f'{params["input_label_images"]}_slicesdir')
    return FirstRoiSlicesdirOutputs(
        root=root_v,
        t1_slicesdir=t1_slicesdir_v,
        label_slicesdir=label_slicesdir_v,
    )

def first_roi_slicesdir_execute(params: FirstRoiSlicesdir, runner: Runner | None = None) -> FirstRoiSlicesdirOutputs:
    """
    first_roi_slicesdir

    A utility for generating slice directories for FIRST-ROI.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    first_roi_slicesdir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIRST_ROI_SLICESDIR_METADATA)
    execution.params(params)
    args = first_roi_slicesdir_cargs(params, execution)
    out = first_roi_slicesdir_outputs(params, execution)
    execution.run(args)
    return out

def first_roi_slicesdir(
    input_t1_images: str,
    input_label_images: str,
    runner: Runner | None = None,
) -> FirstRoiSlicesdirOutputs:
    """
    first_roi_slicesdir

    A utility for generating slice directories for FIRST-ROI.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_t1_images: Input T1-weighted images of the brain (pattern-matched);\
            for example, *_t1.nii.gz
        input_label_images: Input label images corresponding to the T1 images\
            (pattern-matched); for example, *_L_Hipp_first.nii.gz
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = first_roi_slicesdir_params(
        input_t1_images=input_t1_images,
        input_label_images=input_label_images,
    )
    return first_roi_slicesdir_execute(params, runner)

__all__ = [
    "FirstRoiSlicesdir",
    "FirstRoiSlicesdirOutputs",
    "FIRST_ROI_SLICESDIR_METADATA",
    "first_roi_slicesdir_cargs",
    "first_roi_slicesdir_outputs",
    "first_roi_slicesdir_params",
    "first_roi_slicesdir_execute",
    "first_roi_slicesdir_validate",
    "first_roi_slicesdir",
]