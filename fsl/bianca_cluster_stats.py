# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BIANCA_CLUSTER_STATS_METADATA = Metadata(
    id="bianca_cluster_stats",
    name="bianca_cluster_stats",
    package="fsl",
)

BiancaClusterStats = typing.TypedDict(
    "BiancaClusterStats",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/bianca_cluster_stats"]],
        "bianca_output_map": InputPathType,
        "threshold": float,
        "min_cluster_size": float,
        "mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class BiancaClusterStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def bianca_cluster_stats_params(
    bianca_output_map: InputPathType,
    threshold: float,
    min_cluster_size: float,
    mask: InputPathType | None = None,
) -> BiancaClusterStats:
    """
    Build parameters.

    Args:
        bianca_output_map: BIANCA output map file
        threshold: Threshold value to apply
        min_cluster_size: Minimum cluster size in voxels
        mask: Optional mask file (in the same space as the BIANCA output map)

    Returns:
        Parameter dictionary.
    """
    params: BiancaClusterStats = {
        "@type": "fsl/bianca_cluster_stats",
        "bianca_output_map": bianca_output_map,
        "threshold": threshold,
        "min_cluster_size": min_cluster_size,
    }
    if mask is not None:
        params["mask"] = mask
    return params

def bianca_cluster_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BiancaClusterStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("bianca_output_map", None) is None:
        raise StyxValidationError("`bianca_output_map` must not be None")
    if not isinstance(params["bianca_output_map"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bianca_output_map` has the wrong type: Received `{type(params["bianca_output_map"])}` expected `InputPathType`')
    if params.get("threshold", None) is None:
        raise StyxValidationError("`threshold` must not be None")
    if not isinstance(params["threshold"], (float, int)):
        raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("min_cluster_size", None) is None:
        raise StyxValidationError("`min_cluster_size` must not be None")
    if not isinstance(params["min_cluster_size"], (float, int)):
        raise StyxValidationError(f'`min_cluster_size` has the wrong type: Received `{type(params["min_cluster_size"])}` expected `float`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')

def bianca_cluster_stats_cargs(params: BiancaClusterStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bianca_cluster_stats")
    cargs.append(execution.input_file(params["bianca_output_map"]))
    cargs.append(str(params["threshold"]))
    cargs.append(str(params["min_cluster_size"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def bianca_cluster_stats_outputs(params: BiancaClusterStats, execution: Execution) -> BiancaClusterStatsOutputs:
    """Build the BiancaClusterStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BiancaClusterStatsOutputs(
        root=root_v,
    )

def bianca_cluster_stats_execute(params: BiancaClusterStats, runner: Runner | None = None) -> BiancaClusterStatsOutputs:
    """
    bianca_cluster_stats

    Calculate number of clusters and WMH volume in a BIANCA output map

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bianca_cluster_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BIANCA_CLUSTER_STATS_METADATA)
    execution.params(params)
    args = bianca_cluster_stats_cargs(params, execution)
    out = bianca_cluster_stats_outputs(params, execution)
    execution.run(args)
    return out

def bianca_cluster_stats(
    bianca_output_map: InputPathType,
    threshold: float,
    min_cluster_size: float,
    mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> BiancaClusterStatsOutputs:
    """
    bianca_cluster_stats

    Calculate number of clusters and WMH volume in a BIANCA output map

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        bianca_output_map: BIANCA output map file
        threshold: Threshold value to apply
        min_cluster_size: Minimum cluster size in voxels
        mask: Optional mask file (in the same space as the BIANCA output map)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bianca_cluster_stats_params(
        bianca_output_map=bianca_output_map,
        threshold=threshold,
        min_cluster_size=min_cluster_size,
        mask=mask,
    )
    return bianca_cluster_stats_execute(params, runner)

__all__ = [
    "BiancaClusterStats",
    "BiancaClusterStatsOutputs",
    "BIANCA_CLUSTER_STATS_METADATA",
    "bianca_cluster_stats_cargs",
    "bianca_cluster_stats_outputs",
    "bianca_cluster_stats_params",
    "bianca_cluster_stats_execute",
    "bianca_cluster_stats_validate",
    "bianca_cluster_stats",
]