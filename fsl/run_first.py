# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RUN_FIRST_METADATA = Metadata(
    id="run_first",
    name="run_first",
    package="fsl",
)

RunFirst = typing.TypedDict(
    "RunFirst",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/run_first"]],
        "input_image": InputPathType,
        "transformation_matrix": InputPathType,
        "n_modes": float,
        "output_basename": str,
        "model_name": InputPathType,
        "verbose_flag": typing.NotRequired[bool],
        "intref_model_name": typing.NotRequired[str],
        "load_bvars": typing.NotRequired[InputPathType],
        "multiple_images_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RunFirstOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def run_first_params(
    input_image: InputPathType,
    transformation_matrix: InputPathType,
    n_modes: float,
    output_basename: str,
    model_name: InputPathType,
    verbose_flag: bool = False,
    intref_model_name: str | None = None,
    load_bvars: InputPathType | None = None,
    multiple_images_flag: bool = False,
) -> RunFirst:
    """
    Build parameters.

    Args:
        input_image: Input image file (e.g. img.nii.gz)
        transformation_matrix: Input transformation matrix file (e.g.\
            input_to_mni.mat)
        n_modes: Number of modes
        output_basename: Output basename
        model_name: Model name
        verbose_flag: Verbose mode
        intref_model_name: Reference structure for the local intensity\
            normalization
        load_bvars: Initializes FIRST with a previous estimate of the structure
        multiple_images_flag: Run FIRST on multiple images; provide a list of\
            images, transformation matrices, and output names

    Returns:
        Parameter dictionary.
    """
    params: RunFirst = {
        "@type": "fsl/run_first",
        "input_image": input_image,
        "transformation_matrix": transformation_matrix,
        "n_modes": n_modes,
        "output_basename": output_basename,
        "model_name": model_name,
        "verbose_flag": verbose_flag,
        "multiple_images_flag": multiple_images_flag,
    }
    if intref_model_name is not None:
        params["intref_model_name"] = intref_model_name
    if load_bvars is not None:
        params["load_bvars"] = load_bvars
    return params

def run_first_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RunFirst."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("transformation_matrix", None) is None:
        raise StyxValidationError("`transformation_matrix` must not be None")
    if not isinstance(params["transformation_matrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`transformation_matrix` has the wrong type: Received `{type(params["transformation_matrix"])}` expected `InputPathType`')
    if params.get("n_modes", None) is None:
        raise StyxValidationError("`n_modes` must not be None")
    if not isinstance(params["n_modes"], (float, int)):
        raise StyxValidationError(f'`n_modes` has the wrong type: Received `{type(params["n_modes"])}` expected `float`')
    if params.get("output_basename", None) is None:
        raise StyxValidationError("`output_basename` must not be None")
    if not isinstance(params["output_basename"], str):
        raise StyxValidationError(f'`output_basename` has the wrong type: Received `{type(params["output_basename"])}` expected `str`')
    if params.get("model_name", None) is None:
        raise StyxValidationError("`model_name` must not be None")
    if not isinstance(params["model_name"], (pathlib.Path, str)):
        raise StyxValidationError(f'`model_name` has the wrong type: Received `{type(params["model_name"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("intref_model_name", None) is not None:
        if not isinstance(params["intref_model_name"], str):
            raise StyxValidationError(f'`intref_model_name` has the wrong type: Received `{type(params["intref_model_name"])}` expected `str`')
    if params.get("load_bvars", None) is not None:
        if not isinstance(params["load_bvars"], (pathlib.Path, str)):
            raise StyxValidationError(f'`load_bvars` has the wrong type: Received `{type(params["load_bvars"])}` expected `InputPathType`')
    if params.get("multiple_images_flag", None) is not None:
        if not isinstance(params["multiple_images_flag"], bool):
            raise StyxValidationError(f'`multiple_images_flag` has the wrong type: Received `{type(params["multiple_images_flag"])}` expected `bool`')

def run_first_cargs(params: RunFirst, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("run_first")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("-t")
    cargs.append(execution.input_file(params["transformation_matrix"]))
    cargs.append("-n")
    cargs.append(str(params["n_modes"]))
    cargs.append("-o")
    cargs.append(params["output_basename"])
    cargs.append("-m")
    cargs.append(execution.input_file(params["model_name"]))
    if params.get("verbose_flag"):
        cargs.append("-v")
    v_0 = params.get("intref_model_name")
    if v_0 is not None:
        cargs.append("-intref")
        cargs.append(v_0)
    v_1 = params.get("load_bvars")
    if v_1 is not None:
        cargs.append("-loadBvars")
        cargs.append(execution.input_file(v_1))
    if params.get("multiple_images_flag"):
        cargs.append("-multipleImages")
    return cargs

def run_first_outputs(params: RunFirst, execution: Execution) -> RunFirstOutputs:
    """Build the RunFirstOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return RunFirstOutputs(
        root=root_v,
    )

def run_first_execute(params: RunFirst, runner: Runner | None = None) -> RunFirstOutputs:
    """
    run_first

    A tool to run FSL's FIRST for subcortical segmentation

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    run_first_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RUN_FIRST_METADATA)
    execution.params(params)
    args = run_first_cargs(params, execution)
    out = run_first_outputs(params, execution)
    execution.run(args)
    return out

def run_first(
    input_image: InputPathType,
    transformation_matrix: InputPathType,
    n_modes: float,
    output_basename: str,
    model_name: InputPathType,
    verbose_flag: bool = False,
    intref_model_name: str | None = None,
    load_bvars: InputPathType | None = None,
    multiple_images_flag: bool = False,
    runner: Runner | None = None,
) -> RunFirstOutputs:
    """
    run_first

    A tool to run FSL's FIRST for subcortical segmentation

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Input image file (e.g. img.nii.gz)
        transformation_matrix: Input transformation matrix file (e.g.\
            input_to_mni.mat)
        n_modes: Number of modes
        output_basename: Output basename
        model_name: Model name
        verbose_flag: Verbose mode
        intref_model_name: Reference structure for the local intensity\
            normalization
        load_bvars: Initializes FIRST with a previous estimate of the structure
        multiple_images_flag: Run FIRST on multiple images; provide a list of\
            images, transformation matrices, and output names
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = run_first_params(
        input_image=input_image,
        transformation_matrix=transformation_matrix,
        n_modes=n_modes,
        output_basename=output_basename,
        model_name=model_name,
        verbose_flag=verbose_flag,
        intref_model_name=intref_model_name,
        load_bvars=load_bvars,
        multiple_images_flag=multiple_images_flag,
    )
    return run_first_execute(params, runner)

__all__ = [
    "RunFirst",
    "RunFirstOutputs",
    "RUN_FIRST_METADATA",
    "run_first_cargs",
    "run_first_outputs",
    "run_first_params",
    "run_first_execute",
    "run_first_validate",
    "run_first",
]