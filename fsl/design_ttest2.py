# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DESIGN_TTEST2_METADATA = Metadata(
    id="design_ttest2",
    name="design_ttest2",
    package="fsl",
)

DesignTtest2 = typing.TypedDict(
    "DesignTtest2",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/design_ttest2"]],
        "design_files_rootname": str,
        "ngroupa": float,
        "ngroupb": float,
        "include_mean_contrasts": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DesignTtest2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def design_ttest2_params(
    design_files_rootname: str,
    ngroupa: float,
    ngroupb: float,
    include_mean_contrasts: bool = False,
) -> DesignTtest2:
    """
    Build parameters.

    Args:
        design_files_rootname: Root name for design files
        ngroupa: Number of subjects in group A
        ngroupb: Number of subjects in group B
        include_mean_contrasts: Include individual group mean contrasts

    Returns:
        Parameter dictionary.
    """
    params: DesignTtest2 = {
        "@type": "fsl/design_ttest2",
        "design_files_rootname": design_files_rootname,
        "ngroupa": ngroupa,
        "ngroupb": ngroupb,
        "include_mean_contrasts": include_mean_contrasts,
    }
    return params

def design_ttest2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid DesignTtest2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("design_files_rootname", None) is None:
        raise StyxValidationError("`design_files_rootname` must not be None")
    if not isinstance(params["design_files_rootname"], str):
        raise StyxValidationError(f'`design_files_rootname` has the wrong type: Received `{type(params["design_files_rootname"])}` expected `str`')
    if params.get("ngroupa", None) is None:
        raise StyxValidationError("`ngroupa` must not be None")
    if not isinstance(params["ngroupa"], (float, int)):
        raise StyxValidationError(f'`ngroupa` has the wrong type: Received `{type(params["ngroupa"])}` expected `float`')
    if params.get("ngroupb", None) is None:
        raise StyxValidationError("`ngroupb` must not be None")
    if not isinstance(params["ngroupb"], (float, int)):
        raise StyxValidationError(f'`ngroupb` has the wrong type: Received `{type(params["ngroupb"])}` expected `float`')
    if params.get("include_mean_contrasts", None) is not None:
        if not isinstance(params["include_mean_contrasts"], bool):
            raise StyxValidationError(f'`include_mean_contrasts` has the wrong type: Received `{type(params["include_mean_contrasts"])}` expected `bool`')

def design_ttest2_cargs(params: DesignTtest2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("design_ttest2")
    cargs.append(params["design_files_rootname"])
    cargs.append(str(params["ngroupa"]))
    cargs.append(str(params["ngroupb"]))
    if params.get("include_mean_contrasts"):
        cargs.append("-m")
    return cargs

def design_ttest2_outputs(params: DesignTtest2, execution: Execution) -> DesignTtest2Outputs:
    """Build the DesignTtest2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DesignTtest2Outputs(
        root=root_v,
    )

def design_ttest2_execute(params: DesignTtest2, runner: Runner | None = None) -> DesignTtest2Outputs:
    """
    design_ttest2

    Command for generating group mean contrasts for a two-sample t-test design.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    design_ttest2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DESIGN_TTEST2_METADATA)
    execution.params(params)
    args = design_ttest2_cargs(params, execution)
    out = design_ttest2_outputs(params, execution)
    execution.run(args)
    return out

def design_ttest2(
    design_files_rootname: str,
    ngroupa: float,
    ngroupb: float,
    include_mean_contrasts: bool = False,
    runner: Runner | None = None,
) -> DesignTtest2Outputs:
    """
    design_ttest2

    Command for generating group mean contrasts for a two-sample t-test design.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        design_files_rootname: Root name for design files
        ngroupa: Number of subjects in group A
        ngroupb: Number of subjects in group B
        include_mean_contrasts: Include individual group mean contrasts
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = design_ttest2_params(
        design_files_rootname=design_files_rootname,
        ngroupa=ngroupa,
        ngroupb=ngroupb,
        include_mean_contrasts=include_mean_contrasts,
    )
    return design_ttest2_execute(params, runner)

__all__ = [
    "DesignTtest2",
    "DesignTtest2Outputs",
    "DESIGN_TTEST2_METADATA",
    "design_ttest2_cargs",
    "design_ttest2_outputs",
    "design_ttest2_params",
    "design_ttest2_execute",
    "design_ttest2_validate",
    "design_ttest2",
]