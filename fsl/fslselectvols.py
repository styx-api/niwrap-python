# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLSELECTVOLS_METADATA = Metadata(
    id="fslselectvols",
    name="fslselectvols",
    package="fsl",
)

Fslselectvols = typing.TypedDict(
    "Fslselectvols",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslselectvols"]],
        "input_file": InputPathType,
        "output_file": str,
        "vols_list": str,
        "output_mean_flag": typing.NotRequired[bool],
        "output_variance_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FslselectvolsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_4d_image: OutputPathType
    """Output 4D image with selected volumes"""

def fslselectvols_params(
    input_file: InputPathType,
    output_file: str,
    vols_list: str,
    output_mean_flag: bool = False,
    output_variance_flag: bool = False,
    help_flag: bool = False,
) -> Fslselectvols:
    """
    Build parameters.

    Args:
        input_file: Input file name (4D image)
        output_file: Output file name (4D image)
        vols_list: List of volumes to extract (comma-separated list or ascii file)
        output_mean_flag: Output mean instead of concatenation
        output_variance_flag: Output variance instead of concatenation
        help_flag: Display help text

    Returns:
        Parameter dictionary.
    """
    params: Fslselectvols = {
        "@type": "fsl/fslselectvols",
        "input_file": input_file,
        "output_file": output_file,
        "vols_list": vols_list,
        "output_mean_flag": output_mean_flag,
        "output_variance_flag": output_variance_flag,
        "help_flag": help_flag,
    }
    return params

def fslselectvols_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslselectvols."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("vols_list", None) is None:
        raise StyxValidationError("`vols_list` must not be None")
    if not isinstance(params["vols_list"], str):
        raise StyxValidationError(f'`vols_list` has the wrong type: Received `{type(params["vols_list"])}` expected `str`')
    if params.get("output_mean_flag", None) is not None:
        if not isinstance(params["output_mean_flag"], bool):
            raise StyxValidationError(f'`output_mean_flag` has the wrong type: Received `{type(params["output_mean_flag"])}` expected `bool`')
    if params.get("output_variance_flag", None) is not None:
        if not isinstance(params["output_variance_flag"], bool):
            raise StyxValidationError(f'`output_variance_flag` has the wrong type: Received `{type(params["output_variance_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def fslselectvols_cargs(params: Fslselectvols, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslselectvols")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-o")
    cargs.append(params["output_file"])
    cargs.append("--vols")
    cargs.append(params["vols_list"])
    if params.get("output_mean_flag"):
        cargs.append("-m")
    if params.get("output_variance_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def fslselectvols_outputs(params: Fslselectvols, execution: Execution) -> FslselectvolsOutputs:
    """Build the FslselectvolsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_4d_image_v: OutputPathType = execution.output_file(params["output_file"])
    return FslselectvolsOutputs(
        root=root_v,
        output_4d_image=output_4d_image_v,
    )

def fslselectvols_execute(params: Fslselectvols, runner: Runner | None = None) -> FslselectvolsOutputs:
    """
    fslselectvols

    Select volumes from a 4D time series and output a subset 4D volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslselectvols_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLSELECTVOLS_METADATA)
    execution.params(params)
    args = fslselectvols_cargs(params, execution)
    out = fslselectvols_outputs(params, execution)
    execution.run(args)
    return out

def fslselectvols(
    input_file: InputPathType,
    output_file: str,
    vols_list: str,
    output_mean_flag: bool = False,
    output_variance_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> FslselectvolsOutputs:
    """
    fslselectvols

    Select volumes from a 4D time series and output a subset 4D volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input file name (4D image)
        output_file: Output file name (4D image)
        vols_list: List of volumes to extract (comma-separated list or ascii file)
        output_mean_flag: Output mean instead of concatenation
        output_variance_flag: Output variance instead of concatenation
        help_flag: Display help text
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslselectvols_params(
        input_file=input_file,
        output_file=output_file,
        vols_list=vols_list,
        output_mean_flag=output_mean_flag,
        output_variance_flag=output_variance_flag,
        help_flag=help_flag,
    )
    return fslselectvols_execute(params, runner)

__all__ = [
    "Fslselectvols",
    "FslselectvolsOutputs",
    "FSLSELECTVOLS_METADATA",
    "fslselectvols_cargs",
    "fslselectvols_outputs",
    "fslselectvols_params",
    "fslselectvols_execute",
    "fslselectvols_validate",
    "fslselectvols",
]