# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BIANCA_OVERLAP_MEASURES_METADATA = Metadata(
    id="bianca_overlap_measures",
    name="bianca_overlap_measures",
    package="fsl",
)

BiancaOverlapMeasures = typing.TypedDict(
    "BiancaOverlapMeasures",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/bianca_overlap_measures"]],
        "lesion_mask": InputPathType,
        "manual_mask": InputPathType,
        "output_dir": str,
    },
)

@dataclasses.dataclass
class BiancaOverlapMeasuresOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tp_auto: OutputPathType
    """True positive voxels in automatic mask"""
    tp_manual: OutputPathType
    """True positive voxels in manual mask"""
    fp_vox_tp_overlap: OutputPathType
    """False positive voxels that overlap with true positives"""
    fn_vox_tp_overlap: OutputPathType
    """False negative voxels that overlap with true positives"""
    lesmask_idx: OutputPathType
    """Indexed lesion mask"""
    manualmask_idx: OutputPathType
    """Indexed manual mask"""
    lesmask_idx_tp: OutputPathType
    """True positive indexed lesion mask"""
    manualmask_idx_tp: OutputPathType
    """True positive indexed manual mask"""

def bianca_overlap_measures_params(
    lesion_mask: InputPathType,
    manual_mask: InputPathType,
    output_dir: str,
) -> BiancaOverlapMeasures:
    """
    Build parameters.

    Args:
        lesion_mask: Lesion mask image file
        manual_mask: Manual mask image file
        output_dir: Output directory for generated files

    Returns:
        Parameter dictionary.
    """
    params: BiancaOverlapMeasures = {
        "@type": "fsl/bianca_overlap_measures",
        "lesion_mask": lesion_mask,
        "manual_mask": manual_mask,
        "output_dir": output_dir,
    }
    return params

def bianca_overlap_measures_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BiancaOverlapMeasures."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("lesion_mask", None) is None:
        raise StyxValidationError("`lesion_mask` must not be None")
    if not isinstance(params["lesion_mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`lesion_mask` has the wrong type: Received `{type(params["lesion_mask"])}` expected `InputPathType`')
    if params.get("manual_mask", None) is None:
        raise StyxValidationError("`manual_mask` must not be None")
    if not isinstance(params["manual_mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`manual_mask` has the wrong type: Received `{type(params["manual_mask"])}` expected `InputPathType`')
    if params.get("output_dir", None) is None:
        raise StyxValidationError("`output_dir` must not be None")
    if not isinstance(params["output_dir"], str):
        raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')

def bianca_overlap_measures_cargs(params: BiancaOverlapMeasures, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bianca_overlap_measures")
    cargs.append(execution.input_file(params["lesion_mask"]))
    cargs.append(execution.input_file(params["manual_mask"]))
    cargs.append(params["output_dir"])
    return cargs

def bianca_overlap_measures_outputs(params: BiancaOverlapMeasures, execution: Execution) -> BiancaOverlapMeasuresOutputs:
    """Build the BiancaOverlapMeasuresOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tp_auto_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/TPauto.nii.gz")
    tp_manual_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/TPmanual.nii.gz")
    fp_vox_tp_overlap_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/FPvox_TP_overlap.nii.gz")
    fn_vox_tp_overlap_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/FNvox_TP_overlap.nii.gz")
    lesmask_idx_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/lesmask_idx.nii.gz")
    manualmask_idx_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/manualmask_idx.nii.gz")
    lesmask_idx_tp_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/lesmask_idx_TP.nii.gz")
    manualmask_idx_tp_v: OutputPathType = execution.output_file("{OUTPUT_DIR}/manualmask_idx_TP.nii.gz")
    return BiancaOverlapMeasuresOutputs(
        root=root_v,
        tp_auto=tp_auto_v,
        tp_manual=tp_manual_v,
        fp_vox_tp_overlap=fp_vox_tp_overlap_v,
        fn_vox_tp_overlap=fn_vox_tp_overlap_v,
        lesmask_idx=lesmask_idx_v,
        manualmask_idx=manualmask_idx_v,
        lesmask_idx_tp=lesmask_idx_tp_v,
        manualmask_idx_tp=manualmask_idx_tp_v,
    )

def bianca_overlap_measures_execute(params: BiancaOverlapMeasures, runner: Runner | None = None) -> BiancaOverlapMeasuresOutputs:
    """
    bianca_overlap_measures

    BIANCA overlap measures script for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bianca_overlap_measures_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BIANCA_OVERLAP_MEASURES_METADATA)
    execution.params(params)
    args = bianca_overlap_measures_cargs(params, execution)
    out = bianca_overlap_measures_outputs(params, execution)
    execution.run(args)
    return out

def bianca_overlap_measures(
    lesion_mask: InputPathType,
    manual_mask: InputPathType,
    output_dir: str,
    runner: Runner | None = None,
) -> BiancaOverlapMeasuresOutputs:
    """
    bianca_overlap_measures

    BIANCA overlap measures script for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        lesion_mask: Lesion mask image file
        manual_mask: Manual mask image file
        output_dir: Output directory for generated files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bianca_overlap_measures_params(
        lesion_mask=lesion_mask,
        manual_mask=manual_mask,
        output_dir=output_dir,
    )
    return bianca_overlap_measures_execute(params, runner)

__all__ = [
    "BiancaOverlapMeasures",
    "BiancaOverlapMeasuresOutputs",
    "BIANCA_OVERLAP_MEASURES_METADATA",
    "bianca_overlap_measures_cargs",
    "bianca_overlap_measures_outputs",
    "bianca_overlap_measures_params",
    "bianca_overlap_measures_execute",
    "bianca_overlap_measures_validate",
    "bianca_overlap_measures",
]