# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TBSS_NON_FA_METADATA = Metadata(
    id="tbss_non_FA",
    name="tbss_non_FA",
    package="fsl",
)

TbssNonFa = typing.TypedDict(
    "TbssNonFa",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tbss_non_FA"]],
        "concat_auto": typing.NotRequired[bool],
        "output_file": str,
        "input_files": list[InputPathType],
        "concat_x": typing.NotRequired[bool],
        "concat_y": typing.NotRequired[bool],
        "concat_z": typing.NotRequired[bool],
        "concat_t": typing.NotRequired[bool],
        "concat_tr": typing.NotRequired[float],
        "volume_number": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class TbssNonFaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    merged_output: OutputPathType
    """Merged output file"""

def tbss_non_fa_params(
    output_file: str,
    input_files: list[InputPathType],
    concat_auto: bool = False,
    concat_x: bool = False,
    concat_y: bool = False,
    concat_z: bool = False,
    concat_t: bool = False,
    concat_tr: float | None = None,
    volume_number: float | None = None,
) -> TbssNonFa:
    """
    Build parameters.

    Args:
        output_file: Output file for merged images.
        input_files: Images to concatenate.
        concat_auto: Auto-choose: single slices -> volume, volumes -> 4D (time\
            series).
        concat_x: Concatenate images in the x direction.
        concat_y: Concatenate images in the y direction.
        concat_z: Concatenate images in the z direction.
        concat_t: Concatenate images in time.
        concat_tr: Concatenate images in time and set the output image TR\
            (repetition time) to the final option value.
        volume_number: Only use volume <N> from each input file (first volume is 0\
            not 1).

    Returns:
        Parameter dictionary.
    """
    params: TbssNonFa = {
        "@type": "fsl/tbss_non_FA",
        "output_file": output_file,
        "input_files": input_files,
        "concat_auto": concat_auto,
        "concat_x": concat_x,
        "concat_y": concat_y,
        "concat_z": concat_z,
        "concat_t": concat_t,
    }
    if concat_tr is not None:
        params["concat_tr"] = concat_tr
    if volume_number is not None:
        params["volume_number"] = volume_number
    return params

def tbss_non_fa_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TbssNonFa."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("concat_auto", None) is not None:
        if not isinstance(params["concat_auto"], bool):
            raise StyxValidationError(f'`concat_auto` has the wrong type: Received `{type(params["concat_auto"])}` expected `bool`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("input_files", None) is None:
        raise StyxValidationError("`input_files` must not be None")
    if not isinstance(params["input_files"], list):
        raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(params["input_files"])}` expected `list[InputPathType]`')
    for e in params["input_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("concat_x", None) is not None:
        if not isinstance(params["concat_x"], bool):
            raise StyxValidationError(f'`concat_x` has the wrong type: Received `{type(params["concat_x"])}` expected `bool`')
    if params.get("concat_y", None) is not None:
        if not isinstance(params["concat_y"], bool):
            raise StyxValidationError(f'`concat_y` has the wrong type: Received `{type(params["concat_y"])}` expected `bool`')
    if params.get("concat_z", None) is not None:
        if not isinstance(params["concat_z"], bool):
            raise StyxValidationError(f'`concat_z` has the wrong type: Received `{type(params["concat_z"])}` expected `bool`')
    if params.get("concat_t", None) is not None:
        if not isinstance(params["concat_t"], bool):
            raise StyxValidationError(f'`concat_t` has the wrong type: Received `{type(params["concat_t"])}` expected `bool`')
    if params.get("concat_tr", None) is not None:
        if not isinstance(params["concat_tr"], (float, int)):
            raise StyxValidationError(f'`concat_tr` has the wrong type: Received `{type(params["concat_tr"])}` expected `float`')
    if params.get("volume_number", None) is not None:
        if not isinstance(params["volume_number"], (float, int)):
            raise StyxValidationError(f'`volume_number` has the wrong type: Received `{type(params["volume_number"])}` expected `float`')

def tbss_non_fa_cargs(params: TbssNonFa, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tbss_non_FA")
    if params.get("concat_auto"):
        cargs.append("-a")
    cargs.append(params["output_file"])
    for item0 in params["input_files"]:
        cargs.append(execution.input_file(item0))
    if params.get("concat_x"):
        cargs.append("-x")
    if params.get("concat_y"):
        cargs.append("-y")
    if params.get("concat_z"):
        cargs.append("-z")
    if params.get("concat_t"):
        cargs.append("-t")
    v_0 = params.get("concat_tr")
    if v_0 is not None:
        cargs.append("-tr")
        cargs.append(str(v_0))
    v_1 = params.get("volume_number")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    return cargs

def tbss_non_fa_outputs(params: TbssNonFa, execution: Execution) -> TbssNonFaOutputs:
    """Build the TbssNonFaOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    merged_output_v: OutputPathType = execution.output_file(params["output_file"])
    return TbssNonFaOutputs(
        root=root_v,
        merged_output=merged_output_v,
    )

def tbss_non_fa_execute(params: TbssNonFa, runner: Runner | None = None) -> TbssNonFaOutputs:
    """
    tbss_non_FA

    TBSS processing for non-FA images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tbss_non_fa_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TBSS_NON_FA_METADATA)
    execution.params(params)
    args = tbss_non_fa_cargs(params, execution)
    out = tbss_non_fa_outputs(params, execution)
    execution.run(args)
    return out

def tbss_non_fa(
    output_file: str,
    input_files: list[InputPathType],
    concat_auto: bool = False,
    concat_x: bool = False,
    concat_y: bool = False,
    concat_z: bool = False,
    concat_t: bool = False,
    concat_tr: float | None = None,
    volume_number: float | None = None,
    runner: Runner | None = None,
) -> TbssNonFaOutputs:
    """
    tbss_non_FA

    TBSS processing for non-FA images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        output_file: Output file for merged images.
        input_files: Images to concatenate.
        concat_auto: Auto-choose: single slices -> volume, volumes -> 4D (time\
            series).
        concat_x: Concatenate images in the x direction.
        concat_y: Concatenate images in the y direction.
        concat_z: Concatenate images in the z direction.
        concat_t: Concatenate images in time.
        concat_tr: Concatenate images in time and set the output image TR\
            (repetition time) to the final option value.
        volume_number: Only use volume <N> from each input file (first volume is 0\
            not 1).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tbss_non_fa_params(
        output_file=output_file,
        input_files=input_files,
        concat_auto=concat_auto,
        concat_x=concat_x,
        concat_y=concat_y,
        concat_z=concat_z,
        concat_t=concat_t,
        concat_tr=concat_tr,
        volume_number=volume_number,
    )
    return tbss_non_fa_execute(params, runner)

__all__ = [
    "TbssNonFa",
    "TbssNonFaOutputs",
    "TBSS_NON_FA_METADATA",
    "tbss_non_fa_cargs",
    "tbss_non_fa_outputs",
    "tbss_non_fa_params",
    "tbss_non_fa_execute",
    "tbss_non_fa_validate",
    "tbss_non_fa",
]