# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLSWAPDIM_METADATA = Metadata(
    id="fslswapdim",
    name="fslswapdim",
    package="fsl",
)

Fslswapdim = typing.TypedDict(
    "Fslswapdim",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslswapdim"]],
        "input_file": InputPathType,
        "axis_a": str,
        "axis_b": str,
        "axis_c": str,
        "output_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FslswapdimOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType | None
    """Output image with swapped dimensions"""

def fslswapdim_params(
    input_file: InputPathType,
    axis_a: str,
    axis_b: str,
    axis_c: str,
    output_file: str | None = None,
) -> Fslswapdim:
    """
    Build parameters.

    Args:
        input_file: Input image (e.g. img.nii.gz)
        axis_a: New x-axis dimension (e.g., -x, x, RL, etc.)
        axis_b: New y-axis dimension (e.g., -y, y, PA, etc.)
        axis_c: New z-axis dimension (e.g., -z, z, IS, etc.)
        output_file: Output image (e.g., output.nii.gz). If not specified, the\
            equivalent transformation matrix is written to the standard output.

    Returns:
        Parameter dictionary.
    """
    params: Fslswapdim = {
        "@type": "fsl/fslswapdim",
        "input_file": input_file,
        "axis_a": axis_a,
        "axis_b": axis_b,
        "axis_c": axis_c,
    }
    if output_file is not None:
        params["output_file"] = output_file
    return params

def fslswapdim_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslswapdim."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("axis_a", None) is None:
        raise StyxValidationError("`axis_a` must not be None")
    if not isinstance(params["axis_a"], str):
        raise StyxValidationError(f'`axis_a` has the wrong type: Received `{type(params["axis_a"])}` expected `str`')
    if params.get("axis_b", None) is None:
        raise StyxValidationError("`axis_b` must not be None")
    if not isinstance(params["axis_b"], str):
        raise StyxValidationError(f'`axis_b` has the wrong type: Received `{type(params["axis_b"])}` expected `str`')
    if params.get("axis_c", None) is None:
        raise StyxValidationError("`axis_c` must not be None")
    if not isinstance(params["axis_c"], str):
        raise StyxValidationError(f'`axis_c` has the wrong type: Received `{type(params["axis_c"])}` expected `str`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def fslswapdim_cargs(params: Fslswapdim, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslswapdim")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["axis_a"])
    cargs.append(params["axis_b"])
    cargs.append(params["axis_c"])
    v_0 = params.get("output_file")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fslswapdim_outputs(params: Fslswapdim, execution: Execution) -> FslswapdimOutputs:
    """Build the FslswapdimOutputs object for this tool."""
    output_image_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        output_image_v = execution.output_file((__v0 if __v0 is not None else ""))
    return FslswapdimOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def fslswapdim_execute(params: Fslswapdim, runner: Runner | None = None) -> FslswapdimOutputs:
    """
    fslswapdim

    Swap dimensions of an image volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslswapdim_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLSWAPDIM_METADATA)
    execution.params(params)
    args = fslswapdim_cargs(params, execution)
    out = fslswapdim_outputs(params, execution)
    execution.run(args)
    return out

def fslswapdim(
    input_file: InputPathType,
    axis_a: str,
    axis_b: str,
    axis_c: str,
    output_file: str | None = None,
    runner: Runner | None = None,
) -> FslswapdimOutputs:
    """
    fslswapdim

    Swap dimensions of an image volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input image (e.g. img.nii.gz)
        axis_a: New x-axis dimension (e.g., -x, x, RL, etc.)
        axis_b: New y-axis dimension (e.g., -y, y, PA, etc.)
        axis_c: New z-axis dimension (e.g., -z, z, IS, etc.)
        output_file: Output image (e.g., output.nii.gz). If not specified, the\
            equivalent transformation matrix is written to the standard output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslswapdim_params(
        input_file=input_file,
        axis_a=axis_a,
        axis_b=axis_b,
        axis_c=axis_c,
        output_file=output_file,
    )
    return fslswapdim_execute(params, runner)

__all__ = [
    "Fslswapdim",
    "FslswapdimOutputs",
    "FSLSWAPDIM_METADATA",
    "fslswapdim_cargs",
    "fslswapdim_outputs",
    "fslswapdim_params",
    "fslswapdim_execute",
    "fslswapdim_validate",
    "fslswapdim",
]