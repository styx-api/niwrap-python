# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLCOMPLEX_METADATA = Metadata(
    id="fslcomplex",
    name="fslcomplex",
    package="fsl",
)

FslcomplexOutputType = typing.Literal["-realabs"] | typing.Literal["-realphase"] | typing.Literal["-realpolar"] | typing.Literal["-realcartesian"] | typing.Literal["-complex"] | typing.Literal["-complexpolar"] | typing.Literal["-complexsplit"] | typing.Literal["-complexmerge"] | typing.Literal["-copyonly"]

Fslcomplex = typing.TypedDict(
    "Fslcomplex",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslcomplex"]],
        "input_file": InputPathType,
        "output_file": str,
        "output_type": FslcomplexOutputType,
        "start_vol": typing.NotRequired[int],
        "end_vol": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class FslcomplexOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    result_output_file: OutputPathType
    """The resulting output file from the specified operation."""

def fslcomplex_params(
    input_file: InputPathType,
    output_file: str,
    output_type: FslcomplexOutputType,
    start_vol: int | None = None,
    end_vol: int | None = None,
) -> Fslcomplex:
    """
    Build parameters.

    Args:
        input_file: Input volume (e.g. complexvol.nii.gz)
        output_file: Output volume (e.g. absvol.nii.gz)
        output_type: Output type (determines the operation to perform)
        start_vol: Start volume (optional)
        end_vol: End volume (optional)

    Returns:
        Parameter dictionary.
    """
    params: Fslcomplex = {
        "@type": "fsl/fslcomplex",
        "input_file": input_file,
        "output_file": output_file,
        "output_type": output_type,
    }
    if start_vol is not None:
        params["start_vol"] = start_vol
    if end_vol is not None:
        params["end_vol"] = end_vol
    return params

def fslcomplex_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslcomplex."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("output_type", None) is None:
        raise StyxValidationError("`output_type` must not be None")
    if not isinstance(params["output_type"], str):
        raise StyxValidationError(f'`output_type` has the wrong type: Received `{type(params["output_type"])}` expected `FslcomplexOutputType`')
    if params["output_type"] not in ["-realabs", "-realphase", "-realpolar", "-realcartesian", "-complex", "-complexpolar", "-complexsplit", "-complexmerge", "-copyonly"]:
        raise StyxValidationError("Parameter `output_type` must be one of [\"-realabs\", \"-realphase\", \"-realpolar\", \"-realcartesian\", \"-complex\", \"-complexpolar\", \"-complexsplit\", \"-complexmerge\", \"-copyonly\"]")
    if params.get("start_vol", None) is not None:
        if not isinstance(params["start_vol"], int):
            raise StyxValidationError(f'`start_vol` has the wrong type: Received `{type(params["start_vol"])}` expected `int`')
    if params.get("end_vol", None) is not None:
        if not isinstance(params["end_vol"], int):
            raise StyxValidationError(f'`end_vol` has the wrong type: Received `{type(params["end_vol"])}` expected `int`')

def fslcomplex_cargs(params: Fslcomplex, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslcomplex")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    cargs.append(str(params["output_type"]))
    v_0 = params.get("start_vol")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("end_vol")
    if v_1 is not None:
        cargs.append(str(v_1))
    return cargs

def fslcomplex_outputs(params: Fslcomplex, execution: Execution) -> FslcomplexOutputs:
    """Build the FslcomplexOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    result_output_file_v: OutputPathType = execution.output_file(params["output_file"])
    return FslcomplexOutputs(
        root=root_v,
        result_output_file=result_output_file_v,
    )

def fslcomplex_execute(params: Fslcomplex, runner: Runner | None = None) -> FslcomplexOutputs:
    """
    fslcomplex

    Tool for manipulating complex-valued MR data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslcomplex_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLCOMPLEX_METADATA)
    execution.params(params)
    args = fslcomplex_cargs(params, execution)
    out = fslcomplex_outputs(params, execution)
    execution.run(args)
    return out

def fslcomplex(
    input_file: InputPathType,
    output_file: str,
    output_type: FslcomplexOutputType,
    start_vol: int | None = None,
    end_vol: int | None = None,
    runner: Runner | None = None,
) -> FslcomplexOutputs:
    """
    fslcomplex

    Tool for manipulating complex-valued MR data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input volume (e.g. complexvol.nii.gz)
        output_file: Output volume (e.g. absvol.nii.gz)
        output_type: Output type (determines the operation to perform)
        start_vol: Start volume (optional)
        end_vol: End volume (optional)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslcomplex_params(
        input_file=input_file,
        output_file=output_file,
        output_type=output_type,
        start_vol=start_vol,
        end_vol=end_vol,
    )
    return fslcomplex_execute(params, runner)

__all__ = [
    "Fslcomplex",
    "FslcomplexOutputs",
    "FSLCOMPLEX_METADATA",
    "fslcomplex_cargs",
    "fslcomplex_outputs",
    "fslcomplex_params",
    "fslcomplex_execute",
    "fslcomplex_validate",
    "fslcomplex",
]