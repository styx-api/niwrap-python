# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIRST_FLIRT_METADATA = Metadata(
    id="first_flirt",
    name="first_flirt",
    package="fsl",
)

FirstFlirt = typing.TypedDict(
    "FirstFlirt",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/first_flirt"]],
        "input_image": InputPathType,
        "output_basename": str,
        "already_brain_extracted_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "inweight_flag": typing.NotRequired[bool],
        "strucweight_mask": typing.NotRequired[InputPathType],
        "cort_flag": typing.NotRequired[bool],
        "cost_function": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FirstFlirtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_output_image: OutputPathType
    """Output image registered to standard space"""
    log_file: OutputPathType
    """Log file containing details of the registration process"""
    transformation_matrix: OutputPathType
    """Transformation matrix file"""

def first_flirt_params(
    input_image: InputPathType,
    output_basename: str,
    already_brain_extracted_flag: bool = False,
    debug_flag: bool = False,
    inweight_flag: bool = False,
    strucweight_mask: InputPathType | None = None,
    cort_flag: bool = False,
    cost_function: str | None = None,
) -> FirstFlirt:
    """
    Build parameters.

    Args:
        input_image: Input image (e.g. subject10rawT1.nii.gz)
        output_basename: Output basename for the results (e.g.\
            subject10rawT1_to_std_sub)
        already_brain_extracted_flag: Input is already brain extracted
        debug_flag: Debug mode: don't delete intermediate files
        inweight_flag: Use a weighting mask on the first registration
        strucweight_mask: Use a specific structure weighting mask (in standard\
            space) for an optional third-stage registration step (e.g.\
            maskimage.nii.gz)
        cort_flag: Use a weighting mask of the whole brain on the first\
            registration for specific models
        cost_function: Specify the cost function to be used by all FLIRT calls

    Returns:
        Parameter dictionary.
    """
    params: FirstFlirt = {
        "@type": "fsl/first_flirt",
        "input_image": input_image,
        "output_basename": output_basename,
        "already_brain_extracted_flag": already_brain_extracted_flag,
        "debug_flag": debug_flag,
        "inweight_flag": inweight_flag,
        "cort_flag": cort_flag,
    }
    if strucweight_mask is not None:
        params["strucweight_mask"] = strucweight_mask
    if cost_function is not None:
        params["cost_function"] = cost_function
    return params

def first_flirt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FirstFlirt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_basename", None) is None:
        raise StyxValidationError("`output_basename` must not be None")
    if not isinstance(params["output_basename"], str):
        raise StyxValidationError(f'`output_basename` has the wrong type: Received `{type(params["output_basename"])}` expected `str`')
    if params.get("already_brain_extracted_flag", None) is not None:
        if not isinstance(params["already_brain_extracted_flag"], bool):
            raise StyxValidationError(f'`already_brain_extracted_flag` has the wrong type: Received `{type(params["already_brain_extracted_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("inweight_flag", None) is not None:
        if not isinstance(params["inweight_flag"], bool):
            raise StyxValidationError(f'`inweight_flag` has the wrong type: Received `{type(params["inweight_flag"])}` expected `bool`')
    if params.get("strucweight_mask", None) is not None:
        if not isinstance(params["strucweight_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`strucweight_mask` has the wrong type: Received `{type(params["strucweight_mask"])}` expected `InputPathType`')
    if params.get("cort_flag", None) is not None:
        if not isinstance(params["cort_flag"], bool):
            raise StyxValidationError(f'`cort_flag` has the wrong type: Received `{type(params["cort_flag"])}` expected `bool`')
    if params.get("cost_function", None) is not None:
        if not isinstance(params["cost_function"], str):
            raise StyxValidationError(f'`cost_function` has the wrong type: Received `{type(params["cost_function"])}` expected `str`')

def first_flirt_cargs(params: FirstFlirt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("first_flirt")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_basename"])
    if params.get("already_brain_extracted_flag"):
        cargs.append("-b")
    if params.get("debug_flag"):
        cargs.append("-d")
    if params.get("inweight_flag"):
        cargs.append("-inweight")
    v_0 = params.get("strucweight_mask")
    if v_0 is not None:
        cargs.append("-strucweight")
        cargs.append(execution.input_file(v_0))
    if params.get("cort_flag"):
        cargs.append("-cort")
    v_1 = params.get("cost_function")
    if v_1 is not None:
        cargs.append("-cost")
        cargs.append(v_1)
    return cargs

def first_flirt_outputs(params: FirstFlirt, execution: Execution) -> FirstFlirtOutputs:
    """Build the FirstFlirtOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_output_image_v: OutputPathType = execution.output_file(f'{params["output_basename"]}_result.nii.gz')
    log_file_v: OutputPathType = execution.output_file(f'{params["output_basename"]}_log.txt')
    transformation_matrix_v: OutputPathType = execution.output_file(f'{params["output_basename"]}_matrix.mat')
    return FirstFlirtOutputs(
        root=root_v,
        registered_output_image=registered_output_image_v,
        log_file=log_file_v,
        transformation_matrix=transformation_matrix_v,
    )

def first_flirt_execute(params: FirstFlirt, runner: Runner | None = None) -> FirstFlirtOutputs:
    """
    first_flirt

    FLIRT-based image registration tool with additional options for brain extraction and weighting masks

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    first_flirt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIRST_FLIRT_METADATA)
    execution.params(params)
    args = first_flirt_cargs(params, execution)
    out = first_flirt_outputs(params, execution)
    execution.run(args)
    return out

def first_flirt(
    input_image: InputPathType,
    output_basename: str,
    already_brain_extracted_flag: bool = False,
    debug_flag: bool = False,
    inweight_flag: bool = False,
    strucweight_mask: InputPathType | None = None,
    cort_flag: bool = False,
    cost_function: str | None = None,
    runner: Runner | None = None,
) -> FirstFlirtOutputs:
    """
    first_flirt

    FLIRT-based image registration tool with additional options for brain extraction and weighting masks

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Input image (e.g. subject10rawT1.nii.gz)
        output_basename: Output basename for the results (e.g.\
            subject10rawT1_to_std_sub)
        already_brain_extracted_flag: Input is already brain extracted
        debug_flag: Debug mode: don't delete intermediate files
        inweight_flag: Use a weighting mask on the first registration
        strucweight_mask: Use a specific structure weighting mask (in standard\
            space) for an optional third-stage registration step (e.g.\
            maskimage.nii.gz)
        cort_flag: Use a weighting mask of the whole brain on the first\
            registration for specific models
        cost_function: Specify the cost function to be used by all FLIRT calls
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = first_flirt_params(
        input_image=input_image,
        output_basename=output_basename,
        already_brain_extracted_flag=already_brain_extracted_flag,
        debug_flag=debug_flag,
        inweight_flag=inweight_flag,
        strucweight_mask=strucweight_mask,
        cort_flag=cort_flag,
        cost_function=cost_function,
    )
    return first_flirt_execute(params, runner)

__all__ = [
    "FirstFlirt",
    "FirstFlirtOutputs",
    "FIRST_FLIRT_METADATA",
    "first_flirt_cargs",
    "first_flirt_outputs",
    "first_flirt_params",
    "first_flirt_execute",
    "first_flirt_validate",
    "first_flirt",
]