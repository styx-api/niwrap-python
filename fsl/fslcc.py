# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLCC_METADATA = Metadata(
    id="fslcc",
    name="fslcc",
    package="fsl",
)

Fslcc = typing.TypedDict(
    "Fslcc",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslcc"]],
        "first_input": InputPathType,
        "second_input": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "noabs_flag": typing.NotRequired[bool],
        "nodemean_flag": typing.NotRequired[bool],
        "threshold": typing.NotRequired[float],
        "decimal_places": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class FslccOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslcc_params(
    first_input: InputPathType,
    second_input: InputPathType,
    mask: InputPathType | None = None,
    noabs_flag: bool = False,
    nodemean_flag: bool = False,
    threshold: float | None = None,
    decimal_places: float | None = None,
) -> Fslcc:
    """
    Build parameters.

    Args:
        first_input: First input time-series
        second_input: Second input time-series
        mask: Mask file name
        noabs_flag: Don't return absolute values (keep sign)
        nodemean_flag: Don't demean the input files
        threshold: Threshold (default 0.1)
        decimal_places: Number of decimal places to display in output (default 2)

    Returns:
        Parameter dictionary.
    """
    params: Fslcc = {
        "@type": "fsl/fslcc",
        "first_input": first_input,
        "second_input": second_input,
        "noabs_flag": noabs_flag,
        "nodemean_flag": nodemean_flag,
    }
    if mask is not None:
        params["mask"] = mask
    if threshold is not None:
        params["threshold"] = threshold
    if decimal_places is not None:
        params["decimal_places"] = decimal_places
    return params

def fslcc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslcc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("first_input", None) is None:
        raise StyxValidationError("`first_input` must not be None")
    if not isinstance(params["first_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`first_input` has the wrong type: Received `{type(params["first_input"])}` expected `InputPathType`')
    if params.get("second_input", None) is None:
        raise StyxValidationError("`second_input` must not be None")
    if not isinstance(params["second_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`second_input` has the wrong type: Received `{type(params["second_input"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("noabs_flag", None) is not None:
        if not isinstance(params["noabs_flag"], bool):
            raise StyxValidationError(f'`noabs_flag` has the wrong type: Received `{type(params["noabs_flag"])}` expected `bool`')
    if params.get("nodemean_flag", None) is not None:
        if not isinstance(params["nodemean_flag"], bool):
            raise StyxValidationError(f'`nodemean_flag` has the wrong type: Received `{type(params["nodemean_flag"])}` expected `bool`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("decimal_places", None) is not None:
        if not isinstance(params["decimal_places"], (float, int)):
            raise StyxValidationError(f'`decimal_places` has the wrong type: Received `{type(params["decimal_places"])}` expected `float`')

def fslcc_cargs(params: Fslcc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslcc")
    cargs.append(execution.input_file(params["first_input"]))
    cargs.append(execution.input_file(params["second_input"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    if params.get("noabs_flag"):
        cargs.append("--noabs")
    if params.get("nodemean_flag"):
        cargs.append("--nodemean")
    v_1 = params.get("threshold")
    if v_1 is not None:
        cargs.append("-t")
        cargs.append(str(v_1))
    v_2 = params.get("decimal_places")
    if v_2 is not None:
        cargs.append("-p")
        cargs.append(str(v_2))
    return cargs

def fslcc_outputs(params: Fslcc, execution: Execution) -> FslccOutputs:
    """Build the FslccOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslccOutputs(
        root=root_v,
    )

def fslcc_execute(params: Fslcc, runner: Runner | None = None) -> FslccOutputs:
    """
    fslcc

    Cross-correlate two time-series, timepoint by timepoint

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslcc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLCC_METADATA)
    execution.params(params)
    args = fslcc_cargs(params, execution)
    out = fslcc_outputs(params, execution)
    execution.run(args)
    return out

def fslcc(
    first_input: InputPathType,
    second_input: InputPathType,
    mask: InputPathType | None = None,
    noabs_flag: bool = False,
    nodemean_flag: bool = False,
    threshold: float | None = None,
    decimal_places: float | None = None,
    runner: Runner | None = None,
) -> FslccOutputs:
    """
    fslcc

    Cross-correlate two time-series, timepoint by timepoint

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        first_input: First input time-series
        second_input: Second input time-series
        mask: Mask file name
        noabs_flag: Don't return absolute values (keep sign)
        nodemean_flag: Don't demean the input files
        threshold: Threshold (default 0.1)
        decimal_places: Number of decimal places to display in output (default 2)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslcc_params(
        first_input=first_input,
        second_input=second_input,
        mask=mask,
        noabs_flag=noabs_flag,
        nodemean_flag=nodemean_flag,
        threshold=threshold,
        decimal_places=decimal_places,
    )
    return fslcc_execute(params, runner)

__all__ = [
    "Fslcc",
    "FslccOutputs",
    "FSLCC_METADATA",
    "fslcc_cargs",
    "fslcc_outputs",
    "fslcc_params",
    "fslcc_execute",
    "fslcc_validate",
    "fslcc",
]