# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PREWHITEN_METADATA = Metadata(
    id="prewhiten",
    name="prewhiten",
    package="fsl",
)

Prewhiten = typing.TypedDict(
    "Prewhiten",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/prewhiten"]],
        "feat_directory": str,
        "output_directory": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class PrewhitenOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType | None
    """Specified output directory containing generated output files."""

def prewhiten_params(
    feat_directory: str,
    output_directory: str | None = None,
) -> Prewhiten:
    """
    Build parameters.

    Args:
        feat_directory: Input FEAT directory
        output_directory: Change output directory from default of input FEAT\
            directory

    Returns:
        Parameter dictionary.
    """
    params: Prewhiten = {
        "@type": "fsl/prewhiten",
        "feat_directory": feat_directory,
    }
    if output_directory is not None:
        params["output_directory"] = output_directory
    return params

def prewhiten_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Prewhiten."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_directory", None) is None:
        raise StyxValidationError("`feat_directory` must not be None")
    if not isinstance(params["feat_directory"], str):
        raise StyxValidationError(f'`feat_directory` has the wrong type: Received `{type(params["feat_directory"])}` expected `str`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')

def prewhiten_cargs(params: Prewhiten, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("prewhiten")
    cargs.append(params["feat_directory"])
    v_0 = params.get("output_directory")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    return cargs

def prewhiten_outputs(params: Prewhiten, execution: Execution) -> PrewhitenOutputs:
    """Build the PrewhitenOutputs object for this tool."""
    output_directory_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_directory")
    if __v0 is not None:
        output_directory_v = execution.output_file((__v0 if __v0 is not None else ""))
    return PrewhitenOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def prewhiten_execute(params: Prewhiten, runner: Runner | None = None) -> PrewhitenOutputs:
    """
    prewhiten

    Prewhitening tool for FEAT directories

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    prewhiten_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PREWHITEN_METADATA)
    execution.params(params)
    args = prewhiten_cargs(params, execution)
    out = prewhiten_outputs(params, execution)
    execution.run(args)
    return out

def prewhiten(
    feat_directory: str,
    output_directory: str | None = None,
    runner: Runner | None = None,
) -> PrewhitenOutputs:
    """
    prewhiten

    Prewhitening tool for FEAT directories

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        feat_directory: Input FEAT directory
        output_directory: Change output directory from default of input FEAT\
            directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = prewhiten_params(
        feat_directory=feat_directory,
        output_directory=output_directory,
    )
    return prewhiten_execute(params, runner)

__all__ = [
    "Prewhiten",
    "PrewhitenOutputs",
    "PREWHITEN_METADATA",
    "prewhiten_cargs",
    "prewhiten_outputs",
    "prewhiten_params",
    "prewhiten_execute",
    "prewhiten_validate",
    "prewhiten",
]