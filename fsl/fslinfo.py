# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLINFO_METADATA = Metadata(
    id="fslinfo",
    name="fslinfo",
    package="fsl",
)

Fslinfo = typing.TypedDict(
    "Fslinfo",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslinfo"]],
        "filename": InputPathType,
    },
)

@dataclasses.dataclass
class FslinfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslinfo_params(
    filename: InputPathType,
) -> Fslinfo:
    """
    Build parameters.

    Args:
        filename: Input NIFTI-1 image file (e.g. img.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: Fslinfo = {
        "@type": "fsl/fslinfo",
        "filename": filename,
    }
    return params

def fslinfo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslinfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename", None) is None:
        raise StyxValidationError("`filename` must not be None")
    if not isinstance(params["filename"], (pathlib.Path, str)):
        raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `InputPathType`')

def fslinfo_cargs(params: Fslinfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslinfo")
    cargs.append(execution.input_file(params["filename"]))
    return cargs

def fslinfo_outputs(params: Fslinfo, execution: Execution) -> FslinfoOutputs:
    """Build the FslinfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslinfoOutputs(
        root=root_v,
    )

def fslinfo_execute(params: Fslinfo, runner: Runner | None = None) -> FslinfoOutputs:
    """
    fslinfo

    Display information about NIFTI-1 image file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslinfo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLINFO_METADATA)
    execution.params(params)
    args = fslinfo_cargs(params, execution)
    out = fslinfo_outputs(params, execution)
    execution.run(args)
    return out

def fslinfo(
    filename: InputPathType,
    runner: Runner | None = None,
) -> FslinfoOutputs:
    """
    fslinfo

    Display information about NIFTI-1 image file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        filename: Input NIFTI-1 image file (e.g. img.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslinfo_params(
        filename=filename,
    )
    return fslinfo_execute(params, runner)

__all__ = [
    "Fslinfo",
    "FslinfoOutputs",
    "FSLINFO_METADATA",
    "fslinfo_cargs",
    "fslinfo_outputs",
    "fslinfo_params",
    "fslinfo_execute",
    "fslinfo_validate",
    "fslinfo",
]