# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FLIRT_AVERAGE_METADATA = Metadata(
    id="flirt_average",
    name="flirt_average",
    package="fsl",
)

FlirtAverage = typing.TypedDict(
    "FlirtAverage",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/flirt_average"]],
        "ninputs": int,
        "input1": InputPathType,
        "input2": InputPathType,
        "input3": typing.NotRequired[InputPathType],
        "output_file": str,
        "reference_image": typing.NotRequired[InputPathType],
        "flirt_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FlirtAverageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Averaged output image"""

def flirt_average_params(
    ninputs: int,
    input1: InputPathType,
    input2: InputPathType,
    output_file: str,
    input3: InputPathType | None = None,
    reference_image: InputPathType | None = None,
    flirt_options: str | None = None,
) -> FlirtAverage:
    """
    Build parameters.

    Args:
        ninputs: Number of input images
        input1: First input image (e.g. rawT1_1.nii.gz)
        input2: Second input image (e.g. rawT1_2.nii.gz)
        output_file: Output image (e.g. averageT1.nii.gz)
        input3: Third input image (e.g. rawT1_3.nii.gz)
        reference_image: Reference image to use instead of first input
        flirt_options: Options to be passed to FLIRT

    Returns:
        Parameter dictionary.
    """
    params: FlirtAverage = {
        "@type": "fsl/flirt_average",
        "ninputs": ninputs,
        "input1": input1,
        "input2": input2,
        "output_file": output_file,
    }
    if input3 is not None:
        params["input3"] = input3
    if reference_image is not None:
        params["reference_image"] = reference_image
    if flirt_options is not None:
        params["flirt_options"] = flirt_options
    return params

def flirt_average_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FlirtAverage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ninputs", None) is None:
        raise StyxValidationError("`ninputs` must not be None")
    if not isinstance(params["ninputs"], int):
        raise StyxValidationError(f'`ninputs` has the wrong type: Received `{type(params["ninputs"])}` expected `int`')
    if params["ninputs"] < 2:
        raise StyxValidationError("Parameter `ninputs` must be at least 2")
    if params.get("input1", None) is None:
        raise StyxValidationError("`input1` must not be None")
    if not isinstance(params["input1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input1` has the wrong type: Received `{type(params["input1"])}` expected `InputPathType`')
    if params.get("input2", None) is None:
        raise StyxValidationError("`input2` must not be None")
    if not isinstance(params["input2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input2` has the wrong type: Received `{type(params["input2"])}` expected `InputPathType`')
    if params.get("input3", None) is not None:
        if not isinstance(params["input3"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input3` has the wrong type: Received `{type(params["input3"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("reference_image", None) is not None:
        if not isinstance(params["reference_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("flirt_options", None) is not None:
        if not isinstance(params["flirt_options"], str):
            raise StyxValidationError(f'`flirt_options` has the wrong type: Received `{type(params["flirt_options"])}` expected `str`')

def flirt_average_cargs(params: FlirtAverage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("flirt_average")
    cargs.append(str(params["ninputs"]))
    cargs.append(execution.input_file(params["input1"]))
    cargs.append(execution.input_file(params["input2"]))
    v_0 = params.get("input3")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    cargs.append(params["output_file"])
    v_1 = params.get("reference_image")
    if v_1 is not None:
        cargs.append("-FAref")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("flirt_options")
    if v_2 is not None:
        cargs.append(v_2)
    return cargs

def flirt_average_outputs(params: FlirtAverage, execution: Execution) -> FlirtAverageOutputs:
    """Build the FlirtAverageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(f'{params["output_file"]}.nii.gz')
    return FlirtAverageOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def flirt_average_execute(params: FlirtAverage, runner: Runner | None = None) -> FlirtAverageOutputs:
    """
    flirt_average

    Averages multiple input images after linear registration (FLIRT).

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    flirt_average_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FLIRT_AVERAGE_METADATA)
    execution.params(params)
    args = flirt_average_cargs(params, execution)
    out = flirt_average_outputs(params, execution)
    execution.run(args)
    return out

def flirt_average(
    ninputs: int,
    input1: InputPathType,
    input2: InputPathType,
    output_file: str,
    input3: InputPathType | None = None,
    reference_image: InputPathType | None = None,
    flirt_options: str | None = None,
    runner: Runner | None = None,
) -> FlirtAverageOutputs:
    """
    flirt_average

    Averages multiple input images after linear registration (FLIRT).

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        ninputs: Number of input images
        input1: First input image (e.g. rawT1_1.nii.gz)
        input2: Second input image (e.g. rawT1_2.nii.gz)
        output_file: Output image (e.g. averageT1.nii.gz)
        input3: Third input image (e.g. rawT1_3.nii.gz)
        reference_image: Reference image to use instead of first input
        flirt_options: Options to be passed to FLIRT
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = flirt_average_params(
        ninputs=ninputs,
        input1=input1,
        input2=input2,
        output_file=output_file,
        input3=input3,
        reference_image=reference_image,
        flirt_options=flirt_options,
    )
    return flirt_average_execute(params, runner)

__all__ = [
    "FlirtAverage",
    "FlirtAverageOutputs",
    "FLIRT_AVERAGE_METADATA",
    "flirt_average_cargs",
    "flirt_average_outputs",
    "flirt_average_params",
    "flirt_average_execute",
    "flirt_average_validate",
    "flirt_average",
]