# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DISTANCEMAP_METADATA = Metadata(
    id="distancemap",
    name="distancemap",
    package="fsl",
)

Distancemap = typing.TypedDict(
    "Distancemap",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/distancemap"]],
        "input_image": InputPathType,
        "output_image": str,
        "mask_image": typing.NotRequired[InputPathType],
        "second_image": typing.NotRequired[InputPathType],
        "local_maxima_image": typing.NotRequired[InputPathType],
        "segmented_image": typing.NotRequired[InputPathType],
        "invert_flag": typing.NotRequired[bool],
        "interpolate_values": typing.NotRequired[InputPathType],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DistancemapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_distancemap: OutputPathType
    """Output distance map image"""
    output_local_maxima: OutputPathType | None
    """Output local maxima image"""
    output_segmented_image: OutputPathType | None
    """Output segmented distance map image"""

def distancemap_params(
    input_image: InputPathType,
    output_image: str,
    mask_image: InputPathType | None = None,
    second_image: InputPathType | None = None,
    local_maxima_image: InputPathType | None = None,
    segmented_image: InputPathType | None = None,
    invert_flag: bool = False,
    interpolate_values: InputPathType | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Distancemap:
    """
    Build parameters.

    Args:
        input_image: Input image filename (calc distance to non-zero voxels)
        output_image: Output image filename
        mask_image: Mask image filename (only calc values at these voxels)
        second_image: Second image filename (calc closest distance of this and\
            primary input image, using non-zero voxels, negative distances mean\
            this secondary image is the closer one)
        local_maxima_image: Local maxima output image filename
        segmented_image: Segmented output image filename (unique value per segment\
            is local maxima label)
        invert_flag: Invert input image
        interpolate_values: Filename for values to interpolate (sparse sampling\
            interpolation)
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Distancemap = {
        "@type": "fsl/distancemap",
        "input_image": input_image,
        "output_image": output_image,
        "invert_flag": invert_flag,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if mask_image is not None:
        params["mask_image"] = mask_image
    if second_image is not None:
        params["second_image"] = second_image
    if local_maxima_image is not None:
        params["local_maxima_image"] = local_maxima_image
    if segmented_image is not None:
        params["segmented_image"] = segmented_image
    if interpolate_values is not None:
        params["interpolate_values"] = interpolate_values
    return params

def distancemap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Distancemap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("second_image", None) is not None:
        if not isinstance(params["second_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`second_image` has the wrong type: Received `{type(params["second_image"])}` expected `InputPathType`')
    if params.get("local_maxima_image", None) is not None:
        if not isinstance(params["local_maxima_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`local_maxima_image` has the wrong type: Received `{type(params["local_maxima_image"])}` expected `InputPathType`')
    if params.get("segmented_image", None) is not None:
        if not isinstance(params["segmented_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`segmented_image` has the wrong type: Received `{type(params["segmented_image"])}` expected `InputPathType`')
    if params.get("invert_flag", None) is not None:
        if not isinstance(params["invert_flag"], bool):
            raise StyxValidationError(f'`invert_flag` has the wrong type: Received `{type(params["invert_flag"])}` expected `bool`')
    if params.get("interpolate_values", None) is not None:
        if not isinstance(params["interpolate_values"], (pathlib.Path, str)):
            raise StyxValidationError(f'`interpolate_values` has the wrong type: Received `{type(params["interpolate_values"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def distancemap_cargs(params: Distancemap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("distancemap")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("-o")
    cargs.append(params["output_image"])
    v_0 = params.get("mask_image")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("second_image")
    if v_1 is not None:
        cargs.append("--secondim")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("local_maxima_image")
    if v_2 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("segmented_image")
    if v_3 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_3))
    if params.get("invert_flag"):
        cargs.append("--invert")
    v_4 = params.get("interpolate_values")
    if v_4 is not None:
        cargs.append("--interp")
        cargs.append(execution.input_file(v_4))
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def distancemap_outputs(params: Distancemap, execution: Execution) -> DistancemapOutputs:
    """Build the DistancemapOutputs object for this tool."""
    output_local_maxima_v: OutputPathType | None = None
    output_segmented_image_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_distancemap_v: OutputPathType = execution.output_file(params["output_image"])
    __v0 = params.get("local_maxima_image")
    if __v0 is not None:
        output_local_maxima_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("segmented_image")
    if __v1 is not None:
        output_segmented_image_v = execution.output_file((__v1 if __v1 is not None else ""))
    return DistancemapOutputs(
        root=root_v,
        output_distancemap=output_distancemap_v,
        output_local_maxima=output_local_maxima_v,
        output_segmented_image=output_segmented_image_v,
    )

def distancemap_execute(params: Distancemap, runner: Runner | None = None) -> DistancemapOutputs:
    """
    distancemap

    A tool to calculate distance maps using FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    distancemap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DISTANCEMAP_METADATA)
    execution.params(params)
    args = distancemap_cargs(params, execution)
    out = distancemap_outputs(params, execution)
    execution.run(args)
    return out

def distancemap(
    input_image: InputPathType,
    output_image: str,
    mask_image: InputPathType | None = None,
    second_image: InputPathType | None = None,
    local_maxima_image: InputPathType | None = None,
    segmented_image: InputPathType | None = None,
    invert_flag: bool = False,
    interpolate_values: InputPathType | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> DistancemapOutputs:
    """
    distancemap

    A tool to calculate distance maps using FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Input image filename (calc distance to non-zero voxels)
        output_image: Output image filename
        mask_image: Mask image filename (only calc values at these voxels)
        second_image: Second image filename (calc closest distance of this and\
            primary input image, using non-zero voxels, negative distances mean\
            this secondary image is the closer one)
        local_maxima_image: Local maxima output image filename
        segmented_image: Segmented output image filename (unique value per segment\
            is local maxima label)
        invert_flag: Invert input image
        interpolate_values: Filename for values to interpolate (sparse sampling\
            interpolation)
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = distancemap_params(
        input_image=input_image,
        output_image=output_image,
        mask_image=mask_image,
        second_image=second_image,
        local_maxima_image=local_maxima_image,
        segmented_image=segmented_image,
        invert_flag=invert_flag,
        interpolate_values=interpolate_values,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return distancemap_execute(params, runner)

__all__ = [
    "Distancemap",
    "DistancemapOutputs",
    "DISTANCEMAP_METADATA",
    "distancemap_cargs",
    "distancemap_outputs",
    "distancemap_params",
    "distancemap_execute",
    "distancemap_validate",
    "distancemap",
]