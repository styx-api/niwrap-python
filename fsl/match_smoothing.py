# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MATCH_SMOOTHING_METADATA = Metadata(
    id="match_smoothing",
    name="match_smoothing",
    package="fsl",
)

MatchSmoothing = typing.TypedDict(
    "MatchSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/match_smoothing"]],
        "example_func": InputPathType,
        "func_smoothing_FWHM": float,
        "example_structural": InputPathType,
        "standard_space_resolution": float,
    },
)

@dataclasses.dataclass
class MatchSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def match_smoothing_params(
    example_func: InputPathType,
    func_smoothing_FWHM: float,
    example_structural: InputPathType,
    standard_space_resolution: float,
) -> MatchSmoothing:
    """
    Build parameters.

    Args:
        example_func: Path to the example functional image file.
        func_smoothing_FWHM: Full-width at half maximum (FWHM) of the smoothing\
            kernel applied to the functional data, in millimeters.
        example_structural: Path to the example structural image file.
        standard_space_resolution: Resolution of the standard space, in\
            millimeters.

    Returns:
        Parameter dictionary.
    """
    params: MatchSmoothing = {
        "@type": "fsl/match_smoothing",
        "example_func": example_func,
        "func_smoothing_FWHM": func_smoothing_FWHM,
        "example_structural": example_structural,
        "standard_space_resolution": standard_space_resolution,
    }
    return params

def match_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MatchSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("example_func", None) is None:
        raise StyxValidationError("`example_func` must not be None")
    if not isinstance(params["example_func"], (pathlib.Path, str)):
        raise StyxValidationError(f'`example_func` has the wrong type: Received `{type(params["example_func"])}` expected `InputPathType`')
    if params.get("func_smoothing_FWHM", None) is None:
        raise StyxValidationError("`func_smoothing_FWHM` must not be None")
    if not isinstance(params["func_smoothing_FWHM"], (float, int)):
        raise StyxValidationError(f'`func_smoothing_FWHM` has the wrong type: Received `{type(params["func_smoothing_FWHM"])}` expected `float`')
    if params.get("example_structural", None) is None:
        raise StyxValidationError("`example_structural` must not be None")
    if not isinstance(params["example_structural"], (pathlib.Path, str)):
        raise StyxValidationError(f'`example_structural` has the wrong type: Received `{type(params["example_structural"])}` expected `InputPathType`')
    if params.get("standard_space_resolution", None) is None:
        raise StyxValidationError("`standard_space_resolution` must not be None")
    if not isinstance(params["standard_space_resolution"], (float, int)):
        raise StyxValidationError(f'`standard_space_resolution` has the wrong type: Received `{type(params["standard_space_resolution"])}` expected `float`')

def match_smoothing_cargs(params: MatchSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("match_smoothing")
    cargs.append(execution.input_file(params["example_func"]))
    cargs.append(str(params["func_smoothing_FWHM"]))
    cargs.append(execution.input_file(params["example_structural"]))
    cargs.append(str(params["standard_space_resolution"]))
    return cargs

def match_smoothing_outputs(params: MatchSmoothing, execution: Execution) -> MatchSmoothingOutputs:
    """Build the MatchSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MatchSmoothingOutputs(
        root=root_v,
    )

def match_smoothing_execute(params: MatchSmoothing, runner: Runner | None = None) -> MatchSmoothingOutputs:
    """
    match_smoothing

    Computes the smoothing sigma needed to be applied to structural data to match a given functional data smoothing level.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    match_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MATCH_SMOOTHING_METADATA)
    execution.params(params)
    args = match_smoothing_cargs(params, execution)
    out = match_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def match_smoothing(
    example_func: InputPathType,
    func_smoothing_FWHM: float,
    example_structural: InputPathType,
    standard_space_resolution: float,
    runner: Runner | None = None,
) -> MatchSmoothingOutputs:
    """
    match_smoothing

    Computes the smoothing sigma needed to be applied to structural data to match a given functional data smoothing level.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        example_func: Path to the example functional image file.
        func_smoothing_FWHM: Full-width at half maximum (FWHM) of the smoothing\
            kernel applied to the functional data, in millimeters.
        example_structural: Path to the example structural image file.
        standard_space_resolution: Resolution of the standard space, in\
            millimeters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = match_smoothing_params(
        example_func=example_func,
        func_smoothing_FWHM=func_smoothing_FWHM,
        example_structural=example_structural,
        standard_space_resolution=standard_space_resolution,
    )
    return match_smoothing_execute(params, runner)

__all__ = [
    "MatchSmoothing",
    "MatchSmoothingOutputs",
    "MATCH_SMOOTHING_METADATA",
    "match_smoothing_cargs",
    "match_smoothing_outputs",
    "match_smoothing_params",
    "match_smoothing_execute",
    "match_smoothing_validate",
    "match_smoothing",
]