# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SUSAN_METADATA = Metadata(
    id="susan",
    name="susan",
    package="fsl",
)

Susan = typing.TypedDict(
    "Susan",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/susan"]],
        "input_file": InputPathType,
        "brightness_threshold": float,
        "spatial_size": float,
        "dimensionality": float,
        "use_median_filter": float,
        "n_usans": float,
        "usan1": typing.NotRequired[InputPathType],
        "brightness_threshold1": typing.NotRequired[float],
        "usan2": typing.NotRequired[InputPathType],
        "brightness_threshold2": typing.NotRequired[float],
        "output_file": str,
    },
)

@dataclasses.dataclass
class SusanOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    filtered_output: OutputPathType
    """Filtered output image file"""

def susan_params(
    input_file: InputPathType,
    brightness_threshold: float,
    spatial_size: float,
    dimensionality: float,
    use_median_filter: float,
    n_usans: float,
    output_file: str,
    usan1: InputPathType | None = None,
    brightness_threshold1: float | None = None,
    usan2: InputPathType | None = None,
    brightness_threshold2: float | None = None,
) -> Susan:
    """
    Build parameters.

    Args:
        input_file: Input image file
        brightness_threshold: Brightness threshold; should be greater than noise\
            level and less than contrast of edges to be preserved
        spatial_size: Spatial size (sigma, i.e., half-width) of smoothing, in mm
        dimensionality: Dimensionality (2 or 3), for within-plane (2) or fully 3D\
            (3) smoothing
        use_median_filter: Use median filter for cases where single-point noise is\
            detected (0 or 1)
        n_usans: Determine if the smoothing area is found from secondary images (0,\
            1 or 2)
        output_file: Output image file
        usan1: First USAN image file
        brightness_threshold1: Brightness threshold for first USAN image
        usan2: Second USAN image file
        brightness_threshold2: Brightness threshold for second USAN image

    Returns:
        Parameter dictionary.
    """
    params: Susan = {
        "@type": "fsl/susan",
        "input_file": input_file,
        "brightness_threshold": brightness_threshold,
        "spatial_size": spatial_size,
        "dimensionality": dimensionality,
        "use_median_filter": use_median_filter,
        "n_usans": n_usans,
        "output_file": output_file,
    }
    if usan1 is not None:
        params["usan1"] = usan1
    if brightness_threshold1 is not None:
        params["brightness_threshold1"] = brightness_threshold1
    if usan2 is not None:
        params["usan2"] = usan2
    if brightness_threshold2 is not None:
        params["brightness_threshold2"] = brightness_threshold2
    return params

def susan_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Susan."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("brightness_threshold", None) is None:
        raise StyxValidationError("`brightness_threshold` must not be None")
    if not isinstance(params["brightness_threshold"], (float, int)):
        raise StyxValidationError(f'`brightness_threshold` has the wrong type: Received `{type(params["brightness_threshold"])}` expected `float`')
    if params.get("spatial_size", None) is None:
        raise StyxValidationError("`spatial_size` must not be None")
    if not isinstance(params["spatial_size"], (float, int)):
        raise StyxValidationError(f'`spatial_size` has the wrong type: Received `{type(params["spatial_size"])}` expected `float`')
    if params.get("dimensionality", None) is None:
        raise StyxValidationError("`dimensionality` must not be None")
    if not isinstance(params["dimensionality"], (float, int)):
        raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `float`')
    if not (2 <= params["dimensionality"] <= 3):
        raise StyxValidationError("Parameter `dimensionality` must be between 2 and 3 (inclusive)")
    if params.get("use_median_filter", None) is None:
        raise StyxValidationError("`use_median_filter` must not be None")
    if not isinstance(params["use_median_filter"], (float, int)):
        raise StyxValidationError(f'`use_median_filter` has the wrong type: Received `{type(params["use_median_filter"])}` expected `float`')
    if not (0 <= params["use_median_filter"] <= 1):
        raise StyxValidationError("Parameter `use_median_filter` must be between 0 and 1 (inclusive)")
    if params.get("n_usans", None) is None:
        raise StyxValidationError("`n_usans` must not be None")
    if not isinstance(params["n_usans"], (float, int)):
        raise StyxValidationError(f'`n_usans` has the wrong type: Received `{type(params["n_usans"])}` expected `float`')
    if not (0 <= params["n_usans"] <= 2):
        raise StyxValidationError("Parameter `n_usans` must be between 0 and 2 (inclusive)")
    if params.get("usan1", None) is not None:
        if not isinstance(params["usan1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`usan1` has the wrong type: Received `{type(params["usan1"])}` expected `InputPathType`')
    if params.get("brightness_threshold1", None) is not None:
        if not isinstance(params["brightness_threshold1"], (float, int)):
            raise StyxValidationError(f'`brightness_threshold1` has the wrong type: Received `{type(params["brightness_threshold1"])}` expected `float`')
    if params.get("usan2", None) is not None:
        if not isinstance(params["usan2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`usan2` has the wrong type: Received `{type(params["usan2"])}` expected `InputPathType`')
    if params.get("brightness_threshold2", None) is not None:
        if not isinstance(params["brightness_threshold2"], (float, int)):
            raise StyxValidationError(f'`brightness_threshold2` has the wrong type: Received `{type(params["brightness_threshold2"])}` expected `float`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')

def susan_cargs(params: Susan, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("susan")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(str(params["brightness_threshold"]))
    cargs.append(str(params["spatial_size"]))
    cargs.append(str(params["dimensionality"]))
    cargs.append(str(params["use_median_filter"]))
    cargs.append(str(params["n_usans"]))
    v_0 = params.get("usan1")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("brightness_threshold1")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("usan2")
    if v_2 is not None:
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("brightness_threshold2")
    if v_3 is not None:
        cargs.append(str(v_3))
    cargs.append(params["output_file"])
    return cargs

def susan_outputs(params: Susan, execution: Execution) -> SusanOutputs:
    """Build the SusanOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    filtered_output_v: OutputPathType = execution.output_file(params["output_file"])
    return SusanOutputs(
        root=root_v,
        filtered_output=filtered_output_v,
    )

def susan_execute(params: Susan, runner: Runner | None = None) -> SusanOutputs:
    """
    susan

    Non-linear noise reduction filtering tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    susan_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SUSAN_METADATA)
    execution.params(params)
    args = susan_cargs(params, execution)
    out = susan_outputs(params, execution)
    execution.run(args)
    return out

def susan(
    input_file: InputPathType,
    brightness_threshold: float,
    spatial_size: float,
    dimensionality: float,
    use_median_filter: float,
    n_usans: float,
    output_file: str,
    usan1: InputPathType | None = None,
    brightness_threshold1: float | None = None,
    usan2: InputPathType | None = None,
    brightness_threshold2: float | None = None,
    runner: Runner | None = None,
) -> SusanOutputs:
    """
    susan

    Non-linear noise reduction filtering tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input image file
        brightness_threshold: Brightness threshold; should be greater than noise\
            level and less than contrast of edges to be preserved
        spatial_size: Spatial size (sigma, i.e., half-width) of smoothing, in mm
        dimensionality: Dimensionality (2 or 3), for within-plane (2) or fully 3D\
            (3) smoothing
        use_median_filter: Use median filter for cases where single-point noise is\
            detected (0 or 1)
        n_usans: Determine if the smoothing area is found from secondary images (0,\
            1 or 2)
        output_file: Output image file
        usan1: First USAN image file
        brightness_threshold1: Brightness threshold for first USAN image
        usan2: Second USAN image file
        brightness_threshold2: Brightness threshold for second USAN image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = susan_params(
        input_file=input_file,
        brightness_threshold=brightness_threshold,
        spatial_size=spatial_size,
        dimensionality=dimensionality,
        use_median_filter=use_median_filter,
        n_usans=n_usans,
        output_file=output_file,
        usan1=usan1,
        brightness_threshold1=brightness_threshold1,
        usan2=usan2,
        brightness_threshold2=brightness_threshold2,
    )
    return susan_execute(params, runner)

__all__ = [
    "Susan",
    "SusanOutputs",
    "SUSAN_METADATA",
    "susan_cargs",
    "susan_outputs",
    "susan_params",
    "susan_execute",
    "susan_validate",
    "susan",
]