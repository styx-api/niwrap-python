# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLSPLIT_METADATA = Metadata(
    id="fslsplit",
    name="fslsplit",
    package="fsl",
)

Fslsplit = typing.TypedDict(
    "Fslsplit",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslsplit"]],
        "infile": InputPathType,
        "output_basename": typing.NotRequired[str],
        "separation_x": typing.NotRequired[bool],
        "separation_y": typing.NotRequired[bool],
        "separation_z": typing.NotRequired[bool],
        "separation_time": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FslsplitOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_files: OutputPathType | None
    """Output volumes/slices"""

def fslsplit_params(
    infile: InputPathType,
    output_basename: str | None = None,
    separation_x: bool = False,
    separation_y: bool = False,
    separation_z: bool = False,
    separation_time: bool = False,
) -> Fslsplit:
    """
    Build parameters.

    Args:
        infile: Input image (e.g. img.nii.gz)
        output_basename: Output basename (default: vol)
        separation_x: Separate images in the x direction
        separation_y: Separate images in the y direction
        separation_z: Separate images in the z direction
        separation_time: Separate images in time (default behaviour)

    Returns:
        Parameter dictionary.
    """
    params: Fslsplit = {
        "@type": "fsl/fslsplit",
        "infile": infile,
        "separation_x": separation_x,
        "separation_y": separation_y,
        "separation_z": separation_z,
        "separation_time": separation_time,
    }
    if output_basename is not None:
        params["output_basename"] = output_basename
    return params

def fslsplit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslsplit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("output_basename", None) is not None:
        if not isinstance(params["output_basename"], str):
            raise StyxValidationError(f'`output_basename` has the wrong type: Received `{type(params["output_basename"])}` expected `str`')
    if params.get("separation_x", None) is not None:
        if not isinstance(params["separation_x"], bool):
            raise StyxValidationError(f'`separation_x` has the wrong type: Received `{type(params["separation_x"])}` expected `bool`')
    if params.get("separation_y", None) is not None:
        if not isinstance(params["separation_y"], bool):
            raise StyxValidationError(f'`separation_y` has the wrong type: Received `{type(params["separation_y"])}` expected `bool`')
    if params.get("separation_z", None) is not None:
        if not isinstance(params["separation_z"], bool):
            raise StyxValidationError(f'`separation_z` has the wrong type: Received `{type(params["separation_z"])}` expected `bool`')
    if params.get("separation_time", None) is not None:
        if not isinstance(params["separation_time"], bool):
            raise StyxValidationError(f'`separation_time` has the wrong type: Received `{type(params["separation_time"])}` expected `bool`')

def fslsplit_cargs(params: Fslsplit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslsplit")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("output_basename")
    if v_0 is not None:
        cargs.append(v_0)
    if params.get("separation_x"):
        cargs.append("-x")
    if params.get("separation_y"):
        cargs.append("-y")
    if params.get("separation_z"):
        cargs.append("-z")
    if params.get("separation_time"):
        cargs.append("-t")
    return cargs

def fslsplit_outputs(params: Fslsplit, execution: Execution) -> FslsplitOutputs:
    """Build the FslsplitOutputs object for this tool."""
    out_files_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_basename")
    if __v0 is not None:
        out_files_v = execution.output_file((__v0 if __v0 is not None else ""))
    return FslsplitOutputs(
        root=root_v,
        out_files=out_files_v,
    )

def fslsplit_execute(params: Fslsplit, runner: Runner | None = None) -> FslsplitOutputs:
    """
    fslsplit

    Split a 4D image into separate volumes or a 3D image into separate slices

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslsplit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLSPLIT_METADATA)
    execution.params(params)
    args = fslsplit_cargs(params, execution)
    out = fslsplit_outputs(params, execution)
    execution.run(args)
    return out

def fslsplit(
    infile: InputPathType,
    output_basename: str | None = None,
    separation_x: bool = False,
    separation_y: bool = False,
    separation_z: bool = False,
    separation_time: bool = False,
    runner: Runner | None = None,
) -> FslsplitOutputs:
    """
    fslsplit

    Split a 4D image into separate volumes or a 3D image into separate slices

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input image (e.g. img.nii.gz)
        output_basename: Output basename (default: vol)
        separation_x: Separate images in the x direction
        separation_y: Separate images in the y direction
        separation_z: Separate images in the z direction
        separation_time: Separate images in time (default behaviour)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslsplit_params(
        infile=infile,
        output_basename=output_basename,
        separation_x=separation_x,
        separation_y=separation_y,
        separation_z=separation_z,
        separation_time=separation_time,
    )
    return fslsplit_execute(params, runner)

__all__ = [
    "Fslsplit",
    "FslsplitOutputs",
    "FSLSPLIT_METADATA",
    "fslsplit_cargs",
    "fslsplit_outputs",
    "fslsplit_params",
    "fslsplit_execute",
    "fslsplit_validate",
    "fslsplit",
]