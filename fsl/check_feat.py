# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CHECK_FEAT_METADATA = Metadata(
    id="checkFEAT",
    name="checkFEAT",
    package="fsl",
)

CheckFeat = typing.TypedDict(
    "CheckFeat",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/checkFEAT"]],
        "report_file": InputPathType,
        "report_log_file": InputPathType,
    },
)

@dataclasses.dataclass
class CheckFeatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_report: OutputPathType
    """Output HTML report"""
    output_report_log: OutputPathType
    """Output HTML report log"""

def check_feat_params(
    report_file: InputPathType,
    report_log_file: InputPathType,
) -> CheckFeat:
    """
    Build parameters.

    Args:
        report_file: Path to the HTML report
        report_log_file: Path to the HTML report log

    Returns:
        Parameter dictionary.
    """
    params: CheckFeat = {
        "@type": "fsl/checkFEAT",
        "report_file": report_file,
        "report_log_file": report_log_file,
    }
    return params

def check_feat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CheckFeat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("report_file", None) is None:
        raise StyxValidationError("`report_file` must not be None")
    if not isinstance(params["report_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`report_file` has the wrong type: Received `{type(params["report_file"])}` expected `InputPathType`')
    if params.get("report_log_file", None) is None:
        raise StyxValidationError("`report_log_file` must not be None")
    if not isinstance(params["report_log_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`report_log_file` has the wrong type: Received `{type(params["report_log_file"])}` expected `InputPathType`')

def check_feat_cargs(params: CheckFeat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("checkFEAT")
    cargs.append(execution.input_file(params["report_file"]))
    cargs.append(execution.input_file(params["report_log_file"]))
    return cargs

def check_feat_outputs(params: CheckFeat, execution: Execution) -> CheckFeatOutputs:
    """Build the CheckFeatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_report_v: OutputPathType = execution.output_file("output_report.html")
    output_report_log_v: OutputPathType = execution.output_file("output_report_log.html")
    return CheckFeatOutputs(
        root=root_v,
        output_report=output_report_v,
        output_report_log=output_report_log_v,
    )

def check_feat_execute(params: CheckFeat, runner: Runner | None = None) -> CheckFeatOutputs:
    """
    checkFEAT

    Perform checks on FEAT analysis results

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    check_feat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CHECK_FEAT_METADATA)
    execution.params(params)
    args = check_feat_cargs(params, execution)
    out = check_feat_outputs(params, execution)
    execution.run(args)
    return out

def check_feat(
    report_file: InputPathType,
    report_log_file: InputPathType,
    runner: Runner | None = None,
) -> CheckFeatOutputs:
    """
    checkFEAT

    Perform checks on FEAT analysis results

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        report_file: Path to the HTML report
        report_log_file: Path to the HTML report log
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = check_feat_params(
        report_file=report_file,
        report_log_file=report_log_file,
    )
    return check_feat_execute(params, runner)

__all__ = [
    "CheckFeat",
    "CheckFeatOutputs",
    "CHECK_FEAT_METADATA",
    "check_feat_cargs",
    "check_feat_outputs",
    "check_feat_params",
    "check_feat_execute",
    "check_feat_validate",
    "check_feat",
]