# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TTOZ_METADATA = Metadata(
    id="ttoz",
    name="ttoz",
    package="fsl",
)

Ttoz = typing.TypedDict(
    "Ttoz",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/ttoz"]],
        "varsfile": InputPathType,
        "cbsfile": InputPathType,
        "dof": int,
        "outputvol": typing.NotRequired[str],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TtozOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_zvol: OutputPathType | None
    """Output Z-statistic volume"""

def ttoz_params(
    varsfile: InputPathType,
    cbsfile: InputPathType,
    dof: int,
    outputvol: str | None = None,
    help_flag: bool = False,
) -> Ttoz:
    """
    Build parameters.

    Args:
        varsfile: Input variables file
        cbsfile: Input CBS file
        dof: Degrees of freedom
        outputvol: Output volume name (default is zstats)
        help_flag: Display help information

    Returns:
        Parameter dictionary.
    """
    params: Ttoz = {
        "@type": "fsl/ttoz",
        "varsfile": varsfile,
        "cbsfile": cbsfile,
        "dof": dof,
        "help_flag": help_flag,
    }
    if outputvol is not None:
        params["outputvol"] = outputvol
    return params

def ttoz_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ttoz."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("varsfile", None) is None:
        raise StyxValidationError("`varsfile` must not be None")
    if not isinstance(params["varsfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`varsfile` has the wrong type: Received `{type(params["varsfile"])}` expected `InputPathType`')
    if params.get("cbsfile", None) is None:
        raise StyxValidationError("`cbsfile` must not be None")
    if not isinstance(params["cbsfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cbsfile` has the wrong type: Received `{type(params["cbsfile"])}` expected `InputPathType`')
    if params.get("dof", None) is None:
        raise StyxValidationError("`dof` must not be None")
    if not isinstance(params["dof"], int):
        raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `int`')
    if params.get("outputvol", None) is not None:
        if not isinstance(params["outputvol"], str):
            raise StyxValidationError(f'`outputvol` has the wrong type: Received `{type(params["outputvol"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def ttoz_cargs(params: Ttoz, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ttoz")
    cargs.append(execution.input_file(params["varsfile"]))
    cargs.append(execution.input_file(params["cbsfile"]))
    cargs.append(str(params["dof"]))
    v_0 = params.get("outputvol")
    if v_0 is not None:
        cargs.append("-zout")
        cargs.append(v_0)
    if params.get("help_flag"):
        cargs.append("-help")
    return cargs

def ttoz_outputs(params: Ttoz, execution: Execution) -> TtozOutputs:
    """Build the TtozOutputs object for this tool."""
    output_zvol_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outputvol")
    if __v0 is not None:
        output_zvol_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return TtozOutputs(
        root=root_v,
        output_zvol=output_zvol_v,
    )

def ttoz_execute(params: Ttoz, runner: Runner | None = None) -> TtozOutputs:
    """
    ttoz

    Tool to convert a T-statistic image to a Z-statistic image

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ttoz_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TTOZ_METADATA)
    execution.params(params)
    args = ttoz_cargs(params, execution)
    out = ttoz_outputs(params, execution)
    execution.run(args)
    return out

def ttoz(
    varsfile: InputPathType,
    cbsfile: InputPathType,
    dof: int,
    outputvol: str | None = None,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> TtozOutputs:
    """
    ttoz

    Tool to convert a T-statistic image to a Z-statistic image

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        varsfile: Input variables file
        cbsfile: Input CBS file
        dof: Degrees of freedom
        outputvol: Output volume name (default is zstats)
        help_flag: Display help information
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ttoz_params(
        varsfile=varsfile,
        cbsfile=cbsfile,
        dof=dof,
        outputvol=outputvol,
        help_flag=help_flag,
    )
    return ttoz_execute(params, runner)

__all__ = [
    "Ttoz",
    "TtozOutputs",
    "TTOZ_METADATA",
    "ttoz_cargs",
    "ttoz_outputs",
    "ttoz_params",
    "ttoz_execute",
    "ttoz_validate",
    "ttoz",
]