# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SWAP_VOXELWISE_METADATA = Metadata(
    id="swap_voxelwise",
    name="swap_voxelwise",
    package="fsl",
)

SwapVoxelwise = typing.TypedDict(
    "SwapVoxelwise",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/swap_voxelwise"]],
        "vectors_file_list": InputPathType,
        "scalars_file_list": typing.NotRequired[InputPathType],
        "mask": InputPathType,
        "output_base_name": typing.NotRequired[str],
        "reorder_mode": typing.NotRequired[str],
        "init_mask": typing.NotRequired[InputPathType],
        "crossing_thresh": typing.NotRequired[float],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SwapVoxelwiseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    reordered_output: OutputPathType | None
    """Output file with reordered vectors (default name 'reordered.nii.gz')"""

def swap_voxelwise_params(
    vectors_file_list: InputPathType,
    mask: InputPathType,
    scalars_file_list: InputPathType | None = None,
    output_base_name: str | None = None,
    reorder_mode: str | None = None,
    init_mask: InputPathType | None = None,
    crossing_thresh: float | None = None,
    verbose_flag: bool = False,
) -> SwapVoxelwise:
    """
    Build parameters.

    Args:
        vectors_file_list: Text file containing list of vectors
        mask: Filename of brain mask or skeleton
        scalars_file_list: Text file containing list of scalars
        output_base_name: Output base name
        reorder_mode: Reordering mode - choose between 'voxels' (default) or\
            'volumes'
        init_mask: Filename of initialization mask
        crossing_thresh: Threshold for considering a crossing fibre region -\
            default=0.1
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: SwapVoxelwise = {
        "@type": "fsl/swap_voxelwise",
        "vectors_file_list": vectors_file_list,
        "mask": mask,
        "verbose_flag": verbose_flag,
    }
    if scalars_file_list is not None:
        params["scalars_file_list"] = scalars_file_list
    if output_base_name is not None:
        params["output_base_name"] = output_base_name
    if reorder_mode is not None:
        params["reorder_mode"] = reorder_mode
    if init_mask is not None:
        params["init_mask"] = init_mask
    if crossing_thresh is not None:
        params["crossing_thresh"] = crossing_thresh
    return params

def swap_voxelwise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SwapVoxelwise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vectors_file_list", None) is None:
        raise StyxValidationError("`vectors_file_list` must not be None")
    if not isinstance(params["vectors_file_list"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vectors_file_list` has the wrong type: Received `{type(params["vectors_file_list"])}` expected `InputPathType`')
    if params.get("scalars_file_list", None) is not None:
        if not isinstance(params["scalars_file_list"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scalars_file_list` has the wrong type: Received `{type(params["scalars_file_list"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("output_base_name", None) is not None:
        if not isinstance(params["output_base_name"], str):
            raise StyxValidationError(f'`output_base_name` has the wrong type: Received `{type(params["output_base_name"])}` expected `str`')
    if params.get("reorder_mode", None) is not None:
        if not isinstance(params["reorder_mode"], str):
            raise StyxValidationError(f'`reorder_mode` has the wrong type: Received `{type(params["reorder_mode"])}` expected `str`')
    if params.get("init_mask", None) is not None:
        if not isinstance(params["init_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_mask` has the wrong type: Received `{type(params["init_mask"])}` expected `InputPathType`')
    if params.get("crossing_thresh", None) is not None:
        if not isinstance(params["crossing_thresh"], (float, int)):
            raise StyxValidationError(f'`crossing_thresh` has the wrong type: Received `{type(params["crossing_thresh"])}` expected `float`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def swap_voxelwise_cargs(params: SwapVoxelwise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("swap_voxelwise")
    cargs.append("-v")
    cargs.append(execution.input_file(params["vectors_file_list"]))
    v_0 = params.get("scalars_file_list")
    if v_0 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_0))
    cargs.append("-m")
    cargs.append(execution.input_file(params["mask"]))
    v_1 = params.get("output_base_name")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(v_1)
    v_2 = params.get("reorder_mode")
    if v_2 is not None:
        cargs.append("--mode")
        cargs.append(v_2)
    v_3 = params.get("init_mask")
    if v_3 is not None:
        cargs.append("--initmask")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("crossing_thresh")
    if v_4 is not None:
        cargs.append("--xthresh")
        cargs.append(str(v_4))
    if params.get("verbose_flag"):
        cargs.append("-V")
    return cargs

def swap_voxelwise_outputs(params: SwapVoxelwise, execution: Execution) -> SwapVoxelwiseOutputs:
    """Build the SwapVoxelwiseOutputs object for this tool."""
    reordered_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_base_name")
    if __v0 is not None:
        reordered_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return SwapVoxelwiseOutputs(
        root=root_v,
        reordered_output=reordered_output_v,
    )

def swap_voxelwise_execute(params: SwapVoxelwise, runner: Runner | None = None) -> SwapVoxelwiseOutputs:
    """
    swap_voxelwise

    Reordering of vectors with direction preservation

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    swap_voxelwise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SWAP_VOXELWISE_METADATA)
    execution.params(params)
    args = swap_voxelwise_cargs(params, execution)
    out = swap_voxelwise_outputs(params, execution)
    execution.run(args)
    return out

def swap_voxelwise(
    vectors_file_list: InputPathType,
    mask: InputPathType,
    scalars_file_list: InputPathType | None = None,
    output_base_name: str | None = None,
    reorder_mode: str | None = None,
    init_mask: InputPathType | None = None,
    crossing_thresh: float | None = None,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> SwapVoxelwiseOutputs:
    """
    swap_voxelwise

    Reordering of vectors with direction preservation

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        vectors_file_list: Text file containing list of vectors
        mask: Filename of brain mask or skeleton
        scalars_file_list: Text file containing list of scalars
        output_base_name: Output base name
        reorder_mode: Reordering mode - choose between 'voxels' (default) or\
            'volumes'
        init_mask: Filename of initialization mask
        crossing_thresh: Threshold for considering a crossing fibre region -\
            default=0.1
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = swap_voxelwise_params(
        vectors_file_list=vectors_file_list,
        mask=mask,
        scalars_file_list=scalars_file_list,
        output_base_name=output_base_name,
        reorder_mode=reorder_mode,
        init_mask=init_mask,
        crossing_thresh=crossing_thresh,
        verbose_flag=verbose_flag,
    )
    return swap_voxelwise_execute(params, runner)

__all__ = [
    "SwapVoxelwise",
    "SwapVoxelwiseOutputs",
    "SWAP_VOXELWISE_METADATA",
    "swap_voxelwise_cargs",
    "swap_voxelwise_outputs",
    "swap_voxelwise_params",
    "swap_voxelwise_execute",
    "swap_voxelwise_validate",
    "swap_voxelwise",
]