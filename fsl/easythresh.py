# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EASYTHRESH_METADATA = Metadata(
    id="easythresh",
    name="easythresh",
    package="fsl",
)

Easythresh = typing.TypedDict(
    "Easythresh",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/easythresh"]],
        "raw_zstat_input": InputPathType,
        "brain_mask_input": InputPathType,
        "cluster_z_thresh_input": float,
        "cluster_prob_thresh_input": float,
        "background_image_input": InputPathType,
        "output_root": str,
        "mm_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class EasythreshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_thresh_image: OutputPathType
    """Output thresholded image"""

def easythresh_params(
    raw_zstat_input: InputPathType,
    brain_mask_input: InputPathType,
    cluster_z_thresh_input: float,
    cluster_prob_thresh_input: float,
    background_image_input: InputPathType,
    output_root: str,
    mm_flag: bool = False,
) -> Easythresh:
    """
    Build parameters.

    Args:
        raw_zstat_input: Input raw z-statistics image
        brain_mask_input: Brain mask image
        cluster_z_thresh_input: Cluster z-threshold
        cluster_prob_thresh_input: Cluster probability threshold
        background_image_input: Background image for thresholding
        output_root: Root of output file names
        mm_flag: Flag to indicate the use of mm (millimeters)

    Returns:
        Parameter dictionary.
    """
    params: Easythresh = {
        "@type": "fsl/easythresh",
        "raw_zstat_input": raw_zstat_input,
        "brain_mask_input": brain_mask_input,
        "cluster_z_thresh_input": cluster_z_thresh_input,
        "cluster_prob_thresh_input": cluster_prob_thresh_input,
        "background_image_input": background_image_input,
        "output_root": output_root,
        "mm_flag": mm_flag,
    }
    return params

def easythresh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Easythresh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("raw_zstat_input", None) is None:
        raise StyxValidationError("`raw_zstat_input` must not be None")
    if not isinstance(params["raw_zstat_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`raw_zstat_input` has the wrong type: Received `{type(params["raw_zstat_input"])}` expected `InputPathType`')
    if params.get("brain_mask_input", None) is None:
        raise StyxValidationError("`brain_mask_input` must not be None")
    if not isinstance(params["brain_mask_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain_mask_input` has the wrong type: Received `{type(params["brain_mask_input"])}` expected `InputPathType`')
    if params.get("cluster_z_thresh_input", None) is None:
        raise StyxValidationError("`cluster_z_thresh_input` must not be None")
    if not isinstance(params["cluster_z_thresh_input"], (float, int)):
        raise StyxValidationError(f'`cluster_z_thresh_input` has the wrong type: Received `{type(params["cluster_z_thresh_input"])}` expected `float`')
    if params.get("cluster_prob_thresh_input", None) is None:
        raise StyxValidationError("`cluster_prob_thresh_input` must not be None")
    if not isinstance(params["cluster_prob_thresh_input"], (float, int)):
        raise StyxValidationError(f'`cluster_prob_thresh_input` has the wrong type: Received `{type(params["cluster_prob_thresh_input"])}` expected `float`')
    if params.get("background_image_input", None) is None:
        raise StyxValidationError("`background_image_input` must not be None")
    if not isinstance(params["background_image_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`background_image_input` has the wrong type: Received `{type(params["background_image_input"])}` expected `InputPathType`')
    if params.get("output_root", None) is None:
        raise StyxValidationError("`output_root` must not be None")
    if not isinstance(params["output_root"], str):
        raise StyxValidationError(f'`output_root` has the wrong type: Received `{type(params["output_root"])}` expected `str`')
    if params.get("mm_flag", None) is not None:
        if not isinstance(params["mm_flag"], bool):
            raise StyxValidationError(f'`mm_flag` has the wrong type: Received `{type(params["mm_flag"])}` expected `bool`')

def easythresh_cargs(params: Easythresh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("easythresh")
    cargs.append(execution.input_file(params["raw_zstat_input"]))
    cargs.append(execution.input_file(params["brain_mask_input"]))
    cargs.append(str(params["cluster_z_thresh_input"]))
    cargs.append(str(params["cluster_prob_thresh_input"]))
    cargs.append(execution.input_file(params["background_image_input"]))
    cargs.append(params["output_root"])
    if params.get("mm_flag"):
        cargs.append("--mm")
    return cargs

def easythresh_outputs(params: Easythresh, execution: Execution) -> EasythreshOutputs:
    """Build the EasythreshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_thresh_image_v: OutputPathType = execution.output_file(f'{params["output_root"]}_thresh.nii.gz')
    return EasythreshOutputs(
        root=root_v,
        output_thresh_image=output_thresh_image_v,
    )

def easythresh_execute(params: Easythresh, runner: Runner | None = None) -> EasythreshOutputs:
    """
    easythresh

    Cluster-based statistical thresholding tool from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    easythresh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EASYTHRESH_METADATA)
    execution.params(params)
    args = easythresh_cargs(params, execution)
    out = easythresh_outputs(params, execution)
    execution.run(args)
    return out

def easythresh(
    raw_zstat_input: InputPathType,
    brain_mask_input: InputPathType,
    cluster_z_thresh_input: float,
    cluster_prob_thresh_input: float,
    background_image_input: InputPathType,
    output_root: str,
    mm_flag: bool = False,
    runner: Runner | None = None,
) -> EasythreshOutputs:
    """
    easythresh

    Cluster-based statistical thresholding tool from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        raw_zstat_input: Input raw z-statistics image
        brain_mask_input: Brain mask image
        cluster_z_thresh_input: Cluster z-threshold
        cluster_prob_thresh_input: Cluster probability threshold
        background_image_input: Background image for thresholding
        output_root: Root of output file names
        mm_flag: Flag to indicate the use of mm (millimeters)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = easythresh_params(
        raw_zstat_input=raw_zstat_input,
        brain_mask_input=brain_mask_input,
        cluster_z_thresh_input=cluster_z_thresh_input,
        cluster_prob_thresh_input=cluster_prob_thresh_input,
        background_image_input=background_image_input,
        output_root=output_root,
        mm_flag=mm_flag,
    )
    return easythresh_execute(params, runner)

__all__ = [
    "Easythresh",
    "EasythreshOutputs",
    "EASYTHRESH_METADATA",
    "easythresh_cargs",
    "easythresh_outputs",
    "easythresh_params",
    "easythresh_execute",
    "easythresh_validate",
    "easythresh",
]