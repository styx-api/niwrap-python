# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SYSTEMNOISE_METADATA = Metadata(
    id="systemnoise",
    name="systemnoise",
    package="fsl",
)

Systemnoise = typing.TypedDict(
    "Systemnoise",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/systemnoise"]],
        "input_signal": InputPathType,
        "output_signal": str,
        "noise_standard_deviation": float,
        "seed": typing.NotRequired[float],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SystemnoiseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_signal_file: OutputPathType
    """Output signal with added system noise"""

def systemnoise_params(
    input_signal: InputPathType,
    output_signal: str,
    noise_standard_deviation: float,
    seed: float | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Systemnoise:
    """
    Build parameters.

    Args:
        input_signal: Input signal (possum output matrix)
        output_signal: Output signal (possum matrix form)
        noise_standard_deviation: Set noise standard deviation (units of intensity)
        seed: Input seed value for the sequence
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Systemnoise = {
        "@type": "fsl/systemnoise",
        "input_signal": input_signal,
        "output_signal": output_signal,
        "noise_standard_deviation": noise_standard_deviation,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if seed is not None:
        params["seed"] = seed
    return params

def systemnoise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Systemnoise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_signal", None) is None:
        raise StyxValidationError("`input_signal` must not be None")
    if not isinstance(params["input_signal"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_signal` has the wrong type: Received `{type(params["input_signal"])}` expected `InputPathType`')
    if params.get("output_signal", None) is None:
        raise StyxValidationError("`output_signal` must not be None")
    if not isinstance(params["output_signal"], str):
        raise StyxValidationError(f'`output_signal` has the wrong type: Received `{type(params["output_signal"])}` expected `str`')
    if params.get("noise_standard_deviation", None) is None:
        raise StyxValidationError("`noise_standard_deviation` must not be None")
    if not isinstance(params["noise_standard_deviation"], (float, int)):
        raise StyxValidationError(f'`noise_standard_deviation` has the wrong type: Received `{type(params["noise_standard_deviation"])}` expected `float`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (float, int)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `float`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def systemnoise_cargs(params: Systemnoise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("systemnoise")
    cargs.append("--in")
    cargs.append(execution.input_file(params["input_signal"]))
    cargs.append("--out")
    cargs.append(params["output_signal"])
    cargs.append("--sigma")
    cargs.append(str(params["noise_standard_deviation"]))
    v_0 = params.get("seed")
    if v_0 is not None:
        cargs.append("--seed")
        cargs.append(str(v_0))
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    if params.get("help_flag"):
        cargs.append("--help")
    return cargs

def systemnoise_outputs(params: Systemnoise, execution: Execution) -> SystemnoiseOutputs:
    """Build the SystemnoiseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_signal_file_v: OutputPathType = execution.output_file(params["output_signal"])
    return SystemnoiseOutputs(
        root=root_v,
        output_signal_file=output_signal_file_v,
    )

def systemnoise_execute(params: Systemnoise, runner: Runner | None = None) -> SystemnoiseOutputs:
    """
    systemnoise

    Tool for adding system noise to a given signal using FSL's utilities

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    systemnoise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SYSTEMNOISE_METADATA)
    execution.params(params)
    args = systemnoise_cargs(params, execution)
    out = systemnoise_outputs(params, execution)
    execution.run(args)
    return out

def systemnoise(
    input_signal: InputPathType,
    output_signal: str,
    noise_standard_deviation: float,
    seed: float | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> SystemnoiseOutputs:
    """
    systemnoise

    Tool for adding system noise to a given signal using FSL's utilities

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_signal: Input signal (possum output matrix)
        output_signal: Output signal (possum matrix form)
        noise_standard_deviation: Set noise standard deviation (units of intensity)
        seed: Input seed value for the sequence
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = systemnoise_params(
        input_signal=input_signal,
        output_signal=output_signal,
        noise_standard_deviation=noise_standard_deviation,
        seed=seed,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return systemnoise_execute(params, runner)

__all__ = [
    "Systemnoise",
    "SystemnoiseOutputs",
    "SYSTEMNOISE_METADATA",
    "systemnoise_cargs",
    "systemnoise_outputs",
    "systemnoise_params",
    "systemnoise_execute",
    "systemnoise_validate",
    "systemnoise",
]