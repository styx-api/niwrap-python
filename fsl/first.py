# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIRST_METADATA = Metadata(
    id="first",
    name="first",
    package="fsl",
)

First = typing.TypedDict(
    "First",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/first"]],
        "input_file": InputPathType,
        "output_name": str,
        "input_model": InputPathType,
        "flirt_matrix": InputPathType,
        "verbose": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
        "input_model2": typing.NotRequired[InputPathType],
        "nmodes": typing.NotRequired[float],
        "intref": typing.NotRequired[bool],
        "multi_image_input": typing.NotRequired[bool],
        "binary_surface_output": typing.NotRequired[bool],
        "bmap_name": typing.NotRequired[InputPathType],
        "bvars": typing.NotRequired[InputPathType],
        "shcond": typing.NotRequired[bool],
        "loadbvars": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FirstOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmented_output_image: OutputPathType
    """Segmented output image"""

def first_params(
    input_file: InputPathType,
    output_name: str,
    input_model: InputPathType,
    flirt_matrix: InputPathType,
    verbose: bool = False,
    help: bool = False,
    input_model2: InputPathType | None = None,
    nmodes: float | None = None,
    intref: bool = False,
    multi_image_input: bool = False,
    binary_surface_output: bool = False,
    bmap_name: InputPathType | None = None,
    bvars: InputPathType | None = None,
    shcond: bool = False,
    loadbvars: bool = False,
) -> First:
    """
    Build parameters.

    Args:
        input_file: Filename of input image to be segmented.
        output_name: Output name
        input_model: Filename of input model (the structure to be segmented).
        flirt_matrix: Filename of flirt matrix that transform input image to MNI\
            space (output of first_flirt).
        verbose: Switch on diagnostic messages
        help: Display help message
        input_model2: Filename of second input model (the structure to be\
            segmented).
        nmodes: Specifies number of modes used.
        intref: Use structure specified by modelname2 as intensity reference
        multi_image_input: Use structure specified by modelname2 as intensity\
            reference
        binary_surface_output: Use structure specified by modelname2 as intensity\
            reference
        bmap_name: Filename of conditional mapping matrix
        bvars: Initialize using bvars from a previous segmentation. When using with\
            --shcond specifies the shape of the structure we are conditioning on.
        shcond: Use conditional shape probability
        loadbvars: Load initial parameter estimates from a previous segmentation.

    Returns:
        Parameter dictionary.
    """
    params: First = {
        "@type": "fsl/first",
        "input_file": input_file,
        "output_name": output_name,
        "input_model": input_model,
        "flirt_matrix": flirt_matrix,
        "verbose": verbose,
        "help": help,
        "intref": intref,
        "multi_image_input": multi_image_input,
        "binary_surface_output": binary_surface_output,
        "shcond": shcond,
        "loadbvars": loadbvars,
    }
    if input_model2 is not None:
        params["input_model2"] = input_model2
    if nmodes is not None:
        params["nmodes"] = nmodes
    if bmap_name is not None:
        params["bmap_name"] = bmap_name
    if bvars is not None:
        params["bvars"] = bvars
    return params

def first_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid First."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_name", None) is None:
        raise StyxValidationError("`output_name` must not be None")
    if not isinstance(params["output_name"], str):
        raise StyxValidationError(f'`output_name` has the wrong type: Received `{type(params["output_name"])}` expected `str`')
    if params.get("input_model", None) is None:
        raise StyxValidationError("`input_model` must not be None")
    if not isinstance(params["input_model"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_model` has the wrong type: Received `{type(params["input_model"])}` expected `InputPathType`')
    if params.get("flirt_matrix", None) is None:
        raise StyxValidationError("`flirt_matrix` must not be None")
    if not isinstance(params["flirt_matrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`flirt_matrix` has the wrong type: Received `{type(params["flirt_matrix"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("input_model2", None) is not None:
        if not isinstance(params["input_model2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_model2` has the wrong type: Received `{type(params["input_model2"])}` expected `InputPathType`')
    if params.get("nmodes", None) is not None:
        if not isinstance(params["nmodes"], (float, int)):
            raise StyxValidationError(f'`nmodes` has the wrong type: Received `{type(params["nmodes"])}` expected `float`')
    if params.get("intref", None) is not None:
        if not isinstance(params["intref"], bool):
            raise StyxValidationError(f'`intref` has the wrong type: Received `{type(params["intref"])}` expected `bool`')
    if params.get("multi_image_input", None) is not None:
        if not isinstance(params["multi_image_input"], bool):
            raise StyxValidationError(f'`multi_image_input` has the wrong type: Received `{type(params["multi_image_input"])}` expected `bool`')
    if params.get("binary_surface_output", None) is not None:
        if not isinstance(params["binary_surface_output"], bool):
            raise StyxValidationError(f'`binary_surface_output` has the wrong type: Received `{type(params["binary_surface_output"])}` expected `bool`')
    if params.get("bmap_name", None) is not None:
        if not isinstance(params["bmap_name"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bmap_name` has the wrong type: Received `{type(params["bmap_name"])}` expected `InputPathType`')
    if params.get("bvars", None) is not None:
        if not isinstance(params["bvars"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvars` has the wrong type: Received `{type(params["bvars"])}` expected `InputPathType`')
    if params.get("shcond", None) is not None:
        if not isinstance(params["shcond"], bool):
            raise StyxValidationError(f'`shcond` has the wrong type: Received `{type(params["shcond"])}` expected `bool`')
    if params.get("loadbvars", None) is not None:
        if not isinstance(params["loadbvars"], bool):
            raise StyxValidationError(f'`loadbvars` has the wrong type: Received `{type(params["loadbvars"])}` expected `bool`')

def first_cargs(params: First, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("first")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-k")
    cargs.append(params["output_name"])
    cargs.append("-m")
    cargs.append(execution.input_file(params["input_model"]))
    cargs.append("-l")
    cargs.append(execution.input_file(params["flirt_matrix"]))
    if params.get("verbose"):
        cargs.append("-v")
    if params.get("help"):
        cargs.append("-h")
    v_0 = params.get("input_model2")
    if v_0 is not None:
        cargs.append("-p")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("nmodes")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    if params.get("intref"):
        cargs.append("--intref")
    if params.get("multi_image_input"):
        cargs.append("--multiImageInput")
    if params.get("binary_surface_output"):
        cargs.append("--binarySurfaceOutput")
    v_2 = params.get("bmap_name")
    if v_2 is not None:
        cargs.append("-b")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("bvars")
    if v_3 is not None:
        cargs.append("-o")
        cargs.append(execution.input_file(v_3))
    if params.get("shcond"):
        cargs.append("--shcond")
    if params.get("loadbvars"):
        cargs.append("--loadbvars")
    return cargs

def first_outputs(params: First, execution: Execution) -> FirstOutputs:
    """Build the FirstOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    segmented_output_image_v: OutputPathType = execution.output_file(f'{params["output_name"]}_seg.nii.gz')
    return FirstOutputs(
        root=root_v,
        segmented_output_image=segmented_output_image_v,
    )

def first_execute(params: First, runner: Runner | None = None) -> FirstOutputs:
    """
    first

    A command-line tool for segmenting subcortical structures in MRI images using models and transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    first_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIRST_METADATA)
    execution.params(params)
    args = first_cargs(params, execution)
    out = first_outputs(params, execution)
    execution.run(args)
    return out

def first(
    input_file: InputPathType,
    output_name: str,
    input_model: InputPathType,
    flirt_matrix: InputPathType,
    verbose: bool = False,
    help: bool = False,
    input_model2: InputPathType | None = None,
    nmodes: float | None = None,
    intref: bool = False,
    multi_image_input: bool = False,
    binary_surface_output: bool = False,
    bmap_name: InputPathType | None = None,
    bvars: InputPathType | None = None,
    shcond: bool = False,
    loadbvars: bool = False,
    runner: Runner | None = None,
) -> FirstOutputs:
    """
    first

    A command-line tool for segmenting subcortical structures in MRI images using models and transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Filename of input image to be segmented.
        output_name: Output name
        input_model: Filename of input model (the structure to be segmented).
        flirt_matrix: Filename of flirt matrix that transform input image to MNI\
            space (output of first_flirt).
        verbose: Switch on diagnostic messages
        help: Display help message
        input_model2: Filename of second input model (the structure to be\
            segmented).
        nmodes: Specifies number of modes used.
        intref: Use structure specified by modelname2 as intensity reference
        multi_image_input: Use structure specified by modelname2 as intensity\
            reference
        binary_surface_output: Use structure specified by modelname2 as intensity\
            reference
        bmap_name: Filename of conditional mapping matrix
        bvars: Initialize using bvars from a previous segmentation. When using with\
            --shcond specifies the shape of the structure we are conditioning on.
        shcond: Use conditional shape probability
        loadbvars: Load initial parameter estimates from a previous segmentation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = first_params(
        input_file=input_file,
        output_name=output_name,
        input_model=input_model,
        flirt_matrix=flirt_matrix,
        verbose=verbose,
        help=help,
        input_model2=input_model2,
        nmodes=nmodes,
        intref=intref,
        multi_image_input=multi_image_input,
        binary_surface_output=binary_surface_output,
        bmap_name=bmap_name,
        bvars=bvars,
        shcond=shcond,
        loadbvars=loadbvars,
    )
    return first_execute(params, runner)

__all__ = [
    "First",
    "FirstOutputs",
    "FIRST_METADATA",
    "first_cargs",
    "first_outputs",
    "first_params",
    "first_execute",
    "first_validate",
    "first",
]