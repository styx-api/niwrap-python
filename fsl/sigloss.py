# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIGLOSS_METADATA = Metadata(
    id="sigloss",
    name="sigloss",
    package="fsl",
)

SiglossSliceDirection = typing.Literal["x"] | typing.Literal["y"] | typing.Literal["z"]

Sigloss = typing.TypedDict(
    "Sigloss",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/sigloss"]],
        "input_b0map": InputPathType,
        "output_sigloss": str,
        "input_mask": typing.NotRequired[InputPathType],
        "echo_time": typing.NotRequired[float],
        "slice_direction": typing.NotRequired[SiglossSliceDirection],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SiglossOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def sigloss_params(
    input_b0map: InputPathType,
    output_sigloss: str,
    input_mask: InputPathType | None = None,
    echo_time: float | None = None,
    slice_direction: SiglossSliceDirection | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Sigloss:
    """
    Build parameters.

    Args:
        input_b0map: Input b0 map image filename (in rad/s)
        output_sigloss: Output signal loss image filename
        input_mask: Input mask filename
        echo_time: Echo time (in seconds)
        slice_direction: Slice direction (either x, y or z)
        verbose_flag: Switch on diagnostic messages
        help_flag: Display this help message

    Returns:
        Parameter dictionary.
    """
    params: Sigloss = {
        "@type": "fsl/sigloss",
        "input_b0map": input_b0map,
        "output_sigloss": output_sigloss,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if input_mask is not None:
        params["input_mask"] = input_mask
    if echo_time is not None:
        params["echo_time"] = echo_time
    if slice_direction is not None:
        params["slice_direction"] = slice_direction
    return params

def sigloss_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sigloss."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_b0map", None) is None:
        raise StyxValidationError("`input_b0map` must not be None")
    if not isinstance(params["input_b0map"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_b0map` has the wrong type: Received `{type(params["input_b0map"])}` expected `InputPathType`')
    if params.get("output_sigloss", None) is None:
        raise StyxValidationError("`output_sigloss` must not be None")
    if not isinstance(params["output_sigloss"], str):
        raise StyxValidationError(f'`output_sigloss` has the wrong type: Received `{type(params["output_sigloss"])}` expected `str`')
    if params.get("input_mask", None) is not None:
        if not isinstance(params["input_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_mask` has the wrong type: Received `{type(params["input_mask"])}` expected `InputPathType`')
    if params.get("echo_time", None) is not None:
        if not isinstance(params["echo_time"], (float, int)):
            raise StyxValidationError(f'`echo_time` has the wrong type: Received `{type(params["echo_time"])}` expected `float`')
    if params.get("slice_direction", None) is not None:
        if not isinstance(params["slice_direction"], str):
            raise StyxValidationError(f'`slice_direction` has the wrong type: Received `{type(params["slice_direction"])}` expected `SiglossSliceDirection`')
        if params["slice_direction"] not in ["x", "y", "z"]:
            raise StyxValidationError("Parameter `slice_direction` must be one of [\"x\", \"y\", \"z\"]")
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def sigloss_cargs(params: Sigloss, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sigloss")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_b0map"]))
    cargs.append("-s")
    cargs.append(params["output_sigloss"])
    v_0 = params.get("input_mask")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("echo_time")
    if v_1 is not None:
        cargs.append("--te")
        cargs.append(str(v_1))
    v_2 = params.get("slice_direction")
    if v_2 is not None:
        cargs.append("-d")
        cargs.append(str(v_2))
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def sigloss_outputs(params: Sigloss, execution: Execution) -> SiglossOutputs:
    """Build the SiglossOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SiglossOutputs(
        root=root_v,
    )

def sigloss_execute(params: Sigloss, runner: Runner | None = None) -> SiglossOutputs:
    """
    sigloss

    Estimates signal loss from a field map (in rad/s)

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sigloss_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIGLOSS_METADATA)
    execution.params(params)
    args = sigloss_cargs(params, execution)
    out = sigloss_outputs(params, execution)
    execution.run(args)
    return out

def sigloss(
    input_b0map: InputPathType,
    output_sigloss: str,
    input_mask: InputPathType | None = None,
    echo_time: float | None = None,
    slice_direction: SiglossSliceDirection | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> SiglossOutputs:
    """
    sigloss

    Estimates signal loss from a field map (in rad/s)

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_b0map: Input b0 map image filename (in rad/s)
        output_sigloss: Output signal loss image filename
        input_mask: Input mask filename
        echo_time: Echo time (in seconds)
        slice_direction: Slice direction (either x, y or z)
        verbose_flag: Switch on diagnostic messages
        help_flag: Display this help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sigloss_params(
        input_b0map=input_b0map,
        output_sigloss=output_sigloss,
        input_mask=input_mask,
        echo_time=echo_time,
        slice_direction=slice_direction,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return sigloss_execute(params, runner)

__all__ = [
    "Sigloss",
    "SiglossOutputs",
    "SIGLOSS_METADATA",
    "sigloss_cargs",
    "sigloss_outputs",
    "sigloss_params",
    "sigloss_execute",
    "sigloss_validate",
    "sigloss",
]