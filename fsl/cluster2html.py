# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CLUSTER2HTML_METADATA = Metadata(
    id="cluster2html",
    name="cluster2html",
    package="fsl",
)

Cluster2html = typing.TypedDict(
    "Cluster2html",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/cluster2html"]],
        "featdir": str,
        "inroot": str,
        "std_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Cluster2htmlOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def cluster2html_params(
    featdir: str,
    inroot: str,
    std_flag: bool = False,
) -> Cluster2html:
    """
    Build parameters.

    Args:
        featdir: Directory containing the FEAT analysis results
        inroot: Root name for cluster files (should not contain the _std extension)
        std_flag: Indicate that the input files contain the _std extension

    Returns:
        Parameter dictionary.
    """
    params: Cluster2html = {
        "@type": "fsl/cluster2html",
        "featdir": featdir,
        "inroot": inroot,
        "std_flag": std_flag,
    }
    return params

def cluster2html_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Cluster2html."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("featdir", None) is None:
        raise StyxValidationError("`featdir` must not be None")
    if not isinstance(params["featdir"], str):
        raise StyxValidationError(f'`featdir` has the wrong type: Received `{type(params["featdir"])}` expected `str`')
    if params.get("inroot", None) is None:
        raise StyxValidationError("`inroot` must not be None")
    if not isinstance(params["inroot"], str):
        raise StyxValidationError(f'`inroot` has the wrong type: Received `{type(params["inroot"])}` expected `str`')
    if params.get("std_flag", None) is not None:
        if not isinstance(params["std_flag"], bool):
            raise StyxValidationError(f'`std_flag` has the wrong type: Received `{type(params["std_flag"])}` expected `bool`')

def cluster2html_cargs(params: Cluster2html, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cluster2html")
    cargs.append(params["featdir"])
    cargs.append(params["inroot"])
    if params.get("std_flag"):
        cargs.append("-std")
    return cargs

def cluster2html_outputs(params: Cluster2html, execution: Execution) -> Cluster2htmlOutputs:
    """Build the Cluster2htmlOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Cluster2htmlOutputs(
        root=root_v,
    )

def cluster2html_execute(params: Cluster2html, runner: Runner | None = None) -> Cluster2htmlOutputs:
    """
    cluster2html

    Generates an HTML report from cluster-based FEAT analysis

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cluster2html_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CLUSTER2HTML_METADATA)
    execution.params(params)
    args = cluster2html_cargs(params, execution)
    out = cluster2html_outputs(params, execution)
    execution.run(args)
    return out

def cluster2html(
    featdir: str,
    inroot: str,
    std_flag: bool = False,
    runner: Runner | None = None,
) -> Cluster2htmlOutputs:
    """
    cluster2html

    Generates an HTML report from cluster-based FEAT analysis

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        featdir: Directory containing the FEAT analysis results
        inroot: Root name for cluster files (should not contain the _std extension)
        std_flag: Indicate that the input files contain the _std extension
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cluster2html_params(
        featdir=featdir,
        inroot=inroot,
        std_flag=std_flag,
    )
    return cluster2html_execute(params, runner)

__all__ = [
    "Cluster2html",
    "Cluster2htmlOutputs",
    "CLUSTER2HTML_METADATA",
    "cluster2html_cargs",
    "cluster2html_outputs",
    "cluster2html_params",
    "cluster2html_execute",
    "cluster2html_validate",
    "cluster2html",
]