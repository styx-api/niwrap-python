# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DTIGEN_METADATA = Metadata(
    id="dtigen",
    name="dtigen",
    package="fsl",
)

Dtigen = typing.TypedDict(
    "Dtigen",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/dtigen"]],
        "tensor": InputPathType,
        "s0": InputPathType,
        "output_data": str,
        "bvecs": InputPathType,
        "bvals": InputPathType,
        "brainmask": InputPathType,
        "kurtosis": typing.NotRequired[InputPathType],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class DtigenOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_diffusion_data: OutputPathType
    """Output diffusion data"""
    output_kurtosis_map: OutputPathType | None
    """Mean kurtosis map"""

def dtigen_params(
    tensor: InputPathType,
    s0: InputPathType,
    output_data: str,
    bvecs: InputPathType,
    bvals: InputPathType,
    brainmask: InputPathType,
    kurtosis: InputPathType | None = None,
    help: bool = False,
) -> Dtigen:
    """
    Build parameters.

    Args:
        tensor: Input tensor file
        s0: Input S0 file
        output_data: Output data file
        bvecs: bvecs ASCII text file
        bvals: bvals ASCII text file
        brainmask: Brain mask file
        kurtosis: Mean kurtosis map
        help: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Dtigen = {
        "@type": "fsl/dtigen",
        "tensor": tensor,
        "s0": s0,
        "output_data": output_data,
        "bvecs": bvecs,
        "bvals": bvals,
        "brainmask": brainmask,
        "help": help,
    }
    if kurtosis is not None:
        params["kurtosis"] = kurtosis
    return params

def dtigen_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dtigen."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("tensor", None) is None:
        raise StyxValidationError("`tensor` must not be None")
    if not isinstance(params["tensor"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tensor` has the wrong type: Received `{type(params["tensor"])}` expected `InputPathType`')
    if params.get("s0", None) is None:
        raise StyxValidationError("`s0` must not be None")
    if not isinstance(params["s0"], (pathlib.Path, str)):
        raise StyxValidationError(f'`s0` has the wrong type: Received `{type(params["s0"])}` expected `InputPathType`')
    if params.get("output_data", None) is None:
        raise StyxValidationError("`output_data` must not be None")
    if not isinstance(params["output_data"], str):
        raise StyxValidationError(f'`output_data` has the wrong type: Received `{type(params["output_data"])}` expected `str`')
    if params.get("bvecs", None) is None:
        raise StyxValidationError("`bvecs` must not be None")
    if not isinstance(params["bvecs"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["bvecs"])}` expected `InputPathType`')
    if params.get("bvals", None) is None:
        raise StyxValidationError("`bvals` must not be None")
    if not isinstance(params["bvals"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["bvals"])}` expected `InputPathType`')
    if params.get("brainmask", None) is None:
        raise StyxValidationError("`brainmask` must not be None")
    if not isinstance(params["brainmask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brainmask` has the wrong type: Received `{type(params["brainmask"])}` expected `InputPathType`')
    if params.get("kurtosis", None) is not None:
        if not isinstance(params["kurtosis"], (pathlib.Path, str)):
            raise StyxValidationError(f'`kurtosis` has the wrong type: Received `{type(params["kurtosis"])}` expected `InputPathType`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def dtigen_cargs(params: Dtigen, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dtigen")
    cargs.append("-t")
    cargs.append(execution.input_file(params["tensor"]))
    cargs.append("--s0")
    cargs.append(execution.input_file(params["s0"]))
    cargs.append("-o")
    cargs.append(params["output_data"])
    cargs.append("-r")
    cargs.append(execution.input_file(params["bvecs"]))
    cargs.append("-b")
    cargs.append(execution.input_file(params["bvals"]))
    cargs.append("-m")
    cargs.append(execution.input_file(params["brainmask"]))
    v_0 = params.get("kurtosis")
    if v_0 is not None:
        cargs.append("--kurt")
        cargs.append(execution.input_file(v_0))
    if params.get("help"):
        cargs.append("-h")
    return cargs

def dtigen_outputs(params: Dtigen, execution: Execution) -> DtigenOutputs:
    """Build the DtigenOutputs object for this tool."""
    output_kurtosis_map_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_diffusion_data_v: OutputPathType = execution.output_file(f'{params["output_data"]}.nii.gz')
    __v0 = params.get("kurtosis")
    if __v0 is not None:
        output_kurtosis_map_v = execution.output_file((__v0 if __v0 is not None else ""))
    return DtigenOutputs(
        root=root_v,
        output_diffusion_data=output_diffusion_data_v,
        output_kurtosis_map=output_kurtosis_map_v,
    )

def dtigen_execute(params: Dtigen, runner: Runner | None = None) -> DtigenOutputs:
    """
    dtigen

    Generate diffusion data using tensor model

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dtigen_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DTIGEN_METADATA)
    execution.params(params)
    args = dtigen_cargs(params, execution)
    out = dtigen_outputs(params, execution)
    execution.run(args)
    return out

def dtigen(
    tensor: InputPathType,
    s0: InputPathType,
    output_data: str,
    bvecs: InputPathType,
    bvals: InputPathType,
    brainmask: InputPathType,
    kurtosis: InputPathType | None = None,
    help: bool = False,
    runner: Runner | None = None,
) -> DtigenOutputs:
    """
    dtigen

    Generate diffusion data using tensor model

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        tensor: Input tensor file
        s0: Input S0 file
        output_data: Output data file
        bvecs: bvecs ASCII text file
        bvals: bvals ASCII text file
        brainmask: Brain mask file
        kurtosis: Mean kurtosis map
        help: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dtigen_params(
        tensor=tensor,
        s0=s0,
        output_data=output_data,
        bvecs=bvecs,
        bvals=bvals,
        brainmask=brainmask,
        kurtosis=kurtosis,
        help=help,
    )
    return dtigen_execute(params, runner)

__all__ = [
    "Dtigen",
    "DtigenOutputs",
    "DTIGEN_METADATA",
    "dtigen_cargs",
    "dtigen_outputs",
    "dtigen_params",
    "dtigen_execute",
    "dtigen_validate",
    "dtigen",
]