# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCALC_METADATA = Metadata(
    id="tcalc",
    name="tcalc",
    package="fsl",
)

Tcalc = typing.TypedDict(
    "Tcalc",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tcalc"]],
        "input_image": InputPathType,
        "output_image": str,
        "echo_time": typing.NotRequired[float],
        "repetition_time": typing.NotRequired[float],
        "mrpar_file": typing.NotRequired[InputPathType],
        "num_voxel_x": typing.NotRequired[float],
        "num_voxel_y": typing.NotRequired[float],
        "num_voxel_z": typing.NotRequired[float],
        "voxel_size_x": typing.NotRequired[float],
        "voxel_size_y": typing.NotRequired[float],
        "voxel_size_z": typing.NotRequired[float],
        "start_position": typing.NotRequired[float],
        "noise_sigma": typing.NotRequired[float],
        "save_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TcalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType
    """The output image generated after resampling"""
    original_output_image_file: OutputPathType
    """The original non-resampled output image, if save flag is used"""

def tcalc_params(
    input_image: InputPathType,
    output_image: str,
    echo_time: float | None = None,
    repetition_time: float | None = None,
    mrpar_file: InputPathType | None = None,
    num_voxel_x: float | None = None,
    num_voxel_y: float | None = None,
    num_voxel_z: float | None = None,
    voxel_size_x: float | None = None,
    voxel_size_y: float | None = None,
    voxel_size_z: float | None = None,
    start_position: float | None = None,
    noise_sigma: float | None = None,
    save_flag: bool = False,
    verbose_flag: bool = False,
) -> Tcalc:
    """
    Build parameters.

    Args:
        input_image: Input image (4D phantom for theoretical calculations)
        output_image: Output image
        echo_time: Echo Time (TE) in seconds [e.g., T1-weighted images for 3T\
            TE=0.01 s]
        repetition_time: Repetition Time (TR) in seconds [e.g., T1-weighted images\
            for 3T TR=0.7 s]
        mrpar_file: MRpar File
        num_voxel_x: Number of Voxels along X (default: phantom)
        num_voxel_y: Number of Voxels along Y (default: phantom)
        num_voxel_z: Number of Voxels along Z (default: phantom)
        voxel_size_x: Size of voxels along X (default: phantom)
        voxel_size_y: Size of voxels along Y (default: phantom)
        voxel_size_z: Size of voxels along Z i.e., number of slices (default:\
            phantom)
        start_position: Starting position of the volume in mm (default = 0mm)
        noise_sigma: Add noise with given sigma (default: 0 i.e., no noise)
        save_flag: Save original non-resample output image
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Tcalc = {
        "@type": "fsl/tcalc",
        "input_image": input_image,
        "output_image": output_image,
        "save_flag": save_flag,
        "verbose_flag": verbose_flag,
    }
    if echo_time is not None:
        params["echo_time"] = echo_time
    if repetition_time is not None:
        params["repetition_time"] = repetition_time
    if mrpar_file is not None:
        params["mrpar_file"] = mrpar_file
    if num_voxel_x is not None:
        params["num_voxel_x"] = num_voxel_x
    if num_voxel_y is not None:
        params["num_voxel_y"] = num_voxel_y
    if num_voxel_z is not None:
        params["num_voxel_z"] = num_voxel_z
    if voxel_size_x is not None:
        params["voxel_size_x"] = voxel_size_x
    if voxel_size_y is not None:
        params["voxel_size_y"] = voxel_size_y
    if voxel_size_z is not None:
        params["voxel_size_z"] = voxel_size_z
    if start_position is not None:
        params["start_position"] = start_position
    if noise_sigma is not None:
        params["noise_sigma"] = noise_sigma
    return params

def tcalc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tcalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("echo_time", None) is not None:
        if not isinstance(params["echo_time"], (float, int)):
            raise StyxValidationError(f'`echo_time` has the wrong type: Received `{type(params["echo_time"])}` expected `float`')
    if params.get("repetition_time", None) is not None:
        if not isinstance(params["repetition_time"], (float, int)):
            raise StyxValidationError(f'`repetition_time` has the wrong type: Received `{type(params["repetition_time"])}` expected `float`')
    if params.get("mrpar_file", None) is not None:
        if not isinstance(params["mrpar_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mrpar_file` has the wrong type: Received `{type(params["mrpar_file"])}` expected `InputPathType`')
    if params.get("num_voxel_x", None) is not None:
        if not isinstance(params["num_voxel_x"], (float, int)):
            raise StyxValidationError(f'`num_voxel_x` has the wrong type: Received `{type(params["num_voxel_x"])}` expected `float`')
    if params.get("num_voxel_y", None) is not None:
        if not isinstance(params["num_voxel_y"], (float, int)):
            raise StyxValidationError(f'`num_voxel_y` has the wrong type: Received `{type(params["num_voxel_y"])}` expected `float`')
    if params.get("num_voxel_z", None) is not None:
        if not isinstance(params["num_voxel_z"], (float, int)):
            raise StyxValidationError(f'`num_voxel_z` has the wrong type: Received `{type(params["num_voxel_z"])}` expected `float`')
    if params.get("voxel_size_x", None) is not None:
        if not isinstance(params["voxel_size_x"], (float, int)):
            raise StyxValidationError(f'`voxel_size_x` has the wrong type: Received `{type(params["voxel_size_x"])}` expected `float`')
    if params.get("voxel_size_y", None) is not None:
        if not isinstance(params["voxel_size_y"], (float, int)):
            raise StyxValidationError(f'`voxel_size_y` has the wrong type: Received `{type(params["voxel_size_y"])}` expected `float`')
    if params.get("voxel_size_z", None) is not None:
        if not isinstance(params["voxel_size_z"], (float, int)):
            raise StyxValidationError(f'`voxel_size_z` has the wrong type: Received `{type(params["voxel_size_z"])}` expected `float`')
    if params.get("start_position", None) is not None:
        if not isinstance(params["start_position"], (float, int)):
            raise StyxValidationError(f'`start_position` has the wrong type: Received `{type(params["start_position"])}` expected `float`')
    if params.get("noise_sigma", None) is not None:
        if not isinstance(params["noise_sigma"], (float, int)):
            raise StyxValidationError(f'`noise_sigma` has the wrong type: Received `{type(params["noise_sigma"])}` expected `float`')
    if params.get("save_flag", None) is not None:
        if not isinstance(params["save_flag"], bool):
            raise StyxValidationError(f'`save_flag` has the wrong type: Received `{type(params["save_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def tcalc_cargs(params: Tcalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tcalc")
    cargs.append("--input=")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("--output=")
    cargs.append(params["output_image"])
    v_0 = params.get("echo_time")
    if v_0 is not None:
        cargs.append("--te")
        cargs.append(str(v_0))
    v_1 = params.get("repetition_time")
    if v_1 is not None:
        cargs.append("--tr")
        cargs.append(str(v_1))
    v_2 = params.get("mrpar_file")
    if v_2 is not None:
        cargs.append("--mrpar")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("num_voxel_x")
    if v_3 is not None:
        cargs.append("--nx")
        cargs.append(str(v_3))
    v_4 = params.get("num_voxel_y")
    if v_4 is not None:
        cargs.append("--ny")
        cargs.append(str(v_4))
    v_5 = params.get("num_voxel_z")
    if v_5 is not None:
        cargs.append("--nz")
        cargs.append(str(v_5))
    v_6 = params.get("voxel_size_x")
    if v_6 is not None:
        cargs.append("--dx")
        cargs.append(str(v_6))
    v_7 = params.get("voxel_size_y")
    if v_7 is not None:
        cargs.append("--dy")
        cargs.append(str(v_7))
    v_8 = params.get("voxel_size_z")
    if v_8 is not None:
        cargs.append("--dz")
        cargs.append(str(v_8))
    v_9 = params.get("start_position")
    if v_9 is not None:
        cargs.append("--zstart")
        cargs.append(str(v_9))
    v_10 = params.get("noise_sigma")
    if v_10 is not None:
        cargs.append("--sigma")
        cargs.append(str(v_10))
    if params.get("save_flag"):
        cargs.append("--save")
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    return cargs

def tcalc_outputs(params: Tcalc, execution: Execution) -> TcalcOutputs:
    """Build the TcalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_file_v: OutputPathType = execution.output_file(params["output_image"])
    original_output_image_file_v: OutputPathType = execution.output_file(f'{params["output_image"]}_original')
    return TcalcOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
        original_output_image_file=original_output_image_file_v,
    )

def tcalc_execute(params: Tcalc, runner: Runner | None = None) -> TcalcOutputs:
    """
    tcalc

    Resample a 4D phantom for theoretical calculations

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tcalc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCALC_METADATA)
    execution.params(params)
    args = tcalc_cargs(params, execution)
    out = tcalc_outputs(params, execution)
    execution.run(args)
    return out

def tcalc(
    input_image: InputPathType,
    output_image: str,
    echo_time: float | None = None,
    repetition_time: float | None = None,
    mrpar_file: InputPathType | None = None,
    num_voxel_x: float | None = None,
    num_voxel_y: float | None = None,
    num_voxel_z: float | None = None,
    voxel_size_x: float | None = None,
    voxel_size_y: float | None = None,
    voxel_size_z: float | None = None,
    start_position: float | None = None,
    noise_sigma: float | None = None,
    save_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> TcalcOutputs:
    """
    tcalc

    Resample a 4D phantom for theoretical calculations

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Input image (4D phantom for theoretical calculations)
        output_image: Output image
        echo_time: Echo Time (TE) in seconds [e.g., T1-weighted images for 3T\
            TE=0.01 s]
        repetition_time: Repetition Time (TR) in seconds [e.g., T1-weighted images\
            for 3T TR=0.7 s]
        mrpar_file: MRpar File
        num_voxel_x: Number of Voxels along X (default: phantom)
        num_voxel_y: Number of Voxels along Y (default: phantom)
        num_voxel_z: Number of Voxels along Z (default: phantom)
        voxel_size_x: Size of voxels along X (default: phantom)
        voxel_size_y: Size of voxels along Y (default: phantom)
        voxel_size_z: Size of voxels along Z i.e., number of slices (default:\
            phantom)
        start_position: Starting position of the volume in mm (default = 0mm)
        noise_sigma: Add noise with given sigma (default: 0 i.e., no noise)
        save_flag: Save original non-resample output image
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tcalc_params(
        input_image=input_image,
        output_image=output_image,
        echo_time=echo_time,
        repetition_time=repetition_time,
        mrpar_file=mrpar_file,
        num_voxel_x=num_voxel_x,
        num_voxel_y=num_voxel_y,
        num_voxel_z=num_voxel_z,
        voxel_size_x=voxel_size_x,
        voxel_size_y=voxel_size_y,
        voxel_size_z=voxel_size_z,
        start_position=start_position,
        noise_sigma=noise_sigma,
        save_flag=save_flag,
        verbose_flag=verbose_flag,
    )
    return tcalc_execute(params, runner)

__all__ = [
    "Tcalc",
    "TcalcOutputs",
    "TCALC_METADATA",
    "tcalc_cargs",
    "tcalc_outputs",
    "tcalc_params",
    "tcalc_execute",
    "tcalc_validate",
    "tcalc",
]