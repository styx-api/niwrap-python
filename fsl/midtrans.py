# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MIDTRANS_METADATA = Metadata(
    id="midtrans",
    name="midtrans",
    package="fsl",
)

Midtrans = typing.TypedDict(
    "Midtrans",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/midtrans"]],
        "transforms": list[InputPathType],
        "output_matrix": typing.NotRequired[str],
        "template_image": typing.NotRequired[InputPathType],
        "separate_basename": typing.NotRequired[str],
        "debug_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class MidtransOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def midtrans_params(
    transforms: list[InputPathType],
    output_matrix: str | None = None,
    template_image: InputPathType | None = None,
    separate_basename: str | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
) -> Midtrans:
    """
    Build parameters.

    Args:
        transforms: List of input transform files (e.g. transform1.mat\
            transform2.mat ... transformN.mat)
        output_matrix: Output filename for the resulting matrix
        template_image: Input filename for template image (needed for fix origin)
        separate_basename: Basename for the output of separate matrices (final name\
            includes a number; e.g. img2mid0001.mat)
        debug_flag: Switch on debugging output
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Midtrans = {
        "@type": "fsl/midtrans",
        "transforms": transforms,
        "debug_flag": debug_flag,
        "verbose_flag": verbose_flag,
    }
    if output_matrix is not None:
        params["output_matrix"] = output_matrix
    if template_image is not None:
        params["template_image"] = template_image
    if separate_basename is not None:
        params["separate_basename"] = separate_basename
    return params

def midtrans_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Midtrans."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("transforms", None) is None:
        raise StyxValidationError("`transforms` must not be None")
    if not isinstance(params["transforms"], list):
        raise StyxValidationError(f'`transforms` has the wrong type: Received `{type(params["transforms"])}` expected `list[InputPathType]`')
    for e in params["transforms"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`transforms` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_matrix", None) is not None:
        if not isinstance(params["output_matrix"], str):
            raise StyxValidationError(f'`output_matrix` has the wrong type: Received `{type(params["output_matrix"])}` expected `str`')
    if params.get("template_image", None) is not None:
        if not isinstance(params["template_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template_image` has the wrong type: Received `{type(params["template_image"])}` expected `InputPathType`')
    if params.get("separate_basename", None) is not None:
        if not isinstance(params["separate_basename"], str):
            raise StyxValidationError(f'`separate_basename` has the wrong type: Received `{type(params["separate_basename"])}` expected `str`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def midtrans_cargs(params: Midtrans, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("midtrans")
    for item0 in params["transforms"]:
        cargs.append(execution.input_file(item0))
    v_0 = params.get("output_matrix")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("template_image")
    if v_1 is not None:
        cargs.append("--template")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("separate_basename")
    if v_2 is not None:
        cargs.append("--separate")
        cargs.append(v_2)
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("verbose_flag"):
        cargs.append("-v, --verbose")
    return cargs

def midtrans_outputs(params: Midtrans, execution: Execution) -> MidtransOutputs:
    """Build the MidtransOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MidtransOutputs(
        root=root_v,
    )

def midtrans_execute(params: Midtrans, runner: Runner | None = None) -> MidtransOutputs:
    """
    midtrans

    Calculate the midpoint transform from a series of input transforms.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    midtrans_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MIDTRANS_METADATA)
    execution.params(params)
    args = midtrans_cargs(params, execution)
    out = midtrans_outputs(params, execution)
    execution.run(args)
    return out

def midtrans(
    transforms: list[InputPathType],
    output_matrix: str | None = None,
    template_image: InputPathType | None = None,
    separate_basename: str | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> MidtransOutputs:
    """
    midtrans

    Calculate the midpoint transform from a series of input transforms.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        transforms: List of input transform files (e.g. transform1.mat\
            transform2.mat ... transformN.mat)
        output_matrix: Output filename for the resulting matrix
        template_image: Input filename for template image (needed for fix origin)
        separate_basename: Basename for the output of separate matrices (final name\
            includes a number; e.g. img2mid0001.mat)
        debug_flag: Switch on debugging output
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = midtrans_params(
        transforms=transforms,
        output_matrix=output_matrix,
        template_image=template_image,
        separate_basename=separate_basename,
        debug_flag=debug_flag,
        verbose_flag=verbose_flag,
    )
    return midtrans_execute(params, runner)

__all__ = [
    "Midtrans",
    "MidtransOutputs",
    "MIDTRANS_METADATA",
    "midtrans_cargs",
    "midtrans_outputs",
    "midtrans_params",
    "midtrans_execute",
    "midtrans_validate",
    "midtrans",
]