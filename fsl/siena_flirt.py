# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIENA_FLIRT_METADATA = Metadata(
    id="siena_flirt",
    name="siena_flirt",
    package="fsl",
)

SienaFlirt = typing.TypedDict(
    "SienaFlirt",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/siena_flirt"]],
        "input1_fileroot": str,
        "input2_fileroot": str,
    },
)

@dataclasses.dataclass
class SienaFlirtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_transform_matrix: OutputPathType
    """Output transformation matrix file"""
    output_registered_image: OutputPathType
    """Output registered image"""

def siena_flirt_params(
    input1_fileroot: str,
    input2_fileroot: str,
) -> SienaFlirt:
    """
    Build parameters.

    Args:
        input1_fileroot: First input file root (e.g. first time-point image root,\
            without file extension)
        input2_fileroot: Second input file root (e.g. second time-point image root,\
            without file extension)

    Returns:
        Parameter dictionary.
    """
    params: SienaFlirt = {
        "@type": "fsl/siena_flirt",
        "input1_fileroot": input1_fileroot,
        "input2_fileroot": input2_fileroot,
    }
    return params

def siena_flirt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SienaFlirt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1_fileroot", None) is None:
        raise StyxValidationError("`input1_fileroot` must not be None")
    if not isinstance(params["input1_fileroot"], str):
        raise StyxValidationError(f'`input1_fileroot` has the wrong type: Received `{type(params["input1_fileroot"])}` expected `str`')
    if params.get("input2_fileroot", None) is None:
        raise StyxValidationError("`input2_fileroot` must not be None")
    if not isinstance(params["input2_fileroot"], str):
        raise StyxValidationError(f'`input2_fileroot` has the wrong type: Received `{type(params["input2_fileroot"])}` expected `str`')

def siena_flirt_cargs(params: SienaFlirt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("siena_flirt")
    cargs.append(params["input1_fileroot"])
    cargs.append(params["input2_fileroot"])
    return cargs

def siena_flirt_outputs(params: SienaFlirt, execution: Execution) -> SienaFlirtOutputs:
    """Build the SienaFlirtOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_transform_matrix_v: OutputPathType = execution.output_file(f'{params["input1_fileroot"]}_to_{params["input2_fileroot"]}_flirt.mat')
    output_registered_image_v: OutputPathType = execution.output_file(f'{params["input1_fileroot"]}_to_{params["input2_fileroot"]}_flirt.nii.gz')
    return SienaFlirtOutputs(
        root=root_v,
        output_transform_matrix=output_transform_matrix_v,
        output_registered_image=output_registered_image_v,
    )

def siena_flirt_execute(params: SienaFlirt, runner: Runner | None = None) -> SienaFlirtOutputs:
    """
    siena_flirt

    Wrapper for FLIRT image registration within the SIENA framework.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    siena_flirt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIENA_FLIRT_METADATA)
    execution.params(params)
    args = siena_flirt_cargs(params, execution)
    out = siena_flirt_outputs(params, execution)
    execution.run(args)
    return out

def siena_flirt(
    input1_fileroot: str,
    input2_fileroot: str,
    runner: Runner | None = None,
) -> SienaFlirtOutputs:
    """
    siena_flirt

    Wrapper for FLIRT image registration within the SIENA framework.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input1_fileroot: First input file root (e.g. first time-point image root,\
            without file extension)
        input2_fileroot: Second input file root (e.g. second time-point image root,\
            without file extension)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = siena_flirt_params(
        input1_fileroot=input1_fileroot,
        input2_fileroot=input2_fileroot,
    )
    return siena_flirt_execute(params, runner)

__all__ = [
    "SienaFlirt",
    "SienaFlirtOutputs",
    "SIENA_FLIRT_METADATA",
    "siena_flirt_cargs",
    "siena_flirt_outputs",
    "siena_flirt_params",
    "siena_flirt_execute",
    "siena_flirt_validate",
    "siena_flirt",
]