# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIENA_METADATA = Metadata(
    id="siena",
    name="siena",
    package="fsl",
)

Siena = typing.TypedDict(
    "Siena",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/siena"]],
        "input1": InputPathType,
        "input2": InputPathType,
        "output_dir": typing.NotRequired[str],
        "debug_flag": typing.NotRequired[bool],
        "bet_options": typing.NotRequired[str],
        "two_class_seg_flag": typing.NotRequired[bool],
        "t2_weighted_flag": typing.NotRequired[bool],
        "standard_space_mask_flag": typing.NotRequired[bool],
        "upper_ignore": typing.NotRequired[float],
        "lower_ignore": typing.NotRequired[float],
        "sienadiff_options": typing.NotRequired[str],
        "ventricle_analysis_flag": typing.NotRequired[bool],
        "ventricle_mask": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class SienaOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_report: OutputPathType | None
    """Siena analysis report"""
    ventricle_analysis_report: OutputPathType | None
    """Siena ventricle analysis report"""

def siena_params(
    input1: InputPathType,
    input2: InputPathType,
    output_dir: str | None = None,
    debug_flag: bool = False,
    bet_options: str | None = None,
    two_class_seg_flag: bool = False,
    t2_weighted_flag: bool = False,
    standard_space_mask_flag: bool = False,
    upper_ignore: float | None = None,
    lower_ignore: float | None = None,
    sienadiff_options: str | None = None,
    ventricle_analysis_flag: bool = False,
    ventricle_mask: InputPathType | None = None,
) -> Siena:
    """
    Build parameters.

    Args:
        input1: First input image (e.g. timepoint 1 image, img1.nii.gz)
        input2: Second input image (e.g. timepoint 2 image, img2.nii.gz)
        output_dir: Set output directory (default output is\
            <input1>_to_<input2>_siena)
        debug_flag: Debug (don't delete intermediate files)
        bet_options: Options to pass to BET brain extraction (inside\
            double-quotes), e.g. -B "-f 0.3"
        two_class_seg_flag: Two-class segmentation (don't segment grey and white\
            matter separately)
        t2_weighted_flag: T2-weighted input image (default T1-weighted)
        standard_space_mask_flag: Use standard-space masking as well as BET
        upper_ignore: Ignore from t (mm) upwards in MNI152/Talairach space
        lower_ignore: Ignore from b (mm) downwards in MNI152/Talairach space (b\
            should probably be negative)
        sienadiff_options: Options to pass to siena_diff timepoint differencing\
            (inside double-quotes), e.g. -S "-s -i 20"
        ventricle_analysis_flag: Run ventricle analysis (VIENA)
        ventricle_mask: Optional user-supplied ventricle mask (default is\
            /usr/local/fsl/data/standard/MNI152_T1_2mm_VentricleMask)

    Returns:
        Parameter dictionary.
    """
    params: Siena = {
        "@type": "fsl/siena",
        "input1": input1,
        "input2": input2,
        "debug_flag": debug_flag,
        "two_class_seg_flag": two_class_seg_flag,
        "t2_weighted_flag": t2_weighted_flag,
        "standard_space_mask_flag": standard_space_mask_flag,
        "ventricle_analysis_flag": ventricle_analysis_flag,
    }
    if output_dir is not None:
        params["output_dir"] = output_dir
    if bet_options is not None:
        params["bet_options"] = bet_options
    if upper_ignore is not None:
        params["upper_ignore"] = upper_ignore
    if lower_ignore is not None:
        params["lower_ignore"] = lower_ignore
    if sienadiff_options is not None:
        params["sienadiff_options"] = sienadiff_options
    if ventricle_mask is not None:
        params["ventricle_mask"] = ventricle_mask
    return params

def siena_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Siena."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1", None) is None:
        raise StyxValidationError("`input1` must not be None")
    if not isinstance(params["input1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input1` has the wrong type: Received `{type(params["input1"])}` expected `InputPathType`')
    if params.get("input2", None) is None:
        raise StyxValidationError("`input2` must not be None")
    if not isinstance(params["input2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input2` has the wrong type: Received `{type(params["input2"])}` expected `InputPathType`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("bet_options", None) is not None:
        if not isinstance(params["bet_options"], str):
            raise StyxValidationError(f'`bet_options` has the wrong type: Received `{type(params["bet_options"])}` expected `str`')
    if params.get("two_class_seg_flag", None) is not None:
        if not isinstance(params["two_class_seg_flag"], bool):
            raise StyxValidationError(f'`two_class_seg_flag` has the wrong type: Received `{type(params["two_class_seg_flag"])}` expected `bool`')
    if params.get("t2_weighted_flag", None) is not None:
        if not isinstance(params["t2_weighted_flag"], bool):
            raise StyxValidationError(f'`t2_weighted_flag` has the wrong type: Received `{type(params["t2_weighted_flag"])}` expected `bool`')
    if params.get("standard_space_mask_flag", None) is not None:
        if not isinstance(params["standard_space_mask_flag"], bool):
            raise StyxValidationError(f'`standard_space_mask_flag` has the wrong type: Received `{type(params["standard_space_mask_flag"])}` expected `bool`')
    if params.get("upper_ignore", None) is not None:
        if not isinstance(params["upper_ignore"], (float, int)):
            raise StyxValidationError(f'`upper_ignore` has the wrong type: Received `{type(params["upper_ignore"])}` expected `float`')
    if params.get("lower_ignore", None) is not None:
        if not isinstance(params["lower_ignore"], (float, int)):
            raise StyxValidationError(f'`lower_ignore` has the wrong type: Received `{type(params["lower_ignore"])}` expected `float`')
    if params.get("sienadiff_options", None) is not None:
        if not isinstance(params["sienadiff_options"], str):
            raise StyxValidationError(f'`sienadiff_options` has the wrong type: Received `{type(params["sienadiff_options"])}` expected `str`')
    if params.get("ventricle_analysis_flag", None) is not None:
        if not isinstance(params["ventricle_analysis_flag"], bool):
            raise StyxValidationError(f'`ventricle_analysis_flag` has the wrong type: Received `{type(params["ventricle_analysis_flag"])}` expected `bool`')
    if params.get("ventricle_mask", None) is not None:
        if not isinstance(params["ventricle_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ventricle_mask` has the wrong type: Received `{type(params["ventricle_mask"])}` expected `InputPathType`')

def siena_cargs(params: Siena, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("siena")
    cargs.append(execution.input_file(params["input1"]))
    cargs.append(execution.input_file(params["input2"]))
    v_0 = params.get("output_dir")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    if params.get("debug_flag"):
        cargs.append("-d")
    v_1 = params.get("bet_options")
    if v_1 is not None:
        cargs.append("-B")
        cargs.append(v_1)
    if params.get("two_class_seg_flag"):
        cargs.append("-2")
    if params.get("t2_weighted_flag"):
        cargs.append("-t2")
    if params.get("standard_space_mask_flag"):
        cargs.append("-m")
    v_2 = params.get("upper_ignore")
    if v_2 is not None:
        cargs.append("-t")
        cargs.append(str(v_2))
    v_3 = params.get("lower_ignore")
    if v_3 is not None:
        cargs.append("-b")
        cargs.append(str(v_3))
    v_4 = params.get("sienadiff_options")
    if v_4 is not None:
        cargs.append("-S")
        cargs.append(v_4)
    if params.get("ventricle_analysis_flag"):
        cargs.append("-V")
    v_5 = params.get("ventricle_mask")
    if v_5 is not None:
        cargs.append("-v")
        cargs.append(execution.input_file(v_5))
    return cargs

def siena_outputs(params: Siena, execution: Execution) -> SienaOutputs:
    """Build the SienaOutputs object for this tool."""
    output_report_v: OutputPathType | None = None
    ventricle_analysis_report_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        output_report_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/report.html')
    __v1 = params.get("output_dir")
    if __v1 is not None:
        ventricle_analysis_report_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/report_vent.html')
    return SienaOutputs(
        root=root_v,
        output_report=output_report_v,
        ventricle_analysis_report=ventricle_analysis_report_v,
    )

def siena_execute(params: Siena, runner: Runner | None = None) -> SienaOutputs:
    """
    siena

    Structural Image Evaluation, using Normalization, of Atrophy tool for evaluating brain atrophy between two timepoints

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    siena_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIENA_METADATA)
    execution.params(params)
    args = siena_cargs(params, execution)
    out = siena_outputs(params, execution)
    execution.run(args)
    return out

def siena(
    input1: InputPathType,
    input2: InputPathType,
    output_dir: str | None = None,
    debug_flag: bool = False,
    bet_options: str | None = None,
    two_class_seg_flag: bool = False,
    t2_weighted_flag: bool = False,
    standard_space_mask_flag: bool = False,
    upper_ignore: float | None = None,
    lower_ignore: float | None = None,
    sienadiff_options: str | None = None,
    ventricle_analysis_flag: bool = False,
    ventricle_mask: InputPathType | None = None,
    runner: Runner | None = None,
) -> SienaOutputs:
    """
    siena

    Structural Image Evaluation, using Normalization, of Atrophy tool for evaluating brain atrophy between two timepoints

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input1: First input image (e.g. timepoint 1 image, img1.nii.gz)
        input2: Second input image (e.g. timepoint 2 image, img2.nii.gz)
        output_dir: Set output directory (default output is\
            <input1>_to_<input2>_siena)
        debug_flag: Debug (don't delete intermediate files)
        bet_options: Options to pass to BET brain extraction (inside\
            double-quotes), e.g. -B "-f 0.3"
        two_class_seg_flag: Two-class segmentation (don't segment grey and white\
            matter separately)
        t2_weighted_flag: T2-weighted input image (default T1-weighted)
        standard_space_mask_flag: Use standard-space masking as well as BET
        upper_ignore: Ignore from t (mm) upwards in MNI152/Talairach space
        lower_ignore: Ignore from b (mm) downwards in MNI152/Talairach space (b\
            should probably be negative)
        sienadiff_options: Options to pass to siena_diff timepoint differencing\
            (inside double-quotes), e.g. -S "-s -i 20"
        ventricle_analysis_flag: Run ventricle analysis (VIENA)
        ventricle_mask: Optional user-supplied ventricle mask (default is\
            /usr/local/fsl/data/standard/MNI152_T1_2mm_VentricleMask)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = siena_params(
        input1=input1,
        input2=input2,
        output_dir=output_dir,
        debug_flag=debug_flag,
        bet_options=bet_options,
        two_class_seg_flag=two_class_seg_flag,
        t2_weighted_flag=t2_weighted_flag,
        standard_space_mask_flag=standard_space_mask_flag,
        upper_ignore=upper_ignore,
        lower_ignore=lower_ignore,
        sienadiff_options=sienadiff_options,
        ventricle_analysis_flag=ventricle_analysis_flag,
        ventricle_mask=ventricle_mask,
    )
    return siena_execute(params, runner)

__all__ = [
    "Siena",
    "SienaOutputs",
    "SIENA_METADATA",
    "siena_cargs",
    "siena_outputs",
    "siena_params",
    "siena_execute",
    "siena_validate",
    "siena",
]