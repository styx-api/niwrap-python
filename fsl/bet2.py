# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BET2_METADATA = Metadata(
    id="bet2",
    name="bet2",
    package="fsl",
)

Bet2 = typing.TypedDict(
    "Bet2",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/bet2"]],
        "input_fileroot": str,
        "output_fileroot": str,
        "fractional_intensity": typing.NotRequired[float],
        "vertical_gradient": typing.NotRequired[float],
        "center_of_gravity": typing.NotRequired[list[float]],
        "outline_flag": typing.NotRequired[bool],
        "mask_flag": typing.NotRequired[bool],
        "skull_flag": typing.NotRequired[bool],
        "no_output_flag": typing.NotRequired[bool],
        "mesh_flag": typing.NotRequired[bool],
        "head_radius": typing.NotRequired[float],
        "smooth_factor": typing.NotRequired[float],
        "threshold_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Bet2Outputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_mask: OutputPathType
    """Binary brain mask output (if mask flag is set)"""
    output_skull: OutputPathType
    """Approximate skull image output (if skull flag is set)"""
    output_mesh: OutputPathType
    """Brain surface mesh output in VTK format (if mesh flag is set)"""
    output_overlay: OutputPathType
    """Brain surface outline overlaid onto original image (if outline flag is set)"""

def bet2_params(
    input_fileroot: str,
    output_fileroot: str,
    fractional_intensity: float | None = None,
    vertical_gradient: float | None = None,
    center_of_gravity: list[float] | None = None,
    outline_flag: bool = False,
    mask_flag: bool = False,
    skull_flag: bool = False,
    no_output_flag: bool = False,
    mesh_flag: bool = False,
    head_radius: float | None = None,
    smooth_factor: float | None = None,
    threshold_flag: bool = False,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Bet2:
    """
    Build parameters.

    Args:
        input_fileroot: Input file root (e.g. img)
        output_fileroot: Output file root (e.g. img_bet)
        fractional_intensity: Fractional intensity threshold (0->1); default=0.5;\
            smaller values give larger brain outline estimates
        vertical_gradient: Vertical gradient in fractional intensity threshold\
            (-1->1); default=0; positive values give larger brain outline at\
            bottom, smaller at top
        center_of_gravity: The xyz coordinates of the center of gravity (voxels,\
            not mm) of initial mesh surface. Must have exactly three numerical\
            entries in the list (3-vector).
        outline_flag: Generate brain surface outline overlaid onto original image
        mask_flag: Generate binary brain mask
        skull_flag: Generate approximate skull image
        no_output_flag: Don't generate segmented brain image output
        mesh_flag: Generate brain surface as mesh in vtk format
        head_radius: Head radius (mm not voxels); initial surface sphere is set to\
            half of this
        smooth_factor: Smoothness factor; default=1; values smaller than 1 produce\
            more detailed brain surface, values larger than one produce smoother,\
            less detailed surface
        threshold_flag: Apply thresholding to segmented brain image and mask
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message and exit

    Returns:
        Parameter dictionary.
    """
    params: Bet2 = {
        "@type": "fsl/bet2",
        "input_fileroot": input_fileroot,
        "output_fileroot": output_fileroot,
        "outline_flag": outline_flag,
        "mask_flag": mask_flag,
        "skull_flag": skull_flag,
        "no_output_flag": no_output_flag,
        "mesh_flag": mesh_flag,
        "threshold_flag": threshold_flag,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if fractional_intensity is not None:
        params["fractional_intensity"] = fractional_intensity
    if vertical_gradient is not None:
        params["vertical_gradient"] = vertical_gradient
    if center_of_gravity is not None:
        params["center_of_gravity"] = center_of_gravity
    if head_radius is not None:
        params["head_radius"] = head_radius
    if smooth_factor is not None:
        params["smooth_factor"] = smooth_factor
    return params

def bet2_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bet2."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_fileroot", None) is None:
        raise StyxValidationError("`input_fileroot` must not be None")
    if not isinstance(params["input_fileroot"], str):
        raise StyxValidationError(f'`input_fileroot` has the wrong type: Received `{type(params["input_fileroot"])}` expected `str`')
    if params.get("output_fileroot", None) is None:
        raise StyxValidationError("`output_fileroot` must not be None")
    if not isinstance(params["output_fileroot"], str):
        raise StyxValidationError(f'`output_fileroot` has the wrong type: Received `{type(params["output_fileroot"])}` expected `str`')
    if params.get("fractional_intensity", None) is not None:
        if not isinstance(params["fractional_intensity"], (float, int)):
            raise StyxValidationError(f'`fractional_intensity` has the wrong type: Received `{type(params["fractional_intensity"])}` expected `float`')
        if not (0 <= params["fractional_intensity"] <= 1):
            raise StyxValidationError("Parameter `fractional_intensity` must be between 0 and 1 (inclusive)")
    if params.get("vertical_gradient", None) is not None:
        if not isinstance(params["vertical_gradient"], (float, int)):
            raise StyxValidationError(f'`vertical_gradient` has the wrong type: Received `{type(params["vertical_gradient"])}` expected `float`')
        if not (-1 <= params["vertical_gradient"] <= 1):
            raise StyxValidationError("Parameter `vertical_gradient` must be between -1 and 1 (inclusive)")
    if params.get("center_of_gravity", None) is not None:
        if not isinstance(params["center_of_gravity"], list):
            raise StyxValidationError(f'`center_of_gravity` has the wrong type: Received `{type(params["center_of_gravity"])}` expected `list[float]`')
        if not (3 <= len(params["center_of_gravity"]) <= 3):
            raise StyxValidationError("Parameter `center_of_gravity` must contain between 3 and 3 elements (inclusive)")
        for e in params["center_of_gravity"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`center_of_gravity` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("outline_flag", None) is not None:
        if not isinstance(params["outline_flag"], bool):
            raise StyxValidationError(f'`outline_flag` has the wrong type: Received `{type(params["outline_flag"])}` expected `bool`')
    if params.get("mask_flag", None) is not None:
        if not isinstance(params["mask_flag"], bool):
            raise StyxValidationError(f'`mask_flag` has the wrong type: Received `{type(params["mask_flag"])}` expected `bool`')
    if params.get("skull_flag", None) is not None:
        if not isinstance(params["skull_flag"], bool):
            raise StyxValidationError(f'`skull_flag` has the wrong type: Received `{type(params["skull_flag"])}` expected `bool`')
    if params.get("no_output_flag", None) is not None:
        if not isinstance(params["no_output_flag"], bool):
            raise StyxValidationError(f'`no_output_flag` has the wrong type: Received `{type(params["no_output_flag"])}` expected `bool`')
    if params.get("mesh_flag", None) is not None:
        if not isinstance(params["mesh_flag"], bool):
            raise StyxValidationError(f'`mesh_flag` has the wrong type: Received `{type(params["mesh_flag"])}` expected `bool`')
    if params.get("head_radius", None) is not None:
        if not isinstance(params["head_radius"], (float, int)):
            raise StyxValidationError(f'`head_radius` has the wrong type: Received `{type(params["head_radius"])}` expected `float`')
    if params.get("smooth_factor", None) is not None:
        if not isinstance(params["smooth_factor"], (float, int)):
            raise StyxValidationError(f'`smooth_factor` has the wrong type: Received `{type(params["smooth_factor"])}` expected `float`')
    if params.get("threshold_flag", None) is not None:
        if not isinstance(params["threshold_flag"], bool):
            raise StyxValidationError(f'`threshold_flag` has the wrong type: Received `{type(params["threshold_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def bet2_cargs(params: Bet2, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bet2")
    cargs.append(params["input_fileroot"])
    cargs.append(params["output_fileroot"])
    v_0 = params.get("fractional_intensity")
    if v_0 is not None:
        cargs.append("-f")
        cargs.append(str(v_0))
    v_1 = params.get("vertical_gradient")
    if v_1 is not None:
        cargs.append("-g")
        cargs.append(str(v_1))
    v_2 = params.get("center_of_gravity")
    if v_2 is not None:
        cargs.append("-c")
        for item0 in v_2:
            cargs.append(str(item0))
    if params.get("outline_flag"):
        cargs.append("-o")
    if params.get("mask_flag"):
        cargs.append("-m")
    if params.get("skull_flag"):
        cargs.append("-s")
    if params.get("no_output_flag"):
        cargs.append("-n")
    if params.get("mesh_flag"):
        cargs.append("-e")
    v_3 = params.get("head_radius")
    if v_3 is not None:
        cargs.append("-r")
        cargs.append(str(v_3))
    v_4 = params.get("smooth_factor")
    if v_4 is not None:
        cargs.append("-w")
        cargs.append(str(v_4))
    if params.get("threshold_flag"):
        cargs.append("-t")
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def bet2_outputs(params: Bet2, execution: Execution) -> Bet2Outputs:
    """Build the Bet2Outputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_mask_v: OutputPathType = execution.output_file(f'{params["output_fileroot"]}_mask.nii.gz')
    output_skull_v: OutputPathType = execution.output_file(f'{params["output_fileroot"]}_skull.nii.gz')
    output_mesh_v: OutputPathType = execution.output_file(f'{params["output_fileroot"]}_mesh.vtk')
    output_overlay_v: OutputPathType = execution.output_file(f'{params["output_fileroot"]}_overlay.nii.gz')
    return Bet2Outputs(
        root=root_v,
        output_mask=output_mask_v,
        output_skull=output_skull_v,
        output_mesh=output_mesh_v,
        output_overlay=output_overlay_v,
    )

def bet2_execute(params: Bet2, runner: Runner | None = None) -> Bet2Outputs:
    """
    bet2

    Automated brain extraction tool for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bet2_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BET2_METADATA)
    execution.params(params)
    args = bet2_cargs(params, execution)
    out = bet2_outputs(params, execution)
    execution.run(args)
    return out

def bet2(
    input_fileroot: str,
    output_fileroot: str,
    fractional_intensity: float | None = None,
    vertical_gradient: float | None = None,
    center_of_gravity: list[float] | None = None,
    outline_flag: bool = False,
    mask_flag: bool = False,
    skull_flag: bool = False,
    no_output_flag: bool = False,
    mesh_flag: bool = False,
    head_radius: float | None = None,
    smooth_factor: float | None = None,
    threshold_flag: bool = False,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> Bet2Outputs:
    """
    bet2

    Automated brain extraction tool for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_fileroot: Input file root (e.g. img)
        output_fileroot: Output file root (e.g. img_bet)
        fractional_intensity: Fractional intensity threshold (0->1); default=0.5;\
            smaller values give larger brain outline estimates
        vertical_gradient: Vertical gradient in fractional intensity threshold\
            (-1->1); default=0; positive values give larger brain outline at\
            bottom, smaller at top
        center_of_gravity: The xyz coordinates of the center of gravity (voxels,\
            not mm) of initial mesh surface. Must have exactly three numerical\
            entries in the list (3-vector).
        outline_flag: Generate brain surface outline overlaid onto original image
        mask_flag: Generate binary brain mask
        skull_flag: Generate approximate skull image
        no_output_flag: Don't generate segmented brain image output
        mesh_flag: Generate brain surface as mesh in vtk format
        head_radius: Head radius (mm not voxels); initial surface sphere is set to\
            half of this
        smooth_factor: Smoothness factor; default=1; values smaller than 1 produce\
            more detailed brain surface, values larger than one produce smoother,\
            less detailed surface
        threshold_flag: Apply thresholding to segmented brain image and mask
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bet2_params(
        input_fileroot=input_fileroot,
        output_fileroot=output_fileroot,
        fractional_intensity=fractional_intensity,
        vertical_gradient=vertical_gradient,
        center_of_gravity=center_of_gravity,
        outline_flag=outline_flag,
        mask_flag=mask_flag,
        skull_flag=skull_flag,
        no_output_flag=no_output_flag,
        mesh_flag=mesh_flag,
        head_radius=head_radius,
        smooth_factor=smooth_factor,
        threshold_flag=threshold_flag,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return bet2_execute(params, runner)

__all__ = [
    "Bet2",
    "Bet2Outputs",
    "BET2_METADATA",
    "bet2_cargs",
    "bet2_outputs",
    "bet2_params",
    "bet2_execute",
    "bet2_validate",
    "bet2",
]