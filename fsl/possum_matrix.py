# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POSSUM_MATRIX_METADATA = Metadata(
    id="possum_matrix",
    name="possum_matrix",
    package="fsl",
)

PossumMatrix = typing.TypedDict(
    "PossumMatrix",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/possum_matrix"]],
        "pulse_sequence": str,
        "motion_matrix": str,
        "output_matrix": str,
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
        "old_version_flag": typing.NotRequired[bool],
        "segment_size": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class PossumMatrixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_main_matrix: OutputPathType
    """Main event matrix output file"""

def possum_matrix_params(
    pulse_sequence: str,
    motion_matrix: str,
    output_matrix: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
    old_version_flag: bool = False,
    segment_size: float | None = None,
) -> PossumMatrix:
    """
    Build parameters.

    Args:
        pulse_sequence: Pulse sequence - all additional files with extensions\
            .posx, .posy, etc., expected to be in the same directory
        motion_matrix: Motion matrix [time(s) Tx(m) Ty(m) Tz(m) Rx(rad) Ry(rad)\
            Rz(rad)]
        output_matrix: Main event matrix [t(s), rf_ang(rad), rf_freq_band(Hz),\
            (4)=rf_cent_freq(Hz), read(1/0), Gx, Gy, Gz(T/m), Tx, Ty, Tz(m),\
            angle_of_rot B(rad), rot_axis Bx, By, Bz(m), angle_of_rot A(rad),\
            rot_axis Ax, Ay, Az(m)]
        verbose_flag: Switch on diagnostic messages
        help_flag: Display this help message
        old_version_flag: Allows for the old version of the sorter to run
        segment_size: Setting the size of the segment of the matrix that is read in\
            one at a time

    Returns:
        Parameter dictionary.
    """
    params: PossumMatrix = {
        "@type": "fsl/possum_matrix",
        "pulse_sequence": pulse_sequence,
        "motion_matrix": motion_matrix,
        "output_matrix": output_matrix,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
        "old_version_flag": old_version_flag,
    }
    if segment_size is not None:
        params["segment_size"] = segment_size
    return params

def possum_matrix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PossumMatrix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("pulse_sequence", None) is None:
        raise StyxValidationError("`pulse_sequence` must not be None")
    if not isinstance(params["pulse_sequence"], str):
        raise StyxValidationError(f'`pulse_sequence` has the wrong type: Received `{type(params["pulse_sequence"])}` expected `str`')
    if params.get("motion_matrix", None) is None:
        raise StyxValidationError("`motion_matrix` must not be None")
    if not isinstance(params["motion_matrix"], str):
        raise StyxValidationError(f'`motion_matrix` has the wrong type: Received `{type(params["motion_matrix"])}` expected `str`')
    if params.get("output_matrix", None) is None:
        raise StyxValidationError("`output_matrix` must not be None")
    if not isinstance(params["output_matrix"], str):
        raise StyxValidationError(f'`output_matrix` has the wrong type: Received `{type(params["output_matrix"])}` expected `str`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("old_version_flag", None) is not None:
        if not isinstance(params["old_version_flag"], bool):
            raise StyxValidationError(f'`old_version_flag` has the wrong type: Received `{type(params["old_version_flag"])}` expected `bool`')
    if params.get("segment_size", None) is not None:
        if not isinstance(params["segment_size"], (float, int)):
            raise StyxValidationError(f'`segment_size` has the wrong type: Received `{type(params["segment_size"])}` expected `float`')

def possum_matrix_cargs(params: PossumMatrix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("possum_matrix")
    cargs.append("-p")
    cargs.append(params["pulse_sequence"])
    cargs.append("-m")
    cargs.append(params["motion_matrix"])
    cargs.append("-o")
    cargs.append(params["output_matrix"])
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    if params.get("old_version_flag"):
        cargs.append("--old")
    v_0 = params.get("segment_size")
    if v_0 is not None:
        cargs.append("--seg")
        cargs.append(str(v_0))
    return cargs

def possum_matrix_outputs(params: PossumMatrix, execution: Execution) -> PossumMatrixOutputs:
    """Build the PossumMatrixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_main_matrix_v: OutputPathType = execution.output_file(params["output_matrix"])
    return PossumMatrixOutputs(
        root=root_v,
        output_main_matrix=output_main_matrix_v,
    )

def possum_matrix_execute(params: PossumMatrix, runner: Runner | None = None) -> PossumMatrixOutputs:
    """
    possum_matrix

    Event matrix generator for POSSUM simulation in FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    possum_matrix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POSSUM_MATRIX_METADATA)
    execution.params(params)
    args = possum_matrix_cargs(params, execution)
    out = possum_matrix_outputs(params, execution)
    execution.run(args)
    return out

def possum_matrix(
    pulse_sequence: str,
    motion_matrix: str,
    output_matrix: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
    old_version_flag: bool = False,
    segment_size: float | None = None,
    runner: Runner | None = None,
) -> PossumMatrixOutputs:
    """
    possum_matrix

    Event matrix generator for POSSUM simulation in FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        pulse_sequence: Pulse sequence - all additional files with extensions\
            .posx, .posy, etc., expected to be in the same directory
        motion_matrix: Motion matrix [time(s) Tx(m) Ty(m) Tz(m) Rx(rad) Ry(rad)\
            Rz(rad)]
        output_matrix: Main event matrix [t(s), rf_ang(rad), rf_freq_band(Hz),\
            (4)=rf_cent_freq(Hz), read(1/0), Gx, Gy, Gz(T/m), Tx, Ty, Tz(m),\
            angle_of_rot B(rad), rot_axis Bx, By, Bz(m), angle_of_rot A(rad),\
            rot_axis Ax, Ay, Az(m)]
        verbose_flag: Switch on diagnostic messages
        help_flag: Display this help message
        old_version_flag: Allows for the old version of the sorter to run
        segment_size: Setting the size of the segment of the matrix that is read in\
            one at a time
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = possum_matrix_params(
        pulse_sequence=pulse_sequence,
        motion_matrix=motion_matrix,
        output_matrix=output_matrix,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
        old_version_flag=old_version_flag,
        segment_size=segment_size,
    )
    return possum_matrix_execute(params, runner)

__all__ = [
    "PossumMatrix",
    "PossumMatrixOutputs",
    "POSSUM_MATRIX_METADATA",
    "possum_matrix_cargs",
    "possum_matrix_outputs",
    "possum_matrix_params",
    "possum_matrix_execute",
    "possum_matrix_validate",
    "possum_matrix",
]