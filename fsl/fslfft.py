# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLFFT_METADATA = Metadata(
    id="fslfft",
    name="fslfft",
    package="fsl",
)

Fslfft = typing.TypedDict(
    "Fslfft",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslfft"]],
        "input_volume": InputPathType,
        "output_volume": str,
        "inverse_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FslfftOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output volume result of the Fourier transform"""

def fslfft_params(
    input_volume: InputPathType,
    output_volume: str,
    inverse_flag: bool = False,
) -> Fslfft:
    """
    Build parameters.

    Args:
        input_volume: Input volume file (e.g. invol.nii.gz)
        output_volume: Output volume file (e.g. outvol.nii.gz)
        inverse_flag: Flag to perform the inverse Fourier transform

    Returns:
        Parameter dictionary.
    """
    params: Fslfft = {
        "@type": "fsl/fslfft",
        "input_volume": input_volume,
        "output_volume": output_volume,
        "inverse_flag": inverse_flag,
    }
    return params

def fslfft_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslfft."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params["input_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is None:
        raise StyxValidationError("`output_volume` must not be None")
    if not isinstance(params["output_volume"], str):
        raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("inverse_flag", None) is not None:
        if not isinstance(params["inverse_flag"], bool):
            raise StyxValidationError(f'`inverse_flag` has the wrong type: Received `{type(params["inverse_flag"])}` expected `bool`')

def fslfft_cargs(params: Fslfft, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslfft")
    cargs.append(execution.input_file(params["input_volume"]))
    cargs.append(params["output_volume"])
    if params.get("inverse_flag"):
        cargs.append("-inv")
    return cargs

def fslfft_outputs(params: Fslfft, execution: Execution) -> FslfftOutputs:
    """Build the FslfftOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["output_volume"]}.nii.gz')
    return FslfftOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def fslfft_execute(params: Fslfft, runner: Runner | None = None) -> FslfftOutputs:
    """
    fslfft

    A tool to compute the Fourier transform of an input volume and save the result in an output volume.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslfft_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLFFT_METADATA)
    execution.params(params)
    args = fslfft_cargs(params, execution)
    out = fslfft_outputs(params, execution)
    execution.run(args)
    return out

def fslfft(
    input_volume: InputPathType,
    output_volume: str,
    inverse_flag: bool = False,
    runner: Runner | None = None,
) -> FslfftOutputs:
    """
    fslfft

    A tool to compute the Fourier transform of an input volume and save the result in an output volume.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_volume: Input volume file (e.g. invol.nii.gz)
        output_volume: Output volume file (e.g. outvol.nii.gz)
        inverse_flag: Flag to perform the inverse Fourier transform
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslfft_params(
        input_volume=input_volume,
        output_volume=output_volume,
        inverse_flag=inverse_flag,
    )
    return fslfft_execute(params, runner)

__all__ = [
    "Fslfft",
    "FslfftOutputs",
    "FSLFFT_METADATA",
    "fslfft_cargs",
    "fslfft_outputs",
    "fslfft_params",
    "fslfft_execute",
    "fslfft_validate",
    "fslfft",
]