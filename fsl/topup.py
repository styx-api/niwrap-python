# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TOPUP_METADATA = Metadata(
    id="topup",
    name="topup",
    package="fsl",
)

TopupRegmod = typing.Literal["membrane_energy"] | typing.Literal["bending_energy"]

TopupSplineorder = typing.Literal[2] | typing.Literal[3]

TopupNumprec = typing.Literal["double"] | typing.Literal["float"]

TopupInterp = typing.Literal["linear"] | typing.Literal["spline"]

Topup = typing.TypedDict(
    "Topup",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/topup"]],
        "imain": InputPathType,
        "datain": InputPathType,
        "out": typing.NotRequired[str],
        "fout": typing.NotRequired[str],
        "iout": typing.NotRequired[str],
        "logout": typing.NotRequired[str],
        "warpres": typing.NotRequired[float],
        "subsamp": typing.NotRequired[float],
        "fwhm": typing.NotRequired[float],
        "config": typing.NotRequired[InputPathType],
        "miter": typing.NotRequired[float],
        "lambda": typing.NotRequired[float],
        "ssqlambda": typing.NotRequired[bool],
        "regmod": typing.NotRequired[TopupRegmod],
        "estmov": typing.NotRequired[bool],
        "minmet": typing.NotRequired[bool],
        "splineorder": typing.NotRequired[TopupSplineorder],
        "numprec": typing.NotRequired[TopupNumprec],
        "interp": typing.NotRequired[TopupInterp],
        "scale": typing.NotRequired[bool],
        "regrid": typing.NotRequired[bool],
        "nthr": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TopupOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fieldcoef: OutputPathType | None
    """Spline coefficient field estimates (Hz)"""
    movpar: OutputPathType | None
    """Movement parameters"""
    fout: OutputPathType | None
    """Image file with field (Hz)"""
    iout: OutputPathType | None
    """4D image file with unwarped images"""
    logout: OutputPathType | None
    """Log file"""

def topup_params(
    imain: InputPathType,
    datain: InputPathType,
    out: str | None = None,
    fout: str | None = None,
    iout: str | None = None,
    logout: str | None = None,
    warpres: float | None = None,
    subsamp: float | None = None,
    fwhm: float | None = None,
    config: InputPathType | None = None,
    miter: float | None = None,
    lambda_: float | None = None,
    ssqlambda: bool = False,
    regmod: TopupRegmod | None = None,
    estmov: bool = False,
    minmet: bool | None = None,
    splineorder: TopupSplineorder | None = None,
    numprec: TopupNumprec | None = None,
    interp: TopupInterp | None = None,
    scale: bool = False,
    regrid: bool = False,
    nthr: float | None = None,
    verbose: bool = False,
) -> Topup:
    """
    Build parameters.

    Args:
        imain: Name of 4D file with images
        datain: Name of text file with PE directions/times
        out: Base-name of output files (spline coefficients (Hz) and movement\
            parameters)
        fout: Name of image file with field (Hz)
        iout: Name of 4D image file with unwarped images
        logout: Name of log-file
        warpres: (Approximate) resolution (in mm) of warp basis for the different\
            sub-sampling levels, default 10
        subsamp: Sub-sampling scheme, default 1
        fwhm: FWHM (in mm) of gaussian smoothing kernel, default 8
        config: Name of config file specifying command line arguments
        miter: Max # of non-linear iterations, default 5
        lambda_: Weight of regularisation, default depending on --ssqlambda and\
            --regmod switches. See user documentation.
        ssqlambda: If set (=1), lambda is weighted by current ssq, default 1
        regmod: Model for regularisation of warp-field [membrane_energy\
            bending_energy], default bending_energy
        estmov: Estimate movements if set, default 1 (true)
        minmet: Minimisation method 0=Levenberg-Marquardt, 1=Scaled Conjugate\
            Gradient, default 0 (LM)
        splineorder: Order of spline, 2=Quadratic spline, 3=Cubic spline. Default=3
        numprec: Precision for representing Hessian, double or float. Default\
            double
        interp: Image interpolation model, linear or spline. Default spline
        scale: If set (=1), the images are individually scaled to a common mean,\
            default 0 (false)
        regrid: If set (=1), the calculations are done in a different grid, default\
            1 (true)
        nthr: Number of threads to use (cannot be greater than numbers of hardware\
            cores), default 1
        verbose: Print diagnostic information while running

    Returns:
        Parameter dictionary.
    """
    params: Topup = {
        "@type": "fsl/topup",
        "imain": imain,
        "datain": datain,
        "ssqlambda": ssqlambda,
        "estmov": estmov,
        "scale": scale,
        "regrid": regrid,
        "verbose": verbose,
    }
    if out is not None:
        params["out"] = out
    if fout is not None:
        params["fout"] = fout
    if iout is not None:
        params["iout"] = iout
    if logout is not None:
        params["logout"] = logout
    if warpres is not None:
        params["warpres"] = warpres
    if subsamp is not None:
        params["subsamp"] = subsamp
    if fwhm is not None:
        params["fwhm"] = fwhm
    if config is not None:
        params["config"] = config
    if miter is not None:
        params["miter"] = miter
    if lambda_ is not None:
        params["lambda"] = lambda_
    if regmod is not None:
        params["regmod"] = regmod
    if minmet is not None:
        params["minmet"] = minmet
    if splineorder is not None:
        params["splineorder"] = splineorder
    if numprec is not None:
        params["numprec"] = numprec
    if interp is not None:
        params["interp"] = interp
    if nthr is not None:
        params["nthr"] = nthr
    return params

def topup_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Topup."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("imain", None) is None:
        raise StyxValidationError("`imain` must not be None")
    if not isinstance(params["imain"], (pathlib.Path, str)):
        raise StyxValidationError(f'`imain` has the wrong type: Received `{type(params["imain"])}` expected `InputPathType`')
    if params.get("datain", None) is None:
        raise StyxValidationError("`datain` must not be None")
    if not isinstance(params["datain"], (pathlib.Path, str)):
        raise StyxValidationError(f'`datain` has the wrong type: Received `{type(params["datain"])}` expected `InputPathType`')
    if params.get("out", None) is not None:
        if not isinstance(params["out"], str):
            raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')
    if params.get("fout", None) is not None:
        if not isinstance(params["fout"], str):
            raise StyxValidationError(f'`fout` has the wrong type: Received `{type(params["fout"])}` expected `str`')
    if params.get("iout", None) is not None:
        if not isinstance(params["iout"], str):
            raise StyxValidationError(f'`iout` has the wrong type: Received `{type(params["iout"])}` expected `str`')
    if params.get("logout", None) is not None:
        if not isinstance(params["logout"], str):
            raise StyxValidationError(f'`logout` has the wrong type: Received `{type(params["logout"])}` expected `str`')
    if params.get("warpres", None) is not None:
        if not isinstance(params["warpres"], (float, int)):
            raise StyxValidationError(f'`warpres` has the wrong type: Received `{type(params["warpres"])}` expected `float`')
    if params.get("subsamp", None) is not None:
        if not isinstance(params["subsamp"], (float, int)):
            raise StyxValidationError(f'`subsamp` has the wrong type: Received `{type(params["subsamp"])}` expected `float`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `float`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], (pathlib.Path, str)):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `InputPathType`')
    if params.get("miter", None) is not None:
        if not isinstance(params["miter"], (float, int)):
            raise StyxValidationError(f'`miter` has the wrong type: Received `{type(params["miter"])}` expected `float`')
    if params.get("lambda", None) is not None:
        if not isinstance(params["lambda"], (float, int)):
            raise StyxValidationError(f'`lambda` has the wrong type: Received `{type(params["lambda"])}` expected `float`')
    if params.get("ssqlambda", None) is not None:
        if not isinstance(params["ssqlambda"], bool):
            raise StyxValidationError(f'`ssqlambda` has the wrong type: Received `{type(params["ssqlambda"])}` expected `bool`')
    if params.get("regmod", None) is not None:
        if not isinstance(params["regmod"], str):
            raise StyxValidationError(f'`regmod` has the wrong type: Received `{type(params["regmod"])}` expected `TopupRegmod`')
        if params["regmod"] not in ["membrane_energy", "bending_energy"]:
            raise StyxValidationError("Parameter `regmod` must be one of [\"membrane_energy\", \"bending_energy\"]")
    if params.get("estmov", None) is not None:
        if not isinstance(params["estmov"], bool):
            raise StyxValidationError(f'`estmov` has the wrong type: Received `{type(params["estmov"])}` expected `bool`')
    if params.get("minmet", None) is not None:
        if not isinstance(params["minmet"], bool):
            raise StyxValidationError(f'`minmet` has the wrong type: Received `{type(params["minmet"])}` expected `bool`')
    if params.get("splineorder", None) is not None:
        if not isinstance(params["splineorder"], (float, int)):
            raise StyxValidationError(f'`splineorder` has the wrong type: Received `{type(params["splineorder"])}` expected `TopupSplineorder`')
        if params["splineorder"] not in [2, 3]:
            raise StyxValidationError("Parameter `splineorder` must be one of [2, 3]")
    if params.get("numprec", None) is not None:
        if not isinstance(params["numprec"], str):
            raise StyxValidationError(f'`numprec` has the wrong type: Received `{type(params["numprec"])}` expected `TopupNumprec`')
        if params["numprec"] not in ["double", "float"]:
            raise StyxValidationError("Parameter `numprec` must be one of [\"double\", \"float\"]")
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `TopupInterp`')
        if params["interp"] not in ["linear", "spline"]:
            raise StyxValidationError("Parameter `interp` must be one of [\"linear\", \"spline\"]")
    if params.get("scale", None) is not None:
        if not isinstance(params["scale"], bool):
            raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `bool`')
    if params.get("regrid", None) is not None:
        if not isinstance(params["regrid"], bool):
            raise StyxValidationError(f'`regrid` has the wrong type: Received `{type(params["regrid"])}` expected `bool`')
    if params.get("nthr", None) is not None:
        if not isinstance(params["nthr"], (float, int)):
            raise StyxValidationError(f'`nthr` has the wrong type: Received `{type(params["nthr"])}` expected `float`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def topup_cargs(params: Topup, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("topup")
    cargs.append("--imain=")
    cargs.append(execution.input_file(params["imain"]))
    cargs.append("--datain=")
    cargs.append(execution.input_file(params["datain"]))
    v_0 = params.get("out")
    if v_0 is not None:
        cargs.append("--out=")
        cargs.append(v_0)
    v_1 = params.get("fout")
    if v_1 is not None:
        cargs.append("--fout=")
        cargs.append(v_1)
    v_2 = params.get("iout")
    if v_2 is not None:
        cargs.append("--iout=")
        cargs.append(v_2)
    v_3 = params.get("logout")
    if v_3 is not None:
        cargs.append("--logout=")
        cargs.append(v_3)
    v_4 = params.get("warpres")
    if v_4 is not None:
        cargs.append("--warpres=")
        cargs.append(str(v_4))
    v_5 = params.get("subsamp")
    if v_5 is not None:
        cargs.append("--subsamp=")
        cargs.append(str(v_5))
    v_6 = params.get("fwhm")
    if v_6 is not None:
        cargs.append("--fwhm=")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        cargs.append("--config=")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("miter")
    if v_8 is not None:
        cargs.append("--miter=")
        cargs.append(str(v_8))
    v_9 = params.get("lambda")
    if v_9 is not None:
        cargs.append("--lambda=")
        cargs.append(str(v_9))
    if params.get("ssqlambda"):
        cargs.append("--ssqlambda")
    v_10 = params.get("regmod")
    if v_10 is not None:
        cargs.append("--regmod=")
        cargs.append(str(v_10))
    if params.get("estmov"):
        cargs.append("--estmov")
    v_11 = params.get("minmet")
    if v_11 is not None:
        cargs.append("--minmet=")
        if v_11:
            cargs.append("0")
        else:
            cargs.append("1")
    v_12 = params.get("splineorder")
    if v_12 is not None:
        cargs.append("--splineorder=")
        cargs.append(str(v_12))
    v_13 = params.get("numprec")
    if v_13 is not None:
        cargs.append("--numprec=")
        cargs.append(str(v_13))
    v_14 = params.get("interp")
    if v_14 is not None:
        cargs.append("--interp=")
        cargs.append(str(v_14))
    if params.get("scale"):
        cargs.append("--scale")
    if params.get("regrid"):
        cargs.append("--regrid")
    v_15 = params.get("nthr")
    if v_15 is not None:
        cargs.append("--nthr=")
        cargs.append(str(v_15))
    if params.get("verbose"):
        cargs.append("--verbose")
    return cargs

def topup_outputs(params: Topup, execution: Execution) -> TopupOutputs:
    """Build the TopupOutputs object for this tool."""
    fieldcoef_v: OutputPathType | None = None
    movpar_v: OutputPathType | None = None
    fout_v: OutputPathType | None = None
    iout_v: OutputPathType | None = None
    logout_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out")
    if __v0 is not None:
        fieldcoef_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_fieldcoef.nii.gz')
    __v1 = params.get("out")
    if __v1 is not None:
        movpar_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}_movpar.txt')
    __v2 = params.get("fout")
    if __v2 is not None:
        fout_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}.nii.gz')
    __v3 = params.get("iout")
    if __v3 is not None:
        iout_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}.nii.gz')
    __v4 = params.get("logout")
    if __v4 is not None:
        logout_v = execution.output_file((__v4 if __v4 is not None else ""))
    return TopupOutputs(
        root=root_v,
        fieldcoef=fieldcoef_v,
        movpar=movpar_v,
        fout=fout_v,
        iout=iout_v,
        logout=logout_v,
    )

def topup_execute(params: Topup, runner: Runner | None = None) -> TopupOutputs:
    """
    topup

    topup is part of FSL and is used to estimate and correct for susceptibility-induced distortions in echo planar imaging (EPI) data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    topup_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TOPUP_METADATA)
    execution.params(params)
    args = topup_cargs(params, execution)
    out = topup_outputs(params, execution)
    execution.run(args)
    return out

def topup(
    imain: InputPathType,
    datain: InputPathType,
    out: str | None = None,
    fout: str | None = None,
    iout: str | None = None,
    logout: str | None = None,
    warpres: float | None = None,
    subsamp: float | None = None,
    fwhm: float | None = None,
    config: InputPathType | None = None,
    miter: float | None = None,
    lambda_: float | None = None,
    ssqlambda: bool = False,
    regmod: TopupRegmod | None = None,
    estmov: bool = False,
    minmet: bool | None = None,
    splineorder: TopupSplineorder | None = None,
    numprec: TopupNumprec | None = None,
    interp: TopupInterp | None = None,
    scale: bool = False,
    regrid: bool = False,
    nthr: float | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> TopupOutputs:
    """
    topup

    topup is part of FSL and is used to estimate and correct for susceptibility-induced distortions in echo planar imaging (EPI) data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        imain: Name of 4D file with images
        datain: Name of text file with PE directions/times
        out: Base-name of output files (spline coefficients (Hz) and movement\
            parameters)
        fout: Name of image file with field (Hz)
        iout: Name of 4D image file with unwarped images
        logout: Name of log-file
        warpres: (Approximate) resolution (in mm) of warp basis for the different\
            sub-sampling levels, default 10
        subsamp: Sub-sampling scheme, default 1
        fwhm: FWHM (in mm) of gaussian smoothing kernel, default 8
        config: Name of config file specifying command line arguments
        miter: Max # of non-linear iterations, default 5
        lambda_: Weight of regularisation, default depending on --ssqlambda and\
            --regmod switches. See user documentation.
        ssqlambda: If set (=1), lambda is weighted by current ssq, default 1
        regmod: Model for regularisation of warp-field [membrane_energy\
            bending_energy], default bending_energy
        estmov: Estimate movements if set, default 1 (true)
        minmet: Minimisation method 0=Levenberg-Marquardt, 1=Scaled Conjugate\
            Gradient, default 0 (LM)
        splineorder: Order of spline, 2=Quadratic spline, 3=Cubic spline. Default=3
        numprec: Precision for representing Hessian, double or float. Default\
            double
        interp: Image interpolation model, linear or spline. Default spline
        scale: If set (=1), the images are individually scaled to a common mean,\
            default 0 (false)
        regrid: If set (=1), the calculations are done in a different grid, default\
            1 (true)
        nthr: Number of threads to use (cannot be greater than numbers of hardware\
            cores), default 1
        verbose: Print diagnostic information while running
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = topup_params(
        imain=imain,
        datain=datain,
        out=out,
        fout=fout,
        iout=iout,
        logout=logout,
        warpres=warpres,
        subsamp=subsamp,
        fwhm=fwhm,
        config=config,
        miter=miter,
        lambda_=lambda_,
        ssqlambda=ssqlambda,
        regmod=regmod,
        estmov=estmov,
        minmet=minmet,
        splineorder=splineorder,
        numprec=numprec,
        interp=interp,
        scale=scale,
        regrid=regrid,
        nthr=nthr,
        verbose=verbose,
    )
    return topup_execute(params, runner)

__all__ = [
    "Topup",
    "TopupOutputs",
    "TOPUP_METADATA",
    "topup_cargs",
    "topup_outputs",
    "topup_params",
    "topup_execute",
    "topup_validate",
    "topup",
]