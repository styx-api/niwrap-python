# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLADD_METADATA = Metadata(
    id="fsladd",
    name="fsladd",
    package="fsl",
)

Fsladd = typing.TypedDict(
    "Fsladd",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fsladd"]],
        "output_file": str,
        "mean_flag": typing.NotRequired[bool],
        "scale_flag": typing.NotRequired[bool],
        "volume_list": list[InputPathType],
    },
)

@dataclasses.dataclass
class FsladdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resulting_output: OutputPathType
    """Resulting output file"""

def fsladd_params(
    output_file: str,
    volume_list: list[InputPathType],
    mean_flag: bool = False,
    scale_flag: bool = False,
) -> Fsladd:
    """
    Build parameters.

    Args:
        output_file: Output volume file
        volume_list: List of input volumes
        mean_flag: Calculate mean instead of sum
        scale_flag: Scale each input image mean to 1000 before processing

    Returns:
        Parameter dictionary.
    """
    params: Fsladd = {
        "@type": "fsl/fsladd",
        "output_file": output_file,
        "volume_list": volume_list,
        "mean_flag": mean_flag,
        "scale_flag": scale_flag,
    }
    return params

def fsladd_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fsladd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("mean_flag", None) is not None:
        if not isinstance(params["mean_flag"], bool):
            raise StyxValidationError(f'`mean_flag` has the wrong type: Received `{type(params["mean_flag"])}` expected `bool`')
    if params.get("scale_flag", None) is not None:
        if not isinstance(params["scale_flag"], bool):
            raise StyxValidationError(f'`scale_flag` has the wrong type: Received `{type(params["scale_flag"])}` expected `bool`')
    if params.get("volume_list", None) is None:
        raise StyxValidationError("`volume_list` must not be None")
    if not isinstance(params["volume_list"], list):
        raise StyxValidationError(f'`volume_list` has the wrong type: Received `{type(params["volume_list"])}` expected `list[InputPathType]`')
    for e in params["volume_list"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`volume_list` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def fsladd_cargs(params: Fsladd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fsladd")
    cargs.append(params["output_file"])
    if params.get("mean_flag"):
        cargs.append("-m")
    if params.get("scale_flag"):
        cargs.append("-s")
    for item0 in params["volume_list"]:
        cargs.append(execution.input_file(item0))
    return cargs

def fsladd_outputs(params: Fsladd, execution: Execution) -> FsladdOutputs:
    """Build the FsladdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    resulting_output_v: OutputPathType = execution.output_file(params["output_file"])
    return FsladdOutputs(
        root=root_v,
        resulting_output=resulting_output_v,
    )

def fsladd_execute(params: Fsladd, runner: Runner | None = None) -> FsladdOutputs:
    """
    fsladd

    Tool for adding or averaging multiple input volumes.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fsladd_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLADD_METADATA)
    execution.params(params)
    args = fsladd_cargs(params, execution)
    out = fsladd_outputs(params, execution)
    execution.run(args)
    return out

def fsladd(
    output_file: str,
    volume_list: list[InputPathType],
    mean_flag: bool = False,
    scale_flag: bool = False,
    runner: Runner | None = None,
) -> FsladdOutputs:
    """
    fsladd

    Tool for adding or averaging multiple input volumes.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        output_file: Output volume file
        volume_list: List of input volumes
        mean_flag: Calculate mean instead of sum
        scale_flag: Scale each input image mean to 1000 before processing
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fsladd_params(
        output_file=output_file,
        volume_list=volume_list,
        mean_flag=mean_flag,
        scale_flag=scale_flag,
    )
    return fsladd_execute(params, runner)

__all__ = [
    "Fsladd",
    "FsladdOutputs",
    "FSLADD_METADATA",
    "fsladd_cargs",
    "fsladd_outputs",
    "fsladd_params",
    "fsladd_execute",
    "fsladd_validate",
    "fsladd",
]