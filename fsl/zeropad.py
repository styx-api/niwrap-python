# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ZEROPAD_METADATA = Metadata(
    id="zeropad",
    name="zeropad",
    package="fsl",
)

Zeropad = typing.TypedDict(
    "Zeropad",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/zeropad"]],
        "input_number": str,
        "length": float,
    },
)

@dataclasses.dataclass
class ZeropadOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """File containing the zero-padded output"""

def zeropad_params(
    input_number: str,
    length: float,
) -> Zeropad:
    """
    Build parameters.

    Args:
        input_number: Input number to be zero-padded
        length: Desired length of the output string

    Returns:
        Parameter dictionary.
    """
    params: Zeropad = {
        "@type": "fsl/zeropad",
        "input_number": input_number,
        "length": length,
    }
    return params

def zeropad_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Zeropad."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_number", None) is None:
        raise StyxValidationError("`input_number` must not be None")
    if not isinstance(params["input_number"], str):
        raise StyxValidationError(f'`input_number` has the wrong type: Received `{type(params["input_number"])}` expected `str`')
    if params.get("length", None) is None:
        raise StyxValidationError("`length` must not be None")
    if not isinstance(params["length"], (float, int)):
        raise StyxValidationError(f'`length` has the wrong type: Received `{type(params["length"])}` expected `float`')

def zeropad_cargs(params: Zeropad, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("zeropad")
    cargs.append(params["input_number"])
    cargs.append(str(params["length"]))
    return cargs

def zeropad_outputs(params: Zeropad, execution: Execution) -> ZeropadOutputs:
    """Build the ZeropadOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("padded_output.txt")
    return ZeropadOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def zeropad_execute(params: Zeropad, runner: Runner | None = None) -> ZeropadOutputs:
    """
    zeropad

    Tool for zero-padding numbers to a specified length

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    zeropad_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ZEROPAD_METADATA)
    execution.params(params)
    args = zeropad_cargs(params, execution)
    out = zeropad_outputs(params, execution)
    execution.run(args)
    return out

def zeropad(
    input_number: str,
    length: float,
    runner: Runner | None = None,
) -> ZeropadOutputs:
    """
    zeropad

    Tool for zero-padding numbers to a specified length

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_number: Input number to be zero-padded
        length: Desired length of the output string
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = zeropad_params(
        input_number=input_number,
        length=length,
    )
    return zeropad_execute(params, runner)

__all__ = [
    "Zeropad",
    "ZeropadOutputs",
    "ZEROPAD_METADATA",
    "zeropad_cargs",
    "zeropad_outputs",
    "zeropad_params",
    "zeropad_execute",
    "zeropad_validate",
    "zeropad",
]