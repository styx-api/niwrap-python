# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMMV_METADATA = Metadata(
    id="immv",
    name="immv",
    package="fsl",
)

Immv = typing.TypedDict(
    "Immv",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/immv"]],
        "source_files": list[InputPathType],
        "destination": str,
    },
)

@dataclasses.dataclass
class ImmvOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def immv_params(
    source_files: list[InputPathType],
    destination: str,
) -> Immv:
    """
    Build parameters.

    Args:
        source_files: Source files to be moved. Recognized file extensions:\
            .nii.gz, .nii, .img, .hdr, .img.gz, .hdr.gz
        destination: Destination file or directory.

    Returns:
        Parameter dictionary.
    """
    params: Immv = {
        "@type": "fsl/immv",
        "source_files": source_files,
        "destination": destination,
    }
    return params

def immv_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Immv."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("source_files", None) is None:
        raise StyxValidationError("`source_files` must not be None")
    if not isinstance(params["source_files"], list):
        raise StyxValidationError(f'`source_files` has the wrong type: Received `{type(params["source_files"])}` expected `list[InputPathType]`')
    for e in params["source_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`source_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("destination", None) is None:
        raise StyxValidationError("`destination` must not be None")
    if not isinstance(params["destination"], str):
        raise StyxValidationError(f'`destination` has the wrong type: Received `{type(params["destination"])}` expected `str`')

def immv_cargs(params: Immv, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("immv")
    for item0 in params["source_files"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["destination"])
    return cargs

def immv_outputs(params: Immv, execution: Execution) -> ImmvOutputs:
    """Build the ImmvOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ImmvOutputs(
        root=root_v,
    )

def immv_execute(params: Immv, runner: Runner | None = None) -> ImmvOutputs:
    """
    immv

    Moves images from one file or directory to another.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    immv_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMMV_METADATA)
    execution.params(params)
    args = immv_cargs(params, execution)
    out = immv_outputs(params, execution)
    execution.run(args)
    return out

def immv(
    source_files: list[InputPathType],
    destination: str,
    runner: Runner | None = None,
) -> ImmvOutputs:
    """
    immv

    Moves images from one file or directory to another.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        source_files: Source files to be moved. Recognized file extensions:\
            .nii.gz, .nii, .img, .hdr, .img.gz, .hdr.gz
        destination: Destination file or directory.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = immv_params(
        source_files=source_files,
        destination=destination,
    )
    return immv_execute(params, runner)

__all__ = [
    "Immv",
    "ImmvOutputs",
    "IMMV_METADATA",
    "immv_cargs",
    "immv_outputs",
    "immv_params",
    "immv_execute",
    "immv_validate",
    "immv",
]