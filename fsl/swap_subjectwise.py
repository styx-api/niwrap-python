# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SWAP_SUBJECTWISE_METADATA = Metadata(
    id="swap_subjectwise",
    name="swap_subjectwise",
    package="fsl",
)

SwapSubjectwise = typing.TypedDict(
    "SwapSubjectwise",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/swap_subjectwise"]],
        "dyads": InputPathType,
        "fmean": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "obasename": typing.NotRequired[str],
        "xthresh": typing.NotRequired[float],
        "averageonly_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SwapSubjectwiseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def swap_subjectwise_params(
    dyads: InputPathType,
    fmean: InputPathType,
    mask: InputPathType | None = None,
    obasename: str | None = None,
    xthresh: float | None = None,
    averageonly_flag: bool = False,
    verbose_flag: bool = False,
) -> SwapSubjectwise:
    """
    Build parameters.

    Args:
        dyads: List of list of dyads
        fmean: List of list of mean fsamples
        mask: Filename of brain mask
        obasename: Output obasename [default=swapped]
        xthresh: A.R.D. threshold - default=0.1
        averageonly_flag: Average only?
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: SwapSubjectwise = {
        "@type": "fsl/swap_subjectwise",
        "dyads": dyads,
        "fmean": fmean,
        "averageonly_flag": averageonly_flag,
        "verbose_flag": verbose_flag,
    }
    if mask is not None:
        params["mask"] = mask
    if obasename is not None:
        params["obasename"] = obasename
    if xthresh is not None:
        params["xthresh"] = xthresh
    return params

def swap_subjectwise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SwapSubjectwise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dyads", None) is None:
        raise StyxValidationError("`dyads` must not be None")
    if not isinstance(params["dyads"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dyads` has the wrong type: Received `{type(params["dyads"])}` expected `InputPathType`')
    if params.get("fmean", None) is None:
        raise StyxValidationError("`fmean` must not be None")
    if not isinstance(params["fmean"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fmean` has the wrong type: Received `{type(params["fmean"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("obasename", None) is not None:
        if not isinstance(params["obasename"], str):
            raise StyxValidationError(f'`obasename` has the wrong type: Received `{type(params["obasename"])}` expected `str`')
    if params.get("xthresh", None) is not None:
        if not isinstance(params["xthresh"], (float, int)):
            raise StyxValidationError(f'`xthresh` has the wrong type: Received `{type(params["xthresh"])}` expected `float`')
    if params.get("averageonly_flag", None) is not None:
        if not isinstance(params["averageonly_flag"], bool):
            raise StyxValidationError(f'`averageonly_flag` has the wrong type: Received `{type(params["averageonly_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def swap_subjectwise_cargs(params: SwapSubjectwise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("swap_subjectwise")
    cargs.append("-r")
    cargs.append(execution.input_file(params["dyads"]))
    cargs.append("-f")
    cargs.append(execution.input_file(params["fmean"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("obasename")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(v_1)
    v_2 = params.get("xthresh")
    if v_2 is not None:
        cargs.append("--xthresh")
        cargs.append(str(v_2))
    if params.get("averageonly_flag"):
        cargs.append("--averageonly")
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    return cargs

def swap_subjectwise_outputs(params: SwapSubjectwise, execution: Execution) -> SwapSubjectwiseOutputs:
    """Build the SwapSubjectwiseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SwapSubjectwiseOutputs(
        root=root_v,
    )

def swap_subjectwise_execute(params: SwapSubjectwise, runner: Runner | None = None) -> SwapSubjectwiseOutputs:
    """
    swap_subjectwise

    Reordering of the dyadic vectors and fsamples according to average inter-subject modal orientations

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    swap_subjectwise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SWAP_SUBJECTWISE_METADATA)
    execution.params(params)
    args = swap_subjectwise_cargs(params, execution)
    out = swap_subjectwise_outputs(params, execution)
    execution.run(args)
    return out

def swap_subjectwise(
    dyads: InputPathType,
    fmean: InputPathType,
    mask: InputPathType | None = None,
    obasename: str | None = None,
    xthresh: float | None = None,
    averageonly_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> SwapSubjectwiseOutputs:
    """
    swap_subjectwise

    Reordering of the dyadic vectors and fsamples according to average inter-subject modal orientations

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        dyads: List of list of dyads
        fmean: List of list of mean fsamples
        mask: Filename of brain mask
        obasename: Output obasename [default=swapped]
        xthresh: A.R.D. threshold - default=0.1
        averageonly_flag: Average only?
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = swap_subjectwise_params(
        dyads=dyads,
        fmean=fmean,
        mask=mask,
        obasename=obasename,
        xthresh=xthresh,
        averageonly_flag=averageonly_flag,
        verbose_flag=verbose_flag,
    )
    return swap_subjectwise_execute(params, runner)

__all__ = [
    "SwapSubjectwise",
    "SwapSubjectwiseOutputs",
    "SWAP_SUBJECTWISE_METADATA",
    "swap_subjectwise_cargs",
    "swap_subjectwise_outputs",
    "swap_subjectwise_params",
    "swap_subjectwise_execute",
    "swap_subjectwise_validate",
    "swap_subjectwise",
]