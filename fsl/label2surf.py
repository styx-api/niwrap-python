# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL2SURF_METADATA = Metadata(
    id="label2surf",
    name="label2surf",
    package="fsl",
)

Label2surf = typing.TypedDict(
    "Label2surf",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/label2surf"]],
        "input_surface": InputPathType,
        "output_surface": str,
        "labels": InputPathType,
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Label2surfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_surf: OutputPathType
    """Resulting output surface file"""

def label2surf_params(
    input_surface: InputPathType,
    output_surface: str,
    labels: InputPathType,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Label2surf:
    """
    Build parameters.

    Args:
        input_surface: Input surface file
        output_surface: Output surface file
        labels: ASCII list of label files
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Label2surf = {
        "@type": "fsl/label2surf",
        "input_surface": input_surface,
        "output_surface": output_surface,
        "labels": labels,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    return params

def label2surf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Label2surf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params["input_surface"])}` expected `InputPathType`')
    if params.get("output_surface", None) is None:
        raise StyxValidationError("`output_surface` must not be None")
    if not isinstance(params["output_surface"], str):
        raise StyxValidationError(f'`output_surface` has the wrong type: Received `{type(params["output_surface"])}` expected `str`')
    if params.get("labels", None) is None:
        raise StyxValidationError("`labels` must not be None")
    if not isinstance(params["labels"], (pathlib.Path, str)):
        raise StyxValidationError(f'`labels` has the wrong type: Received `{type(params["labels"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def label2surf_cargs(params: Label2surf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label2surf")
    cargs.append("--surf")
    cargs.append(execution.input_file(params["input_surface"]))
    cargs.append("--out")
    cargs.append(params["output_surface"])
    cargs.append("--labels")
    cargs.append(execution.input_file(params["labels"]))
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    if params.get("help_flag"):
        cargs.append("--help")
    return cargs

def label2surf_outputs(params: Label2surf, execution: Execution) -> Label2surfOutputs:
    """Build the Label2surfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_surf_v: OutputPathType = execution.output_file(params["output_surface"])
    return Label2surfOutputs(
        root=root_v,
        out_surf=out_surf_v,
    )

def label2surf_execute(params: Label2surf, runner: Runner | None = None) -> Label2surfOutputs:
    """
    label2surf

    Transform a group of labels into a surface

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label2surf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL2SURF_METADATA)
    execution.params(params)
    args = label2surf_cargs(params, execution)
    out = label2surf_outputs(params, execution)
    execution.run(args)
    return out

def label2surf(
    input_surface: InputPathType,
    output_surface: str,
    labels: InputPathType,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> Label2surfOutputs:
    """
    label2surf

    Transform a group of labels into a surface

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_surface: Input surface file
        output_surface: Output surface file
        labels: ASCII list of label files
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label2surf_params(
        input_surface=input_surface,
        output_surface=output_surface,
        labels=labels,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return label2surf_execute(params, runner)

__all__ = [
    "Label2surf",
    "Label2surfOutputs",
    "LABEL2SURF_METADATA",
    "label2surf_cargs",
    "label2surf_outputs",
    "label2surf_params",
    "label2surf_execute",
    "label2surf_validate",
    "label2surf",
]