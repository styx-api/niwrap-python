# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POSSUM_SUM_METADATA = Metadata(
    id="possum_sum",
    name="possum_sum",
    package="fsl",
)

PossumSum = typing.TypedDict(
    "PossumSum",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/possum_sum"]],
        "input_signal": InputPathType,
        "output_signal": str,
        "num_processors": typing.NotRequired[int],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PossumSumOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Sum of all input signals from processors"""

def possum_sum_params(
    input_signal: InputPathType,
    output_signal: str,
    num_processors: int | None = None,
    verbose_flag: bool = False,
) -> PossumSum:
    """
    Build parameters.

    Args:
        input_signal: Input signal for one processor (possum output matrix)
        output_signal: Output signal: sum of all the processors (possum matrix\
            form)
        num_processors: Number of processors
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: PossumSum = {
        "@type": "fsl/possum_sum",
        "input_signal": input_signal,
        "output_signal": output_signal,
        "verbose_flag": verbose_flag,
    }
    if num_processors is not None:
        params["num_processors"] = num_processors
    return params

def possum_sum_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PossumSum."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_signal", None) is None:
        raise StyxValidationError("`input_signal` must not be None")
    if not isinstance(params["input_signal"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_signal` has the wrong type: Received `{type(params["input_signal"])}` expected `InputPathType`')
    if params.get("output_signal", None) is None:
        raise StyxValidationError("`output_signal` must not be None")
    if not isinstance(params["output_signal"], str):
        raise StyxValidationError(f'`output_signal` has the wrong type: Received `{type(params["output_signal"])}` expected `str`')
    if params.get("num_processors", None) is not None:
        if not isinstance(params["num_processors"], int):
            raise StyxValidationError(f'`num_processors` has the wrong type: Received `{type(params["num_processors"])}` expected `int`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def possum_sum_cargs(params: PossumSum, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("possum_sum")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_signal"]))
    cargs.append("-o")
    cargs.append(params["output_signal"])
    v_0 = params.get("num_processors")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    if params.get("verbose_flag"):
        cargs.append("-v")
    return cargs

def possum_sum_outputs(params: PossumSum, execution: Execution) -> PossumSumOutputs:
    """Build the PossumSumOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_signal"])
    return PossumSumOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def possum_sum_execute(params: PossumSum, runner: Runner | None = None) -> PossumSumOutputs:
    """
    possum_sum

    Sum of output signals from multiple possum processors

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    possum_sum_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POSSUM_SUM_METADATA)
    execution.params(params)
    args = possum_sum_cargs(params, execution)
    out = possum_sum_outputs(params, execution)
    execution.run(args)
    return out

def possum_sum(
    input_signal: InputPathType,
    output_signal: str,
    num_processors: int | None = None,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> PossumSumOutputs:
    """
    possum_sum

    Sum of output signals from multiple possum processors

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_signal: Input signal for one processor (possum output matrix)
        output_signal: Output signal: sum of all the processors (possum matrix\
            form)
        num_processors: Number of processors
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = possum_sum_params(
        input_signal=input_signal,
        output_signal=output_signal,
        num_processors=num_processors,
        verbose_flag=verbose_flag,
    )
    return possum_sum_execute(params, runner)

__all__ = [
    "PossumSum",
    "PossumSumOutputs",
    "POSSUM_SUM_METADATA",
    "possum_sum_cargs",
    "possum_sum_outputs",
    "possum_sum_params",
    "possum_sum_execute",
    "possum_sum_validate",
    "possum_sum",
]