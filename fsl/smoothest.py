# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SMOOTHEST_METADATA = Metadata(
    id="smoothest",
    name="smoothest",
    package="fsl",
)

Smoothest = typing.TypedDict(
    "Smoothest",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/smoothest"]],
        "dof": typing.NotRequired[float],
        "residual_fit_image": typing.NotRequired[InputPathType],
        "zstat_image": typing.NotRequired[InputPathType],
        "mask": InputPathType,
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SmoothestOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def smoothest_params(
    mask: InputPathType,
    dof: float | None = None,
    residual_fit_image: InputPathType | None = None,
    zstat_image: InputPathType | None = None,
    verbose_flag: bool = False,
) -> Smoothest:
    """
    Build parameters.

    Args:
        mask: Brain mask volume
        dof: Number of degrees of freedom
        residual_fit_image: Filename of `residual-fit` image (use -d)
        zstat_image: Filename of zstat image (not with -d)
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Smoothest = {
        "@type": "fsl/smoothest",
        "mask": mask,
        "verbose_flag": verbose_flag,
    }
    if dof is not None:
        params["dof"] = dof
    if residual_fit_image is not None:
        params["residual_fit_image"] = residual_fit_image
    if zstat_image is not None:
        params["zstat_image"] = zstat_image
    return params

def smoothest_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Smoothest."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dof", None) is not None:
        if not isinstance(params["dof"], (float, int)):
            raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `float`')
    if params.get("residual_fit_image", None) is not None:
        if not isinstance(params["residual_fit_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`residual_fit_image` has the wrong type: Received `{type(params["residual_fit_image"])}` expected `InputPathType`')
    if params.get("zstat_image", None) is not None:
        if not isinstance(params["zstat_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`zstat_image` has the wrong type: Received `{type(params["zstat_image"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def smoothest_cargs(params: Smoothest, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("smoothest")
    v_0 = params.get("dof")
    if v_0 is not None:
        cargs.append("-d")
        cargs.append(str(v_0))
    v_1 = params.get("residual_fit_image")
    if v_1 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("zstat_image")
    if v_2 is not None:
        cargs.append("-z")
        cargs.append(execution.input_file(v_2))
    cargs.append("-m")
    cargs.append(execution.input_file(params["mask"]))
    if params.get("verbose_flag"):
        cargs.append("-V")
    return cargs

def smoothest_outputs(params: Smoothest, execution: Execution) -> SmoothestOutputs:
    """Build the SmoothestOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SmoothestOutputs(
        root=root_v,
    )

def smoothest_execute(params: Smoothest, runner: Runner | None = None) -> SmoothestOutputs:
    """
    smoothest

    Tool to estimate smoothness of data from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    smoothest_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SMOOTHEST_METADATA)
    execution.params(params)
    args = smoothest_cargs(params, execution)
    out = smoothest_outputs(params, execution)
    execution.run(args)
    return out

def smoothest(
    mask: InputPathType,
    dof: float | None = None,
    residual_fit_image: InputPathType | None = None,
    zstat_image: InputPathType | None = None,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> SmoothestOutputs:
    """
    smoothest

    Tool to estimate smoothness of data from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        mask: Brain mask volume
        dof: Number of degrees of freedom
        residual_fit_image: Filename of `residual-fit` image (use -d)
        zstat_image: Filename of zstat image (not with -d)
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = smoothest_params(
        mask=mask,
        dof=dof,
        residual_fit_image=residual_fit_image,
        zstat_image=zstat_image,
        verbose_flag=verbose_flag,
    )
    return smoothest_execute(params, runner)

__all__ = [
    "Smoothest",
    "SmoothestOutputs",
    "SMOOTHEST_METADATA",
    "smoothest_cargs",
    "smoothest_outputs",
    "smoothest_params",
    "smoothest_execute",
    "smoothest_validate",
    "smoothest",
]