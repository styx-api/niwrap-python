# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MAKEROT_METADATA = Metadata(
    id="makerot",
    name="makerot",
    package="fsl",
)

Makerot = typing.TypedDict(
    "Makerot",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/makerot"]],
        "axis": typing.NotRequired[str],
        "cov": typing.NotRequired[InputPathType],
        "center": typing.NotRequired[str],
        "output_file": typing.NotRequired[str],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
        "theta": float,
    },
)

@dataclasses.dataclass
class MakerotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    matrix_output: OutputPathType | None
    """Output file for the rotation matrix"""

def makerot_params(
    theta: float,
    axis: str | None = None,
    cov: InputPathType | None = None,
    center: str | None = None,
    output_file: str | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Makerot:
    """
    Build parameters.

    Args:
        theta: Angle of rotation (in degrees)
        axis: Unnormalized axis vector (comma separated)
        cov: Image filename used for center of volume
        center: Center of rotation in mm (comma separated)
        output_file: Output filename for matrix
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Makerot = {
        "@type": "fsl/makerot",
        "theta": theta,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if axis is not None:
        params["axis"] = axis
    if cov is not None:
        params["cov"] = cov
    if center is not None:
        params["center"] = center
    if output_file is not None:
        params["output_file"] = output_file
    return params

def makerot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Makerot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("axis", None) is not None:
        if not isinstance(params["axis"], str):
            raise StyxValidationError(f'`axis` has the wrong type: Received `{type(params["axis"])}` expected `str`')
    if params.get("cov", None) is not None:
        if not isinstance(params["cov"], (pathlib.Path, str)):
            raise StyxValidationError(f'`cov` has the wrong type: Received `{type(params["cov"])}` expected `InputPathType`')
    if params.get("center", None) is not None:
        if not isinstance(params["center"], str):
            raise StyxValidationError(f'`center` has the wrong type: Received `{type(params["center"])}` expected `str`')
    if params.get("output_file", None) is not None:
        if not isinstance(params["output_file"], str):
            raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')
    if params.get("theta", None) is None:
        raise StyxValidationError("`theta` must not be None")
    if not isinstance(params["theta"], (float, int)):
        raise StyxValidationError(f'`theta` has the wrong type: Received `{type(params["theta"])}` expected `float`')

def makerot_cargs(params: Makerot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("makerot")
    v_0 = params.get("axis")
    if v_0 is not None:
        cargs.append("--axis")
        cargs.append(v_0)
    v_1 = params.get("cov")
    if v_1 is not None:
        cargs.append("--cov")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("center")
    if v_2 is not None:
        cargs.append("--centre")
        cargs.append(v_2)
    v_3 = params.get("output_file")
    if v_3 is not None:
        cargs.append("--out")
        cargs.append(v_3)
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    if params.get("help_flag"):
        cargs.append("--help")
    cargs.append("--theta")
    cargs.append(str(params["theta"]))
    return cargs

def makerot_outputs(params: Makerot, execution: Execution) -> MakerotOutputs:
    """Build the MakerotOutputs object for this tool."""
    matrix_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_file")
    if __v0 is not None:
        matrix_output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MakerotOutputs(
        root=root_v,
        matrix_output=matrix_output_v,
    )

def makerot_execute(params: Makerot, runner: Runner | None = None) -> MakerotOutputs:
    """
    makerot

    Tool to create a rotation matrix for a given angle and axis of rotation.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    makerot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MAKEROT_METADATA)
    execution.params(params)
    args = makerot_cargs(params, execution)
    out = makerot_outputs(params, execution)
    execution.run(args)
    return out

def makerot(
    theta: float,
    axis: str | None = None,
    cov: InputPathType | None = None,
    center: str | None = None,
    output_file: str | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> MakerotOutputs:
    """
    makerot

    Tool to create a rotation matrix for a given angle and axis of rotation.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        theta: Angle of rotation (in degrees)
        axis: Unnormalized axis vector (comma separated)
        cov: Image filename used for center of volume
        center: Center of rotation in mm (comma separated)
        output_file: Output filename for matrix
        verbose_flag: Switch on diagnostic messages
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = makerot_params(
        theta=theta,
        axis=axis,
        cov=cov,
        center=center,
        output_file=output_file,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return makerot_execute(params, runner)

__all__ = [
    "Makerot",
    "MakerotOutputs",
    "MAKEROT_METADATA",
    "makerot_cargs",
    "makerot_outputs",
    "makerot_params",
    "makerot_execute",
    "makerot_validate",
    "makerot",
]