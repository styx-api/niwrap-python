# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMLN_METADATA = Metadata(
    id="imln",
    name="imln",
    package="fsl",
)

Imln = typing.TypedDict(
    "Imln",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/imln"]],
        "input_file": InputPathType,
        "link_name": str,
    },
)

@dataclasses.dataclass
class ImlnOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_link: OutputPathType
    """The created link to the input file"""

def imln_params(
    input_file: InputPathType,
    link_name: str,
) -> Imln:
    """
    Build parameters.

    Args:
        input_file: The source file (file1) to create a link to
        link_name: The name for the link (file2)

    Returns:
        Parameter dictionary.
    """
    params: Imln = {
        "@type": "fsl/imln",
        "input_file": input_file,
        "link_name": link_name,
    }
    return params

def imln_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imln."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("link_name", None) is None:
        raise StyxValidationError("`link_name` must not be None")
    if not isinstance(params["link_name"], str):
        raise StyxValidationError(f'`link_name` has the wrong type: Received `{type(params["link_name"])}` expected `str`')

def imln_cargs(params: Imln, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imln")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["link_name"])
    return cargs

def imln_outputs(params: Imln, execution: Execution) -> ImlnOutputs:
    """Build the ImlnOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_link_v: OutputPathType = execution.output_file(params["link_name"])
    return ImlnOutputs(
        root=root_v,
        output_link=output_link_v,
    )

def imln_execute(params: Imln, runner: Runner | None = None) -> ImlnOutputs:
    """
    imln

    Creates a link (called file2) to file1

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imln_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMLN_METADATA)
    execution.params(params)
    args = imln_cargs(params, execution)
    out = imln_outputs(params, execution)
    execution.run(args)
    return out

def imln(
    input_file: InputPathType,
    link_name: str,
    runner: Runner | None = None,
) -> ImlnOutputs:
    """
    imln

    Creates a link (called file2) to file1

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: The source file (file1) to create a link to
        link_name: The name for the link (file2)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imln_params(
        input_file=input_file,
        link_name=link_name,
    )
    return imln_execute(params, runner)

__all__ = [
    "Imln",
    "ImlnOutputs",
    "IMLN_METADATA",
    "imln_cargs",
    "imln_outputs",
    "imln_params",
    "imln_execute",
    "imln_validate",
    "imln",
]