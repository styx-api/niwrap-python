# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

OVERLAY_METADATA = Metadata(
    id="overlay",
    name="overlay",
    package="fsl",
)

OverlayOutType = typing.Literal["float"] | typing.Literal["int"]

OverlayOutputType = typing.Literal["NIFTI"] | typing.Literal["NIFTI_PAIR"] | typing.Literal["NIFTI_GZ"] | typing.Literal["NIFTI_PAIR_GZ"]

Overlay = typing.TypedDict(
    "Overlay",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/overlay"]],
        "auto_thresh_bg": typing.NotRequired[bool],
        "background_image": InputPathType,
        "bg_thresh": list[float],
        "full_bg_range": typing.NotRequired[bool],
        "out_file": typing.NotRequired[str],
        "out_type": typing.NotRequired[OverlayOutType],
        "output_type": typing.NotRequired[OverlayOutputType],
        "stat_image": InputPathType,
        "stat_image2": typing.NotRequired[InputPathType],
        "stat_thresh": list[float],
        "stat_thresh2": typing.NotRequired[list[float]],
        "use_checkerboard": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class OverlayOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_file_outfile: OutputPathType | None
    """Combined image volume."""

def overlay_params(
    background_image: InputPathType,
    bg_thresh: list[float],
    stat_image: InputPathType,
    stat_thresh: list[float],
    auto_thresh_bg: bool = False,
    full_bg_range: bool = False,
    out_file: str | None = None,
    out_type: OverlayOutType | None = None,
    output_type: OverlayOutputType | None = None,
    stat_image2: InputPathType | None = None,
    stat_thresh2: list[float] | None = None,
    use_checkerboard: bool = False,
) -> Overlay:
    """
    Build parameters.

    Args:
        background_image: Image to use as background.
        bg_thresh: (a float, a float). Min and max values for background intensity.
        stat_image: Statistical image to overlay in color.
        stat_thresh: (a float, a float). Min and max values for the statistical\
            overlay.
        auto_thresh_bg: Automatically threshold the background image.
        full_bg_range: Use full range of background image.
        out_file: Combined image volume.
        out_type: 'float' or 'int'. Write output with float or int.
        output_type: 'nifti' or 'nifti_pair' or 'nifti_gz' or 'nifti_pair_gz'. Fsl\
            output type.
        stat_image2: Second statistical image to overlay in color.
        stat_thresh2: (a float, a float). Min and max values for second statistical\
            overlay.
        use_checkerboard: Use checkerboard mask for overlay.

    Returns:
        Parameter dictionary.
    """
    params: Overlay = {
        "@type": "fsl/overlay",
        "background_image": background_image,
        "bg_thresh": bg_thresh,
        "stat_image": stat_image,
        "stat_thresh": stat_thresh,
        "auto_thresh_bg": auto_thresh_bg,
        "full_bg_range": full_bg_range,
        "use_checkerboard": use_checkerboard,
    }
    if out_file is not None:
        params["out_file"] = out_file
    if out_type is not None:
        params["out_type"] = out_type
    if output_type is not None:
        params["output_type"] = output_type
    if stat_image2 is not None:
        params["stat_image2"] = stat_image2
    if stat_thresh2 is not None:
        params["stat_thresh2"] = stat_thresh2
    return params

def overlay_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Overlay."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("auto_thresh_bg", None) is not None:
        if not isinstance(params["auto_thresh_bg"], bool):
            raise StyxValidationError(f'`auto_thresh_bg` has the wrong type: Received `{type(params["auto_thresh_bg"])}` expected `bool`')
    if params.get("background_image", None) is None:
        raise StyxValidationError("`background_image` must not be None")
    if not isinstance(params["background_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`background_image` has the wrong type: Received `{type(params["background_image"])}` expected `InputPathType`')
    if params.get("bg_thresh", None) is None:
        raise StyxValidationError("`bg_thresh` must not be None")
    if not isinstance(params["bg_thresh"], list):
        raise StyxValidationError(f'`bg_thresh` has the wrong type: Received `{type(params["bg_thresh"])}` expected `list[float]`')
    if not (2 <= len(params["bg_thresh"]) <= 2):
        raise StyxValidationError("Parameter `bg_thresh` must contain between 2 and 2 elements (inclusive)")
    for e in params["bg_thresh"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`bg_thresh` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("full_bg_range", None) is not None:
        if not isinstance(params["full_bg_range"], bool):
            raise StyxValidationError(f'`full_bg_range` has the wrong type: Received `{type(params["full_bg_range"])}` expected `bool`')
    if params.get("out_file", None) is not None:
        if not isinstance(params["out_file"], str):
            raise StyxValidationError(f'`out_file` has the wrong type: Received `{type(params["out_file"])}` expected `str`')
    if params.get("out_type", None) is not None:
        if not isinstance(params["out_type"], str):
            raise StyxValidationError(f'`out_type` has the wrong type: Received `{type(params["out_type"])}` expected `OverlayOutType`')
        if params["out_type"] not in ["float", "int"]:
            raise StyxValidationError("Parameter `out_type` must be one of [\"float\", \"int\"]")
    if params.get("output_type", None) is not None:
        if not isinstance(params["output_type"], str):
            raise StyxValidationError(f'`output_type` has the wrong type: Received `{type(params["output_type"])}` expected `OverlayOutputType`')
        if params["output_type"] not in ["NIFTI", "NIFTI_PAIR", "NIFTI_GZ", "NIFTI_PAIR_GZ"]:
            raise StyxValidationError("Parameter `output_type` must be one of [\"NIFTI\", \"NIFTI_PAIR\", \"NIFTI_GZ\", \"NIFTI_PAIR_GZ\"]")
    if params.get("stat_image", None) is None:
        raise StyxValidationError("`stat_image` must not be None")
    if not isinstance(params["stat_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`stat_image` has the wrong type: Received `{type(params["stat_image"])}` expected `InputPathType`')
    if params.get("stat_image2", None) is not None:
        if not isinstance(params["stat_image2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`stat_image2` has the wrong type: Received `{type(params["stat_image2"])}` expected `InputPathType`')
    if params.get("stat_thresh", None) is None:
        raise StyxValidationError("`stat_thresh` must not be None")
    if not isinstance(params["stat_thresh"], list):
        raise StyxValidationError(f'`stat_thresh` has the wrong type: Received `{type(params["stat_thresh"])}` expected `list[float]`')
    if not (2 <= len(params["stat_thresh"]) <= 2):
        raise StyxValidationError("Parameter `stat_thresh` must contain between 2 and 2 elements (inclusive)")
    for e_2 in params["stat_thresh"]:
        if not isinstance(e_2, (float, int)):
            raise StyxValidationError(f'`stat_thresh` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("stat_thresh2", None) is not None:
        if not isinstance(params["stat_thresh2"], list):
            raise StyxValidationError(f'`stat_thresh2` has the wrong type: Received `{type(params["stat_thresh2"])}` expected `list[float]`')
        if not (2 <= len(params["stat_thresh2"]) <= 2):
            raise StyxValidationError("Parameter `stat_thresh2` must contain between 2 and 2 elements (inclusive)")
        for e_3 in params["stat_thresh2"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`stat_thresh2` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("use_checkerboard", None) is not None:
        if not isinstance(params["use_checkerboard"], bool):
            raise StyxValidationError(f'`use_checkerboard` has the wrong type: Received `{type(params["use_checkerboard"])}` expected `bool`')

def overlay_cargs(params: Overlay, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("overlay")
    if params.get("auto_thresh_bg"):
        cargs.append("-a")
    cargs.append(execution.input_file(params["background_image"]))
    for item0 in params["bg_thresh"]:
        cargs.append(str(item0))
    if params.get("full_bg_range"):
        cargs.append("-A")
    v_0 = params.get("out_file")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("out_type")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("output_type")
    if v_2 is not None:
        cargs.append(str(v_2))
    cargs.append(execution.input_file(params["stat_image"]))
    v_3 = params.get("stat_image2")
    if v_3 is not None:
        cargs.append(execution.input_file(v_3))
    for item1 in params["stat_thresh"]:
        cargs.append(str(item1))
    v_4 = params.get("stat_thresh2")
    if v_4 is not None:
        for item2 in v_4:
            cargs.append(str(item2))
    if params.get("use_checkerboard"):
        cargs.append("-c")
    return cargs

def overlay_outputs(params: Overlay, execution: Execution) -> OverlayOutputs:
    """Build the OverlayOutputs object for this tool."""
    out_file_outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_file")
    if __v0 is not None:
        out_file_outfile_v = execution.output_file((__v0 if __v0 is not None else ""))
    return OverlayOutputs(
        root=root_v,
        out_file_outfile=out_file_outfile_v,
    )

def overlay_execute(params: Overlay, runner: Runner | None = None) -> OverlayOutputs:
    """
    overlay

    Use FSL's overlay command to combine background and statistical images into one volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    overlay_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(OVERLAY_METADATA)
    execution.params(params)
    args = overlay_cargs(params, execution)
    out = overlay_outputs(params, execution)
    execution.run(args)
    return out

def overlay(
    background_image: InputPathType,
    bg_thresh: list[float],
    stat_image: InputPathType,
    stat_thresh: list[float],
    auto_thresh_bg: bool = False,
    full_bg_range: bool = False,
    out_file: str | None = None,
    out_type: OverlayOutType | None = None,
    output_type: OverlayOutputType | None = None,
    stat_image2: InputPathType | None = None,
    stat_thresh2: list[float] | None = None,
    use_checkerboard: bool = False,
    runner: Runner | None = None,
) -> OverlayOutputs:
    """
    overlay

    Use FSL's overlay command to combine background and statistical images into one volume

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        background_image: Image to use as background.
        bg_thresh: (a float, a float). Min and max values for background intensity.
        stat_image: Statistical image to overlay in color.
        stat_thresh: (a float, a float). Min and max values for the statistical\
            overlay.
        auto_thresh_bg: Automatically threshold the background image.
        full_bg_range: Use full range of background image.
        out_file: Combined image volume.
        out_type: 'float' or 'int'. Write output with float or int.
        output_type: 'nifti' or 'nifti_pair' or 'nifti_gz' or 'nifti_pair_gz'. Fsl\
            output type.
        stat_image2: Second statistical image to overlay in color.
        stat_thresh2: (a float, a float). Min and max values for second statistical\
            overlay.
        use_checkerboard: Use checkerboard mask for overlay.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = overlay_params(
        background_image=background_image,
        bg_thresh=bg_thresh,
        stat_image=stat_image,
        stat_thresh=stat_thresh,
        auto_thresh_bg=auto_thresh_bg,
        full_bg_range=full_bg_range,
        out_file=out_file,
        out_type=out_type,
        output_type=output_type,
        stat_image2=stat_image2,
        stat_thresh2=stat_thresh2,
        use_checkerboard=use_checkerboard,
    )
    return overlay_execute(params, runner)

__all__ = [
    "Overlay",
    "OverlayOutputs",
    "OVERLAY_METADATA",
    "overlay_cargs",
    "overlay_outputs",
    "overlay_params",
    "overlay_execute",
    "overlay_validate",
    "overlay",
]