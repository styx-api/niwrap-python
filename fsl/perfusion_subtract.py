# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PERFUSION_SUBTRACT_METADATA = Metadata(
    id="perfusion_subtract",
    name="perfusion_subtract",
    package="fsl",
)

PerfusionSubtract = typing.TypedDict(
    "PerfusionSubtract",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/perfusion_subtract"]],
        "four_d_input": InputPathType,
        "four_d_output": str,
        "control_first_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PerfusionSubtractOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output 4D image with subtraction results"""

def perfusion_subtract_params(
    four_d_input: InputPathType,
    four_d_output: str,
    control_first_flag: bool = False,
) -> PerfusionSubtract:
    """
    Build parameters.

    Args:
        four_d_input: Input 4D perfusion image (e.g. perfusion.nii.gz)
        four_d_output: Output 4D image with subtraction results (e.g.\
            perfusion_subtracted.nii.gz)
        control_first_flag: First timepoint is control instead of tag. Default is\
            tag first.

    Returns:
        Parameter dictionary.
    """
    params: PerfusionSubtract = {
        "@type": "fsl/perfusion_subtract",
        "four_d_input": four_d_input,
        "four_d_output": four_d_output,
        "control_first_flag": control_first_flag,
    }
    return params

def perfusion_subtract_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PerfusionSubtract."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("four_d_input", None) is None:
        raise StyxValidationError("`four_d_input` must not be None")
    if not isinstance(params["four_d_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`four_d_input` has the wrong type: Received `{type(params["four_d_input"])}` expected `InputPathType`')
    if params.get("four_d_output", None) is None:
        raise StyxValidationError("`four_d_output` must not be None")
    if not isinstance(params["four_d_output"], str):
        raise StyxValidationError(f'`four_d_output` has the wrong type: Received `{type(params["four_d_output"])}` expected `str`')
    if params.get("control_first_flag", None) is not None:
        if not isinstance(params["control_first_flag"], bool):
            raise StyxValidationError(f'`control_first_flag` has the wrong type: Received `{type(params["control_first_flag"])}` expected `bool`')

def perfusion_subtract_cargs(params: PerfusionSubtract, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("perfusion_subtract")
    cargs.append(execution.input_file(params["four_d_input"]))
    cargs.append(params["four_d_output"])
    if params.get("control_first_flag"):
        cargs.append("-c")
    return cargs

def perfusion_subtract_outputs(params: PerfusionSubtract, execution: Execution) -> PerfusionSubtractOutputs:
    """Build the PerfusionSubtractOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(f'{params["four_d_output"]}.nii.gz')
    return PerfusionSubtractOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def perfusion_subtract_execute(params: PerfusionSubtract, runner: Runner | None = None) -> PerfusionSubtractOutputs:
    """
    perfusion_subtract

    Subtract control images from tag images in 4D perfusion data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    perfusion_subtract_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PERFUSION_SUBTRACT_METADATA)
    execution.params(params)
    args = perfusion_subtract_cargs(params, execution)
    out = perfusion_subtract_outputs(params, execution)
    execution.run(args)
    return out

def perfusion_subtract(
    four_d_input: InputPathType,
    four_d_output: str,
    control_first_flag: bool = False,
    runner: Runner | None = None,
) -> PerfusionSubtractOutputs:
    """
    perfusion_subtract

    Subtract control images from tag images in 4D perfusion data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        four_d_input: Input 4D perfusion image (e.g. perfusion.nii.gz)
        four_d_output: Output 4D image with subtraction results (e.g.\
            perfusion_subtracted.nii.gz)
        control_first_flag: First timepoint is control instead of tag. Default is\
            tag first.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = perfusion_subtract_params(
        four_d_input=four_d_input,
        four_d_output=four_d_output,
        control_first_flag=control_first_flag,
    )
    return perfusion_subtract_execute(params, runner)

__all__ = [
    "PerfusionSubtract",
    "PerfusionSubtractOutputs",
    "PERFUSION_SUBTRACT_METADATA",
    "perfusion_subtract_cargs",
    "perfusion_subtract_outputs",
    "perfusion_subtract_params",
    "perfusion_subtract_execute",
    "perfusion_subtract_validate",
    "perfusion_subtract",
]