# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIRST_MULT_BCORR_METADATA = Metadata(
    id="first_mult_bcorr",
    name="first_mult_bcorr",
    package="fsl",
)

FirstMultBcorr = typing.TypedDict(
    "FirstMultBcorr",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/first_mult_bcorr"]],
        "input_image": InputPathType,
        "corrected_4d_labels": InputPathType,
        "uncorrected_4d_labels": InputPathType,
        "output_image": str,
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FirstMultBcorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output image name (3D label image)"""

def first_mult_bcorr_params(
    input_image: InputPathType,
    corrected_4d_labels: InputPathType,
    uncorrected_4d_labels: InputPathType,
    output_image: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> FirstMultBcorr:
    """
    Build parameters.

    Args:
        input_image: Filename of original T1 input image
        corrected_4d_labels: Filename of 4D image of individually corrected labels
        uncorrected_4d_labels: Filename of 4D image of uncorrected labels (with\
            boundaries)
        output_image: Output image name (3D label image)
        verbose_flag: Output F-stats to standard out
        help_flag: Display this help message

    Returns:
        Parameter dictionary.
    """
    params: FirstMultBcorr = {
        "@type": "fsl/first_mult_bcorr",
        "input_image": input_image,
        "corrected_4d_labels": corrected_4d_labels,
        "uncorrected_4d_labels": uncorrected_4d_labels,
        "output_image": output_image,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    return params

def first_mult_bcorr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FirstMultBcorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("corrected_4d_labels", None) is None:
        raise StyxValidationError("`corrected_4d_labels` must not be None")
    if not isinstance(params["corrected_4d_labels"], (pathlib.Path, str)):
        raise StyxValidationError(f'`corrected_4d_labels` has the wrong type: Received `{type(params["corrected_4d_labels"])}` expected `InputPathType`')
    if params.get("uncorrected_4d_labels", None) is None:
        raise StyxValidationError("`uncorrected_4d_labels` must not be None")
    if not isinstance(params["uncorrected_4d_labels"], (pathlib.Path, str)):
        raise StyxValidationError(f'`uncorrected_4d_labels` has the wrong type: Received `{type(params["uncorrected_4d_labels"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def first_mult_bcorr_cargs(params: FirstMultBcorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("first_mult_bcorr")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("-c")
    cargs.append(execution.input_file(params["corrected_4d_labels"]))
    cargs.append("-u")
    cargs.append(execution.input_file(params["uncorrected_4d_labels"]))
    cargs.append("-o")
    cargs.append(params["output_image"])
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def first_mult_bcorr_outputs(params: FirstMultBcorr, execution: Execution) -> FirstMultBcorrOutputs:
    """Build the FirstMultBcorrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_image"])
    return FirstMultBcorrOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def first_mult_bcorr_execute(params: FirstMultBcorr, runner: Runner | None = None) -> FirstMultBcorrOutputs:
    """
    first_mult_bcorr

    Part of FSL (ID: 6.0.5:9e026117), first_mult_bcorr converts label images to an output image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    first_mult_bcorr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIRST_MULT_BCORR_METADATA)
    execution.params(params)
    args = first_mult_bcorr_cargs(params, execution)
    out = first_mult_bcorr_outputs(params, execution)
    execution.run(args)
    return out

def first_mult_bcorr(
    input_image: InputPathType,
    corrected_4d_labels: InputPathType,
    uncorrected_4d_labels: InputPathType,
    output_image: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> FirstMultBcorrOutputs:
    """
    first_mult_bcorr

    Part of FSL (ID: 6.0.5:9e026117), first_mult_bcorr converts label images to an output image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Filename of original T1 input image
        corrected_4d_labels: Filename of 4D image of individually corrected labels
        uncorrected_4d_labels: Filename of 4D image of uncorrected labels (with\
            boundaries)
        output_image: Output image name (3D label image)
        verbose_flag: Output F-stats to standard out
        help_flag: Display this help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = first_mult_bcorr_params(
        input_image=input_image,
        corrected_4d_labels=corrected_4d_labels,
        uncorrected_4d_labels=uncorrected_4d_labels,
        output_image=output_image,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return first_mult_bcorr_execute(params, runner)

__all__ = [
    "FirstMultBcorr",
    "FirstMultBcorrOutputs",
    "FIRST_MULT_BCORR_METADATA",
    "first_mult_bcorr_cargs",
    "first_mult_bcorr_outputs",
    "first_mult_bcorr_params",
    "first_mult_bcorr_execute",
    "first_mult_bcorr_validate",
    "first_mult_bcorr",
]