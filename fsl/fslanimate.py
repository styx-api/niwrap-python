# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLANIMATE_METADATA = Metadata(
    id="fslanimate",
    name="fslanimate",
    package="fsl",
)

Fslanimate = typing.TypedDict(
    "Fslanimate",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslanimate"]],
        "input_file": InputPathType,
        "output_file": str,
        "tmp_dir": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FslanimateOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_animation: OutputPathType
    """The resulting animation file"""

def fslanimate_params(
    input_file: InputPathType,
    output_file: str,
    tmp_dir: str | None = None,
) -> Fslanimate:
    """
    Build parameters.

    Args:
        input_file: Input image file (e.g., input.nii.gz)
        output_file: Output file (e.g., output.gif)
        tmp_dir: Temporary directory for intermediate files

    Returns:
        Parameter dictionary.
    """
    params: Fslanimate = {
        "@type": "fsl/fslanimate",
        "input_file": input_file,
        "output_file": output_file,
    }
    if tmp_dir is not None:
        params["tmp_dir"] = tmp_dir
    return params

def fslanimate_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslanimate."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("tmp_dir", None) is not None:
        if not isinstance(params["tmp_dir"], str):
            raise StyxValidationError(f'`tmp_dir` has the wrong type: Received `{type(params["tmp_dir"])}` expected `str`')

def fslanimate_cargs(params: Fslanimate, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslanimate")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    v_0 = params.get("tmp_dir")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fslanimate_outputs(params: Fslanimate, execution: Execution) -> FslanimateOutputs:
    """Build the FslanimateOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_animation_v: OutputPathType = execution.output_file(params["output_file"])
    return FslanimateOutputs(
        root=root_v,
        output_animation=output_animation_v,
    )

def fslanimate_execute(params: Fslanimate, runner: Runner | None = None) -> FslanimateOutputs:
    """
    fslanimate

    Tool for creating animations from imaging data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslanimate_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLANIMATE_METADATA)
    execution.params(params)
    args = fslanimate_cargs(params, execution)
    out = fslanimate_outputs(params, execution)
    execution.run(args)
    return out

def fslanimate(
    input_file: InputPathType,
    output_file: str,
    tmp_dir: str | None = None,
    runner: Runner | None = None,
) -> FslanimateOutputs:
    """
    fslanimate

    Tool for creating animations from imaging data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input image file (e.g., input.nii.gz)
        output_file: Output file (e.g., output.gif)
        tmp_dir: Temporary directory for intermediate files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslanimate_params(
        input_file=input_file,
        output_file=output_file,
        tmp_dir=tmp_dir,
    )
    return fslanimate_execute(params, runner)

__all__ = [
    "Fslanimate",
    "FslanimateOutputs",
    "FSLANIMATE_METADATA",
    "fslanimate_cargs",
    "fslanimate_outputs",
    "fslanimate_params",
    "fslanimate_execute",
    "fslanimate_validate",
    "fslanimate",
]