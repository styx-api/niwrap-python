# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FAST_METADATA = Metadata(
    id="fast",
    name="fast",
    package="fsl",
)

FastImgType = typing.Literal[1] | typing.Literal[2] | typing.Literal[3]

Fast = typing.TypedDict(
    "Fast",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fast"]],
        "number_classes": typing.NotRequired[int],
        "bias_iters": typing.NotRequired[int],
        "bias_lowpass": typing.NotRequired[float],
        "img_type": typing.NotRequired[FastImgType],
        "init_seg_smooth": typing.NotRequired[float],
        "segments": typing.NotRequired[bool],
        "init_transform": typing.NotRequired[InputPathType],
        "other_priors": typing.NotRequired[list[InputPathType]],
        "output_biasfield": typing.NotRequired[bool],
        "output_biascorrected": typing.NotRequired[bool],
        "no_bias": typing.NotRequired[bool],
        "channels": typing.NotRequired[int],
        "out_basename": typing.NotRequired[str],
        "use_priors": typing.NotRequired[bool],
        "no_pve": typing.NotRequired[bool],
        "segment_iters": typing.NotRequired[int],
        "mixel_smooth": typing.NotRequired[float],
        "hyper": typing.NotRequired[float],
        "verbose": typing.NotRequired[bool],
        "manual_seg": typing.NotRequired[InputPathType],
        "iters_afterbias": typing.NotRequired[int],
        "output_probabilities": typing.NotRequired[bool],
        "in_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class FastOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tissue_class_map: OutputPathType | None
    """Path/name of binary segmented volume file one val for each class _seg."""
    partial_volume_map: OutputPathType | None
    """Path/name of partial volume file _pveseg."""
    mixeltype: OutputPathType | None
    """Path/name of mixeltype volume file _mixeltype."""
    bias_field: OutputPathType | None
    """Estimated bias field (if -b flag is set)."""
    restored_image: OutputPathType | None
    """Bias-corrected image (if -B flag is set)."""

def fast_params(
    in_files: list[InputPathType],
    number_classes: int | None = None,
    bias_iters: int | None = None,
    bias_lowpass: float | None = None,
    img_type: FastImgType | None = None,
    init_seg_smooth: float | None = None,
    segments: bool = False,
    init_transform: InputPathType | None = None,
    other_priors: list[InputPathType] | None = None,
    output_biasfield: bool = False,
    output_biascorrected: bool = False,
    no_bias: bool = False,
    channels: int | None = None,
    out_basename: str | None = None,
    use_priors: bool = False,
    no_pve: bool = False,
    segment_iters: int | None = None,
    mixel_smooth: float | None = None,
    hyper: float | None = None,
    verbose: bool = False,
    manual_seg: InputPathType | None = None,
    iters_afterbias: int | None = None,
    output_probabilities: bool = False,
) -> Fast:
    """
    Build parameters.

    Args:
        in_files: Image, or multi-channel set of images, to be segmented.
        number_classes: number of tissue-type classes; default=3
        bias_iters: number of main-loop iterations during bias-field removal;\
            default=4
        bias_lowpass: bias field smoothing extent (FWHM) in mm; default=20
        img_type: type of image 1=T1, 2=T2, 3=PD; default=T1
        init_seg_smooth: initial segmentation spatial smoothness (during bias field\
            estimation); default=0.02
        segments: outputs a separate binary image for each tissue type
        init_transform: initialise using priors; you must supply a FLIRT transform
        other_priors: Alternative prior images (requires 3 files: prior1 prior2\
            prior3).
        output_biasfield: Output estimated bias field.
        output_biascorrected: Output restored image (bias-corrected image).
        no_bias: Do not remove bias field.
        channels: number of input images (channels); default 1
        out_basename: Base name of output files.
        use_priors: Use priors throughout (requires -a option).
        no_pve: Turn off pve (partial volume estimation).
        segment_iters: number of segmentation-initialisation iterations; default=15
        mixel_smooth: spatial smoothness for mixeltype; default=0.3
        hyper: segmentation spatial smoothness (0.0 to 1.0); default=0.1
        verbose: Switch on diagnostic messages.
        manual_seg: Filename containing intensities.
        iters_afterbias: number of main-loop iterations after bias-field removal;\
            default=4
        output_probabilities: outputs individual probability maps

    Returns:
        Parameter dictionary.
    """
    params: Fast = {
        "@type": "fsl/fast",
        "in_files": in_files,
        "segments": segments,
        "output_biasfield": output_biasfield,
        "output_biascorrected": output_biascorrected,
        "no_bias": no_bias,
        "use_priors": use_priors,
        "no_pve": no_pve,
        "verbose": verbose,
        "output_probabilities": output_probabilities,
    }
    if number_classes is not None:
        params["number_classes"] = number_classes
    if bias_iters is not None:
        params["bias_iters"] = bias_iters
    if bias_lowpass is not None:
        params["bias_lowpass"] = bias_lowpass
    if img_type is not None:
        params["img_type"] = img_type
    if init_seg_smooth is not None:
        params["init_seg_smooth"] = init_seg_smooth
    if init_transform is not None:
        params["init_transform"] = init_transform
    if other_priors is not None:
        params["other_priors"] = other_priors
    if channels is not None:
        params["channels"] = channels
    if out_basename is not None:
        params["out_basename"] = out_basename
    if segment_iters is not None:
        params["segment_iters"] = segment_iters
    if mixel_smooth is not None:
        params["mixel_smooth"] = mixel_smooth
    if hyper is not None:
        params["hyper"] = hyper
    if manual_seg is not None:
        params["manual_seg"] = manual_seg
    if iters_afterbias is not None:
        params["iters_afterbias"] = iters_afterbias
    return params

def fast_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fast."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("number_classes", None) is not None:
        if not isinstance(params["number_classes"], int):
            raise StyxValidationError(f'`number_classes` has the wrong type: Received `{type(params["number_classes"])}` expected `int`')
        if params["number_classes"] < 1:
            raise StyxValidationError("Parameter `number_classes` must be at least 1")
    if params.get("bias_iters", None) is not None:
        if not isinstance(params["bias_iters"], int):
            raise StyxValidationError(f'`bias_iters` has the wrong type: Received `{type(params["bias_iters"])}` expected `int`')
        if params["bias_iters"] < 1:
            raise StyxValidationError("Parameter `bias_iters` must be at least 1")
    if params.get("bias_lowpass", None) is not None:
        if not isinstance(params["bias_lowpass"], (float, int)):
            raise StyxValidationError(f'`bias_lowpass` has the wrong type: Received `{type(params["bias_lowpass"])}` expected `float`')
        if params["bias_lowpass"] < 0:
            raise StyxValidationError("Parameter `bias_lowpass` must be at least 0")
    if params.get("img_type", None) is not None:
        if not isinstance(params["img_type"], (float, int)):
            raise StyxValidationError(f'`img_type` has the wrong type: Received `{type(params["img_type"])}` expected `FastImgType`')
        if params["img_type"] not in [1, 2, 3]:
            raise StyxValidationError("Parameter `img_type` must be one of [1, 2, 3]")
    if params.get("init_seg_smooth", None) is not None:
        if not isinstance(params["init_seg_smooth"], (float, int)):
            raise StyxValidationError(f'`init_seg_smooth` has the wrong type: Received `{type(params["init_seg_smooth"])}` expected `float`')
    if params.get("segments", None) is not None:
        if not isinstance(params["segments"], bool):
            raise StyxValidationError(f'`segments` has the wrong type: Received `{type(params["segments"])}` expected `bool`')
    if params.get("init_transform", None) is not None:
        if not isinstance(params["init_transform"], (pathlib.Path, str)):
            raise StyxValidationError(f'`init_transform` has the wrong type: Received `{type(params["init_transform"])}` expected `InputPathType`')
    if params.get("other_priors", None) is not None:
        if not isinstance(params["other_priors"], list):
            raise StyxValidationError(f'`other_priors` has the wrong type: Received `{type(params["other_priors"])}` expected `list[InputPathType]`')
        if not (3 <= len(params["other_priors"]) <= 3):
            raise StyxValidationError("Parameter `other_priors` must contain between 3 and 3 elements (inclusive)")
        for e in params["other_priors"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`other_priors` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_biasfield", None) is not None:
        if not isinstance(params["output_biasfield"], bool):
            raise StyxValidationError(f'`output_biasfield` has the wrong type: Received `{type(params["output_biasfield"])}` expected `bool`')
    if params.get("output_biascorrected", None) is not None:
        if not isinstance(params["output_biascorrected"], bool):
            raise StyxValidationError(f'`output_biascorrected` has the wrong type: Received `{type(params["output_biascorrected"])}` expected `bool`')
    if params.get("no_bias", None) is not None:
        if not isinstance(params["no_bias"], bool):
            raise StyxValidationError(f'`no_bias` has the wrong type: Received `{type(params["no_bias"])}` expected `bool`')
    if params.get("channels", None) is not None:
        if not isinstance(params["channels"], int):
            raise StyxValidationError(f'`channels` has the wrong type: Received `{type(params["channels"])}` expected `int`')
        if params["channels"] < 1:
            raise StyxValidationError("Parameter `channels` must be at least 1")
    if params.get("out_basename", None) is not None:
        if not isinstance(params["out_basename"], str):
            raise StyxValidationError(f'`out_basename` has the wrong type: Received `{type(params["out_basename"])}` expected `str`')
    if params.get("use_priors", None) is not None:
        if not isinstance(params["use_priors"], bool):
            raise StyxValidationError(f'`use_priors` has the wrong type: Received `{type(params["use_priors"])}` expected `bool`')
    if params.get("no_pve", None) is not None:
        if not isinstance(params["no_pve"], bool):
            raise StyxValidationError(f'`no_pve` has the wrong type: Received `{type(params["no_pve"])}` expected `bool`')
    if params.get("segment_iters", None) is not None:
        if not isinstance(params["segment_iters"], int):
            raise StyxValidationError(f'`segment_iters` has the wrong type: Received `{type(params["segment_iters"])}` expected `int`')
        if params["segment_iters"] < 1:
            raise StyxValidationError("Parameter `segment_iters` must be at least 1")
    if params.get("mixel_smooth", None) is not None:
        if not isinstance(params["mixel_smooth"], (float, int)):
            raise StyxValidationError(f'`mixel_smooth` has the wrong type: Received `{type(params["mixel_smooth"])}` expected `float`')
    if params.get("hyper", None) is not None:
        if not isinstance(params["hyper"], (float, int)):
            raise StyxValidationError(f'`hyper` has the wrong type: Received `{type(params["hyper"])}` expected `float`')
        if not (0 <= params["hyper"] <= 1):
            raise StyxValidationError("Parameter `hyper` must be between 0 and 1 (inclusive)")
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')
    if params.get("manual_seg", None) is not None:
        if not isinstance(params["manual_seg"], (pathlib.Path, str)):
            raise StyxValidationError(f'`manual_seg` has the wrong type: Received `{type(params["manual_seg"])}` expected `InputPathType`')
    if params.get("iters_afterbias", None) is not None:
        if not isinstance(params["iters_afterbias"], int):
            raise StyxValidationError(f'`iters_afterbias` has the wrong type: Received `{type(params["iters_afterbias"])}` expected `int`')
        if params["iters_afterbias"] < 1:
            raise StyxValidationError("Parameter `iters_afterbias` must be at least 1")
    if params.get("output_probabilities", None) is not None:
        if not isinstance(params["output_probabilities"], bool):
            raise StyxValidationError(f'`output_probabilities` has the wrong type: Received `{type(params["output_probabilities"])}` expected `bool`')
    if params.get("in_files", None) is None:
        raise StyxValidationError("`in_files` must not be None")
    if not isinstance(params["in_files"], list):
        raise StyxValidationError(f'`in_files` has the wrong type: Received `{type(params["in_files"])}` expected `list[InputPathType]`')
    for e_2 in params["in_files"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`in_files` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')

def fast_cargs(params: Fast, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fast")
    v_0 = params.get("number_classes")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    v_1 = params.get("bias_iters")
    if v_1 is not None:
        cargs.append("-I")
        cargs.append(str(v_1))
    v_2 = params.get("bias_lowpass")
    if v_2 is not None:
        cargs.append("-l")
        cargs.append(str(v_2))
    v_3 = params.get("img_type")
    if v_3 is not None:
        cargs.append("-t")
        cargs.append(str(v_3))
    v_4 = params.get("init_seg_smooth")
    if v_4 is not None:
        cargs.append("-f")
        cargs.append(str(v_4))
    if params.get("segments"):
        cargs.append("-g")
    v_5 = params.get("init_transform")
    if v_5 is not None:
        cargs.append("-a")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("other_priors")
    if v_6 is not None:
        cargs.append("-A")
        for item0 in v_6:
            cargs.append(execution.input_file(item0))
    if params.get("output_biasfield"):
        cargs.append("-b")
    if params.get("output_biascorrected"):
        cargs.append("-B")
    if params.get("no_bias"):
        cargs.append("-N")
    v_7 = params.get("channels")
    if v_7 is not None:
        cargs.append("-S")
        cargs.append(str(v_7))
    v_8 = params.get("out_basename")
    if v_8 is not None:
        cargs.append("-o")
        cargs.append(v_8)
    if params.get("use_priors"):
        cargs.append("-P")
    if params.get("no_pve"):
        cargs.append("--nopve")
    v_9 = params.get("segment_iters")
    if v_9 is not None:
        cargs.append("-W")
        cargs.append(str(v_9))
    v_10 = params.get("mixel_smooth")
    if v_10 is not None:
        cargs.append("-R")
        cargs.append(str(v_10))
    v_11 = params.get("hyper")
    if v_11 is not None:
        cargs.append("-H")
        cargs.append(str(v_11))
    if params.get("verbose"):
        cargs.append("-v")
    v_12 = params.get("manual_seg")
    if v_12 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("iters_afterbias")
    if v_13 is not None:
        cargs.append("-O")
        cargs.append(str(v_13))
    if params.get("output_probabilities"):
        cargs.append("-p")
    for item1 in params["in_files"]:
        cargs.append(execution.input_file(item1))
    return cargs

def fast_outputs(params: Fast, execution: Execution) -> FastOutputs:
    """Build the FastOutputs object for this tool."""
    tissue_class_map_v: OutputPathType | None = None
    partial_volume_map_v: OutputPathType | None = None
    mixeltype_v: OutputPathType | None = None
    bias_field_v: OutputPathType | None = None
    restored_image_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_basename")
    if __v0 is not None:
        tissue_class_map_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}_seg.nii.gz')
    __v1 = params.get("out_basename")
    if __v1 is not None:
        partial_volume_map_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}_pveseg.nii.gz')
    __v2 = params.get("out_basename")
    if __v2 is not None:
        mixeltype_v = execution.output_file(f'{(__v2 if __v2 is not None else "")}_mixeltype.nii.gz')
    __v3 = params.get("out_basename")
    if __v3 is not None:
        bias_field_v = execution.output_file(f'{(__v3 if __v3 is not None else "")}_bias.nii.gz')
    __v4 = params.get("out_basename")
    if __v4 is not None:
        restored_image_v = execution.output_file(f'{(__v4 if __v4 is not None else "")}_restore.nii.gz')
    return FastOutputs(
        root=root_v,
        tissue_class_map=tissue_class_map_v,
        partial_volume_map=partial_volume_map_v,
        mixeltype=mixeltype_v,
        bias_field=bias_field_v,
        restored_image=restored_image_v,
    )

def fast_execute(params: Fast, runner: Runner | None = None) -> FastOutputs:
    """
    fast

    FAST (FMRIB's Automated Segmentation Tool) segments a 3D image of the brain into different tissue types (Grey Matter, White Matter, CSF, etc.), whilst also correcting for spatial intensity variations (also known as bias field or RF inhomogeneities). The underlying method is based on a hidden Markov random field model and an associated Expectation-Maximization algorithm. The whole process is fully automated and can also produce a bias field-corrected input image and a probabilistic and/or partial volume tissue segmentation. It is robust and reliable, compared to most finite mixture model-based methods, which are sensitive to noise.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fast_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FAST_METADATA)
    execution.params(params)
    args = fast_cargs(params, execution)
    out = fast_outputs(params, execution)
    execution.run(args)
    return out

def fast(
    in_files: list[InputPathType],
    number_classes: int | None = None,
    bias_iters: int | None = None,
    bias_lowpass: float | None = None,
    img_type: FastImgType | None = None,
    init_seg_smooth: float | None = None,
    segments: bool = False,
    init_transform: InputPathType | None = None,
    other_priors: list[InputPathType] | None = None,
    output_biasfield: bool = False,
    output_biascorrected: bool = False,
    no_bias: bool = False,
    channels: int | None = None,
    out_basename: str | None = None,
    use_priors: bool = False,
    no_pve: bool = False,
    segment_iters: int | None = None,
    mixel_smooth: float | None = None,
    hyper: float | None = None,
    verbose: bool = False,
    manual_seg: InputPathType | None = None,
    iters_afterbias: int | None = None,
    output_probabilities: bool = False,
    runner: Runner | None = None,
) -> FastOutputs:
    """
    fast

    FAST (FMRIB's Automated Segmentation Tool) segments a 3D image of the brain into different tissue types (Grey Matter, White Matter, CSF, etc.), whilst also correcting for spatial intensity variations (also known as bias field or RF inhomogeneities). The underlying method is based on a hidden Markov random field model and an associated Expectation-Maximization algorithm. The whole process is fully automated and can also produce a bias field-corrected input image and a probabilistic and/or partial volume tissue segmentation. It is robust and reliable, compared to most finite mixture model-based methods, which are sensitive to noise.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        in_files: Image, or multi-channel set of images, to be segmented.
        number_classes: number of tissue-type classes; default=3
        bias_iters: number of main-loop iterations during bias-field removal;\
            default=4
        bias_lowpass: bias field smoothing extent (FWHM) in mm; default=20
        img_type: type of image 1=T1, 2=T2, 3=PD; default=T1
        init_seg_smooth: initial segmentation spatial smoothness (during bias field\
            estimation); default=0.02
        segments: outputs a separate binary image for each tissue type
        init_transform: initialise using priors; you must supply a FLIRT transform
        other_priors: Alternative prior images (requires 3 files: prior1 prior2\
            prior3).
        output_biasfield: Output estimated bias field.
        output_biascorrected: Output restored image (bias-corrected image).
        no_bias: Do not remove bias field.
        channels: number of input images (channels); default 1
        out_basename: Base name of output files.
        use_priors: Use priors throughout (requires -a option).
        no_pve: Turn off pve (partial volume estimation).
        segment_iters: number of segmentation-initialisation iterations; default=15
        mixel_smooth: spatial smoothness for mixeltype; default=0.3
        hyper: segmentation spatial smoothness (0.0 to 1.0); default=0.1
        verbose: Switch on diagnostic messages.
        manual_seg: Filename containing intensities.
        iters_afterbias: number of main-loop iterations after bias-field removal;\
            default=4
        output_probabilities: outputs individual probability maps
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fast_params(
        in_files=in_files,
        number_classes=number_classes,
        bias_iters=bias_iters,
        bias_lowpass=bias_lowpass,
        img_type=img_type,
        init_seg_smooth=init_seg_smooth,
        segments=segments,
        init_transform=init_transform,
        other_priors=other_priors,
        output_biasfield=output_biasfield,
        output_biascorrected=output_biascorrected,
        no_bias=no_bias,
        channels=channels,
        out_basename=out_basename,
        use_priors=use_priors,
        no_pve=no_pve,
        segment_iters=segment_iters,
        mixel_smooth=mixel_smooth,
        hyper=hyper,
        verbose=verbose,
        manual_seg=manual_seg,
        iters_afterbias=iters_afterbias,
        output_probabilities=output_probabilities,
    )
    return fast_execute(params, runner)

__all__ = [
    "Fast",
    "FastOutputs",
    "FAST_METADATA",
    "fast_cargs",
    "fast_outputs",
    "fast_params",
    "fast_execute",
    "fast_validate",
    "fast",
]