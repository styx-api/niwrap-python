# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BEDPOSTX_METADATA = Metadata(
    id="bedpostx",
    name="bedpostx",
    package="fsl",
)

Bedpostx = typing.TypedDict(
    "Bedpostx",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/bedpostx"]],
        "subject_dir": str,
        "num_fibres": typing.NotRequired[float],
        "ard_weight": typing.NotRequired[float],
        "burnin": typing.NotRequired[float],
        "num_jumps": typing.NotRequired[float],
        "sample_every": typing.NotRequired[float],
        "model_type": typing.NotRequired[float],
        "grad_nonlinear": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BedpostxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    xfms_output: OutputPathType
    """Output transformations directory."""
    diff_slices_output: OutputPathType
    """Output diffusion slices directory."""

def bedpostx_params(
    subject_dir: str,
    num_fibres: float | None = None,
    ard_weight: float | None = None,
    burnin: float | None = None,
    num_jumps: float | None = None,
    sample_every: float | None = None,
    model_type: float | None = None,
    grad_nonlinear: bool = False,
) -> Bedpostx:
    """
    Build parameters.

    Args:
        subject_dir: Input subject directory which contains bvals, bvecs, data, and\
            nodif_brain_mask files.
        num_fibres: Number of fibres per voxel (default 3).
        ard_weight: ARD weight, more weight means less secondary fibres per voxel\
            (default 1).
        burnin: Burnin period (default 1000).
        num_jumps: Number of jumps (default 1250).
        sample_every: Sample every (default 25).
        model_type: Deconvolution model. 1: with sticks, 2: with sticks with a\
            range of diffusivities (default), 3: with zeppelins.
        grad_nonlinear: Consider gradient nonlinearities, expects grad_dev in the\
            subject directory.

    Returns:
        Parameter dictionary.
    """
    params: Bedpostx = {
        "@type": "fsl/bedpostx",
        "subject_dir": subject_dir,
        "grad_nonlinear": grad_nonlinear,
    }
    if num_fibres is not None:
        params["num_fibres"] = num_fibres
    if ard_weight is not None:
        params["ard_weight"] = ard_weight
    if burnin is not None:
        params["burnin"] = burnin
    if num_jumps is not None:
        params["num_jumps"] = num_jumps
    if sample_every is not None:
        params["sample_every"] = sample_every
    if model_type is not None:
        params["model_type"] = model_type
    return params

def bedpostx_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Bedpostx."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("num_fibres", None) is not None:
        if not isinstance(params["num_fibres"], (float, int)):
            raise StyxValidationError(f'`num_fibres` has the wrong type: Received `{type(params["num_fibres"])}` expected `float`')
    if params.get("ard_weight", None) is not None:
        if not isinstance(params["ard_weight"], (float, int)):
            raise StyxValidationError(f'`ard_weight` has the wrong type: Received `{type(params["ard_weight"])}` expected `float`')
    if params.get("burnin", None) is not None:
        if not isinstance(params["burnin"], (float, int)):
            raise StyxValidationError(f'`burnin` has the wrong type: Received `{type(params["burnin"])}` expected `float`')
    if params.get("num_jumps", None) is not None:
        if not isinstance(params["num_jumps"], (float, int)):
            raise StyxValidationError(f'`num_jumps` has the wrong type: Received `{type(params["num_jumps"])}` expected `float`')
    if params.get("sample_every", None) is not None:
        if not isinstance(params["sample_every"], (float, int)):
            raise StyxValidationError(f'`sample_every` has the wrong type: Received `{type(params["sample_every"])}` expected `float`')
    if params.get("model_type", None) is not None:
        if not isinstance(params["model_type"], (float, int)):
            raise StyxValidationError(f'`model_type` has the wrong type: Received `{type(params["model_type"])}` expected `float`')
    if params.get("grad_nonlinear", None) is not None:
        if not isinstance(params["grad_nonlinear"], bool):
            raise StyxValidationError(f'`grad_nonlinear` has the wrong type: Received `{type(params["grad_nonlinear"])}` expected `bool`')

def bedpostx_cargs(params: Bedpostx, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bedpostx")
    cargs.append(params["subject_dir"])
    v_0 = params.get("num_fibres")
    if v_0 is not None:
        cargs.append("-n")
        cargs.append(str(v_0))
    v_1 = params.get("ard_weight")
    if v_1 is not None:
        cargs.append("-w")
        cargs.append(str(v_1))
    v_2 = params.get("burnin")
    if v_2 is not None:
        cargs.append("-b")
        cargs.append(str(v_2))
    v_3 = params.get("num_jumps")
    if v_3 is not None:
        cargs.append("-j")
        cargs.append(str(v_3))
    v_4 = params.get("sample_every")
    if v_4 is not None:
        cargs.append("-s")
        cargs.append(str(v_4))
    v_5 = params.get("model_type")
    if v_5 is not None:
        cargs.append("-model")
        cargs.append(str(v_5))
    if params.get("grad_nonlinear"):
        cargs.append("-g")
    return cargs

def bedpostx_outputs(params: Bedpostx, execution: Execution) -> BedpostxOutputs:
    """Build the BedpostxOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    xfms_output_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}_bedpostx/xfms')
    diff_slices_output_v: OutputPathType = execution.output_file(f'{params["subject_dir"]}_bedpostx/diff_slices')
    return BedpostxOutputs(
        root=root_v,
        xfms_output=xfms_output_v,
        diff_slices_output=diff_slices_output_v,
    )

def bedpostx_execute(params: Bedpostx, runner: Runner | None = None) -> BedpostxOutputs:
    """
    bedpostx

    Bayesian Estimation of Diffusion Parameters Obtained using Sampling Techniques (BEDPOST) for modeling multiple fibers per voxel.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bedpostx_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BEDPOSTX_METADATA)
    execution.params(params)
    args = bedpostx_cargs(params, execution)
    out = bedpostx_outputs(params, execution)
    execution.run(args)
    return out

def bedpostx(
    subject_dir: str,
    num_fibres: float | None = None,
    ard_weight: float | None = None,
    burnin: float | None = None,
    num_jumps: float | None = None,
    sample_every: float | None = None,
    model_type: float | None = None,
    grad_nonlinear: bool = False,
    runner: Runner | None = None,
) -> BedpostxOutputs:
    """
    bedpostx

    Bayesian Estimation of Diffusion Parameters Obtained using Sampling Techniques (BEDPOST) for modeling multiple fibers per voxel.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        subject_dir: Input subject directory which contains bvals, bvecs, data, and\
            nodif_brain_mask files.
        num_fibres: Number of fibres per voxel (default 3).
        ard_weight: ARD weight, more weight means less secondary fibres per voxel\
            (default 1).
        burnin: Burnin period (default 1000).
        num_jumps: Number of jumps (default 1250).
        sample_every: Sample every (default 25).
        model_type: Deconvolution model. 1: with sticks, 2: with sticks with a\
            range of diffusivities (default), 3: with zeppelins.
        grad_nonlinear: Consider gradient nonlinearities, expects grad_dev in the\
            subject directory.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bedpostx_params(
        subject_dir=subject_dir,
        num_fibres=num_fibres,
        ard_weight=ard_weight,
        burnin=burnin,
        num_jumps=num_jumps,
        sample_every=sample_every,
        model_type=model_type,
        grad_nonlinear=grad_nonlinear,
    )
    return bedpostx_execute(params, runner)

__all__ = [
    "Bedpostx",
    "BedpostxOutputs",
    "BEDPOSTX_METADATA",
    "bedpostx_cargs",
    "bedpostx_outputs",
    "bedpostx_params",
    "bedpostx_execute",
    "bedpostx_validate",
    "bedpostx",
]