# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIENAX_METADATA = Metadata(
    id="sienax",
    name="sienax",
    package="fsl",
)

Sienax = typing.TypedDict(
    "Sienax",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/sienax"]],
        "infile": InputPathType,
        "output_dir": typing.NotRequired[str],
        "debug_flag": typing.NotRequired[bool],
        "bet_options": typing.NotRequired[str],
        "twoclass_segment_flag": typing.NotRequired[bool],
        "t2_flag": typing.NotRequired[bool],
        "top_threshold": typing.NotRequired[float],
        "bottom_threshold": typing.NotRequired[float],
        "regional_flag": typing.NotRequired[bool],
        "lesion_mask": typing.NotRequired[InputPathType],
        "fast_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SienaxOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    segmentation_output: OutputPathType | None
    """Segmentation output file"""
    report_output: OutputPathType | None
    """Summary report file"""

def sienax_params(
    infile: InputPathType,
    output_dir: str | None = None,
    debug_flag: bool = False,
    bet_options: str | None = None,
    twoclass_segment_flag: bool = False,
    t2_flag: bool = False,
    top_threshold: float | None = None,
    bottom_threshold: float | None = None,
    regional_flag: bool = False,
    lesion_mask: InputPathType | None = None,
    fast_options: str | None = None,
) -> Sienax:
    """
    Build parameters.

    Args:
        infile: Input image (e.g. img.nii.gz)
        output_dir: Output directory (default output is <input>_sienax)
        debug_flag: Debug (don't delete intermediate files)
        bet_options: Options to pass to BET brain extraction (inside\
            double-quotes), e.g. -B "-f 0.3"
        twoclass_segment_flag: Two-class segmentation (don't segment grey and white\
            matter separately)
        t2_flag: Input image is T2-weighted (default is T1-weighted)
        top_threshold: Ignore from t (mm) upwards in MNI152/Talairach space
        bottom_threshold: Ignore from b (mm) downwards in MNI152/Talairach space (b\
            should probably be negative)
        regional_flag: Regional - use standard-space masks to give peripheral\
            cortex GM volume (3-class segmentation only) and ventricular CSF volume
        lesion_mask: Use lesion (or lesion+CSF) mask to remove incorrectly labelled\
            'grey matter' voxels
        fast_options: Options to pass to FAST segmentation (inside double-quotes),\
            e.g. -S "I 20"

    Returns:
        Parameter dictionary.
    """
    params: Sienax = {
        "@type": "fsl/sienax",
        "infile": infile,
        "debug_flag": debug_flag,
        "twoclass_segment_flag": twoclass_segment_flag,
        "t2_flag": t2_flag,
        "regional_flag": regional_flag,
    }
    if output_dir is not None:
        params["output_dir"] = output_dir
    if bet_options is not None:
        params["bet_options"] = bet_options
    if top_threshold is not None:
        params["top_threshold"] = top_threshold
    if bottom_threshold is not None:
        params["bottom_threshold"] = bottom_threshold
    if lesion_mask is not None:
        params["lesion_mask"] = lesion_mask
    if fast_options is not None:
        params["fast_options"] = fast_options
    return params

def sienax_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sienax."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("bet_options", None) is not None:
        if not isinstance(params["bet_options"], str):
            raise StyxValidationError(f'`bet_options` has the wrong type: Received `{type(params["bet_options"])}` expected `str`')
    if params.get("twoclass_segment_flag", None) is not None:
        if not isinstance(params["twoclass_segment_flag"], bool):
            raise StyxValidationError(f'`twoclass_segment_flag` has the wrong type: Received `{type(params["twoclass_segment_flag"])}` expected `bool`')
    if params.get("t2_flag", None) is not None:
        if not isinstance(params["t2_flag"], bool):
            raise StyxValidationError(f'`t2_flag` has the wrong type: Received `{type(params["t2_flag"])}` expected `bool`')
    if params.get("top_threshold", None) is not None:
        if not isinstance(params["top_threshold"], (float, int)):
            raise StyxValidationError(f'`top_threshold` has the wrong type: Received `{type(params["top_threshold"])}` expected `float`')
    if params.get("bottom_threshold", None) is not None:
        if not isinstance(params["bottom_threshold"], (float, int)):
            raise StyxValidationError(f'`bottom_threshold` has the wrong type: Received `{type(params["bottom_threshold"])}` expected `float`')
    if params.get("regional_flag", None) is not None:
        if not isinstance(params["regional_flag"], bool):
            raise StyxValidationError(f'`regional_flag` has the wrong type: Received `{type(params["regional_flag"])}` expected `bool`')
    if params.get("lesion_mask", None) is not None:
        if not isinstance(params["lesion_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lesion_mask` has the wrong type: Received `{type(params["lesion_mask"])}` expected `InputPathType`')
    if params.get("fast_options", None) is not None:
        if not isinstance(params["fast_options"], str):
            raise StyxValidationError(f'`fast_options` has the wrong type: Received `{type(params["fast_options"])}` expected `str`')

def sienax_cargs(params: Sienax, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sienax")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("output_dir")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    if params.get("debug_flag"):
        cargs.append("-d")
    v_1 = params.get("bet_options")
    if v_1 is not None:
        cargs.append("-B")
        cargs.append(v_1)
    if params.get("twoclass_segment_flag"):
        cargs.append("-2")
    if params.get("t2_flag"):
        cargs.append("-t2")
    v_2 = params.get("top_threshold")
    if v_2 is not None:
        cargs.append("-t")
        cargs.append(str(v_2))
    v_3 = params.get("bottom_threshold")
    if v_3 is not None:
        cargs.append("-b")
        cargs.append(str(v_3))
    if params.get("regional_flag"):
        cargs.append("-r")
    v_4 = params.get("lesion_mask")
    if v_4 is not None:
        cargs.append("-lm")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("fast_options")
    if v_5 is not None:
        cargs.append("-S")
        cargs.append(v_5)
    return cargs

def sienax_outputs(params: Sienax, execution: Execution) -> SienaxOutputs:
    """Build the SienaxOutputs object for this tool."""
    segmentation_output_v: OutputPathType | None = None
    report_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        segmentation_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/segmentation.nii.gz')
    __v1 = params.get("output_dir")
    if __v1 is not None:
        report_output_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}/report.txt')
    return SienaxOutputs(
        root=root_v,
        segmentation_output=segmentation_output_v,
        report_output=report_output_v,
    )

def sienax_execute(params: Sienax, runner: Runner | None = None) -> SienaxOutputs:
    """
    sienax

    A tool to estimate brain tissue volume from a single MR image and to compare it to an external standard.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sienax_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIENAX_METADATA)
    execution.params(params)
    args = sienax_cargs(params, execution)
    out = sienax_outputs(params, execution)
    execution.run(args)
    return out

def sienax(
    infile: InputPathType,
    output_dir: str | None = None,
    debug_flag: bool = False,
    bet_options: str | None = None,
    twoclass_segment_flag: bool = False,
    t2_flag: bool = False,
    top_threshold: float | None = None,
    bottom_threshold: float | None = None,
    regional_flag: bool = False,
    lesion_mask: InputPathType | None = None,
    fast_options: str | None = None,
    runner: Runner | None = None,
) -> SienaxOutputs:
    """
    sienax

    A tool to estimate brain tissue volume from a single MR image and to compare it to an external standard.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input image (e.g. img.nii.gz)
        output_dir: Output directory (default output is <input>_sienax)
        debug_flag: Debug (don't delete intermediate files)
        bet_options: Options to pass to BET brain extraction (inside\
            double-quotes), e.g. -B "-f 0.3"
        twoclass_segment_flag: Two-class segmentation (don't segment grey and white\
            matter separately)
        t2_flag: Input image is T2-weighted (default is T1-weighted)
        top_threshold: Ignore from t (mm) upwards in MNI152/Talairach space
        bottom_threshold: Ignore from b (mm) downwards in MNI152/Talairach space (b\
            should probably be negative)
        regional_flag: Regional - use standard-space masks to give peripheral\
            cortex GM volume (3-class segmentation only) and ventricular CSF volume
        lesion_mask: Use lesion (or lesion+CSF) mask to remove incorrectly labelled\
            'grey matter' voxels
        fast_options: Options to pass to FAST segmentation (inside double-quotes),\
            e.g. -S "I 20"
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sienax_params(
        infile=infile,
        output_dir=output_dir,
        debug_flag=debug_flag,
        bet_options=bet_options,
        twoclass_segment_flag=twoclass_segment_flag,
        t2_flag=t2_flag,
        top_threshold=top_threshold,
        bottom_threshold=bottom_threshold,
        regional_flag=regional_flag,
        lesion_mask=lesion_mask,
        fast_options=fast_options,
    )
    return sienax_execute(params, runner)

__all__ = [
    "Sienax",
    "SienaxOutputs",
    "SIENAX_METADATA",
    "sienax_cargs",
    "sienax_outputs",
    "sienax_params",
    "sienax_execute",
    "sienax_validate",
    "sienax",
]