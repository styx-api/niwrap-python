# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SLICESDIR_METADATA = Metadata(
    id="slicesdir",
    name="slicesdir",
    package="fsl",
)

Slicesdir = typing.TypedDict(
    "Slicesdir",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/slicesdir"]],
        "flag_filelist": typing.NotRequired[bool],
        "outline_image": typing.NotRequired[InputPathType],
        "edge_threshold": typing.NotRequired[float],
        "slice_option": typing.NotRequired[bool],
        "filelist": list[str],
    },
)

@dataclasses.dataclass
class SlicesdirOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def slicesdir_params(
    filelist: list[str],
    flag_filelist: bool = False,
    outline_image: InputPathType | None = None,
    edge_threshold: float | None = None,
    slice_option: bool = False,
) -> Slicesdir:
    """
    Build parameters.

    Args:
        filelist: List of image files to process
        flag_filelist: Filelist contains pairs of images (underlying and\
            red-outline images)
        outline_image: Use the specified image as the red-outline image on top of\
            all images in the file list
        edge_threshold: Use specified threshold for edges. If >0, use this\
            proportion of max-min; if <0, use the absolute value
        slice_option: Output every second axial slice instead of 9 ortho slices

    Returns:
        Parameter dictionary.
    """
    params: Slicesdir = {
        "@type": "fsl/slicesdir",
        "filelist": filelist,
        "flag_filelist": flag_filelist,
        "slice_option": slice_option,
    }
    if outline_image is not None:
        params["outline_image"] = outline_image
    if edge_threshold is not None:
        params["edge_threshold"] = edge_threshold
    return params

def slicesdir_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Slicesdir."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("flag_filelist", None) is not None:
        if not isinstance(params["flag_filelist"], bool):
            raise StyxValidationError(f'`flag_filelist` has the wrong type: Received `{type(params["flag_filelist"])}` expected `bool`')
    if params.get("outline_image", None) is not None:
        if not isinstance(params["outline_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`outline_image` has the wrong type: Received `{type(params["outline_image"])}` expected `InputPathType`')
    if params.get("edge_threshold", None) is not None:
        if not isinstance(params["edge_threshold"], (float, int)):
            raise StyxValidationError(f'`edge_threshold` has the wrong type: Received `{type(params["edge_threshold"])}` expected `float`')
    if params.get("slice_option", None) is not None:
        if not isinstance(params["slice_option"], bool):
            raise StyxValidationError(f'`slice_option` has the wrong type: Received `{type(params["slice_option"])}` expected `bool`')
    if params.get("filelist", None) is None:
        raise StyxValidationError("`filelist` must not be None")
    if not isinstance(params["filelist"], list):
        raise StyxValidationError(f'`filelist` has the wrong type: Received `{type(params["filelist"])}` expected `list[str]`')
    for e in params["filelist"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`filelist` has the wrong type: Received `{type(e)}` expected `list[str]`')

def slicesdir_cargs(params: Slicesdir, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("slicesdir")
    if params.get("flag_filelist"):
        cargs.append("-o")
    v_0 = params.get("outline_image")
    if v_0 is not None:
        cargs.append("-p")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("edge_threshold")
    if v_1 is not None:
        cargs.append("-e")
        cargs.append(str(v_1))
    if params.get("slice_option"):
        cargs.append("-S")
    for item0 in params["filelist"]:
        cargs.append(item0)
    return cargs

def slicesdir_outputs(params: Slicesdir, execution: Execution) -> SlicesdirOutputs:
    """Build the SlicesdirOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SlicesdirOutputs(
        root=root_v,
    )

def slicesdir_execute(params: Slicesdir, runner: Runner | None = None) -> SlicesdirOutputs:
    """
    slicesdir

    slicesdir generates a directory containing orthogonal slices through a set of images.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    slicesdir_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SLICESDIR_METADATA)
    execution.params(params)
    args = slicesdir_cargs(params, execution)
    out = slicesdir_outputs(params, execution)
    execution.run(args)
    return out

def slicesdir(
    filelist: list[str],
    flag_filelist: bool = False,
    outline_image: InputPathType | None = None,
    edge_threshold: float | None = None,
    slice_option: bool = False,
    runner: Runner | None = None,
) -> SlicesdirOutputs:
    """
    slicesdir

    slicesdir generates a directory containing orthogonal slices through a set of images.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        filelist: List of image files to process
        flag_filelist: Filelist contains pairs of images (underlying and\
            red-outline images)
        outline_image: Use the specified image as the red-outline image on top of\
            all images in the file list
        edge_threshold: Use specified threshold for edges. If >0, use this\
            proportion of max-min; if <0, use the absolute value
        slice_option: Output every second axial slice instead of 9 ortho slices
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = slicesdir_params(
        filelist=filelist,
        flag_filelist=flag_filelist,
        outline_image=outline_image,
        edge_threshold=edge_threshold,
        slice_option=slice_option,
    )
    return slicesdir_execute(params, runner)

__all__ = [
    "Slicesdir",
    "SlicesdirOutputs",
    "SLICESDIR_METADATA",
    "slicesdir_cargs",
    "slicesdir_outputs",
    "slicesdir_params",
    "slicesdir_execute",
    "slicesdir_validate",
    "slicesdir",
]