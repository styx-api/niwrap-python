# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INVFEATREG_METADATA = Metadata(
    id="invfeatreg",
    name="invfeatreg",
    package="fsl",
)

Invfeatreg = typing.TypedDict(
    "Invfeatreg",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/invfeatreg"]],
        "feat_directory": str,
    },
)

@dataclasses.dataclass
class InvfeatregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def invfeatreg_params(
    feat_directory: str,
) -> Invfeatreg:
    """
    Build parameters.

    Args:
        feat_directory: FEAT Directory

    Returns:
        Parameter dictionary.
    """
    params: Invfeatreg = {
        "@type": "fsl/invfeatreg",
        "feat_directory": feat_directory,
    }
    return params

def invfeatreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Invfeatreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_directory", None) is None:
        raise StyxValidationError("`feat_directory` must not be None")
    if not isinstance(params["feat_directory"], str):
        raise StyxValidationError(f'`feat_directory` has the wrong type: Received `{type(params["feat_directory"])}` expected `str`')

def invfeatreg_cargs(params: Invfeatreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("invfeatreg")
    cargs.append(params["feat_directory"])
    return cargs

def invfeatreg_outputs(params: Invfeatreg, execution: Execution) -> InvfeatregOutputs:
    """Build the InvfeatregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return InvfeatregOutputs(
        root=root_v,
    )

def invfeatreg_execute(params: Invfeatreg, runner: Runner | None = None) -> InvfeatregOutputs:
    """
    invfeatreg

    Inverse warp image using FNIRT

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    invfeatreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INVFEATREG_METADATA)
    execution.params(params)
    args = invfeatreg_cargs(params, execution)
    out = invfeatreg_outputs(params, execution)
    execution.run(args)
    return out

def invfeatreg(
    feat_directory: str,
    runner: Runner | None = None,
) -> InvfeatregOutputs:
    """
    invfeatreg

    Inverse warp image using FNIRT

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        feat_directory: FEAT Directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = invfeatreg_params(
        feat_directory=feat_directory,
    )
    return invfeatreg_execute(params, runner)

__all__ = [
    "Invfeatreg",
    "InvfeatregOutputs",
    "INVFEATREG_METADATA",
    "invfeatreg_cargs",
    "invfeatreg_outputs",
    "invfeatreg_params",
    "invfeatreg_execute",
    "invfeatreg_validate",
    "invfeatreg",
]