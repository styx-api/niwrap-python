# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONCAT_BVARS_METADATA = Metadata(
    id="concat_bvars",
    name="concat_bvars",
    package="fsl",
)

ConcatBvars = typing.TypedDict(
    "ConcatBvars",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/concat_bvars"]],
        "output_bvars": str,
        "input_bvars": list[InputPathType],
    },
)

@dataclasses.dataclass
class ConcatBvarsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Concatenated output .bvars file"""

def concat_bvars_params(
    output_bvars: str,
    input_bvars: list[InputPathType],
) -> ConcatBvars:
    """
    Build parameters.

    Args:
        output_bvars: Output .bvars file
        input_bvars: List of input .bvars files

    Returns:
        Parameter dictionary.
    """
    params: ConcatBvars = {
        "@type": "fsl/concat_bvars",
        "output_bvars": output_bvars,
        "input_bvars": input_bvars,
    }
    return params

def concat_bvars_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ConcatBvars."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output_bvars", None) is None:
        raise StyxValidationError("`output_bvars` must not be None")
    if not isinstance(params["output_bvars"], str):
        raise StyxValidationError(f'`output_bvars` has the wrong type: Received `{type(params["output_bvars"])}` expected `str`')
    if params.get("input_bvars", None) is None:
        raise StyxValidationError("`input_bvars` must not be None")
    if not isinstance(params["input_bvars"], list):
        raise StyxValidationError(f'`input_bvars` has the wrong type: Received `{type(params["input_bvars"])}` expected `list[InputPathType]`')
    for e in params["input_bvars"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_bvars` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def concat_bvars_cargs(params: ConcatBvars, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("concat_bvars")
    cargs.append(params["output_bvars"])
    for item0 in params["input_bvars"]:
        cargs.append(execution.input_file(item0))
    return cargs

def concat_bvars_outputs(params: ConcatBvars, execution: Execution) -> ConcatBvarsOutputs:
    """Build the ConcatBvarsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output_bvars"])
    return ConcatBvarsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def concat_bvars_execute(params: ConcatBvars, runner: Runner | None = None) -> ConcatBvarsOutputs:
    """
    concat_bvars

    Concatenate multiple .bvars files into a single .bvars file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    concat_bvars_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONCAT_BVARS_METADATA)
    execution.params(params)
    args = concat_bvars_cargs(params, execution)
    out = concat_bvars_outputs(params, execution)
    execution.run(args)
    return out

def concat_bvars(
    output_bvars: str,
    input_bvars: list[InputPathType],
    runner: Runner | None = None,
) -> ConcatBvarsOutputs:
    """
    concat_bvars

    Concatenate multiple .bvars files into a single .bvars file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        output_bvars: Output .bvars file
        input_bvars: List of input .bvars files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = concat_bvars_params(
        output_bvars=output_bvars,
        input_bvars=input_bvars,
    )
    return concat_bvars_execute(params, runner)

__all__ = [
    "ConcatBvars",
    "ConcatBvarsOutputs",
    "CONCAT_BVARS_METADATA",
    "concat_bvars_cargs",
    "concat_bvars_outputs",
    "concat_bvars_params",
    "concat_bvars_execute",
    "concat_bvars_validate",
    "concat_bvars",
]