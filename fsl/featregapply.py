# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FEATREGAPPLY_METADATA = Metadata(
    id="featregapply",
    name="featregapply",
    package="fsl",
)

Featregapply = typing.TypedDict(
    "Featregapply",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/featregapply"]],
        "feat_directory": str,
        "force_flag": typing.NotRequired[bool],
        "cleanup_flag": typing.NotRequired[bool],
        "upsample_trilinear": typing.NotRequired[InputPathType],
        "upsample_spline": typing.NotRequired[InputPathType],
        "standard_space_res": typing.NotRequired[float],
        "exclude_filtered_func_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FeatregapplyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType
    """Directory where the output files will be stored"""

def featregapply_params(
    feat_directory: str,
    force_flag: bool = False,
    cleanup_flag: bool = False,
    upsample_trilinear: InputPathType | None = None,
    upsample_spline: InputPathType | None = None,
    standard_space_res: float | None = None,
    exclude_filtered_func_flag: bool = False,
) -> Featregapply:
    """
    Build parameters.

    Args:
        feat_directory: FEAT directory from which registration will be taken
        force_flag: Force featregapply to run even if it has already been run on\
            this FEAT directory
        cleanup_flag: Cleanup, i.e. remove all featregapply output
        upsample_trilinear: Upsample functional-space image to standard space using\
            trilinear interpolation
        upsample_spline: Upsample functional-space image to standard space using\
            spline (like sinc) interpolation
        standard_space_res: Specify the standard space resolution for melodic (e.g.\
            3 for 3mm)
        exclude_filtered_func_flag: Exclude filtered func when processing melodic\
            directories (for FEAT directories filtered func is never processed)

    Returns:
        Parameter dictionary.
    """
    params: Featregapply = {
        "@type": "fsl/featregapply",
        "feat_directory": feat_directory,
        "force_flag": force_flag,
        "cleanup_flag": cleanup_flag,
        "exclude_filtered_func_flag": exclude_filtered_func_flag,
    }
    if upsample_trilinear is not None:
        params["upsample_trilinear"] = upsample_trilinear
    if upsample_spline is not None:
        params["upsample_spline"] = upsample_spline
    if standard_space_res is not None:
        params["standard_space_res"] = standard_space_res
    return params

def featregapply_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Featregapply."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("feat_directory", None) is None:
        raise StyxValidationError("`feat_directory` must not be None")
    if not isinstance(params["feat_directory"], str):
        raise StyxValidationError(f'`feat_directory` has the wrong type: Received `{type(params["feat_directory"])}` expected `str`')
    if params.get("force_flag", None) is not None:
        if not isinstance(params["force_flag"], bool):
            raise StyxValidationError(f'`force_flag` has the wrong type: Received `{type(params["force_flag"])}` expected `bool`')
    if params.get("cleanup_flag", None) is not None:
        if not isinstance(params["cleanup_flag"], bool):
            raise StyxValidationError(f'`cleanup_flag` has the wrong type: Received `{type(params["cleanup_flag"])}` expected `bool`')
    if params.get("upsample_trilinear", None) is not None:
        if not isinstance(params["upsample_trilinear"], (pathlib.Path, str)):
            raise StyxValidationError(f'`upsample_trilinear` has the wrong type: Received `{type(params["upsample_trilinear"])}` expected `InputPathType`')
    if params.get("upsample_spline", None) is not None:
        if not isinstance(params["upsample_spline"], (pathlib.Path, str)):
            raise StyxValidationError(f'`upsample_spline` has the wrong type: Received `{type(params["upsample_spline"])}` expected `InputPathType`')
    if params.get("standard_space_res", None) is not None:
        if not isinstance(params["standard_space_res"], (float, int)):
            raise StyxValidationError(f'`standard_space_res` has the wrong type: Received `{type(params["standard_space_res"])}` expected `float`')
    if params.get("exclude_filtered_func_flag", None) is not None:
        if not isinstance(params["exclude_filtered_func_flag"], bool):
            raise StyxValidationError(f'`exclude_filtered_func_flag` has the wrong type: Received `{type(params["exclude_filtered_func_flag"])}` expected `bool`')

def featregapply_cargs(params: Featregapply, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("featregapply")
    cargs.append(params["feat_directory"])
    if params.get("force_flag"):
        cargs.append("-f")
    if params.get("cleanup_flag"):
        cargs.append("-c")
    v_0 = params.get("upsample_trilinear")
    if v_0 is not None:
        cargs.append("-l")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("upsample_spline")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("standard_space_res")
    if v_2 is not None:
        cargs.append("-r")
        cargs.append(str(v_2))
    if params.get("exclude_filtered_func_flag"):
        cargs.append("-e")
    return cargs

def featregapply_outputs(params: Featregapply, execution: Execution) -> FeatregapplyOutputs:
    """Build the FeatregapplyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_directory_v: OutputPathType = execution.output_file(f'{params["feat_directory"]}/reg_standard')
    return FeatregapplyOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def featregapply_execute(params: Featregapply, runner: Runner | None = None) -> FeatregapplyOutputs:
    """
    featregapply

    Apply registration from FEAT analysis to other images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    featregapply_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FEATREGAPPLY_METADATA)
    execution.params(params)
    args = featregapply_cargs(params, execution)
    out = featregapply_outputs(params, execution)
    execution.run(args)
    return out

def featregapply(
    feat_directory: str,
    force_flag: bool = False,
    cleanup_flag: bool = False,
    upsample_trilinear: InputPathType | None = None,
    upsample_spline: InputPathType | None = None,
    standard_space_res: float | None = None,
    exclude_filtered_func_flag: bool = False,
    runner: Runner | None = None,
) -> FeatregapplyOutputs:
    """
    featregapply

    Apply registration from FEAT analysis to other images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        feat_directory: FEAT directory from which registration will be taken
        force_flag: Force featregapply to run even if it has already been run on\
            this FEAT directory
        cleanup_flag: Cleanup, i.e. remove all featregapply output
        upsample_trilinear: Upsample functional-space image to standard space using\
            trilinear interpolation
        upsample_spline: Upsample functional-space image to standard space using\
            spline (like sinc) interpolation
        standard_space_res: Specify the standard space resolution for melodic (e.g.\
            3 for 3mm)
        exclude_filtered_func_flag: Exclude filtered func when processing melodic\
            directories (for FEAT directories filtered func is never processed)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = featregapply_params(
        feat_directory=feat_directory,
        force_flag=force_flag,
        cleanup_flag=cleanup_flag,
        upsample_trilinear=upsample_trilinear,
        upsample_spline=upsample_spline,
        standard_space_res=standard_space_res,
        exclude_filtered_func_flag=exclude_filtered_func_flag,
    )
    return featregapply_execute(params, runner)

__all__ = [
    "Featregapply",
    "FeatregapplyOutputs",
    "FEATREGAPPLY_METADATA",
    "featregapply_cargs",
    "featregapply_outputs",
    "featregapply_params",
    "featregapply_execute",
    "featregapply_validate",
    "featregapply",
]