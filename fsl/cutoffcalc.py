# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CUTOFFCALC_METADATA = Metadata(
    id="cutoffcalc",
    name="cutoffcalc",
    package="fsl",
)

Cutoffcalc = typing.TypedDict(
    "Cutoffcalc",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/cutoffcalc"]],
        "input_design": InputPathType,
        "threshold": typing.NotRequired[float],
        "tr": typing.NotRequired[float],
        "lower_limit": typing.NotRequired[float],
        "example_sigma": typing.NotRequired[float],
        "verbose_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CutoffcalcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    example_output: OutputPathType
    """Filtered output file if example sigma is provided"""

def cutoffcalc_params(
    input_design: InputPathType,
    threshold: float | None = None,
    tr: float | None = None,
    lower_limit: float | None = None,
    example_sigma: float | None = None,
    verbose_flag: bool = False,
    debug_flag: bool = False,
) -> Cutoffcalc:
    """
    Build parameters.

    Args:
        input_design: Input design matrix (e.g. design.mat)
        threshold: Threshold for retained variance (default=0.9)
        tr: Time between successive data points (default=3.0s)
        lower_limit: Lower limit on period due to autocorr estimation (default=90s)
        example_sigma: Example sigma (in sec) to produce output called\
            example_filt.mtx
        verbose_flag: Switch on diagnostic messages
        debug_flag: Switch on debugging messages

    Returns:
        Parameter dictionary.
    """
    params: Cutoffcalc = {
        "@type": "fsl/cutoffcalc",
        "input_design": input_design,
        "verbose_flag": verbose_flag,
        "debug_flag": debug_flag,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if tr is not None:
        params["tr"] = tr
    if lower_limit is not None:
        params["lower_limit"] = lower_limit
    if example_sigma is not None:
        params["example_sigma"] = example_sigma
    return params

def cutoffcalc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Cutoffcalc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_design", None) is None:
        raise StyxValidationError("`input_design` must not be None")
    if not isinstance(params["input_design"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_design` has the wrong type: Received `{type(params["input_design"])}` expected `InputPathType`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("tr", None) is not None:
        if not isinstance(params["tr"], (float, int)):
            raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("lower_limit", None) is not None:
        if not isinstance(params["lower_limit"], (float, int)):
            raise StyxValidationError(f'`lower_limit` has the wrong type: Received `{type(params["lower_limit"])}` expected `float`')
    if params.get("example_sigma", None) is not None:
        if not isinstance(params["example_sigma"], (float, int)):
            raise StyxValidationError(f'`example_sigma` has the wrong type: Received `{type(params["example_sigma"])}` expected `float`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')

def cutoffcalc_cargs(params: Cutoffcalc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("cutoffcalc")
    cargs.append("-i ")
    cargs.append(execution.input_file(params["input_design"]))
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-t ")
        cargs.append(str(v_0))
    v_1 = params.get("tr")
    if v_1 is not None:
        cargs.append("--tr ")
        cargs.append(str(v_1))
    v_2 = params.get("lower_limit")
    if v_2 is not None:
        cargs.append("--limit ")
        cargs.append(str(v_2))
    v_3 = params.get("example_sigma")
    if v_3 is not None:
        cargs.append("--example_sig ")
        cargs.append(str(v_3))
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("debug_flag"):
        cargs.append("--debug")
    return cargs

def cutoffcalc_outputs(params: Cutoffcalc, execution: Execution) -> CutoffcalcOutputs:
    """Build the CutoffcalcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    example_output_v: OutputPathType = execution.output_file("example_filt.mtx")
    return CutoffcalcOutputs(
        root=root_v,
        example_output=example_output_v,
    )

def cutoffcalc_execute(params: Cutoffcalc, runner: Runner | None = None) -> CutoffcalcOutputs:
    """
    cutoffcalc

    Calculates the minimal period for the highpass filter that still preserves a specified amount of variance in all the design matrix regressors.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    cutoffcalc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CUTOFFCALC_METADATA)
    execution.params(params)
    args = cutoffcalc_cargs(params, execution)
    out = cutoffcalc_outputs(params, execution)
    execution.run(args)
    return out

def cutoffcalc(
    input_design: InputPathType,
    threshold: float | None = None,
    tr: float | None = None,
    lower_limit: float | None = None,
    example_sigma: float | None = None,
    verbose_flag: bool = False,
    debug_flag: bool = False,
    runner: Runner | None = None,
) -> CutoffcalcOutputs:
    """
    cutoffcalc

    Calculates the minimal period for the highpass filter that still preserves a specified amount of variance in all the design matrix regressors.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_design: Input design matrix (e.g. design.mat)
        threshold: Threshold for retained variance (default=0.9)
        tr: Time between successive data points (default=3.0s)
        lower_limit: Lower limit on period due to autocorr estimation (default=90s)
        example_sigma: Example sigma (in sec) to produce output called\
            example_filt.mtx
        verbose_flag: Switch on diagnostic messages
        debug_flag: Switch on debugging messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = cutoffcalc_params(
        input_design=input_design,
        threshold=threshold,
        tr=tr,
        lower_limit=lower_limit,
        example_sigma=example_sigma,
        verbose_flag=verbose_flag,
        debug_flag=debug_flag,
    )
    return cutoffcalc_execute(params, runner)

__all__ = [
    "Cutoffcalc",
    "CutoffcalcOutputs",
    "CUTOFFCALC_METADATA",
    "cutoffcalc_cargs",
    "cutoffcalc_outputs",
    "cutoffcalc_params",
    "cutoffcalc_execute",
    "cutoffcalc_validate",
    "cutoffcalc",
]