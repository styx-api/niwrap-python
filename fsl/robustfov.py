# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ROBUSTFOV_METADATA = Metadata(
    id="robustfov",
    name="robustfov",
    package="fsl",
)

Robustfov = typing.TypedDict(
    "Robustfov",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/robustfov"]],
        "input_file": InputPathType,
        "output_image": typing.NotRequired[str],
        "brain_size": typing.NotRequired[float],
        "matrix_output": typing.NotRequired[str],
        "debug_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class RobustfovOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_roi_volume: OutputPathType | None
    """ROI volume output"""
    output_matrix_file: OutputPathType | None
    """Matrix output (ROI to full FOV)"""

def _strip_extensions(value: str, exts: list[str]) -> str:
    for ext in exts:
        if value.endswith(ext):
            return value[: -len(ext)]
    return value

def robustfov_params(
    input_file: InputPathType,
    output_image: str | None = None,
    brain_size: float | None = None,
    matrix_output: str | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
) -> Robustfov:
    """
    Build parameters.

    Args:
        input_file: Input image file
        output_image: ROI volume output name
        brain_size: Size of the brain in z-dimension (default 170mm)
        matrix_output: Matrix output name (ROI to full FOV)
        debug_flag: Turn on debugging output
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Robustfov = {
        "@type": "fsl/robustfov",
        "input_file": input_file,
        "debug_flag": debug_flag,
        "verbose_flag": verbose_flag,
    }
    if output_image is not None:
        params["output_image"] = output_image
    if brain_size is not None:
        params["brain_size"] = brain_size
    if matrix_output is not None:
        params["matrix_output"] = matrix_output
    return params

def robustfov_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Robustfov."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("brain_size", None) is not None:
        if not isinstance(params["brain_size"], (float, int)):
            raise StyxValidationError(f'`brain_size` has the wrong type: Received `{type(params["brain_size"])}` expected `float`')
    if params.get("matrix_output", None) is not None:
        if not isinstance(params["matrix_output"], str):
            raise StyxValidationError(f'`matrix_output` has the wrong type: Received `{type(params["matrix_output"])}` expected `str`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def robustfov_cargs(params: Robustfov, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("robustfov")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    v_0 = params.get("output_image")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(v_0)
    v_1 = params.get("brain_size")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(str(v_1))
    v_2 = params.get("matrix_output")
    if v_2 is not None:
        cargs.append("-m")
        cargs.append(v_2)
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    return cargs

def robustfov_outputs(params: Robustfov, execution: Execution) -> RobustfovOutputs:
    """Build the RobustfovOutputs object for this tool."""
    output_roi_volume_v: OutputPathType | None = None
    output_matrix_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_image")
    if __v0 is not None:
        output_roi_volume_v = execution.output_file(f'{_strip_extensions((__v0 if __v0 is not None else ""), [".nii.gz"])}.nii.gz')
    __v1 = params.get("matrix_output")
    if __v1 is not None:
        output_matrix_file_v = execution.output_file(f'{(__v1 if __v1 is not None else "")}.txt')
    return RobustfovOutputs(
        root=root_v,
        output_roi_volume=output_roi_volume_v,
        output_matrix_file=output_matrix_file_v,
    )

def robustfov_execute(params: Robustfov, runner: Runner | None = None) -> RobustfovOutputs:
    """
    robustfov

    Reduce FOV of image to remove lower head and neck.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    robustfov_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ROBUSTFOV_METADATA)
    execution.params(params)
    args = robustfov_cargs(params, execution)
    out = robustfov_outputs(params, execution)
    execution.run(args)
    return out

def robustfov(
    input_file: InputPathType,
    output_image: str | None = None,
    brain_size: float | None = None,
    matrix_output: str | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> RobustfovOutputs:
    """
    robustfov

    Reduce FOV of image to remove lower head and neck.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input image file
        output_image: ROI volume output name
        brain_size: Size of the brain in z-dimension (default 170mm)
        matrix_output: Matrix output name (ROI to full FOV)
        debug_flag: Turn on debugging output
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = robustfov_params(
        input_file=input_file,
        output_image=output_image,
        brain_size=brain_size,
        matrix_output=matrix_output,
        debug_flag=debug_flag,
        verbose_flag=verbose_flag,
    )
    return robustfov_execute(params, runner)

__all__ = [
    "Robustfov",
    "RobustfovOutputs",
    "ROBUSTFOV_METADATA",
    "robustfov_cargs",
    "robustfov_outputs",
    "robustfov_params",
    "robustfov_execute",
    "robustfov_validate",
    "robustfov",
]