# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FEAT_GM_PREPARE_METADATA = Metadata(
    id="feat_gm_prepare",
    name="feat_gm_prepare",
    package="fsl",
)

FeatGmPrepare = typing.TypedDict(
    "FeatGmPrepare",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/feat_gm_prepare"]],
        "gm_output": str,
        "feat_dirs_list": list[InputPathType],
    },
)

@dataclasses.dataclass
class FeatGmPrepareOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def feat_gm_prepare_params(
    gm_output: str,
    feat_dirs_list: list[InputPathType],
) -> FeatGmPrepare:
    """
    Build parameters.

    Args:
        gm_output: 4D grey matter output file
        feat_dirs_list: List of first-level FEAT output directories

    Returns:
        Parameter dictionary.
    """
    params: FeatGmPrepare = {
        "@type": "fsl/feat_gm_prepare",
        "gm_output": gm_output,
        "feat_dirs_list": feat_dirs_list,
    }
    return params

def feat_gm_prepare_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FeatGmPrepare."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("gm_output", None) is None:
        raise StyxValidationError("`gm_output` must not be None")
    if not isinstance(params["gm_output"], str):
        raise StyxValidationError(f'`gm_output` has the wrong type: Received `{type(params["gm_output"])}` expected `str`')
    if params.get("feat_dirs_list", None) is None:
        raise StyxValidationError("`feat_dirs_list` must not be None")
    if not isinstance(params["feat_dirs_list"], list):
        raise StyxValidationError(f'`feat_dirs_list` has the wrong type: Received `{type(params["feat_dirs_list"])}` expected `list[InputPathType]`')
    for e in params["feat_dirs_list"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`feat_dirs_list` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def feat_gm_prepare_cargs(params: FeatGmPrepare, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("feat_gm_prepare")
    cargs.append(params["gm_output"])
    for item0 in params["feat_dirs_list"]:
        cargs.append(execution.input_file(item0))
    return cargs

def feat_gm_prepare_outputs(params: FeatGmPrepare, execution: Execution) -> FeatGmPrepareOutputs:
    """Build the FeatGmPrepareOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FeatGmPrepareOutputs(
        root=root_v,
    )

def feat_gm_prepare_execute(params: FeatGmPrepare, runner: Runner | None = None) -> FeatGmPrepareOutputs:
    """
    feat_gm_prepare

    Prepare 4D grey matter files for higher-level analysis in FEAT.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    feat_gm_prepare_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FEAT_GM_PREPARE_METADATA)
    execution.params(params)
    args = feat_gm_prepare_cargs(params, execution)
    out = feat_gm_prepare_outputs(params, execution)
    execution.run(args)
    return out

def feat_gm_prepare(
    gm_output: str,
    feat_dirs_list: list[InputPathType],
    runner: Runner | None = None,
) -> FeatGmPrepareOutputs:
    """
    feat_gm_prepare

    Prepare 4D grey matter files for higher-level analysis in FEAT.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        gm_output: 4D grey matter output file
        feat_dirs_list: List of first-level FEAT output directories
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = feat_gm_prepare_params(
        gm_output=gm_output,
        feat_dirs_list=feat_dirs_list,
    )
    return feat_gm_prepare_execute(params, runner)

__all__ = [
    "FeatGmPrepare",
    "FeatGmPrepareOutputs",
    "FEAT_GM_PREPARE_METADATA",
    "feat_gm_prepare_cargs",
    "feat_gm_prepare_outputs",
    "feat_gm_prepare_params",
    "feat_gm_prepare_execute",
    "feat_gm_prepare_validate",
    "feat_gm_prepare",
]