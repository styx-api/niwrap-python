# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMRM_METADATA = Metadata(
    id="imrm",
    name="imrm",
    package="fsl",
)

Imrm = typing.TypedDict(
    "Imrm",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/imrm"]],
        "images_to_remove": list[str],
    },
)

@dataclasses.dataclass
class ImrmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def imrm_params(
    images_to_remove: list[str],
) -> Imrm:
    """
    Build parameters.

    Args:
        images_to_remove: List of image names to remove. Filenames can be basenames\
            or full names.

    Returns:
        Parameter dictionary.
    """
    params: Imrm = {
        "@type": "fsl/imrm",
        "images_to_remove": images_to_remove,
    }
    return params

def imrm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imrm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("images_to_remove", None) is None:
        raise StyxValidationError("`images_to_remove` must not be None")
    if not isinstance(params["images_to_remove"], list):
        raise StyxValidationError(f'`images_to_remove` has the wrong type: Received `{type(params["images_to_remove"])}` expected `list[str]`')
    for e in params["images_to_remove"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`images_to_remove` has the wrong type: Received `{type(e)}` expected `list[str]`')

def imrm_cargs(params: Imrm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imrm")
    for item0 in params["images_to_remove"]:
        cargs.append(item0)
    return cargs

def imrm_outputs(params: Imrm, execution: Execution) -> ImrmOutputs:
    """Build the ImrmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ImrmOutputs(
        root=root_v,
    )

def imrm_execute(params: Imrm, runner: Runner | None = None) -> ImrmOutputs:
    """
    imrm

    Remove specified image files.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imrm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMRM_METADATA)
    execution.params(params)
    args = imrm_cargs(params, execution)
    out = imrm_outputs(params, execution)
    execution.run(args)
    return out

def imrm(
    images_to_remove: list[str],
    runner: Runner | None = None,
) -> ImrmOutputs:
    """
    imrm

    Remove specified image files.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        images_to_remove: List of image names to remove. Filenames can be basenames\
            or full names.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imrm_params(
        images_to_remove=images_to_remove,
    )
    return imrm_execute(params, runner)

__all__ = [
    "Imrm",
    "ImrmOutputs",
    "IMRM_METADATA",
    "imrm_cargs",
    "imrm_outputs",
    "imrm_params",
    "imrm_execute",
    "imrm_validate",
    "imrm",
]