# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIGNAL2IMAGE_METADATA = Metadata(
    id="signal2image",
    name="signal2image",
    package="fsl",
)

Signal2image = typing.TypedDict(
    "Signal2image",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/signal2image"]],
        "pulse_sequence": InputPathType,
        "input_signal": typing.NotRequired[InputPathType],
        "output_image": typing.NotRequired[str],
        "kspace_coordinates": typing.NotRequired[InputPathType],
        "output_kspace": typing.NotRequired[str],
        "abs_flag": typing.NotRequired[bool],
        "homodyne_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
        "apodize_flag": typing.NotRequired[bool],
        "cutoff": typing.NotRequired[float],
        "rolloff": typing.NotRequired[float],
        "save_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class Signal2imageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType | None
    """The resultant image file from the input signal and pulse sequence."""
    kspace_outfile: OutputPathType | None
    """The resultant k-space file."""

def signal2image_params(
    pulse_sequence: InputPathType,
    input_signal: InputPathType | None = None,
    output_image: str | None = None,
    kspace_coordinates: InputPathType | None = None,
    output_kspace: str | None = None,
    abs_flag: bool = False,
    homodyne_flag: bool = False,
    verbose_flag: bool = False,
    apodize_flag: bool = False,
    cutoff: float | None = None,
    rolloff: float | None = None,
    save_flag: bool = False,
    help_flag: bool = False,
) -> Signal2image:
    """
    Build parameters.

    Args:
        pulse_sequence: 8-column pulse_sequence matrix. Expects to find all other\
            pulse sequence files in the same directory.
        input_signal: Input signal file.
        output_image: Output image file.
        kspace_coordinates: K-space coordinates file.
        output_kspace: Output k-space file.
        abs_flag: Save absolute magnitude and phase.
        homodyne_flag: Do the homodyne reconstruction.
        verbose_flag: Switch on diagnostic messages.
        apodize_flag: Do apodization.
        cutoff: Apodization with this cutoff; default 100.
        rolloff: Apodization with this rolloff; default 10.
        save_flag: Save window as ASCII matrix (DEBUG!)
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: Signal2image = {
        "@type": "fsl/signal2image",
        "pulse_sequence": pulse_sequence,
        "abs_flag": abs_flag,
        "homodyne_flag": homodyne_flag,
        "verbose_flag": verbose_flag,
        "apodize_flag": apodize_flag,
        "save_flag": save_flag,
        "help_flag": help_flag,
    }
    if input_signal is not None:
        params["input_signal"] = input_signal
    if output_image is not None:
        params["output_image"] = output_image
    if kspace_coordinates is not None:
        params["kspace_coordinates"] = kspace_coordinates
    if output_kspace is not None:
        params["output_kspace"] = output_kspace
    if cutoff is not None:
        params["cutoff"] = cutoff
    if rolloff is not None:
        params["rolloff"] = rolloff
    return params

def signal2image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Signal2image."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("pulse_sequence", None) is None:
        raise StyxValidationError("`pulse_sequence` must not be None")
    if not isinstance(params["pulse_sequence"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pulse_sequence` has the wrong type: Received `{type(params["pulse_sequence"])}` expected `InputPathType`')
    if params.get("input_signal", None) is not None:
        if not isinstance(params["input_signal"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_signal` has the wrong type: Received `{type(params["input_signal"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("kspace_coordinates", None) is not None:
        if not isinstance(params["kspace_coordinates"], (pathlib.Path, str)):
            raise StyxValidationError(f'`kspace_coordinates` has the wrong type: Received `{type(params["kspace_coordinates"])}` expected `InputPathType`')
    if params.get("output_kspace", None) is not None:
        if not isinstance(params["output_kspace"], str):
            raise StyxValidationError(f'`output_kspace` has the wrong type: Received `{type(params["output_kspace"])}` expected `str`')
    if params.get("abs_flag", None) is not None:
        if not isinstance(params["abs_flag"], bool):
            raise StyxValidationError(f'`abs_flag` has the wrong type: Received `{type(params["abs_flag"])}` expected `bool`')
    if params.get("homodyne_flag", None) is not None:
        if not isinstance(params["homodyne_flag"], bool):
            raise StyxValidationError(f'`homodyne_flag` has the wrong type: Received `{type(params["homodyne_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("apodize_flag", None) is not None:
        if not isinstance(params["apodize_flag"], bool):
            raise StyxValidationError(f'`apodize_flag` has the wrong type: Received `{type(params["apodize_flag"])}` expected `bool`')
    if params.get("cutoff", None) is not None:
        if not isinstance(params["cutoff"], (float, int)):
            raise StyxValidationError(f'`cutoff` has the wrong type: Received `{type(params["cutoff"])}` expected `float`')
    if params.get("rolloff", None) is not None:
        if not isinstance(params["rolloff"], (float, int)):
            raise StyxValidationError(f'`rolloff` has the wrong type: Received `{type(params["rolloff"])}` expected `float`')
    if params.get("save_flag", None) is not None:
        if not isinstance(params["save_flag"], bool):
            raise StyxValidationError(f'`save_flag` has the wrong type: Received `{type(params["save_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def signal2image_cargs(params: Signal2image, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("signal2image")
    cargs.append("-p")
    cargs.append(execution.input_file(params["pulse_sequence"]))
    v_0 = params.get("input_signal")
    if v_0 is not None:
        cargs.append("-i")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("output_image")
    if v_1 is not None:
        cargs.append("-o")
        cargs.append(v_1)
    v_2 = params.get("kspace_coordinates")
    if v_2 is not None:
        cargs.append("-c")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("output_kspace")
    if v_3 is not None:
        cargs.append("-k")
        cargs.append(v_3)
    if params.get("abs_flag"):
        cargs.append("-a")
    if params.get("homodyne_flag"):
        cargs.append("--homo")
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("apodize_flag"):
        cargs.append("-z")
    v_4 = params.get("cutoff")
    if v_4 is not None:
        cargs.append("-l")
        cargs.append(str(v_4))
    v_5 = params.get("rolloff")
    if v_5 is not None:
        cargs.append("-r")
        cargs.append(str(v_5))
    if params.get("save_flag"):
        cargs.append("-s")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def signal2image_outputs(params: Signal2image, execution: Execution) -> Signal2imageOutputs:
    """Build the Signal2imageOutputs object for this tool."""
    outfile_v: OutputPathType | None = None
    kspace_outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_image")
    if __v0 is not None:
        outfile_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("output_kspace")
    if __v1 is not None:
        kspace_outfile_v = execution.output_file((__v1 if __v1 is not None else ""))
    return Signal2imageOutputs(
        root=root_v,
        outfile=outfile_v,
        kspace_outfile=kspace_outfile_v,
    )

def signal2image_execute(params: Signal2image, runner: Runner | None = None) -> Signal2imageOutputs:
    """
    signal2image

    A tool for converting MR signal data to images using specified k-space coordinates and pulse sequences.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    signal2image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIGNAL2IMAGE_METADATA)
    execution.params(params)
    args = signal2image_cargs(params, execution)
    out = signal2image_outputs(params, execution)
    execution.run(args)
    return out

def signal2image(
    pulse_sequence: InputPathType,
    input_signal: InputPathType | None = None,
    output_image: str | None = None,
    kspace_coordinates: InputPathType | None = None,
    output_kspace: str | None = None,
    abs_flag: bool = False,
    homodyne_flag: bool = False,
    verbose_flag: bool = False,
    apodize_flag: bool = False,
    cutoff: float | None = None,
    rolloff: float | None = None,
    save_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> Signal2imageOutputs:
    """
    signal2image

    A tool for converting MR signal data to images using specified k-space coordinates and pulse sequences.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        pulse_sequence: 8-column pulse_sequence matrix. Expects to find all other\
            pulse sequence files in the same directory.
        input_signal: Input signal file.
        output_image: Output image file.
        kspace_coordinates: K-space coordinates file.
        output_kspace: Output k-space file.
        abs_flag: Save absolute magnitude and phase.
        homodyne_flag: Do the homodyne reconstruction.
        verbose_flag: Switch on diagnostic messages.
        apodize_flag: Do apodization.
        cutoff: Apodization with this cutoff; default 100.
        rolloff: Apodization with this rolloff; default 10.
        save_flag: Save window as ASCII matrix (DEBUG!)
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = signal2image_params(
        pulse_sequence=pulse_sequence,
        input_signal=input_signal,
        output_image=output_image,
        kspace_coordinates=kspace_coordinates,
        output_kspace=output_kspace,
        abs_flag=abs_flag,
        homodyne_flag=homodyne_flag,
        verbose_flag=verbose_flag,
        apodize_flag=apodize_flag,
        cutoff=cutoff,
        rolloff=rolloff,
        save_flag=save_flag,
        help_flag=help_flag,
    )
    return signal2image_execute(params, runner)

__all__ = [
    "Signal2image",
    "Signal2imageOutputs",
    "SIGNAL2IMAGE_METADATA",
    "signal2image_cargs",
    "signal2image_outputs",
    "signal2image_params",
    "signal2image_execute",
    "signal2image_validate",
    "signal2image",
]