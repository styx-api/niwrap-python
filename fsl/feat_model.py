# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FEAT_MODEL_METADATA = Metadata(
    id="feat_model",
    name="feat_model",
    package="fsl",
)

FeatModel = typing.TypedDict(
    "FeatModel",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/feat_model"]],
        "design_name_root": str,
        "confound_matrix": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class FeatModelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def feat_model_params(
    design_name_root: str,
    confound_matrix: InputPathType | None = None,
) -> FeatModel:
    """
    Build parameters.

    Args:
        design_name_root: Design name root
        confound_matrix: Confound matrix text file

    Returns:
        Parameter dictionary.
    """
    params: FeatModel = {
        "@type": "fsl/feat_model",
        "design_name_root": design_name_root,
    }
    if confound_matrix is not None:
        params["confound_matrix"] = confound_matrix
    return params

def feat_model_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FeatModel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("design_name_root", None) is None:
        raise StyxValidationError("`design_name_root` must not be None")
    if not isinstance(params["design_name_root"], str):
        raise StyxValidationError(f'`design_name_root` has the wrong type: Received `{type(params["design_name_root"])}` expected `str`')
    if params.get("confound_matrix", None) is not None:
        if not isinstance(params["confound_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`confound_matrix` has the wrong type: Received `{type(params["confound_matrix"])}` expected `InputPathType`')

def feat_model_cargs(params: FeatModel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("feat_model")
    cargs.append(params["design_name_root"])
    v_0 = params.get("confound_matrix")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def feat_model_outputs(params: FeatModel, execution: Execution) -> FeatModelOutputs:
    """Build the FeatModelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FeatModelOutputs(
        root=root_v,
    )

def feat_model_execute(params: FeatModel, runner: Runner | None = None) -> FeatModelOutputs:
    """
    feat_model

    Generate design matrices for use by FEAT

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    feat_model_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FEAT_MODEL_METADATA)
    execution.params(params)
    args = feat_model_cargs(params, execution)
    out = feat_model_outputs(params, execution)
    execution.run(args)
    return out

def feat_model(
    design_name_root: str,
    confound_matrix: InputPathType | None = None,
    runner: Runner | None = None,
) -> FeatModelOutputs:
    """
    feat_model

    Generate design matrices for use by FEAT

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        design_name_root: Design name root
        confound_matrix: Confound matrix text file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = feat_model_params(
        design_name_root=design_name_root,
        confound_matrix=confound_matrix,
    )
    return feat_model_execute(params, runner)

__all__ = [
    "FeatModel",
    "FeatModelOutputs",
    "FEAT_MODEL_METADATA",
    "feat_model_cargs",
    "feat_model_outputs",
    "feat_model_params",
    "feat_model_execute",
    "feat_model_validate",
    "feat_model",
]