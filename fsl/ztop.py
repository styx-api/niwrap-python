# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ZTOP_METADATA = Metadata(
    id="ztop",
    name="ztop",
    package="fsl",
)

Ztop = typing.TypedDict(
    "Ztop",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/ztop"]],
        "z_score": float,
        "tail_flag": typing.NotRequired[bool],
        "grf_flag": typing.NotRequired[bool],
        "number_of_resels": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class ZtopOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def ztop_params(
    z_score: float,
    tail_flag: bool = False,
    grf_flag: bool = False,
    number_of_resels: float | None = None,
) -> Ztop:
    """
    Build parameters.

    Args:
        z_score: Input z-score
        tail_flag: Use 2-tailed conversion (default is 1-tailed)
        grf_flag: Use GRF maximum-height theory instead of Gaussian PDF
        number_of_resels: Number of resels (resolution elements) for GRF correction

    Returns:
        Parameter dictionary.
    """
    params: Ztop = {
        "@type": "fsl/ztop",
        "z_score": z_score,
        "tail_flag": tail_flag,
        "grf_flag": grf_flag,
    }
    if number_of_resels is not None:
        params["number_of_resels"] = number_of_resels
    return params

def ztop_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ztop."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("z_score", None) is None:
        raise StyxValidationError("`z_score` must not be None")
    if not isinstance(params["z_score"], (float, int)):
        raise StyxValidationError(f'`z_score` has the wrong type: Received `{type(params["z_score"])}` expected `float`')
    if params.get("tail_flag", None) is not None:
        if not isinstance(params["tail_flag"], bool):
            raise StyxValidationError(f'`tail_flag` has the wrong type: Received `{type(params["tail_flag"])}` expected `bool`')
    if params.get("grf_flag", None) is not None:
        if not isinstance(params["grf_flag"], bool):
            raise StyxValidationError(f'`grf_flag` has the wrong type: Received `{type(params["grf_flag"])}` expected `bool`')
    if params.get("number_of_resels", None) is not None:
        if not isinstance(params["number_of_resels"], (float, int)):
            raise StyxValidationError(f'`number_of_resels` has the wrong type: Received `{type(params["number_of_resels"])}` expected `float`')

def ztop_cargs(params: Ztop, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ztop")
    cargs.append(str(params["z_score"]))
    if params.get("tail_flag"):
        cargs.append("-2")
    if params.get("grf_flag"):
        cargs.append("-g")
    v_0 = params.get("number_of_resels")
    if v_0 is not None:
        cargs.append(str(v_0))
    return cargs

def ztop_outputs(params: Ztop, execution: Execution) -> ZtopOutputs:
    """Build the ZtopOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ZtopOutputs(
        root=root_v,
    )

def ztop_execute(params: Ztop, runner: Runner | None = None) -> ZtopOutputs:
    """
    ztop

    Converts a z-score to a p-value.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ztop_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ZTOP_METADATA)
    execution.params(params)
    args = ztop_cargs(params, execution)
    out = ztop_outputs(params, execution)
    execution.run(args)
    return out

def ztop(
    z_score: float,
    tail_flag: bool = False,
    grf_flag: bool = False,
    number_of_resels: float | None = None,
    runner: Runner | None = None,
) -> ZtopOutputs:
    """
    ztop

    Converts a z-score to a p-value.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        z_score: Input z-score
        tail_flag: Use 2-tailed conversion (default is 1-tailed)
        grf_flag: Use GRF maximum-height theory instead of Gaussian PDF
        number_of_resels: Number of resels (resolution elements) for GRF correction
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ztop_params(
        z_score=z_score,
        tail_flag=tail_flag,
        grf_flag=grf_flag,
        number_of_resels=number_of_resels,
    )
    return ztop_execute(params, runner)

__all__ = [
    "Ztop",
    "ZtopOutputs",
    "ZTOP_METADATA",
    "ztop_cargs",
    "ztop_outputs",
    "ztop_params",
    "ztop_execute",
    "ztop_validate",
    "ztop",
]