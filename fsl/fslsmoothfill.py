# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLSMOOTHFILL_METADATA = Metadata(
    id="fslsmoothfill",
    name="fslsmoothfill",
    package="fsl",
)

Fslsmoothfill = typing.TypedDict(
    "Fslsmoothfill",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslsmoothfill"]],
        "input_image": InputPathType,
        "mask_image": InputPathType,
        "output_image": str,
        "number_of_iterations": typing.NotRequired[int],
        "debug_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FslsmoothfillOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslsmoothfill_params(
    input_image: InputPathType,
    mask_image: InputPathType,
    output_image: str,
    number_of_iterations: int | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
) -> Fslsmoothfill:
    """
    Build parameters.

    Args:
        input_image: Filename of the input image.
        mask_image: Filename of the mask image.
        output_image: Filename for the output smoothed result image.
        number_of_iterations: Number of iterations.
        debug_flag: Turn on debugging output.
        verbose_flag: Switch on diagnostic messages.

    Returns:
        Parameter dictionary.
    """
    params: Fslsmoothfill = {
        "@type": "fsl/fslsmoothfill",
        "input_image": input_image,
        "mask_image": mask_image,
        "output_image": output_image,
        "debug_flag": debug_flag,
        "verbose_flag": verbose_flag,
    }
    if number_of_iterations is not None:
        params["number_of_iterations"] = number_of_iterations
    return params

def fslsmoothfill_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslsmoothfill."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("mask_image", None) is None:
        raise StyxValidationError("`mask_image` must not be None")
    if not isinstance(params["mask_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("number_of_iterations", None) is not None:
        if not isinstance(params["number_of_iterations"], int):
            raise StyxValidationError(f'`number_of_iterations` has the wrong type: Received `{type(params["number_of_iterations"])}` expected `int`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def fslsmoothfill_cargs(params: Fslsmoothfill, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslsmoothfill")
    cargs.append("--in=")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("--mask=")
    cargs.append(execution.input_file(params["mask_image"]))
    cargs.append("--out=")
    cargs.append(params["output_image"])
    v_0 = params.get("number_of_iterations")
    if v_0 is not None:
        cargs.append("--niter")
        cargs.append(str(v_0))
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    return cargs

def fslsmoothfill_outputs(params: Fslsmoothfill, execution: Execution) -> FslsmoothfillOutputs:
    """Build the FslsmoothfillOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslsmoothfillOutputs(
        root=root_v,
    )

def fslsmoothfill_execute(params: Fslsmoothfill, runner: Runner | None = None) -> FslsmoothfillOutputs:
    """
    fslsmoothfill

    Smoothfill is a tool designed to fill in holes in images by smoothly interpolating the pixel values.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslsmoothfill_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLSMOOTHFILL_METADATA)
    execution.params(params)
    args = fslsmoothfill_cargs(params, execution)
    out = fslsmoothfill_outputs(params, execution)
    execution.run(args)
    return out

def fslsmoothfill(
    input_image: InputPathType,
    mask_image: InputPathType,
    output_image: str,
    number_of_iterations: int | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> FslsmoothfillOutputs:
    """
    fslsmoothfill

    Smoothfill is a tool designed to fill in holes in images by smoothly interpolating the pixel values.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Filename of the input image.
        mask_image: Filename of the mask image.
        output_image: Filename for the output smoothed result image.
        number_of_iterations: Number of iterations.
        debug_flag: Turn on debugging output.
        verbose_flag: Switch on diagnostic messages.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslsmoothfill_params(
        input_image=input_image,
        mask_image=mask_image,
        output_image=output_image,
        number_of_iterations=number_of_iterations,
        debug_flag=debug_flag,
        verbose_flag=verbose_flag,
    )
    return fslsmoothfill_execute(params, runner)

__all__ = [
    "Fslsmoothfill",
    "FslsmoothfillOutputs",
    "FSLSMOOTHFILL_METADATA",
    "fslsmoothfill_cargs",
    "fslsmoothfill_outputs",
    "fslsmoothfill_params",
    "fslsmoothfill_execute",
    "fslsmoothfill_validate",
    "fslsmoothfill",
]