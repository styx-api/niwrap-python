# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLREORIENT2STD_METADATA = Metadata(
    id="fslreorient2std",
    name="fslreorient2std",
    package="fsl",
)

Fslreorient2std = typing.TypedDict(
    "Fslreorient2std",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslreorient2std"]],
        "input_image": InputPathType,
        "output_image": typing.NotRequired[str],
        "matrix_file": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Fslreorient2stdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType | None
    """Reoriented output image (NIfTI format)"""
    matrix_output: OutputPathType | None
    """File to save the transformation matrix"""

def _strip_extensions(value: str, exts: list[str]) -> str:
    for ext in exts:
        if value.endswith(ext):
            return value[: -len(ext)]
    return value

def fslreorient2std_params(
    input_image: InputPathType,
    output_image: str | None = None,
    matrix_file: str | None = None,
) -> Fslreorient2std:
    """
    Build parameters.

    Args:
        input_image: Input image to be reoriented (NIfTI format, e.g. img.nii.gz)
        output_image: Output image with the reoriented result (NIfTI format, e.g.\
            reoriented_img.nii.gz). If not provided, transformation matrix is\
            output to standard output.
        matrix_file: File to save the transformation matrix

    Returns:
        Parameter dictionary.
    """
    params: Fslreorient2std = {
        "@type": "fsl/fslreorient2std",
        "input_image": input_image,
    }
    if output_image is not None:
        params["output_image"] = output_image
    if matrix_file is not None:
        params["matrix_file"] = matrix_file
    return params

def fslreorient2std_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslreorient2std."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is not None:
        if not isinstance(params["output_image"], str):
            raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("matrix_file", None) is not None:
        if not isinstance(params["matrix_file"], str):
            raise StyxValidationError(f'`matrix_file` has the wrong type: Received `{type(params["matrix_file"])}` expected `str`')

def fslreorient2std_cargs(params: Fslreorient2std, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslreorient2std")
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("output_image")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("matrix_file")
    if v_1 is not None:
        cargs.append("-m")
        cargs.append(v_1)
    return cargs

def fslreorient2std_outputs(params: Fslreorient2std, execution: Execution) -> Fslreorient2stdOutputs:
    """Build the Fslreorient2stdOutputs object for this tool."""
    output_image_v: OutputPathType | None = None
    matrix_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_image")
    if __v0 is not None:
        output_image_v = execution.output_file(f'{_strip_extensions((__v0 if __v0 is not None else ""), [".nii.gz"])}.nii.gz')
    __v1 = params.get("matrix_file")
    if __v1 is not None:
        matrix_output_v = execution.output_file((__v1 if __v1 is not None else ""))
    return Fslreorient2stdOutputs(
        root=root_v,
        output_image=output_image_v,
        matrix_output=matrix_output_v,
    )

def fslreorient2std_execute(params: Fslreorient2std, runner: Runner | None = None) -> Fslreorient2stdOutputs:
    """
    fslreorient2std

    A tool for reorienting an image to match the approximate orientation of standard template images (MNI152).

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslreorient2std_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLREORIENT2STD_METADATA)
    execution.params(params)
    args = fslreorient2std_cargs(params, execution)
    out = fslreorient2std_outputs(params, execution)
    execution.run(args)
    return out

def fslreorient2std(
    input_image: InputPathType,
    output_image: str | None = None,
    matrix_file: str | None = None,
    runner: Runner | None = None,
) -> Fslreorient2stdOutputs:
    """
    fslreorient2std

    A tool for reorienting an image to match the approximate orientation of standard template images (MNI152).

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_image: Input image to be reoriented (NIfTI format, e.g. img.nii.gz)
        output_image: Output image with the reoriented result (NIfTI format, e.g.\
            reoriented_img.nii.gz). If not provided, transformation matrix is\
            output to standard output.
        matrix_file: File to save the transformation matrix
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslreorient2std_params(
        input_image=input_image,
        output_image=output_image,
        matrix_file=matrix_file,
    )
    return fslreorient2std_execute(params, runner)

__all__ = [
    "Fslreorient2std",
    "Fslreorient2stdOutputs",
    "FSLREORIENT2STD_METADATA",
    "fslreorient2std_cargs",
    "fslreorient2std_outputs",
    "fslreorient2std_params",
    "fslreorient2std_execute",
    "fslreorient2std_validate",
    "fslreorient2std",
]