# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TBSS_DEPROJECT_METADATA = Metadata(
    id="tbss_deproject",
    name="tbss_deproject",
    package="fsl",
)

TbssDeproject = typing.TypedDict(
    "TbssDeproject",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tbss_deproject"]],
        "skeleton_space_input_image": InputPathType,
        "final_space_option": int,
        "index_image_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TbssDeprojectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    stats_output: OutputPathType
    """Output in stats directory."""
    fa_output: OutputPathType
    """Output in FA directory."""

def tbss_deproject_params(
    skeleton_space_input_image: InputPathType,
    final_space_option: int,
    index_image_flag: bool = False,
) -> TbssDeproject:
    """
    Build parameters.

    Args:
        skeleton_space_input_image: Skeleton space input image.
        final_space_option: Final space option.
        index_image_flag: Assume that the skeleton space input image contains one\
            or more integer index numbers and ensure that those exact values are\
            deprojected without being changed by interpolations.

    Returns:
        Parameter dictionary.
    """
    params: TbssDeproject = {
        "@type": "fsl/tbss_deproject",
        "skeleton_space_input_image": skeleton_space_input_image,
        "final_space_option": final_space_option,
        "index_image_flag": index_image_flag,
    }
    return params

def tbss_deproject_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TbssDeproject."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("skeleton_space_input_image", None) is None:
        raise StyxValidationError("`skeleton_space_input_image` must not be None")
    if not isinstance(params["skeleton_space_input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`skeleton_space_input_image` has the wrong type: Received `{type(params["skeleton_space_input_image"])}` expected `InputPathType`')
    if params.get("final_space_option", None) is None:
        raise StyxValidationError("`final_space_option` must not be None")
    if not isinstance(params["final_space_option"], int):
        raise StyxValidationError(f'`final_space_option` has the wrong type: Received `{type(params["final_space_option"])}` expected `int`')
    if params.get("index_image_flag", None) is not None:
        if not isinstance(params["index_image_flag"], bool):
            raise StyxValidationError(f'`index_image_flag` has the wrong type: Received `{type(params["index_image_flag"])}` expected `bool`')

def tbss_deproject_cargs(params: TbssDeproject, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tbss_deproject")
    cargs.append(execution.input_file(params["skeleton_space_input_image"]))
    cargs.append(str(params["final_space_option"]))
    if params.get("index_image_flag"):
        cargs.append("-n")
    return cargs

def tbss_deproject_outputs(params: TbssDeproject, execution: Execution) -> TbssDeprojectOutputs:
    """Build the TbssDeprojectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    stats_output_v: OutputPathType = execution.output_file(f'stats/{params["skeleton_space_input_image"]}')
    fa_output_v: OutputPathType = execution.output_file(f'FA/{params["skeleton_space_input_image"]}')
    return TbssDeprojectOutputs(
        root=root_v,
        stats_output=stats_output_v,
        fa_output=fa_output_v,
    )

def tbss_deproject_execute(params: TbssDeproject, runner: Runner | None = None) -> TbssDeprojectOutputs:
    """
    tbss_deproject

    Tool to deproject images from skeleton space to final space

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tbss_deproject_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TBSS_DEPROJECT_METADATA)
    execution.params(params)
    args = tbss_deproject_cargs(params, execution)
    out = tbss_deproject_outputs(params, execution)
    execution.run(args)
    return out

def tbss_deproject(
    skeleton_space_input_image: InputPathType,
    final_space_option: int,
    index_image_flag: bool = False,
    runner: Runner | None = None,
) -> TbssDeprojectOutputs:
    """
    tbss_deproject

    Tool to deproject images from skeleton space to final space

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        skeleton_space_input_image: Skeleton space input image.
        final_space_option: Final space option.
        index_image_flag: Assume that the skeleton space input image contains one\
            or more integer index numbers and ensure that those exact values are\
            deprojected without being changed by interpolations.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tbss_deproject_params(
        skeleton_space_input_image=skeleton_space_input_image,
        final_space_option=final_space_option,
        index_image_flag=index_image_flag,
    )
    return tbss_deproject_execute(params, runner)

__all__ = [
    "TbssDeproject",
    "TbssDeprojectOutputs",
    "TBSS_DEPROJECT_METADATA",
    "tbss_deproject_cargs",
    "tbss_deproject_outputs",
    "tbss_deproject_params",
    "tbss_deproject_execute",
    "tbss_deproject_validate",
    "tbss_deproject",
]