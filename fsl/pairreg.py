# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PAIRREG_METADATA = Metadata(
    id="pairreg",
    name="pairreg",
    package="fsl",
)

Pairreg = typing.TypedDict(
    "Pairreg",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/pairreg"]],
        "brain1": InputPathType,
        "brain2": InputPathType,
        "skull1": InputPathType,
        "skull2": InputPathType,
        "outputmatrix": InputPathType,
        "extra_flirt_args": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class PairregOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_matrix: OutputPathType
    """Pairwise registration output transformation matrix file"""

def pairreg_params(
    brain1: InputPathType,
    brain2: InputPathType,
    skull1: InputPathType,
    skull2: InputPathType,
    outputmatrix: InputPathType,
    extra_flirt_args: str | None = None,
) -> Pairreg:
    """
    Build parameters.

    Args:
        brain1: Brain image 1 (used as -ref internally)
        brain2: Brain image 2
        skull1: Skull image 1 (used as -ref internally)
        skull2: Skull image 2
        outputmatrix: Output transformation matrix file
        extra_flirt_args: Extra arguments to pass to flirt

    Returns:
        Parameter dictionary.
    """
    params: Pairreg = {
        "@type": "fsl/pairreg",
        "brain1": brain1,
        "brain2": brain2,
        "skull1": skull1,
        "skull2": skull2,
        "outputmatrix": outputmatrix,
    }
    if extra_flirt_args is not None:
        params["extra_flirt_args"] = extra_flirt_args
    return params

def pairreg_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Pairreg."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("brain1", None) is None:
        raise StyxValidationError("`brain1` must not be None")
    if not isinstance(params["brain1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain1` has the wrong type: Received `{type(params["brain1"])}` expected `InputPathType`')
    if params.get("brain2", None) is None:
        raise StyxValidationError("`brain2` must not be None")
    if not isinstance(params["brain2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`brain2` has the wrong type: Received `{type(params["brain2"])}` expected `InputPathType`')
    if params.get("skull1", None) is None:
        raise StyxValidationError("`skull1` must not be None")
    if not isinstance(params["skull1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`skull1` has the wrong type: Received `{type(params["skull1"])}` expected `InputPathType`')
    if params.get("skull2", None) is None:
        raise StyxValidationError("`skull2` must not be None")
    if not isinstance(params["skull2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`skull2` has the wrong type: Received `{type(params["skull2"])}` expected `InputPathType`')
    if params.get("outputmatrix", None) is None:
        raise StyxValidationError("`outputmatrix` must not be None")
    if not isinstance(params["outputmatrix"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outputmatrix` has the wrong type: Received `{type(params["outputmatrix"])}` expected `InputPathType`')
    if params.get("extra_flirt_args", None) is not None:
        if not isinstance(params["extra_flirt_args"], str):
            raise StyxValidationError(f'`extra_flirt_args` has the wrong type: Received `{type(params["extra_flirt_args"])}` expected `str`')

def pairreg_cargs(params: Pairreg, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("pairreg")
    cargs.append(execution.input_file(params["brain1"]))
    cargs.append(execution.input_file(params["brain2"]))
    cargs.append(execution.input_file(params["skull1"]))
    cargs.append(execution.input_file(params["skull2"]))
    cargs.append(execution.input_file(params["outputmatrix"]))
    v_0 = params.get("extra_flirt_args")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def pairreg_outputs(params: Pairreg, execution: Execution) -> PairregOutputs:
    """Build the PairregOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_matrix_v: OutputPathType = execution.output_file(params["outputmatrix"])
    return PairregOutputs(
        root=root_v,
        output_matrix=output_matrix_v,
    )

def pairreg_execute(params: Pairreg, runner: Runner | None = None) -> PairregOutputs:
    """
    pairreg

    Pairwise registration tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    pairreg_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PAIRREG_METADATA)
    execution.params(params)
    args = pairreg_cargs(params, execution)
    out = pairreg_outputs(params, execution)
    execution.run(args)
    return out

def pairreg(
    brain1: InputPathType,
    brain2: InputPathType,
    skull1: InputPathType,
    skull2: InputPathType,
    outputmatrix: InputPathType,
    extra_flirt_args: str | None = None,
    runner: Runner | None = None,
) -> PairregOutputs:
    """
    pairreg

    Pairwise registration tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        brain1: Brain image 1 (used as -ref internally)
        brain2: Brain image 2
        skull1: Skull image 1 (used as -ref internally)
        skull2: Skull image 2
        outputmatrix: Output transformation matrix file
        extra_flirt_args: Extra arguments to pass to flirt
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = pairreg_params(
        brain1=brain1,
        brain2=brain2,
        skull1=skull1,
        skull2=skull2,
        outputmatrix=outputmatrix,
        extra_flirt_args=extra_flirt_args,
    )
    return pairreg_execute(params, runner)

__all__ = [
    "Pairreg",
    "PairregOutputs",
    "PAIRREG_METADATA",
    "pairreg_cargs",
    "pairreg_outputs",
    "pairreg_params",
    "pairreg_execute",
    "pairreg_validate",
    "pairreg",
]