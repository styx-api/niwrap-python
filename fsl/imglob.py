# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMGLOB_METADATA = Metadata(
    id="imglob",
    name="imglob",
    package="fsl",
)

Imglob = typing.TypedDict(
    "Imglob",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/imglob"]],
        "multiple_extensions": typing.NotRequired[bool],
        "input_list": list[str],
        "single_extension": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ImglobOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def imglob_params(
    input_list: list[str],
    multiple_extensions: bool = False,
    single_extension: bool = False,
) -> Imglob:
    """
    Build parameters.

    Args:
        input_list: List of image names or file paths
        multiple_extensions: Output list of images with full extensions
        single_extension: Output one image with full extension

    Returns:
        Parameter dictionary.
    """
    params: Imglob = {
        "@type": "fsl/imglob",
        "input_list": input_list,
        "multiple_extensions": multiple_extensions,
        "single_extension": single_extension,
    }
    return params

def imglob_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imglob."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("multiple_extensions", None) is not None:
        if not isinstance(params["multiple_extensions"], bool):
            raise StyxValidationError(f'`multiple_extensions` has the wrong type: Received `{type(params["multiple_extensions"])}` expected `bool`')
    if params.get("input_list", None) is None:
        raise StyxValidationError("`input_list` must not be None")
    if not isinstance(params["input_list"], list):
        raise StyxValidationError(f'`input_list` has the wrong type: Received `{type(params["input_list"])}` expected `list[str]`')
    for e in params["input_list"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`input_list` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("single_extension", None) is not None:
        if not isinstance(params["single_extension"], bool):
            raise StyxValidationError(f'`single_extension` has the wrong type: Received `{type(params["single_extension"])}` expected `bool`')

def imglob_cargs(params: Imglob, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imglob")
    if params.get("multiple_extensions"):
        cargs.append("-extensions")
    for item0 in params["input_list"]:
        cargs.append(item0)
    if params.get("single_extension"):
        cargs.append("-extension")
    return cargs

def imglob_outputs(params: Imglob, execution: Execution) -> ImglobOutputs:
    """Build the ImglobOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ImglobOutputs(
        root=root_v,
    )

def imglob_execute(params: Imglob, runner: Runner | None = None) -> ImglobOutputs:
    """
    imglob

    Tool for generating image lists with specific extensions.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imglob_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMGLOB_METADATA)
    execution.params(params)
    args = imglob_cargs(params, execution)
    out = imglob_outputs(params, execution)
    execution.run(args)
    return out

def imglob(
    input_list: list[str],
    multiple_extensions: bool = False,
    single_extension: bool = False,
    runner: Runner | None = None,
) -> ImglobOutputs:
    """
    imglob

    Tool for generating image lists with specific extensions.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_list: List of image names or file paths
        multiple_extensions: Output list of images with full extensions
        single_extension: Output one image with full extension
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imglob_params(
        input_list=input_list,
        multiple_extensions=multiple_extensions,
        single_extension=single_extension,
    )
    return imglob_execute(params, runner)

__all__ = [
    "Imglob",
    "ImglobOutputs",
    "IMGLOB_METADATA",
    "imglob_cargs",
    "imglob_outputs",
    "imglob_params",
    "imglob_execute",
    "imglob_validate",
    "imglob",
]