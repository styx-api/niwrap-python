# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EDDY_QUAD_METADATA = Metadata(
    id="eddy_quad",
    name="eddy_quad",
    package="fsl",
)

EddyQuad = typing.TypedDict(
    "EddyQuad",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/eddy_quad"]],
        "eddyBase": str,
        "eddyIndex": InputPathType,
        "eddyParams": InputPathType,
        "mask": InputPathType,
        "bvals": InputPathType,
        "bvecs": typing.NotRequired[InputPathType],
        "output_dir": typing.NotRequired[str],
        "field": typing.NotRequired[InputPathType],
        "slspec": typing.NotRequired[InputPathType],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class EddyQuadOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dir_qc: OutputPathType | None
    """Quality control data outputs"""

def eddy_quad_params(
    eddyBase: str,
    eddyIndex: InputPathType,
    eddyParams: InputPathType,
    mask: InputPathType,
    bvals: InputPathType,
    bvecs: InputPathType | None = None,
    output_dir: str | None = None,
    field: InputPathType | None = None,
    slspec: InputPathType | None = None,
    verbose: bool = False,
) -> EddyQuad:
    """
    Build parameters.

    Args:
        eddyBase: Basename (including path) specified when running EDDY
        eddyIndex: File containing indices for all volumes into acquisition\
            parameters
        eddyParams: File containing acquisition parameters
        mask: Binary mask file
        bvals: b-values file
        bvecs: b-vectors file - only used when <eddyBase>.eddy_residuals file is\
            present
        output_dir: Output directory - default = '<eddyBase>.qc'
        field: TOPUP estimated field (in Hz)
        slspec: Text file specifying slice/group acquisition
        verbose: Display debug messages

    Returns:
        Parameter dictionary.
    """
    params: EddyQuad = {
        "@type": "fsl/eddy_quad",
        "eddyBase": eddyBase,
        "eddyIndex": eddyIndex,
        "eddyParams": eddyParams,
        "mask": mask,
        "bvals": bvals,
        "verbose": verbose,
    }
    if bvecs is not None:
        params["bvecs"] = bvecs
    if output_dir is not None:
        params["output_dir"] = output_dir
    if field is not None:
        params["field"] = field
    if slspec is not None:
        params["slspec"] = slspec
    return params

def eddy_quad_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid EddyQuad."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("eddyBase", None) is None:
        raise StyxValidationError("`eddyBase` must not be None")
    if not isinstance(params["eddyBase"], str):
        raise StyxValidationError(f'`eddyBase` has the wrong type: Received `{type(params["eddyBase"])}` expected `str`')
    if params.get("eddyIndex", None) is None:
        raise StyxValidationError("`eddyIndex` must not be None")
    if not isinstance(params["eddyIndex"], (pathlib.Path, str)):
        raise StyxValidationError(f'`eddyIndex` has the wrong type: Received `{type(params["eddyIndex"])}` expected `InputPathType`')
    if params.get("eddyParams", None) is None:
        raise StyxValidationError("`eddyParams` must not be None")
    if not isinstance(params["eddyParams"], (pathlib.Path, str)):
        raise StyxValidationError(f'`eddyParams` has the wrong type: Received `{type(params["eddyParams"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("bvals", None) is None:
        raise StyxValidationError("`bvals` must not be None")
    if not isinstance(params["bvals"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["bvals"])}` expected `InputPathType`')
    if params.get("bvecs", None) is not None:
        if not isinstance(params["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["bvecs"])}` expected `InputPathType`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("field", None) is not None:
        if not isinstance(params["field"], (pathlib.Path, str)):
            raise StyxValidationError(f'`field` has the wrong type: Received `{type(params["field"])}` expected `InputPathType`')
    if params.get("slspec", None) is not None:
        if not isinstance(params["slspec"], (pathlib.Path, str)):
            raise StyxValidationError(f'`slspec` has the wrong type: Received `{type(params["slspec"])}` expected `InputPathType`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def eddy_quad_cargs(params: EddyQuad, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("eddy_quad")
    cargs.append(params["eddyBase"])
    cargs.append("--eddyIdx")
    cargs.append(execution.input_file(params["eddyIndex"]))
    cargs.append("--eddyParams")
    cargs.append(execution.input_file(params["eddyParams"]))
    cargs.append("--mask")
    cargs.append(execution.input_file(params["mask"]))
    cargs.append("--bvals")
    cargs.append(execution.input_file(params["bvals"]))
    v_0 = params.get("bvecs")
    if v_0 is not None:
        cargs.append("--bvecs")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("output_dir")
    if v_1 is not None:
        cargs.append("--output-dir")
        cargs.append(v_1)
    v_2 = params.get("field")
    if v_2 is not None:
        cargs.append("--field")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("slspec")
    if v_3 is not None:
        cargs.append("--slspec")
        cargs.append(execution.input_file(v_3))
    if params.get("verbose"):
        cargs.append("--verbose")
    return cargs

def eddy_quad_outputs(params: EddyQuad, execution: Execution) -> EddyQuadOutputs:
    """Build the EddyQuadOutputs object for this tool."""
    output_dir_qc_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        output_dir_qc_v = execution.output_file((__v0 if __v0 is not None else ""))
    return EddyQuadOutputs(
        root=root_v,
        output_dir_qc=output_dir_qc_v,
    )

def eddy_quad_execute(params: EddyQuad, runner: Runner | None = None) -> EddyQuadOutputs:
    """
    eddy_quad

    QUality Assessment for DMRI

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    eddy_quad_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EDDY_QUAD_METADATA)
    execution.params(params)
    args = eddy_quad_cargs(params, execution)
    out = eddy_quad_outputs(params, execution)
    execution.run(args)
    return out

def eddy_quad(
    eddyBase: str,
    eddyIndex: InputPathType,
    eddyParams: InputPathType,
    mask: InputPathType,
    bvals: InputPathType,
    bvecs: InputPathType | None = None,
    output_dir: str | None = None,
    field: InputPathType | None = None,
    slspec: InputPathType | None = None,
    verbose: bool = False,
    runner: Runner | None = None,
) -> EddyQuadOutputs:
    """
    eddy_quad

    QUality Assessment for DMRI

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        eddyBase: Basename (including path) specified when running EDDY
        eddyIndex: File containing indices for all volumes into acquisition\
            parameters
        eddyParams: File containing acquisition parameters
        mask: Binary mask file
        bvals: b-values file
        bvecs: b-vectors file - only used when <eddyBase>.eddy_residuals file is\
            present
        output_dir: Output directory - default = '<eddyBase>.qc'
        field: TOPUP estimated field (in Hz)
        slspec: Text file specifying slice/group acquisition
        verbose: Display debug messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = eddy_quad_params(
        eddyBase=eddyBase,
        eddyIndex=eddyIndex,
        eddyParams=eddyParams,
        mask=mask,
        bvals=bvals,
        bvecs=bvecs,
        output_dir=output_dir,
        field=field,
        slspec=slspec,
        verbose=verbose,
    )
    return eddy_quad_execute(params, runner)

__all__ = [
    "EddyQuad",
    "EddyQuadOutputs",
    "EDDY_QUAD_METADATA",
    "eddy_quad_cargs",
    "eddy_quad_outputs",
    "eddy_quad_params",
    "eddy_quad_execute",
    "eddy_quad_validate",
    "eddy_quad",
]