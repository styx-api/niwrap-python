# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EDDY_SQUAD_METADATA = Metadata(
    id="eddy_squad",
    name="eddy_squad",
    package="fsl",
)

EddySquad = typing.TypedDict(
    "EddySquad",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/eddy_squad"]],
        "grouping": typing.NotRequired[str],
        "group_db": typing.NotRequired[InputPathType],
        "update": typing.NotRequired[bool],
        "output_dir": typing.NotRequired[str],
        "subject_list": str,
    },
)

@dataclasses.dataclass
class EddySquadOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    qc_results: OutputPathType | None
    """QC results in JSON format"""

def eddy_squad_params(
    subject_list: str,
    grouping: str | None = None,
    group_db: InputPathType | None = None,
    update: bool = False,
    output_dir: str | None = None,
) -> EddySquad:
    """
    Build parameters.

    Args:
        subject_list: List of subject IDs for the QC.
        grouping: Specifies the grouping of studies.
        group_db: Path to the group database.
        update: Option to update the QC results.
        output_dir: Output directory for the QC results.

    Returns:
        Parameter dictionary.
    """
    params: EddySquad = {
        "@type": "fsl/eddy_squad",
        "subject_list": subject_list,
        "update": update,
    }
    if grouping is not None:
        params["grouping"] = grouping
    if group_db is not None:
        params["group_db"] = group_db
    if output_dir is not None:
        params["output_dir"] = output_dir
    return params

def eddy_squad_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid EddySquad."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("grouping", None) is not None:
        if not isinstance(params["grouping"], str):
            raise StyxValidationError(f'`grouping` has the wrong type: Received `{type(params["grouping"])}` expected `str`')
    if params.get("group_db", None) is not None:
        if not isinstance(params["group_db"], (pathlib.Path, str)):
            raise StyxValidationError(f'`group_db` has the wrong type: Received `{type(params["group_db"])}` expected `InputPathType`')
    if params.get("update", None) is not None:
        if not isinstance(params["update"], bool):
            raise StyxValidationError(f'`update` has the wrong type: Received `{type(params["update"])}` expected `bool`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("subject_list", None) is None:
        raise StyxValidationError("`subject_list` must not be None")
    if not isinstance(params["subject_list"], str):
        raise StyxValidationError(f'`subject_list` has the wrong type: Received `{type(params["subject_list"])}` expected `str`')

def eddy_squad_cargs(params: EddySquad, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("eddy_squad")
    v_0 = params.get("grouping")
    if v_0 is not None:
        cargs.append("-g")
        cargs.append(v_0)
    v_1 = params.get("group_db")
    if v_1 is not None:
        cargs.append("-gdb")
        cargs.append(execution.input_file(v_1))
    if params.get("update"):
        cargs.append("-u")
    v_2 = params.get("output_dir")
    if v_2 is not None:
        cargs.append("-o")
        cargs.append(v_2)
    cargs.append(params["subject_list"])
    return cargs

def eddy_squad_outputs(params: EddySquad, execution: Execution) -> EddySquadOutputs:
    """Build the EddySquadOutputs object for this tool."""
    qc_results_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_dir")
    if __v0 is not None:
        qc_results_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/qc_results.json')
    return EddySquadOutputs(
        root=root_v,
        qc_results=qc_results_v,
    )

def eddy_squad_execute(params: EddySquad, runner: Runner | None = None) -> EddySquadOutputs:
    """
    eddy_squad

    Study-wise QC for dMRI data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    eddy_squad_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EDDY_SQUAD_METADATA)
    execution.params(params)
    args = eddy_squad_cargs(params, execution)
    out = eddy_squad_outputs(params, execution)
    execution.run(args)
    return out

def eddy_squad(
    subject_list: str,
    grouping: str | None = None,
    group_db: InputPathType | None = None,
    update: bool = False,
    output_dir: str | None = None,
    runner: Runner | None = None,
) -> EddySquadOutputs:
    """
    eddy_squad

    Study-wise QC for dMRI data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        subject_list: List of subject IDs for the QC.
        grouping: Specifies the grouping of studies.
        group_db: Path to the group database.
        update: Option to update the QC results.
        output_dir: Output directory for the QC results.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = eddy_squad_params(
        subject_list=subject_list,
        grouping=grouping,
        group_db=group_db,
        update=update,
        output_dir=output_dir,
    )
    return eddy_squad_execute(params, runner)

__all__ = [
    "EddySquad",
    "EddySquadOutputs",
    "EDDY_SQUAD_METADATA",
    "eddy_squad_cargs",
    "eddy_squad_outputs",
    "eddy_squad_params",
    "eddy_squad_execute",
    "eddy_squad_validate",
    "eddy_squad",
]