# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CALC_GRAD_PERC_DEV_METADATA = Metadata(
    id="calc_grad_perc_dev",
    name="calc_grad_perc_dev",
    package="fsl",
)

CalcGradPercDev = typing.TypedDict(
    "CalcGradPercDev",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/calc_grad_perc_dev"]],
        "fullwarp_image": InputPathType,
        "out_basename": str,
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CalcGradPercDevOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def calc_grad_perc_dev_params(
    fullwarp_image: InputPathType,
    out_basename: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> CalcGradPercDev:
    """
    Build parameters.

    Args:
        fullwarp_image: Full warp image from gradient_unwarp.py
        out_basename: Output basename
        verbose_flag: Switch on diagnostic messages
        help_flag: Display the help message

    Returns:
        Parameter dictionary.
    """
    params: CalcGradPercDev = {
        "@type": "fsl/calc_grad_perc_dev",
        "fullwarp_image": fullwarp_image,
        "out_basename": out_basename,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    return params

def calc_grad_perc_dev_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CalcGradPercDev."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fullwarp_image", None) is None:
        raise StyxValidationError("`fullwarp_image` must not be None")
    if not isinstance(params["fullwarp_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fullwarp_image` has the wrong type: Received `{type(params["fullwarp_image"])}` expected `InputPathType`')
    if params.get("out_basename", None) is None:
        raise StyxValidationError("`out_basename` must not be None")
    if not isinstance(params["out_basename"], str):
        raise StyxValidationError(f'`out_basename` has the wrong type: Received `{type(params["out_basename"])}` expected `str`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def calc_grad_perc_dev_cargs(params: CalcGradPercDev, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("calc_grad_perc_dev")
    cargs.append("--fullwarp")
    cargs.append(execution.input_file(params["fullwarp_image"]))
    cargs.append("-o,--out")
    cargs.append(params["out_basename"])
    if params.get("verbose_flag"):
        cargs.append("-v,--verbose")
    if params.get("help_flag"):
        cargs.append("-h,--help")
    return cargs

def calc_grad_perc_dev_outputs(params: CalcGradPercDev, execution: Execution) -> CalcGradPercDevOutputs:
    """Build the CalcGradPercDevOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return CalcGradPercDevOutputs(
        root=root_v,
    )

def calc_grad_perc_dev_execute(params: CalcGradPercDev, runner: Runner | None = None) -> CalcGradPercDevOutputs:
    """
    calc_grad_perc_dev

    Compute the gradient percent deviation based on a full warp image from gradient_unwarp.py

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    calc_grad_perc_dev_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CALC_GRAD_PERC_DEV_METADATA)
    execution.params(params)
    args = calc_grad_perc_dev_cargs(params, execution)
    out = calc_grad_perc_dev_outputs(params, execution)
    execution.run(args)
    return out

def calc_grad_perc_dev(
    fullwarp_image: InputPathType,
    out_basename: str,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> CalcGradPercDevOutputs:
    """
    calc_grad_perc_dev

    Compute the gradient percent deviation based on a full warp image from gradient_unwarp.py

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        fullwarp_image: Full warp image from gradient_unwarp.py
        out_basename: Output basename
        verbose_flag: Switch on diagnostic messages
        help_flag: Display the help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = calc_grad_perc_dev_params(
        fullwarp_image=fullwarp_image,
        out_basename=out_basename,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return calc_grad_perc_dev_execute(params, runner)

__all__ = [
    "CalcGradPercDev",
    "CalcGradPercDevOutputs",
    "CALC_GRAD_PERC_DEV_METADATA",
    "calc_grad_perc_dev_cargs",
    "calc_grad_perc_dev_outputs",
    "calc_grad_perc_dev_params",
    "calc_grad_perc_dev_execute",
    "calc_grad_perc_dev_validate",
    "calc_grad_perc_dev",
]