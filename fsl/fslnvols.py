# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLNVOLS_METADATA = Metadata(
    id="fslnvols",
    name="fslnvols",
    package="fsl",
)

Fslnvols = typing.TypedDict(
    "Fslnvols",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslnvols"]],
        "infile": InputPathType,
    },
)

@dataclasses.dataclass
class FslnvolsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslnvols_params(
    infile: InputPathType,
) -> Fslnvols:
    """
    Build parameters.

    Args:
        infile: Input NIfTI file (e.g., fmri.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: Fslnvols = {
        "@type": "fsl/fslnvols",
        "infile": infile,
    }
    return params

def fslnvols_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslnvols."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')

def fslnvols_cargs(params: Fslnvols, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslnvols")
    cargs.append(execution.input_file(params["infile"]))
    return cargs

def fslnvols_outputs(params: Fslnvols, execution: Execution) -> FslnvolsOutputs:
    """Build the FslnvolsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslnvolsOutputs(
        root=root_v,
    )

def fslnvols_execute(params: Fslnvols, runner: Runner | None = None) -> FslnvolsOutputs:
    """
    fslnvols

    Retrieve the number of volumes in a 4D NIfTI file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslnvols_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLNVOLS_METADATA)
    execution.params(params)
    args = fslnvols_cargs(params, execution)
    out = fslnvols_outputs(params, execution)
    execution.run(args)
    return out

def fslnvols(
    infile: InputPathType,
    runner: Runner | None = None,
) -> FslnvolsOutputs:
    """
    fslnvols

    Retrieve the number of volumes in a 4D NIfTI file

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input NIfTI file (e.g., fmri.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslnvols_params(
        infile=infile,
    )
    return fslnvols_execute(params, runner)

__all__ = [
    "Fslnvols",
    "FslnvolsOutputs",
    "FSLNVOLS_METADATA",
    "fslnvols_cargs",
    "fslnvols_outputs",
    "fslnvols_params",
    "fslnvols_execute",
    "fslnvols_validate",
    "fslnvols",
]