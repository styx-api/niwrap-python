# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POSSUM_PLOT_PY_METADATA = Metadata(
    id="possum_plot.py",
    name="possum_plot.py",
    package="fsl",
)

PossumPlotPy = typing.TypedDict(
    "PossumPlotPy",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/possum_plot.py"]],
        "input_file": InputPathType,
        "output_basename": str,
    },
)

@dataclasses.dataclass
class PossumPlotPyOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def possum_plot_py_params(
    input_file: InputPathType,
    output_basename: str,
) -> PossumPlotPy:
    """
    Build parameters.

    Args:
        input_file: Input file from POSSUM simulation (e.g. simulation_results.txt)
        output_basename: Basename for output files (e.g. plot_output)

    Returns:
        Parameter dictionary.
    """
    params: PossumPlotPy = {
        "@type": "fsl/possum_plot.py",
        "input_file": input_file,
        "output_basename": output_basename,
    }
    return params

def possum_plot_py_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PossumPlotPy."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_basename", None) is None:
        raise StyxValidationError("`output_basename` must not be None")
    if not isinstance(params["output_basename"], str):
        raise StyxValidationError(f'`output_basename` has the wrong type: Received `{type(params["output_basename"])}` expected `str`')

def possum_plot_py_cargs(params: PossumPlotPy, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("possum_plot.py")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_basename"])
    return cargs

def possum_plot_py_outputs(params: PossumPlotPy, execution: Execution) -> PossumPlotPyOutputs:
    """Build the PossumPlotPyOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PossumPlotPyOutputs(
        root=root_v,
    )

def possum_plot_py_execute(params: PossumPlotPy, runner: Runner | None = None) -> PossumPlotPyOutputs:
    """
    possum_plot.py

    Tool for plotting results from POSSUM simulations in FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    possum_plot_py_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POSSUM_PLOT_PY_METADATA)
    execution.params(params)
    args = possum_plot_py_cargs(params, execution)
    out = possum_plot_py_outputs(params, execution)
    execution.run(args)
    return out

def possum_plot_py(
    input_file: InputPathType,
    output_basename: str,
    runner: Runner | None = None,
) -> PossumPlotPyOutputs:
    """
    possum_plot.py

    Tool for plotting results from POSSUM simulations in FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input file from POSSUM simulation (e.g. simulation_results.txt)
        output_basename: Basename for output files (e.g. plot_output)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = possum_plot_py_params(
        input_file=input_file,
        output_basename=output_basename,
    )
    return possum_plot_py_execute(params, runner)

__all__ = [
    "PossumPlotPy",
    "PossumPlotPyOutputs",
    "POSSUM_PLOT_PY_METADATA",
    "possum_plot_py_cargs",
    "possum_plot_py_outputs",
    "possum_plot_py_params",
    "possum_plot_py_execute",
    "possum_plot_py_validate",
    "possum_plot_py",
]