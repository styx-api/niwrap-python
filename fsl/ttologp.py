# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TTOLOGP_METADATA = Metadata(
    id="ttologp",
    name="ttologp",
    package="fsl",
)

Ttologp = typing.TypedDict(
    "Ttologp",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/ttologp"]],
        "varsfile": InputPathType,
        "cbsfile": InputPathType,
        "dof": str,
        "outputvol": typing.NotRequired[str],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TtologpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_logpvol: OutputPathType | None
    """Output volume for logp value"""

def ttologp_params(
    varsfile: InputPathType,
    cbsfile: InputPathType,
    dof: str,
    outputvol: str | None = None,
    help_flag: bool = False,
) -> Ttologp:
    """
    Build parameters.

    Args:
        varsfile: Path to the vars file
        cbsfile: Path to the cbs file
        dof: Degree of freedom
        outputvol: Output volume for logp value (default is logps)
        help_flag: Display help information and exit

    Returns:
        Parameter dictionary.
    """
    params: Ttologp = {
        "@type": "fsl/ttologp",
        "varsfile": varsfile,
        "cbsfile": cbsfile,
        "dof": dof,
        "help_flag": help_flag,
    }
    if outputvol is not None:
        params["outputvol"] = outputvol
    return params

def ttologp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Ttologp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("varsfile", None) is None:
        raise StyxValidationError("`varsfile` must not be None")
    if not isinstance(params["varsfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`varsfile` has the wrong type: Received `{type(params["varsfile"])}` expected `InputPathType`')
    if params.get("cbsfile", None) is None:
        raise StyxValidationError("`cbsfile` must not be None")
    if not isinstance(params["cbsfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`cbsfile` has the wrong type: Received `{type(params["cbsfile"])}` expected `InputPathType`')
    if params.get("dof", None) is None:
        raise StyxValidationError("`dof` must not be None")
    if not isinstance(params["dof"], str):
        raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `str`')
    if params.get("outputvol", None) is not None:
        if not isinstance(params["outputvol"], str):
            raise StyxValidationError(f'`outputvol` has the wrong type: Received `{type(params["outputvol"])}` expected `str`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def ttologp_cargs(params: Ttologp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ttologp")
    cargs.append(execution.input_file(params["varsfile"]))
    cargs.append(execution.input_file(params["cbsfile"]))
    cargs.append(params["dof"])
    v_0 = params.get("outputvol")
    if v_0 is not None:
        cargs.append("-logpout")
        cargs.append(v_0)
    if params.get("help_flag"):
        cargs.append("-help")
    return cargs

def ttologp_outputs(params: Ttologp, execution: Execution) -> TtologpOutputs:
    """Build the TtologpOutputs object for this tool."""
    output_logpvol_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outputvol")
    if __v0 is not None:
        output_logpvol_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    return TtologpOutputs(
        root=root_v,
        output_logpvol=output_logpvol_v,
    )

def ttologp_execute(params: Ttologp, runner: Runner | None = None) -> TtologpOutputs:
    """
    ttologp

    Tool for computing logp

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ttologp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TTOLOGP_METADATA)
    execution.params(params)
    args = ttologp_cargs(params, execution)
    out = ttologp_outputs(params, execution)
    execution.run(args)
    return out

def ttologp(
    varsfile: InputPathType,
    cbsfile: InputPathType,
    dof: str,
    outputvol: str | None = None,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> TtologpOutputs:
    """
    ttologp

    Tool for computing logp

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        varsfile: Path to the vars file
        cbsfile: Path to the cbs file
        dof: Degree of freedom
        outputvol: Output volume for logp value (default is logps)
        help_flag: Display help information and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ttologp_params(
        varsfile=varsfile,
        cbsfile=cbsfile,
        dof=dof,
        outputvol=outputvol,
        help_flag=help_flag,
    )
    return ttologp_execute(params, runner)

__all__ = [
    "Ttologp",
    "TtologpOutputs",
    "TTOLOGP_METADATA",
    "ttologp_cargs",
    "ttologp_outputs",
    "ttologp_params",
    "ttologp_execute",
    "ttologp_validate",
    "ttologp",
]