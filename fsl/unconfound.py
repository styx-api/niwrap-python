# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

UNCONFOUND_METADATA = Metadata(
    id="unconfound",
    name="unconfound",
    package="fsl",
)

Unconfound = typing.TypedDict(
    "Unconfound",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/unconfound"]],
        "in4d": InputPathType,
        "out4d": str,
        "confound_mat": InputPathType,
    },
)

@dataclasses.dataclass
class UnconfoundOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_4d: OutputPathType
    """Output 4D fMRI data after removing confounds"""

def unconfound_params(
    in4d: InputPathType,
    out4d: str,
    confound_mat: InputPathType,
) -> Unconfound:
    """
    Build parameters.

    Args:
        in4d: Input 4D fMRI data (e.g., in4d.nii.gz)
        out4d: Output 4D fMRI data after removing confounds (e.g., out4d.nii.gz)
        confound_mat: Confound matrix file (e.g., confound.mat)

    Returns:
        Parameter dictionary.
    """
    params: Unconfound = {
        "@type": "fsl/unconfound",
        "in4d": in4d,
        "out4d": out4d,
        "confound_mat": confound_mat,
    }
    return params

def unconfound_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Unconfound."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in4d", None) is None:
        raise StyxValidationError("`in4d` must not be None")
    if not isinstance(params["in4d"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in4d` has the wrong type: Received `{type(params["in4d"])}` expected `InputPathType`')
    if params.get("out4d", None) is None:
        raise StyxValidationError("`out4d` must not be None")
    if not isinstance(params["out4d"], str):
        raise StyxValidationError(f'`out4d` has the wrong type: Received `{type(params["out4d"])}` expected `str`')
    if params.get("confound_mat", None) is None:
        raise StyxValidationError("`confound_mat` must not be None")
    if not isinstance(params["confound_mat"], (pathlib.Path, str)):
        raise StyxValidationError(f'`confound_mat` has the wrong type: Received `{type(params["confound_mat"])}` expected `InputPathType`')

def unconfound_cargs(params: Unconfound, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("unconfound")
    cargs.append(execution.input_file(params["in4d"]))
    cargs.append(params["out4d"])
    cargs.append(execution.input_file(params["confound_mat"]))
    return cargs

def unconfound_outputs(params: Unconfound, execution: Execution) -> UnconfoundOutputs:
    """Build the UnconfoundOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_4d_v: OutputPathType = execution.output_file(f'{params["out4d"]}.nii.gz')
    return UnconfoundOutputs(
        root=root_v,
        output_4d=output_4d_v,
    )

def unconfound_execute(params: Unconfound, runner: Runner | None = None) -> UnconfoundOutputs:
    """
    unconfound

    Removing confounds from 4D fMRI data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    unconfound_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(UNCONFOUND_METADATA)
    execution.params(params)
    args = unconfound_cargs(params, execution)
    out = unconfound_outputs(params, execution)
    execution.run(args)
    return out

def unconfound(
    in4d: InputPathType,
    out4d: str,
    confound_mat: InputPathType,
    runner: Runner | None = None,
) -> UnconfoundOutputs:
    """
    unconfound

    Removing confounds from 4D fMRI data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        in4d: Input 4D fMRI data (e.g., in4d.nii.gz)
        out4d: Output 4D fMRI data after removing confounds (e.g., out4d.nii.gz)
        confound_mat: Confound matrix file (e.g., confound.mat)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = unconfound_params(
        in4d=in4d,
        out4d=out4d,
        confound_mat=confound_mat,
    )
    return unconfound_execute(params, runner)

__all__ = [
    "Unconfound",
    "UnconfoundOutputs",
    "UNCONFOUND_METADATA",
    "unconfound_cargs",
    "unconfound_outputs",
    "unconfound_params",
    "unconfound_execute",
    "unconfound_validate",
    "unconfound",
]