# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLSLICE_METADATA = Metadata(
    id="fslslice",
    name="fslslice",
    package="fsl",
)

Fslslice = typing.TypedDict(
    "Fslslice",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslslice"]],
        "volume": InputPathType,
        "output_basename": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FslsliceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslslice_params(
    volume: InputPathType,
    output_basename: str | None = None,
) -> Fslslice:
    """
    Build parameters.

    Args:
        volume: Input 3D volume (e.g. volume.nii.gz)
        output_basename: Output basename for extracted slices

    Returns:
        Parameter dictionary.
    """
    params: Fslslice = {
        "@type": "fsl/fslslice",
        "volume": volume,
    }
    if output_basename is not None:
        params["output_basename"] = output_basename
    return params

def fslslice_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslslice."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("volume", None) is None:
        raise StyxValidationError("`volume` must not be None")
    if not isinstance(params["volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`volume` has the wrong type: Received `{type(params["volume"])}` expected `InputPathType`')
    if params.get("output_basename", None) is not None:
        if not isinstance(params["output_basename"], str):
            raise StyxValidationError(f'`output_basename` has the wrong type: Received `{type(params["output_basename"])}` expected `str`')

def fslslice_cargs(params: Fslslice, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslslice")
    cargs.append(execution.input_file(params["volume"]))
    v_0 = params.get("output_basename")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fslslice_outputs(params: Fslslice, execution: Execution) -> FslsliceOutputs:
    """Build the FslsliceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslsliceOutputs(
        root=root_v,
    )

def fslslice_execute(params: Fslslice, runner: Runner | None = None) -> FslsliceOutputs:
    """
    fslslice

    Tool to extract all slices from a 3D volume and store as 2D images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslslice_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLSLICE_METADATA)
    execution.params(params)
    args = fslslice_cargs(params, execution)
    out = fslslice_outputs(params, execution)
    execution.run(args)
    return out

def fslslice(
    volume: InputPathType,
    output_basename: str | None = None,
    runner: Runner | None = None,
) -> FslsliceOutputs:
    """
    fslslice

    Tool to extract all slices from a 3D volume and store as 2D images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        volume: Input 3D volume (e.g. volume.nii.gz)
        output_basename: Output basename for extracted slices
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslslice_params(
        volume=volume,
        output_basename=output_basename,
    )
    return fslslice_execute(params, runner)

__all__ = [
    "Fslslice",
    "FslsliceOutputs",
    "FSLSLICE_METADATA",
    "fslslice_cargs",
    "fslslice_outputs",
    "fslslice_params",
    "fslslice_execute",
    "fslslice_validate",
    "fslslice",
]