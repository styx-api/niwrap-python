# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLPSPEC_METADATA = Metadata(
    id="fslpspec",
    name="fslpspec",
    package="fsl",
)

Fslpspec = typing.TypedDict(
    "Fslpspec",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslpspec"]],
        "infile": InputPathType,
        "outfile": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FslpspecOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_pspec: OutputPathType | None
    """Output power spectrum image"""

def fslpspec_params(
    infile: InputPathType,
    outfile: str | None = None,
) -> Fslpspec:
    """
    Build parameters.

    Args:
        infile: Input 4D fMRI time series image (e.g. fMRI_data.nii.gz)
        outfile: Output power spectrum image (e.g. pspec_data.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: Fslpspec = {
        "@type": "fsl/fslpspec",
        "infile": infile,
    }
    if outfile is not None:
        params["outfile"] = outfile
    return params

def fslpspec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslpspec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is not None:
        if not isinstance(params["outfile"], str):
            raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def fslpspec_cargs(params: Fslpspec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslpspec")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("outfile")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def fslpspec_outputs(params: Fslpspec, execution: Execution) -> FslpspecOutputs:
    """Build the FslpspecOutputs object for this tool."""
    output_pspec_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("outfile")
    if __v0 is not None:
        output_pspec_v = execution.output_file((__v0 if __v0 is not None else ""))
    return FslpspecOutputs(
        root=root_v,
        output_pspec=output_pspec_v,
    )

def fslpspec_execute(params: Fslpspec, runner: Runner | None = None) -> FslpspecOutputs:
    """
    fslpspec

    Estimate the power spectrum of 4D fMRI time series data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslpspec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLPSPEC_METADATA)
    execution.params(params)
    args = fslpspec_cargs(params, execution)
    out = fslpspec_outputs(params, execution)
    execution.run(args)
    return out

def fslpspec(
    infile: InputPathType,
    outfile: str | None = None,
    runner: Runner | None = None,
) -> FslpspecOutputs:
    """
    fslpspec

    Estimate the power spectrum of 4D fMRI time series data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input 4D fMRI time series image (e.g. fMRI_data.nii.gz)
        outfile: Output power spectrum image (e.g. pspec_data.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslpspec_params(
        infile=infile,
        outfile=outfile,
    )
    return fslpspec_execute(params, runner)

__all__ = [
    "Fslpspec",
    "FslpspecOutputs",
    "FSLPSPEC_METADATA",
    "fslpspec_cargs",
    "fslpspec_outputs",
    "fslpspec_params",
    "fslpspec_execute",
    "fslpspec_validate",
    "fslpspec",
]