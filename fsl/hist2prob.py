# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

HIST2PROB_METADATA = Metadata(
    id="hist2prob",
    name="hist2prob",
    package="fsl",
)

Hist2prob = typing.TypedDict(
    "Hist2prob",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/hist2prob"]],
        "image": InputPathType,
        "size": int,
        "low_threshold": float,
        "high_threshold": float,
    },
)

@dataclasses.dataclass
class Hist2probOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_probability_map: OutputPathType
    """Output probability map converted from histogram"""

def hist2prob_params(
    image: InputPathType,
    size: int,
    low_threshold: float,
    high_threshold: float,
) -> Hist2prob:
    """
    Build parameters.

    Args:
        image: Input histogram image
        size: Size of the histogram
        low_threshold: Lower threshold for probability conversion
        high_threshold: Higher threshold for probability conversion

    Returns:
        Parameter dictionary.
    """
    params: Hist2prob = {
        "@type": "fsl/hist2prob",
        "image": image,
        "size": size,
        "low_threshold": low_threshold,
        "high_threshold": high_threshold,
    }
    return params

def hist2prob_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Hist2prob."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("size", None) is None:
        raise StyxValidationError("`size` must not be None")
    if not isinstance(params["size"], int):
        raise StyxValidationError(f'`size` has the wrong type: Received `{type(params["size"])}` expected `int`')
    if params.get("low_threshold", None) is None:
        raise StyxValidationError("`low_threshold` must not be None")
    if not isinstance(params["low_threshold"], (float, int)):
        raise StyxValidationError(f'`low_threshold` has the wrong type: Received `{type(params["low_threshold"])}` expected `float`')
    if params.get("high_threshold", None) is None:
        raise StyxValidationError("`high_threshold` must not be None")
    if not isinstance(params["high_threshold"], (float, int)):
        raise StyxValidationError(f'`high_threshold` has the wrong type: Received `{type(params["high_threshold"])}` expected `float`')

def hist2prob_cargs(params: Hist2prob, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("hist2prob")
    cargs.append(execution.input_file(params["image"]))
    cargs.append(str(params["size"]))
    cargs.append(str(params["low_threshold"]))
    cargs.append(str(params["high_threshold"]))
    return cargs

def hist2prob_outputs(params: Hist2prob, execution: Execution) -> Hist2probOutputs:
    """Build the Hist2probOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_probability_map_v: OutputPathType = execution.output_file(f'{params["image"]}_probability_map.nii.gz')
    return Hist2probOutputs(
        root=root_v,
        output_probability_map=output_probability_map_v,
    )

def hist2prob_execute(params: Hist2prob, runner: Runner | None = None) -> Hist2probOutputs:
    """
    hist2prob

    Converts a histogram image to a probability map based on specified thresholds

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    hist2prob_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(HIST2PROB_METADATA)
    execution.params(params)
    args = hist2prob_cargs(params, execution)
    out = hist2prob_outputs(params, execution)
    execution.run(args)
    return out

def hist2prob(
    image: InputPathType,
    size: int,
    low_threshold: float,
    high_threshold: float,
    runner: Runner | None = None,
) -> Hist2probOutputs:
    """
    hist2prob

    Converts a histogram image to a probability map based on specified thresholds

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        image: Input histogram image
        size: Size of the histogram
        low_threshold: Lower threshold for probability conversion
        high_threshold: Higher threshold for probability conversion
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = hist2prob_params(
        image=image,
        size=size,
        low_threshold=low_threshold,
        high_threshold=high_threshold,
    )
    return hist2prob_execute(params, runner)

__all__ = [
    "Hist2prob",
    "Hist2probOutputs",
    "HIST2PROB_METADATA",
    "hist2prob_cargs",
    "hist2prob_outputs",
    "hist2prob_params",
    "hist2prob_execute",
    "hist2prob_validate",
    "hist2prob",
]