# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BEDPOSTX_GPU_METADATA = Metadata(
    id="bedpostx_gpu",
    name="bedpostx_gpu",
    package="fsl",
)

BedpostxGpu = typing.TypedDict(
    "BedpostxGpu",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/bedpostx_gpu"]],
        "subject_dir": str,
        "gpu_queue": typing.NotRequired[str],
        "num_jobs": typing.NotRequired[float],
        "num_fibers": typing.NotRequired[float],
        "ard_weight": typing.NotRequired[float],
        "burnin_period": typing.NotRequired[float],
        "num_jumps": typing.NotRequired[float],
        "sample_every": typing.NotRequired[float],
        "deconv_model": typing.NotRequired[float],
        "grad_nonlinear": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class BedpostxGpuOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def bedpostx_gpu_params(
    subject_dir: str,
    gpu_queue: str | None = None,
    num_jobs: float | None = None,
    num_fibers: float | None = None,
    ard_weight: float | None = None,
    burnin_period: float | None = None,
    num_jumps: float | None = None,
    sample_every: float | None = None,
    deconv_model: float | None = None,
    grad_nonlinear: bool = False,
) -> BedpostxGpu:
    """
    Build parameters.

    Args:
        subject_dir: Directory containing the subject's DWI data and associated\
            files (bvals, bvecs, data, nodif_brain_mask).
        gpu_queue: Name of the GPU(s) queue. Default: --coprocessor=cuda to let\
            fsl_sub decide on the queue
        num_jobs: Number of jobs to queue. The data is divided into this number of\
            parts, useful for a GPU cluster. Default: 4
        num_fibers: Number of fibres per voxel. Default: 3
        ard_weight: Automatic Relevance Determination (ARD) weight. More weight\
            means fewer secondary fibres per voxel. Default: 1
        burnin_period: Burn-in period. Default: 1000
        num_jumps: Number of jumps. Default: 1250
        sample_every: Sample every N steps. Default: 25
        deconv_model: Deconvolution model. 1: with sticks, 2: with sticks with a\
            range of diffusivities (default), 3: with zeppelins
        grad_nonlinear: Consider gradient nonlinearities. Default: off

    Returns:
        Parameter dictionary.
    """
    params: BedpostxGpu = {
        "@type": "fsl/bedpostx_gpu",
        "subject_dir": subject_dir,
        "grad_nonlinear": grad_nonlinear,
    }
    if gpu_queue is not None:
        params["gpu_queue"] = gpu_queue
    if num_jobs is not None:
        params["num_jobs"] = num_jobs
    if num_fibers is not None:
        params["num_fibers"] = num_fibers
    if ard_weight is not None:
        params["ard_weight"] = ard_weight
    if burnin_period is not None:
        params["burnin_period"] = burnin_period
    if num_jumps is not None:
        params["num_jumps"] = num_jumps
    if sample_every is not None:
        params["sample_every"] = sample_every
    if deconv_model is not None:
        params["deconv_model"] = deconv_model
    return params

def bedpostx_gpu_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BedpostxGpu."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("subject_dir", None) is None:
        raise StyxValidationError("`subject_dir` must not be None")
    if not isinstance(params["subject_dir"], str):
        raise StyxValidationError(f'`subject_dir` has the wrong type: Received `{type(params["subject_dir"])}` expected `str`')
    if params.get("gpu_queue", None) is not None:
        if not isinstance(params["gpu_queue"], str):
            raise StyxValidationError(f'`gpu_queue` has the wrong type: Received `{type(params["gpu_queue"])}` expected `str`')
    if params.get("num_jobs", None) is not None:
        if not isinstance(params["num_jobs"], (float, int)):
            raise StyxValidationError(f'`num_jobs` has the wrong type: Received `{type(params["num_jobs"])}` expected `float`')
    if params.get("num_fibers", None) is not None:
        if not isinstance(params["num_fibers"], (float, int)):
            raise StyxValidationError(f'`num_fibers` has the wrong type: Received `{type(params["num_fibers"])}` expected `float`')
    if params.get("ard_weight", None) is not None:
        if not isinstance(params["ard_weight"], (float, int)):
            raise StyxValidationError(f'`ard_weight` has the wrong type: Received `{type(params["ard_weight"])}` expected `float`')
    if params.get("burnin_period", None) is not None:
        if not isinstance(params["burnin_period"], (float, int)):
            raise StyxValidationError(f'`burnin_period` has the wrong type: Received `{type(params["burnin_period"])}` expected `float`')
    if params.get("num_jumps", None) is not None:
        if not isinstance(params["num_jumps"], (float, int)):
            raise StyxValidationError(f'`num_jumps` has the wrong type: Received `{type(params["num_jumps"])}` expected `float`')
    if params.get("sample_every", None) is not None:
        if not isinstance(params["sample_every"], (float, int)):
            raise StyxValidationError(f'`sample_every` has the wrong type: Received `{type(params["sample_every"])}` expected `float`')
    if params.get("deconv_model", None) is not None:
        if not isinstance(params["deconv_model"], (float, int)):
            raise StyxValidationError(f'`deconv_model` has the wrong type: Received `{type(params["deconv_model"])}` expected `float`')
    if params.get("grad_nonlinear", None) is not None:
        if not isinstance(params["grad_nonlinear"], bool):
            raise StyxValidationError(f'`grad_nonlinear` has the wrong type: Received `{type(params["grad_nonlinear"])}` expected `bool`')

def bedpostx_gpu_cargs(params: BedpostxGpu, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("bedpostx_gpu")
    cargs.append(params["subject_dir"])
    v_0 = params.get("gpu_queue")
    if v_0 is not None:
        cargs.append("-Q")
        cargs.append(v_0)
    v_1 = params.get("num_jobs")
    if v_1 is not None:
        cargs.append("-NJOBS")
        cargs.append(str(v_1))
    v_2 = params.get("num_fibers")
    if v_2 is not None:
        cargs.append("-n")
        cargs.append(str(v_2))
    v_3 = params.get("ard_weight")
    if v_3 is not None:
        cargs.append("-w")
        cargs.append(str(v_3))
    v_4 = params.get("burnin_period")
    if v_4 is not None:
        cargs.append("-b")
        cargs.append(str(v_4))
    v_5 = params.get("num_jumps")
    if v_5 is not None:
        cargs.append("-j")
        cargs.append(str(v_5))
    v_6 = params.get("sample_every")
    if v_6 is not None:
        cargs.append("-s")
        cargs.append(str(v_6))
    v_7 = params.get("deconv_model")
    if v_7 is not None:
        cargs.append("-model")
        cargs.append(str(v_7))
    if params.get("grad_nonlinear"):
        cargs.append("-g")
    return cargs

def bedpostx_gpu_outputs(params: BedpostxGpu, execution: Execution) -> BedpostxGpuOutputs:
    """Build the BedpostxGpuOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BedpostxGpuOutputs(
        root=root_v,
    )

def bedpostx_gpu_execute(params: BedpostxGpu, runner: Runner | None = None) -> BedpostxGpuOutputs:
    """
    bedpostx_gpu

    Probabilistic tractography and diffusion MRI fitting tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    bedpostx_gpu_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BEDPOSTX_GPU_METADATA)
    execution.params(params)
    args = bedpostx_gpu_cargs(params, execution)
    out = bedpostx_gpu_outputs(params, execution)
    execution.run(args)
    return out

def bedpostx_gpu(
    subject_dir: str,
    gpu_queue: str | None = None,
    num_jobs: float | None = None,
    num_fibers: float | None = None,
    ard_weight: float | None = None,
    burnin_period: float | None = None,
    num_jumps: float | None = None,
    sample_every: float | None = None,
    deconv_model: float | None = None,
    grad_nonlinear: bool = False,
    runner: Runner | None = None,
) -> BedpostxGpuOutputs:
    """
    bedpostx_gpu

    Probabilistic tractography and diffusion MRI fitting tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        subject_dir: Directory containing the subject's DWI data and associated\
            files (bvals, bvecs, data, nodif_brain_mask).
        gpu_queue: Name of the GPU(s) queue. Default: --coprocessor=cuda to let\
            fsl_sub decide on the queue
        num_jobs: Number of jobs to queue. The data is divided into this number of\
            parts, useful for a GPU cluster. Default: 4
        num_fibers: Number of fibres per voxel. Default: 3
        ard_weight: Automatic Relevance Determination (ARD) weight. More weight\
            means fewer secondary fibres per voxel. Default: 1
        burnin_period: Burn-in period. Default: 1000
        num_jumps: Number of jumps. Default: 1250
        sample_every: Sample every N steps. Default: 25
        deconv_model: Deconvolution model. 1: with sticks, 2: with sticks with a\
            range of diffusivities (default), 3: with zeppelins
        grad_nonlinear: Consider gradient nonlinearities. Default: off
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = bedpostx_gpu_params(
        subject_dir=subject_dir,
        gpu_queue=gpu_queue,
        num_jobs=num_jobs,
        num_fibers=num_fibers,
        ard_weight=ard_weight,
        burnin_period=burnin_period,
        num_jumps=num_jumps,
        sample_every=sample_every,
        deconv_model=deconv_model,
        grad_nonlinear=grad_nonlinear,
    )
    return bedpostx_gpu_execute(params, runner)

__all__ = [
    "BedpostxGpu",
    "BedpostxGpuOutputs",
    "BEDPOSTX_GPU_METADATA",
    "bedpostx_gpu_cargs",
    "bedpostx_gpu_outputs",
    "bedpostx_gpu_params",
    "bedpostx_gpu_execute",
    "bedpostx_gpu_validate",
    "bedpostx_gpu",
]