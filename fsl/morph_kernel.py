# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MORPH_KERNEL_METADATA = Metadata(
    id="morph_kernel",
    name="morph_kernel",
    package="fsl",
)

MorphKernel = typing.TypedDict(
    "MorphKernel",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/morph_kernel"]],
        "cube_side_length": float,
        "sphere_radius": float,
    },
)

@dataclasses.dataclass
class MorphKernelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    morph_kernel_output: OutputPathType
    """Output morphological kernel file"""

def morph_kernel_params(
    cube_side_length: float,
    sphere_radius: float,
) -> MorphKernel:
    """
    Build parameters.

    Args:
        cube_side_length: Side length of the cube (e.g., 11)
        sphere_radius: Radius of the sphere (e.g., 5.5)

    Returns:
        Parameter dictionary.
    """
    params: MorphKernel = {
        "@type": "fsl/morph_kernel",
        "cube_side_length": cube_side_length,
        "sphere_radius": sphere_radius,
    }
    return params

def morph_kernel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MorphKernel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("cube_side_length", None) is None:
        raise StyxValidationError("`cube_side_length` must not be None")
    if not isinstance(params["cube_side_length"], (float, int)):
        raise StyxValidationError(f'`cube_side_length` has the wrong type: Received `{type(params["cube_side_length"])}` expected `float`')
    if params.get("sphere_radius", None) is None:
        raise StyxValidationError("`sphere_radius` must not be None")
    if not isinstance(params["sphere_radius"], (float, int)):
        raise StyxValidationError(f'`sphere_radius` has the wrong type: Received `{type(params["sphere_radius"])}` expected `float`')

def morph_kernel_cargs(params: MorphKernel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("morph_kernel")
    cargs.append(str(params["cube_side_length"]))
    cargs.append(str(params["sphere_radius"]))
    return cargs

def morph_kernel_outputs(params: MorphKernel, execution: Execution) -> MorphKernelOutputs:
    """Build the MorphKernelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    morph_kernel_output_v: OutputPathType = execution.output_file("sphere[OUTPUT_PREFIX].ker")
    return MorphKernelOutputs(
        root=root_v,
        morph_kernel_output=morph_kernel_output_v,
    )

def morph_kernel_execute(params: MorphKernel, runner: Runner | None = None) -> MorphKernelOutputs:
    """
    morph_kernel

    Tool to generate morphological kernels

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    morph_kernel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MORPH_KERNEL_METADATA)
    execution.params(params)
    args = morph_kernel_cargs(params, execution)
    out = morph_kernel_outputs(params, execution)
    execution.run(args)
    return out

def morph_kernel(
    cube_side_length: float,
    sphere_radius: float,
    runner: Runner | None = None,
) -> MorphKernelOutputs:
    """
    morph_kernel

    Tool to generate morphological kernels

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        cube_side_length: Side length of the cube (e.g., 11)
        sphere_radius: Radius of the sphere (e.g., 5.5)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = morph_kernel_params(
        cube_side_length=cube_side_length,
        sphere_radius=sphere_radius,
    )
    return morph_kernel_execute(params, runner)

__all__ = [
    "MorphKernel",
    "MorphKernelOutputs",
    "MORPH_KERNEL_METADATA",
    "morph_kernel_cargs",
    "morph_kernel_outputs",
    "morph_kernel_params",
    "morph_kernel_execute",
    "morph_kernel_validate",
    "morph_kernel",
]