# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FEAT_METADATA = Metadata(
    id="feat",
    name="feat",
    package="fsl",
)

Feat = typing.TypedDict(
    "Feat",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/feat"]],
        "design_file": InputPathType,
    },
)

@dataclasses.dataclass
class FeatOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dir: OutputPathType
    """Output directory containing all results from FEAT"""

def feat_params(
    design_file: InputPathType,
) -> Feat:
    """
    Build parameters.

    Args:
        design_file: FEAT design file (e.g. design.fsf)

    Returns:
        Parameter dictionary.
    """
    params: Feat = {
        "@type": "fsl/feat",
        "design_file": design_file,
    }
    return params

def feat_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Feat."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("design_file", None) is None:
        raise StyxValidationError("`design_file` must not be None")
    if not isinstance(params["design_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`design_file` has the wrong type: Received `{type(params["design_file"])}` expected `InputPathType`')

def feat_cargs(params: Feat, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("feat")
    cargs.append(execution.input_file(params["design_file"]))
    return cargs

def feat_outputs(params: Feat, execution: Execution) -> FeatOutputs:
    """Build the FeatOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dir_v: OutputPathType = execution.output_file("design.feat")
    return FeatOutputs(
        root=root_v,
        output_dir=output_dir_v,
    )

def feat_execute(params: Feat, runner: Runner | None = None) -> FeatOutputs:
    """
    feat

    fMRI Expert Analysis Tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    feat_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FEAT_METADATA)
    execution.params(params)
    args = feat_cargs(params, execution)
    out = feat_outputs(params, execution)
    execution.run(args)
    return out

def feat(
    design_file: InputPathType,
    runner: Runner | None = None,
) -> FeatOutputs:
    """
    feat

    fMRI Expert Analysis Tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        design_file: FEAT design file (e.g. design.fsf)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = feat_params(
        design_file=design_file,
    )
    return feat_execute(params, runner)

__all__ = [
    "Feat",
    "FeatOutputs",
    "FEAT_METADATA",
    "feat_cargs",
    "feat_outputs",
    "feat_params",
    "feat_execute",
    "feat_validate",
    "feat",
]