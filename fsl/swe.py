# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SWE_METADATA = Metadata(
    id="swe",
    name="swe",
    package="fsl",
)

Swe = typing.TypedDict(
    "Swe",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/swe"]],
        "input_file": InputPathType,
        "output_root": str,
        "design_mat": InputPathType,
        "design_con": InputPathType,
        "design_sub": InputPathType,
        "mask": typing.NotRequired[InputPathType],
        "fcon": typing.NotRequired[InputPathType],
        "modified": typing.NotRequired[bool],
        "wild_bootstrap": typing.NotRequired[bool],
        "logp": typing.NotRequired[bool],
        "nboot": typing.NotRequired[float],
        "corrp": typing.NotRequired[bool],
        "fonly": typing.NotRequired[bool],
        "tfce": typing.NotRequired[bool],
        "tfce_2d": typing.NotRequired[bool],
        "cluster_t": typing.NotRequired[float],
        "cluster_t_mass": typing.NotRequired[float],
        "cluster_f": typing.NotRequired[float],
        "cluster_f_mass": typing.NotRequired[float],
        "quiet": typing.NotRequired[bool],
        "raw": typing.NotRequired[bool],
        "equiv": typing.NotRequired[bool],
        "dof": typing.NotRequired[bool],
        "uncorr_p": typing.NotRequired[bool],
        "null_dist": typing.NotRequired[bool],
        "no_rc_mask": typing.NotRequired[bool],
        "seed": typing.NotRequired[float],
        "tfce_h": typing.NotRequired[float],
        "tfce_d": typing.NotRequired[float],
        "tfce_e": typing.NotRequired[float],
        "tfce_c": typing.NotRequired[float],
        "voxelwise_ev": typing.NotRequired[list[float]],
        "voxelwise_evs": typing.NotRequired[list[InputPathType]],
        "glm_output": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SweOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    logp_img: OutputPathType
    """Output -log_10(p) images"""
    corrp_img: OutputPathType
    """Output voxel-corrected p-value images"""
    raw_img: OutputPathType
    """Output raw voxelwise statistic images"""
    equiv_img: OutputPathType
    """Output equivalent z or chi-squared statistic images"""
    dof_img: OutputPathType
    """Output effective number of degrees of freedom images"""
    uncorrp_img: OutputPathType
    """Output uncorrected p-value images"""
    null_dist_file: OutputPathType
    """Output null distribution text files"""
    glm_output_file: OutputPathType
    """Output GLM information (pe, cope, & varcope)"""

def swe_params(
    input_file: InputPathType,
    output_root: str,
    design_mat: InputPathType,
    design_con: InputPathType,
    design_sub: InputPathType,
    mask: InputPathType | None = None,
    fcon: InputPathType | None = None,
    modified: bool = False,
    wild_bootstrap: bool = False,
    logp: bool = False,
    nboot: float | None = None,
    corrp: bool = False,
    fonly: bool = False,
    tfce: bool = False,
    tfce_2d: bool = False,
    cluster_t: float | None = None,
    cluster_t_mass: float | None = None,
    cluster_f: float | None = None,
    cluster_f_mass: float | None = None,
    quiet: bool = False,
    raw: bool = False,
    equiv: bool = False,
    dof: bool = False,
    uncorr_p: bool = False,
    null_dist: bool = False,
    no_rc_mask: bool = False,
    seed: float | None = None,
    tfce_h: float | None = None,
    tfce_d: float | None = None,
    tfce_e: float | None = None,
    tfce_c: float | None = None,
    voxelwise_ev: list[float] | None = None,
    voxelwise_evs: list[InputPathType] | None = None,
    glm_output: bool = False,
) -> Swe:
    """
    Build parameters.

    Args:
        input_file: 4D input image
        output_root: Output file root name
        design_mat: Design matrix file
        design_con: T contrasts file
        design_sub: Subjects file
        mask: Mask image
        fcon: F contrasts file
        modified: Use the modified 'Homogeneous' SwE instead of the classic\
            'Heterogeneous' SwE
        wild_bootstrap: Inference using a non-parametric Wild Bootstrap procedure
        logp: Return -log_10(p) images instead of 1-p images
        nboot: Number of bootstraps (default 999)
        corrp: Output voxelwise corrected p-value images
        fonly: Calculate f-statistics only
        tfce: Threshold-Free Cluster Enhancement
        tfce_2d: Threshold-Free Cluster Enhancement with 2D optimisation, e.g. for\
            TBSS data (H=2, E=1, C=26)
        cluster_t: Cluster-extent-based inference for t-contrasts with specified\
            cluster-forming threshold (z-score if >= 1, uncorrected p-value if < 1)
        cluster_t_mass: Cluster-mass-based inference for t-contrasts with specified\
            cluster-forming threshold (z-score if >= 1, uncorrected p-value if < 1)
        cluster_f: Cluster-extent-based inference for f-contrasts with specified\
            cluster-forming threshold (chi-squared-score if >= 1, uncorrected\
            p-value if < 1)
        cluster_f_mass: Cluster-mass-based inference for f-contrasts with specified\
            cluster-forming threshold (chi-squared-score if >= 1, uncorrected\
            p-value if < 1)
        quiet: Switch off diagnostic messages
        raw: Output raw voxelwise statistic images
        equiv: Output equivalent z or chi-squared statistic images
        dof: Output effective number of degrees of freedom images
        uncorr_p: Output uncorrected p-value images
        null_dist: Output null distribution text files
        no_rc_mask: Don't remove constant voxels from mask
        seed: Specific integer seed for random number generator
        tfce_h: TFCE height parameter (default=2)
        tfce_d: TFCE delta parameter override
        tfce_e: TFCE extent parameter (default=0.5)
        tfce_c: TFCE connectivity (6 or 26; default=6)
        voxelwise_ev: List of numbers indicating voxelwise EVs position in the\
            design matrix
        voxelwise_evs: List of 4D images containing voxelwise EVs
        glm_output: Output GLM information (pe, cope, & varcope)

    Returns:
        Parameter dictionary.
    """
    params: Swe = {
        "@type": "fsl/swe",
        "input_file": input_file,
        "output_root": output_root,
        "design_mat": design_mat,
        "design_con": design_con,
        "design_sub": design_sub,
        "modified": modified,
        "wild_bootstrap": wild_bootstrap,
        "logp": logp,
        "corrp": corrp,
        "fonly": fonly,
        "tfce": tfce,
        "tfce_2d": tfce_2d,
        "quiet": quiet,
        "raw": raw,
        "equiv": equiv,
        "dof": dof,
        "uncorr_p": uncorr_p,
        "null_dist": null_dist,
        "no_rc_mask": no_rc_mask,
        "glm_output": glm_output,
    }
    if mask is not None:
        params["mask"] = mask
    if fcon is not None:
        params["fcon"] = fcon
    if nboot is not None:
        params["nboot"] = nboot
    if cluster_t is not None:
        params["cluster_t"] = cluster_t
    if cluster_t_mass is not None:
        params["cluster_t_mass"] = cluster_t_mass
    if cluster_f is not None:
        params["cluster_f"] = cluster_f
    if cluster_f_mass is not None:
        params["cluster_f_mass"] = cluster_f_mass
    if seed is not None:
        params["seed"] = seed
    if tfce_h is not None:
        params["tfce_h"] = tfce_h
    if tfce_d is not None:
        params["tfce_d"] = tfce_d
    if tfce_e is not None:
        params["tfce_e"] = tfce_e
    if tfce_c is not None:
        params["tfce_c"] = tfce_c
    if voxelwise_ev is not None:
        params["voxelwise_ev"] = voxelwise_ev
    if voxelwise_evs is not None:
        params["voxelwise_evs"] = voxelwise_evs
    return params

def swe_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Swe."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_root", None) is None:
        raise StyxValidationError("`output_root` must not be None")
    if not isinstance(params["output_root"], str):
        raise StyxValidationError(f'`output_root` has the wrong type: Received `{type(params["output_root"])}` expected `str`')
    if params.get("design_mat", None) is None:
        raise StyxValidationError("`design_mat` must not be None")
    if not isinstance(params["design_mat"], (pathlib.Path, str)):
        raise StyxValidationError(f'`design_mat` has the wrong type: Received `{type(params["design_mat"])}` expected `InputPathType`')
    if params.get("design_con", None) is None:
        raise StyxValidationError("`design_con` must not be None")
    if not isinstance(params["design_con"], (pathlib.Path, str)):
        raise StyxValidationError(f'`design_con` has the wrong type: Received `{type(params["design_con"])}` expected `InputPathType`')
    if params.get("design_sub", None) is None:
        raise StyxValidationError("`design_sub` must not be None")
    if not isinstance(params["design_sub"], (pathlib.Path, str)):
        raise StyxValidationError(f'`design_sub` has the wrong type: Received `{type(params["design_sub"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("fcon", None) is not None:
        if not isinstance(params["fcon"], (pathlib.Path, str)):
            raise StyxValidationError(f'`fcon` has the wrong type: Received `{type(params["fcon"])}` expected `InputPathType`')
    if params.get("modified", None) is not None:
        if not isinstance(params["modified"], bool):
            raise StyxValidationError(f'`modified` has the wrong type: Received `{type(params["modified"])}` expected `bool`')
    if params.get("wild_bootstrap", None) is not None:
        if not isinstance(params["wild_bootstrap"], bool):
            raise StyxValidationError(f'`wild_bootstrap` has the wrong type: Received `{type(params["wild_bootstrap"])}` expected `bool`')
    if params.get("logp", None) is not None:
        if not isinstance(params["logp"], bool):
            raise StyxValidationError(f'`logp` has the wrong type: Received `{type(params["logp"])}` expected `bool`')
    if params.get("nboot", None) is not None:
        if not isinstance(params["nboot"], (float, int)):
            raise StyxValidationError(f'`nboot` has the wrong type: Received `{type(params["nboot"])}` expected `float`')
    if params.get("corrp", None) is not None:
        if not isinstance(params["corrp"], bool):
            raise StyxValidationError(f'`corrp` has the wrong type: Received `{type(params["corrp"])}` expected `bool`')
    if params.get("fonly", None) is not None:
        if not isinstance(params["fonly"], bool):
            raise StyxValidationError(f'`fonly` has the wrong type: Received `{type(params["fonly"])}` expected `bool`')
    if params.get("tfce", None) is not None:
        if not isinstance(params["tfce"], bool):
            raise StyxValidationError(f'`tfce` has the wrong type: Received `{type(params["tfce"])}` expected `bool`')
    if params.get("tfce_2d", None) is not None:
        if not isinstance(params["tfce_2d"], bool):
            raise StyxValidationError(f'`tfce_2d` has the wrong type: Received `{type(params["tfce_2d"])}` expected `bool`')
    if params.get("cluster_t", None) is not None:
        if not isinstance(params["cluster_t"], (float, int)):
            raise StyxValidationError(f'`cluster_t` has the wrong type: Received `{type(params["cluster_t"])}` expected `float`')
    if params.get("cluster_t_mass", None) is not None:
        if not isinstance(params["cluster_t_mass"], (float, int)):
            raise StyxValidationError(f'`cluster_t_mass` has the wrong type: Received `{type(params["cluster_t_mass"])}` expected `float`')
    if params.get("cluster_f", None) is not None:
        if not isinstance(params["cluster_f"], (float, int)):
            raise StyxValidationError(f'`cluster_f` has the wrong type: Received `{type(params["cluster_f"])}` expected `float`')
    if params.get("cluster_f_mass", None) is not None:
        if not isinstance(params["cluster_f_mass"], (float, int)):
            raise StyxValidationError(f'`cluster_f_mass` has the wrong type: Received `{type(params["cluster_f_mass"])}` expected `float`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("raw", None) is not None:
        if not isinstance(params["raw"], bool):
            raise StyxValidationError(f'`raw` has the wrong type: Received `{type(params["raw"])}` expected `bool`')
    if params.get("equiv", None) is not None:
        if not isinstance(params["equiv"], bool):
            raise StyxValidationError(f'`equiv` has the wrong type: Received `{type(params["equiv"])}` expected `bool`')
    if params.get("dof", None) is not None:
        if not isinstance(params["dof"], bool):
            raise StyxValidationError(f'`dof` has the wrong type: Received `{type(params["dof"])}` expected `bool`')
    if params.get("uncorr_p", None) is not None:
        if not isinstance(params["uncorr_p"], bool):
            raise StyxValidationError(f'`uncorr_p` has the wrong type: Received `{type(params["uncorr_p"])}` expected `bool`')
    if params.get("null_dist", None) is not None:
        if not isinstance(params["null_dist"], bool):
            raise StyxValidationError(f'`null_dist` has the wrong type: Received `{type(params["null_dist"])}` expected `bool`')
    if params.get("no_rc_mask", None) is not None:
        if not isinstance(params["no_rc_mask"], bool):
            raise StyxValidationError(f'`no_rc_mask` has the wrong type: Received `{type(params["no_rc_mask"])}` expected `bool`')
    if params.get("seed", None) is not None:
        if not isinstance(params["seed"], (float, int)):
            raise StyxValidationError(f'`seed` has the wrong type: Received `{type(params["seed"])}` expected `float`')
    if params.get("tfce_h", None) is not None:
        if not isinstance(params["tfce_h"], (float, int)):
            raise StyxValidationError(f'`tfce_h` has the wrong type: Received `{type(params["tfce_h"])}` expected `float`')
    if params.get("tfce_d", None) is not None:
        if not isinstance(params["tfce_d"], (float, int)):
            raise StyxValidationError(f'`tfce_d` has the wrong type: Received `{type(params["tfce_d"])}` expected `float`')
    if params.get("tfce_e", None) is not None:
        if not isinstance(params["tfce_e"], (float, int)):
            raise StyxValidationError(f'`tfce_e` has the wrong type: Received `{type(params["tfce_e"])}` expected `float`')
    if params.get("tfce_c", None) is not None:
        if not isinstance(params["tfce_c"], (float, int)):
            raise StyxValidationError(f'`tfce_c` has the wrong type: Received `{type(params["tfce_c"])}` expected `float`')
    if params.get("voxelwise_ev", None) is not None:
        if not isinstance(params["voxelwise_ev"], list):
            raise StyxValidationError(f'`voxelwise_ev` has the wrong type: Received `{type(params["voxelwise_ev"])}` expected `list[float]`')
        for e in params["voxelwise_ev"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`voxelwise_ev` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("voxelwise_evs", None) is not None:
        if not isinstance(params["voxelwise_evs"], list):
            raise StyxValidationError(f'`voxelwise_evs` has the wrong type: Received `{type(params["voxelwise_evs"])}` expected `list[InputPathType]`')
        for e_2 in params["voxelwise_evs"]:
            if not isinstance(e_2, (pathlib.Path, str)):
                raise StyxValidationError(f'`voxelwise_evs` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("glm_output", None) is not None:
        if not isinstance(params["glm_output"], bool):
            raise StyxValidationError(f'`glm_output` has the wrong type: Received `{type(params["glm_output"])}` expected `bool`')

def swe_cargs(params: Swe, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("swe")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("-o")
    cargs.append(params["output_root"])
    cargs.append("-d")
    cargs.append(execution.input_file(params["design_mat"]))
    cargs.append("-t")
    cargs.append(execution.input_file(params["design_con"]))
    cargs.append("-s")
    cargs.append(execution.input_file(params["design_sub"]))
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("fcon")
    if v_1 is not None:
        cargs.append("-f")
        cargs.append(execution.input_file(v_1))
    if params.get("modified"):
        cargs.append("--modified")
    if params.get("wild_bootstrap"):
        cargs.append("--wb")
    if params.get("logp"):
        cargs.append("--logp")
    v_2 = params.get("nboot")
    if v_2 is not None:
        cargs.append("-n")
        cargs.append(str(v_2))
    if params.get("corrp"):
        cargs.append("-x")
    if params.get("fonly"):
        cargs.append("--fonly")
    if params.get("tfce"):
        cargs.append("-T")
    if params.get("tfce_2d"):
        cargs.append("--T2")
    v_3 = params.get("cluster_t")
    if v_3 is not None:
        cargs.append("-c")
        cargs.append(str(v_3))
    v_4 = params.get("cluster_t_mass")
    if v_4 is not None:
        cargs.append("-C")
        cargs.append(str(v_4))
    v_5 = params.get("cluster_f")
    if v_5 is not None:
        cargs.append("-F")
        cargs.append(str(v_5))
    v_6 = params.get("cluster_f_mass")
    if v_6 is not None:
        cargs.append("-S")
        cargs.append(str(v_6))
    if params.get("quiet"):
        cargs.append("--quiet")
    if params.get("raw"):
        cargs.append("-R")
    if params.get("equiv"):
        cargs.append("-E")
    if params.get("dof"):
        cargs.append("-D")
    if params.get("uncorr_p"):
        cargs.append("--uncorrp")
    if params.get("null_dist"):
        cargs.append("-N")
    if params.get("no_rc_mask"):
        cargs.append("--norcmask")
    v_7 = params.get("seed")
    if v_7 is not None:
        cargs.append("--seed")
        cargs.append(str(v_7))
    v_8 = params.get("tfce_h")
    if v_8 is not None:
        cargs.append("--tfce_H")
        cargs.append(str(v_8))
    v_9 = params.get("tfce_d")
    if v_9 is not None:
        cargs.append("--tfce_D")
        cargs.append(str(v_9))
    v_10 = params.get("tfce_e")
    if v_10 is not None:
        cargs.append("--tfce_E")
        cargs.append(str(v_10))
    v_11 = params.get("tfce_c")
    if v_11 is not None:
        cargs.append("--tfce_C")
        cargs.append(str(v_11))
    v_12 = params.get("voxelwise_ev")
    if v_12 is not None:
        cargs.append("--vxl")
        for item0 in v_12:
            cargs.append(str(item0))
    v_13 = params.get("voxelwise_evs")
    if v_13 is not None:
        cargs.append("--vxf")
        for item1 in v_13:
            cargs.append(execution.input_file(item1))
    if params.get("glm_output"):
        cargs.append("--glm_output")
    return cargs

def swe_outputs(params: Swe, execution: Execution) -> SweOutputs:
    """Build the SweOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    logp_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_logp.nii.gz')
    corrp_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_corrp.nii.gz')
    raw_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_raw.nii.gz')
    equiv_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_equiv.nii.gz')
    dof_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_dof.nii.gz')
    uncorrp_img_v: OutputPathType = execution.output_file(f'{params["output_root"]}_uncorrp.nii.gz')
    null_dist_file_v: OutputPathType = execution.output_file(f'{params["output_root"]}_null_dist.txt')
    glm_output_file_v: OutputPathType = execution.output_file(f'{params["output_root"]}_glm.nii.gz')
    return SweOutputs(
        root=root_v,
        logp_img=logp_img_v,
        corrp_img=corrp_img_v,
        raw_img=raw_img_v,
        equiv_img=equiv_img_v,
        dof_img=dof_img_v,
        uncorrp_img=uncorrp_img_v,
        null_dist_file=null_dist_file_v,
        glm_output_file=glm_output_file_v,
    )

def swe_execute(params: Swe, runner: Runner | None = None) -> SweOutputs:
    """
    swe

    SwE (summary statistics and voxelwise statistical analyses tool for FSL)

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    swe_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SWE_METADATA)
    execution.params(params)
    args = swe_cargs(params, execution)
    out = swe_outputs(params, execution)
    execution.run(args)
    return out

def swe(
    input_file: InputPathType,
    output_root: str,
    design_mat: InputPathType,
    design_con: InputPathType,
    design_sub: InputPathType,
    mask: InputPathType | None = None,
    fcon: InputPathType | None = None,
    modified: bool = False,
    wild_bootstrap: bool = False,
    logp: bool = False,
    nboot: float | None = None,
    corrp: bool = False,
    fonly: bool = False,
    tfce: bool = False,
    tfce_2d: bool = False,
    cluster_t: float | None = None,
    cluster_t_mass: float | None = None,
    cluster_f: float | None = None,
    cluster_f_mass: float | None = None,
    quiet: bool = False,
    raw: bool = False,
    equiv: bool = False,
    dof: bool = False,
    uncorr_p: bool = False,
    null_dist: bool = False,
    no_rc_mask: bool = False,
    seed: float | None = None,
    tfce_h: float | None = None,
    tfce_d: float | None = None,
    tfce_e: float | None = None,
    tfce_c: float | None = None,
    voxelwise_ev: list[float] | None = None,
    voxelwise_evs: list[InputPathType] | None = None,
    glm_output: bool = False,
    runner: Runner | None = None,
) -> SweOutputs:
    """
    swe

    SwE (summary statistics and voxelwise statistical analyses tool for FSL)

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: 4D input image
        output_root: Output file root name
        design_mat: Design matrix file
        design_con: T contrasts file
        design_sub: Subjects file
        mask: Mask image
        fcon: F contrasts file
        modified: Use the modified 'Homogeneous' SwE instead of the classic\
            'Heterogeneous' SwE
        wild_bootstrap: Inference using a non-parametric Wild Bootstrap procedure
        logp: Return -log_10(p) images instead of 1-p images
        nboot: Number of bootstraps (default 999)
        corrp: Output voxelwise corrected p-value images
        fonly: Calculate f-statistics only
        tfce: Threshold-Free Cluster Enhancement
        tfce_2d: Threshold-Free Cluster Enhancement with 2D optimisation, e.g. for\
            TBSS data (H=2, E=1, C=26)
        cluster_t: Cluster-extent-based inference for t-contrasts with specified\
            cluster-forming threshold (z-score if >= 1, uncorrected p-value if < 1)
        cluster_t_mass: Cluster-mass-based inference for t-contrasts with specified\
            cluster-forming threshold (z-score if >= 1, uncorrected p-value if < 1)
        cluster_f: Cluster-extent-based inference for f-contrasts with specified\
            cluster-forming threshold (chi-squared-score if >= 1, uncorrected\
            p-value if < 1)
        cluster_f_mass: Cluster-mass-based inference for f-contrasts with specified\
            cluster-forming threshold (chi-squared-score if >= 1, uncorrected\
            p-value if < 1)
        quiet: Switch off diagnostic messages
        raw: Output raw voxelwise statistic images
        equiv: Output equivalent z or chi-squared statistic images
        dof: Output effective number of degrees of freedom images
        uncorr_p: Output uncorrected p-value images
        null_dist: Output null distribution text files
        no_rc_mask: Don't remove constant voxels from mask
        seed: Specific integer seed for random number generator
        tfce_h: TFCE height parameter (default=2)
        tfce_d: TFCE delta parameter override
        tfce_e: TFCE extent parameter (default=0.5)
        tfce_c: TFCE connectivity (6 or 26; default=6)
        voxelwise_ev: List of numbers indicating voxelwise EVs position in the\
            design matrix
        voxelwise_evs: List of 4D images containing voxelwise EVs
        glm_output: Output GLM information (pe, cope, & varcope)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = swe_params(
        input_file=input_file,
        output_root=output_root,
        design_mat=design_mat,
        design_con=design_con,
        design_sub=design_sub,
        mask=mask,
        fcon=fcon,
        modified=modified,
        wild_bootstrap=wild_bootstrap,
        logp=logp,
        nboot=nboot,
        corrp=corrp,
        fonly=fonly,
        tfce=tfce,
        tfce_2d=tfce_2d,
        cluster_t=cluster_t,
        cluster_t_mass=cluster_t_mass,
        cluster_f=cluster_f,
        cluster_f_mass=cluster_f_mass,
        quiet=quiet,
        raw=raw,
        equiv=equiv,
        dof=dof,
        uncorr_p=uncorr_p,
        null_dist=null_dist,
        no_rc_mask=no_rc_mask,
        seed=seed,
        tfce_h=tfce_h,
        tfce_d=tfce_d,
        tfce_e=tfce_e,
        tfce_c=tfce_c,
        voxelwise_ev=voxelwise_ev,
        voxelwise_evs=voxelwise_evs,
        glm_output=glm_output,
    )
    return swe_execute(params, runner)

__all__ = [
    "Swe",
    "SweOutputs",
    "SWE_METADATA",
    "swe_cargs",
    "swe_outputs",
    "swe_params",
    "swe_execute",
    "swe_validate",
    "swe",
]