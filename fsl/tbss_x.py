# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TBSS_X_METADATA = Metadata(
    id="tbss_x",
    name="tbss_x",
    package="fsl",
)

TbssX = typing.TypedDict(
    "TbssX",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tbss_x"]],
        "scalar_dirs": list[str],
        "vector_dirs": list[str],
    },
)

@dataclasses.dataclass
class TbssXOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tbss_x_params(
    scalar_dirs: list[str],
    vector_dirs: list[str],
) -> TbssX:
    """
    Build parameters.

    Args:
        scalar_dirs: List of scalar directories (e.g., F1, F2)
        vector_dirs: List of vector directories (e.g., D1, D2)

    Returns:
        Parameter dictionary.
    """
    params: TbssX = {
        "@type": "fsl/tbss_x",
        "scalar_dirs": scalar_dirs,
        "vector_dirs": vector_dirs,
    }
    return params

def tbss_x_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TbssX."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scalar_dirs", None) is None:
        raise StyxValidationError("`scalar_dirs` must not be None")
    if not isinstance(params["scalar_dirs"], list):
        raise StyxValidationError(f'`scalar_dirs` has the wrong type: Received `{type(params["scalar_dirs"])}` expected `list[str]`')
    for e in params["scalar_dirs"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`scalar_dirs` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("vector_dirs", None) is None:
        raise StyxValidationError("`vector_dirs` must not be None")
    if not isinstance(params["vector_dirs"], list):
        raise StyxValidationError(f'`vector_dirs` has the wrong type: Received `{type(params["vector_dirs"])}` expected `list[str]`')
    for e_2 in params["vector_dirs"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`vector_dirs` has the wrong type: Received `{type(e_2)}` expected `list[str]`')

def tbss_x_cargs(params: TbssX, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tbss_x")
    for item0 in params["scalar_dirs"]:
        cargs.append(item0)
    for item1 in params["vector_dirs"]:
        cargs.append(item1)
    return cargs

def tbss_x_outputs(params: TbssX, execution: Execution) -> TbssXOutputs:
    """Build the TbssXOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TbssXOutputs(
        root=root_v,
    )

def tbss_x_execute(params: TbssX, runner: Runner | None = None) -> TbssXOutputs:
    """
    tbss_x

    TBSS cross-subject script for processing scalar and vector directories.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tbss_x_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TBSS_X_METADATA)
    execution.params(params)
    args = tbss_x_cargs(params, execution)
    out = tbss_x_outputs(params, execution)
    execution.run(args)
    return out

def tbss_x(
    scalar_dirs: list[str],
    vector_dirs: list[str],
    runner: Runner | None = None,
) -> TbssXOutputs:
    """
    tbss_x

    TBSS cross-subject script for processing scalar and vector directories.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        scalar_dirs: List of scalar directories (e.g., F1, F2)
        vector_dirs: List of vector directories (e.g., D1, D2)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tbss_x_params(
        scalar_dirs=scalar_dirs,
        vector_dirs=vector_dirs,
    )
    return tbss_x_execute(params, runner)

__all__ = [
    "TbssX",
    "TbssXOutputs",
    "TBSS_X_METADATA",
    "tbss_x_cargs",
    "tbss_x_outputs",
    "tbss_x_params",
    "tbss_x_execute",
    "tbss_x_validate",
    "tbss_x",
]