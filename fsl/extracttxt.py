# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EXTRACTTXT_METADATA = Metadata(
    id="extracttxt",
    name="extracttxt",
    package="fsl",
)

Extracttxt = typing.TypedDict(
    "Extracttxt",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/extracttxt"]],
        "search_word": str,
        "file": InputPathType,
        "num_trailing_lines": typing.NotRequired[float],
        "relative_start": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class ExtracttxtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Extracted text output file"""

def extracttxt_params(
    search_word: str,
    file: InputPathType,
    num_trailing_lines: float | None = None,
    relative_start: float | None = None,
) -> Extracttxt:
    """
    Build parameters.

    Args:
        search_word: The word to search for in the file
        file: Path to the file where text is to be extracted
        num_trailing_lines: Number of trailing lines to include after the search\
            word
        relative_start: Relative start position to begin the search

    Returns:
        Parameter dictionary.
    """
    params: Extracttxt = {
        "@type": "fsl/extracttxt",
        "search_word": search_word,
        "file": file,
    }
    if num_trailing_lines is not None:
        params["num_trailing_lines"] = num_trailing_lines
    if relative_start is not None:
        params["relative_start"] = relative_start
    return params

def extracttxt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Extracttxt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("search_word", None) is None:
        raise StyxValidationError("`search_word` must not be None")
    if not isinstance(params["search_word"], str):
        raise StyxValidationError(f'`search_word` has the wrong type: Received `{type(params["search_word"])}` expected `str`')
    if params.get("file", None) is None:
        raise StyxValidationError("`file` must not be None")
    if not isinstance(params["file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file` has the wrong type: Received `{type(params["file"])}` expected `InputPathType`')
    if params.get("num_trailing_lines", None) is not None:
        if not isinstance(params["num_trailing_lines"], (float, int)):
            raise StyxValidationError(f'`num_trailing_lines` has the wrong type: Received `{type(params["num_trailing_lines"])}` expected `float`')
    if params.get("relative_start", None) is not None:
        if not isinstance(params["relative_start"], (float, int)):
            raise StyxValidationError(f'`relative_start` has the wrong type: Received `{type(params["relative_start"])}` expected `float`')

def extracttxt_cargs(params: Extracttxt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("extracttxt")
    cargs.append(params["search_word"])
    cargs.append(execution.input_file(params["file"]))
    v_0 = params.get("num_trailing_lines")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("relative_start")
    if v_1 is not None:
        cargs.append(str(v_1))
    return cargs

def extracttxt_outputs(params: Extracttxt, execution: Execution) -> ExtracttxtOutputs:
    """Build the ExtracttxtOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file("output.txt")
    return ExtracttxtOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def extracttxt_execute(params: Extracttxt, runner: Runner | None = None) -> ExtracttxtOutputs:
    """
    extracttxt

    Extracts text from a file based on a search word

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    extracttxt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EXTRACTTXT_METADATA)
    execution.params(params)
    args = extracttxt_cargs(params, execution)
    out = extracttxt_outputs(params, execution)
    execution.run(args)
    return out

def extracttxt(
    search_word: str,
    file: InputPathType,
    num_trailing_lines: float | None = None,
    relative_start: float | None = None,
    runner: Runner | None = None,
) -> ExtracttxtOutputs:
    """
    extracttxt

    Extracts text from a file based on a search word

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        search_word: The word to search for in the file
        file: Path to the file where text is to be extracted
        num_trailing_lines: Number of trailing lines to include after the search\
            word
        relative_start: Relative start position to begin the search
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = extracttxt_params(
        search_word=search_word,
        file=file,
        num_trailing_lines=num_trailing_lines,
        relative_start=relative_start,
    )
    return extracttxt_execute(params, runner)

__all__ = [
    "Extracttxt",
    "ExtracttxtOutputs",
    "EXTRACTTXT_METADATA",
    "extracttxt_cargs",
    "extracttxt_outputs",
    "extracttxt_params",
    "extracttxt_execute",
    "extracttxt_validate",
    "extracttxt",
]