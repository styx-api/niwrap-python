# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ATLASQUERY_METADATA = Metadata(
    id="atlasquery",
    name="atlasquery",
    package="fsl",
)

Atlasquery = typing.TypedDict(
    "Atlasquery",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/atlasquery"]],
        "dumpatlases_flag": typing.NotRequired[bool],
        "atlas": typing.NotRequired[str],
        "coord": typing.NotRequired[str],
        "mask": typing.NotRequired[InputPathType],
        "verbose_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AtlasqueryOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def atlasquery_params(
    dumpatlases_flag: bool = False,
    atlas: str | None = None,
    coord: str | None = None,
    mask: InputPathType | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
) -> Atlasquery:
    """
    Build parameters.

    Args:
        dumpatlases_flag: Dump a list of available atlases
        atlas: Name of atlas to use
        coord: Coordinate to query in the format X,Y,Z
        mask: A mask image to use during structural lookups
        verbose_flag: Switch on diagnostic messages
        help_flag: Show help message and exit

    Returns:
        Parameter dictionary.
    """
    params: Atlasquery = {
        "@type": "fsl/atlasquery",
        "dumpatlases_flag": dumpatlases_flag,
        "verbose_flag": verbose_flag,
        "help_flag": help_flag,
    }
    if atlas is not None:
        params["atlas"] = atlas
    if coord is not None:
        params["coord"] = coord
    if mask is not None:
        params["mask"] = mask
    return params

def atlasquery_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Atlasquery."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dumpatlases_flag", None) is not None:
        if not isinstance(params["dumpatlases_flag"], bool):
            raise StyxValidationError(f'`dumpatlases_flag` has the wrong type: Received `{type(params["dumpatlases_flag"])}` expected `bool`')
    if params.get("atlas", None) is not None:
        if not isinstance(params["atlas"], str):
            raise StyxValidationError(f'`atlas` has the wrong type: Received `{type(params["atlas"])}` expected `str`')
    if params.get("coord", None) is not None:
        if not isinstance(params["coord"], str):
            raise StyxValidationError(f'`coord` has the wrong type: Received `{type(params["coord"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def atlasquery_cargs(params: Atlasquery, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("atlasquery")
    if params.get("dumpatlases_flag"):
        cargs.append("--dumpatlases")
    v_0 = params.get("atlas")
    if v_0 is not None:
        cargs.append("-a")
        cargs.append(v_0)
    v_1 = params.get("coord")
    if v_1 is not None:
        cargs.append("-c")
        cargs.append(v_1)
    v_2 = params.get("mask")
    if v_2 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_2))
    if params.get("verbose_flag"):
        cargs.append("-V")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def atlasquery_outputs(params: Atlasquery, execution: Execution) -> AtlasqueryOutputs:
    """Build the AtlasqueryOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AtlasqueryOutputs(
        root=root_v,
    )

def atlasquery_execute(params: Atlasquery, runner: Runner | None = None) -> AtlasqueryOutputs:
    """
    atlasquery

    Structural lookup tool for FSL atlases.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    atlasquery_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ATLASQUERY_METADATA)
    execution.params(params)
    args = atlasquery_cargs(params, execution)
    out = atlasquery_outputs(params, execution)
    execution.run(args)
    return out

def atlasquery(
    dumpatlases_flag: bool = False,
    atlas: str | None = None,
    coord: str | None = None,
    mask: InputPathType | None = None,
    verbose_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> AtlasqueryOutputs:
    """
    atlasquery

    Structural lookup tool for FSL atlases.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        dumpatlases_flag: Dump a list of available atlases
        atlas: Name of atlas to use
        coord: Coordinate to query in the format X,Y,Z
        mask: A mask image to use during structural lookups
        verbose_flag: Switch on diagnostic messages
        help_flag: Show help message and exit
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = atlasquery_params(
        dumpatlases_flag=dumpatlases_flag,
        atlas=atlas,
        coord=coord,
        mask=mask,
        verbose_flag=verbose_flag,
        help_flag=help_flag,
    )
    return atlasquery_execute(params, runner)

__all__ = [
    "Atlasquery",
    "AtlasqueryOutputs",
    "ATLASQUERY_METADATA",
    "atlasquery_cargs",
    "atlasquery_outputs",
    "atlasquery_params",
    "atlasquery_execute",
    "atlasquery_validate",
    "atlasquery",
]