# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FDR_METADATA = Metadata(
    id="fdr",
    name="fdr",
    package="fsl",
)

Fdr = typing.TypedDict(
    "Fdr",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fdr"]],
        "infile": InputPathType,
        "maskfile": typing.NotRequired[InputPathType],
        "qvalue": typing.NotRequired[float],
        "adjustedimage": typing.NotRequired[str],
        "othresh_flag": typing.NotRequired[bool],
        "order_flag": typing.NotRequired[bool],
        "oneminusp_flag": typing.NotRequired[bool],
        "positive_corr_flag": typing.NotRequired[bool],
        "independent_flag": typing.NotRequired[bool],
        "conservative_flag": typing.NotRequired[bool],
        "debug_flag": typing.NotRequired[bool],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class FdrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_adjusted: OutputPathType | None
    """Output FDR-adjusted p-values image"""
    output_thresholded: OutputPathType
    """Thresholded output p-value image"""
    output_order: OutputPathType
    """Output order values image"""

def fdr_params(
    infile: InputPathType,
    maskfile: InputPathType | None = None,
    qvalue: float | None = None,
    adjustedimage: str | None = None,
    othresh_flag: bool = False,
    order_flag: bool = False,
    oneminusp_flag: bool = False,
    positive_corr_flag: bool = False,
    independent_flag: bool = False,
    conservative_flag: bool = False,
    debug_flag: bool = False,
    verbose_flag: bool = False,
) -> Fdr:
    """
    Build parameters.

    Args:
        infile: Input p-value image file
        maskfile: Mask image file
        qvalue: Q-value (FDR) threshold
        adjustedimage: Output image with FDR-adjusted p-values
        othresh_flag: Output a thresholded p-value image
        order_flag: Output image of order values
        oneminusp_flag: Treat input as 1-p (also save output like this)
        positive_corr_flag: Use FDR correction factor that assumes positive\
            correlation
        independent_flag: Use FDR correction factor that assumes independence
        conservative_flag: Use conservative FDR correction factor (allows for any\
            correlation)
        debug_flag: Switch on debugging output
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Fdr = {
        "@type": "fsl/fdr",
        "infile": infile,
        "othresh_flag": othresh_flag,
        "order_flag": order_flag,
        "oneminusp_flag": oneminusp_flag,
        "positive_corr_flag": positive_corr_flag,
        "independent_flag": independent_flag,
        "conservative_flag": conservative_flag,
        "debug_flag": debug_flag,
        "verbose_flag": verbose_flag,
    }
    if maskfile is not None:
        params["maskfile"] = maskfile
    if qvalue is not None:
        params["qvalue"] = qvalue
    if adjustedimage is not None:
        params["adjustedimage"] = adjustedimage
    return params

def fdr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fdr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("maskfile", None) is not None:
        if not isinstance(params["maskfile"], (pathlib.Path, str)):
            raise StyxValidationError(f'`maskfile` has the wrong type: Received `{type(params["maskfile"])}` expected `InputPathType`')
    if params.get("qvalue", None) is not None:
        if not isinstance(params["qvalue"], (float, int)):
            raise StyxValidationError(f'`qvalue` has the wrong type: Received `{type(params["qvalue"])}` expected `float`')
    if params.get("adjustedimage", None) is not None:
        if not isinstance(params["adjustedimage"], str):
            raise StyxValidationError(f'`adjustedimage` has the wrong type: Received `{type(params["adjustedimage"])}` expected `str`')
    if params.get("othresh_flag", None) is not None:
        if not isinstance(params["othresh_flag"], bool):
            raise StyxValidationError(f'`othresh_flag` has the wrong type: Received `{type(params["othresh_flag"])}` expected `bool`')
    if params.get("order_flag", None) is not None:
        if not isinstance(params["order_flag"], bool):
            raise StyxValidationError(f'`order_flag` has the wrong type: Received `{type(params["order_flag"])}` expected `bool`')
    if params.get("oneminusp_flag", None) is not None:
        if not isinstance(params["oneminusp_flag"], bool):
            raise StyxValidationError(f'`oneminusp_flag` has the wrong type: Received `{type(params["oneminusp_flag"])}` expected `bool`')
    if params.get("positive_corr_flag", None) is not None:
        if not isinstance(params["positive_corr_flag"], bool):
            raise StyxValidationError(f'`positive_corr_flag` has the wrong type: Received `{type(params["positive_corr_flag"])}` expected `bool`')
    if params.get("independent_flag", None) is not None:
        if not isinstance(params["independent_flag"], bool):
            raise StyxValidationError(f'`independent_flag` has the wrong type: Received `{type(params["independent_flag"])}` expected `bool`')
    if params.get("conservative_flag", None) is not None:
        if not isinstance(params["conservative_flag"], bool):
            raise StyxValidationError(f'`conservative_flag` has the wrong type: Received `{type(params["conservative_flag"])}` expected `bool`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def fdr_cargs(params: Fdr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fdr")
    cargs.append("-i")
    cargs.append(execution.input_file(params["infile"]))
    v_0 = params.get("maskfile")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("qvalue")
    if v_1 is not None:
        cargs.append("-q")
        cargs.append(str(v_1))
    v_2 = params.get("adjustedimage")
    if v_2 is not None:
        cargs.append("-a")
        cargs.append(v_2)
    if params.get("othresh_flag"):
        cargs.append("--othresh")
    if params.get("order_flag"):
        cargs.append("--order")
    if params.get("oneminusp_flag"):
        cargs.append("--oneminusp")
    if params.get("positive_corr_flag"):
        cargs.append("--positivecorr")
    if params.get("independent_flag"):
        cargs.append("--independent")
    if params.get("conservative_flag"):
        cargs.append("--conservative")
    if params.get("debug_flag"):
        cargs.append("--debug")
    if params.get("verbose_flag"):
        cargs.append("-v")
    return cargs

def fdr_outputs(params: Fdr, execution: Execution) -> FdrOutputs:
    """Build the FdrOutputs object for this tool."""
    output_adjusted_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("adjustedimage")
    if __v0 is not None:
        output_adjusted_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}.nii.gz')
    output_thresholded_v: OutputPathType = execution.output_file(f'{params["infile"]}_thr.nii.gz')
    output_order_v: OutputPathType = execution.output_file(f'{params["infile"]}_order.nii.gz')
    return FdrOutputs(
        root=root_v,
        output_adjusted=output_adjusted_v,
        output_thresholded=output_thresholded_v,
        output_order=output_order_v,
    )

def fdr_execute(params: Fdr, runner: Runner | None = None) -> FdrOutputs:
    """
    fdr

    False Discovery Rate (FDR) correction tool from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fdr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FDR_METADATA)
    execution.params(params)
    args = fdr_cargs(params, execution)
    out = fdr_outputs(params, execution)
    execution.run(args)
    return out

def fdr(
    infile: InputPathType,
    maskfile: InputPathType | None = None,
    qvalue: float | None = None,
    adjustedimage: str | None = None,
    othresh_flag: bool = False,
    order_flag: bool = False,
    oneminusp_flag: bool = False,
    positive_corr_flag: bool = False,
    independent_flag: bool = False,
    conservative_flag: bool = False,
    debug_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> FdrOutputs:
    """
    fdr

    False Discovery Rate (FDR) correction tool from FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input p-value image file
        maskfile: Mask image file
        qvalue: Q-value (FDR) threshold
        adjustedimage: Output image with FDR-adjusted p-values
        othresh_flag: Output a thresholded p-value image
        order_flag: Output image of order values
        oneminusp_flag: Treat input as 1-p (also save output like this)
        positive_corr_flag: Use FDR correction factor that assumes positive\
            correlation
        independent_flag: Use FDR correction factor that assumes independence
        conservative_flag: Use conservative FDR correction factor (allows for any\
            correlation)
        debug_flag: Switch on debugging output
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fdr_params(
        infile=infile,
        maskfile=maskfile,
        qvalue=qvalue,
        adjustedimage=adjustedimage,
        othresh_flag=othresh_flag,
        order_flag=order_flag,
        oneminusp_flag=oneminusp_flag,
        positive_corr_flag=positive_corr_flag,
        independent_flag=independent_flag,
        conservative_flag=conservative_flag,
        debug_flag=debug_flag,
        verbose_flag=verbose_flag,
    )
    return fdr_execute(params, runner)

__all__ = [
    "Fdr",
    "FdrOutputs",
    "FDR_METADATA",
    "fdr_cargs",
    "fdr_outputs",
    "fdr_params",
    "fdr_execute",
    "fdr_validate",
    "fdr",
]