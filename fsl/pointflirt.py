# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

POINTFLIRT_METADATA = Metadata(
    id="pointflirt",
    name="pointflirt",
    package="fsl",
)

Pointflirt = typing.TypedDict(
    "Pointflirt",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/pointflirt"]],
        "invol_coords": InputPathType,
        "refvol_coords": InputPathType,
        "out_matrix": typing.NotRequired[str],
        "use_vox": typing.NotRequired[bool],
        "vol_input": typing.NotRequired[InputPathType],
        "vol_ref": typing.NotRequired[InputPathType],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class PointflirtOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_matrix_file: OutputPathType | None
    """Affine matrix output file"""

def pointflirt_params(
    invol_coords: InputPathType,
    refvol_coords: InputPathType,
    out_matrix: str | None = None,
    use_vox: bool = False,
    vol_input: InputPathType | None = None,
    vol_ref: InputPathType | None = None,
    verbose_flag: bool = False,
) -> Pointflirt:
    """
    Build parameters.

    Args:
        invol_coords: Filename of input volume coordinates
        refvol_coords: Filename of reference volume coordinates
        out_matrix: Filename of affine matrix output
        use_vox: Use voxel coordinates, not mm, for input
        vol_input: Filename of input volume (needed for --vox option)
        vol_ref: Filename of reference volume (needed for --vox option)
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: Pointflirt = {
        "@type": "fsl/pointflirt",
        "invol_coords": invol_coords,
        "refvol_coords": refvol_coords,
        "use_vox": use_vox,
        "verbose_flag": verbose_flag,
    }
    if out_matrix is not None:
        params["out_matrix"] = out_matrix
    if vol_input is not None:
        params["vol_input"] = vol_input
    if vol_ref is not None:
        params["vol_ref"] = vol_ref
    return params

def pointflirt_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Pointflirt."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("invol_coords", None) is None:
        raise StyxValidationError("`invol_coords` must not be None")
    if not isinstance(params["invol_coords"], (pathlib.Path, str)):
        raise StyxValidationError(f'`invol_coords` has the wrong type: Received `{type(params["invol_coords"])}` expected `InputPathType`')
    if params.get("refvol_coords", None) is None:
        raise StyxValidationError("`refvol_coords` must not be None")
    if not isinstance(params["refvol_coords"], (pathlib.Path, str)):
        raise StyxValidationError(f'`refvol_coords` has the wrong type: Received `{type(params["refvol_coords"])}` expected `InputPathType`')
    if params.get("out_matrix", None) is not None:
        if not isinstance(params["out_matrix"], str):
            raise StyxValidationError(f'`out_matrix` has the wrong type: Received `{type(params["out_matrix"])}` expected `str`')
    if params.get("use_vox", None) is not None:
        if not isinstance(params["use_vox"], bool):
            raise StyxValidationError(f'`use_vox` has the wrong type: Received `{type(params["use_vox"])}` expected `bool`')
    if params.get("vol_input", None) is not None:
        if not isinstance(params["vol_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_input` has the wrong type: Received `{type(params["vol_input"])}` expected `InputPathType`')
    if params.get("vol_ref", None) is not None:
        if not isinstance(params["vol_ref"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vol_ref` has the wrong type: Received `{type(params["vol_ref"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def pointflirt_cargs(params: Pointflirt, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("pointflirt")
    cargs.append("-i")
    cargs.append(execution.input_file(params["invol_coords"]))
    cargs.append("-r")
    cargs.append(execution.input_file(params["refvol_coords"]))
    v_0 = params.get("out_matrix")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    if params.get("use_vox"):
        cargs.append("--vox")
    v_1 = params.get("vol_input")
    if v_1 is not None:
        cargs.append("--invol")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("vol_ref")
    if v_2 is not None:
        cargs.append("--refvol")
        cargs.append(execution.input_file(v_2))
    if params.get("verbose_flag"):
        cargs.append("-v")
    return cargs

def pointflirt_outputs(params: Pointflirt, execution: Execution) -> PointflirtOutputs:
    """Build the PointflirtOutputs object for this tool."""
    output_matrix_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("out_matrix")
    if __v0 is not None:
        output_matrix_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return PointflirtOutputs(
        root=root_v,
        output_matrix_file=output_matrix_file_v,
    )

def pointflirt_execute(params: Pointflirt, runner: Runner | None = None) -> PointflirtOutputs:
    """
    pointflirt

    A tool to align point coordinates between volumes and compute affine transformation matrices.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    pointflirt_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(POINTFLIRT_METADATA)
    execution.params(params)
    args = pointflirt_cargs(params, execution)
    out = pointflirt_outputs(params, execution)
    execution.run(args)
    return out

def pointflirt(
    invol_coords: InputPathType,
    refvol_coords: InputPathType,
    out_matrix: str | None = None,
    use_vox: bool = False,
    vol_input: InputPathType | None = None,
    vol_ref: InputPathType | None = None,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> PointflirtOutputs:
    """
    pointflirt

    A tool to align point coordinates between volumes and compute affine transformation matrices.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        invol_coords: Filename of input volume coordinates
        refvol_coords: Filename of reference volume coordinates
        out_matrix: Filename of affine matrix output
        use_vox: Use voxel coordinates, not mm, for input
        vol_input: Filename of input volume (needed for --vox option)
        vol_ref: Filename of reference volume (needed for --vox option)
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = pointflirt_params(
        invol_coords=invol_coords,
        refvol_coords=refvol_coords,
        out_matrix=out_matrix,
        use_vox=use_vox,
        vol_input=vol_input,
        vol_ref=vol_ref,
        verbose_flag=verbose_flag,
    )
    return pointflirt_execute(params, runner)

__all__ = [
    "Pointflirt",
    "PointflirtOutputs",
    "POINTFLIRT_METADATA",
    "pointflirt_cargs",
    "pointflirt_outputs",
    "pointflirt_params",
    "pointflirt_execute",
    "pointflirt_validate",
    "pointflirt",
]