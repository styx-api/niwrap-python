# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ESTNOISE_METADATA = Metadata(
    id="estnoise",
    name="estnoise",
    package="fsl",
)

Estnoise = typing.TypedDict(
    "Estnoise",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/estnoise"]],
        "input_4d_data": InputPathType,
        "spatial_sigma": typing.NotRequired[float],
        "temp_hp_sigma": typing.NotRequired[float],
        "temp_lp_sigma": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class EstnoiseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_noise_file: OutputPathType
    """Output text file containing noise estimates"""

def estnoise_params(
    input_4d_data: InputPathType,
    spatial_sigma: float | None = None,
    temp_hp_sigma: float | None = None,
    temp_lp_sigma: float | None = None,
) -> Estnoise:
    """
    Build parameters.

    Args:
        input_4d_data: Input 4D fMRI data (e.g., fmri_data.nii.gz)
        spatial_sigma: Spatial smoothing sigma
        temp_hp_sigma: Temporal high-pass filter sigma
        temp_lp_sigma: Temporal low-pass filter sigma

    Returns:
        Parameter dictionary.
    """
    params: Estnoise = {
        "@type": "fsl/estnoise",
        "input_4d_data": input_4d_data,
    }
    if spatial_sigma is not None:
        params["spatial_sigma"] = spatial_sigma
    if temp_hp_sigma is not None:
        params["temp_hp_sigma"] = temp_hp_sigma
    if temp_lp_sigma is not None:
        params["temp_lp_sigma"] = temp_lp_sigma
    return params

def estnoise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Estnoise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_4d_data", None) is None:
        raise StyxValidationError("`input_4d_data` must not be None")
    if not isinstance(params["input_4d_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_4d_data` has the wrong type: Received `{type(params["input_4d_data"])}` expected `InputPathType`')
    if params.get("spatial_sigma", None) is not None:
        if not isinstance(params["spatial_sigma"], (float, int)):
            raise StyxValidationError(f'`spatial_sigma` has the wrong type: Received `{type(params["spatial_sigma"])}` expected `float`')
    if params.get("temp_hp_sigma", None) is not None:
        if not isinstance(params["temp_hp_sigma"], (float, int)):
            raise StyxValidationError(f'`temp_hp_sigma` has the wrong type: Received `{type(params["temp_hp_sigma"])}` expected `float`')
    if params.get("temp_lp_sigma", None) is not None:
        if not isinstance(params["temp_lp_sigma"], (float, int)):
            raise StyxValidationError(f'`temp_lp_sigma` has the wrong type: Received `{type(params["temp_lp_sigma"])}` expected `float`')

def estnoise_cargs(params: Estnoise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("estnoise")
    cargs.append(execution.input_file(params["input_4d_data"]))
    v_0 = params.get("spatial_sigma")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("temp_hp_sigma")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("temp_lp_sigma")
    if v_2 is not None:
        cargs.append(str(v_2))
    return cargs

def estnoise_outputs(params: Estnoise, execution: Execution) -> EstnoiseOutputs:
    """Build the EstnoiseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_noise_file_v: OutputPathType = execution.output_file("noise_estimate.txt")
    return EstnoiseOutputs(
        root=root_v,
        output_noise_file=output_noise_file_v,
    )

def estnoise_execute(params: Estnoise, runner: Runner | None = None) -> EstnoiseOutputs:
    """
    estnoise

    Estimate noise in 4D fMRI data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    estnoise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ESTNOISE_METADATA)
    execution.params(params)
    args = estnoise_cargs(params, execution)
    out = estnoise_outputs(params, execution)
    execution.run(args)
    return out

def estnoise(
    input_4d_data: InputPathType,
    spatial_sigma: float | None = None,
    temp_hp_sigma: float | None = None,
    temp_lp_sigma: float | None = None,
    runner: Runner | None = None,
) -> EstnoiseOutputs:
    """
    estnoise

    Estimate noise in 4D fMRI data.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_4d_data: Input 4D fMRI data (e.g., fmri_data.nii.gz)
        spatial_sigma: Spatial smoothing sigma
        temp_hp_sigma: Temporal high-pass filter sigma
        temp_lp_sigma: Temporal low-pass filter sigma
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = estnoise_params(
        input_4d_data=input_4d_data,
        spatial_sigma=spatial_sigma,
        temp_hp_sigma=temp_hp_sigma,
        temp_lp_sigma=temp_lp_sigma,
    )
    return estnoise_execute(params, runner)

__all__ = [
    "Estnoise",
    "EstnoiseOutputs",
    "ESTNOISE_METADATA",
    "estnoise_cargs",
    "estnoise_outputs",
    "estnoise_params",
    "estnoise_execute",
    "estnoise_validate",
    "estnoise",
]