# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMCP_METADATA = Metadata(
    id="imcp",
    name="imcp",
    package="fsl",
)

Imcp = typing.TypedDict(
    "Imcp",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/imcp"]],
        "infiles": list[InputPathType],
        "output_location": str,
    },
)

@dataclasses.dataclass
class ImcpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfiles: OutputPathType
    """Output file or directory"""

def imcp_params(
    infiles: list[InputPathType],
    output_location: str,
) -> Imcp:
    """
    Build parameters.

    Args:
        infiles: Input image files (e.g. img1.nii.gz, img2.nii.gz)
        output_location: Output file or directory

    Returns:
        Parameter dictionary.
    """
    params: Imcp = {
        "@type": "fsl/imcp",
        "infiles": infiles,
        "output_location": output_location,
    }
    return params

def imcp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Imcp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infiles", None) is None:
        raise StyxValidationError("`infiles` must not be None")
    if not isinstance(params["infiles"], list):
        raise StyxValidationError(f'`infiles` has the wrong type: Received `{type(params["infiles"])}` expected `list[InputPathType]`')
    for e in params["infiles"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`infiles` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("output_location", None) is None:
        raise StyxValidationError("`output_location` must not be None")
    if not isinstance(params["output_location"], str):
        raise StyxValidationError(f'`output_location` has the wrong type: Received `{type(params["output_location"])}` expected `str`')

def imcp_cargs(params: Imcp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("imcp")
    for item0 in params["infiles"]:
        cargs.append(execution.input_file(item0))
    cargs.append(params["output_location"])
    return cargs

def imcp_outputs(params: Imcp, execution: Execution) -> ImcpOutputs:
    """Build the ImcpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfiles_v: OutputPathType = execution.output_file(params["output_location"])
    return ImcpOutputs(
        root=root_v,
        outfiles=outfiles_v,
    )

def imcp_execute(params: Imcp, runner: Runner | None = None) -> ImcpOutputs:
    """
    imcp

    Copy images from one location to another

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    imcp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMCP_METADATA)
    execution.params(params)
    args = imcp_cargs(params, execution)
    out = imcp_outputs(params, execution)
    execution.run(args)
    return out

def imcp(
    infiles: list[InputPathType],
    output_location: str,
    runner: Runner | None = None,
) -> ImcpOutputs:
    """
    imcp

    Copy images from one location to another

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infiles: Input image files (e.g. img1.nii.gz, img2.nii.gz)
        output_location: Output file or directory
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = imcp_params(
        infiles=infiles,
        output_location=output_location,
    )
    return imcp_execute(params, runner)

__all__ = [
    "Imcp",
    "ImcpOutputs",
    "IMCP_METADATA",
    "imcp_cargs",
    "imcp_outputs",
    "imcp_params",
    "imcp_execute",
    "imcp_validate",
    "imcp",
]