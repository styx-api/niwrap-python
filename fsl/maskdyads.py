# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MASKDYADS_METADATA = Metadata(
    id="maskdyads",
    name="maskdyads",
    package="fsl",
)

Maskdyads = typing.TypedDict(
    "Maskdyads",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/maskdyads"]],
        "dyads": InputPathType,
        "fsamples": InputPathType,
        "threshold": typing.NotRequired[float],
    },
)

@dataclasses.dataclass
class MaskdyadsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def maskdyads_params(
    dyads: InputPathType,
    fsamples: InputPathType,
    threshold: float | None = None,
) -> Maskdyads:
    """
    Build parameters.

    Args:
        dyads: Input dyads file
        fsamples: Input fsamples file
        threshold: Threshold (default is 0.05)

    Returns:
        Parameter dictionary.
    """
    params: Maskdyads = {
        "@type": "fsl/maskdyads",
        "dyads": dyads,
        "fsamples": fsamples,
    }
    if threshold is not None:
        params["threshold"] = threshold
    return params

def maskdyads_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Maskdyads."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dyads", None) is None:
        raise StyxValidationError("`dyads` must not be None")
    if not isinstance(params["dyads"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dyads` has the wrong type: Received `{type(params["dyads"])}` expected `InputPathType`')
    if params.get("fsamples", None) is None:
        raise StyxValidationError("`fsamples` must not be None")
    if not isinstance(params["fsamples"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fsamples` has the wrong type: Received `{type(params["fsamples"])}` expected `InputPathType`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')

def maskdyads_cargs(params: Maskdyads, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("maskdyads")
    cargs.append(execution.input_file(params["dyads"]))
    cargs.append(execution.input_file(params["fsamples"]))
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("[THR]")
        cargs.append(str(v_0))
    return cargs

def maskdyads_outputs(params: Maskdyads, execution: Execution) -> MaskdyadsOutputs:
    """Build the MaskdyadsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MaskdyadsOutputs(
        root=root_v,
    )

def maskdyads_execute(params: Maskdyads, runner: Runner | None = None) -> MaskdyadsOutputs:
    """
    maskdyads

    Tool to mask dyads with threshold

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    maskdyads_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MASKDYADS_METADATA)
    execution.params(params)
    args = maskdyads_cargs(params, execution)
    out = maskdyads_outputs(params, execution)
    execution.run(args)
    return out

def maskdyads(
    dyads: InputPathType,
    fsamples: InputPathType,
    threshold: float | None = None,
    runner: Runner | None = None,
) -> MaskdyadsOutputs:
    """
    maskdyads

    Tool to mask dyads with threshold

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        dyads: Input dyads file
        fsamples: Input fsamples file
        threshold: Threshold (default is 0.05)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = maskdyads_params(
        dyads=dyads,
        fsamples=fsamples,
        threshold=threshold,
    )
    return maskdyads_execute(params, runner)

__all__ = [
    "Maskdyads",
    "MaskdyadsOutputs",
    "MASKDYADS_METADATA",
    "maskdyads_cargs",
    "maskdyads_outputs",
    "maskdyads_params",
    "maskdyads_execute",
    "maskdyads_validate",
    "maskdyads",
]