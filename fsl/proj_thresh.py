# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PROJ_THRESH_METADATA = Metadata(
    id="proj_thresh",
    name="proj_thresh",
    package="fsl",
)

ProjThresh = typing.TypedDict(
    "ProjThresh",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/proj_thresh"]],
        "input_paths": list[InputPathType],
        "threshold": float,
    },
)

@dataclasses.dataclass
class ProjThreshOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def proj_thresh_params(
    input_paths: list[InputPathType],
    threshold: float,
) -> ProjThresh:
    """
    Build parameters.

    Args:
        input_paths: Paths to volume or surface files. Please use either volumes or\
            surfaces but not both.
        threshold: Threshold value to be applied.

    Returns:
        Parameter dictionary.
    """
    params: ProjThresh = {
        "@type": "fsl/proj_thresh",
        "input_paths": input_paths,
        "threshold": threshold,
    }
    return params

def proj_thresh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ProjThresh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_paths", None) is None:
        raise StyxValidationError("`input_paths` must not be None")
    if not isinstance(params["input_paths"], list):
        raise StyxValidationError(f'`input_paths` has the wrong type: Received `{type(params["input_paths"])}` expected `list[InputPathType]`')
    for e in params["input_paths"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_paths` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("threshold", None) is None:
        raise StyxValidationError("`threshold` must not be None")
    if not isinstance(params["threshold"], (float, int)):
        raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')

def proj_thresh_cargs(params: ProjThresh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("proj_thresh")
    for item0 in params["input_paths"]:
        cargs.append(execution.input_file(item0))
    cargs.append(str(params["threshold"]))
    return cargs

def proj_thresh_outputs(params: ProjThresh, execution: Execution) -> ProjThreshOutputs:
    """Build the ProjThreshOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ProjThreshOutputs(
        root=root_v,
    )

def proj_thresh_execute(params: ProjThresh, runner: Runner | None = None) -> ProjThreshOutputs:
    """
    proj_thresh

    A tool to apply a threshold to either volumes or surfaces.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    proj_thresh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PROJ_THRESH_METADATA)
    execution.params(params)
    args = proj_thresh_cargs(params, execution)
    out = proj_thresh_outputs(params, execution)
    execution.run(args)
    return out

def proj_thresh(
    input_paths: list[InputPathType],
    threshold: float,
    runner: Runner | None = None,
) -> ProjThreshOutputs:
    """
    proj_thresh

    A tool to apply a threshold to either volumes or surfaces.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_paths: Paths to volume or surface files. Please use either volumes or\
            surfaces but not both.
        threshold: Threshold value to be applied.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = proj_thresh_params(
        input_paths=input_paths,
        threshold=threshold,
    )
    return proj_thresh_execute(params, runner)

__all__ = [
    "ProjThresh",
    "ProjThreshOutputs",
    "PROJ_THRESH_METADATA",
    "proj_thresh_cargs",
    "proj_thresh_outputs",
    "proj_thresh_params",
    "proj_thresh_execute",
    "proj_thresh_validate",
    "proj_thresh",
]