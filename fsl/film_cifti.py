# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FILM_CIFTI_METADATA = Metadata(
    id="film_cifti",
    name="film_cifti",
    package="fsl",
)

FilmCifti = typing.TypedDict(
    "FilmCifti",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/film_cifti"]],
        "input_filename": InputPathType,
        "basename": str,
        "left_surface": InputPathType,
        "right_surface": InputPathType,
        "susan_threshold": typing.NotRequired[float],
        "susan_extent": typing.NotRequired[float],
        "surface_sigma": typing.NotRequired[float],
        "surface_extent": typing.NotRequired[float],
        "film_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class FilmCiftiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_results: OutputPathType
    """Output results file"""

def film_cifti_params(
    input_filename: InputPathType,
    basename: str,
    left_surface: InputPathType,
    right_surface: InputPathType,
    susan_threshold: float | None = None,
    susan_extent: float | None = None,
    surface_sigma: float | None = None,
    surface_extent: float | None = None,
    film_options: str | None = None,
) -> FilmCifti:
    """
    Build parameters.

    Args:
        input_filename: Input CIFTI file
        basename: Output basename
        left_surface: Geometry for left cortex
        right_surface: Geometry for right cortex
        susan_threshold: Susan brightness threshold for volumetric analysis\
            (default: 0)
        susan_extent: Susan mask size for volumetric analysis (default: 4)
        surface_sigma: Smoothing sigma for surface analysis (default: 0)
        surface_extent: Smoothing extent for surface analysis (default: 4)
        film_options: Film options to be used with all modes, list must be wrapped\
            by quotes

    Returns:
        Parameter dictionary.
    """
    params: FilmCifti = {
        "@type": "fsl/film_cifti",
        "input_filename": input_filename,
        "basename": basename,
        "left_surface": left_surface,
        "right_surface": right_surface,
    }
    if susan_threshold is not None:
        params["susan_threshold"] = susan_threshold
    if susan_extent is not None:
        params["susan_extent"] = susan_extent
    if surface_sigma is not None:
        params["surface_sigma"] = surface_sigma
    if surface_extent is not None:
        params["surface_extent"] = surface_extent
    if film_options is not None:
        params["film_options"] = film_options
    return params

def film_cifti_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid FilmCifti."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_filename", None) is None:
        raise StyxValidationError("`input_filename` must not be None")
    if not isinstance(params["input_filename"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_filename` has the wrong type: Received `{type(params["input_filename"])}` expected `InputPathType`')
    if params.get("basename", None) is None:
        raise StyxValidationError("`basename` must not be None")
    if not isinstance(params["basename"], str):
        raise StyxValidationError(f'`basename` has the wrong type: Received `{type(params["basename"])}` expected `str`')
    if params.get("left_surface", None) is None:
        raise StyxValidationError("`left_surface` must not be None")
    if not isinstance(params["left_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`left_surface` has the wrong type: Received `{type(params["left_surface"])}` expected `InputPathType`')
    if params.get("right_surface", None) is None:
        raise StyxValidationError("`right_surface` must not be None")
    if not isinstance(params["right_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`right_surface` has the wrong type: Received `{type(params["right_surface"])}` expected `InputPathType`')
    if params.get("susan_threshold", None) is not None:
        if not isinstance(params["susan_threshold"], (float, int)):
            raise StyxValidationError(f'`susan_threshold` has the wrong type: Received `{type(params["susan_threshold"])}` expected `float`')
    if params.get("susan_extent", None) is not None:
        if not isinstance(params["susan_extent"], (float, int)):
            raise StyxValidationError(f'`susan_extent` has the wrong type: Received `{type(params["susan_extent"])}` expected `float`')
    if params.get("surface_sigma", None) is not None:
        if not isinstance(params["surface_sigma"], (float, int)):
            raise StyxValidationError(f'`surface_sigma` has the wrong type: Received `{type(params["surface_sigma"])}` expected `float`')
    if params.get("surface_extent", None) is not None:
        if not isinstance(params["surface_extent"], (float, int)):
            raise StyxValidationError(f'`surface_extent` has the wrong type: Received `{type(params["surface_extent"])}` expected `float`')
    if params.get("film_options", None) is not None:
        if not isinstance(params["film_options"], str):
            raise StyxValidationError(f'`film_options` has the wrong type: Received `{type(params["film_options"])}` expected `str`')

def film_cifti_cargs(params: FilmCifti, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("film_cifti")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_filename"]))
    cargs.append("-o")
    cargs.append(params["basename"])
    cargs.append("-l")
    cargs.append(execution.input_file(params["left_surface"]))
    cargs.append("-r")
    cargs.append(execution.input_file(params["right_surface"]))
    v_0 = params.get("susan_threshold")
    if v_0 is not None:
        cargs.append("--st")
        cargs.append(str(v_0))
    v_1 = params.get("susan_extent")
    if v_1 is not None:
        cargs.append("--sm")
        cargs.append(str(v_1))
    v_2 = params.get("surface_sigma")
    if v_2 is not None:
        cargs.append("--ss")
        cargs.append(str(v_2))
    v_3 = params.get("surface_extent")
    if v_3 is not None:
        cargs.append("--se")
        cargs.append(str(v_3))
    v_4 = params.get("film_options")
    if v_4 is not None:
        cargs.append("--filmOptions")
        cargs.append(v_4)
    return cargs

def film_cifti_outputs(params: FilmCifti, execution: Execution) -> FilmCiftiOutputs:
    """Build the FilmCiftiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_results_v: OutputPathType = execution.output_file(f'{params["basename"]}_results.nii.gz')
    return FilmCiftiOutputs(
        root=root_v,
        output_results=output_results_v,
    )

def film_cifti_execute(params: FilmCifti, runner: Runner | None = None) -> FilmCiftiOutputs:
    """
    film_cifti

    A tool for statistical analysis of CIFTI files using FILM.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    film_cifti_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FILM_CIFTI_METADATA)
    execution.params(params)
    args = film_cifti_cargs(params, execution)
    out = film_cifti_outputs(params, execution)
    execution.run(args)
    return out

def film_cifti(
    input_filename: InputPathType,
    basename: str,
    left_surface: InputPathType,
    right_surface: InputPathType,
    susan_threshold: float | None = None,
    susan_extent: float | None = None,
    surface_sigma: float | None = None,
    surface_extent: float | None = None,
    film_options: str | None = None,
    runner: Runner | None = None,
) -> FilmCiftiOutputs:
    """
    film_cifti

    A tool for statistical analysis of CIFTI files using FILM.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_filename: Input CIFTI file
        basename: Output basename
        left_surface: Geometry for left cortex
        right_surface: Geometry for right cortex
        susan_threshold: Susan brightness threshold for volumetric analysis\
            (default: 0)
        susan_extent: Susan mask size for volumetric analysis (default: 4)
        surface_sigma: Smoothing sigma for surface analysis (default: 0)
        surface_extent: Smoothing extent for surface analysis (default: 4)
        film_options: Film options to be used with all modes, list must be wrapped\
            by quotes
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = film_cifti_params(
        input_filename=input_filename,
        basename=basename,
        left_surface=left_surface,
        right_surface=right_surface,
        susan_threshold=susan_threshold,
        susan_extent=susan_extent,
        surface_sigma=surface_sigma,
        surface_extent=surface_extent,
        film_options=film_options,
    )
    return film_cifti_execute(params, runner)

__all__ = [
    "FilmCifti",
    "FilmCiftiOutputs",
    "FILM_CIFTI_METADATA",
    "film_cifti_cargs",
    "film_cifti_outputs",
    "film_cifti_params",
    "film_cifti_execute",
    "film_cifti_validate",
    "film_cifti",
]