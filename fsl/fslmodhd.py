# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLMODHD_METADATA = Metadata(
    id="fslmodhd",
    name="fslmodhd",
    package="fsl",
)

Fslmodhd = typing.TypedDict(
    "Fslmodhd",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslmodhd"]],
        "image": InputPathType,
        "keyword": str,
        "value": str,
    },
)

@dataclasses.dataclass
class FslmodhdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fslmodhd_params(
    image: InputPathType,
    keyword: str,
    value: str,
) -> Fslmodhd:
    """
    Build parameters.

    Args:
        image: Input image file (e.g. image.nii.gz)
        keyword: Header keyword to modify (e.g. 'dim', 'pixdim')
        value: New value for the given header keyword

    Returns:
        Parameter dictionary.
    """
    params: Fslmodhd = {
        "@type": "fsl/fslmodhd",
        "image": image,
        "keyword": keyword,
        "value": value,
    }
    return params

def fslmodhd_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslmodhd."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("keyword", None) is None:
        raise StyxValidationError("`keyword` must not be None")
    if not isinstance(params["keyword"], str):
        raise StyxValidationError(f'`keyword` has the wrong type: Received `{type(params["keyword"])}` expected `str`')
    if params.get("value", None) is None:
        raise StyxValidationError("`value` must not be None")
    if not isinstance(params["value"], str):
        raise StyxValidationError(f'`value` has the wrong type: Received `{type(params["value"])}` expected `str`')

def fslmodhd_cargs(params: Fslmodhd, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslmodhd")
    cargs.append(execution.input_file(params["image"]))
    cargs.append(params["keyword"])
    cargs.append(params["value"])
    return cargs

def fslmodhd_outputs(params: Fslmodhd, execution: Execution) -> FslmodhdOutputs:
    """Build the FslmodhdOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FslmodhdOutputs(
        root=root_v,
    )

def fslmodhd_execute(params: Fslmodhd, runner: Runner | None = None) -> FslmodhdOutputs:
    """
    fslmodhd

    A tool for modifying header information of NIfTI images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslmodhd_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLMODHD_METADATA)
    execution.params(params)
    args = fslmodhd_cargs(params, execution)
    out = fslmodhd_outputs(params, execution)
    execution.run(args)
    return out

def fslmodhd(
    image: InputPathType,
    keyword: str,
    value: str,
    runner: Runner | None = None,
) -> FslmodhdOutputs:
    """
    fslmodhd

    A tool for modifying header information of NIfTI images

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        image: Input image file (e.g. image.nii.gz)
        keyword: Header keyword to modify (e.g. 'dim', 'pixdim')
        value: New value for the given header keyword
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslmodhd_params(
        image=image,
        keyword=keyword,
        value=value,
    )
    return fslmodhd_execute(params, runner)

__all__ = [
    "Fslmodhd",
    "FslmodhdOutputs",
    "FSLMODHD_METADATA",
    "fslmodhd_cargs",
    "fslmodhd_outputs",
    "fslmodhd_params",
    "fslmodhd_execute",
    "fslmodhd_validate",
    "fslmodhd",
]