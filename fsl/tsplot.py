# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TSPLOT_METADATA = Metadata(
    id="tsplot",
    name="tsplot",
    package="fsl",
)

Tsplot = typing.TypedDict(
    "Tsplot",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tsplot"]],
        "input_directory": str,
        "main_filtered_data": typing.NotRequired[InputPathType],
        "coordinates": typing.NotRequired[list[float]],
        "coordinates_output": typing.NotRequired[list[float]],
        "mask": typing.NotRequired[InputPathType],
        "output_directory": typing.NotRequired[str],
        "no_weight_flag": typing.NotRequired[bool],
        "prewhiten_flag": typing.NotRequired[bool],
        "no_raw_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TsplotOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    timeseries_output: OutputPathType | None
    """Output timeseries data"""

def tsplot_params(
    input_directory: str,
    main_filtered_data: InputPathType | None = None,
    coordinates: list[float] | None = None,
    coordinates_output: list[float] | None = None,
    mask: InputPathType | None = None,
    output_directory: str | None = None,
    no_weight_flag: bool = False,
    prewhiten_flag: bool = False,
    no_raw_flag: bool = False,
) -> Tsplot:
    """
    Build parameters.

    Args:
        input_directory: Input FEAT directory (e.g. feat_directory.feat)
        main_filtered_data: Input main filtered data, in case it's not\
            <feat_directory.feat>/filtered_func_data
        coordinates: Use X, Y, Z instead of max Z stat position
        coordinates_output: Use X,Y,Z to output time series only - no stats or\
            modelling
        mask: Use mask image instead of thresholded activation images
        output_directory: Change output directory from default of input FEAT\
            directory
        no_weight_flag: Don't weight cluster averaging with Z stats
        prewhiten_flag: Prewhiten data and model timeseries before plotting
        no_raw_flag: Don't keep raw data text files

    Returns:
        Parameter dictionary.
    """
    params: Tsplot = {
        "@type": "fsl/tsplot",
        "input_directory": input_directory,
        "no_weight_flag": no_weight_flag,
        "prewhiten_flag": prewhiten_flag,
        "no_raw_flag": no_raw_flag,
    }
    if main_filtered_data is not None:
        params["main_filtered_data"] = main_filtered_data
    if coordinates is not None:
        params["coordinates"] = coordinates
    if coordinates_output is not None:
        params["coordinates_output"] = coordinates_output
    if mask is not None:
        params["mask"] = mask
    if output_directory is not None:
        params["output_directory"] = output_directory
    return params

def tsplot_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tsplot."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_directory", None) is None:
        raise StyxValidationError("`input_directory` must not be None")
    if not isinstance(params["input_directory"], str):
        raise StyxValidationError(f'`input_directory` has the wrong type: Received `{type(params["input_directory"])}` expected `str`')
    if params.get("main_filtered_data", None) is not None:
        if not isinstance(params["main_filtered_data"], (pathlib.Path, str)):
            raise StyxValidationError(f'`main_filtered_data` has the wrong type: Received `{type(params["main_filtered_data"])}` expected `InputPathType`')
    if params.get("coordinates", None) is not None:
        if not isinstance(params["coordinates"], list):
            raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(params["coordinates"])}` expected `list[float]`')
        if not (3 <= len(params["coordinates"]) <= 3):
            raise StyxValidationError("Parameter `coordinates` must contain between 3 and 3 elements (inclusive)")
        for e in params["coordinates"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`coordinates` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("coordinates_output", None) is not None:
        if not isinstance(params["coordinates_output"], list):
            raise StyxValidationError(f'`coordinates_output` has the wrong type: Received `{type(params["coordinates_output"])}` expected `list[float]`')
        if not (3 <= len(params["coordinates_output"]) <= 3):
            raise StyxValidationError("Parameter `coordinates_output` must contain between 3 and 3 elements (inclusive)")
        for e_2 in params["coordinates_output"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`coordinates_output` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("output_directory", None) is not None:
        if not isinstance(params["output_directory"], str):
            raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("no_weight_flag", None) is not None:
        if not isinstance(params["no_weight_flag"], bool):
            raise StyxValidationError(f'`no_weight_flag` has the wrong type: Received `{type(params["no_weight_flag"])}` expected `bool`')
    if params.get("prewhiten_flag", None) is not None:
        if not isinstance(params["prewhiten_flag"], bool):
            raise StyxValidationError(f'`prewhiten_flag` has the wrong type: Received `{type(params["prewhiten_flag"])}` expected `bool`')
    if params.get("no_raw_flag", None) is not None:
        if not isinstance(params["no_raw_flag"], bool):
            raise StyxValidationError(f'`no_raw_flag` has the wrong type: Received `{type(params["no_raw_flag"])}` expected `bool`')

def tsplot_cargs(params: Tsplot, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tsplot")
    cargs.append(params["input_directory"])
    v_0 = params.get("main_filtered_data")
    if v_0 is not None:
        cargs.append("-f")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("coordinates")
    if v_1 is not None:
        cargs.append("-c")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("coordinates_output")
    if v_2 is not None:
        cargs.append("-C")
        for item1 in v_2:
            cargs.append(str(item1))
    v_3 = params.get("mask")
    if v_3 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("output_directory")
    if v_4 is not None:
        cargs.append("-o")
        cargs.append(v_4)
    if params.get("no_weight_flag"):
        cargs.append("-n")
    if params.get("prewhiten_flag"):
        cargs.append("-p")
    if params.get("no_raw_flag"):
        cargs.append("-d")
    return cargs

def tsplot_outputs(params: Tsplot, execution: Execution) -> TsplotOutputs:
    """Build the TsplotOutputs object for this tool."""
    timeseries_output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_directory")
    if __v0 is not None:
        timeseries_output_v = execution.output_file(f'{(__v0 if __v0 is not None else "")}/timeseries.txt')
    return TsplotOutputs(
        root=root_v,
        timeseries_output=timeseries_output_v,
    )

def tsplot_execute(params: Tsplot, runner: Runner | None = None) -> TsplotOutputs:
    """
    tsplot

    Time series plotting tool for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tsplot_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TSPLOT_METADATA)
    execution.params(params)
    args = tsplot_cargs(params, execution)
    out = tsplot_outputs(params, execution)
    execution.run(args)
    return out

def tsplot(
    input_directory: str,
    main_filtered_data: InputPathType | None = None,
    coordinates: list[float] | None = None,
    coordinates_output: list[float] | None = None,
    mask: InputPathType | None = None,
    output_directory: str | None = None,
    no_weight_flag: bool = False,
    prewhiten_flag: bool = False,
    no_raw_flag: bool = False,
    runner: Runner | None = None,
) -> TsplotOutputs:
    """
    tsplot

    Time series plotting tool for FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_directory: Input FEAT directory (e.g. feat_directory.feat)
        main_filtered_data: Input main filtered data, in case it's not\
            <feat_directory.feat>/filtered_func_data
        coordinates: Use X, Y, Z instead of max Z stat position
        coordinates_output: Use X,Y,Z to output time series only - no stats or\
            modelling
        mask: Use mask image instead of thresholded activation images
        output_directory: Change output directory from default of input FEAT\
            directory
        no_weight_flag: Don't weight cluster averaging with Z stats
        prewhiten_flag: Prewhiten data and model timeseries before plotting
        no_raw_flag: Don't keep raw data text files
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tsplot_params(
        input_directory=input_directory,
        main_filtered_data=main_filtered_data,
        coordinates=coordinates,
        coordinates_output=coordinates_output,
        mask=mask,
        output_directory=output_directory,
        no_weight_flag=no_weight_flag,
        prewhiten_flag=prewhiten_flag,
        no_raw_flag=no_raw_flag,
    )
    return tsplot_execute(params, runner)

__all__ = [
    "Tsplot",
    "TsplotOutputs",
    "TSPLOT_METADATA",
    "tsplot_cargs",
    "tsplot_outputs",
    "tsplot_params",
    "tsplot_execute",
    "tsplot_validate",
    "tsplot",
]