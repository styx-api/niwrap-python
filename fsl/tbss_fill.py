# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TBSS_FILL_METADATA = Metadata(
    id="tbss_fill",
    name="tbss_fill",
    package="fsl",
)

TbssFill = typing.TypedDict(
    "TbssFill",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/tbss_fill"]],
        "stats_image": InputPathType,
        "threshold": float,
        "mean_fa": InputPathType,
        "output": str,
        "include_negative_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class TbssFillOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    filled_skeleton: OutputPathType
    """Filled skeletonized FA image"""

def tbss_fill_params(
    stats_image: InputPathType,
    threshold: float,
    mean_fa: InputPathType,
    output: str,
    include_negative_flag: bool = False,
) -> TbssFill:
    """
    Build parameters.

    Args:
        stats_image: Stats image
        threshold: Threshold value
        mean_fa: Mean FA image
        output: Output image
        include_negative_flag: Include negative stat values (below -threshold)

    Returns:
        Parameter dictionary.
    """
    params: TbssFill = {
        "@type": "fsl/tbss_fill",
        "stats_image": stats_image,
        "threshold": threshold,
        "mean_fa": mean_fa,
        "output": output,
        "include_negative_flag": include_negative_flag,
    }
    return params

def tbss_fill_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TbssFill."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stats_image", None) is None:
        raise StyxValidationError("`stats_image` must not be None")
    if not isinstance(params["stats_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`stats_image` has the wrong type: Received `{type(params["stats_image"])}` expected `InputPathType`')
    if params.get("threshold", None) is None:
        raise StyxValidationError("`threshold` must not be None")
    if not isinstance(params["threshold"], (float, int)):
        raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("mean_fa", None) is None:
        raise StyxValidationError("`mean_fa` must not be None")
    if not isinstance(params["mean_fa"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mean_fa` has the wrong type: Received `{type(params["mean_fa"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("include_negative_flag", None) is not None:
        if not isinstance(params["include_negative_flag"], bool):
            raise StyxValidationError(f'`include_negative_flag` has the wrong type: Received `{type(params["include_negative_flag"])}` expected `bool`')

def tbss_fill_cargs(params: TbssFill, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tbss_fill")
    cargs.append(execution.input_file(params["stats_image"]))
    cargs.append(str(params["threshold"]))
    cargs.append(execution.input_file(params["mean_fa"]))
    cargs.append(params["output"])
    if params.get("include_negative_flag"):
        cargs.append("-n")
    return cargs

def tbss_fill_outputs(params: TbssFill, execution: Execution) -> TbssFillOutputs:
    """Build the TbssFillOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    filled_skeleton_v: OutputPathType = execution.output_file(params["output"])
    return TbssFillOutputs(
        root=root_v,
        filled_skeleton=filled_skeleton_v,
    )

def tbss_fill_execute(params: TbssFill, runner: Runner | None = None) -> TbssFillOutputs:
    """
    tbss_fill

    Tool for filling skeletonized FA images in TBSS

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tbss_fill_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TBSS_FILL_METADATA)
    execution.params(params)
    args = tbss_fill_cargs(params, execution)
    out = tbss_fill_outputs(params, execution)
    execution.run(args)
    return out

def tbss_fill(
    stats_image: InputPathType,
    threshold: float,
    mean_fa: InputPathType,
    output: str,
    include_negative_flag: bool = False,
    runner: Runner | None = None,
) -> TbssFillOutputs:
    """
    tbss_fill

    Tool for filling skeletonized FA images in TBSS

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        stats_image: Stats image
        threshold: Threshold value
        mean_fa: Mean FA image
        output: Output image
        include_negative_flag: Include negative stat values (below -threshold)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tbss_fill_params(
        stats_image=stats_image,
        threshold=threshold,
        mean_fa=mean_fa,
        output=output,
        include_negative_flag=include_negative_flag,
    )
    return tbss_fill_execute(params, runner)

__all__ = [
    "TbssFill",
    "TbssFillOutputs",
    "TBSS_FILL_METADATA",
    "tbss_fill_cargs",
    "tbss_fill_outputs",
    "tbss_fill_params",
    "tbss_fill_execute",
    "tbss_fill_validate",
    "tbss_fill",
]