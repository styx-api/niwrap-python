# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AVSCALE_METADATA = Metadata(
    id="avscale",
    name="avscale",
    package="fsl",
)

Avscale = typing.TypedDict(
    "Avscale",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/avscale"]],
        "allparams_flag": typing.NotRequired[bool],
        "inverteddies_flag": typing.NotRequired[bool],
        "matrix_file": InputPathType,
        "non_reference_volume": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class AvscaleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: list[str]
    """output affine transfomration file"""

def avscale_params(
    matrix_file: InputPathType,
    allparams_flag: bool = False,
    inverteddies_flag: bool = False,
    non_reference_volume: InputPathType | None = None,
) -> Avscale:
    """
    Build parameters.

    Args:
        matrix_file: The path to the matrix file.
        allparams_flag: Flag for all parameters.
        inverteddies_flag: Flag for inverted eddies.
        non_reference_volume: The path to the non-reference volume.

    Returns:
        Parameter dictionary.
    """
    params: Avscale = {
        "@type": "fsl/avscale",
        "matrix_file": matrix_file,
        "allparams_flag": allparams_flag,
        "inverteddies_flag": inverteddies_flag,
    }
    if non_reference_volume is not None:
        params["non_reference_volume"] = non_reference_volume
    return params

def avscale_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Avscale."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("allparams_flag", None) is not None:
        if not isinstance(params["allparams_flag"], bool):
            raise StyxValidationError(f'`allparams_flag` has the wrong type: Received `{type(params["allparams_flag"])}` expected `bool`')
    if params.get("inverteddies_flag", None) is not None:
        if not isinstance(params["inverteddies_flag"], bool):
            raise StyxValidationError(f'`inverteddies_flag` has the wrong type: Received `{type(params["inverteddies_flag"])}` expected `bool`')
    if params.get("matrix_file", None) is None:
        raise StyxValidationError("`matrix_file` must not be None")
    if not isinstance(params["matrix_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`matrix_file` has the wrong type: Received `{type(params["matrix_file"])}` expected `InputPathType`')
    if params.get("non_reference_volume", None) is not None:
        if not isinstance(params["non_reference_volume"], (pathlib.Path, str)):
            raise StyxValidationError(f'`non_reference_volume` has the wrong type: Received `{type(params["non_reference_volume"])}` expected `InputPathType`')

def avscale_cargs(params: Avscale, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("avscale")
    if params.get("allparams_flag"):
        cargs.append("--allparams")
    if params.get("inverteddies_flag"):
        cargs.append("--inverteddies")
    cargs.append(execution.input_file(params["matrix_file"]))
    v_0 = params.get("non_reference_volume")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def avscale_outputs(params: Avscale, execution: Execution) -> AvscaleOutputs:
    """Build the AvscaleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AvscaleOutputs(
        root=root_v,
        output=[],
    )

def avscale_execute(params: Avscale, runner: Runner | None = None) -> AvscaleOutputs:
    """
    avscale

    A command line tool for computing affine transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    avscale_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AVSCALE_METADATA)
    execution.params(params)
    args = avscale_cargs(params, execution)
    out = avscale_outputs(params, execution)
    execution.run(args, handle_stdout=lambda s: out.output.append(s))
    return out

def avscale(
    matrix_file: InputPathType,
    allparams_flag: bool = False,
    inverteddies_flag: bool = False,
    non_reference_volume: InputPathType | None = None,
    runner: Runner | None = None,
) -> AvscaleOutputs:
    """
    avscale

    A command line tool for computing affine transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        matrix_file: The path to the matrix file.
        allparams_flag: Flag for all parameters.
        inverteddies_flag: Flag for inverted eddies.
        non_reference_volume: The path to the non-reference volume.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = avscale_params(
        matrix_file=matrix_file,
        allparams_flag=allparams_flag,
        inverteddies_flag=inverteddies_flag,
        non_reference_volume=non_reference_volume,
    )
    return avscale_execute(params, runner)

__all__ = [
    "Avscale",
    "AvscaleOutputs",
    "AVSCALE_METADATA",
    "avscale_cargs",
    "avscale_outputs",
    "avscale_params",
    "avscale_execute",
    "avscale_validate",
    "avscale",
]