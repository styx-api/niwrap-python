# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FSLASCII2IMG_METADATA = Metadata(
    id="fslascii2img",
    name="fslascii2img",
    package="fsl",
)

Fslascii2img = typing.TypedDict(
    "Fslascii2img",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/fslascii2img"]],
        "infile": InputPathType,
        "xsize": int,
        "ysize": int,
        "zsize": int,
        "tsize": int,
        "xdim": float,
        "ydim": float,
        "zdim": float,
        "tr": float,
        "outfile": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class Fslascii2imgOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Converted NIfTI file from ASCII input"""

def fslascii2img_params(
    infile: InputPathType,
    xsize: int,
    ysize: int,
    zsize: int,
    tsize: int,
    xdim: float,
    ydim: float,
    zdim: float,
    tr: float,
    outfile: str = "output",
) -> Fslascii2img:
    """
    Build parameters.

    Args:
        infile: Input ASCII file
        xsize: Size in the x dimension (in voxels)
        ysize: Size in the y dimension (in voxels)
        zsize: Size in the z dimension (in voxels)
        tsize: Size in the t dimension (in voxels)
        xdim: Dimension size in the x dimension (in mm)
        ydim: Dimension size in the y dimension (in mm)
        zdim: Dimension size in the z dimension (in mm)
        tr: Repetition time (TR) in seconds
        outfile: Output NIfTI file

    Returns:
        Parameter dictionary.
    """
    params: Fslascii2img = {
        "@type": "fsl/fslascii2img",
        "infile": infile,
        "xsize": xsize,
        "ysize": ysize,
        "zsize": zsize,
        "tsize": tsize,
        "xdim": xdim,
        "ydim": ydim,
        "zdim": zdim,
        "tr": tr,
        "outfile": outfile,
    }
    return params

def fslascii2img_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fslascii2img."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("xsize", None) is None:
        raise StyxValidationError("`xsize` must not be None")
    if not isinstance(params["xsize"], int):
        raise StyxValidationError(f'`xsize` has the wrong type: Received `{type(params["xsize"])}` expected `int`')
    if params.get("ysize", None) is None:
        raise StyxValidationError("`ysize` must not be None")
    if not isinstance(params["ysize"], int):
        raise StyxValidationError(f'`ysize` has the wrong type: Received `{type(params["ysize"])}` expected `int`')
    if params.get("zsize", None) is None:
        raise StyxValidationError("`zsize` must not be None")
    if not isinstance(params["zsize"], int):
        raise StyxValidationError(f'`zsize` has the wrong type: Received `{type(params["zsize"])}` expected `int`')
    if params.get("tsize", None) is None:
        raise StyxValidationError("`tsize` must not be None")
    if not isinstance(params["tsize"], int):
        raise StyxValidationError(f'`tsize` has the wrong type: Received `{type(params["tsize"])}` expected `int`')
    if params.get("xdim", None) is None:
        raise StyxValidationError("`xdim` must not be None")
    if not isinstance(params["xdim"], (float, int)):
        raise StyxValidationError(f'`xdim` has the wrong type: Received `{type(params["xdim"])}` expected `float`')
    if params.get("ydim", None) is None:
        raise StyxValidationError("`ydim` must not be None")
    if not isinstance(params["ydim"], (float, int)):
        raise StyxValidationError(f'`ydim` has the wrong type: Received `{type(params["ydim"])}` expected `float`')
    if params.get("zdim", None) is None:
        raise StyxValidationError("`zdim` must not be None")
    if not isinstance(params["zdim"], (float, int)):
        raise StyxValidationError(f'`zdim` has the wrong type: Received `{type(params["zdim"])}` expected `float`')
    if params.get("tr", None) is None:
        raise StyxValidationError("`tr` must not be None")
    if not isinstance(params["tr"], (float, int)):
        raise StyxValidationError(f'`tr` has the wrong type: Received `{type(params["tr"])}` expected `float`')
    if params.get("outfile", None) is not None:
        if not isinstance(params["outfile"], str):
            raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')

def fslascii2img_cargs(params: Fslascii2img, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fslascii2img")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(str(params["xsize"]))
    cargs.append(str(params["ysize"]))
    cargs.append(str(params["zsize"]))
    cargs.append(str(params["tsize"]))
    cargs.append(str(params["xdim"]))
    cargs.append(str(params["ydim"]))
    cargs.append(str(params["zdim"]))
    cargs.append(str(params["tr"]))
    cargs.append(params.get("outfile", "output"))
    return cargs

def fslascii2img_outputs(params: Fslascii2img, execution: Execution) -> Fslascii2imgOutputs:
    """Build the Fslascii2imgOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params.get("outfile", "output"))
    return Fslascii2imgOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def fslascii2img_execute(params: Fslascii2img, runner: Runner | None = None) -> Fslascii2imgOutputs:
    """
    fslascii2img

    Convert data from ASCII format to NIfTI format

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fslascii2img_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FSLASCII2IMG_METADATA)
    execution.params(params)
    args = fslascii2img_cargs(params, execution)
    out = fslascii2img_outputs(params, execution)
    execution.run(args)
    return out

def fslascii2img(
    infile: InputPathType,
    xsize: int,
    ysize: int,
    zsize: int,
    tsize: int,
    xdim: float,
    ydim: float,
    zdim: float,
    tr: float,
    outfile: str = "output",
    runner: Runner | None = None,
) -> Fslascii2imgOutputs:
    """
    fslascii2img

    Convert data from ASCII format to NIfTI format

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input ASCII file
        xsize: Size in the x dimension (in voxels)
        ysize: Size in the y dimension (in voxels)
        zsize: Size in the z dimension (in voxels)
        tsize: Size in the t dimension (in voxels)
        xdim: Dimension size in the x dimension (in mm)
        ydim: Dimension size in the y dimension (in mm)
        zdim: Dimension size in the z dimension (in mm)
        tr: Repetition time (TR) in seconds
        outfile: Output NIfTI file
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fslascii2img_params(
        infile=infile,
        xsize=xsize,
        ysize=ysize,
        zsize=zsize,
        tsize=tsize,
        xdim=xdim,
        ydim=ydim,
        zdim=zdim,
        tr=tr,
        outfile=outfile,
    )
    return fslascii2img_execute(params, runner)

__all__ = [
    "Fslascii2img",
    "Fslascii2imgOutputs",
    "FSLASCII2IMG_METADATA",
    "fslascii2img_cargs",
    "fslascii2img_outputs",
    "fslascii2img_params",
    "fslascii2img_execute",
    "fslascii2img_validate",
    "fslascii2img",
]