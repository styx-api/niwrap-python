# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

EDDY_COMBINE_METADATA = Metadata(
    id="eddy_combine",
    name="eddy_combine",
    package="fsl",
)

EddyCombine = typing.TypedDict(
    "EddyCombine",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/eddy_combine"]],
        "pos_data": InputPathType,
        "pos_bvals": InputPathType,
        "pos_bvecs": InputPathType,
        "pos_series_vol": float,
        "neg_data": InputPathType,
        "neg_bvals": InputPathType,
        "neg_bvecs": InputPathType,
        "neg_series_vol": float,
        "output_path": str,
        "only_matched_flag": int,
    },
)

@dataclasses.dataclass
class EddyCombineOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    combined_data: OutputPathType
    """Combined positive and negative phase-encoded data"""
    combined_bvals: OutputPathType
    """Combined b-values file"""
    combined_bvecs: OutputPathType
    """Combined b-vectors file"""

def eddy_combine_params(
    pos_data: InputPathType,
    pos_bvals: InputPathType,
    pos_bvecs: InputPathType,
    pos_series_vol: float,
    neg_data: InputPathType,
    neg_bvals: InputPathType,
    neg_bvecs: InputPathType,
    neg_series_vol: float,
    output_path: str,
    only_matched_flag: int,
) -> EddyCombine:
    """
    Build parameters.

    Args:
        pos_data: Path to the positive phase-encoded data file (e.g.\
            PosData.nii.gz)
        pos_bvals: Path to the positive phase-encoded b-values file (e.g. Posbvals)
        pos_bvecs: Path to the positive phase-encoded b-vectors file (e.g.\
            Posbvecs)
        pos_series_vol: Positive series volume count
        neg_data: Path to the negative phase-encoded data file (e.g.\
            NegData.nii.gz)
        neg_bvals: Path to the negative phase-encoded b-values file (e.g. Negbvals)
        neg_bvecs: Path to the negative phase-encoded b-vectors file (e.g.\
            Negbvecs)
        neg_series_vol: Negative series volume count
        output_path: Output directory path
        only_matched_flag: Flag to include only matched volumes (set to 1 to\
            include only matched volumes, otherwise 0)

    Returns:
        Parameter dictionary.
    """
    params: EddyCombine = {
        "@type": "fsl/eddy_combine",
        "pos_data": pos_data,
        "pos_bvals": pos_bvals,
        "pos_bvecs": pos_bvecs,
        "pos_series_vol": pos_series_vol,
        "neg_data": neg_data,
        "neg_bvals": neg_bvals,
        "neg_bvecs": neg_bvecs,
        "neg_series_vol": neg_series_vol,
        "output_path": output_path,
        "only_matched_flag": only_matched_flag,
    }
    return params

def eddy_combine_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid EddyCombine."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("pos_data", None) is None:
        raise StyxValidationError("`pos_data` must not be None")
    if not isinstance(params["pos_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pos_data` has the wrong type: Received `{type(params["pos_data"])}` expected `InputPathType`')
    if params.get("pos_bvals", None) is None:
        raise StyxValidationError("`pos_bvals` must not be None")
    if not isinstance(params["pos_bvals"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pos_bvals` has the wrong type: Received `{type(params["pos_bvals"])}` expected `InputPathType`')
    if params.get("pos_bvecs", None) is None:
        raise StyxValidationError("`pos_bvecs` must not be None")
    if not isinstance(params["pos_bvecs"], (pathlib.Path, str)):
        raise StyxValidationError(f'`pos_bvecs` has the wrong type: Received `{type(params["pos_bvecs"])}` expected `InputPathType`')
    if params.get("pos_series_vol", None) is None:
        raise StyxValidationError("`pos_series_vol` must not be None")
    if not isinstance(params["pos_series_vol"], (float, int)):
        raise StyxValidationError(f'`pos_series_vol` has the wrong type: Received `{type(params["pos_series_vol"])}` expected `float`')
    if params.get("neg_data", None) is None:
        raise StyxValidationError("`neg_data` must not be None")
    if not isinstance(params["neg_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`neg_data` has the wrong type: Received `{type(params["neg_data"])}` expected `InputPathType`')
    if params.get("neg_bvals", None) is None:
        raise StyxValidationError("`neg_bvals` must not be None")
    if not isinstance(params["neg_bvals"], (pathlib.Path, str)):
        raise StyxValidationError(f'`neg_bvals` has the wrong type: Received `{type(params["neg_bvals"])}` expected `InputPathType`')
    if params.get("neg_bvecs", None) is None:
        raise StyxValidationError("`neg_bvecs` must not be None")
    if not isinstance(params["neg_bvecs"], (pathlib.Path, str)):
        raise StyxValidationError(f'`neg_bvecs` has the wrong type: Received `{type(params["neg_bvecs"])}` expected `InputPathType`')
    if params.get("neg_series_vol", None) is None:
        raise StyxValidationError("`neg_series_vol` must not be None")
    if not isinstance(params["neg_series_vol"], (float, int)):
        raise StyxValidationError(f'`neg_series_vol` has the wrong type: Received `{type(params["neg_series_vol"])}` expected `float`')
    if params.get("output_path", None) is None:
        raise StyxValidationError("`output_path` must not be None")
    if not isinstance(params["output_path"], str):
        raise StyxValidationError(f'`output_path` has the wrong type: Received `{type(params["output_path"])}` expected `str`')
    if params.get("only_matched_flag", None) is None:
        raise StyxValidationError("`only_matched_flag` must not be None")
    if not isinstance(params["only_matched_flag"], int):
        raise StyxValidationError(f'`only_matched_flag` has the wrong type: Received `{type(params["only_matched_flag"])}` expected `int`')
    if not (0 <= params["only_matched_flag"] <= 1):
        raise StyxValidationError("Parameter `only_matched_flag` must be between 0 and 1 (inclusive)")

def eddy_combine_cargs(params: EddyCombine, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("eddy_combine")
    cargs.append(execution.input_file(params["pos_data"]))
    cargs.append(execution.input_file(params["pos_bvals"]))
    cargs.append(execution.input_file(params["pos_bvecs"]))
    cargs.append(str(params["pos_series_vol"]))
    cargs.append(execution.input_file(params["neg_data"]))
    cargs.append(execution.input_file(params["neg_bvals"]))
    cargs.append(execution.input_file(params["neg_bvecs"]))
    cargs.append(str(params["neg_series_vol"]))
    cargs.append(params["output_path"])
    cargs.append(str(params["only_matched_flag"]))
    return cargs

def eddy_combine_outputs(params: EddyCombine, execution: Execution) -> EddyCombineOutputs:
    """Build the EddyCombineOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    combined_data_v: OutputPathType = execution.output_file(f'{params["output_path"]}/combined_data.nii.gz')
    combined_bvals_v: OutputPathType = execution.output_file(f'{params["output_path"]}/combined_bvals')
    combined_bvecs_v: OutputPathType = execution.output_file(f'{params["output_path"]}/combined_bvecs')
    return EddyCombineOutputs(
        root=root_v,
        combined_data=combined_data_v,
        combined_bvals=combined_bvals_v,
        combined_bvecs=combined_bvecs_v,
    )

def eddy_combine_execute(params: EddyCombine, runner: Runner | None = None) -> EddyCombineOutputs:
    """
    eddy_combine

    Combines diffusion data sets with opposite phase encoding directions for use with FSL's EDDY.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    eddy_combine_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(EDDY_COMBINE_METADATA)
    execution.params(params)
    args = eddy_combine_cargs(params, execution)
    out = eddy_combine_outputs(params, execution)
    execution.run(args)
    return out

def eddy_combine(
    pos_data: InputPathType,
    pos_bvals: InputPathType,
    pos_bvecs: InputPathType,
    pos_series_vol: float,
    neg_data: InputPathType,
    neg_bvals: InputPathType,
    neg_bvecs: InputPathType,
    neg_series_vol: float,
    output_path: str,
    only_matched_flag: int,
    runner: Runner | None = None,
) -> EddyCombineOutputs:
    """
    eddy_combine

    Combines diffusion data sets with opposite phase encoding directions for use with FSL's EDDY.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        pos_data: Path to the positive phase-encoded data file (e.g.\
            PosData.nii.gz)
        pos_bvals: Path to the positive phase-encoded b-values file (e.g. Posbvals)
        pos_bvecs: Path to the positive phase-encoded b-vectors file (e.g.\
            Posbvecs)
        pos_series_vol: Positive series volume count
        neg_data: Path to the negative phase-encoded data file (e.g.\
            NegData.nii.gz)
        neg_bvals: Path to the negative phase-encoded b-values file (e.g. Negbvals)
        neg_bvecs: Path to the negative phase-encoded b-vectors file (e.g.\
            Negbvecs)
        neg_series_vol: Negative series volume count
        output_path: Output directory path
        only_matched_flag: Flag to include only matched volumes (set to 1 to\
            include only matched volumes, otherwise 0)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = eddy_combine_params(
        pos_data=pos_data,
        pos_bvals=pos_bvals,
        pos_bvecs=pos_bvecs,
        pos_series_vol=pos_series_vol,
        neg_data=neg_data,
        neg_bvals=neg_bvals,
        neg_bvecs=neg_bvecs,
        neg_series_vol=neg_series_vol,
        output_path=output_path,
        only_matched_flag=only_matched_flag,
    )
    return eddy_combine_execute(params, runner)

__all__ = [
    "EddyCombine",
    "EddyCombineOutputs",
    "EDDY_COMBINE_METADATA",
    "eddy_combine_cargs",
    "eddy_combine_outputs",
    "eddy_combine_params",
    "eddy_combine_execute",
    "eddy_combine_validate",
    "eddy_combine",
]