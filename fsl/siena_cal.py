# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIENA_CAL_METADATA = Metadata(
    id="siena_cal",
    name="siena_cal",
    package="fsl",
)

SienaCal = typing.TypedDict(
    "SienaCal",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/siena_cal"]],
        "input1_file": InputPathType,
        "input2_file": InputPathType,
        "scale": float,
        "siena_diff_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SienaCalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_dir: OutputPathType
    """Output directory containing SIENA results"""

def siena_cal_params(
    input1_file: InputPathType,
    input2_file: InputPathType,
    scale: float,
    siena_diff_options: str | None = None,
) -> SienaCal:
    """
    Build parameters.

    Args:
        input1_file: First input image file root (e.g., baseline image root).
        input2_file: Second input image file root (e.g., follow-up image root).
        scale: Voxel size scaling factor.
        siena_diff_options: Optional SIENA difference options.

    Returns:
        Parameter dictionary.
    """
    params: SienaCal = {
        "@type": "fsl/siena_cal",
        "input1_file": input1_file,
        "input2_file": input2_file,
        "scale": scale,
    }
    if siena_diff_options is not None:
        params["siena_diff_options"] = siena_diff_options
    return params

def siena_cal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SienaCal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1_file", None) is None:
        raise StyxValidationError("`input1_file` must not be None")
    if not isinstance(params["input1_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input1_file` has the wrong type: Received `{type(params["input1_file"])}` expected `InputPathType`')
    if params.get("input2_file", None) is None:
        raise StyxValidationError("`input2_file` must not be None")
    if not isinstance(params["input2_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input2_file` has the wrong type: Received `{type(params["input2_file"])}` expected `InputPathType`')
    if params.get("scale", None) is None:
        raise StyxValidationError("`scale` must not be None")
    if not isinstance(params["scale"], (float, int)):
        raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `float`')
    if params.get("siena_diff_options", None) is not None:
        if not isinstance(params["siena_diff_options"], str):
            raise StyxValidationError(f'`siena_diff_options` has the wrong type: Received `{type(params["siena_diff_options"])}` expected `str`')

def siena_cal_cargs(params: SienaCal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("siena_cal")
    cargs.append(execution.input_file(params["input1_file"]))
    cargs.append(execution.input_file(params["input2_file"]))
    cargs.append(str(params["scale"]))
    v_0 = params.get("siena_diff_options")
    if v_0 is not None:
        cargs.append(v_0)
    return cargs

def siena_cal_outputs(params: SienaCal, execution: Execution) -> SienaCalOutputs:
    """Build the SienaCalOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_dir_v: OutputPathType = execution.output_file(f'{params["input1_file"]}_to_{params["input2_file"]}_siena')
    return SienaCalOutputs(
        root=root_v,
        output_dir=output_dir_v,
    )

def siena_cal_execute(params: SienaCal, runner: Runner | None = None) -> SienaCalOutputs:
    """
    siena_cal

    SIENA is part of FSL (FMRIB Software Library), which performs a two-timepoint brain volume change analysis.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    siena_cal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIENA_CAL_METADATA)
    execution.params(params)
    args = siena_cal_cargs(params, execution)
    out = siena_cal_outputs(params, execution)
    execution.run(args)
    return out

def siena_cal(
    input1_file: InputPathType,
    input2_file: InputPathType,
    scale: float,
    siena_diff_options: str | None = None,
    runner: Runner | None = None,
) -> SienaCalOutputs:
    """
    siena_cal

    SIENA is part of FSL (FMRIB Software Library), which performs a two-timepoint brain volume change analysis.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input1_file: First input image file root (e.g., baseline image root).
        input2_file: Second input image file root (e.g., follow-up image root).
        scale: Voxel size scaling factor.
        siena_diff_options: Optional SIENA difference options.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = siena_cal_params(
        input1_file=input1_file,
        input2_file=input2_file,
        scale=scale,
        siena_diff_options=siena_diff_options,
    )
    return siena_cal_execute(params, runner)

__all__ = [
    "SienaCal",
    "SienaCalOutputs",
    "SIENA_CAL_METADATA",
    "siena_cal_cargs",
    "siena_cal_outputs",
    "siena_cal_params",
    "siena_cal_execute",
    "siena_cal_validate",
    "siena_cal",
]