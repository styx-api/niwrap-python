# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MEDIANFILTER_METADATA = Metadata(
    id="medianfilter",
    name="medianfilter",
    package="fsl",
)

Medianfilter = typing.TypedDict(
    "Medianfilter",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/medianfilter"]],
        "infile": InputPathType,
        "outfile": InputPathType,
    },
)

@dataclasses.dataclass
class MedianfilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    filtered_file: OutputPathType
    """Output file containing the median filtered image"""

def medianfilter_params(
    infile: InputPathType,
    outfile: InputPathType,
) -> Medianfilter:
    """
    Build parameters.

    Args:
        infile: Input image file to be filtered (e.g., img.nii.gz)
        outfile: Output file to store the filtered image (e.g.,\
            img_filtered.nii.gz)

    Returns:
        Parameter dictionary.
    """
    params: Medianfilter = {
        "@type": "fsl/medianfilter",
        "infile": infile,
        "outfile": outfile,
    }
    return params

def medianfilter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Medianfilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `InputPathType`')

def medianfilter_cargs(params: Medianfilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("medianfilter")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(execution.input_file(params["outfile"]))
    return cargs

def medianfilter_outputs(params: Medianfilter, execution: Execution) -> MedianfilterOutputs:
    """Build the MedianfilterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    filtered_file_v: OutputPathType = execution.output_file(params["outfile"])
    return MedianfilterOutputs(
        root=root_v,
        filtered_file=filtered_file_v,
    )

def medianfilter_execute(params: Medianfilter, runner: Runner | None = None) -> MedianfilterOutputs:
    """
    medianfilter

    A tool to perform 26 neighbourhood median filtering on an input image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    medianfilter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MEDIANFILTER_METADATA)
    execution.params(params)
    args = medianfilter_cargs(params, execution)
    out = medianfilter_outputs(params, execution)
    execution.run(args)
    return out

def medianfilter(
    infile: InputPathType,
    outfile: InputPathType,
    runner: Runner | None = None,
) -> MedianfilterOutputs:
    """
    medianfilter

    A tool to perform 26 neighbourhood median filtering on an input image.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input image file to be filtered (e.g., img.nii.gz)
        outfile: Output file to store the filtered image (e.g.,\
            img_filtered.nii.gz)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = medianfilter_params(
        infile=infile,
        outfile=outfile,
    )
    return medianfilter_execute(params, runner)

__all__ = [
    "Medianfilter",
    "MedianfilterOutputs",
    "MEDIANFILTER_METADATA",
    "medianfilter_cargs",
    "medianfilter_outputs",
    "medianfilter_params",
    "medianfilter_execute",
    "medianfilter_validate",
    "medianfilter",
]