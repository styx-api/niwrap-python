# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

BASIL_VAR_METADATA = Metadata(
    id="basil_var",
    name="basil_var",
    package="fsl",
)

BasilVar = typing.TypedDict(
    "BasilVar",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/basil_var"]],
        "results_dir": str,
        "mask_image": InputPathType,
    },
)

@dataclasses.dataclass
class BasilVarOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def basil_var_params(
    results_dir: str,
    mask_image: InputPathType,
) -> BasilVar:
    """
    Build parameters.

    Args:
        results_dir:
        mask_image:

    Returns:
        Parameter dictionary.
    """
    params: BasilVar = {
        "@type": "fsl/basil_var",
        "results_dir": results_dir,
        "mask_image": mask_image,
    }
    return params

def basil_var_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid BasilVar."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("results_dir", None) is None:
        raise StyxValidationError("`results_dir` must not be None")
    if not isinstance(params["results_dir"], str):
        raise StyxValidationError(f'`results_dir` has the wrong type: Received `{type(params["results_dir"])}` expected `str`')
    if params.get("mask_image", None) is None:
        raise StyxValidationError("`mask_image` must not be None")
    if not isinstance(params["mask_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')

def basil_var_cargs(params: BasilVar, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("basil_var")
    cargs.append("-d")
    cargs.append(params["results_dir"])
    cargs.append("-m")
    cargs.append(execution.input_file(params["mask_image"]))
    return cargs

def basil_var_outputs(params: BasilVar, execution: Execution) -> BasilVarOutputs:
    """Build the BasilVarOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return BasilVarOutputs(
        root=root_v,
    )

def basil_var_execute(params: BasilVar, runner: Runner | None = None) -> BasilVarOutputs:
    """
    basil_var

    Variance calculator for BASIL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    basil_var_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(BASIL_VAR_METADATA)
    execution.params(params)
    args = basil_var_cargs(params, execution)
    out = basil_var_outputs(params, execution)
    execution.run(args)
    return out

def basil_var(
    results_dir: str,
    mask_image: InputPathType,
    runner: Runner | None = None,
) -> BasilVarOutputs:
    """
    basil_var

    Variance calculator for BASIL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        results_dir:
        mask_image:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = basil_var_params(
        results_dir=results_dir,
        mask_image=mask_image,
    )
    return basil_var_execute(params, runner)

__all__ = [
    "BasilVar",
    "BasilVarOutputs",
    "BASIL_VAR_METADATA",
    "basil_var_cargs",
    "basil_var_outputs",
    "basil_var_params",
    "basil_var_execute",
    "basil_var_validate",
    "basil_var",
]