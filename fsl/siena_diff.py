# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIENA_DIFF_METADATA = Metadata(
    id="siena_diff",
    name="siena_diff",
    package="fsl",
)

SienaDiff = typing.TypedDict(
    "SienaDiff",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/siena_diff"]],
        "input1_basename": str,
        "input2_basename": str,
        "debug_flag": typing.NotRequired[bool],
        "no_seg_flag": typing.NotRequired[bool],
        "self_corr_factor": typing.NotRequired[float],
        "ignore_z_flow_flag": typing.NotRequired[bool],
        "apply_std_mask_flag": typing.NotRequired[bool],
        "segment_options": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class SienaDiffOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def siena_diff_params(
    input1_basename: str,
    input2_basename: str,
    debug_flag: bool = False,
    no_seg_flag: bool = False,
    self_corr_factor: float | None = None,
    ignore_z_flow_flag: bool = False,
    apply_std_mask_flag: bool = False,
    segment_options: str | None = None,
) -> SienaDiff:
    """
    Build parameters.

    Args:
        input1_basename: Input image 1 basename
        input2_basename: Input image 2 basename
        debug_flag: Debug - generate edge images and don't remove temporary images
        no_seg_flag: Don't segment grey+white separately (because there is poor\
            grey-white contrast)
        self_corr_factor: Apply self-calibrating correction factor
        ignore_z_flow_flag: Ignore flow in z (may be beneficial if top of brain is\
            missing)
        apply_std_mask_flag: Apply <input1_basename>_stdmask to brain edge points
        segment_options: Options to be passed to segmentation (type 'fast' to get\
            these)

    Returns:
        Parameter dictionary.
    """
    params: SienaDiff = {
        "@type": "fsl/siena_diff",
        "input1_basename": input1_basename,
        "input2_basename": input2_basename,
        "debug_flag": debug_flag,
        "no_seg_flag": no_seg_flag,
        "ignore_z_flow_flag": ignore_z_flow_flag,
        "apply_std_mask_flag": apply_std_mask_flag,
    }
    if self_corr_factor is not None:
        params["self_corr_factor"] = self_corr_factor
    if segment_options is not None:
        params["segment_options"] = segment_options
    return params

def siena_diff_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SienaDiff."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input1_basename", None) is None:
        raise StyxValidationError("`input1_basename` must not be None")
    if not isinstance(params["input1_basename"], str):
        raise StyxValidationError(f'`input1_basename` has the wrong type: Received `{type(params["input1_basename"])}` expected `str`')
    if params.get("input2_basename", None) is None:
        raise StyxValidationError("`input2_basename` must not be None")
    if not isinstance(params["input2_basename"], str):
        raise StyxValidationError(f'`input2_basename` has the wrong type: Received `{type(params["input2_basename"])}` expected `str`')
    if params.get("debug_flag", None) is not None:
        if not isinstance(params["debug_flag"], bool):
            raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params["debug_flag"])}` expected `bool`')
    if params.get("no_seg_flag", None) is not None:
        if not isinstance(params["no_seg_flag"], bool):
            raise StyxValidationError(f'`no_seg_flag` has the wrong type: Received `{type(params["no_seg_flag"])}` expected `bool`')
    if params.get("self_corr_factor", None) is not None:
        if not isinstance(params["self_corr_factor"], (float, int)):
            raise StyxValidationError(f'`self_corr_factor` has the wrong type: Received `{type(params["self_corr_factor"])}` expected `float`')
    if params.get("ignore_z_flow_flag", None) is not None:
        if not isinstance(params["ignore_z_flow_flag"], bool):
            raise StyxValidationError(f'`ignore_z_flow_flag` has the wrong type: Received `{type(params["ignore_z_flow_flag"])}` expected `bool`')
    if params.get("apply_std_mask_flag", None) is not None:
        if not isinstance(params["apply_std_mask_flag"], bool):
            raise StyxValidationError(f'`apply_std_mask_flag` has the wrong type: Received `{type(params["apply_std_mask_flag"])}` expected `bool`')
    if params.get("segment_options", None) is not None:
        if not isinstance(params["segment_options"], str):
            raise StyxValidationError(f'`segment_options` has the wrong type: Received `{type(params["segment_options"])}` expected `str`')

def siena_diff_cargs(params: SienaDiff, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("siena_diff")
    cargs.append(params["input1_basename"])
    cargs.append(params["input2_basename"])
    if params.get("debug_flag"):
        cargs.append("-d")
    if params.get("no_seg_flag"):
        cargs.append("-2")
    v_0 = params.get("self_corr_factor")
    if v_0 is not None:
        cargs.append("-c")
        cargs.append(str(v_0))
    if params.get("ignore_z_flow_flag"):
        cargs.append("-i")
    if params.get("apply_std_mask_flag"):
        cargs.append("-m")
    v_1 = params.get("segment_options")
    if v_1 is not None:
        cargs.append("-s")
        cargs.append(v_1)
    return cargs

def siena_diff_outputs(params: SienaDiff, execution: Execution) -> SienaDiffOutputs:
    """Build the SienaDiffOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return SienaDiffOutputs(
        root=root_v,
    )

def siena_diff_execute(params: SienaDiff, runner: Runner | None = None) -> SienaDiffOutputs:
    """
    siena_diff

    SIENA_diff: Analysis of longitudinal brain image differences.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    siena_diff_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIENA_DIFF_METADATA)
    execution.params(params)
    args = siena_diff_cargs(params, execution)
    out = siena_diff_outputs(params, execution)
    execution.run(args)
    return out

def siena_diff(
    input1_basename: str,
    input2_basename: str,
    debug_flag: bool = False,
    no_seg_flag: bool = False,
    self_corr_factor: float | None = None,
    ignore_z_flow_flag: bool = False,
    apply_std_mask_flag: bool = False,
    segment_options: str | None = None,
    runner: Runner | None = None,
) -> SienaDiffOutputs:
    """
    siena_diff

    SIENA_diff: Analysis of longitudinal brain image differences.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input1_basename: Input image 1 basename
        input2_basename: Input image 2 basename
        debug_flag: Debug - generate edge images and don't remove temporary images
        no_seg_flag: Don't segment grey+white separately (because there is poor\
            grey-white contrast)
        self_corr_factor: Apply self-calibrating correction factor
        ignore_z_flow_flag: Ignore flow in z (may be beneficial if top of brain is\
            missing)
        apply_std_mask_flag: Apply <input1_basename>_stdmask to brain edge points
        segment_options: Options to be passed to segmentation (type 'fast' to get\
            these)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = siena_diff_params(
        input1_basename=input1_basename,
        input2_basename=input2_basename,
        debug_flag=debug_flag,
        no_seg_flag=no_seg_flag,
        self_corr_factor=self_corr_factor,
        ignore_z_flow_flag=ignore_z_flow_flag,
        apply_std_mask_flag=apply_std_mask_flag,
        segment_options=segment_options,
    )
    return siena_diff_execute(params, runner)

__all__ = [
    "SienaDiff",
    "SienaDiffOutputs",
    "SIENA_DIFF_METADATA",
    "siena_diff_cargs",
    "siena_diff_outputs",
    "siena_diff_params",
    "siena_diff_execute",
    "siena_diff_validate",
    "siena_diff",
]