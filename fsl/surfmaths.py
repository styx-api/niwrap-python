# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFMATHS_METADATA = Metadata(
    id="surfmaths",
    name="surfmaths",
    package="fsl",
)

Surfmaths = typing.TypedDict(
    "Surfmaths",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/surfmaths"]],
        "first_input": InputPathType,
        "operations_inputs": typing.NotRequired[list[str]],
        "output": str,
    },
)

@dataclasses.dataclass
class SurfmathsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_file: OutputPathType
    """Output surface file generated by the tool."""

def surfmaths_params(
    first_input: InputPathType,
    output: str,
    operations_inputs: list[str] | None = None,
) -> Surfmaths:
    """
    Build parameters.

    Args:
        first_input: First input surface file.
        output: Output surface file.
        operations_inputs: Mathematical operations and additional inputs.

    Returns:
        Parameter dictionary.
    """
    params: Surfmaths = {
        "@type": "fsl/surfmaths",
        "first_input": first_input,
        "output": output,
    }
    if operations_inputs is not None:
        params["operations_inputs"] = operations_inputs
    return params

def surfmaths_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Surfmaths."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("first_input", None) is None:
        raise StyxValidationError("`first_input` must not be None")
    if not isinstance(params["first_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`first_input` has the wrong type: Received `{type(params["first_input"])}` expected `InputPathType`')
    if params.get("operations_inputs", None) is not None:
        if not isinstance(params["operations_inputs"], list):
            raise StyxValidationError(f'`operations_inputs` has the wrong type: Received `{type(params["operations_inputs"])}` expected `list[str]`')
        for e in params["operations_inputs"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`operations_inputs` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def surfmaths_cargs(params: Surfmaths, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("surfmaths")
    cargs.append(execution.input_file(params["first_input"]))
    v_0 = params.get("operations_inputs")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append(item0)
    cargs.append(params["output"])
    return cargs

def surfmaths_outputs(params: Surfmaths, execution: Execution) -> SurfmathsOutputs:
    """Build the SurfmathsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_file_v: OutputPathType = execution.output_file(params["output"])
    return SurfmathsOutputs(
        root=root_v,
        output_file=output_file_v,
    )

def surfmaths_execute(params: Surfmaths, runner: Runner | None = None) -> SurfmathsOutputs:
    """
    surfmaths

    A command-line tool for performing various mathematical operations on surface files.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surfmaths_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFMATHS_METADATA)
    execution.params(params)
    args = surfmaths_cargs(params, execution)
    out = surfmaths_outputs(params, execution)
    execution.run(args)
    return out

def surfmaths(
    first_input: InputPathType,
    output: str,
    operations_inputs: list[str] | None = None,
    runner: Runner | None = None,
) -> SurfmathsOutputs:
    """
    surfmaths

    A command-line tool for performing various mathematical operations on surface files.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        first_input: First input surface file.
        output: Output surface file.
        operations_inputs: Mathematical operations and additional inputs.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surfmaths_params(
        first_input=first_input,
        output=output,
        operations_inputs=operations_inputs,
    )
    return surfmaths_execute(params, runner)

__all__ = [
    "Surfmaths",
    "SurfmathsOutputs",
    "SURFMATHS_METADATA",
    "surfmaths_cargs",
    "surfmaths_outputs",
    "surfmaths_params",
    "surfmaths_execute",
    "surfmaths_validate",
    "surfmaths",
]