# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

INVWARP_METADATA = Metadata(
    id="invwarp",
    name="invwarp",
    package="fsl",
)

Invwarp = typing.TypedDict(
    "Invwarp",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/invwarp"]],
        "warp": InputPathType,
        "out_img": str,
        "ref_img": InputPathType,
        "absolute": typing.NotRequired[bool],
        "relative": typing.NotRequired[bool],
        "noconstraint": typing.NotRequired[bool],
        "jacobian_min": typing.NotRequired[float],
        "jacobian_max": typing.NotRequired[float],
        "debug": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class InvwarpOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    inverse_warp: OutputPathType
    """
    Name of output file, containing warps that are the "reverse" of those in --warp. this will be a field-file (rather than a file of spline coefficients), and it will have any affine component included as part of the displacements.
    """

def invwarp_params(
    warp: InputPathType,
    out_img: str,
    ref_img: InputPathType,
    absolute: bool = False,
    relative: bool = False,
    noconstraint: bool = False,
    jacobian_min: float | None = None,
    jacobian_max: float | None = None,
    debug: bool = False,
) -> Invwarp:
    """
    Build parameters.

    Args:
        warp: Filename for warp/shiftmap transform (volume).
        out_img: Filename for output (inverse warped) image.
        ref_img: Filename for new reference image.
        absolute: Use absolute warp convention (default): x' = w(x).
        relative: Use relative warp convention (default): x' = x + w(x).
        noconstraint: Do not apply jacobian constraint.
        jacobian_min: Minimum acceptable jacobian value for constraint (default\
            0.01).
        jacobian_max: Maximum acceptable jacobian value for constraint (default\
            100.0).
        debug: Turn on debugging output.

    Returns:
        Parameter dictionary.
    """
    params: Invwarp = {
        "@type": "fsl/invwarp",
        "warp": warp,
        "out_img": out_img,
        "ref_img": ref_img,
        "absolute": absolute,
        "relative": relative,
        "noconstraint": noconstraint,
        "debug": debug,
    }
    if jacobian_min is not None:
        params["jacobian_min"] = jacobian_min
    if jacobian_max is not None:
        params["jacobian_max"] = jacobian_max
    return params

def invwarp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Invwarp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("warp", None) is None:
        raise StyxValidationError("`warp` must not be None")
    if not isinstance(params["warp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`warp` has the wrong type: Received `{type(params["warp"])}` expected `InputPathType`')
    if params.get("out_img", None) is None:
        raise StyxValidationError("`out_img` must not be None")
    if not isinstance(params["out_img"], str):
        raise StyxValidationError(f'`out_img` has the wrong type: Received `{type(params["out_img"])}` expected `str`')
    if params.get("ref_img", None) is None:
        raise StyxValidationError("`ref_img` must not be None")
    if not isinstance(params["ref_img"], (pathlib.Path, str)):
        raise StyxValidationError(f'`ref_img` has the wrong type: Received `{type(params["ref_img"])}` expected `InputPathType`')
    if params.get("absolute", None) is not None:
        if not isinstance(params["absolute"], bool):
            raise StyxValidationError(f'`absolute` has the wrong type: Received `{type(params["absolute"])}` expected `bool`')
    if params.get("relative", None) is not None:
        if not isinstance(params["relative"], bool):
            raise StyxValidationError(f'`relative` has the wrong type: Received `{type(params["relative"])}` expected `bool`')
    if params.get("noconstraint", None) is not None:
        if not isinstance(params["noconstraint"], bool):
            raise StyxValidationError(f'`noconstraint` has the wrong type: Received `{type(params["noconstraint"])}` expected `bool`')
    if params.get("jacobian_min", None) is not None:
        if not isinstance(params["jacobian_min"], (float, int)):
            raise StyxValidationError(f'`jacobian_min` has the wrong type: Received `{type(params["jacobian_min"])}` expected `float`')
    if params.get("jacobian_max", None) is not None:
        if not isinstance(params["jacobian_max"], (float, int)):
            raise StyxValidationError(f'`jacobian_max` has the wrong type: Received `{type(params["jacobian_max"])}` expected `float`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')

def invwarp_cargs(params: Invwarp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("invwarp")
    cargs.append("--warp=")
    cargs.append(execution.input_file(params["warp"]))
    cargs.append("--out=")
    cargs.append(params["out_img"])
    cargs.append("--ref=")
    cargs.append(execution.input_file(params["ref_img"]))
    if params.get("absolute"):
        cargs.append("--abs")
    if params.get("relative"):
        cargs.append("--rel")
    if params.get("noconstraint"):
        cargs.append("--noconstraint")
    v_0 = params.get("jacobian_min")
    if v_0 is not None:
        cargs.append("--jmin=")
        cargs.append(str(v_0))
    v_1 = params.get("jacobian_max")
    if v_1 is not None:
        cargs.append("--jmax=")
        cargs.append(str(v_1))
    if params.get("debug"):
        cargs.append("--debug")
    return cargs

def invwarp_outputs(params: Invwarp, execution: Execution) -> InvwarpOutputs:
    """Build the InvwarpOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    inverse_warp_v: OutputPathType = execution.output_file(params["out_img"])
    return InvwarpOutputs(
        root=root_v,
        inverse_warp=inverse_warp_v,
    )

def invwarp_execute(params: Invwarp, runner: Runner | None = None) -> InvwarpOutputs:
    """
    invwarp

    
Use FSL Invwarp to invert a FNIRT warp

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    invwarp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(INVWARP_METADATA)
    execution.params(params)
    args = invwarp_cargs(params, execution)
    out = invwarp_outputs(params, execution)
    execution.run(args)
    return out

def invwarp(
    warp: InputPathType,
    out_img: str,
    ref_img: InputPathType,
    absolute: bool = False,
    relative: bool = False,
    noconstraint: bool = False,
    jacobian_min: float | None = None,
    jacobian_max: float | None = None,
    debug: bool = False,
    runner: Runner | None = None,
) -> InvwarpOutputs:
    """
    invwarp

    
Use FSL Invwarp to invert a FNIRT warp

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        warp: Filename for warp/shiftmap transform (volume).
        out_img: Filename for output (inverse warped) image.
        ref_img: Filename for new reference image.
        absolute: Use absolute warp convention (default): x' = w(x).
        relative: Use relative warp convention (default): x' = x + w(x).
        noconstraint: Do not apply jacobian constraint.
        jacobian_min: Minimum acceptable jacobian value for constraint (default\
            0.01).
        jacobian_max: Maximum acceptable jacobian value for constraint (default\
            100.0).
        debug: Turn on debugging output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = invwarp_params(
        warp=warp,
        out_img=out_img,
        ref_img=ref_img,
        absolute=absolute,
        relative=relative,
        noconstraint=noconstraint,
        jacobian_min=jacobian_min,
        jacobian_max=jacobian_max,
        debug=debug,
    )
    return invwarp_execute(params, runner)

__all__ = [
    "Invwarp",
    "InvwarpOutputs",
    "INVWARP_METADATA",
    "invwarp_cargs",
    "invwarp_outputs",
    "invwarp_params",
    "invwarp_execute",
    "invwarp_validate",
    "invwarp",
]