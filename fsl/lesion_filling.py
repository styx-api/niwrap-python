# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LESION_FILLING_METADATA = Metadata(
    id="lesion_filling",
    name="lesion_filling",
    package="fsl",
)

LesionFilling = typing.TypedDict(
    "LesionFilling",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/lesion_filling"]],
        "infile": InputPathType,
        "outfile": str,
        "lesionmask": InputPathType,
        "wmmask": typing.NotRequired[InputPathType],
        "verbose_flag": typing.NotRequired[bool],
        "components_flag": typing.NotRequired[bool],
        "help_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class LesionFillingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Lesion filled output image"""

def lesion_filling_params(
    infile: InputPathType,
    outfile: str,
    lesionmask: InputPathType,
    wmmask: InputPathType | None = None,
    verbose_flag: bool = False,
    components_flag: bool = False,
    help_flag: bool = False,
) -> LesionFilling:
    """
    Build parameters.

    Args:
        infile: Input image filename (e.g., T1w image)
        outfile: Output filename (lesion filled image)
        lesionmask: Filename of lesion mask image
        wmmask: Filename of white matter mask image
        verbose_flag: Switch on diagnostic messages
        components_flag: Save all lesion components as volumes
        help_flag: Display help message

    Returns:
        Parameter dictionary.
    """
    params: LesionFilling = {
        "@type": "fsl/lesion_filling",
        "infile": infile,
        "outfile": outfile,
        "lesionmask": lesionmask,
        "verbose_flag": verbose_flag,
        "components_flag": components_flag,
        "help_flag": help_flag,
    }
    if wmmask is not None:
        params["wmmask"] = wmmask
    return params

def lesion_filling_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid LesionFilling."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], str):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `str`')
    if params.get("lesionmask", None) is None:
        raise StyxValidationError("`lesionmask` must not be None")
    if not isinstance(params["lesionmask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`lesionmask` has the wrong type: Received `{type(params["lesionmask"])}` expected `InputPathType`')
    if params.get("wmmask", None) is not None:
        if not isinstance(params["wmmask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`wmmask` has the wrong type: Received `{type(params["wmmask"])}` expected `InputPathType`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')
    if params.get("components_flag", None) is not None:
        if not isinstance(params["components_flag"], bool):
            raise StyxValidationError(f'`components_flag` has the wrong type: Received `{type(params["components_flag"])}` expected `bool`')
    if params.get("help_flag", None) is not None:
        if not isinstance(params["help_flag"], bool):
            raise StyxValidationError(f'`help_flag` has the wrong type: Received `{type(params["help_flag"])}` expected `bool`')

def lesion_filling_cargs(params: LesionFilling, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("lesion_filling")
    cargs.append("-i")
    cargs.append(execution.input_file(params["infile"]))
    cargs.append("-o")
    cargs.append(params["outfile"])
    cargs.append("-l")
    cargs.append(execution.input_file(params["lesionmask"]))
    v_0 = params.get("wmmask")
    if v_0 is not None:
        cargs.append("-w")
        cargs.append(execution.input_file(v_0))
    if params.get("verbose_flag"):
        cargs.append("-v")
    if params.get("components_flag"):
        cargs.append("-c")
    if params.get("help_flag"):
        cargs.append("-h")
    return cargs

def lesion_filling_outputs(params: LesionFilling, execution: Execution) -> LesionFillingOutputs:
    """Build the LesionFillingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["outfile"])
    return LesionFillingOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def lesion_filling_execute(params: LesionFilling, runner: Runner | None = None) -> LesionFillingOutputs:
    """
    lesion_filling

    Lesion filling tool as part of FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    lesion_filling_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LESION_FILLING_METADATA)
    execution.params(params)
    args = lesion_filling_cargs(params, execution)
    out = lesion_filling_outputs(params, execution)
    execution.run(args)
    return out

def lesion_filling(
    infile: InputPathType,
    outfile: str,
    lesionmask: InputPathType,
    wmmask: InputPathType | None = None,
    verbose_flag: bool = False,
    components_flag: bool = False,
    help_flag: bool = False,
    runner: Runner | None = None,
) -> LesionFillingOutputs:
    """
    lesion_filling

    Lesion filling tool as part of FSL

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        infile: Input image filename (e.g., T1w image)
        outfile: Output filename (lesion filled image)
        lesionmask: Filename of lesion mask image
        wmmask: Filename of white matter mask image
        verbose_flag: Switch on diagnostic messages
        components_flag: Save all lesion components as volumes
        help_flag: Display help message
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = lesion_filling_params(
        infile=infile,
        outfile=outfile,
        lesionmask=lesionmask,
        wmmask=wmmask,
        verbose_flag=verbose_flag,
        components_flag=components_flag,
        help_flag=help_flag,
    )
    return lesion_filling_execute(params, runner)

__all__ = [
    "LesionFilling",
    "LesionFillingOutputs",
    "LESION_FILLING_METADATA",
    "lesion_filling_cargs",
    "lesion_filling_outputs",
    "lesion_filling_params",
    "lesion_filling_execute",
    "lesion_filling_validate",
    "lesion_filling",
]