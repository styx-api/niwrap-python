# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SPHARM_RM_METADATA = Metadata(
    id="spharm_rm",
    name="spharm_rm",
    package="fsl",
)

SpharmRm = typing.TypedDict(
    "SpharmRm",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/spharm_rm"]],
        "input_file": InputPathType,
        "output_file": str,
        "mask_file": typing.NotRequired[InputPathType],
        "number_of_terms": typing.NotRequired[float],
        "verbose_flag": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SpharmRmOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType
    """Output image file generated by spharm_rm"""

def spharm_rm_params(
    input_file: InputPathType,
    output_file: str,
    mask_file: InputPathType | None = None,
    number_of_terms: float | None = None,
    verbose_flag: bool = False,
) -> SpharmRm:
    """
    Build parameters.

    Args:
        input_file: Input image filename
        output_file: Output image filename
        mask_file: Mask filename
        number_of_terms: Number of terms to remove (order is\
            1,x,y,z,z^2+(x^2+y^2)/2,zx,zy,xy,x^2-y^2)
        verbose_flag: Switch on diagnostic messages

    Returns:
        Parameter dictionary.
    """
    params: SpharmRm = {
        "@type": "fsl/spharm_rm",
        "input_file": input_file,
        "output_file": output_file,
        "verbose_flag": verbose_flag,
    }
    if mask_file is not None:
        params["mask_file"] = mask_file
    if number_of_terms is not None:
        params["number_of_terms"] = number_of_terms
    return params

def spharm_rm_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SpharmRm."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params["mask_file"])}` expected `InputPathType`')
    if params.get("number_of_terms", None) is not None:
        if not isinstance(params["number_of_terms"], (float, int)):
            raise StyxValidationError(f'`number_of_terms` has the wrong type: Received `{type(params["number_of_terms"])}` expected `float`')
    if params.get("verbose_flag", None) is not None:
        if not isinstance(params["verbose_flag"], bool):
            raise StyxValidationError(f'`verbose_flag` has the wrong type: Received `{type(params["verbose_flag"])}` expected `bool`')

def spharm_rm_cargs(params: SpharmRm, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("spharm_rm")
    cargs.append("--in")
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append("--out")
    cargs.append(params["output_file"])
    v_0 = params.get("mask_file")
    if v_0 is not None:
        cargs.append("-m")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("number_of_terms")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    if params.get("verbose_flag"):
        cargs.append("--verbose")
    return cargs

def spharm_rm_outputs(params: SpharmRm, execution: Execution) -> SpharmRmOutputs:
    """Build the SpharmRmOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    outfile_v: OutputPathType = execution.output_file(params["output_file"])
    return SpharmRmOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def spharm_rm_execute(params: SpharmRm, runner: Runner | None = None) -> SpharmRmOutputs:
    """
    spharm_rm

    Part of FSL - Spherical harmonics removal tool to process neuroimaging data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    spharm_rm_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SPHARM_RM_METADATA)
    execution.params(params)
    args = spharm_rm_cargs(params, execution)
    out = spharm_rm_outputs(params, execution)
    execution.run(args)
    return out

def spharm_rm(
    input_file: InputPathType,
    output_file: str,
    mask_file: InputPathType | None = None,
    number_of_terms: float | None = None,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> SpharmRmOutputs:
    """
    spharm_rm

    Part of FSL - Spherical harmonics removal tool to process neuroimaging data

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_file: Input image filename
        output_file: Output image filename
        mask_file: Mask filename
        number_of_terms: Number of terms to remove (order is\
            1,x,y,z,z^2+(x^2+y^2)/2,zx,zy,xy,x^2-y^2)
        verbose_flag: Switch on diagnostic messages
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = spharm_rm_params(
        input_file=input_file,
        output_file=output_file,
        mask_file=mask_file,
        number_of_terms=number_of_terms,
        verbose_flag=verbose_flag,
    )
    return spharm_rm_execute(params, runner)

__all__ = [
    "SpharmRm",
    "SpharmRmOutputs",
    "SPHARM_RM_METADATA",
    "spharm_rm_cargs",
    "spharm_rm_outputs",
    "spharm_rm_params",
    "spharm_rm_execute",
    "spharm_rm_validate",
    "spharm_rm",
]