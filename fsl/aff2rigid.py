# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AFF2RIGID_METADATA = Metadata(
    id="aff2rigid",
    name="aff2rigid",
    package="fsl",
)

Aff2rigid = typing.TypedDict(
    "Aff2rigid",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/aff2rigid"]],
        "input_transform": InputPathType,
        "output_transform": str,
    },
)

@dataclasses.dataclass
class Aff2rigidOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def aff2rigid_params(
    input_transform: InputPathType,
    output_transform: str,
) -> Aff2rigid:
    """
    Build parameters.

    Args:
        input_transform: Input FLIRT transform (12 DOF) from the input image to\
            standard space.
        output_transform: Output matrix which will convert the input image to\
            standard space using a 6 DOF transformation.

    Returns:
        Parameter dictionary.
    """
    params: Aff2rigid = {
        "@type": "fsl/aff2rigid",
        "input_transform": input_transform,
        "output_transform": output_transform,
    }
    return params

def aff2rigid_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Aff2rigid."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_transform", None) is None:
        raise StyxValidationError("`input_transform` must not be None")
    if not isinstance(params["input_transform"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_transform` has the wrong type: Received `{type(params["input_transform"])}` expected `InputPathType`')
    if params.get("output_transform", None) is None:
        raise StyxValidationError("`output_transform` must not be None")
    if not isinstance(params["output_transform"], str):
        raise StyxValidationError(f'`output_transform` has the wrong type: Received `{type(params["output_transform"])}` expected `str`')

def aff2rigid_cargs(params: Aff2rigid, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("aff2rigid")
    cargs.append(execution.input_file(params["input_transform"]))
    cargs.append(params["output_transform"])
    return cargs

def aff2rigid_outputs(params: Aff2rigid, execution: Execution) -> Aff2rigidOutputs:
    """Build the Aff2rigidOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Aff2rigidOutputs(
        root=root_v,
    )

def aff2rigid_execute(params: Aff2rigid, runner: Runner | None = None) -> Aff2rigidOutputs:
    """
    aff2rigid

    Tool for converting affine transformations to rigid transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    aff2rigid_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AFF2RIGID_METADATA)
    execution.params(params)
    args = aff2rigid_cargs(params, execution)
    out = aff2rigid_outputs(params, execution)
    execution.run(args)
    return out

def aff2rigid(
    input_transform: InputPathType,
    output_transform: str,
    runner: Runner | None = None,
) -> Aff2rigidOutputs:
    """
    aff2rigid

    Tool for converting affine transformations to rigid transformations.

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        input_transform: Input FLIRT transform (12 DOF) from the input image to\
            standard space.
        output_transform: Output matrix which will convert the input image to\
            standard space using a 6 DOF transformation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = aff2rigid_params(
        input_transform=input_transform,
        output_transform=output_transform,
    )
    return aff2rigid_execute(params, runner)

__all__ = [
    "Aff2rigid",
    "Aff2rigidOutputs",
    "AFF2RIGID_METADATA",
    "aff2rigid_cargs",
    "aff2rigid_outputs",
    "aff2rigid_params",
    "aff2rigid_execute",
    "aff2rigid_validate",
    "aff2rigid",
]