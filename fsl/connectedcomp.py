# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONNECTEDCOMP_METADATA = Metadata(
    id="connectedcomp",
    name="connectedcomp",
    package="fsl",
)

Connectedcomp = typing.TypedDict(
    "Connectedcomp",
    {
        "@type": typing.NotRequired[typing.Literal["fsl/connectedcomp"]],
        "in_volume": InputPathType,
        "output_volume": typing.NotRequired[str],
        "num_connect": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class ConnectedcompOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    outfile: OutputPathType | None
    """The output image volume"""

def connectedcomp_params(
    in_volume: InputPathType,
    output_volume: str | None = None,
    num_connect: int | None = None,
) -> Connectedcomp:
    """
    Build parameters.

    Args:
        in_volume: Input image volume
        output_volume: Output image volume
        num_connect: Number of connected components

    Returns:
        Parameter dictionary.
    """
    params: Connectedcomp = {
        "@type": "fsl/connectedcomp",
        "in_volume": in_volume,
    }
    if output_volume is not None:
        params["output_volume"] = output_volume
    if num_connect is not None:
        params["num_connect"] = num_connect
    return params

def connectedcomp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Connectedcomp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("in_volume", None) is None:
        raise StyxValidationError("`in_volume` must not be None")
    if not isinstance(params["in_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_volume` has the wrong type: Received `{type(params["in_volume"])}` expected `InputPathType`')
    if params.get("output_volume", None) is not None:
        if not isinstance(params["output_volume"], str):
            raise StyxValidationError(f'`output_volume` has the wrong type: Received `{type(params["output_volume"])}` expected `str`')
    if params.get("num_connect", None) is not None:
        if not isinstance(params["num_connect"], int):
            raise StyxValidationError(f'`num_connect` has the wrong type: Received `{type(params["num_connect"])}` expected `int`')

def connectedcomp_cargs(params: Connectedcomp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("connectedcomp")
    cargs.append(execution.input_file(params["in_volume"]))
    v_0 = params.get("output_volume")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("num_connect")
    if v_1 is not None:
        cargs.append(str(v_1))
    return cargs

def connectedcomp_outputs(params: Connectedcomp, execution: Execution) -> ConnectedcompOutputs:
    """Build the ConnectedcompOutputs object for this tool."""
    outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output_volume")
    if __v0 is not None:
        outfile_v = execution.output_file((__v0 if __v0 is not None else ""))
    return ConnectedcompOutputs(
        root=root_v,
        outfile=outfile_v,
    )

def connectedcomp_execute(params: Connectedcomp, runner: Runner | None = None) -> ConnectedcompOutputs:
    """
    connectedcomp

    Connected component analysis tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    connectedcomp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONNECTEDCOMP_METADATA)
    execution.params(params)
    args = connectedcomp_cargs(params, execution)
    out = connectedcomp_outputs(params, execution)
    execution.run(args)
    return out

def connectedcomp(
    in_volume: InputPathType,
    output_volume: str | None = None,
    num_connect: int | None = None,
    runner: Runner | None = None,
) -> ConnectedcompOutputs:
    """
    connectedcomp

    Connected component analysis tool

    Author: FMRIB Analysis Group, University of Oxford

    URL: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki

    Args:
        in_volume: Input image volume
        output_volume: Output image volume
        num_connect: Number of connected components
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = connectedcomp_params(
        in_volume=in_volume,
        output_volume=output_volume,
        num_connect=num_connect,
    )
    return connectedcomp_execute(params, runner)

__all__ = [
    "Connectedcomp",
    "ConnectedcompOutputs",
    "CONNECTEDCOMP_METADATA",
    "connectedcomp_cargs",
    "connectedcomp_outputs",
    "connectedcomp_params",
    "connectedcomp_execute",
    "connectedcomp_validate",
    "connectedcomp",
]