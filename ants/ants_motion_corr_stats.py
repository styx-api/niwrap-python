# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_MOTION_CORR_STATS_METADATA = Metadata(
    id="antsMotionCorrStats",
    name="antsMotionCorrStats",
    package="ants",
)

AntsMotionCorrStats = typing.TypedDict(
    "AntsMotionCorrStats",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsMotionCorrStats"]],
        "mask": InputPathType,
        "moco_params": InputPathType,
        "output": str,
        "transform_index": typing.NotRequired[int],
        "framewise": typing.NotRequired[bool],
        "spatial_map": typing.NotRequired[bool],
        "timeseries_displacement": typing.NotRequired[bool],
        "help": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsMotionCorrStatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_csv: OutputPathType
    """The corrected motion parameters.csv file."""

def ants_motion_corr_stats_params(
    mask: InputPathType,
    moco_params: InputPathType,
    output: str,
    transform_index: int | None = None,
    framewise: bool | None = None,
    spatial_map: bool = False,
    timeseries_displacement: bool = False,
    help: bool | None = None,
) -> AntsMotionCorrStats:
    """
    Build parameters.

    Args:
        mask: Mask image - compute displacements within mask.
        moco_params: Motion correction parameters from antsMotionCorr.
        output: Specify the output file.
        transform_index: Specify the index for a 3D transform to output.
        framewise: Do framewise summary stats.
        spatial_map: Output image of displacement magnitude.
        timeseries_displacement: Output 4d time-series image of displacement\
            magnitude.
        help: Print the help menu. Short version with -h.

    Returns:
        Parameter dictionary.
    """
    params: AntsMotionCorrStats = {
        "@type": "ants/antsMotionCorrStats",
        "mask": mask,
        "moco_params": moco_params,
        "output": output,
        "spatial_map": spatial_map,
        "timeseries_displacement": timeseries_displacement,
    }
    if transform_index is not None:
        params["transform_index"] = transform_index
    if framewise is not None:
        params["framewise"] = framewise
    if help is not None:
        params["help"] = help
    return params

def ants_motion_corr_stats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsMotionCorrStats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("moco_params", None) is None:
        raise StyxValidationError("`moco_params` must not be None")
    if not isinstance(params["moco_params"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moco_params` has the wrong type: Received `{type(params["moco_params"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("transform_index", None) is not None:
        if not isinstance(params["transform_index"], int):
            raise StyxValidationError(f'`transform_index` has the wrong type: Received `{type(params["transform_index"])}` expected `int`')
    if params.get("framewise", None) is not None:
        if not isinstance(params["framewise"], bool):
            raise StyxValidationError(f'`framewise` has the wrong type: Received `{type(params["framewise"])}` expected `bool`')
    if params.get("spatial_map", None) is not None:
        if not isinstance(params["spatial_map"], bool):
            raise StyxValidationError(f'`spatial_map` has the wrong type: Received `{type(params["spatial_map"])}` expected `bool`')
    if params.get("timeseries_displacement", None) is not None:
        if not isinstance(params["timeseries_displacement"], bool):
            raise StyxValidationError(f'`timeseries_displacement` has the wrong type: Received `{type(params["timeseries_displacement"])}` expected `bool`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')

def ants_motion_corr_stats_cargs(params: AntsMotionCorrStats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsMotionCorrStats")
    cargs.append("-x")
    cargs.append(execution.input_file(params["mask"]))
    cargs.append("-m")
    cargs.append(execution.input_file(params["moco_params"]))
    cargs.append("-o")
    cargs.append(params["output"])
    v_0 = params.get("transform_index")
    if v_0 is not None:
        cargs.append("-t")
        cargs.append(str(v_0))
    v_1 = params.get("framewise")
    if v_1 is not None:
        cargs.append("-f")
        if v_1:
            cargs.append("0")
        else:
            cargs.append("1")
    if params.get("spatial_map"):
        cargs.append("-s")
    if params.get("timeseries_displacement"):
        cargs.append("-d")
    v_2 = params.get("help")
    if v_2 is not None:
        cargs.append("--help")
        if v_2:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def ants_motion_corr_stats_outputs(params: AntsMotionCorrStats, execution: Execution) -> AntsMotionCorrStatsOutputs:
    """Build the AntsMotionCorrStatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_csv_v: OutputPathType = execution.output_file(params["output"])
    return AntsMotionCorrStatsOutputs(
        root=root_v,
        corrected_csv=corrected_csv_v,
    )

def ants_motion_corr_stats_execute(params: AntsMotionCorrStats, runner: Runner | None = None) -> AntsMotionCorrStatsOutputs:
    """
    antsMotionCorrStats

    Create summary measures of the parameters that are output by antsMotionCorr. Currently only works for linear transforms. Outputs the mean and max displacements for the voxels within a provided mask, at each time point. By default the displacements are relative to the reference space, but the framewise option may be used to provide displacements between consecutive time points.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_motion_corr_stats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_MOTION_CORR_STATS_METADATA)
    execution.params(params)
    args = ants_motion_corr_stats_cargs(params, execution)
    out = ants_motion_corr_stats_outputs(params, execution)
    execution.run(args)
    return out

def ants_motion_corr_stats(
    mask: InputPathType,
    moco_params: InputPathType,
    output: str,
    transform_index: int | None = None,
    framewise: bool | None = None,
    spatial_map: bool = False,
    timeseries_displacement: bool = False,
    help: bool | None = None,
    runner: Runner | None = None,
) -> AntsMotionCorrStatsOutputs:
    """
    antsMotionCorrStats

    Create summary measures of the parameters that are output by antsMotionCorr. Currently only works for linear transforms. Outputs the mean and max displacements for the voxels within a provided mask, at each time point. By default the displacements are relative to the reference space, but the framewise option may be used to provide displacements between consecutive time points.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        mask: Mask image - compute displacements within mask.
        moco_params: Motion correction parameters from antsMotionCorr.
        output: Specify the output file.
        transform_index: Specify the index for a 3D transform to output.
        framewise: Do framewise summary stats.
        spatial_map: Output image of displacement magnitude.
        timeseries_displacement: Output 4d time-series image of displacement\
            magnitude.
        help: Print the help menu. Short version with -h.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_motion_corr_stats_params(
        mask=mask,
        moco_params=moco_params,
        output=output,
        transform_index=transform_index,
        framewise=framewise,
        spatial_map=spatial_map,
        timeseries_displacement=timeseries_displacement,
        help=help,
    )
    return ants_motion_corr_stats_execute(params, runner)

__all__ = [
    "AntsMotionCorrStats",
    "AntsMotionCorrStatsOutputs",
    "ANTS_MOTION_CORR_STATS_METADATA",
    "ants_motion_corr_stats_cargs",
    "ants_motion_corr_stats_outputs",
    "ants_motion_corr_stats_params",
    "ants_motion_corr_stats_execute",
    "ants_motion_corr_stats_validate",
    "ants_motion_corr_stats",
]