# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CREATE_DTI_COHORT_METADATA = Metadata(
    id="CreateDTICohort",
    name="CreateDTICohort",
    package="ants",
)

CreateDtiCohortImageDimensionality = typing.Literal[2] | typing.Literal[3]

CreateDtiCohort = typing.TypedDict(
    "CreateDtiCohort",
    {
        "@type": typing.NotRequired[typing.Literal["ants/CreateDTICohort"]],
        "image_dimensionality": typing.NotRequired[CreateDtiCohortImageDimensionality],
        "dti_atlas": InputPathType,
        "label_mask_image": typing.NotRequired[str],
        "noise_sigma": typing.NotRequired[float],
        "pathology": typing.NotRequired[str],
        "dwi_parameters": str,
        "registered_population": typing.NotRequired[InputPathType],
        "output": str,
    },
)

@dataclasses.dataclass
class CreateDtiCohortOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_directory: OutputPathType
    """The directory where the output data will be stored."""

def create_dti_cohort_params(
    dti_atlas: InputPathType,
    dwi_parameters: str,
    output: str,
    image_dimensionality: CreateDtiCohortImageDimensionality | None = None,
    label_mask_image: str | None = None,
    noise_sigma: float | None = None,
    pathology: str | None = None,
    registered_population: InputPathType | None = None,
) -> CreateDtiCohort:
    """
    Build parameters.

    Args:
        dti_atlas: A diffusion tensor atlas image is required input for creating\
            the cohort.
        dwi_parameters: This option specifies the parameters of the output\
            diffusion-weighted images, including the directions and b-values.\
            Directions can be specified using a direction file or scheme file.
        output: The output consists of a set of diffusion-weighted images for each\
            subject. Control and experimental subject numbers can be specified.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        label_mask_image: A mask image can be specified which determines the\
            region(s) to which the simulated pathology operations are applied. If\
            no mask is specified one is created by thresholding the atlas FA map at\
            0.2 unless a lower threshold is specified.
        noise_sigma: This parameter characterizes the Rician noise in the original\
            DWI images. Default value is 18.
        pathology: The user can specify the simulated pathology in a given area\
            using a label mask. Pathology is simulated by changing the eigenvalues.\
            One can specify the number of voxels affected in each region or the\
            proportion of voxels affected. Change is specified as a proportion of\
            the current eigenvalues.
        registered_population: To introduce inter-subject variability, a registered\
            DTI population to the DTI atlas is required. This is modeled by PCA\
            decomposition.

    Returns:
        Parameter dictionary.
    """
    params: CreateDtiCohort = {
        "@type": "ants/CreateDTICohort",
        "dti_atlas": dti_atlas,
        "dwi_parameters": dwi_parameters,
        "output": output,
    }
    if image_dimensionality is not None:
        params["image_dimensionality"] = image_dimensionality
    if label_mask_image is not None:
        params["label_mask_image"] = label_mask_image
    if noise_sigma is not None:
        params["noise_sigma"] = noise_sigma
    if pathology is not None:
        params["pathology"] = pathology
    if registered_population is not None:
        params["registered_population"] = registered_population
    return params

def create_dti_cohort_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CreateDtiCohort."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimensionality", None) is not None:
        if not isinstance(params["image_dimensionality"], (float, int)):
            raise StyxValidationError(f'`image_dimensionality` has the wrong type: Received `{type(params["image_dimensionality"])}` expected `CreateDtiCohortImageDimensionality`')
        if params["image_dimensionality"] not in [2, 3]:
            raise StyxValidationError("Parameter `image_dimensionality` must be one of [2, 3]")
    if params.get("dti_atlas", None) is None:
        raise StyxValidationError("`dti_atlas` must not be None")
    if not isinstance(params["dti_atlas"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dti_atlas` has the wrong type: Received `{type(params["dti_atlas"])}` expected `InputPathType`')
    if params.get("label_mask_image", None) is not None:
        if not isinstance(params["label_mask_image"], str):
            raise StyxValidationError(f'`label_mask_image` has the wrong type: Received `{type(params["label_mask_image"])}` expected `str`')
    if params.get("noise_sigma", None) is not None:
        if not isinstance(params["noise_sigma"], (float, int)):
            raise StyxValidationError(f'`noise_sigma` has the wrong type: Received `{type(params["noise_sigma"])}` expected `float`')
    if params.get("pathology", None) is not None:
        if not isinstance(params["pathology"], str):
            raise StyxValidationError(f'`pathology` has the wrong type: Received `{type(params["pathology"])}` expected `str`')
    if params.get("dwi_parameters", None) is None:
        raise StyxValidationError("`dwi_parameters` must not be None")
    if not isinstance(params["dwi_parameters"], str):
        raise StyxValidationError(f'`dwi_parameters` has the wrong type: Received `{type(params["dwi_parameters"])}` expected `str`')
    if params.get("registered_population", None) is not None:
        if not isinstance(params["registered_population"], (pathlib.Path, str)):
            raise StyxValidationError(f'`registered_population` has the wrong type: Received `{type(params["registered_population"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def create_dti_cohort_cargs(params: CreateDtiCohort, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("CreateDTICohort")
    v_0 = params.get("image_dimensionality")
    if v_0 is not None:
        cargs.append("--image-dimensionality")
        cargs.append(str(v_0))
    cargs.append("--dti-atlas")
    cargs.append(execution.input_file(params["dti_atlas"]))
    v_1 = params.get("label_mask_image")
    if v_1 is not None:
        cargs.append("--label-mask-image")
        cargs.append(v_1)
    v_2 = params.get("noise_sigma")
    if v_2 is not None:
        cargs.append("--noise-sigma")
        cargs.append(str(v_2))
    v_3 = params.get("pathology")
    if v_3 is not None:
        cargs.append("--pathology")
        cargs.append(v_3)
    cargs.append("--dwi-parameters")
    cargs.append(params["dwi_parameters"])
    v_4 = params.get("registered_population")
    if v_4 is not None:
        cargs.append("--registered-population")
        cargs.append(execution.input_file(v_4))
    cargs.append("--output")
    cargs.append(params["output"])
    return cargs

def create_dti_cohort_outputs(params: CreateDtiCohort, execution: Execution) -> CreateDtiCohortOutputs:
    """Build the CreateDtiCohortOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_directory_v: OutputPathType = execution.output_file("[OUTPUT_DIRECTORY]")
    return CreateDtiCohortOutputs(
        root=root_v,
        output_directory=output_directory_v,
    )

def create_dti_cohort_execute(params: CreateDtiCohort, runner: Runner | None = None) -> CreateDtiCohortOutputs:
    """
    CreateDTICohort

    CreateDTICohort implements the work of Van Hecke et al. to create simulated DTI data sets. The only difference is that all registrations (both for the input population and for the output population) are assumed to take place outside of this program.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    create_dti_cohort_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CREATE_DTI_COHORT_METADATA)
    execution.params(params)
    args = create_dti_cohort_cargs(params, execution)
    out = create_dti_cohort_outputs(params, execution)
    execution.run(args)
    return out

def create_dti_cohort(
    dti_atlas: InputPathType,
    dwi_parameters: str,
    output: str,
    image_dimensionality: CreateDtiCohortImageDimensionality | None = None,
    label_mask_image: str | None = None,
    noise_sigma: float | None = None,
    pathology: str | None = None,
    registered_population: InputPathType | None = None,
    runner: Runner | None = None,
) -> CreateDtiCohortOutputs:
    """
    CreateDTICohort

    CreateDTICohort implements the work of Van Hecke et al. to create simulated DTI data sets. The only difference is that all registrations (both for the input population and for the output population) are assumed to take place outside of this program.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dti_atlas: A diffusion tensor atlas image is required input for creating\
            the cohort.
        dwi_parameters: This option specifies the parameters of the output\
            diffusion-weighted images, including the directions and b-values.\
            Directions can be specified using a direction file or scheme file.
        output: The output consists of a set of diffusion-weighted images for each\
            subject. Control and experimental subject numbers can be specified.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        label_mask_image: A mask image can be specified which determines the\
            region(s) to which the simulated pathology operations are applied. If\
            no mask is specified one is created by thresholding the atlas FA map at\
            0.2 unless a lower threshold is specified.
        noise_sigma: This parameter characterizes the Rician noise in the original\
            DWI images. Default value is 18.
        pathology: The user can specify the simulated pathology in a given area\
            using a label mask. Pathology is simulated by changing the eigenvalues.\
            One can specify the number of voxels affected in each region or the\
            proportion of voxels affected. Change is specified as a proportion of\
            the current eigenvalues.
        registered_population: To introduce inter-subject variability, a registered\
            DTI population to the DTI atlas is required. This is modeled by PCA\
            decomposition.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = create_dti_cohort_params(
        dti_atlas=dti_atlas,
        dwi_parameters=dwi_parameters,
        output=output,
        image_dimensionality=image_dimensionality,
        label_mask_image=label_mask_image,
        noise_sigma=noise_sigma,
        pathology=pathology,
        registered_population=registered_population,
    )
    return create_dti_cohort_execute(params, runner)

__all__ = [
    "CreateDtiCohort",
    "CreateDtiCohortOutputs",
    "CREATE_DTI_COHORT_METADATA",
    "create_dti_cohort_cargs",
    "create_dti_cohort_outputs",
    "create_dti_cohort_params",
    "create_dti_cohort_execute",
    "create_dti_cohort_validate",
    "create_dti_cohort",
]