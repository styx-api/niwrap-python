# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TILE_IMAGES_METADATA = Metadata(
    id="TileImages",
    name="TileImages",
    package="ants",
)

TileImages = typing.TypedDict(
    "TileImages",
    {
        "@type": typing.NotRequired[typing.Literal["ants/TileImages"]],
        "image_dimension": int,
        "output_image": str,
        "layout": str,
        "input_images": list[InputPathType],
    },
)

@dataclasses.dataclass
class TileImagesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tiled_image: OutputPathType
    """The final tiled output image."""

def tile_images_params(
    image_dimension: int,
    output_image: str,
    layout: str,
    input_images: list[InputPathType],
) -> TileImages:
    """
    Build parameters.

    Args:
        image_dimension: Dimensionality of the output image.
        output_image: The path for the output tiled image.
        layout: Defines the structure of the tiled output image. The layout\
            dictates the number and arrangement of input images in the output\
            image.
        input_images: Input images to be tiled into the output image. The number of\
            input images should match the layout specification.

    Returns:
        Parameter dictionary.
    """
    params: TileImages = {
        "@type": "ants/TileImages",
        "image_dimension": image_dimension,
        "output_image": output_image,
        "layout": layout,
        "input_images": input_images,
    }
    return params

def tile_images_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TileImages."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("layout", None) is None:
        raise StyxValidationError("`layout` must not be None")
    if not isinstance(params["layout"], str):
        raise StyxValidationError(f'`layout` has the wrong type: Received `{type(params["layout"])}` expected `str`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    if len(params["input_images"]) < 1:
        raise StyxValidationError("Parameter `input_images` must contain at least 1 element")
    for e in params["input_images"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def tile_images_cargs(params: TileImages, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("TileImages")
    cargs.append(str(params["image_dimension"]))
    cargs.append(params["output_image"])
    cargs.append(params["layout"])
    for item0 in params["input_images"]:
        cargs.append(execution.input_file(item0))
    return cargs

def tile_images_outputs(params: TileImages, execution: Execution) -> TileImagesOutputs:
    """Build the TileImagesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tiled_image_v: OutputPathType = execution.output_file(params["output_image"])
    return TileImagesOutputs(
        root=root_v,
        tiled_image=tiled_image_v,
    )

def tile_images_execute(params: TileImages, runner: Runner | None = None) -> TileImagesOutputs:
    """
    TileImages

    TileImages allows assembling images into a multi-dimensional array, producing a single output image. The input images must have a dimension less than or equal to the specified output image dimension.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tile_images_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TILE_IMAGES_METADATA)
    execution.params(params)
    args = tile_images_cargs(params, execution)
    out = tile_images_outputs(params, execution)
    execution.run(args)
    return out

def tile_images(
    image_dimension: int,
    output_image: str,
    layout: str,
    input_images: list[InputPathType],
    runner: Runner | None = None,
) -> TileImagesOutputs:
    """
    TileImages

    TileImages allows assembling images into a multi-dimensional array, producing a single output image. The input images must have a dimension less than or equal to the specified output image dimension.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Dimensionality of the output image.
        output_image: The path for the output tiled image.
        layout: Defines the structure of the tiled output image. The layout\
            dictates the number and arrangement of input images in the output\
            image.
        input_images: Input images to be tiled into the output image. The number of\
            input images should match the layout specification.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tile_images_params(
        image_dimension=image_dimension,
        output_image=output_image,
        layout=layout,
        input_images=input_images,
    )
    return tile_images_execute(params, runner)

__all__ = [
    "TileImages",
    "TileImagesOutputs",
    "TILE_IMAGES_METADATA",
    "tile_images_cargs",
    "tile_images_outputs",
    "tile_images_params",
    "tile_images_execute",
    "tile_images_validate",
    "tile_images",
]