# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_USE_LANDMARK_IMAGES_TO_GET_AFFINE_TRANSFORM_METADATA = Metadata(
    id="ANTSUseLandmarkImagesToGetAffineTransform",
    name="ANTSUseLandmarkImagesToGetAffineTransform",
    package="ants",
)

AntsUseLandmarkImagesToGetAffineTransformTransformType = typing.Literal["rigid"] | typing.Literal["affine"]

AntsUseLandmarkImagesToGetAffineTransform = typing.TypedDict(
    "AntsUseLandmarkImagesToGetAffineTransform",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ANTSUseLandmarkImagesToGetAffineTransform"]],
        "fixed_image": InputPathType,
        "moving_image": InputPathType,
        "transform_type": AntsUseLandmarkImagesToGetAffineTransformTransformType,
        "output_affine": str,
    },
)

@dataclasses.dataclass
class AntsUseLandmarkImagesToGetAffineTransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    affine_transform_matrix: OutputPathType
    """The output is the affine transformation matrix file."""

def ants_use_landmark_images_to_get_affine_transform_params(
    fixed_image: InputPathType,
    moving_image: InputPathType,
    transform_type: AntsUseLandmarkImagesToGetAffineTransformTransformType,
    output_affine: str,
) -> AntsUseLandmarkImagesToGetAffineTransform:
    """
    Build parameters.

    Args:
        fixed_image: The fixed image containing labeled landmarks (N-ary image).
        moving_image: The moving image containing labeled landmarks (N-ary image).
        transform_type: Type of transform to compute: 'rigid' or 'affine'.
        output_affine: The output file for the affine transform matrix (e.g.,\
            OutAffine.txt).

    Returns:
        Parameter dictionary.
    """
    params: AntsUseLandmarkImagesToGetAffineTransform = {
        "@type": "ants/ANTSUseLandmarkImagesToGetAffineTransform",
        "fixed_image": fixed_image,
        "moving_image": moving_image,
        "transform_type": transform_type,
        "output_affine": output_affine,
    }
    return params

def ants_use_landmark_images_to_get_affine_transform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsUseLandmarkImagesToGetAffineTransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed_image", None) is None:
        raise StyxValidationError("`fixed_image` must not be None")
    if not isinstance(params["fixed_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_image` has the wrong type: Received `{type(params["fixed_image"])}` expected `InputPathType`')
    if params.get("moving_image", None) is None:
        raise StyxValidationError("`moving_image` must not be None")
    if not isinstance(params["moving_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_image` has the wrong type: Received `{type(params["moving_image"])}` expected `InputPathType`')
    if params.get("transform_type", None) is None:
        raise StyxValidationError("`transform_type` must not be None")
    if not isinstance(params["transform_type"], str):
        raise StyxValidationError(f'`transform_type` has the wrong type: Received `{type(params["transform_type"])}` expected `AntsUseLandmarkImagesToGetAffineTransformTransformType`')
    if params["transform_type"] not in ["rigid", "affine"]:
        raise StyxValidationError("Parameter `transform_type` must be one of [\"rigid\", \"affine\"]")
    if params.get("output_affine", None) is None:
        raise StyxValidationError("`output_affine` must not be None")
    if not isinstance(params["output_affine"], str):
        raise StyxValidationError(f'`output_affine` has the wrong type: Received `{type(params["output_affine"])}` expected `str`')

def ants_use_landmark_images_to_get_affine_transform_cargs(params: AntsUseLandmarkImagesToGetAffineTransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ANTSUseLandmarkImagesToGetAffineTransform")
    cargs.append(execution.input_file(params["fixed_image"]))
    cargs.append(execution.input_file(params["moving_image"]))
    cargs.append(str(params["transform_type"]))
    cargs.append(params["output_affine"])
    return cargs

def ants_use_landmark_images_to_get_affine_transform_outputs(params: AntsUseLandmarkImagesToGetAffineTransform, execution: Execution) -> AntsUseLandmarkImagesToGetAffineTransformOutputs:
    """Build the AntsUseLandmarkImagesToGetAffineTransformOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    affine_transform_matrix_v: OutputPathType = execution.output_file(params["output_affine"])
    return AntsUseLandmarkImagesToGetAffineTransformOutputs(
        root=root_v,
        affine_transform_matrix=affine_transform_matrix_v,
    )

def ants_use_landmark_images_to_get_affine_transform_execute(params: AntsUseLandmarkImagesToGetAffineTransform, runner: Runner | None = None) -> AntsUseLandmarkImagesToGetAffineTransformOutputs:
    """
    ANTSUseLandmarkImagesToGetAffineTransform

    This tool computes an affine transform (rigid or affine) from labeled landmark images. It requires input images to be N-ary, in the same physical space as the images you want to register, and to have the same landmark points defined within them. Landmarks are defined from the center of mass of the labels in the input images. ITK-snap can be used to generate the label images.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_use_landmark_images_to_get_affine_transform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_USE_LANDMARK_IMAGES_TO_GET_AFFINE_TRANSFORM_METADATA)
    execution.params(params)
    args = ants_use_landmark_images_to_get_affine_transform_cargs(params, execution)
    out = ants_use_landmark_images_to_get_affine_transform_outputs(params, execution)
    execution.run(args)
    return out

def ants_use_landmark_images_to_get_affine_transform(
    fixed_image: InputPathType,
    moving_image: InputPathType,
    transform_type: AntsUseLandmarkImagesToGetAffineTransformTransformType,
    output_affine: str,
    runner: Runner | None = None,
) -> AntsUseLandmarkImagesToGetAffineTransformOutputs:
    """
    ANTSUseLandmarkImagesToGetAffineTransform

    This tool computes an affine transform (rigid or affine) from labeled landmark images. It requires input images to be N-ary, in the same physical space as the images you want to register, and to have the same landmark points defined within them. Landmarks are defined from the center of mass of the labels in the input images. ITK-snap can be used to generate the label images.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        fixed_image: The fixed image containing labeled landmarks (N-ary image).
        moving_image: The moving image containing labeled landmarks (N-ary image).
        transform_type: Type of transform to compute: 'rigid' or 'affine'.
        output_affine: The output file for the affine transform matrix (e.g.,\
            OutAffine.txt).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_use_landmark_images_to_get_affine_transform_params(
        fixed_image=fixed_image,
        moving_image=moving_image,
        transform_type=transform_type,
        output_affine=output_affine,
    )
    return ants_use_landmark_images_to_get_affine_transform_execute(params, runner)

__all__ = [
    "AntsUseLandmarkImagesToGetAffineTransform",
    "AntsUseLandmarkImagesToGetAffineTransformOutputs",
    "ANTS_USE_LANDMARK_IMAGES_TO_GET_AFFINE_TRANSFORM_METADATA",
    "ants_use_landmark_images_to_get_affine_transform_cargs",
    "ants_use_landmark_images_to_get_affine_transform_outputs",
    "ants_use_landmark_images_to_get_affine_transform_params",
    "ants_use_landmark_images_to_get_affine_transform_execute",
    "ants_use_landmark_images_to_get_affine_transform_validate",
    "ants_use_landmark_images_to_get_affine_transform",
]