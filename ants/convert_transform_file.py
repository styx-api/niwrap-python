# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONVERT_TRANSFORM_FILE_METADATA = Metadata(
    id="ConvertTransformFile",
    name="ConvertTransformFile",
    package="ants",
)

ConvertTransformFile = typing.TypedDict(
    "ConvertTransformFile",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ConvertTransformFile"]],
        "dimensions": int,
        "input_transform_file": InputPathType,
        "output_transform_file": str,
        "matrix": typing.NotRequired[bool],
        "homogeneous_matrix": typing.NotRequired[bool],
        "RAS": typing.NotRequired[bool],
        "convert_to_affine_type": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class ConvertTransformFileOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def convert_transform_file_params(
    dimensions: int,
    input_transform_file: InputPathType,
    output_transform_file: str,
    matrix: bool = False,
    homogeneous_matrix: bool = False,
    RAS: bool = False,
    convert_to_affine_type: bool = False,
) -> ConvertTransformFile:
    """
    Build parameters.

    Args:
        dimensions: Dimensionality of the transform file.
        input_transform_file: Path to the input transform file.
        output_transform_file: Path for the output transform file.
        matrix: Output only the transform matrix to a text file, one row per line\
            with space-delimited values. Only works for specific transform types.
        homogeneous_matrix: Output an N+1 square homogeneous matrix from the\
            transform matrix and offset. Only works for specific transform types.
        RAS: Convert the output into the RAS coordinate system if combined with\
            'matrix' or 'homogeneousMatrix'.
        convert_to_affine_type: Convert the input transform type to AffineTransform\
            and output again as a binary transform file.

    Returns:
        Parameter dictionary.
    """
    params: ConvertTransformFile = {
        "@type": "ants/ConvertTransformFile",
        "dimensions": dimensions,
        "input_transform_file": input_transform_file,
        "output_transform_file": output_transform_file,
        "matrix": matrix,
        "homogeneous_matrix": homogeneous_matrix,
        "RAS": RAS,
        "convert_to_affine_type": convert_to_affine_type,
    }
    return params

def convert_transform_file_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ConvertTransformFile."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensions", None) is None:
        raise StyxValidationError("`dimensions` must not be None")
    if not isinstance(params["dimensions"], int):
        raise StyxValidationError(f'`dimensions` has the wrong type: Received `{type(params["dimensions"])}` expected `int`')
    if params.get("input_transform_file", None) is None:
        raise StyxValidationError("`input_transform_file` must not be None")
    if not isinstance(params["input_transform_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_transform_file` has the wrong type: Received `{type(params["input_transform_file"])}` expected `InputPathType`')
    if params.get("output_transform_file", None) is None:
        raise StyxValidationError("`output_transform_file` must not be None")
    if not isinstance(params["output_transform_file"], str):
        raise StyxValidationError(f'`output_transform_file` has the wrong type: Received `{type(params["output_transform_file"])}` expected `str`')
    if params.get("matrix", None) is not None:
        if not isinstance(params["matrix"], bool):
            raise StyxValidationError(f'`matrix` has the wrong type: Received `{type(params["matrix"])}` expected `bool`')
    if params.get("homogeneous_matrix", None) is not None:
        if not isinstance(params["homogeneous_matrix"], bool):
            raise StyxValidationError(f'`homogeneous_matrix` has the wrong type: Received `{type(params["homogeneous_matrix"])}` expected `bool`')
    if params.get("RAS", None) is not None:
        if not isinstance(params["RAS"], bool):
            raise StyxValidationError(f'`RAS` has the wrong type: Received `{type(params["RAS"])}` expected `bool`')
    if params.get("convert_to_affine_type", None) is not None:
        if not isinstance(params["convert_to_affine_type"], bool):
            raise StyxValidationError(f'`convert_to_affine_type` has the wrong type: Received `{type(params["convert_to_affine_type"])}` expected `bool`')

def convert_transform_file_cargs(params: ConvertTransformFile, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ConvertTransformFile")
    cargs.append(str(params["dimensions"]))
    cargs.append(execution.input_file(params["input_transform_file"]))
    cargs.append(params["output_transform_file"])
    if params.get("matrix"):
        cargs.append("--matrix")
    if params.get("homogeneous_matrix"):
        cargs.append("--homogeneousMatrix")
    if params.get("RAS"):
        cargs.append("--RAS")
    if params.get("convert_to_affine_type"):
        cargs.append("--convertToAffineType")
    return cargs

def convert_transform_file_outputs(params: ConvertTransformFile, execution: Execution) -> ConvertTransformFileOutputs:
    """Build the ConvertTransformFileOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return ConvertTransformFileOutputs(
        root=root_v,
    )

def convert_transform_file_execute(params: ConvertTransformFile, runner: Runner | None = None) -> ConvertTransformFileOutputs:
    """
    ConvertTransformFile

    Utility to read in a transform file (presumed to be in binary format) and output it in various formats. Default output is legacy human-readable text format. Without any options, the output filename extension must be .txt or .tfm to signify a text-formatted transform file.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    convert_transform_file_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONVERT_TRANSFORM_FILE_METADATA)
    execution.params(params)
    args = convert_transform_file_cargs(params, execution)
    out = convert_transform_file_outputs(params, execution)
    execution.run(args)
    return out

def convert_transform_file(
    dimensions: int,
    input_transform_file: InputPathType,
    output_transform_file: str,
    matrix: bool = False,
    homogeneous_matrix: bool = False,
    RAS: bool = False,
    convert_to_affine_type: bool = False,
    runner: Runner | None = None,
) -> ConvertTransformFileOutputs:
    """
    ConvertTransformFile

    Utility to read in a transform file (presumed to be in binary format) and output it in various formats. Default output is legacy human-readable text format. Without any options, the output filename extension must be .txt or .tfm to signify a text-formatted transform file.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimensions: Dimensionality of the transform file.
        input_transform_file: Path to the input transform file.
        output_transform_file: Path for the output transform file.
        matrix: Output only the transform matrix to a text file, one row per line\
            with space-delimited values. Only works for specific transform types.
        homogeneous_matrix: Output an N+1 square homogeneous matrix from the\
            transform matrix and offset. Only works for specific transform types.
        RAS: Convert the output into the RAS coordinate system if combined with\
            'matrix' or 'homogeneousMatrix'.
        convert_to_affine_type: Convert the input transform type to AffineTransform\
            and output again as a binary transform file.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = convert_transform_file_params(
        dimensions=dimensions,
        input_transform_file=input_transform_file,
        output_transform_file=output_transform_file,
        matrix=matrix,
        homogeneous_matrix=homogeneous_matrix,
        RAS=RAS,
        convert_to_affine_type=convert_to_affine_type,
    )
    return convert_transform_file_execute(params, runner)

__all__ = [
    "ConvertTransformFile",
    "ConvertTransformFileOutputs",
    "CONVERT_TRANSFORM_FILE_METADATA",
    "convert_transform_file_cargs",
    "convert_transform_file_outputs",
    "convert_transform_file_params",
    "convert_transform_file_execute",
    "convert_transform_file_validate",
    "convert_transform_file",
]