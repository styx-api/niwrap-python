# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PRINT_HEADER_METADATA = Metadata(
    id="PrintHeader",
    name="PrintHeader",
    package="ants",
)

PrintHeaderWhatInformation = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

PrintHeader = typing.TypedDict(
    "PrintHeader",
    {
        "@type": typing.NotRequired[typing.Literal["ants/PrintHeader"]],
        "image": InputPathType,
        "what_information": typing.NotRequired[PrintHeaderWhatInformation],
    },
)

@dataclasses.dataclass
class PrintHeaderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: list[str]
    """The requested header information. When a specific code is given, values are printed as 'x'-delimited strings (e.g. '1.0x1.0x1.0' for spacing). When no code is given, prints a formatted multi-line report of all header fields and metadata."""

def print_header_params(
    image: InputPathType,
    what_information: PrintHeaderWhatInformation | None = None,
) -> PrintHeader:
    """
    Build parameters.

    Args:
        image: Input image file (any format supported by ITK, e.g. NIfTI, NRRD,\
            MetaImage). Supports 1D through 5D images.
        what_information: Specify which header field to print: 0 = origin, 1 =\
            spacing, 2 = size, 3 = index, 4 = direction. Values are printed as\
            'x'-delimited strings. If omitted, prints a full header dump.

    Returns:
        Parameter dictionary.
    """
    params: PrintHeader = {
        "@type": "ants/PrintHeader",
        "image": image,
    }
    if what_information is not None:
        params["what_information"] = what_information
    return params

def print_header_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid PrintHeader."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("what_information", None) is not None:
        if not isinstance(params["what_information"], (float, int)):
            raise StyxValidationError(f'`what_information` has the wrong type: Received `{type(params["what_information"])}` expected `PrintHeaderWhatInformation`')
        if params["what_information"] not in [0, 1, 2, 3, 4]:
            raise StyxValidationError("Parameter `what_information` must be one of [0, 1, 2, 3, 4]")

def print_header_cargs(params: PrintHeader, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("PrintHeader")
    cargs.append(execution.input_file(params["image"]))
    v_0 = params.get("what_information")
    if v_0 is not None:
        cargs.append(str(v_0))
    return cargs

def print_header_outputs(params: PrintHeader, execution: Execution) -> PrintHeaderOutputs:
    """Build the PrintHeaderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return PrintHeaderOutputs(
        root=root_v,
        output=[],
    )

def print_header_execute(params: PrintHeader, runner: Runner | None = None) -> PrintHeaderOutputs:
    """
    PrintHeader

    Prints header information from a medical image file. When called with only an image, prints a full header dump including dimensions, bounding box, origin, spacing, intensity range and mean, canonical orientation code, direction cosine matrix, and all image metadata. When called with a specific information code, prints only that property.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    print_header_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PRINT_HEADER_METADATA)
    execution.params(params)
    args = print_header_cargs(params, execution)
    out = print_header_outputs(params, execution)
    execution.run(args, handle_stdout=lambda s: out.output.append(s))
    return out

def print_header(
    image: InputPathType,
    what_information: PrintHeaderWhatInformation | None = None,
    runner: Runner | None = None,
) -> PrintHeaderOutputs:
    """
    PrintHeader

    Prints header information from a medical image file. When called with only an image, prints a full header dump including dimensions, bounding box, origin, spacing, intensity range and mean, canonical orientation code, direction cosine matrix, and all image metadata. When called with a specific information code, prints only that property.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image: Input image file (any format supported by ITK, e.g. NIfTI, NRRD,\
            MetaImage). Supports 1D through 5D images.
        what_information: Specify which header field to print: 0 = origin, 1 =\
            spacing, 2 = size, 3 = index, 4 = direction. Values are printed as\
            'x'-delimited strings. If omitted, prints a full header dump.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = print_header_params(
        image=image,
        what_information=what_information,
    )
    return print_header_execute(params, runner)

__all__ = [
    "PrintHeader",
    "PrintHeaderOutputs",
    "PRINT_HEADER_METADATA",
    "print_header_cargs",
    "print_header_outputs",
    "print_header_params",
    "print_header_execute",
    "print_header_validate",
    "print_header",
]