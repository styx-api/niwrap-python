# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_APPLY_TRANSFORMS_METADATA = Metadata(
    id="antsApplyTransforms",
    name="antsApplyTransforms",
    package="ants",
)

AntsApplyTransformsDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

AntsApplyTransformsInputImageType = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4] | typing.Literal[5]

AntsApplyTransformsWarpedOutput = typing.TypedDict(
    "AntsApplyTransformsWarpedOutput",
    {
        "@type": typing.Literal["warpedOutput"],
        "warpedOutputFileName": str,
    },
)

AntsApplyTransformsCompositeDisplacementFieldOutput = typing.TypedDict(
    "AntsApplyTransformsCompositeDisplacementFieldOutput",
    {
        "@type": typing.Literal["compositeDisplacementFieldOutput"],
        "compositeDisplacementField": str,
        "printOutCompositeWarpFile": typing.NotRequired[bool],
    },
)

AntsApplyTransformsGenericAffineTransformOutput = typing.TypedDict(
    "AntsApplyTransformsGenericAffineTransformOutput",
    {
        "@type": typing.Literal["genericAffineTransformOutput"],
        "genericAffineTransformFile": str,
        "calculateInverse": typing.NotRequired[bool],
    },
)

AntsApplyTransformsOutput = AntsApplyTransformsWarpedOutput | AntsApplyTransformsCompositeDisplacementFieldOutput | AntsApplyTransformsGenericAffineTransformOutput

AntsApplyTransformsSigma = typing.TypedDict(
    "AntsApplyTransformsSigma",
    {
        "@type": typing.Literal["sigma"],
        "sigma": float,
    },
)

AntsApplyTransformsAlpha = typing.TypedDict(
    "AntsApplyTransformsAlpha",
    {
        "@type": typing.Literal["alpha"],
        "alpha": float,
    },
)

AntsApplyTransformsParams = AntsApplyTransformsSigma | AntsApplyTransformsAlpha

AntsApplyTransformsMultiLabel = typing.TypedDict(
    "AntsApplyTransformsMultiLabel",
    {
        "@type": typing.Literal["multiLabel"],
        "params": list[AntsApplyTransformsParams],
    },
)

AntsApplyTransformsGaussian = typing.TypedDict(
    "AntsApplyTransformsGaussian",
    {
        "@type": typing.Literal["gaussian"],
        "sigma": float,
        "alpha": typing.NotRequired[float],
    },
)

AntsApplyTransformsBspline = typing.TypedDict(
    "AntsApplyTransformsBspline",
    {
        "@type": typing.Literal["bspline"],
        "order": int,
    },
)

AntsApplyTransformsGenericLabel = typing.TypedDict(
    "AntsApplyTransformsGenericLabel",
    {
        "@type": typing.Literal["genericLabel"],
        "interpolator": str,
    },
)

AntsApplyTransformsInterpolation = typing.Literal["Linear"] | typing.Literal["NearestNeighbor"] | typing.Literal["MultiLabel"] | AntsApplyTransformsMultiLabel | typing.Literal["Gaussian"] | AntsApplyTransformsGaussian | typing.Literal["BSpline"] | AntsApplyTransformsBspline | typing.Literal["CosineWindowedSinc"] | typing.Literal["WelchWindowedSinc"] | typing.Literal["HammingWindowedSinc"] | typing.Literal["LanczosWindowedSinc"] | typing.Literal["GenericLabel"] | AntsApplyTransformsGenericLabel

AntsApplyTransformsOutputDataType = typing.Literal["char"] | typing.Literal["uchar"] | typing.Literal["short"] | typing.Literal["int"] | typing.Literal["float"] | typing.Literal["double"] | typing.Literal["default"]

AntsApplyTransformsTransformFileName = typing.TypedDict(
    "AntsApplyTransformsTransformFileName",
    {
        "@type": typing.Literal["transformFileName"],
        "transformFileName": InputPathType,
    },
)

AntsApplyTransformsUseInverse = typing.TypedDict(
    "AntsApplyTransformsUseInverse",
    {
        "@type": typing.Literal["useInverse"],
        "transformFileName": InputPathType,
    },
)

AntsApplyTransformsTransform = AntsApplyTransformsTransformFileName | AntsApplyTransformsUseInverse

AntsApplyTransforms = typing.TypedDict(
    "AntsApplyTransforms",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsApplyTransforms"]],
        "dimensionality": typing.NotRequired[AntsApplyTransformsDimensionality],
        "input_image_type": typing.NotRequired[AntsApplyTransformsInputImageType],
        "input_image": typing.NotRequired[InputPathType],
        "reference_image": InputPathType,
        "output": AntsApplyTransformsOutput,
        "interpolation": typing.NotRequired[AntsApplyTransformsInterpolation],
        "output_data_type": typing.NotRequired[AntsApplyTransformsOutputDataType],
        "transform": typing.NotRequired[list[AntsApplyTransformsTransform]],
        "default_value": typing.NotRequired[float],
        "static_cast_for_R": typing.NotRequired[str],
        "float": typing.NotRequired[bool],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsApplyTransformsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_outfile: OutputPathType | None
    """Warped image."""

def ants_apply_transforms_params(
    reference_image: InputPathType,
    output: AntsApplyTransformsOutput,
    dimensionality: AntsApplyTransformsDimensionality | None = None,
    input_image_type: AntsApplyTransformsInputImageType | None = None,
    input_image: InputPathType | None = None,
    interpolation: AntsApplyTransformsInterpolation | None = None,
    output_data_type: AntsApplyTransformsOutputDataType | None = None,
    transform: list[AntsApplyTransformsTransform] | None = None,
    default_value: float | None = None,
    static_cast_for_R: str | None = None,
    float_: bool | None = None,
    verbose: bool | None = None,
) -> AntsApplyTransforms:
    """
    Build parameters.

    Args:
        reference_image: For warping input images, the reference image defines the\
            spacing, origin, size, and direction of the output warped image.
        output: One can either output the warped image or, if the boolean is set,\
            one can print out the displacement field based on the composite\
            transform and the reference image. A third option is to compose all\
            affine transforms and (if boolean is set) calculate its inverse which\
            is then written to an ITK file.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. if not specified, antswarp tries to infer\
            the dimensionality from the input image.
        input_image_type: Option specifying the input image type of scalar\
            (default), vector, tensor, time series, or multi-channel. A time series\
            image is a scalar image defined by an additional dimension for the time\
            component whereas a multi-channel image is a vector image with only\
            spatial dimensions. Five-dimensional images are e.g., AFNI stats image.
        input_image: Currently, the only input objects supported are image objects.\
            However, the current framework allows for warping of other objects such\
            as meshes and point sets.
        interpolation: Several interpolation options are available in ITK. These\
            have all been made available.
        output_data_type: Output image data type. This is a direct typecast; output\
            values are not rescaled. Default is to use the internal data type\
            (float or double). uchar is unsigned char; others are signed. WARNING:\
            Outputs will be incorrect (overflowed/reinterpreted) if values exceed\
            the range allowed by your choice. Note that some pixel types are not\
            supported by some image formats. e.g. int is not supported by jpg.
        transform: Several transform options are supported including all those\
            defined in the ITK library in addition to a deformation field\
            transform. The ordering of the transformations follows the ordering\
            specified on the command line. An identity transform is pushed onto the\
            transformation stack. Each new transform encountered on the command\
            line is also pushed onto the transformation stack. Then, to warp the\
            input object, each point comprising the input object is warped first\
            according to the last transform pushed onto the stack followed by the\
            second to last transform, etc. until the last transform encountered\
            which is the identity transform. Also, it should be noted that the\
            inverse transform can be accommodated with the usual caveat that such\
            an inverse must be defined by the specified transform class.
        default_value: Default voxel value to be used with input images only.\
            Specifies the voxel value when the input point maps outside the output\
            domain. With tensor input images, specifies the default voxel\
            eigenvalues.
        static_cast_for_R: Forces static cast in ReadTransform (for R).
        float_: Use float instead of double for computations.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: AntsApplyTransforms = {
        "@type": "ants/antsApplyTransforms",
        "reference_image": reference_image,
        "output": output,
    }
    if dimensionality is not None:
        params["dimensionality"] = dimensionality
    if input_image_type is not None:
        params["input_image_type"] = input_image_type
    if input_image is not None:
        params["input_image"] = input_image
    if interpolation is not None:
        params["interpolation"] = interpolation
    if output_data_type is not None:
        params["output_data_type"] = output_data_type
    if transform is not None:
        params["transform"] = transform
    if default_value is not None:
        params["default_value"] = default_value
    if static_cast_for_R is not None:
        params["static_cast_for_R"] = static_cast_for_R
    if float_ is not None:
        params["float"] = float_
    if verbose is not None:
        params["verbose"] = verbose
    return params

def ants_apply_transforms_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsApplyTransforms."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensionality", None) is not None:
        if not isinstance(params["dimensionality"], (float, int)):
            raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `AntsApplyTransformsDimensionality`')
        if params["dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `dimensionality` must be one of [2, 3, 4]")
    if params.get("input_image_type", None) is not None:
        if not isinstance(params["input_image_type"], (float, int)):
            raise StyxValidationError(f'`input_image_type` has the wrong type: Received `{type(params["input_image_type"])}` expected `AntsApplyTransformsInputImageType`')
        if params["input_image_type"] not in [0, 1, 2, 3, 4, 5]:
            raise StyxValidationError("Parameter `input_image_type` must be one of [0, 1, 2, 3, 4, 5]")
    if params.get("input_image", None) is not None:
        if not isinstance(params["input_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["output"])}\'')
    if "@type" not in params["output"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["output"]["@type"] not in ["warpedOutput", "compositeDisplacementFieldOutput", "genericAffineTransformOutput"]:
        raise StyxValidationError("Parameter `output`s `@type` must be one of [\"warpedOutput\", \"compositeDisplacementFieldOutput\", \"genericAffineTransformOutput\"]")
    if params["output"]["@type"] == "warpedOutput":
        if params["output"].get("warpedOutputFileName", None) is None:
            raise StyxValidationError("`warpedOutputFileName` must not be None")
        if not isinstance(params["output"]["warpedOutputFileName"], str):
            raise StyxValidationError(f'`warpedOutputFileName` has the wrong type: Received `{type(params["output"]["warpedOutputFileName"])}` expected `str`')
    elif params["output"]["@type"] == "compositeDisplacementFieldOutput":
        if params["output"].get("compositeDisplacementField", None) is None:
            raise StyxValidationError("`compositeDisplacementField` must not be None")
        if not isinstance(params["output"]["compositeDisplacementField"], str):
            raise StyxValidationError(f'`compositeDisplacementField` has the wrong type: Received `{type(params["output"]["compositeDisplacementField"])}` expected `str`')
        if params["output"].get("printOutCompositeWarpFile", None) is not None:
            if not isinstance(params["output"]["printOutCompositeWarpFile"], bool):
                raise StyxValidationError(f'`printOutCompositeWarpFile` has the wrong type: Received `{type(params["output"]["printOutCompositeWarpFile"])}` expected `bool`')
    elif params["output"]["@type"] == "genericAffineTransformOutput":
        if params["output"].get("genericAffineTransformFile", None) is None:
            raise StyxValidationError("`genericAffineTransformFile` must not be None")
        if not isinstance(params["output"]["genericAffineTransformFile"], str):
            raise StyxValidationError(f'`genericAffineTransformFile` has the wrong type: Received `{type(params["output"]["genericAffineTransformFile"])}` expected `str`')
        if params["output"].get("calculateInverse", None) is not None:
            if not isinstance(params["output"]["calculateInverse"], bool):
                raise StyxValidationError(f'`calculateInverse` has the wrong type: Received `{type(params["output"]["calculateInverse"])}` expected `bool`')
    if params.get("interpolation", None) is not None:
        if isinstance(params["interpolation"], dict):
            if "@type" not in params["interpolation"]:
                raise StyxValidationError("Params object is missing `@type`")
            if params["interpolation"]["@type"] not in ["multiLabel", "gaussian", "bspline", "genericLabel"]:
                raise StyxValidationError("Parameter `interpolation`s `@type` must be one of [\"multiLabel\", \"gaussian\", \"bspline\", \"genericLabel\"]")
            if params["interpolation"]["@type"] == "multiLabel":
                if params["interpolation"].get("params", None) is None:
                    raise StyxValidationError("`params` must not be None")
                if not isinstance(params["interpolation"]["params"], list):
                    raise StyxValidationError(f'`params` has the wrong type: Received `{type(params["interpolation"]["params"])}` expected `list[AntsApplyTransformsParams]`')
                for e in params["interpolation"]["params"]:
                    if not isinstance(e, dict):
                        raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
                    if "@type" not in e:
                        raise StyxValidationError("Params object is missing `@type`")
                    if e["@type"] not in ["sigma", "alpha"]:
                        raise StyxValidationError("Parameter `params`s `@type` must be one of [\"sigma\", \"alpha\"]")
                    if e["@type"] == "sigma":
                        if e.get("sigma", None) is None:
                            raise StyxValidationError("`sigma` must not be None")
                        if not isinstance(e["sigma"], (float, int)):
                            raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(e["sigma"])}` expected `float`')
                    elif e["@type"] == "alpha":
                        if e.get("alpha", None) is None:
                            raise StyxValidationError("`alpha` must not be None")
                        if not isinstance(e["alpha"], (float, int)):
                            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(e["alpha"])}` expected `float`')
            elif params["interpolation"]["@type"] == "gaussian":
                if params["interpolation"].get("sigma", None) is None:
                    raise StyxValidationError("`sigma` must not be None")
                if not isinstance(params["interpolation"]["sigma"], (float, int)):
                    raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["interpolation"]["sigma"])}` expected `float`')
                if params["interpolation"].get("alpha", None) is not None:
                    if not isinstance(params["interpolation"]["alpha"], (float, int)):
                        raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["interpolation"]["alpha"])}` expected `float`')
            elif params["interpolation"]["@type"] == "bspline":
                if params["interpolation"].get("order", None) is None:
                    raise StyxValidationError("`order` must not be None")
                if not isinstance(params["interpolation"]["order"], int):
                    raise StyxValidationError(f'`order` has the wrong type: Received `{type(params["interpolation"]["order"])}` expected `int`')
            elif params["interpolation"]["@type"] == "genericLabel":
                if params["interpolation"].get("interpolator", None) is None:
                    raise StyxValidationError("`interpolator` must not be None")
                if not isinstance(params["interpolation"]["interpolator"], str):
                    raise StyxValidationError(f'`interpolator` has the wrong type: Received `{type(params["interpolation"]["interpolator"])}` expected `str`')
        else:
            if params["interpolation"] not in ["Linear", "NearestNeighbor", "MultiLabel", "Gaussian", "BSpline", "CosineWindowedSinc", "WelchWindowedSinc", "HammingWindowedSinc", "LanczosWindowedSinc", "GenericLabel"]:
                raise StyxValidationError("Parameter `interpolation` must be one of [\"Linear\", \"NearestNeighbor\", \"MultiLabel\", \"Gaussian\", \"BSpline\", \"CosineWindowedSinc\", \"WelchWindowedSinc\", \"HammingWindowedSinc\", \"LanczosWindowedSinc\", \"GenericLabel\"]")
    if params.get("output_data_type", None) is not None:
        if not isinstance(params["output_data_type"], str):
            raise StyxValidationError(f'`output_data_type` has the wrong type: Received `{type(params["output_data_type"])}` expected `AntsApplyTransformsOutputDataType`')
        if params["output_data_type"] not in ["char", "uchar", "short", "int", "float", "double", "default"]:
            raise StyxValidationError("Parameter `output_data_type` must be one of [\"char\", \"uchar\", \"short\", \"int\", \"float\", \"double\", \"default\"]")
    if params.get("transform", None) is not None:
        if not isinstance(params["transform"], list):
            raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `list[AntsApplyTransformsTransform]`')
        for e_2 in params["transform"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if "@type" not in e_2:
                raise StyxValidationError("Params object is missing `@type`")
            if e_2["@type"] not in ["transformFileName", "useInverse"]:
                raise StyxValidationError("Parameter `transform`s `@type` must be one of [\"transformFileName\", \"useInverse\"]")
            if e_2["@type"] == "transformFileName":
                if e_2.get("transformFileName", None) is None:
                    raise StyxValidationError("`transformFileName` must not be None")
                if not isinstance(e_2["transformFileName"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`transformFileName` has the wrong type: Received `{type(e_2["transformFileName"])}` expected `InputPathType`')
            elif e_2["@type"] == "useInverse":
                if e_2.get("transformFileName", None) is None:
                    raise StyxValidationError("`transformFileName` must not be None")
                if not isinstance(e_2["transformFileName"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`transformFileName` has the wrong type: Received `{type(e_2["transformFileName"])}` expected `InputPathType`')
    if params.get("default_value", None) is not None:
        if not isinstance(params["default_value"], (float, int)):
            raise StyxValidationError(f'`default_value` has the wrong type: Received `{type(params["default_value"])}` expected `float`')
    if params.get("static_cast_for_R", None) is not None:
        if not isinstance(params["static_cast_for_R"], str):
            raise StyxValidationError(f'`static_cast_for_R` has the wrong type: Received `{type(params["static_cast_for_R"])}` expected `str`')
    if params.get("float", None) is not None:
        if not isinstance(params["float"], bool):
            raise StyxValidationError(f'`float` has the wrong type: Received `{type(params["float"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def ants_apply_transforms_cargs(params: AntsApplyTransforms, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsApplyTransforms")
    v_0 = params.get("dimensionality")
    if v_0 is not None:
        cargs.append("--dimensionality")
        cargs.append(str(v_0))
    v_1 = params.get("input_image_type")
    if v_1 is not None:
        cargs.append("--input-image-type")
        cargs.append(str(v_1))
    v_2 = params.get("input_image")
    if v_2 is not None:
        cargs.append("--input")
        cargs.append(execution.input_file(v_2))
    cargs.append("--reference-image")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("--output")
    if params["output"]["@type"] == "warpedOutput":
        cargs.append(str(params["output"]["warpedOutputFileName"]))
    elif params["output"]["@type"] == "compositeDisplacementFieldOutput":
        cargs.append("".join(["[", params["output"]["compositeDisplacementField"], ("".join([",", ("0" if v_3 else "1")]) if (v_3 := params["output"].get("printOutCompositeWarpFile")) is not None else ""), "]"]))
    elif params["output"]["@type"] == "genericAffineTransformOutput":
        cargs.append("".join(["Linear[", params["output"]["genericAffineTransformFile"], ("".join([",", ("0" if v_4 else "1")]) if (v_4 := params["output"].get("calculateInverse")) is not None else ""), "]"]))
    v_5 = params.get("interpolation")
    if v_5 is not None:
        cargs.append("--interpolation")
        if isinstance(v_5, dict):
            if v_5["@type"] == "multiLabel":
                cargs.append("".join(["MultiLabel", "".join(["[", ",".join([(str(item0["sigma"]) if item0["@type"] == "sigma" else (str(item0["alpha"]) if item0["@type"] == "alpha" else "")) for item0 in v_5["params"]]), "]"])]))
            elif v_5["@type"] == "gaussian":
                cargs.append("".join(["Gaussian[", str(v_5["sigma"]), ("".join([",", str(v_6)]) if (v_6 := v_5.get("alpha")) is not None else ""), "]"]))
            elif v_5["@type"] == "bspline":
                cargs.append("".join(["BSpline[", str(v_5["order"]), "]"]))
            elif v_5["@type"] == "genericLabel":
                cargs.append("".join(["GenericLabel[", v_5["interpolator"], "]"]))
        else:
            cargs.append(str(v_5))
    v_7 = params.get("output_data_type")
    if v_7 is not None:
        cargs.append("--output-data-type")
        cargs.append(str(v_7))
    v_8 = params.get("transform")
    if v_8 is not None:
        for item1 in v_8:
            if item1["@type"] == "transformFileName":
                cargs.append("--transform")
                cargs.append(execution.input_file(item1["transformFileName"]))
            elif item1["@type"] == "useInverse":
                cargs.append("--transform")
                cargs.append("".join(["[", execution.input_file(item1["transformFileName"]), ",1]"]))
    v_9 = params.get("default_value")
    if v_9 is not None:
        cargs.append("--default-value")
        cargs.append(str(v_9))
    v_10 = params.get("static_cast_for_R")
    if v_10 is not None:
        cargs.append("--static-cast-for-R")
        cargs.append(v_10)
    v_11 = params.get("float")
    if v_11 is not None:
        cargs.append("--float")
        if v_11:
            cargs.append("0")
        else:
            cargs.append("1")
    v_12 = params.get("verbose")
    if v_12 is not None:
        cargs.append("--verbose")
        if v_12:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def ants_apply_transforms_outputs(params: AntsApplyTransforms, execution: Execution) -> AntsApplyTransformsOutputs:
    """Build the AntsApplyTransformsOutputs object for this tool."""
    output_image_outfile_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    if params["output"]["@type"] == "warpedOutput":
        output_image_outfile_v = execution.output_file(params["output"]["warpedOutputFileName"])
    if params["output"]["@type"] == "compositeDisplacementFieldOutput":
        output_image_outfile_v = execution.output_file(params["output"]["compositeDisplacementField"])
    if params["output"]["@type"] == "genericAffineTransformOutput":
        output_image_outfile_v = execution.output_file(params["output"]["genericAffineTransformFile"])
    return AntsApplyTransformsOutputs(
        root=root_v,
        output_image_outfile=output_image_outfile_v,
    )

def ants_apply_transforms_execute(params: AntsApplyTransforms, runner: Runner | None = None) -> AntsApplyTransformsOutputs:
    """
    antsApplyTransforms

    antsApplyTransforms, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms).

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_apply_transforms_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_APPLY_TRANSFORMS_METADATA)
    execution.params(params)
    args = ants_apply_transforms_cargs(params, execution)
    out = ants_apply_transforms_outputs(params, execution)
    execution.run(args)
    return out

def ants_apply_transforms(
    reference_image: InputPathType,
    output: AntsApplyTransformsOutput,
    dimensionality: AntsApplyTransformsDimensionality | None = None,
    input_image_type: AntsApplyTransformsInputImageType | None = None,
    input_image: InputPathType | None = None,
    interpolation: AntsApplyTransformsInterpolation | None = None,
    output_data_type: AntsApplyTransformsOutputDataType | None = None,
    transform: list[AntsApplyTransformsTransform] | None = None,
    default_value: float | None = None,
    static_cast_for_R: str | None = None,
    float_: bool | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AntsApplyTransformsOutputs:
    """
    antsApplyTransforms

    antsApplyTransforms, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms).

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        reference_image: For warping input images, the reference image defines the\
            spacing, origin, size, and direction of the output warped image.
        output: One can either output the warped image or, if the boolean is set,\
            one can print out the displacement field based on the composite\
            transform and the reference image. A third option is to compose all\
            affine transforms and (if boolean is set) calculate its inverse which\
            is then written to an ITK file.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. if not specified, antswarp tries to infer\
            the dimensionality from the input image.
        input_image_type: Option specifying the input image type of scalar\
            (default), vector, tensor, time series, or multi-channel. A time series\
            image is a scalar image defined by an additional dimension for the time\
            component whereas a multi-channel image is a vector image with only\
            spatial dimensions. Five-dimensional images are e.g., AFNI stats image.
        input_image: Currently, the only input objects supported are image objects.\
            However, the current framework allows for warping of other objects such\
            as meshes and point sets.
        interpolation: Several interpolation options are available in ITK. These\
            have all been made available.
        output_data_type: Output image data type. This is a direct typecast; output\
            values are not rescaled. Default is to use the internal data type\
            (float or double). uchar is unsigned char; others are signed. WARNING:\
            Outputs will be incorrect (overflowed/reinterpreted) if values exceed\
            the range allowed by your choice. Note that some pixel types are not\
            supported by some image formats. e.g. int is not supported by jpg.
        transform: Several transform options are supported including all those\
            defined in the ITK library in addition to a deformation field\
            transform. The ordering of the transformations follows the ordering\
            specified on the command line. An identity transform is pushed onto the\
            transformation stack. Each new transform encountered on the command\
            line is also pushed onto the transformation stack. Then, to warp the\
            input object, each point comprising the input object is warped first\
            according to the last transform pushed onto the stack followed by the\
            second to last transform, etc. until the last transform encountered\
            which is the identity transform. Also, it should be noted that the\
            inverse transform can be accommodated with the usual caveat that such\
            an inverse must be defined by the specified transform class.
        default_value: Default voxel value to be used with input images only.\
            Specifies the voxel value when the input point maps outside the output\
            domain. With tensor input images, specifies the default voxel\
            eigenvalues.
        static_cast_for_R: Forces static cast in ReadTransform (for R).
        float_: Use float instead of double for computations.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_apply_transforms_params(
        reference_image=reference_image,
        output=output,
        dimensionality=dimensionality,
        input_image_type=input_image_type,
        input_image=input_image,
        interpolation=interpolation,
        output_data_type=output_data_type,
        transform=transform,
        default_value=default_value,
        static_cast_for_R=static_cast_for_R,
        float_=float_,
        verbose=verbose,
    )
    return ants_apply_transforms_execute(params, runner)

__all__ = [
    "AntsApplyTransforms",
    "AntsApplyTransformsOutputs",
    "ANTS_APPLY_TRANSFORMS_METADATA",
    "ants_apply_transforms_cargs",
    "ants_apply_transforms_outputs",
    "ants_apply_transforms_params",
    "ants_apply_transforms_execute",
    "ants_apply_transforms_validate",
    "ants_apply_transforms",
]