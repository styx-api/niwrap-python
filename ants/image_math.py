# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAGE_MATH_METADATA = Metadata(
    id="ImageMath",
    name="ImageMath",
    package="ants",
)

ImageMathImageDimension = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

ImageMath = typing.TypedDict(
    "ImageMath",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ImageMath"]],
        "image_dimension": ImageMathImageDimension,
        "output_image": str,
        "operations_and_inputs": str,
        "image1": InputPathType,
        "image2": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class ImageMathOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType
    """The resulting image after processing."""

def image_math_params(
    image_dimension: ImageMathImageDimension,
    output_image: str,
    operations_and_inputs: str,
    image1: InputPathType,
    image2: InputPathType | None = None,
) -> ImageMath:
    """
    Build parameters.

    Args:
        image_dimension: The dimensionality of the image. Use 2 or 3 for spatial\
            images, and 4 for 4D images like time-series data.
        output_image: The output image file resulting from the operations.
        operations_and_inputs: Mathematical operations and inputs to be applied on\
            the images.
        image1: The first input image for the operation.
        image2: The second input image for the operation, if required.

    Returns:
        Parameter dictionary.
    """
    params: ImageMath = {
        "@type": "ants/ImageMath",
        "image_dimension": image_dimension,
        "output_image": output_image,
        "operations_and_inputs": operations_and_inputs,
        "image1": image1,
    }
    if image2 is not None:
        params["image2"] = image2
    return params

def image_math_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ImageMath."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], (float, int)):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `ImageMathImageDimension`')
    if params["image_dimension"] not in [2, 3, 4]:
        raise StyxValidationError("Parameter `image_dimension` must be one of [2, 3, 4]")
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("operations_and_inputs", None) is None:
        raise StyxValidationError("`operations_and_inputs` must not be None")
    if not isinstance(params["operations_and_inputs"], str):
        raise StyxValidationError(f'`operations_and_inputs` has the wrong type: Received `{type(params["operations_and_inputs"])}` expected `str`')
    if params.get("image1", None) is None:
        raise StyxValidationError("`image1` must not be None")
    if not isinstance(params["image1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image1` has the wrong type: Received `{type(params["image1"])}` expected `InputPathType`')
    if params.get("image2", None) is not None:
        if not isinstance(params["image2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`image2` has the wrong type: Received `{type(params["image2"])}` expected `InputPathType`')

def image_math_cargs(params: ImageMath, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ImageMath")
    cargs.append(str(params["image_dimension"]))
    cargs.append(params["output_image"])
    cargs.append(params["operations_and_inputs"])
    cargs.append(execution.input_file(params["image1"]))
    v_0 = params.get("image2")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def image_math_outputs(params: ImageMath, execution: Execution) -> ImageMathOutputs:
    """Build the ImageMathOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_v: OutputPathType = execution.output_file(params["output_image"])
    return ImageMathOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def image_math_execute(params: ImageMath, runner: Runner | None = None) -> ImageMathOutputs:
    """
    ImageMath

    A versatile tool for performing various mathematical and manipulation operations on images.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    image_math_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAGE_MATH_METADATA)
    execution.params(params)
    args = image_math_cargs(params, execution)
    out = image_math_outputs(params, execution)
    execution.run(args)
    return out

def image_math(
    image_dimension: ImageMathImageDimension,
    output_image: str,
    operations_and_inputs: str,
    image1: InputPathType,
    image2: InputPathType | None = None,
    runner: Runner | None = None,
) -> ImageMathOutputs:
    """
    ImageMath

    A versatile tool for performing various mathematical and manipulation operations on images.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: The dimensionality of the image. Use 2 or 3 for spatial\
            images, and 4 for 4D images like time-series data.
        output_image: The output image file resulting from the operations.
        operations_and_inputs: Mathematical operations and inputs to be applied on\
            the images.
        image1: The first input image for the operation.
        image2: The second input image for the operation, if required.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = image_math_params(
        image_dimension=image_dimension,
        output_image=output_image,
        operations_and_inputs=operations_and_inputs,
        image1=image1,
        image2=image2,
    )
    return image_math_execute(params, runner)

__all__ = [
    "ImageMath",
    "ImageMathOutputs",
    "IMAGE_MATH_METADATA",
    "image_math_cargs",
    "image_math_outputs",
    "image_math_params",
    "image_math_execute",
    "image_math_validate",
    "image_math",
]