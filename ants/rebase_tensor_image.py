# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

REBASE_TENSOR_IMAGE_METADATA = Metadata(
    id="RebaseTensorImage",
    name="RebaseTensorImage",
    package="ants",
)

RebaseTensorImageMethod = typing.Literal["PHYSICAL"] | typing.Literal["LOCAL"]

RebaseTensorImage = typing.TypedDict(
    "RebaseTensorImage",
    {
        "@type": typing.NotRequired[typing.Literal["ants/RebaseTensorImage"]],
        "dimension": int,
        "infile": InputPathType,
        "outfile": InputPathType,
        "method": RebaseTensorImageMethod,
        "reference": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class RebaseTensorImageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    rebased_image: OutputPathType
    """The rebased tensor image."""

def rebase_tensor_image_params(
    dimension: int,
    infile: InputPathType,
    outfile: InputPathType,
    method: RebaseTensorImageMethod,
    reference: InputPathType | None = None,
) -> RebaseTensorImage:
    """
    Build parameters.

    Args:
        dimension: The dimensionality of the input image.
        infile: The input image file.
        outfile: The output image file.
        method: Method of rebasing the tensor image.
        reference: Reference image file (required if PHYSICAL or LOCAL method is\
            chosen).

    Returns:
        Parameter dictionary.
    """
    params: RebaseTensorImage = {
        "@type": "ants/RebaseTensorImage",
        "dimension": dimension,
        "infile": infile,
        "outfile": outfile,
        "method": method,
    }
    if reference is not None:
        params["reference"] = reference
    return params

def rebase_tensor_image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid RebaseTensorImage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimension", None) is None:
        raise StyxValidationError("`dimension` must not be None")
    if not isinstance(params["dimension"], int):
        raise StyxValidationError(f'`dimension` has the wrong type: Received `{type(params["dimension"])}` expected `int`')
    if params.get("infile", None) is None:
        raise StyxValidationError("`infile` must not be None")
    if not isinstance(params["infile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`infile` has the wrong type: Received `{type(params["infile"])}` expected `InputPathType`')
    if params.get("outfile", None) is None:
        raise StyxValidationError("`outfile` must not be None")
    if not isinstance(params["outfile"], (pathlib.Path, str)):
        raise StyxValidationError(f'`outfile` has the wrong type: Received `{type(params["outfile"])}` expected `InputPathType`')
    if params.get("method", None) is None:
        raise StyxValidationError("`method` must not be None")
    if not isinstance(params["method"], str):
        raise StyxValidationError(f'`method` has the wrong type: Received `{type(params["method"])}` expected `RebaseTensorImageMethod`')
    if params["method"] not in ["PHYSICAL", "LOCAL"]:
        raise StyxValidationError("Parameter `method` must be one of [\"PHYSICAL\", \"LOCAL\"]")
    if params.get("reference", None) is not None:
        if not isinstance(params["reference"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reference` has the wrong type: Received `{type(params["reference"])}` expected `InputPathType`')

def rebase_tensor_image_cargs(params: RebaseTensorImage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("RebaseTensorImage")
    cargs.append(str(params["dimension"]))
    cargs.append(execution.input_file(params["infile"]))
    cargs.append(execution.input_file(params["outfile"]))
    cargs.append(str(params["method"]))
    v_0 = params.get("reference")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def rebase_tensor_image_outputs(params: RebaseTensorImage, execution: Execution) -> RebaseTensorImageOutputs:
    """Build the RebaseTensorImageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    rebased_image_v: OutputPathType = execution.output_file(params["outfile"])
    return RebaseTensorImageOutputs(
        root=root_v,
        rebased_image=rebased_image_v,
    )

def rebase_tensor_image_execute(params: RebaseTensorImage, runner: Runner | None = None) -> RebaseTensorImageOutputs:
    """
    RebaseTensorImage

    Rebase Tensor Image using specified dimensionality and method.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    rebase_tensor_image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(REBASE_TENSOR_IMAGE_METADATA)
    execution.params(params)
    args = rebase_tensor_image_cargs(params, execution)
    out = rebase_tensor_image_outputs(params, execution)
    execution.run(args)
    return out

def rebase_tensor_image(
    dimension: int,
    infile: InputPathType,
    outfile: InputPathType,
    method: RebaseTensorImageMethod,
    reference: InputPathType | None = None,
    runner: Runner | None = None,
) -> RebaseTensorImageOutputs:
    """
    RebaseTensorImage

    Rebase Tensor Image using specified dimensionality and method.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimension: The dimensionality of the input image.
        infile: The input image file.
        outfile: The output image file.
        method: Method of rebasing the tensor image.
        reference: Reference image file (required if PHYSICAL or LOCAL method is\
            chosen).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = rebase_tensor_image_params(
        dimension=dimension,
        infile=infile,
        outfile=outfile,
        method=method,
        reference=reference,
    )
    return rebase_tensor_image_execute(params, runner)

__all__ = [
    "RebaseTensorImage",
    "RebaseTensorImageOutputs",
    "REBASE_TENSOR_IMAGE_METADATA",
    "rebase_tensor_image_cargs",
    "rebase_tensor_image_outputs",
    "rebase_tensor_image_params",
    "rebase_tensor_image_execute",
    "rebase_tensor_image_validate",
    "rebase_tensor_image",
]