# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_INTEGRATE_VECTOR_FIELD_METADATA = Metadata(
    id="ANTSIntegrateVectorField",
    name="ANTSIntegrateVectorField",
    package="ants",
)

AntsIntegrateVectorField = typing.TypedDict(
    "AntsIntegrateVectorField",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ANTSIntegrateVectorField"]],
        "vector_field_input": InputPathType,
        "roi_mask_input": InputPathType,
        "fibers_output": str,
        "length_image_output": str,
    },
)

@dataclasses.dataclass
class AntsIntegrateVectorFieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fibers_out_vtk: OutputPathType
    """The output is the fibers as a VTK text file."""
    length_image_out_nii: OutputPathType
    """The output is the length image."""

def ants_integrate_vector_field_params(
    vector_field_input: InputPathType,
    roi_mask_input: InputPathType,
    fibers_output: str,
    length_image_output: str,
) -> AntsIntegrateVectorField:
    """
    Build parameters.

    Args:
        vector_field_input: Input vector field image (e.g., VecImageIN.nii.gz),\
            where vectors are voxels.
        roi_mask_input: Input ROI mask image (e.g., ROIMaskIN.nii.gz), an integer\
            image controlling where the integration is performed.
        fibers_output: Output VTK text file for fibers (e.g., FibersOUT.vtk).
        length_image_output: Output length image (e.g., LengthImageOUT.nii.gz).

    Returns:
        Parameter dictionary.
    """
    params: AntsIntegrateVectorField = {
        "@type": "ants/ANTSIntegrateVectorField",
        "vector_field_input": vector_field_input,
        "roi_mask_input": roi_mask_input,
        "fibers_output": fibers_output,
        "length_image_output": length_image_output,
    }
    return params

def ants_integrate_vector_field_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsIntegrateVectorField."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("vector_field_input", None) is None:
        raise StyxValidationError("`vector_field_input` must not be None")
    if not isinstance(params["vector_field_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`vector_field_input` has the wrong type: Received `{type(params["vector_field_input"])}` expected `InputPathType`')
    if params.get("roi_mask_input", None) is None:
        raise StyxValidationError("`roi_mask_input` must not be None")
    if not isinstance(params["roi_mask_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`roi_mask_input` has the wrong type: Received `{type(params["roi_mask_input"])}` expected `InputPathType`')
    if params.get("fibers_output", None) is None:
        raise StyxValidationError("`fibers_output` must not be None")
    if not isinstance(params["fibers_output"], str):
        raise StyxValidationError(f'`fibers_output` has the wrong type: Received `{type(params["fibers_output"])}` expected `str`')
    if params.get("length_image_output", None) is None:
        raise StyxValidationError("`length_image_output` must not be None")
    if not isinstance(params["length_image_output"], str):
        raise StyxValidationError(f'`length_image_output` has the wrong type: Received `{type(params["length_image_output"])}` expected `str`')

def ants_integrate_vector_field_cargs(params: AntsIntegrateVectorField, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ANTSIntegrateVectorField")
    cargs.append(execution.input_file(params["vector_field_input"]))
    cargs.append(execution.input_file(params["roi_mask_input"]))
    cargs.append(params["fibers_output"])
    cargs.append(params["length_image_output"])
    return cargs

def ants_integrate_vector_field_outputs(params: AntsIntegrateVectorField, execution: Execution) -> AntsIntegrateVectorFieldOutputs:
    """Build the AntsIntegrateVectorFieldOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fibers_out_vtk_v: OutputPathType = execution.output_file(params["fibers_output"])
    length_image_out_nii_v: OutputPathType = execution.output_file(params["length_image_output"])
    return AntsIntegrateVectorFieldOutputs(
        root=root_v,
        fibers_out_vtk=fibers_out_vtk_v,
        length_image_out_nii=length_image_out_nii_v,
    )

def ants_integrate_vector_field_execute(params: AntsIntegrateVectorField, runner: Runner | None = None) -> AntsIntegrateVectorFieldOutputs:
    """
    ANTSIntegrateVectorField

    This tool integrates a vector field, where vectors are voxels, using a region of interest (ROI) mask. The ROI mask controls where the integration is performed and specifies the starting point region.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_integrate_vector_field_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_INTEGRATE_VECTOR_FIELD_METADATA)
    execution.params(params)
    args = ants_integrate_vector_field_cargs(params, execution)
    out = ants_integrate_vector_field_outputs(params, execution)
    execution.run(args)
    return out

def ants_integrate_vector_field(
    vector_field_input: InputPathType,
    roi_mask_input: InputPathType,
    fibers_output: str,
    length_image_output: str,
    runner: Runner | None = None,
) -> AntsIntegrateVectorFieldOutputs:
    """
    ANTSIntegrateVectorField

    This tool integrates a vector field, where vectors are voxels, using a region of interest (ROI) mask. The ROI mask controls where the integration is performed and specifies the starting point region.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        vector_field_input: Input vector field image (e.g., VecImageIN.nii.gz),\
            where vectors are voxels.
        roi_mask_input: Input ROI mask image (e.g., ROIMaskIN.nii.gz), an integer\
            image controlling where the integration is performed.
        fibers_output: Output VTK text file for fibers (e.g., FibersOUT.vtk).
        length_image_output: Output length image (e.g., LengthImageOUT.nii.gz).
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_integrate_vector_field_params(
        vector_field_input=vector_field_input,
        roi_mask_input=roi_mask_input,
        fibers_output=fibers_output,
        length_image_output=length_image_output,
    )
    return ants_integrate_vector_field_execute(params, runner)

__all__ = [
    "AntsIntegrateVectorField",
    "AntsIntegrateVectorFieldOutputs",
    "ANTS_INTEGRATE_VECTOR_FIELD_METADATA",
    "ants_integrate_vector_field_cargs",
    "ants_integrate_vector_field_outputs",
    "ants_integrate_vector_field_params",
    "ants_integrate_vector_field_execute",
    "ants_integrate_vector_field_validate",
    "ants_integrate_vector_field",
]