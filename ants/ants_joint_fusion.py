# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_JOINT_FUSION_METADATA = Metadata(
    id="antsJointFusion",
    name="antsJointFusion",
    package="ants",
)

AntsJointFusionImageDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

AntsJointFusionPatchMetric = typing.Literal["PC"] | typing.Literal["MSQ"]

AntsJointFusion = typing.TypedDict(
    "AntsJointFusion",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsJointFusion"]],
        "image_dimensionality": typing.NotRequired[AntsJointFusionImageDimensionality],
        "target_image": list[InputPathType],
        "atlas_image": list[InputPathType],
        "atlas_segmentation": InputPathType,
        "alpha": typing.NotRequired[float],
        "beta": typing.NotRequired[float],
        "constrain_nonnegative": typing.NotRequired[bool],
        "patch_radius": typing.NotRequired[str],
        "patch_metric": typing.NotRequired[AntsJointFusionPatchMetric],
        "search_radius": typing.NotRequired[str],
        "exclusion_image": typing.NotRequired[InputPathType],
        "mask_image": typing.NotRequired[InputPathType],
        "output": str,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsJointFusionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_fusion_image: OutputPathType
    """The output label fusion image."""
    intensity_fusion_image: OutputPathType
    """The output intensity fusion image format."""
    label_posterior_probability_image: OutputPathType
    """The output label posterior probability image format."""
    atlas_voting_weight_image: OutputPathType
    """The output atlas voting weight image format."""

def ants_joint_fusion_params(
    target_image: list[InputPathType],
    atlas_image: list[InputPathType],
    atlas_segmentation: InputPathType,
    output: str,
    image_dimensionality: AntsJointFusionImageDimensionality | None = None,
    alpha: float | None = None,
    beta: float | None = None,
    constrain_nonnegative: bool | None = None,
    patch_radius: str | None = None,
    patch_metric: AntsJointFusionPatchMetric | None = None,
    search_radius: str | None = None,
    exclusion_image: InputPathType | None = None,
    mask_image: InputPathType | None = None,
    verbose: bool | None = None,
) -> AntsJointFusion:
    """
    Build parameters.

    Args:
        target_image: The target image (or multimodal target images) assumed to be\
            aligned to a common image domain.
        atlas_image: The atlas image (or multimodal atlas images) assumed to be\
            aligned to a common image domain.
        atlas_segmentation: The atlas segmentation images. For performing label\
            fusion the number of specified segmentations should be identical to the\
            number of atlas image sets.
        output: The output is the intensity and/or label fusion image. Additional\
            optional outputs include the label posterior probability images and the\
            atlas voting weight images.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        alpha: Regularization term added to matrix Mx for calculating the inverse.\
            Default = 0.1.
        beta: Exponent for mapping intensity difference to the joint error. Default\
            = 2.0.
        constrain_nonnegative: Constrain solution to non-negative weights.
        patch_radius: Patch radius for similarity measures. Default = 2x2x2.
        patch_metric: Metric to be used in determining the most similar\
            neighborhood patch. Options include Pearson's correlation (PC) and mean\
            squares (MSQ). Default = PC (Pearson correlation).
        search_radius: Search radius for similarity measures. Default = 3x3x3. One\
            can also specify an image where the value at the voxel specifies the\
            isotropic search radius at that voxel.
        exclusion_image: Specify an exclusion region for the given label.
        mask_image: If a mask image is specified, fusion is only performed in the\
            mask region.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: AntsJointFusion = {
        "@type": "ants/antsJointFusion",
        "target_image": target_image,
        "atlas_image": atlas_image,
        "atlas_segmentation": atlas_segmentation,
        "output": output,
    }
    if image_dimensionality is not None:
        params["image_dimensionality"] = image_dimensionality
    if alpha is not None:
        params["alpha"] = alpha
    if beta is not None:
        params["beta"] = beta
    if constrain_nonnegative is not None:
        params["constrain_nonnegative"] = constrain_nonnegative
    if patch_radius is not None:
        params["patch_radius"] = patch_radius
    if patch_metric is not None:
        params["patch_metric"] = patch_metric
    if search_radius is not None:
        params["search_radius"] = search_radius
    if exclusion_image is not None:
        params["exclusion_image"] = exclusion_image
    if mask_image is not None:
        params["mask_image"] = mask_image
    if verbose is not None:
        params["verbose"] = verbose
    return params

def ants_joint_fusion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsJointFusion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimensionality", None) is not None:
        if not isinstance(params["image_dimensionality"], (float, int)):
            raise StyxValidationError(f'`image_dimensionality` has the wrong type: Received `{type(params["image_dimensionality"])}` expected `AntsJointFusionImageDimensionality`')
        if params["image_dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `image_dimensionality` must be one of [2, 3, 4]")
    if params.get("target_image", None) is None:
        raise StyxValidationError("`target_image` must not be None")
    if not isinstance(params["target_image"], list):
        raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(params["target_image"])}` expected `list[InputPathType]`')
    for e in params["target_image"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("atlas_image", None) is None:
        raise StyxValidationError("`atlas_image` must not be None")
    if not isinstance(params["atlas_image"], list):
        raise StyxValidationError(f'`atlas_image` has the wrong type: Received `{type(params["atlas_image"])}` expected `list[InputPathType]`')
    for e_2 in params["atlas_image"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`atlas_image` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("atlas_segmentation", None) is None:
        raise StyxValidationError("`atlas_segmentation` must not be None")
    if not isinstance(params["atlas_segmentation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_segmentation` has the wrong type: Received `{type(params["atlas_segmentation"])}` expected `InputPathType`')
    if params.get("alpha", None) is not None:
        if not isinstance(params["alpha"], (float, int)):
            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["alpha"])}` expected `float`')
    if params.get("beta", None) is not None:
        if not isinstance(params["beta"], (float, int)):
            raise StyxValidationError(f'`beta` has the wrong type: Received `{type(params["beta"])}` expected `float`')
    if params.get("constrain_nonnegative", None) is not None:
        if not isinstance(params["constrain_nonnegative"], bool):
            raise StyxValidationError(f'`constrain_nonnegative` has the wrong type: Received `{type(params["constrain_nonnegative"])}` expected `bool`')
    if params.get("patch_radius", None) is not None:
        if not isinstance(params["patch_radius"], str):
            raise StyxValidationError(f'`patch_radius` has the wrong type: Received `{type(params["patch_radius"])}` expected `str`')
    if params.get("patch_metric", None) is not None:
        if not isinstance(params["patch_metric"], str):
            raise StyxValidationError(f'`patch_metric` has the wrong type: Received `{type(params["patch_metric"])}` expected `AntsJointFusionPatchMetric`')
        if params["patch_metric"] not in ["PC", "MSQ"]:
            raise StyxValidationError("Parameter `patch_metric` must be one of [\"PC\", \"MSQ\"]")
    if params.get("search_radius", None) is not None:
        if not isinstance(params["search_radius"], str):
            raise StyxValidationError(f'`search_radius` has the wrong type: Received `{type(params["search_radius"])}` expected `str`')
    if params.get("exclusion_image", None) is not None:
        if not isinstance(params["exclusion_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`exclusion_image` has the wrong type: Received `{type(params["exclusion_image"])}` expected `InputPathType`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def ants_joint_fusion_cargs(params: AntsJointFusion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsJointFusion")
    v_0 = params.get("image_dimensionality")
    if v_0 is not None:
        cargs.append("--image-dimensionality")
        cargs.append(str(v_0))
    cargs.append("--target-image")
    for item0 in params["target_image"]:
        cargs.append(execution.input_file(item0))
    cargs.append("--atlas-image")
    for item1 in params["atlas_image"]:
        cargs.append(execution.input_file(item1))
    cargs.append("--atlas-segmentation")
    cargs.append(execution.input_file(params["atlas_segmentation"]))
    v_1 = params.get("alpha")
    if v_1 is not None:
        cargs.append("--alpha")
        cargs.append(str(v_1))
    v_2 = params.get("beta")
    if v_2 is not None:
        cargs.append("--beta")
        cargs.append(str(v_2))
    v_3 = params.get("constrain_nonnegative")
    if v_3 is not None:
        cargs.append("--constrain-nonnegative")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    v_4 = params.get("patch_radius")
    if v_4 is not None:
        cargs.append("--patch-radius")
        cargs.append(v_4)
    v_5 = params.get("patch_metric")
    if v_5 is not None:
        cargs.append("--patch-metric")
        cargs.append(str(v_5))
    v_6 = params.get("search_radius")
    if v_6 is not None:
        cargs.append("--search-radius")
        cargs.append(v_6)
    v_7 = params.get("exclusion_image")
    if v_7 is not None:
        cargs.append("--exclusion-image")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("mask_image")
    if v_8 is not None:
        cargs.append("--mask-image")
        cargs.append(execution.input_file(v_8))
    cargs.append("--output")
    cargs.append(params["output"])
    v_9 = params.get("verbose")
    if v_9 is not None:
        cargs.append("--verbose")
        if v_9:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def ants_joint_fusion_outputs(params: AntsJointFusion, execution: Execution) -> AntsJointFusionOutputs:
    """Build the AntsJointFusionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_fusion_image_v: OutputPathType = execution.output_file("[LABELFUSIONIMAGE]")
    intensity_fusion_image_v: OutputPathType = execution.output_file("[INTENSITYFUSIONIMAGEFILENAMEFORMAT]")
    label_posterior_probability_image_v: OutputPathType = execution.output_file("[LABELPOSTERIORPROBABILITYIMAGEFILENAMEFORMAT]")
    atlas_voting_weight_image_v: OutputPathType = execution.output_file("[ATLASVOTINGWEIGHTIMAGEFILENAMEFORMAT]")
    return AntsJointFusionOutputs(
        root=root_v,
        label_fusion_image=label_fusion_image_v,
        intensity_fusion_image=intensity_fusion_image_v,
        label_posterior_probability_image=label_posterior_probability_image_v,
        atlas_voting_weight_image=atlas_voting_weight_image_v,
    )

def ants_joint_fusion_execute(params: AntsJointFusion, runner: Runner | None = None) -> AntsJointFusionOutputs:
    """
    antsJointFusion

    antsJointFusion is an image fusion algorithm developed by Hongzhi Wang and Paul Yushkevich. This implementation is based on Paul's original ITK-style implementation and Brian's ANTsR implementation. The original label fusion framework was extended to accommodate intensities.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_joint_fusion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_JOINT_FUSION_METADATA)
    execution.params(params)
    args = ants_joint_fusion_cargs(params, execution)
    out = ants_joint_fusion_outputs(params, execution)
    execution.run(args)
    return out

def ants_joint_fusion(
    target_image: list[InputPathType],
    atlas_image: list[InputPathType],
    atlas_segmentation: InputPathType,
    output: str,
    image_dimensionality: AntsJointFusionImageDimensionality | None = None,
    alpha: float | None = None,
    beta: float | None = None,
    constrain_nonnegative: bool | None = None,
    patch_radius: str | None = None,
    patch_metric: AntsJointFusionPatchMetric | None = None,
    search_radius: str | None = None,
    exclusion_image: InputPathType | None = None,
    mask_image: InputPathType | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AntsJointFusionOutputs:
    """
    antsJointFusion

    antsJointFusion is an image fusion algorithm developed by Hongzhi Wang and Paul Yushkevich. This implementation is based on Paul's original ITK-style implementation and Brian's ANTsR implementation. The original label fusion framework was extended to accommodate intensities.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        target_image: The target image (or multimodal target images) assumed to be\
            aligned to a common image domain.
        atlas_image: The atlas image (or multimodal atlas images) assumed to be\
            aligned to a common image domain.
        atlas_segmentation: The atlas segmentation images. For performing label\
            fusion the number of specified segmentations should be identical to the\
            number of atlas image sets.
        output: The output is the intensity and/or label fusion image. Additional\
            optional outputs include the label posterior probability images and the\
            atlas voting weight images.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        alpha: Regularization term added to matrix Mx for calculating the inverse.\
            Default = 0.1.
        beta: Exponent for mapping intensity difference to the joint error. Default\
            = 2.0.
        constrain_nonnegative: Constrain solution to non-negative weights.
        patch_radius: Patch radius for similarity measures. Default = 2x2x2.
        patch_metric: Metric to be used in determining the most similar\
            neighborhood patch. Options include Pearson's correlation (PC) and mean\
            squares (MSQ). Default = PC (Pearson correlation).
        search_radius: Search radius for similarity measures. Default = 3x3x3. One\
            can also specify an image where the value at the voxel specifies the\
            isotropic search radius at that voxel.
        exclusion_image: Specify an exclusion region for the given label.
        mask_image: If a mask image is specified, fusion is only performed in the\
            mask region.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_joint_fusion_params(
        target_image=target_image,
        atlas_image=atlas_image,
        atlas_segmentation=atlas_segmentation,
        output=output,
        image_dimensionality=image_dimensionality,
        alpha=alpha,
        beta=beta,
        constrain_nonnegative=constrain_nonnegative,
        patch_radius=patch_radius,
        patch_metric=patch_metric,
        search_radius=search_radius,
        exclusion_image=exclusion_image,
        mask_image=mask_image,
        verbose=verbose,
    )
    return ants_joint_fusion_execute(params, runner)

__all__ = [
    "AntsJointFusion",
    "AntsJointFusionOutputs",
    "ANTS_JOINT_FUSION_METADATA",
    "ants_joint_fusion_cargs",
    "ants_joint_fusion_outputs",
    "ants_joint_fusion_params",
    "ants_joint_fusion_execute",
    "ants_joint_fusion_validate",
    "ants_joint_fusion",
]