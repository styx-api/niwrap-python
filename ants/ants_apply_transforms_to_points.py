# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_APPLY_TRANSFORMS_TO_POINTS_METADATA = Metadata(
    id="antsApplyTransformsToPoints",
    name="antsApplyTransformsToPoints",
    package="ants",
)

AntsApplyTransformsToPointsDimensionality = typing.Literal[2] | typing.Literal[3]

AntsApplyTransformsToPointsInverseTransform = typing.TypedDict(
    "AntsApplyTransformsToPointsInverseTransform",
    {
        "@type": typing.Literal["inverse_transform"],
        "transform_file": InputPathType,
    },
)

AntsApplyTransformsToPointsTransform = typing.Literal["[TRANSFORM]"] | AntsApplyTransformsToPointsInverseTransform

AntsApplyTransformsToPoints = typing.TypedDict(
    "AntsApplyTransformsToPoints",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsApplyTransformsToPoints"]],
        "dimensionality": typing.NotRequired[AntsApplyTransformsToPointsDimensionality],
        "precision": typing.NotRequired[bool],
        "forantsr": typing.NotRequired[bool],
        "input": InputPathType,
        "output": str,
        "transform": typing.NotRequired[AntsApplyTransformsToPointsTransform],
    },
)

@dataclasses.dataclass
class AntsApplyTransformsToPointsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    warped_points: OutputPathType
    """The output is the CSV file containing warped points."""

def ants_apply_transforms_to_points_params(
    input_: InputPathType,
    output: str,
    dimensionality: AntsApplyTransformsToPointsDimensionality | None = None,
    precision: bool | None = None,
    forantsr: bool | None = None,
    transform: AntsApplyTransformsToPointsTransform | None = None,
) -> AntsApplyTransformsToPoints:
    """
    Build parameters.

    Args:
        input_: Input CSV file with columns including x,y,z,t headers that define\
            the points in physical space, or a 2D .mha binary image file.
        output: Output the warped points to a CSV file.
        dimensionality: This option forces the points to be treated as a\
            specified-dimensionality.
        precision: Use double precision.
        forantsr: Set true for ANTsR IO.
        transform: Transform file(s) to apply to the input points. Uses an inverse\
            transform if specified as [transformFileName,1].

    Returns:
        Parameter dictionary.
    """
    params: AntsApplyTransformsToPoints = {
        "@type": "ants/antsApplyTransformsToPoints",
        "input": input_,
        "output": output,
    }
    if dimensionality is not None:
        params["dimensionality"] = dimensionality
    if precision is not None:
        params["precision"] = precision
    if forantsr is not None:
        params["forantsr"] = forantsr
    if transform is not None:
        params["transform"] = transform
    return params

def ants_apply_transforms_to_points_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsApplyTransformsToPoints."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensionality", None) is not None:
        if not isinstance(params["dimensionality"], (float, int)):
            raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `AntsApplyTransformsToPointsDimensionality`')
        if params["dimensionality"] not in [2, 3]:
            raise StyxValidationError("Parameter `dimensionality` must be one of [2, 3]")
    if params.get("precision", None) is not None:
        if not isinstance(params["precision"], bool):
            raise StyxValidationError(f'`precision` has the wrong type: Received `{type(params["precision"])}` expected `bool`')
    if params.get("forantsr", None) is not None:
        if not isinstance(params["forantsr"], bool):
            raise StyxValidationError(f'`forantsr` has the wrong type: Received `{type(params["forantsr"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("transform", None) is not None:
        if isinstance(params["transform"], dict):
            if "@type" not in params["transform"]:
                raise StyxValidationError("Params object is missing `@type`")
            if params["transform"]["@type"] not in ["inverse_transform"]:
                raise StyxValidationError("Parameter `transform`s `@type` must be one of [\"inverse_transform\"]")
            if params["transform"]["@type"] == "inverse_transform":
                if params["transform"].get("transform_file", None) is None:
                    raise StyxValidationError("`transform_file` must not be None")
                if not isinstance(params["transform"]["transform_file"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`transform_file` has the wrong type: Received `{type(params["transform"]["transform_file"])}` expected `InputPathType`')
        else:
            if params["transform"] not in ["[TRANSFORM]"]:
                raise StyxValidationError("Parameter `transform` must be one of [\"[TRANSFORM]\"]")

def ants_apply_transforms_to_points_cargs(params: AntsApplyTransformsToPoints, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsApplyTransformsToPoints")
    v_0 = params.get("dimensionality")
    if v_0 is not None:
        cargs.append("--dimensionality")
        cargs.append(str(v_0))
    v_1 = params.get("precision")
    if v_1 is not None:
        cargs.append("--precision")
        if v_1:
            cargs.append("0")
        else:
            cargs.append("1")
    v_2 = params.get("forantsr")
    if v_2 is not None:
        cargs.append("--forantsr")
        if v_2:
            cargs.append("0")
        else:
            cargs.append("1")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input"]))
    cargs.append("-o")
    cargs.append(params["output"])
    v_3 = params.get("transform")
    if v_3 is not None:
        cargs.append("-t")
        if isinstance(v_3, dict):
            if v_3["@type"] == "inverse_transform":
                cargs.append("".join([execution.input_file(v_3["transform_file"]), ",1"]))
        else:
            cargs.append(str(v_3))
    return cargs

def ants_apply_transforms_to_points_outputs(params: AntsApplyTransformsToPoints, execution: Execution) -> AntsApplyTransformsToPointsOutputs:
    """Build the AntsApplyTransformsToPointsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    warped_points_v: OutputPathType = execution.output_file(params["output"])
    return AntsApplyTransformsToPointsOutputs(
        root=root_v,
        warped_points=warped_points_v,
    )

def ants_apply_transforms_to_points_execute(params: AntsApplyTransformsToPoints, runner: Runner | None = None) -> AntsApplyTransformsToPointsOutputs:
    """
    antsApplyTransformsToPoints

    antsApplyTransformsToPoints, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms). It reads in a CSV file with the first D columns defining the spatial location where the spatial location is defined in physical coordinates. The CSV file should have a header row.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_apply_transforms_to_points_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_APPLY_TRANSFORMS_TO_POINTS_METADATA)
    execution.params(params)
    args = ants_apply_transforms_to_points_cargs(params, execution)
    out = ants_apply_transforms_to_points_outputs(params, execution)
    execution.run(args)
    return out

def ants_apply_transforms_to_points(
    input_: InputPathType,
    output: str,
    dimensionality: AntsApplyTransformsToPointsDimensionality | None = None,
    precision: bool | None = None,
    forantsr: bool | None = None,
    transform: AntsApplyTransformsToPointsTransform | None = None,
    runner: Runner | None = None,
) -> AntsApplyTransformsToPointsOutputs:
    """
    antsApplyTransformsToPoints

    antsApplyTransformsToPoints, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms). It reads in a CSV file with the first D columns defining the spatial location where the spatial location is defined in physical coordinates. The CSV file should have a header row.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        input_: Input CSV file with columns including x,y,z,t headers that define\
            the points in physical space, or a 2D .mha binary image file.
        output: Output the warped points to a CSV file.
        dimensionality: This option forces the points to be treated as a\
            specified-dimensionality.
        precision: Use double precision.
        forantsr: Set true for ANTsR IO.
        transform: Transform file(s) to apply to the input points. Uses an inverse\
            transform if specified as [transformFileName,1].
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_apply_transforms_to_points_params(
        input_=input_,
        output=output,
        dimensionality=dimensionality,
        precision=precision,
        forantsr=forantsr,
        transform=transform,
    )
    return ants_apply_transforms_to_points_execute(params, runner)

__all__ = [
    "AntsApplyTransformsToPoints",
    "AntsApplyTransformsToPointsOutputs",
    "ANTS_APPLY_TRANSFORMS_TO_POINTS_METADATA",
    "ants_apply_transforms_to_points_cargs",
    "ants_apply_transforms_to_points_outputs",
    "ants_apply_transforms_to_points_params",
    "ants_apply_transforms_to_points_execute",
    "ants_apply_transforms_to_points_validate",
    "ants_apply_transforms_to_points",
]