# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAGE_INTENSITY_STATISTICS_METADATA = Metadata(
    id="ImageIntensityStatistics",
    name="ImageIntensityStatistics",
    package="ants",
)

ImageIntensityStatistics = typing.TypedDict(
    "ImageIntensityStatistics",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ImageIntensityStatistics"]],
        "image_dimension": int,
        "input_image": InputPathType,
        "label_image": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class ImageIntensityStatisticsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    intensity_statistics: OutputPathType
    """The output file containing intensity statistics."""

def image_intensity_statistics_params(
    image_dimension: int,
    input_image: InputPathType,
    label_image: InputPathType | None = None,
) -> ImageIntensityStatistics:
    """
    Build parameters.

    Args:
        image_dimension: The dimensionality of the image (e.g., 2D, 3D).
        input_image: The input image for which intensity statistics will be\
            computed.
        label_image: An optional label image which defines regions of interest.

    Returns:
        Parameter dictionary.
    """
    params: ImageIntensityStatistics = {
        "@type": "ants/ImageIntensityStatistics",
        "image_dimension": image_dimension,
        "input_image": input_image,
    }
    if label_image is not None:
        params["label_image"] = label_image
    return params

def image_intensity_statistics_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ImageIntensityStatistics."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("label_image", None) is not None:
        if not isinstance(params["label_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_image` has the wrong type: Received `{type(params["label_image"])}` expected `InputPathType`')

def image_intensity_statistics_cargs(params: ImageIntensityStatistics, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ImageIntensityStatistics")
    cargs.append(str(params["image_dimension"]))
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("label_image")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def image_intensity_statistics_outputs(params: ImageIntensityStatistics, execution: Execution) -> ImageIntensityStatisticsOutputs:
    """Build the ImageIntensityStatisticsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    intensity_statistics_v: OutputPathType = execution.output_file("intensity_statistics.txt")
    return ImageIntensityStatisticsOutputs(
        root=root_v,
        intensity_statistics=intensity_statistics_v,
    )

def image_intensity_statistics_execute(params: ImageIntensityStatistics, runner: Runner | None = None) -> ImageIntensityStatisticsOutputs:
    """
    ImageIntensityStatistics

    This tool computes intensity statistics of an input image, optionally given a label image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    image_intensity_statistics_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAGE_INTENSITY_STATISTICS_METADATA)
    execution.params(params)
    args = image_intensity_statistics_cargs(params, execution)
    out = image_intensity_statistics_outputs(params, execution)
    execution.run(args)
    return out

def image_intensity_statistics(
    image_dimension: int,
    input_image: InputPathType,
    label_image: InputPathType | None = None,
    runner: Runner | None = None,
) -> ImageIntensityStatisticsOutputs:
    """
    ImageIntensityStatistics

    This tool computes intensity statistics of an input image, optionally given a label image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: The dimensionality of the image (e.g., 2D, 3D).
        input_image: The input image for which intensity statistics will be\
            computed.
        label_image: An optional label image which defines regions of interest.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = image_intensity_statistics_params(
        image_dimension=image_dimension,
        input_image=input_image,
        label_image=label_image,
    )
    return image_intensity_statistics_execute(params, runner)

__all__ = [
    "ImageIntensityStatistics",
    "ImageIntensityStatisticsOutputs",
    "IMAGE_INTENSITY_STATISTICS_METADATA",
    "image_intensity_statistics_cargs",
    "image_intensity_statistics_outputs",
    "image_intensity_statistics_params",
    "image_intensity_statistics_execute",
    "image_intensity_statistics_validate",
    "image_intensity_statistics",
]