# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ATROPOS_METADATA = Metadata(
    id="Atropos",
    name="Atropos",
    package="ants",
)

AtroposImageDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

Atropos = typing.TypedDict(
    "Atropos",
    {
        "@type": typing.NotRequired[typing.Literal["ants/Atropos"]],
        "image_dimensionality": typing.NotRequired[AtroposImageDimensionality],
        "intensity_image": str,
        "bspline": typing.NotRequired[str],
        "initialization": str,
        "partial_volume_label_set": typing.NotRequired[str],
        "use_partial_volume_likelihoods": typing.NotRequired[bool],
        "posterior_formulation": typing.NotRequired[str],
        "mask_image": InputPathType,
        "convergence": str,
        "likelihood_model": str,
        "mrf": typing.NotRequired[str],
        "icm": typing.NotRequired[str],
        "use_random_seed": typing.NotRequired[bool],
        "output": str,
        "minimize_memory_usage": typing.NotRequired[bool],
        "winsorize_outliers": typing.NotRequired[str],
        "use_euclidean_distance": typing.NotRequired[bool],
        "label_propagation": typing.NotRequired[str],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AtroposOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    classified_image: OutputPathType
    """The output labeled image with assigned labels for each voxel in the masked region."""
    posterior_probability_images: OutputPathType
    """Output posterior probability images in specified format."""

def atropos_params(
    intensity_image: str,
    initialization: str,
    mask_image: InputPathType,
    convergence: str,
    likelihood_model: str,
    output: str,
    image_dimensionality: AtroposImageDimensionality | None = None,
    bspline: str | None = None,
    partial_volume_label_set: str | None = None,
    use_partial_volume_likelihoods: bool | None = None,
    posterior_formulation: str | None = None,
    mrf: str | None = None,
    icm: str | None = None,
    use_random_seed: bool | None = None,
    minimize_memory_usage: bool | None = None,
    winsorize_outliers: str | None = None,
    use_euclidean_distance: bool | None = None,
    label_propagation: str | None = None,
    verbose: bool | None = None,
) -> Atropos:
    """
    Build parameters.

    Args:
        intensity_image: One or more scalar images is specified for segmentation.\
            For scenarios with no prior information, the first scalar image is used\
            to order labelings by intensity. The optional adaptive smoothing weight\
            is applicable with prior images, specified between [0,1].
        initialization: Initialize the FMM parameters. options include Random,\
            Otsu, KMeans, PriorProbabilityImages, and PriorLabelImage.
        mask_image: The required image mask defines the region to be labeled by\
            Atropos.
        convergence: Determine convergence based on mean maximum posterior\
            probability over region of interest.
        likelihood_model: Specify parametric or non-parametric likelihood model.\
            Options include Gaussian, HistogramParzenWindows,\
            ManifoldParzenWindows, among others.
        output: Output labeled image and optionally posterior probability images.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, Atropos tries to infer\
            the dimensionality from the first input image.
        bspline: Parameters for B-Spline. Adaptive smoothing is applied to\
            intensity images if smoothing weights > 0.
        partial_volume_label_set: Model mixtures of classes within single voxels.\
            Specify labels for each partial volume class.
        use_partial_volume_likelihoods: Whether to use partial volume likelihoods.\
            A value of 1 considers the partial volume class separate from tissue\
            classes.
        posterior_formulation: Specify posterior probability formulation. Options\
            are Socrates, Plato, Aristotle, or Sigmoid.
        mrf: Markov Random Field parameters to enforce spatial constraints on\
            segmentation.
        icm: ICM (Iterated Conditional Modes) parameters for asynchronous updating.
        use_random_seed: Initialize with a random seed or a constant seed number.
        minimize_memory_usage: Minimize memory usage by calculating images on the\
            fly and storing only non-negligible pixel values.
        winsorize_outliers: Options to remove effects of outliers in calculations\
            using methods like BoxPlot or GrubbsRosner.
        use_euclidean_distance: Propagate labels throughout the mask using a\
            distance transform.
        label_propagation: Control propagation of each prior label by specified\
            lambda and boundary probability.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: Atropos = {
        "@type": "ants/Atropos",
        "intensity_image": intensity_image,
        "initialization": initialization,
        "mask_image": mask_image,
        "convergence": convergence,
        "likelihood_model": likelihood_model,
        "output": output,
    }
    if image_dimensionality is not None:
        params["image_dimensionality"] = image_dimensionality
    if bspline is not None:
        params["bspline"] = bspline
    if partial_volume_label_set is not None:
        params["partial_volume_label_set"] = partial_volume_label_set
    if use_partial_volume_likelihoods is not None:
        params["use_partial_volume_likelihoods"] = use_partial_volume_likelihoods
    if posterior_formulation is not None:
        params["posterior_formulation"] = posterior_formulation
    if mrf is not None:
        params["mrf"] = mrf
    if icm is not None:
        params["icm"] = icm
    if use_random_seed is not None:
        params["use_random_seed"] = use_random_seed
    if minimize_memory_usage is not None:
        params["minimize_memory_usage"] = minimize_memory_usage
    if winsorize_outliers is not None:
        params["winsorize_outliers"] = winsorize_outliers
    if use_euclidean_distance is not None:
        params["use_euclidean_distance"] = use_euclidean_distance
    if label_propagation is not None:
        params["label_propagation"] = label_propagation
    if verbose is not None:
        params["verbose"] = verbose
    return params

def atropos_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Atropos."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimensionality", None) is not None:
        if not isinstance(params["image_dimensionality"], (float, int)):
            raise StyxValidationError(f'`image_dimensionality` has the wrong type: Received `{type(params["image_dimensionality"])}` expected `AtroposImageDimensionality`')
        if params["image_dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `image_dimensionality` must be one of [2, 3, 4]")
    if params.get("intensity_image", None) is None:
        raise StyxValidationError("`intensity_image` must not be None")
    if not isinstance(params["intensity_image"], str):
        raise StyxValidationError(f'`intensity_image` has the wrong type: Received `{type(params["intensity_image"])}` expected `str`')
    if params.get("bspline", None) is not None:
        if not isinstance(params["bspline"], str):
            raise StyxValidationError(f'`bspline` has the wrong type: Received `{type(params["bspline"])}` expected `str`')
    if params.get("initialization", None) is None:
        raise StyxValidationError("`initialization` must not be None")
    if not isinstance(params["initialization"], str):
        raise StyxValidationError(f'`initialization` has the wrong type: Received `{type(params["initialization"])}` expected `str`')
    if params.get("partial_volume_label_set", None) is not None:
        if not isinstance(params["partial_volume_label_set"], str):
            raise StyxValidationError(f'`partial_volume_label_set` has the wrong type: Received `{type(params["partial_volume_label_set"])}` expected `str`')
    if params.get("use_partial_volume_likelihoods", None) is not None:
        if not isinstance(params["use_partial_volume_likelihoods"], bool):
            raise StyxValidationError(f'`use_partial_volume_likelihoods` has the wrong type: Received `{type(params["use_partial_volume_likelihoods"])}` expected `bool`')
    if params.get("posterior_formulation", None) is not None:
        if not isinstance(params["posterior_formulation"], str):
            raise StyxValidationError(f'`posterior_formulation` has the wrong type: Received `{type(params["posterior_formulation"])}` expected `str`')
    if params.get("mask_image", None) is None:
        raise StyxValidationError("`mask_image` must not be None")
    if not isinstance(params["mask_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("convergence", None) is None:
        raise StyxValidationError("`convergence` must not be None")
    if not isinstance(params["convergence"], str):
        raise StyxValidationError(f'`convergence` has the wrong type: Received `{type(params["convergence"])}` expected `str`')
    if params.get("likelihood_model", None) is None:
        raise StyxValidationError("`likelihood_model` must not be None")
    if not isinstance(params["likelihood_model"], str):
        raise StyxValidationError(f'`likelihood_model` has the wrong type: Received `{type(params["likelihood_model"])}` expected `str`')
    if params.get("mrf", None) is not None:
        if not isinstance(params["mrf"], str):
            raise StyxValidationError(f'`mrf` has the wrong type: Received `{type(params["mrf"])}` expected `str`')
    if params.get("icm", None) is not None:
        if not isinstance(params["icm"], str):
            raise StyxValidationError(f'`icm` has the wrong type: Received `{type(params["icm"])}` expected `str`')
    if params.get("use_random_seed", None) is not None:
        if not isinstance(params["use_random_seed"], bool):
            raise StyxValidationError(f'`use_random_seed` has the wrong type: Received `{type(params["use_random_seed"])}` expected `bool`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("minimize_memory_usage", None) is not None:
        if not isinstance(params["minimize_memory_usage"], bool):
            raise StyxValidationError(f'`minimize_memory_usage` has the wrong type: Received `{type(params["minimize_memory_usage"])}` expected `bool`')
    if params.get("winsorize_outliers", None) is not None:
        if not isinstance(params["winsorize_outliers"], str):
            raise StyxValidationError(f'`winsorize_outliers` has the wrong type: Received `{type(params["winsorize_outliers"])}` expected `str`')
    if params.get("use_euclidean_distance", None) is not None:
        if not isinstance(params["use_euclidean_distance"], bool):
            raise StyxValidationError(f'`use_euclidean_distance` has the wrong type: Received `{type(params["use_euclidean_distance"])}` expected `bool`')
    if params.get("label_propagation", None) is not None:
        if not isinstance(params["label_propagation"], str):
            raise StyxValidationError(f'`label_propagation` has the wrong type: Received `{type(params["label_propagation"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def atropos_cargs(params: Atropos, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("Atropos")
    v_0 = params.get("image_dimensionality")
    if v_0 is not None:
        cargs.append("--image-dimensionality")
        cargs.append(str(v_0))
    cargs.append("-a")
    cargs.append(params["intensity_image"])
    v_1 = params.get("bspline")
    if v_1 is not None:
        cargs.append("-b")
        cargs.append(v_1)
    cargs.append("-i")
    cargs.append(params["initialization"])
    v_2 = params.get("partial_volume_label_set")
    if v_2 is not None:
        cargs.append("-s")
        cargs.append(v_2)
    v_3 = params.get("use_partial_volume_likelihoods")
    if v_3 is not None:
        cargs.append("--use-partial-volume-likelihoods")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    v_4 = params.get("posterior_formulation")
    if v_4 is not None:
        cargs.append("-p")
        cargs.append(v_4)
    cargs.append("-x")
    cargs.append(execution.input_file(params["mask_image"]))
    cargs.append("-c")
    cargs.append(params["convergence"])
    cargs.append("-k")
    cargs.append(params["likelihood_model"])
    v_5 = params.get("mrf")
    if v_5 is not None:
        cargs.append("-m")
        cargs.append(v_5)
    v_6 = params.get("icm")
    if v_6 is not None:
        cargs.append("-g")
        cargs.append(v_6)
    v_7 = params.get("use_random_seed")
    if v_7 is not None:
        cargs.append("-r")
        if v_7:
            cargs.append("0")
        else:
            cargs.append("1")
    cargs.append("-o")
    cargs.append(params["output"])
    v_8 = params.get("minimize_memory_usage")
    if v_8 is not None:
        cargs.append("-u")
        if v_8:
            cargs.append("0")
        else:
            cargs.append("1")
    v_9 = params.get("winsorize_outliers")
    if v_9 is not None:
        cargs.append("-w")
        cargs.append(v_9)
    v_10 = params.get("use_euclidean_distance")
    if v_10 is not None:
        cargs.append("-e")
        if v_10:
            cargs.append("0")
        else:
            cargs.append("1")
    v_11 = params.get("label_propagation")
    if v_11 is not None:
        cargs.append("-l")
        cargs.append(v_11)
    v_12 = params.get("verbose")
    if v_12 is not None:
        cargs.append("-v")
        if v_12:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def atropos_outputs(params: Atropos, execution: Execution) -> AtroposOutputs:
    """Build the AtroposOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    classified_image_v: OutputPathType = execution.output_file(f'{params["output"]}_classified.nii.gz')
    posterior_probability_images_v: OutputPathType = execution.output_file("[POSTERIOR_PROBABILITY_IMAGE_FILE_NAME_FORMAT]")
    return AtroposOutputs(
        root=root_v,
        classified_image=classified_image_v,
        posterior_probability_images=posterior_probability_images_v,
    )

def atropos_execute(params: Atropos, runner: Runner | None = None) -> AtroposOutputs:
    """
    Atropos

    Atropos is a finite mixture modeling (FMM) segmentation approach that allows for prior constraints including a prior label image, prior probability images, and/or an MRF prior to enforce spatial smoothing of the labels.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    atropos_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ATROPOS_METADATA)
    execution.params(params)
    args = atropos_cargs(params, execution)
    out = atropos_outputs(params, execution)
    execution.run(args)
    return out

def atropos(
    intensity_image: str,
    initialization: str,
    mask_image: InputPathType,
    convergence: str,
    likelihood_model: str,
    output: str,
    image_dimensionality: AtroposImageDimensionality | None = None,
    bspline: str | None = None,
    partial_volume_label_set: str | None = None,
    use_partial_volume_likelihoods: bool | None = None,
    posterior_formulation: str | None = None,
    mrf: str | None = None,
    icm: str | None = None,
    use_random_seed: bool | None = None,
    minimize_memory_usage: bool | None = None,
    winsorize_outliers: str | None = None,
    use_euclidean_distance: bool | None = None,
    label_propagation: str | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AtroposOutputs:
    """
    Atropos

    Atropos is a finite mixture modeling (FMM) segmentation approach that allows for prior constraints including a prior label image, prior probability images, and/or an MRF prior to enforce spatial smoothing of the labels.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        intensity_image: One or more scalar images is specified for segmentation.\
            For scenarios with no prior information, the first scalar image is used\
            to order labelings by intensity. The optional adaptive smoothing weight\
            is applicable with prior images, specified between [0,1].
        initialization: Initialize the FMM parameters. options include Random,\
            Otsu, KMeans, PriorProbabilityImages, and PriorLabelImage.
        mask_image: The required image mask defines the region to be labeled by\
            Atropos.
        convergence: Determine convergence based on mean maximum posterior\
            probability over region of interest.
        likelihood_model: Specify parametric or non-parametric likelihood model.\
            Options include Gaussian, HistogramParzenWindows,\
            ManifoldParzenWindows, among others.
        output: Output labeled image and optionally posterior probability images.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, Atropos tries to infer\
            the dimensionality from the first input image.
        bspline: Parameters for B-Spline. Adaptive smoothing is applied to\
            intensity images if smoothing weights > 0.
        partial_volume_label_set: Model mixtures of classes within single voxels.\
            Specify labels for each partial volume class.
        use_partial_volume_likelihoods: Whether to use partial volume likelihoods.\
            A value of 1 considers the partial volume class separate from tissue\
            classes.
        posterior_formulation: Specify posterior probability formulation. Options\
            are Socrates, Plato, Aristotle, or Sigmoid.
        mrf: Markov Random Field parameters to enforce spatial constraints on\
            segmentation.
        icm: ICM (Iterated Conditional Modes) parameters for asynchronous updating.
        use_random_seed: Initialize with a random seed or a constant seed number.
        minimize_memory_usage: Minimize memory usage by calculating images on the\
            fly and storing only non-negligible pixel values.
        winsorize_outliers: Options to remove effects of outliers in calculations\
            using methods like BoxPlot or GrubbsRosner.
        use_euclidean_distance: Propagate labels throughout the mask using a\
            distance transform.
        label_propagation: Control propagation of each prior label by specified\
            lambda and boundary probability.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = atropos_params(
        intensity_image=intensity_image,
        initialization=initialization,
        mask_image=mask_image,
        convergence=convergence,
        likelihood_model=likelihood_model,
        output=output,
        image_dimensionality=image_dimensionality,
        bspline=bspline,
        partial_volume_label_set=partial_volume_label_set,
        use_partial_volume_likelihoods=use_partial_volume_likelihoods,
        posterior_formulation=posterior_formulation,
        mrf=mrf,
        icm=icm,
        use_random_seed=use_random_seed,
        minimize_memory_usage=minimize_memory_usage,
        winsorize_outliers=winsorize_outliers,
        use_euclidean_distance=use_euclidean_distance,
        label_propagation=label_propagation,
        verbose=verbose,
    )
    return atropos_execute(params, runner)

__all__ = [
    "Atropos",
    "AtroposOutputs",
    "ATROPOS_METADATA",
    "atropos_cargs",
    "atropos_outputs",
    "atropos_params",
    "atropos_execute",
    "atropos_validate",
    "atropos",
]