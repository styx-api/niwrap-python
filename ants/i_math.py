# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

I_MATH_METADATA = Metadata(
    id="iMath",
    name="iMath",
    package="ants",
)

IMathImageDimension = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

IMath = typing.TypedDict(
    "IMath",
    {
        "@type": typing.NotRequired[typing.Literal["ants/iMath"]],
        "image_dimension": IMathImageDimension,
        "output_image": str,
        "operations": str,
        "image1": InputPathType,
        "image2": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class IMathOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resulting_image: OutputPathType
    """The output image resulting from the operation."""

def i_math_params(
    image_dimension: IMathImageDimension,
    output_image: str,
    operations: str,
    image1: InputPathType,
    image2: InputPathType | None = None,
) -> IMath:
    """
    Build parameters.

    Args:
        image_dimension: Dimensionality of the image, either 2, 3, or 4.
        output_image: Path for the output image file.
        operations: Operations to be performed along with parameters, e.g.,\
            GetLargestComponent, MC for Closing, etc.
        image1: First input image file.
        image2: Second input image file, if required by operation.

    Returns:
        Parameter dictionary.
    """
    params: IMath = {
        "@type": "ants/iMath",
        "image_dimension": image_dimension,
        "output_image": output_image,
        "operations": operations,
        "image1": image1,
    }
    if image2 is not None:
        params["image2"] = image2
    return params

def i_math_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid IMath."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], (float, int)):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `IMathImageDimension`')
    if params["image_dimension"] not in [2, 3, 4]:
        raise StyxValidationError("Parameter `image_dimension` must be one of [2, 3, 4]")
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("operations", None) is None:
        raise StyxValidationError("`operations` must not be None")
    if not isinstance(params["operations"], str):
        raise StyxValidationError(f'`operations` has the wrong type: Received `{type(params["operations"])}` expected `str`')
    if params.get("image1", None) is None:
        raise StyxValidationError("`image1` must not be None")
    if not isinstance(params["image1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image1` has the wrong type: Received `{type(params["image1"])}` expected `InputPathType`')
    if params.get("image2", None) is not None:
        if not isinstance(params["image2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`image2` has the wrong type: Received `{type(params["image2"])}` expected `InputPathType`')

def i_math_cargs(params: IMath, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("iMath")
    cargs.append(str(params["image_dimension"]))
    cargs.append(params["output_image"])
    cargs.append(params["operations"])
    cargs.append(execution.input_file(params["image1"]))
    v_0 = params.get("image2")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    return cargs

def i_math_outputs(params: IMath, execution: Execution) -> IMathOutputs:
    """Build the IMathOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    resulting_image_v: OutputPathType = execution.output_file(params["output_image"])
    return IMathOutputs(
        root=root_v,
        resulting_image=resulting_image_v,
    )

def i_math_execute(params: IMath, runner: Runner | None = None) -> IMathOutputs:
    """
    iMath

    iMath is a tool for performing various image mathematical operations on medical images, specifically supporting operations on 2D, 3D, and 4D data.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    i_math_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(I_MATH_METADATA)
    execution.params(params)
    args = i_math_cargs(params, execution)
    out = i_math_outputs(params, execution)
    execution.run(args)
    return out

def i_math(
    image_dimension: IMathImageDimension,
    output_image: str,
    operations: str,
    image1: InputPathType,
    image2: InputPathType | None = None,
    runner: Runner | None = None,
) -> IMathOutputs:
    """
    iMath

    iMath is a tool for performing various image mathematical operations on medical images, specifically supporting operations on 2D, 3D, and 4D data.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Dimensionality of the image, either 2, 3, or 4.
        output_image: Path for the output image file.
        operations: Operations to be performed along with parameters, e.g.,\
            GetLargestComponent, MC for Closing, etc.
        image1: First input image file.
        image2: Second input image file, if required by operation.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = i_math_params(
        image_dimension=image_dimension,
        output_image=output_image,
        operations=operations,
        image1=image1,
        image2=image2,
    )
    return i_math_execute(params, runner)

__all__ = [
    "IMath",
    "IMathOutputs",
    "I_MATH_METADATA",
    "i_math_cargs",
    "i_math_outputs",
    "i_math_params",
    "i_math_execute",
    "i_math_validate",
    "i_math",
]