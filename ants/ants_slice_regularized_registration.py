# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_SLICE_REGULARIZED_REGISTRATION_METADATA = Metadata(
    id="antsSliceRegularizedRegistration",
    name="antsSliceRegularizedRegistration",
    package="ants",
)

AntsSliceRegularizedRegistrationInterpolation = typing.Literal["Linear"] | typing.Literal["NearestNeighbor"] | typing.Literal["MultiLabel"] | typing.Literal["Gaussian"] | typing.Literal["BSpline"] | typing.Literal["CosineWindowedSinc"] | typing.Literal["WelchWindowedSinc"] | typing.Literal["HammingWindowedSinc"] | typing.Literal["LanczosWindowedSinc"] | typing.Literal["GenericLabel"]

AntsSliceRegularizedRegistrationVerbose = typing.Literal[0]

AntsSliceRegularizedRegistration = typing.TypedDict(
    "AntsSliceRegularizedRegistration",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsSliceRegularizedRegistration"]],
        "polydegree": int,
        "output": str,
        "metric": str,
        "transform": str,
        "iterations": str,
        "shrink_factors": str,
        "smoothing_sigmas": str,
        "mask": typing.NotRequired[InputPathType],
        "interpolation": typing.NotRequired[AntsSliceRegularizedRegistrationInterpolation],
        "verbose": typing.NotRequired[AntsSliceRegularizedRegistrationVerbose],
    },
)

@dataclasses.dataclass
class AntsSliceRegularizedRegistrationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    polynomial_fit: OutputPathType
    """Output is the polynomial fit to Tx & Ty."""
    transformed_image: OutputPathType
    """Output is the transformed image."""

def ants_slice_regularized_registration_params(
    polydegree: int,
    output: str,
    metric: str,
    transform: str,
    iterations: str,
    shrink_factors: str,
    smoothing_sigmas: str,
    mask: InputPathType | None = None,
    interpolation: AntsSliceRegularizedRegistrationInterpolation | None = None,
    verbose: AntsSliceRegularizedRegistrationVerbose | None = None,
) -> AntsSliceRegularizedRegistration:
    """
    Build parameters.

    Args:
        polydegree: Degree of polynomial up to zDimension-2. Controls the\
            polynomial degree. 0 means no regularization.
        output: Specify the output transform prefix (output format is .nii.gz).\
            Optionally, one can choose to warp the moving image to the fixed space,\
            and if the inverse transform exists, one can also output the warped\
            fixed image.
        metric: Four image metrics are available: GC: global correlation, CC: ANTS\
            neighborhood cross correlation, MI: Mutual information, and\
            MeanSquares: mean-squares intensity difference.
        transform: Several transform options are available. The gradientStep or\
            learningRate characterizes the gradient descent optimization.
        iterations: Specify the number of iterations at each level.
        shrink_factors: Specify the shrink factor for the virtual domain (typically\
            the fixed image) at each level.
        smoothing_sigmas: Specify the amount of smoothing at each level.
        mask: Fixed image mask to limit voxels considered by the metric.
        interpolation: Several interpolation options are available in ITK.
        verbose: Verbose option.

    Returns:
        Parameter dictionary.
    """
    params: AntsSliceRegularizedRegistration = {
        "@type": "ants/antsSliceRegularizedRegistration",
        "polydegree": polydegree,
        "output": output,
        "metric": metric,
        "transform": transform,
        "iterations": iterations,
        "shrink_factors": shrink_factors,
        "smoothing_sigmas": smoothing_sigmas,
    }
    if mask is not None:
        params["mask"] = mask
    if interpolation is not None:
        params["interpolation"] = interpolation
    if verbose is not None:
        params["verbose"] = verbose
    return params

def ants_slice_regularized_registration_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsSliceRegularizedRegistration."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("polydegree", None) is None:
        raise StyxValidationError("`polydegree` must not be None")
    if not isinstance(params["polydegree"], int):
        raise StyxValidationError(f'`polydegree` has the wrong type: Received `{type(params["polydegree"])}` expected `int`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("metric", None) is None:
        raise StyxValidationError("`metric` must not be None")
    if not isinstance(params["metric"], str):
        raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')
    if params.get("transform", None) is None:
        raise StyxValidationError("`transform` must not be None")
    if not isinstance(params["transform"], str):
        raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `str`')
    if params.get("iterations", None) is None:
        raise StyxValidationError("`iterations` must not be None")
    if not isinstance(params["iterations"], str):
        raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `str`')
    if params.get("shrink_factors", None) is None:
        raise StyxValidationError("`shrink_factors` must not be None")
    if not isinstance(params["shrink_factors"], str):
        raise StyxValidationError(f'`shrink_factors` has the wrong type: Received `{type(params["shrink_factors"])}` expected `str`')
    if params.get("smoothing_sigmas", None) is None:
        raise StyxValidationError("`smoothing_sigmas` must not be None")
    if not isinstance(params["smoothing_sigmas"], str):
        raise StyxValidationError(f'`smoothing_sigmas` has the wrong type: Received `{type(params["smoothing_sigmas"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("interpolation", None) is not None:
        if not isinstance(params["interpolation"], str):
            raise StyxValidationError(f'`interpolation` has the wrong type: Received `{type(params["interpolation"])}` expected `AntsSliceRegularizedRegistrationInterpolation`')
        if params["interpolation"] not in ["Linear", "NearestNeighbor", "MultiLabel", "Gaussian", "BSpline", "CosineWindowedSinc", "WelchWindowedSinc", "HammingWindowedSinc", "LanczosWindowedSinc", "GenericLabel"]:
            raise StyxValidationError("Parameter `interpolation` must be one of [\"Linear\", \"NearestNeighbor\", \"MultiLabel\", \"Gaussian\", \"BSpline\", \"CosineWindowedSinc\", \"WelchWindowedSinc\", \"HammingWindowedSinc\", \"LanczosWindowedSinc\", \"GenericLabel\"]")
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], (float, int)):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `AntsSliceRegularizedRegistrationVerbose`')
        if params["verbose"] not in [0]:
            raise StyxValidationError("Parameter `verbose` must be one of [0]")

def ants_slice_regularized_registration_cargs(params: AntsSliceRegularizedRegistration, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsSliceRegularizedRegistration")
    cargs.append("-p")
    cargs.append(str(params["polydegree"]))
    cargs.append("-o")
    cargs.append(params["output"])
    cargs.append("-m")
    cargs.append(params["metric"])
    cargs.append("-t")
    cargs.append(params["transform"])
    cargs.append("-i")
    cargs.append(params["iterations"])
    cargs.append("-f")
    cargs.append(params["shrink_factors"])
    cargs.append("-s")
    cargs.append(params["smoothing_sigmas"])
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-x")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("interpolation")
    if v_1 is not None:
        cargs.append("-n")
        cargs.append(str(v_1))
    v_2 = params.get("verbose")
    if v_2 is not None:
        cargs.append("-v")
        cargs.append(str(v_2))
    return cargs

def ants_slice_regularized_registration_outputs(params: AntsSliceRegularizedRegistration, execution: Execution) -> AntsSliceRegularizedRegistrationOutputs:
    """Build the AntsSliceRegularizedRegistrationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    polynomial_fit_v: OutputPathType = execution.output_file("[OUTPUT_PREFIX]TxTy_poly.csv")
    transformed_image_v: OutputPathType = execution.output_file("[OUTPUT_PREFIX].nii.gz")
    return AntsSliceRegularizedRegistrationOutputs(
        root=root_v,
        polynomial_fit=polynomial_fit_v,
        transformed_image=transformed_image_v,
    )

def ants_slice_regularized_registration_execute(params: AntsSliceRegularizedRegistration, runner: Runner | None = None) -> AntsSliceRegularizedRegistrationOutputs:
    """
    antsSliceRegularizedRegistration

    This program is a user-level application for slice-by-slice translation registration. Results are regularized in z using polynomial regression. The program is targeted at spinal cord MRI. Only one stage is supported where a stage consists of a transform; an image metric; and iterations, shrink factors, and smoothing sigmas for each level. Specialized for 3D data: fixed image is 3D, moving image is 3D. Registration is performed slice-by-slice then regularized in z. The parameter -p controls the polynomial degree. -p 0 means no regularization.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_slice_regularized_registration_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_SLICE_REGULARIZED_REGISTRATION_METADATA)
    execution.params(params)
    args = ants_slice_regularized_registration_cargs(params, execution)
    out = ants_slice_regularized_registration_outputs(params, execution)
    execution.run(args)
    return out

def ants_slice_regularized_registration(
    polydegree: int,
    output: str,
    metric: str,
    transform: str,
    iterations: str,
    shrink_factors: str,
    smoothing_sigmas: str,
    mask: InputPathType | None = None,
    interpolation: AntsSliceRegularizedRegistrationInterpolation | None = None,
    verbose: AntsSliceRegularizedRegistrationVerbose | None = None,
    runner: Runner | None = None,
) -> AntsSliceRegularizedRegistrationOutputs:
    """
    antsSliceRegularizedRegistration

    This program is a user-level application for slice-by-slice translation registration. Results are regularized in z using polynomial regression. The program is targeted at spinal cord MRI. Only one stage is supported where a stage consists of a transform; an image metric; and iterations, shrink factors, and smoothing sigmas for each level. Specialized for 3D data: fixed image is 3D, moving image is 3D. Registration is performed slice-by-slice then regularized in z. The parameter -p controls the polynomial degree. -p 0 means no regularization.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        polydegree: Degree of polynomial up to zDimension-2. Controls the\
            polynomial degree. 0 means no regularization.
        output: Specify the output transform prefix (output format is .nii.gz).\
            Optionally, one can choose to warp the moving image to the fixed space,\
            and if the inverse transform exists, one can also output the warped\
            fixed image.
        metric: Four image metrics are available: GC: global correlation, CC: ANTS\
            neighborhood cross correlation, MI: Mutual information, and\
            MeanSquares: mean-squares intensity difference.
        transform: Several transform options are available. The gradientStep or\
            learningRate characterizes the gradient descent optimization.
        iterations: Specify the number of iterations at each level.
        shrink_factors: Specify the shrink factor for the virtual domain (typically\
            the fixed image) at each level.
        smoothing_sigmas: Specify the amount of smoothing at each level.
        mask: Fixed image mask to limit voxels considered by the metric.
        interpolation: Several interpolation options are available in ITK.
        verbose: Verbose option.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_slice_regularized_registration_params(
        polydegree=polydegree,
        output=output,
        metric=metric,
        transform=transform,
        iterations=iterations,
        shrink_factors=shrink_factors,
        smoothing_sigmas=smoothing_sigmas,
        mask=mask,
        interpolation=interpolation,
        verbose=verbose,
    )
    return ants_slice_regularized_registration_execute(params, runner)

__all__ = [
    "AntsSliceRegularizedRegistration",
    "AntsSliceRegularizedRegistrationOutputs",
    "ANTS_SLICE_REGULARIZED_REGISTRATION_METADATA",
    "ants_slice_regularized_registration_cargs",
    "ants_slice_regularized_registration_outputs",
    "ants_slice_regularized_registration_params",
    "ants_slice_regularized_registration_execute",
    "ants_slice_regularized_registration_validate",
    "ants_slice_regularized_registration",
]