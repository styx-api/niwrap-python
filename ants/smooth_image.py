# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SMOOTH_IMAGE_METADATA = Metadata(
    id="SmoothImage",
    name="SmoothImage",
    package="ants",
)

SmoothImage = typing.TypedDict(
    "SmoothImage",
    {
        "@type": typing.NotRequired[typing.Literal["ants/SmoothImage"]],
        "image_dimension": int,
        "image_ext": InputPathType,
        "smoothing_sigma": str,
        "out_image_ext": str,
        "sigma_units": typing.NotRequired[bool],
        "median_filter": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class SmoothImageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    smoothed_image: OutputPathType
    """The output smoothed image file."""

def smooth_image_params(
    image_dimension: int,
    image_ext: InputPathType,
    smoothing_sigma: str,
    out_image_ext: str,
    sigma_units: bool | None = None,
    median_filter: bool | None = None,
) -> SmoothImage:
    """
    Build parameters.

    Args:
        image_dimension: Specifies the dimensionality of the image.
        image_ext: The input image file to be smoothed.
        smoothing_sigma: The sigma value for smoothing. A separate sigma may be\
            specified for each dimension, e.g., '1.5x1x2'.
        out_image_ext: The output smoothed image file.
        sigma_units: Determines if sigma is in spacing units (1) or not (0).\
            Default is 0.
        median_filter: Whether to use median filter. Default is 0. If using median\
            filter, sigma represents the radius in voxels.

    Returns:
        Parameter dictionary.
    """
    params: SmoothImage = {
        "@type": "ants/SmoothImage",
        "image_dimension": image_dimension,
        "image_ext": image_ext,
        "smoothing_sigma": smoothing_sigma,
        "out_image_ext": out_image_ext,
    }
    if sigma_units is not None:
        params["sigma_units"] = sigma_units
    if median_filter is not None:
        params["median_filter"] = median_filter
    return params

def smooth_image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SmoothImage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("image_ext", None) is None:
        raise StyxValidationError("`image_ext` must not be None")
    if not isinstance(params["image_ext"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image_ext` has the wrong type: Received `{type(params["image_ext"])}` expected `InputPathType`')
    if params.get("smoothing_sigma", None) is None:
        raise StyxValidationError("`smoothing_sigma` must not be None")
    if not isinstance(params["smoothing_sigma"], str):
        raise StyxValidationError(f'`smoothing_sigma` has the wrong type: Received `{type(params["smoothing_sigma"])}` expected `str`')
    if params.get("out_image_ext", None) is None:
        raise StyxValidationError("`out_image_ext` must not be None")
    if not isinstance(params["out_image_ext"], str):
        raise StyxValidationError(f'`out_image_ext` has the wrong type: Received `{type(params["out_image_ext"])}` expected `str`')
    if params.get("sigma_units", None) is not None:
        if not isinstance(params["sigma_units"], bool):
            raise StyxValidationError(f'`sigma_units` has the wrong type: Received `{type(params["sigma_units"])}` expected `bool`')
    if params.get("median_filter", None) is not None:
        if not isinstance(params["median_filter"], bool):
            raise StyxValidationError(f'`median_filter` has the wrong type: Received `{type(params["median_filter"])}` expected `bool`')

def smooth_image_cargs(params: SmoothImage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SmoothImage")
    cargs.append(str(params["image_dimension"]))
    cargs.append(execution.input_file(params["image_ext"]))
    cargs.append(params["smoothing_sigma"])
    cargs.append(params["out_image_ext"])
    v_0 = params.get("sigma_units")
    if v_0 is not None:
        if v_0:
            cargs.append("0")
        else:
            cargs.append("1")
    v_1 = params.get("median_filter")
    if v_1 is not None:
        if v_1:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def smooth_image_outputs(params: SmoothImage, execution: Execution) -> SmoothImageOutputs:
    """Build the SmoothImageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    smoothed_image_v: OutputPathType = execution.output_file(params["out_image_ext"])
    return SmoothImageOutputs(
        root=root_v,
        smoothed_image=smoothed_image_v,
    )

def smooth_image_execute(params: SmoothImage, runner: Runner | None = None) -> SmoothImageOutputs:
    """
    SmoothImage

    SmoothImage allows smoothing of images with adjustable sigma values, offering optional median filtering functionality.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    smooth_image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SMOOTH_IMAGE_METADATA)
    execution.params(params)
    args = smooth_image_cargs(params, execution)
    out = smooth_image_outputs(params, execution)
    execution.run(args)
    return out

def smooth_image(
    image_dimension: int,
    image_ext: InputPathType,
    smoothing_sigma: str,
    out_image_ext: str,
    sigma_units: bool | None = None,
    median_filter: bool | None = None,
    runner: Runner | None = None,
) -> SmoothImageOutputs:
    """
    SmoothImage

    SmoothImage allows smoothing of images with adjustable sigma values, offering optional median filtering functionality.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Specifies the dimensionality of the image.
        image_ext: The input image file to be smoothed.
        smoothing_sigma: The sigma value for smoothing. A separate sigma may be\
            specified for each dimension, e.g., '1.5x1x2'.
        out_image_ext: The output smoothed image file.
        sigma_units: Determines if sigma is in spacing units (1) or not (0).\
            Default is 0.
        median_filter: Whether to use median filter. Default is 0. If using median\
            filter, sigma represents the radius in voxels.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = smooth_image_params(
        image_dimension=image_dimension,
        image_ext=image_ext,
        smoothing_sigma=smoothing_sigma,
        out_image_ext=out_image_ext,
        sigma_units=sigma_units,
        median_filter=median_filter,
    )
    return smooth_image_execute(params, runner)

__all__ = [
    "SmoothImage",
    "SmoothImageOutputs",
    "SMOOTH_IMAGE_METADATA",
    "smooth_image_cargs",
    "smooth_image_outputs",
    "smooth_image_params",
    "smooth_image_execute",
    "smooth_image_validate",
    "smooth_image",
]