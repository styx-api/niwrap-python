# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_MOTION_CORR_METADATA = Metadata(
    id="antsMotionCorr",
    name="antsMotionCorr",
    package="ants",
)

AntsMotionCorrDimensionality = typing.Literal[2] | typing.Literal[3]

AntsMotionCorrInterpolation = typing.Literal["Linear"] | typing.Literal["NearestNeighbor"] | typing.Literal["BSpline"] | typing.Literal["BlackmanWindowedSinc"] | typing.Literal["CosineWindowedSinc"] | typing.Literal["WelchWindowedSinc"] | typing.Literal["HammingWindowedSinc"] | typing.Literal["LanczosWindowedSinc"]

AntsMotionCorr = typing.TypedDict(
    "AntsMotionCorr",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsMotionCorr"]],
        "dimensionality": typing.NotRequired[AntsMotionCorrDimensionality],
        "n_images": typing.NotRequired[int],
        "metric": typing.NotRequired[str],
        "use_fixed_reference_image": typing.NotRequired[bool],
        "use_scales_estimator": typing.NotRequired[bool],
        "transform": typing.NotRequired[str],
        "iterations": typing.NotRequired[str],
        "smoothing_sigmas": typing.NotRequired[str],
        "shrink_factors": typing.NotRequired[str],
        "output": typing.NotRequired[str],
        "average_image": typing.NotRequired[bool],
        "write_displacement": typing.NotRequired[bool],
        "use_histogram_matching": typing.NotRequired[bool],
        "random_seed": typing.NotRequired[int],
        "interpolation": typing.NotRequired[AntsMotionCorrInterpolation],
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsMotionCorrOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_transform_prefix: OutputPathType
    """The output is the transformation matrix."""
    warped_image: OutputPathType
    """The output is the warped moving image."""
    average_image_output: OutputPathType
    """The output is the averaged image of the input time series."""

def ants_motion_corr_params(
    dimensionality: AntsMotionCorrDimensionality | None = None,
    n_images: int | None = None,
    metric: str | None = None,
    use_fixed_reference_image: bool | None = None,
    use_scales_estimator: bool = False,
    transform: str | None = None,
    iterations: str | None = None,
    smoothing_sigmas: str | None = None,
    shrink_factors: str | None = None,
    output: str | None = None,
    average_image: bool = False,
    write_displacement: bool = False,
    use_histogram_matching: bool | None = None,
    random_seed: int | None = None,
    interpolation: AntsMotionCorrInterpolation | None = None,
    verbose: bool | None = None,
) -> AntsMotionCorr:
    """
    Build parameters.

    Args:
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        n_images: This option sets the number of images to use to construct the\
            template image.
        metric: Metrics for registration: GC (global correlation), CC (ANTS\
            neighborhood cross correlation), MI (Mutual information), and Demons.
        use_fixed_reference_image: Use a fixed reference image to correct all\
            volumes, instead of correcting each image to the prior volume in the\
            time series.
        use_scales_estimator: Use the scale estimator to control optimization.
        transform: Several transform options are available: Affine, Rigid,\
            GaussianDisplacementField, SyN.
        iterations: Specify the number of iterations at each level.
        smoothing_sigmas: Specify the sigma for smoothing at each level. Smoothing\
            may be specified in mm units or voxels with 'AxBxCmm' or 'AxBxCvox'. No\
            units implies voxels.
        shrink_factors: Specify the shrink factor for the virtual domain (typically\
            the fixed image) at each level.
        output: Specify the output transform prefix (output format is .nii.gz ).\
            Optionally, one can choose to warp the moving image to the fixed space\
            and, if the inverse transform exists, one can also output the warped\
            fixed image.
        average_image: Average the input time series image.
        write_displacement: Write the low-dimensional 3D transforms to a 4D\
            displacement field.
        use_histogram_matching: Histogram match the moving images to the reference\
            image.
        random_seed: Use a fixed seed for random number generation.
        interpolation: Several interpolation options are available in ITK. The\
            above are available (default Linear).
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: AntsMotionCorr = {
        "@type": "ants/antsMotionCorr",
        "use_scales_estimator": use_scales_estimator,
        "average_image": average_image,
        "write_displacement": write_displacement,
    }
    if dimensionality is not None:
        params["dimensionality"] = dimensionality
    if n_images is not None:
        params["n_images"] = n_images
    if metric is not None:
        params["metric"] = metric
    if use_fixed_reference_image is not None:
        params["use_fixed_reference_image"] = use_fixed_reference_image
    if transform is not None:
        params["transform"] = transform
    if iterations is not None:
        params["iterations"] = iterations
    if smoothing_sigmas is not None:
        params["smoothing_sigmas"] = smoothing_sigmas
    if shrink_factors is not None:
        params["shrink_factors"] = shrink_factors
    if output is not None:
        params["output"] = output
    if use_histogram_matching is not None:
        params["use_histogram_matching"] = use_histogram_matching
    if random_seed is not None:
        params["random_seed"] = random_seed
    if interpolation is not None:
        params["interpolation"] = interpolation
    if verbose is not None:
        params["verbose"] = verbose
    return params

def ants_motion_corr_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsMotionCorr."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensionality", None) is not None:
        if not isinstance(params["dimensionality"], (float, int)):
            raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `AntsMotionCorrDimensionality`')
        if params["dimensionality"] not in [2, 3]:
            raise StyxValidationError("Parameter `dimensionality` must be one of [2, 3]")
    if params.get("n_images", None) is not None:
        if not isinstance(params["n_images"], int):
            raise StyxValidationError(f'`n_images` has the wrong type: Received `{type(params["n_images"])}` expected `int`')
    if params.get("metric", None) is not None:
        if not isinstance(params["metric"], str):
            raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')
    if params.get("use_fixed_reference_image", None) is not None:
        if not isinstance(params["use_fixed_reference_image"], bool):
            raise StyxValidationError(f'`use_fixed_reference_image` has the wrong type: Received `{type(params["use_fixed_reference_image"])}` expected `bool`')
    if params.get("use_scales_estimator", None) is not None:
        if not isinstance(params["use_scales_estimator"], bool):
            raise StyxValidationError(f'`use_scales_estimator` has the wrong type: Received `{type(params["use_scales_estimator"])}` expected `bool`')
    if params.get("transform", None) is not None:
        if not isinstance(params["transform"], str):
            raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `str`')
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], str):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `str`')
    if params.get("smoothing_sigmas", None) is not None:
        if not isinstance(params["smoothing_sigmas"], str):
            raise StyxValidationError(f'`smoothing_sigmas` has the wrong type: Received `{type(params["smoothing_sigmas"])}` expected `str`')
    if params.get("shrink_factors", None) is not None:
        if not isinstance(params["shrink_factors"], str):
            raise StyxValidationError(f'`shrink_factors` has the wrong type: Received `{type(params["shrink_factors"])}` expected `str`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("average_image", None) is not None:
        if not isinstance(params["average_image"], bool):
            raise StyxValidationError(f'`average_image` has the wrong type: Received `{type(params["average_image"])}` expected `bool`')
    if params.get("write_displacement", None) is not None:
        if not isinstance(params["write_displacement"], bool):
            raise StyxValidationError(f'`write_displacement` has the wrong type: Received `{type(params["write_displacement"])}` expected `bool`')
    if params.get("use_histogram_matching", None) is not None:
        if not isinstance(params["use_histogram_matching"], bool):
            raise StyxValidationError(f'`use_histogram_matching` has the wrong type: Received `{type(params["use_histogram_matching"])}` expected `bool`')
    if params.get("random_seed", None) is not None:
        if not isinstance(params["random_seed"], int):
            raise StyxValidationError(f'`random_seed` has the wrong type: Received `{type(params["random_seed"])}` expected `int`')
        if params["random_seed"] < 1:
            raise StyxValidationError("Parameter `random_seed` must be at least 1")
    if params.get("interpolation", None) is not None:
        if not isinstance(params["interpolation"], str):
            raise StyxValidationError(f'`interpolation` has the wrong type: Received `{type(params["interpolation"])}` expected `AntsMotionCorrInterpolation`')
        if params["interpolation"] not in ["Linear", "NearestNeighbor", "BSpline", "BlackmanWindowedSinc", "CosineWindowedSinc", "WelchWindowedSinc", "HammingWindowedSinc", "LanczosWindowedSinc"]:
            raise StyxValidationError("Parameter `interpolation` must be one of [\"Linear\", \"NearestNeighbor\", \"BSpline\", \"BlackmanWindowedSinc\", \"CosineWindowedSinc\", \"WelchWindowedSinc\", \"HammingWindowedSinc\", \"LanczosWindowedSinc\"]")
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def ants_motion_corr_cargs(params: AntsMotionCorr, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsMotionCorr")
    v_0 = params.get("dimensionality")
    if v_0 is not None:
        cargs.append("--dimensionality")
        cargs.append(str(v_0))
    v_1 = params.get("n_images")
    if v_1 is not None:
        cargs.append("--n-images")
        cargs.append(str(v_1))
    v_2 = params.get("metric")
    if v_2 is not None:
        cargs.append("--metric")
        cargs.append(v_2)
    v_3 = params.get("use_fixed_reference_image")
    if v_3 is not None:
        cargs.append("--useFixedReferenceImage")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    if params.get("use_scales_estimator"):
        cargs.append("--useScalesEstimator")
    v_4 = params.get("transform")
    if v_4 is not None:
        cargs.append("--transform")
        cargs.append(v_4)
    v_5 = params.get("iterations")
    if v_5 is not None:
        cargs.append("--iterations")
        cargs.append(v_5)
    v_6 = params.get("smoothing_sigmas")
    if v_6 is not None:
        cargs.append("--smoothingSigmas")
        cargs.append(v_6)
    v_7 = params.get("shrink_factors")
    if v_7 is not None:
        cargs.append("--shrinkFactors")
        cargs.append(v_7)
    v_8 = params.get("output")
    if v_8 is not None:
        cargs.append("--output")
        cargs.append(v_8)
    if params.get("average_image"):
        cargs.append("--average-image")
    if params.get("write_displacement"):
        cargs.append("--write-displacement")
    v_9 = params.get("use_histogram_matching")
    if v_9 is not None:
        cargs.append("--use-histogram-matching")
        if v_9:
            cargs.append("0")
        else:
            cargs.append("1")
    v_10 = params.get("random_seed")
    if v_10 is not None:
        cargs.append("--random-seed")
        cargs.append(str(v_10))
    v_11 = params.get("interpolation")
    if v_11 is not None:
        cargs.append("--interpolation")
        cargs.append(str(v_11))
    v_12 = params.get("verbose")
    if v_12 is not None:
        cargs.append("--verbose")
        if v_12:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def ants_motion_corr_outputs(params: AntsMotionCorr, execution: Execution) -> AntsMotionCorrOutputs:
    """Build the AntsMotionCorrOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_transform_prefix_v: OutputPathType = execution.output_file("[OUTPUT_TRANSFORM_PREFIX]Affine.mat")
    warped_image_v: OutputPathType = execution.output_file("[OUTPUT_TRANSFORM_PREFIX]Warped.nii.gz")
    average_image_output_v: OutputPathType = execution.output_file("[OUTPUT_TRANSFORM_PREFIX]Average.nii.gz")
    return AntsMotionCorrOutputs(
        root=root_v,
        output_transform_prefix=output_transform_prefix_v,
        warped_image=warped_image_v,
        average_image_output=average_image_output_v,
    )

def ants_motion_corr_execute(params: AntsMotionCorr, runner: Runner | None = None) -> AntsMotionCorrOutputs:
    """
    antsMotionCorr

    ANTS Motion Correction application to perform motion correction on 4D time series data.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_motion_corr_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_MOTION_CORR_METADATA)
    execution.params(params)
    args = ants_motion_corr_cargs(params, execution)
    out = ants_motion_corr_outputs(params, execution)
    execution.run(args)
    return out

def ants_motion_corr(
    dimensionality: AntsMotionCorrDimensionality | None = None,
    n_images: int | None = None,
    metric: str | None = None,
    use_fixed_reference_image: bool | None = None,
    use_scales_estimator: bool = False,
    transform: str | None = None,
    iterations: str | None = None,
    smoothing_sigmas: str | None = None,
    shrink_factors: str | None = None,
    output: str | None = None,
    average_image: bool = False,
    write_displacement: bool = False,
    use_histogram_matching: bool | None = None,
    random_seed: int | None = None,
    interpolation: AntsMotionCorrInterpolation | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AntsMotionCorrOutputs:
    """
    antsMotionCorr

    ANTS Motion Correction application to perform motion correction on 4D time series data.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        n_images: This option sets the number of images to use to construct the\
            template image.
        metric: Metrics for registration: GC (global correlation), CC (ANTS\
            neighborhood cross correlation), MI (Mutual information), and Demons.
        use_fixed_reference_image: Use a fixed reference image to correct all\
            volumes, instead of correcting each image to the prior volume in the\
            time series.
        use_scales_estimator: Use the scale estimator to control optimization.
        transform: Several transform options are available: Affine, Rigid,\
            GaussianDisplacementField, SyN.
        iterations: Specify the number of iterations at each level.
        smoothing_sigmas: Specify the sigma for smoothing at each level. Smoothing\
            may be specified in mm units or voxels with 'AxBxCmm' or 'AxBxCvox'. No\
            units implies voxels.
        shrink_factors: Specify the shrink factor for the virtual domain (typically\
            the fixed image) at each level.
        output: Specify the output transform prefix (output format is .nii.gz ).\
            Optionally, one can choose to warp the moving image to the fixed space\
            and, if the inverse transform exists, one can also output the warped\
            fixed image.
        average_image: Average the input time series image.
        write_displacement: Write the low-dimensional 3D transforms to a 4D\
            displacement field.
        use_histogram_matching: Histogram match the moving images to the reference\
            image.
        random_seed: Use a fixed seed for random number generation.
        interpolation: Several interpolation options are available in ITK. The\
            above are available (default Linear).
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_motion_corr_params(
        dimensionality=dimensionality,
        n_images=n_images,
        metric=metric,
        use_fixed_reference_image=use_fixed_reference_image,
        use_scales_estimator=use_scales_estimator,
        transform=transform,
        iterations=iterations,
        smoothing_sigmas=smoothing_sigmas,
        shrink_factors=shrink_factors,
        output=output,
        average_image=average_image,
        write_displacement=write_displacement,
        use_histogram_matching=use_histogram_matching,
        random_seed=random_seed,
        interpolation=interpolation,
        verbose=verbose,
    )
    return ants_motion_corr_execute(params, runner)

__all__ = [
    "AntsMotionCorr",
    "AntsMotionCorrOutputs",
    "ANTS_MOTION_CORR_METADATA",
    "ants_motion_corr_cargs",
    "ants_motion_corr_outputs",
    "ants_motion_corr_params",
    "ants_motion_corr_execute",
    "ants_motion_corr_validate",
    "ants_motion_corr",
]