# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CREATE_TILED_MOSAIC_METADATA = Metadata(
    id="CreateTiledMosaic",
    name="CreateTiledMosaic",
    package="ants",
)

CreateTiledMosaicDirection = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal["x"] | typing.Literal["y"] | typing.Literal["z"]

CreateTiledMosaic = typing.TypedDict(
    "CreateTiledMosaic",
    {
        "@type": typing.NotRequired[typing.Literal["ants/CreateTiledMosaic"]],
        "input_image": InputPathType,
        "rgb_image": typing.NotRequired[InputPathType],
        "mask_image": typing.NotRequired[InputPathType],
        "alpha": typing.NotRequired[float],
        "functional_overlay": typing.NotRequired[str],
        "output": str,
        "tile_geometry": typing.NotRequired[str],
        "direction": typing.NotRequired[CreateTiledMosaicDirection],
        "pad_or_crop": typing.NotRequired[str],
        "slices": typing.NotRequired[str],
        "flip_slice": typing.NotRequired[str],
        "permute_axes": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class CreateTiledMosaicOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tiled_mosaic_image: OutputPathType
    """The output is the tiled mosaic image."""

def create_tiled_mosaic_params(
    input_image: InputPathType,
    output: str,
    rgb_image: InputPathType | None = None,
    mask_image: InputPathType | None = None,
    alpha: float | None = None,
    functional_overlay: str | None = None,
    tile_geometry: str | None = None,
    direction: CreateTiledMosaicDirection | None = None,
    pad_or_crop: str | None = None,
    slices: str | None = None,
    flip_slice: str | None = None,
    permute_axes: bool | None = None,
) -> CreateTiledMosaic:
    """
    Build parameters.

    Args:
        input_image: Main input is a 3-D grayscale image.
        output: The output is the tiled mosaic image. The format must support the\
            specific data type: floating point images without RGB overlays, Rgb\
            images with intensities scaled to [0,255] if overlays are present.
        rgb_image: An optional Rgb image can be added as an overlay. It must have\
            the same image geometry as the input grayscale image.
        mask_image: Specifies the ROI of the RGB voxels used.
        alpha: If an Rgb image is provided, render the overlay using the specified\
            alpha parameter.
        functional_overlay: A functional overlay can be specified using both and\
            rgb image and a mask specifying where that rgb image should be applied.\
            Both images must have the same image geometry as the input image.\
            Optionally, an alpha parameter can be specified.
        tile_geometry: The tile geometry specifies the number of rows and columns\
            in the output image. For example, specifying '5x10' renders 5 rows by\
            10 columns of slices.
        direction: Specifies the direction of the slices. Can be based on image\
            storage in memory or aligned physical space. Defaults to z-direction if\
            unspecified.
        pad_or_crop: Specify padding or cropping with a voxel-width boundary for\
            each slice. Padding uses a specified constant value. Cropping pads with\
            negative voxel-widths.
        slices: Control over which slices to render. Specify slices directly or\
            incrementally with optional start and end slices.
        flip_slice: Flip individual slice images horizontally and/or vertically.
        permute_axes: Permute (or swap) the axes of the individual slice images.

    Returns:
        Parameter dictionary.
    """
    params: CreateTiledMosaic = {
        "@type": "ants/CreateTiledMosaic",
        "input_image": input_image,
        "output": output,
    }
    if rgb_image is not None:
        params["rgb_image"] = rgb_image
    if mask_image is not None:
        params["mask_image"] = mask_image
    if alpha is not None:
        params["alpha"] = alpha
    if functional_overlay is not None:
        params["functional_overlay"] = functional_overlay
    if tile_geometry is not None:
        params["tile_geometry"] = tile_geometry
    if direction is not None:
        params["direction"] = direction
    if pad_or_crop is not None:
        params["pad_or_crop"] = pad_or_crop
    if slices is not None:
        params["slices"] = slices
    if flip_slice is not None:
        params["flip_slice"] = flip_slice
    if permute_axes is not None:
        params["permute_axes"] = permute_axes
    return params

def create_tiled_mosaic_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CreateTiledMosaic."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("rgb_image", None) is not None:
        if not isinstance(params["rgb_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`rgb_image` has the wrong type: Received `{type(params["rgb_image"])}` expected `InputPathType`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("alpha", None) is not None:
        if not isinstance(params["alpha"], (float, int)):
            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["alpha"])}` expected `float`')
    if params.get("functional_overlay", None) is not None:
        if not isinstance(params["functional_overlay"], str):
            raise StyxValidationError(f'`functional_overlay` has the wrong type: Received `{type(params["functional_overlay"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("tile_geometry", None) is not None:
        if not isinstance(params["tile_geometry"], str):
            raise StyxValidationError(f'`tile_geometry` has the wrong type: Received `{type(params["tile_geometry"])}` expected `str`')
    if params.get("direction", None) is not None:
        if not isinstance(params["direction"], (float, int)):
            raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `CreateTiledMosaicDirection`')
        if params["direction"] not in [0, 1, 2, "x", "y", "z"]:
            raise StyxValidationError("Parameter `direction` must be one of [0, 1, 2, \"x\", \"y\", \"z\"]")
    if params.get("pad_or_crop", None) is not None:
        if not isinstance(params["pad_or_crop"], str):
            raise StyxValidationError(f'`pad_or_crop` has the wrong type: Received `{type(params["pad_or_crop"])}` expected `str`')
    if params.get("slices", None) is not None:
        if not isinstance(params["slices"], str):
            raise StyxValidationError(f'`slices` has the wrong type: Received `{type(params["slices"])}` expected `str`')
    if params.get("flip_slice", None) is not None:
        if not isinstance(params["flip_slice"], str):
            raise StyxValidationError(f'`flip_slice` has the wrong type: Received `{type(params["flip_slice"])}` expected `str`')
    if params.get("permute_axes", None) is not None:
        if not isinstance(params["permute_axes"], bool):
            raise StyxValidationError(f'`permute_axes` has the wrong type: Received `{type(params["permute_axes"])}` expected `bool`')

def create_tiled_mosaic_cargs(params: CreateTiledMosaic, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("CreateTiledMosaic")
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("rgb_image")
    if v_0 is not None:
        cargs.append("-r")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask_image")
    if v_1 is not None:
        cargs.append("-x")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("alpha")
    if v_2 is not None:
        cargs.append("-a")
        cargs.append(str(v_2))
    v_3 = params.get("functional_overlay")
    if v_3 is not None:
        cargs.append("-e")
        cargs.append(v_3)
    cargs.append("-o")
    cargs.append(params["output"])
    v_4 = params.get("tile_geometry")
    if v_4 is not None:
        cargs.append("-t")
        cargs.append(v_4)
    v_5 = params.get("direction")
    if v_5 is not None:
        cargs.append("-d")
        cargs.append(str(v_5))
    v_6 = params.get("pad_or_crop")
    if v_6 is not None:
        cargs.append("-p")
        cargs.append(v_6)
    v_7 = params.get("slices")
    if v_7 is not None:
        cargs.append("-s")
        cargs.append(v_7)
    v_8 = params.get("flip_slice")
    if v_8 is not None:
        cargs.append("-f")
        cargs.append(v_8)
    v_9 = params.get("permute_axes")
    if v_9 is not None:
        cargs.append("-g")
        if v_9:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def create_tiled_mosaic_outputs(params: CreateTiledMosaic, execution: Execution) -> CreateTiledMosaicOutputs:
    """Build the CreateTiledMosaicOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tiled_mosaic_image_v: OutputPathType = execution.output_file(params["output"])
    return CreateTiledMosaicOutputs(
        root=root_v,
        tiled_mosaic_image=tiled_mosaic_image_v,
    )

def create_tiled_mosaic_execute(params: CreateTiledMosaic, runner: Runner | None = None) -> CreateTiledMosaicOutputs:
    """
    CreateTiledMosaic

    Render a 3-D image volume with optional Rgb overlay.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    create_tiled_mosaic_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CREATE_TILED_MOSAIC_METADATA)
    execution.params(params)
    args = create_tiled_mosaic_cargs(params, execution)
    out = create_tiled_mosaic_outputs(params, execution)
    execution.run(args)
    return out

def create_tiled_mosaic(
    input_image: InputPathType,
    output: str,
    rgb_image: InputPathType | None = None,
    mask_image: InputPathType | None = None,
    alpha: float | None = None,
    functional_overlay: str | None = None,
    tile_geometry: str | None = None,
    direction: CreateTiledMosaicDirection | None = None,
    pad_or_crop: str | None = None,
    slices: str | None = None,
    flip_slice: str | None = None,
    permute_axes: bool | None = None,
    runner: Runner | None = None,
) -> CreateTiledMosaicOutputs:
    """
    CreateTiledMosaic

    Render a 3-D image volume with optional Rgb overlay.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        input_image: Main input is a 3-D grayscale image.
        output: The output is the tiled mosaic image. The format must support the\
            specific data type: floating point images without RGB overlays, Rgb\
            images with intensities scaled to [0,255] if overlays are present.
        rgb_image: An optional Rgb image can be added as an overlay. It must have\
            the same image geometry as the input grayscale image.
        mask_image: Specifies the ROI of the RGB voxels used.
        alpha: If an Rgb image is provided, render the overlay using the specified\
            alpha parameter.
        functional_overlay: A functional overlay can be specified using both and\
            rgb image and a mask specifying where that rgb image should be applied.\
            Both images must have the same image geometry as the input image.\
            Optionally, an alpha parameter can be specified.
        tile_geometry: The tile geometry specifies the number of rows and columns\
            in the output image. For example, specifying '5x10' renders 5 rows by\
            10 columns of slices.
        direction: Specifies the direction of the slices. Can be based on image\
            storage in memory or aligned physical space. Defaults to z-direction if\
            unspecified.
        pad_or_crop: Specify padding or cropping with a voxel-width boundary for\
            each slice. Padding uses a specified constant value. Cropping pads with\
            negative voxel-widths.
        slices: Control over which slices to render. Specify slices directly or\
            incrementally with optional start and end slices.
        flip_slice: Flip individual slice images horizontally and/or vertically.
        permute_axes: Permute (or swap) the axes of the individual slice images.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = create_tiled_mosaic_params(
        input_image=input_image,
        output=output,
        rgb_image=rgb_image,
        mask_image=mask_image,
        alpha=alpha,
        functional_overlay=functional_overlay,
        tile_geometry=tile_geometry,
        direction=direction,
        pad_or_crop=pad_or_crop,
        slices=slices,
        flip_slice=flip_slice,
        permute_axes=permute_axes,
    )
    return create_tiled_mosaic_execute(params, runner)

__all__ = [
    "CreateTiledMosaic",
    "CreateTiledMosaicOutputs",
    "CREATE_TILED_MOSAIC_METADATA",
    "create_tiled_mosaic_cargs",
    "create_tiled_mosaic_outputs",
    "create_tiled_mosaic_params",
    "create_tiled_mosaic_execute",
    "create_tiled_mosaic_validate",
    "create_tiled_mosaic",
]