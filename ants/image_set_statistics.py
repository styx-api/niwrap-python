# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

IMAGE_SET_STATISTICS_METADATA = Metadata(
    id="ImageSetStatistics",
    name="ImageSetStatistics",
    package="ants",
)

ImageSetStatisticsWhichStat = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4] | typing.Literal[5] | typing.Literal[6] | typing.Literal[7]

ImageSetStatistics = typing.TypedDict(
    "ImageSetStatistics",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ImageSetStatistics"]],
        "image_dimension": int,
        "controls_list": InputPathType,
        "output_image": str,
        "which_stat": ImageSetStatisticsWhichStat,
        "roi": typing.NotRequired[InputPathType],
        "imagelist2": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class ImageSetStatisticsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    computed_statistics_image: OutputPathType
    """The output image containing the computed statistics."""

def image_set_statistics_params(
    image_dimension: int,
    controls_list: InputPathType,
    output_image: str,
    which_stat: ImageSetStatisticsWhichStat,
    roi: InputPathType | None = None,
    imagelist2: InputPathType | None = None,
) -> ImageSetStatistics:
    """
    Build parameters.

    Args:
        image_dimension: The dimensionality of the images to be processed by\
            ImageSetStatistics.
        controls_list: Text file containing the list of control images.
        output_image: The output image file where the computed statistics are\
            stored.
        which_stat: Choice of statistic to compute: 0 for median, 1 for max\
            probability appearance, 2 for weighted mean appearance, 3 for trimmed\
            mean, 4 for max value, 5 for similarity-weighted (requires imagelist2),\
            6 for best local match label, 7 for max value from ROI.
        roi: Region of interest image file, optional depending on the whichstat\
            option.
        imagelist2: List of similarity images used for similarity-weighted\
            statistics. Required if whichstat equals 5 or 6.

    Returns:
        Parameter dictionary.
    """
    params: ImageSetStatistics = {
        "@type": "ants/ImageSetStatistics",
        "image_dimension": image_dimension,
        "controls_list": controls_list,
        "output_image": output_image,
        "which_stat": which_stat,
    }
    if roi is not None:
        params["roi"] = roi
    if imagelist2 is not None:
        params["imagelist2"] = imagelist2
    return params

def image_set_statistics_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ImageSetStatistics."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("controls_list", None) is None:
        raise StyxValidationError("`controls_list` must not be None")
    if not isinstance(params["controls_list"], (pathlib.Path, str)):
        raise StyxValidationError(f'`controls_list` has the wrong type: Received `{type(params["controls_list"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("which_stat", None) is None:
        raise StyxValidationError("`which_stat` must not be None")
    if not isinstance(params["which_stat"], (float, int)):
        raise StyxValidationError(f'`which_stat` has the wrong type: Received `{type(params["which_stat"])}` expected `ImageSetStatisticsWhichStat`')
    if params["which_stat"] not in [0, 1, 2, 3, 4, 5, 6, 7]:
        raise StyxValidationError("Parameter `which_stat` must be one of [0, 1, 2, 3, 4, 5, 6, 7]")
    if params.get("roi", None) is not None:
        if not isinstance(params["roi"], (pathlib.Path, str)):
            raise StyxValidationError(f'`roi` has the wrong type: Received `{type(params["roi"])}` expected `InputPathType`')
    if params.get("imagelist2", None) is not None:
        if not isinstance(params["imagelist2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`imagelist2` has the wrong type: Received `{type(params["imagelist2"])}` expected `InputPathType`')

def image_set_statistics_cargs(params: ImageSetStatistics, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ImageSetStatistics")
    cargs.append(str(params["image_dimension"]))
    cargs.append("[CONTROLS_LIST]")
    cargs.append(execution.input_file(params["controls_list"]))
    cargs.append("[OUTPUT_IMAGE]")
    cargs.append(params["output_image"])
    cargs.append("[WHICH_STAT]")
    cargs.append(str(params["which_stat"]))
    v_0 = params.get("roi")
    if v_0 is not None:
        cargs.append("[ROI]")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("imagelist2")
    if v_1 is not None:
        cargs.append("[IMAGELIST2]")
        cargs.append(execution.input_file(v_1))
    return cargs

def image_set_statistics_outputs(params: ImageSetStatistics, execution: Execution) -> ImageSetStatisticsOutputs:
    """Build the ImageSetStatisticsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    computed_statistics_image_v: OutputPathType = execution.output_file(params["output_image"])
    return ImageSetStatisticsOutputs(
        root=root_v,
        computed_statistics_image=computed_statistics_image_v,
    )

def image_set_statistics_execute(params: ImageSetStatistics, runner: Runner | None = None) -> ImageSetStatisticsOutputs:
    """
    ImageSetStatistics

    ImageSetStatistics computes statistics from a set of images. The whichstat option defines the type of statistic to compute, ranging from median to similarity-weighted metrics.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    image_set_statistics_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(IMAGE_SET_STATISTICS_METADATA)
    execution.params(params)
    args = image_set_statistics_cargs(params, execution)
    out = image_set_statistics_outputs(params, execution)
    execution.run(args)
    return out

def image_set_statistics(
    image_dimension: int,
    controls_list: InputPathType,
    output_image: str,
    which_stat: ImageSetStatisticsWhichStat,
    roi: InputPathType | None = None,
    imagelist2: InputPathType | None = None,
    runner: Runner | None = None,
) -> ImageSetStatisticsOutputs:
    """
    ImageSetStatistics

    ImageSetStatistics computes statistics from a set of images. The whichstat option defines the type of statistic to compute, ranging from median to similarity-weighted metrics.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: The dimensionality of the images to be processed by\
            ImageSetStatistics.
        controls_list: Text file containing the list of control images.
        output_image: The output image file where the computed statistics are\
            stored.
        which_stat: Choice of statistic to compute: 0 for median, 1 for max\
            probability appearance, 2 for weighted mean appearance, 3 for trimmed\
            mean, 4 for max value, 5 for similarity-weighted (requires imagelist2),\
            6 for best local match label, 7 for max value from ROI.
        roi: Region of interest image file, optional depending on the whichstat\
            option.
        imagelist2: List of similarity images used for similarity-weighted\
            statistics. Required if whichstat equals 5 or 6.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = image_set_statistics_params(
        image_dimension=image_dimension,
        controls_list=controls_list,
        output_image=output_image,
        which_stat=which_stat,
        roi=roi,
        imagelist2=imagelist2,
    )
    return image_set_statistics_execute(params, runner)

__all__ = [
    "ImageSetStatistics",
    "ImageSetStatisticsOutputs",
    "IMAGE_SET_STATISTICS_METADATA",
    "image_set_statistics_cargs",
    "image_set_statistics_outputs",
    "image_set_statistics_params",
    "image_set_statistics_execute",
    "image_set_statistics_validate",
    "image_set_statistics",
]