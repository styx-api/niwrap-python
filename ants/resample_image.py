# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

RESAMPLE_IMAGE_METADATA = Metadata(
    id="ResampleImage",
    name="ResampleImage",
    package="ants",
)

ResampleImageInterpolateType = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

ResampleImagePixeltype = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4] | typing.Literal[5] | typing.Literal[6] | typing.Literal[7]

ResampleImage = typing.TypedDict(
    "ResampleImage",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ResampleImage"]],
        "image_dimension": int,
        "input_image": InputPathType,
        "output_image": str,
        "size_spacing": str,
        "interpolate_type": typing.NotRequired[ResampleImageInterpolateType],
        "pixeltype": typing.NotRequired[ResampleImagePixeltype],
    },
)

@dataclasses.dataclass
class ResampleImageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    resampled_output_image: OutputPathType
    """The resultant image after resampling."""

def resample_image_params(
    image_dimension: int,
    input_image: InputPathType,
    output_image: str,
    size_spacing: str,
    interpolate_type: ResampleImageInterpolateType | None = None,
    pixeltype: ResampleImagePixeltype | None = None,
) -> ResampleImage:
    """
    Build parameters.

    Args:
        image_dimension: Dimension of the image to be resampled.
        input_image: The image file to be resampled.
        output_image: The output image file after resampling.
        size_spacing: Resampling size and spacing specification, e.g., 'MxNxO'.
        interpolate_type: Specifies the interpolation type. 0: linear (default), 1:\
            nearest-neighbor, 2: gaussian, 3: windowedSinc, 4: B-Spline.
        pixeltype: Specifies the pixel type of the output image. 0: char, 1:\
            unsigned char, 2: short, 3: unsigned short, 4: int, 5: unsigned int, 6:\
            float (default), 7: double.

    Returns:
        Parameter dictionary.
    """
    params: ResampleImage = {
        "@type": "ants/ResampleImage",
        "image_dimension": image_dimension,
        "input_image": input_image,
        "output_image": output_image,
        "size_spacing": size_spacing,
    }
    if interpolate_type is not None:
        params["interpolate_type"] = interpolate_type
    if pixeltype is not None:
        params["pixeltype"] = pixeltype
    return params

def resample_image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid ResampleImage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("size_spacing", None) is None:
        raise StyxValidationError("`size_spacing` must not be None")
    if not isinstance(params["size_spacing"], str):
        raise StyxValidationError(f'`size_spacing` has the wrong type: Received `{type(params["size_spacing"])}` expected `str`')
    if params.get("interpolate_type", None) is not None:
        if not isinstance(params["interpolate_type"], (float, int)):
            raise StyxValidationError(f'`interpolate_type` has the wrong type: Received `{type(params["interpolate_type"])}` expected `ResampleImageInterpolateType`')
        if params["interpolate_type"] not in [0, 1, 2, 3, 4]:
            raise StyxValidationError("Parameter `interpolate_type` must be one of [0, 1, 2, 3, 4]")
    if params.get("pixeltype", None) is not None:
        if not isinstance(params["pixeltype"], (float, int)):
            raise StyxValidationError(f'`pixeltype` has the wrong type: Received `{type(params["pixeltype"])}` expected `ResampleImagePixeltype`')
        if params["pixeltype"] not in [0, 1, 2, 3, 4, 5, 6, 7]:
            raise StyxValidationError("Parameter `pixeltype` must be one of [0, 1, 2, 3, 4, 5, 6, 7]")

def resample_image_cargs(params: ResampleImage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ResampleImage")
    cargs.append(str(params["image_dimension"]))
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    cargs.append(params["size_spacing"])
    v_0 = params.get("interpolate_type")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("pixeltype")
    if v_1 is not None:
        cargs.append(str(v_1))
    return cargs

def resample_image_outputs(params: ResampleImage, execution: Execution) -> ResampleImageOutputs:
    """Build the ResampleImageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    resampled_output_image_v: OutputPathType = execution.output_file(params["output_image"])
    return ResampleImageOutputs(
        root=root_v,
        resampled_output_image=resampled_output_image_v,
    )

def resample_image_execute(params: ResampleImage, runner: Runner | None = None) -> ResampleImageOutputs:
    """
    ResampleImage

    ResampleImage is a tool used to resample images to specified sizes and spacings, using various interpolation methods and pixel types.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    resample_image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(RESAMPLE_IMAGE_METADATA)
    execution.params(params)
    args = resample_image_cargs(params, execution)
    out = resample_image_outputs(params, execution)
    execution.run(args)
    return out

def resample_image(
    image_dimension: int,
    input_image: InputPathType,
    output_image: str,
    size_spacing: str,
    interpolate_type: ResampleImageInterpolateType | None = None,
    pixeltype: ResampleImagePixeltype | None = None,
    runner: Runner | None = None,
) -> ResampleImageOutputs:
    """
    ResampleImage

    ResampleImage is a tool used to resample images to specified sizes and spacings, using various interpolation methods and pixel types.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Dimension of the image to be resampled.
        input_image: The image file to be resampled.
        output_image: The output image file after resampling.
        size_spacing: Resampling size and spacing specification, e.g., 'MxNxO'.
        interpolate_type: Specifies the interpolation type. 0: linear (default), 1:\
            nearest-neighbor, 2: gaussian, 3: windowedSinc, 4: B-Spline.
        pixeltype: Specifies the pixel type of the output image. 0: char, 1:\
            unsigned char, 2: short, 3: unsigned short, 4: int, 5: unsigned int, 6:\
            float (default), 7: double.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = resample_image_params(
        image_dimension=image_dimension,
        input_image=input_image,
        output_image=output_image,
        size_spacing=size_spacing,
        interpolate_type=interpolate_type,
        pixeltype=pixeltype,
    )
    return resample_image_execute(params, runner)

__all__ = [
    "ResampleImage",
    "ResampleImageOutputs",
    "RESAMPLE_IMAGE_METADATA",
    "resample_image_cargs",
    "resample_image_outputs",
    "resample_image_params",
    "resample_image_execute",
    "resample_image_validate",
    "resample_image",
]