# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SUPER_RESOLUTION_METADATA = Metadata(
    id="SuperResolution",
    name="SuperResolution",
    package="ants",
)

SuperResolution = typing.TypedDict(
    "SuperResolution",
    {
        "@type": typing.NotRequired[typing.Literal["ants/SuperResolution"]],
        "image_dimension": int,
        "output_image": str,
        "domain_image": InputPathType,
        "gradient_sigma": float,
        "mesh_size": float,
        "number_of_levels": int,
        "input_image_files": list[InputPathType],
    },
)

@dataclasses.dataclass
class SuperResolutionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    super_resolved_image: OutputPathType
    """The output super-resolved image file."""

def super_resolution_params(
    image_dimension: int,
    output_image: str,
    domain_image: InputPathType,
    gradient_sigma: float,
    mesh_size: float,
    number_of_levels: int,
    input_image_files: list[InputPathType],
) -> SuperResolution:
    """
    Build parameters.

    Args:
        image_dimension: Specifies the dimensionality of the input images (e.g., 2\
            for 2D images, 3 for 3D images).
        output_image: The file path for the output super-resolved image.
        domain_image: The domain image is used as the template space for the\
            alignment of input images.
        gradient_sigma: The sigma used for calculating the gradient magnitude of\
            input images. If negative, no weighting is applied.
        mesh_size: The size of the mesh used in fitting.
        number_of_levels: The number of resolution levels to process.
        input_image_files: List of paths to input images to be processed for super\
            resolution.

    Returns:
        Parameter dictionary.
    """
    params: SuperResolution = {
        "@type": "ants/SuperResolution",
        "image_dimension": image_dimension,
        "output_image": output_image,
        "domain_image": domain_image,
        "gradient_sigma": gradient_sigma,
        "mesh_size": mesh_size,
        "number_of_levels": number_of_levels,
        "input_image_files": input_image_files,
    }
    return params

def super_resolution_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SuperResolution."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("domain_image", None) is None:
        raise StyxValidationError("`domain_image` must not be None")
    if not isinstance(params["domain_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`domain_image` has the wrong type: Received `{type(params["domain_image"])}` expected `InputPathType`')
    if params.get("gradient_sigma", None) is None:
        raise StyxValidationError("`gradient_sigma` must not be None")
    if not isinstance(params["gradient_sigma"], (float, int)):
        raise StyxValidationError(f'`gradient_sigma` has the wrong type: Received `{type(params["gradient_sigma"])}` expected `float`')
    if params.get("mesh_size", None) is None:
        raise StyxValidationError("`mesh_size` must not be None")
    if not isinstance(params["mesh_size"], (float, int)):
        raise StyxValidationError(f'`mesh_size` has the wrong type: Received `{type(params["mesh_size"])}` expected `float`')
    if params.get("number_of_levels", None) is None:
        raise StyxValidationError("`number_of_levels` must not be None")
    if not isinstance(params["number_of_levels"], int):
        raise StyxValidationError(f'`number_of_levels` has the wrong type: Received `{type(params["number_of_levels"])}` expected `int`')
    if params.get("input_image_files", None) is None:
        raise StyxValidationError("`input_image_files` must not be None")
    if not isinstance(params["input_image_files"], list):
        raise StyxValidationError(f'`input_image_files` has the wrong type: Received `{type(params["input_image_files"])}` expected `list[InputPathType]`')
    for e in params["input_image_files"]:
        if not isinstance(e, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_image_files` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')

def super_resolution_cargs(params: SuperResolution, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SuperResolution")
    cargs.append(str(params["image_dimension"]))
    cargs.append(params["output_image"])
    cargs.append(execution.input_file(params["domain_image"]))
    cargs.append(str(params["gradient_sigma"]))
    cargs.append(str(params["mesh_size"]))
    cargs.append(str(params["number_of_levels"]))
    for item0 in params["input_image_files"]:
        cargs.append(execution.input_file(item0))
    return cargs

def super_resolution_outputs(params: SuperResolution, execution: Execution) -> SuperResolutionOutputs:
    """Build the SuperResolutionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    super_resolved_image_v: OutputPathType = execution.output_file(params["output_image"])
    return SuperResolutionOutputs(
        root=root_v,
        super_resolved_image=super_resolved_image_v,
    )

def super_resolution_execute(params: SuperResolution, runner: Runner | None = None) -> SuperResolutionOutputs:
    """
    SuperResolution

    The SuperResolution tool enhances the spatial resolution of input images. The 'gradientSigma' parameter is used in calculating the gradient magnitude of the input images for weighting the voxel points during fitting. If a negative 'gradient' sigma is specified then no weighting is used.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    super_resolution_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SUPER_RESOLUTION_METADATA)
    execution.params(params)
    args = super_resolution_cargs(params, execution)
    out = super_resolution_outputs(params, execution)
    execution.run(args)
    return out

def super_resolution(
    image_dimension: int,
    output_image: str,
    domain_image: InputPathType,
    gradient_sigma: float,
    mesh_size: float,
    number_of_levels: int,
    input_image_files: list[InputPathType],
    runner: Runner | None = None,
) -> SuperResolutionOutputs:
    """
    SuperResolution

    The SuperResolution tool enhances the spatial resolution of input images. The 'gradientSigma' parameter is used in calculating the gradient magnitude of the input images for weighting the voxel points during fitting. If a negative 'gradient' sigma is specified then no weighting is used.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Specifies the dimensionality of the input images (e.g., 2\
            for 2D images, 3 for 3D images).
        output_image: The file path for the output super-resolved image.
        domain_image: The domain image is used as the template space for the\
            alignment of input images.
        gradient_sigma: The sigma used for calculating the gradient magnitude of\
            input images. If negative, no weighting is applied.
        mesh_size: The size of the mesh used in fitting.
        number_of_levels: The number of resolution levels to process.
        input_image_files: List of paths to input images to be processed for super\
            resolution.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = super_resolution_params(
        image_dimension=image_dimension,
        output_image=output_image,
        domain_image=domain_image,
        gradient_sigma=gradient_sigma,
        mesh_size=mesh_size,
        number_of_levels=number_of_levels,
        input_image_files=input_image_files,
    )
    return super_resolution_execute(params, runner)

__all__ = [
    "SuperResolution",
    "SuperResolutionOutputs",
    "SUPER_RESOLUTION_METADATA",
    "super_resolution_cargs",
    "super_resolution_outputs",
    "super_resolution_params",
    "super_resolution_execute",
    "super_resolution_validate",
    "super_resolution",
]