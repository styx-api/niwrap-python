# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MULTIPLY_IMAGES_METADATA = Metadata(
    id="MultiplyImages",
    name="MultiplyImages",
    package="ants",
)

MultiplyImagesDimension = typing.Literal[3] | typing.Literal[2]

MultiplyImages = typing.TypedDict(
    "MultiplyImages",
    {
        "@type": typing.NotRequired[typing.Literal["ants/MultiplyImages"]],
        "dimension": MultiplyImagesDimension,
        "first_input": InputPathType,
        "second_input": typing.NotRequired[InputPathType],
        "second_input_2": typing.NotRequired[float],
        "output_product_image": str,
        "num_threads": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class MultiplyImagesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_product_image_outfile: OutputPathType
    """Average image file."""

def multiply_images_params(
    dimension: MultiplyImagesDimension,
    first_input: InputPathType,
    output_product_image: str,
    second_input: InputPathType | None = None,
    second_input_2: float | None = None,
    num_threads: int | None = None,
) -> MultiplyImages:
    """
    Build parameters.

    Args:
        dimension: 3 or 2. Image dimension (2 or 3).
        first_input: Image 1.
        output_product_image: Outputfname.nii.gz: the name of the resulting image.
        second_input: file or string or a float. Image 2 or multiplication weight.
        second_input_2: file or string or a float. Image 2 or multiplication\
            weight.
        num_threads: Number of itk threads to use.

    Returns:
        Parameter dictionary.
    """
    params: MultiplyImages = {
        "@type": "ants/MultiplyImages",
        "dimension": dimension,
        "first_input": first_input,
        "output_product_image": output_product_image,
    }
    if second_input is not None:
        params["second_input"] = second_input
    if second_input_2 is not None:
        params["second_input_2"] = second_input_2
    if num_threads is not None:
        params["num_threads"] = num_threads
    return params

def multiply_images_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid MultiplyImages."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimension", None) is None:
        raise StyxValidationError("`dimension` must not be None")
    if not isinstance(params["dimension"], (float, int)):
        raise StyxValidationError(f'`dimension` has the wrong type: Received `{type(params["dimension"])}` expected `MultiplyImagesDimension`')
    if params["dimension"] not in [3, 2]:
        raise StyxValidationError("Parameter `dimension` must be one of [3, 2]")
    if params.get("first_input", None) is None:
        raise StyxValidationError("`first_input` must not be None")
    if not isinstance(params["first_input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`first_input` has the wrong type: Received `{type(params["first_input"])}` expected `InputPathType`')
    if params.get("second_input", None) is not None:
        if not isinstance(params["second_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`second_input` has the wrong type: Received `{type(params["second_input"])}` expected `InputPathType`')
    if params.get("second_input_2", None) is not None:
        if not isinstance(params["second_input_2"], (float, int)):
            raise StyxValidationError(f'`second_input_2` has the wrong type: Received `{type(params["second_input_2"])}` expected `float`')
    if params.get("output_product_image", None) is None:
        raise StyxValidationError("`output_product_image` must not be None")
    if not isinstance(params["output_product_image"], str):
        raise StyxValidationError(f'`output_product_image` has the wrong type: Received `{type(params["output_product_image"])}` expected `str`')
    if params.get("num_threads", None) is not None:
        if not isinstance(params["num_threads"], int):
            raise StyxValidationError(f'`num_threads` has the wrong type: Received `{type(params["num_threads"])}` expected `int`')

def multiply_images_cargs(params: MultiplyImages, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("MultiplyImages")
    cargs.append(str(params["dimension"]))
    cargs.append(execution.input_file(params["first_input"]))
    v_0 = params.get("second_input")
    if v_0 is not None:
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("second_input_2")
    if v_1 is not None:
        cargs.append(str(v_1))
    cargs.append(params["output_product_image"])
    v_2 = params.get("num_threads")
    if v_2 is not None:
        cargs.append(str(v_2))
    return cargs

def multiply_images_outputs(params: MultiplyImages, execution: Execution) -> MultiplyImagesOutputs:
    """Build the MultiplyImagesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_product_image_outfile_v: OutputPathType = execution.output_file(params["output_product_image"])
    return MultiplyImagesOutputs(
        root=root_v,
        output_product_image_outfile=output_product_image_outfile_v,
    )

def multiply_images_execute(params: MultiplyImages, runner: Runner | None = None) -> MultiplyImagesOutputs:
    """
    MultiplyImages

    Multiply 2 images; 2nd image file may also be floating point numerical value, and program will act accordingly -- i.e. read as a number. Program handles vector and tensor images as well

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    multiply_images_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MULTIPLY_IMAGES_METADATA)
    execution.params(params)
    args = multiply_images_cargs(params, execution)
    out = multiply_images_outputs(params, execution)
    execution.run(args)
    return out

def multiply_images(
    dimension: MultiplyImagesDimension,
    first_input: InputPathType,
    output_product_image: str,
    second_input: InputPathType | None = None,
    second_input_2: float | None = None,
    num_threads: int | None = None,
    runner: Runner | None = None,
) -> MultiplyImagesOutputs:
    """
    MultiplyImages

    Multiply 2 images; 2nd image file may also be floating point numerical value, and program will act accordingly -- i.e. read as a number. Program handles vector and tensor images as well

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimension: 3 or 2. Image dimension (2 or 3).
        first_input: Image 1.
        output_product_image: Outputfname.nii.gz: the name of the resulting image.
        second_input: file or string or a float. Image 2 or multiplication weight.
        second_input_2: file or string or a float. Image 2 or multiplication\
            weight.
        num_threads: Number of itk threads to use.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = multiply_images_params(
        dimension=dimension,
        first_input=first_input,
        output_product_image=output_product_image,
        second_input=second_input,
        second_input_2=second_input_2,
        num_threads=num_threads,
    )
    return multiply_images_execute(params, runner)

__all__ = [
    "MultiplyImages",
    "MultiplyImagesOutputs",
    "MULTIPLY_IMAGES_METADATA",
    "multiply_images_cargs",
    "multiply_images_outputs",
    "multiply_images_params",
    "multiply_images_execute",
    "multiply_images_validate",
    "multiply_images",
]