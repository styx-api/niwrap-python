# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_USE_LANDMARK_IMAGES_TO_GET_B_SPLINE_DISPLACEMENT_FIELD_METADATA = Metadata(
    id="ANTSUseLandmarkImagesToGetBSplineDisplacementField",
    name="ANTSUseLandmarkImagesToGetBSplineDisplacementField",
    package="ants",
)

AntsUseLandmarkImagesToGetBSplineDisplacementField = typing.TypedDict(
    "AntsUseLandmarkImagesToGetBSplineDisplacementField",
    {
        "@type": typing.NotRequired[typing.Literal["ants/ANTSUseLandmarkImagesToGetBSplineDisplacementField"]],
        "fixed_image_with_labeled_landmarks": InputPathType,
        "moving_image_with_labeled_landmarks": InputPathType,
        "output_displacement_field": str,
        "mesh_size": str,
        "number_of_levels": int,
        "order": typing.NotRequired[int],
        "enforce_stationary_boundaries": typing.NotRequired[int],
        "landmark_weights": typing.NotRequired[InputPathType],
    },
)

@dataclasses.dataclass
class AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    displacement_field: OutputPathType
    """The resultant displacement field for the registration process."""

def ants_use_landmark_images_to_get_b_spline_displacement_field_params(
    fixed_image_with_labeled_landmarks: InputPathType,
    moving_image_with_labeled_landmarks: InputPathType,
    output_displacement_field: str,
    mesh_size: str,
    number_of_levels: int,
    order: int | None = None,
    enforce_stationary_boundaries: int | None = None,
    landmark_weights: InputPathType | None = None,
) -> AntsUseLandmarkImagesToGetBSplineDisplacementField:
    """
    Build parameters.

    Args:
        fixed_image_with_labeled_landmarks: The fixed image with labeled landmarks.
        moving_image_with_labeled_landmarks: The moving image with labeled\
            landmarks.
        output_displacement_field: The output displacement field file.
        mesh_size: The mesh size specified as meshSize[0]xmeshSize[1]x...
        number_of_levels: The number of levels in the B-spline hierarchy.
        order: The order of the B-spline (default is 3).
        enforce_stationary_boundaries: Whether to enforce stationary boundaries\
            (default is 1).
        landmark_weights: Optional text file containing landmark weights. Each row\
            is either "label,labelWeight" or "labelWeight". If the latter format is\
            used, the weights are assumed to be arranged in ascending order by\
            label.

    Returns:
        Parameter dictionary.
    """
    params: AntsUseLandmarkImagesToGetBSplineDisplacementField = {
        "@type": "ants/ANTSUseLandmarkImagesToGetBSplineDisplacementField",
        "fixed_image_with_labeled_landmarks": fixed_image_with_labeled_landmarks,
        "moving_image_with_labeled_landmarks": moving_image_with_labeled_landmarks,
        "output_displacement_field": output_displacement_field,
        "mesh_size": mesh_size,
        "number_of_levels": number_of_levels,
    }
    if order is not None:
        params["order"] = order
    if enforce_stationary_boundaries is not None:
        params["enforce_stationary_boundaries"] = enforce_stationary_boundaries
    if landmark_weights is not None:
        params["landmark_weights"] = landmark_weights
    return params

def ants_use_landmark_images_to_get_b_spline_displacement_field_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsUseLandmarkImagesToGetBSplineDisplacementField."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed_image_with_labeled_landmarks", None) is None:
        raise StyxValidationError("`fixed_image_with_labeled_landmarks` must not be None")
    if not isinstance(params["fixed_image_with_labeled_landmarks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_image_with_labeled_landmarks` has the wrong type: Received `{type(params["fixed_image_with_labeled_landmarks"])}` expected `InputPathType`')
    if params.get("moving_image_with_labeled_landmarks", None) is None:
        raise StyxValidationError("`moving_image_with_labeled_landmarks` must not be None")
    if not isinstance(params["moving_image_with_labeled_landmarks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_image_with_labeled_landmarks` has the wrong type: Received `{type(params["moving_image_with_labeled_landmarks"])}` expected `InputPathType`')
    if params.get("output_displacement_field", None) is None:
        raise StyxValidationError("`output_displacement_field` must not be None")
    if not isinstance(params["output_displacement_field"], str):
        raise StyxValidationError(f'`output_displacement_field` has the wrong type: Received `{type(params["output_displacement_field"])}` expected `str`')
    if params.get("mesh_size", None) is None:
        raise StyxValidationError("`mesh_size` must not be None")
    if not isinstance(params["mesh_size"], str):
        raise StyxValidationError(f'`mesh_size` has the wrong type: Received `{type(params["mesh_size"])}` expected `str`')
    if params.get("number_of_levels", None) is None:
        raise StyxValidationError("`number_of_levels` must not be None")
    if not isinstance(params["number_of_levels"], int):
        raise StyxValidationError(f'`number_of_levels` has the wrong type: Received `{type(params["number_of_levels"])}` expected `int`')
    if params.get("order", None) is not None:
        if not isinstance(params["order"], int):
            raise StyxValidationError(f'`order` has the wrong type: Received `{type(params["order"])}` expected `int`')
    if params.get("enforce_stationary_boundaries", None) is not None:
        if not isinstance(params["enforce_stationary_boundaries"], int):
            raise StyxValidationError(f'`enforce_stationary_boundaries` has the wrong type: Received `{type(params["enforce_stationary_boundaries"])}` expected `int`')
    if params.get("landmark_weights", None) is not None:
        if not isinstance(params["landmark_weights"], (pathlib.Path, str)):
            raise StyxValidationError(f'`landmark_weights` has the wrong type: Received `{type(params["landmark_weights"])}` expected `InputPathType`')

def ants_use_landmark_images_to_get_b_spline_displacement_field_cargs(params: AntsUseLandmarkImagesToGetBSplineDisplacementField, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("ANTSUseLandmarkImagesToGetBSplineDisplacementField")
    cargs.append(execution.input_file(params["fixed_image_with_labeled_landmarks"]))
    cargs.append(execution.input_file(params["moving_image_with_labeled_landmarks"]))
    cargs.append(params["output_displacement_field"])
    cargs.append(params["mesh_size"])
    cargs.append(str(params["number_of_levels"]))
    v_0 = params.get("order")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("enforce_stationary_boundaries")
    if v_1 is not None:
        cargs.append(str(v_1))
    v_2 = params.get("landmark_weights")
    if v_2 is not None:
        cargs.append(execution.input_file(v_2))
    return cargs

def ants_use_landmark_images_to_get_b_spline_displacement_field_outputs(params: AntsUseLandmarkImagesToGetBSplineDisplacementField, execution: Execution) -> AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs:
    """Build the AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    displacement_field_v: OutputPathType = execution.output_file(params["output_displacement_field"])
    return AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs(
        root=root_v,
        displacement_field=displacement_field_v,
    )

def ants_use_landmark_images_to_get_b_spline_displacement_field_execute(params: AntsUseLandmarkImagesToGetBSplineDisplacementField, runner: Runner | None = None) -> AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs:
    """
    ANTSUseLandmarkImagesToGetBSplineDisplacementField

    We expect the input images to be (1) N-ary (2) in the same physical space as the images you want to register and (3) to have the same landmark points defined within them. Landmarks will be defined from the center of mass of the labels in the input images. You can use ITK-snap to generate the label images. The optional landmarks weights are read from a text file where each row is either: "label,labelWeight" or "labelWeight". If the latter format is used, the label weights are assumed to be arranged in ascending order by label.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_use_landmark_images_to_get_b_spline_displacement_field_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_USE_LANDMARK_IMAGES_TO_GET_B_SPLINE_DISPLACEMENT_FIELD_METADATA)
    execution.params(params)
    args = ants_use_landmark_images_to_get_b_spline_displacement_field_cargs(params, execution)
    out = ants_use_landmark_images_to_get_b_spline_displacement_field_outputs(params, execution)
    execution.run(args)
    return out

def ants_use_landmark_images_to_get_b_spline_displacement_field(
    fixed_image_with_labeled_landmarks: InputPathType,
    moving_image_with_labeled_landmarks: InputPathType,
    output_displacement_field: str,
    mesh_size: str,
    number_of_levels: int,
    order: int | None = None,
    enforce_stationary_boundaries: int | None = None,
    landmark_weights: InputPathType | None = None,
    runner: Runner | None = None,
) -> AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs:
    """
    ANTSUseLandmarkImagesToGetBSplineDisplacementField

    We expect the input images to be (1) N-ary (2) in the same physical space as the images you want to register and (3) to have the same landmark points defined within them. Landmarks will be defined from the center of mass of the labels in the input images. You can use ITK-snap to generate the label images. The optional landmarks weights are read from a text file where each row is either: "label,labelWeight" or "labelWeight". If the latter format is used, the label weights are assumed to be arranged in ascending order by label.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        fixed_image_with_labeled_landmarks: The fixed image with labeled landmarks.
        moving_image_with_labeled_landmarks: The moving image with labeled\
            landmarks.
        output_displacement_field: The output displacement field file.
        mesh_size: The mesh size specified as meshSize[0]xmeshSize[1]x...
        number_of_levels: The number of levels in the B-spline hierarchy.
        order: The order of the B-spline (default is 3).
        enforce_stationary_boundaries: Whether to enforce stationary boundaries\
            (default is 1).
        landmark_weights: Optional text file containing landmark weights. Each row\
            is either "label,labelWeight" or "labelWeight". If the latter format is\
            used, the weights are assumed to be arranged in ascending order by\
            label.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_use_landmark_images_to_get_b_spline_displacement_field_params(
        fixed_image_with_labeled_landmarks=fixed_image_with_labeled_landmarks,
        moving_image_with_labeled_landmarks=moving_image_with_labeled_landmarks,
        output_displacement_field=output_displacement_field,
        mesh_size=mesh_size,
        number_of_levels=number_of_levels,
        order=order,
        enforce_stationary_boundaries=enforce_stationary_boundaries,
        landmark_weights=landmark_weights,
    )
    return ants_use_landmark_images_to_get_b_spline_displacement_field_execute(params, runner)

__all__ = [
    "AntsUseLandmarkImagesToGetBSplineDisplacementField",
    "AntsUseLandmarkImagesToGetBSplineDisplacementFieldOutputs",
    "ANTS_USE_LANDMARK_IMAGES_TO_GET_B_SPLINE_DISPLACEMENT_FIELD_METADATA",
    "ants_use_landmark_images_to_get_b_spline_displacement_field_cargs",
    "ants_use_landmark_images_to_get_b_spline_displacement_field_outputs",
    "ants_use_landmark_images_to_get_b_spline_displacement_field_params",
    "ants_use_landmark_images_to_get_b_spline_displacement_field_execute",
    "ants_use_landmark_images_to_get_b_spline_displacement_field_validate",
    "ants_use_landmark_images_to_get_b_spline_displacement_field",
]