# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_MULTIVARIATE_TEMPLATE_CONSTRUCTION2_SH_METADATA = Metadata(
    id="antsMultivariateTemplateConstruction2.sh",
    name="antsMultivariateTemplateConstruction2.sh",
    package="ants",
)

AntsMultivariateTemplateConstruction2ShDimension = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

AntsMultivariateTemplateConstruction2ShImageStatistic = typing.Literal[0] | typing.Literal[1] | typing.Literal[2]

AntsMultivariateTemplateConstruction2ShParallelControl = typing.Literal[0] | typing.Literal[1] | typing.Literal[2] | typing.Literal[3] | typing.Literal[4] | typing.Literal[5]

AntsMultivariateTemplateConstruction2Sh = typing.TypedDict(
    "AntsMultivariateTemplateConstruction2Sh",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsMultivariateTemplateConstruction2.sh"]],
        "dimension": AntsMultivariateTemplateConstruction2ShDimension,
        "output_prefix": typing.NotRequired[str],
        "image_statistic": typing.NotRequired[AntsMultivariateTemplateConstruction2ShImageStatistic],
        "sharpening": typing.NotRequired[AntsMultivariateTemplateConstruction2ShImageStatistic],
        "backup_images": typing.NotRequired[bool],
        "parallel_control": typing.NotRequired[AntsMultivariateTemplateConstruction2ShParallelControl],
        "single_precision": typing.NotRequired[bool],
        "gradient_step": typing.NotRequired[float],
        "iterations": typing.NotRequired[int],
        "cpu_cores": typing.NotRequired[int],
        "num_modalities": typing.NotRequired[int],
        "modality_weights": typing.NotRequired[str],
        "max_iterations": typing.NotRequired[str],
        "shrink_factors": typing.NotRequired[str],
        "smoothing_kernels": typing.NotRequired[str],
        "n4_bias_correction": typing.NotRequired[bool],
        "prepend_commands": typing.NotRequired[str],
        "rigid_registration": typing.NotRequired[bool],
        "linear_registration": typing.NotRequired[bool],
        "similarity_metric": typing.NotRequired[str],
        "transformation_type": typing.NotRequired[str],
        "walltime": typing.NotRequired[str],
        "memory_limit": typing.NotRequired[str],
        "xgrid_args": typing.NotRequired[str],
        "update_template": typing.NotRequired[bool],
        "target_volume": typing.NotRequired[list[InputPathType]],
        "input_images": list[InputPathType],
    },
)

@dataclasses.dataclass
class AntsMultivariateTemplateConstruction2ShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    intermediate_templates_dir: OutputPathType
    """Directory containing intermediate templates and shape update warps from each iteration"""

def ants_multivariate_template_construction2_sh_params(
    dimension: AntsMultivariateTemplateConstruction2ShDimension,
    input_images: list[InputPathType],
    output_prefix: str | None = None,
    image_statistic: AntsMultivariateTemplateConstruction2ShImageStatistic | None = None,
    sharpening: AntsMultivariateTemplateConstruction2ShImageStatistic | None = None,
    backup_images: bool | None = None,
    parallel_control: AntsMultivariateTemplateConstruction2ShParallelControl | None = None,
    single_precision: bool | None = None,
    gradient_step: float | None = None,
    iterations: int | None = None,
    cpu_cores: int | None = None,
    num_modalities: int | None = None,
    modality_weights: str | None = None,
    max_iterations: str | None = None,
    shrink_factors: str | None = None,
    smoothing_kernels: str | None = None,
    n4_bias_correction: bool | None = None,
    prepend_commands: str | None = None,
    rigid_registration: bool | None = None,
    linear_registration: bool | None = None,
    similarity_metric: str | None = None,
    transformation_type: str | None = None,
    walltime: str | None = None,
    memory_limit: str | None = None,
    xgrid_args: str | None = None,
    update_template: bool | None = None,
    target_volume: list[InputPathType] | None = None,
) -> AntsMultivariateTemplateConstruction2Sh:
    """
    Build parameters.

    Args:
        dimension: ImageDimension: 2 or 3 (for 2 or 3 dimensional registration of\
            single volume), 4 (for template generation of time-series data)
        input_images: List of images in the current directory, eg *_t1.nii.gz.\
            Should be at the end of the command. Optionally, one can specify a .csv\
            or .txt file where each line is the location of the input image. One\
            can also specify more than one file for each image for multi-modal\
            template construction (e.g. t1 and t2). For the multi-modal case, the\
            templates will be consecutively numbered.
        output_prefix: A prefix that is prepended to all output files (default =\
            "antsBTP")
        image_statistic: Image statistic used to summarize images (default 1): 0 =\
            mean, 1 = mean of normalized intensities, 2 = median. Normalization\
            here means dividing each image by its mean intensity.
        sharpening: Sharpening applied to template at each iteration (default 1): 0\
            = none, 1 = Laplacian, 2 = Unsharp mask
        backup_images: Backup images and results from all iterations (default = 0):\
            Boolean to save the transform files, bias corrected inputs, templates,\
            transforms, and warped images for each iteration. By default, only the\
            templates and the shape update warp field are saved.
        parallel_control: Control for parallel computation (default 0): 0 = run\
            serially, 1 = SGE qsub, 2 = use PEXEC (localhost), 3 = Apple XGrid, 4 =\
            PBS qsub, 5 = SLURM
        single_precision: Use single precision (default 1)
        gradient_step: Gradient step size (default 0.25): smaller in magnitude\
            results in more cautious steps. Use smaller steps to refine template\
            details. 0.25 is an upper (aggressive) limit for this parameter.
        iterations: Iteration limit (default 4): iterations of the template\
            construction (Iteration limit)*NumImages registrations
        cpu_cores: Number of cpu cores to use locally for pexec option (default 2;\
            requires "-c 2")
        num_modalities: Number of modalities used to construct the template\
            (default 1): For example, if one wanted to create a multimodal template\
            consisting of T1,T2,and FA components ("-k 3").
        modality_weights: Modality weights used in the similarity metric (default =\
            1): specified as e.g. 1x0.5x0.75.
        max_iterations: Max iterations for each pairwise registration (default =\
            100x100x70x20): specified in the form JxK...xF where J = max iterations\
            at first (coarsest) resolution, K = max iterations at next resolution,\
            F = max iterations at the final resolution
        shrink_factors: Shrink factors in pairwise registration (default =\
            6x4x2x1): in the same form as max iterations. Must have the same number\
            of components as the iterations and smoothing. The shrink factors are\
            integer factors for downsampling the virtual space (usually the\
            template image) during registration.
        smoothing_kernels: Smoothing kernels in pairwise registration (default =\
            3x2x1x0): also in the same form as max iterations and shrink factors,\
            with the same number of components. Standard deviation of a Gaussian\
            smoothing kernel applied to the images before downsampling at each\
            level. The kernel may be specified in mm units or voxels with "AxBxCmm"\
            or "AxBxCvox". Missing units implies vox.
        n4_bias_correction: N4BiasFieldCorrection of moving image: 0 off, 1 on\
            (default 1)
        prepend_commands: Commands to prepend to job scripts (e.g., change into\
            appropriate directory, set paths, etc)
        rigid_registration: Do rigid-body registration of inputs to the initial\
            template, before doing the main pairwise registration. 0 off 1 on\
            (default 0). If you are trying to refine or update an existing\
            template, you would use '-r 0'. Rigid initialization is useful when you\
            do not have an initial template, or you want to use a single image as a\
            reference for rigid alignment only.
        linear_registration: Use linear image registration stages during the\
            pairwise (template/subject) deformable registration. Otherwise,\
            registration is limited to SyN or B-spline SyN (see '-t' option). This\
            is '1' by default.
        similarity_metric: Type of similarity metric used for pairwise registration\
            (default = CC). Options are case sensitive. CC = cross-correlation, MI\
            = mutual information, MSQ = mean square difference, DEMONS = demon's\
            metric. A similarity metric per modality can be specified. If the CC\
            metric is chosen, one can also specify the radius in brackets, e.g. '-m\
            "CC[4]"'.
        transformation_type: Type of transformation model used for registration\
            (default = SyN): Options are case sensitive. SyN = Greedy SyN,\
            BSplineSyN = Greedy B-spline SyN, TimeVaryingVelocityField =\
            Time-varying velocity field, TimeVaryingBSplineVelocityField =\
            Time-varying B-spline velocity field. Transformation parameters may be\
            specified with brackets, eg '-t "SyN[0.1,3,0]"'. To use linear\
            registration only, options are: Affine = Affine (runs Rigid + Affine),\
            Rigid = Rigid (runs Rigid only).
        walltime: Walltime (default = 20:00:00): Option for PBS/SLURM qsub\
            specifying requested time per pairwise registration
        memory_limit: Memory limit (default = 8gb): Option for PBS/SLURM qsub\
            specifying requested memory per pairwise registration
        xgrid_args: XGrid arguments (e.g., -x "-p password -h controlhost")
        update_template: Update the template with the full affine transform\
            (default 1). If 0, the rigid component of the affine transform will not\
            be used to update the template. If your template drifts in translation\
            or orientation try "-y 0".
        target_volume: Use this this volume as the target of all inputs. When not\
            used, the script will create an unbiased starting point by averaging\
            all inputs, then aligning the center of mass of all inputs to that of\
            the initial average. If you do not use -z, it is recommended to use "-r\
            1". Use the full path. For multiple modalities, specify -z\
            modality1.nii.gz -z modality2.nii.gz ... in the same modality order as\
            the input images.

    Returns:
        Parameter dictionary.
    """
    params: AntsMultivariateTemplateConstruction2Sh = {
        "@type": "ants/antsMultivariateTemplateConstruction2.sh",
        "dimension": dimension,
        "input_images": input_images,
    }
    if output_prefix is not None:
        params["output_prefix"] = output_prefix
    if image_statistic is not None:
        params["image_statistic"] = image_statistic
    if sharpening is not None:
        params["sharpening"] = sharpening
    if backup_images is not None:
        params["backup_images"] = backup_images
    if parallel_control is not None:
        params["parallel_control"] = parallel_control
    if single_precision is not None:
        params["single_precision"] = single_precision
    if gradient_step is not None:
        params["gradient_step"] = gradient_step
    if iterations is not None:
        params["iterations"] = iterations
    if cpu_cores is not None:
        params["cpu_cores"] = cpu_cores
    if num_modalities is not None:
        params["num_modalities"] = num_modalities
    if modality_weights is not None:
        params["modality_weights"] = modality_weights
    if max_iterations is not None:
        params["max_iterations"] = max_iterations
    if shrink_factors is not None:
        params["shrink_factors"] = shrink_factors
    if smoothing_kernels is not None:
        params["smoothing_kernels"] = smoothing_kernels
    if n4_bias_correction is not None:
        params["n4_bias_correction"] = n4_bias_correction
    if prepend_commands is not None:
        params["prepend_commands"] = prepend_commands
    if rigid_registration is not None:
        params["rigid_registration"] = rigid_registration
    if linear_registration is not None:
        params["linear_registration"] = linear_registration
    if similarity_metric is not None:
        params["similarity_metric"] = similarity_metric
    if transformation_type is not None:
        params["transformation_type"] = transformation_type
    if walltime is not None:
        params["walltime"] = walltime
    if memory_limit is not None:
        params["memory_limit"] = memory_limit
    if xgrid_args is not None:
        params["xgrid_args"] = xgrid_args
    if update_template is not None:
        params["update_template"] = update_template
    if target_volume is not None:
        params["target_volume"] = target_volume
    return params

def ants_multivariate_template_construction2_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsMultivariateTemplateConstruction2Sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimension", None) is None:
        raise StyxValidationError("`dimension` must not be None")
    if not isinstance(params["dimension"], (float, int)):
        raise StyxValidationError(f'`dimension` has the wrong type: Received `{type(params["dimension"])}` expected `AntsMultivariateTemplateConstruction2ShDimension`')
    if params["dimension"] not in [2, 3, 4]:
        raise StyxValidationError("Parameter `dimension` must be one of [2, 3, 4]")
    if params.get("output_prefix", None) is not None:
        if not isinstance(params["output_prefix"], str):
            raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')
    if params.get("image_statistic", None) is not None:
        if not isinstance(params["image_statistic"], (float, int)):
            raise StyxValidationError(f'`image_statistic` has the wrong type: Received `{type(params["image_statistic"])}` expected `AntsMultivariateTemplateConstruction2ShImageStatistic`')
        if params["image_statistic"] not in [0, 1, 2]:
            raise StyxValidationError("Parameter `image_statistic` must be one of [0, 1, 2]")
    if params.get("sharpening", None) is not None:
        if not isinstance(params["sharpening"], (float, int)):
            raise StyxValidationError(f'`sharpening` has the wrong type: Received `{type(params["sharpening"])}` expected `AntsMultivariateTemplateConstruction2ShImageStatistic`')
        if params["sharpening"] not in [0, 1, 2]:
            raise StyxValidationError("Parameter `sharpening` must be one of [0, 1, 2]")
    if params.get("backup_images", None) is not None:
        if not isinstance(params["backup_images"], bool):
            raise StyxValidationError(f'`backup_images` has the wrong type: Received `{type(params["backup_images"])}` expected `bool`')
    if params.get("parallel_control", None) is not None:
        if not isinstance(params["parallel_control"], (float, int)):
            raise StyxValidationError(f'`parallel_control` has the wrong type: Received `{type(params["parallel_control"])}` expected `AntsMultivariateTemplateConstruction2ShParallelControl`')
        if params["parallel_control"] not in [0, 1, 2, 3, 4, 5]:
            raise StyxValidationError("Parameter `parallel_control` must be one of [0, 1, 2, 3, 4, 5]")
    if params.get("single_precision", None) is not None:
        if not isinstance(params["single_precision"], bool):
            raise StyxValidationError(f'`single_precision` has the wrong type: Received `{type(params["single_precision"])}` expected `bool`')
    if params.get("gradient_step", None) is not None:
        if not isinstance(params["gradient_step"], (float, int)):
            raise StyxValidationError(f'`gradient_step` has the wrong type: Received `{type(params["gradient_step"])}` expected `float`')
        if params["gradient_step"] > 0.25:
            raise StyxValidationError("Parameter `gradient_step` must be at most 0.25")
    if params.get("iterations", None) is not None:
        if not isinstance(params["iterations"], int):
            raise StyxValidationError(f'`iterations` has the wrong type: Received `{type(params["iterations"])}` expected `int`')
    if params.get("cpu_cores", None) is not None:
        if not isinstance(params["cpu_cores"], int):
            raise StyxValidationError(f'`cpu_cores` has the wrong type: Received `{type(params["cpu_cores"])}` expected `int`')
    if params.get("num_modalities", None) is not None:
        if not isinstance(params["num_modalities"], int):
            raise StyxValidationError(f'`num_modalities` has the wrong type: Received `{type(params["num_modalities"])}` expected `int`')
    if params.get("modality_weights", None) is not None:
        if not isinstance(params["modality_weights"], str):
            raise StyxValidationError(f'`modality_weights` has the wrong type: Received `{type(params["modality_weights"])}` expected `str`')
    if params.get("max_iterations", None) is not None:
        if not isinstance(params["max_iterations"], str):
            raise StyxValidationError(f'`max_iterations` has the wrong type: Received `{type(params["max_iterations"])}` expected `str`')
    if params.get("shrink_factors", None) is not None:
        if not isinstance(params["shrink_factors"], str):
            raise StyxValidationError(f'`shrink_factors` has the wrong type: Received `{type(params["shrink_factors"])}` expected `str`')
    if params.get("smoothing_kernels", None) is not None:
        if not isinstance(params["smoothing_kernels"], str):
            raise StyxValidationError(f'`smoothing_kernels` has the wrong type: Received `{type(params["smoothing_kernels"])}` expected `str`')
    if params.get("n4_bias_correction", None) is not None:
        if not isinstance(params["n4_bias_correction"], bool):
            raise StyxValidationError(f'`n4_bias_correction` has the wrong type: Received `{type(params["n4_bias_correction"])}` expected `bool`')
    if params.get("prepend_commands", None) is not None:
        if not isinstance(params["prepend_commands"], str):
            raise StyxValidationError(f'`prepend_commands` has the wrong type: Received `{type(params["prepend_commands"])}` expected `str`')
    if params.get("rigid_registration", None) is not None:
        if not isinstance(params["rigid_registration"], bool):
            raise StyxValidationError(f'`rigid_registration` has the wrong type: Received `{type(params["rigid_registration"])}` expected `bool`')
    if params.get("linear_registration", None) is not None:
        if not isinstance(params["linear_registration"], bool):
            raise StyxValidationError(f'`linear_registration` has the wrong type: Received `{type(params["linear_registration"])}` expected `bool`')
    if params.get("similarity_metric", None) is not None:
        if not isinstance(params["similarity_metric"], str):
            raise StyxValidationError(f'`similarity_metric` has the wrong type: Received `{type(params["similarity_metric"])}` expected `str`')
    if params.get("transformation_type", None) is not None:
        if not isinstance(params["transformation_type"], str):
            raise StyxValidationError(f'`transformation_type` has the wrong type: Received `{type(params["transformation_type"])}` expected `str`')
    if params.get("walltime", None) is not None:
        if not isinstance(params["walltime"], str):
            raise StyxValidationError(f'`walltime` has the wrong type: Received `{type(params["walltime"])}` expected `str`')
    if params.get("memory_limit", None) is not None:
        if not isinstance(params["memory_limit"], str):
            raise StyxValidationError(f'`memory_limit` has the wrong type: Received `{type(params["memory_limit"])}` expected `str`')
    if params.get("xgrid_args", None) is not None:
        if not isinstance(params["xgrid_args"], str):
            raise StyxValidationError(f'`xgrid_args` has the wrong type: Received `{type(params["xgrid_args"])}` expected `str`')
    if params.get("update_template", None) is not None:
        if not isinstance(params["update_template"], bool):
            raise StyxValidationError(f'`update_template` has the wrong type: Received `{type(params["update_template"])}` expected `bool`')
    if params.get("target_volume", None) is not None:
        if not isinstance(params["target_volume"], list):
            raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(params["target_volume"])}` expected `list[InputPathType]`')
        for e in params["target_volume"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`target_volume` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("input_images", None) is None:
        raise StyxValidationError("`input_images` must not be None")
    if not isinstance(params["input_images"], list):
        raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(params["input_images"])}` expected `list[InputPathType]`')
    for e_2 in params["input_images"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`input_images` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')

def ants_multivariate_template_construction2_sh_cargs(params: AntsMultivariateTemplateConstruction2Sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsMultivariateTemplateConstruction2.sh")
    cargs.append("-d")
    cargs.append(str(params["dimension"]))
    v_0 = params.get("output_prefix")
    if v_0 is not None:
        cargs.append("-o")
        cargs.append(v_0)
    v_1 = params.get("image_statistic")
    if v_1 is not None:
        cargs.append("-a")
        cargs.append(str(v_1))
    v_2 = params.get("sharpening")
    if v_2 is not None:
        cargs.append("-A")
        cargs.append(str(v_2))
    v_3 = params.get("backup_images")
    if v_3 is not None:
        cargs.append("-b")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    v_4 = params.get("parallel_control")
    if v_4 is not None:
        cargs.append("-c")
        cargs.append(str(v_4))
    v_5 = params.get("single_precision")
    if v_5 is not None:
        cargs.append("-e")
        if v_5:
            cargs.append("0")
        else:
            cargs.append("1")
    v_6 = params.get("gradient_step")
    if v_6 is not None:
        cargs.append("-g")
        cargs.append(str(v_6))
    v_7 = params.get("iterations")
    if v_7 is not None:
        cargs.append("-i")
        cargs.append(str(v_7))
    v_8 = params.get("cpu_cores")
    if v_8 is not None:
        cargs.append("-j")
        cargs.append(str(v_8))
    v_9 = params.get("num_modalities")
    if v_9 is not None:
        cargs.append("-k")
        cargs.append(str(v_9))
    v_10 = params.get("modality_weights")
    if v_10 is not None:
        cargs.append("-w")
        cargs.append(v_10)
    v_11 = params.get("max_iterations")
    if v_11 is not None:
        cargs.append("-q")
        cargs.append(v_11)
    v_12 = params.get("shrink_factors")
    if v_12 is not None:
        cargs.append("-f")
        cargs.append(v_12)
    v_13 = params.get("smoothing_kernels")
    if v_13 is not None:
        cargs.append("-s")
        cargs.append(v_13)
    v_14 = params.get("n4_bias_correction")
    if v_14 is not None:
        cargs.append("-n")
        if v_14:
            cargs.append("0")
        else:
            cargs.append("1")
    v_15 = params.get("prepend_commands")
    if v_15 is not None:
        cargs.append("-p")
        cargs.append(v_15)
    v_16 = params.get("rigid_registration")
    if v_16 is not None:
        cargs.append("-r")
        if v_16:
            cargs.append("0")
        else:
            cargs.append("1")
    v_17 = params.get("linear_registration")
    if v_17 is not None:
        cargs.append("-l")
        if v_17:
            cargs.append("0")
        else:
            cargs.append("1")
    v_18 = params.get("similarity_metric")
    if v_18 is not None:
        cargs.append("-m")
        cargs.append(v_18)
    v_19 = params.get("transformation_type")
    if v_19 is not None:
        cargs.append("-t")
        cargs.append(v_19)
    v_20 = params.get("walltime")
    if v_20 is not None:
        cargs.append("-u")
        cargs.append(v_20)
    v_21 = params.get("memory_limit")
    if v_21 is not None:
        cargs.append("-v")
        cargs.append(v_21)
    v_22 = params.get("xgrid_args")
    if v_22 is not None:
        cargs.append("-x")
        cargs.append(v_22)
    v_23 = params.get("update_template")
    if v_23 is not None:
        cargs.append("-y")
        if v_23:
            cargs.append("0")
        else:
            cargs.append("1")
    v_24 = params.get("target_volume")
    if v_24 is not None:
        cargs.append("-z")
        for item0 in v_24:
            cargs.append(execution.input_file(item0))
    for item1 in params["input_images"]:
        cargs.append(execution.input_file(item1))
    return cargs

def ants_multivariate_template_construction2_sh_outputs(params: AntsMultivariateTemplateConstruction2Sh, execution: Execution) -> AntsMultivariateTemplateConstruction2ShOutputs:
    """Build the AntsMultivariateTemplateConstruction2ShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    intermediate_templates_dir_v: OutputPathType = execution.output_file("intermediateTemplates")
    return AntsMultivariateTemplateConstruction2ShOutputs(
        root=root_v,
        intermediate_templates_dir=intermediate_templates_dir_v,
    )

def ants_multivariate_template_construction2_sh_execute(params: AntsMultivariateTemplateConstruction2Sh, runner: Runner | None = None) -> AntsMultivariateTemplateConstruction2ShOutputs:
    """
    antsMultivariateTemplateConstruction2.sh

    Multivariate template construction using ANTs registration tools (version 2). All images to be added to the template should be in the same directory, and this script should be invoked from that directory.

    Author: Brian B. Avants, Nick Tustison and Gang Song

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_multivariate_template_construction2_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_MULTIVARIATE_TEMPLATE_CONSTRUCTION2_SH_METADATA)
    execution.params(params)
    args = ants_multivariate_template_construction2_sh_cargs(params, execution)
    out = ants_multivariate_template_construction2_sh_outputs(params, execution)
    execution.run(args)
    return out

def ants_multivariate_template_construction2_sh(
    dimension: AntsMultivariateTemplateConstruction2ShDimension,
    input_images: list[InputPathType],
    output_prefix: str | None = None,
    image_statistic: AntsMultivariateTemplateConstruction2ShImageStatistic | None = None,
    sharpening: AntsMultivariateTemplateConstruction2ShImageStatistic | None = None,
    backup_images: bool | None = None,
    parallel_control: AntsMultivariateTemplateConstruction2ShParallelControl | None = None,
    single_precision: bool | None = None,
    gradient_step: float | None = None,
    iterations: int | None = None,
    cpu_cores: int | None = None,
    num_modalities: int | None = None,
    modality_weights: str | None = None,
    max_iterations: str | None = None,
    shrink_factors: str | None = None,
    smoothing_kernels: str | None = None,
    n4_bias_correction: bool | None = None,
    prepend_commands: str | None = None,
    rigid_registration: bool | None = None,
    linear_registration: bool | None = None,
    similarity_metric: str | None = None,
    transformation_type: str | None = None,
    walltime: str | None = None,
    memory_limit: str | None = None,
    xgrid_args: str | None = None,
    update_template: bool | None = None,
    target_volume: list[InputPathType] | None = None,
    runner: Runner | None = None,
) -> AntsMultivariateTemplateConstruction2ShOutputs:
    """
    antsMultivariateTemplateConstruction2.sh

    Multivariate template construction using ANTs registration tools (version 2). All images to be added to the template should be in the same directory, and this script should be invoked from that directory.

    Author: Brian B. Avants, Nick Tustison and Gang Song

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimension: ImageDimension: 2 or 3 (for 2 or 3 dimensional registration of\
            single volume), 4 (for template generation of time-series data)
        input_images: List of images in the current directory, eg *_t1.nii.gz.\
            Should be at the end of the command. Optionally, one can specify a .csv\
            or .txt file where each line is the location of the input image. One\
            can also specify more than one file for each image for multi-modal\
            template construction (e.g. t1 and t2). For the multi-modal case, the\
            templates will be consecutively numbered.
        output_prefix: A prefix that is prepended to all output files (default =\
            "antsBTP")
        image_statistic: Image statistic used to summarize images (default 1): 0 =\
            mean, 1 = mean of normalized intensities, 2 = median. Normalization\
            here means dividing each image by its mean intensity.
        sharpening: Sharpening applied to template at each iteration (default 1): 0\
            = none, 1 = Laplacian, 2 = Unsharp mask
        backup_images: Backup images and results from all iterations (default = 0):\
            Boolean to save the transform files, bias corrected inputs, templates,\
            transforms, and warped images for each iteration. By default, only the\
            templates and the shape update warp field are saved.
        parallel_control: Control for parallel computation (default 0): 0 = run\
            serially, 1 = SGE qsub, 2 = use PEXEC (localhost), 3 = Apple XGrid, 4 =\
            PBS qsub, 5 = SLURM
        single_precision: Use single precision (default 1)
        gradient_step: Gradient step size (default 0.25): smaller in magnitude\
            results in more cautious steps. Use smaller steps to refine template\
            details. 0.25 is an upper (aggressive) limit for this parameter.
        iterations: Iteration limit (default 4): iterations of the template\
            construction (Iteration limit)*NumImages registrations
        cpu_cores: Number of cpu cores to use locally for pexec option (default 2;\
            requires "-c 2")
        num_modalities: Number of modalities used to construct the template\
            (default 1): For example, if one wanted to create a multimodal template\
            consisting of T1,T2,and FA components ("-k 3").
        modality_weights: Modality weights used in the similarity metric (default =\
            1): specified as e.g. 1x0.5x0.75.
        max_iterations: Max iterations for each pairwise registration (default =\
            100x100x70x20): specified in the form JxK...xF where J = max iterations\
            at first (coarsest) resolution, K = max iterations at next resolution,\
            F = max iterations at the final resolution
        shrink_factors: Shrink factors in pairwise registration (default =\
            6x4x2x1): in the same form as max iterations. Must have the same number\
            of components as the iterations and smoothing. The shrink factors are\
            integer factors for downsampling the virtual space (usually the\
            template image) during registration.
        smoothing_kernels: Smoothing kernels in pairwise registration (default =\
            3x2x1x0): also in the same form as max iterations and shrink factors,\
            with the same number of components. Standard deviation of a Gaussian\
            smoothing kernel applied to the images before downsampling at each\
            level. The kernel may be specified in mm units or voxels with "AxBxCmm"\
            or "AxBxCvox". Missing units implies vox.
        n4_bias_correction: N4BiasFieldCorrection of moving image: 0 off, 1 on\
            (default 1)
        prepend_commands: Commands to prepend to job scripts (e.g., change into\
            appropriate directory, set paths, etc)
        rigid_registration: Do rigid-body registration of inputs to the initial\
            template, before doing the main pairwise registration. 0 off 1 on\
            (default 0). If you are trying to refine or update an existing\
            template, you would use '-r 0'. Rigid initialization is useful when you\
            do not have an initial template, or you want to use a single image as a\
            reference for rigid alignment only.
        linear_registration: Use linear image registration stages during the\
            pairwise (template/subject) deformable registration. Otherwise,\
            registration is limited to SyN or B-spline SyN (see '-t' option). This\
            is '1' by default.
        similarity_metric: Type of similarity metric used for pairwise registration\
            (default = CC). Options are case sensitive. CC = cross-correlation, MI\
            = mutual information, MSQ = mean square difference, DEMONS = demon's\
            metric. A similarity metric per modality can be specified. If the CC\
            metric is chosen, one can also specify the radius in brackets, e.g. '-m\
            "CC[4]"'.
        transformation_type: Type of transformation model used for registration\
            (default = SyN): Options are case sensitive. SyN = Greedy SyN,\
            BSplineSyN = Greedy B-spline SyN, TimeVaryingVelocityField =\
            Time-varying velocity field, TimeVaryingBSplineVelocityField =\
            Time-varying B-spline velocity field. Transformation parameters may be\
            specified with brackets, eg '-t "SyN[0.1,3,0]"'. To use linear\
            registration only, options are: Affine = Affine (runs Rigid + Affine),\
            Rigid = Rigid (runs Rigid only).
        walltime: Walltime (default = 20:00:00): Option for PBS/SLURM qsub\
            specifying requested time per pairwise registration
        memory_limit: Memory limit (default = 8gb): Option for PBS/SLURM qsub\
            specifying requested memory per pairwise registration
        xgrid_args: XGrid arguments (e.g., -x "-p password -h controlhost")
        update_template: Update the template with the full affine transform\
            (default 1). If 0, the rigid component of the affine transform will not\
            be used to update the template. If your template drifts in translation\
            or orientation try "-y 0".
        target_volume: Use this this volume as the target of all inputs. When not\
            used, the script will create an unbiased starting point by averaging\
            all inputs, then aligning the center of mass of all inputs to that of\
            the initial average. If you do not use -z, it is recommended to use "-r\
            1". Use the full path. For multiple modalities, specify -z\
            modality1.nii.gz -z modality2.nii.gz ... in the same modality order as\
            the input images.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_multivariate_template_construction2_sh_params(
        dimension=dimension,
        input_images=input_images,
        output_prefix=output_prefix,
        image_statistic=image_statistic,
        sharpening=sharpening,
        backup_images=backup_images,
        parallel_control=parallel_control,
        single_precision=single_precision,
        gradient_step=gradient_step,
        iterations=iterations,
        cpu_cores=cpu_cores,
        num_modalities=num_modalities,
        modality_weights=modality_weights,
        max_iterations=max_iterations,
        shrink_factors=shrink_factors,
        smoothing_kernels=smoothing_kernels,
        n4_bias_correction=n4_bias_correction,
        prepend_commands=prepend_commands,
        rigid_registration=rigid_registration,
        linear_registration=linear_registration,
        similarity_metric=similarity_metric,
        transformation_type=transformation_type,
        walltime=walltime,
        memory_limit=memory_limit,
        xgrid_args=xgrid_args,
        update_template=update_template,
        target_volume=target_volume,
    )
    return ants_multivariate_template_construction2_sh_execute(params, runner)

__all__ = [
    "AntsMultivariateTemplateConstruction2Sh",
    "AntsMultivariateTemplateConstruction2ShOutputs",
    "ANTS_MULTIVARIATE_TEMPLATE_CONSTRUCTION2_SH_METADATA",
    "ants_multivariate_template_construction2_sh_cargs",
    "ants_multivariate_template_construction2_sh_outputs",
    "ants_multivariate_template_construction2_sh_params",
    "ants_multivariate_template_construction2_sh_execute",
    "ants_multivariate_template_construction2_sh_validate",
    "ants_multivariate_template_construction2_sh",
]