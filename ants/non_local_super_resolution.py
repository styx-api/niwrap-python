# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

NON_LOCAL_SUPER_RESOLUTION_METADATA = Metadata(
    id="NonLocalSuperResolution",
    name="NonLocalSuperResolution",
    package="ants",
)

NonLocalSuperResolutionImageDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

NonLocalSuperResolutionPatchRadius = typing.Literal[1] | typing.Literal["1x1x1"]

NonLocalSuperResolutionSearchRadius = typing.Literal[3] | typing.Literal["3x3x3"]

NonLocalSuperResolutionInterpolation = typing.Literal["Linear"] | typing.Literal["NearestNeighbor"] | typing.Literal["Gaussian"] | typing.Literal["BSpline"] | typing.Literal["CosineWindowedSinc"] | typing.Literal["WelchWindowedSinc"] | typing.Literal["HammingWindowedSinc"] | typing.Literal["LanczosWindowedSinc"]

NonLocalSuperResolution = typing.TypedDict(
    "NonLocalSuperResolution",
    {
        "@type": typing.NotRequired[typing.Literal["ants/NonLocalSuperResolution"]],
        "image_dimensionality": typing.NotRequired[NonLocalSuperResolutionImageDimensionality],
        "input_image": InputPathType,
        "interpolated_image": typing.NotRequired[InputPathType],
        "reference_image": typing.NotRequired[InputPathType],
        "patch_radius": typing.NotRequired[NonLocalSuperResolutionPatchRadius],
        "search_radius": typing.NotRequired[NonLocalSuperResolutionSearchRadius],
        "intensity_difference_sigma": typing.NotRequired[float],
        "patch_similarity_sigma": typing.NotRequired[float],
        "scale_levels": typing.NotRequired[str],
        "interpolation": typing.NotRequired[NonLocalSuperResolutionInterpolation],
        "output": str,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class NonLocalSuperResolutionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    superresoluted_output: OutputPathType
    """The superresoluted output image."""

def non_local_super_resolution_params(
    input_image: InputPathType,
    output: str,
    image_dimensionality: NonLocalSuperResolutionImageDimensionality | None = None,
    interpolated_image: InputPathType | None = None,
    reference_image: InputPathType | None = None,
    patch_radius: NonLocalSuperResolutionPatchRadius | None = None,
    search_radius: NonLocalSuperResolutionSearchRadius | None = None,
    intensity_difference_sigma: float | None = None,
    patch_similarity_sigma: float | None = None,
    scale_levels: str | None = None,
    interpolation: NonLocalSuperResolutionInterpolation | None = None,
    verbose: bool | None = None,
) -> NonLocalSuperResolution:
    """
    Build parameters.

    Args:
        input_image: A low-resolution input image to be superresoluted.
        output: The output consists of the noise corrected version of the input\
            image. Optionally, one can also output the estimated noise image.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        interpolated_image: An interpolated version of the low-resolution image\
            (such as B-spline). Specify either this option or a high-resolution\
            multi-modal counterpart (cf the -k option).
        reference_image: A high-resolution reference multi-modal image. Assumed to\
            be in the same space as the low-resolution input image. Specify either\
            this option or an interpolated version (cf the -j option).
        patch_radius: Patch radius. Default = 1x1x1.
        search_radius: Search radius. Default = 3x3x3.
        intensity_difference_sigma: Intensity difference sigma. Default = 1.0.
        patch_similarity_sigma: Patch similarity sigma. Default = 1.0.
        scale_levels: Scale levels. Default = 32x16x8x2x1.
        interpolation: Several interpolation options are available in ITK.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: NonLocalSuperResolution = {
        "@type": "ants/NonLocalSuperResolution",
        "input_image": input_image,
        "output": output,
    }
    if image_dimensionality is not None:
        params["image_dimensionality"] = image_dimensionality
    if interpolated_image is not None:
        params["interpolated_image"] = interpolated_image
    if reference_image is not None:
        params["reference_image"] = reference_image
    if patch_radius is not None:
        params["patch_radius"] = patch_radius
    if search_radius is not None:
        params["search_radius"] = search_radius
    if intensity_difference_sigma is not None:
        params["intensity_difference_sigma"] = intensity_difference_sigma
    if patch_similarity_sigma is not None:
        params["patch_similarity_sigma"] = patch_similarity_sigma
    if scale_levels is not None:
        params["scale_levels"] = scale_levels
    if interpolation is not None:
        params["interpolation"] = interpolation
    if verbose is not None:
        params["verbose"] = verbose
    return params

def non_local_super_resolution_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid NonLocalSuperResolution."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimensionality", None) is not None:
        if not isinstance(params["image_dimensionality"], (float, int)):
            raise StyxValidationError(f'`image_dimensionality` has the wrong type: Received `{type(params["image_dimensionality"])}` expected `NonLocalSuperResolutionImageDimensionality`')
        if params["image_dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `image_dimensionality` must be one of [2, 3, 4]")
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("interpolated_image", None) is not None:
        if not isinstance(params["interpolated_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`interpolated_image` has the wrong type: Received `{type(params["interpolated_image"])}` expected `InputPathType`')
    if params.get("reference_image", None) is not None:
        if not isinstance(params["reference_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("patch_radius", None) is not None:
        if not isinstance(params["patch_radius"], (float, int)):
            raise StyxValidationError(f'`patch_radius` has the wrong type: Received `{type(params["patch_radius"])}` expected `NonLocalSuperResolutionPatchRadius`')
        if params["patch_radius"] not in [1, "1x1x1"]:
            raise StyxValidationError("Parameter `patch_radius` must be one of [1, \"1x1x1\"]")
    if params.get("search_radius", None) is not None:
        if not isinstance(params["search_radius"], (float, int)):
            raise StyxValidationError(f'`search_radius` has the wrong type: Received `{type(params["search_radius"])}` expected `NonLocalSuperResolutionSearchRadius`')
        if params["search_radius"] not in [3, "3x3x3"]:
            raise StyxValidationError("Parameter `search_radius` must be one of [3, \"3x3x3\"]")
    if params.get("intensity_difference_sigma", None) is not None:
        if not isinstance(params["intensity_difference_sigma"], (float, int)):
            raise StyxValidationError(f'`intensity_difference_sigma` has the wrong type: Received `{type(params["intensity_difference_sigma"])}` expected `float`')
    if params.get("patch_similarity_sigma", None) is not None:
        if not isinstance(params["patch_similarity_sigma"], (float, int)):
            raise StyxValidationError(f'`patch_similarity_sigma` has the wrong type: Received `{type(params["patch_similarity_sigma"])}` expected `float`')
    if params.get("scale_levels", None) is not None:
        if not isinstance(params["scale_levels"], str):
            raise StyxValidationError(f'`scale_levels` has the wrong type: Received `{type(params["scale_levels"])}` expected `str`')
    if params.get("interpolation", None) is not None:
        if not isinstance(params["interpolation"], str):
            raise StyxValidationError(f'`interpolation` has the wrong type: Received `{type(params["interpolation"])}` expected `NonLocalSuperResolutionInterpolation`')
        if params["interpolation"] not in ["Linear", "NearestNeighbor", "Gaussian", "BSpline", "CosineWindowedSinc", "WelchWindowedSinc", "HammingWindowedSinc", "LanczosWindowedSinc"]:
            raise StyxValidationError("Parameter `interpolation` must be one of [\"Linear\", \"NearestNeighbor\", \"Gaussian\", \"BSpline\", \"CosineWindowedSinc\", \"WelchWindowedSinc\", \"HammingWindowedSinc\", \"LanczosWindowedSinc\"]")
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def non_local_super_resolution_cargs(params: NonLocalSuperResolution, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("NonLocalSuperResolution")
    v_0 = params.get("image_dimensionality")
    if v_0 is not None:
        cargs.append("-d")
        cargs.append(str(v_0))
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    v_1 = params.get("interpolated_image")
    if v_1 is not None:
        cargs.append("-j")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("reference_image")
    if v_2 is not None:
        cargs.append("-k")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("patch_radius")
    if v_3 is not None:
        cargs.append("-p")
        cargs.append(str(v_3))
    v_4 = params.get("search_radius")
    if v_4 is not None:
        cargs.append("-r")
        cargs.append(str(v_4))
    v_5 = params.get("intensity_difference_sigma")
    if v_5 is not None:
        cargs.append("-g")
        cargs.append(str(v_5))
    v_6 = params.get("patch_similarity_sigma")
    if v_6 is not None:
        cargs.append("-t")
        cargs.append(str(v_6))
    v_7 = params.get("scale_levels")
    if v_7 is not None:
        cargs.append("-s")
        cargs.append(v_7)
    v_8 = params.get("interpolation")
    if v_8 is not None:
        cargs.append("-n")
        cargs.append(str(v_8))
    cargs.append("-o")
    cargs.append(params["output"])
    v_9 = params.get("verbose")
    if v_9 is not None:
        cargs.append("-v")
        if v_9:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def non_local_super_resolution_outputs(params: NonLocalSuperResolution, execution: Execution) -> NonLocalSuperResolutionOutputs:
    """Build the NonLocalSuperResolutionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    superresoluted_output_v: OutputPathType = execution.output_file(params["output"])
    return NonLocalSuperResolutionOutputs(
        root=root_v,
        superresoluted_output=superresoluted_output_v,
    )

def non_local_super_resolution_execute(params: NonLocalSuperResolution, runner: Runner | None = None) -> NonLocalSuperResolutionOutputs:
    """
    NonLocalSuperResolution

    Non-local super resolution described in the papers by JV Manjon et al., focusing on MRI superresolution using self-similarity and image priors.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    non_local_super_resolution_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(NON_LOCAL_SUPER_RESOLUTION_METADATA)
    execution.params(params)
    args = non_local_super_resolution_cargs(params, execution)
    out = non_local_super_resolution_outputs(params, execution)
    execution.run(args)
    return out

def non_local_super_resolution(
    input_image: InputPathType,
    output: str,
    image_dimensionality: NonLocalSuperResolutionImageDimensionality | None = None,
    interpolated_image: InputPathType | None = None,
    reference_image: InputPathType | None = None,
    patch_radius: NonLocalSuperResolutionPatchRadius | None = None,
    search_radius: NonLocalSuperResolutionSearchRadius | None = None,
    intensity_difference_sigma: float | None = None,
    patch_similarity_sigma: float | None = None,
    scale_levels: str | None = None,
    interpolation: NonLocalSuperResolutionInterpolation | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> NonLocalSuperResolutionOutputs:
    """
    NonLocalSuperResolution

    Non-local super resolution described in the papers by JV Manjon et al., focusing on MRI superresolution using self-similarity and image priors.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        input_image: A low-resolution input image to be superresoluted.
        output: The output consists of the noise corrected version of the input\
            image. Optionally, one can also output the estimated noise image.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        interpolated_image: An interpolated version of the low-resolution image\
            (such as B-spline). Specify either this option or a high-resolution\
            multi-modal counterpart (cf the -k option).
        reference_image: A high-resolution reference multi-modal image. Assumed to\
            be in the same space as the low-resolution input image. Specify either\
            this option or an interpolated version (cf the -j option).
        patch_radius: Patch radius. Default = 1x1x1.
        search_radius: Search radius. Default = 3x3x3.
        intensity_difference_sigma: Intensity difference sigma. Default = 1.0.
        patch_similarity_sigma: Patch similarity sigma. Default = 1.0.
        scale_levels: Scale levels. Default = 32x16x8x2x1.
        interpolation: Several interpolation options are available in ITK.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = non_local_super_resolution_params(
        input_image=input_image,
        output=output,
        image_dimensionality=image_dimensionality,
        interpolated_image=interpolated_image,
        reference_image=reference_image,
        patch_radius=patch_radius,
        search_radius=search_radius,
        intensity_difference_sigma=intensity_difference_sigma,
        patch_similarity_sigma=patch_similarity_sigma,
        scale_levels=scale_levels,
        interpolation=interpolation,
        verbose=verbose,
    )
    return non_local_super_resolution_execute(params, runner)

__all__ = [
    "NonLocalSuperResolution",
    "NonLocalSuperResolutionOutputs",
    "NON_LOCAL_SUPER_RESOLUTION_METADATA",
    "non_local_super_resolution_cargs",
    "non_local_super_resolution_outputs",
    "non_local_super_resolution_params",
    "non_local_super_resolution_execute",
    "non_local_super_resolution_validate",
    "non_local_super_resolution",
]