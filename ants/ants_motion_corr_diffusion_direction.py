# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_MOTION_CORR_DIFFUSION_DIRECTION_METADATA = Metadata(
    id="antsMotionCorrDiffusionDirection",
    name="antsMotionCorrDiffusionDirection",
    package="ants",
)

AntsMotionCorrDiffusionDirection = typing.TypedDict(
    "AntsMotionCorrDiffusionDirection",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsMotionCorrDiffusionDirection"]],
        "scheme": InputPathType,
        "bvec": InputPathType,
        "physical": InputPathType,
        "moco": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class AntsMotionCorrDiffusionDirectionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    corrected_scheme: OutputPathType
    """The output file for corrected diffusion directions."""

def ants_motion_corr_diffusion_direction_params(
    scheme: InputPathType,
    bvec: InputPathType,
    physical: InputPathType,
    moco: InputPathType,
    output: str,
) -> AntsMotionCorrDiffusionDirection:
    """
    Build parameters.

    Args:
        scheme:
        bvec:
        physical:
        moco:
        output:

    Returns:
        Parameter dictionary.
    """
    params: AntsMotionCorrDiffusionDirection = {
        "@type": "ants/antsMotionCorrDiffusionDirection",
        "scheme": scheme,
        "bvec": bvec,
        "physical": physical,
        "moco": moco,
        "output": output,
    }
    return params

def ants_motion_corr_diffusion_direction_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsMotionCorrDiffusionDirection."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scheme", None) is None:
        raise StyxValidationError("`scheme` must not be None")
    if not isinstance(params["scheme"], (pathlib.Path, str)):
        raise StyxValidationError(f'`scheme` has the wrong type: Received `{type(params["scheme"])}` expected `InputPathType`')
    if params.get("bvec", None) is None:
        raise StyxValidationError("`bvec` must not be None")
    if not isinstance(params["bvec"], (pathlib.Path, str)):
        raise StyxValidationError(f'`bvec` has the wrong type: Received `{type(params["bvec"])}` expected `InputPathType`')
    if params.get("physical", None) is None:
        raise StyxValidationError("`physical` must not be None")
    if not isinstance(params["physical"], (pathlib.Path, str)):
        raise StyxValidationError(f'`physical` has the wrong type: Received `{type(params["physical"])}` expected `InputPathType`')
    if params.get("moco", None) is None:
        raise StyxValidationError("`moco` must not be None")
    if not isinstance(params["moco"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moco` has the wrong type: Received `{type(params["moco"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def ants_motion_corr_diffusion_direction_cargs(params: AntsMotionCorrDiffusionDirection, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsMotionCorrDiffusionDirection")
    cargs.append("-s")
    cargs.append(execution.input_file(params["scheme"]))
    cargs.append("-b")
    cargs.append(execution.input_file(params["bvec"]))
    cargs.append("-p")
    cargs.append(execution.input_file(params["physical"]))
    cargs.append("-m")
    cargs.append(execution.input_file(params["moco"]))
    cargs.append("-o")
    cargs.append(params["output"])
    return cargs

def ants_motion_corr_diffusion_direction_outputs(params: AntsMotionCorrDiffusionDirection, execution: Execution) -> AntsMotionCorrDiffusionDirectionOutputs:
    """Build the AntsMotionCorrDiffusionDirectionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    corrected_scheme_v: OutputPathType = execution.output_file(params["output"])
    return AntsMotionCorrDiffusionDirectionOutputs(
        root=root_v,
        corrected_scheme=corrected_scheme_v,
    )

def ants_motion_corr_diffusion_direction_execute(params: AntsMotionCorrDiffusionDirection, runner: Runner | None = None) -> AntsMotionCorrDiffusionDirectionOutputs:
    """
    antsMotionCorrDiffusionDirection

    This tool adjusts the diffusion scheme for motion correction.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_motion_corr_diffusion_direction_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_MOTION_CORR_DIFFUSION_DIRECTION_METADATA)
    execution.params(params)
    args = ants_motion_corr_diffusion_direction_cargs(params, execution)
    out = ants_motion_corr_diffusion_direction_outputs(params, execution)
    execution.run(args)
    return out

def ants_motion_corr_diffusion_direction(
    scheme: InputPathType,
    bvec: InputPathType,
    physical: InputPathType,
    moco: InputPathType,
    output: str,
    runner: Runner | None = None,
) -> AntsMotionCorrDiffusionDirectionOutputs:
    """
    antsMotionCorrDiffusionDirection

    This tool adjusts the diffusion scheme for motion correction.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        scheme:
        bvec:
        physical:
        moco:
        output:
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_motion_corr_diffusion_direction_params(
        scheme=scheme,
        bvec=bvec,
        physical=physical,
        moco=moco,
        output=output,
    )
    return ants_motion_corr_diffusion_direction_execute(params, runner)

__all__ = [
    "AntsMotionCorrDiffusionDirection",
    "AntsMotionCorrDiffusionDirectionOutputs",
    "ANTS_MOTION_CORR_DIFFUSION_DIRECTION_METADATA",
    "ants_motion_corr_diffusion_direction_cargs",
    "ants_motion_corr_diffusion_direction_outputs",
    "ants_motion_corr_diffusion_direction_params",
    "ants_motion_corr_diffusion_direction_execute",
    "ants_motion_corr_diffusion_direction_validate",
    "ants_motion_corr_diffusion_direction",
]