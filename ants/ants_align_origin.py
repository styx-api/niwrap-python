# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_ALIGN_ORIGIN_METADATA = Metadata(
    id="antsAlignOrigin",
    name="antsAlignOrigin",
    package="ants",
)

AntsAlignOriginDimensionality = typing.Literal[2] | typing.Literal[3]

AntsAlignOrigin = typing.TypedDict(
    "AntsAlignOrigin",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsAlignOrigin"]],
        "dimensionality": typing.NotRequired[AntsAlignOriginDimensionality],
        "input": InputPathType,
        "reference_image": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class AntsAlignOriginOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    aligned_image: OutputPathType
    """The output is the aligned image."""

def ants_align_origin_params(
    input_: InputPathType,
    reference_image: InputPathType,
    output: str,
    dimensionality: AntsAlignOriginDimensionality | None = None,
) -> AntsAlignOrigin:
    """
    Build parameters.

    Args:
        input_: Currently, the only input objects supported are image objects.\
            However, the current framework allows for warping of other objects such\
            as meshes and point sets.
        reference_image: For warping input images, the reference image defines the\
            spacing, origin, size, and direction of the output warped image.
        output: One can either output the warped image or, if the boolean is set,\
            one can print out the displacement field based on the composite\
            transform and the reference image.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, antsWarp tries to infer\
            the dimensionality from the input image.

    Returns:
        Parameter dictionary.
    """
    params: AntsAlignOrigin = {
        "@type": "ants/antsAlignOrigin",
        "input": input_,
        "reference_image": reference_image,
        "output": output,
    }
    if dimensionality is not None:
        params["dimensionality"] = dimensionality
    return params

def ants_align_origin_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsAlignOrigin."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensionality", None) is not None:
        if not isinstance(params["dimensionality"], (float, int)):
            raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `AntsAlignOriginDimensionality`')
        if params["dimensionality"] not in [2, 3]:
            raise StyxValidationError("Parameter `dimensionality` must be one of [2, 3]")
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def ants_align_origin_cargs(params: AntsAlignOrigin, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsAlignOrigin")
    v_0 = params.get("dimensionality")
    if v_0 is not None:
        cargs.append("--dimensionality")
        cargs.append(str(v_0))
    cargs.append("--input")
    cargs.append(execution.input_file(params["input"]))
    cargs.append("--reference-image")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("--output")
    cargs.append(params["output"])
    return cargs

def ants_align_origin_outputs(params: AntsAlignOrigin, execution: Execution) -> AntsAlignOriginOutputs:
    """Build the AntsAlignOriginOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    aligned_image_v: OutputPathType = execution.output_file(params["output"])
    return AntsAlignOriginOutputs(
        root=root_v,
        aligned_image=aligned_image_v,
    )

def ants_align_origin_execute(params: AntsAlignOrigin, runner: Runner | None = None) -> AntsAlignOriginOutputs:
    """
    antsAlignOrigin

    antsAlignOrigin, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms).

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_align_origin_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_ALIGN_ORIGIN_METADATA)
    execution.params(params)
    args = ants_align_origin_cargs(params, execution)
    out = ants_align_origin_outputs(params, execution)
    execution.run(args)
    return out

def ants_align_origin(
    input_: InputPathType,
    reference_image: InputPathType,
    output: str,
    dimensionality: AntsAlignOriginDimensionality | None = None,
    runner: Runner | None = None,
) -> AntsAlignOriginOutputs:
    """
    antsAlignOrigin

    antsAlignOrigin, applied to an input image, transforms it according to a reference image and a transform (or a set of transforms).

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        input_: Currently, the only input objects supported are image objects.\
            However, the current framework allows for warping of other objects such\
            as meshes and point sets.
        reference_image: For warping input images, the reference image defines the\
            spacing, origin, size, and direction of the output warped image.
        output: One can either output the warped image or, if the boolean is set,\
            one can print out the displacement field based on the composite\
            transform and the reference image.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, antsWarp tries to infer\
            the dimensionality from the input image.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_align_origin_params(
        input_=input_,
        reference_image=reference_image,
        output=output,
        dimensionality=dimensionality,
    )
    return ants_align_origin_execute(params, runner)

__all__ = [
    "AntsAlignOrigin",
    "AntsAlignOriginOutputs",
    "ANTS_ALIGN_ORIGIN_METADATA",
    "ants_align_origin_cargs",
    "ants_align_origin_outputs",
    "ants_align_origin_params",
    "ants_align_origin_execute",
    "ants_align_origin_validate",
    "ants_align_origin",
]