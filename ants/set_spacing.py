# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SET_SPACING_METADATA = Metadata(
    id="SetSpacing",
    name="SetSpacing",
    package="ants",
)

SetSpacing = typing.TypedDict(
    "SetSpacing",
    {
        "@type": typing.NotRequired[typing.Literal["ants/SetSpacing"]],
        "dimension": int,
        "input_file": InputPathType,
        "output_file": str,
        "spacing": list[float],
    },
)

@dataclasses.dataclass
class SetSpacingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType
    """The output image with the specified spacing."""

def set_spacing_params(
    dimension: int,
    input_file: InputPathType,
    output_file: str,
    spacing: list[float],
) -> SetSpacing:
    """
    Build parameters.

    Args:
        dimension: The dimensionality of the image (e.g., 2 or 3).
        input_file: The input image file in HDR format.
        output_file: The output image file in NII format.
        spacing: Spacing values for each dimension. Requires SpacingX, SpacingY,\
            and optionally SpacingZ.

    Returns:
        Parameter dictionary.
    """
    params: SetSpacing = {
        "@type": "ants/SetSpacing",
        "dimension": dimension,
        "input_file": input_file,
        "output_file": output_file,
        "spacing": spacing,
    }
    return params

def set_spacing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SetSpacing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimension", None) is None:
        raise StyxValidationError("`dimension` must not be None")
    if not isinstance(params["dimension"], int):
        raise StyxValidationError(f'`dimension` has the wrong type: Received `{type(params["dimension"])}` expected `int`')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params["input_file"])}` expected `InputPathType`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params["output_file"])}` expected `str`')
    if params.get("spacing", None) is None:
        raise StyxValidationError("`spacing` must not be None")
    if not isinstance(params["spacing"], list):
        raise StyxValidationError(f'`spacing` has the wrong type: Received `{type(params["spacing"])}` expected `list[float]`')
    if not (2 <= len(params["spacing"]) <= 3):
        raise StyxValidationError("Parameter `spacing` must contain between 2 and 3 elements (inclusive)")
    for e in params["spacing"]:
        if not isinstance(e, (float, int)):
            raise StyxValidationError(f'`spacing` has the wrong type: Received `{type(e)}` expected `list[float]`')

def set_spacing_cargs(params: SetSpacing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SetSpacing")
    cargs.append(str(params["dimension"]))
    cargs.append(execution.input_file(params["input_file"]))
    cargs.append(params["output_file"])
    for item0 in params["spacing"]:
        cargs.append(str(item0))
    return cargs

def set_spacing_outputs(params: SetSpacing, execution: Execution) -> SetSpacingOutputs:
    """Build the SetSpacingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_v: OutputPathType = execution.output_file(params["output_file"])
    return SetSpacingOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def set_spacing_execute(params: SetSpacing, runner: Runner | None = None) -> SetSpacingOutputs:
    """
    SetSpacing

    A tool to set the spacing of an image in each dimension.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    set_spacing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SET_SPACING_METADATA)
    execution.params(params)
    args = set_spacing_cargs(params, execution)
    out = set_spacing_outputs(params, execution)
    execution.run(args)
    return out

def set_spacing(
    dimension: int,
    input_file: InputPathType,
    output_file: str,
    spacing: list[float],
    runner: Runner | None = None,
) -> SetSpacingOutputs:
    """
    SetSpacing

    A tool to set the spacing of an image in each dimension.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        dimension: The dimensionality of the image (e.g., 2 or 3).
        input_file: The input image file in HDR format.
        output_file: The output image file in NII format.
        spacing: Spacing values for each dimension. Requires SpacingX, SpacingY,\
            and optionally SpacingZ.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = set_spacing_params(
        dimension=dimension,
        input_file=input_file,
        output_file=output_file,
        spacing=spacing,
    )
    return set_spacing_execute(params, runner)

__all__ = [
    "SetSpacing",
    "SetSpacingOutputs",
    "SET_SPACING_METADATA",
    "set_spacing_cargs",
    "set_spacing_outputs",
    "set_spacing_params",
    "set_spacing_execute",
    "set_spacing_validate",
    "set_spacing",
]