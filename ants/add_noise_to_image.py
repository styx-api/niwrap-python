# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ADD_NOISE_TO_IMAGE_METADATA = Metadata(
    id="AddNoiseToImage",
    name="AddNoiseToImage",
    package="ants",
)

AddNoiseToImageImageDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

AddNoiseToImageNoiseModel = typing.Literal["AdditiveGaussian"] | typing.Literal["SaltAndPepper"] | typing.Literal["Shot"] | typing.Literal["Speckle"]

AddNoiseToImage = typing.TypedDict(
    "AddNoiseToImage",
    {
        "@type": typing.NotRequired[typing.Literal["ants/AddNoiseToImage"]],
        "image_dimensionality": typing.NotRequired[AddNoiseToImageImageDimensionality],
        "input_image": InputPathType,
        "noise_model": AddNoiseToImageNoiseModel,
        "output": str,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AddNoiseToImageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    noise_corrupted_image: OutputPathType
    """The output is the noise corrupted version of the input image."""

def add_noise_to_image_params(
    input_image: InputPathType,
    noise_model: AddNoiseToImageNoiseModel,
    output: str,
    image_dimensionality: AddNoiseToImageImageDimensionality | None = None,
    verbose: bool | None = None,
) -> AddNoiseToImage:
    """
    Build parameters.

    Args:
        input_image: A scalar image is expected as input for noise correction.
        noise_model: Use different noise models each with its own (default)\
            parameters.
        output: The output consists of the noise corrupted version of the input\
            image.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: AddNoiseToImage = {
        "@type": "ants/AddNoiseToImage",
        "input_image": input_image,
        "noise_model": noise_model,
        "output": output,
    }
    if image_dimensionality is not None:
        params["image_dimensionality"] = image_dimensionality
    if verbose is not None:
        params["verbose"] = verbose
    return params

def add_noise_to_image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AddNoiseToImage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimensionality", None) is not None:
        if not isinstance(params["image_dimensionality"], (float, int)):
            raise StyxValidationError(f'`image_dimensionality` has the wrong type: Received `{type(params["image_dimensionality"])}` expected `AddNoiseToImageImageDimensionality`')
        if params["image_dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `image_dimensionality` must be one of [2, 3, 4]")
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("noise_model", None) is None:
        raise StyxValidationError("`noise_model` must not be None")
    if not isinstance(params["noise_model"], str):
        raise StyxValidationError(f'`noise_model` has the wrong type: Received `{type(params["noise_model"])}` expected `AddNoiseToImageNoiseModel`')
    if params["noise_model"] not in ["AdditiveGaussian", "SaltAndPepper", "Shot", "Speckle"]:
        raise StyxValidationError("Parameter `noise_model` must be one of [\"AdditiveGaussian\", \"SaltAndPepper\", \"Shot\", \"Speckle\"]")
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def add_noise_to_image_cargs(params: AddNoiseToImage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("AddNoiseToImage")
    v_0 = params.get("image_dimensionality")
    if v_0 is not None:
        cargs.append("--image-dimensionality")
        cargs.append(str(v_0))
    cargs.append("--input-image")
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append("--noise-model")
    cargs.append(str(params["noise_model"]))
    cargs.append("--output")
    cargs.append(params["output"])
    v_1 = params.get("verbose")
    if v_1 is not None:
        cargs.append("--verbose")
        if v_1:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def add_noise_to_image_outputs(params: AddNoiseToImage, execution: Execution) -> AddNoiseToImageOutputs:
    """Build the AddNoiseToImageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    noise_corrupted_image_v: OutputPathType = execution.output_file(params["output"])
    return AddNoiseToImageOutputs(
        root=root_v,
        noise_corrupted_image=noise_corrupted_image_v,
    )

def add_noise_to_image_execute(params: AddNoiseToImage, runner: Runner | None = None) -> AddNoiseToImageOutputs:
    """
    AddNoiseToImage

    Add various types of noise to an image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    add_noise_to_image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ADD_NOISE_TO_IMAGE_METADATA)
    execution.params(params)
    args = add_noise_to_image_cargs(params, execution)
    out = add_noise_to_image_outputs(params, execution)
    execution.run(args)
    return out

def add_noise_to_image(
    input_image: InputPathType,
    noise_model: AddNoiseToImageNoiseModel,
    output: str,
    image_dimensionality: AddNoiseToImageImageDimensionality | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AddNoiseToImageOutputs:
    """
    AddNoiseToImage

    Add various types of noise to an image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        input_image: A scalar image is expected as input for noise correction.
        noise_model: Use different noise models each with its own (default)\
            parameters.
        output: The output consists of the noise corrupted version of the input\
            image.
        image_dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = add_noise_to_image_params(
        input_image=input_image,
        noise_model=noise_model,
        output=output,
        image_dimensionality=image_dimensionality,
        verbose=verbose,
    )
    return add_noise_to_image_execute(params, runner)

__all__ = [
    "AddNoiseToImage",
    "AddNoiseToImageOutputs",
    "ADD_NOISE_TO_IMAGE_METADATA",
    "add_noise_to_image_cargs",
    "add_noise_to_image_outputs",
    "add_noise_to_image_params",
    "add_noise_to_image_execute",
    "add_noise_to_image_validate",
    "add_noise_to_image",
]