# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_JOINT_TENSOR_FUSION_METADATA = Metadata(
    id="antsJointTensorFusion",
    name="antsJointTensorFusion",
    package="ants",
)

AntsJointTensorFusionDimensionality = typing.Literal[2] | typing.Literal[3] | typing.Literal[4]

AntsJointTensorFusionPatchMetric = typing.Literal["PC"] | typing.Literal["MSQ"]

AntsJointTensorFusion = typing.TypedDict(
    "AntsJointTensorFusion",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsJointTensorFusion"]],
        "dimensionality": typing.NotRequired[AntsJointTensorFusionDimensionality],
        "target_image": list[str],
        "atlas_image": list[str],
        "atlas_segmentation": InputPathType,
        "alpha": typing.NotRequired[float],
        "beta": typing.NotRequired[float],
        "retain_label_posterior_images": typing.NotRequired[bool],
        "retain_atlas_voting_images": typing.NotRequired[bool],
        "constrain_nonnegative": typing.NotRequired[bool],
        "log_euclidean": typing.NotRequired[bool],
        "patch_radius": typing.NotRequired[str],
        "patch_metric": typing.NotRequired[AntsJointTensorFusionPatchMetric],
        "search_radius": typing.NotRequired[str],
        "exclusion_image": typing.NotRequired[str],
        "mask_image": typing.NotRequired[InputPathType],
        "output": str,
        "verbose": typing.NotRequired[bool],
    },
)

@dataclasses.dataclass
class AntsJointTensorFusionOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    label_fusion_image: OutputPathType
    """The label fusion image output."""
    intensity_fusion_image: OutputPathType
    """The intensity fusion image output."""
    label_posterior_probability_image: OutputPathType
    """The label posterior probability images."""
    atlas_voting_weight_image: OutputPathType
    """The atlas voting weight images."""

def ants_joint_tensor_fusion_params(
    target_image: list[str],
    atlas_image: list[str],
    atlas_segmentation: InputPathType,
    output: str,
    dimensionality: AntsJointTensorFusionDimensionality | None = None,
    alpha: float | None = None,
    beta: float | None = None,
    retain_label_posterior_images: bool | None = None,
    retain_atlas_voting_images: bool | None = None,
    constrain_nonnegative: bool | None = None,
    log_euclidean: bool | None = None,
    patch_radius: str | None = None,
    patch_metric: AntsJointTensorFusionPatchMetric | None = None,
    search_radius: str | None = None,
    exclusion_image: str | None = None,
    mask_image: InputPathType | None = None,
    verbose: bool | None = None,
) -> AntsJointTensorFusion:
    """
    Build parameters.

    Args:
        target_image: The target image (or multimodal target images) assumed to be\
            aligned to a common image domain.
        atlas_image: The atlas image (or multimodal atlas images) assumed to be\
            aligned to a common image domain.
        atlas_segmentation: The atlas segmentation images. For performing label\
            fusion the number of specified segmentations should be identical to the\
            number of atlas image sets.
        output: The output is the intensity and/or label fusion image. Additional\
            optional outputs include the label posterior probability images and the\
            atlas voting weight images.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        alpha: Regularization term added to matrix Mx for calculating the inverse.\
            Default = 0.1
        beta: Exponent for mapping intensity difference to the joint error. Default\
            = 2.0
        retain_label_posterior_images: Retain label posterior probability images.\
            Requires atlas segmentations to be specified. Default = false
        retain_atlas_voting_images: Retain atlas voting images. Default = false
        constrain_nonnegative: Constrain solution to non-negative weights.
        log_euclidean: Use log Euclidean space for tensor math.
        patch_radius: Patch radius for similarity measures. Default = 2x2x2
        patch_metric: Metric to be used in determining the most similar\
            neighborhood patch. Options include Pearson's correlation (PC) and mean\
            squares (MSQ). Default = PC.
        search_radius: Search radius for similarity measures. Default = 3x3x3
        exclusion_image: Specify an exclusion region for the given label.
        mask_image: If a mask image is specified, fusion is only performed in the\
            mask region.
        verbose: Verbose output.

    Returns:
        Parameter dictionary.
    """
    params: AntsJointTensorFusion = {
        "@type": "ants/antsJointTensorFusion",
        "target_image": target_image,
        "atlas_image": atlas_image,
        "atlas_segmentation": atlas_segmentation,
        "output": output,
    }
    if dimensionality is not None:
        params["dimensionality"] = dimensionality
    if alpha is not None:
        params["alpha"] = alpha
    if beta is not None:
        params["beta"] = beta
    if retain_label_posterior_images is not None:
        params["retain_label_posterior_images"] = retain_label_posterior_images
    if retain_atlas_voting_images is not None:
        params["retain_atlas_voting_images"] = retain_atlas_voting_images
    if constrain_nonnegative is not None:
        params["constrain_nonnegative"] = constrain_nonnegative
    if log_euclidean is not None:
        params["log_euclidean"] = log_euclidean
    if patch_radius is not None:
        params["patch_radius"] = patch_radius
    if patch_metric is not None:
        params["patch_metric"] = patch_metric
    if search_radius is not None:
        params["search_radius"] = search_radius
    if exclusion_image is not None:
        params["exclusion_image"] = exclusion_image
    if mask_image is not None:
        params["mask_image"] = mask_image
    if verbose is not None:
        params["verbose"] = verbose
    return params

def ants_joint_tensor_fusion_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsJointTensorFusion."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dimensionality", None) is not None:
        if not isinstance(params["dimensionality"], (float, int)):
            raise StyxValidationError(f'`dimensionality` has the wrong type: Received `{type(params["dimensionality"])}` expected `AntsJointTensorFusionDimensionality`')
        if params["dimensionality"] not in [2, 3, 4]:
            raise StyxValidationError("Parameter `dimensionality` must be one of [2, 3, 4]")
    if params.get("target_image", None) is None:
        raise StyxValidationError("`target_image` must not be None")
    if not isinstance(params["target_image"], list):
        raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(params["target_image"])}` expected `list[str]`')
    for e in params["target_image"]:
        if not isinstance(e, str):
            raise StyxValidationError(f'`target_image` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("atlas_image", None) is None:
        raise StyxValidationError("`atlas_image` must not be None")
    if not isinstance(params["atlas_image"], list):
        raise StyxValidationError(f'`atlas_image` has the wrong type: Received `{type(params["atlas_image"])}` expected `list[str]`')
    for e_2 in params["atlas_image"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`atlas_image` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("atlas_segmentation", None) is None:
        raise StyxValidationError("`atlas_segmentation` must not be None")
    if not isinstance(params["atlas_segmentation"], (pathlib.Path, str)):
        raise StyxValidationError(f'`atlas_segmentation` has the wrong type: Received `{type(params["atlas_segmentation"])}` expected `InputPathType`')
    if params.get("alpha", None) is not None:
        if not isinstance(params["alpha"], (float, int)):
            raise StyxValidationError(f'`alpha` has the wrong type: Received `{type(params["alpha"])}` expected `float`')
    if params.get("beta", None) is not None:
        if not isinstance(params["beta"], (float, int)):
            raise StyxValidationError(f'`beta` has the wrong type: Received `{type(params["beta"])}` expected `float`')
    if params.get("retain_label_posterior_images", None) is not None:
        if not isinstance(params["retain_label_posterior_images"], bool):
            raise StyxValidationError(f'`retain_label_posterior_images` has the wrong type: Received `{type(params["retain_label_posterior_images"])}` expected `bool`')
    if params.get("retain_atlas_voting_images", None) is not None:
        if not isinstance(params["retain_atlas_voting_images"], bool):
            raise StyxValidationError(f'`retain_atlas_voting_images` has the wrong type: Received `{type(params["retain_atlas_voting_images"])}` expected `bool`')
    if params.get("constrain_nonnegative", None) is not None:
        if not isinstance(params["constrain_nonnegative"], bool):
            raise StyxValidationError(f'`constrain_nonnegative` has the wrong type: Received `{type(params["constrain_nonnegative"])}` expected `bool`')
    if params.get("log_euclidean", None) is not None:
        if not isinstance(params["log_euclidean"], bool):
            raise StyxValidationError(f'`log_euclidean` has the wrong type: Received `{type(params["log_euclidean"])}` expected `bool`')
    if params.get("patch_radius", None) is not None:
        if not isinstance(params["patch_radius"], str):
            raise StyxValidationError(f'`patch_radius` has the wrong type: Received `{type(params["patch_radius"])}` expected `str`')
    if params.get("patch_metric", None) is not None:
        if not isinstance(params["patch_metric"], str):
            raise StyxValidationError(f'`patch_metric` has the wrong type: Received `{type(params["patch_metric"])}` expected `AntsJointTensorFusionPatchMetric`')
        if params["patch_metric"] not in ["PC", "MSQ"]:
            raise StyxValidationError("Parameter `patch_metric` must be one of [\"PC\", \"MSQ\"]")
    if params.get("search_radius", None) is not None:
        if not isinstance(params["search_radius"], str):
            raise StyxValidationError(f'`search_radius` has the wrong type: Received `{type(params["search_radius"])}` expected `str`')
    if params.get("exclusion_image", None) is not None:
        if not isinstance(params["exclusion_image"], str):
            raise StyxValidationError(f'`exclusion_image` has the wrong type: Received `{type(params["exclusion_image"])}` expected `str`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], bool):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `bool`')

def ants_joint_tensor_fusion_cargs(params: AntsJointTensorFusion, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsJointTensorFusion")
    v_0 = params.get("dimensionality")
    if v_0 is not None:
        cargs.append("--image-dimensionality")
        cargs.append(str(v_0))
    cargs.append("-t")
    cargs.append(",".join([item0 for item0 in params["target_image"]]))
    cargs.append("-g")
    cargs.append(",".join([item1 for item1 in params["atlas_image"]]))
    cargs.append("-l")
    cargs.append(execution.input_file(params["atlas_segmentation"]))
    v_1 = params.get("alpha")
    if v_1 is not None:
        cargs.append("-a")
        cargs.append(str(v_1))
    v_2 = params.get("beta")
    if v_2 is not None:
        cargs.append("-b")
        cargs.append(str(v_2))
    v_3 = params.get("retain_label_posterior_images")
    if v_3 is not None:
        cargs.append("-r")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    v_4 = params.get("retain_atlas_voting_images")
    if v_4 is not None:
        cargs.append("-f")
        if v_4:
            cargs.append("0")
        else:
            cargs.append("1")
    v_5 = params.get("constrain_nonnegative")
    if v_5 is not None:
        cargs.append("-c")
        if v_5:
            cargs.append("0")
        else:
            cargs.append("1")
    v_6 = params.get("log_euclidean")
    if v_6 is not None:
        cargs.append("-u")
        if v_6:
            cargs.append("0")
        else:
            cargs.append("1")
    v_7 = params.get("patch_radius")
    if v_7 is not None:
        cargs.append("-p")
        cargs.append(v_7)
    v_8 = params.get("patch_metric")
    if v_8 is not None:
        cargs.append("-m")
        cargs.append(str(v_8))
    v_9 = params.get("search_radius")
    if v_9 is not None:
        cargs.append("-s")
        cargs.append(v_9)
    v_10 = params.get("exclusion_image")
    if v_10 is not None:
        cargs.append("-e")
        cargs.append(v_10)
    v_11 = params.get("mask_image")
    if v_11 is not None:
        cargs.append("-x")
        cargs.append(execution.input_file(v_11))
    cargs.append("-o")
    cargs.append(params["output"])
    v_12 = params.get("verbose")
    if v_12 is not None:
        cargs.append("-v")
        if v_12:
            cargs.append("0")
        else:
            cargs.append("1")
    return cargs

def ants_joint_tensor_fusion_outputs(params: AntsJointTensorFusion, execution: Execution) -> AntsJointTensorFusionOutputs:
    """Build the AntsJointTensorFusionOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    label_fusion_image_v: OutputPathType = execution.output_file(f'{params["output"]}_LabelFusion.nii.gz')
    intensity_fusion_image_v: OutputPathType = execution.output_file(f'{params["output"]}_IntensityFusion.nii.gz')
    label_posterior_probability_image_v: OutputPathType = execution.output_file(f'{params["output"]}_LabelPosterior.nii.gz')
    atlas_voting_weight_image_v: OutputPathType = execution.output_file(f'{params["output"]}_AtlasVoting.nii.gz')
    return AntsJointTensorFusionOutputs(
        root=root_v,
        label_fusion_image=label_fusion_image_v,
        intensity_fusion_image=intensity_fusion_image_v,
        label_posterior_probability_image=label_posterior_probability_image_v,
        atlas_voting_weight_image=atlas_voting_weight_image_v,
    )

def ants_joint_tensor_fusion_execute(params: AntsJointTensorFusion, runner: Runner | None = None) -> AntsJointTensorFusionOutputs:
    """
    antsJointTensorFusion

    antsJointTensorFusion is an image fusion algorithm developed by Hongzhi Wang and Paul Yushkevich which won segmentation challenges at MICCAI 2012 and MICCAI 2013. The original label fusion framework was extended to accommodate intensities by Brian Avants. This implementation is based on the original ITK-style implementation and ANTsR implementation.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_joint_tensor_fusion_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_JOINT_TENSOR_FUSION_METADATA)
    execution.params(params)
    args = ants_joint_tensor_fusion_cargs(params, execution)
    out = ants_joint_tensor_fusion_outputs(params, execution)
    execution.run(args)
    return out

def ants_joint_tensor_fusion(
    target_image: list[str],
    atlas_image: list[str],
    atlas_segmentation: InputPathType,
    output: str,
    dimensionality: AntsJointTensorFusionDimensionality | None = None,
    alpha: float | None = None,
    beta: float | None = None,
    retain_label_posterior_images: bool | None = None,
    retain_atlas_voting_images: bool | None = None,
    constrain_nonnegative: bool | None = None,
    log_euclidean: bool | None = None,
    patch_radius: str | None = None,
    patch_metric: AntsJointTensorFusionPatchMetric | None = None,
    search_radius: str | None = None,
    exclusion_image: str | None = None,
    mask_image: InputPathType | None = None,
    verbose: bool | None = None,
    runner: Runner | None = None,
) -> AntsJointTensorFusionOutputs:
    """
    antsJointTensorFusion

    antsJointTensorFusion is an image fusion algorithm developed by Hongzhi Wang and Paul Yushkevich which won segmentation challenges at MICCAI 2012 and MICCAI 2013. The original label fusion framework was extended to accommodate intensities by Brian Avants. This implementation is based on the original ITK-style implementation and ANTsR implementation.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        target_image: The target image (or multimodal target images) assumed to be\
            aligned to a common image domain.
        atlas_image: The atlas image (or multimodal atlas images) assumed to be\
            aligned to a common image domain.
        atlas_segmentation: The atlas segmentation images. For performing label\
            fusion the number of specified segmentations should be identical to the\
            number of atlas image sets.
        output: The output is the intensity and/or label fusion image. Additional\
            optional outputs include the label posterior probability images and the\
            atlas voting weight images.
        dimensionality: This option forces the image to be treated as a\
            specified-dimensional image. If not specified, the program tries to\
            infer the dimensionality from the input image.
        alpha: Regularization term added to matrix Mx for calculating the inverse.\
            Default = 0.1
        beta: Exponent for mapping intensity difference to the joint error. Default\
            = 2.0
        retain_label_posterior_images: Retain label posterior probability images.\
            Requires atlas segmentations to be specified. Default = false
        retain_atlas_voting_images: Retain atlas voting images. Default = false
        constrain_nonnegative: Constrain solution to non-negative weights.
        log_euclidean: Use log Euclidean space for tensor math.
        patch_radius: Patch radius for similarity measures. Default = 2x2x2
        patch_metric: Metric to be used in determining the most similar\
            neighborhood patch. Options include Pearson's correlation (PC) and mean\
            squares (MSQ). Default = PC.
        search_radius: Search radius for similarity measures. Default = 3x3x3
        exclusion_image: Specify an exclusion region for the given label.
        mask_image: If a mask image is specified, fusion is only performed in the\
            mask region.
        verbose: Verbose output.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_joint_tensor_fusion_params(
        target_image=target_image,
        atlas_image=atlas_image,
        atlas_segmentation=atlas_segmentation,
        output=output,
        dimensionality=dimensionality,
        alpha=alpha,
        beta=beta,
        retain_label_posterior_images=retain_label_posterior_images,
        retain_atlas_voting_images=retain_atlas_voting_images,
        constrain_nonnegative=constrain_nonnegative,
        log_euclidean=log_euclidean,
        patch_radius=patch_radius,
        patch_metric=patch_metric,
        search_radius=search_radius,
        exclusion_image=exclusion_image,
        mask_image=mask_image,
        verbose=verbose,
    )
    return ants_joint_tensor_fusion_execute(params, runner)

__all__ = [
    "AntsJointTensorFusion",
    "AntsJointTensorFusionOutputs",
    "ANTS_JOINT_TENSOR_FUSION_METADATA",
    "ants_joint_tensor_fusion_cargs",
    "ants_joint_tensor_fusion_outputs",
    "ants_joint_tensor_fusion_params",
    "ants_joint_tensor_fusion_execute",
    "ants_joint_tensor_fusion_validate",
    "ants_joint_tensor_fusion",
]