# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

ANTS_INTRODUCTION_SH_METADATA = Metadata(
    id="antsIntroduction.sh",
    name="antsIntroduction.sh",
    package="ants",
)

AntsIntroductionShImageDimension = typing.Literal[2] | typing.Literal[3]

AntsIntroductionSh = typing.TypedDict(
    "AntsIntroductionSh",
    {
        "@type": typing.NotRequired[typing.Literal["ants/antsIntroduction.sh"]],
        "image_dimension": AntsIntroductionShImageDimension,
        "reference_image": InputPathType,
        "input_image": InputPathType,
        "force": typing.NotRequired[bool],
        "labels_in_fixed_image_space": typing.NotRequired[str],
        "max_iterations": typing.NotRequired[int],
        "n4_bias_field_correction": typing.NotRequired[bool],
        "outprefix": typing.NotRequired[str],
        "quality_check": typing.NotRequired[bool],
        "similarity_metric": typing.NotRequired[str],
        "transformation_model": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class AntsIntroductionShOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def ants_introduction_sh_params(
    image_dimension: AntsIntroductionShImageDimension,
    reference_image: InputPathType,
    input_image: InputPathType,
    force: bool | None = None,
    labels_in_fixed_image_space: str | None = None,
    max_iterations: int | None = None,
    n4_bias_field_correction_2: bool | None = None,
    outprefix: str | None = None,
    quality_check: bool | None = None,
    similarity_metric: str | None = None,
    transformation_model: str | None = None,
) -> AntsIntroductionSh:
    """
    Build parameters.

    Args:
        image_dimension: Image dimension for registration: 2 or 3.
        reference_image: Reference image for registration.
        input_image: Input image to be registered.
        force: Force script to proceed even if headers may be incompatible.
        labels_in_fixed_image_space: Labels in fixed image space to deform to\
            moving image.
        max_iterations: Maximum number of iterations.
        n4_bias_field_correction_2: N4 Bias Field Correction of moving image: 0 for\
            off, 1 for on.
        outprefix: A prefix that is prepended to all output files.
        quality_check: Perform a Quality Check (QC) of the result: 0 for off, 1 for\
            on.
        similarity_metric: Type of similarity metric used for registration.
        transformation_model: Type of transformation model used for registration.

    Returns:
        Parameter dictionary.
    """
    params: AntsIntroductionSh = {
        "@type": "ants/antsIntroduction.sh",
        "image_dimension": image_dimension,
        "reference_image": reference_image,
        "input_image": input_image,
    }
    if force is not None:
        params["force"] = force
    if labels_in_fixed_image_space is not None:
        params["labels_in_fixed_image_space"] = labels_in_fixed_image_space
    if max_iterations is not None:
        params["max_iterations"] = max_iterations
    if n4_bias_field_correction_2 is not None:
        params["n4_bias_field_correction"] = n4_bias_field_correction_2
    if outprefix is not None:
        params["outprefix"] = outprefix
    if quality_check is not None:
        params["quality_check"] = quality_check
    if similarity_metric is not None:
        params["similarity_metric"] = similarity_metric
    if transformation_model is not None:
        params["transformation_model"] = transformation_model
    return params

def ants_introduction_sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid AntsIntroductionSh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], (float, int)):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `AntsIntroductionShImageDimension`')
    if params["image_dimension"] not in [2, 3]:
        raise StyxValidationError("Parameter `image_dimension` must be one of [2, 3]")
    if params.get("reference_image", None) is None:
        raise StyxValidationError("`reference_image` must not be None")
    if not isinstance(params["reference_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`reference_image` has the wrong type: Received `{type(params["reference_image"])}` expected `InputPathType`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("labels_in_fixed_image_space", None) is not None:
        if not isinstance(params["labels_in_fixed_image_space"], str):
            raise StyxValidationError(f'`labels_in_fixed_image_space` has the wrong type: Received `{type(params["labels_in_fixed_image_space"])}` expected `str`')
    if params.get("max_iterations", None) is not None:
        if not isinstance(params["max_iterations"], int):
            raise StyxValidationError(f'`max_iterations` has the wrong type: Received `{type(params["max_iterations"])}` expected `int`')
    if params.get("n4_bias_field_correction", None) is not None:
        if not isinstance(params["n4_bias_field_correction"], bool):
            raise StyxValidationError(f'`n4_bias_field_correction` has the wrong type: Received `{type(params["n4_bias_field_correction"])}` expected `bool`')
    if params.get("outprefix", None) is not None:
        if not isinstance(params["outprefix"], str):
            raise StyxValidationError(f'`outprefix` has the wrong type: Received `{type(params["outprefix"])}` expected `str`')
    if params.get("quality_check", None) is not None:
        if not isinstance(params["quality_check"], bool):
            raise StyxValidationError(f'`quality_check` has the wrong type: Received `{type(params["quality_check"])}` expected `bool`')
    if params.get("similarity_metric", None) is not None:
        if not isinstance(params["similarity_metric"], str):
            raise StyxValidationError(f'`similarity_metric` has the wrong type: Received `{type(params["similarity_metric"])}` expected `str`')
    if params.get("transformation_model", None) is not None:
        if not isinstance(params["transformation_model"], str):
            raise StyxValidationError(f'`transformation_model` has the wrong type: Received `{type(params["transformation_model"])}` expected `str`')

def ants_introduction_sh_cargs(params: AntsIntroductionSh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("antsIntroduction.sh")
    cargs.append("-d")
    cargs.append(str(params["image_dimension"]))
    cargs.append("-r")
    cargs.append(execution.input_file(params["reference_image"]))
    cargs.append("-i")
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("force")
    if v_0 is not None:
        cargs.append("-f")
        if v_0:
            cargs.append("0")
        else:
            cargs.append("1")
    v_1 = params.get("labels_in_fixed_image_space")
    if v_1 is not None:
        cargs.append("-l")
        cargs.append(v_1)
    v_2 = params.get("max_iterations")
    if v_2 is not None:
        cargs.append("-m")
        cargs.append(str(v_2))
    v_3 = params.get("n4_bias_field_correction")
    if v_3 is not None:
        cargs.append("-n")
        if v_3:
            cargs.append("0")
        else:
            cargs.append("1")
    v_4 = params.get("outprefix")
    if v_4 is not None:
        cargs.append("-o")
        cargs.append(v_4)
    v_5 = params.get("quality_check")
    if v_5 is not None:
        cargs.append("-q")
        if v_5:
            cargs.append("0")
        else:
            cargs.append("1")
    v_6 = params.get("similarity_metric")
    if v_6 is not None:
        cargs.append("-s")
        cargs.append(v_6)
    v_7 = params.get("transformation_model")
    if v_7 is not None:
        cargs.append("-t")
        cargs.append(v_7)
    return cargs

def ants_introduction_sh_outputs(params: AntsIntroductionSh, execution: Execution) -> AntsIntroductionShOutputs:
    """Build the AntsIntroductionShOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return AntsIntroductionShOutputs(
        root=root_v,
    )

def ants_introduction_sh_execute(params: AntsIntroductionSh, runner: Runner | None = None) -> AntsIntroductionShOutputs:
    """
    antsIntroduction.sh

    Script for registration using ANTS with compulsory and optional arguments.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    ants_introduction_sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(ANTS_INTRODUCTION_SH_METADATA)
    execution.params(params)
    args = ants_introduction_sh_cargs(params, execution)
    out = ants_introduction_sh_outputs(params, execution)
    execution.run(args)
    return out

def ants_introduction_sh(
    image_dimension: AntsIntroductionShImageDimension,
    reference_image: InputPathType,
    input_image: InputPathType,
    force: bool | None = None,
    labels_in_fixed_image_space: str | None = None,
    max_iterations: int | None = None,
    n4_bias_field_correction_2: bool | None = None,
    outprefix: str | None = None,
    quality_check: bool | None = None,
    similarity_metric: str | None = None,
    transformation_model: str | None = None,
    runner: Runner | None = None,
) -> AntsIntroductionShOutputs:
    """
    antsIntroduction.sh

    Script for registration using ANTS with compulsory and optional arguments.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: Image dimension for registration: 2 or 3.
        reference_image: Reference image for registration.
        input_image: Input image to be registered.
        force: Force script to proceed even if headers may be incompatible.
        labels_in_fixed_image_space: Labels in fixed image space to deform to\
            moving image.
        max_iterations: Maximum number of iterations.
        n4_bias_field_correction_2: N4 Bias Field Correction of moving image: 0 for\
            off, 1 for on.
        outprefix: A prefix that is prepended to all output files.
        quality_check: Perform a Quality Check (QC) of the result: 0 for off, 1 for\
            on.
        similarity_metric: Type of similarity metric used for registration.
        transformation_model: Type of transformation model used for registration.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = ants_introduction_sh_params(
        image_dimension=image_dimension,
        reference_image=reference_image,
        input_image=input_image,
        force=force,
        labels_in_fixed_image_space=labels_in_fixed_image_space,
        max_iterations=max_iterations,
        n4_bias_field_correction_2=n4_bias_field_correction_2,
        outprefix=outprefix,
        quality_check=quality_check,
        similarity_metric=similarity_metric,
        transformation_model=transformation_model,
    )
    return ants_introduction_sh_execute(params, runner)

__all__ = [
    "AntsIntroductionSh",
    "AntsIntroductionShOutputs",
    "ANTS_INTRODUCTION_SH_METADATA",
    "ants_introduction_sh_cargs",
    "ants_introduction_sh_outputs",
    "ants_introduction_sh_params",
    "ants_introduction_sh_execute",
    "ants_introduction_sh_validate",
    "ants_introduction_sh",
]