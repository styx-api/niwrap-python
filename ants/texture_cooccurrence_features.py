# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TEXTURE_COOCCURRENCE_FEATURES_METADATA = Metadata(
    id="TextureCooccurrenceFeatures",
    name="TextureCooccurrenceFeatures",
    package="ants",
)

TextureCooccurrenceFeatures = typing.TypedDict(
    "TextureCooccurrenceFeatures",
    {
        "@type": typing.NotRequired[typing.Literal["ants/TextureCooccurrenceFeatures"]],
        "image_dimension": int,
        "input_image": InputPathType,
        "number_of_bins_per_axis": typing.NotRequired[int],
        "mask_image": typing.NotRequired[InputPathType],
        "mask_label": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class TextureCooccurrenceFeaturesOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    features_output: OutputPathType
    """The output file containing the calculated texture co-occurrence features."""

def texture_cooccurrence_features_params(
    image_dimension: int,
    input_image: InputPathType,
    number_of_bins_per_axis: int | None = None,
    mask_image: InputPathType | None = None,
    mask_label: int | None = None,
) -> TextureCooccurrenceFeatures:
    """
    Build parameters.

    Args:
        image_dimension: The dimensionality of the input image, e.g., 2 for 2D\
            images, 3 for 3D images.
        input_image: The input image file for which texture co-occurrence features\
            will be calculated.
        number_of_bins_per_axis: The number of bins per axis to be used in the\
            histogram for texture calculation. Defaults to 256.
        mask_image: Optional mask image to specify the regions of interest in the\
            input image for which features will be calculated.
        mask_label: Label value in the mask image to specify which region to\
            process. Defaults to 1.

    Returns:
        Parameter dictionary.
    """
    params: TextureCooccurrenceFeatures = {
        "@type": "ants/TextureCooccurrenceFeatures",
        "image_dimension": image_dimension,
        "input_image": input_image,
    }
    if number_of_bins_per_axis is not None:
        params["number_of_bins_per_axis"] = number_of_bins_per_axis
    if mask_image is not None:
        params["mask_image"] = mask_image
    if mask_label is not None:
        params["mask_label"] = mask_label
    return params

def texture_cooccurrence_features_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid TextureCooccurrenceFeatures."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("number_of_bins_per_axis", None) is not None:
        if not isinstance(params["number_of_bins_per_axis"], int):
            raise StyxValidationError(f'`number_of_bins_per_axis` has the wrong type: Received `{type(params["number_of_bins_per_axis"])}` expected `int`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("mask_label", None) is not None:
        if not isinstance(params["mask_label"], int):
            raise StyxValidationError(f'`mask_label` has the wrong type: Received `{type(params["mask_label"])}` expected `int`')

def texture_cooccurrence_features_cargs(params: TextureCooccurrenceFeatures, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("TextureCooccurrenceFeatures")
    cargs.append(str(params["image_dimension"]))
    cargs.append(execution.input_file(params["input_image"]))
    v_0 = params.get("number_of_bins_per_axis")
    if v_0 is not None:
        cargs.append(str(v_0))
    v_1 = params.get("mask_image")
    if v_1 is not None:
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("mask_label")
    if v_2 is not None:
        cargs.append(str(v_2))
    return cargs

def texture_cooccurrence_features_outputs(params: TextureCooccurrenceFeatures, execution: Execution) -> TextureCooccurrenceFeaturesOutputs:
    """Build the TextureCooccurrenceFeaturesOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    features_output_v: OutputPathType = execution.output_file(f'{params["input_image"]}_features.txt')
    return TextureCooccurrenceFeaturesOutputs(
        root=root_v,
        features_output=features_output_v,
    )

def texture_cooccurrence_features_execute(params: TextureCooccurrenceFeatures, runner: Runner | None = None) -> TextureCooccurrenceFeaturesOutputs:
    """
    TextureCooccurrenceFeatures

    Calculates texture co-occurrence features such as Energy, Entropy, Inverse Difference Moment, Inertia, Cluster Shade, and Cluster Prominence from an input image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    texture_cooccurrence_features_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TEXTURE_COOCCURRENCE_FEATURES_METADATA)
    execution.params(params)
    args = texture_cooccurrence_features_cargs(params, execution)
    out = texture_cooccurrence_features_outputs(params, execution)
    execution.run(args)
    return out

def texture_cooccurrence_features(
    image_dimension: int,
    input_image: InputPathType,
    number_of_bins_per_axis: int | None = None,
    mask_image: InputPathType | None = None,
    mask_label: int | None = None,
    runner: Runner | None = None,
) -> TextureCooccurrenceFeaturesOutputs:
    """
    TextureCooccurrenceFeatures

    Calculates texture co-occurrence features such as Energy, Entropy, Inverse Difference Moment, Inertia, Cluster Shade, and Cluster Prominence from an input image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: The dimensionality of the input image, e.g., 2 for 2D\
            images, 3 for 3D images.
        input_image: The input image file for which texture co-occurrence features\
            will be calculated.
        number_of_bins_per_axis: The number of bins per axis to be used in the\
            histogram for texture calculation. Defaults to 256.
        mask_image: Optional mask image to specify the regions of interest in the\
            input image for which features will be calculated.
        mask_label: Label value in the mask image to specify which region to\
            process. Defaults to 1.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = texture_cooccurrence_features_params(
        image_dimension=image_dimension,
        input_image=input_image,
        number_of_bins_per_axis=number_of_bins_per_axis,
        mask_image=mask_image,
        mask_label=mask_label,
    )
    return texture_cooccurrence_features_execute(params, runner)

__all__ = [
    "TextureCooccurrenceFeatures",
    "TextureCooccurrenceFeaturesOutputs",
    "TEXTURE_COOCCURRENCE_FEATURES_METADATA",
    "texture_cooccurrence_features_cargs",
    "texture_cooccurrence_features_outputs",
    "texture_cooccurrence_features_params",
    "texture_cooccurrence_features_execute",
    "texture_cooccurrence_features_validate",
    "texture_cooccurrence_features",
]