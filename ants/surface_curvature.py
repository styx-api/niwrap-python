# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_CURVATURE_METADATA = Metadata(
    id="SurfaceCurvature",
    name="SurfaceCurvature",
    package="ants",
)

SurfaceCurvatureOption = typing.Literal[0] | typing.Literal[5] | typing.Literal[6] | typing.Literal[7]

SurfaceCurvature = typing.TypedDict(
    "SurfaceCurvature",
    {
        "@type": typing.NotRequired[typing.Literal["ants/SurfaceCurvature"]],
        "filename_in": InputPathType,
        "filename_out": str,
        "sigma": float,
        "option": SurfaceCurvatureOption,
    },
)

@dataclasses.dataclass
class SurfaceCurvatureOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image: OutputPathType
    """The processed image file."""

def surface_curvature_params(
    filename_in: InputPathType,
    filename_out: str,
    sigma: float,
    option: SurfaceCurvatureOption,
) -> SurfaceCurvature:
    """
    Build parameters.

    Args:
        filename_in: The input image file in .nii format.
        filename_out: The output image file in .nii format.
        sigma: The sigma value for analysis.
        option: The operation mode: 0 for mean curvature, 5 for surface\
            characterization, 6 for Gaussian curvature, and 7 for surface area.

    Returns:
        Parameter dictionary.
    """
    params: SurfaceCurvature = {
        "@type": "ants/SurfaceCurvature",
        "filename_in": filename_in,
        "filename_out": filename_out,
        "sigma": sigma,
        "option": option,
    }
    return params

def surface_curvature_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceCurvature."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("filename_in", None) is None:
        raise StyxValidationError("`filename_in` must not be None")
    if not isinstance(params["filename_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`filename_in` has the wrong type: Received `{type(params["filename_in"])}` expected `InputPathType`')
    if params.get("filename_out", None) is None:
        raise StyxValidationError("`filename_out` must not be None")
    if not isinstance(params["filename_out"], str):
        raise StyxValidationError(f'`filename_out` has the wrong type: Received `{type(params["filename_out"])}` expected `str`')
    if params.get("sigma", None) is None:
        raise StyxValidationError("`sigma` must not be None")
    if not isinstance(params["sigma"], (float, int)):
        raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')
    if params.get("option", None) is None:
        raise StyxValidationError("`option` must not be None")
    if not isinstance(params["option"], (float, int)):
        raise StyxValidationError(f'`option` has the wrong type: Received `{type(params["option"])}` expected `SurfaceCurvatureOption`')
    if params["option"] not in [0, 5, 6, 7]:
        raise StyxValidationError("Parameter `option` must be one of [0, 5, 6, 7]")

def surface_curvature_cargs(params: SurfaceCurvature, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SurfaceCurvature")
    cargs.append(execution.input_file(params["filename_in"]))
    cargs.append(params["filename_out"])
    cargs.append(str(params["sigma"]))
    cargs.append(str(params["option"]))
    return cargs

def surface_curvature_outputs(params: SurfaceCurvature, execution: Execution) -> SurfaceCurvatureOutputs:
    """Build the SurfaceCurvatureOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_image_v: OutputPathType = execution.output_file(params["filename_out"])
    return SurfaceCurvatureOutputs(
        root=root_v,
        output_image=output_image_v,
    )

def surface_curvature_execute(params: SurfaceCurvature, runner: Runner | None = None) -> SurfaceCurvatureOutputs:
    """
    SurfaceCurvature

    The Shape Operator for Differential Analysis of Images. It can operate on binary or gray scale images with various modes to see different effects.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_curvature_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_CURVATURE_METADATA)
    execution.params(params)
    args = surface_curvature_cargs(params, execution)
    out = surface_curvature_outputs(params, execution)
    execution.run(args)
    return out

def surface_curvature(
    filename_in: InputPathType,
    filename_out: str,
    sigma: float,
    option: SurfaceCurvatureOption,
    runner: Runner | None = None,
) -> SurfaceCurvatureOutputs:
    """
    SurfaceCurvature

    The Shape Operator for Differential Analysis of Images. It can operate on binary or gray scale images with various modes to see different effects.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        filename_in: The input image file in .nii format.
        filename_out: The output image file in .nii format.
        sigma: The sigma value for analysis.
        option: The operation mode: 0 for mean curvature, 5 for surface\
            characterization, 6 for Gaussian curvature, and 7 for surface area.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_curvature_params(
        filename_in=filename_in,
        filename_out=filename_out,
        sigma=sigma,
        option=option,
    )
    return surface_curvature_execute(params, runner)

__all__ = [
    "SurfaceCurvature",
    "SurfaceCurvatureOutputs",
    "SURFACE_CURVATURE_METADATA",
    "surface_curvature_cargs",
    "surface_curvature_outputs",
    "surface_curvature_params",
    "surface_curvature_execute",
    "surface_curvature_validate",
    "surface_curvature",
]