# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CREATE_WARPED_GRID_IMAGE_METADATA = Metadata(
    id="CreateWarpedGridImage",
    name="CreateWarpedGridImage",
    package="ants",
)

CreateWarpedGridImage = typing.TypedDict(
    "CreateWarpedGridImage",
    {
        "@type": typing.NotRequired[typing.Literal["ants/CreateWarpedGridImage"]],
        "image_dimension": int,
        "deformation_field": InputPathType,
        "output_image": str,
        "directions": typing.NotRequired[str],
        "grid_spacing": typing.NotRequired[str],
        "grid_sigma": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class CreateWarpedGridImageOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    warped_grid_image: OutputPathType
    """The resultant warped grid image."""

def create_warped_grid_image_params(
    image_dimension: int,
    deformation_field: InputPathType,
    output_image: str,
    directions: str | None = None,
    grid_spacing: str | None = None,
    grid_sigma: str | None = None,
) -> CreateWarpedGridImage:
    """
    Build parameters.

    Args:
        image_dimension: The dimensionality of the input image.
        deformation_field: File containing the deformation field to be applied.
        output_image: The filename of the output warped grid image.
        directions: Directions for the grid warping, e.g., '1x0x0'.
        grid_spacing: Spacing of the grid, e.g., '10x10x10'.
        grid_sigma: Sigma value for the grid smoothing, e.g., '1x1x1'.

    Returns:
        Parameter dictionary.
    """
    params: CreateWarpedGridImage = {
        "@type": "ants/CreateWarpedGridImage",
        "image_dimension": image_dimension,
        "deformation_field": deformation_field,
        "output_image": output_image,
    }
    if directions is not None:
        params["directions"] = directions
    if grid_spacing is not None:
        params["grid_spacing"] = grid_spacing
    if grid_sigma is not None:
        params["grid_sigma"] = grid_sigma
    return params

def create_warped_grid_image_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid CreateWarpedGridImage."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_dimension", None) is None:
        raise StyxValidationError("`image_dimension` must not be None")
    if not isinstance(params["image_dimension"], int):
        raise StyxValidationError(f'`image_dimension` has the wrong type: Received `{type(params["image_dimension"])}` expected `int`')
    if params.get("deformation_field", None) is None:
        raise StyxValidationError("`deformation_field` must not be None")
    if not isinstance(params["deformation_field"], (pathlib.Path, str)):
        raise StyxValidationError(f'`deformation_field` has the wrong type: Received `{type(params["deformation_field"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("directions", None) is not None:
        if not isinstance(params["directions"], str):
            raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `str`')
    if params.get("grid_spacing", None) is not None:
        if not isinstance(params["grid_spacing"], str):
            raise StyxValidationError(f'`grid_spacing` has the wrong type: Received `{type(params["grid_spacing"])}` expected `str`')
    if params.get("grid_sigma", None) is not None:
        if not isinstance(params["grid_sigma"], str):
            raise StyxValidationError(f'`grid_sigma` has the wrong type: Received `{type(params["grid_sigma"])}` expected `str`')

def create_warped_grid_image_cargs(params: CreateWarpedGridImage, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("CreateWarpedGridImage")
    cargs.append(str(params["image_dimension"]))
    cargs.append(execution.input_file(params["deformation_field"]))
    cargs.append(params["output_image"])
    v_0 = params.get("directions")
    if v_0 is not None:
        cargs.append(v_0)
    v_1 = params.get("grid_spacing")
    if v_1 is not None:
        cargs.append(v_1)
    v_2 = params.get("grid_sigma")
    if v_2 is not None:
        cargs.append(v_2)
    return cargs

def create_warped_grid_image_outputs(params: CreateWarpedGridImage, execution: Execution) -> CreateWarpedGridImageOutputs:
    """Build the CreateWarpedGridImageOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    warped_grid_image_v: OutputPathType = execution.output_file(params["output_image"])
    return CreateWarpedGridImageOutputs(
        root=root_v,
        warped_grid_image=warped_grid_image_v,
    )

def create_warped_grid_image_execute(params: CreateWarpedGridImage, runner: Runner | None = None) -> CreateWarpedGridImageOutputs:
    """
    CreateWarpedGridImage

    Create a warped grid image based on the specified deformation field.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    create_warped_grid_image_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CREATE_WARPED_GRID_IMAGE_METADATA)
    execution.params(params)
    args = create_warped_grid_image_cargs(params, execution)
    out = create_warped_grid_image_outputs(params, execution)
    execution.run(args)
    return out

def create_warped_grid_image(
    image_dimension: int,
    deformation_field: InputPathType,
    output_image: str,
    directions: str | None = None,
    grid_spacing: str | None = None,
    grid_sigma: str | None = None,
    runner: Runner | None = None,
) -> CreateWarpedGridImageOutputs:
    """
    CreateWarpedGridImage

    Create a warped grid image based on the specified deformation field.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_dimension: The dimensionality of the input image.
        deformation_field: File containing the deformation field to be applied.
        output_image: The filename of the output warped grid image.
        directions: Directions for the grid warping, e.g., '1x0x0'.
        grid_spacing: Spacing of the grid, e.g., '10x10x10'.
        grid_sigma: Sigma value for the grid smoothing, e.g., '1x1x1'.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = create_warped_grid_image_params(
        image_dimension=image_dimension,
        deformation_field=deformation_field,
        output_image=output_image,
        directions=directions,
        grid_spacing=grid_spacing,
        grid_sigma=grid_sigma,
    )
    return create_warped_grid_image_execute(params, runner)

__all__ = [
    "CreateWarpedGridImage",
    "CreateWarpedGridImageOutputs",
    "CREATE_WARPED_GRID_IMAGE_METADATA",
    "create_warped_grid_image_cargs",
    "create_warped_grid_image_outputs",
    "create_warped_grid_image_params",
    "create_warped_grid_image_execute",
    "create_warped_grid_image_validate",
    "create_warped_grid_image",
]