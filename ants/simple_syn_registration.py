# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SIMPLE_SYN_REGISTRATION_METADATA = Metadata(
    id="simpleSynRegistration",
    name="simpleSynRegistration",
    package="ants",
)

SimpleSynRegistration = typing.TypedDict(
    "SimpleSynRegistration",
    {
        "@type": typing.NotRequired[typing.Literal["ants/simpleSynRegistration"]],
        "fixed_image": InputPathType,
        "moving_image": InputPathType,
        "initial_transform": str,
        "output_prefix": str,
    },
)

@dataclasses.dataclass
class SimpleSynRegistrationOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    registered_image: OutputPathType
    """The output registered image."""
    transform_matrix: OutputPathType
    """The output transformation matrix."""

def simple_syn_registration_params(
    fixed_image: InputPathType,
    moving_image: InputPathType,
    initial_transform: str,
    output_prefix: str,
) -> SimpleSynRegistration:
    """
    Build parameters.

    Args:
        fixed_image: The fixed image for registration.
        moving_image: The moving image to be registered.
        initial_transform: The initial transform for registration.
        output_prefix: Prefix for the output file name without any extension.

    Returns:
        Parameter dictionary.
    """
    params: SimpleSynRegistration = {
        "@type": "ants/simpleSynRegistration",
        "fixed_image": fixed_image,
        "moving_image": moving_image,
        "initial_transform": initial_transform,
        "output_prefix": output_prefix,
    }
    return params

def simple_syn_registration_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SimpleSynRegistration."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fixed_image", None) is None:
        raise StyxValidationError("`fixed_image` must not be None")
    if not isinstance(params["fixed_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixed_image` has the wrong type: Received `{type(params["fixed_image"])}` expected `InputPathType`')
    if params.get("moving_image", None) is None:
        raise StyxValidationError("`moving_image` must not be None")
    if not isinstance(params["moving_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`moving_image` has the wrong type: Received `{type(params["moving_image"])}` expected `InputPathType`')
    if params.get("initial_transform", None) is None:
        raise StyxValidationError("`initial_transform` must not be None")
    if not isinstance(params["initial_transform"], str):
        raise StyxValidationError(f'`initial_transform` has the wrong type: Received `{type(params["initial_transform"])}` expected `str`')
    if params.get("output_prefix", None) is None:
        raise StyxValidationError("`output_prefix` must not be None")
    if not isinstance(params["output_prefix"], str):
        raise StyxValidationError(f'`output_prefix` has the wrong type: Received `{type(params["output_prefix"])}` expected `str`')

def simple_syn_registration_cargs(params: SimpleSynRegistration, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("simpleSynRegistration")
    cargs.append(execution.input_file(params["fixed_image"]))
    cargs.append(execution.input_file(params["moving_image"]))
    cargs.append(params["initial_transform"])
    cargs.append(params["output_prefix"])
    return cargs

def simple_syn_registration_outputs(params: SimpleSynRegistration, execution: Execution) -> SimpleSynRegistrationOutputs:
    """Build the SimpleSynRegistrationOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    registered_image_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}Registered.nii.gz')
    transform_matrix_v: OutputPathType = execution.output_file(f'{params["output_prefix"]}Transform.mat')
    return SimpleSynRegistrationOutputs(
        root=root_v,
        registered_image=registered_image_v,
        transform_matrix=transform_matrix_v,
    )

def simple_syn_registration_execute(params: SimpleSynRegistration, runner: Runner | None = None) -> SimpleSynRegistrationOutputs:
    """
    simpleSynRegistration

    A simple SyN registration tool.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    simple_syn_registration_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SIMPLE_SYN_REGISTRATION_METADATA)
    execution.params(params)
    args = simple_syn_registration_cargs(params, execution)
    out = simple_syn_registration_outputs(params, execution)
    execution.run(args)
    return out

def simple_syn_registration(
    fixed_image: InputPathType,
    moving_image: InputPathType,
    initial_transform: str,
    output_prefix: str,
    runner: Runner | None = None,
) -> SimpleSynRegistrationOutputs:
    """
    simpleSynRegistration

    A simple SyN registration tool.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        fixed_image: The fixed image for registration.
        moving_image: The moving image to be registered.
        initial_transform: The initial transform for registration.
        output_prefix: Prefix for the output file name without any extension.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = simple_syn_registration_params(
        fixed_image=fixed_image,
        moving_image=moving_image,
        initial_transform=initial_transform,
        output_prefix=output_prefix,
    )
    return simple_syn_registration_execute(params, runner)

__all__ = [
    "SimpleSynRegistration",
    "SimpleSynRegistrationOutputs",
    "SIMPLE_SYN_REGISTRATION_METADATA",
    "simple_syn_registration_cargs",
    "simple_syn_registration_outputs",
    "simple_syn_registration_params",
    "simple_syn_registration_execute",
    "simple_syn_registration_validate",
    "simple_syn_registration",
]