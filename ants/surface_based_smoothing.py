# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SURFACE_BASED_SMOOTHING_METADATA = Metadata(
    id="SurfaceBasedSmoothing",
    name="SurfaceBasedSmoothing",
    package="ants",
)

SurfaceBasedSmoothing = typing.TypedDict(
    "SurfaceBasedSmoothing",
    {
        "@type": typing.NotRequired[typing.Literal["ants/SurfaceBasedSmoothing"]],
        "image_to_smooth": InputPathType,
        "sigma": float,
        "surface_image": InputPathType,
        "outname": str,
        "num_repeats": typing.NotRequired[int],
    },
)

@dataclasses.dataclass
class SurfaceBasedSmoothingOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    smoothed_output: OutputPathType
    """The output smoothed image."""

def surface_based_smoothing_params(
    image_to_smooth: InputPathType,
    sigma: float,
    surface_image: InputPathType,
    outname: str,
    num_repeats: int | None = None,
) -> SurfaceBasedSmoothing:
    """
    Build parameters.

    Args:
        image_to_smooth: The image that needs to be smoothed.
        sigma: Geodesic neighborhood radius.
        surface_image: Assumes a label == 1 that defines the surface.
        outname: The name of the output file.
        num_repeats: Number of times the geodesic neighborhood is applied\
            repeatedly.

    Returns:
        Parameter dictionary.
    """
    params: SurfaceBasedSmoothing = {
        "@type": "ants/SurfaceBasedSmoothing",
        "image_to_smooth": image_to_smooth,
        "sigma": sigma,
        "surface_image": surface_image,
        "outname": outname,
    }
    if num_repeats is not None:
        params["num_repeats"] = num_repeats
    return params

def surface_based_smoothing_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid SurfaceBasedSmoothing."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("image_to_smooth", None) is None:
        raise StyxValidationError("`image_to_smooth` must not be None")
    if not isinstance(params["image_to_smooth"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image_to_smooth` has the wrong type: Received `{type(params["image_to_smooth"])}` expected `InputPathType`')
    if params.get("sigma", None) is None:
        raise StyxValidationError("`sigma` must not be None")
    if not isinstance(params["sigma"], (float, int)):
        raise StyxValidationError(f'`sigma` has the wrong type: Received `{type(params["sigma"])}` expected `float`')
    if params.get("surface_image", None) is None:
        raise StyxValidationError("`surface_image` must not be None")
    if not isinstance(params["surface_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`surface_image` has the wrong type: Received `{type(params["surface_image"])}` expected `InputPathType`')
    if params.get("outname", None) is None:
        raise StyxValidationError("`outname` must not be None")
    if not isinstance(params["outname"], str):
        raise StyxValidationError(f'`outname` has the wrong type: Received `{type(params["outname"])}` expected `str`')
    if params.get("num_repeats", None) is not None:
        if not isinstance(params["num_repeats"], int):
            raise StyxValidationError(f'`num_repeats` has the wrong type: Received `{type(params["num_repeats"])}` expected `int`')

def surface_based_smoothing_cargs(params: SurfaceBasedSmoothing, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("SurfaceBasedSmoothing")
    cargs.append(execution.input_file(params["image_to_smooth"]))
    cargs.append(str(params["sigma"]))
    cargs.append(execution.input_file(params["surface_image"]))
    cargs.append(params["outname"])
    v_0 = params.get("num_repeats")
    if v_0 is not None:
        cargs.append(str(v_0))
    return cargs

def surface_based_smoothing_outputs(params: SurfaceBasedSmoothing, execution: Execution) -> SurfaceBasedSmoothingOutputs:
    """Build the SurfaceBasedSmoothingOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    smoothed_output_v: OutputPathType = execution.output_file(params["outname"])
    return SurfaceBasedSmoothingOutputs(
        root=root_v,
        smoothed_output=smoothed_output_v,
    )

def surface_based_smoothing_execute(params: SurfaceBasedSmoothing, runner: Runner | None = None) -> SurfaceBasedSmoothingOutputs:
    """
    SurfaceBasedSmoothing

    Surface-based smoothing applied to ImageToSmooth using a geodesic neighbourhood defined by sigma and the surface image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    surface_based_smoothing_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SURFACE_BASED_SMOOTHING_METADATA)
    execution.params(params)
    args = surface_based_smoothing_cargs(params, execution)
    out = surface_based_smoothing_outputs(params, execution)
    execution.run(args)
    return out

def surface_based_smoothing(
    image_to_smooth: InputPathType,
    sigma: float,
    surface_image: InputPathType,
    outname: str,
    num_repeats: int | None = None,
    runner: Runner | None = None,
) -> SurfaceBasedSmoothingOutputs:
    """
    SurfaceBasedSmoothing

    Surface-based smoothing applied to ImageToSmooth using a geodesic neighbourhood defined by sigma and the surface image.

    Author: ANTs Developers

    URL: https://github.com/ANTsX/ANTs

    Args:
        image_to_smooth: The image that needs to be smoothed.
        sigma: Geodesic neighborhood radius.
        surface_image: Assumes a label == 1 that defines the surface.
        outname: The name of the output file.
        num_repeats: Number of times the geodesic neighborhood is applied\
            repeatedly.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = surface_based_smoothing_params(
        image_to_smooth=image_to_smooth,
        sigma=sigma,
        surface_image=surface_image,
        outname=outname,
        num_repeats=num_repeats,
    )
    return surface_based_smoothing_execute(params, runner)

__all__ = [
    "SurfaceBasedSmoothing",
    "SurfaceBasedSmoothingOutputs",
    "SURFACE_BASED_SMOOTHING_METADATA",
    "surface_based_smoothing_cargs",
    "surface_based_smoothing_outputs",
    "surface_based_smoothing_params",
    "surface_based_smoothing_execute",
    "surface_based_smoothing_validate",
    "surface_based_smoothing",
]