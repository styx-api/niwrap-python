# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MESHFILTER_METADATA = Metadata(
    id="meshfilter",
    name="meshfilter",
    package="mrtrix",
)

class MeshfilterConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Meshfilter = typing.TypedDict(
    "Meshfilter",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/meshfilter"]],
        "smooth_spatial": typing.NotRequired[float],
        "smooth_influence": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MeshfilterConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "filter": str,
        "output": str,
    },
)

@dataclasses.dataclass
class MeshfilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output mesh file"""

def meshfilter_params(
    input_: InputPathType,
    filter: str,
    output: str,
    smooth_spatial: float | None = None,
    smooth_influence: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MeshfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Meshfilter:
    """
    Build parameters.

    Args:
        input_: the input mesh file
        filter: the filter to apply.Options are: smooth
        output: the output mesh file
        smooth_spatial: spatial extent of smoothing (default: 10mm)
        smooth_influence: influence factor for smoothing (default: 10)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Meshfilter = {
        "@type": "mrtrix/meshfilter",
        "input": input_,
        "filter": filter,
        "output": output,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if smooth_spatial is not None:
        params["smooth_spatial"] = smooth_spatial
    if smooth_influence is not None:
        params["smooth_influence"] = smooth_influence
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def meshfilter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Meshfilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("smooth_spatial", None) is not None:
        if not isinstance(params["smooth_spatial"], (float, int)):
            raise StyxValidationError(f'`smooth_spatial` has the wrong type: Received `{type(params["smooth_spatial"])}` expected `float`')
    if params.get("smooth_influence", None) is not None:
        if not isinstance(params["smooth_influence"], (float, int)):
            raise StyxValidationError(f'`smooth_influence` has the wrong type: Received `{type(params["smooth_influence"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MeshfilterConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("filter", None) is None:
        raise StyxValidationError("`filter` must not be None")
    if not isinstance(params["filter"], str):
        raise StyxValidationError(f'`filter` has the wrong type: Received `{type(params["filter"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def meshfilter_cargs(params: Meshfilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("meshfilter")
    v_0 = params.get("smooth_spatial")
    if v_0 is not None:
        cargs.append("-smooth_spatial")
        cargs.append(str(v_0))
    v_1 = params.get("smooth_influence")
    if v_1 is not None:
        cargs.append("-smooth_influence")
        cargs.append(str(v_1))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("nthreads")
    if v_2 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_2))
    v_3 = params.get("config")
    if v_3 is not None:
        for item0 in v_3:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["filter"])
    cargs.append(params["output"])
    return cargs

def meshfilter_outputs(params: Meshfilter, execution: Execution) -> MeshfilterOutputs:
    """Build the MeshfilterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MeshfilterOutputs(
        root=root_v,
        output=output_v,
    )

def meshfilter_execute(params: Meshfilter, runner: Runner | None = None) -> MeshfilterOutputs:
    """
    meshfilter

    Apply filter operations to meshes.

While this command has only one filter operation currently available, it nevertheless presents with a comparable interface to the MRtrix3 commands maskfilter and mrfilter commands.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    meshfilter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MESHFILTER_METADATA)
    execution.params(params)
    args = meshfilter_cargs(params, execution)
    out = meshfilter_outputs(params, execution)
    execution.run(args)
    return out

def meshfilter(
    input_: InputPathType,
    filter: str,
    output: str,
    smooth_spatial: float | None = None,
    smooth_influence: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MeshfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MeshfilterOutputs:
    """
    meshfilter

    Apply filter operations to meshes.

While this command has only one filter operation currently available, it nevertheless presents with a comparable interface to the MRtrix3 commands maskfilter and mrfilter commands.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input mesh file
        filter: the filter to apply.Options are: smooth
        output: the output mesh file
        smooth_spatial: spatial extent of smoothing (default: 10mm)
        smooth_influence: influence factor for smoothing (default: 10)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = meshfilter_params(
        input_=input_,
        filter=filter,
        output=output,
        smooth_spatial=smooth_spatial,
        smooth_influence=smooth_influence,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return meshfilter_execute(params, runner)

__all__ = [
    "Meshfilter",
    "MeshfilterOutputs",
    "MESHFILTER_METADATA",
    "meshfilter_cargs",
    "meshfilter_outputs",
    "meshfilter_params",
    "meshfilter_execute",
    "meshfilter_validate",
    "meshfilter",
]