# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKSTATS_METADATA = Metadata(
    id="tckstats",
    name="tckstats",
    package="mrtrix",
)

class TckstatsConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckstats = typing.TypedDict(
    "Tckstats",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckstats"]],
        "output": typing.NotRequired[list[str]],
        "histogram": typing.NotRequired[str],
        "dump": typing.NotRequired[str],
        "ignorezero": typing.NotRequired[bool],
        "tck_weights_in": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckstatsConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks_in": InputPathType,
    },
)

@dataclasses.dataclass
class TckstatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    histogram: OutputPathType | None
    """output a histogram of streamline lengths """
    dump: OutputPathType | None
    """dump the streamlines lengths to a text file """

def tckstats_params(
    tracks_in: InputPathType,
    output: list[str] | None = None,
    histogram: str | None = None,
    dump: str | None = None,
    ignorezero: bool = False,
    tck_weights_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckstatsConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckstats:
    """
    Build parameters.

    Args:
        tracks_in: the input track file
        output: output only the field specified. Multiple such options can be\
            supplied if required. Choices are: mean, median, std, min, max, count.\
            Useful for use in scripts.
        histogram: output a histogram of streamline lengths
        dump: dump the streamlines lengths to a text file
        ignorezero: do not generate a warning if the track file contains\
            streamlines with zero length
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckstats = {
        "@type": "mrtrix/tckstats",
        "tracks_in": tracks_in,
        "ignorezero": ignorezero,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if output is not None:
        params["output"] = output
    if histogram is not None:
        params["histogram"] = histogram
    if dump is not None:
        params["dump"] = dump
    if tck_weights_in is not None:
        params["tck_weights_in"] = tck_weights_in
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckstats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckstats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], list):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `list[str]`')
        for e in params["output"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`output` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("histogram", None) is not None:
        if not isinstance(params["histogram"], str):
            raise StyxValidationError(f'`histogram` has the wrong type: Received `{type(params["histogram"])}` expected `str`')
    if params.get("dump", None) is not None:
        if not isinstance(params["dump"], str):
            raise StyxValidationError(f'`dump` has the wrong type: Received `{type(params["dump"])}` expected `str`')
    if params.get("ignorezero", None) is not None:
        if not isinstance(params["ignorezero"], bool):
            raise StyxValidationError(f'`ignorezero` has the wrong type: Received `{type(params["ignorezero"])}` expected `bool`')
    if params.get("tck_weights_in", None) is not None:
        if not isinstance(params["tck_weights_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tck_weights_in` has the wrong type: Received `{type(params["tck_weights_in"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckstatsConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks_in", None) is None:
        raise StyxValidationError("`tracks_in` must not be None")
    if not isinstance(params["tracks_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks_in` has the wrong type: Received `{type(params["tracks_in"])}` expected `InputPathType`')

def tckstats_cargs(params: Tckstats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckstats")
    v_0 = params.get("output")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append("-output")
            cargs.append(item0)
    v_1 = params.get("histogram")
    if v_1 is not None:
        cargs.append("-histogram")
        cargs.append(v_1)
    v_2 = params.get("dump")
    if v_2 is not None:
        cargs.append("-dump")
        cargs.append(v_2)
    if params.get("ignorezero"):
        cargs.append("-ignorezero")
    v_3 = params.get("tck_weights_in")
    if v_3 is not None:
        cargs.append("-tck_weights_in")
        cargs.append(execution.input_file(v_3))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_4 = params.get("nthreads")
    if v_4 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_4))
    v_5 = params.get("config")
    if v_5 is not None:
        for item1 in v_5:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks_in"]))
    return cargs

def tckstats_outputs(params: Tckstats, execution: Execution) -> TckstatsOutputs:
    """Build the TckstatsOutputs object for this tool."""
    histogram_v: OutputPathType | None = None
    dump_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("histogram")
    if __v0 is not None:
        histogram_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("dump")
    if __v1 is not None:
        dump_v = execution.output_file((__v1 if __v1 is not None else ""))
    return TckstatsOutputs(
        root=root_v,
        histogram=histogram_v,
        dump=dump_v,
    )

def tckstats_execute(params: Tckstats, runner: Runner | None = None) -> TckstatsOutputs:
    """
    tckstats

    Calculate statistics on streamlines lengths.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckstats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKSTATS_METADATA)
    execution.params(params)
    args = tckstats_cargs(params, execution)
    out = tckstats_outputs(params, execution)
    execution.run(args)
    return out

def tckstats(
    tracks_in: InputPathType,
    output: list[str] | None = None,
    histogram: str | None = None,
    dump: str | None = None,
    ignorezero: bool = False,
    tck_weights_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckstatsConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckstatsOutputs:
    """
    tckstats

    Calculate statistics on streamlines lengths.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks_in: the input track file
        output: output only the field specified. Multiple such options can be\
            supplied if required. Choices are: mean, median, std, min, max, count.\
            Useful for use in scripts.
        histogram: output a histogram of streamline lengths
        dump: dump the streamlines lengths to a text file
        ignorezero: do not generate a warning if the track file contains\
            streamlines with zero length
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckstats_params(
        tracks_in=tracks_in,
        output=output,
        histogram=histogram,
        dump=dump,
        ignorezero=ignorezero,
        tck_weights_in=tck_weights_in,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckstats_execute(params, runner)

__all__ = [
    "Tckstats",
    "TckstatsOutputs",
    "TCKSTATS_METADATA",
    "tckstats_cargs",
    "tckstats_outputs",
    "tckstats_params",
    "tckstats_execute",
    "tckstats_validate",
    "tckstats",
]