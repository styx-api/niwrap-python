# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKSIFT_METADATA = Metadata(
    id="tcksift",
    name="tcksift",
    package="mrtrix",
)

class TcksiftConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tcksift = typing.TypedDict(
    "Tcksift",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tcksift"]],
        "nofilter": typing.NotRequired[bool],
        "output_at_counts": typing.NotRequired[list[int]],
        "proc_mask": typing.NotRequired[InputPathType],
        "act": typing.NotRequired[InputPathType],
        "fd_scale_gm": typing.NotRequired[bool],
        "no_dilate_lut": typing.NotRequired[bool],
        "make_null_lobes": typing.NotRequired[bool],
        "remove_untracked": typing.NotRequired[bool],
        "fd_thresh": typing.NotRequired[float],
        "csv": typing.NotRequired[str],
        "out_mu": typing.NotRequired[str],
        "output_debug": typing.NotRequired[bool],
        "out_selection": typing.NotRequired[str],
        "term_number": typing.NotRequired[int],
        "term_ratio": typing.NotRequired[float],
        "term_mu": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TcksiftConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in_tracks": InputPathType,
        "in_fod": InputPathType,
        "out_tracks": str,
    },
)

@dataclasses.dataclass
class TcksiftOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_tracks: OutputPathType
    """the output filtered tracks file"""
    csv: OutputPathType | None
    """output statistics of execution per iteration to a .csv file """
    out_mu: OutputPathType | None
    """output the final value of SIFT proportionality coefficient mu to a text file """
    out_selection: OutputPathType | None
    """output a text file containing the binary selection of streamlines """

def tcksift_params(
    in_tracks: InputPathType,
    in_fod: InputPathType,
    out_tracks: str,
    nofilter: bool = False,
    output_at_counts: list[int] | None = None,
    proc_mask: InputPathType | None = None,
    act: InputPathType | None = None,
    fd_scale_gm: bool = False,
    no_dilate_lut: bool = False,
    make_null_lobes: bool = False,
    remove_untracked: bool = False,
    fd_thresh: float | None = None,
    csv: str | None = None,
    out_mu: str | None = None,
    output_debug: bool = False,
    out_selection: str | None = None,
    term_number: int | None = None,
    term_ratio: float | None = None,
    term_mu: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TcksiftConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tcksift:
    """
    Build parameters.

    Args:
        in_tracks: the input track file
        in_fod: input image containing the spherical harmonics of the fibre\
            orientation distributions
        out_tracks: the output filtered tracks file
        nofilter: do NOT perform track filtering - just construct the model in\
            order to provide output debugging images
        output_at_counts: output filtered track files (and optionally debugging\
            images if -output_debug is specified) at specific numbers of remaining\
            streamlines; provide as comma-separated list of integers
        proc_mask: provide an image containing the processing mask weights for the\
            model; image spatial dimensions must match the fixel image
        act: use an ACT five-tissue-type segmented anatomical image to derive the\
            processing mask
        fd_scale_gm: provide this option (in conjunction with -act) to\
            heuristically downsize the fibre density estimates based on the\
            presence of GM in the voxel. This can assist in reducing tissue\
            interface effects when using a single-tissue deconvolution algorithm
        no_dilate_lut: do NOT dilate FOD lobe lookup tables; only map streamlines\
            to FOD lobes if the precise tangent lies within the angular spread of\
            that lobe
        make_null_lobes: add an additional FOD lobe to each voxel, with zero\
            integral, that covers all directions with zero / negative FOD\
            amplitudes
        remove_untracked: remove FOD lobes that do not have any streamline density\
            attributed to them; this improves filtering slightly, at the expense of\
            longer computation time (and you can no longer do quantitative\
            comparisons between reconstructions if this is enabled)
        fd_thresh: fibre density threshold; exclude an FOD lobe from filtering\
            processing if its integral is less than this amount (streamlines will\
            still be mapped to it, but it will not contribute to the cost function\
            or the filtering)
        csv: output statistics of execution per iteration to a .csv file
        out_mu: output the final value of SIFT proportionality coefficient mu to a\
            text file
        output_debug: provide various output images for assessing & debugging\
            performance etc.
        out_selection: output a text file containing the binary selection of\
            streamlines
        term_number: number of streamlines - continue filtering until this number\
            of streamlines remain
        term_ratio: termination ratio - defined as the ratio between reduction in\
            cost function, and reduction in density of streamlines. Smaller values\
            result in more streamlines being filtered out.
        term_mu: terminate filtering once the SIFT proportionality coefficient\
            reaches a given value
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tcksift = {
        "@type": "mrtrix/tcksift",
        "in_tracks": in_tracks,
        "in_fod": in_fod,
        "out_tracks": out_tracks,
        "nofilter": nofilter,
        "fd_scale_gm": fd_scale_gm,
        "no_dilate_lut": no_dilate_lut,
        "make_null_lobes": make_null_lobes,
        "remove_untracked": remove_untracked,
        "output_debug": output_debug,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if output_at_counts is not None:
        params["output_at_counts"] = output_at_counts
    if proc_mask is not None:
        params["proc_mask"] = proc_mask
    if act is not None:
        params["act"] = act
    if fd_thresh is not None:
        params["fd_thresh"] = fd_thresh
    if csv is not None:
        params["csv"] = csv
    if out_mu is not None:
        params["out_mu"] = out_mu
    if out_selection is not None:
        params["out_selection"] = out_selection
    if term_number is not None:
        params["term_number"] = term_number
    if term_ratio is not None:
        params["term_ratio"] = term_ratio
    if term_mu is not None:
        params["term_mu"] = term_mu
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tcksift_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tcksift."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("nofilter", None) is not None:
        if not isinstance(params["nofilter"], bool):
            raise StyxValidationError(f'`nofilter` has the wrong type: Received `{type(params["nofilter"])}` expected `bool`')
    if params.get("output_at_counts", None) is not None:
        if not isinstance(params["output_at_counts"], list):
            raise StyxValidationError(f'`output_at_counts` has the wrong type: Received `{type(params["output_at_counts"])}` expected `list[int]`')
        for e in params["output_at_counts"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`output_at_counts` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("proc_mask", None) is not None:
        if not isinstance(params["proc_mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`proc_mask` has the wrong type: Received `{type(params["proc_mask"])}` expected `InputPathType`')
    if params.get("act", None) is not None:
        if not isinstance(params["act"], (pathlib.Path, str)):
            raise StyxValidationError(f'`act` has the wrong type: Received `{type(params["act"])}` expected `InputPathType`')
    if params.get("fd_scale_gm", None) is not None:
        if not isinstance(params["fd_scale_gm"], bool):
            raise StyxValidationError(f'`fd_scale_gm` has the wrong type: Received `{type(params["fd_scale_gm"])}` expected `bool`')
    if params.get("no_dilate_lut", None) is not None:
        if not isinstance(params["no_dilate_lut"], bool):
            raise StyxValidationError(f'`no_dilate_lut` has the wrong type: Received `{type(params["no_dilate_lut"])}` expected `bool`')
    if params.get("make_null_lobes", None) is not None:
        if not isinstance(params["make_null_lobes"], bool):
            raise StyxValidationError(f'`make_null_lobes` has the wrong type: Received `{type(params["make_null_lobes"])}` expected `bool`')
    if params.get("remove_untracked", None) is not None:
        if not isinstance(params["remove_untracked"], bool):
            raise StyxValidationError(f'`remove_untracked` has the wrong type: Received `{type(params["remove_untracked"])}` expected `bool`')
    if params.get("fd_thresh", None) is not None:
        if not isinstance(params["fd_thresh"], (float, int)):
            raise StyxValidationError(f'`fd_thresh` has the wrong type: Received `{type(params["fd_thresh"])}` expected `float`')
    if params.get("csv", None) is not None:
        if not isinstance(params["csv"], str):
            raise StyxValidationError(f'`csv` has the wrong type: Received `{type(params["csv"])}` expected `str`')
    if params.get("out_mu", None) is not None:
        if not isinstance(params["out_mu"], str):
            raise StyxValidationError(f'`out_mu` has the wrong type: Received `{type(params["out_mu"])}` expected `str`')
    if params.get("output_debug", None) is not None:
        if not isinstance(params["output_debug"], bool):
            raise StyxValidationError(f'`output_debug` has the wrong type: Received `{type(params["output_debug"])}` expected `bool`')
    if params.get("out_selection", None) is not None:
        if not isinstance(params["out_selection"], str):
            raise StyxValidationError(f'`out_selection` has the wrong type: Received `{type(params["out_selection"])}` expected `str`')
    if params.get("term_number", None) is not None:
        if not isinstance(params["term_number"], int):
            raise StyxValidationError(f'`term_number` has the wrong type: Received `{type(params["term_number"])}` expected `int`')
    if params.get("term_ratio", None) is not None:
        if not isinstance(params["term_ratio"], (float, int)):
            raise StyxValidationError(f'`term_ratio` has the wrong type: Received `{type(params["term_ratio"])}` expected `float`')
    if params.get("term_mu", None) is not None:
        if not isinstance(params["term_mu"], (float, int)):
            raise StyxValidationError(f'`term_mu` has the wrong type: Received `{type(params["term_mu"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TcksiftConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in_tracks", None) is None:
        raise StyxValidationError("`in_tracks` must not be None")
    if not isinstance(params["in_tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_tracks` has the wrong type: Received `{type(params["in_tracks"])}` expected `InputPathType`')
    if params.get("in_fod", None) is None:
        raise StyxValidationError("`in_fod` must not be None")
    if not isinstance(params["in_fod"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_fod` has the wrong type: Received `{type(params["in_fod"])}` expected `InputPathType`')
    if params.get("out_tracks", None) is None:
        raise StyxValidationError("`out_tracks` must not be None")
    if not isinstance(params["out_tracks"], str):
        raise StyxValidationError(f'`out_tracks` has the wrong type: Received `{type(params["out_tracks"])}` expected `str`')

def tcksift_cargs(params: Tcksift, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tcksift")
    if params.get("nofilter"):
        cargs.append("-nofilter")
    v_0 = params.get("output_at_counts")
    if v_0 is not None:
        cargs.append("-output_at_counts")
        cargs.append(",".join([str(item0) for item0 in v_0]))
    v_1 = params.get("proc_mask")
    if v_1 is not None:
        cargs.append("-proc_mask")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("act")
    if v_2 is not None:
        cargs.append("-act")
        cargs.append(execution.input_file(v_2))
    if params.get("fd_scale_gm"):
        cargs.append("-fd_scale_gm")
    if params.get("no_dilate_lut"):
        cargs.append("-no_dilate_lut")
    if params.get("make_null_lobes"):
        cargs.append("-make_null_lobes")
    if params.get("remove_untracked"):
        cargs.append("-remove_untracked")
    v_3 = params.get("fd_thresh")
    if v_3 is not None:
        cargs.append("-fd_thresh")
        cargs.append(str(v_3))
    v_4 = params.get("csv")
    if v_4 is not None:
        cargs.append("-csv")
        cargs.append(v_4)
    v_5 = params.get("out_mu")
    if v_5 is not None:
        cargs.append("-out_mu")
        cargs.append(v_5)
    if params.get("output_debug"):
        cargs.append("-output_debug")
    v_6 = params.get("out_selection")
    if v_6 is not None:
        cargs.append("-out_selection")
        cargs.append(v_6)
    v_7 = params.get("term_number")
    if v_7 is not None:
        cargs.append("-term_number")
        cargs.append(str(v_7))
    v_8 = params.get("term_ratio")
    if v_8 is not None:
        cargs.append("-term_ratio")
        cargs.append(str(v_8))
    v_9 = params.get("term_mu")
    if v_9 is not None:
        cargs.append("-term_mu")
        cargs.append(str(v_9))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_10 = params.get("nthreads")
    if v_10 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_10))
    v_11 = params.get("config")
    if v_11 is not None:
        for item1 in v_11:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in_tracks"]))
    cargs.append(execution.input_file(params["in_fod"]))
    cargs.append(params["out_tracks"])
    return cargs

def tcksift_outputs(params: Tcksift, execution: Execution) -> TcksiftOutputs:
    """Build the TcksiftOutputs object for this tool."""
    csv_v: OutputPathType | None = None
    out_mu_v: OutputPathType | None = None
    out_selection_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_tracks_v: OutputPathType = execution.output_file(params["out_tracks"])
    __v0 = params.get("csv")
    if __v0 is not None:
        csv_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("out_mu")
    if __v1 is not None:
        out_mu_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("out_selection")
    if __v2 is not None:
        out_selection_v = execution.output_file((__v2 if __v2 is not None else ""))
    return TcksiftOutputs(
        root=root_v,
        out_tracks=out_tracks_v,
        csv=csv_v,
        out_mu=out_mu_v,
        out_selection=out_selection_v,
    )

def tcksift_execute(params: Tcksift, runner: Runner | None = None) -> TcksiftOutputs:
    """
    tcksift

    Filter a whole-brain fibre-tracking data set such that the streamline densities match the FOD lobe integrals.



References:

Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tcksift_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKSIFT_METADATA)
    execution.params(params)
    args = tcksift_cargs(params, execution)
    out = tcksift_outputs(params, execution)
    execution.run(args)
    return out

def tcksift(
    in_tracks: InputPathType,
    in_fod: InputPathType,
    out_tracks: str,
    nofilter: bool = False,
    output_at_counts: list[int] | None = None,
    proc_mask: InputPathType | None = None,
    act: InputPathType | None = None,
    fd_scale_gm: bool = False,
    no_dilate_lut: bool = False,
    make_null_lobes: bool = False,
    remove_untracked: bool = False,
    fd_thresh: float | None = None,
    csv: str | None = None,
    out_mu: str | None = None,
    output_debug: bool = False,
    out_selection: str | None = None,
    term_number: int | None = None,
    term_ratio: float | None = None,
    term_mu: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TcksiftConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TcksiftOutputs:
    """
    tcksift

    Filter a whole-brain fibre-tracking data set such that the streamline densities match the FOD lobe integrals.



References:

Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_tracks: the input track file
        in_fod: input image containing the spherical harmonics of the fibre\
            orientation distributions
        out_tracks: the output filtered tracks file
        nofilter: do NOT perform track filtering - just construct the model in\
            order to provide output debugging images
        output_at_counts: output filtered track files (and optionally debugging\
            images if -output_debug is specified) at specific numbers of remaining\
            streamlines; provide as comma-separated list of integers
        proc_mask: provide an image containing the processing mask weights for the\
            model; image spatial dimensions must match the fixel image
        act: use an ACT five-tissue-type segmented anatomical image to derive the\
            processing mask
        fd_scale_gm: provide this option (in conjunction with -act) to\
            heuristically downsize the fibre density estimates based on the\
            presence of GM in the voxel. This can assist in reducing tissue\
            interface effects when using a single-tissue deconvolution algorithm
        no_dilate_lut: do NOT dilate FOD lobe lookup tables; only map streamlines\
            to FOD lobes if the precise tangent lies within the angular spread of\
            that lobe
        make_null_lobes: add an additional FOD lobe to each voxel, with zero\
            integral, that covers all directions with zero / negative FOD\
            amplitudes
        remove_untracked: remove FOD lobes that do not have any streamline density\
            attributed to them; this improves filtering slightly, at the expense of\
            longer computation time (and you can no longer do quantitative\
            comparisons between reconstructions if this is enabled)
        fd_thresh: fibre density threshold; exclude an FOD lobe from filtering\
            processing if its integral is less than this amount (streamlines will\
            still be mapped to it, but it will not contribute to the cost function\
            or the filtering)
        csv: output statistics of execution per iteration to a .csv file
        out_mu: output the final value of SIFT proportionality coefficient mu to a\
            text file
        output_debug: provide various output images for assessing & debugging\
            performance etc.
        out_selection: output a text file containing the binary selection of\
            streamlines
        term_number: number of streamlines - continue filtering until this number\
            of streamlines remain
        term_ratio: termination ratio - defined as the ratio between reduction in\
            cost function, and reduction in density of streamlines. Smaller values\
            result in more streamlines being filtered out.
        term_mu: terminate filtering once the SIFT proportionality coefficient\
            reaches a given value
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tcksift_params(
        in_tracks=in_tracks,
        in_fod=in_fod,
        out_tracks=out_tracks,
        nofilter=nofilter,
        output_at_counts=output_at_counts,
        proc_mask=proc_mask,
        act=act,
        fd_scale_gm=fd_scale_gm,
        no_dilate_lut=no_dilate_lut,
        make_null_lobes=make_null_lobes,
        remove_untracked=remove_untracked,
        fd_thresh=fd_thresh,
        csv=csv,
        out_mu=out_mu,
        output_debug=output_debug,
        out_selection=out_selection,
        term_number=term_number,
        term_ratio=term_ratio,
        term_mu=term_mu,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tcksift_execute(params, runner)

__all__ = [
    "Tcksift",
    "TcksiftOutputs",
    "TCKSIFT_METADATA",
    "tcksift_cargs",
    "tcksift_outputs",
    "tcksift_params",
    "tcksift_execute",
    "tcksift_validate",
    "tcksift",
]