# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FOD2DEC_METADATA = Metadata(
    id="fod2dec",
    name="fod2dec",
    package="mrtrix",
)

class Fod2decConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fod2dec = typing.TypedDict(
    "Fod2dec",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fod2dec"]],
        "mask": typing.NotRequired[InputPathType],
        "contrast": typing.NotRequired[InputPathType],
        "lum": typing.NotRequired[bool],
        "lum_coefs": typing.NotRequired[list[float]],
        "lum_gamma": typing.NotRequired[float],
        "threshold": typing.NotRequired[float],
        "no_weight": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fod2decConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class Fod2decOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """The output DEC image (weighted RGB triplets)."""

def fod2dec_params(
    input_: InputPathType,
    output: str,
    mask: InputPathType | None = None,
    contrast: InputPathType | None = None,
    lum: bool = False,
    lum_coefs: list[float] | None = None,
    lum_gamma: float | None = None,
    threshold: float | None = None,
    no_weight: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fod2decConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fod2dec:
    """
    Build parameters.

    Args:
        input_: The input FOD image (spherical harmonic coefficients).
        output: The output DEC image (weighted RGB triplets).
        mask: Only perform DEC computation within the specified mask image.
        contrast: Weight the computed DEC map by the provided image contrast. If\
            the contrast has a different image grid, the DEC map is first resliced\
            and renormalised. To achieve panchromatic sharpening, provide an image\
            with a higher spatial resolution than the input FOD image; e.g., a T1\
            anatomical volume. Only the DEC is subject to the mask, so as to allow\
            for partial colouring of the contrast image. Default when this option\
            is *not* provided: integral of input FOD, subject to the same\
            mask/threshold as used for DEC computation.
        lum: Correct for luminance/perception, using default values Cr,Cg,Cb =\
            0.3,0.5,0.2 and gamma = 2.2 (*not* correcting is the theoretical\
            equivalent of Cr,Cg,Cb = 1,1,1 and gamma = 2).
        lum_coefs: The coefficients Cr,Cg,Cb to correct for luminance/perception.\
            Note: this implicitly switches on luminance/perception correction,\
            using a default gamma = 2.2 unless specified otherwise.
        lum_gamma: The gamma value to correct for luminance/perception. Note: this\
            implicitly switches on luminance/perception correction, using a default\
            Cr,Cg,Cb = 0.3,0.5,0.2 unless specified otherwise.
        threshold: FOD amplitudes below the threshold value are considered zero.
        no_weight: Do not weight the DEC map; just output the unweighted colours.\
            Reslicing and renormalising of colours will still happen when providing\
            the -contrast option as a template.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fod2dec = {
        "@type": "mrtrix/fod2dec",
        "input": input_,
        "output": output,
        "lum": lum,
        "no_weight": no_weight,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask is not None:
        params["mask"] = mask
    if contrast is not None:
        params["contrast"] = contrast
    if lum_coefs is not None:
        params["lum_coefs"] = lum_coefs
    if lum_gamma is not None:
        params["lum_gamma"] = lum_gamma
    if threshold is not None:
        params["threshold"] = threshold
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fod2dec_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fod2dec."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("contrast", None) is not None:
        if not isinstance(params["contrast"], (pathlib.Path, str)):
            raise StyxValidationError(f'`contrast` has the wrong type: Received `{type(params["contrast"])}` expected `InputPathType`')
    if params.get("lum", None) is not None:
        if not isinstance(params["lum"], bool):
            raise StyxValidationError(f'`lum` has the wrong type: Received `{type(params["lum"])}` expected `bool`')
    if params.get("lum_coefs", None) is not None:
        if not isinstance(params["lum_coefs"], list):
            raise StyxValidationError(f'`lum_coefs` has the wrong type: Received `{type(params["lum_coefs"])}` expected `list[float]`')
        for e in params["lum_coefs"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`lum_coefs` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("lum_gamma", None) is not None:
        if not isinstance(params["lum_gamma"], (float, int)):
            raise StyxValidationError(f'`lum_gamma` has the wrong type: Received `{type(params["lum_gamma"])}` expected `float`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("no_weight", None) is not None:
        if not isinstance(params["no_weight"], bool):
            raise StyxValidationError(f'`no_weight` has the wrong type: Received `{type(params["no_weight"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fod2decConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def fod2dec_cargs(params: Fod2dec, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fod2dec")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("contrast")
    if v_1 is not None:
        cargs.append("-contrast")
        cargs.append(execution.input_file(v_1))
    if params.get("lum"):
        cargs.append("-lum")
    v_2 = params.get("lum_coefs")
    if v_2 is not None:
        cargs.append("-lum_coefs")
        for item0 in v_2:
            cargs.append(str(item0))
    v_3 = params.get("lum_gamma")
    if v_3 is not None:
        cargs.append("-lum_gamma")
        cargs.append(str(v_3))
    v_4 = params.get("threshold")
    if v_4 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_4))
    if params.get("no_weight"):
        cargs.append("-no_weight")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item1 in v_6:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["output"])
    return cargs

def fod2dec_outputs(params: Fod2dec, execution: Execution) -> Fod2decOutputs:
    """Build the Fod2decOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return Fod2decOutputs(
        root=root_v,
        output=output_v,
    )

def fod2dec_execute(params: Fod2dec, runner: Runner | None = None) -> Fod2decOutputs:
    """
    fod2dec

    Generate FOD-based DEC maps, with optional panchromatic sharpening and/or luminance/perception correction.

By default, the FOD-based DEC is weighted by the integral of the FOD. To weight by another scalar map, use the -contrast option. This option can also be used for panchromatic sharpening, e.g., by supplying a T1 (or other sensible) anatomical volume with a higher spatial resolution.

References:

Dhollander T, Smith RE, Tournier JD, Jeurissen B, Connelly A. Time to move on: an FOD-based DEC map to replace DTI's trademark DEC FA. Proc Intl Soc Mag Reson Med, 2015, 23, 1027

Dhollander T, Raffelt D, Smith RE, Connelly A. Panchromatic sharpening of FOD-based DEC maps by structural T1 information. Proc Intl Soc Mag Reson Med, 2015, 23, 566

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fod2dec_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FOD2DEC_METADATA)
    execution.params(params)
    args = fod2dec_cargs(params, execution)
    out = fod2dec_outputs(params, execution)
    execution.run(args)
    return out

def fod2dec(
    input_: InputPathType,
    output: str,
    mask: InputPathType | None = None,
    contrast: InputPathType | None = None,
    lum: bool = False,
    lum_coefs: list[float] | None = None,
    lum_gamma: float | None = None,
    threshold: float | None = None,
    no_weight: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fod2decConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fod2decOutputs:
    """
    fod2dec

    Generate FOD-based DEC maps, with optional panchromatic sharpening and/or luminance/perception correction.

By default, the FOD-based DEC is weighted by the integral of the FOD. To weight by another scalar map, use the -contrast option. This option can also be used for panchromatic sharpening, e.g., by supplying a T1 (or other sensible) anatomical volume with a higher spatial resolution.

References:

Dhollander T, Smith RE, Tournier JD, Jeurissen B, Connelly A. Time to move on: an FOD-based DEC map to replace DTI's trademark DEC FA. Proc Intl Soc Mag Reson Med, 2015, 23, 1027

Dhollander T, Raffelt D, Smith RE, Connelly A. Panchromatic sharpening of FOD-based DEC maps by structural T1 information. Proc Intl Soc Mag Reson Med, 2015, 23, 566

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: The input FOD image (spherical harmonic coefficients).
        output: The output DEC image (weighted RGB triplets).
        mask: Only perform DEC computation within the specified mask image.
        contrast: Weight the computed DEC map by the provided image contrast. If\
            the contrast has a different image grid, the DEC map is first resliced\
            and renormalised. To achieve panchromatic sharpening, provide an image\
            with a higher spatial resolution than the input FOD image; e.g., a T1\
            anatomical volume. Only the DEC is subject to the mask, so as to allow\
            for partial colouring of the contrast image. Default when this option\
            is *not* provided: integral of input FOD, subject to the same\
            mask/threshold as used for DEC computation.
        lum: Correct for luminance/perception, using default values Cr,Cg,Cb =\
            0.3,0.5,0.2 and gamma = 2.2 (*not* correcting is the theoretical\
            equivalent of Cr,Cg,Cb = 1,1,1 and gamma = 2).
        lum_coefs: The coefficients Cr,Cg,Cb to correct for luminance/perception.\
            Note: this implicitly switches on luminance/perception correction,\
            using a default gamma = 2.2 unless specified otherwise.
        lum_gamma: The gamma value to correct for luminance/perception. Note: this\
            implicitly switches on luminance/perception correction, using a default\
            Cr,Cg,Cb = 0.3,0.5,0.2 unless specified otherwise.
        threshold: FOD amplitudes below the threshold value are considered zero.
        no_weight: Do not weight the DEC map; just output the unweighted colours.\
            Reslicing and renormalising of colours will still happen when providing\
            the -contrast option as a template.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fod2dec_params(
        input_=input_,
        output=output,
        mask=mask,
        contrast=contrast,
        lum=lum,
        lum_coefs=lum_coefs,
        lum_gamma=lum_gamma,
        threshold=threshold,
        no_weight=no_weight,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fod2dec_execute(params, runner)

__all__ = [
    "Fod2dec",
    "Fod2decOutputs",
    "FOD2DEC_METADATA",
    "fod2dec_cargs",
    "fod2dec_outputs",
    "fod2dec_params",
    "fod2dec_execute",
    "fod2dec_validate",
    "fod2dec",
]