# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXELCONNECTIVITY_METADATA = Metadata(
    id="fixelconnectivity",
    name="fixelconnectivity",
    package="mrtrix",
)

class FixelconnectivityConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixelconnectivity = typing.TypedDict(
    "Fixelconnectivity",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixelconnectivity"]],
        "threshold": typing.NotRequired[float],
        "angle": typing.NotRequired[float],
        "mask": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[FixelconnectivityConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fixel_directory": InputPathType,
        "tracks": InputPathType,
        "matrix": str,
    },
)

@dataclasses.dataclass
class FixelconnectivityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    matrix: OutputPathType
    """the output fixel-fixel connectivity matrix directory path"""

def fixelconnectivity_params(
    fixel_directory: InputPathType,
    tracks: InputPathType,
    matrix: str,
    threshold: float | None = None,
    angle: float | None = None,
    mask: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelconnectivityConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixelconnectivity:
    """
    Build parameters.

    Args:
        fixel_directory: the directory containing the fixels between which\
            connectivity will be quantified
        tracks: the tracks used to determine fixel-fixel connectivity
        matrix: the output fixel-fixel connectivity matrix directory path
        threshold: a threshold to define the required fraction of shared\
            connections to be included in the neighbourhood (default: 0.01)
        angle: the max angle threshold for assigning streamline tangents to fixels\
            (Default: 45 degrees)
        mask: provide a fixel data file containing a mask of those fixels to be\
            computed; fixels outside the mask will be empty in the output matrix
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixelconnectivity = {
        "@type": "mrtrix/fixelconnectivity",
        "fixel_directory": fixel_directory,
        "tracks": tracks,
        "matrix": matrix,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if threshold is not None:
        params["threshold"] = threshold
    if angle is not None:
        params["angle"] = angle
    if mask is not None:
        params["mask"] = mask
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixelconnectivity_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixelconnectivity."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("angle", None) is not None:
        if not isinstance(params["angle"], (float, int)):
            raise StyxValidationError(f'`angle` has the wrong type: Received `{type(params["angle"])}` expected `float`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[FixelconnectivityConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fixel_directory", None) is None:
        raise StyxValidationError("`fixel_directory` must not be None")
    if not isinstance(params["fixel_directory"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_directory` has the wrong type: Received `{type(params["fixel_directory"])}` expected `InputPathType`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("matrix", None) is None:
        raise StyxValidationError("`matrix` must not be None")
    if not isinstance(params["matrix"], str):
        raise StyxValidationError(f'`matrix` has the wrong type: Received `{type(params["matrix"])}` expected `str`')

def fixelconnectivity_cargs(params: Fixelconnectivity, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixelconnectivity")
    v_0 = params.get("threshold")
    if v_0 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_0))
    v_1 = params.get("angle")
    if v_1 is not None:
        cargs.append("-angle")
        cargs.append(str(v_1))
    v_2 = params.get("mask")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["fixel_directory"]))
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(params["matrix"])
    return cargs

def fixelconnectivity_outputs(params: Fixelconnectivity, execution: Execution) -> FixelconnectivityOutputs:
    """Build the FixelconnectivityOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    matrix_v: OutputPathType = execution.output_file(params["matrix"])
    return FixelconnectivityOutputs(
        root=root_v,
        matrix=matrix_v,
    )

def fixelconnectivity_execute(params: Fixelconnectivity, runner: Runner | None = None) -> FixelconnectivityOutputs:
    """
    fixelconnectivity

    Generate a fixel-fixel connectivity matrix.

This command will generate a directory containing three images, which encodes the fixel-fixel connectivity matrix. Documentation regarding this format and how to use it will come in the future.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixelconnectivity_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXELCONNECTIVITY_METADATA)
    execution.params(params)
    args = fixelconnectivity_cargs(params, execution)
    out = fixelconnectivity_outputs(params, execution)
    execution.run(args)
    return out

def fixelconnectivity(
    fixel_directory: InputPathType,
    tracks: InputPathType,
    matrix: str,
    threshold: float | None = None,
    angle: float | None = None,
    mask: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelconnectivityConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> FixelconnectivityOutputs:
    """
    fixelconnectivity

    Generate a fixel-fixel connectivity matrix.

This command will generate a directory containing three images, which encodes the fixel-fixel connectivity matrix. Documentation regarding this format and how to use it will come in the future.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fixel_directory: the directory containing the fixels between which\
            connectivity will be quantified
        tracks: the tracks used to determine fixel-fixel connectivity
        matrix: the output fixel-fixel connectivity matrix directory path
        threshold: a threshold to define the required fraction of shared\
            connections to be included in the neighbourhood (default: 0.01)
        angle: the max angle threshold for assigning streamline tangents to fixels\
            (Default: 45 degrees)
        mask: provide a fixel data file containing a mask of those fixels to be\
            computed; fixels outside the mask will be empty in the output matrix
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixelconnectivity_params(
        fixel_directory=fixel_directory,
        tracks=tracks,
        matrix=matrix,
        threshold=threshold,
        angle=angle,
        mask=mask,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixelconnectivity_execute(params, runner)

__all__ = [
    "Fixelconnectivity",
    "FixelconnectivityOutputs",
    "FIXELCONNECTIVITY_METADATA",
    "fixelconnectivity_cargs",
    "fixelconnectivity_outputs",
    "fixelconnectivity_params",
    "fixelconnectivity_execute",
    "fixelconnectivity_validate",
    "fixelconnectivity",
]