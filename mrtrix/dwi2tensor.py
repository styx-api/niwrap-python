# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DWI2TENSOR_METADATA = Metadata(
    id="dwi2tensor",
    name="dwi2tensor",
    package="mrtrix",
)

class Dwi2tensorFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class Dwi2tensorConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dwi2tensor = typing.TypedDict(
    "Dwi2tensor",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dwi2tensor"]],
        "ols": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
        "b0": typing.NotRequired[str],
        "dkt": typing.NotRequired[str],
        "iter": typing.NotRequired[int],
        "predicted_signal": typing.NotRequired[str],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[Dwi2tensorFslgrad],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Dwi2tensorConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "dwi": InputPathType,
        "dt": str,
    },
)

@dataclasses.dataclass
class Dwi2tensorOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    dt: OutputPathType
    """the output dt image."""
    b0: OutputPathType | None
    """the output b0 image. """
    dkt: OutputPathType | None
    """the output dkt image. """
    predicted_signal: OutputPathType | None
    """the predicted dwi image. """

def dwi2tensor_params(
    dwi: InputPathType,
    dt: str,
    ols: bool = False,
    mask: InputPathType | None = None,
    b0: str | None = None,
    dkt: str | None = None,
    iter: int | None = None,
    predicted_signal: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: Dwi2tensorFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Dwi2tensorConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dwi2tensor:
    """
    Build parameters.

    Args:
        dwi: the input dwi image.
        dt: the output dt image.
        ols: perform initial fit using an ordinary least-squares (OLS) fit (see\
            Description).
        mask: only perform computation within the specified binary brain mask\
            image.
        b0: the output b0 image.
        dkt: the output dkt image.
        iter: number of iterative reweightings for IWLS algorithm (default: 2) (see\
            Description).
        predicted_signal: the predicted dwi image.
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dwi2tensor = {
        "@type": "mrtrix/dwi2tensor",
        "dwi": dwi,
        "dt": dt,
        "ols": ols,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask is not None:
        params["mask"] = mask
    if b0 is not None:
        params["b0"] = b0
    if dkt is not None:
        params["dkt"] = dkt
    if iter is not None:
        params["iter"] = iter
    if predicted_signal is not None:
        params["predicted_signal"] = predicted_signal
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dwi2tensor_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dwi2tensor."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("ols", None) is not None:
        if not isinstance(params["ols"], bool):
            raise StyxValidationError(f'`ols` has the wrong type: Received `{type(params["ols"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("b0", None) is not None:
        if not isinstance(params["b0"], str):
            raise StyxValidationError(f'`b0` has the wrong type: Received `{type(params["b0"])}` expected `str`')
    if params.get("dkt", None) is not None:
        if not isinstance(params["dkt"], str):
            raise StyxValidationError(f'`dkt` has the wrong type: Received `{type(params["dkt"])}` expected `str`')
    if params.get("iter", None) is not None:
        if not isinstance(params["iter"], int):
            raise StyxValidationError(f'`iter` has the wrong type: Received `{type(params["iter"])}` expected `int`')
    if params.get("predicted_signal", None) is not None:
        if not isinstance(params["predicted_signal"], str):
            raise StyxValidationError(f'`predicted_signal` has the wrong type: Received `{type(params["predicted_signal"])}` expected `str`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Dwi2tensorConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("dwi", None) is None:
        raise StyxValidationError("`dwi` must not be None")
    if not isinstance(params["dwi"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(params["dwi"])}` expected `InputPathType`')
    if params.get("dt", None) is None:
        raise StyxValidationError("`dt` must not be None")
    if not isinstance(params["dt"], str):
        raise StyxValidationError(f'`dt` has the wrong type: Received `{type(params["dt"])}` expected `str`')

def dwi2tensor_cargs(params: Dwi2tensor, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dwi2tensor")
    if params.get("ols"):
        cargs.append("-ols")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("b0")
    if v_1 is not None:
        cargs.append("-b0")
        cargs.append(v_1)
    v_2 = params.get("dkt")
    if v_2 is not None:
        cargs.append("-dkt")
        cargs.append(v_2)
    v_3 = params.get("iter")
    if v_3 is not None:
        cargs.append("-iter")
        cargs.append(str(v_3))
    v_4 = params.get("predicted_signal")
    if v_4 is not None:
        cargs.append("-predicted_signal")
        cargs.append(v_4)
    v_5 = params.get("grad")
    if v_5 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("fslgrad")
    if v_6 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_6["bvecs"]))
        cargs.append(execution.input_file(v_6["bvals"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_7 = params.get("nthreads")
    if v_7 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_7))
    v_8 = params.get("config")
    if v_8 is not None:
        for item0 in v_8:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["dwi"]))
    cargs.append(params["dt"])
    return cargs

def dwi2tensor_outputs(params: Dwi2tensor, execution: Execution) -> Dwi2tensorOutputs:
    """Build the Dwi2tensorOutputs object for this tool."""
    b0_v: OutputPathType | None = None
    dkt_v: OutputPathType | None = None
    predicted_signal_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    dt_v: OutputPathType = execution.output_file(params["dt"])
    __v0 = params.get("b0")
    if __v0 is not None:
        b0_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("dkt")
    if __v1 is not None:
        dkt_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("predicted_signal")
    if __v2 is not None:
        predicted_signal_v = execution.output_file((__v2 if __v2 is not None else ""))
    return Dwi2tensorOutputs(
        root=root_v,
        dt=dt_v,
        b0=b0_v,
        dkt=dkt_v,
        predicted_signal=predicted_signal_v,
    )

def dwi2tensor_execute(params: Dwi2tensor, runner: Runner | None = None) -> Dwi2tensorOutputs:
    """
    dwi2tensor

    Diffusion (kurtosis) tensor estimation.

By default, the diffusion tensor (and optionally its kurtosis) is fitted to the log-signal in two steps: firstly, using weighted least-squares (WLS) with weights based on the empirical signal intensities; secondly, by further iterated weighted least-squares (IWLS) with weights determined by the signal predictions from the previous iteration (by default, 2 iterations will be performed). This behaviour can be altered in two ways:

* The -ols option will cause the first fitting step to be performed using ordinary least-squares (OLS); that is, all measurements contribute equally to the fit, instead of the default behaviour of weighting based on the empirical signal intensities.

* The -iter option controls the number of iterations of the IWLS prodedure. If this is set to zero, then the output model parameters will be those resulting from the first fitting step only: either WLS by default, or OLS if the -ols option is used in conjunction with -iter 0.

The tensor coefficients are stored in the output image as follows:
volumes 0-5: D11, D22, D33, D12, D13, D23

If diffusion kurtosis is estimated using the -dkt option, these are stored as follows:
volumes 0-2: W1111, W2222, W3333
volumes 3-8: W1112, W1113, W1222, W1333, W2223, W2333
volumes 9-11: W1122, W1133, W2233
volumes 12-14: W1123, W1223, W1233

References:

References based on fitting algorithm used:

* OLS, WLS:
Basser, P.J.; Mattiello, J.; LeBihan, D. Estimation of the effective self-diffusion tensor from the NMR spin echo. J Magn Reson B., 1994, 103, 247â€“254.

* IWLS:
Veraart, J.; Sijbers, J.; Sunaert, S.; Leemans, A. & Jeurissen, B. Weighted linear least squares estimation of diffusion MRI parameters: strengths, limitations, and pitfalls. NeuroImage, 2013, 81, 335-346

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dwi2tensor_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DWI2TENSOR_METADATA)
    execution.params(params)
    args = dwi2tensor_cargs(params, execution)
    out = dwi2tensor_outputs(params, execution)
    execution.run(args)
    return out

def dwi2tensor(
    dwi: InputPathType,
    dt: str,
    ols: bool = False,
    mask: InputPathType | None = None,
    b0: str | None = None,
    dkt: str | None = None,
    iter: int | None = None,
    predicted_signal: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: Dwi2tensorFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Dwi2tensorConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Dwi2tensorOutputs:
    """
    dwi2tensor

    Diffusion (kurtosis) tensor estimation.

By default, the diffusion tensor (and optionally its kurtosis) is fitted to the log-signal in two steps: firstly, using weighted least-squares (WLS) with weights based on the empirical signal intensities; secondly, by further iterated weighted least-squares (IWLS) with weights determined by the signal predictions from the previous iteration (by default, 2 iterations will be performed). This behaviour can be altered in two ways:

* The -ols option will cause the first fitting step to be performed using ordinary least-squares (OLS); that is, all measurements contribute equally to the fit, instead of the default behaviour of weighting based on the empirical signal intensities.

* The -iter option controls the number of iterations of the IWLS prodedure. If this is set to zero, then the output model parameters will be those resulting from the first fitting step only: either WLS by default, or OLS if the -ols option is used in conjunction with -iter 0.

The tensor coefficients are stored in the output image as follows:
volumes 0-5: D11, D22, D33, D12, D13, D23

If diffusion kurtosis is estimated using the -dkt option, these are stored as follows:
volumes 0-2: W1111, W2222, W3333
volumes 3-8: W1112, W1113, W1222, W1333, W2223, W2333
volumes 9-11: W1122, W1133, W2233
volumes 12-14: W1123, W1223, W1233

References:

References based on fitting algorithm used:

* OLS, WLS:
Basser, P.J.; Mattiello, J.; LeBihan, D. Estimation of the effective self-diffusion tensor from the NMR spin echo. J Magn Reson B., 1994, 103, 247â€“254.

* IWLS:
Veraart, J.; Sijbers, J.; Sunaert, S.; Leemans, A. & Jeurissen, B. Weighted linear least squares estimation of diffusion MRI parameters: strengths, limitations, and pitfalls. NeuroImage, 2013, 81, 335-346

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        dwi: the input dwi image.
        dt: the output dt image.
        ols: perform initial fit using an ordinary least-squares (OLS) fit (see\
            Description).
        mask: only perform computation within the specified binary brain mask\
            image.
        b0: the output b0 image.
        dkt: the output dkt image.
        iter: number of iterative reweightings for IWLS algorithm (default: 2) (see\
            Description).
        predicted_signal: the predicted dwi image.
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dwi2tensor_params(
        dwi=dwi,
        dt=dt,
        ols=ols,
        mask=mask,
        b0=b0,
        dkt=dkt,
        iter=iter,
        predicted_signal=predicted_signal,
        grad=grad,
        fslgrad=fslgrad,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dwi2tensor_execute(params, runner)

__all__ = [
    "Dwi2tensor",
    "Dwi2tensorOutputs",
    "DWI2TENSOR_METADATA",
    "dwi2tensor_cargs",
    "dwi2tensor_outputs",
    "dwi2tensor_params",
    "dwi2tensor_execute",
    "dwi2tensor_validate",
    "dwi2tensor",
]