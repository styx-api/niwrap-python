# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FOD2FIXEL_METADATA = Metadata(
    id="fod2fixel",
    name="fod2fixel",
    package="mrtrix",
)

class Fod2fixelConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fod2fixel = typing.TypedDict(
    "Fod2fixel",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fod2fixel"]],
        "afd": typing.NotRequired[str],
        "peak_amp": typing.NotRequired[str],
        "disp": typing.NotRequired[str],
        "fmls_integral": typing.NotRequired[float],
        "fmls_peak_value": typing.NotRequired[float],
        "fmls_no_thresholds": typing.NotRequired[bool],
        "fmls_lobe_merge_ratio": typing.NotRequired[float],
        "mask": typing.NotRequired[InputPathType],
        "maxnum": typing.NotRequired[int],
        "nii": typing.NotRequired[bool],
        "dirpeak": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fod2fixelConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fod": InputPathType,
        "fixel_directory": str,
    },
)

@dataclasses.dataclass
class Fod2fixelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fixel_directory: OutputPathType
    """the output fixel directory"""
    afd: OutputPathType | None
    """output the total Apparent Fibre Density per fixel (integral of FOD lobe) """
    peak_amp: OutputPathType | None
    """output the amplitude of the FOD at the maximal peak per fixel """
    disp: OutputPathType | None
    """output a measure of dispersion per fixel as the ratio between FOD lobe integral and maximal peak amplitude """

def fod2fixel_params(
    fod: InputPathType,
    fixel_directory: str,
    afd: str | None = None,
    peak_amp: str | None = None,
    disp: str | None = None,
    fmls_integral: float | None = None,
    fmls_peak_value: float | None = None,
    fmls_no_thresholds: bool = False,
    fmls_lobe_merge_ratio: float | None = None,
    mask: InputPathType | None = None,
    maxnum: int | None = None,
    nii: bool = False,
    dirpeak: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fod2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fod2fixel:
    """
    Build parameters.

    Args:
        fod: the input fod image.
        fixel_directory: the output fixel directory
        afd: output the total Apparent Fibre Density per fixel (integral of FOD\
            lobe)
        peak_amp: output the amplitude of the FOD at the maximal peak per fixel
        disp: output a measure of dispersion per fixel as the ratio between FOD\
            lobe integral and maximal peak amplitude
        fmls_integral: threshold absolute numerical integral of positive FOD lobes.\
            Any lobe for which the integral is smaller than this threshold will be\
            discarded. Default: 0.
        fmls_peak_value: threshold peak amplitude of positive FOD lobes. Any lobe\
            for which the maximal peak amplitude is smaller than this threshold\
            will be discarded. Default: 0.1.
        fmls_no_thresholds: disable all FOD lobe thresholding; every lobe where the\
            FOD is positive will be retained.
        fmls_lobe_merge_ratio: Specify the ratio between a given FOD amplitude\
            sample between two lobes, and the smallest peak amplitude of the\
            adjacent lobes, above which those lobes will be merged. This is the\
            amplitude of the FOD at the 'bridge' point between the two lobes,\
            divided by the peak amplitude of the smaller of the two adjoining\
            lobes. A value of 1.0 will never merge two lobes into one; a value of\
            0.0 will always merge lobes unless they are bisected by a zero-valued\
            crossing. Default: 1.
        mask: only perform computation within the specified binary brain mask\
            image.
        maxnum: maximum number of fixels to output for any particular voxel\
            (default: no limit)
        nii: output the directions and index file in nii format (instead of the\
            default mif)
        dirpeak: define the fixel direction as that of the lobe's maximal peak as\
            opposed to its weighted mean direction (the default)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fod2fixel = {
        "@type": "mrtrix/fod2fixel",
        "fod": fod,
        "fixel_directory": fixel_directory,
        "fmls_no_thresholds": fmls_no_thresholds,
        "nii": nii,
        "dirpeak": dirpeak,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if afd is not None:
        params["afd"] = afd
    if peak_amp is not None:
        params["peak_amp"] = peak_amp
    if disp is not None:
        params["disp"] = disp
    if fmls_integral is not None:
        params["fmls_integral"] = fmls_integral
    if fmls_peak_value is not None:
        params["fmls_peak_value"] = fmls_peak_value
    if fmls_lobe_merge_ratio is not None:
        params["fmls_lobe_merge_ratio"] = fmls_lobe_merge_ratio
    if mask is not None:
        params["mask"] = mask
    if maxnum is not None:
        params["maxnum"] = maxnum
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fod2fixel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fod2fixel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("afd", None) is not None:
        if not isinstance(params["afd"], str):
            raise StyxValidationError(f'`afd` has the wrong type: Received `{type(params["afd"])}` expected `str`')
    if params.get("peak_amp", None) is not None:
        if not isinstance(params["peak_amp"], str):
            raise StyxValidationError(f'`peak_amp` has the wrong type: Received `{type(params["peak_amp"])}` expected `str`')
    if params.get("disp", None) is not None:
        if not isinstance(params["disp"], str):
            raise StyxValidationError(f'`disp` has the wrong type: Received `{type(params["disp"])}` expected `str`')
    if params.get("fmls_integral", None) is not None:
        if not isinstance(params["fmls_integral"], (float, int)):
            raise StyxValidationError(f'`fmls_integral` has the wrong type: Received `{type(params["fmls_integral"])}` expected `float`')
    if params.get("fmls_peak_value", None) is not None:
        if not isinstance(params["fmls_peak_value"], (float, int)):
            raise StyxValidationError(f'`fmls_peak_value` has the wrong type: Received `{type(params["fmls_peak_value"])}` expected `float`')
    if params.get("fmls_no_thresholds", None) is not None:
        if not isinstance(params["fmls_no_thresholds"], bool):
            raise StyxValidationError(f'`fmls_no_thresholds` has the wrong type: Received `{type(params["fmls_no_thresholds"])}` expected `bool`')
    if params.get("fmls_lobe_merge_ratio", None) is not None:
        if not isinstance(params["fmls_lobe_merge_ratio"], (float, int)):
            raise StyxValidationError(f'`fmls_lobe_merge_ratio` has the wrong type: Received `{type(params["fmls_lobe_merge_ratio"])}` expected `float`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("maxnum", None) is not None:
        if not isinstance(params["maxnum"], int):
            raise StyxValidationError(f'`maxnum` has the wrong type: Received `{type(params["maxnum"])}` expected `int`')
    if params.get("nii", None) is not None:
        if not isinstance(params["nii"], bool):
            raise StyxValidationError(f'`nii` has the wrong type: Received `{type(params["nii"])}` expected `bool`')
    if params.get("dirpeak", None) is not None:
        if not isinstance(params["dirpeak"], bool):
            raise StyxValidationError(f'`dirpeak` has the wrong type: Received `{type(params["dirpeak"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fod2fixelConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fod", None) is None:
        raise StyxValidationError("`fod` must not be None")
    if not isinstance(params["fod"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fod` has the wrong type: Received `{type(params["fod"])}` expected `InputPathType`')
    if params.get("fixel_directory", None) is None:
        raise StyxValidationError("`fixel_directory` must not be None")
    if not isinstance(params["fixel_directory"], str):
        raise StyxValidationError(f'`fixel_directory` has the wrong type: Received `{type(params["fixel_directory"])}` expected `str`')

def fod2fixel_cargs(params: Fod2fixel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fod2fixel")
    v_0 = params.get("afd")
    if v_0 is not None:
        cargs.append("-afd")
        cargs.append(v_0)
    v_1 = params.get("peak_amp")
    if v_1 is not None:
        cargs.append("-peak_amp")
        cargs.append(v_1)
    v_2 = params.get("disp")
    if v_2 is not None:
        cargs.append("-disp")
        cargs.append(v_2)
    v_3 = params.get("fmls_integral")
    if v_3 is not None:
        cargs.append("-fmls_integral")
        cargs.append(str(v_3))
    v_4 = params.get("fmls_peak_value")
    if v_4 is not None:
        cargs.append("-fmls_peak_value")
        cargs.append(str(v_4))
    if params.get("fmls_no_thresholds"):
        cargs.append("-fmls_no_thresholds")
    v_5 = params.get("fmls_lobe_merge_ratio")
    if v_5 is not None:
        cargs.append("-fmls_lobe_merge_ratio")
        cargs.append(str(v_5))
    v_6 = params.get("mask")
    if v_6 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("maxnum")
    if v_7 is not None:
        cargs.append("-maxnum")
        cargs.append(str(v_7))
    if params.get("nii"):
        cargs.append("-nii")
    if params.get("dirpeak"):
        cargs.append("-dirpeak")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_8 = params.get("nthreads")
    if v_8 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_8))
    v_9 = params.get("config")
    if v_9 is not None:
        for item0 in v_9:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["fod"]))
    cargs.append(params["fixel_directory"])
    return cargs

def fod2fixel_outputs(params: Fod2fixel, execution: Execution) -> Fod2fixelOutputs:
    """Build the Fod2fixelOutputs object for this tool."""
    afd_v: OutputPathType | None = None
    peak_amp_v: OutputPathType | None = None
    disp_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    fixel_directory_v: OutputPathType = execution.output_file(params["fixel_directory"])
    __v0 = params.get("afd")
    if __v0 is not None:
        afd_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("peak_amp")
    if __v1 is not None:
        peak_amp_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("disp")
    if __v2 is not None:
        disp_v = execution.output_file((__v2 if __v2 is not None else ""))
    return Fod2fixelOutputs(
        root=root_v,
        fixel_directory=fixel_directory_v,
        afd=afd_v,
        peak_amp=peak_amp_v,
        disp=disp_v,
    )

def fod2fixel_execute(params: Fod2fixel, runner: Runner | None = None) -> Fod2fixelOutputs:
    """
    fod2fixel

    Perform segmentation of continuous Fibre Orientation Distributions (FODs) to produce discrete fixels.



References:

* Reference for the FOD segmentation method:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312 (Appendix 2)

* Reference for Apparent Fibre Density (AFD):
Raffelt, D.; Tournier, J.-D.; Rose, S.; Ridgway, G.R.; Henderson, R.; Crozier, S.; Salvado, O.; Connelly, A. Apparent Fibre Density: a novel measure for the analysis of diffusion-weighted magnetic resonance images.Neuroimage, 2012, 15;59(4), 3976-94

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fod2fixel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FOD2FIXEL_METADATA)
    execution.params(params)
    args = fod2fixel_cargs(params, execution)
    out = fod2fixel_outputs(params, execution)
    execution.run(args)
    return out

def fod2fixel(
    fod: InputPathType,
    fixel_directory: str,
    afd: str | None = None,
    peak_amp: str | None = None,
    disp: str | None = None,
    fmls_integral: float | None = None,
    fmls_peak_value: float | None = None,
    fmls_no_thresholds: bool = False,
    fmls_lobe_merge_ratio: float | None = None,
    mask: InputPathType | None = None,
    maxnum: int | None = None,
    nii: bool = False,
    dirpeak: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fod2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fod2fixelOutputs:
    """
    fod2fixel

    Perform segmentation of continuous Fibre Orientation Distributions (FODs) to produce discrete fixels.



References:

* Reference for the FOD segmentation method:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312 (Appendix 2)

* Reference for Apparent Fibre Density (AFD):
Raffelt, D.; Tournier, J.-D.; Rose, S.; Ridgway, G.R.; Henderson, R.; Crozier, S.; Salvado, O.; Connelly, A. Apparent Fibre Density: a novel measure for the analysis of diffusion-weighted magnetic resonance images.Neuroimage, 2012, 15;59(4), 3976-94

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fod: the input fod image.
        fixel_directory: the output fixel directory
        afd: output the total Apparent Fibre Density per fixel (integral of FOD\
            lobe)
        peak_amp: output the amplitude of the FOD at the maximal peak per fixel
        disp: output a measure of dispersion per fixel as the ratio between FOD\
            lobe integral and maximal peak amplitude
        fmls_integral: threshold absolute numerical integral of positive FOD lobes.\
            Any lobe for which the integral is smaller than this threshold will be\
            discarded. Default: 0.
        fmls_peak_value: threshold peak amplitude of positive FOD lobes. Any lobe\
            for which the maximal peak amplitude is smaller than this threshold\
            will be discarded. Default: 0.1.
        fmls_no_thresholds: disable all FOD lobe thresholding; every lobe where the\
            FOD is positive will be retained.
        fmls_lobe_merge_ratio: Specify the ratio between a given FOD amplitude\
            sample between two lobes, and the smallest peak amplitude of the\
            adjacent lobes, above which those lobes will be merged. This is the\
            amplitude of the FOD at the 'bridge' point between the two lobes,\
            divided by the peak amplitude of the smaller of the two adjoining\
            lobes. A value of 1.0 will never merge two lobes into one; a value of\
            0.0 will always merge lobes unless they are bisected by a zero-valued\
            crossing. Default: 1.
        mask: only perform computation within the specified binary brain mask\
            image.
        maxnum: maximum number of fixels to output for any particular voxel\
            (default: no limit)
        nii: output the directions and index file in nii format (instead of the\
            default mif)
        dirpeak: define the fixel direction as that of the lobe's maximal peak as\
            opposed to its weighted mean direction (the default)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fod2fixel_params(
        fod=fod,
        fixel_directory=fixel_directory,
        afd=afd,
        peak_amp=peak_amp,
        disp=disp,
        fmls_integral=fmls_integral,
        fmls_peak_value=fmls_peak_value,
        fmls_no_thresholds=fmls_no_thresholds,
        fmls_lobe_merge_ratio=fmls_lobe_merge_ratio,
        mask=mask,
        maxnum=maxnum,
        nii=nii,
        dirpeak=dirpeak,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fod2fixel_execute(params, runner)

__all__ = [
    "Fod2fixel",
    "Fod2fixelOutputs",
    "FOD2FIXEL_METADATA",
    "fod2fixel_cargs",
    "fod2fixel_outputs",
    "fod2fixel_params",
    "fod2fixel_execute",
    "fod2fixel_validate",
    "fod2fixel",
]