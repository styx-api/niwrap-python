# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

CONNECTOME2TCK_METADATA = Metadata(
    id="connectome2tck",
    name="connectome2tck",
    package="mrtrix",
)

class Connectome2tckConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Connectome2tck = typing.TypedDict(
    "Connectome2tck",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/connectome2tck"]],
        "nodes": typing.NotRequired[list[int]],
        "exclusive": typing.NotRequired[bool],
        "files": typing.NotRequired[str],
        "exemplars": typing.NotRequired[InputPathType],
        "keep_unassigned": typing.NotRequired[bool],
        "keep_self": typing.NotRequired[bool],
        "tck_weights_in": typing.NotRequired[InputPathType],
        "prefix_tck_weights_out": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Connectome2tckConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks_in": InputPathType,
        "assignments_in": InputPathType,
        "prefix_out": str,
    },
)

@dataclasses.dataclass
class Connectome2tckOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def connectome2tck_params(
    tracks_in: InputPathType,
    assignments_in: InputPathType,
    prefix_out: str,
    nodes: list[int] | None = None,
    exclusive: bool = False,
    files: str | None = None,
    exemplars: InputPathType | None = None,
    keep_unassigned: bool = False,
    keep_self: bool = False,
    tck_weights_in: InputPathType | None = None,
    prefix_tck_weights_out: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Connectome2tckConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Connectome2tck:
    """
    Build parameters.

    Args:
        tracks_in: the input track file
        assignments_in: input text file containing the node assignments for each\
            streamline
        prefix_out: the output file / prefix
        nodes: only select tracks that involve a set of nodes of interest (provide\
            as a comma-separated list of integers)
        exclusive: only select tracks that exclusively connect nodes from within\
            the list of nodes of interest
        files: select how the resulting streamlines will be grouped in output\
            files. Options are: per_edge, per_node, single (default: per_edge)
        exemplars: generate a mean connection exemplar per edge, rather than\
            keeping all streamlines (the parcellation node image must be provided\
            in order to constrain the exemplar endpoints)
        keep_unassigned: by default, the program discards those streamlines that\
            are not successfully assigned to a node. Set this option to generate\
            corresponding outputs containing these streamlines (labelled as node\
            index 0)
        keep_self: by default, the program will not output streamlines that connect\
            to the same node at both ends. Set this option to instead keep these\
            self-connections.
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        prefix_tck_weights_out: provide a prefix for outputting a text file\
            corresponding to each output file, each containing only the streamline\
            weights relevant for that track file
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Connectome2tck = {
        "@type": "mrtrix/connectome2tck",
        "tracks_in": tracks_in,
        "assignments_in": assignments_in,
        "prefix_out": prefix_out,
        "exclusive": exclusive,
        "keep_unassigned": keep_unassigned,
        "keep_self": keep_self,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nodes is not None:
        params["nodes"] = nodes
    if files is not None:
        params["files"] = files
    if exemplars is not None:
        params["exemplars"] = exemplars
    if tck_weights_in is not None:
        params["tck_weights_in"] = tck_weights_in
    if prefix_tck_weights_out is not None:
        params["prefix_tck_weights_out"] = prefix_tck_weights_out
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def connectome2tck_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Connectome2tck."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("nodes", None) is not None:
        if not isinstance(params["nodes"], list):
            raise StyxValidationError(f'`nodes` has the wrong type: Received `{type(params["nodes"])}` expected `list[int]`')
        for e in params["nodes"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`nodes` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("exclusive", None) is not None:
        if not isinstance(params["exclusive"], bool):
            raise StyxValidationError(f'`exclusive` has the wrong type: Received `{type(params["exclusive"])}` expected `bool`')
    if params.get("files", None) is not None:
        if not isinstance(params["files"], str):
            raise StyxValidationError(f'`files` has the wrong type: Received `{type(params["files"])}` expected `str`')
    if params.get("exemplars", None) is not None:
        if not isinstance(params["exemplars"], (pathlib.Path, str)):
            raise StyxValidationError(f'`exemplars` has the wrong type: Received `{type(params["exemplars"])}` expected `InputPathType`')
    if params.get("keep_unassigned", None) is not None:
        if not isinstance(params["keep_unassigned"], bool):
            raise StyxValidationError(f'`keep_unassigned` has the wrong type: Received `{type(params["keep_unassigned"])}` expected `bool`')
    if params.get("keep_self", None) is not None:
        if not isinstance(params["keep_self"], bool):
            raise StyxValidationError(f'`keep_self` has the wrong type: Received `{type(params["keep_self"])}` expected `bool`')
    if params.get("tck_weights_in", None) is not None:
        if not isinstance(params["tck_weights_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tck_weights_in` has the wrong type: Received `{type(params["tck_weights_in"])}` expected `InputPathType`')
    if params.get("prefix_tck_weights_out", None) is not None:
        if not isinstance(params["prefix_tck_weights_out"], str):
            raise StyxValidationError(f'`prefix_tck_weights_out` has the wrong type: Received `{type(params["prefix_tck_weights_out"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Connectome2tckConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks_in", None) is None:
        raise StyxValidationError("`tracks_in` must not be None")
    if not isinstance(params["tracks_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks_in` has the wrong type: Received `{type(params["tracks_in"])}` expected `InputPathType`')
    if params.get("assignments_in", None) is None:
        raise StyxValidationError("`assignments_in` must not be None")
    if not isinstance(params["assignments_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`assignments_in` has the wrong type: Received `{type(params["assignments_in"])}` expected `InputPathType`')
    if params.get("prefix_out", None) is None:
        raise StyxValidationError("`prefix_out` must not be None")
    if not isinstance(params["prefix_out"], str):
        raise StyxValidationError(f'`prefix_out` has the wrong type: Received `{type(params["prefix_out"])}` expected `str`')

def connectome2tck_cargs(params: Connectome2tck, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("connectome2tck")
    v_0 = params.get("nodes")
    if v_0 is not None:
        cargs.append("-nodes")
        cargs.append(",".join([str(item0) for item0 in v_0]))
    if params.get("exclusive"):
        cargs.append("-exclusive")
    v_1 = params.get("files")
    if v_1 is not None:
        cargs.append("-files")
        cargs.append(v_1)
    v_2 = params.get("exemplars")
    if v_2 is not None:
        cargs.append("-exemplars")
        cargs.append(execution.input_file(v_2))
    if params.get("keep_unassigned"):
        cargs.append("-keep_unassigned")
    if params.get("keep_self"):
        cargs.append("-keep_self")
    v_3 = params.get("tck_weights_in")
    if v_3 is not None:
        cargs.append("-tck_weights_in")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("prefix_tck_weights_out")
    if v_4 is not None:
        cargs.append("-prefix_tck_weights_out")
        cargs.append(v_4)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item1 in v_6:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks_in"]))
    cargs.append(execution.input_file(params["assignments_in"]))
    cargs.append(params["prefix_out"])
    return cargs

def connectome2tck_outputs(params: Connectome2tck, execution: Execution) -> Connectome2tckOutputs:
    """Build the Connectome2tckOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Connectome2tckOutputs(
        root=root_v,
    )

def connectome2tck_execute(params: Connectome2tck, runner: Runner | None = None) -> Connectome2tckOutputs:
    """
    connectome2tck

    Extract streamlines from a tractogram based on their assignment to parcellated nodes.

The compulsory input file "assignments_in" should contain a text file where there is one row for each streamline, and each row contains a list of numbers corresponding to the parcels to which that streamline was assigned (most typically there will be two entries per streamline, one for each endpoint; but this is not strictly a requirement). This file will most typically be generated using the tck2connectome command with the -out_assignments option.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    connectome2tck_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(CONNECTOME2TCK_METADATA)
    execution.params(params)
    args = connectome2tck_cargs(params, execution)
    out = connectome2tck_outputs(params, execution)
    execution.run(args)
    return out

def connectome2tck(
    tracks_in: InputPathType,
    assignments_in: InputPathType,
    prefix_out: str,
    nodes: list[int] | None = None,
    exclusive: bool = False,
    files: str | None = None,
    exemplars: InputPathType | None = None,
    keep_unassigned: bool = False,
    keep_self: bool = False,
    tck_weights_in: InputPathType | None = None,
    prefix_tck_weights_out: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Connectome2tckConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Connectome2tckOutputs:
    """
    connectome2tck

    Extract streamlines from a tractogram based on their assignment to parcellated nodes.

The compulsory input file "assignments_in" should contain a text file where there is one row for each streamline, and each row contains a list of numbers corresponding to the parcels to which that streamline was assigned (most typically there will be two entries per streamline, one for each endpoint; but this is not strictly a requirement). This file will most typically be generated using the tck2connectome command with the -out_assignments option.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks_in: the input track file
        assignments_in: input text file containing the node assignments for each\
            streamline
        prefix_out: the output file / prefix
        nodes: only select tracks that involve a set of nodes of interest (provide\
            as a comma-separated list of integers)
        exclusive: only select tracks that exclusively connect nodes from within\
            the list of nodes of interest
        files: select how the resulting streamlines will be grouped in output\
            files. Options are: per_edge, per_node, single (default: per_edge)
        exemplars: generate a mean connection exemplar per edge, rather than\
            keeping all streamlines (the parcellation node image must be provided\
            in order to constrain the exemplar endpoints)
        keep_unassigned: by default, the program discards those streamlines that\
            are not successfully assigned to a node. Set this option to generate\
            corresponding outputs containing these streamlines (labelled as node\
            index 0)
        keep_self: by default, the program will not output streamlines that connect\
            to the same node at both ends. Set this option to instead keep these\
            self-connections.
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        prefix_tck_weights_out: provide a prefix for outputting a text file\
            corresponding to each output file, each containing only the streamline\
            weights relevant for that track file
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = connectome2tck_params(
        tracks_in=tracks_in,
        assignments_in=assignments_in,
        prefix_out=prefix_out,
        nodes=nodes,
        exclusive=exclusive,
        files=files,
        exemplars=exemplars,
        keep_unassigned=keep_unassigned,
        keep_self=keep_self,
        tck_weights_in=tck_weights_in,
        prefix_tck_weights_out=prefix_tck_weights_out,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return connectome2tck_execute(params, runner)

__all__ = [
    "Connectome2tck",
    "Connectome2tckOutputs",
    "CONNECTOME2TCK_METADATA",
    "connectome2tck_cargs",
    "connectome2tck_outputs",
    "connectome2tck_params",
    "connectome2tck_execute",
    "connectome2tck_validate",
    "connectome2tck",
]