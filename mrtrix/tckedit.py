# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKEDIT_METADATA = Metadata(
    id="tckedit",
    name="tckedit",
    package="mrtrix",
)

TckeditVariousString = typing.TypedDict(
    "TckeditVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

TckeditVariousFile = typing.TypedDict(
    "TckeditVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

TckeditInclude = TckeditVariousString | TckeditVariousFile

class TckeditConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckedit = typing.TypedDict(
    "Tckedit",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckedit"]],
        "include": typing.NotRequired[list[TckeditInclude]],
        "include_ordered": typing.NotRequired[list[str]],
        "exclude": typing.NotRequired[list[TckeditInclude]],
        "mask": typing.NotRequired[list[TckeditInclude]],
        "maxlength": typing.NotRequired[float],
        "minlength": typing.NotRequired[float],
        "number": typing.NotRequired[int],
        "skip": typing.NotRequired[int],
        "maxweight": typing.NotRequired[float],
        "minweight": typing.NotRequired[float],
        "inverse": typing.NotRequired[bool],
        "ends_only": typing.NotRequired[bool],
        "tck_weights_in": typing.NotRequired[InputPathType],
        "tck_weights_out": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckeditConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks_in": list[InputPathType],
        "tracks_out": str,
    },
)

@dataclasses.dataclass
class TckeditOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tracks_out: OutputPathType
    """the output track file"""
    tck_weights_out: OutputPathType | None
    """specify the path for an output text scalar file containing streamline weights """

def tckedit_params(
    tracks_in: list[InputPathType],
    tracks_out: str,
    include: list[TckeditInclude] | None = None,
    include_ordered: list[str] | None = None,
    exclude: list[TckeditInclude] | None = None,
    mask: list[TckeditInclude] | None = None,
    maxlength: float | None = None,
    minlength: float | None = None,
    number: int | None = None,
    skip: int | None = None,
    maxweight: float | None = None,
    minweight: float | None = None,
    inverse: bool = False,
    ends_only: bool = False,
    tck_weights_in: InputPathType | None = None,
    tck_weights_out: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckeditConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckedit:
    """
    Build parameters.

    Args:
        tracks_in: the input track file(s)
        tracks_out: the output track file
        include: specify an inclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines must traverse ALL inclusion regions to be accepted.
        include_ordered: specify an inclusion region of interest, as either a\
            binary mask image, or as a sphere using 4 comma-separared values\
            (x,y,z,radius). Streamlines must traverse ALL inclusion_ordered regions\
            in the order they are specified in order to be accepted.
        exclude: specify an exclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines that enter ANY exclude region will be discarded.
        mask: specify a masking region of interest, as either a binary mask image,\
            or as a sphere using 4 comma-separared values (x,y,z,radius). If\
            defined, streamlines exiting the mask will be truncated.
        maxlength: set the maximum length of any streamline in mm
        minlength: set the minimum length of any streamline in mm
        number: set the desired number of selected streamlines to be propagated to\
            the output file
        skip: omit this number of selected streamlines before commencing writing to\
            the output file
        maxweight: set the maximum weight of any streamline
        minweight: set the minimum weight of any streamline
        inverse: output the inverse selection of streamlines based on the criteria\
            provided; i.e. only those streamlines that fail at least one selection\
            criterion, and/or vertices that are outside masks if provided, will be\
            written to file
        ends_only: only test the ends of each streamline against the provided\
            include/exclude ROIs
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        tck_weights_out: specify the path for an output text scalar file containing\
            streamline weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckedit = {
        "@type": "mrtrix/tckedit",
        "tracks_in": tracks_in,
        "tracks_out": tracks_out,
        "inverse": inverse,
        "ends_only": ends_only,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if include is not None:
        params["include"] = include
    if include_ordered is not None:
        params["include_ordered"] = include_ordered
    if exclude is not None:
        params["exclude"] = exclude
    if mask is not None:
        params["mask"] = mask
    if maxlength is not None:
        params["maxlength"] = maxlength
    if minlength is not None:
        params["minlength"] = minlength
    if number is not None:
        params["number"] = number
    if skip is not None:
        params["skip"] = skip
    if maxweight is not None:
        params["maxweight"] = maxweight
    if minweight is not None:
        params["minweight"] = minweight
    if tck_weights_in is not None:
        params["tck_weights_in"] = tck_weights_in
    if tck_weights_out is not None:
        params["tck_weights_out"] = tck_weights_out
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckedit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckedit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("include", None) is not None:
        if not isinstance(params["include"], list):
            raise StyxValidationError(f'`include` has the wrong type: Received `{type(params["include"])}` expected `list[TckeditInclude]`')
        for e in params["include"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if "@type" not in e:
                raise StyxValidationError("Params object is missing `@type`")
            if e["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `include`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e["@type"] == "VariousString":
                if e.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e["obj"])}` expected `str`')
            elif e["@type"] == "VariousFile":
                if e.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e["obj"])}` expected `InputPathType`')
    if params.get("include_ordered", None) is not None:
        if not isinstance(params["include_ordered"], list):
            raise StyxValidationError(f'`include_ordered` has the wrong type: Received `{type(params["include_ordered"])}` expected `list[str]`')
        for e_2 in params["include_ordered"]:
            if not isinstance(e_2, str):
                raise StyxValidationError(f'`include_ordered` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("exclude", None) is not None:
        if not isinstance(params["exclude"], list):
            raise StyxValidationError(f'`exclude` has the wrong type: Received `{type(params["exclude"])}` expected `list[TckeditInclude]`')
        for e_3 in params["exclude"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if "@type" not in e_3:
                raise StyxValidationError("Params object is missing `@type`")
            if e_3["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `exclude`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e_3["@type"] == "VariousString":
                if e_3.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_3["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_3["obj"])}` expected `str`')
            elif e_3["@type"] == "VariousFile":
                if e_3.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_3["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_3["obj"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], list):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `list[TckeditInclude]`')
        for e_4 in params["mask"]:
            if not isinstance(e_4, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_4)}\'')
            if "@type" not in e_4:
                raise StyxValidationError("Params object is missing `@type`")
            if e_4["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `mask`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e_4["@type"] == "VariousString":
                if e_4.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_4["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_4["obj"])}` expected `str`')
            elif e_4["@type"] == "VariousFile":
                if e_4.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_4["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_4["obj"])}` expected `InputPathType`')
    if params.get("maxlength", None) is not None:
        if not isinstance(params["maxlength"], (float, int)):
            raise StyxValidationError(f'`maxlength` has the wrong type: Received `{type(params["maxlength"])}` expected `float`')
    if params.get("minlength", None) is not None:
        if not isinstance(params["minlength"], (float, int)):
            raise StyxValidationError(f'`minlength` has the wrong type: Received `{type(params["minlength"])}` expected `float`')
    if params.get("number", None) is not None:
        if not isinstance(params["number"], int):
            raise StyxValidationError(f'`number` has the wrong type: Received `{type(params["number"])}` expected `int`')
    if params.get("skip", None) is not None:
        if not isinstance(params["skip"], int):
            raise StyxValidationError(f'`skip` has the wrong type: Received `{type(params["skip"])}` expected `int`')
    if params.get("maxweight", None) is not None:
        if not isinstance(params["maxweight"], (float, int)):
            raise StyxValidationError(f'`maxweight` has the wrong type: Received `{type(params["maxweight"])}` expected `float`')
    if params.get("minweight", None) is not None:
        if not isinstance(params["minweight"], (float, int)):
            raise StyxValidationError(f'`minweight` has the wrong type: Received `{type(params["minweight"])}` expected `float`')
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')
    if params.get("ends_only", None) is not None:
        if not isinstance(params["ends_only"], bool):
            raise StyxValidationError(f'`ends_only` has the wrong type: Received `{type(params["ends_only"])}` expected `bool`')
    if params.get("tck_weights_in", None) is not None:
        if not isinstance(params["tck_weights_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tck_weights_in` has the wrong type: Received `{type(params["tck_weights_in"])}` expected `InputPathType`')
    if params.get("tck_weights_out", None) is not None:
        if not isinstance(params["tck_weights_out"], str):
            raise StyxValidationError(f'`tck_weights_out` has the wrong type: Received `{type(params["tck_weights_out"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckeditConfig]`')
        for e_5 in params["config"]:
            if not isinstance(e_5, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_5)}\'')
            if e_5.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_5["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_5["key"])}` expected `str`')
            if e_5.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_5["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_5["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks_in", None) is None:
        raise StyxValidationError("`tracks_in` must not be None")
    if not isinstance(params["tracks_in"], list):
        raise StyxValidationError(f'`tracks_in` has the wrong type: Received `{type(params["tracks_in"])}` expected `list[InputPathType]`')
    for e_6 in params["tracks_in"]:
        if not isinstance(e_6, (pathlib.Path, str)):
            raise StyxValidationError(f'`tracks_in` has the wrong type: Received `{type(e_6)}` expected `list[InputPathType]`')
    if params.get("tracks_out", None) is None:
        raise StyxValidationError("`tracks_out` must not be None")
    if not isinstance(params["tracks_out"], str):
        raise StyxValidationError(f'`tracks_out` has the wrong type: Received `{type(params["tracks_out"])}` expected `str`')

def tckedit_cargs(params: Tckedit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckedit")
    v_0 = params.get("include")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append("-include")
            if item0["@type"] == "VariousString":
                cargs.append(str(item0["obj"]))
            elif item0["@type"] == "VariousFile":
                cargs.append(str(item0["obj"]))
    v_1 = params.get("include_ordered")
    if v_1 is not None:
        for item1 in v_1:
            cargs.append("-include_ordered")
            cargs.append(item1)
    v_2 = params.get("exclude")
    if v_2 is not None:
        for item2 in v_2:
            cargs.append("-exclude")
            if item2["@type"] == "VariousString":
                cargs.append(str(item2["obj"]))
            elif item2["@type"] == "VariousFile":
                cargs.append(str(item2["obj"]))
    v_3 = params.get("mask")
    if v_3 is not None:
        for item3 in v_3:
            cargs.append("-mask")
            if item3["@type"] == "VariousString":
                cargs.append(str(item3["obj"]))
            elif item3["@type"] == "VariousFile":
                cargs.append(str(item3["obj"]))
    v_4 = params.get("maxlength")
    if v_4 is not None:
        cargs.append("-maxlength")
        cargs.append(str(v_4))
    v_5 = params.get("minlength")
    if v_5 is not None:
        cargs.append("-minlength")
        cargs.append(str(v_5))
    v_6 = params.get("number")
    if v_6 is not None:
        cargs.append("-number")
        cargs.append(str(v_6))
    v_7 = params.get("skip")
    if v_7 is not None:
        cargs.append("-skip")
        cargs.append(str(v_7))
    v_8 = params.get("maxweight")
    if v_8 is not None:
        cargs.append("-maxweight")
        cargs.append(str(v_8))
    v_9 = params.get("minweight")
    if v_9 is not None:
        cargs.append("-minweight")
        cargs.append(str(v_9))
    if params.get("inverse"):
        cargs.append("-inverse")
    if params.get("ends_only"):
        cargs.append("-ends_only")
    v_10 = params.get("tck_weights_in")
    if v_10 is not None:
        cargs.append("-tck_weights_in")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("tck_weights_out")
    if v_11 is not None:
        cargs.append("-tck_weights_out")
        cargs.append(v_11)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_12 = params.get("nthreads")
    if v_12 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_12))
    v_13 = params.get("config")
    if v_13 is not None:
        for item4 in v_13:
            cargs.append("-config")
            cargs.append(item4["key"])
            cargs.append(item4["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item5 in params["tracks_in"]:
        cargs.append(execution.input_file(item5))
    cargs.append(params["tracks_out"])
    return cargs

def tckedit_outputs(params: Tckedit, execution: Execution) -> TckeditOutputs:
    """Build the TckeditOutputs object for this tool."""
    tck_weights_out_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    tracks_out_v: OutputPathType = execution.output_file(params["tracks_out"])
    __v0 = params.get("tck_weights_out")
    if __v0 is not None:
        tck_weights_out_v = execution.output_file((__v0 if __v0 is not None else ""))
    return TckeditOutputs(
        root=root_v,
        tracks_out=tracks_out_v,
        tck_weights_out=tck_weights_out_v,
    )

def tckedit_execute(params: Tckedit, runner: Runner | None = None) -> TckeditOutputs:
    """
    tckedit

    Perform various editing operations on track files.

This command can be used to perform various types of manipulations on track data. A range of such manipulations are demonstrated in the examples provided below.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckedit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKEDIT_METADATA)
    execution.params(params)
    args = tckedit_cargs(params, execution)
    out = tckedit_outputs(params, execution)
    execution.run(args)
    return out

def tckedit(
    tracks_in: list[InputPathType],
    tracks_out: str,
    include: list[TckeditInclude] | None = None,
    include_ordered: list[str] | None = None,
    exclude: list[TckeditInclude] | None = None,
    mask: list[TckeditInclude] | None = None,
    maxlength: float | None = None,
    minlength: float | None = None,
    number: int | None = None,
    skip: int | None = None,
    maxweight: float | None = None,
    minweight: float | None = None,
    inverse: bool = False,
    ends_only: bool = False,
    tck_weights_in: InputPathType | None = None,
    tck_weights_out: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckeditConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckeditOutputs:
    """
    tckedit

    Perform various editing operations on track files.

This command can be used to perform various types of manipulations on track data. A range of such manipulations are demonstrated in the examples provided below.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks_in: the input track file(s)
        tracks_out: the output track file
        include: specify an inclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines must traverse ALL inclusion regions to be accepted.
        include_ordered: specify an inclusion region of interest, as either a\
            binary mask image, or as a sphere using 4 comma-separared values\
            (x,y,z,radius). Streamlines must traverse ALL inclusion_ordered regions\
            in the order they are specified in order to be accepted.
        exclude: specify an exclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines that enter ANY exclude region will be discarded.
        mask: specify a masking region of interest, as either a binary mask image,\
            or as a sphere using 4 comma-separared values (x,y,z,radius). If\
            defined, streamlines exiting the mask will be truncated.
        maxlength: set the maximum length of any streamline in mm
        minlength: set the minimum length of any streamline in mm
        number: set the desired number of selected streamlines to be propagated to\
            the output file
        skip: omit this number of selected streamlines before commencing writing to\
            the output file
        maxweight: set the maximum weight of any streamline
        minweight: set the minimum weight of any streamline
        inverse: output the inverse selection of streamlines based on the criteria\
            provided; i.e. only those streamlines that fail at least one selection\
            criterion, and/or vertices that are outside masks if provided, will be\
            written to file
        ends_only: only test the ends of each streamline against the provided\
            include/exclude ROIs
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        tck_weights_out: specify the path for an output text scalar file containing\
            streamline weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckedit_params(
        tracks_in=tracks_in,
        tracks_out=tracks_out,
        include=include,
        include_ordered=include_ordered,
        exclude=exclude,
        mask=mask,
        maxlength=maxlength,
        minlength=minlength,
        number=number,
        skip=skip,
        maxweight=maxweight,
        minweight=minweight,
        inverse=inverse,
        ends_only=ends_only,
        tck_weights_in=tck_weights_in,
        tck_weights_out=tck_weights_out,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckedit_execute(params, runner)

__all__ = [
    "Tckedit",
    "TckeditOutputs",
    "TCKEDIT_METADATA",
    "tckedit_cargs",
    "tckedit_outputs",
    "tckedit_params",
    "tckedit_execute",
    "tckedit_validate",
    "tckedit",
]