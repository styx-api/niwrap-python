# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TENSOR2METRIC_METADATA = Metadata(
    id="tensor2metric",
    name="tensor2metric",
    package="mrtrix",
)

class Tensor2metricConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tensor2metric = typing.TypedDict(
    "Tensor2metric",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tensor2metric"]],
        "adc": typing.NotRequired[str],
        "fa": typing.NotRequired[str],
        "ad": typing.NotRequired[str],
        "rd": typing.NotRequired[str],
        "cl": typing.NotRequired[str],
        "cp": typing.NotRequired[str],
        "cs": typing.NotRequired[str],
        "value": typing.NotRequired[str],
        "vector": typing.NotRequired[str],
        "num": typing.NotRequired[list[int]],
        "modulate": typing.NotRequired[str],
        "mask": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Tensor2metricConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tensor": InputPathType,
    },
)

@dataclasses.dataclass
class Tensor2metricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    adc: OutputPathType | None
    """compute the mean apparent diffusion coefficient (ADC) of the diffusion tensor. (sometimes also referred to as the mean diffusivity (MD)) """
    fa: OutputPathType | None
    """compute the fractional anisotropy (FA) of the diffusion tensor. """
    ad: OutputPathType | None
    """compute the axial diffusivity (AD) of the diffusion tensor. (equivalent to the principal eigenvalue) """
    rd: OutputPathType | None
    """compute the radial diffusivity (RD) of the diffusion tensor. (equivalent to the mean of the two non-principal eigenvalues) """
    cl: OutputPathType | None
    """compute the linearity metric of the diffusion tensor. (one of the three Westin shape metrics) """
    cp: OutputPathType | None
    """compute the planarity metric of the diffusion tensor. (one of the three Westin shape metrics) """
    cs: OutputPathType | None
    """compute the sphericity metric of the diffusion tensor. (one of the three Westin shape metrics) """
    value: OutputPathType | None
    """compute the selected eigenvalue(s) of the diffusion tensor. """
    vector: OutputPathType | None
    """compute the selected eigenvector(s) of the diffusion tensor. """

def tensor2metric_params(
    tensor: InputPathType,
    adc: str | None = None,
    fa: str | None = None,
    ad: str | None = None,
    rd: str | None = None,
    cl: str | None = None,
    cp: str | None = None,
    cs: str | None = None,
    value: str | None = None,
    vector: str | None = None,
    num: list[int] | None = None,
    modulate: str | None = None,
    mask: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tensor2metricConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tensor2metric:
    """
    Build parameters.

    Args:
        tensor: the input tensor image.
        adc: compute the mean apparent diffusion coefficient (ADC) of the diffusion\
            tensor. (sometimes also referred to as the mean diffusivity (MD))
        fa: compute the fractional anisotropy (FA) of the diffusion tensor.
        ad: compute the axial diffusivity (AD) of the diffusion tensor. (equivalent\
            to the principal eigenvalue)
        rd: compute the radial diffusivity (RD) of the diffusion tensor.\
            (equivalent to the mean of the two non-principal eigenvalues)
        cl: compute the linearity metric of the diffusion tensor. (one of the three\
            Westin shape metrics)
        cp: compute the planarity metric of the diffusion tensor. (one of the three\
            Westin shape metrics)
        cs: compute the sphericity metric of the diffusion tensor. (one of the\
            three Westin shape metrics)
        value: compute the selected eigenvalue(s) of the diffusion tensor.
        vector: compute the selected eigenvector(s) of the diffusion tensor.
        num: specify the desired eigenvalue/eigenvector(s). Note that several\
            eigenvalues can be specified as a number sequence. For example, '1,3'\
            specifies the principal (1) and minor (3) eigenvalues/eigenvectors\
            (default = 1).
        modulate: specify how to modulate the magnitude of the eigenvectors. Valid\
            choices are: none, FA, eigval (default = FA).
        mask: only perform computation within the specified binary brain mask\
            image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tensor2metric = {
        "@type": "mrtrix/tensor2metric",
        "tensor": tensor,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if adc is not None:
        params["adc"] = adc
    if fa is not None:
        params["fa"] = fa
    if ad is not None:
        params["ad"] = ad
    if rd is not None:
        params["rd"] = rd
    if cl is not None:
        params["cl"] = cl
    if cp is not None:
        params["cp"] = cp
    if cs is not None:
        params["cs"] = cs
    if value is not None:
        params["value"] = value
    if vector is not None:
        params["vector"] = vector
    if num is not None:
        params["num"] = num
    if modulate is not None:
        params["modulate"] = modulate
    if mask is not None:
        params["mask"] = mask
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tensor2metric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tensor2metric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("adc", None) is not None:
        if not isinstance(params["adc"], str):
            raise StyxValidationError(f'`adc` has the wrong type: Received `{type(params["adc"])}` expected `str`')
    if params.get("fa", None) is not None:
        if not isinstance(params["fa"], str):
            raise StyxValidationError(f'`fa` has the wrong type: Received `{type(params["fa"])}` expected `str`')
    if params.get("ad", None) is not None:
        if not isinstance(params["ad"], str):
            raise StyxValidationError(f'`ad` has the wrong type: Received `{type(params["ad"])}` expected `str`')
    if params.get("rd", None) is not None:
        if not isinstance(params["rd"], str):
            raise StyxValidationError(f'`rd` has the wrong type: Received `{type(params["rd"])}` expected `str`')
    if params.get("cl", None) is not None:
        if not isinstance(params["cl"], str):
            raise StyxValidationError(f'`cl` has the wrong type: Received `{type(params["cl"])}` expected `str`')
    if params.get("cp", None) is not None:
        if not isinstance(params["cp"], str):
            raise StyxValidationError(f'`cp` has the wrong type: Received `{type(params["cp"])}` expected `str`')
    if params.get("cs", None) is not None:
        if not isinstance(params["cs"], str):
            raise StyxValidationError(f'`cs` has the wrong type: Received `{type(params["cs"])}` expected `str`')
    if params.get("value", None) is not None:
        if not isinstance(params["value"], str):
            raise StyxValidationError(f'`value` has the wrong type: Received `{type(params["value"])}` expected `str`')
    if params.get("vector", None) is not None:
        if not isinstance(params["vector"], str):
            raise StyxValidationError(f'`vector` has the wrong type: Received `{type(params["vector"])}` expected `str`')
    if params.get("num", None) is not None:
        if not isinstance(params["num"], list):
            raise StyxValidationError(f'`num` has the wrong type: Received `{type(params["num"])}` expected `list[int]`')
        for e in params["num"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`num` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("modulate", None) is not None:
        if not isinstance(params["modulate"], str):
            raise StyxValidationError(f'`modulate` has the wrong type: Received `{type(params["modulate"])}` expected `str`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Tensor2metricConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tensor", None) is None:
        raise StyxValidationError("`tensor` must not be None")
    if not isinstance(params["tensor"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tensor` has the wrong type: Received `{type(params["tensor"])}` expected `InputPathType`')

def tensor2metric_cargs(params: Tensor2metric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tensor2metric")
    v_0 = params.get("adc")
    if v_0 is not None:
        cargs.append("-adc")
        cargs.append(v_0)
    v_1 = params.get("fa")
    if v_1 is not None:
        cargs.append("-fa")
        cargs.append(v_1)
    v_2 = params.get("ad")
    if v_2 is not None:
        cargs.append("-ad")
        cargs.append(v_2)
    v_3 = params.get("rd")
    if v_3 is not None:
        cargs.append("-rd")
        cargs.append(v_3)
    v_4 = params.get("cl")
    if v_4 is not None:
        cargs.append("-cl")
        cargs.append(v_4)
    v_5 = params.get("cp")
    if v_5 is not None:
        cargs.append("-cp")
        cargs.append(v_5)
    v_6 = params.get("cs")
    if v_6 is not None:
        cargs.append("-cs")
        cargs.append(v_6)
    v_7 = params.get("value")
    if v_7 is not None:
        cargs.append("-value")
        cargs.append(v_7)
    v_8 = params.get("vector")
    if v_8 is not None:
        cargs.append("-vector")
        cargs.append(v_8)
    v_9 = params.get("num")
    if v_9 is not None:
        cargs.append("-num")
        for item0 in v_9:
            cargs.append(str(item0))
    v_10 = params.get("modulate")
    if v_10 is not None:
        cargs.append("-modulate")
        cargs.append(v_10)
    v_11 = params.get("mask")
    if v_11 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_11))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_12 = params.get("nthreads")
    if v_12 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_12))
    v_13 = params.get("config")
    if v_13 is not None:
        for item1 in v_13:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tensor"]))
    return cargs

def tensor2metric_outputs(params: Tensor2metric, execution: Execution) -> Tensor2metricOutputs:
    """Build the Tensor2metricOutputs object for this tool."""
    adc_v: OutputPathType | None = None
    fa_v: OutputPathType | None = None
    ad_v: OutputPathType | None = None
    rd_v: OutputPathType | None = None
    cl_v: OutputPathType | None = None
    cp_v: OutputPathType | None = None
    cs_v: OutputPathType | None = None
    value_v: OutputPathType | None = None
    vector_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("adc")
    if __v0 is not None:
        adc_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("fa")
    if __v1 is not None:
        fa_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("ad")
    if __v2 is not None:
        ad_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("rd")
    if __v3 is not None:
        rd_v = execution.output_file((__v3 if __v3 is not None else ""))
    __v4 = params.get("cl")
    if __v4 is not None:
        cl_v = execution.output_file((__v4 if __v4 is not None else ""))
    __v5 = params.get("cp")
    if __v5 is not None:
        cp_v = execution.output_file((__v5 if __v5 is not None else ""))
    __v6 = params.get("cs")
    if __v6 is not None:
        cs_v = execution.output_file((__v6 if __v6 is not None else ""))
    __v7 = params.get("value")
    if __v7 is not None:
        value_v = execution.output_file((__v7 if __v7 is not None else ""))
    __v8 = params.get("vector")
    if __v8 is not None:
        vector_v = execution.output_file((__v8 if __v8 is not None else ""))
    return Tensor2metricOutputs(
        root=root_v,
        adc=adc_v,
        fa=fa_v,
        ad=ad_v,
        rd=rd_v,
        cl=cl_v,
        cp=cp_v,
        cs=cs_v,
        value=value_v,
        vector=vector_v,
    )

def tensor2metric_execute(params: Tensor2metric, runner: Runner | None = None) -> Tensor2metricOutputs:
    """
    tensor2metric

    Generate maps of tensor-derived parameters.



References:

Basser, P. J.; Mattiello, J. & Lebihan, D. MR diffusion tensor spectroscopy and imaging. Biophysical Journal, 1994, 66, 259-267

Westin, C. F.; Peled, S.; Gudbjartsson, H.; Kikinis, R. & Jolesz, F. A. Geometrical diffusion measures for MRI from tensor basis analysis. Proc Intl Soc Mag Reson Med, 1997, 5, 1742

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tensor2metric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TENSOR2METRIC_METADATA)
    execution.params(params)
    args = tensor2metric_cargs(params, execution)
    out = tensor2metric_outputs(params, execution)
    execution.run(args)
    return out

def tensor2metric(
    tensor: InputPathType,
    adc: str | None = None,
    fa: str | None = None,
    ad: str | None = None,
    rd: str | None = None,
    cl: str | None = None,
    cp: str | None = None,
    cs: str | None = None,
    value: str | None = None,
    vector: str | None = None,
    num: list[int] | None = None,
    modulate: str | None = None,
    mask: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tensor2metricConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Tensor2metricOutputs:
    """
    tensor2metric

    Generate maps of tensor-derived parameters.



References:

Basser, P. J.; Mattiello, J. & Lebihan, D. MR diffusion tensor spectroscopy and imaging. Biophysical Journal, 1994, 66, 259-267

Westin, C. F.; Peled, S.; Gudbjartsson, H.; Kikinis, R. & Jolesz, F. A. Geometrical diffusion measures for MRI from tensor basis analysis. Proc Intl Soc Mag Reson Med, 1997, 5, 1742

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tensor: the input tensor image.
        adc: compute the mean apparent diffusion coefficient (ADC) of the diffusion\
            tensor. (sometimes also referred to as the mean diffusivity (MD))
        fa: compute the fractional anisotropy (FA) of the diffusion tensor.
        ad: compute the axial diffusivity (AD) of the diffusion tensor. (equivalent\
            to the principal eigenvalue)
        rd: compute the radial diffusivity (RD) of the diffusion tensor.\
            (equivalent to the mean of the two non-principal eigenvalues)
        cl: compute the linearity metric of the diffusion tensor. (one of the three\
            Westin shape metrics)
        cp: compute the planarity metric of the diffusion tensor. (one of the three\
            Westin shape metrics)
        cs: compute the sphericity metric of the diffusion tensor. (one of the\
            three Westin shape metrics)
        value: compute the selected eigenvalue(s) of the diffusion tensor.
        vector: compute the selected eigenvector(s) of the diffusion tensor.
        num: specify the desired eigenvalue/eigenvector(s). Note that several\
            eigenvalues can be specified as a number sequence. For example, '1,3'\
            specifies the principal (1) and minor (3) eigenvalues/eigenvectors\
            (default = 1).
        modulate: specify how to modulate the magnitude of the eigenvectors. Valid\
            choices are: none, FA, eigval (default = FA).
        mask: only perform computation within the specified binary brain mask\
            image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tensor2metric_params(
        tensor=tensor,
        adc=adc,
        fa=fa,
        ad=ad,
        rd=rd,
        cl=cl,
        cp=cp,
        cs=cs,
        value=value,
        vector=vector,
        num=num,
        modulate=modulate,
        mask=mask,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tensor2metric_execute(params, runner)

__all__ = [
    "Tensor2metric",
    "Tensor2metricOutputs",
    "TENSOR2METRIC_METADATA",
    "tensor2metric_cargs",
    "tensor2metric_outputs",
    "tensor2metric_params",
    "tensor2metric_execute",
    "tensor2metric_validate",
    "tensor2metric",
]