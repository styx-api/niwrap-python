# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRHISTOGRAM_METADATA = Metadata(
    id="mrhistogram",
    name="mrhistogram",
    package="mrtrix",
)

class MrhistogramConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrhistogram = typing.TypedDict(
    "Mrhistogram",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrhistogram"]],
        "bins": typing.NotRequired[int],
        "template": typing.NotRequired[InputPathType],
        "mask": typing.NotRequired[InputPathType],
        "ignorezero": typing.NotRequired[bool],
        "allvolumes": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrhistogramConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image": InputPathType,
        "hist": str,
    },
)

@dataclasses.dataclass
class MrhistogramOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    hist: OutputPathType
    """the output histogram file"""

def mrhistogram_params(
    image: InputPathType,
    hist: str,
    bins: int | None = None,
    template: InputPathType | None = None,
    mask: InputPathType | None = None,
    ignorezero: bool = False,
    allvolumes: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrhistogramConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrhistogram:
    """
    Build parameters.

    Args:
        image: the input image from which the histogram will be computed
        hist: the output histogram file
        bins: Manually set the number of bins to use to generate the histogram.
        template: Use an existing histogram file as the template for histogram\
            formation
        mask: Calculate the histogram only within a mask image.
        ignorezero: ignore zero-valued data during histogram construction.
        allvolumes: generate one histogram across all image volumes, rather than\
            one per image volume
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrhistogram = {
        "@type": "mrtrix/mrhistogram",
        "image": image,
        "hist": hist,
        "ignorezero": ignorezero,
        "allvolumes": allvolumes,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if bins is not None:
        params["bins"] = bins
    if template is not None:
        params["template"] = template
    if mask is not None:
        params["mask"] = mask
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrhistogram_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrhistogram."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("bins", None) is not None:
        if not isinstance(params["bins"], int):
            raise StyxValidationError(f'`bins` has the wrong type: Received `{type(params["bins"])}` expected `int`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("ignorezero", None) is not None:
        if not isinstance(params["ignorezero"], bool):
            raise StyxValidationError(f'`ignorezero` has the wrong type: Received `{type(params["ignorezero"])}` expected `bool`')
    if params.get("allvolumes", None) is not None:
        if not isinstance(params["allvolumes"], bool):
            raise StyxValidationError(f'`allvolumes` has the wrong type: Received `{type(params["allvolumes"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrhistogramConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("hist", None) is None:
        raise StyxValidationError("`hist` must not be None")
    if not isinstance(params["hist"], str):
        raise StyxValidationError(f'`hist` has the wrong type: Received `{type(params["hist"])}` expected `str`')

def mrhistogram_cargs(params: Mrhistogram, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrhistogram")
    v_0 = params.get("bins")
    if v_0 is not None:
        cargs.append("-bins")
        cargs.append(str(v_0))
    v_1 = params.get("template")
    if v_1 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("mask")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    if params.get("ignorezero"):
        cargs.append("-ignorezero")
    if params.get("allvolumes"):
        cargs.append("-allvolumes")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["image"]))
    cargs.append(params["hist"])
    return cargs

def mrhistogram_outputs(params: Mrhistogram, execution: Execution) -> MrhistogramOutputs:
    """Build the MrhistogramOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    hist_v: OutputPathType = execution.output_file(params["hist"])
    return MrhistogramOutputs(
        root=root_v,
        hist=hist_v,
    )

def mrhistogram_execute(params: Mrhistogram, runner: Runner | None = None) -> MrhistogramOutputs:
    """
    mrhistogram

    Generate a histogram of image intensities.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrhistogram_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRHISTOGRAM_METADATA)
    execution.params(params)
    args = mrhistogram_cargs(params, execution)
    out = mrhistogram_outputs(params, execution)
    execution.run(args)
    return out

def mrhistogram(
    image: InputPathType,
    hist: str,
    bins: int | None = None,
    template: InputPathType | None = None,
    mask: InputPathType | None = None,
    ignorezero: bool = False,
    allvolumes: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrhistogramConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrhistogramOutputs:
    """
    mrhistogram

    Generate a histogram of image intensities.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image: the input image from which the histogram will be computed
        hist: the output histogram file
        bins: Manually set the number of bins to use to generate the histogram.
        template: Use an existing histogram file as the template for histogram\
            formation
        mask: Calculate the histogram only within a mask image.
        ignorezero: ignore zero-valued data during histogram construction.
        allvolumes: generate one histogram across all image volumes, rather than\
            one per image volume
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrhistogram_params(
        image=image,
        hist=hist,
        bins=bins,
        template=template,
        mask=mask,
        ignorezero=ignorezero,
        allvolumes=allvolumes,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrhistogram_execute(params, runner)

__all__ = [
    "Mrhistogram",
    "MrhistogramOutputs",
    "MRHISTOGRAM_METADATA",
    "mrhistogram_cargs",
    "mrhistogram_outputs",
    "mrhistogram_params",
    "mrhistogram_execute",
    "mrhistogram_validate",
    "mrhistogram",
]