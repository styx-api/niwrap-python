# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXEL2TSF_METADATA = Metadata(
    id="fixel2tsf",
    name="fixel2tsf",
    package="mrtrix",
)

class Fixel2tsfConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixel2tsf = typing.TypedDict(
    "Fixel2tsf",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixel2tsf"]],
        "angle": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fixel2tsfConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fixel_in": InputPathType,
        "tracks": InputPathType,
        "tsf": str,
    },
)

@dataclasses.dataclass
class Fixel2tsfOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tsf: OutputPathType
    """the output track scalar file"""

def fixel2tsf_params(
    fixel_in: InputPathType,
    tracks: InputPathType,
    tsf: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2tsfConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixel2tsf:
    """
    Build parameters.

    Args:
        fixel_in: the input fixel data file (within the fixel directory)
        tracks: the input track file
        tsf: the output track scalar file
        angle: the max anglular threshold for computing correspondence between a\
            fixel direction and track tangent (default = 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixel2tsf = {
        "@type": "mrtrix/fixel2tsf",
        "fixel_in": fixel_in,
        "tracks": tracks,
        "tsf": tsf,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if angle is not None:
        params["angle"] = angle
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixel2tsf_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixel2tsf."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("angle", None) is not None:
        if not isinstance(params["angle"], (float, int)):
            raise StyxValidationError(f'`angle` has the wrong type: Received `{type(params["angle"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fixel2tsfConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fixel_in", None) is None:
        raise StyxValidationError("`fixel_in` must not be None")
    if not isinstance(params["fixel_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_in` has the wrong type: Received `{type(params["fixel_in"])}` expected `InputPathType`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("tsf", None) is None:
        raise StyxValidationError("`tsf` must not be None")
    if not isinstance(params["tsf"], str):
        raise StyxValidationError(f'`tsf` has the wrong type: Received `{type(params["tsf"])}` expected `str`')

def fixel2tsf_cargs(params: Fixel2tsf, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixel2tsf")
    v_0 = params.get("angle")
    if v_0 is not None:
        cargs.append("-angle")
        cargs.append(str(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["fixel_in"]))
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(params["tsf"])
    return cargs

def fixel2tsf_outputs(params: Fixel2tsf, execution: Execution) -> Fixel2tsfOutputs:
    """Build the Fixel2tsfOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    tsf_v: OutputPathType = execution.output_file(params["tsf"])
    return Fixel2tsfOutputs(
        root=root_v,
        tsf=tsf_v,
    )

def fixel2tsf_execute(params: Fixel2tsf, runner: Runner | None = None) -> Fixel2tsfOutputs:
    """
    fixel2tsf

    Map fixel values to a track scalar file based on an input tractogram.

This command is useful for visualising all brain fixels (e.g. the output from fixelcfestats) in 3D.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixel2tsf_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXEL2TSF_METADATA)
    execution.params(params)
    args = fixel2tsf_cargs(params, execution)
    out = fixel2tsf_outputs(params, execution)
    execution.run(args)
    return out

def fixel2tsf(
    fixel_in: InputPathType,
    tracks: InputPathType,
    tsf: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2tsfConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fixel2tsfOutputs:
    """
    fixel2tsf

    Map fixel values to a track scalar file based on an input tractogram.

This command is useful for visualising all brain fixels (e.g. the output from fixelcfestats) in 3D.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fixel_in: the input fixel data file (within the fixel directory)
        tracks: the input track file
        tsf: the output track scalar file
        angle: the max anglular threshold for computing correspondence between a\
            fixel direction and track tangent (default = 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixel2tsf_params(
        fixel_in=fixel_in,
        tracks=tracks,
        tsf=tsf,
        angle=angle,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixel2tsf_execute(params, runner)

__all__ = [
    "Fixel2tsf",
    "Fixel2tsfOutputs",
    "FIXEL2TSF_METADATA",
    "fixel2tsf_cargs",
    "fixel2tsf_outputs",
    "fixel2tsf_params",
    "fixel2tsf_execute",
    "fixel2tsf_validate",
    "fixel2tsf",
]