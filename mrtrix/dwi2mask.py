# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DWI2MASK_METADATA = Metadata(
    id="dwi2mask",
    name="dwi2mask",
    package="mrtrix",
)

class Dwi2maskFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class Dwi2maskConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dwi2mask = typing.TypedDict(
    "Dwi2mask",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dwi2mask"]],
        "clean_scale": typing.NotRequired[int],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[Dwi2maskFslgrad],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Dwi2maskConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class Dwi2maskOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output whole-brain mask image"""

def dwi2mask_params(
    input_: InputPathType,
    output: str,
    clean_scale: int | None = None,
    grad: InputPathType | None = None,
    fslgrad: Dwi2maskFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Dwi2maskConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dwi2mask:
    """
    Build parameters.

    Args:
        input_: the input DWI image containing volumes that are both diffusion\
            weighted and b=0
        output: the output whole-brain mask image
        clean_scale: the maximum scale used to cut bridges. A certain maximum scale\
            cuts bridges up to a width (in voxels) of 2x the provided scale.\
            Setting this to 0 disables the mask cleaning step. (Default: 2)
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dwi2mask = {
        "@type": "mrtrix/dwi2mask",
        "input": input_,
        "output": output,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if clean_scale is not None:
        params["clean_scale"] = clean_scale
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dwi2mask_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dwi2mask."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("clean_scale", None) is not None:
        if not isinstance(params["clean_scale"], int):
            raise StyxValidationError(f'`clean_scale` has the wrong type: Received `{type(params["clean_scale"])}` expected `int`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Dwi2maskConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def dwi2mask_cargs(params: Dwi2mask, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dwi2mask")
    v_0 = params.get("clean_scale")
    if v_0 is not None:
        cargs.append("-clean_scale")
        cargs.append(str(v_0))
    v_1 = params.get("grad")
    if v_1 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("fslgrad")
    if v_2 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_2["bvecs"]))
        cargs.append(execution.input_file(v_2["bvals"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["output"])
    return cargs

def dwi2mask_outputs(params: Dwi2mask, execution: Execution) -> Dwi2maskOutputs:
    """Build the Dwi2maskOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return Dwi2maskOutputs(
        root=root_v,
        output=output_v,
    )

def dwi2mask_execute(params: Dwi2mask, runner: Runner | None = None) -> Dwi2maskOutputs:
    """
    dwi2mask

    Generates a whole brain mask from a DWI image.

All diffusion weighted and b=0 volumes are used to obtain a mask that includes both brain tissue and CSF.

In a second step peninsula-like extensions, where the peninsula itself is wider than the bridge connecting it to the mask, are removed. This may help removing artefacts and non-brain parts, e.g. eyes, from the mask.

References:

Dhollander T, Raffelt D, Connelly A. Unsupervised 3-tissue response function estimation from single-shell or multi-shell diffusion MR data without a co-registered T1 image. ISMRM Workshop on Breaking the Barriers of Diffusion MRI, 2016, 5

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dwi2mask_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DWI2MASK_METADATA)
    execution.params(params)
    args = dwi2mask_cargs(params, execution)
    out = dwi2mask_outputs(params, execution)
    execution.run(args)
    return out

def dwi2mask(
    input_: InputPathType,
    output: str,
    clean_scale: int | None = None,
    grad: InputPathType | None = None,
    fslgrad: Dwi2maskFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Dwi2maskConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Dwi2maskOutputs:
    """
    dwi2mask

    Generates a whole brain mask from a DWI image.

All diffusion weighted and b=0 volumes are used to obtain a mask that includes both brain tissue and CSF.

In a second step peninsula-like extensions, where the peninsula itself is wider than the bridge connecting it to the mask, are removed. This may help removing artefacts and non-brain parts, e.g. eyes, from the mask.

References:

Dhollander T, Raffelt D, Connelly A. Unsupervised 3-tissue response function estimation from single-shell or multi-shell diffusion MR data without a co-registered T1 image. ISMRM Workshop on Breaking the Barriers of Diffusion MRI, 2016, 5

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input DWI image containing volumes that are both diffusion\
            weighted and b=0
        output: the output whole-brain mask image
        clean_scale: the maximum scale used to cut bridges. A certain maximum scale\
            cuts bridges up to a width (in voxels) of 2x the provided scale.\
            Setting this to 0 disables the mask cleaning step. (Default: 2)
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dwi2mask_params(
        input_=input_,
        output=output,
        clean_scale=clean_scale,
        grad=grad,
        fslgrad=fslgrad,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dwi2mask_execute(params, runner)

__all__ = [
    "Dwi2mask",
    "Dwi2maskOutputs",
    "DWI2MASK_METADATA",
    "dwi2mask_cargs",
    "dwi2mask_outputs",
    "dwi2mask_params",
    "dwi2mask_execute",
    "dwi2mask_validate",
    "dwi2mask",
]