# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DWIBIASCORRECT_METADATA = Metadata(
    id="dwibiascorrect",
    name="dwibiascorrect",
    package="mrtrix",
)

class DwibiascorrectFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class DwibiascorrectConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dwibiascorrect = typing.TypedDict(
    "Dwibiascorrect",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dwibiascorrect"]],
        "algorithm": str,
        "input_image": InputPathType,
        "output_image": str,
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[DwibiascorrectFslgrad],
        "mask_image": typing.NotRequired[InputPathType],
        "bias_image": typing.NotRequired[InputPathType],
        "nocleanup": typing.NotRequired[bool],
        "scratch_dir": typing.NotRequired[InputPathType],
        "continue_scratch_dir": typing.NotRequired[list[InputPathType]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[float],
        "config": typing.NotRequired[list[DwibiascorrectConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "ants_b": typing.NotRequired[str],
        "ants_c": typing.NotRequired[str],
        "ants_s": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class DwibiascorrectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_image_file: OutputPathType
    """File containing the corrected image series"""
    bias_image_file: OutputPathType | None
    """File containing the estimated bias field"""

def dwibiascorrect_params(
    algorithm: str,
    input_image: InputPathType,
    output_image: str,
    grad: InputPathType | None = None,
    fslgrad: DwibiascorrectFslgrad | None = None,
    mask_image: InputPathType | None = None,
    bias_image: InputPathType | None = None,
    nocleanup: bool = False,
    scratch_dir: InputPathType | None = None,
    continue_scratch_dir: list[InputPathType] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: float | None = None,
    config: list[DwibiascorrectConfig] | None = None,
    help: bool = False,
    version: bool = False,
    ants_b: str | None = None,
    ants_c: str | None = None,
    ants_s: str | None = None,
) -> Dwibiascorrect:
    """
    Build parameters.

    Args:
        algorithm: Select the algorithm to be used for bias correction. Options\
            are: ants, fsl
        input_image: The input image series to be corrected
        output_image: The output corrected image series
        grad: Provide the diffusion gradient table in MRtrix format
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        mask_image: Manually provide a mask image for bias field estimation
        bias_image: Output the estimated bias field
        nocleanup: Do not delete intermediate files during script execution, and do\
            not delete scratch directory at script completion
        scratch_dir: Manually specify the path in which to generate the scratch\
            directory
        continue_scratch_dir: Continue the script from a previous execution; must\
            provide the scratch directory path
        info: Display information messages
        quiet: Do not display information messages or progress status
        debug: Display debugging messages
        force: Force overwrite of output files
        nthreads: Use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading)
        config: temporarily set the value of an MRtrix config file entry.
        help: Display help information and exit
        version: Display version information and exit
        ants_b: N4BiasFieldCorrection option -b (initial mesh resolution in mm,\
            spline order)
        ants_c: N4BiasFieldCorrection option -c (number of iterations, convergence\
            threshold)
        ants_s: N4BiasFieldCorrection option -s (shrink-factor applied to spatial\
            dimensions)

    Returns:
        Parameter dictionary.
    """
    params: Dwibiascorrect = {
        "@type": "mrtrix/dwibiascorrect",
        "algorithm": algorithm,
        "input_image": input_image,
        "output_image": output_image,
        "nocleanup": nocleanup,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if mask_image is not None:
        params["mask_image"] = mask_image
    if bias_image is not None:
        params["bias_image"] = bias_image
    if scratch_dir is not None:
        params["scratch_dir"] = scratch_dir
    if continue_scratch_dir is not None:
        params["continue_scratch_dir"] = continue_scratch_dir
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    if ants_b is not None:
        params["ants_b"] = ants_b
    if ants_c is not None:
        params["ants_c"] = ants_c
    if ants_s is not None:
        params["ants_s"] = ants_s
    return params

def dwibiascorrect_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dwibiascorrect."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("algorithm", None) is None:
        raise StyxValidationError("`algorithm` must not be None")
    if not isinstance(params["algorithm"], str):
        raise StyxValidationError(f'`algorithm` has the wrong type: Received `{type(params["algorithm"])}` expected `str`')
    if params.get("input_image", None) is None:
        raise StyxValidationError("`input_image` must not be None")
    if not isinstance(params["input_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_image` has the wrong type: Received `{type(params["input_image"])}` expected `InputPathType`')
    if params.get("output_image", None) is None:
        raise StyxValidationError("`output_image` must not be None")
    if not isinstance(params["output_image"], str):
        raise StyxValidationError(f'`output_image` has the wrong type: Received `{type(params["output_image"])}` expected `str`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("mask_image", None) is not None:
        if not isinstance(params["mask_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_image` has the wrong type: Received `{type(params["mask_image"])}` expected `InputPathType`')
    if params.get("bias_image", None) is not None:
        if not isinstance(params["bias_image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bias_image` has the wrong type: Received `{type(params["bias_image"])}` expected `InputPathType`')
    if params.get("nocleanup", None) is not None:
        if not isinstance(params["nocleanup"], bool):
            raise StyxValidationError(f'`nocleanup` has the wrong type: Received `{type(params["nocleanup"])}` expected `bool`')
    if params.get("scratch_dir", None) is not None:
        if not isinstance(params["scratch_dir"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scratch_dir` has the wrong type: Received `{type(params["scratch_dir"])}` expected `InputPathType`')
    if params.get("continue_scratch_dir", None) is not None:
        if not isinstance(params["continue_scratch_dir"], list):
            raise StyxValidationError(f'`continue_scratch_dir` has the wrong type: Received `{type(params["continue_scratch_dir"])}` expected `list[InputPathType]`')
        if not (2 <= len(params["continue_scratch_dir"]) <= 2):
            raise StyxValidationError("Parameter `continue_scratch_dir` must contain between 2 and 2 elements (inclusive)")
        for e in params["continue_scratch_dir"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`continue_scratch_dir` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], (float, int)):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `float`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DwibiascorrectConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("ants_b", None) is not None:
        if not isinstance(params["ants_b"], str):
            raise StyxValidationError(f'`ants_b` has the wrong type: Received `{type(params["ants_b"])}` expected `str`')
    if params.get("ants_c", None) is not None:
        if not isinstance(params["ants_c"], str):
            raise StyxValidationError(f'`ants_c` has the wrong type: Received `{type(params["ants_c"])}` expected `str`')
    if params.get("ants_s", None) is not None:
        if not isinstance(params["ants_s"], str):
            raise StyxValidationError(f'`ants_s` has the wrong type: Received `{type(params["ants_s"])}` expected `str`')

def dwibiascorrect_cargs(params: Dwibiascorrect, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dwibiascorrect")
    cargs.append(params["algorithm"])
    cargs.append(execution.input_file(params["input_image"]))
    cargs.append(params["output_image"])
    v_0 = params.get("grad")
    if v_0 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("fslgrad")
    if v_1 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_1["bvecs"]))
        cargs.append(execution.input_file(v_1["bvals"]))
    v_2 = params.get("mask_image")
    if v_2 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("bias_image")
    if v_3 is not None:
        cargs.append("-bias")
        cargs.append(execution.input_file(v_3))
    if params.get("nocleanup"):
        cargs.append("-nocleanup")
    v_4 = params.get("scratch_dir")
    if v_4 is not None:
        cargs.append("-scratch")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("continue_scratch_dir")
    if v_5 is not None:
        cargs.append("-continue")
        for item0 in v_5:
            cargs.append(execution.input_file(item0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_6 = params.get("nthreads")
    if v_6 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        for item1 in v_7:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    v_8 = params.get("ants_b")
    if v_8 is not None:
        cargs.append("-ants.b")
        cargs.append(v_8)
    v_9 = params.get("ants_c")
    if v_9 is not None:
        cargs.append("-ants.c")
        cargs.append(v_9)
    v_10 = params.get("ants_s")
    if v_10 is not None:
        cargs.append("-ants.s")
        cargs.append(v_10)
    return cargs

def dwibiascorrect_outputs(params: Dwibiascorrect, execution: Execution) -> DwibiascorrectOutputs:
    """Build the DwibiascorrectOutputs object for this tool."""
    bias_image_file_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_image_file_v: OutputPathType = execution.output_file(params["output_image"])
    __v0 = params.get("bias_image")
    if __v0 is not None:
        bias_image_file_v = execution.output_file((__v0 if __v0 is not None else ""))
    return DwibiascorrectOutputs(
        root=root_v,
        output_image_file=output_image_file_v,
        bias_image_file=bias_image_file_v,
    )

def dwibiascorrect_execute(params: Dwibiascorrect, runner: Runner | None = None) -> DwibiascorrectOutputs:
    """
    dwibiascorrect

    Perform B1 field inhomogeneity correction for a DWI volume series using either ANTs or FSL.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dwibiascorrect_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DWIBIASCORRECT_METADATA)
    execution.params(params)
    args = dwibiascorrect_cargs(params, execution)
    out = dwibiascorrect_outputs(params, execution)
    execution.run(args)
    return out

def dwibiascorrect(
    algorithm: str,
    input_image: InputPathType,
    output_image: str,
    grad: InputPathType | None = None,
    fslgrad: DwibiascorrectFslgrad | None = None,
    mask_image: InputPathType | None = None,
    bias_image: InputPathType | None = None,
    nocleanup: bool = False,
    scratch_dir: InputPathType | None = None,
    continue_scratch_dir: list[InputPathType] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: float | None = None,
    config: list[DwibiascorrectConfig] | None = None,
    help: bool = False,
    version: bool = False,
    ants_b: str | None = None,
    ants_c: str | None = None,
    ants_s: str | None = None,
    runner: Runner | None = None,
) -> DwibiascorrectOutputs:
    """
    dwibiascorrect

    Perform B1 field inhomogeneity correction for a DWI volume series using either ANTs or FSL.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        algorithm: Select the algorithm to be used for bias correction. Options\
            are: ants, fsl
        input_image: The input image series to be corrected
        output_image: The output corrected image series
        grad: Provide the diffusion gradient table in MRtrix format
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        mask_image: Manually provide a mask image for bias field estimation
        bias_image: Output the estimated bias field
        nocleanup: Do not delete intermediate files during script execution, and do\
            not delete scratch directory at script completion
        scratch_dir: Manually specify the path in which to generate the scratch\
            directory
        continue_scratch_dir: Continue the script from a previous execution; must\
            provide the scratch directory path
        info: Display information messages
        quiet: Do not display information messages or progress status
        debug: Display debugging messages
        force: Force overwrite of output files
        nthreads: Use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading)
        config: temporarily set the value of an MRtrix config file entry.
        help: Display help information and exit
        version: Display version information and exit
        ants_b: N4BiasFieldCorrection option -b (initial mesh resolution in mm,\
            spline order)
        ants_c: N4BiasFieldCorrection option -c (number of iterations, convergence\
            threshold)
        ants_s: N4BiasFieldCorrection option -s (shrink-factor applied to spatial\
            dimensions)
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dwibiascorrect_params(
        algorithm=algorithm,
        input_image=input_image,
        output_image=output_image,
        grad=grad,
        fslgrad=fslgrad,
        mask_image=mask_image,
        bias_image=bias_image,
        nocleanup=nocleanup,
        scratch_dir=scratch_dir,
        continue_scratch_dir=continue_scratch_dir,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
        ants_b=ants_b,
        ants_c=ants_c,
        ants_s=ants_s,
    )
    return dwibiascorrect_execute(params, runner)

__all__ = [
    "Dwibiascorrect",
    "DwibiascorrectOutputs",
    "DWIBIASCORRECT_METADATA",
    "dwibiascorrect_cargs",
    "dwibiascorrect_outputs",
    "dwibiascorrect_params",
    "dwibiascorrect_execute",
    "dwibiascorrect_validate",
    "dwibiascorrect",
]