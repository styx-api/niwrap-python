# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DWIEXTRACT_METADATA = Metadata(
    id="dwiextract",
    name="dwiextract",
    package="mrtrix",
)

class DwiextractFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class DwiextractExportGradFsl(typing.TypedDict):
    bvecs_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""
    bvals_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""

class DwiextractImportPeEddy(typing.TypedDict):
    config: InputPathType
    """import phase-encoding information from an EDDY-style config / index file pair"""
    indices: InputPathType
    """import phase-encoding information from an EDDY-style config / index file pair"""

DwiextractVariousString = typing.TypedDict(
    "DwiextractVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

DwiextractVariousFile = typing.TypedDict(
    "DwiextractVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

DwiextractStrides = DwiextractVariousString | DwiextractVariousFile

class DwiextractConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dwiextract = typing.TypedDict(
    "Dwiextract",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dwiextract"]],
        "bzero": typing.NotRequired[bool],
        "no_bzero": typing.NotRequired[bool],
        "singleshell": typing.NotRequired[bool],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[DwiextractFslgrad],
        "shells": typing.NotRequired[list[float]],
        "export_grad_mrtrix": typing.NotRequired[str],
        "export_grad_fsl": typing.NotRequired[DwiextractExportGradFsl],
        "import_pe_table": typing.NotRequired[InputPathType],
        "import_pe_eddy": typing.NotRequired[DwiextractImportPeEddy],
        "pe": typing.NotRequired[list[float]],
        "strides": typing.NotRequired[DwiextractStrides],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[DwiextractConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class DwiextractOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image (diffusion-weighted volumes by default)."""
    export_grad_mrtrix: OutputPathType | None
    """export the diffusion-weighted gradient table to file in MRtrix format """

def dwiextract_params(
    input_: InputPathType,
    output: str,
    bzero: bool = False,
    no_bzero: bool = False,
    singleshell: bool = False,
    grad: InputPathType | None = None,
    fslgrad: DwiextractFslgrad | None = None,
    shells: list[float] | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: DwiextractExportGradFsl | None = None,
    import_pe_table: InputPathType | None = None,
    import_pe_eddy: DwiextractImportPeEddy | None = None,
    pe: list[float] | None = None,
    strides: DwiextractStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DwiextractConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dwiextract:
    """
    Build parameters.

    Args:
        input_: the input DW image.
        output: the output image (diffusion-weighted volumes by default).
        bzero: Output b=0 volumes (instead of the diffusion weighted volumes, if\
            -singleshell is not specified).
        no_bzero: Output only non b=0 volumes (default, if -singleshell is not\
            specified).
        singleshell: Force a single-shell (single non b=0 shell) output. This will\
            include b=0 volumes, if present. Use with -bzero to enforce presence of\
            b=0 volumes (error if not present) or with -no_bzero to exclude them.
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        import_pe_table: import a phase-encoding table from file
        import_pe_eddy: import phase-encoding information from an EDDY-style config\
            / index file pair
        pe: select volumes with a particular phase encoding; this can be three\
            comma-separated values (for i,j,k components of vector direction) or\
            four (direction & total readout time)
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dwiextract = {
        "@type": "mrtrix/dwiextract",
        "input": input_,
        "output": output,
        "bzero": bzero,
        "no_bzero": no_bzero,
        "singleshell": singleshell,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if shells is not None:
        params["shells"] = shells
    if export_grad_mrtrix is not None:
        params["export_grad_mrtrix"] = export_grad_mrtrix
    if export_grad_fsl is not None:
        params["export_grad_fsl"] = export_grad_fsl
    if import_pe_table is not None:
        params["import_pe_table"] = import_pe_table
    if import_pe_eddy is not None:
        params["import_pe_eddy"] = import_pe_eddy
    if pe is not None:
        params["pe"] = pe
    if strides is not None:
        params["strides"] = strides
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dwiextract_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dwiextract."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("bzero", None) is not None:
        if not isinstance(params["bzero"], bool):
            raise StyxValidationError(f'`bzero` has the wrong type: Received `{type(params["bzero"])}` expected `bool`')
    if params.get("no_bzero", None) is not None:
        if not isinstance(params["no_bzero"], bool):
            raise StyxValidationError(f'`no_bzero` has the wrong type: Received `{type(params["no_bzero"])}` expected `bool`')
    if params.get("singleshell", None) is not None:
        if not isinstance(params["singleshell"], bool):
            raise StyxValidationError(f'`singleshell` has the wrong type: Received `{type(params["singleshell"])}` expected `bool`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("shells", None) is not None:
        if not isinstance(params["shells"], list):
            raise StyxValidationError(f'`shells` has the wrong type: Received `{type(params["shells"])}` expected `list[float]`')
        for e in params["shells"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`shells` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("export_grad_mrtrix", None) is not None:
        if not isinstance(params["export_grad_mrtrix"], str):
            raise StyxValidationError(f'`export_grad_mrtrix` has the wrong type: Received `{type(params["export_grad_mrtrix"])}` expected `str`')
    if params.get("export_grad_fsl", None) is not None:
        if not isinstance(params["export_grad_fsl"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["export_grad_fsl"])}\'')
        if params["export_grad_fsl"].get("bvecs_path", None) is None:
            raise StyxValidationError("`bvecs_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvecs_path"], str):
            raise StyxValidationError(f'`bvecs_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvecs_path"])}` expected `str`')
        if params["export_grad_fsl"].get("bvals_path", None) is None:
            raise StyxValidationError("`bvals_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvals_path"], str):
            raise StyxValidationError(f'`bvals_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvals_path"])}` expected `str`')
    if params.get("import_pe_table", None) is not None:
        if not isinstance(params["import_pe_table"], (pathlib.Path, str)):
            raise StyxValidationError(f'`import_pe_table` has the wrong type: Received `{type(params["import_pe_table"])}` expected `InputPathType`')
    if params.get("import_pe_eddy", None) is not None:
        if not isinstance(params["import_pe_eddy"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["import_pe_eddy"])}\'')
        if params["import_pe_eddy"].get("config", None) is None:
            raise StyxValidationError("`config` must not be None")
        if not isinstance(params["import_pe_eddy"]["config"], (pathlib.Path, str)):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["import_pe_eddy"]["config"])}` expected `InputPathType`')
        if params["import_pe_eddy"].get("indices", None) is None:
            raise StyxValidationError("`indices` must not be None")
        if not isinstance(params["import_pe_eddy"]["indices"], (pathlib.Path, str)):
            raise StyxValidationError(f'`indices` has the wrong type: Received `{type(params["import_pe_eddy"]["indices"])}` expected `InputPathType`')
    if params.get("pe", None) is not None:
        if not isinstance(params["pe"], list):
            raise StyxValidationError(f'`pe` has the wrong type: Received `{type(params["pe"])}` expected `list[float]`')
        for e_2 in params["pe"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`pe` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DwiextractConfig]`')
        for e_3 in params["config"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_3["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_3["key"])}` expected `str`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def dwiextract_cargs(params: Dwiextract, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dwiextract")
    if params.get("bzero"):
        cargs.append("-bzero")
    if params.get("no_bzero"):
        cargs.append("-no_bzero")
    if params.get("singleshell"):
        cargs.append("-singleshell")
    v_0 = params.get("grad")
    if v_0 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("fslgrad")
    if v_1 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_1["bvecs"]))
        cargs.append(execution.input_file(v_1["bvals"]))
    v_2 = params.get("shells")
    if v_2 is not None:
        cargs.append("-shells")
        cargs.append(",".join([str(item0) for item0 in v_2]))
    v_3 = params.get("export_grad_mrtrix")
    if v_3 is not None:
        cargs.append("-export_grad_mrtrix")
        cargs.append(v_3)
    v_4 = params.get("export_grad_fsl")
    if v_4 is not None:
        cargs.append("-export_grad_fsl")
        cargs.append(v_4["bvecs_path"])
        cargs.append(v_4["bvals_path"])
    v_5 = params.get("import_pe_table")
    if v_5 is not None:
        cargs.append("-import_pe_table")
        cargs.append(execution.input_file(v_5))
    v_6 = params.get("import_pe_eddy")
    if v_6 is not None:
        cargs.append("-import_pe_eddy")
        cargs.append(execution.input_file(v_6["config"]))
        cargs.append(execution.input_file(v_6["indices"]))
    v_7 = params.get("pe")
    if v_7 is not None:
        cargs.append("-pe")
        cargs.append(",".join([str(item1) for item1 in v_7]))
    v_8 = params.get("strides")
    if v_8 is not None:
        cargs.append("-strides")
        if v_8["@type"] == "VariousString":
            cargs.append(str(v_8["obj"]))
        elif v_8["@type"] == "VariousFile":
            cargs.append(str(v_8["obj"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_9 = params.get("nthreads")
    if v_9 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_9))
    v_10 = params.get("config")
    if v_10 is not None:
        for item2 in v_10:
            cargs.append("-config")
            cargs.append(item2["key"])
            cargs.append(item2["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["output"])
    return cargs

def dwiextract_outputs(params: Dwiextract, execution: Execution) -> DwiextractOutputs:
    """Build the DwiextractOutputs object for this tool."""
    export_grad_mrtrix_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    __v0 = params.get("export_grad_mrtrix")
    if __v0 is not None:
        export_grad_mrtrix_v = execution.output_file((__v0 if __v0 is not None else ""))
    return DwiextractOutputs(
        root=root_v,
        output=output_v,
        export_grad_mrtrix=export_grad_mrtrix_v,
    )

def dwiextract_execute(params: Dwiextract, runner: Runner | None = None) -> DwiextractOutputs:
    """
    dwiextract

    Extract diffusion-weighted volumes, b=0 volumes, or certain shells from a DWI dataset.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dwiextract_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DWIEXTRACT_METADATA)
    execution.params(params)
    args = dwiextract_cargs(params, execution)
    out = dwiextract_outputs(params, execution)
    execution.run(args)
    return out

def dwiextract(
    input_: InputPathType,
    output: str,
    bzero: bool = False,
    no_bzero: bool = False,
    singleshell: bool = False,
    grad: InputPathType | None = None,
    fslgrad: DwiextractFslgrad | None = None,
    shells: list[float] | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: DwiextractExportGradFsl | None = None,
    import_pe_table: InputPathType | None = None,
    import_pe_eddy: DwiextractImportPeEddy | None = None,
    pe: list[float] | None = None,
    strides: DwiextractStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DwiextractConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DwiextractOutputs:
    """
    dwiextract

    Extract diffusion-weighted volumes, b=0 volumes, or certain shells from a DWI dataset.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input DW image.
        output: the output image (diffusion-weighted volumes by default).
        bzero: Output b=0 volumes (instead of the diffusion weighted volumes, if\
            -singleshell is not specified).
        no_bzero: Output only non b=0 volumes (default, if -singleshell is not\
            specified).
        singleshell: Force a single-shell (single non b=0 shell) output. This will\
            include b=0 volumes, if present. Use with -bzero to enforce presence of\
            b=0 volumes (error if not present) or with -no_bzero to exclude them.
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        import_pe_table: import a phase-encoding table from file
        import_pe_eddy: import phase-encoding information from an EDDY-style config\
            / index file pair
        pe: select volumes with a particular phase encoding; this can be three\
            comma-separated values (for i,j,k components of vector direction) or\
            four (direction & total readout time)
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dwiextract_params(
        input_=input_,
        output=output,
        bzero=bzero,
        no_bzero=no_bzero,
        singleshell=singleshell,
        grad=grad,
        fslgrad=fslgrad,
        shells=shells,
        export_grad_mrtrix=export_grad_mrtrix,
        export_grad_fsl=export_grad_fsl,
        import_pe_table=import_pe_table,
        import_pe_eddy=import_pe_eddy,
        pe=pe,
        strides=strides,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dwiextract_execute(params, runner)

__all__ = [
    "Dwiextract",
    "DwiextractOutputs",
    "DWIEXTRACT_METADATA",
    "dwiextract_cargs",
    "dwiextract_outputs",
    "dwiextract_params",
    "dwiextract_execute",
    "dwiextract_validate",
    "dwiextract",
]