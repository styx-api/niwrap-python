# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKCONVERT_METADATA = Metadata(
    id="tckconvert",
    name="tckconvert",
    package="mrtrix",
)

class TckconvertConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

TckconvertVariousString = typing.TypedDict(
    "TckconvertVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

TckconvertVariousFile = typing.TypedDict(
    "TckconvertVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

TckconvertInput = TckconvertVariousString | TckconvertVariousFile

Tckconvert = typing.TypedDict(
    "Tckconvert",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckconvert"]],
        "scanner2voxel": typing.NotRequired[InputPathType],
        "scanner2image": typing.NotRequired[InputPathType],
        "voxel2scanner": typing.NotRequired[InputPathType],
        "image2scanner": typing.NotRequired[InputPathType],
        "sides": typing.NotRequired[int],
        "increment": typing.NotRequired[int],
        "dec": typing.NotRequired[bool],
        "radius": typing.NotRequired[float],
        "ascii": typing.NotRequired[bool],
        "binary": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckconvertConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": TckconvertInput,
        "output": str,
    },
)

@dataclasses.dataclass
class TckconvertOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output track file."""

def tckconvert_params(
    input_: TckconvertInput,
    output: str,
    scanner2voxel: InputPathType | None = None,
    scanner2image: InputPathType | None = None,
    voxel2scanner: InputPathType | None = None,
    image2scanner: InputPathType | None = None,
    sides: int | None = None,
    increment: int | None = None,
    dec: bool = False,
    radius: float | None = None,
    ascii: bool = False,
    binary: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckconvert:
    """
    Build parameters.

    Args:
        input_: the input track file.
        output: the output track file.
        scanner2voxel: if specified, the properties of this image will be used to\
            convert track point positions from real (scanner) coordinates into\
            voxel coordinates.
        scanner2image: if specified, the properties of this image will be used to\
            convert track point positions from real (scanner) coordinates into\
            image coordinates (in mm).
        voxel2scanner: if specified, the properties of this image will be used to\
            convert track point positions from voxel coordinates into real\
            (scanner) coordinates.
        image2scanner: if specified, the properties of this image will be used to\
            convert track point positions from image coordinates (in mm) into real\
            (scanner) coordinates.
        sides: number of sides for streamlines
        increment: generate streamline points at every (increment) points
        dec: add DEC as a primvar
        radius: radius of the streamlines
        ascii: write an ASCII VTK file (this is the default)
        binary: write a binary VTK file
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckconvert = {
        "@type": "mrtrix/tckconvert",
        "input": input_,
        "output": output,
        "dec": dec,
        "ascii": ascii,
        "binary": binary,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if scanner2voxel is not None:
        params["scanner2voxel"] = scanner2voxel
    if scanner2image is not None:
        params["scanner2image"] = scanner2image
    if voxel2scanner is not None:
        params["voxel2scanner"] = voxel2scanner
    if image2scanner is not None:
        params["image2scanner"] = image2scanner
    if sides is not None:
        params["sides"] = sides
    if increment is not None:
        params["increment"] = increment
    if radius is not None:
        params["radius"] = radius
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckconvert_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckconvert."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scanner2voxel", None) is not None:
        if not isinstance(params["scanner2voxel"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scanner2voxel` has the wrong type: Received `{type(params["scanner2voxel"])}` expected `InputPathType`')
    if params.get("scanner2image", None) is not None:
        if not isinstance(params["scanner2image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scanner2image` has the wrong type: Received `{type(params["scanner2image"])}` expected `InputPathType`')
    if params.get("voxel2scanner", None) is not None:
        if not isinstance(params["voxel2scanner"], (pathlib.Path, str)):
            raise StyxValidationError(f'`voxel2scanner` has the wrong type: Received `{type(params["voxel2scanner"])}` expected `InputPathType`')
    if params.get("image2scanner", None) is not None:
        if not isinstance(params["image2scanner"], (pathlib.Path, str)):
            raise StyxValidationError(f'`image2scanner` has the wrong type: Received `{type(params["image2scanner"])}` expected `InputPathType`')
    if params.get("sides", None) is not None:
        if not isinstance(params["sides"], int):
            raise StyxValidationError(f'`sides` has the wrong type: Received `{type(params["sides"])}` expected `int`')
    if params.get("increment", None) is not None:
        if not isinstance(params["increment"], int):
            raise StyxValidationError(f'`increment` has the wrong type: Received `{type(params["increment"])}` expected `int`')
    if params.get("dec", None) is not None:
        if not isinstance(params["dec"], bool):
            raise StyxValidationError(f'`dec` has the wrong type: Received `{type(params["dec"])}` expected `bool`')
    if params.get("radius", None) is not None:
        if not isinstance(params["radius"], (float, int)):
            raise StyxValidationError(f'`radius` has the wrong type: Received `{type(params["radius"])}` expected `float`')
    if params.get("ascii", None) is not None:
        if not isinstance(params["ascii"], bool):
            raise StyxValidationError(f'`ascii` has the wrong type: Received `{type(params["ascii"])}` expected `bool`')
    if params.get("binary", None) is not None:
        if not isinstance(params["binary"], bool):
            raise StyxValidationError(f'`binary` has the wrong type: Received `{type(params["binary"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckconvertConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["input"])}\'')
    if "@type" not in params["input"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["input"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `input`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["input"]["@type"] == "VariousString":
        if params["input"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["input"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["input"]["obj"])}` expected `str`')
    elif params["input"]["@type"] == "VariousFile":
        if params["input"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["input"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["input"]["obj"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def tckconvert_cargs(params: Tckconvert, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckconvert")
    v_0 = params.get("scanner2voxel")
    if v_0 is not None:
        cargs.append("-scanner2voxel")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("scanner2image")
    if v_1 is not None:
        cargs.append("-scanner2image")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("voxel2scanner")
    if v_2 is not None:
        cargs.append("-voxel2scanner")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("image2scanner")
    if v_3 is not None:
        cargs.append("-image2scanner")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("sides")
    if v_4 is not None:
        cargs.append("-sides")
        cargs.append(str(v_4))
    v_5 = params.get("increment")
    if v_5 is not None:
        cargs.append("-increment")
        cargs.append(str(v_5))
    if params.get("dec"):
        cargs.append("-dec")
    v_6 = params.get("radius")
    if v_6 is not None:
        cargs.append("-radius")
        cargs.append(str(v_6))
    if params.get("ascii"):
        cargs.append("-ascii")
    if params.get("binary"):
        cargs.append("-binary")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_7 = params.get("nthreads")
    if v_7 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_7))
    v_8 = params.get("config")
    if v_8 is not None:
        for item0 in v_8:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    if params["input"]["@type"] == "VariousString":
        cargs.append(str(params["input"]["obj"]))
    elif params["input"]["@type"] == "VariousFile":
        cargs.append(str(params["input"]["obj"]))
    cargs.append(params["output"])
    return cargs

def tckconvert_outputs(params: Tckconvert, execution: Execution) -> TckconvertOutputs:
    """Build the TckconvertOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return TckconvertOutputs(
        root=root_v,
        output=output_v,
    )

def tckconvert_execute(params: Tckconvert, runner: Runner | None = None) -> TckconvertOutputs:
    """
    tckconvert

    Convert between different track file formats.

The program currently supports MRtrix .tck files (input/output), ascii text files (input/output), VTK polydata files (input/output), and RenderMan RIB (export only).

Note that ascii files will be stored with one streamline per numbered file. To support this, the command will use the multi-file numbering syntax, where square brackets denote the position of the numbering for the files, for example:

$ tckconvert input.tck output-'[]'.txt

will produce files named output-0000.txt, output-0001.txt, output-0002.txt, ...

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckconvert_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKCONVERT_METADATA)
    execution.params(params)
    args = tckconvert_cargs(params, execution)
    out = tckconvert_outputs(params, execution)
    execution.run(args)
    return out

def tckconvert(
    input_: TckconvertInput,
    output: str,
    scanner2voxel: InputPathType | None = None,
    scanner2image: InputPathType | None = None,
    voxel2scanner: InputPathType | None = None,
    image2scanner: InputPathType | None = None,
    sides: int | None = None,
    increment: int | None = None,
    dec: bool = False,
    radius: float | None = None,
    ascii: bool = False,
    binary: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckconvertOutputs:
    """
    tckconvert

    Convert between different track file formats.

The program currently supports MRtrix .tck files (input/output), ascii text files (input/output), VTK polydata files (input/output), and RenderMan RIB (export only).

Note that ascii files will be stored with one streamline per numbered file. To support this, the command will use the multi-file numbering syntax, where square brackets denote the position of the numbering for the files, for example:

$ tckconvert input.tck output-'[]'.txt

will produce files named output-0000.txt, output-0001.txt, output-0002.txt, ...

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input track file.
        output: the output track file.
        scanner2voxel: if specified, the properties of this image will be used to\
            convert track point positions from real (scanner) coordinates into\
            voxel coordinates.
        scanner2image: if specified, the properties of this image will be used to\
            convert track point positions from real (scanner) coordinates into\
            image coordinates (in mm).
        voxel2scanner: if specified, the properties of this image will be used to\
            convert track point positions from voxel coordinates into real\
            (scanner) coordinates.
        image2scanner: if specified, the properties of this image will be used to\
            convert track point positions from image coordinates (in mm) into real\
            (scanner) coordinates.
        sides: number of sides for streamlines
        increment: generate streamline points at every (increment) points
        dec: add DEC as a primvar
        radius: radius of the streamlines
        ascii: write an ASCII VTK file (this is the default)
        binary: write a binary VTK file
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckconvert_params(
        input_=input_,
        output=output,
        scanner2voxel=scanner2voxel,
        scanner2image=scanner2image,
        voxel2scanner=voxel2scanner,
        image2scanner=image2scanner,
        sides=sides,
        increment=increment,
        dec=dec,
        radius=radius,
        ascii=ascii,
        binary=binary,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckconvert_execute(params, runner)

__all__ = [
    "Tckconvert",
    "TckconvertOutputs",
    "TCKCONVERT_METADATA",
    "tckconvert_cargs",
    "tckconvert_outputs",
    "tckconvert_params",
    "tckconvert_execute",
    "tckconvert_validate",
    "tckconvert",
]