# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

VOXEL2FIXEL_METADATA = Metadata(
    id="voxel2fixel",
    name="voxel2fixel",
    package="mrtrix",
)

class Voxel2fixelConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Voxel2fixel = typing.TypedDict(
    "Voxel2fixel",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/voxel2fixel"]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Voxel2fixelConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image_in": InputPathType,
        "fixel_directory_in": InputPathType,
        "fixel_directory_out": str,
        "fixel_data_out": str,
    },
)

@dataclasses.dataclass
class Voxel2fixelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def voxel2fixel_params(
    image_in: InputPathType,
    fixel_directory_in: InputPathType,
    fixel_directory_out: str,
    fixel_data_out: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Voxel2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Voxel2fixel:
    """
    Build parameters.

    Args:
        image_in: the input image.
        fixel_directory_in: the input fixel directory. Used to define the fixels\
            and their directions
        fixel_directory_out: the fixel directory where the output will be written.\
            This can be the same as the input directory if desired
        fixel_data_out: the name of the fixel data image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Voxel2fixel = {
        "@type": "mrtrix/voxel2fixel",
        "image_in": image_in,
        "fixel_directory_in": fixel_directory_in,
        "fixel_directory_out": fixel_directory_out,
        "fixel_data_out": fixel_data_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def voxel2fixel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Voxel2fixel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Voxel2fixelConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image_in", None) is None:
        raise StyxValidationError("`image_in` must not be None")
    if not isinstance(params["image_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image_in` has the wrong type: Received `{type(params["image_in"])}` expected `InputPathType`')
    if params.get("fixel_directory_in", None) is None:
        raise StyxValidationError("`fixel_directory_in` must not be None")
    if not isinstance(params["fixel_directory_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_directory_in` has the wrong type: Received `{type(params["fixel_directory_in"])}` expected `InputPathType`')
    if params.get("fixel_directory_out", None) is None:
        raise StyxValidationError("`fixel_directory_out` must not be None")
    if not isinstance(params["fixel_directory_out"], str):
        raise StyxValidationError(f'`fixel_directory_out` has the wrong type: Received `{type(params["fixel_directory_out"])}` expected `str`')
    if params.get("fixel_data_out", None) is None:
        raise StyxValidationError("`fixel_data_out` must not be None")
    if not isinstance(params["fixel_data_out"], str):
        raise StyxValidationError(f'`fixel_data_out` has the wrong type: Received `{type(params["fixel_data_out"])}` expected `str`')

def voxel2fixel_cargs(params: Voxel2fixel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("voxel2fixel")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_0 = params.get("nthreads")
    if v_0 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_0))
    v_1 = params.get("config")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["image_in"]))
    cargs.append(execution.input_file(params["fixel_directory_in"]))
    cargs.append(params["fixel_directory_out"])
    cargs.append(params["fixel_data_out"])
    return cargs

def voxel2fixel_outputs(params: Voxel2fixel, execution: Execution) -> Voxel2fixelOutputs:
    """Build the Voxel2fixelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Voxel2fixelOutputs(
        root=root_v,
    )

def voxel2fixel_execute(params: Voxel2fixel, runner: Runner | None = None) -> Voxel2fixelOutputs:
    """
    voxel2fixel

    Map the scalar value in each voxel to all fixels within that voxel.

This command is designed to enable CFE-based statistical analysis to be performed on voxel-wise measures.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    voxel2fixel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(VOXEL2FIXEL_METADATA)
    execution.params(params)
    args = voxel2fixel_cargs(params, execution)
    out = voxel2fixel_outputs(params, execution)
    execution.run(args)
    return out

def voxel2fixel(
    image_in: InputPathType,
    fixel_directory_in: InputPathType,
    fixel_directory_out: str,
    fixel_data_out: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Voxel2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Voxel2fixelOutputs:
    """
    voxel2fixel

    Map the scalar value in each voxel to all fixels within that voxel.

This command is designed to enable CFE-based statistical analysis to be performed on voxel-wise measures.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image_in: the input image.
        fixel_directory_in: the input fixel directory. Used to define the fixels\
            and their directions
        fixel_directory_out: the fixel directory where the output will be written.\
            This can be the same as the input directory if desired
        fixel_data_out: the name of the fixel data image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = voxel2fixel_params(
        image_in=image_in,
        fixel_directory_in=fixel_directory_in,
        fixel_directory_out=fixel_directory_out,
        fixel_data_out=fixel_data_out,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return voxel2fixel_execute(params, runner)

__all__ = [
    "Voxel2fixel",
    "Voxel2fixelOutputs",
    "VOXEL2FIXEL_METADATA",
    "voxel2fixel_cargs",
    "voxel2fixel_outputs",
    "voxel2fixel_params",
    "voxel2fixel_execute",
    "voxel2fixel_validate",
    "voxel2fixel",
]