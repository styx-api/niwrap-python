# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRINFO_METADATA = Metadata(
    id="mrinfo",
    name="mrinfo",
    package="mrtrix",
)

class MrinfoFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class MrinfoExportGradFsl(typing.TypedDict):
    bvecs_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""
    bvals_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""

class MrinfoExportPeEddy(typing.TypedDict):
    config: str
    """export phase-encoding information to an EDDY-style config / index file pair"""
    indices: str
    """export phase-encoding information to an EDDY-style config / index file pair"""

class MrinfoConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrinfo = typing.TypedDict(
    "Mrinfo",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrinfo"]],
        "all": typing.NotRequired[bool],
        "name": typing.NotRequired[bool],
        "format": typing.NotRequired[bool],
        "ndim": typing.NotRequired[bool],
        "size": typing.NotRequired[bool],
        "spacing": typing.NotRequired[bool],
        "datatype": typing.NotRequired[bool],
        "strides": typing.NotRequired[bool],
        "offset": typing.NotRequired[bool],
        "multiplier": typing.NotRequired[bool],
        "transform": typing.NotRequired[bool],
        "property": typing.NotRequired[list[str]],
        "json_keyval": typing.NotRequired[str],
        "json_all": typing.NotRequired[str],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[MrinfoFslgrad],
        "bvalue_scaling": typing.NotRequired[str],
        "export_grad_mrtrix": typing.NotRequired[str],
        "export_grad_fsl": typing.NotRequired[MrinfoExportGradFsl],
        "dwgrad": typing.NotRequired[bool],
        "shell_bvalues": typing.NotRequired[bool],
        "shell_sizes": typing.NotRequired[bool],
        "shell_indices": typing.NotRequired[bool],
        "export_pe_table": typing.NotRequired[str],
        "export_pe_eddy": typing.NotRequired[MrinfoExportPeEddy],
        "petable": typing.NotRequired[bool],
        "nodelete": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrinfoConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image": list[InputPathType],
    },
)

@dataclasses.dataclass
class MrinfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    json_keyval: OutputPathType | None
    """export header key/value entries to a JSON file """
    json_all: OutputPathType | None
    """export all header contents to a JSON file """
    export_grad_mrtrix: OutputPathType | None
    """export the diffusion-weighted gradient table to file in MRtrix format """
    export_pe_table: OutputPathType | None
    """export phase-encoding table to file """

def mrinfo_params(
    image: list[InputPathType],
    all: bool = False,
    name: bool = False,
    format: bool = False,
    ndim: bool = False,
    size: bool = False,
    spacing: bool = False,
    datatype: bool = False,
    strides: bool = False,
    offset: bool = False,
    multiplier: bool = False,
    transform: bool = False,
    property: list[str] | None = None,
    json_keyval: str | None = None,
    json_all: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: MrinfoFslgrad | None = None,
    bvalue_scaling: str | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: MrinfoExportGradFsl | None = None,
    dwgrad: bool = False,
    shell_bvalues: bool = False,
    shell_sizes: bool = False,
    shell_indices: bool = False,
    export_pe_table: str | None = None,
    export_pe_eddy: MrinfoExportPeEddy | None = None,
    petable: bool = False,
    nodelete: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrinfoConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrinfo:
    """
    Build parameters.

    Args:
        image: the input image(s).
        all: print all properties, rather than the first and last 2 of each.
        name: print the file system path of the image
        format: image file format
        ndim: number of image dimensions
        size: image size along each axis
        spacing: voxel spacing along each image dimension
        datatype: data type used for image data storage
        strides: data strides i.e. order and direction of axes data layout
        offset: image intensity offset
        multiplier: image intensity multiplier
        transform: the transformation from image coordinates [mm] to scanner / real\
            world coordinates [mm]
        property: any text properties embedded in the image header under the\
            specified key (use 'all' to list all keys found)
        json_keyval: export header key/value entries to a JSON file
        json_all: export all header contents to a JSON file
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        bvalue_scaling: enable or disable scaling of diffusion b-values by the\
            square of the corresponding DW gradient norm (see Desciption). Valid\
            choices are yes/no, true/false, 0/1 (default: automatic).
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        dwgrad: the diffusion-weighting gradient table, as interpreted by MRtrix3
        shell_bvalues: list the average b-value of each shell
        shell_sizes: list the number of volumes in each shell
        shell_indices: list the image volumes attributed to each b-value shell
        export_pe_table: export phase-encoding table to file
        export_pe_eddy: export phase-encoding information to an EDDY-style config /\
            index file pair
        petable: print the phase encoding table
        nodelete: don't delete temporary images or images passed to mrinfo via Unix\
            pipes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrinfo = {
        "@type": "mrtrix/mrinfo",
        "image": image,
        "all": all,
        "name": name,
        "format": format,
        "ndim": ndim,
        "size": size,
        "spacing": spacing,
        "datatype": datatype,
        "strides": strides,
        "offset": offset,
        "multiplier": multiplier,
        "transform": transform,
        "dwgrad": dwgrad,
        "shell_bvalues": shell_bvalues,
        "shell_sizes": shell_sizes,
        "shell_indices": shell_indices,
        "petable": petable,
        "nodelete": nodelete,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if property is not None:
        params["property"] = property
    if json_keyval is not None:
        params["json_keyval"] = json_keyval
    if json_all is not None:
        params["json_all"] = json_all
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if bvalue_scaling is not None:
        params["bvalue_scaling"] = bvalue_scaling
    if export_grad_mrtrix is not None:
        params["export_grad_mrtrix"] = export_grad_mrtrix
    if export_grad_fsl is not None:
        params["export_grad_fsl"] = export_grad_fsl
    if export_pe_table is not None:
        params["export_pe_table"] = export_pe_table
    if export_pe_eddy is not None:
        params["export_pe_eddy"] = export_pe_eddy
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrinfo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrinfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("all", None) is not None:
        if not isinstance(params["all"], bool):
            raise StyxValidationError(f'`all` has the wrong type: Received `{type(params["all"])}` expected `bool`')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], bool):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `bool`')
    if params.get("format", None) is not None:
        if not isinstance(params["format"], bool):
            raise StyxValidationError(f'`format` has the wrong type: Received `{type(params["format"])}` expected `bool`')
    if params.get("ndim", None) is not None:
        if not isinstance(params["ndim"], bool):
            raise StyxValidationError(f'`ndim` has the wrong type: Received `{type(params["ndim"])}` expected `bool`')
    if params.get("size", None) is not None:
        if not isinstance(params["size"], bool):
            raise StyxValidationError(f'`size` has the wrong type: Received `{type(params["size"])}` expected `bool`')
    if params.get("spacing", None) is not None:
        if not isinstance(params["spacing"], bool):
            raise StyxValidationError(f'`spacing` has the wrong type: Received `{type(params["spacing"])}` expected `bool`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], bool):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `bool`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], bool):
            raise StyxValidationError(f'`strides` has the wrong type: Received `{type(params["strides"])}` expected `bool`')
    if params.get("offset", None) is not None:
        if not isinstance(params["offset"], bool):
            raise StyxValidationError(f'`offset` has the wrong type: Received `{type(params["offset"])}` expected `bool`')
    if params.get("multiplier", None) is not None:
        if not isinstance(params["multiplier"], bool):
            raise StyxValidationError(f'`multiplier` has the wrong type: Received `{type(params["multiplier"])}` expected `bool`')
    if params.get("transform", None) is not None:
        if not isinstance(params["transform"], bool):
            raise StyxValidationError(f'`transform` has the wrong type: Received `{type(params["transform"])}` expected `bool`')
    if params.get("property", None) is not None:
        if not isinstance(params["property"], list):
            raise StyxValidationError(f'`property` has the wrong type: Received `{type(params["property"])}` expected `list[str]`')
        for e in params["property"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`property` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("json_keyval", None) is not None:
        if not isinstance(params["json_keyval"], str):
            raise StyxValidationError(f'`json_keyval` has the wrong type: Received `{type(params["json_keyval"])}` expected `str`')
    if params.get("json_all", None) is not None:
        if not isinstance(params["json_all"], str):
            raise StyxValidationError(f'`json_all` has the wrong type: Received `{type(params["json_all"])}` expected `str`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("bvalue_scaling", None) is not None:
        if not isinstance(params["bvalue_scaling"], str):
            raise StyxValidationError(f'`bvalue_scaling` has the wrong type: Received `{type(params["bvalue_scaling"])}` expected `str`')
    if params.get("export_grad_mrtrix", None) is not None:
        if not isinstance(params["export_grad_mrtrix"], str):
            raise StyxValidationError(f'`export_grad_mrtrix` has the wrong type: Received `{type(params["export_grad_mrtrix"])}` expected `str`')
    if params.get("export_grad_fsl", None) is not None:
        if not isinstance(params["export_grad_fsl"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["export_grad_fsl"])}\'')
        if params["export_grad_fsl"].get("bvecs_path", None) is None:
            raise StyxValidationError("`bvecs_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvecs_path"], str):
            raise StyxValidationError(f'`bvecs_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvecs_path"])}` expected `str`')
        if params["export_grad_fsl"].get("bvals_path", None) is None:
            raise StyxValidationError("`bvals_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvals_path"], str):
            raise StyxValidationError(f'`bvals_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvals_path"])}` expected `str`')
    if params.get("dwgrad", None) is not None:
        if not isinstance(params["dwgrad"], bool):
            raise StyxValidationError(f'`dwgrad` has the wrong type: Received `{type(params["dwgrad"])}` expected `bool`')
    if params.get("shell_bvalues", None) is not None:
        if not isinstance(params["shell_bvalues"], bool):
            raise StyxValidationError(f'`shell_bvalues` has the wrong type: Received `{type(params["shell_bvalues"])}` expected `bool`')
    if params.get("shell_sizes", None) is not None:
        if not isinstance(params["shell_sizes"], bool):
            raise StyxValidationError(f'`shell_sizes` has the wrong type: Received `{type(params["shell_sizes"])}` expected `bool`')
    if params.get("shell_indices", None) is not None:
        if not isinstance(params["shell_indices"], bool):
            raise StyxValidationError(f'`shell_indices` has the wrong type: Received `{type(params["shell_indices"])}` expected `bool`')
    if params.get("export_pe_table", None) is not None:
        if not isinstance(params["export_pe_table"], str):
            raise StyxValidationError(f'`export_pe_table` has the wrong type: Received `{type(params["export_pe_table"])}` expected `str`')
    if params.get("export_pe_eddy", None) is not None:
        if not isinstance(params["export_pe_eddy"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["export_pe_eddy"])}\'')
        if params["export_pe_eddy"].get("config", None) is None:
            raise StyxValidationError("`config` must not be None")
        if not isinstance(params["export_pe_eddy"]["config"], str):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["export_pe_eddy"]["config"])}` expected `str`')
        if params["export_pe_eddy"].get("indices", None) is None:
            raise StyxValidationError("`indices` must not be None")
        if not isinstance(params["export_pe_eddy"]["indices"], str):
            raise StyxValidationError(f'`indices` has the wrong type: Received `{type(params["export_pe_eddy"]["indices"])}` expected `str`')
    if params.get("petable", None) is not None:
        if not isinstance(params["petable"], bool):
            raise StyxValidationError(f'`petable` has the wrong type: Received `{type(params["petable"])}` expected `bool`')
    if params.get("nodelete", None) is not None:
        if not isinstance(params["nodelete"], bool):
            raise StyxValidationError(f'`nodelete` has the wrong type: Received `{type(params["nodelete"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrinfoConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], list):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `list[InputPathType]`')
    for e_3 in params["image"]:
        if not isinstance(e_3, (pathlib.Path, str)):
            raise StyxValidationError(f'`image` has the wrong type: Received `{type(e_3)}` expected `list[InputPathType]`')

def mrinfo_cargs(params: Mrinfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrinfo")
    if params.get("all"):
        cargs.append("-all")
    if params.get("name"):
        cargs.append("-name")
    if params.get("format"):
        cargs.append("-format")
    if params.get("ndim"):
        cargs.append("-ndim")
    if params.get("size"):
        cargs.append("-size")
    if params.get("spacing"):
        cargs.append("-spacing")
    if params.get("datatype"):
        cargs.append("-datatype")
    if params.get("strides"):
        cargs.append("-strides")
    if params.get("offset"):
        cargs.append("-offset")
    if params.get("multiplier"):
        cargs.append("-multiplier")
    if params.get("transform"):
        cargs.append("-transform")
    v_0 = params.get("property")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append("-property")
            cargs.append(item0)
    v_1 = params.get("json_keyval")
    if v_1 is not None:
        cargs.append("-json_keyval")
        cargs.append(v_1)
    v_2 = params.get("json_all")
    if v_2 is not None:
        cargs.append("-json_all")
        cargs.append(v_2)
    v_3 = params.get("grad")
    if v_3 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("fslgrad")
    if v_4 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_4["bvecs"]))
        cargs.append(execution.input_file(v_4["bvals"]))
    v_5 = params.get("bvalue_scaling")
    if v_5 is not None:
        cargs.append("-bvalue_scaling")
        cargs.append(v_5)
    v_6 = params.get("export_grad_mrtrix")
    if v_6 is not None:
        cargs.append("-export_grad_mrtrix")
        cargs.append(v_6)
    v_7 = params.get("export_grad_fsl")
    if v_7 is not None:
        cargs.append("-export_grad_fsl")
        cargs.append(v_7["bvecs_path"])
        cargs.append(v_7["bvals_path"])
    if params.get("dwgrad"):
        cargs.append("-dwgrad")
    if params.get("shell_bvalues"):
        cargs.append("-shell_bvalues")
    if params.get("shell_sizes"):
        cargs.append("-shell_sizes")
    if params.get("shell_indices"):
        cargs.append("-shell_indices")
    v_8 = params.get("export_pe_table")
    if v_8 is not None:
        cargs.append("-export_pe_table")
        cargs.append(v_8)
    v_9 = params.get("export_pe_eddy")
    if v_9 is not None:
        cargs.append("-export_pe_eddy")
        cargs.append(v_9["config"])
        cargs.append(v_9["indices"])
    if params.get("petable"):
        cargs.append("-petable")
    if params.get("nodelete"):
        cargs.append("-nodelete")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_10 = params.get("nthreads")
    if v_10 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_10))
    v_11 = params.get("config")
    if v_11 is not None:
        for item1 in v_11:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item2 in params["image"]:
        cargs.append(execution.input_file(item2))
    return cargs

def mrinfo_outputs(params: Mrinfo, execution: Execution) -> MrinfoOutputs:
    """Build the MrinfoOutputs object for this tool."""
    json_keyval_v: OutputPathType | None = None
    json_all_v: OutputPathType | None = None
    export_grad_mrtrix_v: OutputPathType | None = None
    export_pe_table_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("json_keyval")
    if __v0 is not None:
        json_keyval_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("json_all")
    if __v1 is not None:
        json_all_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("export_grad_mrtrix")
    if __v2 is not None:
        export_grad_mrtrix_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("export_pe_table")
    if __v3 is not None:
        export_pe_table_v = execution.output_file((__v3 if __v3 is not None else ""))
    return MrinfoOutputs(
        root=root_v,
        json_keyval=json_keyval_v,
        json_all=json_all_v,
        export_grad_mrtrix=export_grad_mrtrix_v,
        export_pe_table=export_pe_table_v,
    )

def mrinfo_execute(params: Mrinfo, runner: Runner | None = None) -> MrinfoOutputs:
    """
    mrinfo

    Display image header information, or extract specific information from the header.

By default, all information contained in each image header will be printed to the console in a reader-friendly format.

Alternatively, command-line options may be used to extract specific details from the header(s); these are printed to the console in a format more appropriate for scripting purposes or piping to file. If multiple options and/or images are provided, the requested header fields will be printed in the order in which they appear in the help page, with all requested details from each input image in sequence printed before the next image is processed.

The command can also write the diffusion gradient table from a single input image to file; either in the MRtrix or FSL format (bvecs/bvals file pair; includes appropriate diffusion gradient vector reorientation)

The -dwgrad, -export_* and -shell_* options provide (information about) the diffusion weighting gradient table after it has been processed by the MRtrix3 back-end (vectors normalised, b-values scaled by the square of the vector norm, depending on the -bvalue_scaling option). To see the raw gradient table information as stored in the image header, i.e. without MRtrix3 back-end processing, use "-property dw_scheme".

The -bvalue_scaling option controls an aspect of the import of diffusion gradient tables. When the input diffusion-weighting direction vectors have norms that differ substantially from unity, the b-values will be scaled by the square of their corresponding vector norm (this is how multi-shell acquisitions are frequently achieved on scanner platforms). However in some rare instances, the b-values may be correct, despite the vectors not being of unit norm (or conversely, the b-values may need to be rescaled even though the vectors are close to unit norm). This option allows the user to control this operation and override MRrtix3's automatic detection.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrinfo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRINFO_METADATA)
    execution.params(params)
    args = mrinfo_cargs(params, execution)
    out = mrinfo_outputs(params, execution)
    execution.run(args)
    return out

def mrinfo(
    image: list[InputPathType],
    all: bool = False,
    name: bool = False,
    format: bool = False,
    ndim: bool = False,
    size: bool = False,
    spacing: bool = False,
    datatype: bool = False,
    strides: bool = False,
    offset: bool = False,
    multiplier: bool = False,
    transform: bool = False,
    property: list[str] | None = None,
    json_keyval: str | None = None,
    json_all: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: MrinfoFslgrad | None = None,
    bvalue_scaling: str | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: MrinfoExportGradFsl | None = None,
    dwgrad: bool = False,
    shell_bvalues: bool = False,
    shell_sizes: bool = False,
    shell_indices: bool = False,
    export_pe_table: str | None = None,
    export_pe_eddy: MrinfoExportPeEddy | None = None,
    petable: bool = False,
    nodelete: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrinfoConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrinfoOutputs:
    """
    mrinfo

    Display image header information, or extract specific information from the header.

By default, all information contained in each image header will be printed to the console in a reader-friendly format.

Alternatively, command-line options may be used to extract specific details from the header(s); these are printed to the console in a format more appropriate for scripting purposes or piping to file. If multiple options and/or images are provided, the requested header fields will be printed in the order in which they appear in the help page, with all requested details from each input image in sequence printed before the next image is processed.

The command can also write the diffusion gradient table from a single input image to file; either in the MRtrix or FSL format (bvecs/bvals file pair; includes appropriate diffusion gradient vector reorientation)

The -dwgrad, -export_* and -shell_* options provide (information about) the diffusion weighting gradient table after it has been processed by the MRtrix3 back-end (vectors normalised, b-values scaled by the square of the vector norm, depending on the -bvalue_scaling option). To see the raw gradient table information as stored in the image header, i.e. without MRtrix3 back-end processing, use "-property dw_scheme".

The -bvalue_scaling option controls an aspect of the import of diffusion gradient tables. When the input diffusion-weighting direction vectors have norms that differ substantially from unity, the b-values will be scaled by the square of their corresponding vector norm (this is how multi-shell acquisitions are frequently achieved on scanner platforms). However in some rare instances, the b-values may be correct, despite the vectors not being of unit norm (or conversely, the b-values may need to be rescaled even though the vectors are close to unit norm). This option allows the user to control this operation and override MRrtix3's automatic detection.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image: the input image(s).
        all: print all properties, rather than the first and last 2 of each.
        name: print the file system path of the image
        format: image file format
        ndim: number of image dimensions
        size: image size along each axis
        spacing: voxel spacing along each image dimension
        datatype: data type used for image data storage
        strides: data strides i.e. order and direction of axes data layout
        offset: image intensity offset
        multiplier: image intensity multiplier
        transform: the transformation from image coordinates [mm] to scanner / real\
            world coordinates [mm]
        property: any text properties embedded in the image header under the\
            specified key (use 'all' to list all keys found)
        json_keyval: export header key/value entries to a JSON file
        json_all: export all header contents to a JSON file
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        bvalue_scaling: enable or disable scaling of diffusion b-values by the\
            square of the corresponding DW gradient norm (see Desciption). Valid\
            choices are yes/no, true/false, 0/1 (default: automatic).
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        dwgrad: the diffusion-weighting gradient table, as interpreted by MRtrix3
        shell_bvalues: list the average b-value of each shell
        shell_sizes: list the number of volumes in each shell
        shell_indices: list the image volumes attributed to each b-value shell
        export_pe_table: export phase-encoding table to file
        export_pe_eddy: export phase-encoding information to an EDDY-style config /\
            index file pair
        petable: print the phase encoding table
        nodelete: don't delete temporary images or images passed to mrinfo via Unix\
            pipes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrinfo_params(
        image=image,
        all=all,
        name=name,
        format=format,
        ndim=ndim,
        size=size,
        spacing=spacing,
        datatype=datatype,
        strides=strides,
        offset=offset,
        multiplier=multiplier,
        transform=transform,
        property=property,
        json_keyval=json_keyval,
        json_all=json_all,
        grad=grad,
        fslgrad=fslgrad,
        bvalue_scaling=bvalue_scaling,
        export_grad_mrtrix=export_grad_mrtrix,
        export_grad_fsl=export_grad_fsl,
        dwgrad=dwgrad,
        shell_bvalues=shell_bvalues,
        shell_sizes=shell_sizes,
        shell_indices=shell_indices,
        export_pe_table=export_pe_table,
        export_pe_eddy=export_pe_eddy,
        petable=petable,
        nodelete=nodelete,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrinfo_execute(params, runner)

__all__ = [
    "Mrinfo",
    "MrinfoOutputs",
    "MRINFO_METADATA",
    "mrinfo_cargs",
    "mrinfo_outputs",
    "mrinfo_params",
    "mrinfo_execute",
    "mrinfo_validate",
    "mrinfo",
]