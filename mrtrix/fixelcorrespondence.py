# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXELCORRESPONDENCE_METADATA = Metadata(
    id="fixelcorrespondence",
    name="fixelcorrespondence",
    package="mrtrix",
)

class FixelcorrespondenceConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixelcorrespondence = typing.TypedDict(
    "Fixelcorrespondence",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixelcorrespondence"]],
        "angle": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[FixelcorrespondenceConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "subject_data": InputPathType,
        "template_directory": InputPathType,
        "output_directory": str,
        "output_data": str,
    },
)

@dataclasses.dataclass
class FixelcorrespondenceOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fixelcorrespondence_params(
    subject_data: InputPathType,
    template_directory: InputPathType,
    output_directory: str,
    output_data: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcorrespondenceConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixelcorrespondence:
    """
    Build parameters.

    Args:
        subject_data: the input subject fixel data file. This should be a file\
            inside the fixel directory
        template_directory: the input template fixel directory.
        output_directory: the fixel directory where the output file will be\
            written.
        output_data: the name of the output fixel data file. This will be placed in\
            the output fixel directory
        angle: the max angle threshold for computing inter-subject fixel\
            correspondence (Default: 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixelcorrespondence = {
        "@type": "mrtrix/fixelcorrespondence",
        "subject_data": subject_data,
        "template_directory": template_directory,
        "output_directory": output_directory,
        "output_data": output_data,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if angle is not None:
        params["angle"] = angle
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixelcorrespondence_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixelcorrespondence."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("angle", None) is not None:
        if not isinstance(params["angle"], (float, int)):
            raise StyxValidationError(f'`angle` has the wrong type: Received `{type(params["angle"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[FixelcorrespondenceConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("subject_data", None) is None:
        raise StyxValidationError("`subject_data` must not be None")
    if not isinstance(params["subject_data"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subject_data` has the wrong type: Received `{type(params["subject_data"])}` expected `InputPathType`')
    if params.get("template_directory", None) is None:
        raise StyxValidationError("`template_directory` must not be None")
    if not isinstance(params["template_directory"], (pathlib.Path, str)):
        raise StyxValidationError(f'`template_directory` has the wrong type: Received `{type(params["template_directory"])}` expected `InputPathType`')
    if params.get("output_directory", None) is None:
        raise StyxValidationError("`output_directory` must not be None")
    if not isinstance(params["output_directory"], str):
        raise StyxValidationError(f'`output_directory` has the wrong type: Received `{type(params["output_directory"])}` expected `str`')
    if params.get("output_data", None) is None:
        raise StyxValidationError("`output_data` must not be None")
    if not isinstance(params["output_data"], str):
        raise StyxValidationError(f'`output_data` has the wrong type: Received `{type(params["output_data"])}` expected `str`')

def fixelcorrespondence_cargs(params: Fixelcorrespondence, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixelcorrespondence")
    v_0 = params.get("angle")
    if v_0 is not None:
        cargs.append("-angle")
        cargs.append(str(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["subject_data"]))
    cargs.append(execution.input_file(params["template_directory"]))
    cargs.append(params["output_directory"])
    cargs.append(params["output_data"])
    return cargs

def fixelcorrespondence_outputs(params: Fixelcorrespondence, execution: Execution) -> FixelcorrespondenceOutputs:
    """Build the FixelcorrespondenceOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixelcorrespondenceOutputs(
        root=root_v,
    )

def fixelcorrespondence_execute(params: Fixelcorrespondence, runner: Runner | None = None) -> FixelcorrespondenceOutputs:
    """
    fixelcorrespondence

    Obtain fixel-fixel correpondence between a subject fixel image and a template fixel mask.

It is assumed that the subject image has already been spatially normalised and is aligned with the template. The output fixel image will have the same fixels (and directions) of the template.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixelcorrespondence_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXELCORRESPONDENCE_METADATA)
    execution.params(params)
    args = fixelcorrespondence_cargs(params, execution)
    out = fixelcorrespondence_outputs(params, execution)
    execution.run(args)
    return out

def fixelcorrespondence(
    subject_data: InputPathType,
    template_directory: InputPathType,
    output_directory: str,
    output_data: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcorrespondenceConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> FixelcorrespondenceOutputs:
    """
    fixelcorrespondence

    Obtain fixel-fixel correpondence between a subject fixel image and a template fixel mask.

It is assumed that the subject image has already been spatially normalised and is aligned with the template. The output fixel image will have the same fixels (and directions) of the template.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        subject_data: the input subject fixel data file. This should be a file\
            inside the fixel directory
        template_directory: the input template fixel directory.
        output_directory: the fixel directory where the output file will be\
            written.
        output_data: the name of the output fixel data file. This will be placed in\
            the output fixel directory
        angle: the max angle threshold for computing inter-subject fixel\
            correspondence (Default: 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixelcorrespondence_params(
        subject_data=subject_data,
        template_directory=template_directory,
        output_directory=output_directory,
        output_data=output_data,
        angle=angle,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixelcorrespondence_execute(params, runner)

__all__ = [
    "Fixelcorrespondence",
    "FixelcorrespondenceOutputs",
    "FIXELCORRESPONDENCE_METADATA",
    "fixelcorrespondence_cargs",
    "fixelcorrespondence_outputs",
    "fixelcorrespondence_params",
    "fixelcorrespondence_execute",
    "fixelcorrespondence_validate",
    "fixelcorrespondence",
]