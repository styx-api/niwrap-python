# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKSAMPLE_METADATA = Metadata(
    id="tcksample",
    name="tcksample",
    package="mrtrix",
)

class TcksampleConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tcksample = typing.TypedDict(
    "Tcksample",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tcksample"]],
        "stat_tck": typing.NotRequired[str],
        "nointerp": typing.NotRequired[bool],
        "precise": typing.NotRequired[bool],
        "use_tdi_fraction": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TcksampleConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks": InputPathType,
        "image": InputPathType,
        "values": str,
    },
)

@dataclasses.dataclass
class TcksampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    values: OutputPathType
    """the output sampled values"""

def tcksample_params(
    tracks: InputPathType,
    image: InputPathType,
    values: str,
    stat_tck: str | None = None,
    nointerp: bool = False,
    precise: bool = False,
    use_tdi_fraction: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TcksampleConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tcksample:
    """
    Build parameters.

    Args:
        tracks: the input track file
        image: the image to be sampled
        values: the output sampled values
        stat_tck: compute some statistic from the values along each streamline\
            (options are: mean,median,min,max)
        nointerp: do not use trilinear interpolation when sampling image values
        precise: use the precise mechanism for mapping streamlines to voxels\
            (obviates the need for trilinear interpolation) (only applicable if\
            some per-streamline statistic is requested)
        use_tdi_fraction: each streamline is assigned a fraction of the image\
            intensity in each voxel based on the fraction of the track density\
            contributed by that streamline (this is only appropriate for processing\
            a whole-brain tractogram, and images for which the quantiative\
            parameter is additive)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tcksample = {
        "@type": "mrtrix/tcksample",
        "tracks": tracks,
        "image": image,
        "values": values,
        "nointerp": nointerp,
        "precise": precise,
        "use_tdi_fraction": use_tdi_fraction,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if stat_tck is not None:
        params["stat_tck"] = stat_tck
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tcksample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tcksample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("stat_tck", None) is not None:
        if not isinstance(params["stat_tck"], str):
            raise StyxValidationError(f'`stat_tck` has the wrong type: Received `{type(params["stat_tck"])}` expected `str`')
    if params.get("nointerp", None) is not None:
        if not isinstance(params["nointerp"], bool):
            raise StyxValidationError(f'`nointerp` has the wrong type: Received `{type(params["nointerp"])}` expected `bool`')
    if params.get("precise", None) is not None:
        if not isinstance(params["precise"], bool):
            raise StyxValidationError(f'`precise` has the wrong type: Received `{type(params["precise"])}` expected `bool`')
    if params.get("use_tdi_fraction", None) is not None:
        if not isinstance(params["use_tdi_fraction"], bool):
            raise StyxValidationError(f'`use_tdi_fraction` has the wrong type: Received `{type(params["use_tdi_fraction"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TcksampleConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("values", None) is None:
        raise StyxValidationError("`values` must not be None")
    if not isinstance(params["values"], str):
        raise StyxValidationError(f'`values` has the wrong type: Received `{type(params["values"])}` expected `str`')

def tcksample_cargs(params: Tcksample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tcksample")
    v_0 = params.get("stat_tck")
    if v_0 is not None:
        cargs.append("-stat_tck")
        cargs.append(v_0)
    if params.get("nointerp"):
        cargs.append("-nointerp")
    if params.get("precise"):
        cargs.append("-precise")
    if params.get("use_tdi_fraction"):
        cargs.append("-use_tdi_fraction")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(execution.input_file(params["image"]))
    cargs.append(params["values"])
    return cargs

def tcksample_outputs(params: Tcksample, execution: Execution) -> TcksampleOutputs:
    """Build the TcksampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    values_v: OutputPathType = execution.output_file(params["values"])
    return TcksampleOutputs(
        root=root_v,
        values=values_v,
    )

def tcksample_execute(params: Tcksample, runner: Runner | None = None) -> TcksampleOutputs:
    """
    tcksample

    Sample values of an associated image along tracks.

By default, the value of the underlying image at each point along the track is written to either an ASCII file (with all values for each track on the same line), or a track scalar file (.tsf). Alternatively, some statistic can be taken from the values along each streamline and written to a vector file.

References:

* If using -precise option: Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tcksample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKSAMPLE_METADATA)
    execution.params(params)
    args = tcksample_cargs(params, execution)
    out = tcksample_outputs(params, execution)
    execution.run(args)
    return out

def tcksample(
    tracks: InputPathType,
    image: InputPathType,
    values: str,
    stat_tck: str | None = None,
    nointerp: bool = False,
    precise: bool = False,
    use_tdi_fraction: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TcksampleConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TcksampleOutputs:
    """
    tcksample

    Sample values of an associated image along tracks.

By default, the value of the underlying image at each point along the track is written to either an ASCII file (with all values for each track on the same line), or a track scalar file (.tsf). Alternatively, some statistic can be taken from the values along each streamline and written to a vector file.

References:

* If using -precise option: Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks: the input track file
        image: the image to be sampled
        values: the output sampled values
        stat_tck: compute some statistic from the values along each streamline\
            (options are: mean,median,min,max)
        nointerp: do not use trilinear interpolation when sampling image values
        precise: use the precise mechanism for mapping streamlines to voxels\
            (obviates the need for trilinear interpolation) (only applicable if\
            some per-streamline statistic is requested)
        use_tdi_fraction: each streamline is assigned a fraction of the image\
            intensity in each voxel based on the fraction of the track density\
            contributed by that streamline (this is only appropriate for processing\
            a whole-brain tractogram, and images for which the quantiative\
            parameter is additive)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tcksample_params(
        tracks=tracks,
        image=image,
        values=values,
        stat_tck=stat_tck,
        nointerp=nointerp,
        precise=precise,
        use_tdi_fraction=use_tdi_fraction,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tcksample_execute(params, runner)

__all__ = [
    "Tcksample",
    "TcksampleOutputs",
    "TCKSAMPLE_METADATA",
    "tcksample_cargs",
    "tcksample_outputs",
    "tcksample_params",
    "tcksample_execute",
    "tcksample_validate",
    "tcksample",
]