# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCMEDIT_METADATA = Metadata(
    id="dcmedit",
    name="dcmedit",
    package="mrtrix",
)

class DcmeditTag(typing.TypedDict):
    group: str
    """replace specific tag."""
    element: str
    """replace specific tag."""
    newvalue: str
    """replace specific tag."""

class DcmeditConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dcmedit = typing.TypedDict(
    "Dcmedit",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dcmedit"]],
        "anonymise": typing.NotRequired[bool],
        "id": typing.NotRequired[str],
        "tag": typing.NotRequired[list[DcmeditTag]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[DcmeditConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "file": InputPathType,
    },
)

@dataclasses.dataclass
class DcmeditOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dcmedit_params(
    file: InputPathType,
    anonymise: bool = False,
    id_: str | None = None,
    tag: list[DcmeditTag] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DcmeditConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dcmedit:
    """
    Build parameters.

    Args:
        file: the DICOM file to be edited.
        anonymise: remove any identifiable information, by replacing the following\
            tags: - any tag with Value Representation PN will be replaced with\
            'anonymous' - tag (0010,0030) PatientBirthDate will be replaced with an\
            empty string WARNING: there is no guarantee that this command will\
            remove all identiable information, since such information may be\
            contained in any number of private vendor-specific tags. You will need\
            to double-check the results independently if you need to ensure\
            anonymity.
        id_: replace all ID tags with string supplied. This consists of tags (0010,\
            0020) PatientID and (0010, 1000) OtherPatientIDs
        tag: replace specific tag.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dcmedit = {
        "@type": "mrtrix/dcmedit",
        "file": file,
        "anonymise": anonymise,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if id_ is not None:
        params["id"] = id_
    if tag is not None:
        params["tag"] = tag
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dcmedit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dcmedit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("anonymise", None) is not None:
        if not isinstance(params["anonymise"], bool):
            raise StyxValidationError(f'`anonymise` has the wrong type: Received `{type(params["anonymise"])}` expected `bool`')
    if params.get("id", None) is not None:
        if not isinstance(params["id"], str):
            raise StyxValidationError(f'`id` has the wrong type: Received `{type(params["id"])}` expected `str`')
    if params.get("tag", None) is not None:
        if not isinstance(params["tag"], list):
            raise StyxValidationError(f'`tag` has the wrong type: Received `{type(params["tag"])}` expected `list[DcmeditTag]`')
        for e in params["tag"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("group", None) is None:
                raise StyxValidationError("`group` must not be None")
            if not isinstance(e["group"], str):
                raise StyxValidationError(f'`group` has the wrong type: Received `{type(e["group"])}` expected `str`')
            if e.get("element", None) is None:
                raise StyxValidationError("`element` must not be None")
            if not isinstance(e["element"], str):
                raise StyxValidationError(f'`element` has the wrong type: Received `{type(e["element"])}` expected `str`')
            if e.get("newvalue", None) is None:
                raise StyxValidationError("`newvalue` must not be None")
            if not isinstance(e["newvalue"], str):
                raise StyxValidationError(f'`newvalue` has the wrong type: Received `{type(e["newvalue"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DcmeditConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("file", None) is None:
        raise StyxValidationError("`file` must not be None")
    if not isinstance(params["file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`file` has the wrong type: Received `{type(params["file"])}` expected `InputPathType`')

def dcmedit_cargs(params: Dcmedit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcmedit")
    if params.get("anonymise"):
        cargs.append("-anonymise")
    v_0 = params.get("id")
    if v_0 is not None:
        cargs.append("-id")
        cargs.append(v_0)
    v_1 = params.get("tag")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-tag")
            cargs.append(item0["group"])
            cargs.append(item0["element"])
            cargs.append(item0["newvalue"])
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("nthreads")
    if v_2 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_2))
    v_3 = params.get("config")
    if v_3 is not None:
        for item1 in v_3:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["file"]))
    return cargs

def dcmedit_outputs(params: Dcmedit, execution: Execution) -> DcmeditOutputs:
    """Build the DcmeditOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return DcmeditOutputs(
        root=root_v,
    )

def dcmedit_execute(params: Dcmedit, runner: Runner | None = None) -> DcmeditOutputs:
    """
    dcmedit

    Edit DICOM file in-place.

Note that this command simply replaces the existing values without modifying the DICOM structure in any way. Replacement text will be truncated if it is too long to fit inside the existing tag.

WARNING: this command will modify existing data! It is recommended to run this command on a copy of the original data set to avoid loss of data.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcmedit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCMEDIT_METADATA)
    execution.params(params)
    args = dcmedit_cargs(params, execution)
    out = dcmedit_outputs(params, execution)
    execution.run(args)
    return out

def dcmedit(
    file: InputPathType,
    anonymise: bool = False,
    id_: str | None = None,
    tag: list[DcmeditTag] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DcmeditConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DcmeditOutputs:
    """
    dcmedit

    Edit DICOM file in-place.

Note that this command simply replaces the existing values without modifying the DICOM structure in any way. Replacement text will be truncated if it is too long to fit inside the existing tag.

WARNING: this command will modify existing data! It is recommended to run this command on a copy of the original data set to avoid loss of data.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        file: the DICOM file to be edited.
        anonymise: remove any identifiable information, by replacing the following\
            tags: - any tag with Value Representation PN will be replaced with\
            'anonymous' - tag (0010,0030) PatientBirthDate will be replaced with an\
            empty string WARNING: there is no guarantee that this command will\
            remove all identiable information, since such information may be\
            contained in any number of private vendor-specific tags. You will need\
            to double-check the results independently if you need to ensure\
            anonymity.
        id_: replace all ID tags with string supplied. This consists of tags (0010,\
            0020) PatientID and (0010, 1000) OtherPatientIDs
        tag: replace specific tag.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcmedit_params(
        file=file,
        anonymise=anonymise,
        id_=id_,
        tag=tag,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dcmedit_execute(params, runner)

__all__ = [
    "Dcmedit",
    "DcmeditOutputs",
    "DCMEDIT_METADATA",
    "dcmedit_cargs",
    "dcmedit_outputs",
    "dcmedit_params",
    "dcmedit_execute",
    "dcmedit_validate",
    "dcmedit",
]