# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MASKFILTER_METADATA = Metadata(
    id="maskfilter",
    name="maskfilter",
    package="mrtrix",
)

MaskfilterVariousString = typing.TypedDict(
    "MaskfilterVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

MaskfilterVariousFile = typing.TypedDict(
    "MaskfilterVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

MaskfilterStrides = MaskfilterVariousString | MaskfilterVariousFile

class MaskfilterConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Maskfilter = typing.TypedDict(
    "Maskfilter",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/maskfilter"]],
        "scale": typing.NotRequired[int],
        "axes": typing.NotRequired[list[int]],
        "largest": typing.NotRequired[bool],
        "connectivity": typing.NotRequired[bool],
        "npass": typing.NotRequired[int],
        "extent": typing.NotRequired[list[int]],
        "strides": typing.NotRequired[MaskfilterStrides],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MaskfilterConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "filter": str,
        "output": str,
    },
)

@dataclasses.dataclass
class MaskfilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image."""

def maskfilter_params(
    input_: InputPathType,
    filter: str,
    output: str,
    scale: int | None = None,
    axes: list[int] | None = None,
    largest: bool = False,
    connectivity: bool = False,
    npass: int | None = None,
    extent: list[int] | None = None,
    strides: MaskfilterStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MaskfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Maskfilter:
    """
    Build parameters.

    Args:
        input_: the input image.
        filter: the type of filter to be applied (clean, connect, dilate, erode,\
            median)
        output: the output image.
        scale: the maximum scale used to cut bridges. A certain maximum scale cuts\
            bridges up to a width (in voxels) of 2x the provided scale. (Default:\
            2)
        axes: specify which axes should be included in the connected components. By\
            default only the first 3 axes are included. The axes should be provided\
            as a comma-separated list of values.
        largest: only retain the largest connected component
        connectivity: use 26-voxel-neighbourhood connectivity (Default: 6)
        npass: the number of times to repeatedly apply the filter
        extent: specify the extent (width) of kernel size in voxels. This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the extent for each axis. The default is 3x3x3.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Maskfilter = {
        "@type": "mrtrix/maskfilter",
        "input": input_,
        "filter": filter,
        "output": output,
        "largest": largest,
        "connectivity": connectivity,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if scale is not None:
        params["scale"] = scale
    if axes is not None:
        params["axes"] = axes
    if npass is not None:
        params["npass"] = npass
    if extent is not None:
        params["extent"] = extent
    if strides is not None:
        params["strides"] = strides
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def maskfilter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Maskfilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("scale", None) is not None:
        if not isinstance(params["scale"], int):
            raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `int`')
    if params.get("axes", None) is not None:
        if not isinstance(params["axes"], list):
            raise StyxValidationError(f'`axes` has the wrong type: Received `{type(params["axes"])}` expected `list[int]`')
        for e in params["axes"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`axes` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("largest", None) is not None:
        if not isinstance(params["largest"], bool):
            raise StyxValidationError(f'`largest` has the wrong type: Received `{type(params["largest"])}` expected `bool`')
    if params.get("connectivity", None) is not None:
        if not isinstance(params["connectivity"], bool):
            raise StyxValidationError(f'`connectivity` has the wrong type: Received `{type(params["connectivity"])}` expected `bool`')
    if params.get("npass", None) is not None:
        if not isinstance(params["npass"], int):
            raise StyxValidationError(f'`npass` has the wrong type: Received `{type(params["npass"])}` expected `int`')
    if params.get("extent", None) is not None:
        if not isinstance(params["extent"], list):
            raise StyxValidationError(f'`extent` has the wrong type: Received `{type(params["extent"])}` expected `list[int]`')
        for e_2 in params["extent"]:
            if not isinstance(e_2, int):
                raise StyxValidationError(f'`extent` has the wrong type: Received `{type(e_2)}` expected `list[int]`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MaskfilterConfig]`')
        for e_3 in params["config"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_3["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_3["key"])}` expected `str`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("filter", None) is None:
        raise StyxValidationError("`filter` must not be None")
    if not isinstance(params["filter"], str):
        raise StyxValidationError(f'`filter` has the wrong type: Received `{type(params["filter"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def maskfilter_cargs(params: Maskfilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("maskfilter")
    v_0 = params.get("scale")
    if v_0 is not None:
        cargs.append("-scale")
        cargs.append(str(v_0))
    v_1 = params.get("axes")
    if v_1 is not None:
        cargs.append("-axes")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    if params.get("largest"):
        cargs.append("-largest")
    if params.get("connectivity"):
        cargs.append("-connectivity")
    v_2 = params.get("npass")
    if v_2 is not None:
        cargs.append("-npass")
        cargs.append(str(v_2))
    v_3 = params.get("extent")
    if v_3 is not None:
        cargs.append("-extent")
        cargs.append(",".join([str(item1) for item1 in v_3]))
    v_4 = params.get("strides")
    if v_4 is not None:
        cargs.append("-strides")
        if v_4["@type"] == "VariousString":
            cargs.append(str(v_4["obj"]))
        elif v_4["@type"] == "VariousFile":
            cargs.append(str(v_4["obj"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item2 in v_6:
            cargs.append("-config")
            cargs.append(item2["key"])
            cargs.append(item2["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["filter"])
    cargs.append(params["output"])
    return cargs

def maskfilter_outputs(params: Maskfilter, execution: Execution) -> MaskfilterOutputs:
    """Build the MaskfilterOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MaskfilterOutputs(
        root=root_v,
        output=output_v,
    )

def maskfilter_execute(params: Maskfilter, runner: Runner | None = None) -> MaskfilterOutputs:
    """
    maskfilter

    Perform filtering operations on 3D / 4D mask images.

The available filters are: clean, connect, dilate, erode, median.

Each filter has its own unique set of optional parameters.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    maskfilter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MASKFILTER_METADATA)
    execution.params(params)
    args = maskfilter_cargs(params, execution)
    out = maskfilter_outputs(params, execution)
    execution.run(args)
    return out

def maskfilter(
    input_: InputPathType,
    filter: str,
    output: str,
    scale: int | None = None,
    axes: list[int] | None = None,
    largest: bool = False,
    connectivity: bool = False,
    npass: int | None = None,
    extent: list[int] | None = None,
    strides: MaskfilterStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MaskfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MaskfilterOutputs:
    """
    maskfilter

    Perform filtering operations on 3D / 4D mask images.

The available filters are: clean, connect, dilate, erode, median.

Each filter has its own unique set of optional parameters.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image.
        filter: the type of filter to be applied (clean, connect, dilate, erode,\
            median)
        output: the output image.
        scale: the maximum scale used to cut bridges. A certain maximum scale cuts\
            bridges up to a width (in voxels) of 2x the provided scale. (Default:\
            2)
        axes: specify which axes should be included in the connected components. By\
            default only the first 3 axes are included. The axes should be provided\
            as a comma-separated list of values.
        largest: only retain the largest connected component
        connectivity: use 26-voxel-neighbourhood connectivity (Default: 6)
        npass: the number of times to repeatedly apply the filter
        extent: specify the extent (width) of kernel size in voxels. This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the extent for each axis. The default is 3x3x3.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = maskfilter_params(
        input_=input_,
        filter=filter,
        output=output,
        scale=scale,
        axes=axes,
        largest=largest,
        connectivity=connectivity,
        npass=npass,
        extent=extent,
        strides=strides,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return maskfilter_execute(params, runner)

__all__ = [
    "Maskfilter",
    "MaskfilterOutputs",
    "MASKFILTER_METADATA",
    "maskfilter_cargs",
    "maskfilter_outputs",
    "maskfilter_params",
    "maskfilter_execute",
    "maskfilter_validate",
    "maskfilter",
]