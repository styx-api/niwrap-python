# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXELCONVERT_METADATA = Metadata(
    id="fixelconvert",
    name="fixelconvert",
    package="mrtrix",
)

class FixelconvertConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

FixelconvertVariousString = typing.TypedDict(
    "FixelconvertVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

FixelconvertVariousFile = typing.TypedDict(
    "FixelconvertVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

FixelconvertFixelIn = FixelconvertVariousString | FixelconvertVariousFile

Fixelconvert = typing.TypedDict(
    "Fixelconvert",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixelconvert"]],
        "name": typing.NotRequired[str],
        "nii": typing.NotRequired[bool],
        "out_size": typing.NotRequired[bool],
        "template": typing.NotRequired[InputPathType],
        "value": typing.NotRequired[InputPathType],
        "in_size": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[FixelconvertConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fixel_in": FixelconvertFixelIn,
        "fixel_out": FixelconvertFixelIn,
    },
)

@dataclasses.dataclass
class FixelconvertOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fixelconvert_params(
    fixel_in: FixelconvertFixelIn,
    fixel_out: FixelconvertFixelIn,
    name: str | None = None,
    nii: bool = False,
    out_size: bool = False,
    template: InputPathType | None = None,
    value: InputPathType | None = None,
    in_size: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixelconvert:
    """
    Build parameters.

    Args:
        fixel_in: the input fixel file / directory.
        fixel_out: the output fixel file / directory.
        name: assign a different name to the value field output (Default: value).\
            Do not include the file extension.
        nii: output the index, directions and data file in NIfTI format instead of\
            .mif
        out_size: also output the 'size' field from the old format
        template: specify an existing fixel directory (in the new format) to which\
            the new output should conform
        value: nominate the data file to import to the 'value' field in the old\
            format
        in_size: import data for the 'size' field in the old format
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixelconvert = {
        "@type": "mrtrix/fixelconvert",
        "fixel_in": fixel_in,
        "fixel_out": fixel_out,
        "nii": nii,
        "out_size": out_size,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if name is not None:
        params["name"] = name
    if template is not None:
        params["template"] = template
    if value is not None:
        params["value"] = value
    if in_size is not None:
        params["in_size"] = in_size
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixelconvert_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixelconvert."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("name", None) is not None:
        if not isinstance(params["name"], str):
            raise StyxValidationError(f'`name` has the wrong type: Received `{type(params["name"])}` expected `str`')
    if params.get("nii", None) is not None:
        if not isinstance(params["nii"], bool):
            raise StyxValidationError(f'`nii` has the wrong type: Received `{type(params["nii"])}` expected `bool`')
    if params.get("out_size", None) is not None:
        if not isinstance(params["out_size"], bool):
            raise StyxValidationError(f'`out_size` has the wrong type: Received `{type(params["out_size"])}` expected `bool`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("value", None) is not None:
        if not isinstance(params["value"], (pathlib.Path, str)):
            raise StyxValidationError(f'`value` has the wrong type: Received `{type(params["value"])}` expected `InputPathType`')
    if params.get("in_size", None) is not None:
        if not isinstance(params["in_size"], (pathlib.Path, str)):
            raise StyxValidationError(f'`in_size` has the wrong type: Received `{type(params["in_size"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[FixelconvertConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fixel_in", None) is None:
        raise StyxValidationError("`fixel_in` must not be None")
    if not isinstance(params["fixel_in"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["fixel_in"])}\'')
    if "@type" not in params["fixel_in"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["fixel_in"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `fixel_in`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["fixel_in"]["@type"] == "VariousString":
        if params["fixel_in"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["fixel_in"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["fixel_in"]["obj"])}` expected `str`')
    elif params["fixel_in"]["@type"] == "VariousFile":
        if params["fixel_in"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["fixel_in"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["fixel_in"]["obj"])}` expected `InputPathType`')
    if params.get("fixel_out", None) is None:
        raise StyxValidationError("`fixel_out` must not be None")
    if not isinstance(params["fixel_out"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["fixel_out"])}\'')
    if "@type" not in params["fixel_out"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["fixel_out"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `fixel_out`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["fixel_out"]["@type"] == "VariousString":
        if params["fixel_out"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["fixel_out"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["fixel_out"]["obj"])}` expected `str`')
    elif params["fixel_out"]["@type"] == "VariousFile":
        if params["fixel_out"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["fixel_out"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["fixel_out"]["obj"])}` expected `InputPathType`')

def fixelconvert_cargs(params: Fixelconvert, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixelconvert")
    v_0 = params.get("name")
    if v_0 is not None:
        cargs.append("-name")
        cargs.append(v_0)
    if params.get("nii"):
        cargs.append("-nii")
    if params.get("out_size"):
        cargs.append("-out_size")
    v_1 = params.get("template")
    if v_1 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("value")
    if v_2 is not None:
        cargs.append("-value")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("in_size")
    if v_3 is not None:
        cargs.append("-in_size")
        cargs.append(execution.input_file(v_3))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_4 = params.get("nthreads")
    if v_4 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_4))
    v_5 = params.get("config")
    if v_5 is not None:
        for item0 in v_5:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    if params["fixel_in"]["@type"] == "VariousString":
        cargs.append(str(params["fixel_in"]["obj"]))
    elif params["fixel_in"]["@type"] == "VariousFile":
        cargs.append(str(params["fixel_in"]["obj"]))
    if params["fixel_out"]["@type"] == "VariousString":
        cargs.append(str(params["fixel_out"]["obj"]))
    elif params["fixel_out"]["@type"] == "VariousFile":
        cargs.append(str(params["fixel_out"]["obj"]))
    return cargs

def fixelconvert_outputs(params: Fixelconvert, execution: Execution) -> FixelconvertOutputs:
    """Build the FixelconvertOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixelconvertOutputs(
        root=root_v,
    )

def fixelconvert_execute(params: Fixelconvert, runner: Runner | None = None) -> FixelconvertOutputs:
    """
    fixelconvert

    Convert between the old format fixel image (.msf / .msh) and the new fixel directory format.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixelconvert_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXELCONVERT_METADATA)
    execution.params(params)
    args = fixelconvert_cargs(params, execution)
    out = fixelconvert_outputs(params, execution)
    execution.run(args)
    return out

def fixelconvert(
    fixel_in: FixelconvertFixelIn,
    fixel_out: FixelconvertFixelIn,
    name: str | None = None,
    nii: bool = False,
    out_size: bool = False,
    template: InputPathType | None = None,
    value: InputPathType | None = None,
    in_size: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> FixelconvertOutputs:
    """
    fixelconvert

    Convert between the old format fixel image (.msf / .msh) and the new fixel directory format.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fixel_in: the input fixel file / directory.
        fixel_out: the output fixel file / directory.
        name: assign a different name to the value field output (Default: value).\
            Do not include the file extension.
        nii: output the index, directions and data file in NIfTI format instead of\
            .mif
        out_size: also output the 'size' field from the old format
        template: specify an existing fixel directory (in the new format) to which\
            the new output should conform
        value: nominate the data file to import to the 'value' field in the old\
            format
        in_size: import data for the 'size' field in the old format
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixelconvert_params(
        fixel_in=fixel_in,
        fixel_out=fixel_out,
        name=name,
        nii=nii,
        out_size=out_size,
        template=template,
        value=value,
        in_size=in_size,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixelconvert_execute(params, runner)

__all__ = [
    "Fixelconvert",
    "FixelconvertOutputs",
    "FIXELCONVERT_METADATA",
    "fixelconvert_cargs",
    "fixelconvert_outputs",
    "fixelconvert_params",
    "fixelconvert_execute",
    "fixelconvert_validate",
    "fixelconvert",
]