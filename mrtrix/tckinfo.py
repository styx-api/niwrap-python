# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKINFO_METADATA = Metadata(
    id="tckinfo",
    name="tckinfo",
    package="mrtrix",
)

class TckinfoConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckinfo = typing.TypedDict(
    "Tckinfo",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckinfo"]],
        "count": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckinfoConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks": list[InputPathType],
    },
)

@dataclasses.dataclass
class TckinfoOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tckinfo_params(
    tracks: list[InputPathType],
    count: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckinfoConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckinfo:
    """
    Build parameters.

    Args:
        tracks: the input track file.
        count: count number of tracks in file explicitly, ignoring the header
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckinfo = {
        "@type": "mrtrix/tckinfo",
        "tracks": tracks,
        "count": count,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckinfo_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckinfo."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("count", None) is not None:
        if not isinstance(params["count"], bool):
            raise StyxValidationError(f'`count` has the wrong type: Received `{type(params["count"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckinfoConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], list):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `list[InputPathType]`')
    for e_2 in params["tracks"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')

def tckinfo_cargs(params: Tckinfo, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckinfo")
    if params.get("count"):
        cargs.append("-count")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_0 = params.get("nthreads")
    if v_0 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_0))
    v_1 = params.get("config")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item1 in params["tracks"]:
        cargs.append(execution.input_file(item1))
    return cargs

def tckinfo_outputs(params: Tckinfo, execution: Execution) -> TckinfoOutputs:
    """Build the TckinfoOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TckinfoOutputs(
        root=root_v,
    )

def tckinfo_execute(params: Tckinfo, runner: Runner | None = None) -> TckinfoOutputs:
    """
    tckinfo

    Print out information about a track file.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckinfo_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKINFO_METADATA)
    execution.params(params)
    args = tckinfo_cargs(params, execution)
    out = tckinfo_outputs(params, execution)
    execution.run(args)
    return out

def tckinfo(
    tracks: list[InputPathType],
    count: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckinfoConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckinfoOutputs:
    """
    tckinfo

    Print out information about a track file.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks: the input track file.
        count: count number of tracks in file explicitly, ignoring the header
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckinfo_params(
        tracks=tracks,
        count=count,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckinfo_execute(params, runner)

__all__ = [
    "Tckinfo",
    "TckinfoOutputs",
    "TCKINFO_METADATA",
    "tckinfo_cargs",
    "tckinfo_outputs",
    "tckinfo_params",
    "tckinfo_execute",
    "tckinfo_validate",
    "tckinfo",
]