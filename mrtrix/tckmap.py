# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKMAP_METADATA = Metadata(
    id="tckmap",
    name="tckmap",
    package="mrtrix",
)

TckmapVariousString = typing.TypedDict(
    "TckmapVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

TckmapVariousFile = typing.TypedDict(
    "TckmapVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

TckmapDixel = TckmapVariousString | TckmapVariousFile

class TckmapConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckmap = typing.TypedDict(
    "Tckmap",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckmap"]],
        "template": typing.NotRequired[InputPathType],
        "vox": typing.NotRequired[list[float]],
        "datatype": typing.NotRequired[str],
        "dec": typing.NotRequired[bool],
        "dixel": typing.NotRequired[TckmapDixel],
        "tod": typing.NotRequired[int],
        "contrast": typing.NotRequired[str],
        "image": typing.NotRequired[InputPathType],
        "vector_file": typing.NotRequired[InputPathType],
        "stat_vox": typing.NotRequired[str],
        "stat_tck": typing.NotRequired[str],
        "fwhm_tck": typing.NotRequired[float],
        "map_zero": typing.NotRequired[bool],
        "backtrack": typing.NotRequired[bool],
        "upsample": typing.NotRequired[int],
        "precise": typing.NotRequired[bool],
        "ends_only": typing.NotRequired[bool],
        "tck_weights_in": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckmapConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class TckmapOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output track-weighted image"""

def tckmap_params(
    tracks: InputPathType,
    output: str,
    template: InputPathType | None = None,
    vox: list[float] | None = None,
    datatype: str | None = None,
    dec: bool = False,
    dixel: TckmapDixel | None = None,
    tod: int | None = None,
    contrast: str | None = None,
    image: InputPathType | None = None,
    vector_file: InputPathType | None = None,
    stat_vox: str | None = None,
    stat_tck: str | None = None,
    fwhm_tck: float | None = None,
    map_zero: bool = False,
    backtrack: bool = False,
    upsample: int | None = None,
    precise: bool = False,
    ends_only: bool = False,
    tck_weights_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckmapConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckmap:
    """
    Build parameters.

    Args:
        tracks: the input track file.
        output: the output track-weighted image
        template: an image file to be used as a template for the output (the output\
            image will have the same transform and field of view).
        vox: provide either an isotropic voxel size (in mm), or comma-separated\
            list of 3 voxel dimensions.
        datatype: specify output image data type.
        dec: perform track mapping in directionally-encoded colour (DEC) space
        dixel: map streamlines to dixels within each voxel; requires either a\
            number of dixels (references an internal direction set), or a path to a\
            text file containing a set of directions stored as azimuth/elevation\
            pairs
        tod: generate a Track Orientation Distribution (TOD) in each voxel; need to\
            specify the maximum spherical harmonic degree lmax to use when\
            generating Apodised Point Spread Functions
        contrast: define the desired form of contrast for the output image Options\
            are: tdi, length, invlength, scalar_map, scalar_map_count, fod_amp,\
            curvature, vector_file (default: tdi)
        image: provide the scalar image map for generating images with 'scalar_map'\
            / 'scalar_map_count' contrast, or the spherical harmonics image for\
            'fod_amp' contrast
        vector_file: provide the vector data file for generating images with\
            'vector_file' contrast
        stat_vox: define the statistic for choosing the final voxel intensities for\
            a given contrast type given the individual values from the tracks\
            passing through each voxel. Options are: sum, min, mean, max (default:\
            sum)
        stat_tck: define the statistic for choosing the contribution to be made by\
            each streamline as a function of the samples taken along their lengths.\
            Only has an effect for 'scalar_map', 'fod_amp' and 'curvature' contrast\
            types. Options are: sum, min, mean, max, median, mean_nonzero,\
            gaussian, ends_min, ends_mean, ends_max, ends_prod (default: mean)
        fwhm_tck: when using gaussian-smoothed per-track statistic, specify the\
            desired full-width half-maximum of the Gaussian smoothing kernel (in\
            mm)
        map_zero: if a streamline has zero contribution based on the contrast &\
            statistic, typically it is not mapped; use this option to still\
            contribute to the map even if this is the case (these non-contributing\
            voxels can then influence the mean value in each voxel of the map)
        backtrack: when using -stat_tck ends_*, if the streamline endpoint is\
            outside the FoV, backtrack along the streamline trajectory until an\
            appropriate point is found
        upsample: upsample the tracks by some ratio using Hermite interpolation\
            before mappping (If omitted, an appropriate ratio will be determined\
            automatically)
        precise: use a more precise streamline mapping strategy, that accurately\
            quantifies the length through each voxel (these lengths are then taken\
            into account during TWI calculation)
        ends_only: only map the streamline endpoints to the image
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckmap = {
        "@type": "mrtrix/tckmap",
        "tracks": tracks,
        "output": output,
        "dec": dec,
        "map_zero": map_zero,
        "backtrack": backtrack,
        "precise": precise,
        "ends_only": ends_only,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if template is not None:
        params["template"] = template
    if vox is not None:
        params["vox"] = vox
    if datatype is not None:
        params["datatype"] = datatype
    if dixel is not None:
        params["dixel"] = dixel
    if tod is not None:
        params["tod"] = tod
    if contrast is not None:
        params["contrast"] = contrast
    if image is not None:
        params["image"] = image
    if vector_file is not None:
        params["vector_file"] = vector_file
    if stat_vox is not None:
        params["stat_vox"] = stat_vox
    if stat_tck is not None:
        params["stat_tck"] = stat_tck
    if fwhm_tck is not None:
        params["fwhm_tck"] = fwhm_tck
    if upsample is not None:
        params["upsample"] = upsample
    if tck_weights_in is not None:
        params["tck_weights_in"] = tck_weights_in
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckmap_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckmap."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("vox", None) is not None:
        if not isinstance(params["vox"], list):
            raise StyxValidationError(f'`vox` has the wrong type: Received `{type(params["vox"])}` expected `list[float]`')
        for e in params["vox"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`vox` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("dec", None) is not None:
        if not isinstance(params["dec"], bool):
            raise StyxValidationError(f'`dec` has the wrong type: Received `{type(params["dec"])}` expected `bool`')
    if params.get("dixel", None) is not None:
        if not isinstance(params["dixel"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["dixel"])}\'')
        if "@type" not in params["dixel"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["dixel"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `dixel`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["dixel"]["@type"] == "VariousString":
            if params["dixel"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["dixel"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["dixel"]["obj"])}` expected `str`')
        elif params["dixel"]["@type"] == "VariousFile":
            if params["dixel"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["dixel"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["dixel"]["obj"])}` expected `InputPathType`')
    if params.get("tod", None) is not None:
        if not isinstance(params["tod"], int):
            raise StyxValidationError(f'`tod` has the wrong type: Received `{type(params["tod"])}` expected `int`')
    if params.get("contrast", None) is not None:
        if not isinstance(params["contrast"], str):
            raise StyxValidationError(f'`contrast` has the wrong type: Received `{type(params["contrast"])}` expected `str`')
    if params.get("image", None) is not None:
        if not isinstance(params["image"], (pathlib.Path, str)):
            raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("vector_file", None) is not None:
        if not isinstance(params["vector_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`vector_file` has the wrong type: Received `{type(params["vector_file"])}` expected `InputPathType`')
    if params.get("stat_vox", None) is not None:
        if not isinstance(params["stat_vox"], str):
            raise StyxValidationError(f'`stat_vox` has the wrong type: Received `{type(params["stat_vox"])}` expected `str`')
    if params.get("stat_tck", None) is not None:
        if not isinstance(params["stat_tck"], str):
            raise StyxValidationError(f'`stat_tck` has the wrong type: Received `{type(params["stat_tck"])}` expected `str`')
    if params.get("fwhm_tck", None) is not None:
        if not isinstance(params["fwhm_tck"], (float, int)):
            raise StyxValidationError(f'`fwhm_tck` has the wrong type: Received `{type(params["fwhm_tck"])}` expected `float`')
    if params.get("map_zero", None) is not None:
        if not isinstance(params["map_zero"], bool):
            raise StyxValidationError(f'`map_zero` has the wrong type: Received `{type(params["map_zero"])}` expected `bool`')
    if params.get("backtrack", None) is not None:
        if not isinstance(params["backtrack"], bool):
            raise StyxValidationError(f'`backtrack` has the wrong type: Received `{type(params["backtrack"])}` expected `bool`')
    if params.get("upsample", None) is not None:
        if not isinstance(params["upsample"], int):
            raise StyxValidationError(f'`upsample` has the wrong type: Received `{type(params["upsample"])}` expected `int`')
    if params.get("precise", None) is not None:
        if not isinstance(params["precise"], bool):
            raise StyxValidationError(f'`precise` has the wrong type: Received `{type(params["precise"])}` expected `bool`')
    if params.get("ends_only", None) is not None:
        if not isinstance(params["ends_only"], bool):
            raise StyxValidationError(f'`ends_only` has the wrong type: Received `{type(params["ends_only"])}` expected `bool`')
    if params.get("tck_weights_in", None) is not None:
        if not isinstance(params["tck_weights_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tck_weights_in` has the wrong type: Received `{type(params["tck_weights_in"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckmapConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def tckmap_cargs(params: Tckmap, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckmap")
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("vox")
    if v_1 is not None:
        cargs.append("-vox")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    v_2 = params.get("datatype")
    if v_2 is not None:
        cargs.append("-datatype")
        cargs.append(v_2)
    if params.get("dec"):
        cargs.append("-dec")
    v_3 = params.get("dixel")
    if v_3 is not None:
        cargs.append("-dixel")
        if v_3["@type"] == "VariousString":
            cargs.append(str(v_3["obj"]))
        elif v_3["@type"] == "VariousFile":
            cargs.append(str(v_3["obj"]))
    v_4 = params.get("tod")
    if v_4 is not None:
        cargs.append("-tod")
        cargs.append(str(v_4))
    v_5 = params.get("contrast")
    if v_5 is not None:
        cargs.append("-contrast")
        cargs.append(v_5)
    v_6 = params.get("image")
    if v_6 is not None:
        cargs.append("-image")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("vector_file")
    if v_7 is not None:
        cargs.append("-vector_file")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("stat_vox")
    if v_8 is not None:
        cargs.append("-stat_vox")
        cargs.append(v_8)
    v_9 = params.get("stat_tck")
    if v_9 is not None:
        cargs.append("-stat_tck")
        cargs.append(v_9)
    v_10 = params.get("fwhm_tck")
    if v_10 is not None:
        cargs.append("-fwhm_tck")
        cargs.append(str(v_10))
    if params.get("map_zero"):
        cargs.append("-map_zero")
    if params.get("backtrack"):
        cargs.append("-backtrack")
    v_11 = params.get("upsample")
    if v_11 is not None:
        cargs.append("-upsample")
        cargs.append(str(v_11))
    if params.get("precise"):
        cargs.append("-precise")
    if params.get("ends_only"):
        cargs.append("-ends_only")
    v_12 = params.get("tck_weights_in")
    if v_12 is not None:
        cargs.append("-tck_weights_in")
        cargs.append(execution.input_file(v_12))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_13 = params.get("nthreads")
    if v_13 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_13))
    v_14 = params.get("config")
    if v_14 is not None:
        for item1 in v_14:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(params["output"])
    return cargs

def tckmap_outputs(params: Tckmap, execution: Execution) -> TckmapOutputs:
    """Build the TckmapOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return TckmapOutputs(
        root=root_v,
        output=output_v,
    )

def tckmap_execute(params: Tckmap, runner: Runner | None = None) -> TckmapOutputs:
    """
    tckmap

    Use track data as a form of contrast for producing a high-resolution image.

Note: if you run into limitations with RAM usage, make sure you output the results to a .mif file or .mih / .dat file pair - this will avoid the allocation of an additional buffer to store the output for write-out.

References:

* For TDI or DEC TDI:
Calamante, F.; Tournier, J.-D.; Jackson, G. D. & Connelly, A. Track-density imaging (TDI): Super-resolution white matter imaging using whole-brain track-density mapping. NeuroImage, 2010, 53, 1233-1243

* If using -contrast length and -stat_vox mean:
Pannek, K.; Mathias, J. L.; Bigler, E. D.; Brown, G.; Taylor, J. D. & Rose, S. E. The average pathlength map: A diffusion MRI tractography-derived index for studying brain pathology. NeuroImage, 2011, 55, 133-141

* If using -dixel option with TDI contrast only:
Smith, R.E., Tournier, J-D., Calamante, F., Connelly, A. A novel paradigm for automated segmentation of very large whole-brain probabilistic tractography data sets. In proc. ISMRM, 2011, 19, 673

* If using -dixel option with any other contrast:
Pannek, K., Raffelt, D., Salvado, O., Rose, S. Incorporating directional information in diffusion tractography derived maps: angular track imaging (ATI). In Proc. ISMRM, 2012, 20, 1912

* If using -tod option:
Dhollander, T., Emsell, L., Van Hecke, W., Maes, F., Sunaert, S., Suetens, P. Track Orientation Density Imaging (TODI) and Track Orientation Distribution (TOD) based tractography. NeuroImage, 2014, 94, 312-336

* If using other contrasts / statistics:
Calamante, F.; Tournier, J.-D.; Smith, R. E. & Connelly, A. A generalised framework for super-resolution track-weighted imaging. NeuroImage, 2012, 59, 2494-2503

* If using -precise mapping option:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312 (Appendix 3)

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckmap_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKMAP_METADATA)
    execution.params(params)
    args = tckmap_cargs(params, execution)
    out = tckmap_outputs(params, execution)
    execution.run(args)
    return out

def tckmap(
    tracks: InputPathType,
    output: str,
    template: InputPathType | None = None,
    vox: list[float] | None = None,
    datatype: str | None = None,
    dec: bool = False,
    dixel: TckmapDixel | None = None,
    tod: int | None = None,
    contrast: str | None = None,
    image: InputPathType | None = None,
    vector_file: InputPathType | None = None,
    stat_vox: str | None = None,
    stat_tck: str | None = None,
    fwhm_tck: float | None = None,
    map_zero: bool = False,
    backtrack: bool = False,
    upsample: int | None = None,
    precise: bool = False,
    ends_only: bool = False,
    tck_weights_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckmapConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckmapOutputs:
    """
    tckmap

    Use track data as a form of contrast for producing a high-resolution image.

Note: if you run into limitations with RAM usage, make sure you output the results to a .mif file or .mih / .dat file pair - this will avoid the allocation of an additional buffer to store the output for write-out.

References:

* For TDI or DEC TDI:
Calamante, F.; Tournier, J.-D.; Jackson, G. D. & Connelly, A. Track-density imaging (TDI): Super-resolution white matter imaging using whole-brain track-density mapping. NeuroImage, 2010, 53, 1233-1243

* If using -contrast length and -stat_vox mean:
Pannek, K.; Mathias, J. L.; Bigler, E. D.; Brown, G.; Taylor, J. D. & Rose, S. E. The average pathlength map: A diffusion MRI tractography-derived index for studying brain pathology. NeuroImage, 2011, 55, 133-141

* If using -dixel option with TDI contrast only:
Smith, R.E., Tournier, J-D., Calamante, F., Connelly, A. A novel paradigm for automated segmentation of very large whole-brain probabilistic tractography data sets. In proc. ISMRM, 2011, 19, 673

* If using -dixel option with any other contrast:
Pannek, K., Raffelt, D., Salvado, O., Rose, S. Incorporating directional information in diffusion tractography derived maps: angular track imaging (ATI). In Proc. ISMRM, 2012, 20, 1912

* If using -tod option:
Dhollander, T., Emsell, L., Van Hecke, W., Maes, F., Sunaert, S., Suetens, P. Track Orientation Density Imaging (TODI) and Track Orientation Distribution (TOD) based tractography. NeuroImage, 2014, 94, 312-336

* If using other contrasts / statistics:
Calamante, F.; Tournier, J.-D.; Smith, R. E. & Connelly, A. A generalised framework for super-resolution track-weighted imaging. NeuroImage, 2012, 59, 2494-2503

* If using -precise mapping option:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT: Spherical-deconvolution informed filtering of tractograms. NeuroImage, 2013, 67, 298-312 (Appendix 3)

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks: the input track file.
        output: the output track-weighted image
        template: an image file to be used as a template for the output (the output\
            image will have the same transform and field of view).
        vox: provide either an isotropic voxel size (in mm), or comma-separated\
            list of 3 voxel dimensions.
        datatype: specify output image data type.
        dec: perform track mapping in directionally-encoded colour (DEC) space
        dixel: map streamlines to dixels within each voxel; requires either a\
            number of dixels (references an internal direction set), or a path to a\
            text file containing a set of directions stored as azimuth/elevation\
            pairs
        tod: generate a Track Orientation Distribution (TOD) in each voxel; need to\
            specify the maximum spherical harmonic degree lmax to use when\
            generating Apodised Point Spread Functions
        contrast: define the desired form of contrast for the output image Options\
            are: tdi, length, invlength, scalar_map, scalar_map_count, fod_amp,\
            curvature, vector_file (default: tdi)
        image: provide the scalar image map for generating images with 'scalar_map'\
            / 'scalar_map_count' contrast, or the spherical harmonics image for\
            'fod_amp' contrast
        vector_file: provide the vector data file for generating images with\
            'vector_file' contrast
        stat_vox: define the statistic for choosing the final voxel intensities for\
            a given contrast type given the individual values from the tracks\
            passing through each voxel. Options are: sum, min, mean, max (default:\
            sum)
        stat_tck: define the statistic for choosing the contribution to be made by\
            each streamline as a function of the samples taken along their lengths.\
            Only has an effect for 'scalar_map', 'fod_amp' and 'curvature' contrast\
            types. Options are: sum, min, mean, max, median, mean_nonzero,\
            gaussian, ends_min, ends_mean, ends_max, ends_prod (default: mean)
        fwhm_tck: when using gaussian-smoothed per-track statistic, specify the\
            desired full-width half-maximum of the Gaussian smoothing kernel (in\
            mm)
        map_zero: if a streamline has zero contribution based on the contrast &\
            statistic, typically it is not mapped; use this option to still\
            contribute to the map even if this is the case (these non-contributing\
            voxels can then influence the mean value in each voxel of the map)
        backtrack: when using -stat_tck ends_*, if the streamline endpoint is\
            outside the FoV, backtrack along the streamline trajectory until an\
            appropriate point is found
        upsample: upsample the tracks by some ratio using Hermite interpolation\
            before mappping (If omitted, an appropriate ratio will be determined\
            automatically)
        precise: use a more precise streamline mapping strategy, that accurately\
            quantifies the length through each voxel (these lengths are then taken\
            into account during TWI calculation)
        ends_only: only map the streamline endpoints to the image
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckmap_params(
        tracks=tracks,
        output=output,
        template=template,
        vox=vox,
        datatype=datatype,
        dec=dec,
        dixel=dixel,
        tod=tod,
        contrast=contrast,
        image=image,
        vector_file=vector_file,
        stat_vox=stat_vox,
        stat_tck=stat_tck,
        fwhm_tck=fwhm_tck,
        map_zero=map_zero,
        backtrack=backtrack,
        upsample=upsample,
        precise=precise,
        ends_only=ends_only,
        tck_weights_in=tck_weights_in,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckmap_execute(params, runner)

__all__ = [
    "Tckmap",
    "TckmapOutputs",
    "TCKMAP_METADATA",
    "tckmap_cargs",
    "tckmap_outputs",
    "tckmap_params",
    "tckmap_execute",
    "tckmap_validate",
    "tckmap",
]