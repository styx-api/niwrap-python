# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXELCROP_METADATA = Metadata(
    id="fixelcrop",
    name="fixelcrop",
    package="mrtrix",
)

class FixelcropConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixelcrop = typing.TypedDict(
    "Fixelcrop",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixelcrop"]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[FixelcropConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input_fixel_directory": InputPathType,
        "input_fixel_mask": InputPathType,
        "output_fixel_directory": str,
    },
)

@dataclasses.dataclass
class FixelcropOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output_fixel_directory: OutputPathType
    """the output directory to store the cropped directions and data files"""

def fixelcrop_params(
    input_fixel_directory: InputPathType,
    input_fixel_mask: InputPathType,
    output_fixel_directory: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcropConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixelcrop:
    """
    Build parameters.

    Args:
        input_fixel_directory: input fixel directory, all data files and directions\
            file will be cropped and saved in the output fixel directory
        input_fixel_mask: the input fixel data file defining which fixels to crop.\
            Fixels with zero values will be removed
        output_fixel_directory: the output directory to store the cropped\
            directions and data files
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixelcrop = {
        "@type": "mrtrix/fixelcrop",
        "input_fixel_directory": input_fixel_directory,
        "input_fixel_mask": input_fixel_mask,
        "output_fixel_directory": output_fixel_directory,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixelcrop_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixelcrop."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[FixelcropConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input_fixel_directory", None) is None:
        raise StyxValidationError("`input_fixel_directory` must not be None")
    if not isinstance(params["input_fixel_directory"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_fixel_directory` has the wrong type: Received `{type(params["input_fixel_directory"])}` expected `InputPathType`')
    if params.get("input_fixel_mask", None) is None:
        raise StyxValidationError("`input_fixel_mask` must not be None")
    if not isinstance(params["input_fixel_mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_fixel_mask` has the wrong type: Received `{type(params["input_fixel_mask"])}` expected `InputPathType`')
    if params.get("output_fixel_directory", None) is None:
        raise StyxValidationError("`output_fixel_directory` must not be None")
    if not isinstance(params["output_fixel_directory"], str):
        raise StyxValidationError(f'`output_fixel_directory` has the wrong type: Received `{type(params["output_fixel_directory"])}` expected `str`')

def fixelcrop_cargs(params: Fixelcrop, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixelcrop")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_0 = params.get("nthreads")
    if v_0 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_0))
    v_1 = params.get("config")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input_fixel_directory"]))
    cargs.append(execution.input_file(params["input_fixel_mask"]))
    cargs.append(params["output_fixel_directory"])
    return cargs

def fixelcrop_outputs(params: Fixelcrop, execution: Execution) -> FixelcropOutputs:
    """Build the FixelcropOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_fixel_directory_v: OutputPathType = execution.output_file(params["output_fixel_directory"])
    return FixelcropOutputs(
        root=root_v,
        output_fixel_directory=output_fixel_directory_v,
    )

def fixelcrop_execute(params: Fixelcrop, runner: Runner | None = None) -> FixelcropOutputs:
    """
    fixelcrop

    Crop/remove fixels from sparse fixel image using a binary fixel mask.

The mask must be input as a fixel data file the same dimensions as the fixel data file(s) to be cropped.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixelcrop_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXELCROP_METADATA)
    execution.params(params)
    args = fixelcrop_cargs(params, execution)
    out = fixelcrop_outputs(params, execution)
    execution.run(args)
    return out

def fixelcrop(
    input_fixel_directory: InputPathType,
    input_fixel_mask: InputPathType,
    output_fixel_directory: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcropConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> FixelcropOutputs:
    """
    fixelcrop

    Crop/remove fixels from sparse fixel image using a binary fixel mask.

The mask must be input as a fixel data file the same dimensions as the fixel data file(s) to be cropped.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_fixel_directory: input fixel directory, all data files and directions\
            file will be cropped and saved in the output fixel directory
        input_fixel_mask: the input fixel data file defining which fixels to crop.\
            Fixels with zero values will be removed
        output_fixel_directory: the output directory to store the cropped\
            directions and data files
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixelcrop_params(
        input_fixel_directory=input_fixel_directory,
        input_fixel_mask=input_fixel_mask,
        output_fixel_directory=output_fixel_directory,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixelcrop_execute(params, runner)

__all__ = [
    "Fixelcrop",
    "FixelcropOutputs",
    "FIXELCROP_METADATA",
    "fixelcrop_cargs",
    "fixelcrop_outputs",
    "fixelcrop_params",
    "fixelcrop_execute",
    "fixelcrop_validate",
    "fixelcrop",
]