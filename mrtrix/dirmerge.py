# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DIRMERGE_METADATA = Metadata(
    id="dirmerge",
    name="dirmerge",
    package="mrtrix",
)

class DirmergeConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dirmerge = typing.TypedDict(
    "Dirmerge",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dirmerge"]],
        "unipolar_weight": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[DirmergeConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "subsets": int,
        "bvalue_files": list[str],
        "out": str,
    },
)

@dataclasses.dataclass
class DirmergeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output directions file, with each row listing the X Y Z gradient directions, the b-value, and an index representing the phase encode direction"""

def dirmerge_params(
    subsets: int,
    bvalue_files: list[str],
    out: str,
    unipolar_weight: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DirmergeConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dirmerge:
    """
    Build parameters.

    Args:
        subsets: the number of subsets (eg. phase encoding directions) per b-value
        bvalue_files: the b-value and sets of corresponding files, in order
        out: the output directions file, with each row listing the X Y Z gradient\
            directions, the b-value, and an index representing the phase encode\
            direction
        unipolar_weight: set the weight given to the unipolar electrostatic\
            repulsion model compared to the bipolar model (default: 0.2).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dirmerge = {
        "@type": "mrtrix/dirmerge",
        "subsets": subsets,
        "bvalue_files": bvalue_files,
        "out": out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if unipolar_weight is not None:
        params["unipolar_weight"] = unipolar_weight
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dirmerge_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dirmerge."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("unipolar_weight", None) is not None:
        if not isinstance(params["unipolar_weight"], (float, int)):
            raise StyxValidationError(f'`unipolar_weight` has the wrong type: Received `{type(params["unipolar_weight"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DirmergeConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("subsets", None) is None:
        raise StyxValidationError("`subsets` must not be None")
    if not isinstance(params["subsets"], int):
        raise StyxValidationError(f'`subsets` has the wrong type: Received `{type(params["subsets"])}` expected `int`')
    if params.get("bvalue_files", None) is None:
        raise StyxValidationError("`bvalue_files` must not be None")
    if not isinstance(params["bvalue_files"], list):
        raise StyxValidationError(f'`bvalue_files` has the wrong type: Received `{type(params["bvalue_files"])}` expected `list[str]`')
    for e_2 in params["bvalue_files"]:
        if not isinstance(e_2, str):
            raise StyxValidationError(f'`bvalue_files` has the wrong type: Received `{type(e_2)}` expected `list[str]`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def dirmerge_cargs(params: Dirmerge, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dirmerge")
    v_0 = params.get("unipolar_weight")
    if v_0 is not None:
        cargs.append("-unipolar_weight")
        cargs.append(str(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(str(params["subsets"]))
    for item1 in params["bvalue_files"]:
        cargs.append(item1)
    cargs.append(params["out"])
    return cargs

def dirmerge_outputs(params: Dirmerge, execution: Execution) -> DirmergeOutputs:
    """Build the DirmergeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    return DirmergeOutputs(
        root=root_v,
        out=out_v,
    )

def dirmerge_execute(params: Dirmerge, runner: Runner | None = None) -> DirmergeOutputs:
    """
    dirmerge

    Splice / merge multiple sets of directions in such a way as to maintain near-optimality upon truncation.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dirmerge_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DIRMERGE_METADATA)
    execution.params(params)
    args = dirmerge_cargs(params, execution)
    out = dirmerge_outputs(params, execution)
    execution.run(args)
    return out

def dirmerge(
    subsets: int,
    bvalue_files: list[str],
    out: str,
    unipolar_weight: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DirmergeConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DirmergeOutputs:
    """
    dirmerge

    Splice / merge multiple sets of directions in such a way as to maintain near-optimality upon truncation.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        subsets: the number of subsets (eg. phase encoding directions) per b-value
        bvalue_files: the b-value and sets of corresponding files, in order
        out: the output directions file, with each row listing the X Y Z gradient\
            directions, the b-value, and an index representing the phase encode\
            direction
        unipolar_weight: set the weight given to the unipolar electrostatic\
            repulsion model compared to the bipolar model (default: 0.2).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dirmerge_params(
        subsets=subsets,
        bvalue_files=bvalue_files,
        out=out,
        unipolar_weight=unipolar_weight,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dirmerge_execute(params, runner)

__all__ = [
    "Dirmerge",
    "DirmergeOutputs",
    "DIRMERGE_METADATA",
    "dirmerge_cargs",
    "dirmerge_outputs",
    "dirmerge_params",
    "dirmerge_execute",
    "dirmerge_validate",
    "dirmerge",
]