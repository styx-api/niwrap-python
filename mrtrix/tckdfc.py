# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKDFC_METADATA = Metadata(
    id="tckdfc",
    name="tckdfc",
    package="mrtrix",
)

class TckdfcDynamic(typing.TypedDict):
    shape: str
    """
    generate a "dynamic" (4D) output image; must additionally provide the shape and width (in volumes) of the sliding window.
    """
    width: int
    """
    generate a "dynamic" (4D) output image; must additionally provide the shape and width (in volumes) of the sliding window.
    """

class TckdfcConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckdfc = typing.TypedDict(
    "Tckdfc",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckdfc"]],
        "static": typing.NotRequired[bool],
        "dynamic": typing.NotRequired[TckdfcDynamic],
        "template": typing.NotRequired[InputPathType],
        "vox": typing.NotRequired[list[float]],
        "stat_vox": typing.NotRequired[str],
        "backtrack": typing.NotRequired[bool],
        "upsample": typing.NotRequired[int],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckdfcConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks": InputPathType,
        "fmri": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class TckdfcOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output TW-dFC image"""

def tckdfc_params(
    tracks: InputPathType,
    fmri: InputPathType,
    output: str,
    static: bool = False,
    dynamic: TckdfcDynamic | None = None,
    template: InputPathType | None = None,
    vox: list[float] | None = None,
    stat_vox: str | None = None,
    backtrack: bool = False,
    upsample: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckdfcConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckdfc:
    """
    Build parameters.

    Args:
        tracks: the input track file.
        fmri: the pre-processed fMRI time series
        output: the output TW-dFC image
        static: generate a "static" (3D) output image.
        dynamic: generate a "dynamic" (4D) output image; must additionally provide\
            the shape and width (in volumes) of the sliding window.
        template: an image file to be used as a template for the output (the output\
            image will have the same transform and field of view).
        vox: provide either an isotropic voxel size (in mm), or comma-separated\
            list of 3 voxel dimensions.
        stat_vox: define the statistic for choosing the final voxel intensities for\
            a given contrast type given the individual values from the tracks\
            passing through each voxel Options are: sum, min, mean, max (default:\
            mean)
        backtrack: if no valid timeseries is found at the streamline endpoint,\
            back-track along the streamline trajectory until a valid timeseries is\
            found
        upsample: upsample the tracks by some ratio using Hermite interpolation\
            before mapping (if omitted, an appropriate ratio will be determined\
            automatically)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckdfc = {
        "@type": "mrtrix/tckdfc",
        "tracks": tracks,
        "fmri": fmri,
        "output": output,
        "static": static,
        "backtrack": backtrack,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if dynamic is not None:
        params["dynamic"] = dynamic
    if template is not None:
        params["template"] = template
    if vox is not None:
        params["vox"] = vox
    if stat_vox is not None:
        params["stat_vox"] = stat_vox
    if upsample is not None:
        params["upsample"] = upsample
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckdfc_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckdfc."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("static", None) is not None:
        if not isinstance(params["static"], bool):
            raise StyxValidationError(f'`static` has the wrong type: Received `{type(params["static"])}` expected `bool`')
    if params.get("dynamic", None) is not None:
        if not isinstance(params["dynamic"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["dynamic"])}\'')
        if params["dynamic"].get("shape", None) is None:
            raise StyxValidationError("`shape` must not be None")
        if not isinstance(params["dynamic"]["shape"], str):
            raise StyxValidationError(f'`shape` has the wrong type: Received `{type(params["dynamic"]["shape"])}` expected `str`')
        if params["dynamic"].get("width", None) is None:
            raise StyxValidationError("`width` must not be None")
        if not isinstance(params["dynamic"]["width"], int):
            raise StyxValidationError(f'`width` has the wrong type: Received `{type(params["dynamic"]["width"])}` expected `int`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("vox", None) is not None:
        if not isinstance(params["vox"], list):
            raise StyxValidationError(f'`vox` has the wrong type: Received `{type(params["vox"])}` expected `list[float]`')
        for e in params["vox"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`vox` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("stat_vox", None) is not None:
        if not isinstance(params["stat_vox"], str):
            raise StyxValidationError(f'`stat_vox` has the wrong type: Received `{type(params["stat_vox"])}` expected `str`')
    if params.get("backtrack", None) is not None:
        if not isinstance(params["backtrack"], bool):
            raise StyxValidationError(f'`backtrack` has the wrong type: Received `{type(params["backtrack"])}` expected `bool`')
    if params.get("upsample", None) is not None:
        if not isinstance(params["upsample"], int):
            raise StyxValidationError(f'`upsample` has the wrong type: Received `{type(params["upsample"])}` expected `int`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckdfcConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("fmri", None) is None:
        raise StyxValidationError("`fmri` must not be None")
    if not isinstance(params["fmri"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fmri` has the wrong type: Received `{type(params["fmri"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def tckdfc_cargs(params: Tckdfc, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckdfc")
    if params.get("static"):
        cargs.append("-static")
    v_0 = params.get("dynamic")
    if v_0 is not None:
        cargs.append("-dynamic")
        cargs.append(v_0["shape"])
        cargs.append(str(v_0["width"]))
    v_1 = params.get("template")
    if v_1 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("vox")
    if v_2 is not None:
        cargs.append("-vox")
        cargs.append(",".join([str(item0) for item0 in v_2]))
    v_3 = params.get("stat_vox")
    if v_3 is not None:
        cargs.append("-stat_vox")
        cargs.append(v_3)
    if params.get("backtrack"):
        cargs.append("-backtrack")
    v_4 = params.get("upsample")
    if v_4 is not None:
        cargs.append("-upsample")
        cargs.append(str(v_4))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item1 in v_6:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(execution.input_file(params["fmri"]))
    cargs.append(params["output"])
    return cargs

def tckdfc_outputs(params: Tckdfc, execution: Execution) -> TckdfcOutputs:
    """Build the TckdfcOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return TckdfcOutputs(
        root=root_v,
        output=output_v,
    )

def tckdfc_execute(params: Tckdfc, runner: Runner | None = None) -> TckdfcOutputs:
    """
    tckdfc

    Perform the Track-Weighted Dynamic Functional Connectivity (TW-dFC) method.

This command generates a Track-Weighted Image (TWI), where the contribution from each streamline to the image is the Pearson correlation between the fMRI time series at the streamline endpoints.

The output image can be generated in one of two ways (note that one of these two command-line options MUST be provided): 

- "Static" functional connectivity (-static option): Each streamline contributes to a static 3D output image based on the correlation between the signals at the streamline endpoints using the entirety of the input time series.

- "Dynamic" functional connectivity (-dynamic option): The output image is a 4D image, with the same number of volumes as the input fMRI time series. For each volume, the contribution from each streamline is calculated based on a finite-width sliding time window, centred at the timepoint corresponding to that volume.

Note that the -backtrack option in this command is similar, but not precisely equivalent, to back-tracking as can be used with Anatomically-Constrained Tractography (ACT) in the tckgen command. However, here the feature does not change the streamlines trajectories in any way; it simply enables detection of the fact that the input fMRI image may not contain a valid timeseries underneath the streamline endpoint, and where this occurs, searches from the streamline endpoint inwards along the streamline trajectory in search of a valid timeseries to sample from the input image.

References:

Calamante, F.; Smith, R.E.; Liang, X.; Zalesky, A.; Connelly, A Track-weighted dynamic functional connectivity (TW-dFC): a new method to study time-resolved functional connectivity. Brain Struct Funct, 2017, doi: 10.1007/s00429-017-1431-1

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckdfc_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKDFC_METADATA)
    execution.params(params)
    args = tckdfc_cargs(params, execution)
    out = tckdfc_outputs(params, execution)
    execution.run(args)
    return out

def tckdfc(
    tracks: InputPathType,
    fmri: InputPathType,
    output: str,
    static: bool = False,
    dynamic: TckdfcDynamic | None = None,
    template: InputPathType | None = None,
    vox: list[float] | None = None,
    stat_vox: str | None = None,
    backtrack: bool = False,
    upsample: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckdfcConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckdfcOutputs:
    """
    tckdfc

    Perform the Track-Weighted Dynamic Functional Connectivity (TW-dFC) method.

This command generates a Track-Weighted Image (TWI), where the contribution from each streamline to the image is the Pearson correlation between the fMRI time series at the streamline endpoints.

The output image can be generated in one of two ways (note that one of these two command-line options MUST be provided): 

- "Static" functional connectivity (-static option): Each streamline contributes to a static 3D output image based on the correlation between the signals at the streamline endpoints using the entirety of the input time series.

- "Dynamic" functional connectivity (-dynamic option): The output image is a 4D image, with the same number of volumes as the input fMRI time series. For each volume, the contribution from each streamline is calculated based on a finite-width sliding time window, centred at the timepoint corresponding to that volume.

Note that the -backtrack option in this command is similar, but not precisely equivalent, to back-tracking as can be used with Anatomically-Constrained Tractography (ACT) in the tckgen command. However, here the feature does not change the streamlines trajectories in any way; it simply enables detection of the fact that the input fMRI image may not contain a valid timeseries underneath the streamline endpoint, and where this occurs, searches from the streamline endpoint inwards along the streamline trajectory in search of a valid timeseries to sample from the input image.

References:

Calamante, F.; Smith, R.E.; Liang, X.; Zalesky, A.; Connelly, A Track-weighted dynamic functional connectivity (TW-dFC): a new method to study time-resolved functional connectivity. Brain Struct Funct, 2017, doi: 10.1007/s00429-017-1431-1

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks: the input track file.
        fmri: the pre-processed fMRI time series
        output: the output TW-dFC image
        static: generate a "static" (3D) output image.
        dynamic: generate a "dynamic" (4D) output image; must additionally provide\
            the shape and width (in volumes) of the sliding window.
        template: an image file to be used as a template for the output (the output\
            image will have the same transform and field of view).
        vox: provide either an isotropic voxel size (in mm), or comma-separated\
            list of 3 voxel dimensions.
        stat_vox: define the statistic for choosing the final voxel intensities for\
            a given contrast type given the individual values from the tracks\
            passing through each voxel Options are: sum, min, mean, max (default:\
            mean)
        backtrack: if no valid timeseries is found at the streamline endpoint,\
            back-track along the streamline trajectory until a valid timeseries is\
            found
        upsample: upsample the tracks by some ratio using Hermite interpolation\
            before mapping (if omitted, an appropriate ratio will be determined\
            automatically)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckdfc_params(
        tracks=tracks,
        fmri=fmri,
        output=output,
        static=static,
        dynamic=dynamic,
        template=template,
        vox=vox,
        stat_vox=stat_vox,
        backtrack=backtrack,
        upsample=upsample,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckdfc_execute(params, runner)

__all__ = [
    "Tckdfc",
    "TckdfcOutputs",
    "TCKDFC_METADATA",
    "tckdfc_cargs",
    "tckdfc_outputs",
    "tckdfc_params",
    "tckdfc_execute",
    "tckdfc_validate",
    "tckdfc",
]