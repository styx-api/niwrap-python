# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABELCONVERT_METADATA = Metadata(
    id="labelconvert",
    name="labelconvert",
    package="mrtrix",
)

class LabelconvertConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Labelconvert = typing.TypedDict(
    "Labelconvert",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/labelconvert"]],
        "spine": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[LabelconvertConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "path_in": InputPathType,
        "lut_in": InputPathType,
        "lut_out": InputPathType,
        "image_out": str,
    },
)

@dataclasses.dataclass
class LabelconvertOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    image_out: OutputPathType
    """the output image"""

def labelconvert_params(
    path_in: InputPathType,
    lut_in: InputPathType,
    lut_out: InputPathType,
    image_out: str,
    spine: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[LabelconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Labelconvert:
    """
    Build parameters.

    Args:
        path_in: the input image
        lut_in: the connectome lookup table corresponding to the input image
        lut_out: the target connectome lookup table for the output image
        image_out: the output image
        spine: provide a manually-defined segmentation of the base of the spine\
            where the streamlines terminate, so that this can become a node in the\
            connection matrix.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Labelconvert = {
        "@type": "mrtrix/labelconvert",
        "path_in": path_in,
        "lut_in": lut_in,
        "lut_out": lut_out,
        "image_out": image_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if spine is not None:
        params["spine"] = spine
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def labelconvert_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Labelconvert."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spine", None) is not None:
        if not isinstance(params["spine"], (pathlib.Path, str)):
            raise StyxValidationError(f'`spine` has the wrong type: Received `{type(params["spine"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[LabelconvertConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("path_in", None) is None:
        raise StyxValidationError("`path_in` must not be None")
    if not isinstance(params["path_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`path_in` has the wrong type: Received `{type(params["path_in"])}` expected `InputPathType`')
    if params.get("lut_in", None) is None:
        raise StyxValidationError("`lut_in` must not be None")
    if not isinstance(params["lut_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`lut_in` has the wrong type: Received `{type(params["lut_in"])}` expected `InputPathType`')
    if params.get("lut_out", None) is None:
        raise StyxValidationError("`lut_out` must not be None")
    if not isinstance(params["lut_out"], (pathlib.Path, str)):
        raise StyxValidationError(f'`lut_out` has the wrong type: Received `{type(params["lut_out"])}` expected `InputPathType`')
    if params.get("image_out", None) is None:
        raise StyxValidationError("`image_out` must not be None")
    if not isinstance(params["image_out"], str):
        raise StyxValidationError(f'`image_out` has the wrong type: Received `{type(params["image_out"])}` expected `str`')

def labelconvert_cargs(params: Labelconvert, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("labelconvert")
    v_0 = params.get("spine")
    if v_0 is not None:
        cargs.append("-spine")
        cargs.append(execution.input_file(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["path_in"]))
    cargs.append(execution.input_file(params["lut_in"]))
    cargs.append(execution.input_file(params["lut_out"]))
    cargs.append(params["image_out"])
    return cargs

def labelconvert_outputs(params: Labelconvert, execution: Execution) -> LabelconvertOutputs:
    """Build the LabelconvertOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    image_out_v: OutputPathType = execution.output_file(params["image_out"])
    return LabelconvertOutputs(
        root=root_v,
        image_out=image_out_v,
    )

def labelconvert_execute(params: Labelconvert, runner: Runner | None = None) -> LabelconvertOutputs:
    """
    labelconvert

    Convert a connectome node image from one lookup table to another.

Typical usage is to convert a parcellation image provided by some other software, based on the lookup table provided by that software, to conform to a new lookup table, particularly one where the node indices increment from 1, in preparation for connectome construction; examples of such target lookup table files are provided in share//mrtrix3//labelconvert//, but can be created by the user to provide the desired node set // ordering // colours.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    labelconvert_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABELCONVERT_METADATA)
    execution.params(params)
    args = labelconvert_cargs(params, execution)
    out = labelconvert_outputs(params, execution)
    execution.run(args)
    return out

def labelconvert(
    path_in: InputPathType,
    lut_in: InputPathType,
    lut_out: InputPathType,
    image_out: str,
    spine: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[LabelconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> LabelconvertOutputs:
    """
    labelconvert

    Convert a connectome node image from one lookup table to another.

Typical usage is to convert a parcellation image provided by some other software, based on the lookup table provided by that software, to conform to a new lookup table, particularly one where the node indices increment from 1, in preparation for connectome construction; examples of such target lookup table files are provided in share//mrtrix3//labelconvert//, but can be created by the user to provide the desired node set // ordering // colours.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        path_in: the input image
        lut_in: the connectome lookup table corresponding to the input image
        lut_out: the target connectome lookup table for the output image
        image_out: the output image
        spine: provide a manually-defined segmentation of the base of the spine\
            where the streamlines terminate, so that this can become a node in the\
            connection matrix.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = labelconvert_params(
        path_in=path_in,
        lut_in=lut_in,
        lut_out=lut_out,
        image_out=image_out,
        spine=spine,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return labelconvert_execute(params, runner)

__all__ = [
    "Labelconvert",
    "LabelconvertOutputs",
    "LABELCONVERT_METADATA",
    "labelconvert_cargs",
    "labelconvert_outputs",
    "labelconvert_params",
    "labelconvert_execute",
    "labelconvert_validate",
    "labelconvert",
]