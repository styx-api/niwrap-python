# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AFDCONNECTIVITY_METADATA = Metadata(
    id="afdconnectivity",
    name="afdconnectivity",
    package="mrtrix",
)

class AfdconnectivityConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Afdconnectivity = typing.TypedDict(
    "Afdconnectivity",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/afdconnectivity"]],
        "wbft": typing.NotRequired[InputPathType],
        "afd_map": typing.NotRequired[str],
        "all_fixels": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[AfdconnectivityConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image": InputPathType,
        "tracks": InputPathType,
    },
)

@dataclasses.dataclass
class AfdconnectivityOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    afd_map: OutputPathType | None
    """output a 3D image containing the AFD estimated for each voxel. """

def afdconnectivity_params(
    image: InputPathType,
    tracks: InputPathType,
    wbft: InputPathType | None = None,
    afd_map: str | None = None,
    all_fixels: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[AfdconnectivityConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Afdconnectivity:
    """
    Build parameters.

    Args:
        image: the input FOD image.
        tracks: the input track file defining the bundle of interest.
        wbft: provide a whole-brain fibre-tracking data set (of which the input\
            track file should be a subset), to improve the estimate of fibre bundle\
            volume in the presence of partial volume
        afd_map: output a 3D image containing the AFD estimated for each voxel.
        all_fixels: if whole-brain fibre-tracking is NOT provided, then if multiple\
            fixels within a voxel are traversed by the pathway of interest, by\
            default the fixel with the greatest streamlines density is selected to\
            contribute to the AFD in that voxel. If this option is provided, then\
            ALL fixels with non-zero streamlines density will contribute to the\
            result, even if multiple fixels per voxel are selected.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Afdconnectivity = {
        "@type": "mrtrix/afdconnectivity",
        "image": image,
        "tracks": tracks,
        "all_fixels": all_fixels,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if wbft is not None:
        params["wbft"] = wbft
    if afd_map is not None:
        params["afd_map"] = afd_map
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def afdconnectivity_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Afdconnectivity."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("wbft", None) is not None:
        if not isinstance(params["wbft"], (pathlib.Path, str)):
            raise StyxValidationError(f'`wbft` has the wrong type: Received `{type(params["wbft"])}` expected `InputPathType`')
    if params.get("afd_map", None) is not None:
        if not isinstance(params["afd_map"], str):
            raise StyxValidationError(f'`afd_map` has the wrong type: Received `{type(params["afd_map"])}` expected `str`')
    if params.get("all_fixels", None) is not None:
        if not isinstance(params["all_fixels"], bool):
            raise StyxValidationError(f'`all_fixels` has the wrong type: Received `{type(params["all_fixels"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[AfdconnectivityConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image", None) is None:
        raise StyxValidationError("`image` must not be None")
    if not isinstance(params["image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image` has the wrong type: Received `{type(params["image"])}` expected `InputPathType`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')

def afdconnectivity_cargs(params: Afdconnectivity, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("afdconnectivity")
    v_0 = params.get("wbft")
    if v_0 is not None:
        cargs.append("-wbft")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("afd_map")
    if v_1 is not None:
        cargs.append("-afd_map")
        cargs.append(v_1)
    if params.get("all_fixels"):
        cargs.append("-all_fixels")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("nthreads")
    if v_2 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_2))
    v_3 = params.get("config")
    if v_3 is not None:
        for item0 in v_3:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["image"]))
    cargs.append(execution.input_file(params["tracks"]))
    return cargs

def afdconnectivity_outputs(params: Afdconnectivity, execution: Execution) -> AfdconnectivityOutputs:
    """Build the AfdconnectivityOutputs object for this tool."""
    afd_map_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("afd_map")
    if __v0 is not None:
        afd_map_v = execution.output_file((__v0 if __v0 is not None else ""))
    return AfdconnectivityOutputs(
        root=root_v,
        afd_map=afd_map_v,
    )

def afdconnectivity_execute(params: Afdconnectivity, runner: Runner | None = None) -> AfdconnectivityOutputs:
    """
    afdconnectivity

    Obtain an estimate of fibre connectivity between two regions using AFD and streamlines tractography.

This estimate is obtained by determining a fibre volume (AFD) occupied by the pathway of interest, and dividing by the streamline length.

If only the streamlines belonging to the pathway of interest are provided, then ALL of the fibre volume within each fixel selected will contribute to the result. If the -wbft option is used to provide whole-brain fibre-tracking (of which the pathway of interest should contain a subset), only the fraction of the fibre volume in each fixel estimated to belong to the pathway of interest will contribute to the result.

Use -quiet to suppress progress messages and output fibre connectivity value only.

For valid comparisons of AFD connectivity across scans, images MUST be intensity normalised and bias field corrected, and a common response function for all subjects must be used.

Note that the sum of the AFD is normalised by streamline length to account for subject differences in fibre bundle length. This normalisation results in a measure that is more related to the cross-sectional volume of the tract (and therefore 'connectivity'). Note that SIFT-ed tract count is a superior measure because it is unaffected by tangential yet unrelated fibres. However, AFD connectivity may be used as a substitute when Anatomically Constrained Tractography is not possible due to uncorrectable EPI distortions, and SIFT may therefore not be as effective.

Longer discussion regarding this command can additionally be found at: https://mrtrix.readthedocs.io/en/3.0.4/concepts/afd_connectivity.html (as well as in the relevant reference).

References:

Smith, R. E.; Raffelt, D.; Tournier, J.-D.; Connelly, A. Quantitative Streamlines Tractography: Methods and Inter-Subject Normalisation. Open Science Framework, https://doi.org/10.31219/osf.io/c67kn.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    afdconnectivity_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AFDCONNECTIVITY_METADATA)
    execution.params(params)
    args = afdconnectivity_cargs(params, execution)
    out = afdconnectivity_outputs(params, execution)
    execution.run(args)
    return out

def afdconnectivity(
    image: InputPathType,
    tracks: InputPathType,
    wbft: InputPathType | None = None,
    afd_map: str | None = None,
    all_fixels: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[AfdconnectivityConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> AfdconnectivityOutputs:
    """
    afdconnectivity

    Obtain an estimate of fibre connectivity between two regions using AFD and streamlines tractography.

This estimate is obtained by determining a fibre volume (AFD) occupied by the pathway of interest, and dividing by the streamline length.

If only the streamlines belonging to the pathway of interest are provided, then ALL of the fibre volume within each fixel selected will contribute to the result. If the -wbft option is used to provide whole-brain fibre-tracking (of which the pathway of interest should contain a subset), only the fraction of the fibre volume in each fixel estimated to belong to the pathway of interest will contribute to the result.

Use -quiet to suppress progress messages and output fibre connectivity value only.

For valid comparisons of AFD connectivity across scans, images MUST be intensity normalised and bias field corrected, and a common response function for all subjects must be used.

Note that the sum of the AFD is normalised by streamline length to account for subject differences in fibre bundle length. This normalisation results in a measure that is more related to the cross-sectional volume of the tract (and therefore 'connectivity'). Note that SIFT-ed tract count is a superior measure because it is unaffected by tangential yet unrelated fibres. However, AFD connectivity may be used as a substitute when Anatomically Constrained Tractography is not possible due to uncorrectable EPI distortions, and SIFT may therefore not be as effective.

Longer discussion regarding this command can additionally be found at: https://mrtrix.readthedocs.io/en/3.0.4/concepts/afd_connectivity.html (as well as in the relevant reference).

References:

Smith, R. E.; Raffelt, D.; Tournier, J.-D.; Connelly, A. Quantitative Streamlines Tractography: Methods and Inter-Subject Normalisation. Open Science Framework, https://doi.org/10.31219/osf.io/c67kn.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image: the input FOD image.
        tracks: the input track file defining the bundle of interest.
        wbft: provide a whole-brain fibre-tracking data set (of which the input\
            track file should be a subset), to improve the estimate of fibre bundle\
            volume in the presence of partial volume
        afd_map: output a 3D image containing the AFD estimated for each voxel.
        all_fixels: if whole-brain fibre-tracking is NOT provided, then if multiple\
            fixels within a voxel are traversed by the pathway of interest, by\
            default the fixel with the greatest streamlines density is selected to\
            contribute to the AFD in that voxel. If this option is provided, then\
            ALL fixels with non-zero streamlines density will contribute to the\
            result, even if multiple fixels per voxel are selected.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = afdconnectivity_params(
        image=image,
        tracks=tracks,
        wbft=wbft,
        afd_map=afd_map,
        all_fixels=all_fixels,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return afdconnectivity_execute(params, runner)

__all__ = [
    "Afdconnectivity",
    "AfdconnectivityOutputs",
    "AFDCONNECTIVITY_METADATA",
    "afdconnectivity_cargs",
    "afdconnectivity_outputs",
    "afdconnectivity_params",
    "afdconnectivity_execute",
    "afdconnectivity_validate",
    "afdconnectivity",
]