# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRTRANSFORM_METADATA = Metadata(
    id="mrtransform",
    name="mrtransform",
    package="mrtrix",
)

class MrtransformFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class MrtransformExportGradFsl(typing.TypedDict):
    bvecs_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""
    bvals_path: str
    """export the diffusion-weighted gradient table to files in FSL (bvecs / bvals) format"""

MrtransformVariousString = typing.TypedDict(
    "MrtransformVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

MrtransformVariousFile = typing.TypedDict(
    "MrtransformVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

MrtransformStrides = MrtransformVariousString | MrtransformVariousFile

class MrtransformConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrtransform = typing.TypedDict(
    "Mrtransform",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrtransform"]],
        "linear": typing.NotRequired[InputPathType],
        "flip": typing.NotRequired[list[int]],
        "inverse": typing.NotRequired[bool],
        "half": typing.NotRequired[bool],
        "replace": typing.NotRequired[InputPathType],
        "identity": typing.NotRequired[bool],
        "template": typing.NotRequired[InputPathType],
        "midway_space": typing.NotRequired[bool],
        "interp": typing.NotRequired[str],
        "oversample": typing.NotRequired[list[int]],
        "warp": typing.NotRequired[InputPathType],
        "warp_full": typing.NotRequired[InputPathType],
        "from": typing.NotRequired[int],
        "modulate": typing.NotRequired[str],
        "directions": typing.NotRequired[InputPathType],
        "reorient_fod": typing.NotRequired[str],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[MrtransformFslgrad],
        "export_grad_mrtrix": typing.NotRequired[str],
        "export_grad_fsl": typing.NotRequired[MrtransformExportGradFsl],
        "datatype": typing.NotRequired[str],
        "strides": typing.NotRequired[MrtransformStrides],
        "nan": typing.NotRequired[bool],
        "no_reorientation": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrtransformConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class MrtransformOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image."""
    export_grad_mrtrix: OutputPathType | None
    """export the diffusion-weighted gradient table to file in MRtrix format """

def mrtransform_params(
    input_: InputPathType,
    output: str,
    linear: InputPathType | None = None,
    flip: list[int] | None = None,
    inverse: bool = False,
    half: bool = False,
    replace: InputPathType | None = None,
    identity: bool = False,
    template: InputPathType | None = None,
    midway_space: bool = False,
    interp: str | None = None,
    oversample: list[int] | None = None,
    warp: InputPathType | None = None,
    warp_full: InputPathType | None = None,
    from_: int | None = None,
    modulate: str | None = None,
    directions: InputPathType | None = None,
    reorient_fod: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: MrtransformFslgrad | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: MrtransformExportGradFsl | None = None,
    datatype: str | None = None,
    strides: MrtransformStrides | None = None,
    nan: bool = False,
    no_reorientation: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrtransformConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrtransform:
    """
    Build parameters.

    Args:
        input_: input image to be transformed.
        output: the output image.
        linear: specify a linear transform to apply, in the form of a 3x4 or 4x4\
            ascii file. Note the standard 'reverse' convention is used, where the\
            transform maps points in the template image to the moving image. Note\
            that the reverse convention is still assumed even if no -template image\
            is supplied
        flip: flip the specified axes, provided as a comma-separated list of\
            indices (0:x, 1:y, 2:z).
        inverse: apply the inverse transformation
        half: apply the matrix square root of the transformation. This can be\
            combined with the inverse option.
        replace: replace the linear transform of the original image by that\
            specified, rather than applying it to the original image. The specified\
            transform can be either a template image, or a 3x4 or 4x4 ascii file.
        identity: set the header transform of the image to the identity matrix
        template: reslice the input image to match the specified template image\
            grid.
        midway_space: reslice the input image to the midway space. Requires either\
            the -template or -warp option. If used with -template and -linear\
            option the input image will be resliced onto the grid halfway between\
            the input and template. If used with the -warp option the input will be\
            warped to the midway space defined by the grid of the input warp (i.e.\
            half way between image1 and image2)
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: cubic).
        oversample: set the amount of over-sampling (in the target space) to\
            perform when regridding. This is particularly relevant when downsamping\
            a high-resolution image to a low-resolution image, to avoid aliasing\
            artefacts. This can consist of a single integer, or a comma-separated\
            list of 3 integers if different oversampling factors are desired along\
            the different axes. Default is determined from ratio of voxel\
            dimensions (disabled for nearest-neighbour interpolation).
        warp: apply a non-linear 4D deformation field to warp the input image. Each\
            voxel in the deformation field must define the scanner space position\
            that will be used to interpolate the input image during warping (i.e.\
            pull-back/reverse warp convention). If the -template image is also\
            supplied the deformation field will be resliced first to the template\
            image grid. If no -template option is supplied then the output image\
            will have the same image grid as the deformation field. This option can\
            be used in combination with the -affine option, in which case the\
            affine will be applied first)
        warp_full: warp the input image using a 5D warp file output from\
            mrregister. Any linear transforms in the warp image header will also be\
            applied. The -warp_full option must be used in combination with either\
            the -template option or the -midway_space option. If a -template image\
            is supplied then the full warp will be used. By default the\
            image1->image2 transform will be applied, however the -from 2 option\
            can be used to apply the image2->image1 transform. Use the\
            -midway_space option to warp the input image to the midway space. The\
            -from option can also be used to define which warp to use when\
            transforming to midway space
        from_: used to define which space the input image is when using the\
            -warp_mid option. Use -from 1 to warp from image1 or -from 2 to warp\
            from image2
        modulate: Valid choices are: fod and jac. fod: modulate FODs during\
            reorientation to preserve the apparent fibre density across fibre\
            bundle widths before and after the transformation. jac: modulate the\
            image intensity with the determinant of the Jacobian of the warp of\
            linear transformation to preserve the total intensity before and after\
            the transformation.
        directions: directions defining the number and orientation of the apodised\
            point spread functions used in FOD reorientation (Default: 300\
            directions)
        reorient_fod: specify whether to perform FOD reorientation. This is\
            required if the number of volumes in the 4th dimension corresponds to\
            the number of coefficients in an antipodally symmetric spherical\
            harmonic series with lmax >= 2 (i.e. 6, 15, 28, 45, 66 volumes).
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        nan: Use NaN as the out of bounds value (Default: 0.0)
        no_reorientation: deprecated, use -reorient_fod instead
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrtransform = {
        "@type": "mrtrix/mrtransform",
        "input": input_,
        "output": output,
        "inverse": inverse,
        "half": half,
        "identity": identity,
        "midway_space": midway_space,
        "nan": nan,
        "no_reorientation": no_reorientation,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if linear is not None:
        params["linear"] = linear
    if flip is not None:
        params["flip"] = flip
    if replace is not None:
        params["replace"] = replace
    if template is not None:
        params["template"] = template
    if interp is not None:
        params["interp"] = interp
    if oversample is not None:
        params["oversample"] = oversample
    if warp is not None:
        params["warp"] = warp
    if warp_full is not None:
        params["warp_full"] = warp_full
    if from_ is not None:
        params["from"] = from_
    if modulate is not None:
        params["modulate"] = modulate
    if directions is not None:
        params["directions"] = directions
    if reorient_fod is not None:
        params["reorient_fod"] = reorient_fod
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if export_grad_mrtrix is not None:
        params["export_grad_mrtrix"] = export_grad_mrtrix
    if export_grad_fsl is not None:
        params["export_grad_fsl"] = export_grad_fsl
    if datatype is not None:
        params["datatype"] = datatype
    if strides is not None:
        params["strides"] = strides
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrtransform_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrtransform."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("linear", None) is not None:
        if not isinstance(params["linear"], (pathlib.Path, str)):
            raise StyxValidationError(f'`linear` has the wrong type: Received `{type(params["linear"])}` expected `InputPathType`')
    if params.get("flip", None) is not None:
        if not isinstance(params["flip"], list):
            raise StyxValidationError(f'`flip` has the wrong type: Received `{type(params["flip"])}` expected `list[int]`')
        for e in params["flip"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`flip` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')
    if params.get("half", None) is not None:
        if not isinstance(params["half"], bool):
            raise StyxValidationError(f'`half` has the wrong type: Received `{type(params["half"])}` expected `bool`')
    if params.get("replace", None) is not None:
        if not isinstance(params["replace"], (pathlib.Path, str)):
            raise StyxValidationError(f'`replace` has the wrong type: Received `{type(params["replace"])}` expected `InputPathType`')
    if params.get("identity", None) is not None:
        if not isinstance(params["identity"], bool):
            raise StyxValidationError(f'`identity` has the wrong type: Received `{type(params["identity"])}` expected `bool`')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("midway_space", None) is not None:
        if not isinstance(params["midway_space"], bool):
            raise StyxValidationError(f'`midway_space` has the wrong type: Received `{type(params["midway_space"])}` expected `bool`')
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `str`')
    if params.get("oversample", None) is not None:
        if not isinstance(params["oversample"], list):
            raise StyxValidationError(f'`oversample` has the wrong type: Received `{type(params["oversample"])}` expected `list[int]`')
        for e_2 in params["oversample"]:
            if not isinstance(e_2, int):
                raise StyxValidationError(f'`oversample` has the wrong type: Received `{type(e_2)}` expected `list[int]`')
    if params.get("warp", None) is not None:
        if not isinstance(params["warp"], (pathlib.Path, str)):
            raise StyxValidationError(f'`warp` has the wrong type: Received `{type(params["warp"])}` expected `InputPathType`')
    if params.get("warp_full", None) is not None:
        if not isinstance(params["warp_full"], (pathlib.Path, str)):
            raise StyxValidationError(f'`warp_full` has the wrong type: Received `{type(params["warp_full"])}` expected `InputPathType`')
    if params.get("from", None) is not None:
        if not isinstance(params["from"], int):
            raise StyxValidationError(f'`from` has the wrong type: Received `{type(params["from"])}` expected `int`')
    if params.get("modulate", None) is not None:
        if not isinstance(params["modulate"], str):
            raise StyxValidationError(f'`modulate` has the wrong type: Received `{type(params["modulate"])}` expected `str`')
    if params.get("directions", None) is not None:
        if not isinstance(params["directions"], (pathlib.Path, str)):
            raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("reorient_fod", None) is not None:
        if not isinstance(params["reorient_fod"], str):
            raise StyxValidationError(f'`reorient_fod` has the wrong type: Received `{type(params["reorient_fod"])}` expected `str`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("export_grad_mrtrix", None) is not None:
        if not isinstance(params["export_grad_mrtrix"], str):
            raise StyxValidationError(f'`export_grad_mrtrix` has the wrong type: Received `{type(params["export_grad_mrtrix"])}` expected `str`')
    if params.get("export_grad_fsl", None) is not None:
        if not isinstance(params["export_grad_fsl"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["export_grad_fsl"])}\'')
        if params["export_grad_fsl"].get("bvecs_path", None) is None:
            raise StyxValidationError("`bvecs_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvecs_path"], str):
            raise StyxValidationError(f'`bvecs_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvecs_path"])}` expected `str`')
        if params["export_grad_fsl"].get("bvals_path", None) is None:
            raise StyxValidationError("`bvals_path` must not be None")
        if not isinstance(params["export_grad_fsl"]["bvals_path"], str):
            raise StyxValidationError(f'`bvals_path` has the wrong type: Received `{type(params["export_grad_fsl"]["bvals_path"])}` expected `str`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("nan", None) is not None:
        if not isinstance(params["nan"], bool):
            raise StyxValidationError(f'`nan` has the wrong type: Received `{type(params["nan"])}` expected `bool`')
    if params.get("no_reorientation", None) is not None:
        if not isinstance(params["no_reorientation"], bool):
            raise StyxValidationError(f'`no_reorientation` has the wrong type: Received `{type(params["no_reorientation"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrtransformConfig]`')
        for e_3 in params["config"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_3["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_3["key"])}` expected `str`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrtransform_cargs(params: Mrtransform, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrtransform")
    v_0 = params.get("linear")
    if v_0 is not None:
        cargs.append("-linear")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("flip")
    if v_1 is not None:
        cargs.append("-flip")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    if params.get("inverse"):
        cargs.append("-inverse")
    if params.get("half"):
        cargs.append("-half")
    v_2 = params.get("replace")
    if v_2 is not None:
        cargs.append("-replace")
        cargs.append(execution.input_file(v_2))
    if params.get("identity"):
        cargs.append("-identity")
    v_3 = params.get("template")
    if v_3 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_3))
    if params.get("midway_space"):
        cargs.append("-midway_space")
    v_4 = params.get("interp")
    if v_4 is not None:
        cargs.append("-interp")
        cargs.append(v_4)
    v_5 = params.get("oversample")
    if v_5 is not None:
        cargs.append("-oversample")
        cargs.append(",".join([str(item1) for item1 in v_5]))
    v_6 = params.get("warp")
    if v_6 is not None:
        cargs.append("-warp")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("warp_full")
    if v_7 is not None:
        cargs.append("-warp_full")
        cargs.append(execution.input_file(v_7))
    v_8 = params.get("from")
    if v_8 is not None:
        cargs.append("-from")
        cargs.append(str(v_8))
    v_9 = params.get("modulate")
    if v_9 is not None:
        cargs.append("-modulate")
        cargs.append(v_9)
    v_10 = params.get("directions")
    if v_10 is not None:
        cargs.append("-directions")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("reorient_fod")
    if v_11 is not None:
        cargs.append("-reorient_fod")
        cargs.append(v_11)
    v_12 = params.get("grad")
    if v_12 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_12))
    v_13 = params.get("fslgrad")
    if v_13 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_13["bvecs"]))
        cargs.append(execution.input_file(v_13["bvals"]))
    v_14 = params.get("export_grad_mrtrix")
    if v_14 is not None:
        cargs.append("-export_grad_mrtrix")
        cargs.append(v_14)
    v_15 = params.get("export_grad_fsl")
    if v_15 is not None:
        cargs.append("-export_grad_fsl")
        cargs.append(v_15["bvecs_path"])
        cargs.append(v_15["bvals_path"])
    v_16 = params.get("datatype")
    if v_16 is not None:
        cargs.append("-datatype")
        cargs.append(v_16)
    v_17 = params.get("strides")
    if v_17 is not None:
        cargs.append("-strides")
        if v_17["@type"] == "VariousString":
            cargs.append(str(v_17["obj"]))
        elif v_17["@type"] == "VariousFile":
            cargs.append(str(v_17["obj"]))
    if params.get("nan"):
        cargs.append("-nan")
    if params.get("no_reorientation"):
        cargs.append("-no_reorientation")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_18 = params.get("nthreads")
    if v_18 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_18))
    v_19 = params.get("config")
    if v_19 is not None:
        for item2 in v_19:
            cargs.append("-config")
            cargs.append(item2["key"])
            cargs.append(item2["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["output"])
    return cargs

def mrtransform_outputs(params: Mrtransform, execution: Execution) -> MrtransformOutputs:
    """Build the MrtransformOutputs object for this tool."""
    export_grad_mrtrix_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    __v0 = params.get("export_grad_mrtrix")
    if __v0 is not None:
        export_grad_mrtrix_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrtransformOutputs(
        root=root_v,
        output=output_v,
        export_grad_mrtrix=export_grad_mrtrix_v,
    )

def mrtransform_execute(params: Mrtransform, runner: Runner | None = None) -> MrtransformOutputs:
    """
    mrtransform

    Apply spatial transformations to an image.

If a linear transform is applied without a template image the command will modify the image header transform matrix

FOD reorientation (with apodised point spread functions) can be performed if the number of volumes in the 4th dimension equals the number of coefficients in an antipodally symmetric spherical harmonic series (e.g. 6, 15, 28 etc). For such data, the -reorient_fod yes/no option must be used to specify if reorientation is required.

The output image intensity can be modulated using the (local or global) volume change if a linear or nonlinear transformation is applied. 'FOD' modulation preserves the apparent fibre density across the fibre bundle width and can only be applied if FOD reorientation is used. Alternatively, non-directional scaling by the Jacobian determinant can be applied to any image type. 

If a DW scheme is contained in the header (or specified separately), and the number of directions matches the number of volumes in the images, any transformation applied using the -linear option will also be applied to the directions.

When the -template option is used to specify the target image grid, the image provided via this option will not influence the axis data strides of the output image; these are determined based on the input image, or the input to the -strides option.

References:

* If FOD reorientation is being performed:
Raffelt, D.; Tournier, J.-D.; Crozier, S.; Connelly, A. & Salvado, O. Reorientation of fiber orientation distributions using apodized point spread functions. Magnetic Resonance in Medicine, 2012, 67, 844-855

* If FOD modulation is being performed:
Raffelt, D.; Tournier, J.-D.; Rose, S.; Ridgway, G.R.; Henderson, R.; Crozier, S.; Salvado, O.; Connelly, A.; Apparent Fibre Density: a novel measure for the analysis of diffusion-weighted magnetic resonance images. NeuroImage, 2012, 15;59(4), 3976-94

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrtransform_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRTRANSFORM_METADATA)
    execution.params(params)
    args = mrtransform_cargs(params, execution)
    out = mrtransform_outputs(params, execution)
    execution.run(args)
    return out

def mrtransform(
    input_: InputPathType,
    output: str,
    linear: InputPathType | None = None,
    flip: list[int] | None = None,
    inverse: bool = False,
    half: bool = False,
    replace: InputPathType | None = None,
    identity: bool = False,
    template: InputPathType | None = None,
    midway_space: bool = False,
    interp: str | None = None,
    oversample: list[int] | None = None,
    warp: InputPathType | None = None,
    warp_full: InputPathType | None = None,
    from_: int | None = None,
    modulate: str | None = None,
    directions: InputPathType | None = None,
    reorient_fod: str | None = None,
    grad: InputPathType | None = None,
    fslgrad: MrtransformFslgrad | None = None,
    export_grad_mrtrix: str | None = None,
    export_grad_fsl: MrtransformExportGradFsl | None = None,
    datatype: str | None = None,
    strides: MrtransformStrides | None = None,
    nan: bool = False,
    no_reorientation: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrtransformConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrtransformOutputs:
    """
    mrtransform

    Apply spatial transformations to an image.

If a linear transform is applied without a template image the command will modify the image header transform matrix

FOD reorientation (with apodised point spread functions) can be performed if the number of volumes in the 4th dimension equals the number of coefficients in an antipodally symmetric spherical harmonic series (e.g. 6, 15, 28 etc). For such data, the -reorient_fod yes/no option must be used to specify if reorientation is required.

The output image intensity can be modulated using the (local or global) volume change if a linear or nonlinear transformation is applied. 'FOD' modulation preserves the apparent fibre density across the fibre bundle width and can only be applied if FOD reorientation is used. Alternatively, non-directional scaling by the Jacobian determinant can be applied to any image type. 

If a DW scheme is contained in the header (or specified separately), and the number of directions matches the number of volumes in the images, any transformation applied using the -linear option will also be applied to the directions.

When the -template option is used to specify the target image grid, the image provided via this option will not influence the axis data strides of the output image; these are determined based on the input image, or the input to the -strides option.

References:

* If FOD reorientation is being performed:
Raffelt, D.; Tournier, J.-D.; Crozier, S.; Connelly, A. & Salvado, O. Reorientation of fiber orientation distributions using apodized point spread functions. Magnetic Resonance in Medicine, 2012, 67, 844-855

* If FOD modulation is being performed:
Raffelt, D.; Tournier, J.-D.; Rose, S.; Ridgway, G.R.; Henderson, R.; Crozier, S.; Salvado, O.; Connelly, A.; Apparent Fibre Density: a novel measure for the analysis of diffusion-weighted magnetic resonance images. NeuroImage, 2012, 15;59(4), 3976-94

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: input image to be transformed.
        output: the output image.
        linear: specify a linear transform to apply, in the form of a 3x4 or 4x4\
            ascii file. Note the standard 'reverse' convention is used, where the\
            transform maps points in the template image to the moving image. Note\
            that the reverse convention is still assumed even if no -template image\
            is supplied
        flip: flip the specified axes, provided as a comma-separated list of\
            indices (0:x, 1:y, 2:z).
        inverse: apply the inverse transformation
        half: apply the matrix square root of the transformation. This can be\
            combined with the inverse option.
        replace: replace the linear transform of the original image by that\
            specified, rather than applying it to the original image. The specified\
            transform can be either a template image, or a 3x4 or 4x4 ascii file.
        identity: set the header transform of the image to the identity matrix
        template: reslice the input image to match the specified template image\
            grid.
        midway_space: reslice the input image to the midway space. Requires either\
            the -template or -warp option. If used with -template and -linear\
            option the input image will be resliced onto the grid halfway between\
            the input and template. If used with the -warp option the input will be\
            warped to the midway space defined by the grid of the input warp (i.e.\
            half way between image1 and image2)
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: cubic).
        oversample: set the amount of over-sampling (in the target space) to\
            perform when regridding. This is particularly relevant when downsamping\
            a high-resolution image to a low-resolution image, to avoid aliasing\
            artefacts. This can consist of a single integer, or a comma-separated\
            list of 3 integers if different oversampling factors are desired along\
            the different axes. Default is determined from ratio of voxel\
            dimensions (disabled for nearest-neighbour interpolation).
        warp: apply a non-linear 4D deformation field to warp the input image. Each\
            voxel in the deformation field must define the scanner space position\
            that will be used to interpolate the input image during warping (i.e.\
            pull-back/reverse warp convention). If the -template image is also\
            supplied the deformation field will be resliced first to the template\
            image grid. If no -template option is supplied then the output image\
            will have the same image grid as the deformation field. This option can\
            be used in combination with the -affine option, in which case the\
            affine will be applied first)
        warp_full: warp the input image using a 5D warp file output from\
            mrregister. Any linear transforms in the warp image header will also be\
            applied. The -warp_full option must be used in combination with either\
            the -template option or the -midway_space option. If a -template image\
            is supplied then the full warp will be used. By default the\
            image1->image2 transform will be applied, however the -from 2 option\
            can be used to apply the image2->image1 transform. Use the\
            -midway_space option to warp the input image to the midway space. The\
            -from option can also be used to define which warp to use when\
            transforming to midway space
        from_: used to define which space the input image is when using the\
            -warp_mid option. Use -from 1 to warp from image1 or -from 2 to warp\
            from image2
        modulate: Valid choices are: fod and jac. fod: modulate FODs during\
            reorientation to preserve the apparent fibre density across fibre\
            bundle widths before and after the transformation. jac: modulate the\
            image intensity with the determinant of the Jacobian of the warp of\
            linear transformation to preserve the total intensity before and after\
            the transformation.
        directions: directions defining the number and orientation of the apodised\
            point spread functions used in FOD reorientation (Default: 300\
            directions)
        reorient_fod: specify whether to perform FOD reorientation. This is\
            required if the number of volumes in the 4th dimension corresponds to\
            the number of coefficients in an antipodally symmetric spherical\
            harmonic series with lmax >= 2 (i.e. 6, 15, 28, 45, 66 volumes).
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        export_grad_mrtrix: export the diffusion-weighted gradient table to file in\
            MRtrix format
        export_grad_fsl: export the diffusion-weighted gradient table to files in\
            FSL (bvecs / bvals) format
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        nan: Use NaN as the out of bounds value (Default: 0.0)
        no_reorientation: deprecated, use -reorient_fod instead
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrtransform_params(
        input_=input_,
        output=output,
        linear=linear,
        flip=flip,
        inverse=inverse,
        half=half,
        replace=replace,
        identity=identity,
        template=template,
        midway_space=midway_space,
        interp=interp,
        oversample=oversample,
        warp=warp,
        warp_full=warp_full,
        from_=from_,
        modulate=modulate,
        directions=directions,
        reorient_fod=reorient_fod,
        grad=grad,
        fslgrad=fslgrad,
        export_grad_mrtrix=export_grad_mrtrix,
        export_grad_fsl=export_grad_fsl,
        datatype=datatype,
        strides=strides,
        nan=nan,
        no_reorientation=no_reorientation,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrtransform_execute(params, runner)

__all__ = [
    "Mrtransform",
    "MrtransformOutputs",
    "MRTRANSFORM_METADATA",
    "mrtransform_cargs",
    "mrtransform_outputs",
    "mrtransform_params",
    "mrtransform_execute",
    "mrtransform_validate",
    "mrtransform",
]