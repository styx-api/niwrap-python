# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DIRFLIP_METADATA = Metadata(
    id="dirflip",
    name="dirflip",
    package="mrtrix",
)

class DirflipConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dirflip = typing.TypedDict(
    "Dirflip",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dirflip"]],
        "permutations": typing.NotRequired[int],
        "cartesian": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[DirflipConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in": InputPathType,
        "out": str,
    },
)

@dataclasses.dataclass
class DirflipOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output files for the directions."""

def dirflip_params(
    in_: InputPathType,
    out: str,
    permutations: int | None = None,
    cartesian: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DirflipConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dirflip:
    """
    Build parameters.

    Args:
        in_: the input files for the directions.
        out: the output files for the directions.
        permutations: number of permutations to try (default: 100000000)
        cartesian: Output the directions in Cartesian coordinates [x y z] instead\
            of [az el].
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dirflip = {
        "@type": "mrtrix/dirflip",
        "in": in_,
        "out": out,
        "cartesian": cartesian,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if permutations is not None:
        params["permutations"] = permutations
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dirflip_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dirflip."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("permutations", None) is not None:
        if not isinstance(params["permutations"], int):
            raise StyxValidationError(f'`permutations` has the wrong type: Received `{type(params["permutations"])}` expected `int`')
    if params.get("cartesian", None) is not None:
        if not isinstance(params["cartesian"], bool):
            raise StyxValidationError(f'`cartesian` has the wrong type: Received `{type(params["cartesian"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DirflipConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in", None) is None:
        raise StyxValidationError("`in` must not be None")
    if not isinstance(params["in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in` has the wrong type: Received `{type(params["in"])}` expected `InputPathType`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def dirflip_cargs(params: Dirflip, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dirflip")
    v_0 = params.get("permutations")
    if v_0 is not None:
        cargs.append("-permutations")
        cargs.append(str(v_0))
    if params.get("cartesian"):
        cargs.append("-cartesian")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in"]))
    cargs.append(params["out"])
    return cargs

def dirflip_outputs(params: Dirflip, execution: Execution) -> DirflipOutputs:
    """Build the DirflipOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    return DirflipOutputs(
        root=root_v,
        out=out_v,
    )

def dirflip_execute(params: Dirflip, runner: Runner | None = None) -> DirflipOutputs:
    """
    dirflip

    Invert the polarity of individual directions so as to optimise a unipolar electrostatic repulsion model.

The orientations themselves are not affected, only their polarity; this is necessary to ensure near-optimal distribution of DW directions for eddy-current correction.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dirflip_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DIRFLIP_METADATA)
    execution.params(params)
    args = dirflip_cargs(params, execution)
    out = dirflip_outputs(params, execution)
    execution.run(args)
    return out

def dirflip(
    in_: InputPathType,
    out: str,
    permutations: int | None = None,
    cartesian: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DirflipConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DirflipOutputs:
    """
    dirflip

    Invert the polarity of individual directions so as to optimise a unipolar electrostatic repulsion model.

The orientations themselves are not affected, only their polarity; this is necessary to ensure near-optimal distribution of DW directions for eddy-current correction.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_: the input files for the directions.
        out: the output files for the directions.
        permutations: number of permutations to try (default: 100000000)
        cartesian: Output the directions in Cartesian coordinates [x y z] instead\
            of [az el].
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dirflip_params(
        in_=in_,
        out=out,
        permutations=permutations,
        cartesian=cartesian,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dirflip_execute(params, runner)

__all__ = [
    "Dirflip",
    "DirflipOutputs",
    "DIRFLIP_METADATA",
    "dirflip_cargs",
    "dirflip_outputs",
    "dirflip_params",
    "dirflip_execute",
    "dirflip_validate",
    "dirflip",
]