# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TRANSFORMCOMPOSE_METADATA = Metadata(
    id="transformcompose",
    name="transformcompose",
    package="mrtrix",
)

class TransformcomposeConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

TransformcomposeVariousString = typing.TypedDict(
    "TransformcomposeVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

TransformcomposeVariousFile = typing.TypedDict(
    "TransformcomposeVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

TransformcomposeOutput = TransformcomposeVariousString | TransformcomposeVariousFile

Transformcompose = typing.TypedDict(
    "Transformcompose",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/transformcompose"]],
        "template": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TransformcomposeConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": list[InputPathType],
        "output": TransformcomposeOutput,
    },
)

@dataclasses.dataclass
class TransformcomposeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def transformcompose_params(
    input_: list[InputPathType],
    output: TransformcomposeOutput,
    template: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TransformcomposeConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Transformcompose:
    """
    Build parameters.

    Args:
        input_: the input transforms (either linear or non-linear warps).
        output: the output file (may be a linear transformation text file, or a\
            deformation warp field image, depending on usage)
        template: define the output grid defined by a template image
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Transformcompose = {
        "@type": "mrtrix/transformcompose",
        "input": input_,
        "output": output,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if template is not None:
        params["template"] = template
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def transformcompose_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Transformcompose."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TransformcomposeConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], list):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `list[InputPathType]`')
    for e_2 in params["input"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["output"])}\'')
    if "@type" not in params["output"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["output"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `output`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["output"]["@type"] == "VariousString":
        if params["output"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["output"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["output"]["obj"])}` expected `str`')
    elif params["output"]["@type"] == "VariousFile":
        if params["output"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["output"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["output"]["obj"])}` expected `InputPathType`')

def transformcompose_cargs(params: Transformcompose, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("transformcompose")
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item1 in params["input"]:
        cargs.append(execution.input_file(item1))
    if params["output"]["@type"] == "VariousString":
        cargs.append(str(params["output"]["obj"]))
    elif params["output"]["@type"] == "VariousFile":
        cargs.append(str(params["output"]["obj"]))
    return cargs

def transformcompose_outputs(params: Transformcompose, execution: Execution) -> TransformcomposeOutputs:
    """Build the TransformcomposeOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return TransformcomposeOutputs(
        root=root_v,
    )

def transformcompose_execute(params: Transformcompose, runner: Runner | None = None) -> TransformcomposeOutputs:
    """
    transformcompose

    Compose any number of linear transformations and/or warps into a single transformation.

Any linear transforms must be supplied as a 4x4 matrix in a text file (e.g. as per the output of mrregister). Any warp fields must be supplied as a 4D image representing a deformation field (e.g. as output from mrrregister -nl_warp).

Input transformations should be provided to the command in the order in which they would be applied to an image if they were to be applied individually.

If all input transformations are linear, and the -template option is not provided, then the file output by the command will also be a linear transformation saved as a 4x4 matrix in a text file. If a template image is supplied, then the output will always be a deformation field. If at least one of the inputs is a warp field, then the output will be a deformation field, which will be defined on the grid of the last input warp image supplied if the -template option is not used.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    transformcompose_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TRANSFORMCOMPOSE_METADATA)
    execution.params(params)
    args = transformcompose_cargs(params, execution)
    out = transformcompose_outputs(params, execution)
    execution.run(args)
    return out

def transformcompose(
    input_: list[InputPathType],
    output: TransformcomposeOutput,
    template: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TransformcomposeConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TransformcomposeOutputs:
    """
    transformcompose

    Compose any number of linear transformations and/or warps into a single transformation.

Any linear transforms must be supplied as a 4x4 matrix in a text file (e.g. as per the output of mrregister). Any warp fields must be supplied as a 4D image representing a deformation field (e.g. as output from mrrregister -nl_warp).

Input transformations should be provided to the command in the order in which they would be applied to an image if they were to be applied individually.

If all input transformations are linear, and the -template option is not provided, then the file output by the command will also be a linear transformation saved as a 4x4 matrix in a text file. If a template image is supplied, then the output will always be a deformation field. If at least one of the inputs is a warp field, then the output will be a deformation field, which will be defined on the grid of the last input warp image supplied if the -template option is not used.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input transforms (either linear or non-linear warps).
        output: the output file (may be a linear transformation text file, or a\
            deformation warp field image, depending on usage)
        template: define the output grid defined by a template image
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = transformcompose_params(
        input_=input_,
        output=output,
        template=template,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return transformcompose_execute(params, runner)

__all__ = [
    "Transformcompose",
    "TransformcomposeOutputs",
    "TRANSFORMCOMPOSE_METADATA",
    "transformcompose_cargs",
    "transformcompose_outputs",
    "transformcompose_params",
    "transformcompose_execute",
    "transformcompose_validate",
    "transformcompose",
]