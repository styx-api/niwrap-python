# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MTNORMALISE_METADATA = Metadata(
    id="mtnormalise",
    name="mtnormalise",
    package="mrtrix",
)

class MtnormaliseConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

class MtnormaliseInputOutput(typing.TypedDict):
    input: InputPathType
    """input tissue compartment image."""
    output: str
    """output normalised tissue compartment image."""

Mtnormalise = typing.TypedDict(
    "Mtnormalise",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mtnormalise"]],
        "mask": InputPathType,
        "order": typing.NotRequired[str],
        "niter": typing.NotRequired[list[int]],
        "reference": typing.NotRequired[float],
        "balanced": typing.NotRequired[bool],
        "check_norm": typing.NotRequired[str],
        "check_mask": typing.NotRequired[str],
        "check_factors": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MtnormaliseConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input_output": list[MtnormaliseInputOutput],
    },
)

@dataclasses.dataclass
class MtnormaliseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    check_norm: OutputPathType | None
    """output the final estimated spatially varying intensity level that is used for normalisation. """
    check_mask: OutputPathType | None
    """output the final mask used to compute the normalisation. This mask excludes regions identified as outliers by the optimisation process. """
    check_factors: OutputPathType | None
    """output the tissue balance factors computed during normalisation. """

def mtnormalise_params(
    mask: InputPathType,
    input_output: list[MtnormaliseInputOutput],
    order: str | None = None,
    niter: list[int] | None = None,
    reference: float | None = None,
    balanced: bool = False,
    check_norm: str | None = None,
    check_mask: str | None = None,
    check_factors: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MtnormaliseConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mtnormalise:
    """
    Build parameters.

    Args:
        mask: the mask defines the data used to compute the intensity\
            normalisation. This option is mandatory.
        input_output: list of all input and output tissue compartment files (see\
            example usage).
        order: the maximum order of the polynomial basis used to fit the\
            normalisation field in the log-domain. An order of 0 is equivalent to\
            not allowing spatial variance of the intensity normalisation factor.\
            (default: 3)
        niter: set the number of iterations. The first (and potentially only) entry\
            applies to the main loop. If supplied as a comma-separated list of\
            integers, the second entry applies to the inner loop to update the\
            balance factors (default: 15,7).
        reference: specify the (positive) reference value to which the summed\
            tissue compartments will be normalised. (default: 0.282095, SH DC term\
            for unit angular integral)
        balanced: incorporate the per-tissue balancing factors into scaling of the\
            output images (NOTE: use of this option has critical consequences for\
            AFD intensity normalisation; should not be used unless these\
            consequences are fully understood)
        check_norm: output the final estimated spatially varying intensity level\
            that is used for normalisation.
        check_mask: output the final mask used to compute the normalisation. This\
            mask excludes regions identified as outliers by the optimisation\
            process.
        check_factors: output the tissue balance factors computed during\
            normalisation.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mtnormalise = {
        "@type": "mrtrix/mtnormalise",
        "mask": mask,
        "input_output": input_output,
        "balanced": balanced,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if order is not None:
        params["order"] = order
    if niter is not None:
        params["niter"] = niter
    if reference is not None:
        params["reference"] = reference
    if check_norm is not None:
        params["check_norm"] = check_norm
    if check_mask is not None:
        params["check_mask"] = check_mask
    if check_factors is not None:
        params["check_factors"] = check_factors
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mtnormalise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mtnormalise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("order", None) is not None:
        if not isinstance(params["order"], str):
            raise StyxValidationError(f'`order` has the wrong type: Received `{type(params["order"])}` expected `str`')
    if params.get("niter", None) is not None:
        if not isinstance(params["niter"], list):
            raise StyxValidationError(f'`niter` has the wrong type: Received `{type(params["niter"])}` expected `list[int]`')
        for e in params["niter"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`niter` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("reference", None) is not None:
        if not isinstance(params["reference"], (float, int)):
            raise StyxValidationError(f'`reference` has the wrong type: Received `{type(params["reference"])}` expected `float`')
    if params.get("balanced", None) is not None:
        if not isinstance(params["balanced"], bool):
            raise StyxValidationError(f'`balanced` has the wrong type: Received `{type(params["balanced"])}` expected `bool`')
    if params.get("check_norm", None) is not None:
        if not isinstance(params["check_norm"], str):
            raise StyxValidationError(f'`check_norm` has the wrong type: Received `{type(params["check_norm"])}` expected `str`')
    if params.get("check_mask", None) is not None:
        if not isinstance(params["check_mask"], str):
            raise StyxValidationError(f'`check_mask` has the wrong type: Received `{type(params["check_mask"])}` expected `str`')
    if params.get("check_factors", None) is not None:
        if not isinstance(params["check_factors"], str):
            raise StyxValidationError(f'`check_factors` has the wrong type: Received `{type(params["check_factors"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MtnormaliseConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input_output", None) is None:
        raise StyxValidationError("`input_output` must not be None")
    if not isinstance(params["input_output"], list):
        raise StyxValidationError(f'`input_output` has the wrong type: Received `{type(params["input_output"])}` expected `list[MtnormaliseInputOutput]`')
    for e_3 in params["input_output"]:
        if not isinstance(e_3, dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
        if e_3.get("input", None) is None:
            raise StyxValidationError("`input` must not be None")
        if not isinstance(e_3["input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(e_3["input"])}` expected `InputPathType`')
        if e_3.get("output", None) is None:
            raise StyxValidationError("`output` must not be None")
        if not isinstance(e_3["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(e_3["output"])}` expected `str`')

def mtnormalise_cargs(params: Mtnormalise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mtnormalise")
    cargs.append("-mask")
    cargs.append(execution.input_file(params["mask"]))
    v_0 = params.get("order")
    if v_0 is not None:
        cargs.append("-order")
        cargs.append(v_0)
    v_1 = params.get("niter")
    if v_1 is not None:
        cargs.append("-niter")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    v_2 = params.get("reference")
    if v_2 is not None:
        cargs.append("-reference")
        cargs.append(str(v_2))
    if params.get("balanced"):
        cargs.append("-balanced")
    v_3 = params.get("check_norm")
    if v_3 is not None:
        cargs.append("-check_norm")
        cargs.append(v_3)
    v_4 = params.get("check_mask")
    if v_4 is not None:
        cargs.append("-check_mask")
        cargs.append(v_4)
    v_5 = params.get("check_factors")
    if v_5 is not None:
        cargs.append("-check_factors")
        cargs.append(v_5)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_6 = params.get("nthreads")
    if v_6 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        for item1 in v_7:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item2 in params["input_output"]:
        cargs.append(execution.input_file(item2["input"]))
        cargs.append(item2["output"])
    return cargs

def mtnormalise_outputs(params: Mtnormalise, execution: Execution) -> MtnormaliseOutputs:
    """Build the MtnormaliseOutputs object for this tool."""
    check_norm_v: OutputPathType | None = None
    check_mask_v: OutputPathType | None = None
    check_factors_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("check_norm")
    if __v0 is not None:
        check_norm_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("check_mask")
    if __v1 is not None:
        check_mask_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("check_factors")
    if __v2 is not None:
        check_factors_v = execution.output_file((__v2 if __v2 is not None else ""))
    return MtnormaliseOutputs(
        root=root_v,
        check_norm=check_norm_v,
        check_mask=check_mask_v,
        check_factors=check_factors_v,
    )

def mtnormalise_execute(params: Mtnormalise, runner: Runner | None = None) -> MtnormaliseOutputs:
    """
    mtnormalise

    Multi-tissue informed log-domain intensity normalisation.

This command takes as input any number of tissue components (e.g. from multi-tissue CSD) and outputs corresponding normalised tissue components corrected for the effects of (residual) intensity inhomogeneities. Intensity normalisation is performed by optimising the voxel-wise sum of all tissue compartments towards a constant value, under constraints of spatial smoothness (polynomial basis of a given order). Different to the Raffelt et al. 2017 abstract, this algorithm performs this task in the log-domain instead, with added gradual outlier rejection, different handling of the balancing factors between tissue compartments and a different iteration structure.

The -mask option is mandatory and is optimally provided with a brain mask (such as the one obtained from dwi2mask earlier in the processing pipeline). Outlier areas with exceptionally low or high combined tissue contributions are accounted for and reoptimised as the intensity inhomogeneity estimation becomes more accurate.

References:

Raffelt, D.; Dhollander, T.; Tournier, J.-D.; Tabbara, R.; Smith, R. E.; Pierre, E. & Connelly, A. Bias Field Correction and Intensity Normalisation for Quantitative Analysis of Apparent Fibre Density. In Proc. ISMRM, 2017, 26, 3541

Dhollander, T.; Tabbara, R.; Rosnarho-Tornstrand, J.; Tournier, J.-D.; Raffelt, D. & Connelly, A. Multi-tissue log-domain intensity and inhomogeneity normalisation for quantitative apparent fibre density. In Proc. ISMRM, 2021, 29, 2472

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mtnormalise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MTNORMALISE_METADATA)
    execution.params(params)
    args = mtnormalise_cargs(params, execution)
    out = mtnormalise_outputs(params, execution)
    execution.run(args)
    return out

def mtnormalise(
    mask: InputPathType,
    input_output: list[MtnormaliseInputOutput],
    order: str | None = None,
    niter: list[int] | None = None,
    reference: float | None = None,
    balanced: bool = False,
    check_norm: str | None = None,
    check_mask: str | None = None,
    check_factors: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MtnormaliseConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MtnormaliseOutputs:
    """
    mtnormalise

    Multi-tissue informed log-domain intensity normalisation.

This command takes as input any number of tissue components (e.g. from multi-tissue CSD) and outputs corresponding normalised tissue components corrected for the effects of (residual) intensity inhomogeneities. Intensity normalisation is performed by optimising the voxel-wise sum of all tissue compartments towards a constant value, under constraints of spatial smoothness (polynomial basis of a given order). Different to the Raffelt et al. 2017 abstract, this algorithm performs this task in the log-domain instead, with added gradual outlier rejection, different handling of the balancing factors between tissue compartments and a different iteration structure.

The -mask option is mandatory and is optimally provided with a brain mask (such as the one obtained from dwi2mask earlier in the processing pipeline). Outlier areas with exceptionally low or high combined tissue contributions are accounted for and reoptimised as the intensity inhomogeneity estimation becomes more accurate.

References:

Raffelt, D.; Dhollander, T.; Tournier, J.-D.; Tabbara, R.; Smith, R. E.; Pierre, E. & Connelly, A. Bias Field Correction and Intensity Normalisation for Quantitative Analysis of Apparent Fibre Density. In Proc. ISMRM, 2017, 26, 3541

Dhollander, T.; Tabbara, R.; Rosnarho-Tornstrand, J.; Tournier, J.-D.; Raffelt, D. & Connelly, A. Multi-tissue log-domain intensity and inhomogeneity normalisation for quantitative apparent fibre density. In Proc. ISMRM, 2021, 29, 2472

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        mask: the mask defines the data used to compute the intensity\
            normalisation. This option is mandatory.
        input_output: list of all input and output tissue compartment files (see\
            example usage).
        order: the maximum order of the polynomial basis used to fit the\
            normalisation field in the log-domain. An order of 0 is equivalent to\
            not allowing spatial variance of the intensity normalisation factor.\
            (default: 3)
        niter: set the number of iterations. The first (and potentially only) entry\
            applies to the main loop. If supplied as a comma-separated list of\
            integers, the second entry applies to the inner loop to update the\
            balance factors (default: 15,7).
        reference: specify the (positive) reference value to which the summed\
            tissue compartments will be normalised. (default: 0.282095, SH DC term\
            for unit angular integral)
        balanced: incorporate the per-tissue balancing factors into scaling of the\
            output images (NOTE: use of this option has critical consequences for\
            AFD intensity normalisation; should not be used unless these\
            consequences are fully understood)
        check_norm: output the final estimated spatially varying intensity level\
            that is used for normalisation.
        check_mask: output the final mask used to compute the normalisation. This\
            mask excludes regions identified as outliers by the optimisation\
            process.
        check_factors: output the tissue balance factors computed during\
            normalisation.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mtnormalise_params(
        mask=mask,
        input_output=input_output,
        order=order,
        niter=niter,
        reference=reference,
        balanced=balanced,
        check_norm=check_norm,
        check_mask=check_mask,
        check_factors=check_factors,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mtnormalise_execute(params, runner)

__all__ = [
    "Mtnormalise",
    "MtnormaliseOutputs",
    "MTNORMALISE_METADATA",
    "mtnormalise_cargs",
    "mtnormalise_outputs",
    "mtnormalise_params",
    "mtnormalise_execute",
    "mtnormalise_validate",
    "mtnormalise",
]