# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DWIDENOISE_METADATA = Metadata(
    id="dwidenoise",
    name="dwidenoise",
    package="mrtrix",
)

class DwidenoiseConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Dwidenoise = typing.TypedDict(
    "Dwidenoise",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/dwidenoise"]],
        "mask": typing.NotRequired[InputPathType],
        "extent": typing.NotRequired[list[int]],
        "noise": typing.NotRequired[str],
        "datatype": typing.NotRequired[str],
        "estimator": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[DwidenoiseConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "dwi": InputPathType,
        "out": str,
    },
)

@dataclasses.dataclass
class DwidenoiseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output denoised DWI image."""
    noise: OutputPathType | None
    """The output noise map, i.e., the estimated noise level 'sigma' in the data. Note that on complex input data, this will be the total noise level across real and imaginary channels, so a scale factor sqrt(2) applies. """

def dwidenoise_params(
    dwi: InputPathType,
    out: str,
    mask: InputPathType | None = None,
    extent: list[int] | None = None,
    noise: str | None = None,
    datatype: str | None = None,
    estimator: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DwidenoiseConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Dwidenoise:
    """
    Build parameters.

    Args:
        dwi: the input diffusion-weighted image.
        out: the output denoised DWI image.
        mask: Only process voxels within the specified binary brain mask image.
        extent: Set the patch size of the denoising filter. By default, the command\
            will select the smallest isotropic patch size that exceeds the number\
            of DW images in the input data, e.g., 5x5x5 for data with <= 125 DWI\
            volumes, 7x7x7 for data with <= 343 DWI volumes, etc.
        noise: The output noise map, i.e., the estimated noise level 'sigma' in the\
            data. Note that on complex input data, this will be the total noise\
            level across real and imaginary channels, so a scale factor sqrt(2)\
            applies.
        datatype: Datatype for the eigenvalue decomposition (single or double\
            precision). For complex input data, this will select complex float32 or\
            complex float64 datatypes.
        estimator: Select the noise level estimator (default = Exp2), either: *\
            Exp1: the original estimator used in Veraart et al. (2016), or * Exp2:\
            the improved estimator introduced in Cordero-Grande et al. (2019).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Dwidenoise = {
        "@type": "mrtrix/dwidenoise",
        "dwi": dwi,
        "out": out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask is not None:
        params["mask"] = mask
    if extent is not None:
        params["extent"] = extent
    if noise is not None:
        params["noise"] = noise
    if datatype is not None:
        params["datatype"] = datatype
    if estimator is not None:
        params["estimator"] = estimator
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def dwidenoise_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dwidenoise."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("extent", None) is not None:
        if not isinstance(params["extent"], list):
            raise StyxValidationError(f'`extent` has the wrong type: Received `{type(params["extent"])}` expected `list[int]`')
        for e in params["extent"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`extent` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("noise", None) is not None:
        if not isinstance(params["noise"], str):
            raise StyxValidationError(f'`noise` has the wrong type: Received `{type(params["noise"])}` expected `str`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("estimator", None) is not None:
        if not isinstance(params["estimator"], str):
            raise StyxValidationError(f'`estimator` has the wrong type: Received `{type(params["estimator"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[DwidenoiseConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("dwi", None) is None:
        raise StyxValidationError("`dwi` must not be None")
    if not isinstance(params["dwi"], (pathlib.Path, str)):
        raise StyxValidationError(f'`dwi` has the wrong type: Received `{type(params["dwi"])}` expected `InputPathType`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def dwidenoise_cargs(params: Dwidenoise, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dwidenoise")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("extent")
    if v_1 is not None:
        cargs.append("-extent")
        for item0 in v_1:
            cargs.append(str(item0))
    v_2 = params.get("noise")
    if v_2 is not None:
        cargs.append("-noise")
        cargs.append(v_2)
    v_3 = params.get("datatype")
    if v_3 is not None:
        cargs.append("-datatype")
        cargs.append(v_3)
    v_4 = params.get("estimator")
    if v_4 is not None:
        cargs.append("-estimator")
        cargs.append(v_4)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item1 in v_6:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["dwi"]))
    cargs.append(params["out"])
    return cargs

def dwidenoise_outputs(params: Dwidenoise, execution: Execution) -> DwidenoiseOutputs:
    """Build the DwidenoiseOutputs object for this tool."""
    noise_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    __v0 = params.get("noise")
    if __v0 is not None:
        noise_v = execution.output_file((__v0 if __v0 is not None else ""))
    return DwidenoiseOutputs(
        root=root_v,
        out=out_v,
        noise=noise_v,
    )

def dwidenoise_execute(params: Dwidenoise, runner: Runner | None = None) -> DwidenoiseOutputs:
    """
    dwidenoise

    dMRI noise level estimation and denoising using Marchenko-Pastur PCA.

DWI data denoising and noise map estimation by exploiting data redundancy in the PCA domain using the prior knowledge that the eigenspectrum of random covariance matrices is described by the universal Marchenko-Pastur (MP) distribution. Fitting the MP distribution to the spectrum of patch-wise signal matrices hence provides an estimator of the noise level 'sigma', as was first shown in Veraart et al. (2016) and later improved in Cordero-Grande et al. (2019). This noise level estimate then determines the optimal cut-off for PCA denoising.

Important note: image denoising must be performed as the first step of the image processing pipeline. The routine will fail if interpolation or smoothing has been applied to the data prior to denoising.

Note that this function does not correct for non-Gaussian noise biases present in magnitude-reconstructed MRI images. If available, including the MRI phase data can reduce such non-Gaussian biases, and the command now supports complex input data.

References:

Veraart, J.; Novikov, D.S.; Christiaens, D.; Ades-aron, B.; Sijbers, J. & Fieremans, E. Denoising of diffusion MRI using random matrix theory. NeuroImage, 2016, 142, 394-406, doi: 10.1016/j.neuroimage.2016.08.016

Veraart, J.; Fieremans, E. & Novikov, D.S. Diffusion MRI noise mapping using random matrix theory. Magn. Res. Med., 2016, 76(5), 1582-1593, doi: 10.1002/mrm.26059

Cordero-Grande, L.; Christiaens, D.; Hutter, J.; Price, A.N.; Hajnal, J.V. Complex diffusion-weighted image estimation via matrix recovery under general noise models. NeuroImage, 2019, 200, 391-404, doi: 10.1016/j.neuroimage.2019.06.039

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dwidenoise_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DWIDENOISE_METADATA)
    execution.params(params)
    args = dwidenoise_cargs(params, execution)
    out = dwidenoise_outputs(params, execution)
    execution.run(args)
    return out

def dwidenoise(
    dwi: InputPathType,
    out: str,
    mask: InputPathType | None = None,
    extent: list[int] | None = None,
    noise: str | None = None,
    datatype: str | None = None,
    estimator: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[DwidenoiseConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> DwidenoiseOutputs:
    """
    dwidenoise

    dMRI noise level estimation and denoising using Marchenko-Pastur PCA.

DWI data denoising and noise map estimation by exploiting data redundancy in the PCA domain using the prior knowledge that the eigenspectrum of random covariance matrices is described by the universal Marchenko-Pastur (MP) distribution. Fitting the MP distribution to the spectrum of patch-wise signal matrices hence provides an estimator of the noise level 'sigma', as was first shown in Veraart et al. (2016) and later improved in Cordero-Grande et al. (2019). This noise level estimate then determines the optimal cut-off for PCA denoising.

Important note: image denoising must be performed as the first step of the image processing pipeline. The routine will fail if interpolation or smoothing has been applied to the data prior to denoising.

Note that this function does not correct for non-Gaussian noise biases present in magnitude-reconstructed MRI images. If available, including the MRI phase data can reduce such non-Gaussian biases, and the command now supports complex input data.

References:

Veraart, J.; Novikov, D.S.; Christiaens, D.; Ades-aron, B.; Sijbers, J. & Fieremans, E. Denoising of diffusion MRI using random matrix theory. NeuroImage, 2016, 142, 394-406, doi: 10.1016/j.neuroimage.2016.08.016

Veraart, J.; Fieremans, E. & Novikov, D.S. Diffusion MRI noise mapping using random matrix theory. Magn. Res. Med., 2016, 76(5), 1582-1593, doi: 10.1002/mrm.26059

Cordero-Grande, L.; Christiaens, D.; Hutter, J.; Price, A.N.; Hajnal, J.V. Complex diffusion-weighted image estimation via matrix recovery under general noise models. NeuroImage, 2019, 200, 391-404, doi: 10.1016/j.neuroimage.2019.06.039

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        dwi: the input diffusion-weighted image.
        out: the output denoised DWI image.
        mask: Only process voxels within the specified binary brain mask image.
        extent: Set the patch size of the denoising filter. By default, the command\
            will select the smallest isotropic patch size that exceeds the number\
            of DW images in the input data, e.g., 5x5x5 for data with <= 125 DWI\
            volumes, 7x7x7 for data with <= 343 DWI volumes, etc.
        noise: The output noise map, i.e., the estimated noise level 'sigma' in the\
            data. Note that on complex input data, this will be the total noise\
            level across real and imaginary channels, so a scale factor sqrt(2)\
            applies.
        datatype: Datatype for the eigenvalue decomposition (single or double\
            precision). For complex input data, this will select complex float32 or\
            complex float64 datatypes.
        estimator: Select the noise level estimator (default = Exp2), either: *\
            Exp1: the original estimator used in Veraart et al. (2016), or * Exp2:\
            the improved estimator introduced in Cordero-Grande et al. (2019).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dwidenoise_params(
        dwi=dwi,
        out=out,
        mask=mask,
        extent=extent,
        noise=noise,
        datatype=datatype,
        estimator=estimator,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return dwidenoise_execute(params, runner)

__all__ = [
    "Dwidenoise",
    "DwidenoiseOutputs",
    "DWIDENOISE_METADATA",
    "dwidenoise_cargs",
    "dwidenoise_outputs",
    "dwidenoise_params",
    "dwidenoise_execute",
    "dwidenoise_validate",
    "dwidenoise",
]