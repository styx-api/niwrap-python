# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKGEN_METADATA = Metadata(
    id="tckgen",
    name="tckgen",
    package="mrtrix",
)

class TckgenSeedRandomPerVoxel(typing.TypedDict):
    image: InputPathType
    """seed a fixed number of streamlines per voxel in a mask image; random placement of seeds in each voxel"""
    num_per_voxel: int
    """seed a fixed number of streamlines per voxel in a mask image; random placement of seeds in each voxel"""

class TckgenSeedGridPerVoxel(typing.TypedDict):
    image: InputPathType
    """seed a fixed number of streamlines per voxel in a mask image; place seeds on a 3D mesh grid (grid_size argument is per axis; so a grid_size of 3 results in 27 seeds per voxel)"""
    grid_size: int
    """seed a fixed number of streamlines per voxel in a mask image; place seeds on a 3D mesh grid (grid_size argument is per axis; so a grid_size of 3 results in 27 seeds per voxel)"""

TckgenVariousString = typing.TypedDict(
    "TckgenVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

TckgenVariousFile = typing.TypedDict(
    "TckgenVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

TckgenInclude = TckgenVariousString | TckgenVariousFile

class TckgenFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

class TckgenConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckgen = typing.TypedDict(
    "Tckgen",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckgen"]],
        "algorithm": typing.NotRequired[str],
        "select": typing.NotRequired[int],
        "step": typing.NotRequired[float],
        "angle": typing.NotRequired[float],
        "minlength": typing.NotRequired[float],
        "maxlength": typing.NotRequired[float],
        "cutoff": typing.NotRequired[float],
        "trials": typing.NotRequired[int],
        "noprecomputed": typing.NotRequired[bool],
        "rk4": typing.NotRequired[bool],
        "stop": typing.NotRequired[bool],
        "downsample": typing.NotRequired[int],
        "seed_image": typing.NotRequired[list[InputPathType]],
        "seed_sphere": typing.NotRequired[list[list[float]]],
        "seed_random_per_voxel": typing.NotRequired[list[TckgenSeedRandomPerVoxel]],
        "seed_grid_per_voxel": typing.NotRequired[list[TckgenSeedGridPerVoxel]],
        "seed_rejection": typing.NotRequired[list[InputPathType]],
        "seed_gmwmi": typing.NotRequired[list[InputPathType]],
        "seed_dynamic": typing.NotRequired[InputPathType],
        "seeds": typing.NotRequired[int],
        "max_attempts_per_seed": typing.NotRequired[int],
        "seed_cutoff": typing.NotRequired[float],
        "seed_unidirectional": typing.NotRequired[bool],
        "seed_direction": typing.NotRequired[list[float]],
        "output_seeds": typing.NotRequired[str],
        "include": typing.NotRequired[list[TckgenInclude]],
        "include_ordered": typing.NotRequired[list[str]],
        "exclude": typing.NotRequired[list[TckgenInclude]],
        "mask": typing.NotRequired[list[TckgenInclude]],
        "act": typing.NotRequired[InputPathType],
        "backtrack": typing.NotRequired[bool],
        "crop_at_gmwmi": typing.NotRequired[bool],
        "power": typing.NotRequired[float],
        "samples": typing.NotRequired[int],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[TckgenFslgrad],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckgenConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "source": InputPathType,
        "tracks": str,
    },
)

@dataclasses.dataclass
class TckgenOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tracks: OutputPathType
    """the output file containing the tracks generated."""
    output_seeds: OutputPathType | None
    """output the seed location of all successful streamlines to a file """

def tckgen_params(
    source: InputPathType,
    tracks: str,
    algorithm: str | None = None,
    select: int | None = None,
    step: float | None = None,
    angle: float | None = None,
    minlength: float | None = None,
    maxlength: float | None = None,
    cutoff: float | None = None,
    trials: int | None = None,
    noprecomputed: bool = False,
    rk4: bool = False,
    stop: bool = False,
    downsample: int | None = None,
    seed_image: list[InputPathType] | None = None,
    seed_sphere: list[list[float]] | None = None,
    seed_random_per_voxel: list[TckgenSeedRandomPerVoxel] | None = None,
    seed_grid_per_voxel: list[TckgenSeedGridPerVoxel] | None = None,
    seed_rejection: list[InputPathType] | None = None,
    seed_gmwmi: list[InputPathType] | None = None,
    seed_dynamic: InputPathType | None = None,
    seeds: int | None = None,
    max_attempts_per_seed: int | None = None,
    seed_cutoff: float | None = None,
    seed_unidirectional: bool = False,
    seed_direction: list[float] | None = None,
    output_seeds: str | None = None,
    include: list[TckgenInclude] | None = None,
    include_ordered: list[str] | None = None,
    exclude: list[TckgenInclude] | None = None,
    mask: list[TckgenInclude] | None = None,
    act: InputPathType | None = None,
    backtrack: bool = False,
    crop_at_gmwmi: bool = False,
    power: float | None = None,
    samples: int | None = None,
    grad: InputPathType | None = None,
    fslgrad: TckgenFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckgenConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckgen:
    """
    Build parameters.

    Args:
        source: The image containing the source data. The type of image data\
            required depends on the algorithm used (see Description section).
        tracks: the output file containing the tracks generated.
        algorithm: specify the tractography algorithm to use. Valid choices are:\
            FACT, iFOD1, iFOD2, Nulldist1, Nulldist2, SD_Stream, Seedtest,\
            Tensor_Det, Tensor_Prob (default: iFOD2).
        select: set the desired number of streamlines to be selected by tckgen,\
            after all selection criteria have been applied (i.e.\
            inclusion/exclusion ROIs, min/max length, etc). tckgen will keep\
            seeding streamlines until this number of streamlines have been\
            selected, or the maximum allowed number of seeds has been exceeded (see\
            -seeds option). By default, 5000 streamlines are to be selected. Set to\
            zero to disable, which will result in streamlines being seeded until\
            the number specified by -seeds has been reached.
        step: set the step size of the algorithm in mm (defaults: for first-order\
            algorithms, 0.1 x voxelsize; if using RK4, 0.25 x voxelsize; for iFOD2:\
            0.5 x voxelsize).
        angle: set the maximum angle in degrees between successive steps (defaults:\
            60 for deterministic algorithms; 15 for iFOD1 / nulldist1; 45 for iFOD2\
            / nulldist2)
        minlength: set the minimum length of any track in mm (defaults: without\
            ACT, 5 x voxelsize; with ACT, 2 x voxelsize).
        maxlength: set the maximum length of any track in mm (default: 100 x\
            voxelsize).
        cutoff: set the FOD amplitude / fixel size / tensor FA cutoff for\
            terminating tracks (defaults: 0.1 for FOD-based algorithms; 0.1 for\
            fixel-based algorithms; 0.1 for tensor-based algorithms; threshold\
            multiplied by 0.5 when using ACT).
        trials: set the maximum number of sampling trials at each point (only used\
            for iFOD1 / iFOD2) (default: 1000).
        noprecomputed: do NOT pre-compute legendre polynomial values. Warning: this\
            will slow down the algorithm by a factor of approximately 4.
        rk4: use 4th-order Runge-Kutta integration (slower, but eliminates\
            curvature overshoot in 1st-order deterministic methods)
        stop: stop propagating a streamline once it has traversed all include\
            regions
        downsample: downsample the generated streamlines to reduce output file size\
            (default is (samples-1) for iFOD2, no downsampling for all other\
            algorithms)
        seed_image: seed streamlines entirely at random within a mask image
        seed_sphere: spherical seed as four comma-separated values (XYZ position\
            and radius)
        seed_random_per_voxel: seed a fixed number of streamlines per voxel in a\
            mask image; random placement of seeds in each voxel
        seed_grid_per_voxel: seed a fixed number of streamlines per voxel in a mask\
            image; place seeds on a 3D mesh grid (grid_size argument is per axis;\
            so a grid_size of 3 results in 27 seeds per voxel)
        seed_rejection: seed from an image using rejection sampling (higher values\
            = more probable to seed from)
        seed_gmwmi: seed from the grey matter - white matter interface (only valid\
            if using ACT framework). Input image should be a 3D seeding volume;\
            seeds drawn within this image will be optimised to the interface using\
            the 5TT image provided using the -act option.
        seed_dynamic: determine seed points dynamically using the SIFT model (must\
            not provide any other seeding mechanism). Note that while this seeding\
            mechanism improves the distribution of reconstructed streamlines\
            density, it should NOT be used as a substitute for the SIFT method\
            itself.
        seeds: set the number of seeds that tckgen will attempt to track from. If\
            this option is NOT provided, the default number of seeds is set to\
            1000Ã— the number of selected streamlines. If -select is NOT also\
            specified, tckgen will continue tracking until this number of seeds has\
            been attempted. However, if -select is also specified, tckgen will stop\
            when the number of seeds attempted reaches the number specified here,\
            OR when the number of streamlines selected reaches the number requested\
            with the -select option. This can be used to prevent the program from\
            running indefinitely when no or very few streamlines can be found that\
            match the selection criteria. Setting this to zero will cause tckgen to\
            keep attempting seeds until the number specified by -select has been\
            reached.
        max_attempts_per_seed: set the maximum number of times that the tracking\
            algorithm should attempt to find an appropriate tracking direction from\
            a given seed point. This should be set high enough to ensure that an\
            actual plausible seed point is not discarded prematurely as being\
            unable to initiate tracking from. Higher settings may affect\
            performance if many seeds are genuinely impossible to track from, as\
            many attempts will still be made in vain for such seeds. (default:\
            1000)
        seed_cutoff: set the minimum FA or FOD amplitude for seeding tracks\
            (default is the same as the normal -cutoff).
        seed_unidirectional: track from the seed point in one direction only\
            (default is to track in both directions).
        seed_direction: specify a seeding direction for the tracking (this should\
            be supplied as a vector of 3 comma-separated values.
        output_seeds: output the seed location of all successful streamlines to a\
            file
        include: specify an inclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines must traverse ALL inclusion regions to be accepted.
        include_ordered: specify an inclusion region of interest, as either a\
            binary mask image, or as a sphere using 4 comma-separared values\
            (x,y,z,radius). Streamlines must traverse ALL inclusion_ordered regions\
            in the order they are specified in order to be accepted.
        exclude: specify an exclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines that enter ANY exclude region will be discarded.
        mask: specify a masking region of interest, as either a binary mask image,\
            or as a sphere using 4 comma-separared values (x,y,z,radius). If\
            defined, streamlines exiting the mask will be truncated.
        act: use the Anatomically-Constrained Tractography framework during\
            tracking; provided image must be in the 5TT (five-tissue-type) format
        backtrack: allow tracks to be truncated and re-tracked if a poor structural\
            termination is encountered
        crop_at_gmwmi: crop streamline endpoints more precisely as they cross the\
            GM-WM interface
        power: raise the FOD to the power specified (defaults are: 1.0 for iFOD1;\
            1.0/nsamples for iFOD2).
        samples: set the number of FOD samples to take per step (Default: 4).
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckgen = {
        "@type": "mrtrix/tckgen",
        "source": source,
        "tracks": tracks,
        "noprecomputed": noprecomputed,
        "rk4": rk4,
        "stop": stop,
        "seed_unidirectional": seed_unidirectional,
        "backtrack": backtrack,
        "crop_at_gmwmi": crop_at_gmwmi,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if algorithm is not None:
        params["algorithm"] = algorithm
    if select is not None:
        params["select"] = select
    if step is not None:
        params["step"] = step
    if angle is not None:
        params["angle"] = angle
    if minlength is not None:
        params["minlength"] = minlength
    if maxlength is not None:
        params["maxlength"] = maxlength
    if cutoff is not None:
        params["cutoff"] = cutoff
    if trials is not None:
        params["trials"] = trials
    if downsample is not None:
        params["downsample"] = downsample
    if seed_image is not None:
        params["seed_image"] = seed_image
    if seed_sphere is not None:
        params["seed_sphere"] = seed_sphere
    if seed_random_per_voxel is not None:
        params["seed_random_per_voxel"] = seed_random_per_voxel
    if seed_grid_per_voxel is not None:
        params["seed_grid_per_voxel"] = seed_grid_per_voxel
    if seed_rejection is not None:
        params["seed_rejection"] = seed_rejection
    if seed_gmwmi is not None:
        params["seed_gmwmi"] = seed_gmwmi
    if seed_dynamic is not None:
        params["seed_dynamic"] = seed_dynamic
    if seeds is not None:
        params["seeds"] = seeds
    if max_attempts_per_seed is not None:
        params["max_attempts_per_seed"] = max_attempts_per_seed
    if seed_cutoff is not None:
        params["seed_cutoff"] = seed_cutoff
    if seed_direction is not None:
        params["seed_direction"] = seed_direction
    if output_seeds is not None:
        params["output_seeds"] = output_seeds
    if include is not None:
        params["include"] = include
    if include_ordered is not None:
        params["include_ordered"] = include_ordered
    if exclude is not None:
        params["exclude"] = exclude
    if mask is not None:
        params["mask"] = mask
    if act is not None:
        params["act"] = act
    if power is not None:
        params["power"] = power
    if samples is not None:
        params["samples"] = samples
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckgen_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckgen."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("algorithm", None) is not None:
        if not isinstance(params["algorithm"], str):
            raise StyxValidationError(f'`algorithm` has the wrong type: Received `{type(params["algorithm"])}` expected `str`')
    if params.get("select", None) is not None:
        if not isinstance(params["select"], int):
            raise StyxValidationError(f'`select` has the wrong type: Received `{type(params["select"])}` expected `int`')
    if params.get("step", None) is not None:
        if not isinstance(params["step"], (float, int)):
            raise StyxValidationError(f'`step` has the wrong type: Received `{type(params["step"])}` expected `float`')
    if params.get("angle", None) is not None:
        if not isinstance(params["angle"], (float, int)):
            raise StyxValidationError(f'`angle` has the wrong type: Received `{type(params["angle"])}` expected `float`')
    if params.get("minlength", None) is not None:
        if not isinstance(params["minlength"], (float, int)):
            raise StyxValidationError(f'`minlength` has the wrong type: Received `{type(params["minlength"])}` expected `float`')
    if params.get("maxlength", None) is not None:
        if not isinstance(params["maxlength"], (float, int)):
            raise StyxValidationError(f'`maxlength` has the wrong type: Received `{type(params["maxlength"])}` expected `float`')
    if params.get("cutoff", None) is not None:
        if not isinstance(params["cutoff"], (float, int)):
            raise StyxValidationError(f'`cutoff` has the wrong type: Received `{type(params["cutoff"])}` expected `float`')
    if params.get("trials", None) is not None:
        if not isinstance(params["trials"], int):
            raise StyxValidationError(f'`trials` has the wrong type: Received `{type(params["trials"])}` expected `int`')
    if params.get("noprecomputed", None) is not None:
        if not isinstance(params["noprecomputed"], bool):
            raise StyxValidationError(f'`noprecomputed` has the wrong type: Received `{type(params["noprecomputed"])}` expected `bool`')
    if params.get("rk4", None) is not None:
        if not isinstance(params["rk4"], bool):
            raise StyxValidationError(f'`rk4` has the wrong type: Received `{type(params["rk4"])}` expected `bool`')
    if params.get("stop", None) is not None:
        if not isinstance(params["stop"], bool):
            raise StyxValidationError(f'`stop` has the wrong type: Received `{type(params["stop"])}` expected `bool`')
    if params.get("downsample", None) is not None:
        if not isinstance(params["downsample"], int):
            raise StyxValidationError(f'`downsample` has the wrong type: Received `{type(params["downsample"])}` expected `int`')
    if params.get("seed_image", None) is not None:
        if not isinstance(params["seed_image"], list):
            raise StyxValidationError(f'`seed_image` has the wrong type: Received `{type(params["seed_image"])}` expected `list[InputPathType]`')
        for e in params["seed_image"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`seed_image` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("seed_sphere", None) is not None:
        if not isinstance(params["seed_sphere"], list):
            raise StyxValidationError(f'`seed_sphere` has the wrong type: Received `{type(params["seed_sphere"])}` expected `list[list[float]]`')
        for e_2 in params["seed_sphere"]:
            if not isinstance(e_2, list):
                raise StyxValidationError(f'`seed_sphere` has the wrong type: Received `{type(e_2)}` expected `list[list[float]]`')
            for e_3 in e_2:
                if not isinstance(e_3, (float, int)):
                    raise StyxValidationError(f'`seed_sphere` has the wrong type: Received `{type(e_3)}` expected `list[list[float]]`')
    if params.get("seed_random_per_voxel", None) is not None:
        if not isinstance(params["seed_random_per_voxel"], list):
            raise StyxValidationError(f'`seed_random_per_voxel` has the wrong type: Received `{type(params["seed_random_per_voxel"])}` expected `list[TckgenSeedRandomPerVoxel]`')
        for e_4 in params["seed_random_per_voxel"]:
            if not isinstance(e_4, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_4)}\'')
            if e_4.get("image", None) is None:
                raise StyxValidationError("`image` must not be None")
            if not isinstance(e_4["image"], (pathlib.Path, str)):
                raise StyxValidationError(f'`image` has the wrong type: Received `{type(e_4["image"])}` expected `InputPathType`')
            if e_4.get("num_per_voxel", None) is None:
                raise StyxValidationError("`num_per_voxel` must not be None")
            if not isinstance(e_4["num_per_voxel"], int):
                raise StyxValidationError(f'`num_per_voxel` has the wrong type: Received `{type(e_4["num_per_voxel"])}` expected `int`')
    if params.get("seed_grid_per_voxel", None) is not None:
        if not isinstance(params["seed_grid_per_voxel"], list):
            raise StyxValidationError(f'`seed_grid_per_voxel` has the wrong type: Received `{type(params["seed_grid_per_voxel"])}` expected `list[TckgenSeedGridPerVoxel]`')
        for e_5 in params["seed_grid_per_voxel"]:
            if not isinstance(e_5, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_5)}\'')
            if e_5.get("image", None) is None:
                raise StyxValidationError("`image` must not be None")
            if not isinstance(e_5["image"], (pathlib.Path, str)):
                raise StyxValidationError(f'`image` has the wrong type: Received `{type(e_5["image"])}` expected `InputPathType`')
            if e_5.get("grid_size", None) is None:
                raise StyxValidationError("`grid_size` must not be None")
            if not isinstance(e_5["grid_size"], int):
                raise StyxValidationError(f'`grid_size` has the wrong type: Received `{type(e_5["grid_size"])}` expected `int`')
    if params.get("seed_rejection", None) is not None:
        if not isinstance(params["seed_rejection"], list):
            raise StyxValidationError(f'`seed_rejection` has the wrong type: Received `{type(params["seed_rejection"])}` expected `list[InputPathType]`')
        for e_6 in params["seed_rejection"]:
            if not isinstance(e_6, (pathlib.Path, str)):
                raise StyxValidationError(f'`seed_rejection` has the wrong type: Received `{type(e_6)}` expected `list[InputPathType]`')
    if params.get("seed_gmwmi", None) is not None:
        if not isinstance(params["seed_gmwmi"], list):
            raise StyxValidationError(f'`seed_gmwmi` has the wrong type: Received `{type(params["seed_gmwmi"])}` expected `list[InputPathType]`')
        for e_7 in params["seed_gmwmi"]:
            if not isinstance(e_7, (pathlib.Path, str)):
                raise StyxValidationError(f'`seed_gmwmi` has the wrong type: Received `{type(e_7)}` expected `list[InputPathType]`')
    if params.get("seed_dynamic", None) is not None:
        if not isinstance(params["seed_dynamic"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seed_dynamic` has the wrong type: Received `{type(params["seed_dynamic"])}` expected `InputPathType`')
    if params.get("seeds", None) is not None:
        if not isinstance(params["seeds"], int):
            raise StyxValidationError(f'`seeds` has the wrong type: Received `{type(params["seeds"])}` expected `int`')
    if params.get("max_attempts_per_seed", None) is not None:
        if not isinstance(params["max_attempts_per_seed"], int):
            raise StyxValidationError(f'`max_attempts_per_seed` has the wrong type: Received `{type(params["max_attempts_per_seed"])}` expected `int`')
    if params.get("seed_cutoff", None) is not None:
        if not isinstance(params["seed_cutoff"], (float, int)):
            raise StyxValidationError(f'`seed_cutoff` has the wrong type: Received `{type(params["seed_cutoff"])}` expected `float`')
    if params.get("seed_unidirectional", None) is not None:
        if not isinstance(params["seed_unidirectional"], bool):
            raise StyxValidationError(f'`seed_unidirectional` has the wrong type: Received `{type(params["seed_unidirectional"])}` expected `bool`')
    if params.get("seed_direction", None) is not None:
        if not isinstance(params["seed_direction"], list):
            raise StyxValidationError(f'`seed_direction` has the wrong type: Received `{type(params["seed_direction"])}` expected `list[float]`')
        for e_8 in params["seed_direction"]:
            if not isinstance(e_8, (float, int)):
                raise StyxValidationError(f'`seed_direction` has the wrong type: Received `{type(e_8)}` expected `list[float]`')
    if params.get("output_seeds", None) is not None:
        if not isinstance(params["output_seeds"], str):
            raise StyxValidationError(f'`output_seeds` has the wrong type: Received `{type(params["output_seeds"])}` expected `str`')
    if params.get("include", None) is not None:
        if not isinstance(params["include"], list):
            raise StyxValidationError(f'`include` has the wrong type: Received `{type(params["include"])}` expected `list[TckgenInclude]`')
        for e_9 in params["include"]:
            if not isinstance(e_9, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_9)}\'')
            if "@type" not in e_9:
                raise StyxValidationError("Params object is missing `@type`")
            if e_9["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `include`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e_9["@type"] == "VariousString":
                if e_9.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_9["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_9["obj"])}` expected `str`')
            elif e_9["@type"] == "VariousFile":
                if e_9.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_9["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_9["obj"])}` expected `InputPathType`')
    if params.get("include_ordered", None) is not None:
        if not isinstance(params["include_ordered"], list):
            raise StyxValidationError(f'`include_ordered` has the wrong type: Received `{type(params["include_ordered"])}` expected `list[str]`')
        for e_10 in params["include_ordered"]:
            if not isinstance(e_10, str):
                raise StyxValidationError(f'`include_ordered` has the wrong type: Received `{type(e_10)}` expected `list[str]`')
    if params.get("exclude", None) is not None:
        if not isinstance(params["exclude"], list):
            raise StyxValidationError(f'`exclude` has the wrong type: Received `{type(params["exclude"])}` expected `list[TckgenInclude]`')
        for e_11 in params["exclude"]:
            if not isinstance(e_11, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_11)}\'')
            if "@type" not in e_11:
                raise StyxValidationError("Params object is missing `@type`")
            if e_11["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `exclude`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e_11["@type"] == "VariousString":
                if e_11.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_11["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_11["obj"])}` expected `str`')
            elif e_11["@type"] == "VariousFile":
                if e_11.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_11["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_11["obj"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], list):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `list[TckgenInclude]`')
        for e_12 in params["mask"]:
            if not isinstance(e_12, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_12)}\'')
            if "@type" not in e_12:
                raise StyxValidationError("Params object is missing `@type`")
            if e_12["@type"] not in ["VariousString", "VariousFile"]:
                raise StyxValidationError("Parameter `mask`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
            if e_12["@type"] == "VariousString":
                if e_12.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_12["obj"], str):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_12["obj"])}` expected `str`')
            elif e_12["@type"] == "VariousFile":
                if e_12.get("obj", None) is None:
                    raise StyxValidationError("`obj` must not be None")
                if not isinstance(e_12["obj"], (pathlib.Path, str)):
                    raise StyxValidationError(f'`obj` has the wrong type: Received `{type(e_12["obj"])}` expected `InputPathType`')
    if params.get("act", None) is not None:
        if not isinstance(params["act"], (pathlib.Path, str)):
            raise StyxValidationError(f'`act` has the wrong type: Received `{type(params["act"])}` expected `InputPathType`')
    if params.get("backtrack", None) is not None:
        if not isinstance(params["backtrack"], bool):
            raise StyxValidationError(f'`backtrack` has the wrong type: Received `{type(params["backtrack"])}` expected `bool`')
    if params.get("crop_at_gmwmi", None) is not None:
        if not isinstance(params["crop_at_gmwmi"], bool):
            raise StyxValidationError(f'`crop_at_gmwmi` has the wrong type: Received `{type(params["crop_at_gmwmi"])}` expected `bool`')
    if params.get("power", None) is not None:
        if not isinstance(params["power"], (float, int)):
            raise StyxValidationError(f'`power` has the wrong type: Received `{type(params["power"])}` expected `float`')
    if params.get("samples", None) is not None:
        if not isinstance(params["samples"], int):
            raise StyxValidationError(f'`samples` has the wrong type: Received `{type(params["samples"])}` expected `int`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckgenConfig]`')
        for e_13 in params["config"]:
            if not isinstance(e_13, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_13)}\'')
            if e_13.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_13["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_13["key"])}` expected `str`')
            if e_13.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_13["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_13["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], str):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `str`')

def tckgen_cargs(params: Tckgen, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckgen")
    v_0 = params.get("algorithm")
    if v_0 is not None:
        cargs.append("-algorithm")
        cargs.append(v_0)
    v_1 = params.get("select")
    if v_1 is not None:
        cargs.append("-select")
        cargs.append(str(v_1))
    v_2 = params.get("step")
    if v_2 is not None:
        cargs.append("-step")
        cargs.append(str(v_2))
    v_3 = params.get("angle")
    if v_3 is not None:
        cargs.append("-angle")
        cargs.append(str(v_3))
    v_4 = params.get("minlength")
    if v_4 is not None:
        cargs.append("-minlength")
        cargs.append(str(v_4))
    v_5 = params.get("maxlength")
    if v_5 is not None:
        cargs.append("-maxlength")
        cargs.append(str(v_5))
    v_6 = params.get("cutoff")
    if v_6 is not None:
        cargs.append("-cutoff")
        cargs.append(str(v_6))
    v_7 = params.get("trials")
    if v_7 is not None:
        cargs.append("-trials")
        cargs.append(str(v_7))
    if params.get("noprecomputed"):
        cargs.append("-noprecomputed")
    if params.get("rk4"):
        cargs.append("-rk4")
    if params.get("stop"):
        cargs.append("-stop")
    v_8 = params.get("downsample")
    if v_8 is not None:
        cargs.append("-downsample")
        cargs.append(str(v_8))
    v_9 = params.get("seed_image")
    if v_9 is not None:
        for item0 in v_9:
            cargs.append("-seed_image")
            cargs.append(execution.input_file(item0))
    v_10 = params.get("seed_sphere")
    if v_10 is not None:
        for item1 in v_10:
            cargs.append("-seed_sphere")
            cargs.append(",".join([str(item2) for item2 in item1]))
    v_11 = params.get("seed_random_per_voxel")
    if v_11 is not None:
        for item3 in v_11:
            cargs.append("-seed_random_per_voxel")
            cargs.append(execution.input_file(item3["image"]))
            cargs.append(str(item3["num_per_voxel"]))
    v_12 = params.get("seed_grid_per_voxel")
    if v_12 is not None:
        for item4 in v_12:
            cargs.append("-seed_grid_per_voxel")
            cargs.append(execution.input_file(item4["image"]))
            cargs.append(str(item4["grid_size"]))
    v_13 = params.get("seed_rejection")
    if v_13 is not None:
        for item5 in v_13:
            cargs.append("-seed_rejection")
            cargs.append(execution.input_file(item5))
    v_14 = params.get("seed_gmwmi")
    if v_14 is not None:
        for item6 in v_14:
            cargs.append("-seed_gmwmi")
            cargs.append(execution.input_file(item6))
    v_15 = params.get("seed_dynamic")
    if v_15 is not None:
        cargs.append("-seed_dynamic")
        cargs.append(execution.input_file(v_15))
    v_16 = params.get("seeds")
    if v_16 is not None:
        cargs.append("-seeds")
        cargs.append(str(v_16))
    v_17 = params.get("max_attempts_per_seed")
    if v_17 is not None:
        cargs.append("-max_attempts_per_seed")
        cargs.append(str(v_17))
    v_18 = params.get("seed_cutoff")
    if v_18 is not None:
        cargs.append("-seed_cutoff")
        cargs.append(str(v_18))
    if params.get("seed_unidirectional"):
        cargs.append("-seed_unidirectional")
    v_19 = params.get("seed_direction")
    if v_19 is not None:
        cargs.append("-seed_direction")
        cargs.append(",".join([str(item7) for item7 in v_19]))
    v_20 = params.get("output_seeds")
    if v_20 is not None:
        cargs.append("-output_seeds")
        cargs.append(v_20)
    v_21 = params.get("include")
    if v_21 is not None:
        for item8 in v_21:
            cargs.append("-include")
            if item8["@type"] == "VariousString":
                cargs.append(str(item8["obj"]))
            elif item8["@type"] == "VariousFile":
                cargs.append(str(item8["obj"]))
    v_22 = params.get("include_ordered")
    if v_22 is not None:
        for item9 in v_22:
            cargs.append("-include_ordered")
            cargs.append(item9)
    v_23 = params.get("exclude")
    if v_23 is not None:
        for item10 in v_23:
            cargs.append("-exclude")
            if item10["@type"] == "VariousString":
                cargs.append(str(item10["obj"]))
            elif item10["@type"] == "VariousFile":
                cargs.append(str(item10["obj"]))
    v_24 = params.get("mask")
    if v_24 is not None:
        for item11 in v_24:
            cargs.append("-mask")
            if item11["@type"] == "VariousString":
                cargs.append(str(item11["obj"]))
            elif item11["@type"] == "VariousFile":
                cargs.append(str(item11["obj"]))
    v_25 = params.get("act")
    if v_25 is not None:
        cargs.append("-act")
        cargs.append(execution.input_file(v_25))
    if params.get("backtrack"):
        cargs.append("-backtrack")
    if params.get("crop_at_gmwmi"):
        cargs.append("-crop_at_gmwmi")
    v_26 = params.get("power")
    if v_26 is not None:
        cargs.append("-power")
        cargs.append(str(v_26))
    v_27 = params.get("samples")
    if v_27 is not None:
        cargs.append("-samples")
        cargs.append(str(v_27))
    v_28 = params.get("grad")
    if v_28 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_28))
    v_29 = params.get("fslgrad")
    if v_29 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_29["bvecs"]))
        cargs.append(execution.input_file(v_29["bvals"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_30 = params.get("nthreads")
    if v_30 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_30))
    v_31 = params.get("config")
    if v_31 is not None:
        for item12 in v_31:
            cargs.append("-config")
            cargs.append(item12["key"])
            cargs.append(item12["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(params["tracks"])
    return cargs

def tckgen_outputs(params: Tckgen, execution: Execution) -> TckgenOutputs:
    """Build the TckgenOutputs object for this tool."""
    output_seeds_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    tracks_v: OutputPathType = execution.output_file(params["tracks"])
    __v0 = params.get("output_seeds")
    if __v0 is not None:
        output_seeds_v = execution.output_file((__v0 if __v0 is not None else ""))
    return TckgenOutputs(
        root=root_v,
        tracks=tracks_v,
        output_seeds=output_seeds_v,
    )

def tckgen_execute(params: Tckgen, runner: Runner | None = None) -> TckgenOutputs:
    """
    tckgen

    Perform streamlines tractography.

By default, tckgen produces a fixed number of streamlines, by attempting to seed from new random locations until the target number of streamlines have been selected (in other words, after all inclusion & exclusion criteria have been applied), or the maximum number of seeds has been exceeded (by default, this is 1000 x the desired number of selected streamlines). Use the -select and/or -seeds options to modify as required. See also the Seeding options section for alternative seeding strategies.

Below is a list of available tracking algorithms, the input image data that they require, and a brief description of their behaviour:

- FACT: Fiber Assigned by Continuous Tracking. A deterministic algorithm that takes as input a 4D image, with 3xN volumes, where N is the maximum number of fiber orientations in a voxel. Each triplet of volumes represents a 3D vector corresponding to a fiber orientation; the length of the vector additionally indicates some measure of density or anisotropy. As streamlines move from one voxel to another, the fiber orientation most collinear with the streamline orientation is selected (i.e. there is no intra-voxel interpolation).

- iFOD1: First-order Integration over Fiber Orientation Distributions. A probabilistic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. At each streamline step, random samples from the local (trilinear interpolated) FOD are taken. A streamline is more probable to follow orientations where the FOD amplitude is large; but it may also rarely traverse orientations with small FOD amplitude.

- iFOD2 (default): Second-order Integration over Fiber Orientation Distributions. A probabilistic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. Candidate streamline paths (based on short curved "arcs") are drawn, and the underlying (trilinear-interpolated) FOD amplitudes along those arcs are sampled. A streamline is more probable to follow a path where the FOD amplitudes along that path are large; but it may also rarely traverse orientations where the FOD amplitudes are small, as long as the amplitude remains above the FOD amplitude threshold along the entire path.

- NullDist1 / NullDist2: Null Distribution tracking algorithms. These probabilistic algorithms expect as input the same image that was used when invoking the corresponding algorithm for which the null distribution is sought. These algorithms generate streamlines based on random orientation samples; that is, no image information relating to fiber orientations is used, and streamlines trajectories are determined entirely from random sampling. The NullDist2 algorithm is designed to be used in conjunction with iFOD2; NullDist1 should be used in conjunction with any first-order algorithm.

- SD_STREAM: Streamlines tractography based on Spherical Deconvolution (SD). A deterministic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. At each streamline step, the local (trilinear-interpolated) FOD is sampled, and from the current streamline tangent orientation, a Newton optimisation on the sphere is performed in order to locate the orientation of the nearest FOD amplitude peak.

- SeedTest: A dummy streamlines algorithm used for testing streamline seeding mechanisms. Any image can be used as input; the image will not be used in any way. For each seed point generated by the seeding mechanism(s), a streamline containing a single point corresponding to that seed location will be written to the output track file.

- Tensor_Det: A deterministic algorithm that takes as input a 4D diffusion-weighted image (DWI) series. At each streamline step, the diffusion tensor is fitted to the local (trilinear-interpolated) diffusion data, and the streamline trajectory is determined as the principal eigenvector of that tensor.

- Tensor_Prob: A probabilistic algorithm that takes as input a 4D diffusion-weighted image (DWI) series. Within each image voxel, a residual bootstrap is performed to obtain a unique realisation of the DWI data in that voxel for each streamline. These data are then sampled via trilinear interpolation at each streamline step, the diffusion tensor model is fitted, and the streamline follows the orientation of the principal eigenvector of that tensor.

Note that the behaviour of the -angle option varies slightly depending on the order of integration: for any first-order method, this angle corresponds to the deviation in streamline trajectory per step; for higher-order methods, this corresponds to the change in underlying fibre orientation between the start and end points of each step.

References:

References based on streamlines algorithm used:

* FACT:
Mori, S.; Crain, B. J.; Chacko, V. P. & van Zijl, P. C. M. Three-dimensional tracking of axonal projections in the brain by magnetic resonance imaging. Annals of Neurology, 1999, 45, 265-269

* iFOD1 or SD_STREAM:
Tournier, J.-D.; Calamante, F. & Connelly, A. MRtrix: Diffusion tractography in crossing fiber regions. Int. J. Imaging Syst. Technol., 2012, 22, 53-66

* iFOD2:
Tournier, J.-D.; Calamante, F. & Connelly, A. Improved probabilistic streamlines tractography by 2nd order integration over fibre orientation distributions. Proceedings of the International Society for Magnetic Resonance in Medicine, 2010, 1670

* Nulldist1 / Nulldist2:
Morris, D. M.; Embleton, K. V. & Parker, G. J. Probabilistic fibre tracking: Differentiation of connections from chance events. NeuroImage, 2008, 42, 1329-1339

* Tensor_Det:
Basser, P. J.; Pajevic, S.; Pierpaoli, C.; Duda, J. & Aldroubi, A. In vivo fiber tractography using DT-MRI data. Magnetic Resonance in Medicine, 2000, 44, 625-632

* Tensor_Prob:
Jones, D. Tractography Gone Wild: Probabilistic Fibre Tracking Using the Wild Bootstrap With Diffusion Tensor MRI. IEEE Transactions on Medical Imaging, 2008, 27, 1268-1274

References based on command-line options:

* -rk4:
Basser, P. J.; Pajevic, S.; Pierpaoli, C.; Duda, J. & Aldroubi, A. In vivo fiber tractography using DT-MRI data. Magnetic Resonance in Medicine, 2000, 44, 625-632

* -act, -backtrack, -seed_gmwmi:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. Anatomically-constrained tractography: Improved diffusion MRI streamlines tractography through effective use of anatomical information. NeuroImage, 2012, 62, 1924-1938

* -seed_dynamic:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT2: Enabling dense quantitative assessment of brain white matter connectivity using streamlines tractography. NeuroImage, 2015, 119, 338-351

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckgen_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKGEN_METADATA)
    execution.params(params)
    args = tckgen_cargs(params, execution)
    out = tckgen_outputs(params, execution)
    execution.run(args)
    return out

def tckgen(
    source: InputPathType,
    tracks: str,
    algorithm: str | None = None,
    select: int | None = None,
    step: float | None = None,
    angle: float | None = None,
    minlength: float | None = None,
    maxlength: float | None = None,
    cutoff: float | None = None,
    trials: int | None = None,
    noprecomputed: bool = False,
    rk4: bool = False,
    stop: bool = False,
    downsample: int | None = None,
    seed_image: list[InputPathType] | None = None,
    seed_sphere: list[list[float]] | None = None,
    seed_random_per_voxel: list[TckgenSeedRandomPerVoxel] | None = None,
    seed_grid_per_voxel: list[TckgenSeedGridPerVoxel] | None = None,
    seed_rejection: list[InputPathType] | None = None,
    seed_gmwmi: list[InputPathType] | None = None,
    seed_dynamic: InputPathType | None = None,
    seeds: int | None = None,
    max_attempts_per_seed: int | None = None,
    seed_cutoff: float | None = None,
    seed_unidirectional: bool = False,
    seed_direction: list[float] | None = None,
    output_seeds: str | None = None,
    include: list[TckgenInclude] | None = None,
    include_ordered: list[str] | None = None,
    exclude: list[TckgenInclude] | None = None,
    mask: list[TckgenInclude] | None = None,
    act: InputPathType | None = None,
    backtrack: bool = False,
    crop_at_gmwmi: bool = False,
    power: float | None = None,
    samples: int | None = None,
    grad: InputPathType | None = None,
    fslgrad: TckgenFslgrad | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckgenConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckgenOutputs:
    """
    tckgen

    Perform streamlines tractography.

By default, tckgen produces a fixed number of streamlines, by attempting to seed from new random locations until the target number of streamlines have been selected (in other words, after all inclusion & exclusion criteria have been applied), or the maximum number of seeds has been exceeded (by default, this is 1000 x the desired number of selected streamlines). Use the -select and/or -seeds options to modify as required. See also the Seeding options section for alternative seeding strategies.

Below is a list of available tracking algorithms, the input image data that they require, and a brief description of their behaviour:

- FACT: Fiber Assigned by Continuous Tracking. A deterministic algorithm that takes as input a 4D image, with 3xN volumes, where N is the maximum number of fiber orientations in a voxel. Each triplet of volumes represents a 3D vector corresponding to a fiber orientation; the length of the vector additionally indicates some measure of density or anisotropy. As streamlines move from one voxel to another, the fiber orientation most collinear with the streamline orientation is selected (i.e. there is no intra-voxel interpolation).

- iFOD1: First-order Integration over Fiber Orientation Distributions. A probabilistic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. At each streamline step, random samples from the local (trilinear interpolated) FOD are taken. A streamline is more probable to follow orientations where the FOD amplitude is large; but it may also rarely traverse orientations with small FOD amplitude.

- iFOD2 (default): Second-order Integration over Fiber Orientation Distributions. A probabilistic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. Candidate streamline paths (based on short curved "arcs") are drawn, and the underlying (trilinear-interpolated) FOD amplitudes along those arcs are sampled. A streamline is more probable to follow a path where the FOD amplitudes along that path are large; but it may also rarely traverse orientations where the FOD amplitudes are small, as long as the amplitude remains above the FOD amplitude threshold along the entire path.

- NullDist1 / NullDist2: Null Distribution tracking algorithms. These probabilistic algorithms expect as input the same image that was used when invoking the corresponding algorithm for which the null distribution is sought. These algorithms generate streamlines based on random orientation samples; that is, no image information relating to fiber orientations is used, and streamlines trajectories are determined entirely from random sampling. The NullDist2 algorithm is designed to be used in conjunction with iFOD2; NullDist1 should be used in conjunction with any first-order algorithm.

- SD_STREAM: Streamlines tractography based on Spherical Deconvolution (SD). A deterministic algorithm that takes as input a Fiber Orientation Distribution (FOD) image represented in the Spherical Harmonic (SH) basis. At each streamline step, the local (trilinear-interpolated) FOD is sampled, and from the current streamline tangent orientation, a Newton optimisation on the sphere is performed in order to locate the orientation of the nearest FOD amplitude peak.

- SeedTest: A dummy streamlines algorithm used for testing streamline seeding mechanisms. Any image can be used as input; the image will not be used in any way. For each seed point generated by the seeding mechanism(s), a streamline containing a single point corresponding to that seed location will be written to the output track file.

- Tensor_Det: A deterministic algorithm that takes as input a 4D diffusion-weighted image (DWI) series. At each streamline step, the diffusion tensor is fitted to the local (trilinear-interpolated) diffusion data, and the streamline trajectory is determined as the principal eigenvector of that tensor.

- Tensor_Prob: A probabilistic algorithm that takes as input a 4D diffusion-weighted image (DWI) series. Within each image voxel, a residual bootstrap is performed to obtain a unique realisation of the DWI data in that voxel for each streamline. These data are then sampled via trilinear interpolation at each streamline step, the diffusion tensor model is fitted, and the streamline follows the orientation of the principal eigenvector of that tensor.

Note that the behaviour of the -angle option varies slightly depending on the order of integration: for any first-order method, this angle corresponds to the deviation in streamline trajectory per step; for higher-order methods, this corresponds to the change in underlying fibre orientation between the start and end points of each step.

References:

References based on streamlines algorithm used:

* FACT:
Mori, S.; Crain, B. J.; Chacko, V. P. & van Zijl, P. C. M. Three-dimensional tracking of axonal projections in the brain by magnetic resonance imaging. Annals of Neurology, 1999, 45, 265-269

* iFOD1 or SD_STREAM:
Tournier, J.-D.; Calamante, F. & Connelly, A. MRtrix: Diffusion tractography in crossing fiber regions. Int. J. Imaging Syst. Technol., 2012, 22, 53-66

* iFOD2:
Tournier, J.-D.; Calamante, F. & Connelly, A. Improved probabilistic streamlines tractography by 2nd order integration over fibre orientation distributions. Proceedings of the International Society for Magnetic Resonance in Medicine, 2010, 1670

* Nulldist1 / Nulldist2:
Morris, D. M.; Embleton, K. V. & Parker, G. J. Probabilistic fibre tracking: Differentiation of connections from chance events. NeuroImage, 2008, 42, 1329-1339

* Tensor_Det:
Basser, P. J.; Pajevic, S.; Pierpaoli, C.; Duda, J. & Aldroubi, A. In vivo fiber tractography using DT-MRI data. Magnetic Resonance in Medicine, 2000, 44, 625-632

* Tensor_Prob:
Jones, D. Tractography Gone Wild: Probabilistic Fibre Tracking Using the Wild Bootstrap With Diffusion Tensor MRI. IEEE Transactions on Medical Imaging, 2008, 27, 1268-1274

References based on command-line options:

* -rk4:
Basser, P. J.; Pajevic, S.; Pierpaoli, C.; Duda, J. & Aldroubi, A. In vivo fiber tractography using DT-MRI data. Magnetic Resonance in Medicine, 2000, 44, 625-632

* -act, -backtrack, -seed_gmwmi:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. Anatomically-constrained tractography: Improved diffusion MRI streamlines tractography through effective use of anatomical information. NeuroImage, 2012, 62, 1924-1938

* -seed_dynamic:
Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. SIFT2: Enabling dense quantitative assessment of brain white matter connectivity using streamlines tractography. NeuroImage, 2015, 119, 338-351

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        source: The image containing the source data. The type of image data\
            required depends on the algorithm used (see Description section).
        tracks: the output file containing the tracks generated.
        algorithm: specify the tractography algorithm to use. Valid choices are:\
            FACT, iFOD1, iFOD2, Nulldist1, Nulldist2, SD_Stream, Seedtest,\
            Tensor_Det, Tensor_Prob (default: iFOD2).
        select: set the desired number of streamlines to be selected by tckgen,\
            after all selection criteria have been applied (i.e.\
            inclusion/exclusion ROIs, min/max length, etc). tckgen will keep\
            seeding streamlines until this number of streamlines have been\
            selected, or the maximum allowed number of seeds has been exceeded (see\
            -seeds option). By default, 5000 streamlines are to be selected. Set to\
            zero to disable, which will result in streamlines being seeded until\
            the number specified by -seeds has been reached.
        step: set the step size of the algorithm in mm (defaults: for first-order\
            algorithms, 0.1 x voxelsize; if using RK4, 0.25 x voxelsize; for iFOD2:\
            0.5 x voxelsize).
        angle: set the maximum angle in degrees between successive steps (defaults:\
            60 for deterministic algorithms; 15 for iFOD1 / nulldist1; 45 for iFOD2\
            / nulldist2)
        minlength: set the minimum length of any track in mm (defaults: without\
            ACT, 5 x voxelsize; with ACT, 2 x voxelsize).
        maxlength: set the maximum length of any track in mm (default: 100 x\
            voxelsize).
        cutoff: set the FOD amplitude / fixel size / tensor FA cutoff for\
            terminating tracks (defaults: 0.1 for FOD-based algorithms; 0.1 for\
            fixel-based algorithms; 0.1 for tensor-based algorithms; threshold\
            multiplied by 0.5 when using ACT).
        trials: set the maximum number of sampling trials at each point (only used\
            for iFOD1 / iFOD2) (default: 1000).
        noprecomputed: do NOT pre-compute legendre polynomial values. Warning: this\
            will slow down the algorithm by a factor of approximately 4.
        rk4: use 4th-order Runge-Kutta integration (slower, but eliminates\
            curvature overshoot in 1st-order deterministic methods)
        stop: stop propagating a streamline once it has traversed all include\
            regions
        downsample: downsample the generated streamlines to reduce output file size\
            (default is (samples-1) for iFOD2, no downsampling for all other\
            algorithms)
        seed_image: seed streamlines entirely at random within a mask image
        seed_sphere: spherical seed as four comma-separated values (XYZ position\
            and radius)
        seed_random_per_voxel: seed a fixed number of streamlines per voxel in a\
            mask image; random placement of seeds in each voxel
        seed_grid_per_voxel: seed a fixed number of streamlines per voxel in a mask\
            image; place seeds on a 3D mesh grid (grid_size argument is per axis;\
            so a grid_size of 3 results in 27 seeds per voxel)
        seed_rejection: seed from an image using rejection sampling (higher values\
            = more probable to seed from)
        seed_gmwmi: seed from the grey matter - white matter interface (only valid\
            if using ACT framework). Input image should be a 3D seeding volume;\
            seeds drawn within this image will be optimised to the interface using\
            the 5TT image provided using the -act option.
        seed_dynamic: determine seed points dynamically using the SIFT model (must\
            not provide any other seeding mechanism). Note that while this seeding\
            mechanism improves the distribution of reconstructed streamlines\
            density, it should NOT be used as a substitute for the SIFT method\
            itself.
        seeds: set the number of seeds that tckgen will attempt to track from. If\
            this option is NOT provided, the default number of seeds is set to\
            1000Ã— the number of selected streamlines. If -select is NOT also\
            specified, tckgen will continue tracking until this number of seeds has\
            been attempted. However, if -select is also specified, tckgen will stop\
            when the number of seeds attempted reaches the number specified here,\
            OR when the number of streamlines selected reaches the number requested\
            with the -select option. This can be used to prevent the program from\
            running indefinitely when no or very few streamlines can be found that\
            match the selection criteria. Setting this to zero will cause tckgen to\
            keep attempting seeds until the number specified by -select has been\
            reached.
        max_attempts_per_seed: set the maximum number of times that the tracking\
            algorithm should attempt to find an appropriate tracking direction from\
            a given seed point. This should be set high enough to ensure that an\
            actual plausible seed point is not discarded prematurely as being\
            unable to initiate tracking from. Higher settings may affect\
            performance if many seeds are genuinely impossible to track from, as\
            many attempts will still be made in vain for such seeds. (default:\
            1000)
        seed_cutoff: set the minimum FA or FOD amplitude for seeding tracks\
            (default is the same as the normal -cutoff).
        seed_unidirectional: track from the seed point in one direction only\
            (default is to track in both directions).
        seed_direction: specify a seeding direction for the tracking (this should\
            be supplied as a vector of 3 comma-separated values.
        output_seeds: output the seed location of all successful streamlines to a\
            file
        include: specify an inclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines must traverse ALL inclusion regions to be accepted.
        include_ordered: specify an inclusion region of interest, as either a\
            binary mask image, or as a sphere using 4 comma-separared values\
            (x,y,z,radius). Streamlines must traverse ALL inclusion_ordered regions\
            in the order they are specified in order to be accepted.
        exclude: specify an exclusion region of interest, as either a binary mask\
            image, or as a sphere using 4 comma-separared values (x,y,z,radius).\
            Streamlines that enter ANY exclude region will be discarded.
        mask: specify a masking region of interest, as either a binary mask image,\
            or as a sphere using 4 comma-separared values (x,y,z,radius). If\
            defined, streamlines exiting the mask will be truncated.
        act: use the Anatomically-Constrained Tractography framework during\
            tracking; provided image must be in the 5TT (five-tissue-type) format
        backtrack: allow tracks to be truncated and re-tracked if a poor structural\
            termination is encountered
        crop_at_gmwmi: crop streamline endpoints more precisely as they cross the\
            GM-WM interface
        power: raise the FOD to the power specified (defaults are: 1.0 for iFOD1;\
            1.0/nsamples for iFOD2).
        samples: set the number of FOD samples to take per step (Default: 4).
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckgen_params(
        source=source,
        tracks=tracks,
        algorithm=algorithm,
        select=select,
        step=step,
        angle=angle,
        minlength=minlength,
        maxlength=maxlength,
        cutoff=cutoff,
        trials=trials,
        noprecomputed=noprecomputed,
        rk4=rk4,
        stop=stop,
        downsample=downsample,
        seed_image=seed_image,
        seed_sphere=seed_sphere,
        seed_random_per_voxel=seed_random_per_voxel,
        seed_grid_per_voxel=seed_grid_per_voxel,
        seed_rejection=seed_rejection,
        seed_gmwmi=seed_gmwmi,
        seed_dynamic=seed_dynamic,
        seeds=seeds,
        max_attempts_per_seed=max_attempts_per_seed,
        seed_cutoff=seed_cutoff,
        seed_unidirectional=seed_unidirectional,
        seed_direction=seed_direction,
        output_seeds=output_seeds,
        include=include,
        include_ordered=include_ordered,
        exclude=exclude,
        mask=mask,
        act=act,
        backtrack=backtrack,
        crop_at_gmwmi=crop_at_gmwmi,
        power=power,
        samples=samples,
        grad=grad,
        fslgrad=fslgrad,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckgen_execute(params, runner)

__all__ = [
    "Tckgen",
    "TckgenOutputs",
    "TCKGEN_METADATA",
    "tckgen_cargs",
    "tckgen_outputs",
    "tckgen_params",
    "tckgen_execute",
    "tckgen_validate",
    "tckgen",
]