# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXEL2SH_METADATA = Metadata(
    id="fixel2sh",
    name="fixel2sh",
    package="mrtrix",
)

class Fixel2shConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixel2sh = typing.TypedDict(
    "Fixel2sh",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixel2sh"]],
        "lmax": typing.NotRequired[int],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fixel2shConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fixel_in": InputPathType,
        "sh_out": str,
    },
)

@dataclasses.dataclass
class Fixel2shOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    sh_out: OutputPathType
    """the output sh image."""

def fixel2sh_params(
    fixel_in: InputPathType,
    sh_out: str,
    lmax: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2shConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixel2sh:
    """
    Build parameters.

    Args:
        fixel_in: the input fixel data file.
        sh_out: the output sh image.
        lmax: set the maximum harmonic order for the output series (Default: 8)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixel2sh = {
        "@type": "mrtrix/fixel2sh",
        "fixel_in": fixel_in,
        "sh_out": sh_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if lmax is not None:
        params["lmax"] = lmax
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixel2sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixel2sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("lmax", None) is not None:
        if not isinstance(params["lmax"], int):
            raise StyxValidationError(f'`lmax` has the wrong type: Received `{type(params["lmax"])}` expected `int`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fixel2shConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fixel_in", None) is None:
        raise StyxValidationError("`fixel_in` must not be None")
    if not isinstance(params["fixel_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_in` has the wrong type: Received `{type(params["fixel_in"])}` expected `InputPathType`')
    if params.get("sh_out", None) is None:
        raise StyxValidationError("`sh_out` must not be None")
    if not isinstance(params["sh_out"], str):
        raise StyxValidationError(f'`sh_out` has the wrong type: Received `{type(params["sh_out"])}` expected `str`')

def fixel2sh_cargs(params: Fixel2sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixel2sh")
    v_0 = params.get("lmax")
    if v_0 is not None:
        cargs.append("-lmax")
        cargs.append(str(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["fixel_in"]))
    cargs.append(params["sh_out"])
    return cargs

def fixel2sh_outputs(params: Fixel2sh, execution: Execution) -> Fixel2shOutputs:
    """Build the Fixel2shOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    sh_out_v: OutputPathType = execution.output_file(params["sh_out"])
    return Fixel2shOutputs(
        root=root_v,
        sh_out=sh_out_v,
    )

def fixel2sh_execute(params: Fixel2sh, runner: Runner | None = None) -> Fixel2shOutputs:
    """
    fixel2sh

    Convert a fixel-based sparse-data image into an spherical harmonic image.

This command generates spherical harmonic data from fixels that can be visualised using the ODF tool in MRview. The output ODF lobes are scaled according to the values in the input fixel image.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixel2sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXEL2SH_METADATA)
    execution.params(params)
    args = fixel2sh_cargs(params, execution)
    out = fixel2sh_outputs(params, execution)
    execution.run(args)
    return out

def fixel2sh(
    fixel_in: InputPathType,
    sh_out: str,
    lmax: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2shConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fixel2shOutputs:
    """
    fixel2sh

    Convert a fixel-based sparse-data image into an spherical harmonic image.

This command generates spherical harmonic data from fixels that can be visualised using the ODF tool in MRview. The output ODF lobes are scaled according to the values in the input fixel image.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fixel_in: the input fixel data file.
        sh_out: the output sh image.
        lmax: set the maximum harmonic order for the output series (Default: 8)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixel2sh_params(
        fixel_in=fixel_in,
        sh_out=sh_out,
        lmax=lmax,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixel2sh_execute(params, runner)

__all__ = [
    "Fixel2sh",
    "Fixel2shOutputs",
    "FIXEL2SH_METADATA",
    "fixel2sh_cargs",
    "fixel2sh_outputs",
    "fixel2sh_params",
    "fixel2sh_execute",
    "fixel2sh_validate",
    "fixel2sh",
]