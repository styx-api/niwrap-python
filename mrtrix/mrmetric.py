# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRMETRIC_METADATA = Metadata(
    id="mrmetric",
    name="mrmetric",
    package="mrtrix",
)

class MrmetricConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrmetric = typing.TypedDict(
    "Mrmetric",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrmetric"]],
        "space": typing.NotRequired[str],
        "interp": typing.NotRequired[str],
        "metric": typing.NotRequired[str],
        "mask1": typing.NotRequired[InputPathType],
        "mask2": typing.NotRequired[InputPathType],
        "nonormalisation": typing.NotRequired[bool],
        "overlap": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrmetricConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image1": InputPathType,
        "image2": InputPathType,
    },
)

@dataclasses.dataclass
class MrmetricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def mrmetric_params(
    image1: InputPathType,
    image2: InputPathType,
    space: str | None = None,
    interp: str | None = None,
    metric: str | None = None,
    mask1: InputPathType | None = None,
    mask2: InputPathType | None = None,
    nonormalisation: bool = False,
    overlap: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrmetricConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrmetric:
    """
    Build parameters.

    Args:
        image1: the first input image.
        image2: the second input image.
        space: voxel (default): per voxel image1: scanner space of image 1 image2:\
            scanner space of image 2 average: scanner space of the average affine\
            transformation of image 1 and 2
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: linear).
        metric: define the dissimilarity metric used to calculate the cost.\
            Choices: diff (squared differences), cc (non-normalised negative cross\
            correlation aka negative cross covariance). Default: diff). cc is only\
            implemented for -space average and -interp linear and cubic.
        mask1: mask for image 1
        mask2: mask for image 2
        nonormalisation: do not normalise the dissimilarity metric to the number of\
            voxels.
        overlap: output number of voxels that were used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrmetric = {
        "@type": "mrtrix/mrmetric",
        "image1": image1,
        "image2": image2,
        "nonormalisation": nonormalisation,
        "overlap": overlap,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if space is not None:
        params["space"] = space
    if interp is not None:
        params["interp"] = interp
    if metric is not None:
        params["metric"] = metric
    if mask1 is not None:
        params["mask1"] = mask1
    if mask2 is not None:
        params["mask2"] = mask2
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrmetric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrmetric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("space", None) is not None:
        if not isinstance(params["space"], str):
            raise StyxValidationError(f'`space` has the wrong type: Received `{type(params["space"])}` expected `str`')
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `str`')
    if params.get("metric", None) is not None:
        if not isinstance(params["metric"], str):
            raise StyxValidationError(f'`metric` has the wrong type: Received `{type(params["metric"])}` expected `str`')
    if params.get("mask1", None) is not None:
        if not isinstance(params["mask1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask1` has the wrong type: Received `{type(params["mask1"])}` expected `InputPathType`')
    if params.get("mask2", None) is not None:
        if not isinstance(params["mask2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask2` has the wrong type: Received `{type(params["mask2"])}` expected `InputPathType`')
    if params.get("nonormalisation", None) is not None:
        if not isinstance(params["nonormalisation"], bool):
            raise StyxValidationError(f'`nonormalisation` has the wrong type: Received `{type(params["nonormalisation"])}` expected `bool`')
    if params.get("overlap", None) is not None:
        if not isinstance(params["overlap"], bool):
            raise StyxValidationError(f'`overlap` has the wrong type: Received `{type(params["overlap"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrmetricConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image1", None) is None:
        raise StyxValidationError("`image1` must not be None")
    if not isinstance(params["image1"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image1` has the wrong type: Received `{type(params["image1"])}` expected `InputPathType`')
    if params.get("image2", None) is None:
        raise StyxValidationError("`image2` must not be None")
    if not isinstance(params["image2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image2` has the wrong type: Received `{type(params["image2"])}` expected `InputPathType`')

def mrmetric_cargs(params: Mrmetric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrmetric")
    v_0 = params.get("space")
    if v_0 is not None:
        cargs.append("-space")
        cargs.append(v_0)
    v_1 = params.get("interp")
    if v_1 is not None:
        cargs.append("-interp")
        cargs.append(v_1)
    v_2 = params.get("metric")
    if v_2 is not None:
        cargs.append("-metric")
        cargs.append(v_2)
    v_3 = params.get("mask1")
    if v_3 is not None:
        cargs.append("-mask1")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("mask2")
    if v_4 is not None:
        cargs.append("-mask2")
        cargs.append(execution.input_file(v_4))
    if params.get("nonormalisation"):
        cargs.append("-nonormalisation")
    if params.get("overlap"):
        cargs.append("-overlap")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_5 = params.get("nthreads")
    if v_5 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_5))
    v_6 = params.get("config")
    if v_6 is not None:
        for item0 in v_6:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["image1"]))
    cargs.append(execution.input_file(params["image2"]))
    return cargs

def mrmetric_outputs(params: Mrmetric, execution: Execution) -> MrmetricOutputs:
    """Build the MrmetricOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return MrmetricOutputs(
        root=root_v,
    )

def mrmetric_execute(params: Mrmetric, runner: Runner | None = None) -> MrmetricOutputs:
    """
    mrmetric

    Computes a dissimilarity metric between two images.

Currently only the mean squared difference is fully implemented.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrmetric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRMETRIC_METADATA)
    execution.params(params)
    args = mrmetric_cargs(params, execution)
    out = mrmetric_outputs(params, execution)
    execution.run(args)
    return out

def mrmetric(
    image1: InputPathType,
    image2: InputPathType,
    space: str | None = None,
    interp: str | None = None,
    metric: str | None = None,
    mask1: InputPathType | None = None,
    mask2: InputPathType | None = None,
    nonormalisation: bool = False,
    overlap: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrmetricConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrmetricOutputs:
    """
    mrmetric

    Computes a dissimilarity metric between two images.

Currently only the mean squared difference is fully implemented.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image1: the first input image.
        image2: the second input image.
        space: voxel (default): per voxel image1: scanner space of image 1 image2:\
            scanner space of image 2 average: scanner space of the average affine\
            transformation of image 1 and 2
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: linear).
        metric: define the dissimilarity metric used to calculate the cost.\
            Choices: diff (squared differences), cc (non-normalised negative cross\
            correlation aka negative cross covariance). Default: diff). cc is only\
            implemented for -space average and -interp linear and cubic.
        mask1: mask for image 1
        mask2: mask for image 2
        nonormalisation: do not normalise the dissimilarity metric to the number of\
            voxels.
        overlap: output number of voxels that were used.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrmetric_params(
        image1=image1,
        image2=image2,
        space=space,
        interp=interp,
        metric=metric,
        mask1=mask1,
        mask2=mask2,
        nonormalisation=nonormalisation,
        overlap=overlap,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrmetric_execute(params, runner)

__all__ = [
    "Mrmetric",
    "MrmetricOutputs",
    "MRMETRIC_METADATA",
    "mrmetric_cargs",
    "mrmetric_outputs",
    "mrmetric_params",
    "mrmetric_execute",
    "mrmetric_validate",
    "mrmetric",
]