# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRCOLOUR_METADATA = Metadata(
    id="mrcolour",
    name="mrcolour",
    package="mrtrix",
)

class MrcolourConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrcolour = typing.TypedDict(
    "Mrcolour",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrcolour"]],
        "upper": typing.NotRequired[float],
        "lower": typing.NotRequired[float],
        "colour": typing.NotRequired[list[float]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrcolourConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "map": str,
        "output": str,
    },
)

@dataclasses.dataclass
class MrcolourOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image"""

def mrcolour_params(
    input_: InputPathType,
    map: str,
    output: str,
    upper: float | None = None,
    lower: float | None = None,
    colour: list[float] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrcolourConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrcolour:
    """
    Build parameters.

    Args:
        input_: the input image
        map: the colourmap to apply; choices are:\
            gray,hot,cool,jet,inferno,viridis,pet,colour,rgb
        output: the output image
        upper: manually set the upper intensity of the colour mapping
        lower: manually set the lower intensity of the colour mapping
        colour: set the target colour for use of the 'colour' map (three\
            comma-separated floating-point values)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrcolour = {
        "@type": "mrtrix/mrcolour",
        "input": input_,
        "map": map,
        "output": output,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if upper is not None:
        params["upper"] = upper
    if lower is not None:
        params["lower"] = lower
    if colour is not None:
        params["colour"] = colour
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrcolour_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrcolour."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("upper", None) is not None:
        if not isinstance(params["upper"], (float, int)):
            raise StyxValidationError(f'`upper` has the wrong type: Received `{type(params["upper"])}` expected `float`')
    if params.get("lower", None) is not None:
        if not isinstance(params["lower"], (float, int)):
            raise StyxValidationError(f'`lower` has the wrong type: Received `{type(params["lower"])}` expected `float`')
    if params.get("colour", None) is not None:
        if not isinstance(params["colour"], list):
            raise StyxValidationError(f'`colour` has the wrong type: Received `{type(params["colour"])}` expected `list[float]`')
        for e in params["colour"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`colour` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrcolourConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("map", None) is None:
        raise StyxValidationError("`map` must not be None")
    if not isinstance(params["map"], str):
        raise StyxValidationError(f'`map` has the wrong type: Received `{type(params["map"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrcolour_cargs(params: Mrcolour, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrcolour")
    v_0 = params.get("upper")
    if v_0 is not None:
        cargs.append("-upper")
        cargs.append(str(v_0))
    v_1 = params.get("lower")
    if v_1 is not None:
        cargs.append("-lower")
        cargs.append(str(v_1))
    v_2 = params.get("colour")
    if v_2 is not None:
        cargs.append("-colour")
        cargs.append(",".join([str(item0) for item0 in v_2]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item1 in v_4:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["map"])
    cargs.append(params["output"])
    return cargs

def mrcolour_outputs(params: Mrcolour, execution: Execution) -> MrcolourOutputs:
    """Build the MrcolourOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MrcolourOutputs(
        root=root_v,
        output=output_v,
    )

def mrcolour_execute(params: Mrcolour, runner: Runner | None = None) -> MrcolourOutputs:
    """
    mrcolour

    Apply a colour map to an image.

Under typical usage, this command will receive as input ad 3D greyscale image, and output a 4D image with 3 volumes corresponding to red-green-blue components; other use cases are possible, and are described in more detail below.

By default, the command will automatically determine the maximum and minimum intensities of the input image, and use that information to set the upper and lower bounds of the applied colourmap. This behaviour can be overridden by manually specifying these bounds using the -upper and -lower options respectively.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrcolour_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRCOLOUR_METADATA)
    execution.params(params)
    args = mrcolour_cargs(params, execution)
    out = mrcolour_outputs(params, execution)
    execution.run(args)
    return out

def mrcolour(
    input_: InputPathType,
    map: str,
    output: str,
    upper: float | None = None,
    lower: float | None = None,
    colour: list[float] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrcolourConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrcolourOutputs:
    """
    mrcolour

    Apply a colour map to an image.

Under typical usage, this command will receive as input ad 3D greyscale image, and output a 4D image with 3 volumes corresponding to red-green-blue components; other use cases are possible, and are described in more detail below.

By default, the command will automatically determine the maximum and minimum intensities of the input image, and use that information to set the upper and lower bounds of the applied colourmap. This behaviour can be overridden by manually specifying these bounds using the -upper and -lower options respectively.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image
        map: the colourmap to apply; choices are:\
            gray,hot,cool,jet,inferno,viridis,pet,colour,rgb
        output: the output image
        upper: manually set the upper intensity of the colour mapping
        lower: manually set the lower intensity of the colour mapping
        colour: set the target colour for use of the 'colour' map (three\
            comma-separated floating-point values)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrcolour_params(
        input_=input_,
        map=map,
        output=output,
        upper=upper,
        lower=lower,
        colour=colour,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrcolour_execute(params, runner)

__all__ = [
    "Mrcolour",
    "MrcolourOutputs",
    "MRCOLOUR_METADATA",
    "mrcolour_cargs",
    "mrcolour_outputs",
    "mrcolour_params",
    "mrcolour_execute",
    "mrcolour_validate",
    "mrcolour",
]