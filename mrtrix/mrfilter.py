# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRFILTER_METADATA = Metadata(
    id="mrfilter",
    name="mrfilter",
    package="mrtrix",
)

MrfilterVariousString = typing.TypedDict(
    "MrfilterVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

MrfilterVariousFile = typing.TypedDict(
    "MrfilterVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

MrfilterStrides = MrfilterVariousString | MrfilterVariousFile

class MrfilterConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrfilter = typing.TypedDict(
    "Mrfilter",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrfilter"]],
        "axes": typing.NotRequired[list[int]],
        "inverse": typing.NotRequired[bool],
        "magnitude": typing.NotRequired[bool],
        "centre_zero": typing.NotRequired[bool],
        "stdev": typing.NotRequired[list[float]],
        "scanner": typing.NotRequired[bool],
        "extent": typing.NotRequired[list[int]],
        "fwhm": typing.NotRequired[list[float]],
        "zupper": typing.NotRequired[float],
        "zlower": typing.NotRequired[float],
        "bridge": typing.NotRequired[int],
        "maskin": typing.NotRequired[InputPathType],
        "maskout": typing.NotRequired[str],
        "strides": typing.NotRequired[MrfilterStrides],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrfilterConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "filter": str,
        "output": str,
    },
)

@dataclasses.dataclass
class MrfilterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image."""
    maskout: OutputPathType | None
    """Output a refined mask based on a spatially coherent region with normal intensity range. """

def mrfilter_params(
    input_: InputPathType,
    filter: str,
    output: str,
    axes: list[int] | None = None,
    inverse: bool = False,
    magnitude: bool = False,
    centre_zero: bool = False,
    stdev: list[float] | None = None,
    scanner: bool = False,
    extent: list[int] | None = None,
    fwhm: list[float] | None = None,
    zupper: float | None = None,
    zlower: float | None = None,
    bridge: int | None = None,
    maskin: InputPathType | None = None,
    maskout: str | None = None,
    strides: MrfilterStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrfilter:
    """
    Build parameters.

    Args:
        input_: the input image.
        filter: the type of filter to be applied
        output: the output image.
        axes: the axes along which to apply the Fourier Transform. By default, the\
            transform is applied along the three spatial axes. Provide as a\
            comma-separate list of axis indices.
        inverse: apply the inverse FFT
        magnitude: output the gradient magnitude, rather than the default x,y,z\
            components
        centre_zero: re-arrange the FFT results so that the zero-frequency\
            component appears in the centre of the image, rather than at the edges
        stdev: apply Gaussian smoothing with the specified standard deviation. The\
            standard deviation is defined in mm (Default 1 voxel). This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the stdev for each axis.
        scanner: define the gradient with respect to the scanner coordinate frame\
            of reference.
        extent: specify the extent (width) of kernel size in voxels. This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the extent for each axis. The default extent is\
            2 * ceil(2.5 * stdev / voxel_size) - 1.
        fwhm: apply Gaussian smoothing with the specified full-width half maximum.\
            The FWHM is defined in mm (Default 1 voxel * 2.3548). This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the FWHM for each axis.
        zupper: define high intensity outliers: default: 2.5
        zlower: define low intensity outliers: default: 2.5
        bridge: number of voxels to gap to fill holes in mask: default: 4
        maskin: initial mask that defines the maximum spatial extent and the region\
            from which to smaple the intensity range.
        maskout: Output a refined mask based on a spatially coherent region with\
            normal intensity range.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrfilter = {
        "@type": "mrtrix/mrfilter",
        "input": input_,
        "filter": filter,
        "output": output,
        "inverse": inverse,
        "magnitude": magnitude,
        "centre_zero": centre_zero,
        "scanner": scanner,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if axes is not None:
        params["axes"] = axes
    if stdev is not None:
        params["stdev"] = stdev
    if extent is not None:
        params["extent"] = extent
    if fwhm is not None:
        params["fwhm"] = fwhm
    if zupper is not None:
        params["zupper"] = zupper
    if zlower is not None:
        params["zlower"] = zlower
    if bridge is not None:
        params["bridge"] = bridge
    if maskin is not None:
        params["maskin"] = maskin
    if maskout is not None:
        params["maskout"] = maskout
    if strides is not None:
        params["strides"] = strides
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrfilter_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrfilter."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("axes", None) is not None:
        if not isinstance(params["axes"], list):
            raise StyxValidationError(f'`axes` has the wrong type: Received `{type(params["axes"])}` expected `list[int]`')
        for e in params["axes"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`axes` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("inverse", None) is not None:
        if not isinstance(params["inverse"], bool):
            raise StyxValidationError(f'`inverse` has the wrong type: Received `{type(params["inverse"])}` expected `bool`')
    if params.get("magnitude", None) is not None:
        if not isinstance(params["magnitude"], bool):
            raise StyxValidationError(f'`magnitude` has the wrong type: Received `{type(params["magnitude"])}` expected `bool`')
    if params.get("centre_zero", None) is not None:
        if not isinstance(params["centre_zero"], bool):
            raise StyxValidationError(f'`centre_zero` has the wrong type: Received `{type(params["centre_zero"])}` expected `bool`')
    if params.get("stdev", None) is not None:
        if not isinstance(params["stdev"], list):
            raise StyxValidationError(f'`stdev` has the wrong type: Received `{type(params["stdev"])}` expected `list[float]`')
        for e_2 in params["stdev"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`stdev` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("scanner", None) is not None:
        if not isinstance(params["scanner"], bool):
            raise StyxValidationError(f'`scanner` has the wrong type: Received `{type(params["scanner"])}` expected `bool`')
    if params.get("extent", None) is not None:
        if not isinstance(params["extent"], list):
            raise StyxValidationError(f'`extent` has the wrong type: Received `{type(params["extent"])}` expected `list[int]`')
        for e_3 in params["extent"]:
            if not isinstance(e_3, int):
                raise StyxValidationError(f'`extent` has the wrong type: Received `{type(e_3)}` expected `list[int]`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], list):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params["fwhm"])}` expected `list[float]`')
        for e_4 in params["fwhm"]:
            if not isinstance(e_4, (float, int)):
                raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
    if params.get("zupper", None) is not None:
        if not isinstance(params["zupper"], (float, int)):
            raise StyxValidationError(f'`zupper` has the wrong type: Received `{type(params["zupper"])}` expected `float`')
    if params.get("zlower", None) is not None:
        if not isinstance(params["zlower"], (float, int)):
            raise StyxValidationError(f'`zlower` has the wrong type: Received `{type(params["zlower"])}` expected `float`')
    if params.get("bridge", None) is not None:
        if not isinstance(params["bridge"], int):
            raise StyxValidationError(f'`bridge` has the wrong type: Received `{type(params["bridge"])}` expected `int`')
    if params.get("maskin", None) is not None:
        if not isinstance(params["maskin"], (pathlib.Path, str)):
            raise StyxValidationError(f'`maskin` has the wrong type: Received `{type(params["maskin"])}` expected `InputPathType`')
    if params.get("maskout", None) is not None:
        if not isinstance(params["maskout"], str):
            raise StyxValidationError(f'`maskout` has the wrong type: Received `{type(params["maskout"])}` expected `str`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrfilterConfig]`')
        for e_5 in params["config"]:
            if not isinstance(e_5, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_5)}\'')
            if e_5.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_5["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_5["key"])}` expected `str`')
            if e_5.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_5["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_5["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("filter", None) is None:
        raise StyxValidationError("`filter` must not be None")
    if not isinstance(params["filter"], str):
        raise StyxValidationError(f'`filter` has the wrong type: Received `{type(params["filter"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrfilter_cargs(params: Mrfilter, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrfilter")
    v_0 = params.get("axes")
    if v_0 is not None:
        cargs.append("-axes")
        for item0 in v_0:
            cargs.append(str(item0))
    if params.get("inverse"):
        cargs.append("-inverse")
    if params.get("magnitude"):
        cargs.append("-magnitude")
    if params.get("centre_zero"):
        cargs.append("-centre_zero")
    v_1 = params.get("stdev")
    if v_1 is not None:
        cargs.append("-stdev")
        cargs.append(",".join([str(item1) for item1 in v_1]))
    if params.get("magnitude"):
        cargs.append("-magnitude")
    if params.get("scanner"):
        cargs.append("-scanner")
    v_2 = params.get("extent")
    if v_2 is not None:
        cargs.append("-extent")
        cargs.append(",".join([str(item2) for item2 in v_2]))
    v_3 = params.get("extent")
    if v_3 is not None:
        cargs.append("-extent")
        cargs.append(",".join([str(item3) for item3 in v_3]))
    v_4 = params.get("stdev")
    if v_4 is not None:
        cargs.append("-stdev")
        cargs.append(",".join([str(item4) for item4 in v_4]))
    v_5 = params.get("fwhm")
    if v_5 is not None:
        cargs.append("-fwhm")
        cargs.append(",".join([str(item5) for item5 in v_5]))
    v_6 = params.get("extent")
    if v_6 is not None:
        cargs.append("-extent")
        cargs.append(",".join([str(item6) for item6 in v_6]))
    v_7 = params.get("zupper")
    if v_7 is not None:
        cargs.append("-zupper")
        cargs.append(str(v_7))
    v_8 = params.get("zlower")
    if v_8 is not None:
        cargs.append("-zlower")
        cargs.append(str(v_8))
    v_9 = params.get("bridge")
    if v_9 is not None:
        cargs.append("-bridge")
        cargs.append(str(v_9))
    v_10 = params.get("maskin")
    if v_10 is not None:
        cargs.append("-maskin")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("maskout")
    if v_11 is not None:
        cargs.append("-maskout")
        cargs.append(v_11)
    v_12 = params.get("strides")
    if v_12 is not None:
        cargs.append("-strides")
        if v_12["@type"] == "VariousString":
            cargs.append(str(v_12["obj"]))
        elif v_12["@type"] == "VariousFile":
            cargs.append(str(v_12["obj"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_13 = params.get("nthreads")
    if v_13 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_13))
    v_14 = params.get("config")
    if v_14 is not None:
        for item7 in v_14:
            cargs.append("-config")
            cargs.append(item7["key"])
            cargs.append(item7["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["filter"])
    cargs.append(params["output"])
    return cargs

def mrfilter_outputs(params: Mrfilter, execution: Execution) -> MrfilterOutputs:
    """Build the MrfilterOutputs object for this tool."""
    maskout_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    __v0 = params.get("maskout")
    if __v0 is not None:
        maskout_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrfilterOutputs(
        root=root_v,
        output=output_v,
        maskout=maskout_v,
    )

def mrfilter_execute(params: Mrfilter, runner: Runner | None = None) -> MrfilterOutputs:
    """
    mrfilter

    Perform filtering operations on 3D / 4D MR images.

The available filters are: fft, gradient, median, smooth, normalise, zclean.

Each filter has its own unique set of optional parameters.

For 4D images, each 3D volume is processed independently.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrfilter_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRFILTER_METADATA)
    execution.params(params)
    args = mrfilter_cargs(params, execution)
    out = mrfilter_outputs(params, execution)
    execution.run(args)
    return out

def mrfilter(
    input_: InputPathType,
    filter: str,
    output: str,
    axes: list[int] | None = None,
    inverse: bool = False,
    magnitude: bool = False,
    centre_zero: bool = False,
    stdev: list[float] | None = None,
    scanner: bool = False,
    extent: list[int] | None = None,
    fwhm: list[float] | None = None,
    zupper: float | None = None,
    zlower: float | None = None,
    bridge: int | None = None,
    maskin: InputPathType | None = None,
    maskout: str | None = None,
    strides: MrfilterStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrfilterConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrfilterOutputs:
    """
    mrfilter

    Perform filtering operations on 3D / 4D MR images.

The available filters are: fft, gradient, median, smooth, normalise, zclean.

Each filter has its own unique set of optional parameters.

For 4D images, each 3D volume is processed independently.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image.
        filter: the type of filter to be applied
        output: the output image.
        axes: the axes along which to apply the Fourier Transform. By default, the\
            transform is applied along the three spatial axes. Provide as a\
            comma-separate list of axis indices.
        inverse: apply the inverse FFT
        magnitude: output the gradient magnitude, rather than the default x,y,z\
            components
        centre_zero: re-arrange the FFT results so that the zero-frequency\
            component appears in the centre of the image, rather than at the edges
        stdev: apply Gaussian smoothing with the specified standard deviation. The\
            standard deviation is defined in mm (Default 1 voxel). This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the stdev for each axis.
        scanner: define the gradient with respect to the scanner coordinate frame\
            of reference.
        extent: specify the extent (width) of kernel size in voxels. This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the extent for each axis. The default extent is\
            2 * ceil(2.5 * stdev / voxel_size) - 1.
        fwhm: apply Gaussian smoothing with the specified full-width half maximum.\
            The FWHM is defined in mm (Default 1 voxel * 2.3548). This can be\
            specified either as a single value to be used for all axes, or as a\
            comma-separated list of the FWHM for each axis.
        zupper: define high intensity outliers: default: 2.5
        zlower: define low intensity outliers: default: 2.5
        bridge: number of voxels to gap to fill holes in mask: default: 4
        maskin: initial mask that defines the maximum spatial extent and the region\
            from which to smaple the intensity range.
        maskout: Output a refined mask based on a spatially coherent region with\
            normal intensity range.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrfilter_params(
        input_=input_,
        filter=filter,
        output=output,
        axes=axes,
        inverse=inverse,
        magnitude=magnitude,
        centre_zero=centre_zero,
        stdev=stdev,
        scanner=scanner,
        extent=extent,
        fwhm=fwhm,
        zupper=zupper,
        zlower=zlower,
        bridge=bridge,
        maskin=maskin,
        maskout=maskout,
        strides=strides,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrfilter_execute(params, runner)

__all__ = [
    "Mrfilter",
    "MrfilterOutputs",
    "MRFILTER_METADATA",
    "mrfilter_cargs",
    "mrfilter_outputs",
    "mrfilter_params",
    "mrfilter_execute",
    "mrfilter_validate",
    "mrfilter",
]