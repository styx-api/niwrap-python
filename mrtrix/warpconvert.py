# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WARPCONVERT_METADATA = Metadata(
    id="warpconvert",
    name="warpconvert",
    package="mrtrix",
)

class WarpconvertConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Warpconvert = typing.TypedDict(
    "Warpconvert",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/warpconvert"]],
        "template": typing.NotRequired[InputPathType],
        "midway_space": typing.NotRequired[bool],
        "from": typing.NotRequired[int],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[WarpconvertConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in": InputPathType,
        "type": str,
        "out": str,
    },
)

@dataclasses.dataclass
class WarpconvertOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output warp image."""

def warpconvert_params(
    in_: InputPathType,
    type_: str,
    out: str,
    template: InputPathType | None = None,
    midway_space: bool = False,
    from_: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[WarpconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Warpconvert:
    """
    Build parameters.

    Args:
        in_: the input warp image.
        type_: the conversion type required. Valid choices are:\
            deformation2displacement, displacement2deformation,\
            warpfull2deformation, warpfull2displacement
        out: the output warp image.
        template: define a template image when converting a warpfull file (which is\
            defined on a grid in the midway space between image 1 & 2). For example\
            to generate the deformation field that maps image1 to image2, then\
            supply image2 as the template image
        midway_space: to be used only with warpfull2deformation and\
            warpfull2displacement conversion types. The output will only contain\
            the non-linear warp to map an input image to the midway space (defined\
            by the warpfull grid). If a linear transform exists in the warpfull\
            file header then it will be composed and included in the output.
        from_: to be used only with warpfull2deformation and warpfull2displacement\
            conversion types. Used to define the direction of the desired output\
            field.Use -from 1 to obtain the image1->image2 field and from 2 for\
            image2->image1. Can be used in combination with the -midway_space\
            option to produce a field that only maps to midway space.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Warpconvert = {
        "@type": "mrtrix/warpconvert",
        "in": in_,
        "type": type_,
        "out": out,
        "midway_space": midway_space,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if template is not None:
        params["template"] = template
    if from_ is not None:
        params["from"] = from_
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def warpconvert_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Warpconvert."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("midway_space", None) is not None:
        if not isinstance(params["midway_space"], bool):
            raise StyxValidationError(f'`midway_space` has the wrong type: Received `{type(params["midway_space"])}` expected `bool`')
    if params.get("from", None) is not None:
        if not isinstance(params["from"], int):
            raise StyxValidationError(f'`from` has the wrong type: Received `{type(params["from"])}` expected `int`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[WarpconvertConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in", None) is None:
        raise StyxValidationError("`in` must not be None")
    if not isinstance(params["in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in` has the wrong type: Received `{type(params["in"])}` expected `InputPathType`')
    if params.get("type", None) is None:
        raise StyxValidationError("`type` must not be None")
    if not isinstance(params["type"], str):
        raise StyxValidationError(f'`type` has the wrong type: Received `{type(params["type"])}` expected `str`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def warpconvert_cargs(params: Warpconvert, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("warpconvert")
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_0))
    if params.get("midway_space"):
        cargs.append("-midway_space")
    v_1 = params.get("from")
    if v_1 is not None:
        cargs.append("-from")
        cargs.append(str(v_1))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("nthreads")
    if v_2 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_2))
    v_3 = params.get("config")
    if v_3 is not None:
        for item0 in v_3:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in"]))
    cargs.append(params["type"])
    cargs.append(params["out"])
    return cargs

def warpconvert_outputs(params: Warpconvert, execution: Execution) -> WarpconvertOutputs:
    """Build the WarpconvertOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    return WarpconvertOutputs(
        root=root_v,
        out=out_v,
    )

def warpconvert_execute(params: Warpconvert, runner: Runner | None = None) -> WarpconvertOutputs:
    """
    warpconvert

    Convert between different representations of a non-linear warp.

A deformation field is defined as an image where each voxel defines the corresponding position in the other image (in scanner space coordinates). A displacement field stores the displacements (in mm) to the other image from the each voxel's position (in scanner space). The warpfull file is the 5D format output from mrregister -nl_warp_full, which contains linear transforms, warps and their inverses that map each image to a midway space.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    warpconvert_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WARPCONVERT_METADATA)
    execution.params(params)
    args = warpconvert_cargs(params, execution)
    out = warpconvert_outputs(params, execution)
    execution.run(args)
    return out

def warpconvert(
    in_: InputPathType,
    type_: str,
    out: str,
    template: InputPathType | None = None,
    midway_space: bool = False,
    from_: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[WarpconvertConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> WarpconvertOutputs:
    """
    warpconvert

    Convert between different representations of a non-linear warp.

A deformation field is defined as an image where each voxel defines the corresponding position in the other image (in scanner space coordinates). A displacement field stores the displacements (in mm) to the other image from the each voxel's position (in scanner space). The warpfull file is the 5D format output from mrregister -nl_warp_full, which contains linear transforms, warps and their inverses that map each image to a midway space.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_: the input warp image.
        type_: the conversion type required. Valid choices are:\
            deformation2displacement, displacement2deformation,\
            warpfull2deformation, warpfull2displacement
        out: the output warp image.
        template: define a template image when converting a warpfull file (which is\
            defined on a grid in the midway space between image 1 & 2). For example\
            to generate the deformation field that maps image1 to image2, then\
            supply image2 as the template image
        midway_space: to be used only with warpfull2deformation and\
            warpfull2displacement conversion types. The output will only contain\
            the non-linear warp to map an input image to the midway space (defined\
            by the warpfull grid). If a linear transform exists in the warpfull\
            file header then it will be composed and included in the output.
        from_: to be used only with warpfull2deformation and warpfull2displacement\
            conversion types. Used to define the direction of the desired output\
            field.Use -from 1 to obtain the image1->image2 field and from 2 for\
            image2->image1. Can be used in combination with the -midway_space\
            option to produce a field that only maps to midway space.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = warpconvert_params(
        in_=in_,
        type_=type_,
        out=out,
        template=template,
        midway_space=midway_space,
        from_=from_,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return warpconvert_execute(params, runner)

__all__ = [
    "Warpconvert",
    "WarpconvertOutputs",
    "WARPCONVERT_METADATA",
    "warpconvert_cargs",
    "warpconvert_outputs",
    "warpconvert_params",
    "warpconvert_execute",
    "warpconvert_validate",
    "warpconvert",
]