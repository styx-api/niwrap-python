# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PEAKS2AMP_METADATA = Metadata(
    id="peaks2amp",
    name="peaks2amp",
    package="mrtrix",
)

class Peaks2ampConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Peaks2amp = typing.TypedDict(
    "Peaks2amp",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/peaks2amp"]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Peaks2ampConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "directions": InputPathType,
        "amplitudes": str,
    },
)

@dataclasses.dataclass
class Peaks2ampOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    amplitudes: OutputPathType
    """the output amplitudes image."""

def peaks2amp_params(
    directions: InputPathType,
    amplitudes: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Peaks2ampConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Peaks2amp:
    """
    Build parameters.

    Args:
        directions: the input directions image. Each volume corresponds to the x, y\
            & z component of each direction vector in turn.
        amplitudes: the output amplitudes image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Peaks2amp = {
        "@type": "mrtrix/peaks2amp",
        "directions": directions,
        "amplitudes": amplitudes,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def peaks2amp_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Peaks2amp."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Peaks2ampConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("directions", None) is None:
        raise StyxValidationError("`directions` must not be None")
    if not isinstance(params["directions"], (pathlib.Path, str)):
        raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("amplitudes", None) is None:
        raise StyxValidationError("`amplitudes` must not be None")
    if not isinstance(params["amplitudes"], str):
        raise StyxValidationError(f'`amplitudes` has the wrong type: Received `{type(params["amplitudes"])}` expected `str`')

def peaks2amp_cargs(params: Peaks2amp, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("peaks2amp")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_0 = params.get("nthreads")
    if v_0 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_0))
    v_1 = params.get("config")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["directions"]))
    cargs.append(params["amplitudes"])
    return cargs

def peaks2amp_outputs(params: Peaks2amp, execution: Execution) -> Peaks2ampOutputs:
    """Build the Peaks2ampOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    amplitudes_v: OutputPathType = execution.output_file(params["amplitudes"])
    return Peaks2ampOutputs(
        root=root_v,
        amplitudes=amplitudes_v,
    )

def peaks2amp_execute(params: Peaks2amp, runner: Runner | None = None) -> Peaks2ampOutputs:
    """
    peaks2amp

    Extract amplitudes from a peak directions image.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    peaks2amp_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PEAKS2AMP_METADATA)
    execution.params(params)
    args = peaks2amp_cargs(params, execution)
    out = peaks2amp_outputs(params, execution)
    execution.run(args)
    return out

def peaks2amp(
    directions: InputPathType,
    amplitudes: str,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Peaks2ampConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Peaks2ampOutputs:
    """
    peaks2amp

    Extract amplitudes from a peak directions image.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        directions: the input directions image. Each volume corresponds to the x, y\
            & z component of each direction vector in turn.
        amplitudes: the output amplitudes image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = peaks2amp_params(
        directions=directions,
        amplitudes=amplitudes,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return peaks2amp_execute(params, runner)

__all__ = [
    "Peaks2amp",
    "Peaks2ampOutputs",
    "PEAKS2AMP_METADATA",
    "peaks2amp_cargs",
    "peaks2amp_outputs",
    "peaks2amp_params",
    "peaks2amp_execute",
    "peaks2amp_validate",
    "peaks2amp",
]