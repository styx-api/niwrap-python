# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCK2FIXEL_METADATA = Metadata(
    id="tck2fixel",
    name="tck2fixel",
    package="mrtrix",
)

class Tck2fixelConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tck2fixel = typing.TypedDict(
    "Tck2fixel",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tck2fixel"]],
        "angle": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Tck2fixelConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks": InputPathType,
        "fixel_folder_in": InputPathType,
        "fixel_folder_out": str,
        "fixel_data_out": str,
    },
)

@dataclasses.dataclass
class Tck2fixelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def tck2fixel_params(
    tracks: InputPathType,
    fixel_folder_in: InputPathType,
    fixel_folder_out: str,
    fixel_data_out: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tck2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tck2fixel:
    """
    Build parameters.

    Args:
        tracks: the input tracks.
        fixel_folder_in: the input fixel folder. Used to define the fixels and\
            their directions
        fixel_folder_out: the fixel folder to which the output will be written.\
            This can be the same as the input folder if desired
        fixel_data_out: the name of the fixel data image.
        angle: the max angle threshold for assigning streamline tangents to fixels\
            (Default: 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tck2fixel = {
        "@type": "mrtrix/tck2fixel",
        "tracks": tracks,
        "fixel_folder_in": fixel_folder_in,
        "fixel_folder_out": fixel_folder_out,
        "fixel_data_out": fixel_data_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if angle is not None:
        params["angle"] = angle
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tck2fixel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tck2fixel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("angle", None) is not None:
        if not isinstance(params["angle"], (float, int)):
            raise StyxValidationError(f'`angle` has the wrong type: Received `{type(params["angle"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Tck2fixelConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `InputPathType`')
    if params.get("fixel_folder_in", None) is None:
        raise StyxValidationError("`fixel_folder_in` must not be None")
    if not isinstance(params["fixel_folder_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_folder_in` has the wrong type: Received `{type(params["fixel_folder_in"])}` expected `InputPathType`')
    if params.get("fixel_folder_out", None) is None:
        raise StyxValidationError("`fixel_folder_out` must not be None")
    if not isinstance(params["fixel_folder_out"], str):
        raise StyxValidationError(f'`fixel_folder_out` has the wrong type: Received `{type(params["fixel_folder_out"])}` expected `str`')
    if params.get("fixel_data_out", None) is None:
        raise StyxValidationError("`fixel_data_out` must not be None")
    if not isinstance(params["fixel_data_out"], str):
        raise StyxValidationError(f'`fixel_data_out` has the wrong type: Received `{type(params["fixel_data_out"])}` expected `str`')

def tck2fixel_cargs(params: Tck2fixel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tck2fixel")
    v_0 = params.get("angle")
    if v_0 is not None:
        cargs.append("-angle")
        cargs.append(str(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks"]))
    cargs.append(execution.input_file(params["fixel_folder_in"]))
    cargs.append(params["fixel_folder_out"])
    cargs.append(params["fixel_data_out"])
    return cargs

def tck2fixel_outputs(params: Tck2fixel, execution: Execution) -> Tck2fixelOutputs:
    """Build the Tck2fixelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Tck2fixelOutputs(
        root=root_v,
    )

def tck2fixel_execute(params: Tck2fixel, runner: Runner | None = None) -> Tck2fixelOutputs:
    """
    tck2fixel

    Compute a fixel TDI map from a tractogram.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tck2fixel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCK2FIXEL_METADATA)
    execution.params(params)
    args = tck2fixel_cargs(params, execution)
    out = tck2fixel_outputs(params, execution)
    execution.run(args)
    return out

def tck2fixel(
    tracks: InputPathType,
    fixel_folder_in: InputPathType,
    fixel_folder_out: str,
    fixel_data_out: str,
    angle: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tck2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Tck2fixelOutputs:
    """
    tck2fixel

    Compute a fixel TDI map from a tractogram.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks: the input tracks.
        fixel_folder_in: the input fixel folder. Used to define the fixels and\
            their directions
        fixel_folder_out: the fixel folder to which the output will be written.\
            This can be the same as the input folder if desired
        fixel_data_out: the name of the fixel data image.
        angle: the max angle threshold for assigning streamline tangents to fixels\
            (Default: 45 degrees)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tck2fixel_params(
        tracks=tracks,
        fixel_folder_in=fixel_folder_in,
        fixel_folder_out=fixel_folder_out,
        fixel_data_out=fixel_data_out,
        angle=angle,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tck2fixel_execute(params, runner)

__all__ = [
    "Tck2fixel",
    "Tck2fixelOutputs",
    "TCK2FIXEL_METADATA",
    "tck2fixel_cargs",
    "tck2fixel_outputs",
    "tck2fixel_params",
    "tck2fixel_execute",
    "tck2fixel_validate",
    "tck2fixel",
]