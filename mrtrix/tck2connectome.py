# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCK2CONNECTOME_METADATA = Metadata(
    id="tck2connectome",
    name="tck2connectome",
    package="mrtrix",
)

class Tck2connectomeConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tck2connectome = typing.TypedDict(
    "Tck2connectome",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tck2connectome"]],
        "assignment_end_voxels": typing.NotRequired[bool],
        "assignment_radial_search": typing.NotRequired[float],
        "assignment_reverse_search": typing.NotRequired[float],
        "assignment_forward_search": typing.NotRequired[float],
        "assignment_all_voxels": typing.NotRequired[bool],
        "scale_length": typing.NotRequired[bool],
        "scale_invlength": typing.NotRequired[bool],
        "scale_invnodevol": typing.NotRequired[bool],
        "scale_file": typing.NotRequired[InputPathType],
        "symmetric": typing.NotRequired[bool],
        "zero_diagonal": typing.NotRequired[bool],
        "stat_edge": typing.NotRequired[str],
        "tck_weights_in": typing.NotRequired[InputPathType],
        "keep_unassigned": typing.NotRequired[bool],
        "out_assignments": typing.NotRequired[str],
        "vector": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Tck2connectomeConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "tracks_in": InputPathType,
        "nodes_in": InputPathType,
        "connectome_out": str,
    },
)

@dataclasses.dataclass
class Tck2connectomeOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    connectome_out: OutputPathType
    """the output .csv file containing edge weights"""
    out_assignments: OutputPathType | None
    """output the node assignments of each streamline to a file; this can be used subsequently e.g. by the command connectome2tck """

def tck2connectome_params(
    tracks_in: InputPathType,
    nodes_in: InputPathType,
    connectome_out: str,
    assignment_end_voxels: bool = False,
    assignment_radial_search: float | None = None,
    assignment_reverse_search: float | None = None,
    assignment_forward_search: float | None = None,
    assignment_all_voxels: bool = False,
    scale_length: bool = False,
    scale_invlength: bool = False,
    scale_invnodevol: bool = False,
    scale_file: InputPathType | None = None,
    symmetric: bool = False,
    zero_diagonal: bool = False,
    stat_edge: str | None = None,
    tck_weights_in: InputPathType | None = None,
    keep_unassigned: bool = False,
    out_assignments: str | None = None,
    vector: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tck2connectomeConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tck2connectome:
    """
    Build parameters.

    Args:
        tracks_in: the input track file
        nodes_in: the input node parcellation image
        connectome_out: the output .csv file containing edge weights
        assignment_end_voxels: use a simple voxel lookup value at each streamline\
            endpoint
        assignment_radial_search: perform a radial search from each streamline\
            endpoint to locate the nearest node. Argument is the maximum radius in\
            mm; if no node is found within this radius, the streamline endpoint is\
            not assigned to any node. Default search distance is 4mm.
        assignment_reverse_search: traverse from each streamline endpoint inwards\
            along the streamline, in search of the last node traversed by the\
            streamline. Argument is the maximum traversal length in mm (set to 0 to\
            allow search to continue to the streamline midpoint).
        assignment_forward_search: project the streamline forwards from the\
            endpoint in search of a parcellation node voxel. Argument is the\
            maximum traversal length in mm.
        assignment_all_voxels: assign the streamline to all nodes it intersects\
            along its length (note that this means a streamline may be assigned to\
            more than two nodes, or indeed none at all)
        scale_length: scale each contribution to the connectome edge by the length\
            of the streamline
        scale_invlength: scale each contribution to the connectome edge by the\
            inverse of the streamline length
        scale_invnodevol: scale each contribution to the connectome edge by the\
            inverse of the two node volumes
        scale_file: scale each contribution to the connectome edge according to the\
            values in a vector file
        symmetric: Make matrices symmetric on output
        zero_diagonal: Set matrix diagonal to zero on output
        stat_edge: statistic for combining the values from all streamlines in an\
            edge into a single scale value for that edge (options are:\
            sum,mean,min,max; default=sum)
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        keep_unassigned: By default, the program discards the information regarding\
            those streamlines that are not successfully assigned to a node pair.\
            Set this option to keep these values (will be the first row/column in\
            the output matrix)
        out_assignments: output the node assignments of each streamline to a file;\
            this can be used subsequently e.g. by the command connectome2tck
        vector: output a vector representing connectivities from a given seed point\
            to target nodes, rather than a matrix of node-node connectivities
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tck2connectome = {
        "@type": "mrtrix/tck2connectome",
        "tracks_in": tracks_in,
        "nodes_in": nodes_in,
        "connectome_out": connectome_out,
        "assignment_end_voxels": assignment_end_voxels,
        "assignment_all_voxels": assignment_all_voxels,
        "scale_length": scale_length,
        "scale_invlength": scale_invlength,
        "scale_invnodevol": scale_invnodevol,
        "symmetric": symmetric,
        "zero_diagonal": zero_diagonal,
        "keep_unassigned": keep_unassigned,
        "vector": vector,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if assignment_radial_search is not None:
        params["assignment_radial_search"] = assignment_radial_search
    if assignment_reverse_search is not None:
        params["assignment_reverse_search"] = assignment_reverse_search
    if assignment_forward_search is not None:
        params["assignment_forward_search"] = assignment_forward_search
    if scale_file is not None:
        params["scale_file"] = scale_file
    if stat_edge is not None:
        params["stat_edge"] = stat_edge
    if tck_weights_in is not None:
        params["tck_weights_in"] = tck_weights_in
    if out_assignments is not None:
        params["out_assignments"] = out_assignments
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tck2connectome_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tck2connectome."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("assignment_end_voxels", None) is not None:
        if not isinstance(params["assignment_end_voxels"], bool):
            raise StyxValidationError(f'`assignment_end_voxels` has the wrong type: Received `{type(params["assignment_end_voxels"])}` expected `bool`')
    if params.get("assignment_radial_search", None) is not None:
        if not isinstance(params["assignment_radial_search"], (float, int)):
            raise StyxValidationError(f'`assignment_radial_search` has the wrong type: Received `{type(params["assignment_radial_search"])}` expected `float`')
    if params.get("assignment_reverse_search", None) is not None:
        if not isinstance(params["assignment_reverse_search"], (float, int)):
            raise StyxValidationError(f'`assignment_reverse_search` has the wrong type: Received `{type(params["assignment_reverse_search"])}` expected `float`')
    if params.get("assignment_forward_search", None) is not None:
        if not isinstance(params["assignment_forward_search"], (float, int)):
            raise StyxValidationError(f'`assignment_forward_search` has the wrong type: Received `{type(params["assignment_forward_search"])}` expected `float`')
    if params.get("assignment_all_voxels", None) is not None:
        if not isinstance(params["assignment_all_voxels"], bool):
            raise StyxValidationError(f'`assignment_all_voxels` has the wrong type: Received `{type(params["assignment_all_voxels"])}` expected `bool`')
    if params.get("scale_length", None) is not None:
        if not isinstance(params["scale_length"], bool):
            raise StyxValidationError(f'`scale_length` has the wrong type: Received `{type(params["scale_length"])}` expected `bool`')
    if params.get("scale_invlength", None) is not None:
        if not isinstance(params["scale_invlength"], bool):
            raise StyxValidationError(f'`scale_invlength` has the wrong type: Received `{type(params["scale_invlength"])}` expected `bool`')
    if params.get("scale_invnodevol", None) is not None:
        if not isinstance(params["scale_invnodevol"], bool):
            raise StyxValidationError(f'`scale_invnodevol` has the wrong type: Received `{type(params["scale_invnodevol"])}` expected `bool`')
    if params.get("scale_file", None) is not None:
        if not isinstance(params["scale_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`scale_file` has the wrong type: Received `{type(params["scale_file"])}` expected `InputPathType`')
    if params.get("symmetric", None) is not None:
        if not isinstance(params["symmetric"], bool):
            raise StyxValidationError(f'`symmetric` has the wrong type: Received `{type(params["symmetric"])}` expected `bool`')
    if params.get("zero_diagonal", None) is not None:
        if not isinstance(params["zero_diagonal"], bool):
            raise StyxValidationError(f'`zero_diagonal` has the wrong type: Received `{type(params["zero_diagonal"])}` expected `bool`')
    if params.get("stat_edge", None) is not None:
        if not isinstance(params["stat_edge"], str):
            raise StyxValidationError(f'`stat_edge` has the wrong type: Received `{type(params["stat_edge"])}` expected `str`')
    if params.get("tck_weights_in", None) is not None:
        if not isinstance(params["tck_weights_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`tck_weights_in` has the wrong type: Received `{type(params["tck_weights_in"])}` expected `InputPathType`')
    if params.get("keep_unassigned", None) is not None:
        if not isinstance(params["keep_unassigned"], bool):
            raise StyxValidationError(f'`keep_unassigned` has the wrong type: Received `{type(params["keep_unassigned"])}` expected `bool`')
    if params.get("out_assignments", None) is not None:
        if not isinstance(params["out_assignments"], str):
            raise StyxValidationError(f'`out_assignments` has the wrong type: Received `{type(params["out_assignments"])}` expected `str`')
    if params.get("vector", None) is not None:
        if not isinstance(params["vector"], bool):
            raise StyxValidationError(f'`vector` has the wrong type: Received `{type(params["vector"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Tck2connectomeConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("tracks_in", None) is None:
        raise StyxValidationError("`tracks_in` must not be None")
    if not isinstance(params["tracks_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`tracks_in` has the wrong type: Received `{type(params["tracks_in"])}` expected `InputPathType`')
    if params.get("nodes_in", None) is None:
        raise StyxValidationError("`nodes_in` must not be None")
    if not isinstance(params["nodes_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`nodes_in` has the wrong type: Received `{type(params["nodes_in"])}` expected `InputPathType`')
    if params.get("connectome_out", None) is None:
        raise StyxValidationError("`connectome_out` must not be None")
    if not isinstance(params["connectome_out"], str):
        raise StyxValidationError(f'`connectome_out` has the wrong type: Received `{type(params["connectome_out"])}` expected `str`')

def tck2connectome_cargs(params: Tck2connectome, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tck2connectome")
    if params.get("assignment_end_voxels"):
        cargs.append("-assignment_end_voxels")
    v_0 = params.get("assignment_radial_search")
    if v_0 is not None:
        cargs.append("-assignment_radial_search")
        cargs.append(str(v_0))
    v_1 = params.get("assignment_reverse_search")
    if v_1 is not None:
        cargs.append("-assignment_reverse_search")
        cargs.append(str(v_1))
    v_2 = params.get("assignment_forward_search")
    if v_2 is not None:
        cargs.append("-assignment_forward_search")
        cargs.append(str(v_2))
    if params.get("assignment_all_voxels"):
        cargs.append("-assignment_all_voxels")
    if params.get("scale_length"):
        cargs.append("-scale_length")
    if params.get("scale_invlength"):
        cargs.append("-scale_invlength")
    if params.get("scale_invnodevol"):
        cargs.append("-scale_invnodevol")
    v_3 = params.get("scale_file")
    if v_3 is not None:
        cargs.append("-scale_file")
        cargs.append(execution.input_file(v_3))
    if params.get("symmetric"):
        cargs.append("-symmetric")
    if params.get("zero_diagonal"):
        cargs.append("-zero_diagonal")
    v_4 = params.get("stat_edge")
    if v_4 is not None:
        cargs.append("-stat_edge")
        cargs.append(v_4)
    v_5 = params.get("tck_weights_in")
    if v_5 is not None:
        cargs.append("-tck_weights_in")
        cargs.append(execution.input_file(v_5))
    if params.get("keep_unassigned"):
        cargs.append("-keep_unassigned")
    v_6 = params.get("out_assignments")
    if v_6 is not None:
        cargs.append("-out_assignments")
        cargs.append(v_6)
    if params.get("vector"):
        cargs.append("-vector")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_7 = params.get("nthreads")
    if v_7 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_7))
    v_8 = params.get("config")
    if v_8 is not None:
        for item0 in v_8:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["tracks_in"]))
    cargs.append(execution.input_file(params["nodes_in"]))
    cargs.append(params["connectome_out"])
    return cargs

def tck2connectome_outputs(params: Tck2connectome, execution: Execution) -> Tck2connectomeOutputs:
    """Build the Tck2connectomeOutputs object for this tool."""
    out_assignments_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    connectome_out_v: OutputPathType = execution.output_file(params["connectome_out"])
    __v0 = params.get("out_assignments")
    if __v0 is not None:
        out_assignments_v = execution.output_file((__v0 if __v0 is not None else ""))
    return Tck2connectomeOutputs(
        root=root_v,
        connectome_out=connectome_out_v,
        out_assignments=out_assignments_v,
    )

def tck2connectome_execute(params: Tck2connectome, runner: Runner | None = None) -> Tck2connectomeOutputs:
    """
    tck2connectome

    Generate a connectome matrix from a streamlines file and a node parcellation image.



References:

If using the default streamline-parcel assignment mechanism (or -assignment_radial_search option): Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. The effects of SIFT on the reproducibility and biological accuracy of the structural connectome. NeuroImage, 2015, 104, 253-265

If using -scale_invlength or -scale_invnodevol options: Hagmann, P.; Cammoun, L.; Gigandet, X.; Meuli, R.; Honey, C.; Wedeen, V. & Sporns, O. Mapping the Structural Core of Human Cerebral Cortex. PLoS Biology 6(7), e159

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tck2connectome_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCK2CONNECTOME_METADATA)
    execution.params(params)
    args = tck2connectome_cargs(params, execution)
    out = tck2connectome_outputs(params, execution)
    execution.run(args)
    return out

def tck2connectome(
    tracks_in: InputPathType,
    nodes_in: InputPathType,
    connectome_out: str,
    assignment_end_voxels: bool = False,
    assignment_radial_search: float | None = None,
    assignment_reverse_search: float | None = None,
    assignment_forward_search: float | None = None,
    assignment_all_voxels: bool = False,
    scale_length: bool = False,
    scale_invlength: bool = False,
    scale_invnodevol: bool = False,
    scale_file: InputPathType | None = None,
    symmetric: bool = False,
    zero_diagonal: bool = False,
    stat_edge: str | None = None,
    tck_weights_in: InputPathType | None = None,
    keep_unassigned: bool = False,
    out_assignments: str | None = None,
    vector: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Tck2connectomeConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Tck2connectomeOutputs:
    """
    tck2connectome

    Generate a connectome matrix from a streamlines file and a node parcellation image.



References:

If using the default streamline-parcel assignment mechanism (or -assignment_radial_search option): Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. The effects of SIFT on the reproducibility and biological accuracy of the structural connectome. NeuroImage, 2015, 104, 253-265

If using -scale_invlength or -scale_invnodevol options: Hagmann, P.; Cammoun, L.; Gigandet, X.; Meuli, R.; Honey, C.; Wedeen, V. & Sporns, O. Mapping the Structural Core of Human Cerebral Cortex. PLoS Biology 6(7), e159

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        tracks_in: the input track file
        nodes_in: the input node parcellation image
        connectome_out: the output .csv file containing edge weights
        assignment_end_voxels: use a simple voxel lookup value at each streamline\
            endpoint
        assignment_radial_search: perform a radial search from each streamline\
            endpoint to locate the nearest node. Argument is the maximum radius in\
            mm; if no node is found within this radius, the streamline endpoint is\
            not assigned to any node. Default search distance is 4mm.
        assignment_reverse_search: traverse from each streamline endpoint inwards\
            along the streamline, in search of the last node traversed by the\
            streamline. Argument is the maximum traversal length in mm (set to 0 to\
            allow search to continue to the streamline midpoint).
        assignment_forward_search: project the streamline forwards from the\
            endpoint in search of a parcellation node voxel. Argument is the\
            maximum traversal length in mm.
        assignment_all_voxels: assign the streamline to all nodes it intersects\
            along its length (note that this means a streamline may be assigned to\
            more than two nodes, or indeed none at all)
        scale_length: scale each contribution to the connectome edge by the length\
            of the streamline
        scale_invlength: scale each contribution to the connectome edge by the\
            inverse of the streamline length
        scale_invnodevol: scale each contribution to the connectome edge by the\
            inverse of the two node volumes
        scale_file: scale each contribution to the connectome edge according to the\
            values in a vector file
        symmetric: Make matrices symmetric on output
        zero_diagonal: Set matrix diagonal to zero on output
        stat_edge: statistic for combining the values from all streamlines in an\
            edge into a single scale value for that edge (options are:\
            sum,mean,min,max; default=sum)
        tck_weights_in: specify a text scalar file containing the streamline\
            weights
        keep_unassigned: By default, the program discards the information regarding\
            those streamlines that are not successfully assigned to a node pair.\
            Set this option to keep these values (will be the first row/column in\
            the output matrix)
        out_assignments: output the node assignments of each streamline to a file;\
            this can be used subsequently e.g. by the command connectome2tck
        vector: output a vector representing connectivities from a given seed point\
            to target nodes, rather than a matrix of node-node connectivities
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tck2connectome_params(
        tracks_in=tracks_in,
        nodes_in=nodes_in,
        connectome_out=connectome_out,
        assignment_end_voxels=assignment_end_voxels,
        assignment_radial_search=assignment_radial_search,
        assignment_reverse_search=assignment_reverse_search,
        assignment_forward_search=assignment_forward_search,
        assignment_all_voxels=assignment_all_voxels,
        scale_length=scale_length,
        scale_invlength=scale_invlength,
        scale_invnodevol=scale_invnodevol,
        scale_file=scale_file,
        symmetric=symmetric,
        zero_diagonal=zero_diagonal,
        stat_edge=stat_edge,
        tck_weights_in=tck_weights_in,
        keep_unassigned=keep_unassigned,
        out_assignments=out_assignments,
        vector=vector,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tck2connectome_execute(params, runner)

__all__ = [
    "Tck2connectome",
    "Tck2connectomeOutputs",
    "TCK2CONNECTOME_METADATA",
    "tck2connectome_cargs",
    "tck2connectome_outputs",
    "tck2connectome_params",
    "tck2connectome_execute",
    "tck2connectome_validate",
    "tck2connectome",
]