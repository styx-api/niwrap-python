# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKGLOBAL_METADATA = Metadata(
    id="tckglobal",
    name="tckglobal",
    package="mrtrix",
)

class TckglobalConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckglobal = typing.TypedDict(
    "Tckglobal",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckglobal"]],
        "grad": typing.NotRequired[InputPathType],
        "mask": typing.NotRequired[InputPathType],
        "riso": typing.NotRequired[list[InputPathType]],
        "lmax": typing.NotRequired[int],
        "length": typing.NotRequired[float],
        "weight": typing.NotRequired[float],
        "ppot": typing.NotRequired[float],
        "cpot": typing.NotRequired[float],
        "t0": typing.NotRequired[float],
        "t1": typing.NotRequired[float],
        "niter": typing.NotRequired[int],
        "fod": typing.NotRequired[str],
        "noapo": typing.NotRequired[bool],
        "fiso": typing.NotRequired[str],
        "eext": typing.NotRequired[str],
        "etrend": typing.NotRequired[str],
        "balance": typing.NotRequired[float],
        "density": typing.NotRequired[float],
        "prob": typing.NotRequired[list[float]],
        "beta": typing.NotRequired[float],
        "lambda": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckglobalConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "source": InputPathType,
        "response": InputPathType,
        "tracks": str,
    },
)

@dataclasses.dataclass
class TckglobalOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    tracks: OutputPathType
    """the output file containing the tracks generated."""
    fod: OutputPathType | None
    """
    Predicted fibre orientation distribution function (fODF).
    This fODF is estimated as part of the global track optimization, and therefore incorporates the spatial regularization that it imposes. Internally, the fODF is represented as a discrete sum of apodized point spread functions (aPSF) oriented along the directions of all particles in the voxel, used to predict the DWI signal from the particle configuration. 
    """
    fiso: OutputPathType | None
    """Predicted isotropic fractions of the tissues for which response functions were provided with -riso. Typically, these are CSF and GM. """
    eext: OutputPathType | None
    """Residual external energy in every voxel. """
    etrend: OutputPathType | None
    """internal and external energy trend and cooling statistics. """

def tckglobal_params(
    source: InputPathType,
    response: InputPathType,
    tracks: str,
    grad: InputPathType | None = None,
    mask: InputPathType | None = None,
    riso: list[InputPathType] | None = None,
    lmax: int | None = None,
    length: float | None = None,
    weight: float | None = None,
    ppot: float | None = None,
    cpot: float | None = None,
    t0: float | None = None,
    t1: float | None = None,
    niter: int | None = None,
    fod: str | None = None,
    noapo: bool = False,
    fiso: str | None = None,
    eext: str | None = None,
    etrend: str | None = None,
    balance: float | None = None,
    density: float | None = None,
    prob: list[float] | None = None,
    beta: float | None = None,
    lambda_: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckglobalConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckglobal:
    """
    Build parameters.

    Args:
        source: the image containing the raw DWI data.
        response: the response of a track segment on the DWI signal.
        tracks: the output file containing the tracks generated.
        grad: specify the diffusion encoding scheme (required if not supplied in\
            the header).
        mask: only reconstruct the tractogram within the specified brain mask\
            image.
        riso: set one or more isotropic response functions. (multiple allowed)
        lmax: set the maximum harmonic order for the output series. (default = 8)
        length: set the length of the particles (fibre segments). (default = 1mm)
        weight: set the weight by which particles contribute to the model. (default\
            = 0.1)
        ppot: set the particle potential, i.e., the cost of adding one segment,\
            relative to the particle weight. (default = 0.05)
        cpot: set the connection potential, i.e., the energy term that drives two\
            segments together. (default = 0.5)
        t0: set the initial temperature of the metropolis hastings optimizer.\
            (default = 0.1)
        t1: set the final temperature of the metropolis hastings optimizer.\
            (default = 0.001)
        niter: set the number of iterations of the metropolis hastings optimizer.\
            (default = 10M)
        fod: Predicted fibre orientation distribution function (fODF). This fODF is\
            estimated as part of the global track optimization, and therefore\
            incorporates the spatial regularization that it imposes. Internally,\
            the fODF is represented as a discrete sum of apodized point spread\
            functions (aPSF) oriented along the directions of all particles in the\
            voxel, used to predict the DWI signal from the particle configuration.
        noapo: disable spherical convolution of fODF with apodized PSF, to output a\
            sum of delta functions rather than a sum of aPSFs.
        fiso: Predicted isotropic fractions of the tissues for which response\
            functions were provided with -riso. Typically, these are CSF and GM.
        eext: Residual external energy in every voxel.
        etrend: internal and external energy trend and cooling statistics.
        balance: balance internal and external energy. (default = 0) Negative\
            values give more weight to the internal energy, positive to the\
            external energy.
        density: set the desired density of the free Poisson process. (default = 1)
        prob: set the probabilities of generating birth, death, randshift, optshift\
            and connect proposals respectively. (default = 0.25,0.05,0.25,0.1,0.35)
        beta: set the width of the Hanning interpolation window. (in [0, 1],\
            default = 0) If used, a mask is required, and this mask must keep at\
            least one voxel distance to the image bounding box.
        lambda_: set the weight of the internal energy directly. (default = 1) If\
            provided, any value of -balance will be ignored.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckglobal = {
        "@type": "mrtrix/tckglobal",
        "source": source,
        "response": response,
        "tracks": tracks,
        "noapo": noapo,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if grad is not None:
        params["grad"] = grad
    if mask is not None:
        params["mask"] = mask
    if riso is not None:
        params["riso"] = riso
    if lmax is not None:
        params["lmax"] = lmax
    if length is not None:
        params["length"] = length
    if weight is not None:
        params["weight"] = weight
    if ppot is not None:
        params["ppot"] = ppot
    if cpot is not None:
        params["cpot"] = cpot
    if t0 is not None:
        params["t0"] = t0
    if t1 is not None:
        params["t1"] = t1
    if niter is not None:
        params["niter"] = niter
    if fod is not None:
        params["fod"] = fod
    if fiso is not None:
        params["fiso"] = fiso
    if eext is not None:
        params["eext"] = eext
    if etrend is not None:
        params["etrend"] = etrend
    if balance is not None:
        params["balance"] = balance
    if density is not None:
        params["density"] = density
    if prob is not None:
        params["prob"] = prob
    if beta is not None:
        params["beta"] = beta
    if lambda_ is not None:
        params["lambda"] = lambda_
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckglobal_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckglobal."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("riso", None) is not None:
        if not isinstance(params["riso"], list):
            raise StyxValidationError(f'`riso` has the wrong type: Received `{type(params["riso"])}` expected `list[InputPathType]`')
        for e in params["riso"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`riso` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("lmax", None) is not None:
        if not isinstance(params["lmax"], int):
            raise StyxValidationError(f'`lmax` has the wrong type: Received `{type(params["lmax"])}` expected `int`')
    if params.get("length", None) is not None:
        if not isinstance(params["length"], (float, int)):
            raise StyxValidationError(f'`length` has the wrong type: Received `{type(params["length"])}` expected `float`')
    if params.get("weight", None) is not None:
        if not isinstance(params["weight"], (float, int)):
            raise StyxValidationError(f'`weight` has the wrong type: Received `{type(params["weight"])}` expected `float`')
    if params.get("ppot", None) is not None:
        if not isinstance(params["ppot"], (float, int)):
            raise StyxValidationError(f'`ppot` has the wrong type: Received `{type(params["ppot"])}` expected `float`')
    if params.get("cpot", None) is not None:
        if not isinstance(params["cpot"], (float, int)):
            raise StyxValidationError(f'`cpot` has the wrong type: Received `{type(params["cpot"])}` expected `float`')
    if params.get("t0", None) is not None:
        if not isinstance(params["t0"], (float, int)):
            raise StyxValidationError(f'`t0` has the wrong type: Received `{type(params["t0"])}` expected `float`')
    if params.get("t1", None) is not None:
        if not isinstance(params["t1"], (float, int)):
            raise StyxValidationError(f'`t1` has the wrong type: Received `{type(params["t1"])}` expected `float`')
    if params.get("niter", None) is not None:
        if not isinstance(params["niter"], int):
            raise StyxValidationError(f'`niter` has the wrong type: Received `{type(params["niter"])}` expected `int`')
    if params.get("fod", None) is not None:
        if not isinstance(params["fod"], str):
            raise StyxValidationError(f'`fod` has the wrong type: Received `{type(params["fod"])}` expected `str`')
    if params.get("noapo", None) is not None:
        if not isinstance(params["noapo"], bool):
            raise StyxValidationError(f'`noapo` has the wrong type: Received `{type(params["noapo"])}` expected `bool`')
    if params.get("fiso", None) is not None:
        if not isinstance(params["fiso"], str):
            raise StyxValidationError(f'`fiso` has the wrong type: Received `{type(params["fiso"])}` expected `str`')
    if params.get("eext", None) is not None:
        if not isinstance(params["eext"], str):
            raise StyxValidationError(f'`eext` has the wrong type: Received `{type(params["eext"])}` expected `str`')
    if params.get("etrend", None) is not None:
        if not isinstance(params["etrend"], str):
            raise StyxValidationError(f'`etrend` has the wrong type: Received `{type(params["etrend"])}` expected `str`')
    if params.get("balance", None) is not None:
        if not isinstance(params["balance"], (float, int)):
            raise StyxValidationError(f'`balance` has the wrong type: Received `{type(params["balance"])}` expected `float`')
    if params.get("density", None) is not None:
        if not isinstance(params["density"], (float, int)):
            raise StyxValidationError(f'`density` has the wrong type: Received `{type(params["density"])}` expected `float`')
    if params.get("prob", None) is not None:
        if not isinstance(params["prob"], list):
            raise StyxValidationError(f'`prob` has the wrong type: Received `{type(params["prob"])}` expected `list[float]`')
        for e_2 in params["prob"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`prob` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("beta", None) is not None:
        if not isinstance(params["beta"], (float, int)):
            raise StyxValidationError(f'`beta` has the wrong type: Received `{type(params["beta"])}` expected `float`')
    if params.get("lambda", None) is not None:
        if not isinstance(params["lambda"], (float, int)):
            raise StyxValidationError(f'`lambda` has the wrong type: Received `{type(params["lambda"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckglobalConfig]`')
        for e_3 in params["config"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_3["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_3["key"])}` expected `str`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("source", None) is None:
        raise StyxValidationError("`source` must not be None")
    if not isinstance(params["source"], (pathlib.Path, str)):
        raise StyxValidationError(f'`source` has the wrong type: Received `{type(params["source"])}` expected `InputPathType`')
    if params.get("response", None) is None:
        raise StyxValidationError("`response` must not be None")
    if not isinstance(params["response"], (pathlib.Path, str)):
        raise StyxValidationError(f'`response` has the wrong type: Received `{type(params["response"])}` expected `InputPathType`')
    if params.get("tracks", None) is None:
        raise StyxValidationError("`tracks` must not be None")
    if not isinstance(params["tracks"], str):
        raise StyxValidationError(f'`tracks` has the wrong type: Received `{type(params["tracks"])}` expected `str`')

def tckglobal_cargs(params: Tckglobal, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckglobal")
    v_0 = params.get("grad")
    if v_0 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask")
    if v_1 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("riso")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-riso")
            cargs.append(execution.input_file(item0))
    v_3 = params.get("lmax")
    if v_3 is not None:
        cargs.append("-lmax")
        cargs.append(str(v_3))
    v_4 = params.get("length")
    if v_4 is not None:
        cargs.append("-length")
        cargs.append(str(v_4))
    v_5 = params.get("weight")
    if v_5 is not None:
        cargs.append("-weight")
        cargs.append(str(v_5))
    v_6 = params.get("ppot")
    if v_6 is not None:
        cargs.append("-ppot")
        cargs.append(str(v_6))
    v_7 = params.get("cpot")
    if v_7 is not None:
        cargs.append("-cpot")
        cargs.append(str(v_7))
    v_8 = params.get("t0")
    if v_8 is not None:
        cargs.append("-t0")
        cargs.append(str(v_8))
    v_9 = params.get("t1")
    if v_9 is not None:
        cargs.append("-t1")
        cargs.append(str(v_9))
    v_10 = params.get("niter")
    if v_10 is not None:
        cargs.append("-niter")
        cargs.append(str(v_10))
    v_11 = params.get("fod")
    if v_11 is not None:
        cargs.append("-fod")
        cargs.append(v_11)
    if params.get("noapo"):
        cargs.append("-noapo")
    v_12 = params.get("fiso")
    if v_12 is not None:
        cargs.append("-fiso")
        cargs.append(v_12)
    v_13 = params.get("eext")
    if v_13 is not None:
        cargs.append("-eext")
        cargs.append(v_13)
    v_14 = params.get("etrend")
    if v_14 is not None:
        cargs.append("-etrend")
        cargs.append(v_14)
    v_15 = params.get("balance")
    if v_15 is not None:
        cargs.append("-balance")
        cargs.append(str(v_15))
    v_16 = params.get("density")
    if v_16 is not None:
        cargs.append("-density")
        cargs.append(str(v_16))
    v_17 = params.get("prob")
    if v_17 is not None:
        cargs.append("-prob")
        for item1 in v_17:
            cargs.append(str(item1))
    v_18 = params.get("beta")
    if v_18 is not None:
        cargs.append("-beta")
        cargs.append(str(v_18))
    v_19 = params.get("lambda")
    if v_19 is not None:
        cargs.append("-lambda")
        cargs.append(str(v_19))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_20 = params.get("nthreads")
    if v_20 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_20))
    v_21 = params.get("config")
    if v_21 is not None:
        for item2 in v_21:
            cargs.append("-config")
            cargs.append(item2["key"])
            cargs.append(item2["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["source"]))
    cargs.append(execution.input_file(params["response"]))
    cargs.append(params["tracks"])
    return cargs

def tckglobal_outputs(params: Tckglobal, execution: Execution) -> TckglobalOutputs:
    """Build the TckglobalOutputs object for this tool."""
    fod_v: OutputPathType | None = None
    fiso_v: OutputPathType | None = None
    eext_v: OutputPathType | None = None
    etrend_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    tracks_v: OutputPathType = execution.output_file(params["tracks"])
    __v0 = params.get("fod")
    if __v0 is not None:
        fod_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("fiso")
    if __v1 is not None:
        fiso_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("eext")
    if __v2 is not None:
        eext_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("etrend")
    if __v3 is not None:
        etrend_v = execution.output_file((__v3 if __v3 is not None else ""))
    return TckglobalOutputs(
        root=root_v,
        tracks=tracks_v,
        fod=fod_v,
        fiso=fiso_v,
        eext=eext_v,
        etrend=etrend_v,
    )

def tckglobal_execute(params: Tckglobal, runner: Runner | None = None) -> TckglobalOutputs:
    """
    tckglobal

    Multi-Shell Multi-Tissue Global Tractography.

This command will reconstruct the global white matter fibre tractogram that best explains the input DWI data, using a multi-tissue spherical convolution model.

Example use: 

 $ tckglobal dwi.mif wmr.txt -riso csfr.txt -riso gmr.txt -mask mask.mif 
   -niter 1e9 -fod fod.mif -fiso fiso.mif tracks.tck 

in which dwi.mif is the input image, wmr.txt is an anisotropic, multi-shell response function for WM, and csfr.txt and gmr.txt are isotropic response functions for CSF and GM. The output tractogram is saved to tracks.tck. Optional output images fod.mif and fiso.mif contain the predicted WM fODF and isotropic tissue fractions of CSF and GM respectively, estimated as part of the global optimization and thus affected by spatial regularization.

References:

Christiaens, D.; Reisert, M.; Dhollander, T.; Sunaert, S.; Suetens, P. & Maes, F. Global tractography of multi-shell diffusion-weighted imaging data using a multi-tissue model. NeuroImage, 2015, 123, 89-101

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckglobal_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKGLOBAL_METADATA)
    execution.params(params)
    args = tckglobal_cargs(params, execution)
    out = tckglobal_outputs(params, execution)
    execution.run(args)
    return out

def tckglobal(
    source: InputPathType,
    response: InputPathType,
    tracks: str,
    grad: InputPathType | None = None,
    mask: InputPathType | None = None,
    riso: list[InputPathType] | None = None,
    lmax: int | None = None,
    length: float | None = None,
    weight: float | None = None,
    ppot: float | None = None,
    cpot: float | None = None,
    t0: float | None = None,
    t1: float | None = None,
    niter: int | None = None,
    fod: str | None = None,
    noapo: bool = False,
    fiso: str | None = None,
    eext: str | None = None,
    etrend: str | None = None,
    balance: float | None = None,
    density: float | None = None,
    prob: list[float] | None = None,
    beta: float | None = None,
    lambda_: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckglobalConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckglobalOutputs:
    """
    tckglobal

    Multi-Shell Multi-Tissue Global Tractography.

This command will reconstruct the global white matter fibre tractogram that best explains the input DWI data, using a multi-tissue spherical convolution model.

Example use: 

 $ tckglobal dwi.mif wmr.txt -riso csfr.txt -riso gmr.txt -mask mask.mif 
   -niter 1e9 -fod fod.mif -fiso fiso.mif tracks.tck 

in which dwi.mif is the input image, wmr.txt is an anisotropic, multi-shell response function for WM, and csfr.txt and gmr.txt are isotropic response functions for CSF and GM. The output tractogram is saved to tracks.tck. Optional output images fod.mif and fiso.mif contain the predicted WM fODF and isotropic tissue fractions of CSF and GM respectively, estimated as part of the global optimization and thus affected by spatial regularization.

References:

Christiaens, D.; Reisert, M.; Dhollander, T.; Sunaert, S.; Suetens, P. & Maes, F. Global tractography of multi-shell diffusion-weighted imaging data using a multi-tissue model. NeuroImage, 2015, 123, 89-101

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        source: the image containing the raw DWI data.
        response: the response of a track segment on the DWI signal.
        tracks: the output file containing the tracks generated.
        grad: specify the diffusion encoding scheme (required if not supplied in\
            the header).
        mask: only reconstruct the tractogram within the specified brain mask\
            image.
        riso: set one or more isotropic response functions. (multiple allowed)
        lmax: set the maximum harmonic order for the output series. (default = 8)
        length: set the length of the particles (fibre segments). (default = 1mm)
        weight: set the weight by which particles contribute to the model. (default\
            = 0.1)
        ppot: set the particle potential, i.e., the cost of adding one segment,\
            relative to the particle weight. (default = 0.05)
        cpot: set the connection potential, i.e., the energy term that drives two\
            segments together. (default = 0.5)
        t0: set the initial temperature of the metropolis hastings optimizer.\
            (default = 0.1)
        t1: set the final temperature of the metropolis hastings optimizer.\
            (default = 0.001)
        niter: set the number of iterations of the metropolis hastings optimizer.\
            (default = 10M)
        fod: Predicted fibre orientation distribution function (fODF). This fODF is\
            estimated as part of the global track optimization, and therefore\
            incorporates the spatial regularization that it imposes. Internally,\
            the fODF is represented as a discrete sum of apodized point spread\
            functions (aPSF) oriented along the directions of all particles in the\
            voxel, used to predict the DWI signal from the particle configuration.
        noapo: disable spherical convolution of fODF with apodized PSF, to output a\
            sum of delta functions rather than a sum of aPSFs.
        fiso: Predicted isotropic fractions of the tissues for which response\
            functions were provided with -riso. Typically, these are CSF and GM.
        eext: Residual external energy in every voxel.
        etrend: internal and external energy trend and cooling statistics.
        balance: balance internal and external energy. (default = 0) Negative\
            values give more weight to the internal energy, positive to the\
            external energy.
        density: set the desired density of the free Poisson process. (default = 1)
        prob: set the probabilities of generating birth, death, randshift, optshift\
            and connect proposals respectively. (default = 0.25,0.05,0.25,0.1,0.35)
        beta: set the width of the Hanning interpolation window. (in [0, 1],\
            default = 0) If used, a mask is required, and this mask must keep at\
            least one voxel distance to the image bounding box.
        lambda_: set the weight of the internal energy directly. (default = 1) If\
            provided, any value of -balance will be ignored.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckglobal_params(
        source=source,
        response=response,
        tracks=tracks,
        grad=grad,
        mask=mask,
        riso=riso,
        lmax=lmax,
        length=length,
        weight=weight,
        ppot=ppot,
        cpot=cpot,
        t0=t0,
        t1=t1,
        niter=niter,
        fod=fod,
        noapo=noapo,
        fiso=fiso,
        eext=eext,
        etrend=etrend,
        balance=balance,
        density=density,
        prob=prob,
        beta=beta,
        lambda_=lambda_,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckglobal_execute(params, runner)

__all__ = [
    "Tckglobal",
    "TckglobalOutputs",
    "TCKGLOBAL_METADATA",
    "tckglobal_cargs",
    "tckglobal_outputs",
    "tckglobal_params",
    "tckglobal_execute",
    "tckglobal_validate",
    "tckglobal",
]