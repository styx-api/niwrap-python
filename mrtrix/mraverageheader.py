# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRAVERAGEHEADER_METADATA = Metadata(
    id="mraverageheader",
    name="mraverageheader",
    package="mrtrix",
)

class MraverageheaderConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mraverageheader = typing.TypedDict(
    "Mraverageheader",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mraverageheader"]],
        "padding": typing.NotRequired[float],
        "resolution": typing.NotRequired[str],
        "fill": typing.NotRequired[bool],
        "datatype": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MraverageheaderConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": list[InputPathType],
        "output": str,
    },
)

@dataclasses.dataclass
class MraverageheaderOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image"""

def mraverageheader_params(
    input_: list[InputPathType],
    output: str,
    padding: float | None = None,
    resolution: str | None = None,
    fill: bool = False,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MraverageheaderConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mraverageheader:
    """
    Build parameters.

    Args:
        input_: the input image(s).
        output: the output image
        padding: boundary box padding in voxels. Default: 0
        resolution: subsampling of template compared to smallest voxel size in any\
            input image. Valid options are 'mean': unbiased but loss of resolution\
            for individual images possible, and 'max': smallest voxel size of any\
            input image defines the resolution. Default: mean
        fill: set the intensity in the first volume of the average space to 1
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mraverageheader = {
        "@type": "mrtrix/mraverageheader",
        "input": input_,
        "output": output,
        "fill": fill,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if padding is not None:
        params["padding"] = padding
    if resolution is not None:
        params["resolution"] = resolution
    if datatype is not None:
        params["datatype"] = datatype
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mraverageheader_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mraverageheader."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("padding", None) is not None:
        if not isinstance(params["padding"], (float, int)):
            raise StyxValidationError(f'`padding` has the wrong type: Received `{type(params["padding"])}` expected `float`')
    if params.get("resolution", None) is not None:
        if not isinstance(params["resolution"], str):
            raise StyxValidationError(f'`resolution` has the wrong type: Received `{type(params["resolution"])}` expected `str`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], bool):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `bool`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MraverageheaderConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], list):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `list[InputPathType]`')
    for e_2 in params["input"]:
        if not isinstance(e_2, (pathlib.Path, str)):
            raise StyxValidationError(f'`input` has the wrong type: Received `{type(e_2)}` expected `list[InputPathType]`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mraverageheader_cargs(params: Mraverageheader, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mraverageheader")
    v_0 = params.get("padding")
    if v_0 is not None:
        cargs.append("-padding")
        cargs.append(str(v_0))
    v_1 = params.get("resolution")
    if v_1 is not None:
        cargs.append("-resolution")
        cargs.append(v_1)
    if params.get("fill"):
        cargs.append("-fill")
    v_2 = params.get("datatype")
    if v_2 is not None:
        cargs.append("-datatype")
        cargs.append(v_2)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    for item1 in params["input"]:
        cargs.append(execution.input_file(item1))
    cargs.append(params["output"])
    return cargs

def mraverageheader_outputs(params: Mraverageheader, execution: Execution) -> MraverageheaderOutputs:
    """Build the MraverageheaderOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MraverageheaderOutputs(
        root=root_v,
        output=output_v,
    )

def mraverageheader_execute(params: Mraverageheader, runner: Runner | None = None) -> MraverageheaderOutputs:
    """
    mraverageheader

    Calculate the average (unbiased) coordinate space of all input images.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mraverageheader_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRAVERAGEHEADER_METADATA)
    execution.params(params)
    args = mraverageheader_cargs(params, execution)
    out = mraverageheader_outputs(params, execution)
    execution.run(args)
    return out

def mraverageheader(
    input_: list[InputPathType],
    output: str,
    padding: float | None = None,
    resolution: str | None = None,
    fill: bool = False,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MraverageheaderConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MraverageheaderOutputs:
    """
    mraverageheader

    Calculate the average (unbiased) coordinate space of all input images.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image(s).
        output: the output image
        padding: boundary box padding in voxels. Default: 0
        resolution: subsampling of template compared to smallest voxel size in any\
            input image. Valid options are 'mean': unbiased but loss of resolution\
            for individual images possible, and 'max': smallest voxel size of any\
            input image defines the resolution. Default: mean
        fill: set the intensity in the first volume of the average space to 1
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mraverageheader_params(
        input_=input_,
        output=output,
        padding=padding,
        resolution=resolution,
        fill=fill,
        datatype=datatype,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mraverageheader_execute(params, runner)

__all__ = [
    "Mraverageheader",
    "MraverageheaderOutputs",
    "MRAVERAGEHEADER_METADATA",
    "mraverageheader_cargs",
    "mraverageheader_outputs",
    "mraverageheader_params",
    "mraverageheader_execute",
    "mraverageheader_validate",
    "mraverageheader",
]