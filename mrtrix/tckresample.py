# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

TCKRESAMPLE_METADATA = Metadata(
    id="tckresample",
    name="tckresample",
    package="mrtrix",
)

class TckresampleLine(typing.TypedDict):
    num: int
    """resample tracks at 'num' equidistant locations along a line between 'start' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""
    start: list[float]
    """resample tracks at 'num' equidistant locations along a line between 'start' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""
    end: list[float]
    """resample tracks at 'num' equidistant locations along a line between 'start' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""

class TckresampleArc(typing.TypedDict):
    num: int
    """resample tracks at 'num' equidistant locations along a circular arc specified by points 'start', 'mid' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""
    start: list[float]
    """resample tracks at 'num' equidistant locations along a circular arc specified by points 'start', 'mid' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""
    mid: list[float]
    """resample tracks at 'num' equidistant locations along a circular arc specified by points 'start', 'mid' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""
    end: list[float]
    """resample tracks at 'num' equidistant locations along a circular arc specified by points 'start', 'mid' and 'end' (specified as comma-separated 3-vectors in scanner coordinates)"""

class TckresampleConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Tckresample = typing.TypedDict(
    "Tckresample",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/tckresample"]],
        "upsample": typing.NotRequired[int],
        "downsample": typing.NotRequired[int],
        "step_size": typing.NotRequired[float],
        "num_points": typing.NotRequired[int],
        "endpoints": typing.NotRequired[bool],
        "line": typing.NotRequired[TckresampleLine],
        "arc": typing.NotRequired[TckresampleArc],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[TckresampleConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in_tracks": InputPathType,
        "out_tracks": str,
    },
)

@dataclasses.dataclass
class TckresampleOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out_tracks: OutputPathType
    """the output resampled tracks"""

def tckresample_params(
    in_tracks: InputPathType,
    out_tracks: str,
    upsample: int | None = None,
    downsample: int | None = None,
    step_size: float | None = None,
    num_points: int | None = None,
    endpoints: bool = False,
    line: TckresampleLine | None = None,
    arc: TckresampleArc | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckresampleConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Tckresample:
    """
    Build parameters.

    Args:
        in_tracks: the input track file
        out_tracks: the output resampled tracks
        upsample: increase the density of points along the length of each\
            streamline by some factor (may improve mapping streamlines to ROIs,\
            and/or visualisation)
        downsample: increase the density of points along the length of each\
            streamline by some factor (decreases required storage space)
        step_size: re-sample the streamlines to a desired step size (in mm)
        num_points: re-sample each streamline to a fixed number of points
        endpoints: only output the two endpoints of each streamline
        line: resample tracks at 'num' equidistant locations along a line between\
            'start' and 'end' (specified as comma-separated 3-vectors in scanner\
            coordinates)
        arc: resample tracks at 'num' equidistant locations along a circular arc\
            specified by points 'start', 'mid' and 'end' (specified as\
            comma-separated 3-vectors in scanner coordinates)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Tckresample = {
        "@type": "mrtrix/tckresample",
        "in_tracks": in_tracks,
        "out_tracks": out_tracks,
        "endpoints": endpoints,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if upsample is not None:
        params["upsample"] = upsample
    if downsample is not None:
        params["downsample"] = downsample
    if step_size is not None:
        params["step_size"] = step_size
    if num_points is not None:
        params["num_points"] = num_points
    if line is not None:
        params["line"] = line
    if arc is not None:
        params["arc"] = arc
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def tckresample_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Tckresample."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("upsample", None) is not None:
        if not isinstance(params["upsample"], int):
            raise StyxValidationError(f'`upsample` has the wrong type: Received `{type(params["upsample"])}` expected `int`')
    if params.get("downsample", None) is not None:
        if not isinstance(params["downsample"], int):
            raise StyxValidationError(f'`downsample` has the wrong type: Received `{type(params["downsample"])}` expected `int`')
    if params.get("step_size", None) is not None:
        if not isinstance(params["step_size"], (float, int)):
            raise StyxValidationError(f'`step_size` has the wrong type: Received `{type(params["step_size"])}` expected `float`')
    if params.get("num_points", None) is not None:
        if not isinstance(params["num_points"], int):
            raise StyxValidationError(f'`num_points` has the wrong type: Received `{type(params["num_points"])}` expected `int`')
    if params.get("endpoints", None) is not None:
        if not isinstance(params["endpoints"], bool):
            raise StyxValidationError(f'`endpoints` has the wrong type: Received `{type(params["endpoints"])}` expected `bool`')
    if params.get("line", None) is not None:
        if not isinstance(params["line"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["line"])}\'')
        if params["line"].get("num", None) is None:
            raise StyxValidationError("`num` must not be None")
        if not isinstance(params["line"]["num"], int):
            raise StyxValidationError(f'`num` has the wrong type: Received `{type(params["line"]["num"])}` expected `int`')
        if params["line"].get("start", None) is None:
            raise StyxValidationError("`start` must not be None")
        if not isinstance(params["line"]["start"], list):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["line"]["start"])}` expected `list[float]`')
        for e in params["line"]["start"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`start` has the wrong type: Received `{type(e)}` expected `list[float]`')
        if params["line"].get("end", None) is None:
            raise StyxValidationError("`end` must not be None")
        if not isinstance(params["line"]["end"], list):
            raise StyxValidationError(f'`end` has the wrong type: Received `{type(params["line"]["end"])}` expected `list[float]`')
        for e_2 in params["line"]["end"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`end` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("arc", None) is not None:
        if not isinstance(params["arc"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["arc"])}\'')
        if params["arc"].get("num", None) is None:
            raise StyxValidationError("`num` must not be None")
        if not isinstance(params["arc"]["num"], int):
            raise StyxValidationError(f'`num` has the wrong type: Received `{type(params["arc"]["num"])}` expected `int`')
        if params["arc"].get("start", None) is None:
            raise StyxValidationError("`start` must not be None")
        if not isinstance(params["arc"]["start"], list):
            raise StyxValidationError(f'`start` has the wrong type: Received `{type(params["arc"]["start"])}` expected `list[float]`')
        for e_3 in params["arc"]["start"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`start` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
        if params["arc"].get("mid", None) is None:
            raise StyxValidationError("`mid` must not be None")
        if not isinstance(params["arc"]["mid"], list):
            raise StyxValidationError(f'`mid` has the wrong type: Received `{type(params["arc"]["mid"])}` expected `list[float]`')
        for e_4 in params["arc"]["mid"]:
            if not isinstance(e_4, (float, int)):
                raise StyxValidationError(f'`mid` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
        if params["arc"].get("end", None) is None:
            raise StyxValidationError("`end` must not be None")
        if not isinstance(params["arc"]["end"], list):
            raise StyxValidationError(f'`end` has the wrong type: Received `{type(params["arc"]["end"])}` expected `list[float]`')
        for e_5 in params["arc"]["end"]:
            if not isinstance(e_5, (float, int)):
                raise StyxValidationError(f'`end` has the wrong type: Received `{type(e_5)}` expected `list[float]`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[TckresampleConfig]`')
        for e_6 in params["config"]:
            if not isinstance(e_6, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_6)}\'')
            if e_6.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_6["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_6["key"])}` expected `str`')
            if e_6.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_6["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_6["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in_tracks", None) is None:
        raise StyxValidationError("`in_tracks` must not be None")
    if not isinstance(params["in_tracks"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_tracks` has the wrong type: Received `{type(params["in_tracks"])}` expected `InputPathType`')
    if params.get("out_tracks", None) is None:
        raise StyxValidationError("`out_tracks` must not be None")
    if not isinstance(params["out_tracks"], str):
        raise StyxValidationError(f'`out_tracks` has the wrong type: Received `{type(params["out_tracks"])}` expected `str`')

def tckresample_cargs(params: Tckresample, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("tckresample")
    v_0 = params.get("upsample")
    if v_0 is not None:
        cargs.append("-upsample")
        cargs.append(str(v_0))
    v_1 = params.get("downsample")
    if v_1 is not None:
        cargs.append("-downsample")
        cargs.append(str(v_1))
    v_2 = params.get("step_size")
    if v_2 is not None:
        cargs.append("-step_size")
        cargs.append(str(v_2))
    v_3 = params.get("num_points")
    if v_3 is not None:
        cargs.append("-num_points")
        cargs.append(str(v_3))
    if params.get("endpoints"):
        cargs.append("-endpoints")
    v_4 = params.get("line")
    if v_4 is not None:
        cargs.append("-line")
        cargs.append(str(v_4["num"]))
        cargs.append(",".join([str(item0) for item0 in v_4["start"]]))
        cargs.append(",".join([str(item1) for item1 in v_4["end"]]))
    v_5 = params.get("arc")
    if v_5 is not None:
        cargs.append("-arc")
        cargs.append(str(v_5["num"]))
        cargs.append(",".join([str(item2) for item2 in v_5["start"]]))
        cargs.append(",".join([str(item3) for item3 in v_5["mid"]]))
        cargs.append(",".join([str(item4) for item4 in v_5["end"]]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_6 = params.get("nthreads")
    if v_6 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        for item5 in v_7:
            cargs.append("-config")
            cargs.append(item5["key"])
            cargs.append(item5["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in_tracks"]))
    cargs.append(params["out_tracks"])
    return cargs

def tckresample_outputs(params: Tckresample, execution: Execution) -> TckresampleOutputs:
    """Build the TckresampleOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_tracks_v: OutputPathType = execution.output_file(params["out_tracks"])
    return TckresampleOutputs(
        root=root_v,
        out_tracks=out_tracks_v,
    )

def tckresample_execute(params: Tckresample, runner: Runner | None = None) -> TckresampleOutputs:
    """
    tckresample

    Resample each streamline in a track file to a new set of vertices.

It is necessary to specify precisely ONE of the command-line options for controlling how this resampling takes place; this may be either increasing or decreasing the number of samples along each streamline, or may involve changing the positions of the samples according to some specified trajectory.

Note that because the length of a streamline is calculated based on the sums of distances between adjacent vertices, resampling a streamline to a new set of vertices will typically change the quantified length of that streamline; the magnitude of the difference will typically depend on the discrepancy in the number of vertices, with less vertices leading to a shorter length (due to taking chordal lengths of curved trajectories).

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    tckresample_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(TCKRESAMPLE_METADATA)
    execution.params(params)
    args = tckresample_cargs(params, execution)
    out = tckresample_outputs(params, execution)
    execution.run(args)
    return out

def tckresample(
    in_tracks: InputPathType,
    out_tracks: str,
    upsample: int | None = None,
    downsample: int | None = None,
    step_size: float | None = None,
    num_points: int | None = None,
    endpoints: bool = False,
    line: TckresampleLine | None = None,
    arc: TckresampleArc | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[TckresampleConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> TckresampleOutputs:
    """
    tckresample

    Resample each streamline in a track file to a new set of vertices.

It is necessary to specify precisely ONE of the command-line options for controlling how this resampling takes place; this may be either increasing or decreasing the number of samples along each streamline, or may involve changing the positions of the samples according to some specified trajectory.

Note that because the length of a streamline is calculated based on the sums of distances between adjacent vertices, resampling a streamline to a new set of vertices will typically change the quantified length of that streamline; the magnitude of the difference will typically depend on the discrepancy in the number of vertices, with less vertices leading to a shorter length (due to taking chordal lengths of curved trajectories).

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_tracks: the input track file
        out_tracks: the output resampled tracks
        upsample: increase the density of points along the length of each\
            streamline by some factor (may improve mapping streamlines to ROIs,\
            and/or visualisation)
        downsample: increase the density of points along the length of each\
            streamline by some factor (decreases required storage space)
        step_size: re-sample the streamlines to a desired step size (in mm)
        num_points: re-sample each streamline to a fixed number of points
        endpoints: only output the two endpoints of each streamline
        line: resample tracks at 'num' equidistant locations along a line between\
            'start' and 'end' (specified as comma-separated 3-vectors in scanner\
            coordinates)
        arc: resample tracks at 'num' equidistant locations along a circular arc\
            specified by points 'start', 'mid' and 'end' (specified as\
            comma-separated 3-vectors in scanner coordinates)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = tckresample_params(
        in_tracks=in_tracks,
        out_tracks=out_tracks,
        upsample=upsample,
        downsample=downsample,
        step_size=step_size,
        num_points=num_points,
        endpoints=endpoints,
        line=line,
        arc=arc,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return tckresample_execute(params, runner)

__all__ = [
    "Tckresample",
    "TckresampleOutputs",
    "TCKRESAMPLE_METADATA",
    "tckresample_cargs",
    "tckresample_outputs",
    "tckresample_params",
    "tckresample_execute",
    "tckresample_validate",
    "tckresample",
]