# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

LABEL2COLOUR_METADATA = Metadata(
    id="label2colour",
    name="label2colour",
    package="mrtrix",
)

class Label2colourConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Label2colour = typing.TypedDict(
    "Label2colour",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/label2colour"]],
        "lut": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Label2colourConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "nodes_in": InputPathType,
        "colour_out": str,
    },
)

@dataclasses.dataclass
class Label2colourOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    colour_out: OutputPathType
    """the output colour image"""

def label2colour_params(
    nodes_in: InputPathType,
    colour_out: str,
    lut: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Label2colourConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Label2colour:
    """
    Build parameters.

    Args:
        nodes_in: the input node parcellation image
        colour_out: the output colour image
        lut: Provide the relevant colour lookup table (if not provided, nodes will\
            be coloured randomly)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Label2colour = {
        "@type": "mrtrix/label2colour",
        "nodes_in": nodes_in,
        "colour_out": colour_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if lut is not None:
        params["lut"] = lut
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def label2colour_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Label2colour."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("lut", None) is not None:
        if not isinstance(params["lut"], (pathlib.Path, str)):
            raise StyxValidationError(f'`lut` has the wrong type: Received `{type(params["lut"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Label2colourConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("nodes_in", None) is None:
        raise StyxValidationError("`nodes_in` must not be None")
    if not isinstance(params["nodes_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`nodes_in` has the wrong type: Received `{type(params["nodes_in"])}` expected `InputPathType`')
    if params.get("colour_out", None) is None:
        raise StyxValidationError("`colour_out` must not be None")
    if not isinstance(params["colour_out"], str):
        raise StyxValidationError(f'`colour_out` has the wrong type: Received `{type(params["colour_out"])}` expected `str`')

def label2colour_cargs(params: Label2colour, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("label2colour")
    v_0 = params.get("lut")
    if v_0 is not None:
        cargs.append("-lut")
        cargs.append(execution.input_file(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["nodes_in"]))
    cargs.append(params["colour_out"])
    return cargs

def label2colour_outputs(params: Label2colour, execution: Execution) -> Label2colourOutputs:
    """Build the Label2colourOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    colour_out_v: OutputPathType = execution.output_file(params["colour_out"])
    return Label2colourOutputs(
        root=root_v,
        colour_out=colour_out_v,
    )

def label2colour_execute(params: Label2colour, runner: Runner | None = None) -> Label2colourOutputs:
    """
    label2colour

    Convert a parcellated image (where values are node indices) into a colour image.

Many software packages handle this colouring internally within their viewer program; this binary explicitly converts a parcellation image into a colour image that should be viewable in any software.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    label2colour_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(LABEL2COLOUR_METADATA)
    execution.params(params)
    args = label2colour_cargs(params, execution)
    out = label2colour_outputs(params, execution)
    execution.run(args)
    return out

def label2colour(
    nodes_in: InputPathType,
    colour_out: str,
    lut: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Label2colourConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Label2colourOutputs:
    """
    label2colour

    Convert a parcellated image (where values are node indices) into a colour image.

Many software packages handle this colouring internally within their viewer program; this binary explicitly converts a parcellation image into a colour image that should be viewable in any software.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        nodes_in: the input node parcellation image
        colour_out: the output colour image
        lut: Provide the relevant colour lookup table (if not provided, nodes will\
            be coloured randomly)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = label2colour_params(
        nodes_in=nodes_in,
        colour_out=colour_out,
        lut=lut,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return label2colour_execute(params, runner)

__all__ = [
    "Label2colour",
    "Label2colourOutputs",
    "LABEL2COLOUR_METADATA",
    "label2colour_cargs",
    "label2colour_outputs",
    "label2colour_params",
    "label2colour_execute",
    "label2colour_validate",
    "label2colour",
]