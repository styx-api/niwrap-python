# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXEL2PEAKS_METADATA = Metadata(
    id="fixel2peaks",
    name="fixel2peaks",
    package="mrtrix",
)

class Fixel2peaksConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixel2peaksVariousString = typing.TypedDict(
    "Fixel2peaksVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

Fixel2peaksVariousFile = typing.TypedDict(
    "Fixel2peaksVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

Fixel2peaksIn = Fixel2peaksVariousString | Fixel2peaksVariousFile

Fixel2peaks = typing.TypedDict(
    "Fixel2peaks",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixel2peaks"]],
        "number": typing.NotRequired[int],
        "nan": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fixel2peaksConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in": Fixel2peaksIn,
        "out": str,
    },
)

@dataclasses.dataclass
class Fixel2peaksOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output peaks image"""

def fixel2peaks_params(
    in_: Fixel2peaksIn,
    out: str,
    number: int | None = None,
    nan: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2peaksConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixel2peaks:
    """
    Build parameters.

    Args:
        in_: the input fixel information
        out: the output peaks image
        number: maximum number of fixels in each voxel (default: based on input\
            data)
        nan: fill excess peak data with NaNs rather than zeroes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixel2peaks = {
        "@type": "mrtrix/fixel2peaks",
        "in": in_,
        "out": out,
        "nan": nan,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if number is not None:
        params["number"] = number
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixel2peaks_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixel2peaks."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("number", None) is not None:
        if not isinstance(params["number"], int):
            raise StyxValidationError(f'`number` has the wrong type: Received `{type(params["number"])}` expected `int`')
    if params.get("nan", None) is not None:
        if not isinstance(params["nan"], bool):
            raise StyxValidationError(f'`nan` has the wrong type: Received `{type(params["nan"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fixel2peaksConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in", None) is None:
        raise StyxValidationError("`in` must not be None")
    if not isinstance(params["in"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["in"])}\'')
    if "@type" not in params["in"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["in"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `in`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["in"]["@type"] == "VariousString":
        if params["in"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["in"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["in"]["obj"])}` expected `str`')
    elif params["in"]["@type"] == "VariousFile":
        if params["in"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["in"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["in"]["obj"])}` expected `InputPathType`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def fixel2peaks_cargs(params: Fixel2peaks, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixel2peaks")
    v_0 = params.get("number")
    if v_0 is not None:
        cargs.append("-number")
        cargs.append(str(v_0))
    if params.get("nan"):
        cargs.append("-nan")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    if params["in"]["@type"] == "VariousString":
        cargs.append(str(params["in"]["obj"]))
    elif params["in"]["@type"] == "VariousFile":
        cargs.append(str(params["in"]["obj"]))
    cargs.append(params["out"])
    return cargs

def fixel2peaks_outputs(params: Fixel2peaks, execution: Execution) -> Fixel2peaksOutputs:
    """Build the Fixel2peaksOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    return Fixel2peaksOutputs(
        root=root_v,
        out=out_v,
    )

def fixel2peaks_execute(params: Fixel2peaks, runner: Runner | None = None) -> Fixel2peaksOutputs:
    """
    fixel2peaks

    Convert data in the fixel directory format into a 4D image of 3-vectors.

If a fixel data file is provided as input, then the 3-vectors in the output image will be scaled based on the data in that file. If the input is instead the fixel directory, or the index or directions file, then all output 3-vectors will possess unit norm.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixel2peaks_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXEL2PEAKS_METADATA)
    execution.params(params)
    args = fixel2peaks_cargs(params, execution)
    out = fixel2peaks_outputs(params, execution)
    execution.run(args)
    return out

def fixel2peaks(
    in_: Fixel2peaksIn,
    out: str,
    number: int | None = None,
    nan: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2peaksConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fixel2peaksOutputs:
    """
    fixel2peaks

    Convert data in the fixel directory format into a 4D image of 3-vectors.

If a fixel data file is provided as input, then the 3-vectors in the output image will be scaled based on the data in that file. If the input is instead the fixel directory, or the index or directions file, then all output 3-vectors will possess unit norm.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_: the input fixel information
        out: the output peaks image
        number: maximum number of fixels in each voxel (default: based on input\
            data)
        nan: fill excess peak data with NaNs rather than zeroes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixel2peaks_params(
        in_=in_,
        out=out,
        number=number,
        nan=nan,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixel2peaks_execute(params, runner)

__all__ = [
    "Fixel2peaks",
    "Fixel2peaksOutputs",
    "FIXEL2PEAKS_METADATA",
    "fixel2peaks_cargs",
    "fixel2peaks_outputs",
    "fixel2peaks_params",
    "fixel2peaks_execute",
    "fixel2peaks_validate",
    "fixel2peaks",
]