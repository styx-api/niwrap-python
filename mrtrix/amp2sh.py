# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AMP2SH_METADATA = Metadata(
    id="amp2sh",
    name="amp2sh",
    package="mrtrix",
)

class Amp2shFslgrad(typing.TypedDict):
    bvecs: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""
    bvals: InputPathType
    """Provide the diffusion-weighted gradient scheme used in the acquisition in FSL bvecs/bvals format files. If a diffusion gradient scheme is present in the input image header, the data provided with this option will be instead used."""

Amp2shVariousString = typing.TypedDict(
    "Amp2shVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

Amp2shVariousFile = typing.TypedDict(
    "Amp2shVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

Amp2shStrides = Amp2shVariousString | Amp2shVariousFile

class Amp2shConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Amp2sh = typing.TypedDict(
    "Amp2sh",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/amp2sh"]],
        "lmax": typing.NotRequired[int],
        "normalise": typing.NotRequired[bool],
        "directions": typing.NotRequired[InputPathType],
        "rician": typing.NotRequired[InputPathType],
        "grad": typing.NotRequired[InputPathType],
        "fslgrad": typing.NotRequired[Amp2shFslgrad],
        "shells": typing.NotRequired[list[float]],
        "strides": typing.NotRequired[Amp2shStrides],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Amp2shConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "amp": InputPathType,
        "SH": str,
    },
)

@dataclasses.dataclass
class Amp2shOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    SH: OutputPathType
    """the output spherical harmonics coefficients image."""

def amp2sh_params(
    amp: InputPathType,
    SH: str,
    lmax: int | None = None,
    normalise: bool = False,
    directions: InputPathType | None = None,
    rician: InputPathType | None = None,
    grad: InputPathType | None = None,
    fslgrad: Amp2shFslgrad | None = None,
    shells: list[float] | None = None,
    strides: Amp2shStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Amp2shConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Amp2sh:
    """
    Build parameters.

    Args:
        amp: the input amplitude image.
        SH: the output spherical harmonics coefficients image.
        lmax: set the maximum harmonic order for the output series. By default, the\
            program will use the highest possible lmax given the number of\
            diffusion-weighted images, up to a maximum of 8.
        normalise: normalise the DW signal to the b=0 image
        directions: the directions corresponding to the input amplitude image used\
            to sample AFD. By default this option is not required providing the\
            direction set is supplied in the amplitude image. This should be\
            supplied as a list of directions [az el], as generated using the dirgen\
            command, or as a list of [ x y z ] Cartesian coordinates.
        rician: correct for Rician noise induced bias, using noise map supplied
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Amp2sh = {
        "@type": "mrtrix/amp2sh",
        "amp": amp,
        "SH": SH,
        "normalise": normalise,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if lmax is not None:
        params["lmax"] = lmax
    if directions is not None:
        params["directions"] = directions
    if rician is not None:
        params["rician"] = rician
    if grad is not None:
        params["grad"] = grad
    if fslgrad is not None:
        params["fslgrad"] = fslgrad
    if shells is not None:
        params["shells"] = shells
    if strides is not None:
        params["strides"] = strides
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def amp2sh_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Amp2sh."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("lmax", None) is not None:
        if not isinstance(params["lmax"], int):
            raise StyxValidationError(f'`lmax` has the wrong type: Received `{type(params["lmax"])}` expected `int`')
    if params.get("normalise", None) is not None:
        if not isinstance(params["normalise"], bool):
            raise StyxValidationError(f'`normalise` has the wrong type: Received `{type(params["normalise"])}` expected `bool`')
    if params.get("directions", None) is not None:
        if not isinstance(params["directions"], (pathlib.Path, str)):
            raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("rician", None) is not None:
        if not isinstance(params["rician"], (pathlib.Path, str)):
            raise StyxValidationError(f'`rician` has the wrong type: Received `{type(params["rician"])}` expected `InputPathType`')
    if params.get("grad", None) is not None:
        if not isinstance(params["grad"], (pathlib.Path, str)):
            raise StyxValidationError(f'`grad` has the wrong type: Received `{type(params["grad"])}` expected `InputPathType`')
    if params.get("fslgrad", None) is not None:
        if not isinstance(params["fslgrad"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fslgrad"])}\'')
        if params["fslgrad"].get("bvecs", None) is None:
            raise StyxValidationError("`bvecs` must not be None")
        if not isinstance(params["fslgrad"]["bvecs"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvecs` has the wrong type: Received `{type(params["fslgrad"]["bvecs"])}` expected `InputPathType`')
        if params["fslgrad"].get("bvals", None) is None:
            raise StyxValidationError("`bvals` must not be None")
        if not isinstance(params["fslgrad"]["bvals"], (pathlib.Path, str)):
            raise StyxValidationError(f'`bvals` has the wrong type: Received `{type(params["fslgrad"]["bvals"])}` expected `InputPathType`')
    if params.get("shells", None) is not None:
        if not isinstance(params["shells"], list):
            raise StyxValidationError(f'`shells` has the wrong type: Received `{type(params["shells"])}` expected `list[float]`')
        for e in params["shells"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`shells` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Amp2shConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("amp", None) is None:
        raise StyxValidationError("`amp` must not be None")
    if not isinstance(params["amp"], (pathlib.Path, str)):
        raise StyxValidationError(f'`amp` has the wrong type: Received `{type(params["amp"])}` expected `InputPathType`')
    if params.get("SH", None) is None:
        raise StyxValidationError("`SH` must not be None")
    if not isinstance(params["SH"], str):
        raise StyxValidationError(f'`SH` has the wrong type: Received `{type(params["SH"])}` expected `str`')

def amp2sh_cargs(params: Amp2sh, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("amp2sh")
    v_0 = params.get("lmax")
    if v_0 is not None:
        cargs.append("-lmax")
        cargs.append(str(v_0))
    if params.get("normalise"):
        cargs.append("-normalise")
    v_1 = params.get("directions")
    if v_1 is not None:
        cargs.append("-directions")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("rician")
    if v_2 is not None:
        cargs.append("-rician")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("grad")
    if v_3 is not None:
        cargs.append("-grad")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("fslgrad")
    if v_4 is not None:
        cargs.append("-fslgrad")
        cargs.append(execution.input_file(v_4["bvecs"]))
        cargs.append(execution.input_file(v_4["bvals"]))
    v_5 = params.get("shells")
    if v_5 is not None:
        cargs.append("-shells")
        cargs.append(",".join([str(item0) for item0 in v_5]))
    v_6 = params.get("strides")
    if v_6 is not None:
        cargs.append("-strides")
        if v_6["@type"] == "VariousString":
            cargs.append(str(v_6["obj"]))
        elif v_6["@type"] == "VariousFile":
            cargs.append(str(v_6["obj"]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_7 = params.get("nthreads")
    if v_7 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_7))
    v_8 = params.get("config")
    if v_8 is not None:
        for item1 in v_8:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["amp"]))
    cargs.append(params["SH"])
    return cargs

def amp2sh_outputs(params: Amp2sh, execution: Execution) -> Amp2shOutputs:
    """Build the Amp2shOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    SH_v: OutputPathType = execution.output_file(params["SH"])
    return Amp2shOutputs(
        root=root_v,
        SH=SH_v,
    )

def amp2sh_execute(params: Amp2sh, runner: Runner | None = None) -> Amp2shOutputs:
    """
    amp2sh

    Convert a set of amplitudes (defined along a set of corresponding directions) to their spherical harmonic representation.

The spherical harmonic decomposition is calculated by least-squares linear fitting to the amplitude data.

The directions can be defined either as a DW gradient scheme (for example to compute the SH representation of the DW signal), a set of [az el] pairs as output by the dirgen command, or a set of [ x y z ] directions in Cartesian coordinates. The DW gradient scheme or direction set can be supplied within the input image header or using the -gradient or -directions option. Note that if a direction set and DW gradient scheme can be found, the direction set will be used by default.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    amp2sh_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AMP2SH_METADATA)
    execution.params(params)
    args = amp2sh_cargs(params, execution)
    out = amp2sh_outputs(params, execution)
    execution.run(args)
    return out

def amp2sh(
    amp: InputPathType,
    SH: str,
    lmax: int | None = None,
    normalise: bool = False,
    directions: InputPathType | None = None,
    rician: InputPathType | None = None,
    grad: InputPathType | None = None,
    fslgrad: Amp2shFslgrad | None = None,
    shells: list[float] | None = None,
    strides: Amp2shStrides | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Amp2shConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Amp2shOutputs:
    """
    amp2sh

    Convert a set of amplitudes (defined along a set of corresponding directions) to their spherical harmonic representation.

The spherical harmonic decomposition is calculated by least-squares linear fitting to the amplitude data.

The directions can be defined either as a DW gradient scheme (for example to compute the SH representation of the DW signal), a set of [az el] pairs as output by the dirgen command, or a set of [ x y z ] directions in Cartesian coordinates. The DW gradient scheme or direction set can be supplied within the input image header or using the -gradient or -directions option. Note that if a direction set and DW gradient scheme can be found, the direction set will be used by default.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        amp: the input amplitude image.
        SH: the output spherical harmonics coefficients image.
        lmax: set the maximum harmonic order for the output series. By default, the\
            program will use the highest possible lmax given the number of\
            diffusion-weighted images, up to a maximum of 8.
        normalise: normalise the DW signal to the b=0 image
        directions: the directions corresponding to the input amplitude image used\
            to sample AFD. By default this option is not required providing the\
            direction set is supplied in the amplitude image. This should be\
            supplied as a list of directions [az el], as generated using the dirgen\
            command, or as a list of [ x y z ] Cartesian coordinates.
        rician: correct for Rician noise induced bias, using noise map supplied
        grad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in a text file. This should be supplied as a 4xN text file\
            with each line is in the format [ X Y Z b ], where [ X Y Z ] describe\
            the direction of the applied gradient, and b gives the b-value in units\
            of s/mm^2. If a diffusion gradient scheme is present in the input image\
            header, the data provided with this option will be instead used.
        fslgrad: Provide the diffusion-weighted gradient scheme used in the\
            acquisition in FSL bvecs/bvals format files. If a diffusion gradient\
            scheme is present in the input image header, the data provided with\
            this option will be instead used.
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = amp2sh_params(
        amp=amp,
        SH=SH,
        lmax=lmax,
        normalise=normalise,
        directions=directions,
        rician=rician,
        grad=grad,
        fslgrad=fslgrad,
        shells=shells,
        strides=strides,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return amp2sh_execute(params, runner)

__all__ = [
    "Amp2sh",
    "Amp2shOutputs",
    "AMP2SH_METADATA",
    "amp2sh_cargs",
    "amp2sh_outputs",
    "amp2sh_params",
    "amp2sh_execute",
    "amp2sh_validate",
    "amp2sh",
]