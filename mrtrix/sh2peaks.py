# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SH2PEAKS_METADATA = Metadata(
    id="sh2peaks",
    name="sh2peaks",
    package="mrtrix",
)

class Sh2peaksDirection(typing.TypedDict):
    phi: float
    """the direction of a peak to estimate. The algorithm will attempt to find the same number of peaks as have been specified using this option."""
    theta: float
    """the direction of a peak to estimate. The algorithm will attempt to find the same number of peaks as have been specified using this option."""

class Sh2peaksConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Sh2peaks = typing.TypedDict(
    "Sh2peaks",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/sh2peaks"]],
        "num": typing.NotRequired[int],
        "direction": typing.NotRequired[list[Sh2peaksDirection]],
        "peaks": typing.NotRequired[InputPathType],
        "threshold": typing.NotRequired[float],
        "seeds": typing.NotRequired[InputPathType],
        "mask": typing.NotRequired[InputPathType],
        "fast": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Sh2peaksConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "SH": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class Sh2peaksOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image. Each volume corresponds to the x, y & z component of each peak direction vector in turn."""

def sh2peaks_params(
    SH: InputPathType,
    output: str,
    num: int | None = None,
    direction: list[Sh2peaksDirection] | None = None,
    peaks: InputPathType | None = None,
    threshold: float | None = None,
    seeds: InputPathType | None = None,
    mask: InputPathType | None = None,
    fast: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Sh2peaksConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Sh2peaks:
    """
    Build parameters.

    Args:
        SH: the input image of SH coefficients.
        output: the output image. Each volume corresponds to the x, y & z component\
            of each peak direction vector in turn.
        num: the number of peaks to extract (default: 3).
        direction: the direction of a peak to estimate. The algorithm will attempt\
            to find the same number of peaks as have been specified using this\
            option.
        peaks: the program will try to find the peaks that most closely match those\
            in the image provided.
        threshold: only peak amplitudes greater than the threshold will be\
            considered.
        seeds: specify a set of directions from which to start the multiple\
            restarts of the optimisation (by default, the built-in 60 direction set\
            is used)
        mask: only perform computation within the specified binary brain mask\
            image.
        fast: use lookup table to compute associated Legendre polynomials (faster,\
            but approximate).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Sh2peaks = {
        "@type": "mrtrix/sh2peaks",
        "SH": SH,
        "output": output,
        "fast": fast,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if num is not None:
        params["num"] = num
    if direction is not None:
        params["direction"] = direction
    if peaks is not None:
        params["peaks"] = peaks
    if threshold is not None:
        params["threshold"] = threshold
    if seeds is not None:
        params["seeds"] = seeds
    if mask is not None:
        params["mask"] = mask
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def sh2peaks_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sh2peaks."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("num", None) is not None:
        if not isinstance(params["num"], int):
            raise StyxValidationError(f'`num` has the wrong type: Received `{type(params["num"])}` expected `int`')
    if params.get("direction", None) is not None:
        if not isinstance(params["direction"], list):
            raise StyxValidationError(f'`direction` has the wrong type: Received `{type(params["direction"])}` expected `list[Sh2peaksDirection]`')
        for e in params["direction"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("phi", None) is None:
                raise StyxValidationError("`phi` must not be None")
            if not isinstance(e["phi"], (float, int)):
                raise StyxValidationError(f'`phi` has the wrong type: Received `{type(e["phi"])}` expected `float`')
            if e.get("theta", None) is None:
                raise StyxValidationError("`theta` must not be None")
            if not isinstance(e["theta"], (float, int)):
                raise StyxValidationError(f'`theta` has the wrong type: Received `{type(e["theta"])}` expected `float`')
    if params.get("peaks", None) is not None:
        if not isinstance(params["peaks"], (pathlib.Path, str)):
            raise StyxValidationError(f'`peaks` has the wrong type: Received `{type(params["peaks"])}` expected `InputPathType`')
    if params.get("threshold", None) is not None:
        if not isinstance(params["threshold"], (float, int)):
            raise StyxValidationError(f'`threshold` has the wrong type: Received `{type(params["threshold"])}` expected `float`')
    if params.get("seeds", None) is not None:
        if not isinstance(params["seeds"], (pathlib.Path, str)):
            raise StyxValidationError(f'`seeds` has the wrong type: Received `{type(params["seeds"])}` expected `InputPathType`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("fast", None) is not None:
        if not isinstance(params["fast"], bool):
            raise StyxValidationError(f'`fast` has the wrong type: Received `{type(params["fast"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Sh2peaksConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("SH", None) is None:
        raise StyxValidationError("`SH` must not be None")
    if not isinstance(params["SH"], (pathlib.Path, str)):
        raise StyxValidationError(f'`SH` has the wrong type: Received `{type(params["SH"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def sh2peaks_cargs(params: Sh2peaks, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sh2peaks")
    v_0 = params.get("num")
    if v_0 is not None:
        cargs.append("-num")
        cargs.append(str(v_0))
    v_1 = params.get("direction")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-direction")
            cargs.append(str(item0["phi"]))
            cargs.append(str(item0["theta"]))
    v_2 = params.get("peaks")
    if v_2 is not None:
        cargs.append("-peaks")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("threshold")
    if v_3 is not None:
        cargs.append("-threshold")
        cargs.append(str(v_3))
    v_4 = params.get("seeds")
    if v_4 is not None:
        cargs.append("-seeds")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("mask")
    if v_5 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_5))
    if params.get("fast"):
        cargs.append("-fast")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_6 = params.get("nthreads")
    if v_6 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        for item1 in v_7:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["SH"]))
    cargs.append(params["output"])
    return cargs

def sh2peaks_outputs(params: Sh2peaks, execution: Execution) -> Sh2peaksOutputs:
    """Build the Sh2peaksOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return Sh2peaksOutputs(
        root=root_v,
        output=output_v,
    )

def sh2peaks_execute(params: Sh2peaks, runner: Runner | None = None) -> Sh2peaksOutputs:
    """
    sh2peaks

    Extract the peaks of a spherical harmonic function in each voxel.

Peaks of the spherical harmonic function in each voxel are located by commencing a Newton search along each of a set of pre-specified directions

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:

Jeurissen, B.; Leemans, A.; Tournier, J.-D.; Jones, D.K.; Sijbers, J. Investigating the prevalence of complex fiber configurations in white matter tissue with diffusion magnetic resonance imaging. Human Brain Mapping, 2013, 34(11), 2747-2766

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sh2peaks_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SH2PEAKS_METADATA)
    execution.params(params)
    args = sh2peaks_cargs(params, execution)
    out = sh2peaks_outputs(params, execution)
    execution.run(args)
    return out

def sh2peaks(
    SH: InputPathType,
    output: str,
    num: int | None = None,
    direction: list[Sh2peaksDirection] | None = None,
    peaks: InputPathType | None = None,
    threshold: float | None = None,
    seeds: InputPathType | None = None,
    mask: InputPathType | None = None,
    fast: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Sh2peaksConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Sh2peaksOutputs:
    """
    sh2peaks

    Extract the peaks of a spherical harmonic function in each voxel.

Peaks of the spherical harmonic function in each voxel are located by commencing a Newton search along each of a set of pre-specified directions

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:

Jeurissen, B.; Leemans, A.; Tournier, J.-D.; Jones, D.K.; Sijbers, J. Investigating the prevalence of complex fiber configurations in white matter tissue with diffusion magnetic resonance imaging. Human Brain Mapping, 2013, 34(11), 2747-2766

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        SH: the input image of SH coefficients.
        output: the output image. Each volume corresponds to the x, y & z component\
            of each peak direction vector in turn.
        num: the number of peaks to extract (default: 3).
        direction: the direction of a peak to estimate. The algorithm will attempt\
            to find the same number of peaks as have been specified using this\
            option.
        peaks: the program will try to find the peaks that most closely match those\
            in the image provided.
        threshold: only peak amplitudes greater than the threshold will be\
            considered.
        seeds: specify a set of directions from which to start the multiple\
            restarts of the optimisation (by default, the built-in 60 direction set\
            is used)
        mask: only perform computation within the specified binary brain mask\
            image.
        fast: use lookup table to compute associated Legendre polynomials (faster,\
            but approximate).
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sh2peaks_params(
        SH=SH,
        output=output,
        num=num,
        direction=direction,
        peaks=peaks,
        threshold=threshold,
        seeds=seeds,
        mask=mask,
        fast=fast,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return sh2peaks_execute(params, runner)

__all__ = [
    "Sh2peaks",
    "Sh2peaksOutputs",
    "SH2PEAKS_METADATA",
    "sh2peaks_cargs",
    "sh2peaks_outputs",
    "sh2peaks_params",
    "sh2peaks_execute",
    "sh2peaks_validate",
    "sh2peaks",
]