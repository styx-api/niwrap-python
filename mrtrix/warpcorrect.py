# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WARPCORRECT_METADATA = Metadata(
    id="warpcorrect",
    name="warpcorrect",
    package="mrtrix",
)

class WarpcorrectConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Warpcorrect = typing.TypedDict(
    "Warpcorrect",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/warpcorrect"]],
        "marker": typing.NotRequired[list[float]],
        "tolerance": typing.NotRequired[float],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[WarpcorrectConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in": InputPathType,
        "out": str,
    },
)

@dataclasses.dataclass
class WarpcorrectOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    out: OutputPathType
    """the output warp image."""

def warpcorrect_params(
    in_: InputPathType,
    out: str,
    marker: list[float] | None = None,
    tolerance: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[WarpcorrectConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Warpcorrect:
    """
    Build parameters.

    Args:
        in_: the input warp image.
        out: the output warp image.
        marker: single value or a comma separated list of values that define out of\
            bounds voxels in the input warp image. Default: (0,0,0).
        tolerance: numerical precision used for L2 matrix norm comparison. Default:\
            9.99999975e-06.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Warpcorrect = {
        "@type": "mrtrix/warpcorrect",
        "in": in_,
        "out": out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if marker is not None:
        params["marker"] = marker
    if tolerance is not None:
        params["tolerance"] = tolerance
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def warpcorrect_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Warpcorrect."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("marker", None) is not None:
        if not isinstance(params["marker"], list):
            raise StyxValidationError(f'`marker` has the wrong type: Received `{type(params["marker"])}` expected `list[float]`')
        for e in params["marker"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`marker` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("tolerance", None) is not None:
        if not isinstance(params["tolerance"], (float, int)):
            raise StyxValidationError(f'`tolerance` has the wrong type: Received `{type(params["tolerance"])}` expected `float`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[WarpcorrectConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in", None) is None:
        raise StyxValidationError("`in` must not be None")
    if not isinstance(params["in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in` has the wrong type: Received `{type(params["in"])}` expected `InputPathType`')
    if params.get("out", None) is None:
        raise StyxValidationError("`out` must not be None")
    if not isinstance(params["out"], str):
        raise StyxValidationError(f'`out` has the wrong type: Received `{type(params["out"])}` expected `str`')

def warpcorrect_cargs(params: Warpcorrect, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("warpcorrect")
    v_0 = params.get("marker")
    if v_0 is not None:
        cargs.append("-marker")
        for item0 in v_0:
            cargs.append(str(item0))
    v_1 = params.get("tolerance")
    if v_1 is not None:
        cargs.append("-tolerance")
        cargs.append(str(v_1))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_2 = params.get("nthreads")
    if v_2 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_2))
    v_3 = params.get("config")
    if v_3 is not None:
        for item1 in v_3:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in"]))
    cargs.append(params["out"])
    return cargs

def warpcorrect_outputs(params: Warpcorrect, execution: Execution) -> WarpcorrectOutputs:
    """Build the WarpcorrectOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    out_v: OutputPathType = execution.output_file(params["out"])
    return WarpcorrectOutputs(
        root=root_v,
        out=out_v,
    )

def warpcorrect_execute(params: Warpcorrect, runner: Runner | None = None) -> WarpcorrectOutputs:
    """
    warpcorrect

    Replaces voxels in a deformation field that point to a specific out of bounds location with nan,nan,nan.

This can be used in conjunction with the warpinit command to compute a MRtrix compatible deformation field from non-linear transformations generated by any other registration package.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    warpcorrect_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WARPCORRECT_METADATA)
    execution.params(params)
    args = warpcorrect_cargs(params, execution)
    out = warpcorrect_outputs(params, execution)
    execution.run(args)
    return out

def warpcorrect(
    in_: InputPathType,
    out: str,
    marker: list[float] | None = None,
    tolerance: float | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[WarpcorrectConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> WarpcorrectOutputs:
    """
    warpcorrect

    Replaces voxels in a deformation field that point to a specific out of bounds location with nan,nan,nan.

This can be used in conjunction with the warpinit command to compute a MRtrix compatible deformation field from non-linear transformations generated by any other registration package.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_: the input warp image.
        out: the output warp image.
        marker: single value or a comma separated list of values that define out of\
            bounds voxels in the input warp image. Default: (0,0,0).
        tolerance: numerical precision used for L2 matrix norm comparison. Default:\
            9.99999975e-06.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = warpcorrect_params(
        in_=in_,
        out=out,
        marker=marker,
        tolerance=tolerance,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return warpcorrect_execute(params, runner)

__all__ = [
    "Warpcorrect",
    "WarpcorrectOutputs",
    "WARPCORRECT_METADATA",
    "warpcorrect_cargs",
    "warpcorrect_outputs",
    "warpcorrect_params",
    "warpcorrect_execute",
    "warpcorrect_validate",
    "warpcorrect",
]