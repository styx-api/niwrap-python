# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRGRID_METADATA = Metadata(
    id="mrgrid",
    name="mrgrid",
    package="mrtrix",
)

class MrgridAxis(typing.TypedDict):
    index: int
    """pad or crop the input image along the provided axis (defined by index). The specifier argument defines the number of voxels added or removed on the lower or upper end of the axis (-axis index delta_lower,delta_upper) or acts as a voxel selection range (-axis index start:stop). In both modes, values are relative to the input image (overriding all other extent-specifying options). Negative delta specifier values trigger the inverse operation (pad instead of crop and vice versa) and negative range specifier trigger padding. Note that the deprecated commands 'mrcrop' and 'mrpad' used range-based and delta-based -axis indices, respectively."""
    spec: str
    """pad or crop the input image along the provided axis (defined by index). The specifier argument defines the number of voxels added or removed on the lower or upper end of the axis (-axis index delta_lower,delta_upper) or acts as a voxel selection range (-axis index start:stop). In both modes, values are relative to the input image (overriding all other extent-specifying options). Negative delta specifier values trigger the inverse operation (pad instead of crop and vice versa) and negative range specifier trigger padding. Note that the deprecated commands 'mrcrop' and 'mrpad' used range-based and delta-based -axis indices, respectively."""

MrgridVariousString = typing.TypedDict(
    "MrgridVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

MrgridVariousFile = typing.TypedDict(
    "MrgridVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

MrgridStrides = MrgridVariousString | MrgridVariousFile

class MrgridConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrgrid = typing.TypedDict(
    "Mrgrid",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrgrid"]],
        "template": typing.NotRequired[InputPathType],
        "size": typing.NotRequired[list[int]],
        "voxel": typing.NotRequired[list[float]],
        "scale": typing.NotRequired[list[float]],
        "interp": typing.NotRequired[str],
        "oversample": typing.NotRequired[list[int]],
        "as": typing.NotRequired[InputPathType],
        "uniform": typing.NotRequired[int],
        "mask": typing.NotRequired[InputPathType],
        "crop_unbound": typing.NotRequired[bool],
        "axis": typing.NotRequired[list[MrgridAxis]],
        "all_axes": typing.NotRequired[bool],
        "fill": typing.NotRequired[float],
        "strides": typing.NotRequired[MrgridStrides],
        "datatype": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrgridConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "operation": str,
        "output": str,
    },
)

@dataclasses.dataclass
class MrgridOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image."""

def mrgrid_params(
    input_: InputPathType,
    operation: str,
    output: str,
    template: InputPathType | None = None,
    size: list[int] | None = None,
    voxel: list[float] | None = None,
    scale: list[float] | None = None,
    interp: str | None = None,
    oversample: list[int] | None = None,
    as_: InputPathType | None = None,
    uniform: int | None = None,
    mask: InputPathType | None = None,
    crop_unbound: bool = False,
    axis: list[MrgridAxis] | None = None,
    all_axes: bool = False,
    fill: float | None = None,
    strides: MrgridStrides | None = None,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrgridConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrgrid:
    """
    Build parameters.

    Args:
        input_: input image to be regridded.
        operation: the operation to be performed, one of: regrid, crop, pad.
        output: the output image.
        template: match the input image grid (voxel spacing, image size, header\
            transformation) to that of a reference image. The image resolution\
            relative to the template image can be changed with one of -size,\
            -voxel, -scale.
        size: define the size (number of voxels) in each spatial dimension for the\
            output image. This should be specified as a comma-separated list.
        voxel: define the new voxel size for the output image. This can be\
            specified either as a single value to be used for all spatial\
            dimensions, or as a comma-separated list of the size for each voxel\
            dimension.
        scale: scale the image resolution by the supplied factor. This can be\
            specified either as a single value to be used for all dimensions, or as\
            a comma-separated list of scale factors for each dimension.
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: cubic).
        oversample: set the amount of over-sampling (in the target space) to\
            perform when regridding. This is particularly relevant when downsamping\
            a high-resolution image to a low-resolution image, to avoid aliasing\
            artefacts. This can consist of a single integer, or a comma-separated\
            list of 3 integers if different oversampling factors are desired along\
            the different axes. Default is determined from ratio of voxel\
            dimensions (disabled for nearest-neighbour interpolation).
        as_: pad or crop the input image on the upper bound to match the specified\
            reference image grid. This operation ignores differences in image\
            transformation between input and reference image.
        uniform: pad or crop the input image by a uniform number of voxels on all\
            sides
        mask: crop the input image according to the spatial extent of a mask image.\
            The mask must share a common voxel grid with the input image but\
            differences in image transformations are ignored. Note that even though\
            only 3 dimensions are cropped when using a mask, the bounds are\
            computed by checking the extent for all dimensions. Note that by\
            default a gap of 1 voxel is left at all edges of the image to allow\
            valid trilinear interpolation. This gap can be modified with the\
            -uniform option but by default it does not extend beyond the FOV unless\
            -crop_unbound is used.
        crop_unbound: Allow padding beyond the original FOV when cropping.
        axis: pad or crop the input image along the provided axis (defined by\
            index). The specifier argument defines the number of voxels added or\
            removed on the lower or upper end of the axis (-axis index\
            delta_lower,delta_upper) or acts as a voxel selection range (-axis\
            index start:stop). In both modes, values are relative to the input\
            image (overriding all other extent-specifying options). Negative delta\
            specifier values trigger the inverse operation (pad instead of crop and\
            vice versa) and negative range specifier trigger padding. Note that the\
            deprecated commands 'mrcrop' and 'mrpad' used range-based and\
            delta-based -axis indices, respectively.
        all_axes: Crop or pad all, not just spatial axes.
        fill: Use number as the out of bounds value. nan, inf and -inf are valid\
            arguments. (Default: 0.0)
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrgrid = {
        "@type": "mrtrix/mrgrid",
        "input": input_,
        "operation": operation,
        "output": output,
        "crop_unbound": crop_unbound,
        "all_axes": all_axes,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if template is not None:
        params["template"] = template
    if size is not None:
        params["size"] = size
    if voxel is not None:
        params["voxel"] = voxel
    if scale is not None:
        params["scale"] = scale
    if interp is not None:
        params["interp"] = interp
    if oversample is not None:
        params["oversample"] = oversample
    if as_ is not None:
        params["as"] = as_
    if uniform is not None:
        params["uniform"] = uniform
    if mask is not None:
        params["mask"] = mask
    if axis is not None:
        params["axis"] = axis
    if fill is not None:
        params["fill"] = fill
    if strides is not None:
        params["strides"] = strides
    if datatype is not None:
        params["datatype"] = datatype
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrgrid_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrgrid."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("template", None) is not None:
        if not isinstance(params["template"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template` has the wrong type: Received `{type(params["template"])}` expected `InputPathType`')
    if params.get("size", None) is not None:
        if not isinstance(params["size"], list):
            raise StyxValidationError(f'`size` has the wrong type: Received `{type(params["size"])}` expected `list[int]`')
        for e in params["size"]:
            if not isinstance(e, int):
                raise StyxValidationError(f'`size` has the wrong type: Received `{type(e)}` expected `list[int]`')
    if params.get("voxel", None) is not None:
        if not isinstance(params["voxel"], list):
            raise StyxValidationError(f'`voxel` has the wrong type: Received `{type(params["voxel"])}` expected `list[float]`')
        for e_2 in params["voxel"]:
            if not isinstance(e_2, (float, int)):
                raise StyxValidationError(f'`voxel` has the wrong type: Received `{type(e_2)}` expected `list[float]`')
    if params.get("scale", None) is not None:
        if not isinstance(params["scale"], list):
            raise StyxValidationError(f'`scale` has the wrong type: Received `{type(params["scale"])}` expected `list[float]`')
        for e_3 in params["scale"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`scale` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("interp", None) is not None:
        if not isinstance(params["interp"], str):
            raise StyxValidationError(f'`interp` has the wrong type: Received `{type(params["interp"])}` expected `str`')
    if params.get("oversample", None) is not None:
        if not isinstance(params["oversample"], list):
            raise StyxValidationError(f'`oversample` has the wrong type: Received `{type(params["oversample"])}` expected `list[int]`')
        for e_4 in params["oversample"]:
            if not isinstance(e_4, int):
                raise StyxValidationError(f'`oversample` has the wrong type: Received `{type(e_4)}` expected `list[int]`')
    if params.get("as", None) is not None:
        if not isinstance(params["as"], (pathlib.Path, str)):
            raise StyxValidationError(f'`as` has the wrong type: Received `{type(params["as"])}` expected `InputPathType`')
    if params.get("uniform", None) is not None:
        if not isinstance(params["uniform"], int):
            raise StyxValidationError(f'`uniform` has the wrong type: Received `{type(params["uniform"])}` expected `int`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("crop_unbound", None) is not None:
        if not isinstance(params["crop_unbound"], bool):
            raise StyxValidationError(f'`crop_unbound` has the wrong type: Received `{type(params["crop_unbound"])}` expected `bool`')
    if params.get("axis", None) is not None:
        if not isinstance(params["axis"], list):
            raise StyxValidationError(f'`axis` has the wrong type: Received `{type(params["axis"])}` expected `list[MrgridAxis]`')
        for e_5 in params["axis"]:
            if not isinstance(e_5, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_5)}\'')
            if e_5.get("index", None) is None:
                raise StyxValidationError("`index` must not be None")
            if not isinstance(e_5["index"], int):
                raise StyxValidationError(f'`index` has the wrong type: Received `{type(e_5["index"])}` expected `int`')
            if e_5.get("spec", None) is None:
                raise StyxValidationError("`spec` must not be None")
            if not isinstance(e_5["spec"], str):
                raise StyxValidationError(f'`spec` has the wrong type: Received `{type(e_5["spec"])}` expected `str`')
    if params.get("all_axes", None) is not None:
        if not isinstance(params["all_axes"], bool):
            raise StyxValidationError(f'`all_axes` has the wrong type: Received `{type(params["all_axes"])}` expected `bool`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], (float, int)):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `float`')
    if params.get("strides", None) is not None:
        if not isinstance(params["strides"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["strides"])}\'')
        if "@type" not in params["strides"]:
            raise StyxValidationError("Params object is missing `@type`")
        if params["strides"]["@type"] not in ["VariousString", "VariousFile"]:
            raise StyxValidationError("Parameter `strides`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
        if params["strides"]["@type"] == "VariousString":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], str):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `str`')
        elif params["strides"]["@type"] == "VariousFile":
            if params["strides"].get("obj", None) is None:
                raise StyxValidationError("`obj` must not be None")
            if not isinstance(params["strides"]["obj"], (pathlib.Path, str)):
                raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["strides"]["obj"])}` expected `InputPathType`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrgridConfig]`')
        for e_6 in params["config"]:
            if not isinstance(e_6, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_6)}\'')
            if e_6.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_6["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_6["key"])}` expected `str`')
            if e_6.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_6["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_6["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrgrid_cargs(params: Mrgrid, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrgrid")
    v_0 = params.get("template")
    if v_0 is not None:
        cargs.append("-template")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("size")
    if v_1 is not None:
        cargs.append("-size")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    v_2 = params.get("voxel")
    if v_2 is not None:
        cargs.append("-voxel")
        cargs.append(",".join([str(item1) for item1 in v_2]))
    v_3 = params.get("scale")
    if v_3 is not None:
        cargs.append("-scale")
        cargs.append(",".join([str(item2) for item2 in v_3]))
    v_4 = params.get("interp")
    if v_4 is not None:
        cargs.append("-interp")
        cargs.append(v_4)
    v_5 = params.get("oversample")
    if v_5 is not None:
        cargs.append("-oversample")
        cargs.append(",".join([str(item3) for item3 in v_5]))
    v_6 = params.get("as")
    if v_6 is not None:
        cargs.append("-as")
        cargs.append(execution.input_file(v_6))
    v_7 = params.get("uniform")
    if v_7 is not None:
        cargs.append("-uniform")
        cargs.append(str(v_7))
    v_8 = params.get("mask")
    if v_8 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_8))
    if params.get("crop_unbound"):
        cargs.append("-crop_unbound")
    v_9 = params.get("axis")
    if v_9 is not None:
        for item4 in v_9:
            cargs.append("-axis")
            cargs.append(str(item4["index"]))
            cargs.append(item4["spec"])
    if params.get("all_axes"):
        cargs.append("-all_axes")
    v_10 = params.get("fill")
    if v_10 is not None:
        cargs.append("-fill")
        cargs.append(str(v_10))
    v_11 = params.get("strides")
    if v_11 is not None:
        cargs.append("-strides")
        if v_11["@type"] == "VariousString":
            cargs.append(str(v_11["obj"]))
        elif v_11["@type"] == "VariousFile":
            cargs.append(str(v_11["obj"]))
    v_12 = params.get("datatype")
    if v_12 is not None:
        cargs.append("-datatype")
        cargs.append(v_12)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_13 = params.get("nthreads")
    if v_13 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_13))
    v_14 = params.get("config")
    if v_14 is not None:
        for item5 in v_14:
            cargs.append("-config")
            cargs.append(item5["key"])
            cargs.append(item5["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    cargs.append(params["operation"])
    cargs.append(params["output"])
    return cargs

def mrgrid_outputs(params: Mrgrid, execution: Execution) -> MrgridOutputs:
    """Build the MrgridOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MrgridOutputs(
        root=root_v,
        output=output_v,
    )

def mrgrid_execute(params: Mrgrid, runner: Runner | None = None) -> MrgridOutputs:
    """
    mrgrid

    Modify the grid of an image without interpolation (cropping or padding) or by regridding to an image grid with modified orientation, location and or resolution. The image content remains in place in real world coordinates..

- regrid: This operation performs changes of the voxel grid that require interpolation of the image such as changing the resolution or location and orientation of the voxel grid. If the image is down-sampled, the appropriate smoothing is automatically applied using Gaussian smoothing unless nearest neighbour interpolation is selected or oversample is changed explicitly. The resolution can only be changed for spatial dimensions. 

- crop: The image extent after cropping, can be specified either manually for each axis dimensions, or via a mask or reference image. The image can be cropped to the extent of a mask. This is useful for axially-acquired brain images, where the image size can be reduced by a factor of 2 by removing the empty space on either side of the brain. Note that cropping does not extend the image beyond the original FOV unless explicitly specified (via -crop_unbound or negative -axis extent).

- pad: Analogously to cropping, padding increases the FOV of an image without image interpolation. Pad and crop can be performed simultaneously by specifying signed specifier argument values to the -axis option.

This command encapsulates and extends the functionality of the superseded commands 'mrpad', 'mrcrop' and 'mrresize'. Note the difference in -axis convention used for 'mrcrop' and 'mrpad' (see -axis option description).

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrgrid_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRGRID_METADATA)
    execution.params(params)
    args = mrgrid_cargs(params, execution)
    out = mrgrid_outputs(params, execution)
    execution.run(args)
    return out

def mrgrid(
    input_: InputPathType,
    operation: str,
    output: str,
    template: InputPathType | None = None,
    size: list[int] | None = None,
    voxel: list[float] | None = None,
    scale: list[float] | None = None,
    interp: str | None = None,
    oversample: list[int] | None = None,
    as_: InputPathType | None = None,
    uniform: int | None = None,
    mask: InputPathType | None = None,
    crop_unbound: bool = False,
    axis: list[MrgridAxis] | None = None,
    all_axes: bool = False,
    fill: float | None = None,
    strides: MrgridStrides | None = None,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrgridConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrgridOutputs:
    """
    mrgrid

    Modify the grid of an image without interpolation (cropping or padding) or by regridding to an image grid with modified orientation, location and or resolution. The image content remains in place in real world coordinates..

- regrid: This operation performs changes of the voxel grid that require interpolation of the image such as changing the resolution or location and orientation of the voxel grid. If the image is down-sampled, the appropriate smoothing is automatically applied using Gaussian smoothing unless nearest neighbour interpolation is selected or oversample is changed explicitly. The resolution can only be changed for spatial dimensions. 

- crop: The image extent after cropping, can be specified either manually for each axis dimensions, or via a mask or reference image. The image can be cropped to the extent of a mask. This is useful for axially-acquired brain images, where the image size can be reduced by a factor of 2 by removing the empty space on either side of the brain. Note that cropping does not extend the image beyond the original FOV unless explicitly specified (via -crop_unbound or negative -axis extent).

- pad: Analogously to cropping, padding increases the FOV of an image without image interpolation. Pad and crop can be performed simultaneously by specifying signed specifier argument values to the -axis option.

This command encapsulates and extends the functionality of the superseded commands 'mrpad', 'mrcrop' and 'mrresize'. Note the difference in -axis convention used for 'mrcrop' and 'mrpad' (see -axis option description).

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: input image to be regridded.
        operation: the operation to be performed, one of: regrid, crop, pad.
        output: the output image.
        template: match the input image grid (voxel spacing, image size, header\
            transformation) to that of a reference image. The image resolution\
            relative to the template image can be changed with one of -size,\
            -voxel, -scale.
        size: define the size (number of voxels) in each spatial dimension for the\
            output image. This should be specified as a comma-separated list.
        voxel: define the new voxel size for the output image. This can be\
            specified either as a single value to be used for all spatial\
            dimensions, or as a comma-separated list of the size for each voxel\
            dimension.
        scale: scale the image resolution by the supplied factor. This can be\
            specified either as a single value to be used for all dimensions, or as\
            a comma-separated list of scale factors for each dimension.
        interp: set the interpolation method to use when reslicing (choices:\
            nearest, linear, cubic, sinc. Default: cubic).
        oversample: set the amount of over-sampling (in the target space) to\
            perform when regridding. This is particularly relevant when downsamping\
            a high-resolution image to a low-resolution image, to avoid aliasing\
            artefacts. This can consist of a single integer, or a comma-separated\
            list of 3 integers if different oversampling factors are desired along\
            the different axes. Default is determined from ratio of voxel\
            dimensions (disabled for nearest-neighbour interpolation).
        as_: pad or crop the input image on the upper bound to match the specified\
            reference image grid. This operation ignores differences in image\
            transformation between input and reference image.
        uniform: pad or crop the input image by a uniform number of voxels on all\
            sides
        mask: crop the input image according to the spatial extent of a mask image.\
            The mask must share a common voxel grid with the input image but\
            differences in image transformations are ignored. Note that even though\
            only 3 dimensions are cropped when using a mask, the bounds are\
            computed by checking the extent for all dimensions. Note that by\
            default a gap of 1 voxel is left at all edges of the image to allow\
            valid trilinear interpolation. This gap can be modified with the\
            -uniform option but by default it does not extend beyond the FOV unless\
            -crop_unbound is used.
        crop_unbound: Allow padding beyond the original FOV when cropping.
        axis: pad or crop the input image along the provided axis (defined by\
            index). The specifier argument defines the number of voxels added or\
            removed on the lower or upper end of the axis (-axis index\
            delta_lower,delta_upper) or acts as a voxel selection range (-axis\
            index start:stop). In both modes, values are relative to the input\
            image (overriding all other extent-specifying options). Negative delta\
            specifier values trigger the inverse operation (pad instead of crop and\
            vice versa) and negative range specifier trigger padding. Note that the\
            deprecated commands 'mrcrop' and 'mrpad' used range-based and\
            delta-based -axis indices, respectively.
        all_axes: Crop or pad all, not just spatial axes.
        fill: Use number as the out of bounds value. nan, inf and -inf are valid\
            arguments. (Default: 0.0)
        strides: specify the strides of the output data in memory; either as a\
            comma-separated list of (signed) integers, or as a template image from\
            which the strides shall be extracted and used. The actual strides\
            produced will depend on whether the output image format can support it.
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrgrid_params(
        input_=input_,
        operation=operation,
        output=output,
        template=template,
        size=size,
        voxel=voxel,
        scale=scale,
        interp=interp,
        oversample=oversample,
        as_=as_,
        uniform=uniform,
        mask=mask,
        crop_unbound=crop_unbound,
        axis=axis,
        all_axes=all_axes,
        fill=fill,
        strides=strides,
        datatype=datatype,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrgrid_execute(params, runner)

__all__ = [
    "Mrgrid",
    "MrgridOutputs",
    "MRGRID_METADATA",
    "mrgrid_cargs",
    "mrgrid_outputs",
    "mrgrid_params",
    "mrgrid_execute",
    "mrgrid_validate",
    "mrgrid",
]