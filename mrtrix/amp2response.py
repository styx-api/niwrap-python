# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

AMP2RESPONSE_METADATA = Metadata(
    id="amp2response",
    name="amp2response",
    package="mrtrix",
)

class Amp2responseConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Amp2response = typing.TypedDict(
    "Amp2response",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/amp2response"]],
        "isotropic": typing.NotRequired[bool],
        "noconstraint": typing.NotRequired[bool],
        "directions": InputPathType,
        "shells": typing.NotRequired[list[float]],
        "lmax": typing.NotRequired[list[int]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Amp2responseConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "amps": InputPathType,
        "mask": InputPathType,
        "response": str,
    },
)

@dataclasses.dataclass
class Amp2responseOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    response: OutputPathType
    """the output zonal spherical harmonic coefficients"""

def amp2response_params(
    directions: InputPathType,
    amps: InputPathType,
    mask: InputPathType,
    response: str,
    isotropic: bool = False,
    noconstraint: bool = False,
    shells: list[float] | None = None,
    lmax: list[int] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Amp2responseConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Amp2response:
    """
    Build parameters.

    Args:
        directions: a 4D image containing the estimated fibre directions
        amps: the amplitudes image
        mask: the mask containing the voxels from which to estimate the response\
            function
        response: the output zonal spherical harmonic coefficients
        isotropic: estimate an isotropic response function (lmax=0 for all shells)
        noconstraint: disable the non-negativity and monotonicity constraints
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        lmax: specify the maximum harmonic degree of the response function to\
            estimate (can be a comma-separated list for multi-shell data)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Amp2response = {
        "@type": "mrtrix/amp2response",
        "directions": directions,
        "amps": amps,
        "mask": mask,
        "response": response,
        "isotropic": isotropic,
        "noconstraint": noconstraint,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if shells is not None:
        params["shells"] = shells
    if lmax is not None:
        params["lmax"] = lmax
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def amp2response_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Amp2response."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("isotropic", None) is not None:
        if not isinstance(params["isotropic"], bool):
            raise StyxValidationError(f'`isotropic` has the wrong type: Received `{type(params["isotropic"])}` expected `bool`')
    if params.get("noconstraint", None) is not None:
        if not isinstance(params["noconstraint"], bool):
            raise StyxValidationError(f'`noconstraint` has the wrong type: Received `{type(params["noconstraint"])}` expected `bool`')
    if params.get("directions", None) is None:
        raise StyxValidationError("`directions` must not be None")
    if not isinstance(params["directions"], (pathlib.Path, str)):
        raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("shells", None) is not None:
        if not isinstance(params["shells"], list):
            raise StyxValidationError(f'`shells` has the wrong type: Received `{type(params["shells"])}` expected `list[float]`')
        for e in params["shells"]:
            if not isinstance(e, (float, int)):
                raise StyxValidationError(f'`shells` has the wrong type: Received `{type(e)}` expected `list[float]`')
    if params.get("lmax", None) is not None:
        if not isinstance(params["lmax"], list):
            raise StyxValidationError(f'`lmax` has the wrong type: Received `{type(params["lmax"])}` expected `list[int]`')
        for e_2 in params["lmax"]:
            if not isinstance(e_2, int):
                raise StyxValidationError(f'`lmax` has the wrong type: Received `{type(e_2)}` expected `list[int]`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Amp2responseConfig]`')
        for e_3 in params["config"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_3["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_3["key"])}` expected `str`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("amps", None) is None:
        raise StyxValidationError("`amps` must not be None")
    if not isinstance(params["amps"], (pathlib.Path, str)):
        raise StyxValidationError(f'`amps` has the wrong type: Received `{type(params["amps"])}` expected `InputPathType`')
    if params.get("mask", None) is None:
        raise StyxValidationError("`mask` must not be None")
    if not isinstance(params["mask"], (pathlib.Path, str)):
        raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("response", None) is None:
        raise StyxValidationError("`response` must not be None")
    if not isinstance(params["response"], str):
        raise StyxValidationError(f'`response` has the wrong type: Received `{type(params["response"])}` expected `str`')

def amp2response_cargs(params: Amp2response, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("amp2response")
    if params.get("isotropic"):
        cargs.append("-isotropic")
    if params.get("noconstraint"):
        cargs.append("-noconstraint")
    v_0 = params.get("directions")
    if v_0 is not None:
        cargs.append("-directions")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("shells")
    if v_1 is not None:
        cargs.append("-shells")
        cargs.append(",".join([str(item0) for item0 in v_1]))
    v_2 = params.get("lmax")
    if v_2 is not None:
        cargs.append("-lmax")
        cargs.append(",".join([str(item1) for item1 in v_2]))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item2 in v_4:
            cargs.append("-config")
            cargs.append(item2["key"])
            cargs.append(item2["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["amps"]))
    cargs.append(execution.input_file(params["mask"]))
    cargs.append(execution.input_file(params["directions"]))
    cargs.append(params["response"])
    return cargs

def amp2response_outputs(params: Amp2response, execution: Execution) -> Amp2responseOutputs:
    """Build the Amp2responseOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    response_v: OutputPathType = execution.output_file(params["response"])
    return Amp2responseOutputs(
        root=root_v,
        response=response_v,
    )

def amp2response_execute(params: Amp2response, runner: Runner | None = None) -> Amp2responseOutputs:
    """
    amp2response

    Estimate response function coefficients based on the DWI signal in single-fibre voxels.

This command uses the image data from all selected single-fibre voxels concurrently, rather than simply averaging their individual spherical harmonic coefficients. It also ensures that the response function is non-negative, and monotonic (i.e. its amplitude must increase from the fibre direction out to the orthogonal plane).

If multi-shell data are provided, and one or more b-value shells are not explicitly requested, the command will generate a response function for every b-value shell (including b=0 if present).

References:

Smith, R. E.; Dhollander, T. & Connelly, A. Constrained linear least squares estimation of anisotropic response function for spherical deconvolution. ISMRM Workshop on Breaking the Barriers of Diffusion MRI, 23.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    amp2response_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(AMP2RESPONSE_METADATA)
    execution.params(params)
    args = amp2response_cargs(params, execution)
    out = amp2response_outputs(params, execution)
    execution.run(args)
    return out

def amp2response(
    directions: InputPathType,
    amps: InputPathType,
    mask: InputPathType,
    response: str,
    isotropic: bool = False,
    noconstraint: bool = False,
    shells: list[float] | None = None,
    lmax: list[int] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Amp2responseConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Amp2responseOutputs:
    """
    amp2response

    Estimate response function coefficients based on the DWI signal in single-fibre voxels.

This command uses the image data from all selected single-fibre voxels concurrently, rather than simply averaging their individual spherical harmonic coefficients. It also ensures that the response function is non-negative, and monotonic (i.e. its amplitude must increase from the fibre direction out to the orthogonal plane).

If multi-shell data are provided, and one or more b-value shells are not explicitly requested, the command will generate a response function for every b-value shell (including b=0 if present).

References:

Smith, R. E.; Dhollander, T. & Connelly, A. Constrained linear least squares estimation of anisotropic response function for spherical deconvolution. ISMRM Workshop on Breaking the Barriers of Diffusion MRI, 23.

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        directions: a 4D image containing the estimated fibre directions
        amps: the amplitudes image
        mask: the mask containing the voxels from which to estimate the response\
            function
        response: the output zonal spherical harmonic coefficients
        isotropic: estimate an isotropic response function (lmax=0 for all shells)
        noconstraint: disable the non-negativity and monotonicity constraints
        shells: specify one or more b-values to use during processing, as a\
            comma-separated list of the desired approximate b-values (b-values are\
            clustered to allow for small deviations). Note that some commands are\
            incompatible with multiple b-values, and will report an error if more\
            than one b-value is provided. WARNING: note that, even though the b=0\
            volumes are never referred to as shells in the literature, they still\
            have to be explicitly included in the list of b-values as provided to\
            the -shell option! Several algorithms which include the b=0 volumes in\
            their computations may otherwise return an undesired result.
        lmax: specify the maximum harmonic degree of the response function to\
            estimate (can be a comma-separated list for multi-shell data)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = amp2response_params(
        directions=directions,
        amps=amps,
        mask=mask,
        response=response,
        isotropic=isotropic,
        noconstraint=noconstraint,
        shells=shells,
        lmax=lmax,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return amp2response_execute(params, runner)

__all__ = [
    "Amp2response",
    "Amp2responseOutputs",
    "AMP2RESPONSE_METADATA",
    "amp2response_cargs",
    "amp2response_outputs",
    "amp2response_params",
    "amp2response_execute",
    "amp2response_validate",
    "amp2response",
]